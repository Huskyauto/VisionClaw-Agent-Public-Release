# Project Compass implementation

1. Add pure validation and bounded prompt rendering with behavior tests.
2. Add the tenant-scoped profile table and migration.
3. Add tenant-scoped read/replace API and extend the existing project tool.
4. Inject the Compass into the shared project context.
5. Add the Projects-page editor.
6. Run focused tests, schema checks, browser verification, and independent review.