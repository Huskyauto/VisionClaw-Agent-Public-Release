# Current Platform Totals

> **Generated release facts:** [release-facts.json](release-facts.json) is the
> machine-readable source for the current release. This page is a generated
> human-readable rendering; public metadata and count checks consume the same
> facts. Do not hand-edit either generated output.
>
> **Tool-count semantics:** **417 total registered tools** is the
> complete governed registry. **386 public documented tools**
> is the filtered public index and intentionally excludes trusted-persona-only
> tools. These are different valid metrics, not competing totals.

**Release evidence date:** 2026-09-06. Database-derived values are a
publication-time evidence snapshot generated by `scripts/refresh-totals.ts`
with maintainer database access; they are not reproducible from a fresh public
checkout. Fresh checkouts verify committed facts with `verify-counts.ts`.

---

## Current Release Facts

| Metric | Value | How it's verified |
|---|---|---|
| **Tools (total registered)** | **417** | Static unique-name extraction across `server/tools.ts` + migrated domain definitions |
| **Tools (public documented)** | **386** | Registered tools excluding `trustedPersonasOnly` policy entries |
| **AI agent personas (active)** | **18** | `SELECT count(*) FROM personas WHERE is_active=true` |
| **Skills (DB seeded)** | **63** | `SELECT count(*) FROM skills` |
| **Skills (total incl. .agents/skills/)** | **67** | DB count + 4 platform agent skills not yet seeded |
| **Capabilities (active)** | **136** | `SELECT count(*) FROM capabilities WHERE is_active=true` |
| **Database tables (declared)** | **165** | `rg -c "= pgTable("` in `shared/schema.ts` |
| **Database tables (live in `public` schema)** | **242** | `SELECT count(*) FROM information_schema.tables WHERE table_schema='public'` |
| **Governance rules** | **41** | `SELECT count(*) FROM governance_rules` |
| **Platform indexes (release aggregate)** | **743** | Live `pg_indexes` count minus versioned auth/session infrastructure exclusions |
| **Production indexes (non-PK operational probe)** | **507** | `SELECT count(*) FROM pg_indexes WHERE schemaname='public' AND indexname NOT LIKE '%_pkey'` |
| **AI providers** | **6** | OpenAI, Anthropic, Google, xAI, OpenRouter, Perplexity |
| **AI models (core registry)** | **80 curated** | `MODEL_REGISTRY.length` in `server/model-registry.ts` |

---

## Regeneration

Maintainers run `npx tsx scripts/refresh-totals.ts` after a release that changes
a measured registry or database count, with the production evidence database
available. Update the explicit release input before publication. Public
checkouts validate committed facts with `npx tsx scripts/verify-counts.ts`;
they do not require database access to reproduce this snapshot.
