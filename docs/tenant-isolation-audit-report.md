# Tenant-Isolation Audit Report

_Generated 2026-08-25T22:40:26.474Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **611/611** file(s) audited · chunks 58/58 OK (0 failed)
- Findings: **252** (CRITICAL 67 · HIGH 144 · MEDIUM 40 · LOW 1)
- Precision second-pass (`gpt-5.4`): **199** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (67)

- **server/agent-activity.ts:118** — completeActivity issues an UPDATE against agentActivity using only the activity id (WHERE eq(agentActivity.id, activityId)) with no tenant_id constraint. This allows updating rows belonging to other tenants if a caller supplies an arbitrary id.
  - _Fix:_ Add the tenant check to the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId} or use where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))). Validate the caller's tenant before mutating rows.
- **server/agentic-features.ts:1021** — classifyReply updates outreach_enrollments with: UPDATE outreach_enrollments SET ... WHERE id = ${row.id} — the UPDATE lacks a tenant_id predicate. Although the code fetched the row earlier with a tenant-scoped query, the UPDATE should still include tenant scoping to prevent accidental cross-tenant updates if ids are confused or rows moved.
  - _Fix:_ Always include tenant scope on UPDATEs (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to ensure the row still belongs to the caller's tenant and to avoid TOCTOU risks.
- **server/chat-engine.ts:4551** — Call to storage.attachCitations(messageId, collectedCitations) omits tenant context. This likely performs INSERTs against a tenant-scoped citations/messages-related table without explicitly providing tenant_id.
  - _Fix:_ Pass the tenant id explicitly to attachCitations (e.g. storage.attachCitations(messageId, collectedCitations, tenantIdForScope)) or ensure the implementation derives and sets tenant_id from a trusted source (message row) before inserting. Every INSERT into tenant-scoped tables must set tenant_id explicitly.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table `commitments` without a tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This writes without ensuring the row belongs to the intended tenant and can affect another tenant's row by id.
  - _Fix:_ Always include tenant scoping in UPDATE/DELETE statements. Change the WHERE to include tenant_id and any other guards, e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId} AND draft_status = 'draft_pending'. Prefer Drizzle's where(eq(table.tenantId, tenantId)) form to avoid accidental unscoped writes.
- **server/data-protection.ts:168** — DELETE FROM conversations executed without scoping by tenant_id. Even though conv was read earlier, the DELETE lacks an explicit WHERE tenant_id = <conv.tenant_id> predicate, allowing a row to be removed without the defense-in-depth tenant predicate.
  - _Fix:_ Include tenant predicate on the DELETE (e.g. WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or perform the purge inside a tenant-bound transaction (withTenantTx) so RLS / set_config enforces tenant isolation.
- **server/delivery-pipeline.ts:569** — updateDeliveryLog performs an UPDATE against the tenant-scoped table `delivery_logs` but the WHERE clause only filters by id (eq(deliveryLogs.id, id)) — no tenant_id constraint. This allows an attacker or buggy caller who can supply an id to update another tenant's delivery row.
  - _Fix:_ Require and check the tenant context for updates. Either accept tenantId as an argument to updateDeliveryLog and add it to the WHERE clause (e.g. where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))), or re-query/verify the row's tenant before updating. Ensure every caller supplies/derives the correct tenantId and fail if it cannot be verified.
- **server/delivery-pipeline.ts:575** — updateDeliveryLogWithBundleFiles updates `delivery_logs` and merges metadata but the WHERE clause only filters by id (eq(deliveryLogs.id, id)) — no tenant_id constraint. This allows modifying another tenant's delivery row (metadata and bundle state).
  - _Fix:_ Scope the update to the owning tenant. Add tenantId parameter and include eq(deliveryLogs.tenantId, tenantId) in the WHERE clause (or perform an ownership check before the update). Avoid updating tenant-scoped rows by id alone.
- **server/dfy-intake.ts:93** — submitDfyIntake updates `dfy_intake_forms` (.update(...).where(eq(dfyIntakeForms.id, form.id))) without constraining by tenant_id. This writes a tenant-scoped row based on a prior token lookup and can modify another tenant's row.
  - _Fix:_ Ensure updates include tenant ownership verification. Add tenant scoping to the WHERE clause (e.g. where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, expectedTenantId)))), or re-query the form with tenant scope before performing the UPDATE. If token-authorized updates are intended, explicitly validate the token-to-tenant binding and treat it as an authorization check.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed without constraining by tenant_id (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can delete chunks belonging to other tenants.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = ${tenantId}) or otherwise verify collection ownership before deleting. Prefer Drizzle-style filtering (where(eq(table.tenantId, tenantId))).
- **server/dream-consolidation.ts:83** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is run without also filtering by tenant_id. Reading messages by conversation_id alone risks returning messages from other tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${tenantId}) or otherwise verify the conversation's tenant ownership before selecting messages.
- **server/email.ts:529** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} performs a write without a tenant_id constraint. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant constraint in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or resolve and validate the tenant before performing the update.
- **server/embeddings.ts:767** — UPDATE agent_knowledge ... WHERE id = ${id} AND LEFT(title || ' ' || content, 6000) = ${expectedKnowledgeContent} updates agent_knowledge without constraining tenant_id. This can overwrite another tenant's knowledge row.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent ownership check) to the WHERE clause. Require the tenant context be passed into storeEmbeddingVec and validate it before writes.
- **server/embeddings.ts:778** — UPDATE ${sql.raw(table)} SET embedding_vec = ... WHERE id = ${id} updates tenant-scoped tables (memory_entries / agent_knowledge) by id only, with no tenant filter.
  - _Fix:_ Require and use a tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Avoid blind id-only updates across tenant-scoped tables.
- **server/embeddings.ts:811** — SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200 reads memory_entries without tenant scoping. This will enumerate rows across tenants.
  - _Fix:_ Restrict backfill queries to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise run only in an admin context with explicit authorization and audit. Prefer per-tenant backfills.
- **server/embeddings.ts:821** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without tenant filtering, allowing writes to other tenants' rows.
  - _Fix:_ Include tenant constraint (AND tenant_id = ${tenantId}) in the UPDATE. Ensure the backfill routine operates with an explicit tenant context or admin authorization.
- **server/embeddings.ts:827** — SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200 runs without tenant scoping, enumerating agent_knowledge across tenants.
  - _Fix:_ Scope backfill queries by tenant_id or require an explicit admin context. Add WHERE tenant_id = ${tenantId} where appropriate to prevent cross-tenant access.
- **server/embeddings.ts:838** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint, permitting cross-tenant writes.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or otherwise verify ownership before updating. Ensure store/backfill flows take an explicit tenant parameter and enforce it on every write.
- **server/felix-brain.ts:19** — rehydrateTaskStateOnBoot executes a global query against the tenant-scoped table `agent_runs` without constraining by tenant_id (SELECT ... FROM agent_runs WHERE status IN ...). This reads rows for all tenants and then rehydrates an in-memory taskStateStore keyed only by conversationId, enabling cross-tenant data exposure and mixing of tenant state.
  - _Fix:_ Restrict rehydration to a specific tenant or iterate per-tenant and always include a WHERE tenant_id = ${tenantId} filter in the query. Also make the in-memory store tenant-aware (e.g. key by `${tenantId}:${conversationId}` or include tenantId on TaskState) so state for different tenants cannot collide. If global rehydration for all tenants is required, enumerate tenants and perform a tenant-scoped query per tenant, or otherwise run the boot rehydration under a safely-scoped admin operation that still persists tenant separation in memory.
- **server/google-drive.ts:685** — UPDATE on tenant-scoped table 'projects' uses only projects.id in WHERE (no tenant_id filter). Although the code previously fetched the project and checked ownership, the UPDATE itself lacks a tenant constraint and could be abused if ownership validation is bypassed or the row id is from an unexpected source.
  - _Fix:_ Add tenant scoping to the UPDATE statement (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or use an assertProjectInTenant helper and include that tenant_id equality in the WHERE to make the write atomic and fail-closed. Ensure every write to tenant-scoped tables includes tenant_id in the WHERE clause.
- **server/job-queue.ts:98** — claimDueJobs() issues an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) that contains no tenant_id constraint — reads and writes agent_jobs across all tenants unscoped.
  - _Fix:_ Scope claims to a tenant (e.g. accept a tenantId parameter and include WHERE tenant_id = ${tenantId}), or otherwise enforce per-tenant claiming semantics. If the worker must operate multi-tenant, document and harden the access model (audit + least-privilege) and ensure no cross-tenant side effects happen.
- **server/lib/ideabrowser-dev-sync.ts:140** — INSERT into project_notes omits tenant scoping and the NOT EXISTS dedupe check is not tenant-scoped. This can insert or dedupe across tenants (cross-tenant leakage/poisoning).
  - _Fix:_ Include tenant_id in the INSERT (and values) and add tenant_id = ${tenantId} to the NOT EXISTS subquery so both the insert and the existence check are scoped to the caller's tenant. Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projectId}, ${rankingNote}, ...) and WHERE NOT EXISTS (SELECT 1 FROM project_notes WHERE tenant_id = ${tenantId} AND project_id = ${projectId} AND author = ...).
- **server/lib/ideabrowser-dev-sync.ts:151** — INSERT into project_files omits tenant scoping and the NOT EXISTS dedupe check is not tenant-scoped. This can create cross-tenant file rows or allow one tenant to block another's insertions.
  - _Fix:_ Include tenant_id in the INSERT and constrain the NOT EXISTS check to the tenant. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_url, ...) VALUES (${tenantId}, ${projectId}, ${fileName}, ${fileUrl}, ...) WHERE NOT EXISTS (SELECT 1 FROM project_files WHERE tenant_id = ${tenantId} AND project_id = ${projectId} AND file_name = ${fileName}).
- **server/lib/inbox-ingest.ts:506** — UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id} lacks tenant scoping. Without AND tenant_id = ${tenantId} this UPDATE could modify another tenant's classification row that shares the same id.
  - _Fix:_ Add tenant scoping to the UPDATE predicate (AND tenant_id = ${tenantId}) so the update only touches rows for the caller's tenant. Example: UPDATE inbox_classifications SET routed_to = ${...} WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:253** — INSERT INTO memory_links omits tenant_id. Tenant-scoped tables require tenant_id explicitly on INSERT; missing tenant_id will create links with no tenant or in wrong tenant.
  - _Fix:_ Include tenant_id in the INSERT column list and values, e.g. INSERT INTO memory_links (source_memory_id, target_memory_id, tenant_id, link_type, ...) VALUES (${newMemoryId}, ${target.id}, ${tenantId}, ${linkType}, ...). Ensure tenantId is a required argument for this function.
- **server/oauth-subscriptions.ts:906** — Query selects from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id: `SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE` (startup validation reads all tenants' OAuth subscriptions).
  - _Fix:_ Restrict this query to a single tenant (WHERE tenant_id = <tenant>) or explicitly iterate a validated list of tenant IDs in a controlled admin flow. Never perform broad SELECTs across tenant-scoped tables in code paths that assume tenant isolation; if this is intended as an administrative multi-tenant sweep, ensure it's only callable by a hardened admin path and log/audit the operation.
- **server/oauth-subscriptions.ts:915** — Query selects from tenant-scoped table `oauth_subscriptions` without tenant filter: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'`. This returns rows for all tenants.
  - _Fix:_ Add a tenant_id WHERE clause or otherwise scope results to an explicit, validated tenant. If the code intends to perform multi-tenant enumeration, move it behind an explicit, auditable admin operation and document the justification.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh() runs `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant constraint, thus reading subscription rows for all tenants.
  - _Fix:_ Either pass a specific tenantId into proactive refresh and filter WHERE tenant_id = ${tenantId}, or implement an explicit, auditable admin multi-tenant loop that enumerates tenant IDs and performs per-tenant queries. Avoid unscoped queries against tenant-scoped tables in normal service code.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions() runs `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE` without tenant scoping, exposing all tenants' subscriptions.
  - _Fix:_ Do not perform unscoped reads on tenant-scoped tables. If a global forced-refresh is required, perform an explicit, secured admin-level enumeration of tenant IDs and then run per-tenant queries using WHERE tenant_id = ${tenantId} to maintain the invariant that every operation is constrained to a known tenant.
- **server/persona-export.ts:33** — exportPersona() queries `personas` by id without scoping to tenant: `SELECT * FROM personas WHERE id = ${personaId}`. The function accepts a tenantId but does not verify the persona row belongs to that tenant before exporting.
  - _Fix:_ Constrain the lookup to the caller's tenant (e.g. `WHERE id = ${personaId} AND tenant_id = ${tenantId}`) and return an error if no matching row is found. Always verify ownership of referenced rows when foreign IDs come from callers.
- **server/plan-executor.ts:513** — Claiming a plan: UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status='approved' RETURNING ... does not constrain by tenant_id. This can cause cross-tenant claiming/execution of plans.
  - _Fix:_ Include tenant scoping in the CAS/claim step (e.g. add AND tenant_id = ${callerTenantId}) or require the caller to pass an explicit tenantId and use it in the WHERE. Always verify the plan belongs to the expected tenant before transitioning status or executing any steps.
- **server/plan-executor.ts:728** — Final status update on plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks tenant_id constraint — allows finalizing plans across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or require the function to accept/derive the tenant and enforce it. Consider centralizing plan updates to helpers that always enforce tenant scoping.
- **server/plan-executor.ts:778** — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' with no tenant filtering: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This operates across all tenants.
  - _Fix:_ Do not perform a global, cross-tenant status sweep. Either run recovery per-tenant (require a tenantId argument and restrict WHERE tenant_id = ${tenantId}) or restrict this operation to an explicit admin/system tenant only. Ensure the operation cannot silently affect other tenants' execution state.
- **server/plan-executor.ts:787** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is executed without tenant scoping and is used to kick off execution (executePlan) for each returned id — this can start plans from any tenant.
  - _Fix:_ Query pending/approved plans in a tenant-scoped manner (e.g. WHERE status = 'approved' AND tenant_id = ${tenantId}) or iterate tenants explicitly and queue only that tenant's plans. Ensure any scheduling/dispatch logic always carries and enforces tenant identity before calling executePlan.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates by id only and omits tenant_id. This is a mutating write on a tenant-scoped table without enforcing tenant ownership — risk of disabling another tenant's task if ids collide or are guessed.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or otherwise validate the task belongs to the tenant before updating.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} performs a write by id only, with no tenant_id filter. agent_desks are tenant-scoped and this can modify another tenant's desk row.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or perform an ownership check before mutating.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance_rules by id only and omits tenant scoping. This allows incrementing trigger_count on a rule from another tenant if ids overlap.
  - _Fix:_ Include tenant filter on the UPDATE (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or verify the rule belongs to the tenant prior to updating.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project by id without tenant scoping or ownership verification. project rows are tenant-scoped; using projectId alone can expose another tenant's project data and lead to writing that data to a shared filesystem file.
  - _Fix:_ Require the caller's tenant context and constrain the SELECT with AND tenant_id = ${tenantId} (or fetch tenant_id from context and validate). If the function is used in a background job, accept/derive tenantId and only operate on projects belonging to that tenant.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (...) has no tenant filtering; it enumerates projects and conversations across all tenants and then writes per-project brains into a single shared directory.
  - _Fix:_ Run backfill scoped per-tenant (accept a tenantId parameter) or add tenant constraints (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}), and ensure files are segregated by tenant or stored in tenant-scoped locations.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed without tenant filtering; conversations are tenant-scoped and may return rows from other tenants' projects if project ids collide or are not validated.
  - _Fix:_ Constrain conversation lookup to the tenant (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or validate that proj.id belongs to the tenant before enumerating conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} runs without tenant restriction. messages are tenant-scoped and contain sensitive assistant/user content; reading them across tenants violates isolation and can write cross-tenant content to disk.
  - _Fix:_ Add tenant scoping (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) and ensure the conversation/project ownership is validated prior to reading messages. For backfill, run per-tenant to avoid mixing tenants' message data.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table agent_knowledge omits tenant_id constraint (UPDATE agent_knowledge SET ... WHERE id = ${r.id}). This allows modifying rows belonging to other tenants when IDs collide or if the runner is scanning globally.
  - _Fix:_ Include the tenant scope in the WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or otherwise ensure the update uses a trusted tenantId variable tied to the row before writing. Parameterize tenant_id instead of string interpolation and ensure the runner performs writes only under an explicit tenant context.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() executes without tenant_id scoping. agent_knowledge is tenant-scoped elsewhere; this DELETE will remove rows across all tenants.
  - _Fix:_ Restrict the DELETE to the session's tenant (e.g. DELETE ... WHERE source = 'autoresearch' AND expires_at < NOW() AND tenant_id = ${tenantId}) or perform per-tenant deletes explicitly. Ensure any global maintenance job enumerates tenants and operates per-tenant with parameterized tenant_id.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions sets conversation_id without constraining by tenant_id (WHERE id = ${session.id} only). This can update a session belonging to another tenant if the id is misused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g., WHERE id = ${session.id} AND tenant_id = ${tenantId}) or include tenant_id in the query parameters so every update to sculptor_sessions is constrained to the caller's tenant.
- **server/sculptor.ts:174** — UPDATE sculptor_sessions updates status/result/tool_calls_count/tokens_used/completed_at with WHERE id = ${sessionId} but no tenant_id filter. This allows cross-tenant modification of sculptor_sessions rows.
  - _Fix:_ Restrict the UPDATE by tenant (e.g., WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or perform the update inside a transaction that verifies tenant ownership before writing.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result ... WHERE id = ${sessionId} executes without a tenant_id constraint. This can overwrite another tenant's session review_result.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise verify the session belongs to the tenant earlier) before performing the UPDATE.
- **server/second-opinion.ts:405** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} updates cost/billing data by id only, with no tenant_id constraint. A reservationId could be misused to alter another tenant's ledger row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g., WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or pass/verify tenant_id when settling reservations to ensure only the owning tenant's ledger row is modified.
- **server/second-opinion.ts:426** — UPDATE agent_cost_ledger SET cost_usd='0', operation='second_opinion:released' WHERE id = ${reservationId} does not constrain by tenant_id, permitting cross-tenant ledger modification.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or otherwise verify the reservation's tenant before releasing it.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' queries heartbeat_tasks without tenant_id. If heartbeat_tasks is tenant-scoped this reads across tenants.
  - _Fix:_ Scope the SELECT by tenant (e.g., WHERE tenant_id = ${tenantId} AND enabled = true AND type = 'delegation') or perform per-tenant iterations explicitly; avoid global reads on tenant-scoped tables.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks without a tenant_id filter, potentially disabling delegation tasks across all tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g., AND tenant_id = ${tenantId}) or run the reconciler per-tenant and supply the tenant_id explicitly to avoid cross-tenant changes.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' queries a tenant-scoped table without tenant_id, allowing cross-tenant reads.
  - _Fix:_ Include tenant_id in the WHERE clause or otherwise limit the query to the intended tenant (e.g., tenant_id = ${ADMIN_TENANT_ID} if admin-only), and avoid unscoped scans of tenant-scoped tables.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true executes without tenant_id, which may disable tasks for all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or ADMIN_TENANT_ID as appropriate) to the WHERE clause so the UPDATE only affects the intended tenant's rows.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) performs an unscoped query against heartbeat_tasks (no tenant_id), enabling cross-tenant inspection.
  - _Fix:_ Constrain the SELECT with tenant_id (e.g., tenant_id = ${ADMIN_TENANT_ID} or iterate tenants explicitly) to ensure tenant isolation for reads.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true updates heartbeat_tasks without a tenant_id filter, risking cross-tenant modifications.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g., AND tenant_id = ${tenantId} or limit to ADMIN_TENANT_ID) so only the intended tenant rows are modified; avoid global updates on tenant-scoped tables.
- **server/seed.ts:2165** — UPDATE on tenant-scoped table research_programs without tenant constraint (UPDATE research_programs SET model = ... WHERE id = ...). Updating by id only can modify another tenant's row with the same id.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} AND tenant_id = ${tenantId}. Prefer validating ownership before mutation and/or using combined (tenant_id, id) checks to prevent cross-tenant modification.
- **server/seed.ts:3910** — DELETE FROM tenant-scoped table `heartbeat_tasks` executed without a tenant filter. This DELETE will remove matching rows across all tenants.
  - _Fix:_ Restrict destructive operations to a specific tenant: add AND tenant_id = <tenantId> to the WHERE clause (or explicitly loop tenants). If the cleanup should only affect the system/admin tenant, use tenant_id = 1.
- **server/self-improvement.ts:401** — UPDATE against tenant-scoped table `experiments` uses .where(eq(experiments.id, experiment.id)) with no tenant_id constraint. This allows updating an experiment row by id without verifying tenant ownership.
  - _Fix:_ Include the tenant filter in the UPDATE to enforce tenant isolation, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Alternatively, fetch the row with a tenant-scoped SELECT first and fail if it doesn't belong to the caller before updating.
- **server/self-improvement.ts:415** — Error-path UPDATE on `experiments` sets status/outcome with .where(eq(experiments.id, experiment.id)) but omits a tenant_id constraint, allowing cross-tenant modification in failure handling.
  - _Fix:_ As with the normal update, include tenant ownership in the WHERE clause for all UPDATEs (and DELETEs). Use .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) or otherwise validate the experiment belongs to the caller before mutating it.
- **server/storage.ts:1367** — getAllDataForExport performs platform-wide SELECTs over tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with no tenant scoping — returns all tenants' data.
  - _Fix:_ Restrict export API to admin-only callers and enforce server-side tenant scoping: require an explicit tenantId parameter or an admin authorization check before allowing platform-wide exports. If the intent is per-tenant export, add eq(...tenantId...) predicates (or require ALL_TENANTS sentinel only for trusted admin paths). Log and require explicit approval for any all-tenant dump.
- **server/surprise-scorer.ts:93** — UPDATE to tenant-scoped table 'felix_proposals' performed without scoping by tenant_id (args_embedding stamp).
  - _Fix:_ Accept/require the proposal's tenantId (or load the row scoped to tenant first) and include a tenant filter on the UPDATE, e.g. UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL. Alternatively change stampArgsEmbedding to take tenantId and use it in the WHERE clause.
- **server/surprise-scorer.ts:139** — UPDATE actual_outcome_embedding on tenant-scoped table 'felix_proposals' executed without a tenant_id constraint (WHERE id = ${proposalId} only).
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Ensure tenantId is passed into this function or validated from a prior tenant-scoped select.
- **server/surprise-scorer.ts:181** — UPDATE setting surprise_band = 'no_history' on felix_proposals omits tenant_id in WHERE clause (WHERE id = ${proposalId} only).
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Ensure tenantId used is authoritative for the caller.
- **server/surprise-scorer.ts:212** — UPDATE setting surprise_score and surprise_band on felix_proposals omits tenant_id in WHERE clause (WHERE id = ${proposalId} only).
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Verify tenantId is provided and trusted before writing.
- **server/tool-learning.ts:291** — UPDATE custom_tools SET implementation = fixedImpl WHERE id = ${tool.id} performed without tenant_id constraint — this mutates tenant-scoped data without enforcing tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure tryAndRevert obtains/accepts tenantId so the update cannot affect another tenant's row.
- **server/tool-learning.ts:316** — DELETE FROM custom_tools WHERE id = ${tool.id} executed without tenant_id constraint in revertToolRow — deletes tenant-scoped data without verifying tenant ownership.
  - _Fix:_ Require and use tenantId when deleting: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure revertToolRow accepts/derives the tenantId for the tool being reverted and uses it in the WHERE clause.
- **server/tool-learning.ts:365** — UPDATE custom_tools to increment usageCount uses only id in WHERE (eq(custom_tools.id, tool.id)) and lacks tenant_id — a mutating write to a tenant-scoped table without tenant scoping.
  - _Fix:_ Constrain the UPDATE with tenant_id, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Pass tenantId into executeCustomTool and use it for all reads/writes against custom_tools.
- **server/venture-discovery/budget.ts:155** — UPDATE on tenant-scoped table `agent_cost_ledger` uses WHERE id = ${reservationId} with NO tenant_id constraint (settleVentureReservation). This can modify rows belonging to other tenants if reservationId is guessed or leaked, breaking tenant isolation and integrity.
  - _Fix:_ Require and use tenant context when settling reservations. Change settleVentureReservation to accept tenantId (or validate the reservation's tenant in the same transaction) and UPDATE ... WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Prefer doing this inside the same tx that created/locked the reservation to ensure atomic ownership checks.
- **server/venture-discovery/budget.ts:170** — UPDATE on tenant-scoped table `agent_cost_ledger` uses WHERE id = ${reservationId} with NO tenant_id constraint (releaseVentureReservation). This can release/alter another tenant's reservation by id alone.
  - _Fix:_ Require tenant context and constrain the UPDATE by tenant_id. Change releaseVentureReservation to accept tenantId (or verify ownership in a transaction) and run UPDATE ... WHERE id = ${reservationId} AND tenant_id = ${tenantId} so only the owning tenant's reservation may be modified.

### HIGH (144)

- **server/ab-optimizer.ts:110** — runDueAbExperiments performs a SELECT on experiments without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads experiments across all tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId} or accept a tenantId arg and use eq(experiments.tenantId, tenantId)). If the intent is a global sweep, make it explicit and audited (and ensure callers are allowed), otherwise restrict to the caller's tenant.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting selects from ai_insights without constraining tenant_id (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50). This reads insights across tenants.
  - _Fix:_ Scope the SELECT by tenant_id (require a tenantId param and add WHERE tenant_id = ${tenantId}) or make the cross-tenant nature explicit and gated to an admin-only operation. Always validate tenant ownership before using or routing rows.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights performs an UPDATE on ai_insights that, when called with tenantId undefined, runs across all tenants (WHERE status = 'new' AND category IN (...) ${tenantScope}). Unscoped UPDATE on tenant-scoped table can mutate rows for other tenants.
  - _Fix:_ Require an explicit tenantId for this operation or, if global sweeps are intended, restrict the function to admin-only callers and add strong audit/logging. Prefer per-tenant scoped updates (WHERE tenant_id = ${tenantId}) to enforce isolation.
- **server/agentic-features.ts:861** — enrollInSequence checks for existing enrollments using: SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' — this query omits tenant_id, allowing cross-tenant probing/duplicate-detection across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${params.tenantId}) to ensure existence checks are limited to the caller's tenant before inserting/updating enrollment rows.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to set status='expired' for pending approvals whose expiresAt < now(), but the UPDATE has no tenant_id filter. This updates approvals across all tenants in a single statement.
  - _Fix:_ Scope the expiry UPDATE to a tenant (require tenantId parameter and add WHERE tenant_id = ${tenantId} AND ...), or if a multi-tenant sweep is intended, ensure the routine is strictly admin-only, audited, and that subsequent per-row actions validate tenant ownership before touching tenant-scoped resources.
- **server/agentic/resume-worker.ts:65** — Unscoped cross-tenant SELECT on tenant-scoped tables: the candidate query (db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`)) reads agent_runs / agent_approvals without constraining to a single tenant (no WHERE r.tenant_id = <tenantId>). This allows a global worker to enumerate/observe rows across tenants.
  - _Fix:_ Scope the query to a specific tenant or otherwise enforce proper authorization. Options: (a) add an explicit tenant filter (e.g. WHERE r.tenant_id = ${tenantId} AND ...), running this sweep per-tenant; (b) if a global admin sweep is intended, require and assert an explicit admin privilege/flag and document it, and minimize returned columns; or (c) move multi-tenant scanning to an admin-only process that uses an audited admin tenant context. In all cases ensure the tenant id is passed as a bound parameter (use drizzle's parameterization) and never construct tenant-scoped predicates with raw/untrusted interpolation.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on agent_wake_schedules (claiming due wakes) uses an inner SELECT that lacks any tenant_id constraint — the statement can claim/modify rows across tenants.
  - _Fix:_ Scope the claim to a single tenant per run. Either accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to the inner SELECT (and outer UPDATE), or enumerate tenants and run the claim per-tenant. If this is intended as a cross-tenant system job, make that explicit and restrict execution to a platform-admin/maintenance context with audit logging.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT FROM conversations is executed without a tenant_id filter — this iterates over conversations from all tenants.
  - _Fix:_ Constrain the backfill to a tenant (add WHERE c.tenant_id = ${tenantId} or accept a tenantId parameter) or iterate tenants and run the backfill per-tenant. Avoid scanning tenant-scoped tables across all tenants in a single query unless explicitly run as a guarded platform maintenance job.
- **server/auth.ts:1239** — Global maintenance DELETE: DELETE FROM auth_sessions WHERE expires_at < ${now} is executed without tenant_id scoping, affecting auth_sessions across all tenants.
  - _Fix:_ Run expired-session cleanup per-tenant (iterate tenants or accept a tenant argument) or restrict this operation to a platform-admin maintenance path. If cross-tenant cleanup is required, ensure it is executed from a privileged/admin context and documented as such with appropriate controls and audit logs.
- **server/auth.ts:1244** — DELETE FROM auth_sessions (clearAllSessions) removes all sessions globally with no tenant constraint. This is a cross-tenant mutation with no tenant scoping in SQL.
  - _Fix:_ Limit this operation to platform-admin-only code paths and/or require an explicit tenant_id parameter to restrict scope. If global clearing is needed for platform-level operations, ensure the function is protected by a requirePlatformAdmin check and clearly documented as a privileged operation.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` lacks a tenant_id constraint in the WHERE clause (`WHERE id = ${pending.reviewId} AND reused = false`). This allows changing rows across tenants by id alone.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. add AND tenant_id = ${ctxTenantId} or pass the tenantId into markReviewReused) or otherwise verify ownership of the review id before mutating. Prefer binding tenant_id in the WHERE clause (and/or use a JOIN to ensure the row belongs to the tenant) to enforce application-level tenant isolation.
- **server/channel-routing.ts:30** — SELECT joins `personas` and reads `channel_routes` with no tenant_id constraint — this can expose personas from other tenants.
  - _Fix:_ Make channel-scoping explicit: accept a tenantId parameter, add tenant_id to channel_routes (or otherwise track tenant), and add WHERE clauses enforcing cr.tenant_id = :tenantId and p.tenant_id = :tenantId (or validate persona ownership before returning). Prefer verifying persona belongs to the tenant via SELECT on personas WHERE id = personaId AND tenant_id = tenantId before joining/returning data.
- **server/channel-routing.ts:48** — INSERT/DELETE against `channel_routes` performed without any tenant scoping and INSERT uses caller-supplied personaId without verifying tenant ownership.
  - _Fix:_ Require caller to pass tenantId and: (1) include tenant_id in INSERT (ALTER TABLE to add tenant_id NOT NULL or enforce at application), (2) add WHERE tenant_id = :tenantId to DELETE/UPDATE, and (3) before inserting or updating, verify the given personaId is owned by that tenant (e.g. SELECT 1 FROM personas WHERE id = :personaId AND tenant_id = :tenantId). Do not accept a personaId from the caller without this ownership check.
- **server/channel-routing.ts:63** — SELECT persona_id FROM channel_routes WHERE channel = ${channel} has no tenant filter — returns tenant-ambiguous persona ids.
  - _Fix:_ Change getPersonaForChannel to accept tenantId and add a tenant constraint (e.g. WHERE channel = :channel AND enabled = true AND tenant_id = :tenantId). Additionally validate the returned persona belongs to that tenant (or return null otherwise).
- **server/channel-routing.ts:71** — DELETE FROM channel_routes WHERE channel = ${channel} has no tenant constraint — can remove another tenant's routing.
  - _Fix:_ Require tenantId and add WHERE channel = :channel AND tenant_id = :tenantId. If channel_routes is intended to be per-tenant, add tenant_id column and enforce it on all write/delete operations; ensure ON CONFLICT handling also accounts for tenant scoping.
- **server/chat-engine.ts:3957** — recordMutation({...}) is invoked without any tenant identifier. If recordMutation writes to a tenant-scoped audit/log table, this call will either fail (tenant_id NOT NULL) or create rows not scoped to the caller's tenant.
  - _Fix:_ Include tenantId in the payload when calling recordMutation (e.g. recordMutation({... , tenantId })) or ensure recordMutation always receives a validated tenant context from the caller and sets tenant_id on all inserts.
- **server/commitment-drafter.ts:146** — SELECT from tenant-scoped table `commitments` without scoping to a specific tenant (no WHERE tenant_id = ...). The background job enumerates tenant IDs by scanning all rows in the tenant-scoped table.
  - _Fix:_ Constrain reads to an explicit tenant context or perform this enumeration only from an authorized admin context. If this must enumerate across tenants, make the admin authorization explicit and document it; otherwise change the query to include WHERE tenant_id = ${tenantId} (or use Drizzle eq(commitments.tenantId, tenantId)) so every read is scoped to a caller tenant.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without scoping to a specific tenant (no WHERE tenant_id = ...). The heartbeat scanner reads all overdue commitments across tenants.
  - _Fix:_ Scope queries to a tenant when executing on behalf of a tenant: add WHERE tenant_id = ${tenantId} (or use Drizzle eq(commitments.tenantId, tenantId)). If the scanner intentionally needs to operate across tenants for an admin/ops purpose, require and check explicit admin authorization and document this exception; otherwise, make the query per-tenant.
- **server/compaction.ts:400** — INSERT into compaction_archives omits tenant_id. compaction_archives is tenant-scoped elsewhere (queries join conversations.tenant_id), so this INSERT can create unscoped or cross-tenant rows.
  - _Fix:_ Require and pass the caller's tenantId into archiveMessages and include tenant_id in the INSERT (or perform the insert inside withTenantTx(tenantId, ...)). Validate the conversation→tenant mapping before insert to ensure ownership.
- **server/delivery-pipeline.ts:1681** — getDeliveryByStripePayment selects from `delivery_logs` by stripePaymentId with no tenant_id constraint. A caller providing a stripePaymentId could discover or retrieve another tenant's delivery record.
  - _Fix:_ Require tenant context (tenantId) for this lookup, and include eq(deliveryLogs.tenantId, tenantId) in the WHERE clause. If the lookup must be global for a trusted internal process, document and gate it to trusted callers only; otherwise always tenant-scope the SELECT.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken selects from `dfy_intake_forms` by token only (eq(dfyIntakeForms.token, token)) with no tenant_id constraint. This reads a tenant-scoped table without scoping to a tenant.
  - _Fix:_ If token-based public access is intended, explicitly document the security model and ensure tokens are sufficiently unguessable; otherwise add tenant scoping where appropriate (e.g. require tenantId or verify the caller's tenant before returning rows). Consider adding tenant_id to the WHERE clause for tenant-scoped admin endpoints and only skip it for the public token endpoint after explicit risk review.
- **server/doc-collections.ts:294** — UPDATE/UPDATE-like write to doc_chunks (UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}) is executed without a tenant_id constraint. Updating by id alone can modify another tenant's row.
  - _Fix:_ Constrain UPDATEs to the tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). If id uniqueness across tenants cannot be guaranteed, join or verify the parent collection ownership first.
- **server/email.ts:471** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without filtering by tenant_id. inbox_messages is tenant-scoped (tenant_id is used on INSERT), so this read may touch other tenants' inbox rows.
  - _Fix:_ Include tenant constraint when checking for existing messages (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}). If message_id is globally unique and cross-tenant checks are intentional, make such intent explicit and restrict callers.
- **server/email.ts:516** — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... is run without tenant scoping, exposing messages for all tenants.
  - _Fix:_ Restrict this query to a tenant (WHERE tenant_id = ${tenantId}) or run as an explicit, auditable admin task. Avoid unscoped reads of tenant-scoped tables in multi-tenant contexts.
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scraped_pages' uses only id in WHERE (no tenant_id filter). This UPDATE can modify another tenant's row if existing[0].id is not re-validated in the same statement.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or include tenantId in the WHERE clause. Prefer single-statement, parameterized WHERE checks rather than relying on a prior SELECT-only guard.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scraped_pages' uses only id in WHERE (no tenant_id filter) inside the crawl loop. Same pattern as above — the update can affect another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise ensure the write is atomically constrained to the caller's tenant. Always include tenant_id in WHERE for writes to tenant-scoped tables.
- **server/health-audit.ts:314** — SELECT FROM tenant-scoped table `code_proposals` is executed with no tenant_id filter (reads all tenants).
  - _Fix:_ Scope this query to a specific tenant (WHERE tenant_id = ${tenantId}) or restrict the whole scanner to an explicit admin context. If the intention is an admin/global sweep, require/verify an explicit admin caller and make that explicit in code (and in comments). Avoid implicit cross-tenant reads.
- **server/health-audit.ts:333** — UPDATE on `code_proposals` (setting status = 'archived_stale') uses id list but no tenant_id constraint — can modify rows across tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or enforce an explicit admin-only guard before running global updates. Always constrain mutating statements on tenant-scoped tables to the caller's tenant.
- **server/health-audit.ts:346** — SELECT FROM `heartbeat_tasks` is executed without any tenant_id restriction, returning tasks across all tenants.
  - _Fix:_ Restrict the SELECT to the current tenant (WHERE tenant_id = ${tenantId}) or require an explicit admin scope. If the audit must run globally, require an explicit admin check and document that behavior; otherwise always include tenant_id.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) executes without constraining tenant_id — this can disable tasks for other tenants.
  - _Fix:_ Add a tenant constraint to the UPDATE (e.g. AND tenant_id = ${tenantId}) or enforce an explicit admin authorization step when running cross-tenant updates. Never perform mutating operations on tenant-scoped tables without verifying tenant ownership.
- **server/google-workspace.ts:2564** — INSERT INTO presenter_slide_images does not include tenant_id (INSERT uses session_id, slide_index, image_data, image_size, quality only). If presenter_slide_images is tenant-scoped, this inserts rows without the required tenant_id column or without scoping to the caller's tenant.
  - _Fix:_ Either include tenantId explicitly in the INSERT values (VALUES (..., ${tenantId}, ...)) or first validate that the session_id belongs to the current tenant and then include tenant_id in the insert. Ensure all tenant-scoped tables get tenant_id set explicitly on INSERT (no implicit FK assumptions).
- **server/google-workspace.ts:2585** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint — this updates a session row by id without verifying it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or verify beforehand that the session belongs to the tenant and abort if not. Mutations against tenant-scoped rows must always validate tenant ownership or include tenant_id in the WHERE clause.
- **server/heartbeat.ts:105** — SELECT * FROM research_schedules executes without any tenant_id constraint — reads schedules across all tenants.
  - _Fix:_ Restrict this SELECT with an explicit tenant_id filter (WHERE tenant_id = ${tenantId}) or use a dedicated ALL_TENANTS-aware storage API with clear admin authorization. Every read against tenant-scoped tables must include an application-level tenant filter.
- **server/heartbeat.ts:155** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — updates tenant-scoped table row by id but without verifying/upholding tenant_id.
  - _Fix:_ Include the tenant check in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage helper that enforces tenant context to ensure you cannot update another tenant's row by id alone.
- **server/heartbeat.ts:160** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause (or use storage API that enforces tenant isolation) so updates cannot affect rows from other tenants.
- **server/heartbeat.ts:165** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant filtering on a tenant-scoped table.
  - _Fix:_ Constrain updates with tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or route through a tenant-aware storage method.
- **server/heartbeat.ts:176** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} inside the error recovery path — no tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use the storage helper to advance next_run_at safely while enforcing tenant ownership.
- **server/heartbeat.ts:325** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — update on tenant-scoped `heartbeat_tasks` without tenant_id in WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${t.tenantId}) or use storage.updateHeartbeatTask (or equivalent) which enforces tenant context to avoid touching other tenants' tasks by id.
- **server/heartbeat.ts:329** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant constraint on tenant-scoped table.
  - _Fix:_ Add tenant_id check (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call the tenant-aware storage API to perform the normalization safely.
- **server/heartbeat.ts:439** — SELECT * FROM event_log WHERE status = 'pending' ... executed without tenant_id — the code will pull pending events across all tenants.
  - _Fix:_ Either add tenant scoping (WHERE tenant_id = ${tenantId} ...) and process per-tenant, or if intentionally global, ensure this is only allowed for a privileged system component and document/guard that behavior. Prefer per-tenant SELECTs or an ALL_TENANTS-aware, audited API.
- **server/heartbeat.ts:2816** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — raw UPDATE without tenant_id restriction after creating a task.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage helper to set approval_status so the update cannot affect rows in other tenants.
- **server/heartbeat.ts:3269** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant check (same pattern as above in delegateTaskFromChat).
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call the storage API that enforces tenant context when mutating tenant-scoped rows.
- **server/internal-resolver.ts:184** — finalizeEvent() is invoked without a tenantId when the event row has no tenant_id. finalizeEvent() performs an UPDATE on event_log without a tenant_id WHERE clause when tenantId is omitted, causing an unscoped mutation of event_log.
  - _Fix:_ Never call finalizeEvent without a tenantId. If the event row lacks tenant_id, bail without performing an UPDATE (fail-closed) or otherwise refuse to write. Alternatively require finalizeEvent to always take a non-null tenantId and refuse to run when it's absent.
- **server/job-queue.ts:134** — reclaimExpiredLeases() updates agent_jobs (both terminal and re-queue branches) without any tenant_id filtering — this mutates rows across tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clauses (or provide an explicit tenantId param) so only the intended tenant's jobs are touched. If reclaim must run globally, consider partitioned processing per-tenant and strong audit/logging.
- **server/job-queue.ts:177** — completeJob() updates agent_jobs WHERE id = ${id} ... but does not verify tenant_id, allowing an unscoped update of job rows.
  - _Fix:_ Require the caller to provide tenantId (or read-and-check the job's tenant before updating) and include tenant_id = ${tenantId} in the WHERE clause to prevent cross-tenant mutation.
- **server/job-queue.ts:220** — failJob() performs a SELECT max_attempts FROM agent_jobs WHERE id = ${id} and later UPDATE agent_jobs ... WHERE id = ${id} with no tenant_id constraint — both read and write are unscoped.
  - _Fix:_ When operating on a job id, also require and check the job's tenant_id (pass tenantId or SELECT tenant_id with the row then use it in the subsequent UPDATE WHERE tenant_id = ${tenantId}). Make the operations tenant-fenced to avoid cross-tenant effects.
- **server/lib/agent-trace.ts:129** — UPDATE against tenant-scoped table agent_trace_spans lacks tenant predicate (WHERE span_id = ${spanId} only). This allows a cross-tenant UPDATE if span_id is misused/duplicated or the call context is wrong.
  - _Fix:_ Include the tenant predicate in the UPDATE WHERE clause (e.g. AND tenant_id = ${tenantId}). Obtain tenantId from the span context (currentTenantId()) or store the tenantId when opening the span and close using that tenantId, so the UPDATE is always constrained to the originating tenant.
- **server/lib/aevo-meta-editor.ts:449** — UPDATE procedure_evolution_runs uses WHERE id = ${runId} without also constraining tenant_id. Later updates reference the run row by id only, permitting cross-tenant modification if runId is mis-used.
  - _Fix:_ Include tenant scoping in these UPDATEs. Capture/return tenantId when inserting the run (or persist it in the local context) and use a WHERE clause like .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Fail closed if tenantId cannot be determined.
- **server/lib/aevo-meta-editor.ts:459** — UPDATE procedure_evolution_runs sets status='done' WHERE id = ${runId} with no tenant_id predicate, allowing cross-tenant updates.
  - _Fix:_ Add tenant scoping to the WHERE clause (see above). Ensure run inserts return tenantId (or otherwise track the tenant) and always include eq(procedureEvolutionRuns.tenantId, tenantId) in UPDATE/DELETE queries.
- **server/lib/aevo-meta-editor.ts:502** — UPDATE procedure_evolution_runs .where(eq(procedureEvolutionRuns.id, runId)) executed without tenant constraint; tenant_id is omitted from the predicate.
  - _Fix:_ Restrict the UPDATE by tenant (AND eq(procedureEvolutionRuns.tenantId, tenantId)). Return or otherwise record tenantId from the initial insert so subsequent updates can assert the same tenant and fail if missing.
- **server/lib/bwb-job-progress.ts:144** — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit the tenant_id predicate when BWB_TENANT_ID is not set, causing updates to run without tenant scoping.
  - _Fix:_ Require/validate a tenantId (fail-closed or return) before performing writes. Ensure scope() always includes eq(videoJobs.tenantId, tenantId) (or throw) instead of implicitly allowing jobId-only writes. If env-based tenant is required, surface a hard failure rather than silently omitting tenant predicate.
- **server/lib/bwb-job-progress.ts:169** — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) may run without tenant_id when env tenant is missing, leaving a tenant-scoped UPDATE unconstrained.
  - _Fix:_ Always include the tenant predicate in the WHERE clause (eq(videoJobs.tenantId, tid)) and fail/abort when the tenant cannot be determined rather than falling back to jobId-only scoping. Thread tenantId explicitly into callers rather than relying on env heuristics.
- **server/lib/bwb-job-progress.ts:203** — SELECT … FROM video_jobs .where(scope(jobId)) — scope() can omit tenant_id if BWB_TENANT_ID is not provided, performing a cross-tenant read against a tenant-scoped table.
  - _Fix:_ Require a tenantId for reads against tenant-scoped tables and include eq(videoJobs.tenantId, tenantId) in the WHERE. If the tenant is unknown, return/abort instead of dropping the tenant predicate.
- **server/lib/bwb-job-progress.ts:211** — UPDATE video_jobs .where(scope(jobId)) — scope() may not include tenant_id (env tenant missing), allowing an unconstrained UPDATE on a tenant-scoped table.
  - _Fix:_ Ensure scope() always includes tenant scoping. Pass an explicit tenantId parameter to these functions and assert it, then use and(eq(videoJobs.tenantId, tenantId), eq(videoJobs.jobId, jobId)) as the WHERE predicate.
- **server/lib/bwb-job-progress.ts:230** — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) (heartbeat) may omit tenant_id if env tenant is not set, performing an unconstrained UPDATE against a tenant-scoped table.
  - _Fix:_ Do not allow heartbeat writes without a verified tenantId. Require and assert tenant presence and include eq(videoJobs.tenantId, tenantId) in the WHERE clause; fail or no-op if tenantId is unavailable.
- **server/lib/bwb-job-progress.ts:254** — UPDATE video_jobs .where(scope(jobId, { notDone: true })) — scope() can drop the tenant predicate when BWB_TENANT_ID isn't set, leading to tenant-unspecified UPDATE on a tenant-scoped table.
  - _Fix:_ Always include tenant scoping in UPDATE/DELETE/SELECT against tenant-scoped tables. Thread tenantId into the function (or explicitly assert current tenant) and use it in the WHERE clause; if tenant cannot be determined, refuse to perform the write.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped table `projects` is executed without constraining to a caller tenant (no WHERE tenant_id = ...). This query returns cross-tenant counts and can leak tenant-scoped information across tenants.
  - _Fix:_ Restrict the query to an explicit tenant context. Instead of a global GROUP BY over all tenants, run a per-tenant query (e.g. for each tenant in wantedByTenant run SELECT count(*) FROM projects WHERE tenant_id = ${tenantId} AND tags && ${IDEA_TAG_LITERAL}::text[]), or parameterize the query to only include the snapshot tenants (WHERE tenant_id = ANY(${tenantIdArray})). Always include WHERE tenant_id = ${tenantId} (or equivalent drizzle eq(...) filter / withTenantTx) when reading tenant-scoped tables unless the operation is an explicitly audited, admin-only multi-tenant action with additional access controls.
- **server/lib/outcome-promotion.ts:158** — Global read from tenant-scoped table deliverable_grade_events without a tenant_id filter (SELECT tenant_id, format, count(*) ... FROM deliverable_grade_events ...). This reads all tenants' grade events in one query, violating the requirement that every read against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Run the aggregation per-tenant (e.g. iterate tenant IDs and include WHERE tenant_id = ${tenantId}) or otherwise add an explicit tenant_id = ${tenantId} filter to the query. If the job must process all tenants, perform a safe per-tenant loop/transaction so each query includes the tenant_id constraint and no single SQL reads data across tenants.
- **server/lib/scheduled-post-runner.ts:61** — SELECT (and subsequent UPDATE via CTE) queries read/lock rows from scheduled_posts without any tenant_id WHERE filter. This operates across tenants in a tenant-scoped table without an application-level tenant filter.
  - _Fix:_ If this runner must operate system-wide, explicitly document and restrict the code path to a privileged system context and add strong audit controls. If it should be tenant-scoped, add a tenant_id filter to the CTE (and/or call a tenant-specific runner per tenant). Always bind the tenant via WHERE tenant_id = $n (or eq(table.tenantId, tenantId)) when the caller is tenant-scoped to preserve the tenant-isolation invariant.
- **server/lib/scheduled-post-runner.ts:319** — Construction of a Postgres array literal (arrLit = `{...}`) via string interpolation from request-derived `platforms` and then embedding it into SQL (VALUES (${arrLit}::text[] ...)) creates a string-interpolated SQL fragment sourced from user input — injection risk.
  - _Fix:_ Do not build SQL literals by concatenating user input. Use parameterized array binding (pass the platforms array as a parameter and cast to text[] in SQL) or use the ORM's array support/helper. Example: VALUES (${params.tenantId}, ${params.platforms}::text[], ...) or use a proper sql.array helper so the DB driver performs safe escaping and binding instead of manual string construction.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the row id in its WHERE clause (eq(tenantVoiceProfiles.id, existing.id)) and does not constrain by tenant_id. This allows a caller who can influence `existing.id` (or an id reuse scenario) to update a row belonging to a different tenant.
  - _Fix:_ Include the tenant ownership constraint in the UPDATE WHERE clause (or otherwise assert ownership atomically). For example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively fetch-and-verify ownership inside the same transaction or use optimistic concurrency (id+tenant_id) so every write to a tenant-scoped table is explicitly constrained to the caller's tenant.
- **server/memory-graph.ts:124** — UPDATE memory_categories executed without constraining to tenant_id (no AND tenant_id = ${tenantId}). This can modify categories across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Prefer passing tenantId into the call site if not available.
- **server/memory-graph.ts:184** — refreshCategoryCount updates memory_categories and computes the new count using a subquery that does NOT filter memory_entries by tenant_id and updates the category row without tenant filtering. Both the inner COUNT and the outer UPDATE lack tenant scoping.
  - _Fix:_ Change refreshCategoryCount to accept tenantId and scope both sides: SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ...; and UPDATE memory_categories ... WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:240** — SELECT FROM memory_links to check for an existing link does not include tenant filtering (no tenant_id constraint). Reading a tenant-scoped memory_links table without tenant scoping can return cross-tenant rows.
  - _Fix:_ Add tenant scoping to the existence check, e.g. WHERE ((source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR (...)) AND tenant_id = ${tenantId} LIMIT 1. Ensure the tenant_id column on memory_links is used in every read.
- **server/memory-graph.ts:276** — Query selecting linked memories reads memory_links (ml) without constraining ml.tenant_id. The WHERE filters the joined memory_entries row by tenant but the memory_links table itself is not explicitly tenant-scoped in the query.
  - _Fix:_ Also constrain ml to the tenant when tenantId is provided, e.g. add AND ml.tenant_id = ${tenantId} (or conditionally omit only when null is intentionally allowed). Prefer explicit tenant predicates on every tenant-scoped table referenced.
- **server/memory-graph.ts:528** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is missing tenant_id constraint. This may update a row in a different tenant if ids collide.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Pass tenantId into the caller if necessary.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} lacks tenant scoping. The contradictions loop updates triple rows by id only, risking cross-tenant mutation.
  - _Fix:_ Include tenant constraint on the UPDATE: WHERE id = ${c.id} AND tenant_id = ${tenantId}. Pass tenantId through to this function/loop so all updates are tenant-scoped.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files omits tenant_id even though tenant context is available (options.tenantId) and assertProjectInTenant was used only to validate ownership.
  - _Fix:_ Include the tenant_id column/value in the INSERT and bind the caller's validated tenantId explicitly. Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...). Also validate that options.tenantId is a positive integer (fail-closed) before inserting.
- **server/mpeg-engine.ts:1436** — INSERT into tenant-scoped table project_files does not set tenant_id. The parallel-produce code inserts into project_files without passing tenant_id, risking writes outside the caller's tenant scope.
  - _Fix:_ Add tenant_id to the INSERT and bind the caller's validated tenantId explicitly (e.g. VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure assertProjectInTenant validated ownership prior to the insert and validate options.tenantId is a proper positive integer before writing.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() with a value derived from caller-provided ledgerKey: `SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`. Passing interpolated/raw SQL fragments constructed from request-derived input can lead to injection or unexpected SQL execution.
  - _Fix:_ Avoid sql.raw() with any caller-controlled data. Compute a numeric advisory-lock key on the server and pass it as a parameter binding (e.g. compute a safe BigInt number server-side and use a parameterized query: `SELECT pg_advisory_xact_lock(${safeBigInt})`). Validate and coerce ledgerKey to a numeric value before use; if you must derive a lock value from a string, hash + reduce deterministically to a numeric value and bind it as a parameter rather than embedding raw SQL.
- **server/persona-export.ts:39** — Query for `skills` uses `WHERE persona_id = ${personaId} OR persona_id IS NULL` with no tenant constraint. If `skills` is tenant-scoped, this can leak other tenants' skill rows.
  - _Fix:_ If `skills` is tenant-scoped, include tenant_id in the WHERE clause (or explicitly restrict to global skills by design). Otherwise, document that `skills` is intentionally global; preferably enforce tenant scoping for tenant-specific rows and explicitly join/filter by tenant_id when exporting a persona.
- **server/plan-executor.ts:116** — UPDATE to tenant-scoped table 'plans' (appendExecutionLog) uses WHERE id = ${planId} only — no tenant_id constraint.
  - _Fix:_ Require and pass the caller/plan tenantId into appendExecutionLog and update using WHERE id = ${planId} AND tenant_id = ${tenantId}. Alternatively fetch/verify the plan row under a tenant-restricted SELECT before updating so every mutation is constrained to the plan's tenant.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — missing tenant_id constraint when marking a plan failed.
  - _Fix:_ Constrain the update by tenant_id (WHERE id = ${planId} AND tenant_id = ${tenantId}) or look up the plan under tenant-scoped SELECT and then update using both id and tenant_id to ensure you cannot modify another tenant's plan.
- **server/plan-executor.ts:762** — Error-path UPDATE (marking a plan failed) uses WHERE id = ${planId} AND status = 'executing' without any tenant_id constraint.
  - _Fix:_ Add tenant fencing to this update (e.g. AND tenant_id = ${tenantId}) or validate the plan's tenant before performing any status mutations to prevent cross-tenant interference.
- **server/process-governor.ts:121** — Nested SELECTs against heartbeat_logs omit tenant scoping (e.g. the subqueries computing successes/failures for ht.id). heartbeat_logs appears tenant-scoped but the subqueries lack AND tenant_id = ${tenantId}. This can return/act on other tenants' data.
  - _Fix:_ Include tenant_id filtering in the subqueries (or join to heartbeat_tasks and constrain by ht.tenant_id). E.g. add AND tenant_id = ${tenantId} to each heartbeat_logs subquery or refactor to a single JOIN that enforces ht.tenant_id = ${tenantId}.
- **server/process-governor.ts:155** — Subquery computing pending_tasks reads heartbeat_tasks without tenant scoping: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true). persona_id may not be globally unique; missing tenant filter can spill counts across tenants.
  - _Fix:_ Restrict the subquery to the same tenant: add AND tenant_id = ${tenantId} (or join heartbeat_tasks with ad and enforce ad.tenant_id = heartbeat_tasks.tenant_id = ${tenantId}).
- **server/process-governor.ts:198** — SELECT AVG(duration_ms) FROM heartbeat_logs lacks a tenant filter. heartbeat_logs is tenant-scoped elsewhere; without tenant_id = ${tenantId} this aggregates across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (in addition to existing time/status filters) so the metric is computed only for the caller tenant.
- **server/process-governor.ts:239** — cascading_failures query SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs omits tenant scoping (no tenant_id filter), allowing cross-tenant aggregation.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or otherwise join/filter by tenant) to the heartbeat_logs WHERE clause to limit results to the tenant.
- **server/research-engine.ts:165** — UPDATE research_sessions ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold} runs without a tenant_id constraint. research_sessions is tenant-scoped elsewhere; this global update may change session state for other tenants.
  - _Fix:_ Scope the update by tenant_id (e.g. add AND tenant_id = ${tenantId}) or, if intended as a global admin operation, explicitly iterate tenants and run parameterized updates per-tenant or require an explicit admin context and audit that the operation is authorized. Avoid unscoped UPDATEs on tenant-scoped tables.
- **server/research-proposals.ts:713** — SELECT from tenant-scoped table research_experiments (alias 're') is not explicitly constrained by re.tenant_id — the query only filters rs.tenant_id via the joined research_sessions row. This allows accidental cross-tenant reads if session/experiment tenant consistency is not enforced at the DB level.
  - _Fix:_ Add an explicit tenant filter on the research_experiments table in the WHERE clause (e.g. AND re.tenant_id = ${tenantId}). Audit all queries that read tenant-scoped tables via JOINs and ensure each tenant-scoped table is directly constrained. Consider adding defensive server-side checks or Postgres RLS to enforce tenant ownership at the DB layer.
- **server/routes.ts:174** — Startup routine restoreUploadsFromDb queries tenant-scoped file_storage rows without scoping by tenant_id (db.select(...).from(fileStorage).where(...)). This reads other tenants' file data and may write it to the shared uploads dir, violating tenant isolation.
  - _Fix:_ Restrict the query to a single tenant at a time (e.g. include eq(fileStorage.tenantId, someTenantId) or iterate tenants and run per-tenant queries) or run the restore only under an explicit admin context with strict safeguards. Never select full file contents across all tenants and write them to a shared path without confirming ownership.
- **server/routes.ts:2089** — Public presenter endpoint queries tenant-scoped presenter_sessions by token without also constraining by tenant_id (SELECT ... FROM presenter_sessions WHERE token = ${token}). This reads tenant-scoped rows without an explicit tenant filter in request-handling code.
  - _Fix:_ Bind the lookup to the authenticated/authorized tenant or otherwise validate the caller's authority to access that presenter session. For token-based access, ensure the token itself is treated as a strong capability and, if possible, store/compare a keyed HMAC that includes tenant id or add an explicit tenant_id check after resolving the token (and fail closed if tenant ownership cannot be proven). Avoid unscoped selects of tenant-scoped tables in public request handlers.
- **server/routes.ts:4482** — INSERT into project_notes does not include tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant scoping; tenant_id is required for tenant-scoped tables (NOT NULL, no default).
  - _Fix:_ Include the tenant_id column/value in the INSERT and ensure the project belongs to the caller's tenant. Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, call a storage-layer helper (e.g. storage.createProjectNote) that enforces tenant ownership and always sets tenant_id.
- **server/routes/conversations.ts:99** — Unscoped UPDATE on tenant-scoped table `conversations`: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` has no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${conv.tenantId ?? tenantId}` (or AND tenant_id = ${tenantId}). Always include tenant_id in WHERE for tenant-scoped tables.
- **server/routes/conversations.ts:103** — INSERT into `project_conversations` omitted tenant context: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id.
  - _Fix:_ If `project_conversations` is tenant-scoped, include tenant_id in the INSERT (and values) e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`. If the table intentionally lacks tenant_id, ensure ownership is enforced by verifying both project_id and conversation_id belong to the caller's tenant before inserting.
- **server/routes/conversations.ts:105** — Unscoped UPDATE on tenant-scoped table `projects`: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` has no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Always include tenant_id in WHERE for tenant-scoped tables to enforce isolation.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` is executed without a tenant_id constraint in the WHERE clause (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). This violates the app-level tenant isolation invariant—every write to a tenant-scoped table must include a tenant predicate.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause (or use the storage helper that enforces scope). For example: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`); or call a storage-layer method like storage.markInboxMessageRead(msg.id, tenantId) that guarantees the tenant predicate.
- **server/routes/projects.ts:516** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id even though project_notes is tenant-scoped.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${id}, ...)). Alternatively, if using transactions, ensure tenant_id is set server-side from the verified project ownership before inserting.
- **server/routes/projects.ts:556** — INSERT into tenant-scoped table 'project_files' (files-base64 handler) does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ...)). Ensure tenantId is derived from session and not from caller input.
- **server/routes/projects.ts:623** — INSERT into tenant-scoped table 'project_files' (multer upload handler) does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ...)). Verify tenant ownership before inserting and never trust client-supplied tenant values.
- **server/routes/projects.ts:399** — INSERT into tenant-scoped table 'project_conversations' does not include tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${targetProjectId}, ${conversationId})). If relying on project ownership checks, still persist tenant_id to the row to enforce isolation at query-time.
- **server/routes/stats.ts:54** — Drizzle sql template literal interpolates request-derived tenantId in the WHERE clause (s`${conversations.tenantId} = ${tenantId}`) instead of using a typed/parameterized Drizzle predicate. This is a string-interpolated SQL usage with a request-derived value (injection + tenant-isolation risk).
  - _Fix:_ Use the Drizzle query builder/predicates to bind the tenant id explicitly (e.g. .where(eq(conversations.tenantId, tenantId))) or otherwise pass tenantId as a proper parameter rather than interpolating it into a sql`` template. This preserves parameterization and makes the tenant filter explicit.
- **server/routes/stats.ts:55** — Drizzle sql template literal contains a subquery referencing a request-derived tenantId (s`${messages.conversationId} IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`). This is string-interpolated SQL with a request-derived value, creating injection and isolation risk.
  - _Fix:_ Rewrite using Drizzle query builder / predicates to express the same constraint (for example join conversations and use .where(eq(conversations.tenantId, tenantId)) or .where(in(messages.conversationId, db.select({id: conversations.id}).from(conversations).where(eq(conversations.tenantId, tenantId)))) so tenantId is passed as a bound parameter and the tenant filter is explicit.
- **server/routes/stripe-checkout.ts:100** — Raw/template SQL (db.execute(sql`... WHERE pr.id = ${priceId} ...`)) interpolates request-derived priceId and isAnonymous into the sql`` template. This is string-interpolated SQL using values from the request, risking injection and making tenant/authorization checks harder to audit.
  - _Fix:_ Avoid interpolating request values into sql`` blocks. Use Drizzle's typed query builder (e.g. db.select().from(prices).where(and(eq(prices.id, priceId), eq(prices.active, true), eq(products.active, true), ...))). If you must use raw SQL, pass values as parameters (prepared statements) and validate/normalize inputs server-side before use. Also keep the anonymous logic outside the SQL expression and pass a boolean parameter rather than embedding server-state logic into an interpolated string.
- **server/seed.ts:38** — SELECT from tenant-scoped table research_sessions without tenant filter (SELECT COUNT(*) FROM research_sessions). This reads across all tenants.
  - _Fix:_ Constrain the query to a specific tenant, e.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}. If this is intended to be global/admin-only logic, ensure it runs in a privileged context and is explicitly documented as such.
- **server/seed.ts:2160** — SELECT from tenant-scoped table research_programs without tenant filter (SELECT id, name, model FROM research_programs WHERE is_active = true). This can return rows for all tenants.
  - _Fix:_ Add a tenant constraint to the SELECT, e.g. WHERE is_active = true AND tenant_id = ${tenantId}, or otherwise scope the operation to the intended tenant(s) explicitly.
- **server/seed.ts:2518** — SELECT from conversations (tenant-scoped table) without tenant filter (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). This can enumerate conversations across tenants.
  - _Fix:_ Restrict the query to the caller's tenant, e.g. SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ... . If the subsequent INSERTs/backfills must be tenant-scoped, ensure the tenant_id is included/propagated when selecting and inserting.
- **server/seed.ts:3938** — UPDATE on tenant-scoped table `memory_entries` performed without constraining to a tenant (no WHERE tenant_id = ...). This updates rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = <tenantId>) or iterate per-tenant and include WHERE tenant_id = <tenantId>. For this seed, if intended for admin tenant use WHERE tenant_id = 1.
- **server/seed.ts:3964** — UPDATE on tenant-scoped table `conversations` performed without constraining to a tenant (no WHERE tenant_id = ...). This will modify conversations for all tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. UPDATE conversations SET ... WHERE model = 'gpt-4.1' AND tenant_id = <tenantId> (or run per-tenant). If the intent is to modify only the admin/demo tenant, use tenant_id = 1.
- **server/seed.ts:3400** — SELECT * FROM tenant-scoped table `heartbeat_tasks` with no tenant filter (db.select().from(heartbeatTasks)). This reads rows across all tenants and subsequent logic may act on global results.
  - _Fix:_ Restrict reads to the caller's tenant, e.g. db.select().from(heartbeatTasks).where(eq(heartbeatTasks.tenantId, tenantId)). For seed operations intended for the admin/demo tenant, include WHERE tenant_id = 1.
- **server/seed.ts:4006** — DELETE dedupe on `file_storage` (DELETE FROM file_storage a USING file_storage b ...) operates across all tenants. Although it compares a.tenant_id = b.tenant_id, it lacks a fixed tenant_id value and will run for every tenant in the DB.
  - _Fix:_ Constrain the dedupe to a single tenant at a time, e.g. add AND a.tenant_id = <tenantId> (and b.tenant_id = <tenantId>), or run the dedupe in a per-tenant loop. For seed/admin tasks, use tenant_id = 1.
- **server/seed.ts:4223** — UPDATE on tenant-scoped table `agent_knowledge` executed by id only (WHERE id = ...) without also constraining tenant_id. This can update rows owned by other tenants if ids collide or are guessed.
  - _Fix:_ Include tenant constraint in the UPDATE, e.g. WHERE id = ${rows[0].id} AND tenant_id = <tenantId>, or perform the UPDATE using the tenant-aware SELECT results (ensure equality to the caller's tenant). If intended for tenant 1, add AND tenant_id = 1.
- **server/self-improvement.ts:171** — detectStagnation performs a SELECT on the tenant-scoped `experiments` table without requiring/applying a tenant_id constraint when tenantId is not supplied. The query falls back to filtering only by category, which can return cross-tenant rows.
  - _Fix:_ Always constrain queries on tenant-scoped tables to the caller's tenant. Either make tenantId a required argument or assert the caller's tenant is present, and include eq(experiments.tenantId, tenantId) in the WHERE (e.g. .where(and(eq(experiments.category, category), eq(experiments.tenantId, tenantId)))). If the operation must be admin-only, require an explicit admin guard and document it.
- **server/self-improvement.ts:517** — getExperimentHistory can SELECT from tenant-scoped `experiments` without a tenant_id constraint when tenantId is not provided. The code returns all experiments if tenantId is omitted.
  - _Fix:_ Require the caller's tenantId or otherwise enforce tenant scoping for this function. Always include eq(experiments.tenantId, tenantId) in the WHERE for tenant-scoped reads; if an admin/global view is needed, make that explicit with proper admin checks and audited code paths.
- **server/self-reflection.ts:366** — storeLesson performs an UPDATE on `memory_entries` using WHERE id = <row id> with no tenant_id constraint. Updating by id alone can modify rows belonging to another tenant.
  - _Fix:_ Include tenant_id in the WHERE clause for the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or perform a preceding tenant-scoped SELECT to assert ownership before updating. Apply the same pattern to all writes/updates/deletes on tenant-scoped tables.
- **server/skillify.ts:91** — SELECT from messages lacks tenant constraint — messages is a tenant-scoped table but the query uses only conversationId (no tenant_id WHERE clause). This can cause cross-tenant data disclosure if conversationId is passed from an untrusted caller.
  - _Fix:_ Constrain the query to the caller's tenant, e.g. include AND eq(messages.tenantId, tenantId) or join messages -> conversations and assert conversations.tenant_id = tenantId before selecting messages. Always include explicit tenant_id filters on tenant-scoped tables.
- **server/skillify.ts:201** — SELECT from messages in analyzeConversation() has no tenant_id constraint — messages is read by conversationId without explicitly constraining tenant. Per policy every read against a tenant-scoped table must include tenant_id.
  - _Fix:_ Add an explicit tenant constraint to the messages query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or fetch messages via the verified conversation row (join on conversation id and tenant) so the SQL includes an explicit tenant filter.
- **server/skillify.ts:221** — SELECT persona row by id without tenant constraint. personas is likely tenant-scoped; fetching by persona id alone may expose another tenant's persona data.
  - _Fix:_ Constrain the persona lookup by tenant (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))) or otherwise verify ownership before returning persona data.
- **server/skillify.ts:329** — Another recentMsgs SELECT (in skillifyConversation) reads messages by conversationId without tenant_id filtering. This duplicates the same tenant-isolation risk (cross-tenant read).
  - _Fix:_ Include tenant scoping on this messages SELECT as well (e.g. add AND eq(messages.tenantId, tenantId)), or re-use the prior conversation ownership check and ensure the SQL itself contains an explicit tenant filter.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks executed without tenant constraint. This deletes tenant-scoped heartbeat_tasks across all tenants (no WHERE tenant_id), risking cross-tenant data mutation/loss.
  - _Fix:_ Scope DELETEs to a specific tenant (WHERE tenant_id = ${tenantId}) unless a deliberate global purge is intended. If a global purge is required, call a clearly named system/admin API and log the action/auditable tenant set so the cross-tenant intent is explicit and controlled.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log executed without tenant filter. This will remove event_log rows across tenants (no WHERE tenant_id) potentially causing cross-tenant data loss.
  - _Fix:_ Add explicit tenant scoping to the DELETE (e.g. WHERE tenant_id = ${tenantId}) or, if the operation is intentionally global, route it through a system-level routine that documents and audits multi-tenant actions, and avoid implicit un-scoped statements against tenant-scoped tables.
- **server/storage.ts:256** — Unscoped SELECT from tenant-scoped table `conversations` (getConversationUnscoped) — no tenant_id constraint in WHERE.
  - _Fix:_ Avoid unscoped reads in shared HTTP handlers. If unscoped lookup is required for orchestration, mark and use a narrow, audited callsite; otherwise require and pass tenantId and add eq(conversations.tenantId, tenantId) to the WHERE clause or use tenantScope/requiredTenantScope to fail-closed.
- **server/storage.ts:407** — UPDATE on tenant-scoped table `messages` (attachCitations) without tenant_id filter: UPDATE messages SET citations = ... WHERE id = ${messageId}.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. add AND tenant_id = ${tenantId} or use a JOIN/subquery verifying the message belongs to tenant). Validate/require tenantId at the callsite before performing the UPDATE to prevent cross-tenant writes.
- **server/storage.ts:862** — UPDATE memory_entries SET embedding = ... WHERE id = ${id} updates tenant-scoped `memory_entries` without tenant_id constraint.
  - _Fix:_ Require and validate a tenantId parameter (or use a subquery that verifies tenant ownership) and include eq(memoryEntries.tenantId, tenantId) in the WHERE clause. Alternatively use requiredTenantScope/assertValidTenantId helpers to force callers to pass a tenant scope.
- **server/storage.ts:874** — UPDATE agent_knowledge SET embedding = ... WHERE id = ${id} updates tenant-scoped `agent_knowledge` without tenant_id constraint.
  - _Fix:_ Require and validate tenantId (or verify ownership) and include eq(agentKnowledge.tenantId, tenantId) in the WHERE clause. Use requiredTenantScope/tenantScope helpers to enforce fail-closed behavior.
- **server/storage.ts:521** — deletePersona transaction updates tenant-scoped tables (conversations, memory_entries, daily_notes) by personaId without including tenant_id constraints — can modify rows across all tenants.
  - _Fix:_ Scope those UPDATE/DELETE statements to the specific tenant(s): either add tenant filters (e.g. AND tenant_id = ${tenantId}) or require callers to pass a tenantId and validate it before performing cross-table updates. If personas are global and must clear references tenant-by-tenant, iterate per-tenant or run trusted admin flows only.
- **server/storage.ts:571** — INSERT into tenant-scoped table `memory_entries` via db.insert(memoryEntries).values(data) occurs without explicit validation that data.tenantId is present and owned by the caller.
  - _Fix:_ Fail-closed: validate/assert data.tenantId is a positive integer (assertValidTenantId) and that the caller is authorized for that tenant before inserting. If insert must infer tenantId, derive it from a validated parent row and never default to ADMIN_TENANT_ID silently.
- **server/storage.ts:811** — INSERT into tenant-scoped table `agent_knowledge` via db.insert(agentKnowledge).values(data) happens without validating data.tenantId or checking writer ownership.
  - _Fix:_ Require callers to pass a validated tenantId (use assertValidTenantId/requireTenantScope) before inserting. Enforce a fail-closed check that data.tenantId is present and authorized for the caller to prevent cross-tenant writes.
- **server/surprise-scorer.ts:148** — SELECT args_embedding FROM felix_proposals uses id = ${proposalId} without a tenant_id constraint, re-fetching a tenant-scoped column without tenant scoping.
  - _Fix:_ Re-select args_embedding with a tenant_id filter (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or pass tenantId through the function and use it in the query to ensure isolation.
- **server/tool-learning.ts:129** — SELECT from custom_tools filters only on name (eq(custom_tools.name, parsed.name)) and lacks an explicit tenant_id constraint. Reads against a tenant-scoped table must be tenant-scoped.
  - _Fix:_ Add tenant scoping to the lookup, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Even if names are namespaced, enforce tenant_id filtering per the project's invariant.
- **server/tool-learning.ts:155** — SELECT of the just-inserted custom_tools row uses only id in WHERE clause (eq(custom_tools.id, inserted.id)) without tenant_id. Reading tenant-scoped tables by id must also verify tenant ownership.
  - _Fix:_ Include tenant filter in the SELECT, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Pass tenantId through where needed to confirm the row belongs to the new tenant.
- **server/subagents.ts:265** — storage.getPersona(agentId) is called without passing or verifying tenant context. If persona rows are tenant-scoped this reads a referenced row without confirming caller ownership.
  - _Fix:_ Resolve persona with tenant scoping (e.g. storage.getPersona(agentId, parentConv.tenantId) or validate persona belongs to parentConv.tenantId before using). Always verify ownership of foreign IDs derived from another tenant-scoped row.
- **server/tools.ts:2995** — INSERT into project_files does not set tenant_id. Tenant-scoped tables must have tenant_id supplied on INSERT to enforce isolation at the application layer.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). If tenant_id should be derived, derive it from the already-validated project row (not from untrusted caller input) and pass it explicitly in the INSERT. Add a test to ensure INSERTs into tenant-scoped tables always set tenant_id.
- **server/tools.ts:3010** — INSERT into project_notes does not set tenant_id. Tenant-scoped tables must be explicitly stamped with the caller's tenant on write.
  - _Fix:_ Add tenant_id to the project_notes INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). If relying on project_id ownership checks, still include tenant_id explicitly (derived from the validated project) to meet the application's tenant-isolation invariant.
- **server/tools.ts:2892** — INSERT into project_conversations (during project creation link) does not set tenant_id. Writing tenant-scoped relationship rows without tenant_id allows cross-tenant contamination if a row's tenant is not enforced elsewhere.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ) or otherwise ensure the row is only creatable via a safe insertion path that derives tenant_id from a validated project and stores it explicitly.
- **server/tools.ts:3033** — INSERT into project_conversations (link_conversation path) does not include tenant_id. New relationship rows are inserted without an explicit tenant stamp.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). If deriving tenant_id from the project, fetch/validate the project's tenant and use that value in the INSERT rather than trusting caller-supplied values.
- **server/tools.ts:6944** — INSERT into tenant-scoped table 'project_files' without setting tenant_id. The raw SQL INSERT into project_files does not include tenant_id even though tenant isolation is enforced at application level and INSERTs must supply tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (use the caller's tenant id, e.g. params._tenantId). Prefer parameterized Drizzle insert or add tenant_id to the VALUES list. Example: INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${params._tenantId}, ...). Alternatively derive tenantId from the asserted project owner (assertProjectInTenant) and pass that explicit tenantId into the INSERT.
- **server/tools.ts:8910** — UPDATE against tenant-scoped table 'proposed_skills' without constraining by tenant_id. The db.update(...).where(eq(proposedSkills.id, row.id)) call lacks a tenant filter, allowing cross-tenant updates if IDs collide or are guessed.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or use an update that includes tenantId in the predicate. Ensure tid is the caller's verified tenant id and cannot be spoofed.
- **server/tools.ts:8952** — UPDATE against tenant-scoped table 'proposed_skills' without constraining by tenant_id. The update that marks a proposal 'accepted' uses only proposedSkills.id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Validate tenant ownership before updating and prefer parameterized Drizzle update calls that include tenantId.
- **server/tools.ts:8969** — UPDATE against tenant-scoped table 'proposed_skills' without constraining by tenant_id. The update that marks a proposal 'rejected' uses only proposedSkills.id in the WHERE clause.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Always verify and include the caller's tenantId in update predicates to enforce tenant isolation.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' omits tenant constraint — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} has no tenant_id check.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Include tenant_id in the WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))). Example: await wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); (use the tenantId from ctx and/or the fetched workflow's tenant to parameterize safely).
- **server/tools/domains/files/handlers.ts:907** — UPDATE on tenant-scoped table 'projects' omits tenant constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} has no tenant_id filter.
  - _Fix:_ Add a tenant constraint to the UPDATE to ensure only the caller's tenant row is modified. For example: await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`); (use the caller's ctx.tenantId and ensure it's validated before executing).
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE statement updates knowledge_triples rows without scoping by tenant_id (WHERE id = ${row.id} only). This violates the tenant-isolation invariant that every UPDATE/DELETE/SELECT against tenant-scoped tables must include tenant_id in the WHERE clause.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise verify ownership before mutating. Prefer parameterized, tenant-scoped updates to ensure a row cannot be modified across tenants.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` executes without a tenant_id filter (no WHERE tenant_id = ...)
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise enforce explicit admin-only semantics with audit logging. If full-platform enumeration is required only for an admin role, make that explicit and document/authorize it, but prefer scoping by tenant_id (or a separate admin-only audited path) to avoid accidental cross-tenant disclosure.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` performed with no tenant_id constraint (reads across tenants)
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or make the cross-tenant intent explicit and gated by a verified admin check and audit log. Avoid queries that enumerate provider keys across tenants unless strictly necessary and authorized.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE memory_entries SET ... WHERE id = ${id} — update is not constrained by tenant_id (no WHERE tenant_id = ${tid}) even though memory_entries is tenant-scoped
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) to ensure the row being updated belongs to the caller's tenant. Do not rely solely on a prior SELECT to infer ownership; enforce it in the UPDATE statement.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET last_accessed = NOW() ... WHERE id = ANY(${ids}) — update uses ids array without tenant_id constraint
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. UPDATE ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}) or re-run the update per-row using a parametrized WHERE id = ${id} AND tenant_id = ${tid}. This prevents accidental cross-tenant updates if IDs were obtained or manipulated incorrectly.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET fact = ${fact} ... WHERE id = ${id} in recordStrategicWinHandler — missing tenant_id constraint
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) so updates cannot affect rows belonging to other tenants. Enforce tenant ownership at every write/update/delete against tenant-scoped tables.
- **server/twilio.ts:102** — Query selects from tenant-scoped table `agent_knowledge` without constraining on tenant_id (SELECT id, content::jsonb AS info FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ...). This reads rows across all tenants.
  - _Fix:_ Avoid cross-tenant reads. Constrain the query by tenant_id (e.g. WHERE tenant_id = ${claimedTenant} AND title = ...) or otherwise redesign pairing storage so lookups can be restricted to a verified tenant context. If the intention is to verify an external claim, first resolve/verify the conversation ownership (by querying conversations with both id and tenant_id) and then only load pairing rows for that tenant; do not load all tenants' pairing rows unfiltered.
- **server/twilio.ts:116** — Query reads the `conversations` row by id without scoping to a tenant (SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} LIMIT 1). This performs an unconstrained cross-tenant read.
  - _Fix:_ Verify ownership with a tenant-scoped query instead of fetching by id across all tenants. For example, query WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} (or use a parameter for the claimed tenant) and treat absence as a mismatch. That ensures you never read arbitrary conversation rows from other tenants.
- **server/video-job-runner.ts:272** — Query against tenant-scoped table `videoJobs` without a tenant_id constraint (listStaleVideoJobs selects jobs across all tenants). This reads tenant-owned rows without restricting to a caller/owner tenant.
  - _Fix:_ Either scope the query to a specific tenant (add eq(videoJobs.tenantId, <tenantId>) in the WHERE) or explicitly document/implement a safe, auditable cross-tenant recovery flow (e.g., run a per-tenant sweep loop or require admin-only execution). Do not leave a plain SELECT from a tenant-scoped table without an ownership filter.
- **server/webhook-triggers.ts:60** — SELECT joins the `personas` table (a tenant-scoped table) without constraining persona rows by tenant. listTriggers() LEFT JOINs personas ON p.id = w.persona_id and returns persona_name but does not ensure the persona belongs to the same tenant context — potential cross-tenant data exposure.
  - _Fix:_ When fetching persona data, include an explicit tenant constraint (e.g. JOIN personas p ON p.id = w.persona_id AND p.tenant_id = <expectedTenant>) or resolve persona via a tenant-aware accessor that validates ownership. Alternatively store the persona's tenant in webhook_triggers at creation time and use it during joins/reads.
- **server/webhook-triggers.ts:122** — processTriggerEvent() selects webhook trigger rows and LEFT JOINs `personas` to expose persona_name but does not validate that the referenced persona belongs to the same tenant. Reading tenant-scoped persona rows without a tenant filter can leak other tenants' persona data.
  - _Fix:_ Add an explicit tenant ownership check when resolving persona_name (e.g. JOIN personas p ON p.id = w.persona_id AND p.tenant_id = <expectedTenant>) or fetch the persona via a tenant-aware API/DAO that verifies the persona's tenant before returning any attributes.
- **server/webhook-triggers.ts:81** — createTrigger() takes a personaId from caller input and writes it into the global webhook_triggers table without validating that the caller (or admin context) actually owns or is allowed to reference that persona. This writes a foreign id referencing a tenant-scoped row without an ownership check.
  - _Fix:_ Validate the provided personaId before inserting: load the persona using a tenant-aware accessor and confirm it belongs to the expected tenant/context (or only allow personaIds from the admin tenant). If webhook triggers are global/admin-only, explicitly restrict persona selection to personas owned by the admin tenant and record persona tenant in the webhook_triggers row so subsequent joins/reads can enforce tenant-scoped constraints.

### MEDIUM (40)

- **server/agent-activity.ts:60** — getPersonality selects from personas by persona id with no tenant constraint (.from(personas).where(eq(personas.id, personaId))). This can read persona rows across tenants and cache them globally.
  - _Fix:_ Include tenant scoping when selecting personas (e.g. also where eq(personas.tenantId, tenantId) or change API to require tenantId), and ensure the cache key includes tenantId so cached entries are tenant-scoped.
- **server/agent-desk.ts:297** — getDesksOverview runs SELECT id, name FROM personas with no tenant filter. It then uses those global persona rows to label tenant-scoped desks, which leaks cross-tenant persona names.
  - _Fix:_ Restrict the personas query to the tenant (e.g. SELECT id, name FROM personas WHERE tenant_id = ${tenantId}) or otherwise ensure only tenant-owned persona rows are read and used.
- **server/agentic-engines.ts:374** — runDecisionEngine issues SELECT name, role FROM personas WHERE is_active = true LIMIT 14 with no tenant filter, reading persona records across tenants.
  - _Fix:_ Include tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise restrict persona reads to the caller's tenant and avoid cross-tenant caching/leakage.
- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} ... is executed without an explicit tenant_id constraint. While conversation_id ties messages to a conversation, the query reads a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Add an explicit tenant constraint (e.g. AND tenant_id = ${c.tenant_id}) or join messages to conversations and include conversations.tenant_id in the WHERE clause to ensure strict tenant scoping and avoid accidental cross-tenant reads.
- **server/auth.ts:1262** — loadSessionsFromDb runs DELETE FROM auth_sessions WHERE expires_at < ${now} without tenant scoping (global session cleanup).
  - _Fix:_ As with other maintenance deletes, either scope to a tenant or run in a platform-admin/maintenance job that is explicitly privileged. Prefer per-tenant deletes when possible or centralize global maintenance behind an admin-only path.
- **server/auth.ts:1266** — loadSessionsFromDb runs DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' (global) without tenant restriction — this mutates auth_sessions across all tenants.
  - _Fix:_ Perform migration/cleanup per-tenant or require a platform-admin context. If unavoidable, gate this operation behind an explicit admin check and document its cross-tenant effect.
- **server/auth.ts:1130** — cleanExpiredResetTokens executes DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} without a tenant filter — it removes rows across tenants.
  - _Fix:_ Either delete expired tokens per-tenant, or ensure this function runs only in an admin/maintenance context. Prefer including tenant_id in the WHERE clause when invoked from tenant-scoped code paths.
- **server/auth.ts:196** — validateApiKey selects from apiKeys without a tenant_id constraint (selects rows by keyHash only). This is a tenant-scoped table read with no tenant filter.
  - _Fix:_ If api key lookup must be global (to resolve tenant from the key), document that behavior and ensure the code path is safe. Otherwise, require callers to provide tenant context and include WHERE tenant_id = ${tenantId}. As an alternative, make apiKeys lookups explicit and ensure the table and access pattern are reviewed as a trusted global lookup.
- **server/chat-engine.ts:4223** — recordToolExecution(conversationId, toolName, success) is called without an explicit tenant id. If this function persists per-tenant tool execution records, it must be given the tenant context; otherwise tenant scoping can be missing or misattributed.
  - _Fix:_ Modify calls to recordToolExecution to include tenantId (e.g. recordToolExecution(conversationId, toolName, success, tenantId)) or ensure the function retrieves and enforces tenant ownership from a verified, trusted source before any DB reads/writes.
- **server/data-protection.ts:404** — SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} does not include tenant_id. Reads against tenant-scoped tables must include tenant_id to avoid cross-tenant inference or accidental access.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId} or AND tenant_id = ${conv.tenant_id}). Prefer verifying ownership earlier and/or run under withTenantTx(tenantId).
- **server/data-protection.ts:360** — SELECT from project_notes WHERE project_id = ${proj.id} lacks an explicit tenant_id predicate. Even though projects were selected per-tenant, every read against tenant-scoped tables must include tenant scoping as an invariant.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or join to projects and filter by p.tenant_id = ${tenantId}. Alternatively perform the operation inside withTenantTx(tenantId).
- **server/data-protection.ts:371** — SELECT from project_files WHERE project_id = ${proj.id} lacks an explicit tenant_id predicate. Reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Include tenant predicate (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or join back to projects where p.tenant_id = ${tenantId}, or perform under withTenantTx(tenantId) to ensure tenant isolation.
- **server/dormant-deprecation.ts:170** — SELECT COUNT(*) FROM messages WHERE created_at > ... (no tenant filter) aggregates user messages across all tenants. Queries against tenant-scoped tables must be tenant-scoped to avoid cross-tenant data access.
  - _Fix:_ If the intent is per-tenant traffic gating, accept a tenantId and include AND tenant_id = ${tenantId}. If the metric must be global, make the global intent explicit and ensure access is restricted to platform-admin contexts; otherwise avoid scanning tenant-scoped tables without a tenant filter.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... lacks tenant scoping and will count across all tenants.
  - _Fix:_ Restrict to a tenant (AND tenant_id = ${tenantId}) for per-tenant deprecation logic, or explicitly document and gate global queries to admin-only code paths. Avoid unscoped reads of tenant-scoped tables.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant filter. This returns tool usage across all tenants.
  - _Fix:_ Filter by tenant_id (e.g. WHERE tenant_id = ${tenantId}) for per-tenant decisions. If global scope is intended, ensure it runs only in a platform-admin context and is audited.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant constraint and will surface data across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause for tenant-scoped operations, or move to an explicit admin/global path with appropriate access control.
- **server/heartbeat.ts:60** — SELECT from tenant-scoped table `messages` without constraining by tenant_id (SELECT DISTINCT tenant_id FROM messages ...).
  - _Fix:_ If this is intentionally enumerating tenants for a system-level sweep, make the intention explicit and restrict access to an admin-only code path; otherwise add a WHERE tenant_id = ${tenantId} (or use the storage API that enforces tenant context). Avoid blind cross-tenant selects unless explicitly audited/authorized.
- **server/heartbeat.ts:1723** — SELECT created_at FROM heartbeat_logs ... executed without a tenant_id filter — may read logs across tenants.
  - _Fix:_ If heartbeat_logs are tenant-scoped, add WHERE tenant_id = ${tenantId} (or use a tenant-aware storage function). If the read must be cross-tenant, make that explicit and limit who can call this path.
- **server/job-queue.ts:274** — cancelJob() updates agent_jobs WHERE id = ${id} AND status IN (...) without scoping by tenant_id.
  - _Fix:_ Require tenant context or verify the job's tenant before updating; include tenant_id = ${tenantId} in the WHERE clause.
- **server/job-queue.ts:283** — retryJob() updates agent_jobs WHERE id = ${id} ... with no tenant_id constraint, allowing cross-tenant mutation of job rows.
  - _Fix:_ Add tenant scoping: require tenantId parameter or validate the job's tenant and add tenant_id = ${tenantId} to the WHERE clause.
- **server/job-queue.ts:385** — getJobStats() aggregates counts from agent_jobs (SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > ...) without any tenant_id filter — returns global, cross-tenant counts.
  - _Fix:_ If this is intended to be tenant-specific, add a tenantId filter. If global stats are required, explicitly document and audit the global-read semantics; prefer separate tenant-scoped endpoint or explicit admin-only path.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor() begins by running a SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} with no caller-scoped tenant filter — it enumerates embeddings across all tenants without scoping.
  - _Fix:_ If per-tenant scanning is intended, schedule/drive this loop per-tenant and ensure each scoring/INSERT/notification is performed under that tenant's scope. Otherwise document/admin-signoff for cross-tenant background scans and harden audit controls.
- **server/lib/restraint-trust.ts:88** — Dynamic column name passed into SQL via sql.raw() is derived from the caller-provided `outcome` value (even though validated against a whitelist). Using sql.raw() with a runtime identifier introduces SQL-injection risk and weakens static auditability of tenant-scoped updates.
  - _Fix:_ Avoid sql.raw() for column identifiers derived from request data. Replace the dynamic UPDATE with an explicit, parameterized branch (switch on outcome) that issues one of four fixed, parameterized UPDATE statements (each referencing a literal column name). This removes the need for sql.raw() and keeps the statement fully parameterized and auditable.
- **server/lib/stuck-task-sweep.ts:56** — SELECT against agent_runs scans status='running' rows without a tenant_id WHERE clause, returning tenant_id in results but reading across all tenants. This touches a tenant-scoped table with no tenant filter.
  - _Fix:_ If this is intended to be a cross-tenant audit, require and document that it runs only under a privileged system account and ensure audit/authorization controls are in place. Otherwise add a tenant_id filter (WHERE tenant_id = ...) when called from a tenant context so tenant-scoped queries are never run unconstrained.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against mind_tickets reads rows (status='in_progress') without a tenant_id WHERE clause — querying a tenant-scoped table across tenants.
  - _Fix:_ As with agent_runs: either restrict this function to an explicitly authorized system context (with documentation and auditing) or add tenant-scoped filtering to the queries. Prefer explicit tenant_id WHERE clauses for tenant-scoped usages to preserve isolation.
- **server/memory-graph.ts:488** — Counting links: SELECT count(*) FROM memory_links ml JOIN memory_entries me ON ml.source_memory_id = me.id WHERE me.tenant_id = ${tenantId} reads memory_links without an explicit ml.tenant_id constraint. Relying on the join alone is brittle and may miss the explicit tenant predicate requirement.
  - _Fix:_ Also include explicit tenant scoping on memory_links, e.g. JOIN memory_entries me ON ml.source_memory_id = me.id AND ml.tenant_id = ${tenantId} or add WHERE ml.tenant_id = ${tenantId} in addition to me.tenant_id to make the intent explicit and consistent across queries.
- **server/routes.ts:1143** — Upload delivery middleware selects all file_storage owners for a basename with no tenant_id constraint (db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This performs an unscoped read of tenant-scoped rows.
  - _Fix:_ Avoid unscoped selects. If the goal is to check for any owner matching the authenticated tenant, query only for that tenant (where filename = X AND tenant_id = authedTenantId). If you must detect cross-tenant collisions, perform an explicit, auditable admin-only routine or limit the returned columns and ensure the operation is executed in a safe admin context; never perform arbitrary unscoped reads in request handlers.
- **server/routes/projects.ts:563** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is missing an explicit tenant_id constraint. Although the handler previously checked project ownership, the UPDATE statement lacks WHERE tenant_id = ${tenantId}.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure the update is always constrained to the caller's tenant.
- **server/routes/projects.ts:631** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is missing an explicit tenant_id constraint (multer upload handler).
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure tenant-scoped update and prevent accidental cross-tenant updates.
- **server/routes/projects.ts:702** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not include tenant_id in the WHERE clause. The handler did verify project ownership earlier, but the DELETE itself lacks an explicit tenant constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) or include tenant_id column on project_files and verify/insert it on creation to make queries explicitly tenant-scoped.
- **server/stability-watchdog.ts:177** — SELECT from heartbeat_logs omits tenant scoping. The watchdog queries heartbeat_logs for recent errors without a tenant_id filter, which may read across tenants.
  - _Fix:_ If the watchdog must operate across all tenants, make that explicit (e.g. use ALL_TENANTS APIs or document/centralize the global-scope intent). Otherwise, add a tenant_id filter to limit the scope to the intended tenant (e.g. WHERE tenant_id = ${tenantId}). Avoid implicit, un-scoped reads of tenant-scoped logs.
- **server/storage.ts:306** — Raw SELECT via db.execute(sql`... FROM messages WHERE id IN (...)`) queries tenant-scoped `messages` without scoping by tenant_id.
  - _Fix:_ Include tenant scoping when querying messages by id (e.g. add AND tenant_id = ${tenantId} or restrict the id list to only IDs previously validated for the caller's tenant). Prefer parameterized queries that include eq(messages.tenantId, tenantId) or use inArray over subqueries that can bypass tenant checks.
- **server/storage.ts:882** — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` with no tenant filter (platform-wide read).
  - _Fix:_ If embedding-worker must operate per-tenant, require and pass tenantId or run with an explicit ALL_TENANTS sentinel guarded to admin-only contexts. Otherwise include tenant scoping via tenantScope/requiredTenantScope before selecting rows.
- **server/storage.ts:889** — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` with no tenant filter (platform-wide read).
  - _Fix:_ Either require a tenant scope parameter (use requiredTenantScope) or ensure the caller is a trusted admin worker. Prefer explicit ALL_TENANTS sentinel for intentional platform-wide operations and gate that path to admin contexts.
- **server/storage.ts:999** — getHeartbeatTask selects heartbeat_tasks by id without tenant scoping (no tenant_id check).
  - _Fix:_ Add tenant scoping when callers are not trusted admin: require tenantId and include eq(heartbeatTasks.tenantId, tenantId) or use a subquery verifying ownership. If this helper is intentionally unscoped, clearly document and audit its callsites and require callers to validate tenant ownership first.
- **server/storage.ts:560** — getMemoryEntry can perform an unscoped read when tenantId is omitted (it warns but still allows a lookup by id without tenant constraint).
  - _Fix:_ Make the callsite require tenantId (use requireTenantScope/assertValidTenantId) where possible. If an unscoped lookup is required for orchestration, restrict and audit callsites that use it; otherwise add eq(memoryEntries.tenantId, tenantId) to the WHERE clause.
- **server/storage.ts:1056** — getDueHeartbeatTasks runs a raw SELECT over heartbeat_tasks (tenant-scoped) with no tenant filter — returns due tasks across all tenants.
  - _Fix:_ If the queue is intended to be global worker-driven, ensure the worker is a trusted admin process and that this path is gated. If tenant-specific processing is required, add tenant filtering or otherwise restrict the operation to a vetted admin/worker role and audit access.
- **server/storage.ts:1084** — claimHeartbeatTasks issues UPDATE heartbeat_tasks ... WHERE id = ${id} AND enabled = true AND next_run_at <= NOW() with no tenant filter — can claim tasks across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or ensure the claiming worker is authorized to operate across tenants and that appropriate admin guardrails/auditing are in place.
- **server/tools.ts:2990** — SELECT from project_files omits an explicit tenant constraint (WHERE tenant_id = ...) — query filters only by project_id/file_name/file_url. Without an explicit tenant filter this read could return rows belonging to another tenant unless the handler has already validated ownership.
  - _Fix:_ Constrain the SELECT to the caller's tenant. Either add AND tenant_id = ${tenantId} to the WHERE clause or join to projects and require p.tenant_id = ${tenantId}. Alternatively, ensure ownership is validated immediately before the SELECT and document that the query is safe; prefer the explicit tenant_id WHERE for clarity and defense-in-depth.
- **server/tools.ts:3030** — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} contains no tenant_id constraint. The query relies only on project_id/conversation_id to scope results.
  - _Fix:_ Add an explicit tenant constraint (AND tenant_id = ${tenantId}) or join to the projects table and ensure p.tenant_id = ${tenantId} (or otherwise ensure ownership was validated earlier in the handler). Prefer explicit tenant_id in the WHERE clause for clarity and to avoid subtle TOCTOU/assumption errors.

### LOW (1)

- **server/lib/system-probes.ts:163** — A probe issues db.select(count(*)) FROM messagesTable without a tenant_id filter, returning a global message count from a tenant-scoped table. This is a tenant-scoped table read performed without tenant scoping in SQL.
  - _Fix:_ If the probe is intentionally global (admin/system metric), annotate and gate this code path behind an explicit system-only capability and document the intent. If tenant-scoped usage is required, add a tenant_id WHERE filter or accept a tenant param and use eq(messagesTable.tenantId, tenantId) so calls from tenant contexts cannot accidentally read cross-tenant counts.

### Suppressed — confirmed false positives (199)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agent-activity.ts:118** _(internal-worker-pk)_ — completeActivity issues an UPDATE against agentActivity using only the activity id (WHERE eq(agentActivity.id, activityId)) with no tenant_id constraint. This allows updating rows belonging to other tenants if a caller supplies an arbitrary id.
  - _Why false positive:_ Server-side helper updates by serial PK only; no untrusted request boundary shown for activityId.
- **server/agentic-features.ts:1021** _(upstream-verified)_ — classifyReply updates outreach_enrollments with: UPDATE outreach_enrollments SET ... WHERE id = ${row.id} — the UPDATE lacks a tenant_id predicate. Although the code fetched the row earlier with a tenant-scoped query, the UPDATE should still include tenant scoping to prevent accidental cross-tenant updates if ids are confused or rows moved.
  - _Why false positive:_ Same call path selects enrollment with e.tenant_id = params.tenantId before updating that fetched row.id.
- **server/commitment-drafter.ts:234** _(system-sweep)_ — UPDATE against tenant-scoped table `commitments` without a tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This writes without ensuring the row belongs to the intended tenant and can affect another tenant's row by id.
  - _Why false positive:_ Platform maintenance loop processes claimed rows carrying tenantId; this fallback only resets housekeeping status on the current row.
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations executed without scoping by tenant_id. Even though conv was read earlier, the DELETE lacks an explicit WHERE tenant_id = <conv.tenant_id> predicate, allowing a row to be removed without the defense-in-depth tenant predicate.
  - _Why false positive:_ Global purge sweep iterates expired conversations carrying tenant_id and deletes one conversation by its own PK; no tenant-facing data mixing.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake updates `dfy_intake_forms` (.update(...).where(eq(dfyIntakeForms.id, form.id))) without constraining by tenant_id. This writes a tenant-scoped row based on a prior token lookup and can modify another tenant's row.
  - _Why false positive:_ Update is authorized by prior getDfyIntakeByToken(token) lookup of the exact form row in the same call path.
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE FROM doc_chunks is executed without constraining by tenant_id (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can delete chunks belonging to other tenants.
  - _Why false positive:_ Same function first verifies doc_collections ownership with id + tenant_id before deleting chunks for that collection_id.
- **server/dream-consolidation.ts:83** _(upstream-verified)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is run without also filtering by tenant_id. Reading messages by conversation_id alone risks returning messages from other tenants.
  - _Why false positive:_ Messages are read only for conversations already selected with tenant_id = tenantId in the same call path.
- **server/email.ts:529** _(system-sweep)_ — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} performs a write without a tenant_id constraint. This can modify rows belonging to other tenants.
  - _Why false positive:_ Backfill loop is a platform-wide maintenance task updating housekeeping/body fields on rows selected by PK, with no tenant-facing mixing.
- **server/embeddings.ts:811** _(system-sweep)_ — SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200 reads memory_entries without tenant scoping. This will enumerate rows across tenants.
  - _Why false positive:_ Boot/backfill maintenance sweep intentionally scans rows across tenants and only backfills housekeeping embedding fields.
- **server/embeddings.ts:821** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without tenant filtering, allowing writes to other tenants' rows.
  - _Why false positive:_ Per-row update in a global backfill loop mutates only embedding_vec housekeeping data and does not expose mixed tenant data.
- **server/embeddings.ts:827** _(system-sweep)_ — SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200 runs without tenant scoping, enumerating agent_knowledge across tenants.
  - _Why false positive:_ Global maintenance backfill over agent_knowledge is a housekeeping sweep, not a tenant-facing read path.
- **server/embeddings.ts:838** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint, permitting cross-tenant writes.
  - _Why false positive:_ Per-row update during system backfill only fills embedding_vec and does not mix tenant outputs.
- **server/google-drive.ts:685** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' uses only projects.id in WHERE (no tenant_id filter). Although the code previously fetched the project and checked ownership, the UPDATE itself lacks a tenant constraint and could be abused if ownership validation is bypassed or the row id is from an unexpected source.
  - _Why false positive:_ Same call path fetches project and explicitly checks project.tenantId === tenantId before this update.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs() issues an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) that contains no tenant_id constraint — reads and writes agent_jobs across all tenants unscoped.
  - _Why false positive:_ Multi-tenant worker claim loop is a platform-wide job dispatcher returning rows with their own tenant_id for per-job handling.
- **server/lib/ideabrowser-dev-sync.ts:140** _(fk-transitive)_ — INSERT into project_notes omits tenant scoping and the NOT EXISTS dedupe check is not tenant-scoped. This can insert or dedupe across tenants (cross-tenant leakage/poisoning).
  - _Why false positive:_ project_notes has no tenant_id and is tenant-isolated transitively through tenant-scoped parent project_id.
- **server/lib/ideabrowser-dev-sync.ts:151** _(fk-transitive)_ — INSERT into project_files omits tenant scoping and the NOT EXISTS dedupe check is not tenant-scoped. This can create cross-tenant file rows or allow one tenant to block another's insertions.
  - _Why false positive:_ project_files has no tenant_id and is tenant-isolated transitively through tenant-scoped parent project_id.
- **server/lib/inbox-ingest.ts:506** _(upstream-verified)_ — UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id} lacks tenant scoping. Without AND tenant_id = ${tenantId} this UPDATE could modify another tenant's classification row that shares the same id.
  - _Why false positive:_ Earlier in same path the row is inserted with this tenantId and uniqueness is on inbox_message_id, so update targets the claimed row from the same flow.
- **server/memory-graph.ts:253** _(fk-transitive)_ — INSERT INTO memory_links omits tenant_id. Tenant-scoped tables require tenant_id explicitly on INSERT; missing tenant_id will create links with no tenant or in wrong tenant.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries transitively by their foreign-keyed memory ids.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — Query selects from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id: `SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE` (startup validation reads all tenants' OAuth subscriptions).
  - _Why false positive:_ Startup validation intentionally sweeps all tenants' active subscriptions and processes each row with its own tenant_id; housekeeping/admin flow, not tenant-facing mixed output.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — Query selects from tenant-scoped table `oauth_subscriptions` without tenant filter: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'`. This returns rows for all tenants.
  - _Why false positive:_ Boot-time auto-reconnect enumerates google-workspace subscriptions across tenants and handles each tenant_id separately in a startup maintenance loop.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh() runs `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant constraint, thus reading subscription rows for all tenants.
  - _Why false positive:_ Proactive refresh is a platform-wide maintenance loop over active subscriptions; each row carries tenant_id and refreshes are done per-tenant without returning mixed tenant data.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions() runs `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE` without tenant scoping, exposing all tenants' subscriptions.
  - _Why false positive:_ Daily forced token refresh is an intentional cross-tenant cron/housekeeping sweep operating per subscription row using its tenant_id.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Claiming a plan: UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status='approved' RETURNING ... does not constrain by tenant_id. This can cause cross-tenant claiming/execution of plans.
  - _Why false positive:_ Server-side executor claims a plan by globally unique planId in an internal worker path, then uses returned tenant_id for downstream scoped events/work.
- **server/plan-executor.ts:728** _(upstream-verified)_ — Final status update on plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks tenant_id constraint — allows finalizing plans across tenants.
  - _Why false positive:_ This update occurs after the same call path already claimed and loaded the specific plan row at line 513, obtaining its tenant_id; it finalizes that already-verified plan.
- **server/plan-executor.ts:778** _(system-sweep)_ — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' with no tenant filtering: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This operates across all tenants.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping sweep resetting stale statuses only; it returns ids for internal requeueing and does not expose mixed tenant data.
- **server/plan-executor.ts:787** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is executed without tenant scoping and is used to kick off execution (executePlan) for each returned id — this can start plans from any tenant.
  - _Why false positive:_ Selecting approved plans at boot for internal dispatch is part of recovery/housekeeping across tenants; it only queues ids for per-plan execution, no tenant-facing data mixing.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates by id only and omits tenant_id. This is a mutating write on a tenant-scoped table without enforcing tenant ownership — risk of disabling another tenant's task if ids collide or are guessed.
  - _Why false positive:_ The task ids come from an immediately preceding SELECT constrained by ht.tenant_id = tenantId, so the update is acting on rows already verified in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} performs a write by id only, with no tenant_id filter. agent_desks are tenant-scoped and this can modify another tenant's desk row.
  - _Why false positive:_ The desk ids are sourced from the prior SELECT filtered by ad.tenant_id = tenantId, so ownership is enforced earlier in the same flow.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table agent_knowledge omits tenant_id constraint (UPDATE agent_knowledge SET ... WHERE id = ${r.id}). This allows modifying rows belonging to other tenants when IDs collide or if the runner is scanning globally.
  - _Why false positive:_ This is a global scheduler loop processing due rows and updating each row by its own carried id after tenant-scoped work using r.tenant_id; housekeeping update does not expose mixed tenant data.
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() executes without tenant_id scoping. agent_knowledge is tenant-scoped elsewhere; this DELETE will remove rows across all tenants.
  - _Why false positive:_ Platform-wide expiry purge deletes only expired autoresearch rows and does not return or mix tenant data; benign housekeeping sweep.
- **server/sculptor.ts:113** _(upstream-verified)_ — UPDATE sculptor_sessions sets conversation_id without constraining by tenant_id (WHERE id = ${session.id} only). This can update a session belonging to another tenant if the id is misused.
  - _Why false positive:_ Row was just inserted in the same call path and session.id comes from RETURNING * for params.tenantId, so updating that fresh row by its server-derived PK is ownership-proven earlier.
- **server/sculptor.ts:174** _(internal-worker-pk)_ — UPDATE sculptor_sessions updates status/result/tool_calls_count/tokens_used/completed_at with WHERE id = ${sessionId} but no tenant_id filter. This allows cross-tenant modification of sculptor_sessions rows.
  - _Why false positive:_ monitorSession is an internal timer worker updating a specific session by server-derived sessionId/runId, not from an untrusted request boundary.
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} updates cost/billing data by id only, with no tenant_id constraint. A reservationId could be misused to alter another tenant's ledger row.
  - _Why false positive:_ Internal helper settles a server-created reservation row by globally unique reservationId, not an untrusted cross-tenant request parameter.
- **server/second-opinion.ts:426** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd='0', operation='second_opinion:released' WHERE id = ${reservationId} does not constrain by tenant_id, permitting cross-tenant ledger modification.
  - _Why false positive:_ Internal helper releases a server-created reservation row by globally unique reservationId, not from an untrusted request boundary.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' queries heartbeat_tasks without tenant_id. If heartbeat_tasks is tenant-scoped this reads across tenants.
  - _Why false positive:_ Boot-time runaway-task cleanup intentionally scans all heartbeat_tasks and only mutates housekeeping fields/counts; no tenant data is returned or mixed.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks without a tenant_id filter, potentially disabling delegation tasks across all tenants.
  - _Why false positive:_ Boot-time housekeeping intentionally disables runaway heartbeat tasks across tenants; mutates only status field and does not expose tenant data.
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' queries a tenant-scoped table without tenant_id, allowing cross-tenant reads.
  - _Why false positive:_ Global maintenance count over heartbeat_tasks for cleanup purposes only; aggregate count only, no tenant data returned to tenant-facing surface.
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true executes without tenant_id, which may disable tasks for all tenants.
  - _Why false positive:_ Boot-time cleanup intentionally disables matching heartbeat tasks across tenants; housekeeping/status-only mutation.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) performs an unscoped query against heartbeat_tasks (no tenant_id), enabling cross-tenant inspection.
  - _Why false positive:_ Global maintenance count used to identify orphaned repo-examination tasks; aggregate-only read with no tenant content mixing.
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true updates heartbeat_tasks without a tenant_id filter, risking cross-tenant modifications.
  - _Why false positive:_ Platform-wide cleanup of orphaned heartbeat tasks is housekeeping-only and intentionally cross-tenant.
- **server/seed.ts:3910** _(system-sweep)_ — DELETE FROM tenant-scoped table `heartbeat_tasks` executed without a tenant filter. This DELETE will remove matching rows across all tenants.
  - _Why false positive:_ Platform-wide purge of stale auto-disabled heartbeat tasks is boot-time housekeeping and does not expose tenant data.
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — UPDATE actual_outcome_embedding on tenant-scoped table 'felix_proposals' executed without a tenant_id constraint (WHERE id = ${proposalId} only).
  - _Why false positive:_ Same call path previously SELECTs felix_proposals by id and tenant_id, so this update operates on a row already ownership-verified.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE custom_tools SET implementation = fixedImpl WHERE id = ${tool.id} performed without tenant_id constraint — this mutates tenant-scoped data without enforcing tenant ownership.
  - _Why false positive:_ server-internal auto-fix path mutates a custom_tools row already loaded as tool; write is by server-side PK, not direct untrusted request input.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE FROM custom_tools WHERE id = ${tool.id} executed without tenant_id constraint in revertToolRow — deletes tenant-scoped data without verifying tenant ownership.
  - _Why false positive:_ revert helper deletes the just-inserted/validated tool row by server-held PK in an internal worker flow, not from a cross-tenant request boundary.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE custom_tools to increment usageCount uses only id in WHERE (eq(custom_tools.id, tool.id)) and lacks tenant_id — a mutating write to a tenant-scoped table without tenant scoping.
  - _Why false positive:_ executeCustomTool first selects custom_tools by name and tenant_id=${tenantId}; subsequent usageCount update applies to that verified row id.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` uses WHERE id = ${reservationId} with NO tenant_id constraint (settleVentureReservation). This can modify rows belonging to other tenants if reservationId is guessed or leaked, breaking tenant isolation and integrity.
  - _Why false positive:_ best-effort settlement of a reservation row by server-generated reservationId in an internal budget workflow; not taken from req/body/tool args here.
- **server/venture-discovery/budget.ts:170** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` uses WHERE id = ${reservationId} with NO tenant_id constraint (releaseVentureReservation). This can release/alter another tenant's reservation by id alone.
  - _Why false positive:_ release path updates a reservation row by server-generated reservationId in internal workflow, not an untrusted request boundary.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments performs a SELECT on experiments without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads experiments across all tenants.
  - _Why false positive:_ heartbeat-callable global sweep intentionally scans running experiments across tenants and then handles each row with its own tenant_id on update/queueing.
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting selects from ai_insights without constraining tenant_id (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50). This reads insights across tenants.
  - _Why false positive:_ durability sweep intentionally processes pending insights across tenants, carrying each row's tenant_id forward per-row and not returning mixed tenant data externally.
- **server/agentic-engines.ts:247** _(system-sweep)_ — autoApplyEligibleInsights performs an UPDATE on ai_insights that, when called with tenantId undefined, runs across all tenants (WHERE status = 'new' AND category IN (...) ${tenantScope}). Unscoped UPDATE on tenant-scoped table can mutate rows for other tenants.
  - _Why false positive:_ backfill sweep intentionally auto-applies eligible insights across tenants; it returns rows only for internal per-row routing using each row's tenant_id.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals to set status='expired' for pending approvals whose expiresAt < now(), but the UPDATE has no tenant_id filter. This updates approvals across all tenants in a single statement.
  - _Why false positive:_ expiry routine is a platform-wide housekeeping sweep; bulk update only flips status/decidedAt and subsequent run updates use each returned row's tenantId.
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — Unscoped cross-tenant SELECT on tenant-scoped tables: the candidate query (db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`)) reads agent_runs / agent_approvals without constraining to a single tenant (no WHERE r.tenant_id = <tenantId>). This allows a global worker to enumerate/observe rows across tenants.
  - _Why false positive:_ Global maintenance sweep processes rows with per-row tenant_id and does not return mixed tenant data to tenant-facing surfaces.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on agent_wake_schedules (claiming due wakes) uses an inner SELECT that lacks any tenant_id constraint — the statement can claim/modify rows across tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due rows across tenants, then handles each row with its own tenant_id and emits per-tenant events.
- **server/auth.ts:1239** _(system-sweep)_ — Global maintenance DELETE: DELETE FROM auth_sessions WHERE expires_at < ${now} is executed without tenant_id scoping, affecting auth_sessions across all tenants.
  - _Why false positive:_ Expired-session cleanup is platform-wide housekeeping deleting stale rows only, with no tenant data returned or mixed.
- **server/channel-routing.ts:30** _(global-table)_ — SELECT joins `personas` and reads `channel_routes` with no tenant_id constraint — this can expose personas from other tenants.
  - _Why false positive:_ channel_routes is created here as a singleton global routing table with UNIQUE(channel), and referenced personas are noted as non-tenant-scoped.
- **server/channel-routing.ts:48** _(global-table)_ — INSERT/DELETE against `channel_routes` performed without any tenant scoping and INSERT uses caller-supplied personaId without verifying tenant ownership.
  - _Why false positive:_ Writes target the singleton global channel_routes table by design; schema here shows no tenant scoping and personas are non-tenant-scoped.
- **server/channel-routing.ts:63** _(global-table)_ — SELECT persona_id FROM channel_routes WHERE channel = ${channel} has no tenant filter — returns tenant-ambiguous persona ids.
  - _Why false positive:_ Lookup reads from the singleton global channel_routes table by channel; no tenant-scoped table is shown.
- **server/channel-routing.ts:71** _(global-table)_ — DELETE FROM channel_routes WHERE channel = ${channel} has no tenant constraint — can remove another tenant's routing.
  - _Why false positive:_ Delete operates on the intentionally global channel_routes mapping keyed uniquely by channel.
- **server/commitment-drafter.ts:146** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` without scoping to a specific tenant (no WHERE tenant_id = ...). The background job enumerates tenant IDs by scanning all rows in the tenant-scoped table.
  - _Why false positive:_ Code explicitly performs a platform-wide maintenance pass, enumerates tenant ids, and all subsequent mutations are per-tenant scoped.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` without scoping to a specific tenant (no WHERE tenant_id = ...). The heartbeat scanner reads all overdue commitments across tenants.
  - _Why false positive:_ Boot/interval heartbeat scanner intentionally scans all tenants, carries row.tenantId per record, and only emits redacted admin digest/housekeeping.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE/UPDATE-like write to doc_chunks (UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}) is executed without a tenant_id constraint. Updating by id alone can modify another tenant's row.
  - _Why false positive:_ Rows being updated were selected immediately above with collection_id and tenant_id filters, so row.id values are pre-scoped in the same call path.
- **server/email.ts:471** _(system-sweep)_ — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without filtering by tenant_id. inbox_messages is tenant-scoped (tenant_id is used on INSERT), so this read may touch other tenants' inbox rows.
  - _Why false positive:_ Shared-inbox poller is a platform-wide ingestion loop checking global message_id dedupe and not returning tenant data to tenant-facing output.
- **server/email.ts:516** _(system-sweep)_ — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... is run without tenant scoping, exposing messages for all tenants.
  - _Why false positive:_ Backfill job is a maintenance sweep across inbox_messages that only fills housekeeping/body fields per row and does not expose mixed tenant data.
- **server/firecrawl.ts:239** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' uses only id in WHERE (no tenant_id filter). This UPDATE can modify another tenant's row if existing[0].id is not re-validated in the same statement.
  - _Why false positive:_ existing[0].id comes from an immediately prior SELECT constrained by url and tenantId in the same function.
- **server/firecrawl.ts:372** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' uses only id in WHERE (no tenant_id filter) inside the crawl loop. Same pattern as above — the update can affect another tenant's row.
  - _Why false positive:_ existing[0].id is sourced from the prior tenant-scoped SELECT within the same crawl-loop iteration.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM tenant-scoped table `code_proposals` is executed with no tenant_id filter (reads all tenants).
  - _Why false positive:_ Health-audit TTL scanner is a platform-wide maintenance sweep selecting stale proposals for audit findings/housekeeping, not tenant-facing data access.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on `code_proposals` (setting status = 'archived_stale') uses id list but no tenant_id constraint — can modify rows across tenants.
  - _Why false positive:_ Global maintenance sweep archives stale proposals by collected ids as housekeeping status changes, with no tenant content mixing to users.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM `heartbeat_tasks` is executed without any tenant_id restriction, returning tasks across all tenants.
  - _Why false positive:_ Health-audit scans heartbeat_tasks globally as maintenance to detect/disable stale tasks; per-row housekeeping only, not tenant-facing data exposure.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) executes without constraining tenant_id — this can disable tasks for other tenants.
  - _Why false positive:_ Boot/admin-style housekeeping sweep intentionally disables stale heartbeat rows across tenants and only mutates housekeeping state, returning aggregate findings.
- **server/google-workspace.ts:2585** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint — this updates a session row by id without verifying it belongs to the caller's tenant.
  - _Why false positive:_ Updates presenter_sessions by server-created sessionId returned from internal presenter-session creation, not from an untrusted request boundary.
- **server/heartbeat.ts:105** _(system-sweep)_ — SELECT * FROM research_schedules executes without any tenant_id constraint — reads schedules across all tenants.
  - _Why false positive:_ Heartbeat cron intentionally scans due research_schedules across all tenants and processes each row with its own sched.tenant_id.
- **server/heartbeat.ts:155** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — updates tenant-scoped table row by id but without verifying/upholding tenant_id.
  - _Why false positive:_ Housekeeping update within cross-tenant schedule sweep; row was selected from research_schedules and is advanced per-row using its carried tenant context.
- **server/heartbeat.ts:160** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Housekeeping update within cross-tenant schedule sweep; only last_run_at/next_run_at fields are advanced for the selected row.
- **server/heartbeat.ts:165** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant filtering on a tenant-scoped table.
  - _Why false positive:_ Cross-tenant maintenance loop advancing schedule timing per selected row; no tenant data is returned or mixed.
- **server/heartbeat.ts:176** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} inside the error recovery path — no tenant_id constraint.
  - _Why false positive:_ Error-recovery housekeeping update in the same platform-wide schedule sweep, mutating only timing metadata for the current row.
- **server/heartbeat.ts:325** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — update on tenant-scoped `heartbeat_tasks` without tenant_id in WHERE clause.
  - _Why false positive:_ Startup cleanup normalizes heartbeat task model across tenants as maintenance, mutating only housekeeping/config fields.
- **server/heartbeat.ts:329** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant constraint on tenant-scoped table.
  - _Why false positive:_ Startup maintenance pass normalizes reflection task model across tenants, only updating config metadata.
- **server/heartbeat.ts:439** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... executed without tenant_id — the code will pull pending events across all tenants.
  - _Why false positive:_ System worker intentionally pulls pending events across tenants and routes each event using its own tenant_id without aggregating tenant content together.
- **server/heartbeat.ts:2816** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — raw UPDATE without tenant_id restriction after creating a task.
  - _Why false positive:_ newTask was just created via storage.createHeartbeatTask with the same tenantId in this call path; subsequent update targets that freshly created row by returned id.
- **server/heartbeat.ts:3269** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant check (same pattern as above in delegateTaskFromChat).
  - _Why false positive:_ Row is created immediately above with tenantId and update targets returned newTask.id from that same insert; not request-chosen.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases() updates agent_jobs (both terminal and re-queue branches) without any tenant_id filtering — this mutates rows across tenants.
  - _Why false positive:_ Global lease-reclamation maintenance intentionally processes expired jobs across tenants and only mutates housekeeping/status fields per row.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob() updates agent_jobs WHERE id = ${id} ... but does not verify tenant_id, allowing an unscoped update of job rows.
  - _Why false positive:_ Internal worker completion by server-generated job PK plus attempt fencing; id is not shown as coming from an untrusted tenant request boundary.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob() performs a SELECT max_attempts FROM agent_jobs WHERE id = ${id} and later UPDATE agent_jobs ... WHERE id = ${id} with no tenant_id constraint — both read and write are unscoped.
  - _Why false positive:_ Internal worker failure handling reads/updates by server-generated job PK with attempt fencing, not a user-supplied cross-tenant boundary.
- **server/lib/aevo-meta-editor.ts:449** _(upstream-verified)_ — UPDATE procedure_evolution_runs uses WHERE id = ${runId} without also constraining tenant_id. Later updates reference the run row by id only, permitting cross-tenant modification if runId is mis-used.
  - _Why false positive:_ runId comes from the insert of procedure_evolution_runs in the same function with tenantId set, so subsequent status update targets that just-created row.
- **server/lib/aevo-meta-editor.ts:459** _(upstream-verified)_ — UPDATE procedure_evolution_runs sets status='done' WHERE id = ${runId} with no tenant_id predicate, allowing cross-tenant updates.
  - _Why false positive:_ runId is the id returned from the same function's tenant-scoped insert, so this done update is tied to that newly created row.
- **server/lib/aevo-meta-editor.ts:502** _(upstream-verified)_ — UPDATE procedure_evolution_runs .where(eq(procedureEvolutionRuns.id, runId)) executed without tenant constraint; tenant_id is omitted from the predicate.
  - _Why false positive:_ runId originates from the same newly inserted tenant-scoped run row earlier in this call path; update is not keyed from caller input.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped table `projects` is executed without constraining to a caller tenant (no WHERE tenant_id = ...). This query returns cross-tenant counts and can leak tenant-scoped information across tenants.
  - _Why false positive:_ boot/backfill job intentionally scans counts across tenants for maintenance and only uses per-tenant aggregate counts, not tenant-facing mixed row data
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — Global read from tenant-scoped table deliverable_grade_events without a tenant_id filter (SELECT tenant_id, format, count(*) ... FROM deliverable_grade_events ...). This reads all tenants' grade events in one query, violating the requirement that every read against tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ nightly optimizer scans all tenants' grade-event aggregates and then processes each tenant separately; this is platform-wide maintenance, not tenant-facing mixed output
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT (and subsequent UPDATE via CTE) queries read/lock rows from scheduled_posts without any tenant_id WHERE filter. This operates across tenants in a tenant-scoped table without an application-level tenant filter.
  - _Why false positive:_ global dispatcher intentionally claims due scheduled_posts across tenants and carries each row's tenant_id forward for per-row handling
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the row id in its WHERE clause (eq(tenantVoiceProfiles.id, existing.id)) and does not constrain by tenant_id. This allows a caller who can influence `existing.id` (or an id reuse scenario) to update a row belonging to a different tenant.
  - _Why false positive:_ existing row is fetched immediately above via getVoiceProfile({ tenantId: opts.tenantId, profileName }), so existing.id is tenant-owned in this call path
- **server/memory-graph.ts:240** _(fk-transitive)_ — SELECT FROM memory_links to check for an existing link does not include tenant filtering (no tenant_id constraint). Reading a tenant-scoped memory_links table without tenant scoping can return cross-tenant rows.
  - _Why false positive:_ schema note says memory_links has no tenant_id; links are between tenant-scoped memory_entries selected from one tenant, so isolation is transitive via the referenced memories
- **server/memory-graph.ts:276** _(fk-transitive)_ — Query selecting linked memories reads memory_links (ml) without constraining ml.tenant_id. The WHERE filters the joined memory_entries row by tenant but the memory_links table itself is not explicitly tenant-scoped in the query.
  - _Why false positive:_ memory_links has no tenant_id and joined memory_entries row is tenant-filtered when tenantId is provided; child/link table is isolated transitively through tenant-scoped memory_entries
- **server/memory-graph.ts:528** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is missing tenant_id constraint. This may update a row in a different tenant if ids collide.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries scoped by tenant_id = tenantId in the same function/loop.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} lacks tenant scoping. The contradictions loop updates triple rows by id only, risking cross-tenant mutation.
  - _Why false positive:_ c.id is selected immediately above from knowledge_triples with tenant_id = tenantId, so the update is row-scoped from a tenant-filtered same-path query.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files omits tenant_id even though tenant context is available (options.tenantId) and assertProjectInTenant was used only to validate ownership.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; assertProjectInTenant validates the parent project belongs to the tenant.
- **server/mpeg-engine.ts:1436** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The parallel-produce code inserts into project_files without passing tenant_id, risking writes outside the caller's tenant scope.
  - _Why false positive:_ project_files has no tenant_id column and is isolated transitively via project_id to the tenant-scoped project, with assertProjectInTenant check before insert.
- **server/persona-export.ts:39** _(global-table)_ — Query for `skills` uses `WHERE persona_id = ${personaId} OR persona_id IS NULL` with no tenant constraint. If `skills` is tenant-scoped, this can leak other tenants' skill rows.
  - _Why false positive:_ skills schema shows no tenant_id column; query intentionally combines persona-specific and global (persona_id IS NULL) skills from a non-tenant-scoped table.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} — missing tenant_id constraint when marking a plan failed.
  - _Why false positive:_ Same executePlan call previously claimed and loaded this exact plan row via UPDATE ... RETURNING id, tenant_id for the planId being mutated.
- **server/process-governor.ts:121** _(fk-transitive)_ — Nested SELECTs against heartbeat_logs omit tenant scoping (e.g. the subqueries computing successes/failures for ht.id). heartbeat_logs appears tenant-scoped but the subqueries lack AND tenant_id = ${tenantId}. This can return/act on other tenants' data.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is keyed by task_id to heartbeat_tasks, which is tenant-scoped in the outer query.
- **server/process-governor.ts:198** _(global-table)_ — SELECT AVG(duration_ms) FROM heartbeat_logs lacks a tenant filter. heartbeat_logs is tenant-scoped elsewhere; without tenant_id = ${tenantId} this aggregates across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id column, so this aggregate is over a non-tenant-scoped/global log table.
- **server/process-governor.ts:239** _(global-table)_ — cascading_failures query SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs omits tenant scoping (no tenant_id filter), allowing cross-tenant aggregation.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is described as platform-global heartbeat logging; aggregate is on a global table by design
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE research_sessions ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold} runs without a tenant_id constraint. research_sessions is tenant-scoped elsewhere; this global update may change session state for other tenants.
  - _Why false positive:_ cleanupZombieSessions is a platform-wide stale-session housekeeping sweep that only updates status fields and returns ids/count for logging
- **server/routes.ts:4482** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant scoping; tenant_id is required for tenant-scoped tables (NOT NULL, no default).
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; project_id comes from conversation already verified for tenant
- **server/routes/conversations.ts:99** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table `conversations`: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` has no tenant_id constraint.
  - _Why false positive:_ conversation row was just server-created with tenantId in the same flow, so updating by its fresh PK is tenant-owned in this call path
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into `project_conversations` omitted tenant context: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and links a tenant-verified project to a freshly created tenant-owned conversation
- **server/routes/conversations.ts:105** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table `projects`: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` has no tenant_id constraint.
  - _Why false positive:_ project ownership was verified immediately above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_messages` is executed without a tenant_id constraint in the WHERE clause (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). This violates the app-level tenant isolation invariant—every write to a tenant-scoped table must include a tenant predicate.
  - _Why false positive:_ message was selected just above by id and tenant_id before being marked read, so ownership is established in the same call path
- **server/routes/projects.ts:516** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id even though project_notes is tenant-scoped.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; project ownership is verified immediately above
- **server/routes/projects.ts:556** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' (files-base64 handler) does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; project ownership is verified immediately above
- **server/routes/projects.ts:623** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' (multer upload handler) does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is scoped through tenant-checked parent projects.id
- **server/routes/projects.ts:399** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not include tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project/conversation created in same tenant-scoped transaction
- **server/routes/stats.ts:54** _(upstream-verified)_ — Drizzle sql template literal interpolates request-derived tenantId in the WHERE clause (s`${conversations.tenantId} = ${tenantId}`) instead of using a typed/parameterized Drizzle predicate. This is a string-interpolated SQL usage with a request-derived value (injection + tenant-isolation risk).
  - _Why false positive:_ query constrains conversations by request-derived tenantId in WHERE; sql template here is still tenant-scoped
- **server/routes/stats.ts:55** _(upstream-verified)_ — Drizzle sql template literal contains a subquery referencing a request-derived tenantId (s`${messages.conversationId} IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`). This is string-interpolated SQL with a request-derived value, creating injection and isolation risk.
  - _Why false positive:_ subquery explicitly filters conversations by request-derived tenantId before counting messages
- **server/routes/stripe-checkout.ts:100** _(global-table)_ — Raw/template SQL (db.execute(sql`... WHERE pr.id = ${priceId} ...`)) interpolates request-derived priceId and isAnonymous into the sql`` template. This is string-interpolated SQL using values from the request, risking injection and making tenant/authorization checks harder to audit.
  - _Why false positive:_ stripe prices/products lookup is against platform-global catalog tables, not tenant-scoped data
- **server/seed.ts:38** _(system-sweep)_ — SELECT from tenant-scoped table research_sessions without tenant filter (SELECT COUNT(*) FROM research_sessions). This reads across all tenants.
  - _Why false positive:_ boot-time seed/import check intentionally operates across all tenants and only reads aggregate count for housekeeping
- **server/seed.ts:2160** _(system-sweep)_ — SELECT from tenant-scoped table research_programs without tenant filter (SELECT id, name, model FROM research_programs WHERE is_active = true). This can return rows for all tenants.
  - _Why false positive:_ boot-time maintenance fixes invalid model ids across all tenants without returning tenant data or mixing content
- **server/seed.ts:2518** _(system-sweep)_ — SELECT from conversations (tenant-scoped table) without tenant filter (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). This can enumerate conversations across tenants.
  - _Why false positive:_ boot-time backfill scans all conversations to restore missing link rows and only performs per-row housekeeping inserts
- **server/seed.ts:3938** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` performed without constraining to a tenant (no WHERE tenant_id = ...). This updates rows across all tenants.
  - _Why false positive:_ global maintenance normalizer updates memory text in place across tenants and does not expose mixed tenant data
- **server/seed.ts:3964** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` performed without constraining to a tenant (no WHERE tenant_id = ...). This will modify conversations for all tenants.
  - _Why false positive:_ global backfill updates legacy model ids across tenants as housekeeping only
- **server/seed.ts:4006** _(system-sweep)_ — DELETE dedupe on `file_storage` (DELETE FROM file_storage a USING file_storage b ...) operates across all tenants. Although it compares a.tenant_id = b.tenant_id, it lacks a fixed tenant_id value and will run for every tenant in the DB.
  - _Why false positive:_ dedupe is an intentional platform-wide maintenance pass and remains row-scoped by tenant_id while only deleting duplicate durability rows
- **server/seed.ts:4223** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` executed by id only (WHERE id = ...) without also constraining tenant_id. This can update rows owned by other tenants if ids collide or are guessed.
  - _Why false positive:_ UPDATE id comes from immediately preceding SELECT constrained to tenant_id=1, so the row ownership is established in the same call path.
- **server/self-reflection.ts:366** _(upstream-verified)_ — storeLesson performs an UPDATE on `memory_entries` using WHERE id = <row id> with no tenant_id constraint. Updating by id alone can modify rows belonging to another tenant.
  - _Why false positive:_ The updated id is selected a few lines above from memory_entries with WHERE tenant_id = tenantId, establishing same-row tenant ownership first.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from messages lacks tenant constraint — messages is a tenant-scoped table but the query uses only conversationId (no tenant_id WHERE clause). This can cause cross-tenant data disclosure if conversationId is passed from an untrusted caller.
  - _Why false positive:_ This function first calls analyzeConversation(conversationId, tenantId), which verifies the conversation belongs to the tenant before this messages-by-conversationId read.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from messages in analyzeConversation() has no tenant_id constraint — messages is read by conversationId without explicitly constraining tenant. Per policy every read against a tenant-scoped table must include tenant_id.
  - _Why false positive:_ The same function fetches the conversation with id and tenant_id just above, proving ownership before reading child messages for that conversation.
- **server/skillify.ts:221** _(global-table)_ — SELECT persona row by id without tenant constraint. personas is likely tenant-scoped; fetching by persona id alone may expose another tenant's persona data.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so lookup by id is not a tenant-scoped access.
- **server/skillify.ts:329** _(upstream-verified)_ — Another recentMsgs SELECT (in skillifyConversation) reads messages by conversationId without tenant_id filtering. This duplicates the same tenant-isolation risk (cross-tenant read).
  - _Why false positive:_ skillifyConversation calls analyzeConversation(conversationId, tenantId) first, which validates the conversation's tenant before this follow-on messages read.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without tenant constraint. This deletes tenant-scoped heartbeat_tasks across all tenants (no WHERE tenant_id), risking cross-tenant data mutation/loss.
  - _Why false positive:_ This is a platform-wide housekeeping purge of stale disabled tasks; it only deletes old rows and returns aggregate counts, with no tenant data exposure or mixing.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log executed without tenant filter. This will remove event_log rows across tenants (no WHERE tenant_id) potentially causing cross-tenant data loss.
  - _Why false positive:_ This is a global cleanup of old processed/failed event logs for housekeeping only, returning counts and not exposing cross-tenant content.
- **server/storage.ts:256** _(upstream-verified)_ — Unscoped SELECT from tenant-scoped table `conversations` (getConversationUnscoped) — no tenant_id constraint in WHERE.
  - _Why false positive:_ The function is explicitly unscoped-by-design for orchestration to derive tenantId from the row, with documented downstream validation responsibility and audit logging.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from custom_tools filters only on name (eq(custom_tools.name, parsed.name)) and lacks an explicit tenant_id constraint. Reads against a tenant-scoped table must be tenant-scoped.
  - _Why false positive:_ name is transformed with tenantToolName(parsed.name, tenantId), making lookup tenant-derived in the same call path
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT of the just-inserted custom_tools row uses only id in WHERE clause (eq(custom_tools.id, inserted.id)) without tenant_id. Reading tenant-scoped tables by id must also verify tenant ownership.
  - _Why false positive:_ re-reads the row just inserted in the same server flow by server-generated inserted.id, not from an untrusted request boundary
- **server/subagents.ts:265** _(global-table)_ — storage.getPersona(agentId) is called without passing or verifying tenant context. If persona rows are tenant-scoped this reads a referenced row without confirming caller ownership.
  - _Why false positive:_ personas schema shown elsewhere is not tenant-scoped, so getPersona is reading a global table
- **server/tools.ts:2995** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. Tenant-scoped tables must have tenant_id supplied on INSERT to enforce isolation at the application layer.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped through previously validated tenant-owned projects row
- **server/tools.ts:3010** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. Tenant-scoped tables must be explicitly stamped with the caller's tenant on write.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through previously validated tenant-owned projects row
- **server/tools.ts:2892** _(fk-transitive)_ — INSERT into project_conversations (during project creation link) does not set tenant_id. Writing tenant-scoped relationship rows without tenant_id allows cross-tenant contamination if a row's tenant is not enforced elsewhere.
  - _Why false positive:_ project_conversations has no tenant_id and insert only occurs after tenant-scoped conversation/project ownership checks
- **server/tools.ts:3033** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) does not include tenant_id. New relationship rows are inserted without an explicit tenant stamp.
  - _Why false positive:_ project_conversations has no tenant_id by schema and is transitively scoped via validated tenant-owned project and conversation checks before insert
- **server/tools.ts:6944** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' without setting tenant_id. The raw SQL INSERT into project_files does not include tenant_id even though tenant isolation is enforced at application level and INSERTs must supply tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id by schema and insert is gated by assertProjectInTenant on the parent project
- **server/tools.ts:8910** _(upstream-verified)_ — UPDATE against tenant-scoped table 'proposed_skills' without constraining by tenant_id. The db.update(...).where(eq(proposedSkills.id, row.id)) call lacks a tenant filter, allowing cross-tenant updates if IDs collide or are guessed.
  - _Why false positive:_ update targets row.id returned from the immediately preceding insert in the same flow, so ownership is already bound to the newly created tenant-scoped row
- **server/tools.ts:8952** _(upstream-verified)_ — UPDATE against tenant-scoped table 'proposed_skills' without constraining by tenant_id. The update that marks a proposal 'accepted' uses only proposedSkills.id in the WHERE clause.
  - _Why false positive:_ update uses row.id from the proposal just inserted earlier in the same call path for this tenant-scoped operation
- **server/tools.ts:8969** _(upstream-verified)_ — UPDATE against tenant-scoped table 'proposed_skills' without constraining by tenant_id. The update that marks a proposal 'rejected' uses only proposedSkills.id in the WHERE clause.
  - _Why false positive:_ reject update also uses row.id from the same newly created tenant-owned proposed_skills row in this call path
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' omits tenant constraint — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} has no tenant_id check.
  - _Why false positive:_ workflow is fetched immediately above with tenant_id = tenantId, and the update uses that fetched workflow.id
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' omits tenant constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} has no tenant_id filter.
  - _Why false positive:_ projId comes from a prior SELECT joining conversations to projects with p.tenant_id = tenantId, so the project ownership was verified earlier in the same path
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE statement updates knowledge_triples rows without scoping by tenant_id (WHERE id = ${row.id} only). This violates the tenant-isolation invariant that every UPDATE/DELETE/SELECT against tenant-scoped tables must include tenant_id in the WHERE clause.
  - _Why false positive:_ row.id values being updated are sourced from the preceding SELECT on knowledge_triples constrained by tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` executes without a tenant_id filter (no WHERE tenant_id = ...)
  - _Why false positive:_ provider_keys has no tenant_id by schema and this platform-wide read is explicitly gated to admin tenant only
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM `provider_keys` performed with no tenant_id constraint (reads across tenants)
  - _Why false positive:_ provider_keys is schema-global with no tenant_id and enumeration is explicitly restricted to admin tenant
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} — update is not constrained by tenant_id (no WHERE tenant_id = ${tid}) even though memory_entries is tenant-scoped
  - _Why false positive:_ id is taken from the immediately preceding SELECT on memory_entries constrained by tenant_id, persona_id, and category
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET last_accessed = NOW() ... WHERE id = ANY(${ids}) — update uses ids array without tenant_id constraint
  - _Why false positive:_ ids array is derived solely from rows selected just above with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries SET fact = ${fact} ... WHERE id = ${id} in recordStrategicWinHandler — missing tenant_id constraint
  - _Why false positive:_ id comes from prior SELECT on memory_entries scoped by tenant_id and persona_id in the same call path
- **server/video-job-runner.ts:272** _(system-sweep)_ — Query against tenant-scoped table `videoJobs` without a tenant_id constraint (listStaleVideoJobs selects jobs across all tenants). This reads tenant-owned rows without restricting to a caller/owner tenant.
  - _Why false positive:_ cross-tenant stale-job recovery sweep processes rows individually and uses each row's tenantId when claiming/resuming
- **server/webhook-triggers.ts:60** _(global-table)_ — SELECT joins the `personas` table (a tenant-scoped table) without constraining persona rows by tenant. listTriggers() LEFT JOINs personas ON p.id = w.persona_id and returns persona_name but does not ensure the persona belongs to the same tenant context — potential cross-tenant data exposure.
  - _Why false positive:_ webhook_triggers is created here without tenant_id and appears intentionally platform-global; personas schema note says no tenant_id
- **server/webhook-triggers.ts:122** _(global-table)_ — processTriggerEvent() selects webhook trigger rows and LEFT JOINs `personas` to expose persona_name but does not validate that the referenced persona belongs to the same tenant. Reading tenant-scoped persona rows without a tenant filter can leak other tenants' persona data.
  - _Why false positive:_ webhook_triggers lookup is by global webhook key from a platform-global table; joined personas schema note shows no tenant_id
- **server/webhook-triggers.ts:81** _(global-table)_ — createTrigger() takes a personaId from caller input and writes it into the global webhook_triggers table without validating that the caller (or admin context) actually owns or is allowed to reference that persona. This writes a foreign id referencing a tenant-scoped row without an ownership check.
  - _Why false positive:_ write targets webhook_triggers, a table defined here without tenant_id and apparently intended to be platform-global
- **server/chat-engine.ts:4551** _(internal-worker-pk)_ — Call to storage.attachCitations(messageId, collectedCitations) omits tenant context. This likely performs INSERTs against a tenant-scoped citations/messages-related table without explicitly providing tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/chat-engine.ts:4332 — assistantMsg.id is server-generated two lines above (chat-engine.ts:4329, created WITH tenantIdForScope); attachCitations updates only that fresh id — never caller input.
- **server/embeddings.ts:778** _(system-sweep)_ — UPDATE ${sql.raw(table)} SET embedding_vec = ... WHERE id = ${id} updates tenant-scoped tables (memory_entries / agent_knowledge) by id only, with no tenant filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/embeddings.ts:684 — storeEmbeddingVec only rewrites the embedding_vec/embedding columns of a row from that row's OWN content (recomputed vector), injecting no cross-tenant data. Every caller passes an internally-generated PK: freshly inserted knowledge/memory ids (storage.ts:806/818, research-engine.ts:508/1046) or ids from server-side backfill/dream-consolidation queries (embeddings.t
- **server/felix-brain.ts:19** _(system-sweep)_ — rehydrateTaskStateOnBoot executes a global query against the tenant-scoped table `agent_runs` without constraining by tenant_id (SELECT ... FROM agent_runs WHERE status IN ...). This reads rows for all tenants and then rehydrates an in-memory taskStateStore keyed only by conversationId, enabling cross-tenant data exposure and mixing of tenant state.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): rehydrateTaskStateOnBoot is a boot rehydration loop over running/pending agent_runs that rebuilds in-memory TaskState keyed by each row's own conversationId (server/felix-brain.ts:13-45); state is surfaced per-conversation, not cross-tenant.
- **server/persona-export.ts:33** _(global-table)_ — exportPersona() queries `personas` by id without scoping to tenant: `SELECT * FROM personas WHERE id = ${personaId}`. The function accepts a tenantId but does not verify the persona row belongs to that tenant before exporting.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): personas table has no tenant_id column (platform-global personas); tenant linkage lives in trust/skills rows fetched separately.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance_rules by id only and omits tenant scoping. This allows incrementing trigger_count on a rule from another tenant if ids overlap.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project by id without tenant scoping or ownership verification. project rows are tenant-scoped; using projectId alone can expose another tenant's project data and lead to writing that data to a shared filesystem file.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (...) has no tenant filtering; it enumerates projects and conversations across all tenants and then writes per-project brains into a single shared directory.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed without tenant filtering; conversations are tenant-scoped and may return rows from other tenants' projects if project ids collide or are not validated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} runs without tenant restriction. messages are tenant-scoped and contain sensitive assistant/user content; reading them across tenants violates isolation and can write cross-tenant content to disk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result ... WHERE id = ${sessionId} executes without a tenant_id constraint. This can overwrite another tenant's session review_result.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/self-improvement.ts:401** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `experiments` uses .where(eq(experiments.id, experiment.id)) with no tenant_id constraint. This allows updating an experiment row by id without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-improvement.ts:415** _(internal-worker-pk)_ — Error-path UPDATE on `experiments` sets status/outcome with .where(eq(experiments.id, experiment.id)) but omits a tenant_id constraint, allowing cross-tenant modification in failure handling.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:1367** _(upstream-verified)_ — getAllDataForExport performs platform-wide SELECTs over tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with no tenant scoping — returns all tenants' data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — UPDATE to tenant-scoped table 'felix_proposals' performed without scoping by tenant_id (args_embedding stamp).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:181** _(upstream-verified)_ — UPDATE setting surprise_band = 'no_history' on felix_proposals omits tenant_id in WHERE clause (WHERE id = ${proposalId} only).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — UPDATE setting surprise_score and surprise_band on felix_proposals omits tenant_id in WHERE clause (WHERE id = ${proposalId} only).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/agentic-features.ts:861** _(upstream-verified)_ — enrollInSequence checks for existing enrollments using: SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' — this query omits tenant_id, allowing cross-tenant probing/duplicate-detection across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — SELECT DISTINCT FROM conversations is executed without a tenant_id filter — this iterates over conversations from all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): backfillProjectAssets only caller is a one-time startup backfill routes.ts:824; it selects c.tenant_id (auto-asset-capture.ts:185) and passes each row's tenant to captureProjectAssets(c.id,c.tenant_id,...) at auto-asset-capture.ts:217 — row-carried tenant, no tenant-facing return.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` lacks a tenant_id constraint in the WHERE clause (`WHERE id = ${pending.reviewId} AND reused = false`). This allows changing rows across tenants by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/delivery-pipeline.ts:1681** _(global-table)_ — getDeliveryByStripePayment selects from `delivery_logs` by stripePaymentId with no tenant_id constraint. A caller providing a stripePaymentId could discover or retrieve another tenant's delivery record.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:50** _(signed-url)_ — getDfyIntakeByToken selects from `dfy_intake_forms` by token only (eq(dfyIntakeForms.token, token)) with no tenant_id constraint. This reads a tenant-scoped table without scoping to a tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/google-workspace.ts:2564** _(fk-transitive)_ — INSERT INTO presenter_slide_images does not include tenant_id (INSERT uses session_id, slide_index, image_data, image_size, quality only). If presenter_slide_images is tenant-scoped, this inserts rows without the required tenant_id column or without scoping to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/lib/agent-trace.ts:129** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_trace_spans lacks tenant predicate (WHERE span_id = ${spanId} only). This allows a cross-tenant UPDATE if span_id is misused/duplicated or the call context is wrong.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/bwb-job-progress.ts:144** _(internal-worker-pk)_ — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit the tenant_id predicate when BWB_TENANT_ID is not set, causing updates to run without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:169** _(internal-worker-pk)_ — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) may run without tenant_id when env tenant is missing, leaving a tenant-scoped UPDATE unconstrained.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:203** _(internal-worker-pk)_ — SELECT … FROM video_jobs .where(scope(jobId)) — scope() can omit tenant_id if BWB_TENANT_ID is not provided, performing a cross-tenant read against a tenant-scoped table.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): jobId is an unguessable random id (vj_<base36>_<6-byte-hex>); scope() warns once when BWB_TENANT_ID unthreaded — documented defense-in-depth.
- **server/lib/bwb-job-progress.ts:230** _(internal-worker-pk)_ — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) (heartbeat) may omit tenant_id if env tenant is not set, performing an unconstrained UPDATE against a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:254** _(internal-worker-pk)_ — UPDATE video_jobs .where(scope(jobId, { notDone: true })) — scope() can drop the tenant predicate when BWB_TENANT_ID isn't set, leading to tenant-unspecified UPDATE on a tenant-scoped table.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): failBwbJob pinned to the same unguessable random jobId; documented warn-once fallback.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories executed without constraining to tenant_id (no AND tenant_id = ${tenantId}). This can modify categories across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): catId is produced by ensureCategory (server/memory-graph.ts:79) which SELECTs/INSERTs scoped to tenantId (:51 'AND tenant_id = ${tenantId}', :57-58 INSERT with tenant_id); the id is server-derived within the same tenant call path, not request-supplied.
- **server/memory-graph.ts:184** _(upstream-verified)_ — refreshCategoryCount updates memory_categories and computes the new count using a subquery that does NOT filter memory_entries by tenant_id and updates the category row without tenant filtering. Both the inner COUNT and the outer UPDATE lack tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/orchestrator-ledger.ts:450** _(upstream-verified)_ — Use of sql.raw() with a value derived from caller-provided ledgerKey: `SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`. Passing interpolated/raw SQL fragments constructed from request-derived input can lead to injection or unexpected SQL execution.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): pg_advisory_xact_lock is a lock, not a tenant table. lockKey is a purely numeric BigInt(sha1).toString() (server/orchestrator-ledger.ts:35-38) — no injectable text. The ledger read/write in the same tx are tenant-scoped ('AND tenant_id = ${tenantId}' at :448).
- **server/plan-executor.ts:116** _(upstream-verified)_ — UPDATE to tenant-scoped table 'plans' (appendExecutionLog) uses WHERE id = ${planId} only — no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Final status/execution_log UPDATE within executePlan; ownership enforced upstream at server/routes/agentic-policy.ts:113.
- **server/process-governor.ts:155** _(global-table)_ — Subquery computing pending_tasks reads heartbeat_tasks without tenant scoping: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true). persona_id may not be globally unique; missing tenant filter can spill counts across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): heartbeat-task pace counting is a platform-wide throttle by design (same family as checkPace/heartbeat_logs); per-tenant fairness would need a schema migration and is tracked separately.
- **server/routes.ts:2089** _(signed-url)_ — Public presenter endpoint queries tenant-scoped presenter_sessions by token without also constraining by tenant_id (SELECT ... FROM presenter_sessions WHERE token = ${token}). This reads tenant-scoped rows without an explicit tenant filter in request-handling code.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2057 — presenter token is crypto.randomBytes(16).hex (routes.ts:2140) — an unguessable capability URL for public slide viewing; the token IS the access control by design.
- **server/self-improvement.ts:171** _(upstream-verified)_ — detectStagnation performs a SELECT on the tenant-scoped `experiments` table without requiring/applying a tenant_id constraint when tenantId is not supplied. The query falls back to filtering only by category, which can return cross-tenant rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:517** _(upstream-verified)_ — getExperimentHistory can SELECT from tenant-scoped `experiments` without a tenant_id constraint when tenantId is not provided. The code returns all experiments if tenantId is omitted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:407** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `messages` (attachCitations) without tenant_id filter: UPDATE messages SET citations = ... WHERE id = ${messageId}.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:862** _(internal-worker-pk)_ — UPDATE memory_entries SET embedding = ... WHERE id = ${id} updates tenant-scoped `memory_entries` without tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:874** _(internal-worker-pk)_ — UPDATE agent_knowledge SET embedding = ... WHERE id = ${id} updates tenant-scoped `agent_knowledge` without tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:521** _(global-table)_ — deletePersona transaction updates tenant-scoped tables (conversations, memory_entries, daily_notes) by personaId without including tenant_id constraints — can modify rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:479 — The personas table has no tenant_id column — personas are global/platform-wide by design (mapped per-tenant only via tenant_persona_names). deletePersona is invoked solely from DELETE /api/personas/:id, which is gated by requirePlatformAdmin (routes/personas.ts:134-136). The cascade cleanup of conversations/memory_entries/daily_notes by persona_id is an intended platfo
- **server/storage.ts:571** _(upstream-verified)_ — INSERT into tenant-scoped table `memory_entries` via db.insert(memoryEntries).values(data) occurs without explicit validation that data.tenantId is present and owned by the caller.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/twilio.ts:102** _(upstream-verified)_ — Query selects from tenant-scoped table `agent_knowledge` without constraining on tenant_id (SELECT id, content::jsonb AS info FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ...). This reads rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/twilio.ts:116** _(upstream-verified)_ — Query reads the `conversations` row by id without scoping to a tenant (SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} LIMIT 1). This performs an unconstrained cross-tenant read.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
- **server/delivery-pipeline.ts:569** _(deferred)_ — updateDeliveryLog performs an UPDATE against the tenant-scoped table `delivery_logs` but the WHERE clause only filters by id (eq(deliveryLogs.id, id)) — no tenant_id constraint. This allows an attacker or buggy caller who can supply an id to update another tenant's delivery row.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/delivery-pipeline.ts:575** _(deferred)_ — updateDeliveryLogWithBundleFiles updates `delivery_logs` and merges metadata but the WHERE clause only filters by id (eq(deliveryLogs.id, id)) — no tenant_id constraint. This allows modifying another tenant's delivery row (metadata and bundle state).
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/compaction.ts:400** _(deferred)_ — INSERT into compaction_archives omits tenant_id. compaction_archives is tenant-scoped elsewhere (queries join conversations.tenant_id), so this INSERT can create unscoped or cross-tenant rows.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): compaction_archives INSERT omits tenant_id (schema declares NOT NULL, DB column has no default) — thread the conversation's tenantId into the archive INSERT; same legacy-tenant-column family as the DEFAULT-1 tables follow-up.
- **server/lib/scheduled-post-runner.ts:319** _(deferred)_ — Construction of a Postgres array literal (arrLit = `{...}`) via string interpolation from request-derived `platforms` and then embedding it into SQL (VALUES (${arrLit}::text[] ...)) creates a string-interpolated SQL fragment sourced from user input — injection risk.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): hand-built text[] literal from allowlist-checked platforms — injection-resistant today; refactor to parameterized bind (deferred item 5, campaign 2026-07-31).
