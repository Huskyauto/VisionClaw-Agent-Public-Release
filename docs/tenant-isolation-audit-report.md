# Tenant-Isolation Audit Report

_Generated 2026-09-08T19:41:38.680Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 0 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **667/667** file(s) audited · chunks 61/61 OK (0 failed)
- Findings: **213** (CRITICAL 66 · HIGH 115 · MEDIUM 30 · LOW 2)
- Severe dispositions: **181 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (66)

- **server/agent-eval.ts:218** — UPDATE against tenant-scoped table `agent_evals` omits tenant scoping: `UPDATE agent_evals SET ... WHERE id = ${evalId}`. This can update rows owned by other tenants.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or otherwise verify the row belongs to the caller's tenant before updating. Enforce a runtime check that evalId to-be-updated belongs to the passed tenant.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT across tenant-scoped tables (agent_runs / agent_approvals) — the initial candidate query (SELECT ... FROM agent_runs LEFT JOIN agent_approvals ...) does not constrain results to a single tenant_id. This can return rows from other tenants and violates the requirement that every read against tenant-scoped tables include a tenant_id filter.
  - _Fix:_ Restrict the query to a single tenant context (add WHERE r.tenant_id = <callerTenantId> and/or a.tenant_id = <callerTenantId>) or otherwise iterate per-tenant and run the query within that tenant scope. Do not issue broad cross-tenant SELECTs from this worker. If the worker must operate cross-tenant, explicitly document and gate that capability and ensure callers are system/admin contexts that are authorized; otherwise change the SQL to accept a tenantId parameter and include AND r.tenant_id = ${tenantId} (and equivalent for joined tables) using parameterized query binding.
- **server/commitment-drafter.ts:234** — Unscoped UPDATE on tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` lacks a tenant_id constraint and can modify rows across tenants.
  - _Fix:_ Restrict the UPDATE to the owning tenant. For example: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}` (or use the current tenantId variable if available). Alternatively, skip the UPDATE entirely when the row's tenant is invalid. Ensure every UPDATE/DELETE/SELECT against tenant-scoped tables includes `AND tenant_id = <trusted tenant>` (parameterized) to enforce tenant isolation.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows from all tenants and leaks tenant data to the caller.
  - _Fix:_ If this routine must operate across tenants, run it under an explicit privileged/admin audit context separate from tenant callers and avoid returning sensitive fields; otherwise scope the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. Prefer using withTenantTx for per-tenant operations or add explicit tenant_id filtering to every read.
- **server/email.ts:370** — SELECT against tenant-scoped table `inbox_messages` performed without constraining by tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can return rows belonging to other tenants and allows cross-tenant information leakage.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1) or otherwise scope the lookup by a known tenant/inbox identifier. If the intent is a global de-dup keyed by tenant, ensure uniqueness and all reads/writes are tenant-scoped.
- **server/email.ts:415** — Backfill query scans inbox_messages without a tenant filter (SELECT id, message_id FROM inbox_messages WHERE ...). This reads rows across all tenants.
  - _Fix:_ Require and pass an explicit tenantId (or inbox/id scope) into backfillEmptyBodies and add WHERE tenant_id = ${tenantId} to the SELECT so only a single tenant's messages are read/processed.
- **server/email.ts:429** — UPDATE on tenant-scoped table `inbox_messages` omits tenant constraint (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). This can update a row belonging to another tenant if ids collide or the job is mis-scoped.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or derive tenantId earlier and bind it into the update to guarantee the update only affects the owning tenant's row.
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot performs a cross-tenant SELECT from agent_runs without constraining tenant_id (SELECT ... FROM agent_runs WHERE status IN ('running','pending') ...). This reads rows for all tenants into memory and can leak/merge tenant-scoped data into the in-process task state store.
  - _Fix:_ Restrict the query to a single tenant or iterate per-tenant. Require an explicit tenantId parameter or enumerate tenants and run the rehydration per tenant, e.g. SELECT ... FROM agent_runs WHERE tenant_id = ${tenantId} AND status IN (...). Alternatively, fetch only rows for the service account / admin tenant if that's the intent. Ensure any conversation/run rows are validated as belonging to the tenant before adding to in-memory state.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' uses only eq(scrapedPages.id, existing[0].id) in WHERE — missing explicit tenant_id constraint. Every write to a tenant-scoped table must include the caller's tenant filter.
  - _Fix:_ Constrain the UPDATE by tenant as well. Example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Alternatively, re-check tenant ownership and fail-closed before issuing the UPDATE.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' inside crawl loop uses only eq(scrapedPages.id, existing[0].id) in WHERE — missing explicit tenant_id constraint.
  - _Fix:_ Add the tenant constraint to the WHERE clause, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Ensure every mutating query includes the tenant filter even if the id was looked up earlier.
- **server/google-drive.ts:880** — UPDATE on tenant-scoped table 'projects' uses .where(eq(projects.id, projectId)) without also constraining by tenant_id. This allows a project row to be updated without an explicit tenant-scoped WHERE clause.
  - _Fix:_ Add the tenant predicate to the update WHERE, for example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Verify tenant ownership prior to issuing the UPDATE and fail if it cannot be proven.
- **server/glasses-gateway.ts:173** — UPDATE on 'apiKeys' sets lastUsedAt with .where(eq(apiKeys.id, key.id)) but no tenant_id constraint is applied. Mutating a tenant-scoped table without including the caller's tenant in the WHERE violates the tenant-isolation invariant.
  - _Fix:_ Include tenant scoping in the UPDATE, for example: .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, (req as any).tenantId))). Alternatively, perform the update via the previously-fetched key row in a safe code path that ensures the tenant association cannot be tampered with.
- **server/heartbeat-startup.ts:76** — Direct UPDATE on tenant-scoped table `heartbeat_tasks` with no tenant_id constraint (db.execute(sql`UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id}`)). This modifies tenant-scoped rows without asserting tenant ownership.
  - _Fix:_ Include an explicit tenant filter in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage-layer helper (e.g. storage.updateHeartbeatTask) that enforces tenant scoping. If this is intentionally cross-tenant scheduler code, make the cross-tenant intent explicit and audited; otherwise fix the WHERE clause to bind tenant_id.
- **server/heartbeat-startup.ts:80** — Direct UPDATE on tenant-scoped table `heartbeat_tasks` with no tenant_id constraint (db.execute(sql`UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id}`)). This can change rows belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call the tenant-aware storage API. If the update is intentionally global, clearly document and isolate it in a vetted admin-only path.
- **server/heartbeat.ts:2609** — UPDATE on tenant-scoped table `heartbeat_tasks` with no tenant_id constraint (db.execute(sql`UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}`)). This mutates a task approval_status without asserting tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the tenant-aware storage API to set approval_status. Ensure the tenantId used matches the task's tenant to prevent cross-tenant modifications.
- **server/heartbeat.ts:3065** — UPDATE on tenant-scoped table `heartbeat_tasks` with no tenant_id constraint (db.execute(sql`UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}`) in delegateTaskFromChat). This updates approval status without binding to tenant_id.
  - _Fix:_ Add a tenant filter to the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a storage-layer call that enforces tenant ownership. If this is intended behavior, gate it behind an explicit admin/scheduler API and audit it.
- **server/job-queue.ts:160** — claimDueJobs issues an UPDATE on agent_jobs (claims jobs) with no tenant_id constraint — this updates/claims rows across all tenants in a single statement.
  - _Fix:_ If this is intended to be multi-tenant work-claiming, make the risk explicit and harden the code (audit + admin guard). Otherwise scope claims to a tenant (e.g. include tenant_id = ${tenantId} or run per-tenant claim loops). At minimum, document and protect this cross-tenant mutation path or partition the queue so updates always include tenant_id.
- **server/job-queue.ts:196** — reclaimExpiredLeases step 1 UPDATE touches agent_jobs (marks as failed_terminal) without any tenant_id filter — this can flip rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (or perform the reclamation per-tenant). If global reclamation is required, require an explicit admin-only code path and add extra audit/guards to prevent accidental cross-tenant mutations.
- **server/job-queue.ts:209** — reclaimExpiredLeases step 2 UPDATE re-queues expired running jobs without tenant_id constraint — affects all tenants.
  - _Fix:_ Scope the update by tenant_id (or iterate per-tenant) or ensure the operation is intentionally global and guarded; otherwise add tenant_id = ${tenantId} to the WHERE clause to enforce isolation.
- **server/job-queue.ts:238** — completeJob issues an UPDATE on agent_jobs (mark succeeded) without constraining by tenant_id — a late/stray complete call could affect another tenant's job.
  - _Fix:_ Include tenant_id fencing in the WHERE (e.g. AND tenant_id = ${expectedTenantId}), or require the caller to pass/validate the job's tenant before performing the UPDATE to preserve tenant isolation and maintain lease-fencing guarantees.
- **server/job-queue.ts:281** — failJob SELECT reads max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — this reads a tenant-scoped row unscoped by tenant.
  - _Fix:_ Require and use tenant_id when reading agent_jobs (or explicitly restrict this helper to admin usage). Prefer SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} to avoid cross-tenant information disclosure.
- **server/job-queue.ts:298** — failJob terminal-path UPDATE on agent_jobs (set failed_terminal) does not include tenant_id in the WHERE clause — it can mutate jobs for other tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or otherwise validate the job's tenant prior to updating. Ensure the lease-fencing guard includes tenant scope to prevent cross-tenant writes.
- **server/job-queue.ts:318** — failJob retry-path UPDATE (re-schedule pending) updates agent_jobs without a tenant_id constraint in the WHERE clause.
  - _Fix:_ Scope this UPDATE to the job's tenant (AND tenant_id = ${tenantId}) or implement per-tenant retry logic; ensure callers provide and verify tenantId to preserve isolation.
- **server/job-queue.ts:336** — cancelJob's UPDATE cancels a job by id/status but has no tenant_id condition — an operator call could cancel another tenant's job by id.
  - _Fix:_ Require tenant scoping for cancel operations (e.g. require tenantId param and AND tenant_id = ${tenantId}) or enforce admin-only semantics with explicit authorization checks and audit logging.
- **server/lib/scheduled-post-runner.ts:61** — The CTE/UPDATE block selects and updates rows in the tenant-scoped table `scheduled_posts` without constraining by tenant_id (no WHERE tenant_id = ...). This performs cross-tenant reads/writes via application SQL rather than an explicit per-tenant filter.
  - _Fix:_ Either run this logic per-tenant (e.g. iterate tenants and call withTenantTx/filtered queries) or include an explicit tenant_id predicate in the CTE and UPDATE. If the runner must operate as an admin across tenants, make that explicit in the audit and minimize data returned (avoid returning tenant-scoped data to untrusted callers) or implement safe admin-only paths. Ensure every SELECT/UPDATE/DELETE against tenant-scoped tables includes a tenant_id predicate or runs inside a documented admin-only boundary.
- **server/martech-bundle.ts:325** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the row id in the WHERE clause (WHERE id = existing.id) with no tenant_id predicate. This allows a caller who can supply or manipulate the id to update another tenant's profile.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add a tenant_id predicate (and validate caller tenant) so the WHERE is at least: WHERE id = ${existing.id} AND tenant_id = ${opts.tenantId}. Prefer using the same tenant-scoped selector used for reads (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) and validate opts.tenantId before the update.
- **server/memory-graph.ts:130** — UPDATE on tenant-scoped table `memory_categories` increments memory_count using only the category id in the WHERE clause (WHERE id = ${catId}) with no tenant_id predicate. A malformed or cross-tenant category id could cause counts to be updated across tenants.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE. Use WHERE id = ${catId} AND tenant_id = ${tenantId} (or the equivalent Drizzle .where(and(eq(memoryCategories.id, catId), eq(memoryCategories.tenantId, tenantId)))). Validate tenantId is the caller's tenant before performing the write.
- **server/memory-graph.ts:709** — UPDATE on tenant-scoped table `memory_entries` sets category_id using only the memory id in the WHERE clause (WHERE id = ${mem.id}) with no tenant_id predicate. This can allow an attacker controlling mem.id to modify another tenant's memory row.
  - _Fix:_ Constrain the UPDATE to the caller's tenant: WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Prefer using the same tenantId variable passed into categorizeExistingMemories and validate it before the update; use Drizzle's .where(eq(memoryEntries.tenantId, tenantId)) form when available.
- **server/minerva-planner.ts:395** — SELECT against tenant-scoped table `plans` uses the wrong binding for tenant_id (AND tenant_id = ${args.planId}) — this fails to verify the caller's tenant and can leak or mis-validate cross-tenant rows.
  - _Fix:_ Use the caller's tenant binding (tenant_id = ${args.tenantId}) instead of args.planId. Ensure every read of plans includes WHERE tenant_id = ${args.tenantId} so ownership is validated before acting on the row.
- **server/minerva-planner.ts:412** — SELECT checking for a child plan uses the wrong tenant binding (WHERE parent_plan_id = ${args.planId} AND tenant_id = ${args.planId}) — tenant filter is incorrectly set to the planId.
  - _Fix:_ Replace the incorrect tenant filter with the caller's tenant (tenant_id = ${args.tenantId}). Always validate tenant scope on parent/child plan lookups (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${args.tenantId}).
- **server/mpeg-engine.ts:1057** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The raw INSERT INTO project_files (...) VALUES (...) omits tenant_id even though the caller-supplied options.projectId/tenantId are used for ownership checks separately. This violates the invariant that every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Include the tenant_id column in the INSERT and bind the validated tenant id (e.g. VALUES (..., ${options.tenantId}, ...)) or use Drizzle's insert API with an object that includes tenantId. Do not rely solely on a prior assertProjectInTenant call — the INSERT must explicitly set tenant_id to the caller's tenant context. Example: await db.execute(sql`INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ${...})`);
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code inserts a project file record after assertProjectInTenant but the INSERT statement omits tenant_id, failing the requirement that every insert into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Add tenant_id to the INSERT and bind the verified tenant id (e.g. VALUES (${options.tenantId}, ${options.projectId}, ...)) or use Drizzle's `.insert()` with an object containing tenantId. Even if ownership was asserted, the row must explicitly carry tenant_id to preserve tenant isolation.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table oauth_subscriptions without tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads subscription rows across all tenants (access tokens/refresh tokens) in a single query.
  - _Fix:_ Limit this query to a single tenant (add WHERE tenant_id = ${tenantId}) or explicitly iterate tenants under an authenticated/authorized multi-tenant admin flow. Avoid global SELECTs that return secrets from all tenants; perform per-tenant operations with an explicit tenantId parameter and least-privilege checks.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is executed without a tenant filter. This enumerates rows across tenants.
  - _Fix:_ Include tenant scoping (WHERE tenant_id = ${tenantId}) or otherwise restrict the query to an authorized set of tenant IDs. Do not run unscoped queries against oauth_subscriptions; if you need to discover tenants, use a guarded admin-only audit path with strict access controls and logging.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh issues a cross-tenant query: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — no tenant_id constraint. This reads/iterates all tenants' subscriptions and touches sensitive token state.
  - _Fix:_ Refactor to operate per-tenant: either accept/derive a tenantId and query only that tenant's rows, or fetch a vetted list of tenant IDs through an authorized admin endpoint and handle each tenant separately. Never SELECT all rows from tenant-scoped secret-bearing tables in an unscoped query.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions issues a global SELECT on oauth_subscriptions (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE) with no tenant_id constraint, then proceeds to refresh/operate across tenants.
  - _Fix:_ Same mitigation as above: make the operation explicitly tenant-scoped, require an explicit admin authorization surface if truly necessary to operate across tenants, or limit the function to operate with an explicit tenantId. Avoid unscoped reads/writes against tenant-scoped tables holding secrets.
- **server/plan-executor.ts:516** — CAS claim UPDATE on plans is missing a tenant_id constraint: `UPDATE plans SET status = 'executing' ... WHERE id = ${planId} AND status = 'approved' RETURNING ...`. Without a tenant filter an attacker (or misused internal call) can claim/execute a plan belonging to any tenant.
  - _Fix:_ Include tenant scoping in the claim (e.g. require a tenantId parameter and use `... WHERE id = ${planId} AND tenant_id = ${tenantId} AND status = 'approved' RETURNING ...`), or perform a prior validated SELECT that confirms caller ownership/authorization before switching status. Do NOT infer tenant from the returned row later; enforce it in the WHERE clause for the UPDATE (or use SELECT ... FOR UPDATE with explicit tenant validation).
- **server/process-governor.ts:897** — UPDATE against tenant-scoped table `agent_desks` omits tenant filter. The code updates rows selected earlier but the UPDATE statement only uses `WHERE id = ${desk.id}` (no `tenant_id`), allowing cross-tenant modification if ids collide.
  - _Fix:_ Include tenant scoping on the UPDATE and verify ownership. Example: add `AND tenant_id = ${tenantId}` to the WHERE clause (or pass/verify the tenant when selecting desks) so the update becomes `WHERE id = ${desk.id} AND tenant_id = ${tenantId}`. Prefer validating the desk row's tenant before performing the update.
- **server/process-governor.ts:1235** — UPDATE against tenant-scoped table `governance_rules` omits tenant filter. The logAction() helper updates `governance_rules` (`UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id}`) without constraining by `tenant_id`, allowing modification of rules across tenants.
  - _Fix:_ Constrain the UPDATE by tenant to avoid cross-tenant writes. Add `AND tenant_id = ${tenantId}` (or otherwise validate the rule belongs to the tenant) so the WHERE becomes `WHERE id = ${rule.id} AND tenant_id = ${tenantId}`.
- **server/research-engine.ts:169** — cleanupZombieSessions performs a global UPDATE on research_sessions without constraining by tenant_id (UPDATE research_sessions ... WHERE status = 'running' ...). This can mark sessions from other tenants as completed, breaking the tenant-isolation invariant.
  - _Fix:_ Constrain the UPDATE to a tenant scope. Either run the cleanup per-tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'running' AND started_at < ${staleThreshold}) or pass an explicit, validated list of tenant IDs and use a parameterized IN clause (sql.join with parameterized values) so the database update never touches rows outside the intended tenant(s). Avoid constructing raw SQL with externally-derived values and ensure the cleanup only operates within the caller's tenant context or an authorized admin scope.
- **server/sculptor.ts:106** — UPDATE on tenant-scoped table `sculptor_sessions` lacks a tenant_id constraint (UPDATE sculptor_sessions SET status=... WHERE id = ${session.id}). This can modify rows in other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or use the session.tenantId). Prefer verifying ownership before the UPDATE and/or using parameterized tenantId in the WHERE to enforce tenant isolation.
- **server/sculptor.ts:113** — UPDATE on tenant-scoped table `sculptor_sessions` lacks a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id=... WHERE id = ${session.id}). This can update another tenant's session if id is forged.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Alternatively, fetch/verify the session's tenant before updating and then include tenant_id in the WHERE to prevent cross-tenant writes.
- **server/sculptor.ts:168** — UPDATE on tenant-scoped table `sculptor_sessions` performed inside monitorSession uses WHERE id = ${sessionId} with NO tenant_id constraint. This allows a caller with a sessionId from another tenant to affect that tenant's rows.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure the tenantId variable passed into monitorSession is used in all UPDATE/DELETE statements against tenant-scoped tables.
- **server/sculptor.ts:359** — UPDATE on tenant-scoped table `sculptor_sessions` sets review_result WHERE id = ${sessionId} with NO tenant_id constraint. This can overwrite another tenant's review data.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or verify session ownership before performing the write. All writes to tenant-scoped tables must include tenant_id in their WHERE clause.
- **server/second-opinion.ts:416** — SETTLE update against tenant-scoped `agent_cost_ledger` uses only WHERE id = ${reservationId} and does NOT verify tenant_id. Without tenant scoping, this can alter another tenant's ledger row (financial integrity risk).
  - _Fix:_ Bind the settle operation to the tenant. Pass tenantId into settleFusionReservation (or query the row first and verify its tenant_id) and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. Never update ledger rows by id alone when rows are tenant-scoped.
- **server/second-opinion.ts:439** — releaseFusionReservation updates tenant-scoped `agent_cost_ledger` (SET cost_usd='0', operation=...) WHERE id = ${reservationId} with NO tenant_id constraint. This can release/modify reservations of other tenants.
  - _Fix:_ Require and use the reservation's tenantId in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or fetch and validate the row's tenant before modifying. Ensure releaseFusionReservation is tenant-aware.
- **server/seed.ts:3634** — DELETE from tenant-scoped table heartbeat_tasks is executed without any tenant_id constraint (DELETE FROM heartbeat_tasks WHERE ...). This will delete matching rows across all tenants.
  - _Fix:_ Restrict the DELETE to a specific tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or iterate per-tenant and include tenant_id in the WHERE clause. Never run DELETE/UPDATE on tenant-scoped tables without an explicit tenant_id filter.
- **server/self-improvement.ts:423** — UPDATE against tenant-scoped table `experiments` is performed without constraining the row to the caller's tenant (WHERE clause only filters by experiments.id). This allows an update to affect an experiment belonging to another tenant if an id is supplied or leaked.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example use .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) or in raw SQL add AND p.tenant_id = ${tenantId}. Always include the tenant_id predicate on updates/deletes to tenant-scoped tables (use Drizzle eq() helpers, avoid concatenated SQL).
- **server/self-reflection.ts:366** — UPDATE on `memory_entries` uses WHERE id = <existing id> without also verifying tenant ownership (no tenant_id constraint). Although the id was looked up earlier, the subsequent UPDATE does not include tenant_id, permitting cross-tenant modification if an id from another tenant is reused or tampered with.
  - _Fix:_ Add a tenant constraint to the UPDATE, e.g. WHERE id = ${id} AND tenant_id = ${tenantId}, or re-validate ownership by selecting the row with tenant_id=${tenantId} before updating. Prefer parameterized Drizzle .where(eq(...)) expressions rather than relying solely on id equality.
- **server/storage.ts:601** — INSERT into tenant-scoped table `memory_entries` without validating that the inserted row contains an explicit, trusted tenant_id (createMemoryEntry inserts `data` without checking data.tenantId).
  - _Fix:_ Fail-closed: validate `data.tenantId` is a positive integer before inserting (or require caller to pass/derive a verified tenantId). Reject the insert if tenantId is missing/invalid.
- **server/storage.ts:624** — UPDATE of tenant-scoped table `memory_entries` performed without tenant scoping when tenantId is not provided (deleteMemoryEntry uses WHERE id = ... and only optionally adds tenant filter).
  - _Fix:_ Fail-closed: require a tenantId for delete/update operations or otherwise enforce requiredTenantScope so the UPDATE cannot affect rows in other tenants. Add `AND tenant_id = ${tenantId}` (or use requiredTenantScope) to the WHERE.
- **server/storage.ts:632** — UPDATE of tenant-scoped table `memory_entries` (touchMemoryEntries) can be executed without tenant_id constraint when tenantId is omitted — code warns but still runs an unscoped update.
  - _Fix:_ Require and validate tenantId for this operation (use requiredTenantScope/assertValidTenantId) and include the tenant equality predicate in the WHERE clause so only the caller's tenant rows are updated.
- **server/storage.ts:656** — INSERT into tenant-scoped table `conversation_facts` without validating tenant ownership (createConversationFact inserts provided `data` without checking data.tenantId).
  - _Fix:_ Validate `data.tenantId` (positive integer) or otherwise verify the caller's tenant owns the referenced conversation before inserting. Reject the insert if tenantId is missing/invalid.
- **server/storage.ts:892** — UPDATE of tenant-scoped table `memory_entries` without tenant scoping (updateMemoryEmbedding updates `embedding` WHERE id = ${id} with no tenant_id constraint).
  - _Fix:_ Require and validate tenantId (or otherwise ensure ownership) and include `AND tenant_id = ${tenantId}` in the WHERE clause to avoid updating other tenants' rows.
- **server/storage.ts:929** — UPDATE of tenant-scoped table `memory_entries` without tenant scoping (archiveExpiredMemories updates all expired active memory rows platform-wide).
  - _Fix:_ Archive operations that touch tenant-scoped rows must either run per-tenant with a validated tenantId or be gated to a privileged admin path. Add tenant scoping to the UPDATE or iterate per-tenant to avoid cross-tenant modifications.
- **server/storage.ts:946** — UPDATE of tenant-scoped table `memory_entries` without tenant scoping (archiveStaleMemories runs a platform-wide UPDATE using date cutoffs, affecting all tenants).
  - _Fix:_ Same fix as above — require an explicit tenant context or run the archive per-tenant with validated tenantId, or otherwise restrict this to an admin-only operation and document the risk.
- **server/storage.ts:1358** — Mass SELECTs for data export (getAllDataForExport) query many tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, dailyNotes, heartbeatTasks, etc.) with no tenant filters — returns data for all tenants.
  - _Fix:_ Make this an explicit admin-only export and require a privileged context, or accept an explicit tenantId/ALL_TENANTS sentinel and enforce requiredTenantScope for tenant-scoped tables. Never perform unscoped platform-wide reads in normal request paths.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding performs an UPDATE on felix_proposals without constraining by tenant_id (WHERE id = ${proposalId} AND args_embedding IS NULL) — this can modify another tenant's row when called with a global id.
  - _Fix:_ Require and accept a tenantId parameter (or otherwise resolve caller tenant) in stampArgsEmbedding, and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. Validate tenantId is a positive integer before executing the UPDATE.
- **server/tools.ts:2803** — INSERT into tenant-scoped table `project_conversations` without setting tenant_id. The INSERT (INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING) writes a tenant-scoped relationship row but does not include tenant_id in the column list/VALUES.
  - _Fix:_ Include tenant_id in the INSERT VALUES (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conversationId}, ${tenantId}) ...). If the code already verified ownership of the referenced rows, still write tenant_id to the new row to enforce isolation at the application level. Prefer adding a unit-tested helper to always stub tenant_id into INSERTs for tenant-scoped tables.
- **server/tools.ts:2928** — INSERT into tenant-scoped table `project_files` without setting tenant_id. The INSERT (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)) does not persist tenant_id on the new file row.
  - _Fix:_ Add tenant_id to the INSERT column list and pass the caller's tenant (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by, tenant_id) VALUES (..., ${tenantId})). Ensure this is done even after verifying project ownership. Consider a central DB helper to append tenant_id to all INSERTs into tenant-scoped tables to avoid omissions.
- **server/tools.ts:2942** — INSERT into tenant-scoped table `project_notes` without setting tenant_id. The INSERT (INSERT INTO project_notes (project_id, note, author) VALUES (...)) writes a note row but omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Even when the project was validated as belonging to the tenant, always persist tenant_id on created rows to maintain consistent application-level scoping and avoid cross-tenant contamination.
- **server/tools.ts:2966** — INSERT into tenant-scoped table `project_conversations` without setting tenant_id. The handler inserts (INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)) during link_conversation but omits tenant_id, creating rows with no explicit tenant scope.
  - _Fix:_ Change the INSERT to include tenant_id (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conversationId}, ${tenantId})). Also ensure any foreign ids accepted from callers are ownership-validated (as this handler already does) and that the tenant_id written matches the verified tenant. Consider adding a linter/test to catch INSERTs into tenant-scoped tables that don't include tenant_id.
- **server/tools.ts:6909** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT statement builds VALUES (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without passing tenant_id even though tenant scoping is enforced only at the application layer.
  - _Fix:_ Include tenant_id in the INSERT values (and bind it from the caller's verified tenant context). Example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Also keep the existing assertProjectInTenant check (ownership verification) before inserting.
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` is executed without constraining by tenant_id: `SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} LIMIT 1`. This reads a tenant-scoped row by id with no tenant filter, violating the invariant that every read must include tenant_id = <caller tenant>.
  - _Fix:_ Constrain the lookup to the claimed tenant when verifying ownership, e.g. SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 (or use an equivalent Drizzle eq filter). That way the code only reads the conversation row within the expected tenant context and cannot observe or validate cross-tenant rows.
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger uses only reservation id in WHERE clause (no tenant_id constraint). This allows a caller who knows/resolves an arbitrary reservation id to modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership in the UPDATE predicate (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or require/accept tenantId as a parameter and use a verified tenant-scoped constraint. Alternatively, SELECT the row first to verify tenant ownership before updating.
- **server/venture-discovery/budget.ts:169** — UPDATE against tenant-scoped table agent_cost_ledger (releaseVentureReservation) uses only reservation id in WHERE clause (no tenant_id constraint). This can be used to alter other tenants' ledger rows if an attacker learns reservation ids.
  - _Fix:_ Require and use the tenantId when releasing a reservation, or query the row first to verify the reservation belongs to the expected tenant. Change the UPDATE to include AND tenant_id = ${tenantId} (or equivalent Drizzle eq check).

### HIGH (115)

- **server/ab-optimizer.ts:111** — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads rows across all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. If the intent is a multi-tenant sweep, make it explicit (accept tenantId or perform per-tenant iteration) and ensure caller authorization/validation before touching tenant rows.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting is missing tenant scoping: `SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER}` — reads across tenants.
  - _Fix:_ Either accept a tenantId and add `AND tenant_id = ${tenantId}`, or if this is intentionally a global sweep, make the multi-tenant nature explicit and add strict validation and auditing. Avoid unscoped reads on tenant tables unless the operation is explicitly authorized and handled with care.
- **server/agentic-engines.ts:247** — UPDATE on tenant-scoped table `ai_insights` in autoApplyEligibleInsights may run without tenant scoping when tenantId is omitted (tenantScope becomes empty): `UPDATE ai_insights SET status = 'applied', action_taken = ... WHERE status = 'new' AND category IN (...) ${tenantScope}` — potentially updates rows across all tenants.
  - _Fix:_ Require an explicit tenantId for tenant-scoped updates, or perform a controlled per-tenant iteration. If global sweep behavior is needed, make it explicit (e.g. require an admin flag) and add safeguards so accidental global updates cannot occur.
- **server/agentic-features.ts:861** — SELECT against tenant-scoped table `outreach_enrollments` lacks tenant_id constraint: `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'`. This checks enrollment across all tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${params.tenantId}) so the uniqueness/duplicate check is restricted to the caller's tenant. Always validate ownership in pre-insert checks.
- **server/agentic/approvals.ts:135** — Bulk UPDATE in expireStaleApprovals updates agentApprovals without a tenant filter: `UPDATE agent_approvals SET status = 'expired' ... WHERE status = 'pending' AND expiresAt < ...`. This will expire approvals across all tenants in one shot.
  - _Fix:_ Scope expirations to a tenant (accept a tenantId) or explicitly enumerate tenants and run tenant-scoped updates per tenant. If a global cron is intended, add strong safeguards and consider processing per-tenant within a transaction that validates tenant ownership before affecting related tenant-scoped tables.
- **server/agentic/wake-scheduler.ts:142** — Global UPDATE on tenant-scoped table `agent_wake_schedules` performed without a tenant_id constraint. The statement: `UPDATE agent_wake_schedules ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... )` can claim/modify rows across all tenants.
  - _Fix:_ Restrict the claim to a specific tenant (or iterate per-tenant). Add tenant scoping to the UPDATE (and/or the inner SELECT), e.g. include `WHERE tenant_id = ${tenantId}` or drive the scheduler to process each tenant separately so every UPDATE/SELECT on agent_wake_schedules includes `AND tenant_id = <tenant>`.
- **server/agentic/task-forces.ts:41** — INSERT into `task_forces` writes a caller-supplied foreign id (project_id = ${p.projectId}) without verifying the referenced project belongs to the caller's tenant. Because there is no DB-enforced FK/tenant constraint, this allows cross-tenant linking.
  - _Fix:_ Validate ownership before inserting: if p.projectId is provided, SELECT the project row and ensure `tenant_id = ${p.tenantId}` (or reject/unset projectId). Only persist project_id after confirming the project is owned by the same tenant (or perform the INSERT using a guarded statement that enforces the tenant constraint server-side).
- **server/business-tools.ts:127** — String-interpolated SQL fragment uses a request-derived value (params.status) — `const updates: string[] = [`status = '${params.status}'`, ...]`. This injects user data into SQL-text construction (even if not directly executed here) and is an unsafe pattern that can lead to SQL injection or unsafe future reuse.
  - _Fix:_ Do not compose SQL snippets by interpolating request data into raw strings. Validate status against an allowlist and use parameterized queries (drizzle sql templates) instead. Remove the unused vulnerable 'updates' string construction, or build updates using sql`` with ${params.status} (parameterized) or a controlled allowlist -> e.g. ensure status is one of VALID_INVOICE_STATUSES and then run the parameterized update: db.execute(sql`UPDATE invoices SET status = ${params.status}, amount_paid = COALESCE(${params.amount_paid || null}, amount_paid), updated_at = CURRENT_TIMESTAMP WHERE id = ${params.invoice_id} AND tenant_id = ${tid}`)
- **server/channel-routing.ts:31** — SELECT joins `personas` to `channel_routes` without constraining the `personas` rows to the caller's tenant (no WHERE p.tenant_id = <tenant> or equivalent). If `personas` is a tenant-scoped table this can leak persona names across tenants.
  - _Fix:_ Either scope channel_routes by tenant (add tenant_id to channel_routes and filter cr.tenant_id = tenantId) or, when joining to personas, include and enforce the caller's tenant filter (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = :tenantId). Ensure callers pass tenantId and the app enforces ownership before returning persona data.
- **server/channel-routing.ts:53** — INSERT into `channel_routes` uses caller-supplied personaId directly (VALUES (${channel}, ${personaId})) with no validation that the persona belongs to the caller's tenant. This allows binding a channel to a persona from another tenant or otherwise tampering with cross-tenant references.
  - _Fix:_ Validate ownership of the personaId before inserting (e.g. SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}) and/or record tenant_id in channel_routes and insert it explicitly. Reject the request if the persona is not owned by the caller's tenant.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` omits an explicit tenant_id constraint (WHERE tenant_id = ...). Relying on prior project ownership checks is not sufficient per the project's invariant.
  - _Fix:_ Add a tenant filter to the query, e.g. `SELECT ... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY ...` (or use Drizzle .where(eq(projectFiles.tenantId, tenantId))). Ensure every read against project_files includes the caller's tenant.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` omits an explicit tenant_id constraint (WHERE tenant_id = ...).
  - _Fix:_ Constrain the query to the caller's tenant, e.g. `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY ...`. Ensure every read against project_notes includes the tenant filter.
- **server/chat-engine/project-context.ts:46** — EXISTS(...) subquery reads `project_conversations pc` without an explicit tenant_id constraint on pc. If `project_conversations` is tenant-scoped, this query touches that table without the required tenant filter.
  - _Fix:_ Add an explicit tenant fence for project_conversations, e.g. `... WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId}` (or equivalent Drizzle eq). Do not rely on filtering only the joined `projects` row; every tenant-scoped table read must include the tenant constraint.
- **server/chat-engine/project-context.ts:27** — Query joining `project_conversations pc` to `projects p` uses `p.tenant_id = ${tenantId}` but does not include an explicit tenant constraint on `pc`. If `project_conversations` is tenant-scoped, this is a missing per-table tenant filter.
  - _Fix:_ Include `pc.tenant_id = ${tenantId}` in the WHERE clause (or use Drizzle eq on pc.tenantId). Ensure all accesses to project_conversations include tenant ownership checks on that table itself.
- **server/chat-engine/project-context.ts:184** — SELECT joining `project_conversations pc` and `conversations c` constrains `c.tenant_id` but does not constrain `pc` by tenant. If `project_conversations` is tenant-scoped, this is missing the required tenant_id filter on pc.
  - _Fix:_ Add an explicit tenant filter for `pc`, e.g. `WHERE pc.project_id = ${projectId} AND pc.tenant_id = ${tenantId} AND c.id != ${conversationId} AND c.tenant_id = ${tenantId}` (or use Drizzle eq on pc.tenantId). Ensure each tenant-scoped table read/write includes tenant_id = callerTenant.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` by project_id only (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) — missing tenant_id constraint. This can return notes belonging to other tenants if project_id values collide across tenants.
  - _Fix:_ Add tenant scoping to the query: WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}. Enforce tenant_id in the SELECT and any downstream processing to guarantee tenant isolation.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` by project_id only (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}) — missing tenant_id constraint. Can expose other tenants' file metadata.
  - _Fix:_ Constrain the query with tenant_id: WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}. Ensure all reads against tenant-scoped tables include the tenant predicate or run inside a per-tenant transaction (withTenantTx) that enforces RLS context.
- **server/deep-interview.ts:337** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ... omits tenant_id, violating the invariant that all tenant-scoped INSERTs must provide tenant_id explicitly.
  - _Fix:_ Include tenant_id in the inserted columns and values, e.g. INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, ${state.tenantId}, ${briefContent}, NOW() ...; or validate ownership and supply the tenantId from the trusted server-side context before inserting.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken performs a SELECT on dfy_intake_forms without constraining by tenant_id (no WHERE tenant_id = ...). This reads a tenant-scoped table by token only, enabling cross-tenant access if token handling is abused.
  - _Fix:_ Add an explicit tenant constraint or otherwise ensure the token→tenant mapping is enforced at the SQL layer (e.g. .where(and(eq(dfyIntakeForms.token, token), eq(dfyIntakeForms.tenantId, expectedTenantId)))) or document and review the token-only design as an intentional, audited exception.
- **server/embeddings.ts:811** — SELECT on tenant-scoped table `agent_knowledge` executed without tenant constraint (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ...). This touches rows across tenants.
  - _Fix:_ Run backfills in a tenant-scoped loop or add an explicit tenant_id filter to the query. If a global admin job must run across tenants, make the tenant intent explicit and document/guard it, and use tenant_id in subsequent UPDATEs to avoid accidental cross-tenant writes.
- **server/embeddings.ts:842** — SELECT from tenant-scoped table `memory_entries` without tenant_id (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200). This reads rows for all tenants.
  - _Fix:_ Limit the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate tenants explicitly; never run broad selects on tenant-scoped tables without an explicit tenant parameter.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} (no tenant filter). Updating by id alone on a tenant-scoped table can modify other tenants' rows if the operation was seeded by a non-tenant-scoped query.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or perform the update via a tenant-scoped transaction that guarantees ownership before writing.
- **server/embeddings.ts:858** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id (SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200). This reads entries across tenants.
  - _Fix:_ Scope these backfill queries by tenant_id (or run per-tenant), and ensure any later UPDATE references the same tenant_id to prevent accidental cross-tenant modifications.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} lacks tenant_id constraint. Same-risk cross-tenant write as above.
  - _Fix:_ Constrain the UPDATE with tenant_id (WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or otherwise perform per-tenant updates where tenant ownership is explicitly checked before writing.
- **server/google-drive.ts:867** — SELECT from tenant-scoped table 'projects' is performed with where(eq(projects.id, projectId)) but without scoping by projects.tenant_id. Reads against tenant-scoped tables must include an application-level tenant filter.
  - _Fix:_ Include the tenant check in the WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise fail-closed if tenant ownership cannot be proven before the SELECT.
- **server/google-drive.ts:1474** — SELECT from 'projects' during uploadAndShare is performed with where(eq(projects.id, params.projectId)) and no tenant filter. A caller-supplied project_id is being looked up without scoping to the caller tenant.
  - _Fix:_ Restrict the SELECT by tenant (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId || <caller tenant>)))) or otherwise verify project.tenantId equals the caller's tenant BEFORE proceeding.
- **server/glasses-gateway.ts:157** — SELECT from 'apiKeys' uses only keyHash/isRevoked predicates (.where(and(eq(apiKeys.keyHash, keyHash), eq(apiKeys.isRevoked, false)))) with no tenant scoping. If apiKeys is a tenant-scoped table this read lacks an explicit tenant filter.
  - _Fix:_ If apiKeys is tenant-scoped, include tenant scoping where appropriate or otherwise ensure the lookup cannot cross tenants. If lookup by keyHash intentionally returns the owning tenant, ensure subsequent writes/updates also include tenant filters or re-verify ownership before mutating.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id (missing tenant scoping). The INSERT writes session-related/per-tenant data without an explicit tenant_id column or ownership check.
  - _Fix:_ If presenter_slide_images is tenant-scoped, include tenant_id in the INSERT (and in the table schema if absent), or derive/validate the tenant from the session (JOIN or prior lookup) and pass it as a parameter. If the table is intentionally global, document why and ensure session_id ownership is verified before writing.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint. This updates a potentially tenant-scoped session row using only the session id, allowing cross-tenant modification if sessionId can be influenced or reused.
  - _Fix:_ Constrain the UPDATE to the owning tenant, e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}, and verify the caller actually owns the session before updating. Alternatively, fetch and assert session.tenant_id === tenantId before performing the UPDATE.
- **server/health-audit.ts:314** — SELECT FROM code_proposals has no tenant_id filter. This reads proposal rows across tenants (and the subsequent UPDATE/archival acts on IDs gathered from that global SELECT).
  - _Fix:_ Scope the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or make the cross-tenant nature explicit and gated (admin-only), and ensure any later UPDATE uses a tenant-qualified WHERE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or performs an explicit ownership check/audit log before modifying rows.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) performs a write without constraining by tenant_id, potentially modifying other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE, for example: WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}. If this is intended to be an admin cross-tenant archival, require an explicit admin authorization and make the tenant(s) explicit in the query.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (enabled + last_run_at conditions) runs without a tenant_id filter and enumerates tasks across tenants. Later code updates these rows without tenant scoping when apply=true.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, either (a) limit the SELECT to a tenant (WHERE tenant_id = ${tenantId}), or (b) if this admin/global scan is intentional, require explicit admin authorization and ensure all subsequent UPDATEs include tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform explicit per-row tenant-aware updates to avoid accidental cross-tenant changes.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables tasks by id only, with no tenant_id constraint — this can affect tasks belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform per-row checks to ensure the task belongs to the intended tenant(s). If disabling tasks globally is intended, make the admin-only nature explicit and add auditing/authorization checks.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT from tenant-scoped table `heartbeat_tasks` without any tenant_id constraint (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ...). This reads rows across tenants with no per-tenant filter.
  - _Fix:_ If this function is intentionally a cross-tenant scheduler, keep its name but document and centralize cross-tenant access; otherwise add a tenant_id WHERE clause or accept a tenant parameter. Prefer using an explicit, auditable scheduler-only API and/or require an ALL_TENANTS flag to be explicit.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint (UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ...). The UPDATE modifies tenant-scoped rows by id only, with no tenant binding.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. AND tenant_id = ${expectedTenantId}) or route the change through a tenant-aware storage API. If this routine is intentionally cross-tenant, make that explicit and add auditing/authorization checks.
- **server/heartbeat.ts:255** — SELECT from tenant-scoped table `event_log` with no tenant filter (db.execute(sql`SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10`)). This reads pending events across tenants in a non-scoped query.
  - _Fix:_ If processing should be cross-tenant, keep it only in a dedicated scheduler boundary with strong review and explicit naming; otherwise add WHERE tenant_id = ${tenantId} (or otherwise scope by tenant) so event rows are only read within the correct tenant context.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} lacks a tenant_id constraint — a tenant-facing caller could misuse this to infer or interact with other tenants' jobs if not strictly admin-only.
  - _Fix:_ Make this explicit admin-only functionality (and enforce authorization), or require the caller to pass an expected tenantId and validate it. Prefer parameterized queries including tenant_id when this is used in tenant-scoped flows.
- **server/job-queue.ts:406** — listJobs builds a SELECT from agent_jobs that may execute without any tenant_id WHERE clause (tenantId is optional). This can list jobs across tenants if tenantId is omitted.
  - _Fix:_ Require an explicit tenantId unless the caller is an operator with elevated privileges (and enforce that in auth). Default tenant-facing APIs to always include tenant_id = ${tenantId} to avoid accidental cross-tenant data exposure.
- **server/job-worker.ts:148** — research_code_proposal job handler reconstructs a session including tenantId from job.payload and passes it to generateCodeProposal without verifying payload.tenantId matches job.tenantId (the worker's tenant context). This allows a job payload to cause reads/writes for a different tenant.
  - _Fix:_ Validate that payload.tenantId (or any referenced foreign id) equals job.tenantId before using it, or prefer using the authoritative job.tenantId and ignore tenant values coming from the payload. Add explicit checks and fail fast on mismatches.
- **server/job-worker.ts:168** — research_digest handler reads tenantId from job.payload and calls generateResearchDigest(tenantId) without verifying the payload tenant matches the job's tenant (job.tenantId). This can cause cross-tenant reads/writes driven by untrusted payload data.
  - _Fix:_ Assert payload.tenantId === job.tenantId (or otherwise validate ownership) before invoking tenant-scoped work. Use the worker's job.tenantId as the source of truth whenever possible.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes a SELECT over agent_knowledge grouping by tenant_id/persona_id without a tenant filter — it enumerates rows across all tenants in a single query (reads tenant-scoped data unscoped).
  - _Fix:_ If this diagnostic must be multi-tenant, run it from an admin-only path with strict authorization and audit, or change the implementation to iterate per-tenant (e.g. fetch tenant ids and then scope subsequent queries by tenant_id). Ensure any global scans are intentional and access-controlled.
- **server/lib/aevo-meta-editor.ts:450** — Update to procedureEvolutionRuns uses only the run id in WHERE (eq(procedureEvolutionRuns.id, runId)) and does NOT constrain the update to the tenant (tenantId). This allows cross-tenant row mutation on a tenant-scoped table.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) so all updates are scoped to the caller's tenant. Ensure runId/tenantId binding cannot be influenced by an untrusted caller.
- **server/lib/aevo-meta-editor.ts:460** — Update to procedureEvolutionRuns uses only the run id in WHERE (eq(procedureEvolutionRuns.id, runId)) and does NOT constrain the update to the tenant (tenantId). This allows cross-tenant row mutation on a tenant-scoped table.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) so all updates are scoped to the caller's tenant. Use the local tenantId variable available in this function.
- **server/lib/aevo-meta-editor.ts:472** — Update to procedureEvolutionRuns uses only the run id in WHERE (eq(procedureEvolutionRuns.id, runId)) and does NOT constrain the update to the tenant (tenantId). This allows cross-tenant row mutation on a tenant-scoped table.
  - _Fix:_ Add an explicit tenant check to the WHERE clause (and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Avoid updating by id alone on tenant-scoped tables.
- **server/lib/aevo-meta-editor.ts:500** — Final update to procedureEvolutionRuns uses only the run id in WHERE (eq(procedureEvolutionRuns.id, runId)) and does NOT constrain the update to the tenant (tenantId). This permits cross-tenant updates.
  - _Fix:_ Scope the update to the tenant by adding eq(procedureEvolutionRuns.tenantId, tenantId) to the WHERE (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))). Ensure every UPDATE/DELETE on tenant-scoped tables includes the tenant predicate.
- **server/lib/customer-revenue-launchpad-seed.ts:90** — SELECT from tenant-scoped table `project_notes` is missing a tenant_id constraint (uses only project_id and note).
  - _Fix:_ Constrain the SELECT to the caller/owner tenant. For example: SELECT id FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${OWNER_TENANT_ID} AND note = ${POINTER_NOTE} ... — or join to projects and check p.tenant_id = ${OWNER_TENANT_ID} so the lookup is explicitly tenant-scoped.
- **server/lib/customer-revenue-launchpad-seed.ts:103** — INSERT into `project_notes` does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (no FK-based implicit ownership).
  - _Fix:_ Include tenant_id in the INSERT and bind it explicitly (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projectId}, ${OWNER_TENANT_ID}, ${POINTER_NOTE}, 'seed-...')). Alternatively verify the project belongs to the tenant and insert tenant_id derived from that verified project row.
- **server/lib/ideabrowser-backfill.ts:49** — Query aggregates project counts across all tenants without constraining to a specific tenant. The SQL `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id` reads tenant-scoped rows for every tenant in the DB, violating the invariant that reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Avoid cross-tenant SELECTs. Run per-tenant queries that include an explicit tenant_id filter (e.g. iterate wantedByTenant keys and for each do `SELECT count(*) FROM projects WHERE tenant_id = ${t} AND tags && ${IDEA_TAG_LITERAL}::text[]`) or otherwise ensure the operation runs under an explicit admin flow that enforces and documents authority for multi-tenant reads. Do NOT perform an unbounded GROUP BY tenant_id on tenant-scoped tables unless you have an explicit, auditable admin-only guard and appropriate access controls.
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey() performs a SELECT from the tenant-scoped table `mcp_api_keys` using only `key_prefix = ${prefix}` (no WHERE tenant_id = …). This reads a row from another tenant's namespace during authentication lookup and violates the project invariant that every read against a tenant-scoped table must be constrained to the caller's tenant.
  - _Fix:_ Avoid unconstrained reads. If the caller can supply a tenantId, include WHERE tenant_id = ${tenantId}. For the bearer-key verification flow (where the tenant is unknown and must be resolved from the key), avoid selecting arbitrary tenant rows by moving the hash check into the query so the DB only returns a row when the presented token actually matches: e.g. SELECT id, tenant_id, scopes FROM mcp_api_keys WHERE key_prefix = ${prefix} AND key_hash = ${expectedHash} AND revoked_at IS NULL LIMIT 1. Alternatively, implement a DB-side verification function/stored-procedure that takes the presented token and returns tenant_id only on an exact hash match, or make key_prefix globally unique and document that lookup surface as an explicit exception. Ensure minimal columns are returned and add limits to prevent any cross-tenant data exposure.
- **server/lib/outcome-promotion.ts:158** — Query against tenant-scoped table deliverable_grade_events without constraining tenant_id (SELECT ... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format). This performs a cross-tenant read using application SQL rather than enforcing tenant isolation per-tenant.
  - _Fix:_ Run the aggregation per tenant (or explicitly constrain the query to a single tenant) instead of selecting across all tenants. For example, iterate over tenants and run: SELECT format, count(*)::int AS total, count(*) FILTER (WHERE NOT passed)::int AS failed FROM deliverable_grade_events WHERE tenant_id = ${tenantId} AND created_at > ... GROUP BY format. Ensure any file generation or downstream processing is performed only for that tenant context.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from `agent_runs` scans rows in the tenant-scoped table without a tenant_id WHERE clause (reads tenant-scoped data across all tenants).
  - _Fix:_ Add a tenant_id filter to the query or accept a tenantId parameter and run per-tenant. If this is intended to be an admin cross-tenant audit, make the admin-only intent explicit and ensure callers are restricted; otherwise ensure application-level tenant predicates are present on all reads.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from `mind_tickets` queries a tenant-scoped table with no tenant_id predicate (returns rows across tenants).
  - _Fix:_ Require and apply a tenant_id filter, or run this audit per-tenant via a withTenantTx/explicit tenant parameter. If an aggregated cross-tenant admin view is required, restrict access to admin-only callers and document the exception; otherwise enforce tenant-level WHERE clauses.
- **server/lib/system-probes.ts:163** — Direct db.select(...) from messages table performs count(*) with no tenant_id filter (tenant-scoped table queried without tenant predicate).
  - _Fix:_ Constrain the count by tenant_id (pass tenantId into the probe) or perform per-tenant counts under an explicit admin aggregation function that is access-controlled and audited. Avoid issuing queries against tenant-scoped tables without an explicit tenant predicate unless running in a documented, tightly controlled admin-only path.
- **server/memory-intelligence.ts:399** — UPDATE against tenant-scoped table `knowledge_triples` omits a tenant_id constraint in the WHERE clause (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}).
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. ... WHERE id = ${c.id} AND tenant_id = ${tenantId}) so the mutation is constrained to the caller's tenant. Alternatively, re-check ownership before updating (SELECT ... WHERE id = ${c.id} AND tenant_id = ${tenantId}) and fail if not found.
- **server/pace-control.ts:85** — checkPace queries heartbeat_logs without tenant scoping: FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}. If heartbeat_logs is tenant-scoped, this reads aggregate usage across tenants and could mis-enforce quotas across tenant boundaries.
  - _Fix:_ Require and pass an explicit tenantId into checkPace (or otherwise determine tenant context) and add WHERE tenant_id = ${tenantId} to the queries. All reads that enforce per-tenant rate/cap limits must be constrained to that tenant to preserve isolation and correct accounting.
- **server/pace-control.ts:140** — getPaceSnapshot executes unscoped queries against heartbeat_logs (SELECT COUNT(...) FROM heartbeat_logs WHERE created_at > ... and subsequent per-persona aggregation) with no tenant_id filter, returning cross-tenant aggregates.
  - _Fix:_ Make getPaceSnapshot tenant-aware (accept tenantId) and add WHERE tenant_id = ${tenantId} to all heartbeat_logs queries. If a global operator view is required, gate it behind an explicit admin-only API and log/authorize its usage.
- **server/persona-export.ts:32** — exportPersona does SELECT * FROM personas WHERE id = ${personaId} with no tenant_id constraint. That reads a persona record by id without verifying it belongs to the caller's tenant (ownership not validated).
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) and fail if no row is returned. Validate that the persona belongs to the requested tenant before exporting. If personas can be shared/globally-scoped, make that explicit and handle shared vs tenant-owned cases separately with explicit checks.
- **server/plan-executor.ts:793** — Boot-time recovery UPDATE resets executing plans across all tenants: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id`. This write touches tenant-scoped rows with no tenant_id constraint and can affect plans for every tenant.
  - _Fix:_ Limit rollback to an explicit tenant context (pass tenantId or restrict to ADMIN_TENANT_ID if this is intended as an admin operation) or iterate per-tenant and validate admin privileges before performing the update. If truly global recovery is required, gate this code behind an explicit, auditable admin check and log the tenant set being affected.
- **server/project-brain.ts:135** — SELECT from tenant-scoped table `projects` omits tenant filter. updateProjectBrain() queries `projects` by id only (`WHERE id = ${projectId}`) and does not accept or verify a tenant, allowing read access to projects across tenants.
  - _Fix:_ Require and pass a tenantId to updateProjectBrain (or otherwise determine caller tenant) and add `AND tenant_id = ${tenantId}` to the SELECT. Always verify the project belongs to the caller's tenant before reading/writing related data.
- **server/project-brain.ts:332** — Query enumerates projects across tenants. The backfill query `SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0` has no tenant scoping and will iterate projects from all tenants.
  - _Fix:_ Scope the backfill to a single tenant (pass tenantId) or add `WHERE p.tenant_id = ${tenantId}` / `AND c.tenant_id = ${tenantId}` so it only processes projects for the intended tenant. Avoid global scans across tenants.
- **server/project-brain.ts:349** — SELECT from tenant-scoped table `conversations` omits tenant filter. Backfill selects conversations by project id (`WHERE c.project_id = ${proj.id}`) without verifying the conversation/project tenant.
  - _Fix:_ Include tenant scoping (e.g. `AND c.tenant_id = ${tenantId}`) or derive tenant from the project and validate before selecting conversations. Ensure the conversation belongs to the same tenant before reading messages.
- **server/project-brain.ts:359** — SELECT from tenant-scoped table `messages` omits tenant filter. The backfill reads messages for a conversation (`WHERE conversation_id = ${conv.id}`) without verifying tenant ownership, enabling cross-tenant data disclosure.
  - _Fix:_ Ensure the conversation is verified to belong to the caller's tenant and/or add `AND tenant_id = ${tenantId}` to the messages query. Always constrain message reads by tenant or by a previously-validated parent resource tied to the tenant.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table agent_knowledge performed without constraining by tenant_id (WHERE id = ${r.id} only). This can modify another tenant's row if IDs are misused or race conditions occur.
  - _Fix:_ Include the tenant constraint in the UPDATE predicate and verify the row belongs to the expected tenant. e.g. UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. Also check the affected rowCount/RETURNING to detect unexpected cross-tenant updates.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table memory_entries performed using only id in WHERE clause (WHERE id = ${storedId}). No tenant_id constraint is present, allowing potential cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant (and optionally persona) to enforce tenant isolation. e.g. UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId} AND tenant_id = ${input.tenantId} (and/or AND persona_id = ${personaId}). Also validate the returned rowCount/RETURNING to ensure the update applied to the expected tenant row.
- **server/routes.ts:2021** — INSERT INTO presenter_sessions sets tenant_id from a request body field (tenantId) without validating ownership or that the caller is authorized to act for that tenant. This lets the caller (or a compromised internal client) create rows attributed to arbitrary tenants.
  - _Fix:_ Do NOT accept tenantId from untrusted request body. Derive tenantId from the authenticated session or from a validated internal mapping. If this route must accept an explicit tenant id, enforce strong server-side validation (verify the caller is a platform/admin identity and that the tenant exists and the caller is authorized), or remove tenantId from the payload and use the caller's tenant.
- **server/routes.ts:4542** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code inserts (project_id, note, author) VALUES (...) without explicitly setting tenant_id, relying only on prior SELECT ownership check. The invariant requires every INSERT into tenant-scoped tables to pass tenant_id explicitly (columns are NOT NULL with no default) to enforce tenant isolation at the application layer.
  - _Fix:_ Include tenant_id in the INSERT statement (or call a storage helper that requires tenantId). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Also ensure the tenantId used is the same tenant validated earlier and prefer using parameterized Drizzle helpers or storage APIs that accept tenantId to avoid accidental cross-tenant writes.
- **server/routes/admin-zombie-detector.ts:38** — Query in the `used` CTE selects from `agent_trace_spans` (agent usage) with no tenant_id filter. This reads tenant-scoped telemetry/data across all tenants rather than scoping to the caller's tenant.
  - _Fix:_ Constrain the CTE by the caller's tenant: add `AND tenant_id = ${tenantId}` (or equivalent Drizzle .where(eq(...))) in the `used` subquery so only rows for the resolved tenant are returned. Ensure tenantId is passed into the transaction and bound as a parameter (do not concatenate it into a raw SQL string).
- **server/routes/admin-zombie-detector.ts:54** — Query in the `used` CTE selects from `conversations` without any `tenant_id` constraint. `conversations` is a tenant-scoped table in this codebase, so this will aggregate across all tenants and leak cross-tenant data.
  - _Fix:_ Add a tenant filter to the `used` subquery, e.g. `WHERE persona_id IS NOT NULL AND tenant_id = ${tenantId} AND updated_at > ...`. Use parameterized binding (Drizzle sql template or .where(eq(...))) and validate/derive tenantId from the authenticated request rather than trusting client input.
- **server/routes/conversations.ts:118** — UPDATE on tenant-scoped table `conversations` without tenant constraint. Query: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` lacks `AND tenant_id = ${tenantId}`.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause (e.g. `WHERE id = ${conv.id} AND tenant_id = ${tenantId}`) so the write cannot affect rows owned by other tenants.
- **server/routes/conversations.ts:122** — INSERT into `project_conversations` omits tenant identifier. Query: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id.
  - _Fix:_ Include the tenant_id column on insert (and derive it from the authenticated tenant): `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})` (and ensure any UNIQUE/index expectations are adjusted accordingly).
- **server/routes/conversations.ts:124** — UPDATE on tenant-scoped table `projects` without tenant constraint. Query: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` lacks `AND tenant_id = ${tenantId}`.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) to prevent updating projects belonging to other tenants.
- **server/routes/conversations.ts:256** — INSERT into `project_notes` omits tenant identifier. Query: `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id.
  - _Fix:_ Include tenant_id when inserting audit/archive notes (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`) so notes are correctly scoped to the tenant.
- **server/routes/inbox-notifications.ts:101** — An UPDATE to inbox_messages omits tenant scoping: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`. This mutates a tenant-scoped table without constraining tenant_id in the WHERE clause.
  - _Fix:_ Include the tenant predicate on the UPDATE (e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) so only rows owned by the calling tenant may be modified. Prefer using the existing storage layer helper that accepts tenantId or parameterized queries with tenantId to avoid accidental cross-tenant writes.
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` is executed without constraining to a caller tenant (no WHERE tenant_id = ...). This exposes cross-tenant aggregate data via the /metrics endpoint.
  - _Fix:_ Add an explicit tenant filter (WHERE tenant_id = ${tenantId}) when the intent is per-tenant metrics, or if global aggregation is required, ensure this endpoint is only reachable to an appropriately authorized platform operator and document that it reads global data. Prefer parameterized tenant-scoped queries for any data derived from tenant-scoped tables.
- **server/routes/projects.ts:596** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is executed without constraining by tenant_id. Although the code verified project ownership earlier, the UPDATE itself lacks AND tenant_id = ${tenantId}, allowing a malformed request or bug to update a foreign-tenant project row if projectId is manipulated.
  - _Fix:_ Include the tenant predicate on the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Prefer always embedding tenant constraints into every write to tenant-scoped tables rather than relying on prior checks alone.
- **server/routes/projects.ts:664** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is executed without constraining by tenant_id. This mirrors the same missing tenant filter in the files-base64 handler and can allow cross-tenant updates.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE so the write is explicitly scoped to the caller's tenant. Ensure all UPDATE/DELETE statements touching tenant-scoped tables include tenant_id predicates.
- **server/routes/slack.ts:124** — Query against tenant-scoped `personas` table does not constrain `p.tenant_id` — it can return rows from any tenant. This SELECT matches p.name across all personas without scoping to the platform/global tenant or the caller's tenant, allowing cross-tenant enumeration.
  - _Fix:_ Restrict the query to only the intended persona rows. For example, only match global/platform personas (e.g. tenant_id = 1) and the caller's tenant, e.g. add `AND (p.tenant_id = 1 OR p.tenant_id = ${tenantId})` (or use Drizzle: .where(or(eq(personas.tenantId, PLATFORM_TENANT_ID), eq(personas.tenantId, tenantId)))). Avoid leaving tenant_id unconstrained on tenant-scoped tables.
- **server/routes/slack.ts:191** — Enumeration query over `personas` selects COALESCE(tpn.display_name, p.name) from `personas` without constraining p.tenant_id. This exposes persona names from other tenants and leaks tenant-scoped data.
  - _Fix:_ Add an explicit tenant constraint so only platform/global personas and the caller's tenant are returned. E.g. in the SQL add `AND (p.tenant_id = 1 OR p.tenant_id = ${tenantId})` or, using Drizzle, .where(or(eq(personas.tenantId, PLATFORM_TENANT_ID), eq(personas.tenantId, tenantId))). Ensure all reads from tenant-scoped tables include a tenant_id filter.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has NO tenant_id filter. If `heartbeat_tasks` is tenant-scoped, this aggregates across tenants and subsequent actions act globally.
  - _Fix:_ Scope the query to the intended tenant(s) (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true AND type = 'delegation'), or explicitly document and assert that this script must operate as a global admin. Prefer explicit tenant scoping for tenant-scoped tables.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has NO tenant_id constraint and will affect rows across tenants if `heartbeat_tasks` is tenant-scoped.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise restrict the update to the intended tenant(s). Avoid global updates on tenant-scoped tables without explicit intent and safeguards.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant scoping, potentially counting across tenants.
  - _Fix:_ Filter by tenant_id (e.g. tenant_id = ${ADMIN_TENANT_ID}) or otherwise constrain the operation to the correct tenant. Ensure subsequent disabling updates are also tenant-scoped.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is missing tenant_id and will disable tasks across tenants if the table is tenant-scoped.
  - _Fix:_ Include AND tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant) in the WHERE clause, or explicitly require per-tenant execution. Avoid unscoped updates to tenant-scoped tables.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has NO tenant_id constraint; subsequent UPDATE also lacks tenant scoping. These statements may affect other tenants' heartbeat_tasks.
  - _Fix:_ Restrict both the SELECT and UPDATE to the intended tenant(s) by adding tenant_id = ${ADMIN_TENANT_ID} to the WHERE clauses, or run the cleanup per-tenant. Document and assert the intended scope if global changes are truly desired.
- **server/seed.ts:41** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped but this read scans all tenants.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE tenant_id = ${tenantId} or use Drizzle .where(eq(researchSessions.tenantId, tenantId))). If the intent is to check only a single tenant (admin/owner), pass that tenant id explicitly and document the elevated scope.
- **server/seed.ts:2171** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... performs a write against the projects table without constraining by tenant_id. projects is a tenant-scoped table, so this can modify another tenant's data.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId} or use Drizzle .where(eq(projects.tenantId, tenantId)). If this is a migration targeting a specific tenant, pass that tenant id explicitly and avoid relying on global identifiers alone.
- **server/seed.ts:2239** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... checks conversations (tenant-scoped) without filtering by tenant_id, then inserts project→conversation links. This can enumerate or operate across tenants.
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. WHERE c.tenant_id = ${tenantId}) or join on the project tenant and ensure both project and conversation belong to the same tenant before inserting links. Always validate ownership before cross-row inserts/updates.
- **server/seed.ts:3662** — UPDATE memory_entries (REPLACE 'Manjaro'→'wellness-program') runs without a tenant_id constraint. memory_entries is tenant-scoped (idx_memory_entries_tenant_id exists), so this will modify rows for every tenant.
  - _Fix:_ Add an explicit tenant_id filter (e.g. AND tenant_id = ${targetTenantId}) or run the update per-tenant. Ensure all UPDATEs against tenant-scoped tables include WHERE tenant_id = <caller tenant>.
- **server/seed.ts:3688** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped (idx_conversations_tenant_id exists), so this will update every tenant's conversations.
  - _Fix:_ Constrain the UPDATE to the intended tenant(s) by adding AND tenant_id = ${tenantId} or perform the update in a per-tenant loop. Always include tenant_id in UPDATEs on tenant-scoped tables.
- **server/skillify.ts:91** — SELECT from messages performed without constraining by tenant_id (reads tenant-scoped messages by conversationId only).
  - _Fix:_ Include the tenant filter in the query (e.g. add AND messages.tenant_id = ${tenantId} or join via conversations and constrain by conversations.tenant_id) so every read against messages enforces the caller's tenant.
- **server/skillify.ts:201** — SELECT from messages performed without constraining by tenant_id (reads messages for a conversation with no tenant_id WHERE clause).
  - _Fix:_ Add an explicit tenant constraint to the messages query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) so the read cannot cross tenants.
- **server/skillify.ts:329** — SELECT recent messages for a conversation (messages table) without a tenant_id constraint when assembling excerpt for skillify — tenant-scoped read lacks explicit tenant filter.
  - _Fix:_ Add tenant scoping to this messages query (e.g. where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or otherwise validate and enforce tenant ownership at the SQL level for every messages read.
- **server/skills-marketplace.ts:209** — SELECT id FROM skills WHERE name = ${template.name} has no tenant_id constraint — querying a tenant-scoped skills table without scoping can return another tenant's installed skill (cross-tenant disclosure).
  - _Fix:_ Constrain the lookup by tenant (e.g. WHERE name = ${template.name} AND tenant_id = ${tenantId}) or otherwise make the intent explicit (global vs tenant-local). Pass/require tenantId where appropriate.
- **server/skills-marketplace.ts:235** — SELECT * FROM skills WHERE id = ${skillId} occurs with no tenant constraint — exporting a skill by id without tenant scoping may expose another tenant's skill data.
  - _Fix:_ Require and use tenantId in exportSkill and add WHERE tenant_id = ${tenantId} to the SELECT (or verify ownership prior to returning skill data).
- **server/storage.ts:289** — SELECT against tenant-scoped table `messages` without a tenant_id constraint (attachCitationMetadata runs: `SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL`).
  - _Fix:_ Include tenant scoping in the query (e.g. accept/pass a tenantId and add `AND tenant_id = ${tenantId}` or join to validate ownership) so the lookup cannot return another tenant's rows.
- **server/storage.ts:581** — SELECT from tenant-scoped table `memory_entries` with no tenant filter (getAllMemoriesForBackup selects all memory entries platform-wide).
  - _Fix:_ Restrict this operation to an explicit admin-only path or accept a tenantId/ALL_TENANTS sentinel and enforce requiredTenantScope/tenantScope so callers cannot accidentally return other tenants' data.
- **server/storage.ts:590** — SELECT from tenant-scoped table `memory_entries` may be executed without tenant_id constraint when tenantId is omitted (getMemoryEntry warns but proceeds to run WHERE id = ... with no tenant filter).
  - _Fix:_ Either require tenantId (use requiredTenantScope/assertValidTenantId) or make the caller pass an explicit ALL_TENANTS sentinel for platform-wide reads; do not silently run unscoped selects. Add `AND tenant_id = ${tenantId}` to the WHERE when expecting tenant-scoped access.
- **server/storage.ts:915** — SELECT from tenant-scoped table `memory_entries` without tenant filter (getMemoriesWithoutEmbeddings returns rows platform-wide).
  - _Fix:_ Restrict this operation with an explicit tenant parameter or require the ALL_TENANTS sentinel and perform requiredTenantScope validation; add tenant scoping to the WHERE when appropriate.
- **server/storage.ts:921** — SELECT from tenant-scoped table `agent_knowledge` without tenant filter (getKnowledgeWithoutEmbeddings returns knowledge rows across tenants).
  - _Fix:_ Require an explicit tenant scope (tenantId or ALL_TENANTS sentinel) and add the tenant equality predicate to the WHERE (use requiredTenantScope) or gate this function to admin-only contexts.
- **server/storage.ts:1057** — UPDATE on tenant-scoped table `heartbeat_tasks` can be performed unscoped when tenantId is omitted (updateHeartbeatTask builds `WHERE id = ${id}` and only conditionally adds a tenant subselect if tenantId provided).
  - _Fix:_ Require and validate tenantId for mutation paths (use requiredTenantScope or requireTenantScope) and include a direct `AND tenant_id = ${tenantId}` predicate rather than a subselect, to avoid unintended cross-tenant updates.
- **server/storage.ts:1064** — DELETE on tenant-scoped table `heartbeat_tasks` can be performed unscoped when tenantId is omitted (deleteHeartbeatTask builds `WHERE id = ${id}` and only conditionally adds a tenant subselect).
  - _Fix:_ Require and validate tenantId for delete operations or use requiredTenantScope so the DELETE always includes `AND tenant_id = ${tenantId}` (or is explicitly allowed only in an admin context).
- **server/tool-learning.ts:129** — SELECT FROM custom_tools checks only name (where(eq(customTools.name, parsed.name))) with no tenant_id constraint — this reads a tenant-scoped table without scoping to the caller's tenant and may leak cross-tenant existence or behavior.
  - _Fix:_ Scope the lookup to the creating tenant, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Ensure the uniqueness check is performed within the tenant namespace (or explicitly handle admin-tenant semantics).
- **server/tool-learning.ts:155** — After insertion, the code re-selects the tool by id via WHERE id = inserted.id with no tenant_id constraint. Reads against tenant-scoped tables must include tenant filtering.
  - _Fix:_ Include the tenant filter when re-reading rows, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). If the function operates in a transactional/admin context, still assert tenant ownership before trusting the row.
- **server/tool-learning.ts:291** — Auto-fix path updates custom_tools implementation using WHERE eq(customTools.id, tool.id) but omits tenant_id filtering — an update against a tenant-scoped table without tenant constraint can affect other tenants' rows.
  - _Fix:_ Include the tenant constraint in the UPDATE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId))). Ensure tool objects carry tenantId (or pass tenantId into tryAndRevert) and validate it before the update.
- **server/tool-learning.ts:316** — revertToolRow deletes a custom_tools row via WHERE eq(customTools.id, tool.id) with no tenant_id constraint — this can delete another tenant's tool if misused.
  - _Fix:_ Constrain the DELETE by tenant_id as well, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Ensure the function receives/derives the correct tenantId and validate it before performing the delete.
- **server/tool-learning.ts:365** — executeCustomTool increments usageCount with an UPDATE that uses WHERE eq(customTools.id, tool.id) but omits a tenant_id filter — writes to a tenant-scoped table must be scoped to tenant_id.
  - _Fix:_ Include the tenant constraint when updating usageCount, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the tenantId used is the same one validated when the tool was looked up, or pass tenantId explicitly into the function to enforce ownership.
- **server/tools.ts:8881** — UPDATE on tenant-scoped table `proposed_skills` uses only id in WHERE (where(eq(proposedSkills.id, row.id))) and omits a tenant filter. This allows accidental cross-tenant updates if IDs collide or admin tooling is abused.
  - _Fix:_ Constrain updates by tenant: include tenant guard in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or use an API that binds tenantId into the update to ensure the row belongs to the caller's tenant.
- **server/tools.ts:8924** — UPDATE on tenant-scoped table `proposed_skills` uses only id in WHERE (where(eq(proposedSkills.id, row.id))) and omits tenant scoping. This updates proposal rows without verifying tenant ownership.
  - _Fix:_ Add tenant constraint to the update (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) so only the originating tenant's row can be modified. Prefer using the stored tenantId from the created row and the caller's tenant for the check.
- **server/tools.ts:8941** — UPDATE on tenant-scoped table `proposed_skills` uses only id in WHERE (where(eq(proposedSkills.id, row.id))) and omits tenant scoping. This can allow cross-tenant modification of proposals.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or otherwise assert the row's tenant before updating. Ensure all UPDATE/DELETE operations on tenant-scoped tables always include tenant_id in their predicates.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE query does not restrict by tenant_id. The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` lacks a tenant filter, allowing an attacker (or caller) to update a workflow row owned by another tenant by supplying/using a known id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or the equivalent Drizzle .where(eq(table.tenantId, tenantId))). Alternatively, fetch and verify ownership in a transaction before updating (i.e. SELECT ... WHERE id = ${workflow.id} AND tenant_id = ${tenantId} and fail if not found) to ensure tenant isolation.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table 'knowledge_triples' omits tenant constraint. The code runs: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}`); — this UPDATE does not include tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE to the caller tenant. e.g. include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise pass/validate tenant ownership in the same statement) so the update cannot affect rows belonging to other tenants.
- **server/tools/domains/files/handlers.ts:941** — UPDATE against tenant-scoped table 'projects' omits tenant constraint. The code runs: await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`); — there is no WHERE tenant_id = ${tenantId} guard.
  - _Fix:_ Restrict the UPDATE to the caller's tenant. Add a tenant predicate to the WHERE clause (e.g. WHERE id = ${projId} AND tenant_id = ${tenantId}) or otherwise verify ownership when performing the UPDATE so projects from other tenants cannot be modified.
- **server/tools/domains/security/handlers.ts:163** — Unscoped SELECT from tenant-scoped table `provider_keys` (SELECT provider, LENGTH(api_key) ...) without a tenant_id filter. This returns provider keys across all tenants (even though the code branch runs only for admin tenant), violating the requirement that tenant-scoped reads must include an application-level tenant filter.
  - _Fix:_ Constrain the query with an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise implement an explicit, auditable admin-only path that documents and justifies cross-tenant access. Prefer passing tenantId as a query parameter (scanSql`... WHERE tenant_id = ${tenantId}`) to avoid accidental cross-tenant data exposure.
- **server/tools/domains/security/handlers.ts:260** — Unscoped SELECT from tenant-scoped table `provider_keys` (SELECT COUNT(DISTINCT provider) ...) with no tenant_id constraint. This aggregates provider key data across all tenants and can leak cross-tenant information when executed by an admin context.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or implement a clearly documented, audited admin-only aggregation that intentionally and safely accesses cross-tenant data. Use parameterized queries (scanSql`... WHERE tenant_id = ${tenantId}`) rather than unscoped queries to enforce tenant isolation.
- **server/twilio.ts:103** — SELECT from tenant-scoped table `agent_knowledge` is executed without constraining by tenant_id. The query loads candidate pairing rows across all tenants (`WHERE category = 'messaging_pairing' AND title = ${channel}:${phone}`) which can return rows belonging to other tenants.
  - _Fix:_ Avoid scanning agent_knowledge across tenants. If you only intend to look up pairings for a specific tenant, add AND tenant_id = ${tenantId} (or equivalent Drizzle filter). If cross-tenant detection/ambiguity-checking is intentionally required, perform that work in a strictly-audited operator/admin flow or use a dedicated admin-only query path that does not expose tenant-scoped rows to normal request handlers, and ensure any returned rows are treated as untrusted until ownership is validated.
- **server/watchlist.ts:111** — getAlerts builds a query that LEFT JOINs watchlist_items (wi) but the JOIN condition does not require wi.tenant_id = ${tenantId}. As written, wa rows are tenant-scoped but the joined wi.name/wi.category may come from a different tenant's row with a colliding id, leaking other tenants' metadata.
  - _Fix:_ Constrain the joined table to the same tenant. For example, include wi.tenant_id = ${tenantId} in the JOIN condition or add AND wi.tenant_id = ${tenantId} to the WHERE clause. Prefer explicit ON clause: LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id AND wi.tenant_id = ${tenantId}.

### MEDIUM (30)

- **server/agentic-features.ts:903** — String-interpolated SQL used to build an ANY(...) array: `AND sequence_id = ANY(${`{${seqIds.join(",")}}`}::int[])`. This injects a constructed string into SQL (array literal), which is fragile and can lead to injection or tenant-isolation mistakes if seqIds are influenced by untrusted input.
  - _Fix:_ Use parameterized arrays instead of embedding a string array literal. For example, pass the ids as a parameterized array (driver/sql helper or sql.array(seqIds, 'int')) or build the query using parameter placeholders so values are bound, not string-concatenated. Validate seqIds are integers and belong to the tenant before using them.
- **server/auto-asset-capture.ts:184** — Backfill query selects conversations across all tenants: `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0`. This reads tenant-scoped tables without a tenant_id filter.
  - _Fix:_ Either run the backfill per-tenant or add an explicit tenant filter to the query (e.g. `WHERE c.tenant_id = ${tenantId}`) when operating in a tenant-scoped context. If the routine is intentionally global, ensure it is run under an explicitly-authorized platform-admin boundary and audit/log every cross-tenant operation.
- **server/auto-asset-capture.ts:207** — Query reads messages without constraining by tenant: `SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'`. Reading messages by conversation_id alone can cross tenant boundaries if conversation_id is attacker-controlled or earlier queries are not tenant-scoped.
  - _Fix:_ Include tenant scoping on message reads, e.g. `AND tenant_id = ${c.tenant_id}` (or otherwise ensure conversation ownership was validated before performing the SELECT). Also update the earlier sub-select/count used in the backfill WHERE clause to include tenant scoping.
- **server/channel-routing.ts:64** — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true returns a persona id with no tenant scoping or ownership check. Returning this id (or using it subsequently) can lead to cross-tenant access if consumers use it to read tenant-scoped persona records without verification.
  - _Fix:_ If channel_routes are intended to be tenant-scoped, add tenant_id to the table and filter by tenant. If channel_routes are global, only return persona info after verifying the persona belongs to the requesting tenant or only allow this operation for admin contexts. Ensure any subsequent use of the returned persona_id validates tenant ownership before accessing tenant-scoped persona rows.
- **server/data-protection.ts:404** — SELECT COUNT(*) FROM tenant-scoped table `messages` using only conversation_id (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) — missing tenant_id constraint, enabling a cross-tenant existence oracle.
  - _Fix:_ Add tenant_id scoping to the COUNT: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}. Even when conversation IDs were retrieved earlier with tenant filtering, every subsequent read must defensively include the tenant predicate.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed with WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but does not include tenant_id. This writes a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. add AND tenant_id = ${tenantId}) or otherwise prove-and-document (in-code) an equivalent invariant (for example, atomic prior validation that collection_id belongs to the tenant and that it cannot race/churn between checks). Prefer explicit tenant equality in the statement.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates a tenant-scoped table by id only (no tenant_id check). Although the id originated from a prior tenant-scoped SELECT, the UPDATE itself lacks an explicit tenant constraint.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) to enforce the tenant boundary at the DB statement level rather than relying solely on prior selection context.
- **server/dormancy-nudge.ts:202** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with no tenant filter. tool_performance elsewhere is queried with tenant_id, indicating it is tenant-scoped — this read is missing tenant scoping and may mix data from multiple tenants.
  - _Fix:_ If the intent is per-tenant computation, add tenant_id filtering (e.g. WHERE tenant_id = ${tenantId}) or accept and document that this is intentionally global. Prefer explicit tenant scoping in the SQL when operating in tenant contexts.
- **server/dormancy-nudge.ts:213** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... but does not filter by tenant_id. This reads a tenant-scoped table without a tenant constraint.
  - _Fix:_ Add tenant scoping (e.g. WHERE last_success_at > ... AND tenant_id = ${tenantId}) for per-tenant logic, or clearly document/centralize global intent if this query must be cross-tenant.
- **server/dormancy-nudge.ts:169** — getTrafficSnapshot issues COUNT(*) from messages WHERE created_at > ... AND role = 'user' with no tenant_id constraint (and likewise the distinct tool_name count at lines ~179-185). If messages and tool_performance are tenant-scoped, these queries aggregate across tenants unexpectedly.
  - _Fix:_ If traffic gating is intended to be per-tenant, add tenant_id filters to these queries (e.g. AND tenant_id = ${tenantId}) or make the global intent explicit and isolated from tenant-scoped code paths. Prefer explicit WHERE tenant_id = ... to avoid cross-tenant aggregation.
- **server/embeddings.ts:806** — storeEmbeddingVec has a branch that executes sql`UPDATE ${sql.raw(table)} ... WHERE id = ${id}` when no tenantId is provided — uses sql.raw(table) and updates rows by id only (no tenant constraint). This allows unscoped writes across tenants and uses sql.raw with interpolated values.
  - _Fix:_ Disallow the branch that omits tenantId: require a positive tenantId for tenant-scoped tables. Avoid using sql.raw(table) for user/input-driven values; instead use a typed/allowlisted path and parameterized queries. Ensure tenant_id is always included in the WHERE clause for tenant-scoped tables.
- **server/embeddings.ts:259** — Use of sql.raw() with an interpolated vector literal derived from a model embedding (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). Although vecLiteral validates numeric shape, using sql.raw() with request-derived content increases code-injection / isolation risk.
  - _Fix:_ Avoid sql.raw interpolation of externally-derived values. If the pgvector expression cannot be parameterized directly via the driver, encode/validate the vector in a safe binary/parameterized form (or use prepared statements / driver-supported vector parameters). Keep strict validation and explicitly document why raw is necessary; prefer passing vector content as a parameter bound by the DB driver and include tenant filters as usual.
- **server/lib/agent-trace.ts:130** — UPDATE on agent_trace_spans uses WHERE span_id = ${spanId} with NO tenant_id constraint. agent_trace_spans is tenant-scoped and the close UPDATE can therefore affect spans belonging to other tenants.
  - _Fix:_ Record or otherwise associate tenantId with the span when opening it (e.g. store tenantId in the in-memory span map) and include tenant_id in the UPDATE WHERE clause (WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). This ensures closes are scoped to the originating tenant.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(lockKey) inside a parameterized query: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). lockKey is derived from caller-supplied ledgerKey/tenantId, and sql.raw bypasses parameter binding — risk of injection or unexpected SQL if ledgerKey isn't strictly controlled.
  - _Fix:_ Avoid sql.raw with values derived from request/caller input. Compute a numeric BigInt server-side and pass it as a bound parameter (e.g. sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)})`) or validate/sanitize the value strictly before using sql.raw. If the DB driver requires literal, ensure ledgerKey is validated and constructed entirely from server-controlled deterministic hashing, or use a safe API to pass it as a parameterized value.
- **server/plan-executor.ts:802** — Pending-plan SELECT pulls approved plans for all tenants: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`. This read from a tenant-scoped table is not constrained to a tenant and will kick off plans across tenants without tenant-scoped filtering or explicit admin scoping checks.
  - _Fix:_ Either add a tenant_id predicate to the query (and call this per-tenant), or ensure the caller is an explicit, authenticated admin and document/centralize the cross-tenant intent. Prefer per-tenant selection when invoked by non-global processes to avoid cross-tenant side-effects.
- **server/routes.ts:179** — Startup restore selects from tenant-scoped table `file_storage` without constraining by tenant_id (no tenant filter in WHERE). This will read rows for all tenants.
  - _Fix:_ Limit the restore to a single tenant at a time or add an explicit tenant filter (e.g. WHERE tenant_id = <trusted tenant id> / eq(fileStorage.tenantId, tenantId)), or iterate per-tenant with an explicit trusted tenant id so you never do an unscoped scan of tenant-scoped data.
- **server/routes.ts:618** — Backfill query SELECT ... FROM projects JOIN tenants WHERE p.drive_folder_id IS NULL reads the tenant-scoped `projects` table across all tenants (no tenant_id constraint).
  - _Fix:_ If this operation must run across tenants, run it from an admin-only background job that is auditable and clearly authorized. Otherwise, add a tenant_id constraint or iterate per-tenant (e.g. select tenants then process projects for each tenant using eq(projects.tenantId, tenantId)). Avoid unscoped reads on tenant-scoped tables.
- **server/routes/admin-zombie-detector.ts:38** — Request-derived value `windowDays` is interpolated directly into multiple sql`...` template fragments (e.g. `NOW() - (${windowDays}::int * INTERVAL '1 day')`). While Drizzle will parameterize values in a tagged template, embedding calculated expressions with request-derived variables increases injection and logic risk if the value isn't strictly validated.
  - _Fix:_ Validate and coerce `windowDays` to a safe integer (already partially done); prefer binding it as a single parameter or use explicit numeric casting in a safe way. For example, ensure `windowDays` is a Number integer and then use `... WHERE updated_at > NOW() - (${windowDays}::int * INTERVAL '1 day')` only after strict validation, or compute the interval server-side and pass it as a parameter. Avoid constructing raw SQL expressions from untrusted inputs.
- **server/routes/goal-ledger.ts:92** — readActiveJobs() executes a SELECT on agent_jobs (tenant-scoped) without constraining rows to a tenant (no WHERE tenant_id = ...). This returns active jobs across all tenants.
  - _Fix:_ If the intent is to show only the caller's tenant jobs, add a tenant predicate (e.g. WHERE j.tenant_id = ${tenantId}). If the intent is to present cross-tenant data for platform operators, make that explicit (require/verify platform-admin gating at call site) and document the cross-tenant intent. Prefer an explicit ALL_TENANTS constant or an admin-only data-access path so the multi-tenant read is intentional and auditable.
- **server/routes/metrics.ts:79** — Tool-usage aggregation query reads `agent_cost_ledger` without a tenant predicate (no WHERE tenant_id = ...), returning global/tool-level counts across tenants.
  - _Fix:_ If per-tenant tool counts are required, add WHERE tenant_id = ${tenantId}. If global top-N is intentional, restrict access to platform operators and ensure the exposure is acceptable; otherwise make the query tenant-scoped.
- **server/routes/metrics.ts:95** — Token totals query reads `agent_cost_ledger` without any tenant constraint (no WHERE tenant_id = ...), aggregating tokens across all tenants.
  - _Fix:_ Scope this aggregation to the requesting tenant by adding WHERE tenant_id = ${tenantId}, or if the intent is global metrics, ensure the endpoint is hardened to platform-only access and that returning global totals is an explicit, reviewed decision.
- **server/routes/oauth-subscriptions.ts:325** — SELECT DISTINCT tenant_id FROM `oauth_subscriptions` is executed with no tenant constraint, enumerating tenant IDs from a tenant-scoped table in a handler that can be triggered (cron heal path).
  - _Fix:_ Limit this query to only the operational scope you need (e.g. restrict to specific tenants or require stronger admin auth). If the purpose is to enumerate tenants for platform-level maintenance, ensure the endpoint is accessible only with platform-level credentials and consider logging/monitoring. Alternatively, move this logic to an administrative-only job/process outside user-triggerable endpoints.
- **server/seed.ts:3947** — UPDATE agent_knowledge SET content = ... WHERE id = ${rows[0].id} does not include tenant_id in the WHERE clause. Although the id was discovered from a prior SELECT that filtered tenant_id = 1, the subsequent UPDATE is not independently scoped to tenant_id and could affect another row if ids are re-used or if race conditions occur.
  - _Fix:_ Include tenant_id = <tenant> in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) to ensure the write is scoped to the correct tenant. Always verify ownership via tenant_id on writes even when id was previously selected.
- **server/skillify.ts:221** — SELECT from personas does not include a tenant filter. If personas is a tenant-scoped table, this can leak persona rows across tenants.
  - _Fix:_ Constrain the personas lookup by tenant (e.g. include AND personas.tenant_id = ${tenantId}) or otherwise ensure personas are global and documented as non-tenant-scoped. Prefer explicit tenant-scoping on tenant-scoped tables.
- **server/tenant-fork.ts:226** — During two-pass copy for self-referencing tables, the code updates the cloned row via WHERE id = p.newId without also constraining to tenant_id. Writes against tenant-scoped tables must include the tenant filter.
  - _Fix:_ Add an explicit tenant check to the UPDATE, e.g. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId))). Even though the id was just inserted, include tenant_id in the WHERE to follow the project's tenant-isolation invariant.
- **server/video-job-runner.ts:273** — listStaleVideoJobs queries the tenant-scoped videoJobs table without constraining by tenant_id. Background recovery will scan rows across all tenants.
  - _Fix:_ If the intention is single-tenant-scoped operation, add eq(videoJobs.tenantId, <tenantId>) to the WHERE clause. If cross-tenant system-level processing is required, make this explicit and ensure the code path is strictly internal and that any external-facing operations never operate on rows without checking the caller's tenant. At minimum, document and restrict the operation to trusted internal processes and audit access.
- **server/wiring-invariants.ts:366** — SELECT COUNT(...) executed with a raw/interpolated table name (`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`) queries subsystem tables (e.g. agent_knowledge, messages, memory_entries) without any tenant_id constraint — reads tenant-scoped data across all tenants and uses sql.raw() with interpolated identifiers.
  - _Fix:_ If this is intended as an admin/global health check, make that explicit (guard the check to run only in an admin context) and avoid building SQL with unescaped/interpolated identifiers. Prefer parameterized queries or a safe identifier-quoting helper (do not inject untrusted values into sql.raw). If the intent is per-tenant probing, add an explicit WHERE tenant_id = ${tenantId} (or aggregate BY tenant_id) so queries never return cross-tenant data.
- **server/wiring-invariants.ts:535** — Raw SQL uses an interpolated table name to compute counts/sizes (`SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`) against tables such as messages, graph_memory, etc., with no tenant_id constraint — this reads tenant-scoped tables across all tenants and uses string interpolation for identifiers.
  - _Fix:_ Either restrict the probe to a specific tenant (WHERE tenant_id = ${tenantId}) or run it explicitly only in a protected admin context. Replace sql.raw string interpolation with safe identifier handling and parameterized queries (or use database metadata APIs) to avoid accidental injection and to make it clear when an aggregate, cross-tenant metric is intended.
- **server/wiring-invariants.ts:244** — Query against tool_performance (`SELECT count(*)::int AS n FROM tool_performance WHERE last_success_at > NOW() - INTERVAL '14 days'`) lacks any tenant_id constraint and thus reads tenant-scoped performance data across tenants without an explicit admin guard.
  - _Fix:_ If tool_performance is tenant-scoped, add a tenant filter (WHERE tenant_id = ${tenantId}) or group results per-tenant. If this is intentionally global, ensure the function is only runnable in an admin-only context and document that it aggregates across tenants. Consider parameterizing tenant scope and avoiding implicit cross-tenant reads.
- **server/wiring-invariants.ts:417** — Several queries operate on tool_performance without tenant scoping (e.g. SELECT DISTINCT tool_name ... FROM tool_performance WHERE last_success_at > ... and the drift detector SELECT ... FROM tool_performance WHERE last_failure_at > ...) — these read tenant-scoped data across all tenants.
  - _Fix:_ Scope these queries by tenant_id when analyzing tenant-specific tool health (e.g. WHERE tenant_id = ${tenantId}), or, if an aggregated global view is required, run them under an explicit admin-only path and avoid implicitly exposing cross-tenant aggregates. Also prefer parameterized queries and avoid untrusted interpolation.

### LOW (2)

- **server/routes.ts:1916** — Public GET selects presenter_sessions by token with no tenant_id constraint and returns tenant_id in the result. While token acts as a capability, this performs an unscoped read on a tenant-scoped table and leaks tenant_id.
  - _Fix:_ Avoid returning tenant_id in public capability responses unless necessary. Consider validating the token maps to an expected tenant and only returning public-safe fields, or bind the capability check to a tenant-scoped query (e.g. WHERE token = ${token} AND tenant_id = ${expectedTid}) when possible to make the ownership explicit.
- **server/routes.ts:477** — Prune job executes DELETE FROM order_lookup_codes WHERE expires_at < NOW() with no tenant constraint. `order_lookup_codes` is tenant-scoped (has tenant_id) so this performs a cross-tenant write.
  - _Fix:_ If the prune should affect all tenants run it from an authorized system context and ensure this behavior is intentional and audited. Otherwise, constrain the DELETE by tenant (e.g. per-tenant prune) or include an explicit admin-only guard before running an unscoped DELETE on a tenant-scoped table.
