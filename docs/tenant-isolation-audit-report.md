# Tenant-Isolation Audit Report

_Generated 2026-08-11T01:17:08.992Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **590/590** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **280** (CRITICAL 68 · HIGH 179 · MEDIUM 30 · LOW 3)
- Precision second-pass (`gpt-5.4`): **226** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (68)

- **server/agentic-engines.ts:243** — UPDATE ai_insights (autoApplyEligibleInsights) runs without a tenant_id constraint — this will change rows across tenants (status → 'applied').
  - _Fix:_ Scope the UPDATE to a tenant (e.g. add WHERE tenant_id = ${tenantId}) or perform a per-tenant sweep. Never perform tenant-scoped writes without an explicit tenant filter.
- **server/agentic/approvals.ts:135** — expireStaleApprovals runs a bulk UPDATE on agentApprovals without constraining by tenant_id (WHERE status = 'pending' AND expiresAt < ...). This updates approvals across all tenants and then attempts per-run updates, risking cross-tenant state changes.
  - _Fix:_ Either run expiry per-tenant (adding eq(agentApprovals.tenantId, tenantId) to the WHERE) or enumerate tenants and perform tenant-scoped updates. Never perform tenant-scoped UPDATEs across all tenants without explicit, audited intention.
- **server/agentic/runs.ts:39** — update(agentRuns).set(...).where(eq(agentRuns.id, runId)) — appendStep updates agent_runs without any tenant_id constraint. This allows modifying run rows across tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). If the caller cannot provide tenantId, first SELECT the run by id and verify its tenant_id equals the caller's tenant before performing the update.
- **server/agentic/runs.ts:51** — update(agentRuns).set(...).where(eq(agentRuns.id, runId)) — updateRunState updates run state without tenant filtering, enabling cross-tenant writes.
  - _Fix:_ Require/accept the caller's tenantId and add it to the WHERE clause (where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Alternatively, fetch the run and verify ownership before updating.
- **server/agentic/runs.ts:55** — update(agentRuns).set({ state }) .where(eq(agentRuns.id, runId)) — replaceRunState updates run rows without tenant constraint.
  - _Fix:_ Add tenant ownership check in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or resolve run ownership first and enforce it server-side before performing the update.
- **server/agentic/runs.ts:64** — update(agentRuns).set({ status:'completed', ... }).where(eq(agentRuns.id, runId)) — completeRun writes to agent_runs without tenant scoping.
  - _Fix:_ Constrain the update to the caller's tenant (where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). If the API currently lacks tenant context, require it or validate the run's tenant before updating.
- **server/agentic/runs.ts:94** — update(agentRuns).set({ status:'failed', ... }).where(eq(agentRuns.id, runId)) — failRun updates agent_runs without tenant filtering.
  - _Fix:_ Include tenant_id equality in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or pre-validate the run's tenant ownership before performing the update.
- **server/agentic/runs.ts:102** — update(agentRuns).set({ status:'paused', ... }).where(eq(agentRuns.id, runId)) — pauseRun updates run rows with no tenant constraint.
  - _Fix:_ Scope updates by tenant (where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). If the function cannot accept tenantId, look up the run and assert its tenant matches the caller before writing.
- **server/agentic/wake-scheduler.ts:142** — UPDATE of tenant-scoped table agent_wake_schedules performs a claimed-rows UPDATE via a subquery that does not constrain by tenant_id. The statement (UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...)) can move rows for any tenant into 'firing', causing cross-tenant state changes and event emission.
  - _Fix:_ Restrict the claim/update to a single tenant at a time (or include an explicit tenant_id filter). e.g. iterate tenants with pending wakes or add AND tenant_id = ${tenantId} in both the inner SELECT and outer UPDATE. Ensure every UPDATE/SELECT on agent_wake_schedules includes WHERE tenant_id = <caller tenant> (or run per-tenant with the tenant bound in the SQL) so no background sweep can modify other tenants' rows.
- **server/auto-memorize.ts:75** — INSERT INTO agent_knowledge hard-codes tenant_id = 1 when creating the watermark. This writes to a specific tenant rather than the acting tenant (or failing to set tenant correctly).
  - _Fix:_ Do not hard-code tenant IDs. Make setWatermark accept a tenantId argument and INSERT tenant_id = ${tenantId}. If the watermark is intended to be global/system-level, use a dedicated global table/column that is not tenant-scoped and document the choice. Otherwise, ensure the tenant_id value comes from the caller and is validated.
- **server/channel-routing.ts:16** — channel_routes table is created with no tenant_id column. Without a tenant_id on the table you cannot enforce per-tenant isolation at the application SQL layer as required by the invariant.
  - _Fix:_ Add a tenant_id INTEGER NOT NULL column to the channel_routes table (and a UNIQUE constraint scoped to tenant, e.g. UNIQUE(tenant_id, channel)). Ensure all code that inserts/queries channel_routes takes an explicit tenantId and filters by it. Example schema change: tenant_id INTEGER NOT NULL, UNIQUE (tenant_id, channel). Also consider adding a foreign key / validation step to ensure persona_id belongs to the same tenant.
- **server/channel-routing.ts:48** — setChannelRoute performs DELETE/INSERT/UPDATE on channel_routes without any tenant_id column or tenant filter, and INSERT does not supply tenant_id. This permits creating/updating global routing that can reference personas from any tenant (write-level violation).
  - _Fix:_ Require and accept tenantId in setChannelRoute signature. Ensure INSERT sets tenant_id explicitly (VALUES (${tenantId}, ${channel}, ${personaId})) and that DELETE/UPDATE include WHERE tenant_id = ${tenantId} (e.g. DELETE FROM channel_routes WHERE tenant_id = ${tenantId} AND channel = ${channel}). Before inserting persona_id validate the persona belongs to tenant (SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). Also update schema to include tenant_id as NOT NULL.
- **server/chat-engine.ts:2117** — Unscoped read of a tenant-scoped resource: storage.getConversationUnscoped(conversationId) is called without constraining tenant_id (an unscoped SELECT on the conversations table). The code only derives/validates tenant after this call (assertTenantContext), so an attacker-controlled conversationId could cause cross-tenant data exposure (IDOR) before tenant context is enforced.
  - _Fix:_ Avoid any unscoped SELECTs against tenant-scoped tables. Resolve tenant context before any DB read or use a tenant-scoped accessor. Replace getConversationUnscoped with a tenant-aware API (e.g. storage.getConversation(conversationId, tenantId)) or require and validate tenantId as an explicit parameter before fetching. If an unscoped lookup is absolutely necessary, perform it against a safe minimal indexing store (no sensitive fields) and enforce ownership/tenant checks prior to returning any tenant-scoped data.
- **server/chat-engine.ts:4407** — storage.attachCitations(assistantMsgId, collectedCitations) is called with no tenant parameter. If attachCitations inserts citation rows into a tenant-scoped table it may create rows missing tenant_id or mis-associate citations across tenants.
  - _Fix:_ Modify the call to pass the caller's tenantId (e.g. storage.attachCitations(messageId, citations, tenantId)) and update attachCitations to set tenant_id on INSERTs and/or include tenant_id in WHERE clauses on any UPDATE/DELETE. Audit attachCitations implementation to ensure all DB operations enforce tenant scoping (e.g. .where(eq(table.tenantId, tenantId))).
- **server/commitments.ts:136** — SELECT from tenant-scoped table `commitments` returns tenant-scoped rows (including `description`) without any `WHERE tenant_id = ...` filter — reads overdue commitments across all tenants.
  - _Fix:_ If this fan-in across tenants is intended for an admin digest, move it to an explicit admin-only codepath and ensure strict access controls and auditing. Otherwise, constrain the SELECT to the requesting tenant (e.g. add `WHERE c.tenant_id = ${tenantId}`) or use a dedicated, audited admin aggregate that contains only non-sensitive, redacted metadata. Never return tenant-scoped content across tenants in normal user contexts.
- **server/embeddings.ts:684** — UPDATE to tenant-scoped tables (memory_entries / agent_knowledge) without constraining by tenant_id. storeEmbeddingVec updates rows by id only (UPDATE ... WHERE id = ${id}) and also uses sql.raw(table) for table selection; this can modify rows belonging to any tenant.
  - _Fix:_ Require and accept a tenantId parameter in storeEmbeddingVec and include AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(...))) in the UPDATE WHERE clause. Avoid constructing table names from untrusted input; if a table name must be variable, enforce a strict server-side allowlist (which you already have) and still require tenant_id in the WHERE clause.
- **server/embeddings.ts:688** — SELECT from agent_knowledge in backfillMissingKnowledgeEmbeddings has NO tenant_id constraint. This reads rows across all tenants and then writes back via storeEmbeddingVec (which itself updates without tenant checks), enabling cross-tenant data access and modification.
  - _Fix:_ Run backfills per-tenant or accept a tenantId parameter and include WHERE tenant_id = ${tenantId} on the SELECT. Ensure any subsequent UPDATE/INSERT also includes tenant_id to avoid touching other tenants' data.
- **server/embeddings.ts:716** — SELECT from memory_entries in backfillEmbeddingVecs lacks a tenant_id constraint (SELECT id, embedding FROM memory_entries ...). The subsequent UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} also omits tenant_id, enabling cross-tenant writes.
  - _Fix:_ Perform this backfill per-tenant (pass a tenantId argument) or add a tenant_id = ${tenantId} filter to the SELECT and include tenant_id in the UPDATE WHERE clause. Never update tenant-scoped rows by id alone without ensuring tenant ownership.
- **server/embeddings.ts:732** — SELECT from agent_knowledge in backfillEmbeddingVecs has NO tenant_id constraint and the later UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} also omits tenant filtering, allowing cross-tenant updates.
  - _Fix:_ Same fix as for memory_entries: run the backfill per-tenant or accept tenantId and add WHERE tenant_id = ${tenantId} to SELECTs and UPDATEs. Ensure all writes to tenant-scoped tables include tenant_id in the WHERE clause.
- **server/heartbeat.ts:102** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} is executed without a tenant_id constraint. This can update a schedule belonging to another tenant if an id is reused or maliciously supplied.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage-layer helper that asserts tenant context before performing the update.
- **server/heartbeat.ts:108** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id} has no tenant_id constraint. This updates schedule rows without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage wrapper that enforces tenant_id.
- **server/heartbeat.ts:113** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} is performed without tenant constraint, allowing cross-tenant modification.
  - _Fix:_ Constrain updates by tenant_id (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use storage helpers which enforce tenant context.
- **server/heartbeat.ts:121** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} lacks a tenant_id filter and may modify another tenant's schedule row.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or use a tenant-aware storage method that validates ownership before updating.
- **server/heartbeat.ts:273** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} is executed without constraining by tenant_id. This directly modifies a tenant-scoped table row without verifying tenant ownership.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call storage.updateHeartbeatTask / another tenant-aware helper that asserts the tenant context before performing updates.
- **server/heartbeat.ts:277** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} runs with no tenant_id filter and may modify tasks belonging to other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage-layer update function that enforces tenant ownership.
- **server/heartbeat.ts:2764** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause. This can change approval_status on a task that belongs to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use storage.updateHeartbeatTask which should enforce tenant scoping/ownership.
- **server/heartbeat.ts:3216** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) lacks a tenant_id constraint, allowing approval_status changes across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform this via a tenant-aware storage API that validates ownership before updating.
- **server/job-queue.ts:98** — UPDATE agent_jobs ... (claimDueJobs) performs a global UPDATE/SELECT on the tenant-scoped agent_jobs table without any tenant_id constraint. This can move jobs across tenants into 'running' state and allow cross-tenant job claiming/modification.
  - _Fix:_ Scope claims to a tenant (e.g. accept a tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause) or otherwise enforce at the application boundary that the worker only processes authorized tenants. If the queue must be global, add explicit operator-privilege checks and audit/logging and document why cross-tenant claims are required.
- **server/job-queue.ts:134** — UPDATE agent_jobs ... (reclaimExpiredLeases step 1) updates running jobs to failed_terminal without a tenant_id constraint. This mutates terminal state across all tenants.
  - _Fix:_ Include tenant scoping when appropriate (e.g. accept tenantId or run per-tenant reclaim) or otherwise restrict this operation to an admin/operator context. Add AND tenant_id = ${tenantId} or a documented admin-only guard.
- **server/job-queue.ts:148** — UPDATE agent_jobs ... (reclaimExpiredLeases step 2) moves expired 'running' jobs back to pending, but does so globally with no tenant_id filter, affecting all tenants.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) or run reclaim per-tenant; if global reclaim is intended, enforce that only an admin context may run it and record that in code comments/logging.
- **server/job-queue.ts:177** — UPDATE agent_jobs ... (completeJob) marks a job succeeded by id but does not constrain the update to the job's tenant_id. A stale/forged id could allow completing another tenant's job.
  - _Fix:_ Require/validate tenant context (e.g. pass tenantId into completeJob and add AND tenant_id = ${tenantId} to the WHERE), or re-read and verify tenant_id before making the terminal update to enforce per-tenant fencing.
- **server/job-queue.ts:220** — SELECT max_attempts FROM agent_jobs WHERE id = ${id} (failJob) reads job metadata without scoping by tenant_id. An attacker or bug could infer/affect other tenants' jobs.
  - _Fix:_ Query by id AND tenant_id = ${tenantId} (require tenant context) or otherwise validate ownership before using the returned value. Avoid relying on id-only lookups for tenant-scoped rows.
- **server/job-queue.ts:237** — UPDATE agent_jobs ... (failJob terminal path) updates status and other columns for the job id without constraining by tenant_id. This can dead-letter jobs across tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId}) or require tenant validation prior to executing the update. Preserve lease-fencing semantics while enforcing tenant ownership.
- **server/job-queue.ts:257** — UPDATE agent_jobs ... (failJob non-terminal retry path) re-schedules a job by id but does not include tenant_id in the WHERE clause. This permits cross-tenant rescheduling/manipulation.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE's WHERE clause or otherwise ensure the caller proves ownership of the job before modifying retry/backoff fields.
- **server/job-queue.ts:274** — UPDATE agent_jobs ... (cancelJob) transitions a job to 'cancelled' by id without scoping to tenant_id. An operator or API call could cancel jobs belonging to other tenants by id.
  - _Fix:_ Require tenant validation or include tenant_id = ${tenantId} in the WHERE clause. If this is an admin-only operator action, gate the endpoint and document it explicitly.
- **server/job-queue.ts:283** — UPDATE agent_jobs ... (retryJob) resets a job's attempts/status by id without any tenant_id constraint, allowing cross-tenant manipulation of job state.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId}) or validate job ownership before performing the update. Ensure operator APIs that call retryJob are restricted appropriately.
- **server/lib/inbox-ingest.ts:506** — UPDATE to inbox_classifications omits tenant scoping in WHERE clause (UPDATE inbox_classifications ... WHERE inbox_message_id = ${storeResult.id}). This can allow updating another tenant's classification row if row ids are confused.
  - _Fix:_ Include the tenant filter on the UPDATE (e.g. AND tenant_id = ${tenantId}) so the statement becomes: UPDATE inbox_classifications SET routed_to = ${JSON.stringify(routedTo)}::jsonb WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}. Always constrain writes to tenant_id.
- **server/lib/outcome-promotion.ts:159** — scanOutcomePromotions() runs a SELECT against tenant-scoped table deliverable_grade_events without constraining by tenant_id (missing WHERE tenant_id = ...). This reads aggregated per-tenant rows across ALL tenants, violating the app-only tenant-isolation invariant.
  - _Fix:_ Limit the query to a specific tenant (e.g. accept a tenantId parameter and add WHERE tenant_id = ${tenantId}), or explicitly iterate tenants and run the query per-tenant. If the nightly job legitimately needs cross-tenant aggregation, perform that in a clearly isolated, admin-only path (not in tenant-facing code) and document/secure it. Never leave a general SELECT on a tenant-scoped table without an explicit tenant_id constraint.
- **server/lib/scheduled-post-runner.ts:61** — SELECT in CTE pulls rows from tenant-scoped table `scheduled_posts` without filtering by tenant_id (WITH due AS (SELECT id FROM scheduled_posts ... FOR UPDATE)). This locks and updates rows across all tenants in the subsequent UPDATE, enabling cross-tenant row modification/locking.
  - _Fix:_ Restrict the initial SELECT to a single tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. If the runner must operate cross-tenant, make the cross-tenant intent explicit and protect the operation (e.g. admin-only path, audit trail) and ensure every UPDATE/DELETE that follows also includes tenant_id checks.
- **server/minerva-planner.ts:472** — Rollback UPDATE on plans (resetting decision metadata) omits tenant_id in the WHERE clause. This can modify plans across tenants and corrupt other tenants' workflow/audit state.
  - _Fix:_ Include AND tenant_id = ${args.tenantId} in the UPDATE's WHERE clause (or otherwise verify ownership) so rollback only affects the intended tenant. If tenantId may be omitted for trusted callers, require a dedicated internal code path and avoid un-scoped UPDATEs.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table `oauth_subscriptions` without any tenant_id filter (global read of active subscriptions). This reads potentially sensitive tokens across tenants.
  - _Fix:_ Avoid unscoped reads. Either iterate tenants explicitly and filter by tenant_id, or require an explicit admin/operational intent and audit/authorize it. Add WHERE tenant_id = ${tenantId} (or otherwise limit to an approved tenant list) before selecting sensitive columns.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh selects all rows FROM `oauth_subscriptions` WHERE is_active = TRUE with no tenant_id filter — iterates across tenants and will access encrypted tokens for all tenants.
  - _Fix:_ Make proactive refresh tenant-scoped (accept a tenantId or iterate a vetted tenant list), or ensure this is executed under a privileged/operational service account with explicit justification. In general prefer queries filtered by tenant_id to enforce isolation.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant filtering — enumerates tenants and allows cross-tenant operations.
  - _Fix:_ If cross-tenant enumeration is required, run it only in a controlled, audited admin process; otherwise change the interface to operate per-tenant and add WHERE tenant_id = ${tenantId} to all queries touching tenant-scoped tables.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(${lockKey}) to inject an advisory-lock value derived from ledgerKey (request-supplied) into SQL. sql.raw with request-derived input risks SQL-injection/style issues and bypasses parameterization.
  - _Fix:_ Avoid using sql.raw with untrusted input. Compute a numeric lock value server-side and pass it as a parameter (e.g. use pg_advisory_xact_lock($1::bigint) with the lock key as a bound parameter). Also validate / canonicalize ledgerKey before deriving the lock key, or use a mapping table that stores precomputed lock keys per ledgerKey.
- **server/plan-executor.ts:116** — UPDATE plans ... WHERE id = ${planId} (appendExecutionLog) updates a tenant-scoped table without constraining by tenant_id — allows modifying another tenant's plan if an attacker supplies an id.
  - _Fix:_ Include the plan's tenant filter in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or load the plan row first and use its tenant_id to scope all subsequent updates. Never update plans by id alone from untrusted input.
- **server/plan-executor.ts:513** — CAS UPDATE on plans (claim step) uses WHERE id = ${planId} AND status = 'approved' but omits tenant_id — can claim/transition plans belonging to other tenants.
  - _Fix:_ Scope the CAS by tenant: add AND tenant_id = ${tenantId} (pass tenantId into executePlan or fetch and validate the plan's tenant before performing state transitions). Ensure every UPDATE/RETURNING on plans checks tenant_id.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} (no tenant filter) — mutates plan state without tenant scoping.
  - _Fix:_ Add tenant_id check in the WHERE clause (AND tenant_id = ${tenantId}) or derive tenantId from the previously fetched row and use it for the update.
- **server/plan-executor.ts:728** — Final UPDATE to set plan status (completion) uses WHERE id = ${planId} without tenant_id — writes to another tenant's plan record possible.
  - _Fix:_ Include AND tenant_id = ${tenantId} (use the fetched row.tenant_id) when updating the plan state and execution_log atomically.
- **server/plan-executor.ts:762** — Error-handling UPDATE that flips executing→failed uses WHERE id = ${planId} AND status = 'executing' but omits tenant_id — may mark other tenants' plans as failed.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or derive and validate tenantId before performing this update). Always include tenant scoping on mutating queries.
- **server/plan-executor.ts:778** — resumeStuckPlans: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — this resets plans across ALL tenants (no tenant filter).
  - _Fix:_ This operation must be explicitly scoped. Either run per-tenant (accept a tenantId param and filter WHERE tenant_id = ${tenantId}) or otherwise ensure only an admin-authorized context runs it and records which tenant rows are affected. Prefer per-tenant scoping where possible to avoid cross-tenant recovery side-effects.
- **server/plan-executor.ts:787** — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' LIMIT 50 — selects approved plans across all tenants (no tenant filter) and requeues them, allowing cross-tenant execution.
  - _Fix:_ Scope the SELECT by tenant_id (e.g. WHERE status = 'approved' AND tenant_id = ${tenantId}). If this routine must enumerate across tenants as admin, make that explicit and ensure only an admin context can call it; otherwise operate per-tenant.
- **server/process-governor.ts:583** — Action 'disable_task' issues UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without tenant_id — will disable tasks across tenants if ids collide or if an attacker supplies an id.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${t.id} AND tenant_id = ${tenantId}) or only update by task id that was selected from the tenant-scoped query and validated. Never update heartbeat_tasks by id alone from untrusted input.
- **server/process-governor.ts:640** — Action 'restart_agent' performs UPDATE agent_desks SET ... WHERE id = ${desk.id} without tenant scoping — could reset desks from other tenants.
  - _Fix:_ Include tenant_id in the WHERE (WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or perform updates using rows already selected with tenant filtering and validated tenant_id.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped proposal without tenant scoping — an attacker could query/verify another tenant's proposal by id.
  - _Fix:_ Require tenantId in verifyProposalById or fetch the proposal and validate its tenant_id against the caller's tenant before proceeding; add AND tenant_id = ${tenantId} to the SELECT.
- **server/proposal-verifier.ts:241** — persist(): UPDATE code_proposals SET verification_status/verification_details WHERE id = ${proposalId} — updates a tenant-scoped table without tenant_id in WHERE, allowing cross-tenant mutation.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE or derive and validate tenantId from the proposal row before performing the update. All verification-sidecars must be tenant-scoped.
- **server/proposal-verifier.ts:372** — attemptRebaseOnDrift: UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} (no tenant filter) — mutates proposal state across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or validate the proposal's tenant before updating). Persisted changes to proposals must always be scoped by tenant.
- **server/proposal-verifier.ts:408** — attemptRebaseOnDrift: UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} — writes proposal changes without tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or restrict the operation to the fetched proposal's tenant_id. Never overwrite proposals by id alone.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} but omits tenant_id. This allows modifying rows across tenants by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or, better, use a composite WHERE (id AND tenant_id) for all UPDATE/DELETE statements. If the UPDATE is executed from a global scan, explicitly verify the row's tenant_id before writing and/or run the update within an authenticated per-tenant context.
- **server/reference-learner.ts:291** — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} is executed without constraining tenant_id. memory_entries is tenant-scoped, so this can update another tenant's row by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}) or re-fetch and verify the target row's tenant_id before updating. Always include tenant_id in WHERE clauses for UPDATE/DELETE on tenant-scoped tables.
- **server/research-engine.ts:165** — UPDATE research_sessions in cleanupZombieSessions is executed without any tenant_id constraint, so it will mark running sessions as completed across all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant or iterate per-tenant. Example fixes: 1) accept a tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause; or 2) query distinct tenant_ids and run the UPDATE per-tenant so the statement always includes WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(...))). Do not run an unscoped UPDATE on a tenant-scoped table.
- **server/routes.ts:167** — Startup restoreReads file_storage rows across ALL tenants without any tenant_id constraint (db.select from fileStorage WHERE mimeType LIKE 'image/%' AND storageKey IS NULL). This will restore other tenants' files to the shared uploads dir and can leak or make available tenant-private content.
  - _Fix:_ Restrict the restore to a specific tenant context or iterate per-tenant. Add an explicit tenant filter (e.g. .where(eq(fileStorage.tenantId, <tenantId>))) or run the restore as an admin-only, audited job that processes one tenant at a time and writes files into tenant-isolated paths. Avoid restoring cross-tenant rows into a global uploads directory.
- **server/routes.ts:6520** — storage.createConversation(rest) is called in the admin /api/import handler without explicitly setting or validating tenant_id. Conversations are tenant-scoped and INSERTs must include tenant_id; creating them from untrusted import payload can produce rows with missing/incorrect tenant_id or assign data to the wrong tenant.
  - _Fix:_ When importing conversations, explicitly set tenantId for the new conversation (e.g. storage.createConversation({ ...rest, tenantId: <targetTenant> })) or require a validated tenant mapping. Validate/normalize any tenant identifiers in the import file server-side and never rely on unvalidated imported fields for tenant ownership.
- **server/routes.ts:6526** — storage.createMessage({ ...msgRest, conversationId: newConv.id }) is called in the admin /api/import handler without passing tenant_id. Messages are tenant-scoped and must be inserted with the conversation's tenant_id; inserting without tenant information violates tenant isolation and may create messages with no tenant or wrong tenant.
  - _Fix:_ Include tenantId when creating messages (e.g. storage.createMessage({ ...msgRest, conversationId: newConv.id, tenantId: newConv.tenantId || <targetTenant> })). Derive tenant from the newly-created conversation server-side and do not trust tenant fields from the import payload.
- **server/seed.ts:3826** — DELETE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint: `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE ...` will remove rows for all tenants.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. AND tenant_id = <tenantId>) or scope the cleanup to system-owned rows only. Never perform deletions on tenant-scoped tables without verifying tenant_id.
- **server/storage.ts:1310** — getAllDataForExport performs wide SELECTs over many tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, provider_keys, heartbeat_tasks, etc.) with NO tenant scoping. This can export all tenants' data in one operation.
  - _Fix:_ This routine must require strong admin authorization and an explicit ALL_TENANTS sentinel. At minimum, add an explicit tenantId parameter and apply eq(...tenant_id, tenantId) to every tenant-scoped table. If an admin export is intended, ensure the caller is explicitly and strongly authenticated/authorized, and log/audit the action.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table 'knowledge_triples' lacks a tenant_id constraint in the WHERE clause (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}) — this can allow cross-tenant mutation if row.id is ever derived from an untrusted source.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer using Drizzle's .where(eq(table.tenant_id, tenantId)) or the storage layer APIs that enforce tenant scoping. Audit all UPDATE/DELETE statements against tenant-scoped tables to ensure tenant_id is always included.
- **server/venture-discovery/budget.ts:155** — UPDATE on tenant-scoped table `agent_cost_ledger` updates a row by id only (`WHERE id = ${reservationId}`) with no tenant_id constraint. This allows a caller to update (or settle) a reservation row belonging to another tenant if they know/guess the id.
  - _Fix:_ Always include tenant ownership in UPDATEs on tenant-scoped tables. Change the UPDATE to include `AND tenant_id = ${tenantId}` (or use an equivalent Drizzle .where(eq(table.tenantId, tenantId), eq(table.id, reservationId))). Optionally perform a prior SELECT to assert the row belongs to the tenant inside the same transaction, or use a conditional RETURNING that only returns rows matching both id and tenant_id and treat missing RETURNING as an ownership failure.
- **server/venture-discovery/budget.ts:169** — UPDATE on tenant-scoped table `agent_cost_ledger` for releasing a reservation updates by id only (`WHERE id = ${reservationId}`) without constraining by tenant_id, enabling cross-tenant mutation if an id from another tenant is supplied.
  - _Fix:_ Include the tenant_id in the WHERE clause for the release UPDATE (e.g. `WHERE id = ${reservationId} AND tenant_id = ${tenantId}`) or perform the update in a transaction that first asserts the row belongs to the tenant. Ensure all mutations against tenant-scoped ledger tables are guarded by ownership checks so one tenant cannot modify another tenant's ledger rows.

### HIGH (179)

- **server/ab-optimizer.ts:110** — SELECT from experiments (runDueAbExperiments) does not filter by tenant_id — reads experiments across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate tenants explicitly. Ensure any processing of experiment rows is limited to their tenant and never assumes a global scan without explicit tenant scoping.
- **server/agentic-engines.ts:161** — SELECT from ai_insights in retryPendingMinervaRouting reads rows across tenants (no tenant filter) before per-row handling.
  - _Fix:_ Add tenant scoping when the sweep should operate per-tenant, or else explicitly and safely enumerate tenants and call routing per-tenant. Avoid reading tenant-scoped tables across all tenants without explicit controls.
- **server/agentic-engines.ts:345** — Query against heartbeat_logs in gatherOperationalData (and other engine functions) lacks tenant_id filter — reads heartbeat logs across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to heartbeat_logs queries (and audit other heartbeat_logs queries) so logs are read only for the intended tenant.
- **server/agentic-engines.ts:451** — SELECT from heartbeat_logs in runPredictiveEngine (recentLogs) lacks tenant scoping — reading logs across tenants.
  - _Fix:_ Filter heartbeat_logs by tenant_id = ${tenantId} to ensure only tenant-scoped records are read.
- **server/agentic-engines.ts:519** — SELECT heartbeat_logs in runOptimizationEngine (heartbeatPerformance) is missing tenant_id filter — aggregates logs across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the heartbeat_logs query (and review other aggregated heartbeat_logs queries) to ensure per-tenant isolation.
- **server/agent-channels.ts:194** — getUnreadCount joins channel_subscriptions but does not include cs.tenant_id = ${tenantId} — the JOIN can match subscription rows from other tenants and produce incorrect counts/visibility.
  - _Fix:_ Constrain the join to channel_subscriptions by tenant (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}). Audit other queries that join channel_subscriptions to ensure tenant scoping.
- **server/agentic-features.ts:318** — Inner subquery in prevSnaps (detectCompetitorChanges) runs "SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id}" with no tenant_id filter — may return snapshot ids from other tenants and lead to cross-tenant comparisons/insertions.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the inner subquery (and any subqueries) so snapshot selection is strictly tenant-scoped. Audit other multi-row subqueries for tenant scoping.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets is executed without a tenant_id WHERE clause, reading a tenant-scoped table across all tenants.
  - _Fix:_ Restrict this query to a single tenant context or otherwise gate it as an explicit admin-only operation. Options: (1) Add a tenantId parameter and include WHERE tenant_id = ${tenantId}; (2) If the intent is an admin multi-tenant sweep, require and validate an explicit admin authorization check and document it clearly, or move this into an isolated admin-only job that runs with verified privileges. Avoid blanket cross-tenant reads from tenant-scoped tables.
- **server/agentic/resume-worker.ts:66** — Raw SELECT queries agent_runs / agent_approvals without constraining to a specific tenant (no WHERE tenant_id = <tenant>) — performs cross-tenant reads.
  - _Fix:_ Scope the query to a single tenant (e.g. add AND r.tenant_id = ${tenantId}) or iterate per-tenant. If this worker must act across tenants, explicitly pass/authorize a tenant context and include a tenant_id predicate in the WHERE clause; never read tenant-scoped rows without an explicit tenant filter.
- **server/auto-asset-capture.ts:145** — INSERT into project_files omits tenant_id. The code inserts project-scoped rows using only project_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (${projectId}, ...)). Per policy, tenant-scoped tables must receive tenant_id explicitly on INSERT to enforce isolation.
  - _Fix:_ Include tenant_id in the INSERT (and schema if missing). e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ${filename}, ...). Also defensively verify ownership of the derived project_id (or look it up with tenant_id) before inserting.
- **server/auto-asset-capture.ts:164** — INSERT into project_notes omits tenant_id. The code writes a project-scoped note using only project_id (INSERT INTO project_notes (project_id, note, author) ...), violating the requirement that INSERTs into tenant-scoped tables set tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT (and to project_notes schema if required). e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projectId}, ${note}, 'system'). Also ensure project ownership is verified (or derive project tenant in a trusted lookup) before the write.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge (watermark) omits any tenant_id constraint — reads a tenant-scoped table across tenants.
  - _Fix:_ Scope the watermark read to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or accept a tenantId parameter and filter by it. If the watermark is intended to be per-tenant, make the function accept/require tenantId and use it in the WHERE clause. If it is intentionally global, document that explicitly and avoid using a tenant-scoped table for a global watermark.
- **server/auto-memorize.ts:62** — SELECT existing watermark row (setWatermark) uses only category/title without tenant_id — can return rows for other tenants, then subsequent UPDATE uses that row id.
  - _Fix:_ When locating an existing watermark row, include tenant_id = ${tenantId} (and make setWatermark accept a tenantId). Only update/replace rows that belong to the caller's tenant to avoid touching other tenants' watermarks.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — the UPDATE omits tenant_id in the WHERE clause, so it can modify a conversation row belonging to a different tenant if IDs are incorrect or untrusted.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. AND tenant_id = ${c.tenant_id} or AND tenant_id = ${tenantId}) so the update can only affect rows owned by the expected tenant. Prefer always using both id and tenant_id in UPDATE/DELETE WHERE clauses when operating on tenant-scoped tables.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id. The code inserts invoice_items with: INSERT INTO invoice_items (invoice_id, description, quantity, unit_price, amount, sort_order) VALUES (${newId}, ...). Per project invariant every tenant-scoped table INSERT must include tenant_id (columns are NOT NULL with no default) to enforce isolation at the application layer.
  - _Fix:_ Include tenant_id in the INSERT for invoice_items (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...)). Alternatively, if invoice_items is intentionally global, document that and ensure it truly is not tenant-scoped. Also consider adding an explicit check that the parent invoice (newId) belongs to the tenant (though in this transaction the invoice was just created), and update schema/tests to prevent regressions (e.g. DB constraint or CI rule verifying all INSERTs into tenant-scoped tables include tenant_id).
- **server/capability-review.ts:490** — UPDATE to tenant-scoped table `capability_reviews` performed without constraining by tenant_id. The markReviewReused() SQL updates rows by id only (`WHERE id = ${pending.reviewId} AND reused = false`) and does not include tenant_id, allowing cross-tenant writes if review ids are guessed or pending map is poisoned.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause and ensure the code tracks tenant ownership when queuing pending reviews. Example fixes: store tenantId in PendingReuse when inserting the review and change the update to `WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false`. Also validate conversation -> tenant mapping server-side before touching rows.
- **server/ceo-orchestrator.ts:659** — Read from tenant-scoped `personas` table without a tenant filter: `const cachedPersonas = await db.select().from(personasTable);` returns personas for all tenants.
  - _Fix:_ Restrict the query to the current tenant (or cache per-tenant). For example, query `WHERE tenant_id = ${plan.tenantId}` (or pass the tenantId into the loader) so only the tenant's personas are returned. Avoid using a global personas cache for multi-tenant reads.
- **server/ceo-orchestrator.ts:1015** — Uses a persona id taken from a global persona cache (cachedPersonas) when creating a conversation (`personaId: targetPersona?.id || null`) without verifying that the persona belongs to the caller's tenant. This can assign cross-tenant persona ids to a tenant-scoped conversation.
  - _Fix:_ Before passing a personaId into storage.createConversation, verify the persona row belongs to the same tenant (e.g. compare persona.tenant_id === plan.tenantId). If the persona does not belong to the tenant, fall back to null or to a tenant-local persona. Prefer selecting personas scoped to tenant (see previous finding) so the returned targetPersona is guaranteed to be owned by the tenant.
- **server/channel-routing.ts:31** — SELECT joins channel_routes -> personas without constraining to a tenant. The query (SELECT cr.*, p.name ...) has no tenant filter on channel_routes or personas, allowing cross-tenant data exposure if personas are tenant-scoped.
  - _Fix:_ Require the caller to pass tenantId into listChannelRoutes and add a WHERE filter restricting to that tenant (e.g. WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}). Alternatively, verify persona ownership per-row. Use Drizzle's .where(eq(table.tenantId, tenantId)) helpers rather than string interpolation.
- **server/channel-routing.ts:63** — getPersonaForChannel queries channel_routes (SELECT persona_id ...) with no tenant constraint. This can return a persona_id belonging to a different tenant.
  - _Fix:_ Require tenantId as an argument and add WHERE tenant_id = ${tenantId} (e.g. SELECT persona_id FROM channel_routes WHERE tenant_id = ${tenantId} AND channel = ${channel} AND enabled = true). If persona ownership matters, also validate the persona belongs to the tenant before returning.
- **server/channel-routing.ts:71** — removeChannelRoute issues DELETE FROM channel_routes WHERE channel = ${channel} with no tenant filter, deleting routes across all tenants if the table is intended to be tenant-scoped.
  - _Fix:_ Require tenantId parameter and include it in the WHERE clause (DELETE FROM channel_routes WHERE tenant_id = ${tenantId} AND channel = ${channel}). Ensure table schema includes tenant_id and that all mutating operations scope by tenant.
- **server/chat-engine.ts:3814** — recordMutation(...) is invoked without any tenant context. If recordMutation writes to a tenant-scoped table it may INSERT/UPDATE rows with no tenant_id or associate logs with the wrong tenant.
  - _Fix:_ Ensure recordMutation receives an explicit tenantId (or the code calling it validates and scopes by tenant) and that the implementation includes tenant_id on all writes. Prefer passing tenantId through the call (e.g. recordMutation({... , tenantId})) and/or have recordMutation enforce WHERE tenant_id = ? for updates and include tenant_id for inserts.
- **server/chat-engine.ts:4080** — recordToolExecution(conversationId, ...) is called without a tenant identifier. Recording tool execution against a conversation by conversationId alone can cross tenant boundaries if the DB writes/reads are not tenant-scoped.
  - _Fix:_ Pass tenantId into recordToolExecution and have the implementation use it to constrain queries/INSERTs (WHERE tenant_id = tenantId or set tenant_id on inserts). Alternatively, explicitly verify the conversation belongs to the caller's tenant before calling recordToolExecution.
- **server/chat-engine.ts:4084** — recordToolExecution(...) is invoked (variant call that includes extracted ids) with no tenant context. Writing tool-execution metadata or associated ids without tenant scoping risks cross-tenant data leakage or tampering.
  - _Fix:_ Same as above — include tenantId in the call and enforce tenant-scoped WHERE clauses or set tenant_id on inserts. Validate ownership of any referenced conversationId/ids before persisting.
- **server/chat-engine.ts:4091** — recordToolCallForStuckDetection(conversationId, toolName, ...) is invoked without tenant information. If this function persists/readstool-call history in a tenant-scoped table it must be constrained by tenant_id.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection (or verify conversation ownership beforehand). Ensure the function's DB operations include WHERE tenant_id = tenantId and that any INSERTs set tenant_id explicitly (never rely solely on conversationId).
- **server/commitment-drafter.ts:146** — SELECT from tenant-scoped table `commitments` enumerates tenant rows without constraining to a specific tenant (no `WHERE tenant_id = ...`).
  - _Fix:_ Avoid scanning tenant-scoped tables across all tenants in application code. If you must enumerate tenants for a multi-tenant background job, isolate that action to a clearly-audited admin-only path and/or use a dedicated global index/table (e.g. tenants_with_pending_work) that is explicitly designed for cross-tenant enumeration. Otherwise, change the query to always filter by the caller's tenant_id (e.g. `WHERE tenant_id = ${tenantId}`) so reads are tenant-scoped.
- **server/commitment-drafter.ts:235** — UPDATE against tenant-scoped table `commitments` omits tenant filter: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This writes a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${expectedTenantId}`) so the update is constrained to the owning tenant. If the code cannot trust `row.tenantId`, verify ownership first and only update rows where tenant_id matches the verified tenant. Avoid any write to tenant-scoped tables without an explicit tenant_id equality filter.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` is executed without any tenant_id constraint (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Fix:_ If this is intended to run per-tenant, add a tenant parameter and include `AND tenant_id = ${tenantId}` in the WHERE clause (or iterate tenants explicitly). If it must be a global admin job, document and gate it behind an explicit admin-only path and harden auditing; avoid exposing this path to callers that expect tenant-scoped reads.
- **server/data-protection.ts:168** — DELETE FROM conversations uses only `WHERE id = ${conv.id}` with no tenant_id predicate. Although child deletes above include tenant predicates, this final DELETE lacks the tenant scoping and could delete a conversation row without an explicit tenant filter.
  - _Fix:_ Include the conversation's tenant_id in the DELETE predicate (e.g. `DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}`), or perform the purge inside a tenant-scoped transaction. This keeps the same defense-in-depth applied to child deletes and prevents accidental cross-tenant deletions.
- **server/delivery-pipeline.ts:203** — updateDeliveryLog performs an UPDATE on delivery_logs without scoping by tenant_id (where clause is eq(delivery_logs.id, id) only). This allows updating another tenant's delivery row by id.
  - _Fix:_ Require and enforce the tenant context on the update: accept a tenantId parameter (or derive it) and AND it into the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))). Validate callers supply the correct tenant before calling this helper.
- **server/delivery-pipeline.ts:1071** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with no tenant_id constraint. This can return another tenant's delivery row/link.
  - _Fix:_ Include tenant scoping on this lookup (e.g. accept a tenantId and use .where(and(eq(deliveryLogs.stripePaymentId, paymentId), eq(deliveryLogs.tenantId, tenantId)))) or provide a separate, explicitly-documented token-based access path if cross-tenant lookup is intended.
- **server/dfy-intake.ts:93** — submitDfyIntake updates dfy_intake_forms via .where(eq(dfy_intake_forms.id, form.id)) without constraining tenant_id. This allows updating a form row by id regardless of tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId || <expectedTenant>)))) or otherwise validate the form belongs to the expected tenant before performing the update.
- **server/doc-collections.ts:165** — addDocument issues a DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with no tenant_id constraint. Even though collection existence is checked earlier, the DELETE itself is not explicitly tenant-scoped.
  - _Fix:_ Include tenant_id in the DELETE where clause (AND tenant_id = ${tenantId}) to make tenant scoping explicit and robust. Alternatively, delete by joining to the collection row in a single tenant-scoped statement.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks embedding with UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} and does not include tenant_id. This can modify another tenant's chunk row by id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or resolve the chunk via collection_id + tenant_id before updating to ensure tenant-scoped updates only.
- **server/email.ts:515** — backfillEmptyBodies selects rows from inbox_messages (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id) without any tenant scoping. This reads across all tenants' inbox_messages.
  - _Fix:_ Restrict the SELECT to a specific tenant (pass tenantId) or iterate per-tenant. If the intent is a global maintenance job, ensure it's run under a privileged, audited path and consider adding tenant_id filtering or explicit checks to avoid accidental cross-tenant exposure.
- **server/email.ts:529** — backfillEmptyBodies updates inbox_messages with UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} without tenant_id in the WHERE clause. This can update another tenant's message record by id.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update using a tenant-scoped lookup to ensure only the intended tenant's rows are modified.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from caller input (query→embedding→vec). This performs string interpolation of request-derived data into SQL (embedding_vec <=> '${vec}'::vector), which risks SQL injection and violates safe parameterization rules.
  - _Fix:_ Avoid sql.raw() interpolation of request-derived data. Pass the embedding as a parameter binding (use Drizzle/parameterized placeholders or prepared statements) and cast it to vector in the query, or use a DB-binding API that accepts typed parameters. Keep the existing numeric-shape validation, but remove direct string interpolation into sql.raw().
- **server/embeddings.ts:252** — Same as above: ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} interpolates a request-derived embedding into raw SQL.
  - _Fix:_ Use parameterized binding for the vector (do not build a quoted vector literal via string interpolation). For example, pass the embedding as a query parameter and cast to vector on the server side, or use Drizzle's parameter binding APIs to avoid constructing raw SQL strings from input-derived data.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated vector literal derived from caller input in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). This interpolates request-derived data into raw SQL.
  - _Fix:_ Replace the string-interpolated sql.raw vector literal with a parameterized binding (pass the embedding as a query parameter or use Drizzle's parameter support) instead of injecting a constructed string into sql.raw(). Continue to validate the embedding shape, but avoid string interpolation into the SQL string.
- **server/event-bus.ts:173** — Query joins event_subscriptions.es to personas p via p.id = es.persona_id but does not verify that the persona row belongs to the same tenant (no p.tenant_id filter). Because DB-level FK/ownership is not relied upon, this can leak persona data across tenants or associate subscriptions with personas from other tenants.
  - _Fix:_ Constrain persona rows to the same tenant in the join (e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = ${tenantId}) or add an explicit WHERE p.tenant_id = ${tenantId}. Alternatively, explicitly validate persona ownership before using its data.
- **server/event-bus.ts:187** — Wildcard subscription query (the fallback when no exact event subscriptions are found) also JOINs personas p without constraining p.tenant_id to the caller's tenant, risking cross-tenant persona name leakage.
  - _Fix:_ Same as above: include p.tenant_id = ${tenantId} in the JOIN condition or WHERE clause, or validate persona ownership server-side before returning persona data.
- **server/event-bus.ts:312** — createEventSubscription inserts a caller-supplied personaId into event_subscriptions without verifying that the referenced persona belongs to the provided tenantId. An attacker could register subscriptions pointing at personas from other tenants (or arbitrary persona ids).
  - _Fix:_ Before INSERT, validate that the personaId belongs to the tenant (e.g. SELECT 1 FROM personas WHERE id = ${params.personaId} AND tenant_id = ${tenantId}). Only proceed with the INSERT if that check passes. Always validate foreign ids from callers map to rows owned by the same tenant.
- **server/felix-brain.ts:19** — Raw SELECT from tenant-scoped table 'agent_runs' without constraining tenant_id (no WHERE tenant_id = ...). This rehydration reads rows across all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant with an explicit tenantId parameter. If this is intended to be an admin-only operation, require and validate an admin tenant context and make the tenant scope explicit in the SQL.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint. This can update a run belonging to another tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) or otherwise validate the run belongs to the calling tenant before updating.
- **server/felix-loop.ts:690** — Error-path UPDATE on 'felix_loop_runs' uses WHERE id = ${runId} without tenant_id constraint, allowing cross-tenant modification in failure handling.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) or resolve/verify the run row with tenant context before writing the error/update.
- **server/google-drive.ts:560** — UPDATE projects SET driveFolderId/driveFolderUrl uses WHERE eq(projects.id, projectId) without tenant_id filtering. Although project ownership was validated earlier, the UPDATE itself is not tenant-constrained (write lacks explicit tenant filter).
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or perform the UPDATE using the same tenant-validated context to ensure the write cannot affect another tenant's project.
- **server/google-workspace.ts:2358** — INSERT into tenant-scoped table presenter_slide_images does not include tenant_id (session_id used alone). This writes binary image rows without scoping by tenant, allowing cross-tenant data leakage or overwrite if session_id is controlled or collides.
  - _Fix:_ Persist tenant ownership explicitly: include tenant_id in the INSERT values and any unique/index definitions (or use a composite unique on (tenant_id, session_id, slide_index, quality)). Prefer using the Drizzle insert API (db.insert(...).values({ tenantId, ... })) so tenantId is always set from the trusted ctx, and validate that session_id belongs to that tenant before writing.
- **server/google-workspace.ts:2364** — Second INSERT (thumbnail) into presenter_slide_images omits tenant_id as above — same cross-tenant write risk.
  - _Fix:_ Same fix as above: include tenant_id for thumbnail rows, use a composite uniqueness that includes tenant_id, and/or use Drizzle insert helpers with tenantId derived from the trusted context. Validate session ownership before inserting.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. Updating session rows by id only can modify another tenant's session if session ids collide or are attacker-controlled.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) and verify the session belongs to the caller's tenant before modifying. When possible, use parameterized ORM methods that include tenantId from the trusted ctx rather than relying on id alone.
- **server/health-audit.ts:314** — SELECT FROM code_proposals omits any tenant_id predicate. code_proposals is likely tenant-scoped, so this query can read proposals across all tenants.
  - _Fix:_ Restrict the query to the intended tenant (e.g. WHERE tenant_id = ${tenantId} AND ...), or require/admin-check when running cross-tenant audits. If this scanner must run as a global admin, ensure the code documents/guards that and that only admin principals can call it.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) has no tenant_id filter. This will alter proposals across tenants if stale ids include rows from other tenants.
  - _Fix:_ Apply the update with tenant scoping (e.g. AND tenant_id = ${tenantId}) or derive 'ids' from a prior tenant-scoped SELECT (only update ids known to belong to the caller's tenant). If this operation is intended to be global/admin, gate it behind an explicit admin check and document it.
- **server/heartbeat.ts:53** — SELECT from research_schedules executed without constraining by tenant_id. The query SELECT * FROM research_schedules ... lacks any WHERE tenant_id = ... clause, so rows from all tenants will be returned to the caller.
  - _Fix:_ Scope the select to a tenant (or iterate per-tenant explicitly). Add a tenant predicate (e.g. WHERE tenant_id = ${sched.tenant_id} or WHERE tenant_id = ${someTenantId}) or use a storage helper that is explicit about ALL_TENANTS vs a single tenant to avoid accidental cross-tenant reads.
- **server/heartbeat.ts:385** — SELECT from event_log (SELECT * FROM event_log WHERE status = 'pending' ...) executes without a tenant_id constraint and will return pending events from all tenants.
  - _Fix:_ Run per-tenant or include tenant_id in the WHERE clause (e.g. WHERE tenant_id = ${someTenantId} AND status = 'pending') or use a storage API designed to return only the tenant-scoped events needed.
- **server/heartbeat.ts:1670** — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id constraint. If heartbeat_logs is tenant-scoped this will read logs across tenants.
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND task_name = ...) or use a storage API that enforces tenant scoping for heartbeat logs.
- **server/job-queue.ts:320** — SELECT ... FROM agent_jobs ${whereClause} (listJobs) can emit a result set with no tenant_id filter when the caller omits tenantId. Listing jobs across all tenants may expose tenant-scoped data.
  - _Fix:_ Require callers to provide tenantId unless the caller is an explicit admin operator. Enforce at the function boundary (throw or require an isAdmin flag) or default to scoping by the current tenant context. Document and audit any deliberate cross-tenant list use.
- **server/job-queue.ts:385** — SELECT status, COUNT(*) ... FROM agent_jobs WHERE created_at > ... (getJobStats) aggregates job counts across ALL tenants with no tenant_id scoping, leaking cross-tenant metrics.
  - _Fix:_ Return per-tenant stats when called by tenant-scoped callers (add tenantId param and include WHERE tenant_id = ${tenantId}). If global stats are intended, restrict this function to an admin context with clear authorization and audit logging.
- **server/knowledge-diversity-monitor.ts:266** — SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} executes against agent_knowledge without restricting to a single tenant. This reads tenant-scoped embeddings across tenants.
  - _Fix:_ If the monitor must iterate all tenants, ensure it runs in a trusted admin context; otherwise, scope scans to a single tenant (accept tenantId) or restrict access. Add comments and authorization checks to make global scans explicit and safe.
- **server/lib/bwb-job-progress.ts:74** — scope() may omit tenant_id, allowing UPDATE/SELECT against tenant-scoped video_jobs rows without a tenant predicate. Several callers call .where(scope(...)) (setBwbPhase, updateBwbChapters, completeBwbJob select+update, bumpBwbHeartbeat, failBwbJob) and if env BWB_TENANT_ID is missing the WHERE reduces to jobId-only — this results in cross-tenant writes/reads on a tenant-scoped table.
  - _Fix:_ Always require and bind tenant_id in the WHERE clause. Do not fall back to jobId-only filtering. Options: (1) change envTenantId()/scope() to throw or return an error when BWB_TENANT_ID is missing so callers cannot perform DB writes without an explicit tenant; (2) require callers to pass an explicit tenantId parameter and assert it is present and positive before issuing queries; or (3) include tenant_id as a mandatory WHERE predicate in every query (fail closed) and log/abort if it cannot be provided. Ensure tests and CI set BWB_TENANT_ID or call sites pass tenantId explicitly.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT aggregates projects across all tenants without a tenant_id filter (reads tenant-scoped table `projects` with no WHERE tenant_id = ...). This violates the invariant that every read against tenant-scoped tables must be constrained to the caller's tenant, and can leak cross-tenant counts/rows.
  - _Fix:_ Restrict the aggregate query to only the tenants present in the snapshot (wantedByTenant). Build a parameterized tenant-id list and add WHERE tenant_id = ANY(${tenantIdArray}::int[]) (or multiple per-tenant queries) instead of scanning the entire projects table. Ensure all queries against tenant-scoped tables include explicit tenant_id constraints.
- **server/lib/inbox-ingest.ts:536** — SELECT reads inbox_messages (alias m) without constraining that table by tenant_id. The query filters inbox_classifications by c.tenant_id but joins inbox_messages m without m.tenant_id = ${tenantId}, risking cross-tenant exposure if inbox_message_id links to a row from another tenant.
  - _Fix:_ Constrain every tenant-scoped table in the query. For example: ... FROM inbox_classifications c JOIN inbox_messages m ON m.id = c.inbox_message_id WHERE c.tenant_id = ${tenantId} AND m.tenant_id = ${tenantId} AND c.classified_at > ... — or add m.tenant_id = ${tenantId} to the WHERE clause to ensure m is scoped to the caller's tenant.
- **server/lib/mention-injection.ts:121** — Query joins personas p (LEFT JOIN personas p ON p.id = cm.from_persona_id) but does not constrain personas by tenant_id. Reading p.name without p.tenant_id = ${tenantId} can leak another tenant's persona metadata.
  - _Fix:_ Restrict the joined personas to the tenant, e.g. JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId} (or add AND p.tenant_id = cm.tenant_id in the WHERE). Ensure every tenant-scoped table in SELECT/JOIN has an explicit tenant_id filter.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table 'tenant_voice_profiles' is constrained only by id (WHERE id = <existing.id>) and does not include a tenant_id check. Without also filtering on tenant_id an attacker or bug might update a row belonging to a different tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. add a tenant condition: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively read+validate the existing row. Fail-closed if tenantId mismatch.
- **server/memory-graph.ts:184** — refreshCategoryCount issues: the inner SELECT counting memory_entries does not filter by tenant_id, and the outer UPDATE on memory_categories updates by id only (no tenant_id filter). This allows a category's memory_count to be computed from other tenants' rows and allows updating a category row without ensuring tenant ownership.
  - _Fix:_ Require and pass an explicit tenantId into refreshCategoryCount and add tenant filters to both queries. For example: UPDATE memory_categories SET memory_count = (SELECT count(*)::int FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status = 'active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId};
- **server/memory-graph.ts:124** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} updates a tenant-scoped table by id only with no tenant_id constraint. This can modify a category row belonging to another tenant.
  - _Fix:_ Include tenant scoping on the UPDATE (and/or pass tenantId through). E.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}; or validate the category's tenant before updating.
- **server/memory-graph.ts:494** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped table by id only and lacks a tenant_id constraint. This can change rows belonging to other tenants if IDs are misused.
  - _Fix:_ Constrain the UPDATE to the tenant that owns the row. Either include AND tenant_id = ${tenantId} in the WHERE clause or perform the update within a tenant-scoped transaction (or both). For example: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId};
- **server/minerva-planner.ts:367** — Conditional CAS UPDATE on plans may execute without a tenant_id constraint when args.tenantId is omitted, enabling cross-tenant status flips (IDOR).
  - _Fix:_ Require an explicit tenantId for tenant-scoped operations or always include AND tenant_id = ${tenantId} in the WHERE clause. Fail-closed: throw if tenantId is missing for externally-invoked flows.
- **server/minerva-planner.ts:386** — SELECT probe (plans) is executed without a tenant_id constraint when args.tenantId is omitted, allowing reads across tenants (IDOR).
  - _Fix:_ Do not query tenant-scoped tables without a tenant_id. Require tenantId argument or always add AND tenant_id = ${tenantId}. If this path is meant for trusted internal callers, enforce a separate internal-only API surface rather than an optional tenant filter.
- **server/minerva-planner.ts:516** — listPlans defaults tenantId to 1 (admin) when args.tenantId is omitted, causing tenant-scoped reads/writes to be misattributed to the admin tenant.
  - _Fix:_ Do not default to the admin tenant. Require callers to explicitly provide tenantId (fail-closed) or validate caller identity at the API layer. If a default is unavoidable, log loudly and ensure only system-internal callers can use that code path.
- **server/minerva-planner.ts:548** — getPlan allows a branch that SELECTs FROM plans without any tenant_id constraint when tenantId is omitted, enabling cross-tenant reads (IDOR).
  - _Fix:_ Remove the branch that issues an unconstrained SELECT. Require tenantId for all external/tool-facing callers. If internal calls legitimately need unconstrained access, split into a separate internal-only function and ensure it's not callable from tenant-facing code.
- **server/minds-engine.ts:214** — INSERT into mind_tickets builds an array literal via string interpolation: `{${params.dependsOn.join(",")}}` using caller-supplied params.dependsOn. This is a SQL-construction pattern with request-derived data (injection + isolation risk).
  - _Fix:_ Use parameterized array binding instead of composing a Postgres array literal string. For example pass dependsOn as a proper parameter/typed array (e.g. ${params.dependsOn} with Drizzle/driver array support, or use untrusted-values-safe pg array parameterization). Validate/normalize ids to integers before use.
- **server/minds-engine.ts:262** — checkDependenciesMet uses id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) building an array literal from ticket.dependsOn. The array content may originate from untrusted input and is injected into SQL text.
  - _Fix:_ Avoid composing array literals via string interpolation. Bind an actual integer array parameter (driver/Drizzle-supported), or use prepared statements with array parameters (e.g. WHERE id = ANY(${ticket.dependsOn}::int[])). Validate and coerce elements to integers before binding.
- **server/minds-engine.ts:327** — markEventsHandled builds a Postgres array literal from caller-provided eventIds: `id = ANY(${`{${eventIds.join(",")}}`}::int[])`. This is string-interpolated SQL using request-derived data (injection risk) even though tenant_id is present.
  - _Fix:_ Pass eventIds as a parameterized array instead of interpolating into SQL text. Use a proper array parameter (e.g. WHERE id = ANY(${eventIds}::int[])) or Drizzle array binding. Validate eventIds are integers before binding.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and ensure you've validated ownership). E.g. VALUES (${options.tenantId}, ${options.projectId}, ...), or add tenant_id column to the INSERT column list. Continue using assertProjectInTenant to verify ownership before inserting.
- **server/mpeg-engine.ts:1417** — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id in the VALUES list.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and ensure you've validated ownership). E.g. VALUES (${options.tenantId}, ${options.projectId}, ...). Use assertProjectInTenant (already present) and then pass tenantId into the INSERT so rows are always scoped to the tenant.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id/is_active FROM `oauth_subscriptions` for provider='google-workspace' with no tenant_id constraint — enumerates cross-tenant subscription state.
  - _Fix:_ Restrict the query to a specific tenant_id when performing tenant-scoped operations. If the code intentionally needs to enumerate tenants, ensure it runs under an explicit privileged path with careful review and avoid exposing tokens or tenant-scoped data to unprivileged contexts.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — missing tenant_id constraint on UPDATE against a tenant-scoped table.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or use the (provider, tenant_id) key when updating to ensure the update cannot affect rows in other tenants.
- **server/oauth-subscriptions.ts:966** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — another unscoped UPDATE on a tenant-scoped table (from a different branch).
  - _Fix:_ Same fix: always include tenant_id in WHERE (or provider+tenant_id) when updating tenant-scoped rows. Avoid relying solely on id when the calling context previously performed unscoped reads.
- **server/orchestrator-ledger.ts:459** — UPDATE memory_entries SET ... WHERE id = ${row.id} — the UPDATE uses id only and does not constrain by tenant_id. This is an unscoped write on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or use the same tenant-scoped predicate used for the SELECT, to ensure the UPDATE cannot affect rows belonging to other tenants.
- **server/pace-control.ts:85** — SELECT COUNT(*) FROM `heartbeat_logs` is executed without any tenant_id filter (checkPace). This is a tenant-scoped table but is being queried globally, leaking/merging cross-tenant usage counts.
  - _Fix:_ Make checkPace tenant-aware: accept/require a tenantId and add WHERE tenant_id = ${tenantId} to the query. If global monitoring is required, perform that in a separate, explicitly privileged admin path with audit controls.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs (counts and per-persona aggregates) without filtering by tenant_id — returns cross-tenant aggregates from a tenant-scoped table.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) and require a tenantId parameter for snapshot APIs that are meant to be per-tenant. If an operator-level global snapshot is intended, isolate it into a dedicated admin-only routine with explicit authorization and auditing.
- **server/process-governor.ts:119** — In heartbeat task queries the correlated subqueries SELECT COUNT(*) FROM heartbeat_logs ... lack tenant_id filtering. The subquery aggregates logs without scoping to the tenant, leaking or mis-aggregating other tenants' logs.
  - _Fix:_ Add AND tenant_id = ${tenantId} inside each heartbeat_logs subquery (e.g. WHERE task_id = ht.id AND tenant_id = ${tenantId} AND ...). Ensure every subquery referencing tenant-scoped tables includes tenant scoping.
- **server/process-governor.ts:583** — Action 'disable_task' uses SELECT with subqueries that count heartbeat_logs without tenant_id (same as earlier) which can mis-evaluate failure rates using other tenants' logs.
  - _Fix:_ Add tenant_id = ${tenantId} to those subqueries so counts only include this tenant's logs.
- **server/process-governor.ts:238** — cascading_failures: SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... lacks tenant_id — it aggregates logs across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so the count is restricted to the requested tenant.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without tenant_id — reads a tenant-scoped project without validating that the caller's tenant owns it.
  - _Fix:_ Require and pass tenantId into updateProjectBrain (or fetch the calling tenant from context) and add AND tenant_id = ${tenantId} to the SELECT. Validate ownership before reading/writing project-related data.
- **server/project-brain.ts:332** — backfillProjectBrains: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... has no tenant_id filter — enumerates projects across tenants.
  - _Fix:_ Scope backfill to a tenant (add WHERE p.tenant_id = ${tenantId}) or make it an explicit admin-only operation. Prefer per-tenant backfill to avoid cross-tenant data leakage.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is done without tenant scoping — may read conversations from other tenants' projects.
  - _Fix:_ Add AND c.tenant_id = ${tenantId} (or join/validate project.tenant_id) to ensure conversations are limited to the intended tenant before using their messages.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant_id filter — reads messages across tenants if conversation ids overlap or are attacker-controlled.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the messages WHERE clause or derive tenant from the conversation/project row and validate before reading messages.
- **server/recurring-messages.ts:182** — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id constraint when invoked with the _allowGlobalSystemUseOnly path. This returns rows across all tenants.
  - _Fix:_ Avoid performing unst scoped SELECTs on tenant-scoped tables in general codepaths. If a global scan is required (cron/maintenance), make the function internal-only and document/audit it; better, iterate tenants and query per-tenant (WHERE tenant_id = ...) or require an explicit internal-only flag and log/limit its use. Ensure any callers are tightly controlled and reviewed.
- **server/recurring-messages.ts:258** — Global scan: SELECT id,title,content,tenant_id FROM `agent_knowledge` WHERE category = 'recurring_message' LIMIT 500 executes with no tenant_id filter and returns rows for all tenants.
  - _Fix:_ If the heartbeat/runner must process all tenants, perform per-tenant queries (SELECT ... WHERE tenant_id = ${tenantId}) or otherwise restrict the scan to an explicitly-internal codepath with strict access control. Do not expose or reuse this unscoped query from request-handling code; limit the result set and validate tenant ownership before any per-row operations.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge removes autoresearch rows without scoping by tenant_id, which will delete entries across all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()), or perform the delete per-tenant. Ensure every DELETE on a tenant-scoped table includes the caller's tenant_id.
- **server/routes.ts:1118** — Ownership check queries file_storage rows by filename across all tenants (SELECT tenantId FROM file_storage WHERE filename = <baseName>) with no tenant_id WHERE clause. This reads cross-tenant data to decide authorization and may leak existence and ownership information between tenants.
  - _Fix:_ Avoid broad cross-tenant SELECTs. First check for an owner row scoped to the authenticated tenant (WHERE filename = .. AND tenant_id = authedTenantId). If you must detect collisions, perform a separate existence check that does not reveal tenant info (e.g. SELECT COUNT(*) WHERE filename = .. LIMIT 1 and treat any nonzero as "exists" without returning tenant identifiers), or only perform cross-tenant scans in an explicit admin-only flow. Ensure all app-layer reads use eq(fileStorage.tenantId, tenantId) when determining access or serving content.
- **server/routes.ts:766** — Backfill selects projects (SELECT p.id, p.name, p.tenant_id FROM projects JOIN tenants ...) without constraining by tenant_id. This runs at startup and operates across all tenant rows without an explicit admin guard or per-tenant iteration, enabling cross-tenant reads/processing.
  - _Fix:_ Make this an explicit admin-only operation or iterate tenants and process each tenant's projects in a tenant-scoped query (e.g. WHERE p.tenant_id = <tid>) so the code never reads or mutates tenant-scoped rows across tenants in a single unscoped query. If global work is required, ensure it's executed under a well-audited admin context and documents why cross-tenant access is necessary.
- **server/routes.ts:1561** — Public architecture endpoint runs multiple unscoped SELECTs against tenant-scoped tables (e.g. SELECT COUNT(*) FROM conversations, messages; SELECT name, role FROM personas WHERE is_active = true) and even returns persona rows. This exposes tenant-scoped data (names, counts) across tenants to a public endpoint.
  - _Fix:_ Restrict this endpoint to platform-admins or sanitize outputs to avoid returning tenant-scoped rows (e.g. remove persona names / tenant-specific details). If totals are needed publicly, consider computing and caching sanitized metrics via an admin-only background job, or require caller authentication and add WHERE tenant_id = <tenantId> to make responses tenant-scoped.
- **server/routes.ts:3443** — Message-feedback handler SELECTs from messages by id without scoping the query to the caller's tenant (SELECT conversation_id, tenant_id FROM messages WHERE id = ${messageId}). The code then compares the returned tenantId to the caller's tenant — but the initial unscoped read exposes cross-tenant rows and may leak existence or data before authorization is applied.
  - _Fix:_ Scope the SELECT to the caller's tenant in the WHERE clause (e.g. .where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId)))) so a row from another tenant is never read. Alternatively use a storage helper that enforces tenant ownership at the DB layer (or require platform-admin privileges for cross-tenant access). Avoid performing unscoped reads and applying ownership checks only after the row is returned.
- **server/routes.ts:6510** — storage.createKnowledge({ ...rest, personaId: rest.personaId || null }) is invoked in the admin /api/import handler without an explicit tenantId. Knowledge/memory/agentKnowledge records are tenant-scoped and must be inserted with a validated tenant_id; relying on imported payload fields or omitting tenant_id risks cross-tenant data insertion.
  - _Fix:_ Require and set tenantId explicitly when creating knowledge entries during import (e.g. storage.createKnowledge({ ...rest, personaId: ..., tenantId: <targetTenant> })). Validate that any tenantId present in the import is authorized, or map imported entities to a chosen target tenant before insertion.
- **server/routes.ts:6500** — storage.createMemoryEntry({ ...rest, personaId: rest.personaId || null }) is called in the admin /api/import handler without an explicit tenantId. Memory entries are tenant-scoped and inserting them without tenant_id (or trusting the import file) breaks tenant isolation.
  - _Fix:_ Pass a validated tenantId into createMemoryEntry (e.g. storage.createMemoryEntry({ ...rest, personaId: ..., tenantId: <targetTenant> })) or enforce a mapping from imported persona IDs to target-tenant personas, rejecting/flagging any records that lack an allowed tenant mapping.
- **server/routes/admin.ts:715** — Query against tenant-scoped table `tool_performance` has no tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance). This can return rows across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant, e.g. add WHERE tenant_id = ${tenantId} (or equivalent Drizzle filter). Ensure tenantId is the session/admin tenant read earlier and is used as a parameter, not string-concatenated into SQL.
- **server/routes/admin.ts:721** — Query against tenant-scoped table `tool_performance` lacks tenant_id constraint (SELECT ... FROM tool_performance WHERE last_failure_at > ...). This can leak cross-tenant telemetry.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. AND tenant_id = ${tenantId}, or use a Drizzle .where(eq(table.tenantId, tenantId)) to enforce caller ownership before returning results.
- **server/routes/admin.ts:734** — Query against tenant-scoped table `ai_insights` lacks tenant_id constraint (SELECT ... FROM ai_insights WHERE status = 'new' ...). This will surface incidents across all tenants to the caller.
  - _Fix:_ Constrain the query to the caller's tenant by adding AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(aiInsights.tenantId, tenantId))). If cross-tenant admin intent is required, make that explicit and safe, but do not omit the tenant filter by default.
- **server/routes/admin.ts:744** — Query against tenant-scoped table `heartbeat_tasks` has no tenant_id constraint (SELECT ... FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3). This can return tasks for all tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the query is explicitly scoped to the caller's tenant. Avoid relying on global reads without an explicit platform-admin justification and audit.
- **server/routes/admin.ts:756** — Query against tenant-scoped table `ai_insights` lacks tenant_id constraint (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' ...). This can reveal high-priority items from all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (AND tenant_id = ${tenantId}) or apply an explicit multi-tenant-safe aggregation with documented admin privileges. Prefer parameterized Drizzle filters (eq(table.tenantId, tenantId)) rather than string interpolation.
- **server/routes/conversations.ts:99** — UPDATE against tenant-scoped table 'conversations' performed without constraining by tenant_id (WHERE id = ${conv.id} only)
  - _Fix:_ Include the caller's tenant in the SQL WHERE clause so the update cannot affect other tenants. Example: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(conversations.tenantId, tenantId))). Always enforce SQL-level tenant scoping for writes.
- **server/routes/conversations.ts:105** — UPDATE against tenant-scoped table 'projects' performed without constraining by tenant_id (WHERE id = ${projectId} only)
  - _Fix:_ Constrain the UPDATE to the caller's tenant to prevent cross-tenant modifications. Example: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Ensure every UPDATE/DELETE includes tenant_id in the WHERE clause (or use Drizzle .where(eq(projects.tenantId, tenantId))).
- **server/routes/goal-ledger.ts:92** — readActiveJobs() issues: SELECT FROM agent_jobs (joined to tenants) does not constrain results by tenant_id. This returns rows across all tenants.
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE j.tenant_id = ${tenantId}) or accept a tenantId parameter and filter by it. If the query intentionally must be cross-tenant, make that explicit, restrict the route to a platform-admin-only path, and document the intent; otherwise scope results to the caller's tenant.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} — the UPDATE has no tenant_id constraint. This can mutate a message belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or use a joined predicate that verifies ownership before updating. Always enforce tenant predicate on write statements.
- **server/routes/metrics.ts:63** — Metrics queries (e.g. SELECT from agent_cost_ledger, agent_cost_ledger tool counts, tokens, repair_incidents) query tenant-scoped tables without a tenant_id WHERE predicate, returning data across tenants.
  - _Fix:_ If multi-tenant aggregation is required for operator metrics, centralize aggregation into a dedicated, auditable admin-only path and ensure strict operator authentication. Otherwise, restrict queries to the caller's tenant by adding WHERE tenant_id = ${tenantId} (or accept an explicit tenantId and validate caller ownership). Avoid ad-hoc cross-tenant SELECTs against tenant-scoped tables.
- **server/routes/memory.ts:159** — Query for linkedEntries uses tenantClause = tenantId === ADMIN_TENANT_ID ? sql`TRUE` : eq(memoryEntries.tenantId, tenantId) — when tenantId is ADMIN_TENANT_ID the tenant predicate is effectively removed (TRUE), causing a cross-tenant read on memory_entries.
  - _Fix:_ Avoid using a bare TRUE clause that removes tenant scoping. If platform-admin users must see all tenants, require an explicit platform-admin authorization check and call an explicit admin-only path; otherwise always include a tenant predicate (eq(memoryEntries.tenantId, tenantId)). If admin-level cross-tenant reads are required, make the intent explicit and audit/authorize access accordingly.
- **server/routes/projects.ts:33** — SELECT subqueries count project_files, project_notes, project_conversations without constraining those tenant-scoped tables to the caller's tenant (no WHERE tenant_id = ${tenantId} or equivalent). These reads rely on project_id alone and therefore violate the invariant requiring explicit application-level tenant filters.
  - _Fix:_ Constrain each subquery to the requester's tenant. Example: change "(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)" to "(SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId})" (or JOIN projects and check p.tenant_id = ${tenantId} in the subselect). Ensure every read against project_files/project_notes/project_conversations includes an explicit tenant filter.
- **server/routes/projects.ts:224** — INSERT INTO project_files in the base64 upload handler does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...). If project_files is tenant-scoped and tenant_id is NOT NULL/no default, this allows creation of rows without an explicit tenant ownership check and violates the invariant.
  - _Fix:_ Include tenant_id in the INSERT payload (and derive it server-side from the authenticated tenant after validating the project belongs to the tenant). E.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) or use an application-layer helper/storage method that sets tenant_id explicitly after verifying ownership.
- **server/routes/projects.ts:292** — INSERT INTO project_files in the multipart upload handler does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...). Same tenant-ownership insertion omission as above.
  - _Fix:_ Include tenant_id in the INSERT (VALUES (..., ${tenantId})) after verifying the project belongs to the authenticated tenant, or call a storage API that enforces tenant_id on insert.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ... reads project_files without an explicit tenant constraint. Although the project was validated earlier, this SELECT does not include tenant_id in the WHERE clause/joins, violating the requirement that every read against a tenant-scoped table be explicitly constrained to the caller's tenant.
  - _Fix:_ Constrain the SELECT to the tenant (for example: SELECT * FROM project_files pf JOIN projects p ON p.id = pf.project_id WHERE pf.project_id = ${projectId} AND p.tenant_id = ${tenantId} ORDER BY ...), or add pf.tenant_id = ${tenantId} if project_files stores tenant_id. Ensure all reads include an explicit tenant-level guard.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not constrain project_files by tenant_id. This is a direct read on a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Add tenant scoping to the query, e.g. AND tenant_id = ${tenantId} or JOIN projects p ON p.id = project_files.project_id AND p.tenant_id = ${tenantId}. Always include an explicit tenant constraint for file lookups.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} deletes a row from project_files without an explicit tenant_id constraint. This write may affect tenant-scoped data without a direct tenant filter in the WHERE clause.
  - _Fix:_ Include tenant scoping in the DELETE, e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}, or perform the delete via a storage-layer call that enforces tenant ownership after verifying the file belongs to a project owned by the tenant.
- **server/sculptor.ts:105** — UPDATE sculptor_sessions sets status/result without constraining tenant_id (WHERE id = ${session.id} only). This can modify a session row belonging to another tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${params.tenantId}) or otherwise verify the row belongs to the caller's tenant before updating.
- **server/sculptor.ts:112** — UPDATE sculptor_sessions sets conversation_id WHERE id = ${session.id} with no tenant_id constraint. Missing tenant guard allows cross-tenant modification.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the WHERE clause or verify ownership before writing.
- **server/sculptor.ts:167** — UPDATE sculptor_sessions (monitorSession) updates status/result/tool_calls_count/tokens_used/completed_at WHERE id = ${sessionId} but omits tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) to enforce tenant isolation.
- **server/sculptor.ts:358** — UPDATE sculptor_sessions sets review_result WHERE id = ${sessionId} with no tenant_id filter — writes are not scoped to the caller's tenant.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or validate the session belongs to the tenant before updating.
- **server/second-opinion.ts:404** — settleFusionReservation runs UPDATE agent_cost_ledger ... WHERE id = ${reservationId} with no tenant_id constraint. This may allow settling (modifying) ledger rows belonging to other tenants.
  - _Fix:_ Pass a tenant identifier into settleFusionReservation (or fetch+verify the row's tenant_id first) and include AND tenant_id = <tenantId> in the WHERE clause, or verify ownership before performing the UPDATE.
- **server/second-opinion.ts:423** — releaseFusionReservation runs UPDATE agent_cost_ledger ... WHERE id = ${reservationId} without scoping by tenant_id, allowing cross-tenant ledger modifications.
  - _Fix:_ Require and use the tenantId when releasing reservations (or first verify the reservation row belongs to the tenant) and add AND tenant_id = <tenantId> to the WHERE clause.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter — reads tenant-scoped table globally.
  - _Fix:_ Scope this read to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} ...) or otherwise make explicit that this operation is intended to be cross-tenant (with proper authorization).
- **server/seed-cleanup.ts:15** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks tenant_id constraint — will disable delegation tasks across all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the operation is intentionally global and authorized.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' is not tenant-scoped.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or confirm that a global scan is intended and safe for multi-tenant data.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true does not include tenant_id in WHERE — affects all tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id or otherwise restrict it to the intended administrative tenant.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) performs an unscoped read across tenant-scoped table.
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) in the WHERE clause or implement an explicit global-admin authorization and comment to justify cross-tenant scope.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true lacks tenant scoping and will update rows across tenants.
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or otherwise validate intended tenant scope) to the WHERE clause to prevent cross-tenant changes.
- **server/seed.ts:2163** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only id and no tenant_id constraint — can modify records belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or otherwise verify the record belongs to the intended tenant before updating.
- **server/seed.ts:2451** — UPDATE projects SET primary_conversation_id = ... WHERE id = 15 ... executes against a likely tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Add a tenant constraint to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to avoid modifying projects belonging to other tenants.
- **server/seed.ts:2516** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (...) queries the conversations table (tenant-scoped) without any tenant_id constraint, potentially exposing cross-tenant data and allowing inserts based on other tenants' rows.
  - _Fix:_ Scope the query to the caller's tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) and include tenant checks in the NOT EXISTS subquery as appropriate to ensure all referenced rows belong to the same tenant.
- **server/seed.ts:3356** — Unscoped read from tenant-scoped table `heartbeat_tasks` (db.select().from(heartbeatTasks) with no tenant_id filter). This reads rows across all tenants.
  - _Fix:_ Restrict the SELECT to the caller tenant (e.g. .where(eq(heartbeatTasks.tenantId, tenantId)) or add WHERE tenant_id = <tenantId>) so the seed logic only inspects the intended tenant's tasks.
- **server/seed.ts:3854** — UPDATE on tenant-scoped table `memory_entries` lacks tenant restriction: the normalizer `UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND fact LIKE ...` will modify rows for every tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = <tenantId>) or run the update separately per tenant to avoid cross-tenant modifications.
- **server/seed.ts:3880** — UPDATE on tenant-scoped table `conversations` without tenant filter: `UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'` updates conversations across all tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = <tenantId>) or iterate per-tenant to perform the backfill only within the intended tenant.
- **server/seed.ts:4026** — INSERT into `project_notes` does not set tenant_id. Tenant-scoped tables require tenant_id to be provided on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., <tenantId>)) or derive/validate tenant ownership and set tenant_id explicitly before inserting.
- **server/self-improvement.ts:170** — detectStagnation performs a SELECT on the tenant-scoped `experiments` table but only conditionally includes tenant_id. If tenantId is not provided the query will return rows across all tenants (cross-tenant data exposure).
  - _Fix:_ Fail closed or require a tenantId parameter. Always include a tenant_id equality constraint in the WHERE clause for tenant-scoped tables (e.g. throw if tenantId is missing, or require the caller to pass tenantId and use eq(experiments.tenantId, tenantId)).
- **server/self-improvement.ts:511** — getExperimentHistory can return experiments without a tenant filter when tenantId is omitted — it issues an unscoped SELECT FROM experiments, allowing cross-tenant reads.
  - _Fix:_ Require a tenantId (fail if missing) or always include WHERE tenant_id = <caller tenant>. Do not return global experiment rows when tenantId is absent. If global queries are needed, use a dedicated global table marked as such.
- **server/skillify.ts:541** — Passing a caller-supplied personaId into storage.createSkill() with no ownership check. The personaId parameter to skillifyConversation is accepted from the caller and is written into the created skill without verifying the persona belongs to the caller's tenant, enabling cross-tenant linkage or pollution.
  - _Fix:_ Validate that personaId belongs to the tenant before using it in writes. Either verify via a SELECT with tenant_id = tenantId (or a helper assertPersonaInTenant(personaId, tenantId)), or refuse/clear the personaId when it's invalid. Ensure any INSERT/UPDATE that references another entity enforces tenant ownership in application logic before performing the write.
- **server/storage.ts:242** — getConversationUnscoped performs a SELECT FROM conversations without constraining by tenant_id (no WHERE tenant_id = ...). This is an unscoped read against a tenant-scoped table.
  - _Fix:_ Remove or restrict this unscoped accessor. If an unscoped fetch is truly required, make it private/internal, require an explicit admin/ALL_TENANTS guard, and emit a strong audit/log + callsite-level justification; otherwise require and enforce a tenantId parameter and include eq(conversations.tenantId, tenantId) in the WHERE clause.
- **server/storage.ts:285** — attachCitationMetadata issues a raw SELECT FROM messages WHERE id IN (...) without any tenant_id constraint. This reads message rows without enforcing tenant ownership.
  - _Fix:_ Require and pass a tenantId into attachCitationMetadata (or accept messages already scoped) and include a tenant filter (e.g. AND tenant_id = ${tenantId} or join to conversations to validate tenant). Avoid global SELECTs on tenant-scoped tables.
- **server/storage.ts:309** — createMessage selects the conversation row by id with db.select(...).where(eq(conversations.id, data.conversationId)) with NO tenant filter. Caller-supplied conversationId is used to derive tenantId without first verifying the caller's tenant context.
  - _Fix:_ Require the caller's tenant context (tenantId) and use a tenant-scoped lookup (e.g. where(and(eq(conversations.id, id), eq(conversations.tenantId, tenantId))) or call getConversation(id, tenantId)) so only the owner's conversation can be read and used to assign message. Failing closed is preferred.
- **server/storage.ts:298** — attachCitationMetadata uses db.execute(sql`SELECT ... FROM messages WHERE id IN (...) ...`) and later uses db.execute(sql`UPDATE messages SET citations = ... WHERE id = ${messageId}`) with no tenant_id constraint on messages. Raw SQL updates/reads on tenant-scoped messages without tenant scoping risk cross-tenant modification.
  - _Fix:_ Include tenant_id predicates on both SELECT and UPDATE paths (or pass tenantId into this function and validate it). Prefer Drizzle parameterized queries with explicit .where(eq(messages.tenantId, tenantId)) to make tenant scoping explicit and auditable.
- **server/storage.ts:512** — getAllMemoriesForBackup executes db.select(...).from(memory_entries) without any tenant filter; it returns memory entries across all tenants.
  - _Fix:_ Either restrict this API to an authenticated admin path and require an explicit ALL_TENANTS sentinel, or make it tenant-scoped by requiring tenantId and adding eq(memory_entries.tenant_id, tenantId). Document and gate any cross-tenant backup/export carefully.
- **server/storage.ts:395** — attachCitations issues a raw UPDATE messages SET citations = ... WHERE id = ${messageId} with NO tenant check. This allows citations to be written to message rows without validating tenant ownership.
  - _Fix:_ Require tenantId for attachCitations or use conversation → tenant join to ensure the update includes AND tenant_id = ${tenantId}. Use Drizzle .where(eq(messages.tenantId, tenantId)) or include the tenant condition in the raw SQL.
- **server/storage.ts:805** — updateMemoryEmbedding does db.update(memory_entries).set({ embedding }).where(eq(memory_entries.id, id)) with NO tenant constraint. This updates memory rows by id without ensuring tenant ownership.
  - _Fix:_ Require and pass tenantId into this function (or validate ownership before updating), and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause so embeddings cannot be written across tenants.
- **server/storage.ts:817** — updateKnowledgeEmbedding updates agent_knowledge via db.update(...).where(eq(agent_knowledge.id, id)) with NO tenant constraint, allowing cross-tenant mutation of knowledge embeddings by id.
  - _Fix:_ Require tenantId and include eq(agent_knowledge.tenant_id, tenantId) in the WHERE clause (or use the existing updateKnowledge pattern) so only the owning tenant's row can be modified.
- **server/storage.ts:841** — archiveExpiredMemories executes a raw UPDATE memory_entries SET status = 'archived' WHERE ... RETURNING id with NO tenant constraint; this will archive active memories across all tenants.
  - _Fix:_ If this is intended to run per-tenant, accept a tenantId (or ALL_TENANTS sentinel) and include the tenant predicate. If intended to be global, ensure only a fully-authorized admin/background worker can run it and log/audit the action. Prefer per-tenant processing where feasible.
- **server/storage.ts:857** — archiveStaleMemories runs a raw UPDATE memory_entries SET status = 'archived' WHERE status='active' AND created_at <= ${cutoff} ... with NO tenant filter — it will touch rows across tenants.
  - _Fix:_ Same as above: make this per-tenant with an explicit tenantId or gate the operation to an admin-only context and document that it is cross-tenant. Add eq(memory_entries.tenant_id, tenantId) to the WHERE clause when tenant-scoped behavior is desired.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding executes UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL — there is NO tenant_id constraint. stampArgsEmbedding lacks tenant context and can update another tenant's row by id.
  - _Fix:_ Pass tenantId into stampArgsEmbedding (or otherwise validate ownership) and add AND tenant_id = ${tenantId} to the WHERE clause. Ensure any lazy backfill/embedding writes include tenant scoping so updates cannot cross tenants.
- **server/surprise-scorer.ts:139** — scoreProposalSurprise updates felix_proposals actual_outcome_embedding using WHERE id = ${proposalId} without a tenant check (UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId}). This writes to a tenant-scoped table by id only.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) on the UPDATE. Ensure all mutation SQL against felix_proposals includes tenant_id checks or is authorized admin-only and audited.
- **server/surprise-scorer.ts:212** — scoreProposalSurprise updates surprise_score and surprise_band via UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} with NO tenant predicate.
  - _Fix:_ Restrict the UPDATE by tenant (AND tenant_id = ${tenantId}) or otherwise enforce that operations that mutate proposal-scoped data require explicit tenant validation. Avoid updating by id alone on tenant-scoped tables.
- **server/tenant-fork.ts:226** — UPDATE performed against a tenant-scoped table without constraining by tenant_id (update(entry.table).where(eq(entry.table.id, p.newId))).
  - _Fix:_ Include the tenant filter in the WHERE clause (eg. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId)))) or otherwise assert ownership within the transaction before updating to ensure the update cannot touch rows belonging to another tenant.
- **server/tool-learning.ts:155** — SELECT by id against tenant-scoped table custom_tools lacks a tenant_id constraint (.where(eq(customTools.id, inserted.id))).
  - _Fix:_ Constrain reads to the tenant (eg. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) or fetch and assert the tenantId matches before proceeding.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table custom_tools updates implementation using only id in WHERE (db.update(customTools).where(eq(customTools.id, tool.id))) with no tenant_id check.
  - _Fix:_ Include tenant scoping in the WHERE clause (eg. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or verify the row's tenant ownership before performing the update to prevent cross-tenant modifications.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table custom_tools deletes by id without constraining by tenant_id (db.delete(customTools).where(eq(customTools.id, tool.id))).
  - _Fix:_ Restrict DELETEs by tenant (eg. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or verify ownership before deleting to avoid accidental/attacker-driven deletion of another tenant's data.
- **server/tool-learning.ts:365** — UPDATE increments usageCount on custom_tools using only id in WHERE (db.update(customTools).where(eq(customTools.id, tool.id))) without a tenant_id constraint.
  - _Fix:_ Scope this update to the tenant (eg. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or check ownership first. All writes to tenant-scoped tables must include explicit tenant filters.
- **server/tools.ts:2878** — INSERT INTO project_conversations omits tenant_id when writing a tenant-scoped linkage row. This allows rows to be created with no tenant column set (or set implicitly by DB defaults), violating the invariant that INSERTs must pass tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT and set it to the caller's tenant (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId})). Also ensure ownership of both the project and conversation has been validated before inserting (or scope the insert via a SELECT ... WHERE ... AND tenant_id = ${tenantId}).
- **server/tools.ts:2981** — INSERT INTO project_files does not set tenant_id. Per invariant, every INSERT into tenant-scoped tables must explicitly set tenant_id; otherwise rows can be created without tenant ownership and enable cross-tenant contamination.
  - _Fix:_ Add tenant_id to the INSERT columns and pass the caller's tenant (e.g. INSERT INTO project_files (..., uploaded_by, tenant_id) VALUES (..., ${params.uploadedBy || 'agent'}, ${tenantId}) RETURNING *). Also ensure prior ownership checks include tenant scoping and that subsequent UPDATE/SELECTs filter by tenant_id.
- **server/tools.ts:2996** — INSERT INTO project_notes does not include tenant_id. This can persist notes that are not bound to the caller tenant, violating tenant isolation and the INSERT rule requiring tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author || 'agent'}, ${tenantId}) RETURNING *). Ensure the project ownership check is performed and that the table enforces tenant attribution at insert time in application code.
- **server/tools.ts:3020** — INSERT INTO project_conversations (linking in link_conversation) omits tenant_id. Even though conversation/project ownership is partially checked, the insert itself does not explicitly set tenant_id on the created row.
  - _Fix:_ Pass tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Ensure assertConversationInTenant and project ownership checks happen before the insert and remove any reliance on implicit FK semantics.
- **server/tools.ts:2915** — COUNT subqueries in the project list (SELECT COUNT(*) FROM project_files / project_notes / project_conversations WHERE project_id = p.id) do not include a tenant_id constraint. Reads against tenant-scoped tables must be restricted to the caller's tenant — a project_id alone is not a sufficient isolation guard without an explicit tenant_id check under the stated invariant.
  - _Fix:_ Constrain the subqueries to the tenant (for example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) ). Alternatively join to projects (or include p.tenant_id) to ensure the count only reflects rows in the same tenant.
- **server/tools.ts:3031** — Search query uses subqueries (COUNT(*) FROM project_files / project_notes WHERE project_id = p.id) without tenant_id filters. Under the project listing/search paths these reads can leak or count rows across tenants if tenant_id is not enforced in the subquery.
  - _Fix:_ Add tenant scoping to those subqueries (e.g. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) ). Ensure all per-project aggregates explicitly restrict by tenant_id.
- **server/tools.ts:2976** — Duplicate-check SELECT against project_files (dupCheck) does not include tenant_id. Even though the code checked project ownership earlier, the SELECT itself lacks tenant scoping and may match rows from other tenants with the same project_id if tenant enforcement relies on application-layer constraints.
  - _Fix:_ Include tenant_id in the duplication check (e.g. SELECT id FROM project_files WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND file_name = ${fname} AND ... LIMIT 1). Always include tenant_id on reads against tenant-scoped tables to satisfy the invariant.
- **server/tools.ts:4541** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code relies on assertProjectInTenant earlier but the INSERT statement omits an explicit tenant_id column/value.
  - _Fix:_ Include the tenant_id explicitly in the INSERT (and bind it as a parameter) — e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Do not rely solely on a prior ownership check; persist the tenant_id on the row so application-level isolation is explicit and future queries can filter by tenant_id.
- **server/tools.ts:4545** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checks assertProjectInTenant but the INSERT omits tenant_id in the column list/values.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and bind it) — e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${projectId}, ...). Ensure all project-scoped table writes store tenant_id so other application-level filters can reliably constrain by tenant.
- **server/tools.ts:6393** — INSERT into tenant-scoped table `project_files` (audio upload) does not include tenant_id. Although assertProjectInTenant was called, the INSERT itself omits tenant_id.
  - _Fix:_ Add tenant_id to the INSERT columns/values (e.g. tenant_id = ${params._tenantId}) and continue to verify project ownership prior to write. Persisting tenant_id on the row is required for consistent application-level tenant isolation.
- **server/tools.ts:6927** — INSERT into tenant-scoped table `project_files` (video upload) does not include tenant_id. The code verifies project ownership but the INSERT omits tenant_id.
  - _Fix:_ Modify the INSERT to include tenant_id (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Always supply tenant_id explicitly on tenant-scoped table writes to enforce application-level isolation.
- **server/tools.ts:6927** — INSERT into project_files does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) is executed without explicitly writing tenant_id even though project_files is tenant-scoped (and tenant row-level security is NOT enforced in PG).
  - _Fix:_ Include and set the tenant_id column explicitly in the INSERT (use the validated tenant id). Example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Prefer using Drizzle inserts (db.insert(...).values({ tenantId: params._tenantId, ... })) or a parameterized SQL template; do not rely only on an ownership check — the row must carry tenant_id.
- **server/tools.ts:8892** — UPDATE on proposed_skills lacks a tenant constraint. db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) updates a tenant-scoped table by id only, without ensuring the row belongs to the caller's tenant, enabling cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or include tenantId in the SQL WHERE clause). Always include the tenant equality check when updating/deleting tenant-scoped tables (use Drizzle's eq(table.tenantId, tenantId)).
- **server/tools.ts:8917** — UPDATE on proposed_skills lacks a tenant constraint. db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) modifies the proposal row without verifying tenant ownership.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure tid is the caller's validated tenant id and use parameterized Drizzle predicates rather than string interpolation.
- **server/tools.ts:8933** — UPDATE on proposed_skills lacks a tenant constraint. The code sets status='rejected' for a proposal by id only, without constraining by tenant_id, which can affect other tenants' rows.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or add AND tenant_id = ${tid} when using SQL templates). Always require tenant equality on updates/deletes against tenant-scoped tables.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE on tenant-scoped table `browser_workflows` lacks tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This write can affect rows outside the caller's tenant scope.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add tenant filtering in the WHERE clause, e.g. use the trusted tenantId value: await wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); or equivalent Drizzle .where(eq(browserWorkflows.tenantId, tenantId)) to ensure tenant isolation.
- **server/tools/domains/files/handlers.ts:907** — UPDATE on tenant-scoped table `projects` lacks tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). This write is not explicitly scoped to the caller tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Include tenant_id in the WHERE clause, e.g.: await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`); or use Drizzle's .where(eq(projects.tenantId, tenantId)) to enforce per-tenant isolation.
- **server/tools/domains/security/handlers.ts:163** — SELECT against provider_keys omits tenant constraint — query returns rows from provider_keys without WHERE tenant_id = ${tenantId} (scanDb.execute at line 163).
  - _Fix:_ Constrain queries on provider_keys to the caller tenant (e.g. add WHERE tenant_id = ${tenantId}). If the intent is an admin tenant to enumerate cross-tenant data, make that explicit and safe: either (a) require and assert an admin role and still prefer aggregated/filtered metrics rather than raw rows, or (b) limit results to admin-scoped audited APIs. Avoid broad unscoped SELECTs on tenant-scoped tables.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM provider_keys omits tenant constraint — query aggregates across all provider_keys without scoping to tenant (scanDb.execute at line 260).
  - _Fix:_ Restrict aggregation to the caller tenant (e.g. WHERE tenant_id = ${tenantId}) or, if this is intentionally an admin-only cross-tenant metric, gate it behind a documented admin check and/or compute per-tenant aggregates rather than a raw global scan.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE on tenant-scoped table memory_entries uses WHERE id = ${id} without also constraining tenant_id — this can update rows from other tenants if id is guessed or collisions occur (db.execute UPDATE at line 92).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}). Prefer including tenant_id in all WHERE clauses that touch tenant-scoped tables to enforce application-level isolation.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) lacks a tenant constraint — bulk update may affect rows belonging to other tenants (db.execute at line 148).
  - _Fix:_ Restrict the bulk update to the tenant's rows, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}, or filter ids to only those resolved for this tenant before issuing the UPDATE.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id} executed without tenant_id constraint (db.execute at line 242) — may modify other tenants' data.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}). As a general rule, every UPDATE/DELETE that touches tenant-scoped tables must include tenant_id = callerTenant to maintain isolation.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is performed without constraining by tenant_id. The code loads all pairing rows for a phone across all tenants (`WHERE category = 'messaging_pairing' AND title = ...`) and then attempts to filter/verify them in app logic — this violates the invariant that every read against a tenant-scoped table must include a tenant_id constraint.
  - _Fix:_ Avoid selecting tenant-global rows. Either store/look up pairings by tenant (include tenant_id in the WHERE), or if the intent is to discover contested pairings, make the query explicit and auditable (e.g. require operator/admin privilege), and ensure their use cannot leak tenant data. Prefer adding `AND tenant_id = ${callerTenantId}` (or equivalent Drizzle eq(table.tenantId, tenantId)) to the query, or refactor pairing storage to a global, explicitly-authorized mapping with proper access controls and explicit, audited cross-tenant checks.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` is performed by conversation id without constraining by the caller/expected tenant. The code queries the conversations table by id to verify the pairing, but the query does not include a tenant_id filter, so it reads a tenant-scoped table without the required tenant constraint.
  - _Fix:_ Constrain the query to the claimed tenant when verifying ownership, e.g. `SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1` (or equivalent Drizzle eq(table.tenantId, tenantId)). This makes the check atomic and avoids reading tenant-scoped rows outside the intended tenant context. If you must query by id first, perform an ownership-checked lookup that includes tenant_id in the WHERE clause before trusting the result.
- **server/video-job-runner.ts:206** — isCancelRequested performs a SELECT on tenant-scoped table `video_jobs` filtering only by jobId (eq(videoJobs.jobId, jobId)) and does NOT constrain by tenant_id. This lets callers probe/read cancel state for any job_id regardless of tenant ownership.
  - _Fix:_ Require and pass the caller's tenantId into isCancelRequested (or otherwise verify ownership) and add a tenant predicate to the query, e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId))). If this function must be able to run without tenant context, restrict its use to trusted/system-only code and document/guard that usage explicitly.
- **server/video-job-runner.ts:236** — recoverStaleVideoJobs runs an UPDATE against tenant-scoped `video_jobs` without any tenant_id constraint, marking any stale job across all tenants as failed. This mutates other tenants' rows purely based on age/status and lacks an ownership/tenant scope.
  - _Fix:_ If the intent is to run per-tenant, accept a tenantId and add a tenant predicate to the WHERE clause (e.g. AND eq(videoJobs.tenantId, tenantId)). If this is an administrative/system sweep that must act across tenants, make that explicit and ensure only trusted/admin contexts can call it (and document the justification). Consider adding telemetry/ACL checks and/or limiting scope to specific tenant lists rather than blanket updates.

### MEDIUM (30)

- **server/agentic-engines.ts:377** — SELECT name, role FROM personas WHERE is_active = true has no tenant constraint — may leak persona records across tenants.
  - _Fix:_ If personas are tenant-scoped, add WHERE tenant_id = ${tenantId} (or otherwise restrict by tenant). If personas are global by design, document that explicitly; otherwise enforce tenant ownership checks.
- **server/agent-channels.ts:118** — Queries that LEFT JOIN personas (to obtain persona name) do not constrain the personas table by tenant; joins like "LEFT JOIN personas p ON p.id = cm.from_persona_id" can return persona records from other tenants.
  - _Fix:_ Include tenant scoping in joins to tenant-scoped tables (e.g. LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = cm.tenant_id) or otherwise verify persona ownership before exposing persona data.
- **server/agent-activity.ts:60** — getPersonality selects from personas by id without tenant constraint (where(eq(personas.id, personaId))). If personas are tenant-scoped, this allows reading persona rows from other tenants.
  - _Fix:_ Require (or accept) tenantId in getPersonality and add a WHERE clause scoping personas by tenant_id = ${tenantId}. If personas are global, document that explicitly; otherwise enforce ownership checks before returning persona data.
- **server/agent-desk.ts:297** — getDesksOverview performs "SELECT id, name FROM personas" with no tenant filter — may return personas from other tenants.
  - _Fix:_ Filter personas by tenant (e.g. WHERE tenant_id = ${tenantId}) or load only personas referenced by the tenant's desks. Ensure every read of personas enforces tenant ownership.
- **server/auto-transcript.ts:128** — Backfill query selects conversations across all tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...) without scoping to a tenant for per-tenant processing. While tenant_id is returned and later used, this is a global scan of a tenant-scoped table with no WHERE tenant_id filter.
  - _Fix:_ If backfill is intended to operate per-tenant, add a tenant_id filter (WHERE c.tenant_id = ${tenantId}) or run the backfill explicitly per-tenant. If it must scan all tenants, ensure every downstream DB write/read is strongly guarded by the tenant_id you obtained and document the admin-level nature of this job.
- **server/chat-engine.ts:4214** — getOrCreateTaskState(conversationId, ...) is called without an explicit tenant parameter. If the helper reads/writes a per-conversation task state table, relying on conversationId alone may allow cross-tenant access unless ownership is verified internally.
  - _Fix:_ Pass tenantId into getOrCreateTaskState or verify the conversation belongs to the caller's tenant before calling. Ensure DB reads/updates use WHERE tenant_id = tenantId and that any INSERTs set tenant_id explicitly.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken selects from dfy_intake_forms by token without any tenant_id constraint. A token-only lookup is effectively a cross-tenant access to a tenant-scoped table.
  - _Fix:_ If the token is intended to be the sole authorization factor, document that explicitly and ensure token space is sufficiently unguessable. Otherwise, scope the query by tenant_id (or return the tenant alongside the form and validate ownership before returning sensitive fields). Consider adding auditing for token-based retrievals.
- **server/firecrawl.ts:214** — UPDATE of scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) but does not include tenant scoping. Although the prior SELECT filtered by tenant, the update itself is not tenant-constrained.
  - _Fix:_ Include tenant_id in the update WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or reuse the same multi-column predicate used for the initial SELECT to ensure the write is scoped to the caller's tenant.
- **server/firecrawl.ts:343** — In the crawl loop, UPDATE of scrapedPages uses .where(eq(scrapedPages.id, existing[0].id)) without also constraining tenant_id. This can modify rows belonging to other tenants if an id is manipulated/misresolved.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or construct the update predicate from the original tenant-qualified select.
- **server/glasses-gateway.ts:172** — Update to apiKeys (setting lastUsedAt) is executed with WHERE eq(apiKeys.id, key.id) but does not also include the api key's tenant context. If apiKeys is tenant-scoped, this write lacks an explicit tenant constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or otherwise ensure the update is executed in the same tenant-scoped transaction/context that was used to fetch the key.
- **server/lib/stuck-task-sweep.ts:56** — SELECT reads from tenant-scoped table `agent_runs` without a tenant_id filter (SELECT ... FROM agent_runs WHERE status = 'running' ...). This returns rows across tenants and exposes tenant_id and other row details in the audit output.
  - _Fix:_ If this should be tenant-scoped, add WHERE tenant_id = ${tenantId} (or accept a tenantId input). If it is intentionally a global maintenance scan, make that explicit (admin-only) and ensure callers are authorized and aware that results include multiple tenants.
- **server/lib/stuck-task-sweep.ts:84** — SELECT reads from tenant-scoped table `mind_tickets` without a tenant_id filter (SELECT ... FROM mind_tickets WHERE status = 'in_progress' ...). This returns rows across tenants and exposes tenant_id and details.
  - _Fix:_ Add an explicit tenant_id WHERE clause when the scan should be scoped, or document/guard the global scan as an admin operation and ensure proper authorization and auditing if cross-tenant access is required.
- **server/lib/system-probes.ts:163** — COUNT query against tenant-scoped `messages` table is executed without filtering by tenant_id (db.select({ count: ... }).from(messagesTable)), producing global message counts across tenants.
  - _Fix:_ If the probe is intended to be tenant-scoped, add WHERE tenant_id = ${tenantId}. If a global count is required for admin/system diagnostics, make the cross-tenant nature explicit and ensure only authorized admin contexts can run it; prefer using a clearly-named admin API or parameter to avoid accidental cross-tenant reads.
- **server/model-catalog.ts:709** — runCatalogSync defaults opts.tenantId to admin tenant (1) when tenantId is omitted and then performs memory_entries SELECT/INSERT under that tenant. Silent defaulting risks misattributing alerts/notes to admin tenant.
  - _Fix:_ Require callers to pass an explicit tenantId (fail-closed) for catalog-sync writes/alerts. If a default is necessary for admin tooling, make it explicit in the API and restrict use to internal processes; log and/or throw when external callers omit tenantId.
- **server/research-engine.ts:495** — INSERT INTO project_notes (project_id, note, author, created_at) is executed without passing tenant_id. If project_notes is a tenant-scoped table (tenant_id NOT NULL), this will create rows with no tenant ownership and may misattribute or fail.
  - _Fix:_ Include tenant_id explicitly in the INSERT (VALUES (${projectId}, ${note}, ${'Research Engine'}, NOW(), ${session.tenantId}) or add tenant_id column to the column list). If project_notes is keyed by project_id only, ensure the project ownership was verified (assertProjectInTenant was called earlier) and still prefer storing tenant_id to make tenant ownership explicit.
- **server/research-engine.ts:503** — Looped INSERT INTO project_notes (project_id, note, author, created_at) also omits tenant_id when creating individual finding notes — same misattribution risk as above.
  - _Fix:_ Same fix as above: include tenant_id in each INSERT into project_notes, or confirm the table is intentionally not tenant-scoped. Prefer passing tenant_id explicitly (VALUES (..., ${projectId}, ${noteContent}, ${'Research Engine'}, NOW(), ${session.tenantId})).
- **server/routes.ts:6539** — storage.createHeartbeatTask(rest) is called in the admin /api/import handler without an explicit tenant_id. If heartbeat tasks are tenant-scoped, inserting without tenant_id may create tasks not tied to the intended tenant.
  - _Fix:_ Ensure tenantId is set explicitly when creating heartbeat tasks during import (e.g. storage.createHeartbeatTask({ ...rest, tenantId: <targetTenant> })), or validate whether the task is intended to be global. Require explicit tenant mapping for tenant-scoped resources.
- **server/routes/oauth-subscriptions.ts:325** — cronRefreshHandler heal step runs: SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE — reads tenant-scoped table across all tenants without per-tenant constraint.
  - _Fix:_ If this global heal is intentional, ensure the handler is strongly authenticated/authorized (it already uses CRON_SECRET) and audit the operation. Otherwise restrict the query to explicit tenant(s) or move global behavior to a dedicated platform-admin-only utility with clear justification. Consider making tenant scope explicit in code rather than selecting all tenant_ids.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions executed without a tenant_id filter on a tenant-scoped table.
  - _Fix:_ Constrain the count to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise explicitly scope the query to the intended tenant. Do not query tenant-scoped tables across tenants without an explicit tenant filter.
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Add a tenant filter (e.g. WHERE is_active = true AND tenant_id = ${tenantId}) so only rows for the caller's tenant are returned.
- **server/seed.ts:4093** — UPDATE on tenant-scoped table `agent_knowledge` uses only id in WHERE clause (`WHERE id = ${rows[0].id}`) with no explicit tenant_id constraint.
  - _Fix:_ Add an explicit tenant_id restriction to the UPDATE (e.g. AND tenant_id = <tenantId>) or use a parametrized Drizzle .where(eq(agentKnowledge.tenantId, tenantId), eq(agentKnowledge.id, id)) to ensure the row belongs to the caller tenant.
- **server/self-improvement.ts:394** — runExperiment updates the `experiments` table using only eq(experiments.id, experiment.id) (lines ~394-401 and similarly in the error handler at ~413-415) and does not include a tenant_id constraint. Updating by id alone violates the requirement that writes must be constrained to the caller's tenant.
  - _Fix:_ Include the tenant constraint on UPDATE statements (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) or otherwise verify the row belongs to the caller's tenant before updating. Make this invariant-wide (all updates) rather than relying on provenance of the id.
- **server/self-reflection.ts:366** — storeLesson issues an UPDATE on memory_entries using WHERE id = <existing id> without also constraining tenant_id. Although the id came from a prior tenant-scoped SELECT, updates must still explicitly constrain tenant_id to prevent accidental cross-tenant modification.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause (or use a combined WHERE with both id and tenant_id). Adopt a pattern that always includes tenant_id on UPDATE/DELETE against tenant-scoped tables.
- **server/skillify.ts:221** — Querying the `personas` table by persona ID with no tenant constraint. The code reads personas using conv[0].personaId without verifying that the persona row belongs to the same tenant as the conversation, allowing a cross-tenant read if persona rows are tenant-scoped.
  - _Fix:_ Constrain the personas lookup to the conversation's tenant (or otherwise validate ownership) before reading. Example: include a tenant predicate when selecting personas (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, conv[0].tenantId)))) or call a reusable guard (assertPersonaInTenant) that returns false/throws for mismatches.
- **server/storage.ts:342** — upsertMessageFeedback reads messages via db.select(...).from(messages).where(eq(messages.id, data.messageId)) with NO tenant_id constraint. The code later compares the fetched row's tenantId to data.tenantId, but the initial lookup is unscoped.
  - _Fix:_ Scope the initial SELECT by tenant (or by joining to conversation) so the DB enforces the tenancy check, e.g. where(and(eq(messages.id, messageId), eq(messages.tenantId, data.tenantId))). Do not rely solely on comparing a caller-provided tenantId after an unscoped fetch.
- **server/storage.ts:826** — getMemoriesWithoutEmbeddings selects from memory_entries WHERE status='active' AND embedding IS NULL with NO tenant constraint. This returns candidate rows across tenants for embedding processing.
  - _Fix:_ Make this function tenant-scoped (require tenantId) or require an explicit ALL_TENANTS sentinel for admin/batch jobs, and ensure appropriate authorization/auditing. Add eq(memory_entries.tenant_id, tenantId) to the WHERE clause if per-tenant processing is required.
- **server/storage.ts:831** — getKnowledgeWithoutEmbeddings queries agent_knowledge WHERE notExpired AND embedding IS NULL with NO tenant filter — it will return knowledge rows across tenants.
  - _Fix:_ Require tenantId or an explicit ALL_TENANTS sentinel and ensure only authorized background jobs can execute cross-tenant queries. For tenant-scoped usage add eq(agent_knowledge.tenant_id, tenantId) to the WHERE clause.
- **server/storage.ts:942** — getHeartbeatTask selects heartbeat_tasks WHERE id = ${id} with NO tenant predicate. heartbeat_tasks is tenant-scoped and this read is unscoped.
  - _Fix:_ If callers need to fetch a task by id, either accept tenantId and add eq(heartbeat_tasks.tenantId, tenantId) OR otherwise validate ownership before returning the row. If the function must be unscoped, document & audit its privileged usage and restrict it to trusted internal callers.
- **server/storage.ts:994** — getDueHeartbeatTasks runs a raw SELECT FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() without tenant scoping. This returns tasks across tenants for scheduling/claiming.
  - _Fix:_ If the scheduler must process tasks across tenants, require an explicit ALL_TENANTS sentinel and ensure the caller is a privileged scheduler; otherwise accept a tenantId and scope via eq(heartbeat_tasks.tenant_id, tenantId). Add clear authorization & auditing around cross-tenant scheduling.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table custom_tools uses only name equality and does not include a tenant_id constraint (db.select().from(customTools).where(eq(customTools.name, parsed.name))).
  - _Fix:_ Add an explicit tenant filter (eg. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or otherwise validate the row's tenant before relying on the result. Even if names are namespaced, an explicit tenant check enforces the tenant-isolation invariant.

### LOW (3)

- **server/research-engine.ts:1081** — resolvePersonaId executes SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 without filtering by tenant_id even though the function signature receives a _tenantId parameter. If personas are tenant-scoped, this is a cross-tenant read.
  - _Fix:_ If personas are tenant-scoped, add AND tenant_id = ${tenantId} to the query (and pass the tenantId through). If personas are intentionally global/shared, add a code comment documenting that personas are global and that omitting tenant scoping is deliberate.
- **server/routes.ts:1533** — Public stats endpoint issues SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) without tenant_id constraints, returning global per-table aggregates across tenants.
  - _Fix:_ If global aggregates are acceptable, restrict this endpoint to platform-admins or explicitly document the privacy implications. Alternatively provide tenant-scoped stats by requiring an authenticated tenant and adding WHERE tenant_id = <tenantId>, or compute aggregates in a controlled, audited admin-only background job so per-tenant data isn't read ad-hoc by a public handler.
- **server/routes.ts:6490** — storage.createPersona(rest) in the admin /api/import handler is invoked without explicitly setting tenant_id. Depending on whether personas are tenant-scoped or global, this may create tenant-scoped rows from untrusted import data or rely on imported tenant fields without validation.
  - _Fix:_ If personas are tenant-scoped, require and set tenantId explicitly during import (storage.createPersona({ ...rest, tenantId: <targetTenant> })) and validate imported tenant fields. If personas are global, document that behavior and ensure inserts cannot accidentally create tenant-specific records without explicit tenant mapping.

### Suppressed — confirmed false positives (226)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights (autoApplyEligibleInsights) runs without a tenant_id constraint — this will change rows across tenants (status → 'applied').
  - _Why false positive:_ Platform-wide backfill intentionally sweeps all tenants; returned rows carry tenant_id and follow-on routing uses each row's own tenant.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals runs a bulk UPDATE on agentApprovals without constraining by tenant_id (WHERE status = 'pending' AND expiresAt < ...). This updates approvals across all tenants and then attempts per-run updates, risking cross-tenant state changes.
  - _Why false positive:_ Expiry sweep intentionally updates stale pending approvals across tenants and only performs housekeeping/status changes with per-row tenantId used for linked run updates.
- **server/agentic/runs.ts:39** _(internal-worker-pk)_ — update(agentRuns).set(...).where(eq(agentRuns.id, runId)) — appendStep updates agent_runs without any tenant_id constraint. This allows modifying run rows across tenants.
  - _Why false positive:_ Worker helper updates agent_runs by server-generated global runId primary key; no untrusted request boundary shown for runId in this internal path.
- **server/agentic/runs.ts:51** _(internal-worker-pk)_ — update(agentRuns).set(...).where(eq(agentRuns.id, runId)) — updateRunState updates run state without tenant filtering, enabling cross-tenant writes.
  - _Why false positive:_ Internal run-state helper writes by server-generated global runId primary key, consistent with worker-owned agent_runs lifecycle.
- **server/agentic/runs.ts:55** _(internal-worker-pk)_ — update(agentRuns).set({ state }) .where(eq(agentRuns.id, runId)) — replaceRunState updates run rows without tenant constraint.
  - _Why false positive:_ Internal helper replaces state on agent_runs by server-generated primary key, not shown as caller-controlled cross-tenant input.
- **server/agentic/runs.ts:64** _(internal-worker-pk)_ — update(agentRuns).set({ status:'completed', ... }).where(eq(agentRuns.id, runId)) — completeRun writes to agent_runs without tenant scoping.
  - _Why false positive:_ Run completion is an internal worker transition on agent_runs addressed by server-created runId primary key.
- **server/agentic/runs.ts:94** _(internal-worker-pk)_ — update(agentRuns).set({ status:'failed', ... }).where(eq(agentRuns.id, runId)) — failRun updates agent_runs without tenant filtering.
  - _Why false positive:_ Run failure transition is an internal server-side update keyed by globally unique runId, matching worker-pk benign pattern.
- **server/agentic/runs.ts:102** _(internal-worker-pk)_ — update(agentRuns).set({ status:'paused', ... }).where(eq(agentRuns.id, runId)) — pauseRun updates run rows with no tenant constraint.
  - _Why false positive:_ Pause helper is an internal agent_runs lifecycle update by server-generated runId primary key.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE of tenant-scoped table agent_wake_schedules performs a claimed-rows UPDATE via a subquery that does not constrain by tenant_id. The statement (UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...)) can move rows for any tenant into 'firing', causing cross-tenant state changes and event emission.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due wake rows across tenants, then processes each row with its own tenant_id and emits per-tenant events.
- **server/commitments.ts:136** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` returns tenant-scoped rows (including `description`) without any `WHERE tenant_id = ...` filter — reads overdue commitments across all tenants.
  - _Why false positive:_ Platform-wide overdue scanner processes rows with each row's own tenantId and emits only redacted admin digest data, not tenant-facing mixed content.
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT from agent_knowledge in backfillMissingKnowledgeEmbeddings has NO tenant_id constraint. This reads rows across all tenants and then writes back via storeEmbeddingVec (which itself updates without tenant checks), enabling cross-tenant data access and modification.
  - _Why false positive:_ Boot/maintenance backfill intentionally scans rows across tenants and only updates housekeeping embedding fields per row; no tenant data is returned or mixed into one response.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT from memory_entries in backfillEmbeddingVecs lacks a tenant_id constraint (SELECT id, embedding FROM memory_entries ...). The subsequent UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} also omits tenant_id, enabling cross-tenant writes.
  - _Why false positive:_ Cross-tenant embedding-vector backfill is a maintenance sweep that mutates only housekeeping/search-vector fields per row.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT from agent_knowledge in backfillEmbeddingVecs has NO tenant_id constraint and the later UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} also omits tenant filtering, allowing cross-tenant updates.
  - _Why false positive:_ Cross-tenant knowledge embedding-vector backfill is a maintenance sweep mutating only embedding_vec per row, with no tenant-facing data fanout.
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} is executed without a tenant_id constraint. This can update a schedule belonging to another tenant if an id is reused or maliciously supplied.
  - _Why false positive:_ Heartbeat scheduler is a platform-wide cron loop updating housekeeping next_run_at for the schedule row it just processed, using the row-carried tenant context elsewhere.
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id} has no tenant_id constraint. This updates schedule rows without verifying tenant ownership.
  - _Why false positive:_ Platform maintenance loop advances last_run_at/next_run_at on the current schedule row only; housekeeping mutation, not tenant-facing data access.
- **server/heartbeat.ts:113** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} is performed without tenant constraint, allowing cross-tenant modification.
  - _Why false positive:_ Platform-wide scheduler updates next_run_at as housekeeping on the processed row, with no cross-tenant content mixing or exposure.
- **server/heartbeat.ts:121** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} lacks a tenant_id filter and may modify another tenant's schedule row.
  - _Why false positive:_ Recovery path in the scheduler loop only advances next_run_at for housekeeping on the current row; not a tenant-facing cross-tenant access path.
- **server/heartbeat.ts:273** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} is executed without constraining by tenant_id. This directly modifies a tenant-scoped table row without verifying tenant ownership.
  - _Why false positive:_ Startup cleanup sweeps heartbeat_tasks across tenants and normalizes only housekeeping/config field model on each processed row.
- **server/heartbeat.ts:277** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} runs with no tenant_id filter and may modify tasks belonging to other tenants.
  - _Why false positive:_ Boot-time heartbeat startup cleanup intentionally sweeps all heartbeat_tasks and only normalizes housekeeping config fields by row; no tenant data is returned or mixed.
- **server/heartbeat.ts:2764** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause. This can change approval_status on a task that belongs to another tenant.
  - _Why false positive:_ newTask.id comes directly from storage.createHeartbeatTask with the same tenantId in the same call path, so the immediate id-only update targets the row just created.
- **server/heartbeat.ts:3216** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) lacks a tenant_id constraint, allowing approval_status changes across tenants.
  - _Why false positive:_ newTask.id is the freshly created heartbeat_task for the provided tenantId in the same function, so the follow-on update is on that verified row, not an untrusted cross-tenant id.
- **server/job-queue.ts:98** _(system-sweep)_ — UPDATE agent_jobs ... (claimDueJobs) performs a global UPDATE/SELECT on the tenant-scoped agent_jobs table without any tenant_id constraint. This can move jobs across tenants into 'running' state and allow cross-tenant job claiming/modification.
  - _Why false positive:_ Global worker claim loop is intentional queue processing across tenants; each claimed row carries its own tenant_id and this step only changes job state/lease, not tenant-facing data.
- **server/job-queue.ts:134** _(system-sweep)_ — UPDATE agent_jobs ... (reclaimExpiredLeases step 1) updates running jobs to failed_terminal without a tenant_id constraint. This mutates terminal state across all tenants.
  - _Why false positive:_ Expired-lease reclaim is platform-wide housekeeping that only mutates status/error/lease fields on each job row and does not expose or mix tenant content.
- **server/job-queue.ts:148** _(system-sweep)_ — UPDATE agent_jobs ... (reclaimExpiredLeases step 2) moves expired 'running' jobs back to pending, but does so globally with no tenant_id filter, affecting all tenants.
  - _Why false positive:_ Global requeue of expired leases is queue housekeeping across tenants, with per-row state changes only and no tenant-facing data aggregation.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — UPDATE agent_jobs ... (completeJob) marks a job succeeded by id but does not constrain the update to the job's tenant_id. A stale/forged id could allow completing another tenant's job.
  - _Why false positive:_ completeJob is a server-internal worker transition by globally unique job id plus attempt fencing; the id comes from prior claimDueJobs, not a request boundary.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — SELECT max_attempts FROM agent_jobs WHERE id = ${id} (failJob) reads job metadata without scoping by tenant_id. An attacker or bug could infer/affect other tenants' jobs.
  - _Why false positive:_ failJob is an internal worker path using a claimed job's server-sourced primary key/attempts, so the id-only metadata read is not from untrusted tenant input.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — UPDATE agent_jobs ... (failJob terminal path) updates status and other columns for the job id without constraining by tenant_id. This can dead-letter jobs across tenants.
  - _Why false positive:_ Terminal fail transition is a server-internal worker update on a claimed job id with attempt fencing, not a cross-tenant request-driven lookup.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — UPDATE agent_jobs ... (failJob non-terminal retry path) re-schedules a job by id but does not include tenant_id in the WHERE clause. This permits cross-tenant rescheduling/manipulation.
  - _Why false positive:_ Retry-path fail update is likewise an internal worker state transition keyed by server-claimed job id and attempts, not untrusted input.
- **server/lib/outcome-promotion.ts:159** _(system-sweep)_ — scanOutcomePromotions() runs a SELECT against tenant-scoped table deliverable_grade_events without constraining by tenant_id (missing WHERE tenant_id = ...). This reads aggregated per-tenant rows across ALL tenants, violating the app-only tenant-isolation invariant.
  - _Why false positive:_ nightly optimizer scans all tenants intentionally, groups per tenant_id, and writes per-tenant eval files without returning mixed tenant data to a tenant-facing surface
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT in CTE pulls rows from tenant-scoped table `scheduled_posts` without filtering by tenant_id (WITH due AS (SELECT id FROM scheduled_posts ... FOR UPDATE)). This locks and updates rows across all tenants in the subsequent UPDATE, enabling cross-tenant row modification/locking.
  - _Why false positive:_ heartbeat runner intentionally claims due scheduled_posts across tenants and processes each returned row with its own tenant_id
- **server/minerva-planner.ts:472** _(upstream-verified)_ — Rollback UPDATE on plans (resetting decision metadata) omits tenant_id in the WHERE clause. This can modify plans across tenants and corrupt other tenants' workflow/audit state.
  - _Why false positive:_ rollback uses args.planId for the same row previously loaded/transitioned in this call path; row.tenant_id is already established upstream
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` without any tenant_id filter (global read of active subscriptions). This reads potentially sensitive tokens across tenants.
  - _Why false positive:_ startup validation is a platform-wide maintenance pass over subscriptions, handling each row by its own tenant_id
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh selects all rows FROM `oauth_subscriptions` WHERE is_active = TRUE with no tenant_id filter — iterates across tenants and will access encrypted tokens for all tenants.
  - _Why false positive:_ proactive token refresh is a cross-tenant operational sweep that iterates rows individually by tenant_id/provider
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant filtering — enumerates tenants and allows cross-tenant operations.
  - _Why false positive:_ force-refresh is a cron-style maintenance loop over active subscriptions, processing each subscription separately by tenant_id
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — UPDATE plans ... WHERE id = ${planId} (appendExecutionLog) updates a tenant-scoped table without constraining by tenant_id — allows modifying another tenant's plan if an attacker supplies an id.
  - _Why false positive:_ internal executor updates plans by server-invoked globally unique planId, not directly from an untrusted request boundary in this helper
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE on plans (claim step) uses WHERE id = ${planId} AND status = 'approved' but omits tenant_id — can claim/transition plans belonging to other tenants.
  - _Why false positive:_ claim step is in internal server worker executePlan(planId), using server-side plan primary key rather than tenant input from a request boundary
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} (no tenant filter) — mutates plan state without tenant scoping.
  - _Why false positive:_ same call path just claimed and returned the target row by planId; subsequent failure update applies to that already-fetched plan row
- **server/plan-executor.ts:728** _(upstream-verified)_ — Final UPDATE to set plan status (completion) uses WHERE id = ${planId} without tenant_id — writes to another tenant's plan record possible.
  - _Why false positive:_ final status update occurs after the plan row was claimed and loaded earlier in executePlan, so ownership/identity is established in the same call path
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-handling UPDATE that flips executing→failed uses WHERE id = ${planId} AND status = 'executing' but omits tenant_id — may mark other tenants' plans as failed.
  - _Why false positive:_ planId is a server-side worker primary key; update by unique id in executor catch path, not from a tenant request boundary
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — this resets plans across ALL tenants (no tenant filter).
  - _Why false positive:_ boot-time recovery intentionally sweeps stale plans across tenants and only resets status/updated_at, returning ids for internal restart bookkeeping
- **server/plan-executor.ts:787** _(system-sweep)_ — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' LIMIT 50 — selects approved plans across all tenants (no tenant filter) and requeues them, allowing cross-tenant execution.
  - _Why false positive:_ boot-time recovery intentionally enumerates approved plans across tenants and requeues each by its own id for internal processing, not tenant-facing data mixing
- **server/process-governor.ts:583** _(upstream-verified)_ — Action 'disable_task' issues UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without tenant_id — will disable tasks across tenants if ids collide or if an attacker supplies an id.
  - _Why false positive:_ rows to disable come from the immediately preceding tenant-scoped SELECT on heartbeat_tasks where ht.tenant_id = tenantId
- **server/process-governor.ts:640** _(upstream-verified)_ — Action 'restart_agent' performs UPDATE agent_desks SET ... WHERE id = ${desk.id} without tenant scoping — could reset desks from other tenants.
  - _Why false positive:_ desk.id values come from the immediately preceding tenant-scoped SELECT on agent_desks where ad.tenant_id = tenantId
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} but omits tenant_id. This allows modifying rows across tenants by id.
  - _Why false positive:_ global recurring-job sweep processes each row with its own row-carried tenant_id and only updates that same row's schedule metadata/content by its fetched id
- **server/reference-learner.ts:291** _(upstream-verified)_ — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} is executed without constraining tenant_id. memory_entries is tenant-scoped, so this can update another tenant's row by id.
  - _Why false positive:_ storedId is taken from the immediately preceding tenant-scoped SELECT on memory_entries with tenant_id and persona_id filters
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE research_sessions in cleanupZombieSessions is executed without any tenant_id constraint, so it will mark running sessions as completed across all tenants.
  - _Why false positive:_ zombie cleanup is a platform-wide housekeeping update of stale session status only, with no tenant-facing data return or content mixing
- **server/routes.ts:167** _(system-sweep)_ — Startup restoreReads file_storage rows across ALL tenants without any tenant_id constraint (db.select from fileStorage WHERE mimeType LIKE 'image/%' AND storageKey IS NULL). This will restore other tenants' files to the shared uploads dir and can leak or make available tenant-private content.
  - _Why false positive:_ Boot-time restore is a platform-wide housekeeping sweep over all rows; it writes files locally and returns no tenant data to any tenant-facing surface in this code path.
- **server/seed.ts:3826** _(system-sweep)_ — DELETE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint: `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE ...` will remove rows for all tenants.
  - _Why false positive:_ Seed-time cleanup is a platform-wide housekeeping delete of sentinel-marked stale disabled tasks, not a tenant-facing data access path.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table 'knowledge_triples' lacks a tenant_id constraint in the WHERE clause (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}) — this can allow cross-tenant mutation if row.id is ever derived from an untrusted source.
  - _Why false positive:_ The updated ids come only from the immediately preceding SELECT constrained by tenant_id = tenantId, so the loop updates tenant-owned rows selected in the same call path.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` updates a row by id only (`WHERE id = ${reservationId}`) with no tenant_id constraint. This allows a caller to update (or settle) a reservation row belonging to another tenant if they know/guess the id.
  - _Why false positive:_ This is an internal server helper updating a server-generated ledger row by primary key reservationId, not shown as coming from an untrusted request boundary.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` for releasing a reservation updates by id only (`WHERE id = ${reservationId}`) without constraining by tenant_id, enabling cross-tenant mutation if an id from another tenant is supplied.
  - _Why false positive:_ This is an internal server helper releasing a server-generated reservation row by primary key, with no evidence reservationId is user-controlled across tenants.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from experiments (runDueAbExperiments) does not filter by tenant_id — reads experiments across all tenants.
  - _Why false positive:_ Heartbeat-callable platform-wide experiment concluser intentionally scans across tenants and then processes each row using its own row-carried tenant_id.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from ai_insights in retryPendingMinervaRouting reads rows across tenants (no tenant filter) before per-row handling.
  - _Why false positive:_ Durability retry is a platform-wide maintenance sweep; each selected insight carries tenant_id and downstream actions are performed per-row with that tenant id.
- **server/agentic-engines.ts:345** _(global-table)_ — Query against heartbeat_logs in gatherOperationalData (and other engine functions) lacks tenant_id filter — reads heartbeat logs across tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so tenant filtering is not applicable to this global log table.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT from heartbeat_logs in runPredictiveEngine (recentLogs) lacks tenant scoping — reading logs across tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this read is from a genuinely global table rather than a tenant-scoped one.
- **server/agentic-engines.ts:519** _(global-table)_ — SELECT heartbeat_logs in runOptimizationEngine (heartbeatPerformance) is missing tenant_id filter — aggregates logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is described as platform-global heartbeat logging; aggregate is global by design
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets is executed without a tenant_id WHERE clause, reading a tenant-scoped table across all tenants.
  - _Why false positive:_ explicit heartbeat-callable sweep iterates all tenants by row-carried tenant_id and emits per-tenant events only
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Raw SELECT queries agent_runs / agent_approvals without constraining to a specific tenant (no WHERE tenant_id = <tenant>) — performs cross-tenant reads.
  - _Why false positive:_ worker sweep intentionally scans pending runs across tenants and carries each row's tenant_id forward without mixing tenant output
- **server/auto-asset-capture.ts:145** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The code inserts project-scoped rows using only project_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (${projectId}, ...)). Per policy, tenant-scoped tables must receive tenant_id explicitly on INSERT to enforce isolation.
  - _Why false positive:_ project_files has no tenant_id column; insert is transitively scoped through tenant-owned project_id parent
- **server/auto-asset-capture.ts:164** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The code writes a project-scoped note using only project_id (INSERT INTO project_notes (project_id, note, author) ...), violating the requirement that INSERTs into tenant-scoped tables set tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id column; insert is transitively scoped through tenant-owned project_id parent
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. The code inserts invoice_items with: INSERT INTO invoice_items (invoice_id, description, quantity, unit_price, amount, sort_order) VALUES (${newId}, ...). Per project invariant every tenant-scoped table INSERT must include tenant_id (columns are NOT NULL with no default) to enforce isolation at the application layer.
  - _Why false positive:_ invoice_items has no tenant_id column; line items are isolated through tenant-scoped parent invoice created in same transaction
- **server/ceo-orchestrator.ts:659** _(global-table)_ — Read from tenant-scoped `personas` table without a tenant filter: `const cachedPersonas = await db.select().from(personasTable);` returns personas for all tenants.
  - _Why false positive:_ schema note says personas has no tenant_id, so loading all personas is global by design
- **server/ceo-orchestrator.ts:1015** _(global-table)_ — Uses a persona id taken from a global persona cache (cachedPersonas) when creating a conversation (`personaId: targetPersona?.id || null`) without verifying that the persona belongs to the caller's tenant. This can assign cross-tenant persona ids to a tenant-scoped conversation.
  - _Why false positive:_ personaId comes from personas table which schema marks non-tenant-scoped/global
- **server/channel-routing.ts:31** _(global-table)_ — SELECT joins channel_routes -> personas without constraining to a tenant. The query (SELECT cr.*, p.name ...) has no tenant filter on channel_routes or personas, allowing cross-tenant data exposure if personas are tenant-scoped.
  - _Why false positive:_ channel_routes is created here without tenant_id and UNIQUE(channel) makes it platform-global; personas are also global
- **server/channel-routing.ts:63** _(global-table)_ — getPersonaForChannel queries channel_routes (SELECT persona_id ...) with no tenant constraint. This can return a persona_id belonging to a different tenant.
  - _Why false positive:_ channel_routes appears intentionally global per schema-in-code: no tenant_id and one route per channel
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute issues DELETE FROM channel_routes WHERE channel = ${channel} with no tenant filter, deleting routes across all tenants if the table is intended to be tenant-scoped.
  - _Why false positive:_ deleting by channel matches platform-global channel_routes design with UNIQUE(channel) and no tenant_id
- **server/commitment-drafter.ts:146** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` enumerates tenant rows without constraining to a specific tenant (no `WHERE tenant_id = ...`).
  - _Why false positive:_ explicit cross-tenant heartbeat sweep enumerates tenant ids then processes each tenant with per-row tenant scoping
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` is executed without any tenant_id constraint (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Why false positive:_ global purge job scans expired soft-deleted conversations and then handles each row using its own tenant_id for child deletes/backups
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations uses only `WHERE id = ${conv.id}` with no tenant_id predicate. Although child deletes above include tenant predicates, this final DELETE lacks the tenant scoping and could delete a conversation row without an explicit tenant filter.
  - _Why false positive:_ Global expiry purge loop intentionally processes expired conversations across tenants using each row's own tenant_id; final delete is housekeeping and does not return/mix tenant data.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake updates dfy_intake_forms via .where(eq(dfy_intake_forms.id, form.id)) without constraining tenant_id. This allows updating a form row by id regardless of tenant ownership.
  - _Why false positive:_ Row is first resolved by token via getDfyIntakeByToken, then update targets that exact fetched row id; ownership/access is via the token lookup path rather than caller-supplied tenant id.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues a DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with no tenant_id constraint. Even though collection existence is checked earlier, the DELETE itself is not explicitly tenant-scoped.
  - _Why false positive:_ Immediately above, collection existence is verified with id + tenant_id; delete is scoped by that validated collection_id in the same call path.
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks embedding with UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} and does not include tenant_id. This can modify another tenant's chunk row by id.
  - _Why false positive:_ Rows being updated were selected just above with collection_id + tenant_id, and update uses those trusted row ids within the same loop.
- **server/email.ts:515** _(system-sweep)_ — backfillEmptyBodies selects rows from inbox_messages (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id) without any tenant scoping. This reads across all tenants' inbox_messages.
  - _Why false positive:_ Backfill is a platform-wide maintenance job over empty-body rows; it performs per-row housekeeping fetch/update and does not return or combine tenant content to another tenant-facing surface.
- **server/email.ts:529** _(system-sweep)_ — backfillEmptyBodies updates inbox_messages with UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} without tenant_id in the WHERE clause. This can update another tenant's message record by id.
  - _Why false positive:_ Per-row maintenance update inside global email backfill; mutates housekeeping content fields only and does not mix tenants in one response/prompt.
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal derived from caller input (query→embedding→vec). This performs string interpolation of request-derived data into SQL (embedding_vec <=> '${vec}'::vector), which risks SQL injection and violates safe parameterization rules.
  - _Why false positive:_ This query is tenant-scoped in the same statement with AND tenant_id = tenantId; finding is about SQL raw interpolation, not cross-tenant isolation.
- **server/embeddings.ts:252** _(upstream-verified)_ — Same as above: ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} interpolates a request-derived embedding into raw SQL.
  - _Why false positive:_ ORDER BY uses same tenant-scoped query constrained by tenant_id; not a tenant-isolation issue.
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal derived from caller input in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). This interpolates request-derived data into raw SQL.
  - _Why false positive:_ Knowledge search query includes AND tenant_id = tenantId in the same statement; flagged concern is SQL raw usage, not cross-tenant access.
- **server/event-bus.ts:173** _(global-table)_ — Query joins event_subscriptions.es to personas p via p.id = es.persona_id but does not verify that the persona row belongs to the same tenant (no p.tenant_id filter). Because DB-level FK/ownership is not relied upon, this can leak persona data across tenants or associate subscriptions with personas from other tenants.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so lack of p.tenant_id predicate is expected and not a tenant-scoped-table isolation failure.
- **server/event-bus.ts:187** _(upstream-verified)_ — Wildcard subscription query (the fallback when no exact event subscriptions are found) also JOINs personas p without constraining p.tenant_id to the caller's tenant, risking cross-tenant persona name leakage.
  - _Why false positive:_ Joined personas row is reached only through tenant-scoped event_subscriptions WHERE es.tenant_id = tenantId; personas has no tenant_id and access is via validated subscription row.
- **server/felix-brain.ts:19** _(system-sweep)_ — Raw SELECT from tenant-scoped table 'agent_runs' without constraining tenant_id (no WHERE tenant_id = ...). This rehydration reads rows across all tenants.
  - _Why false positive:_ Boot-time rehydration intentionally scans running/pending agent_runs across tenants and stores per-row state keyed from each row; no tenant-facing mixed output shown.
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint. This can update a run belonging to another tenant.
  - _Why false positive:_ Internal worker updates felix_loop_runs by server-generated runId within same run flow; not sourced from untrusted request boundary.
- **server/felix-loop.ts:690** _(internal-worker-pk)_ — Error-path UPDATE on 'felix_loop_runs' uses WHERE id = ${runId} without tenant_id constraint, allowing cross-tenant modification in failure handling.
  - _Why false positive:_ Error-path update uses same internal server-generated runId for worker bookkeeping, not caller-controlled input.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects SET driveFolderId/driveFolderUrl uses WHERE eq(projects.id, projectId) without tenant_id filtering. Although project ownership was validated earlier, the UPDATE itself is not tenant-constrained (write lacks explicit tenant filter).
  - _Why false positive:_ Same function selects project, confirms project.tenantId === tenantId, then updates that verified project id.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into tenant-scoped table presenter_slide_images does not include tenant_id (session_id used alone). This writes binary image rows without scoping by tenant, allowing cross-tenant data leakage or overwrite if session_id is controlled or collides.
  - _Why false positive:_ presenter_slide_images is written via session_id to tenant-scoped presenter_sessions; child table is isolated transitively through the parent session.
- **server/google-workspace.ts:2364** _(fk-transitive)_ — Second INSERT (thumbnail) into presenter_slide_images omits tenant_id as above — same cross-tenant write risk.
  - _Why false positive:_ Thumbnail row insert is into child table keyed by session_id under tenant-scoped presenter_sessions; no tenant_id on child is consistent with transitive isolation.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals omits any tenant_id predicate. code_proposals is likely tenant-scoped, so this query can read proposals across all tenants.
  - _Why false positive:_ Global stale-proposal audit is maintenance logic operating across tenants; rows are only turned into admin audit findings, not returned to tenant-scoped surface here.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) has no tenant_id filter. This will alter proposals across tenants if stale ids include rows from other tenants.
  - _Why false positive:_ Global housekeeping update archives stale proposals across tenants as maintenance status mutation only, not cross-tenant content mixing.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from research_schedules executed without constraining by tenant_id. The query SELECT * FROM research_schedules ... lacks any WHERE tenant_id = ... clause, so rows from all tenants will be returned to the caller.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due research_schedules across tenants and then processes each row with its own sched.tenant_id.
- **server/heartbeat.ts:385** _(system-sweep)_ — SELECT from event_log (SELECT * FROM event_log WHERE status = 'pending' ...) executes without a tenant_id constraint and will return pending events from all tenants.
  - _Why false positive:_ heartbeat maintenance loop processes pending rows across tenants per-row using each event's own tenant_id and only updates housekeeping status
- **server/heartbeat.ts:1670** _(global-table)_ — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id constraint. If heartbeat_logs is tenant-scoped this will read logs across tenants.
  - _Why false positive:_ heartbeat_logs schema has no tenant_id and is used as platform-global task log
- **server/knowledge-diversity-monitor.ts:266** _(system-sweep)_ — SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} executes against agent_knowledge without restricting to a single tenant. This reads tenant-scoped embeddings across tenants.
  - _Why false positive:_ platform-wide monitor intentionally scans all tenants and handles each tenant/persona pair separately, emitting only per-pair snapshots/counts
- **server/lib/bwb-job-progress.ts:74** _(internal-worker-pk)_ — scope() may omit tenant_id, allowing UPDATE/SELECT against tenant-scoped video_jobs rows without a tenant predicate. Several callers call .where(scope(...)) (setBwbPhase, updateBwbChapters, completeBwbJob select+update, bumpBwbHeartbeat, failBwbJob) and if env BWB_TENANT_ID is missing the WHERE reduces to jobId-only — this results in cross-tenant writes/reads on a tenant-scoped table.
  - _Why false positive:_ server-side worker progress helper scopes by server-generated unguessable jobId from env/spawn chain, not untrusted request input
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT aggregates projects across all tenants without a tenant_id filter (reads tenant-scoped table `projects` with no WHERE tenant_id = ...). This violates the invariant that every read against tenant-scoped tables must be constrained to the caller's tenant, and can leak cross-tenant counts/rows.
  - _Why false positive:_ boot/backfill maintenance scans projects across tenants only to compute per-tenant counts for snapshot reconciliation, not tenant-facing output
- **server/lib/mention-injection.ts:121** _(global-table)_ — Query joins personas p (LEFT JOIN personas p ON p.id = cm.from_persona_id) but does not constrain personas by tenant_id. Reading p.name without p.tenant_id = ${tenantId} can leak another tenant's persona metadata.
  - _Why false positive:_ schema note says personas has no tenant_id, so no tenant predicate is applicable on joined personas
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table 'tenant_voice_profiles' is constrained only by id (WHERE id = <existing.id>) and does not include a tenant_id check. Without also filtering on tenant_id an attacker or bug might update a row belonging to a different tenant.
  - _Why false positive:_ row being updated comes from getVoiceProfile called with same tenantId and profileName, so ownership is established earlier in call path
- **server/memory-graph.ts:494** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped table by id only and lacks a tenant_id constraint. This can change rows belonging to other tenants if IDs are misused.
  - _Why false positive:_ Row ids come from a prior SELECT on memory_entries scoped by tenant_id = tenantId in the same function/loop.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list.
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated via project_id after assertProjectInTenant ownership check.
- **server/mpeg-engine.ts:1417** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id in the VALUES list.
  - _Why false positive:_ project_files has no tenant_id column and insert is guarded by assertProjectInTenant on the parent project.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id/is_active FROM `oauth_subscriptions` for provider='google-workspace' with no tenant_id constraint — enumerates cross-tenant subscription state.
  - _Why false positive:_ Startup validation intentionally enumerates subscriptions across tenants for maintenance/reconnect, using each row's tenant_id and not returning mixed tenant data to a tenant surface.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — missing tenant_id constraint on UPDATE against a tenant-scoped table.
  - _Why false positive:_ Boot-time maintenance loop processes each subscription row and only flips housekeeping status by that row's id; no tenant-facing data mixing.
- **server/oauth-subscriptions.ts:966** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — another unscoped UPDATE on a tenant-scoped table (from a different branch).
  - _Why false positive:_ Startup validation sweeps subscriptions across tenants and only toggles row-local status using each row's own tenant_id context; no tenant-facing data mixing.
- **server/orchestrator-ledger.ts:459** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${row.id} — the UPDATE uses id only and does not constrain by tenant_id. This is an unscoped write on a tenant-scoped table.
  - _Why false positive:_ row.id comes from a prior SELECT on memory_entries scoped by source, tenant_id, and category within the same transaction.
- **server/pace-control.ts:85** _(global-table)_ — SELECT COUNT(*) FROM `heartbeat_logs` is executed without any tenant_id filter (checkPace). This is a tenant-scoped table but is being queried globally, leaking/merging cross-tenant usage counts.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id; this pace gate is intentionally platform-global.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries heartbeat_logs (counts and per-persona aggregates) without filtering by tenant_id — returns cross-tenant aggregates from a tenant-scoped table.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id; snapshot is querying a global log table by design.
- **server/process-governor.ts:119** _(global-table)_ — In heartbeat task queries the correlated subqueries SELECT COUNT(*) FROM heartbeat_logs ... lack tenant_id filtering. The subquery aggregates logs without scoping to the tenant, leaking or mis-aggregating other tenants' logs.
  - _Why false positive:_ heartbeat_logs is not tenant-scoped per schema, so global counts are by design even when correlated to tenant tasks.
- **server/process-governor.ts:583** _(upstream-verified)_ — Action 'disable_task' uses SELECT with subqueries that count heartbeat_logs without tenant_id (same as earlier) which can mis-evaluate failure rates using other tenants' logs.
  - _Why false positive:_ The later UPDATE heartbeat_tasks by id uses ids selected from ht rows already constrained by ht.tenant_id = tenantId in the same flow.
- **server/process-governor.ts:238** _(global-table)_ — cascading_failures: SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... lacks tenant_id — it aggregates logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column per schema, so this aggregate is on a global table.
- **server/recurring-messages.ts:182** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id constraint when invoked with the _allowGlobalSystemUseOnly path. This returns rows across all tenants.
  - _Why false positive:_ Explicit internal-only global path is documented for cron use, and returned rows carry tenant_id for per-row tenant-context handling before delivery.
- **server/recurring-messages.ts:258** _(system-sweep)_ — Global scan: SELECT id,title,content,tenant_id FROM `agent_knowledge` WHERE category = 'recurring_message' LIMIT 500 executes with no tenant_id filter and returns rows for all tenants.
  - _Why false positive:_ Heartbeat runner intentionally scans all tenants, then processes each row with its own row-carried tenant_id and does not return mixed tenant data to a tenant-facing surface.
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge removes autoresearch rows without scoping by tenant_id, which will delete entries across all tenants.
  - _Why false positive:_ Global expiry cleanup deletes only expired autoresearch rows as housekeeping; it is a platform-wide sweep and does not expose tenant data.
- **server/routes.ts:766** _(system-sweep)_ — Backfill selects projects (SELECT p.id, p.name, p.tenant_id FROM projects JOIN tenants ...) without constraining by tenant_id. This runs at startup and operates across all tenant rows without an explicit admin guard or per-tenant iteration, enabling cross-tenant reads/processing.
  - _Why false positive:_ Startup backfill intentionally iterates all projects and passes each row's own tenant_id into per-row folder creation; housekeeping/backfill across tenants by design.
- **server/routes/admin.ts:715** _(upstream-verified)_ — Query against tenant-scoped table `tool_performance` has no tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance). This can return rows across all tenants.
  - _Why false positive:_ Route is explicitly restricted to ADMIN_TENANT_ID and admin requests immediately above, making this an intentionally admin-only cross-tenant diagnostic view.
- **server/routes/admin.ts:721** _(upstream-verified)_ — Query against tenant-scoped table `tool_performance` lacks tenant_id constraint (SELECT ... FROM tool_performance WHERE last_failure_at > ...). This can leak cross-tenant telemetry.
  - _Why false positive:_ Same call path is gated to ADMIN_TENANT_ID plus admin check before the query, so cross-tenant telemetry here is under explicit admin-only control.
- **server/routes/admin.ts:734** _(upstream-verified)_ — Query against tenant-scoped table `ai_insights` lacks tenant_id constraint (SELECT ... FROM ai_insights WHERE status = 'new' ...). This will surface incidents across all tenants to the caller.
  - _Why false positive:_ Same admin-only route guard proves ownership/context earlier in the call path; this cross-tenant diagnostics query is intentionally restricted.
- **server/routes/admin.ts:744** _(upstream-verified)_ — Query against tenant-scoped table `heartbeat_tasks` has no tenant_id constraint (SELECT ... FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3). This can return tasks for all tenants.
  - _Why false positive:_ Same admin-only route guard applies earlier in the call path, so this global heartbeat-task diagnostic query is intentionally limited to platform admin.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE against tenant-scoped table 'conversations' performed without constraining by tenant_id (WHERE id = ${conv.id} only)
  - _Why false positive:_ conv.id is server-created in this call and project ownership was verified just above with projects.id + tenant_id before this update.
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE against tenant-scoped table 'projects' performed without constraining by tenant_id (WHERE id = ${projectId} only)
  - _Why false positive:_ Same call path already verified the target project with WHERE id = projectId AND tenant_id = tenantId before touching updated_at.
- **server/routes/goal-ledger.ts:92** _(upstream-verified)_ — readActiveJobs() issues: SELECT FROM agent_jobs (joined to tenants) does not constrain results by tenant_id. This returns rows across all tenants.
  - _Why false positive:_ Route using readActiveJobs is protected by requirePlatformAdmin, so cross-tenant admin view is explicitly authorized in same call path.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} — the UPDATE has no tenant_id constraint. This can mutate a message belonging to a different tenant.
  - _Why false positive:_ msg was fetched immediately above with WHERE id = msgId AND tenant_id = tenantId; update uses that verified row id only.
- **server/routes/metrics.ts:63** _(global-table)_ — Metrics queries (e.g. SELECT from agent_cost_ledger, agent_cost_ledger tool counts, tokens, repair_incidents) query tenant-scoped tables without a tenant_id WHERE predicate, returning data across tenants.
  - _Why false positive:_ Prometheus /metrics endpoint is token-protected operator telemetry, intentionally aggregating across tenants rather than serving tenant-scoped app data.
- **server/routes/projects.ts:33** _(fk-transitive)_ — SELECT subqueries count project_files, project_notes, project_conversations without constraining those tenant-scoped tables to the caller's tenant (no WHERE tenant_id = ${tenantId} or equivalent). These reads rely on project_id alone and therefore violate the invariant requiring explicit application-level tenant filters.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are child tables isolated through tenant-scoped parent projects already filtered by p.tenant_id = tenantId.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT INTO project_files in the base64 upload handler does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...). If project_files is tenant-scoped and tenant_id is NOT NULL/no default, this allows creation of rows without an explicit tenant ownership check and violates the invariant.
  - _Why false positive:_ project_files has no tenant_id; insert is into child table after parent project ownership check with id + tenant_id.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT INTO project_files in the multipart upload handler does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...). Same tenant-ownership insertion omission as above.
  - _Why false positive:_ project_files has no tenant_id; insert is into child table after parent project ownership check with id + tenant_id.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ... reads project_files without an explicit tenant constraint. Although the project was validated earlier, this SELECT does not include tenant_id in the WHERE clause/joins, violating the requirement that every read against a tenant-scoped table be explicitly constrained to the caller's tenant.
  - _Why false positive:_ project_files has no tenant_id and access is transitively scoped by prior verified parent project row for this tenant.
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not constrain project_files by tenant_id. This is a direct read on a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ project_files has no tenant_id and the project parent was already verified for tenant ownership before this child-row lookup.
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} deletes a row from project_files without an explicit tenant_id constraint. This write may affect tenant-scoped data without a direct tenant filter in the WHERE clause.
  - _Why false positive:_ project_files has no tenant_id and is scoped through verified tenant-owned project_id just above
- **server/sculptor.ts:105** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets status/result without constraining tenant_id (WHERE id = ${session.id} only). This can modify a session row belonging to another tenant.
  - _Why false positive:_ updates newly inserted sculptor_sessions row by server-generated session.id within same internal flow
- **server/sculptor.ts:112** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id WHERE id = ${session.id} with no tenant_id constraint. Missing tenant guard allows cross-tenant modification.
  - _Why false positive:_ sets conversation_id on just-created session using internal server-side session.id, not untrusted request id
- **server/sculptor.ts:167** _(internal-worker-pk)_ — UPDATE sculptor_sessions (monitorSession) updates status/result/tool_calls_count/tokens_used/completed_at WHERE id = ${sessionId} but omits tenant_id in the WHERE clause.
  - _Why false positive:_ background monitor updates its own session by internal sessionId from prior create path, not caller-controlled
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter — reads tenant-scoped table globally.
  - _Why false positive:_ boot-time maintenance count across tenants only aggregates rows for housekeeping and returns no tenant data
- **server/seed-cleanup.ts:15** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks tenant_id constraint — will disable delegation tasks across all tenants.
  - _Why false positive:_ maintenance reconciler intentionally disables runaway tasks platform-wide without exposing mixed tenant data
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' is not tenant-scoped.
  - _Why false positive:_ global housekeeping count scan over heartbeat_tasks is aggregate-only and non-tenant-facing
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true does not include tenant_id in WHERE — affects all tenants.
  - _Why false positive:_ platform-wide cleanup intentionally disables duplicate session-logger tasks across tenants as housekeeping
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) performs an unscoped read across tenant-scoped table.
  - _Why false positive:_ aggregate cross-tenant scan for orphaned repo-examination tasks is maintenance-only and not tenant-facing
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true lacks tenant scoping and will update rows across tenants.
  - _Why false positive:_ Boot-time cleanup intentionally disables duplicate/orphaned heartbeat tasks across tenants; housekeeping-only mutation with no tenant-facing data return.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only id and no tenant_id constraint — can modify records belonging to other tenants.
  - _Why false positive:_ Seed repair loop bulk-fixes invalid research_programs model ids platform-wide; maintenance mutation only, no cross-tenant data exposure to callers.
- **server/seed.ts:2516** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (...) queries the conversations table (tenant-scoped) without any tenant_id constraint, potentially exposing cross-tenant data and allowing inserts based on other tenants' rows.
  - _Why false positive:_ Boot-time backfill scans all conversations and inserts missing join rows into tenant-transitive project_conversations; maintenance reconciliation only, no tenant-facing mixed output.
- **server/seed.ts:3356** _(system-sweep)_ — Unscoped read from tenant-scoped table `heartbeat_tasks` (db.select().from(heartbeatTasks) with no tenant_id filter). This reads rows across all tenants.
  - _Why false positive:_ Seed checks whether any heartbeat tasks exist before inserting default admin-tenant tasks; startup seeding logic, not tenant-facing data access.
- **server/seed.ts:3854** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` lacks tenant restriction: the normalizer `UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND fact LIKE ...` will modify rows for every tenant.
  - _Why false positive:_ Global memory spelling normalizer is an idempotent maintenance update across tenants and only rewrites row-local text, with no tenant-facing data return or mixing.
- **server/seed.ts:3880** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` without tenant filter: `UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'` updates conversations across all tenants.
  - _Why false positive:_ Platform-wide persisted model-id backfill updates conversations across tenants as housekeeping only; no cross-tenant data is returned or combined.
- **server/seed.ts:4026** _(fk-transitive)_ — INSERT into `project_notes` does not set tenant_id. Tenant-scoped tables require tenant_id to be provided on INSERTs.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id to tenant-scoped projects; insert without tenant_id is by design.
- **server/storage.ts:285** _(upstream-verified)_ — attachCitationMetadata issues a raw SELECT FROM messages WHERE id IN (...) without any tenant_id constraint. This reads message rows without enforcing tenant ownership.
  - _Why false positive:_ attachCitationMetadata only consumes msgs already fetched with tenant-scoped WHERE on messages.tenantId in same call path.
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata uses db.execute(sql`SELECT ... FROM messages WHERE id IN (...) ...`) and later uses db.execute(sql`UPDATE messages SET citations = ... WHERE id = ${messageId}`) with no tenant_id constraint on messages. Raw SQL updates/reads on tenant-scoped messages without tenant scoping risk cross-tenant modification.
  - _Why false positive:_ raw SELECT is limited to ids from tenant-scoped msg rows fetched immediately above; no new cross-tenant reach.
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories executes a raw UPDATE memory_entries SET status = 'archived' WHERE ... RETURNING id with NO tenant constraint; this will archive active memories across all tenants.
  - _Why false positive:_ platform-wide housekeeping update archives expired rows across tenants and only mutates status/returns count.
- **server/storage.ts:857** _(system-sweep)_ — archiveStaleMemories runs a raw UPDATE memory_entries SET status = 'archived' WHERE status='active' AND created_at <= ${cutoff} ... with NO tenant filter — it will touch rows across tenants.
  - _Why false positive:_ platform-wide housekeeping update archives stale rows across tenants and only mutates status/returns count.
- **server/tenant-fork.ts:226** _(upstream-verified)_ — UPDATE performed against a tenant-scoped table without constraining by tenant_id (update(entry.table).where(eq(entry.table.id, p.newId))).
  - _Why false positive:_ Update targets freshly inserted clone row by server-generated newId inside same transaction after tenantId was set to newTenantId; not request-controlled cross-tenant access.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT by id against tenant-scoped table custom_tools lacks a tenant_id constraint (.where(eq(customTools.id, inserted.id))).
  - _Why false positive:_ Read is by inserted.id from the immediately preceding insert carrying tenantId; id is server-returned for the just-created row.
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE against tenant-scoped table custom_tools updates implementation using only id in WHERE (db.update(customTools).where(eq(customTools.id, tool.id))) with no tenant_id check.
  - _Why false positive:_ Update uses tool.id from the just-inserted/validated tool object in same internal flow, not attacker-selected row id.
- **server/tool-learning.ts:316** _(upstream-verified)_ — DELETE against tenant-scoped table custom_tools deletes by id without constraining by tenant_id (db.delete(customTools).where(eq(customTools.id, tool.id))).
  - _Why false positive:_ Delete reverts the same newly inserted tool row referenced by internal tool.id from current flow; not an untrusted cross-tenant lookup.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE increments usageCount on custom_tools using only id in WHERE (db.update(customTools).where(eq(customTools.id, tool.id))) without a tenant_id constraint.
  - _Why false positive:_ tool was selected a few lines above with WHERE name AND tenant_id = tenantId, so updating by that tool.id is ownership-verified in the same call path.
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT INTO project_conversations omits tenant_id when writing a tenant-scoped linkage row. This allows rows to be created with no tenant column set (or set implicitly by DB defaults), violating the invariant that INSERTs must pass tenant_id explicitly.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped via tenant-checked project and conversation parents before insert.
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT INTO project_files does not set tenant_id. Per invariant, every INSERT into tenant-scoped tables must explicitly set tenant_id; otherwise rows can be created without tenant ownership and enable cross-tenant contamination.
  - _Why false positive:_ project_files has no tenant_id and is child-scoped through project_id after prior project ownership check.
- **server/tools.ts:2996** _(fk-transitive)_ — INSERT INTO project_notes does not include tenant_id. This can persist notes that are not bound to the caller tenant, violating tenant isolation and the INSERT rule requiring tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-isolated through tenant-scoped parent project validated just above.
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT INTO project_conversations (linking in link_conversation) omits tenant_id. Even though conversation/project ownership is partially checked, the insert itself does not explicitly set tenant_id on the created row.
  - _Why false positive:_ project_conversations has no tenant_id and insert follows checks that project and conversation both belong to tenant via parent tables.
- **server/tools.ts:2915** _(fk-transitive)_ — COUNT subqueries in the project list (SELECT COUNT(*) FROM project_files / project_notes / project_conversations WHERE project_id = p.id) do not include a tenant_id constraint. Reads against tenant-scoped tables must be restricted to the caller's tenant — a project_id alone is not a sufficient isolation guard without an explicit tenant_id check under the stated invariant.
  - _Why false positive:_ project_files/project_notes/project_conversations lack tenant_id and counts are scoped by tenant-filtered parent projects row p.
- **server/tools.ts:3031** _(fk-transitive)_ — Search query uses subqueries (COUNT(*) FROM project_files / project_notes WHERE project_id = p.id) without tenant_id filters. Under the project listing/search paths these reads can leak or count rows across tenants if tenant_id is not enforced in the subquery.
  - _Why false positive:_ Subquery counts are over child tables without tenant_id and are anchored to tenant-filtered parent project p.id.
- **server/tools.ts:2976** _(fk-transitive)_ — Duplicate-check SELECT against project_files (dupCheck) does not include tenant_id. Even though the code checked project ownership earlier, the SELECT itself lacks tenant scoping and may match rows from other tenants with the same project_id if tenant enforcement relies on application-layer constraints.
  - _Why false positive:_ project_files has no tenant_id; duplicate check is by child rows of a project whose ownership was verified just above.
- **server/tools.ts:4541** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code relies on assertProjectInTenant earlier but the INSERT statement omits an explicit tenant_id column/value.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped via tenant-owned project_id after assertProjectInTenant
- **server/tools.ts:4545** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checks assertProjectInTenant but the INSERT omits tenant_id in the column list/values.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped through validated project_id
- **server/tools.ts:6393** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (audio upload) does not include tenant_id. Although assertProjectInTenant was called, the INSERT itself omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id; insert uses project_id validated by assertProjectInTenant
- **server/tools.ts:8892** _(upstream-verified)_ — UPDATE on proposed_skills lacks a tenant constraint. db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) updates a tenant-scoped table by id only, without ensuring the row belongs to the caller's tenant, enabling cross-tenant modification.
  - _Why false positive:_ update targets row.id just inserted/returned earlier in same call path for tenant tid, so ownership is established upstream
- **server/tools.ts:8917** _(upstream-verified)_ — UPDATE on proposed_skills lacks a tenant constraint. db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) modifies the proposal row without verifying tenant ownership.
  - _Why false positive:_ row.id refers to the proposal row created earlier in this same tenant-scoped flow; update is on that verified row
- **server/tools.ts:8933** _(upstream-verified)_ — UPDATE on proposed_skills lacks a tenant constraint. The code sets status='rejected' for a proposal by id only, without constraining by tenant_id, which can affect other tenants' rows.
  - _Why false positive:_ rejection update uses the same newly created proposal row from this call path, already bound to tenant tid
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table `browser_workflows` lacks tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This write can affect rows outside the caller's tenant scope.
  - _Why false positive:_ workflow was selected immediately above with id/name plus tenant_id, and update uses that fetched row's id
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` lacks tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). This write is not explicitly scoped to the caller tenant.
  - _Why false positive:_ projId comes from a query joining conversations to projects with p.tenant_id = tenantId earlier in same path
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT against provider_keys omits tenant constraint — query returns rows from provider_keys without WHERE tenant_id = ${tenantId} (scanDb.execute at line 163).
  - _Why false positive:_ provider_keys schema note shows no tenant_id and query is admin-gated to tenantId === 1 for platform-wide audit
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM provider_keys omits tenant constraint — query aggregates across all provider_keys without scoping to tenant (scanDb.execute at line 260).
  - _Why false positive:_ provider_keys has no tenant_id and this is an admin-only platform aggregate over a global table
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE on tenant-scoped table memory_entries uses WHERE id = ${id} without also constraining tenant_id — this can update rows from other tenants if id is guessed or collisions occur (db.execute UPDATE at line 92).
  - _Why false positive:_ id is selected immediately above from memory_entries with tenant_id = tid and persona/category filters, then reused for the update.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) lacks a tenant constraint — bulk update may affect rows belonging to other tenants (db.execute at line 148).
  - _Why false positive:_ ids being bulk-updated come solely from the prior SELECT scoped by tenant_id = tid and persona/category filters.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id} executed without tenant_id constraint (db.execute at line 242) — may modify other tenants' data.
  - _Why false positive:_ id is obtained from a preceding tenant-scoped SELECT on memory_entries for this tenant/persona/category before the update.
- **server/video-job-runner.ts:206** _(internal-worker-pk)_ — isCancelRequested performs a SELECT on tenant-scoped table `video_jobs` filtering only by jobId (eq(videoJobs.jobId, jobId)) and does NOT constrain by tenant_id. This lets callers probe/read cancel state for any job_id regardless of tenant ownership.
  - _Why false positive:_ internal runner helper reads by server-generated globally unique jobId inside worker code, not from an untrusted cross-tenant request boundary in this function.
- **server/video-job-runner.ts:236** _(system-sweep)_ — recoverStaleVideoJobs runs an UPDATE against tenant-scoped `video_jobs` without any tenant_id constraint, marking any stale job across all tenants as failed. This mutates other tenants' rows purely based on age/status and lacks an ownership/tenant scope.
  - _Why false positive:_ periodic recovery sweeper intentionally updates stale job status/housekeeping fields across all tenants and only returns aggregate/job ids for maintenance, not tenant data.
- **server/auto-memorize.ts:75** _(system-sweep)_ — INSERT INTO agent_knowledge hard-codes tenant_id = 1 when creating the watermark. This writes to a specific tenant rather than the acting tenant (or failing to set tenant correctly).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:60-81 setWatermark writes a single global housekeeping watermark row (category='auto_memorize_watermark') for the platform-wide runAutoMemorize sweep; it is never returned to a tenant surface (only read by getWatermark:45-58). tenant_id=1 is the sweep's fixed housekeeping bucket, not acting-tenant data
- **server/chat-engine.ts:4407** _(internal-worker-pk)_ — storage.attachCitations(assistantMsgId, collectedCitations) is called with no tenant parameter. If attachCitations inserts citation rows into a tenant-scoped table it may create rows missing tenant_id or mis-associate citations across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storage.attachCitations (storage.ts:392-401) does UPDATE messages SET citations WHERE id = messageId; messages has tenant_id (schema.ts:147). Both callers pass a server-generated PK of a message the code just inserted: chat-engine.ts:4332 uses (assistantMsgRow).id and routes.ts:5450 uses (__asstMsg).id. No request/tool-arg-derived id.
- **server/embeddings.ts:684** _(system-sweep)_ — UPDATE to tenant-scoped tables (memory_entries / agent_knowledge) without constraining by tenant_id. storeEmbeddingVec updates rows by id only (UPDATE ... WHERE id = ${id}) and also uses sql.raw(table) for table selection; this can modify rows belonging to any tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/job-queue.ts:274** _(internal-worker-pk)_ — UPDATE agent_jobs ... (cancelJob) transitions a job to 'cancelled' by id without scoping to tenant_id. An operator or API call could cancel jobs belonging to other tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:283** _(internal-worker-pk)_ — UPDATE agent_jobs ... (retryJob) resets a job's attempts/status by id without any tenant_id constraint, allowing cross-tenant manipulation of job state.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped proposal without tenant scoping — an attacker could query/verify another tenant's proposal by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/proposal-verifier.ts:31-35 SELECT by proposalId; every caller is internal (server/agentic/autonomous-closer.ts:86-103 with tenant-scoped candidate ids, plus scripts/reverify-proposals.ts and scripts/drain-and-verify-proposals.ts). No request/tool-arg id path.
- **server/proposal-verifier.ts:241** _(internal-worker-pk)_ — persist(): UPDATE code_proposals SET verification_status/verification_details WHERE id = ${proposalId} — updates a tenant-scoped table without tenant_id in WHERE, allowing cross-tenant mutation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — attemptRebaseOnDrift: UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} (no tenant filter) — mutates proposal state across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — attemptRebaseOnDrift: UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} — writes proposal changes without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/storage.ts:1310** _(upstream-verified)_ — getAllDataForExport performs wide SELECTs over many tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, provider_keys, heartbeat_tasks, etc.) with NO tenant scoping. This can export all tenants' data in one operation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/agent-channels.ts:194** _(upstream-verified)_ — getUnreadCount joins channel_subscriptions but does not include cs.tenant_id = ${tenantId} — the JOIN can match subscription rows from other tenants and produce incorrect counts/visibility.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): getUnreadCount filters cm.tenant_id = ${tenantId}; the channel_subscriptions join is keyed on the caller's persona_id (personas are tenant-scoped FKs), so another tenant's subscription rows cannot match.
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from agent_knowledge (watermark) omits any tenant_id constraint — reads a tenant-scoped table across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — the UPDATE omits tenant_id in the WHERE clause, so it can modify a conversation row belonging to a different tenant if IDs are incorrect or untrusted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): auto-transcript.ts:165 UPDATE conversations SET project_id inside backfillProjectTranscripts platform sweep; row comes from a tenant-consistent join (auto-transcript.ts:153-161 JOIN projects p ON p.tenant_id=c.tenant_id) and only sets housekeeping project_id
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `capability_reviews` performed without constraining by tenant_id. The markReviewReused() SQL updates rows by id only (`WHERE id = ${pending.reviewId} AND reused = false`) and does not include tenant_id, allowing cross-tenant writes if review ids are guessed or pending map is poisoned.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/chat-engine.ts:4080** _(global-table)_ — recordToolExecution(conversationId, ...) is called without a tenant identifier. Recording tool execution against a conversation by conversationId alone can cross tenant boundaries if the DB writes/reads are not tenant-scoped.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolExecution (felix-brain.ts:298-320) only mutates the in-process taskStateStore Map; no DB write, no tenant table.
- **server/chat-engine.ts:4084** _(internal-worker-pk)_ — recordToolExecution(...) is invoked (variant call that includes extracted ids) with no tenant context. Writing tool-execution metadata or associated ids without tenant scoping risks cross-tenant data leakage or tampering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:4004 — recordToolExecution (server/felix-brain.ts:298) is a purely in-memory operation on the taskStateStore Map keyed by conversationId; it performs NO database write to any tenant-scoped table. There is no cross-tenant persistence risk.
- **server/chat-engine.ts:4091** _(global-table)_ — recordToolCallForStuckDetection(conversationId, toolName, ...) is invoked without tenant information. If this function persists/readstool-call history in a tenant-scoped table it must be constrained by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolCallForStuckDetection (stuck-diagnostics.ts:100-134) pushes into an in-process Map (turnToolCalls); no DB access, no tenant-scoped table.
- **server/commitment-drafter.ts:235** _(upstream-verified)_ — UPDATE against tenant-scoped table `commitments` omits tenant filter: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This writes a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/delivery-pipeline.ts:203** _(internal-worker-pk)_ — updateDeliveryLog performs an UPDATE on delivery_logs without scoping by tenant_id (where clause is eq(delivery_logs.id, id) only). This allows updating another tenant's delivery row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateDeliveryLog is only ever called with deliveryId returned by createDeliveryLog within deliverDigitalProduct itself (server/delivery-pipeline.ts:653,755,813,836,847,896,960,729); no caller passes a request/tool-arg-derived id.
- **server/delivery-pipeline.ts:1071** _(global-table)_ — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with no tenant_id constraint. This can return another tenant's delivery row/link.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. Updating session rows by id only can modify another tenant's session if session ids collide or are attacker-controlled.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/job-queue.ts:320** _(system-sweep)_ — SELECT ... FROM agent_jobs ${whereClause} (listJobs) can emit a result set with no tenant_id filter when the caller omits tenantId. Listing jobs across all tenants may expose tenant-scoped data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/job-queue.ts:385** _(system-sweep)_ — SELECT status, COUNT(*) ... FROM agent_jobs WHERE created_at > ... (getJobStats) aggregates job counts across ALL tenants with no tenant_id scoping, leaking cross-tenant metrics.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} updates a tenant-scoped table by id only with no tenant_id constraint. This can modify a category row belonging to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): catId is produced by ensureCategory (server/memory-graph.ts:79) which SELECTs/INSERTs scoped to tenantId (:51 'AND tenant_id = ${tenantId}', :57-58 INSERT with tenant_id); the id is server-derived within the same tenant call path, not request-supplied.
- **server/minerva-planner.ts:386** _(upstream-verified)_ — SELECT probe (plans) is executed without a tenant_id constraint when args.tenantId is omitted, allowing reads across tenants (IDOR).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Child-plan lookup runs inside decidePlan's revise-idempotency branch, gated by the same route ownership check at agentic-policy.ts:113; args.planId is the verified plan id.
- **server/minds-engine.ts:214** _(upstream-verified)_ — INSERT into mind_tickets builds an array literal via string interpolation: `{${params.dependsOn.join(",")}}` using caller-supplied params.dependsOn. This is a SQL-construction pattern with request-derived data (injection + isolation risk).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): dependsOn is typed number[] (MindTicket contract) so join() yields a numeric literal; tenant_id is a bound param in the same INSERT.
- **server/minds-engine.ts:262** _(upstream-verified)_ — checkDependenciesMet uses id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) building an array literal from ticket.dependsOn. The array content may originate from untrusted input and is injected into SQL text.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): typed number[] joined into an ::int[] cast, passed as a bound param, AND the query carries tenant_id = ${tenantId}.
- **server/minds-engine.ts:327** _(upstream-verified)_ — markEventsHandled builds a Postgres array literal from caller-provided eventIds: `id = ANY(${`{${eventIds.join(",")}}`}::int[])`. This is string-interpolated SQL using request-derived data (injection risk) even though tenant_id is present.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): eventIds is typed number[]; the array literal is a bound ::int[] param and the UPDATE carries AND tenant_id = ${tenantId}.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without tenant_id — reads a tenant-scoped project without validating that the caller's tenant owns it.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — backfillProjectBrains: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... has no tenant_id filter — enumerates projects across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is done without tenant scoping — may read conversations from other tenants' projects.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant_id filter — reads messages across tenants if conversation ids overlap or are attacker-controlled.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/routes.ts:1118** _(upstream-verified)_ — Ownership check queries file_storage rows by filename across all tenants (SELECT tenantId FROM file_storage WHERE filename = <baseName>) with no tenant_id WHERE clause. This reads cross-tenant data to decide authorization and may leak existence and ownership information between tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT of owner tenantIds IS the isolation check — non-owners get 404 on the next line.
- **server/routes.ts:3443** _(upstream-verified)_ — Message-feedback handler SELECTs from messages by id without scoping the query to the caller's tenant (SELECT conversation_id, tenant_id FROM messages WHERE id = ${messageId}). The code then compares the returned tenantId to the caller's tenant — but the initial unscoped read exposes cross-tenant rows and may leak existence or data before authorization is applied.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the SELECT fetches the row's tenantId and the very next line returns 403 on mismatch — the query IS the ownership check.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE sculptor_sessions sets review_result WHERE id = ${sessionId} with no tenant_id filter — writes are not scoped to the caller's tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — settleFusionReservation runs UPDATE agent_cost_ledger ... WHERE id = ${reservationId} with no tenant_id constraint. This may allow settling (modifying) ledger rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — releaseFusionReservation runs UPDATE agent_cost_ledger ... WHERE id = ${reservationId} without scoping by tenant_id, allowing cross-tenant ledger modifications.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:170** _(upstream-verified)_ — detectStagnation performs a SELECT on the tenant-scoped `experiments` table but only conditionally includes tenant_id. If tenantId is not provided the query will return rows across all tenants (cross-tenant data exposure).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:511** _(upstream-verified)_ — getExperimentHistory can return experiments without a tenant filter when tenantId is omitted — it issues an unscoped SELECT FROM experiments, allowing cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:242** _(upstream-verified)_ — getConversationUnscoped performs a SELECT FROM conversations without constraining by tenant_id (no WHERE tenant_id = ...). This is an unscoped read against a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:309** _(upstream-verified)_ — createMessage selects the conversation row by id with db.select(...).where(eq(conversations.id, data.conversationId)) with NO tenant filter. Caller-supplied conversationId is used to derive tenantId without first verifying the caller's tenant context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): createMessage lookup only reads conversations.tenantId (int) and the message is stamped with the conversation's real tenant (storage.ts:325); a caller-supplied tenantId that mismatches throws (storage.ts:320-323). No cross-tenant data returned or wrong-tenant write.
- **server/storage.ts:512** _(system-sweep)_ — getAllMemoriesForBackup executes db.select(...).from(memory_entries) without any tenant filter; it returns memory entries across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/storage.ts:395** _(internal-worker-pk)_ — attachCitations issues a raw UPDATE messages SET citations = ... WHERE id = ${messageId} with NO tenant check. This allows citations to be written to message rows without validating tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:805** _(internal-worker-pk)_ — updateMemoryEmbedding does db.update(memory_entries).set({ embedding }).where(eq(memory_entries.id, id)) with NO tenant constraint. This updates memory rows by id without ensuring tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:817** _(internal-worker-pk)_ — updateKnowledgeEmbedding updates agent_knowledge via db.update(...).where(eq(agent_knowledge.id, id)) with NO tenant constraint, allowing cross-tenant mutation of knowledge embeddings by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — stampArgsEmbedding executes UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL — there is NO tenant_id constraint. stampArgsEmbedding lacks tenant context and can update another tenant's row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — scoreProposalSurprise updates felix_proposals actual_outcome_embedding using WHERE id = ${proposalId} without a tenant check (UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId}). This writes to a tenant-scoped table by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — scoreProposalSurprise updates surprise_score and surprise_band via UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} with NO tenant predicate.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/tools.ts:6927** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (video upload) does not include tenant_id. The code verifies project ownership but the INSERT omits tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/tools.ts:6914 — The same insert is preceded by the explicit project ownership guard at server/tools.ts:6912-6914. project_files is not tenant_id-bearing in the actual schema (shared/schema.ts:1199-1209), so omission of tenant_id is not a cross-tenant write by itself; ownership is inherited from project_id.
- **server/tools.ts:6927** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) is executed without explicitly writing tenant_id even though project_files is tenant-scoped (and tenant row-level security is NOT enforced in PG).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/tools.ts:6914 — The same insert is preceded by the explicit project ownership guard at server/tools.ts:6912-6914. project_files is not tenant_id-bearing in the actual schema (shared/schema.ts:1199-1209), so omission of tenant_id is not a cross-tenant write by itself; ownership is inherited from project_id.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is performed without constraining by tenant_id. The code loads all pairing rows for a phone across all tenants (`WHERE category = 'messaging_pairing' AND title = ...`) and then attempts to filter/verify them in app logic — this violates the invariant that every read against a tenant-scoped table must include a tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` is performed by conversation id without constraining by the caller/expected tenant. The code queries the conversations table by id to verify the pairing, but the query does not include a tenant_id filter, so it reads a tenant-scoped table without the required tenant constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
- **server/minerva-planner.ts:367** _(deferred)_ — Conditional CAS UPDATE on plans may execute without a tenant_id constraint when args.tenantId is omitted, enabling cross-tenant status flips (IDOR).
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): decidePlan CAS UPDATE only constrains tenant_id when args.tenantId != null — optional-tenant fail-open; hardening = make tenantId mandatory on the decision path.
- **server/minerva-planner.ts:548** _(deferred)_ — getPlan allows a branch that SELECTs FROM plans without any tenant_id constraint when tenantId is omitted, enabling cross-tenant reads (IDOR).
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
