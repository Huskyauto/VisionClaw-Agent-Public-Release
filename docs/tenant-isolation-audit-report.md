# Tenant-Isolation Audit Report

_Generated 2026-08-06T21:42:12.412Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **587/587** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **302** (CRITICAL 68 · HIGH 180 · MEDIUM 52 · LOW 2)
- Precision second-pass (`gpt-5.4`): **236** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (68)

- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` performed with WHERE eq(agentActivity.id, activityId) only — no tenant_id constraint. This allows updating another tenant's row by id.
  - _Fix:_ Require and pass the caller's tenantId into completeActivity and include it in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))).
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} without scoping by tenant_id. Missing tenant filter on an UPDATE can modify other tenants' data.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. add AND tenant_id = ${tenantId}) or otherwise validate the eval row belongs to the caller's tenant before updating.
- **server/agentic-engines.ts:243** — UPDATE on tenant-scoped table `ai_insights` (autoApplyEligibleInsights) performs a bulk update across all tenants (no tenant_id in WHERE).
  - _Fix:_ Restrict the UPDATE to a tenant (add tenant_id = ${tenantId}) or iterate tenants and apply per-tenant. Avoid global writes to tenant-scoped tables.
- **server/agentic-features.ts:525** — UPDATE on tenant-scoped table `lead_enrichments` uses WHERE id = ${lead.id} only (no tenant_id constraint). This can update another tenant's lead row by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or verify ownership before the update.
- **server/agentic-features.ts:751** — UPDATE on tenant-scoped table `outreach_enrollments` uses WHERE id = ${row.id} only (no tenant_id constraint). Missing tenant filter on UPDATE may modify other tenants' rows.
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or otherwise validate the row belongs to the tenant before updating.
- **server/agentic/approvals.ts:135** — Bulk UPDATE on tenant-scoped table `agent_approvals` to expire stale approvals is performed without a tenant_id predicate — writing across all tenants.
  - _Fix:_ Either accept and require a tenantId and run expiry per-tenant, or add tenant filtering to the UPDATE. Do not perform unscoped writes on tenant-scoped tables.
- **server/agentic/resume-worker.ts:66** — Unscoped SELECT against tenant-scoped tables (agent_runs / agent_approvals) — the query at line ~66 scans/returns rows across all tenants (no WHERE tenant_id = <caller tenant>).
  - _Fix:_ Constrain this query to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId} or change the worker to iterate per-tenant), or otherwise ensure an explicit caller-provided tenant_id filter is applied. Avoid global scans of tenant-scoped tables to prevent cross-tenant access.
- **server/agentic/runs.ts:34** — appendStep() updates agent_runs without scoping by tenant_id (db.update(...).where(eq(agentRuns.id, runId))). This allows modifying a run by id across tenants.
  - _Fix:_ Require and pass tenantId into appendStep (or validate run ownership) and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Ensure all call sites provide the tenantId or resolve it atomically before updating.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) only — no tenant_id constraint, allowing cross-tenant writes/overwrites.
  - _Fix:_ Add tenantId to the function signature (or otherwise validate ownership) and include eq(agentRuns.tenantId, tenantId) in the WHERE clause to ensure tenant-scoped updates only affect the caller's tenant.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs using WHERE id = runId without tenant scoping, permitting cross-tenant state replacement.
  - _Fix:_ Require the tenantId for this operation and constrain the update with AND eq(agentRuns.tenantId, tenantId). Propagate tenant-scoped checks to all callers.
- **server/agentic/runs.ts:58** — completeRun() performs a db.update(agentRuns) WHERE eq(agentRuns.id, runId) with no tenant_id constraint — it can mark runs for other tenants completed.
  - _Fix:_ Include a tenant check in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and change function signature to accept tenantId or resolve ownership before updating.
- **server/agentic/runs.ts:88** — failRun() updates agent_runs WHERE id = runId but omits tenant scoping, enabling cross-tenant status changes.
  - _Fix:_ Add tenantId to the API and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all callers supply/verify tenant ownership before invoking failRun.
- **server/agentic/runs.ts:97** — pauseRun() updates agent_runs to status 'paused' using WHERE eq(agentRuns.id, runId) with no tenant_id filter — unscoped write across tenants.
  - _Fix:_ Require tenantId and add it to the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Update call sites to pass tenant-scoped context to prevent cross-tenant modifications.
- **server/auto-memorize.ts:73** — setWatermark() INSERTs a watermark row with tenant_id hardcoded to 1 (VALUES (..., 1, ...)). This writes a watermark into a specific tenant regardless of the caller and violates tenant isolation and the rule that INSERTs must pass tenantId explicitly.
  - _Fix:_ Do not hardcode tenant_id. Require and pass the correct tenantId into setWatermark and use VALUES (..., ${tenantId}, ...) (or the Drizzle equivalent). Ensure all INSERTs to tenant-scoped tables receive an explicit, validated tenantId.
- **server/data-protection.ts:150** — SELECT on tenant-scoped table `conversations` without tenant_id constraint (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This enumerates soft-deleted conversations across all tenants.
  - _Fix:_ Filter the SELECT by tenant (e.g. WHERE tenant_id = ${tenantId} AND deleted_at < ${cutoff}), or run the purge per-tenant. If this is an administrative global purge, require an explicit admin context and audit trail. Prefer using withTenantTx or otherwise ensure the operation is intentionally scoped and authorized before reading tenant data.
- **server/data-protection.ts:163** — DELETE FROM messages WHERE conversation_id = ${conv.id} deletes rows in a tenant-scoped table without a tenant_id predicate. This can remove another tenant's messages if conv.id isn't constrained by tenant in the DELETE itself.
  - _Fix:_ Include tenant scoping in destructive statements (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}). Better: perform the purge inside a transaction and/or with withTenantTx(conv.tenant_id, ...) so every write is executed with explicit tenant context and cannot cross tenants.
- **server/data-protection.ts:164** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} deletes tenant-scoped rows without constraining tenant_id.
  - _Fix:_ Add AND tenant_id = ${conv.tenant_id} to the WHERE clause (DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}). Consider running these deletes inside a tenant-bound transaction (withTenantTx) to avoid races and ensure policy enforcement.
- **server/data-protection.ts:165** — DELETE FROM conversations WHERE id = ${conv.id} executes a destructive operation on the tenant-scoped `conversations` table without an explicit tenant_id predicate.
  - _Fix:_ Constrain the delete by tenant (DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}). Prefer wrapping the backup+delete sequence in a tenant-scoped transaction (withTenantTx) to make the operation atomic and prevent cross-tenant races.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped tables (memory_entries|agent_knowledge) performed without constraining to tenant_id (storeEmbeddingVec). This UPDATE sets embedding_vec/embedding by id only (WHERE id = ${id}) and can modify rows across tenants.
  - _Fix:_ Require and pass an explicit tenantId to storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Alternatively, verify ownership of the row before updating (SELECT tenant_id FOR UPDATE) or restrict this operation to a trusted admin path that enforces an explicit admin tenant constant and audit logs.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} — UPDATE executed without tenant_id constraint. This can overwrite other tenants' rows when ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or verify the row's tenant_id before updating. Prefer passing tenantId into backfill routines and enforcing eq(memory_entries.tenant_id, tenantId) in all writes.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} — UPDATE executed without tenant_id constraint and can modify rows belonging to other tenants.
  - _Fix:_ Require tenantId and include it in the WHERE clause (WHERE id = ${row.id} AND tenant_id = ${tenantId}), or verify row ownership before performing the UPDATE. Avoid blind id-only UPDATEs on tenant-scoped tables.
- **server/heartbeat.ts:2680** — Raw UPDATE: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates heartbeat_tasks without constraining tenant_id — can change approval_status for a row belonging to another tenant if id collisions or misuse occur.
  - _Fix:_ Include AND tenant_id = ${newTask.tenantId} in the WHERE clause, or use a storage helper that enforces tenant ownership when updating approval_status. Always validate ownership before modifying tenant-scoped rows.
- **server/heartbeat.ts:3132** — Same pattern: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat) — no tenant_id constraint on update to a tenant-scoped table.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call a storage-layer function that validates tenant ownership. Never perform UPDATE/DELETE against tenant-scoped tables using only id without validating tenant_id.
- **server/lib/ideabrowser-backfill.ts:49** — Query reads the tenant-scoped `projects` table without a tenant_id filter (SELECT tenant_id, count(*) ... FROM projects ... GROUP BY tenant_id). This scans/aggregates across all tenants and breaks the per-tenant isolation invariant.
  - _Fix:_ Avoid scanning projects across tenants. Constrain the query to the caller tenant (e.g. WHERE tenant_id = ${tenantId}) or compute counts per-snapshot-tenant by iterating the snapshot's tenant list. Do not perform unscoped reads over tenant-scoped tables.
- **server/memory-graph.ts:183** — refreshCategoryCount updates memory_categories.memory_count using a subquery that counts memory_entries without constraining by tenant_id. The computed count can include rows from other tenants, corrupting per-tenant counters and leaking cross-tenant information.
  - _Fix:_ Include tenant scoping in the subquery (and preferably also in the outer UPDATE) — e.g. add AND tenant_id = ${tenantId} to the SELECT count(*) subquery, and change the function signature to accept and use the tenantId so the UPDATE only considers the given tenant's memory_entries. Example: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status = 'active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/minerva-planner.ts:367** — CAS UPDATE on plans conditionally omits tenant filter. The UPDATE uses a template that only adds AND tenant_id = ${args.tenantId} when args.tenantId is provided — if tenantId is omitted this UPDATE can modify a plan row across tenants (no tenant_id constraint).
  - _Fix:_ Do not allow an unconditional path that executes UPDATE plans ... WHERE id = ${planId} without tenant scoping. Require a validated tenantId for all external/route callers and include AND tenant_id = ${tenantId} unconditionally, or perform an explicit ownership check (SELECT tenant_id = ?) before mutating. Failing closed (throw on missing tenantId) is recommended.
- **server/minerva-planner.ts:472** — Rollback UPDATE flips fields on plans without any tenant constraint. The error-recovery UPDATE (rolling status back to 'awaiting_approval') issues WHERE id = ${args.planId} AND status = 'revising' with no tenant_id check, allowing an attacker or buggy internal flow to update another tenant's plan.
  - _Fix:_ Never execute UPDATEs on tenant-scoped tables without a tenant_id WHERE clause. Include AND tenant_id = ${tenantId} (and require tenantId), or fetch and validate the row's tenant_id before issuing the rollback. Failing closed (do not perform rollback if tenantId is missing/unverified) is safer.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does NOT set tenant_id. The INSERT lists (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) but omits tenant_id even though tenantId is available via options. This violates the invariant that every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT column list and VALUES (e.g. tenant_id => ${options.tenantId}). Prefer explicit column binding rather than relying on asserts alone (assertProjectInTenant is good for ownership checks but still must not replace writing tenant_id). Example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...).
- **server/mpeg-engine.ts:1417** — INSERT into tenant-scoped table `project_files` in the parallel producer does NOT set tenant_id. The INSERT omits tenant_id even though assertProjectInTenant is called and options.tenantId is available.
  - _Fix:_ Same fix as above: include tenant_id explicitly in the INSERT (e.g. VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure the tenant_id value is passed from the caller and not derived from untrusted input.
- **server/plan-executor.ts:117** — UPDATE on tenant-scoped table `plans` (execution_log) is performed without constraining by tenant_id (WHERE id = ${planId} only). This allows cross-tenant modification of plan rows.
  - _Fix:_ Include the plan's tenant identity in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Alternatively fetch/verify the plan row first and then use its tenant_id when updating so every write is scoped to the owning tenant.
- **server/plan-executor.ts:513** — CAS UPDATE that claims a plan (SET status='executing' WHERE id = ${planId} AND status = 'approved') does not include tenant_id. Claiming by id alone can allow an attacker/process to claim and execute another tenant's plan.
  - _Fix:_ Add tenant scoping to the CAS UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved'), or validate the plan's tenant before performing the state transition.
- **server/plan-executor.ts:728** — Final status UPDATE on `plans` (SET status = ${finalStatus} ... WHERE id = ${planId}) is not scoped by tenant_id. This atomically writes execution results across tenants when planId is global.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${planTenantId}). Prefer validating the plan row's tenant before composing/updating execution_log and status.
- **server/process-governor.ts:977** — UPDATE governance_rules SET ... WHERE id = ${rule.id} lacks tenant_id constraint — may update another tenant's governance rules.
  - _Fix:_ Constrain the update by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) to enforce tenant isolation on mutating governance rules.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` uses only `id = ${r.id}` in WHERE and omits tenant_id. This allows the runner (which enumerates rows across tenants) to accidentally update a row belonging to a different tenant if id collisions occur or if an attacker can influence ids.
  - _Fix:_ Include the tenant_id in the WHERE clause for the UPDATE. For example: `UPDATE agent_knowledge SET ... WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}`. Prefer passing the tenant id from the fetched row (r.tenant_id) and enforce it on all updates/deletes performed by the global runner.
- **server/reference-learner.ts:291** — UPDATE against tenant-scoped table `memory_entries` uses only `id = ${storedId}` in WHERE and omits tenant_id. Although the id was discovered via a tenant-scoped SELECT earlier, the UPDATE must still enforce the tenant constraint at write time to prevent cross-tenant modification if code paths change or ids are re-used.
  - _Fix:_ Constrain the UPDATE by tenant as well. For example: `UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId}` (or verify the tenant of the row before updating) so writes are always scoped to the tenant.
- **server/routes/conversations.ts:103** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Fix:_ Include the tenant_id column in the INSERT and pass the caller's tenant (e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`). If the table design differs, verify and enforce tenant ownership before inserting.
- **server/routes/conversations.ts:230** — INSERT into 'project_notes' omitted tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` (likely a tenant-scoped table)
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`) or otherwise ensure the insert records the correct tenant ownership before persisting.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions sets status/result using WHERE id = ${session.id} but omits tenant_id — write to a tenant-scoped table is not constrained to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. add AND tenant_id = ${params.tenantId} or the appropriate tenantId variable). Alternatively, pass and use a trusted tenantId and include it in the WHERE clause so the update cannot touch other tenants' rows.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions sets conversation_id using WHERE id = ${session.id} with no tenant_id constraint. This can modify sessions across tenants if an id is forged or guessed.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the row's tenant ownership before updating.
- **server/sculptor.ts:168** — monitorSession performs UPDATE sculptor_sessions ... WHERE id = ${sessionId} but does not constrain by tenant_id (tenantId is available to the function). This violates tenant isolation for writes.
  - _Fix:_ Restrict the UPDATE to the tenant that owns the session (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Always include tenant_id on writes to tenant-scoped tables even when the code previously validated ownership.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} has no tenant_id constraint. Even though the session was selected earlier, every write must include an explicit tenant constraint per the isolation invariant.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise pass a trusted tenantId into this operation) so the update cannot affect rows belonging to other tenants.
- **server/second-opinion.ts:405** — settleFusionReservation issues UPDATE agent_cost_ledger ... WHERE id = ${reservationId} without constraining tenant_id. agent_cost_ledger is tenant-scoped, so this write may modify other tenants' ledger rows by id alone.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). If the function doesn't currently receive tenantId, change the API to accept a trusted tenantId (or look it up and verify ownership) and use it in the WHERE clause before updating.
- **server/second-opinion.ts:425** — releaseFusionReservation updates agent_cost_ledger SET cost_usd='0' WHERE id = ${reservationId} with no tenant_id check, allowing cross-tenant ledger modifications.
  - _Fix:_ Require and use the reservation's tenantId (or accept tenantId as a parameter) and include AND tenant_id = ${tenantId} in the WHERE clause. Never update tenant-scoped ledger rows by id alone.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) and does not constrain by tenant_id. This allows an attacker/process with an experiment id to update another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause. Pass the calling tenantId into runExperiment and change the where to .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Always enforce tenant ownership on UPDATE/DELETE by id.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) (no tenant_id constraint), allowing cross-tenant modification in failure handling.
  - _Fix:_ Same fix as for the success-path update — include eq(experiments.tenantId, tenantId) in the WHERE and ensure tenantId is provided to the function so the error-path update cannot modify other tenants' rows.
- **server/self-reflection.ts:366** — UPDATE memory_entries sets fact/access metadata WHERE id = <existing id> but does not include tenant_id in the WHERE clause. The earlier SELECT was tenant-scoped, but the UPDATE by id can modify rows belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${id} AND tenant_id = ${tenantId}. Alternatively, fetch the row and verify its tenant_id matches the caller before updating. Never update by id alone on tenant-scoped tables.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks is executed with no tenant_id constraint — this will delete rows across all tenants.
  - _Fix:_ Constrain the DELETE to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or explicitly require/validate an ALL_TENANTS sentinel. Prefer using the tenant-scope helpers (tenantScope/requireTenantScope) or iterate per-tenant so the operation cannot accidentally delete other tenants' rows.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log is executed with no tenant_id constraint — this will remove event_log rows for all tenants instead of a single tenant.
  - _Fix:_ Add a tenant filter (WHERE tenant_id = ${tenantId}) or require an explicit ALL_TENANTS sentinel. Use the tenant-scope helpers to validate and apply tenant scoping before performing destructive operations.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs is executed without a tenant_id filter (pruneOldLogs) — this prunes logs for all tenants rather than scoping per-tenant.
  - _Fix:_ Scope the DELETE to a tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...) or require explicit administrator intent via an ALL_TENANTS sentinel and strong validation. Never run broad DELETEs on tenant-scoped tables without an explicit, validated tenant scope.
- **server/stability-watchdog.ts:154** — storage.createHeartbeatLog(...) is called without passing a tenantId. If createHeartbeatLog inserts into a tenant-scoped table, the INSERT may miss tenant_id (or default to an admin tenant), causing cross-tenant writes/misattribution.
  - _Fix:_ Ensure createHeartbeatLog is always called with an explicit tenantId (derive it from the task/handle/context) or validate that storage.createHeartbeatLog enforces and sets tenant_id server-side. Prefer fail-closed: throw if tenant context is unknown, or pass tenantId through from the active task/run context.
- **server/storage.ts:1311** — getAllDataForExport performs wide SELECTs across many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, etc.) and returns all rows platform-wide with no tenant filtering or access control. This exposes every tenant's data in bulk.
  - _Fix:_ Restrict this high-risk export API to an explicit, strongly-guarded admin-only path. Require authentication and explicit approval, and/or accept a tenantId parameter to scope export to a single tenant. Log and audit any invocation; do not expose a no-tenant-scoped export as a general storage API.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` without tenant constraint. The update `.where(eq(customTools.id, tool.id))` can modify a row belonging to a different tenant if IDs are guessable/shared.
  - _Fix:_ Always include tenant ownership in UPDATEs: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Validate that `tool.id` belongs to the invoking tenant before applying updates.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table `custom_tools` without tenant constraint. `db.delete(customTools).where(eq(customTools.id, tool.id))` can delete another tenant's tool row.
  - _Fix:_ Add an explicit tenant filter on DELETEs: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Verify tenant ownership before deleting to prevent cross-tenant data loss.
- **server/tools.ts:2878** — INSERT into project_conversations does not include tenant_id (writes to a tenant-scoped table without stamping tenant).
  - _Fix:_ Explicitly set tenant_id on INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ..., ${tenantId})). Also ensure you validated both project and conversation ownership before inserting so the row cannot link cross-tenant entities.
- **server/tools.ts:2981** — INSERT INTO project_files does not set tenant_id. Tenant-scoped table writes must explicitly record tenant_id (columns are NOT NULL with no default per invariant).
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure prior ownership checks (project belongs to tenant) are performed and fail-closed if not.
- **server/tools.ts:2995** — INSERT INTO project_notes does not set tenant_id. Writing to a tenant-scoped table without tenant_id breaks the invariant and can allow cross-tenant contamination or insertion failures.
  - _Fix:_ Add tenant_id to the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ..., ..., ${tenantId})). Verify ownership of the project before inserting.
- **server/tools.ts:3020** — INSERT INTO project_conversations (link_conversation path) does not include tenant_id when creating the link. This is a write into a tenant-scoped table without stamping tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Ensure earlier assertConversationInTenant and project ownership checks are enforced before inserting.
- **server/twilio.ts:103** — SELECT from tenant-scoped table `agent_knowledge` is performed without restricting by tenant_id (WHERE tenant_id = ...). This reads pairing rows across all tenants and relies on later application logic to filter/validate, violating the invariant that every read against tenant-scoped tables must include an explicit tenant constraint.
  - _Fix:_ Restrict the initial query to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or else move this multi-tenant/administrative logic behind an explicit, well-audited admin/repair path. If the intent is to detect cross-tenant collisions, perform an explicitly-authorized system operation that documents the reason and uses safe checks (and still avoid leaking rows to a non-admin context). Always include tenant_id in WHERE for normal request-handling queries.
- **server/venture-discovery/budget.ts:156** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped ledger row by id only, without verifying tenant_id. This can modify another tenant's ledger row if a reservationId is reused or tampered with.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or first fetch the row by id and assert its tenant_id matches the expected tenant before updating. Treat reservation ids as tied to a tenant and enforce that at update time.
- **server/video-job-runner.ts:236** — recoverStaleVideoJobs performs a db.update(...) to mark stale jobs as failed without constraining by tenant_id — it will update matching rows across all tenants. Per the invariant, updates to tenant-scoped tables must include an explicit tenant filter or be run from an explicit global/admin context.
  - _Fix:_ Run per-tenant sweeps (iterate tenants and filter by tenant_id), or restrict the update to a specific tenant_id when called in a tenant-scoped context. If this is intended as a global maintenance task, ensure it's only callable from a privileged/operational context and document/authorize that behavior; otherwise add tenant scoping to the WHERE clause.
- **server/wiring-invariants.ts:244** — Query reads tool_performance without constraining by tenant_id. tool_performance appears tenant-scoped but the SELECT aggregates across all tenants.
  - _Fix:_ If this is meant to inspect a single tenant, add a WHERE tenant_id = <tenantId> (or use Drizzle .where(eq(table.tenantId, tenantId))). If this is an admin/global diagnostic, make that explicit and restrict execution to an admin context or parameterize the tenant(s) being inspected to avoid accidental cross-tenant data exposure.
- **server/wiring-invariants.ts:258** — Query SELECT count(*) FROM tool_performance (everResult) has no tenant_id filter and thus counts rows across all tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = <tenantId>) or explicitly restrict this diagnostic to an admin-only path that intentionally aggregates across tenants. Prefer Drizzle .where(eq(table.tenantId, tenantId)) for correctness and parameterization.
- **server/wiring-invariants.ts:260** — Query SELECT count(*) FROM messages WHERE created_at > ... runs against messages with no tenant filter. messages is a tenant-scoped chat table, so this reads data across tenants.
  - _Fix:_ Constrain the query by tenant_id (WHERE tenant_id = <tenantId>) or run as a documented admin/monitoring operation with explicit tenant selection. Use parameterized filters (Drizzle .where(eq(messages.tenantId, tenantId))).
- **server/wiring-invariants.ts:293** — Query SELECT DISTINCT tool_name FROM tool_performance WHERE ... reads tool_performance without tenant scoping, exposing cross-tenant usage data.
  - _Fix:_ Scope to a tenant (WHERE tenant_id = <tenantId>) or require an explicit admin context/tenant list for cross-tenant aggregation. Prefer using Drizzle query builders to add eq(table.tenantId, tenantId) explicitly.
- **server/wiring-invariants.ts:366** — Raw SQL: SELECT COUNT(*)..., MAX(...) FROM ${spec.table} is executed without any tenant_id constraint. The loop enumerates tables such as tool_performance and knowledge_nudges which are tenant-scoped, so this probes counts across all tenants.
  - _Fix:_ Include a tenant filter in the SELECT (e.g. WHERE tenant_id = <tenantId>) or, if an aggregate across tenants is intended, run only within an authorized admin path and make tenant scope explicit. Avoid building raw SQL without parameterized tenant filters; use Drizzle APIs to inject safe parameters.
- **server/wiring-invariants.ts:417** — Tool-drift query selects from tool_performance without tenant scoping (WHERE last_failure_at > ... but no tenant_id filter). This can surface per-tenant fail rates across all tenants.
  - _Fix:_ Add tenant_id = <tenantId> to the WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))). If this is an admin-level health check intended to aggregate across tenants, require explicit admin authorization and document the cross-tenant intent.
- **server/wiring-invariants.ts:535** — Raw SQL SELECT COUNT(*)..., pg_total_relation_size('${tbl.name}') FROM ${tbl.name} runs against memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) without tenant filtering — reading counts 'across all tenants'.
  - _Fix:_ For per-tenant freshness checks, add WHERE tenant_id = <tenantId>. For storage-size checks that must be global, explicitly run them only from an admin context; otherwise use parameterized Drizzle queries per tenant to avoid accidental cross-tenant inspection.
- **server/wiring-invariants.ts:561** — Query SELECT COUNT(*)::int AS cnt FROM graph_memory is executed with no tenant filter; graph_memory is tenant-scoped but the code comment even references 'across all tenants', so this reads data from all tenants.
  - _Fix:_ If you intend to check a single tenant, add WHERE tenant_id = <tenantId> (or use Drizzle .where(eq(table.tenantId, tenantId))). If a global check is required, run it only in an authorized admin context and document/limit access. Avoid unscoped selects on tenant-scoped tables.

### HIGH (180)

- **server/ab-optimizer.ts:111** — SELECT from tenant-scoped table `experiments` without restricting tenant_id (reads experiments across all tenants).
  - _Fix:_ Limit the query to a specific tenant (e.g. accept a tenantId param and add WHERE tenant_id = ${tenantId}) or iterate per-tenant so you never select tenant-scoped rows across all tenants.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting lacks a tenant_id constraint — reading pending insights across tenants.
  - _Fix:_ Run this sweep per-tenant (accept tenantId) or add a tenant_id = ${tenantId} predicate so you only select rows for the intended tenant.
- **server/agentic-engines.ts:347** — SELECT from tenant-scoped table `heartbeat_logs` in gatherOperationalData has no tenant filter (reads logs across all tenants).
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) or otherwise restrict the query to the caller's tenant.
- **server/agentic-engines.ts:451** — SELECT from tenant-scoped table `heartbeat_logs` in runPredictiveEngine (recentLogs) lacks tenant_id constraint — reading other tenants' heartbeat logs.
  - _Fix:_ Filter by tenant_id (e.g. WHERE tenant_id = ${tenantId}) so only this tenant's logs are returned.
- **server/agentic-engines.ts:520** — SELECT from tenant-scoped table `heartbeat_logs` in runOptimizationEngine (heartbeatPerformance) has no tenant filter and will aggregate logs across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (and any subqueries) to scope performance data to the tenant.
- **server/agentic-engines.ts:534** — SELECT from tenant-scoped table `heartbeat_tasks` (taskConfig) executes without tenant_id = ${tenantId}, reading tasks across tenants.
  - _Fix:_ Restrict the query to the tenant (e.g. WHERE tenant_id = ${tenantId} AND enabled = true) or run per-tenant to avoid cross-tenant reads.
- **server/agentic-features.ts:600** — SELECT checking for an existing outreach enrollment reads `outreach_enrollments` without tenant_id in WHERE — the uniqueness/existence check is not tenant-scoped.
  - _Fix:_ Include tenant_id = ${params.tenantId} in that SELECT so the check only considers enrollments for the caller's tenant.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes a cross-tenant scan without a tenant_id filter against a tenant-scoped table.
  - _Fix:_ Avoid unbounded cross-tenant reads. Either (A) restrict the query to a single tenant (e.g. WHERE tenant_id = ${tenantId}) and run the sweep per-tenant, or (B) make this an explicit admin-only operation with strong RBAC and an audited admin tenant context, and document/verify that only privileged code can call it. Ensure the application-level tenant filter is always present for non-admin flows (or adopt Postgres RLS to enforce isolation at the DB level).
- **server/agentic/wake-scheduler.ts:142** — UPDATE on agent_wake_schedules uses a sub-select that does not constrain by tenant_id. The statement claims and mutates pending wake rows across all tenants (WHERE id IN (SELECT id FROM agent_wake_schedules ...)) without a tenant_id filter, enabling cross-tenant row updates.
  - _Fix:_ Restrict the inner SELECT/UPDATE to a specific tenant, e.g. include AND tenant_id = ${tenantId} (and pass tenantId into the function) or iterate per-tenant. Ensure the UPDATE predicate itself also includes tenant_id to prevent touching rows from other tenants.
- **server/auto-memorize.ts:45** — getWatermark() reads agent_knowledge without scoping by tenant_id (SELECT ... FROM agent_knowledge WHERE category = ... AND title = ...). This can return another tenant's watermark.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE tenant_id = ${tenantId} AND category = ... AND title = ...) or otherwise ensure the function is passed and uses an explicit tenantId so reads are always constrained to that tenant.
- **server/auto-memorize.ts:62** — setWatermark() initial SELECT (SELECT id FROM agent_knowledge WHERE category = ... AND title = ...) lacks tenant_id in the WHERE clause, allowing cross-tenant reads/updates.
  - _Fix:_ Query for the existing watermark row by tenant as well (e.g. WHERE tenant_id = ${tenantId} AND category = ... AND title = ...). Ensure the tenantId is passed into setWatermark and used for all selects/updates.
- **server/auto-memorize.ts:163** — Query that reads messages (SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ... AND m.role IN ...) has no tenant_id WHERE clause and scans messages from all tenants in a single query.
  - _Fix:_ Either run per-tenant queries with WHERE m.tenant_id = ${tenantId} or otherwise ensure the caller is authorized to read across tenants. If the intent is multi-tenant processing, make authorization explicit and document it; otherwise restrict the SELECT with an explicit tenant_id parameter to enforce isolation at the SQL level.
- **server/auto-transcript.ts:128** — backfillProjectTranscripts() performs a cross-tenant scan: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 — the outer query and the subquery over messages are not scoped by tenant_id.
  - _Fix:_ When scanning/processing data across tenants in bulk, make the multi-tenant nature explicit and ensure the routine is run with appropriate admin privileges. If this code should only operate per-tenant, add WHERE c.tenant_id = ${tenantId} and scope the messages subquery similarly (or iterate tenants and run tenant-scoped queries). Avoid unscoped scans of tenant-scoped tables.
- **server/auto-transcript.ts:153** — The linked-query SELECT DISTINCT pc.conversation_id, pc.project_id, c.tenant_id ... includes subqueries over messages without tenant scoping and performs cross-tenant reads. Later the code runs UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL without constraining by tenant_id.
  - _Fix:_ Ensure the initial selection is explicitly tenant-scoped or run per-tenant. For updates, include tenant_id in the WHERE clause (e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL) to prevent accidental cross-tenant writes. Validate ownership of referenced IDs before performing updates.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id. Tenant-scoped rows must include tenant_id on INSERT to enforce isolation; currently invoice_items(...) VALUES (${newId}, ...) omits tenant_id.
  - _Fix:_ Include tenant_id in the invoice_items INSERT (and any other tenant-scoped child tables). For example: INSERT INTO invoice_items (tenant_id, invoice_id, description, quantity, unit_price, amount, sort_order) VALUES (${tid}, ${newId}, ${item.description}, ${item.quantity || 1}, ${item.unit_price}, ${amount}, ${i + 1}). Ensure the transaction uses the same tid and that all tenant-scoped tables always receive tenant_id explicitly.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` omits any tenant_id constraint. The function markReviewReused() executes `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` without ensuring the row belongs to the caller's tenant, allowing a cross-tenant modification if an attacker can guess/trigger a pending reviewId/conversationId mapping.
  - _Fix:_ Record and use the tenant context when creating the pending entry (e.g. store tenantId in PendingReuse) and require it when marking reuse. Change the UPDATE to include tenant_id in the WHERE clause (e.g. `WHERE id = ${pending.reviewId} AND tenant_id = ${expectedTenantId} AND reused = false`). Alternatively, accept/verify a tenantId parameter, fetch the row first and assert its tenant_id matches the caller's tenant before updating. Always constrain writes to tenant-scoped tables by tenant_id.
- **server/channels/kernel.ts:177** — dispatchInbound calls opts.resolveConversation(turn) and then passes the returned conversationId to processMessage without verifying that the conversation belongs to turn.tenantId. The kernel explicitly “trust[s] the adapter” for tenant resolution (see comment at line 172) and does not enforce any tenant-scoped check here. If resolveConversation or processMessage do not themselves enforce tenant-scoped WHERE tenant_id = <tenant> filters, this permits cross-tenant access/modification via a foreign id provided by the caller.
  - _Fix:_ Enforce tenant ownership before using a foreign id. Options: (1) Require resolveConversation to validate and return a conversation only after verifying its tenant_id matches turn.tenantId (and document/test this contract). (2) Have dispatchInbound perform an explicit DB lookup on the conversation and assert row.tenant_id === turn.tenantId (using a WHERE tenant_id = :tenantId clause) before calling processMessage. (3) Modify processMessage to accept tenantId and ensure every tenant-scoped DB read/write uses tenant_id = tenantId (or enable Postgres RLS). Additionally, never use sql.raw or string-interpolated SQL with request-derived values and add unit tests to cover cross-tenant attempts.
- **server/chat-engine.ts:4365** — Call to storage.attachCitations(...) omits any tenant context. If attachCitations inserts into a tenant-scoped table it does not receive an explicit tenantId here and thus risks inserting rows without tenant_id (or relying on inferred/derived ownership).
  - _Fix:_ Pass the tenant id explicitly to attachCitations (or ensure the helper requires and uses a tenantId parameter). For example: storage.attachCitations((assistantMsgRow as any).id, collectedCitations, tenantIdForScope). Ensure the implementation sets tenant_id on INSERTs and/or applies WHERE tenant_id = ? for any updates/selects.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` is executed without any tenant_id constraint (cross-tenant read).
  - _Fix:_ Avoid scanning commitments across all tenants. Either enumerate tenants first and run tenant-scoped queries (WHERE tenant_id = $t) or restrict this path to an explicit admin-only flow with appropriate access checks and remove any tenant-identifying or sensitive payloads from the result.
- **server/commitment-drafter.ts:234** — UPDATE on `commitments` performed without tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` (unscoped write).
  - _Fix:_ Do not perform unscoped UPDATEs. Remove this unscoped unclaim or add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) after validating tenant ownership. If tenantId is invalid/untrusted, skip the UPDATE and log instead to preserve tenant isolation.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table `credential_vault` lacks tenant scoping: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` (missing tenant_id in WHERE).
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. `WHERE id = ${id} AND tenant_id = ${tenantId}`, and validate tenantId before executing so the write cannot affect another tenant's rows.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table `credential_vault` lacks tenant scoping: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` (missing tenant_id in WHERE).
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. `WHERE id = ${cred.id} AND tenant_id = ${tenantId}`, and ensure tenantId is validated/derived from the authenticated caller before performing the update.
- **server/data-protection.ts:215** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} reads from the tenant-scoped `messages` table without an explicit tenant_id predicate. Although conversation ownership was checked earlier, subsequent queries should still be tenant-constrained to follow the invariant that every read carries an application-level tenant filter.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or perform the read via a tenant-scoped transaction after verifying the conversation owner. This prevents accidental cross-tenant access if the pre-check or assumptions change.
- **server/data-protection.ts:221** — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} reads compaction archives without scoping by tenant_id after verifying the conversation. This violates the rule that every read against tenant-scoped tables must include a tenant predicate.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or perform the operation inside withTenantTx to force tenant-scoped enforcement for the duration of the operation.
- **server/delivery-pipeline.ts:203** — updateDeliveryLog performs an UPDATE on tenant-scoped table delivery_logs using only the delivery id (WHERE delivery_logs.id = id) with NO tenant_id constraint. This permits cross-tenant updates if an attacker/bug supplies an id belonging to another tenant.
  - _Fix:_ Scope the UPDATE to the caller's tenant (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or accept/require tenantId and verify ownership before updating. Use a helper that always includes tenant_id in the WHERE clause.
- **server/delivery-pipeline.ts:1071** — getDeliveryByStripePayment selects from tenant-scoped delivery_logs by stripePaymentId only (WHERE stripe_payment_id = paymentId) with NO tenant_id constraint. This can leak another tenant's delivery row via a shared payment id lookup.
  - _Fix:_ Require and apply tenant scoping (e.g. .where(and(eq(deliveryLogs.stripePaymentId, paymentId), eq(deliveryLogs.tenantId, tenantId)))) or make the function accept a tenantId and enforce it. If intended to be global, document and restrict to trusted internal callers only.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken SELECTs dfy_intake_forms by token only (WHERE token = ...) with NO tenant_id constraint. A valid token could expose a form belonging to any tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise validate the caller's tenant before returning the form. If the token is intentionally global, enforce additional checks (eg. map token→tenant or store per-tenant tokens) and document the trusted caller boundary.
- **server/dfy-intake.ts:93** — submitDfyIntake updates dfy_intake_forms (UPDATE ... WHERE dfy_intake_forms.id = form.id) but does NOT constrain the UPDATE by tenant_id. Because getDfyIntakeByToken earlier did not tenant-scope, this can update another tenant's row.
  - _Fix:_ When updating, include tenant scoping in the WHERE clause (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId)))) and/or verify the fetched form belongs to the caller's tenant before performing the UPDATE.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete chunks for the same collection_id across other tenants (if collection_id collision or misuse) or otherwise allow cross-tenant deletion.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}) and verify the collection belongs to the tenant before deleting.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks embedding using WHERE id = ${row.id} with NO tenant_id constraint (UPDATE doc_chunks SET embedding = ... WHERE id = ...). This may modify another tenant's chunk if an id is reused or supplied incorrectly.
  - _Fix:_ Include tenant scoping in UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or fetch and validate the chunk's tenant before updating. Prefer multi-column WHERE including tenant_id for all updates/deletes.
- **server/embeddings.ts:688** — SELECT from tenant-scoped table 'agent_knowledge' (backfillMissingKnowledgeEmbeddings) with NO tenant_id filter — reads tenant data across all tenants.
  - _Fix:_ Scope backfill queries to a tenant (pass tenantId) or explicitly restrict/backfill only an admin-owned, documented tenant. Prefer per-tenant iteration (fetch list of tenant ids and run backfill per-tenant with WHERE tenant_id = ${tenantId}) or ensure this admin background job runs under a verified admin tenant constant and is auditable.
- **server/embeddings.ts:716** — SELECT from tenant-scoped table 'memory_entries' (backfillEmbeddingVecs) without any tenant_id filter — unscoped read across tenants.
  - _Fix:_ Run backfill per-tenant (pass tenantId) or add WHERE tenant_id = ${tenantId} to the SELECT. If this is intended to be an admin-only operation, require an explicit admin tenant argument and document/ audit that admin intent.
- **server/embeddings.ts:732** — SELECT from tenant-scoped table 'agent_knowledge' (backfillEmbeddingVecs) without tenant_id constraint — reads can leak data across tenants.
  - _Fix:_ Add tenant scoping to the SELECT (WHERE tenant_id = ${tenantId}) or run this backfill per-tenant with an explicit tenantId parameter. If global/admin behavior is intended, require an explicit admin tenant and harden the path.
- **server/felix-brain.ts:19** — Unscoped SELECT from tenant-scoped table 'agent_runs' — rehydrateTaskStateOnBoot executes a raw query selecting rows from agent_runs without a WHERE tenant_id = <tenant> constraint. This reads other tenants' agent_runs and then stores per-conversation state into a global in-memory Map keyed only by conversationId, enabling cross-tenant data leakage and collisions.
  - _Fix:_ Scope the query to a specific tenant (or iterate tenants explicitly). Example fixes: (a) pass a tenantId into rehydrateTaskStateOnBoot and add WHERE tenant_id = ${tenantId} to the sql query (or use Drizzle .where(eq(agentRuns.tenantId, tenantId))); (b) if boot must rehydrate for all tenants, enumerate known tenant IDs and run the query per-tenant inside a loop, and when caching in taskStateStore include the tenant in the key (e.g. `${tenantId}:${conversationId}`) or maintain a per-tenant store. Never query tenant-scoped tables without binding tenant_id.
- **server/google-workspace.ts:2358** — INSERT into tenant-scoped table 'presenter_slide_images' does not set tenant_id (INSERT uses session_id, slide_index, image_data, image_size, quality only).
  - _Fix:_ Include tenant_id in the INSERT values (and bind it as a parameter). Alternatively, verify the sessionId belongs to the expected tenant and derive tenant_id from that trusted check before inserting. Ensure presenter_slide_images row schema enforces tenant_id and that all writer paths supply it.
- **server/google-workspace.ts:2364** — INSERT into tenant-scoped table 'presenter_slide_images' for thumbnail variant does not set tenant_id (same INSERT/ON CONFLICT pattern for 'thumb').
  - _Fix:_ Same fix as above: include tenant_id explicitly or validate session ownership and pass the tenant id into the INSERT. Avoid relying solely on session_id without an ownership check.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks tenant_id constraint or ownership verification on sessionId. This updates a tenant-scoped resource by id only, allowing cross-tenant modification if sessionId is forged/misresolved.
  - _Fix:_ Constrain the UPDATE by tenant: e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Before any write, verify the session row belongs to the canonical tenant (or fetch the session with tenant_id and refuse if mismatch).
- **server/health-audit.ts:314** — SELECT FROM code_proposals has no tenant_id filter (SELECT id,title,status,created_at FROM code_proposals ...). code_proposals appears tenant-scoped but this query scans across all tenants.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) or explicitly run per-tenant. If this is intended as an admin/global scan, make the admin intent explicit, restrict execution to admin principals, and document that it's global; otherwise add tenant_id parameterization.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows without ensuring those rows belong to a specific tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the update via a join/subquery that ensures the ids belong to the intended tenant. If running as a global admin operation, require/verify an explicit admin context.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (SELECT id,name,created_at,next_run_at FROM heartbeat_tasks WHERE ...) has no tenant_id filter; heartbeat_tasks are likely tenant-scoped.
  - _Fix:_ Add tenant_id filtering (WHERE tenant_id = ${tenantId}) or otherwise ensure the scanner is intentionally global and restricted to admin-only execution. Prefer per-tenant scoping to avoid cross-tenant data exposure.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables tasks without constraining by tenant_id (risk of modifying other tenants' heartbeat tasks).
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform the update using a subquery that selects only tasks belonging to the intended tenant. If intended global admin action, require explicit admin authorization and make it explicit.
- **server/heartbeat-seed.ts:27** — Call to storage.getHeartbeatTasks(undefined, ALL_TENANTS) fetches heartbeat_tasks across all tenants (no tenant constraint).
  - _Fix:_ Avoid reading tenant-scoped heartbeat_tasks across all tenants. If you intend an admin-only seed, scope the query to the ADMIN tenant id (or otherwise filter by tenant_id) or use a storage API that enforces the intended tenant filter instead of ALL_TENANTS.
- **server/heartbeat.ts:53** — Raw SELECT: SELECT * FROM research_schedules ... has no tenant_id WHERE filter. This reads research_schedules for all tenants.
  - _Fix:_ Restrict the query to a tenant (WHERE tenant_id = ${sched.tenant_id} or pass an explicit tenant parameter). If you need to iterate per-tenant, query per-tenant or include tenant_id in the WHERE to enforce tenant isolation.
- **server/heartbeat.ts:102** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} has no tenant_id constraint — it can update a row belonging to a different tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}), or call a storage helper that enforces tenant ownership before updating.
- **server/heartbeat.ts:120** — In error-recovery path: UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.cron_expression} — uses no tenant_id filter and appears to compare id to cron_expression (likely a bug).
  - _Fix:_ Fix the WHERE clause to reference the correct id (WHERE id = ${sched.id}) and add tenant scoping (AND tenant_id = ${sched.tenant_id}). Use a storage helper to ensure tenant ownership and avoid accidental cross-tenant updates.
- **server/heartbeat.ts:381** — Raw SELECT: SELECT * FROM event_log WHERE status = 'pending' ... does not filter by tenant_id — it returns pending events across all tenants.
  - _Fix:_ Process events in a tenant-scoped manner. Add tenant_id = ${...} to the WHERE clause or iterate per-tenant. If multi-tenant processing across all tenants is intentional, make that explicit and ensure downstream code enforces per-event tenant checks before acting (but prefer per-tenant queries to enforce isolation at the SQL layer).
- **server/heartbeat.ts:1586** — Raw SELECT on heartbeat_logs: SELECT created_at FROM heartbeat_logs ... ORDER BY id DESC LIMIT 1 has no tenant_id filter — this may read logs belonging to other tenants.
  - _Fix:_ Scope the query by tenant (e.g. WHERE task_name = ... AND status = 'success' AND tenant_id = ${tenantId}). Prefer using storage APIs that accept tenantId and enforce the tenant filter in SQL.
- **server/heartbeat.ts:2635** — Call to storage.getHeartbeatTasks(undefined, ALL_TENANTS) inside delegation processing reads heartbeat_tasks across all tenants (no tenant constraint).
  - _Fix:_ Do not enumerate heartbeat_tasks across all tenants for delegation logic. Restrict to the caller's tenant (pass tenantId) or otherwise ensure the query includes tenant_id so task-creation/duplication/cap checks are tenant-scoped.
- **server/job-queue.ts:98** — claimDueJobs performs an UPDATE on agent_jobs (claiming jobs) without any tenant_id constraint — it can update/return rows across all tenants.
  - _Fix:_ Scope claims to a specific tenant (e.g. accept a tenant parameter and add WHERE tenant_id = ${tenantId}) or run per-tenant claim loops. If the worker must be global, ensure the update/SELECT includes tenant scoping or otherwise enforce authorization so a caller cannot cause cross-tenant job disclosure/mutation.
- **server/job-queue.ts:134** — reclaimExpiredLeases STEP 1 updates agent_jobs to failed_terminal for expired leases without filtering by tenant_id — mutates rows across tenants.
  - _Fix:_ Make lease-reclaim operations tenant-scoped (accept tenantId or run per-tenant), or otherwise ensure only authorized operator context can run global reclamation. Add WHERE tenant_id = ${tenantId} (or a safe global-only policy documented and enforced separately).
- **server/job-queue.ts:148** — reclaimExpiredLeases STEP 2 updates agent_jobs (requeueing expired rows) without any tenant_id constraint — affects jobs for all tenants.
  - _Fix:_ Same as above: restrict the UPDATE to a specific tenant (WHERE tenant_id = ${tenantId}) or process expired leases in a tenant-aware loop. Ensure multi-tenant safety when re-queueing jobs.
- **server/job-queue.ts:177** — completeJob updates agent_jobs to 'succeeded' by id without scoping to tenant_id — a caller could complete a job belonging to another tenant by id.
  - _Fix:_ Include tenant context in the WHERE clause (AND tenant_id = ${tenantId}) — derive tenantId from the caller or require it as a parameter. If called from internal worker, ensure the worker passes the job's tenantId into the DB update to fence by tenant.
- **server/job-queue.ts:220** — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} with no tenant constraint — this reads tenant-scoped table rows by id only.
  - _Fix:_ Select with tenant scope (WHERE id = ${id} AND tenant_id = ${tenantId}) or validate the caller's tenant before reading. Pass tenantId from the bound job context when performing lookups to avoid cross-tenant reads.
- **server/job-queue.ts:237** — failJob UPDATE to mark terminal failure writes agent_jobs rows (status, error, failure_class, etc.) using only id/attempts guards and no tenant_id filter — can mutate other tenants' jobs.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (AND tenant_id = ${tenantId}). Ensure the caller supplies/derives tenantId from the job-row or execution context before performing the update.
- **server/job-queue.ts:258** — failJob's retry-path UPDATE (setting next_run_at back to pending) updates agent_jobs without a tenant_id constraint — may requeue jobs across tenants.
  - _Fix:_ Scope the UPDATE to the job's tenant (AND tenant_id = ${tenantId}) or perform requeueing in a tenant-aware manner. Ensure tenantId is derived from the claimed job row and used in the UPDATE for lease-fencing.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} ... without tenant_id filtering — callers could cancel jobs belonging to other tenants by id.
  - _Fix:_ Require tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or validate the operator's scope before performing the cancellation.
- **server/job-queue.ts:283** — retryJob resets attempts/next_run_at for an arbitrary job id without scoping by tenant_id — allows retrying another tenant's job by id.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}), and require the caller to supply or derive tenantId from an authoritative source. Do not rely on id-only fencing for tenant isolation.
- **server/job-worker.ts:147** — research_code_proposal handler destructures tenantId from job.payload and passes it into generateCodeProposal without validating it against the job row's tenant (job.tenantId). The handler relies on job.payload.tenantId instead of the authoritative job.tenantId, risking cross-tenant reads/writes if payload tenant differs.
  - _Fix:_ Do not trust tenant identifiers inside payload. Derive tenantId from the claimed job row (job.tenantId) and validate/override payload. Add an explicit check: if (payload.tenantId && payload.tenantId !== job.tenantId) reject or normalize to job.tenantId before calling downstream DB/logic.
- **server/job-worker.ts:168** — research_digest handler reads tenantId from job.payload and calls generateResearchDigest(tenantId) instead of using the claimed job.tenantId. This can cause the handler to operate on a different tenant's data if payload is tampered with.
  - _Fix:_ Use the job's authoritative tenant (job.tenantId) for any tenant-scoped operations. Validate or override any tenantId found in job.payload to match job.tenantId, or require the handler to accept tenantId only from the claimed job row.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on procedureEvolutionRuns uses only the run id in the WHERE clause and omits tenant scoping (no tenant_id predicate). This updates a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or otherwise require/derive the tenantId and use it in the UPDATE. Always constrain updates to the owning tenant.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on procedureEvolutionRuns (noop_proposal path) filters only by run id and omits tenant_id. This writes to a tenant-scoped table without tenant constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (include procedureEvolutionRuns.tenantId equal to the caller's tenantId) or pass the tenantId into this code path and use and(eq(..., tenantId)) in the predicate.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE on procedureEvolutionRuns to set status='done' uses only run id in the WHERE clause and omits tenant predicate. This can modify rows across tenants.
  - _Fix:_ Constrain this UPDATE by tenant (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or otherwise ensure the tenantId is asserted and applied to the DB predicate.
- **server/lib/agent-trace.ts:124** — UPDATE agent_trace_spans in persistSpanClose filters only by span_id and omits tenant_id. This writes to a tenant-scoped telemetry table without constraining the update to the owning tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Persist the tenantId with the span open metadata (or retrieve it from a trusted source) so persistSpanClose can use it safely, and fail/abort if tenant context is unavailable rather than omitting the tenant predicate.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into `project_files` does not include tenant_id. Platform invariant requires explicit tenant_id on tenant-scoped INSERTs; omitting it can misattribute rows or leave them unscoped.
  - _Fix:_ Include tenant_id in the INSERT (and in the table schema if required), or otherwise ensure the row is atomically created with the parent project's tenant ownership (e.g. INSERT ... SELECT parent_id, tenant_id FROM projects WHERE id=${newProjectId} AND tenant_id=${tenantId}). Always set tenant_id explicitly on tenant-scoped tables.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into `project_notes` does not include tenant_id. This writes child rows without explicit tenant attribution, violating the 'explicit tenant_id on every INSERT' invariant.
  - _Fix:_ Add tenant_id to the INSERT values (or derive it atomically from the parent project row while enforcing the tenant), and ensure child tables are either unscoped or explicitly include tenant_id. Never insert tenant-scoped rows without tenant_id.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` without constraining by tenant_id. verifyApiKey reads the row by key_prefix alone, returning tenant_id for a presented token — this SELECT can be used to enumerate prefixes across tenants and violates the invariant that tenant-scoped reads must be constrained.
  - _Fix:_ Avoid selecting by prefix alone. Require proof of the full key in the WHERE clause (e.g. WHERE key_prefix = ${prefix} AND key_hash = ${expectedHash}) so the DB only returns a row when the presented token exactly matches. This derives tenant_id only after a full-match, preventing cross-tenant enumeration. Keep the query parameterized (no string interpolation).
- **server/lib/mcp-api-keys.ts:182** — UPDATE to tenant-scoped table `mcp_api_keys` without constraining by tenant_id. The best-effort touch (UPDATE last_used_at) uses WHERE id = ${row.id} only, lacking tenant scoping.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) so writes are always constrained to the owning tenant. Alternatively, perform the touch as part of the original verified SELECT/transaction or include key_hash check in the WHERE to ensure the row belongs to the presented key's tenant.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE 'due' and subsequent UPDATE operate across all tenants).
  - _Fix:_ Either run the picker per-tenant (pass a tenantId and include WHERE tenant_id = ${tenantId} in the CTE) or explicitly include tenant_id in the CTE/UPDATE join (e.g. SELECT id, tenant_id FROM scheduled_posts WHERE tenant_id = ${tenantId} ...; UPDATE ... WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id). If this cross-tenant behavior is intentional, make it explicit and ensure only an admin-scoped, audited runner may perform it and that downstream actions honour tenant scoping.
- **server/memory-graph.ts:123** — When creating a new category (fallback path) the code runs: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} without constraining by tenant_id. That can modify a category row belonging to a different tenant if IDs are mis-matched or attacker-influenced data exists.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or, even better, compute and set the count using a tenant-aware subquery. Pass tenantId into the caller so all category counters are updated only for the owning tenant.
- **server/memory-graph.ts:252** — INSERT INTO memory_links does not include any tenant identifier in the VALUES list. If memory_links is a tenant-scoped table (NOT NULL tenant_id), this either fails or results in tenant-less/cross-tenant links; if it is unscoped, it allows cross-tenant linking and loses per-tenant ownership metadata.
  - _Fix:_ Make the tenant boundary explicit: (a) add tenant_id to the memory_links schema if missing and include tenantId in the INSERT values, or (b) before inserting, verify both source and target memory_entries belong to the same tenant and then record that tenant_id with the new link. Example: INSERT INTO memory_links (source_memory_id, target_memory_id, tenant_id, link_type, ...) VALUES (${newMemoryId}, ${target.id}, ${tenantId}, ${linkType}, ...).
- **server/minerva-planner.ts:386** — Probe SELECT (SELECT status, ceo_decision FROM plans ...) conditionally omits tenant filter. The SELECT appends AND tenant_id = ${args.tenantId} only when args.tenantId is supplied, otherwise it queries by id alone, exposing possible cross-tenant reads.
  - _Fix:_ Always constrain plan reads by tenant_id for any caller that is not proven-trusted. Either require tenantId and include WHERE tenant_id = ${tenantId} or, if supporting internal trusted callers, centralize and document that trust boundary and avoid a code path that accepts unscoped external inputs.
- **server/minerva-planner.ts:403** — SELECT child plan (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...) conditionally omits tenant filter. The query only adds tenant_id when args.tenantId is provided, allowing discovery/reads of child plans in other tenants when tenantId is omitted.
  - _Fix:_ Require and validate tenantId for this probe, or perform an ownership check on the parent plan (SELECT tenant_id) before selecting child rows. Always include AND tenant_id = ${tenantId} when querying tenant-scoped tables.
- **server/minerva-planner.ts:547** — getPlan allows unscoped SELECT when tenantId is omitted. The function returns SELECT * FROM plans WHERE id = ${planId} if tenantId is undefined, enabling cross-tenant reads when callers omit tenantId.
  - _Fix:_ Require callers to pass a validated tenantId for external contexts. If the function must support internal trusted callers, make that explicit in a private/internal API and avoid exposing the unscoped code-path to route handlers. Prefer always including AND tenant_id = ${tenantId} or performing an ownership check before returning the row.
- **server/minerva-planner.ts:516** — listPlans defaults tenantId to 1 (admin) when args.tenantId is omitted: const tenantId = args.tenantId ?? 1. This silently scopes queries to the admin tenant for callers that forget to pass tenantId, causing misfiled reads (and potential data exposure).
  - _Fix:_ Do not default to the admin tenant. Require and validate tenantId for tenant-scoped listing functions and fail (throw) if missing. If a default is needed for admin-only internal tasks, require a clearly separate admin-only API and avoid implicit defaults in general-purpose functions.
- **server/model-catalog.ts:712** — runCatalogSync defaults tenantId to 1 (admin) when opts.tenantId is omitted and logs a warning. Subsequent SELECT/INSERT against memory_entries use that tenantId, so callers that forget to supply tenantId will write admin-scoped rows.
  - _Fix:_ Do not silently default to the admin tenant. Require callers to supply a validated tenantId or separate admin-only code paths. Fail closed (throw) when tenantId is missing for tenant-scoped writes/alerts, or require an explicit opt-in parameter that is only set by privileged callers.
- **server/model-catalog.ts:811** — INSERT INTO memory_entries uses ${tenantId} that may have been defaulted to admin (1). If runCatalogSync was called without an explicit tenantId, new memory_entries are written into the admin tenant, misfiling tenant-scoped alerts/records.
  - _Fix:_ Ensure runCatalogSync cannot proceed with an implicit admin tenant. Require and validate tenantId before performing INSERTs into tenant-scoped tables. If admin writes are intended, use a distinct admin-only API and make the behavior explicit; otherwise fail when tenantId is missing.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table `oauth_subscriptions` without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads across tenants with no tenant constraint.
  - _Fix:_ If this is intended only for admin/global maintenance, gate the call with proper platform-admin authorization and add a clear comment. Otherwise add WHERE tenant_id = ${tenantId} (or equivalent parameter binding) to scope results to the caller's tenant.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id FROM `oauth_subscriptions` WHERE provider = 'google-workspace' has no tenant_id constraint. It enumerates tenant rows globally and then iterates tenant ids — this reads tenant-scoped data without scoping to a caller tenant.
  - _Fix:_ If this is an intentional global admin/repair routine, ensure it is strongly authorization-checked and documented. If not, add a tenant_id filter or otherwise restrict which tenant ids are queried. Prefer explicit tenant scoping (WHERE tenant_id = ${tenantId}) for per-tenant operations.
- **server/oauth-subscriptions.ts:1258** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — reads all active subscriptions across tenants with no tenant_id constraint.
  - _Fix:_ Either restrict to a specific tenant (WHERE tenant_id = ${tenantId}) or ensure this routine runs only in a privileged, audited admin context. Document and gate global operations; avoid unscoped reads in paths reachable by non-admin callers.
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — global scan across tenants (no tenant_id filter).
  - _Fix:_ As above: if this is intended as a global operation, require and assert admin privileges / operator context. Otherwise add tenant_id filtering. Avoid unscoped queries against tenant-scoped tables in normal request paths.
- **server/pace-control.ts:85** — SELECT COUNT(*) FROM heartbeat_logs ... WHERE created_at > ... AND ${laneFilter} — query against tenant-scoped table `heartbeat_logs` with NO tenant_id constraint. This counts runs across all tenants instead of scoping to a tenant.
  - _Fix:_ Require and accept a tenantId (or otherwise determine tenant context) and add WHERE tenant_id = ${tenantId}. Ensure pace checks are performed per-tenant so one tenant cannot affect another's pacing/quota.
- **server/pace-control.ts:140** — Global SELECT over `heartbeat_logs` in getPaceSnapshot: SELECT COUNT(*) ... WHERE created_at > NOW() - (...) — no tenant_id filtering; returns global usage across tenants.
  - _Fix:_ If the snapshot is intended per-tenant, add WHERE tenant_id = ${tenantId} and accept a tenantId parameter. If a global operator view is intended, ensure this path is restricted to admins and documented accordingly.
- **server/pace-control.ts:147** — SELECT COALESCE(persona_name, 'unknown') AS persona_name, COUNT(*) FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name — this groups heartbeat logs across all tenants (no tenant_id filter).
  - _Fix:_ Scope the aggregation to a tenant by adding WHERE tenant_id = ${tenantId}, or gate this function to admin-only users. Per-tenant grouping should include tenant_id in the WHERE clause to enforce tenant isolation.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — write to tenant-scoped `plans` table without tenant_id filter when rejecting empty plans.
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). If tenantId is not available at that point, re-query the plan and use its tenant_id to scope the update.
- **server/plan-executor.ts:762** — Error-path UPDATE (SET status = 'failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id, allowing cross-tenant state changes in plans on error handling.
  - _Fix:_ Scope the UPDATE by tenant_id (WHERE id = ${planId} AND tenant_id = ${planTenantId} AND status='executing') or otherwise verify tenant ownership before changing status.
- **server/plan-executor.ts:778** — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' without any tenant constraint (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...). This is a global write across tenants.
  - _Fix:_ Either run recovery per-tenant (iterate tenants and include tenant_id = X in the WHERE) or add tenant scoping/limits. If the operation must be global, ensure an explicit admin-only execution path with audited intent; do not perform unauthenticated cross-tenant writes.
- **server/plan-executor.ts:788** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 selects approved plans across all tenants (no tenant_id constraint) and then enqueues executePlan for them, potentially executing other tenants' plans.
  - _Fix:_ When selecting pending plans to kick off, include tenant scoping (e.g. WHERE status='approved' AND tenant_id = ${tenantId}) or enumerate tenants and select per-tenant. Ensure executePlan() is invoked only for plans in the caller's tenant context.
- **server/process-governor.ts:198** — SELECT from heartbeat_logs (response_time check) has no tenant_id constraint — reads tenant-scoped telemetry globally.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or otherwise verify the caller's tenant before reading heartbeat_logs to prevent cross-tenant data leakage.
- **server/process-governor.ts:239** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs (cascading_failures) lacks tenant_id filter — global aggregation across tenants.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status != 'success' ...) so results are limited to the caller's tenant.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not include tenant_id — mutates a tenant-scoped table by id only.
  - _Fix:_ Always include tenant scoping on writes (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or re-run ownership check before the UPDATE to ensure the task belongs to the tenant.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} executes without tenant_id filter — writes to a tenant-scoped table by id only.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or verify desk.tenant_id first) to ensure only the caller's tenant rows are updated.
- **server/project-brain.ts:135** — SELECT name,status,description FROM projects WHERE id = ${projectId} has no tenant_id constraint — may return another tenant's project.
  - _Fix:_ Require and pass tenantId and add WHERE tenant_id = ${tenantId} (or verify project ownership) before reading project records to prevent cross-tenant exposure.
- **server/project-brain.ts:332** — SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has no tenant_id scoping — enumerates projects across tenants.
  - _Fix:_ Scope the query by tenant_id (e.g. FROM projects p WHERE p.tenant_id = ${tenantId} ...) or otherwise restrict to the caller's tenant to avoid cross-tenant data leakage.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is missing tenant filtering — can expose conversations from other tenants if project lookup isn't tenant-scoped.
  - _Fix:_ Ensure the project is validated to belong to the tenant and/or include tenant_id in the conversations WHERE clause (AND c.tenant_id = ${tenantId}). Do not rely on id-only joins without tenant checks.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks tenant_id restriction — messages may be returned across tenants.
  - _Fix:_ Either include AND tenant_id = ${tenantId} on messages queries or verify the conversation belongs to the tenant before querying messages to ensure tenant isolation.
- **server/proposal-verifier.ts:33** — SELECT id,... FROM code_proposals WHERE id = ${proposalId} has no tenant_id constraint — reads proposal potentially belonging to another tenant.
  - _Fix:_ Require tenant context and add tenant scoping (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or verify ownership before reading code_proposals.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (clear rebased_at) has no tenant_id constraint — mutates another tenant's proposal row by id only.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or validate proposal ownership prior to updating tenant-scoped rows.
- **server/proposal-verifier.ts:250** — persist() UPDATE code_proposals ... WHERE id = ${proposalId} updates verification fields without tenant scoping — mutates tenant-scoped table by id only.
  - _Fix:_ Add tenant filtering (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), or require caller tenant and verify ownership before updating verification fields.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status='rejected', ... WHERE id = ${proposalId} in rebase flow lacks tenant_id protection — can change other tenants' proposals.
  - _Fix:_ Constrain the UPDATE with tenant_id (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or perform an ownership check before mutating proposal rows.
- **server/proposal-verifier.ts:396** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (hallucinated case) has no tenant filter — mutates proposals across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} or otherwise assert the proposal belongs to the calling tenant prior to updating tenant-scoped records.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, ... WHERE id = ${proposalId} (persist rebased diff) lacks tenant_id constraint — may overwrite another tenant's proposal.
  - _Fix:_ Always scope such updates by tenant (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or validate ownership before persisting rebased diffs.
- **server/research-engine.ts:165** — UPDATE research_sessions executes without any tenant_id constraint — this will mark running sessions as completed across all tenants (cross-tenant mutation).
  - _Fix:_ Scope the UPDATE to a specific tenant (or iterate tenants) by adding a tenant_id filter in the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND status = 'running' ...) or compute and apply per-tenant updates. If the intent is to exclude activeSessions only, include both id AND tenant_id checks to avoid touching rows from other tenants.
- **server/research-engine.ts:400** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() is executed without a tenant_id filter — this will delete agent_knowledge rows for all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()), or run the cleanup per-tenant. If global cleanup is intended, make that explicit and protect it with a privileged admin-only path.
- **server/routes.ts:167** — Startup DB query selects from tenant-scoped table `file_storage` without constraining tenant_id (no tenant filter).
  - _Fix:_ Add a tenant constraint or iterate per-tenant. If the intent is per-tenant processing, include a WHERE eq(file_storage.tenant_id, <tenant>) (or in Drizzle: .where(eq(fileStorage.tenantId, tenantId))). If the operation must touch all tenants, make it explicit and run under an admin-only/maintenance context with careful review and audit logging.
- **server/routes.ts:2143** — POST /api/presenter inserts into tenant-scoped table `presenter_sessions` using tenantId provided directly in request body (untrusted input) without verifying caller's ownership/authorization for that tenant.
  - _Fix:_ Do NOT accept tenantId from arbitrary callers. Derive tenantId from the authenticated session (getTenantFromRequest) or verify that the caller is an authorized internal service/admin for the specified tenant (e.g., strict internal key + mapping to allowed tenant). Alternatively validate the tenantId against the caller's session or require a platform-admin context before inserting. Ensure the INSERT uses an explicit eq(file.tenantId, validatedTenantId) and never trusts client-supplied tenant identifiers.
- **server/routes.ts:4333** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The INSERT at lines 4333-4336 uses: INSERT INTO project_notes (project_id, note, author) VALUES (...) — missing tenant_id column/value, so the row will be created without explicit tenant scoping (or fail if tenant_id is NOT NULL). This violates the invariant that every INSERT into tenant-scoped tables must include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and pass the caller's tenant (tenantId) explicitly. For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, call a storage-layer helper (e.g. storage.createProjectNote) that enforces tenant ownership and consistently sets tenant_id, and ensure ownership of projId was verified before inserting.
- **server/routes/admin-extra.ts:125** — Raw/interpolated SQL (db.execute(sql`...`)) is used with request-derived values (tenantId, limit) when selecting from decline_events. This mixes request data into template SQL and risks injection/isolation issues.
  - _Fix:_ Use the Drizzle query builder or parameterized queries instead of in-template interpolation. Example: await db.select().from(decline_events).where(eq(decline_events.tenantId, tenantId)).orderBy(desc(decline_events.id)).limit(limit). Also validate/normalize limit as a number before use.
- **server/routes/admin-extra.ts:154** — Raw/interpolated SQL is used with request-derived values (tenantId, status, limit) to query self_initiatives (db.execute(sql`... ${status} ... ${limit}`)). Passing a query param directly into SQL risks injection and logic errors.
  - _Fix:_ Avoid string-interpolated SQL with request inputs. Use the query builder with explicit .where(eq(self_initiatives.tenantId, tenantId)) and add conditional .where(eq(self_initiatives.status, status)) only after validating status. Use parameterized limit handling via the query API.
- **server/routes/admin-extra.ts:184** — UPDATE uses sql`` with request-derived id and tenantId (db.execute(sql`UPDATE self_initiatives ... WHERE id = ${id} AND tenant_id = ${tenantId} ... RETURNING ...`)). Although tenant_id is present, the query is built via template interpolation of request values.
  - _Fix:_ Use Drizzle's update builder to perform the UPDATE with .where(eq(self_initiatives.id, id), eq(self_initiatives.tenantId, tenantId)) and .returning(). This keeps parameterization explicit and avoids string-interpolated SQL.
- **server/routes/admin-extra.ts:216** — Multiple db.execute(s`...`) queries in this handler interpolate tenantId (and other request-derived values) directly into SQL for counts (conversations, messages, memory_entries, file_storage, etc.). These are string-interpolated SQL usages with request-derived values.
  - _Fix:_ Replace these raw SQL calls with the query builder (db.select().from(...).where(eq(table.tenantId, tenantId)).count()) or use parameterized queries. Grouping counts via safe parameterized queries avoids injection/isolation risks and improves readability.
- **server/routes/admin-extra.ts:254** — Schedule-deletion handler issues an UPDATE via database.execute(s`UPDATE tenants SET ... WHERE id = ${tenantId}`) where tenantId is request-derived and interpolated into SQL.
  - _Fix:_ Use the query builder or parameterized update: await database.update(tenants).set({ account_status: 'pending_deletion', deletion_scheduled_at: deletionDate }).where(eq(tenants.id, tenantId)). Ensure tenantId came from a trusted helper and is validated as a number.
- **server/routes/admin-extra.ts:312** — Account deletion uses multiple interpolated DELETE statements (db.execute(s`DELETE FROM ... WHERE tenant_id = ${tenantId}` / WHERE conversation_id IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`)). Request-derived tenantId and subqueries are injected into raw SQL templates.
  - _Fix:_ Use the DB abstraction or parameterized queries (or a transaction with Drizzle's delete builder) and ensure tenantId is validated. Prefer deletions like await database.delete(messages).where(in(messages.conversationId, subquery(...))) implemented safely via the query API.
- **server/routes/admin-wedges.ts:41** — Query uses db.execute(sql`... WHERE tenant_id = ${tenantId} AND ${tag} = ANY(tags)`) where tenantId (from request) is interpolated into a raw SQL template. The query mixes request-derived tenantId into template SQL.
  - _Fix:_ Use the query builder with explicit where(eq(projects.tenantId, tenantId)) and a parameterized check for tags (e.g. sql`$${tag} = ANY(tags)` replaced with a safe parameter) or use Drizzle helpers for array containment to avoid inline interpolation of request-derived values.
- **server/routes/admin-wedges.ts:53** — Pending drafts COUNT query uses db.execute(sql`... WHERE tenant_id = ${tenantId} AND wedge_slug = ${w.slug} AND status = 'pending_review'`) with tenantId interpolated from request and limit later interpolated; request-derived data is inserted into raw SQL template.
  - _Fix:_ Use the query builder (db.select()...where(eq(table.tenantId, tenantId), eq(table.wedgeSlug, w.slug), eq(table.status,'pending_review')).limit(limit)) or otherwise parameterize these values to avoid constructing SQL strings with request-derived data.
- **server/routes/admin-wedges.ts:73** — Heartbeat lookup uses db.execute(sql`... WHERE tenant_id = ${tenantId} AND (LOWER(name) ~ ${slugRe} ...)`) with tenantId and a regex string interpolated. The regex and tenantId are derived/calculated at runtime and placed into a templated SQL string.
  - _Fix:_ Parameterize tenantId via the query builder and sanitize/validate the regex before use. If using a regex match, build the clause with a parameter placeholder rather than embedding raw strings into a template literal, or use a safe abstraction that passes values as parameters.
- **server/routes/admin-zombie-detector.ts:36** — Within a transaction, multiple tx.execute(sql`...`) queries interpolate windowDays (derived from req.query) directly into SQL expressions (e.g. started_at > NOW() - (${windowDays}::int * INTERVAL '1 day')). WindowDays is request-derived and is injected into raw SQL templates.
  - _Fix:_ Validate and coerce windowDays to an integer first (already done) and prefer parameterized usage via query builder. Avoid embedding request values into raw SQL expressions; use parameter binding or computed Date arithmetic in application code, or express the interval via parameterized constructs supported by the DB library.
- **server/routes/admin.ts:514** — Admin tenant-update endpoints call db.execute(sql`UPDATE tenants SET plan = ${plan} WHERE id = ${targetId}`) (and other similar updates) with targetId and body values interpolated directly into SQL templates. These are request-derived values used in raw SQL.
  - _Fix:_ Use Drizzle's update API: await db.update(tenants).set({ plan }).where(eq(tenants.id, targetId)). This ensures proper parameterization and avoids inline string-interpolation of request values. Validate targetId and input values before applying updates.
- **server/routes/admin.ts:547** — Reset-usage endpoint performs db.execute(sql`UPDATE tenants SET trial_conversations_used = 0 WHERE id = ${targetId}`) with targetId interpolated directly into SQL from req.params.
  - _Fix:_ Use the query builder or parameterized update and validate targetId. Example: await db.update(tenants).set({ trial_conversations_used: 0 }).where(eq(tenants.id, targetId)). Ensure the caller is authorized to administer the target tenant (already gated) and that inputs are numeric.
- **server/routes/admin.ts:799** — Repair-incidents handler composes WHERE clauses using sql fragments (sql`tenant_id = ${tenantId}` and sql`source = ${source}`) then injects the composed where into a db.execute(sql`... WHERE ${where} ...`). The 'source' value is request-derived and is interpolated into the final SQL fragment.
  - _Fix:_ Avoid assembling SQL from multiple interpolated fragments with request data. Build the WHERE clause using the query builder and add conditional filters only after validating inputs. This reduces injection/isolation risk and keeps tenant scoping explicit (use eq(repair_incidents.tenantId, tenantId)).
- **server/routes/agent-insights.ts:49** — Multiple db.execute(sql`...`) calls interpolate tenantId and windowDays (from req.query) directly into raw SQL (agent_activity, agent_cost_ledger, step_rewards, agent_approvals). Request-derived values are placed inside template SQL.
  - _Fix:_ Use the query builder to express these queries (e.g., db.select().from(agent_activity).where(eq(agent_activity.tenantId, tenantId), gt(agent_activity.startedAt, sql`NOW() - ${windowDays} * INTERVAL '1 day'`))). Validate/coerce windowDays before use and avoid embedding it raw into SQL templates; prefer parameter binding or compute the cutoff timestamp in application code and pass it as a parameter.
- **server/routes/agent-insights.ts:244** — Runs list query uses db.execute(sql`SELECT ... FROM plans WHERE tenant_id = ${tenantId} ORDER BY id DESC LIMIT ${limit}`) with tenantId and limit interpolated from the request into a raw SQL template.
  - _Fix:_ Replace with the query builder and explicit parameterization: await db.select().from(plans).where(eq(plans.tenantId, tenantId)).orderBy(desc(plans.id)).limit(limit). Validate limit as an integer before use.
- **server/routes/api-v1.ts:392** — GET /api/v1/agents uses db.execute(sql`... LEFT JOIN tenant_persona_names tpn ON tpn.persona_id = p.id AND tpn.tenant_id = ${tenantId}`) where tenantId from the request is interpolated into raw SQL.
  - _Fix:_ Use the query builder and explicit .where/.join conditions with parameter binding (e.g., .leftJoin(tenant_persona_names, eq(tenant_persona_names.personaId, p.id) and eq(tenant_persona_names.tenantId, tenantId))). Avoid embedding tenantId into sql`` templates.
- **server/routes/api-v1.ts:466** — Dispatch resolution uses db.execute(sql`SELECT id, name FROM personas WHERE is_active = true AND LOWER(name) = LOWER(${agent}) LIMIT 1`) with agent (request body) interpolated into the SQL template. agent is request-derived.
  - _Fix:_ Although personas are global, avoid passing request values directly into raw SQL templates. Use the query builder with parameterized comparisons or sanitize/validate agent before the query. Example: await db.select().from(personas).where(and(eq(personas.is_active, true), sql`LOWER(name) = LOWER(${agent})`)).limit(1) — but prefer a safe API for case-insensitive match if available.
- **server/routes/api-v1.ts:654** — AG-UI persona resolution issues a db.execute(sql`SELECT id FROM personas WHERE is_active = true AND LOWER(name) = LOWER(${body.agent}) LIMIT 1`) with body.agent interpolated into the SQL template (request-derived).
  - _Fix:_ Use parameterized query builder calls and validate body.agent before use. Avoid direct interpolation inside sql`` templates for request inputs; instead pass values as parameters or use safe utility functions that build case-insensitive comparisons without string injection.
- **server/routes/api-v1.ts:777** — Conversation poll uses db.execute(sql`SELECT m.id, m.role, m.content, m.created_at FROM messages m JOIN conversations c ON c.id = m.conversation_id WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} ORDER BY m.id DESC LIMIT 50`) with conversationId and tenantId interpolated from the request.
  - _Fix:_ Use the query builder: await db.select({ ... }).from(messages).join(conversations, eq(conversations.id, messages.conversation_id)).where(eq(messages.conversation_id, conversationId), eq(conversations.tenant_id, tenantId)).orderBy(desc(messages.id)).limit(50). Validate conversationId/tenantId beforehand.
- **server/routes/api-v1.ts:889** — A2A message/send persona lookup uses db.execute(sql`SELECT id FROM personas WHERE is_active = true AND LOWER(REGEXP_REPLACE(name, '[^a-zA-Z0-9]+', '-', 'g')) = LOWER(${skillId}) LIMIT 1`) with skillId derived from request metadata and interpolated into raw SQL.
  - _Fix:_ Avoid embedding request-provided skillId into sql`` directly. Parameterize the comparison or compute the normalized persona slug in application code and then query by that normalized value using safe binding (e.g., WHERE normalized_name = ${slugParam}). Validate skillId before use.
- **server/routes/api-v1.ts:940** — A2A tasks/get SELECT of last assistant message uses db.execute(sql`... WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} AND m.role = 'assistant' ORDER BY m.id DESC LIMIT 1`) with conversationId and tenantId interpolated from request.
  - _Fix:_ Replace with safe, parameterized query builder calls and validate inputs. Example: await db.select(...).from(messages).join(conversations, eq(conversations.id, messages.conversation_id)).where(eq(messages.conversation_id, conversationId), eq(conversations.tenant_id, tenantId), eq(messages.role, 'assistant')).orderBy(desc(messages.id)).limit(1).
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table 'conversations' missing tenant_id constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (use conv.tenantId/tenantId as appropriate).
- **server/routes/conversations.ts:105** — UPDATE on tenant-scoped table 'projects' missing tenant_id constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Fix:_ Add the tenant_id filter to the WHERE clause, e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}` to ensure only the caller's tenant row is updated.
- **server/routes/inbox-notifications.ts:101** — GET /api/inbox/:id marks messages as read using UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} with NO tenant_id constraint. This can flip read state for a message belonging to a different tenant (cross-tenant update).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or perform the update via a storage helper that enforces tenant ownership before mutating. Always bind tenant_id on mutating statements.
- **server/routes/metrics.ts:63** — /metrics handler runs a query on tenant-scoped table `agent_cost_ledger` that groups by tenant_id but does not include a WHERE tenant_id = ... predicate. Even though /metrics is gated by a token, this still exposes per-tenant data without explicit tenant scoping in SQL.
  - _Fix:_ Either restrict this probe to operator-only usage (and clearly document the cross-tenant aggregate intent) or add application-level filtering to limit results to an authorized tenant. If the intent is to return per-tenant aggregates, ensure the metrics endpoint only runs in a tightly controlled operator context and consider performing aggregation in a dedicated, audited admin function. Otherwise add WHERE tenant_id = ${tenantId} or similar scoping.
- **server/routes/tenants.ts:273** — INSERT via storage.createMessage omits tenantId when adding the template system prompt to a newly-created conversation.
  - _Fix:_ Pass the resolved tenantId into createMessage (or otherwise ensure the storage layer infers and validates tenant ownership). Example: await storage.createMessage({ conversationId: conv.id, role: 'system', content: template.systemPromptPrefix, tenantId }); Validate that storage.createMessage requires/uses tenantId and enforces it server-side.
- **server/routes/tenants.ts:277** — INSERT via storage.createMessage omits tenantId when adding template starter messages to a newly-created conversation.
  - _Fix:_ Include tenantId in each createMessage call for starter messages (or validate ownership inside the storage function). Example: await storage.createMessage({ conversationId: conv.id, role: 'user', content: msg, tenantId }); Ensure storage.createMessage enforces tenant ownership before inserting.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' lacks tenant_id. heartbeat_tasks appears to be tenant-scoped elsewhere in the code (some queries use tenant_id), so this read may leak or act across tenants unintentionally.
  - _Fix:_ Scope the SELECT to the desired tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true ...) or explicitly document that this operation is intentionally global. If it should be tenant-scoped, add AND tenant_id = ${someTenantId}.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has no tenant_id constraint and may disable tasks across tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${...} (for example the admin tenant or another explicit tenant) or otherwise ensure this admin script explicitly intends to operate across all tenants. Per the tenant-isolation invariant, writes to tenant-scoped tables must include tenant_id.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id — potentially reading across tenants when heartbeat_tasks is tenant-scoped.
  - _Fix:_ Add a tenant_id condition (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or the intended tenant) or document/guard that this query is intentionally global.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant scoping and may affect multiple tenants' tasks.
  - _Fix:_ Include AND tenant_id = ${...} in the WHERE clause to limit the update to the intended tenant, or make explicit that the operation is global and acceptable for your deployment model.
- **server/seed-cleanup.ts:59** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... OR ...) has no tenant_id constraint; this reads across all tenants if heartbeat_tasks is tenant-scoped.
  - _Fix:_ Add tenant filtering (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the script intentionally operates globally; if tenant-scoped, include tenant_id in the WHERE clause.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (<name ILIKE predicates>) AND enabled = true omits tenant_id; this will update rows across tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. add AND tenant_id = ${ADMIN_TENANT_ID}) or explicitly gate this script so it only runs with a clear global admin intent. Per the project's tenant-isolation rule, writes must include tenant_id.
- **server/seed.ts:36** — Unscoped SELECT against tenant-scoped table `research_sessions`: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id filter.
  - _Fix:_ Add an explicit tenant constraint (e.g. WHERE tenant_id = ${tenantId}) or call into a tenant-scoped query helper. If using Drizzle, use .where(eq(research_sessions.tenant_id, tenantId)). Avoid global counts across tenants unless intended and safe.
- **server/seed.ts:2158** — Unscoped SELECT against tenant-scoped table `research_programs`: `SELECT id, name, model FROM research_programs WHERE is_active = true` has no tenant_id filter and will return rows across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise ensure tenant isolation before operating on the returned rows. Prefer Drizzle's eq(table.tenantId, tenantId) helper when available.
- **server/seed.ts:2163** — Unscoped UPDATE against tenant-scoped table `research_programs`: `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}` lacks a tenant_id constraint and may modify another tenant's row with the same id.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or verify the program's tenant before updating. With Drizzle use .where(eq(research_programs.id, id), eq(research_programs.tenant_id, tenantId)).
- **server/seed.ts:2515** — Unscoped SELECT against tenant-scoped table `conversations`: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)` does not filter by tenant_id and may touch conversations from other tenants.
  - _Fix:_ Scope the SELECT to the caller's tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or otherwise ensure tenant ownership before using the results. When updating/insert-creating derived rows, ensure tenant_id is passed explicitly and validated.
- **server/seed.ts:3812** — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The statement `UPDATE memory_entries SET fact = REPLACE(...) WHERE deleted_at IS NULL AND fact LIKE ...` will modify rows across all tenants.
  - _Fix:_ Constrain the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant and include tenant_id in the WHERE clause so changes only affect the intended tenant. Alternatively run this as an explicit single-tenant admin migration that verifies target tenant scope before applying.
- **server/seed.ts:3838** — UPDATE on tenant-scoped table `conversations` lacks a tenant_id constraint. The statement `UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'` will update conversations for every tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or run the update per-tenant. Ensure any migration that intends to be global is explicitly gated and audited.
- **server/seed.ts:3784** — DELETE on tenant-scoped table `heartbeat_tasks` lacks a tenant_id constraint. The statement `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'` will delete matching rows across all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${tenantId}) or perform the deletion per-tenant. If the intent is to operate only on system/admin tenant(s), explicitly filter to those tenant IDs and document the operation.
- **server/seed.ts:4051** — UPDATE on tenant-scoped table `agent_knowledge` omits tenant_id in WHERE. After selecting a row by tenant (tenant_id = 1), the code runs `UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id}` without confirming tenant ownership in the WHERE clause.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) to enforce tenant-scoped writes even when updating by primary key.
- **server/seed.ts:3984** — INSERT into `project_notes` omits tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note}, 'system:import')` does not set tenant_id; tenant-scoped tables require tenant_id to enforce isolation and may be NOT NULL.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projId}, ${tenantId}, ${note}, 'system:import')). Derive tenantId from the parent project (validate ownership) before inserting.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` may run without a tenant_id constraint in detectStagnation. If tenantId is not supplied the query uses only category and will return rows across all tenants.
  - _Fix:_ Make tenant scoping mandatory for this query: require a tenantId parameter (throw if missing) or always add eq(experiments.tenantId, tenantId) to the WHERE. Avoid running cross-tenant selects; if multi-tenant global analytics are needed implement a safe, explicit admin path.
- **server/self-improvement.ts:521** — getExperimentHistory returns db.select().from(experiments) without a tenant_id constraint when tenantId argument is not provided. That performs an unscoped read across all tenants.
  - _Fix:_ Require caller to supply tenantId or default to the caller's tenant context. If an admin/global view is required, make that explicit and gated by an admin check. Always include eq(experiments.tenantId, tenantId) for tenant-scoped reads.
- **server/stability-watchdog.ts:177** — SELECT FROM heartbeat_logs (recentLogs) is performed with no tenant_id constraint — this reads cross-tenant data and can leak tenant-specific task/error information.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE tenant_id = ${tenantId}) or otherwise validate caller intent. If the code needs to examine logs across tenants, require an explicit, auditable ALL_TENANTS mode and restrict that to admin/ops paths only.
- **server/storage.ts:298** — attachCitationMetadata issues a raw SELECT FROM messages by id list (messages WHERE id IN (...)) with no tenant_id constraint. This reads tenant-scoped rows without enforcing tenant isolation.
  - _Fix:_ Include tenant scoping in the query (JOIN or WHERE messages.tenant_id = <tenant>) or accept/propagate the tenantId from the caller and enforce it. If the caller already filtered messages, require its tenantId as an explicit parameter and assert it matches each message row before reading citations.
- **server/storage.ts:398** — attachCitations updates messages (UPDATE messages SET citations = ...) constrained only by message id, with no tenant_id constraint. This is a write to a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Require and validate tenantId for this operation and include AND tenant_id = <tenantId> in the WHERE clause. Alternatively fetch and verify the message's tenant first, then perform the update with that tenantId explicitly set.
- **server/storage.ts:533** — createMemoryEntry inserts into memory_entries without validating/ensuring an explicit tenantId value is present in the data. The schema is noted to have tenant_id NOT NULL, so this could produce unscoped writes or runtime errors.
  - _Fix:_ Validate that data.tenantId is an explicit positive integer (assertValidTenantId or similar) before inserting; fail closed if missing. If creation is allowed only from trusted contexts, require the caller to pass tenantId explicitly.
- **server/storage.ts:805** — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} without any tenant_id constraint. This writes tenant-scoped data by id only, allowing potential cross-tenant mutation if an id is controlled or leaked.
  - _Fix:_ Accept and validate tenantId (or derive it securely) and include AND tenant_id = <tenantId> in the WHERE clause before updating the embedding.
- **server/storage.ts:818** — updateKnowledgeEmbedding updates agent_knowledge SET embedding WHERE id = ${id} with no tenant_id check. agent_knowledge is tenant-scoped, so this is a tenant-isolation risk.
  - _Fix:_ Require and verify tenantId ownership of the knowledge row before updating, or include eq(agent_knowledge.tenantId, tenantId) in the WHERE clause. Fail closed if tenantId cannot be validated.
- **server/storage.ts:841** — archiveExpiredMemories performs a global UPDATE on memory_entries (UPDATE memory_entries SET status = 'archived' WHERE ... ) with no tenant scoping. This mutates rows across all tenants without tenant constraints.
  - _Fix:_ If this is intended to be a platform-wide maintenance operation, restrict execution to an admin-only process and annotate/guard it accordingly; otherwise include tenant scoping (or accept an ALL_TENANTS sentinel and use requiredTenantScope) so it cannot be invoked by a caller that lacks platform privilege.
- **server/storage.ts:858** — archiveStaleMemories executes a multi-tenant UPDATE on memory_entries (WHERE status = 'active' AND created_at <= ...) without tenant_id filtering, causing cross-tenant writes.
  - _Fix:_ Same as above: either require an explicit tenant selector (or ALL_TENANTS sentinel) and gate the operation to an admin/maintenance context, or add tenant_id filtering to the UPDATE so only the intended tenant's rows are archived.
- **server/storage.ts:1000** — getDueHeartbeatTasks runs a raw SELECT FROM heartbeat_tasks (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ...) with no tenant_id scoping. heartbeat_tasks is tenant-scoped; reading due tasks across tenants without explicit admin intent may expose or mix tenants' tasks.
  - _Fix:_ If the scheduler must process tasks across tenants, make that explicit and restrict this function to a single privileged worker; otherwise require a tenantId or an ALL_TENANTS sentinel and validate the caller's privilege before running. Avoid unscoped platform-wide reads from regular request paths.
- **server/storage.ts:1027** — claimHeartbeatTasks issues an UPDATE on heartbeat_tasks (SET last_run_at/next_run_at WHERE id = ${id} AND enabled = true ...) with no tenant check. This mutates tenant-scoped rows by id without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or ensure the caller/worker is authorized to claim tasks across tenants; prefer explicit tenant argument or an ALL_TENANTS admin-only path.
- **server/subagents.ts:248** — _spawnSubagentImpl calls storage.getConversation(parentConversationId, params.tenantId ?? ADMIN_TENANT_ID), defaulting to ADMIN_TENANT_ID when params.tenantId is not supplied. This leads to reading a conversation under admin tenant if a tenantId is omitted and can allow cross-tenant access.
  - _Fix:_ Require the caller to provide an explicit tenantId (fail-closed) or derive tenantId from an authenticated request context (do not default to ADMIN). Validate that the caller is authorized to access the parent conversation before proceeding.
- **server/subagents.ts:771** — announceResult calls storage.createMessage to post the subagent announcement using tenantId: run.tenantId ?? 1 (fallback to tenant 1/admin). If run.tenantId is undefined this will write the message under admin tenant, allowing misattribution or cross-tenant writes.
  - _Fix:_ Ensure run.tenantId is always set and valid before announcing; fail the announce if tenantId is missing. Do not use a hardcoded admin fallback — require explicit tenant context or an admin-only explicit path with strict checks.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. The query `db.select().from(customTools).where(eq(customTools.name, parsed.name))` can return rows from other tenants or leak existence.
  - _Fix:_ Constrain reads to the caller's tenant, e.g. add eq(customTools.tenantId, tenantId) to the WHERE (or otherwise validate ownership). Use tenantId in the query: .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))).
- **server/tool-learning.ts:155** — SELECT by id on tenant-scoped table `custom_tools` without tenant filter. `db.select().from(customTools).where(eq(customTools.id, inserted.id))` does not validate that the returned row belongs to the expected tenant.
  - _Fix:_ Include tenant constraint in the WHERE clause to ensure ownership: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Alternatively, fetch by id+tenant together to enforce tenant isolation.
- **server/tool-learning.ts:365** — UPDATE on tenant-scoped table `custom_tools` to increment usageCount without tenant constraint. `.where(eq(customTools.id, tool.id))` may update a row in another tenant's scope.
  - _Fix:_ Scope usage updates to the tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Prefer selecting the row with tenantId first and then updating to ensure ownership.
- **server/tools.ts:2913** — SELECT subqueries counting project_files, project_notes, and project_conversations omit an explicit tenant filter (SELECT COUNT(*) FROM project_files WHERE project_id = p.id ...). Reads against tenant-scoped tables must be constrained to the caller tenant.
  - _Fix:_ Add tenant scoping to each subquery, e.g. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) and similarly for project_notes and project_conversations — or JOIN through projects/p and assert p.tenant_id = ${tenantId} in the subquery to make tenant intent explicit.
- **server/tools.ts:2976** — Duplicate-check SELECT against project_files uses project_id and file_name/file_url but does not include tenant_id. This read can return rows belonging to another tenant if project_id is misused or not globally unique.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause of the duplicate-check queries, or otherwise derive tenant from the owning project and assert ownership before the check. Prefer parameterized tenant scoping rather than relying solely on project_id.
- **server/tools.ts:3016** — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} lacks tenant scoping. Reads against tenant-scoped tables must include tenant_id constraints.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}), or JOIN to projects and assert p.tenant_id = ${tenantId} so the lookup cannot return another tenant's row.
- **server/tools.ts:4566** — SELECT from presenter_sessions (SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...) executes without constraining by tenant_id or otherwise verifying that the found presenter_session belongs to the caller's tenant. This can allow cross-tenant lookup of presenter session tokens or other sensitive session data.
  - _Fix:_ Constrain the query to the caller's tenant or verify ownership before reading. Options: (a) add AND tenant_id = ${params._tenantId} to the WHERE clause, e.g. SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} AND tenant_id = ${params._tenantId} ..., or (b) call a helper like assertPresenterSessionInTenant(sessionId|presentationId, params._tenantId) before the SELECT and only query when it returns true. Ensure any returned tokens/URLs are only used for the caller's tenant.
- **server/tools.ts:6926** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT uses project_id but does not explicitly set tenant_id (INSERT INTO project_files (... ) VALUES (...)).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. include tenant_id column and pass params._tenantId or resolved tenant id). Prefer using Drizzle's insert API or parameterized SQL and do: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${project_id}, ...). If relying on assertProjectInTenant for ownership, still persist tenant_id to the row to enforce isolation at the data layer and to meet the NOT NULL/no-default invariant.
- **server/tools.ts:8891** — UPDATE on tenant-scoped table 'proposed_skills' is performed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This can update a row belonging to a different tenant if ids are global.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Always include tenant_id in UPDATE/DELETE/SELECT predicates for tenant-scoped tables to enforce isolation.
- **server/tools.ts:8916** — UPDATE on tenant-scoped table 'proposed_skills' (marking promoted/accepted) uses only the row id in the WHERE clause and lacks a tenant_id constraint.
  - _Fix:_ Restrict the UPDATE to the tenant by adding a tenant_id predicate (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Prefer using the tenant-scoped row returned from the original INSERT or load-and-verify ownership before modifying.
- **server/tools.ts:8933** — UPDATE on tenant-scoped table 'proposed_skills' (marking rejected) uses only the row id in the WHERE clause with no tenant_id check.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure every UPDATE/DELETE/SELECT against tenant-scoped tables enforces tenant_id = callerTenant to prevent cross-tenant modifications.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against tenant-scoped table browser_workflows without constraining to caller tenant (no WHERE tenant_id = ...). This updates last_replayed by id only, allowing cross-tenant writes if id is not owned by the caller.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. include AND tenant_id = ${tenantId} in the WHERE clause (or use Drizzle's .where(eq(table.tenantId, tenantId))). Example: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`);
- **server/tools/domains/files/handlers.ts:839** — UPDATE executed against tenant-scoped table projects without constraining to caller tenant (no WHERE tenant_id = ...). The code updates projects.updated_at by project id only, relying on earlier provenance rather than an explicit tenant filter, which violates the 'must include tenant filter' invariant.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE. E.g. await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`); or use Drizzle .where(eq(projects.id, projId)).and(eq(projects.tenantId, tenantId)). Ensure all UPDATE/DELETE/SELECT statements on tenant-scoped tables include the tenant_id check.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE against tenant-scoped table knowledge_triples does not constrain by tenant_id. The loop updates rows with WHERE id = ${row.id} but omits AND tenant_id = ${tenantId}, allowing a caller who can supply or discover an id to affect rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}`);. Also consider validating the affected row (RETURNING tenant_id) and failing if no rows were updated, or perform the read→update inside a transaction to avoid TOCTOU issues.
- **server/tools/domains/security/handlers.ts:163** — Unscoped SELECT from provider_keys (SELECT provider, LENGTH(api_key) ...) executed without a tenant_id WHERE clause. This reads a tenant-scoped table (provider_keys) without constraining to the caller's tenant, relying only on an admin-only branch — violates the invariant that every tenant-scoped read must include tenant_id = callerTenant.
  - _Fix:_ If provider_keys is tenant-scoped, add a tenant filter (WHERE tenant_id = ${tenantId}) and/or pass tenantId as a parameter to the query. If this query intentionally enumerates keys across tenants, isolate it behind an explicit owner-only administrative API, log/authorize the action, and ensure the code and policy explicitly document and justify global access; otherwise restrict to the caller's tenant.
- **server/tools/domains/security/handlers.ts:260** — Unscoped SELECT COUNT(DISTINCT provider) FROM provider_keys executed without tenant_id WHERE clause. This again queries provider_keys without constraining by tenant, leaking cross-tenant information when run by an admin-path branch.
  - _Fix:_ Restrict the query with WHERE tenant_id = ${tenantId} or otherwise scope to a specific tenant. If the intent is to allow the owner/admin to see global provider configuration, perform this only via a well-audited owner-only admin endpoint (with explicit authorization), or materialize a separate global view/table for admin reporting rather than querying tenant-scoped rows directly.
- **server/twilio.ts:117** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without a tenant_id constraint. Any SELECT against a tenant-scoped table must include tenant filtering; selecting by id alone can leak or mix tenant data if used elsewhere.
  - _Fix:_ When verifying conversation ownership, SELECT with both id and tenant_id (e.g. WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId}) or first fetch the conversation by id and then assert its tenant_id equals the expected tenant. Ensure every read from tenant-scoped tables includes an explicit tenant filter tied to the caller's context.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET cost_usd='0' ... WHERE id = ${reservationId} (releaseVentureReservation) executes a tenant-scoped update by id only, with no tenant_id constraint, allowing cross-tenant mutation if reservationId is abused.
  - _Fix:_ As with settle, accept tenantId and constrain the UPDATE to WHERE id = ${reservationId} AND tenant_id = ${tenantId}, or verify ownership of the reservation row before performing the release. Ensure all writes to tenant-scoped ledger rows validate tenant ownership.

### MEDIUM (52)

- **server/auto-asset-capture.ts:184** — Query SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... queries conversations across all tenants (no tenant_id WHERE clause). This background scan reads tenant-scoped rows without a tenant constraint.
  - _Fix:_ If this is intended to be a per-tenant operation, add WHERE tenant_id = ${tenantId} or run the job per-tenant. If the process must scan all tenants, ensure only platform-admin contexts run it and document the privilege; otherwise restrict queries to the caller's tenant.
- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} ... reads messages without an explicit tenant_id filter. While conversation_id may imply ownership, the query itself does not include tenant scoping which risks cross-tenant reads if conversation ids are attacker-controlled or mis-derived.
  - _Fix:_ Include tenant scoping in the query (e.g. JOIN to conversations and filter by conversations.tenant_id = ${tenantId} or add AND tenant_id = ${tenantId} if messages table has tenant_id). Alternatively, validate/derive the tenant from the conversation row first and use that tenant to scope the messages query.
- **server/auto-consolidation.ts:335** — SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' reads tenant-scoped memory_entries rows for all tenants with no tenant_id filter. The code then iterates tenants to run per-tenant consolidation — the initial query is an unscoped read of tenant data.
  - _Fix:_ If the code needs to enumerate tenants, ensure this operation runs only in a trusted platform process (document privileges) or move tenant enumeration into a safe admin-only path. For per-tenant workloads, prefer queries that accept a tenantId parameter and only access that tenant's rows.
- **server/auth.ts:196** — validateApiKey queries api_keys by keyHash and isRevoked but does not include tenant filtering. If api_keys is a tenant-scoped table, this SELECT reads rows without constraining to the caller's tenant.
  - _Fix:_ If api_keys is tenant-scoped, constrain the lookup by tenant (WHERE tenant_id = ${expectedTenantId}) where appropriate, or ensure api key validation is explicitly authorized as a global lookup only in admin/platform contexts. Consider making the query include tenant_id or otherwise prove the caller's tenant before returning tenant-scoped data.
- **server/auth.ts:1209** — loadSessionsFromDb executes SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions without a tenant filter, loading sessions for all tenants into the in-memory cache. This is an unscoped read of a tenant-scoped table.
  - _Fix:_ Restrict session loading to admin-only contexts, or add an explicit tenant filter to load only sessions for the relevant tenant. If global load is required for an admin operation, ensure the operation is strongly authenticated and documented as platform-level code.
- **server/chat-engine.ts:4038** — Calls to recordToolExecution(conversationId, ...) (lines ~4038 and ~4042) do not include tenant context. If recordToolExecution writes to tenant-scoped tables it may perform writes without an explicit tenant_id constraint.
  - _Fix:_ Modify recordToolExecution to accept and require the tenantId (e.g. recordToolExecution(conversationId, toolName, success, ids, tenantId)) or ensure callers pass tenantId so all INSERT/UPDATE operations explicitly set tenant_id or apply a tenant WHERE clause before reading/updating conversation-scoped rows.
- **server/chat-engine.ts:3731** — Async fire-and-forget call markReviewReused(conversationId, toolName) (capability-review) is invoked without tenant information. If that function persists a row in a tenant-scoped table it may do so without an explicit tenant_id.
  - _Fix:_ Pass tenantId into markReviewReused (or require it in the capability-review API) so the implementation can set tenant_id on INSERTs and use tenant-scoped WHERE clauses when reading/updating.
- **server/email.ts:470** — startInboxPolling/storeEmail existence check runs SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without tenant_id constraint. This reads a tenant-scoped inbox_messages table without scoping to a tenant.
  - _Fix:_ Constrain this lookup by tenant where possible (e.g. include tenant_id) or otherwise design the dedup logic to operate per-tenant; if message_id is globally unique and intended global, document and ensure message_id uniqueness invariant is enforced at the DB level.
- **server/email.ts:515** — backfillEmptyBodies SELECTs FROM inbox_messages (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id) with NO tenant_id constraint, reading tenant-scoped rows across tenants.
  - _Fix:_ Run backfill per-tenant (add tenant_id filter) or otherwise ensure the operation is explicitly intended to be global and restricted to trusted admin processes. Prefer explicit tenant scoping to prevent accidental cross-tenant data access.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from user input (vec from generateEmbedding(query)) inside SQL: sql.raw(`'${vec}'::vector`). This mixes string interpolation / raw SQL with request-derived data (injection / isolation risk).
  - _Fix:_ Avoid sql.raw interpolation of request-derived values. Instead bind the vector as a parameter (use parameterized query support or a prepared statement that accepts the vector as a parameter) or use Drizzle's parameterization APIs. If raw literal is required for pgvector, validate and cast with safe parameter binding or ensure the vector originates from a trusted/validated source and document the validation (and prefer passing tenantId as a separate explicit parameter).
- **server/embeddings.ts:559** — Another occurrence of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (embedding vector is interpolated into `'${vec}'::vector`). This is using raw/interpolated SQL with request-derived data (query → embedding).
  - _Fix:_ Same mitigation: avoid embedding user-derived data inside sql.raw(). Use parameterized bindings or Drizzle primitives that pass the vector safely as a parameter, or restructure to use a pg function that accepts a parameterized vector. Validate shape strictly before any raw interpolation and prefer adding tenant-scoped parameterization separate from any raw literal.
- **server/heartbeat.ts:215** — Call to storage.getHeartbeatTasks(undefined, ALL_TENANTS) in startup reads heartbeat tasks across all tenants (no tenant filter at the SQL level).
  - _Fix:_ If this must examine tasks across tenants for admin/system reasons, make the intent explicit and audit the code path; otherwise restrict to a specific tenant (pass tenantId) or use a storage helper that enforces the appropriate tenant scoping to avoid accidental cross-tenant effects.
- **server/heartbeat.ts:268** — Raw UPDATE: UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} does not constrain by tenant_id and can modify another tenant's task row.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call storage.updateHeartbeatTask which should enforce tenant ownership. Ensure all UPDATE/DELETE against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/heartbeat.ts:272** — Raw UPDATE: UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} lacks a tenant_id constraint.
  - _Fix:_ Include AND tenant_id = ${t.tenantId} in the WHERE clause or use the storage-layer updater that validates tenant ownership before modifying heartbeat_tasks.
- **server/heartbeat.ts:766** — Call to storage.getHeartbeatTasks(undefined, ALL_TENANTS) in maintenance reads heartbeat tasks across all tenants (no tenant filter at the SQL level).
  - _Fix:_ If the maintenance loop intends to operate per-tenant, query per-tenant or use a storage API that enforces tenant scoping. Avoid passing ALL_TENANTS unless the cross-tenant read is deliberate and safe.
- **server/job-queue.ts:320** — listJobs will execute a SELECT FROM agent_jobs without a tenant_id WHERE clause when caller does not pass tenantId — this returns jobs across all tenants.
  - _Fix:_ Require the caller to specify tenantId for tenant-scoped listing, or provide a clear admin-only path. Default to scoping by tenant_id where possible (e.g. if request context has a tenant), and avoid returning global job rows to non-admin callers.
- **server/job-queue.ts:385** — getJobStats aggregates agent_jobs across all tenants (no tenant_id filter) which produces cross-tenant statistics and reads tenant-scoped rows.
  - _Fix:_ If stats are intended per-tenant, add an optional tenantId filter and default to scoped stats. If global stats are intentional, document and gate this for operator/admin use only. Otherwise include WHERE tenant_id = ${tenantId}.
- **server/lib/bwb-job-progress.ts:141** — UPDATE videoJobs uses scope(jobId, ...) where scope may omit tenant predicate when BWB_TENANT_ID is missing. That results in an UPDATE against a tenant-scoped table keyed only by jobId (no tenant_id clause) when tenant is unknown.
  - _Fix:_ Require tenantId for these mutator operations or make scope() fail-closed when the tenantId is not available. Always include eq(videoJobs.tenantId, tenantId) in the WHERE clause, or accept an explicit tenantId parameter and enforce it; do not rely on jobId-only scoping.
- **server/lib/bwb-job-progress.ts:166** — UPDATE videoJobs in updateBwbChapters uses scope(jobId, ...) which can omit tenant_id if BWB_TENANT_ID is not set, permitting cross-tenant writes (jobId-only).
  - _Fix:_ Ensure the WHERE predicate always includes tenant_id (require env-threaded tenant or accept tenantId param). If tenant is unknown, fail closed or refuse the update instead of executing without tenant scoping.
- **server/lib/bwb-job-progress.ts:200** — SELECT videoJobs.chapters uses .where(scope(jobId)) where scope may omit tenant_id (if BWB_TENANT_ID missing). This reads tenant-scoped rows without an explicit tenant filter.
  - _Fix:_ Include tenant scoping in the SELECT predicate (eq(videoJobs.tenantId, tenantId)). Require the calling code to supply tenantId or make envTenantId() mandatory for these flows; avoid selecting from tenant-scoped tables without an explicit tenant filter.
- **server/lib/bwb-job-progress.ts:211** — UPDATE videoJobs in completeBwbJob uses .where(scope(jobId)) which may omit tenant_id when BWB_TENANT_ID is absent, allowing a tenant-less update to a tenant-scoped table.
  - _Fix:_ Require and use tenantId in the WHERE clause (e.g. scope should always include tenant predicate or throw when tenant is unavailable). Prefer failing the operation if tenant context is missing rather than doing a jobId-only update.
- **server/lib/bwb-job-progress.ts:227** — UPDATE videoJobs in bumpBwbHeartbeat uses .where(scope(jobId, { notDone: true, notFailed: true })) which can omit tenant_id if the env-threaded tenant is missing, performing tenant-unspecified writes.
  - _Fix:_ Make tenantId mandatory for heartbeat writes and include eq(videoJobs.tenantId, tenantId) in the WHERE. If tenant cannot be determined, abort the update to preserve tenant isolation.
- **server/lib/bwb-job-progress.ts:245** — UPDATE videoJobs in failBwbJob uses .where(scope(jobId, { notDone: true })) and can run without a tenant predicate when BWB_TENANT_ID is missing, resulting in tenant-unspecified writes to a tenant-scoped table.
  - _Fix:_ Ensure the WHERE clause includes the tenant predicate (eq(videoJobs.tenantId, tenantId)). Either require an explicit tenantId parameter for these calls, or make scope() throw/fail when tenant context is not available, preventing jobId-only writes.
- **server/lib/ideabrowser-backfill.ts:86** — Composes a Postgres text-array literal (`tagsLiteral`) from snapshot data and injects it into SQL (VALUES ... ${tagsLiteral}::text[]). This is string-interpolated SQL using external data.
  - _Fix:_ Stop building array literals by string concatenation. Use proper parameterized array binding (Drizzle/pg array binding or pass JSONB) or use UNNEST/VALUES to pass arrays as parameters. Validate and normalize array elements before binding.
- **server/lib/ideabrowser-score.ts:181** — Constructs a Postgres array literal from caller-supplied projectIds (`literal = `{${ids.join(',')}}`) and interpolates it into SQL (ANY(${literal}::int[])). This is string-interpolated SQL using derived data.
  - _Fix:_ Use parameterized array binding (pass the ids as a bound array/JSONB and use = ANY($1::int[]) or UNNEST) rather than composing SQL fragments. Ensure ids are validated as integers and bound as parameters to avoid injection and maintain isolation guarantees.
- **server/lib/decline-events.ts:85** — Builds a Postgres array literal (`flaggedLiteral`) from request-derived flaggedCategories and interpolates it into the SQL (VALUES ... ${flaggedLiteral}::text[]). This composes SQL text from user data instead of binding a parameterized array.
  - _Fix:_ Avoid composing array literals as SQL text. Prefer binding a JS array (if supported), using Drizzle/pg array parameter binding, or storing flagged categories as JSONB. If you must construct a literal, centralize and harden the escaping and prefer a parametrized approach to eliminate SQL injection risk.
- **server/lib/outcome-promotion.ts:158** — Global scan SELECT on tenant-scoped table `deliverable_grade_events` with no tenant_id filter. scanOutcomePromotions queries tenant_id, format, counts across all tenants (GROUP BY tenant_id, format) — this is an unscoped read against a tenant-scoped table.
  - _Fix:_ If this cross-tenant scan is intended (e.g. nightly optimizer run), document and isolate it to a dedicated admin job with appropriate access controls. Otherwise, constrain queries by tenant (WHERE tenant_id = ${tenantId}) or run the scan per-tenant via an explicit tenant loop. Ensure the code path running this query is limited to an authorized global job and audited.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` executed without filtering by tenant_id (scans all tenants).
  - _Fix:_ If this audit should be per-tenant, add a tenantId parameter and include WHERE tenant_id = ${tenantId}. If it must be global, document and gate the cross-tenant read to an admin-only context and ensure callers are allowed to see aggregated cross-tenant data; avoid returning tenant-scoped row contents to non-admin callers.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` executed without filtering by tenant_id (scans all tenants).
  - _Fix:_ Either scope the query to a specific tenant with WHERE tenant_id = ${tenantId} or explicitly require and document admin scope for global scans. Don't expose tenant-scoped rows to callers that lack permission.
- **server/lib/system-probes.ts:163** — COUNT(*) query against `messages` table executed without a tenant_id filter (db.select(...).from(messagesTable) with no WHERE tenant_id).
  - _Fix:_ If the probe is intended to be per-tenant, add WHERE tenant_id = ${tenantId}. If it intentionally aggregates across tenants, ensure it's only run in an admin/system context and that any surfaced results don't leak tenant-specific counts to unauthorized callers; alternatively, run per-tenant and aggregate in application code with explicit admin consent.
- **server/memory-graph.ts:239** — Existence check for an existing memory link uses: SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ... LIMIT 1 — there is no tenant constraint. If memory_links are intended to be tenant-scoped (or expected to be audited per-tenant), this check can return matches across tenants.
  - _Fix:_ Scope the existence check to the tenant. If memory_links has a tenant_id column, add AND tenant_id = ${tenantId}. If memory_links is intentionally unscoped, perform a join to memory_entries to ensure the referenced memories belong to the expected tenant (e.g. JOIN memory_entries me ON me.id = ml.source_memory_id AND me.tenant_id = ${tenantId}). Ensure the existence test cannot match rows from other tenants.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(lockKey) inserted into SQL: `SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`. sql.raw() with derived values can cause SQL injection or parameterization bypass if lockKey is constructed from caller-controlled input (ledgerKey).
  - _Fix:_ Avoid embedding raw strings into SQL. Compute a numeric bigint value server-side and pass it as a bound parameter instead, e.g. tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(computedNumeric)}::bigint)`), or otherwise ensure lockKey is derived via a fixed hashing function producing a safe numeric value. Document and sanitize ledgerKey before use; prefer parameter binding over sql.raw().
- **server/recurring-messages.ts:287** — When expanding scheduled messages via a persona, code INSERTs a conversation with `persona_id = ${p.expandViaPersona}` (from stored payload) but does not verify that that persona id actually belongs to the tenant (r.tenant_id). Writing a tenant-scoped row that references a foreign id supplied/stored from an unvalidated source can cause cross-tenant references or privilege escalation.
  - _Fix:_ Before creating the conversation, validate ownership of the persona id: SELECT the persona by id and tenant_id (e.g. `SELECT id FROM personas WHERE id = ${p.expandViaPersona} AND tenant_id = ${r.tenant_id} LIMIT 1`). Only use the persona_id if the row exists and belongs to the same tenant; otherwise reject or map to a safe default persona.
- **server/reference-learner.ts:292** — The code uses `personaId = input.personaId || 2` when persisting STRATEGIC_REFERENCE_V1 into `memory_entries` without validating that the supplied personaId actually belongs to the tenant. This lets a caller supply an arbitrary persona id which may not be owned by the tenant.
  - _Fix:_ Validate the persona id before using it in reads/writes. Query the personas table to ensure `persona.id = ${personaId} AND tenant_id = ${input.tenantId}` and fail or substitute a tenant-local persona if not owned. Ensure any INSERT/UPDATE that references persona_id only does so with a persona already verified to belong to the calling tenant.
- **server/research-engine.ts:480** — INSERT INTO project_notes (project_id, note, author, created_at) omits tenant_id. If project_notes is a tenant-scoped table (tenant_id NOT NULL), this writes records without an explicit tenant_id and can misattribute or fail.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)). If project_notes intentionally relies on project_id only, make the schema/contract explicit and document why tenant_id is not required.
- **server/routes.ts:1118** — /uploads middleware performs db.select(...) FROM file_storage WHERE filename = ${baseName} with no tenant constraint — it reads rows across tenants to determine ownership.
  - _Fix:_ Avoid querying all tenants' file_storage rows. Instead either: (a) require the signed URL or the authenticated tenant and query only rows with eq(fileStorage.tenantId, authedTenantId), or (b) if you must detect any owner collision, perform an admin-scoped operation protected by a strict platform-admin gate (and avoid running it in the request path), or add additional application-level checks that do not enumerate other tenants' rows. Ensure the query cannot be exploited to infer existence of files across tenants.
- **server/routes.ts:1532** — GET /api/public/stats issues SELECT COUNT(*) FROM tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id constraint — reading tenant-scoped data across all tenants in an unauthenticated public endpoint.
  - _Fix:_ If global aggregates are intended, make the admin intent explicit and ensure only non-sensitive aggregated metrics are exposed (and consider computing aggregates in a safe, pre-aggregated store). Otherwise, scope queries to the caller's tenant via WHERE tenant_id = <caller tenant> (or use ADMIN_TENANT_ID for platform-admin requests). Avoid ad-hoc cross-tenant reads in public endpoints.
- **server/routes.ts:1561** — GET /api/public/architecture runs multiple unscoped SELECTs against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) without tenant filters.
  - _Fix:_ Either restrict this endpoint to platform admins or explicitly aggregate only platform-safe metrics. For per-tenant metrics, add WHERE tenant_id = <caller tenant>. If global metrics are required, compute and serve them from a dedicated, audited aggregation process rather than ad-hoc cross-tenant selects in request handlers.
- **server/routes/conversations.ts:210** — SELECT from tenant-scoped table 'conversations' lacks tenant_id constraint: `SELECT c.project_id FROM conversations c JOIN projects p ... WHERE c.id = ${id} ... AND p.tenant_id = ${conv.tenantId ?? tenantId}` omits `c.tenant_id`
  - _Fix:_ Add an explicit tenant filter on conversations as well, e.g. `WHERE c.id = ${id} AND c.tenant_id = ${conv.tenantId ?? tenantId} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}` so the DB query is strictly tenant-scoped.
- **server/routes/goal-ledger.ts:92** — readActiveJobs() performs SELECT from tenant-scoped table `agent_jobs` without a tenant_id predicate (no WHERE tenant_id = ...). This returns rows across all tenants.
  - _Fix:_ Add an application-level tenant filter (e.g. WHERE j.tenant_id = ${tenantId}) or otherwise explicitly document and gate the cross-tenant intent. If this handler is meant to be platform-admin-only, require and verify platform-admin context and consider making the tenant-scope explicit in the query (or use an audited admin-only path).
- **server/routes/oauth-subscriptions.ts:325** — Cron refresh handler executes SELECT DISTINCT tenant_id FROM oauth_subscriptions ... to enumerate tenants (tenant-scoped table) with NO per-request tenant constraint. This intentionally queries cross-tenant data but lacks explicit application-level justification in SQL.
  - _Fix:_ If this endpoint is intended to iterate across tenants (cron), ensure it's strictly protected (it already requires CRON_SECRET) and document the cross-tenant nature. If the select must be limited to a subset, add an explicit tenant predicate. Consider moving tenant enumeration logic into a dedicated admin/cron module and ensure it is audited and protected.
- **server/routes/memory.ts:159** — In /api/memory/:id/links the code builds tenantClause = tenantId === ADMIN_TENANT_ID ? sql`TRUE` : eq(memoryEntries.tenantId, tenantId), which results in a WHERE clause that can be TRUE (i.e. no tenant_id constraint) when the caller is the ADMIN tenant. This allows an admin request to run an unscoped SELECT against tenant-scoped `memory_entries`.
  - _Fix:_ Avoid using a raw TRUE tenant-clause. If platform-admins must access cross-tenant data, make that explicit (e.g. use a separate admin-only path) and ensure the decision is reviewed/audited. Alternatively, require an explicit tenantId parameter for admin actions and pass that value into the SQL WHERE clause to avoid an implicit unscoped query.
- **server/routes/projects.ts:185** — INSERT into project_notes does not include tenant_id in the INSERT payload (project_notes row is referenced by project_id but tenant_id is not explicitly set).
  - _Fix:_ Either include tenant_id explicitly in the INSERT (VALUES (..., tenantId)) or ensure the table is intentionally non-tenant-scoped. If project_notes is tenant-scoped, change the INSERT to pass tenantId and/or validate ownership server-side before insert.
- **server/routes/projects.ts:225** — INSERT into project_files (base64 upload path) does not include tenant_id in the INSERT payload (project_files row is referenced by project_id but tenant_id is not explicitly set).
  - _Fix:_ If project_files is tenant-scoped, add tenant_id to the INSERT (VALUES (..., ${tenantId})) or otherwise ensure the storage layer enforces tenant ownership. If the table intentionally relies solely on project_id as the FK, document and verify that project_id ownership is validated prior to INSERT.
- **server/routes/projects.ts:293** — INSERT into project_files (multipart upload path) does not include tenant_id in the INSERT payload.
  - _Fix:_ As above: include tenantId in the INSERT if project_files has a tenant_id column, or ensure the code validates the project belongs to the caller and that storage enforces tenant ownership before persisting.
- **server/storage.ts:242** — getConversationUnscoped performs a SELECT on conversations without any tenant_id constraint (WHERE clause lacks tenant scoping).
  - _Fix:_ Either require the caller to pass and assert a tenantId, or restrict this function to internal-only use and add explicit audit/authorization that the caller is allowed to read that conversation. Prefer returning tenantId alongside the row only from a tightly-controlled orchestration boundary and document/lock usage sites.
- **server/storage.ts:548** — updateMemoryEntry executes an UPDATE on memory_entries using only eq(memory_entries.id, id) when tenantId is omitted. The function allows tenantId to be optional and emits a warning, which can still result in an unscoped update.
  - _Fix:_ Make tenantId required for this API (fail-closed) or enforce requiredTenantScope. If optional behavior is needed, clearly document and restrict callers; always include tenant filtering in the WHERE clause whenever tenantId can be derived or provided.
- **server/storage.ts:556** — deleteMemoryEntry updates memory_entries to set status without requiring tenantId; if tenantId is omitted the WHERE clause may only be id-based, resulting in an unscoped modify across tenants.
  - _Fix:_ Require tenantId and include eq(memory_entries.tenantId, tenantId) in the WHERE condition. If supporting an unscoped admin path, make it explicit and gated behind admin privileges/flag.
- **server/storage.ts:564** — touchMemoryEntries performs a db.update on memory_entries with only inArray(id, ids) when tenantId is not supplied. The function warns but still allows unscoped updates.
  - _Fix:_ Require tenantId (fail-closed) or otherwise ensure tenant scoping is always added to the WHERE clause. Prefer requiredTenantScope for cross-tenant-safe semantics.
- **server/storage.ts:1042** — markHeartbeatTaskRun updates heartbeat_tasks WHERE id = ${id} with no tenant constraint. This updates tenant-scoped rows without verifying tenant ownership.
  - _Fix:_ Require and validate tenantId or otherwise gate this maintenance method to an admin/worker with explicit platform privileges; include tenant filtering in the WHERE clause when appropriate.
- **server/video-job-runner.ts:208** — isCancelRequested reads videoJobs.cancelRequested using WHERE jobId = ${jobId} without a tenant_id constraint. Reads from tenant-scoped tables should include tenant filtering to avoid cross-tenant information exposure or accidental action based on another tenant's row.
  - _Fix:_ Pass tenantId into isCancelRequested and SELECT cancelRequested WHERE jobId = ${jobId} AND tenant_id = ${tenantId}; or fetch the job row and validate its tenant_id matches the expected tenant before returning the cancel flag.
- **server/video-job-runner.ts:178** — mirrorStateToDb upsert (db.insert(...).onConflictDoUpdate) upserts/updates videoJobs by jobId without constraining the update to the job's tenant_id (the onConflict update's WHERE only checks updatedAt). This can unintentionally update a row belonging to a different tenant if jobId collides or is crafted.
  - _Fix:_ Include tenant matching in the onConflict update condition (for example: where: and(eq(videoJobs.tenantId, ${row.tenantId}), lt(videoJobs.updatedAt, row.updatedAt))) or otherwise ensure the upsert will not overwrite a row owned by a different tenant. Validate the existing row's tenant_id before performing the update.

### LOW (2)

- **server/auth.ts:275** — getSession selects tenant_id/is_admin/expires_at from auth_sessions WHERE token = ${token} without also scoping by tenant. While token is a primary key, the query reads auth_sessions (which contains tenant_id) without a tenant constraint.
  - _Fix:_ If session tokens are globally unique and this code is intended to look up the tenant by token, keep but document the global nature. If the call should be tenant-scoped, add tenant_id to the WHERE clause when the caller's tenant is known, or validate the token belongs to the expected tenant before using it in tenant-scoped operations.
- **server/chat-engine.ts:4050** — Call to recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) has no tenant argument. If stuck-diagnostics records findings into tenant-scoped tables, it may do so without an explicit tenant_id or tenant-scoped WHERE filters.
  - _Fix:_ Include tenantId when calling recordToolCallForStuckDetection or change the API to require tenant context, ensuring any persistence or lookups include tenant_id in INSERTs/WHERE clauses and that ownership is validated before reading/updating related rows.

### Suppressed — confirmed false positives (236)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} without scoping by tenant_id. Missing tenant filter on an UPDATE can modify other tenants' data.
  - _Why false positive:_ evalId is created server-side by the immediately preceding INSERT and then updated by that PK within the same internal flow, not from an untrusted request boundary.
- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE on tenant-scoped table `ai_insights` (autoApplyEligibleInsights) performs a bulk update across all tenants (no tenant_id in WHERE).
  - _Why false positive:_ This is an intentional platform-wide housekeeping backfill that updates status fields across tenants and carries tenant_id per returned row for later per-row handling.
- **server/agentic-features.ts:525** _(upstream-verified)_ — UPDATE on tenant-scoped table `lead_enrichments` uses WHERE id = ${lead.id} only (no tenant_id constraint). This can update another tenant's lead row by id.
  - _Why false positive:_ lead.id comes from the earlier SELECT of lead_enrichments scoped by params.tenantId, so the updated row was already tenant-filtered in the same call path.
- **server/agentic-features.ts:751** _(upstream-verified)_ — UPDATE on tenant-scoped table `outreach_enrollments` uses WHERE id = ${row.id} only (no tenant_id constraint). Missing tenant filter on UPDATE may modify other tenants' rows.
  - _Why false positive:_ row.id is obtained from the immediately preceding SELECT on outreach_enrollments filtered by e.tenant_id = params.tenantId, so ownership is established earlier in the same flow.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE on tenant-scoped table `agent_approvals` to expire stale approvals is performed without a tenant_id predicate — writing across all tenants.
  - _Why false positive:_ This is a global expiry sweep that mutates housekeeping status on stale approvals across tenants and returns each row with its own tenantId for per-row follow-up.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Unscoped SELECT against tenant-scoped tables (agent_runs / agent_approvals) — the query at line ~66 scans/returns rows across all tenants (no WHERE tenant_id = <caller tenant>).
  - _Why false positive:_ This worker intentionally scans pending approval/run state across tenants for maintenance/resume processing and keeps tenant_id per row rather than returning mixed tenant data to a tenant-facing surface.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() updates agent_runs to status 'paused' using WHERE eq(agentRuns.id, runId) with no tenant_id filter — unscoped write across tenants.
  - _Why false positive:_ Internal server worker helper updates agent_runs by server-generated PK runId; same module uses id-only worker transitions and tenant-scoped reads for user access.
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT on tenant-scoped table `conversations` without tenant_id constraint (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This enumerates soft-deleted conversations across all tenants.
  - _Why false positive:_ Global purge sweep intentionally scans expired soft-deleted conversations across tenants and then processes each row with its own tenant_id.
- **server/data-protection.ts:163** _(system-sweep)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} deletes rows in a tenant-scoped table without a tenant_id predicate. This can remove another tenant's messages if conv.id isn't constrained by tenant in the DELETE itself.
  - _Why false positive:_ Delete occurs inside platform-wide purge loop over expired conversations; operation is housekeeping and keyed from each selected row, not tenant-facing output.
- **server/data-protection.ts:164** _(system-sweep)_ — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} deletes tenant-scoped rows without constraining tenant_id.
  - _Why false positive:_ Delete is part of cross-tenant housekeeping purge driven by per-row expired conversation selection, with no tenant data returned or mixed into one response.
- **server/data-protection.ts:165** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} executes a destructive operation on the tenant-scoped `conversations` table without an explicit tenant_id predicate.
  - _Why false positive:_ Conversation delete is in maintenance purge of expired soft-deleted rows, processed per row from prior sweep selection.
- **server/embeddings.ts:684** _(system-sweep)_ — UPDATE against tenant-scoped tables (memory_entries|agent_knowledge) performed without constraining to tenant_id (storeEmbeddingVec). This UPDATE sets embedding_vec/embedding by id only (WHERE id = ${id}) and can modify rows across tenants.
  - _Why false positive:_ Embedding vector write is a housekeeping/backfill helper updating non-content embedding fields by row PK, used by maintenance routines not tenant-facing flows.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} — UPDATE executed without tenant_id constraint. This can overwrite other tenants' rows when ids collide.
  - _Why false positive:_ Backfill loop is platform-wide maintenance updating only embedding_vec on selected memory rows, with no tenant data exposure or mixed output.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} — UPDATE executed without tenant_id constraint and can modify rows belonging to other tenants.
  - _Why false positive:_ Backfill loop is housekeeping for agent_knowledge embeddings, mutating only embedding_vec per row and not returning tenant content.
- **server/heartbeat.ts:2680** _(upstream-verified)_ — Raw UPDATE: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates heartbeat_tasks without constraining tenant_id — can change approval_status for a row belonging to another tenant if id collisions or misuse occur.
  - _Why false positive:_ Row was just created in same call path via createHeartbeatTask with explicit tenantId; subsequent id-only status update targets that newly created row.
- **server/heartbeat.ts:3132** _(upstream-verified)_ — Same pattern: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat) — no tenant_id constraint on update to a tenant-scoped table.
  - _Why false positive:_ Same-path createHeartbeatTask call includes tenantId and returns newTask.id for the row immediately updated to pending.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Query reads the tenant-scoped `projects` table without a tenant_id filter (SELECT tenant_id, count(*) ... FROM projects ... GROUP BY tenant_id). This scans/aggregates across all tenants and breaks the per-tenant isolation invariant.
  - _Why false positive:_ Boot-time backfill/reconciler intentionally aggregates counts across tenants only for maintenance short-circuiting and does not expose tenant row data.
- **server/minerva-planner.ts:367** _(internal-worker-pk)_ — CAS UPDATE on plans conditionally omits tenant filter. The UPDATE uses a template that only adds AND tenant_id = ${args.tenantId} when args.tenantId is provided — if tenantId is omitted this UPDATE can modify a plan row across tenants (no tenant_id constraint).
  - _Why false positive:_ Code comment and call pattern indicate tenant-facing routes provide tenantId, while omission is only for internal system callers operating on server-generated plan ids.
- **server/minerva-planner.ts:472** _(internal-worker-pk)_ — Rollback UPDATE flips fields on plans without any tenant constraint. The error-recovery UPDATE (rolling status back to 'awaiting_approval') issues WHERE id = ${args.planId} AND status = 'revising' with no tenant_id check, allowing an attacker or buggy internal flow to update another tenant's plan.
  - _Why false positive:_ Rollback acts on the same server-owned plan id already claimed in this internal planner flow; row tenant_id was obtained from the prior CAS result.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does NOT set tenant_id. The INSERT lists (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) but omits tenant_id even though tenantId is available via options. This violates the invariant that every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Why false positive:_ Referenced schema says project_files has no tenant_id; it is a child of tenant-scoped projects and ownership is enforced via assertProjectInTenant.
- **server/mpeg-engine.ts:1417** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` in the parallel producer does NOT set tenant_id. The INSERT omits tenant_id even though assertProjectInTenant is called and options.tenantId is available.
  - _Why false positive:_ Referenced schema says project_files has no tenant_id; this child insert is correctly scoped transitively through project_id after assertProjectInTenant.
- **server/plan-executor.ts:117** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `plans` (execution_log) is performed without constraining by tenant_id (WHERE id = ${planId} only). This allows cross-tenant modification of plan rows.
  - _Why false positive:_ appendExecutionLog is an internal helper used by the server executor on server-generated plan ids, not directly from a tenant request boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE that claims a plan (SET status='executing' WHERE id = ${planId} AND status = 'approved') does not include tenant_id. Claiming by id alone can allow an attacker/process to claim and execute another tenant's plan.
  - _Why false positive:_ executePlan is an internal worker claiming plans by globally unique server-generated plan id for background execution.
- **server/plan-executor.ts:728** _(internal-worker-pk)_ — Final status UPDATE on `plans` (SET status = ${finalStatus} ... WHERE id = ${planId}) is not scoped by tenant_id. This atomically writes execution results across tenants when planId is global.
  - _Why false positive:_ Final status write is part of the same internal execution flow on the already-claimed server-owned plan id, with tenant carried from the claimed row.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` uses only `id = ${r.id}` in WHERE and omits tenant_id. This allows the runner (which enumerates rows across tenants) to accidentally update a row belonging to a different tenant if id collisions occur or if an attacker can influence ids.
  - _Why false positive:_ This is a platform-wide scheduler sweep; each row is processed with its own row-carried tenant_id and the update only advances housekeeping fields on that same row.
- **server/reference-learner.ts:291** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses only `id = ${storedId}` in WHERE and omits tenant_id. Although the id was discovered via a tenant-scoped SELECT earlier, the UPDATE must still enforce the tenant constraint at write time to prevent cross-tenant modification if code paths change or ids are re-used.
  - _Why false positive:_ storedId comes directly from the immediately preceding SELECT on memory_entries scoped by input.tenantId and personaId in the same call path.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Why false positive:_ Referenced schema says project_conversations has no tenant_id; it is a join/child table transitively scoped through tenant-owned projects/conversations already verified in this flow.
- **server/routes/conversations.ts:230** _(fk-transitive)_ — INSERT into 'project_notes' omitted tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` (likely a tenant-scoped table)
  - _Why false positive:_ project_notes has no tenant_id and is scoped via tenant-checked parent project_id selected through conversations/projects with p.tenant_id enforced
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets status/result using WHERE id = ${session.id} but omits tenant_id — write to a tenant-scoped table is not constrained to the caller's tenant.
  - _Why false positive:_ updates newly inserted sculptor_sessions row by server-generated session.id within same function, not from untrusted request id
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id using WHERE id = ${session.id} with no tenant_id constraint. This can modify sessions across tenants if an id is forged or guessed.
  - _Why false positive:_ updates same freshly created session row by server-generated primary key after launch, not user-controlled cross-tenant input
- **server/sculptor.ts:168** _(internal-worker-pk)_ — monitorSession performs UPDATE sculptor_sessions ... WHERE id = ${sessionId} but does not constrain by tenant_id (tenantId is available to the function). This violates tenant isolation for writes.
  - _Why false positive:_ background monitor updates session by server-originated sessionId passed from creator path, not from request boundary
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE memory_entries sets fact/access metadata WHERE id = <existing id> but does not include tenant_id in the WHERE clause. The earlier SELECT was tenant-scoped, but the UPDATE by id can modify rows belonging to another tenant.
  - _Why false positive:_ same call path first selects existing memory_entries id under tenant_id, then updates that matched row id
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed with no tenant_id constraint — this will delete rows across all tenants.
  - _Why false positive:_ global housekeeping purge of stale disabled heartbeat tasks; only deletes old rows and reports aggregate count
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log is executed with no tenant_id constraint — this will remove event_log rows for all tenants instead of a single tenant.
  - _Why false positive:_ platform-wide cleanup of processed/failed event logs; housekeeping delete with aggregate count only, no tenant data returned or mixed
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs is executed without a tenant_id filter (pruneOldLogs) — this prunes logs for all tenants rather than scoping per-tenant.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is a platform heartbeat log table pruned globally by age.
- **server/stability-watchdog.ts:154** _(global-table)_ — storage.createHeartbeatLog(...) is called without passing a tenantId. If createHeartbeatLog inserts into a tenant-scoped table, the INSERT may miss tenant_id (or default to an admin tenant), causing cross-tenant writes/misattribution.
  - _Why false positive:_ createHeartbeatLog writes to heartbeat_logs, which schema marks as non-tenant-scoped.
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into project_conversations does not include tenant_id (writes to a tenant-scoped table without stamping tenant).
  - _Why false positive:_ project_conversations has no tenant_id; insert is only after confirming conversation tenant and project ownership, so isolation is through parent FKs.
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT INTO project_files does not set tenant_id. Tenant-scoped table writes must explicitly record tenant_id (columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_files has no tenant_id and row is inserted only after verifying owning project belongs to tenant.
- **server/tools.ts:2995** _(fk-transitive)_ — INSERT INTO project_notes does not set tenant_id. Writing to a tenant-scoped table without tenant_id breaks the invariant and can allow cross-tenant contamination or insertion failures.
  - _Why false positive:_ project_notes has no tenant_id and row is inserted only after verifying owning project belongs to tenant.
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT INTO project_conversations (link_conversation path) does not include tenant_id when creating the link. This is a write into a tenant-scoped table without stamping tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id; project ownership and conversation-in-tenant are validated before link insert.
- **server/video-job-runner.ts:236** _(system-sweep)_ — recoverStaleVideoJobs performs a db.update(...) to mark stale jobs as failed without constraining by tenant_id — it will update matching rows across all tenants. Per the invariant, updates to tenant-scoped tables must include an explicit tenant filter or be run from an explicit global/admin context.
  - _Why false positive:_ recoverStaleVideoJobs is a startup/interval maintenance sweep marking stale jobs failed globally; it mutates housekeeping fields only and returns aggregate/job ids, not tenant data to tenants.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Query reads tool_performance without constraining by tenant_id. tool_performance appears tenant-scoped but the SELECT aggregates across all tenants.
  - _Why false positive:_ Global wiring diagnostic counts tool_performance rows for housekeeping/health only; emits aggregate findings, no tenant data returned or mixed into tenant-facing output.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — Query SELECT count(*) FROM tool_performance (everResult) has no tenant_id filter and thus counts rows across all tenants.
  - _Why false positive:_ Boot-time/global dormant-tools check intentionally counts all historical tool_performance rows; only aggregate count used for maintenance finding.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Query SELECT count(*) FROM messages WHERE created_at > ... runs against messages with no tenant filter. messages is a tenant-scoped chat table, so this reads data across tenants.
  - _Why false positive:_ Cross-tenant message count is used only as aggregate traffic heuristic in a maintenance diagnostic, not exposed as tenant content.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — Query SELECT DISTINCT tool_name FROM tool_performance WHERE ... reads tool_performance without tenant scoping, exposing cross-tenant usage data.
  - _Why false positive:_ Global health check derives distinct tool names used in last 14d to detect dormant registrations; outputs platform-level wiring status, not tenant row data.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — Raw SQL: SELECT COUNT(*)..., MAX(...) FROM ${spec.table} is executed without any tenant_id constraint. The loop enumerates tables such as tool_performance and knowledge_nudges which are tenant-scoped, so this probes counts across all tenants.
  - _Why false positive:_ Subsystem freshness probe intentionally scans tables platform-wide for row counts/last-write timestamps and emits only housekeeping findings.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — Tool-drift query selects from tool_performance without tenant scoping (WHERE last_failure_at > ... but no tenant_id filter). This can surface per-tenant fail rates across all tenants.
  - _Why false positive:_ Tool drift detector is a platform-wide maintenance/observability sweep over cumulative tool stats; returns only tool-level aggregate failure summaries.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Raw SQL SELECT COUNT(*)..., pg_total_relation_size('${tbl.name}') FROM ${tbl.name} runs against memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) without tenant filtering — reading counts 'across all tenants'.
  - _Why false positive:_ Memory-growth tripwire intentionally measures whole-table counts/size for retention housekeeping and emits only aggregate storage metrics.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — Query SELECT COUNT(*)::int AS cnt FROM graph_memory is executed with no tenant filter; graph_memory is tenant-scoped but the code comment even references 'across all tenants', so this reads data from all tenants.
  - _Why false positive:_ Global graph_memory emptiness check is a platform-wide bootstrap/housekeeping probe using only aggregate count.
- **server/ab-optimizer.ts:111** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without restricting tenant_id (reads experiments across all tenants).
  - _Why false positive:_ Heartbeat-callable conclusion loop intentionally iterates running experiments across all tenants and uses each row's tenant_id for per-row scoped updates/inserts.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting lacks a tenant_id constraint — reading pending insights across tenants.
  - _Why false positive:_ Durability sweep intentionally selects pending insights across tenants, then handles each row with its own tenant_id and does not mix content into one tenant response.
- **server/agentic-engines.ts:347** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` in gatherOperationalData has no tenant filter (reads logs across all tenants).
  - _Why false positive:_ Schema note shows heartbeat_logs has no tenant_id column; query reads intentional platform-global heartbeat/task logs.
- **server/agentic-engines.ts:520** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` in runOptimizationEngine (heartbeatPerformance) has no tenant filter and will aggregate logs across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; query reads platform-global heartbeat telemetry by design.
- **server/agentic-features.ts:600** _(upstream-verified)_ — SELECT checking for an existing outreach enrollment reads `outreach_enrollments` without tenant_id in WHERE — the uniqueness/existence check is not tenant-scoped.
  - _Why false positive:_ sequence ownership is verified immediately above via outreach_sequences id+tenant_id; enrollment existence check is constrained by that tenant-owned sequence_id.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes a cross-tenant scan without a tenant_id filter against a tenant-scoped table.
  - _Why false positive:_ explicit heartbeat sweep processes all tenants row-by-row using each row's tenant_id and only emits per-tenant budget events.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on agent_wake_schedules uses a sub-select that does not constrain by tenant_id. The statement claims and mutates pending wake rows across all tenants (WHERE id IN (SELECT id FROM agent_wake_schedules ...)) without a tenant_id filter, enabling cross-tenant row updates.
  - _Why false positive:_ heartbeat scheduler intentionally claims due rows across tenants, then handles each returned row with its own tenant_id when emitting/finalizing wakes.
- **server/auto-memorize.ts:163** _(system-sweep)_ — Query that reads messages (SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ... AND m.role IN ...) has no tenant_id WHERE clause and scans messages from all tenants in a single query.
  - _Why false positive:_ code explicitly partitions cross-tenant message scan by tenant before any synthesis, preventing mixed-tenant prompts while performing maintenance-style processing.
- **server/auto-transcript.ts:128** _(system-sweep)_ — backfillProjectTranscripts() performs a cross-tenant scan: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 — the outer query and the subquery over messages are not scoped by tenant_id.
  - _Why false positive:_ bulk backfill intentionally scans conversations across tenants and then processes each conversation with its own tenant_id; no mixed tenant output surface shown here.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. Tenant-scoped rows must include tenant_id on INSERT to enforce isolation; currently invoice_items(...) VALUES (${newId}, ...) omits tenant_id.
  - _Why false positive:_ invoice_items schema shows no tenant_id; it is a child table scoped transitively through tenant-scoped invoices via invoice_id.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` omits any tenant_id constraint. The function markReviewReused() executes `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` without ensuring the row belongs to the caller's tenant, allowing a cross-tenant modification if an attacker can guess/trigger a pending reviewId/conversationId mapping.
  - _Why false positive:_ update is by server-maintained pending.reviewId from in-memory worker state keyed by conversation, not directly from an untrusted request boundary.
- **server/chat-engine.ts:4365** _(upstream-verified)_ — Call to storage.attachCitations(...) omits any tenant context. If attachCitations inserts into a tenant-scoped table it does not receive an explicit tenantId here and thus risks inserting rows without tenant_id (or relying on inferred/derived ownership).
  - _Why false positive:_ The cited row was created immediately above via createMessage with tenantId: tenantIdForScope, so citation attachment is anchored to an already tenant-scoped message id in the same flow.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` is executed without any tenant_id constraint (cross-tenant read).
  - _Why false positive:_ This is a platform-wide heartbeat scanner; it processes rows one-by-one using each row's own tenantId and emits only redacted admin digest content, not tenant data to another tenant.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` lacks tenant scoping: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` (missing tenant_id in WHERE).
  - _Why false positive:_ getDecryptedPassword first calls getCredential(id, tenantId) and returns null if not found, so ownership is verified earlier in the same call path before this housekeeping UPDATE by that same id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` lacks tenant scoping: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` (missing tenant_id in WHERE).
  - _Why false positive:_ getLoginCredentials first fetches cred via getCredentialForSite(siteUrl, tenantId) and only then updates last_used_at for cred.id; ownership was verified earlier in the same flow.
- **server/data-protection.ts:215** _(upstream-verified)_ — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} reads from the tenant-scoped `messages` table without an explicit tenant_id predicate. Although conversation ownership was checked earlier, subsequent queries should still be tenant-constrained to follow the invariant that every read carries an application-level tenant filter.
  - _Why false positive:_ The conversation row is fetched immediately above with id and tenant_id, and messages are read by that verified conversationId in the same backup flow.
- **server/data-protection.ts:221** _(upstream-verified)_ — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} reads compaction archives without scoping by tenant_id after verifying the conversation. This violates the rule that every read against tenant-scoped tables must include a tenant predicate.
  - _Why false positive:_ The conversation is explicitly verified for the tenant immediately above; archive rows are then fetched for that verified conversationId within the same call path.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete chunks for the same collection_id across other tenants (if collection_id collision or misuse) or otherwise allow cross-tenant deletion.
  - _Why false positive:_ collection ownership is verified immediately above via SELECT on doc_collections with id and tenant_id; delete is constrained to that validated collectionId/docPath pair
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks embedding using WHERE id = ${row.id} with NO tenant_id constraint (UPDATE doc_chunks SET embedding = ... WHERE id = ...). This may modify another tenant's chunk if an id is reused or supplied incorrectly.
  - _Why false positive:_ row.id values come only from the prior SELECT scoped by collection_id and tenant_id in the same function
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_knowledge' (backfillMissingKnowledgeEmbeddings) with NO tenant_id filter — reads tenant data across all tenants.
  - _Why false positive:_ boot/backfill job intentionally scans across tenants and only computes/stores embeddings by row id; no tenant data is returned or mixed into a tenant-facing output
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT from tenant-scoped table 'memory_entries' (backfillEmbeddingVecs) without any tenant_id filter — unscoped read across tenants.
  - _Why false positive:_ platform-wide embedding_vec backfill updates housekeeping/vector fields per selected row and does not expose cross-tenant data
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_knowledge' (backfillEmbeddingVecs) without tenant_id constraint — reads can leak data across tenants.
  - _Why false positive:_ platform-wide knowledge embedding_vec backfill is maintenance-only, handling each row independently without tenant-facing data mixing
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals has no tenant_id filter (SELECT id,title,status,created_at FROM code_proposals ...). code_proposals appears tenant-scoped but this query scans across all tenants.
  - _Why false positive:_ admin health-audit stale-proposal scan is a platform-wide maintenance check and only emits audit findings/housekeeping state, not tenant-facing mixed content
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows without ensuring those rows belong to a specific tenant.
  - _Why false positive:_ global TTL archival updates stale proposal status as housekeeping across tenants; no per-tenant response surface is involved
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks (SELECT id,name,created_at,next_run_at FROM heartbeat_tasks WHERE ...) has no tenant_id filter; heartbeat_tasks are likely tenant-scoped.
  - _Why false positive:_ heartbeat stale-task scan is a global maintenance sweep over housekeeping rows and does not return tenant content to tenants
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables tasks without constraining by tenant_id (risk of modifying other tenants' heartbeat tasks).
  - _Why false positive:_ boot/maintenance audit disables stale heartbeat tasks platform-wide and only mutates enabled status; no tenant data returned or mixed
- **server/heartbeat-seed.ts:27** _(system-sweep)_ — Call to storage.getHeartbeatTasks(undefined, ALL_TENANTS) fetches heartbeat_tasks across all tenants (no tenant constraint).
  - _Why false positive:_ startup seeding intentionally scans all heartbeat tasks across tenants to avoid duplicate system task creation; maintenance/global bootstrap path
- **server/heartbeat.ts:53** _(system-sweep)_ — Raw SELECT: SELECT * FROM research_schedules ... has no tenant_id WHERE filter. This reads research_schedules for all tenants.
  - _Why false positive:_ heartbeat scheduler intentionally sweeps due research schedules across tenants and processes each row with its own sched.tenant_id
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} has no tenant_id constraint — it can update a row belonging to a different tenant.
  - _Why false positive:_ global heartbeat maintenance advances next_run_at for the specific swept schedule row; housekeeping-only mutation in platform-wide loop
- **server/heartbeat.ts:120** _(system-sweep)_ — In error-recovery path: UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.cron_expression} — uses no tenant_id filter and appears to compare id to cron_expression (likely a bug).
  - _Why false positive:_ error-recovery update is within the same global schedule sweep and only advances housekeeping field for the current row
- **server/heartbeat.ts:381** _(system-sweep)_ — Raw SELECT: SELECT * FROM event_log WHERE status = 'pending' ... does not filter by tenant_id — it returns pending events across all tenants.
  - _Why false positive:_ heartbeat processes pending events platform-wide but routes each event using its own tenant_id and failure update is tenant-scoped
- **server/heartbeat.ts:1586** _(global-table)_ — Raw SELECT on heartbeat_logs: SELECT created_at FROM heartbeat_logs ... ORDER BY id DESC LIMIT 1 has no tenant_id filter — this may read logs belonging to other tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column per schema, so this log lookup is against a non-tenant-scoped table
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs an UPDATE on agent_jobs (claiming jobs) without any tenant_id constraint — it can update/return rows across all tenants.
  - _Why false positive:_ global worker claim loop intentionally claims due jobs across tenants and returns rows for internal processing, with per-row tenant_id carried on claimed jobs
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases STEP 1 updates agent_jobs to failed_terminal for expired leases without filtering by tenant_id — mutates rows across tenants.
  - _Why false positive:_ lease reclamation is platform-wide housekeeping over expired jobs and only mutates status/error metadata
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases STEP 2 updates agent_jobs (requeueing expired rows) without any tenant_id constraint — affects jobs for all tenants.
  - _Why false positive:_ requeueing expired leases is platform-wide housekeeping and only resets status/lease timing fields
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs to 'succeeded' by id without scoping to tenant_id — a caller could complete a job belonging to another tenant by id.
  - _Why false positive:_ completeJob is an internal worker update by server-generated job primary key plus attempts fencing, not from an untrusted tenant request boundary
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} with no tenant constraint — this reads tenant-scoped table rows by id only.
  - _Why false positive:_ Worker failure handler reads agent_jobs by server-side PK id passed from claimed job context, not a tenant request boundary.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob UPDATE to mark terminal failure writes agent_jobs rows (status, error, failure_class, etc.) using only id/attempts guards and no tenant_id filter — can mutate other tenants' jobs.
  - _Why false positive:_ Internal worker state transition updates agent_jobs by server-generated job id with lease-fencing attempts guard, not caller-supplied tenant input.
- **server/job-queue.ts:258** _(internal-worker-pk)_ — failJob's retry-path UPDATE (setting next_run_at back to pending) updates agent_jobs without a tenant_id constraint — may requeue jobs across tenants.
  - _Why false positive:_ Retry-path worker update is an internal job-runner transition on a claimed job PK, fenced by attempts/status.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns uses only the run id in the WHERE clause and omits tenant scoping (no tenant_id predicate). This updates a tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ Run row is inserted in the same function with tenantId and the subsequent update targets that freshly created server-side runId only.
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns (noop_proposal path) filters only by run id and omits tenant_id. This writes to a tenant-scoped table without tenant constraint.
  - _Why false positive:_ Noop-path update is by runId that was just created server-side in this call, not from untrusted request input.
- **server/lib/aevo-meta-editor.ts:499** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns to set status='done' uses only run id in the WHERE clause and omits tenant predicate. This can modify rows across tenants.
  - _Why false positive:_ Completion update operates on the same freshly inserted procedureEvolutionRuns row identified by internal runId.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into `project_files` does not include tenant_id. Platform invariant requires explicit tenant_id on tenant-scoped INSERTs; omitting it can misattribute rows or leave them unscoped.
  - _Why false positive:_ project_files has no tenant_id by schema and is transitively scoped through tenant-scoped parent projects via project_id.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into `project_notes` does not include tenant_id. This writes child rows without explicit tenant attribution, violating the 'explicit tenant_id on every INSERT' invariant.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped via tenant-scoped parent projects through project_id
- **server/lib/mcp-api-keys.ts:182** _(upstream-verified)_ — UPDATE to tenant-scoped table `mcp_api_keys` without constraining by tenant_id. The best-effort touch (UPDATE last_used_at) uses WHERE id = ${row.id} only, lacking tenant scoping.
  - _Why false positive:_ UPDATE uses row.id obtained from the immediately preceding verified key lookup and only touches housekeeping field last_used_at
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE 'due' and subsequent UPDATE operate across all tenants).
  - _Why false positive:_ heartbeat runner intentionally sweeps due scheduled_posts across tenants and returns per-row tenant_id for downstream per-row handling
- **server/memory-graph.ts:252** _(fk-transitive)_ — INSERT INTO memory_links does not include any tenant identifier in the VALUES list. If memory_links is a tenant-scoped table (NOT NULL tenant_id), this either fails or results in tenant-less/cross-tenant links; if it is unscoped, it allows cross-tenant linking and loses per-tenant ownership metadata.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries; later reads join through memory_entries tenant scope
- **server/model-catalog.ts:712** _(global-table)_ — runCatalogSync defaults tenantId to 1 (admin) when opts.tenantId is omitted and logs a warning. Subsequent SELECT/INSERT against memory_entries use that tenantId, so callers that forget to supply tenantId will write admin-scoped rows.
  - _Why false positive:_ catalog sync is an admin/platform-global workflow and defaulting discovery notes to ADMIN tenant 1 is intentional per code comments
- **server/model-catalog.ts:811** _(global-table)_ — INSERT INTO memory_entries uses ${tenantId} that may have been defaulted to admin (1). If runCatalogSync was called without an explicit tenantId, new memory_entries are written into the admin tenant, misfiling tenant-scoped alerts/records.
  - _Why false positive:_ memory_entries written here are admin-facing platform-global catalog alerts, intentionally stored under ADMIN tenant 1
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads across tenants with no tenant constraint.
  - _Why false positive:_ Boot/startup validation intentionally scans all active subscriptions and handles each row by its own tenant_id for maintenance.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id FROM `oauth_subscriptions` WHERE provider = 'google-workspace' has no tenant_id constraint. It enumerates tenant rows globally and then iterates tenant ids — this reads tenant-scoped data without scoping to a caller tenant.
  - _Why false positive:_ Startup auto-reconnect enumerates subscription tenant_ids globally for per-tenant repair, not tenant-facing data return.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — reads all active subscriptions across tenants with no tenant_id constraint.
  - _Why false positive:_ Proactive token refresh is a global maintenance loop over subscriptions using each row's tenant_id independently.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — global scan across tenants (no tenant_id filter).
  - _Why false positive:_ Force-refresh is explicitly a daily cron/global maintenance routine operating per subscription row across tenants.
- **server/pace-control.ts:85** _(global-table)_ — SELECT COUNT(*) FROM heartbeat_logs ... WHERE created_at > ... AND ${laneFilter} — query against tenant-scoped table `heartbeat_logs` with NO tenant_id constraint. This counts runs across all tenants instead of scoping to a tenant.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id and is a platform-global heartbeat/throttle log used for global pace caps.
- **server/pace-control.ts:140** _(global-table)_ — Global SELECT over `heartbeat_logs` in getPaceSnapshot: SELECT COUNT(*) ... WHERE created_at > NOW() - (...) — no tenant_id filtering; returns global usage across tenants.
  - _Why false positive:_ heartbeat_logs is non-tenant-scoped per schema; snapshot reads intended platform-global pacing telemetry.
- **server/pace-control.ts:147** _(global-table)_ — SELECT COALESCE(persona_name, 'unknown') AS persona_name, COUNT(*) FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name — this groups heartbeat logs across all tenants (no tenant_id filter).
  - _Why false positive:_ Aggregation is over heartbeat_logs, which schema marks as not tenant-scoped and used for global operational telemetry.
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} — write to tenant-scoped `plans` table without tenant_id filter when rejecting empty plans.
  - _Why false positive:_ executePlan is an internal worker operating on a server-generated plan primary key already claimed from the row, not an untrusted tenant request id.
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-path UPDATE (SET status = 'failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id, allowing cross-tenant state changes in plans on error handling.
  - _Why false positive:_ Error-path status update is inside internal executePlan worker and targets the same server-invoked plan id/state transition.
- **server/plan-executor.ts:778** _(system-sweep)_ — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' without any tenant constraint (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...). This is a global write across tenants.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping sweep resetting stale statuses only, with no tenant data returned or mixed.
- **server/plan-executor.ts:788** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 selects approved plans across all tenants (no tenant_id constraint) and then enqueues executePlan for them, potentially executing other tenants' plans.
  - _Why false positive:_ Boot recovery intentionally selects pending plans across tenants to restart internal execution; it is a maintenance dispatcher, not tenant-facing access.
- **server/process-governor.ts:198** _(global-table)_ — SELECT from heartbeat_logs (response_time check) has no tenant_id constraint — reads tenant-scoped telemetry globally.
  - _Why false positive:_ heartbeat_logs has no tenant_id per schema, so this response-time check reads platform-global telemetry by design.
- **server/process-governor.ts:239** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs (cascading_failures) lacks tenant_id filter — global aggregation across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is used here for platform-wide failure aggregation only
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not include tenant_id — mutates a tenant-scoped table by id only.
  - _Why false positive:_ t.id comes from prior SELECT of heartbeat_tasks constrained by ht.tenant_id = tenantId in same flow
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} executes without tenant_id filter — writes to a tenant-scoped table by id only.
  - _Why false positive:_ desk.id is sourced from prior SELECT on agent_desks filtered by ad.tenant_id = tenantId in same call path
- **server/project-brain.ts:332** _(system-sweep)_ — SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has no tenant_id scoping — enumerates projects across tenants.
  - _Why false positive:_ backfillProjectBrains is a platform-wide maintenance backfill that processes projects separately and writes per-project local brain files, not tenant-facing mixed output
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is missing tenant filtering — can expose conversations from other tenants if project lookup isn't tenant-scoped.
  - _Why false positive:_ conversation scan occurs inside global backfill maintenance loop over one project at a time, not a tenant-facing cross-tenant response
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks tenant_id restriction — messages may be returned across tenants.
  - _Why false positive:_ message read is part of offline backfill maintenance for one conversation at a time and does not return mixed tenant data to a caller
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT id,... FROM code_proposals WHERE id = ${proposalId} has no tenant_id constraint — reads proposal potentially belonging to another tenant.
  - _Why false positive:_ server-side verifier worker reads code_proposals by server-invoked proposalId primary key, not a shown untrusted request boundary
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (clear rebased_at) has no tenant_id constraint — mutates another tenant's proposal row by id only.
  - _Why false positive:_ internal verifier worker updates one proposal row by server-invoked primary key proposalId
- **server/proposal-verifier.ts:250** _(internal-worker-pk)_ — persist() UPDATE code_proposals ... WHERE id = ${proposalId} updates verification fields without tenant scoping — mutates tenant-scoped table by id only.
  - _Why false positive:_ persist() is only used by internal verifier flow to update a single proposal by server-side primary key
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — UPDATE code_proposals SET status='rejected', ... WHERE id = ${proposalId} in rebase flow lacks tenant_id protection — can change other tenants' proposals.
  - _Why false positive:_ rebase flow is an internal verifier worker mutating one proposal by server-controlled primary key
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (hallucinated case) has no tenant filter — mutates proposals across tenants.
  - _Why false positive:_ hallucination-status update is inside internal verifier worker operating on one proposal by server-side primary key
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, ... WHERE id = ${proposalId} (persist rebased diff) lacks tenant_id constraint — may overwrite another tenant's proposal.
  - _Why false positive:_ Server-side verifier worker updates by proposalId passed into fireAndForgetVerify/verifyProposalById, not directly from a request boundary.
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE research_sessions executes without any tenant_id constraint — this will mark running sessions as completed across all tenants (cross-tenant mutation).
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping sweep that only marks stale running sessions completed and returns ids for logging, not tenant-facing mixed data.
- **server/research-engine.ts:400** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() is executed without a tenant_id filter — this will delete agent_knowledge rows for all tenants.
  - _Why false positive:_ Global expiry cleanup deletes expired autoresearch knowledge across tenants as maintenance; no tenant content is returned or mixed.
- **server/routes.ts:167** _(system-sweep)_ — Startup DB query selects from tenant-scoped table `file_storage` without constraining tenant_id (no tenant filter).
  - _Why false positive:_ Startup restore loop intentionally scans all tenants' image blobs to recreate local files; maintenance action writes files only and does not expose mixed tenant data to users.
- **server/routes.ts:2143** _(upstream-verified)_ — POST /api/presenter inserts into tenant-scoped table `presenter_sessions` using tenantId provided directly in request body (untrusted input) without verifying caller's ownership/authorization for that tenant.
  - _Why false positive:_ Route is gated by x-internal-key/SESSION_SECRET before accepting body tenantId, so tenant selection is restricted to a trusted internal caller rather than arbitrary tenant-facing input.
- **server/routes.ts:4333** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The INSERT at lines 4333-4336 uses: INSERT INTO project_notes (project_id, note, author) VALUES (...) — missing tenant_id column/value, so the row will be created without explicit tenant scoping (or fail if tenant_id is NOT NULL). This violates the invariant that every INSERT into tenant-scoped tables must include tenant_id.
  - _Why false positive:_ project_notes has no tenant_id by schema and is inserted only after selecting project_id from a tenant-scoped conversation row constrained by tenant_id.
- **server/routes/admin-extra.ts:125** _(upstream-verified)_ — Raw/interpolated SQL (db.execute(sql`...`)) is used with request-derived values (tenantId, limit) when selecting from decline_events. This mixes request data into template SQL and risks injection/isolation issues.
  - _Why false positive:_ Query is explicitly scoped with WHERE tenant_id = session-derived tenantId; request limit is normalized and this is not a tenant-isolation gap.
- **server/routes/admin-extra.ts:154** _(upstream-verified)_ — Raw/interpolated SQL is used with request-derived values (tenantId, status, limit) to query self_initiatives (db.execute(sql`... ${status} ... ${limit}`)). Passing a query param directly into SQL risks injection and logic errors.
  - _Why false positive:_ Both branches constrain self_initiatives by session-derived tenant_id, with optional status filter; no cross-tenant access path shown.
- **server/routes/admin-extra.ts:184** _(upstream-verified)_ — UPDATE uses sql`` with request-derived id and tenantId (db.execute(sql`UPDATE self_initiatives ... WHERE id = ${id} AND tenant_id = ${tenantId} ... RETURNING ...`)). Although tenant_id is present, the query is built via template interpolation of request values.
  - _Why false positive:_ UPDATE includes both id and session-derived tenant_id in WHERE, so ownership is enforced in the same statement.
- **server/routes/admin-extra.ts:216** _(upstream-verified)_ — Multiple db.execute(s`...`) queries in this handler interpolate tenantId (and other request-derived values) directly into SQL for counts (conversations, messages, memory_entries, file_storage, etc.). These are string-interpolated SQL usages with request-derived values.
  - _Why false positive:_ All counts are read using tenantId from getTenantFromRequest and each tenant-scoped table query filters by that tenant or by tenant-scoped subquery.
- **server/routes/admin-extra.ts:254** _(upstream-verified)_ — Schedule-deletion handler issues an UPDATE via database.execute(s`UPDATE tenants SET ... WHERE id = ${tenantId}`) where tenantId is request-derived and interpolated into SQL.
  - _Why false positive:_ tenants is keyed by id and the id comes from getTenantFromRequest for the authenticated caller; this updates only the caller's own tenant row.
- **server/routes/admin-extra.ts:312** _(upstream-verified)_ — Account deletion uses multiple interpolated DELETE statements (db.execute(s`DELETE FROM ... WHERE tenant_id = ${tenantId}` / WHERE conversation_id IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`)). Request-derived tenantId and subqueries are injected into raw SQL templates.
  - _Why false positive:_ Account deletion flow derives tenantId from getTenantFromRequest and every delete is scoped to that tenant (or its tenant-scoped conversations), deleting only the caller's own account data.
- **server/routes/admin-wedges.ts:41** _(upstream-verified)_ — Query uses db.execute(sql`... WHERE tenant_id = ${tenantId} AND ${tag} = ANY(tags)`) where tenantId (from request) is interpolated into a raw SQL template. The query mixes request-derived tenantId into template SQL.
  - _Why false positive:_ Route is platform-admin gated to ADMIN_TENANT_ID before query; query also filters projects by tenant_id.
- **server/routes/admin-wedges.ts:53** _(upstream-verified)_ — Pending drafts COUNT query uses db.execute(sql`... WHERE tenant_id = ${tenantId} AND wedge_slug = ${w.slug} AND status = 'pending_review'`) with tenantId interpolated from request and limit later interpolated; request-derived data is inserted into raw SQL template.
  - _Why false positive:_ Route is platform-admin gated to ADMIN_TENANT_ID before query; count query includes tenant_id predicate.
- **server/routes/admin-wedges.ts:73** _(upstream-verified)_ — Heartbeat lookup uses db.execute(sql`... WHERE tenant_id = ${tenantId} AND (LOWER(name) ~ ${slugRe} ...)`) with tenantId and a regex string interpolated. The regex and tenantId are derived/calculated at runtime and placed into a templated SQL string.
  - _Why false positive:_ Route is platform-admin gated to ADMIN_TENANT_ID before query; heartbeat lookup includes tenant_id predicate.
- **server/routes/admin-zombie-detector.ts:36** _(system-sweep)_ — Within a transaction, multiple tx.execute(sql`...`) queries interpolate windowDays (derived from req.query) directly into SQL expressions (e.g. started_at > NOW() - (${windowDays}::int * INTERVAL '1 day')). WindowDays is request-derived and is injected into raw SQL templates.
  - _Why false positive:_ Admin-only platform-wide diagnostics intentionally aggregate usage across tenants and global tables; no tenant-facing mixed content output.
- **server/routes/admin.ts:514** _(global-table)_ — Admin tenant-update endpoints call db.execute(sql`UPDATE tenants SET plan = ${plan} WHERE id = ${targetId}`) (and other similar updates) with targetId and body values interpolated directly into SQL templates. These are request-derived values used in raw SQL.
  - _Why false positive:_ tenants is platform-global with no tenant_id; endpoint is platform-admin gated and intentionally updates tenant records by id.
- **server/routes/admin.ts:547** _(global-table)_ — Reset-usage endpoint performs db.execute(sql`UPDATE tenants SET trial_conversations_used = 0 WHERE id = ${targetId}`) with targetId interpolated directly into SQL from req.params.
  - _Why false positive:_ tenants is platform-global with no tenant_id; endpoint is platform-admin gated and intentionally resets usage by tenant id.
- **server/routes/admin.ts:799** _(upstream-verified)_ — Repair-incidents handler composes WHERE clauses using sql fragments (sql`tenant_id = ${tenantId}` and sql`source = ${source}`) then injects the composed where into a db.execute(sql`... WHERE ${where} ...`). The 'source' value is request-derived and is interpolated into the final SQL fragment.
  - _Why false positive:_ Admin-only handler is gated to ADMIN_TENANT_ID and query constrains repair_incidents by tenant_id.
- **server/routes/agent-insights.ts:49** _(upstream-verified)_ — Multiple db.execute(sql`...`) calls interpolate tenantId and windowDays (from req.query) directly into raw SQL (agent_activity, agent_cost_ledger, step_rewards, agent_approvals). Request-derived values are placed inside template SQL.
  - _Why false positive:_ Tenant is resolved from authenticated request and every tenant-scoped query shown filters by tenant_id; personas/capabilities are global tables.
- **server/routes/agent-insights.ts:244** _(upstream-verified)_ — Runs list query uses db.execute(sql`SELECT ... FROM plans WHERE tenant_id = ${tenantId} ORDER BY id DESC LIMIT ${limit}`) with tenantId and limit interpolated from the request into a raw SQL template.
  - _Why false positive:_ Runs list resolves tenant from auth context and explicitly filters plans by tenant_id.
- **server/routes/api-v1.ts:392** _(upstream-verified)_ — GET /api/v1/agents uses db.execute(sql`... LEFT JOIN tenant_persona_names tpn ON tpn.persona_id = p.id AND tpn.tenant_id = ${tenantId}`) where tenantId from the request is interpolated into raw SQL.
  - _Why false positive:_ Join uses authenticated tenantId to select only that tenant's override rows from tenant_persona_names; personas are global by schema.
- **server/routes/api-v1.ts:466** _(global-table)_ — Dispatch resolution uses db.execute(sql`SELECT id, name FROM personas WHERE is_active = true AND LOWER(name) = LOWER(${agent}) LIMIT 1`) with agent (request body) interpolated into the SQL template. agent is request-derived.
  - _Why false positive:_ personas is platform-global with no tenant_id; lookup does not cross tenant-scoped data.
- **server/routes/api-v1.ts:654** _(global-table)_ — AG-UI persona resolution issues a db.execute(sql`SELECT id FROM personas WHERE is_active = true AND LOWER(name) = LOWER(${body.agent}) LIMIT 1`) with body.agent interpolated into the SQL template (request-derived).
  - _Why false positive:_ personas is platform-global with no tenant_id; agent-name lookup is not a tenant-isolation issue.
- **server/routes/api-v1.ts:777** _(upstream-verified)_ — Conversation poll uses db.execute(sql`SELECT m.id, m.role, m.content, m.created_at FROM messages m JOIN conversations c ON c.id = m.conversation_id WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} ORDER BY m.id DESC LIMIT 50`) with conversationId and tenantId interpolated from the request.
  - _Why false positive:_ Conversation ownership is verified immediately above via storage.getConversation(conversationId, tenantId) and conv.tenantId check, and the SQL also filters c.tenant_id = tenantId.
- **server/routes/api-v1.ts:889** _(global-table)_ — A2A message/send persona lookup uses db.execute(sql`SELECT id FROM personas WHERE is_active = true AND LOWER(REGEXP_REPLACE(name, '[^a-zA-Z0-9]+', '-', 'g')) = LOWER(${skillId}) LIMIT 1`) with skillId derived from request metadata and interpolated into raw SQL.
  - _Why false positive:_ personas is marked tenant_id column: NO and comments show persona resolution is intentionally platform-global.
- **server/routes/api-v1.ts:940** _(upstream-verified)_ — A2A tasks/get SELECT of last assistant message uses db.execute(sql`... WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} AND m.role = 'assistant' ORDER BY m.id DESC LIMIT 1`) with conversationId and tenantId interpolated from request.
  - _Why false positive:_ Same handler already verifies the conversation belongs to tenantId via storage.getConversation and conv.tenantId before selecting the assistant message; SQL also includes c.tenant_id = tenantId.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' missing tenant_id constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Why false positive:_ conv.id comes from a conversation just created with tenantId in the same flow, and project ownership is checked with WHERE id = projectId AND tenant_id = tenantId before the update.
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' missing tenant_id constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Why false positive:_ The project row is verified immediately above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId, so the subsequent timestamp update targets a proven tenant-owned project.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — GET /api/inbox/:id marks messages as read using UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} with NO tenant_id constraint. This can flip read state for a message belonging to a different tenant (cross-tenant update).
  - _Why false positive:_ msg is fetched just above with WHERE id = msgId AND tenant_id = tenantId; the update uses that verified row's id only.
- **server/routes/metrics.ts:63** _(system-sweep)_ — /metrics handler runs a query on tenant-scoped table `agent_cost_ledger` that groups by tenant_id but does not include a WHERE tenant_id = ... predicate. Even though /metrics is gated by a token, this still exposes per-tenant data without explicit tenant scoping in SQL.
  - _Why false positive:_ This is an operator-only /metrics endpoint guarded by METRICS_TOKEN and emits cross-tenant aggregate telemetry, not tenant-facing mixed content.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' lacks tenant_id. heartbeat_tasks appears to be tenant-scoped elsewhere in the code (some queries use tenant_id), so this read may leak or act across tenants unintentionally.
  - _Why false positive:_ Boot-time cleanup intentionally counts tasks across all tenants as housekeeping only and does not return tenant data to any tenant-facing surface.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has no tenant_id constraint and may disable tasks across tenants.
  - _Why false positive:_ This is a platform-wide maintenance cleanup disabling runaway task rows across tenants; it mutates housekeeping state only and does not expose mixed tenant content.
- **server/seed-cleanup.ts:45** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id — potentially reading across tenants when heartbeat_tasks is tenant-scoped.
  - _Why false positive:_ This count is part of a boot-time maintenance reconciler scanning for duplicate session-logger tasks globally; housekeeping-only, non-tenant-facing.
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant scoping and may affect multiple tenants' tasks.
  - _Why false positive:_ Boot-time cleanup intentionally disables matching heartbeat task rows across tenants; housekeeping-only mutation with aggregate log output.
- **server/seed-cleanup.ts:59** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... OR ...) has no tenant_id constraint; this reads across all tenants if heartbeat_tasks is tenant-scoped.
  - _Why false positive:_ Global count used only to drive maintenance cleanup/logging; no tenant data returned to any tenant-facing surface.
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (<name ILIKE predicates>) AND enabled = true omits tenant_id; this will update rows across tenants.
  - _Why false positive:_ Boot-time maintenance update disables orphaned repo-examination tasks across tenants; housekeeping-only sweep.
- **server/seed.ts:36** _(system-sweep)_ — Unscoped SELECT against tenant-scoped table `research_sessions`: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id filter.
  - _Why false positive:_ Seed import guard checks whether any research_sessions exist globally before one-time snapshot import; maintenance/bootstrap logic, not tenant-facing read.
- **server/seed.ts:2158** _(system-sweep)_ — Unscoped SELECT against tenant-scoped table `research_programs`: `SELECT id, name, model FROM research_programs WHERE is_active = true` has no tenant_id filter and will return rows across all tenants.
  - _Why false positive:_ Boot-time repair scans all active research programs to normalize invalid model ids; global maintenance read only used for per-row fix/logging.
- **server/seed.ts:2163** _(system-sweep)_ — Unscoped UPDATE against tenant-scoped table `research_programs`: `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}` lacks a tenant_id constraint and may modify another tenant's row with the same id.
  - _Why false positive:_ Per-row maintenance fix updates model field by selected program id during global seed repair; housekeeping mutation, not tenant-mixing output.
- **server/seed.ts:2515** _(system-sweep)_ — Unscoped SELECT against tenant-scoped table `conversations`: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)` does not filter by tenant_id and may touch conversations from other tenants.
  - _Why false positive:_ Backfill loop repairs missing project_conversations links across tenants using each row's own project_id/conversation_id; no tenant-facing data exposure.
- **server/seed.ts:3812** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The statement `UPDATE memory_entries SET fact = REPLACE(...) WHERE deleted_at IS NULL AND fact LIKE ...` will modify rows across all tenants.
  - _Why false positive:_ Global canonical-spelling migration updates memory rows in place across tenants; housekeeping/content normalization only with aggregate logging.
- **server/seed.ts:3838** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` lacks a tenant_id constraint. The statement `UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'` will update conversations for every tenant.
  - _Why false positive:_ Global model-id backfill updates conversation metadata across tenants; maintenance-only mutation with aggregate counts.
- **server/seed.ts:3784** _(system-sweep)_ — DELETE on tenant-scoped table `heartbeat_tasks` lacks a tenant_id constraint. The statement `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'` will delete matching rows across all tenants.
  - _Why false positive:_ Boot-time purge of stale auto-disabled heartbeat tasks is platform-wide housekeeping and only deletes sentinel-marked maintenance rows.
- **server/seed.ts:4051** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` omits tenant_id in WHERE. After selecting a row by tenant (tenant_id = 1), the code runs `UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id}` without confirming tenant ownership in the WHERE clause.
  - _Why false positive:_ Row id is selected immediately above with tenant_id = 1 and matching category/title, so the update targets a tenant-verified row in the same call path.
- **server/seed.ts:3984** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note}, 'system:import')` does not set tenant_id; tenant-scoped tables require tenant_id to enforce isolation and may be NOT NULL.
  - _Why false positive:_ project_notes has no tenant_id and is transitively isolated via project_id to tenant-scoped projects; insert is correct by design.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT FROM heartbeat_logs (recentLogs) is performed with no tenant_id constraint — this reads cross-tenant data and can leak tenant-specific task/error information.
  - _Why false positive:_ heartbeat_logs is explicitly non-tenant-scoped, so cross-tenant aggregation on this global log table is by design.
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata issues a raw SELECT FROM messages by id list (messages WHERE id IN (...)) with no tenant_id constraint. This reads tenant-scoped rows without enforcing tenant isolation.
  - _Why false positive:_ attachCitationMetadata only re-reads ids from msgs already fetched by tenant-scoped callers shown immediately above, and merges citations back onto those same rows.
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories performs a global UPDATE on memory_entries (UPDATE memory_entries SET status = 'archived' WHERE ... ) with no tenant scoping. This mutates rows across all tenants without tenant constraints.
  - _Why false positive:_ Platform-wide archival sweep only mutates housekeeping status on expired memory rows and returns aggregate count, not tenant data.
- **server/storage.ts:858** _(system-sweep)_ — archiveStaleMemories executes a multi-tenant UPDATE on memory_entries (WHERE status = 'active' AND created_at <= ...) without tenant_id filtering, causing cross-tenant writes.
  - _Why false positive:_ Platform-wide stale-memory archival is a housekeeping sweep that only updates status fields and returns count.
- **server/storage.ts:1000** _(system-sweep)_ — getDueHeartbeatTasks runs a raw SELECT FROM heartbeat_tasks (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ...) with no tenant_id scoping. heartbeat_tasks is tenant-scoped; reading due tasks across tenants without explicit admin intent may expose or mix tenants' tasks.
  - _Why false positive:_ This is a scheduler worker selecting due heartbeat tasks across tenants for maintenance execution, carrying each row tenantId separately.
- **server/storage.ts:1027** _(system-sweep)_ — claimHeartbeatTasks issues an UPDATE on heartbeat_tasks (SET last_run_at/next_run_at WHERE id = ${id} AND enabled = true ...) with no tenant check. This mutates tenant-scoped rows by id without verifying tenant ownership.
  - _Why false positive:_ Claiming due heartbeat tasks is part of the cross-tenant scheduler maintenance loop and only updates run timestamps per task row.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. The query `db.select().from(customTools).where(eq(customTools.name, parsed.name))` can return rows from other tenants or leak existence.
  - _Why false positive:_ Name is transformed by tenantToolName(parsed.name, tenantId) before lookup, making the existence check tenant-scoped by derived namespacing.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT by id on tenant-scoped table `custom_tools` without tenant filter. `db.select().from(customTools).where(eq(customTools.id, inserted.id))` does not validate that the returned row belongs to the expected tenant.
  - _Why false positive:_ Row is re-selected by inserted.id immediately after this function inserted that row with tenantId; id comes from trusted prior insert, not cross-tenant input.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` to increment usageCount without tenant constraint. `.where(eq(customTools.id, tool.id))` may update a row in another tenant's scope.
  - _Why false positive:_ tool.id comes from an earlier SELECT constrained by customTools.tenantId = tenantId in the same function, so the updated row was already ownership-verified.
- **server/tools.ts:2913** _(fk-transitive)_ — SELECT subqueries counting project_files, project_notes, and project_conversations omit an explicit tenant filter (SELECT COUNT(*) FROM project_files WHERE project_id = p.id ...). Reads against tenant-scoped tables must be constrained to the caller tenant.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are scoped transitively via parent projects row filtered by p.tenant_id = tenantId.
- **server/tools.ts:2976** _(fk-transitive)_ — Duplicate-check SELECT against project_files uses project_id and file_name/file_url but does not include tenant_id. This read can return rows belonging to another tenant if project_id is misused or not globally unique.
  - _Why false positive:_ project_files has no tenant_id and access is transitively scoped by prior owner check on projects WHERE id = params.id AND tenant_id = tenantId.
- **server/tools.ts:3016** _(fk-transitive)_ — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} lacks tenant scoping. Reads against tenant-scoped tables must include tenant_id constraints.
  - _Why false positive:_ project_conversations has no tenant_id and both parent project ownership and conversation tenant are validated earlier in the same path before duplicate check/insert.
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT uses project_id but does not explicitly set tenant_id (INSERT INTO project_files (... ) VALUES (...)).
  - _Why false positive:_ project_files has no tenant_id; insert is correct by design after assertProjectInTenant confirms parent project ownership.
- **server/tools.ts:8891** _(upstream-verified)_ — UPDATE on tenant-scoped table 'proposed_skills' is performed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This can update a row belonging to a different tenant if ids are global.
  - _Why false positive:_ Update targets row.id returned from the immediately preceding insert into proposed_skills with tenantId = tid, so ownership is established in the same call path.
- **server/tools.ts:8916** _(upstream-verified)_ — UPDATE on tenant-scoped table 'proposed_skills' (marking promoted/accepted) uses only the row id in the WHERE clause and lacks a tenant_id constraint.
  - _Why false positive:_ Update uses row.id from the same trusted insert into proposed_skills for tenant tid; not sourced from untrusted cross-tenant input.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against tenant-scoped table browser_workflows without constraining to caller tenant (no WHERE tenant_id = ...). This updates last_replayed by id only, allowing cross-tenant writes if id is not owned by the caller.
  - _Why false positive:_ workflow is loaded immediately above with id/name plus tenant_id, and update uses that verified row's id
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE executed against tenant-scoped table projects without constraining to caller tenant (no WHERE tenant_id = ...). The code updates projects.updated_at by project id only, relying on earlier provenance rather than an explicit tenant filter, which violates the 'must include tenant filter' invariant.
  - _Why false positive:_ projId comes from a prior join that proves project belongs to ctx.tenantId before this timestamp-only update
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples does not constrain by tenant_id. The loop updates rows with WHERE id = ${row.id} but omits AND tenant_id = ${tenantId}, allowing a caller who can supply or discover an id to affect rows belonging to other tenants.
  - _Why false positive:_ row.id values come solely from contradictionRows selected just above with tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — Unscoped SELECT from provider_keys (SELECT provider, LENGTH(api_key) ...) executed without a tenant_id WHERE clause. This reads a tenant-scoped table (provider_keys) without constraining to the caller's tenant, relying only on an admin-only branch — violates the invariant that every tenant-scoped read must include tenant_id = callerTenant.
  - _Why false positive:_ schema note says provider_keys has no tenant_id, so this is not a tenant-scoped read
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — Unscoped SELECT COUNT(DISTINCT provider) FROM provider_keys executed without tenant_id WHERE clause. This again queries provider_keys without constraining by tenant, leaking cross-tenant information when run by an admin-path branch.
  - _Why false positive:_ provider_keys is schema-global with no tenant_id; admin-only aggregate query is not cross-tenant isolation bug
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd='0' ... WHERE id = ${reservationId} (releaseVentureReservation) executes a tenant-scoped update by id only, with no tenant_id constraint, allowing cross-tenant mutation if reservationId is abused.
  - _Why false positive:_ best-effort internal worker update by server-generated reservation primary key, not shown as sourced from request params/body
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_activity` performed with WHERE eq(agentActivity.id, activityId) only — no tenant_id constraint. This allows updating another tenant's row by id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() updates agent_runs without scoping by tenant_id (db.update(...).where(eq(agentRuns.id, runId))). This allows modifying a run by id across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): appendStep (runs.ts:34) callers: executor.ts (createRun→run.id), resume-worker.ts:129 (runId=row.run_id from a tenant-scoped claim, resume-worker.ts:111-121), self-heal.ts:302 (runId param from server-side attemptSelfHeal). All runIds are server-generated PKs; none from req.params/tool args.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) only — no tenant_id constraint, allowing cross-tenant writes/overwrites.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): appendStep (runs.ts:34) callers: executor.ts (createRun→run.id), resume-worker.ts:129 (runId=row.run_id from a tenant-scoped claim, resume-worker.ts:111-121), self-heal.ts:302 (runId param from server-side attemptSelfHeal). All runIds are server-generated PKs; none from req.params/tool args.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs using WHERE id = runId without tenant scoping, permitting cross-tenant state replacement.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): appendStep (runs.ts:34) callers: executor.ts (createRun→run.id), resume-worker.ts:129 (runId=row.run_id from a tenant-scoped claim, resume-worker.ts:111-121), self-heal.ts:302 (runId param from server-side attemptSelfHeal). All runIds are server-generated PKs; none from req.params/tool args.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() performs a db.update(agentRuns) WHERE eq(agentRuns.id, runId) with no tenant_id constraint — it can mark runs for other tenants completed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): appendStep (runs.ts:34) callers: executor.ts (createRun→run.id), resume-worker.ts:129 (runId=row.run_id from a tenant-scoped claim, resume-worker.ts:111-121), self-heal.ts:302 (runId param from server-side attemptSelfHeal). All runIds are server-generated PKs; none from req.params/tool args.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs WHERE id = runId but omits tenant scoping, enabling cross-tenant status changes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): appendStep (runs.ts:34) callers: executor.ts (createRun→run.id), resume-worker.ts:129 (runId=row.run_id from a tenant-scoped claim, resume-worker.ts:111-121), self-heal.ts:302 (runId param from server-side attemptSelfHeal). All runIds are server-generated PKs; none from req.params/tool args.
- **server/memory-graph.ts:183** _(upstream-verified)_ — refreshCategoryCount updates memory_categories.memory_count using a subquery that counts memory_entries without constraining by tenant_id. The computed count can include rows from other tenants, corrupting per-tenant counters and leaking cross-tenant information.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET ... WHERE id = ${rule.id} lacks tenant_id constraint — may update another tenant's governance rules.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} has no tenant_id constraint. Even though the session was selected earlier, every write must include an explicit tenant constraint per the isolation invariant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — settleFusionReservation issues UPDATE agent_cost_ledger ... WHERE id = ${reservationId} without constraining tenant_id. agent_cost_ledger is tenant-scoped, so this write may modify other tenants' ledger rows by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:425** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger SET cost_usd='0' WHERE id = ${reservationId} with no tenant_id check, allowing cross-tenant ledger modifications.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) and does not constrain by tenant_id. This allows an attacker/process with an experiment id to update another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-improvement.ts:413** _(internal-worker-pk)_ — Error-path UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) (no tenant_id constraint), allowing cross-tenant modification in failure handling.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:1311** _(upstream-verified)_ — getAllDataForExport performs wide SELECTs across many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, etc.) and returns all rows platform-wide with no tenant filtering or access control. This exposes every tenant's data in bulk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `custom_tools` without tenant constraint. The update `.where(eq(customTools.id, tool.id))` can modify a row belonging to a different tenant if IDs are guessable/shared.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE against tenant-scoped table `custom_tools` without tenant constraint. `db.delete(customTools).where(eq(customTools.id, tool.id))` can delete another tenant's tool row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/twilio.ts:103** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is performed without restricting by tenant_id (WHERE tenant_id = ...). This reads pairing rows across all tenants and relies on later application logic to filter/validate, violating the invariant that every read against tenant-scoped tables must include an explicit tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/venture-discovery/budget.ts:156** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped ledger row by id only, without verifying tenant_id. This can modify another tenant's ledger row if a reservationId is reused or tampered with.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` in runPredictiveEngine (recentLogs) lacks tenant_id constraint — reading other tenants' heartbeat logs.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/agentic-engines.ts:451 — The query at server/agentic-engines.ts:451-454 reads heartbeat_logs for recent successful automated activities and feeds a platform-level predictive prompt at :456-463; it is not tenant-facing row retrieval. shared/schema.ts:557-575 shows heartbeat_logs has no tenant_id, and the surrounding engine is a platform-wide aggregate rather than a tenant response.
- **server/agentic-engines.ts:534** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_tasks` (taskConfig) executes without tenant_id = ${tenantId}, reading tasks across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/auto-memorize.ts:45** _(system-sweep)_ — getWatermark() reads agent_knowledge without scoping by tenant_id (SELECT ... FROM agent_knowledge WHERE category = ... AND title = ...). This can return another tenant's watermark.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/channels/kernel.ts:177** _(upstream-verified)_ — dispatchInbound calls opts.resolveConversation(turn) and then passes the returned conversationId to processMessage without verifying that the conversation belongs to turn.tenantId. The kernel explicitly “trust[s] the adapter” for tenant resolution (see comment at line 172) and does not enforce any tenant-scoped check here. If resolveConversation or processMessage do not themselves enforce tenant-scoped WHERE tenant_id = <tenant> filters, this permits cross-tenant access/modification via a foreign id provided by the caller.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): kernel.ts:177 resolveConversation; sole caller whatsapp.ts:442 binds the callback to a tenant WA session and turn.tenantId=s.tenantId (whatsapp.ts:429-432); getOrCreateConversation only returns a conv verified for s.tenantId (whatsapp.ts:490) or freshly created with tenantId=s.tenantId (whatsapp.ts:520-525) — no attacker-supplied foreign id
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on `commitments` performed without tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` (unscoped write).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/delivery-pipeline.ts:1071** _(global-table)_ — getDeliveryByStripePayment selects from tenant-scoped delivery_logs by stripePaymentId only (WHERE stripe_payment_id = paymentId) with NO tenant_id constraint. This can leak another tenant's delivery row via a shared payment id lookup.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:50** _(signed-url)_ — getDfyIntakeByToken SELECTs dfy_intake_forms by token only (WHERE token = ...) with NO tenant_id constraint. A valid token could expose a form belonging to any tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/dfy-intake.ts:93** _(signed-url)_ — submitDfyIntake updates dfy_intake_forms (UPDATE ... WHERE dfy_intake_forms.id = form.id) but does NOT constrain the UPDATE by tenant_id. Because getDfyIntakeByToken earlier did not tenant-scope, this can update another tenant's row.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): form row fetched by unguessable 48-hex token (the documented authorization for the public intake form); UPDATE reuses that row's own id.
- **server/felix-brain.ts:19** _(system-sweep)_ — Unscoped SELECT from tenant-scoped table 'agent_runs' — rehydrateTaskStateOnBoot executes a raw query selecting rows from agent_runs without a WHERE tenant_id = <tenant> constraint. This reads other tenants' agent_runs and then stores per-conversation state into a global in-memory Map keyed only by conversationId, enabling cross-tenant data leakage and collisions.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): rehydrateTaskStateOnBoot is a boot rehydration loop over running/pending agent_runs that rebuilds in-memory TaskState keyed by each row's own conversationId (server/felix-brain.ts:13-45); state is surfaced per-conversation, not cross-tenant.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into tenant-scoped table 'presenter_slide_images' does not set tenant_id (INSERT uses session_id, slide_index, image_data, image_size, quality only).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/google-workspace.ts:2364** _(fk-transitive)_ — INSERT into tenant-scoped table 'presenter_slide_images' for thumbnail variant does not set tenant_id (same INSERT/ON CONFLICT pattern for 'thumb').
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks tenant_id constraint or ownership verification on sessionId. This updates a tenant-scoped resource by id only, allowing cross-tenant modification if sessionId is forged/misresolved.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/job-queue.ts:274** _(internal-worker-pk)_ — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} ... without tenant_id filtering — callers could cancel jobs belonging to other tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:124** _(internal-worker-pk)_ — UPDATE agent_trace_spans in persistSpanClose filters only by span_id and omits tenant_id. This writes to a tenant-scoped telemetry table without constraining the update to the owning tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — SELECT from tenant-scoped table `mcp_api_keys` without constraining by tenant_id. verifyApiKey reads the row by key_prefix alone, returning tenant_id for a presented token — this SELECT can be used to enumerate prefixes across tenants and violates the invariant that tenant-scoped reads must be constrained.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/mcp-api-keys.ts:166 — verifyApiKey compares the sha256 of the FULL presented key against key_hash in the same function (mcp-api-keys.ts:163-180); the credential itself is the access control and selects the tenant.
- **server/memory-graph.ts:123** _(upstream-verified)_ — When creating a new category (fallback path) the code runs: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} without constraining by tenant_id. That can modify a category row belonging to a different tenant if IDs are mis-matched or attacker-influenced data exists.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): catId is produced by ensureCategory (server/memory-graph.ts:79) which SELECTs/INSERTs scoped to tenantId (:51 'AND tenant_id = ${tenantId}', :57-58 INSERT with tenant_id); the id is server-derived within the same tenant call path, not request-supplied.
- **server/minerva-planner.ts:386** _(upstream-verified)_ — Probe SELECT (SELECT status, ceo_decision FROM plans ...) conditionally omits tenant filter. The SELECT appends AND tenant_id = ${args.tenantId} only when args.tenantId is supplied, otherwise it queries by id alone, exposing possible cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Child-plan lookup runs inside decidePlan's revise-idempotency branch, gated by the same route ownership check at agentic-policy.ts:113; args.planId is the verified plan id.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name,status,description FROM projects WHERE id = ${projectId} has no tenant_id constraint — may return another tenant's project.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/self-improvement.ts:170** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` may run without a tenant_id constraint in detectStagnation. If tenantId is not supplied the query uses only category and will return rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — getExperimentHistory returns db.select().from(experiments) without a tenant_id constraint when tenantId argument is not provided. That performs an unscoped read across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:398** _(internal-worker-pk)_ — attachCitations updates messages (UPDATE messages SET citations = ...) constrained only by message id, with no tenant_id constraint. This is a write to a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:533** _(upstream-verified)_ — createMemoryEntry inserts into memory_entries without validating/ensuring an explicit tenantId value is present in the data. The schema is noted to have tenant_id NOT NULL, so this could produce unscoped writes or runtime errors.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/storage.ts:805** _(internal-worker-pk)_ — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} without any tenant_id constraint. This writes tenant-scoped data by id only, allowing potential cross-tenant mutation if an id is controlled or leaked.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding updates agent_knowledge SET embedding WHERE id = ${id} with no tenant_id check. agent_knowledge is tenant-scoped, so this is a tenant-isolation risk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/tools.ts:4566** _(internal-worker-pk)_ — SELECT from presenter_sessions (SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...) executes without constraining by tenant_id or otherwise verifying that the found presenter_session belongs to the caller's tenant. This can allow cross-tenant lookup of presenter session tokens or other sensitive session data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:4537 — The presenter_sessions SELECT keys on slideResult.presentationId, which was just created by this same create_slides tool invocation for the caller's own tenant (conversation ownership is asserted upstream at ~4500 via 'conversation not owned by tenant' and assertProjectInTenant). The presentation id is not attacker-supplied cross-tenant input; it is a self-generated art
- **server/tools.ts:8933** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' (marking rejected) uses only the row id in the WHERE clause with no tenant_id check.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/twilio.ts:117** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without a tenant_id constraint. Any SELECT against a tenant-scoped table must include tenant filtering; selecting by id alone can leak or mix tenant data if used elsewhere.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
- **server/delivery-pipeline.ts:203** _(deferred)_ — updateDeliveryLog performs an UPDATE on tenant-scoped table delivery_logs using only the delivery id (WHERE delivery_logs.id = id) with NO tenant_id constraint. This permits cross-tenant updates if an attacker/bug supplies an id belonging to another tenant.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/minerva-planner.ts:403** _(deferred)_ — SELECT child plan (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...) conditionally omits tenant filter. The query only adds tenant_id when args.tenantId is provided, allowing discovery/reads of child plans in other tenants when tenantId is omitted.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): child-plan SELECT applies the tenant filter conditionally (args.tenantId != null) — optional-tenant fail-open; hardening = require tenantId.
- **server/minerva-planner.ts:547** _(deferred)_ — getPlan allows unscoped SELECT when tenantId is omitted. The function returns SELECT * FROM plans WHERE id = ${planId} if tenantId is undefined, enabling cross-tenant reads when callers omit tenantId.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
