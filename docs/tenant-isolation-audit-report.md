# Tenant-Isolation Audit Report

_Generated 2026-09-18T12:11:18.936Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **682/682** file(s) audited · chunks 63/63 OK (0 failed)
- Findings: **228** (CRITICAL 55 · HIGH 135 · MEDIUM 37 · LOW 1)
- Severe dispositions: **8 kept** · 145 precision false-positive · 37 allowlist false-positive · 0 deferred real risk

### CRITICAL (55)

- **server/ab-optimizer.ts:110** — runDueAbExperiments executes a SELECT on the tenant-scoped `experiments` table without constraining to a specific tenant (no WHERE tenant_id = ...). This reads running A/B experiments across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant in a controlled loop. Ensure any background/cron code runs with an explicit tenant context so reads never span tenants.
- **server/agent-activity.ts:122** — completeActivity updates agentActivity rows using only the activity id (WHERE id = activityId) with no tenant constraint. The function signature lacks a tenantId, so callers can update rows across tenants.
  - _Fix:_ Require tenantId in the function signature and include it in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}). Validate ownership before updating to prevent cross-tenant modification.
- **server/agentic/approvals.ts:134** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to mark pending approvals expired without scoping by tenant_id, updating approvals across all tenants.
  - _Fix:_ Either run expiry per-tenant (require tenant context) or add a tenant filter to the UPDATE. If a global sweep is intended, ensure it has explicit admin authorization and audit logging; otherwise constrain the query to the relevant tenant(s).
- **server/channel-routing.ts:53** — setChannelRoute INSERT/UPSERT writes persona_id into channel_routes with no tenant context and does not validate that the provided personaId belongs to the caller's tenant. This lets a caller insert or overwrite a global mapping that references personas from other tenants.
  - _Fix:_ Require and pass tenantId to setChannelRoute, validate ownership first (e.g. SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}), and include tenant_id in the INSERT/ON CONFLICT (e.g. INSERT INTO channel_routes (tenant_id, channel, persona_id) VALUES (${tenantId}, ${channel}, ${personaId}) ON CONFLICT (tenant_id, channel) ...). Reject the operation if the persona is not owned by the tenant.
- **server/email.ts:429** — UPDATE against tenant-scoped table `inbox_messages` uses only id in WHERE (UPDATE ... WHERE id = ${row.id}) but the row was selected from a global (non-tenant-scoped) query; this allows updating a row without enforcing the tenant constraint and risks cross-tenant modification.
  - _Fix:_ Include tenant context in both the SELECT and the UPDATE. Select tenant_id in the initial query and perform the update with WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} (or add AND tenant_id = ${tenantId}). Do not perform backfill writes without verifying tenant ownership.
- **server/embeddings.ts:809** — storeEmbeddingVec falls back to executing UPDATE ${sql.raw(table)} ... WHERE id = ${id} when no tenantId is supplied, performing an UPDATE on a tenant-scoped table without any tenant_id constraint. This can overwrite rows across tenants.
  - _Fix:_ Require a positive tenantId for tenant-scoped tables and include AND tenant_id = ${tenantId} in the WHERE clause. Remove the unconditional raw-table update path or make it an admin-only, explicitly-audited operation that still enforces tenant scoping when appropriate.
- **server/embeddings.ts:855** — backfillEmbeddingVecs performs UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} without constraining by tenant. Updating by id alone on a tenant-scoped table (after a global SELECT) can modify rows across tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} or tenantId passed in). Ensure initial SELECT returns tenant_id so updates can be scoped properly; never update tenant-scoped tables by id only.
- **server/embeddings.ts:872** — backfillEmbeddingVecs updates `agent_knowledge` rows via UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without constraining by tenant_id. This permits cross-tenant writes based solely on id.
  - _Fix:_ Include tenant_id in the UPDATE (WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} or pass tenantId explicitly). Ensure any backfill or update path enforces tenant ownership before modifying tenant-scoped rows.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table "agent_runs" without constraining tenant_id (no WHERE tenant_id = ...). The boot-time rehydration query returns rows across all tenants, violating tenant isolation.
  - _Fix:_ Restrict the query to a specific tenant or iterate per-tenant with explicit authorization. For example, accept/derive a tenantId and add a tenant filter (parameterized) to the query: WHERE tenant_id = ${tenantId} AND status IN (...) ...; or refactor to perform per-tenant queries (or run only in a tightly-controlled admin context with audited access). Use parameterized Drizzle filters (e.g. .where(eq(agentRuns.tenantId, tenantId))) rather than unbounded selects.
- **server/glasses-gateway.ts:158** — SELECT from apiKeys does not include a tenant_id constraint. Looking up API keys by keyHash alone queries a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Constrain the lookup by tenant (or otherwise ensure the lookup is scoped to an auth context). Example: include eq(apiKeys.tenantId, expectedTenant) in the WHERE or perform a lookup that ties the key to the tenant before returning the row. Ensure any SELECT on apiKeys always includes tenant_id or an equivalent ownership check.
- **server/glasses-gateway.ts:174** — UPDATE on apiKeys uses only id in WHERE (no tenant_id constraint). This writes into a tenant-scoped table without scoping to tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) so the update cannot modify a row belonging to a different tenant.
- **server/memory-graph.ts:130** — UPDATE against tenant-scoped table memory_categories lacks a tenant_id predicate (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This allows a category id from another tenant to be modified.
  - _Fix:_ Include the tenant predicate in the UPDATE WHERE clause (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or perform the UPDATE inside a tenant-scoped transaction / helper that enforces the tenant. Also validate that the category id returned by ensureCategory belongs to the same tenant before updating.
- **server/memory-graph.ts:709** — UPDATE against tenant-scoped table memory_entries lacks a tenant_id predicate (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). This can update a memory row belonging to a different tenant if ids are crafted.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or perform the write via a helper that enforces tenant ownership. Additionally, verify the mem.id was loaded from the expected tenant before performing the update.
- **server/oauth-subscriptions.ts:906** — Query selects FROM oauth_subscriptions WHERE is_active = TRUE without constraining by tenant_id — reads active subscriptions across all tenants (sensitive token metadata).
  - _Fix:_ Restrict the query to a specific tenant (add WHERE tenant_id = ${tenantId}) or, if you truly need to operate across tenants, iterate tenants explicitly and fetch each subscription with a tenant_id predicate. Avoid returning secret token material in bulk; limit columns to non-sensitive metadata and require a privileged/system-only execution path with audit logging.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh reads oauth_subscriptions WHERE is_active = TRUE without tenant_id — iterates all tenants' subscriptions and may process secrets/refresh logic across tenants.
  - _Fix:_ Change the refresh strategy to operate per-tenant: query the tenants you intend to refresh and then SELECT/UPDATE oauth_subscriptions WITH WHERE tenant_id = ${tenantId} for each tenant. Alternatively, ensure this routine runs under a hardened, audited system identity and only selects minimal non-secret fields before performing tenant-scoped operations with explicit tenant filters.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions SELECTs provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id constraint — enumerates all tenants' subscriptions.
  - _Fix:_ If you need to refresh all subscriptions, implement a controlled per-tenant loop: first enumerate tenant IDs from a trusted tenants table (or an explicit admin-provided list), then perform SELECT/UPDATE on oauth_subscriptions using WHERE tenant_id = ${tenantId}. Ensure any mass-refresh operation is authorized, logged, and avoids returning or exposing sensitive tokens without tenant-scoped predicates.
- **server/orchestrator-ledger.ts:465** — UPDATE to tenant-scoped table memory_entries performed without constraining by tenant_id (WHERE id = ${row.id} only). This can allow an update to affect a row belonging to a different tenant if id collisions or misuse occur.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause. e.g. UPDATE memory_entries SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer defensive multi-column WHEREs rather than relying on a previously read row alone.
- **server/plan-executor.ts:613** — UPDATE on tenant-scoped table `plans` performed without constraining by tenant_id: `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...`. This allows claiming/updating a plan row from any tenant if the caller supplies a plan id.
  - _Fix:_ Restrict the CAS update to the plan's owning tenant (e.g. add `AND tenant_id = ${callerTenantId}`), or require the caller to pass an authenticated tenantId and validate it before the UPDATE. Alternatively SELECT the row first (with tenant filter), verify ownership, then perform the status transition using the validated tenant_id in the WHERE clause.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... has no tenant scoping — it scans projects/conversations across all tenants and will expose/misprocess data from other tenants.
  - _Fix:_ Restrict backfill to a single tenant at a time (accept a tenantId parameter) or add WHERE p.tenant_id = ${tenantId} (and c.tenant_id = ${tenantId}) so only that tenant's projects/conversations/messages are processed. Never run cross-tenant reads that surface tenant-scoped data.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM `conversations` WHERE c.project_id = ${proj.id} is executed without tenant_id filtering — conversation rows may belong to other tenants.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or derive tenant from the owning project and verify it before reading conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM `messages` WHERE conversation_id = ${conv.id} is executed without a tenant filter — this can read messages from other tenants' conversations.
  - _Fix:_ Include tenant scoping on message reads (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or ensure conversation ownership is verified before querying messages. All message/conversation reads must be constrained to the caller's tenant.
- **server/research-engine.ts:170** — UPDATE on tenant-scoped table research_sessions performed without any tenant_id constraint (cleanupZombieSessions). This will mark running sessions across all tenants as completed (cross-tenant modification).
  - _Fix:_ Restrict the UPDATE to a specific tenant. Either run the cleanup per-tenant (group activeSessions by tenantId and add `AND tenant_id = ${tenantId}`), or include an explicit tenant filter in the WHERE clause. Ensure any bulk/daemon operations never update tenant-scoped rows across tenants without an explicit tenant_id condition.
- **server/routes/projects.ts:568** — INSERT INTO project_notes does not include tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT; otherwise tenant isolation can be violated or inserts can fail if tenant_id is NOT NULL.
  - _Fix:_ Include tenant_id in the INSERT and pass the authenticated tenant (tenantId). Example: INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) RETURNING *; or derive/validate tenantId from the project and use it.
- **server/routes/projects.ts:608** — INSERT INTO project_files (base64 upload path) does not include tenant_id. Tenant-scoped tables require tenant_id on INSERT.
  - _Fix:_ Add tenant_id to the INSERT and bind the authenticated tenant (tenantId). Example: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (${projectId}, ${f.fileName}, ${'uploads/' + uniqueName}, ${fileUrl || downloadPath}, ${f.mimeType || 'application/octet-stream'}, ${fileBuffer.length}, ${tenantId}) RETURNING *; validate project ownership before inserting.
- **server/routes/projects.ts:676** — INSERT INTO project_files (multipart upload path) does not include tenant_id. Tenant-scoped INSERTs must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the INSERT values (tenant_id = ${tenantId}) and ensure the caller owns the referenced project before inserting.
- **server/sculptor.ts:106** — UPDATE on sculptor_sessions lacks tenant_id constraint (UPDATE sculptor_sessions SET status = ... WHERE id = ${session.id})
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise validate the session belongs to the caller's tenant before updating.
- **server/sculptor.ts:113** — UPDATE on sculptor_sessions lacks tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id})
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or verify ownership prior to writing to avoid cross-tenant modification.
- **server/sculptor.ts:168** — UPDATE on sculptor_sessions in monitorSession lacks tenant_id constraint (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId})
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) since monitorSession receives tenantId; always enforce per-tenant constraint on writes.
- **server/sculptor.ts:358** — UPDATE on sculptor_sessions lacks tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId})
  - _Fix:_ Include tenant filtering (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or re-fetch and assert session. Do not perform writes on tenant-scoped tables without tenant_id protection.
- **server/second-opinion.ts:502** — UPDATE on agent_cost_ledger uses only reservation id in WHERE (WHERE id = ${reservationId}) with no tenant_id constraint
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). If tenantId is not available in this function, change the API to accept/derive the tenantId and validate the reservation row belongs to that tenant before updating.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks has no tenant_id filter — reads tenant-scoped table globally
  - _Fix:_ Scope the query to the intended tenant (e.g. WHERE enabled = true AND type = 'delegation' AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise restrict to the correct tenant to avoid cross-tenant effects.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' ... has no tenant_id constraint — may disable rows across tenants
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE type = 'delegation' AND enabled = true AND tenant_id = ${ADMIN_TENANT_ID}) so the operation only affects the intended tenant.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE name ILIKE '%session%logger%' lacks tenant_id filter
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant) in the WHERE clause to avoid counting across tenants.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' ... has no tenant_id constraint
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) to avoid disabling tasks in other tenants.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has no tenant_id filter
  - _Fix:_ Add tenant scoping (AND tenant_id = ${ADMIN_TENANT_ID}) to the SELECT and subsequent UPDATE to ensure only the target tenant's rows are inspected/modified.
- **server/seed.ts:2240** — SELECT that scans tenant-scoped tables (conversations JOIN projects) without restricting to a tenant, then subsequent INSERTs create project_conversations rows based on that result. This performs cross-tenant reads and writes (no WHERE tenant_id = ...), risking cross-tenant data exposure or cross-tenant linking.
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. WHERE c.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}) or explicitly to ADMIN_TENANT_ID if the operation is owner-only. Ensure any INSERT into derived link tables either includes a tenant_id column or is only performed for rows verified to belong to the intended tenant. Prefer Drizzle .where(eq(table.tenantId, tenantId)) for clarity and safety.
- **server/self-improvement.ts:427** — UPDATE against tenant-scoped table `experiments` uses only the primary key in the WHERE clause (where(eq(experiments.id, experiment.id))) and lacks a tenant_id constraint. This allows updating an experiment row belonging to a different tenant if an attacker/bug can guess the id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add tenant protection to the WHERE clause, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Prefer parameterized Drizzle predicates rather than interpolating ids into raw SQL.
- **server/skillify.ts:91** — SELECT from 'messages' (recentMsgs) is executed without constraining by tenant_id. This can leak messages across tenants.
  - _Fix:_ Pass and use the caller's tenantId in the query (e.g. add AND messages.tenant_id = ${tenantId}), or derive/validate tenant ownership from the conversation row and enforce it in the WHERE clause before returning messages.
- **server/skillify.ts:201** — SELECT from 'messages' inside analyzeConversation is missing a tenant_id constraint (reads all messages for a conversation without verifying tenant).
  - _Fix:_ Constrain the messages query to the conversation's tenant (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}), or join messages against conversations and validate tenantId to ensure tenant isolation.
- **server/skillify.ts:329** — SELECT from 'messages' in skillifyConversation (recentMsgs) is executed without tenant scoping. Unscoped reads of message content can disclose other tenants' data.
  - _Fix:_ Require and pass tenantId into this flow and add a tenant_id filter to the messages query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}). Alternatively, fetch messages through a helper that enforces tenant ownership.
- **server/skills-marketplace.ts:235** — SELECT * FROM skills WHERE id = ${skillId} is performed with no tenant constraint. Exporting skill details without tenant scoping can disclose another tenant's skill content.
  - _Fix:_ Require and accept a tenantId in exportSkill and constrain the SELECT by tenant_id = ${tenantId}. If export should operate on global templates only, ensure the skills table is global or use a separate global table.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks is executed without a tenant_id filter (DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' AND created_at < ...). This can delete rows across all tenants.
  - _Fix:_ Either restrict cleanup to a specific tenant (add AND tenant_id = ${tenantId}) or explicitly iterate tenants and perform per-tenant deletions, ensuring the operation is authorized for each tenant. If truly global maintenance is required, make the global intent explicit and gate it via an admin-only path.
- **server/stability-watchdog.ts:177** — SELECT from heartbeat_logs to detect flaky tasks has no tenant_id constraint (GROUP BY task_id, task_name HAVING COUNT(*) >= 4). This can surface failures across tenants and may produce actions that affect other tenants' tasks.
  - _Fix:_ Add tenant scoping to the query (WHERE tenant_id = ${tenantId} AND status = 'error' ...) or run the detection per-tenant with explicit authorization. Ensure subsequent remedial writes (disable/update) are scoped to the affected tenant.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs WHERE created_at < NOW() - INTERVAL '... hours' is executed without tenant_id scoping. This prunes logs across all tenants.
  - _Fix:_ Perform per-tenant pruning (add AND tenant_id = ${tenantId}) or explicitly accept an admin/all-tenants mode that is tightly controlled. Avoid blanket deletions that affect all tenant data without explicit per-tenant checks.
- **server/storage.ts:1409** — getAllDataForExport performs SELECTs across many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with no tenant_id filtering, returning platform-wide tenant data. There is no explicit tenant scope/ALL_TENANTS sentinel — this exposes all tenants' data.
  - _Fix:_ Restrict this API to an authenticated, audited admin-only path and require an explicit ALL_TENANTS sentinel parameter if platform-wide export is intended. Otherwise require a tenantId and add WHERE eq(table.tenant_id, tenantId) (or use requiredTenantScope) for every tenant-scoped table. Add authorization checks and logging/auditing for any platform-wide export.
- **server/tools.ts:7006** — INSERT into project_files omits tenant_id. The INSERT uses params.project_id etc but does not set tenant_id on the tenant-scoped table (project_files). Relying on prior assertProjectInTenant is insufficient for the invariant that every INSERT must include tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (or use drizzle's insert API with a tenantId field). Example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Alternatively use db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: params.project_id, ... }). Always pass the caller's tenant id as a NOT NULL column on tenant-scoped tables.
- **server/tools.ts:8978** — UPDATE on proposed_skills uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) without constraining tenant_id. proposed_skills is written with tenantId on insert, but subsequent updates lack a tenant filter, allowing cross-tenant modification by id.
  - _Fix:_ Constrain updates to the caller's tenant. Add tenant guard to the WHERE clause, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) where tid is the current tenant id. Alternatively, select+verify tenant ownership before update. Always include tenant_id in UPDATE WHERE for tenant-scoped tables.
- **server/tools.ts:9007** — UPDATE on proposed_skills uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) without constraining tenant_id, allowing modifications across tenants.
  - _Fix:_ Restrict the UPDATE by tenant: add a tenant predicate like .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Ensure tid (the caller tenant) is used and validated. Prefer using the drizzle API with an explicit tenant filter on all updates to tenant-scoped tables.
- **server/tools.ts:9021** — UPDATE on proposed_skills (marking accepted/promotedSkillId) lacks a tenant_id constraint in the WHERE clause; it updates by id only, risking cross-tenant changes.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Validate that tid is the caller's tenant id. All UPDATEs to tenant-scoped tables must include tenant constraints.
- **server/tools.ts:9038** — UPDATE on proposed_skills (marking rejected) uses only id in WHERE and omits tenant_id, allowing a caller to alter another tenant's proposed_skills row by id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Alternatively, perform a SELECT to verify ownership before updating. Enforce tenant-scoped WHERE clauses on every UPDATE/DELETE against tenant tables.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table `browser_workflows` omits tenant scoping — `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` updates by id only and lacks `tenant_id = <caller tenant>` constraint, enabling cross-tenant modification.
  - _Fix:_ Include the caller's tenant in the WHERE clause (or otherwise validate ownership) so writes are tenant-scoped. Example: `... WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or use the Drizzle eq(table.tenantId, tenantId) helper). Always enforce tenant_id on UPDATE/DELETE for tenant-scoped tables.
- **server/tools/domains/files/handlers.ts:937** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES clause. Every insert into a tenant-scoped table must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include the caller's tenant id when inserting, e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...), or use the application's Drizzle insert builder ensuring tenant_id is set from ctx.tenantId. Verify the inserted row cannot be orphaned/created under the wrong tenant.
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (only filters by id). This allows updating a reservation row by id across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or fetch/verify the row's tenant_id before updating. Prefer passing tenantId into settleVentureReservation and enforcing eq(agent_cost_ledger.tenant_id, tenantId) in the WHERE clause.
- **server/venture-discovery/budget.ts:169** — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (only filters by id) when releasing a reservation. This can modify another tenant's ledger row if an attacker supplies a reservation id.
  - _Fix:_ Same fix as above: require and use the owning tenantId in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or validate ownership before performing the UPDATE. Propagate tenantId into releaseVentureReservation to enforce this.
- **server/webhook-triggers.ts:255** — SELECT from tenant-scoped table `webhook_triggers` executed without any tenant_id constraint (lookup by webhook_key only). This allows a caller with a webhook key to read trigger rows across all tenants and then act in the context of the returned row(s).
  - _Fix:_ Restrict the lookup to an explicit tenant scope or otherwise prove ownership before returning tenant-scoped rows. For example, require/derive an expected tenantId and run: SELECT ... FROM webhook_triggers WHERE webhook_key = ${webhookKey} AND tenant_id = ${expectedTenantId}. If webhook_key is intended to be a global capability, either document/make the table non-tenant-scoped or embed tenant ownership in the capability and validate it before reading any tenant-scoped data.

### HIGH (135)

- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs rows from tenant-scoped `ai_insights` without a tenant filter, reading pending auto-applied insights across all tenants.
  - _Fix:_ Scope the sweep per-tenant (accept a tenantId or loop tenants explicitly) or include WHERE tenant_id = ${tenantId} to avoid cross-tenant reads. Validate tenant_id for each row before routing.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights issues a bulk UPDATE on tenant-scoped `ai_insights` and, when called with undefined tenantId, the tenantScope placeholder is empty — causing a global (cross-tenant) UPDATE.
  - _Fix:_ Make tenantId required or fail when undefined. Always include an explicit tenant_id predicate in the UPDATE (e.g. WHERE tenant_id = ${tenantId} AND ...). If intentional global admin behavior is needed, require a distinct, well-audited admin path.
- **server/agentic-features.ts:861** — enrollInSequence checks for existing outreach_enrollments with SELECT ... WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant_id — a read against a tenant-scoped table without tenant scoping.
  - _Fix:_ Include tenant_id in the existence check (AND tenant_id = ${params.tenantId}) to ensure the check is limited to the caller's tenant. Validate sequence ownership (already done earlier) and use tenant-scoped queries consistently.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table with no tenant_id filter — this reads rows across all tenants.
  - _Fix:_ Either scope the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or, if an admin/global sweep is required, make the admin intent explicit and protect the call with an explicit admin-only guard/audit (and document why cross-tenant reads are required). Avoid unscoped selects on tenant-scoped tables — prefer iterating tenants with an explicit, authorized operation.
- **server/agentic/resume-worker.ts:65** — SELECT query scans tenant-scoped tables (agent_runs and agent_approvals) without constraining by tenant_id. The initial db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`) has no WHERE r.tenant_id = <tenant> clause, allowing cross-tenant reads of sensitive rows.
  - _Fix:_ Limit the sweep to a single tenant or otherwise include an explicit tenant_id predicate. For example, accept a tenantId argument (or iterate per-tenant in a safe coordinator) and add WHERE r.tenant_id = ${tenantId} (or the Drizzle equivalent eq(agentRuns.tenantId, tenantId)) to the query. Do NOT rely on post-processing to enforce isolation; every SELECT/UPDATE/DELETE against tenant-scoped tables must include an application-level tenant_id filter.
- **server/agentic/wake-scheduler.ts:142** — UPDATE/SELECT on tenant-scoped table `agent_wake_schedules` performed without any tenant_id constraint. The subquery SELECT id FROM agent_wake_schedules ... FOR UPDATE SKIP LOCKED can claim rows across all tenants, causing cross-tenant row mutation and event emission.
  - _Fix:_ Restrict the claim to a single tenant at a time (e.g. run the sweep per-tenant) or add an explicit tenant_id filter to both the inner SELECT and outer UPDATE. Example: iterate tenants and use WHERE tenant_id = ${tenantId} AND status = 'pending' ... FOR UPDATE SKIP LOCKED, or include tenant_id = w.tenant_id consistently in the UPDATE predicate.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT ... FROM conversations queries the tenant-scoped `conversations` table with no tenant_id WHERE clause. This reads potentially many tenants' conversation rows from a background job without tenant scoping.
  - _Fix:_ Include tenant scoping in the query or run the backfill per-tenant. For example, either iterate over tenant ids and run WHERE c.tenant_id = ${tenantId}, or add a WHERE clause restricting to an intended tenant set to avoid cross-tenant reads.
- **server/auto-project.ts:135** — SELECT from messages reads conversation messages without constraining by tenant_id. The query uses conversation_id but does not include an explicit tenant_id filter, violating the invariant that every read against a tenant-scoped table must include tenant_id = <caller tenant>.
  - _Fix:_ Add an explicit tenant constraint to the messages query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or JOIN via a conversation row already constrained to the tenant and include m.tenant_id = ${tenantId} in the WHERE clause to enforce isolation.
- **server/auto-transcript.ts:46** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} executes against the messages table without an explicit tenant_id filter. Even though conversation ownership was verified earlier, the invariant requires each tenant-scoped table access to include the tenant filter.
  - _Fix:_ Include tenant scoping on the messages read, e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}, or explicitly JOIN messages to conversations with the conversation's tenant_id enforced in the WHERE clause.
- **server/auto-transcript.ts:132** — Backfill query SELECT DISTINCT c.id,... WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 performs a subquery against messages without scoping by tenant_id. This reads a tenant-scoped table (messages) across tenants.
  - _Fix:_ Restrict the subquery to the conversation's tenant, for example: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0. Ensure all subqueries against tenant-scoped tables include an explicit tenant_id filter.
- **server/auto-transcript.ts:160** — In the 'alsoLinked' query the subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 reads messages without tenant scoping. This can expose cross-tenant data when scanning project_conversations/conversations.
  - _Fix:_ Add tenant scoping inside the subquery (e.g. AND tenant_id = c.tenant_id) so the COUNT only considers messages belonging to the same tenant as the conversation.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL updates the conversations table without constraining by tenant_id. The backfill loop derived c.tenant_id earlier but the UPDATE itself lacks the tenant filter and can modify rows across tenants if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}, or bind tenantId as a parameter to the query to ensure the operation only affects the intended tenant's rows.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id (no tenant scoping on the insert). If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this INSERT omits tenant_id and violates the app-level isolation invariant.
  - _Fix:_ Include the tenant id explicitly when inserting invoice line items and/or ensure the table is not tenant-scoped. For example, INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). Also ensure the invoice_id being used was created for the caller's tenant (the surrounding transaction creates the invoice with tenant_id=${tid}, so propagate ${tid} into invoice_items INSERT to make tenant ownership explicit).
- **server/channel-routing.ts:16** — channel_routes table is created without a tenant_id column, making routing records global. In a multi-tenant system this allows one tenant to affect or observe another tenant's channel routing and persona mappings.
  - _Fix:_ Make channel_routes tenant-scoped by adding a NOT NULL tenant_id column and update the UNIQUE constraint to include tenant_id (e.g. UNIQUE(tenant_id, channel)). Backfill existing rows appropriately and update all queries to filter by tenant_id.
- **server/channel-routing.ts:30** — listChannelRoutes SELECT joins personas (p) but does not constrain the query to a tenant. If personas are tenant-scoped this can leak or mix persona names across tenants because there's no tenant_id filter on channel_routes or personas.
  - _Fix:_ Require a tenantId parameter and include it in the query (e.g. WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}). Alternatively, make channel_routes tenant-scoped and filter cr.tenant_id = ${tenantId} and ensure the join also enforces p.tenant_id = ${tenantId}.
- **server/channel-routing.ts:63** — getPersonaForChannel SELECT queries channel_routes by channel only and returns persona_id with no tenant filtering. This can return mappings belonging to other tenants.
  - _Fix:_ Require a tenantId parameter and add it to the WHERE clause (e.g. WHERE channel = ${channel} AND tenant_id = ${tenantId}). Ensure the channel_routes table is tenant-scoped as above.
- **server/channel-routing.ts:71** — removeChannelRoute deletes by channel only (DELETE FROM channel_routes WHERE channel = ${channel}) with no tenant constraint, which can remove other tenants' routing entries.
  - _Fix:_ Require tenantId and include it in the DELETE WHERE clause (e.g. DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}). If channel_routes is made tenant-scoped, use tenant_id in all mutating statements.
- **server/chat-engine.ts:3805** — call to markReviewReused(conversationId, toolName) (capability-review) is invoked WITHOUT passing tenantId. If that function performs any DB reads/writes keyed only by conversationId it can bypass tenant-scoped filtering and break tenant isolation.
  - _Fix:_ Pass the tenantId into markReviewReused (and any other capability-review APIs) and update the capability-review implementation to require tenantId for all DB access. Ensure every SELECT/UPDATE/DELETE includes WHERE tenant_id = <tenantId> and every INSERT sets tenant_id explicitly (NOT NULL, no default). Audit capability-review queries for any unscoped use of conversationId.
- **server/chat-engine.ts:3926** — call to recordErrorForRetryStormDetection(conversationId, ...) (stuck-diagnostics) is invoked WITHOUT tenantId. The stuck-diagnostics hook may persist or read diagnostic rows by conversationId only, enabling cross-tenant access/modification.
  - _Fix:_ Include tenantId when calling recordErrorForRetryStormDetection and any other stuck-diagnostics APIs. Update stuck-diagnostics to accept tenantId and apply tenant-scoped filtering on all queries (WHERE tenant_id = ?) and ensure inserts explicitly set tenant_id. Perform a review of stuck-diagnostics for any other DB calls missing tenant scoping.
- **server/chat-engine.ts:4161** — calls to recordToolCallForStuckDetection(...) and postDiagnosticReport(...) (stuck-diagnostics) are invoked WITHOUT passing tenantId. These functions may write/read diagnostic/report rows keyed only by conversationId, risking tenant-cross contamination.
  - _Fix:_ Modify calls to recordToolCallForStuckDetection and postDiagnosticReport to include tenantId. Update the stuck-diagnostics module so all queries include WHERE tenant_id = <tenantId> and all INSERTs set tenant_id explicitly. Audit the module to ensure no query or insert relies solely on conversationId or other unvalidated foreign ids.
- **server/commitment-drafter.ts:234** — UPDATE on commitments (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}) is missing a tenant_id constraint. This is a cross-tenant writable operation and can alter another tenant's row by id.
  - _Fix:_ Add explicit tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and keep any status guards (e.g. AND draft_status = 'draft_pending') to avoid touching rows owned by other tenants. Always include tenant_id in UPDATE/DELETE statements on tenant-scoped tables or verify ownership before mutating.
- **server/commitments.ts:135** — SELECT ... FROM commitments (scanAndEscalate) queries overdue commitments across all tenants without a WHERE tenant_id = ... constraint. This background scanner reads tenant-scoped rows platform-wide and then triggers per-row actions (including emailing an owner) based on those results.
  - _Fix:_ Restrict the scan to a single tenant when running in tenant-context (add WHERE tenant_id = ${tenantId}) or ensure this function is explicitly an admin/operator-only routine with enforced authorization and minimized data returned (redact tenant-sensitive fields). If cross-tenant visibility is required for operators, document and gate it behind explicit admin-only checks and ensure any notifications do not leak tenant-private content or identifiers beyond what is strictly necessary.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate per-tenant explicitly. If this maintenance job must touch all tenants, run it in a privileged, auditable admin context and minimize returned columns, but do NOT perform ad-hoc cross-tenant SELECTs from request-handling code.
- **server/deep-interview.ts:338** — INSERT into tenant-scoped table `project_notes` does not set tenant_id (INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ...). Tenant_id columns are NOT NULL with no default — writes must include tenant_id or validate ownership.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO project_notes (project_id, tenant_id, content, created_at) VALUES (...) or SELECT p.id, p.tenant_id, ${briefContent}, NOW() ...) and/or validate that the project belongs to the caller tenant before inserting. Prefer explicit tenant_id binding rather than relying on joins or implicit defaults.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken performs SELECT FROM dfy_intake_forms without constraining by tenant_id. This reads a tenant-scoped row using only a request-derived token (no tenant filter).
  - _Fix:_ Include the tenant_id in the WHERE clause (or require the caller to provide/derive a tenantId and validate ownership) when selecting dfy_intake_forms. Example: .where(and(eq(dfyIntakeForms.token, token), eq(dfyIntakeForms.tenantId, tenantId))). If the token is intentionally global/public, document the rationale and ensure additional application-level protections (e.g. token scoping) are in place.
- **server/email.ts:370** — SELECT on tenant-scoped table `inbox_messages` is executed without constraining by tenant (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can cause cross-tenant collisions/leaks (a message_id belonging to another tenant will be treated as 'already stored').
  - _Fix:_ Include a tenant constraint (or inbox identifier) in the WHERE clause, e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} (or AND tenant_id = ${tenantId}). Ensure the existence check is performed per-tenant/inbox.
- **server/email.ts:415** — Global SELECT from tenant-scoped table `inbox_messages` (SELECT id, message_id FROM inbox_messages WHERE ... ) has no tenant filter. This reads rows across all tenants and can surface or operate on other tenants' data.
  - _Fix:_ Scope the backfill to a tenant (pass tenantId) or iterate per-tenant. Add WHERE tenant_id = ${tenantId} (or inbox_id = ${inboxId}) to the SELECT and fetch only rows for that tenant before processing.
- **server/embeddings.ts:814** — backfillMissingKnowledgeEmbeddings runs a SELECT on tenant-scoped table `agent_knowledge` without any tenant filter (SELECT id, tenant_id, title, content FROM agent_knowledge ... LIMIT 50). This reads rows for all tenants in one operation.
  - _Fix:_ Run backfill per-tenant (iterate tenants) or otherwise constrain the SELECT with WHERE tenant_id = ${tenantId}. If global admin backfills are necessary, make that explicit and audit/authorize the operation.
- **server/embeddings.ts:845** — backfillEmbeddingVecs selects rows from tenant-scoped `memory_entries` without a tenant filter (SELECT id, embedding FROM memory_entries ... LIMIT 200). This reads active memory entries across all tenants.
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant to backfill embeddings. Avoid scanning tenant-scoped tables globally without explicit admin intent and authorization.
- **server/embeddings.ts:861** — backfillEmbeddingVecs SELECT on tenant-scoped table `agent_knowledge` lacks tenant scoping (SELECT id, embedding FROM agent_knowledge ... LIMIT 200), reading rows across all tenants.
  - _Fix:_ Perform backfill per-tenant or add WHERE tenant_id = ${tenantId} to the query. If run as a global admin job, make the cross-tenant nature explicit, ensure authorization, and include explicit tenant scoping on subsequent writes.
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scrapedPages' uses only id in WHERE (no tenant_id constraint). This allows an update to a row by id regardless of tenant ownership.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or otherwise verify ownership before issuing the UPDATE to enforce tenant isolation.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' uses only id in WHERE (no tenant_id constraint). This allows an update to a row by id regardless of tenant ownership.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or otherwise verify ownership before issuing the UPDATE to enforce tenant isolation.
- **server/google-drive.ts:866** — SELECT from projects is performed using only project id (where eq(projects.id, projectId)) and does not include tenant_id in the WHERE. Reading a tenant-scoped projects row without scoping risks cross-tenant access.
  - _Fix:_ Scope the SELECT by tenant (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise verify ownership in the same query before returning any project data.
- **server/google-drive.ts:879** — UPDATE on projects sets driveFolderId/driveFolderUrl but the WHERE clause uses only project id (no tenant_id constraint). This mutates a tenant-scoped row without enforcing the caller's tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the update cannot affect another tenant's project.
- **server/google-drive.ts:1476** — SELECT from projects (resolving projectId in uploadAndShare) queries projects by id only (where eq(projects.id, params.projectId)) without restricting by tenant_id prior to using the result. This reads a tenant-scoped row without an ownership constraint.
  - _Fix:_ Add tenant scoping to the project lookup (e.g. include AND eq(projects.tenantId, params.tenantId) when available) or perform a separate explicit ownership check before proceeding, and fail-closed if ownership cannot be proven.
- **server/google-drive.ts:1723** — SELECT from projects in searchDriveFiles queries the project by id (where eq(projects.id, params.projectId)) without including tenant_id in the WHERE. Reading projects without scoping allows cross-tenant disclosure.
  - _Fix:_ Constrain the query by tenant (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or return an error if the project is not owned by the caller's tenant. Ensure all reads of tenant-scoped 'projects' include tenant_id.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id. The raw INSERT/UPSERT uses session_id but never sets or checks tenant ownership, allowing cross-tenant persistence if session_id is incorrect or attacker-controlled.
  - _Fix:_ Make presenter_slide_images tenant-scoped by including tenant_id in the INSERT (e.g. VALUES (${tenantId}, ${sessionId}, ...)) or otherwise validate that sessionId belongs to the expected tenant before inserting. If the table is multi-tenant, ensure tenant_id is NOT NULL and always supplied from the trusted execution context (the function's tenantId), or add a prior check/JOIN that verifies session→tenant ownership and fail-closed.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} executes with no tenant constraint. This mutates a likely tenant-scoped table (presenter_sessions) by primary key only, without verifying the session belongs to the caller's tenant.
  - _Fix:_ Require tenant scoping on the update, e.g. UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, first query and verify that presenter_sessions.id = sessionId belongs to the expected tenant, then perform the update. Do not rely on sessionId alone without ownership checks.
- **server/health-audit.ts:314** — SELECT FROM code_proposals is executed without any tenant_id filter. If code_proposals is a tenant-scoped table, this read will expose/allows operating across all tenants.
  - _Fix:_ If this scanner is intended to operate per-tenant, add a tenant_id = ${tenantId} WHERE clause and accept an explicit tenant context. If it is intended to be an admin/global operation, require and assert an explicit admin authorization path (do not run under normal tenant privileges), and document that this action intentionally enumerates across tenants. Prefer explicit admin-only gated code or include tenant_id when performing tenant-scoped maintenance.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) runs without tenant_id constraint. This modifies proposals across all tenants by id list with no tenant scoping or ownership check.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or ensure the ids list is prefiltered to only include rows owned by the expected tenant. If the procedure is an admin global cleanup, require an explicit admin authorization guard before performing cross-tenant updates and document the intent.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks is executed with no tenant_id filter. heartbeat_tasks appear to be tenant-aware in other parts of the app; running this query globally can read tasks for all tenants.
  - _Fix:_ Scope heartbeat task scans by tenant_id when performing per-tenant audits, or require an explicit admin context/authorization if the scan must be global. Prefer WHERE tenant_id = ${tenantId} (or equivalent) or verify caller is an admin before touching tenant-scoped tables.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) performs tenant-agnostic updates. This can disable tasks across tenants since no tenant_id ownership is checked.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or validate each task id belongs to the allowed tenant(s) before mutating. If this is intended to be an admin/global action, require an explicit admin authorization check and fail-closed for non-admins.
- **server/heartbeat-startup.ts:80** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). This mutates tenant-scoped rows by id only and lacks explicit tenant scoping.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage helper that requires tenant context (storage.updateHeartbeatTask) after asserting the intended tenant via assertTenantContext. Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id.
- **server/heartbeat-startup.ts:85** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). Missing tenant filter allows cross-tenant mutation.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or call storage.updateHeartbeatTask which accepts an explicit tenantId after asserting context. Never run unscoped UPDATEs against tenant-scoped tables.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks ... WHERE id = ${id} ... RETURNING id performed without tenant_id constraint in WHERE clause. This updates task rows without asserting tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. AND tenant_id = ${tenantId}) or ensure the caller provides and asserts tenantId for each id before issuing the UPDATE. Alternatively, use a storage helper that enforces tenant context.
- **server/heartbeat.ts:2679** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint after creating a tenant-scoped task. The UPDATE can affect tenant rows without explicit tenant scoping.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use storage.updateHeartbeatTask which requires an explicit tenantId. Ensure all writes/updates to tenant-scoped tables include tenant_id.
- **server/heartbeat.ts:3151** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint in delegateTaskFromChat. Missing tenant filtering permits cross-tenant mutation.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId}) or use the storage abstraction that enforces tenant context. Validate and assert the tenant before performing any UPDATE on tenant-scoped tables.
- **server/job-queue.ts:160** — UPDATE on tenant-scoped table `agent_jobs` (claimDueJobs) performs a global claim across all tenants — the UPDATE/SELECT lacks any tenant_id constraint, allowing cross-tenant mutation/claiming of jobs.
  - _Fix:_ Scope claims per-tenant (or require an explicit admin/system context). Add tenant_id to the WHERE clause (or accept a tenantId param) so the inner SELECT only returns rows for that tenant. Alternatively iterate tenants and claim per-tenant to ensure every UPDATE/SELECT against agent_jobs includes a WHERE tenant_id = <tenant> guard.
- **server/job-queue.ts:195** — UPDATE in reclaimExpiredLeases marks expired leases as failed_terminal without a tenant_id filter (mutates `agent_jobs` across all tenants).
  - _Fix:_ Include tenant scoping when transitioning rows (e.g. perform per-tenant reclaim or accept a tenantId for scoped reclaim). Ensure every UPDATE touching `agent_jobs` includes AND tenant_id = <tenant> (or run as an explicit platform operation that documents/limits cross-tenant scope).
- **server/job-queue.ts:209** — UPDATE in reclaimExpiredLeases (requeue step) re-queues expired 'running' rows back to pending with no tenant_id constraint, mutating rows across all tenants.
  - _Fix:_ Make the requeue step tenant-scoped (e.g. perform per-tenant reclaims) or otherwise ensure only rows belonging to the intended tenant are updated by adding AND tenant_id = <tenant> to the WHERE clause.
- **server/job-queue.ts:238** — completeJob performs UPDATE on `agent_jobs` without constraining by tenant_id. A caller that passes an id could complete a job belonging to any tenant.
  - _Fix:_ Require the caller to supply the job's tenantId (or look it up and validate it) and add AND tenant_id = ${tenantId} to the WHERE clause. Keep the attempts fencing but also enforce tenant scoping so writes cannot affect other tenants' jobs.
- **server/job-queue.ts:298** — failJob updates `agent_jobs` (both terminal and retry branches) without tenant_id in the WHERE clause (writes can affect jobs for any tenant).
  - _Fix:_ After fetching/validating the job's max_attempts, enforce tenant scoping on the UPDATE (e.g. include AND tenant_id = ${tenantId}) or require callers to pass an authoritative tenantId and check it against the persisted row before mutating. Maintain the lease/attempt fences but add tenant constraints.
- **server/job-queue.ts:335** — cancelJob updates `agent_jobs` by id without verifying tenant_id — this allows cancelling jobs across tenants by id.
  - _Fix:_ Require tenantId when cancelling (or first lookup and assert tenant), and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. Ensure tenant-facing routes cannot call this helper to affect other tenants' jobs.
- **server/lib/agent-trace.ts:125** — UPDATE on tenant-scoped table `agent_trace_spans` lacks tenant_id constraint (WHERE clause only has span_id = ${spanId}). This allows a cross-tenant UPDATE if span_id collisions occur or a malicious/buggy call reuses a span id.
  - _Fix:_ Include tenant scoping in the UPDATE. Pass the caller's tenantId (or derive it from the active span context) into persistSpanClose and add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise verify ownership before updating). Example: UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} AND tenant_id = ${tenantId} — and fail/avoid the update when tenant context is missing.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT against project_files and subsequent INSERT do not include tenant scoping; project_files SELECT (line ~101) uses only project_id/file_name and the INSERT (line ~105) does not set tenant_id. If project_files is tenant-scoped this lets code read/insert rows without constraining them to the caller's tenant (cross-tenant leakage or orphan writes).
  - _Fix:_ Constrain reads/exists checks by tenant_id (e.g. WHERE project_id = ${newProjectId} AND file_name = ${f.fileName} AND tenant_id = ${tenantId}) and include tenant_id in the INSERT column list/values. Alternatively, verify the project's tenant (SELECT id, tenant_id FROM projects WHERE id = ${newProjectId}) and use that tenantId when inserting child rows so every write/read is explicitly tenant-scoped.
- **server/lib/ideabrowser-backfill.ts:115** — SELECT against project_notes and subsequent INSERT do not include tenant scoping; the SELECT (line ~116) checks project_id/note without tenant_id and the INSERT (line ~120) does not set tenant_id. This violates the invariant that tenant-scoped tables must be filtered by tenant_id on every read/write.
  - _Fix:_ Add tenant_id checks to the SELECT/NOT EXISTS and include tenant_id in the INSERT (or verify the target project's tenant and use that tenant id). For example: WHERE project_id = ${newProjectId} AND note = ${n.note} AND tenant_id = ${tenantId} and INSERT INTO project_notes (project_id, tenant_id, note, ...) VALUES (${newProjectId}, ${tenantId}, ...). Also prefer a single tenant-scoped upsert or use a transaction that validates project ownership first.
- **server/lib/ideabrowser-dev-sync.ts:139** — INSERT INTO project_notes (via SELECT ... WHERE NOT EXISTS ...) omits tenant scoping in the NOT EXISTS subquery and the INSERT does not include tenant_id. The WHERE NOT EXISTS checks only project_id and author (line ~142–144) and can produce cross-tenant ambiguity if project_notes is tenant-scoped.
  - _Fix:_ Include tenant_id in the NOT EXISTS predicate and in the persisted row. Either add AND tenant_id = ${tenantId} inside the subquery and INSERT tenant_id explicitly, or first verify the project belongs to the tenant and then insert using that tenant id. Use a tenant-scoped transaction/helper to ensure atomic, ownership-checked writes.
- **server/lib/ideabrowser-dev-sync.ts:150** — INSERT INTO project_files (via SELECT ... WHERE NOT EXISTS ...) omits tenant scoping in the NOT EXISTS subquery and the INSERT does not include tenant_id. The existence check and INSERT only reference project_id and file_name (lines ~151–155), lacking tenant_id constraints for tenant-scoped child rows.
  - _Fix:_ Add tenant_id to the NOT EXISTS predicate and to the inserted columns/values (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (..., ${tenantId}, ...)). Alternatively, resolve and validate the project's tenant first and use that tenant id for the child inserts to guarantee per-tenant isolation.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` is executed without constraining by tenant_id (query: `WHERE key_prefix = ${prefix} LIMIT 1`). This reads a tenant-scoped table without a tenant filter and can enable cross-tenant probing/enumeration or accidental leakage of other tenants' rows.
  - _Fix:_ If the caller has a tenant context, include `AND tenant_id = <callerTenant>` in the WHERE clause. If this function must resolve tenant from the key, ensure `key_prefix` is globally unique (enforce a UNIQUE constraint/index) so a prefix maps to a single tenant and harden against enumeration (rate-limit probes, constant-time responses). Also avoid returning any sensitive row fields before the presented-key hash is fully verified.
- **server/lib/mcp-api-keys.ts:70** — Constructing a Postgres text[] literal by concatenating/escaping user-provided scopes into `scopesLit` and inlining it into an SQL template (`... ${scopesLit}::text[] ...`). This is manual string interpolation of request-derived data into SQL and risks SQL injection or quoting/encoding bugs.
  - _Fix:_ Stop building SQL array literals by hand. Use parameterized array binding (pass the JS array as a bound parameter or use Drizzle's insert API to bind arrays) or explicitly validate/whitelist each scope value before persisting. Example fixes: pass `scopes` as a parameter and use `... VALUES (..., ${params.scopes}::text[], ...)` if your driver supports array binding, or use Drizzle's .insert(file).values({ scopes: params.scopes }) so values are bound safely. If manual construction is unavoidable, strictly validate/whitelist scope values and escape/encode them with a proven library rather than manual string concatenation.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions executes a cross-tenant aggregation: `SELECT tenant_id, format, count(*)... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format` has NO caller-scoped WHERE tenant_id filter. This reads tenant-scoped rows for all tenants in one query and therefore violates the invariant that every read against a tenant-scoped table must be constrained to a caller tenant.
  - _Fix:_ Restrict reads to a single tenant context or make tenant explicit. Options: (a) run a per-tenant scan—query a tenants list and call the scan logic with WHERE tenant_id = <tenant> for each tenant; (b) add an explicit tenant_id parameter to the function and include `WHERE tenant_id = ${tenantId}` on the aggregation; or (c) if truly intended as a global admin job, gate the function behind an explicit admin-only path and document/verify that this privileged global read is acceptable. In all cases, avoid querying tenant-scoped tables across all tenants in a single unconstrained statement.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a request-derived value (the column name is derived from the caller-provided `outcome`), which can lead to SQL-injection or other risks if the whitelist/validation is bypassed or changed. Even though the code includes a whitelist check, sql.raw() is being used with a value originating from user input.
  - _Fix:_ Avoid sql.raw() with request-derived values. Instead map the validated outcome to an explicit branch that updates the column with a parameterized query (e.g. switch on outcome and run a parameterized UPDATE that sets action_alpha = action_alpha + ${delta} or action_beta = action_beta + ${delta}, etc.). Or use Drizzle's query builder column references (not sql.raw) so the column identifier is not injected into raw SQL. Keep the tenant_id filter in the WHERE clause (already present) and retain strict validation of outcome before choosing the update path.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE against tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE selects due rows across all tenants). This allows cross-tenant locking/processing and violates the app-level tenant-isolation invariant.
  - _Fix:_ Restrict the CTE to a specific tenant (WHERE tenant_id = $1) or iterate per-tenant and run the pick+update inside a per-tenant transaction. If the runner must be system-scoped, make the cross-tenant intent explicit and enforce authorization/audit, or explicitly pass and use an admin-scoped tenant identifier in the WHERE clauses so tenant scoping is still enforced at the SQL level.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the row id in the WHERE clause and does not constrain by tenant_id. This relies on a prior lookup (existing) instead of enforcing the caller's tenant in the UPDATE, violating the invariant that every write to a tenant-scoped table must include an explicit tenant filter.
  - _Fix:_ Include the tenant filter in the UPDATE WHERE clause (and/or use a composite WHERE that includes both id and tenant_id). Example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively, perform the UPDATE using the same tenant-scoped key used to SELECT the row (e.g. tenantId + profileName) to ensure no cross-tenant update is possible.
- **server/minerva-planner.ts:563** — Tenant filter bug in rollback UPDATE: the SQL uses AND tenant_id = ${args.planId} (planId) instead of the caller's tenantId. This makes the UPDATE filter wrong and can modify rows across tenants (or fail to restrict to the caller's tenant).
  - _Fix:_ Use the caller's tenantId in the WHERE clause. Replace AND tenant_id = ${args.planId} with AND tenant_id = ${args.tenantId}. Add a unit/integration test to exercise the rollback path and verify the UPDATE only affects rows for the provided tenant, and consider defensive logging/guards to prevent future copy/paste errors.
- **server/oauth-subscriptions.ts:915** — Query SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — scans subscriptions across tenants.
  - _Fix:_ Add an explicit tenant_id filter when querying tenant-scoped tables. If the goal is to discover tenant IDs for auto-connect on startup, derive the tenant list from a trusted tenants table or use a privileged, audited routine that explicitly documents and limits cross-tenant operations.
- **server/pace-control.ts:85** — Queries counting rows in heartbeat_logs do not include any tenant_id filter. If heartbeat_logs is tenant-scoped, this allows cross-tenant aggregation and a tenant can exhaust another tenant's window or observe global usage.
  - _Fix:_ Make pace checks tenant-scoped: accept a tenantId parameter (or otherwise determine tenant context) and include WHERE tenant_id = ${tenantId} in queries against heartbeat_logs. If the intent is a global window, document and enforce that explicitly; otherwise scope by tenant.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs without tenant scoping, returning cross-tenant aggregates (SELECT ... FROM heartbeat_logs WHERE created_at > ...).
  - _Fix:_ Scope the snapshot queries to a tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...), or provide separate per-tenant snapshots. If global statistics are intended, make that explicit and ensure APIs exposed to callers cannot leak tenant-specific data.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} reads a persona row without constraining by tenant_id. The function receives tenantId but does not validate that the persona belongs to that tenant — this allows cross-tenant data access.
  - _Fix:_ Constrain the SELECT by tenant_id and validate ownership: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. If personas may be global, document that explicitly; otherwise enforce tenant ownership and fail if the persona is not found for the given tenant.
- **server/persona-export.ts:39** — SELECT from skills uses WHERE persona_id = ${personaId} OR persona_id IS NULL but does NOT include tenant_id scoping. Tenant-specific skills will be missed or tenant data could be mixed with global skills incorrectly.
  - _Fix:_ Include tenant scoping for tenant-specific skills and still allow global skills. Example: SELECT ... FROM skills WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR (persona_id IS NULL). Alternatively explicitly add tenant_id = ${tenantId} in the persona-specific branch of the predicate.
- **server/plan-executor.ts:1024** — SELECT from tenant-scoped table `plans` without tenant filter: `SELECT id, tenant_id, status, plan_json, execution_log FROM plans WHERE status = 'executing' AND updated_at < ...`. This reads rows across all tenants with no tenant_id constraint.
  - _Fix:_ If this is intended system-wide maintenance, restrict execution to an explicit admin context with strong authorization and audit logging. Otherwise add `AND tenant_id = ${tenantId}` (or accept an explicit tenantId parameter) so reads are scoped to the caller's tenant. Avoid blind cross-tenant SELECTs.
- **server/plan-executor.ts:1090** — SELECT from tenant-scoped table `plans` without tenant filter: `SELECT id, tenant_id, status, ceo_decision, ... FROM plans WHERE status = 'failed' AND ceo_decision = 'approved' ...`. This reads/filters rows across tenants with no tenant constraint before later using tenant-aware UPDATEs.
  - _Fix:_ Scope the query by tenant (e.g. `AND tenant_id = ${tenantId}`) or perform the operation only in an explicitly authorized/trusted admin context with clear audit/logging. Ensure any subsequent UPDATEs operate only on rows validated to belong to the intended tenant.
- **server/plan-executor.ts:1149** — SELECT from tenant-scoped table `plans` without tenant filter: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`. This will return approved plans from all tenants.
  - _Fix:_ Filter by tenant_id (e.g. `WHERE status = 'approved' AND tenant_id = ${tenantId}`) or otherwise require an explicit, auditable admin context for cross-tenant boot/kicker operations. Never silently default to cross-tenant reads.
- **server/process-governor.ts:898** — UPDATE to tenant-scoped table `agent_desks` is executed without constraining by tenant_id (WHERE id = ${desk.id} only). This allows mutating rows belonging to other tenants if id values collide or are guessed.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or otherwise verify the desk belongs to the caller's tenant before updating. Ensure all mutating statements against tenant-scoped tables include tenant_id = <caller tenant>.
- **server/process-governor.ts:1235** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks a tenant_id constraint. This mutates a tenant-scoped table without ensuring the row belongs to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) so only the specific tenant's governance_rule row is modified.
- **server/project-brain.ts:135** — SELECT name,status,description FROM `projects` is executed with WHERE id = ${projectId} but no tenant_id constraint. The function accepts only projectId and may read another tenant's project.
  - _Fix:_ Require and pass a tenantId into updateProjectBrain (or otherwise resolve/validate ownership) and constrain the query: WHERE id = ${projectId} AND tenant_id = ${tenantId}. Always verify the caller's tenant owns the referenced project before reading or proceeding.
- **server/recurring-messages.ts:307** — UPDATE to tenant-scoped table 'agent_knowledge' omits tenant_id in WHERE clause (UPDATE ... WHERE id = ${r.id}). This violates the invariant that every read/write against a tenant-scoped table must be constrained to the caller's tenant.
  - _Fix:_ Include the tenant predicate in the UPDATE so the row update is scoped to that tenant, e.g. add AND tenant_id = ${r.tenant_id}. Prefer using the Drizzle builder (where(eq(agentKnowledge.tenantId, r.tenant_id))) or parameterized SQL to avoid TOCTOU issues. Example: UPDATE agent_knowledge SET ... WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}.
- **server/reference-learner.ts:296** — UPDATE to tenant-scoped table 'memory_entries' omits tenant_id in WHERE clause (UPDATE ... WHERE id = ${storedId}). Even though the row was previously selected with tenant_id, the subsequent UPDATE must also enforce tenant scoping to prevent cross-tenant modification.
  - _Fix:_ Constrain the UPDATE to the tenant as well. Add AND tenant_id = ${input.tenantId} (or the persona-scoped tenant value) to the WHERE clause, or use Drizzle's where(eq(memoryEntries.tenantId, input.tenantId)). Example: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId}.
- **server/research-engine.ts:457** — INSERT into tenant-scoped table project_notes omits tenant_id (autoDepositFindings — summary branch). Per project_notes tenant-scoping, tenant_id must be provided to avoid cross-tenant writes or DB errors.
  - _Fix:_ Include tenant_id in the INSERT columns and values, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ${summaryNote}, ${'Research Engine'}, NOW()). Also keep the existing assertProjectInTenant check (already present) as defense-in-depth.
- **server/research-engine.ts:466** — INSERT into tenant-scoped table project_notes omits tenant_id (autoDepositFindings — per-finding loop). Each persisted project note must include tenant_id to enforce tenant isolation and prevent cross-tenant writes.
  - _Fix:_ Include tenant_id in the INSERT columns and values, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ${noteContent}, ${'Research Engine'}, NOW()). Ensure project ownership is validated (assertProjectInTenant) before inserting.
- **server/routes.ts:482** — DELETE from tenant-scoped table `order_lookup_codes` with no tenant_id constraint (DELETE FROM order_lookup_codes WHERE expires_at < NOW()). This operation deletes rows for all tenants indiscriminately.
  - _Fix:_ Scope the deletion to a specific tenant or iterate per-tenant (e.g. DELETE ... WHERE tenant_id = ${tid} AND expires_at < NOW()), or run the cleanup as an explicit platform/admin operation that documents and auditable-logs cross-tenant maintenance. Avoid blanket deletes without tenant scoping.
- **server/routes.ts:625** — SELECT from tenant-scoped table `projects` without tenant filter (SELECT p.id, p.name, p.tenant_id, ... FROM projects p JOIN tenants t ON t.id = p.tenant_id WHERE p.drive_folder_id IS NULL ...). This reads rows across all tenants with no tenant_id constraint.
  - _Fix:_ If intent is per-tenant backfill, iterate tenants and run a tenant-scoped query (WHERE p.tenant_id = ${tid}). If this must run as a cross-tenant admin job, ensure the code is clearly gated to platform-admin context and document why cross-tenant access is required; consider processing per-tenant to reduce blast radius.
- **server/routes.ts:504** — SELECT subqueries reference tenant-scoped tables (`conversations`, `projects`) without tenant constraints inside a tenants cleanup query (id NOT IN (SELECT DISTINCT tenant_id FROM conversations ...)). This performs unscoped reads against tenant-scoped tables.
  - _Fix:_ Make the existence checks explicit and tenant-scoped (e.g. check counts by tenant id) or otherwise iterate tenants and validate per-tenant before deletion. Avoid subqueries that scan tenant tables without an explicit tenant filter.
- **server/routes.ts:1925** — SELECT from tenant-scoped table `presenter_sessions` by token without any tenant_id constraint (SELECT ... FROM presenter_sessions WHERE token = ${token}). Returns session/tenant information without ensuring the caller is the owning tenant.
  - _Fix:_ Constrain the query by tenant (WHERE token = ${token} AND tenant_id = ${expectedTid}) when you have an authenticated tenant, or require the token to be a tenant-bound capability (signed with tenant) so the DB lookup can be safely scoped. If the token is intended as a global capability, store and validate a tenant binding in the signature and still avoid returning tenant data without explicit tenant scoping/validation.
- **server/routes.ts:1953** — SELECT presenter session by token (db.execute(sql`SELECT id, tenant_id FROM presenter_sessions WHERE token = ${token}`)) without tenant filter. The session is then used to drive subsequent tenant-scoped reads, but the initial lookup itself is unscoped.
  - _Fix:_ Same as above: include tenant scoping when possible, or ensure the token is a signed, tenant-bound capability. Avoid direct unscoped SELECTs on tenant-scoped tables by capability alone without validating tenant ownership explicitly before returning rows.
- **server/routes.ts:1996** — SELECT id FROM presenter_sessions WHERE token = ${token} (presenter TTS) performed without tenant constraint. This looks up a tenant-scoped row based only on token.
  - _Fix:_ Either require and validate the tenant context (e.g. caller session) or make the token strongly tenant-bound (signed HMAC including tenant id) and verify that binding before returning/using the DB row. Prefer queries that include eq(presenter_sessions.tenant_id, tenantId) whenever a tenant context is available.
- **server/routes.ts:2060** — SELECT presenter session by token (SELECT id, tenant_id, presentation_id, slides FROM presenter_sessions WHERE token = ${token}) with no tenant constraint. This unscoped read drives later writes/updates scoped by the returned tenant_id, enabling a token-only access pattern to a tenant-scoped table.
  - _Fix:_ Require tenant validation prior to returning the row or use a tenant-bound signed token. If the endpoint is an internal admin path, enforce platform-admin gating and log/authorize the cross-tenant access carefully; otherwise, add WHERE tenant_id = ${tenantId}.
- **server/routes.ts:4652** — INSERT into project_notes does not include tenant_id. The code inserts a note tied to a project_id without specifying tenant_id, relying on application logic elsewhere to scope the project. Because there is no row-level security or FK cascade enforcing tenant ownership, this can create rows that are not explicitly tenant-scoped and allow cross-tenant data leakage or ambiguity.
  - _Fix:_ Include tenant_id in the INSERT (and pass the caller's tenantId explicitly). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}). Alternatively, use a storage-layer helper (e.g. storage.createProjectNote) that requires and enforces tenantId, and ensure the project ownership check is performed and bound to the same tenantId before inserting.
- **server/routes/conversations.ts:119** — UPDATE on tenant-scoped table `conversations` omits tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This allows modifying a conversation row without ensuring it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or use storage API that enforces tenant ownership). Alternatively, include tenant scoping in the same statement or perform the update via a transaction that locks and verifies the row belongs to tenantId before mutating.
- **server/routes/conversations.ts:123** — INSERT into `project_conversations` does not include tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). If `project_conversations` is a tenant-scoped table this creates rows with no tenant context / allows cross-tenant linkage.
  - _Fix:_ Explicitly set tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). If the table is intended to derive tenant from project_id, still include tenant_id for defense-in-depth and to meet the invariant; also validate project_id and conversation_id ownership before inserting.
- **server/routes/conversations.ts:125** — UPDATE on tenant-scoped table `projects` omits tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This will update a project row without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}, or perform the update via a tenant-aware helper that enforces ownership checks before mutating.
- **server/routes/conversations.ts:256** — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). If `project_notes` is tenant-scoped this creates notes without tenant context and may allow cross-tenant writes.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})), and/or verify the project belongs to the caller's tenant prior to inserting. Ensure all tenant-scoped tables are always persisted with tenant_id set explicitly.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` is executed without a tenant_id predicate (`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`). This violates the app-level tenant-isolation invariant.
  - _Fix:_ Include the tenant predicate in the UPDATE so the statement is always scoped to the caller's tenant. Example: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(inboxMessages.tenantId, tenantId))).
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` aggregates and groups by tenant_id but has no WHERE tenant_id = ... predicate, returning (or enabling enumeration of) cross-tenant data to anyone holding METRICS_TOKEN.
  - _Fix:_ Either scope this query to a single tenant (add `WHERE tenant_id = ${tenantId}` / use Drizzle with eq(table.tenantId, tenantId)) for per-tenant metrics, or if global, ensure only a fully-privileged platform/operator identity may call it and do not emit raw tenant identifiers (mask or anonymize). At minimum require platform-admin session-level auth (not just a bearer token) and document the trust boundary; or produce anonymized aggregates instead of per-tenant rows.
- **server/routes/metrics.ts:79** — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) lacks a tenant predicate and returns cross-tenant usage counts.
  - _Fix:_ Scope to tenant (`WHERE tenant_id = ${tenantId}`) if endpoint is per-tenant, or restrict the endpoint to platform/operator-level callers and avoid returning tenant-identifying data (mask or aggregate across tenants). Ensure the access control model is explicit and enforced before running unscoped queries.
- **server/routes/metrics.ts:95** — SELECT summarizing tokens (FROM tenant-scoped `agent_cost_ledger`) has no tenant filter, allowing cross-tenant aggregation to be read by the caller.
  - _Fix:_ Either add a tenant WHERE clause to restrict to the caller's tenant, or require platform/operator admin access and avoid exposing tenant identifiers or raw per-tenant values. Prefer explicit tenant-scoping in SQL or a documented, strongly-authenticated operator-only channel for global metrics.
- **server/routes/metrics.ts:113** — SELECT from tenant-scoped table `repair_incidents` (grouping by classification/action) lacks any tenant predicate, exposing cross-tenant incident counts.
  - _Fix:_ If this endpoint must return tenant-scoped metrics, add `WHERE tenant_id = ${tenantId}`. If it must be global, ensure only tightly-held operator credentials can call it and consider removing tenant identifiers or aggregating in a way that cannot leak tenant-specific data.
- **server/routes/oauth-subscriptions.ts:325** — Admin/cron handler executes an unscoped SELECT DISTINCT tenant_id FROM oauth_subscriptions to enumerate tenant IDs (`SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE`) without applying any tenant predicate or platform-admin session guard. This enumerates tenants and can be abused if the cron secret is leaked.
  - _Fix:_ Do not enumerate tenant IDs from a publicly-exposed endpoint without stronger auth. Move this logic to an internal, platform-admin-only path or a background job running with internal credentials. If it must be triggered via HTTP, require platform-admin session auth (not only a shared CRON_SECRET) or ensure the cron secret is extremely restricted and the endpoint does not return tenant identifiers (perform refresh actions server-side without exposing tenant ids).
- **server/routes/projects.ts:165** — SELECT subqueries counting project_files, project_notes, and project_conversations do not constrain those tenant-scoped tables by tenant_id (they only filter by project_id). This can leak or mis-attribute rows if rows in those tables are not tenant-isolated.
  - _Fix:_ Constrain the subqueries to the caller tenant as well. Example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) — or use driz zle eq(table.tenantId, tenantId). Ensure every read against tenant-scoped tables includes tenant_id = tenantId.
- **server/routes/projects.ts:616** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} has no tenant_id constraint. Without tenant filtering this UPDATE could affect a row outside the caller's tenant (even if ownership was checked earlier).
  - _Fix:_ Add tenant constraint to the WHERE clause: WHERE id = ${projectId} AND tenant_id = ${tenantId}. Prefer including tenant_id on all UPDATE/DELETE statements against tenant-scoped tables (or validate ownership immediately prior to mutation and include tenant_id in query).
- **server/routes/projects.ts:684** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (multipart upload flow) omits tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (or include tenant_id in the query) to enforce tenant isolation.
- **server/routes/projects.ts:703** — SELECT * FROM project_files WHERE project_id = ${projectId} does not include tenant_id. Reading tenant-scoped tables must include tenant_id in the predicate to enforce isolation.
  - _Fix:_ Add tenant_id constraint: SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC. Alternatively, join to projects with p.tenant_id = ${tenantId} but prefer an explicit tenant_id WHERE clause.
- **server/routes/projects.ts:726** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 omits tenant_id. This read of a tenant-scoped table lacks an explicit tenant_id constraint.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or join to projects and check p.tenant_id = ${tenantId}). Also continue to verify the project belongs to the tenant before querying.
- **server/routes/projects.ts:755** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not constrain by tenant_id. Deleting from a tenant-scoped table without tenant filtering risks affecting other tenants' rows.
  - _Fix:_ Include tenant_id in the DELETE predicate: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}. Also ensure project ownership was validated before delete.
- **server/routes/watchlist.ts:39** — addWatchlistItem is called with an object literal that spreads req.body after tenantId ({ tenantId, ...req.body }). This allows a malicious caller to include a tenantId in the request body and override the enforced tenant, causing inserts into a tenant-scoped table to be attributed to an attacker-controlled tenant.
  - _Fix:_ Ensure tenant_id cannot be overridden by request input. Pass tenantId last (e.g. addWatchlistItem({ ...req.body, tenantId })) or better: call addWatchlistItem with tenantId as an explicit separate parameter (e.g. addWatchlistItem(tenantId, req.body)) and have the implementation ignore any tenantId in req.body. Validate/normalize inside addWatchlistItem to always set tenant_id from the authenticated session.
- **server/seed.ts:42** — SELECT on tenant-scoped table 'research_sessions' without any tenant filter (SELECT COUNT(*) FROM research_sessions). This reads across all tenants rather than being constrained to a specific tenant.
  - _Fix:_ Constrain the query to a single tenant (or explicitly the admin tenant) — e.g. add WHERE tenant_id = ${tenantId} or use Drizzle .where(eq(research_sessions.tenantId, tenantId)). If you intend to check emptiness per-tenant, run the count per-tenant. Avoid global scans on tenant-scoped tables.
- **server/seed.ts:2172** — UPDATE on tenant-scoped table 'projects' without a tenant constraint (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ...). This can modify rows belonging to any tenant if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE — e.g. add AND tenant_id = ${tenantId} or use Drizzle .where(eq(projects.tenantId, tenantId)). If this is intended to affect the platform owner only, use the explicit admin tenant constant (ADMIN_TENANT_ID) in the WHERE clause.
- **server/seed.ts:3667** — UPDATE on tenant-scoped table `memory_entries` is executed without any tenant_id constraint (global update).
  - _Fix:_ Constrain the UPDATE to a specific tenant (e.g. WHERE tenant_id = <tenantId> AND ...), or iterate tenants and run the UPDATE per-tenant. If this is intended to run only for the platform owner, explicitly include tenant_id = ADMIN_TENANT_ID. Avoid global updates on tenant-scoped tables.
- **server/seed.ts:3693** — UPDATE on tenant-scoped table `conversations` is executed without any tenant_id constraint (global update).
  - _Fix:_ Add a WHERE tenant_id = <tenantId> clause (or run the update per-tenant) so only rows belonging to the caller/target tenant are modified. If this migration must be applied across all tenants, enumerate tenants and apply the change per-tenant to preserve tenant isolation guarantees.
- **server/seed.ts:3639** — DELETE on tenant-scoped table `heartbeat_tasks` is executed without any tenant_id constraint (global delete).
  - _Fix:_ Restrict the DELETE to a tenant (WHERE tenant_id = <tenantId> ...) or perform the cleanup per-tenant. If the intent is to operate only on platform/system tasks, include an explicit tenant_id = ADMIN_TENANT_ID filter.
- **server/self-reflection.ts:381** — UPDATE on tenant-scoped table `memory_entries` performs WHERE id = <row id> without also checking tenant_id. Even though the prior SELECT checked tenant_id, the UPDATE by id can update another tenant's row (race or ID reuse).
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause. For example: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tenantId}. Use the same tenantId parameter that was used in the SELECT/lookup to ensure the caller only updates their own tenant's rows.
- **server/skillify.ts:221** — SELECT from 'personas' uses persona id from a conversation but does not check persona.tenant_id. This may allow reading a persona that belongs to another tenant.
  - _Fix:_ Verify the persona belongs to the same tenant before using it (e.g. add AND personas.tenant_id = ${tenantId}), or fetch persona via a join on conversations.persona_id with a tenant restriction so ownership is always enforced.
- **server/skills-marketplace.ts:209** — SELECT id FROM skills WHERE name = ${template.name} is run with no tenant constraint. If 'skills' is tenant-scoped, this leaks cross-tenant existence/ids.
  - _Fix:_ If skills are tenant-scoped, include tenant_id = ${tenantId} in the WHERE clause and accept an explicit tenantId parameter for this operation. If the intent is to check a global catalog, document it and ensure the table is indeed global/shared.
- **server/storage.ts:300** — createMessage reads the referenced conversation row by id without first verifying the caller's tenant owns that conversation. This lets a caller supply an arbitrary conversation_id and cause a message to be created for another tenant's conversation (cross-tenant write).
  - _Fix:_ Require the caller's tenant context (or an explicit data.tenantId) and verify it matches the conversation's tenant_id before proceeding. E.g. SELECT ... FROM conversations WHERE id=${conversationId} AND tenant_id=${callerTenantId} and fail if not found. Never derive tenant for the new message solely from an un-validated foreign id.
- **server/storage.ts:585** — getAllMemoriesForBackup selects all rows from tenant-scoped `memory_entries` with no tenant_id filter, returning platform-wide tenant data.
  - _Fix:_ Require an explicit tenant scope parameter or an explicit ALL_TENANTS sentinel for admin calls. If the function must run platform-wide, restrict it to an authenticated admin path and document/guard usage; otherwise add WHERE eq(memory_entries.tenant_id, tenantId).
- **server/surprise-scorer.ts:96** — UPDATE on tenant-scoped table 'felix_proposals' (setting args_embedding) lacks a tenant_id filter — the query is: `UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL`.
  - _Fix:_ Restrict the UPDATE to the intended tenant. Pass tenantId into stampArgsEmbedding (or otherwise derive it) and include `AND tenant_id = ${tenantId}` (or use Drizzle: .where(eq(felix_proposals.tenantId, tenantId), eq(felix_proposals.id, proposalId))). Failing to do so can modify another tenant's row with the same id.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table 'custom_tools' modifies implementation using only the PK id in WHERE: `db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id))` — missing tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE. Use a compound WHERE that includes tenant id (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or ensure `tool` carries tenantId and check it before updating to avoid cross-tenant updates.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table 'custom_tools' uses only id in the WHERE clause: `db.delete(customTools).where(eq(customTools.id, tool.id))` — no tenant_id constraint.
  - _Fix:_ Fail-closed: restrict the DELETE to the tenant by including tenant_id in the WHERE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))). Ensure revertToolRow receives/validates the tenantId of the row being removed before deleting.
- **server/tool-learning.ts:365** — UPDATE that increments usageCount on tenant-scoped 'custom_tools' uses only id in WHERE: `db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(eq(customTools.id, tool.id))` — missing tenant filter.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or fetch and assert the row's tenantId before updating to avoid impacting another tenant's row.
- **server/tools.ts:2806** — INSERT into project_conversations omits tenant_id. Tenant-scoped rows must include tenant_id on INSERT to ensure per-tenant isolation.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). If relying on prior ownership checks, still write tenant_id explicitly to the row so the DB state is always tenant-scoped.
- **server/tools.ts:2931** — INSERT into project_files does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. INSERT INTO project_files (..., uploaded_by, tenant_id) VALUES (..., ${params.uploadedBy}, ${tenantId})). Ensure tenantId is validated/derived from the authenticated context, not from caller input.
- **server/tools.ts:2946** — INSERT into project_notes omits tenant_id. Writing tenant-scoped data without tenant_id risks cross-tenant contamination.
  - _Fix:_ Include tenant_id when inserting notes (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author || 'agent'}, ${tenantId})). Do not rely solely on prior owner-checks; always persist tenant_id explicitly.
- **server/tools.ts:2970** — INSERT into project_conversations (second occurrence) omits tenant_id. Rows linking projects/conversations must be tenant-scoped in-table.
  - _Fix:_ Persist tenant_id with the link (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). If the table design intentionally omits tenant_id, consider adding it so application-level filters are enforceable and unambiguous.
- **server/tools.ts:4546** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. Even though the code verified project ownership earlier, the INSERT omits the required tenant_id column/value, violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT and bind it from the verified caller context (e.g. VALUES (${params._tenantId}, ...)). Example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ${note}, 'system', NOW()). Ensure the project ownership check (assertProjectInTenant) runs before the INSERT.
- **server/tools.ts:4550** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checked project ownership, but the INSERT omits tenant_id so the row could be written without explicit tenant attribution.
  - _Fix:_ Add tenant_id to the INSERT columns and pass the verified tenant (e.g. VALUES (${params._tenantId}, ${projectId}, ...)). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ${fileName}, ${fileType}, ${editUrl}, 'Felix', NOW()). Keep the assertProjectInTenant guard before writing.
- **server/tools.ts:6465** — INSERT into tenant-scoped table `project_files` (in generate_audio) does not include tenant_id. Although the code calls assertProjectInTenant, the INSERT omits tenant_id, violating the rule that every INSERT must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT and supply the verified params._tenantId (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${filename + '.' + ext}, ${outPath}, ${driveUrl || null}, 'audio', ${audioBuffer.length}, 'system')). Ensure assertProjectInTenant was successful before inserting.
- **server/tools/domains/cmmc-prospecting/handlers.ts:156** — INSERT into `project_files` does not include tenant_id. The INSERT lists (project_id, file_name, file_path, ...) but omits tenant_id despite the app using tenant-scoped access controls elsewhere — this can misattribute or bypass tenant scoping.
  - _Fix:_ Explicitly set tenant_id in the INSERT values (and column list) using the caller's tenant (e.g. include `tenant_id` in the column list and `${ctx.tenantId}` in the VALUES/SELECT). If the table is tenant-scoped, never rely solely on a JOIN/EXISTS check — write the tenant_id column explicitly on INSERT.
- **server/tools/domains/files/handlers.ts:941** — UPDATE against tenant-scoped table `projects` uses WHERE id = ${projId} with no tenant_id constraint. This can modify another tenant's project if ids are not otherwise constrained by tenant in the WHERE clause.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}, or use a parameterized Drizzle update with eq(projects.id, projId) AND eq(projects.tenantId, tenantId). Always verify ownership before mutating tenant-scoped rows.
- **server/tools/domains/knowledge/handlers.ts:91** — UPDATE on tenant-scoped table `knowledge_triples` uses WHERE id = ${row.id} without constraining tenant_id. Although the row was selected earlier under tenant scope, UPDATE should still require tenant_id in WHERE to prevent cross-tenant modification if ids are reused or otherwise accessible.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer using the tenantId bound value (from the earlier SELECT context) in all UPDATE/DELETE statements against tenant-scoped tables.
- **server/tools/domains/strategic-memory/handlers.ts:99** — UPDATE against tenant-scoped table memory_entries does not constrain by tenant_id (WHERE id = ${id} only). This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Add tenant scoping to the UPDATE predicate. Example: WHERE id = ${id} AND tenant_id = ${tid}. Always include tenant_id in WHERE for updates to tenant-scoped tables (or validate ownership server-side before writing).
- **server/tools/domains/strategic-memory/handlers.ts:159** — UPDATE against tenant-scoped table memory_entries uses WHERE id = ANY(${ids}) without also constraining tenant_id. Writes derived from selected ids must still enforce tenant scoping in the UPDATE to prevent accidental cross-tenant modification.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE memory_entries SET ... WHERE id = ANY(${ids as any}) AND tenant_id = ${tid}. Alternatively re-run a scoped UPDATE using a SELECT ... WHERE tenant_id = ${tid} AND id = ANY(...) to ensure ownership before modifying rows.
- **server/tools/domains/strategic-memory/handlers.ts:258** — UPDATE against tenant-scoped table memory_entries does not include tenant_id in the WHERE clause (WHERE id = ${id} only). This allows a row id to be updated without verifying it belongs to the caller's tenant.
  - _Fix:_ Restrict the UPDATE to the tenant: WHERE id = ${id} AND tenant_id = ${tid}. Prefer always including tenant_id in WHERE for all UPDATE/DELETE statements on tenant-scoped tables (or use a defensive ownership check before modifying).
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This reads pairing rows across all tenants, violating the invariant that tenant-scoped tables must be filtered by the caller's tenant.
  - _Fix:_ Restrict the query to a single tenant rather than scanning agent_knowledge across all tenants. E.g. store/require tenant_id on pairing rows and change the query to: SELECT id, content::jsonb AS info FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ${`${channel}:${phone}`} AND tenant_id = ${claimedTenantId} ORDER BY id DESC. If the intention is to detect contested cross-tenant pairings, implement an operator-only/global provisioning path (or an explicit, audited cross-tenant check) rather than allowing an unfiltered application-level SELECT against a tenant-scoped table.
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` uses only conversation id (WHERE id = ${info.conversationId}) and does not constrain by tenant_id. This performs a tenant-scoped read without enforcing the caller/claimant tenant filter.
  - _Fix:_ Verify ownership with a tenant-scoped WHERE clause or EXISTS check so the read is constrained to the claimed tenant. For example: SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 (or use SELECT 1 ... LIMIT 1). Avoid selecting conversation rows across tenants; ensure the check fails/returns nothing unless the conversation belongs to the expected tenant.
- **server/video-job-runner.ts:273** — SELECT from tenant-scoped table videoJobs (listStaleVideoJobs) runs without filtering by tenant_id. This returns rows across all tenants (stale-job recovery scans the global table), violating the codebase's tenant-isolation-by-application-filters invariant.
  - _Fix:_ If cross-tenant scanning is required for maintenance, ensure the function runs in a trusted context and explicitly documents that elevated access is intended; otherwise restrict the query by tenant_id (e.g. accept a tenantId parameter and add eq(videoJobs.tenantId, tenantId)). Consider implementing a safe admin-only path or per-tenant sweep logic so ordinary code paths never read other tenants' rows.

### MEDIUM (37)

- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} ... reads the tenant-scoped `messages` table without an explicit tenant_id constraint. Although filtered by conversation_id, the query lacks the required tenant_id guard.
  - _Fix:_ Add tenant_id = ${c.tenant_id} to the WHERE clause (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id} AND role = 'assistant') or otherwise ensure the caller's tenant is enforced before reading messages.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments is executed without constraining to a caller tenant. This reads across all tenant-scoped rows (cross-tenant read).
  - _Fix:_ If this enumeration must run cross-tenant, make it explicit and restrict execution to an authorized admin/background process only (add an admin/auth check before running). Otherwise restrict the query to a single tenant (e.g. WHERE tenant_id = ${tenantId}). If the goal is to find tenants with candidate rows for per-tenant processing, derive the tenant list from a trusted source or run tenant-scoped queries per known tenant id rather than performing an unconstrained SELECT on the tenant-scoped table.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` uses only project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) and lacks an explicit tenant_id constraint.
  - _Fix:_ Add an explicit tenant_id predicate (AND tenant_id = ${tenantId}) when reading project_notes, or otherwise ensure the query runs inside an audited transaction that enforces tenant context (e.g. withTenantTx / RLS). Do not rely on implicit FK ownership alone.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` uses only project_id (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}) and lacks an explicit tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or run inside an explicit tenant-scoped transaction) so reads cannot cross tenant boundaries even if project_id is known.
- **server/data-protection.ts:404** — SELECT COUNT(*) FROM `messages` WHERE conversation_id = ${conv.id} executes without scoping by tenant_id, leaking a cross-tenant existence/count oracle.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or obtain the message count via a tenant-scoped helper (or call backupConversationToDrive which already enforces tenant) to avoid cross-tenant leakage.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed with WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but lacks an explicit tenant_id constraint. doc_chunks is tenant-scoped (tenant_id is present on inserts).
  - _Fix:_ Add tenant scoping to the DELETE so it cannot affect other tenants' rows. For example: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Alternatively ensure the collection ownership check is enforced in the same SQL statement (e.g. join on doc_collections with tenant_id) to make the tenant constraint explicit and atomic.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates by id only and does not include tenant_id. Although the id was selected earlier with a tenant filter, the UPDATE itself lacks an explicit tenant constraint.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause to enforce tenant isolation (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). This prevents accidental cross-tenant updates if ids are ever mis-derived or if code paths change.
- **server/dormant-deprecation.ts:170** — Query counts messages globally: SELECT COUNT(*) FROM messages ... lacks any tenant_id filter. messages appears to be a tenant-scoped table, but this read is unscoped.
  - _Fix:_ If this metric is intended to be per-tenant, add tenant_id to the WHERE clause. If the metric is intentionally global, document why a global aggregate over tenant-scoped data is safe and acceptable. Prefer explicit scoping (AND tenant_id = ${tenantId}) or an explicit global-aggregation path/table to avoid accidental cross-tenant reads.
- **server/dormant-deprecation.ts:180** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance ... is executed without tenant_id scoping. tool_performance is a tenant-scoped table but is being read globally.
  - _Fix:_ If the intent is per-tenant measurement, add AND tenant_id = ${tenantId}. If it is intentionally global, make that explicit and ensure this global read is acceptable; otherwise restrict scope to tenant to preserve tenant isolation.
- **server/dormant-deprecation.ts:204** — getEverInvokedTools issues SELECT DISTINCT tool_name FROM tool_performance with no tenant filter, reading a tenant-scoped table globally.
  - _Fix:_ Restrict to a tenant (e.g. WHERE tenant_id = ${tenantId}) when you need tenant-local results. If a global scan is required by design, isolate it in a documented admin-only path and consider storing global aggregates in a dedicated global table to avoid ad-hoc cross-tenant reads.
- **server/dormant-deprecation.ts:214** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... but omits tenant_id, performing an unscoped read of a tenant-scoped table.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) when computing per-tenant active tools. If a global view is required, separate that logic explicitly and document the security reasoning; otherwise enforce tenant_id in the WHERE clause.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks is executed without a tenant_id filter (cross-tenant SELECT). Reading tenant-scoped rows without tenant scoping may expose other tenants' data if this function is callable from unprivileged code.
  - _Fix:_ If this is intended to be an explicit cross-tenant scheduler-only operation, make that explicit and ensure it's only callable from platform/admin code. Otherwise add a tenant filter (WHERE tenant_id = ${tenantId}) or provide a dedicated admin-authorized pathway. Prefer using application-level checks and assertTenantContext before querying tenant-scoped tables.
- **server/heartbeat.ts:257** — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id constraint. This reads rows from a tenant-scoped event_log table across tenants.
  - _Fix:_ If the worker must process events across tenants, ensure the code explicitly documents and restricts this cross-tenant operation to a trusted platform worker. Otherwise, add tenant filtering (WHERE tenant_id = ${tenantId}) or enumerate tenant ids first and process per-tenant. Always assert tenant context for tenant-scoped reads when used in request-handlers.
- **server/knowledge-diversity-monitor.ts:266** — Global SELECT groups rows from tenant-scoped table `agent_knowledge` (SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge ...) without scoping to a specific caller/tenant. This reads across tenants and then calls per-tenant snapshot logic — the read itself is unscoped.
  - _Fix:_ If this is intended as a platform-level diagnostic, make that explicit and restrict invocation to an admin/background context. Otherwise, rework to iterate tenants explicitly (e.g. SELECT DISTINCT tenant_id ... then run per-tenant queries with WHERE tenant_id = <tenant>) so every read against tenant-scoped data is performed with an explicit tenant_id constraint.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` (query at lines 56-64) is executed without a tenant_id filter — a tenant-scoped table is being scanned across all tenants.
  - _Fix:_ Add a tenant_id predicate to the query (WHERE tenant_id = $1) or run the sweep per-tenant with explicit authorization. If the goal is a platform-wide audit, require and document an explicit admin-mode and ensure the SQL makes that intent explicit (e.g. accept an admin flag and still bind it into the query).
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` (query at lines 84-92) is executed without a tenant_id filter — scanning tenant-scoped rows across all tenants.
  - _Fix:_ Add a tenant_id predicate to the query (WHERE tenant_id = $1) or perform per-tenant scans with explicit admin authorization. If platform-wide visibility is required, make that explicit and ensure appropriate audit/authorization controls and explicit SQL binding of tenant(s).
- **server/lib/system-probes.ts:163** — SELECT count(*) FROM tenant-scoped `messages` table is performed without a tenant_id constraint (line 163), returning cross-tenant aggregate data in a non-tenant-scoped probe.
  - _Fix:_ Limit the query to a tenant (WHERE tenant_id = $1) or, if an aggregate across tenants is required for system diagnostics, require explicit admin authorization and document it. Alternatively, compute per-tenant counts by iterating tenants and binding tenant_id into the query rather than issuing an unscoped count(*) on the tenant table.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw() with a value derived from caller-provided ledgerKey when issuing pg_advisory_xact_lock: sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`. Using raw SQL with request-derived data risks injection and bypassing parameterization/isolation checks.
  - _Fix:_ Avoid sql.raw with request-derived values. Compute a numeric lock key server-side and pass it as a parameterized value (or otherwise ensure it's fully validated). Example: const numericLock = BigInt(lockKey); await tx.execute(sql`SELECT pg_advisory_xact_lock(${numericLock}::bigint)`); or build the lock key from a trusted internal mapping rather than interpolating raw SQL.
- **server/persona-routing-outcomes.ts:44** — Query joins action_outcomes (filtered by ao.tenant_id = ${tenantId}) to personas p ON p.id = ao.persona_id but does not constrain the personas read by tenant. This reads from personas without an explicit tenant constraint.
  - _Fix:_ Constrain the joined personas to the same tenant to be explicit and defensive: JOIN personas p ON p.id = ao.persona_id AND p.tenant_id = ${tenantId} (or add WHERE p.tenant_id = ${tenantId}). This avoids accidental cross-tenant reads if persona ids are reused or data model changes.
- **server/platform-admin-conversations.ts:18** — Cross-tenant SELECT on tenant-scoped table `conversations` without tenant_id constraint: `.from(conversations).where(and(eq(conversations.id, conversationId), isNull(conversations.deletedAt)))`. The function relies on `isPlatformAdmin(req)` for authorization rather than a tenant filter.
  - _Fix:_ If cross-tenant access is intended, ensure the platform-admin boundary enforces strict authorization, audit logging, and rate-limiting. Otherwise scope the query by tenant (add `AND conversations.tenant_id = ${tenantId}`) or provide a dedicated admin-only API that logs the tenant(s) accessed and checks a platform admin context before running cross-tenant queries.
- **server/process-governor.ts:891** — SELECT joins `personas` using ON p.id = ad.persona_id but does not constrain the joined `personas` row by tenant (no p.tenant_id filter). If `personas` is tenant-scoped this can leak cross-tenant data.
  - _Fix:_ Constrain the JOIN by tenant (e.g. LEFT JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId} or p.tenant_id = ad.tenant_id) so persona rows returned are always scoped to the same tenant.
- **server/routes.ts:973** — SELECT on tenant-scoped table `file_storage` without tenant filter (selecting all owners for a basename: .from(fileStorage).where(eq(fileStorage.filename, baseName))). This returns owner rows across tenants and then relies on application logic to compare tenant ids.
  - _Fix:_ If the intent is to detect cross-tenant basename collisions, make this explicit and audit/comment the behavior; better, limit the initial query to the authenticated tenant and separately perform a controlled collision check or require the signed URL to include tenant binding. At minimum, document the reason and ensure callers are platform-admin or the signed-url flow that justifies cross-tenant reads.
- **server/routes.ts:1394** — Multiple SELECTs aggregating rows from tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) are executed without tenant_id constraints to build global stats (db.select...from(conversations) etc.). These are broad, unscoped reads across all tenants.
  - _Fix:_ If global totals are required only for platform-admins, ensure the endpoint is strictly platform-admin-gated (it already calls requirePlatformAdmin) and document that global aggregation is intentional. Where possible prefer auditing/aggregation materialized views or per-tenant aggregation to reduce exposure and make cross-tenant operations explicit and auditable.
- **server/routes.ts:1424** — Multiple db.execute COUNT(*) queries against tenant-scoped tables in /api/public/architecture (e.g. SELECT count(*) FROM conversations, messages, projects, skills, memory_entries, etc.) are executed globally without tenant filters.
  - _Fix:_ Same mitigation as above: ensure strict platform-admin gating is enforced, and consider running cross-tenant aggregation through a dedicated admin-only code path that clearly documents and logs its usage. If per-tenant scoping is preferred, add WHERE tenant_id = ${tid} or iterate tenants.
- **server/self-improvement.ts:561** — SELECT from tenant-scoped table `experiments` is executed without any tenant filter in the fallback path (when tenantId === ALL_TENANTS). This returns rows across all tenants and can leak cross-tenant data if misused.
  - _Fix:_ Avoid returning tenant-scoped rows without an explicit admin authorization check. Either require an explicit admin scope and guard the call with an authorization check, or always include tenant_id = <tenant> unless the caller is explicitly authorized and audited. If ALL_TENANTS is required for admins, make that path explicit and enforce an admin-only guard before executing the cross-tenant SELECT.
- **server/storage.ts:289** — SELECT against tenant-scoped table `messages` has no tenant_id constraint (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL).
  - _Fix:_ Constrain the lookup by tenant (e.g. pass tenantId into attachCitationMetadata and add WHERE tenant_id = ${tenantId}) or join back to a previously-validated source so the DB enforces the tenant filter. Avoid platform-wide selects on tenant tables.
- **server/storage.ts:959** — getMemoriesWithoutEmbeddings selects tenant-scoped `memory_entries` without any tenant_id constraint (no WHERE tenant_id = ...).
  - _Fix:_ Accept/require a tenantId or use requiredTenantScope/assertValidTenantId and add a tenant predicate to the WHERE clause. If this is intended as a platform-wide background job, make that explicit and call via an admin-only path with explicit ALL_TENANTS handling.
- **server/storage.ts:971** — getKnowledgeWithoutEmbeddings selects tenant-scoped `agent_knowledge` without any tenant_id constraint.
  - _Fix:_ Add tenant scoping (require tenantId or the ALL_TENANTS sentinel via requiredTenantScope) so background embedding updates cannot read/write across tenants unintentionally.
- **server/storage.ts:936** — updateMemoryEmbedding updates tenant-scoped table `memory_entries` by id only (WHERE id = ...), with no tenant_id constraint. This can modify rows across tenants if misused.
  - _Fix:_ Require and validate tenantId (assertValidTenantId or requireTenantScope) and add it to the WHERE clause: WHERE id = ${id} AND tenant_id = ${tenantId}. If this routine is only used by a trusted per-tenant worker, pass that tenant explicitly.
- **server/storage.ts:979** — archiveExpiredMemories issues an UPDATE on tenant-scoped `memory_entries` without any tenant_id predicate — it will archive rows for all tenants in one statement.
  - _Fix:_ Run archive/update per-tenant with an explicit tenantId predicate or restrict this operation to a controlled admin path. Prefer adding requiredTenantScope(memory_entries.tenantId, tenantId) so accidental platform-wide mutation is prevented.
- **server/storage.ts:995** — archiveStaleMemories issues an UPDATE on tenant-scoped `memory_entries` without tenant scoping, affecting all tenants.
  - _Fix:_ As above, require an explicit tenantId or perform the archival per-tenant using requiredTenantScope(memory_entries.tenantId, tenantId) to avoid cross-tenant mutations.
- **server/storage.ts:594** — getMemoryEntry can execute a SELECT on tenant-scoped `memory_entries` with only the id predicate if tenantId is omitted — the code warns but still allows an unscoped lookup.
  - _Fix:_ Make tenantId required for this function (use requireTenantScope/requiredTenantScope) or otherwise enforce ownership by validating caller tenant before performing the SELECT. If an unscoped read is intentionally allowed, restrict it to admin-only callers and document the choice.
- **server/tool-learning.ts:129** — SELECT against tenant-scoped table 'custom_tools' checks only `name` and does not constrain by tenant_id: `db.select().from(customTools).where(eq(customTools.name, parsed.name))`.
  - _Fix:_ Constrain reads to the caller's tenant. Add tenant filter (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or include tenantId in the WHERE so a tool lookup cannot read another tenant's row.
- **server/webhook-triggers.ts:85** — UPDATE on tenant-scoped table `webhook_triggers` modifies tenant_id for rows with `tenant_id IS NULL` without constraining to a caller tenant. This is a cross-tenant mutation performed at runtime/migration, touching tenant-scoped rows globally.
  - _Fix:_ Perform this migration as an explicit one-time migration under operator control (not in general runtime), or scope the update to only the intended tenant(s). If it must run in-app, ensure it's run only in an admin/migration path and document/audit it. Prefer explicit per-tenant updates (or a safe join that verifies intent) instead of an unscoped UPDATE.
- **server/webhook-triggers.ts:86** — DELETE FROM tenant-scoped table `webhook_trigger_events` executed without tenant scoping: the DELETE removes rows where no matching trigger exists across all tenants (subquery doesn't consider tenant_id). This may delete events belonging to tenants incorrectly.
  - _Fix:_ Limit the orphan delete to the correct tenant context, e.g. DELETE ... WHERE NOT EXISTS (SELECT 1 FROM webhook_triggers w WHERE w.id = e.trigger_id AND w.tenant_id = e.tenant_id). If global cleanup is required, run it as an explicit, audited migration with operator approval rather than as normal application logic.
- **server/webhook-triggers.ts:93** — UPDATE on tenant-scoped table `webhook_trigger_events` sets `tenant_id` by joining `webhook_triggers` without being constrained by caller tenant context. This is a cross-tenant mass-update performed at runtime/migration.
  - _Fix:_ Do this assignment via an explicit, audited migration (not general runtime code) or perform the update per-tenant and with safeguards that ensure no accidental cross-tenant assignment. Alternatively, run within a maintenance/admin-only path and log/verifiy affected rows before changing tenant_id values.
- **server/webhook-triggers.ts:98** — DELETE USING between tenant-scoped tables (`webhook_trigger_events` USING `webhook_triggers`) deletes rows where e.tenant_id <> w.tenant_id without scoping to any caller tenant. This mutates tenant-scoped data across tenants in one operation.
  - _Fix:_ Avoid unscoped cross-tenant DELETEs in application code. If this is intended as a migration cleanup, run it as part of an explicit migration/maintenance step under operator control. If run in-app, narrow the delete to a tenant context or at least require an admin/migration flag and audit the action before applying.

### LOW (1)

- **server/job-queue.ts:377** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} with no tenant filter. While this helper is intended for admin flows, it performs an unscoped read on a tenant-scoped table.
  - _Fix:_ Document and restrict this helper to admin-only code paths. If used by non-admin flows, require an additional verification step: callers who will mutate the job must re-query with AND tenant_id = ${expectedTenant} or accept an explicit tenantId param and validate it before making changes.

### Resolved — confirmed false positives (182)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments executes a SELECT on the tenant-scoped `experiments` table without constraining to a specific tenant (no WHERE tenant_id = ...). This reads running A/B experiments across all tenants.
  - _Why false positive:_ Global cron sweep reads across tenants but processes each row with its own tenant_id and updates per-row; no mixed tenant output shown.
- **server/agentic/approvals.ts:134** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals to mark pending approvals expired without scoping by tenant_id, updating approvals across all tenants.
  - _Why false positive:_ Expiry housekeeping intentionally sweeps pending approvals across tenants and carries tenantId/runId per row for follow-up status updates.
- **server/channel-routing.ts:53** _(global-table)_ — setChannelRoute INSERT/UPSERT writes persona_id into channel_routes with no tenant context and does not validate that the provided personaId belongs to the caller's tenant. This lets a caller insert or overwrite a global mapping that references personas from other tenants.
  - _Why false positive:_ Schema note shows personas are not tenant-scoped and channel_routes is keyed globally by channel, indicating intentional platform-global routing.
- **server/email.ts:429** _(system-sweep)_ — UPDATE against tenant-scoped table `inbox_messages` uses only id in WHERE (UPDATE ... WHERE id = ${row.id}) but the row was selected from a global (non-tenant-scoped) query; this allows updating a row without enforcing the tenant constraint and risks cross-tenant modification.
  - _Why false positive:_ Backfill sweep updates housekeeping/content fields on selected rows and does not return or mix tenant data to tenant-facing output.
- **server/embeddings.ts:855** _(system-sweep)_ — backfillEmbeddingVecs performs UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} without constraining by tenant. Updating by id alone on a tenant-scoped table (after a global SELECT) can modify rows across tenants.
  - _Why false positive:_ Backfill loop globally repairs embedding_vec housekeeping field only and does not expose mixed tenant data; this is maintenance across rows.
- **server/embeddings.ts:872** _(system-sweep)_ — backfillEmbeddingVecs updates `agent_knowledge` rows via UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without constraining by tenant_id. This permits cross-tenant writes based solely on id.
  - _Why false positive:_ Backfill loop globally repairs embedding_vec housekeeping field only and does not expose mixed tenant data; this is maintenance across rows.
- **server/felix-brain.ts:23** _(system-sweep)_ — SELECT from tenant-scoped table "agent_runs" without constraining tenant_id (no WHERE tenant_id = ...). The boot-time rehydration query returns rows across all tenants, violating tenant isolation.
  - _Why false positive:_ Boot-time rehydration intentionally scans all tenants and stores state keyed by row-carried tenant_id per conversation without tenant-facing mixed output.
- **server/glasses-gateway.ts:174** _(upstream-verified)_ — UPDATE on apiKeys uses only id in WHERE (no tenant_id constraint). This writes into a tenant-scoped table without scoping to tenant ownership.
  - _Why false positive:_ The row being updated was just selected in the same call path by keyHash and its id/tenant captured; update targets that verified row only.
- **server/memory-graph.ts:130** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_categories lacks a tenant_id predicate (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This allows a category id from another tenant to be modified.
  - _Why false positive:_ catId comes from ensureCategory, which selects/inserts memory_categories with the same tenantId immediately above in the same call path.
- **server/memory-graph.ts:709** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries lacks a tenant_id predicate (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). This can update a memory row belonging to a different tenant if ids are crafted.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries scoped by tenant_id = tenantId in the same function; update is on that vetted row set.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — Query selects FROM oauth_subscriptions WHERE is_active = TRUE without constraining by tenant_id — reads active subscriptions across all tenants (sensitive token metadata).
  - _Why false positive:_ startup validation intentionally scans active subscriptions across tenants for maintenance/refresh handling using each row's tenant_id; not tenant-facing output.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh reads oauth_subscriptions WHERE is_active = TRUE without tenant_id — iterates all tenants' subscriptions and may process secrets/refresh logic across tenants.
  - _Why false positive:_ proactive refresh is a platform-wide maintenance loop over active subscriptions, processing each row with its own tenant_id and only emitting labels/logs.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions SELECTs provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id constraint — enumerates all tenants' subscriptions.
  - _Why false positive:_ force-refresh-all is an explicit cron-style housekeeping loop over all active subscriptions, acting per row with tenant_id and not returning cross-tenant content.
- **server/orchestrator-ledger.ts:465** _(upstream-verified)_ — UPDATE to tenant-scoped table memory_entries performed without constraining by tenant_id (WHERE id = ${row.id} only). This can allow an update to affect a row belonging to a different tenant if id collisions or misuse occur.
  - _Why false positive:_ row.id is selected in the same transaction from memory_entries with source and tenant_id = tenantId before the update.
- **server/research-engine.ts:170** _(system-sweep)_ — UPDATE on tenant-scoped table research_sessions performed without any tenant_id constraint (cleanupZombieSessions). This will mark running sessions across all tenants as completed (cross-tenant modification).
  - _Why false positive:_ zombie-session cleanup is platform-wide housekeeping that only flips stale session status fields and returns ids/counts, not tenant data.
- **server/routes/projects.ts:568** _(fk-transitive)_ — INSERT INTO project_notes does not include tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT; otherwise tenant isolation can be violated or inserts can fail if tenant_id is NOT NULL.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively scoped through project_id after verifying the parent project belongs to tenantId.
- **server/routes/projects.ts:608** _(fk-transitive)_ — INSERT INTO project_files (base64 upload path) does not include tenant_id. Tenant-scoped tables require tenant_id on INSERT.
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped through project_id after verifying the parent project belongs to tenantId.
- **server/routes/projects.ts:676** _(fk-transitive)_ — INSERT INTO project_files (multipart upload path) does not include tenant_id. Tenant-scoped INSERTs must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by tenant-checked parent project_id
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE on sculptor_sessions lacks tenant_id constraint (UPDATE sculptor_sessions SET status = ... WHERE id = ${session.id})
  - _Why false positive:_ updates newly inserted session by server-generated session.id within same internal flow
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE on sculptor_sessions lacks tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id})
  - _Why false positive:_ updates newly created session row by server-generated primary key after internal launch
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE on sculptor_sessions in monitorSession lacks tenant_id constraint (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId})
  - _Why false positive:_ background monitor updates session by internal sessionId passed from creator, not untrusted request input
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks has no tenant_id filter — reads tenant-scoped table globally
  - _Why false positive:_ boot-time housekeeping count across tenants only computes aggregate for maintenance and returns no tenant data
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' ... has no tenant_id constraint — may disable rows across tenants
  - _Why false positive:_ boot-time maintenance sweep intentionally disables runaway tasks globally as housekeeping
- **server/seed-cleanup.ts:45** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE name ILIKE '%session%logger%' lacks tenant_id filter
  - _Why false positive:_ global maintenance count only aggregates rows for cleanup decision and exposes no tenant data
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' ... has no tenant_id constraint
  - _Why false positive:_ boot-time cleanup intentionally disables matching tasks across tenants as housekeeping only
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has no tenant_id filter
  - _Why false positive:_ global maintenance count for orphaned task patterns is aggregate-only housekeeping
- **server/seed.ts:2240** _(system-sweep)_ — SELECT that scans tenant-scoped tables (conversations JOIN projects) without restricting to a tenant, then subsequent INSERTs create project_conversations rows based on that result. This performs cross-tenant reads and writes (no WHERE tenant_id = ...), risking cross-tenant data exposure or cross-tenant linking.
  - _Why false positive:_ boot-time backfill scans all tenants but preserves per-row project/conversation pairing and inserts only link-table housekeeping rows
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from 'messages' (recentMsgs) is executed without constraining by tenant_id. This can leak messages across tenants.
  - _Why false positive:_ distillIntent calls analyzeConversation(conversationId, tenantId) first, which verifies the conversation belongs to tenantId in the same call path.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from 'messages' in skillifyConversation (recentMsgs) is executed without tenant scoping. Unscoped reads of message content can disclose other tenants' data.
  - _Why false positive:_ skillifyConversation calls analyzeConversation(conversationId, tenantId) first, which verifies tenant ownership of the conversation in the same call path.
- **server/skills-marketplace.ts:235** _(global-table)_ — SELECT * FROM skills WHERE id = ${skillId} is performed with no tenant constraint. Exporting skill details without tenant scoping can disclose another tenant's skill content.
  - _Why false positive:_ skills schema note says tenant_id column: NO; export reads a platform-global skills table.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without a tenant_id filter (DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' AND created_at < ...). This can delete rows across all tenants.
  - _Why false positive:_ watchdog cleanup is a platform-wide housekeeping delete of stale disabled delegation tasks, not tenant-facing data mixing.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from heartbeat_logs to detect flaky tasks has no tenant_id constraint (GROUP BY task_id, task_name HAVING COUNT(*) >= 4). This can surface failures across tenants and may produce actions that affect other tenants' tasks.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column: NO; this watchdog query operates on a global log table.
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs WHERE created_at < NOW() - INTERVAL '... hours' is executed without tenant_id scoping. This prunes logs across all tenants.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column: NO; pruning old global logs is benign maintenance.
- **server/tools.ts:7006** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The INSERT uses params.project_id etc but does not set tenant_id on the tenant-scoped table (project_files). Relying on prior assertProjectInTenant is insufficient for the invariant that every INSERT must include tenant_id explicitly.
  - _Why false positive:_ project_files schema note says tenant_id column: NO; insert is transitively scoped through tenant-owned project_id after assertProjectInTenant.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table `browser_workflows` omits tenant scoping — `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` updates by id only and lacks `tenant_id = <caller tenant>` constraint, enabling cross-tenant modification.
  - _Why false positive:_ workflow row is selected immediately above with tenant_id = tenantId, and UPDATE uses that verified row id
- **server/tools/domains/files/handlers.ts:937** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES clause. Every insert into a tenant-scoped table must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_files has no tenant_id and is linked through tenant-scoped projects/project_id validated by join to projects with tenant_id
- **server/webhook-triggers.ts:255** _(upstream-verified)_ — SELECT from tenant-scoped table `webhook_triggers` executed without any tenant_id constraint (lookup by webhook_key only). This allows a caller with a webhook key to read trigger rows across all tenants and then act in the context of the returned row(s).
  - _Why false positive:_ webhook_key is treated as the bearer capability; lookup validates exact key format and rejects ambiguous duplicates before use
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting SELECTs rows from tenant-scoped `ai_insights` without a tenant filter, reading pending auto-applied insights across all tenants.
  - _Why false positive:_ durability sweep intentionally scans across tenants and processes each row with its own tenant_id, skipping invalid tenant ids
- **server/agentic-engines.ts:247** _(system-sweep)_ — autoApplyEligibleInsights issues a bulk UPDATE on tenant-scoped `ai_insights` and, when called with undefined tenantId, the tenantScope placeholder is empty — causing a global (cross-tenant) UPDATE.
  - _Why false positive:_ bulk auto-apply is an intentional maintenance/backfill sweep across tenants; it mutates status/action fields and carries tenant_id per returned row
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table with no tenant_id filter — this reads rows across all tenants.
  - _Why false positive:_ heartbeat budget enforcement intentionally iterates all tenants and emits per-tenant events using each row's tenant_id
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — SELECT query scans tenant-scoped tables (agent_runs and agent_approvals) without constraining by tenant_id. The initial db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`) has no WHERE r.tenant_id = <tenant> clause, allowing cross-tenant reads of sensitive rows.
  - _Why false positive:_ resume worker is a maintenance sweep over pending runs across tenants; rows carry tenant_id and are handled per-row rather than mixed into one tenant response
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE/SELECT on tenant-scoped table `agent_wake_schedules` performed without any tenant_id constraint. The subquery SELECT id FROM agent_wake_schedules ... FOR UPDATE SKIP LOCKED can claim rows across all tenants, causing cross-tenant row mutation and event emission.
  - _Why false positive:_ Platform-wide scheduler intentionally claims due rows across tenants and emits each row with its own tenant_id; housekeeping/event dispatch does not mix tenants into one output.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — SELECT DISTINCT ... FROM conversations queries the tenant-scoped `conversations` table with no tenant_id WHERE clause. This reads potentially many tenants' conversation rows from a background job without tenant scoping.
  - _Why false positive:_ Backfill scans all tenants intentionally, carries c.tenant_id per row, and processes each conversation separately rather than returning mixed tenant data to a tenant-facing surface.
- **server/auto-project.ts:135** _(upstream-verified)_ — SELECT from messages reads conversation messages without constraining by tenant_id. The query uses conversation_id but does not include an explicit tenant_id filter, violating the invariant that every read against a tenant-scoped table must include tenant_id = <caller tenant>.
  - _Why false positive:_ Same transaction first proves conversations.id belongs to tenantId via WHERE id = conversationId AND tenant_id = tenantId; message read is downstream of that verified conversation.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} executes against the messages table without an explicit tenant_id filter. Even though conversation ownership was verified earlier, the invariant requires each tenant-scoped table access to include the tenant filter.
  - _Why false positive:_ Call path fail-closed with assertConversationInTenant and explicit conversation/project ownership checks before reading messages for that conversation.
- **server/auto-transcript.ts:132** _(system-sweep)_ — Backfill query SELECT DISTINCT c.id,... WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 performs a subquery against messages without scoping by tenant_id. This reads a tenant-scoped table (messages) across tenants.
  - _Why false positive:_ Tenant-wide backfill intentionally enumerates conversations across tenants and then invokes per-row processing with c.tenant_id; no mixed tenant output is produced.
- **server/auto-transcript.ts:160** _(system-sweep)_ — In the 'alsoLinked' query the subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 reads messages without tenant scoping. This can expose cross-tenant data when scanning project_conversations/conversations.
  - _Why false positive:_ Maintenance backfill scans all tenants and handles each linked conversation using its own c.tenant_id; aggregate housekeeping only, no tenant-facing mixed content.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id (no tenant scoping on the insert). If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this INSERT omits tenant_id and violates the app-level isolation invariant.
  - _Why false positive:_ invoice_items has no tenant_id and is a child of tenant-scoped invoices created in the same transaction; isolation is transitive via invoice_id.
- **server/channel-routing.ts:16** _(global-table)_ — channel_routes table is created without a tenant_id column, making routing records global. In a multi-tenant system this allows one tenant to affect or observe another tenant's channel routing and persona mappings.
  - _Why false positive:_ channel_routes is defined as a single global registry keyed by UNIQUE(channel), and referenced personas table is also non-tenant-scoped.
- **server/channel-routing.ts:30** _(global-table)_ — listChannelRoutes SELECT joins personas (p) but does not constrain the query to a tenant. If personas are tenant-scoped this can leak or mix persona names across tenants because there's no tenant_id filter on channel_routes or personas.
  - _Why false positive:_ Reads from global channel_routes joined to non-tenant-scoped personas; no tenant isolation boundary applies here.
- **server/channel-routing.ts:63** _(global-table)_ — getPersonaForChannel SELECT queries channel_routes by channel only and returns persona_id with no tenant filtering. This can return mappings belonging to other tenants.
  - _Why false positive:_ Lookup by channel reads the global routing registry, not a tenant-scoped table.
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute deletes by channel only (DELETE FROM channel_routes WHERE channel = ${channel}) with no tenant constraint, which can remove other tenants' routing entries.
  - _Why false positive:_ Delete targets the global routing registry by channel; table is intentionally not tenant-scoped.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT ... FROM commitments (scanAndEscalate) queries overdue commitments across all tenants without a WHERE tenant_id = ... constraint. This background scanner reads tenant-scoped rows platform-wide and then triggers per-row actions (including emailing an owner) based on those results.
  - _Why false positive:_ Boot/scheduler scanner intentionally sweeps all tenants, carries tenantId per row, and emits only redacted operator digest.
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Why false positive:_ Global purge sweep processes each soft-deleted conversation with its own tenant_id and only performs housekeeping deletes.
- **server/deep-interview.ts:338** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id (INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ...). Tenant_id columns are NOT NULL with no default — writes must include tenant_id or validate ownership.
  - _Why false positive:_ project_notes has no tenant_id and is inserted via tenant-scoped parent project selected with p.tenant_id = state.tenantId.
- **server/dfy-intake.ts:50** _(signed-url)_ — getDfyIntakeByToken performs SELECT FROM dfy_intake_forms without constraining by tenant_id. This reads a tenant-scoped row using only a request-derived token (no tenant filter).
  - _Why false positive:_ Lookup is by high-entropy public token; token itself is the bearer authorization rather than tenant scoping.
- **server/email.ts:370** _(system-sweep)_ — SELECT on tenant-scoped table `inbox_messages` is executed without constraining by tenant (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can cause cross-tenant collisions/leaks (a message_id belonging to another tenant will be treated as 'already stored').
  - _Why false positive:_ Shared-inbox poller is a platform-wide ingestion loop; existence check is housekeeping dedupe and does not return tenant data.
- **server/email.ts:415** _(system-sweep)_ — Global SELECT from tenant-scoped table `inbox_messages` (SELECT id, message_id FROM inbox_messages WHERE ... ) has no tenant filter. This reads rows across all tenants and can surface or operate on other tenants' data.
  - _Why false positive:_ Backfill is a maintenance sweep over stored inbox rows, updating empty body fields by row id only without tenant-facing output.
- **server/embeddings.ts:814** _(system-sweep)_ — backfillMissingKnowledgeEmbeddings runs a SELECT on tenant-scoped table `agent_knowledge` without any tenant filter (SELECT id, tenant_id, title, content FROM agent_knowledge ... LIMIT 50). This reads rows for all tenants in one operation.
  - _Why false positive:_ Embedding backfill is a maintenance loop processing each row with its own tenant_id and updating only embedding fields.
- **server/embeddings.ts:845** _(system-sweep)_ — backfillEmbeddingVecs selects rows from tenant-scoped `memory_entries` without a tenant filter (SELECT id, embedding FROM memory_entries ... LIMIT 200). This reads active memory entries across all tenants.
  - _Why false positive:_ Embedding vector backfill is platform-wide housekeeping that mutates only per-row embedding_vec state and returns aggregate counts.
- **server/embeddings.ts:861** _(system-sweep)_ — backfillEmbeddingVecs SELECT on tenant-scoped table `agent_knowledge` lacks tenant scoping (SELECT id, embedding FROM agent_knowledge ... LIMIT 200), reading rows across all tenants.
  - _Why false positive:_ Global backfill job updates housekeeping field embedding_vec by row id and returns only aggregate counts; no tenant-facing data mixing.
- **server/firecrawl.ts:239** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' uses only id in WHERE (no tenant_id constraint). This allows an update to a row by id regardless of tenant ownership.
  - _Why false positive:_ existing[0].id was selected in the same call path with url + tenantId filter, so the update target is already tenant-owned.
- **server/firecrawl.ts:372** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' uses only id in WHERE (no tenant_id constraint). This allows an update to a row by id regardless of tenant ownership.
  - _Why false positive:_ existing[0].id comes from prior SELECT scoped by pageUrl and tenantId in the same loop, proving ownership before UPDATE.
- **server/google-drive.ts:866** _(upstream-verified)_ — SELECT from projects is performed using only project id (where eq(projects.id, projectId)) and does not include tenant_id in the WHERE. Reading a tenant-scoped projects row without scoping risks cross-tenant access.
  - _Why false positive:_ Row is read by id only, but code immediately verifies project.tenantId === tenantId before using/returning folder data and throws on mismatch.
- **server/google-drive.ts:879** _(upstream-verified)_ — UPDATE on projects sets driveFolderId/driveFolderUrl but the WHERE clause uses only project id (no tenant_id constraint). This mutates a tenant-scoped row without enforcing the caller's tenant ownership.
  - _Why false positive:_ Same function first loads project and fail-closes on tenant mismatch, so UPDATE by projectId is guarded by prior ownership check.
- **server/google-drive.ts:1476** _(upstream-verified)_ — SELECT from projects (resolving projectId in uploadAndShare) queries projects by id only (where eq(projects.id, params.projectId)) without restricting by tenant_id prior to using the result. This reads a tenant-scoped row without an ownership constraint.
  - _Why false positive:_ Caller path immediately checks proj.tenantId against params.tenantId when provided and then calls ensureProjectFolder, which also enforces tenant ownership.
- **server/google-drive.ts:1723** _(upstream-verified)_ — SELECT from projects in searchDriveFiles queries the project by id (where eq(projects.id, params.projectId)) without including tenant_id in the WHERE. Reading projects without scoping allows cross-tenant disclosure.
  - _Why false positive:_ Project lookup is followed immediately by explicit tenant mismatch check before any search proceeds.
- **server/google-workspace.ts:2287** _(fk-transitive)_ — INSERT into presenter_slide_images does not include tenant_id. The raw INSERT/UPSERT uses session_id but never sets or checks tenant ownership, allowing cross-tenant persistence if session_id is incorrect or attacker-controlled.
  - _Why false positive:_ presenter_slide_images has no tenant_id and is keyed by session_id; isolation is transitively through tenant-scoped presenter_sessions parent.
- **server/google-workspace.ts:2307** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} executes with no tenant constraint. This mutates a likely tenant-scoped table (presenter_sessions) by primary key only, without verifying the session belongs to the caller's tenant.
  - _Why false positive:_ sessionId is a server-generated presenter session id returned from internal creation flow, then updated by primary key within same server routine.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals is executed without any tenant_id filter. If code_proposals is a tenant-scoped table, this read will expose/allows operating across all tenants.
  - _Why false positive:_ Admin health-audit stale-proposal scan is a platform-wide maintenance sweep; it only emits audit findings and archives stale status, not tenant-facing mixed content.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) runs without tenant_id constraint. This modifies proposals across all tenants by id list with no tenant scoping or ownership check.
  - _Why false positive:_ Global housekeeping update archives stale proposals by preselected ids as maintenance status change only, with no tenant data returned or mixed.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks is executed with no tenant_id filter. heartbeat_tasks appear to be tenant-aware in other parts of the app; running this query globally can read tasks for all tenants.
  - _Why false positive:_ Global health-audit scans heartbeat_tasks to flag/disable stale jobs as housekeeping; no tenant content is exposed or mixed into tenant-facing output.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) performs tenant-agnostic updates. This can disable tasks across tenants since no tenant_id ownership is checked.
  - _Why false positive:_ health-audit maintenance sweep disables stale heartbeat rows platform-wide and only mutates housekeeping state/counts
- **server/heartbeat-startup.ts:80** _(system-sweep)_ — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). This mutates tenant-scoped rows by id only and lacks explicit tenant scoping.
  - _Why false positive:_ startup normalization loop intentionally fixes seeded heartbeat task config across tenants as platform maintenance
- **server/heartbeat-startup.ts:85** _(system-sweep)_ — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant_id constraint (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). Missing tenant filter allows cross-tenant mutation.
  - _Why false positive:_ startup reflection-model normalization is a platform-wide housekeeping fix, not tenant-facing mixed access
- **server/heartbeat-task-scheduler-storage.ts:43** _(system-sweep)_ — UPDATE heartbeat_tasks ... WHERE id = ${id} ... RETURNING id performed without tenant_id constraint in WHERE clause. This updates task rows without asserting tenant ownership.
  - _Why false positive:_ scheduler claims due heartbeat tasks across tenants intentionally; each claimed row carries its own tenant_id for later per-row handling
- **server/job-queue.ts:160** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_jobs` (claimDueJobs) performs a global claim across all tenants — the UPDATE/SELECT lacks any tenant_id constraint, allowing cross-tenant mutation/claiming of jobs.
  - _Why false positive:_ global worker claim loop intentionally operates across tenants and returns row-carried tenant_id for isolated per-job processing
- **server/job-queue.ts:195** _(system-sweep)_ — UPDATE in reclaimExpiredLeases marks expired leases as failed_terminal without a tenant_id filter (mutates `agent_jobs` across all tenants).
  - _Why false positive:_ expired-lease reclaim is platform-wide housekeeping that only transitions job status fields across tenants
- **server/job-queue.ts:209** _(system-sweep)_ — UPDATE in reclaimExpiredLeases (requeue step) re-queues expired 'running' rows back to pending with no tenant_id constraint, mutating rows across all tenants.
  - _Why false positive:_ requeue of expired leases is cross-tenant maintenance updating only queue housekeeping fields
- **server/job-queue.ts:238** _(internal-worker-pk)_ — completeJob performs UPDATE on `agent_jobs` without constraining by tenant_id. A caller that passes an id could complete a job belonging to any tenant.
  - _Why false positive:_ completeJob is server worker state transition on globally unique job id with attempt fencing, not a tenant request boundary
- **server/job-queue.ts:298** _(internal-worker-pk)_ — failJob updates `agent_jobs` (both terminal and retry branches) without tenant_id in the WHERE clause (writes can affect jobs for any tenant).
  - _Why false positive:_ failJob is internal worker retry/terminal transition by server-claimed job primary key with attempt fencing
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT against project_files and subsequent INSERT do not include tenant scoping; project_files SELECT (line ~101) uses only project_id/file_name and the INSERT (line ~105) does not set tenant_id. If project_files is tenant-scoped this lets code read/insert rows without constraining them to the caller's tenant (cross-tenant leakage or orphan writes).
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects via project_id; insert/read is transitively scoped.
- **server/lib/ideabrowser-backfill.ts:115** _(fk-transitive)_ — SELECT against project_notes and subsequent INSERT do not include tenant scoping; the SELECT (line ~116) checks project_id/note without tenant_id and the INSERT (line ~120) does not set tenant_id. This violates the invariant that tenant-scoped tables must be filtered by tenant_id on every read/write.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects via project_id; insert/read is transitively scoped.
- **server/lib/ideabrowser-dev-sync.ts:139** _(fk-transitive)_ — INSERT INTO project_notes (via SELECT ... WHERE NOT EXISTS ...) omits tenant scoping in the NOT EXISTS subquery and the INSERT does not include tenant_id. The WHERE NOT EXISTS checks only project_id and author (line ~142–144) and can produce cross-tenant ambiguity if project_notes is tenant-scoped.
  - _Why false positive:_ project_notes has no tenant_id and child rows are scoped through the newly inserted tenant-scoped project_id.
- **server/lib/ideabrowser-dev-sync.ts:150** _(fk-transitive)_ — INSERT INTO project_files (via SELECT ... WHERE NOT EXISTS ...) omits tenant scoping in the NOT EXISTS subquery and the INSERT does not include tenant_id. The existence check and INSERT only reference project_id and file_name (lines ~151–155), lacking tenant_id constraints for tenant-scoped child rows.
  - _Why false positive:_ project_files has no tenant_id and child rows are scoped through the newly inserted tenant-scoped project_id.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — SELECT from tenant-scoped table `mcp_api_keys` is executed without constraining by tenant_id (query: `WHERE key_prefix = ${prefix} LIMIT 1`). This reads a tenant-scoped table without a tenant filter and can enable cross-tenant probing/enumeration or accidental leakage of other tenants' rows.
  - _Why false positive:_ API key verification intentionally resolves tenant_id from a presented key prefix/hash; tenant context is established by this lookup, not omitted accidentally.
- **server/lib/mcp-api-keys.ts:70** _(global-table)_ — Constructing a Postgres text[] literal by concatenating/escaping user-provided scopes into `scopesLit` and inlining it into an SQL template (`... ${scopesLit}::text[] ...`). This is manual string interpolation of request-derived data into SQL and risks SQL injection or quoting/encoding bugs.
  - _Why false positive:_ Not a tenant-isolation finding; issue is SQL construction, outside cross-tenant data-isolation scope.
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — scanOutcomePromotions executes a cross-tenant aggregation: `SELECT tenant_id, format, count(*)... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format` has NO caller-scoped WHERE tenant_id filter. This reads tenant-scoped rows for all tenants in one query and therefore violates the invariant that every read against a tenant-scoped table must be constrained to a caller tenant.
  - _Why false positive:_ Nightly maintenance scan intentionally aggregates across tenants and then processes each tenant separately by row-carried tenant_id.
- **server/lib/restraint-trust.ts:88** _(global-table)_ — Use of sql.raw() with a request-derived value (the column name is derived from the caller-provided `outcome`), which can lead to SQL-injection or other risks if the whitelist/validation is bypassed or changed. Even though the code includes a whitelist check, sql.raw() is being used with a value originating from user input.
  - _Why false positive:_ Not a tenant-isolation finding; UPDATE includes tenant_id filter and flagged concern is sql.raw injection, outside tenant isolation scope.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE against tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE selects due rows across all tenants). This allows cross-tenant locking/processing and violates the app-level tenant-isolation invariant.
  - _Why false positive:_ Heartbeat runner intentionally claims due rows across tenants, returning each row with its own tenant_id for per-row processing.
- **server/minerva-planner.ts:563** _(upstream-verified)_ — Tenant filter bug in rollback UPDATE: the SQL uses AND tenant_id = ${args.planId} (planId) instead of the caller's tenantId. This makes the UPDATE filter wrong and can modify rows across tenants (or fail to restrict to the caller's tenant).
  - _Why false positive:_ Code shown already uses args.tenantId in the rollback WHERE; first-pass description of planId-vs-tenantId bug does not match the snippet.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — Query SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — scans subscriptions across tenants.
  - _Why false positive:_ Boot-time startup auto-reconnect intentionally scans all subscription rows and then handles each using its own tenant_id; maintenance routine, not tenant-facing data return.
- **server/pace-control.ts:85** _(global-table)_ — Queries counting rows in heartbeat_logs do not include any tenant_id filter. If heartbeat_logs is tenant-scoped, this allows cross-tenant aggregation and a tenant can exhaust another tenant's window or observe global usage.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is described as platform-global pace/heartbeat logging.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries heartbeat_logs without tenant scoping, returning cross-tenant aggregates (SELECT ... FROM heartbeat_logs WHERE created_at > ...).
  - _Why false positive:_ heartbeat_logs is a non-tenant-scoped global table; snapshot is over global logs by design.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} reads a persona row without constraining by tenant_id. The function receives tenantId but does not validate that the persona belongs to that tenant — this allows cross-tenant data access.
  - _Why false positive:_ personas table has no tenant_id column, so this read is from a global/shared table rather than a tenant-scoped one.
- **server/persona-export.ts:39** _(global-table)_ — SELECT from skills uses WHERE persona_id = ${personaId} OR persona_id IS NULL but does NOT include tenant_id scoping. Tenant-specific skills will be missed or tenant data could be mixed with global skills incorrectly.
  - _Why false positive:_ skills table has no tenant_id column; query mixes persona-specific/global skills within a globally scoped table, not a tenant-scoped one.
- **server/plan-executor.ts:1024** _(system-sweep)_ — SELECT from tenant-scoped table `plans` without tenant filter: `SELECT id, tenant_id, status, plan_json, execution_log FROM plans WHERE status = 'executing' AND updated_at < ...`. This reads rows across all tenants with no tenant_id constraint.
  - _Why false positive:_ Boot-time recovery intentionally scans stale plans across tenants and processes each row with its own tenant_id for row-scoped recovery updates.
- **server/plan-executor.ts:1090** _(system-sweep)_ — SELECT from tenant-scoped table `plans` without tenant filter: `SELECT id, tenant_id, status, ceo_decision, ... FROM plans WHERE status = 'failed' AND ceo_decision = 'approved' ...`. This reads/filters rows across tenants with no tenant constraint before later using tenant-aware UPDATEs.
  - _Why false positive:_ Recovery loop intentionally selects recent transient failures across tenants and resets each candidate using its carried tenant_id; housekeeping, not tenant-facing output.
- **server/plan-executor.ts:1149** _(system-sweep)_ — SELECT from tenant-scoped table `plans` without tenant filter: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`. This will return approved plans from all tenants.
  - _Why false positive:_ Boot kicker intentionally finds approved plans platform-wide and schedules per-plan execution; maintenance routine without mixed tenant data exposure.
- **server/reference-learner.ts:296** _(upstream-verified)_ — UPDATE to tenant-scoped table 'memory_entries' omits tenant_id in WHERE clause (UPDATE ... WHERE id = ${storedId}). Even though the row was previously selected with tenant_id, the subsequent UPDATE must also enforce tenant scoping to prevent cross-tenant modification.
  - _Why false positive:_ storedId comes from a prior SELECT on memory_entries scoped by tenant_id and persona_id in the same flow before the UPDATE
- **server/research-engine.ts:457** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes omits tenant_id (autoDepositFindings — summary branch). Per project_notes tenant-scoping, tenant_id must be provided to avoid cross-tenant writes or DB errors.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id; project ownership is verified by assertProjectInTenant
- **server/research-engine.ts:466** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes omits tenant_id (autoDepositFindings — per-finding loop). Each persisted project note must include tenant_id to enforce tenant isolation and prevent cross-tenant writes.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id; project ownership is verified by assertProjectInTenant
- **server/routes.ts:482** _(system-sweep)_ — DELETE from tenant-scoped table `order_lookup_codes` with no tenant_id constraint (DELETE FROM order_lookup_codes WHERE expires_at < NOW()). This operation deletes rows for all tenants indiscriminately.
  - _Why false positive:_ boot/interval housekeeping deletes only expired order_lookup_codes across tenants and does not return or mix tenant data
- **server/routes.ts:625** _(system-sweep)_ — SELECT from tenant-scoped table `projects` without tenant filter (SELECT p.id, p.name, p.tenant_id, ... FROM projects p JOIN tenants t ON t.id = p.tenant_id WHERE p.drive_folder_id IS NULL ...). This reads rows across all tenants with no tenant_id constraint.
  - _Why false positive:_ cross-tenant backfill loop processes each project with its own row-carried tenant_id for maintenance only
- **server/routes.ts:504** _(system-sweep)_ — SELECT subqueries reference tenant-scoped tables (`conversations`, `projects`) without tenant constraints inside a tenants cleanup query (id NOT IN (SELECT DISTINCT tenant_id FROM conversations ...)). This performs unscoped reads against tenant-scoped tables.
  - _Why false positive:_ test-tenant cleanup is a platform maintenance sweep using aggregate existence checks, not tenant-facing data access or mixing
- **server/routes.ts:2060** _(upstream-verified)_ — SELECT presenter session by token (SELECT id, tenant_id, presentation_id, slides FROM presenter_sessions WHERE token = ${token}) with no tenant constraint. This unscoped read drives later writes/updates scoped by the returned tenant_id, enabling a token-only access pattern to a tenant-scoped table.
  - _Why false positive:_ same call path is gated by x-internal-key/SESSION_SECRET before the token lookup, making this an internal-only operation
- **server/routes.ts:4652** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The code inserts a note tied to a project_id without specifying tenant_id, relying on application logic elsewhere to scope the project. Because there is no row-level security or FK cascade enforcing tenant ownership, this can create rows that are not explicitly tenant-scoped and allow cross-tenant data leakage or ambiguity.
  - _Why false positive:_ project_notes has no tenant_id column and note insertion is tied transitively to tenant-scoped project_id obtained from a tenant-scoped conversation query
- **server/routes/conversations.ts:119** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` omits tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This allows modifying a conversation row without ensuring it belongs to the caller's tenant.
  - _Why false positive:_ conv.id comes from storage.createConversation with tenantId in the same request and project ownership is checked for the same tenant before update
- **server/routes/conversations.ts:123** _(fk-transitive)_ — INSERT into `project_conversations` does not include tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). If `project_conversations` is a tenant-scoped table this creates rows with no tenant context / allows cross-tenant linkage.
  - _Why false positive:_ project_conversations has no tenant_id by schema and is linked through tenant-scoped projects; project ownership is checked just above.
- **server/routes/conversations.ts:125** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` omits tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This will update a project row without verifying tenant ownership.
  - _Why false positive:_ same call path first verifies project id with WHERE id = projectId AND tenant_id = tenantId before this timestamp-only update.
- **server/routes/conversations.ts:256** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). If `project_notes` is tenant-scoped this creates notes without tenant context and may allow cross-tenant writes.
  - _Why false positive:_ project_notes has no tenant_id by schema and insert is tied to projId derived from tenant-checked conversation/project join.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` is executed without a tenant_id predicate (`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`). This violates the app-level tenant-isolation invariant.
  - _Why false positive:_ message row is fetched immediately above with WHERE id = msgId AND tenant_id = tenantId; update uses that verified row id in same path.
- **server/routes/metrics.ts:79** _(system-sweep)_ — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) lacks a tenant predicate and returns cross-tenant usage counts.
  - _Why false positive:_ operator metrics endpoint intentionally aggregates platform-wide tool counts only and returns no tenant-specific rows or mixed tenant content.
- **server/routes/metrics.ts:95** _(system-sweep)_ — SELECT summarizing tokens (FROM tenant-scoped `agent_cost_ledger`) has no tenant filter, allowing cross-tenant aggregation to be read by the caller.
  - _Why false positive:_ platform-wide maintenance/observability aggregate of total tokens only; no tenant identifiers or tenant-specific content returned.
- **server/routes/oauth-subscriptions.ts:325** _(system-sweep)_ — Admin/cron handler executes an unscoped SELECT DISTINCT tenant_id FROM oauth_subscriptions to enumerate tenant IDs (`SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE`) without applying any tenant predicate or platform-admin session guard. This enumerates tenants and can be abused if the cron secret is leaked.
  - _Why false positive:_ cron-secret-protected maintenance loop intentionally enumerates inactive rows and processes each tenant separately; not a tenant-facing data surface.
- **server/routes/projects.ts:165** _(fk-transitive)_ — SELECT subqueries counting project_files, project_notes, and project_conversations do not constrain those tenant-scoped tables by tenant_id (they only filter by project_id). This can leak or mis-attribute rows if rows in those tables are not tenant-isolated.
  - _Why false positive:_ all counted child tables shown have no tenant_id by schema and are transitively scoped by parent project_id; outer query already scopes projects by tenant.
- **server/routes/projects.ts:616** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} has no tenant_id constraint. Without tenant filtering this UPDATE could affect a row outside the caller's tenant (even if ownership was checked earlier).
  - _Why false positive:_ same handler validated project ownership with SELECT ... WHERE id = projectId AND tenant_id = tenantId before this timestamp update.
- **server/routes/projects.ts:684** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (multipart upload flow) omits tenant_id in the WHERE clause.
  - _Why false positive:_ multipart upload flow checks project ownership just above with tenant-scoped SELECT before this timestamp-only update.
- **server/routes/projects.ts:703** _(upstream-verified)_ — SELECT * FROM project_files WHERE project_id = ${projectId} does not include tenant_id. Reading tenant-scoped tables must include tenant_id in the predicate to enforce isolation.
  - _Why false positive:_ Project ownership is verified immediately above via projects.id + tenant_id before reading child rows by project_id.
- **server/routes/projects.ts:726** _(upstream-verified)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 omits tenant_id. This read of a tenant-scoped table lacks an explicit tenant_id constraint.
  - _Why false positive:_ Route first proves the project belongs to tenantId, then reads project_files scoped by that verified project_id.
- **server/routes/projects.ts:755** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not constrain by tenant_id. Deleting from a tenant-scoped table without tenant filtering risks affecting other tenants' rows.
  - _Why false positive:_ Delete is limited to file within a project already verified as owned by tenantId in the same call path.
- **server/seed.ts:42** _(system-sweep)_ — SELECT on tenant-scoped table 'research_sessions' without any tenant filter (SELECT COUNT(*) FROM research_sessions). This reads across all tenants rather than being constrained to a specific tenant.
  - _Why false positive:_ Boot-time snapshot import/seed logic intentionally checks global emptiness and does not return mixed tenant data to any tenant-facing surface.
- **server/seed.ts:3667** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` is executed without any tenant_id constraint (global update).
  - _Why false positive:_ Boot-time maintenance migration updates rows across tenants but only normalizes stored text and emits counts, with no tenant data returned or mixed.
- **server/seed.ts:3693** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` is executed without any tenant_id constraint (global update).
  - _Why false positive:_ Global migration updates only a housekeeping/model field across tenants during seed and only logs aggregate counts.
- **server/seed.ts:3639** _(system-sweep)_ — DELETE on tenant-scoped table `heartbeat_tasks` is executed without any tenant_id constraint (global delete).
  - _Why false positive:_ Boot-time housekeeping purge removes stale disabled task rows across tenants and only emits aggregate count, no tenant content exposure.
- **server/self-reflection.ts:381** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `memory_entries` performs WHERE id = <row id> without also checking tenant_id. Even though the prior SELECT checked tenant_id, the UPDATE by id can update another tenant's row (race or ID reuse).
  - _Why false positive:_ Server-internal function updates by previously selected row id after tenant-scoped lookup; id is not taken from an untrusted request boundary.
- **server/skillify.ts:221** _(fk-transitive)_ — SELECT from 'personas' uses persona id from a conversation but does not check persona.tenant_id. This may allow reading a persona that belongs to another tenant.
  - _Why false positive:_ Referenced table personas has no tenant_id; persona id comes from a conversation already constrained to tenantId.
- **server/skills-marketplace.ts:209** _(global-table)_ — SELECT id FROM skills WHERE name = ${template.name} is run with no tenant constraint. If 'skills' is tenant-scoped, this leaks cross-tenant existence/ids.
  - _Why false positive:_ Schema note shows skills has no tenant_id, indicating a shared/global skills catalog rather than tenant-scoped rows.
- **server/surprise-scorer.ts:96** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_proposals' (setting args_embedding) lacks a tenant_id filter — the query is: `UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL`.
  - _Why false positive:_ helper is server-invoked with a server-side proposalId from scoreProposalSurprise/felix flow, updating a row by primary key only
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE on tenant-scoped table 'custom_tools' modifies implementation using only the PK id in WHERE: `db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id))` — missing tenant_id constraint.
  - _Why false positive:_ update uses tool.id from a row already fetched in executeCustomTool via name+tenant_id scoping, so target row ownership was established earlier in the same flow
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE that increments usageCount on tenant-scoped 'custom_tools' uses only id in WHERE: `db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(eq(customTools.id, tool.id))` — missing tenant filter.
  - _Why false positive:_ tool row is selected immediately above with tenant_id = tenantId, and update uses that verified row's id
- **server/tools.ts:2806** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. Tenant-scoped rows must include tenant_id on INSERT to ensure per-tenant isolation.
  - _Why false positive:_ project_conversations has no tenant_id by schema and insert occurs only after project/conversation link is ownership-checked through tenant-scoped parents
- **server/tools.ts:2931** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Why false positive:_ project_files has no tenant_id by schema and insert is under prior project ownership check, isolating transitively via tenant-scoped projects parent
- **server/tools.ts:2946** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. Writing tenant-scoped data without tenant_id risks cross-tenant contamination.
  - _Why false positive:_ project_notes has no tenant_id by schema and insert is under prior project ownership check, isolating transitively via tenant-scoped projects parent
- **server/tools.ts:2970** _(fk-transitive)_ — INSERT into project_conversations (second occurrence) omits tenant_id. Rows linking projects/conversations must be tenant-scoped in-table.
  - _Why false positive:_ project_conversations has no tenant_id by schema and both project ownership and conversation tenant ownership are verified before insert
- **server/tools.ts:4546** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. Even though the code verified project ownership earlier, the INSERT omits the required tenant_id column/value, violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id by schema and write happens only after assertProjectInTenant on the parent project
- **server/tools.ts:4550** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checked project ownership, but the INSERT omits tenant_id so the row could be written without explicit tenant attribution.
  - _Why false positive:_ project_files has no tenant_id by schema and write happens only after assertProjectInTenant on the parent project
- **server/tools.ts:6465** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (in generate_audio) does not include tenant_id. Although the code calls assertProjectInTenant, the INSERT omits tenant_id, violating the rule that every INSERT must set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by assertProjectInTenant on tenant-scoped parent project
- **server/tools/domains/cmmc-prospecting/handlers.ts:156** _(fk-transitive)_ — INSERT into `project_files` does not include tenant_id. The INSERT lists (project_id, file_name, file_path, ...) but omits tenant_id despite the app using tenant-scoped access controls elsewhere — this can misattribute or bypass tenant scoping.
  - _Why false positive:_ project_files has no tenant_id; insert is transitively scoped via project_id and WHERE EXISTS on projects.tenant_id = ctx.tenantId
- **server/tools/domains/files/handlers.ts:941** _(upstream-verified)_ — UPDATE against tenant-scoped table `projects` uses WHERE id = ${projId} with no tenant_id constraint. This can modify another tenant's project if ids are not otherwise constrained by tenant in the WHERE clause.
  - _Why false positive:_ projId comes from prior SELECT joining conversations to projects with p.tenant_id = tenantId in same call path
- **server/tools/domains/knowledge/handlers.ts:91** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` uses WHERE id = ${row.id} without constraining tenant_id. Although the row was selected earlier under tenant scope, UPDATE should still require tenant_id in WHERE to prevent cross-tenant modification if ids are reused or otherwise accessible.
  - _Why false positive:_ row.id values were selected immediately above from knowledge_triples filtered by tenant_id = tenantId and then updated in same loop
- **server/tools/domains/strategic-memory/handlers.ts:99** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries does not constrain by tenant_id (WHERE id = ${id} only). This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ id is obtained from an immediately prior SELECT on memory_entries scoped by tenant_id = tid and persona_id in same flow
- **server/tools/domains/strategic-memory/handlers.ts:159** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses WHERE id = ANY(${ids}) without also constraining tenant_id. Writes derived from selected ids must still enforce tenant scoping in the UPDATE to prevent accidental cross-tenant modification.
  - _Why false positive:_ ids array is derived solely from rows selected just above with tenant_id = tid and persona_id filter before the update
- **server/tools/domains/strategic-memory/handlers.ts:258** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries does not include tenant_id in the WHERE clause (WHERE id = ${id} only). This allows a row id to be updated without verifying it belongs to the caller's tenant.
  - _Why false positive:_ id comes from the immediately preceding tenant-scoped SELECT on memory_entries with tenant_id = tid
- **server/video-job-runner.ts:273** _(system-sweep)_ — SELECT from tenant-scoped table videoJobs (listStaleVideoJobs) runs without filtering by tenant_id. This returns rows across all tenants (stale-job recovery scans the global table), violating the codebase's tenant-isolation-by-application-filters invariant.
  - _Why false positive:_ stale-job recovery is a platform-wide maintenance sweep over jobs and downstream claim/update uses each row's own tenantId
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — completeActivity updates agentActivity rows using only the activity id (WHERE id = activityId) with no tenant constraint. The function signature lacks a tenantId, so callers can update rows across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/embeddings.ts:809** _(system-sweep)_ — storeEmbeddingVec falls back to executing UPDATE ${sql.raw(table)} ... WHERE id = ${id} when no tenantId is supplied, performing an UPDATE on a tenant-scoped table without any tenant_id constraint. This can overwrite rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/plan-executor.ts:613** _(upstream-verified)_ — UPDATE on tenant-scoped table `plans` performed without constraining by tenant_id: `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...`. This allows claiming/updating a plan row from any tenant if the caller supplies a plan id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): CAS claim UPDATE runs inside executePlan whose planId ownership is validated upstream by getPlan(id, tenantId) at server/routes/agentic-policy.ts:113 (route gated requirePlatformAdmin at :95).
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... has no tenant scoping — it scans projects/conversations across all tenants and will expose/misprocess data from other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM `conversations` WHERE c.project_id = ${proj.id} is executed without tenant_id filtering — conversation rows may belong to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM `messages` WHERE conversation_id = ${conv.id} is executed without a tenant filter — this can read messages from other tenants' conversations.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE on sculptor_sessions lacks tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId})
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:502** _(internal-worker-pk)_ — UPDATE on agent_cost_ledger uses only reservation id in WHERE (WHERE id = ${reservationId}) with no tenant_id constraint
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:427** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `experiments` uses only the primary key in the WHERE clause (where(eq(experiments.id, experiment.id))) and lacks a tenant_id constraint. This allows updating an experiment row belonging to a different tenant if an attacker/bug can guess the id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from 'messages' inside analyzeConversation is missing a tenant_id constraint (reads all messages for a conversation without verifying tenant).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Within analyzeConversation, the conversation ownership is asserted immediately before at skillify.ts:196-199 (WHERE eq(id) AND eq(tenantId), throws if absent); the messages SELECT at 201-203 reads the same owned conversation's messages.
- **server/storage.ts:1409** _(upstream-verified)_ — getAllDataForExport performs SELECTs across many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with no tenant_id filtering, returning platform-wide tenant data. There is no explicit tenant scope/ALL_TENANTS sentinel — this exposes all tenants' data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tools.ts:8978** _(internal-worker-pk)_ — UPDATE on proposed_skills uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) without constraining tenant_id. proposed_skills is written with tenantId on insert, but subsequent updates lack a tenant filter, allowing cross-tenant modification by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9007** _(internal-worker-pk)_ — UPDATE on proposed_skills uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) without constraining tenant_id, allowing modifications across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9021** _(internal-worker-pk)_ — UPDATE on proposed_skills (marking accepted/promotedSkillId) lacks a tenant_id constraint in the WHERE clause; it updates by id only, risking cross-tenant changes.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:9038** _(internal-worker-pk)_ — UPDATE on proposed_skills (marking rejected) uses only id in WHERE and omits tenant_id, allowing a caller to alter another tenant's proposed_skills row by id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (only filters by id). This allows updating a reservation row by id across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (only filters by id) when releasing a reservation. This can modify another tenant's ledger row if an attacker supplies a reservation id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agentic-features.ts:861** _(upstream-verified)_ — enrollInSequence checks for existing outreach_enrollments with SELECT ... WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant_id — a read against a tenant-scoped table without tenant scoping.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL updates the conversations table without constraining by tenant_id. The backfill loop derived c.tenant_id earlier but the UPDATE itself lacks the tenant filter and can modify rows across tenants if ids collide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): auto-transcript.ts:165 UPDATE conversations SET project_id inside backfillProjectTranscripts platform sweep; row comes from a tenant-consistent join (auto-transcript.ts:153-161 JOIN projects p ON p.tenant_id=c.tenant_id) and only sets housekeeping project_id
- **server/chat-engine.ts:3926** _(upstream-verified)_ — call to recordErrorForRetryStormDetection(conversationId, ...) (stuck-diagnostics) is invoked WITHOUT tenantId. The stuck-diagnostics hook may persist or read diagnostic rows by conversationId only, enabling cross-tenant access/modification.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): recordErrorForRetryStormDetection is an in-memory diagnostics counter keyed by conversationId (stuck-diagnostics); it performs no DB read/write and returns no tenant data.
- **server/chat-engine.ts:4161** _(internal-worker-pk)_ — calls to recordToolCallForStuckDetection(...) and postDiagnosticReport(...) (stuck-diagnostics) are invoked WITHOUT passing tenantId. These functions may write/read diagnostic/report rows keyed only by conversationId, risking tenant-cross contamination.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): recordToolCallForStuckDetection is in-memory loop diagnostics keyed by conversationId; no DB write occurs.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on commitments (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}) is missing a tenant_id constraint. This is a cross-tenant writable operation and can alter another tenant's row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/heartbeat.ts:2679** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint after creating a tenant-scoped task. The UPDATE can affect tenant rows without explicit tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3151** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint in delegateTaskFromChat. Missing tenant filtering permits cross-tenant mutation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/job-queue.ts:335** _(internal-worker-pk)_ — cancelJob updates `agent_jobs` by id without verifying tenant_id — this allows cancelling jobs across tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:125** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_trace_spans` lacks tenant_id constraint (WHERE clause only has span_id = ${spanId}). This allows a cross-tenant UPDATE if span_id collisions occur or a malicious/buggy call reuses a span id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the row id in the WHERE clause and does not constrain by tenant_id. This relies on a prior lookup (existing) instead of enforcing the caller's tenant in the UPDATE, violating the invariant that every write to a tenant-scoped table must include an explicit tenant filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/martech-bundle.ts:311 existing = getVoiceProfile({tenantId: opts.tenantId, profileName}), which filters WHERE tenantId=opts.tenantId (194-196), so existing.id is proven tenant-owned before the UPDATE at 315
- **server/process-governor.ts:898** _(upstream-verified)_ — UPDATE to tenant-scoped table `agent_desks` is executed without constraining by tenant_id (WHERE id = ${desk.id} only). This allows mutating rows belonging to other tenants if id values collide or are guessed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:641 UPDATE agent_desks WHERE id=desk.id; desk.id from tenant-scoped SELECT at process-governor.ts:632-637 (WHERE ad.tenant_id)
- **server/process-governor.ts:1235** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks a tenant_id constraint. This mutates a tenant-scoped table without ensuring the row belongs to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name,status,description FROM `projects` is executed with WHERE id = ${projectId} but no tenant_id constraint. The function accepts only projectId and may read another tenant's project.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/recurring-messages.ts:307** _(system-sweep)_ — UPDATE to tenant-scoped table 'agent_knowledge' omits tenant_id in WHERE clause (UPDATE ... WHERE id = ${r.id}). This violates the invariant that every read/write against a tenant-scoped table must be constrained to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/routes.ts:1925** _(signed-url)_ — SELECT from tenant-scoped table `presenter_sessions` by token without any tenant_id constraint (SELECT ... FROM presenter_sessions WHERE token = ${token}). Returns session/tenant information without ensuring the caller is the owning tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2057 — presenter token is crypto.randomBytes(16).hex (routes.ts:2140) — an unguessable capability URL for public slide viewing; the token IS the access control by design.
- **server/seed.ts:2172** _(system-sweep)_ — UPDATE on tenant-scoped table 'projects' without a tenant constraint (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ...). This can modify rows belonging to any tenant if ids collide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:3131-3138 inside seedDatabase(); guarded by isOwnerDeployment() (3133) and uses fully hardcoded literals (id=15, primary_conversation_id=272) — a one-time owner-deployment migration, not reachable from any request input.
- **server/storage.ts:585** _(system-sweep)_ — getAllMemoriesForBackup selects all rows from tenant-scoped `memory_entries` with no tenant_id filter, returning platform-wide tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table 'custom_tools' uses only id in the WHERE clause: `db.delete(customTools).where(eq(customTools.id, tool.id))` — no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This reads pairing rows across all tenants, violating the invariant that tenant-scoped tables must be filtered by the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT from tenant-scoped table `conversations` uses only conversation id (WHERE id = ${info.conversationId}) and does not constrain by tenant_id. This performs a tenant-scoped read without enforcing the caller/claimant tenant filter.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
