# Tenant-Isolation Audit Report

_Generated 2026-08-10T22:28:16.219Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **589/589** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **292** (CRITICAL 69 · HIGH 178 · MEDIUM 43 · LOW 2)
- Precision second-pass (`gpt-5.4`): **237** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (69)

- **server/agentic-engines.ts:243** — autoApplyEligibleInsights executes an UPDATE on `ai_insights` (SET status='applied' ...) without any tenant_id constraint — this can modify rows across all tenants in bulk.
  - _Fix:_ Do not perform bulk tenant-agnostic updates. Restrict the UPDATE to a single tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant and run the UPDATE per-tenant. Ensure any RETURNING results are tenant-scoped before taking follow-up actions.
- **server/agentic/resume-worker.ts:66** — Cross-tenant SELECT: the candidate query (SELECT ... FROM agent_runs JOIN agent_approvals ...) does not constrain agent_runs to a specific tenant_id. It scans/returns rows across all tenants, enabling cross-tenant observation/processing.
  - _Fix:_ Limit the scan to a single tenant (or iterate per-tenant). Add a WHERE clause filtering r.tenant_id = ${tenantId} (or accept a tenantId parameter) so every read from tenant-scoped tables is constrained to the caller's tenant. Ensure any subsequent processing uses that tenant value rather than trusting global rows.
- **server/agentic/runs.ts:34** — UPDATE without tenant constraint: appendStep() updates agent_runs by id only (WHERE id = runId) and does not include tenant_id in the WHERE clause, allowing a caller with a runId to update another tenant's run.
  - _Fix:_ Require/accept tenantId in appendStep and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Always constrain updates to the tenant to enforce tenant isolation.
- **server/agentic/runs.ts:45** — UPDATE without tenant constraint: updateRunState() merges JSONB into agent_runs and uses WHERE id = runId only; tenant_id is not enforced, permitting cross-tenant mutation of run state.
  - _Fix:_ Add tenantId as an argument and include eq(agentRuns.tenantId, tenantId) in the WHERE clause (use and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all state updates are tenant-scoped.
- **server/agentic/runs.ts:54** — UPDATE without tenant constraint: replaceRunState() calls update(agentRuns).set({ state }) with WHERE id = runId only, missing tenant scoping and allowing cross-tenant overwrite of run state.
  - _Fix:_ Require tenantId and add it to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Do not perform updates by id alone on tenant-scoped tables.
- **server/agentic/runs.ts:58** — UPDATE without tenant constraint: completeRun() sets status/result/completedAt for a run using WHERE id = runId only (no tenant_id filter), allowing occasion of completing a run belonging to another tenant.
  - _Fix:_ Include tenant scoping in the update: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Modify call sites to pass tenantId through to this function so updates always enforce tenant ownership.
- **server/agentic/runs.ts:88** — UPDATE without tenant constraint: failRun() updates agent_runs.status/error/completedAt using WHERE id = runId only, lacking tenant filtering and permitting cross-tenant state changes.
  - _Fix:_ Require tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Audit all call sites to provide tenant context and prevent cross-tenant modification.
- **server/agentic/runs.ts:97** — UPDATE without tenant constraint: pauseRun() updates agent_runs.status and state using WHERE id = runId only; tenant_id is not constrained, enabling a tenant to pause/mutate another tenant's run.
  - _Fix:_ Add tenantId to the function signature and use it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all run lifecycle mutations always include tenant scoping.
- **server/auto-memorize.ts:74** — INSERT into `agent_knowledge` hardcodes tenant_id = 1. This writes a tenant-scoped table to tenant 1 regardless of the acting tenant, enabling cross-tenant poisoning and incorrect attribution.
  - _Fix:_ Do not hardcode tenant IDs. Accept and use the real tenantId (e.g. INSERT ... tenant_id = ${tenantId}) or otherwise record watermarks in a properly-scoped/global table. Validate all callers supply the tenant.
- **server/auto-memorize.ts:163** — SELECT from `messages` (SELECT m.tenant_id, m.role, ...) has no WHERE tenant_id filter and pulls messages for many tenants into a single LLM prompt pipeline. Even though tenant_id is selected for later partitioning, this reads other tenants' data into memory and risks cross-tenant data exposure.
  - _Fix:_ Do not SELECT messages across tenants in a single query. Either (a) run the query per-tenant with WHERE m.tenant_id = ${tenantId} (and iterate tenants safely), or (b) add a strict tenant filter driven by an explicit admin flow. Ensure each LLM prompt only contains data from one tenant.
- **server/channel-routing.ts:16** — channel_routes table is created without any tenant_id column; this makes routing entries global and allows cross-tenant references to tenant-scoped rows (e.g. personas).
  - _Fix:_ If channel routing is intended to be per-tenant, add a NOT NULL tenant_id column (and appropriate unique index like UNIQUE(channel, tenant_id)). Ensure all queries/CRUD operations include WHERE tenant_id = <callerTenant> and always set tenant_id on INSERT. If the table must remain global, enforce ownership checks when referencing tenant-scoped entities (see other findings).
- **server/data-protection.ts:168** — DELETE against tenant-scoped table `conversations` without tenant_id constraint: `DELETE FROM conversations WHERE id = ${conv.id}`. This removes a conversation row by id without constraining to the conversation's tenant.
  - _Fix:_ Always include tenant scoping on destructive operations. Change to `DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id} RETURNING id` (or run the delete inside a withTenantTx for conv.tenant_id). This prevents accidental cross-tenant deletes if IDs collide or if data was malformed.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped tables (memory_entries | agent_knowledge) performed without scoping by tenant_id (WHERE id = ${id} only). This allows updating rows across tenants by id.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or otherwise validate ownership before updating). Do not perform cross-tenant updates by id alone.
- **server/felix-brain.ts:19** — Boot-time rehydration SELECT on tenant-scoped table 'agent_runs' has no tenant_id constraint — it reads all tenants' running/pending runs.
  - _Fix:_ Restrict the query to a single tenant or iterate per-tenant with an authenticated tenant context. Example: add WHERE tenant_id = ${tenantId} (or run one query per tenant and call runWithTenant/ALSM to establish context) so you don't leak/rebuild state across tenants.
- **server/health-audit.ts:314** — SELECT from code_proposals (archiveStaleProposals) lacks any tenant_id constraint. If code_proposals is tenant-scoped this reads proposals for all tenants and later the code may act on them globally.
  - _Fix:_ Scope this query to a tenant (e.g. WHERE tenant_id = ${tenantId} AND ...), or if the scanner is intentionally global require an explicit, auditable admin context (explicit admin tenant id) and document that intent. Prefer per-tenant processing (loop tenants or accept a tenant param) to avoid accidental cross-tenant reads.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows without constraining by tenant_id. This can change proposals belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform the update via a query that joins/filter by tenant. If global admin action is intended, require an explicit admin tenant context with clear checks/logging.
- **server/job-queue.ts:98** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id constraint — it can atomically claim jobs across all tenants.
  - _Fix:_ If this is intended as per-process/global queue work, make the behavior explicit and defend it (audit, admin-only daemon). Otherwise, scope claims to a tenant (e.g. include AND tenant_id = ${tenantId}) or iterate per-tenant to avoid cross-tenant claiming. At minimum: record and check the job.tenant_id before executing tenant-scoped handlers and add a WHERE tenant_id = ${job.tenantId} on any follow-up writes to ensure the write is scoped.
- **server/lib/scheduled-post-runner.ts:61** — Initial CTE SELECT/UPDATE on tenant-scoped table `scheduled_posts` (lines 61–81) has no tenant_id constraint. The statement selects due rows and then UPDATEs `scheduled_posts` SET status='publishing' ... FROM due WHERE sp.id = due.id without constraining by tenant_id, allowing cross-tenant locking/updates.
  - _Fix:_ Limit this system operation explicitly per-tenant or include tenant_id in the CTE and the UPDATE join. For example, SELECT id, tenant_id FROM scheduled_posts ... and UPDATE ... WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id (or otherwise pass a caller tenant and add AND sp.tenant_id = <tenantId>). If this must run across tenants, ensure it's executed in a controlled admin context and audit/authorize that behavior; otherwise enforce per-tenant WHERE tenant_id = ${tenantId}.
- **server/minds-engine.ts:214** — createTicket constructs a Postgres array literal for depends_on via string interpolation: {${params.dependsOn.join(",")}. This directly embeds request-derived data into SQL text and is vulnerable to SQL injection and tenant-isolation bypass.
  - _Fix:_ Do not construct SQL array literals via string interpolation. Use parameterized array binding or Drizzle helpers (e.g. pass an actual array parameter or use sql`ARRAY[...]` with parameterized elements) so dependsOn values are bound safely. Also validate/normalize the dependsOn elements as integers before binding.
- **server/minds-engine.ts:262** — checkDependenciesMet builds an ANY(...) array using string interpolation: id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]). If ticket.dependsOn contains untrusted values this is SQL-injectable (dependsOn may originate from requests via createTicket).
  - _Fix:_ Replace string-interpolated array construction with a parameterized array/ANY binding. E.g. use parameterized SQL with an integer array parameter or Drizzle's array helpers to bind each id safely (and validate they are integers). Ensure dependsOn is validated at creation time and stored safely.
- **server/minds-engine.ts:327** — markEventsHandled builds id = ANY(${`{${eventIds.join(",")}}`}::int[]) using string interpolation of the caller-supplied eventIds array. This is a direct SQL-injection risk when eventIds are request-derived.
  - _Fix:_ Use a parameterized array binding for eventIds instead of interpolating an array literal. Validate/normalize eventIds to integers and pass them as a bound parameter or use the DB/Drizzle array binding mechanism (e.g. WHERE id = ANY(${eventIdsParam})) so values are not injected into SQL text.
- **server/minerva-planner.ts:367** — UPDATE on tenant-scoped table `plans` may execute without a tenant constraint — the query conditionally appends `AND tenant_id = ${args.tenantId}`. If args.tenantId is omitted, the UPDATE will run without scoping to a tenant, allowing cross-tenant modifications.
  - _Fix:_ Require and validate tenantId for this operation (do not allow a missing/optional tenantId), or unconditionally include `AND tenant_id = ${tenantId}` in the WHERE clause. Validate the caller-supplied tenantId before executing the UPDATE and fail rather than executing an unscoped update.
- **server/minerva-planner.ts:386** — SELECT probe (SELECT status, ceo_decision FROM plans ...) conditionally includes `AND tenant_id = ${args.tenantId}`. When args.tenantId is omitted this SELECT runs without tenant scoping, enabling cross-tenant reads (IDOR).
  - _Fix:_ Do not allow an unscoped probe SELECT. Make tenantId required for external/route callers and always include `AND tenant_id = ${tenantId}`. If internal callers legitimately omit tenantId, provide a separate internal-only helper that is clearly gated and not reachable from tenant-facing paths.
- **server/minerva-planner.ts:403** — SELECT child plan (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...) conditionally appends `AND tenant_id = ${args.tenantId}`. If args.tenantId is omitted this read is unscoped and can expose/return rows from other tenants.
  - _Fix:_ Always constrain this SELECT by tenant_id (or require and assert a valid tenantId before running). If an internal flow must omit tenantId, call a trusted internal helper (documented and access-controlled) — do not use a branch that allows an unscoped SELECT in code reachable from tenant-facing callers.
- **server/minerva-planner.ts:472** — Rollback UPDATE flips status back to 'awaiting_approval' with WHERE id = ${args.planId} AND status = 'revising' — missing any tenant_id constraint. This unscoped UPDATE can modify plans across tenants.
  - _Fix:_ Add `AND tenant_id = ${args.tenantId}` to the WHERE clause and require/validate args.tenantId. If tenantId is not available in this context, fetch the row first in a tenant-scoped manner and use that tenant_id for the rollback UPDATE. Never issue an UPDATE to tenant-scoped tables without tenant scoping.
- **server/minerva-planner.ts:549** — getPlan has an unchecked branch that executes `SELECT * FROM plans WHERE id = ${planId}` when tenantId is omitted. This returns plan rows without tenant scoping (cross-tenant read) when called without tenantId.
  - _Fix:_ Remove the unscoped branch or make tenantId required. If internal callers legitimately need global access, provide a clearly documented internal-only helper and ensure it's not callable from tenant-facing routes. Otherwise always include `AND tenant_id = ${tenantId}` in the SELECT.
- **server/plan-executor.ts:116** — UPDATE plans executed in appendExecutionLog does not constrain by tenant_id (UPDATE plans ... WHERE id = ${planId}). This mutates a tenant-scoped table without verifying the caller/plan tenant.
  - _Fix:_ Include tenant scoping (WHERE id = ${planId} AND tenant_id = ${tenantId}). Pass the plan's tenant_id into appendExecutionLog (or re-SELECT/verify ownership) and use it in the WHERE clause so writes are restricted to the owning tenant.
- **server/plan-executor.ts:513** — CAS claim UPDATE on plans lacks a tenant_id constraint (UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved' RETURNING ...). This allows changing rows across tenants by id only.
  - _Fix:_ Add AND tenant_id = ${callerTenantId} to the WHERE clause. Alternatively pre-validate plan ownership (SELECT tenant_id) and then perform the CAS using both id and tenant_id to ensure the operation is limited to the plan's tenant.
- **server/plan-executor.ts:527** — UPDATE plans sets status='failed' using only id (WHERE id = ${planId}) with no tenant_id filter — write to tenant-scoped table without tenant scoping.
  - _Fix:_ Constrain the UPDATE with tenant_id: WHERE id = ${planId} AND tenant_id = ${tenantId}. Ensure the function has or derives the tenantId (from the claimed row) before performing the update.
- **server/plan-executor.ts:728** — Final UPDATE of plans to set status uses WHERE id = ${planId} but no tenant_id filter, allowing cross-tenant mutation.
  - _Fix:_ Use WHERE id = ${planId} AND tenant_id = ${tenantId} (pass/derive tenantId from the claim row) to ensure only the owning tenant's plan row is updated.
- **server/plan-executor.ts:762** — Error-path UPDATE plans (setting status='failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id in WHERE clause.
  - _Fix:_ Add tenant scoping: WHERE id = ${planId} AND tenant_id = ${tenantId} AND status = 'executing'. Ensure tenantId is available in the catch handler (derive from earlier claim or safe lookup).
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write against heartbeat_tasks without constraining tenant_id. This can disable tasks belonging to other tenants if id collision or attacker-supplied id is used.
  - _Fix:_ Include tenant scoping on the UPDATE: WHERE id = ${t.id} AND tenant_id = ${tenantId}. Prefer passing tenantId into the closure/context so every UPDATE includes the tenant constraint.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped table without filtering by tenant_id — can reveal proposals belonging to other tenants.
  - _Fix:_ Require tenant context and add AND tenant_id = ${tenantId} to the WHERE clause (or perform an initial tenant-scoped lookup and validate ownership before proceeding).
- **server/proposal-verifier.ts:241** — persist() updates code_proposals (UPDATE code_proposals SET verification_status=... WHERE id = ${proposalId}) without a tenant_id constraint — writes can affect proposals across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Ensure persist() receives/derives the tenantId before performing the update.
- **server/proposal-verifier.ts:107** — After a rebase race the code clears rebased_at via UPDATE code_proposals ... WHERE id = ${proposalId} with no tenant_id filter — mutates validation_result for proposals across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause. Ensure the tenant is validated prior to modifying proposal rows.
- **server/proposal-verifier.ts:372** — Rebase path performs UPDATE code_proposals SET status='rejected', verification_status='skipped', ... WHERE id = ${proposalId} without tenant_id scoping — cross-tenant mutation risk.
  - _Fix:_ Constrain the UPDATE with tenant_id (WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Make tenant context explicit for rebase flows.
- **server/proposal-verifier.ts:408** — After successful rebase the code updates code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} without tenant filtering.
  - _Fix:_ Include tenant constraint in the UPDATE: WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Pass tenantId into attemptRebaseOnDrift and persist updates accordingly.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` performed without constraining by tenant (UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}). Missing tenant_id in WHERE allows modifying another tenant's row if an id is reused or in races.
  - _Fix:_ Include the tenant_id condition in the UPDATE to enforce per-tenant isolation, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. If the code is running in a global/system context, derive and pass an explicit tenantId for the row being updated and use it in the WHERE clause.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` executed without tenant_id in the WHERE clause (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This can modify another tenant's memory entry by id.
  - _Fix:_ Constrain the UPDATE by tenant_id (and persona_id if relevant). Change to: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId} (and optionally AND persona_id = ${personaId}) so the update only affects the owning tenant's row.
- **server/research-engine.ts:165** — Cross-tenant UPDATE: cleanupZombieSessions runs an UPDATE on research_sessions (marks running sessions completed) with no tenant_id constraint, affecting sessions across all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant context or iterate per-tenant. Add a WHERE tenant_id = ${tenantId} (or otherwise parameterize/derive the tenant list) so the operation cannot modify other tenants' rows. If this is intended as a global admin operation, require and validate an explicit admin/tenant scope parameter and log/authorize it.
- **server/research-engine.ts:400** — Unscoped DELETE: startup DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() has no tenant restriction and will delete autoresearch knowledge for all tenants.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = ${tenantId}) or perform the cleanup per-tenant. If the call runs during a session start, pass session.tenantId and use it in the WHERE clause. Ensure every destructive statement on tenant-scoped tables includes tenant_id parameters.
- **server/routes.ts:2144** — Presenter creation INSERT sets tenant_id directly from req.body (INSERT INTO presenter_sessions (tenant_id, ...) VALUES (${tenantId}, ...)). tenantId is caller-supplied and is not validated against the caller's session/ownership.
  - _Fix:_ Do not accept tenantId from untrusted request body. Derive tenantId from the authenticated session or a verified signed capability. If this endpoint must be able to create sessions for arbitrary tenants, require a strong internal admin authentication and explicitly validate/authorize the requested tenantId before inserting. At minimum, check that the caller is platform-admin and confirm ownership before inserting.
- **server/routes/admin-zombie-detector.ts:38** — Query reads from agent_trace_spans (a tenant-scoped table) without constraining it by tenant_id. The WITH/SELECT pulls usage across all tenants (no WHERE tenant_id = ...) inside a transaction, which allows cross-tenant data exposure.
  - _Fix:_ If this endpoint is intended to produce per-tenant diagnostics, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) to the agent_trace_spans subquery. If it must run globally, make the behavior explicit and require/verify an explicit admin scope parameter (and audit logging). Avoid implicit cross-tenant SELECTs — always bind tenantId from the caller (or accept and validate an explicit tenant parameter) before querying tenant-scoped tables.
- **server/routes/admin-zombie-detector.ts:54** — Query aggregates conversation activity (SELECT ... FROM conversations ...) without a tenant_id constraint. The derived 'used' CTE and subsequent join on personas therefore reflect cross-tenant conversation usage.
  - _Fix:_ Restrict the conversations query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId} in the CTE). Alternatively, require an explicit, validated tenant parameter when running cross-tenant diagnostics and document/audit the action. Do not read tenant-scoped tables without an explicit tenant_id filter.
- **server/routes/conversations.ts:99** — UPDATE on tenant-scoped table 'conversations' is missing a tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This can modify rows belonging to other tenants if a row id collides or code is invoked with different context.
  - _Fix:_ Constrain the UPDATE by tenant. e.g. UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId} (or use conv.tenantId if already validated). Always include tenant_id in WHERE for writes to tenant-scoped tables.
- **server/routes/conversations.ts:103** — INSERT into project_conversations does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). tenant_id is required and not provided — new rows may be orphaned or attributed to the wrong tenant.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})). Ensure every INSERT into tenant-scoped tables sets tenant_id from the authenticated tenant and does not rely on derived/untrusted sources.
- **server/routes/conversations.ts:105** — UPDATE on tenant-scoped table 'projects' lacks a tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This can update another tenant's project if ids collide or context is incorrect.
  - _Fix:_ Constrain the UPDATE with tenant_id: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Always include tenant_id in WHERE for updates to tenant-scoped tables.
- **server/routes/conversations.ts:230** — INSERT into project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). project_notes is tenant-scoped but the tenant_id column is not provided, producing an insert that lacks tenant attribution.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})). Ensure all INSERTs into tenant-scoped tables set tenant_id from the authenticated tenant and do not use untrusted/derived sources for it.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This allows a caller to modify a session row by id across tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${params.tenantId} or WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Validate the tenant from a trusted source and use it in the UPDATE parameterization.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions executed without tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). This can link/modify a session belonging to another tenant if id is guessed.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) and ensure the tenantId is the trusted caller tenant.
- **server/sculptor.ts:168** — UPDATE sculptor_sessions executed without tenant_id constraint inside monitorSession (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). This permits cross-tenant status/result mutations by session id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Propagate and use the trusted tenantId when updating session rows.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions executed without tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). This can overwrite another tenant's session review by id.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Verify the session belongs to the caller's tenant before updating.
- **server/second-opinion.ts:405** — UPDATE agent_cost_ledger executed without tenant_id constraint in settleFusionReservation (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}). agent_cost_ledger is used per-tenant; updating by id alone can settle or tamper with another tenant's reservation row.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Pass the tenantId into settleFusionReservation (or look up/validate the row's tenant before updating) to ensure only the owning tenant's ledger row is modified.
- **server/second-opinion.ts:423** — UPDATE agent_cost_ledger executed without tenant_id constraint in releaseFusionReservation (UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId}). This can release/zero another tenant's reservation row by id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) and ensure releaseFusionReservation receives/validates the tenantId for the reservation before performing the write.
- **server/seed.ts:2163** — UPDATE on tenant-scoped table `research_programs` without tenant constraint (UPDATE research_programs SET model = ... WHERE id = ${p.id}). Even though id is a primary key, the update lacks an explicit tenant_id check and can modify another tenant's row if misused.
  - _Fix:_ Include tenant ownership in the UPDATE WHERE clause. E.g. UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} AND tenant_id = ${tenantId} (or .where(eq(research_programs.id, p.id), eq(research_programs.tenantId, tenantId))). Ensure tenantId is validated/available in this context before performing the update.
- **server/seed.ts:3855** — UPDATE on tenant-scoped table memory_entries without any tenant_id constraint (mass cross-tenant update).
  - _Fix:_ Restrict the UPDATE to a specific tenant (e.g. add WHERE tenant_id = <tenantId> in addition to existing predicates) or iterate per-tenant and run within an explicit tenant-scoped transaction so only the intended tenant's rows are modified.
- **server/seed.ts:3880** — UPDATE on tenant-scoped table conversations without any tenant_id constraint (will modify conversations across all tenants).
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. WHERE model = 'gpt-4.1' AND tenant_id = <tenantId>) or run this backfill per-tenant after determining the target tenant id(s). Avoid global updates on tenant-scoped tables.
- **server/seed.ts:3826** — DELETE FROM heartbeat_tasks performed with no tenant_id constraint — can delete rows across all tenants.
  - _Fix:_ Scope the DELETE to the intended tenant (e.g. WHERE ... AND tenant_id = <tenantId>) or run tenant-scoped cleanup loops. If the intent is to affect only system/admin tenant, explicitly include tenant_id = 1.
- **server/seed.ts:4094** — UPDATE on tenant-scoped table agent_knowledge uses only id in WHERE clause (no tenant_id constraint). Although id was selected previously with a tenant filter, the UPDATE statement itself lacks tenant scoping and can affect rows if ids are re-used or if called with different context.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = ${rows[0].id} AND tenant_id = <tenantId>) or perform the UPDATE using the original tenant-scoped SELECT results within a verified tenant context to ensure only the intended tenant row is modified.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` is performed using only the experiment id: .where(eq(experiments.id, experiment.id)). This allows cross-tenant row modification if an attacker knows an id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Ensure you have the experiment's tenantId in scope when performing the update. The same fix applies to the similar UPDATE in the catch block that uses eq(experiments.id, experiment.id).
- **server/self-improvement.ts:413** — UPDATE in error-handling path updates `experiments` by id only (.where(eq(experiments.id, experiment.id))) without tenant_id constraint.
  - _Fix:_ Include tenant ownership in the WHERE clause (AND tenant_id = <caller tenant>) so the error-status update cannot affect other tenants' rows.
- **server/storage.ts:1311** — getAllDataForExport() performs many unscoped SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant-specific keys, heartbeat_tasks, etc.), exporting all tenants' data with no tenant filter or admin guard.
  - _Fix:_ This is a highly sensitive operation — require an explicit admin-only code path, multi-factor/role-based guard, and/or require an explicit ALL_TENANTS sentinel called only after authorization. Prefer offering per-tenant exports (require tenantId) and, if a full export is needed, gate it behind strict audit/logging and an explicit admin flag; avoid an unconstrained function callable from regular code paths.
- **server/tools.ts:2878** — INSERT into project_conversations omits tenant_id. This writes to a tenant-scoped association table without explicitly setting the caller's tenant, allowing cross-tenant contamination or orphaned rows if the table expects tenant scope.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). If the table is intentionally scoped by project_id only, enforce and assert ownership before the INSERT and still record tenant_id on the row to make tenant ownership explicit and robust to future schema/usage changes.
- **server/tools.ts:3019** — INSERT into project_conversations (link_conversation path) does not set tenant_id. Even though the code validated the conversation and project beforehand, the INSERT itself lacks an explicit tenant_id column value, violating the invariant that every INSERT into tenant-scoped tables must pass tenantId.
  - _Fix:_ Pass tenant_id explicitly in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Keep the prior ownership checks but also write tenant_id to the row to prevent cross-tenant ambiguity and enforce isolation at the application level.
- **server/tools.ts:2981** — INSERT into project_files (add_file) does not include tenant_id. This writes a file row linked to a project without explicitly recording the caller's tenant, violating the requirement that INSERTs into tenant-scoped tables must set tenantId.
  - _Fix:_ Add tenant_id to the INSERT (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by, tenant_id) VALUES (..., ${tenantId})). If tenancy is derived from project_id, still persist tenant_id on the project_files row and/or enforce a WHERE tenant_id = ${tenantId} guard on writes to avoid future IDORs.
- **server/tools.ts:2995** — INSERT into project_notes (add_note) omits tenant_id. The code verifies project ownership, but the INSERT into the tenant-scoped notes table does not set tenant_id, breaking the rule that every INSERT must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). Retain the ownership check, but also persist tenant_id on the note row so tenant isolation is explicit and enforced by application-level filters.
- **server/tools/domains/files/handlers.ts:903** — INSERT into `project_files` does not set tenant_id (sql: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`). Per invariant, tenant-scoped tables require tenant_id to be explicitly provided on INSERT. Omitting it can misattribute rows or fail if tenant_id is NOT NULL with no default.
  - _Fix:_ Pass the tenant id explicitly in the INSERT columns/values (and derive it from the trusted ctx.tenantId). Example: `INSERT INTO project_files (project_id, tenant_id, file_name, file_path, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ${absPath}, ...)`. Also keep verifying ownership of the referenced project_id before inserting (which you already do), but still include tenant_id on the row itself.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} updates a tenant-scoped table using only the row id and NO tenant_id constraint. This allows a caller who can supply or guess an id to modify another tenant's ledger row.
  - _Fix:_ Always include tenant scoping on writes. Change the UPDATE to include tenant_id in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${expectedTenantId}) or use Drizzle's eq(agentCostLedger.tenantId, tenantId) in the where clause. Ensure the caller provides/derives the tenantId (e.g. the reservation was created under a known tenant) and verify it before updating.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (release path) updates a tenant-scoped table by id only, with no tenant_id constraint. This lets code release (modify) reservation rows belonging to other tenants if an id is guessed or misrouted.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${expectedTenantId}) or use Drizzle eq(table.tenantId, tenantId). Verify reservation ownership before modifying or releasing it. Alternatively, perform the update within the same transaction/context that created the reservation and carry the tenantId through to the settle/release call.

### HIGH (178)

- **server/ab-optimizer.ts:110** — SELECT from `experiments` in runDueAbExperiments has no tenant_id constraint (reads experiments across all tenants).
  - _Fix:_ Scope the query to a specific tenant. Either accept a tenantId parameter and add WHERE tenant_id = ${tenantId}, or iterate per-tenant and only operate on rows for that tenant. Never run unscoped cross-tenant SELECTs on tenant-scoped tables.
- **server/agent-activity.ts:122** — UPDATE agentActivity (completeActivity) uses WHERE id = <activityId> with no tenant_id constraint — allows updating another tenant's activity row by id.
  - _Fix:_ Require the caller to pass tenantId and include it in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) so updates are tenant-scoped.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs rows from `ai_insights` without a tenant_id filter (reads high-priority applied insights across tenants).
  - _Fix:_ Make the sweep tenant-scoped: accept/derive a tenantId and add WHERE tenant_id = ${tenantId}, or iterate per-tenant and execute the query per-tenant. Avoid unscoped reads of tenant-scoped tables.
- **server/agentic-engines.ts:347** — gatherOperationalData reads from `heartbeat_logs` (count of calls) with no tenant_id filter — returns data across tenants.
  - _Fix:_ Add tenant scoping to the heartbeat_logs query (WHERE tenant_id = ${tenantId}) or otherwise ensure the read is intentionally global and safe. By default treat heartbeat_logs as tenant-scoped.
- **server/agentic-engines.ts:451** — runPredictiveEngine selects recent rows from `heartbeat_logs` (status = 'success') with no tenant_id constraint — reads logs for all tenants.
  - _Fix:_ Filter the query by tenant_id (WHERE tenant_id = ${tenantId}) or run it per-tenant. Avoid unscoped selects on tenant-scoped logging tables.
- **server/agentic-engines.ts:519** — runOptimizationEngine queries `heartbeat_logs` for task performance with no tenant_id constraint (GROUP BY across all tenants).
  - _Fix:_ Scope this query to the tenant of interest (WHERE tenant_id = ${tenantId}) or run per-tenant aggregation. Do not aggregate tenant-scoped logs globally.
- **server/agentic-engines.ts:533** — runOptimizationEngine selects heartbeat task configuration with WHERE enabled = true but no tenant_id filter (reads config across tenants).
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND enabled = true) to avoid leaking or acting on other tenants' task config.
- **server/agentic-features.ts:563** — UPDATE lead_enrichments sets icp_score/etc WHERE id = ${lead.id} but does not constrain by tenant_id — allows updating another tenant's lead row by id.
  - _Fix:_ Include AND tenant_id = ${params.tenantId} in the WHERE clause (and validate tenantId at function entry) to ensure updates affect only the caller's tenant rows.
- **server/agentic-features.ts:638** — SELECT existing outreach_enrollments checks for an existing active enrollment but omits tenant_id in the WHERE, potentially matching rows from other tenants.
  - _Fix:_ Add tenant scoping to the existence check (e.g. WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' AND tenant_id = ${params.tenantId}).
- **server/agentic-features.ts:797** — UPDATE outreach_enrollments sets reply_classification and status WHERE id = ${row.id} but does not include tenant_id — may update another tenant's enrollment by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to ensure the update only affects rows owned by the caller's tenant.
- **server/agentic/approvals.ts:136** — expireStaleApprovals performs a bulk UPDATE on agentApprovals (set status='expired') with no tenant_id constraint — will expire pending approvals across all tenants.
  - _Fix:_ Restrict expiry to a specific tenant or iterate per-tenant. Add tenant_id to the WHERE clause (or accept a tenantId parameter) so expiry only touches approvals for the intended tenant and avoid cross-tenant mutation.
- **server/agentic/department-budgets.ts:143** — Unrestricted SELECT across tenant-scoped table `department_budgets`: the query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` has no tenant_id constraint. This reads rows for all tenants from a tenant-scoped table, violating the requirement that reads be constrained to the caller's tenant.
  - _Fix:_ If this is intended as a cross-tenant admin sweep, make the admin intent explicit and restrict execution to a privileged, audited admin job; otherwise add an explicit tenant filter (WHERE tenant_id = ${tenantId}) or accept a tenantId parameter and iterate per-tenant. Ensure any multi-tenant sweep runs under a clearly-documented admin context and avoid unfiltered selects on tenant-scoped tables from general-purpose code.
- **server/agentic/wake-scheduler.ts:142** — UPDATE/SELECT on agent_wake_schedules claims rows via a subquery that lacks any tenant_id constraint. The statement `UPDATE agent_wake_schedules ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE SKIP LOCKED)` can lock and mutate pending wakes across all tenants.
  - _Fix:_ Limit the claim to a single tenant (or iterate per-tenant). Add an explicit tenant_id predicate to the inner SELECT/outer UPDATE (e.g. WHERE tenant_id = ${tenantId}) or change the scheduler to fetch/process rows per-tenant in a controlled loop so no cross-tenant rows are claimed or mutated.
- **server/auto-memorize.ts:47** — getWatermark SELECT from tenant-scoped table `agent_knowledge` has no tenant_id constraint. This reads watermark rows across all tenants.
  - _Fix:_ Make watermarks tenant-scoped: accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to the SELECT (or explicitly document/implement a true global watermark table). Ensure all callers pass the correct tenantId.
- **server/auto-memorize.ts:62** — setWatermark first SELECT from `agent_knowledge` to check existing rows lacks a tenant_id filter. This can return rows for other tenants and lead to incorrect UPDATE/INSERT behavior.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT that looks up an existing watermark (or otherwise scope the transaction to the tenant). All watermark lookups must be tenant-scoped.
- **server/auto-transcript.ts:128** — backfillProjectTranscripts SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... has no tenant_id constraint. This scans conversations across all tenants and drives per-tenant side-effects (file writes, DB updates) without scoping by the caller tenant.
  - _Fix:_ Scope the backfill by tenant (add WHERE c.tenant_id = ${tenantId}) or explicitly iterate over tenants and call the per-tenant save for each. Ensure the operation only processes a tenant when the caller is authorized for that tenant.
- **server/auto-transcript.ts:153** — Query assigned to `alsoLinked` joins project_conversations/conversations/projects and uses a subquery on messages but has no overall tenant filter. It can return rows across tenants and is then used to UPDATE conversations and call autoSaveProjectTranscript for each returned row.
  - _Fix:_ Add an explicit tenant filter (e.g. AND c.tenant_id = ${tenantId}) or iterate per-tenant. Before performing any UPDATE/side-effect, verify the project and conversation ownership for that same tenant.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL runs without scoping the UPDATE by tenant_id. If the earlier query returned rows from other tenants, this UPDATE could modify another tenant's conversation row.
  - _Fix:_ Include AND tenant_id = ${c.tenant_id} (or the acting tenant) in the UPDATE WHERE clause, or perform an ownership assertion before updating. Never update tenant-scoped rows solely by id without also verifying tenant_id.
- **server/auto-transcript.ts:171** — missingLinks SELECT c.id, c.project_id, c.tenant_id FROM conversations ... (and subsequent logic) is executed without filtering by tenant_id. The result is used to insert project_conversations and to call autoSaveProjectTranscript across tenants.
  - _Fix:_ Constrain this query by tenant (WHERE c.tenant_id = ${tenantId}) or run it per-tenant with explicit authorization. Before inserting into project_conversations or making other writes, assert the project and conversation both belong to the same, authorized tenant.
- **server/auto-transcript.ts:180** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING is executed based on a prior SELECT that was not tenant-scoped. project_conversations has no tenant_id column (isolation is transitive), so missing upstream tenant verification can cause cross-tenant link insertion.
  - _Fix:_ Ensure ownership of both the project and conversation has been validated for the intended tenant before inserting into project_conversations. Either include a tenant-aware guard or perform the INSERT inside a transaction after verifying projects. If project_conversations must remain unscoped, make the insert only after explicit tenant checks.
- **server/business-tools.ts:95** — INSERT into tenant-scoped table 'invoice_items' omits tenant_id in the VALUES clause (no tenant_id column is set on invoice_items rows). This violates the tenant-isolation invariant: tenant_id must be explicitly stored on each tenant-scoped row.
  - _Fix:_ Include the tenant id when inserting invoice_items inside the transaction (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, description, quantity, unit_price, amount, sort_order) VALUES (${tid}, ${newId}, ${item.description}, ${item.quantity || 1}, ${item.unit_price}, ${amount}, ${i + 1}) ). Alternatively, derive and assert the tenant from the parent invoice before inserting. Ensure every INSERT into tenant-scoped tables sets tenant_id and every read/update/delete includes tenant_id constraints.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` omits a tenant_id constraint in the WHERE clause (UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false). This allows a row belonging to any tenant to be updated if its id is known.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause. Persist the tenantId into the in-memory PendingReuse entry when the row is inserted and use it here (e.g. WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false). Also validate ownership server-side before updating (or fetch the row by id+tenant_id first) so writes cannot cross tenant boundaries.
- **server/channel-routing.ts:33** — listChannelRoutes JOINs the personas table (JOIN personas p ON p.id = cr.persona_id) without any tenant_id constraint — this can return persona names from other tenants.
  - _Fix:_ Constrain the join by tenant (e.g. AND p.tenant_id = $tenantId and cr.tenant_id = $tenantId) or otherwise verify that each persona_id belongs to the caller's tenant before returning persona data. Prefer adding tenant_id to channel_routes and filtering both tables by tenant_id.
- **server/channel-routing.ts:54** — setChannelRoute INSERTs a persona_id supplied by the caller into channel_routes without validating that the persona belongs to the caller's tenant (and without storing a tenant_id). This lets a caller point to personas owned by other tenants.
  - _Fix:_ Require the caller's tenantId be passed to setChannelRoute, validate ownership (e.g. SELECT 1 FROM personas WHERE id = $personaId AND tenant_id = $tenantId) before inserting/updating, and include tenant_id in the inserted row. Alternatively, reject personaIds that don't belong to the tenant.
- **server/channel-routing.ts:63** — getPersonaForChannel reads persona_id from channel_routes WHERE channel = ${channel} without any tenant constraint, so it can return mappings belonging to other tenants (or be manipulated to point to other tenants' personas).
  - _Fix:_ Require tenant context for this call and include WHERE tenant_id = $tenantId in the query. If channel_routes is made tenant-scoped, ensure tenant_id is included in the SELECT/WHERE.
- **server/channel-routing.ts:71** — removeChannelRoute deletes from channel_routes WHERE channel = ${channel} with no tenant constraint; a caller can remove another tenant's mapping if channel_routes is intended to be tenant-scoped.
  - _Fix:_ Include tenant_id in the DELETE WHERE clause (e.g. WHERE channel = $channel AND tenant_id = $tenantId) and require tenant context. If channel_routes must be global, enforce caller authorization and ownership checks before allowing deletes.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` without tenant filter — query: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...`
  - _Fix:_ Add an explicit tenant constraint (e.g. `AND tenant_id = ${tenantId}`) or join through a tenant-scoped parent row in the same query. Alternatively, use a storage helper that enforces tenant scoping before querying project_files.
- **server/chat-engine.ts:584** — SELECT from tenant-scoped table `project_notes` without tenant filter — query: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...`
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `AND tenant_id = ${tenantId}`) or join with projects (ensuring `projects.tenant_id = ${tenantId}`) in the same SQL to guarantee tenant isolation at the DB level.
- **server/chat-engine.ts:633** — SELECT from tenant-scoped table `compaction_archives` without tenant filter — query: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...`
  - _Fix:_ Restrict the query by tenant (e.g. `AND tenant_id = ${tenantId}`) or join compaction_archives to conversations/projects while checking `tenant_id = ${tenantId}` so the DB enforces tenant isolation.
- **server/chat-engine.ts:651** — SELECT from tenant-scoped table `messages` without tenant filter — query: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...`
  - _Fix:_ Add an explicit tenant predicate (e.g. `AND tenant_id = ${tenantId}`) or perform a JOIN to conversations that checks `conversations.tenant_id = ${tenantId}` in the same SQL statement to avoid relying on prior in-memory verification.
- **server/chat-engine.ts:832** — Subquery counts tenant-scoped table `project_files` without tenant filter — `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)` can return cross-tenant counts
  - _Fix:_ Constrain the subquery by tenant (e.g. `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tid})`) or rewrite the subquery to join on project/project_files with an explicit tenant predicate so counts are scoped to the caller's tenant.
- **server/chat-engine.ts:4407** — Call to storage.attachCitations(...) is made without passing tenant context. If attachCitations writes to a tenant-scoped table it may perform INSERT/UPDATE without an explicit tenant_id, violating tenant isolation (or it may operate on the wrong tenant's rows).
  - _Fix:_ Pass the explicit tenant id/tenant context to attachCitations (e.g. storage.attachCitations(messageId, citations, { tenantId: tenantIdForScope }) or add tenantId parameter). Additionally, before attaching citations verify the assistantMsgRow.id actually belongs to the caller's tenant (ownership check) or ensure storage.attachCitations enforces and requires tenant scoping internally.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` performed without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can unclaim/modify a row across tenants by id.
  - _Fix:_ Include the tenant guard in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenantId}), and validate/derive tenantId from the claimed row before running the UPDATE. Prefer using the same tenant-scoped checks used elsewhere in this module and fail-closed if tenant context is missing.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` (SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...) has NO tenant_id constraint — it pulls overdue commitments across all tenants.
  - _Fix:_ Run this query scoped per-tenant (WHERE tenant_id = ${tenantId}) or restrict the function to a properly authorized platform-admin flow and ensure the admin contract is explicit. If platform-wide aggregation is needed, ensure the caller is an operator-admin and that sensitive fields are redacted and access controlled.
- **server/credential-vault.ts:169** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} updates a tenant-scoped table row by id only, without constraining by tenant_id — allows touching other tenants' rows by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), or perform the update via the already-validated getCredential() result and use its tenantId to guard the UPDATE. Fail closed if tenant context is missing.
- **server/credential-vault.ts:176** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} (in getLoginCredentials) updates last_used_at without a tenant_id constraint, despite having tenant context — this can modify another tenant's row if ids are guessed.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${cred.id} AND tenant_id = ${tenantId}) after validating the credential belongs to the requesting tenant. Use parameterized tenantId rather than unscoped id-only updates.
- **server/data-protection.ts:150** — SELECT against tenant-scoped table `conversations` with no tenant_id predicate: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` reads rows across all tenants.
  - _Fix:_ Don’t scan the global conversations table without scoping. Either iterate per-tenant and include `WHERE tenant_id = <tenant>` or perform the work inside a tenant-scoped transaction (e.g. withTenantTx) / use a dedicated admin flow that explicitly and safely enumerates tenants. At minimum, add an explicit tenant predicate or otherwise ensure the read is performed under an audited, intentional cross-tenant maintenance path.
- **server/delivery-pipeline.ts:203** — db.update(deliveryLogs).set(...) is executed without scoping the UPDATE to the delivery's tenant (no tenant_id constraint in the WHERE clause). This allows modifying another tenant's delivery row.
  - _Fix:_ Require and apply the tenant context on updateDeliveryLog (e.g. accept a tenantId argument and use .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))) or otherwise validate ownership before updating).
- **server/delivery-pipeline.ts:1071** — getDeliveryByStripePayment performs SELECT FROM delivery_logs WHERE stripe_payment_id = $1 with no tenant_id filter. This can expose another tenant's delivery data by payment id.
  - _Fix:_ Require tenant context (tenantId param) or scope the query to the caller's tenant: .where(and(eq(deliveryLogs.stripePaymentId, paymentId), eq(deliveryLogs.tenantId, tenantId))). If intent is an admin-only lookup, enforce that at the callsite and make it explicit in the API signature/documentation.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken selects from dfy_intake_forms WHERE token = $1 with no tenant_id constraint. Reading by token without tenant scoping can leak other tenants' intake forms.
  - _Fix:_ Either store/derive a tenant-bound token or require a tenantId argument and scope the SELECT with AND tenant_id = ${tenantId}. If tokens are intended global, ensure they are cryptographically tied to tenant ownership and validate that before returning rows.
- **server/dfy-intake.ts:93** — submitDfyIntake updates dfy_intake_forms via .where(eq(dfyIntakeForms.id, form.id)) with no tenant_id constraint. This UPDATE can modify another tenant's row if IDs collide or caller manipulates form.id.
  - _Fix:_ Include tenant scoping in the UPDATE: .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId))). Prefer passing tenantId explicitly and verify ownership before writing.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} without AND tenant_id = ${tenantId}. This can delete chunks belonging to other tenants sharing doc_path/collection_id naming collisions.
  - _Fix:_ Include tenant scoping on destructive operations: add AND tenant_id = ${tenantId} to the DELETE. Ensure every DELETE/UPDATE on tenant-scoped tables includes eq(<table>.tenant_id, tenantId) or verifies collection ownership first.
- **server/embeddings.ts:716** — SELECT from memory_entries (backfillEmbeddingVecs) lacks tenant scoping: SELECT id, embedding FROM memory_entries ... LIMIT 200 — no tenant_id filter. This reads embeddings across all tenants.
  - _Fix:_ Scope the backfill to a single tenant at a time (accept a tenantId param) and add WHERE tenant_id = ${tenantId} to the SELECT. If this must run as a system job, explicitly document and enforce an ADMIN/maintenance tenant constant and protect the path carefully.
- **server/embeddings.ts:726** — UPDATE memory_entries in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant constraint — cross-tenant update risk.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause (or lookup the tenant for the id and verify it matches the expected tenant) before updating embedding_vec.
- **server/embeddings.ts:732** — SELECT from agent_knowledge (backfillEmbeddingVecs) lacks tenant scoping: SELECT id, embedding FROM agent_knowledge ... LIMIT 200 — no tenant_id filter. This reads rows across tenants.
  - _Fix:_ Scope the backfill to a tenant (accept tenantId) and add WHERE tenant_id = ${tenantId} to the SELECT. If intended to scan global/autoresearch-only rows, explicitly restrict to an ADMIN tenant constant and document that behavior.
- **server/embeddings.ts:743** — UPDATE agent_knowledge in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant constraint — cross-tenant update risk.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause (or verify ownership) to prevent updating other tenants' rows by id.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint — write is not scoped to the caller's tenant.
  - _Fix:_ Include tenant scoping on the UPDATE to ensure only the owning tenant's row is modified, e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}. Do the same for all writes that reference rows created/owned by a tenant.
- **server/felix-loop.ts:690** — Error-recovery UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the recovery UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) so an error logged for one tenant cannot modify another tenant's run row.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) but no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or use the previously-verified project.tenantId when building the WHERE clause to ensure you only update the intended tenant's project row.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images does not set or validate tenant context. The INSERT uses session_id and other values but does not include tenant_id (nor does it verify the session belongs to the calling tenant), allowing cross-tenant data writes if session_id is abused.
  - _Fix:_ Include the tenant_id column in the INSERT (e.g. VALUES (..., ${tenantId}, ...)) and/or validate ownership before inserting (SELECT presenter_sessions WHERE id=${sessionId} AND tenant_id=${tenantId} and fail if not matched). Ensure any ON CONFLICT upsert also preserves tenant scoping (e.g. include tenant_id in conflict key or add tenant_id filter in subsequent updates).
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions ... WHERE id = ${sessionId} performs an update without constraining by tenant_id. This updates a session row by id only and can modify another tenant's session/metadata if sessionId is not tenant-bound/validated.
  - _Fix:_ Constrain the UPDATE by tenant as well: UPDATE presenter_sessions SET ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, first SELECT the session by id and tenant_id to verify ownership before making updates, and fail if the session doesn't belong to the caller's tenant.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks (archiveStaleProposals) has no tenant_id constraint. If heartbeat_tasks is tenant-scoped this will discover tasks across tenants and treat them as stale regardless of tenant context.
  - _Fix:_ Scope the SELECT to the intended tenant (WHERE tenant_id = ${tenantId}) or require explicit system/admin intent. If tasks can be global, make that explicit and ensure proper authorization and auditing; otherwise run per-tenant queries only.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id only, with no tenant constraint. This can disable tasks belonging to other tenants.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform a safe per-tenant disable using tenant-scoped queries. Ensure only tasks belonging to the intended tenant are modified, and validate ownership before mutating.
- **server/heartbeat.ts:53** — SELECT from tenant-scoped table research_schedules without a tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. Alternatively use a storage helper that accepts a tenant param (or assertTenantContext) so you never issue an unscoped SELECT against a tenant table.
- **server/heartbeat.ts:102** — UPDATE on tenant-scoped table research_schedules uses only id in WHERE clause (UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id}) — no tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage helper/assertTenantContext to ensure the update is scoped to the intended tenant.
- **server/heartbeat.ts:108** — UPDATE on tenant-scoped table research_schedules uses only id in WHERE clause (UPDATE research_schedules SET last_run_at = ..., next_run_at = ... WHERE id = ${sched.id}) — no tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage wrapper that enforces tenant scoping.
- **server/heartbeat.ts:121** — UPDATE on tenant-scoped table research_schedules uses only id in WHERE clause (UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id}) — no tenant_id constraint in recovery path.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use the storage layer/helper which enforces tenant scoping to avoid touching other tenants' rows.
- **server/heartbeat.ts:385** — SELECT from tenant-scoped table event_log without a tenant_id constraint (SELECT * FROM event_log WHERE status = 'pending' ...). This reads pending events across all tenants.
  - _Fix:_ Query pending events per-tenant (add WHERE tenant_id = ${tenantId}) or use a storage helper that accepts a tenant id or an explicit ALL_TENANTS-aware function. If the tick must process multi-tenant events, do it via a controlled per-tenant loop that asserts context for each tenant.
- **server/heartbeat.ts:273** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — updating a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Use the storage API (e.g. storage.updateHeartbeatTask) or add AND tenant_id = ${t.tenantId} to the WHERE clause / assertTenantContext before issuing the update so you cannot modify another tenant's heartbeat row.
- **server/heartbeat.ts:277** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — tenant_id not constrained in WHERE clause.
  - _Fix:_ Scope the update by tenant_id (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or invoke the storage-layer update function that enforces tenant ownership to avoid cross-tenant modification.
- **server/heartbeat.ts:2764** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — write to tenant-scoped heartbeat_tasks without tenant_id in WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call storage.updateHeartbeatTask/newTask helper which applies the tenant constraint. Avoid raw UPDATEs on tenant tables without tenant scoping.
- **server/heartbeat.ts:3216** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — tenant_id not included in WHERE clause (possible cross-tenant write).
  - _Fix:_ Use a tenant-scoped update (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage abstraction (storage.updateHeartbeatTask) which enforces tenant ownership to prevent modifying another tenant's task.
- **server/job-queue.ts:134** — reclaimExpiredLeases STEP 1 updates agent_jobs to failed_terminal without any tenant_id constraint — can mark jobs from any tenant terminal in bulk.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE tenant_id = ${tenantId} AND ...), or perform per-tenant reclamation loops. If bulk/global reclamation is required, add explicit justification/logging and ensure operator approval and auditing to avoid accidental cross-tenant state changes.
- **server/job-queue.ts:148** — reclaimExpiredLeases STEP 2 requeues expired 'running' jobs (UPDATE ... SET status='pending') without tenant_id in the WHERE clause — affects jobs across all tenants.
  - _Fix:_ Scope the UPDATE by tenant_id or rework to claim/requeue per-tenant. If global behavior is intentional, add explicit safeguards: only run under a trusted system context, log affected tenant_ids, or partition agent_jobs by tenant to avoid cross-tenant updates.
- **server/job-queue.ts:177** — completeJob updates agent_jobs by id/status/attempts without verifying tenant_id — a write against a tenant-scoped table lacks tenant scoping.
  - _Fix:_ Add tenant fencing to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or require the caller to pass job.tenantId and assert it matches before updating. Alternatively, perform a prior SELECT that confirms tenant ownership and include tenant_id in the UPDATE to enforce isolation.
- **server/job-queue.ts:220** — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} without tenant scoping, then later updates the job row without tenant filtering — unscoped read/write on a tenant-scoped table.
  - _Fix:_ When operating on a tenant-scoped job, include tenant_id in the SELECT/UPDATE WHERE clause (or pass and verify tenantId). Enforce lease-fencing + tenant fencing together (e.g. WHERE id=${id} AND tenant_id=${tenantId} AND attempts=${attempts}).
- **server/job-queue.ts:237** — failJob's UPDATE to mark terminal failure writes agent_jobs without tenant_id constraint in WHERE clause.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}). If the caller doesn't have tenant context, require it or perform a safe lookup to bind the tenant before mutating rows.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id check — allows cancelling another tenant's job by id.
  - _Fix:_ Include tenant_id in the WHERE or require the caller to provide and validate tenantId prior to performing the UPDATE (e.g. WHERE id=${id} AND tenant_id=${tenantId}).
- **server/job-queue.ts:282** — retryJob resets job state via UPDATE ... WHERE id = ${id} without tenant_id constraint — mutates tenant-scoped rows unscoped by tenant.
  - _Fix:_ Require tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or perform an ownership check before the UPDATE. Prefer parameterized tenant scoping rather than string interpolation.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on tenant-scoped table `procedure_evolution_runs` uses WHERE(eq(procedureEvolutionRuns.id, runId)) with NO tenant_id predicate — write could affect another tenant's row if IDs collide or be abused.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or otherwise bind/validate the tenantId for this run before issuing the UPDATE. Ensure every UPDATE/SELECT/DELETE on procedure_evolution_runs includes eq(tenant_id, callerTenantId).
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on tenant-scoped table `procedure_evolution_runs` (noop_proposal path) filters only by id (runId) and omits tenant_id — violates tenant isolation guarantee.
  - _Fix:_ Add tenant scoping to the WHERE (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))). Use the same tenantId value that was used when inserting the run row to avoid cross-tenant updates.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE on tenant-scoped table `procedure_evolution_runs` sets status/done but filters only by id (runId) and omits tenant_id — potential cross-tenant write.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))). If tenantId is not in scope, capture it when creating the run row and thread it through to these update sites.
- **server/lib/agent-trace.ts:124** — persistSpanClose issues UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} with NO tenant_id constraint. agent_trace_spans is tenant-scoped and the close update can therefore affect spans across tenants.
  - _Fix:_ Include tenant_id in the UPDATE predicate. For example, store tenantId when the span is opened (or make it available in the open promise/map) and use .where(and(eq(agent_trace_spans.span_id, spanId), eq(agent_trace_spans.tenant_id, tenantId))). Do not update rows by span_id alone.
- **server/lib/bwb-job-progress.ts:141** — UPDATE on tenant-scoped table `video_jobs` uses where(scope(jobId,...)) but scope() may omit the tenant predicate when BWB_TENANT_ID is not present, resulting in writes that lack tenant_id filtering.
  - _Fix:_ Require and validate tenantId for these progress writes (fail closed or return) or ensure envTenantId always returns a valid tenant and include eq(videoJobs.tenantId, tenantId) in the WHERE. Do not rely on jobId alone as a substitute for tenant scoping.
- **server/lib/bwb-job-progress.ts:166** — UPDATE on tenant-scoped table `video_jobs` (updateBwbChapters) calls .where(scope(jobId,...)) which can omit tenant_id when the env/threaded tenant is missing — write may cross tenant rows.
  - _Fix:_ Pass an explicit tenantId to this function or require BWB_TENANT_ID; then include eq(videoJobs.tenantId, tenantId) in the WHERE. Guarantee every UPDATE/SELECT/DELETE on video_jobs includes tenant scoping.
- **server/lib/bwb-job-progress.ts:200** — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope() may omit tenant_id if the env-threaded tenant is missing — read can leak another tenant's job data.
  - _Fix:_ Include tenant filtering in the SELECT (e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId)))) or require and validate the tenantId before selecting. Do not select tenant-scoped rows by jobId alone.
- **server/lib/bwb-job-progress.ts:211** — UPDATE that marks job done performs .where(scope(jobId)) — scope() may omit tenant_id and thus permit cross-tenant updates when the threaded tenant is absent.
  - _Fix:_ Ensure the WHERE includes the tenant predicate (eq(videoJobs.tenantId, tenantId)). Prefer making tenantId an explicit parameter to completeBwbJob and fail if it's not provided, rather than relying on environment threading.
- **server/lib/bwb-job-progress.ts:227** — UPDATE (bumpBwbHeartbeat) uses .where(scope(jobId,...)) which can omit tenant_id when BWB_TENANT_ID is not available — heartbeats may update rows for the wrong tenant.
  - _Fix:_ Require an explicit tenantId or validate env BWB_TENANT_ID and include eq(videoJobs.tenantId, tenantId) in the WHERE. Avoid performing tenant-scoped writes without an explicit tenant predicate.
- **server/lib/bwb-job-progress.ts:245** — UPDATE (failBwbJob) uses .where(scope(jobId, { notDone: true })) — scope may not include tenant_id, allowing cross-tenant writes/failed-state flips if the env tenant is missing.
  - _Fix:_ Make tenantId explicit or assert its presence and add eq(videoJobs.tenantId, tenantId) to the WHERE. Never perform UPDATE/DELETE/SELECT on tenant-scoped tables without a tenant_id filter.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT against tenant-scoped table `projects` without a tenant_id constraint (reads rows for all tenants). The query `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id` can enumerate / aggregate other tenants' projects.
  - _Fix:_ Restrict the query to only the tenants you intend to consider. For example, compute the list of snapshot tenant IDs and pass them as a parameter, then use WHERE tenant_id = ANY(${tenantIdArray}::int[]) or run per-tenant counts inside a loop: `SELECT count(*) FROM projects WHERE tenant_id = ${tenantId} AND tags && ${IDEA_TAG_LITERAL}::text[]`. Avoid global scans on tenant-scoped tables unless explicitly required by an admin job and protected by additional access controls; prefer scoped queries that only touch the caller's tenant.
- **server/lib/stuck-task-sweep.ts:56** — SELECT on tenant-scoped table `agent_runs` (lines 56–64) omits any tenant_id filter. This read returns/inspects rows across all tenants.
  - _Fix:_ If this is intended as a cross-tenant operational audit, document and gate it appropriately; otherwise add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or scan per-tenant in a controlled loop so reads are constrained to the caller's tenant.
- **server/lib/stuck-task-sweep.ts:84** — SELECT on tenant-scoped table `mind_tickets` (lines 84–92) omits any tenant_id filter. This read returns/inspects rows across all tenants.
  - _Fix:_ Same as for agent_runs: either restrict the query to a specific tenant_id, or perform per-tenant scans in an authorized admin context with explicit auditing. Add WHERE tenant_id = ${tenantId} when used in a tenant-scoped context.
- **server/lib/system-probes.ts:163** — Database query counting messages (line 163) does a db.select(count(*)) FROM messagesTable with no tenant filter, returning a global message count across tenants.
  - _Fix:_ If the probe is intended to be tenant-scoped, add WHERE tenant_id = ${tenantId}. If it is intentionally global (admin probe), ensure the admin/global intent is explicit and authorized; otherwise perform per-tenant counts or supply an ALL_TENANTS-aware aggregation that preserves tenant isolation guarantees.
- **server/martech-bundle.ts:325** — UPDATE on tenantVoiceProfiles uses WHERE id = existing.id but does NOT constrain by tenant_id. This is an update to a tenant-scoped table with no tenant_id in the WHERE clause.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) or re-query/validate the row ownership before updating to ensure the update is scoped to the caller's tenant.
- **server/memory-graph.ts:183** — refreshCategoryCount updates memory_categories using a subquery that counts memory_entries WITHOUT filtering by tenant_id, and the outer UPDATE does not constrain the memory_categories row by tenant. This can mix counts across tenants and update rows belonging to other tenants.
  - _Fix:_ Pass tenantId into refreshCategoryCount and add tenant scoping to both the inner count and outer WHERE. Example: SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ... WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:124** — In classifyMemoryCategory (new-category branch) the code executes UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} without constraining by tenant_id. This writes to a tenant-scoped table without tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or accept/propagate tenantId into this function so the update is performed only for the correct tenant.
- **server/memory-graph.ts:494** — categorizeExistingMemories calls UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} with no tenant_id constraint. This mutates a tenant-scoped table without verifying the row belongs to the caller tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or re-check ownership before the update to ensure the change is limited to the correct tenant.
- **server/memory-intelligence.ts:273** — After creating a memory entry the code runs UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} with no tenant_id constraint. This updates a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include tenant filtering on the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or have the storage API perform the update under the correct tenant context.
- **server/memory-intelligence.ts:316** — In the 'update' flow the code runs UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (after creating a replacement entry) with no tenant_id constraint. This mutates a tenant-scoped table without tenant scoping.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage helper that enforces tenant_id on all writes.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not include tenant_id in VALUES (project_files likely tenant-scoped and app relies on tenant_id column to enforce isolation).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. add tenant_id column and pass options.tenantId) or ensure the table is intentionally global. Also keep the existing assertProjectInTenant check (good) but still write tenant_id in the row: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...) .
- **server/mpeg-engine.ts:1416** — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id in VALUES. Even though assertProjectInTenant is used, the row is written without tenant context.
  - _Fix:_ Same fix as above: write tenant_id explicitly (INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Do not rely solely on assertProjectInTenant — always persist tenant_id on tenant-scoped tables.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(lockKey) inside a parameterized query: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). lockKey is derived from caller-provided ledgerKey/tenantId and is injected raw into SQL via sql.raw — this is a SQL-injection / semantics risk when ledgerKey is request-controlled.
  - _Fix:_ Avoid sql.raw with request-derived values. Bind the numeric lock value as a parameter instead of inlining raw SQL. E.g. compute a BigInt and call tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), or better: pass the numeric value as a bound parameter so the DB client handles escaping/typing. Validate/normalize ledgerKey server-side before using it.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table `oauth_subscriptions` with no tenant_id constraint (startup validation queries SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This scans rows across all tenants and may expose multi-tenant sensitive state.
  - _Fix:_ If the operation must run across tenants (admin/maintenance), enforce that this code is admin-only and audit/log its use. Otherwise, restrict queries to a specific tenant_id (WHERE tenant_id = ${tenantId}). Avoid returning sensitive columns (refresh_token/access_token) unless absolutely necessary and ensure results are handled securely.
- **server/oauth-subscriptions.ts:915** — SELECT FROM tenant-scoped table `oauth_subscriptions` with no tenant filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace').
  - _Fix:_ Restrict to specific tenant(s) where appropriate (WHERE tenant_id = ${tenantId}), or require an explicit admin-only code path for global scans. Minimize the set of columns returned and avoid pulling secrets in bulk.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh performs a global SELECT on oauth_subscriptions (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) with no tenant_id constraint — it iterates over all tenants' subscriptions.
  - _Fix:_ If this is intended as a system-wide maintenance operation, make that explicit (admin-only) and avoid selecting sensitive token columns. Otherwise, update the function to accept/require a tenantId and add WHERE tenant_id = ${tenantId} to the query. Consider paginating and limiting data returned and avoid decrypting or returning secrets in bulk.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions queries oauth_subscriptions without tenant filter (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE), iterating across all tenants.
  - _Fix:_ Same guidance: require explicit admin privileges for cross-tenant maintenance paths or change the API to operate per-tenant (accept tenantId) and add WHERE tenant_id = ${tenantId}. Add auditing and rate limiting for global refresh operations.
- **server/plan-executor.ts:778** — resumeStuckPlans performs a global UPDATE on plans (SET status='approved' WHERE status='executing' AND updated_at < ...) with no tenant_id filter — affects all tenants' plans.
  - _Fix:_ Restrict recovery to a tenant context or iterate tenants and restrict UPDATE to each tenant (e.g. WHERE status='executing' AND tenant_id = ${tenantId} AND updated_at < ...). If this is intended to be global admin behaviour, make that explicit and gate it behind a privileged admin flow, but still prefer explicit tenant scoping for per-tenant operations.
- **server/plan-executor.ts:788** — SELECT id FROM plans WHERE status = 'approved' ... has no tenant_id filter — reading pending plans across all tenants.
  - _Fix:_ Add tenant scoping (WHERE status='approved' AND tenant_id = ${tenantId}) or otherwise iterate per-tenant. If the function is supposed to kick plans globally, ensure it's an explicit admin-only operation and document/gate it.
- **server/process-governor.ts:198** — SELECT AVG(duration_ms) FROM heartbeat_logs aggregates heartbeat logs without tenant scope (no tenant_id in WHERE) — cross-tenant read of telemetry/PII.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so query averages only the requesting tenant's heartbeat logs.
- **server/process-governor.ts:240** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... lacks tenant_id filter — counts failing agents across all tenants.
  - _Fix:_ Restrict the query with AND tenant_id = ${tenantId} to scope the metric to the tenant of interest.
- **server/process-governor.ts:641** — UPDATE agent_desks SET status_note = ... WHERE id = ${desk.id} omits tenant_id constraint — mutates agent_desks across tenants.
  - _Fix:_ Add tenant scoping: WHERE id = ${desk.id} AND tenant_id = ${tenantId}. Ensure tenantId is used consistently when updating agent_desks entries.
- **server/process-governor.ts:976** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant_id constraint — increments trigger state without verifying tenant ownership.
  - _Fix:_ Use WHERE id = ${rule.id} AND tenant_id = ${tenantId} to ensure the rule being updated belongs to the tenant being evaluated.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant_id filter — reads project metadata cross-tenant.
  - _Fix:_ Require and pass tenantId into updateProjectBrain (or verify project.tenant_id via an initial, tenant-scoped query). Use WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/project-brain.ts:332** — Backfill: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... does not filter by tenant_id — iterates projects across tenants.
  - _Fix:_ Restrict the query to a tenant (e.g. WHERE p.tenant_id = ${tenantId}) or otherwise iterate per-tenant. Make tenant context explicit for backfill operations.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks tenant_id scoping — can enumerate conversations across tenants/projects.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or JOIN via project tenant) so conversations are limited to the owning tenant.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ORDER BY created_at ASC omits tenant_id filter — reads message content cross-tenant.
  - _Fix:_ Constrain messages by tenant_id (WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or derive tenant via the conversation/project and enforce it before selecting messages.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` executed without a tenant_id constraint (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This reads rows across all tenants.
  - _Fix:_ Avoid a global unbounded scan of a tenant-scoped table. Either (a) iterate tenants and run a per-tenant query with WHERE tenant_id = <tenantId>, or (b) require and pass an explicit system/global guard flag and document it, or (c) call the existing listScheduledMessages(...) helper with the explicit _allowGlobalSystemUseOnly path. At minimum ensure the caller is an authorized global system actor and make that explicit in the code and audit logs.
- **server/research-engine.ts:331** — Missing tenant guard on personas read: SELECT name FROM personas WHERE id = ${program.persona_id} (persona lookup does not constrain by tenant_id). This can return persona data from other tenants.
  - _Fix:_ Constrain persona lookups by tenant_id (e.g. WHERE id = ${program.persona_id} AND tenant_id = ${tenantId}), or use a tenant-aware helper (assertPersonaInTenant) to validate ownership before reading.
- **server/research-engine.ts:1053** — resolvePersonaId ignores tenant context: SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 — the _tenantId parameter is unused. This can resolve a persona from another tenant and enable cross-tenant reference/placement of persona_id values.
  - _Fix:_ Use the provided tenant id in the query (e.g. WHERE name = ${personaSlug} AND tenant_id = ${_tenantId} LIMIT 1). If personas are intentionally global, document that explicitly; otherwise enforce tenant scoping and/or add an ownership assertion helper.
- **server/research-engine.ts:480** — INSERT into project_notes without tenant_id: INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) — if project_notes is a tenant-scoped table (tenant_id NOT NULL), the INSERT omits tenant_id, enabling mis-attribution or constraint violations.
  - _Fix:_ Include tenant_id in project_notes INSERTs (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${tenantId}, ...)). If project_notes relies on project_id for scoping, query/derive the project owner (or use assertProjectInTenant) and explicitly set tenant_id in the row.
- **server/research-engine.ts:488** — Second INSERT into project_notes also omits tenant_id (similar to the previous INSERT).
  - _Fix:_ Same fix: explicitly set tenant_id when inserting project_notes, or derive and assert the project's tenant and include tenant_id in the VALUES list.
- **server/routes.ts:1118** — Uploads middleware selects all file_storage rows matching filename without restricting by tenant_id (db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This fetches tenant-scoped rows across tenants and relies on app-side filtering to decide ownership.
  - _Fix:_ Avoid selecting other tenants' rows. Query only rows for the authenticated/signed tenant (e.g. WHERE filename = X AND tenant_id = authedTenantId). If you need an existence check for other tenants to return a generic 404, perform a boolean exists check that does not return other tenants' identifying data, or restructure logic so no cross-tenant SELECT is necessary.
- **server/routes.ts:2059** — Presenter public GET looks up presenter_sessions by token using an unscoped SELECT (no tenant_id WHERE). This reads a tenant-scoped table without constraining tenant context; caller-supplied token is used to retrieve the row directly.
  - _Fix:_ Bind the capability token to a tenant at verification time (e.g. include tenant in the signed token or require a correlated signed parameter) and then query with WHERE token = ${token} AND tenant_id = ${boundTenantId}. Alternatively, ensure the token lookup is performed in an admin/trusted internal path that cannot be invoked by arbitrary callers.
- **server/routes.ts:2091** — Presenter slide image lookup fetches presenter_slide_images by session_id derived from a prior unscoped presenter_sessions lookup. The initial presenter_sessions lookup did not constrain tenant_id, so this chain reads tenant-scoped data without an explicit tenant filter.
  - _Fix:_ Ensure presenter_sessions lookup is performed with an explicit tenant binding (see previous finding) or otherwise validate that the caller is authorized for the tenantId returned before using session_id in subsequent tenant-scoped reads. Prefer querying presenter_slide_images with both session_id and tenant_id when possible.
- **server/routes.ts:2174** — Presenter repair-images endpoint selects presenter_sessions WHERE token = ${token} without constraining by tenant_id. Although the route validates an internal key, it still performs an unscoped read on a tenant-scoped table.
  - _Fix:_ Bind/verify token to tenant context before querying, or include tenant_id in the WHERE clause (e.g. WHERE token = ${token} AND tenant_id = ${tenantId}). If the internal key must allow cross-tenant admin actions, run this code only in a dedicated admin-only maintenance path with strict auditing.
- **server/routes.ts:2315** — Public order lookup queries delivery_logs by order/session id using WHERE orderId = ${sessionId} OR stripePaymentId = ${sessionId} with no tenant_id constraint. This reads tenant-scoped delivery logs across tenants based solely on a session identifier.
  - _Fix:_ If the API is intended as a capability-based lookup (unguessable session id), ensure the session identifier itself is a cryptographically bound capability and that any returned row is verified against that capability. Alternatively, require the caller to present an associated tenant context (or a signed capability that includes tenant), and add WHERE tenant_id = ${tenantId} to the query to avoid unscoped reads across tenants.
- **server/routes.ts:2423** — Asynchronous order-recovery work queries delivery_logs by lower(customer_email) = ${rawEmail} without a tenant filter, reading tenant-scoped rows across tenants while processing the public order-lookup flow.
  - _Fix:_ Avoid unscoped scans across tenant-scoped delivery logs. If you must look up by email, ensure this background task runs under an admin-only context with explicit authorization, or restrict results to the tenant that initiated the request via a signed capability. Consider alternative designs that prevent cross-tenant reads (e.g. require the unguessable sessionId in the initial request to drive lookups).
- **server/routes.ts:2567** — Order verify flow selects delivery_logs by lower(customer_email) = ${rawEmail} (no tenant filter) and returns a tenant's orders after consuming a one-time code. This reads tenant-scoped rows without scoping by tenant_id.
  - _Fix:_ Constrain the query by tenant context, or ensure the one-time code is bound to a tenant (store tenant_id with the order-lookup code) so the verify step can safely query WHERE lower(customer_email)=${rawEmail} AND tenant_id=${boundTenantId}. Alternatively, change the flow to only return orders that match a signed/unguessable capability tied to a specific tenant.
- **server/routes.ts:4334** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The raw SQL inserts project_id, note, author but omits tenant_id which breaks the invariant that every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT and ensure it is the caller's tenant (or validate the project belongs to the caller's tenant before inserting). For example, change the query to INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}) or call a storage-layer helper like storage.createProjectNote({ tenantId, projectId: projId, note: snapshot.slice(0,5000), author: 'system:context-guard' }) after verifying ownership of projId.
- **server/routes/conversations.ts:100** — SELECT from project_conversations omits tenant scope (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}). A plain SELECT could expose cross-tenant metadata or cause subsequent logic to act on the wrong tenant's rows.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId} or tenant_id = ${conv.tenantId}). Apply the same constraint to any SELECT against tenant-scoped tables.
- **server/routes/conversations.ts:210** — SELECT joining conversations c does not include an explicit tenant_id constraint on the conversations table (SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}). While project tenant is checked, the query lacks c.tenant_id and therefore reads a conversation row without SQL-level tenant scoping.
  - _Fix:_ Add c.tenant_id = ${tenantId} (or use conv.tenantId) to the WHERE clause so the SQL read is tenant-scoped: ... WHERE c.id = ${id} AND c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL AND p.tenant_id = ${tenantId}.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} has no tenant_id constraint — allows modifying another tenant's row if id is misused.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}. Prefer using the same tenant predicate used in the prior SELECT to avoid TOCTOU cross-tenant writes.
- **server/routes/memory.ts:162** — SELECT memory_entries ... WHERE id IN (...) AND tenantClause uses `sql`TRUE` for ADMIN_TENANT_ID — effectively removes the tenant_id constraint for admin sessions and issues an unscoped read across tenant-scoped `memory_entries`.
  - _Fix:_ Avoid emitting a raw TRUE tenant clause. If admin must see all tenants, make that explicit and guarded by a strict platform-admin check; otherwise require eq(memory_entries.tenant_id, ${tenantId}). If intent is platform-admin cross-tenant access, ensure the callsite has a defensible, auditable platform-admin gate and consider projecting only safe fields or adding explicit admin-only auditing.
- **server/routes/metrics.ts:63** — Multiple queries against tenant-scoped tables (agent_cost_ledger, repair_incidents, etc.) lack a tenant_id constraint — these SELECTs aggregate or list data across all tenants (e.g. cost per tenant, tool calls, tokens) without per-request tenant scoping.
  - _Fix:_ Either (a) restrict the metrics endpoint to per-tenant metrics by adding WHERE tenant_id = ${tenantId} (and require session-auth), or (b) if global metrics are required, keep them strictly behind a strong operator-only gate but make that explicit (e.g. requirePlatformAdmin or equivalent) and ensure the METRICS_TOKEN is treated as high-privilege and rotated/audited. Avoid accidentally exposing per-tenant identifiers to callers without operator-level authorization.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The route verifies project ownership earlier, but the INSERT itself omits the tenant_id column (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant_id explicitly in the INSERT, e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) — or have the storage layer set tenant_id server-side after verifying ownership.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload handler) does not set tenant_id. The code checks the project's tenancy earlier but the INSERT omits tenant_id, violating the rule that every INSERT must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT values, e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (${projectId}, ${f.fileName}, ${'uploads/'+uniqueName}, ${fileUrl || downloadPath}, ${f.mimeType || 'application/octet-stream'}, ${fileBuffer.length}, ${tenantId}). Alternatively, perform the insert via a storage helper that attaches tenantId after ownership verification.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload handler) does not set tenant_id. Although the route checks project ownership, the INSERT itself omits the tenant_id column.
  - _Fix:_ Add tenant_id to the INSERT, e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (${projectId}, ${file.originalname}, ${filePath}, ${fileUrl || downloadPath}, ${file.mimetype}, ${file.size}, ${tenantId}), or centralize the insert in a storage function that attaches tenantId after verifying ownership.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks without tenant_id filter. heartbeat_tasks appears tenant-scoped in other queries; a cross-tenant count may be unintended and subsequent actions operate on the global result.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, add tenant_id to the WHERE (or iterate per-tenant) so counts are per-tenant. If the intent is global/admin-only, document it and scope the operation to an admin tenant explicitly.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true executes without tenant_id constraint and will disable matching tasks across all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant(s) (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or param-driven tenant_id). If a global sweep is intended, make that explicit and ensure only global/shared rows are targeted.
- **server/seed-cleanup.ts:46** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant_id filtering; it aggregates across tenants though later updates disable rows globally.
  - _Fix:_ Add tenant_id to the WHERE or perform the check/update per-tenant. Avoid unconstrained queries/updates on tenant-scoped tables.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant_id constraint and will affect rows across tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or loop per tenant) or otherwise ensure the operation only touches intended tenant rows.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) is executed without tenant scoping; subsequent disable UPDATE also lacks tenant_id and may disable tenant-specific tasks globally.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, include tenant_id in the SELECT and UPDATE predicates or iterate explicitly for each tenant; if the operation must target admin tasks only, scope to ADMIN_TENANT_ID explicitly.
- **server/seed-cleanup.ts:76** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true executes without tenant_id constraint and will modify rows across all tenants.
  - _Fix:_ Add an explicit tenant_id filter (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or param-driven tenant) to prevent cross-tenant modifications. Ensure intended global vs tenant-local semantics are explicit and safe.
- **server/seed.ts:36** — Unscoped read on tenant-scoped table `research_sessions` (SELECT COUNT(*) FROM research_sessions) — no tenant_id filter. This reads across all tenants and can leak global state or make decisions using cross-tenant data.
  - _Fix:_ Restrict the query to a specific tenant. Pass a tenantId into the seed logic (or use the intended tenant constant) and add a WHERE tenant_id = ${tenantId} (or Drizzle .where(eq(research_sessions.tenantId, tenantId))). For example: SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}.
- **server/seed.ts:2158** — Unscoped read on tenant-scoped table `research_programs` (SELECT id, name, model FROM research_programs WHERE is_active = true) — missing tenant_id filter. This iterates over programs from all tenants.
  - _Fix:_ Restrict the SELECT to the caller tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise pass an explicit tenantId and use Drizzle's .where(eq(research_programs.tenantId, tenantId)).
- **server/seed.ts:2515** — Unscoped read on `conversations` (SELECT DISTINCT c.id, c.project_id FROM conversations c ... ) with no tenant_id filter. This iterates over conversations from all tenants and then inserts links into project_conversations, risking cross-tenant linking or leakage.
  - _Fix:_ Constrain the SELECT to the appropriate tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...). Additionally, ensure the link/association table includes tenant_id (or otherwise enforces tenant ownership) and include tenant_id on INSERTs so associations are tenant-isolated.
- **server/seed.ts:3356** — SELECT from tenant-scoped table heartbeat_tasks with no tenant filter (reads all tenants' heartbeat tasks).
  - _Fix:_ Constrain SELECTs to the caller/target tenant (e.g. db.select().from(heartbeatTasks).where(eq(heartbeatTasks.tenantId, tenantId))) to avoid cross-tenant data exposure and to make subsequent inserts/logic tenant-aware.
- **server/self-improvement.ts:170** — Unscoped SELECT against tenant-scoped table `experiments` when tenantId is not provided. detectStagnation builds a conditions array but if tenantId is omitted the query runs without tenant_id in the WHERE clause.
  - _Fix:_ Require an explicit tenantId (throw if missing) or always add eq(experiments.tenantId, tenantId) to the WHERE clause. Do not allow a path that performs db.select().from(experiments) without a tenant filter.
- **server/self-improvement.ts:511** — getExperimentHistory can return experiments across all tenants when tenantId is not supplied (it falls back to db.select().from(experiments) without a tenant filter).
  - _Fix:_ Require tenantId (throw or return empty) or always include eq(experiments.tenantId, tenantId) in the WHERE clause. Avoid unscoped reads of tenant-scoped tables.
- **server/self-improvement.ts:536** — INSERT into `experiments` (logExperiment) uses tenantId: data.tenantId! but does not validate that tenantId is present/owned by the caller. Using a non-validated or missing tenantId can create rows with no/incorrect tenant scoping.
  - _Fix:_ Validate and require tenantId before inserting (throw if missing). Do not rely on a non-null assertion; explicitly check and ensure the caller is authorized for the tenant being written.
- **server/skill-seeker.ts:749** — seekAndLearn defaults tenantId parameter to 1 (admin) if caller omits tenantId. Calls downstream functions that create/modify tenant-scoped rows using that tenantId, potentially causing unintended cross-tenant writes as admin.
  - _Fix:_ Do not default to an admin tenant. Require the caller to explicitly provide a tenantId (or derive it from caller auth). If an operation must run as platform/admin, require an explicit privileged path and strict checks.
- **server/skill-synthesizer.ts:112** — INSERT into tenant-scoped table `agent_knowledge` sets persona_id from caller-supplied input without verifying the persona belongs to the same tenant.
  - _Fix:_ Before inserting, validate that the provided personaId (if present) is owned by the caller's tenant (SELECT id FROM personas WHERE id = $personaId AND tenant_id = $tenantId). If the persona is not owned, reject the request or clear personaId. Alternatively introduce/ call a helper like assertPersonaInTenant(personaId, tenantId).
- **server/stability-watchdog.ts:177** — SELECT from `heartbeat_logs` (recent failure aggregation) has no tenant_id constraint. This reads tenant-scoped logs across all tenants without an explicit sentinel, violating the requirement that tenant-scoped reads be explicitly filtered.
  - _Fix:_ If the watchdog must operate per-tenant, add a WHERE tenant_id = ${tenantId} (and iterate tenants as needed). If it must run across all tenants, make the intent explicit (use the ALL_TENANTS sentinel / a documented admin-only path) and ensure the read is audited and safe. Avoid implicit unscoped reads on tenant-scoped tables.
- **server/stability-watchdog.ts:211** — DELETE FROM `heartbeat_tasks` executed without any tenant_id constraint. This can purge tasks for all tenants unintentionally.
  - _Fix:_ Scope DELETEs to a tenant (WHERE tenant_id = ${tenantId}) or, if this cleanup is intended to be global, make that explicit and gate it with an admin-only, well-audited path. Prefer iterating tenants and running tenant-scoped deletes to avoid accidental cross-tenant data removal.
- **server/stability-watchdog.ts:225** — DELETE FROM `event_log` executed without tenant_id constraint. This will remove processed/failed event_log entries across all tenants silently.
  - _Fix:_ Apply tenant scoping (WHERE tenant_id = ${tenantId}) or explicitly document and gate a global cleanup. If the operation must be global, require an explicit ALL_TENANTS sentinel / admin flag and log/ audit the action before executing.
- **server/stability-watchdog.ts:259** — DELETE FROM `heartbeat_logs` uses only a created_at predicate and no tenant_id filter. This prunes old logs across all tenants without explicit tenant scoping.
  - _Fix:_ Scope the deletion by tenant (WHERE tenant_id = ${tenantId} AND created_at < ...) or explicitly use an audited global cleanup path (ALL_TENANTS) if that is the intention. Prefer per-tenant cleanup loops to avoid accidental cross-tenant deletions.
- **server/storage.ts:242** — getConversationUnscoped() performs an unscoped SELECT on the tenant-scoped conversations table (no tenant_id filter).
  - _Fix:_ Remove or limit use of an unscoped fetcher in general HTTP/handler paths. If an unscoped read is truly required, make callers pass an explicit admin authorization token and/or return the row with clear caveats; better: return a function that returns both row and tenantId and force the caller to assert ownership. At a minimum add an explicit tenant parameter or strong audit + access checks so the call cannot be invoked from normal request handlers.
- **server/storage.ts:298** — attachCitationMetadata() executes raw SQL SELECT against messages without constraining by tenant_id (SELECT id, citations FROM messages WHERE id IN (...) ...).
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or join to a conversation table constrained to the caller's tenant. Alternatively accept and validate a tenantId parameter derived from the already-scoped messages before running the raw query.
- **server/storage.ts:399** — attachCitations() issues UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id constraint, updating tenant-scoped rows by id alone.
  - _Fix:_ Require and validate tenantId for this operation and add WHERE tenant_id = ${tenantId} to the UPDATE, or perform the update via a safer Drizzle query that enforces eq(messages.tenantId, tenantId). Validate that the message belongs to the caller's tenant before updating.
- **server/storage.ts:806** — updateMemoryEmbedding() updates memory_entries by id only (WHERE id = id) without any tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Change API to accept tenantId (or otherwise verify ownership) and add the tenant filter to the WHERE clause (e.g. .where(and(eq(memory_entries.id, id), eq(memory_entries.tenant_id, tenantId))). If this is an admin-wide job, require explicit admin authorization and audit logging.
- **server/storage.ts:818** — updateKnowledgeEmbedding() updates agent_knowledge by id only (WHERE id = id) with no tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Require and validate tenantId (or ensure caller is an authorized admin) and include eq(agent_knowledge.tenant_id, tenantId) in the WHERE clause before updating. Use Drizzle eq() to avoid accidental unscoped writes.
- **server/storage.ts:841** — archiveExpiredMemories() executes a global UPDATE on memory_entries (SET status='archived' WHERE ...expires_at <= NOW()) with no tenant_id constraint — modifies all tenants' rows.
  - _Fix:_ If this is intended to be tenant-scoped, add tenant filtering or requiredTenantScope. If intended as a global maintenance job, require an explicit admin-run path and add logging/auditing; consider iterating tenants and applying the update per-tenant so ownership is explicit.
- **server/storage.ts:857** — archiveStaleMemories() executes a global UPDATE on memory_entries (SET status='archived' WHERE created_at <= ${cutoff} ...) with no tenant_id constraint — affects all tenants.
  - _Fix:_ Same as archiveExpiredMemories: make tenant scoping explicit (per-tenant update) or restrict to an admin-only maintenance task with explicit authorization and logging. Prefer requiredTenantScope to avoid accidental cross-tenant writes.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (reads by name only). This violates tenant isolation — reads must be scoped to the caller's tenant.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Ensure every read from custom_tools includes the tenantId.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id with no tenant_id constraint. Reads by PK alone can cross tenant boundaries if tenant guard is not enforced.
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Prefer passing/validating tenantId rather than relying on id-only selects.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` uses only eq(custom_tools.id, tool.id) in WHERE — missing tenant_id constraint. Updates must be constrained to the tenant to prevent cross-tenant modification.
  - _Fix:_ Constrain the update by tenant as well, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, /*expected tenantId*/))). Validate the tenant before applying updates.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` uses only eq(custom_tools.id, tool.id) in WHERE — missing tenant_id constraint. This can delete rows belonging to another tenant.
  - _Fix:_ Include tenantId in the delete predicate, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, /*expected tenantId*/))). Always verify ownership before deleting.
- **server/tool-learning.ts:365** — UPDATE to increment usageCount on tenant-scoped `custom_tools` uses only the row id in WHERE (eq(custom_tools.id, tool.id)) and lacks a tenant_id constraint.
  - _Fix:_ Include the tenant constraint in the WHERE clause (and(eq(customTools.id, tool.id), eq(customTools.tenantId, /*expected tenantId*/))). Ensure all writes to tenant-scoped tables include and validate tenantId.
- **server/tools.ts:4543** — INSERT into project_notes omits tenant_id. The code inserts a project-scoped row (project_notes) without providing tenant_id in the VALUES clause.
  - _Fix:_ Include the verified tenant id in the INSERT (e.g. VALUES (${projectId}, ${note}, 'system', NOW(), ${tenantId})) or add an explicit tenant_id column in the INSERT. If project_notes is intended to be derivable by project_id only, ensure the schema has no NOT NULL tenant_id; otherwise always persist tenant_id from the verified owner context (and/or enforce assertProjectInTenant prior to insert).
- **server/tools.ts:4547** — INSERT into project_files omits tenant_id. A project_files row is inserted using projectId but no tenant_id value is provided.
  - _Fix:_ Pass the verified tenant id explicitly in the INSERT values (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). If the table is keyed by project only, ensure that is intentional; otherwise enforce including tenant_id on every write and avoid relying solely on project_id for tenant scoping.
- **server/tools.ts:6392** — INSERT into project_files omits tenant_id. After asserting project ownership, code inserts into project_files without specifying tenant_id in the VALUES list.
  - _Fix:_ Even when assertProjectInTenant(project_id, tenantId) has been called, include tenant_id explicitly in the INSERT to satisfy the one-row-per-tenant invariant and prevent future accidental cross-tenant writes. Example: INSERT INTO project_files (project_id,file_name,file_path,file_url,file_type,file_size,uploaded_by,tenant_id) VALUES (..., ${params._tenantId}).
- **server/tools.ts:6925** — INSERT into project_files omits tenant_id. In create_slideshow_video the code inserts a project_files row (after asserting project ownership) but does not set tenant_id in the INSERT.
  - _Fix:_ Add tenant_id to the INSERT using the verified params._tenantId (or otherwise ensure tenant_id is set by the INSERT). Consistently include tenant_id on all inserts into tenant-scoped tables to avoid relying solely on foreign keys for tenant isolation.
- **server/tools.ts:6926** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT statement `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` is missing an explicit tenant identifier, relying solely on project_id ownership being asserted earlier. This violates the invariant that every INSERT into tenant-scoped tables must include the caller's tenant in the row.
  - _Fix:_ Include tenant_id explicitly in the INSERT and derive it from a trusted source. For example, after verifying ownership with assertProjectInTenant, INSERT with tenant_id: `INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ... )`. Prefer using the schema-backed Drizzle insert API (db.insert(projectFiles).values({... , tenantId: params._tenantId, ...})) to avoid column-order mistakes and ensure tenant_id is non-null and originates from the authenticated context (or from the verified project row). Also ensure the tenantId used is the same one validated by assertProjectInTenant.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against tenant-scoped table `browser_workflows` without constraining by tenant_id (sql: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`). This can modify another tenant's workflow entry.
  - _Fix:_ Include the tenant filter in the WHERE clause. Example: `... WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or use the Drizzle .where(eq(table.tenantId, tenantId))). Always ensure every UPDATE/DELETE on tenant-scoped tables includes the caller's tenant_id.
- **server/tools/domains/files/handlers.ts:907** — UPDATE on `projects` table performed without scoping by tenant_id (sql: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`). This can update a project belonging to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant. Example: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(projects.tenantId, tenantId))). Always include tenant_id on UPDATE/DELETE for tenant-scoped tables.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE statement modifies knowledge_triples by id only without constraining tenant_id. The loop at lines ~84-87 executes: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} — this can update rows belonging to other tenants if an attacker supplies an id that matches another tenant's row.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause (and/or verify ownership before update). Example: include AND tenant_id = ${tenantId} (or use Drizzle: .where(eq(table.tenantId, tenantId))). Alternatively, perform a SELECT to confirm the row belongs to tenantId before updating, or perform the UPDATE via the storage API that enforces tenant scoping.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` is executed without constraining to the caller's tenant (no WHERE tenant_id = ${tenantId}). This can expose provider key counts/details across tenants.
  - _Fix:_ Restrict queries to the calling tenant (e.g. add AND tenant_id = ${tenantId} to the WHERE clause) or otherwise enforce and document an explicit admin-only path. If the intent is to permit the admin tenant to enumerate all tenants, make that explicit with an admin-check and audit trail; otherwise scope the query to tenant_id = ${tenantId}.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` is executed without filtering by tenant_id. This reads tenant-scoped provider_keys globally for the caller (currently done under tenantId === 1) and risks cross-tenant exposure.
  - _Fix:_ Either add WHERE tenant_id = ${tenantId} to scope to the current tenant, or require/verify explicit admin privileges and ensure the admin-only intent is audited and safe. Prefer explicit tenant scoping when possible.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE memory_entries SET ... WHERE id = ${id} is executed without also constraining by tenant_id. Although `id` was selected earlier with a tenant_id filter, the UPDATE itself lacks a tenant_id WHERE clause and could update rows outside the current tenant in some race or misuse scenarios.
  - _Fix:_ Add tenant scoping to the UPDATE (for example: WHERE id = ${id} AND tenant_id = ${tid}) so every UPDATE explicitly verifies the row belongs to the caller's tenant.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) is executed without constraining by tenant_id — the UPDATE may affect rows across tenants if ids contain non-owned values.
  - _Fix:_ Constrain the UPDATE by tenant_id as well (for example: WHERE id = ANY(${ids}) AND tenant_id = ${tid}) or perform the update using the tenant-scoped SELECT results (e.g. UPDATE ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}) to ensure only the caller's tenant rows are modified.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET ... WHERE id = ${id} in recordStrategicWin is performed without a tenant_id predicate. The UPDATE may modify rows belonging to other tenants if id is not validated on the UPDATE statement itself.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) to ensure updates can only affect rows owned by the caller's tenant.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id filter. This reads pairing rows across all tenants, violating the invariant that tenant-scoped reads must be constrained to a caller/tenant.
  - _Fix:_ Do not scan agent_knowledge across all tenants. Restrict the query to a specific tenant (e.g. add AND tenant_id = ${tenantId} or use the Drizzle eq(table.tenantId, tenantId) predicate), or move messaging pairings to an operator-owned table/index that you may safely query from this webhook. If cross-tenant discovery is genuinely required, implement an operator-only lookup API (or require an explicit operator credential) rather than scanning tenant-scoped rows.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without constraining by tenant_id. Reading a tenant-scoped `conversations` row by id alone allows cross-tenant data access/verifications without an application-level tenant filter.
  - _Fix:_ Scope the check to the claimed tenant when verifying the pairing. For example: SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 — or use Drizzle: .where(and(eq(conversations.id, info.conversationId), eq(conversations.tenantId, info.tenantId))). This ensures the lookup only returns a row owned by that tenant and prevents cross-tenant reads.
- **server/video-job-runner.ts:236** — recoverStaleVideoJobs performs a global UPDATE on tenant-scoped table `videoJobs` without constraining by tenant_id. This will mark jobs across all tenants as failed instead of scoping to the intended tenant(s).
  - _Fix:_ Scope the UPDATE to a specific tenant (e.g. include eq(videoJobs.tenantId, tenantId) in the WHERE) or iterate tenants and run the stale-job recovery per-tenant. If this is intended as a global operator action, require an explicit admin-only path or configuration and document it — but do not run an unconstrained UPDATE on a tenant-scoped table in normal runtime.
- **server/video-job-runner.ts:206** — isCancelRequested queries `videoJobs` by jobId only (WHERE jobId = ...) with no tenant_id constraint. Reading tenant-scoped cancelRequested without verifying tenant ownership allows cross-tenant observation of job state and may be abused if jobIds are guessable.
  - _Fix:_ Require and pass an authenticated tenantId to isCancelRequested and include a tenant filter in the WHERE clause (e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId))). Alternatively, first resolve the job and verify its tenant ownership before returning cancelRequested.
- **server/wiring-invariants.ts:260** — SELECT on messages (SELECT count(*) FROM messages WHERE created_at > ...) lacks a tenant_id WHERE clause. messages is a tenant-scoped table and this reads across all tenants.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or otherwise restrict the check to an admin-only operation with proper authorization. If the intent is to compute system-wide metrics, make that explicit and ensure only trusted admin code runs it.
- **server/wiring-invariants.ts:366** — Raw SQL built via sql.raw reads COUNT/MAX from subsystem table (`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) ... FROM ${spec.table}`) with no tenant_id constraint. The subsystems listed (e.g. knowledge_nudges, tool_performance) can be tenant-scoped.
  - _Fix:_ Avoid raw unscoped table queries against tenant-scoped tables. Either add WHERE tenant_id = ${tenantId} to these queries (and pass tenantId into the checker) or explicitly document and gate the check as admin-only, executed with proper authorization. Prefer parameterized Drizzle queries over string interpolation.
- **server/wiring-invariants.ts:535** — Memory-growth probe uses sql.raw to SELECT COUNT and pg_total_relation_size FROM ${tbl.name} with no tenant_id restriction. Several tables in MEMORY_TABLES (e.g. agent_knowledge, memory_entries, messages, graph_memory) are typically tenant-scoped.
  - _Fix:_ For tenant-scoped tables, either scope counts to a tenant (WHERE tenant_id = ${tenantId}) or compute and report per-tenant metrics. If the check must report system-wide values, require an explicit admin context and document it; avoid interpolating table names into raw SQL from untrusted sources.

### MEDIUM (43)

- **server/auto-asset-capture.ts:184** — SELECT DISTINCT over conversations queries the tenant-scoped conversations table with no tenant_id WHERE clause. The query `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...` reads rows across all tenants.
  - _Fix:_ If this is intended as a system-wide job, enumerate tenants explicitly and run tenant-scoped queries per-tenant. Otherwise add an explicit tenant_id filter to the SELECT. Ensure any downstream writes are performed only after explicit ownership checks (and prefer SQL-level tenant predicates where possible).
- **server/auto-asset-capture.ts:207** — SELECT from messages uses only conversation_id (no tenant_id) when reading assistant messages: `SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'`. messages is a tenant-scoped table and the query does not include tenant_id.
  - _Fix:_ Include tenant_id = ${c.tenant_id} in the WHERE clause (or otherwise scope the query by tenant) to ensure reads are constrained to the owning tenant. Alternatively run the query within a per-tenant processing loop where the tenant is an explicit parameter in SQL.
- **server/auto-consolidation.ts:335** — A system scan does `SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL` without restricting tenant scope. This reads the tenant-scoped memory_entries table across all tenants.
  - _Fix:_ If the scheduler must discover tenants to process, this pattern is acceptable only as an explicit system operation — document it and ensure it runs in a trusted context. Prefer an explicit tenant list table or run per-tenant queries with tenant_id provided by the scheduler. Where possible, avoid ad-hoc cross-tenant reads and ensure any processing later re-validates ownership before writes.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM `commitments` runs without a caller-scoped tenant filter (enumerates tenant_ids from a tenant-scoped table). This reads cross-tenant data.
  - _Fix:_ If this enumeration is required only for an admin operation, restrict the call to admin-only flows and document/authorize it explicitly (or query a tenants table instead). If not admin-only, avoid scanning the tenant-scoped commitments table across all tenants — require a tenant parameter and scope the SELECT with WHERE tenant_id = ${tenantId}.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` without tenant predicate: `SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}`.
  - _Fix:_ Constrain reads to the tenant (e.g. `WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}`) or explicitly join through the already-tenant-scoped `projects` row (e.g. JOIN/WHERE `p.tenant_id = ${tenantId}`) so every access to project_notes is validated to belong to the target tenant.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` without tenant predicate: `SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}`.
  - _Fix:_ Add an explicit tenant filter (e.g. `AND tenant_id = ${tenantId}`) or join through the `projects` row that is already tenant-scoped. Ensure every read from project_files is constrained to the tenant that owns the project.
- **server/data-protection.ts:404** — SELECT from tenant-scoped table `messages` without tenant predicate: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}` — this can count messages across tenants if conversation_id is ambiguous or malformed.
  - _Fix:_ Include the tenant predicate when counting messages (e.g. `WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}`). If conversation_id was obtained from a tenant-scoped query, still enforce tenant scoping on subsequent queries to follow the invariant that every read/write on tenant-scoped tables carries an application-level tenant filter.
- **server/delivery-pipeline.ts:1048** — getDeliveryStats queries delivery_logs without a tenant filter when tenantId is omitted (await db.select().from(deliveryLogs)). This returns rows across all tenants when tenantId is not supplied.
  - _Fix:_ Always require a tenantId or explicitly guard/authorize admin calls. Add a WHERE tenant_id = ${tenantId} or otherwise restrict this function to an authenticated admin path. Avoid defaulting to unfiltered SELECTs on tenant-scoped tables.
- **server/delivery-pipeline.ts:1043** — listDeliveries builds a base db.select().from(deliveryLogs) and returns it unscoped when tenantId is omitted, performing an unfiltered SELECT from a tenant-scoped table.
  - _Fix:_ Make tenantId mandatory for non-admin callers or enforce an explicit admin authorization branch. Apply .where(eq(deliveryLogs.tenantId, tenantId)) for normal callers and avoid returning rows across tenants by default.
- **server/delivery-pipeline.ts:1034** — getDeliveryStatus allows an omitted tenantId and will execute a SELECT on delivery_logs with only eq(deliveryLogs.id, deliveryId) — no tenant_id constraint when tenantId is not provided.
  - _Fix:_ Require tenantId from the caller or enforce authorization for admin callers. Add eq(deliveryLogs.tenantId, tenantId) to the WHERE clause (or disallow the omitted path) to prevent cross-tenant reads.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot runs SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' with no tenant filter. messages is tenant-scoped, so this aggregates across tenants.
  - _Fix:_ If traffic gating is intended to be tenant-specific, add tenant filtering (WHERE tenant_id = ${tenantId}). If it is intentionally global, document and centralize the admin-only nature and ensure callers are authorized. Avoid unscoped queries against tenant-scoped tables.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot runs SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... with no tenant filter. tool_performance is tenant-scoped and this query aggregates across tenants.
  - _Fix:_ If the metric should be tenant-specific, add tenant_id = ${tenantId} to the WHERE clause. Otherwise, require and validate admin privileges and document that this is a global operation. Do not run unscoped reads on tenant scoped tables from code paths reachable by tenant callers.
- **server/dormant-deprecation.ts:204** — getEverInvokedTools executes SELECT DISTINCT tool_name FROM tool_performance with no tenant filter, reading tenant-scoped usage data across all tenants.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) if the deprecation decision is per-tenant; otherwise restrict this to an authorized global admin path and make the intent explicit.
- **server/dormant-deprecation.ts:214** — getActiveTools14d executes SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant scoping — it returns tool activity across all tenants.
  - _Fix:_ Add tenant_id = ${tenantId} when computing per-tenant activity. If the routine intentionally requires global activity, ensure it's only callable by an authorized admin and document that behavior.
- **server/email.ts:470** — startInboxPolling's existence check uses SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 with no tenant constraint. inbox_messages is tenant-scoped, so this can match rows from other tenants (or incorrectly suppress storing for the intended tenant).
  - _Fix:_ Include inbox or tenant constraints when checking for existing messages (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} OR tenant_id = ${tenantId}). Ensure message dedup checks are scoped to the correct tenant/inbox.
- **server/email.ts:515** — backfillEmptyBodies selects rows from inbox_messages WHERE body_text IS NULL ... ORDER BY id without a tenant filter, thus processing records across tenants.
  - _Fix:_ Restrict the backfill to a specific tenant/inbox (e.g. WHERE (body_text IS NULL ...) AND tenant_id = ${tenantId} OR inbox_id = ${inboxId}). If the operation is intended to be global, make that explicit and ensure it's admin-restricted.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from request input (vec built from generateEmbedding(query)). Even though numbers are validated by vecLiteral, interpolating request-derived content into sql.raw() is an injection/isolation risk.
  - _Fix:_ Avoid sql.raw interpolation of request-derived values. Use parameterized binding where possible, or ensure vec is strictly validated and then use a safe parameter binding API. At minimum, document the validation and consider using prepared statements or Drizzle parameter bindings for the vector literal.
- **server/embeddings.ts:252** — Use of sql.raw() with an interpolated vector literal for ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} — embeds request-derived data into raw SQL.
  - _Fix:_ Replace raw string interpolation with a safe parameterized approach or validate and encode the vector robustly before interpolation. Prefer passing parameters rather than building SQL fragments via string interpolation.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) where vec is derived from user query embedding.
  - _Fix:_ As above, avoid sql.raw interpolation of request-derived data; use parameterized bindings or ensure strict validation/escaping of the vector and add a tenant filter (already present) — consider refactoring to a safe prepared query path.
- **server/glasses-gateway.ts:172** — UPDATE on apiKeys table (db.update(apiKeys).set({ lastUsedAt: ... }).where(eq(apiKeys.id, key.id))) lacks a tenant_id constraint — write to a tenant-scoped table is not explicitly scoped.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${key.id} AND tenant_id = ${key.tenantId}) or otherwise ensure the UPDATE is executed inside a tenant-bound context so the write cannot affect another tenant's apiKeys row.
- **server/job-queue.ts:385** — getJobStats runs a SELECT COUNT(*) FROM agent_jobs filtering only by time window and grouping by status — no tenant_id filter, so it returns counts across all tenants.
  - _Fix:_ If statistics should be per-tenant expose a tenant-scoped variant (WHERE tenant_id = ${tenantId}). If an aggregate operator metric is intended, mark it explicit and ensure only authorized operator contexts call it (and document as a global read).
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor queries agent_knowledge grouped by tenant_id/persona_id without a WHERE tenant_id = ... — enumerates tenant rows across all tenants.
  - _Fix:_ If the monitor intentionally scans all tenants, document and restrict the caller to an operator-only context. Otherwise, iterate per-tenant (pass tenantId in) and add WHERE tenant_id = ${tenantId} to each query so tenant-scoped reads are always explicitly constrained.
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey() queries tenant-scoped table `mcp_api_keys` without constraining by tenant_id (SELECT ... FROM mcp_api_keys WHERE key_prefix = ${prefix} LIMIT 1). This read against a tenant-scoped table has no tenant_id filter/guard.
  - _Fix:_ Either include tenant_id in the WHERE clause when the caller's tenant is known, or ensure the lookup is performed against a globally-unique, unguessable index designed for cross-tenant auth (and document that invariant). A safer design: store and index the full keyed lookup (e.g. key_hash) and use it in a parameterized query that returns the tenant_id only for the matched key, or require the authentication layer to derive tenantId in a constant-time, non-enumerable way before any subsequent tenant-scoped reads. If keeping prefix-only lookup, ensure prefix uniqueness and non-guessability (enforce UNIQUE across tenants and rotate/lengthen secrets), and audit that no other paths perform tenant-scoped reads without tenant filters.
- **server/pace-control.ts:85** — SELECT COUNT(*) FROM heartbeat_logs is executed without any tenant_id filter (counts runs across tenants). heartbeat_logs is tenant-scoped so this allows cross-tenant aggregation and the checkPace function has no tenant parameter.
  - _Fix:_ Require a tenantId parameter for checkPace and filter heartbeat_logs by tenant_id (WHERE tenant_id = ${tenantId} AND ...). If counts are intentionally global for ops, make the intent explicit and restrict to admin-only code paths.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs across all tenants (SELECT ... FROM heartbeat_logs WHERE created_at > ... ) without tenant_id filter; persona aggregation likewise lacks tenant scoping.
  - _Fix:_ Add a tenantId parameter and include WHERE tenant_id = ${tenantId} in the queries. If a global snapshot is required for operators, ensure it's only callable by authorized admin tooling and document/secure access.
- **server/research-proposals.ts:340** — Tenant scoping missing on JOIN: the replayHighValueFindings initial SELECT joins research_programs rp ON rp.id = re.program_id but does not constrain rp.tenant_id. Although re.tenant_id is filtered, the join can read a research_programs row that belongs to a different tenant if ids collide.
  - _Fix:_ Ensure joins to tenant-scoped tables also enforce tenant ownership, e.g. JOIN research_programs rp ON rp.id = re.program_id AND rp.tenant_id = re.tenant_id (or add rp.tenant_id = ${tenantId} to the WHERE). Apply the same pattern to all joins that include tenant-scoped tables.
- **server/research-proposals.ts:702** — Tenant scoping missing on JOINs used by generateResearchDigest: queries JOIN research_programs rp ON rs.program_id = rp.id but only filter rs.tenant_id = ${tenantId}; rp.tenant_id is not checked explicitly.
  - _Fix:_ Constrain joined research_programs rows to the same tenant (e.g. JOIN research_programs rp ON rs.program_id = rp.id AND rp.tenant_id = rs.tenant_id or add rp.tenant_id = ${tenantId}). Audit other JOINs in this file for the same pattern and add explicit tenant checks where needed.
- **server/routes.ts:649** — Startup cleanup query reads tenant-scoped tables (conversations, projects) without constraining by tenant_id. The subqueries SELECT DISTINCT tenant_id FROM conversations / projects run with no tenant filter.
  - _Fix:_ Restrict any reads against tenant-scoped tables to an explicit tenant context (e.g. WHERE tenant_id = <admin-or-caller-tenant>) or, if this is an admin-wide maintenance operation, run it in an explicit admin-only code path with documented justification. Avoid unscoped subqueries that enumerate other tenants' ids; instead perform tenant-scoped existence checks or iterate explicit tenant ids from a safe, audited source.
- **server/routes.ts:766** — backfillProjectDriveFolders selects from projects JOIN tenants without a WHERE tenant_id = ... constraint. This reads tenant-scoped rows across all tenants without scoping.
  - _Fix:_ If this is intended as a global backfill, ensure it runs only in a trusted admin context and document that. Otherwise, add an explicit tenant filter (e.g. WHERE p.tenant_id = ${tenantId}) or iterate per-tenant after enumerating tenant ids via a controlled admin-only path. Never issue unscoped queries against tenant-scoped tables from request-handling code.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table `agent_jobs` with no tenant_id predicate — query returns rows across all tenants.
  - _Fix:_ Add an application-level tenant filter (e.g. WHERE j.tenant_id = ${tenantId}) or otherwise explicitly bind the query to the intended tenant. If this aggregate was intentionally global, make the intent explicit and ensure the caller is strictly platform-admin and the privilege is documented/monitored.
- **server/routes/oauth-subscriptions.ts:325** — Cron heal handler issues SELECT DISTINCT tenant_id FROM oauth_subscriptions without a tenant predicate — reads tenant-scoped table across all tenants.
  - _Fix:_ If the cron operation must iterate all tenants, ensure the endpoint is strongly authenticated (it is via CRON_SECRET) and clearly documented as a global operation. Otherwise, restrict the query to the intended tenant (WHERE tenant_id = ${tenantId}) or add an explicit platform-admin guard. Consider auditing/limiting results and avoid returning raw tenant identifiers to less-privileged callers.
- **server/seed.ts:4026** — INSERT INTO project_notes omits tenant_id. If project_notes is tenant-scoped/NOT NULL tenant_id, this either fails or creates rows without explicit tenant ownership.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${projId}, ${note}, 'system:import', <tenantId>)). If tenant_id should be derived from project_id, query the project's tenant_id first and use it explicitly in the INSERT.
- **server/skill-seeker.ts:779** — runSkillSeekerSweep defaults tenantId to 1 (admin) when omitted. Scheduling/automation that calls this without an explicit tenant may operate on admin tenant unintentionally.
  - _Fix:_ Remove the implicit admin default. Require explicit tenant context or provide a dedicated admin-only entrypoint with authorization checks.
- **server/skill-seeker.ts:845** — Hook handler uses personaId from incoming event data and passes it directly to detectGap without verifying that the persona belongs to the supplied tenantId. This can record/associate gaps under a tenant for a persona owned by another tenant.
  - _Fix:_ Before using caller-supplied personaId, verify ownership (e.g. query persona by id and assert persona.tenant_id === tenantId) or drop personaId when tenant mismatch is detected.
- **server/skillify.ts:221** — Querying `personas` by persona id without constraining to the caller's tenant. The code reads personaRows via WHERE personas.id = conv[0].personaId with no tenant_id check, which may leak or associate persona data across tenants.
  - _Fix:_ When loading a persona referenced by a conversation, ensure the persona row is validated against the conversation's tenant (e.g. add AND eq(personas.tenantId, tenantId) to the WHERE clause or call a guard like assertPersonaInTenant). If personas are intentionally global, document that explicitly; otherwise enforce tenant scoping.
- **server/storage.ts:826** — getMemoriesWithoutEmbeddings() selects from memory_entries without any tenant scoping, returning rows across tenants.
  - _Fix:_ Add a tenantId parameter (or requiredTenantScope) and filter via eq(memory_entries.tenant_id, tenantId). If this function must run across tenants (admin job), require and assert explicit admin privileges and document reason clearly.
- **server/storage.ts:831** — getKnowledgeWithoutEmbeddings() queries agent_knowledge without a tenant filter and will return knowledge rows across tenants.
  - _Fix:_ Require a tenantId or requiredTenantScope and include eq(agent_knowledge.tenant_id, tenantId) in the WHERE clause, or restrict this routine to an explicit admin-only path with audit controls.
- **server/storage.ts:942** — getHeartbeatTask(id) selects heartbeat_tasks WHERE id = ${id} with no tenant filter. heartbeat_tasks has tenant_id and this routine returns a task without verifying tenant ownership.
  - _Fix:_ Require caller to supply tenantId (or use requiredTenantScope) and include eq(heartbeat_tasks.tenant_id, tenantId) in the WHERE clause, or document and restrict this function to trusted internal/admin use only with appropriate checks.
- **server/tools.ts:4566** — SELECT from presenter_sessions by presentation_id without tenant constraint or ownership assertion. The query reads presenter_sessions WHERE presentation_id = ${slideResult.presentationId} with no tenant_id filter or explicit verification that the presentation belongs to the caller's tenant.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE presentation_id = ${presentationId} AND tenant_id = ${tenantId}) or perform an explicit ownership check prior to the SELECT (assert presentation belongs to params._tenantId). If presenter_sessions is globally indexed by presentation_id that is unique and bound to the tenant, document that invariant; otherwise add tenant scoping to the query.
- **server/wiring-invariants.ts:244** — Query on tool_performance (SELECT count(*) FROM tool_performance WHERE last_success_at > ...) runs without any tenant_id constraint. If tool_performance is tenant-scoped this reads across tenants.
  - _Fix:_ If the table is tenant-scoped, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or aggregate per-tenant. If this is intended admin/global telemetry, make the admin-only intent explicit (use an ADMIN_TENANT_ID context or documented admin-only query path) and ensure only an authorized admin can run it.
- **server/wiring-invariants.ts:258** — Query on tool_performance (SELECT count(*) FROM tool_performance) executes with no tenant_id constraint. This computes a global count across tenants.
  - _Fix:_ Restrict the query to a tenant via WHERE tenant_id = ${tenantId} when used in tenant-facing contexts, or explicitly run under an admin context and gate execution to admin-only code paths. Alternatively, compute per-tenant counts rather than a global scan.
- **server/wiring-invariants.ts:417** — tool_drift check queries tool_performance (SELECT ... FROM tool_performance WHERE last_failure_at > ...) and aggregates across tenants with no tenant_id constraint.
  - _Fix:_ If tool_performance is tenant-scoped, include tenant_id in the WHERE/GROUP BY so drift is computed per-tenant, or otherwise restrict execution to an admin-only context with proper authorization/audit logging.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory executes without a tenant_id WHERE clause. graph_memory is likely tenant-scoped and this reads across all tenants.
  - _Fix:_ Add a tenant filter (WHERE tenant_id = ${tenantId}) or run this as an explicit admin-only diagnostic operation that is properly authorized and audited. Ensure application-level tenant scoping is enforced for all reads of tenant-scoped tables.

### LOW (2)

- **server/routes.ts:1532** — Public stats endpoint runs count queries against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) with no tenant_id constraint, returning global aggregates.
  - _Fix:_ Either scope stats to the caller's tenant (add WHERE tenant_id = ${tenantId}) or explicitly gate this API to platform-admins and document the global aggregate behavior. Ensure any global reads are intentional and run in admin-only paths.
- **server/routes.ts:1561** — Public architecture endpoint issues multiple unscoped SELECTs (count queries and a personas listing) against tenant-scoped tables without tenant_id filters, returning data across tenants.
  - _Fix:_ Scope these queries to the caller's tenant (WHERE tenant_id = ${tenantId}) or restrict the endpoint to platform-admins only. If global metrics are required, run them through an explicit admin-only maintenance job, not the public endpoint.

### Suppressed — confirmed false positives (237)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — autoApplyEligibleInsights executes an UPDATE on `ai_insights` (SET status='applied' ...) without any tenant_id constraint — this can modify rows across all tenants in bulk.
  - _Why false positive:_ Platform-wide backfill sweep intentionally updates eligible rows across tenants and uses returned tenant_id per row for follow-up routing, not a tenant-facing mixed output.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Cross-tenant SELECT: the candidate query (SELECT ... FROM agent_runs JOIN agent_approvals ...) does not constrain agent_runs to a specific tenant_id. It scans/returns rows across all tenants, enabling cross-tenant observation/processing.
  - _Why false positive:_ Maintenance worker scans pending approvals across tenants, joins on matching tenant_id, and processes each candidate row with its own tenant context rather than exposing mixed tenant data.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — UPDATE without tenant constraint: appendStep() updates agent_runs by id only (WHERE id = runId) and does not include tenant_id in the WHERE clause, allowing a caller with a runId to update another tenant's run.
  - _Why false positive:_ Helper mutates agent_runs by server-generated serial run id in internal worker/run orchestration code; runId is not shown as coming from an untrusted request boundary.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — UPDATE without tenant constraint: updateRunState() merges JSONB into agent_runs and uses WHERE id = runId only; tenant_id is not enforced, permitting cross-tenant mutation of run state.
  - _Why false positive:_ Internal run-state helper updates by globally unique server-side run id within agent orchestration, with no evidence the id is sourced from a cross-tenant request boundary.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — UPDATE without tenant constraint: replaceRunState() calls update(agentRuns).set({ state }) with WHERE id = runId only, missing tenant scoping and allowing cross-tenant overwrite of run state.
  - _Why false positive:_ Server-invoked run-state replacement uses the internal run primary key only; no untrusted caller-provided boundary is shown.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — UPDATE without tenant constraint: completeRun() sets status/result/completedAt for a run using WHERE id = runId only (no tenant_id filter), allowing occasion of completing a run belonging to another tenant.
  - _Why false positive:_ Run completion is an internal lifecycle transition on a server-generated run id, followed by internal distillation using the fetched row's tenantId.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — UPDATE without tenant constraint: failRun() updates agent_runs.status/error/completedAt using WHERE id = runId only, lacking tenant filtering and permitting cross-tenant state changes.
  - _Why false positive:_ Failure-state update is an internal orchestration helper keyed by globally unique run id, not shown reachable from untrusted tenant input.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — UPDATE without tenant constraint: pauseRun() updates agent_runs.status and state using WHERE id = runId only; tenant_id is not constrained, enabling a tenant to pause/mutate another tenant's run.
  - _Why false positive:_ Pause helper is part of internal agent-run lifecycle management and operates on the server-generated run primary key, with no shown untrusted cross-tenant source.
- **server/auto-memorize.ts:163** _(system-sweep)_ — SELECT from `messages` (SELECT m.tenant_id, m.role, ...) has no WHERE tenant_id filter and pulls messages for many tenants into a single LLM prompt pipeline. Even though tenant_id is selected for later partitioning, this reads other tenants' data into memory and risks cross-tenant data exposure.
  - _Why false positive:_ Housekeeping job reads recent messages across tenants but partitions by tenant before synthesis so no single LLM prompt mixes tenants' content.
- **server/channel-routing.ts:16** _(global-table)_ — channel_routes table is created without any tenant_id column; this makes routing entries global and allows cross-tenant references to tenant-scoped rows (e.g. personas).
  - _Why false positive:_ channel_routes is defined as one global route per channel with UNIQUE(channel), and referenced personas schema note shows no tenant_id, matching an intentionally platform-global registry.
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE against tenant-scoped table `conversations` without tenant_id constraint: `DELETE FROM conversations WHERE id = ${conv.id}`. This removes a conversation row by id without constraining to the conversation's tenant.
  - _Why false positive:_ Purge loop is platform-wide housekeeping over expired soft-deleted conversations; each row carries its own tenant_id and the operation only deletes housekeeping data, with no tenant-facing mixed output.
- **server/felix-brain.ts:19** _(system-sweep)_ — Boot-time rehydration SELECT on tenant-scoped table 'agent_runs' has no tenant_id constraint — it reads all tenants' running/pending runs.
  - _Why false positive:_ Boot-time global rehydration intentionally scans all running/pending rows and keeps per-row tenant_id in memory; no tenant-facing mixed output here.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from code_proposals (archiveStaleProposals) lacks any tenant_id constraint. If code_proposals is tenant-scoped this reads proposals for all tenants and later the code may act on them globally.
  - _Why false positive:_ Health-audit TTL sweep intentionally scans stale proposals across tenants for housekeeping/admin findings, not tenant-scoped app reads.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows without constraining by tenant_id. This can change proposals belonging to other tenants.
  - _Why false positive:_ Global housekeeping archive of stale proposals updates status only after sweep selection; maintenance action across tenants is intentional.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id constraint — it can atomically claim jobs across all tenants.
  - _Why false positive:_ Global worker queue intentionally claims due jobs across tenants and returns each row with its own tenant_id for downstream per-job scoping.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — Initial CTE SELECT/UPDATE on tenant-scoped table `scheduled_posts` (lines 61–81) has no tenant_id constraint. The statement selects due rows and then UPDATEs `scheduled_posts` SET status='publishing' ... FROM due WHERE sp.id = due.id without constraining by tenant_id, allowing cross-tenant locking/updates.
  - _Why false positive:_ Heartbeat runner globally claims due scheduled posts across tenants, returning per-row tenant_id and processing each post separately.
- **server/minds-engine.ts:214** _(upstream-verified)_ — createTicket constructs a Postgres array literal for depends_on via string interpolation: {${params.dependsOn.join(",")}. This directly embeds request-derived data into SQL text and is vulnerable to SQL injection and tenant-isolation bypass.
  - _Why false positive:_ This is a SQL-injection finding, not tenant-isolation; tenant ownership is checked via getMind(params.mindId, params.tenantId) before insert and row is inserted with that tenant_id.
- **server/minds-engine.ts:262** _(upstream-verified)_ — checkDependenciesMet builds an ANY(...) array using string interpolation: id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]). If ticket.dependsOn contains untrusted values this is SQL-injectable (dependsOn may originate from requests via createTicket).
  - _Why false positive:_ This query includes AND tenant_id = tenantId, so no cross-tenant read despite the separate SQL-injection concern.
- **server/minds-engine.ts:327** _(upstream-verified)_ — markEventsHandled builds id = ANY(${`{${eventIds.join(",")}}`}::int[]) using string interpolation of the caller-supplied eventIds array. This is a direct SQL-injection risk when eventIds are request-derived.
  - _Why false positive:_ UPDATE is tenant-scoped with AND tenant_id = tenantId; flagged issue is SQL injection, not cross-tenant isolation bypass shown here.
- **server/minerva-planner.ts:472** _(upstream-verified)_ — Rollback UPDATE flips status back to 'awaiting_approval' with WHERE id = ${args.planId} AND status = 'revising' — missing any tenant_id constraint. This unscoped UPDATE can modify plans across tenants.
  - _Why false positive:_ rollback uses args.planId only after earlier tenant-scoped/verified row lookup; tenant ownership is already established in this flow via row.tenant_id
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — UPDATE plans executed in appendExecutionLog does not constrain by tenant_id (UPDATE plans ... WHERE id = ${planId}). This mutates a tenant-scoped table without verifying the caller/plan tenant.
  - _Why false positive:_ server-internal worker helper updates by server-supplied plan PK inside executor flow, not from untrusted request boundary
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim UPDATE on plans lacks a tenant_id constraint (UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved' RETURNING ...). This allows changing rows across tenants by id only.
  - _Why false positive:_ executor claims plans by server-invoked PK in an internal worker path; planId is used for background execution, not directly from tenant request input here
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans sets status='failed' using only id (WHERE id = ${planId}) with no tenant_id filter — write to tenant-scoped table without tenant scoping.
  - _Why false positive:_ this update occurs only after the prior claim RETURNING row for the same plan id established the owning row/tenant in the same call path
- **server/plan-executor.ts:728** _(upstream-verified)_ — Final UPDATE of plans to set status uses WHERE id = ${planId} but no tenant_id filter, allowing cross-tenant mutation.
  - _Why false positive:_ final status update is in same execution flow after successful claim of that exact plan row and retained row.tenant_id context
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-path UPDATE plans (setting status='failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id in WHERE clause.
  - _Why false positive:_ catch-path status update is part of internal background executor handling a server-invoked plan PK, not an untrusted cross-tenant entrypoint
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write against heartbeat_tasks without constraining tenant_id. This can disable tasks belonging to other tenants if id collision or attacker-supplied id is used.
  - _Why false positive:_ t.id comes from a prior SELECT of heartbeat_tasks scoped by ht.tenant_id = tenantId in the same loop, so only already-owned task rows are processed
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — After successful rebase the code updates code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} without tenant filtering.
  - _Why false positive:_ Server-side verifier worker updates by proposalId passed from fireAndForgetVerify, not directly from request boundary.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_knowledge` performed without constraining by tenant (UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}). Missing tenant_id in WHERE allows modifying another tenant's row if an id is reused or in races.
  - _Why false positive:_ Global scheduler loop processes each row with its own row-carried tenant_id and only updates housekeeping fields on that same row.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` executed without tenant_id in the WHERE clause (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This can modify another tenant's memory entry by id.
  - _Why false positive:_ storedId is obtained immediately from a SELECT scoped by input.tenantId and personaId in the same call path.
- **server/research-engine.ts:165** _(system-sweep)_ — Cross-tenant UPDATE: cleanupZombieSessions runs an UPDATE on research_sessions (marks running sessions completed) with no tenant_id constraint, affecting sessions across all tenants.
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping update of status fields only; it does not return tenant data or mix content.
- **server/research-engine.ts:400** _(system-sweep)_ — Unscoped DELETE: startup DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() has no tenant restriction and will delete autoresearch knowledge for all tenants.
  - _Why false positive:_ Expired autoresearch knowledge purge is a platform-wide maintenance delete by expiry/source, not tenant-facing data access.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' is missing a tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This can modify rows belonging to other tenants if a row id collides or code is invoked with different context.
  - _Why false positive:_ conv was just created with tenantId and projectId was prevalidated with WHERE id = projectId AND tenant_id = tenantId.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into project_conversations does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). tenant_id is required and not provided — new rows may be orphaned or attributed to the wrong tenant.
  - _Why false positive:_ project_conversations has no tenant_id and is an association child isolated through tenant-scoped project/conversation parents.
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' lacks a tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This can update another tenant's project if ids collide or context is incorrect.
  - _Why false positive:_ project row ownership was verified a few lines above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId.
- **server/routes/conversations.ts:230** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). project_notes is tenant-scoped but the tenant_id column is not provided, producing an insert that lacks tenant attribution.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; insert is transitively isolated via project_id.
- **server/sculptor.ts:106** _(upstream-verified)_ — UPDATE sculptor_sessions executed without tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This allows a caller to modify a session row by id across tenants.
  - _Why false positive:_ Updates the row just inserted and returned as session in the same call path; id is server-derived from that tenant-scoped INSERT.
- **server/sculptor.ts:113** _(upstream-verified)_ — UPDATE sculptor_sessions executed without tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). This can link/modify a session belonging to another tenant if id is guessed.
  - _Why false positive:_ conversation_id is written back to the newly created session row from the same tenant-scoped INSERT/RETURNING flow.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE on tenant-scoped table `research_programs` without tenant constraint (UPDATE research_programs SET model = ... WHERE id = ${p.id}). Even though id is a primary key, the update lacks an explicit tenant_id check and can modify another tenant's row if misused.
  - _Why false positive:_ Boot-time global seed/backfill repairing invalid model ids across all rows; housekeeping mutation only, no tenant data returned or mixed.
- **server/seed.ts:3855** _(system-sweep)_ — UPDATE on tenant-scoped table memory_entries without any tenant_id constraint (mass cross-tenant update).
  - _Why false positive:_ Global maintenance normalizer updates each memory row in place during seed; housekeeping content fix, not tenant-facing mixing.
- **server/seed.ts:3880** _(system-sweep)_ — UPDATE on tenant-scoped table conversations without any tenant_id constraint (will modify conversations across all tenants).
  - _Why false positive:_ Global boot-time backfill of legacy model id on conversations; housekeeping-only field repair across tenants.
- **server/seed.ts:3826** _(system-sweep)_ — DELETE FROM heartbeat_tasks performed with no tenant_id constraint — can delete rows across all tenants.
  - _Why false positive:_ Boot-time cleanup sweep deleting stale auto-disabled heartbeat task rows across tenants; maintenance only, no tenant-facing data mixing.
- **server/seed.ts:4094** _(upstream-verified)_ — UPDATE on tenant-scoped table agent_knowledge uses only id in WHERE clause (no tenant_id constraint). Although id was selected previously with a tenant filter, the UPDATE statement itself lacks tenant scoping and can affect rows if ids are re-used or if called with different context.
  - _Why false positive:_ The id being updated was selected immediately above with tenant_id = 1 and matching category/title in the same call path.
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. This writes to a tenant-scoped association table without explicitly setting the caller's tenant, allowing cross-tenant contamination or orphaned rows if the table expects tenant scope.
  - _Why false positive:_ project_conversations has no tenant_id and insert occurs only after tenant-scoped conversation update succeeds, so ownership is transitively via project/conversation parents.
- **server/tools.ts:3019** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) does not set tenant_id. Even though the code validated the conversation and project beforehand, the INSERT itself lacks an explicit tenant_id column value, violating the invariant that every INSERT into tenant-scoped tables must pass tenantId.
  - _Why false positive:_ project_conversations has no tenant_id; project ownership and conversation tenant are verified before insert, so association is isolated through tenant-scoped parents.
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT into project_files (add_file) does not include tenant_id. This writes a file row linked to a project without explicitly recording the caller's tenant, violating the requirement that INSERTs into tenant-scoped tables must set tenantId.
  - _Why false positive:_ project_files has no tenant_id and row is a child of tenant-scoped projects after ownership check.
- **server/tools.ts:2995** _(fk-transitive)_ — INSERT into project_notes (add_note) omits tenant_id. The code verifies project ownership, but the INSERT into the tenant-scoped notes table does not set tenant_id, breaking the rule that every INSERT must pass tenantId explicitly.
  - _Why false positive:_ project_notes has no tenant_id and insert is into a child table of a tenant-scoped project after tenant ownership verification.
- **server/tools/domains/files/handlers.ts:903** _(fk-transitive)_ — INSERT into `project_files` does not set tenant_id (sql: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`). Per invariant, tenant-scoped tables require tenant_id to be explicitly provided on INSERT. Omitting it can misattribute rows or fail if tenant_id is NOT NULL with no default.
  - _Why false positive:_ project_files has no tenant_id; project is derived from tenant-scoped conversation→project join, so file row is transitively scoped by project.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from `experiments` in runDueAbExperiments has no tenant_id constraint (reads experiments across all tenants).
  - _Why false positive:_ heartbeat-callable cross-tenant sweep processes each experiment with its own row-carried tenant_id and updates each row using id plus tenant_id; no mixed tenant output.
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting SELECTs rows from `ai_insights` without a tenant_id filter (reads high-priority applied insights across tenants).
  - _Why false positive:_ durability sweep intentionally scans across tenants, carries each row tenant_id forward, and routes/updates per row without combining tenant data into one response.
- **server/agentic-engines.ts:347** _(global-table)_ — gatherOperationalData reads from `heartbeat_logs` (count of calls) with no tenant_id filter — returns data across tenants.
  - _Why false positive:_ heartbeat_logs schema shows tenant_id column: NO; query reads aggregate platform-wide log counts from an intentionally global table.
- **server/agentic-engines.ts:451** _(global-table)_ — runPredictiveEngine selects recent rows from `heartbeat_logs` (status = 'success') with no tenant_id constraint — reads logs for all tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is treated as a platform-global log source here.
- **server/agentic-engines.ts:519** _(global-table)_ — runOptimizationEngine queries `heartbeat_logs` for task performance with no tenant_id constraint (GROUP BY across all tenants).
  - _Why false positive:_ heartbeat_logs is non-tenant-scoped per schema, so cross-tenant aggregation is consistent with a global operational log table.
- **server/agentic-features.ts:563** _(upstream-verified)_ — UPDATE lead_enrichments sets icp_score/etc WHERE id = ${lead.id} but does not constrain by tenant_id — allows updating another tenant's lead row by id.
  - _Why false positive:_ lead.id comes from leadRows previously selected for the same tenant, so the update targets tenant-owned rows from the same call path.
- **server/agentic-features.ts:797** _(upstream-verified)_ — UPDATE outreach_enrollments sets reply_classification and status WHERE id = ${row.id} but does not include tenant_id — may update another tenant's enrollment by id.
  - _Why false positive:_ row.id was fetched immediately above with e.tenant_id = params.tenantId, so the update is scoped by prior tenant-verified selection in the same flow.
- **server/agentic/approvals.ts:136** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals (set status='expired') with no tenant_id constraint — will expire pending approvals across all tenants.
  - _Why false positive:_ This is a platform-wide expiry sweep that updates housekeeping status on expired approvals and then processes each returned row with its own tenantId.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Unrestricted SELECT across tenant-scoped table `department_budgets`: the query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` has no tenant_id constraint. This reads rows for all tenants from a tenant-scoped table, violating the requirement that reads be constrained to the caller's tenant.
  - _Why false positive:_ Explicit heartbeat sweep across tenants; each row carries its own tenant_id and processing/emitted events remain per-tenant rather than mixing content into one tenant response.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE/SELECT on agent_wake_schedules claims rows via a subquery that lacks any tenant_id constraint. The statement `UPDATE agent_wake_schedules ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE SKIP LOCKED)` can lock and mutate pending wakes across all tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due rows across tenants, then emits and updates each claimed row using its own tenant_id.
- **server/auto-transcript.ts:128** _(system-sweep)_ — backfillProjectTranscripts SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... has no tenant_id constraint. This scans conversations across all tenants and drives per-tenant side-effects (file writes, DB updates) without scoping by the caller tenant.
  - _Why false positive:_ Backfill function is a platform-wide housekeeping sweep; it iterates rows carrying their own tenant_id and performs per-row maintenance, not tenant-facing mixed output.
- **server/auto-transcript.ts:153** _(system-sweep)_ — Query assigned to `alsoLinked` joins project_conversations/conversations/projects and uses a subquery on messages but has no overall tenant filter. It can return rows across tenants and is then used to UPDATE conversations and call autoSaveProjectTranscript for each returned row.
  - _Why false positive:_ Cross-tenant scan is part of maintenance backfill; rows are joined with tenant-consistent project/conversation ownership and processed per row with carried tenant_id.
- **server/auto-transcript.ts:171** _(system-sweep)_ — missingLinks SELECT c.id, c.project_id, c.tenant_id FROM conversations ... (and subsequent logic) is executed without filtering by tenant_id. The result is used to insert project_conversations and to call autoSaveProjectTranscript across tenants.
  - _Why false positive:_ This is a maintenance query to discover missing links across tenants; it only creates housekeeping links per row and does not expose mixed tenant data.
- **server/auto-transcript.ts:180** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING is executed based on a prior SELECT that was not tenant-scoped. project_conversations has no tenant_id column (isolation is transitive), so missing upstream tenant verification can cause cross-tenant link insertion.
  - _Why false positive:_ project_conversations has no tenant_id and is a transitive child link table; upstream SELECT verifies project ownership matches conversation tenant before insert.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into tenant-scoped table 'invoice_items' omits tenant_id in the VALUES clause (no tenant_id column is set on invoice_items rows). This violates the tenant-isolation invariant: tenant_id must be explicitly stored on each tenant-scoped row.
  - _Why false positive:_ invoice_items has no tenant_id column and is a line-item child of tenant-scoped invoices; inserting via verified parent invoice_id is by design.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` omits a tenant_id constraint in the WHERE clause (UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false). This allows a row belonging to any tenant to be updated if its id is known.
  - _Why false positive:_ Update is by server-generated reviewId from in-memory pending state on internal hot path, not directly from untrusted request input.
- **server/channel-routing.ts:33** _(global-table)_ — listChannelRoutes JOINs the personas table (JOIN personas p ON p.id = cr.persona_id) without any tenant_id constraint — this can return persona names from other tenants.
  - _Why false positive:_ channel_routes is created as a single global routing table with UNIQUE(channel) and no tenant_id; personas schema note also indicates no tenant scoping.
- **server/channel-routing.ts:54** _(global-table)_ — setChannelRoute INSERTs a persona_id supplied by the caller into channel_routes without validating that the persona belongs to the caller's tenant (and without storing a tenant_id). This lets a caller point to personas owned by other tenants.
  - _Why false positive:_ Insert/update targets a platform-global channel_routes table keyed globally by channel, not a tenant-scoped relation.
- **server/channel-routing.ts:63** _(global-table)_ — getPersonaForChannel reads persona_id from channel_routes WHERE channel = ${channel} without any tenant constraint, so it can return mappings belonging to other tenants (or be manipulated to point to other tenants' personas).
  - _Why false positive:_ Read is from global channel_routes mapping by channel; no evidence this table is intended to be tenant-scoped.
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute deletes from channel_routes WHERE channel = ${channel} with no tenant constraint; a caller can remove another tenant's mapping if channel_routes is intended to be tenant-scoped.
  - _Why false positive:_ Delete operates on the same platform-global channel_routes mapping, not on a tenant-scoped table.
- **server/chat-engine.ts:572** _(upstream-verified)_ — SELECT from tenant-scoped table `project_files` without tenant filter — query: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...`
  - _Why false positive:_ A few lines above, project ownership is checked with SELECT * FROM projects WHERE id = projectId AND tenant_id = tenantId; project_files is a child table without tenant_id, scoped transitively by verified project_id.
- **server/chat-engine.ts:584** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` without tenant filter — query: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...`
  - _Why false positive:_ project_notes has no tenant_id and is scoped transitively by project_id; code notes projectId was verified to belong to tenant above
- **server/chat-engine.ts:633** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` without tenant filter — query: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...`
  - _Why false positive:_ c.id comes from prior query joining project_conversations to conversations with c.tenant_id = tenantId in same call path
- **server/chat-engine.ts:651** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without tenant filter — query: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...`
  - _Why false positive:_ messages are fetched for c.id values already constrained by prior conversations query with c.tenant_id = tenantId
- **server/chat-engine.ts:832** _(fk-transitive)_ — Subquery counts tenant-scoped table `project_files` without tenant filter — `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)` can return cross-tenant counts
  - _Why false positive:_ project_files has no tenant_id and count is by tenant-scoped parent project p where p.tenant_id = tid
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` (SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...) has NO tenant_id constraint — it pulls overdue commitments across all tenants.
  - _Why false positive:_ boot/scheduled scanner intentionally scans all tenants and only emits redacted overdue digest per row without tenant content mixing
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} updates a tenant-scoped table row by id only, without constraining by tenant_id — allows touching other tenants' rows by id.
  - _Why false positive:_ UPDATE includes AND tenant_id = tenantId and getCredential(id, tenantId) already verifies ownership
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} (in getLoginCredentials) updates last_used_at without a tenant_id constraint, despite having tenant context — this can modify another tenant's row if ids are guessed.
  - _Why false positive:_ UPDATE includes AND tenant_id = tenantId and cred was retrieved by tenant-scoped getCredentialForSite(siteUrl, tenantId)
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT against tenant-scoped table `conversations` with no tenant_id predicate: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` reads rows across all tenants.
  - _Why false positive:_ purge is a cross-tenant housekeeping sweep; each row carries its own tenant_id and child deletes are tenant-scoped, with no tenant-facing mixed output
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake updates dfy_intake_forms via .where(eq(dfyIntakeForms.id, form.id)) with no tenant_id constraint. This UPDATE can modify another tenant's row if IDs collide or caller manipulates form.id.
  - _Why false positive:_ update uses form.id obtained from prior getDfyIntakeByToken(token); caller cannot supply arbitrary id in this path.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} without AND tenant_id = ${tenantId}. This can delete chunks belonging to other tenants sharing doc_path/collection_id naming collisions.
  - _Why false positive:_ same call path first verifies doc_collections row by collectionId and tenantId before deleting chunks for that collection.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT from memory_entries (backfillEmbeddingVecs) lacks tenant scoping: SELECT id, embedding FROM memory_entries ... LIMIT 200 — no tenant_id filter. This reads embeddings across all tenants.
  - _Why false positive:_ platform-wide embedding backfill sweep reads rows across tenants only to repair embedding_vec fields; no tenant-facing mixed output.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant constraint — cross-tenant update risk.
  - _Why false positive:_ maintenance backfill updates housekeeping field embedding_vec by server-selected row id during global sweep.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT from agent_knowledge (backfillEmbeddingVecs) lacks tenant scoping: SELECT id, embedding FROM agent_knowledge ... LIMIT 200 — no tenant_id filter. This reads rows across tenants.
  - _Why false positive:_ platform-wide embedding backfill sweep reads agent_knowledge across tenants only for maintenance, not tenant-facing data return.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant constraint — cross-tenant update risk.
  - _Why false positive:_ maintenance sweep updates embedding_vec only, using ids from its own cross-tenant scan.
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint — write is not scoped to the caller's tenant.
  - _Why false positive:_ runId is used inside internal worker loop; call path shows server-invoked maybeRunFelixLoop passing fixed tenant and not untrusted request id.
- **server/felix-loop.ts:690** _(internal-worker-pk)_ — Error-recovery UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint.
  - _Why false positive:_ error recovery update targets internal worker-created run row by server-held runId, not caller-controlled input.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) but no tenant_id constraint.
  - _Why false positive:_ same function selects project, checks project.tenantId matches tenantId, then updates that verified project id.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images does not set or validate tenant context. The INSERT uses session_id and other values but does not include tenant_id (nor does it verify the session belongs to the calling tenant), allowing cross-tenant data writes if session_id is abused.
  - _Why false positive:_ presenter_slide_images is keyed by session_id and finding references only parent presenter_sessions as tenant-scoped; child insert is transitively isolated through session FK.
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions ... WHERE id = ${sessionId} performs an update without constraining by tenant_id. This updates a session row by id only and can modify another tenant's session/metadata if sessionId is not tenant-bound/validated.
  - _Why false positive:_ sessionId comes from server-created presenter session response in same internal flow, not from untrusted cross-tenant request input.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from heartbeat_tasks (archiveStaleProposals) has no tenant_id constraint. If heartbeat_tasks is tenant-scoped this will discover tasks across tenants and treat them as stale regardless of tenant context.
  - _Why false positive:_ Global health-audit sweep intentionally scans stale heartbeat rows across tenants and only emits audit findings/housekeeping actions, not tenant-facing mixed data.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id only, with no tenant constraint. This can disable tasks belonging to other tenants.
  - _Why false positive:_ Bulk disable is part of the same cross-tenant maintenance sweep for stale heartbeat tasks; it mutates housekeeping status only.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from tenant-scoped table research_schedules without a tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due research_schedules across all tenants and then processes each row using its own tenant_id.
- **server/heartbeat.ts:102** _(upstream-verified)_ — UPDATE on tenant-scoped table research_schedules uses only id in WHERE clause (UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id}) — no tenant_id constraint.
  - _Why false positive:_ This update operates on sched.id from the immediately preceding scheduler SELECT row; no untrusted tenant boundary and row ownership was established upstream in the same loop.
- **server/heartbeat.ts:108** _(upstream-verified)_ — UPDATE on tenant-scoped table research_schedules uses only id in WHERE clause (UPDATE research_schedules SET last_run_at = ..., next_run_at = ... WHERE id = ${sched.id}) — no tenant_id constraint.
  - _Why false positive:_ This write uses sched.id from the already-selected schedule row in the same control flow, with tenant context carried on sched.tenant_id.
- **server/heartbeat.ts:121** _(upstream-verified)_ — UPDATE on tenant-scoped table research_schedules uses only id in WHERE clause (UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id}) — no tenant_id constraint in recovery path.
  - _Why false positive:_ Recovery-path update still targets the schedule row already fetched by the scheduler loop; same-call-path row context is established earlier.
- **server/heartbeat.ts:385** _(system-sweep)_ — SELECT from tenant-scoped table event_log without a tenant_id constraint (SELECT * FROM event_log WHERE status = 'pending' ...). This reads pending events across all tenants.
  - _Why false positive:_ Heartbeat event processor intentionally drains pending events across tenants, handling each event with its own tenant_id and routing per-row.
- **server/heartbeat.ts:273** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — updating a tenant-scoped table without constraining tenant_id.
  - _Why false positive:_ Startup normalization is a platform-wide housekeeping pass over heartbeat_tasks that only adjusts task metadata/status, not tenant-facing data.
- **server/heartbeat.ts:277** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — tenant_id not constrained in WHERE clause.
  - _Why false positive:_ This reflection-model normalization is part of the same global startup housekeeping sweep over heartbeat tasks.
- **server/heartbeat.ts:2764** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — write to tenant-scoped heartbeat_tasks without tenant_id in WHERE clause.
  - _Why false positive:_ newTask was just created through storage.createHeartbeatTask with tenantId in the same call path; follow-up update targets that freshly created row by returned id.
- **server/heartbeat.ts:3216** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — tenant_id not included in WHERE clause (possible cross-tenant write).
  - _Why false positive:_ newTask comes from immediately prior tenant-scoped storage.createHeartbeatTask({ tenantId }); the approval-status update applies to that just-created row.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases STEP 1 updates agent_jobs to failed_terminal without any tenant_id constraint — can mark jobs from any tenant terminal in bulk.
  - _Why false positive:_ Lease reclamation is a platform-wide worker maintenance sweep that only transitions job state/housekeeping fields across tenants and returns aggregate touched count.
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases STEP 2 requeues expired 'running' jobs (UPDATE ... SET status='pending') without tenant_id in the WHERE clause — affects jobs across all tenants.
  - _Why false positive:_ Global lease-reclaim maintenance intentionally updates expired jobs across tenants and returns only counts/ids, not tenant-facing mixed data.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs by id/status/attempts without verifying tenant_id — a write against a tenant-scoped table lacks tenant scoping.
  - _Why false positive:_ Worker completion updates a server-generated job row by global PK/id with attempt fencing; no untrusted request-boundary tenant input shown.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} without tenant scoping, then later updates the job row without tenant filtering — unscoped read/write on a tenant-scoped table.
  - _Why false positive:_ Failure handling reads job by server-generated PK inside worker path; id/attempts are internal worker state, not tenant request input.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob's UPDATE to mark terminal failure writes agent_jobs without tenant_id constraint in WHERE clause.
  - _Why false positive:_ Terminal fail update is an internal worker state transition on agent_jobs by global PK with attempts fencing.
- **server/lib/aevo-meta-editor.ts:446** _(upstream-verified)_ — UPDATE on tenant-scoped table `procedure_evolution_runs` uses WHERE(eq(procedureEvolutionRuns.id, runId)) with NO tenant_id predicate — write could affect another tenant's row if IDs collide or be abused.
  - _Why false positive:_ runId comes from the immediately preceding insert of procedure_evolution_runs with the same tenantId in this call path, so the update targets that created row.
- **server/lib/aevo-meta-editor.ts:456** _(upstream-verified)_ — UPDATE on tenant-scoped table `procedure_evolution_runs` (noop_proposal path) filters only by id (runId) and omits tenant_id — violates tenant isolation guarantee.
  - _Why false positive:_ noop-path update uses runId created just above from a tenant-scoped insert in the same function, binding it to this run.
- **server/lib/aevo-meta-editor.ts:499** _(upstream-verified)_ — UPDATE on tenant-scoped table `procedure_evolution_runs` sets status/done but filters only by id (runId) and omits tenant_id — potential cross-tenant write.
  - _Why false positive:_ done-path update uses the same runId returned from the local insert earlier in this function, so ownership is established in-call-path.
- **server/lib/agent-trace.ts:124** _(upstream-verified)_ — persistSpanClose issues UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} with NO tenant_id constraint. agent_trace_spans is tenant-scoped and the close update can therefore affect spans across tenants.
  - _Why false positive:_ Close update uses spanId for a span previously opened in the same call path with tenantId and tracked via the in-memory open promise map.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT against tenant-scoped table `projects` without a tenant_id constraint (reads rows for all tenants). The query `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id` can enumerate / aggregate other tenants' projects.
  - _Why false positive:_ boot/backfill job intentionally scans projects across tenants only to compute per-tenant counts for housekeeping, with no tenant-facing data return
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT on tenant-scoped table `agent_runs` (lines 56–64) omits any tenant_id filter. This read returns/inspects rows across all tenants.
  - _Why false positive:_ read-only cross-tenant operational audit returns bounded stuck-row diagnostics with row-carried tenant_id, not mixed tenant content
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT on tenant-scoped table `mind_tickets` (lines 84–92) omits any tenant_id filter. This read returns/inspects rows across all tenants.
  - _Why false positive:_ read-only cross-tenant maintenance audit over mind_tickets with per-row tenant_id and bounded results
- **server/lib/system-probes.ts:163** _(system-sweep)_ — Database query counting messages (line 163) does a db.select(count(*)) FROM messagesTable with no tenant filter, returning a global message count across tenants.
  - _Why false positive:_ global health probe counts messages platform-wide for system status only; aggregate count only, no tenant content exposure
- **server/martech-bundle.ts:325** _(upstream-verified)_ — UPDATE on tenantVoiceProfiles uses WHERE id = existing.id but does NOT constrain by tenant_id. This is an update to a tenant-scoped table with no tenant_id in the WHERE clause.
  - _Why false positive:_ row is first resolved via getVoiceProfile({ tenantId, profileName }); subsequent update uses that owned row's primary key
- **server/memory-graph.ts:124** _(upstream-verified)_ — In classifyMemoryCategory (new-category branch) the code executes UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} without constraining by tenant_id. This writes to a tenant-scoped table without tenant scoping.
  - _Why false positive:_ catId comes from ensureCategory(category, tenantId, ...) which selects/inserts within the same tenant before this primary-key update
- **server/memory-graph.ts:494** _(upstream-verified)_ — categorizeExistingMemories calls UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} with no tenant_id constraint. This mutates a tenant-scoped table without verifying the row belongs to the caller tenant.
  - _Why false positive:_ mem.id is sourced from a prior SELECT on memory_entries constrained by tenant_id in the same function
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — After creating a memory entry the code runs UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} with no tenant_id constraint. This updates a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ Updates the freshly created row referenced by server-generated entry.id; entry was inserted just above with tenantId, so ownership is established in the same call path.
- **server/memory-intelligence.ts:316** _(upstream-verified)_ — In the 'update' flow the code runs UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (after creating a replacement entry) with no tenant_id constraint. This mutates a tenant-scoped table without tenant scoping.
  - _Why false positive:_ Same pattern: UPDATE targets the newly created replacement row by server-generated entry.id after createMemoryEntry(... tenantId), so tenant ownership is already established.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in VALUES (project_files likely tenant-scoped and app relies on tenant_id column to enforce isolation).
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped transitively via project_id; assertProjectInTenant confirms the parent project belongs to the tenant before insert.
- **server/mpeg-engine.ts:1416** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id in VALUES. Even though assertProjectInTenant is used, the row is written without tenant context.
  - _Why false positive:_ project_files has no tenant_id column and is isolated through tenant-scoped parent project_id; guard checks project ownership before insert.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` with no tenant_id constraint (startup validation queries SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This scans rows across all tenants and may expose multi-tenant sensitive state.
  - _Why false positive:_ Startup validation intentionally scans active subscriptions across tenants for maintenance/refresh handling, using each row's own tenant_id and not returning mixed tenant data to tenant-facing output.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` with no tenant filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace').
  - _Why false positive:_ Boot-time auto-reconnect enumerates provider rows across tenants for maintenance, then processes each tenant separately by carried tenant_id.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh performs a global SELECT on oauth_subscriptions (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) with no tenant_id constraint — it iterates over all tenants' subscriptions.
  - _Why false positive:_ Proactive token refresh is a platform-wide maintenance loop over active subscriptions, handled per row with tenant_id and no tenant-facing mixed data output.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions queries oauth_subscriptions without tenant filter (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE), iterating across all tenants.
  - _Why false positive:_ Force-refresh-all is an explicit cron-style maintenance pass across all tenants, processing each subscription independently by tenant_id.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans performs a global UPDATE on plans (SET status='approved' WHERE status='executing' AND updated_at < ...) with no tenant_id filter — affects all tenants' plans.
  - _Why false positive:_ Boot-time recovery intentionally resets stale execution status across all tenants; it only mutates housekeeping/status fields and returns aggregate ids for internal restart logic.
- **server/plan-executor.ts:788** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ... has no tenant_id filter — reading pending plans across all tenants.
  - _Why false positive:_ Boot-time recovery globally selects pending plan ids to restart internal workers; this is cross-tenant maintenance using per-row ids, not tenant-facing data exposure or mixing.
- **server/process-governor.ts:198** _(global-table)_ — SELECT AVG(duration_ms) FROM heartbeat_logs aggregates heartbeat logs without tenant scope (no tenant_id in WHERE) — cross-tenant read of telemetry/PII.
  - _Why false positive:_ heartbeat_logs is schema-marked with no tenant_id and used for platform-wide telemetry aggregation, so averaging it globally matches an intentionally non-tenant-scoped table.
- **server/process-governor.ts:240** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... lacks tenant_id filter — counts failing agents across all tenants.
  - _Why false positive:_ heartbeat_logs schema has no tenant_id; this is a platform-global failure metric query.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET status_note = ... WHERE id = ${desk.id} omits tenant_id constraint — mutates agent_desks across tenants.
  - _Why false positive:_ desk.id values come from prior SELECT scoped by ad.tenant_id = tenantId in same call path.
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... does not filter by tenant_id — iterates projects across tenants.
  - _Why false positive:_ backfillProjectBrains is a platform-wide backfill/maintenance loop operating across tenants, writing per-project artifacts not tenant-facing mixed output.
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks tenant_id scoping — can enumerate conversations across tenants/projects.
  - _Why false positive:_ conversation enumeration occurs inside the global backfill sweep and processes each project separately.
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ORDER BY created_at ASC omits tenant_id filter — reads message content cross-tenant.
  - _Why false positive:_ message reads are part of the backfill maintenance sweep and are used per conversation/project, not returned in mixed tenant output.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` executed without a tenant_id constraint (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This reads rows across all tenants.
  - _Why false positive:_ runner is a heartbeat-driven global scheduler sweep; it selects rows with row-carried tenant_id and processes each job per-row.
- **server/research-engine.ts:331** _(global-table)_ — Missing tenant guard on personas read: SELECT name FROM personas WHERE id = ${program.persona_id} (persona lookup does not constrain by tenant_id). This can return persona data from other tenants.
  - _Why false positive:_ personas schema has no tenant_id; lookup is against a non-tenant-scoped table.
- **server/research-engine.ts:1053** _(global-table)_ — resolvePersonaId ignores tenant context: SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 — the _tenantId parameter is unused. This can resolve a persona from another tenant and enable cross-tenant reference/placement of persona_id values.
  - _Why false positive:_ resolvePersonaId queries personas, which has no tenant_id and is treated as global/shared.
- **server/research-engine.ts:480** _(fk-transitive)_ — INSERT into project_notes without tenant_id: INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) — if project_notes is a tenant-scoped table (tenant_id NOT NULL), the INSERT omits tenant_id, enabling mis-attribution or constraint violations.
  - _Why false positive:_ project_notes has no tenant_id; insert is transitively scoped via guarded project_id after assertProjectInTenant(projectId, session.tenantId).
- **server/research-engine.ts:488** _(fk-transitive)_ — Second INSERT into project_notes also omits tenant_id (similar to the previous INSERT).
  - _Why false positive:_ project_notes has no tenant_id; insert is transitively scoped via guarded project_id after assertProjectInTenant(projectId, session.tenantId).
- **server/routes.ts:2059** _(signed-url)_ — Presenter public GET looks up presenter_sessions by token using an unscoped SELECT (no tenant_id WHERE). This reads a tenant-scoped table without constraining tenant context; caller-supplied token is used to retrieve the row directly.
  - _Why false positive:_ token is an unguessable capability; lookup by presenter token is the intended access control for this public route
- **server/routes.ts:2091** _(signed-url)_ — Presenter slide image lookup fetches presenter_slide_images by session_id derived from a prior unscoped presenter_sessions lookup. The initial presenter_sessions lookup did not constrain tenant_id, so this chain reads tenant-scoped data without an explicit tenant filter.
  - _Why false positive:_ slide image access is authorized by the presenter capability token verified earlier in the same handler
- **server/routes.ts:2174** _(signed-url)_ — Presenter repair-images endpoint selects presenter_sessions WHERE token = ${token} without constraining by tenant_id. Although the route validates an internal key, it still performs an unscoped read on a tenant-scoped table.
  - _Why false positive:_ session is identified by presenter capability token, and route also requires internal key; tenant_id need not be repeated
- **server/routes.ts:2315** _(signed-url)_ — Public order lookup queries delivery_logs by order/session id using WHERE orderId = ${sessionId} OR stripePaymentId = ${sessionId} with no tenant_id constraint. This reads tenant-scoped delivery logs across tenants based solely on a session identifier.
  - _Why false positive:_ public order lookup is explicitly capability-based on unguessable Stripe session id and returns only public-safe fields
- **server/routes.ts:4334** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The raw SQL inserts project_id, note, author but omits tenant_id which breaks the invariant that every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively through project_id after tenant-checked conversation/project lookup
- **server/routes/conversations.ts:100** _(fk-transitive)_ — SELECT from project_conversations omits tenant scope (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}). A plain SELECT could expose cross-tenant metadata or cause subsequent logic to act on the wrong tenant's rows.
  - _Why false positive:_ project_conversations has no tenant_id column and links tenant-validated project and newly created tenant-owned conversation
- **server/routes/conversations.ts:210** _(upstream-verified)_ — SELECT joining conversations c does not include an explicit tenant_id constraint on the conversations table (SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}). While project tenant is checked, the query lacks c.tenant_id and therefore reads a conversation row without SQL-level tenant scoping.
  - _Why false positive:_ conversation ownership is already enforced via storage.getConversation(id, tenantId) or admin gate before this query
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} has no tenant_id constraint — allows modifying another tenant's row if id is misused.
  - _Why false positive:_ same handler just selected inbox_messages row by id and tenant_id, then updates that exact row by trusted msg.id
- **server/routes/memory.ts:162** _(upstream-verified)_ — SELECT memory_entries ... WHERE id IN (...) AND tenantClause uses `sql`TRUE` for ADMIN_TENANT_ID — effectively removes the tenant_id constraint for admin sessions and issues an unscoped read across tenant-scoped `memory_entries`.
  - _Why false positive:_ unscoped read happens only on platform-admin path guarded by isPlatformAdmin/ADMIN_TENANT_ID; non-admins still get tenant predicate
- **server/routes/metrics.ts:63** _(system-sweep)_ — Multiple queries against tenant-scoped tables (agent_cost_ledger, repair_incidents, etc.) lack a tenant_id constraint — these SELECTs aggregate or list data across all tenants (e.g. cost per tenant, tool calls, tokens) without per-request tenant scoping.
  - _Why false positive:_ Token-gated global /metrics endpoint intentionally aggregates across tenants for operator observability, not tenant-facing output.
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The route verifies project ownership earlier, but the INSERT itself omits the tenant_id column (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped via prior verified projects.id + tenant_id check.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload handler) does not set tenant_id. The code checks the project's tenancy earlier but the INSERT omits tenant_id, violating the rule that every INSERT must pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via prior verified projects.id + tenant_id check.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload handler) does not set tenant_id. Although the route checks project ownership, the INSERT itself omits the tenant_id column.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via prior verified projects.id + tenant_id check.
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks without tenant_id filter. heartbeat_tasks appears tenant-scoped in other queries; a cross-tenant count may be unintended and subsequent actions operate on the global result.
  - _Why false positive:_ Boot-time cleanup intentionally counts tasks platform-wide for housekeeping and does not return tenant data.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true executes without tenant_id constraint and will disable matching tasks across all tenants.
  - _Why false positive:_ Boot-time housekeeping sweep intentionally disables runaway delegation tasks across tenants, mutating only enabled status.
- **server/seed-cleanup.ts:46** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant_id filtering; it aggregates across tenants though later updates disable rows globally.
  - _Why false positive:_ Platform-wide maintenance count for duplicate session-logger tasks; aggregate only, no tenant-facing data exposure.
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant_id constraint and will affect rows across tenants.
  - _Why false positive:_ Global housekeeping disables duplicate session-logger tasks across tenants, changing only status fields.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) is executed without tenant scoping; subsequent disable UPDATE also lacks tenant_id and may disable tenant-specific tasks globally.
  - _Why false positive:_ Platform-wide maintenance count for orphaned repo-examination tasks; aggregate only, no mixed tenant output.
- **server/seed-cleanup.ts:76** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true executes without tenant_id constraint and will modify rows across all tenants.
  - _Why false positive:_ Global housekeeping disables orphaned repo-examination tasks across tenants, mutating only enabled status.
- **server/seed.ts:2158** _(system-sweep)_ — Unscoped read on tenant-scoped table `research_programs` (SELECT id, name, model FROM research_programs WHERE is_active = true) — missing tenant_id filter. This iterates over programs from all tenants.
  - _Why false positive:_ Seed-time model-fix loop intentionally scans all active research programs and updates only housekeeping/config field model server-side.
- **server/seed.ts:2515** _(system-sweep)_ — Unscoped read on `conversations` (SELECT DISTINCT c.id, c.project_id FROM conversations c ... ) with no tenant_id filter. This iterates over conversations from all tenants and then inserts links into project_conversations, risking cross-tenant linking or leakage.
  - _Why false positive:_ Boot-time backfill/housekeeping loop repairs missing links globally; target table project_conversations has no tenant_id and is transitively scoped via project/conversation ids, with no tenant-facing mixed output.
- **server/seed.ts:3356** _(upstream-verified)_ — SELECT from tenant-scoped table heartbeat_tasks with no tenant filter (reads all tenants' heartbeat tasks).
  - _Why false positive:_ This is seed/bootstrap logic that immediately checks global emptiness before inserting fixed system rows for tenant 1; not a tenant-facing read path.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from `heartbeat_logs` (recent failure aggregation) has no tenant_id constraint. This reads tenant-scoped logs across all tenants without an explicit sentinel, violating the requirement that tenant-scoped reads be explicitly filtered.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column, so this aggregation is over a non-tenant-scoped/global table.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM `heartbeat_tasks` executed without any tenant_id constraint. This can purge tasks for all tenants unintentionally.
  - _Why false positive:_ Watchdog cleanup globally purges stale disabled delegation heartbeat_tasks as housekeeping only, returning ids just for internal counts/action text, not tenant data.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM `event_log` executed without tenant_id constraint. This will remove processed/failed event_log entries across all tenants silently.
  - _Why false positive:_ Global housekeeping delete of old processed/failed event_log rows mutates only stale status records and emits aggregate/internal count text.
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM `heartbeat_logs` uses only a created_at predicate and no tenant_id filter. This prunes old logs across all tenants without explicit tenant scoping.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column, so pruning old rows is cleanup on a non-tenant-scoped/global table.
- **server/storage.ts:242** _(upstream-verified)_ — getConversationUnscoped() performs an unscoped SELECT on the tenant-scoped conversations table (no tenant_id filter).
  - _Why false positive:_ Code explicitly documents this dangerous helper for orchestration entrypoints that must derive tenantId from the row, with mandatory downstream validation and audit logging.
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata() executes raw SQL SELECT against messages without constraining by tenant_id (SELECT id, citations FROM messages WHERE id IN (...) ...).
  - _Why false positive:_ attachCitationMetadata only receives msgs already selected with tenant_id scoped in same call path; id list is derived from those rows.
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories() executes a global UPDATE on memory_entries (SET status='archived' WHERE ...expires_at <= NOW()) with no tenant_id constraint — modifies all tenants' rows.
  - _Why false positive:_ Global housekeeping sweep archives expired memory rows across tenants and only mutates status/returns count.
- **server/storage.ts:857** _(system-sweep)_ — archiveStaleMemories() executes a global UPDATE on memory_entries (SET status='archived' WHERE created_at <= ${cutoff} ...) with no tenant_id constraint — affects all tenants.
  - _Why false positive:_ Global stale-memory archiver is maintenance logic across tenants, only changing status and returning aggregate count.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (reads by name only). This violates tenant isolation — reads must be scoped to the caller's tenant.
  - _Why false positive:_ Name is deterministically rewritten with tenantToolName(parsed.name, tenantId) just above, so lookup is effectively tenant-scoped by tenant-derived unique name.
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id with no tenant_id constraint. Reads by PK alone can cross tenant boundaries if tenant guard is not enforced.
  - _Why false positive:_ Read is by freshly inserted server-generated custom_tools primary key from same function, not from untrusted request input.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `custom_tools` uses only eq(custom_tools.id, tool.id) in WHERE — missing tenant_id constraint. Updates must be constrained to the tenant to prevent cross-tenant modification.
  - _Why false positive:_ Update targets the same server-created tool row by trusted pk within validation worker flow, not user-supplied id.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table `custom_tools` uses only eq(custom_tools.id, tool.id) in WHERE — missing tenant_id constraint. This can delete rows belonging to another tenant.
  - _Why false positive:_ Delete reverts the same just-inserted server-side tool row by trusted pk in internal validation flow.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment usageCount on tenant-scoped `custom_tools` uses only the row id in WHERE (eq(custom_tools.id, tool.id)) and lacks a tenant_id constraint.
  - _Why false positive:_ tool row is selected immediately above with tenant_id = tenantId; subsequent usageCount update uses that verified row id in same call path.
- **server/tools.ts:4543** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The code inserts a project-scoped row (project_notes) without providing tenant_id in the VALUES clause.
  - _Why false positive:_ project_notes has no tenant_id by schema and is transitively scoped through verified project_id after assertProjectInTenant.
- **server/tools.ts:4547** _(fk-transitive)_ — INSERT into project_files omits tenant_id. A project_files row is inserted using projectId but no tenant_id value is provided.
  - _Why false positive:_ project_files has no tenant_id by schema and insert is gated by assertConversationInTenant plus assertProjectInTenant on parent project
- **server/tools.ts:6392** _(fk-transitive)_ — INSERT into project_files omits tenant_id. After asserting project ownership, code inserts into project_files without specifying tenant_id in the VALUES list.
  - _Why false positive:_ project_files has no tenant_id by schema; insert is correctly scoped transitively via verified tenant-owned project_id
- **server/tools.ts:6925** _(fk-transitive)_ — INSERT into project_files omits tenant_id. In create_slideshow_video the code inserts a project_files row (after asserting project ownership) but does not set tenant_id in the INSERT.
  - _Why false positive:_ project_files has no tenant_id by schema and row is inserted only after assertProjectInTenant verifies parent project ownership
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT statement `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` is missing an explicit tenant identifier, relying solely on project_id ownership being asserted earlier. This violates the invariant that every INSERT into tenant-scoped tables must include the caller's tenant in the row.
  - _Why false positive:_ project_files is child of tenant-scoped project and has no tenant_id column; parent project ownership is asserted first
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against tenant-scoped table `browser_workflows` without constraining by tenant_id (sql: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`). This can modify another tenant's workflow entry.
  - _Why false positive:_ workflow row was selected immediately above with tenant_id filter, and update uses that verified row's id
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE on `projects` table performed without scoping by tenant_id (sql: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`). This can update a project belonging to another tenant.
  - _Why false positive:_ projId comes from same-call-path SELECT joining projects with p.tenant_id = tenantId before this timestamp-only update
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE statement modifies knowledge_triples by id only without constraining tenant_id. The loop at lines ~84-87 executes: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} — this can update rows belonging to other tenants if an attacker supplies an id that matches another tenant's row.
  - _Why false positive:_ each updated id comes from immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` is executed without constraining to the caller's tenant (no WHERE tenant_id = ${tenantId}). This can expose provider key counts/details across tenants.
  - _Why false positive:_ provider_keys has no tenant_id by schema and this query is further restricted to admin tenantId === 1
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM `provider_keys` is executed without filtering by tenant_id. This reads tenant-scoped provider_keys globally for the caller (currently done under tenantId === 1) and risks cross-tenant exposure.
  - _Why false positive:_ provider_keys has no tenant_id by schema and this aggregate query is explicitly admin-only via tenantId === 1
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} is executed without also constraining by tenant_id. Although `id` was selected earlier with a tenant_id filter, the UPDATE itself lacks a tenant_id WHERE clause and could update rows outside the current tenant in some race or misuse scenarios.
  - _Why false positive:_ id being updated was selected just above from memory_entries with tenant_id = tid and matching persona/category constraints
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) is executed without constraining by tenant_id — the UPDATE may affect rows across tenants if ids contain non-owned values.
  - _Why false positive:_ ids array is derived solely from rows selected in same call path with tenant_id = tid before the update
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} in recordStrategicWin is performed without a tenant_id predicate. The UPDATE may modify rows belonging to other tenants if id is not validated on the UPDATE statement itself.
  - _Why false positive:_ updated id comes from immediately preceding tenant-scoped SELECT on memory_entries with tenant_id = tid
- **server/video-job-runner.ts:236** _(system-sweep)_ — recoverStaleVideoJobs performs a global UPDATE on tenant-scoped table `videoJobs` without constraining by tenant_id. This will mark jobs across all tenants as failed instead of scoping to the intended tenant(s).
  - _Why false positive:_ global stale-job recovery sweeper intentionally updates housekeeping status fields across all tenants and does not mix or return tenant data.
- **server/video-job-runner.ts:206** _(internal-worker-pk)_ — isCancelRequested queries `videoJobs` by jobId only (WHERE jobId = ...) with no tenant_id constraint. Reading tenant-scoped cancelRequested without verifying tenant ownership allows cross-tenant observation of job state and may be abused if jobIds are guessable.
  - _Why false positive:_ runner-internal function reads by globally unique server-generated jobId during background processing, not from a cross-tenant request boundary.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT on messages (SELECT count(*) FROM messages WHERE created_at > ...) lacks a tenant_id WHERE clause. messages is a tenant-scoped table and this reads across all tenants.
  - _Why false positive:_ system-wide invariant/health check intentionally computes aggregate chat counts across tenants for diagnostics only, with no tenant-facing data exposure.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — Raw SQL built via sql.raw reads COUNT/MAX from subsystem table (`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) ... FROM ${spec.table}`) with no tenant_id constraint. The subsystems listed (e.g. knowledge_nudges, tool_performance) can be tenant-scoped.
  - _Why false positive:_ maintenance/diagnostic subsystem freshness check reads only aggregate COUNT/MAX across tables and emits health findings, not tenant content.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Memory-growth probe uses sql.raw to SELECT COUNT and pg_total_relation_size FROM ${tbl.name} with no tenant_id restriction. Several tables in MEMORY_TABLES (e.g. agent_knowledge, memory_entries, messages, graph_memory) are typically tenant-scoped.
  - _Why false positive:_ memory-growth tripwire is a platform-wide observability check using aggregate row counts/table size only, with no tenant data returned or mixed into tenant-facing output.
- **server/auto-memorize.ts:74** _(system-sweep)_ — INSERT into `agent_knowledge` hardcodes tenant_id = 1. This writes a tenant-scoped table to tenant 1 regardless of the acting tenant, enabling cross-tenant poisoning and incorrect attribution.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:60-81 setWatermark writes a single global housekeeping watermark row (category='auto_memorize_watermark') for the platform-wide runAutoMemorize sweep; it is never returned to a tenant surface (only read by getWatermark:45-58). tenant_id=1 is the sweep's fixed housekeeping bucket, not acting-tenant data
- **server/embeddings.ts:684** _(internal-worker-pk)_ — UPDATE against tenant-scoped tables (memory_entries | agent_knowledge) performed without scoping by tenant_id (WHERE id = ${id} only). This allows updating rows across tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/embeddings.ts:684 — storeEmbeddingVec is only used by embedding/backfill workers (server/embeddings.ts:693-706; server/research-engine.ts:518,1135; server/dream-consolidation.ts:363,422; server/storage.ts:806,818), with IDs obtained from database-created rows rather than request input. The UPDATE targets a globally unique serial primary key, and this worker path does not expose an attac
- **server/minerva-planner.ts:386** _(upstream-verified)_ — SELECT probe (SELECT status, ceo_decision FROM plans ...) conditionally includes `AND tenant_id = ${args.tenantId}`. When args.tenantId is omitted this SELECT runs without tenant scoping, enabling cross-tenant reads (IDOR).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Child-plan lookup runs inside decidePlan's revise-idempotency branch, gated by the same route ownership check at agentic-policy.ts:113; args.planId is the verified plan id.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped table without filtering by tenant_id — can reveal proposals belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/proposal-verifier.ts:31-35 SELECT by proposalId; every caller is internal (server/agentic/autonomous-closer.ts:86-103 with tenant-scoped candidate ids, plus scripts/reverify-proposals.ts and scripts/drain-and-verify-proposals.ts). No request/tool-arg id path.
- **server/proposal-verifier.ts:241** _(internal-worker-pk)_ — persist() updates code_proposals (UPDATE code_proposals SET verification_status=... WHERE id = ${proposalId}) without a tenant_id constraint — writes can affect proposals across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — After a rebase race the code clears rebased_at via UPDATE code_proposals ... WHERE id = ${proposalId} with no tenant_id filter — mutates validation_result for proposals across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — Rebase path performs UPDATE code_proposals SET status='rejected', verification_status='skipped', ... WHERE id = ${proposalId} without tenant_id scoping — cross-tenant mutation risk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/routes.ts:2144** _(upstream-verified)_ — Presenter creation INSERT sets tenant_id directly from req.body (INSERT INTO presenter_sessions (tenant_id, ...) VALUES (${tenantId}, ...)). tenantId is caller-supplied and is not validated against the caller's session/ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): routes.ts:2141 presenter_sessions INSERT is gated by a constant-time SESSION_SECRET internal-key check at routes.ts:2132-2133 (403 on mismatch); req.body is therefore a trusted server-to-server boundary, not an untrusted caller
- **server/routes/admin-zombie-detector.ts:54** _(upstream-verified)_ — Query aggregates conversation activity (SELECT ... FROM conversations ...) without a tenant_id constraint. The derived 'used' CTE and subsequent join on personas therefore reflect cross-tenant conversation usage.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/routes/admin-zombie-detector.ts:18-21 admin+ADMIN_TENANT_ID guard protects the handler; the conversations CTE (54-67) is intentional cross-tenant admin persona-usage analytics behind that guard.
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE sculptor_sessions executed without tenant_id constraint inside monitorSession (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). This permits cross-tenant status/result mutations by session id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:106 monitorSession is called only with session.id freshly created at 74-82 (server-generated PK); no caller passes a request-derived id
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions executed without tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). This can overwrite another tenant's session review by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — UPDATE agent_cost_ledger executed without tenant_id constraint in settleFusionReservation (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}). agent_cost_ledger is used per-tenant; updating by id alone can settle or tamper with another tenant's reservation row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — UPDATE agent_cost_ledger executed without tenant_id constraint in releaseFusionReservation (UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId}). This can release/zero another tenant's reservation row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` is performed using only the experiment id: .where(eq(experiments.id, experiment.id)). This allows cross-tenant row modification if an attacker knows an id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-improvement.ts:413** _(internal-worker-pk)_ — UPDATE in error-handling path updates `experiments` by id only (.where(eq(experiments.id, experiment.id))) without tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:1311** _(upstream-verified)_ — getAllDataForExport() performs many unscoped SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant-specific keys, heartbeat_tasks, etc.), exporting all tenants' data with no tenant filter or admin guard.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} updates a tenant-scoped table using only the row id and NO tenant_id constraint. This allows a caller who can supply or guess an id to modify another tenant's ledger row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (release path) updates a tenant-scoped table by id only, with no tenant_id constraint. This lets code release (modify) reservation rows belonging to other tenants if an id is guessed or misrouted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE agentActivity (completeActivity) uses WHERE id = <activityId> with no tenant_id constraint — allows updating another tenant's activity row by id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/agentic-features.ts:638** _(upstream-verified)_ — SELECT existing outreach_enrollments checks for an existing active enrollment but omits tenant_id in the WHERE, potentially matching rows from other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/auto-memorize.ts:47** _(system-sweep)_ — getWatermark SELECT from tenant-scoped table `agent_knowledge` has no tenant_id constraint. This reads watermark rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL runs without scoping the UPDATE by tenant_id. If the earlier query returned rows from other tenants, this UPDATE could modify another tenant's conversation row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): auto-transcript.ts:165 UPDATE conversations SET project_id inside backfillProjectTranscripts platform sweep; row comes from a tenant-consistent join (auto-transcript.ts:153-161 JOIN projects p ON p.tenant_id=c.tenant_id) and only sets housekeeping project_id
- **server/chat-engine.ts:4407** _(internal-worker-pk)_ — Call to storage.attachCitations(...) is made without passing tenant context. If attachCitations writes to a tenant-scoped table it may perform INSERT/UPDATE without an explicit tenant_id, violating tenant isolation (or it may operate on the wrong tenant's rows).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/chat-engine.ts:4332 — assistantMsg.id is server-generated two lines above (chat-engine.ts:4329, created WITH tenantIdForScope); attachCitations updates only that fresh id — never caller input.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on tenant-scoped table `commitments` performed without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can unclaim/modify a row across tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/delivery-pipeline.ts:1071** _(global-table)_ — getDeliveryByStripePayment performs SELECT FROM delivery_logs WHERE stripe_payment_id = $1 with no tenant_id filter. This can expose another tenant's delivery data by payment id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:50** _(signed-url)_ — getDfyIntakeByToken selects from dfy_intake_forms WHERE token = $1 with no tenant_id constraint. Reading by token without tenant scoping can leak other tenants' intake forms.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/job-queue.ts:274** _(internal-worker-pk)_ — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id check — allows cancelling another tenant's job by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:282** _(upstream-verified)_ — retryJob resets job state via UPDATE ... WHERE id = ${id} without tenant_id constraint — mutates tenant-scoped rows unscoped by tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:283 — retryJob is only reachable via POST /api/admin/agent-jobs/:id/retry (server/routes/agent-jobs.ts:62-70), gated by requirePlatformAdmin. Same admin-only operator inbox as cancelJob — cross-tenant retry by id is deliberate platform-admin capability, not attacker-influenced.
- **server/lib/bwb-job-progress.ts:141** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `video_jobs` uses where(scope(jobId,...)) but scope() may omit the tenant predicate when BWB_TENANT_ID is not present, resulting in writes that lack tenant_id filtering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:166** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `video_jobs` (updateBwbChapters) calls .where(scope(jobId,...)) which can omit tenant_id when the env/threaded tenant is missing — write may cross tenant rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:200** _(internal-worker-pk)_ — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope() may omit tenant_id if the env-threaded tenant is missing — read can leak another tenant's job data.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): jobId is an unguessable random id (vj_<base36>_<6-byte-hex>); scope() warns once when BWB_TENANT_ID unthreaded — documented defense-in-depth.
- **server/memory-graph.ts:183** _(upstream-verified)_ — refreshCategoryCount updates memory_categories using a subquery that counts memory_entries WITHOUT filtering by tenant_id, and the outer UPDATE does not constrain the memory_categories row by tenant. This can mix counts across tenants and update rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/process-governor.ts:976** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant_id constraint — increments trigger state without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant_id filter — reads project metadata cross-tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/routes.ts:1118** _(upstream-verified)_ — Uploads middleware selects all file_storage rows matching filename without restricting by tenant_id (db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This fetches tenant-scoped rows across tenants and relies on app-side filtering to decide ownership.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT of owner tenantIds IS the isolation check — non-owners get 404 on the next line.
- **server/routes.ts:2423** _(signed-url)_ — Asynchronous order-recovery work queries delivery_logs by lower(customer_email) = ${rawEmail} without a tenant filter, reading tenant-scoped rows across tenants while processing the public order-lookup flow.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2421 — order-recovery flow returns a generic response (anti-enumeration) and emails a one-time code ONLY to the row's own customer_email; inbox ownership is the access control by design.
- **server/routes.ts:2567** _(signed-url)_ — Order verify flow selects delivery_logs by lower(customer_email) = ${rawEmail} (no tenant filter) and returns a tenant's orders after consuming a one-time code. This reads tenant-scoped rows without scoping by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2313 — cs_… Stripe checkout-session id is an unguessable capability URL (regex-validated); endpoint returns only masked public-safe fields — customer re-download by design.
- **server/seed.ts:36** _(system-sweep)_ — Unscoped read on tenant-scoped table `research_sessions` (SELECT COUNT(*) FROM research_sessions) — no tenant_id filter. This reads across all tenants and can leak global state or make decisions using cross-tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:34-36 inside importDevSnapshot() (12-); SELECT COUNT(*) is only a boot-time gate to decide whether to import a dev snapshot; returns a count, never row data to a tenant surface.
- **server/self-improvement.ts:170** _(upstream-verified)_ — Unscoped SELECT against tenant-scoped table `experiments` when tenantId is not provided. detectStagnation builds a conditions array but if tenantId is omitted the query runs without tenant_id in the WHERE clause.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:511** _(upstream-verified)_ — getExperimentHistory can return experiments across all tenants when tenantId is not supplied (it falls back to db.select().from(experiments) without a tenant filter).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:399** _(internal-worker-pk)_ — attachCitations() issues UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id constraint, updating tenant-scoped rows by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:806** _(internal-worker-pk)_ — updateMemoryEmbedding() updates memory_entries by id only (WHERE id = id) without any tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding() updates agent_knowledge by id only (WHERE id = id) with no tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id filter. This reads pairing rows across all tenants, violating the invariant that tenant-scoped reads must be constrained to a caller/tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without constraining by tenant_id. Reading a tenant-scoped `conversations` row by id alone allows cross-tenant data access/verifications without an application-level tenant filter.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
- **server/minerva-planner.ts:367** _(deferred)_ — UPDATE on tenant-scoped table `plans` may execute without a tenant constraint — the query conditionally appends `AND tenant_id = ${args.tenantId}`. If args.tenantId is omitted, the UPDATE will run without scoping to a tenant, allowing cross-tenant modifications.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): decidePlan CAS UPDATE only constrains tenant_id when args.tenantId != null — optional-tenant fail-open; hardening = make tenantId mandatory on the decision path.
- **server/minerva-planner.ts:403** _(deferred)_ — SELECT child plan (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...) conditionally appends `AND tenant_id = ${args.tenantId}`. If args.tenantId is omitted this read is unscoped and can expose/return rows from other tenants.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): child-plan SELECT applies the tenant filter conditionally (args.tenantId != null) — optional-tenant fail-open; hardening = require tenantId.
- **server/minerva-planner.ts:549** _(deferred)_ — getPlan has an unchecked branch that executes `SELECT * FROM plans WHERE id = ${planId}` when tenantId is omitted. This returns plan rows without tenant scoping (cross-tenant read) when called without tenantId.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
- **server/delivery-pipeline.ts:203** _(deferred)_ — db.update(deliveryLogs).set(...) is executed without scoping the UPDATE to the delivery's tenant (no tenant_id constraint in the WHERE clause). This allows modifying another tenant's delivery row.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
