# Tenant-Isolation Audit Report

_Generated 2026-07-19T21:27:11.336Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **542/542** file(s) audited · chunks 51/51 OK (0 failed)
- Findings: **311** (CRITICAL 99 · HIGH 165 · MEDIUM 45 · LOW 2)
- Precision second-pass (`gpt-5.4`): **160** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (99)

- **server/auth.ts:1142** — clearAllSessions executes DELETE FROM auth_sessions with no WHERE clause — this wipes all sessions for all tenants unconditionally.
  - _Fix:_ Remove or restrict this operation. If a full wipe is ever needed, require a platform-admin-only guarded path (with explicit confirmation, audit logs, and safeguards) or accept a tenant-scoped parameter and delete only sessions for the specified tenant. Never run an unscoped DELETE on tenant-scoped session data in normal code paths.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — performing a write to the tenant-scoped `conversations` table without constraining tenant_id. This can modify another tenant's row if an attacker controls the c values returned by upstream queries.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${c.tenant_id}) or perform the update within a context that validated ownership. Prefer using the tenant id discovered earlier (c.tenant_id) in the WHERE clause to enforce isolation on writes.
- **server/chat-engine.ts:571** — SELECT from tenant-scoped table `project_files` is missing a tenant_id constraint (only filtered by project_id). This allows a verified project_id to be used to read files across tenants if project ownership assumptions are ever bypassed.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or JOIN to projects and require p.tenant_id = ${tenantId}. Prefer using Drizzle's eq(fileStorage.tenantId, tenantId) style so every read against tenant-scoped tables includes an explicit tenant_id check instead of relying solely on prior in-memory validation.
- **server/chat-engine.ts:582** — SELECT from tenant-scoped table `project_notes` uses only project_id in the WHERE clause and omits tenant_id. Even though projectId was previously fetched, the query itself lacks an explicit tenant constraint, risking cross-tenant data exposure if assumptions change.
  - _Fix:_ Constrain the query with tenant_id (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or JOIN to projects with p.tenant_id = ${tenantId}. Use Drizzle's where(eq(table.tenantId, tenantId)) pattern to make tenant scoping explicit and robust.
- **server/chat-engine.ts:631** — SELECT from tenant-scoped table `compaction_archives` filters only on conversation_id and does not include tenant_id. Reading an archive by conversation_id without an explicit tenant constraint can leak another tenant's compacted history if conversation_id handling is manipulated.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or join to conversations and require conversations.tenant_id = ${tenantId}. Prefer explicit tenant_id checks on every read of tenant-scoped tables rather than relying on upstream validation.
- **server/chat-engine.ts:649** — SELECT from tenant-scoped table `messages` is executed using only conversation_id (WHERE conversation_id = ${c.id}) and lacks tenant_id. This can expose message content across tenants if conversation_id references are abused.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or join to the conversations table ensuring conversation.tenant_id = ${tenantId}. Use Drizzle's eq(table.tenantId, tenantId) helper to enforce tenant scoping on every messages read.
- **server/chat-engine.ts:4327** — Call to storage.attachCitations(...) does not pass tenantId. If attachCitations inserts into a tenant-scoped citations table, the INSERT may omit tenant_id (or rely on implicit assumptions), violating tenant isolation.
  - _Fix:_ Ensure attachCitations is provided an explicit, trusted tenant id (e.g. tenantIdForScope) or modify attachCitations/storage implementation to require and use tenantId in its INSERTs. Alternatively, make the function validate ownership of the referenced message id and include tenant_id in every row it writes (or apply a WHERE tenant_id = ? on updates). Never rely on implicit/global state for tenant scoping.
- **server/chat-engine.ts:4591** — Call to storage.createMemoryEntry({...}) does not include tenantId. This is an INSERT into memory entries without an explicit tenant_id, risking cross-tenant data leakage or orphaned rows.
  - _Fix:_ Pass the tenantId into createMemoryEntry (e.g. include tenantId: tenantId or tenantIdForScope) so every INSERT explicitly sets tenant_id. If createMemoryEntry cannot be changed, add a wrapper here that injects the tenant id or make the storage layer validate the caller's tenant before inserting.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without any tenant_id constraint (scanAndEscalate reads all tenants' commitments). This allows cross-tenant reads.
  - _Fix:_ Restrict the query to a single tenant (e.g. add AND tenant_id = ${tenantId}) or move to a clearly-admin-only path that enforces strong operator authorization and auditing. If the intent is platform-level admin fan-in, use a dedicated admin-only table/aggregation or ensure the callsite requires and verifies an admin identity before running this query.
- **server/compaction.ts:399** — INSERT INTO `compaction_archives` does not set tenant context (no tenant_id provided). Writing archive content without explicit tenant scoping can lead to cross-tenant data exposure.
  - _Fix:_ Include tenant_id in the INSERT (and validate it before writing), or ensure the table is intentionally global (documented schema) and enforce ownership by validating the conversation→tenant mapping before writing. Prefer storing tenant_id as a NOT NULL column and always populate it from a verified tenant context.
- **server/data-protection.ts:125** — DELETE FROM messages WHERE conversation_id = ${conv.id} is executed without constraining by tenant_id. This deletes tenant-scoped rows (messages) without confirming tenant ownership of those rows.
  - _Fix:_ Restrict deletes to the owning tenant (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}), or perform the delete via a join that asserts the conversation belongs to the expected tenant. Prefer explicit tenant predicates for every destructive SQL on tenant-scoped tables.
- **server/data-protection.ts:126** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} is executed without a tenant_id constraint. This removes tenant-scoped archive rows without ensuring they belong to the targeted tenant.
  - _Fix:_ Add AND tenant_id = ${conv.tenant_id} (or equivalent) to the DELETE, or validate ownership before deleting. All DELETEs against tenant-scoped tables must include the tenant predicate.
- **server/data-protection.ts:127** — DELETE FROM conversations WHERE id = ${conv.id} is executed without constraining by tenant_id. This can remove conversations across tenants if IDs collide or are mis-bound.
  - _Fix:_ Include tenant scoping: DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Ensure all deletes on tenant-scoped tables include tenant_id checks.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog updates the tenant-scoped delivery_logs table without constraining the row to a tenant (WHERE clause uses only delivery_logs.id). This allows modifying another tenant's delivery row by id.
  - _Fix:_ Require and pass the tenantId into updateDeliveryLog (or accept an options param) and include it in the WHERE clause, e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). All callers must supply the caller's tenant context or use a strict internal-trusted branch.
- **server/delivery-pipeline.ts:683** — SELECT on delivery_logs by stripePaymentId is performed without a tenant filter (where(eq(deliveryLogs.stripePaymentId, req.stripePaymentId))). A caller supplying stripePaymentId can enumerate or read another tenant's delivery.
  - _Fix:_ Scope the lookup by tenant (e.g. add AND eq(deliveryLogs.tenantId, req.tenantId)) or require the caller to pass an explicit tenantId and include eq(deliveryLogs.tenantId, tenantId) in the WHERE. If this is meant to be global/trusted, make that explicit and restrict the API to trusted internal callers only.
- **server/embeddings.ts:684** — storeEmbeddingVec performs an UPDATE on tenant-scoped tables (memory_entries / agent_knowledge) without constraining the row by tenant_id.
  - _Fix:_ Require and accept a tenantId in storeEmbeddingVec (or otherwise resolve/verify ownership) and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (use parameterized Drizzle expressions rather than relying only on sql.raw). Ensure callers only invoke this for rows belonging to that tenant or run as an explicit admin-only operation with additional safeguards.
- **server/embeddings.ts:726** — backfillEmbeddingVecs performs UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} with no tenant_id constraint, allowing updates across tenants by id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), and make the function operate per-tenant or verify ownership before updating. Avoid relying solely on id for multi-tenant tables.
- **server/embeddings.ts:743** — backfillEmbeddingVecs performs UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} without constraining by tenant_id, permitting cross-tenant updates.
  - _Fix:_ Require tenant scoping for the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or run per-tenant with explicit admin checks. Never update tenant-scoped rows by id alone without verifying tenant ownership.
- **server/felix-brain.ts:19** — rehydrateTaskStateOnBoot SELECTs agent_runs (SELECT ... FROM agent_runs WHERE status IN ('running','pending') ...) without filtering by tenant_id, then injects data into an in-memory taskStateStore keyed only by conversation id — this can mix state across tenants and leak tenant data into process memory.
  - _Fix:_ Restrict the query to a specific tenant (require tenantId) or iterate per-tenant and include WHERE tenant_id = ${tenantId}. Alternatively, persist tenant_id in the in-memory key (e.g. use `${tenantId}:${conversationId}`) and verify ownership before rehydrating. Ensure only authorized admin contexts run cross-tenant maintenance and log/audit such operations.
- **server/felix-loop.ts:676** — UPDATE against tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} without constraining by tenant_id. This lets a caller (or a bug) update another tenant's run row if runId is misbound.
  - _Fix:_ Add an explicit tenant constraint to the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) or otherwise ensure the update is scoped to the caller's tenant. Prefer using parameterized/drizzle eq(table.tenantId, tenantId).
- **server/felix-loop.ts:690** — Error-path UPDATE against tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} without constraining by tenant_id. This can update a run from a different tenant.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) so recovery/error writes cannot affect other tenants' rows.
- **server/google-drive.ts:560** — UPDATE against tenant-scoped table 'projects' uses WHERE projects.id = ${projectId} without constraining by tenant_id. Even though code validated ownership earlier, the UPDATE itself lacks an explicit tenant constraint and could modify another tenant's row if projectId is tampered or a logic bug occurs.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or WHERE id=${projectId} AND tenant_id=${tenantId}). Always include the tenant filter on writes to tenant-scoped tables.
- **server/heartbeat.ts:101** — Unscoped UPDATE on tenant-scoped table `research_schedules`: UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id restriction in WHERE clause.
  - _Fix:_ Include the tenant constraint in the WHERE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage-layer method that enforces tenant ownership before updating rows.
- **server/heartbeat.ts:106** — Unscoped UPDATE on tenant-scoped table `research_schedules`: UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use an API that validates the schedule belongs to the tenant before persisting changes.
- **server/heartbeat.ts:111** — Unscoped UPDATE on tenant-scoped table `research_schedules`: UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id in WHERE.
  - _Fix:_ Add tenant_id to the WHERE clause (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call into a tenant-aware storage helper to avoid cross-tenant updates.
- **server/heartbeat.ts:119** — Unscoped UPDATE on tenant-scoped table `research_schedules` during error recovery: UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id filter.
  - _Fix:_ Include AND tenant_id = ${sched.tenant_id} in the WHERE clause or perform the update via a storage function that asserts tenant ownership to prevent modifying other tenants' schedules.
- **server/heartbeat.ts:257** — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} — missing tenant_id in WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenant_id}) or call a tenant-aware storage method (e.g. storage.updateHeartbeatTask) that enforces tenant ownership before writing.
- **server/heartbeat.ts:261** — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} — no tenant_id filter.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${t.id} AND tenant_id = ${t.tenant_id}) or use storage-layer APIs which validate the tenant context to prevent cross-tenant modification.
- **server/heartbeat.ts:2955** — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id restriction.
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update via a tenant-aware storage helper so approval_status cannot be changed for tasks belonging to other tenants.
- **server/heartbeat.ts:3406** — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint present.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or use the storage API that enforces tenant ownership when mutating heartbeat_tasks to avoid altering other tenants' tasks.
- **server/lib/scheduled-post-runner.ts:62** — SELECT in CTE pulls rows from tenant-scoped table `scheduled_posts` without any tenant_id filter (SELECT id FROM scheduled_posts ...). This reads tenant data across all tenants under a non-tenant-bound caller context.
  - _Fix:_ Restrict the SELECT to an explicit tenant_id (e.g. WHERE tenant_id = ${callerTenantId} AND ...), or run the runner per-tenant (iterate tenants and include tenant_id in the query). If the runner must be system-scoped, make the cross-tenant intent explicit and audit-approved and ensure all changes are guarded by tenant checks at UPDATE time (see below).
- **server/lib/scheduled-post-runner.ts:70** — UPDATE of tenant-scoped table `scheduled_posts` (UPDATE scheduled_posts sp SET ... FROM due WHERE sp.id = due.id) in the same CTE has no tenant_id constraint. This writes rows across tenants without enforcing the caller's tenant context.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE sp.id = due.id AND sp.tenant_id = ${callerTenantId}), or perform a per-tenant locked-select+update so every mutation is explicitly constrained to the intended tenant. If updating multiple tenants intentionally, document and gate the admin privilege and add logging/audit controls.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} executes without also constraining by tenant_id. This can update another tenant's row if an attacker or race supplies/matches an id from a different tenant.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) so the write is atomically limited to the owning tenant. Prefer binding both id+tenant_id in the WHERE clause for every UPDATE/DELETE on tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:191** — Exception-path UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' omits tenant_id. The error-handling path therefore may modify rows belonging to other tenants.
  - _Fix:_ Add tenant_id to the WHERE clause in the error-path update (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} AND status = 'publishing') to ensure rollback/release/recovery logic only affects the intended tenant's rows.
- **server/memory-graph.ts:194** — INSERT into `memory_links` does not set tenant_id (INSERT INTO memory_links (... ) VALUES (...)). If memory_links is tenant-scoped and tenant_id is NOT NULL with no default, this either fails or silently writes cross-tenant/undetermined rows, allowing cross-tenant link pollution.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId})). Ensure callers pass an explicit, validated tenantId (or derive from the referenced memory entries and assert ownership first).
- **server/minerva-planner.ts:356** — UPDATE on tenant-scoped table `plans` lacks tenant scoping (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING id, tenant_id, objective, plan_json). This allows changing a plan by id without verifying the caller's tenant owns the plan (potential cross-tenant modification).
  - _Fix:_ Require and enforce tenantId for the actor and include AND tenant_id = ${tenantId} in the WHERE clause for the CAS UPDATE, or otherwise validate ownership prior to mutating the row. Do not perform cross-tenant UPDATEs on plan records.
- **server/oauth-subscriptions.ts:906** — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE) — reads all tenants' subscriptions including sensitive tokens/refresh data.
  - _Fix:_ Avoid global scans. Either run this logic per-tenant (iterate known tenant IDs) or add an explicit tenant_id filter (e.g. WHERE tenant_id = ${tenantId}) and limit scope to the caller's tenant. Ensure background jobs explicitly scope / validate tenant context before reading oauth_subscriptions.
- **server/oauth-subscriptions.ts:915** — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace') — returns rows across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (add WHERE tenant_id = ${tenantId}) or otherwise ensure the caller is authorized to iterate all tenants. Do not rely on implicit global access to tenant-scoped tables.
- **server/oauth-subscriptions.ts:956** — UPDATE against tenant-scoped table `oauth_subscriptions` using only id in WHERE (UPDATE ... WHERE id = ${sub.id}) — missing tenant_id constraint. This can modify another tenant's row if IDs collide or are discovered.
  - _Fix:_ Include tenant scoping in UPDATEs, e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} (or the caller's tenant). Prefer parameterizing tenantId and verifying ownership before modifying rows.
- **server/oauth-subscriptions.ts:981** — UPDATE against tenant-scoped table `oauth_subscriptions` using only id in WHERE (UPDATE ... WHERE id = ${sub.id}) — missing tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or the known owner) to the WHERE clause to guarantee tenant isolation when updating subscription state.
- **server/oauth-subscriptions.ts:1258** — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) used by proactiveTokenRefresh — iterates all tenants' active subscriptions.
  - _Fix:_ Proactive refresh must be performed per-tenant or otherwise explicitly scoped. Either iterate a validated list of tenant IDs and call tenant-scoped queries, or add a tenant_id predicate so reads only touch a single tenant's rows at a time.
- **server/oauth-subscriptions.ts:1328** — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE) used by forceRefreshAllSubscriptions — reads across all tenants.
  - _Fix:_ Force-refresh should explicitly scope to tenant(s). Implement a per-tenant iteration or require a tenantId parameter and add WHERE tenant_id = ${tenantId}. Never perform global reads/writes on tenant-scoped tables without strict authorization and rationale.
- **server/orchestrator-ledger.ts:452** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${row.id} without also constraining tenant_id. The save path can thus update a memory entry that belongs to another tenant if id is reused or misbound.
  - _Fix:_ Always include tenant_id in write predicates. Change the UPDATE to include AND tenant_id = ${tenantId} (and/or verify ownership by selecting the row with tenant_id prior to updating) so an attacker or bug cannot alter another tenant's memory entry.
- **server/plan-executor.ts:117** — UPDATE plans ... WHERE id = ${planId} (appendExecutionLog) executes against the tenant-scoped plans table without constraining by tenant_id. This allows cross-tenant mutation/visibility if an attacker or bug supplies another plan id.
  - _Fix:_ Always include tenant scoping in writes/reads against plans. For example, pass an authoritative tenantId into appendExecutionLog and use WHERE id = ${planId} AND tenant_id = ${tenantId}. Alternatively, read the plan row first, verify/derive its tenant, then use that tenant in subsequent UPDATE/WHERE clauses.
- **server/plan-executor.ts:513** — CAS UPDATE that claims a plan: UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ... — missing tenant_id constraint. This can flip/claim a plan belonging to another tenant.
  - _Fix:_ Scope the claim by tenant (e.g. WHERE id = ${planId} AND status = 'approved' AND tenant_id = ${expectedTenantId}) or perform a prior verified read to obtain and validate the plan's tenant before CAS-ing the status. Ensure executePlan receives/validates the authoritative tenant context.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — clearing/failing a plan without constraining by tenant_id. Unscoped writes against tenant-scoped plans table can affect other tenants' plans.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}) or derive tenant from a verified plan row and use it for the update.
- **server/plan-executor.ts:725** — Final status update: UPDATE plans SET status = ${finalStatus}, ... WHERE id = ${planId} — no tenant_id constraint. This updates a tenant-scoped row without tenant scoping, risking cross-tenant modification and leakage.
  - _Fix:_ Restrict updates to the owning tenant (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}). Prefer always carrying an explicit tenantId into DB-write helpers so every statement can include tenant scoping.
- **server/plan-executor.ts:760** — Error-path update: UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' — missing tenant_id. Even in cleanup paths the plans table is modified without tenant scoping.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or verify the plan's tenant before doing the update). Ensure cleanup/error handlers preserve tenant scoping consistently.
- **server/plan-executor.ts:775** — Boot-time recovery: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — this global update touches the tenant-scoped plans table without constraining by tenant_id.
  - _Fix:_ Perform recovery in a tenant-scoped loop or include tenant_id in the WHERE clause. If the operation must run across tenants, run per-tenant iterations under an explicit admin/owner context with strict authorization checks and logging.
- **server/plan-executor.ts:785** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 — selecting pending plans without tenant_id constraint. This reads tenant-scoped data for all tenants with no tenant filter.
  - _Fix:_ Either restrict to a specific tenant (WHERE tenant_id = ${tenantId}) or implement an admin-only, explicitly-audited path. Avoid unscoped selects on tenant tables; pass an explicit tenant context into resumeStuckPlans/dispatcher.
- **server/process-governor.ts:198** — SELECT from heartbeat_logs (response_time case) has no tenant_id constraint — reads tenant-scoped runtime logs across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or equivalent Drizzle .where(eq(table.tenantId, tenantId))) so the AVG only covers the caller's tenant.
- **server/process-governor.ts:239** — SELECT from heartbeat_logs (cascading_failures case) lacks tenant_id filter — counts failing agents across all tenants.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) to prevent cross-tenant leakage.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project by id with NO tenant check — a caller-provided projectId can access another tenant's project.
  - _Fix:_ Require and enforce tenant ownership (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}), or validate that the project belongs to the caller's tenant before proceeding.
- **server/project-brain.ts:332** — Backfill SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has NO tenant_id constraints — enumerates projects/conversations across all tenants.
  - _Fix:_ Scope the query to the tenant (add WHERE p.tenant_id = ${tenantId} or similar) or run per-tenant; never scan global project/conversation tables without filtering by tenant.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} executes without tenant filtering — reads conversations that may belong to other tenants.
  - _Fix:_ Add tenant filtering (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or validate project ownership prior to enumerating conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} selects messages with NO tenant constraint — returns messages from any tenant's conversations.
  - _Fix:_ Constrain to the tenant (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise verify the conversation belongs to the caller's tenant before reading messages.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id with NO tenant check — an external proposalId can expose or allow actions on other tenants' proposals.
  - _Fix:_ Require tenant context and add tenant_id = ${tenantId} to the WHERE clause, or fetch via storage API that enforces tenant ownership (and validate caller is authorized for that tenant).
- **server/proposal-verifier.ts:246** — UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} updates proposal rows without tenant scoping — writes to a tenant-scoped table without verifying ownership.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) to ensure only the tenant's proposal is modified.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} performs tenant-unsafe mutation — no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and ensure rebase logic validates the proposal belongs to the tenant before changing status.
- **server/proposal-verifier.ts:396** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} updates validation_result without tenant filtering — allows cross-tenant mutation if proposalId is attacker-controlled.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause and validate the caller's tenant owns the proposal before persisting rebase outcomes.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes rebased diff back to the DB with NO tenant constraint.
  - _Fix:_ Restrict this UPDATE to the tenant (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and validate proposal ownership before saving rebased diffs.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}) is missing a tenant_id constraint — write scoped only by id may modify another tenant's row if ids collide or are global.
  - _Fix:_ Include tenant guard in the WHERE clause (e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}`) or resolve and pass the tenantId explicitly to the update. All writes to tenant-scoped tables must include tenant_id checks.
- **server/reference-learner.ts:290** — UPDATE to `memory_entries` (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}) omits tenant_id. This updates a tenant-scoped row by id only without re-checking ownership.
  - _Fix:_ Constrain the UPDATE by tenant: include `AND tenant_id = ${input.tenantId}` (or equivalent variable) in the WHERE clause, or perform the UPDATE in the same query that SELECTed the row with tenant_id enforcement to avoid a TOCTOU risk.
- **server/research-engine.ts:157** — cleanupZombieSessions performs a global UPDATE on `research_sessions` (UPDATE research_sessions SET status = 'completed' ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}) with no tenant_id constraint. This can mark sessions for all tenants as completed.
  - _Fix:_ Restrict zombie cleanup to an explicit tenant context or iterate per-tenant. Add `AND tenant_id = ${tenantId}` or pass a tenantId list into the query. If this truly must be global for ops, gate it behind an explicit, auditable operator privilege and document that exceptional authority.
- **server/research-engine.ts:310** — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} — missing tenant constraint. This can change another tenant's program record.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. `WHERE id = ${programId} AND tenant_id = ${tenantId}`) or ensure programId is validated to belong to the intended tenant before the update.
- **server/research-engine.ts:389** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() — no tenant_id constraint. This deletes across all tenants' agent_knowledge rows.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. `AND tenant_id = ${tenantId}`), or run the cleanup per-tenant explicitly. If the intention is a system-wide prune, require an explicit system-tenant flag and strict audit logging.
- **server/research-engine.ts:549** — UPDATE research_sessions SET ... WHERE id = ${sessionId} — write to tenant-scoped `research_sessions` without tenant_id in WHERE. This can alter another tenant's session row.
  - _Fix:_ Include tenant check: `WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}`. Every UPDATE/DELETE on tenant-scoped tables must include tenant_id when performed by application code.
- **server/research-engine.ts:786** — UPDATE research_programs SET baseline_metric_value = ${numericMetricValue}, baseline_label = ${...} WHERE id = ${session.programId} AND baseline_metric_value IS NULL — missing tenant_id constraint, allowing cross-tenant mutation of program baseline.
  - _Fix:_ Add tenant scoping to the UPDATE (`AND tenant_id = ${session.tenantId}`) or ensure program ownership was validated earlier and passed as a trusted object into this function.
- **server/research-engine.ts:820** — UPDATE research_experiments SET ... WHERE id = ${experimentId} — multiple writes to `research_experiments` are performed by id only and do not include tenant_id. This can modify experiments belonging to other tenants.
  - _Fix:_ All UPDATEs/DELETEs against research_experiments must include tenant_id (e.g. `WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}`), or be performed in a transaction that first SELECTs the row with tenant filtering and uses a locked id verified to belong to the tenant.
- **server/research-engine.ts:851** — UPDATE research_experiments ... WHERE id = ${experimentId} in DB-timeout error handler — write executed without tenant constraint.
  - _Fix:_ Include `AND tenant_id = ${session.tenantId}` in the WHERE clause to ensure updates only affect rows owned by that tenant.
- **server/research-engine.ts:878** — UPDATE research_experiments ... WHERE id = ${experimentId} in auth-error handling — missing tenant filtering on a write to a tenant-scoped table.
  - _Fix:_ Add tenant guard `AND tenant_id = ${session.tenantId}` to the WHERE clause. Consider centralizing experiment row updates to a helper that always enforces tenant_id.
- **server/research-engine.ts:894** — UPDATE research_experiments SET ... WHERE id = ${experimentId} in transient-error branch — no tenant_id filter present.
  - _Fix:_ Require tenant_id in WHERE for all updates: `WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}`. Use parameterized queries to avoid TOCTOU issues.
- **server/research-engine.ts:910** — UPDATE research_experiments SET ... WHERE id = ${experimentId} in general error handling — write without tenant scoping.
  - _Fix:_ Add `AND tenant_id = ${session.tenantId}`. Consider factoring DB updates through a wrapper that enforces tenant_id for tenant-scoped tables to prevent regressions.
- **server/research-engine.ts:926** — UPDATE research_sessions SET total_experiments = ..., experiments_kept = ..., experiments_discarded = ..., experiments_crashed = ... WHERE id = ${session.sessionId} — missing tenant constraint on a cross-tenant table write.
  - _Fix:_ Include tenant guard `AND tenant_id = ${session.tenantId}` on the WHERE clause to ensure only the owning tenant's session row is updated.
- **server/research-engine.ts:1697** — UPDATE code_proposals SET status = 'needs_review', validation_result = ... WHERE id = ${proposalId} — write to tenant-scoped `code_proposals` without tenant_id in WHERE. Proposal rows are tenant-scoped and must be protected from cross-tenant mutation.
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${proposalId} AND tenant_id = ${tenantId}` (use the tenantId verified earlier when fetching the proposal). Centralize proposal updates through a function that enforces tenant scoping.
- **server/research-engine.ts:1711** — UPDATE code_proposals SET status = 'applied', applied_at = NOW(), validation_result = ... WHERE id = ${proposalId} — no tenant_id check. This can mark a proposal applied across tenants if id collides or was misused.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the WHERE clause and validate ownership before applying changes. Prefer updating based on the row returned by a prior tenant-scoped SELECT to avoid TOCTOU issues.
- **server/research-engine.ts:1733** — UPDATE code_proposals SET status = 'reverted', reviewed_at = NOW() WHERE id = ${proposalId} — missing tenant_id constraint when reverting proposals.
  - _Fix:_ Add tenant scoping (`AND tenant_id = ${tenantId}`) to the UPDATE. All code_proposals mutations must validate tenant ownership in the WHERE clause to preserve isolation.
- **server/routes.ts:4337** — INSERT into tenant-scoped table 'project_notes' omits tenant_id. The code executes `INSERT INTO project_notes (project_id, note, author) VALUES (...)` without setting tenant_id (columns are NOT NULL with no default per invariant). This can create rows not tied to a tenant or assigned incorrectly, breaking isolation.
  - _Fix:_ Always supply tenant_id explicitly on INSERT into tenant-scoped tables (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}) ). Additionally, validate that the project_id belongs to the caller's tenant before inserting (SELECT project WHERE id = projId AND tenant_id = conv.tenantId) to prevent cross-tenant writes.
- **server/sculptor.ts:94** — UPDATE to tenant-scoped table `sculptor_sessions` executed without constraining by tenant_id (WHERE id = ${session.id} only). This allows cross-tenant modification if an attacker can guess another session id.
  - _Fix:_ Include the caller's tenant in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise bind and check tenant ownership before performing the UPDATE. Every write to a tenant-scoped table must include tenant_id = <caller tenant>.
- **server/sculptor.ts:101** — UPDATE to tenant-scoped table `sculptor_sessions` (setting conversation_id) is performed with WHERE id = ${session.id} only and lacks tenant_id filtering.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or pass and use the tenantId variable so the write cannot affect rows in other tenants.
- **server/sculptor.ts:162** — UPDATE to tenant-scoped table `sculptor_sessions` (setting status/result/tool_calls_count/tokens_used/completed_at) uses WHERE id = ${sessionId} only and does NOT include tenant_id = ${tenantId}.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Even if the row was previously fetched for the tenant, every write must include the tenant_id WHERE filter per the tenant-isolation invariant.
- **server/sculptor.ts:346** — UPDATE to tenant-scoped table `sculptor_sessions` (writing review_result) executes WHERE id = ${sessionId} without tenant_id filtering.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or include tenantId as a parameter to the function and bind it. All writes to tenant-scoped rows must explicitly constrain by tenant_id.
- **server/seed.ts:2859** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without constraining by tenant_id. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or join to a tenant-scoped subquery to ensure the updated row belongs to the caller's tenant before updating.
- **server/seed.ts:3136** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... performs a write to a tenant-scoped table without any tenant_id constraint.
  - _Fix:_ Restrict the UPDATE to the correct tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure you only modify resources owned by the caller's tenant. Verify caller ownership before performing the update.
- **server/seed.ts:3212** — INSERT INTO project_conversations (project_id, conversation_id) ... inserts into a likely tenant-scoped table without specifying tenant_id — tenant_id is required to enforce isolation.
  - _Fix:_ Always include the tenant_id column in INSERTs for tenant-scoped tables (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conversationId})). Before inserting, verify both the project and conversation belong to that tenant to prevent cross-tenant data linking.
- **server/seed.ts:4032** — Inserts into heartbeat_tasks (bulk db.insert(...).values([...])) do not set tenant_id explicitly. Per project invariant, every INSERT into tenant-scoped tables must pass tenantId explicitly.
  - _Fix:_ Include tenant_id explicitly for every inserted row (e.g. tenantId: tenantId or tenant_id: 1 for bootstrap). Do not rely on DB defaults; follow the invariant that INSERTs must supply the tenant id at the application layer.
- **server/seed.ts:4497** — Multiple SELECT/UPDATE statements in cleanupRunawayDelegationTasks operate on tenant-scoped table 'heartbeat_tasks' without tenant_id constraints (e.g. SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation', UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' ...). These updates can disable/modify rows across all tenants.
  - _Fix:_ Constrain every SELECT and UPDATE to the caller's tenant (e.g. AND tenant_id = ${tenantId} or use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId))). If the intent is to perform a global cleanup, run it as an explicit operator migration and document it; otherwise restrict to the appropriate tenant context.
- **server/self-improvement.ts:394** — runExperiment updates the experiments row with .where(eq(experiments.id, experiment.id)) but does NOT constrain by tenant_id. An UPDATE without tenant filter can modify another tenant's row if ids collide.
  - _Fix:_ Constrain UPDATEs to the caller's tenant as well, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Alternatively, fetch and verify the experiment row belongs to the tenant before updating.
- **server/self-improvement.ts:413** — In the error-handling path runExperiment performs an UPDATE on experiments using only the experiment id in the WHERE clause (no tenant_id constraint). This can change another tenant's data.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = callerTenantId) or verify ownership prior to the update. All UPDATE/DELETE ops on tenant-scoped tables must include tenant_id.
- **server/skillify.ts:196** — SELECT from `conversations` executed without a tenant_id WHERE clause. The query fetches a conversation row by id but does not constrain the SQL to the caller's tenant (it checks tenant ownership only after the SELECT). This allows a read of another tenant's row if an attacker supplies a foreign conversationId.
  - _Fix:_ Add a tenant-scoped condition to the SQL itself (fail-closed). Example: .where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))). Or use the tenantScope/assertValidTenantId helpers to produce an eq() predicate and include it in the where clause so the DB never returns rows from other tenants.
- **server/storage.ts:832** — archiveExpiredMemories executes a raw SQL UPDATE on memory_entries to archive expired rows without any tenant_id filter — this will archive rows across all tenants.
  - _Fix:_ Make this operation tenant-aware: either iterate per-tenant and run tenant-scoped UPDATEs (WHERE tenant_id = ${tenantId} AND ...), or require an explicit admin/maintenance tenant argument and document it. Prefer per-tenant scoped updates to avoid accidental cross-tenant data mutation.
- **server/storage.ts:849** — archiveStaleMemories executes a raw SQL UPDATE on memory_entries (WHERE status='active' AND created_at <= ... AND last_accessed <= ...) with no tenant_id filter — this will mutate rows for all tenants.
  - _Fix:_ Same as for archiveExpiredMemories: require tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND ...) or restrict to a vetted admin-only path. Avoid global UPDATEs on tenant-scoped tables without explicit, auditable admin authorization.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` (writing implementation) uses only id in the WHERE clause (eq(customTools.id, tool.id)) with no tenant_id constraint. This allows an update to affect another tenant's row if ids collide or are misused.
  - _Fix:_ Constrain updates to the tenant as well, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the function receives/validates tenantId and uses it for all write operations against tenant-scoped tables.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` deletes by id only (eq(customTools.id, tool.id)) with no tenant_id constraint. This can delete rows belonging to other tenants.
  - _Fix:_ Require and check tenantId when reverting/deleting tools and include it in the WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Pass tenantId into revertToolRow or derive it from the inserted row and assert ownership before deleting.
- **server/tool-learning.ts:365** — UPDATE to increment `usageCount` on tenant-scoped `custom_tools` uses only id in the WHERE clause (.where(eq(customTools.id, tool.id))) with no tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))). Ensure the function has and verifies the caller's tenantId before mutating tenant-scoped rows.
- **server/tools.ts:3205** — INSERT INTO project_files does not set tenant_id. Tenant-scoped table rows must be written with tenant_id to prevent cross-tenant data contamination and to satisfy the invariant that tenant_id is NOT NULL with no default.
  - _Fix:_ Pass the caller's tenantId explicitly in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) RETURNING *). Ensure uploaded_by and other metadata are validated and that an ownership check (project belongs to tenant) is performed prior to insertion.
- **server/tools.ts:3220** — INSERT INTO project_notes omits tenant_id — writing to a tenant-scoped table without tenant_id may create rows visible to other tenants or violate NOT NULL constraints.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId}) RETURNING *). Ensure you have already verified the project belongs to the tenant before inserting.
- **server/tools.ts:3243** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id — creating relationship rows without tenant attribution breaks explicit-tenant invariants and enables cross-tenant contamination.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})) and/or ensure the insert uses a tenant-scoped transaction after validating ownership of both project and conversation.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is performed without constraining on tenant_id (LOOKUP: WHERE category='messaging_pairing' AND title = ...). This returns rows from all tenants and can enable cross-tenant pairing/exfiltration or hijack.
  - _Fix:_ Constrain the query to the caller/claimed tenant (e.g. add AND tenant_id = ${claimedTenantId}) or otherwise limit results to only rows owned by the caller before processing. If the design intentionally inspects candidate rows from other tenants, move that logic into a well-audited admin-only path and document/authorize it; otherwise filter by tenant_id in the SQL binding.

### HIGH (165)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` with no tenant filter (reads all tenants).
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${<tenantId>} or iterate per-authorized tenant). Don’t scan experiments across tenants without explicit system-level authorization.
- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` uses only the row id (WHERE id = activityId) with no tenant_id constraint — allows cross-tenant updates.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}) or require the tenantId in the function signature and validate ownership before updating.
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` uses only id in WHERE (WHERE id = ${evalId}) with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) to prevent cross-tenant modification.
- **server/agentic-engines.ts:114** — SELECT from `plans` (plans likely tenant-scoped) uses source/source_ref but no tenant_id filter — may reveal or reuse plans across tenants.
  - _Fix:_ Add tenant scoping to the SELECT, e.g. WHERE source = ${...} AND source_ref = ${...} AND tenant_id = ${insight.tenantId} (or otherwise verify ownership before reading).
- **server/agentic-engines.ts:147** — UPDATE on tenant-scoped table `ai_insights` (SET action_taken) uses WHERE id = ${insight.insightId} with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${insight.tenantId}) to ensure only the owning tenant's row is modified.
- **server/agentic-engines.ts:160** — SELECT from `ai_insights` to find pending plan markers queries rows across all tenants (no tenant_id filter).
  - _Fix:_ If this sweep should be tenant-scoped, add WHERE tenant_id = ${tenantId}. If the intent is a global sweep, ensure this is an explicitly authorized system operation and document why cross-tenant access is safe; otherwise constrain by tenant.
- **server/agentic-engines.ts:242** — UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...) — this bulk UPDATE has no tenant_id constraint and will update rows across tenants.
  - _Fix:_ Restrict the UPDATE to a tenant (WHERE tenant_id = ${tenantId}) or explicitly run per-tenant. Avoid global updates on tenant-scoped tables unless intentionally authorized and guarded.
- **server/agentic-engines.ts:262** — UPDATE ai_insights SET action_taken = ... WHERE id = ${Number(r.id)} — missing tenant_id in WHERE clause (per-row update during sweep).
  - _Fix:_ Include AND tenant_id = ${r.tenant_id} (or verify ownership before updating) to prevent modifying another tenant's data.
- **server/agentic-engines.ts:344** — SELECT from `heartbeat_logs` counting calls (task_name != 'Self-Reflection') has no tenant filter — reads global logs across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} if the metric should be tenant-scoped, or ensure the operation is intentionally global and authorized; otherwise scope by tenant.
- **server/agentic-engines.ts:448** — SELECT recent heartbeat_logs (status='success') used in predictive engine has no tenant filter — may return logs from other tenants.
  - _Fix:_ Constrain the query with WHERE tenant_id = ${tenantId} or otherwise verify that reading logs across tenants is intended and safe.
- **server/agentic-engines.ts:516** — SELECT aggregating heartbeat_logs (task_name, status, COUNT/AVG) has no tenant filter — aggregates across all tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (or other appropriate tenant scoping) so optimization insights are calculated per-tenant rather than globally.
- **server/agentic-engines.ts:531** — SELECT from `heartbeat_tasks` WHERE enabled = true ORDER BY name has no tenant filter — returns tasks across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) unless the tasks table is explicitly global (document if global).
- **server/agentic-features.ts:600** — SELECT id FROM `outreach_enrollments` to detect existing enrollment lacks tenant filter — could find enrollments belonging to other tenants.
  - _Fix:_ Add tenant scoping to the WHERE (AND tenant_id = ${params.tenantId}) before checking/inserting to ensure ownership and prevent cross-tenant collisions.
- **server/agentic-features.ts:638** — UPDATE outreach_enrollments SET status = 'completed' WHERE id = ${enrollment.id} — missing tenant_id constraint on UPDATE.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${params.tenantId} (or verify ownership) to prevent updating another tenant's enrollment.
- **server/agentic-features.ts:671** — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} — no tenant_id in WHERE clause.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${params.tenantId}) to ensure only the tenant's enrollment is changed.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments SET reply_classification = ... WHERE id = ${row.id} — missing tenant constraint (row.id could belong to another tenant).
  - _Fix:_ Use WHERE id = ${row.id} AND tenant_id = ${params.tenantId} (or otherwise verify ownership) before updating enrollment rows.
- **server/agentic/approvals.ts:122** — Bulk UPDATE agentApprovals to expire stale approvals does not include a tenant_id constraint — it updates pending approvals across all tenants.
  - _Fix:_ Scope the expiry to a tenant when appropriate (e.g. WHERE tenant_id = ${tenantId}) or explicitly make this a documented global system operation; when expiring, subsequent per-run updates already rely on tenantId returned, but the initial UPDATE must still be guarded or documented.
- **server/agentic/harness-adaptation.ts:71** — SELECT from `agent_trace_spans` is executed with no tenant_id constraint (no WHERE tenant_id = ...). If `agent_trace_spans` is tenant-scoped this allows cross-tenant data exposure.
  - _Fix:_ Restrict the query to a specific tenant or run it explicitly as an admin-only operation. Options: (1) add a tenantId parameter and include WHERE tenant_id = ${tenantId} in the SQL; or (2) if this is intended to be an admin/global scan, make that explicit (use ADMIN_TENANT_ID) and guard the routine so it only runs in a privileged context. Ensure any caller-side inputs are validated and that the query is properly parameterized rather than interpolated raw SQL.
- **server/agentic/resume-worker.ts:66** — Unscoped SELECT over tenant-scoped tables (agent_runs / agent_approvals) — the query at lines 66-85 reads rows across all tenants without a tenant_id WHERE filter.
  - _Fix:_ Restrict this sweep to one tenant at a time or include an explicit tenant filter (e.g. WHERE r.tenant_id = ${tenantId}). If the worker must scan all tenants, enumerate tenants and run a tenant-scoped query per-tenant to avoid cross-tenant reads. Ensure any returned rows are validated to belong to the expected tenant before acting on them.
- **server/agentic/runs.ts:34** — appendStep performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — a write to a tenant-scoped table without enforcing tenant ownership.
  - _Fix:_ Require and pass tenantId into appendStep (or fetch+validate run ownership before update) and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Always enforce ownership on UPDATEs to tenant-scoped tables.
- **server/agentic/runs.ts:45** — updateRunState performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Accept/require tenantId (or validate run ownership) and add AND eq(agentRuns.tenantId, tenantId) to the WHERE clause; or select the run and check its tenant before applying the update.
- **server/agentic/runs.ts:54** — replaceRunState performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Add tenant ownership checks (pass tenantId or validate the run's tenant) and include tenant_id in the WHERE clause to prevent cross-tenant updates.
- **server/agentic/runs.ts:58** — completeRun performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Require tenantId or validate ownership and include AND eq(agentRuns.tenantId, tenantId) in the WHERE clause to ensure the update only affects the caller's tenant.
- **server/agentic/runs.ts:67** — failRun performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Include tenant scoping in the WHERE clause (or validate run belongs to tenant before update). All updates to tenant-scoped tables must include tenant_id checks to enforce isolation.
- **server/agentic/runs.ts:76** — pauseRun performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Require a tenantId parameter (or validate ownership) and add eq(agentRuns.tenantId, tenantId) to the WHERE filter to prevent cross-tenant modifications.
- **server/agentic/wake-scheduler.ts:80** — UPDATE on tenant-scoped table `agent_wake_schedules` is executed without any tenant_id constraint (the subquery selects pending rows across all tenants). This claims/fires schedules across tenants in one statement.
  - _Fix:_ Either add an explicit tenant filter (e.g. include WHERE tenant_id = <tenantId> in the subquery/UPDATE) or iterate per-tenant and run the claim/update inside a per-tenant transaction. Ensure every UPDATE/SELECT touching tenant-scoped rows includes tenant_id unless intentionally done in a carefully-audited cross-tenant maintenance routine (and documented/guarded).
- **server/auth.ts:1150** — loadSessionsFromDb does SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions without any tenant restriction — it reads all sessions across tenants into memory.
  - _Fix:_ If the intent is to load only sessions for this process's tenant context, add WHERE tenant_id = ${tenantId}. If the intent is global maintenance, ensure this is only run by an authorized admin/maintenance process and document/guard it. Prefer per-tenant queries to enforce isolation.
- **server/auth.ts:1137** — clearExpiredSessions executes DELETE FROM auth_sessions WHERE expires_at < ${now} with no tenant_id constraint, deleting expired sessions across all tenants.
  - _Fix:_ Scope session cleanup by tenant_id (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ${now}) or perform this in a controlled admin maintenance job. Avoid global deletes that cross tenant boundaries unless explicitly intended and guarded.
- **server/auto-asset-capture.ts:145** — INSERT INTO project_files omits any tenant_id column — project-files rows are project-scoped but the INSERT does not persist tenant_id. Relying on transitive isolation via project_id without storing tenant_id violates the rule that INSERTs must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the project_files row (VALUES ... , ${tenantId}) or otherwise ensure the table enforces tenant ownership at write time (explicit tenant_id column populated from the validated parent resource). If the table is intentionally not tenant-bearing, document and justify; otherwise change the schema/INSERT to record tenant_id.
- **server/auto-asset-capture.ts:165** — INSERT INTO project_notes omits tenant_id (inserting project-scoped notes without explicit tenant column population). This relies on transitive isolation instead of writing tenant_id explicitly.
  - _Fix:_ Populate tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)) after verifying the project/conversation ownership. Make tenant_id explicit on all project-scoped writes.
- **server/auto-memorize.ts:47** — getWatermark selects FROM agent_knowledge (category='auto_memorize_watermark' AND title = ...) without any tenant_id constraint — reads a tenant-scoped table across all tenants.
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. ADMIN_TENANT_ID) by adding AND tenant_id = ${ADMIN_TENANT_ID}, or accept a tenantId argument and filter accordingly. Be explicit about tenant scoping for watermark rows.
- **server/auto-memorize.ts:100** — isDuplicate queries memory_entries with SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ORDER BY id DESC LIMIT 200 with no tenant filter — it scans memory across all tenants for deduplication.
  - _Fix:_ Scope deduplication to the tenant (e.g. add AND tenant_id = ${tenantId}) or pass tenantId into isDuplicate. If global dedup is intentional, make it explicit and ensure it's executed only in a safe, audited admin context.
- **server/auto-memorize.ts:159** — runAutoMemorize executes SELECT ... FROM messages LEFT JOIN conversations ... WHERE m.created_at > ${windowStart} AND m.created_at <= ${windowEnd} ... with no tenant filter — it scans messages across all tenants.
  - _Fix:_ Either run auto-memorize per-tenant (loop tenants and include tenant_id filters on messages/conversations) or explicitly restrict the query to an admin/maintenance tenant and document the cross-tenant intent. Prefer per-tenant queries to preserve isolation and avoid accidental cross-tenant reads.
- **server/auto-transcript.ts:128** — SELECT from tenant-scoped table `conversations` without a tenant filter (backfill query SELECT DISTINCT c.id, c.project_id, c.tenant_id ... WHERE c.project_id IS NOT NULL ...). This reads data across tenants.
  - _Fix:_ Scope the query to a single tenant (e.g. add WHERE c.tenant_id = ${tenantId}) or change the backfill to iterate per-tenant and pass the tenantId into the query. If this is an intentional cross-tenant admin export, require an explicit admin-only path and audit/authorize its use.
- **server/auto-transcript.ts:153** — SELECT (lines ~153) pulls project_conversations / conversations / projects rows without restricting to a caller tenant — the query can return cross-tenant rows for the backfill path.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE c.tenant_id = ${tenantId} or p.tenant_id = ${tenantId}), or run this backfill per-tenant. Ensure any downstream actions (updates/inserts) use the same tenant guard.
- **server/auto-transcript.ts:171** — SELECT (lines ~171) selecting conversations WHERE c.project_id IS NOT NULL AND NOT EXISTS (...) executes against the tenant-scoped `conversations` table with no tenant filter — backfill will examine rows across tenants.
  - _Fix:_ Restrict this SELECT to a tenant (add WHERE c.tenant_id = ${tenantId}) or restructure the backfill to run per-tenant, passing tenantId explicitly. If cross-tenant reads are intended, require an explicit admin authority and document/authorize the behavior.
- **server/business-tools.ts:334** — Query joins `customers` without scoping the joined row to the caller's tenant. The LEFT JOIN `customers cust ON cust.id = c.customer_id` can return customer data from another tenant because the join lacks `cust.tenant_id = c.tenant_id` (or `cust.tenant_id = ${tid}`).
  - _Fix:_ Include tenant scoping on the joined customer row. For example: `LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}` (or add `AND cust.tenant_id = c.tenant_id`), so the joined customer row is always restricted to the caller's tenant. Alternatively, select customer_name via a separate query that includes tenant_id filtering.
- **server/channels/kernel.ts:177** — dispatchInbound calls the adapter-supplied resolveConversation(turn) and then immediately uses the returned conversationId in processMessage without verifying that the resolved conversation belongs to the same tenant (turn.tenantId). The kernel trusts the adapter to do tenant resolution and does not assert ownership before reading/writing data tied to that conversation.
  - _Fix:_ Have dispatchInbound verify ownership before using the conversation id: either require resolveConversation to return both {conversationId, tenantId} (and assert equality with turn.tenantId), or fetch the conversation record and check conversation.tenant_id === turn.tenantId before calling processMessage. Alternatively, enforce this check inside processMessage/storage so passing a cross-tenant conversation id is rejected.
- **server/chat-engine.ts:4599** — Call to storage.updateMemoryEmbedding(entry.id, emb) performs an update using only entry id and embedding; there is no tenantId passed to constrain the UPDATE. Without a tenant-scoped WHERE clause this could update another tenant's row if ids are predictable or leaked.
  - _Fix:_ Change updateMemoryEmbedding to accept tenantId and use a WHERE id = ? AND tenant_id = ? filter (or otherwise validate ownership of entry.id before updating). At minimum, ensure updates include tenantId in their WHERE clause so writes are constrained to the correct tenant.
- **server/compaction.ts:464** — INSERT INTO `memory_entries` sets tenant_id from an optional/unchecked parameter (`${tenantId}`) without validating presence/validity. If tenantId is missing/undefined the insert could create rows with NULL tenant_id or mis-attributed data.
  - _Fix:_ Validate tenantId (must be a positive integer) before inserting. Fail-closed if tenantId is absent. Ensure callers always pass a verified tenantId and add explicit checks in this function to refuse writes when tenantId is invalid.
- **server/credential-vault.ts:169** — UPDATE `credential_vault` sets last_used_at using only the credential id (WHERE id = ${id}) and omits tenant_id, allowing a write to a credential row without confirming tenant ownership.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (and/or fetch-and-verify the credential's tenant before the UPDATE). Always constrain UPDATE/DELETE statements on tenant-scoped tables by tenant_id to enforce isolation.
- **server/credential-vault.ts:176** — UPDATE `credential_vault` sets last_used_at using only the credential id (WHERE id = ${cred.id}) without constraining by tenant_id — same impact as above (cross-tenant write risk).
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${cred.id} AND tenant_id = ${tenantId}). Ensure functions performing post-read writes validate the tenant context before issuing any UPDATE/DELETE statements.
- **server/data-protection.ts:72** — UPDATE conversations executed without constraining by tenant_id. The code SELECTs the conversation scoped to tenantId but then updates deleted_at/deleted_by using only id, lacking AND tenant_id = ${tenantId}. This can alter a conversation belonging to another tenant if IDs collide or in race conditions.
  - _Fix:_ Include the tenant filter on the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or perform the UPDATE inside a transaction that has already validated ownership; ensure all mutation SQL against tenant-scoped tables includes tenant_id = <caller tenant>.
- **server/data-protection.ts:112** — SELECT FROM conversations in permanentlyPurgeSoftDeleted is performed across all tenants (no tenant_id predicate). This reads tenant-scoped rows for all tenants without scoping to the caller/operation context.
  - _Fix:_ If this is intended as an admin/global maintenance job, make that explicit (owner-only) and document the justification. Otherwise scope to a tenant (pass tenantId) or iterate tenants explicitly and run per-tenant queries. Avoid unscoped reads on tenant-scoped tables in regular application code.
- **server/data-protection.ts:144** — verifyMessageSaved runs SELECT id FROM messages WHERE conversation_id = ${conversationId} ... but the function does not take a tenantId and does not verify that the conversation (or messages) belong to the caller's tenant. This allows cross-tenant reads by conversation id.
  - _Fix:_ Require and use tenantId in this API, or verify ownership by joining conversations (e.g. SELECT m.id FROM messages m JOIN conversations c ON m.conversation_id = c.id WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} ...). Ensure every read against tenant-scoped tables is constrained to the caller's tenant.
- **server/delivery-pipeline.ts:720** — UPDATE on delivery_logs to release stripePaymentId uses .where(eq(deliveryLogs.id, prior.id)) with no tenant constraint. This mutates a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Include the tenant constraint in the UPDATE, e.g. .where(and(eq(deliveryLogs.id, prior.id), eq(deliveryLogs.tenantId, prior.tenantId))). Alternatively, perform the update within a transaction using the tenant-aware row fetched earlier to ensure ownership.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId without any tenant constraint (WHERE stripe_payment_id = paymentId). This can return another tenant's delivery record.
  - _Fix:_ Require a tenantId parameter (or derive it from caller context) and add eq(deliveryLogs.tenantId, tenantId) to the WHERE clause. If global lookup is intended, restrict the function to trusted admin/internal callers only and document that clearly.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but omits tenant_id. This can delete chunks belonging to other tenants if collection_id collisions or incorrect references occur.
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}). Ensure addDocument accepts and uses the caller's tenantId and that collection existence checks are tenant-scoped (they already are), then include tenant_id in all DELETE/UPDATE/SELECTs against doc_chunks.
- **server/embeddings.ts:688** — backfillMissingKnowledgeEmbeddings SELECTs from agent_knowledge without any tenant_id filter, so it will read rows across all tenants.
  - _Fix:_ Limit backfillMissingKnowledgeEmbeddings to operate per-tenant (add a tenantId parameter) or explicitly mark it as an admin-only global operation and enforce that only an admin tenant is used. Always include tenant_id = ${tenantId} in the SELECT and subsequent updates, or scope the work to a single tenant at a time.
- **server/embeddings.ts:716** — backfillEmbeddingVecs SELECTs memory_entries rows without a tenant_id constraint (SELECT ... FROM memory_entries ... LIMIT 200). This reads tenant-scoped data across tenants.
  - _Fix:_ Run backfillEmbeddingVecs on a per-tenant basis (accept tenantId) or add WHERE tenant_id = ${tenantId} to the SELECT. If this is meant to be an admin-global job, make that explicit and ensure appropriate access controls and auditing are in place.
- **server/embeddings.ts:732** — backfillEmbeddingVecs SELECTs agent_knowledge rows without a tenant_id filter (SELECT ... FROM agent_knowledge ... LIMIT 200).
  - _Fix:_ Scope the query to a tenant (add tenantId parameter and WHERE tenant_id = ${tenantId}), or otherwise ensure this admin job intentionally and safely processes tenants one-at-a-time with explicit authorization and logging.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' is performed by project id only (WHERE projects.id = ${projectId}) with NO tenant_id filter. Although code later checks project.tenantId, the SQL itself is not tenant-scoped and may read another tenant's project row.
  - _Fix:_ Scope the SELECT to the caller's tenant in SQL (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or add WHERE projects.tenant_id = ${tenantId}). Avoid relying solely on an application-level post-check; include tenant filter in the query itself.
- **server/google-drive.ts:829** — SELECT from 'projects' in uploadAndShare is performed only by project id (.where(eq(projects.id, params.projectId))) with NO tenant_id filter. This reads project metadata across tenants before the explicit ownership check, violating the requirement that every read be tenant-scoped.
  - _Fix:_ Constrain the SELECT by tenant (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId || resolvedTenantId)))) so the DB query itself is scoped to the caller's tenant. Prefer parameterized/drizzle eq on the tenant column.
- **server/google-drive.ts:1066** — SELECT from 'projects' (to get driveFolderId) in searchDriveFiles uses WHERE projects.id = ${params.projectId} without a tenant_id constraint. Even if earlier checks validated ownership, every SQL read must include tenant scoping to avoid accidental cross-tenant exposure.
  - _Fix:_ Include tenant filtering in the query (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or use a JOIN that enforces the tenant_id. Ensure all SELECTs on tenant-scoped tables include the tenant condition.
- **server/health-audit.ts:314** — SELECT from code_proposals is executed without any tenant_id constraint — reading a likely tenant-scoped table globally.
  - _Fix:_ Scope this query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or require an explicit admin context. If the audit must run across tenants, iterate per-tenant and run tenant-scoped queries to preserve tenant isolation.
- **server/health-audit.ts:333** — UPDATE on code_proposals (archiving stale proposals) updates rows by id only, with no tenant_id restriction — this can modify proposals across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or validate each id's tenant ownership before updating. Alternatively run the archive per-tenant so tenant_id is always passed explicitly.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images is performed without passing tenant_id (and without validating that the target session belongs to the expected tenant). If presenter_slide_images is tenant-scoped this allows cross-tenant writes.
  - _Fix:_ Ensure tenant_id is explicitly included in the INSERT (and derive it from a trusted source), or validate the session_id ownership first and include tenant_id in the values. Do not accept tenant information from untrusted inputs.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint — updating by id alone may allow modifying sessions belonging to other tenants.
  - _Fix:_ Constrain the UPDATE to the owning tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or verify session ownership before updating. Always source tenantId from a trusted context and include it in write queries.
- **server/heartbeat.ts:52** — Unscoped SELECT on tenant-scoped table `research_schedules`: SELECT * FROM research_schedules ... has no WHERE tenant_id constraint.
  - _Fix:_ Scope the query by tenant (e.g. add WHERE tenant_id = ${tid}) or iterate per-tenant and pass the tenant id into the query, or use a storage helper that enforces tenant scoping. Avoid global selects across tenants.
- **server/heartbeat.ts:588** — Unscoped SELECT on tenant-scoped table `event_log`: SELECT * FROM event_log WHERE status = 'pending' ... has no tenant_id constraint and will return events across all tenants.
  - _Fix:_ Limit the SELECT to a specific tenant (WHERE tenant_id = ${tid} AND status = 'pending') or iterate per-active-tenant and query per-tenant, or use a tenant-aware storage API so events from other tenants are not processed/read.
- **server/internal-resolver.ts:91** — UPDATE event_log executed without constraining by tenant_id (no WHERE tenant_id = ...). Finalizing an event_log row by id alone can modify rows belonging to other tenants.
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId} AND status IN (...)) or otherwise ensure the resolver is explicitly scoped to the correct tenant before updating event_log. If this is intended as a cross-tenant admin operation, make it explicit, audit it, and restrict to privileged processes only.
- **server/internal-resolver.ts:107** — SELECT FROM plans by id is performed without verifying the plan's tenant ownership (no tenant_id constraint). The resolver reads a tenant-scoped row using an externally-provided planId without validating the caller/actor's tenant.
  - _Fix:_ Restrict the SELECT to the owning tenant (e.g. SELECT ... FROM plans WHERE id = ${planId} AND tenant_id = ${tenantId} LIMIT 1) or otherwise look up the tenant for the event and assert it before reading/modifying tenant-scoped rows. Ensure ownership is validated before taking actions like decidePlan or finalizing events.
- **server/internal-resolver.ts:170** — UPDATE code_proposals changes proposal rows without constraining by tenant_id. The reconciler archives a proposal by id only, which may affect another tenant's proposal.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending') or first load/verify the proposal's tenant and ensure the resolver is authorized to act on it. If the operation must be global, gate it behind an explicit admin privilege check and audit log.
- **server/lib/action-ledger.ts:305** — sweepStaleAttempts performs a cross-tenant UPDATE on action_attempts without constraining by tenant_id (UPDATE action_attempts ... WHERE id IN (SELECT id FROM action_attempts ...)). This will update rows across tenants.
  - _Fix:_ Do not perform a bulk cross-tenant UPDATE. Either (a) include tenant_id in the inner SELECT/outer UPDATE predicate so each UPDATE is scoped to a single tenant, or (b) select candidate ids WITH their tenant_id and perform per-tenant (or per-row) tenant-scoped transitions (e.g. call the tenant-scoped transition() for each id). Ensure every UPDATE on action_attempts includes WHERE tenant_id = <tenant>.
- **server/lib/inbox-ingest.ts:444** — SELECT from inbox_messages to locate an orphaned message does not constrain by tenant_id (WHERE clause lacks tenant filter). This can let a runner pick a message row belonging to a different tenant (or a global duplicate) when matching only on message_id, violating the app-level tenant-isolation invariant.
  - _Fix:_ Include the tenant constraint on the query so the lookup is scoped to the caller's tenant. For example: `WHERE m.message_id = ${id} AND m.tenant_id = ${tenantId} AND c.id IS NULL`. Ensure every read/write against inbox_messages and other tenant-scoped tables always includes `AND tenant_id = ${tenantId}` (or the equivalent Drizzle .where(eq(...))) and validate ownership before proceeding.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without constraining by tenant_id — query returns rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${someTenantId}) or require this function to be run only with an explicit admin-scoped parameter. If the intent is to run a multi-tenant audit, ensure the caller is an authorized admin and document that this routine intentionally scans all tenants; otherwise, restrict the query to the caller's tenant using eq(agent_runs.tenant_id, tenantId).
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without constraining by tenant_id — query returns rows across all tenants.
  - _Fix:_ Same as above: either add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or gate this routine to an authorized admin/audit path that is explicitly allowed to scan across tenants. Prefer per-tenant filtering where possible to preserve tenant isolation.
- **server/memory-graph.ts:65** — UPDATE on tenant-scoped table `memory_categories` missing tenant_id constraint (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This allows updating a category row without verifying it belongs to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId}), and ensure the function has access to tenantId (pass tenantId into ensureCategory/classifyMemoryCategory or refreshCategoryCount so you can bind it).
- **server/memory-graph.ts:125** — UPDATE on tenant-scoped table `memory_categories` (and the inner SELECT) lacks tenant scoping. refreshCategoryCount updates memory_categories.memory_count using a count of memory_entries but neither the outer UPDATE nor the inner SELECT filters by tenant_id.
  - _Fix:_ Change refreshCategoryCount to accept a tenantId parameter and add tenant filters to both the outer update and the inner count (e.g. inner: WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ...; outer: WHERE id = ${categoryId} AND tenant_id = ${tenantId}).
- **server/memory-graph.ts:181** — SELECT from tenant-scoped table `memory_links` has no tenant_id filter (SELECT 1 FROM memory_links WHERE ... LIMIT 1). This reads cross-tenant link rows when checking existence.
  - _Fix:_ Constrain the SELECT by tenant (e.g. add AND tenant_id = ${tenantId}) or otherwise join via a tenant-scoped memory_entries row to ensure ownership before reading memory_links.
- **server/memory-graph.ts:392** — UPDATE on tenant-scoped table `memory_entries` missing tenant scoping (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). This updates rows by id without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or use a storage helper that enforces tenant scoping.
- **server/memory-intelligence.ts:271** — UPDATE on tenant-scoped table `memory_entries` missing tenant scoping inside a callback (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}). TenantId from the outer flow is not applied to this UPDATE.
  - _Fix:_ Bind and enforce tenantId in the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}). Prefer using the storage layer that takes tenantId or pass tenantId into the closure explicitly and use parameterized binding.
- **server/memory-intelligence.ts:314** — UPDATE on tenant-scoped table `memory_entries` missing tenant scoping in the update-branch (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}).
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or call a storage API that enforces tenant ownership when updating rows.
- **server/memory-intelligence.ts:399** — UPDATE on tenant-scoped table `knowledge_triples` missing tenant scoping (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}). The earlier SELECT that produced c filtered by tenant, but the UPDATE itself lacks an explicit tenant_id guard.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use an UPDATE ... FROM join that includes tenant scoping) to ensure only the intended tenant's triple rows are modified.
- **server/minds-engine.ts:214** — INSERT builds a PostgreSQL array literal from request-derived dependsOn via string interpolation ({${params.dependsOn.join(',')}}) and injects it into the query. This is string-interpolated SQL with request-derived values and risks injection or malformed arrays.
  - _Fix:_ Use proper parameterized array binding instead of building array string literals. For example, pass dependsOn as a typed parameter (USE SQL parameterization or Drizzle array helpers) or validate/sanitize each id as an integer before constructing the array server-side.
- **server/minds-engine.ts:327** — UPDATE with id = ANY(${`{${eventIds.join(",")}}`}::int[]) uses a constructed array literal from caller-provided eventIds (string-interpolated SQL). This is SQL built from request-derived values and can be abused or malformed.
  - _Fix:_ Avoid constructing array literals by string concatenation. Use parameterized queries that pass an int[] parameter (or use Drizzle helpers) and validate eventIds as integers before binding. Example: WHERE id = ANY(${eventIds}::int[]) where eventIds is an array parameterized by the driver.
- **server/minerva-planner.ts:373** — SELECT probe lacks tenant scoping (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}). Reading plan status by id without tenant filter can leak or allow logic to act on another tenant's plan.
  - _Fix:_ Include tenant scoping in this probe (e.g. WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId}) and ensure the caller's tenant is known before probing.
- **server/minerva-planner.ts:391** — SELECT child plan by parent_plan_id without tenant scoping (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1). This can return a child plan from another tenant.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${expectedTenantId}) or join to verify tenant ownership before returning rows.
- **server/minerva-planner.ts:459** — Rollback UPDATE on `plans` lacks tenant scoping (UPDATE plans SET status = 'awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising'). This modifies plan rows by id without verifying tenant ownership.
  - _Fix:_ Add AND tenant_id = ${expectedTenantId} to the WHERE clause (and ensure expectedTenantId is obtained/validated from the caller session or earlier trusted row) so rollback only affects the intended tenant's plan.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` omits tenant_id. The code inserts project_files after assertProjectInTenant(...) but does not include tenant_id in the INSERT columns/values, violating the invariant that tenant-scoped writes must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and pass the caller's tenant (e.g. ${"options.tenantId"}). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep the existing assertProjectInTenant(...) check to verify ownership before inserting.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` omits tenant_id. In the parallel-produce path the code checks assertProjectInTenant(...) but then inserts project_files without setting tenant_id, breaking tenant-isolation-by-convention (and risking cross-tenant or null/default writes).
  - _Fix:_ Add tenant_id to the INSERT and bind options.tenantId (and ensure assertProjectInTenant(...) ran successfully). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...).
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(${lockKey}) in SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). lockKey is derived from caller-provided ledgerKey/tenantId and is being inlined into SQL via sql.raw(), which risks injection or incorrect typing for untrusted input.
  - _Fix:_ Avoid sql.raw with values that originate from request/caller input. Compute and validate lockKey server-side as a numeric value and pass it as a parameterized binding (e.g. sql`SELECT pg_advisory_xact_lock(${BigInt(validatedKey)}::bigint)` or use tx.execute with parameter binding). Validate and canonicalize ledgerKey before deriving the lock value.
- **server/persona-export.ts:33** — Unscoped SELECT on tenant-scoped table `personas` (SELECT * FROM personas WHERE id = ${personaId}) — no tenant_id check / ownership verification before returning persona data.
  - _Fix:_ Verify persona ownership before reading/exporting: SELECT ... FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} (or first fetch tenant_id and validate it matches the caller). Refuse to export or expose persona data without explicit tenant scoping/authorization.
- **server/personality-files.ts:5** — Table creation sets tenant_id INTEGER NOT NULL DEFAULT 1. This creates a default tenant and allows INSERTs that omit tenant_id to silently attribute rows to tenant 1, violating the invariant that tenant_id must be provided explicitly (no default).
  - _Fix:_ Remove the DEFAULT on tenant_id and require tenant_id to be passed explicitly on every INSERT (make tenant_id NOT NULL with no default). Add a migration to find/fix rows accidentally attributed to tenant 1 and validate callers always pass tenantId.
- **server/proactive-engine.ts:138** — SELECT persona_id FROM proactive_actions WHERE id = ${actionId} — this read against the tenant-scoped proactive_actions table omits tenant_id, which can leak or return rows for another tenant if actionId is from untrusted input.
  - _Fix:_ Scope the SELECT by tenant_id (e.g. WHERE id = ${actionId} AND tenant_id = ${tenantId}), ensuring reads on tenant-scoped tables always include the caller's tenant or an explicit, validated admin context.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not include tenant_id — a write to a tenant-scoped table is performed without scoping to the caller's tenant.
  - _Fix:_ Include tenant check in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) to ensure only the tenant's task is modified.
- **server/process-governor.ts:641** — UPDATE agent_desks ... WHERE id = ${desk.id} lacks tenant_id constraint — mutates agent_desks without scoping to tenant.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}). Always include tenant scoping on writes even if the id was selected earlier.
- **server/process-governor.ts:977** — UPDATE governance_rules SET ... WHERE id = ${rule.id} executes without tenant_id filter — increments trigger_count on a rule id without checking tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) to ensure rule bookkeeping cannot affect other tenants' rules.
- **server/recurring-messages.ts:258** — Unscoped SELECT from tenant-scoped table `agent_knowledge` (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500) — no tenant_id constraint. This returns rows across all tenants to a cron runner.
  - _Fix:_ Avoid global unfiltered reads. Either run per-tenant queries (pass a tenantId and add `AND tenant_id = ${tenantId}`) or explicitly document and enforce a vetted system-level tenant context that is safe to read across tenants and logged/audited. Prefer the per-tenant scan approach (iterate tenants and query each tenant_id) to preserve isolation.
- **server/research-engine.ts:450** — SELECT from `research_experiments` (SELECT id, hypothesis, result, metric_value FROM research_experiments WHERE session_id = ${sessionId} AND status = 'keep') lacks tenant_id constraint. This can read experiments from other tenants if session ids are not globally isolated.
  - _Fix:_ Include `AND tenant_id = ${session.tenantId}` in the WHERE clause, or join via session table ensuring the session belongs to the same tenant, e.g. `... WHERE session_id = ${sessionId} AND tenant_id = ${session.tenantId}`.
- **server/research-engine.ts:459** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} — missing tenant_id filter. A program id should be validated as belonging to the session tenant before use.
  - _Fix:_ Constrain reads by tenant: `WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}` (or validate ownership earlier and pass only trusted objects to this function).
- **server/research-engine.ts:464** — SELECT summary FROM research_sessions WHERE id = ${sessionId} — missing tenant constraint; reading session data without verifying tenant ownership risks cross-tenant exposure.
  - _Fix:_ Add `AND tenant_id = ${session.tenantId}` to the WHERE clause, or fetch session row earlier with tenant scoping and reuse it rather than a second unscoped lookup.
- **server/research-engine.ts:1482** — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL RETURNING id — claim/update performed without tenant_id. Although the SELECT that fed this loop filtered by tenant earlier, the UPDATE itself lacks tenant scoping.
  - _Fix:_ Add tenant constraint to the UPDATE (`AND tenant_id = ${tenantId}`) to prevent accidentally claiming/replaying experiments from other tenants; or perform the UPDATE using a composite WHERE that includes tenant_id.
- **server/routes.ts:1536** — Public endpoint /api/public/stats performs SELECTs against tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) without any tenant_id constraint.
  - _Fix:_ Restrict these queries to an explicit tenant context (WHERE tenant_id = <caller tenant>) or require admin authorization. If the intent is global aggregated metrics, maintain a separate, explicit aggregation table updated by trusted background jobs rather than scanning tenant tables directly from an unauthenticated endpoint.
- **server/routes.ts:1565** — Public endpoint /api/public/architecture runs multiple unscoped SELECTs (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions, etc.) against tenant-scoped tables with no tenant_id filtering.
  - _Fix:_ Either require admin-only access for this endpoint, or add tenant scoping so each caller only sees their tenant's counts. For public/global architecture data, compute and expose aggregated metrics stored separately by a trusted process rather than executing live unscoped queries across tenant tables.
- **server/routes.ts:2319** — GET /api/store/order/:sessionId queries delivery_logs by orderId/stripePaymentId without any tenant_id constraint, returning tenant-scoped delivery log rows to a public capability URL.
  - _Fix:_ Either include tenant scoping in the query (WHERE tenant_id = <caller tenant>), or ensure the capability identifier (sessionId) is a cryptographically-bound capability that is validated to belong to the intended tenant before returning any row. Alternatively store/publicize only per-order public-safe data separate from tenant-scoped internal rows.
- **server/routes.ts:2427** — POST /api/store/lookup-orders performs a SELECT FROM delivery_logs WHERE lower(customer_email) = ${rawEmail} with no tenant_id constraint; this searches tenant-scoped delivery logs across all tenants.
  - _Fix:_ Avoid scanning tenant-scoped tables globally from an unauthenticated/public route. If the intent is to find orders for a provided email across the platform, require authenticated/authorized access or implement a secure, auditable, privacy-respecting mechanism (e.g. per-tenant lookup via owner-provided token or a separate indexed table designed for cross-tenant recovery with strict access controls). At minimum, enforce tenant scoping or limit results to the requesting tenant.
- **server/routes.ts:2571** — POST /api/store/verify-orders queries delivery_logs .select().from(deliveryLogs).where(lower(deliveryLogs.customerEmail) = ${rawEmail}) without any tenant_id constraint before returning a list of orders.
  - _Fix:_ Do not return slices of tenant-scoped delivery_logs across tenants. Require a tenant context or use a verified capability to ensure the caller is entitled to view those orders. Alternatively, restrict this endpoint to authenticated users and add WHERE tenant_id = <caller tenant> (or provide only public-safe links that do not reveal tenant data).
- **server/routes.ts:4331** — SELECT FROM tenant-scoped table 'conversations' without constraining by tenant_id. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` does not verify the conversation belongs to the caller's tenant (conv.tenantId). This allows reading a conversation's project_id across tenants.
  - _Fix:_ Include a tenant filter in the WHERE clause (e.g. AND tenant_id = ${conv.tenantId}) or otherwise validate ownership before reading. Prefer using the conversation object already in scope (or storage/db API that enforces tenant scope) instead of a raw, tenant-unscoped SELECT.
- **server/routes/admin-zombie-detector.ts:38** — Query reads from agent_trace_spans inside an admin transaction without constraining by tenant_id (no WHERE tenant_id = ...). If agent_trace_spans is tenant-scoped this returns cross-tenant telemetry.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly document/implement a platform-global aggregation table. If this is intended to be an admin cross-tenant view, gate it clearly (requirePlatformAdmin) and/or perform an explicit audit/consent step — do not leave tenant-scoped reads unconstrained.
- **server/routes/admin.ts:569** — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant filter. tool_performance appears to be tenant-related elsewhere — this reads across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope the query to the admin caller's tenant. If the table is intentionally global, document that fact and consider a dedicated global table to avoid accidental cross-tenant access.
- **server/routes/admin.ts:574** — Query selects from tool_performance (fail/ success counts, timestamps) with no tenant_id constraint. This can leak or aggregate tenant-specific metrics across tenants.
  - _Fix:_ Constrain the query to tenant_id = ${tenantId} (or to ADMIN_TENANT_ID if the intention is platform-owner-only reads), or move tenant-aggregated data to a purpose-built global table and mark it as such. Ensure application-level SQL filtering is the sole enforcement point and is present on every read.
- **server/routes/admin.ts:587** — SELECT from ai_insights WHERE status = 'new' ... is executed with no tenant filter. ai_insights is used elsewhere with tenant scoping; this read may expose incidents across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or otherwise explicitly restrict to platform-owned rows). All reads from tenant-scoped tables must include an application-level tenant filter to enforce isolation.
- **server/routes/admin.ts:598** — SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks is executed with no tenant constraint. heartbeat_tasks is a tenant-scoped table in other places — this returns rows for all tenants.
  - _Fix:_ Include WHERE tenant_id = ${tenantId} (or explicitly scope to platform-owner if intended). Ensure every read against tenant-scoped tables contains an explicit tenant_id filter or a documented platform-global exception.
- **server/routes/conversations.ts:97** — UPDATE on conversations omits tenant constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${conv.id} AND tenant_id = ${tenantId}`) so only the caller's tenant row can be modified. Alternatively use storage/update helpers that enforce tenant ownership.
- **server/routes/conversations.ts:98** — SELECT from project_conversations omits tenant constraint: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`
  - _Fix:_ Scope reads to the tenant (e.g. add `AND tenant_id = ${tenantId}`) or join against a tenant-scoped projects/conversations filter so an attacker cannot probe/observe rows belonging to other tenants.
- **server/routes/conversations.ts:101** — INSERT into project_conversations does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Fix:_ Always set tenant_id on tenant-scoped tables (e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`) and/or validate ownership of the referenced project and conversation before inserting.
- **server/routes/conversations.ts:103** — UPDATE on projects omits tenant constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Fix:_ Restrict the update to the caller's tenant (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) to prevent modifying projects in other tenants.
- **server/routes/conversations.ts:228** — INSERT into project_notes omits tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ...)`
  - _Fix:_ If project_notes is tenant-scoped, include tenant_id in the insert (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ...)`) or otherwise ensure the project belongs to the caller's tenant before inserting.
- **server/routes/inbox-notifications.ts:98** — UPDATE inbox_messages sets is_read without tenant constraint: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) or use the tenant id from the fetched row to avoid flipping a message in another tenant.
- **server/routes/heartbeat.ts:102** — UPDATE heartbeat_tasks for approval omits tenant constraint: `UPDATE heartbeat_tasks SET approval_status = 'approved', ... WHERE id = ${taskId}` (called under requireAdmin)
  - _Fix:_ Constrain approval updates to the task's tenant (e.g. `WHERE id = ${taskId} AND tenant_id = ${task.tenantId ?? tenantId}`) or verify ownership before issuing the UPDATE so a tenant-admin cannot affect another tenant's tasks.
- **server/routes/heartbeat.ts:117** — UPDATE heartbeat_tasks for rejection omits tenant constraint: `UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId}`
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `AND tenant_id = ${task.tenantId ?? tenantId}`) or check task.tenantId matches the caller's tenant prior to executing the UPDATE.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT statement only includes (project_id, note, author) and omits tenant_id, violating the requirement that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include the tenant id in the INSERT and bind it from the resolved tenant (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) RETURNING *). Ensure tenantId is validated earlier in the handler.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload path) does not set tenant_id. The INSERT INTO project_files statement omits tenant_id when creating new file rows.
  - _Fix:_ Pass and persist the tenant id explicitly in the INSERT (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}) RETURNING *). Ensure the handler validates the project's ownership and uses that tenantId.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload path) does not set tenant_id. The INSERT INTO project_files statement omits tenant_id when creating new file rows.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}) RETURNING *). Ensure the tenantId is the verified owner of the project and is passed to the query.
- **server/second-opinion.ts:406** — settleFusionReservation updates agent_cost_ledger by id only (WHERE id = ${reservationId}) and does not verify tenant_id. agent_cost_ledger is tenant-scoped, so this can modify another tenant's reservation row.
  - _Fix:_ Require and pass the tenantId into settleFusionReservation (or otherwise resolve and check ownership) and include AND tenant_id = ${tenantId} in the WHERE clause when updating the ledger row. Never update tenant-scoped rows by id alone.
- **server/second-opinion.ts:426** — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without constraining by tenant_id, allowing potential cross-tenant modifications of ledger rows.
  - _Fix:_ Accept/derive tenantId in releaseFusionReservation and include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Ensure any code that calls these functions supplies the correct tenant context.
- **server/seed.ts:34** — SELECT COUNT(*) FROM research_sessions executed with no tenant_id constraint — reads across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise supply an explicit tenant context. Avoid aggregates across all tenants unless running as an admin/maintenance job and make that explicit.
- **server/seed.ts:2854** — SELECT id, name, model FROM research_programs WHERE is_active = true executes without a tenant_id filter — reads tenant-scoped data for all tenants.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or run this code under an explicit admin/maintenance path that enforces and documents cross-tenant intent.
- **server/seed.ts:3204** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... contains no tenant_id constraint — it reads conversations across all tenants and the correlated NOT EXISTS check also lacks tenant-scoped constraints.
  - _Fix:_ Add tenant filters to the outer query and to the NOT EXISTS subquery (e.g. WHERE c.tenant_id = ${tenantId} AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.tenant_id = ${tenantId} AND pc.project_id = c.project_id AND pc.conversation_id = c.id)). Ensure all referenced tables are filtered by tenant.
- **server/seed.ts:3204** — SELECT from tenant-scoped table 'conversations' without constraining tenant_id. The query SELECT DISTINCT c.id, c.project_id FROM conversations c ... lacks any tenant filter and will scan all tenants.
  - _Fix:_ Add an application-level tenant filter to the WHERE clause (e.g. AND c.tenant_id = ${tenantId} or equivalent Drizzle .where(eq(conversations.tenantId, tenantId))). If the intent is cross-tenant maintenance, make that explicit and protect it behind an operator-only code path.
- **server/seed.ts:3212** — INSERT into project_conversations uses project_id and conversation_id derived from an unfiltered SELECT on 'conversations' without validating tenant ownership. This may create cross-tenant links.
  - _Fix:_ Before inserting, verify that both the conversation and the referenced project belong to the same tenant as the caller. Either include tenant_id in project_conversations (and set it explicitly), or only insert when you have confirmed ownership (e.g. SELECT ... WHERE id = X AND tenant_id = ${tenantId}). Do not use values derived from unfiltered queries.
- **server/seed.ts:4030** — SELECT all rows from tenant-scoped table 'heartbeat_tasks' (db.select().from(heartbeatTasks)) with no tenant filter; reads data across tenants.
  - _Fix:_ Restrict selects to the caller's tenant (e.g. .where(eq(heartbeatTasks.tenantId, tenantId))). If this is intended to seed global defaults, do so via an operator-only path and ensure the operation cannot leak tenant-specific data.
- **server/seed.ts:4407** — UPDATE on tenant-scoped table 'memory_entries' (UPDATE memory_entries SET ...) is executed without a tenant_id constraint, so it will modify rows across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(memoryEntries.tenantId, tenantId))) to the WHERE clause to limit the update to the intended tenant. If this is a system-wide migration intended for all tenants, make that explicit and run as an organized, operator-driven migration.
- **server/seed.ts:4685** — UPDATE on tenant-scoped table 'agent_knowledge' uses WHERE id = ${rows[0].id} but does not include tenant_id in the WHERE clause. This writes to a tenant-scoped table without enforcing tenant ownership in the UPDATE.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or otherwise verify ownership before updating. Even if id was selected earlier with a tenant constraint, subsequent writes should still include tenant checks to avoid accidental cross-tenant modification.
- **server/self-improvement.ts:170** — detectStagnation performs a SELECT from experiments that can be executed without a tenant_id constraint (conditions may contain only category). This allows cross-tenant reads when tenantId is omitted.
  - _Fix:_ Require a tenantId or fail-closed, or always include eq(experiments.tenantId, tenantId) in the WHERE clause. Do not issue unscoped SELECTs against tenant-scoped tables — validate/throw if caller tenant is missing.
- **server/self-improvement.ts:511** — getExperimentHistory returns db.select().from(experiments) with no tenant_id constraint when no filters are provided. This performs an unscoped read over the experiments table.
  - _Fix:_ Require the caller to pass tenantId (fail-closed to empty result if missing) or always add WHERE tenant_id = ${tenantId}. Never return global results from a tenant-scoped table without an explicit tenant filter or admin authorization check.
- **server/skill-seeker.ts:766** — listGaps executes a SELECT FROM capability_gaps WHERE status = ${status} without tenant_id when a caller supplies a status but no tenantId. This is an unscoped read across tenants.
  - _Fix:_ Require a tenantId for this query or include AND tenant_id = ${tenantId}. If an admin/global view is intended, gate it behind an explicit admin permission check; do not allow unscoped tenant-table reads by default.
- **server/skill-seeker.ts:777** — listGaps final else branch returns SELECT * FROM capability_gaps ORDER BY ... with no tenant_id filter, returning all gaps across tenants.
  - _Fix:_ Do not perform unscoped reads on tenant-scoped tables. Require tenantId and add WHERE tenant_id = ${tenantId}, or restrict this endpoint to privileged admin callers and document the access control explicitly.
- **server/skillify.ts:91** — SELECT from `messages` executed without a tenant_id constraint. The recentMsgs query filters only by conversationId but does not include tenant scoping in the SQL, relying on upstream checks instead of enforcing isolation at the DB query level.
  - _Fix:_ Constrain the messages query to the tenant in SQL. For example: .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or join messages to conversations with a tenant filter. Prefer using tenantScope(requireTenantScope(...)) or eq(messages.tenantId, tenantId) so reads are DB-scoped to the caller's tenant.
- **server/skillify.ts:203** — SELECT from `messages` in analyzeConversation executed without a tenant_id WHERE clause. The messages for a conversation are fetched by conversationId only; there is no tenantId condition at the SQL level.
  - _Fix:_ Include tenant scoping in the SQL (e.g., .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or perform a join to conversations and require conversations.tenant_id = tenantId in the WHERE clause. Use tenantScope/assertValidTenantId helpers to standardize checks.
- **server/skillify.ts:331** — SELECT from `messages` (recent messages for skillification) executed without a tenant_id constraint. The query filters only by conversationId and orders by createdAt, but lacks a tenant filter in SQL.
  - _Fix:_ Add tenant scoping to the messages query at the SQL level, e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or join via conversations and ensure conversations.tenant_id = tenantId. Prefer using tenantScope/requireTenantScope helpers to make tenant intent explicit.
- **server/storage.ts:248** — createConversation inserts into the tenant-scoped conversations table without enforcing that tenantId is present/valid in the incoming data (signature allows tenantId to be omitted).
  - _Fix:_ Require and validate tenantId before insert (e.g. assertValidTenantId(data.tenantId)), or derive tenantId from a trusted source and never allow a NULL/missing tenantId to be inserted. Fail-closed on missing/invalid tenantId.
- **server/storage.ts:289** — attachCitationMetadata runs raw SQL selecting FROM messages WHERE id IN (...) without any tenant_id constraint — messages is tenant-scoped.
  - _Fix:_ Require and pass the tenantId along, or add an explicit tenant filter (e.g. AND tenant_id = ${tenantId})/JOIN to ensure only citations for messages owned by the caller's tenant are returned. Ensure callers provide the tenant context when calling this helper.
- **server/storage.ts:389** — attachCitations updates the tenant-scoped messages table (UPDATE messages SET citations = ...) using only id in the WHERE clause and no tenant_id filter.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}) or derive and verify the message's tenant before updating. Never update tenant-scoped rows by id alone without verifying tenant ownership.
- **server/storage.ts:474** — deletePersona transaction updates tenant-scoped tables (conversations and memory_entries) by personaId WITHOUT scoping by tenant_id. These UPDATEs will affect rows across all tenants.
  - _Fix:_ Scope these updates/deletes by tenant_id (e.g. include eq(conversations.tenantId, personaTenantId) / eq(memory_entries.tenantId, personaTenantId)) or require the caller to provide the persona's tenant and assert it. Do not perform cross-tenant mutations by persona id alone.
- **server/storage.ts:615** — evictOldestConversationFacts updates conversation_facts (SET status='expired') using WHERE id IN (...) but omits tenant_id in the update WHERE clause — conversation_facts is tenant-scoped.
  - _Fix:_ Include tenant scoping in the final UPDATE (e.g. WHERE id = ANY(${victimIds}) AND tenant_id = ${tenantId}) to ensure only rows owned by the calling tenant are modified. Always carry tenant filter through from selection to mutation.
- **server/storage.ts:796** — updateMemoryEmbedding performs db.update(memory_entries).set(...).where(eq(memory_entries.id, id)) without any tenant_id constraint — memory_entries is tenant-scoped.
  - _Fix:_ Require tenantId as an argument (or validate ownership) and add eq(memory_entries.tenantId, tenantId) to the WHERE clause (or otherwise verify the row's tenant before updating). Avoid updating by id alone on tenant-scoped tables.
- **server/storage.ts:808** — updateKnowledgeEmbedding updates the tenant-scoped agent_knowledge table by id only (WHERE id = ...) with no tenant_id filter.
  - _Fix:_ Require and validate tenantId (or verify ownership of the row) and include eq(agent_knowledge.tenantId, tenantId) in the WHERE clause before updating. Ensure embedding writes are tenant-scoped.
- **server/storage.ts:1279** — getAllDataForExport runs many SELECTs over tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, dailyNotes, etc.) with no tenant filtering — returns platform-wide tenant data.
  - _Fix:_ Restrict this export API to a properly-authorized admin path and require an explicit admin/tenant parameter. If the intent is per-tenant export, require and enforce tenantId in every query (e.g. WHERE tenant_id = ${tenantId}). Add strong access control and audit logging for any global export.
- **server/subagents.ts:248** — _spawnSubagentImpl calls storage.getConversation(parentConversationId, params.tenantId ?? ADMIN_TENANT_ID). It reads a tenant-scoped conversation using caller-supplied parentConversationId but will default to ADMIN_TENANT_ID when tenantId is missing — allowing a lookup without verifying the caller's tenant.
  - _Fix:_ Do not default to an admin tenant when caller tenant is absent. Require the caller to pass a validated tenantId or derive tenantId from an unscoped, auditable lookup followed by explicit ownership validation. Enforce fail-closed behavior: reject spawn requests when tenant context is missing or mismatched.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (no eq(customTools.tenantId, tenantId)). This can leak presence/metadata across tenants or allow cross-tenant name collisions to be detected.
  - _Fix:_ Include an explicit tenant filter in the WHERE clause (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or otherwise scope the query to the caller's tenantId before reading tenant-scoped rows.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without constraining by tenant_id. Reads against tenant-scoped tables must include tenant restrictions.
  - _Fix:_ Add tenant scoping to the query, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). If `inserted` includes tenantId, use it; otherwise pass the tenantId through the call/transaction and assert it in the WHERE clause.
- **server/tools.ts:2978** — COUNT(*) query against messagesTable executed without any tenant_id filter — reads global message counts across tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. filter by WHERE tenant_id = ${tenantId}) or call a tenant-scoped helper (storage/db wrapper) that applies tenant filtering. If this is meant to be per-tenant, change to db.select(...).from(messagesTable).where(eq(messagesTable.tenantId, tenantId)).
- **server/tools.ts:3137** — Project listing query contains sub-selects counting project_files, project_notes, and project_conversations that do not include tenant_id constraints. These read tenant-scoped tables without an explicit tenant filter.
  - _Fix:_ Add tenant constraints to the sub-selects (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or rewrite the query to JOIN on tenant-scoped projects and explicitly include tenant_id on each referenced table to enforce isolation.
- **server/tools.ts:3199** — Duplicate-check SELECT on project_files (SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ...) lacks tenant_id constraint — can match files from other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) when checking for duplicates, or scope the check via a JOIN to a tenant-scoped projects row to guarantee the project belongs to the caller's tenant.
- **server/tools.ts:3240** — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} does not filter by tenant_id — reading project_conversations (tenant-scoped) without tenant constraint can leak or operate on other tenants' links.
  - _Fix:_ Restrict reads to the tenant (e.g. WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) or join via projects/conversations with tenant filters to enforce ownership before acting.
- **server/tools.ts:4412** — Query selects from tenant-scoped table `memory_entries` without constraining by tenant_id. The SELECT (SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ORDER BY created_at DESC LIMIT 5) can return rows across tenants, violating tenant isolation.
  - _Fix:_ Enforce tenant scoping on the query. Pass and use the caller's tenant id (e.g. params._tenantId) and parameterize it in the WHERE clause: WHERE tenant_id = ${params._tenantId} AND persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ... . Alternatively, perform an ownership/assertion check before the query (like assertTenantScope) or use the application's tenant-scoped DB helpers so all reads from tenant-scoped tables include tenant_id.
- **server/tools.ts:7121** — INSERT into project_files does not include tenant_id. The code inserts a project-scoped file row without setting tenant_id even though tenant isolation is enforced at the app level (INSERTs must explicitly set tenant_id).
  - _Fix:_ Include tenant_id in the INSERT and set it to the caller's tenant (params._tenantId). Prefer using Drizzle's insert API (or add tenant_id to the SQL column list) and ensure assertProjectInTenant validated ownership before inserting. Example: await db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: params.project_id, fileName: ..., ... }).returning();
- **server/tools.ts:9081** — UPDATE on proposed_skills lacks a tenant constraint. db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) updates by id only and does not restrict by tenant_id, allowing cross-tenant modification of rows.
  - _Fix:_ Add a tenant equality predicate to the WHERE clause (AND tenant_id = currentTenant). Using Drizzle: await db.update(proposedSkills).set({...}).where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))); Alternatively, include tenantId filtering in raw SQL: WHERE id = ... AND tenant_id = ${tid}.
- **server/tools.ts:9106** — UPDATE on proposed_skills missing tenant constraint. The update that marks a proposal 'accepted' uses where(eq(proposedSkills.id, row.id)) but does not check tenant_id.
  - _Fix:_ Constrain the UPDATE to the calling tenant (e.g. .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)))) so a tenant cannot update another tenant's proposed_skills row.
- **server/tools.ts:9123** — UPDATE on proposed_skills missing tenant constraint. The rejection path updates proposed_skills by id only (no tenant_id guard), enabling potential cross-tenant updates.
  - _Fix:_ Include the tenant_id in the WHERE clause (AND tenant_id = tid) or use Drizzle's eq(proposedSkills.tenantId, tid) in the .where() to ensure only the tenant's own row can be modified.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE on tenant-scoped table `browser_workflows` without tenant_id constraint: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. include tenant_id = ${tenantId} in the WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))). Example: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`) — or otherwise bind the tenant in the update to enforce isolation.
- **server/tools/domains/files/handlers.ts:835** — INSERT into tenant-scoped table `project_files` does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`
  - _Fix:_ Include tenant_id explicitly in the inserted columns and pass the trusted tenant (ctx.tenantId). E.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ${fileName}, ...). Validate tenantId is present/numeric before the INSERT.
- **server/tools/domains/files/handlers.ts:839** — UPDATE on tenant-scoped table `projects` without tenant_id constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`
  - _Fix:_ Constrain the UPDATE by tenant. Add tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where(eq(projects.tenantId, tenantId))). For example: db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`) to prevent cross-tenant writes.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE on tenant-scoped table knowledge_triples is executed without restricting by tenant_id (WHERE id = ${row.id}). This allows modifying rows belonging to other tenants if an id from another tenant is supplied or leaked.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}`); — or, when using Drizzle, use .where(eq(table.tenantId, tenantId)). Alternatively, first SELECT the row scoped to tenant_id and only update if the tenant matches (defense-in-depth / RETURNING tenant_id verification).
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` executed without a tenant_id filter (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). Even though this branch is guarded by tenantId === 1, the query returns data across all tenants without an explicit tenant constraint.
  - _Fix:_ Restrict the query to the intended tenant by adding a WHERE tenant_id = ${tenantId} clause, or if the intent is an admin/global enumeration, make that explicit in code/comments and minimize returned data (avoid any key-related columns). Prefer returning aggregated/admin-only metrics from a safe audit path rather than querying tenant-scoped rows without tenant scoping. Ensure this admin path is logged/audited.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses only `WHERE id = ${id}` (no tenant_id constraint). Writes to tenant-scoped tables must also be constrained by tenant_id to prevent cross-tenant mutation.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (and any other writes) — e.g. `WHERE id = ${id} AND tenant_id = ${tid}` — or otherwise include tenant_id in the WHERE clause. Do not rely solely on a prior SELECT that returned the id; every modifying statement must include tenant scoping.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` uses `WHERE id = ANY(${ids})` with no tenant_id constraint. This can update rows across tenants if ids are known.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. `WHERE id = ANY(${ids as any}) AND tenant_id = ${tid}`. Ensure batch updates always include tenant_id (or otherwise validate ownership for every id) before applying changes.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` uses `WHERE id = ${id}` without any `tenant_id = ...` constraint (same pattern as above in the strategic-win path).
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${id} AND tenant_id = ${tid}` (or pass tenant into the UPDATE predicate). Ensure all reads/writes to tenant-scoped tables always include the caller's tenant_id in the WHERE clause.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without also filtering by tenant_id. Reading a tenant-scoped `conversations` row by id alone violates the app-level isolation invariant.
  - _Fix:_ Query conversations by both id and tenant_id (e.g. WHERE id = ${convId} AND tenant_id = ${claimedTenantId}) or fetch the conversation via a tenant-scoped accessor. Avoid blind single-key reads and ensure you verify ownership using a tenant-bound query rather than post-hoc comparisons alone.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} updates a tenant-scoped table (agent_cost_ledger) using only the row id, with no tenant_id constraint. This can modify rows belonging to other tenants if an id is misused.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) and pass tenantId through to settleVentureReservation so updates are always constrained to the calling tenant. Validate that the reservation belongs to the tenant before performing the UPDATE.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} in releaseVentureReservation likewise updates a tenant-scoped table by id only (no tenant_id filter).
  - _Fix:_ As above, include tenant_id in the WHERE clause (WHERE id = ${reservationId} AND tenant_id = ${tenantId}) and/or verify ownership of the reservation before releasing it. Pass tenantId into releaseVentureReservation and make the update tenant-scoped.
- **server/video-job-runner.ts:204** — SELECT cancelRequested FROM video_jobs WHERE jobId = ${jobId} reads the tenant-scoped videoJobs table by jobId only (no tenant_id constraint) in isCancelRequested. The runner reads tenant-scoped data without an explicit tenant filter/ownership check.
  - _Fix:_ Make isCancelRequested tenant-aware: accept/resolve tenantId and query WHERE jobId = ${jobId} AND tenant_id = ${tenantId}, or first look up the job row and assert tenant ownership before returning the flag. Avoid unscoped reads against tenant-scoped tables.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs performs db.update(video_jobs).set(...) WHERE status IN (...) AND updatedAt < cutoff with no tenant_id constraint — this mutates rows across all tenants without per-tenant scoping.
  - _Fix:_ If this is an admin/maintenance routine, restrict invocation to an admin-only context and document it clearly; otherwise restrict updates to a specific tenant (add AND tenant_id = ${tenantId}) or iterate per-tenant with explicit authorization. At minimum require and check an admin guard before running unscoped updates to tenant-scoped tables.

### MEDIUM (45)

- **server/agentic/task-forces.ts:40** — String-built SQL array literal from request-derived personaIds is interpolated into a sql template (personaIdArrayLiteral -> `${literal}::int[]`) — this constructs SQL from user input and risks injection or unexpected SQL semantics.
  - _Fix:_ Avoid constructing SQL array literals by hand. Pass the persona id array as a parameterized value (use the driver's/ORM's array binding or sql.array helper), or validate/convert to a typed parameterized array and bind it rather than embedding a preformatted string into the SQL fragment.
- **server/auth.ts:177** — Query against `apiKeys` selects rows by keyHash/isRevoked but does not constrain by tenant_id. Reads from a tenant-scoped table occur without tenant scoping.
  - _Fix:_ Restrict the lookup to the expected tenant when possible (e.g. add eq(apiKeys.tenantId, expectedTenantId)) or otherwise verify ownership immediately after fetch and reject if the key's tenantId doesn't match the caller's tenant context. Make the tenant constraint explicit in the SQL when a tenant context is available.
- **server/auth.ts:1019** — SELECT from `password_reset_tokens` uses only the token hash (WHERE token = ${tokenHash}) and does not include tenant_id. This reads tenant-scoped rows without a tenant constraint.
  - _Fix:_ Require/accept tenant context when looking up a reset token and include AND tenant_id = ${tenantId} in the WHERE clause, or change the API to always require tenant scoping for reset-token operations so queries cannot touch rows for other tenants.
- **server/auth.ts:1029** — DELETE FROM `password_reset_tokens` WHERE token = ${tokenHash} executes without a tenant_id constraint (deleting token rows across tenants if a token collides).
  - _Fix:_ Include tenant scoping in the DELETE (e.g. WHERE token = ${tokenHash} AND tenant_id = ${tenantId}), or require a tenant context parameter to the function so cleanup affects only the intended tenant.
- **server/auth.ts:1036** — clearExpiredResetTokens runs DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} with no tenant filter — it removes rows across all tenants.
  - _Fix:_ Perform expiration cleanup per-tenant (loop tenants or run a guarded maintenance job) or otherwise add tenant scoping. If global deletion is intended, gate it behind explicit admin authorization and document the intent.
- **server/auth.ts:292** — deleteSession runs DELETE FROM auth_sessions WHERE token = ${token} without also constraining tenant_id — deleting by token alone risks touching sessions belonging to other tenants if token handling is ambiguous.
  - _Fix:_ Include tenant_id in the DELETE or verify the session's tenant before deleting. Prefer DELETE ... WHERE token = ${token} AND tenant_id = ${tenantId} (or fetch-and-validate the session's tenant before deleting) to ensure tenant isolation.
- **server/auto-memorize.ts:62** — setWatermark does SELECT id FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} LIMIT 1 without tenant_id — may read an unrelated tenant's row.
  - _Fix:_ Add tenant_id = ${ADMIN_TENANT_ID} (or pass a tenant argument) to the SELECT so the code updates/inserts the watermark for the correct tenant only.
- **server/auto-memorize.ts:69** — UPDATE agent_knowledge SET content = ... WHERE id = ${existing.rows[0].id} updates a row by id only without also constraining tenant_id — risk of modifying another tenant's row if ids collide or queries returned a different tenant's id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${ADMIN_TENANT_ID}) to ensure the update only affects the intended tenant's row.
- **server/capability-review.ts:491** — UPDATE against tenant-scoped table `capability_reviews` uses `WHERE id = ${pending.reviewId} AND reused = false` but does NOT constrain by tenant_id. This can update a capability_reviews row belonging to another tenant if ids collide or pending mapping is mis-associated.
  - _Fix:_ Record and check tenant context when marking reviews reused, and include tenant_id in the WHERE clause. For example, store tenantId in the pendingReuseByConversation entry and run `WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false`. Alternatively, change markReviewReused to accept a tenantId and use it in the WHERE clause.
- **server/ceo-orchestrator.ts:1806** — getActivePlan(planId) returns an in-memory OrchestrationPlan by id with no tenant check. A caller that supplies a planId could retrieve another tenant's plan because the function does not validate plan.tenantId against the caller.
  - _Fix:_ Require a tenantId parameter (or a caller context) and verify plan.tenantId === callerTenantId before returning the plan. If this API is only for internal use, document and enforce caller-side tenant checks; otherwise perform the check here to prevent cross-tenant leakage.
- **server/ceo-orchestrator.ts:1810** — getActivePlansForConversation(conversationId) returns activePlans matching a conversationId without verifying tenant ownership. It does not enforce that the caller is authorized for the tenant that owns those plans (plan.tenantId).
  - _Fix:_ Either accept a tenantId argument and filter results to plans where plan.tenantId === tenantId, or fetch and validate each plan's tenantId against the caller's tenant context before returning. Ensure any callers cannot use conversationId alone to enumerate or access other tenants' plans.
- **server/crews-engine.ts:875** — addFlowStep INSERT INTO flow_steps stores a provided crew_id without verifying that the referenced crew belongs to the same tenant. A foreign id supplied by the caller is persisted without ownership validation (cross-tenant reference risk).
  - _Fix:_ Before inserting, validate the supplied crewId is owned by the caller's tenant (e.g. call getCrew(params.crewId, params.tenantId) and reject or nullify crewId if not found). Ensure any foreign id from caller input is ownership-checked prior to persisting.
- **server/doc-collections.ts:136** — listCollections runs subqueries that count rows FROM doc_chunks WHERE collection_id = c.id but the subqueries do not include a tenant_id filter. These counts can surface or mix data across tenants.
  - _Fix:_ Constrain the subqueries by tenant_id, e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id AND tenant_id = ${tenantId}), so all counts are explicitly tenant-scoped.
- **server/doc-collections.ts:456** — getCollectionStatus executes subqueries (COUNT/SUM over doc_chunks) that reference doc_chunks by collection_id without filtering on tenant_id. Aggregates may include rows from other tenants.
  - _Fix:_ Add tenant constraints to those subqueries (AND tenant_id = ${tenantId}) so reported stats are strictly limited to the requested tenant.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks SET embedding WHERE id = ${row.id} without also constraining tenant_id. Even though rows were selected with a tenant filter earlier, the UPDATE is not explicitly tenant-scoped and could be abused if ids are misused.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer passing tenantId through the function and assert it matches the selected rows before performing updates.
- **server/heartbeat.ts:1646** — Unscoped SELECT on tenant-scoped table `heartbeat_logs`: SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ... lacks tenant_id restriction.
  - _Fix:_ Add tenant scoping (e.g. WHERE task_name = ... AND status = 'success' AND tenant_id = ${tenantId}) or use the storage API that filters logs by tenant to avoid leaking/logging other tenants' heartbeat activity.
- **server/job-queue.ts:98** — claimDueJobs UPDATE/SELECT on agent_jobs modifies rows without any tenant_id restriction. This atomically claims pending jobs across all tenants without tenant scoping.
  - _Fix:_ If claiming should be tenant-scoped, add WHERE tenant_id = ${tenantId} (or pass tenant context) in both the inner SELECT and the outer UPDATE. If the worker is intentionally global, document that fact and restrict execution to a trusted background process; otherwise parameterize the claim to run per-tenant.
- **server/job-queue.ts:134** — reclaimExpiredLeases step 1: UPDATE agent_jobs ... WHERE status='running' AND lease_until < NOW() AND attempts >= max_attempts — no tenant_id constraint, touching rows for all tenants.
  - _Fix:_ Add tenant scoping when the reclaim is intended for a single tenant (WHERE tenant_id = ${tenantId} AND ...), or if this is a global reclamation, ensure only a trusted/privileged process runs it and that this behaviour is reviewed/audited.
- **server/job-queue.ts:148** — reclaimExpiredLeases step 2: UPDATE agent_jobs SET status='pending' ... WHERE status='running' AND lease_until < NOW() — no tenant_id filter; requeues jobs across all tenants.
  - _Fix:_ Scope the UPDATE by tenant_id when appropriate (e.g. WHERE tenant_id = ${tenantId} AND ...). If global behavior is intended, document and restrict to privileged system workers and consider per-tenant invocation to preserve isolation semantics.
- **server/job-queue.ts:177** — completeJob updates agent_jobs to mark success using only id/status/attempts guards, without tenant_id in the WHERE clause. A complete call could affect a job belonging to another tenant if misused.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}) or otherwise validate that the caller holds the tenant context for the job being completed.
- **server/job-queue.ts:220** — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} without tenant_id filtering. Reading job metadata by id only can leak cross-tenant information or be abused.
  - _Fix:_ Include tenant_id in the SELECT (WHERE id = ${id} AND tenant_id = ${tenantId}) or require/validate tenant context before reading job rows by id.
- **server/job-queue.ts:237** — failJob UPDATE path (terminal) writes agent_jobs without tenant_id in the WHERE clause (WHERE id = ${id} AND status = 'running' AND attempts = ${attempts}). This may mutate a job belonging to another tenant.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or otherwise assert tenant ownership for the job before writing terminal failure states.
- **server/job-queue.ts:257** — failJob UPDATE path (retry) updates agent_jobs (setting status='pending', next_run_at, etc.) without filtering by tenant_id. Jobs from other tenants could be requeued incorrectly.
  - _Fix:_ Restrict the UPDATE with tenant_id (WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}) or validate tenant ownership prior to updating retry state.
- **server/job-queue.ts:274** — cancelJob performs UPDATE agent_jobs SET status = 'cancelled' WHERE id = ${id} AND status IN (...) without tenant_id constraint — can cancel jobs for other tenants.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status IN (...)) or ensure only privileged admin calls may cancel across tenants and that the operation is explicitly documented and audited.
- **server/job-queue.ts:282** — retryJob UPDATE resets status/attempts for a job by id without including tenant_id in the WHERE clause. This may resurrect jobs belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${id} AND tenant_id = ${tenantId} AND status IN (...)) or require tenant scoping/authorization before performing retries.
- **server/job-queue.ts:385** — getJobStats aggregates counts FROM agent_jobs WHERE created_at > NOW() - INTERVAL '7 days' with no tenant_id filter — returns cross-tenant stats and touches a tenant-scoped table without tenant constraint.
  - _Fix:_ If the stats are intended to be per-tenant, add WHERE tenant_id = ${tenantId}. If global system stats are intended, ensure only privileged processes call this and document the global nature; otherwise provide a tenant-scoped API for tenant-facing dashboards.
- **server/knowledge-diversity-monitor.ts:266** — Query groups agent_knowledge by tenant_id/persona_id (SELECT tenant_id, persona_id, COUNT(*) ...) without a tenant WHERE filter; this scans a tenant-scoped table across all tenants.
  - _Fix:_ If the monitor should run per-tenant, iterate tenants and call the per-tenant snapshot function with the tenantId. If a global scan is intended, ensure it is restricted to a privileged background process, documented, and audited; otherwise add filtering to avoid cross-tenant data access.
- **server/lib/agent-trace.ts:125** — persistSpanClose issues an UPDATE against agent_trace_spans using only span_id in the WHERE clause (WHERE span_id = ${spanId}) but agent_trace_spans is tenant-scoped — the UPDATE lacks tenant_id and could close/modify spans belonging to another tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). If tenantId is not available at close time, record the tenantId with the open promise/context (when the span was opened) so persistSpanClose can safely bind the tenant_id in the WHERE clause.
- **server/lib/bwb-job-progress.ts:144** — setBwbPhase updates videoJobs with .where(scope(jobId,...)). scope() may omit the tenant predicate when BWB_TENANT_ID is missing, causing UPDATEs that are not constrained by tenant_id (writes pinned only by jobId).
  - _Fix:_ Always require and bind a tenant id for these writers. Either make envTenantId throw/refuse when missing or change the API to accept an explicit tenantId and fail/abort when not provided. Ensure the .where clause always contains eq(videoJobs.tenantId, <tenantId>) so updates cannot run unscoped.
- **server/lib/bwb-job-progress.ts:169** — updateBwbChapters updates videoJobs via .where(scope(jobId,...)); scope() can omit tenant_id when BWB_TENANT_ID is not present, causing tenant-unscoped UPDATEs.
  - _Fix:_ Require an explicit tenantId for calls from subprocesses or fail the write if envTenantId is not set. Ensure scope() always includes eq(videoJobs.tenantId, tenantId) so the UPDATE is tenant-scoped.
- **server/lib/bwb-job-progress.ts:203** — completeBwbJob SELECTs videoJobs using .where(scope(jobId)) which may omit tenant_id when BWB_TENANT_ID is missing — reads against a tenant-scoped table should be tenant-constrained.
  - _Fix:_ Require tenantId for the operation or include tenant_id in the scope. If the reconciler intentionally scans by jobId only, make that explicit and safe (e.g. validate jobId unguessability and document/mitigate the cross-tenant risk), but preferably include tenant predicate on the SELECT.
- **server/lib/bwb-job-progress.ts:211** — completeBwbJob later issues an UPDATE videoJobs .where(scope(jobId)) that may run without tenant_id when BWB_TENANT_ID is unset — potentially modifying rows across tenants.
  - _Fix:_ Ensure the UPDATE is always tenant-scoped (include eq(videoJobs.tenantId, tenantId)). Prefer explicit tenant arguments to the API and fail the operation when tenant id is unavailable rather than falling back to jobId-only writes.
- **server/lib/bwb-job-progress.ts:230** — bumpBwbHeartbeat updates videoJobs with .where(scope(jobId, { notDone: true, notFailed: true })) — scope() can omit tenant_id (BWB_TENANT_ID missing), permitting tenant-unscoped UPDATEs.
  - _Fix:_ Require or pass an explicit tenantId and ensure scope() always includes the tenant predicate. If env-threading can be missing, make the function a no-op or fail instead of performing an unscoped update.
- **server/lib/bwb-job-progress.ts:254** — failBwbJob updates videoJobs with .where(scope(jobId, { notDone: true })) — scope() may omit tenant_id when BWB_TENANT_ID is not set, so the UPDATE can affect rows across tenants.
  - _Fix:_ Guarantee tenant scoping for failure writes: require tenantId and include eq(videoJobs.tenantId, tenantId) in the WHERE. If the environment thread cannot supply tenant, abort or log and skip the update rather than performing an unscoped write.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only id in the WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) and does not include a tenant_id constraint. This can risk accidental cross-tenant modification in race/logic edge cases.
  - _Fix:_ Constrain the UPDATE by tenant as well. Change the WHERE to include tenant ownership, e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively re-lookup by id+tenant before updating to ensure the row belongs to the caller's tenant.
- **server/process-governor.ts:1184** — SELECT joining governance_rules (LEFT JOIN governance_rules gr ON gr.id = ga.rule_id) reads governance_rules without verifying gr.tenant_id = caller tenant — a joined tenant-scoped table is not constrained.
  - _Fix:_ Constrain the joined governance_rules to the tenant as well (e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ${tenantId}) or add a WHERE/AND condition ensuring gr.tenant_id = ${tenantId}.
- **server/routes.ts:2063** — GET /api/presenter/:token selects presenter_sessions WHERE token = ${token} without any tenant_id constraint on the presenter_sessions table.
  - _Fix:_ Tie the lookup to an explicit tenant constraint or validate the token in a way that cryptographically binds it to a tenant (e.g. HMAC that includes tenantId) before returning session data. At minimum, ensure the token-check path performs an application-level ownership verification so rows from other tenants cannot be read by token misuse.
- **server/routes.ts:2091** — GET /api/presenter/:token/slide/:index queries presenter_sessions by token (SELECT id FROM presenter_sessions WHERE token = ${token}) with no tenant_id constraint before using the returned session id to access images.
  - _Fix:_ When resolving session by token, enforce an ownership check that binds the session to an expected tenant (or use a token that encodes/validates tenant) so subsequent reads of presenter_slide_images cannot access another tenant's data.
- **server/routes.ts:2118** — POST /api/presenter/:token/tts uses SELECT id FROM presenter_sessions WHERE token = ${token} without a tenant_id constraint.
  - _Fix:_ Ensure presenter session token lookup also validates or enforces the tenant context (either by including tenant_id in the WHERE clause when known, or by verifying the token is a tenant-bound capability) before allowing TTS actions that read or operate on tenant-scoped resources.
- **server/routes.ts:2178** — POST /api/presenter/:token/repair-images selects presenter_sessions WHERE token = ${token} with no tenant_id constraint (then reads/writes presenter_slide_images rows based on that session).
  - _Fix:_ Validate and enforce tenant ownership when resolving presenter_sessions by token. If this endpoint is internal, consider requiring tenant to be part of the authenticated caller's context (or use an internal capability that includes tenant binding) and add a tenant_id = ... filter to the lookup.
- **server/skillify.ts:223** — SELECT from `personas` executed without tenant scoping. When resolving a persona by id the code queries personas by id only and does not include a tenant_id condition, which can leak persona records across tenants if personas are tenant-scoped.
  - _Fix:_ Include tenant filtering in the persona query: .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))). If personas are intended to be global, document that explicitly; otherwise use tenantScope/assertValidTenantId to enforce tenant scoping.
- **server/storage.ts:239** — getConversationUnscoped performs an unscoped SELECT on the tenant-scoped conversations table (no tenant_id filter).
  - _Fix:_ Restrict this API: require an explicit, well-documented internal/trusted caller flag and force callers to validate ownership of the returned row. Prefer returning (and using) a tenant-scoped getter (e.g. getConversation(id, tenantId)) from HTTP handlers; log/monitor and gate unscoped usage tightly.
- **server/storage.ts:300** — createMessage reads conversations by id without a tenant filter to derive tenantId (SELECT ... FROM conversations WHERE id = ...). This is an unscoped read of a tenant-scoped table driven by caller-supplied conversationId.
  - _Fix:_ Make the contract explicit: either accept the caller's tenantId and validate it against the conversation row, or use a clearly-audited 'getConversationUnscoped' path and require the caller to perform ownership checks. Prefer failing if tenant context is absent/ambiguous.
- **server/tenant-fork.ts:226** — UPDATE of a forked config table (intra-table reparenting) updates rows by id only (.where(eq(entry.table.id, p.newId))) with no tenant_id constraint. Even though this runs in the fork transaction, writes against tenant-scoped tables must include tenant scoping.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId)))) so the reparent step explicitly bounds modifications to the newly-created tenant's rows.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed with no tenant_id filter. This enumerates provider configuration across tenants (used in an admin-only branch) without an explicit tenant constraint.
  - _Fix:_ If the intent is to run this only for the admin tenant, ensure an explicit guard and audit trail, or add a tenant_id = ${tenantId} filter for non-admin callers. If a global count is truly required for admin, clearly document and audit the operation; otherwise constrain queries to the caller's tenant to preserve tenant isolation.

### LOW (2)

- **server/ceo-orchestrator.ts:1818** — getAllActivePlans() returns all active plans across tenants (no tenant filtering). If called from request handlers, this could leak cross-tenant state.
  - _Fix:_ Limit this function to internal/admin-only usage or add an optional tenantId arg to filter by plan.tenantId. Enforce access controls so request handlers cannot call this and obtain other tenants' plans.
- **server/routes.ts:771** — backfillProjectDriveFolders() startup routine queries projects JOIN tenants (SELECT p.id, p.name, p.tenant_id, ...) without any tenant_id filter, iterating over all tenant-scoped rows.
  - _Fix:_ If this is intended to run globally by a trusted background job, note the behavior in code comments; otherwise scope operations per-tenant or run via an explicit admin process. Consider performing such wide scans in a maintenance path clearly separated from request handlers and with controlled permissions/auditing.

### Suppressed — confirmed false positives (160)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — performing a write to the tenant-scoped `conversations` table without constraining tenant_id. This can modify another tenant's row if an attacker controls the c values returned by upstream queries.
  - _Why false positive:_ c values come from prior join enforcing projects.tenant_id = conversations.tenant_id; update targets that verified conversation id only.
- **server/chat-engine.ts:571** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` is missing a tenant_id constraint (only filtered by project_id). This allows a verified project_id to be used to read files across tenants if project ownership assumptions are ever bypassed.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via already-verified parent project.
- **server/chat-engine.ts:582** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` uses only project_id in the WHERE clause and omits tenant_id. Even though projectId was previously fetched, the query itself lacks an explicit tenant constraint, risking cross-tenant data exposure if assumptions change.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via already-verified parent project.
- **server/chat-engine.ts:631** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` filters only on conversation_id and does not include tenant_id. Reading an archive by conversation_id without an explicit tenant constraint can leak another tenant's compacted history if conversation_id handling is manipulated.
  - _Why false positive:_ c.id comes from convs query constrained by c.tenant_id = tenantId in same call path.
- **server/chat-engine.ts:649** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed using only conversation_id (WHERE conversation_id = ${c.id}) and lacks tenant_id. This can expose message content across tenants if conversation_id references are abused.
  - _Why false positive:_ messages are read for c.id values already selected from conversations with c.tenant_id = tenantId.
- **server/data-protection.ts:125** _(upstream-verified)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} is executed without constraining by tenant_id. This deletes tenant-scoped rows (messages) without confirming tenant ownership of those rows.
  - _Why false positive:_ conv.id comes from immediately prior SELECT of conversations rows, and delete targets child messages for those verified parent conversations.
- **server/data-protection.ts:126** _(upstream-verified)_ — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} is executed without a tenant_id constraint. This removes tenant-scoped archive rows without ensuring they belong to the targeted tenant.
  - _Why false positive:_ conv.id comes from immediately prior SELECT of conversations rows, and delete targets archive rows for those verified parent conversations.
- **server/data-protection.ts:127** _(upstream-verified)_ — DELETE FROM conversations WHERE id = ${conv.id} is executed without constraining by tenant_id. This can remove conversations across tenants if IDs collide or are mis-bound.
  - _Why false positive:_ DELETE uses conv.id values selected just above from conversations rows including tenant_id; no untrusted caller-supplied id at this sink.
- **server/embeddings.ts:726** _(upstream-verified)_ — backfillEmbeddingVecs performs UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} with no tenant_id constraint, allowing updates across tenants by id alone.
  - _Why false positive:_ Rows being updated were selected immediately above from memory_entries itself; update applies to those fetched rows, not caller input.
- **server/embeddings.ts:743** _(upstream-verified)_ — backfillEmbeddingVecs performs UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} without constraining by tenant_id, permitting cross-tenant updates.
  - _Why false positive:_ Rows being updated were selected immediately above from agent_knowledge itself; update applies to those fetched rows, not caller input.
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} without constraining by tenant_id. This lets a caller (or a bug) update another tenant's run row if runId is misbound.
  - _Why false positive:_ felix_loop_runs row is updated by server-internal runId for the worker's own run, not from an untrusted request boundary.
- **server/felix-loop.ts:690** _(internal-worker-pk)_ — Error-path UPDATE against tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} without constraining by tenant_id. This can update a run from a different tenant.
  - _Why false positive:_ Error-path update uses same server-generated internal runId for the worker's own run, not caller-controlled.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE against tenant-scoped table 'projects' uses WHERE projects.id = ${projectId} without constraining by tenant_id. Even though code validated ownership earlier, the UPDATE itself lacks an explicit tenant constraint and could modify another tenant's row if projectId is tampered or a logic bug occurs.
  - _Why false positive:_ Same function fetched project by id and explicitly compared project.tenantId to tenantId before this update.
- **server/heartbeat.ts:101** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table `research_schedules`: UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id restriction in WHERE clause.
  - _Why false positive:_ Update uses sched.id from the schedule row already loaded in the worker loop, with tenant context carried as sched.tenant_id.
- **server/heartbeat.ts:106** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table `research_schedules`: UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Update targets the current sched row already selected and processed by the worker; not an untrusted id sink.
- **server/heartbeat.ts:111** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table `research_schedules`: UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id in WHERE.
  - _Why false positive:_ heartbeat worker updates by server-derived sched.id from internal scheduler loop, not untrusted request input
- **server/heartbeat.ts:119** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table `research_schedules` during error recovery: UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id filter.
  - _Why false positive:_ error-recovery update uses internal worker row id from sched object, not caller-controlled input
- **server/heartbeat.ts:257** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} — missing tenant_id in WHERE clause.
  - _Why false positive:_ startup cleanup worker mutates heartbeat_tasks by server-fetched primary key t.id only
- **server/heartbeat.ts:261** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} — no tenant_id filter.
  - _Why false positive:_ same internal startup worker updates task by trusted server-loaded t.id
- **server/heartbeat.ts:2955** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id restriction.
  - _Why false positive:_ newTask.id is server-generated immediately after createHeartbeatTask in same flow, not attacker-supplied
- **server/heartbeat.ts:3406** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table `heartbeat_tasks`: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint present.
  - _Why false positive:_ approval_status update targets freshly created row by server-generated newTask.id
- **server/memory-graph.ts:194** _(fk-transitive)_ — INSERT into `memory_links` does not set tenant_id (INSERT INTO memory_links (... ) VALUES (...)). If memory_links is tenant-scoped and tenant_id is NOT NULL with no default, this either fails or silently writes cross-tenant/undetermined rows, allowing cross-tenant link pollution.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries by foreign keys, so isolation is transitive
- **server/oauth-subscriptions.ts:906** _(global-table)_ — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE) — reads all tenants' subscriptions including sensitive tokens/refresh data.
  - _Why false positive:_ Startup validator is an internal background sweep intentionally iterating all tenant-scoped subscriptions across the platform.
- **server/oauth-subscriptions.ts:915** _(global-table)_ — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace') — returns rows across all tenants.
  - _Why false positive:_ Startup auto-reconnect logic intentionally enumerates google-workspace subscriptions for all tenants in a server-side maintenance path.
- **server/oauth-subscriptions.ts:956** _(upstream-verified)_ — UPDATE against tenant-scoped table `oauth_subscriptions` using only id in WHERE (UPDATE ... WHERE id = ${sub.id}) — missing tenant_id constraint. This can modify another tenant's row if IDs collide or are discovered.
  - _Why false positive:_ sub.id comes from the immediately prior SELECT over oauth_subscriptions that returned tenant_id for the same row in this internal loop; update targets that verified row.
- **server/oauth-subscriptions.ts:981** _(upstream-verified)_ — UPDATE against tenant-scoped table `oauth_subscriptions` using only id in WHERE (UPDATE ... WHERE id = ${sub.id}) — missing tenant_id constraint.
  - _Why false positive:_ sub.id is sourced from the earlier validated subscription row selection in the same function, so this internal deactivation update is on that already-fetched row.
- **server/oauth-subscriptions.ts:1258** _(global-table)_ — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) used by proactiveTokenRefresh — iterates all tenants' active subscriptions.
  - _Why false positive:_ Proactive token refresh is a server-side maintenance job that intentionally iterates active subscriptions across tenants.
- **server/oauth-subscriptions.ts:1328** _(global-table)_ — Unscoped SELECT on tenant-scoped table `oauth_subscriptions` (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE) used by forceRefreshAllSubscriptions — reads across all tenants.
  - _Why false positive:_ Force-refresh-all is an internal cron-style maintenance routine expressly designed to scan all active subscriptions platform-wide.
- **server/orchestrator-ledger.ts:452** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${row.id} without also constraining tenant_id. The save path can thus update a memory entry that belongs to another tenant if id is reused or misbound.
  - _Why false positive:_ row.id is obtained moments earlier by SELECT id FROM memory_entries WHERE source = source AND tenant_id = tenantId AND category='orchestrator_ledger' inside the same transaction.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} — clearing/failing a plan without constraining by tenant_id. Unscoped writes against tenant-scoped plans table can affect other tenants' plans.
  - _Why false positive:_ The plan row, including tenant_id, was just claimed and returned by the immediately preceding UPDATE for this exact plan id in the same call path.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final status update: UPDATE plans SET status = ${finalStatus}, ... WHERE id = ${planId} — no tenant_id constraint. This updates a tenant-scoped row without tenant scoping, risking cross-tenant modification and leakage.
  - _Why false positive:_ This final write occurs after the same call path has already claimed and loaded the specific plan row, including tenant_id, for this plan id.
- **server/plan-executor.ts:775** _(internal-worker-pk)_ — Boot-time recovery: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — this global update touches the tenant-scoped plans table without constraining by tenant_id.
  - _Why false positive:_ Boot-time server recovery intentionally operates across all plans; not request-derived tenant access.
- **server/plan-executor.ts:785** _(internal-worker-pk)_ — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 — selecting pending plans without tenant_id constraint. This reads tenant-scoped data for all tenants with no tenant filter.
  - _Why false positive:_ Internal boot dispatcher selects plan ids globally to resume work; no untrusted caller boundary shown.
- **server/process-governor.ts:198** _(global-table)_ — SELECT from heartbeat_logs (response_time case) has no tenant_id constraint — reads tenant-scoped runtime logs across all tenants.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id; this metric reads a platform-global log table.
- **server/process-governor.ts:239** _(global-table)_ — SELECT from heartbeat_logs (cascading_failures case) lacks tenant_id filter — counts failing agents across all tenants.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id, so global failure aggregation matches table design.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id with NO tenant check — an external proposalId can expose or allow actions on other tenants' proposals.
  - _Why false positive:_ Verifier is a server worker reading by globally unique proposal id, not a request-bound tenant lookup in shown path.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} updates proposal rows without tenant scoping — writes to a tenant-scoped table without verifying ownership.
  - _Why false positive:_ Persist updates worker-owned proposal row by server-invoked primary key, not untrusted request input here.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} performs tenant-unsafe mutation — no tenant_id constraint.
  - _Why false positive:_ Rebase cleanup mutates proposal by worker-managed primary key inside internal verifier flow.
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} updates validation_result without tenant filtering — allows cross-tenant mutation if proposalId is attacker-controlled.
  - _Why false positive:_ Internal verifier writes rebase outcome to proposal by server-side id within same worker path.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes rebased diff back to the DB with NO tenant constraint.
  - _Why false positive:_ Background verifier updates code_proposals by server-supplied proposalId via fireAndForgetVerify(), not directly from an untrusted tenant request boundary.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE to `memory_entries` (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}) omits tenant_id. This updates a tenant-scoped row by id only without re-checking ownership.
  - _Why false positive:_ storedId comes from an immediately preceding SELECT on memory_entries constrained by input.tenantId and persona_id in the same call path.
- **server/research-engine.ts:310** _(upstream-verified)_ — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} — missing tenant constraint. This can change another tenant's program record.
  - _Why false positive:_ programId was just verified by SELECT * FROM research_programs WHERE id = programId AND tenant_id = tenantId in the same function before this corrective update.
- **server/research-engine.ts:549** _(internal-worker-pk)_ — UPDATE research_sessions SET ... WHERE id = ${sessionId} — write to tenant-scoped `research_sessions` without tenant_id in WHERE. This can alter another tenant's session row.
  - _Why false positive:_ endSession operates on in-memory activeSessions entries keyed by server-created sessionId, not an untrusted cross-tenant request parameter.
- **server/research-engine.ts:786** _(upstream-verified)_ — UPDATE research_programs SET baseline_metric_value = ${numericMetricValue}, baseline_label = ${...} WHERE id = ${session.programId} AND baseline_metric_value IS NULL — missing tenant_id constraint, allowing cross-tenant mutation of program baseline.
  - _Why false positive:_ session.programId/session.tenantId originate from startResearchSession, which verified the program row with tenant_id before creating the server-side session object.
- **server/research-engine.ts:820** _(internal-worker-pk)_ — UPDATE research_experiments SET ... WHERE id = ${experimentId} — multiple writes to `research_experiments` are performed by id only and do not include tenant_id. This can modify experiments belonging to other tenants.
  - _Why false positive:_ experimentId is a server-generated/internal worker identifier within the research session flow, not taken from an untrusted request boundary.
- **server/research-engine.ts:851** _(internal-worker-pk)_ — UPDATE research_experiments ... WHERE id = ${experimentId} in DB-timeout error handler — write executed without tenant constraint.
  - _Why false positive:_ Timeout handler updates research_experiments by internal server-generated experimentId inside worker flow, not from caller-controlled input.
- **server/research-engine.ts:878** _(internal-worker-pk)_ — UPDATE research_experiments ... WHERE id = ${experimentId} in auth-error handling — missing tenant filtering on a write to a tenant-scoped table.
  - _Why false positive:_ Auth-error handler updates by internal experimentId in the same server worker execution path, not from an untrusted boundary.
- **server/research-engine.ts:894** _(internal-worker-pk)_ — UPDATE research_experiments SET ... WHERE id = ${experimentId} in transient-error branch — no tenant_id filter present.
  - _Why false positive:_ Transient-error handler updates by internal experimentId created and managed server-side within the research worker flow.
- **server/research-engine.ts:1733** _(upstream-verified)_ — UPDATE code_proposals SET status = 'reverted', reviewed_at = NOW() WHERE id = ${proposalId} — missing tenant_id constraint when reverting proposals.
  - _Why false positive:_ same function first SELECTs code_proposals by id and tenant_id, then uses that verified proposal row for subsequent revert logic
- **server/routes.ts:4337** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' omits tenant_id. The code executes `INSERT INTO project_notes (project_id, note, author) VALUES (...)` without setting tenant_id (columns are NOT NULL with no default per invariant). This can create rows not tied to a tenant or assigned incorrectly, breaking isolation.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects via project_id, so insert without tenant_id is by design
- **server/sculptor.ts:94** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` executed without constraining by tenant_id (WHERE id = ${session.id} only). This allows cross-tenant modification if an attacker can guess another session id.
  - _Why false positive:_ session row was just inserted with params.tenantId and returned server-side; UPDATE uses that freshly created server-owned PK, not untrusted input
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` (setting conversation_id) is performed with WHERE id = ${session.id} only and lacks tenant_id filtering.
  - _Why false positive:_ conversation_id update targets the same freshly inserted session row returned from prior INSERT for params.tenantId
- **server/seed.ts:2859** _(upstream-verified)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without constraining by tenant_id. This can modify rows belonging to other tenants.
  - _Why false positive:_ seed routine first SELECTs the exact research_programs rows to fix, then updates each returned server-selected id during internal seeding
- **server/seed.ts:3136** _(upstream-verified)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... performs a write to a tenant-scoped table without any tenant_id constraint.
  - _Why false positive:_ owner-only seed path is gated by existingDeploy check and updates a fixed project id in internal seeding, not caller-controlled input
- **server/seed.ts:3212** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) ... inserts into a likely tenant-scoped table without specifying tenant_id — tenant_id is required to enforce isolation.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table scoped transitively via tenant-scoped project/conversation parents
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment `usageCount` on tenant-scoped `custom_tools` uses only id in the WHERE clause (.where(eq(customTools.id, tool.id))) with no tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ same call path first loads tool with where(name, tenant_id); subsequent update uses that verified row's server-fetched id
- **server/tools.ts:3205** _(fk-transitive)_ — INSERT INTO project_files does not set tenant_id. Tenant-scoped table rows must be written with tenant_id to prevent cross-tenant data contamination and to satisfy the invariant that tenant_id is NOT NULL with no default.
  - _Why false positive:_ project_files has no tenant_id and is transitively isolated through prior tenant-scoped project ownership check
- **server/tools.ts:3220** _(fk-transitive)_ — INSERT INTO project_notes omits tenant_id — writing to a tenant-scoped table without tenant_id may create rows visible to other tenants or violate NOT NULL constraints.
  - _Why false positive:_ project_notes has no tenant_id and insert is preceded by project ownership check on tenant-scoped parent projects.
- **server/tools.ts:3243** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id — creating relationship rows without tenant attribution breaks explicit-tenant invariants and enables cross-tenant contamination.
  - _Why false positive:_ project_conversations has no tenant_id; code verifies project tenant and conversation tenant before inserting child/link row.
- **server/ab-optimizer.ts:110** _(internal-worker-pk)_ — SELECT from tenant-scoped table `experiments` with no tenant filter (reads all tenants).
  - _Why false positive:_ heartbeat-callable system sweep intentionally processes running experiments across tenants and updates each by id+tenant_id.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_activity` uses only the row id (WHERE id = activityId) with no tenant_id constraint — allows cross-tenant updates.
  - _Why false positive:_ completeActivity updates by server-generated activityId returned from internal tracking flow, not from a request boundary.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_evals` uses only id in WHERE (WHERE id = ${evalId}) with no tenant_id constraint.
  - _Why false positive:_ evalId was just inserted server-side in same function and then updated by that internal primary key.
- **server/agentic-engines.ts:147** _(upstream-verified)_ — UPDATE on tenant-scoped table `ai_insights` (SET action_taken) uses WHERE id = ${insight.insightId} with no tenant_id constraint.
  - _Why false positive:_ routeInsightToMinerva is called with tenantId from upstream-selected/stored insight rows and this update targets that known insight id within same call path.
- **server/agentic-engines.ts:160** _(internal-worker-pk)_ — SELECT from `ai_insights` to find pending plan markers queries rows across all tenants (no tenant_id filter).
  - _Why false positive:_ durability sweep is an internal system job intentionally scanning tenant-scoped ai_insights across tenants for retry processing.
- **server/agentic-engines.ts:242** _(internal-worker-pk)_ — UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...) — this bulk UPDATE has no tenant_id constraint and will update rows across tenants.
  - _Why false positive:_ auto-apply sweep is an internal system-wide worker intentionally bulk-updating eligible ai_insights across tenants, then handling returned rows internally.
- **server/agentic-engines.ts:262** _(upstream-verified)_ — UPDATE ai_insights SET action_taken = ... WHERE id = ${Number(r.id)} — missing tenant_id in WHERE clause (per-row update during sweep).
  - _Why false positive:_ row ids come directly from the immediately preceding UPDATE ... RETURNING on ai_insights in the same call path.
- **server/agentic-engines.ts:344** _(global-table)_ — SELECT from `heartbeat_logs` counting calls (task_name != 'Self-Reflection') has no tenant filter — reads global logs across tenants.
  - _Why false positive:_ schema marks heartbeat_logs as not tenant-scoped; aggregate read is from a genuinely global table.
- **server/agentic-engines.ts:448** _(global-table)_ — SELECT recent heartbeat_logs (status='success') used in predictive engine has no tenant filter — may return logs from other tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; query reads platform-global logs by design.
- **server/agentic-engines.ts:516** _(global-table)_ — SELECT aggregating heartbeat_logs (task_name, status, COUNT/AVG) has no tenant filter — aggregates across all tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and appears intentionally global for system-wide performance aggregation.
- **server/agentic-features.ts:638** _(upstream-verified)_ — UPDATE outreach_enrollments SET status = 'completed' WHERE id = ${enrollment.id} — missing tenant_id constraint on UPDATE.
  - _Why false positive:_ enrollment.id comes from rows selected just above with e.tenant_id = params.tenantId in the same call path.
- **server/agentic-features.ts:671** _(upstream-verified)_ — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} — no tenant_id in WHERE clause.
  - _Why false positive:_ update uses enrollment.id from tenant-scoped SELECT at lines 619-627, so ownership was enforced earlier in same flow.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE outreach_enrollments SET reply_classification = ... WHERE id = ${row.id} — missing tenant constraint (row.id could belong to another tenant).
  - _Why false positive:_ row.id is sourced from prior SELECT constrained by e.tenant_id = params.tenantId before the UPDATE.
- **server/agentic/approvals.ts:122** _(internal-worker-pk)_ — Bulk UPDATE agentApprovals to expire stale approvals does not include a tenant_id constraint — it updates pending approvals across all tenants.
  - _Why false positive:_ server-side worker sweep intentionally expires stale approvals across tenants and returns ids/tenantIds for internal follow-up; not driven by untrusted tenant input.
- **server/agentic/resume-worker.ts:66** _(internal-worker-pk)_ — Unscoped SELECT over tenant-scoped tables (agent_runs / agent_approvals) — the query at lines 66-85 reads rows across all tenants without a tenant_id WHERE filter.
  - _Why false positive:_ internal resume worker scans globally then acts using joined run/approval ids plus tenant_id; not sourced from request input.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — a write to a tenant-scoped table without enforcing tenant ownership.
  - _Why false positive:_ helper updates agent_runs by server-generated primary key runId in internal worker code, not from an untrusted request boundary.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ state update is an internal helper keyed by server-generated runId, fitting the internal-worker-pk pattern.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ Updates agent_runs by server-generated PK in internal worker module; nearby tenant-scoped getRun exists, and runId is not shown coming from request input.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ Internal agent worker state-transition helper updates by global run PK, not a request-derived tenant boundary value in shown call path.
- **server/agentic/runs.ts:67** _(internal-worker-pk)_ — failRun performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ failRun is an internal server-side run lifecycle update by globally unique run id in agentic worker code.
- **server/agentic/runs.ts:76** _(internal-worker-pk)_ — pauseRun performs UPDATE agent_runs WHERE id = runId with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ pauseRun is another internal lifecycle mutation keyed by server-side run id rather than untrusted caller-supplied tenant context.
- **server/agentic/wake-scheduler.ts:80** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_wake_schedules` is executed without any tenant_id constraint (the subquery selects pending rows across all tenants). This claims/fires schedules across tenants in one statement.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due rows across tenants as a server-invoked maintenance worker, then emits per-row tenantId and finalizes by id+tenant_id.
- **server/auth.ts:1150** _(global-table)_ — loadSessionsFromDb does SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions without any tenant restriction — it reads all sessions across tenants into memory.
  - _Why false positive:_ Process-wide auth bootstrap intentionally loads all auth_sessions into the in-memory session cache; sessions are a platform-global auth mechanism despite tenant_id field.
- **server/auth.ts:1137** _(global-table)_ — clearExpiredSessions executes DELETE FROM auth_sessions WHERE expires_at < ${now} with no tenant_id constraint, deleting expired sessions across all tenants.
  - _Why false positive:_ Expired-session cleanup is a global maintenance operation over the shared auth_sessions store, not tenant-request data access.
- **server/auto-asset-capture.ts:145** _(fk-transitive)_ — INSERT INTO project_files omits any tenant_id column — project-files rows are project-scoped but the INSERT does not persist tenant_id. Relying on transitive isolation via project_id without storing tenant_id violates the rule that INSERTs must pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is project-scoped via project_id to tenant-scoped parent project.
- **server/auto-asset-capture.ts:165** _(fk-transitive)_ — INSERT INTO project_notes omits tenant_id (inserting project-scoped notes without explicit tenant column population). This relies on transitive isolation instead of writing tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is isolated transitively through project_id to its tenant-scoped parent.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE `credential_vault` sets last_used_at using only the credential id (WHERE id = ${id}) and omits tenant_id, allowing a write to a credential row without confirming tenant ownership.
  - _Why false positive:_ Function first fetches the credential via getCredential(id, tenantId) and only then updates last_used_at for that same verified row id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE `credential_vault` sets last_used_at using only the credential id (WHERE id = ${cred.id}) without constraining by tenant_id — same impact as above (cross-tenant write risk).
  - _Why false positive:_ Credential is first resolved through getCredentialForSite(siteUrl, tenantId); subsequent last_used_at update uses the id from that tenant-scoped row.
- **server/data-protection.ts:72** _(upstream-verified)_ — UPDATE conversations executed without constraining by tenant_id. The code SELECTs the conversation scoped to tenantId but then updates deleted_at/deleted_by using only id, lacking AND tenant_id = ${tenantId}. This can alter a conversation belonging to another tenant if IDs collide or in race conditions.
  - _Why false positive:_ Same function immediately SELECTs the conversation by id and tenant_id and aborts if not found before issuing the update for that verified row.
- **server/embeddings.ts:688** _(global-table)_ — backfillMissingKnowledgeEmbeddings SELECTs from agent_knowledge without any tenant_id filter, so it will read rows across all tenants.
  - _Why false positive:_ backfillMissingKnowledgeEmbeddings is an internal maintenance/backfill job processing rows globally, not a request-scoped tenant access path
- **server/embeddings.ts:716** _(global-table)_ — backfillEmbeddingVecs SELECTs memory_entries rows without a tenant_id constraint (SELECT ... FROM memory_entries ... LIMIT 200). This reads tenant-scoped data across tenants.
  - _Why false positive:_ backfillEmbeddingVecs is a server-side maintenance backfill over all memory_entries, not caller-driven tenant access
- **server/embeddings.ts:732** _(global-table)_ — backfillEmbeddingVecs SELECTs agent_knowledge rows without a tenant_id filter (SELECT ... FROM agent_knowledge ... LIMIT 200).
  - _Why false positive:_ backfillEmbeddingVecs globally processes agent_knowledge embeddings as internal maintenance rather than tenant-facing access
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' is performed by project id only (WHERE projects.id = ${projectId}) with NO tenant_id filter. Although code later checks project.tenantId, the SQL itself is not tenant-scoped and may read another tenant's project row.
  - _Why false positive:_ same function immediately verifies project.tenantId matches caller tenant before any sensitive use or return
- **server/google-drive.ts:829** _(upstream-verified)_ — SELECT from 'projects' in uploadAndShare is performed only by project id (.where(eq(projects.id, params.projectId))) with NO tenant_id filter. This reads project metadata across tenants before the explicit ownership check, violating the requirement that every read be tenant-scoped.
  - _Why false positive:_ same call path immediately checks proj.tenantId against params.tenantId and fails closed before proceeding
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images is performed without passing tenant_id (and without validating that the target session belongs to the expected tenant). If presenter_slide_images is tenant-scoped this allows cross-tenant writes.
  - _Why false positive:_ presenter_slide_images is referenced via presenter session; schema note only marks presenter_sessions as tenant-scoped, consistent with child table isolated transitively by session_id
- **server/google-workspace.ts:2378** _(upstream-verified)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint — updating by id alone may allow modifying sessions belonging to other tenants.
  - _Why false positive:_ sessionId is the server-created presenter session id used throughout the same creation/persist flow; this update finalizes that just-created row, not a caller-selected tenant row
- **server/heartbeat.ts:52** _(global-table)_ — Unscoped SELECT on tenant-scoped table `research_schedules`: SELECT * FROM research_schedules ... has no WHERE tenant_id constraint.
  - _Why false positive:_ heartbeat is an internal scheduler intentionally scanning due research_schedules across all tenants for platform-wide execution
- **server/heartbeat.ts:588** _(global-table)_ — Unscoped SELECT on tenant-scoped table `event_log`: SELECT * FROM event_log WHERE status = 'pending' ... has no tenant_id constraint and will return events across all tenants.
  - _Why false positive:_ heartbeat worker intentionally dequeues pending event_log rows globally and then routes each using the row's tenant_id
- **server/lib/action-ledger.ts:305** _(global-table)_ — sweepStaleAttempts performs a cross-tenant UPDATE on action_attempts without constraining by tenant_id (UPDATE action_attempts ... WHERE id IN (SELECT id FROM action_attempts ...)). This will update rows across tenants.
  - _Why false positive:_ reconciler is an internal maintenance sweep intentionally transitioning stale action_attempts across tenants platform-wide
- **server/lib/stuck-task-sweep.ts:56** _(global-table)_ — SELECT from tenant-scoped table `agent_runs` without constraining by tenant_id — query returns rows across all tenants.
  - _Why false positive:_ read-only stuck-task audit is an internal platform-wide sweep that intentionally reports rows across tenants
- **server/lib/stuck-task-sweep.ts:84** _(global-table)_ — SELECT from tenant-scoped table `mind_tickets` without constraining by tenant_id — query returns rows across all tenants.
  - _Why false positive:_ read-only stuck-task audit intentionally scans mind_tickets across tenants for admin/maintenance reporting
- **server/memory-graph.ts:65** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_categories` missing tenant_id constraint (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This allows updating a category row without verifying it belongs to the caller's tenant.
  - _Why false positive:_ catId comes from same-path tenant-scoped SELECT/INSERT in ensureCategory using tenantId, so the immediate count increment targets a category already proven to belong to that tenant
- **server/memory-graph.ts:181** _(fk-transitive)_ — SELECT from tenant-scoped table `memory_links` has no tenant_id filter (SELECT 1 FROM memory_links WHERE ... LIMIT 1). This reads cross-tenant link rows when checking existence.
  - _Why false positive:_ memory_links has no tenant_id and links memory_entries already scoped to tenant via prior SELECT; isolation is transitive through source/target memory IDs
- **server/memory-graph.ts:392** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` missing tenant scoping (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). This updates rows by id without verifying tenant ownership.
  - _Why false positive:_ mem.id comes from an earlier SELECT on memory_entries constrained by tenant_id = tenantId in the same flow
- **server/memory-intelligence.ts:271** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` missing tenant scoping inside a callback (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}). TenantId from the outer flow is not applied to this UPDATE.
  - _Why false positive:_ entry.id is from storage.createMemoryEntry with tenantId supplied, so this async follow-up updates the newly created tenant-owned row
- **server/memory-intelligence.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` missing tenant scoping in the update-branch (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}).
  - _Why false positive:_ entry.id is from storage.createMemoryEntry with tenantId supplied, so this async follow-up updates the newly created tenant-owned row
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` missing tenant scoping (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}). The earlier SELECT that produced c filtered by tenant, but the UPDATE itself lacks an explicit tenant_id guard.
  - _Why false positive:_ c.id values come from immediately preceding SELECT on knowledge_triples filtered by tenant_id = tenantId
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The code inserts project_files after assertProjectInTenant(...) but does not include tenant_id in the INSERT columns/values, violating the invariant that tenant-scoped writes must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is a child table of tenant-scoped projects; assertProjectInTenant verifies parent ownership before insert
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. In the parallel-produce path the code checks assertProjectInTenant(...) but then inserts project_files without setting tenant_id, breaking tenant-isolation-by-convention (and risking cross-tenant or null/default writes).
  - _Why false positive:_ project_files has no tenant_id and is a child table of tenant-scoped projects; assertProjectInTenant verifies parent ownership before insert
- **server/persona-export.ts:33** _(global-table)_ — Unscoped SELECT on tenant-scoped table `personas` (SELECT * FROM personas WHERE id = ${personaId}) — no tenant_id check / ownership verification before returning persona data.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so this SELECT is against a non-tenant-scoped table rather than a cross-tenant isolation breach.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not include tenant_id — a write to a tenant-scoped table is performed without scoping to the caller's tenant.
  - _Why false positive:_ t.id comes from a prior SELECT restricted to heartbeat_tasks.tenant_id = tenantId in the same call path, so the update targets rows already tenant-vetted.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks ... WHERE id = ${desk.id} lacks tenant_id constraint — mutates agent_desks without scoping to tenant.
  - _Why false positive:_ desk.id is sourced from the immediately preceding SELECT on agent_desks filtered by ad.tenant_id = tenantId, so ownership is enforced earlier in the same path.
- **server/research-engine.ts:1482** _(upstream-verified)_ — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL RETURNING id — claim/update performed without tenant_id. Although the SELECT that fed this loop filtered by tenant earlier, the UPDATE itself lacks tenant scoping.
  - _Why false positive:_ row.id comes from the earlier findings query that filtered research_experiments by tenant in the same replay path, so the claim update is operating on already tenant-scoped rows.
- **server/routes.ts:2319** _(signed-url)_ — GET /api/store/order/:sessionId queries delivery_logs by orderId/stripePaymentId without any tenant_id constraint, returning tenant-scoped delivery log rows to a public capability URL.
  - _Why false positive:_ Public order lookup is explicitly capability-based on unguessable Stripe sessionId and returns only public-safe fields.
- **server/routes/admin-zombie-detector.ts:38** _(upstream-verified)_ — Query reads from agent_trace_spans inside an admin transaction without constraining by tenant_id (no WHERE tenant_id = ...). If agent_trace_spans is tenant-scoped this returns cross-tenant telemetry.
  - _Why false positive:_ Route is explicitly restricted to platform admin tenant plus admin check before executing cross-tenant telemetry query.
- **server/routes/admin.ts:569** _(upstream-verified)_ — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant filter. tool_performance appears to be tenant-related elsewhere — this reads across tenants.
  - _Why false positive:_ Endpoint is gated to ADMIN_TENANT_ID and isAdminRequest, making this an intentional admin-wide diagnostic read.
- **server/routes/admin.ts:574** _(upstream-verified)_ — Query selects from tool_performance (fail/ success counts, timestamps) with no tenant_id constraint. This can leak or aggregate tenant-specific metrics across tenants.
  - _Why false positive:_ Same admin-only route already enforces platform-admin access before cross-tenant tool_performance query.
- **server/routes/admin.ts:587** _(upstream-verified)_ — SELECT from ai_insights WHERE status = 'new' ... is executed with no tenant filter. ai_insights is used elsewhere with tenant scoping; this read may expose incidents across tenants.
  - _Why false positive:_ Same admin-only route already enforces platform-admin access before cross-tenant ai_insights read.
- **server/routes/admin.ts:598** _(upstream-verified)_ — SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks is executed with no tenant constraint. heartbeat_tasks is a tenant-scoped table in other places — this returns rows for all tenants.
  - _Why false positive:_ Same admin-only route already enforces platform-admin access before cross-tenant heartbeat_tasks read.
- **server/routes/conversations.ts:97** _(upstream-verified)_ — UPDATE on conversations omits tenant constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Why false positive:_ Conversation row was just server-created with tenantId in this call path, so UPDATE by conv.id only affects the newly created tenant-owned row.
- **server/routes/conversations.ts:98** _(fk-transitive)_ — SELECT from project_conversations omits tenant constraint: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped parent rows; project ownership is verified just above and conv was just created for this tenant.
- **server/routes/conversations.ts:101** _(fk-transitive)_ — INSERT into project_conversations does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Why false positive:_ insert into project_conversations is correct by design because the table has no tenant_id and isolation is via verified tenant-scoped project/conversation parents.
- **server/routes/conversations.ts:103** _(upstream-verified)_ — UPDATE on projects omits tenant constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Why false positive:_ project id was verified in the same call path with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before this update.
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT into project_notes omits tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ...)`
  - _Why false positive:_ project_notes has no tenant_id and note insert is tied to projId resolved through a join requiring the tenant-scoped project owned by this tenant.
- **server/routes/inbox-notifications.ts:98** _(upstream-verified)_ — UPDATE inbox_messages sets is_read without tenant constraint: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`
  - _Why false positive:_ msg was fetched immediately above with WHERE id = msgId AND tenant_id = tenantId, so the subsequent update by msg.id operates on that verified row.
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT statement only includes (project_id, note, author) and omits tenant_id, violating the requirement that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and the parent project is verified just above with tenant_id = tenantId before inserting the child note row.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload path) does not set tenant_id. The INSERT INTO project_files statement omits tenant_id when creating new file rows.
  - _Why false positive:_ project_files has no tenant_id and file row is inserted only after verifying the parent project belongs to the caller tenant.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload path) does not set tenant_id. The INSERT INTO project_files statement omits tenant_id when creating new file rows.
  - _Why false positive:_ project_files has no tenant_id and the multipart upload path verifies the tenant-scoped parent project before inserting child file rows.
- **server/second-opinion.ts:406** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger by id only (WHERE id = ${reservationId}) and does not verify tenant_id. agent_cost_ledger is tenant-scoped, so this can modify another tenant's reservation row.
  - _Why false positive:_ server-internal helper updates agent_cost_ledger by server-generated reservationId, not directly from a request boundary.
- **server/second-opinion.ts:426** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without constraining by tenant_id, allowing potential cross-tenant modifications of ledger rows.
  - _Why false positive:_ server-internal cleanup helper releases a ledger reservation by server-generated reservationId rather than untrusted caller input.
- **server/seed.ts:34** _(upstream-verified)_ — SELECT COUNT(*) FROM research_sessions executed with no tenant_id constraint — reads across all tenants.
  - _Why false positive:_ Boot/dev seed snapshot import is an explicit maintenance path; COUNT(*) is only used to decide whether to seed any sessions globally, not a tenant request path.
- **server/seed.ts:2854** _(upstream-verified)_ — SELECT id, name, model FROM research_programs WHERE is_active = true executes without a tenant_id filter — reads tenant-scoped data for all tenants.
  - _Why false positive:_ Seed maintenance function fixes invalid models across all active research_programs intentionally in a boot/seed path, not per-tenant request handling.
- **server/seed.ts:3212** _(fk-transitive)_ — INSERT into project_conversations uses project_id and conversation_id derived from an unfiltered SELECT on 'conversations' without validating tenant ownership. This may create cross-tenant links.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table isolated transitively by tenant-scoped parent records; insert uses existing conversation/project pairing from maintenance backfill.
- **server/seed.ts:4030** _(upstream-verified)_ — SELECT all rows from tenant-scoped table 'heartbeat_tasks' (db.select().from(heartbeatTasks)) with no tenant filter; reads data across tenants.
  - _Why false positive:_ Seed file is initializing heartbeat defaults globally; select is only checking whether any heartbeat_tasks exist during bootstrapping, not serving tenant data to a caller.
- **server/seed.ts:4407** _(upstream-verified)_ — UPDATE on tenant-scoped table 'memory_entries' (UPDATE memory_entries SET ...) is executed without a tenant_id constraint, so it will modify rows across all tenants.
  - _Why false positive:_ System-wide migration/normalizer in seed path intentionally updates all matching memory_entries during maintenance, not from an untrusted tenant request.
- **server/seed.ts:4685** _(upstream-verified)_ — UPDATE on tenant-scoped table 'agent_knowledge' uses WHERE id = ${rows[0].id} but does not include tenant_id in the WHERE clause. This writes to a tenant-scoped table without enforcing tenant ownership in the UPDATE.
  - _Why false positive:_ Row id was selected immediately above with tenant_id = 1, and the subsequent update targets that selected row in the same call path.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from `messages` executed without a tenant_id constraint. The recentMsgs query filters only by conversationId but does not include tenant scoping in the SQL, relying on upstream checks instead of enforcing isolation at the DB query level.
  - _Why false positive:_ recentMsgs is fetched after analyzeConversation(conversationId, tenantId) verifies the conversation row belongs to tenantId in the same call path
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from `messages` in analyzeConversation executed without a tenant_id WHERE clause. The messages for a conversation are fetched by conversationId only; there is no tenantId condition at the SQL level.
  - _Why false positive:_ analyzeConversation first loads conversations.id and rejects when conv[0].tenantId !== tenantId before querying messages by that validated conversationId
- **server/skillify.ts:331** _(upstream-verified)_ — SELECT from `messages` (recent messages for skillification) executed without a tenant_id constraint. The query filters only by conversationId and orders by createdAt, but lacks a tenant filter in SQL.
  - _Why false positive:_ this query occurs after analyzeConversation(conversationId, tenantId) has already enforced conversation ownership for the same call path
- **server/storage.ts:289** _(upstream-verified)_ — attachCitationMetadata runs raw SQL selecting FROM messages WHERE id IN (...) without any tenant_id constraint — messages is tenant-scoped.
  - _Why false positive:_ attachCitationMetadata only re-reads message ids taken from msgs that were already selected with tenant_id filtering by the callers shown
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (no eq(customTools.tenantId, tenantId)). This can leak presence/metadata across tenants or allow cross-tenant name collisions to be detected.
  - _Why false positive:_ parsed.name is first rewritten via tenantToolName(parsed.name, tenantId), so the uniqueness check is already scoped by tenant-encoded name.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` by id without constraining by tenant_id. Reads against tenant-scoped tables must include tenant restrictions.
  - _Why false positive:_ The row was just inserted in this flow with tenantId and is re-read by its returned inserted.id; same-call-path provenance fixes tenant ownership.
- **server/tools.ts:3137** _(fk-transitive)_ — Project listing query contains sub-selects counting project_files, project_notes, and project_conversations that do not include tenant_id constraints. These read tenant-scoped tables without an explicit tenant filter.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are scoped through parent projects; outer query already restricts projects by tenant_id.
- **server/tools.ts:3199** _(upstream-verified)_ — Duplicate-check SELECT on project_files (SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ...) lacks tenant_id constraint — can match files from other tenants.
  - _Why false positive:_ A few lines above, ownerCheckF verifies the project belongs to tenantId; project_files has no tenant_id and duplicate check is against that verified project_id.
- **server/tools.ts:3240** _(upstream-verified)_ — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} does not filter by tenant_id — reading project_conversations (tenant-scoped) without tenant constraint can leak or operate on other tenants' links.
  - _Why false positive:_ Same path verifies project ownership and assertConversationInTenant confirms conversation tenant; project_conversations is tenantless child/link table.
- **server/tools.ts:7121** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The code inserts a project-scoped file row without setting tenant_id even though tenant isolation is enforced at the app level (INSERTs must explicitly set tenant_id).
  - _Why false positive:_ project_files has no tenant_id by schema and insertion is gated by assertProjectInTenant on the tenant-scoped parent project.
- **server/tools.ts:9081** _(upstream-verified)_ — UPDATE on proposed_skills lacks a tenant constraint. db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) updates by id only and does not restrict by tenant_id, allowing cross-tenant modification of rows.
  - _Why false positive:_ This update targets row.id created earlier in the same call path for tenant tid, so ownership is established before the by-id update.
- **server/tools.ts:9106** _(upstream-verified)_ — UPDATE on proposed_skills missing tenant constraint. The update that marks a proposal 'accepted' uses where(eq(proposedSkills.id, row.id)) but does not check tenant_id.
  - _Why false positive:_ This accepted-status update uses the same freshly created/owned proposedSkills row.id from the current tenant-scoped flow.
- **server/tools.ts:9123** _(upstream-verified)_ — UPDATE on proposed_skills missing tenant constraint. The rejection path updates proposed_skills by id only (no tenant_id guard), enabling potential cross-tenant updates.
  - _Why false positive:_ This rejection update also operates on the same proposal row created earlier in the same tenant-scoped request path.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table `browser_workflows` without tenant_id constraint: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`
  - _Why false positive:_ workflow is selected immediately above with id/name plus tenant_id, and the update uses workflow.id from that verified row.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`
  - _Why false positive:_ project_files has no tenant_id and is linked via tenant-scoped projects row selected with p.tenant_id = ctx.tenantId
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` without tenant_id constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`
  - _Why false positive:_ projId comes from prior SELECT joining projects with p.tenant_id = tenantId in the same call path
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE on tenant-scoped table knowledge_triples is executed without restricting by tenant_id (WHERE id = ${row.id}). This allows modifying rows belonging to other tenants if an id from another tenant is supplied or leaked.
  - _Why false positive:_ row.id values come only from contradictionRows selected just above with tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` executed without a tenant_id filter (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). Even though this branch is guarded by tenantId === 1, the query returns data across all tenants without an explicit tenant constraint.
  - _Why false positive:_ schema note says provider_keys has no tenant_id, so this is not a tenant-scoped table
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses only `WHERE id = ${id}` (no tenant_id constraint). Writes to tenant-scoped tables must also be constrained by tenant_id to prevent cross-tenant mutation.
  - _Why false positive:_ id is obtained from immediately prior SELECT scoped by tenant_id = tid and persona_id in the same flow
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses `WHERE id = ANY(${ids})` with no tenant_id constraint. This can update rows across tenants if ids are known.
  - _Why false positive:_ ids array is built solely from rows selected just above with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses `WHERE id = ${id}` without any `tenant_id = ...` constraint (same pattern as above in the strategic-win path).
  - _Why false positive:_ id comes from prior tenant-scoped SELECT on memory_entries with tenant_id = tid and persona_id = personaId
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without also filtering by tenant_id. Reading a tenant-scoped `conversations` row by id alone violates the app-level isolation invariant.
  - _Why false positive:_ the unscoped read is used only to verify actual tenant ownership against claimed tenant and rejects mismatches in the same path
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} updates a tenant-scoped table (agent_cost_ledger) using only the row id, with no tenant_id constraint. This can modify rows belonging to other tenants if an id is misused.
  - _Why false positive:_ internal helper settles a server-created reservation row by primary key reservationId, not directly from a request boundary here
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} in releaseVentureReservation likewise updates a tenant-scoped table by id only (no tenant_id filter).
  - _Why false positive:_ internal helper releases a server-created reservation row by primary key reservationId, not directly from a request boundary here
- **server/video-job-runner.ts:204** _(internal-worker-pk)_ — SELECT cancelRequested FROM video_jobs WHERE jobId = ${jobId} reads the tenant-scoped videoJobs table by jobId only (no tenant_id constraint) in isCancelRequested. The runner reads tenant-scoped data without an explicit tenant filter/ownership check.
  - _Why false positive:_ runner-internal function reads cancel flag by globally unique jobId for the server-managed job it is processing
