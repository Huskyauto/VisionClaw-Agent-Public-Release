# Tenant-Isolation Audit Report

_Generated 2026-09-10T01:24:22.654Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 275 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **393/668** file(s) audited · chunks 38/61 OK (0 failed, 23 pending)
- Findings: **139** (CRITICAL 29 · HIGH 83 · MEDIUM 27 · LOW 0)
- Severe dispositions: **112 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (29)

- **server/agentic-engines.ts:247** — UPDATE on tenant-scoped table `ai_insights` may run without tenant constraint when tenantId is undefined: `UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) ${tenantScope}` — tenantScope can be empty, causing cross-tenant writes.
  - _Fix:_ Require a tenantId (do not allow undefined) or ensure tenantScope is always set. Always include `AND tenant_id = ${tenantId}` in the WHERE clause for updates to tenant-scoped tables, or iterate per-tenant with an explicit tenant filter before writing.
- **server/agentic/approvals.ts:134** — Bulk UPDATE on tenant-scoped table `agentApprovals` with no tenant filter in expireStaleApprovals(): `UPDATE agentApprovals SET status = 'expired', decidedAt = new Date() WHERE status = 'pending' AND lt(expiresAt, now())` — this will expire approvals across all tenants.
  - _Fix:_ Scope the expiry to a tenant (add tenant filter) or iterate tenants explicitly. Never perform cross-tenant UPDATEs on tenant-scoped tables; include `AND tenant_id = ${tenantId}` or require the caller to pass a tenantId and apply the WHERE tenant filter for each affected row.
- **server/auth.ts:1323** — clearAllSessionsAsPlatformAdmin executes DELETE FROM auth_sessions with NO tenant filter, deleting every session across all tenants.
  - _Fix:_ If a global session wipe is intentional require and verify an explicit, auditable platform-admin authority (and log it) — which you do — but also make the cross-tenant intent explicit in code and callsites. Prefer requiring an explicit param or separate admin-only path for global wipes. If the operation should be per-tenant, change to DELETE FROM auth_sessions WHERE tenant_id = ${tenantId}.
- **server/commitment-drafter.ts:234** — UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} performs a WRITE on the tenant-scoped table 'commitments' without including tenant_id in the WHERE clause, allowing an update to another tenant's row if IDs collide.
  - _Fix:_ Always include tenant scoping on mutations. Change the statement to include the owning tenant (for example: WHERE id = ${row.id} AND tenant_id = ${row.tenantId} AND draft_status = 'draft_pending') so the update cannot affect rows belonging to other tenants. Apply this pattern to any corrective/unclaim paths.
- **server/email.ts:370** — SELECT against tenant-scoped table `inbox_messages` with no tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can cause cross-tenant dedupe/visibility issues and leaks.
  - _Fix:_ Include tenant filtering in the query (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or otherwise resolve/derive the tenant before querying. Ensure the check is done in the context of the resolved tenant to avoid skipping/stomping another tenant's rows.
- **server/email.ts:415** — SELECT from tenant-scoped table `inbox_messages` (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id) with no tenant_id filter. This scans rows across tenants.
  - _Fix:_ Restrict the query to a specific tenant (add AND tenant_id = ${tenantId}) or iterate per-tenant explicitly. Background maintenance must never run a tenant-scoped SELECT without an explicit tenant parameter.
- **server/email.ts:429** — UPDATE against tenant-scoped table `inbox_messages` (UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id}) without constraining tenant_id. Updating by id alone can modify another tenant's row.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or resolve and validate the tenant before performing the UPDATE.
- **server/embeddings.ts:842** — SELECT from tenant-scoped table `memory_entries` (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200) with no tenant_id filter. This backfill scans memories across all tenants.
  - _Fix:_ Perform backfills in a tenant-aware manner: accept a tenantId and include WHERE tenant_id = ${tenantId}, or iterate per-tenant. Avoid global SELECTs on tenant-scoped tables.
- **server/embeddings.ts:852** — UPDATE of `memory_entries` (UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}) without a tenant_id constraint. This can update rows belonging to other tenants.
  - _Fix:_ Only update rows with an explicit tenant filter (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Require or pass tenantId into the backfill/update function so every write is tenant-scoped.
- **server/embeddings.ts:858** — SELECT from tenant-scoped table `agent_knowledge` (SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200) with no tenant_id filter. This backfill may read knowledge across tenants.
  - _Fix:_ Run the agent_knowledge backfill per-tenant or accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to the query to preserve tenant isolation.
- **server/embeddings.ts:869** — UPDATE of `agent_knowledge` (UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}) without tenant_id constraint. This allows cross-tenant updates.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and ensure the backfill function operates with an explicit tenant context.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to executing an UPDATE without tenant scoping: UPDATE ${sql.raw(table)} ... WHERE id = ${id}. When options.tenantId is not provided this performs a tenant-agnostic write. Additionally it uses sql.raw(table) and sql.raw(...) with interpolated vector literals.
  - _Fix:_ Do not perform tenant-agnostic writes. Require a positive tenantId for all tenant-scoped tables and throw if missing. Replace the final fallback path with an explicit, parameterized code path per allowed table that includes AND tenant_id = ${tenantId}. Avoid sql.raw(table) for untrusted inputs; validate and use explicit branching on the allowlist and parameterized bindings for values.
- **server/health-audit.ts:314** — SELECT FROM tenant-scoped table `code_proposals` without scoping by tenant_id (no WHERE tenant_id = ...). This SELECT is followed by an UPDATE that also lacks tenant scoping — archiveStaleProposals() will mark rows archived across all tenants.
  - _Fix:_ Scope stale-proposal discovery and updates to a single tenant at a time. Either iterate tenants and run the SELECT/UPDATE with WHERE tenant_id = ${tenantId}, or add an explicit tenant_id filter to the SELECT and UPDATE. Do NOT run cross-tenant DELETE/UPDATE/UPDATE-by-id without including tenant_id in the WHERE clause. Consider central admin-only paths that explicitly document and protect cross-tenant operations, or enable Postgres RLS for authoritative protection.
- **server/health-audit.ts:346** — SELECT FROM tenant-scoped table `heartbeat_tasks` (stuck heartbeat query) without scoping by tenant_id. Subsequent UPDATE to disable tasks uses id list without tenant scoping — this may disable tasks for other tenants.
  - _Fix:_ When discovering and disabling stuck heartbeat tasks, include tenant_id in the SELECT and the UPDATE (e.g. WHERE tenant_id = ${tenantId} AND id = ${id}), or iterate per-tenant and constrain all statements to that tenant. Avoid UPDATE ... WHERE id = ANY(ids) across tenants without also checking tenant_id.
- **server/google-workspace.ts:2287** — INSERT INTO presenter_slide_images omits tenant_id (INSERT INTO presenter_slide_images (session_id,...)). If presenter_slide_images is tenant-scoped, this writes tenant-scoped data without explicitly setting tenant_id.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO presenter_slide_images (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)). For ON CONFLICT and DO UPDATE paths ensure tenant_id is part of the uniqueness/partitioning or include tenant_id in the WHERE clause used for conflict resolution/updates so rows cannot be misattributed to the wrong tenant.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint. If presenter_sessions is tenant-scoped, this allows updating another tenant's session by id.
  - _Fix:_ Always include tenant_id when updating tenant-scoped rows (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Prefer writing operations that accept and validate the caller's tenantId, and reject/verify session ownership before performing mutations.
- **server/lib/scheduled-post-runner.ts:61** — The CTE/UPDATE that picks and locks due rows from scheduled_posts does not constrain by tenant_id (SELECT id FROM scheduled_posts ... FOR UPDATE ...; UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). This selects/updates tenant-scoped rows across all tenants in one statement.
  - _Fix:_ Enforce tenant scoping at SQL level. Either run the picker per-tenant (iterate tenants and include WHERE tenant_id = <tenant>) or include tenant_id in the CTE and bind it through the UPDATE (e.g. SELECT id, tenant_id FROM scheduled_posts ... and UPDATE ... WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id). Never select/update tenant-scoped tables across tenants without an explicit tenant predicate and appropriate admin-only auditing.
- **server/memory-graph.ts:709** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped table by id only and omits tenant_id. This permits cross-tenant modification if ids are misused.
  - _Fix:_ Always include tenant scoping on UPDATEs to tenant-scoped tables. Change to: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Prefer verifying ownership of mem.id before the UPDATE (or update by id+tenant_id atomically).
- **server/memory-graph.ts:275** — INSERT INTO memory_links references newMemoryId (caller-supplied) and target ids without first validating that newMemoryId (and/or the referenced target id) belongs to the same tenant. This can create cross-tenant links / leak relationships across tenants.
  - _Fix:_ Verify ownership of any foreign id provided by the caller before writing cross-reference rows. For example, SELECT id FROM memory_entries WHERE id = ${newMemoryId} AND tenant_id = ${tenantId} LIMIT 1 and abort if not found. Ideally add tenant_id to the memory_links table and set it on INSERT (VALUES (..., tenant_id => ${tenantId})), and/or enforce foreign-key+tenant checks server-side so inserts are scoped to the caller's tenant.
- **server/oauth-subscriptions.ts:906** — Query selects rows from oauth_subscriptions without scoping by tenant (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This returns refresh_token/access metadata across all tenants (sensitive) and breaks tenant isolation.
  - _Fix:_ Avoid scanning oauth_subscriptions across tenants. Add an application-level tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise restrict this startup/cron check to operate per-tenant. Do NOT select refresh_token/access_token columns for other tenants; only operate on rows belonging to the intended tenant(s).
- **server/plan-executor.ts:516** — CAS claim UPDATE on tenant-scoped table `plans` mutates rows without constraining by tenant_id. The code executes `UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...` before validating or restricting to the plan's tenant. That lets this process flip/claim an approved plan belonging to any tenant by id.
  - _Fix:_ Include tenant_id in the UPDATE's WHERE clause (e.g. `WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved'`) or otherwise validate caller/admin authority before performing the UPDATE. Do not change a tenant-scoped row prior to verifying that the caller/process is authorized for that tenant. Use a two-column CAS (id+tenant_id) for the claim to make the action atomic and safe.
- **server/plan-executor.ts:794** — Boot-time recovery UPDATE resets `plans` rows from 'executing' → 'approved' without any tenant_id restriction: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id`. This will reset executing plans for all tenants, creating cross-tenant side-effects.
  - _Fix:_ Restrict the recovery UPDATE to an explicit tenant context or perform per-tenant recovery loops. If this is an admin-only recovery path, require and check an explicit admin-authorization context and include `AND tenant_id = ${tenantId}` in the WHERE clause. Avoid blanket cross-tenant updates; operate per-tenant or with an audited, clearly-authorized admin path.
- **server/plan-executor.ts:803** — SELECT to gather pending `plans` uses `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50` with no tenant_id filter and then re-dispatches executePlan(id). This schedules execution across tenants without enforcing tenant-scoped constraints or authorization.
  - _Fix:_ Limit the SELECT to a tenant context (e.g. `WHERE tenant_id = ${tenantId} AND status = 'approved'`) or iterate approved plans per-tenant after validating an explicit admin/maintenance authority. Never enqueue or execute tenant-scoped work across tenants without verifying tenant ownership and including tenant_id in the query.
- **server/process-governor.ts:1235** — UPDATE governance_rules increments trigger_count without a tenant_id constraint: UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id}. This can modify governance rules belonging to other tenants if IDs collide or are misused.
  - _Fix:_ Constrain the UPDATE to the caller's tenant: add AND tenant_id = ${tenantId} to the WHERE clause (WHERE id = ${rule.id} AND tenant_id = ${tenantId}). Ensure every UPDATE/DELETE against tenant-scoped tables includes tenant_id.
- **server/process-governor.ts:898** — UPDATE agent_desks sets status_note/updated_at using WHERE id = ${desk.id} with no tenant_id filter. Even though the desk.id came from a prior tenant-scoped SELECT, writes must also be explicitly tenant-scoped to avoid cross-tenant modification.
  - _Fix:_ Include tenant constraint on the UPDATE, e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}. Always include tenant_id in the WHERE clause on write operations to tenant-scoped tables.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` without tenant constraint: UPDATE agent_knowledge SET content = ... WHERE id = ${r.id} updates by id only and lacks AND tenant_id = <tenant>.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) to ensure the update cannot mutate a row belonging to another tenant. Always combine id-based updates with tenant_id in multi-tenant tables.
- **server/reference-learner.ts:290** — UPDATE against tenant-scoped table `memory_entries` without tenant constraint: UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId} uses id-only WHERE and can modify another tenant's row.
  - _Fix:_ Constrain the UPDATE by tenant_id as well (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}). Even if you previously selected the row by tenant, subsequent writes must still include tenant_id in the WHERE clause to prevent cross-tenant modification.
- **server/research-engine.ts:169** — cleanupZombieSessions performs a global UPDATE on research_sessions without scoping by tenant_id (WHERE clause lacks tenant_id). This can update/complete sessions across all tenants.
  - _Fix:_ Restrict the UPDATE to a tenant scope. Options: (1) require a tenantId parameter and include AND tenant_id = ${tenantId}; (2) build a per-tenant cleanup loop and include tenant_id IN (...) for explicit tenant ids; or (3) compute the set of tenant_ids to affect (from activeSessions or metadata) and parameterize the query. Never run cross-tenant writes without an explicit tenant filter.
- **server/routes.ts:180** — startup restore reads from tenant-scoped table `file_storage` without scoping by tenant_id (db.select(...).from(fileStorage).where(like(fileStorage.mimeType, 'image/%') ...)). This will restore files for all tenants to a single uploads directory, potentially exposing other tenants' private files to disk and to requesters.
  - _Fix:_ Restrict the restore to a single tenant at a time (e.g. iterate tenants and run the restore per-tenant) or include an explicit tenant_id constraint in the WHERE clause. Ensure any cross-tenant maintenance runs are executed in a controlled migration/admin context and never materialize other tenants' content onto a shared public filesystem.

### HIGH (83)

- **server/ab-optimizer.ts:110** — Global SELECT from tenant-scoped table `experiments` without tenant filter: `SELECT id, tenant_id, hypothesis, metric, persona_id, metadata, created_at FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100` (reads rows across all tenants).
  - _Fix:_ Constrain the query to a tenant (e.g. add WHERE tenant_id = ${tenantId} or run the heartbeat per-tenant). If the heartbeat must scan multiple tenants, iterate explicit tenant ids and run a tenant-scoped query per tenant so every SELECT is scoped to a single tenant_id.
- **server/agent-activity.ts:60** — SELECT from `personas` without tenant constraint in getPersonality(): `...from(personas).where(eq(personas.id, personaId))` — this can read persona data from other tenants.
  - _Fix:_ Restrict the lookup by tenant (e.g. include eq(personas.tenantId, tenantId) or otherwise pass and use the caller's tenantId). If personas are global by design, document that explicitly; otherwise make the personas query tenant-scoped.
- **server/agent-desk.ts:297** — SELECT id, name FROM `personas` with no tenant filter in getDesksOverview(): this returns personas across tenants.
  - _Fix:_ Add tenant scoping (e.g. SELECT id, name FROM personas WHERE tenant_id = ${tenantId}) or accept/require a tenantId parameter and filter by it. Do not issue unconstrained reads against tenant-scoped tables.
- **server/agentic-engines.ts:161** — SELECT from `ai_insights` in retryPendingMinervaRouting lacks tenant constraint: `SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50` — reads rows across tenants.
  - _Fix:_ Scope this query to a tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly iterate tenant ids and run tenant-scoped queries. Avoid reading tenant-scoped rows across all tenants in a single unconstrained query.
- **server/agentic-engines.ts:374** — SELECT from `personas` without tenant filter in runDecisionEngine: `SELECT name, role FROM personas WHERE is_active = true LIMIT 14` — can leak personas from other tenants.
  - _Fix:_ Filter personas by tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true). If personas are intentionally global, document and verify that is a deliberate global/shared table; otherwise enforce tenant scoping.
- **server/agentic/department-budgets.ts:143** — SELECT against tenant-scoped table `department_budgets` is executed without a tenant_id WHERE filter (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads rows across all tenants in a context that appears to be a per-tenant enforcement function, violating the invariant that tenant-scoped table access must be constrained by tenant_id at the application level.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly require/verify an admin/global context before enumerating all tenants. If the intent is to perform a global sweep, ensure this function runs only in a tightly-controlled admin process and that results are not exposed to unprivileged callers; otherwise change the implementation to iterate tenants via an explicit, authorized admin path and document the admin-only nature.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT on tenant-scoped tables: the query selects from agent_runs (and joins agent_approvals) with no WHERE tenant_id = <tenant> filter, returning rows across tenants.
  - _Fix:_ Restrict the query to a specific tenant context. Either iterate per-tenant and add a WHERE r.tenant_id = ${tenantId} (or use drizzle eq(agentRuns.tenantId, tenantId)), or otherwise ensure the scan is explicitly authorized/isolated (admin-only) and replace the global SELECT with a tenant-scoped query. Avoid returning rows from multiple tenants in a single unscoped query.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on agent_wake_schedules uses a sub-select without any tenant_id constraint. The statement claims/updates pending wakes across all tenants: UPDATE agent_wake_schedules ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() LIMIT ${limit} FOR UPDATE SKIP LOCKED).
  - _Fix:_ Scope the claim/update per tenant. Either add AND tenant_id = <tenant> to the inner SELECT or iterate per-tenant and run a tenant-scoped claim (e.g. SELECT ... WHERE tenant_id = ${tenantId} ... FOR UPDATE SKIP LOCKED then UPDATE WHERE id = ${id} AND tenant_id = ${tenantId}). Avoid global, un-scoped UPDATEs on tenant-scoped tables.
- **server/auth.ts:1331** — loadSessionsFromDb executes DELETE FROM auth_sessions WHERE expires_at < ${now} without constraining by tenant_id, performing a cross-tenant DELETE.
  - _Fix:_ Run expiry cleanup per-tenant or make this admin-only maintenance explicit. If invoked on boot as a global housekeeping task, clearly document the global nature and guard the call; otherwise add WHERE tenant_id = ${tenantId} or iterate tenant-by-tenant to avoid unscoped deletes.
- **server/auth.ts:1335** — loadSessionsFromDb executes DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' with NO tenant constraint, removing all legacy session rows across tenants.
  - _Fix:_ Limit legacy-token cleanup to a safe scope (e.g. per-tenant during tenant bootstrap/migration) or run under a platform-admin-only maintenance path. If global deletion is intended, ensure an auditable, one-time migration run rather than leaving an unguarded global DELETE in normal boot code.
- **server/auto-asset-capture.ts:184** — Backfill selects conversations across all tenants without a tenant filter: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0. This can lead to cross-tenant processing and writes.
  - _Fix:_ Restrict the backfill to a specific tenant (add AND c.tenant_id = ${tenantId}) or iterate tenants and perform the backfill per-tenant. At minimum, verify ownership before any write for each conversation (use assertConversationInTenant or join on tenant_id in the query).
- **server/chat-engine.ts:3731** — Call to capability-review.markReviewReused(conversationId, toolName) passes only conversationId (no tenant context). If markReviewReused reads/writes tenant-scoped storage keyed by conversationId it may access or mix data across tenants without an explicit tenant_id filter.
  - _Fix:_ Require and pass the caller's tenantId into markReviewReused (e.g. markReviewReused(tenantId, conversationId, toolName)) or perform an ownership check (confirm conversation belongs to tenant) before invoking. Ensure the callee enforces WHERE tenant_id = ? on all DB accesses and update its signature/implementation accordingly.
- **server/chat-engine.ts:3848** — Call to stuck-diagnostics.recordErrorForRetryStormDetection(conversationId, ...) uses conversationId without tenant context. If this diagnostic function persists or queries records keyed by conversationId it may operate across tenant boundaries without a tenant_id WHERE clause.
  - _Fix:_ Pass tenantId into recordErrorForRetryStormDetection (e.g. recordErrorForRetryStormDetection(tenantId, conversationId, ...)) or verify ownership of conversationId up-front. Update stuck-diagnostics to require tenantId and apply tenant_id filters to all DB queries/writes.
- **server/chat-engine.ts:4069** — Calls to stuck-diagnostics functions (recordToolCallForStuckDetection(conversationId, ...), postDiagnosticReport(...)) are invoked with conversation-scoped data but no tenantId. These diagnostic/reporting helpers may perform reads/writes against tenant-scoped tables without enforcing tenant_id constraints.
  - _Fix:_ Change invocations to include tenantId (e.g. recordToolCallForStuckDetection(tenantId, conversationId, ...), postDiagnosticReport(tenantId, ...)) or perform an explicit ownership check before calling. Update stuck-diagnostics implementations to require tenantId and add WHERE tenant_id = ? to all DB operations to guarantee tenant isolation.
- **server/chat-engine/project-context.ts:159** — SELECT from project_files lacks an explicit tenant constraint. The query reads project-scoped files using only project_id without verifying tenant_id, which can expose files across tenants if project_files is tenant-scoped.
  - _Fix:_ Include an explicit tenant filter (e.g. AND tenant_id = ${tenantId} or .where(eq(project_files.tenantId, tenantId))). If you rely on projectId ownership, enforce/verify that projectId belongs to the caller's tenant in the same statement or earlier and document that equivalence; the safest fix is to add tenant_id to the WHERE clause.
- **server/chat-engine/project-context.ts:171** — SELECT from project_notes lacks an explicit tenant constraint. The query filters only by project_id and could return notes from other tenants if project_notes is tenant-scoped.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${tenantId} or .where(eq(project_notes.tenantId, tenantId))). If relying on the previously-verified projectId, still include tenant_id in the query to make isolation explicit and robust against future schema changes.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments executes a SELECT against the tenant-scoped table 'commitments' without constraining to a specific tenant (no WHERE tenant_id = <caller tenant>), enumerating tenant rows.
  - _Fix:_ Avoid unscoped scans of a tenant-scoped table. If you truly need to enumerate tenants, read tenant IDs from the central tenants registry (or run this only in a clearly-administered/audited context). Otherwise iterate per-tenant and run tenant-scoped queries (e.g. WHERE tenant_id = ${tenantId}). Ensure only an authorized admin job can run any cross-tenant operations and document/audit that explicitly.
- **server/commitments.ts:135** — SELECT ... FROM commitments WHERE status = 'active' ... is a read from the tenant-scoped 'commitments' table with NO tenant_id constraint, returning rows across all tenants.
  - _Fix:_ Enforce tenant scoping. If the intent is an admin-global scan, require and verify an explicit admin context and limit exposed fields; preferably enumerate tenants and run this scan per-tenant using WHERE tenant_id = ${tenantId} to preserve tenant isolation. If cross-tenant aggregation is required, implement explicit authorization, logging and minimal redaction at the application boundary.
- **server/data-protection.ts:149** — SELECT on conversations reads all soft-deleted conversations across tenants (no tenant_id predicate). This enumerates other tenants' conversation rows in memory before per-row deletes.
  - _Fix:_ Avoid a cross-tenant scan. Either run purge per-tenant (iterate tenants and add WHERE tenant_id = <tenant>), or scope the initial SELECT with an explicit tenant filter supplied to the function. Alternatively run the purge inside a transaction using withTenantTx(tenantId, ...) so RLS (if enabled) prevents cross-tenant reads.
- **server/deep-interview.ts:337** — INSERT INTO project_notes does not set tenant_id. Per the project's invariant tenant-scoped tables require tenant_id supplied explicitly on INSERTs; omitting it can create rows without tenant ownership or with a default admin tenant, causing cross-tenant contamination.
  - _Fix:_ Include tenant_id in the INSERT column list and values. Example: INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, ${state.tenantId}, ${briefContent}, NOW() FROM ... WHERE p.tenant_id = ${state.tenantId} LIMIT 1. Ensure the SELECT still verifies the project belongs to the tenant (as it already does).
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table dfy_intake_forms by token with NO tenant_id constraint (getDfyIntakeByToken). This returns a tenant-owned row without scoping to the caller's tenant.
  - _Fix:_ Require and pass an explicit tenantId for lookups, or otherwise enforce tenant scoping in the query. E.g. add tenantId parameter to getDfyIntakeByToken and query with WHERE token = ${token} AND tenant_id = ${tenantId}. If token-only public access is intended, document the risk and ensure tokens are globally unique and validated against an explicit caller scope before returning tenant data.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} — missing tenant_id constraint. This DELETE can affect rows for other tenants if collection_id/doc_path collisions exist.
  - _Fix:_ Add an explicit tenant_id predicate to the DELETE (AND tenant_id = ${tenantId}) or otherwise use a tenant-scoped identifier. Verify the collection belongs to the requesting tenant before deleting and include tenant_id in the WHERE clause.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} — no tenant_id constraint. Updating by id alone may modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}, or update via a composite predicate that includes tenant_id. Always assert tenant ownership before mutating tenant-scoped rows.
- **server/embeddings.ts:259** — Use of sql.raw(...) with an interpolated, provider-derived vector literal ('${vec}'::vector) inside queries (used in vectorSearchMemory). Even though vec is validated elsewhere, raw string interpolation of externally-derived data is an injection/isolation risk.
  - _Fix:_ Avoid embedding external values via sql.raw. Prefer safe parameter binding or ensure vec is strictly validated (as numeric array) at the call site (use the existing vecLiteral validator) and centralize the construction so the raw interpolation is provably safe. Where possible, pass vector data as parameters or use a DB driver feature to bind binary/vector parameters instead of string interpolation.
- **server/felix-brain.ts:24** — SELECT from tenant-scoped table `agent_runs` without a tenant_id filter — this reads rows across all tenants during boot rehydration.
  - _Fix:_ Constrain the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant with proper authorization. If the intent is an admin/global operation, require an explicit admin/operational guard and document that this privileged action is safe and audited.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table `felix_loop_runs` uses WHERE id = ${runId} but does not include tenant_id — write may affect rows belonging to other tenants if runId is mis-bound.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId}) so the write is constrained to the caller's tenant. Always combine id-based updates with tenant_id checks for tenant-scoped tables.
- **server/felix-loop.ts:690** — Error-path UPDATE on tenant-scoped table `felix_loop_runs` updates by id only (WHERE id = ${runId}) without tenant_id, allowing cross-tenant modification on error handling paths.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}). Ensure every UPDATE/DELETE/SELECT on tenant-scoped tables includes an explicit tenant_id constraint, including error/recovery code paths.
- **server/firecrawl.ts:231** — UPDATE to tenant-scoped table 'scrapedPages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) and does not constrain the write by tenant_id. This allows an authenticated caller to potentially update another tenant's scraped page if they can supply or guess an id.
  - _Fix:_ Enforce tenant scoping on the UPDATE. Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or re-query/validate ownership and then perform the update using both id and tenantId. Always ensure writes to tenant-scoped tables include tenant_id in the WHERE.
- **server/firecrawl.ts:363** — UPDATE to tenant-scoped table 'scrapedPages' inside the crawl loop uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) and lacks a tenant_id constraint. This similarly permits cross-tenant modification if an id from another tenant is referenced.
  - _Fix:_ Add tenant scoping to the UPDATE. Use a compound WHERE that includes the tenant (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise validate ownership before performing the write. Every write against tenant-scoped tables must be constrained to the caller's tenant.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW() queries tenant-scoped `research_schedules` across all tenants (no tenant_id constraint). This is used to run scheduled work and can read/act on schedules for other tenants.
  - _Fix:_ Either iterate tenants and query research_schedules per-tenant with WHERE tenant_id = ${tenantId}, or explicitly include tenant_id in the WHERE clause when selecting schedules to run. Ensure any updates that follow also include tenant_id in their WHERE clauses to avoid cross-tenant effects. If cross-tenant scheduling is intentional, make it an admin-only, explicitly-audited flow and ensure explicit tenant scoping is applied when mutating rows.
- **server/heartbeat-startup.ts:76** — Direct UPDATE against tenant-scoped table heartbeat_tasks without constraining by tenant_id (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). This can modify rows belonging to other tenants.
  - _Fix:_ Avoid raw cross-tenant UPDATEs. Either include an explicit tenant predicate (e.g. AND tenant_id = ${t.tenantId}) or call the storage layer (e.g. storage.updateHeartbeatTask) which enforces tenant scoping. If this is intentionally cross-tenant scheduler work, move it into a single audited scheduler boundary and document/guard it explicitly.
- **server/heartbeat-startup.ts:80** — Direct UPDATE against tenant-scoped table heartbeat_tasks without constraining by tenant_id (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). This can modify rows belonging to other tenants.
  - _Fix:_ Same fix as above: include AND tenant_id = ${t.tenantId} or use storage.updateHeartbeatTask to enforce tenant scoping, or centralize into a documented cross-tenant scheduler boundary with proper access controls.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE heartbeat_tasks ... WHERE id = ${id} ... (claimDueHeartbeatTasksAcrossTenants) updates heartbeat_tasks without a tenant_id constraint. This can advance/claim tasks for the wrong tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or change the API to accept tenant id(s) per task and use them in the WHERE clause. Alternatively, perform the update through a storage-layer function that enforces tenant ownership and prevents cross-tenant modifications unless explicitly authorized in a documented scheduler boundary.
- **server/heartbeat.ts:256** — SELECT * FROM event_log WHERE status = 'pending' ... selects event_log rows without a tenant_id constraint. This reads tenant-scoped data across tenants from normal heartbeat code.
  - _Fix:_ If the heartbeat should process events across tenants, move this into a clear, audited cross-tenant scheduler module; otherwise add a tenant filter (WHERE tenant_id = ${tenantId}) or use a storage API that makes cross-tenant operation explicit. Avoid ad-hoc unscoped SELECTs against tenant-scoped tables.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint. This can change approval_status for tasks belonging to other tenants.
  - _Fix:_ Use storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }) or include AND tenant_id = ${tenantId} in the WHERE clause. Ensure any direct SQL mutation of tenant-scoped tables requires an explicit tenant_guard or is restricted to an audited scheduler/administrative boundary.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint (second occurrence). This can change approval_status for tasks belonging to other tenants.
  - _Fix:_ Same as above: call the storage-layer updater that enforces tenant scoping, or add AND tenant_id = ${tenantId} to the WHERE clause. Centralize cross-tenant mutations in a clearly documented scheduler/admin boundary if cross-tenant semantics are intended.
- **server/job-queue.ts:160** — UPDATE on tenant-scoped table 'agent_jobs' (claimDueJobs) has no tenant_id constraint — it atomically claims/runs jobs across all tenants.
  - _Fix:_ Scope claims to a specific tenant (add AND tenant_id = ${tenantId} or accept a tenantId parameter) or document/guard this as a platform-level admin operation. Alternatively iterate per-tenant (or partition work by tenant) so every UPDATE includes tenant_id in the WHERE clause.
- **server/job-queue.ts:195** — UPDATE on tenant-scoped table 'agent_jobs' (reclaimExpiredLeases step 1) lacks tenant_id constraint — may mark jobs from other tenants failed_terminal.
  - _Fix:_ When reclaiming leases, restrict the UPDATE to a tenant (AND tenant_id = ${tenantId}) or perform reclamation per-tenant in a controlled loop; if cross-tenant behavior is intentional, limit to an admin-only path and audit it explicitly.
- **server/job-queue.ts:209** — UPDATE on tenant-scoped table 'agent_jobs' (reclaimExpiredLeases step 2 requeue) has no tenant_id filter — requeues pending jobs across tenants without scoping.
  - _Fix:_ Add tenant_id filtering (e.g. iterate tenants and run the UPDATE per-tenant) or require an admin boundary and explicit authorization before touching rows from multiple tenants.
- **server/job-queue.ts:238** — UPDATE on tenant-scoped table 'agent_jobs' (completeJob) does not include tenant_id in WHERE — a stale/rogue completion call could affect another tenant's job.
  - _Fix:_ Require the caller to pass tenantId (or fetch and verify job.tenant_id) and include AND tenant_id = ${tenantId} in the WHERE clause to enforce tenant-scoped lease-fencing at the DB boundary.
- **server/job-queue.ts:281** — SELECT from tenant-scoped table 'agent_jobs' (failJob lookup of max_attempts) lacks a tenant_id constraint — reads cross-tenant job metadata by id without scoping.
  - _Fix:_ Include tenant_id in the SELECT WHERE clause or fetch the job row with tenant verification (e.g. SELECT max_attempts FROM agent_jobs WHERE id = ${id} AND tenant_id = ${tenantId}) before making retry/terminal decisions; if this helper is admin-only, enforce and document that requirement.
- **server/job-queue.ts:298** — UPDATE on tenant-scoped table 'agent_jobs' (failJob -> failed_terminal) missing tenant_id in WHERE — may flip another tenant's job to terminal.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE, or otherwise ensure the operation is executed per-tenant (and that tenantId is derived/validated by the caller) before mutating job rows.
- **server/job-queue.ts:318** — UPDATE on tenant-scoped table 'agent_jobs' (failJob retry path) has no tenant_id constraint — re-schedules jobs across tenants without scoping.
  - _Fix:_ Scope the UPDATE to the tenant (AND tenant_id = ${tenantId}) or perform per-tenant requeueing; require the caller to supply/verify tenantId for lease-fencing.
- **server/job-queue.ts:334** — UPDATE on tenant-scoped table 'agent_jobs' (cancelJob) is missing tenant_id in WHERE — cancelling by id can affect jobs for other tenants.
  - _Fix:_ Require tenantId in cancel operations and add AND tenant_id = ${tenantId} to the WHERE clause; if an admin route needs cross-tenant cancel, place it behind strict admin authorization and auditing.
- **server/lib/agent-trace.ts:130** — UPDATE on tenant-scoped table agent_trace_spans uses WHERE span_id = ${spanId} with NO tenant_id constraint — allows cross-tenant mutation by span id.
  - _Fix:_ Include tenant scoping in the UPDATE predicate. Persist the tenantId when the span is opened (e.g. store tenantId in the _openPromises/_openMeta map keyed by spanId) and use it in the WHERE clause: UPDATE agent_trace_spans ... WHERE span_id = ${spanId} AND tenant_id = ${tenantId}. Alternatively change persistSpanClose to accept the tenantId (or recover it from a safe in-memory map) and always include eq(agent_trace_spans.tenant_id, tenantId) in the WHERE.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on tenant-scoped table procedure_evolution_runs uses WHERE id = ${runId} with NO tenant_id constraint (LLM error handler).
  - _Fix:_ Constrain updates to the calling tenant. Include the tenant id in the WHERE clause, e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Ensure runId and tenantId are validated and available in scope before updating.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on tenant-scoped table procedure_evolution_runs uses WHERE id = ${runId} with NO tenant_id constraint (noop_proposal path).
  - _Fix:_ Add tenant scoping to the UPDATE predicate. Use .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Capture/propagate tenantId into this scope if needed to enforce the guard.
- **server/lib/aevo-meta-editor.ts:466** — UPDATE on tenant-scoped table procedure_evolution_runs uses WHERE id = ${runId} with NO tenant_id constraint (validation_failed path).
  - _Fix:_ Restrict the UPDATE to the tenant that created the run. Change the WHERE to include tenantId: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Validate tenantId exists in this function before the update.
- **server/lib/aevo-meta-editor.ts:500** — UPDATE on tenant-scoped table procedure_evolution_runs uses WHERE id = ${runId} with NO tenant_id constraint (finalize run to done).
  - _Fix:_ Ensure tenant isolation by adding tenant filter to the WHERE clause. For example: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Propagate the tenantId from the original insert or caller context so the update cannot affect other tenants' rows.
- **server/lib/ideabrowser-backfill.ts:49** — Query aggregates projects across all tenants without constraining tenant_id. The SELECT at lines 49–53 (COUNT grouped by tenant_id) reads tenant-scoped projects for tenants other than the caller, violating the invariant that all reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Restrict the aggregation to the snapshot's tenant set or perform per-tenant counts. For example, compute the list of snapshot tenant IDs and add a WHERE tenant_id = ANY(${tenantIdsLiteral}::int[]) clause, or run the count per tenant inside the loop. Example: const tenantIdsLiteral = `{${Array.from(wantedByTenant.keys()).join(',')}}`; then SELECT tenant_id, count(*) ... FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] AND tenant_id = ANY(${tenantIdsLiteral}::int[]) GROUP BY tenant_id. This ensures you only read rows for tenants present in the snapshot instead of all tenants.
- **server/lib/inbox-ingest.ts:536** — SELECT joins inbox_classifications (aliased c) to inbox_messages (aliased m) but only constrains c.tenant_id = ${tenantId}. The query reads from inbox_messages without an explicit tenant_id filter, allowing a row from another tenant with the same id to be returned (no RLS / FK cross-tenant enforcement).
  - _Fix:_ Constrain the joined inbox_messages rows to the caller tenant. For example, add AND m.tenant_id = ${tenantId} to the WHERE clause or change the JOIN to include the tenant check: JOIN inbox_messages m ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId} — ensure every referenced tenant-scoped table is explicitly filtered by tenant_id.
- **server/lib/mention-injection.ts:120** — Query selects from channel_messages (cm) LEFT JOIN personas p ON p.id = cm.from_persona_id but only constrains cm.tenant_id = ${params.tenantId}. The JOIN reads from personas without constraining p.tenant_id, so persona rows from other tenants could be leaked.
  - _Fix:_ Restrict the joined personas rows to the same tenant. e.g. include AND p.tenant_id = ${params.tenantId} in the WHERE clause or put the tenant check into the JOIN: LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId}. Ensure every tenant-scoped table referenced by the query is explicitly tenant-filtered.
- **server/lib/outcome-promotion.ts:158** — SELECT from tenant-scoped table `deliverable_grade_events` without a tenant_id filter (aggregates across all tenants). This reads tenant-scoped rows across tenants and violates the invariant that every read/write against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ If this scan must operate per-tenant, run it in a loop for each tenant and add an explicit WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq) so each query is scoped. Example: SELECT tenant_id, format, count(*) ... FROM deliverable_grade_events WHERE tenant_id = ${tenantId} AND created_at > ... GROUP BY tenant_id, format. If the cross-tenant aggregation is intentional for an admin-only batch job, restrict invocation to a strongly-authenticated admin context and document/centralize the privilege check; otherwise avoid scanning tenant data across tenants.
- **server/lib/restraint-trust.ts:90** — Dynamic use of sql.raw(column) with a request-derived value (outcome → column) — potential SQL-injection / tenant-isolation risk.
  - _Fix:_ Avoid injecting runtime/request-derived identifiers into SQL via sql.raw. Instead implement an explicit mapping from outcome to a fixed, prebuilt parameterized query (e.g. switch on outcome and run one of four fully parameterized UPDATE statements), or use the Drizzle update API with static column references. If sql.raw is absolutely required, construct the statement server-side from trusted constants only and keep a strict allowlist check (and tests) — but prefer separate prepared statements per outcome to eliminate any risk.
- **server/lib/stuck-task-sweep.ts:56** — SELECT against agent_runs (SELECT id, tenant_id, status, ... FROM agent_runs WHERE status='running' AND updated_at < ...) does not include tenant_id in the WHERE clause. agent_runs is tenant-scoped but this query scans across tenants.
  - _Fix:_ Add an explicit tenant_id predicate (WHERE tenant_id = <tenant>) or otherwise run this audit scoped to an admin-only, audited context that explicitly documents cross-tenant access. If the function must remain multi-tenant (admin audit), make the intent explicit and ensure access is tightly controlled and logged; otherwise perform per-tenant scans.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against mind_tickets (SELECT id, tenant_id, status, ... FROM mind_tickets WHERE status='in_progress' AND updated_at < ...) does not include tenant_id in the WHERE clause. This reads tenant-scoped rows across tenants.
  - _Fix:_ Scope queries by tenant_id (WHERE tenant_id = <tenant>) or convert to an explicit admin-audit path with documented, restricted permissions and logging. Avoid cross-tenant reads of tenant-scoped tables without explicit, secure reasoning and controls.
- **server/martech-bundle.ts:325** — UPDATE against tenant-scoped table (tenantVoiceProfiles) uses only id in the WHERE clause and omits tenant_id. This allows a misplaced/malicious id to update another tenant's profile.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. add tenant predicate: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively, use an UPDATE that matches on (id, tenant_id) or fetch-for-update in a transaction and then update by id+tenant_id.
- **server/memory-graph.ts:131** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} has no tenant_id predicate. memory_categories is tenant-scoped — this UPDATE can touch another tenant's category row.
  - _Fix:_ Include the tenant predicate in the WHERE clause. For example: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Ensure tenantId is the authoritative caller tenant (validate before use).
- **server/memory-intelligence.ts:399** — UPDATE to tenant-scoped table `knowledge_triples` performs WHERE id = ${c.id} with NO tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the caller tenant to avoid cross-tenant mutation. For example: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} AND tenant_id = ${tenantId}. Consider also adding AND valid_until IS NULL if appropriate to avoid flipping already-ended rows.
- **server/minds-engine.ts:501** — SELECT from tenant-scoped table `messages` omits an explicit tenant_id filter on the messages table. The JOIN constrains conversations by tenant but the messages table row is not explicitly restricted (no WHERE m.tenant_id = ${tenantId}).
  - _Fix:_ Add an explicit tenant constraint on the messages table (or an equivalent Drizzle eq on messages.tenant_id) to ensure each read is tenant-scoped. For example: JOIN conversations c ON c.id = m.conversation_id AND c.tenant_id = ${tenantId} WHERE m.conversation_id = ${run.conversationId} AND m.tenant_id = ${tenantId} — or simply add AND m.tenant_id = ${tenantId} to the WHERE clause.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checks ownership via assertProjectInTenant but the INSERT omits tenant_id, violating the invariant that every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Fix:_ Include the tenant_id column in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Also validate options.tenantId is a positive integer before the INSERT. Prefer using Drizzle's typed insert helpers to ensure tenant_id is always provided.
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table `project_files` does not include tenant_id. Even though assertProjectInTenant is used, the INSERT still omits tenant_id, violating the requirement that tenant_id be explicitly set on INSERTs.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure options.tenantId is validated as a positive integer and consider using Drizzle insert APIs so tenant_id cannot be accidentally omitted.
- **server/oauth-subscriptions.ts:915** — Query selects tenant_id/is_active from oauth_subscriptions for provider='google-workspace' with no tenant filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This enumerates tenant rows and can leak tenant state across tenants.
  - _Fix:_ Add tenant scoping to the query, or explicitly document and restrict this admin/global operation. If intent is to operate per-tenant, include AND tenant_id = ${tenantId} (or use a safe admin-only path with additional safeguards).
- **server/oauth-subscriptions.ts:956** — UPDATE on oauth_subscriptions updates rows by id without verifying tenant ownership (UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id}). Because the earlier scan pulled subs across tenants, this can deactivate subscriptions for other tenants.
  - _Fix:_ Always include tenant_id in mutation WHERE clauses when updating tenant-scoped tables. For example: WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} (or perform the UPDATE using provider + tenant_id). Ensure the code only updates rows belonging to the expected tenant context before mutating.
- **server/oauth-subscriptions.ts:966** — UPDATE on oauth_subscriptions updates rows by id without tenant constraint (UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id}). This follows an expired-subscriptions sweep that was run without tenant scoping and may mutate other tenants' rows.
  - _Fix:_ Include AND tenant_id = ${sub.tenant_id} (or otherwise ensure the statement is constrained to the tenant) before performing the UPDATE; avoid running sweeping scans that mix tenants and then issuing ID-only updates.
- **server/oauth-subscriptions.ts:981** — UPDATE on oauth_subscriptions updates rows by id without tenant constraint (UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id}). This is executed after an un-scoped read and may deactivate subscriptions across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or refactor to perform per-tenant operations only (select+update within the same tenant context).
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh queries oauth_subscriptions across all tenants (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) — unsafely enumerates tenant rows and may lead to cross-tenant actions.
  - _Fix:_ Refactor proactiveTokenRefresh to operate per-tenant (pass a tenantId or iterate a safe list of tenant IDs obtained via an explicit, audited admin API). If a global scan is required, restrict returned columns to non-sensitive fields and run under explicit admin privileges with auditing; otherwise add WHERE tenant_id = ${tenantId}.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions queries oauth_subscriptions without tenant scoping (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE). This enumerates active subscriptions across tenants and drives per-tenant refresh logic without explicit tenant boundaries.
  - _Fix:_ Do not perform unscoped reads on tenant-scoped tables. Either require an explicit tenantId for the operation or add a safe tenant filter. If this is intended to be an admin/global operation, add explicit access controls and avoid exposing or acting on other tenants' sensitive rows.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} is executed without enforcing tenant_id = ${tenantId}. This can return a persona belonging to another tenant and leak persona details across tenants.
  - _Fix:_ Scope persona reads by tenant. Change the query to SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} (or otherwise validate that the returned persona belongs to the expected tenant before returning or including it in exported data).
- **server/process-governor.ts:399** — SELECT joins personas without scoping the joined personas row to the tenant. The query selects persona name via LEFT JOIN personas p ON p.id = ad.persona_id but does not constrain p.tenant_id = ${tenantId}, so persona data from other tenants may be leaked.
  - _Fix:_ Add tenant scoping to the join (e.g. LEFT JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) or otherwise ensure the joined personas row is filtered by the same tenantId used to select agent_desks.
- **server/project-brain.ts:135** — SELECT projects WHERE id = ${projectId} is missing tenant scoping. updateProjectBrain reads project metadata but does not verify the project's tenant, allowing reads of projects belonging to other tenants.
  - _Fix:_ Require and accept tenantId in updateProjectBrain and scope the query (e.g. SELECT ... FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}). Always verify ownership before reading tenant-scoped rows.
- **server/project-brain.ts:332** — Backfill query selects DISTINCT projects JOIN conversations and filters only by message counts but does not restrict projects/conversations/messages to a tenant. This reads tenant-scoped tables without tenant filters and can enumerate data from all tenants.
  - _Fix:_ Restrict the backfill to a tenant context (accept tenantId or otherwise filter by p.tenant_id = <tenant>) and add tenant_id constraints to conversations and messages subqueries (e.g. WHERE c.tenant_id = <tenant> AND messages.tenant_id = <tenant>). If backfill must run globally, ensure each access enforces per-project tenant ownership and treats tenant separation explicitly.
- **server/project-brain.ts:349** — SELECT conversations WHERE c.project_id = ${proj.id} lacks tenant filtering. Conversations are tenant-scoped but are queried without verifying they belong to the expected tenant, allowing cross-tenant reads.
  - _Fix:_ Include tenant scoping on the query (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${proj.tenant_id} or filter by a provided tenantId). Ensure all SELECTs over tenant-scoped tables include tenant_id checks.
- **server/project-brain.ts:359** — SELECT messages WHERE conversation_id = ${conv.id} has no tenant filter. Messages are tenant-scoped; reading them by conversation id without verifying the conversation/project tenant can leak another tenant's messages.
  - _Fix:_ Add tenant constraints to the messages query (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}), or derive tenant from the project/conversation row and verify ownership before reading messages. Always validate ownership of referenced foreign IDs before reading.
- **server/project-brain.ts:125** — updateProjectBrain(projectId, conversationId, ...) accepts foreign ids but does not take or validate a tenantId; it queries projects by id without verifying tenant ownership. This allows callers to cause reads/writes for projects belonging to other tenants.
  - _Fix:_ Change updateProjectBrain to accept a tenantId and validate that the given projectId and conversationId belong to that tenant before proceeding (e.g. SELECT ... WHERE id = ${projectId} AND tenant_id = ${tenantId}). Never trust a foreign id from input without verifying tenancy/ownership.
- **server/recurring-messages.ts:258** — Unscoped SELECT from tenant-scoped table `agent_knowledge`: the runner queries all rows (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500) with no tenant_id filter.
  - _Fix:_ If this is intentionally a global system scan, document and gate it clearly and ensure every subsequent write/operation enforces per-row tenant checks. Prefer adding an explicit tenant_id filter when possible, or otherwise constrain which tenants are scanned (e.g. pass an explicit tenant context or query by tenant_id). If the runner must be global, make sure all downstream mutations use tenant_id in their WHERE clauses and treat the returned tenant_id as authoritative only after verification.
- **server/research-engine.ts:456** — INSERT into project_notes (project_id, note, author, created_at) does not include tenant_id. project_notes appears to be tenant-scoped, and tenant_id is not being set on the INSERT (columns are NOT NULL with no default per project invariant).
  - _Fix:_ Include tenant_id in the INSERT columns and VALUES (e.g. tenant_id = ${session.tenantId} or the tenant id resolved from the project) so the row is explicitly attributed to the correct tenant. Alternatively, perform an INSERT ... SELECT that joins projects to ensure correct tenant and fails closed if ownership does not match.
- **server/research-engine.ts:464** — INSERT into project_notes within the findings loop inserts notes (project_id, note, author, created_at) but omits tenant_id. Each INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Add tenant_id to the INSERT column list and pass the correct tenant (e.g. ${session.tenantId}). If you rely on assertProjectInTenant for ownership, still write tenant_id explicitly to every tenant-scoped row to enforce isolation at the application layer.
- **server/routes.ts:971** — file-serve ownership check queries `file_storage` by filename without a tenant_id constraint (db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This un-scoped SELECT touches rows from all tenants to determine ownership.
  - _Fix:_ Avoid selecting all owners across tenants. First query only for rows where filename = baseName AND tenant_id = authedTenantId. If none are found, treat as 'no owner' (and require a signed URL). If you need to distinguish 'exists but belongs to another tenant' without leaking existence, implement a safe server-side check (e.g. require signed capability for non-owner access or do an admin-only audit job). In short, ensure the application never performs an unscoped select that reads other tenants' file rows.
- **server/routes.ts:469** — Startup migration runs UPDATE order_lookup_codes SET tenant_id = ${ownerTenantId()} WHERE tenant_id IS NULL — an UPDATE on a tenant-scoped table without constraining tenant_id beyond the NULL condition (affects all rows with NULL tenant_id).
  - _Fix:_ Run this backfill as an explicit migration step (out-of-band) or otherwise ensure it is executed under an administrative migration context and reviewed. If possible, constrain the UPDATE to a well-understood subset or log/verify the rows being updated before applying changes to avoid unintended cross-tenant attribution.
- **server/routes.ts:619** — Backfill query selects projects (SELECT p.id, p.name, p.tenant_id FROM projects JOIN tenants ...) without restricting tenant_id; this iterates all projects across tenants and performs per-project actions that may act across tenant boundaries.
  - _Fix:_ Perform backfill work per-tenant (iterate tenants and backfill only that tenant's projects) or include an explicit tenant filter for the intended tenant set. Any cross-tenant maintenance that touches tenant-scoped rows should be done as a controlled migration with explicit admin intent.
- **server/routes.ts:1917** — Public presenter endpoint selects from tenant-scoped presenter_sessions by token (SELECT ... FROM presenter_sessions WHERE token = ${token}) with no tenant_id constraint. Similar unscoped SELECTs appear in slide and repair flows (lines ~1945 and ~2052). These queries can read rows across tenants based solely on a token parameter.
  - _Fix:_ Scope queries by tenant where possible. If the presenter token is a cross-tenant unguessable capability and is intentionally used to locate sessions across tenants, document and isolate this as a deliberate capability (and ensure tokens are cryptographically strong). Prefer storing/validating a tenant-bound token or require an explicit tenantId verification for these endpoints, or perform a tenant-scoped lookup first (WHERE token = ${token} AND tenant_id = <expectedTenant>) and fall back to a signed capability path if needed. Avoid blanket unscoped selects on tenant-scoped tables using request-derived parameters.
- **server/routes.ts:4543** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The raw SQL INSERT: INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id so the row will not be explicitly tenant-scoped (DB columns are NOT NULL with no default per invariant).
  - _Fix:_ Include the tenant_id explicitly in the INSERT (and use the validated tenantId value). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, use a storage function that enforces tenant scoping and ownership, and ensure project ownership is validated before inserting.

### MEDIUM (27)

- **server/auth.ts:1238** — clearExpiredSessions periodically runs DELETE FROM auth_sessions WHERE expires_at < ${now} without a tenant_id constraint, deleting expired sessions across tenants.
  - _Fix:_ Either iterate and delete per-tenant, or otherwise ensure the maintenance job is explicitly platform-scoped and auditable. Prefer adding tenant scoping where feasible (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ...).
- **server/auth.ts:1132** — cleanExpiredResetTokens runs DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} with NO tenant_id restriction, deleting expired reset tokens for all tenants.
  - _Fix:_ Scope reset-token garbage collection per-tenant or run it from a platform-admin maintenance job that is explicitly guarded/audited. Alternatively add tenant_id filtering when invoked from tenant-scoped contexts.
- **server/auto-asset-capture.ts:207** — Per-conversation messages are read by conversation_id without tenant scoping: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'. Without an explicit tenant constraint or ownership assertion in this code path, this read relies on implicit FK invariants that are not enforced at the DB level.
  - _Fix:_ Include tenant scoping (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}) or join messages to conversations on id AND tenant_id to ensure the messages belong to the expected tenant. If processing is done across tenants, ensure an ownership/assertion check (assertConversationInTenant) is performed before reading/writing per-conversation data.
- **server/chat-engine/project-context.ts:45** — EXISTS(...) subquery reads project_conversations without a tenant_id constraint. The outer query constrains projects by p.tenant_id, but the EXISTS check on project_conversations does not explicitly ensure the project_conversations row belongs to the caller's tenant, which can lead to ambiguous ownership if project_conversations is tenant-scoped.
  - _Fix:_ Constrain the subquery to the tenant as well (e.g. WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId}) or otherwise ensure project_conversations rows are validated against tenantId in the same statement.
- **server/data-protection.ts:359** — SELECT from project_notes uses project_id only and lacks an explicit tenant_id predicate. Even though projects were fetched with tenant_id, every access to tenant-scoped tables must include the tenant predicate to meet the invariant.
  - _Fix:_ Add tenant scoping to the query, e.g. SELECT ... FROM project_notes WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} ORDER BY ...; or join project_notes to projects and assert p.tenant_id = ${tenantId} in the WHERE clause. Prefer explicit tenant_id predicates on tenant-scoped tables.
- **server/data-protection.ts:371** — SELECT from project_files uses project_id only and does not include tenant_id. This can read another tenant's project_files if a project_id were mismatched.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}), or join to the projects table and verify p.tenant_id = ${tenantId} before reading project_files.
- **server/data-protection.ts:403** — COUNT(*) FROM messages filters only by conversation_id and omits tenant_id. A malicious or unexpected conversation_id could allow reading message counts across tenants (existence oracle) or misattribute messages.
  - _Fix:_ Include tenant scoping on the messages query: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}. Always add tenant predicates on tenant-scoped tables even when the parent ID was previously validated.
- **server/doc-collections.ts:138** — SELECT subquery reads doc_chunks (tenant-scoped) using collection_id = c.id but with NO tenant_id constraint (listCollections). Inner COUNTs / aggregations over doc_chunks are not explicitly tenant-scoped.
  - _Fix:_ Include tenant scoping in the subselects (e.g. WHERE collection_id = c.id AND tenant_id = c.tenant_id) or otherwise constrain doc_chunks by the caller's tenant. Avoid relying solely on collection_id for isolation; add explicit tenant_id filters to all doc_chunks subqueries.
- **server/doc-collections.ts:458** — SELECT in getCollectionStatus uses subqueries that read doc_chunks (tenant-scoped) by collection_id = c.id with NO tenant_id constraint (chunk_count, doc_count, embedded_count, total_tokens).
  - _Fix:_ Add explicit tenant filters to each subquery (e.g. AND tenant_id = c.tenant_id) or otherwise ensure doc_chunks is constrained to the caller's tenant when computing collection-level aggregates.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages ... WHERE created_at > ... AND role = 'user' — query lacks tenant_id. messages is a tenant-scoped table; this global count reads across tenants.
  - _Fix:_ If this metric must be tenant-specific, add tenant_id = ${tenantId} or accept a tenant argument and scope the query. If the intent is to operate globally, document that these are global/system queries and ensure they are not exposing per-tenant data to tenant-scoped handlers.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance ... WHERE last_success_at > ... — no tenant filter. tool_performance appears tenant-scoped but is being queried globally.
  - _Fix:_ Scope this query to a tenant (WHERE tenant_id = ${tenantId} AND last_success_at > ...) if the operation should be per-tenant. If the calculation intentionally aggregates globally, make that explicit and ensure it cannot be invoked in a tenant-context that would leak cross-tenant state.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance — no tenant filter (getEverInvokedTools). Reads tenant-scoped table across all tenants.
  - _Fix:_ Add a tenant_id filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise restrict to the appropriate tenant context before enumerating tool names. Avoid global scans of tenant-scoped tables unless explicitly required and safe.
- **server/dormant-deprecation.ts:215** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... — no tenant filter (getActiveTools14d). This returns tenant-scoped rows globally.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause or accept a tenant parameter to scope the query. If global analysis is desired, ensure it is run in an administrative/system context, not on behalf of a tenant caller.
- **server/heartbeat-schedules.ts:26** — SELECT DISTINCT tenant_id FROM messages is a SELECT against a tenant-scoped table (`messages`) with no tenant_id filter. This enumerates tenant IDs by scanning tenant-scoped rows without an explicit per-tenant constraint.
  - _Fix:_ If the intent is to enumerate tenants for a cross-tenant sweep, use an authoritative tenants table (SELECT id FROM tenants WHERE ...), or make the cross-tenant intent explicit and gated by an admin-only path. If the query was meant to operate within a single tenant context, add WHERE tenant_id = ${tenantId} and invoke per-tenant. Document and protect any deliberate cross-tenant enumeration.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks ... (getDueHeartbeatTasksAcrossTenants) queries tenant-scoped heartbeat_tasks with no tenant_id filter. Reads across tenants from application code without explicit cross-tenant guard.
  - _Fix:_ If this function is intentionally cross-tenant (scheduler boundary), clearly document and centralize that behavior. Otherwise restrict the query with a tenant predicate (WHERE tenant_id = ${tenantId}) or expose a storage API that requires an explicit ALL_TENANTS/scheduler authorization flag. Prefer using a dedicated, auditable scheduler module rather than ad-hoc unscoped SELECTs.
- **server/job-queue.ts:375** — SELECT tenant_id FROM agent_jobs WHERE id = ${id} (getJobTenantForAdmin) reads a tenant-scoped table by id with no tenant filter — an unscoped read is used to resolve tenant for admin workflows.
  - _Fix:_ If this is an admin-only helper, ensure it is clearly restricted to admin paths and audited. Otherwise, consider requiring caller-provided tenantId or a separate admin API that verifies authorization before returning tenant mappings.
- **server/job-queue.ts:406** — SELECT from tenant-scoped table 'agent_jobs' (listJobs) may run without tenant_id filtering when filter.tenantId is omitted — returns rows across tenants.
  - _Fix:_ Require callers to pass an explicit tenantId for tenant-scoped listing in tenant-facing APIs. If cross-tenant listing is needed for operators, ensure the function is only callable by admin roles and that such usage is documented and audited.
- **server/knowledge-diversity-monitor.ts:266** — SELECT grouping tenant_id/persona_id from tenant-scoped table 'agent_knowledge' (runKnowledgeDiversityMonitor) uses no tenant_id WHERE clause — it scans all tenants to enumerate pairs.
  - _Fix:_ If the scan is intended, make the admin intent explicit and protect the caller (run only in admin context). Alternatively, iterate tenants and run per-tenant queries that include WHERE tenant_id = ${tenantId} so subsequent per-tenant operations (snapshots/alerts) always use tenant-scoped UPDATE/INSERTs.
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey performs SELECT id, tenant_id, key_hash, scopes, revoked_at FROM mcp_api_keys WHERE key_prefix = ${prefix} LIMIT 1 with no tenant_id constraint. This reads a tenant-scoped table without a tenant filter (even though the code subsequently verifies the full key hash), which violates the rule that tenant-scoped tables must be constrained by tenant at query time.
  - _Fix:_ Either (A) move the prefix→tenant mapping into an explicitly global lookup table (e.g. mcp_api_key_prefixes) and query that table for prefix→tenant, or (B) if resolving tenant from the key is required here, document and centralize this as an explicit, audited exception and ensure the SELECT returns only the minimal columns needed and is carefully parameterized. Alternatively, if the caller already has a tenantId, add WHERE tenant_id = ${tenantId}. The goal is to avoid ad-hoc reads of tenant-scoped tables without an explicit tenant filter.
- **server/lib/scheduled-post-runner.ts:320** — Manual construction of a Postgres text[] literal (arrLit) from request-derived platforms and embedding it into the SQL. Building SQL array/text literals by string-concatenation of user input is error-prone and risks injection/type issues.
  - _Fix:_ Avoid hand-serializing SQL literals. Pass arrays as proper query parameters or use Drizzle/driver helpers to bind an array (or use parameterized JSON/array bind): e.g. pass the platforms as a JS array parameter or use schema/Drizzle utilities to insert an array column rather than building a raw '{...}' string.
- **server/lib/system-probes.ts:163** — A plain db.select(...).from(messagesTable) is used to count messages with no tenant_id predicate (total messages across tenants). This performs a tenant-scoped table read without scoping to a tenant.
  - _Fix:_ If the intent is per-tenant metrics, add WHERE tenant_id = ${tenantId} or use an admin-only, audited path that explicitly documents and restricts cross-tenant aggregation. Prefer using scoped storage helpers or explicitly passing an ALL_TENANTS constant only from a hardened admin context.
- **server/orchestrator-ledger.ts:450** — Using sql.raw(lockKey) inside the query: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). sql.raw() is used with a value (lockKey) that can be derived from caller-supplied ledgerKey, risking SQL injection or unexpected SQL syntax if ledgerKey isn't strictly sanitized.
  - _Fix:_ Avoid sql.raw with request-derived input. Bind the lock value as a parameter instead of injecting raw SQL. For example compute the numeric lock as a BigInt in JS and use: tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockVal)})`) or otherwise ensure lockKey is sanitized and passed as a parameterized value rather than sql.raw(). Document and validate ledgerKey input before deriving the lock value.
- **server/persona-export.ts:39** — Query for skills omits tenant scoping (SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ...). If skills rows are tenant-scoped, this can leak or mix persona-specific skills across tenants.
  - _Fix:_ If skills is a tenant-scoped table, add tenant filtering for persona-specific rows. For example: WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR persona_id IS NULL. Ensure any OR branches that pull global rows are intentionally designed and do not expose other tenants' data.
- **server/routes.ts:478** — Periodic prune uses DELETE FROM order_lookup_codes WHERE expires_at < NOW() without a tenant_id constraint, deleting rows for all tenants in a tenant-scoped table.
  - _Fix:_ Scope the prune to each tenant (e.g. run per-tenant cleanup) or include tenant scoping if the job is intended to operate per-tenant. If a global prune is desired, run it from a controlled migration/admin script with explicit acknowledgement, but do not run unscoped deletes from regular request-handling code.
- **server/routes.ts:498** — cleanupTestTenants runs a SELECT that contains subqueries reading tenant-scoped tables (SELECT DISTINCT tenant_id FROM conversations / projects) without scoping by tenant — these unscoped reads touch tenant-scoped data across all tenants.
  - _Fix:_ Rewrite the check to avoid unscoped reads. For example, check existence of conversations/projects per candidate tenant via EXISTS with the candidate tenant id, or perform the cleanup per-tenant in an explicit migration/maintenance job rather than issuing unscoped subqueries during request startup.
- **server/routes.ts:1386** — Admin stats endpoint selects counts from tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) with no tenant_id constraints (global aggregation).
  - _Fix:_ If the intent is platform-wide metrics only accessible to platform admins, make the admin-only boundary explicit and document the exception; otherwise, scope queries by tenant_id. Prefer aggregating via a dedicated admin-only analytics path that is reviewed and audited rather than ad-hoc unscoped selects in route handlers.
- **server/routes.ts:1416** — Architecture endpoint runs many db.execute SQL statements like SELECT count(*) FROM conversations / messages / projects / skills etc. against tenant-scoped tables without tenant_id filtering.
  - _Fix:_ As with other admin aggregates, either add tenant scoping where appropriate or centralize global aggregations into a dedicated admin-only analytics process. Ensure any unscoped access to tenant tables is intentional, approved, and isolated from regular request flows.
