# Tenant-Isolation Audit Report

_Generated 2026-06-29T22:18:37.983Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **411/411** file(s) audited · chunks 51/51 OK (0 failed)
- Findings: **294** (CRITICAL 75 · HIGH 165 · MEDIUM 53 · LOW 1)

### CRITICAL (75)

- **server/ab-optimizer.ts:110** — Query reads from experiments across all tenants: `SELECT id, tenant_id, hypothesis, metric, persona_id, metadata, created_at FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100` has no tenant_id constraint.
  - _Fix:_ Scope this query to a specific tenant (or iterate per-tenant). Add a tenant filter (e.g. `WHERE tenant_id = ${tenantId} AND ...`) or otherwise ensure you only load rows belonging to the caller's tenant before processing to prevent cross-tenant data leakage.
- **server/business-tools.ts:84** — INSERT into invoice_items omits tenant_id. Every INSERT into tenant-scoped tables must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the invoice_items INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)) or otherwise derive/validate the tenant and pass it as a parameter. If invoice_items is intentionally global, document and confirm; otherwise add the tenant column to the VALUES list and adjust schema/tests.
- **server/chat-engine.ts:4435** — INSERT into tenant-scoped table via storage.createMemoryEntry does not include tenant identifier. The call creates memory entries without passing tenantId, violating the invariant that every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Pass the tenant context into the insert (e.g. include tenantId in the object passed to storage.createMemoryEntry) or ensure storage.createMemoryEntry requires and enforces tenantId. Example fix: await storage.createMemoryEntry({ ..., personaId: personaId ?? null, tenantId }); and/or validate ownership server-side before inserting.
- **server/chat-engine.ts:4443** — UPDATE against a tenant-scoped row via storage.updateMemoryEmbedding(entry.id, emb) is performed without constraining to tenant_id. The update is invoked with only an entry id (no tenant context) so it may update rows across tenants if the storage layer doesn't enforce tenant scoping.
  - _Fix:_ Ensure updateMemoryEmbedding either accepts and uses the tenantId (e.g. storage.updateMemoryEmbedding(entry.id, emb, tenantId)) or that the storage layer verifies the entry belongs to the caller's tenant before updating. Always include WHERE tenant_id = <caller tenant> (or equivalent Drizzle .where(eq(...))) on UPDATEs to tenant-scoped tables.
- **server/commitments.ts:135** — Cross-tenant SELECT on tenant-scoped table `commitments` without tenant_id filter. The scheduler `scanAndEscalate()` runs a SELECT that returns commitments across all tenants (`WHERE status = 'active' ...`), which violates the invariant that reads on tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ If cross-tenant behavior is required, restrict this code path to a clearly authenticated/authorized platform-admin context and document that explicitly; otherwise add a tenant_id filter (e.g. `AND tenant_id = ${tenantId}`) or separate admin-only query executed only by a privileged process. If the intent is to send a global owner-digest, ensure the query and downstream side-effects are reviewed for acceptable data exposure and that returned columns are redacted or limited to non-sensitive fields. Add explicit runtime checks to prevent accidental invocation by regular tenant sessions.
- **server/event-bus.ts:190** — UPDATE against tenant-scoped table `event_log` omits tenant_id in WHERE clause (UPDATE event_log ... WHERE id = ${eventId}). This allows a caller to update rows belonging to other tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId}. Prefer always binding tenantId to statements that modify tenant-scoped tables.
- **server/event-bus.ts:227** — UPDATE against tenant-scoped table `event_log` omits tenant_id in WHERE clause (UPDATE event_log SET ... WHERE id = ${eventId}). This can modify another tenant's event row.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${eventId} AND tenant_id = ${tenantId}. Ensure all writes to tenant-scoped tables include the tenant_id filter.
- **server/felix-brain.ts:22** — SELECT from tenant-scoped table `agent_runs` without tenant filter (SELECT ... FROM agent_runs WHERE status IN (...) AND created_at > ...). The code then rehydrates shared in-memory taskStateStore using rows from all tenants, risking cross-tenant data leakage.
  - _Fix:_ Restrict the SELECT to the appropriate tenant(s) (e.g. WHERE tenant_id = ${tenantId} AND ...). If boot-time rehydration must touch multiple tenants, explicitly iterate tenants with explicit authorization and namespace in-memory state by tenant (e.g. include tenantId in the taskStateStore key) so rows from different tenants are never mixed.
- **server/felix-loop.ts:684** — UPDATE against tenant-scoped table `felix_loop_runs` omits tenant_id in WHERE clause (UPDATE felix_loop_runs ... WHERE id = ${runId}). This can update run rows belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${runId} AND tenant_id = ${tenantId}. Always pair primary-key updates with tenant_id checks to prevent cross-tenant modification.
- **server/felix-loop.ts:690** — UPDATE against tenant-scoped table `felix_loop_runs` in error handling omits tenant_id in WHERE clause (UPDATE felix_loop_runs SET ... WHERE id = ${runId}). This could mark another tenant's run with this error.
  - _Fix:_ Constrain the UPDATE by tenant: WHERE id = ${runId} AND tenant_id = ${tenantId}. Ensure all error/recovery writes include tenant_id to maintain tenant isolation.
- **server/google-drive.ts:946** — INSERT INTO tenant-scoped table 'project_files' is executed without setting tenant_id (INSERT omits tenant column). The INSERT uses caller-derived projectId and file info and writes authoritative metadata without explicitly recording tenant ownership — this can create cross-tenant data or orphan rows.
  - _Fix:_ Always set tenant_id explicitly in the INSERT (e.g. include tenantId in the VALUES). If tenantId is derived from the project, validate ownership first and then insert tenantId = verifiedTenantId. Use parameterized Drizzle queries rather than raw SQL if possible.
- **server/minerva-planner.ts:355** — decidePlan performs an UPDATE on plans without scoping by tenant_id: UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING id, tenant_id, objective, plan_json. This permits modifying plans across tenants by id (write without tenant constraint).
  - _Fix:_ Require and use an explicit tenantId (or derive caller tenant) in decidePlan and add AND tenant_id = ${tenantId} to the UPDATE. At minimum, enforce that the caller is authorized for the plan's tenant before performing the UPDATE to prevent cross-tenant modifications.
- **server/minerva-planner.ts:459** — Rollback UPDATE after failed revise resets decision metadata with no tenant scoping: UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'. This is a write against a tenant-scoped table lacking tenant_id filter.
  - _Fix:_ Constrain the UPDATE to the plan's tenant, e.g. add AND tenant_id = ${tenantId}, and require tenantId to be provided/validated in the revise flow. Ensure the rollback only affects the intended tenant's row.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT uses VALUES (${options.projectId}, ${safeTitle + ".mp4"}, ${outPath}, ${driveUrl || null}, ${"video"}, ${stats.size}, ${"mpeg-engine"}) without setting tenant_id explicitly.
  - _Fix:_ Always include tenant_id in every INSERT into tenant-scoped tables. After verifying ownership (assertProjectInTenant), pass the tenant id into the INSERT, e.g. VALUES (${options.projectId}, ${options.tenantId}, ${safeTitle + ".mp4"}, ${outPath}, ${driveUrl || null}, ${"video"}, ${stats.size}, ${"mpeg-engine"}). If you derive tenant from the project row, SELECT the project.tenant_id (validated) and use that exact value in the INSERT. Keep queries parameterized (sql`...`) and do not rely solely on foreign keys or implicit inference for tenant isolation.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id. The INSERT uses VALUES (${options.projectId}, ${safeTitle + ".mp4"}, ${concatResult.filePath}, ${driveUrl || null}, ${"video"}, ${concatResult.sizeBytes || 0}, ${"mpeg-engine-parallel"}) without tenant_id.
  - _Fix:_ As above, include tenant_id explicitly in the INSERT. After assertProjectInTenant succeeds, use the validated tenant id in the VALUES list (e.g. VALUES (${options.projectId}, ${options.tenantId}, ...)). Alternatively SELECT the project's tenant_id once (validated) and insert that value. This ensures application-level tenant isolation invariants are preserved and avoids cross-tenant data leakage.
- **server/oauth-subscriptions.ts:884** — SELECT from tenant-scoped table `oauth_subscriptions` without any tenant_id filter (reads all tenants' subscriptions).
  - _Fix:_ Do not scan tenant-scoped tables without an explicit tenant constraint. Add a WHERE tenant_id = <tenant> (or equivalent) or limit the operation to an explicitly-authorized admin flow that enforces proper access checks. Avoid returning or operating on rows belonging to other tenants (especially for tables holding secrets/tokens).
- **server/oauth-subscriptions.ts:893** — SELECT tenant_id,is_active FROM `oauth_subscriptions` WHERE provider = 'google-workspace' without tenant scoping — enumerates rows across all tenants.
  - _Fix:_ If intent is per-tenant, add WHERE tenant_id = ${tenantId}. If the code must enumerate tenants for a global startup task, make the admin intent explicit and ensure the operation runs under a tightly-controlled maintenance/admin context and that any downstream writes include tenant_id in their WHERE clauses to avoid cross-tenant side-effects.
- **server/oauth-subscriptions.ts:934** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} executed without constraining by tenant_id. The id came from a global scan, so this can affect rows from any tenant.
  - _Fix:_ Always include tenant scoping in mutating statements, e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} (or perform per-tenant iteration so each UPDATE is constrained to the current tenant). Never rely on id-only updates when rows originate from cross-tenant queries.
- **server/oauth-subscriptions.ts:944** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} executed without tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or refactor to process one tenant at a time so all writes are explicitly scoped to that tenant.
- **server/oauth-subscriptions.ts:960** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} executed without tenant scoping (inside startup validation loop).
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}), or refactor to ensure the code only operates on rows belonging to a known tenant context before mutating them.
- **server/oauth-subscriptions.ts:1236** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM `oauth_subscriptions` WHERE is_active = TRUE — global read across tenants (returns sensitive rows for all tenants).
  - _Fix:_ If this is meant to be per-tenant, add WHERE tenant_id = ${tenantId}. If an admin/global sweep is required, make the admin intent explicit and ensure follow-on operations always include tenant scoping and appropriate authorization; consider paging per-tenant to avoid cross-tenant accidental mutations or exposures.
- **server/oauth-subscriptions.ts:1306** — SELECT provider, tenant_id FROM `oauth_subscriptions` WHERE is_active = TRUE — enumerates active subscriptions across all tenants without tenant scoping.
  - _Fix:_ Restrict reads to a specific tenant (WHERE tenant_id = ${tenantId}) when called from tenant-scoped callers. If running as a global maintenance job, run under an explicit admin context and ensure any mutations are tenant-aware and authorized.
- **server/plan-executor.ts:505** — Claiming an approved plan: UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ... — the UPDATE does not constrain by tenant_id. This permits claiming/updating plans across tenants (cross-tenant plan takeover).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${callerTenantId}) or require the caller to pass/verify the plan's tenant and use it in the UPDATE. Alternatively, SELECT then validate tenant before performing the UPDATE in the same transactional/CAS operation.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET ... WHERE id = ${t.id} executed without constraining by tenant_id. This can mutate another tenant's task by id.
  - _Fix:_ Include tenant filter in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or use the task's tenantId validated earlier; always verify ownership before mutating by id.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} executed without a tenant_id constraint. This can update desks belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or include and validate the tenantId when reading/updating rows.
- **server/process-governor.ts:977** — UPDATE governance_rules SET ... WHERE id = ${rule.id} executed without tenant_id constraint. This updates rule rows by id across tenants.
  - _Fix:_ Scope the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or otherwise verify rule.ruleId belongs to the caller's tenant before updating.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} executed without tenant_id constraint (clearing rebased_at gate). This mutates proposals across tenants.
  - _Fix:_ Include tenant_id in the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or validate ownership before updating proposals.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} executed without tenant scoping in attemptRebaseOnDrift — writes to a tenant-scoped table by id only.
  - _Fix:_ Scope such updates to the caller's tenant (add AND tenant_id = ${tenantId}) or require/validate tenant ownership before writes.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} executed without tenant constraint — persists rebased diffs across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or validate the proposal belongs to the tenant before persisting changes.
- **server/proposal-verifier.ts:246** — persist() issues UPDATE code_proposals SET verification_status... WHERE id = ${proposalId} without tenant scoping — mutates proposal verification metadata across tenants.
  - _Fix:_ Require tenant context and include tenant_id in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or otherwise validate ownership prior to mutating verification state.
- **server/recurring-messages.ts:294** — UPDATE to tenant-scoped table agent_knowledge omits tenant_id in the WHERE clause (UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}). This can update records across tenants if an id from another tenant is used.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id} or WHERE id = ${r.id} AND tenant_id = ${tenantId}) to enforce tenant isolation on writes.
- **server/research-engine.ts:468** — INSERT into project_notes does not include tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (${projectId}, ...)). If project_notes is tenant-scoped (likely), the tenant context is not explicitly recorded, violating the rule that INSERTs must pass tenant_id.
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)) so the created row is explicitly tied to the tenant.
- **server/research-engine.ts:475** — Insertions of project_notes for each finding omit tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) ...). This writes tenant-scoped data without explicitly setting tenant_id.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. tenant_id = ${session.tenantId}) to ensure notes are created under the correct tenant.
- **server/research-engine.ts:545** — UPDATE research_sessions SET status = ${reason} ... WHERE id = ${sessionId} updates a tenant-scoped table without constraining tenant_id, allowing cross-tenant modification if the id is manipulated.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or include tenant_id in the WHERE predicate) so only sessions belonging to that tenant are updated.
- **server/research-engine.ts:810** — UPDATE research_experiments SET ... WHERE id = ${experimentId} performs a write to a tenant-scoped table without a tenant_id WHERE clause. Multiple update sites in runExperiment lack a tenant constraint.
  - _Fix:_ Include tenant_id = ${session.tenantId} in the WHERE clause for all UPDATEs to research_experiments (e.g. WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}) to enforce write isolation.
- **server/research-engine.ts:841** — UPDATE research_experiments ... WHERE id = ${experimentId} occurs without tenant_id constraint (error-handling branch). This permits cross-tenant updates to experiment rows.
  - _Fix:_ Add tenant_id = ${session.tenantId} to the WHERE clauses of these UPDATEs (all occurrences) to ensure only the tenant's rows are modified.
- **server/research-engine.ts:887** — Transient-error UPDATE to research_experiments (UPDATE ... WHERE id = ${experimentId}) lacks tenant_id in WHERE clause. Several error/retry branches update experiments without tenant scoping.
  - _Fix:_ Ensure every UPDATE to research_experiments includes tenant_id (e.g. AND tenant_id = ${session.tenantId}) before executing the write.
- **server/research-engine.ts:901** — UPDATE research_experiments setting error state WHERE id = ${experimentId} is executed without tenant_id in the WHERE clause, allowing modification of other tenants' rows.
  - _Fix:_ Require AND tenant_id = ${session.tenantId} (or equivalent) on this and all other experiment-update statements to enforce tenant ownership checks.
- **server/research-engine.ts:916** — Periodic UPDATE research_sessions SET ... WHERE id = ${session.sessionId} (end-of-run bookkeeping) does not include tenant_id, permitting cross-tenant updates of session records.
  - _Fix:_ Add tenant_id = ${session.tenantId} to the WHERE clause so session updates are restricted to the tenant that owns the session.
- **server/routes.ts:4478** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts `INSERT INTO project_notes (project_id, note, author) VALUES (...)` without providing tenant_id; per project invariant tenant_id must be supplied on every insert to tenant-scoped tables (columns are NOT NULL with no default). Additionally, the project_id being inserted was read from a non-tenant-scoped SELECT (see prior finding), so ownership of the referenced project is not validated.
  - _Fix:_ Include tenant_id in the INSERT and validate project ownership before inserting. Example: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId}, ${projId}, ${snapshot.slice(0,5000)}, 'system:context-guard')`. Also ensure projId was resolved from a tenant-scoped query (or explicitly verify the project belongs to conv.tenantId) before inserting.
- **server/routes/conversations.ts:98** — UPDATE on `conversations` omits tenant scoping: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`. Writes to a tenant-scoped table must include a tenant constraint to prevent cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant in the WHERE clause (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}), or perform the update via a tenant-scoped storage helper that enforces the tenant id.
- **server/routes/conversations.ts:102** — INSERT into `project_conversations` omits tenant_id column entirely: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)`. Tenant-scoped tables must have tenant_id set on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conv.id}, ${tenantId})) or use a storage function that sets tenant_id before inserting.
- **server/routes/conversations.ts:104** — UPDATE on `projects` omits tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`. This can modify another tenant's project if IDs are guessed.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or use a tenant-aware update helper.
- **server/routes/conversations.ts:227** — INSERT into `project_notes` omits tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (...)`. If `project_notes` is tenant-scoped, tenant_id must be inserted to avoid cross-tenant data writes.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projId}, ${conv.tenantId ?? tenantId}, ${note.slice(0,8000)}, 'system:archive')) or use a tenant-scoped storage API that inserts tenant_id.
- **server/routes/heartbeat.ts:102** — UPDATE on `heartbeat_tasks` omits tenant scoping in approve route: `UPDATE heartbeat_tasks SET approval_status = 'approved', ... WHERE id = ${taskId}`. This allows approving tasks in other tenants by ID.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${taskId} AND tenant_id = ${task.tenantId ?? <caller tenant>}) or use a storage helper that enforces tenant ownership before updating.
- **server/routes/heartbeat.ts:116** — UPDATE on `heartbeat_tasks` omits tenant scoping in reject route: `UPDATE heartbeat_tasks SET approval_status = 'rejected', ... WHERE id = ${taskId}`. Writes to tenant-scoped tables must include tenant filters.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${taskId} AND tenant_id = ${task.tenantId ?? <caller tenant>}) or perform the change through a tenant-aware storage API that validates ownership first.
- **server/safety/transactional-snapshot.ts:407** — UPDATE action_snapshots performed via pool.query(`UPDATE action_snapshots SET undone_at = NULL WHERE id = $1`, [row.id]) does not constrain the write by tenant_id. action_snapshots is tenant-scoped (tenant_id is NOT NULL) so this UPDATE could affect a row outside the caller's tenant if an id is misused.
  - _Fix:_ Include tenant scoping in the UPDATE. e.g. UPDATE action_snapshots SET undone_at = NULL WHERE id = $1 AND tenant_id = $2 and pass ctx.tenantId (or use db.execute(sql`...` with ${ctx.tenantId})). Prefer parameterized queries and require the tenantId be passed into this rollback path so every write is explicitly tenant-bounded.
- **server/second-opinion.ts:404** — settleFusionReservation issues an UPDATE on agent_cost_ledger: UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} with no tenant_id constraint. agent_cost_ledger is tenant-scoped, so this update can modify another tenant's ledger row if ids are misassociated or guessed.
  - _Fix:_ Scope the UPDATE by tenant. Change the API to accept tenantId (or otherwise resolve and validate the reservation's tenant) and do UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Use parameterized/sql-template usage to avoid string interpolation.
- **server/second-opinion.ts:423** — releaseFusionReservation issues UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId} without scoping by tenant_id. This can release/modify reservation rows belonging to other tenants.
  - _Fix:_ Require and pass tenantId into releaseFusionReservation (or verify the reservation row maps to the expected tenant) and use WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Keep queries parameterized and avoid relying solely on id equality for tenant-scoped tables.
- **server/sculptor.ts:93** — UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id} — missing tenant_id in WHERE. sculptor_sessions is tenant-scoped; this UPDATE can modify another tenant's session by id.
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} (or the session's tenantId). Ensure every UPDATE/DELETE on sculptor_sessions includes tenant_id and use parameterized templates.
- **server/sculptor.ts:100** — UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id} — no tenant_id constraint. This can associate a conversation_id to a session belonging to another tenant.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} (or the session's tenantId) to the WHERE clause. Alternatively pass tenantId into the update call and validate it before executing the UPDATE, always using parameterized queries.
- **server/sculptor.ts:156** — UPDATE sculptor_sessions SET status = ..., result = ..., tool_calls_count = ..., tokens_used = ... WHERE id = ${sessionId} — missing tenant_id in WHERE. This monitor path updates session rows without enforcing tenant scope.
  - _Fix:_ Constrain the UPDATE by tenant: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Ensure monitorSession receives and uses the tenantId parameter in every write to sculptor_sessions to enforce tenant isolation.
- **server/sculptor.ts:346** — UPDATE sculptor_sessions SET review_result = ${...} WHERE id = ${sessionId} — missing tenant_id filter. review_result write is not scoped by tenant.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Prefer passing tenantId into the function and use it in all UPDATE/DELETE statements against tenant-scoped tables.
- **server/seed.ts:2859** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only id with no tenant_id constraint. This can modify rows belonging to other tenants with the same id values.
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or join to a SELECT that verifies the row's tenant before performing the update. Ensure the code obtains/uses the correct tenant_id for each program before writing.
- **server/seed.ts:3212** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts into a table that likely should be tenant-scoped but does not set tenant_id. Tenant-scoped tables require tenant_id on INSERTs (columns are NOT NULL with no default per project constraints).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id}) ) or compute the correct tenant_id from the referenced project/conversation and validate ownership before inserting. Do not insert cross-tenant links.
- **server/self-improvement.ts:394** — UPDATE against tenant-scoped table `experiments` uses only the experiment id in the WHERE clause (where(eq(experiments.id, experiment.id))). This allows updating an experiment row that belongs to another tenant if an id is known.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause, for example .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Ensure every UPDATE/DELETE on tenant-scoped tables constraints by tenant_id (or use a helper that enforces tenant scoping).
- **server/self-improvement.ts:413** — Error-path UPDATE against tenant-scoped table `experiments` uses only the experiment id in the WHERE clause (no tenant_id check). Same cross-tenant modification risk as above.
  - _Fix:_ Add tenant scoping to the WHERE clause on error-path updates as well (AND eq(experiments.tenantId, config.tenantId)). Ensure all update branches include the tenant constraint.
- **server/self-reflection.ts:366** — UPDATE memory_entries updates rows using only id in the WHERE clause (WHERE id = ...), without constraining tenant_id. Although the code selected the existing row earlier with a tenant filter, the UPDATE itself lacks tenant scoping and could be invoked with a row id from another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${id} AND tenant_id = ${tenantId}. Prefer to propagate tenantId into the update operation rather than relying on a prior SELECT alone.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` (recentMsgs) without constraining tenant_id. Query uses only conversationId: `.where(eq(messages.conversationId, conversationId))` — missing tenant scoping.
  - _Fix:_ Include an explicit tenant filter in the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or use tenantScope/requireTenantScope helper). Failing closed here prevents cross-tenant reads.
- **server/skillify.ts:195** — SELECT from tenant-scoped table `conversations` without scoping to the caller's tenant: `.where(eq(conversations.id, conversationId))`. This issues a tenant-scoped read with no tenant_id filter.
  - _Fix:_ Constrain the SELECT to the caller tenant (e.g. .where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId)))) or use requireTenantScope(conversations.tenantId, tenantId) to fail-closed.
- **server/skillify.ts:203** — SELECT from tenant-scoped table `messages` in analyzeConversation uses only conversationId `.where(eq(messages.conversationId, conversationId))` with no tenant filter — enables cross-tenant data exposure.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise validate ownership before reading messages.
- **server/skillify.ts:223** — SELECT from `personas` by id with no tenant constraint: `.where(eq(personas.id, conv[0].personaId))`. If `personas` is tenant-scoped this allows reading another tenant's persona row.
  - _Fix:_ Verify and enforce ownership: either include tenant filter in the query (and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))) or explicitly assert the persona belongs to the tenant before querying. Use the project's tenant-guard / tenantScope helpers where appropriate.
- **server/skillify.ts:331** — SELECT from tenant-scoped table `messages` (in skillifyConversation) without tenant scoping: `.where(eq(messages.conversationId, conversationId))` — missing tenant constraint.
  - _Fix:_ Scope the query to the tenant (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or validate conversation ownership first and then query messages with tenant filter.
- **server/storage.ts:246** — createConversation() inserts into conversations but does not require or enforce tenantId — INSERT may omit tenant_id or accept an unvalidated tenant value.
  - _Fix:_ Make tenantId mandatory in the function signature (assertValidTenantId) and/or set tenantId from the caller context before insert. Always pass tenant_id explicitly in the INSERT and validate it (e.g. assertValidTenantId(data.tenantId)).
- **server/storage.ts:521** — createMemoryEntry() inserts into memory_entries without ensuring tenantId is present or validated.
  - _Fix:_ Require tenantId on createMemoryEntry (or validate data.tenantId via assertValidTenantId) and always include tenant_id explicitly in the INSERT. Do not rely on callers to set tenantId implicitly.
- **server/storage.ts:1241** — getAllDataForExport() selects from nearly every table (conversations, messages, personas, memory_entries, agent_knowledge, etc.) with no tenant scoping — this exposes/exporting all tenants' data in one call.
  - _Fix:_ Restrict this function to an admin-only, auditable path and require explicit authorization. Prefer requiring a tenantId to export a single tenant's data or implement an admin export that operates under a strict, logged/approved process. Never allow ordinary callers to call this without tenant checks.
- **server/tools.ts:7379** — INSERT into project_conversations does not set tenant_id. Inserting a row into a tenant-scoped table without explicitly including tenant_id violates the invariant that every INSERT must pass tenantId explicitly (columns are NOT NULL with no default).
  - _Fix:_ Include the calling tenant's id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). Alternatively, use a storage helper that enforces tenant assignment or assert ownership and set tenant_id explicitly before inserting.
- **server/tools.ts:7482** — INSERT into project_files does not set tenant_id. The INSERT INTO project_files(...) VALUES(...) omits tenant_id, violating the rule that every INSERT into tenant-scoped tables must include tenant_id.
  - _Fix:_ Pass tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenantId is derived from caller context and not from untrusted input. Prefer using a storage API that enforces tenant scoping if available.
- **server/tools.ts:7496** — INSERT into project_notes does not set tenant_id. The code inserts project_notes (project_id, note, author) without an explicit tenant_id column value.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). If project ownership was verified earlier, still include tenant_id explicitly to satisfy the schema/invariant and avoid cross-tenant writes.
- **server/tools.ts:7521** — INSERT into project_conversations (linking a conversation) does not set tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) omits tenant_id.
  - _Fix:_ Explicitly insert the tenant_id (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). Also ensure the conversation/project ownership check runs before the insert and fail if ownership isn't verified.
- **server/tools.ts:9241** — INSERT into project_files (auto-link from write_file) does not set tenant_id. The auto-link INSERT INTO project_files(...) VALUES(...) omits tenant_id when adding the written file to a project.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenantId is taken from the verified project/ conversation ownership check and not from untrusted input; prefer using centralized storage functions that enforce tenant attribution.
- **server/tools.ts:11441** — SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ORDER BY created_at DESC LIMIT 1 reads a session token (sensitive) without constraining by tenant. This may expose another tenant's presenter session token if a caller supplies/controls presentationId.
  - _Fix:_ Restrict the query to the calling tenant (e.g. WHERE presentation_id = ${...} AND tenant_id = ${params._tenantId}). If presenter_sessions is keyed by presentation ownership, ensure the tenant_id column is present and always used in WHERE clauses. Prefer parameterized Drizzle queries or helper functions that enforce tenant filtering.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} is executed without verifying tenant_id. This lets code update ledger rows by id across tenants if reservationId is guessed or reused.
  - _Fix:_ Require tenant context and include tenant_id in the UPDATE predicate. For example, change settleVentureReservation to accept tenantId and perform: UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} AND tenant_id = ${tenantId} RETURNING ...; this ensures only rows belonging to the caller's tenant may be modified.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET cost_usd='0' WHERE id = ${reservationId} (releaseVentureReservation) runs without tenant scoping. An attacker could release or modify another tenant's reservation row by id.
  - _Fix:_ Add tenantId parameter and include it in the WHERE clause: UPDATE agent_cost_ledger SET cost_usd='0', operation='...released' WHERE id = ${reservationId} AND tenant_id = ${tenantId}; always verify ownership before updating ledger rows.
- **server/video-job-runner.ts:226** — recoverStaleVideoJobs performs a broad UPDATE on videoJobs (setting status='failed') with no tenant_id constraint. This will mark stale jobs across all tenants as failed, potentially impacting unrelated tenants' jobs.
  - _Fix:_ Restrict stale-job recovery to the intended tenant(s) or run it per-tenant from an admin context. At minimum add tenant scoping (e.g. include eq(videoJobs.tenantId, ${tenantId}) in WHERE) or explicitly document and gate that this operation is a system-level admin task. Prefer iterating tenants and applying the update per-tenant so tenant isolation is enforced.

### HIGH (165)

- **server/agent-activity.ts:60** — getPersonality() selects from `personas` by id with no tenant constraint: `... FROM personas WHERE id = ${personaId}` — reads persona data without verifying tenant ownership.
  - _Fix:_ Require and pass a tenantId into getPersonality, and include it in the WHERE (e.g. `WHERE id = ${personaId} AND tenant_id = ${tenantId}`), or otherwise verify the persona belongs to the caller's tenant before returning sensitive fields.
- **server/agent-desk.ts:297** — getDesksOverview() executes `SELECT id, name FROM personas` with no tenant constraint — returns personas across tenants.
  - _Fix:_ Restrict persona lookup to the tenant (e.g. `SELECT id, name FROM personas WHERE tenant_id = ${tenantId}`), or join against tenant-scoped agent_desks/persona mappings so only personas for the requested tenant are returned.
- **server/agentic-engines.ts:344** — gatherOperationalData() queries heartbeat_logs without tenant scoping: `FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` (and similar aggregated queries) — reads logs across tenants.
  - _Fix:_ Add tenant scoping to heartbeat_logs queries (e.g. `WHERE tenant_id = ${tenantId} AND ...`) so operational/log data is limited to the caller's tenant.
- **server/agentic-engines.ts:374** — Query selects personas globally: `SELECT name, role FROM personas WHERE is_active = true LIMIT 14` — missing tenant constraint.
  - _Fix:_ Filter personas by tenant (`WHERE tenant_id = ${tenantId} AND is_active = true`) or otherwise limit to personas visible to the requesting tenant before using names/roles in prompts or reports.
- **server/agentic-engines.ts:448** — runPredictiveEngine() selects recent heartbeat_logs with `WHERE status = 'success'` and no tenant filter — reads logs across tenants.
  - _Fix:_ Include tenant scoping on heartbeat_logs queries (e.g. `WHERE tenant_id = ${tenantId} AND status = 'success'`) to avoid cross-tenant access.
- **server/agentic-engines.ts:516** — runOptimizationEngine() queries heartbeat_logs without tenant restriction (`SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...`) — cross-tenant read.
  - _Fix:_ Constrain heartbeat_logs queries to the tenant (e.g. `WHERE tenant_id = ${tenantId}`) before grouping/aggregating so results are tenant-isolated.
- **server/agentic-engines.ts:531** — runOptimizationEngine() queries heartbeat_tasks with `WHERE enabled = true` but no tenant filter — returns tasks from all tenants.
  - _Fix:_ Add `WHERE tenant_id = ${tenantId} AND enabled = true` (or otherwise restrict to the caller's tenant) to avoid leaking or acting on other tenants' scheduled tasks.
- **server/agentic-features.ts:600** — Existence check for enrollments: `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` lacks tenant constraint — may find enrollments from other tenants.
  - _Fix:_ Include `AND tenant_id = ${params.tenantId}` in the WHERE clause (and/or validate that the referenced sequence belongs to the tenant) before deciding a contact is already enrolled.
- **server/agentic-features.ts:671** — advanceSequence() updates outreach_enrollments using `WHERE id = ${enrollment.id}` with no tenant constraint — write is not explicitly scoped to tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}`) to ensure the update cannot affect rows belonging to another tenant.
- **server/agentic-features.ts:721** — classifyReply() updates outreach_enrollments using `WHERE id = ${row.id}` with no tenant constraint — write lacks explicit tenant scoping.
  - _Fix:_ Change the UPDATE to include tenant constraint (e.g. `WHERE id = ${row.id} AND tenant_id = ${params.tenantId}`) or use a conditional update that also checks tenant_id to enforce per-tenant isolation.
- **server/agentic/harness-adaptation.ts:72** — SELECT from agent_trace_spans without any tenant_id constraint — reads potentially tenant-scoped diagnostic/tracing rows across all tenants.
  - _Fix:_ If agent_trace_spans is tenant-scoped, add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) and pass a validated tenantId into mineFailures. If the intent is an admin-wide system scan, make that explicit (accept an admin-scoped flag/tenant like ADMIN_TENANT_ID), require/verify admin privileges, and document that this routine intentionally reads cross-tenant data. Never leave tenant access implicit.
- **server/agentic/runs.ts:34** — appendStep updates agent_runs without constraining by tenant_id (WHERE only filters on id). This allows cross-tenant modifications of run rows.
  - _Fix:_ Require and pass tenantId into appendStep (or otherwise assert caller ownership) and include it in the WHERE clause, e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Validate callers supply the tenant before performing the update.
- **server/agentic/runs.ts:45** — updateRunState updates agent_runs where id = runId with NO tenant_id constraint. Tenant-scoped table writes must include tenant_id to enforce isolation.
  - _Fix:_ Add tenantId to the function signature and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers pass tenantId from an authenticated context.
- **server/agentic/runs.ts:54** — replaceRunState updates agent_runs (replace state) using only runId in WHERE; missing tenant_id constraint.
  - _Fix:_ Require tenantId and add it to the WHERE filter (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Avoid writing to a run row unless its tenant matches the caller.
- **server/agentic/runs.ts:58** — completeRun updates agent_runs status/result by runId with NO tenant_id filter, permitting cross-tenant writes to agent_runs.
  - _Fix:_ Require tenantId as an argument (or otherwise derive it safely) and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:67** — failRun sets status/error on agent_runs filtering only by id — no tenant_id check present.
  - _Fix:_ Include tenant scoping in the update (AND agent_runs.tenant_id = <caller tenant>) and require tenantId from the caller before updating run rows.
- **server/agentic/runs.ts:76** — pauseRun updates agent_runs status by runId without constraining by tenant_id.
  - _Fix:_ Require tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Update all helper run-mutating functions to enforce tenant scoping consistently.
- **server/agentic/wake-scheduler.ts:79** — runDueWakes performs an UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status='pending' AND wake_at <= now() ... ) returning * without constraining by tenant_id. This will update rows from all tenants in one statement.
  - _Fix:_ Scope the claim/update by tenant (or iterate per-tenant). Include tenant_id in the WHERE clause of the UPDATE (or in the inner SELECT) so each write is constrained, or explicitly design a per-tenant worker that passes tenantId into the statement.
- **server/agentic/resume-worker.ts:66** — Initial candidate SELECT (querying agent_runs JOIN agent_approvals) does not include a tenant_id filter and will read rows across tenants. Reads against tenant-scoped tables must be tenant-scoped.
  - _Fix:_ Either iterate per-tenant or add a tenant_id constraint to the query (e.g. WHERE r.tenant_id = ${tenantId}) depending on intended behavior. Ensure any background sweep that reads tenant-scoped tables documents and enforces per-tenant scoping to avoid cross-tenant reads.
- **server/auto-memorize.ts:159** — SELECT from messages scans recent messages across ALL tenants (no tenant_id filter). This reads tenant-scoped user/assistant content and then calls an LLM (admin-attributed) to synthesize lessons, creating a cross-tenant data-exfil/aggregation risk.
  - _Fix:_ Restrict the SELECT to a single tenant per run (WHERE m.tenant_id = ${tenantId} or join via conversations with tenant_id = ${tenantId}) or iterate tenants and run the pipeline per-tenant under that tenant's context. If intentional platform-wide analytics are required, make that explicit, limit scope, and ensure legal/operator controls and explicit admin-only gating.
- **server/auto-transcript.ts:165** — UPDATE against tenant-scoped table 'conversations' lacks tenant constraint. The code runs `UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL` without constraining by tenant_id, so it can modify rows for the wrong tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}`), or otherwise verify ownership (assert) before performing the UPDATE. Prefer using the same trusted tenant id value retrieved with the row (do not trust unvalidated ids).
- **server/auto-transcript.ts:179** — INSERT into project_conversations does not include tenant_id. The statement `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id})` writes to a potentially tenant-scoped mapping table without setting tenant_id, risking cross-tenant linkage or DB constraint failures.
  - _Fix:_ Ensure tenant_id is provided on INSERT (e.g. `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${r.project_id}, ${r.id}, ${r.tenant_id})`), or validate ownership and use a table that is intentionally global. If the table is intentionally tenant-less, document that and ensure all callers validate the parent project's tenant ownership before inserting.
- **server/business-tools.ts:74** — Invoice INSERT sets customer_id from caller input without verifying that the referenced customer belongs to the caller's tenant. This creates a cross-tenant reference risk (handler writes a foreign id without validating ownership).
  - _Fix:_ Before inserting the invoice with a caller-supplied customer_id, SELECT the customer row and assert it has tenant_id = tid (or reject/clear customer_id). Alternatively, refuse to accept a customer_id that does not belong to the tenant. E.g. SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1 and fail if not found.
- **server/business-tools.ts:321** — createContract INSERT writes customer_id from caller input without verifying that the referenced customer belongs to the tenant. A contract row may therefore reference another tenant's customer id.
  - _Fix:_ Validate ownership before inserting contracts that reference an existing customer_id: SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} and reject the request if not found. Alternatively require callers to pass only customer ids known to belong to their tenant and enforce via DB check prior to INSERT.
- **server/business-tools.ts:334** — listContracts LEFT JOINs customers (cust) using cust.id = c.customer_id but does not constrain cust.tenant_id. If contracts.customer_id could reference a row from a different tenant (see createContract/createInvoice), this join can leak other-tenant customer data.
  - _Fix:_ Constrain the join to the tenant (e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = c.tenant_id) or only select customer fields via a separate tenant-scoped lookup that enforces tenant_id = tid. Also ensure upstream writes that set customer_id always validated ownership.
- **server/ceo-orchestrator.ts:815** — SELECT from tenant-scoped table `personas` without constraining to the plan caller's tenant. The code does `await db.select().from(personasTable);` and caches all personas across tenants into `cachedPersonas`.
  - _Fix:_ Restrict the query to the plan's tenant (plan.tenantId). For example, accept/pass tenantId to this scope and use a WHERE filter (or Drizzle eq): `await db.select().from(personasTable).where(eq(personasTable.tenant_id, plan.tenantId))`. If cross-tenant reads are intentional, document and protect them with an explicit admin check; otherwise always filter by tenant_id.
- **server/channel-routing.ts:30** — Query joins global `channel_routes` to the `personas` table without a tenant filter: `JOIN personas p ON p.id = cr.persona_id`. This reads tenant-scoped persona rows with no `tenant_id = ?` constraint and can leak or mix persona data across tenants.
  - _Fix:_ When joining into tenant-scoped tables like `personas`, include a tenant constraint (pass caller tenantId into the function). For example: `... JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}` or fetch persona rows separately with an explicit `where(eq(personasTable.tenantId, tenantId))`. Ensure the caller's tenantId is supplied/validated by the caller and propagated into this function.
- **server/chat-engine.ts:4177** — Call to storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is made without passing tenant context or enforcing ownership. If attachCitations writes tenant-scoped rows (or links rows by message id), it must ensure the message belongs to the caller's tenant.
  - _Fix:_ Pass tenantId into attachCitations or have attachCitations verify message ownership before writing. For example: storage.attachCitations((assistantMsgRow as any).id, collectedCitations, tenantIdForScope) or inside attachCitations perform a tenant-scoped lookup of the message id and refuse if it doesn't belong to the tenant.
- **server/compaction.ts:400** — INSERT into `compaction_archives` does not set tenant ownership. The DB INSERT `INSERT INTO compaction_archives (conversation_id, message_count, total_messages, content) VALUES (...)` writes conversation content without providing tenant_id, violating the rule that INSERTs into tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Include tenant context when inserting archives. Either add a tenant_id column to the INSERT and pass the caller's tenantId, or resolve the conversation's tenant server-side and set tenant_id explicitly: `INSERT INTO compaction_archives (tenant_id, conversation_id, ...) VALUES (${tenantId}, ${convId}, ...)`. If the table is intentionally global, make this explicit in schema/comments; otherwise fail-closed when tenantId is unavailable.
- **server/data-protection.ts:72** — UPDATE on conversations omits tenant_id constraint. The soft-delete UPDATE uses WHERE id = ${conversationId} but does not include AND tenant_id = ${tenantId}, allowing mutation of a conversation row with that id belonging to another tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. UPDATE conversations SET deleted_at = NOW(), deleted_by = ${deletedBy} WHERE id = ${conversationId} AND tenant_id = ${tenantId}. Alternatively verify ownership earlier and use the validated tenant_id in the WHERE clause.
- **server/data-protection.ts:112** — SELECT/DELETE operations in permanentlyPurgeSoftDeleted act over conversations/messages/compaction_archives without tenant scoping. The expired SELECT fetches conversations across all tenants and subsequent DELETEs remove rows by conversation_id without an explicit tenant_id predicate.
  - _Fix:_ Either scope the purge to a specific tenant (accept tenantId param) or explicitly include tenant_id predicates on the DELETEs and SELECTs. For example, SELECT ... FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} AND tenant_id = ${tenantId}. When deleting: DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. If this routine must run globally, make that explicit and ensure only an admin context can run it and that audit logging is present.
- **server/data-protection.ts:145** — SELECT from messages in verifyMessageSaved omits tenant scoping and the function has no tenantId parameter. The query SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} may return rows for a conversation that the caller does not own.
  - _Fix:_ Require and pass tenantId into verifyMessageSaved and include AND tenant_id = ${tenantId} in the WHERE clause, or first verify the conversation belongs to the tenant (SELECT conversation WHERE id=${conversationId} AND tenant_id=${tenantId}) before querying messages.
- **server/delivery-pipeline.ts:683** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId without constraining tenant_id. This allows cross-tenant enumeration/lookup by payment id.
  - _Fix:_ Include tenant scoping in the query (e.g. add AND eq(deliveryLogs.tenantId, req.tenantId) or require a tenantId parameter) so the lookup is limited to the caller's tenant. If this handler can be called without tenant context, require/validate tenant context before querying.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog performs UPDATE on tenant-scoped table `delivery_logs` using only delivery id (WHERE id = ...) with no tenant_id constraint.
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog and include it in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))). If updates originate only from flows that created the row, enforce ownership checks or use transactions that verify tenant ownership before updating.
- **server/delivery-pipeline.ts:720** — UPDATE on `delivery_logs` (releasing stripePaymentId) uses WHERE id = prior.id without tenant_id constraint, permitting modification of another tenant's delivery row.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = <caller tenant>) or lookup+verify the row's tenant before performing the update. Ensure this operation cannot be invoked to modify rows outside the caller's tenant.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment queries `delivery_logs` by stripePaymentId with no tenant constraint, returning cross-tenant delivery records.
  - _Fix:_ Require and use a tenantId when looking up by payment id, or restrict the query to a known trusted admin flow. Add WHERE tenant_id = ${tenantId} (or equivalent Drizzle filter) to enforce tenant isolation.
- **server/doc-collections.ts:294** — UPDATE doc_chunks sets embedding by id with no tenant filter: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}. This can update rows across tenants if called without tenant scope.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and pass tenantId into the embedding update flow. Always validate ownership of the row before performing writes.
- **server/embeddings.ts:684** — storeEmbeddingVec updates tenant-scoped tables (`memory_entries` / `agent_knowledge`) with SQL that lacks a tenant_id constraint: UPDATE <table> ... WHERE id = ${id}. The function does not accept or enforce tenantId.
  - _Fix:_ Change the API to accept tenantId (or otherwise derive it safely) and include AND tenant_id = ${tenantId} in the WHERE clause. Never perform writes to tenant-scoped tables by id alone — always scope by tenant_id as well.
- **server/embeddings.ts:726** — backfillEmbeddingVecs updates `memory_entries.embedding_vec` using WHERE id = ${row.id} without tenant_id constraint (no tenant scoping on the UPDATE).
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (and/or select tenant_id when iterating rows) so backfill only affects rows within the intended tenant context. Propagate tenantId through the backfill logic and enforce it on writes.
- **server/embeddings.ts:743** — backfillEmbeddingVecs updates `agent_knowledge.embedding_vec` using WHERE id = ${row.id} without tenant_id constraint (no tenant scoping on the UPDATE).
  - _Fix:_ As above, select/track the tenant_id when enumerating rows and include tenant_id = ${tenantId} in the UPDATE WHERE clause, or otherwise ensure the backfill runs with an explicit tenant context before writing.
- **server/firecrawl.ts:192** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE(eq(scrapedPages.id, existing[0].id)) without constraining by tenant_id. This allows updating another tenant's scraped page if an id is supplied/guessed.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise assert ownership before performing the UPDATE.
- **server/firecrawl.ts:316** — UPDATE on tenant-scoped table 'scrapedPages' in crawl loop uses .where(eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint. Same cross-tenant write risk as above.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or re-check ownership before updating.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' sets driveFolderId but uses .where(eq(projects.id, projectId)) without also scoping by tenant_id. Although ownership was checked earlier, the UPDATE itself lacks tenant constraint which can allow cross-tenant mutation if assumptions change.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so writes are always constrained to the caller's tenant.
- **server/health-audit.ts:314** — SELECT/UPDATE against tenant-scoped table `code_proposals` with NO tenant_id constraint. The SELECT at line 314 pulls stale proposals globally and the subsequent UPDATE (lines 333-337) updates rows by id without scoping to tenant_id — this can archive proposals across all tenants.
  - _Fix:_ Scope the query and the update to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate tenants and run per-tenant. If this is intended to be an explicit global admin operation, require an explicit, authenticated admin context and log/authorize it. Always include tenant_id in WHERE clauses for tenant-scoped tables and include tenant_id in the UPDATE condition (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}).
- **server/health-audit.ts:346** — SELECT/UPDATE against tenant-scoped table `heartbeat_tasks` with NO tenant_id constraint. The SELECT at line 346 finds stuck heartbeat tasks across all tenants and the later UPDATE (lines 368-371) disables them by id without constraining tenant_id — this can disable other tenants' scheduled tasks.
  - _Fix:_ Include tenant scoping (WHERE tenant_id = ${tenantId}) when selecting and updating heartbeat_tasks, or run the routine per-tenant with an authenticated admin context. If global action is intended, require explicit admin authorization and narrow the UPDATE to also include tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}).
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table research_schedules without constraining tenant_id (SELECT * FROM research_schedules ...). This reads schedules across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE tenant_id = ${someTenantId}) or, if this is an intentional global admin operation, document and assert admin context explicitly (use ADMIN_TENANT_ID or an admin-only code path). Alternatively iterate per-tenant (getActiveTenantIds) and query per-tenant to avoid cross-tenant reads.
- **server/heartbeat.ts:101** — UPDATE against tenant-scoped table research_schedules without tenant_id constraint (UPDATE research_schedules SET ... WHERE id = ${sched.id}). This can modify rows from other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via a storage helper that enforces/asserts tenant context (assertTenantContext(task.tenantId) etc). Apply same fix to the other research_schedules UPDATEs around this block.
- **server/heartbeat.ts:586** — SELECT from tenant-scoped table event_log without tenant_id constraint (SELECT * FROM event_log WHERE status = 'pending' ...). This returns pending events across tenants and may be processed without explicit tenant restrictions.
  - _Fix:_ Either add tenant filtering (process per-tenant) or ensure the code asserts an explicit admin/system context before reading events across tenants. Prefer processing events per-tenant (loop getActiveTenantIds and query WHERE tenant_id = ${tid}) or add WHERE tenant_id = ${tid} to the query.
- **server/heartbeat.ts:1635** — SELECT from tenant-scoped table heartbeat_logs (SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ...) without tenant_id constraint. This can surface logs from other tenants.
  - _Fix:_ Scope log lookups by tenant (e.g. add AND tenant_id = ${task.tenantId} or ADMIN_TENANT_ID as appropriate) or use a storage helper that enforces tenant context when reading heartbeat_logs.
- **server/heartbeat.ts:257** — UPDATE to heartbeat_tasks without tenant_id constraint (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}). This modifies task rows regardless of tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call the storage API (storage.updateHeartbeatTask) which should enforce tenant assertions. Apply the same fix to other direct heartbeat_tasks UPDATEs in this file.
- **server/heartbeat.ts:2941** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without a tenant_id constraint. This can change approval_state of tasks in other tenants if ids collide or are guessed.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform this update via storage helper that validates the task belongs to the expected tenant.
- **server/heartbeat.ts:3317** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant scoping (another occurrence).
  - _Fix:_ As above, add AND tenant_id = ${tenantId} or use storage.updateHeartbeatTask which ensures the operation is limited to the intended tenant.
- **server/internal-resolver.ts:93** — finalizeEvent() updates event_log without scoping by tenant_id (no tenant_id constraint in WHERE clause). This lets a resolver potentially modify event rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId} to the WHERE), or first load/verify the event_log row's tenant_id and refuse to mutate if it doesn't match the expected tenant. Ensure the resolver receives/derives the tenant id from a trusted source (the event row) before making terminal updates.
- **server/internal-resolver.ts:107** — resolvePlanProposed() SELECTs plans WHERE id = ${planId} with NO tenant_id constraint — reading a tenant-scoped row by id only can cross tenants.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or first load the event row to obtain the authoritative tenant_id and verify plan. Do not trust an ID alone; verify ownership before making decisions or mutating related rows.
- **server/internal-resolver.ts:170** — resolveExperimentFailed() UPDATEs code_proposals WHERE id = ${proposalId} AND status = 'pending' with NO tenant_id constraint — this can modify proposals belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or fetch the proposal first and verify its tenant_id matches the expected tenant before updating. All terminal updates to tenant-scoped rows must include an explicit tenant check.
- **server/job-worker.ts:147** — Handler 'research_code_proposal' reads tenantId from job.payload and uses it to operate (passed into generateCodeProposal) instead of using the job.row's authoritative tenant_id. A crafted payload could request actions under another tenant.
  - _Fix:_ Do not trust tenant identifiers from handler payloads. Use job.tenantId (the DB-stored tenant_id column) as the authoritative tenant context or validate that payload.tenantId matches job.tenantId before performing tenant-scoped work. Prefer withTenantContext({ tenantId: job.tenantId, ... }) to gate LLM/DB calls.
- **server/job-worker.ts:168** — Handler 'research_digest' pulls tenantId from job.payload and passes it to generateResearchDigest instead of using job.tenantId. This can cause cross-tenant reads/writes if payload.tenantId is attacker-controlled.
  - _Fix:_ Use the worker-provided job.tenantId (the value stored in the agent_jobs row) as the authoritative tenant for background jobs. If payload includes tenantId, validate it against job.tenantId or ignore it. Wrap work in the tenant context derived from the DB field (withTenantContext) so downstream calls bill/operate under the correct tenant.
- **server/lib/agent-trace.ts:129** — UPDATE against tenant-scoped table `agent_trace_spans` omits tenant_id in WHERE clause (UPDATE ... WHERE span_id = ${spanId}). This allows the UPDATE to affect spans from other tenants if a span_id is reused/guessed.
  - _Fix:_ Include the tenant predicate in the UPDATE. e.g. pass tenantId into persistSpanClose and use WHERE span_id = ${spanId} AND tenant_id = ${tenantId} (or Drizzle .where(and(eq(agentTraceSpans.spanId, spanId), eq(agentTraceSpans.tenantId, tenantId)))). Ensure the tenantId used is the caller's trusted tenant context (currentTenantId()).
- **server/lib/bwb-job-progress.ts:74** — The scope() builder may return a WHERE clause that lacks a tenant predicate when BWB_TENANT_ID (env thread) is missing — writes/updates use .where(scope(...)) and can therefore execute against video_jobs without constraining tenant_id. This intentionally-fail-open path can permit cross-tenant writes.
  - _Fix:_ Fail-closed: require an explicit tenantId for all progress writes (make tenantId a mandatory parameter to these functions) or abort when envTenantId() is null. Always include eq(videoJobs.tenantId, tenantId) in the scope. Replace the current warn-and-omit behavior with an explicit error or a guarded no-op so no UPDATE/DELETE runs without tenant_id in the WHERE clause.
- **server/lib/inbox-ingest.ts:431** — SELECT that seeks an existing inbox_messages row for orphan retry omits any tenant_id constraint: SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ... WHERE m.message_id = ${id} AND c.id IS NULL. Message IDs are external values — this read can match rows belonging to other tenants.
  - _Fix:_ Constrain the lookup to the caller's tenant. Add AND m.tenant_id = ${tenantId} to the WHERE (or use Drizzle eq(m.tenantId, tenantId)). Ensure the tenantId is the trusted, caller-provided/admin tenant and not derived from untrusted input.
- **server/lib/inbox-ingest.ts:491** — UPDATE on `inbox_classifications` omits tenant scoping: UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}. The update can modify another tenant's row that shares the same inbox_message_id if tenant is not constrained.
  - _Fix:_ Include tenant_id in the WHERE: add AND tenant_id = ${tenantId} (or use Drizzle .where(and(eq(inboxClassifications.inboxMessageId, storeResult.id), eq(inboxClassifications.tenantId, tenantId)))). Prefer always scoping by tenant for UPDATE/DELETE statements rather than relying only on foreign ids.
- **server/lib/mcp-api-keys.ts:70** — Manual construction of a Postgres text[] literal (scopesLit) from params.scopes (request-derived) and interpolation into SQL: `${scopesLit}::text[]`. This performs string interpolation of user-derived values into a SQL fragment and risks injection or syntax errors.
  - _Fix:_ Use parameterized queries/typed array binding instead of crafting a literal. Pass scopes as a parameterized array (e.g. use Drizzle/PG parameter binding for text[] or insert JSONB and cast on the server side) so the DB driver handles quoting. If manual construction is unavoidable, robustly validate and sanitize each element (ensure allowed characters only) and prefer using parameterized multi-value placeholders.
- **server/lib/scheduled-post-runner.ts:267** — Manual construction of a Postgres text[] literal (arrLit) from the request-provided platforms array and interpolation into an INSERT: `${arrLit}::text[]`. This directly injects request-derived strings into SQL text leading to SQL injection risk if escaping is incomplete.
  - _Fix:_ Do not interpolate array literals built from request data. Use parameterized array binding or pass the platforms as a JSON/ARRAY parameter to the query so the DB driver can safely bind values. Validate/normalize platform names before DB use and avoid constructing SQL fragments by string concatenation.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without constraining by tenant_id
  - _Fix:_ If this routine must be tenant-scoped, add a WHERE tenant_id = <tenant> filter (accept a tenantId parameter or read the explicit admin context). If it must scan across tenants, require/verify an explicit admin audit mode and document that this is an intentional, guarded cross-tenant admin operation (preferably restrict to an explicit admin-only path). Avoid returning or logging raw tenant data to non-admin callers.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without constraining by tenant_id
  - _Fix:_ Same as above: either add WHERE tenant_id = <tenant> to scope the query, or make this an explicit admin-only cross-tenant audit with appropriate guards and auditing. Do not perform cross-tenant reads silently in normal code paths.
- **server/memory-graph.ts:66** — UPDATE against tenant-scoped table memory_categories missing tenant_id filter (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. add AND tenant_id = ${tenantId}. Even if id came from a tenant-scoped SELECT earlier, require an explicit tenant filter (or pass tenantId into this helper) to avoid accidental cross-tenant updates.
- **server/memory-graph.ts:125** — refreshCategoryCount performs an UPDATE on memory_categories and a subselect over memory_entries without tenant scoping. The inner SELECT and the outer UPDATE lack tenant_id constraints.
  - _Fix:_ Make refreshCategoryCount accept tenantId (or otherwise determine caller tenant) and filter both the inner SELECT and the outer UPDATE by tenant_id. Example: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status='active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:393** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is missing tenant_id constraint.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}, or use the tenantId bound from the original SELECT to prevent accidental cross-tenant writes.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} is executed without constraining by tenant_id.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} AND tenant_id = ${tenantId}. If tenantId isn't available here, pass it through from the caller so updates are tenant-scoped.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} lacks tenant scoping (no tenant_id filter).
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}), or otherwise ensure the update is performed only within the target tenant's context. If refresh logic runs in a cross-tenant context, add an explicit tenant parameter.
- **server/minerva-planner.ts:373** — Probe SELECT used when CAS missed reads plans without tenant_id: SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}. This is a read against a tenant-scoped table with no tenant constraint.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or require the caller to pass tenantId. Avoid blind reads by id alone on tenant-scoped tables; validate plan ownership before returning data.
- **server/minerva-planner.ts:506** — getPlan allows an unscoped SELECT when tenantId is omitted: SELECT * FROM plans WHERE id = ${planId}. Reading a tenant-scoped table by id without tenant scoping can return other tenants' data.
  - _Fix:_ Never perform unscoped reads on tenant-scoped tables. Either require tenantId for all external callers or, if truly internal, ensure the call runs under a server-side tenant context and document/guard that strongly. Prefer adding AND tenant_id = ${tenantId} or a separate internal-only API that clearly enforces authority.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} — no tenant_id filter. This reads a persona row by id without verifying that the persona belongs to the caller's tenant.
  - _Fix:_ Constrain the lookup by tenant (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) and validate ownership before exporting. Reject or error if the persona does not belong to the requesting tenant to prevent cross-tenant data leakage.
- **server/personality-files.ts:8** — Schema for personality_files defines tenant_id INTEGER NOT NULL DEFAULT 1. This creates a default tenant context and allows INSERTs to succeed without explicitly supplying a tenant_id, violating the rule that every INSERT must pass tenantId explicitly (no default).
  - _Fix:_ Remove the DEFAULT on tenant_id and make it a plain NOT NULL column (tenant_id INTEGER NOT NULL). Ensure all code paths explicitly pass tenantId on INSERT/UPSERT. Consider adding a DB-level CHECK or trigger to fail inserts that do not supply an explicit tenant parameter during INSERT.
- **server/plan-executor.ts:117** — appendExecutionLog performs UPDATE plans SET execution_log = ... WHERE id = ${planId} without constraining by tenant_id. This can modify execution logs for plans belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId}), or load/verify the plan's tenant_id before updating and include it in the WHERE to make the update safe and idempotent.
- **server/plan-executor.ts:519** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — no tenant_id restriction. Status transitions for plans are happening without tenant scoping, allowing cross-tenant state changes.
  - _Fix:_ Constrain status updates by tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId}) or fetch and verify the plan belongs to the caller's tenant before performing the update; perform these changes atomically to avoid TOCTOU issues.
- **server/plan-executor.ts:657** — Final plan status UPDATE uses WHERE id = ${planId} without tenant_id. This writes final status/execution summary into a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${planId} AND tenant_id = ${callerTenantId}) or validate the plan's tenant prior to the UPDATE; ensure all writes to plans are constrained to the plan's tenant.
- **server/plan-executor.ts:691** — Error-path UPDATE: UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' — missing tenant_id filter. Recovery/failure writes can affect other tenants' plans.
  - _Fix:_ Add tenant scoping to the WHERE (AND tenant_id = ${callerTenantId}) or validate tenant ownership before updating; ensure all plan state transitions include the tenant constraint.
- **server/plan-executor.ts:707** — resumeStuckPlans boot-recovery UPDATE resets status for all executing plans older than the cutoff without any tenant filter: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This will affect plans across all tenants.
  - _Fix:_ Scope the recovery UPDATE to a specific tenant or iterate per-tenant (e.g. add AND tenant_id = ${tenantId}) or run recovery in a context that verifies and constrains tenant scope. If recovery must be global, ensure it's audited and safe for multi-tenant semantics (or limit to admin/maintenance mode only).
- **server/process-governor.ts:198** — SELECT from tenant-scoped table 'heartbeat_logs' without tenant_id filter (response_time case).
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}). If aggregate across all tenants is intended, make that explicit and restricted to operator-only code paths.
- **server/process-governor.ts:239** — SELECT from tenant-scoped table 'heartbeat_logs' without tenant_id filter (cascading_failures case).
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${tenantId}) to the WHERE clause to avoid cross-tenant leakage.
- **server/process-governor.ts:1183** — SELECT joins governance_rules (tenant-scoped) via LEFT JOIN gr ON gr.id = ga.rule_id but does not constrain governance_rules by tenant_id. This can expose cross-tenant rule data.
  - _Fix:_ Constrain the joined governance_rules rows to the tenant (e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ${tenantId}), or select from governance_rules with an explicit tenant filter.
- **server/project-brain.ts:135** — SELECT name,status,description FROM projects WHERE id = ${projectId} lacks tenant scoping — reads project records by id across tenants.
  - _Fix:_ Require and pass tenantId into this function and add WHERE tenant_id = ${tenantId} to the SELECT, or validate that the caller is authorized to read this project before returning data.
- **server/project-brain.ts:332** — SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has no tenant scoping — discovers projects across tenants.
  - _Fix:_ Scope the discovery query by tenant (e.g. WHERE p.tenant_id = ${tenantId}) and pass tenantId into the backfill function; ensure all subsequent queries are tenant-constrained.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks tenant constraint — reads conversations across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. AND c.tenant_id = ${tenantId}) or ensure project->conversation ownership is validated before querying.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant filter — can read messages from other tenants' conversations if IDs are reused/guessed.
  - _Fix:_ Constrain the query with tenant_id (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or validate conversation ownership beforehand.
- **server/proposal-verifier.ts:33** — SELECT from tenant-scoped table 'code_proposals' by id (WHERE id = ${proposalId}) without tenant_id constraint — reads proposal across tenants.
  - _Fix:_ Require and check tenant ownership (pass tenantId into verify flow) or add AND tenant_id = ${tenantId} to the SELECT to enforce tenant isolation.
- **server/recurring-messages.ts:252** — SELECT from tenant-scoped table agent_knowledge is performed without any tenant_id WHERE clause (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This reads rows across tenants.
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE category = 'recurring_message' AND tenant_id = ${tenantId}) or otherwise ensure this global scan is intentional and every subsequent use validates tenant ownership before any use. Prefer parameterized tenant filtering over global selects.
- **server/reference-learner.ts:290** — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} executes without a tenant_id WHERE clause. Although storedId came from a prior SELECT scoped by tenant, the UPDATE itself has no tenant constraint.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) so writes cannot accidentally modify another tenant's row.
- **server/research-engine.ts:449** — SELECT from tenant-scoped table research_experiments uses only session_id and status filters (WHERE session_id = ${sessionId} AND status = 'keep') with no tenant_id constraint. This may allow reading rows belonging to other tenants.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or equivalent) to the WHERE clause to ensure the query is constrained to the session's tenant.
- **server/research-engine.ts:458** — SELECT research_programs WHERE id = ${session.programId} is executed without a tenant_id constraint. research_programs appears tenant-scoped in other places and should be read in tenant context.
  - _Fix:_ Include tenant_id = ${session.tenantId} in the WHERE clause (or otherwise verify the program belongs to the tenant before reading) to prevent cross-tenant access.
- **server/research-engine.ts:463** — SELECT summary FROM research_sessions WHERE id = ${sessionId} is executed without a tenant_id constraint, allowing potential cross-tenant reads of session data.
  - _Fix:_ Constrain by tenant_id (e.g. WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}) when reading tenant-scoped sessions.
- **server/research-engine.ts:977** — SELECT name FROM research_programs WHERE id = ${session.programId} occurs inside injectKeepedFinding without tenant_id filtering. research_programs is treated as tenant-scoped elsewhere.
  - _Fix:_ Include tenant_id = ${session.tenantId} in the WHERE clause (or otherwise confirm program ownership) before reading program metadata to avoid cross-tenant information disclosure.
- **server/routes.ts:161** — Startup routine restoreUploadsFromDb performs a SELECT FROM file_storage with no tenant_id constraint (reads all fileStorage rows across tenants).
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = <tenant>) or iterate tenants explicitly. If this must run as a platform job, ensure it runs under an audited admin context and avoid writing tenant data to a shared disk location; or store files to per-tenant isolated locations and/or encrypt per-tenant on-disk artifacts.
- **server/routes.ts:4471** — SELECT against tenant-scoped table 'conversations' omits tenant_id constraint. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` can return a project_id for a conversation that does not belong to the caller's tenant (tenant isolation enforced only at app-level).
  - _Fix:_ Constrain the lookup to the caller's tenant and/or verify ownership first. For example: `SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${conv.tenantId} AND project_id IS NOT NULL`. Alternatively, fetch the conversation via a tenant-scoped storage API (storage.getConversation) that verifies tenant ownership before returning project_id.
- **server/routes/conversations.ts:99** — SELECT from `project_conversations` omits tenant scoping: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`. Reads of tenant-scoped association tables must include tenant filtering.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or resolve existence via a tenant-scoped storage API.
- **server/routes/inbox-notifications.ts:100** — UPDATE on `inbox_messages` omits tenant scoping when marking as read during GET: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`. Writes must include tenant_id to avoid affecting another tenant's message with the same id.
  - _Fix:_ Include tenant constraint in the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}), or call a tenant-scoped storage/update function to change read state after verifying ownership.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id; tenant ownership is only validated by an earlier project existence check, but the row itself will not be stamped with the tenant and can be misattributed or later manipulated cross-tenant.
  - _Fix:_ Include tenant_id explicitly in the INSERT. e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ${req.body.note}, ${req.body.author || 'user'}) RETURNING * . Also keep the existing project ownership check and consider asserting project_id belongs to tenant in a single INSERT ... SELECT or use parameterized DB insert helpers to avoid TOCTOU gaps.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload path) does not set tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...) omits tenant_id, so uploaded files are not stamped with tenant ownership.
  - _Fix:_ Set tenant_id when inserting project files. e.g. INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ${f.fileName}, ${'uploads/' + uniqueName}, ${fileUrl || downloadPath}, ${f.mimeType || 'application/octet-stream'}, ${fileBuffer.length}) RETURNING * . Also validate project ownership beforehand (already done) and consider including tenant_id in WHERE clauses when later selecting/deleting for defense-in-depth.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload path) does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) used when handling multer uploads omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT for multipart uploads as well. e.g. INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ${file.originalname}, ${filePath}, ${fileUrl || downloadPath}, ${file.mimetype}, ${file.size}) RETURNING * . Keep the project ownership check and add tenant predicates to related SELECT/DELETE queries as defense-in-depth.
- **server/seed.ts:34** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id filter. research_sessions appears to be tenant-scoped, so this reads across all tenants.
  - _Fix:_ Constrain the query to the target tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope counts per-tenant. If this is intended to be global admin logic, make that explicit and perform it under a clearly-authorized admin path only.
- **server/seed.ts:2854** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id constraint. research_programs is tenant-scoped (tenant_id present in other INSERTs), so this reads programs across tenants.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or select tenant_id as well and ensure subsequent updates are restricted to that tenant.
- **server/seed.ts:3204** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (...) is executed without filtering by tenant_id. conversations is likely tenant-scoped (project_id relates to tenant-scoped projects), so this will inspect rows across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL AND NOT EXISTS(... WHERE pc.tenant_id = ${tenantId} ...)). Ensure any subqueries also filter by tenant_id to avoid cross-tenant data access.
- **server/seed.ts:3204** — SELECT from tenant-scoped table 'conversations' has no tenant_id constraint. The query SELECT DISTINCT c.id, c.project_id FROM conversations c ... WHERE c.project_id IS NOT NULL AND NOT EXISTS (...) reads conversations across all tenants.
  - _Fix:_ Constrain the SELECT to the intended tenant (e.g. AND c.tenant_id = 1 or parameterize tenantId). If the goal is to backfill only a specific tenant, filter by tenant_id; if the goal is cross-tenant maintenance, explicitly iterate tenants and enforce ownership checks before inserting project_conversations links.
- **server/seed.ts:4374** — DELETE FROM heartbeat_tasks WHERE enabled = false executes on heartbeat_tasks (tenant-scoped) with no tenant_id constraint — will delete disabled tasks for all tenants.
  - _Fix:_ Scope destructive cleanup to a tenant (e.g. WHERE enabled = false AND tenant_id = 1) or iterate tenants and run per-tenant deletions. Avoid global deletes on tenant-scoped tables without explicit tenant scoping and operator intent.
- **server/seed.ts:4402** — UPDATE memory_entries ... WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ... updates tenant-scoped table 'memory_entries' without any tenant_id constraint, affecting all tenants.
  - _Fix:_ Add a tenant filter (e.g. AND tenant_id = 1 or parameterize tenantId) or iterate per-tenant. If intended to be global, require explicit operator confirmation and document the cross-tenant impact.
- **server/seed.ts:4492** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' reads tenant-scoped 'heartbeat_tasks' without any tenant_id filter (counts across all tenants).
  - _Fix:_ Restrict the count to the target tenant (e.g. AND tenant_id = 1 or use a tenantId variable). For platform-wide checks that intentionally span tenants, explicitly document and protect the operation.
- **server/seed.ts:4500** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true modifies tenant-scoped 'heartbeat_tasks' for all tenants (no tenant_id constraint).
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = 1 or loop per tenant) or verify tenant ownership before disabling tasks. Avoid blanket updates that affect every tenant.
- **server/seed.ts:4512** — UPDATE heartbeat_tasks SET cron_expression = '0 * * * *' WHERE name = 'Self-Reflection' ... updates heartbeat_tasks across all tenants because no tenant_id is applied.
  - _Fix:_ Include tenant_id = 1 (or a tenantId parameter) in the WHERE clause, or run this reconciliation per-tenant to avoid changing other tenants' scheduled tasks.
- **server/seed.ts:4517** — UPDATE heartbeat_tasks SET cron_expression = '0 */2 * * *' WHERE name = 'Memory Consolidation' ... has no tenant_id constraint and will alter schedules for all tenants.
  - _Fix:_ Scope the UPDATE to the intended tenant(s) by adding AND tenant_id = <tenantId> or by iterating tenants and applying changes per-tenant.
- **server/seed.ts:4521** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' reads tenant-scoped heartbeat_tasks without tenant_id filter (aggregates across tenants).
  - _Fix:_ Restrict the SELECT to the intended tenant (AND tenant_id = <tenantId>) or iterate tenants to compute per-tenant counts.
- **server/seed.ts:4528** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true disables tasks across all tenants because tenant_id is not constrained.
  - _Fix:_ Add AND tenant_id = <tenantId> to the WHERE clause or perform the operation per-tenant after verifying ownership; do not disable tasks for other tenants implicitly.
- **server/seed.ts:4535** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) reads heartbeat_tasks without tenant scoping, returning counts across all tenants.
  - _Fix:_ Add tenant filtering (AND tenant_id = <tenantId>) or explicitly iterate tenants to produce per-tenant results. Avoid global queries on tenant-scoped data.
- **server/seed.ts:4552** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true updates heartbeat_tasks for all tenants (no tenant_id in WHERE).
  - _Fix:_ Scope the UPDATE with tenant_id = <tenantId> or apply the change per-tenant after confirming intended scope; never perform tenant-scoped updates globally without explicit tenant constraints.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` is executed without a tenant_id constraint when tenantId argument is omitted. The query builds conditions with category only and may return rows across tenants.
  - _Fix:_ Require a tenantId parameter (fail closed) or always include eq(experiments.tenantId, tenantId) in the WHERE. Do not allow a cross-tenant scan; throw if tenantId is missing or not valid.
- **server/self-improvement.ts:521** — getExperimentHistory falls back to db.select().from(experiments) without any tenant filter when tenantId is not provided, returning experiments across all tenants.
  - _Fix:_ Require tenantId or otherwise fail-closed. If global admin listing is needed require an explicit admin flag and authorization. Otherwise always add WHERE eq(experiments.tenantId, tenantId).
- **server/skill-seeker.ts:300** — Function detectGap has a default parameter tenantId: number = 1 and will INSERT into capability_gaps using that tenant when caller omits tenantId. Defaulting to tenant 1 (admin) enables accidental or malicious cross-tenant writes.
  - _Fix:_ Do not default tenantId to 1. Require the caller to supply a tenantId and validate it. Fail closed if tenantId is missing or invalid. If the operation must act as admin, make the admin intent explicit and require appropriate authorization.
- **server/skill-seeker.ts:754** — listGaps contains branches that SELECT from tenant-scoped table `capability_gaps` without a tenant_id constraint: the branch for status only (SELECT ... WHERE status = ${status}) and the final else (SELECT * FROM capability_gaps ORDER BY ... LIMIT ...) return rows across tenants.
  - _Fix:_ Always require tenantId or enforce tenant scoping in every SELECT branch. If an admin/global listing is required, require explicit admin authorization. Add WHERE tenant_id = ${tenantId} to the queries or refuse to run when tenantId is not supplied.
- **server/storage.ts:236** — getConversationUnscoped() performs SELECT FROM conversations with no tenant_id constraint (unscoped read of tenant-scoped table).
  - _Fix:_ Remove or restrict this API. Require an explicit tenantId parameter and enforce WHERE conversations.tenant_id = tenantId (or only allow from a clearly-audited admin path). If an unscoped read is truly required, limit its use to trusted internal code and add explicit audit/logging + a calling-site check that the caller will validate tenant ownership before using the row.
- **server/storage.ts:386** — attachCitations() runs UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id constraint, allowing modification of a message row across tenants by id alone.
  - _Fix:_ Require a tenantId argument (or derive and validate it from a trusted source) and add AND tenant_id = ${tenantId} to the WHERE clause. As defense-in-depth, verify the message's tenant before updating.
- **server/storage.ts:470** — deletePersona() updates conversations and memory_entries and deletes daily_notes without any tenant filter (e.g. tx.update(conversations).set(...).where(eq(conversations.personaId, id))). This mutates tenant-scoped conversations across all tenants.
  - _Fix:_ Scope persona-delete side-effects to a tenant: require a tenantId and add eq(conversations.tenant_id, tenantId) (and similar for memory_entries/daily_notes) to each WHERE. If personas are truly global, document and enforce the intended semantics; otherwise make persona operations tenant-scoped.
- **server/storage.ts:500** — getAllMemoriesForBackup() selects all rows from memory_entries with no tenant filter — returns memory across all tenants.
  - _Fix:_ Require a tenantId parameter for backup operations (or provide a clearly-documented admin-only path). Add WHERE tenant_id = ${tenantId} (or assert admin privileges) before returning tenant data.
- **server/storage.ts:503** — getMemoryEntry(id) allows an unscoped lookup when tenantId is undefined (_warnUnscoped only logs), then SELECTs memory_entries by id potentially without tenant_id constraint.
  - _Fix:_ Make tenantId required (assertValidTenantId) or fail when tenantId is not provided. Do not permit silent/unscoped reads; enforce WHERE memory_entries.tenant_id = ${tenantId}.
- **server/storage.ts:546** — touchMemoryEntries() updates memory_entries WHERE id IN (...) with no tenant_id constraint — can increment accessCount / lastAccessed for rows belonging to other tenants.
  - _Fix:_ Accept/require tenantId and include eq(memory_entries.tenant_id, tenantId) in the WHERE, or apply a JOIN/subselect to ensure only rows matching the caller tenant are updated.
- **server/storage.ts:606** — evictOldestConversationFacts() updates conversation_facts using WHERE id IN (victims) without also constraining tenant_id — victims were selected with a tenant filter, but the UPDATE itself lacks an explicit tenant condition.
  - _Fix:_ Include the tenant check in the UPDATE (e.g. WHERE id = ANY(${victimIds}) AND tenant_id = ${tenantId}) or perform the update through the same tenant-scoped tx/context to enforce the constraint at the write site as well.
- **server/storage.ts:632** — upsertDailyNote() updates daily_notes by id (WHERE daily_notes.id = existing.id) without tenant_id in the WHERE clause — allows cross-tenant modification if id is reused or attacker controls id parameter.
  - _Fix:_ Include tenant scoping on the update (e.g. AND daily_notes.tenant_id = ${data.tenantId}) or require tenantId and validate ownership before updating.
- **server/storage.ts:765** — updateMemoryEmbedding() updates memory_entries SET embedding WHERE id = ${id} with no tenant_id constraint.
  - _Fix:_ Require and validate tenantId on embedding updates and add eq(memory_entries.tenant_id, tenantId) to the WHERE (or otherwise ensure the caller owns the row) to prevent cross-tenant writes.
- **server/storage.ts:778** — updateKnowledgeEmbedding() updates agent_knowledge WHERE id = ${id} with no tenant_id constraint.
  - _Fix:_ Require tenantId (or verify ownership) and include eq(agent_knowledge.tenant_id, tenantId) in the WHERE clause before updating embeddings.
- **server/storage.ts:786** — getMemoriesWithoutEmbeddings() selects memory_entries WHERE status='active' AND embedding IS NULL without any tenant filter — returns rows across all tenants.
  - _Fix:_ Require tenantId or restrict this function to admin-only usage; otherwise add eq(memory_entries.tenant_id, ${tenantId}) to the WHERE to scope results to the caller tenant.
- **server/storage.ts:791** — getKnowledgeWithoutEmbeddings() selects agent_knowledge rows with no tenant filter — returns knowledge across all tenants.
  - _Fix:_ Require tenantId or add eq(agent_knowledge.tenant_id, ${tenantId}) to the WHERE; if this is an administrative bulk job, make that explicit and gated by an admin check.
- **server/storage.ts:801** — archiveExpiredMemories() runs a raw UPDATE memory_entries SET status='archived' ... with no tenant filter — will archive rows across all tenants.
  - _Fix:_ Limit archival jobs to a per-tenant scope by requiring tenantId or add tenant_id = ${tenantId} to the WHERE. If global archival is intended for an admin job, gate the function behind an admin check and document it.
- **server/storage.ts:817** — archiveStaleMemories() runs a raw UPDATE memory_entries ... WHERE status='active' AND created_at <= ${cutoff} AND last_accessed <= ${accessCutoff} without any tenant constraint.
  - _Fix:_ Either require a tenantId and include tenant scoping in the WHERE, or require an explicit admin-only context. Avoid unscoped updates to tenant-scoped tables.
- **server/storage.ts:1390** — supersedeAdr() updates architecture_decisions rows by id without tenant_id constraints in the UPDATEs (even though the code fetched both rows with tenant scoping earlier).
  - _Fix:_ Include tenant checks in the UPDATE WHERE clauses (e.g. AND tenant_id = ${tenantId}) to enforce the tenant invariant at the write site as well, not just at the initial SELECT.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. The query .where(eq(customTools.name, parsed.name)) omits tenant scoping, permitting cross-tenant hits if naming or prefixes change.
  - _Fix:_ Include tenant scoping in the WHERE clause (or otherwise verify ownership). e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Alternatively explicitly verify the resolved row's tenantId matches the provided tenantId before proceeding.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without constraining tenant_id. .where(eq(customTools.id, inserted.id)) lacks tenant filtering and can expose or return rows from other tenants if ids are ever guessed or leaked.
  - _Fix:_ Constrain the query by tenant_id as well: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Or fetch by id and assert the returned row.tenantId === tenantId before using it.
- **server/tool-learning.ts:283** — UPDATE against tenant-scoped table `custom_tools` uses only the PK (eq(customTools.id, tool.id)) without tenant_id constraint. Updating by id alone can mutate another tenant's row if the caller can control/guess ids.
  - _Fix:_ Add tenant scoping to the update predicate: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). If tenantId is not in scope here, first load the row and verify its tenantId equals the caller's tenantId before updating.
- **server/tool-learning.ts:300** — DELETE against tenant-scoped table `custom_tools` uses only the PK in WHERE (eq(customTools.id, tool.id)) with no tenant_id check. This allows deletion of rows across tenants if an id is misused.
  - _Fix:_ Restrict deletes by tenant id: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Alternatively verify ownership (row.tenantId === tenantId) before deleting.
- **server/tool-learning.ts:350** — UPDATE that increments usageCount on `custom_tools` uses only the PK in WHERE (eq(customTools.id, tool.id)) and omits tenant scoping. This can increment usage counters for another tenant's tool if ids are manipulated.
  - _Fix:_ Include tenant scoping in the update: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). If tenantId is not directly available, first read and assert the row belongs to the caller's tenant before performing the update.
- **server/tools.ts:10899** — SELECT from tenant-scoped table 'memory_entries' lacks a tenant_id filter. The query SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' will return rows across all tenants.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add AND tenant_id = ${params._tenantId} or use the Drizzle .where(eq(memoryEntries.tenantId, tenantId))). If the code relies on a prior ownership assertion, still prefer adding the tenant_id predicate to every SQL read against tenant-scoped tables to enforce isolation at the SQL level.
- **server/tools.ts:11106** — Subqueries select from tenant-scoped tables 'conversations' and 'project_conversations' without a tenant filter: (SELECT project_id FROM conversations WHERE id = ${convId} ...) and (SELECT project_id FROM project_conversations WHERE conversation_id = ${convId} ...). These reads can leak project relationships for conversations from other tenants.
  - _Fix:_ Include tenant scoping in the subqueries (e.g. WHERE id = ${convId} AND tenant_id = ${params._tenantId}), or rewrite the lookup to select project_id in a single tenant-scoped query. Always ensure every read against tenant-scoped tables includes a WHERE tenant_id = <caller tenant>.
- **server/tools.ts:14329** — INSERT into project_files omits tenant_id. The code inserts a row into project_files without setting tenant_id (project-scoped table writes must include tenant_id). Relying on assertProjectInTenant alone is insufficient: every INSERT must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and pass the caller tenant (params._tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Keep the assertProjectInTenant ownership check, and ensure the INSERT is parameterized as shown.
- **server/tools.ts:16669** — INSERT into project_files omits tenant_id. This insertion (video file) writes into project_files without providing tenant_id, violating the tenant-scoped INSERT requirement.
  - _Fix:_ Add tenant_id to the inserted columns and values (use the authenticated tenant context). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Continue to verify project ownership before inserting.
- **server/tools.ts:17057** — UPDATE on browser_workflows lacks a tenant_id constraint. The UPDATE 'SET last_replayed = NOW() WHERE id = ${workflow.id}' does not include 'AND tenant_id = <caller tenant>', so it can modify a tenant-scoped row without explicit tenant scoping in the WHERE clause.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}. Prefer parameterized queries (wfSql) and pass tenantId from the request context.
- **server/tools.ts:17057** — UPDATE to browser_workflows omits tenant filter: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. This allows a caller to update a workflow by id across tenant boundaries.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: add `AND tenant_id = ${tenantId}` (or use Drizzle where(eq(browserWorkflows.tenantId, tenantId))). E.g. `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`.
- **server/tools.ts:19036** — UPDATE to memory_entries omits tenant filter: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id}`. Although the SELECT used tenant_id, this UPDATE by id alone can modify another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE. Either add `AND tenant_id = ${tid}` to the WHERE clause or use Drizzle's where(eq(memoryEntries.tenantId, tid), eq(memoryEntries.id, id)). Do the same for any subsequent UPDATEs on memory_entries.
- **server/tools.ts:19109** — Bulk UPDATE on memory_entries to bump last_accessed uses `WHERE id = ANY(${ids})` without also constraining tenant_id. This can increment access_count/last_accessed for rows belonging to other tenants if ids are guessed or supplied.
  - _Fix:_ Add tenant constraint to the UPDATE, e.g. `WHERE tenant_id = ${tid} AND id = ANY(${ids})`. When using Drizzle, call where(eq(memoryEntries.tenantId, tid), inArray(memoryEntries.id, ids)). Always enforce tenant in multi-row updates.
- **server/tools.ts:19775** — UPDATE to memory_entries in record_strategic_win omits tenant filter: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id}`. The prior SELECT scoped by tenant_id, but the UPDATE does not, allowing cross-tenant modification by id.
  - _Fix:_ Constrain the UPDATE with tenant_id: `WHERE id = ${id} AND tenant_id = ${tid}` or use Drizzle's where(eq(memoryEntries.tenantId, tid), eq(memoryEntries.id, id)). Always include tenant in UPDATEs even if the row was found earlier.
- **server/tools.ts:19726** — UPDATE to proposed_skills omits tenant filter when promoting/marking pending: `db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id))`. This updates by id only and could modify proposals from other tenants.
  - _Fix:_ Include tenant scoping in the update predicate. Example with Drizzle: `.where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid}))`. Alternatively use SQL: `WHERE id = ${row.id} AND tenant_id = ${tid}`.
- **server/tools.ts:19751** — UPDATE to proposed_skills omits tenant filter when marking a proposal 'accepted' and setting promotedSkillId: `db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id))`. No tenant constraint is present.
  - _Fix:_ Add tenant constraint to the WHERE clause (e.g. include tenantId in the where). Use `.where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid}))` or `WHERE id = ${row.id} AND tenant_id = ${tid}` to ensure only the caller's tenant row is modified.
- **server/tools.ts:19768** — UPDATE to proposed_skills omits tenant filter when marking a proposal 'rejected': `db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id))`. This allows changing another tenant's proposal by id.
  - _Fix:_ Constrain the update to the tenant: add `AND tenant_id = ${tid}` or use Drizzle's where with both id and tenantId (e.g. `.where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid}))`). Ensure all updates target only rows owned by the caller's tenant.
- **server/twilio.ts:87** — SELECT from agent_knowledge (messaging_pairing) is executed without constraining by tenant_id. This reads pairing records across all tenants using a phone-derived title, allowing cross-tenant lookup/abuse.
  - _Fix:_ Filter the query by tenant_id (or otherwise scope by an authenticated tenant context) instead of querying globally. e.g. SELECT ... FROM agent_knowledge WHERE tenant_id = ${expectedTenantId} AND category='messaging_pairing' AND title=${`${channel}:${phone}`} LIMIT 1. Alternatively store/maintain pairings in a table columned by tenant_id and always include that tenant_id in the WHERE clause.
- **server/twilio.ts:102** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without a tenant_id constraint. Reading the conversations table by id only can leak or allow mapping across tenants; it also relies on a pairing JSON field (info) as the authority.
  - _Fix:_ Constrain the lookup to the expected tenant to enforce isolation, e.g. SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 — or better, avoid trusting info.tenantId from the pairing JSON and instead require pairings be stored as proper tenant-scoped rows (with tenant_id) and query using that tenant_id or the authenticated tenant context.
- **server/video-job-runner.ts:178** — Upsert (db.insert(...).onConflictDoUpdate) mirrors job state to videoJobs without constraining updates by tenant_id — the onConflict update's WHERE uses only updatedAt. This can potentially overwrite another tenant's row for the same jobId or allow cross-tenant regression.
  - _Fix:_ Ensure the upsert's update condition also enforces tenant ownership (e.g. include AND eq(videoJobs.tenantId, ${row.tenantId}) in the onConflict `where` clause), or make jobId unique-per-tenant and use a composite conflict target (jobId, tenantId). Always include tenant_id when performing updates to tenant-scoped tables.
- **server/watchlist.ts:111** — Query selects watchlist_items.* (wi.name, wi.category) via LEFT JOIN without constraining the joined watchlist_items row to the caller's tenant. This reads from a tenant-scoped table (watchlist_items) with no tenant_id filter on the joined table.
  - _Fix:_ Constrain the joined watchlist_items to the same tenant. Example: LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id AND wi.tenant_id = ${tenantId} (or add WHERE wi.tenant_id = ${tenantId} when appropriate). Ensure any referenced tenant-scoped table in a JOIN includes a tenant_id check matching the caller context.
- **server/webhook-triggers.ts:61** — listTriggers() SELECT joins personas (p.name) via LEFT JOIN ON p.id = w.persona_id but does not enforce a tenant_id constraint on personas. personas is tenant-scoped; this can leak persona data across tenants.
  - _Fix:_ Only read persona rows that belong to the expected tenant. Options: (a) add tenant_id to webhook_triggers and join with ON p.id = w.persona_id AND p.tenant_id = w.tenant_id; or (b) avoid the join and fetch persona via a tenant-scoped lookup (e.g., storage.getPersona(persona_id, tenantId)) after resolving the trigger's tenant. Ensure any access to tenant-scoped tables includes an explicit tenant_id check.
- **server/webhook-triggers.ts:123** — processTriggerEvent() SELECT joins personas (p.name) while selecting a webhook trigger, but the JOIN lacks any tenant_id constraint on the personas table. This reads from a tenant-scoped table (personas) without verifying tenant ownership of the persona row.
  - _Fix:_ Enforce tenant scoping when joining personas. For example, ensure webhook_triggers stores a tenant_id and change the JOIN to: LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = w.tenant_id. Alternatively, after loading the trigger, resolve the persona via a tenant-scoped storage call (storage.getPersona(persona_id, tenantId)) before returning persona data. Always validate ownership before reading tenant-scoped rows.
- **server/wiring-invariants.ts:180** — Query against tenant-scoped tables (research_experiments, code_proposals, research_sessions, research_schedules) is executed without any tenant_id constraint. This allows cross-tenant aggregation/observation of tenant data.
  - _Fix:_ Restrict the subqueries to a specific tenant by passing an explicit tenantId into checkOutcomeCanary and adding WHERE tenant_id = ${tenantId} (or using the Drizzle .where(eq(..., tenantId))) to each tenant-scoped subquery. If this is intended as an admin-wide audit, require explicit admin authorization and make the intent explicit (e.g. require ADMIN_TENANT_ID or aggregate per-tenant while verifying caller privileges).
- **server/wiring-invariants.ts:244** — SELECT count(*) FROM tool_performance WHERE last_success_at > ... is run without a tenant_id filter. tool_performance appears tenant-scoped; this aggregates data across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) and accept tenantId as a parameter, or, if an authorized global audit is required, explicitly require and document admin privileges and perform a safe per-tenant aggregation with proper authorization checks.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tool_performance (total rows) is executed with no tenant_id filter, exposing aggregate counts across tenants.
  - _Fix:_ Constrain the count to a tenant (WHERE tenant_id = ${tenantId}) by passing tenantId into the check, or explicitly require admin authorization for any cross-tenant aggregate and make that check safe and auditable.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages WHERE created_at > ... is executed with no tenant_id constraint. messages are tenant-scoped and this leaks cross-tenant message counts.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to the messages query and accept tenantId in the function, or implement an explicit, auditable admin-mode that aggregates per-tenant only after verifying caller privileges.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE ... is run without tenant scoping, reading tenant-scoped tool invocation records across all tenants.
  - _Fix:_ Filter by tenant_id = ${tenantId} (or group by tenant_id and return per-tenant results) and pass tenantId into the function. If a system-wide view is intended, require explicit admin authorization and document that behavior.
- **server/wiring-invariants.ts:366** — Dynamic SELECT COUNT/MAX FROM ${spec.table} is executed without any tenant_id restriction for tenant-scoped tables like tool_performance and knowledge_nudges, allowing cross-tenant inspection.
  - _Fix:_ When probing subsystem tables that are tenant-scoped, include WHERE tenant_id = ${tenantId} (and pass tenantId into checkSubsystemFreshness). If the intent is to check schema existence across the whole DB, separate that from tenant-scoped freshness checks and require explicit admin privileges for any cross-tenant checks.
- **server/wiring-invariants.ts:417** — Query against tool_performance to detect drift (SUM/MAX/GROUP BY) has no tenant_id filter, aggregating data across tenants.
  - _Fix:_ Scope the drift detection to a specific tenant (WHERE tenant_id = ${tenantId}) or perform per-tenant aggregation with explicit admin authorization. Pass tenantId into checkToolDrift and use parameterized filters (Drizzle .where(eq(..., tenantId))).
- **server/wiring-invariants.ts:535** — sql.raw(...) runs SELECT COUNT(...) FROM ${tbl.name} with no tenant filter for memory tables that are tenant-scoped (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.), producing cross-tenant counts.
  - _Fix:_ Avoid unscoped raw queries against tenant-scoped tables. Add tenant filtering (WHERE tenant_id = ${tenantId}) and parameterize the tenantId. If a global admin summary is needed, require explicit admin mode and compute per-tenant counts instead of unscoped aggregates.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory is executed without tenant_id constraint even though graph_memory is tenant-scoped (the code even comments about "across all tenants"). This violates the tenant-isolation invariant.
  - _Fix:_ Restrict the graph_memory count to a particular tenant via WHERE tenant_id = ${tenantId} (pass tenantId into the routine) or, if a global check is intended, require explicit admin authorization and perform a per-tenant aggregation while validating privileges.

### MEDIUM (53)

- **server/agentic/task-forces.ts:40** — createTaskForce inserts caller-supplied foreign ids (projectId, personaIds) into task_forces without verifying those referenced rows belong to the caller's tenant.
  - _Fix:_ Before inserting, validate referenced ids (projectId, personaIds) by selecting the target rows and confirming their tenant_id == p.tenantId. Reject or clear references that are not owned by the tenant. Perform these checks transactionally to avoid races.
- **server/agentic/wake-scheduler.ts:42** — scheduleWake inserts personaId, conversationId, projectId supplied by the caller into agent_wake_schedules without verifying those foreign ids belong to the tenant.
  - _Fix:_ Validate that personaId, conversationId and projectId (if provided) belong to the specified tenant before persisting. If ownership cannot be proven, reject the schedule or null out the foreign id. Use SELECT ... WHERE id = X AND tenant_id = <tenant> to verify.
- **server/auto-memorize.ts:100** — isDuplicate() reads recent rows from memory_entries without any tenant_id filter — dedupe compares facts across tenants, leaking tenant data into deduplication and inference logic.
  - _Fix:_ Add tenant scoping to the query (WHERE tenant_id = ${tenantId}) and pass the tenant id into isDuplicate so deduplication is performed within the tenant only. If global dedupe is required, explicitly run it under an admin-only audited flow and document that boundary.
- **server/auto-memorize.ts:62** — getWatermark()/setWatermark query agent_knowledge rows with no tenant_id constraint. The watermark writes include tenant_id=1 but reads/updates use no tenant scope, causing potential cross-tenant reads/updates.
  - _Fix:_ Include tenant_id in both SELECT and UPDATE/INSERT paths (e.g. WHERE tenant_id = ${tenantId} AND title = ...) and surface tenantId into these helper functions. If a single global/admin watermark is desired, make it explicit and protect its use with admin-only checks and audit logging.
- **server/auto-asset-capture.ts:184** — backfillProjectAssets runs a SELECT DISTINCT on conversations (and correlated messages count) without filtering by tenant_id — this scans conversation rows across tenants.
  - _Fix:_ Limit the initial scan to a specific tenant (WHERE tenant_id = ${tenantId}) or enumerate tenants and run the backfill per-tenant so all downstream processing is tenant-scoped.
- **server/auto-asset-capture.ts:207** — In backfillProjectAssets the query selecting messages for a conversation uses only conversation_id and no tenant_id filter. Reading messages without tenant scoping can expose cross-tenant data if conversation IDs are not globally unique or are abused.
  - _Fix:_ Add tenant_id to the messages WHERE clause (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}) or join messages via conversations with tenant_id enforced to ensure ownership before reading message contents.
- **server/auth.ts:1127** — loadSessionsFromDb executes SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions with no tenant filter, loading sessions for all tenants into process memory. This reads a tenant-scoped table across tenants.
  - _Fix:_ If the intent is to warm a global session cache for all tenants, make this explicit and restrict access to admin processes only. Otherwise, limit the load to the tenant the process is operating under (WHERE tenant_id = ${tenantId}) or provide an admin-only endpoint to perform a full load under strict controls.
- **server/auto-transcript.ts:128** — SELECT uses a subquery on 'messages' without an explicit tenant filter: `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`. If 'messages' is tenant-scoped this read is not constrained by tenant_id.
  - _Fix:_ Either add tenant scoping to the subquery (e.g. `... WHERE conversation_id = c.id AND tenant_id = c.tenant_id`) or JOIN messages through the conversation/project rows that are already tenant-scoped so the DB access is provably limited to the expected tenant. Always ensure every access to a tenant-scoped table includes tenant_id or is guarded by a prior ownership assertion.
- **server/auto-transcript.ts:153** — Query selects rows via `... AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0` without scoping that subquery to tenant_id. If 'messages' is tenant-scoped this read lacks tenant constraint.
  - _Fix:_ Scope the subquery to the tenant (for example: `AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND /*appropriate tenant filter*/) > 0`), or rewrite to join messages via the tenant-scoped conversation/project rows so the messages access cannot cross tenants.
- **server/channel-routing.ts:53** — setChannelRoute(channel, personaId) inserts/updates channel_routes with a persona_id supplied by the caller without validating that the persona belongs to the caller's tenant. This binds a global routing record to an arbitrary persona id and can be used to reference or expose another tenant's persona.
  - _Fix:_ Require and validate tenant context for setChannelRoute. Either (a) make channel_routes tenant-scoped (add tenant_id column) and set it on INSERT, or (b) accept caller tenantId and validate the provided personaId belongs to that tenant before inserting (e.g. `SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}` and reject if not found). Do not accept raw persona ids from untrusted callers without an ownership check.
- **server/chat-engine.ts:571** — SELECT from project_files omits tenant scoping — query: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...` (no `tenant_id` constraint).
  - _Fix:_ Constrain reads against tenant-scoped tables explicitly. Either add `AND tenant_id = ${tenantId}` to the WHERE clause or join to the projects table and assert `projects.tenant_id = ${tenantId}` in the same query. Do not rely on prior verification comments alone — enforce tenant_id at the SQL level (or use Drizzle `.where(eq(project_files.tenantId, tenantId))`).
- **server/chat-engine.ts:582** — SELECT from project_notes omits tenant scoping — query: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...` (no `tenant_id` constraint).
  - _Fix:_ Add an explicit tenant filter. Use `AND tenant_id = ${tenantId}` or join to projects with `projects.tenant_id = ${tenantId}`, or use Drizzle `.where(eq(project_notes.tenantId, tenantId))` so every read is explicitly limited to the caller's tenant.
- **server/chat-engine.ts:631** — SELECT from compaction_archives omits tenant scoping — query: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...` (no `tenant_id` constraint).
  - _Fix:_ Even when the conversation row `c` was fetched under tenant scope, make archival reads explicitly tenant-scoped to avoid implicit IDOR surfaces. Add `AND tenant_id = ${tenantId}` to the WHERE or join via conversation→tenant and enforce `compaction_archives.tenant_id = ${tenantId}` (or use Drizzle `where(eq(...))`).
- **server/chat-engine.ts:649** — SELECT from messages omits tenant scoping — query: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...` (no `tenant_id` constraint).
  - _Fix:_ Constrain message reads by tenant explicitly. Either add `AND tenant_id = ${tenantId}` to the WHERE or join through the conversation row with a tenant check. Prefer explicit `tenant_id` conditions (or Drizzle `.where(eq(messages.tenantId, tenantId))`) rather than relying on contextual guarantees.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table `credential_vault` without tenant_id constraint. `getDecryptedPassword()` updates last_used_at using `WHERE id = ${id}` only (no tenant_id), even though earlier code fetched and validated the row.
  - _Fix:_ Constrain the UPDATE by tenant as well to follow the invariant: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}`. Alternatively, use the earlier validated credential object to drive the update via an explicit tenant-safe API (or pass the tenantId into the query). Always include tenant_id in WHERE clauses to avoid accidental cross-tenant writes if code paths change.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table `credential_vault` without tenant_id constraint. `getLoginCredentials()` updates last_used_at with `WHERE id = ${cred.id}` but omits tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant_id: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}` (use the tenantId that was used to fetch the credential). Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant filters even when the row was previously retrieved/validated.
- **server/crews-engine.ts:883** — addFlowStep inserts an optional crewId into flow_steps without validating that the referenced crew belongs to the caller's tenant. The function verifies the flow belongs to the tenant but does not check ownership of params.crewId before storing it.
  - _Fix:_ If params.crewId is present, verify ownership before inserting (e.g. const crew = await getCrew(params.crewId, params.tenantId); if (!crew) return error). Alternatively add an explicit WHERE tenant_id check at execution time and/or reject cross-tenant crew references at creation time.
- **server/doc-collections.ts:165** — DELETE FROM `doc_chunks` executed without tenant_id constraint: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}.
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}) or otherwise ensure the collection_id is validated to belong to the tenant (and still include tenant_id in the WHERE clause to satisfy the tenant-isolation invariant). Pass tenantId into the function and use it in the query.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from request/provider-derived embedding: 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}. This interpolates a runtime/generated value into raw SQL.
  - _Fix:_ Prefer a parameterized binding mechanism for the vector (avoid string interpolation). If embedding-to-SQL interpolation is necessary, validate/normalize the shape strictly (vecLiteral exists) and document why safe; better: bind the vector as a parameter or construct the query with a DB-supported parameterized representation to avoid any chance of SQL-injection or unintended interpolation.
- **server/firecrawl.ts:386** — Use of sql`...` template including options.search (request-derived value) in WHERE conditions: sql`(${scrapedPages.content} ILIKE ${'%' + options.search + '%'} OR ${scrapedPages.title} ILIKE ${'%' + options.search + '%'})`. This is string-interpolated SQL with request-derived input (injection/isolation risk per project policy).
  - _Fix:_ Avoid building SQL fragments with request-derived strings. Use Drizzle query builder helpers (e.g. ilike/eq) or ensure proper parameterization and explicit tenant scoping. If a raw/sql template is required, validate/sanitize the input and keep tenant constraints explicit (eq(scrapedPages.tenantId, tenantId)).
- **server/firecrawl.ts:459** — Use of sql`...` with request-derived options.pageIds: sql`${scrapedPages.id} = ANY(${options.pageIds})`. This composes SQL with caller-supplied data and can lead to injection or isolation issues.
  - _Fix:_ Use Drizzle's query builder constructs (for example where(in(scrapedPages.id, options.pageIds))) or otherwise parameterize and validate pageIds before use. Always include the tenant constraint (eq(scrapedPages.tenantId, tenantId)) in the delete's WHERE clause.
- **server/firecrawl.ts:467** — Use of sql`...` with request-derived value 'days' in time arithmetic: sql`${scrapedPages.scrapedAt} < NOW() - make_interval(days => ${days})`. Although days is validated earlier, this is still an interpolated SQL fragment using a caller-influenced value.
  - _Fix:_ Prefer expressing the time cutoff using a parameterized value (e.g. compute cutoff = new Date(Date.now() - days*24*60*60*1000) and use eq/lt with that parameter), or ensure strict validation of 'days' and always combine with tenant constraint in the WHERE clause.
- **server/google-drive.ts:1019** — searchDriveFiles uses db.execute(sql`...`) with interpolated, request-derived pattern (LOWER(pf.file_name) LIKE ${'%' + pattern + '%'}). This is string-interpolated SQL with caller-controlled input (injection/isolation risk) even though a tenant filter (p.tenant_id = ${params.tenantId}) is present.
  - _Fix:_ Prefer Drizzle query-builder APIs (where/ilike) to build this query with proper parameterization, or ensure strong sanitization of the pattern. Keep the tenant filter explicit (as already present) and avoid constructing SQL fragments with untrusted input.
- **server/guarded-tool-executor.ts:83** — executeGuardedTool accepts tenant identity from untrusted caller args (uses ctx.tenantId || args._tenantId). Allowing args._tenantId (caller-provided) to determine tenant context can let an untrusted caller cause tools to run under another tenant's context, leading to cross-tenant actions if downstream tools use that value in DB queries.
  - _Fix:_ Do not accept tenant identity from untrusted tool args. Require tenantId to come from the trusted execution context (ctx.tenantId) or an explicitly-authorized system caller. Remove fallback to args._tenantId (or validate it against authenticated caller permissions) and ensure any tool execution that needs cross-tenant privilege requires explicit admin authorization and audit logging.
- **server/hooks.ts:166** — Hook handler 'skill-seeker-gap-detector' reads tenantId from event.context (event.context?.tenantId || event.context?.tenant_id) and calls detectGap(..., personaId, tenantId, ... ) without validating that the caller is authorized for that tenant or verifying ownership of personaId. A maliciously-crafted event payload could cause writes under an arbitrary tenant.
  - _Fix:_ Do not trust tenantId/personaId from event.context. Validate/resolve tenant context via server-auth/session or assertTenantContext before passing tenantId into storage-modifying functions. Verify the personaId belongs to that tenant (e.g. fetch persona by id and check persona.tenantId === tenantId) or restrict hook emission to trusted internal code paths only.
- **server/hooks.ts:184** — Hook handler 'user-model-deriver' uses tenantId extracted from event.context and calls deriveObservations(tenantId, ... ) without verifying the tenant or the origin of the event context. This allows potential cross-tenant writes if the hook is invoked with a forged tenantId.
  - _Fix:_ Validate the tenant context before using tenantId from event.context (use authenticated session info, assertTenantContext, or resolve tenant from a trusted source). Ensure deriveObservations verifies ownership of any referenced entities and that the hook only runs for authenticated/trusted emitters.
- **server/hooks.ts:202** — Hook handler 'knowledge-nudge-detector' takes tenantId from event.context and calls processNudge(tenantId, ... , event.context?.conversationId) without validating that the tenant actually owns the conversationId or that the tenantId is trusted.
  - _Fix:_ Validate tenantId and conversationId before calling into storage — either resolve tenantId from a trusted session/auth source or assert ownership by loading the conversation and checking its tenant_id matches. Avoid trusting event.context-provided tenant ids for DB writes.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a column name derived from caller input (outcome → column) in a dynamic UPDATE. Although the column is validated against an allowlist, sql.raw() is used to interpolate an identifier from request-derived data, creating risk if validation is bypassed or changed.
  - _Fix:_ Avoid injecting identifiers into SQL at runtime. Implement separate, fixed SQL update branches for each outcome (e.g. switch-case that issues a parameterized UPDATE per column), or use a safe identifier-quoting helper from your DB library. Keep the allowlist but remove sql.raw() usage so only fixed, parameterized statements run.
- **server/lib/jury-experience.ts:175** — String-interpolated vector literal built from an embedding array derived from request data is inserted into SQL via sql`${`[${embedding.join(",")}]`}::vector`. This constructs a SQL fragment from request-derived values rather than binding parameters.
  - _Fix:_ Validate that every embedding element is a finite number and then use a safe, parameterized approach to insert vectors. If the DB client/drizzle doesn't support direct vector parameter binding, sanitize and canonicalize the numeric array (Number() each element, ensure finite) before constructing the literal, or store embeddings as JSONB and convert on the DB side; document and centralize the vector-serialization logic to minimize risk.
- **server/lib/paper-ingest.ts:658** — Construction of a vector literal (`vecLiteral = [${row.embedding.join(",")} ]`) from embedding values (derived from input text) and interpolation into the INSERT as `sql`${vecLiteral}::vector``. This builds SQL from runtime data instead of binding parameters.
  - _Fix:_ Sanitize and assert that each embedding element is a finite number before use. Prefer a parameterized binding method for vectors (or store the vector as JSONB and convert/cast safely in SQL), and avoid direct string interpolation into SQL. Centralize vector handling and perform strict validation on embeddings prior to any SQL interpolation.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only eq(id, existing.id) in WHERE clause (missing tenant_id constraint)
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) so writes are explicitly scoped to the caller's tenant. This prevents accidental cross-tenant updates if row ids collide or are misused.
- **server/minds-engine.ts:214** — INSERT into mind_tickets constructs a Postgres array literal from request-derived params.dependsOn via string interpolation: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. This is string-interpolated SQL with request-derived data (injection/isolation risk).
  - _Fix:_ Use proper parameterized array binding instead of building a literal string. For example, pass dependsOn as a bound array parameter (WHERE supported) or use Drizzle/pg param binding helpers (e.g. ANY($1::int[])) or serialize via safe parameterization. Validate/normalize elements before binding.
- **server/minds-engine.ts:325** — markEventsHandled builds an array literal from request-derived eventIds using string interpolation: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. This is string-interpolated SQL with request-derived values (injection risk).
  - _Fix:_ Avoid constructing SQL array literals by concatenation. Use parameterized queries / properly typed array parameters (e.g. WHERE id = ANY(${eventIds}::int[]) with a bound array, or update each id in a parameterized batch), ensuring eventIds are validated and passed as bind parameters rather than injected text.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw() with `lockKey` (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)). `lockKey` is derived from ledgerKey/tenantId (caller-controlled), so using sql.raw with a request-derived value presents SQL injection / raw-sql risk.
  - _Fix:_ Avoid sql.raw() with values derived from user-supplied input. Use parameterized queries or cast the value safely via a parameter (e.g. pass the bigint as a normal placeholder) or validate/sanitize the value strictly (ensure it's a numeric bigint) before embedding. Prefer tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`) using a parameter rather than raw SQL interpolation.
- **server/proactive-engine.ts:138** — SELECT persona_id FROM proactive_actions WHERE id = ${actionId} — no tenant_id constraint. Reading a tenant-scoped row by id without tenant scoping can leak or return rows belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${actionId} AND tenant_id = ${tenantId}) when selecting from proactive_actions. Always verify the tenant before returning or acting on tenant-scoped rows.
- **server/routes.ts:1602** — Public /api/public/stats issues SELECTs against tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filter, exposing aggregated cross-tenant counts without tenant scoping.
  - _Fix:_ If global aggregation is intentional, gate the endpoint to admins or sanitize/aggregate via an audited metrics pipeline. Otherwise, add tenant_id filtering (WHERE tenant_id = ${tenantId}) or compute per-tenant stats only after verifying caller's tenant context.
- **server/routes.ts:1630** — Public /api/public/architecture uses many db.execute SELECTs over tenant-scoped tables (conversations, messages, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, etc.) with no tenant_id constraint.
  - _Fix:_ Restrict these queries to a trusted/admin-only context or add tenant_id filtering where appropriate. Prefer building aggregated metrics from a controlled reporting pipeline rather than ad-hoc unscoped queries on tenant data.
- **server/routes.ts:769** — backfillProjectDriveFolders runs a SELECT joining projects (which is tenant-scoped) to tenants without scoping to a caller tenant — it scans all projects across tenants.
  - _Fix:_ Run this backfill only from an explicit admin/maintenance context that is documented and access-controlled, or iterate per-tenant and include WHERE p.tenant_id = <tenantId>. Avoid broad unscoped reads of tenant-scoped tables in normal runtime paths.
- **server/routes.ts:652** — cleanupTestTenants executes a SELECT against tenants that includes subqueries SELECT DISTINCT tenant_id FROM conversations and projects without scoping those subqueries by a verified tenant context, reading tenant_id values across all tenants.
  - _Fix:_ Rewrite the existence checks to avoid scanning tenant-scoped tables unscoped (for example iterate candidate tenants and verify via scoped checks per tenant), or ensure this runs only under an explicit, audited admin maintenance context.
- **server/routes.ts:2385** — Public order lookup /api/store/order/:sessionId selects from delivery_logs by orderId/stripePaymentId with no tenant_id constraint, returning rows from delivery_logs across tenants based solely on the session id.
  - _Fix:_ Treat delivery session IDs as true capability tokens (documented) or additionally verify tenant ownership before returning rows. Prefer scoping reads by tenant_id when the caller has an authenticated tenant, or ensure the unscoped capability path is safe, logged, and limited to unguessable tokens only.
- **server/routes.ts:2493** — Async handler for /api/store/lookup-orders performs a SELECT FROM delivery_logs WHERE lower(customer_email) = ... with no tenant_id filter (searches delivery logs across all tenants by email).
  - _Fix:_ Avoid unscoped searches across tenant-scoped delivery_logs. If this behavior is required, ensure the operation runs in a tightly controlled back-end workflow with audit logging and rate limits, or restrict results to the tenant context. Consider creating a separate, privacy-safe index/table for public recovery lookups that cannot expose cross-tenant data.
- **server/routes.ts:2637** — /api/store/verify-orders performs a SELECT FROM delivery_logs WHERE lower(customer_email) = ... (and later returns matched orders) without constraining by tenant_id.
  - _Fix:_ Scope queries to tenant ownership or otherwise ensure the verification flow cannot leak cross-tenant order data. Prefer returning only capability-backed orders (unguessable session IDs) or restrict the database lookup to rows intended to be discoverable by that email in a privacy-preserving way.
- **server/routes/conversations.ts:68** — SELECT from `personas` by id omits tenant scoping (no WHERE tenant_id = ...). Reading a tenant-scoped table without tenant filter can leak cross-tenant data.
  - _Fix:_ Include tenant constraint when selecting persona data (e.g. JOIN or WHERE id = ${activePersona.id} AND tenant_id = ${tenantId}), or resolve the persona via a tenant-scoped storage API that enforces ownership.
- **server/seed.ts:3136** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL lacks a tenant_id constraint. If projects is tenant-scoped this may update another tenant's project.
  - _Fix:_ Restrict updates on tenant-scoped tables with tenant_id (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND primary_conversation_id IS NULL). If this is intended only for an owner/development deployment, gate it explicitly behind a verified owner check.
- **server/seed.ts:3212** — INSERT into project_conversations is derived from rows selected without a tenant filter. The inserted pairs (project_id, conversation_id) may link rows from different tenants because the source SELECT lacked tenant scoping.
  - _Fix:_ Only insert links after verifying both project_id and conversation_id belong to the same tenant. Prefer selecting rows already filtered by tenant_id, or include tenant information in the join and enforce it before INSERT.
- **server/storage.ts:286** — attachCitationMetadata() executes raw SELECT FROM messages WHERE id IN (...) without filtering by tenant_id.
  - _Fix:_ Include tenant scoping on the query (e.g. AND tenant_id = ${tenantId} or JOIN back to a tenant-scoped source) or pass the tenantId into attachCitationMetadata and use eq(messages.tenantId, tenantId) to constrain the lookup.
- **server/storage.ts:837** — getMemoryStats() will SELECT memory_entries and agent_knowledge without tenant filters if tenantId is undefined, returning cross-tenant aggregates.
  - _Fix:_ Require tenantId for stats or clearly document/admin-gate global stats. Prefer failing closed (throw) when tenantId is missing for tenant-scoped aggregates, or always include eq(...tenant_id, tenantId) when tenantId provided.
- **server/storage.ts:879** — getHeartbeatTasks() when tenantId is undefined returns all heartbeat_tasks across tenants (no tenant filter by default).
  - _Fix:_ Require tenantId in API handlers or add a failsafe to require tenant scoping for non-admin callers. If a global worker needs to read all tasks, ensure it runs with a clearly-audited admin identity and document the difference.
- **server/storage.ts:897** — getHeartbeatTask(id) SELECTs heartbeat_tasks WHERE id = ${id} with no tenant constraint (heartbeat_tasks is tenant-scoped) — returns task for any tenant.
  - _Fix:_ Require tenantId or verify ownership before returning task rows: add AND heartbeat_tasks.tenant_id = ${tenantId} (or use tenantScope()) to the WHERE.
- **server/storage.ts:950** — getDueHeartbeatTasks() SELECTs due heartbeat_tasks without tenant filtering — may return tasks for all tenants in one call.
  - _Fix:_ If this is an internal scheduler that must process tasks across tenants, ensure the caller is admin/trusted and add explicit auditing. Otherwise scope the read by tenant (or, for workers, claim tasks only after verifying tenant ownership and using a tenant-aware update).
- **server/storage.ts:964** — claimHeartbeatTasks() updates heartbeat_tasks SET last_run_at/next_run_at WHERE id = ${id} AND ... with no tenant constraint — can claim tasks across tenants without checking tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or ensure the worker claiming tasks has appropriate admin authority and that task ids are validated against tenant ownership before update.
- **server/storage.ts:979** — markHeartbeatTaskRun() updates heartbeat_tasks WHERE id = ${id} with no tenant constraint.
  - _Fix:_ Require tenantId or validate task ownership first (add AND tenant_id = ${tenantId} to the WHERE) to avoid updating tasks belonging to other tenants.
- **server/video-job-runner.ts:204** — isCancelRequested reads videoJobs.cancelRequested by jobId only (SELECT ... WHERE jobId = ...), without constraining by tenant_id. This allows discovering cancel flags for jobs across tenants or reacting to cancel state from another tenant's job.
  - _Fix:_ Scope the query by tenant_id. Either accept tenantId as a parameter or read and verify tenant ownership first, e.g. SELECT cancel_requested FROM video_jobs WHERE job_id = ${jobId} AND tenant_id = ${tenantId} LIMIT 1. Ensure any control/decision based on DB state verifies the job belongs to the same tenant.

### LOW (1)

- **server/auto-consolidation.ts:335** — checkAllTenants SELECT DISTINCT tenant_id FROM memory_entries reads memory_entries across all tenants (no tenant filter) to discover active tenants. This is a global scan of a tenant-scoped table.
  - _Fix:_ If enumerating tenants is necessary, consider using a separate tenancy registry table for tenant discovery, or ensure this scan is performed in an audited admin context and minimize returned columns. Alternatively iterate known tenant ids from a tenant registry rather than scanning tenant-scoped data tables.
