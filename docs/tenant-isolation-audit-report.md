# Tenant-Isolation Audit Report

_Generated 2026-09-04T00:39:16.201Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 472 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **189/661** file(s) audited · chunks 20/61 OK (0 failed, 41 pending)
- Findings: **84** (CRITICAL 22 · HIGH 46 · MEDIUM 16 · LOW 0)
- Severe dispositions: **68 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (22)

- **server/agentic/wake-scheduler.ts:142** — Query claims due wake rows without constraining by tenant_id. The UPDATE/SELECT block that claims pending wakes (inner SELECT on agent_wake_schedules) has no tenant_id filter, so it can select and mutate rows belonging to any tenant.
  - _Fix:_ Add tenant scoping to the claim query. Either iterate per-tenant (select tenant ids then run the claim UPDATE with WHERE tenant_id = ${tenantId}) or include a tenant_id predicate in both the inner SELECT and outer UPDATE (e.g. WHERE tenant_id = ${tenantId} in the subselect and UPDATE). Ensure the claim is performed inside the intended tenant context so wakes for other tenants cannot be claimed.
- **server/chat-engine.ts:1557** — buildMemorySection declares tenantId with a default value of 1 (tenantId: number = 1). This can silently attribute reads/writes/analytics to the admin/default tenant when callers forget to pass the real caller tenant, violating tenant isolation and enabling cross-tenant data leakage or incorrect writes (e.g. recordHydeAttempt/recordHydeComparisonFailure/recordHydeShadow/proactiveContextLoad and any DB writes inside this function will use tenantId=1).
  - _Fix:_ Remove the unsafe default and make tenantId an explicit required parameter (e.g. tenantId: number). Add a fail-closed runtime check that throws if tenantId is missing/invalid (not a positive integer). Audit all callers of buildMemorySection to ensure they pass the caller's tenantId, and add unit tests to prevent regression. Optionally, replace any internal writes to use storage accessors that validate tenant ownership server-side.
- **server/commitments.ts:135** — SELECT ... FROM commitments WHERE status='active' AND due_at < NOW() ... is executed without a tenant_id constraint — this reads tenant-scoped commitment rows across all tenants.
  - _Fix:_ Scope the scan per-tenant (enumerate tenant IDs and run the SELECT/processing under that tenant), or add an explicit tenant_id predicate. If global cross-tenant visibility is required, restrict to a trusted/admin path and document/limit the fields returned.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks (in addDocument) omits tenant_id in the WHERE clause: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}. This can delete chunks across tenants.
  - _Fix:_ Always include tenant scoping on DELETEs (AND tenant_id = ${tenantId}). Narrow the DELETE to the caller's tenant to prevent affecting other tenants' rows.
- **server/email.ts:429** — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} performs a write to a tenant-scoped table without constraining by tenant_id — may modify another tenant's row by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update only after verifying the record belongs to the intended tenant.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} updates a tenant-scoped table by id only, with NO tenant_id constraint — allows cross-tenant modifications.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id || tenantId}) or only perform the update after verifying the tenant ownership of the row.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} updates a tenant-scoped table without constraining by tenant_id — can modify other tenants' data.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id || tenantId}) or ensure the backfill uses a tenant-local query + update pattern.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to an unconstrained SQL path: UPDATE ${sql.raw(table)} ... WHERE id = ${id} (no tenant_id) when no tenantId is supplied — this performs writes to tenant-scoped tables without tenant scoping.
  - _Fix:_ Disallow or remove the unconstrained branch. Require a positive tenantId for updates to tenant-scoped tables, or explicitly route admin/global operations through a validated, audited path that includes tenant context.
- **server/google-drive.ts:1665** — INSERT into project_files omits tenant_id — the INSERT INTO project_files (...) VALUES (...) statement does not include a tenant_id column/value. Per invariant, every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Add an explicit tenant_id column/value to the INSERT (and ensure the tenant was verified): INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, ...) VALUES (${params.projectId}, ${resolvedTenantId}, ${params.fileName}, ${result.shareableLink}, ...). Also ensure resolvedTenantId was validated (assertProjectInTenant) before inserting.
- **server/google-workspace.ts:2307** — UPDATE on presenter_sessions omits a tenant_id constraint (UPDATE presenter_sessions SET ... WHERE id = ${sessionId}). This can mutate sessions belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant: add AND tenant_id = ${tenantId} (or otherwise validate session ownership) so the update can only affect rows for the calling tenant. e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}.
- **server/health-audit.ts:333** — UPDATE on code_proposals (SET status = 'archived_stale' WHERE id = ANY(...)) lacks any tenant_id filtering and will modify proposals across tenants.
  - _Fix:_ Restrict the UPDATE to the owning tenant(s). Include tenant_id in the WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or verify each id's tenant before updating. Ensure admin/global operations are explicitly authorized and safe.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a write without tenant_id restriction and can affect tasks for other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or validate ownership per id before disabling tasks. Ensure any cross-tenant administrative writes are explicitly authorized.
- **server/job-queue.ts:99** — claimDueJobs performs UPDATE/SELECT on agent_jobs without any tenant_id constraint — it can claim and mutate jobs across tenants.
  - _Fix:_ Restrict the claim to an explicit tenant scope (e.g. WHERE tenant_id = ${tenantId}) or require/validate an explicit admin/all-tenants authorization boundary. If the worker must process multi-tenant work, make that explicit and auditable (e.g. a separate admin-mode code path) and include tenant_id in the UPDATE CAS to prevent accidental cross-tenant mutation.
- **server/job-queue.ts:135** — reclaimExpiredLeases step 1 UPDATE (marking failed_terminal) mutates agent_jobs without a tenant_id predicate — can dead-letter other tenants' jobs.
  - _Fix:_ Include tenant_id in the WHERE clause or otherwise scope the reclaim to the intended tenant. If reclaim is intended to operate across tenants, make the admin intent explicit and protect the call behind an admin-only boundary with logging/audit.
- **server/job-queue.ts:149** — reclaimExpiredLeases step 2 UPDATE (requeue to pending) updates agent_jobs without any tenant_id constraint, re-queueing jobs across tenants.
  - _Fix:_ Add tenant_id scoping to the UPDATE, or require explicit authorized admin scope for cross-tenant requeue operations. Ensure reclaimed rows are fenced by tenant when updating next_run_at/status.
- **server/job-queue.ts:178** — completeJob updates agent_jobs (marking succeeded) by id/attempts but does not constrain by tenant_id — a stale/compromised caller could complete another tenant's job.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise verify the caller's tenant owns the job before marking it succeeded. Keep lease-fencing semantics but add tenant-based fencing in the DB predicate.
- **server/job-queue.ts:221** — failJob reads max_attempts via SELECT FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — can leak or act on another tenant's job metadata.
  - _Fix:_ Scope the SELECT by tenant_id or require the caller to supply/verify the job's tenant prior to this lookup. Prefer SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} to avoid cross-tenant reads.
- **server/job-queue.ts:238** — failJob terminal UPDATE (marking failed_terminal) updates agent_jobs without tenant_id in the WHERE clause — allows failing other tenants' jobs.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE predicate to ensure only the owning tenant's job can be transitioned. If tenantId is unknown at this call site, fetch and validate it first with a tenant-scoped lookup.
- **server/job-queue.ts:258** — failJob retry-path UPDATE (re-schedule pending with next_run_at) writes agent_jobs without tenant_id scoping — can requeue jobs across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE predicate or verify job ownership before mutating. Ensure retry/backoff changes are applied only to the correct tenant's job.
- **server/job-queue.ts:275** — cancelJob performs UPDATE agent_jobs SET status='cancelled' WHERE id = ${id} ... with no tenant_id constraint — an operator-facing call could cancel jobs belonging to other tenants.
  - _Fix:_ Require tenant_id scoping in the WHERE clause (AND tenant_id = ${tenantId}) or make cancelJob an admin-only API that first resolves and verifies the job's tenant before mutating. Always enforce tenant ownership at the DB predicate.
- **server/job-worker.ts:146** — research_code_proposal handler reconstructs a session object using tenantId from job.payload (untrusted) rather than using the job row's tenant. Passing payload.tenantId into downstream code (generateCodeProposal) risks cross-tenant reads/writes if the payload's tenantId differs from the actual job.owner.
  - _Fix:_ Do NOT trust tenantId from the job payload. Use the job.tenantId (the persisted job scope) as the canonical tenant for any downstream operations or validate payload.tenantId === job.tenantId before using it. Prefer dropping tenantId from the payload entirely and sourcing the tenant from the claimed job row.
- **server/job-worker.ts:167** — research_digest handler reads tenantId from job.payload and passes it to generateResearchDigest without validating ownership. A crafted job payload could cause the worker to run digest generation for another tenant.
  - _Fix:_ Validate that payload.tenantId equals job.tenantId (or always use job.tenantId) before calling tenant-scoped functions. Ensure handlers never perform tenant-scoped reads/writes using untrusted payload-supplied tenant identifiers.

### HIGH (46)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects from tenant-scoped `experiments` table without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This returns rows across tenants and later acts on row.tenant_id, allowing cross-tenant reads/writes to be performed based on unfiltered results.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId} or accept a tenant-scoped parameter) or explicitly iterate tenants and run the query per-tenant. Never issue global SELECTs against tenant-scoped tables unless intentionally and safely designed as a multi-tenant sweep with explicit safeguards and auditing.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting issues a SELECT from tenant-scoped `ai_insights` without a tenant_id filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ...). This reads rows across all tenants.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) or change the function to accept/iterate over tenants and perform per-tenant, audited processing. Ensure any downstream routing (routeInsightToMinerva) uses only validated tenant ids.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights executes an UPDATE on tenant-scoped `ai_insights` and when called without a tenantId it omits tenant scoping (tenantScope becomes empty), causing cross-tenant updates (UPDATE ai_insights SET ... WHERE status = 'new' AND category IN (...) <no tenant constraint>).
  - _Fix:_ Require an explicit tenantId when doing writes, or if a global sweep is intended, iterate tenants explicitly and perform tenant-scoped UPDATEs for each. Never run non-tenant-scoped UPDATEs/DELETEs on tenant-scoped tables; add a runtime guard that refuses to run without a tenant argument or logs and requires explicit admin confirmation.
- **server/agentic-features.ts:861** — enrollInSequence runs SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' with NO tenant_id constraint. This reads a tenant-scoped table without scoping to the caller's tenant and can leak or conflate enrollments across tenants.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the SELECT so the existence check is performed under the caller's tenant. Always include tenant_id in WHERE for tenant-scoped tables.
- **server/agentic-features.ts:1020** — classifyReply updates outreach_enrollments with UPDATE ... WHERE id = ${row.id} but omits tenant_id qualification. An attacker or race could cause a tenant to update another tenant's enrollment row by id.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${params.tenantId} (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) so the update is only applied to rows owned by the calling tenant. Always include tenant scoping on UPDATE/DELETE statements for tenant-scoped tables.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on tenant-scoped `agent_approvals` without a tenant_id constraint (UPDATE agentApprovals SET status = 'expired' WHERE status = 'pending' AND expiresAt < NOW()). This will expire approvals across all tenants in one un-scoped operation.
  - _Fix:_ Either require a tenantId argument and scope the UPDATE (WHERE tenant_id = ${tenantId} AND ...), or iterate per-tenant and perform tenant-scoped updates. Avoid global, un-scoped UPDATEs/DELETEs on tenant-scoped tables; add a safety check to prevent accidental cross-tenant writes.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table with NO tenant_id filter — this performs an unrestricted cross-tenant read of department_budgets.
  - _Fix:_ If this query is supposed to operate per-tenant, add an explicit tenant_id filter (e.g. WHERE tenant_id = ${tenantId}). If it must sweep all tenants (admin job), move it to a clearly privileged/admin-only path and ensure it runs under an audited admin service account (and not in code paths that assume a single tenant context). Document the privileged intent and restrict who/what can call this function; avoid performing cross-tenant reads from unprivileged runtime contexts.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT on tenant-scoped tables (agent_runs / agent_approvals). The db.execute(...) query at this location selects from agent_runs (left-joins agent_approvals) with no WHERE tenant_id = <tenant> filter, allowing cross-tenant reads of tenant-scoped rows.
  - _Fix:_ Constrain the query to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId}) or otherwise iterate per-tenant. If this worker legitimately needs to scan across tenants, implement an explicit, audited tenant loop or use an authorized system context that records/limits cross-tenant access. Never read tenant-scoped tables without an explicit tenant_id predicate matching the caller/context.
- **server/auto-asset-capture.ts:145** — INSERT into project_files omits tenant_id. project_files is project-scoped; without an explicit tenant_id column/value the row is not explicitly tied to the acting tenant (there is no DB-enforced FK/row-level-security).
  - _Fix:_ Include tenant_id explicitly in the INSERT (and make project_files a tenant-scoped table with tenant_id NOT NULL), or otherwise ensure the row contains a trusted tenant identifier (e.g. the verified project owner's tenantId). For example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${projectId}, ...). Do not rely solely on transitive ownership.
- **server/auto-asset-capture.ts:164** — INSERT into project_notes omits tenant_id. project_notes is project-scoped but the INSERT does not set tenant_id, relying on transitive ownership rather than an explicit tenant column.
  - _Fix:_ Persist the tenant_id in project_notes as well (tenant_id NOT NULL) and include it in the INSERT (VALUES (${tenantId}, ${projectId}, ...)), or otherwise ensure the insert is guarded by an explicit tenant-scoped constraint at the application level. Avoid inserting tenant-scoped rows without an explicit tenant column value.
- **server/auto-asset-capture.ts:184** — Backfill query scans conversations across all tenants: SELECT DISTINCT c.id, c.project_id, c.tenant_id ... has no WHERE tenant_id = <tenant> constraint. This reads tenant-scoped tables globally without per-tenant guard.
  - _Fix:_ Either restrict the backfill to a specific tenant (add WHERE c.tenant_id = ${tenantId}) or require/verify platform-admin authorization and make the cross-tenant intent explicit. Prefer iterating tenants and running tenant-scoped queries (or otherwise ensure the job runs under an explicitly authorized admin context and is auditable).
- **server/auto-asset-capture.ts:207** — Query reading messages for a conversation omits tenant scoping: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' — no tenant_id predicate. This reads tenant-scoped rows without verifying tenant ownership in the same query.
  - _Fix:_ Add tenant filtering to the messages query (e.g. AND tenant_id = ${c.tenant_id}), or join messages to conversations with a tenant_id check. Even if subsequent processing verifies ownership, every read against tenant-scoped tables should include an explicit tenant predicate to avoid cross-tenant reads.
- **server/auth.ts:196** — Lookup of API key selects from api_keys without a tenant_id constraint: db.select().from(apiKeys).where(eq(apiKeys.keyHash, keyHash), eq(apiKeys.isRevoked, false)). This is a SELECT on a tenant-scoped table with no tenant filter.
  - _Fix:_ If api keys are tenant-scoped, include tenant scoping in lookups when possible. If lookups must find a key first, ensure any subsequent mutations/uses include an explicit tenant check and/or encode the tenant into the credential (or validate the key in a global table designed for that purpose). At minimum, after retrieving the key validate its tenant_id and use that tenantId for all subsequent operations.
- **server/auth.ts:306** — Session lookup selects from auth_sessions by token only without a tenant_id constraint: SELECT tenant_id, is_admin, expires_at FROM auth_sessions WHERE token = ${tokenHash}. This is a SELECT against a tenant-scoped table without a tenant filter.
  - _Fix:_ Consider encoding the tenant id in the token or otherwise require callers to provide tenant context so the SELECT can include WHERE tenant_id = ${tenantId}. If lookup-by-token must remain global, ensure the code path is strictly hardened and audited (and follow with explicit checks that the resolved tenant is active); prefer a design where the token format includes the tenant or where the caller's tenant is canonical before performing tenant-scoped reads/writes.
- **server/auth.ts:1331** — Startup maintenance DB operations delete auth_sessions across all tenants: DELETE FROM auth_sessions WHERE expires_at < ${now} and DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%'. These are writes against a tenant-scoped table with no tenant_id constraint executed at boot.
  - _Fix:_ Make global cleanup explicit and protect it with platform-admin authorization, or break maintenance into per-tenant cleanup runs (iterate tenants and delete WHERE tenant_id = ${tenantId} AND ...). At minimum, document and gate global deletions and ensure they are executed only by an authorized admin routine.
- **server/auto-transcript.ts:165** — UPDATE against tenant-scoped table 'conversations' is executed without constraining by tenant_id (WHERE clause only checks id and project_id IS NULL). This can allow cross-tenant writes if an id value is mis-bound or comes from an untrusted source.
  - _Fix:_ Include the tenant discriminator in the UPDATE to enforce tenant isolation, e.g. add AND tenant_id = ${c.tenant_id} to the WHERE clause (or JOIN through projects with an explicit tenant check). Alternatively, re-check ownership via assertConversationInTenant/SELECT ... WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} in the same transaction before performing the UPDATE. Use parameterized/drizzle-bound values (as shown) rather than string interpolation.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. Tenant-scoped writes must set tenant_id explicitly to ensure row ownership/isolation.
  - _Fix:_ Add tenant_id to the invoice_items table and include it in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)), or otherwise derive/validate tenant ownership from the parent invoice before inserting. Ensure all tenant-scoped child tables are written with the caller's tenant_id in the same transaction.
- **server/cmmc-assessments.ts:150** — SELECT from tenant-scoped tables (cmmc_assessment_invitations / cmmc_assessments) is performed without constraining the query to a caller-controlled tenant (no tenant_id WHERE filter). getCmmcAccessByToken selects invitation/assessment rows by tokenHash only, which can return rows across tenants.
  - _Fix:_ Enforce a tenant ownership constraint on this lookup. Options: (1) Require the caller to provide an authenticated tenantId and add eq(cmmcAssessmentInvitations.tenantId, tenantId) (and/or eq(cmmcAssessments.tenantId, tenantId)) to the WHERE clause so the DB enforces the tenant fence; or (2) treat the token as the sole bearer credential but perform a two-step guarded lookup inside a transaction that first selects the invitation row by tokenHash, reads its tenantId, then uses that tenantId to scope all further reads/updates in the same transaction — and do not return any cross-tenant data. Avoid relying on implicit joins or assumptions; make the tenant_id explicit in the WHERE clause so tenant isolation cannot be bypassed.
- **server/commitment-drafter.ts:234** — UPDATE commitments ... WHERE id = ${row.id} executes without scoping by tenant_id — a cross-tenant mutation is possible (unclaiming/reverting a row from another tenant).
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenantId}) or otherwise verify ownership before writing. Always bind tenant_id for updates that mutate tenant-scoped tables.
- **server/data-protection.ts:149** — SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} queries conversations across all tenants with no tenant filter — unscoped read of tenant-scoped data.
  - _Fix:_ Run purge/backup sweeps per-tenant (enumerate tenants and operate within tenant scope) or add an explicit tenant predicate. Ensure any cross-tenant maintenance runs are restricted to a privileged admin role and audited.
- **server/data-protection.ts:360** — SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} executes without scoping by tenant_id — project_notes is tenant-scoped and caller-supplied project rows can reference other tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or JOIN project_notes to the projects table to verify project. Validate referenced project_id belongs to the tenant before reading/writing child rows.
- **server/data-protection.ts:371** — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} runs without tenant scoping — can return other tenants' project files.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or join against projects to verify ownership) before selecting project_files; always constrain child-table queries by tenant_id when operating in tenant context.
- **server/data-protection.ts:404** — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} executes without a tenant_id predicate — this is an unscoped read on tenant-scoped messages.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) or otherwise validate conversation ownership before querying message counts. All existence/count queries on tenant-scoped tables must include tenant_id.
- **server/crews-engine.ts:394** — INSERT INTO crew_tasks stores context_task_ids built directly from caller-supplied contextTaskIds without verifying that each referenced task belongs to the caller's tenant — a tenant could reference another tenant's task IDs.
  - _Fix:_ Validate ownership of every contextTaskId before persisting (e.g. SELECT id FROM crew_tasks WHERE id IN (...) AND tenant_id = ${tenantId} and fail if any expected ids are missing). Only write IDs that are confirmed to belong to the caller's tenant, or store canonical references after verification.
- **server/dfy-intake.ts:50** — SELECT from dfy_intake_forms by token (getDfyIntakeByToken) does not constrain by tenant_id. This returns a tenant-scoped row using only the public token and can leak another tenant's intake record/metadata.
  - _Fix:_ Include tenant scoping in the query (e.g. add AND dfy_intake_forms.tenant_id = ${tenantId}) or require a tenantId parameter and validate the token belongs to that tenant before returning the row. If the endpoint is intentionally public, ensure the table/row is explicitly global or store public-facing tokens in a separate global table that does not expose tenant-owned rows.
- **server/doc-collections.ts:294** — UPDATE doc_chunks to set embedding uses only WHERE id = ${row.id} with no tenant filter. Although the row was selected previously for this tenant, the UPDATE itself is not tenant-scoped and could affect an identically numbered row belonging to another tenant if IDs are reused or referenced incorrectly.
  - _Fix:_ Add tenant protection to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Always include tenant_id on writes against tenant-scoped tables even when ID was read earlier.
- **server/email.ts:370** — SELECT from inbox_messages (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1) has no tenant_id constraint — reads tenant-scoped table across all tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise scope the existence check by the target tenant/inbox before returning results.
- **server/email.ts:415** — SELECT scanning inbox_messages for empty bodies (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') AND (body_html IS NULL OR body_html = '') ORDER BY id) is not constrained by tenant_id — reads rows across tenants.
  - _Fix:_ Limit the query to a specific tenant (add AND tenant_id = ${tenantId}) or run the backfill per-tenant so you never enumerate tenant-scoped rows across all tenants.
- **server/embeddings.ts:811** — SELECT id, tenant_id, title, content FROM agent_knowledge ... (backfillMissingKnowledgeEmbeddings) queries a tenant-scoped table without restricting tenant_id — reads across all tenants.
  - _Fix:_ Run backfills per-tenant (pass tenantId) or add a tenant filter to the query (WHERE tenant_id = ${tenantId}) so rows from only the intended tenant are processed.
- **server/embeddings.ts:842** — SELECT id, embedding FROM memory_entries ... (backfillEmbeddingVecs) queries memory_entries without tenant_id filtering — enumerates rows across tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate backfills on a per-tenant basis so you never read other tenants' rows.
- **server/embeddings.ts:858** — SELECT id, embedding FROM agent_knowledge ... (backfillEmbeddingVecs) queries agent_knowledge without tenant_id filtering — reads across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or execute the backfill per tenant so agent_knowledge rows are processed only within their tenant context.
- **server/episode-playbooks.ts:68** — Embedding vector literal (embeddingLiteral) is interpolated directly into SQL (goal_embedding <=> ${embeddingLiteral}::vector) without using the validated vecLiteral helper or parameterized binding — risk of injection or malformed SQL from unvalidated provider-derived data.
  - _Fix:_ Validate and canonicalize the vector (use the same vecLiteral validation used elsewhere) and bind it safely (e.g. construct a validated vec string and use sql.raw only after validation, or use parameterized/cast-safe bindings). Avoid direct string interpolation of provider-derived values into SQL.
- **server/episode-playbooks.ts:92** — INSERT INTO episode_playbooks uses ${embeddingLiteral}::vector interpolated directly into the VALUES clause without validation — same injection/validation risk as above.
  - _Fix:_ Use a validated vector literal (via vecLiteral) or parameterized binding for the vector value before embedding it into the SQL; never inject raw provider-derived strings into SQL.
- **server/episode-playbooks.ts:120** — SELECT that orders by goal_embedding <=> ${embeddingLiteral}::vector in retrieveEpisodePlaybooks interpolates embeddingLiteral directly into SQL without validation — injection / malformed-SQL risk.
  - _Fix:_ Validate the embedding with a proper vecLiteral helper and use parameterized/validated construction for the vector cast; avoid raw string interpolation of untrusted embeddings.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` without constraining `tenant_id` — rehydrateTaskStateOnBoot reads all agent_runs across tenants (no WHERE tenant_id = ...) which can leak other tenants' in-flight state into process memory.
  - _Fix:_ Restrict the query to the intended tenant(s). Options: (1) add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) and pass the tenant as an argument to rehydrateTaskStateOnBoot; (2) if the process should only serve a single tenant instance, use a configured INSTANCE_TENANT_ID env var and filter by it; or (3) iterate an allowed tenant list and run a separate, parameterized query per tenant. Never run a global scan across all tenant rows and then trust/ingest tenant_id values without scoping the DB read to the caller's tenant(s).
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scraped_pages' omits tenant constraint — .where(eq(scrapedPages.id, existing[0].id)) updates by id alone without scoping to caller's tenant.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so writes are constrained to the caller's tenant.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scraped_pages' omits tenant constraint — .where(eq(scrapedPages.id, existing[0].id)) updates by id alone inside crawl loop.
  - _Fix:_ Add an explicit tenant check to the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to enforce tenant isolation.
- **server/google-drive.ts:880** — UPDATE on tenant-scoped table 'projects' omits tenant constraint — db.update(projects).set(...).where(eq(projects.id, projectId)) updates project row by id only without also asserting tenant ownership in the WHERE clause.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise bind the update to the verified tenant to prevent cross-tenant writes.
- **server/google-workspace.ts:2287** — INSERT into tenant-scoped table 'presenter_slide_images' does not include tenant_id (no tenant context passed into the row).
  - _Fix:_ Include the tenant_id column in the INSERT (or otherwise derive and validate the tenant) so each row is explicitly tied to the caller's tenant. Example: INSERT INTO presenter_slide_images (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...). If session_id is authoritative, validate session→tenant before inserting and still persist tenant_id on the row.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at <= NOW() reads research_schedules across all tenants without tenant_id constraint, then subsequent logic updates rows using tenant_id in the WHERE. The initial select can expose or act on other tenants' schedules.
  - _Fix:_ Perform per-tenant selection (e.g. SELECT ... FROM research_schedules WHERE tenant_id = ${tenantId} AND ...) or ensure the global select is only executed in a trusted admin/system context and is properly authorized. Prefer selecting and operating per-tenant to maintain isolation.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks executed with no tenant_id filter (cross-tenant read of a tenant-scoped table).
  - _Fix:_ If this function is intentionally a scheduler-level cross-tenant operation, restrict its callers to a trusted scheduler context and document that explicitly. Otherwise, add a tenant_id WHERE clause (or accept an explicit tenantId parameter) so reads are always scoped to a single tenant. Consider returning only the columns required and/or adding an explicit admin-only guard before returning tenant-scoped rows.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks ... WHERE id = ${id} ... executed with no tenant_id constraint, allowing last_run_at/next_run_at to be updated without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or accept/derive tenantId for each id before updating. If multi-tenant scheduling is needed, centralize and harden this path so only trusted scheduler code can perform cross-tenant updates and log/verifies tenant ownership (or use RETURNING tenant_id and assert it matches expected context).
- **server/heartbeat.ts:255** — SELECT * FROM event_log WHERE status = 'pending' ... executed with no tenant_id filter — reads pending events across all tenants.
  - _Fix:_ Process events per-tenant (add tenant_id = ${tenantId}) or introduce an explicit, tightly-controlled scheduler/worker context that is authorized to read across tenants. At minimum, ensure the code that consumes these rows validates tenant_id before performing tenant-scoped actions and that only trusted runtime contexts can call this function.
- **server/job-queue.ts:317** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} with no tenant filter. While intended as an admin lookup, it performs an unscoped read on a tenant-scoped table.
  - _Fix:_ Document and gate this helper as admin-only. Prefer returning tenant_id from an explicit admin/privileged path, and ensure callers are properly authorized. If possible, pass an explicit admin token or require a separate admin API rather than leaving an unscoped SELECT helper in general server code.
- **server/job-queue.ts:346** — listJobs builds a SELECT from agent_jobs that may execute with an empty WHERE (no tenant_id) when tenantId is not passed — exposes job rows across tenants.
  - _Fix:_ Require callers to pass an explicit tenantId (or an ALL_TENANTS admin scope) and validate the caller's authorization. If listJobs must support admin multi-tenant views, ensure the admin path is separate and protected; otherwise always add WHERE tenant_id = ${tenantId}.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes a SELECT aggregating agent_knowledge across all tenant_id/persona_id pairs (no tenant_id WHERE), enumerating tenant rows. This is an unscoped read over a tenant-scoped table.
  - _Fix:_ If the monitor is intentionally multi-tenant, isolate this behavior behind a documented admin-only job and ensure strong access control/auditing. Otherwise, restrict queries to a single tenant at a time (WHERE tenant_id = ${tenantId}) or require explicit admin authorization to enumerate every tenant's data.

### MEDIUM (16)

- **server/auth.ts:206** — UPDATE of api_keys (lastUsedAt) is done by id only, without constraining by tenant_id. This is a write to a tenant-scoped table lacking an explicit tenant predicate.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause (e.g. .where(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))) so updates cannot be applied across tenants if ids were ever reused or mis-bound.
- **server/auth.ts:336** — DELETE FROM auth_sessions WHERE token = ${hashSessionToken(token)} is executed without a tenant_id predicate. This write/delete on a tenant-scoped table lacks an explicit tenant constraint.
  - _Fix:_ Include tenant_id in the DELETE predicate (e.g. DELETE FROM auth_sessions WHERE token = ${tokenHash} AND tenant_id = ${tenantId}) when tenant context is available. If the operation must be global, ensure it is only performed under an explicit platform-admin authority and is well-audited.
- **server/auth.ts:1231** — clearExpiredSessions() runs db DELETE: DELETE FROM auth_sessions WHERE expires_at < ${now} without tenant scoping. This periodic maintenance mutates tenant-scoped data across tenants.
  - _Fix:_ Scope the periodic cleanup per tenant (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ${now}) or run the cleanup function with explicit admin authorization/audit so cross-tenant deletion is intentional and audited.
- **server/business-tools.ts:127** — String-interpolated SQL snippet includes a request-derived value (params.status) when constructing the updates array (`status = '${params.status}'`). This is a potential injection / isolation risk if later used in an executed query.
  - _Fix:_ Avoid building SQL via plain string interpolation with request-derived values. Use parameterized Drizzle/sql template bindings instead (e.g. sql`status = ${params.status}`) or construct the query with validated/allowlisted identifiers and parameterized values. Remove or stop using the ad-hoc updates string array, or ensure any element used in sql.raw() is validated and not directly from caller input.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments ... reads commitments across all tenants with no caller tenant scope. This is an unscoped read of a tenant-scoped table.
  - _Fix:_ If this global enumeration is required for a system job, make the intent explicit (admin-only context) and minimize returned data (tenant_id only). Otherwise, avoid cross-tenant reads — iterate per-tenant using a vetted tenants table or explicit admin role, or enforce RLS/withTenantTx patterns to avoid accidental data exposure.
- **server/delivery-funnel.ts:71** — recordDeliveryEngagement inserts a delivery_engagement row that references an arbitrary delivery_id supplied by the caller without verifying that the referenced delivery_log row is owned by the caller's tenant.
  - _Fix:_ Before writing an engagement that references delivery_id, verify ownership: SELECT id FROM delivery_logs WHERE id = <deliveryId> AND tenant_id = <caller tenant>; only insert the engagement if that check succeeds. Alternatively, do not accept an externally supplied deliveryId (derive it server-side from an authenticated/validated filename or route) or enforce a DB constraint/transaction that ensures the referenced delivery belongs to the same tenant.
- **server/doc-collections.ts:136** — listCollections query uses subselects that query doc_chunks without tenant_id filtering: (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id) and (SELECT COUNT(DISTINCT doc_path) FROM doc_chunks WHERE collection_id = c.id). These nested selects do not include tenant_id and can surface counts across tenants.
  - _Fix:_ Add tenant scoping to the subselects (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) or correlate via the doc_collections row in a way that enforces tenant_id to avoid cross-tenant aggregation.
- **server/doc-collections.ts:456** — getCollectionStatus uses several subselects over doc_chunks (COUNT, SUM) that omit tenant_id filtering, e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id). These counts can aggregate across tenants without tenant_id in the subselects.
  - _Fix:_ Ensure every subselect that queries tenant-scoped tables includes tenant_id = ${tenantId} (or otherwise correlates to the tenant) so returned statistics are restricted to the requesting tenant.
- **server/glasses-gateway.ts:173** — UPDATE on apiKeys table omits tenant scoping — db.update(apiKeys).set(...).where(eq(apiKeys.id, key.id)) does not include tenant_id. Even though id is unique, the project's invariant requires explicit tenant scoping on tenant-scoped tables.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or use the auth-derived tenantId to ensure the update cannot touch rows outside the caller's tenant.
- **server/health-audit.ts:314** — SELECT from code_proposals has no tenant_id constraint (SELECT ... FROM code_proposals WHERE status IN (...) ...). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Either scope the query to a specific tenant (WHERE tenant_id = ${tenantId}) or ensure this admin-level scan is explicitly gated/authorized and documented. Prefer per-tenant queries when run in tenant context.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks (SELECT id,name,... FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL AND ...) omits tenant scoping and returns tasks across tenants.
  - _Fix:_ If this should be per-tenant, add WHERE tenant_id = ${tenantId}. If it's an intentional cross-tenant admin sweep, ensure the call is strictly admin-only and properly authorized; otherwise avoid unrestricted multi-tenant reads.
- **server/heartbeat-schedules.ts:26** — SELECT DISTINCT tenant_id FROM messages queries tenant-scoped table 'messages' with no tenant filter (enumerates all tenant_ids).
  - _Fix:_ If the heartbeat is intentionally enumerating tenants, ensure this is an admin-only operation and that the caller is authorized. If this is not intended, restrict scope (e.g. operate per-tenant) or use a dedicated tenants table for enumeration.
- **server/heartbeat-startup.ts:75** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executed without a tenant_id constraint (row update by id only).
  - _Fix:_ Constrain updates by tenant_id (e.g. AND tenant_id = ${t.tenantId}) or use the storage layer method that enforces tenant context. Even when updating by primary id, include tenant checks or verify the row's tenant matches the intended context to avoid accidental cross-tenant writes if ids are misused.
- **server/heartbeat-startup.ts:80** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executed without tenant_id in WHERE clause (second normalization branch).
  - _Fix:_ Same as above — add AND tenant_id = ${t.tenantId} or use storage helper that asserts tenant context before updating the row.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed with no tenant_id constraint after creating a task (approval_status write lacks tenant scoping).
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause or use storage.updateHeartbeatTask/newTask wrapper which asserts tenant ownership. Alternatively, read/verify the task row's tenant_id prior to updating approval_status and fail if it doesn't match the invoking tenant context.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed with no tenant_id constraint in delegateTaskFromChat flow.
  - _Fix:_ Same remediation as above: add AND tenant_id = ${tenantId} (or use a storage-layer helper that enforces the tenant) when updating approval_status so approval state changes cannot be applied to rows outside the caller's tenant context.
