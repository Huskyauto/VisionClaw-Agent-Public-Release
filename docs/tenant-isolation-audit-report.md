# Tenant-Isolation Audit Report

_Generated 2026-09-10T22:27:12.322Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 478 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **190/668** file(s) audited · chunks 20/61 OK (0 failed, 41 pending)
- Findings: **89** (CRITICAL 32 · HIGH 42 · MEDIUM 15 · LOW 0)
- Severe dispositions: **74 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (32)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects from experiments across ALL tenants: `SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100` has no tenant_id filter. The function then concludes experiments and writes proposedSkills per-row, meaning a single caller/process can read and act on other tenants' data.
  - _Fix:_ Scope the query to a single tenant. Either accept/require a tenantId parameter and add `AND tenant_id = ${tenantId}` to the WHERE clause, or iterate tenants explicitly (safe admin sweep) and validate/authorize per-tenant before acting. Never read-and-act on tenant-scoped rows without an application-level tenant_id constraint.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting reads ai_insights with `SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50` and no tenant_id constraint. The function then routes insights to Minerva per-row using the returned tenant_id; this allows a process to read and attempt to act on insights for all tenants without scoping or authorization.
  - _Fix:_ Either require a tenantId parameter and add `AND tenant_id = ${tenantId}`, or if this is intended as a global durability sweep, make the multi-tenant nature explicit and enforce admin authorization and per-row tenant validation before performing any tenant-scoped write. Avoid blind cross-tenant selects that drive per-tenant actions.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights performs a bulk UPDATE on ai_insights that can be executed without a tenant scope: `UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope}` — when tenantId is omitted (tenantScope empty) this updates rows across all tenants.
  - _Fix:_ Require an explicit tenantId for this operation or ensure the caller is explicitly authorized to run a global sweep. If the function must support both modes, make the global mode explicit (named admin function) and document/authorize it; otherwise add `AND tenant_id = ${tenantId}` so tenant-scoped updates are enforced.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE: `UPDATE agent_approvals SET status = 'expired' ... WHERE agent_approvals.status = 'pending' AND lt(agent_approvals.expiresAt, ...)` with no tenant_id constraint. This will expire approvals for all tenants in one commit, then attempts per-row run updates — potentially causing cross-tenant state changes without per-tenant scoping or authorization.
  - _Fix:_ Scope the expiry to a tenant (require tenantId) or make this an explicit admin/global-sweep operation with appropriate authorization. If global sweeping is intended, ensure per-row tenant validation is performed before acting and log/authorize the operation; otherwise add `AND tenant_id = ${tenantId}` to the WHERE clause.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table agent_wake_schedules performs a claim across all rows without constraining to a tenant (no tenant_id in WHERE/subselect). This allows a single heartbeat run to move/claim rows belonging to other tenants.
  - _Fix:_ Restrict the claim to a specific tenant (e.g. accept a tenantId and add WHERE tenant_id = ${tenantId} in the outer UPDATE and/or the inner SELECT) or iterate per-tenant. Alternatively run the claim under a platform admin process with explicit per-tenant scoping and authorization. Do NOT perform unscoped UPDATE/SELECT FOR UPDATE on tenant-scoped tables.
- **server/channel-routing.ts:16** — channel_routes table is created with no tenant_id column (global table). Without a tenant_id column you cannot enforce per-tenant isolation at the application level; subsequent reads/writes operate globally and can leak or corrupt other tenants' routing.
  - _Fix:_ Add a NOT NULL tenant_id column and make channel uniqueness tenant-scoped (e.g. UNIQUE(tenant_id, channel)). Backfill existing rows appropriately. Update all access patterns to always pass tenantId and include WHERE tenant_id = ${tenantId}. Consider a migration that sets tenant_id for existing rows or restricts table usage until a safe migration is done.
- **server/channel-routing.ts:54** — INSERT into channel_routes does not set tenant_id. If channel_routes is intended to be tenant-scoped, this INSERT (and ON CONFLICT update) will create global rows and cannot enforce isolation. Even if table is global, inserting persona_id without verifying persona ownership can link a persona from another tenant.
  - _Fix:_ Change setChannelRoute to accept tenantId and INSERT tenant_id explicitly (INSERT INTO channel_routes (tenant_id, channel, persona_id) VALUES (${tenantId}, ${channel}, ${personaId}) ...). Use ON CONFLICT (tenant_id, channel) DO UPDATE. Also verify the provided personaId belongs to the caller's tenant (SELECT p.tenant_id ... or include p in the INSERT validation) before inserting/updating.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken performs a SELECT on dfy_intake_forms without constraining by tenant_id. dfyIntakeForms rows are inserted with tenantId, so this read can return another tenant's form by token.
  - _Fix:_ If the intent is tenant-scoped, require and accept a tenantId parameter and add AND eq(dfyIntakeForms.tenantId, tenantId) to the WHERE clause. If the token must be globally usable, document and enforce that design explicitly (e.g. make token namespace/global and ensure no cross-tenant sensitive data is exposed). Prefer explicit tenant scoping when possible.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} deletes chunks without constraining tenant_id. doc_chunks is persisted with tenant_id on insert, so this delete can affect another tenant's rows.
  - _Fix:_ Add tenant scoping to the DELETE, e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Ensure every mutation on tenant-scoped tables includes the tenant_id filter.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' lacks any tenant filter. messages is tenant-scoped elsewhere; this global count may leak or mix tenant data across tenants.
  - _Fix:_ If this logic is intended to run per-tenant, accept a tenantId and add AND tenant_id = ${tenantId} to the query. If it purposely aggregates system-wide metrics, document that and ensure messages is a non-tenant table; otherwise enforce tenant scoping.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... runs without tenant filter. tool_performance is tenant-scoped elsewhere, so this query aggregates across tenants and can produce cross-tenant results.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) when the computation should be per-tenant. If the intent is a global system metric, ensure tool_performance is truly global or move to a dedicated global table and document the design.
- **server/dormant-deprecation.ts:202** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with no tenant constraint. This returns tool names across all tenants even though tool_performance is tenant-scoped elsewhere.
  - _Fix:_ Restrict to a tenant by adding WHERE tenant_id = ${tenantId} (and accept tenantId into the function), or purposefully use a global source of truth if a system-level list is required. Avoid unscoped reads on tenant-scoped tables.
- **server/dormant-deprecation.ts:213** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... but does not filter by tenant_id. This will mix activity across tenants.
  - _Fix:_ If activity should be evaluated per-tenant, add AND tenant_id = ${tenantId} and accept tenantId in the function. If the intent is to operate system-wide, use a dedicated global dataset and document that choice; do not read tenant-scoped tables unfiltered.
- **server/email.ts:429** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} is executed without also constraining tenant_id. This can update rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) — select tenant_id in the earlier query so the updater can supply the correct tenant_id, or otherwise derive and validate the tenant before updating.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id guard. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) and ensure the SELECT that produced row includes tenant_id so it can be validated before updating.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint, allowing writes across tenant boundaries.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) and ensure the prior SELECT returned tenant_id so updates can be validated.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to executing UPDATE ${sql.raw(table)} ... WHERE id = ${id} when no tenantId is provided, performing an unconditional write to a tenant-scoped table.
  - _Fix:_ Do not perform unconditional updates. Require and validate a positive tenantId for tenant-scoped tables and fail/throw if absent. Use parameterized queries with explicit tenant_id conditions rather than interpolating raw table names or leaving out tenant filters.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` performed without a tenant_id filter (reads rows across all tenants into in-memory taskStateStore).
  - _Fix:_ Constrain the query to a single tenant (e.g. WHERE tenant_id = ${tenantId}) or require/accept an explicit tenantId parameter and only rehydrate that tenant. If the intent is a privileged cross-tenant admin operation, restrict this code path to an admin-only context and avoid populating a global in-memory store that mixes tenant data.
- **server/google-drive.ts:1664** — INSERT into tenant-scoped table 'project_files' omits tenant_id — INSERT INTO project_files (...) VALUES (...) does not provide a tenant_id value.
  - _Fix:_ Always include tenant_id explicitly in INSERTs for tenant-scoped tables (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${params.projectId}, ...)). Also ensure the tenant is validated (assertProjectInTenant) before inserting, and prefer adding a tenant WHERE clause on any subsequent updates/deletes.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks SET ... WHERE id = ${id} has no tenant_id constraint. This updates tenant-scoped rows without verifying tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. AND tenant_id = ${tenantId}) or lookup the task under the intended tenant first and then perform the update. Alternatively, use an existing storage helper that enforces tenant context (assertTenantContext) before performing the DB update to prevent cross-tenant writes.
- **server/heartbeat-startup.ts:76** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} lacks a tenant_id constraint — writes to a tenant-scoped table without confirming the row belongs to the intended tenant.
  - _Fix:_ Include AND tenant_id = ${t.tenantId} in the WHERE clause, or replace the raw db.execute with storage.updateHeartbeatTask(...) or another helper that asserts tenant context. Ensure any cross-tenant operations are explicit and authorized.
- **server/heartbeat-startup.ts:80** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} lacks a tenant_id constraint — writes to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${t.tenantId}) or call a storage-layer function that enforces/asserts tenant context before updating the row. Audit cross-tenant updates and restrict them to admin-only code paths.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executes without a tenant_id constraint — updates a tenant-scoped table by id only, allowing possible cross-tenant mutation.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${newTask.tenantId} or use the storage layer (e.g. storage.updateHeartbeatTask) which should assert tenant ownership. Always verify ownership of the referenced task id before updating approval_status.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat) has no tenant_id constraint — this writes to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${newTask.tenantId}) or perform the update via a storage helper that checks/asserts the tenant context. Validate that newTask belongs to the caller's tenant before mutating approval_status.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE on agent_jobs without any tenant_id constraint (the inner SELECT/UPDATE can claim rows for any tenant).
  - _Fix:_ Scope claims to an explicit tenant (pass a tenantId or iterate per-tenant) or add a tenant_id filter in the UPDATE/SELECT so the worker only touches rows belonging to the intended tenant/context. If the worker must operate cross-tenant, make the cross-tenant intent explicit and protected (e.g. require an ADMIN/ALL_TENANTS flag) and audit that path.
- **server/job-queue.ts:196** — reclaimExpiredLeases STEP 1: UPDATE agent_jobs to mark terminal failures contains no tenant_id constraint — it will mutate rows across all tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. accept a tenantId or run per-tenant), or otherwise ensure this function is only callable in a privileged audited context; prefer per-tenant WHERE tenant_id = ${tenantId} to maintain isolation.
- **server/job-queue.ts:210** — reclaimExpiredLeases STEP 2: UPDATE agent_jobs to requeue expired leases contains no tenant_id constraint and will requeue rows across all tenants.
  - _Fix:_ Same as above: ensure the requeue UPDATE is tenant-scoped (WHERE tenant_id = ${tenantId}) or run per-tenant under an explicit, audited admin path.
- **server/job-queue.ts:239** — completeJob updates agent_jobs without constraining by tenant_id (UPDATE ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts}). This allows an actor with only a job id to modify another tenant's job row if other protections are missing.
  - _Fix:_ Include tenant fencing in the WHERE clause (e.g. AND tenant_id = ${expectedTenantId}) or require the caller to pass and validate tenantId; alternatively resolve the job's tenant first and verify it matches the caller's scope before performing the UPDATE.
- **server/job-queue.ts:282** — failJob reads max_attempts with SELECT max_attempts FROM agent_jobs WHERE id = ${id} (no tenant_id constraint). This read can observe job rows belonging to other tenants.
  - _Fix:_ SELECT max_attempts should be tenant-scoped (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or the function should resolve and validate the job's tenant before reading.
- **server/job-queue.ts:299** — failJob terminal-path UPDATE mutates agent_jobs without a tenant_id WHERE clause (UPDATE ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts}).
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or otherwise validate caller tenant) so only the intended tenant's row can be moved to failed_terminal.
- **server/job-queue.ts:319** — failJob retry-path UPDATE (reschedule/backoff) updates agent_jobs without constraining by tenant_id (UPDATE ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts}).
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or perform an explicit tenant ownership check before performing the UPDATE to enforce isolation.
- **server/job-queue.ts:336** — cancelJob performs UPDATE agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint, allowing cancellation of jobs belonging to other tenants.
  - _Fix:_ Require and validate tenantId in cancelJob and add AND tenant_id = ${tenantId} to the WHERE clause (or restrict this API to a properly audited admin flow that first resolves the job's tenant).

### HIGH (42)

- **server/agent-activity.ts:122** — completeActivity updates agent_activity rows by id only: `db.update(agentActivity).set(updates).where(eq(agentActivity.id, activityId))`. There is no tenant_id constraint, so a caller could update an activity belonging to another tenant.
  - _Fix:_ Require tenant context for this operation and include it in the WHERE clause, e.g. `where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))`. If the caller cannot provide tenantId, first read the row and verify its tenant_id matches the caller's tenant before updating.
- **server/agentic-features.ts:1021** — classifyReply updates outreach_enrollments with `UPDATE outreach_enrollments SET ... WHERE id = ${row.id}` but omits tenant_id. Updating by id only risks modifying another tenant's enrollment if IDs are guessed or mixed up.
  - _Fix:_ Constrain the update by tenant as well: `WHERE id = ${row.id} AND tenant_id = ${params.tenantId}` (or the row's tenantId). Ensure the function has tenantId available and validate ownership before performing the update.
- **server/agentic/department-budgets.ts:143** — runBudgetEnforcement executes a tenant-scoped SELECT without constraining to a caller tenant (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads across all tenants in a tenant-scoped table and violates the invariant that every read/write against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ If this is intended as a privileged admin sweep, make the privilege explicit and narrow: require an explicit admin/owner context and/or an ADMIN_TENANT_ID constant, and document+guard who may call it. Otherwise restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) or add a tenantId parameter and iterate per-tenant from a trusted admin caller. Alternatively move global-sweep behavior into a dedicated admin-only job/service and enforce authorization and audit logging; consider adding DB-side protections (e.g. RLS) or explicit runtime checks so accidental/non-admin callers cannot read other tenants' data.
- **server/agentic/resume-worker.ts:65** — SELECT from tenant-scoped table `agent_runs` (and LEFT JOIN `agent_approvals`) is executed without constraining rows to a specific tenant_id — the query scans rows across all tenants.
  - _Fix:_ Restrict the query to the target tenant(s). Either run the sweep per-tenant and add a WHERE r.tenant_id = ${tenantId} (or eq(agentRuns.tenantId, tenantId) via Drizzle), or otherwise explicitly include a tenant_id predicate on agent_runs in the WHERE clause so every returned row is scoped to the caller's tenant. Avoid any cross-tenant reads from tenant-scoped tables.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT ... FROM conversations in backfillProjectAssets scans conversations across all tenants (no tenant_id WHERE clause). A background backfill is reading per-tenant data without any tenant scoping or authorization check.
  - _Fix:_ Either run the backfill per-tenant (accept/derive a tenantId and add WHERE c.tenant_id = ${tenantId}) or require platform-admin authority and audit/guard the operation. Always scope SELECTs on tenant-scoped tables by tenant_id or otherwise verify authorization before reading rows.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table "conversations" does not include tenant_id constraint in WHERE clause. The statement `UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL` can modify a conversation belonging to another tenant if an attacker or corrupted data supplies a cross-tenant conversation id.
  - _Fix:_ Enforce tenant-scoped WHERE predicate on the UPDATE. Add an explicit tenant check (e.g. `AND tenant_id = ${c.tenant_id}`) or re-use a previously-validated tenant context when issuing the UPDATE. Prefer parameterized tenant_id binding rather than relying on earlier SELECTs to prove ownership.
- **server/business-tools.ts:96** — INSERT into invoice_items does not include tenant_id. Child rows tied to tenant-scoped data (invoice_items) must be attributed to the caller's tenant explicitly — relying only on invoice_id is unsafe because there is no RLS/DB-enforced cascade.
  - _Fix:_ Include the tenant_id when inserting invoice_items (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)). Alternatively, explicitly verify the parent invoice belongs to the tenant before inserting and store tenant_id on the child row. Also add a schema-level NOT NULL tenant_id and/or a DB constraint to prevent future omissions.
- **server/channel-routing.ts:31** — listChannelRoutes SELECT joins personas without any tenant constraint. The query reads persona rows (JOIN personas p ON p.id = cr.persona_id) without verifying the caller's tenant, so a caller may see personas from other tenants.
  - _Fix:_ Make channel_routes tenant-scoped and require caller tenantId for listChannelRoutes. Add WHERE cr.tenant_id = ${tenantId} and ensure p.tenant_id = ${tenantId} (or validate persona ownership separately) before returning persona_name. Pass tenantId into listChannelRoutes and parameterize it in the query.
- **server/channel-routing.ts:64** — getPersonaForChannel selects persona_id from channel_routes filtered only by channel and enabled; no tenant constraint is applied. This can return another tenant's persona_id for the same channel.
  - _Fix:_ Require tenantId parameter and include WHERE tenant_id = ${tenantId} AND channel = ${channel} AND enabled = true. After fetching persona_id, verify the persona row belongs to the same tenant (e.g. SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}) before returning it.
- **server/channel-routing.ts:71** — removeChannelRoute deletes rows from channel_routes by channel only (DELETE FROM channel_routes WHERE channel = ${channel}) with no tenant restriction — will remove routes across all tenants.
  - _Fix:_ Require tenantId parameter and constrain DELETE to WHERE tenant_id = ${tenantId} AND channel = ${channel}. If channel_routes is shared, rethink the table design; otherwise make all mutating operations tenant-scoped and validate persona ownership before deleting/updating.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint. The query uses only project_id which relies on earlier application logic but does not add WHERE tenant_id = ${tenantId}.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or otherwise include eq(project_files.tenantId, tenantId) so every read enforces the caller's tenant.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint. The query filters by project_id only.
  - _Fix:_ Constrain the query to the caller's tenant, e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId} (or use Drizzle eq on note table tenantId) to enforce tenant isolation.
- **server/chat-engine/project-context.ts:23** — Query reads from tenant-scoped table `project_conversations` (pc) without an explicit tenant_id constraint. The JOIN to projects/tenant is used to limit results, but pc itself is not constrained by tenant_id in the WHERE clause.
  - _Fix:_ Add an explicit tenant filter on project_conversations (e.g. AND pc.tenant_id = ${tenantId}) or include eq(pc.tenantId, tenantId) in the WHERE so every accessed tenant-scoped table has an application-level tenant constraint.
- **server/chat-engine/project-context.ts:44** — EXISTS subquery reads `project_conversations pc` without any tenant_id constraint. Even though p.tenant_id is checked for p, pc is not explicitly tenant-scoped in the subquery.
  - _Fix:_ Constrain the subquery to the tenant, e.g. WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId} (or ensure pc.tenant_id = p.tenant_id) to maintain explicit application-level tenant filtering.
- **server/chat-engine/project-context.ts:181** — Query joins `project_conversations pc` but does not constrain pc by tenant_id (only c.tenant_id is checked). project_conversations is tenant-scoped and must be filtered by tenant.
  - _Fix:_ Add an explicit tenant filter on project_conversations, e.g. WHERE pc.project_id = ${projectId} AND pc.tenant_id = ${tenantId} AND c.id != ${conversationId} AND c.tenant_id = ${tenantId}, so every tenant-scoped table in the join includes tenant_id = tenant.
- **server/cmmc-assessments.ts:150** — Lookup by invitation token queries tenant-scoped tables cmmc_assessment_invitations and cmmc_assessments without constraining to a caller-known tenant_id. The SELECT allows returning tenant-scoped rows solely by token (no tenant_id = <caller> filter).
  - _Fix:_ Require and use a tenantId parameter when looking up invitation-based access, or otherwise ensure the query enforces an application-level tenant boundary (e.g. add eq(cmmcAssessmentInvitations.tenantId, <expectedTenant>) or validate the caller's tenant ownership before returning tenant-scoped rows). If token-based public access is intended, document and harden this flow explicitly (e.g. tighten token entropy, rotate policy), but do not rely on implicit joins instead of explicit tenant_id WHERE clauses.
- **server/commitment-drafter.ts:234** — UPDATE commitments executed without tenant_id constraint (revert/unclaim path). This statement can modify a commitment row across tenants because it only filters by id.
  - _Fix:_ Add tenant scoping to the UPDATE. Include AND tenant_id = ${tenantId} in the WHERE clause (or use the row's tenantId validated earlier). Example: UPDATE commitments SET draft_status='open', drafted_at=NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}.
- **server/crews-engine.ts:392** — addCrewTask inserts context_task_ids provided by the caller without verifying that the referenced task IDs belong to the caller's tenant. This allows planting cross-tenant references (foreign IDs) into a tenant-scoped row.
  - _Fix:_ Before inserting, validate ownership of each referenced contextTaskId. E.g. SELECT id FROM crew_tasks WHERE id = ANY(${params.contextTaskIds}) AND tenant_id = ${params.tenantId} and compare counts; reject the request if any id is missing or owned by another tenant. Alternatively, resolve/translate context IDs server-side rather than accepting raw foreign IDs from untrusted input.
- **server/data-protection.ts:150** — SELECT FROM conversations without tenant predicate: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` reads tenant-scoped rows across all tenants (no WHERE tenant_id = ...)
  - _Fix:_ Scope the scan to a single tenant or iterate per-tenant. For example: first SELECT DISTINCT tenant_id FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}, then for each tenant run deletes/backs up using WHERE tenant_id = ${tenantId}. Do NOT perform a blind global SELECT that returns other tenants' titles/ids.
- **server/email.ts:371** — SELECT from inbox_messages performed without scoping by tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This reads a tenant-scoped table globally.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1), or resolve the tenant first and then query only that tenant's rows.
- **server/email.ts:415** — SELECT across inbox_messages without tenant constraint (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id). This reads all tenants' inbox rows.
  - _Fix:_ Make this operation tenant-scoped (add tenant_id = ${tenantId} to the WHERE clause) or iterate per-tenant. If a global sweep is intended, ensure it's safe and uses an explicit admin/maintenance path with authorization and logging.
- **server/embeddings.ts:811** — SELECT from agent_knowledge (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL AND source = 'autoresearch' ...) runs without restricting tenant_id. This reads tenant-scoped rows across tenants.
  - _Fix:_ Scope the SELECT by tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate/backfill per-tenant with explicit tenant context; ensure any maintenance reads are explicit and authorized.
- **server/embeddings.ts:842** — Backfill SELECT from memory_entries (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL ...) lacks tenant_id, returning rows across tenants.
  - _Fix:_ Perform backfill per-tenant (add tenant_id = ${tenantId} to the WHERE) or include tenant_id in the SELECT and explicitly validate it before any writes.
- **server/embeddings.ts:858** — SELECT from agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ...) is executed without tenant_id, reading across tenants.
  - _Fix:_ Scope the query by tenant (add tenant_id = ${tenantId}) or run the backfill in a per-tenant loop. Always fetch and use tenant_id in maintenance reads.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table `felix_loop_runs` uses WHERE id = ${runId} with NO tenant_id constraint — the update could affect a row belonging to another tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) to enforce application-level tenant isolation on writes.
- **server/felix-loop.ts:689** — Error-path UPDATE on tenant-scoped table `felix_loop_runs` uses WHERE id = ${runId} with NO tenant_id constraint — the recovery write could modify another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) on the error-write path as well to ensure verification that the caller can only update its own tenant's rows.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' omits tenant constraint — .where(eq(scrapedPages.id, existing[0].id)) updates by id only without ensuring tenant ownership.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so updates are constrained to the caller's tenant.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' omits tenant constraint inside crawl loop — .where(eq(scrapedPages.id, existing[0].id)) updates by id only.
  - _Fix:_ Add tenant scoping to the update (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to prevent cross-tenant writes.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' omits tenant constraint — db.update(projects).set(...).where(eq(projects.id, projectId)) updates project rows by id only without tenant filter.
  - _Fix:_ Include the tenant filter in the WHERE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) to ensure only the owner's project row is modified.
- **server/google-workspace.ts:2289** — INSERT into presenter_slide_images does not include tenant_id. If presenter_slide_images is tenant-scoped, this writes rows without associating them to the caller/tenant (or uses only session_id), allowing cross-tenant data pollution or unauthorized writes.
  - _Fix:_ Make presenter_slide_images tenant-aware: add tenant_id to the INSERT (and ON CONFLICT keys if required) and pass the canonical tenantId from this request/context. e.g. INSERT INTO presenter_slide_images (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...). Validate that sessionId belongs to the tenant before persisting if session→tenant mapping exists.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a tenant-scoped row by id only (no tenant_id constraint). This can modify another tenant's session if session IDs collide or are guessed.
  - _Fix:_ Constrain the UPDATE to the canonical tenant, e.g. UPDATE presenter_sessions SET slides = ${...} WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Additionally, verify that the session belongs to the expected tenant before performing the update.
- **server/health-audit.ts:314** — SELECT FROM code_proposals has no tenant_id filter. If code_proposals is tenant-scoped, this enumerates proposals across tenants in an unscoped query.
  - _Fix:_ If this scanner is intended to operate per-tenant, add tenant scoping (WHERE tenant_id = ${tenantId}). If it must run as a global/admin operation, ensure it runs under an explicit privileged/admin code path and documents that it intentionally touches all tenants; otherwise restrict the query to the target tenant(s).
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows without constraining by tenant_id. This can change proposals across tenants.
  - _Fix:_ Include tenant scope in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the update using (id, tenant_id) pairs. If running as a global admin action, require an explicit privileged execution path and consider including tenant verification before mutating rows.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (stuck detection) lacks tenant_id filtering. If heartbeat_tasks is tenant-scoped, this will surface tasks across tenants without scoping.
  - _Fix:_ If this audit is intended to operate per-tenant, add tenant_id = ${tenantId} to the WHERE clause. If it is an intentional cross-tenant admin sweep, document the privileged intent and ensure callers are properly authorized.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables tasks by id without constraining by tenant_id. This can disable tasks belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or update by (id, tenant_id) pairs. If this is an admin-wide operation, ensure it runs only under an authorized admin context and consider scoping or auditing which tenants were affected.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT from tenant-scoped table heartbeat_tasks with no tenant_id constraint (SELECT * FROM heartbeat_tasks ...). This reads across tenants without an explicit tenant filter.
  - _Fix:_ If the operation must remain cross-tenant, make that explicit and restrict access to a trusted scheduler/admin context. Otherwise limit the SELECT by tenant_id (e.g. WHERE tenant_id = ${tenantId}) or call a storage API that enforces tenant scoping. Consider adding explicit auditing/authorization for any cross-tenant reads.
- **server/heartbeat.ts:256** — SELECT * FROM event_log WHERE status = 'pending' ... selects rows from event_log without a tenant_id constraint. event_log rows include tenant_id and this read pulls events across tenants.
  - _Fix:_ If the scheduler intentionally processes events across tenants, make this explicit and ensure only a trusted admin process executes it. Otherwise, add a WHERE tenant_id = ${tenantId} filter or use a storage API that enforces tenant scoping. Consider adding authorization/audit for cross-tenant event processing.
- **server/job-queue.ts:378** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} has no tenant filter. Although intended for admin use, this is an unscoped read against a tenant-scoped table.
  - _Fix:_ If this helper is admin-only, ensure it is only callable from audited admin code paths. Otherwise, use tenant-scoped access or require an admin flag; prefer an explicit API that asserts caller privileges before performing the unscoped SELECT.
- **server/job-queue.ts:407** — listJobs executes SELECT ... FROM agent_jobs ${whereClause} where ${whereClause} can be empty (caller may retrieve rows across all tenants). This allows unscoped listing of tenant-scoped rows unless tenantId is always provided by the caller.
  - _Fix:_ Enforce that caller provides a tenantId (or explicit ALL_TENANTS constant checked against caller privileges). If admin functionality is required, gate it behind explicit admin checks. Otherwise always include AND tenant_id = ${tenantId} in the WHERE clause.
- **server/job-worker.ts:147** — Handler 'research_code_proposal' reconstructs a session using tenantId from job.payload (const { tenantId } = job.payload) without validating it against job.tenantId or verifying ownership. A crafted payload could cause reads/writes under a different tenant's identity.
  - _Fix:_ Do not trust tenant identifiers embedded in job payloads. Validate that job.payload.tenantId === job.tenantId (or use job.tenantId exclusively). Explicitly assert and enforce tenant scope before calling downstream functions that access tenant-scoped data.
- **server/job-worker.ts:168** — Handler 'research_digest' reads tenantId from job.payload and passes it into generateResearchDigest without verifying it matches job.tenantId. This can let a job payload trigger actions for another tenant.
  - _Fix:_ Require the handler to use the durable job.tenantId (or validate payload.tenantId against it) and reject or fail the job if there's a mismatch. Ensure every downstream read/write is done under the resolved tenant context.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor performs a SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge GROUP BY tenant_id, persona_id (no caller tenant filter). This enumerates and then processes rows across all tenants without explicit admin gating.
  - _Fix:_ If this monitor must run cross-tenant, make the cross-tenant intent explicit and ensure the job runs under a privileged, audited admin context. Otherwise, change the design to snapshot per-tenant (accept tenantId or iterate an explicit tenant list) and apply WHERE tenant_id = ${tenantId} to each DB query to enforce tenant isolation.

### MEDIUM (15)

- **server/agentic-features.ts:861** — enrollInSequence checks for an existing enrollment with `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` but does not include tenant_id in the WHERE clause. This can leak or operate on enrollments across tenants.
  - _Fix:_ Include tenant scope in the lookup: add `AND tenant_id = ${params.tenantId}`. Alternatively, validate ownership of the referenced sequence row (which is done earlier) and then use an explicitly tenant-scoped WHERE for enrollments to enforce isolation.
- **server/agentic/self-initiative.ts:100** — SELECT from `heartbeat_logs` lacks an explicit tenant_id predicate — it relies on task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}) rather than constraining the heartbeat_logs rows themselves to the caller tenant.
  - _Fix:_ Add an explicit tenant constraint on heartbeat_logs (e.g. WHERE heartbeat_logs.tenant_id = ${tenantId}) or join heartbeat_logs to heartbeat_tasks and require heartbeat_logs.tenant_id = ${tenantId} (or eq(...)) to ensure the read is explicitly tenant-scoped. Do not rely solely on indirect subqueries for tenant isolation.
- **server/agentic/task-forces.ts:40** — createTaskForce inserts caller-supplied projectId into task_forces without verifying that the referenced project actually belongs to the caller's tenant. This permits referencing a foreign project id from another tenant.
  - _Fix:_ Before inserting, verify ownership: SELECT project WHERE id = ${p.projectId} AND tenant_id = ${p.tenantId} (or reject null/invalid). Only accept projectId that belongs to the tenant, or omit the foreign id if ownership isn't proven.
- **server/agentic/task-forces.ts:39** — createTaskForce accepts personaIds from caller and stores them (persona_ids) without verifying that the referenced persona rows belong to the caller's tenant. Persona ids are untrusted foreign ids.
  - _Fix:_ Validate personaIds before persisting (e.g. SELECT id FROM personas WHERE id IN (...) AND tenant_id = ${p.tenantId}) and reject or filter out any ids that are not owned by the tenant. Persist only verified persona ids.
- **server/agentic/wake-scheduler.ts:66** — scheduleWake INSERT writes caller-supplied conversation_id and project_id into agent_wake_schedules without verifying that those referenced entities belong to the provided tenantId.
  - _Fix:_ If conversationId or projectId is supplied, verify ownership before inserting (e.g. SELECT ... FROM conversations/projects WHERE id = ${id} AND tenant_id = ${p.tenantId}). Reject or null the foreign-id fields when ownership is not proven.
- **server/agentic/step-reward.ts:140** — recordStepReward INSERT writes caller-supplied plan_id/run_id/conversation_id into step_rewards without validating that those referenced rows belong to the given tenantId.
  - _Fix:_ Before inserting reward rows that reference other tenant-scoped entities, verify ownership (e.g. SELECT ... FROM plans/runs/conversations WHERE id = ${id} AND tenant_id = ${i.tenantId}) or otherwise enforce that the ids are trusted. Reject or null foreign ids that fail ownership checks.
- **server/commitments.ts:135** — SELECT FROM commitments performs a cross-tenant scan with no tenant_id filter (heartbeat scanner). Reading tenant-scoped table rows without tenant scoping can expose tenant data.
  - _Fix:_ If this aggregated behavior is intentional for platform-admin purposes, ensure the call is restricted to an admin-only context and that any exported content is strictly redacted (do not include tenant-identifying fields). Otherwise, scope the SELECT by tenant_id or run this logic in an explicit admin-only job that uses a safe audit channel.
- **server/data-protection.ts:359** — SELECT project_notes by project_id without tenant predicate: `SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}` — query against a tenant-scoped table lacks `tenant_id = ${tenantId}`
  - _Fix:_ Include the tenant predicate or join through a tenant-scoped parent row. Example: `SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` (or join projects and require p.tenant_id = ${tenantId}) to ensure tenant isolation.
- **server/data-protection.ts:371** — SELECT project_files by project_id without tenant predicate: `SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}` — missing `tenant_id = ${tenantId}`
  - _Fix:_ Add tenant scoping: `SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` or otherwise verify ownership before reading files.
- **server/data-protection.ts:403** — COUNT messages by conversation_id without tenant predicate: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}` — missing `AND tenant_id = ${tenantId}`
  - _Fix:_ Constrain to the conversation's tenant: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}` (use the tenantId passed into backupTenantDataToDrive) to avoid cross-tenant row reads.
- **server/episode-playbooks.ts:133** — String-interpolated SQL used to build an ANY(...) array: WHERE tenant_id = ${tenantId} AND id = ANY(${`{${ids.join(",")}}`}::int[]). The ids array is injected into the SQL string rather than passed as parameterized values, risking SQL-injection / isolation issues.
  - _Fix:_ Avoid string-interpolating id lists. Use a parameterized array binding (e.g. WHERE tenant_id = ${tenantId} AND id = ANY(${sql.array(ids, 'int4')})) or iterate with parameterized placeholders. Ensure ids are integer-validated before use.
- **server/glasses-gateway.ts:157** — SELECT from tenant-scoped table 'apiKeys' does not include an explicit tenant_id constraint (.where(and(eq(apiKeys.keyHash, keyHash), eq(apiKeys.isRevoked, false))).
  - _Fix:_ Either make apiKeys a non-tenant-scoped/global table or include tenant scoping in the query. At minimum, ensure any subsequent logic/writes are strictly constrained by the resolved key. Prefer adding tenant check in the WHERE (or otherwise guarantee the query cannot return keys from other tenants).
- **server/glasses-gateway.ts:173** — UPDATE on tenant-scoped table 'apiKeys' omits tenant constraint — db.update(apiKeys).set(...).where(eq(apiKeys.id, key.id)).
  - _Fix:_ Constrain the update by tenant as well: .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))). Avoid updating rows by id alone when tables are tenant-scoped.
- **server/google-drive.ts:866** — SELECT from tenant-scoped table 'projects' uses only project id in WHERE (eq(projects.id, projectId)) and does not include tenant scoping in the SQL query.
  - _Fix:_ Add explicit tenant scoping to the query (e.g. where(and(eq(projects.id, projectId), eq(projects.tenantId, expectedTenantId)))) or otherwise restrict the SELECT so it cannot return another tenant's project.
- **server/google-drive.ts:1473** — SELECT from tenant-scoped table 'projects' (during uploadAndShare project resolution) uses only project id in WHERE (eq(projects.id, params.projectId)) without tenant scoping.
  - _Fix:_ Constrain the lookup to the caller's tenant in the SQL WHERE (e.g. and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))) instead of selecting purely by id and checking tenant ownership after the fact.
