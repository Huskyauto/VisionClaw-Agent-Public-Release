# Tenant-Isolation Audit Report

_Generated 2026-08-20T09:36:02.095Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **601/601** file(s) audited · chunks 57/57 OK (0 failed)
- Findings: **269** (CRITICAL 71 · HIGH 160 · MEDIUM 37 · LOW 1)
- Precision second-pass (`gpt-5.4`): **220** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (71)

- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` uses WHERE id = ${activityId} with NO tenant_id constraint — allows modifying rows belonging to other tenants.
  - _Fix:_ Change completeActivity signature to accept tenantId and include it in the WHERE clause, e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}. Always include tenant_id on updates/deletes to tenant-scoped tables.
- **server/agent-eval.ts:218** — UPDATE agent_evals SET ... WHERE id = ${evalId} (no tenant_id constraint). This update can modify eval rows belonging to other tenants.
  - _Fix:_ Include tenant scoping on the update, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}, and/or validate the eval belongs to the tenant before updating. Add tenantId to the function context if needed.
- **server/agentic-features.ts:699** — UPDATE lead_enrichments ... WHERE id = ${lead.id} is executed without tenant scoping — allows updating a lead row belonging to another tenant.
  - _Fix:_ Add tenant scoping to the update, e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}. Prefer multi-column WHERE clauses for all UPDATE/DELETE statements on tenant-scoped tables.
- **server/agentic-features.ts:932** — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} is executed without tenant scoping — can modify another tenant's enrollment row.
  - _Fix:_ Include AND tenant_id = ${params.tenantId} in the WHERE clause (and any other identifying conditions as appropriate). Ensure all UPDATE/DELETE statements on tenant-scoped tables include tenant_id.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals WHERE status='pending' AND expiresAt < ... without scoping by tenant_id — this globally expires approvals across all tenants and later per-run updates rely on that result.
  - _Fix:_ Scope expiry operations by tenant (run per-tenant or accept an optional tenantId and include WHERE tenant_id = ${tenantId}). If a global sweep is required, ensure you carefully handle per-tenant isolation and consider iterating tenants rather than a single unscoped UPDATE.
- **server/agentic/resume-worker.ts:65** — resumePendingApprovalRuns performs a global SELECT across agent_runs and agent_approvals without constraining by tenant_id — the initial candidate query scans rows for all tenants (`FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE (...) OR (...)`) and thus reads tenant-scoped tables unfiltered.
  - _Fix:_ Constrain the query to a specific tenant (or iterate per-tenant) so every read is scoped. For example, accept a tenantId parameter and add `WHERE r.tenant_id = ${tenantId}` (and ensure a.tenant_id = ${tenantId}) or rewrite the logic to run per-tenant. Prefer using Drizzle builders (eg. .where(eq(agentRuns.tenantId, tenantId))) to avoid raw SQL scanning multiple tenants. If a global operator must run across tenants, require an explicit admin/privileged path and strong audit/authorization, but do NOT perform unscoped selects in normal multi-tenant request handlers.
- **server/auto-project.ts:112** — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id (reads messages WHERE conversation_id = ${conversationId} but no AND m.tenant_id = ${tenantId}). This relies on prior conversation checks and does not satisfy the invariant that every tenant-scoped read must include an explicit tenant filter.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. WHERE conversation_id = ${conversationId} AND m.tenant_id = ${tenantId} (or join messages to conversations and ensure the WHERE includes c.tenant_id = ${tenantId}). Ensure all reads of messages include tenant_id in the WHERE clause.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id (SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}). Although conversation ownership is checked earlier, the invariant requires every tenant-scoped read include an explicit tenant_id predicate.
  - _Fix:_ Add an explicit tenant filter to the messages query, e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}, or join messages to conversations and include c.tenant_id = ${tenantId} in the WHERE clause.
- **server/auto-transcript.ts:128** — Subquery references tenant-scoped table `messages` without a tenant_id constraint: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id). This counts messages across tenants unless scoped by tenant, violating tenant isolation.
  - _Fix:_ Scope the subquery by tenant, e.g. (SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) or add an explicit tenant filter using c.tenant_id to ensure per-tenant scoping.
- **server/auto-transcript.ts:153** — Subquery references tenant-scoped table `messages` without a tenant_id constraint in a multi-join query: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id). This may surface cross-tenant message counts and violates the requirement that tenant-scoped queries include tenant_id.
  - _Fix:_ Constrain the subquery by tenant, e.g. (SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id), or otherwise join messages with tenant checks to enforce per-tenant scoping.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table `conversations` lacks a tenant_id WHERE clause: UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL. An UPDATE against a tenant-scoped table must include tenant_id to avoid modifying rows in other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL. Always include tenant_id when performing UPDATE/DELETE on tenant-scoped tables.
- **server/channel-routing.ts:51** — DELETE FROM channel_routes WHERE channel = ${channel} deletes routing rows without scoping to the caller's tenant. This can remove other tenants' routes.
  - _Fix:_ Require tenantId from the caller and include it in the WHERE clause (DELETE ... WHERE channel = ${channel} AND tenant_id = ${tenantId}). Alternatively add tenant_id column to channel_routes if missing and enforce tenant-scoped deletes. Ensure callers cannot delete rows belonging to other tenants.
- **server/channel-routing.ts:54** — INSERT INTO channel_routes (channel, persona_id) VALUES (...) / ON CONFLICT ... does an insert/upsert without tenant scoping and without validating that the provided personaId belongs to the caller's tenant. This allows cross-tenant assignment and conflicts across tenants (unique(channel) is global).
  - _Fix:_ Add tenant_id to channel_routes and include tenant_id in the INSERT (INSERT INTO channel_routes (tenant_id, channel, persona_id) VALUES (${tenantId}, ${channel}, ${personaId})). Use a tenant-scoped unique key (e.g. UNIQUE(tenant_id, channel)) so ON CONFLICT is tenant-local. Before inserting/updating, validate persona ownership by selecting the personas row WHERE id = ${personaId} AND tenant_id = ${tenantId} and reject if not found.
- **server/channel-routing.ts:71** — removeChannelRoute executes DELETE FROM channel_routes WHERE channel = ${channel} with no tenant scoping. This can delete routes for all tenants that use the same channel identifier.
  - _Fix:_ Require a tenantId parameter and delete only the caller's row: DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}. Add tenant_id to channel_routes and use tenant-scoped uniqueness (UNIQUE(tenant_id, channel)). Validate tenant ownership before destructive operations.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` is executed without constraining by tenant_id. The query `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...` omits any tenant filter, relying on prior project lookup instead of enforcing tenant scoping at the query.
  - _Fix:_ Include an explicit tenant constraint on every DB access to tenant-scoped tables. e.g. `... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC` (or use Drizzle: .where(eq(project_files.tenantId, tenantId))). Do not rely solely on earlier verification — each SQL against a tenant-scoped table must include tenant_id in the WHERE clause.
- **server/chat-engine.ts:584** — SELECT from tenant-scoped table `project_notes` is executed without constraining by tenant_id. The query `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...` lacks a tenant_id filter.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or Drizzle .where(eq(project_notes.tenantId, tenantId))). Ensure every read from `project_notes` enforces the caller's tenant.
- **server/chat-engine.ts:633** — SELECT from tenant-scoped table `compaction_archives` is executed without constraining by tenant_id. The query `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...` does not include tenant scoping.
  - _Fix:_ Constrain the query by tenant: `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}` (or include an explicit join to a tenant-scoped parent and check its tenant_id). Always include tenant_id when reading tenant-scoped archives to prevent cross-tenant data leakage.
- **server/chat-engine.ts:651** — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id. The query `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...` lacks an explicit tenant_id filter.
  - _Fix:_ Add tenant scoping to message queries, e.g. `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}` (or Drizzle .where(eq(messages.tenantId, tenantId))). Every read of messages must include the caller's tenant to enforce isolation even when conversation_id was obtained earlier.
- **server/commitment-drafter.ts:232** — Unscoped UPDATE against tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — no tenant_id filter. This can modify a commitment from a different tenant (cross-tenant mutation) if `row.id` collides.
  - _Fix:_ Scope the UPDATE to the owning tenant and guard the state transition. e.g. include tenant id and a tight state guard: `WHERE id = ${row.id} AND tenant_id = ${row.tenantId} AND draft_status = 'draft_pending'`. Prefer using the tenant value from the claimed row (row.tenantId) instead of a caller-supplied/derived value. Failing that, avoid auto-unclaim without a verified tenant and log + skip.
- **server/data-protection.ts:168** — DELETE FROM conversations uses only `id` (DELETE FROM conversations WHERE id = ${conv.id}) with no tenant_id predicate — this can delete conversation rows across tenant boundaries.
  - _Fix:_ Always include tenant_id in DELETE/UPDATE predicates for tenant-scoped tables (e.g. WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}). Alternatively perform the delete inside a per-tenant transaction (withTenantTx) so RLS / app-level context enforces isolation.
- **server/delivery-pipeline.ts:313** — Generic UPDATE against tenant-scoped table `delivery_logs` with no tenant_id constraint (updateDeliveryLog uses .where(eq(deliveryLogs.id, id)) only). This allows an update to affect another tenant's row if an id from another tenant is supplied or if there is any bug/race that obtains a cross-tenant id.
  - _Fix:_ Require and check tenant context on updates. Change updateDeliveryLog to accept tenantId (or infer from a verified prior read) and apply .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). Audit all callers to pass the correct tenantId.
- **server/dfy-intake.ts:93** — UPDATE on tenant-scoped table `dfy_intake_forms` with no tenant_id constraint (submitDfyIntake uses .where(eq(dfyIntakeForms.id, form.id)) only). This allows modifying a form row from a different tenant if an id from another tenant is present.
  - _Fix:_ Include tenant scoping in the UPDATE: .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId))). If form comes from an earlier unscoped query, require mapping/verification of tenant ownership before the update.
- **server/doc-collections.ts:165** — DELETE from tenant-scoped table `doc_chunks` without tenant_id constraint (addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can remove chunks belonging to other tenants sharing the same collection_id/doc_path if collection IDs are not globally constrained.
  - _Fix:_ Add tenant_id to the DELETE predicate: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Ensure collection existence check and all deletes/updates are always tenant-scoped.
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table `doc_chunks` without tenant_id constraint (generateCollectionEmbeddings uses UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}). Although rows were selected earlier filtered by tenant, the UPDATE is issued by id only and lacks an explicit tenant filter, enabling cross-tenant modification if id is manipulated or in race scenarios.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer passing tenantId into this routine and assert ownership before updating.
- **server/google-drive.ts:946** — INSERT into tenant-scoped table 'project_files' does not set tenant_id (the INSERT omits tenant_id entirely). Per schema the tenant_id column is NOT NULL/no default so this can create cross-tenant rows or violate invariants.
  - _Fix:_ Always include tenant_id explicitly in INSERTs into tenant-scoped tables (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${projectId}, ...)). Prefer deriving tenantId from the trusted execution context and ensure ownership before inserting.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table "code_proposals" without a tenant_id constraint — this will modify rows for all tenants matching the id list, potentially affecting other customers.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE tenant_id = <tenantId> AND id = ANY(...)) or perform updates per-tenant after verifying ownership. Prefer using a storage/data-access function that enforces tenant isolation server-side.
- **server/health-audit.ts:368** — UPDATE on tenant-scoped table "heartbeat_tasks" without a tenant_id constraint — this can disable/modify heartbeat tasks across tenants.
  - _Fix:_ Require tenant scoping on UPDATEs (e.g. WHERE tenant_id = <tenantId> AND id = ANY(...)) or use a DB/storage API that enforces tenant ownership checks before mutating rows.
- **server/memory-graph.ts:253** — INSERT INTO memory_links (...) VALUES (...) does not set tenant_id. If memory_links is tenant-scoped, this INSERT creates a cross-tenant row with no explicit tenant ownership, violating the platform rule that every INSERT must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the inserted columns and values (e.g. INSERT INTO memory_links (tenant_id, source_memory_id, ...) VALUES (${tenantId}, ${newMemoryId}, ...)). Additionally validate that both source and target memory rows belong to the same tenant (ownership check) before inserting links.
- **server/memory-intelligence.ts:399** — UPDATE against tenant-scoped table `knowledge_triples` omits tenant filter. The code updates rows by id only: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}` — no `AND tenant_id = ${tenantId}`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. add `AND tenant_id = ${tenantId}` to the WHERE clause (or use a parameterized tx scoped to the tenant). Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id to prevent cross-tenant modifications.
- **server/minerva-planner.ts:472** — Rollback UPDATE on `plans` lacks tenant constraint: `UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'` (no `tenant_id` filter). This can modify another tenant's row if tenantId was omitted earlier.
  - _Fix:_ Include tenant scoping on all UPDATE/DELETE statements. Change the WHERE clause to include `AND tenant_id = ${args.tenantId}` (and ensure `args.tenantId` is present/validated before executing). Consider failing closed if tenantId is missing rather than performing an unscoped modification.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table 'oauth_subscriptions' without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants.
  - _Fix:_ Scope the query to a known tenant context (e.g. WHERE tenant_id = ${tenantId}) or, if this is an admin/global startup job that must iterate tenants, enumerate and authorize tenant ids explicitly then run per-tenant queries. Consider minimizing returned columns or performing this work in an admin-only process with strict access controls. For stronger guarantees, implement Postgres RLS to enforce tenant isolation at the DB level.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This enumerates tenant rows across all tenants.
  - _Fix:_ Avoid scanning oauth_subscriptions across tenants. If you need to discover tenants to auto-reconnect, obtain an explicit, authorized list of tenant ids to process and run per-tenant queries (WHERE tenant_id = ...). Alternatively restrict this logic to an admin-only routine that explicitly documents and protects cross-tenant access. Consider DB-level RLS as an additional safeguard.
- **server/oauth-subscriptions.ts:1258** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) inside proactiveTokenRefresh. This reads active subscriptions for all tenants.
  - _Fix:_ Change to per-tenant refresh scheduling: iterate an explicit, authorized tenant list and run tenant-scoped queries (WHERE tenant_id = ${tenantId}). If a global scan is required for an admin job, make it explicit, protected, and minimize returned sensitive columns. Implement DB-enforced tenant isolation (RLS) to prevent accidental cross-tenant reads.
- **server/oauth-subscriptions.ts:1328** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE) inside forceRefreshAllSubscriptions. This enumerates all active subscriptions across tenants.
  - _Fix:_ Do not perform unscoped reads of tenant-scoped tables. If forcing refreshes must run across tenants, obtain an explicit, authorized tenant list and perform per-tenant queries (WHERE tenant_id = ${tenantId}). Otherwise restrict this function to run with explicit admin privileges and audit/log access. Consider adding DB-level protections (RLS) to enforce tenant scoping.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} has no tenant_id constraint. personas is tenant-scoped; this allows reading personas across tenants (data leakage) and exporting another tenant's persona.
  - _Fix:_ Require and use tenantId when selecting the persona: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. Also validate the returned row belongs to the caller's tenant before continuing. Reject the operation if tenant mismatch occurs.
- **server/plan-executor.ts:116** — appendExecutionLog updates plans.execution_log using only WHERE id = ${planId} (no tenant_id constraint). This allows modifying another tenant's plan row by id.
  - _Fix:_ Include the plan's tenant constraint in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${planTenantId}) or otherwise verify plan ownership before updating. Prefer passing the authoritative tenantId into appendExecutionLog and use parameterized comparisons rather than relying on id alone.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' ... WHERE id = ${planId} — no tenant_id constraint when marking a plan failed due to missing steps.
  - _Fix:_ Restrict the update to the owning tenant (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId}). Verify the plan's tenant before any status-changing write to ensure you cannot alter another tenant's plan.
- **server/plan-executor.ts:513** — CAS claim query updates plan status to 'executing' using WHERE id = ${planId} AND status = 'approved' but lacks tenant_id constraint. This can cause one tenant/process to claim and execute a plan belonging to another tenant.
  - _Fix:_ Include tenant scoping in the CAS (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved') or resolve and validate the plan's tenant prior to claiming. Always CAS including both id and tenant_id for tenant-scoped tables.
- **server/plan-executor.ts:728** — Final UPDATE of plans to set status = ${finalStatus} uses WHERE id = ${planId} without tenant_id constraint, allowing cross-tenant status changes.
  - _Fix:_ Add tenant validation to the WHERE clause (WHERE id = ${planId} AND tenant_id = ${planTenantId}) and/or re-fetch and assert the plan's tenant before writing. Ensure every write to the plans table includes the tenant filter.
- **server/plan-executor.ts:778** — resumeStuckPlans resets all plans stuck in 'executing' to 'approved' with UPDATE ... WHERE status = 'executing' AND updated_at < ... — no tenant_id filter. This will affect every tenant's executing plans globally.
  - _Fix:_ Restrict this recovery operation to a single tenant at a time (e.g. accept a tenantId param and include WHERE tenant_id = ${tenantId}) or iterate per-tenant. If intended to be global admin-only, require and validate an explicit admin/trusted context before making cross-tenant changes.
- **server/plan-executor.ts:787** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is used to kick pending plans but lacks tenant scoping, so the executor may pick up and run plans from other tenants.
  - _Fix:_ Query approved plans in a tenant-scoped way (e.g. WHERE status = 'approved' AND tenant_id = ${tenantId}) or implement per-tenant worker logic. If the executor must operate across tenants, require an explicit admin context and limit the operation to known/authorized tenant ids.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write against a tenant-scoped table without constraining tenant_id. Although the row was selected earlier with tenant_id, the UPDATE lacks tenant scoping and could affect another tenant's task with the same id.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or verify ownership before mutating. Prefer parameterized Drizzle .where(eq(table.tenantId, tenantId)) to enforce isolation.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates agent_desks without tenant scoping. This can modify another tenant's agent desk record.
  - _Fix:_ Include tenant_id in the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or validate the desk's tenant before updating. All mutations on agent_desks must be constrained by tenant_id.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates a tenant-scoped table without a tenant_id constraint, allowing cross-tenant modification of rule metadata.
  - _Fix:_ Add tenant scoping to this UPDATE (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or pass/validate the rule's tenantId before mutating. Never update tenant-scoped rows by id alone.
- **server/recurring-messages.ts:258** — SELECT FROM tenant-scoped table agent_knowledge without tenant_id constraint (reads rows for all tenants). The cron runner issues: SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant. If this is intended to be a global system reader, make that explicit (an _allowGlobalSystemUseOnly flag) and keep the query parameterized and audited; otherwise never SELECT tenant-scoped rows without a tenant_id filter. Prefer per-tenant queries or a safe, explicit global-path that enforces owner checks before any action.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table agent_knowledge without tenant_id constraint (UPDATE agent_knowledge SET content=..., updated_at=NOW() WHERE id = ${r.id}). This can modify a row belonging to a different tenant if IDs are guessable or leaked.
  - _Fix:_ Add tenant scoping in the WHERE clause (e.g. AND tenant_id = ${r.tenant_id} or AND tenant_id = ${callerTenantId}) so the UPDATE only affects the intended tenant's row. Ensure any system/global runner only performs updates when tenant ownership is known and enforced in the same statement.
- **server/research-engine.ts:165** — UPDATE research_sessions executed without tenant_id constraint in cleanupZombieSessions: UPDATE research_sessions SET ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold} — this will affect sessions across all tenants.
  - _Fix:_ Always scope updates to the relevant tenant (e.g. add AND tenant_id = ${tenantId}). If this cleanup is intended to be global, make the global intent explicit and add additional authorization/audit checks. Prefer performing per-tenant cleanup or include tenant(s) in the parameterized query rather than leaving tenant_id unbounded.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() — no tenant_id constraint. This will delete autoresearch entries for every tenant.
  - _Fix:_ Add tenant scoping to DELETE (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()), or iterate per-tenant. If the intent is a global purge, require an explicit, auditable admin/global flag and ensure the action is authorized and logged.
- **server/routes/conversations.ts:103** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id.
  - _Fix:_ Always set tenant_id explicitly on INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})) or use a storage/ORM method that enforces the tenant column.
- **server/routes/conversations.ts:229** — INSERT into 'project_notes' does not include tenant_id (tenant-scoped table write without explicit tenant column).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, 'system:archive')) or use a tenant-aware storage API.
- **server/seed.ts:2163** — UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} performs a write to a tenant-scoped table without constraining by tenant_id. Updating by id only can modify another tenant's row if ids overlap across tenants.
  - _Fix:_ Always include tenant_id in the WHERE clause for updates to tenant-scoped tables (e.g. WHERE id = ${p.id} AND tenant_id = ${p.tenantId}). Ensure the initial SELECT fetched tenant_id so you can safely constrain the UPDATE. Alternatively, use Drizzle's .where(eq(table.tenantId, tenantId)) patterns to enforce scoping.
- **server/seed.ts:3882** — DELETE FROM heartbeat_tasks executed without any tenant_id restriction — this deletes matching rows across ALL tenants.
  - _Fix:_ Restrict the DELETE to a specific tenant (e.g. WHERE tenant_id = <tenantId> AND ...) or, if the intent is a system-wide migration, iterate tenants explicitly and perform the delete per-tenant. Avoid blanket DELETEs against tenant-scoped tables.
- **server/self-improvement.ts:243** — runExperiment() updates the `experiments` row via db.update(...).where(eq(experiments.id, experiment.id)) without constraining by tenant_id. This allows an update against an experiments row by id without double-checking the tenant owner.
  - _Fix:_ Add tenant scoping to the UPDATE (and the later error-UPDATE). For example: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Alternatively, fetch-and-validate that the experiment belongs to the caller's tenant before updating.
- **server/self-improvement.ts:413** — In the catch block of runExperiment() the error-path UPDATE on `experiments` also uses .where(eq(experiments.id, experiment.id)) with no tenant constraint.
  - _Fix:_ Same fix as above: include eq(experiments.tenantId, config.tenantId) in the WHERE for this UPDATE or validate ownership before performing the UPDATE to ensure tenant isolation.
- **server/skillify.ts:91** — SELECT from tenant-scoped table 'messages' without constraining tenant_id (reads conversation messages via messages.conversationId but omits tenant filter). This can allow cross-tenant data disclosure if conversation IDs are guessable or re-used.
  - _Fix:_ Constrain the query by tenant. Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or use Drizzle's eq(messages.tenantId, tenantId). Example: db.select().from(messages).where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))).
- **server/skillify.ts:202** — SELECT from tenant-scoped table 'messages' without tenant_id constraint (analyzeConversation reads messages by conversationId but does not include tenant scoping).
  - _Fix:_ Include an explicit tenant filter on the messages query. Use a conjunctive WHERE that includes the caller's tenantId (or eq(messages.tenantId, tenantId) via Drizzle). Do not rely on earlier lookups — every tenant-scoped read must include tenant_id in SQL.
- **server/skillify.ts:223** — SELECT from tenant-scoped table 'personas' without tenant_id constraint (lookup by persona id only). This can leak persona data across tenants.
  - _Fix:_ Constrain the personas lookup to the tenant: add AND persona.tenant_id = ${tenantId} (or eq(personas.tenantId, tenantId)). If persona rows can be shared intentionally, document that explicitly; otherwise enforce ownership checks before returning persona data.
- **server/skillify.ts:329** — SELECT from tenant-scoped table 'messages' (recentMsgs in skillifyConversation) without tenant_id constraint. The recent messages selection uses only conversationId and lacks tenant scoping.
  - _Fix:_ Add tenant scoping to the messages SELECT used to build the excerpt. For example: db.select().from(messages).where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))).orderBy(...).limit(...). Ensure every read against tenant-scoped tables includes the tenant_id filter.
- **server/storage.ts:876** — UPDATE memory_entries SET status='archived' WHERE ... (archiveExpiredMemories) runs without any tenant_id predicate — this mutates rows for all tenants in bulk.
  - _Fix:_ This is a destructive, cross-tenant mutation. Restrict it: either run per-tenant (iterate tenants and include eq(memory_entries.tenantId, tenantId) in the WHERE) or require an explicit admin-only flag and audit logs. Never issue blanket UPDATEs on tenant-scoped tables without explicit tenant scoping and authorization.
- **server/storage.ts:892** — UPDATE memory_entries SET status='archived' WHERE created_at <= ${cutoff} ... (archiveStaleMemories) executes with no tenant_id constraint — bulk cross-tenant mutation.
  - _Fix:_ Same as above: restrict to explicit tenant-scoped updates or require admin-only execution. Add eq(memory_entries.tenantId, tenantId) or iterate tenants and ensure strong authorization and logging before performing bulk updates.
- **server/storage.ts:1346** — getAllDataForExport performs SELECTs from many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with no tenant filters — returns data for all tenants in bulk.
  - _Fix:_ Export operations that span tenants must be explicit, admin-only, and strongly authenticated/authorized. Require an explicit admin flag or a tenantId parameter; if multi-tenant export is needed, implement per-tenant export with explicit tenant scoping or require the ALL_TENANTS sentinel plus strict access controls and logging.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table felix_proposals (stampArgsEmbedding) updates args_embedding WHERE id = ${proposalId} but omits a tenant_id constraint. This can modify a row belonging to another tenant if ids are guessed or collision occurs.
  - _Fix:_ Include tenant scoping in the UPDATE predicate and/or accept tenantId into stampArgsEmbedding (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure every UPDATE/DELETE against felix_proposals includes AND tenant_id = <caller tenant>.
- **server/surprise-scorer.ts:139** — UPDATE sets actual_outcome_embedding WHERE id = ${proposalId} with no tenant_id filter. This writes to a tenant-scoped row without constraining by tenant.
  - _Fix:_ Add tenant scoping: include AND tenant_id = ${tenantId} (pass tenantId into this function) or otherwise validate the proposal belongs to the expected tenant before updating.
- **server/surprise-scorer.ts:149** — SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} has no tenant_id constraint; re-fetch is used to drive later logic and subsequent updates are not tenant-scoped.
  - _Fix:_ Re-query with tenant scoping (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or pass the tenantId through so reads cannot observe another tenant's row.
- **server/surprise-scorer.ts:181** — UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} lacks a tenant_id filter, causing tenant-unsafe write.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) and ensure all branches that update felix_proposals are tenant-scoped.
- **server/surprise-scorer.ts:212** — UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} is missing tenant_id scoping and will update proposals across tenants.
  - _Fix:_ Constrain the write by tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Audit the whole function to ensure every SELECT/UPDATE/INSERT on felix_proposals uses tenant_id = <caller tenant>.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table knowledge_triples omits tenant scoping — the UPDATE uses WHERE id = ${row.id} with no tenant_id constraint, allowing cross-tenant mutation if an attacker can guess an id.
  - _Fix:_ Include tenant scoping on the UPDATE (and any other mutations) so only the caller's tenant row can be modified. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}`); — or otherwise ensure the WHERE clause includes tenant_id = ${tenantId} (and keep all SQL parameterized).
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without filtering by tenant_id (reads all pairing rows across tenants).
  - _Fix:_ Do not scan `agent_knowledge` across tenants. Add an explicit tenant ownership filter (or a tenant_id column) and only SELECT rows for the expected tenant, or redesign pairing storage so phone→conversation mapping includes a dedicated tenant_id column. If ambiguity detection across tenants is required, perform a controlled join against `conversations` (or other canonical ownership table) and fail-closed when multiple tenant owners are found; never return or iterate arbitrary cross-tenant rows to application logic.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} executes without enforcing the caller's tenant constraint (reading a tenant-scoped table by id only).
  - _Fix:_ Verify ownership using a WHERE clause that includes the expected tenant (or pass the caller's tenantId into this function). For example, JOIN agent_knowledge to conversations and filter WHERE c.id = ${conversationId} AND c.tenant_id = ${expectedTenantId}, or perform SELECT ... FROM conversations WHERE id = ${conversationId} AND tenant_id = ${expectedTenantId} and treat missing rows as a failed verification. Avoid blind selects by id that can reveal or act on rows belonging to other tenants.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} updates a tenant-scoped table by id only, with no tenant_id constraint — allows modifying another tenant's ledger row if an id is misused.
  - _Fix:_ Include tenant ownership in the UPDATE predicate. Change the API to accept (or derive) the tenantId for the reservation and use WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Alternatively first SELECT the row and assert its tenant_id equals the expected tenant before performing the UPDATE, then include tenant_id in the WHERE clause for the write (defense-in-depth).
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Same fix as settleVentureReservation: require/accept the tenantId and perform the UPDATE using WHERE id = ${reservationId} AND tenant_id = ${tenantId} (or SELECT+assert ownership then UPDATE with tenant filter). Ensure every UPDATE/DELETE against tenant-scoped tables includes tenant_id in the WHERE clause.

### HIGH (160)

- **server/ab-optimizer.ts:110** — Global SELECT from tenant-scoped table `experiments` has no tenant_id constraint (reads rows across all tenants).
  - _Fix:_ Scope the query to a tenant. Either run this heartbeat per-tenant or add a WHERE tenant_id = ${<tenantId>} clause (or iterate tenant ids and query per-tenant). Ensure every read only returns rows belonging to the caller's tenant.
- **server/agent-activity.ts:60** — SELECT from `personas` does not constrain by tenant_id (getPersonality(personaId) reads persona without verifying tenant ownership).
  - _Fix:_ Require and pass tenantId into getPersonality and include a WHERE tenant_id = ${tenantId} clause (or otherwise verify the persona belongs to the caller's tenant) before returning persona data.
- **server/agent-desk.ts:297** — SELECT id, name FROM personas is executed without a tenant_id filter when building desks overview — reads global persona rows rather than tenant-scoped ones.
  - _Fix:_ Scope the personas query by tenant (e.g. SELECT id,name FROM personas WHERE tenant_id = ${tenantId} ...) or otherwise fetch only personas belonging to the caller's tenant. Pass tenantId into the query and avoid global persona reads.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs from tenant-scoped table `ai_insights` without any tenant_id constraint — a global sweep reads rows across tenants.
  - _Fix:_ Either run this sweep per-tenant (accept a tenantId and include WHERE tenant_id = ${tenantId}) or add tenant scoping to the SELECT. Avoid reading or acting on other tenants' insights in a single global statement.
- **server/agentic-engines.ts:374** — SELECT name, role FROM personas WHERE is_active = true is executed without a tenant_id constraint — reads personas across tenants.
  - _Fix:_ Scope the personas query by tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true). Ensure any persona reads are filtered by the caller's tenant to avoid cross-tenant access.
- **server/agentic-features.ts:773** — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' has NO tenant_id constraint — reads enrollments across tenants and can allow cross-tenant enrollment checks.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause when checking for existing enrollments. Always constrain SELECTs on tenant-scoped tables to the caller's tenant.
- **server/agentic/department-budgets.ts:143** — SELECT on tenant-scoped table `department_budgets` without a tenant_id WHERE filter (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads rows across all tenants from a tenant-scoped table.
  - _Fix:_ If this is intended as an explicit admin/global sweep, make that intent explicit and gate the function with a strong admin authorization check and audit logging. Otherwise restrict the query to a single tenant by adding a tenant_id filter (e.g. WHERE tenant_id = ${tenantId} AND status = 'active'), or iterate tenants from a vetted admin list. Avoid unbounded cross-tenant SELECTs on tenant-scoped tables.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on agent_wake_schedules claims rows via an inner SELECT that lacks any tenant_id constraint. The statement updates/returns wakes across all tenants (no WHERE tenant_id = ...), allowing cross-tenant processing/leakage of tenant-scoped rows.
  - _Fix:_ Restrict the claim/update to a single tenant or otherwise include tenant scoping. Options: (a) runDueWakes should accept a tenantId and add WHERE tenant_id = ${tenantId} to the inner SELECT/outer UPDATE, or (b) iterate per-tenant and claim wakes within each tenant, ensuring every SELECT/UPDATE on agent_wake_schedules includes AND tenant_id = <tenant>. Use parameterized bindings (drizzle) rather than string interpolation.
- **server/auto-asset-capture.ts:184** — Background backfill SELECT queries scan the conversations table without a tenant_id constraint (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...). This reads tenant-scoped conversation rows across all tenants.
  - _Fix:_ Limit the backfill to a tenant context or iterate tenants and add WHERE tenant_id = ${tenantId} to the query. Ensure every read of conversations (and any tenant-scoped tables) includes an explicit tenant_id filter, or otherwise perform the work with an explicit, authenticated tenant context before touching related resources.
- **server/business-tools.ts:96** — INSERT into invoice_items does not include tenant_id (INSERT INTO invoice_items (invoice_id, description, ...) VALUES (${newId}, ...)). If invoice_items is a tenant-scoped table (or relied-on for isolation), omitting tenant_id allows cross-tenant item rows tied only by invoice_id and can break the app-level isolation invariant.
  - _Fix:_ Ensure tenant_id is persisted on every tenant-scoped INSERT. Either (A) add tenant_id to the invoice_items table and include tenant_id = ${tid} in the INSERT, or (B) verify the referenced invoice (newId) belongs to the caller's tenant before inserting and include tenant_id in the row. Prefer explicit tenant_id columns (NOT NULL) on child tables and set them in the same transaction to enforce application-level isolation.
- **server/capability-review.ts:491** — UPDATE to tenant-scoped table `capability_reviews` does not constrain by tenant_id. markReviewReused() issues `UPDATE capability_reviews SET reused = true ... WHERE id = ${pending.reviewId} AND reused = false` — updating by id alone can modify another tenant's row with the same numeric id.
  - _Fix:_ Include tenant ownership in the WHERE clause and/or track tenantId in the in-memory pending entry. Persist tenant_id on insert (already done) and change the UPDATE to include AND tenant_id = ${pending.tenantId}. Example: `UPDATE capability_reviews SET reused = true, reused_capability = ${...} WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false`. Also ensure pendingReuseByConversation stores the tenantId when created and validate it before performing the update (fail-safe: no update if tenantId is missing). This prevents cross-tenant attribution/tainting.
- **server/channel-routing.ts:30** — SELECT joins tenant-scoped table `personas` (p) without constraining by tenant. The query SELECT cr.*, p.name as persona_name FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id WHERE cr.enabled = true can return personas from other tenants.
  - _Fix:_ Accept a tenantId parameter (or derive it from caller context) and add tenant filters: e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = :tenantId and WHERE cr.tenant_id = :tenantId (or use Drizzle .where(eq(table.tenantId, tenantId))). Ensure both channel_routes and personas rows are scoped to the caller's tenant.
- **server/channel-routing.ts:63** — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true reads tenant-scoped data without a tenant filter. Without scoping, it can return another tenant's persona_id.
  - _Fix:_ Require the caller's tenantId and add a tenant filter to the query (e.g. WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId}). If channel_routes currently lacks a tenant_id column, add it and migrate rows accordingly before enforcing filters.
- **server/chat-engine.ts:4446** — storage.attachCitations(...) is called without passing tenant context. This likely performs INSERT/UPDATE against tenant-scoped citation rows using only a message id, so the DB write may omit or not constrain tenant_id permitting cross-tenant writes/association.
  - _Fix:_ Ensure tenant isolation when attaching citations: pass the caller's tenant id (e.g. tenantIdForScope) into attachCitations and/or change attachCitations to require a tenantId parameter. Inside the storage implementation, enforce WHERE tenant_id = ? on any SELECT/UPDATE/DELETE and set tenant_id explicitly on INSERTs. Additionally, verify ownership of the referenced message id (that the message belongs to the same tenant) before attaching citations (e.g. SELECT message WHERE id = ? AND tenant_id = ? -> fail if no row).
- **server/data-protection.ts:150** — SELECT on tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across tenants.
  - _Fix:_ Avoid unscoped reads against tenant-owned tables. Either iterate per-tenant (query tenant list and call a tenant-scoped query inside withTenantTx), or include an explicit tenant predicate (WHERE tenant_id = <tenant>). For admin-wide purge jobs, run each tenant in a tenant-scoped transaction (withTenantTx) so isolation is preserved.
- **server/deep-interview.ts:337** — INSERT INTO project_notes(...) SELECT p.id, ${briefContent}, NOW() ... does not set tenant_id on the inserted project_notes row. Tenant ownership is not being written explicitly into the child row.
  - _Fix:_ Always write tenant_id explicitly on inserts into tenant-scoped tables. Change the INSERT to include tenant_id (INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() ...) or otherwise ensure the table's tenant_id is populated from a verified source (and not derived from untrusted input).
- **server/delivery-pipeline.ts:1252** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId with no tenant_id constraint (getDeliveryByStripePayment uses where(eq(deliveryLogs.stripePaymentId, paymentId)) only). This can leak another tenant's delivery row when a stripePaymentId is supplied from untrusted sources.
  - _Fix:_ Require caller-provided tenantId (or resolve tenant ownership) and include eq(deliveryLogs.tenantId, tenantId) in the WHERE. If this lookup must be global, ensure the stripePaymentId is a trusted, tenant-scoped value or validate ownership before returning any data.
- **server/dfy-intake.ts:50** — SELECT from tenant-scoped table `dfy_intake_forms` by unscoped token (getDfyIntakeByToken queries by token only, no tenant_id filter). Although token is intended as an authorization artifact, the DB read is not constrained by tenant_id per the project's isolation invariant.
  - _Fix:_ Either: (a) store/validate the token together with tenantId and require a tenantId param (and filter on tenant_id), or (b) verify and map the token to an explicit tenant before returning rows. Do not perform tenant-scoped reads without asserting tenant ownership at the SQL level.
- **server/email.ts:470** — Raw SELECT against tenant-scoped table `inbox_messages` without tenant_id filter: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1` (pollSharedInbox). This reads a tenant-scoped table globally and can return rows for other tenants.
  - _Fix:_ Scope the lookup to the resolved tenant. Determine the tenant for this message before the existence check (e.g. derive tenantId from the message/inbox address or mapping) and change the query to include AND tenant_id = ${tenantId}. If the intent is global admin maintenance, make that explicit and require an ADMIN_TENANT_ID check or separate admin path.
- **server/email.ts:515** — Raw SELECT against tenant-scoped table `inbox_messages` with no tenant filter: `SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...)` (backfillEmptyBodies). This scans/reads all tenants' inbox_messages.
  - _Fix:_ Backfill should run per-tenant or be explicit admin-only. Add a tenant parameter and include WHERE tenant_id = ${tenantId} (or otherwise restrict to admin-run context). Ensure maintenance code never scans tenant data cross-tenant.
- **server/email.ts:528** — Raw UPDATE against tenant-scoped table `inbox_messages` without tenant constraint: `UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id}` (backfillEmptyBodies). This can update rows belonging to other tenants if an id is ambiguous or attacker controls ids.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), and ensure backfillEmptyBodies receives/derives tenantId for each row or is run per-tenant. If run as a global maintenance job, require an explicit admin guard and audit log.
- **server/embeddings.ts:748** — UPDATE via storeEmbeddingVec executes `UPDATE ${sql.raw(table)} SET embedding_vec = ..., embedding = ... WHERE id = ${id}` with no tenant filter. memory_entries and agent_knowledge are tenant-scoped: updating by id only may modify another tenant's row.
  - _Fix:_ Require and pass a tenantId into storeEmbeddingVec, and add `AND tenant_id = ${tenantId}` to the WHERE clause. Validate that the caller has ownership before updating. If the operation must be global (admin), make that explicit and gated.
- **server/embeddings.ts:751** — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in backfillMissingKnowledgeEmbeddings: `SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL AND source = 'autoresearch' ...`. This will return rows across tenants.
  - _Fix:_ Backfill should be scoped per-tenant (accept tenantId) or executed under an explicit admin context. Add `AND tenant_id = ${tenantId}` to the query or run per-tenant loop. Never perform cross-tenant reads on tenant-scoped tables without an explicit admin-only guard.
- **server/embeddings.ts:780** — SELECT from tenant-scoped table `memory_entries` without tenant filter in backfillEmbeddingVecs: `SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200`. This reads rows from all tenants.
  - _Fix:_ Scope the backfill to a tenantId (or iterate tenants explicitly) and add `AND tenant_id = ${tenantId}` to the WHERE. Ensure subsequent UPDATE uses the same tenantId to avoid cross-tenant writes.
- **server/embeddings.ts:790** — UPDATE in backfillEmbeddingVecs: `UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}` has no tenant constraint, allowing updates to tenant rows by id only.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) and ensure the function runs with an explicit tenantId (or as an audited admin job).
- **server/embeddings.ts:796** — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in backfillEmbeddingVecs: `SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200`. This will read rows for all tenants.
  - _Fix:_ Backfill should be scoped to a tenant (accept tenantId) or run per-tenant. Add `AND tenant_id = ${tenantId}` to the WHERE clause or require an explicit admin guard and audit logging.
- **server/embeddings.ts:807** — UPDATE in backfillEmbeddingVecs: `UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}` executes without tenant constraint and can update another tenant's row by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) and ensure the selection and update are performed under the same tenant context. If intended as global admin maintenance, require an explicit admin-only path and document/audit it.
- **server/felix-brain.ts:19** — rehydrateTaskStateOnBoot queries agent_runs without constraining tenant_id, allowing loading of in-flight runs from all tenants into the single in-memory taskStateStore. This can leak cross-tenant state and mix conversation/task state between tenants.
  - _Fix:_ Restrict the SELECT to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}) or rehydrate per-tenant: accept a tenantId parameter and iterate tenants explicitly. Alternatively namespace the in-memory keys by tenant (e.g. `${tenantId}:${conversationId}`) and only load rows for tenants you intend to rehydrate. Ensure any future queries against agent_runs include a tenant_id WHERE clause or are otherwise scoped to the authenticated tenant.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' by project id with NO tenant_id constraint (reads a foreign id supplied by caller without scoping).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or otherwise ensure the SELECT is performed using the caller's tenant before returning any project fields.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' (setting driveFolderId/driveFolderUrl) uses WHERE projects.id = projectId but does NOT include tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or perform the UPDATE through a statement that includes the validated tenantId to prevent cross-tenant writes.
- **server/google-drive.ts:827** — Handler reads the 'projects' row by caller-supplied projectId without constraining by tenant_id or otherwise proving caller ownership first — risk of cross-tenant data exposure/manipulation.
  - _Fix:_ Either include tenant scoping in the initial SELECT (WHERE projects.id = ${projectId} AND projects.tenant_id = ${callerTenantId}) or perform an explicit ownership check tied to the caller's tenant before reading project details.
- **server/google-drive.ts:1004** — SELECT from tenant-scoped table 'projects' to validate a projectId is used without scoping by tenant_id (reads a foreign id from caller input with no tenant WHERE).
  - _Fix:_ Scope the SELECT by tenant (WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId}) or otherwise deny/abort until ownership is proven. Avoid selecting project rows using only projectId supplied by caller.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table "code_proposals" without constraining by tenant_id — this will return rows across all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = <tenantId> or use Drizzle .where(eq(code_proposals.tenantId, tenantId))). If the intent is to operate across tenants, iterate tenants explicitly and run the query per-tenant after authorization/approval.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table "heartbeat_tasks" without constraining by tenant_id — this can return tasks belonging to other tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = <tenantId>) or run the query per authorized tenant. Avoid global SELECTs on tenant-scoped tables unless explicitly intended and approved.
- **server/heartbeat.ts:105** — SELECT * FROM research_schedules is reading a tenant-scoped table with no tenant_id constraint in the WHERE clause.
  - _Fix:_ If research_schedules is tenant-scoped, include tenant scoping (WHERE tenant_id = ${...}) or iterate tenant IDs via a safe tenant metadata table and then query per-tenant with WHERE tenant_id = <that tenant>. Do NOT rely on id-only filtering to prevent cross-tenant access.
- **server/heartbeat.ts:155** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} updates a tenant-scoped table without constraining by tenant_id in the WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) so a task cannot accidentally modify another tenant's row by id.
- **server/heartbeat.ts:160** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id} lacks tenant scoping in the WHERE clause.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via a storage helper that enforces tenant ownership.
- **server/heartbeat.ts:165** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} missing tenant constraint.
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}), or perform per-tenant updates through a storage API that enforces tenant checks.
- **server/heartbeat.ts:173** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} executed in an error-recovery path without tenant scoping.
  - _Fix:_ Same fix: include tenant_id in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or use a storage-layer helper that enforces tenant ownership, even in error handlers.
- **server/heartbeat.ts:438** — SELECT * FROM event_log WHERE status = 'pending' ... reads event_log (which contains tenant_id) without a tenant filter — this returns events across tenants.
  - _Fix:_ If this worker must process events cross-tenant, explicitly enumerate tenants from a secure tenant metadata table and then query event_log per-tenant (WHERE tenant_id = <tid> AND status = 'pending') OR include tenant_id in the WHERE clause. Never rely on id-only or global SELECTs against tenant-scoped tables.
- **server/heartbeat.ts:325** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} updates a tenant-scoped table (heartbeat_tasks) without constraining by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform this mutation via storage/updateHeartbeatTask which enforces tenant ownership.
- **server/heartbeat.ts:329** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} (model normalization) lacks a tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call the storage abstraction that enforces tenant boundaries.
- **server/heartbeat.ts:2817** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates heartbeat_tasks without constraining by tenant_id after creating a task.
  - _Fix:_ Use WHERE id = ${newTask.id} AND tenant_id = ${tenantId} (the tenant that owns newTask) or call a storage-layer updater that validates tenant ownership. Ensure approval_status cannot be set for another tenant's task via id-only updates.
- **server/heartbeat.ts:3269** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) lacks tenant scoping in the WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage API that enforces tenant scoping to set approval_status.
- **server/heartbeat.ts:2588** — storage.getPersonas() is called with no tenant context (proofPersonas) and the code then selects a persona by name and uses its id for task creation — no verification that the persona belongs to the tenant.
  - _Fix:_ Call storage.getPersonas(tenantId) or otherwise filter personas by tenant_id before selecting one. Always verify that any persona id or object belongs to the caller/tenant before using it in tenant-scoped writes.
- **server/heartbeat.ts:2723** — processDelegations calls storage.getPersonas() (allPersonas) without a tenant filter, then assigns tasks to personas by id/name without verifying tenant ownership.
  - _Fix:_ Fetch personas scoped to the tenant (or validate persona.tenantId === tenantId) before using persona ids in tenant-scoped operations. Use storage APIs that accept tenantId to ensure correct isolation.
- **server/heartbeat.ts:2915** — delegateTaskFromChat calls storage.getPersonas() without tenant context and can select a target persona across tenants, then uses that persona.id in a tenant-scoped task creation.
  - _Fix:_ Filter personas by tenant (e.g. storage.getPersonas(tenantId)) or validate the chosen persona belongs to tenantId before creating tasks that reference persona.id. Never trust global persona lists when performing tenant-scoped writes.
- **server/job-queue.ts:98** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id filter — the worker may atomically claim and mutate jobs across tenants.
  - _Fix:_ Scope claims to a specific tenant (add a tenantId parameter and include WHERE tenant_id = ${tenantId}), or explicitly document+enforce that this is a global operator-only table. Prefer per-tenant claiming or validate the job's tenant before mutating to avoid cross-tenant effects.
- **server/job-queue.ts:134** — reclaimExpiredLeases STEP 1 updates agent_jobs to 'failed_terminal' without a tenant_id constraint — it mutates rows across all tenants.
  - _Fix:_ Make reclaimExpiredLeases operate per-tenant (accept tenantId or iterate tenants) or add tenant_id = ${tenantId} to the WHERE clause. Ensure any global operator action is guarded and audited so one tenant cannot affect another's jobs.
- **server/job-queue.ts:148** — reclaimExpiredLeases STEP 2 requeues expired agent_jobs (UPDATE ... SET status='pending') with no tenant_id filtering — this touches all tenants' rows.
  - _Fix:_ Restrict the UPDATE to a tenant scope (add tenant_id = ${tenantId}) or otherwise isolate requeue logic per-tenant. If a global sweep is intended, require an operator-only code path and additional safeguards.
- **server/job-queue.ts:172** — completeJob updates agent_jobs to 'succeeded' using only id/attempts in the WHERE clause (no tenant_id) — a caller can complete a job id belonging to another tenant.
  - _Fix:_ Require the caller to validate/submit the tenantId and include WHERE tenant_id = ${tenantId} in the UPDATE (or fetch and verify the job's tenant before writing) to enforce tenant isolation and lease-fencing.
- **server/job-queue.ts:220** — failJob SELECTs max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — this reads tenant-scoped data by bare id.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT or require the function to accept/verify tenantId prior to the lookup. Never read tenant-scoped rows by id alone without verifying the caller's tenant.
- **server/job-queue.ts:237** — failJob UPDATE to mark jobs 'failed_terminal' writes agent_jobs WHERE id = ${id} ... without tenant_id — it can mutate another tenant's job.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or validate the job's tenant ownership before performing the status transition.
- **server/job-queue.ts:257** — failJob requeue UPDATE (setting status = 'pending' and next_run_at) lacks tenant_id in the WHERE clause — cross-tenant rows may be requeued.
  - _Fix:_ Scope this UPDATE by tenant_id or require callers to supply and validate tenantId; use parameterized tenant filters rather than unscoped id-only updates.
- **server/job-queue.ts:273** — cancelJob updates agent_jobs (SET status='cancelled') using only id in the WHERE clause and no tenant_id check — this can cancel jobs from other tenants.
  - _Fix:_ Require tenant validation and include AND tenant_id = ${tenantId} in the WHERE clause (or accept a tenantId parameter) so cancellations cannot be performed across tenant boundaries.
- **server/job-queue.ts:282** — retryJob resets job rows via UPDATE ... WHERE id = ${id} with no tenant_id constraint — allows resetting jobs belonging to other tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause or require caller-provided tenant verification before mutating job state.
- **server/lib/action-ledger.ts:305** — UPDATE on tenant-scoped table `action_attempts` (sweepStaleAttempts) performs a cross-tenant mass update (no tenant_id constraint) via a WHERE id IN (SELECT id FROM action_attempts ...). This will change rows across all tenants.
  - _Fix:_ Avoid cross-tenant UPDATEs. Either add tenant scoping to the inner SELECT/outer UPDATE (e.g. WHERE tenant_id = ${tenantId}) and run per-tenant, or restrict this operation to a vetted platform-maintenance admin path and ensure explicit authorization. Never perform multi-tenant UPDATEs without explicit, auditable admin intent.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on tenant-scoped table `procedure_evolution_runs` uses only runId in WHERE (where(eq(procedureEvolutionRuns.id, runId))) and omits a tenant_id predicate — a caller could flip another tenant's run row by id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or fetch-and-verify the run's tenant before updating. Ensure updates always constrain by tenant_id.
- **server/lib/aevo-meta-editor.ts:458** — UPDATE on tenant-scoped table `procedure_evolution_runs` (noop_proposal path) updates by run id only (.where(eq(procedureEvolutionRuns.id, runId))) with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (eq(procedureEvolutionRuns.tenantId, tenantId)) or otherwise assert ownership before the update so one tenant cannot modify another tenant's run row.
- **server/lib/aevo-meta-editor.ts:500** — UPDATE on tenant-scoped table `procedure_evolution_runs` (set done) uses runId only in WHERE (no tenant_id predicate), allowing cross-tenant row modification by id.
  - _Fix:_ Constrain the UPDATE to the owning tenant (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))). Prefer always including tenant_id in update predicates.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT against tenant-scoped table `projects` is executed without constraining by tenant_id (reads rows across all tenants). This violates the project's tenant-isolation invariant (application-level SQL filters are the only isolation mechanism).
  - _Fix:_ Restrict the query to the relevant tenant(s). For example, run the count per-tenant (iterate wantedByTenant and query WHERE tenant_id = ${t}) or add an explicit tenant filter (e.g. WHERE tenant_id = ANY(${Array.from(wantedByTenant.keys())}::int[])). Ensure every SELECT/UPDATE/DELETE on tenant-scoped tables includes an explicit tenant_id predicate or equivalent application-level ownership check.
- **server/lib/inbox-ingest.ts:507** — UPDATE against tenant-scoped table `inbox_classifications` lacks a tenant_id constraint in the WHERE clause (UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}). This allows a row from a different tenant with the same inbox_message_id to be updated.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. add AND tenant_id = ${tenantId} to the WHERE clause: `WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}` (or use Drizzle eq(file.tenantId, tenantId)). Ensure all UPDATE/DELETE statements include tenant_id to enforce application-level tenant isolation.
- **server/lib/outcome-promotion.ts:158** — Query over tenant-scoped table `deliverable_grade_events` lacks a tenant_id constraint — it SELECTs across all tenants (GROUP BY tenant_id, format) which violates the requirement that every read against a tenant-scoped table be constrained to the caller's tenant.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) and run the scan per-tenant, or ensure this code runs only in an explicit, authenticated maintenance/admin context and documents that cross-tenant reads are intentional. Prefer iterating over tenants and executing a parameterized per-tenant query to produce per-tenant promotions so no single query reads other tenants' rows.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE against tenant-scoped table `scheduled_posts` is executed without constraining by tenant_id (CTE picks due rows across all tenants and the subsequent UPDATE sets status='publishing' by id only). This mutates tenant rows without an explicit tenant-level filter tied to the caller context.
  - _Fix:_ Restrict the selection/update to a single tenant (WHERE tenant_id = <caller tenant>) or make explicit and auditable that this runner runs in an admin/system scope and ensure it is only invocable by a hardened admin process. If the runner must operate across tenants, keep the current design but explicitly require an elevated admin role and add logging/ACL checks; otherwise add tenant_id predicates to the CTE and subsequent UPDATE to enforce per-tenant isolation.
- **server/lib/session-fact-extractor.ts:136** — Inserting conversation-scoped facts via storage.createConversationFact(row) uses caller-supplied conversationId but does not first verify that the referenced conversation belongs to the provided tenantId. This writes a row referencing a foreign id without an ownership check.
  - _Fix:_ Before creating a conversation fact, verify ownership: SELECT the conversation (e.g. SELECT id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} LIMIT 1) and fail-closed if no row found. Ensure every write that references another row first validates that the referenced row belongs to the same tenant (or that the caller has admin privileges).
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table tenant_voice_profiles uses WHERE id = existing.id but lacks an explicit tenant_id constraint. This allows an update to affect a row with the same id belonging to a different tenant.
  - _Fix:_ Add the tenant filter to the WHERE clause (or use Drizzle eq on tenantId). Example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Always constrain UPDATEs on tenant-scoped tables by tenant_id.
- **server/memory-graph.ts:124** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} lacks a tenant_id constraint. This mutating query can affect another tenant's category row.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Include tenant_id in the WHERE (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or use Drizzle .where(and(eq(memoryCategories.id, categoryId), eq(memoryCategories.tenantId, tenantId))). Ensure the tenantId used is validated/authoritative.
- **server/memory-graph.ts:183** — refreshCategoryCount() performs UPDATE memory_categories ... WHERE id = ${categoryId} and its inner SELECT counts memory_entries for the category but does not filter by tenant_id. Both the outer UPDATE and inner aggregation can therefore mix or modify cross-tenant data.
  - _Fix:_ Filter both the inner SELECT and the outer UPDATE by tenant_id. For example: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status='active' ...) WHERE id = ${categoryId} AND tenant_id = ${tenantId}. Prefer Drizzle .where(eq(...)) for clarity.
- **server/memory-graph.ts:240** — SELECT 1 FROM memory_links ... WHERE (source_memory_id = ${newMemoryId} OR target_memory_id = ${target.id}) lacks any tenant restriction. A read against a tenant-scoped link table must be constrained to the caller's tenant.
  - _Fix:_ Include tenant filtering (e.g. AND tenant_id = ${tenantId}) and/or join with memory_entries to verify ownership before considering or creating links. Always ensure SELECT/EXISTS checks on tenant-scoped tables include tenant_id.
- **server/memory-graph.ts:494** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks a tenant_id constraint. Updating a memory row by id only can affect another tenant's row with the same id.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or use Drizzle .where(and(eq(memoryEntries.id, mem.id), eq(memoryEntries.tenantId, tenantId))). Also validate ownership of mem.id before mutating.
- **server/minerva-planner.ts:367** — Compare-and-swap UPDATE on `plans` may run without tenant constraint. The WHERE clause includes tenant filtering only when `args.tenantId != null`, otherwise the UPDATE executes without `tenant_id = ...`.
  - _Fix:_ Require an explicit tenant context for this operation (do not make tenant filtering conditional). Either mandate `tenantId` on the API surface or use a separate internal/trusted function—clearly separated—and document/enforce it. Prefer adding `AND tenant_id = ${args.tenantId}` unconditionally (or assert internal-trust before omitting) to prevent cross-tenant flips.
- **server/minerva-planner.ts:386** — SELECT probe of `plans` can be performed without tenant constraint when `args.tenantId` is omitted: `SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}`.
  - _Fix:_ Always include tenant filtering on reads from tenant-scoped tables, or require callers to explicitly pass a tenantId. If omission is intended for trusted internal callers, separate the internal path into a clearly documented/trusted function and avoid the conditional SQL pattern.
- **server/minerva-planner.ts:403** — SELECT to find child plans `SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}` conditionally omits tenant filtering when `args.tenantId` is not provided.
  - _Fix:_ Do not conditionally omit tenant filters. Either require tenantId or implement a distinct internal-only code path. Add `AND tenant_id = ${args.tenantId}` (or equivalent scoping) so child-plan lookups cannot cross tenant boundaries.
- **server/minerva-planner.ts:548** — getPlan conditionally issues an unscoped SELECT when `tenantId` is omitted: `SELECT * FROM plans WHERE id = ${planId}` (no tenant filter) — external callers may omit tenantId and trigger an unfiltered read.
  - _Fix:_ Require and enforce tenantId for external callers. Avoid conditional SQL that falls back to unscoped queries. Either (a) always add `AND tenant_id = ${tenantId}`, or (b) split into two explicit functions: one internal-trusted variant and one external that always enforces tenant scoping.
- **server/orchestrator-ledger.ts:458** — UPDATE against tenant-scoped table `memory_entries` does not include a tenant_id constraint. The UPDATE uses WHERE id = ${row.id} but omits tenant_id, allowing a stale/incorrect id to update a row belonging to another tenant.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise validate the row's tenant before updating. Prefer always checking tenant_id in DML to enforce isolation even inside transactions.
- **server/pace-control.ts:85** — Query against tenant-scoped table `heartbeat_logs` (COUNT over recent runs) lacks any tenant_id constraint — the function aggregates across all tenants. TenantId is not accepted as an argument, so this read leaks cross-tenant usage and can cause incorrect enforcement of per-tenant pace limits.
  - _Fix:_ Make pace checks tenant-aware: accept a tenantId argument and add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise scope the query to the caller's tenant). Ensure all heartbeat_logs reads are scoped by tenant_id.
- **server/pace-control.ts:140** — getPaceSnapshot issues a SELECT COUNT(*) FROM heartbeat_logs WHERE created_at > ... with no tenant_id constraint, returning global data across tenants.
  - _Fix:_ If heartbeat snapshots should be per-tenant, add tenant_id = ${tenantId} (and accept tenantId param). If an operator-only global view is intended, isolate that functionality behind an elevated/admin path and clearly document it. Do not leave tenant-scoped reads unguarded in general-purpose code.
- **server/pace-control.ts:147** — Query that builds personaRows reads heartbeat_logs (GROUP BY persona_name) without any tenant_id filter — this will aggregate across all tenants and leak/mix tenant data.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE created_at > ... AND tenant_id = ${tenantId}) and require a tenantId parameter for tenant-scoped snapshots. If an aggregated multi-tenant view is required, restrict its use to admin-only code paths.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL has no tenant_id filter. If skills contains tenant-scoped entries, persona-specific skills may be leaked or mixed across tenants.
  - _Fix:_ Include tenant scoping for persona-specific skills. For example: SELECT ... FROM skills WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR (persona_id IS NULL AND tenant_id IS NULL) — or otherwise follow the schema's intended separation of global vs tenant rows and explicitly filter by tenant_id for tenant-scoped rows. Ensure all reads against tenant-scoped tables include tenant_id.
- **server/process-governor.ts:121** — SELECT subqueries against heartbeat_logs (task_failure_rate) do not include tenant_id filter. The inner COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ... can return rows for other tenants.
  - _Fix:_ Constrain these subqueries by tenant. e.g. add AND tenant_id = ${tenantId} to each heartbeat_logs WHERE clause (or use Drizzle .where(eq(heartbeatLogs.tenantId, tenantId))). Ensure every read against heartbeat_logs is scoped to the caller tenant.
- **server/process-governor.ts:199** — response_time query reads heartbeat_logs without tenant scoping (no tenant_id WHERE clause). This can aggregate other tenants' metrics.
  - _Fix:_ Add tenant scoping to the query: WHERE tenant_id = ${tenantId} AND created_at > ... (or use a Drizzle .where(eq(...))). Always filter heartbeat_logs by tenant_id for per-tenant metrics.
- **server/process-governor.ts:240** — cascading_failures SELECT counts distinct persona_id FROM heartbeat_logs without tenant_id. This can include other tenants' failing agents.
  - _Fix:_ Include tenant filter in the WHERE clause (AND tenant_id = ${tenantId}) or use Drizzle .where(eq(table.tenantId, tenantId)). Audit other heartbeat_logs uses for the same omission.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads the projects row without tenant scoping or ownership verification. projects is likely tenant-scoped, so this may disclose another tenant's project metadata.
  - _Fix:_ Require a tenantId parameter or resolve the project's tenant_id and verify it matches the caller. Change the query to include AND tenant_id = ${tenantId} (or use Drizzle .where(eq(projects.tenantId, tenantId))). If this is a background/global operation, make the scope explicit and document it.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 has no tenant filters. This enumerates projects/conversations/messages across all tenants.
  - _Fix:_ Restrict discovery to a specific tenant (add p.tenant_id = ${tenantId} and c.tenant_id = ${tenantId} and message tenant constraints as appropriate), or iterate per-tenant. Add an explicit tenant parameter to backfillProjectBrains and filter every tenant-scoped table by tenant_id.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} runs without tenant scoping. conversations are likely tenant-scoped, so this can return another tenant's conversations.
  - _Fix:_ Include tenant scoping (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or first verify that proj.id belongs to the expected tenant. Make tenant ownership explicit in the function signature if needed.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping. messages are typically tenant-scoped — this can leak cross-tenant message content.
  - _Fix:_ Constrain the query by tenant (e.g. AND tenant_id = ${tenantId}) or verify conversation ownership first. Update backfill/updateProjectBrain to accept/resolve tenantId and apply tenant filters on messages, conversations, and projects.
- **server/reference-learner.ts:290** — UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId} — missing tenant_id filter. memory_entries is tenant-scoped but the UPDATE only filters by id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) to ensure you only modify rows belonging to that tenant. Do not rely solely on a prior SELECT; make the write statement fail-closed when tenant_id does not match.
- **server/routes.ts:1116** — Querying file_storage rows by filename without constraining tenant_id (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))); this reads rows across all tenants and relies on application logic to check ownership instead of restricting at SQL level.
  - _Fix:_ Restrict the SELECT to the authenticated tenant in SQL (e.g. .where(and(eq(fileStorage.filename, baseName), eq(fileStorage.tenantId, authedTenantId))) ), or perform separate explicit queries that include tenant_id. If you intentionally need to detect cross-tenant collisions, do so with an explicit, narrowly-scoped query that does not return other tenants' data (e.g. first query for caller's tenant, then a separate existence/count query that returns only a boolean, not full rows). Avoid selecting full tenant rows and then filtering purely in application code.
- **server/routes.ts:2124** — Inserting presenter_sessions with tenant_id taken directly from the request body (INSERT INTO presenter_sessions ... VALUES (${tenantId}, ...)). The handler only validates that tenantId is a positive integer and does not verify the caller is authorized to create rows for that tenant.
  - _Fix:_ Do not accept arbitrary tenantId from callers. Derive tenant_id from a trusted source (authenticated session, internal signed token that includes tenant binding), or require the caller to be a platform-admin and enforce that via requirePlatformAdmin(). If the endpoint must accept an explicit tenantId, add an ownership/authorization check (e.g. verify the session/credential used to call the endpoint is allowed to act for the supplied tenant) before the INSERT.
- **server/routes.ts:4341** — INSERT into tenant-scoped table 'project_notes' is missing tenant_id in the VALUES clause. The code inserts project notes with INSERT INTO project_notes (project_id, note, author) VALUES (...) without setting tenant_id, relying only on a prior SELECT from conversations for project_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT and ensure the project belongs to the caller's tenant. Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}). Alternatively, use a storage helper (e.g. storage.createProjectNote) that enforces and sets tenantId and performs ownership validation before inserting.
- **server/routes/conversations.ts:99** — UPDATE on tenant-scoped table 'conversations' executed without constraining tenant_id in the WHERE clause.
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or perform the update via a storage layer function that enforces tenant scope to avoid TOCTOU/race conditions.
- **server/routes/archive-rescue.ts:276** — SQL array literal is constructed by string interpolation from request-derived file paths and injected directly into the SQL template (`{...}`::text[]) — risky string-interpolation of request data into SQL.
  - _Fix:_ Avoid building SQL array literals by string concatenation. Use proper parameterized binding (pass savedPaths as an array parameter or use your ORM/Drizzle insert helpers to set a text[] column), or sanitize and bind each element so no SQL parsing/injection or isolation ambiguity can occur. Example: INSERT ... demo_image_paths = ${savedPaths} (with the driver/ORM treating savedPaths as an array) or use db.insert(...) with the array value.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` is executed without constraining by tenant_id (WHERE id = ${msg.id} only). This allows a cross-tenant write if msg.id were to reference a row belonging to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id. For example: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`); Or validate ownership earlier and pass tenant-scoped context to the storage API (use storage helper that enforces tenant scope) so every write includes AND tenant_id = <caller tenant>.
- **server/routes/oauth-subscriptions.ts:325** — Query reads oauth_subscriptions without constraining tenant_id (SELECT DISTINCT tenant_id FROM oauth_subscriptions ...). This performs a cross-tenant read on a tenant-scoped table.
  - _Fix:_ If this table is tenant-scoped, avoid unconstrained reads. Either (a) run this cron logic as a platform/admin operation with explicit platform-authorization and document/approve the cross-tenant intent, or (b) limit the query to an explicit tenant (WHERE tenant_id = <tenant>) or an approved list. If cross-tenant enumeration is required, isolate it behind a well-audited platform-admin path and log/authorize the action. Consider separating truly platform-global data into a distinct table to avoid accidental cross-tenant access.
- **server/routes/projects.ts:185** — INSERT into project_notes omits tenant_id: INSERT INTO project_notes (project_id, note, author) VALUES (...). Tenant-scoped tables require tenant_id on INSERT.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ...)), and validate that tenantId is derived from the authenticated session (not the request body).
- **server/routes/projects.ts:224** — INSERT into project_files (base64 handler) omits tenant_id: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...). Tenant-scoped tables require tenant_id on INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. tenant_id = ${tenantId}) and ensure tenantId comes from getTenantFromRequest and is validated against the project ownership before inserting.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} lacks tenant_id constraint. This updates a tenant-scoped table row without scoping to the caller's tenant.
  - _Fix:_ Constrain updates with tenant_id, e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}, or pass tenantId into the query to ensure the update only affects rows owned by the caller's tenant.
- **server/routes/projects.ts:292** — INSERT into project_files (multer handler) omits tenant_id: INSERT INTO project_files (...). Tenant-scoped tables require tenant_id on INSERT.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)) and ensure project ownership is verified before insertion.
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (multer handler) lacks tenant_id constraint — same unconstrained update as in base64 path.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so only the caller's tenant row is updated.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC lacks tenant_id constraint. This reads tenant-scoped rows without explicitly scoping to the caller's tenant.
  - _Fix:_ Constrain this SELECT to the tenant (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or JOIN to projects and filter on projects.tenant_id = ${tenantId} to ensure tenant isolation in the query itself.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not include tenant_id, allowing cross-tenant file lookups.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or join with projects and verify projects.tenant_id = ${tenantId} to enforce tenant ownership at the SQL level.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} omits tenant_id. Deletion of tenant-scoped rows must be constrained to the caller's tenant.
  - _Fix:_ Require tenant_id in the DELETE (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}), or verify project ownership and include tenant filter to prevent accidental cross-tenant deletion.
- **server/routes/stripe-checkout.ts:104** — Raw/tagged SQL interpolates a request-derived value (priceId) into sql`... ${priceId} ...` when checking allowed prices. This is a use of string-interpolated SQL with request data (sql`... ${...}`) which the audit rules flag as an injection + isolation risk.
  - _Fix:_ Avoid embedding request-derived values into raw SQL templates. Use Drizzle's query builder APIs (e.g. db.select().from(prices).where(eq(prices.id, priceId), eq(prices.active, true), ...)) or otherwise pass priceId as a proper parameterized binding after validating its format. Do not use sql.raw() / template interpolation with untrusted input; ensure any tenant-ownership checks (if applicable) are enforced via explicit eq(table.tenantId, tenantId) filters.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without constraining tenant_id (WHERE id = ${session.id} only). This writes to a tenant-scoped table without enforcing the caller's tenant.
  - _Fix:_ Include tenant-scoping in the WHERE clause, e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} (or the trusted tenant variable). Always bind tenant_id for UPDATEs to tenant-scoped tables.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions sets conversation_id but omits tenant filter (WHERE id = ${session.id} only). Missing tenant_id allows cross-tenant mutation if ids are abused.
  - _Fix:_ Add tenant scoping to the update: WHERE id = ${session.id} AND tenant_id = ${params.tenantId}. Prefer including tenant_id in every UPDATE/DELETE against tenant-scoped tables.
- **server/sculptor.ts:174** — UPDATE sculptor_sessions in monitorSession() uses WHERE id = ${sessionId} without AND tenant_id = ${tenantId}. Monitor is a background task and must still enforce tenant isolation on writes.
  - _Fix:_ Constrain the UPDATE by tenant (e.g., WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure every background/async updater includes the tenant_id guard.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} has no tenant_id constraint. This is a write to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or pass the tenant context into the update). Always verify tenant ownership before updating tenant-scoped rows.
- **server/second-opinion.ts:407** — settleFusionReservation() updates agent_cost_ledger by reservation id only (WHERE id = ${reservationId}) with no tenant_id check. A reservation id could belong to another tenant and be mutated.
  - _Fix:_ Include tenant scoping when settling reservation rows, e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Pass tenantId into settleFusionReservation so writes are constrained to the correct tenant.
- **server/second-opinion.ts:426** — releaseFusionReservation() updates agent_cost_ledger WHERE id = ${reservationId} without tenant_id. This can modify ledger rows across tenants if reservationId is misused.
  - _Fix:_ Scope the UPDATE by tenant: UPDATE agent_cost_ledger ... WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Accept tenantId as a parameter and validate before performing the release.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true executes without tenant_id constraint — this will disable delegation tasks across all tenants.
  - _Fix:_ Constrain the UPDATE to a tenant (e.g., AND tenant_id = ${ADMIN_TENANT_ID} if intended for admin tenant) or iterate per-tenant and run tenant-scoped updates. Never run mass UPDATE on a tenant-scoped table without an explicit tenant_id filter.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant_id constraint and will affect rows across tenants.
  - _Fix:_ Restrict the UPDATE with tenant_id = ${ADMIN_TENANT_ID} if intended for the admin tenant, or otherwise iterate tenants and apply tenant-scoped updates. Ensure all writes to tenant-scoped tables include tenant_id in the WHERE clause.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true lacks tenant_id and will disable rows across tenants that match the name patterns.
  - _Fix:_ Include AND tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant variable) in the WHERE clause or perform tenant-by-tenant updates. Make admin/global intent explicit and safe.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped (has tenant_id) so this reads global counts across tenants.
  - _Fix:_ Restrict the query to the caller's tenant. E.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}. If this count is intended to be multi-tenant/admin-only, make that explicit, run it under an admin-only code path, or document and protect it accordingly.
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter. research_programs is tenant-scoped, so this will return rows from all tenants.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise ensure this operation is explicitly run for a specific tenant (and not across tenants). If the intent is to operate per-tenant, include tenant_id in the initial projection so subsequent updates can be scoped properly.
- **server/seed.ts:2516** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations ...) queries conversations without any tenant filter. conversations is typically tenant-scoped, so this will examine rows across tenants. The related project_conversations table also lacks a tenant_id column, which complicates safe multi-tenant backfills.
  - _Fix:_ Constrain the SELECT to the target tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...). If project_conversations must be tenant-scoped, add tenant_id to that table and include it in the EXISTS check (or join via a project→tenant relationship). Never run cross-tenant SELECT/INSERT backfills without an explicit tenant filter or admin-only execution guard.
- **server/seed.ts:3910** — UPDATE memory_entries ... executed without a tenant_id constraint — this will modify memory entries for all tenants.
  - _Fix:_ Constrain the UPDATE to the intended tenant (e.g. WHERE tenant_id = <tenantId> AND ...) or run the update per-tenant after enumerating tenants. Never run tenant-scoped updates without an explicit tenant_id filter.
- **server/seed.ts:3936** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id filter and will update conversations across all tenants.
  - _Fix:_ Either add WHERE tenant_id = <tenantId> to the UPDATE or perform the schema/data migration per-tenant by enumerating tenants. Ensure application-level tenant scoping is always present for tenant-scoped tables.
- **server/self-improvement.ts:170** — detectStagnation() performs a SELECT on the tenant-scoped `experiments` table without requiring/enforcing a tenant_id filter when tenantId is omitted. The .where(...) built from conditions can end up querying all tenants if tenantId is not provided.
  - _Fix:_ Require an explicit tenantId for this function (throw if missing) or always add eq(experiments.tenantId, tenantId) to the WHERE. Avoid building SQL that runs across tenants; if an admin cross-tenant query is needed, make it an explicit, audited operation separate from this code path.
- **server/self-improvement.ts:546** — logExperiment() does an INSERT into `experiments` and sets tenantId: data.tenantId! using the caller-provided value. If data.tenantId is missing/undefined this may insert a NULL/incorrect tenant or rely on an untrusted/derived value without validation.
  - _Fix:_ Validate and require data.tenantId (throw if absent or invalid) and never trust caller-supplied tenant identifiers. Use the authenticated caller's tenantId or an explicit, validated source; ensure tenant_id column is always set to a trusted tenant id before INSERT.
- **server/self-improvement.ts:511** — getExperimentHistory() performs a SELECT on the tenant-scoped `experiments` table and will return rows across all tenants when tenantId is not supplied (the function allows an optional tenantId and falls back to selecting all rows when omitted).
  - _Fix:_ Require tenantId for this API or always include eq(experiments.tenantId, tenantId) in the WHERE clause. If cross-tenant admin queries are required, implement a separate clearly-audited admin endpoint and do not reuse this function for tenant-scoped access.
- **server/storage.ts:253** — Unscoped SELECT on tenant-scoped table `conversations` (getConversationUnscoped) — query `WHERE id = ${id}` has no tenant_id constraint.
  - _Fix:_ Avoid unscoped reads. Require the caller to provide tenantId and add an equality predicate (eq(conversations.tenantId, tenantId)) or use a dedicated admin-only path with explicit authorization and logging. If an unscoped fetch is required, document/limit its callers and audit every use-site to ensure the tenant is validated before any downstream read/write.
- **server/storage.ts:314** — Unscoped SELECT from `conversations` to derive tenant_id in createMessage: query filters only by conversation id, no tenant_id constraint.
  - _Fix:_ Either accept and validate the caller's tenantId and select with both id and tenant_id, or require callers to call getConversation(id, tenantId). If deriving tenantId from the conversation row, do so via an explicitly-audited unscoped helper and immediately enforce that the eventual INSERT/UPDATE includes the derived tenantId — prefer requiring callers to pass the tenantId and validate with requiredTenantScope/tenantScope.
- **server/storage.ts:404** — UPDATE on tenant-scoped `messages` (attachCitations) uses `WHERE id = ${messageId}` with no tenant_id constraint — can modify rows across tenants.
  - _Fix:_ Require and pass the tenantId for the message and update with an explicit tenant filter (WHERE id = ${messageId} AND tenant_id = ${tenantId}). Validate that the caller is authorized to modify that tenant's message before issuing the UPDATE.
- **server/storage.ts:568** — INSERT into tenant-scoped `memory_entries` (createMemoryEntry) does not validate/ensure an explicit tenantId is present before insert.
  - _Fix:_ Fail-closed: validate data.tenantId is a positive integer (assertValidTenantId or explicit check) before INSERT and throw if missing. Make tenantId a required function parameter when called from untrusted contexts so every INSERT sets tenant_id explicitly.
- **server/storage.ts:806** — INSERT into tenant-scoped `agent_knowledge` (createKnowledge) performs db.insert(values(data)) without validating that data.tenantId is present/owned by caller.
  - _Fix:_ Require and validate a positive tenantId on the input (or require callers to use a trusted admin path). Use assertValidTenantId or requireTenantScope semantics so every INSERT explicitly sets tenant_id and cannot accidentally write across tenants.
- **server/storage.ts:841** — UPDATE memory_entries SET embedding ... WHERE id = ${id} (updateMemoryEmbedding) has no tenant_id constraint — mutates tenant-scoped rows by id only.
  - _Fix:_ Require the tenantId (or a tenant-validated lookup) and include eq(memoryEntries.tenantId, tenantId) in the WHERE clause. If this is a background worker intended to act across tenants, make that explicit (admin-only) and use requiredTenantScope or iterate per-tenant with explicit authorization.
- **server/storage.ts:853** — UPDATE agent_knowledge SET embedding ... WHERE id = ${id} (updateKnowledgeEmbedding) lacks tenant scoping — updates knowledge rows across tenants.
  - _Fix:_ Require and validate tenantId and add eq(agentKnowledge.tenantId, tenantId) to the WHERE clause, or perform a prior tenant-validated lookup before updating. For global workers, only allow explicit admin/ALL_TENANTS sentinel and audit the operation.
- **server/storage.ts:1013** — Raw UPDATE on `heartbeat_tasks` (fixStaleBackupSchedules) executes without tenant filtering — mutates tasks across tenants.
  - _Fix:_ If this maintenance is intended to be global, require explicit admin authorization and auditing. Prefer performing tenant-scoped updates (adding tenant_id condition or iterating tenants) or gating the operation behind an explicit ALL_TENANTS sentinel with strict checks.
- **server/storage.ts:1035** — SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ... (getDueHeartbeatTasks) returns tasks across all tenants — no tenant_id constraint.
  - _Fix:_ If scheduler should run per-tenant, include tenant scoping. If it must obtain global tasks, ensure the caller is an authorized system process and make that explicit. Replace implicit global reads with requiredTenantScope or explicit admin-only code paths.
- **server/storage.ts:1063** — UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... (claimHeartbeatTasks) has no tenant_id constraint — claims tasks across tenants.
  - _Fix:_ Require tenantId in the claim path and include it in the WHERE clause (or perform a prior tenant-validated lookup). For a multi-tenant scheduler, always scope claims by tenant to avoid cross-tenant task interference.
- **server/storage.ts:1077** — UPDATE heartbeat_tasks SET lastRunAt/nextRunAt WHERE id = ${id} (markHeartbeatTaskRun) has no tenant filter — mutates a tenant-scoped table by id only.
  - _Fix:_ Require and validate tenantId and include eq(heartbeatTasks.tenantId, tenantId) in the WHERE clause, or perform a tenant-validated select before updating. If this is a system-level operation, gate it explicitly and audit.
- **server/subagents.ts:264** — Reads a persona row by id (storage.getPersona(agentId)) without verifying the persona belongs to the caller's tenant or otherwise validating ownership. The agentId can come from caller-supplied params and is used without tenant scoping.
  - _Fix:_ Resolve and enforce persona ownership before use: fetch the persona scoped to the parent's tenant (e.g. storage.getPersona(agentId, tenantId) or storage.getPersonaWithTenantCheck(agentId, parentConv.tenantId)) or explicitly verify that the persona.tenantId === parentConv.tenantId. Do not trust caller-supplied agentId without an ownership check.
- **server/tool-learning.ts:291** — UPDATE custom_tools SET implementation = fixedImpl WHERE id = ${tool.id} is executed without constraining the update by tenant_id. custom_tools is tenant-scoped; the update can affect rows outside the caller's tenant context.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${tool.id} AND tenant_id = ${tool.tenantId} or pass caller tenantId into the function). Always combine id + tenant_id for writes to tenant-scoped tables.
- **server/tool-learning.ts:316** — DELETE FROM custom_tools WHERE id = ${tool.id} deletes a tenant-scoped row by id only, without verifying tenant_id. This may delete another tenant's tool if misused.
  - _Fix:_ Constrain the DELETE by tenant (e.g. WHERE id = ${tool.id} AND tenant_id = ${tool.tenantId}) or otherwise verify the caller owns the tenant_id. Ensure all destructive operations on tenant-scoped tables include tenant checks.
- **server/tool-learning.ts:155** — SELECT ... FROM custom_tools WHERE id = ${inserted.id} is performed without an explicit tenant_id filter. Reading tenant-scoped rows by id only can cross tenants in contexts where id isn't sufficient authorization.
  - _Fix:_ Select with tenant scoping (WHERE id = ${inserted.id} AND tenant_id = ${tenantId}) or use the returned row from the insert (which should include tenant_id) rather than re-querying without tenant constraints.
- **server/tool-learning.ts:365** — UPDATE custom_tools SET usageCount = ... WHERE id = ${tool.id} increments usageCount without a tenant_id constraint. This writes to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Restrict the update by tenant (e.g. WHERE id = ${tool.id} AND tenant_id = ${tenantId} or tool.tenantId). Ensure usage counters and all updates against tenant-scoped tables always include tenant_id = <caller tenant>.
- **server/tools.ts:2890** — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The code inserts (project_id, conversation_id) without explicitly storing the caller's tenant_id column.
  - _Fix:_ Include tenant_id in the INSERT and populate it from the caller's tenant context (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ON CONFLICT DO NOTHING). Also keep the existing ownership check (or assert) so you don't accept foreign IDs from untrusted sources.
- **server/tools.ts:2994** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT INTO project_files(...) VALUES(...) omits tenant_id even though this is tenant-scoped data.
  - _Fix:_ Add tenant_id to the inserted columns and pass the caller's tenantId (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by, tenant_id) VALUES (..., ${tenantId}) RETURNING *). Keep or perform an explicit ownership check on the referenced project before inserting.
- **server/tools.ts:3008** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The code inserts project_notes (project_id, note, author) without an explicit tenant_id column value.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author || 'agent'}, ${tenantId}) RETURNING *). Ensure you validate the project's ownership before the insert (the ownerCheck is good) and record tenant_id on the row.
- **server/tools.ts:3031** — INSERT into tenant-scoped table `project_conversations` (link_conversation path) does not set tenant_id. The handler inserts a linking row without storing tenant_id, relying only on prior checks/updates.
  - _Fix:_ Set tenant_id explicitly when creating the link (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Continue to assert the conversation/project belong to the tenant before inserting.
- **server/tools.ts:6942** — INSERT into project_files omits tenant_id. The code asserts project ownership via assertProjectInTenant but then performs raw INSERT INTO project_files (...) VALUES (...) without including tenant_id as a column/value. Per invariant, tenant-scoped tables must have the tenant_id explicitly set on INSERT.
  - _Fix:_ Include the tenant id explicitly in the INSERT (or use Drizzle's insert API with tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Prefer db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: ..., ... }) to ensure the tenant column is always set.
- **server/tools.ts:8910** — UPDATE on proposed_skills lacks a tenant_id constraint in the WHERE clause. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates by id only and does not restrict to the caller's tenant, enabling cross-tenant modification if IDs collide or are abused.
  - _Fix:_ Add a tenant filter to the WHERE clause. Use .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or include tenantId in the predicate (tid must be the validated caller tenant). Prefer using Drizzle typed APIs that include tenant scoping on updates.
- **server/tools.ts:8936** — UPDATE on proposed_skills (security-scan rejection path) missing tenant_id in WHERE clause. The update uses only the row id to identify the row, with no tenant scoping.
  - _Fix:_ Restrict updates to the tenant: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Ensure 'tid' is the caller's validated tenantId (not derived from untrusted input).
- **server/tools.ts:8950** — UPDATE on proposed_skills (promotion path) missing tenant_id in WHERE clause. The update sets promotedSkillId/status but does not restrict by tenant, allowing potential cross-tenant modifications.
  - _Fix:_ Add tenant scoping to the update: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Prefer using a single transactional operation that both verifies tenant and updates to avoid TOCTOU risks.
- **server/tools.ts:8967** — UPDATE on proposed_skills (reject path) performed without tenant_id constraint. The update only filters by id and does not confirm the row belongs to the caller's tenant.
  - _Fix:_ Include tenant_id in the WHERE clause: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Audit other updates/DELETEs in this file for similar missing tenant filters and standardize tenant-scoped helpers to avoid omissions.
- **server/tools/domains/browser/handlers.ts:165** — Raw UPDATE against tenant-scoped table `browser_workflows` omits tenant_id constraint: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. This relies on previously-selected `workflow.id` but the SQL lacks an explicit `tenant_id = <caller tenant>` guard.
  - _Fix:_ Constrain the UPDATE to the caller's tenant in the same statement (e.g. `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`) or otherwise verify and re-check ownership in the same DB statement before modifying rows.
- **server/tools/domains/files/handlers.ts:904** — INSERT into `project_files` does not include an explicit tenant identifier in the INSERT columns/values: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`. Per codebase invariant, tenant-scoped inserts must explicitly pass tenant_id.
  - _Fix:_ Include the tenant id explicitly in the INSERT (e.g. add a tenant_id column/value) or ensure the table is not tenant-scoped. If the row is intended to be tenant-scoped, change the INSERT to set tenant_id: `... (project_id, tenant_id, file_name, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ...)`. If the design relies on project_id referencing a project row, still record tenant_id explicitly to prevent ambiguity and accidental cross-tenant writes.
- **server/tools/domains/files/handlers.ts:907** — UPDATE on `projects` table omits tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`. Without `tenant_id = <caller tenant>` this could update a project belonging to another tenant if IDs are guessed or reused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`) or perform the update via a single statement that ensures project ownership, to avoid relying only on previously fetched/derived project_id values.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` without tenant_id constraint — admin branch performs SELECT provider, LENGTH(api_key) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' allowing cross-tenant enumeration of API keys.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId} or use scanSql`... WHERE tenant_id = ${tenantId} AND ...`). If the admin tenant must legitimately view cross-tenant data, make that explicit: implement an audited, admin-only aggregation endpoint that does not return raw per-tenant API keys, or ensure provider_keys is a genuine global table and document the exception. Consider encrypting key material or returning only counts/metadata rather than keys/lengths.
- **server/tools/domains/security/handlers.ts:260** — SELECT from tenant-scoped table `provider_keys` without tenant_id constraint — admin branch runs SELECT COUNT(DISTINCT provider) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' which enumerates provider configuration across tenants.
  - _Fix:_ Either add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or, if this aggregate is intended for an admin audit, restrict it to an explicit admin-only path with appropriate safeguards (avoid returning tenant-sensitive details; return aggregated/obfuscated metrics). Ensure provider_keys is correctly modeled (tenant-scoped vs global) and that queries follow the tenant-isolation policy.
- **server/video-job-runner.ts:236** — UPDATE on tenant-scoped table `video_jobs` performed without any tenant_id constraint. recoverStaleVideoJobs() updates all matching jobs across every tenant (marks them failed) because the WHERE clause lacks a tenant filter.
  - _Fix:_ Restrict the UPDATE to an explicit tenant (pass tenantId into the function or iterate per-tenant), or otherwise run this maintenance action under an explicit admin/maintenance context with appropriate authorization. e.g. include eq(videoJobs.tenantId, SOME_TENANT_ID) in the where clause or perform per-tenant updates only after enumerating tenants from a trusted source.
- **server/video-job-runner.ts:208** — SELECT from tenant-scoped table `video_jobs` (reading cancelRequested) is done by isCancelRequested(jobId) with only jobId in the WHERE clause and no tenant_id constraint. This allows reading cancellation state for jobs owned by other tenants.
  - _Fix:_ Require and pass the tenantId into isCancelRequested and include eq(videoJobs.tenantId, tenantId) in the WHERE clause. If the caller cannot provide tenantId, validate ownership first (e.g. lookup jobId -> tenantId and verify caller) before returning cancel state.
- **server/wiring-invariants.ts:244** — Query against tenant-scoped table `tool_performance` (SELECT count(*) ...) has no tenant_id constraint — reads aggregated data across all tenants.
  - _Fix:_ If this must run per-tenant, add a WHERE tenant_id = ${tenantId} and iterate tenants. If it's intended as an admin/global metric, explicitly document that and require an admin-only code path or a dedicated aggregated table; avoid ad-hoc cross-tenant reads on tenant-scoped tables.
- **server/wiring-invariants.ts:258** — Query SELECT count(*) FROM tool_performance (everCount) lacks tenant scoping and reads across all tenants.
  - _Fix:_ Scope counts to a tenant via WHERE tenant_id = ${tenantId} or compute/maintain an explicit global aggregated metric in a controlled admin-only context. Do not read tenant-scoped tables across tenants from a regular application path.
- **server/wiring-invariants.ts:260** — Query SELECT count(*) FROM messages WHERE created_at > ... executes without any tenant_id filter, aggregating chat messages across all tenants.
  - _Fix:_ Run this check per-tenant (add WHERE tenant_id = ${tenantId}) or move to an admin-only analytics job that intentionally aggregates multi-tenant data with proper access controls and auditing. Avoid unscoped reads of tenant-scoped message data in normal application code.
- **server/wiring-invariants.ts:293** — Query SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant_id constraint and therefore aggregates tool usage across all tenants.
  - _Fix:_ If the intent is per-tenant dormancy detection, add WHERE tenant_id = ${tenantId} and evaluate per-tenant. If a global view is required, use an explicit admin-only aggregation pipeline or a dedicated global table; ensure it's run only with privileged intent and documented justification.
- **server/wiring-invariants.ts:366** — Raw COUNT/MAX query constructed with sql.raw: `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}` performs unscoped reads against subsystem tables (e.g. tool_performance, knowledge_nudges) with no tenant_id filter.
  - _Fix:_ Avoid issuing unscoped SELECTs against tenant-scoped subsystem tables. Add a tenant filter (WHERE tenant_id = ${tenantId}) or run the probe in a controlled admin-only aggregation context. Prefer parameterized queries and, where a global summary is required, use audited aggregation logic or per-tenant iteration.
- **server/wiring-invariants.ts:417** — Query against tenant-scoped table `tool_performance` (SELECT ... FROM tool_performance WHERE last_failure_at > ...) groups/filters without any tenant_id restriction, exposing cross-tenant data.
  - _Fix:_ Add tenant_id = ${tenantId} to WHERE/GROUP BY logic to restrict analysis to a single tenant, or move multi-tenant aggregation into a dedicated, audited admin/analytics routine that cannot be invoked from regular app code.
- **server/wiring-invariants.ts:535** — Memory-growth probe uses sql.raw to SELECT COUNT(*) and pg_total_relation_size FROM tables listed in MEMORY_TABLES (including `messages`) without any tenant_id constraint, aggregating tenant-scoped data across tenants.
  - _Fix:_ Change these probes to operate per-tenant (add WHERE tenant_id = ${tenantId}) or implement an explicit admin-only aggregate. For table-size checks that are global by design, document and gate them as privileged operations; otherwise restrict to tenant-local scopes.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory executes without a tenant_id filter and thus reads graph_memory rows across all tenants.
  - _Fix:_ If graph_memory is tenant-scoped, add WHERE tenant_id = ${tenantId} to the query and run the check per-tenant. If a platform-wide count is required, move the logic into a privileged, audited admin job or a separate aggregation table; do not perform unscoped reads in normal application code.

### MEDIUM (37)

- **server/agent-channels.ts:198** — Query in getUnreadCount joins channel_subscriptions (cs) but does not constrain cs.tenant_id — reading channel_subscriptions rows without tenant scoping in the JOIN.
  - _Fix:_ Include cs.tenant_id = ${tenantId} in the JOIN condition (or add an explicit WHERE filter) so channel_subscriptions rows are limited to the caller's tenant. Example: JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}.
- **server/agent-eval.ts:178** — Call to storage.getPersona(personaId) occurs without verifying that the persona belongs to the caller's tenant. This may allow using a persona from another tenant.
  - _Fix:_ Use a tenant-aware persona lookup (pass tenantId through to storage.getPersona or perform an explicit check) and fail if the persona does not belong to the caller's tenant before proceeding.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant constraint: the scanner queries all rows (`FROM commitments WHERE status = 'active' AND due_at IS NOT NULL ...`) and returns rows across tenants. This permits cross-tenant reads (exfil) unless explicitly intended and authorized.
  - _Fix:_ If this is intended only for platform-admin telemetry, make that explicit and require an admin authorization check at the call site; otherwise add a tenant_id filter (e.g. `AND tenant_id = ${tenantId}`) or accept a tenantId parameter and enforce it. If cross-tenant scanning must continue, document and gate it behind an audited admin-only path rather than an unguarded public function.
- **server/data-protection.ts:360** — SELECT from `project_notes` lacks tenant scoping (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}). Reads tenant-scoped data without a tenant_id predicate.
  - _Fix:_ Constrain reads to the tenant: add AND tenant_id = ${tenantId} (or AND tenant_id = ${proj.tenant_id} if available), or perform the lookup inside a per-tenant transaction (withTenantTx) to guarantee tenant isolation.
- **server/data-protection.ts:371** — SELECT from `project_files` lacks tenant scoping (SELECT file_name,... FROM project_files WHERE project_id = ${proj.id}). This reads files for a project without constraining tenant_id.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${tenantId} or AND tenant_id = ${proj.tenant_id}), or run the backup per-tenant using withTenantTx so every child-table access is automatically bounded to that tenant context.
- **server/data-protection.ts:404** — SELECT COUNT(*) FROM `messages` WHERE conversation_id = ${conv.id} has no tenant_id predicate. This checks message existence across tenants for a conversation id.
  - _Fix:_ Include tenant scoping in the COUNT (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id} or AND tenant_id = ${tenantId}). Prefer per-tenant transactions (withTenantTx) when iterating conversations to avoid accidental cross-tenant checks.
- **server/google-drive.ts:1066** — SELECT from 'projects' to fetch driveFolderId uses WHERE projects.id = ${projectId} without an explicit tenant_id constraint. While a prior check may exist, this individual query is not tenant-scoped.
  - _Fix:_ Add tenant scoping to the query (AND projects.tenant_id = ${tenantId}) or reuse the previously-validated project row to avoid another unconstrained read.
- **server/heartbeat.ts:60** — SELECT DISTINCT tenant_id FROM messages is querying a tenant-scoped table without an explicit tenant_id filter.
  - _Fix:_ Avoid scanning tenant-scoped content tables for cross-tenant metadata. If you need the list of active tenants, read from a dedicated tenants/metadata table. If this SELECT must read messages, add explicit tenant scoping or clearly document/centralize the cross-tenant entitlement check (e.g. only admin tasks using an explicit ALL_TENANTS code-path).
- **server/heartbeat.ts:1723** — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' reads heartbeat_logs without tenant scoping — may return other-tenants' logs.
  - _Fix:_ If heartbeat_logs are tenant-scoped, query per-tenant (WHERE tenant_id = ${tenantId} AND task_name = ...) or use a storage helper that enforces the tenant context before reading logs.
- **server/job-queue.ts:366** — cancelPendingJobsForTenant builds an SQL array literal by string-concatenating values from opts.excludeKinds into excludeLiteral and then embeds it into the query. Although values are escaped, this constructs SQL via string interpolation from caller-supplied data.
  - _Fix:_ Avoid constructing array literals by string interpolation. Use a parameterized array binding (pass excludeKinds as a parameterized array / use driver parameterization or Drizzle helpers) or rewrite the condition to use e.g. AND (array_length(${exclude}::text[],1) IS NULL OR kind <> ALL(${exclude}::text[])) with exclude passed as a bound parameter to eliminate any injection risk.
- **server/lib/action-ledger.ts:269** — Cross-tenant SELECT on tenant-scoped table `action_attempts` (listUnknownAttemptsAllTenants) has no tenant_id predicate — returns rows for all tenants.
  - _Fix:_ If this must be cross-tenant for a reconciler, restrict access to an approved admin/maintenance process and document/guard it; otherwise add a tenant predicate (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant so queries never return another tenant's rows.
- **server/lib/agent-trace.ts:124** — UPDATE agent_trace_spans in persistSpanClose identifies rows by span_id only (WHERE span_id = ${spanId}) and omits tenant_id. span_id is not a tenant predicate — this can update another tenant's span row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) or otherwise record and use the tenant when closing the span. Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id.
- **server/lib/bwb-job-progress.ts:74** — scope(jobId, ...) builds WHERE clauses that omit the tenant predicate when env/threaded BWB_TENANT_ID is missing. Several updates use scope(...) and therefore may execute without a tenant_id filter (writes pinned only to jobId).
  - _Fix:_ Do not rely on an external env var for tenant scoping. Require an explicit tenantId parameter for these progress writers and include eq(videoJobs.tenantId, tenantId) in the WHERE clause; or, if jobId-alone is used, fetch the job row first and assert its tenant_id before performing updates. Make the absence of tenant context an error (fail-closed) for mutating operations.
- **server/lib/system-probes.ts:163** — A read is performed on the tenant-scoped `messages` table (COUNT(*)) without a tenant_id filter — db.select().from(messagesTable) has no WHERE tenant_id = ... clause, so it returns cross-tenant message counts.
  - _Fix:_ If this probe is intended to be tenant-scoped, add a WHERE tenant_id = ${tenantId} and require the tenantId parameter. If it is an admin/system-wide probe, ensure the API exposing this function is restricted to admin-only contexts and document/centralize that it intentionally queries across tenants. Prefer explicit ALL_TENANTS naming and authorization checks rather than an unfiltered query.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is performed without a tenant_id constraint (scans all running agent_runs older than threshold). This returns rows across tenants.
  - _Fix:_ If the sweep must be cross-tenant only for admin/audit, ensure the function is restricted to admin-only callers and clearly document authorization. Otherwise add tenant_id = ${tenantId} (or accept tenantId param) to scope the query to a single tenant. Avoid unfiltered reads of tenant-scoped tables from code reachable by non-admin callers.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` is performed without a tenant_id constraint (scans all in_progress mind_tickets older than threshold). This returns rows across tenants.
  - _Fix:_ Same as above: scope the query by tenant_id if this routine should operate per-tenant, or enforce/run only in an admin context with strict access control and explicit documentation that the read is cross-tenant. Prefer explicit tenant filters when possible.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() with a value derived from function inputs: sql.raw(lockKey) is interpolated directly into a SQL fragment for pg_advisory_xact_lock. This is a raw SQL insertion of a request-derived value (ledgerKey/tenantId) and risks injection or incorrect parameterization.
  - _Fix:_ Avoid sql.raw() with caller-supplied values. Compute a numeric lock key and pass it as a parameterized value instead (for example: const numericKey = BigInt(lockKey); tx.execute(sql`SELECT pg_advisory_xact_lock(${numericKey}::bigint)`)). If raw must be used, strictly validate/encode the value first; prefer parameterization to eliminate injection/isolation risk.
- **server/routes.ts:2058** — Selecting presenter_sessions by token without constraining tenant_id (db.execute(sql`SELECT ... FROM presenter_sessions WHERE token = ${token}`)). This reads a tenant-scoped table without a tenant filter or verifying ownership.
  - _Fix:_ Either include tenant_id in the WHERE clause or verify the session row's tenant_id matches the expected tenant before returning data. If the token is a capability that must remain cross-tenant-accessible, ensure the token itself is strongly bound to a tenant (e.g. encode/verify tenant id in the signed token) and use it in the SQL WHERE clause (WHERE token = $1 AND tenant_id = $2). Avoid returning tenant-scoped rows based solely on an opaque value without verifying tenant binding.
- **server/routes.ts:2093** — Selecting presenter_slide_images by session_id (derived from an earlier presenter_sessions SELECT that lacked a tenant filter). Because the session lookup did not constrain tenant_id, this read path can access tenant-scoped image rows without SQL-level tenant scoping.
  - _Fix:_ When resolving session_id from token, ensure the initial presenter_sessions lookup is constrained to the caller's tenant (or that the token encodes/cryptographically binds the tenant), or add tenant_id to the subsequent presenter_slide_images query (e.g. join presenter_sessions and presenter_slide_images and include presenter_sessions.tenant_id = <expected tenant> in the SQL). Always enforce tenant_id in the DB query rather than relying on prior unscoped SELECTs.
- **server/routes.ts:2180** — Repair-images route selects presenter_sessions by token without tenant scoping (SELECT id, presentation_id, slides FROM presenter_sessions WHERE token = ${token}). This mirrors other presenter endpoints that query tenant-scoped rows without a tenant_id constraint.
  - _Fix:_ Ensure presenter_sessions lookups include tenant_id in the WHERE clause or verify the tenant binding of the token before accessing/updating any tenant-scoped resources. Prefer a single SQL statement that enforces both token and tenant_id (or use a signed token that includes tenant id and validate it server-side before querying).
- **server/routes.ts:1531** — Admin-only public-stats handler issues SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) without any tenant_id WHERE clause (db.select().from(conversations) etc.). These queries read tenant data across all tenants without explicit SQL tenant scoping.
  - _Fix:_ If the intent is to aggregate across all tenants for platform-admins, mark these tables/queries as platform-level reads and centralize that behavior with explicit comments and admin-only guards. Otherwise, scope queries to a specific tenant by adding WHERE tenant_id = <caller tenant>. Prefer to make cross-tenant queries explicit (e.g. authorized admin-only code paths) and still include tenant filtering where appropriate.
- **server/routes.ts:1560** — Architecture endpoint performs multiple db.execute(...) COUNT SELECTs on tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) with no tenant_id constraints, returning cross-tenant aggregates without explicit SQL tenant filtering.
  - _Fix:_ Either require and document that these are platform-admin global aggregations, gating them with requirePlatformAdmin (already present) and keep them centralized, or, if not intended, add explicit tenant_id filters to each query. For clarity and safety, prefer queries that explicitly state they are global admin reports and do not return per-tenant rows; ensure the admin-only guard cannot be bypassed.
- **server/routes.ts:767** — Backfill job selects projects across all tenants (SELECT p.id, p.name, p.tenant_id, t.name ... WHERE p.drive_folder_id IS NULL). This reads a tenant-scoped table without restricting by tenant_id.
  - _Fix:_ Run backfill on a per-tenant basis (iterate tenants and apply the backfill per tenant) or, if run as a global migration by platform-admins, keep it in a clearly documented admin-only migration path. When reading tenant-scoped rows, prefer adding tenant_id filters or explicitly documenting that the operation intentionally touches all tenants and runs in a trusted admin context.
- **server/routes.ts:2319** — Order lookup endpoint queries delivery_logs by order/session id without tenant scoping (SELECT ... FROM deliveryLogs WHERE orderId = ${sessionId} OR stripePaymentId = ${sessionId}). delivery_logs is tenant-scoped and this SELECT returns rows regardless of tenant_id, based solely on a capability-like session id.
  - _Fix:_ Ensure that capability URLs (sessionId) are cryptographically bound to tenant identity or include tenant_id in the WHERE clause when returning delivery_logs. Alternatively, after fetching the row, validate that the associated tenant_id is authorized for the requester before returning any tenant-scoped data. Prefer parameterized queries that include tenant_id (or validate the capability-to-tenant mapping) to enforce isolation at the DB level.
- **server/routes/conversations.ts:100** — SELECT from 'project_conversations' (project_id, conversation_id) performed with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. AND tenant_id = ${tenantId}) so reads from this tenant-scoped linkage table cannot return other tenants' rows.
- **server/routes/conversations.ts:105** — UPDATE on tenant-scoped table 'projects' executed without constraining tenant_id in the WHERE clause (race condition despite prior existence check).
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so the mutation cannot affect rows from other tenants between the SELECT check and UPDATE.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id constraint. heartbeat_tasks is tenant-scoped elsewhere in this file, so this read is ambiguous and may be followed by global writes.
  - _Fix:_ If this operation is intended to be tenant-scoped, add AND tenant_id = ${tenantId} (or ADMIN_TENANT_ID). If it is intended to be global/admin, make that explicit (comment) and ensure only an admin context runs it. Prefer iterating tenants and running tenant-scoped checks when modifying tenant data.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant_id. Subsequent code performs an UPDATE based on this count without tenant scoping.
  - _Fix:_ Add tenant_id filtering (e.g., WHERE ... AND tenant_id = ${ADMIN_TENANT_ID}) or perform per-tenant queries/updates to avoid touching other tenants' rows.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) is missing tenant scoping; later an UPDATE disables matching rows globally.
  - _Fix:_ Filter the SELECT by tenant_id (ADMIN_TENANT_ID if appropriate) or perform per-tenant operations. Avoid global reads on tenant-scoped tables when followed by writes.
- **server/seed.ts:4107** — INSERT INTO project_notes (project_id, note, author) does not include tenant_id. If project_notes is a tenant-scoped table, this violates the requirement that every INSERT set tenant_id explicitly (or validate ownership of derived foreign ids).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. tenant_id = <tenantId>) or derive and validate the tenant from the referenced project row before inserting (e.g. SELECT project.tenant_id WHERE id = <projId> and then use that tenant_id in the INSERT).
- **server/sentiment-detector.ts:167** — logSentimentEvent() inserts sentiment events including a conversation_id passed in by the caller but does not verify that the referenced conversation belongs to the provided tenantId. This writes a row that references a foreign conversation id without validating ownership.
  - _Fix:_ Before inserting, verify the conversation belongs to the tenant (e.g. SELECT 1 FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise ensure conversationId was obtained from a trusted, tenant-scoped source. Alternatively, avoid accepting raw conversation IDs from untrusted callers and derive the ID from an authenticated tenant context.
- **server/storage.ts:303** — SELECT from tenant-scoped `messages` (attachCitationMetadata) uses `WHERE id IN (...)` with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the query (e.g., AND tenant_id = ${tenantId} or join through a conversation/other known-tenant column) or, if IDs are derived from a prior, tenant-scoped query, assert that source was tenant-scoped and pass the tenantId into this function to filter by messages.tenant_id.
- **server/storage.ts:862** — SELECT memory_entries WHERE status='active' AND embedding IS NULL (getMemoriesWithoutEmbeddings) runs without any tenant_id filter, returning rows across all tenants.
  - _Fix:_ Either require an explicit tenant scope parameter or make this an explicit admin-only operation. If it must run across tenants (e.g. for a worker), iterate tenants explicitly or accept an ALL_TENANTS sentinel and gate execution with strong authorization and logging.
- **server/storage.ts:868** — SELECT agent_knowledge WHERE embedding IS NULL (getKnowledgeWithoutEmbeddings) has no tenant_id constraint — returns knowledge across tenants.
  - _Fix:_ Require explicit tenant scoping or an admin-only sentinel (ALL_TENANTS) and ensure only authorized code paths call this. Prefer adding requiredTenantScope to the call signature so callers must be explicit about tenant scope.
- **server/storage.ts:978** — SELECT from tenant-scoped `heartbeat_tasks` by id only (getHeartbeatTask) — no tenant_id constraint on the query.
  - _Fix:_ Add tenant scoping (require tenantId param and filter by eq(heartbeatTasks.tenantId, tenantId)) or document and restrict this unscoped helper to trusted internal callers and audit every use to ensure tenant ownership is validated before any downstream action.
- **server/webhook-triggers.ts:89** — createTrigger(config) inserts a caller-supplied personaId into the global webhook_triggers row without verifying that the persona actually belongs to the intended tenant/admin context. This stores a foreign persona_id reference that may point to a persona owned by another tenant.
  - _Fix:_ Validate the supplied personaId before persisting: fetch the persona and verify its tenant (or accept only admin-owned personas). Reject or clear personaId if it does not belong to the expected tenant/admin context. Alternatively, only allow persona selection through an admin UI and resolve/lock the persona server-side.
- **server/webhook-triggers.ts:60** — Queries in listTriggers() and processTriggerEvent() LEFT JOIN personas without any tenant-scoping on the personas table (persona_name is exposed even though personas are often tenant-scoped). Similarly, trigger.persona_id is later used to create/admin conversations without verifying persona ownership.
  - _Fix:_ When joining or resolving persona rows, restrict persona lookup to an appropriate tenant (e.g. ADMIN_TENANT_ID or the trigger's tenant if multi-tenant expected) or resolve persona via a trusted function that enforces ownership. Do not display or use persona_name/persona_id from personas unless ownership/tenant affiliation has been validated.

### LOW (1)

- **server/self-reflection.ts:355** — storeLesson() builds and executes SQL using sql`... AND fact LIKE ${"%" + lesson.toolName + "%"} ...` where lesson.toolName is untrusted input. While parameterized via drizzle's sql template, the code relies on dynamic LIKE patterns that could be abused for broad matches.
  - _Fix:_ Ensure lesson.toolName is validated/sanitized (length limits, allowed chars) before use. Prefer using parameterized equality on explicit fields when possible, and bound LIKE patterns (e.g. limit length) to avoid unexpected wide matches. (Note: this is not raw string interpolation — it's a cautionary hygiene item.)

### Suppressed — confirmed false positives (220)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE agent_evals SET ... WHERE id = ${evalId} (no tenant_id constraint). This update can modify eval rows belonging to other tenants.
  - _Why false positive:_ evalId is server-generated from the immediately preceding INSERT and updated within the same internal eval loop, not from an untrusted request boundary.
- **server/agentic-features.ts:699** _(upstream-verified)_ — UPDATE lead_enrichments ... WHERE id = ${lead.id} is executed without tenant scoping — allows updating a lead row belonging to another tenant.
  - _Why false positive:_ lead rows being updated come from a prior tenant-scoped SELECT on lead_enrichments for params.tenantId in the same flow.
- **server/agentic-features.ts:932** _(upstream-verified)_ — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} is executed without tenant scoping — can modify another tenant's enrollment row.
  - _Why false positive:_ row.id comes from a prior SELECT on outreach_enrollments constrained by e.tenant_id = params.tenantId in the same call path.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals WHERE status='pending' AND expiresAt < ... without scoping by tenant_id — this globally expires approvals across all tenants and later per-run updates rely on that result.
  - _Why false positive:_ global expiry is intentional housekeeping across tenants; returned rows carry tenantId and subsequent handling is per-row status maintenance only.
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — resumePendingApprovalRuns performs a global SELECT across agent_runs and agent_approvals without constraining by tenant_id — the initial candidate query scans rows for all tenants (`FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE (...) OR (...)`) and thus reads tenant-scoped tables unfiltered.
  - _Why false positive:_ this is a global maintenance worker sweep over pending runs/approvals; rows carry tenant_id per row and processing is per-row operational state handling, not tenant-facing data mixing.
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id (reads messages WHERE conversation_id = ${conversationId} but no AND m.tenant_id = ${tenantId}). This relies on prior conversation checks and does not satisfy the invariant that every tenant-scoped read must include an explicit tenant filter.
  - _Why false positive:_ conversation ownership is proven earlier by selecting conversations with id and tenant_id before reading messages for that conversation in the same path.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id (SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}). Although conversation ownership is checked earlier, the invariant requires every tenant-scoped read include an explicit tenant_id predicate.
  - _Why false positive:_ assertConversationInTenant plus tenant-scoped conversation/project checks immediately above prove ownership before reading messages for that conversation.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Subquery references tenant-scoped table `messages` without a tenant_id constraint: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id). This counts messages across tenants unless scoped by tenant, violating tenant isolation.
  - _Why false positive:_ backfillProjectTranscripts is a global housekeeping sweep; it only identifies candidate conversations and then processes each with its own c.tenant_id via autoSaveProjectTranscript.
- **server/auto-transcript.ts:153** _(system-sweep)_ — Subquery references tenant-scoped table `messages` without a tenant_id constraint in a multi-join query: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id). This may surface cross-tenant message counts and violates the requirement that tenant-scoped queries include tenant_id.
  - _Why false positive:_ alsoLinked is part of the same maintenance backfill; per-row tenant_id is carried from conversations and each row is handled separately without tenant-facing mixed output.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` lacks a tenant_id WHERE clause: UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL. An UPDATE against a tenant-scoped table must include tenant_id to avoid modifying rows in other tenants.
  - _Why false positive:_ this update occurs inside the backfill housekeeping loop using rows selected from joins that already bind conversation/project consistency and then per-row transcript save uses c.tenant_id.
- **server/channel-routing.ts:51** _(global-table)_ — DELETE FROM channel_routes WHERE channel = ${channel} deletes routing rows without scoping to the caller's tenant. This can remove other tenants' routes.
  - _Why false positive:_ channel_routes is created here with UNIQUE(channel) and no tenant_id, indicating a platform-global routing table rather than tenant-scoped data.
- **server/channel-routing.ts:54** _(global-table)_ — INSERT INTO channel_routes (channel, persona_id) VALUES (...) / ON CONFLICT ... does an insert/upsert without tenant scoping and without validating that the provided personaId belongs to the caller's tenant. This allows cross-tenant assignment and conflicts across tenants (unique(channel) is global).
  - _Why false positive:_ channel_routes/personas are shown without tenant_id and route lookup is global by design
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute executes DELETE FROM channel_routes WHERE channel = ${channel} with no tenant scoping. This can delete routes for all tenants that use the same channel identifier.
  - _Why false positive:_ delete targets same global channel_routes mapping; no tenant-scoped table shown
- **server/chat-engine.ts:572** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` is executed without constraining by tenant_id. The query `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...` omits any tenant filter, relying on prior project lookup instead of enforcing tenant scoping at the query.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by verified parent project id+tenant_id
- **server/chat-engine.ts:584** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` is executed without constraining by tenant_id. The query `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...` lacks a tenant_id filter.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by verified parent project id+tenant_id
- **server/chat-engine.ts:633** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` is executed without constraining by tenant_id. The query `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...` does not include tenant scoping.
  - _Why false positive:_ c.id comes from prior project_conversations join constrained by conversations.tenant_id = tenantId
- **server/chat-engine.ts:651** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id. The query `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...` lacks an explicit tenant_id filter.
  - _Why false positive:_ conversation ids were selected earlier from same call path with c.tenant_id = tenantId
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations uses only `id` (DELETE FROM conversations WHERE id = ${conv.id}) with no tenant_id predicate — this can delete conversation rows across tenant boundaries.
  - _Why false positive:_ maintenance purge loop selects rows with their own tenant_id and performs housekeeping deletion per row
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE on tenant-scoped table `doc_chunks` without tenant_id constraint (generateCollectionEmbeddings uses UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}). Although rows were selected earlier filtered by tenant, the UPDATE is issued by id only and lacks an explicit tenant filter, enabling cross-tenant modification if id is manipulated or in race scenarios.
  - _Why false positive:_ row.id values come from same function's prior SELECT on doc_chunks filtered by collection_id and tenant_id
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id (the INSERT omits tenant_id entirely). Per schema the tenant_id column is NOT NULL/no default so this can create cross-tenant rows or violate invariants.
  - _Why false positive:_ project_files has no tenant_id column and insert is guarded by assertProjectInTenant on tenant-scoped parent project_id
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table "code_proposals" without a tenant_id constraint — this will modify rows for all tenants matching the id list, potentially affecting other customers.
  - _Why false positive:_ stale-proposal archival is a platform-wide housekeeping sweep updating status only per selected rows, not tenant-facing mixed output
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE on tenant-scoped table "heartbeat_tasks" without a tenant_id constraint — this can disable/modify heartbeat tasks across tenants.
  - _Why false positive:_ stuck-heartbeat disable is a platform-wide maintenance sweep toggling enabled status only per selected rows
- **server/memory-graph.ts:253** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) does not set tenant_id. If memory_links is tenant-scoped, this INSERT creates a cross-tenant row with no explicit tenant ownership, violating the platform rule that every INSERT must pass tenantId explicitly.
  - _Why false positive:_ memory_links has no tenant_id column and links tenant-scoped memory_entries by FK-like ids; isolation is through the parent memory rows
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE against tenant-scoped table `knowledge_triples` omits tenant filter. The code updates rows by id only: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}` — no `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ ids updated come only from prior SELECT on knowledge_triples constrained by tenant_id = tenantId in same call path
- **server/minerva-planner.ts:472** _(upstream-verified)_ — Rollback UPDATE on `plans` lacks tenant constraint: `UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'` (no `tenant_id` filter). This can modify another tenant's row if tenantId was omitted earlier.
  - _Why false positive:_ rollback acts on args.planId after earlier row lookup/CAS established the plan row and authoritative row.tenant_id in same flow
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table 'oauth_subscriptions' without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants.
  - _Why false positive:_ startup validation intentionally scans subscriptions across tenants and handles each row with its own tenant_id for maintenance
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This enumerates tenant rows across all tenants.
  - _Why false positive:_ startup auto-reconnect enumerates tenant ids for a platform-wide maintenance loop and processes each tenant separately
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) inside proactiveTokenRefresh. This reads active subscriptions for all tenants.
  - _Why false positive:_ proactive token refresh is a cross-tenant housekeeping loop operating per subscription row with row-carried tenant_id
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE) inside forceRefreshAllSubscriptions. This enumerates all active subscriptions across tenants.
  - _Why false positive:_ force-refresh of active subscriptions is an admin maintenance sweep processing each tenant subscription independently
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} has no tenant_id constraint. personas is tenant-scoped; this allows reading personas across tenants (data leakage) and exporting another tenant's persona.
  - _Why false positive:_ schema note says personas has no tenant_id column, so this base table is not tenant-scoped in this codebase
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — UPDATE plans SET status = 'failed' ... WHERE id = ${planId} — no tenant_id constraint when marking a plan failed due to missing steps.
  - _Why false positive:_ Server-invoked executor already claimed and loaded this globally-unique plan id internally; follow-on status write is by trusted worker PK, not request input.
- **server/plan-executor.ts:513** _(system-sweep)_ — CAS claim query updates plan status to 'executing' using WHERE id = ${planId} AND status = 'approved' but lacks tenant_id constraint. This can cause one tenant/process to claim and execute a plan belonging to another tenant.
  - _Why false positive:_ Plan executor is a platform worker that intentionally claims approved plans across tenants and carries row.tenant_id per plan; this is global background processing, not tenant-mixed output.
- **server/plan-executor.ts:728** _(internal-worker-pk)_ — Final UPDATE of plans to set status = ${finalStatus} uses WHERE id = ${planId} without tenant_id constraint, allowing cross-tenant status changes.
  - _Why false positive:_ Final status update occurs inside the same trusted execution flow after claiming/reading the exact plan row; id is worker-driven and tenant carried on row.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans resets all plans stuck in 'executing' to 'approved' with UPDATE ... WHERE status = 'executing' AND updated_at < ... — no tenant_id filter. This will affect every tenant's executing plans globally.
  - _Why false positive:_ Boot-time recovery intentionally resets stale executing plans across tenants as housekeeping only; per-row content is not mixed or returned to tenants.
- **server/plan-executor.ts:787** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is used to kick pending plans but lacks tenant scoping, so the executor may pick up and run plans from other tenants.
  - _Why false positive:_ Boot-time pending-plan picker is an intentional global worker sweep over approved plans, dispatching each plan id independently for execution.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write against a tenant-scoped table without constraining tenant_id. Although the row was selected earlier with tenant_id, the UPDATE lacks tenant scoping and could affect another tenant's task with the same id.
  - _Why false positive:_ Task ids come from an earlier SELECT constrained by ht.tenant_id = tenantId in the same call path; update acts on those verified rows.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates agent_desks without tenant scoping. This can modify another tenant's agent desk record.
  - _Why false positive:_ Desk ids are sourced from the immediately preceding SELECT filtered by ad.tenant_id = tenantId; mutation is on already tenant-verified rows.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT FROM tenant-scoped table agent_knowledge without tenant_id constraint (reads rows for all tenants). The cron runner issues: SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500
  - _Why false positive:_ Heartbeat runner intentionally scans recurring_message jobs across tenants and processes each row using its own carried tenant_id; no cross-tenant aggregation output.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table agent_knowledge without tenant_id constraint (UPDATE agent_knowledge SET content=..., updated_at=NOW() WHERE id = ${r.id}). This can modify a row belonging to a different tenant if IDs are guessable or leaked.
  - _Why false positive:_ Global scheduler updates each due job row as part of a per-row maintenance loop using row-carried tenant_id elsewhere in the flow; no tenant content mixing.
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE research_sessions executed without tenant_id constraint in cleanupZombieSessions: UPDATE research_sessions SET ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold} — this will affect sessions across all tenants.
  - _Why false positive:_ Zombie-session cleanup is platform-wide housekeeping that only flips status/end fields on stale rows and returns/logs ids, not tenant-facing mixed data.
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() — no tenant_id constraint. This will delete autoresearch entries for every tenant.
  - _Why false positive:_ Expired autoresearch knowledge purge is a global maintenance delete of stale rows across tenants, with no tenant-facing data return or mixing.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project and conversation already validated to same tenant
- **server/routes/conversations.ts:229** _(fk-transitive)_ — INSERT into 'project_notes' does not include tenant_id (tenant-scoped table write without explicit tenant column).
  - _Why false positive:_ project_notes has no tenant_id and note is inserted under projId derived from tenant-scoped project lookup
- **server/seed.ts:3882** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without any tenant_id restriction — this deletes matching rows across ALL tenants.
  - _Why false positive:_ boot-time housekeeping purge intentionally cleans stale heartbeat tasks across tenants without returning tenant data
- **server/self-improvement.ts:243** _(upstream-verified)_ — runExperiment() updates the `experiments` row via db.update(...).where(eq(experiments.id, experiment.id)) without constraining by tenant_id. This allows an update against an experiments row by id without double-checking the tenant owner.
  - _Why false positive:_ shown line is a tenant-scoped SELECT already constrained by experiments.tenantId = tenantId
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' without constraining tenant_id (reads conversation messages via messages.conversationId but omits tenant filter). This can allow cross-tenant data disclosure if conversation IDs are guessable or re-used.
  - _Why false positive:_ analyzeConversation(conversationId, tenantId) first verifies the conversation belongs to tenantId before recent messages are read by that conversationId
- **server/skillify.ts:202** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' without tenant_id constraint (analyzeConversation reads messages by conversationId but does not include tenant scoping).
  - _Why false positive:_ messages are fetched only after conversations table confirmed this conversationId belongs to tenantId in same function
- **server/skillify.ts:223** _(global-table)_ — SELECT from tenant-scoped table 'personas' without tenant_id constraint (lookup by persona id only). This can leak persona data across tenants.
  - _Why false positive:_ schema note says personas has no tenant_id, so persona lookup is against a non-tenant-scoped table
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' (recentMsgs in skillifyConversation) without tenant_id constraint. The recent messages selection uses only conversationId and lacks tenant scoping.
  - _Why false positive:_ skillifyConversation calls analyzeConversation(conversationId, tenantId) first, which validates tenant ownership of the conversation
- **server/storage.ts:876** _(system-sweep)_ — UPDATE memory_entries SET status='archived' WHERE ... (archiveExpiredMemories) runs without any tenant_id predicate — this mutates rows for all tenants in bulk.
  - _Why false positive:_ archiveExpiredMemories is a platform-wide lifecycle sweep updating only status fields and returning counts, not exposing mixed tenant data
- **server/storage.ts:892** _(system-sweep)_ — UPDATE memory_entries SET status='archived' WHERE created_at <= ${cutoff} ... (archiveStaleMemories) executes with no tenant_id constraint — bulk cross-tenant mutation.
  - _Why false positive:_ archiveStaleMemories is a platform-wide housekeeping sweep that only archives stale rows and returns counts
- **server/surprise-scorer.ts:93** _(internal-worker-pk)_ — UPDATE on tenant-scoped table felix_proposals (stampArgsEmbedding) updates args_embedding WHERE id = ${proposalId} but omits a tenant_id constraint. This can modify a row belonging to another tenant if ids are guessed or collision occurs.
  - _Why false positive:_ Best-effort internal worker stamps a server-generated proposal row by global PK; call path shown comes from prior tenant-scoped SELECT of same proposal.
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — UPDATE sets actual_outcome_embedding WHERE id = ${proposalId} with no tenant_id filter. This writes to a tenant-scoped row without constraining by tenant.
  - _Why false positive:_ Same function already selected the proposal with WHERE id = proposalId AND tenant_id = tenantId and operates on that verified row.
- **server/surprise-scorer.ts:149** _(upstream-verified)_ — SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} has no tenant_id constraint; re-fetch is used to drive later logic and subsequent updates are not tenant-scoped.
  - _Why false positive:_ Re-fetch occurs inside same call path after tenant-scoped verification of the same proposalId; used only for continuation of that verified row's processing.
- **server/surprise-scorer.ts:181** _(upstream-verified)_ — UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} lacks a tenant_id filter, causing tenant-unsafe write.
  - _Why false positive:_ Update is in same function after tenant-scoped lookup of the proposal row; proposal ownership was already verified earlier in the call path.
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} is missing tenant_id scoping and will update proposals across tenants.
  - _Why false positive:_ Final score write is for the same proposal previously verified by id and tenant_id in this function.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples omits tenant scoping — the UPDATE uses WHERE id = ${row.id} with no tenant_id constraint, allowing cross-tenant mutation if an attacker can guess an id.
  - _Why false positive:_ Rows being updated were selected immediately above from knowledge_triples with tenant_id = tenantId, so row.id values are already tenant-scoped.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} executes without enforcing the caller's tenant constraint (reading a tenant-scoped table by id only).
  - _Why false positive:_ This read is an ownership verification step comparing conversation.tenant_id to the pairing's claimed tenantId and fails closed on mismatch.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} updates a tenant-scoped table by id only, with no tenant_id constraint — allows modifying another tenant's ledger row if an id is misused.
  - _Why false positive:_ Internal budget worker settles a reservation row by server-issued reservationId primary key, not a shown untrusted request parameter.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table without constraining by tenant_id.
  - _Why false positive:_ Internal budget worker releases a reservation row by server-issued reservationId primary key, not a shown untrusted request parameter.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — Global SELECT from tenant-scoped table `experiments` has no tenant_id constraint (reads rows across all tenants).
  - _Why false positive:_ Heartbeat maintenance loop intentionally scans running experiments across tenants and then processes each row using its own row-carried tenant_id.
- **server/agent-activity.ts:60** _(global-table)_ — SELECT from `personas` does not constrain by tenant_id (getPersonality(personaId) reads persona without verifying tenant ownership).
  - _Why false positive:_ personas schema shows no tenant_id; read is from platform-global personas metadata only
- **server/agent-desk.ts:297** _(global-table)_ — SELECT id, name FROM personas is executed without a tenant_id filter when building desks overview — reads global persona rows rather than tenant-scoped ones.
  - _Why false positive:_ personas has no tenant_id and is used as global id→name lookup
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting SELECTs from tenant-scoped table `ai_insights` without any tenant_id constraint — a global sweep reads rows across tenants.
  - _Why false positive:_ global durability sweep processes each ai_insights row with its own tenant_id and does not mix tenant data into one output
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT on tenant-scoped table `department_budgets` without a tenant_id WHERE filter (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads rows across all tenants from a tenant-scoped table.
  - _Why false positive:_ heartbeat maintenance sweep iterates rows carrying tenant_id and emits per-tenant events only
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on agent_wake_schedules claims rows via an inner SELECT that lacks any tenant_id constraint. The statement updates/returns wakes across all tenants (no WHERE tenant_id = ...), allowing cross-tenant processing/leakage of tenant-scoped rows.
  - _Why false positive:_ scheduler intentionally claims due rows across tenants and handles each row with its own tenant_id when emitting events/updating status
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — Background backfill SELECT queries scan the conversations table without a tenant_id constraint (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...). This reads tenant-scoped conversation rows across all tenants.
  - _Why false positive:_ backfill sweep iterates conversations with row-carried tenant_id/project_id and performs housekeeping capture per row rather than returning mixed tenant output
- **server/business-tools.ts:96** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id (INSERT INTO invoice_items (invoice_id, description, ...) VALUES (${newId}, ...)). If invoice_items is a tenant-scoped table (or relied-on for isolation), omitting tenant_id allows cross-tenant item rows tied only by invoice_id and can break the app-level isolation invariant.
  - _Why false positive:_ invoice_items has no tenant_id and is a child of tenant-scoped invoices verified/created in-transaction
- **server/capability-review.ts:491** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `capability_reviews` does not constrain by tenant_id. markReviewReused() issues `UPDATE capability_reviews SET reused = true ... WHERE id = ${pending.reviewId} AND reused = false` — updating by id alone can modify another tenant's row with the same numeric id.
  - _Why false positive:_ best-effort internal worker updates a server-tracked review by primary key from in-memory pending state, not direct untrusted input
- **server/channel-routing.ts:30** _(global-table)_ — SELECT joins tenant-scoped table `personas` (p) without constraining by tenant. The query SELECT cr.*, p.name as persona_name FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id WHERE cr.enabled = true can return personas from other tenants.
  - _Why false positive:_ channel_routes is defined here without tenant_id and unique(channel), indicating intentional platform-global routing
- **server/channel-routing.ts:63** _(global-table)_ — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true reads tenant-scoped data without a tenant filter. Without scoping, it can return another tenant's persona_id.
  - _Why false positive:_ channel_routes is an intentional platform-global table with no tenant_id and global unique channel mapping
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT on tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across tenants.
  - _Why false positive:_ global purge job intentionally scans expired conversations and processes each row with its own tenant_id for scoped child deletes/backup
- **server/deep-interview.ts:337** _(fk-transitive)_ — INSERT INTO project_notes(...) SELECT p.id, ${briefContent}, NOW() ... does not set tenant_id on the inserted project_notes row. Tenant ownership is not being written explicitly into the child row.
  - _Why false positive:_ project_notes has no tenant_id column and is inserted via tenant-scoped parent project selected with p.tenant_id = state.tenantId
- **server/email.ts:470** _(system-sweep)_ — Raw SELECT against tenant-scoped table `inbox_messages` without tenant_id filter: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1` (pollSharedInbox). This reads a tenant-scoped table globally and can return rows for other tenants.
  - _Why false positive:_ shared inbox polling is platform-wide housekeeping/dedup and only checks existence before per-message storage; no tenant-facing mixed output
- **server/email.ts:515** _(system-sweep)_ — Raw SELECT against tenant-scoped table `inbox_messages` with no tenant filter: `SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...)` (backfillEmptyBodies). This scans/reads all tenants' inbox_messages.
  - _Why false positive:_ backfill job scans rows across tenants only to repair empty body fields and does not return mixed tenant data
- **server/email.ts:528** _(system-sweep)_ — Raw UPDATE against tenant-scoped table `inbox_messages` without tenant constraint: `UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id}` (backfillEmptyBodies). This can update rows belonging to other tenants if an id is ambiguous or attacker controls ids.
  - _Why false positive:_ maintenance backfill updates housekeeping/content fields on each row by its server-read primary key, not from tenant request input
- **server/embeddings.ts:751** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in backfillMissingKnowledgeEmbeddings: `SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL AND source = 'autoresearch' ...`. This will return rows across tenants.
  - _Why false positive:_ embedding backfill is a platform-wide maintenance loop selecting rows to compute vectors; not tenant-facing output
- **server/embeddings.ts:780** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` without tenant filter in backfillEmbeddingVecs: `SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200`. This reads rows from all tenants.
  - _Why false positive:_ vector backfill scans tenant rows globally for housekeeping field population only, with no mixed tenant response
- **server/embeddings.ts:790** _(system-sweep)_ — UPDATE in backfillEmbeddingVecs: `UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}` has no tenant constraint, allowing updates to tenant rows by id only.
  - _Why false positive:_ maintenance backfill updates only embedding_vec on rows selected by the sweep; housekeeping mutation, not tenant-facing data access
- **server/embeddings.ts:796** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in backfillEmbeddingVecs: `SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200`. This will read rows for all tenants.
  - _Why false positive:_ Boot/maintenance backfill intentionally scans across tenants and only updates housekeeping embedding_vec per row; no tenant data is returned or mixed into one tenant-facing output.
- **server/embeddings.ts:807** _(system-sweep)_ — UPDATE in backfillEmbeddingVecs: `UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}` executes without tenant constraint and can update another tenant's row by id.
  - _Why false positive:_ Update is part of the same global backfill sweep and mutates only the selected row's housekeeping vector field by row id, with no tenant-facing data exposure or mixing.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' by project id with NO tenant_id constraint (reads a foreign id supplied by caller without scoping).
  - _Why false positive:_ The function immediately verifies project.tenantId === tenantId before using the row; ownership enforcement is in the same call path.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' (setting driveFolderId/driveFolderUrl) uses WHERE projects.id = projectId but does NOT include tenant_id in the WHERE clause.
  - _Why false positive:_ Update follows a same-function ownership check proving the project belongs to tenantId before the id-only update executes.
- **server/google-drive.ts:827** _(upstream-verified)_ — Handler reads the 'projects' row by caller-supplied projectId without constraining by tenant_id or otherwise proving caller ownership first — risk of cross-tenant data exposure/manipulation.
  - _Why false positive:_ The code reads the project then immediately fail-closes on tenant mismatch before proceeding, and downstream ensureProjectFolder repeats the ownership check.
- **server/google-drive.ts:1004** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' to validate a projectId is used without scoping by tenant_id (reads a foreign id from caller input with no tenant WHERE).
  - _Why false positive:_ This select is itself the ownership-validation step and only returns tenantId, then aborts on mismatch before any tenant data search runs.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table "code_proposals" without constraining by tenant_id — this will return rows across all tenants.
  - _Why false positive:_ Admin health-audit stale-row sweep intentionally operates platform-wide and only reports/archives housekeeping status for stale proposals, not tenant-facing mixed content.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table "heartbeat_tasks" without constraining by tenant_id — this can return tasks belonging to other tenants.
  - _Why false positive:_ Platform-wide maintenance sweep for stuck heartbeat tasks; it only flags/disables housekeeping rows and does not expose mixed tenant content to tenant-facing surfaces.
- **server/heartbeat.ts:105** _(system-sweep)_ — SELECT * FROM research_schedules is reading a tenant-scoped table with no tenant_id constraint in the WHERE clause.
  - _Why false positive:_ Heartbeat cron intentionally scans due research_schedules across tenants and processes each row with its own tenant_id; this is a platform scheduler loop, not a tenant-facing read.
- **server/heartbeat.ts:155** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} updates a tenant-scoped table without constraining by tenant_id in the WHERE clause.
  - _Why false positive:_ Update is part of the platform-wide scheduler sweep and only advances housekeeping timing fields on the schedule row being processed.
- **server/heartbeat.ts:160** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id} lacks tenant scoping in the WHERE clause.
  - _Why false positive:_ Same maintenance scheduler path; it updates only last_run_at/next_run_at on the current schedule row, with per-row tenant handling and no tenant data mixing.
- **server/heartbeat.ts:165** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} missing tenant constraint.
  - _Why false positive:_ heartbeat cron advances schedule housekeeping fields across tenants using each row's own sched context; no tenant-facing data mixing
- **server/heartbeat.ts:173** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} executed in an error-recovery path without tenant scoping.
  - _Why false positive:_ error-recovery path in global heartbeat sweep only updates next_run_at on the current schedule row; housekeeping only
- **server/heartbeat.ts:438** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... reads event_log (which contains tenant_id) without a tenant filter — this returns events across tenants.
  - _Why false positive:_ global worker intentionally sweeps pending events across tenants and routes each row with its own tenant_id; not returned/mixed into one tenant response
- **server/heartbeat.ts:2817** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates heartbeat_tasks without constraining by tenant_id after creating a task.
  - _Why false positive:_ newTask was just created via createHeartbeatTask with tenantId in the same call path; follow-up update targets that server-created row
- **server/heartbeat.ts:3269** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) lacks tenant scoping in the WHERE clause.
  - _Why false positive:_ newTask is freshly created in the same function with tenantId supplied, so id comes from server-side creation not untrusted input
- **server/heartbeat.ts:2723** _(global-table)_ — processDelegations calls storage.getPersonas() (allPersonas) without a tenant filter, then assigns tasks to personas by id/name without verifying tenant ownership.
  - _Why false positive:_ referenced schema says personas has no tenant_id, so global persona registry lookup is not a tenant-isolation violation
- **server/heartbeat.ts:2915** _(global-table)_ — delegateTaskFromChat calls storage.getPersonas() without tenant context and can select a target persona across tenants, then uses that persona.id in a tenant-scoped task creation.
  - _Why false positive:_ personas table is non-tenant-scoped per schema; selecting from global persona list is intentional
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id filter — the worker may atomically claim and mutate jobs across tenants.
  - _Why false positive:_ job queue claim is a platform-wide worker sweep over pending jobs; each claimed row carries its own tenant_id for downstream handling
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases STEP 1 updates agent_jobs to 'failed_terminal' without a tenant_id constraint — it mutates rows across all tenants.
  - _Why false positive:_ lease-reclaim is cross-tenant housekeeping mutating only job status/lease fields, with no tenant-facing data return or mixing
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases STEP 2 requeues expired agent_jobs (UPDATE ... SET status='pending') with no tenant_id filtering — this touches all tenants' rows.
  - _Why false positive:_ Global lease-reclaim housekeeping across tenants only flips job status/timing fields and returns touched-row counts, not tenant data.
- **server/job-queue.ts:172** _(internal-worker-pk)_ — completeJob updates agent_jobs to 'succeeded' using only id/attempts in the WHERE clause (no tenant_id) — a caller can complete a job id belonging to another tenant.
  - _Why false positive:_ Server worker completion path updates by server-generated job PK plus attempt fencing; id is not shown coming from a tenant request boundary.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob SELECTs max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — this reads tenant-scoped data by bare id.
  - _Why false positive:_ Worker failure handler reads max_attempts by server-generated job PK inside internal queue processing, not from an untrusted tenant-facing boundary here.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob UPDATE to mark jobs 'failed_terminal' writes agent_jobs WHERE id = ${id} ... without tenant_id — it can mutate another tenant's job.
  - _Why false positive:_ Internal worker state transition to failed_terminal is by server-generated job PK with attempts fencing, matching worker-by-PK benign pattern.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob requeue UPDATE (setting status = 'pending' and next_run_at) lacks tenant_id in the WHERE clause — cross-tenant rows may be requeued.
  - _Why false positive:_ Internal worker retry-state update uses server-generated job PK and attempt fencing rather than tenant-requested identifiers.
- **server/lib/action-ledger.ts:305** _(system-sweep)_ — UPDATE on tenant-scoped table `action_attempts` (sweepStaleAttempts) performs a cross-tenant mass update (no tenant_id constraint) via a WHERE id IN (SELECT id FROM action_attempts ...). This will change rows across all tenants.
  - _Why false positive:_ Crash-recovery reconciler intentionally sweeps stale rows across tenants, only mutating state/error fields in bounded batches and not returning mixed tenant content.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `procedure_evolution_runs` uses only runId in WHERE (where(eq(procedureEvolutionRuns.id, runId))) and omits a tenant_id predicate — a caller could flip another tenant's run row by id.
  - _Why false positive:_ runId is created by the same function from a fresh insert and immediately used for internal status updates, not taken from an external request boundary.
- **server/lib/aevo-meta-editor.ts:458** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `procedure_evolution_runs` (noop_proposal path) updates by run id only (.where(eq(procedureEvolutionRuns.id, runId))) with no tenant_id constraint.
  - _Why false positive:_ This noop-path update targets the just-inserted run row by server-created PK within the same call flow.
- **server/lib/aevo-meta-editor.ts:500** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `procedure_evolution_runs` (set done) uses runId only in WHERE (no tenant_id predicate), allowing cross-tenant row modification by id.
  - _Why false positive:_ Final done-status update uses the same internally created runId, so it matches internal worker/server-generated PK handling.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT against tenant-scoped table `projects` is executed without constraining by tenant_id (reads rows across all tenants). This violates the project's tenant-isolation invariant (application-level SQL filters are the only isolation mechanism).
  - _Why false positive:_ Boot/backfill maintenance query intentionally aggregates counts across tenants for per-tenant satisfaction checks and does not expose row data or mix tenant content into a tenant-facing output.
- **server/lib/inbox-ingest.ts:507** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_classifications` lacks a tenant_id constraint in the WHERE clause (UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}). This allows a row from a different tenant with the same inbox_message_id to be updated.
  - _Why false positive:_ Row was just claimed/inserted for this tenant using unique inbox_message_id; update targets that claimed row by same server-derived inbox_message_id.
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — Query over tenant-scoped table `deliverable_grade_events` lacks a tenant_id constraint — it SELECTs across all tenants (GROUP BY tenant_id, format) which violates the requirement that every read against a tenant-scoped table be constrained to the caller's tenant.
  - _Why false positive:_ Nightly optimizer scan intentionally aggregates across tenants for maintenance/promotion; handles per-tenant stats separately and does not return mixed tenant content to a tenant-facing surface.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE against tenant-scoped table `scheduled_posts` is executed without constraining by tenant_id (CTE picks due rows across all tenants and the subsequent UPDATE sets status='publishing' by id only). This mutates tenant rows without an explicit tenant-level filter tied to the caller context.
  - _Why false positive:_ Heartbeat runner is a platform-wide scheduler picking due rows across tenants, then processes each row with its own returned tenant_id.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE against tenant-scoped table tenant_voice_profiles uses WHERE id = existing.id but lacks an explicit tenant_id constraint. This allows an update to affect a row with the same id belonging to a different tenant.
  - _Why false positive:_ existing row is fetched via getVoiceProfile({ tenantId, profileName }) before update; update uses that already tenant-scoped row's primary key.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} lacks a tenant_id constraint. This mutating query can affect another tenant's category row.
  - _Why false positive:_ catId comes from ensureCategory(), which selects/inserts memory_categories scoped by the same tenantId immediately above.
- **server/memory-graph.ts:240** _(fk-transitive)_ — SELECT 1 FROM memory_links ... WHERE (source_memory_id = ${newMemoryId} OR target_memory_id = ${target.id}) lacks any tenant restriction. A read against a tenant-scoped link table must be constrained to the caller's tenant.
  - _Why false positive:_ Referenced schema says memory_links has no tenant_id; links are between tenant-scoped memory_entries already selected for one tenant, so isolation is transitive through the foreign keys.
- **server/memory-graph.ts:494** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks a tenant_id constraint. Updating a memory row by id only can affect another tenant's row with the same id.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries constrained by tenant_id = tenantId in the same function.
- **server/minerva-planner.ts:367** _(internal-worker-pk)_ — Compare-and-swap UPDATE on `plans` may run without tenant constraint. The WHERE clause includes tenant filtering only when `args.tenantId != null`, otherwise the UPDATE executes without `tenant_id = ...`.
  - _Why false positive:_ Comments and code show tenantless path is only for internal system callers operating on server-generated plan ids they already own, not untrusted request ids.
- **server/minerva-planner.ts:386** _(internal-worker-pk)_ — SELECT probe of `plans` can be performed without tenant constraint when `args.tenantId` is omitted: `SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}`.
  - _Why false positive:_ Tenantless probe is in the same internal-only decidePlan path for server-owned plan ids, not an untrusted cross-tenant boundary.
- **server/minerva-planner.ts:403** _(internal-worker-pk)_ — SELECT to find child plans `SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}` conditionally omits tenant filtering when `args.tenantId` is not provided.
  - _Why false positive:_ Child-plan lookup without tenant filter is only on the documented internal caller path using a server-generated parent plan id already owned by the worker.
- **server/orchestrator-ledger.ts:458** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` does not include a tenant_id constraint. The UPDATE uses WHERE id = ${row.id} but omits tenant_id, allowing a stale/incorrect id to update a row belonging to another tenant.
  - _Why false positive:_ row.id comes from a prior SELECT on memory_entries scoped by source, tenant_id, and category within the same transaction/advisory lock
- **server/pace-control.ts:85** _(global-table)_ — Query against tenant-scoped table `heartbeat_logs` (COUNT over recent runs) lacks any tenant_id constraint — the function aggregates across all tenants. TenantId is not accepted as an argument, so this read leaks cross-tenant usage and can cause incorrect enforcement of per-tenant pace limits.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column NO and is used here for platform-wide pace caps
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot issues a SELECT COUNT(*) FROM heartbeat_logs WHERE created_at > ... with no tenant_id constraint, returning global data across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and getPaceSnapshot is an operator/debugging global snapshot
- **server/pace-control.ts:147** _(global-table)_ — Query that builds personaRows reads heartbeat_logs (GROUP BY persona_name) without any tenant_id filter — this will aggregate across all tenants and leak/mix tenant data.
  - _Why false positive:_ heartbeat_logs has no tenant_id column; grouped persona counts are from a platform-global log table
- **server/persona-export.ts:39** _(global-table)_ — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL has no tenant_id filter. If skills contains tenant-scoped entries, persona-specific skills may be leaked or mixed across tenants.
  - _Why false positive:_ skills is marked tenant_id column NO, so this read is from a non-tenant-scoped table rather than a tenant isolation bug
- **server/process-governor.ts:121** _(global-table)_ — SELECT subqueries against heartbeat_logs (task_failure_rate) do not include tenant_id filter. The inner COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ... can return rows for other tenants.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column NO, so the subqueries cannot be tenant-scoped and operate on a global log table
- **server/process-governor.ts:199** _(global-table)_ — response_time query reads heartbeat_logs without tenant scoping (no tenant_id WHERE clause). This can aggregate other tenants' metrics.
  - _Why false positive:_ heartbeat_logs has no tenant_id column; this computes global performance metrics from a platform-global log table
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures SELECT counts distinct persona_id FROM heartbeat_logs without tenant_id. This can include other tenants' failing agents.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column, so this aggregate is over a global log table
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId} — missing tenant_id filter. memory_entries is tenant-scoped but the UPDATE only filters by id.
  - _Why false positive:_ storedId comes from the immediately prior SELECT constrained by tenant_id and persona/category/dedup prefix in the same call path.
- **server/routes.ts:1116** _(signed-url)_ — Querying file_storage rows by filename without constraining tenant_id (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))); this reads rows across all tenants and relies on application logic to check ownership instead of restricting at SQL level.
  - _Why false positive:_ This endpoint intentionally checks ownership for a signed-URL/file delivery flow and only reads tenantId values to validate access; signed capability is the access control.
- **server/routes.ts:2124** _(upstream-verified)_ — Inserting presenter_sessions with tenant_id taken directly from the request body (INSERT INTO presenter_sessions ... VALUES (${tenantId}, ...)). The handler only validates that tenantId is a positive integer and does not verify the caller is authorized to create rows for that tenant.
  - _Why false positive:_ Creation endpoint is gated by constant-time verification of x-internal-key against SESSION_SECRET before accepting body tenantId, so caller is authorized as internal server client.
- **server/routes.ts:4341** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' is missing tenant_id in the VALUES clause. The code inserts project notes with INSERT INTO project_notes (project_id, note, author) VALUES (...) without setting tenant_id, relying only on a prior SELECT from conversations for project_id.
  - _Why false positive:_ project_notes has no tenant_id column and is inserted only after selecting project_id from tenant-scoped conversations with tenant_id = tenantId.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' executed without constraining tenant_id in the WHERE clause.
  - _Why false positive:_ conv.id was just created with tenantId and project ownership was verified with SELECT id FROM projects ... AND tenant_id = tenantId in the same path.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_messages` is executed without constraining by tenant_id (WHERE id = ${msg.id} only). This allows a cross-tenant write if msg.id were to reference a row belonging to another tenant.
  - _Why false positive:_ msg is fetched immediately above with WHERE id = msgId AND tenant_id = tenantId; the update uses that verified row id in the same request path.
- **server/routes/oauth-subscriptions.ts:325** _(system-sweep)_ — Query reads oauth_subscriptions without constraining tenant_id (SELECT DISTINCT tenant_id FROM oauth_subscriptions ...). This performs a cross-tenant read on a tenant-scoped table.
  - _Why false positive:_ Cron-secret-gated maintenance sweep intentionally enumerates tenants with deactivated subscriptions and processes each tenant separately by its own tenant_id.
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes omits tenant_id: INSERT INTO project_notes (project_id, note, author) VALUES (...). Tenant-scoped tables require tenant_id on INSERT.
  - _Why false positive:_ project_notes has no tenant_id column and insert is preceded by project ownership check on projects WHERE id = id AND tenant_id = tenantId.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (base64 handler) omits tenant_id: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...). Tenant-scoped tables require tenant_id on INSERT.
  - _Why false positive:_ project_files has no tenant_id column and insert is tied to a project verified in the same path with tenant-scoped project lookup.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} lacks tenant_id constraint. This updates a tenant-scoped table row without scoping to the caller's tenant.
  - _Why false positive:_ Project row was verified immediately above with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before updating updated_at.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into project_files (multer handler) omits tenant_id: INSERT INTO project_files (...). Tenant-scoped tables require tenant_id on INSERT.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped through project_id to tenant-scoped projects; insert is by design after project ownership check
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (multer handler) lacks tenant_id constraint — same unconstrained update as in base64 path.
  - _Why false positive:_ same handler already verified projects.id = projectId AND tenant_id = tenantId before this update; update only touches that verified parent row
- **server/routes/projects.ts:319** _(upstream-verified)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC lacks tenant_id constraint. This reads tenant-scoped rows without explicitly scoping to the caller's tenant.
  - _Why false positive:_ route first checks the requested project belongs to tenant via projects WHERE id AND tenant_id, then reads child rows by that verified project_id
- **server/routes/projects.ts:342** _(upstream-verified)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not include tenant_id, allowing cross-tenant file lookups.
  - _Why false positive:_ download route verifies project ownership in projects with tenant_id before selecting project_files for that same project_id
- **server/routes/projects.ts:371** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} omits tenant_id. Deletion of tenant-scoped rows must be constrained to the caller's tenant.
  - _Why false positive:_ delete route first confirms the project belongs to the caller tenant; delete is limited to a child row under that verified project_id
- **server/routes/stripe-checkout.ts:104** _(global-table)_ — Raw/tagged SQL interpolates a request-derived value (priceId) into sql`... ${priceId} ...` when checking allowed prices. This is a use of string-interpolated SQL with request data (sql`... ${...}`) which the audit rules flag as an injection + isolation risk.
  - _Why false positive:_ stripe price/product allowlist tables are platform-global, not tenant-scoped; finding is not a cross-tenant isolation issue
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE sculptor_sessions executed without constraining tenant_id (WHERE id = ${session.id} only). This writes to a tenant-scoped table without enforcing the caller's tenant.
  - _Why false positive:_ update is by server-generated session.id returned from the prior insert in the same function, not from an untrusted request boundary
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id but omits tenant filter (WHERE id = ${session.id} only). Missing tenant_id allows cross-tenant mutation if ids are abused.
  - _Why false positive:_ update uses server-created session.id from the just-inserted row in the same internal flow, not caller-controlled cross-tenant input
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true executes without tenant_id constraint — this will disable delegation tasks across all tenants.
  - _Why false positive:_ Boot-time housekeeping sweep intentionally disables runaway heartbeat task rows across tenants; only mutates enabled flag and logs aggregate count.
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant_id constraint and will affect rows across tenants.
  - _Why false positive:_ Boot-time cleanup disables misnamed session-logger tasks platform-wide; housekeeping-only mutation with aggregate logging, no tenant data returned.
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true lacks tenant_id and will disable rows across tenants that match the name patterns.
  - _Why false positive:_ Maintenance sweep disables orphaned repo-examination heartbeat tasks across tenants; status-only cleanup, no tenant content exposure.
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped (has tenant_id) so this reads global counts across tenants.
  - _Why false positive:_ Dev snapshot import is a boot-time global seed/backfill check; count is used only to decide whether to seed, not exposed to tenants.
- **server/seed.ts:2158** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter. research_programs is tenant-scoped, so this will return rows from all tenants.
  - _Why false positive:_ Global maintenance loop scans active research programs to repair invalid model ids; per-row updates by PK and only emits aggregate/name logs, not tenant-facing mixed output.
- **server/seed.ts:2516** _(fk-transitive)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations ...) queries conversations without any tenant filter. conversations is typically tenant-scoped, so this will examine rows across tenants. The related project_conversations table also lacks a tenant_id column, which complicates safe multi-tenant backfills.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table of tenant-scoped projects/conversations; backfill inserts only FK pairs, consistent with transitive isolation.
- **server/seed.ts:3910** _(system-sweep)_ — UPDATE memory_entries ... executed without a tenant_id constraint — this will modify memory entries for all tenants.
  - _Why false positive:_ Canonical-spelling normalizer is a platform-wide maintenance migration on memory_entries; it mutates rows in place and only logs aggregate count.
- **server/seed.ts:3936** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id filter and will update conversations across all tenants.
  - _Why false positive:_ Boot-time legacy model-id backfill updates conversation metadata across tenants as housekeeping only; no row data returned or mixed into tenant output.
- **server/storage.ts:314** _(upstream-verified)_ — Unscoped SELECT from `conversations` to derive tenant_id in createMessage: query filters only by conversation id, no tenant_id constraint.
  - _Why false positive:_ Unscoped read is immediately used only to derive conversation tenant and enforce any provided data.tenantId matches before inserting message with convTenantId.
- **server/storage.ts:841** _(system-sweep)_ — UPDATE memory_entries SET embedding ... WHERE id = ${id} (updateMemoryEmbedding) has no tenant_id constraint — mutates tenant-scoped rows by id only.
  - _Why false positive:_ Embedding maintenance functions operate across tenants by row id for background processing and only mutate housekeeping embedding fields, not tenant-facing mixed data.
- **server/storage.ts:853** _(system-sweep)_ — UPDATE agent_knowledge SET embedding ... WHERE id = ${id} (updateKnowledgeEmbedding) lacks tenant scoping — updates knowledge rows across tenants.
  - _Why false positive:_ Knowledge embedding updater is background maintenance across tenants and only sets embedding metadata by row id, not serving or mixing tenant data.
- **server/storage.ts:1013** _(system-sweep)_ — Raw UPDATE on `heartbeat_tasks` (fixStaleBackupSchedules) executes without tenant filtering — mutates tasks across tenants.
  - _Why false positive:_ Global maintenance reschedules stale backup tasks across tenants and only updates housekeeping next_run_at fields.
- **server/storage.ts:1035** _(system-sweep)_ — SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ... (getDueHeartbeatTasks) returns tasks across all tenants — no tenant_id constraint.
  - _Why false positive:_ Scheduler intentionally reads due tasks across all tenants for platform-wide dispatch and preserves per-row tenantId for later handling.
- **server/storage.ts:1063** _(system-sweep)_ — UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... (claimHeartbeatTasks) has no tenant_id constraint — claims tasks across tenants.
  - _Why false positive:_ Global scheduler claim loop intentionally updates due tasks across tenants by task id and only mutates run scheduling fields.
- **server/storage.ts:1077** _(system-sweep)_ — UPDATE heartbeat_tasks SET lastRunAt/nextRunAt WHERE id = ${id} (markHeartbeatTaskRun) has no tenant filter — mutates a tenant-scoped table by id only.
  - _Why false positive:_ Marking heartbeat task run is scheduler housekeeping updating run timestamps, not tenant-facing content access or mixing.
- **server/tool-learning.ts:316** _(upstream-verified)_ — DELETE FROM custom_tools WHERE id = ${tool.id} deletes a tenant-scoped row by id only, without verifying tenant_id. This may delete another tenant's tool if misused.
  - _Why false positive:_ tool.id comes from the just-inserted/validated row in the same call path, not caller input; delete reverts that same row
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT ... FROM custom_tools WHERE id = ${inserted.id} is performed without an explicit tenant_id filter. Reading tenant-scoped rows by id only can cross tenants in contexts where id isn't sufficient authorization.
  - _Why false positive:_ re-reads the row that was just inserted with tenantId in the same flow; inserted.id is server-derived
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE custom_tools SET usageCount = ... WHERE id = ${tool.id} increments usageCount without a tenant_id constraint. This writes to a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ tool was selected a few lines above with name AND tenant_id = tenantId before this id-only update
- **server/tools.ts:2890** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The code inserts (project_id, conversation_id) without explicitly storing the caller's tenant_id column.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped through tenant-owned project/conversation checks
- **server/tools.ts:2994** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT INTO project_files(...) VALUES(...) omits tenant_id even though this is tenant-scoped data.
  - _Why false positive:_ project_files has no tenant_id and insert is guarded by prior project tenant ownership check
- **server/tools.ts:3008** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The code inserts project_notes (project_id, note, author) without an explicit tenant_id column value.
  - _Why false positive:_ project_notes has no tenant_id and insert is scoped transitively via prior tenant-owned project check
- **server/tools.ts:3031** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` (link_conversation path) does not set tenant_id. The handler inserts a linking row without storing tenant_id, relying only on prior checks/updates.
  - _Why false positive:_ project_conversations has no tenant_id; both project ownership and conversation tenant membership are verified before insert
- **server/tools.ts:6942** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The code asserts project ownership via assertProjectInTenant but then performs raw INSERT INTO project_files (...) VALUES (...) without including tenant_id as a column/value. Per invariant, tenant-scoped tables must have the tenant_id explicitly set on INSERT.
  - _Why false positive:_ project_files has no tenant_id and assertProjectInTenant verifies the parent project belongs to this tenant before insert
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — Raw UPDATE against tenant-scoped table `browser_workflows` omits tenant_id constraint: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. This relies on previously-selected `workflow.id` but the SQL lacks an explicit `tenant_id = <caller tenant>` guard.
  - _Why false positive:_ workflow.id comes from an immediately prior SELECT on browser_workflows constrained by tenant_id = tenantId in the same path.
- **server/tools/domains/files/handlers.ts:904** _(fk-transitive)_ — INSERT into `project_files` does not include an explicit tenant identifier in the INSERT columns/values: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`. Per codebase invariant, tenant-scoped inserts must explicitly pass tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped through project_id after selecting a tenant-owned project via conversations/projects join.
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE on `projects` table omits tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`. Without `tenant_id = <caller tenant>` this could update a project belonging to another tenant if IDs are guessed or reused.
  - _Why false positive:_ projId is derived from a prior JOIN that requires p.tenant_id = tenantId in the same call path.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` without tenant_id constraint — admin branch performs SELECT provider, LENGTH(api_key) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' allowing cross-tenant enumeration of API keys.
  - _Why false positive:_ schema note says provider_keys has no tenant_id column; admin-only audit is querying a platform-global table.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` without tenant_id constraint — admin branch runs SELECT COUNT(DISTINCT provider) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' which enumerates provider configuration across tenants.
  - _Why false positive:_ provider_keys is schema-global (no tenant_id column) and this is an admin-only aggregate over that global table.
- **server/video-job-runner.ts:236** _(system-sweep)_ — UPDATE on tenant-scoped table `video_jobs` performed without any tenant_id constraint. recoverStaleVideoJobs() updates all matching jobs across every tenant (marks them failed) because the WHERE clause lacks a tenant filter.
  - _Why false positive:_ recoverStaleVideoJobs is a platform-wide housekeeping sweep marking stale jobs failed; it only mutates status fields and returns aggregate/job ids to logs, not tenant-facing mixed content.
- **server/video-job-runner.ts:208** _(internal-worker-pk)_ — SELECT from tenant-scoped table `video_jobs` (reading cancelRequested) is done by isCancelRequested(jobId) with only jobId in the WHERE clause and no tenant_id constraint. This allows reading cancellation state for jobs owned by other tenants.
  - _Why false positive:_ isCancelRequested is an internal runner helper reading by server-generated globally unique jobId, not directly from an untrusted cross-tenant request boundary.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — Query against tenant-scoped table `tool_performance` (SELECT ... FROM tool_performance WHERE last_failure_at > ...) groups/filters without any tenant_id restriction, exposing cross-tenant data.
  - _Why false positive:_ Platform-wide observability check aggregates tool failure counts and emits warnings only; no tenant-facing row data returned or mixed content surfaced.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Memory-growth probe uses sql.raw to SELECT COUNT(*) and pg_total_relation_size FROM tables listed in MEMORY_TABLES (including `messages`) without any tenant_id constraint, aggregating tenant-scoped data across tenants.
  - _Why false positive:_ Maintenance/observability loop intentionally counts rows and relation sizes across tables, mutating nothing and emitting only aggregate housekeeping findings.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory executes without a tenant_id filter and thus reads graph_memory rows across all tenants.
  - _Why false positive:_ Bootstrapping/housekeeping probe checks only whether graph_memory is globally empty and emits a single aggregate warning, not tenant data.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_activity` uses WHERE id = ${activityId} with NO tenant_id constraint — allows modifying rows belonging to other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/commitment-drafter.ts:232** _(upstream-verified)_ — Unscoped UPDATE against tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — no tenant_id filter. This can modify a commitment from a different tenant (cross-tenant mutation) if `row.id` collides.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/dfy-intake.ts:93** _(signed-url)_ — UPDATE on tenant-scoped table `dfy_intake_forms` with no tenant_id constraint (submitDfyIntake uses .where(eq(dfyIntakeForms.id, form.id)) only). This allows modifying a form row from a different tenant if an id from another tenant is present.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): form row fetched by unguessable 48-hex token (the documented authorization for the public intake form); UPDATE reuses that row's own id.
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE from tenant-scoped table `doc_chunks` without tenant_id constraint (addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can remove chunks belonging to other tenants sharing the same collection_id/doc_path if collection IDs are not globally constrained.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/plan-executor.ts:116** _(upstream-verified)_ — appendExecutionLog updates plans.execution_log using only WHERE id = ${planId} (no tenant_id constraint). This allows modifying another tenant's plan row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Final status/execution_log UPDATE within executePlan; ownership enforced upstream at server/routes/agentic-policy.ts:113.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates a tenant-scoped table without a tenant_id constraint, allowing cross-tenant modification of rule metadata.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/self-improvement.ts:413** _(internal-worker-pk)_ — In the catch block of runExperiment() the error-path UPDATE on `experiments` also uses .where(eq(experiments.id, experiment.id)) with no tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:339 the experiment row is created via db.insert(experiments) with a server-generated serial PK earlier in the same runExperiment() call; the UPDATE at 394 targets that same experiment.id. All callers (runSelfImprovementCycle:272) create the row internally; no request/tool-arg id.
- **server/storage.ts:1346** _(upstream-verified)_ — getAllDataForExport performs SELECTs from many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with no tenant filters — returns data for all tenants in bulk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` without filtering by tenant_id (reads all pairing rows across tenants).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/agentic-features.ts:773** _(upstream-verified)_ — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' has NO tenant_id constraint — reads enrollments across tenants and can allow cross-tenant enrollment checks.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/chat-engine.ts:4446** _(internal-worker-pk)_ — storage.attachCitations(...) is called without passing tenant context. This likely performs INSERT/UPDATE against tenant-scoped citation rows using only a message id, so the DB write may omit or not constrain tenant_id permitting cross-tenant writes/association.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storage.attachCitations (storage.ts:392-401) does UPDATE messages SET citations WHERE id = messageId; messages has tenant_id (schema.ts:147). Both callers pass a server-generated PK of a message the code just inserted: chat-engine.ts:4332 uses (assistantMsgRow).id and routes.ts:5450 uses (__asstMsg).id. No request/tool-arg-derived id.
- **server/delivery-pipeline.ts:1252** _(global-table)_ — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId with no tenant_id constraint (getDeliveryByStripePayment uses where(eq(deliveryLogs.stripePaymentId, paymentId)) only). This can leak another tenant's delivery row when a stripePaymentId is supplied from untrusted sources.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:50** _(signed-url)_ — SELECT from tenant-scoped table `dfy_intake_forms` by unscoped token (getDfyIntakeByToken queries by token only, no tenant_id filter). Although token is intended as an authorization artifact, the DB read is not constrained by tenant_id per the project's isolation invariant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/embeddings.ts:748** _(system-sweep)_ — UPDATE via storeEmbeddingVec executes `UPDATE ${sql.raw(table)} SET embedding_vec = ..., embedding = ... WHERE id = ${id}` with no tenant filter. memory_entries and agent_knowledge are tenant-scoped: updating by id only may modify another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/heartbeat.ts:325** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} updates a tenant-scoped table (heartbeat_tasks) without constraining by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/heartbeat.ts:227-266 startup cleanup loops over allTasks; t.id is a DB-selected PK and only the housekeeping model column is normalized. Not tenant-facing, no untrusted id.
- **server/heartbeat.ts:329** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} (model normalization) lacks a tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/heartbeat.ts:227-266 startup cleanup loops over allTasks; t.id is a DB-selected PK and only the housekeeping model column is normalized. Not tenant-facing, no untrusted id.
- **server/job-queue.ts:273** _(internal-worker-pk)_ — cancelJob updates agent_jobs (SET status='cancelled') using only id in the WHERE clause and no tenant_id check — this can cancel jobs from other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:282** _(upstream-verified)_ — retryJob resets job rows via UPDATE ... WHERE id = ${id} with no tenant_id constraint — allows resetting jobs belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:283 — retryJob is only reachable via POST /api/admin/agent-jobs/:id/retry (server/routes/agent-jobs.ts:62-70), gated by requirePlatformAdmin. Same admin-only operator inbox as cancelJob — cross-tenant retry by id is deliberate platform-admin capability, not attacker-influenced.
- **server/memory-graph.ts:183** _(upstream-verified)_ — refreshCategoryCount() performs UPDATE memory_categories ... WHERE id = ${categoryId} and its inner SELECT counts memory_entries for the category but does not filter by tenant_id. Both the outer UPDATE and inner aggregation can therefore mix or modify cross-tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} reads the projects row without tenant scoping or ownership verification. projects is likely tenant-scoped, so this may disclose another tenant's project metadata.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 has no tenant filters. This enumerates projects/conversations/messages across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} runs without tenant scoping. conversations are likely tenant-scoped, so this can return another tenant's conversations.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping. messages are typically tenant-scoped — this can leak cross-tenant message content.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/sculptor.ts:174** _(internal-worker-pk)_ — UPDATE sculptor_sessions in monitorSession() uses WHERE id = ${sessionId} without AND tenant_id = ${tenantId}. Monitor is a background task and must still enforce tenant isolation on writes.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} has no tenant_id constraint. This is a write to a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:407** _(internal-worker-pk)_ — settleFusionReservation() updates agent_cost_ledger by reservation id only (WHERE id = ${reservationId}) with no tenant_id check. A reservation id could belong to another tenant and be mutated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:426** _(internal-worker-pk)_ — releaseFusionReservation() updates agent_cost_ledger WHERE id = ${reservationId} without tenant_id. This can modify ledger rows across tenants if reservationId is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:170** _(upstream-verified)_ — detectStagnation() performs a SELECT on the tenant-scoped `experiments` table without requiring/enforcing a tenant_id filter when tenantId is omitted. The .where(...) built from conditions can end up querying all tenants if tenantId is not provided.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:511** _(upstream-verified)_ — getExperimentHistory() performs a SELECT on the tenant-scoped `experiments` table and will return rows across all tenants when tenantId is not supplied (the function allows an optional tenantId and falls back to selecting all rows when omitted).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:253** _(upstream-verified)_ — Unscoped SELECT on tenant-scoped table `conversations` (getConversationUnscoped) — query `WHERE id = ${id}` has no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:404** _(internal-worker-pk)_ — UPDATE on tenant-scoped `messages` (attachCitations) uses `WHERE id = ${messageId}` with no tenant_id constraint — can modify rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:568** _(upstream-verified)_ — INSERT into tenant-scoped `memory_entries` (createMemoryEntry) does not validate/ensure an explicit tenantId is present before insert.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/storage.ts:806** _(upstream-verified)_ — INSERT into tenant-scoped `agent_knowledge` (createKnowledge) performs db.insert(values(data)) without validating that data.tenantId is present/owned by caller.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:768 — createKnowledge() inserts a typed InsertKnowledge with tenantId supplied by callers; agent_knowledge.tenantId is NOT NULL so an unscoped insert fails closed at the DB. Callers pass authenticated tenantId (routes.ts:6240 `{...parsed.data, tenantId}`, doc-collections, knowledge/handlers.ts). Additionally the path already enforces a reserved-category guard against the act
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE custom_tools SET implementation = fixedImpl WHERE id = ${tool.id} is executed without constraining the update by tenant_id. custom_tools is tenant-scoped; the update can affect rows outside the caller's tenant context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tools.ts:8910** _(internal-worker-pk)_ — UPDATE on proposed_skills lacks a tenant_id constraint in the WHERE clause. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates by id only and does not restrict to the caller's tenant, enabling cross-tenant modification if IDs collide or are abused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8936** _(internal-worker-pk)_ — UPDATE on proposed_skills (security-scan rejection path) missing tenant_id in WHERE clause. The update uses only the row id to identify the row, with no tenant scoping.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8950** _(internal-worker-pk)_ — UPDATE on proposed_skills (promotion path) missing tenant_id in WHERE clause. The update sets promotedSkillId/status but does not restrict by tenant, allowing potential cross-tenant modifications.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8967** _(internal-worker-pk)_ — UPDATE on proposed_skills (reject path) performed without tenant_id constraint. The update only filters by id and does not confirm the row belongs to the caller's tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Query SELECT count(*) FROM messages WHERE created_at > ... executes without any tenant_id filter, aggregating chat messages across all tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): observability invariant check; aggregate message count only.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — Query SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant_id constraint and therefore aggregates tool usage across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): wiring-invariants.ts:293 SELECT DISTINCT tool_name for platform tool-coverage probe; heartbeat-only call path, no row data returned to tenant
- **server/delivery-pipeline.ts:313** _(deferred)_ — Generic UPDATE against tenant-scoped table `delivery_logs` with no tenant_id constraint (updateDeliveryLog uses .where(eq(deliveryLogs.id, id)) only). This allows an update to affect another tenant's row if an id from another tenant is supplied or if there is any bug/race that obtains a cross-tenant id.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/minerva-planner.ts:548** _(deferred)_ — getPlan conditionally issues an unscoped SELECT when `tenantId` is omitted: `SELECT * FROM plans WHERE id = ${planId}` (no tenant filter) — external callers may omit tenantId and trigger an unfiltered read.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
