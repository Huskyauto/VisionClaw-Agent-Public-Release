# Tenant-Isolation Audit Report

_Generated 2026-08-20T04:08:10.236Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **601/601** file(s) audited · chunks 57/57 OK (0 failed)
- Findings: **269** (CRITICAL 80 · HIGH 155 · MEDIUM 34 · LOW 0)
- Precision second-pass (`gpt-5.4`): **214** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (80)

- **server/agentic-engines.ts:243** — UPDATE ai_insights SET status/action_taken WHERE status='new' AND category IN (...) is executed without any tenant_id constraint — this writes/changes rows across all tenants.
  - _Fix:_ Make this operation tenant-scoped (e.g. accept a tenantId and include WHERE tenant_id = ${tenantId} AND status = 'new' AND category IN (...)), or perform an explicit, auditable multi-tenant operation only from an authorized admin path. Avoid blanket updates that touch tenant data without tenant scoping.
- **server/auth.ts:196** — API key lookup on the api_keys table is performed without any tenant constraint: db.select().from(apiKeys).where(... keyHash ...).
  - _Fix:_ Either explicitly scope the lookup to the expected tenant (WHERE tenant_id = ...), or treat api_keys as a documented global table and move validation into a global-auth code path that clearly records/validates tenant ownership. At minimum, ensure the returned key row is validated for correct tenant use and that searches cannot be abused to act across tenants.
- **server/auth.ts:304** — Session lookup on auth_sessions uses only token equality and no tenant constraint: `SELECT tenant_id, is_admin, expires_at FROM auth_sessions WHERE token = ${tokenHash}`.
  - _Fix:_ Require the caller context to provide the tenant (or otherwise canonicalize session lookup) and include `AND tenant_id = ${expectedTenantId}` where appropriate. If sessions are meant to be global by token, add hard guards/documentation and ensure the token format/lookup cannot be misused to grant cross-tenant access.
- **server/channel-routing.ts:16** — channel_routes table schema has no tenant_id column. With a global channel_routes table there's no way to enforce tenant isolation at the application SQL level for routes (the table uses UNIQUE(channel) globally).
  - _Fix:_ Make channel_routes tenant-scoped: add a tenant_id INTEGER NOT NULL column, remove the global UNIQUE(channel) and replace with UNIQUE(tenant_id, channel). Update all callers to accept a tenantId and always set tenant_id on INSERTs (no defaults). Ensure all SELECT/UPDATE/DELETE statements include WHERE tenant_id = <caller tenant>.
- **server/channel-routing.ts:54** — setChannelRoute INSERT writes persona_id into the (global) channel_routes table using an unvalidated personaId from the caller. There is no check that the persona belongs to the caller's tenant, enabling cross-tenant linkage or privilege escalation.
  - _Fix:_ Require a tenantId parameter and either (A) validate the personaId belongs to that tenant before inserting (SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}), or (B) make channel_routes tenant-scoped and include tenant_id in the INSERT (INSERT ... (tenant_id, channel, persona_id) VALUES (${tenantId}, ${channel}, ${personaId})). Ensure the ON CONFLICT clause uses tenant-scoped uniqueness (ON CONFLICT (tenant_id, channel)).
- **server/data-protection.ts:168** — DELETE FROM conversations WHERE id = ${conv.id} has no tenant_id predicate. Deleting without tenant scoping can remove another tenant's row if IDs collide or if caller context is incorrect.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or perform the operation inside a tenant-scoped transaction (withTenantTx/SET LOCAL) to guarantee tenant isolation.
- **server/deep-interview.ts:337** — INSERT INTO project_notes is executed without setting tenant_id (INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ...). For tenant-scoped tables every INSERT must explicitly include tenant_id or validate ownership before inserting.
  - _Fix:_ Supply tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() FROM ... WHERE p.tenant_id = ${state.tenantId}), or otherwise ensure the inserted row is written with the correct tenant_id and performed inside a tenant-scoped transaction.
- **server/email.ts:470** — SELECT against tenant-scoped table `inbox_messages` without tenant_id constraint: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1`.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add `AND tenant_id = ${tenantId}`) or otherwise resolve the correct tenant before querying. Never query tenant-scoped tables without scoping by tenant_id.
- **server/email.ts:515** — SELECT from tenant-scoped table `inbox_messages` with no tenant_id filter: `SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...)`.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. `WHERE tenant_id = ${tenantId} AND ...`). If this operation is intended to be multi-tenant maintenance, run it per-tenant with an explicit tenantId or move to an admin-only path that is explicit and audited.
- **server/email.ts:528** — UPDATE of tenant-scoped table `inbox_messages` without tenant_id constraint: `UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}`.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) to avoid modifying rows belonging to other tenants.
- **server/embeddings.ts:780** — SELECT from tenant-scoped table `memory_entries` without tenant_id filter: `SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200`.
  - _Fix:_ This reads tenant-scoped rows without scoping by tenant. Make this per-tenant (add `AND tenant_id = ${tenantId}`) or run an explicit admin-only operation that documents and restricts cross-tenant access. Never run tenant-scoped selects globally without explicit admin intent and audit.
- **server/embeddings.ts:796** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id filter: `SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND (expires_at IS NULL OR expires_at > NOW()) LIMIT 200`.
  - _Fix:_ Scope the select by tenant (e.g. `AND tenant_id = ${tenantId}`) or convert to an explicit, audited admin job that iterates tenants. Avoid cross-tenant reads on tenant-scoped tables.
- **server/embeddings.ts:752** — SELECT from tenant-scoped table `agent_knowledge` in backfillMissingKnowledgeEmbeddings without tenant filter: `SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL AND source = 'autoresearch' AND (expires_at IS NULL OR expires_at > NOW()) LIMIT 50`.
  - _Fix:_ Restrict this backfill to a specific tenant (add `AND tenant_id = ${tenantId}`) or implement an explicit per-tenant backfill loop. All reads from tenant-scoped tables must include the caller's tenant_id (or be part of a documented admin-only maintenance path).
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table "code_proposals" (SET status = 'archived_stale') with WHERE id = ANY(...) but without any tenant_id condition — this can modify other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE tenant_id = ${tenantId} AND id = ANY(...)). If operating as an admin that must touch many tenants, perform updates per-tenant with explicit tenantId or use a verified admin-only path that documents and enforces that behavior.
- **server/health-audit.ts:368** — UPDATE on tenant-scoped table "heartbeat_tasks" (SET enabled = false) uses WHERE id = ANY(...) but lacks any tenant_id constraint — this may disable tasks belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE tenant_id = ${tenantId} AND id = ANY(...)) or perform per-tenant updates after verifying ownership. For admin-level maintenance, run explicit per-tenant operations or restrict to ADMIN_TENANT_ID with clear intent.
- **server/lib/scheduled-post-runner.ts:62** — SELECT in CTE pulls due rows from tenant-scoped table `scheduled_posts` with no tenant_id constraint (SELECT id FROM scheduled_posts WHERE status = 'pending' ...). This allows cross-tenant selection/locking and subsequent UPDATE to flip rows across all tenants.
  - _Fix:_ Restrict the CTE to a single tenant (add WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. If the runner must be system-scoped, document and gate that intent (explicit admin-only path) and ensure the CTE and subsequent UPDATE also include tenant_id in the filter to avoid accidental cross-tenant modifications.
- **server/memory-graph.ts:184** — UPDATE memory_categories ... SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND ...) WHERE id = ${categoryId} computes the subquery count without filtering memory_entries by tenant_id. This reads tenant-scoped memory_entries across all tenants and writes the aggregated value into a (presumably) tenant-scoped category row — enabling cross-tenant pollution/leakage and breaking tenant isolation invariants.
  - _Fix:_ Include the tenant constraint in the inner SELECT (and in the outer UPDATE as well). For example, change the subquery to SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status = 'active' AND deleted_at IS NULL, and add AND tenant_id = ${tenantId} to the UPDATE's WHERE clause (or accept tenantId as a param to refreshCategoryCount). This ensures counts are computed per-tenant only.
- **server/memory-intelligence.ts:399** — UPDATE on knowledge_triples omits tenant constraint. The code updates rows by id (`WHERE id = ${c.id}`) after selecting contradictions scoped by tenant_id, but the UPDATE lacks `AND tenant_id = ${tenantId}` allowing a global id match to modify a row belonging to a different tenant.
  - _Fix:_ Constrain the UPDATE by tenant as well. E.g. `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} AND tenant_id = ${tenantId}` (or otherwise ensure the same tenantId binding is applied). Always include tenant_id on updates to tenant-scoped tables.
- **server/minerva-planner.ts:480** — Rollback UPDATE on plans omits tenant scoping. In the revise failure path the code executes `UPDATE plans ... WHERE id = ${args.planId} AND status = 'revising'` without constraining by tenant_id, which can modify a plan row belonging to another tenant.
  - _Fix:_ Include the tenant filter the same way the CAS did. For example: `WHERE id = ${args.planId} AND status = 'revising' ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}` so the rollback only affects rows in the intended tenant. If this path can be called without a tenant (trusted internal), ensure callers are truly internal or fetch/verify the row's tenant before updating.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` without supplying tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id, violating the rule that every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Alternatively, use a parameterized helper that sets tenant_id from the verified caller context or derive tenant_id from the asserted project row and include it in the INSERT.
- **server/mpeg-engine.ts:1436** — INSERT into tenant-scoped table `project_files` without supplying tenant_id. This upsert in the parallel path omits tenant_id in the VALUES list.
  - _Fix:_ As above, include tenant_id in the INSERT (and ON CONFLICT handling) so the row is written with the correct tenant context, e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...). Ensure the tenantId is the same one verified by assertProjectInTenant.
- **server/oauth-subscriptions.ts:956** — UPDATE on tenant-scoped table `oauth_subscriptions` uses only the row id in WHERE clause (UPDATE ... WHERE id = ${sub.id}) and does not include tenant_id. This is a write to a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the tenant context (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} or use WHERE provider = ${provider} AND tenant_id = ${tenantId}). Always include tenant_id in UPDATE/DELETE WHERE clauses for tenant-scoped tables to enforce isolation.
- **server/oauth-subscriptions.ts:981** — UPDATE on tenant-scoped table `oauth_subscriptions` uses only the row id in WHERE clause (UPDATE ... WHERE id = ${sub.id}) with no tenant_id guard, allowing cross-tenant writes.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or use provider+tenant_id filters. Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} has NO tenant_id constraint. exportPersona reads a persona by id without verifying it belongs to the caller's tenant, allowing cross-tenant persona data exposure.
  - _Fix:_ Constrain the SELECT by tenant_id (e.g. SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}) and treat a missing/empty result as 'not found'. Always verify ownership before returning or using the persona row.
- **server/plan-executor.ts:116** — appendExecutionLog updates the tenant-scoped 'plans' table without constraining by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows modifying another tenant's plan by id.
  - _Fix:_ Require and pass the plan's tenant id into appendExecutionLog and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise validate the caller's tenant owns the plan). Validate ownership before any UPDATE to tenant-scoped rows.
- **server/plan-executor.ts:513** — Claiming a plan for execution uses UPDATE plans ... WHERE id = ${planId} AND status = 'approved' without enforcing tenant scope. This can allow a caller to claim and execute a plan belonging to another tenant.
  - _Fix:_ Include tenant validation in the CAS update (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved'). Require the caller's tenant context (or an explicit admin flag) and refuse to proceed if the tenant does not match.
- **server/plan-executor.ts:527** — Fallback path sets plan status to 'failed' with UPDATE plans SET status = 'failed' WHERE id = ${planId} without scoping by tenant_id. This can change another tenant's plan state.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${callerTenantId}) or fetch/validate the plan's tenant before updating. All writes to 'plans' must include the tenant constraint.
- **server/plan-executor.ts:728** — Final plan status update performs UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} with no tenant_id filter. This allows finalizing plans across tenant boundaries.
  - _Fix:_ Constrain the UPDATE with the owning tenant (WHERE id = ${planId} AND tenant_id = ${callerTenantId}). Ensure the executor has been authorized for that tenant before performing the state transition.
- **server/plan-executor.ts:762** — Error recovery path updates plans with UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' but omits tenant scoping. This can mark another tenant's running plan as failed.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${callerTenantId}). Validate plan ownership before changing status in error handling paths.
- **server/plan-executor.ts:778** — resumeStuckPlans performs a global UPDATE on the tenant-scoped 'plans' table (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...) with no tenant restriction, affecting plans for all tenants.
  - _Fix:_ If this operation must run across tenants, require explicit admin privileges and document it; otherwise, scope the update per-tenant (pass tenantId) or iterate per-tenant. Never perform blanket writes on a tenant-scoped table from non-admin callers without explicit authorization and auditing.
- **server/plan-executor.ts:787** — resumeStuckPlans selects pending approved plans with SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 without tenant_id filtering, returning plan ids across tenants.
  - _Fix:_ Restrict the SELECT to the caller's tenant (WHERE tenant_id = ${callerTenantId} AND status = 'approved') or require and verify admin context. Always include tenant_id in read queries against tenant-scoped tables unless the operation is an explicitly-audited cross-tenant admin job.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — update is not constrained by tenant_id, so it could modify a heartbeat_tasks row belonging to another tenant if IDs collide.
  - _Fix:_ Include tenant scoping on UPDATEs that modify tenant-scoped tables, e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}. Prefer using Drizzle's typed .where(eq(table.tenantId, tenantId)).
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} — this update is missing a tenant_id constraint and can modify agent_desks across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or use a Drizzle .where with the tenant id). Ensure every UPDATE/DELETE on agent_desks is tenant-scoped.
- **server/process-governor.ts:972** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — update lacks tenant scoping and may change another tenant's rule row.
  - _Fix:_ Include tenant constraint: WHERE id = ${rule.id} AND tenant_id = ${tenantId}. All mutating queries on governance_rules must include tenant_id.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge content without tenant_id constraint. The runner updates the row by id only: UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}. This can modify another tenant's row if ids collide or are mis-attributed.
  - _Fix:_ Always include tenant_id in the WHERE clause for updates to tenant-scoped tables (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}). Use the per-row tenant_id available from the SELECT to constrain the UPDATE so a row is only changed within its owning tenant.
- **server/reference-learner.ts:290** — UPDATE memory_entries performed without tenant_id constraint: UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}. This can update another tenant's memory entry by id.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId} (or the persona/owner as appropriate). Never rely solely on id for tenant isolation when writing tenant-scoped tables.
- **server/research-engine.ts:165** — cleanupZombieSessions runs a global UPDATE on research_sessions without tenant scoping: UPDATE research_sessions SET status='completed' ... WHERE status = 'running' ... AND started_at < ${staleThreshold}. This will mark sessions for all tenants as completed, not scoped to any tenant.
  - _Fix:_ Scope the cleanup by tenant (e.g. iterate tenant ids and run the UPDATE per-tenant with WHERE tenant_id = ${tenantId}) or otherwise add an explicit tenant predicate to the WHERE clause. If this operation must be global, require an explicit admin guard and ensure it enumerates tenants safely and intentionally.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge without tenant scoping: DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW(). This will delete autoresearch entries for all tenants.
  - _Fix:_ Constrain the DELETE to a tenant (e.g. WHERE tenant_id = ${session.tenantId} AND source = 'autoresearch' AND expires_at < NOW()) or otherwise run the purge per-tenant. Ensure that background cleanup jobs never perform unscoped deletes on tenant-scoped tables.
- **server/routes/archive-rescue.ts:276** — Request-derived file path array is embedded into a SQL literal via string interpolation when inserting demo_image_paths: ${`{...}`}::text[]. This builds a SQL array string from user-supplied values (uploaded file paths) rather than using a parameterized array binding, creating injection risk and tenant-isolation risk if values are attacker-controlled.
  - _Fix:_ Avoid hand-assembling SQL literals from request data. Use parameterized bindings or Drizzle/driver-safe array parameters (e.g. pass savedPaths as a parameter/array column value), or use an INSERT helper (db.insert(...).values(...)) that binds the array. Example: VALUES (${PLATFORM_OWNER_TENANT_ID}, ${body.orgName}, ..., ${body.notes ?? null}, ${ipHash}, ${ua}, ${savedPaths}) with the column typed as text[] in the driver, or use sql`...` with sql.array(savedPaths, 'text'). Ensure all elements are validated/sanitized before DB insert.
- **server/routes/conversations.ts:99** — UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} — the UPDATE lacks a tenant_id constraint. This lets a tenant-side request update a conversation row by id without enforcing the row belongs to the caller's tenant, enabling cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant_id as well: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}. Better: perform the update through a tenant-aware storage helper (or pass conv.tenantId) so all writes to tenant-scoped tables always include tenant_id.
- **server/routes/conversations.ts:103** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) — the INSERT does not set tenant_id. If project_conversations is tenant-scoped, omission of tenant_id creates rows with no tenant ownership or owned by NULL, allowing cross-tenant associations and breaking isolation.
  - _Fix:_ Include tenant_id explicitly in the INSERT columns/values: INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}). Prefer using tenant-aware helpers (or derive tenant from the verified project) and ensure the tenant passed is validated against the project and conversation ownership before inserting.
- **server/routes/conversations.ts:105** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — the UPDATE lacks a tenant_id constraint. Even though the code previously checked project ownership, updating without tenant scoping risks modifying a project row with a matching id in another tenant if race/ID guess occurs.
  - _Fix:_ Add tenant constraint to the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Prefer executing all modifications through tenant-aware queries or helper functions that always include tenant_id in the WHERE clause.
- **server/routes/conversations.ts:231** — INSERT INTO project_notes (project_id, note, author) VALUES (...) — the INSERT omits tenant_id. If project_notes is a tenant-scoped table, this creates notes without explicit tenant ownership, allowing cross-tenant writes via archive/delete flows.
  - _Fix:_ Include tenant_id in the INSERT: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'}). Validate tenant ownership of projectId before inserting and use tenant-aware helper functions to perform the insert.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without constraining by tenant_id (WHERE id = ${session.id} only). This allows cross-tenant modification if an attacker can supply/guess a session id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise assert ownership before the update. Thread the caller's tenantId into this code path and enforce it on every UPDATE.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions SET conversation_id ... WHERE id = ${session.id} lacks tenant_id constraint. Unscoped update may modify another tenant's session row.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the WHERE clause (or pass/verify tenantId) so the update is tenant-scoped. Always include tenant_id when updating tenant-scoped tables.
- **server/sculptor.ts:168** — UPDATE sculptor_sessions (monitorSession) sets status/result/completed_at WHERE id = ${sessionId} with NO tenant_id constraint. This can change state of sessions belonging to other tenants if sessionId is abused or guessed.
  - _Fix:_ Constrain this UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure monitorSession receives/uses the tenantId to enforce ownership on writes.
- **server/sculptor.ts:358** — UPDATE sculptor_sessions SET review_result ... WHERE id = ${sessionId} lacks tenant_id constraint. Although the row was read earlier with tenant scoping, the subsequent write is unscoped and could affect other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Never rely solely on a prior SELECT for scoping; every write must include the tenant filter.
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} but does NOT constrain by tenant_id. A reservationId uniquely identifies a ledger row but without tenant scoping this write could affect another tenant's ledger row.
  - _Fix:_ Require and pass tenantId into settleFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause. Verify the reservation row belongs to the caller's tenant before modifying it.
- **server/second-opinion.ts:424** — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without a tenant_id condition. This can release/alter reservations belonging to other tenants if reservationId is misused.
  - _Fix:_ Add a tenantId parameter to releaseFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause. Always scope ledger updates to the tenant.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' lacks a tenant_id filter. heartbeat_tasks is tenant-scoped, so this reads global state across tenants unexpectedly.
  - _Fix:_ Constrain maintenance queries to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) or make the global intent explicit and document/limit it. Prefer explicit ADMIN_TENANT_ID scoping for admin-only reconciliations.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true runs without tenant_id constraint and will disable delegation tasks across ALL tenants.
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) to this UPDATE. If the intent is global, make that explicit and ensure the code runs under a properly-authorized admin context; otherwise scope to a tenant.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant_id filter and reads across tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise restrict the query to the intended tenant before acting on the result.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true executes without tenant_id constraint, disabling matching tasks for all tenants.
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant) in the WHERE clause. Avoid unscoped updates on tenant-scoped tables.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ...) is missing tenant scoping and will count across tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) to the SELECT so decisions are made per-tenant, or intentionally document and gate any global changes with explicit admin checks.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true is executed without tenant_id constraint, affecting rows across tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${ADMIN_TENANT_ID} or the intended tenant. If global changes are intended, require an explicit admin authorization and clearly document the global scope.
- **server/seed.ts:2163** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without constraining by tenant_id. An UPDATE without tenant scoping can modify rows belonging to other tenants.
  - _Fix:_ Include tenant verification in the UPDATE WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or retrieve the row by id+tenant_id earlier and use that safe reference. Ensure all UPDATE/DELETE statements against tenant-scoped tables assert tenant ownership.
- **server/seed.ts:2516** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... is run with no tenant_id constraint. conversations is tenant-scoped, so this selects rows across all tenants and can return cross-tenant data.
  - _Fix:_ Restrict the SELECT to the appropriate tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or otherwise verify admin-only intent. Never perform unscoped reads of tenant-scoped tables in request-handling code.
- **server/seed.ts:2523** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts linkage rows without any tenant_id. The project_conversations table (as created later) has no tenant_id column, so links can be created across tenants, enabling cross-tenant associations.
  - _Fix:_ Make project_conversations tenant-aware: add tenant_id to the table schema and include tenant_id in INSERTs (or ensure you validate that both project_id and conversation_id belong to the same tenant before inserting). Always store tenant_id on tenant-scoped or tenant-related join tables and include it in INSERTs and WHERE clauses.
- **server/seed.ts:2445** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL performs an UPDATE on projects (a tenant-scoped table) without a tenant_id constraint. This can update a project belonging to a different tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId} AND primary_conversation_id IS NULL). Ensure all UPDATE/DELETE operations on tenant-scoped tables include tenant_id verification or otherwise assert tenant ownership before modifying rows.
- **server/seed.ts:3882** — DELETE FROM heartbeat_tasks executed without any tenant_id constraint. This can delete heartbeat tasks across all tenants.
  - _Fix:_ Constrain the DELETE to the caller's tenant (e.g. DELETE ... WHERE tenant_id = <tenant> AND ...). Validate ownership before destructive operations. Prefer using Drizzle where(eq(heartbeatTasks.tenantId, tenantId)).
- **server/seed.ts:3911** — UPDATE memory_entries performed without a tenant_id constraint (UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND ...). memory_entries is tenant-scoped and this will modify rows for all tenants.
  - _Fix:_ Always include tenant_id in the WHERE clause (e.g. WHERE tenant_id = <tenant> AND deleted_at IS NULL AND ...). If updating by id, also include tenant_id to prevent cross-tenant modification. Use Drizzle eq(memoryEntries.tenantId, tenantId) where possible.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` may run without a tenant_id constraint. detectStagnation builds `conditions` with category always but only adds tenant filter when tenantId is provided; the .where() call will execute a cross-tenant query when tenantId is omitted.
  - _Fix:_ Never execute queries against tenant-scoped tables without an explicit tenant filter. Require a tenantId parameter (or throw) and always include eq(experiments.tenantId, tenantId) in the WHERE clause. Replace the conditional .where(...) path with an enforced tenant check or fail-fast when tenantId is not supplied.
- **server/self-improvement.ts:521** — getExperimentHistory can return experiments across all tenants. When no tenantId is supplied the function falls back to db.select().from(experiments)... (no tenant filter), exposing multi-tenant data.
  - _Fix:_ Require tenantId (or, if allowed optional, document and enforce an explicit admin-only path). Always include eq(experiments.tenantId, tenantId) in the query when reading tenant-scoped tables; do not return unfiltered rows.
- **server/skillify.ts:91** — SELECT from tenant-scoped table 'messages' without any tenant constraint (WHERE tenant_id = ...). This read can leak messages across tenants.
  - _Fix:_ Constrain message reads to the caller's tenant. Add tenant filtering (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or include AND tenant_id = ${tenantId} in the SQL) or perform the lookup via a join to conversations and assert conversation.tenant_id = tenantId.
- **server/skillify.ts:201** — SELECT from tenant-scoped table 'messages' (conversation message history) lacks a tenant_id constraint; query only filters by conversationId.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or otherwise validate the conversation belongs to the caller's tenant before returning messages.)
- **server/skillify.ts:329** — SELECT recent messages for skill extraction without constraining by tenant_id. The query filters only by conversationId and can return cross-tenant data.
  - _Fix:_ Include tenant scoping on the messages query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or verify conversation → tenant ownership and then query messages within that tenant.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks is executed without any tenant constraint, deleting disabled delegation tasks across all tenants.
  - _Fix:_ Avoid unscoped deletes. Either restrict deletion to a specific tenant (WHERE tenant_id = ${tenantId}) or perform deletes via a storage/admin API that explicitly accepts and documents an all-tenants operation. Ensure admin/global actions are explicit, audited and reviewed.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs (pruneOldLogs) is executed without tenant scoping — this will prune heartbeat logs for all tenants rather than just the intended tenant(s).
  - _Fix:_ Add tenant_id filtering to the DELETE (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...) or route pruning through a storage/admin API that accepts an explicit ALL_TENANTS parameter and performs auditing. Ensure global operations are explicit and safe.
- **server/storage.ts:405** — attachCitations() performs an UPDATE on messages (UPDATE messages SET citations = ...) filtering only by id and WITHOUT tenant_id in the WHERE clause. This allows a caller to update another tenant's message row by id, enabling cross-tenant data corruption.
  - _Fix:_ Require and validate the tenantId for the message before updating (e.g., fetch the message's tenant_id and assert it matches the caller's tenant, then UPDATE ... WHERE id = ${messageId} AND tenant_id = ${tenantId}). Do not perform unscoped UPDATEs on tenant-scoped tables.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table 'felix_proposals' performed without constraining tenant_id (stampArgsEmbedding: UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL).
  - _Fix:_ Require and pass an explicit tenantId into stampArgsEmbedding and include AND tenant_id = ${tenantId} in the WHERE clause (or use drizzle .where(eq(felix_proposals.tenantId, tenantId))). Parameterize tenantId rather than relying on caller context or defaults.
- **server/surprise-scorer.ts:139** — UPDATE on tenant-scoped table 'felix_proposals' sets actual_outcome_embedding but omits tenant_id in WHERE (UPDATE felix_proposals SET actual_outcome_embedding = ... WHERE id = ${proposalId}).
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure the function has/receives the correct tenantId (do not rely on implicit/global defaults).
- **server/surprise-scorer.ts:148** — SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} (re-fetch) lacks a tenant_id constraint — reading a tenant-scoped row by id only risks cross-tenant access.
  - _Fix:_ Add tenant filtering to the SELECT (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or otherwise scoping queries to the caller's tenant. Propagate tenantId through helper functions as needed.
- **server/surprise-scorer.ts:181** — UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} omits tenant_id — writing tenant-scoped state without tenant constraint.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure tenantId is available at this call site (pass it into scoreProposalSurprise or related helpers).
- **server/surprise-scorer.ts:200** — SELECT AVG(...) FROM felix_proposals WHERE id = ANY(${neighborIds}::int[]) computes across IDs without also restricting tenant_id. Neighbor IDs were derived earlier with a tenant filter, but this query fails the invariant (reads from tenant-scoped table without tenant constraint).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause of the AVG query (or otherwise use a parameterized IN join scoped to tenantId). Do not assume id-list provenance is sufficient; enforce tenant scoping on every query.
- **server/surprise-scorer.ts:212** — UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} lacks a tenant_id filter — mutates tenant-scoped rows without tenant scoping.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or use drizzle .where(eq(felix_proposals.tenantId, tenantId))). Ensure scoreProposalSurprise accepts/propagates a tenantId and uses it for every read/write on felix_proposals.
- **server/tool-learning.ts:316** — DELETE FROM custom_tools performed with .where(eq(custom_tools.id, tool.id)) in revertToolRow lacking tenant_id constraint — this removes tenant-scoped data by id only, risking cross-tenant deletion.
  - _Fix:_ Require and use tenantId in revertToolRow and constrain deletes with both id and tenant_id (e.g. .where(and(eq(custom_tools.id, tool.id), eq(custom_tools.tenantId, tenantId))). Always verify tenant ownership before destructive operations.
- **server/tools.ts:4552** — INSERT into tenant-scoped tables (project_notes / project_files) omits tenant_id. The code inserts project_notes and project_files using a validated projectId but does not include a tenant_id column in the INSERT list/values. Per the invariant every INSERT into tenant-scoped tables must explicitly pass tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant_id column/value in the INSERTs (and keep the existing assertProjectInTenant validation). For example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Do the same for project_files. This both records the tenant and satisfies the schema invariant (avoid relying solely on project ownership checks).
- **server/tools.ts:6405** — INSERT into project_files omits tenant_id. After assertProjectInTenant succeeds, the code inserts a project_files row but does not include tenant_id in the INSERT. The invariant requires every INSERT into tenant-scoped tables to explicitly set tenant_id.
  - _Fix:_ Add tenant_id to the INSERT column list and pass params._tenantId (or the verified tenant id) as the first value. E.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Retain the prior ownership assertion as defense-in-depth.
- **server/tools.ts:6942** — INSERT into project_files omits tenant_id. In create_slideshow_video the handler inserts into project_files after assertProjectInTenant but the INSERT does not set tenant_id explicitly, violating the rule that every INSERT into tenant-scoped tables must include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (and continue to assert project ownership). For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Ensure all code-paths that write tenant-scoped tables do likewise.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` executed without a tenant_id filter (no WHERE tenant_id = ${tenantId}). This returns provider rows across all tenants and can leak API key metadata to the caller.
  - _Fix:_ Constrain the query to the caller's tenant: add AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(provider_keys.tenantId, tenantId))). If the intent is an admin/global audit, make the admin-only intent explicit (assert admin identity) and log/authorize the operation; do not rely on implicit admin-branch behavior without explicit authorization checks.

### HIGH (155)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (reads running ab_test rows across all tenants).
  - _Fix:_ Scope this read to the caller's tenant. Add an AND tenant_id = ${tenantId} (or equivalent Drizzle where(eq(experiments.tenantId, tenantId))) to the WHERE clause, or iterate per-tenant explicitly if this is intended as a multi-tenant sweep with proper authorization/audit.
- **server/agent-channels.ts:198** — JOIN to channel_subscriptions omits tenant_id constraint (cs.tenant_id not checked). The query can read subscription rows across tenants when joined to tenant-scoped channel_messages.
  - _Fix:_ Include the tenant filter on the subscription side (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}), and/or add a WHERE cs.tenant_id = ${tenantId} to ensure tenant-scoped access.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting without tenant_id constraint — reads pending/high insights across all tenants.
  - _Fix:_ Restrict the sweep to a specific tenant (pass tenantId in and add AND tenant_id = ${tenantId}) or perform per-tenant iteration with explicit authorization/audit. Never read tenant-scoped rows across tenants without an explicit admin intent and access control.
- **server/agentic-engines.ts:347** — SELECT from `heartbeat_logs` (tool call counts) in gatherOperationalData omits tenant scoping (no tenant_id WHERE clause), leaking/logging global heartbeat data across tenants.
  - _Fix:_ Add tenant scoping to the heartbeat_logs query (WHERE tenant_id = ${tenantId} ...) so the operational data is limited to the caller's tenant.
- **server/agentic-engines.ts:451** — SELECT recent heartbeat_logs (recentLogs) in runPredictiveEngine lacks a tenant filter (reads recent successful logs across all tenants).
  - _Fix:_ Include a WHERE tenant_id = ${tenantId} clause on the heartbeat_logs query (and any similar heartbeat/log queries) to restrict results to the current tenant.
- **server/agentic-engines.ts:519** — SELECT aggregating heartbeat_logs in runOptimizationEngine has no tenant_id constraint, returning aggregated task performance across all tenants.
  - _Fix:_ Scope the performance query by tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise ensure this aggregate is intentionally global and protected by proper admin authorization/audit.
- **server/agentic-engines.ts:533** — SELECT from `heartbeat_tasks` (taskConfig) in runOptimizationEngine omits tenant_id filter, returning enabled tasks potentially across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to the heartbeat_tasks query (or pass tenantId in) so task configuration is read only for the current tenant, unless a privileged global read is explicitly intended and access-controlled.
- **server/agentic-features.ts:773** — SELECT id FROM outreach_enrollments to check for existing enrollment does not include tenant_id, allowing cross-tenant visibility/false-positive enrollment checks.
  - _Fix:_ Include tenant scoping in the existence check: SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND tenant_id = ${params.tenantId} AND status = 'active'. Ensure all lookups on tenant-scoped tables include tenant_id.
- **server/agentic-features.ts:932** — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} is missing a tenant_id constraint — a write to a tenant-scoped table without verifying the tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to ensure the update only affects rows owned by the caller's tenant.
- **server/agentic/approvals.ts:135** — Bulk expiry sweep expireStaleApprovals performs UPDATE agentApprovals SET status='expired' ... WHERE status='pending' AND expiresAt < now() without any tenant_id constraint — modifies approvals across all tenants.
  - _Fix:_ Either scope the sweep per-tenant (accept a tenantId and include AND tenant_id = ${tenantId}), or ensure that this cron/admin endpoint is intentionally global and guarded by strict authorization and auditing. Avoid unscoped writes to tenant-scoped tables.
- **server/agentic/department-budgets.ts:143** — runBudgetEnforcement performs a SELECT on the tenant-scoped table `department_budgets` without constraining by tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads across all tenants and violates the invariant that every read against tenant-scoped tables must include a tenant_id filter.
  - _Fix:_ Either restrict the query to a single caller tenant (add WHERE tenant_id = ${tenantId} and call per-tenant), or explicitly make this an admin-only operation with a documented guard (e.g. require ADMIN_TENANT_ID or an explicit admin flag) and audit/authorize it. If the intent is a global sweep, move logic to a clearly privileged admin path and require an explicit admin context rather than leaving an unguarded cross-tenant SELECT.
- **server/agentic/resume-worker.ts:65** — SELECT query scans agent_runs (and joins agent_approvals) without constraining by tenant_id — a cross-tenant read of tenant-scoped tables (agent_runs, agent_approvals).
  - _Fix:_ Restrict the candidate query to a specific tenant_id (or iterate per-tenant) so every read is tenant-scoped. For example, add a WHERE clause like `WHERE r.tenant_id = ${tenantId} AND ...` (or accept a tenantId parameter and use it), or otherwise ensure the query includes `AND r.tenant_id = <caller tenant>` / `AND a.tenant_id = <caller tenant>` before returning rows.
- **server/agentic/wake-scheduler.ts:146** — Subquery selecting due wakes lacks a tenant_id filter: `SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...`
  - _Fix:_ Limit the SELECT to a single tenant (e.g. add `AND tenant_id = ${tenantId}`) or restructure to claim wakes per-tenant in a loop. Ensure every row-lock / UPDATE/RETURNING that touches tenant-scoped rows is constrained by tenant_id or executed inside a tenant-scoped transaction context.
- **server/auth.ts:336** — DELETE from auth_sessions by token lacks tenant constraint: `DELETE FROM auth_sessions WHERE token = ${hashSessionToken(token)}`.
  - _Fix:_ Delete sessions using both token and tenant_id (e.g. `WHERE token = ${tokenHash} AND tenant_id = ${tenantId}`) or ensure the calling context has validated the tenant ownership of the token before issuing a delete that could otherwise affect other tenants' sessions.
- **server/auth.ts:1255** — Global purge: `DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%'` removes rows across all tenants with no tenant constraint.
  - _Fix:_ Restrict this migration/purge to an admin-only maintenance job and/or run per-tenant; if it must be global, protect the call behind a platform-admin gate and log/notify. Avoid performing mass, cross-tenant mutating SQL from tenant-scoped request handlers.
- **server/auth.ts:1233** — clearAllSessions issues `DELETE FROM auth_sessions` with no WHERE clause (global erase across tenants).
  - _Fix:_ Ensure this is a privileged admin action only, and add a platform-admin guard. If intended for a single tenant, change to `DELETE FROM auth_sessions WHERE tenant_id = ${tenantId}` to avoid wiping other tenants' sessions.
- **server/auth.ts:1103** — Password-reset token lookup selects from password_reset_tokens by token only (`WHERE token = ${tokenHash}`) with no tenant constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. require and enforce passing tenant context, then `WHERE token = ${tokenHash} AND tenant_id = ${tenantId}`), or otherwise validate the returned row's tenant before acting on it. Avoid relying solely on token uniqueness to guarantee tenant-isolation.
- **server/auto-project.ts:112** — SELECT from tenant-scoped table `messages` is missing a tenant_id constraint (reads messages by conversation_id without scoping to the caller tenant).
  - _Fix:_ Constrain the query to the caller's tenant. Example: add AND m.tenant_id = ${tenantId} to the WHERE clause (or join messages to conversations and require m.tenant_id = ${tenantId}). Ensure every read against `messages` includes the tenant_id filter.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table `messages` is missing a tenant_id constraint (reads all messages for a conversation without scoping to tenant).
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}, or join messages to conversations and require m.tenant_id = ${tenantId}. Never read messages without an explicit tenant_id filter.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table `conversations` omits tenant_id constraint (updates conversation.project_id using id only). This allows cross-tenant modification if ids are untrusted.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}. Always include tenant_id on writes to tenant-scoped tables.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table this write will not record the tenant and thus violates the tenant-isolation invariant (inserts to tenant-scoped tables must set tenant_id explicitly).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or otherwise ensure invoice_items is constrained to the caller's tenant (validate invoice ownership and add WHERE/tenant_id as appropriate). Run DB schema review to confirm invoice_items is/is-not tenant-scoped and add the tenant_id column or enforcement if needed.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` is executed without constraining by tenant_id. The code updates a review row by id only (`WHERE id = ${pending.reviewId} AND reused = false`) which can allow cross-tenant writes if review ids are not globally unique for the caller's tenant or if the in-memory mapping is poisoned.
  - _Fix:_ Record and carry tenantId when creating the pendingReuse entry (pendingReuseByConversation). When marking reuse, include an explicit tenant filter in the UPDATE (e.g. `WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false`) or first re-read the row and verify its tenant_id matches the expected tenant before performing the UPDATE. Alternatively, change the API to accept an explicit tenantId and validate ownership prior to any mutation.
- **server/channel-routing.ts:31** — listChannelRoutes SELECT joins the personas table (p) without any tenant_id constraint. If personas are tenant-scoped this leaks persona names across tenants.
  - _Fix:_ Require caller to pass tenantId into listChannelRoutes and filter both channel_routes and personas by that tenant_id (e.g. WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}). Alternatively, make channel_routes tenant-scoped (see schema change) and filter only channel_routes. Validate persona ownership before returning persona data.
- **server/channel-routing.ts:51** — DELETE FROM channel_routes WHERE channel = ${channel} executes against a global channel_routes table with no tenant filter — a tenant could delete another tenant's route mapping.
  - _Fix:_ Require tenantId and include WHERE tenant_id = ${tenantId} AND channel = ${channel}. If channel_routes is made tenant-scoped, ensure deletions always constrain by tenant_id.
- **server/channel-routing.ts:63** — getPersonaForChannel SELECT reads persona_id from channel_routes by channel with no tenant constraint. This can return a persona belonging to a different tenant (no ownership verification).
  - _Fix:_ Require tenantId and include it in the WHERE clause (SELECT persona_id FROM channel_routes WHERE tenant_id = ${tenantId} AND channel = ${channel} AND enabled = true). If the returned persona_id will be used to fetch persona details, additionally validate the persona's tenant_id before use.
- **server/channel-routing.ts:71** — removeChannelRoute DELETE uses only channel to delete from channel_routes (no tenant filter), affecting global state and potentially other tenants' mappings.
  - _Fix:_ Require tenantId and constrain DELETE to tenant: DELETE FROM channel_routes WHERE tenant_id = ${tenantId} AND channel = ${channel}. If channel_routes should be tenant-scoped, add tenant_id to the table and enforce it in all write/delete operations.
- **server/chat-engine.ts:4447** — storage.attachCitations(...) is invoked without any tenant identifier. If the citations table is tenant-scoped this can cause inserts/associations to occur without a tenant_id constraint (or could write cross-tenant data).
  - _Fix:_ Pass the caller's tenant (e.g. tenantIdForScope or tenantId) into attachCitations, or have attachCitations require and enforce tenantId. Alternatively, validate the assistantMsgRow belongs to the caller's tenant before attaching citations (SELECT with WHERE tenant_id = ?) and ensure the storage layer always applies the tenant constraint.
- **server/chat-engine.ts:3696** — logDecision(conversationId, ...) is called without a tenant parameter or an explicit ownership check. This writes a decision tied to a conversation id but does not verify/associate the tenant, risking cross-tenant writes or missing tenant scoping.
  - _Fix:_ Include tenantId when calling logDecision, or first verify the conversation belongs to the caller's tenant (e.g. read conversation with WHERE tenant_id = tenantId) before writing. Ensure logDecision enforces tenant scoping server-side if it performs DB writes.
- **server/chat-engine.ts:4119** — recordToolExecution(conversationId, toolName, ...) is called without any tenant identifier or prior ownership verification. This likely writes tool-execution records linked to a conversation but does not constrain them to the caller's tenant.
  - _Fix:_ Pass tenantId (or tenantIdForScope) explicitly to recordToolExecution, or verify conversation ownership before calling. If recordToolExecution writes into tenant-scoped tables, update its signature/implementation to require a tenant_id and use it in WHERE/INSERT values.
- **server/chat-engine.ts:4131** — recordToolCallForStuckDetection(conversationId, ...) is invoked with only a conversation id and args and no tenant context/ownership check. If this function persists or reads tenant-scoped data it lacks a tenant constraint.
  - _Fix:_ Supply tenantId to recordToolCallForStuckDetection or perform an ownership check on conversationId before calling. Prefer making the function signature require tenantId and enforce tenant scoping in the storage layer.
- **server/chat-engine.ts:4137** — postDiagnosticReport([stuckPattern]) is called without tenant context. If postDiagnosticReport persists diagnostics in tenant-scoped tables, it is being called without a tenant constraint or ownership verification.
  - _Fix:_ Modify calls to postDiagnosticReport to include tenantId, or ensure the diagnostic APIs accept and enforce a tenant identifier. If the report is global, document that clearly; otherwise enforce per-tenant writes with WHERE tenant_id = ? or equivalent in the storage layer.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This unclaims/updates a commitment by id alone after detecting an invalid tenant on the returned row, allowing a cross-tenant mutation if the id belongs to another tenant.
  - _Fix:_ Fail-closed: include the tenant guard in the UPDATE. e.g. UPDATE commitments SET draft_status='open', drafted_at=NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}; or, better, verify ownership first and only operate inside the same transaction that fetched/returned the row. Log and skip instead of issuing an unscoped UPDATE.
- **server/data-protection.ts:149** — SELECT from tenant-scoped table `conversations` with no tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads across tenants without scoping.
  - _Fix:_ Restrict the SELECT by tenant (e.g. add AND tenant_id = ${tenantId} and iterate per-tenant) or run inside a tenant-scoped transaction (withTenantTx) / otherwise document this as an explicit, audited multi-tenant sweep. Avoid unscoped reads of tenant data.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` uses only project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) and lacks an explicit tenant_id predicate. This relies on the caller's prior query rather than enforcing tenant scoping on the child table.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId}) when selecting project_notes by project_id, or join to projects constrained by tenant_id to ensure the returned rows belong to the caller's tenant.
- **server/data-protection.ts:372** — SELECT from tenant-scoped table `project_files` uses only project_id (SELECT file_name,... FROM project_files WHERE project_id = ${proj.id}) and omits tenant_id scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise verify tenant ownership) before returning project_files rows to ensure tenant isolation.
- **server/data-protection.ts:404** — COUNT query against tenant-scoped table `messages` (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) omits tenant_id in the WHERE clause.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or verify conversation ownership) so the existence/count check is scoped to the caller's tenant.
- **server/delivery-pipeline.ts:313** — updateDeliveryLog performs an UPDATE on delivery_logs using only the id in the WHERE clause (where(eq(deliveryLogs.id, id))) with no tenant_id constraint.
  - _Fix:_ Require and accept the caller's tenantId (or derive it reliably) and include it in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))). Audit all callers to pass the tenant and fail if not provided.
- **server/delivery-pipeline.ts:1252** — getDeliveryByStripePayment selects a delivery_log by stripePaymentId without any tenant constraint, exposing a referenced delivery row across tenants.
  - _Fix:_ Add a tenantId parameter and require eq(delivery_logs.tenant_id, tenantId) in the WHERE clause (or otherwise validate ownership of the returned delivery before returning sensitive fields).
- **server/delivery-pipeline.ts:1187** — retryDelivery looks up delivery_logs with a WHERE that may be unscoped when tenantId is omitted (where = eq(deliveryLogs.id, deliveryId)), allowing a caller that omits tenantId to access/retrigger another tenant's delivery.
  - _Fix:_ Make the tenantId argument required for non-admin callers or always scope by tenant (and/or fail when tenantId is omitted). Ensure subsequent update/re-trigger calls use the authenticated caller's tenant id in queries/updates.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint, allowing deletion of chunks across tenants.
  - _Fix:_ Include tenant scoping on the DELETE (e.g. AND tenant_id = ${tenantId}) or resolve the collection ownership first and enforce it in the statement. Always include tenant_id in DELETE/UPDATE/SELECT against tenant-scoped tables.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks SET embedding WHERE id = ${row.id} without constraining by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Prefer using the tenant from the initial SELECT that produced row.id and assert ownership before updating.
- **server/dream-consolidation.ts:83** — getRecentSessionSummaries executes SELECT role, content FROM messages WHERE conversation_id = ${conv.id} without an explicit tenant_id constraint (the conversation id was fetched earlier under tenant scoping, but the query itself lacks tenant filtering).
  - _Fix:_ Include tenant_id = ${tenantId} in the messages query (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise verify ownership explicitly before fetching messages. Enforce the habit of always including tenant_id in WHERE clauses for tenant-scoped tables.
- **server/dfy-intake.ts:93** — submitDfyIntake performs an UPDATE on dfy_intake_forms using only the form id in the WHERE clause (.where(eq(dfyIntakeForms.id, form.id))) and omits tenant_id scoping.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId)))). If getDfyIntakeByToken is intentionally cross-tenant by token, still assert and include the tenant_id on all subsequent writes to avoid accidental cross-tenant updates.
- **server/embeddings.ts:244** — Use of sql.raw() with a vector literal built from request-derived embedding (`${sql.raw(`'${vec}'::vector`)}`) inside the query. The vec value comes from generateEmbedding(query) (request-derived).
  - _Fix:_ Avoid injecting interpolated SQL string fragments. Prefer parameterized bindings or a safe driver API for pgvector (bind the vector as a parameter), or otherwise ensure the value is passed as a query parameter instead of via sql.raw(). If raw is unavoidable, keep strict validation and document why it's safe.
- **server/embeddings.ts:564** — Use of sql.raw() with a vector literal derived from the caller's query embedding (`${sql.raw(`'${vec}'::vector`)}`) in vectorSearchKnowledge.
  - _Fix:_ Use parameterized queries or a driver-supported binding for vector literals instead of injecting the literal via sql.raw(). Validate the embedding shape and then bind it as a parameter to the query.
- **server/embeddings.ts:748** — Dynamic sql.raw usages: `UPDATE ${sql.raw(table)} SET embedding_vec = ${sql.raw(`'${vec}'::vector`)}, ...` — combines a runtime table name and an interpolated vector literal. Table and vec ultimately originate from application inputs/calls.
  - _Fix:_ Avoid passing table names via raw string interpolation; map allowed table names to static table objects and use Drizzle's typed update methods. For the vector, bind the value as a parameter (or use a safe pgvector binding) instead of embedding it into sql.raw(). Keep a strict server-side allowlist for table names if dynamic selection is required.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' performed without any tenant_id constraint (reads running/pending agent_runs across all tenants). This loads cross-tenant rows into an in-memory global TaskState store, risking cross-tenant data leakage and breaking the app's tenant-isolation invariant.
  - _Fix:_ Restrict the query to a single tenant or iterate per-tenant. Add a tenantId parameter (or fetch a vetted list of tenantIds) and include WHERE tenant_id = ${tenantId} in the SQL. Alternatively, only select rows for allowed/system tenants and avoid loading other tenants' rows into the global taskStateStore. Also validate/associate each row's tenant before creating in-memory TaskState entries (do not populate global state with rows lacking an explicit, authorized tenant context).
- **server/google-drive.ts:547** — SELECT from tenant-scoped table `projects` is performed using only projects.id (WHERE projects.id = projectId) without scoping by tenant_id. This reads a tenant-scoped row before verifying the caller's tenant ownership (ownership check occurs after the SELECT).
  - _Fix:_ Scope the SELECT by tenant as well (e.g. add AND projects.tenant_id = ${tenantId}) or otherwise verify ownership before fetching tenant-scoped fields. In Drizzle: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or perform a minimal existence check that includes tenant_id before reading other columns.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table `projects` uses only projects.id in WHERE (WHERE projects.id = projectId) and lacks an explicit tenant_id constraint. Per invariant, writes to tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Add the tenant filter to the UPDATE WHERE clause (e.g. WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId}) so the write is guaranteed to affect only rows owned by the calling tenant. In Drizzle: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))).
- **server/google-drive.ts:826** — SELECT from tenant-scoped table `projects` in uploadAndShare reads the project by id (WHERE projects.id = params.projectId) without first scoping the query to the caller's tenant. The code checks tenant ownership only after reading the row, which violates the requirement that tenant-scoped reads be constrained by tenant in the SQL.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. add AND projects.tenant_id = ${params.tenantId}) or otherwise perform an ownership-checked query before reading tenant-scoped fields. Use Drizzle .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))) so no tenant data is read unless the DB enforces the tenant constraint.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table "code_proposals" without scoping by tenant_id — query returns rows across all tenants.
  - _Fix:_ Constrain the SELECT to a specific tenant (e.g. add WHERE tenant_id = ${tenantId} or equivalent Drizzle .where(eq(table.tenantId, tenantId))). If this is intended to be an admin/global scan, iterate per-tenant explicitly or require an explicit ADMIN tenant filter and document the admin-only intent; never silently read all tenant rows.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table "heartbeat_tasks" without scoping by tenant_id — query will return tasks across all tenants.
  - _Fix:_ Add an explicit tenant_id filter (WHERE tenant_id = ${tenantId}) or iterate per-tenant as appropriate. If some heartbeat_tasks are intentionally global/admin-owned, ensure the query explicitly filters for the admin tenant (tenant_id = ADMIN_TENANT_ID) and documents that intent.
- **server/heartbeat.ts:325** — UPDATE against tenant-scoped table `heartbeat_tasks` uses WHERE id = ${t.id} with no tenant_id constraint. This can modify a task belonging to another tenant if id is mis-bound or in race conditions.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage layer helper (e.g. storage.updateHeartbeatTask) which asserts tenant context before writing.
- **server/heartbeat.ts:329** — UPDATE against tenant-scoped table `heartbeat_tasks` uses WHERE id = ${t.id} with no tenant_id constraint (model normalization path).
  - _Fix:_ Add an explicit tenant filter (AND tenant_id = ${t.tenantId}) or call into storage.updateHeartbeatTask so tenant ownership is asserted and enforced.
- **server/heartbeat.ts:154** — Multiple UPDATEs to `research_schedules` use WHERE id = ${sched.id} (e.g. lines 154, 160, 165, 173) but do not include tenant_id in the WHERE clause. `research_schedules` rows are referenced by sched.tenant_id earlier, but the UPDATEs are not tenant-scoped.
  - _Fix:_ Constrain writes to the expected tenant (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage helper that verifies tenant ownership before updating rows.
- **server/heartbeat.ts:2817** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (approval set path) omits tenant filtering. The newly created task includes tenantId but the subsequent UPDATE does not assert tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${newTask.id} AND tenant_id = ${newTask.tenantId}) or perform the update via the storage API which enforces/asserts tenant context.
- **server/heartbeat.ts:3269** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) executes without a tenant_id constraint, potentially affecting another tenant's row.
  - _Fix:_ Use WHERE id = ${newTask.id} AND tenant_id = ${tenantId} (or ${newTask.tenantId}) or call storage.updateHeartbeatTask so the tenant is asserted and enforced before updating approval_status.
- **server/job-queue.ts:98** — UPDATE on tenant-scoped table `agent_jobs` (claimDueJobs) performed without any tenant_id constraint. This atomically transitions jobs to 'running' across all tenants, violating tenant-isolation-by-application-filters.
  - _Fix:_ Restrict the claim to a specific tenant (add a tenantId parameter and include `WHERE tenant_id = ${tenantId}` in the inner SELECT/UPDATE), or otherwise ensure the caller is an explicit cross-tenant admin process and document that exception. Prefer per-tenant claims or an explicit admin-guard to avoid accidental cross-tenant mutation.
- **server/job-queue.ts:134** — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 1) updates rows across all tenants with no tenant_id filter (marks terminal failures).
  - _Fix:_ Add a tenant-scoped variant or accept an explicit tenantId parameter and include `AND tenant_id = ${tenantId}` in the WHERE clause, or require and check admin privileges before performing cross-tenant updates. Ensure any global reclaim is intentional and access-controlled.
- **server/job-queue.ts:148** — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 2 requeue) runs without tenant_id constraint, re-queuing jobs across all tenants.
  - _Fix:_ Scope the requeue to a tenant by adding `AND tenant_id = ${tenantId}` (and accept tenantId), or perform per-tenant iterations. If global behavior is required, gate it behind an explicit admin check and document the exception.
- **server/job-queue.ts:177** — UPDATE on tenant-scoped table `agent_jobs` (completeJob) does not include tenant_id in the WHERE clause. A caller can mark a job (by id) succeeded across tenants.
  - _Fix:_ Require the caller to provide tenantId and include `AND tenant_id = ${tenantId}` in the WHERE clause (or add a separate admin-only path with explicit checks). Apply the same lease-fencing but also tenant-scoping to prevent cross-tenant completes.
- **server/job-queue.ts:220** — SELECT max_attempts FROM `agent_jobs` by id is executed without tenant_id constraint. This read can leak/operate on another tenant's job row by id.
  - _Fix:_ Require tenantId as an argument and include `WHERE id = ${id} AND tenant_id = ${tenantId}` on the SELECT, or otherwise validate that the caller is authorized to inspect/manipulate that job id for cross-tenant operations.
- **server/job-queue.ts:237** — UPDATE on tenant-scoped table `agent_jobs` (failJob terminal branch) updates rows by id without scoping by tenant_id, allowing cross-tenant state changes.
  - _Fix:_ Add tenant scoping to the UPDATE (`AND tenant_id = ${tenantId}`) or require the function to accept and validate tenantId before mutating. Ensure lease-fencing AND tenant fencing are applied together.
- **server/job-queue.ts:255** — UPDATE on tenant-scoped table `agent_jobs` (failJob retry/requeue branch) performed without tenant_id constraint — re-schedules jobs across tenants by id.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the WHERE clause (and add tenantId parameter), or otherwise ensure only authorized/intentional global operations can run. Maintain lease-fencing plus tenant scoping.
- **server/job-queue.ts:274** — Operator cancelJob performs UPDATE on `agent_jobs` WHERE id = ${id} AND status IN (...) with NO tenant_id constraint. This allows cancelling jobs belonging to any tenant by id.
  - _Fix:_ Require tenantId to be provided by the caller (and validated) and add `AND tenant_id = ${tenantId}` to the WHERE clause. If this must be admin-only, enforce and document an explicit admin guard.
- **server/job-queue.ts:283** — Operator retryJob performs UPDATE on `agent_jobs` for arbitrary id without tenant_id filtering, enabling cross-tenant mutation of job state.
  - _Fix:_ Require and validate tenantId and add `AND tenant_id = ${tenantId}` to the WHERE clause, or restrict this operation to an explicit admin path that is access-controlled and audited.
- **server/job-queue.ts:320** — LIST SELECT from tenant-scoped table `agent_jobs` is executed with a constructed WHERE clause that may be empty (no tenant_id) when the caller does not supply tenantId — this can leak job rows across tenants by default.
  - _Fix:_ Make tenantId a required filter for non-admin callers (or default to the caller's tenant from context). If an unscoped listing is needed, require an explicit admin-only flag and check authorization before running the global query.
- **server/job-queue.ts:385** — Aggregation SELECT on tenant-scoped table `agent_jobs` (getJobStats) runs without tenant_id filter and returns counts across all tenants, potentially exposing cross-tenant metrics to non-admin callers.
  - _Fix:_ Require a tenantId parameter and filter `WHERE tenant_id = ${tenantId}` for tenant-scoped stats, or restrict this global aggregation to admin-only code paths and enforce authorization. Ensure any global stats path is explicitly guarded.
- **server/lib/bwb-job-progress.ts:141** — UPDATE against tenant-scoped table `video_jobs` uses scope(jobId, ...) which omits a tenant_id predicate when BWB_TENANT_ID is not set, allowing cross-tenant updates pinned only by jobId.
  - _Fix:_ Require an explicit tenantId for all mutating operations (do not fall back to a jobId-only predicate). Make scope() throw or return a WHERE clause that always includes tenant_id (or abort) when envTenantId() is null so no UPDATE/SELECT can run without a tenant predicate.
- **server/lib/bwb-job-progress.ts:166** — UPDATE against tenant-scoped table `video_jobs` (updateBwbChapters) uses scope(jobId, ...) which can omit tenant_id when BWB_TENANT_ID is missing, permitting cross-tenant writes.
  - _Fix:_ Do not allow a missing BWB_TENANT_ID to cause omission of tenant scoping. Require the caller to pass tenantId or fail the operation if env tenant is unknown. Add an explicit assertion that tenantId is present before calling .where(scope(...)).
- **server/lib/bwb-job-progress.ts:200** — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope may omit tenant_id when BWB_TENANT_ID is not set, enabling cross-tenant reads.
  - _Fix:_ Require tenant context for reads as well. Change scope() to always require tenantId (or accept an explicit tenantId param) and fail if missing, or add a guard in the call site that refuses to perform the SELECT when tenant is unknown.
- **server/lib/bwb-job-progress.ts:211** — UPDATE against tenant-scoped table `video_jobs` uses .where(scope(jobId)) and may run without a tenant_id predicate if BWB_TENANT_ID is not threaded, leading to potential cross-tenant mutation.
  - _Fix:_ Stop permitting updates that rely solely on jobId. Pass tenantId explicitly to these helper functions and assert its presence, or make scope() throw when envTenantId() returns null so writes cannot proceed without tenant scoping.
- **server/lib/bwb-job-progress.ts:227** — UPDATE (heartbeat) against tenant-scoped table `video_jobs` uses scope(jobId, {notDone:true,notFailed:true}) which can omit tenant_id when BWB_TENANT_ID is missing, allowing cross-tenant updates.
  - _Fix:_ Ensure the heartbeat writer always includes tenant_id. Require an explicit tenantId parameter or fail when env tenant is not set; do not silently warn and proceed with jobId-only writes.
- **server/lib/bwb-job-progress.ts:245** — UPDATE to mark a job failed performs .where(scope(jobId, { notDone: true })) where scope may omit tenant_id if BWB_TENANT_ID is absent, permitting cross-tenant state changes.
  - _Fix:_ Do not perform failure/terminal-state updates without a tenant predicate. Change the code to accept and validate tenantId (throw if missing), or make scope() enforce tenant presence so the WHERE always includes tenant_id.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT against tenant-scoped table `projects` has no tenant_id constraint (reads across all tenants). The query at lines 49–53 aggregates per-tenant counts but does not filter to the snapshot's tenant list, violating the invariant that tenant-scoped reads must include an application-level tenant filter.
  - _Fix:_ Restrict the aggregation to only the tenants you intend to inspect rather than scanning the entire projects table. For example, build an array of snapshot tenant ids and bind it into the WHERE clause: WHERE tenant_id = ANY(${Array.from(wantedByTenant.keys())}::int[]) AND tags && ${IDEA_TAG_LITERAL}::text[]. Alternatively run the same COUNT query per tenant (loop) with WHERE tenant_id = ${tenantId}. Ensure the tenant id(s) are passed as query parameters (not string-interpolated SQL).
- **server/lib/ideabrowser-score.ts:185** — Building a Postgres array literal by string-joining caller-supplied projectIds into `literal = `{${ids.join(",")}}` and interpolating it into sql`` (WHERE rm.project_id = ANY(${literal}::int[])). This is string-interpolated SQL with request-derived values (injection + isolation risk) and bypasses safe parameterization.
  - _Fix:_ Do not stringify/join ids into SQL. Validate/normalize ids as integers, then pass them as a safe parameterized array or use a parameterized IN clause. Example: build a sanitized ids array of numbers and use a parameter binding that supports arrays (or dynamically build `WHERE rm.project_id = ANY($1::int[])` with the driver binding). Alternatively, validate each id and construct a parameterized `IN ($1,$2,...)` query rather than injecting a raw string.
- **server/lib/inbox-ingest.ts:506** — UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id} has no tenant_id constraint. This writes to a tenant-scoped table without constraining by tenant_id (violates tenant-isolation-by-app-layer invariant).
  - _Fix:_ Add the tenant scoping clause to the WHERE so the update is explicitly constrained to the caller's tenant. e.g.: `WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}`. Audit other UPDATE/DELETE queries to ensure every write to tenant-scoped tables includes an explicit tenant_id filter.
- **server/lib/system-probes.ts:163** — Probe issues a SELECT count(*) FROM messages (db.select().from(messagesTable)...) with no tenant_id filter — reads global message count across tenants without constraining to a caller tenant.
  - _Fix:_ If this probe is intended to be tenant-scoped, add a WHERE tenant_id = ${tenantId} (or accept tenantId param). If it must be global/admin, ensure only explicit admin contexts call it and document that it reads across tenants; or provide a separate per-tenant count probe to avoid accidental cross-tenant reads.
- **server/lib/stuck-task-sweep.ts:56** — Query selects rows from tenant-scoped table `agent_runs` with no tenant_id constraint (SELECT id, tenant_id, ... FROM agent_runs WHERE status = 'running' ...). This scans/returns rows across tenants.
  - _Fix:_ If the intent is to scan per-tenant, add a tenant_id = ${tenantId} filter (or loop per-tenant). If this is an admin/system audit that must scan all tenants, require an explicit admin-only call-site and clearly document/authorize the cross-tenant scan; otherwise narrow the query to the caller's tenant.
- **server/lib/stuck-task-sweep.ts:84** — Query selects rows from tenant-scoped table `mind_tickets` with no tenant_id constraint (SELECT id, tenant_id, ... FROM mind_tickets WHERE status = 'in_progress' ...). This scans/returns rows across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} if the operation should be tenant-scoped, or require an explicit admin/system context for global scans and document/authorize that behavior. Avoid ad-hoc queries that read tenant-scoped tables without an explicit tenant equality filter.
- **server/martech-bundle.ts:324** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the primary id in the WHERE clause (eq(tenantVoiceProfiles.id, existing.id)) without constraining by tenant_id. Application invariant requires every write against tenant-scoped tables be constrained to the caller's tenant.
  - _Fix:_ Include the tenant constraint in the UPDATE (or otherwise verify ownership at write time). For example, add tenantId to the WHERE: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, ${opts.tenantId}))). Alternatively, change code to pass tenantId into the update helper and assert ownership prior to the update.
- **server/memory-graph.ts:124** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} runs without constraining by tenant_id. This mutates a tenant-scoped table row without enforcing the caller's tenant context.
  - _Fix:_ Constrain the UPDATE by tenant_id (and/or accept tenantId as a parameter). e.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Ensure the calling code supplies the tenantId or verifies ownership before performing the update.
- **server/memory-graph.ts:494** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped row using only its id, without an explicit tenant_id constraint. Although mem was selected earlier with a tenant filter, the write itself is not constrained to the tenant in the UPDATE statement (violates the codebase invariant that every read/write must include tenant filtering).
  - _Fix:_ Constrain the UPDATE to the tenant at write-time, e.g. UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Alternatively, add the tenantId parameter to refresh/update helpers and enforce ownership before performing the update.
- **server/minds-engine.ts:488** — SELECT from messages does not include an explicit tenant_id constraint on the messages table. The query joins conversations with a tenant filter on conversations (`c.tenant_id = ${tenantId}`) but does not constrain messages (`m`) by tenant, so a messages table row could be read without an explicit tenant check.
  - _Fix:_ Add an explicit tenant filter for messages (or otherwise bind messages to tenant) — for example include `AND m.tenant_id = ${tenantId}` in the WHERE or move tenant filter into the JOIN: `JOIN conversations c ON c.id = m.conversation_id AND c.tenant_id = ${tenantId}` AND `m.tenant_id = ${tenantId}`. Per the project invariant, every read against tenant-scoped tables must include the tenant_id constraint.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` without a tenant_id WHERE filter: `SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE`. This reads across all tenants.
  - _Fix:_ If the query must be multi-tenant (startup/admin maintenance), gate the function to an explicit admin/system context and document it; otherwise restrict reads by adding WHERE tenant_id = ${tenantId}. If the intent is to operate per-tenant, iterate tenants in a controlled fashion and always include tenant_id in the SELECT.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id constraint: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'`. This reads rows for all tenants.
  - _Fix:_ Either add a tenant_id filter for per-tenant operations or ensure this cross-tenant operation is only executed in an explicitly authorized/system context. Prefer explicitly iterating known tenant ids and including WHERE tenant_id = ${tenantId} on per-tenant queries.
- **server/oauth-subscriptions.ts:1258** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id constraint: `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`. This enumerates subscriptions across all tenants.
  - _Fix:_ If intended as a global maintenance job, ensure it runs in a tightly controlled admin/system context. Otherwise, restrict to a specific tenant by adding WHERE tenant_id = ${tenantId} or iterate per-tenant and include the tenant_id filter for each query.
- **server/oauth-subscriptions.ts:1328** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id constraint: `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE`. This reads all active subscriptions across tenants.
  - _Fix:_ Either scope the call to a tenant (add WHERE tenant_id = ${tenantId}) or ensure this cross-tenant access is only performed by an authorized admin path and is documented. Prefer explicit per-tenant queries that include tenant_id to preserve tenant isolation.
- **server/pace-control.ts:85** — Query against heartbeat_logs lacks any tenant_id constraint. checkPace() aggregates heartbeat_logs globally which can leak/intermix counts across tenants and allow one tenant to affect another's pace limits.
  - _Fix:_ Make pace checks tenant-scoped. Add a tenantId parameter to checkPace() and include WHERE tenant_id = ${tenantId} in the query (and ensure persona_name filtering remains parameterized). If the intent is global, document and justify; otherwise enforce per-tenant scoping on heartbeat_logs.
- **server/pace-control.ts:140** — getPaceSnapshot() queries heartbeat_logs without tenant scoping, returning global usage instead of a per-tenant snapshot — allows cross-tenant visibility and incorrect caps/monitoring.
  - _Fix:_ Require and accept a tenantId (or an optional tenantId that defaults to current caller) and add WHERE tenant_id = ${tenantId} to the snapshot queries. Ensure all aggregates and persona breakdowns are filtered by tenant_id.
- **server/pace-control.ts:147** — Persona-level aggregation (personaRows) is computed across all heartbeat_logs with no tenant filter, exposing other tenants' persona activity.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = ${tenantId}) to the personaRows query and require tenantId as input to getPaceSnapshot() so the per-tenant breakdown is isolated.
- **server/persona-export.ts:39** — Query for skills uses WHERE persona_id = ${personaId} OR persona_id IS NULL but does not include any tenant scoping. This may return skills belonging to other tenants (or omit intended tenant scoping) and leak tenant-specific data.
  - _Fix:_ Include tenant scoping for tenant-scoped skills (e.g. AND (tenant_id = ${tenantId} OR persona_id IS NULL OR tenant_id IS NULL) depending on schema for global skills). At minimum validate skills rows belong to the requested tenant or are explicitly global, and parameterize tenant_id in the query.
- **server/process-governor.ts:121** — Subquery counts heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads cross-tenant logs.
  - _Fix:_ Include tenant scoping in the subquery, e.g. add AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(heartbeatLogs.tenantId, tenantId))). Ensure every access to heartbeat_logs is filtered by tenant.
- **server/process-governor.ts:155** — Subquery computing pending_tasks: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) does not constrain heartbeat_tasks by tenant_id — it may count tasks from other tenants.
  - _Fix:_ Add tenant scoping to the subquery, e.g. AND tenant_id = ${tenantId} (or join via tenant-aware condition). Always include tenant_id when reading heartbeat_tasks.
- **server/process-governor.ts:198** — Query against heartbeat_logs to compute AVG(duration_ms) lacks tenant filtering (SELECT AVG(duration_ms) FROM heartbeat_logs WHERE ...). This can mix timing data across tenants.
  - _Fix:_ Constrain the query with tenant_id = ${tenantId} (or equivalent Drizzle predicate) to ensure metrics are tenant-isolated.
- **server/process-governor.ts:239** — cascading_failures query counts distinct persona_id from heartbeat_logs without tenant filter (SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ...). This may include other tenants' failures.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where(eq(heartbeatLogs.tenantId, tenantId))).
- **server/process-governor.ts:385** — (Related) Multiple other subqueries reading heartbeat_logs (e.g. in disable_task evaluation path) are missing tenant_id constraints (look for subqueries using heartbeat_logs without tenant filter).
  - _Fix:_ Audit all uses of heartbeat_logs and add tenant_id = ${tenantId} to every SELECT subquery or clause that reads that table to enforce tenant isolation.
- **server/process-governor.ts:656** — SELECT joins watchlist_items (LEFT JOIN watchlist_items wi ON wi.id = wa.watchlist_item_id) but does not ensure the joined watchlist_items row is scoped to the tenant. Reading wi.name without scoping may leak cross-tenant data if IDs are not globally unique.
  - _Fix:_ Constrain the joined table by tenant, e.g. LEFT JOIN watchlist_items wi ON wi.id = wa.watchlist_item_id AND wi.tenant_id = ${tenantId}, or add WHERE wi.tenant_id = ${tenantId}. Always ensure joined tenant-scoped tables are filtered to the same tenant.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} — project lookup is not scoped by tenant_id. A caller providing projectId could read another tenant's project row.
  - _Fix:_ Require a tenantId parameter or otherwise validate ownership, and add AND tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where(eq(projects.tenantId, tenantId))). Verify the caller owns the project before reading or writing related data.
- **server/project-brain.ts:332** — Backfill query: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 — this scans projects/conversations/messages without tenant scoping and can enumerate other tenants' data.
  - _Fix:_ Make backfillProjectBrains tenant-scoped (accept tenantId) or add WHERE clauses filtering projects, conversations and messages by tenant_id. Never iterate tenant-scoped tables globally in multi-tenant deployments without explicit admin intent and safeguards.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — conversation reads are not constrained by tenant_id during backfill and can pull other tenants' conversations.
  - _Fix:_ Add tenant scoping to the query (e.g. AND c.tenant_id = ${tenantId}) or ensure proj.id is resolved within a tenant check before iterating conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — message reads are not constrained by tenant_id; backfill can read messages from other tenants.
  - _Fix:_ Include tenant scoping in messages queries (AND tenant_id = ${tenantId}), or only iterate conversations known to belong to the target tenant. Require tenant ownership validation before reading messages.
- **server/recurring-messages.ts:258** — SELECT FROM tenant-scoped table agent_knowledge without tenant_id constraint (global read). The cron runner issues a broad SELECT of recurring_message rows with no WHERE tenant_id = ...
  - _Fix:_ Don’t perform an unscoped SELECT against a tenant-scoped table. Either run per-tenant queries or explicitly include tenant_id in the WHERE clause (or otherwise ensure only an authorized system-account may perform global reads and that any global-read is strictly audited). If the runner must enumerate all tenants, iterate tenants and query by tenant_id for each, or use a controlled, audited admin pathway with explicit tenant scoping for subsequent writes.
- **server/research-engine.ts:496** — INSERT into project_notes omits tenant_id: INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...). Per schema conventions tenant_id is expected to be set; omitting it risks cross-tenant attribution or DB constraint errors.
  - _Fix:_ Include tenant_id explicitly in the INSERT, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...). If project_notes is intended to inherit tenant via project_id only, still record tenant_id explicitly to enforce isolation at the SQL level.
- **server/research-engine.ts:503** — INSERT into project_notes inside loop omits tenant_id: VALUES (${projectId}, ${noteContent}, ${'Research Engine'}, NOW()). Missing tenant_id on per-finding inserts allows creation of notes without tenant scoping.
  - _Fix:_ Add tenant_id to these INSERTs as above (VALUES (${session.tenantId}, ${projectId}, ...)). Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly and is validated against project ownership (assertProjectInTenant was used earlier; pair that with a tenant_id column on the row).
- **server/routes.ts:1116** — SELECT from tenant-scoped table `file_storage` without constraining by tenant_id (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This returns rows across tenants and relies on JS-side filtering instead of an explicit tenant WHERE clause.
  - _Fix:_ Constrain the DB query to the authenticated/signed tenant in SQL (or compute ownership flags in SQL). Example: SELECT tenant_id FROM file_storage WHERE filename = $1 AND tenant_id = $2 LIMIT 1. Or return aggregate booleans in SQL (has_any_owner, owned_by_requesting_tenant) so no unscoped rows are read into app memory. Use drizzle eq(fileStorage.tenantId, authedTenantId) in the .where().
- **server/routes.ts:2061** — SELECT FROM tenant-scoped table `presenter_sessions` WHERE token = ${token} has no tenant_id SQL constraint. The row carries tenant_id but the query fetches by token only (capability-based auth is used) — this reads tenant-scoped rows without a tenant WHERE clause.
  - _Fix:_ After verifying the capability token, include tenant_id in the SQL WHERE clause (e.g. WHERE token = $1 AND tenant_id = $2) or change the query to return only the row for the requesting tenant (eq(presenter_sessions.tenantId, resolvedTenantId)). If this endpoint intentionally uses the token as the sole capability, ensure the token verification is done BEFORE any unscoped DB access and encode the tenant into the token verification result, then use it in the query's WHERE clause.
- **server/routes.ts:2089** — SELECT id FROM presenter_sessions WHERE token = ${token} (used in /api/presenter/:token/slide/:index) is executed without constraining by tenant_id; presenter_sessions is tenant-scoped but query lacks tenant filter.
  - _Fix:_ After validating/normalizing token, include tenant scoping in the SQL (e.g. .where(and(eq(presenter_sessions.token, token), eq(presenter_sessions.tenantId, expectedTenantId))). If token is the capability, resolve tenant from the token verification result and use it as an explicit WHERE condition to avoid scanning rows across tenants.
- **server/routes.ts:2180** — SELECT id, presentation_id, slides FROM presenter_sessions WHERE token = ${token} (in repair-images POST) reads tenant-scoped presenter_sessions by token only with no tenant_id constraint.
  - _Fix:_ Resolve tenant from the verified internal token or session secret and include eq(presenter_sessions.tenantId, tenantId) in the WHERE clause. If the endpoint is internal and uses a SESSION_SECRET, still prefer including tenant_id in the SQL WHERE to enforce DB-level scoping in the application layer.
- **server/routes.ts:4341** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts project notes via raw SQL: INSERT INTO project_notes (project_id, note, author) VALUES (...) without supplying tenant_id, relying only on prior SELECT from conversations for ownership — violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include the tenant_id explicitly in the INSERT and validate ownership of the project row before inserting. For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Prefer using the Drizzle query builder (e.g. .insert(...) with tenantId) or a storage-layer helper that enforces tenant scoping. Additionally, ensure the project_id belongs to the same tenant (or query the projects table with tenant_id) before inserting as defense-in-depth.
- **server/routes/admin-zombie-detector.ts:38** — Query on agent_trace_spans has no tenant constraint. The tx.execute(...) SELECT reads agent_trace_spans without a WHERE tenant_id = ... filter, so it can return rows across tenants (violates the invariant that tenant-scoped tables must be constrained to the caller's tenant).
  - _Fix:_ If agent_trace_spans is tenant-scoped, add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) and obtain tenantId from the request/session. If the intent is a platform-wide admin view, make the cross-tenant intent explicit (require/verify platform-admin rights) and document that this table is global; otherwise restrict the query to the caller's tenant_id to enforce isolation.
- **server/routes/admin.ts:745** — Query on heartbeat_tasks lacks tenant isolation. The SELECT from heartbeat_tasks used in /api/admin/silent-failures does not include a WHERE tenant_id = ${tenantId} filter, so it will surface heartbeat task rows across all tenants.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, constrain the query with WHERE tenant_id = ${tenantId} (after resolving tenantId from the request). If the endpoint intentionally needs a cross-tenant view, make that explicit by requiring platform-admin authorization and document that the table is global; otherwise add the tenant_id filter to enforce per-tenant isolation.
- **server/routes/conversations.ts:100** — SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} — this SELECT has no tenant_id predicate. project_conversations appears tenant-scoped, so this read may return rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause or use a tenant-aware accessor: SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId} (or conv.tenantId). Always enforce tenant_id on reads from tenant-scoped join tables.
- **server/routes/inbox-notifications.ts:101** — UPDATE on inbox_messages omits tenant predicate — SQL: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`. This allows modifying (marking read) a message by id without ensuring the message belongs to the caller's tenant.
  - _Fix:_ Include the tenant constraint in the UPDATE (or use a storage helper that enforces scope). Example: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`. Prefer using existing storage APIs that accept tenantId to avoid ad-hoc raw updates.
- **server/routes/projects.ts:33** — SELECT in projects listing uses sub-SELECTs against tenant-scoped tables (project_files, project_notes, project_conversations) but those sub-SELECTs do not include an explicit tenant_id filter.
  - _Fix:_ Constrain the sub-SELECTs to the requester's tenant. Example: `SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}` (or join to projects and filter on p.tenant_id = ${tenantId}). Ensure every read from tenant-scoped tables includes tenant_id in SQL predicates.
- **server/routes/projects.ts:185** — INSERT into project_notes does not supply tenant_id. project_notes appears to be tenant-scoped but the INSERT omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT: `INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId})`. Prefer parameterized/drizzle-style values and validate ownership (you already check project ownership earlier).
- **server/routes/projects.ts:224** — INSERT into project_files (base64 upload handler) does not set tenant_id on the inserted row.
  - _Fix:_ Add tenant_id to the INSERT: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId})`. Ensure tenant_id comes from the authenticated session (already present as tenantId) and not from user input.
- **server/routes/projects.ts:293** — INSERT into project_files (multipart upload handler) does not include tenant_id in the VALUES clause.
  - _Fix:_ Include tenant_id in the INSERT: `INSERT INTO project_files (..., tenant_id) VALUES (${projectId}, ${file.originalname}, ..., ${file.size}, ${tenantId})`. Always persist tenant_id on tenant-scoped tables and use parameterized/drizzle inserts.
- **server/routes/projects.ts:319** — SELECT from project_files in GET /api/projects/:id/files lacks an explicit tenant_id constraint (query: `SELECT * FROM project_files WHERE project_id = ${projectId}`), relying only on earlier project ownership check.
  - _Fix:_ Add an explicit tenant filter to the SELECT, e.g. `SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC`. Even when ownership has been validated earlier, enforce tenant_id at the SQL level for defense-in-depth.
- **server/routes/projects.ts:342** — SELECT used when downloading a file does not restrict to tenant_id: `SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1`.
  - _Fix:_ Constrain the SELECT by tenant: `... WHERE project_id = ${projectId} AND file_path = ${...} AND tenant_id = ${tenantId} LIMIT 1`. This prevents cross-tenant access if FK integrity or other checks are bypassed.
- **server/routes/projects.ts:371** — DELETE from project_files omits tenant_id in the WHERE clause (`DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}`).
  - _Fix:_ Include tenant_id in the DELETE predicate: `DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}`. Always include tenant filtering on DELETE/UPDATE to avoid accidental cross-tenant mutations; continue to validate ownership up-front as extra defense.
- **server/seed.ts:36** — SELECT COUNT(*) as count FROM research_sessions is executed without any tenant_id filter. research_sessions is tenant-scoped but the query aggregates across all tenants, risking cross-tenant data exposure.
  - _Fix:_ Constrain the query by tenant. Pass the caller's tenantId and use WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(research_sessions.tenantId, tenantId))). If the intent is an owner-level global check, make that explicit and isolate it from request paths (require admin context).
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true runs without a tenant_id filter. research_programs is tenant-scoped, so this reads data across all tenants.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or explicitly run it under an admin/owner-only path with documented justification. Avoid unscoped reads in application code that can be reached from tenant contexts.
- **server/seed.ts:3388** — SELECT from tenant-scoped table 'heartbeat_tasks' performed without a tenant_id constraint (db.select().from(heartbeatTasks)). This reads rows for all tenants.
  - _Fix:_ Filter reads by the current tenant (e.g. .where(eq(heartbeatTasks.tenantId, tenantId)) or add WHERE tenant_id = <tenant>) or otherwise enumerate per-tenant. Avoid global SELECTs on tenant-scoped tables.
- **server/seed.ts:3936** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' lacks a tenant_id constraint. conversations is tenant-scoped so this will affect all tenants' conversations.
  - _Fix:_ Add a tenant filter to the UPDATE (e.g. WHERE tenant_id = <tenant> AND model = ...), or run the update per-tenant. Use parameterized queries or Drizzle .where(eq(conversations.tenantId, tenantId)).
- **server/seed.ts:4108** — INSERT INTO project_notes (project_id, note, author) does not set tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id explicitly in the INSERT (tenant_id: <tenant>) or derive and validate tenant ownership from project_id before inserting. Ensure every INSERT into tenant-scoped tables sets tenant_id from a trusted source.
- **server/self-improvement.ts:536** — INSERT into tenant-scoped table `experiments` uses tenantId taken directly from the caller-supplied data (`tenantId: data.tenantId!`). The API accepts data.tenantId optionally and the insert relies on that value without validating ownership, allowing injection of arbitrary tenant ids or missing tenant_id.
  - _Fix:_ Require tenantId at the API boundary (do not accept it arbitrarily in the payload) and validate the caller's permission to act for that tenant before inserting. Make tenantId a required, server-controlled value (e.g. from authenticated session) and fail if it's missing rather than using a non-null assertion.
- **server/skillify.ts:221** — SELECT on 'personas' table uses only persona id (WHERE personas.id = ... ) with no tenant constraint. Personas are tenant-affiliated and this can disclose another tenant's persona record.
  - _Fix:_ Constrain persona lookup by tenant (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or fetch the persona via a storage layer that enforces tenant scoping. Use the conversation's tenant to validate persona ownership before returning persona data.
- **server/stability-watchdog.ts:177** — SELECT from 'heartbeat_logs' aggregates errors over the last 2 hours but contains no tenant_id filter. This query scans logs across tenants.
  - _Fix:_ Add tenant scoping to the query (WHERE tenant_id = ${tenantId}) or explicitly document and implement an audited all-tenants path (e.g. use a storage API that accepts an explicit ALL_TENANTS flag) if the watchdog must operate globally. Avoid implicit unscoped reads of tenant-scoped tables.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log uses no tenant filter; this will remove event log rows across all tenants.
  - _Fix:_ Scope deletions to tenant_id where appropriate, or call a centralized admin/storage API that intentionally performs cross-tenant cleanup and records an audit trail. Do not perform implicit global deletes on tenant-scoped tables.
- **server/storage.ts:253** — getConversationUnscoped() performs a SELECT from conversations WITHOUT scoping by tenant_id (WHERE tenant_id = ...). This is an unscoped read from a tenant-scoped table and can be misused to leak cross-tenant data if called from an HTTP handler or other unvalidated context.
  - _Fix:_ Restrict this query to the caller's tenant or make it explicit in the API (e.g., accept a validated tenantId and use eq(conversations.tenantId, tenantId)). If an unscoped fetch is required for internal orchestration, ensure callers are audited and the function name/JSdoc clearly documents the responsibility to validate ownership; consider returning tenantId along with the row and enforce a single approved callsite pattern.
- **server/storage.ts:303** — attachCitationMetadata() issues a raw SELECT from messages WHERE id IN (...) with NO tenant_id filter. messages is tenant-scoped — the SELECT can return messages from other tenants by id alone.
  - _Fix:_ Include tenant scoping in the SELECT (e.g., add AND tenant_id = ${tenantId} to the WHERE) or accept/derive and validate the tenantId for the message ids before running the query. If ids span tenants intentionally, require explicit admin context and document that intent.
- **server/storage.ts:841** — updateMemoryEmbedding() updates memory_entries by id only (WHERE id = <id>) with NO tenant_id constraint. memory_entries is tenant-scoped; this allows modifying another tenant's rows by id.
  - _Fix:_ Require the caller to provide/validate tenantId and include eq(memoryEntries.tenantId, tenantId) in the WHERE clause (or first verify ownership via a SELECT). Failing that, require an explicit admin context and make that explicit in the function contract.
- **server/storage.ts:853** — updateKnowledgeEmbedding() updates agent_knowledge by id only (WHERE id = <id>) with NO tenant_id constraint. agent_knowledge is tenant-scoped; this allows cross-tenant modification by id.
  - _Fix:_ Require and validate tenantId or first SELECT the row to confirm its tenant_id matches the caller, then perform the UPDATE with an AND tenant_id = ${tenantId} predicate. Alternatively provide an admin-only path that is clearly gated.
- **server/storage.ts:806** — createKnowledge() inserts into agent_knowledge without validating or enforcing that data.tenantId is present and belongs to the caller. agent_knowledge is tenant-scoped; an INSERT without a validated tenantId can lead to accidental/malicious cross-tenant writes or rely on callers to always pass correct tenantId.
  - _Fix:_ Validate that data.tenantId is a positive integer and (if applicable) that it matches the caller's tenant. Fail-closed if tenantId is missing/invalid. If certain trusted callers are allowed to write for admin/other tenants, require an explicit admin flag and document it.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table custom_tools performed using only name (where(eq(custom_tools.name, parsed.name))) with no tenant_id filter when checking for existing tool names during createCustomTool. This can leak existence or collide across tenants.
  - _Fix:_ Filter the lookup by tenant (and(eq(custom_tools.tenantId, tenantId))) when checking for existing tools. Ensure all existence/lookups on tenant-scoped custom_tools include tenantId.
- **server/tool-learning.ts:291** — UPDATE custom_tools .where(eq(custom_tools.id, tool.id)) used to persist auto-fixed implementation without constraining tenant_id. Updating by id alone may affect rows beyond the caller's tenant context and violates the tenant-scoping invariant.
  - _Fix:_ Include tenant guard in the UPDATE (e.g. .where(and(eq(custom_tools.id, tool.id), eq(custom_tools.tenantId, tenantId))). Propagate tenantId into the auto-fix flow and enforce it on all updates.
- **server/tool-learning.ts:365** — UPDATE custom_tools SET usageCount = ... WHERE id = ${tool.id} in executeCustomTool omits tenant_id filter — mutating a tenant-scoped table by id only (could increment usage for another tenant's tool row).
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(custom_tools.id, tool.id), eq(custom_tools.tenantId, tenantId)))). Ensure executeCustomTool maintains and uses the caller's tenantId for all writes.
- **server/tools.ts:8907** — UPDATE on tenant-scoped table 'proposed_skills' uses only id in WHERE clause (eq(proposedSkills.id, row.id)) and does not restrict by tenant_id — allows cross-tenant modification by id.
  - _Fix:_ Include the tenant filter in the WHERE clause (or assert ownership before updating). Example: add eq(proposedSkills.tenantId, tid) to the WHERE (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Or use a storage helper that enforces tenant ownership prior to the update.
- **server/tools.ts:8936** — UPDATE on tenant-scoped table 'proposed_skills' sets status='pending' with WHERE only on id (eq(proposedSkills.id, row.id)) — missing tenant_id constraint.
  - _Fix:_ Restrict the UPDATE to the caller's tenant by adding eq(proposedSkills.tenantId, tid) to the WHERE clause, or perform an ownership check (e.g., select+assert or a storage API) before modifying the row.
- **server/tools.ts:8950** — UPDATE on tenant-scoped table 'proposed_skills' to mark accepted/promoted uses WHERE eq(proposedSkills.id, row.id) without tenant_id constraint — permits cross-tenant escalation/modification.
  - _Fix:_ Add tenant scoping to the update (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or validate the row belongs to tid before applying changes.
- **server/tools.ts:8967** — UPDATE on tenant-scoped table 'proposed_skills' to mark rejected uses WHERE only on id (eq(proposedSkills.id, row.id)) and lacks tenant_id restriction.
  - _Fix:_ Enforce tenant isolation by adding eq(proposedSkills.tenantId, tid) to the WHERE clause or perform an explicit ownership check prior to performing the UPDATE.
- **server/tools/domains/browser/handlers.ts:165** — SQL UPDATE against tenant-scoped table 'browser_workflows' lacks tenant scoping in WHERE clause: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. This can allow modification of another tenant's row if only the id is used.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. use a parameterized clause `WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` or the Drizzle equivalent (e.g. .where(eq(browserWorkflows.tenantId, tenantId), eq(browserWorkflows.id, workflow.id))). Always include tenant_id in WHERE for updates to tenant-scoped tables, even if the id was fetched earlier.
- **server/tools/domains/files/handlers.ts:908** — SQL UPDATE on tenant-scoped table 'projects' lacks tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`. Without an explicit tenant constraint this UPDATE could affect a project belonging to another tenant.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE. For example: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}` or use Drizzle .where(eq(projects.id, projId), eq(projects.tenantId, tenantId)). If projId was derived after verifying ownership, still include the tenant_id check in the UPDATE to enforce the invariant.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE on tenant-scoped table `knowledge_triples` is missing a tenant_id constraint in the WHERE clause (UPDATE knowledge_triples ... WHERE id = ${row.id}). This allows changing rows without explicitly scoping to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId} AND valid_until IS NULL`); Additionally, check the number of affected rows and handle zero-updates (treat as not found / access denied) to avoid silent cross-tenant mutations.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(...) from `provider_keys` executed without a tenant_id filter. Even aggregate counts over tenant-scoped tables can reveal cross-tenant information.
  - _Fix:_ Restrict the aggregation to the caller's tenant: add WHERE tenant_id = ${tenantId}. If a global admin aggregate is intended, require and validate an explicit admin check before running an unscoped query and document that exception.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ${id} but does not include tenant_id = ${tid}. An update by id alone can modify rows outside the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE: add AND tenant_id = ${tid} to the WHERE clause (or use Drizzle .where(eq(table.tenantId, tid), eq(table.id, id))). Always combine id-based updates with the tenant filter to enforce isolation.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) executed without a tenant_id constraint. Although ids were previously selected with a tenant filter, issuing writes without tenant scoping violates the invariant and can risk cross-tenant modification in races or if ids are tampered with.
  - _Fix:_ Include tenant_id = ${tid} in the UPDATE predicate (e.g. WHERE tenant_id = ${tid} AND id = ANY(${ids})). Prefer updating rows using both tenant_id and id to enforce tenant isolation for all writes.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ${id} without constraining tenant_id. This allows modifying rows by id across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${id} AND tenant_id = ${tid}) or use an update helper that enforces tenant_id in the predicate. Ensure every UPDATE/DELETE against tenant-scoped tables always includes tenant_id = callerTenant.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger WHERE id = ${reservationId} performs a write to a tenant-scoped table without constraining by tenant_id — a caller could update another tenant's ledger row by id.
  - _Fix:_ Require and use the tenant id when settling a reservation. Either add a tenantId parameter and include AND tenant_id = ${tenantId} in the WHERE clause, or first SELECT the row by id and verify its tenant_id matches the caller before performing the UPDATE. This ensures the update cannot affect another tenant's rows.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger WHERE id = ${reservationId} (releaseVentureReservation) performs a write to a tenant-scoped table without constraining by tenant_id — a caller could release a reservation belonging to another tenant.
  - _Fix:_ Require and use the tenant id when releasing a reservation. Either pass tenantId into releaseVentureReservation and add AND tenant_id = ${tenantId} to the WHERE clause, or fetch the row first and verify tenant ownership before updating. Never update tenant-scoped rows by id alone.
- **server/video-job-runner.ts:236** — UPDATE on tenant-scoped table `video_jobs` (recoverStaleVideoJobs) updates rows across all tenants with no tenant_id filter. This operation can mark other tenants' jobs failed and thus violates tenant isolation constraints enforced at the application level.
  - _Fix:_ Either restrict the update to a specific tenant (add AND eq(videoJobs.tenantId, tenantId)) or iterate per-tenant and run the stale-job recovery scoped to each tenant. If this is intended as a global maintenance operation, ensure it runs under a well-audited admin context and document the justification; otherwise implement per-tenant filtering to preserve tenant isolation.
- **server/wiring-invariants.ts:366** — SELECT against subsystem table built via sql.raw() omits any tenant_id constraint (SELECT COUNT/MAX FROM ${spec.table}). This queries tenant-scoped tables (e.g. knowledge_nudges, tool_performance) across all tenants without enforcing tenant isolation.
  - _Fix:_ If the intent is per-tenant checks, add an explicit WHERE tenant_id = ${tenantId} (pass tenantId in) or otherwise aggregate per-tenant. If the intent is an owner/admin-global diagnostic, make that explicit (comment + require an explicit ADMIN_TENANT_ID param) and avoid ad-hoc cross-tenant reads. Avoid using un-scoped SELECTs on tenant-scoped tables; parameterize the tenant id in the query.
- **server/wiring-invariants.ts:535** — Raw SQL (sql.raw) counting rows and relation size for memory tables runs against tenant-scoped tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) with no WHERE tenant_id filter, returning cross-tenant totals.
  - _Fix:_ Restrict these diagnostics to a specific tenant by adding WHERE tenant_id = ${tenantId} or iterate tenants individually and record per-tenant counts. If a global aggregate is required for admin purposes, make the admin intent explicit and gate the operation to an admin-only path, documenting and approving cross-tenant aggregation.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory executes without a tenant_id constraint — it reads a tenant-scoped table across all tenants (then treats '0 rows' as a system-level warning). This can leak or conflate tenant data in multi-tenant deployments.
  - _Fix:_ Run graph_memory counts per tenant (WHERE tenant_id = ${tenantId}) or scope the check to a specific admin/owner tenant. Do not issue unscoped reads against tenant-scoped tables; require an explicit tenant id or admin audit mode with clear justification and access control.

### MEDIUM (34)

- **server/agentic/self-initiative.ts:100** — SELECT counts from heartbeat_logs do not include an explicit tenant_id WHERE clause. The query instead filters by task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}), but it still reads heartbeat_logs without asserting heartbeat_logs.tenant_id = caller tenant.
  - _Fix:_ Add an explicit tenant filter on heartbeat_logs, e.g. `WHERE tenant_id = ${tenantId} AND status = 'error' AND created_at >= ${since7d} AND task_id IN (...)`. Ensure every read against tenant-scoped tables uses `WHERE tenant_id = <caller tenant>` (or the equivalent drizzle eq(..., tenantId)).
- **server/auth.ts:1251** — Global cleanup DELETE: `DELETE FROM auth_sessions WHERE expires_at < ${now}` executes across all tenants without tenant scoping.
  - _Fix:_ Run cleanup per-tenant (loop tenants and delete per-tenant) or explicitly acknowledge and gate global maintenance operations (restrict to platform-admin-only code paths), and document that these are global maintenance operations separate from tenant-scoped flows.
- **server/auth.ts:1228** — clearExpiredSessions performs `DELETE FROM auth_sessions WHERE expires_at < ${now}` without tenant scoping.
  - _Fix:_ As with other cleanup queries, either scope by tenant when running from a tenant context or run as a clearly privileged global maintenance job. Prefer per-tenant deletes when invoked from tenant-scoped flows.
- **server/auth.ts:1114** — DELETE FROM password_reset_tokens WHERE token = ${tokenHash} removes rows across tenants if token hashes collide or tokens are global; no tenant constraint.
  - _Fix:_ Delete using both token and tenant_id (`WHERE token = ${tokenHash} AND tenant_id = ${tenantId}`) or run this within a privileged global job with explicit controls.
- **server/auth.ts:1119** — cleanExpiredResetTokens executes `DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()}` globally without tenant scoping.
  - _Fix:_ Perform expiration cleanup per-tenant or protect the global maintenance job behind a platform-admin-only path; document and audit global deletes so cross-tenant effects are explicit.
- **server/auto-asset-capture.ts:184** — Backfill SELECT queries conversations across all tenants: `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...` has no tenant filtering.
  - _Fix:_ Run backfill per-tenant (loop tenants) or add `WHERE c.tenant_id = ${tenantId}` when invoked from a tenant-scoped context. Ensure any background job that scans tenant-scoped tables is either explicitly privileged or scoped to a tenant at execution time.
- **server/auto-asset-capture.ts:208** — Selecting messages by conversation_id (`SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'`) omits tenant constraint.
  - _Fix:_ Include `AND tenant_id = ${c.tenant_id}` or fetch messages via a tenant-scoped join/verification. If conversation_id alone is truly globally-unique and safe, document that invariant and assert ownership before reading; otherwise add tenant scoping to the WHERE clause.
- **server/auto-transcript.ts:128** — Subquery SELECT COUNT(*) FROM `messages` (used to find conversations with messages) lacks a tenant_id constraint (counts messages without scoping to conversation’s tenant).
  - _Fix:_ Include tenant scoping in the subquery, e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0, or otherwise ensure the subquery’s messages access is explicitly limited to the conversation's tenant.
- **server/auto-transcript.ts:153** — Subquery SELECT COUNT(*) FROM `messages` inside a multi-join query lacks a tenant_id constraint (may read messages across tenants).
  - _Fix:_ Restrict the subquery to the conversation's tenant, e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0. Ensure all queries against `messages` include a tenant filter.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` is executed without a tenant_id filter (scans overdue commitments across all tenants). This is a cross-tenant read with no explicit caller-scope guard.
  - _Fix:_ If this is intended as a platform-admin job, make the admin intent explicit and enforce an admin-only execution path (and limit returned fields to non-sensitive/redacted values). If not intended, require a tenantId parameter and add WHERE tenant_id = ${tenantId} to the query. Alternatively, enumerate tenants first and run tenant-scoped queries per-tenant so no single query reads all tenant rows.
- **server/delivery-pipeline.ts:1215** — getDeliveryStatus selects from delivery_logs using only the delivery id when tenantId is omitted (the code allows an unscoped query).
  - _Fix:_ Either make tenantId required or enforce tenant scoping by always including eq(delivery_logs.tenant_id, tenantId) in the WHERE clause. If truly admin-level global queries are needed, restrict/mark the API and call site explicitly and ensure only trusted callers can omit tenantId.
- **server/delivery-pipeline.ts:1229** — getDeliveryStats can run a SELECT FROM delivery_logs without any tenant filter when tenantId is omitted, returning cross-tenant data.
  - _Fix:_ Require tenantId for normal callers or, if an admin global view is needed, provide a clearly separate admin-only API. For per-tenant callers, add WHERE tenant_id = ${tenantId} to the query.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot runs SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' with NO tenant_id constraint, returning global message counts across tenants.
  - _Fix:_ If this function is intended to operate per-tenant, accept a tenantId and add WHERE tenant_id = ${tenantId}. If it's truly a global admin metric, make that explicit and restrict callers to admin contexts.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot runs SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... with NO tenant_id constraint, returning global tool usage.
  - _Fix:_ If per-tenant behavior is required, add a tenant filter (tenant_id = ${tenantId}); otherwise make the global/admin nature explicit and limit caller surface.
- **server/dormant-deprecation.ts:203** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with NO tenant filter, yielding tools invoked across all tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or make the global semantics explicit and ensure only authorized admin callers use this function.
- **server/dormant-deprecation.ts:213** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... with NO tenant_id constraint, returning active tools platform-wide.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause for per-tenant computation, or separate the admin/global path and restrict its use to trusted processes.
- **server/heartbeat.ts:1723** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ... (line 1723) executes without a tenant_id filter. If heartbeat_logs are tenant-scoped, this is an unscoped read across tenants.
  - _Fix:_ If heartbeat_logs are tenant-scoped, restrict the query by tenant (e.g. WHERE task_name = ... AND status = ... AND tenant_id = ${tenantId}) or explicitly document that this cross-tenant read is intended for global/system logs. Prefer using storage APIs that encode the intended scope.
- **server/lib/mcp-api-keys.ts:182** — Best-effort touch: `UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id}` omits tenant_id. This updates a tenant-scoped table row by id only, without an explicit tenant_id constraint (violates the invariant).
  - _Fix:_ Constrain the UPDATE by tenant as well (use the tenant from the fetched row). For example: `UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}`. Make this change so even best-effort background touches cannot affect another tenant's rows.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() with a value derived from ledgerKey/tenantId to build the advisory-lock call (pg_advisory_xact_lock). sql.raw() here embeds a request-derived value into the SQL text instead of using a parameterized bind, creating injection/isolation risk.
  - _Fix:_ Avoid sql.raw() with caller-supplied inputs. Compute a numeric BigInt lock value server-side and pass it as a bound parameter (e.g. compute BigInt and use pg_advisory_xact_lock(${bigIntValue}::bigint)). Alternatively validate/normalize the value to a safe numeric type before binding. This removes use of string interpolation in the SQL fragment.
- **server/routes.ts:1531** — Admin endpoint queries tenant-scoped tables with unscoped SELECTs (db.select count(*) FROM conversations/messages/heartbeatTasks/memoryEntries/heartbeatLogs) — no WHERE tenant_id filter present.
  - _Fix:_ If the intention is platform-wide aggregates, make that explicit and restrict to platform-admin context (already gated) or otherwise add tenant_id filters. Prefer explicit SQL scoping (e.g. .where(eq(conversations.tenantId, someTid)) or aggregate by tenant in SQL and then reduce server-side. Document and review any cross-tenant aggregate reads as an intentional admin-only operation.
- **server/routes.ts:1560** — /api/public/architecture handler issues many unscoped SELECTs against tables that appear tenant-scoped (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) — no tenant_id constraints in the SQL calls.
  - _Fix:_ Either add explicit tenant_id constraints where each query is meant to be tenant-scoped, or if these are intended global admin aggregates, centralize and document admin-scoped aggregate queries and ensure they are only callable by platform admins. Prefer SQL-level scoping (eq(table.tenantId, tenantId) or grouping by tenant) rather than relying on implicit trust.
- **server/routes.ts:2322** — SELECT from tenant-scoped table `delivery_logs` using only order/session id (WHERE orderId = ... OR stripePaymentId = ...) — no tenant_id constraint. Endpoint is public by capability (unguessable sessionId) but SQL reads tenant-scoped rows without tenant scoping.
  - _Fix:_ Include tenant scoping in the WHERE or otherwise verify ownership in a prior, trusted step and then use that tenant id in the SQL WHERE clause (e.g. .where(and(or(eq(deliveryLogs.orderId, sessionId), eq(deliveryLogs.stripePaymentId, sessionId)), eq(deliveryLogs.tenantId, tenantId)))) so DB reads are explicitly bound to the resolved tenant.
- **server/routes.ts:2430** — Asynchronous order-lookup task performs db.select().from(deliveryLogs).where(lower(customer_email) = rawEmail) with no tenant_id constraint — reads tenant-scoped delivery_logs across tenants.
  - _Fix:_ Constrain the SELECT to tenant context where applicable, or, if the lookup is intentionally global, make that explicit and ensure the operation is safe for cross-tenant reads. Prefer including tenant scoping in the SQL or partitioning delivery logs by tenant in the query when possible.
- **server/routes.ts:2573** — Order verification flow runs a SELECT on delivery_logs with sqlTag`lower(${deliveryLogs.customerEmail}) = ${rawEmail}` and no tenant_id constraint — returns tenant-scoped rows across tenants.
  - _Fix:_ Restrict the query by tenant (eq(deliveryLogs.tenantId, resolvedTenantId)) when the tenant is known, or otherwise ensure the endpoint's design intentionally permits cross-tenant search and is only accessible with appropriate guarantees. Prefer SQL-level scoping to enforce tenant isolation (drizzle .where(eq(deliveryLogs.tenantId, tenantId))).
- **server/routes/goal-ledger.ts:92** — SELECT from agent_jobs reads active jobs across all tenants with no tenant_id filter: `SELECT j.id, j.kind, j.status, j.tenant_id, t.name AS tenant_name ... WHERE j.status IN ('pending', 'running')`. This is a read against a tenant-scoped table with no tenant constraint.
  - _Fix:_ Scope the query to a tenant or make the global intent explicit and protected. Either add `AND j.tenant_id = ${tenantId}` (or accept tenantId as a parameter) or, if an admin global view is required, ensure the route enforces platform-admin-only access and document the explicit global read. Better: add an explicit parameter (e.g. only run global query when requirePlatformAdmin is true) and/or pass the tenant filter into the function rather than issuing an unconstrained query.
- **server/routes/metrics.ts:63** — Multiple SELECTs query tenant-scoped tables (agent_cost_ledger, repair_incidents, etc.) without a tenant_id predicate (e.g. `SELECT tenant_id, COALESCE(SUM(cost_usd::numeric), 0) AS cost FROM agent_cost_ledger WHERE created_at > NOW() - INTERVAL '24 hours' GROUP BY tenant_id`). These reads return per-tenant data/labels without being constrained to the caller's tenant.
  - _Fix:_ Either restrict metrics queries to the caller's tenant (add `WHERE tenant_id = ${tenantId}`) or ensure the endpoint emits only anonymized/aggregated metrics that do not expose tenant identifiers. If global operator metrics are required, make the operator-only guard and token handling explicit and minimize per-tenant labels (or mask tenant identifiers). At minimum, document and centralize the global-read intent and enforce strict operator-only access controls; preferably add an explicit tenant filter when the caller is a tenant-scoped user.
- **server/seed.ts:4175** — UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id} does not include a tenant_id constraint. agent_knowledge is tenant-scoped; updating by id without also constraining tenant_id may update another tenant's row if ids collide or are misused.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = <tenant>) or perform the UPDATE using the same tenant-scoped query builder (eq(agentKnowledge.tenantId, tenantId)). Always validate ownership before modifying tenant-scoped rows.
- **server/storage.ts:862** — getMemoriesWithoutEmbeddings() selects from memory_entries WHERE status = 'active' AND embedding IS NULL without any tenant_id filter. This is an unscoped read across a tenant-scoped table.
  - _Fix:_ If this background job is intended to process rows for a single tenant, accept and validate tenantId and scope the query. If it is intentionally global (admin worker processing all tenants), make that explicit in the function name/signature and document that it is admin-only. Consider using an explicit ALL_TENANTS sentinel or an admin-only guard.
- **server/storage.ts:868** — getKnowledgeWithoutEmbeddings() selects from agent_knowledge WHERE notExpired AND embedding IS NULL without tenant scoping. agent_knowledge is tenant-scoped, so this is an unscoped read across tenants.
  - _Fix:_ Either accept/require a tenantId and use requiredTenantScope(agentKnowledge.tenantId, tenantId), or clearly document and gate this function as an admin/global worker job. Prefer explicit ALL_TENANTS sentinel semantics for global scans.
- **server/storage.ts:876** — archiveExpiredMemories() runs a raw SQL UPDATE on memory_entries (UPDATE memory_entries SET status = 'archived' WHERE ... AND expires_at <= NOW()) with NO tenant_id constraint. This updates all tenants' memory rows in one statement.
  - _Fix:_ If the job must operate across all tenants, make the admin/global intention explicit and ensure the caller is running in an authenticated admin context. Otherwise, scope the update by tenant_id (loop tenants or accept a tenantId parameter) to avoid accidental cross-tenant updates.
- **server/storage.ts:892** — archiveStaleMemories() performs a raw SQL UPDATE on memory_entries filtering only by timestamps and status, with NO tenant_id constraint. This will archive rows for all tenants.
  - _Fix:_ As with other background updates, either require/validate tenantId and include tenant scoping in the WHERE clause, or make the global/admin nature explicit and protect the callsites so only authorized admin workers run it.
- **server/twilio.ts:102** — SELECT id, content::jsonb AS info FROM agent_knowledge ... does a full scan of agent_knowledge rows for a title without filtering by tenant_id. If agent_knowledge is a tenant-scoped table this reads rows across tenants.
  - _Fix:_ Avoid reading tenant-scoped data for all tenants. If the design requires detecting contested pairings across tenants, make the intent explicit and perform a guarded query that joins to the conversations table to validate ownership (e.g. only return rows whose embedded conversationId maps to a conversation with a tenant_id matching the claimed tenant), or restrict the initial query to the current tenant (WHERE tenant_id = ${tenantId}) and handle cross-tenant conflicts via an operator/admin-only flow. Ensure any cross-tenant read is reviewed and limited to operator/admin contexts.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} LIMIT 1 reads the conversations table by id without an explicit tenant_id constraint. This is a SELECT against a tenant-scoped table with no tenant filter.
  - _Fix:_ Verify ownership using a tenant-aware query. For example, SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 and treat non-matching rows as failures, or fetch the conversation by id and tenant in one query and only accept rows that belong to the expected tenant. Avoid unconstrained selects on tenant-scoped tables.
- **server/video-job-runner.ts:208** — Database SELECT on tenant-scoped table `video_jobs` is performed using only jobId (WHERE job_id = ?) with NO tenant_id constraint. Reads against tenant-scoped tables must be limited to the caller's tenant — otherwise a caller could observe cancel state for jobs they don't own.
  - _Fix:_ Change isCancelRequested to accept/derive the caller's tenantId and include it in the WHERE clause (e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId)))). If this read is intentionally global (runner-internal), document and isolate it in an admin-only context, or otherwise ensure only trusted internal code can call it (and/or make it take a validated internal-only token).

### Suppressed — confirmed false positives (214)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights SET status/action_taken WHERE status='new' AND category IN (...) is executed without any tenant_id constraint — this writes/changes rows across all tenants.
  - _Why false positive:_ Optional tenantId allows explicit per-tenant or platform-wide housekeeping sweep; it only updates status/action_taken and routes returned rows per row-carried tenant_id.
- **server/auth.ts:196** _(upstream-verified)_ — API key lookup on the api_keys table is performed without any tenant constraint: db.select().from(apiKeys).where(... keyHash ...).
  - _Why false positive:_ API key hash is the bearer secret and lookup establishes req.tenantId from the matched key row; tenant cannot be known before this auth step.
- **server/auth.ts:304** _(upstream-verified)_ — Session lookup on auth_sessions uses only token equality and no tenant constraint: `SELECT tenant_id, is_admin, expires_at FROM auth_sessions WHERE token = ${tokenHash}`.
  - _Why false positive:_ Session token is the bearer credential; lookup by globally unique hashed token determines tenant_id for the session before later tenant checks.
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} has no tenant_id predicate. Deleting without tenant scoping can remove another tenant's row if IDs collide or if caller context is incorrect.
  - _Why false positive:_ Platform-wide purge sweep selects each conversation with its tenant_id, scopes child deletes per row, and final delete is by the row's own primary key in housekeeping flow.
- **server/deep-interview.ts:337** _(fk-transitive)_ — INSERT INTO project_notes is executed without setting tenant_id (INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ...). For tenant-scoped tables every INSERT must explicitly include tenant_id or validate ownership before inserting.
  - _Why false positive:_ project_notes has no tenant_id and is inserted via tenant-scoped parent join to projects where p.tenant_id = state.tenantId.
- **server/email.ts:515** _(system-sweep)_ — SELECT from tenant-scoped table `inbox_messages` with no tenant_id filter: `SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...)`.
  - _Why false positive:_ Backfill is a platform-wide maintenance job selecting rows lacking body content and only filling housekeeping fields; it does not return mixed tenant data to tenants.
- **server/email.ts:528** _(system-sweep)_ — UPDATE of tenant-scoped table `inbox_messages` without tenant_id constraint: `UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}`.
  - _Why false positive:_ Update occurs inside the maintenance backfill loop and only fills empty body fields on each selected row by PK, with no tenant-facing data mixing.
- **server/embeddings.ts:780** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` without tenant_id filter: `SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200`.
  - _Why false positive:_ Platform-wide backfill reads rows across tenants only to compute/store embedding_vec on the same row by PK; no tenant-facing output or cross-tenant mixing.
- **server/embeddings.ts:796** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant_id filter: `SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND (expires_at IS NULL OR expires_at > NOW()) LIMIT 200`.
  - _Why false positive:_ Platform-wide embedding_vec backfill for agent_knowledge mutates only housekeeping/vector fields on each row by PK and does not expose mixed tenant data.
- **server/embeddings.ts:752** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` in backfillMissingKnowledgeEmbeddings without tenant filter: `SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL AND source = 'autoresearch' AND (expires_at IS NULL OR expires_at > NOW()) LIMIT 50`.
  - _Why false positive:_ Backfill maintenance sweep processes rows individually to compute/store embeddings and only updates housekeeping fields; no tenant-facing mixed output.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table "code_proposals" (SET status = 'archived_stale') with WHERE id = ANY(...) but without any tenant_id condition — this can modify other tenants' rows.
  - _Why false positive:_ Health-audit TTL archiver is a platform-wide maintenance path archiving stale proposals across tenants, not a tenant-request write path.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE on tenant-scoped table "heartbeat_tasks" (SET enabled = false) uses WHERE id = ANY(...) but lacks any tenant_id constraint — this may disable tasks belonging to other tenants.
  - _Why false positive:_ Disabling stuck heartbeat tasks is cross-tenant housekeeping in a maintenance audit loop; it only flips enabled status and does not expose mixed tenant data.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE on knowledge_triples omits tenant constraint. The code updates rows by id (`WHERE id = ${c.id}`) after selecting contradictions scoped by tenant_id, but the UPDATE lacks `AND tenant_id = ${tenantId}` allowing a global id match to modify a row belonging to a different tenant.
  - _Why false positive:_ Updated ids come directly from the immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId in the same call path.
- **server/minerva-planner.ts:480** _(upstream-verified)_ — Rollback UPDATE on plans omits tenant scoping. In the revise failure path the code executes `UPDATE plans ... WHERE id = ${args.planId} AND status = 'revising'` without constraining by tenant_id, which can modify a plan row belonging to another tenant.
  - _Why false positive:_ Rollback targets the same plan row already loaded/CAS-verified earlier in this revise path, with tenant context carried from that verified row.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` without supplying tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id, violating the rule that every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is inserted only after assertProjectInTenant verifies the tenant-scoped parent project.
- **server/mpeg-engine.ts:1436** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` without supplying tenant_id. This upsert in the parallel path omits tenant_id in the VALUES list.
  - _Why false positive:_ project_files has no tenant_id column and insert is transitively isolated through verified parent project ownership via assertProjectInTenant.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE on tenant-scoped table `oauth_subscriptions` uses only the row id in WHERE clause (UPDATE ... WHERE id = ${sub.id}) and does not include tenant_id. This is a write to a tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ Startup validation loop is a platform-wide maintenance sweep over subscriptions, deactivating expired rows by their selected ids and not returning tenant data.
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE on tenant-scoped table `oauth_subscriptions` uses only the row id in WHERE clause (UPDATE ... WHERE id = ${sub.id}) with no tenant_id guard, allowing cross-tenant writes.
  - _Why false positive:_ This is the same startup housekeeping sweep disabling stale subscriptions after refresh failures; cross-tenant by design and only mutates status.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog updates the tenant-scoped 'plans' table without constraining by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows modifying another tenant's plan by id.
  - _Why false positive:_ appendExecutionLog is an internal helper invoked by executePlan/resume flow using server-generated planId, not a request-boundary tenant input.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Claiming a plan for execution uses UPDATE plans ... WHERE id = ${planId} AND status = 'approved' without enforcing tenant scope. This can allow a caller to claim and execute a plan belonging to another tenant.
  - _Why false positive:_ executePlan is a server-side worker claiming a plan by primary key and immediately returning tenant_id from the claimed row for per-row handling.
- **server/plan-executor.ts:527** _(upstream-verified)_ — Fallback path sets plan status to 'failed' with UPDATE plans SET status = 'failed' WHERE id = ${planId} without scoping by tenant_id. This can change another tenant's plan state.
  - _Why false positive:_ This fallback update occurs only after the prior claim UPDATE returned the same plan row by id, establishing the owned row/tenant in the same call path.
- **server/plan-executor.ts:728** _(upstream-verified)_ — Final plan status update performs UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} with no tenant_id filter. This allows finalizing plans across tenant boundaries.
  - _Why false positive:_ Final status write is in the same executePlan path after the claim RETURNING fetched that exact plan row including tenant_id; it is finalizing the already-claimed row.
- **server/plan-executor.ts:762** _(upstream-verified)_ — Error recovery path updates plans with UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' but omits tenant scoping. This can mark another tenant's running plan as failed.
  - _Why false positive:_ Error-path failure update is within executePlan after the plan was claimed into executing state by id in the same worker flow.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans performs a global UPDATE on the tenant-scoped 'plans' table (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...) with no tenant restriction, affecting plans for all tenants.
  - _Why false positive:_ Boot-time recovery is an intentional cross-tenant housekeeping sweep resetting stale executing rows and not returning tenant data to callers.
- **server/plan-executor.ts:787** _(system-sweep)_ — resumeStuckPlans selects pending approved plans with SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 without tenant_id filtering, returning plan ids across tenants.
  - _Why false positive:_ This boot recovery select intentionally scans approved plans across tenants only to enqueue each id for separate per-row execution, with no mixed tenant output.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — update is not constrained by tenant_id, so it could modify a heartbeat_tasks row belonging to another tenant if IDs collide.
  - _Why false positive:_ t.id comes from a preceding SELECT of heartbeat_tasks scoped by ht.tenant_id = tenantId in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} — this update is missing a tenant_id constraint and can modify agent_desks across tenants.
  - _Why false positive:_ desk.id is sourced from the immediately preceding SELECT on agent_desks constrained by ad.tenant_id = tenantId.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries performed without tenant_id constraint: UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}. This can update another tenant's memory entry by id.
  - _Why false positive:_ storedId is obtained from an immediately preceding SELECT on memory_entries constrained by tenant_id = input.tenantId and persona_id in the same path.
- **server/research-engine.ts:165** _(system-sweep)_ — cleanupZombieSessions runs a global UPDATE on research_sessions without tenant scoping: UPDATE research_sessions SET status='completed' ... WHERE status = 'running' ... AND started_at < ${staleThreshold}. This will mark sessions for all tenants as completed, not scoped to any tenant.
  - _Why false positive:_ Global stale-session cleanup is housekeeping that only flips status/ended_at on old running rows and returns aggregate ids to logs, not tenant-facing mixed data.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} — the UPDATE lacks a tenant_id constraint. This lets a tenant-side request update a conversation row by id without enforcing the row belongs to the caller's tenant, enabling cross-tenant modification.
  - _Why false positive:_ The conversation row was just created with tenantId and project ownership is verified by SELECT ... projects WHERE id = projectId AND tenant_id = tenantId in the same path.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) — the INSERT does not set tenant_id. If project_conversations is tenant-scoped, omission of tenant_id creates rows with no tenant ownership or owned by NULL, allowing cross-tenant associations and breaking isolation.
  - _Why false positive:_ project_conversations has no tenant_id and is a join child isolated through tenant-scoped parent project and newly created tenant-scoped conversation already verified in this path.
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — the UPDATE lacks a tenant_id constraint. Even though the code previously checked project ownership, updating without tenant scoping risks modifying a project row with a matching id in another tenant if race/ID guess occurs.
  - _Why false positive:_ Same call path verifies project ownership with WHERE id = projectId AND tenant_id = tenantId immediately before touching that project row.
- **server/routes/conversations.ts:231** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author) VALUES (...) — the INSERT omits tenant_id. If project_notes is a tenant-scoped table, this creates notes without explicit tenant ownership, allowing cross-tenant writes via archive/delete flows.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; projId is obtained via join requiring p.tenant_id = conv.tenantId/tenantId.
- **server/sculptor.ts:106** _(upstream-verified)_ — UPDATE sculptor_sessions executed without constraining by tenant_id (WHERE id = ${session.id} only). This allows cross-tenant modification if an attacker can supply/guess a session id.
  - _Why false positive:_ This updates the row that was inserted moments earlier with tenant_id = params.tenantId and captured as session.id within the same server flow, not from request-derived id.
- **server/sculptor.ts:113** _(upstream-verified)_ — UPDATE sculptor_sessions SET conversation_id ... WHERE id = ${session.id} lacks tenant_id constraint. Unscoped update may modify another tenant's session row.
  - _Why false positive:_ This is the same newly inserted session row in the same call path; session.id comes from the prior INSERT scoped with params.tenantId.
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE sculptor_sessions (monitorSession) sets status/result/completed_at WHERE id = ${sessionId} with NO tenant_id constraint. This can change state of sessions belonging to other tenants if sessionId is abused or guessed.
  - _Why false positive:_ monitorSession is an internal server worker updating a session by server-generated sessionId passed from create flow, not from an untrusted request boundary.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result ... WHERE id = ${sessionId} lacks tenant_id constraint. Although the row was read earlier with tenant scoping, the subsequent write is unscoped and could affect other tenants' rows.
  - _Why false positive:_ The function reads sculptor_sessions with WHERE id = sessionId AND tenant_id = tenantId immediately before this update in the same path.
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} but does NOT constrain by tenant_id. A reservationId uniquely identifies a ledger row but without tenant scoping this write could affect another tenant's ledger row.
  - _Why false positive:_ settleFusionReservation is an internal helper updating a server-created reservation row by globally unique ledger id, not request-supplied.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' lacks a tenant_id filter. heartbeat_tasks is tenant-scoped, so this reads global state across tenants unexpectedly.
  - _Why false positive:_ boot-time cleanup counts runaway heartbeat tasks globally for maintenance purposes and only emits aggregate count
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true runs without tenant_id constraint and will disable delegation tasks across ALL tenants.
  - _Why false positive:_ boot-time housekeeping intentionally disables runaway delegation tasks across tenants; status-only maintenance mutation, no tenant data returned
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant_id filter and reads across tenants.
  - _Why false positive:_ global maintenance count over heartbeat_tasks used to drive cleanup; aggregate only, no tenant content exposure
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true executes without tenant_id constraint, disabling matching tasks for all tenants.
  - _Why false positive:_ maintenance cleanup intentionally disables matching session-logger tasks platform-wide; status-only change
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ...) is missing tenant scoping and will count across tenants.
  - _Why false positive:_ global boot-time scan for orphaned repo-examination tasks is maintenance logic and reads only aggregate count
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true is executed without tenant_id constraint, affecting rows across tenants.
  - _Why false positive:_ platform-wide cleanup disables orphaned repo-examination tasks by pattern; housekeeping/status mutation only
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without constraining by tenant_id. An UPDATE without tenant scoping can modify rows belonging to other tenants.
  - _Why false positive:_ seed backfill iterates rows selected by primary key and fixes invalid models across tenants as boot-time maintenance; per-row update, no tenant data mixing
- **server/seed.ts:2516** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... is run with no tenant_id constraint. conversations is tenant-scoped, so this selects rows across all tenants and can return cross-tenant data.
  - _Why false positive:_ boot-time backfill scans conversations globally to repair missing join rows; maintenance loop handles each row separately and does not expose mixed tenant content
- **server/seed.ts:2523** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts linkage rows without any tenant_id. The project_conversations table (as created later) has no tenant_id column, so links can be created across tenants, enabling cross-tenant associations.
  - _Why false positive:_ project_conversations has no tenant_id and serves as child/join linking tenant-scoped parents; insert without tenant_id is by design in repair backfill
- **server/seed.ts:3882** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without any tenant_id constraint. This can delete heartbeat tasks across all tenants.
  - _Why false positive:_ boot-time purge of stale auto-disabled heartbeat tasks is intentional platform-wide housekeeping deleting only maintenance/status-marked rows
- **server/seed.ts:3911** _(system-sweep)_ — UPDATE memory_entries performed without a tenant_id constraint (UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND ...). memory_entries is tenant-scoped and this will modify rows for all tenants.
  - _Why false positive:_ Boot-time idempotent maintenance/backfill updates rows across tenants without returning tenant data or mixing content.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' without any tenant constraint (WHERE tenant_id = ...). This read can leak messages across tenants.
  - _Why false positive:_ Same call path first calls analyzeConversation(conversationId, tenantId), which verifies the conversation belongs to tenantId before reading its messages by conversationId.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' (conversation message history) lacks a tenant_id constraint; query only filters by conversationId.
  - _Why false positive:_ Immediately above, conversation ownership is enforced with conversations.id + conversations.tenantId before loading messages for that conversation.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT recent messages for skill extraction without constraining by tenant_id. The query filters only by conversationId and can return cross-tenant data.
  - _Why false positive:_ extractSkillFromConversation first calls analyzeConversation(conversationId, tenantId), which verifies tenant ownership for the conversation in the same path.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without any tenant constraint, deleting disabled delegation tasks across all tenants.
  - _Why false positive:_ Housekeeping purge of old disabled tasks across all tenants; only deletes stale rows and emits aggregate counts.
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs (pruneOldLogs) is executed without tenant scoping — this will prune heartbeat logs for all tenants rather than just the intended tenant(s).
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column, indicating an intentionally global log table.
- **server/surprise-scorer.ts:200** _(upstream-verified)_ — SELECT AVG(...) FROM felix_proposals WHERE id = ANY(${neighborIds}::int[]) computes across IDs without also restricting tenant_id. Neighbor IDs were derived earlier with a tenant filter, but this query fails the invariant (reads from tenant-scoped table without tenant constraint).
  - _Why false positive:_ neighborIds come directly from the immediately preceding tenant-scoped SELECT on felix_proposals WHERE tenant_id = tenantId.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE FROM custom_tools performed with .where(eq(custom_tools.id, tool.id)) in revertToolRow lacking tenant_id constraint — this removes tenant-scoped data by id only, risking cross-tenant deletion.
  - _Why false positive:_ revertToolRow is an internal server helper operating on the just-inserted tool row by server-derived primary key, not an untrusted request id.
- **server/tools.ts:4552** _(fk-transitive)_ — INSERT into tenant-scoped tables (project_notes / project_files) omits tenant_id. The code inserts project_notes and project_files using a validated projectId but does not include a tenant_id column in the INSERT list/values. Per the invariant every INSERT into tenant-scoped tables must explicitly pass tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes/project_files have no tenant_id column and are transitively isolated through verified tenant-owned project_id via assertProjectInTenant.
- **server/tools.ts:6405** _(fk-transitive)_ — INSERT into project_files omits tenant_id. After assertProjectInTenant succeeds, the code inserts a project_files row but does not include tenant_id in the INSERT. The invariant requires every INSERT into tenant-scoped tables to explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert is scoped through tenant-verified parent project_id using assertProjectInTenant.
- **server/tools.ts:6942** _(fk-transitive)_ — INSERT into project_files omits tenant_id. In create_slideshow_video the handler inserts into project_files after assertProjectInTenant but the INSERT does not set tenant_id explicitly, violating the rule that every INSERT into tenant-scoped tables must include tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert uses a parent project_id already verified to belong to the tenant.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` executed without a tenant_id filter (no WHERE tenant_id = ${tenantId}). This returns provider rows across all tenants and can leak API key metadata to the caller.
  - _Why false positive:_ provider_keys schema note says no tenant_id; query is additionally restricted to admin tenant intent only.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (reads running ab_test rows across all tenants).
  - _Why false positive:_ this is a platform-wide maintenance sweep over running experiments; each row carries its own tenant_id and subsequent mutation is per-row scoped by that tenant_id.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting without tenant_id constraint — reads pending/high insights across all tenants.
  - _Why false positive:_ durability sweep intentionally scans across tenants and processes each insight with its own row-carried tenant_id, updating only that row's housekeeping state.
- **server/agentic-engines.ts:347** _(upstream-verified)_ — SELECT from `heartbeat_logs` (tool call counts) in gatherOperationalData omits tenant scoping (no tenant_id WHERE clause), leaking/logging global heartbeat data across tenants.
  - _Why false positive:_ the shown conversations query is already constrained by WHERE tenant_id = tenantId; the heartbeat_logs claim does not match this code.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT recent heartbeat_logs (recentLogs) in runPredictiveEngine lacks a tenant filter (reads recent successful logs across all tenants).
  - _Why false positive:_ heartbeat_logs has no tenant_id and is intentionally platform-global; no tenant-scoped row read shown here
- **server/agentic-engines.ts:519** _(upstream-verified)_ — SELECT aggregating heartbeat_logs in runOptimizationEngine has no tenant_id constraint, returning aggregated task performance across all tenants.
  - _Why false positive:_ the shown messages query already filters by tenant_id; finding mislabels a scoped query as unscoped
- **server/agentic-engines.ts:533** _(upstream-verified)_ — SELECT from `heartbeat_tasks` (taskConfig) in runOptimizationEngine omits tenant_id filter, returning enabled tasks potentially across tenants.
  - _Why false positive:_ heartbeat_tasks query above already includes tenant_id = ${tenantId}; prompt line is not the query
- **server/agentic-features.ts:932** _(upstream-verified)_ — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} is missing a tenant_id constraint — a write to a tenant-scoped table without verifying the tenant.
  - _Why false positive:_ row.id comes from a prior SELECT on outreach_enrollments constrained by tenant_id and contact_email in the same call path
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk expiry sweep expireStaleApprovals performs UPDATE agentApprovals SET status='expired' ... WHERE status='pending' AND expiresAt < now() without any tenant_id constraint — modifies approvals across all tenants.
  - _Why false positive:_ global expiry housekeeping intentionally updates stale approvals across tenants and then handles each returned row with its own tenantId
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — runBudgetEnforcement performs a SELECT on the tenant-scoped table `department_budgets` without constraining by tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads across all tenants and violates the invariant that every read against tenant-scoped tables must include a tenant_id filter.
  - _Why false positive:_ heartbeat sweep intentionally scans all active budgets and emits per-row tenant-scoped events using each row's tenant_id
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — SELECT query scans agent_runs (and joins agent_approvals) without constraining by tenant_id — a cross-tenant read of tenant-scoped tables (agent_runs, agent_approvals).
  - _Why false positive:_ resume worker is a platform-wide maintenance sweep over pending approval/running rows and processes each candidate with its carried tenant_id
- **server/agentic/wake-scheduler.ts:146** _(system-sweep)_ — Subquery selecting due wakes lacks a tenant_id filter: `SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...`
  - _Why false positive:_ due-wake claimer is a global scheduler sweep; rows are claimed then emitted/updated using each row's own tenant_id
- **server/auth.ts:336** _(internal-worker-pk)_ — DELETE from auth_sessions by token lacks tenant constraint: `DELETE FROM auth_sessions WHERE token = ${hashSessionToken(token)}`.
  - _Why false positive:_ delete uses a globally unique session token hash, not caller-supplied tenant context; token itself is the row identity
- **server/auth.ts:1255** _(system-sweep)_ — Global purge: `DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%'` removes rows across all tenants with no tenant constraint.
  - _Why false positive:_ boot-time purge of legacy non-v2 sessions is platform-wide housekeeping and does not return or mix tenant data
- **server/auth.ts:1233** _(system-sweep)_ — clearAllSessions issues `DELETE FROM auth_sessions` with no WHERE clause (global erase across tenants).
  - _Why false positive:_ clearAllSessions is a global session wipe utility operating across tenants as maintenance/administration, not a tenant-facing mixed-data path
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is missing a tenant_id constraint (reads messages by conversation_id without scoping to the caller tenant).
  - _Why false positive:_ conversation ownership is proven just above via conversations WHERE id = conversationId AND tenant_id = tenantId, and messages are read only for that trusted conversation_id
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is missing a tenant_id constraint (reads all messages for a conversation without scoping to tenant).
  - _Why false positive:_ assertConversationInTenant and a tenant-scoped conversation read prove ownership earlier in the same call path before reading messages for that conversation
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` omits tenant_id constraint (updates conversation.project_id using id only). This allows cross-tenant modification if ids are untrusted.
  - _Why false positive:_ this is a maintenance backfill loop operating across tenants; each row carries c.tenant_id from joins and the update is housekeeping to set missing project linkage only
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table this write will not record the tenant and thus violates the tenant-isolation invariant (inserts to tenant-scoped tables must set tenant_id explicitly).
  - _Why false positive:_ invoice_items has no tenant_id and is a child of tenant-scoped invoices; parent invoice is inserted with tenant_id in the same transaction
- **server/channel-routing.ts:31** _(global-table)_ — listChannelRoutes SELECT joins the personas table (p) without any tenant_id constraint. If personas are tenant-scoped this leaks persona names across tenants.
  - _Why false positive:_ channel_routes is defined here as a global unique-by-channel routing table with no tenant_id, and personas are schema-global too
- **server/channel-routing.ts:51** _(global-table)_ — DELETE FROM channel_routes WHERE channel = ${channel} executes against a global channel_routes table with no tenant filter — a tenant could delete another tenant's route mapping.
  - _Why false positive:_ delete operates on intentionally global channel_routes table keyed by unique channel, not a tenant-scoped table
- **server/channel-routing.ts:63** _(global-table)_ — getPersonaForChannel SELECT reads persona_id from channel_routes by channel with no tenant constraint. This can return a persona belonging to a different tenant (no ownership verification).
  - _Why false positive:_ read is from intentionally global channel_routes table and returns schema-global persona_id
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute DELETE uses only channel to delete from channel_routes (no tenant filter), affecting global state and potentially other tenants' mappings.
  - _Why false positive:_ removeChannelRoute deletes from intentionally global channel_routes mapping by global channel key
- **server/data-protection.ts:149** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` with no tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads across tenants without scoping.
  - _Why false positive:_ This is an explicit platform-wide purge of expired soft-deleted conversations; rows are processed per-row with their own tenant_id and not returned to tenants.
- **server/data-protection.ts:360** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` uses only project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) and lacks an explicit tenant_id predicate. This relies on the caller's prior query rather than enforcing tenant scoping on the child table.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects already filtered by tenant_id before this per-project read.
- **server/data-protection.ts:372** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` uses only project_id (SELECT file_name,... FROM project_files WHERE project_id = ${proj.id}) and omits tenant_id scoping.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects already selected with tenant_id = tenantId.
- **server/data-protection.ts:404** _(upstream-verified)_ — COUNT query against tenant-scoped table `messages` (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) omits tenant_id in the WHERE clause.
  - _Why false positive:_ The conversation ids come from an immediately prior SELECT constrained by tenant_id = tenantId, so this count is scoped by owned parent rows in the same call path.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint, allowing deletion of chunks across tenants.
  - _Why false positive:_ A few lines above, the same function verifies the collection belongs to tenantId before deleting chunks for that collectionId/docPath.
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks SET embedding WHERE id = ${row.id} without constraining by tenant_id.
  - _Why false positive:_ row.id comes from immediately prior SELECT on doc_chunks scoped by collection_id and tenant_id; update uses those vetted row ids in same call path
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with a vector literal built from request-derived embedding (`${sql.raw(`'${vec}'::vector`)}`) inside the query. The vec value comes from generateEmbedding(query) (request-derived).
  - _Why false positive:_ query is explicitly constrained by memory_entries.tenant_id = tenantId; finding is about raw vector SQL, not cross-tenant isolation
- **server/embeddings.ts:564** _(upstream-verified)_ — Use of sql.raw() with a vector literal derived from the caller's query embedding (`${sql.raw(`'${vec}'::vector`)}`) in vectorSearchKnowledge.
  - _Why false positive:_ query is explicitly constrained by agent_knowledge.tenant_id = tenantId; no tenant-isolation issue on this line
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' performed without any tenant_id constraint (reads running/pending agent_runs across all tenants). This loads cross-tenant rows into an in-memory global TaskState store, risking cross-tenant data leakage and breaking the app's tenant-isolation invariant.
  - _Why false positive:_ boot-time rehydration intentionally scans all tenants' in-flight agent_runs and rebuilds per-row in-memory state keyed from each row; housekeeping across tenants, not tenant-facing mixed output
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` uses only projects.id in WHERE (WHERE projects.id = projectId) and lacks an explicit tenant_id constraint. Per invariant, writes to tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ same function immediately verified fetched project's tenantId matches caller before this update, so write is gated by prior ownership check in same call path
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table "code_proposals" without scoping by tenant_id — query returns rows across all tenants.
  - _Why false positive:_ health-audit is a platform-wide maintenance scan for stale proposals; cross-tenant read/update is housekeeping and emits audit findings, not tenant-facing mixed content
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table "heartbeat_tasks" without scoping by tenant_id — query will return tasks across all tenants.
  - _Why false positive:_ heartbeat stale-task scan/disable is platform-wide housekeeping over task rows and only mutates enabled status, with no tenant-facing data mixing
- **server/heartbeat.ts:325** _(system-sweep)_ — UPDATE against tenant-scoped table `heartbeat_tasks` uses WHERE id = ${t.id} with no tenant_id constraint. This can modify a task belonging to another tenant if id is mis-bound or in race conditions.
  - _Why false positive:_ boot-time heartbeat maintenance sweep intentionally normalizes housekeeping fields across tenants; no tenant data returned or mixed
- **server/heartbeat.ts:329** _(system-sweep)_ — UPDATE against tenant-scoped table `heartbeat_tasks` uses WHERE id = ${t.id} with no tenant_id constraint (model normalization path).
  - _Why false positive:_ same startup housekeeping sweep updates only task model metadata across all tenants, not tenant-facing content
- **server/heartbeat.ts:154** _(system-sweep)_ — Multiple UPDATEs to `research_schedules` use WHERE id = ${sched.id} (e.g. lines 154, 160, 165, 173) but do not include tenant_id in the WHERE clause. `research_schedules` rows are referenced by sched.tenant_id earlier, but the UPDATEs are not tenant-scoped.
  - _Why false positive:_ heartbeat cron runner is a platform-wide scheduler advancing per-row schedule housekeeping using each row's own tenant_id for execution
- **server/job-queue.ts:98** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_jobs` (claimDueJobs) performed without any tenant_id constraint. This atomically transitions jobs to 'running' across all tenants, violating tenant-isolation-by-application-filters.
  - _Why false positive:_ global worker claim loop intentionally operates across tenants and returns rows with their own tenant_id for per-row processing
- **server/job-queue.ts:134** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 1) updates rows across all tenants with no tenant_id filter (marks terminal failures).
  - _Why false positive:_ lease reclamation is platform-wide housekeeping mutating only job status fields across tenants
- **server/job-queue.ts:148** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 2 requeue) runs without tenant_id constraint, re-queuing jobs across all tenants.
  - _Why false positive:_ global expired-lease requeue is housekeeping-only across tenants with no tenant data mixing or exposure
- **server/job-queue.ts:177** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_jobs` (completeJob) does not include tenant_id in the WHERE clause. A caller can mark a job (by id) succeeded across tenants.
  - _Why false positive:_ completeJob is internal worker state transition by server-generated job primary key with lease fencing, not a request-bound tenant lookup
- **server/job-queue.ts:220** _(internal-worker-pk)_ — SELECT max_attempts FROM `agent_jobs` by id is executed without tenant_id constraint. This read can leak/operate on another tenant's job row by id.
  - _Why false positive:_ failJob reads max_attempts by internal server-generated job id inside worker path, not from an untrusted tenant request boundary
- **server/job-queue.ts:237** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_jobs` (failJob terminal branch) updates rows by id without scoping by tenant_id, allowing cross-tenant state changes.
  - _Why false positive:_ failJob terminal update is internal worker mutation by server-generated job id plus attempts fencing
- **server/job-queue.ts:255** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_jobs` (failJob retry/requeue branch) performed without tenant_id constraint — re-schedules jobs across tenants by id.
  - _Why false positive:_ failJob retry/requeue update is internal worker mutation by server-generated job id plus attempts fencing
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT against tenant-scoped table `projects` has no tenant_id constraint (reads across all tenants). The query at lines 49–53 aggregates per-tenant counts but does not filter to the snapshot's tenant list, violating the invariant that tenant-scoped reads must include an application-level tenant filter.
  - _Why false positive:_ boot/backfill maintenance sweep intentionally scans across tenants and only computes per-tenant counts for housekeeping, not tenant-facing mixed output
- **server/lib/ideabrowser-score.ts:185** _(upstream-verified)_ — Building a Postgres array literal by string-joining caller-supplied projectIds into `literal = `{${ids.join(",")}}` and interpolating it into sql`` (WHERE rm.project_id = ANY(${literal}::int[])). This is string-interpolated SQL with request-derived values (injection + isolation risk) and bypasses safe parameterization.
  - _Why false positive:_ despite unsafe literal construction, both revenue_missions and mission_evidence are explicitly filtered by tenant_id = tenantId in the same query
- **server/lib/inbox-ingest.ts:506** _(upstream-verified)_ — UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id} has no tenant_id constraint. This writes to a tenant-scoped table without constraining by tenant_id (violates tenant-isolation-by-app-layer invariant).
  - _Why false positive:_ Row was just inserted/claimed in same call path with tenantId and unique inbox_message_id storeResult.id; update targets that claimed row.
- **server/lib/system-probes.ts:163** _(system-sweep)_ — Probe issues a SELECT count(*) FROM messages (db.select().from(messagesTable)...) with no tenant_id filter — reads global message count across tenants without constraining to a caller tenant.
  - _Why false positive:_ Global health probe reads aggregate count only, not tenant content, for platform-wide diagnostics.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — Query selects rows from tenant-scoped table `agent_runs` with no tenant_id constraint (SELECT id, tenant_id, ... FROM agent_runs WHERE status = 'running' ...). This scans/returns rows across tenants.
  - _Why false positive:_ Platform-wide stuck-task audit intentionally scans all tenants and carries row tenant_id per result for diagnostics.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — Query selects rows from tenant-scoped table `mind_tickets` with no tenant_id constraint (SELECT id, tenant_id, ... FROM mind_tickets WHERE status = 'in_progress' ...). This scans/returns rows across tenants.
  - _Why false positive:_ Platform-wide stuck-task audit intentionally scans all tenants and carries row tenant_id per result for diagnostics.
- **server/martech-bundle.ts:324** _(upstream-verified)_ — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the primary id in the WHERE clause (eq(tenantVoiceProfiles.id, existing.id)) without constraining by tenant_id. Application invariant requires every write against tenant-scoped tables be constrained to the caller's tenant.
  - _Why false positive:_ existing row is fetched immediately above via getVoiceProfile({ tenantId, profileName }); update uses that owned row's server-fetched PK.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} runs without constraining by tenant_id. This mutates a tenant-scoped table row without enforcing the caller's tenant context.
  - _Why false positive:_ catId comes from same-path SELECT/INSERT scoped by tenant_id=tenantId in ensureCategory before this increment.
- **server/memory-graph.ts:494** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped row using only its id, without an explicit tenant_id constraint. Although mem was selected earlier with a tenant filter, the write itself is not constrained to the tenant in the UPDATE statement (violates the codebase invariant that every read/write must include tenant filtering).
  - _Why false positive:_ mem.id is selected immediately above from memory_entries with tenant_id = tenantId; update is on that previously verified row.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without a tenant_id WHERE filter: `SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE`. This reads across all tenants.
  - _Why false positive:_ Startup validation intentionally iterates active subscriptions across all tenants and processes each row with its own tenant_id.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id constraint: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'`. This reads rows for all tenants.
  - _Why false positive:_ Boot-time reconnect logic intentionally enumerates provider rows across tenants to repair each tenant separately by row-carried tenant_id.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id constraint: `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`. This enumerates subscriptions across all tenants.
  - _Why false positive:_ Proactive refresh is a platform maintenance loop over active subscriptions; each row is handled separately using its own tenant_id.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id constraint: `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE`. This reads all active subscriptions across tenants.
  - _Why false positive:_ Force-refresh is an explicit cron-style maintenance loop over all active subscriptions, scoped per processed row by tenant_id.
- **server/pace-control.ts:85** _(global-table)_ — Query against heartbeat_logs lacks any tenant_id constraint. checkPace() aggregates heartbeat_logs globally which can leak/intermix counts across tenants and allow one tenant to affect another's pace limits.
  - _Why false positive:_ heartbeat_logs has no tenant_id and checkPace enforces documented global/per-lane platform caps by design
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot() queries heartbeat_logs without tenant scoping, returning global usage instead of a per-tenant snapshot — allows cross-tenant visibility and incorrect caps/monitoring.
  - _Why false positive:_ heartbeat_logs is non-tenant-scoped and snapshot is an operator/debug view over global pace data
- **server/pace-control.ts:147** _(global-table)_ — Persona-level aggregation (personaRows) is computed across all heartbeat_logs with no tenant filter, exposing other tenants' persona activity.
  - _Why false positive:_ persona aggregation is over non-tenant-scoped heartbeat_logs used for global pace monitoring
- **server/persona-export.ts:39** _(global-table)_ — Query for skills uses WHERE persona_id = ${personaId} OR persona_id IS NULL but does not include any tenant scoping. This may return skills belonging to other tenants (or omit intended tenant scoping) and leak tenant-specific data.
  - _Why false positive:_ skills table has no tenant_id; query intentionally combines persona-specific and global skills rows
- **server/process-governor.ts:121** _(fk-transitive)_ — Subquery counts heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads cross-tenant logs.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is scoped transitively via task_id -> tenant-scoped heartbeat_tasks row selected as ht.tenant_id = tenantId
- **server/process-governor.ts:198** _(global-table)_ — Query against heartbeat_logs to compute AVG(duration_ms) lacks tenant filtering (SELECT AVG(duration_ms) FROM heartbeat_logs WHERE ...). This can mix timing data across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and query computes platform-wide response-time metric over global log table
- **server/process-governor.ts:239** _(global-table)_ — cascading_failures query counts distinct persona_id from heartbeat_logs without tenant filter (SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ...). This may include other tenants' failures.
  - _Why false positive:_ heartbeat_logs is global and this computes aggregate cascading-failure health metric, not tenant-row retrieval from a tenant-scoped table
- **server/process-governor.ts:385** _(upstream-verified)_ — (Related) Multiple other subqueries reading heartbeat_logs (e.g. in disable_task evaluation path) are missing tenant_id constraints (look for subqueries using heartbeat_logs without tenant filter).
  - _Why false positive:_ shown code reads heartbeat_tasks with WHERE tenant_id = tenantId; cited line itself is just local max computation, not an unscoped query
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 — this scans projects/conversations/messages without tenant scoping and can enumerate other tenants' data.
  - _Why false positive:_ backfillProjectBrains is a platform-wide maintenance/backfill loop creating per-project files, not returning mixed tenant data in one tenant-facing response
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT FROM tenant-scoped table agent_knowledge without tenant_id constraint (global read). The cron runner issues a broad SELECT of recurring_message rows with no WHERE tenant_id = ...
  - _Why false positive:_ cron runner intentionally scans due rows across tenants and each row carries tenant_id used for per-row scoped follow-up actions
- **server/research-engine.ts:496** _(fk-transitive)_ — INSERT into project_notes omits tenant_id: INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...). Per schema conventions tenant_id is expected to be set; omitting it risks cross-tenant attribution or DB constraint errors.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via validated project_id after assertProjectInTenant
- **server/research-engine.ts:503** _(fk-transitive)_ — INSERT into project_notes inside loop omits tenant_id: VALUES (${projectId}, ${noteContent}, ${'Research Engine'}, NOW()). Missing tenant_id on per-finding inserts allows creation of notes without tenant scoping.
  - _Why false positive:_ project_notes has no tenant_id column and inserts are anchored to project_id already verified in tenant via assertProjectInTenant
- **server/routes.ts:1116** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` without constraining by tenant_id (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This returns rows across tenants and relies on JS-side filtering instead of an explicit tenant WHERE clause.
  - _Why false positive:_ asset access is authorized by signed URL or authenticated owner check; tenant_id on file_storage rows is used to verify ownership/telemetry for basename collisions
- **server/routes.ts:4341** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts project notes via raw SQL: INSERT INTO project_notes (project_id, note, author) VALUES (...) without supplying tenant_id, relying only on prior SELECT from conversations for ownership — violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and insert uses project_id derived from conversation already constrained by tenant_id
- **server/routes/admin-zombie-detector.ts:38** _(system-sweep)_ — Query on agent_trace_spans has no tenant constraint. The tx.execute(...) SELECT reads agent_trace_spans without a WHERE tenant_id = ... filter, so it can return rows across tenants (violates the invariant that tenant-scoped tables must be constrained to the caller's tenant).
  - _Why false positive:_ admin-only platform-wide diagnostics intentionally aggregate usage across tenants and return counts/last-used metadata, not tenant content to tenants
- **server/routes/admin.ts:745** _(system-sweep)_ — Query on heartbeat_tasks lacks tenant isolation. The SELECT from heartbeat_tasks used in /api/admin/silent-failures does not include a WHERE tenant_id = ${tenantId} filter, so it will surface heartbeat task rows across all tenants.
  - _Why false positive:_ admin endpoint intentionally surfaces platform-wide heartbeat housekeeping state across tenants after admin authorization
- **server/routes/conversations.ts:100** _(fk-transitive)_ — SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} — this SELECT has no tenant_id predicate. project_conversations appears tenant-scoped, so this read may return rows belonging to other tenants.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped by tenant-checked parent project plus newly created tenant-scoped conversation
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on inbox_messages omits tenant predicate — SQL: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`. This allows modifying (marking read) a message by id without ensuring the message belongs to the caller's tenant.
  - _Why false positive:_ same handler first selects inbox_messages row by id and tenant_id, then updates that verified row by its primary key
- **server/routes/projects.ts:33** _(fk-transitive)_ — SELECT in projects listing uses sub-SELECTs against tenant-scoped tables (project_files, project_notes, project_conversations) but those sub-SELECTs do not include an explicit tenant_id filter.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are counted only via tenant-scoped parent projects p filtered by p.tenant_id
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes does not supply tenant_id. project_notes appears to be tenant-scoped but the INSERT omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is inserted only after verifying parent project belongs to tenant
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (base64 upload handler) does not set tenant_id on the inserted row.
  - _Why false positive:_ project_files has no tenant_id and insert is tied to tenant-verified parent project
- **server/routes/projects.ts:293** _(fk-transitive)_ — INSERT into project_files (multipart upload handler) does not include tenant_id in the VALUES clause.
  - _Why false positive:_ project_files has no tenant_id and insert is tied to tenant-verified parent project
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT from project_files in GET /api/projects/:id/files lacks an explicit tenant_id constraint (query: `SELECT * FROM project_files WHERE project_id = ${projectId}`), relying only on earlier project ownership check.
  - _Why false positive:_ project_files has no tenant_id and read is scoped through earlier verified parent project ownership
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT used when downloading a file does not restrict to tenant_id: `SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1`.
  - _Why false positive:_ project_files has no tenant_id and download lookup is scoped by tenant-verified parent project plus project_id
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE from project_files omits tenant_id in the WHERE clause (`DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}`).
  - _Why false positive:_ project_files has no tenant_id and delete is limited to child row under tenant-verified parent project
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) as count FROM research_sessions is executed without any tenant_id filter. research_sessions is tenant-scoped but the query aggregates across all tenants, risking cross-tenant data exposure.
  - _Why false positive:_ boot-time dev snapshot import performs global existence check during seeding, not tenant-facing data return or mixed output
- **server/seed.ts:2158** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true runs without a tenant_id filter. research_programs is tenant-scoped, so this reads data across all tenants.
  - _Why false positive:_ seed maintenance scans active research_programs globally to normalize model field and only updates each row by its own id
- **server/seed.ts:3388** _(system-sweep)_ — SELECT from tenant-scoped table 'heartbeat_tasks' performed without a tenant_id constraint (db.select().from(heartbeatTasks)). This reads rows for all tenants.
  - _Why false positive:_ seed-time housekeeping reads heartbeat_tasks globally to decide whether to insert system tasks; not tenant-facing and no mixed tenant content output
- **server/seed.ts:3936** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' lacks a tenant_id constraint. conversations is tenant-scoped so this will affect all tenants' conversations.
  - _Why false positive:_ Boot-time seed/backfill intentionally updates housekeeping/model-alias fields across all tenants and only logs aggregate counts.
- **server/seed.ts:4108** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author) does not set tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id column and is transitively scoped via project_id to tenant-scoped projects row selected with tenant_id=1.
- **server/skillify.ts:221** _(global-table)_ — SELECT on 'personas' table uses only persona id (WHERE personas.id = ... ) with no tenant constraint. Personas are tenant-affiliated and this can disclose another tenant's persona record.
  - _Why false positive:_ Referenced schema says personas has no tenant_id column, so persona lookup by id is not a tenant-scoped read.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from 'heartbeat_logs' aggregates errors over the last 2 hours but contains no tenant_id filter. This query scans logs across tenants.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column; watchdog aggregation is intentional platform-wide monitoring.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log uses no tenant filter; this will remove event log rows across all tenants.
  - _Why false positive:_ Global housekeeping delete of old processed/failed event_log rows only mutates stale status logs and returns aggregate counts, not tenant data.
- **server/storage.ts:303** _(upstream-verified)_ — attachCitationMetadata() issues a raw SELECT from messages WHERE id IN (...) with NO tenant_id filter. messages is tenant-scoped — the SELECT can return messages from other tenants by id alone.
  - _Why false positive:_ attachCitationMetadata only re-reads ids taken from already tenant-scoped message rows fetched just above, so no new cross-tenant id source is introduced.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table custom_tools performed using only name (where(eq(custom_tools.name, parsed.name))) with no tenant_id filter when checking for existing tool names during createCustomTool. This can leak existence or collide across tenants.
  - _Why false positive:_ Name is deterministically rewritten with tenantToolName(parsed.name, tenantId) before lookup, making existence check tenant-namespaced.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE custom_tools SET usageCount = ... WHERE id = ${tool.id} in executeCustomTool omits tenant_id filter — mutating a tenant-scoped table by id only (could increment usage for another tenant's tool row).
  - _Why false positive:_ tool row is selected immediately above with tenant_id = tenantId, and update uses that fetched row's id
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — SQL UPDATE against tenant-scoped table 'browser_workflows' lacks tenant scoping in WHERE clause: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. This can allow modification of another tenant's row if only the id is used.
  - _Why false positive:_ workflow is fetched just above with id/name plus tenant_id, then updated by the fetched row id
- **server/tools/domains/files/handlers.ts:908** _(upstream-verified)_ — SQL UPDATE on tenant-scoped table 'projects' lacks tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`. Without an explicit tenant constraint this UPDATE could affect a project belonging to another tenant.
  - _Why false positive:_ projId comes from conversation->project join that enforces p.tenant_id = tenantId earlier in same flow
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` is missing a tenant_id constraint in the WHERE clause (UPDATE knowledge_triples ... WHERE id = ${row.id}). This allows changing rows without explicitly scoping to the caller's tenant.
  - _Why false positive:_ rows being updated were selected immediately above from knowledge_triples with tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(...) from `provider_keys` executed without a tenant_id filter. Even aggregate counts over tenant-scoped tables can reveal cross-tenant information.
  - _Why false positive:_ provider_keys schema note shows no tenant_id; admin-only aggregate over platform-global provider registry
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ${id} but does not include tenant_id = ${tid}. An update by id alone can modify rows outside the caller's tenant.
  - _Why false positive:_ id is obtained from a prior select scoped by tenant_id, persona_id, and category in the same call path
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) executed without a tenant_id constraint. Although ids were previously selected with a tenant filter, issuing writes without tenant scoping violates the invariant and can risk cross-tenant modification in races or if ids are tampered with.
  - _Why false positive:_ ids are derived only from rows selected just above with tenant_id = tid, persona_id, and category in the same flow
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ${id} without constraining tenant_id. This allows modifying rows by id across tenants.
  - _Why false positive:_ id comes from a prior SELECT on memory_entries scoped by tenant_id=${tid} and persona_id, so the update targets a row already verified owned by this tenant.
- **server/video-job-runner.ts:236** _(system-sweep)_ — UPDATE on tenant-scoped table `video_jobs` (recoverStaleVideoJobs) updates rows across all tenants with no tenant_id filter. This operation can mark other tenants' jobs failed and thus violates tenant isolation constraints enforced at the application level.
  - _Why false positive:_ periodic stale-job recovery is a platform-wide maintenance sweep updating housekeeping/status fields across tenants and only returns recovered counts/job ids for logs, not tenant-facing mixed data.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — SELECT against subsystem table built via sql.raw() omits any tenant_id constraint (SELECT COUNT/MAX FROM ${spec.table}). This queries tenant-scoped tables (e.g. knowledge_nudges, tool_performance) across all tenants without enforcing tenant isolation.
  - _Why false positive:_ global wiring diagnostic intentionally aggregates table counts/max timestamps across tenants for observability and emits only findings/aggregate metadata, not tenant row data.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Raw SQL (sql.raw) counting rows and relation size for memory tables runs against tenant-scoped tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) with no WHERE tenant_id filter, returning cross-tenant totals.
  - _Why false positive:_ memory-growth tripwire is a platform-wide observability check using only aggregate counts/relation size across tenants, with no tenant-facing row data exposure or per-tenant mixing.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory executes without a tenant_id constraint — it reads a tenant-scoped table across all tenants (then treats '0 rows' as a system-level warning). This can leak or conflate tenant data in multi-tenant deployments.
  - _Why false positive:_ global bootstrap diagnostic checks whether graph_memory is empty across all tenants and emits only a system warning, not tenant data.
- **server/email.ts:470** _(system-sweep)_ — SELECT against tenant-scoped table `inbox_messages` without tenant_id constraint: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1`.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): platform primary-inbox poller (single system account); message_id dedupe against its own inbox rows.
- **server/memory-graph.ts:184** _(upstream-verified)_ — UPDATE memory_categories ... SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND ...) WHERE id = ${categoryId} computes the subquery count without filtering memory_entries by tenant_id. This reads tenant-scoped memory_entries across all tenants and writes the aggregated value into a (presumably) tenant-scoped category row — enabling cross-tenant pollution/leakage and breaking tenant isolation invariants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} has NO tenant_id constraint. exportPersona reads a persona by id without verifying it belongs to the caller's tenant, allowing cross-tenant persona data exposure.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): personas table has no tenant_id column (platform-global personas); tenant linkage lives in trust/skills rows fetched separately.
- **server/process-governor.ts:972** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — update lacks tenant scoping and may change another tenant's rule row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE agent_knowledge content without tenant_id constraint. The runner updates the row by id only: UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}. This can modify another tenant's row if ids collide or are mis-attributed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/second-opinion.ts:424** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without a tenant_id condition. This can release/alter reservations belonging to other tenants if reservationId is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:170** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` may run without a tenant_id constraint. detectStagnation builds `conditions` with category always but only adds tenant filter when tenantId is provided; the .where() call will execute a cross-tenant query when tenantId is omitted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — getExperimentHistory can return experiments across all tenants. When no tenantId is supplied the function falls back to db.select().from(experiments)... (no tenant filter), exposing multi-tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:405** _(internal-worker-pk)_ — attachCitations() performs an UPDATE on messages (UPDATE messages SET citations = ...) filtering only by id and WITHOUT tenant_id in the WHERE clause. This allows a caller to update another tenant's message row by id, enabling cross-tenant data corruption.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — UPDATE on tenant-scoped table 'felix_proposals' performed without constraining tenant_id (stampArgsEmbedding: UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — UPDATE on tenant-scoped table 'felix_proposals' sets actual_outcome_embedding but omits tenant_id in WHERE (UPDATE felix_proposals SET actual_outcome_embedding = ... WHERE id = ${proposalId}).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:181** _(upstream-verified)_ — UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} omits tenant_id — writing tenant-scoped state without tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} lacks a tenant_id filter — mutates tenant-scoped rows without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/agent-channels.ts:198** _(upstream-verified)_ — JOIN to channel_subscriptions omits tenant_id constraint (cs.tenant_id not checked). The query can read subscription rows across tenants when joined to tenant-scoped channel_messages.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): getUnreadCount filters cm.tenant_id = ${tenantId}; the channel_subscriptions join is keyed on the caller's persona_id (personas are tenant-scoped FKs), so another tenant's subscription rows cannot match.
- **server/agentic-features.ts:773** _(upstream-verified)_ — SELECT id FROM outreach_enrollments to check for existing enrollment does not include tenant_id, allowing cross-tenant visibility/false-positive enrollment checks.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` is executed without constraining by tenant_id. The code updates a review row by id only (`WHERE id = ${pending.reviewId} AND reused = false`) which can allow cross-tenant writes if review ids are not globally unique for the caller's tenant or if the in-memory mapping is poisoned.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/chat-engine.ts:4119** _(global-table)_ — recordToolExecution(conversationId, toolName, ...) is called without any tenant identifier or prior ownership verification. This likely writes tool-execution records linked to a conversation but does not constrain them to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolExecution (felix-brain.ts:298-320) only mutates the in-process taskStateStore Map; no DB write, no tenant table.
- **server/chat-engine.ts:4131** _(global-table)_ — recordToolCallForStuckDetection(conversationId, ...) is invoked with only a conversation id and args and no tenant context/ownership check. If this function persists or reads tenant-scoped data it lacks a tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolCallForStuckDetection (stuck-diagnostics.ts:100-134) pushes into an in-process Map (turnToolCalls); no DB access, no tenant-scoped table.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on tenant-scoped table `commitments` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This unclaims/updates a commitment by id alone after detecting an invalid tenant on the returned row, allowing a cross-tenant mutation if the id belongs to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/delivery-pipeline.ts:1252** _(global-table)_ — getDeliveryByStripePayment selects a delivery_log by stripePaymentId without any tenant constraint, exposing a referenced delivery row across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:93** _(signed-url)_ — submitDfyIntake performs an UPDATE on dfy_intake_forms using only the form id in the WHERE clause (.where(eq(dfyIntakeForms.id, form.id))) and omits tenant_id scoping.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): form row fetched by unguessable 48-hex token (the documented authorization for the public intake form); UPDATE reuses that row's own id.
- **server/embeddings.ts:748** _(system-sweep)_ — Dynamic sql.raw usages: `UPDATE ${sql.raw(table)} SET embedding_vec = ${sql.raw(`'${vec}'::vector`)}, ...` — combines a runtime table name and an interpolated vector literal. Table and vec ultimately originate from application inputs/calls.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/embeddings.ts:684 — storeEmbeddingVec only rewrites the embedding_vec/embedding columns of a row from that row's OWN content (recomputed vector), injecting no cross-tenant data. Every caller passes an internally-generated PK: freshly inserted knowledge/memory ids (storage.ts:806/818, research-engine.ts:508/1046) or ids from server-side backfill/dream-consolidation queries (embeddings.t
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table `projects` is performed using only projects.id (WHERE projects.id = projectId) without scoping by tenant_id. This reads a tenant-scoped row before verifying the caller's tenant ownership (ownership check occurs after the SELECT).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): ensureProjectFolder selects only tenantId/driveFolderId and immediately throws on project.tenantId!==tenantId at google-drive.ts:551-553 before returning or writing anything.
- **server/heartbeat.ts:2817** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (approval set path) omits tenant filtering. The newly created task includes tenantId but the subsequent UPDATE does not assert tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3269** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) executes without a tenant_id constraint, potentially affecting another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/job-queue.ts:274** _(internal-worker-pk)_ — Operator cancelJob performs UPDATE on `agent_jobs` WHERE id = ${id} AND status IN (...) with NO tenant_id constraint. This allows cancelling jobs belonging to any tenant by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:283** _(internal-worker-pk)_ — Operator retryJob performs UPDATE on `agent_jobs` for arbitrary id without tenant_id filtering, enabling cross-tenant mutation of job state.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:320** _(system-sweep)_ — LIST SELECT from tenant-scoped table `agent_jobs` is executed with a constructed WHERE clause that may be empty (no tenant_id) when the caller does not supply tenantId — this can leak job rows across tenants by default.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/job-queue.ts:385** _(system-sweep)_ — Aggregation SELECT on tenant-scoped table `agent_jobs` (getJobStats) runs without tenant_id filter and returns counts across all tenants, potentially exposing cross-tenant metrics to non-admin callers.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/lib/bwb-job-progress.ts:141** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `video_jobs` uses scope(jobId, ...) which omits a tenant_id predicate when BWB_TENANT_ID is not set, allowing cross-tenant updates pinned only by jobId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:166** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `video_jobs` (updateBwbChapters) uses scope(jobId, ...) which can omit tenant_id when BWB_TENANT_ID is missing, permitting cross-tenant writes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:200** _(internal-worker-pk)_ — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope may omit tenant_id when BWB_TENANT_ID is not set, enabling cross-tenant reads.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): jobId is an unguessable random id (vj_<base36>_<6-byte-hex>); scope() warns once when BWB_TENANT_ID unthreaded — documented defense-in-depth.
- **server/lib/bwb-job-progress.ts:227** _(internal-worker-pk)_ — UPDATE (heartbeat) against tenant-scoped table `video_jobs` uses scope(jobId, {notDone:true,notFailed:true}) which can omit tenant_id when BWB_TENANT_ID is missing, allowing cross-tenant updates.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/process-governor.ts:155** _(global-table)_ — Subquery computing pending_tasks: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) does not constrain heartbeat_tasks by tenant_id — it may count tasks from other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): heartbeat-task pace counting is a platform-wide throttle by design (same family as checkPace/heartbeat_logs); per-tenant fairness would need a schema migration and is tracked separately.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} — project lookup is not scoped by tenant_id. A caller providing projectId could read another tenant's project row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — conversation reads are not constrained by tenant_id during backfill and can pull other tenants' conversations.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — message reads are not constrained by tenant_id; backfill can read messages from other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/routes.ts:2061** _(signed-url)_ — SELECT FROM tenant-scoped table `presenter_sessions` WHERE token = ${token} has no tenant_id SQL constraint. The row carries tenant_id but the query fetches by token only (capability-based auth is used) — this reads tenant-scoped rows without a tenant WHERE clause.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2057 — presenter token is crypto.randomBytes(16).hex (routes.ts:2140) — an unguessable capability URL for public slide viewing; the token IS the access control by design.
- **server/storage.ts:253** _(upstream-verified)_ — getConversationUnscoped() performs a SELECT from conversations WITHOUT scoping by tenant_id (WHERE tenant_id = ...). This is an unscoped read from a tenant-scoped table and can be misused to leak cross-tenant data if called from an HTTP handler or other unvalidated context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:841** _(internal-worker-pk)_ — updateMemoryEmbedding() updates memory_entries by id only (WHERE id = <id>) with NO tenant_id constraint. memory_entries is tenant-scoped; this allows modifying another tenant's rows by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:853** _(internal-worker-pk)_ — updateKnowledgeEmbedding() updates agent_knowledge by id only (WHERE id = <id>) with NO tenant_id constraint. agent_knowledge is tenant-scoped; this allows cross-tenant modification by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:806** _(upstream-verified)_ — createKnowledge() inserts into agent_knowledge without validating or enforcing that data.tenantId is present and belongs to the caller. agent_knowledge is tenant-scoped; an INSERT without a validated tenantId can lead to accidental/malicious cross-tenant writes or rely on callers to always pass correct tenantId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:768 — createKnowledge() inserts a typed InsertKnowledge with tenantId supplied by callers; agent_knowledge.tenantId is NOT NULL so an unscoped insert fails closed at the DB. Callers pass authenticated tenantId (routes.ts:6240 `{...parsed.data, tenantId}`, doc-collections, knowledge/handlers.ts). Additionally the path already enforces a reserved-category guard against the act
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE custom_tools .where(eq(custom_tools.id, tool.id)) used to persist auto-fixed implementation without constraining tenant_id. Updating by id alone may affect rows beyond the caller's tenant context and violates the tenant-scoping invariant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tools.ts:8907** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' uses only id in WHERE clause (eq(proposedSkills.id, row.id)) and does not restrict by tenant_id — allows cross-tenant modification by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8936** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' sets status='pending' with WHERE only on id (eq(proposedSkills.id, row.id)) — missing tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8950** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' to mark accepted/promoted uses WHERE eq(proposedSkills.id, row.id) without tenant_id constraint — permits cross-tenant escalation/modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8967** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' to mark rejected uses WHERE only on id (eq(proposedSkills.id, row.id)) and lacks tenant_id restriction.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger WHERE id = ${reservationId} performs a write to a tenant-scoped table without constraining by tenant_id — a caller could update another tenant's ledger row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger WHERE id = ${reservationId} (releaseVentureReservation) performs a write to a tenant-scoped table without constraining by tenant_id — a caller could release a reservation belonging to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/delivery-pipeline.ts:313** _(deferred)_ — updateDeliveryLog performs an UPDATE on delivery_logs using only the id in the WHERE clause (where(eq(deliveryLogs.id, id))) with no tenant_id constraint.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
