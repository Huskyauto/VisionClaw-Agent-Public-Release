# Tenant-Isolation Audit Report

_Generated 2026-07-19T20:13:13.982Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **541/541** file(s) audited · chunks 51/51 OK (0 failed)
- Findings: **292** (CRITICAL 79 · HIGH 174 · MEDIUM 38 · LOW 1)
- Precision second-pass (`gpt-5.4`): **120** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (79)

- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` lacks a tenant_id constraint (WHERE clause only filters by id). This can modify rows belonging to another tenant.
  - _Fix:_ Include the tenant in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) and change the function signature to accept/require tenantId if necessary.
- **server/agentic/approvals.ts:123** — Bulk UPDATE on tenant-scoped table `agent_approvals` (expireStaleApprovals) lacks a tenant_id constraint — it expires pending approvals across all tenants.
  - _Fix:_ Either run expiry per-tenant (accept tenantId and filter on it) or explicitly document and guard this global operation. Prefer adding a tenant filter when called from tenant-scoped contexts, or iterate tenants and apply the UPDATE scoped to each tenant to avoid cross-tenant modifications.
- **server/agentic/harness-adaptation.ts:71** — SELECT from agent_trace_spans (mineFailures) is executed with no tenant_id constraint — it reads cross-tenant trace rows (tenant-scoped table) without scoping to a caller/admin tenant.
  - _Fix:_ If this query is intended to run per-tenant, add an explicit tenant_id filter (e.g. WHERE tenant_id = ${tenantId}) and pass tenantId into mineFailures. If it is intended as a platform-admin/global job, make that explicit (use an ADMIN_TENANT_ID constant, ensure only privileged admin runners execute it) and minimize the returned/used fields (avoid including raw error/log text) or copy required data into an admin-scoped store. Audit and document that this cross-tenant access is allowed and safe; otherwise restrict the query to a single tenant.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' scans a tenant-scoped table across all tenants with no tenant_id constraint, exposing tenant identifiers and department entries across tenants.
  - _Fix:_ If this sweep must examine all tenants, require it to run with an explicit platform-admin privilege and minimize sensitive data use (or read only the tenant ids from a dedicated admin table). Otherwise, restrict the query to a single tenant by adding WHERE tenant_id = ${tenantId} (and pass tenantId into runBudgetEnforcement), or implement a safe, audited cross-tenant mechanism that copies necessary data into an admin-only store prior to processing.
- **server/agentic/wake-scheduler.ts:79** — UPDATE on tenant-scoped table `agent_wake_schedules` performed without any tenant_id constraint (updates rows across all tenants). The inner subquery selecting ids also lacks a tenant filter.
  - _Fix:_ Constrain the claim/update to a specific tenant or iterate tenants. E.g. include tenant_id = <tenantId> in the WHERE of the inner SELECT/outer UPDATE (or run the claim per-tenant):
UPDATE agent_wake_schedules SET ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE tenant_id = ${tenantId} AND status='pending' AND wake_at<=now() ... ) RETURNING *
Or select pending ids per-tenant and operate on those to avoid cross-tenant updates.
- **server/auto-memorize.ts:159** — Global scan: SELECT from `messages` (joined to conversations) reads messages across all tenants (no tenant_id filter). This routine pulls user/assistant messages without tenant scoping, causing cross-tenant data access.
  - _Fix:_ Run the auto-memorize scan per-tenant (pass tenantId) and include tenant_id filtering on messages (e.g. WHERE m.tenant_id = ${tenantId}) or join to conversations and require c.tenant_id = ${tenantId}. If the intent is a platform-level scan, move to a properly authorized admin-only path and ensure any persisted artifacts are written to an admin/global table rather than tenant-scoped tables.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL — this UPDATE does not include tenant_id in the WHERE clause and may modify a conversation belonging to a different tenant.
  - _Fix:_ Include AND tenant_id = ${c.tenant_id} (or otherwise verify ownership within the same transaction) in the WHERE clause for the UPDATE. Always require tenant_id when updating tenant-scoped tables to prevent cross-tenant mutation.
- **server/chat-engine.ts:571** — SELECT from tenant-scoped table `project_files` uses only project_id and lacks an explicit tenant_id constraint in the WHERE clause. Relying on prior project validation is not sufficient per the invariant — every tenant-scoped read must include the tenant filter.
  - _Fix:_ Constrain the query to the caller tenant. e.g. add `AND tenant_id = ${tenantId}` to the WHERE clause (or use Drizzle `.where(eq(project_files.tenantId, tenantId))`). Alternatively, join via projects and assert p.tenant_id = tenantId in the same query before returning rows.
- **server/chat-engine.ts:582** — SELECT from tenant-scoped table `project_notes` uses only project_id and lacks an explicit tenant_id constraint in the WHERE clause.
  - _Fix:_ Add tenant scoping to the query: `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or Drizzle `.where(eq(project_notes.tenantId, tenantId))`). Ensure every access to project_notes includes the tenant filter.
- **server/chat-engine.ts:631** — SELECT from tenant-scoped table `compaction_archives` filters only by conversation_id and omits tenant_id in the WHERE clause, allowing potential cross-tenant access to archived conversation data.
  - _Fix:_ Include tenant scoping on compaction_archives queries, e.g. `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}` (or Drizzle `.where(eq(compaction_archives.tenantId, tenantId))`). If conversation ownership was validated earlier, still enforce tenant_id on every DB read per the invariant.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` filters only by conversation_id and lacks an explicit tenant_id constraint in the WHERE clause, risking exposure of message contents across tenants.
  - _Fix:_ Add tenant scoping to the messages query: `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}` (or use Drizzle `.where(eq(messages.tenantId, tenantId))`). All message reads must include the tenant_id filter even if conversation_id was previously validated.
- **server/data-protection.ts:73** — UPDATE on conversations performed without constraining by tenant_id. The code SELECTs the conversation with tenant filter but then issues UPDATE conversations SET ... WHERE id = ${conversationId} (no tenant_id predicate), allowing a TOCTOU or accidental cross-tenant modification.
  - _Fix:_ Include the tenant predicate on the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or perform the write inside a tenant-scoped transaction (withTenantTx) so the DB operation cannot affect other tenants. Avoid relying solely on a prior SELECT for authorization due to race conditions.
- **server/data-protection.ts:112** — SELECT on conversations that retrieves expired soft-deleted rows has no tenant_id constraint: SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}. This reads across all tenants and can expose or act on other tenants' data.
  - _Fix:_ Restrict the purge query to a single tenant at a time (e.g. add WHERE tenant_id = ${tenantId} and iterate tenants in a controlled/admin path) or require that this maintenance function runs under an elevated, audited admin context (e.g. withTenantTx/admin role) and document the admin-only nature. Prefer explicit tenant scoping when invoked from application code.
- **server/data-protection.ts:125** — DELETE FROM messages WHERE conversation_id = ${conv.id} is executed without a tenant_id constraint. Because the initial expired-rows SELECT lacked tenant scoping, the loop may delete messages across tenant boundaries (and even if conv rows were retrieved, writes should still be scoped to tenant_id to avoid TOCTOU issues).
  - _Fix:_ Include tenant scoping on deletes (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or perform the entire purge inside a tenant-scoped transaction (withTenantTx) so deletes are enforced for the intended tenant. Always include tenant_id on destructive statements.
- **server/data-protection.ts:126** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} is executed without tenant_id constraint, risking cross-tenant data deletion.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${conv.tenant_id}) or run the purge per-tenant within a tenant-scoped transaction. Ensure all deletes on tenant-scoped tables include tenant_id predicates.
- **server/data-protection.ts:127** — DELETE FROM conversations WHERE id = ${conv.id} is executed with no tenant_id predicate. This can delete conversations belonging to other tenants because the initial SELECT was not tenant-scoped and there is no tenant predicate on the DELETE (TOCTOU risk).
  - _Fix:_ Require AND tenant_id = ${conv.tenant_id} on the DELETE, or perform the purge inside an explicitly tenant-scoped transaction/role (withTenantTx or admin transaction) and/or require an explicit admin flag. Avoid destructive statements on tenant-scoped tables without tenant predicates.
- **server/delivery-pipeline.ts:188** — UPDATE against tenant-scoped table `delivery_logs` (function updateDeliveryLog) uses WHERE eq(deliveryLogs.id, id) with NO tenant_id constraint. This allows an update to affect another tenant's delivery row.
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog and include it in the WHERE clause, e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). If the caller is trusted, explicitly assert that trust at call sites rather than omitting tenant scoping.
- **server/delivery-pipeline.ts:720** — UPDATE on `delivery_logs` that NULLs stripePaymentId uses .where(eq(deliveryLogs.id, prior.id)) with NO tenant_id constraint — an attacker could cause writes to another tenant's row.
  - _Fix:_ Add tenant scoping to the UPDATE: .where(and(eq(deliveryLogs.id, prior.id), eq(deliveryLogs.tenantId, expectedTenantId))). Ensure expectedTenantId comes from the same trusted request context used to create/own the prior record.
- **server/embeddings.ts:684** — UPDATE into tenant-scoped table (memory_entries or agent_knowledge) performed without constraining by tenant_id (UPDATE ${sql.raw(table)} ... WHERE id = ${id}). This allows updating rows across tenants by id alone.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), or use Drizzle's .where(eq(table.tenantId, tenantId)). Keep the table allowlist but enforce per-row tenant ownership before any UPDATE.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint. This writes into a tenant-scoped table by id only, enabling cross-tenant mutation.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${row.tenant_id} or accept a tenantId and use WHERE id = ${row.id} AND tenant_id = ${tenantId}). Alternatively iterate per-tenant and enforce ownership checks before updating.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint. This mutates tenant-scoped rows by id only, risking cross-tenant writes.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or ensure the operation runs per-tenant and verifies ownership of each row before updating.
- **server/felix-brain.ts:19** — Boot-time rehydration queries agent_runs without any tenant_id filter (SELECT ... FROM agent_runs WHERE status IN ('running','pending') ...). This reads tenant-scoped rows across all tenants and loads per-conversation state into a global in-memory store, risking cross-tenant data exposure.
  - _Fix:_ Restrict the rehydration query to the intended tenant(s) (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant and only rebuild in-memory state for rows owned by that tenant. Additionally, validate ownership of each row before reconstructing global in-memory task state to prevent cross-tenant leakage.
- **server/lib/inbox-ingest.ts:444** — SELECT from tenant-scoped table `inbox_messages` lacks a tenant_id filter. The query `SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ON c.inbox_message_id = m.id WHERE m.message_id = ${id} AND c.id IS NULL LIMIT 1` can return a row belonging to another tenant, allowing a race/path that lets this runner claim/process another tenant's message under the admin tenant.
  - _Fix:_ Constrain the lookup to the caller's tenant and/or verify ownership before using the id. Example fix: add `AND m.tenant_id = ${tenantId}` to the WHERE clause (and keep `c.id IS NULL`). Ensure the resulting inbox_message id is only used when it belongs to the same tenant as the ingestion run. Also consider making the claim-and-route atomic (e.g. SELECT ... WHERE message_id=$1 AND tenant_id=$2 FOR UPDATE / or include the tenant_id in the subsequent INSERT ON CONFLICT guard) so concurrent runners cannot cross-claim another tenant's row.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only eq(tenantVoiceProfiles.id, existing.id) in the WHERE clause and does not also constrain by tenant_id. This can allow a caller to update another tenant's row if the id is known or reused.
  - _Fix:_ Include the tenant constraint in the WHERE clause for safety, e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Always ensure UPDATE/DELETE statements on tenant-scoped tables include tenant_id = callerTenant.
- **server/minerva-planner.ts:355** — CAS UPDATE on plans (UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING id, tenant_id, objective, plan_json) does NOT constrain by tenant_id. This allows an actor to change the status of a plan belonging to any tenant if they know the plan id.
  - _Fix:_ Include tenant_id = ${expectedTenantId} in the WHERE clause of the CAS UPDATE to ensure only the caller's tenant's plan can be modified (e.g. WHERE id = ${args.planId} AND tenant_id = ${rowTenantId} AND status = 'awaiting_approval'). Validate the caller's tenant before performing the CAS.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` omits tenant_id. The code checks ownership via assertProjectInTenant but still writes the row without setting tenant_id, violating the invariant that every INSERT must explicitly set tenant_id.
  - _Fix:_ Include the tenant_id column in the INSERT and pass the caller's tenant (e.g. ${'${options.tenantId}'}). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep the assertProjectInTenant check (good) and use a parameterized Drizzle/sql template to avoid injection.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` omits tenant_id. As above, assertProjectInTenant is called but the actual INSERT does not set tenant_id, breaking the tenant-isolation invariant.
  - _Fix:_ Add tenant_id to the INSERT and supply options.tenantId when inserting. For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ${safeTitle + ".mp4"}, ${concatResult.filePath}, ${driveUrl || null}, 'video', ${concatResult.sizeBytes || 0}, 'mpeg-engine-parallel'). Use parameterized sql (as already done) and keep the pre-insert ownership assertion.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions without tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This returns rows across all tenants (including tokens/refresh_tokens).
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or explicitly iterate per-tenant with a controlled admin-only flow. Do not scan oauth_subscriptions across tenants in a context that isn't intended and fully access-controlled.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id constraint — reads rows for all tenants.
  - _Fix:_ If you intend to target a particular tenant, add WHERE tenant_id = ${tenantId}. If this is an admin-only multi-tenant operation, ensure the code is hardened (explicit admin checks) and consider iterating an explicit tenant list rather than a blind cross-tenant query.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh: SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE reads all oauth_subscriptions across tenants (no tenant_id filter). This exposes/iterates tenant-scoped secrets and performs actions per-tenant without explicit per-tenant scoping in the query.
  - _Fix:_ Either restrict the query to a specific tenant or explicitly iterate a validated list of tenant IDs (with appropriate admin authorization) before operating. Avoid scanning tenant-scoped secret-bearing tables without deliberate multi-tenant admin controls.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE queries all tenants without limiting by tenant_id.
  - _Fix:_ If this is intended as a global admin job, make admin authorization explicit and ensure the code performing updates uses tenant_id in WHERE clauses. Otherwise, constrain by tenant_id or use a safe, explicit tenant iteration mechanism.
- **server/plan-executor.ts:513** — CAS claim query updates the plans row without constraining by tenant_id: `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...`. This allows the executor to claim/operate on plans belonging to any tenant by id.
  - _Fix:_ Enforce tenant-scoped filtering on the CAS: include `AND tenant_id = ${callerTenantId}` (or similar) in the WHERE clause, or first SELECT/RETURN the row with its tenant and validate the executor's tenant context before performing the UPDATE. Ensure executePlan receives an explicit tenant context or validates row. Avoid relying solely on plan id for authorization.
- **server/plan-executor.ts:725** — Final plan status update writes to plans with `WHERE id = ${planId}` and no tenant filter: `UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}`. This can flip status for plans across tenants.
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${planId} AND tenant_id = ${row.tenant_id}` (or the caller's tenant), or include an explicit tenant parameter to the function so the DB modification cannot affect other tenants' rows.
- **server/plan-executor.ts:775** — Boot-time recovery resets ALL stale 'executing' plans across tenants: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id`. No tenant scoping is applied.
  - _Fix:_ Run recovery per-tenant (loop tenants) or restrict the UPDATE to a specific system tenant if intended, e.g. `AND tenant_id = ${tenantId}`. If the operation must be global, add rigorous authorization and auditing, but preferred fix is to scope recovery to tenant contexts or to only operate on system-owned plans.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates a tenant-scoped table by id only, with no tenant_id constraint. That can modify another tenant's task with the same id.
  - _Fix:_ Always include tenant scoping on mutating statements (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}). Do not rely solely on the provenance of the id variable; enforce tenant_id in the WHERE clause.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} mutates a tenant-scoped table using only the id column and lacks a tenant_id constraint, allowing cross-tenant writes if ids collide.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise verify desk belongs to tenantId before updating) to enforce tenant isolation on writes.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance_rules without tenant scoping. This can increment counters on another tenant's rule with the same id.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or verify ownership before mutating governance_rules.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads the projects table by id with NO tenant filter or ownership check. projects is tenant-scoped; this allows reading another tenant's project.
  - _Fix:_ Require and pass a tenantId, or verify the caller's tenant owns the project before reading. Add WHERE id = ${projectId} AND tenant_id = ${tenantId} (or an equivalent ownership validation) before returning project data.
- **server/project-brain.ts:332** — Backfill SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 contains no tenant scoping. It enumerates projects, conversations and messages across all tenants.
  - _Fix:_ Scope the backfill to a specific tenant (pass tenantId) and add tenant filters to queries (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId} AND messages.tenant_id = ${tenantId}), or otherwise ensure the activity is limited to the caller's tenant.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is executed without tenant scoping or verifying that the project and conversations belong to the caller's tenant.
  - _Fix:_ Add tenant constraints (e.g. AND c.tenant_id = ${tenantId}) or join via a tenant-validated project row to ensure conversations read are owned by the tenant.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant scoping. messages is tenant-scoped and this reads potentially other tenants' messages.
  - _Fix:_ Include tenant_id in WHERE (e.g. conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or verify the conversation belongs to the tenant before selecting messages to prevent cross-tenant data access.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge SET ... WHERE id = ${r.id} — write to a tenant-scoped table with NO tenant_id constraint in the WHERE clause.
  - _Fix:_ Include tenant guard on the UPDATE, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id} (or otherwise validate the row belongs to the intended tenant before updating). Never perform writes to tenant-scoped tables keyed only by id without asserting tenant ownership.
- **server/routes.ts:4331** — SELECT from tenant-scoped table 'conversations' is missing a tenant_id constraint. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` does not restrict results to the caller's tenant, allowing cross-tenant data leakage.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add `AND tenant_id = ${tenantId}`) or, better, use a storage helper that requires and enforces tenantId (e.g. storage.getConversation(conversationId, tenantId)). Validate that conversationId belongs to tenant before using returned project_id.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The statement inserts (project_id, note, author) without explicitly setting tenant_id (project_notes rows are tenant-scoped and tenant_id is NOT NULL/no default).
  - _Fix:_ Include tenant_id in the INSERT (VALUES (${id}, ..., ${tenantId}) / INSERT INTO project_notes (project_id, tenant_id, note, author) ...). Validate the project ownership beforehand and pass the resolved tenantId as a parameter rather than relying on implicit FK behavior.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload handler) does not include tenant_id. The INSERT omits tenant_id and writes project_files rows with only project_id / file metadata.
  - _Fix:_ Include tenant_id when inserting project_files (INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (..., ${tenantId}, ...)). Ensure tenantId is the resolved, trusted tenant from the request and not derived from user input.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload handler) does not include tenant_id. The INSERT omits tenant_id and records file rows without an explicit tenant column value.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Also ensure the project ownership check is enforced and use the resolved tenantId as a parameter in the query.
- **server/sculptor.ts:94** — UPDATE to tenant-scoped table `sculptor_sessions` is performed without constraining by tenant_id (WHERE id = ${session.id} only). This allows an update to a session row belonging to a different tenant.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Prefer passing the caller's tenantId into the scope and always AND against sculptor_sessions.tenant_id for every UPDATE/DELETE against tenant-scoped tables.
- **server/sculptor.ts:101** — UPDATE to tenant-scoped table `sculptor_sessions` (setting conversation_id) lacks a tenant_id constraint (WHERE id = ${session.id} only).
  - _Fix:_ Add tenant scoping: WHERE id = ${session.id} AND tenant_id = ${params.tenantId} (or the appropriate tenantId variable). Ensure all writes to sculptor_sessions include AND tenant_id = <caller tenant>.
- **server/sculptor.ts:156** — UPDATE to tenant-scoped table `sculptor_sessions` in monitorSession writes status/result/tool_calls_count/tokens_used WHERE id = ${sessionId} without also constraining tenant_id. This can allow cross-tenant modification of session rows.
  - _Fix:_ Restrict the UPDATE to the caller's tenant: add AND tenant_id = ${tenantId} to the WHERE clause (and ensure tenantId is passed/trusted in this function). All modifications of tenant-scoped rows must include tenant_id checks.
- **server/sculptor.ts:347** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} does not include tenant_id in the WHERE clause, permitting changes to sessions belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Pass the trusted tenantId into the call site and use it in every write against sculptor_sessions.
- **server/second-opinion.ts:405** — settleFusionReservation issues an UPDATE on agent_cost_ledger WHERE id = ${reservationId} but does not constrain by tenant_id. agent_cost_ledger is tenant-scoped; updating by id alone can modify another tenant's ledger row.
  - _Fix:_ Require and pass the tenantId (or validate row ownership in the same transaction) into settleFusionReservation and add AND tenant_id = ${tenantId} to the WHERE clause. Alternatively, perform the UPDATE within a transaction that validates the reservation row belongs to the expected tenant before updating.
- **server/second-opinion.ts:425** — releaseFusionReservation updates agent_cost_ledger SET cost_usd='0' WHERE id = ${reservationId} without a tenant_id constraint. This may allow clearing reservations for other tenants.
  - _Fix:_ Accept/derive the reservation's tenantId and include AND tenant_id = ${tenantId} in the WHERE clause (or fetch and validate the row's tenant_id before updating). All UPDATEs/DELETEs on agent_cost_ledger must be tenant-scoped.
- **server/seed.ts:3212** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts into a (likely) tenant-scoped table without providing tenant_id. Per the codebase invariant, tenant_id must be explicitly set on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id}) ) or derive/validate tenant_id from the project/conversation (and verify ownership) before inserting. Do not create tenant-scoped rows without an explicit, validated tenant_id.
- **server/seed.ts:3204** — SELECT from tenant-scoped table 'conversations' (and correlated subquery against project_conversations) is executed without constraining to a tenant_id. This can return rows from all tenants.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. AND c.tenant_id = ${tenantId}) and ensure any referenced/joined tables (project_conversations, projects) are also filtered to the same tenant. Prefer using the application tenant variable (or parameterize tenantId) rather than a hardcoded value.
- **server/seed.ts:4497** — SELECT count(*) FROM heartbeat_tasks executed without a tenant_id filter; heartbeat_tasks is tenant-scoped. This reads cross-tenant state.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE tenant_id = ${tenantId} AND enabled = true AND type = 'delegation'), or run the cleanup per-tenant explicitly. Do not rely on global counts without tenant scoping.
- **server/seed.ts:4506** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' ... is executed without a tenant_id filter. This will disable tasks for all tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (or otherwise scope to the intended tenant) to the UPDATE so only the caller's tenant tasks are changed.
- **server/seed.ts:4517** — UPDATE heartbeat_tasks SET cron_expression = '0 * * * *' WHERE name = 'Self-Reflection' ... has no tenant_id constraint and will modify rows for all tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or perform a tenant-scoped UPDATE) so changes affect only the target tenant.
- **server/seed.ts:4526** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks a tenant filter; reads cross-tenant data.
  - _Fix:_ Add tenant filtering (e.g. AND tenant_id = ${tenantId}) or otherwise run per-tenant to avoid aggregating across tenants.
- **server/seed.ts:4533** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' ... executed without tenant_id constraint; will disable tasks across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so only the intended tenant's tasks are affected.
- **server/seed.ts:4540** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ...) is missing tenant scoping and can count tasks across tenants.
  - _Fix:_ Scope the SELECT by tenant (AND tenant_id = ${tenantId}) or iterate tenants explicitly to avoid cross-tenant effects.
- **server/seed.ts:4556** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true executes without tenant_id filter and will impact all tenants.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (or equivalent) so only the target tenant's rows are updated.
- **server/seed.ts:4407** — UPDATE memory_entries SET fact = REPLACE(...) WHERE deleted_at IS NULL AND fact LIKE ... executes without tenant scoping. memory_entries is tenant-scoped (indexes exist), so this will modify other tenants' data.
  - _Fix:_ Add a tenant filter (e.g. AND tenant_id = ${tenantId}) to the WHERE clause, or run the normalizer per-tenant to ensure only the intended tenant's rows are updated.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` updates by experiments.id only (where(eq(experiments.id, experiment.id))) with NO tenant_id constraint — allows modifying another tenant's row if id known.
  - _Fix:_ Constrain UPDATEs to the caller's tenant as well, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, <callerTenantId>))). Ensure the function has access to and uses the tenant id when updating rows.
- **server/self-improvement.ts:413** — UPDATE on tenant-scoped table `experiments` in the catch path uses only experiments.id in the WHERE (where(eq(experiments.id, experiment.id))) with NO tenant filter — cross-tenant write risk.
  - _Fix:_ Add the tenant constraint to this UPDATE as well (AND eq(experiments.tenantId, <callerTenantId>)). Prefer a single helper that updates experiment rows scoped to tenant to avoid missed conditions.
- **server/stability-watchdog.ts:211** — DELETE FROM `heartbeat_tasks` executes without any tenant_id constraint. This can delete tenant-scoped heartbeat tasks across all tenants (fail-open destructive operation).
  - _Fix:_ Restrict deletes to a specific tenant (add AND tenant_id = ${tenantId}) or perform the cleanup per-tenant. If the operation is intended to be global admin maintenance, require an explicit admin/maintenance flag and audit log, and consider a safer per-tenant iteration to avoid accidental cross-tenant deletions.
- **server/stability-watchdog.ts:225** — DELETE FROM `event_log` is executed without a tenant_id WHERE clause, removing event_log rows across all tenants. This is a cross-tenant destructive operation with high blast radius.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${tenantId}) or perform cleanup on a per-tenant basis. If global cleanup is intended, require an explicit admin/maintenance authorization and document why global deletes are safe; otherwise fail-closed by requiring tenant context.
- **server/storage.ts:249** — createConversation inserts into conversations without enforcing that tenantId is provided (values(data) may omit tenant_id). The schema requires tenant_id and there is no DB-level RLS/default to protect against missing tenant.
  - _Fix:_ Require/validate tenantId at the API boundary (assertValidTenantId(data.tenantId)) and pass tenantId explicitly into the INSERT. Fail closed (throw) if tenantId is missing or invalid.
- **server/storage.ts:504** — getAllMemoriesForBackup selects all memory_entries with no tenant filter and returns all tenants' memory entries — mass cross-tenant data exposure risk.
  - _Fix:_ Make this an explicit admin-only export that requires a validated admin tenantId parameter (and assert that caller is authorized), or document and protect it behind strong authorization. Prefer requiring a tenantId argument and scoping the SELECT by tenant_id to avoid accidental cross-tenant export.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` without a tenant_id constraint (db.delete(...).where(eq(customTools.id, tool.id))). Unscoped DELETEs can remove another tenant's data and are a serious tenant-isolation breach.
  - _Fix:_ Require and pass an explicit tenantId to revertToolRow (or read the tenantId from the tool row) and perform the delete with both id AND tenantId: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Add an assertion that the tool belongs to the expected tenant before deleting.
- **server/tools.ts:3198** — INSERT into `project_files` does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`. Tenant_id columns are required by policy and must be explicitly set on INSERT.
  - _Fix:_ Include the tenant_id column/value in the INSERT, e.g. `INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})`. Validate caller ownership before insert and prefer a storage-layer helper that always stamps tenant_id.
- **server/tools.ts:3212** — INSERT into `project_notes` omits tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (...)`. This can cause cross-tenant contamination because tenant_id is not written.
  - _Fix:_ Add tenant_id to the inserted columns and values, e.g. `INSERT INTO project_notes (project_id,note,author,tenant_id) VALUES (..., ${tenantId})`. Ensure all callers cannot bypass tenant stamping.
- **server/tools.ts:3236** — INSERT into `project_conversations` does not include tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)`. Creating rows without tenant_id can produce cross-tenant links and contamination.
  - _Fix:_ Include tenant_id in the INSERT, e.g. `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})`. Also ensure existing code that later reads project_conversations always filters by tenant_id.
- **server/tools.ts:4728** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT uses (project_id, note, author, created_at) but omits tenant_id, violating the rule that tenant_id must be explicitly set on tenant-scoped inserts.
  - _Fix:_ Include the tenant_id explicitly in the INSERT values (use the proven tenant context). E.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). If project_notes is multi-tenant, ensure the code always supplies and binds tenant_id rather than relying on inferred FK constraints.
- **server/tools.ts:4732** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT uses (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) but omits tenant_id.
  - _Fix:_ Add tenant_id to the INSERT. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Always pass tenant_id explicitly for tenant-scoped tables.
- **server/tools.ts:6579** — INSERT into tenant-scoped table 'project_files' (from generate_audio) omits tenant_id. The INSERT uses project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by but does not set tenant_id explicitly.
  - _Fix:_ Include the tenant id when inserting. For example: await db.execute(sql`INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${filename + '.' + ext}, ${outPath}, ${driveUrl || null}, ${'audio'}, ${audioBuffer.length}, ${'system'})`). Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly and validate ownership beforehand.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The UPDATE (SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id}) can modify rows across tenants if `id` is controlled or reused.
  - _Fix:_ Include the caller's tenant in the WHERE clause (e.g. `WHERE id = ${id} AND tenant_id = ${tid}`) so every UPDATE is explicitly scoped to the caller's tenant_id.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The batch UPDATE (SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids})) updates rows by id only, without ensuring those ids belong to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the tenant (for example: `WHERE id = ANY(${ids}) AND tenant_id = ${tid}`) or perform the update via a safe join/CTE that enforces tenant_id = ${tid} on each affected row.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The UPDATE (SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id}) does not include tenant_id in the WHERE clause, allowing cross-tenant modification if ids are mis-bound.
  - _Fix:_ Add an explicit tenant constraint to the WHERE clause (e.g. `WHERE id = ${id} AND tenant_id = ${tid}`) so the UPDATE cannot affect rows outside the caller's tenant.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without constraining tenant_id (reads pairing rows across all tenants). This enables cross-tenant information exposure and is the origin of the pairing-hijack threat described in comments.
  - _Fix:_ Avoid selecting raw agent_knowledge rows across all tenants. Either store/maintain an explicit tenant_id column on pairing rows and include WHERE tenant_id = ${tenantId}, or perform a DB-side join that validates the referenced conversation's tenant (e.g. join agent_knowledge -> conversations and filter on conversations.tenant_id = <claimed tenant>). At minimum, rewrite the lookup to only return rows for a specific tenant or to atomically validate ownership in SQL before returning rows to application logic.

### HIGH (174)

- **server/ab-optimizer.ts:110** — SELECT FROM tenant-scoped table `experiments` without restricting to a tenant (no tenant_id in WHERE). This reads experiment rows across all tenants.
  - _Fix:_ Restrict this query to a specific tenant (e.g. add WHERE tenant_id = ${someTenantId}) or iterate per-tenant. If the intent is to process only one tenant, accept a tenantId parameter and use it in the query.
- **server/agent-channels.ts:198** — JOIN on `channel_subscriptions` omits tenant scoping (cs.tenant_id not constrained). The query joins channel_subscriptions using channel_id and persona_id only, which may match rows from other tenants.
  - _Fix:_ Constrain the join to the tenant (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}), or otherwise ensure cs rows are filtered by tenant_id in the query.
- **server/agentic-engines.ts:114** — SELECT FROM `plans` omits tenant scoping (no tenant_id filter). This may read plans belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE source = ${ROUTE_SOURCE} AND source_ref = ${String(insight.insightId)} AND tenant_id = ${insight.tenantId}). Ensure routeInsightToMinerva is called with and uses the correct tenant context.
- **server/agentic-engines.ts:147** — UPDATE on tenant-scoped table `ai_insights` uses only id to target the row (no tenant_id constraint). This can update another tenant's row if ids collide or are misused.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId}). Ensure the tenantId is available and validated before issuing the update.
- **server/agentic-engines.ts:160** — SELECT FROM tenant-scoped table `ai_insights` without any tenant filter (durability sweep reads rows across all tenants).
  - _Fix:_ Run the sweep per-tenant or accept a tenantId parameter and add WHERE tenant_id = ${tenantId}. If multi-tenant sweeping is intended, ensure it's an explicit, audited operation and safe to touch all tenants.
- **server/agentic-engines.ts:342** — SELECT against `heartbeat_logs` omits tenant scoping (no tenant_id filter). This reads log data across all tenants.
  - _Fix:_ Constrain the query by tenant_id (e.g. add WHERE tenant_id = ${tenantId} ...) or otherwise limit scope to the intended tenant before querying heartbeat_logs.
- **server/agentic-engines.ts:448** — SELECT FROM `heartbeat_logs` (recentLogs) lacks tenant scoping (no tenant_id in WHERE).
  - _Fix:_ Add a tenant filter to the query (e.g. WHERE tenant_id = ${tenantId} AND status = 'success'). Ensure all heartbeat_logs reads are tenant-aware.
- **server/agentic-engines.ts:516** — SELECT aggregating `heartbeat_logs` (heartbeatPerformance) runs without tenant restriction (reads/aggregates across tenants).
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) to this performance aggregation or otherwise run aggregations per-tenant only.
- **server/agentic-engines.ts:530** — SELECT FROM `heartbeat_tasks` (taskConfig) omits tenant constraint (no tenant_id filter). This may reveal or mix tasks across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or accept tenant-scoped params) so heartbeat_tasks are read only for the intended tenant.
- **server/agentic/resume-worker.ts:66** — Cross-tenant SELECT from tenant-scoped tables without a tenant_id constraint. The candidate query selects from agent_runs JOIN agent_approvals with no WHERE tenant_id = <caller tenant>, allowing a background sweep to read rows for all tenants.
  - _Fix:_ Restrict the query to a specific tenant (WHERE r.tenant_id = ${tenantId}) or iterate per-tenant and run tenant-scoped queries. If this is intended as a system-global operation, document and enforce that it runs with an audited system account; otherwise add explicit tenant_id predicates and avoid returning rows from other tenants.
- **server/agentic/runs.ts:34** — UPDATE on agent_runs (appendStep) uses WHERE id = <runId> with NO tenant_id constraint. This allows modifying runs belonging to other tenants when called with an arbitrary runId.
  - _Fix:_ Require and pass the tenantId into appendStep and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Validate the caller owns the run before updating.
- **server/agentic/runs.ts:45** — UPDATE on agent_runs (updateRunState) uses WHERE id = <runId> with NO tenant_id constraint, allowing cross-tenant state mutation.
  - _Fix:_ Accept/require tenantId and add it to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers provide the tenant and ownership is verified before updating.
- **server/agentic/runs.ts:54** — UPDATE on agent_runs (replaceRunState) updates by run id only (WHERE id = <runId>) with NO tenant_id constraint, enabling cross-tenant updates.
  - _Fix:_ Add a tenantId parameter and include eq(agentRuns.tenantId, tenantId) in the WHERE clause to enforce per-tenant scoping before writing state.
- **server/agentic/runs.ts:58** — UPDATE on agent_runs (completeRun) uses WHERE id = <runId> with NO tenant_id constraint; this can mark runs for other tenants completed.
  - _Fix:_ Require tenantId and include it in the WHERE clause (AND tenant_id = ${tenantId}) so only the owning tenant's run is updated. Validate ownership prior to calling completeRun.
- **server/agentic/runs.ts:67** — UPDATE on agent_runs (failRun) uses WHERE id = <runId> with NO tenant_id constraint, allowing an attacker or bug to fail runs across tenants.
  - _Fix:_ Require tenantId and add eq(agentRuns.tenantId, tenantId) to the WHERE clause. Ensure callers cannot call failRun with a runId they don't own.
- **server/agentic/runs.ts:76** — UPDATE on agent_runs (pauseRun) updates runs by id only (WHERE id = <runId>) with NO tenant_id constraint, enabling cross-tenant status changes.
  - _Fix:_ Add tenantId to the function signature and include a tenant_id predicate in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Validate ownership before pausing a run.
- **server/auto-memorize.ts:47** — SELECT from `agent_knowledge` (watermark read) has no tenant_id constraint. This reads a tenant-scoped table without limiting to the intended tenant/namespace.
  - _Fix:_ Include tenant scoping when reading the watermark (or a dedicated global table if intentionally global). For example: SELECT content FROM agent_knowledge WHERE category='auto_memorize_watermark' AND title = ${WATERMARK_TITLE} AND tenant_id = ${ADMIN_TENANT_ID} ORDER BY id DESC LIMIT 1. If the watermark is meant to be global, store and read it from a genuine global table rather than a tenant-scoped table.
- **server/auto-memorize.ts:62** — UPDATE/INSERT/SELECT operations around setting the watermark read/modify `agent_knowledge` without scoping by tenant_id (SELECT id ... WHERE category=... title=... lacks tenant filter).
  - _Fix:_ When updating or selecting the existing watermark row, include tenant_id in the WHERE clause so the operation is unambiguous (e.g. WHERE category=... AND title=... AND tenant_id=${ADMIN_TENANT_ID}). If the watermark is intentionally bound to the admin tenant, consistently use that tenant id for both reads and writes.
- **server/auto-memorize.ts:100** — SELECT from `memory_entries` to detect recent facts (dedupe) lacks a tenant_id constraint (SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ...). This dedupes across all tenants.
  - _Fix:_ Restrict dedupe queries to the tenant in question (or accept a tenant parameter). Example: SELECT fact FROM memory_entries WHERE tenant_id = ${tenantId} AND created_at > NOW() - INTERVAL '60 days' ... . If dedupe must be global, move the function to an explicit global/admin path and document that behavior; otherwise enforce tenant scoping.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT conversations uses a correlated subquery COUNT(*) FROM messages WHERE conversation_id = c.id ... without a tenant_id constraint on messages. The subquery reads messages across tenants.
  - _Fix:_ Constrain the subquery to the conversation's tenant (e.g. COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id AND role='assistant') or otherwise include tenant_id to avoid cross-tenant reads.
- **server/auto-asset-capture.ts:207** — SELECT content FROM `messages` (by conversation_id) is executed without an explicit tenant_id filter. Although the conversation rows were enumerated earlier, the messages SELECT itself lacks a tenant constraint.
  - _Fix:_ Add tenant scoping to the messages read, for example: WHERE conversation_id = ${c.id} AND role = 'assistant' AND tenant_id = ${c.tenant_id}, or join messages to conversations and require conversations.tenant_id = ${expectedTenantId} to ensure the read is limited to the owning tenant.
- **server/auto-project.ts:112** — SELECT of messages for a conversation reads from `messages` without an explicit tenant_id constraint (WHERE conversation_id = ${conversationId} ...). The query relies on conversation_id but does not enforce tenant_id equality in the WHERE clause.
  - _Fix:_ Add tenant scoping to the messages query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or join messages to conversations and include c.tenant_id = ${tenantId} so message reads cannot cross tenant boundaries even if conversation IDs are manipulated.
- **server/auto-transcript.ts:128** — SELECT from tenant-scoped table `conversations` is executed without constraining by tenant_id in the WHERE clause. This allows the backfill job to read conversation rows across all tenants.
  - _Fix:_ Constrain the query to a single tenant (e.g. add WHERE c.tenant_id = ${tenantId}) or explicitly iterate tenants from a trusted tenant list. If the intent is to process per-tenant, fetch tenants first and run the SELECT per-tenant so every conversations read includes a tenant_id filter.
- **server/auto-transcript.ts:153** — SELECT joining project_conversations / conversations / projects runs without a tenant_id WHERE filter. It reads tenant-scoped rows (conversations/projects) with no tenant constraint.
  - _Fix:_ Add an explicit tenant constraint (e.g. WHERE c.tenant_id = ${tenantId}) or reorganize to enumerate tenants and run the join per-tenant. Ensure any derived c.project_id/c.tenant_id are validated against the acting tenant before further processing.
- **server/auto-transcript.ts:171** — SELECT from tenant-scoped table `conversations` (missingLinks query) is executed without a tenant_id WHERE filter. The query enumerates conversations across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE c.tenant_id = ${tenantId}) or iterate per-tenant. If this is a global backfill, make the intention explicit and perform per-tenant operations while passing/validating tenant_id for every read/write against tenant-scoped tables.
- **server/business-tools.ts:334** — Listing contracts JOINs to customers (cust) to surface cust.company_name but does not constrain the joined customers row to the caller's tenant. The query filters c.tenant_id = ${tid} but the LEFT JOIN ON cust.id = c.customer_id lacks AND cust.tenant_id = ${tid}, so a customer row from another tenant could be read via matching id.
  - _Fix:_ Restrict the joined customer row to the caller's tenant. e.g. change the JOIN/WHERE to ensure cust.tenant_id = ${tid} (or join on both id and tenant_id), or fetch the customer row separately and verify its tenant_id before returning company_name.
- **server/ceo-orchestrator.ts:821** — Unrestricted SELECT from personas table: cachedPersonas = await db.select().from(personasTable) reads all persona rows with no tenant_id constraint. This exposes/uses persona rows across tenants.
  - _Fix:_ Limit the query to the plan owner’s tenant. Only select personas for plan.tenantId (e.g. db.select().from(personasTable).where(eq(personasTable.tenantId, plan.tenantId)) or otherwise filter by tenantId). Cache only tenant-scoped results to avoid cross-tenant leakage.
- **server/channel-routing.ts:54** — setChannelRoute inserts/updates a global channel_routes row using a caller-supplied personaId with no ownership check. This allows a caller to register a persona_id they do not own (cross-tenant reference) into a global routing table.
  - _Fix:_ Validate that the provided personaId belongs to the caller's tenant before inserting/updating (SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = <callerTenant>). Better: make channel_routes tenant-scoped (add tenant_id column) and always insert with the caller's tenant_id so routes cannot reference personas from other tenants.
- **server/chat-engine.ts:4591** — INSERT into a likely tenant-scoped table (createMemoryEntry) is performed without passing a tenant identifier. extractMemory(...) does not accept or propagate tenantId and calls storage.createMemoryEntry({...}) with no tenantId, so the DB insert may be executed without constraining tenant_id to the caller’s tenant.
  - _Fix:_ Propagate and enforce tenant context: add a tenantId parameter to extractMemory(...) (and callers), and pass tenantId to storage.createMemoryEntry (e.g. storage.createMemoryEntry({ ..., tenantId }) ). Ensure any follow-up writes/updates (e.g. storage.updateMemoryEmbedding) either accept/verify tenantId or validate the created entry belongs to the tenant before updating. All inserts/updates against tenant-scoped tables must include a WHERE tenant_id = <caller tenant> (or equivalent Drizzle .where(eq(..., tenantId))) and validate ownership of any referenced personaId/entry id.
- **server/compaction.ts:463** — INSERT into tenant-scoped table `memory_entries` uses the passed-in tenantId value without validating it — the function accepts tenantId as optional and may insert NULL or an incorrect tenant_id.
  - _Fix:_ Validate tenantId before inserting (require a non-null, positive integer). Fail closed if tenantId is missing. Always pass an explicit, trusted tenantId value into the INSERT (e.g. VALUES (..., ${tenantId})) and reject the operation if tenantId is invalid to avoid writes with NULL or wrong ownership.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in the WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — missing tenant constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant to enforce isolation: add AND tenant_id = ${tenantId} to the WHERE clause (or derive tenantId from prior ownership-checked row and include it). E.g. UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in the WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — missing tenant constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant to enforce isolation: add AND tenant_id = ${tenantId} to the WHERE clause (or use the tenantId validated by getCredential/getCredentialForSite). E.g. UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}.
- **server/data-protection.ts:145** — verifyMessageSaved queries messages by conversation_id without any tenant context: SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} ... This function accepts only a conversationId and does not verify the caller's tenant owns that conversation, so it may leak existence information across tenants.
  - _Fix:_ Require and pass tenantId into verifyMessageSaved and include AND tenant_id = ${tenantId} in the WHERE clause, or first verify the conversation belongs to the caller's tenant (SELECT conversation with tenant check) before querying messages. Ensure every read against tenant-scoped tables is constrained by tenant_id or validated ownership.
- **server/delivery-pipeline.ts:682** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId (stripePaymentId lookup) without scoping by tenant_id. This can return another tenant's delivery and break idempotency / leak information.
  - _Fix:_ Include an explicit tenant filter (eq(deliveryLogs.tenantId, tenantId)) when looking up by stripePaymentId, or require callers to pass tenantId and enforce it in the query.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment performs SELECT FROM `delivery_logs` WHERE stripePaymentId = ... with NO tenant_id filter — cross-tenant leakage possible.
  - _Fix:_ Require tenantId on this lookup or add tenant filtering to the WHERE clause so stripePaymentId lookups are tenant-scoped.
- **server/delivery-pipeline.ts:952** — retryDelivery builds a WHERE clause that falls back to eq(deliveryLogs.id, deliveryId) when tenantId is omitted, causing an unscoped SELECT on tenant-scoped `delivery_logs` for callers that do not pass tenantId.
  - _Fix:_ Do not allow an omitted tenantId for tenant-scoped operations. Either require tenantId param or explicitly verify caller privilege before performing an unscoped lookup. Prefer .where(and(eq(deliveryLogs.id, deliveryId), eq(deliveryLogs.tenantId, tenantId))).
- **server/delivery-pipeline.ts:976** — getDeliveryStatus uses eq(deliveryLogs.id, deliveryId) when tenantId is omitted — performing an unscoped SELECT on tenant-scoped `delivery_logs`.
  - _Fix:_ Always scope to tenant_id. Require tenantId or check caller privileges, and use .where(and(eq(deliveryLogs.id, deliveryId), eq(deliveryLogs.tenantId, tenantId))).
- **server/delivery-pipeline.ts:984** — listDeliveries constructs a base SELECT from `delivery_logs` and only applies tenant filtering when tenantId != null. If tenantId is omitted the query is unscoped and will return other tenants' deliveries.
  - _Fix:_ Require tenantId for listing tenant-scoped resources or otherwise restrict non-tenant callers to admin paths. Always include eq(deliveryLogs.tenantId, tenantId) in the query for tenant-scoped listing.
- **server/delivery-pipeline.ts:998** — getDeliveryStats selects all rows from `delivery_logs` when tenantId is omitted, then does JS-level aggregation — this returns global/other-tenant stats when tenantId is not passed.
  - _Fix:_ Require tenantId or include tenant_id in the SQL WHERE clause. Avoid pulling whole table into memory; instead perform tenant-scoped aggregate SQL queries (COUNT ... WHERE tenant_id = ...).
- **server/doc-collections.ts:165** — DELETE FROM `doc_chunks` in addDocument uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but does NOT include tenant_id. This is a DELETE against a tenant-scoped table without explicit tenant scoping in the WHERE clause.
  - _Fix:_ Include tenant_id in the DELETE predicate (AND tenant_id = ${tenantId}) or otherwise ensure the deletion is provably scoped to the tenant (e.g. join on collection row owned by tenant and use tenant filter). Prefer explicit tenant filter in every tenant-scoped statement.
- **server/email.ts:365** — In inbox polling, SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without tenant_id filtering. inbox_messages is tenant-scoped and this read can return rows belonging to other tenants.
  - _Fix:_ Scope existence checks by tenant (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or query by inbox_id + message_id (both of which should determine tenant) so the query always includes tenant scoping.
- **server/email.ts:410** — backfillEmptyBodies executes SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id with NO tenant_id constraint — an unscoped read on a tenant-scoped table.
  - _Fix:_ Run backfill per-tenant (require tenantId) or include tenant_id = ${tenantId} in the WHERE clause. Avoid unscoped scans of tenant-scoped tables.
- **server/email.ts:424** — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} is executed without tenant_id in the WHERE clause, allowing an update to affect another tenant's row if id collisions occur or if row.id belongs to a different tenant.
  - _Fix:_ Include tenant_id in the UPDATE predicate (AND tenant_id = ${tenantId}) or perform the update via a SELECT that confirms tenant ownership first. Always constrain writes to tenant-scoped tables by tenant_id.
- **server/embeddings.ts:716** — SELECT FROM memory_entries (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200) is done with no tenant_id filter. Reading tenant-scoped rows without tenant constraint can leak other tenants' data.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or perform backfill per-tenant after enumerating tenants, ensuring only rows belonging to the intended tenant are processed.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200) is done with no tenant_id filter. Reading tenant-scoped rows without tenant constraint can leak other tenants' data.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or perform backfill per-tenant and verify ownership before acting on rows.
- **server/embeddings.ts:688** — SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL AND source = 'autoresearch' ... is executed with no tenant_id filter. This reads tenant-scoped data across all tenants.
  - _Fix:_ Add a tenant_id filter to the query (WHERE tenant_id = ${tenantId} AND ...) or otherwise restrict/backfill per-tenant to avoid cross-tenant data access.
- **server/embeddings.ts:244** — Use of sql.raw() with a string-interpolated vector literal built from the caller's query: sql.raw(`'${vec}'::vector`). The vector literal is constructed from request-derived input (via generateEmbedding(query)) and injected into raw SQL via string interpolation.
  - _Fix:_ Avoid sql.raw interpolation of values derived from caller input. Prefer parameterized binding (pass the vector as a parameter) or use Drizzle-safe APIs to bind the value, and/or persist embeddings via properly parameterized statements. If raw literal construction is unavoidable, ensure strict validation and clearly document why it cannot be parameterized.
- **server/embeddings.ts:559** — Use of sql.raw() with a string-interpolated vector literal built from the caller's query: sql.raw(`'${vec}'::vector`) in vectorSearchKnowledge. This injects a request-derived value into raw SQL.
  - _Fix:_ Replace sql.raw interpolation with a parameterized binding approach (avoid embedding user-derived strings into raw SQL); validate and bind the vector safely or use a Drizzle-supported mechanism for vector parameters to eliminate interpolation/injection risk.
- **server/firecrawl.ts:192** — UPDATE on scrapedPages uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint. This allows updating another tenant's row if an id from a different tenant is used or if race conditions occur.
  - _Fix:_ Constrain the UPDATE by tenant as well. E.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or use the same tenant-scoped query path that found the row. Always include tenantId in the WHERE for tenant-scoped tables.
- **server/firecrawl.ts:316** — UPDATE on scrapedPages inside the crawl loop uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint.
  - _Fix:_ Add an explicit tenant_id constraint to the WHERE clause (e.g. and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Do not rely solely on a prior lookup — every write must include the caller's tenant scoping.
- **server/google-drive.ts:547** — SELECT from projects queries the project by id (where eq(projects.id, projectId)) without scoping the read to the caller's tenant (no WHERE projects.tenant_id = tenantId). Reading an arbitrary project id can leak another tenant's data and violates the tenant-isolation invariant.
  - _Fix:_ Include an explicit tenant filter in the SELECT (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise use a helper that asserts ownership and performs a tenant-scoped query. Every read of a tenant-scoped table must include the tenant constraint.
- **server/google-drive.ts:560** — UPDATE projects sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) with no tenant_id constraint. Without an explicit tenant filter this UPDATE could modify another tenant's project record.
  - _Fix:_ Add the tenant constraint to the UPDATE WHERE clause, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Enforce tenant-scoped WHERE clauses on all updates to tenant-scoped tables.
- **server/google-drive.ts:829** — SELECT to load a project in uploadAndShare uses where(eq(projects.id, params.projectId)) with no tenant_id constraint. This reads a project row by id without scoping to the caller's tenant (even though ownership is checked after the read).
  - _Fix:_ Perform the read as tenant-scoped (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the database query itself is constrained. Do not rely solely on post-read checks — include tenant_id in the WHERE.
- **server/google-drive.ts:1004** — searchDriveFiles: early ownership verification SELECT reads projects by id via where(eq(projects.id, params.projectId)) without also restricting to tenant_id. The query itself is not tenant-scoped.
  - _Fix:_ Include the tenant constraint in the WHERE (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or use a dedicated assertProjectInTenant helper that issues a tenant-scoped query. Every read must be scoped to the caller's tenant.
- **server/google-drive.ts:1066** — searchDriveFiles pass-2 Drive fallback selects projects.driveFolderId with where(eq(projects.id, params.projectId)) and no tenant_id constraint. This read can return another tenant's folder id if projectId is untrusted.
  - _Fix:_ Restrict the SELECT by tenant, e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the DB query is always scoped to the caller's tenant before using returned values.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` with no tenant_id constraint — reads stale proposals across all tenants
  - _Fix:_ If this is intended to act per-tenant, add a WHERE tenant_id = ${tenantId} filter or accept an explicit admin-scoped guard. Otherwise, run this job under a verified admin context and/or include tenant_id in the SELECT to avoid cross-tenant data exposure.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` (archiving stale proposals) with no tenant_id constraint — may modify rows for other tenants
  - _Fix:_ Restrict the UPDATE to the intended tenant(s), e.g. UPDATE code_proposals SET ... WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}, or validate ownership of each id before mutating. If this is intended to be global admin action, require an explicit admin-only code path and audit/log it.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images does not set tenant_id and does not verify that the session_id belongs to the caller's tenant — blind write to a tenant-scoped resource
  - _Fix:_ Either include tenant_id as an explicit column in the INSERT, or (preferably) validate ownership by querying presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId} before inserting. Ensure every INSERT into tenant-scoped tables sets tenant_id from the trusted execution context, not from untrusted inputs.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks tenant_id constraint — may update another tenant's session if sessionId is attacker-controlled
  - _Fix:_ Before updating, verify the session belongs to the current tenant (SELECT id FROM presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}). For any cross-table foreign id (session_id), always validate ownership prior to reads/writes.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table "research_schedules" without constraining tenant_id (no WHERE tenant_id = ...). This reads schedules across all tenants in raw SQL.
  - _Fix:_ Filter by tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant after obtaining an asserted tenant context. Prefer the storage layer helper that enforces tenant scoping rather than raw DB queries across tenants.
- **server/heartbeat.ts:101** — UPDATEs against tenant-scoped table "research_schedules" use WHERE id = ${sched.id} but do not include tenant_id in the WHERE clause (other similar updates at lines 106, 112, 120). This allows updating rows across tenants or otherwise bypassing application-level tenant isolation.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or route these writes through a storage helper that asserts tenant ownership (assertTenantContext) before updating.
- **server/heartbeat.ts:257** — Direct UPDATE on tenant-scoped table "heartbeat_tasks" (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}) without tenant_id in the WHERE clause. This occurs at multiple startup-fix points (e.g. lines 257, 261) and can modify another tenant's row if ids overlap or if row ownership is not enforced by DB.
  - _Fix:_ Use the storage API that requires/validates tenantId or include tenant_id in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}). Prefer assertTenantContext(task.tenantId, ...) before issuing direct SQL.
- **server/heartbeat.ts:588** — SELECT from tenant-scoped table "event_log" (SELECT * FROM event_log WHERE status = 'pending' ...) with no tenant_id constraint. This pulls pending events across tenants into a single processing loop without an asserted tenant context.
  - _Fix:_ Either run the query per-tenant (using a known tenantId/assertTenantContext) or add tenant_id = ${tenantId} to the WHERE clause. If the intent is to process events across tenants, explicitly iterate active tenant IDs and assert tenant context for each tenant before selecting its events.
- **server/heartbeat.ts:1646** — SELECT from tenant-scoped table "heartbeat_logs" (SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ...) without tenant_id filtering. This reads logs across tenants and can leak timing/state between tenants.
  - _Fix:_ Constrain the query to the task's tenant (e.g. WHERE task_name = ... AND tenant_id = ${tenantId} ...) or use a storage helper that takes tenantId and enforces the binding via assertTenantContext.
- **server/heartbeat.ts:2955** — Direct UPDATE on tenant-scoped table "heartbeat_tasks" to set approval_status (UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}) without tenant_id in WHERE. Same pattern also used at line 3407. Writes may affect rows outside the caller's tenant context.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use storage.updateHeartbeatTask/newTask wrapper that asserts tenant ownership before updating. Prefer single-source-of-truth tenant assertion via assertTenantContext.
- **server/heartbeat.ts:2609** — Call to storage.getPersonas() without passing tenantId inside buildTaskContext. storage.getPersonas likely returns tenant-scoped personas; calling it without tenant scoping can leak personas from other tenants into the task context.
  - _Fix:_ Call storage.getPersonas(tenantId) or otherwise ensure the storage function enforces/accepts a tenantId. Always use the hoisted/validated tenantId (obtained via assertTenantContext) when reading tenant-scoped rows.
- **server/heartbeat.ts:2726** — Call to storage.getPersonas() without passing tenantId inside processTaskOutput (used to find "Proof" persona). This can read personas across tenants and allow cross-tenant delegation or data leakage.
  - _Fix:_ Pass tenantId into storage.getPersonas (e.g. storage.getPersonas(tenantId)) or ensure the storage layer enforces tenant-scoping. Verify persona ownership before creating tasks for that persona.
- **server/heartbeat.ts:2861** — Call to storage.getPersonas() without passing tenantId inside processDelegations. Delegation logic uses persona lookup/validation but does not verify persona ownership by tenant, risking delegating tasks to personas from other tenants.
  - _Fix:_ Use storage.getPersonas(tenantId) or otherwise restrict persona list to the asserted tenant. Always validate ownership (assertTenantContext) of any persona or resource before using it.
- **server/heartbeat.ts:3053** — delegateTaskFromChat calls storage.getPersonas() without passing tenantId even though tenantId is provided to the function. This reads personas across tenants and then uses the result to resolve target persona, risking cross-tenant impersonation/leak.
  - _Fix:_ Call storage.getPersonas(tenantId) or have the storage API enforce tenant scoping. Before acting on a persona row, verify it belongs to the caller's tenant (assertTenantContext/explicit WHERE tenant_id = ...).
- **server/internal-resolver.ts:91** — UPDATE on tenant-scoped table `event_log` (finalizeEvent) is executed without any tenant_id constraint. This can allow a resolver to finalize/modify events belonging to other tenants.
  - _Fix:_ Include tenant scoping in the CAS update (e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId} AND status IN (...)), or read the event first to determine / validate its tenant and then update only that tenant's row. Add a tenantId parameter to finalizeEvent or otherwise assert ownership before mutating event_log.
- **server/internal-resolver.ts:106** — SELECT from `plans` by id (resolvePlanProposed) is performed with no tenant_id filter. The code may read and act on a plan belonging to a different tenant.
  - _Fix:_ Constrain the lookup to the caller/expected tenant (e.g. SELECT ... FROM plans WHERE id = ${planId} AND tenant_id = ${tenantId} LIMIT 1). If the resolver runs as a global process, explicitly fetch the plan row first and verify its tenant_id before taking any action.
- **server/internal-resolver.ts:170** — UPDATE on tenant-scoped table `code_proposals` (resolveExperimentFailed) uses WHERE id = ${proposalId} AND status = 'pending' with no tenant_id check. This may archive or modify another tenant's proposal.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the proposal belongs to the tenant referenced by the event before updating. Fetch and verify the row's tenant_id first or accept a tenantId parameter.
- **server/job-queue.ts:98** — claimDueJobs performs a global UPDATE/SELECT on `agent_jobs` without scoping by tenant_id. This atomically claims jobs across all tenants, which can lead to cross-tenant execution or races with tenant-specific expectations.
  - _Fix:_ If worker instances should only process a single tenant, add a tenant_id predicate to the inner SELECT/outer UPDATE. Otherwise, ensure any tenant-sensitive work executed by a claimed job verifies job.tenant_id before accessing tenant-scoped resources and that billing/tenant-context is set from the job row (not from payload). Document that claiming is intentionally global if that's expected.
- **server/job-queue.ts:131** — reclaimExpiredLeases updates `agent_jobs` across all tenants (two global UPDATEs) with no tenant_id restriction. This mutates tenant-scoped rows for all tenants in one statement.
  - _Fix:_ Restrict reclaim operations to the intended tenant when called from a tenant-scoped caller, or else make it explicit that this is a global maintenance operation and ensure any subsequent processing enforces tenant boundaries. Consider iterating per-tenant or including tenant_id = ${tenantId} when invoked on behalf of a specific tenant.
- **server/job-worker.ts:146** — Handler `research_code_proposal` reconstructs a session from job.payload including tenantId and passes it to generateCodeProposal without verifying it matches job.tenantId. Using tenantId from an untrusted payload risks acting on another tenant's resources.
  - _Fix:_ Do not trust tenant identifiers embedded in job payloads. Use the tenantId stored in the agent_jobs row (job.tenantId) as the authoritative tenant, or validate payload.tenantId === job.tenantId before proceeding. Ensure any DB reads/writes inside generateCodeProposal use the job's tenant context (e.g. via withTenantContext) based on job.tenantId.
- **server/job-worker.ts:167** — Handler `research_digest` reads tenantId from job.payload and passes it to generateResearchDigest without verifying ownership (no check against job.tenantId). This can cause cross-tenant reads/writes if payload is tampered with.
  - _Fix:_ Use job.tenantId as the authoritative tenant when executing tenant-scoped operations, or validate that payload.tenantId matches job.tenantId. Run the handler inside a tenant context derived from job.tenantId (e.g. withTenantContext) rather than trusting payload values.
- **server/lib/action-ledger.ts:305** — UPDATE on tenant-scoped table `action_attempts` without tenant_id constraint (sweepStaleAttempts updates all matching rows across tenants).
  - _Fix:_ Do not run unscoped UPDATEs against tenant-scoped tables. Either iterate per-tenant (query tenant ids then run tenant-scoped transition/UPDATE with WHERE tenant_id = ... AND id = ...) or add tenant_id to the WHERE clause. If the reconciler must operate cross-tenant, restrict execution to a privileged/isolated maintenance context and add additional auditing and authorization checks.
- **server/lib/bwb-job-progress.ts:144** — UPDATE on tenant-scoped table `video_jobs` performed via scope(jobId, ...) where scope may omit tenant predicate if BWB_TENANT_ID/threading is missing (setBwbPhase). This can cause cross-tenant writes.
  - _Fix:_ Require an explicit tenantId for every writer path (do not silently fall back to jobId-only writes). Make envTenantId() return null an error for mutators, or fail the operation when tenantId is not present. Ensure the WHERE clause always includes eq(videoJobs.tenantId, tenantId).
- **server/lib/bwb-job-progress.ts:169** — UPDATE on tenant-scoped table `video_jobs` performed via scope(jobId, ...) where scope may omit tenant predicate if BWB_TENANT_ID/threading is missing (updateBwbChapters).
  - _Fix:_ Require an explicit tenantId for every writer path (do not silently fall back to jobId-only writes). Make envTenantId() return null an error for mutators, or fail the operation when tenantId is not present. Ensure the WHERE clause always includes eq(videoJobs.tenantId, tenantId).
- **server/lib/bwb-job-progress.ts:203** — SELECT from tenant-scoped table `video_jobs` performed via scope(jobId) where scope may omit tenant predicate if BWB_TENANT_ID/threading is missing (completeBwbJob reads current row).
  - _Fix:_ Ensure reader contexts for tenant-scoped tables are always passed an explicit tenantId and that scope() always includes eq(videoJobs.tenantId, tenantId). If this read is intended global maintenance, require a privileged context and be explicit in the API surface.
- **server/lib/bwb-job-progress.ts:211** — UPDATE on tenant-scoped table `video_jobs` performed via scope(jobId) where scope may omit tenant predicate if BWB_TENANT_ID/threading is missing (completeBwbJob writes final state).
  - _Fix:_ Require an explicit tenantId for mutating operations; never rely on a silently-omitted tenant predicate. Add assert/guard to fail fast when envTenantId() is missing, or include tenant_id in the WHERE clause explicitly.
- **server/lib/bwb-job-progress.ts:230** — UPDATE on tenant-scoped table `video_jobs` performed via scope(jobId, { notDone: true, notFailed: true }) where scope may omit tenant predicate if BWB_TENANT_ID/threading is missing (bumpBwbHeartbeat).
  - _Fix:_ Ensure the heartbeat writer always runs with an explicit tenant id. Do not allow a quiet fallback to jobId-only scoping; require and enforce eq(videoJobs.tenantId, tenantId) in the WHERE clause or fail the operation when tenant is not available.
- **server/lib/bwb-job-progress.ts:254** — UPDATE on tenant-scoped table `video_jobs` performed via scope(jobId, { notDone: true }) where scope may omit tenant predicate if BWB_TENANT_ID/threading is missing (failBwbJob).
  - _Fix:_ Require and validate tenantId for all mutating calls. Modify scope() to fail or throw when tenant is unknown for mutators, or explicit pass-through the tenantId so WHERE always contains eq(videoJobs.tenantId, tenantId).
- **server/lib/scheduled-post-runner.ts:266** — Manual construction of a Postgres array literal (arrLit) from request-derived platforms (params.platforms) via string concatenation. Building DB-literal strings from user input risks injection or malformed array literals and bypasses safe parameterization.
  - _Fix:_ Do not build SQL array literals by hand from user input. Use parameterized array binding or JSONB storage. For example, pass the platforms array as a parameter and cast to text[] in the SQL (pg supports binding JS arrays to text[]), or store platforms as jsonb: VALUES (${params.tenantId}, ${params.platforms}::text[], ... ) using a parameterized driver call, or use unnest/unnest($1::text[]) patterns. Ensure any transformation/escaping is avoided and let the DB driver perform safe parameterization.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is executed without constraining to a caller tenant (no WHERE tenant_id = <tenant>), allowing cross-tenant data disclosure.
  - _Fix:_ Add an explicit tenant filter (or accept a tenantId param) to the query, e.g. WHERE tenant_id = ${tenantId} AND status = 'running' ...; or document/guard that this admin-only audit intentionally queries all tenants and restrict access to admins only.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` is executed without constraining to a caller tenant (no WHERE tenant_id = <tenant>), allowing cross-tenant data disclosure.
  - _Fix:_ Add an explicit tenant filter (or accept a tenantId param) to the query, e.g. WHERE tenant_id = ${tenantId} AND status = 'in_progress' ...; or make the function require/admin-scope and clearly gate it to avoid accidental multi-tenant reads.
- **server/memory-graph.ts:66** — UPDATE against tenant-scoped table memory_categories without tenant_id constraint (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This can modify another tenant's taxonomy/counts.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or perform the update within a tenant-scoped transaction so the DB call is parameterized by the tenant context.
- **server/memory-graph.ts:125** — UPDATE memory_categories uses a subquery that counts memory_entries but neither the outer UPDATE nor the inner SELECT constrain by tenant_id (risk of mixing counts across tenants).
  - _Fix:_ Filter the inner SELECT by tenant_id and constrain the UPDATE to the tenant (e.g. SET ... WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Ensure the tenantId used is the intended caller's tenant.
- **server/memory-graph.ts:181** — SELECT from memory_links to check existence of a link lacks tenant scoping (SELECT 1 FROM memory_links WHERE ...). If memory_links is tenant-scoped this can read other tenants' relations.
  - _Fix:_ Constrain the SELECT by tenant (e.g. AND tenant_id = ${tenantId}) or join via a tenant-scoped memory_entries row to ensure the link belongs to the caller's tenant.
- **server/memory-graph.ts:195** — INSERT INTO memory_links does not set tenant_id. If memory_links is a tenant-scoped table this creates rows with no tenant binding (or defaults), allowing cross-tenant link pollution.
  - _Fix:_ Include tenant_id explicitly in the INSERT VALUES (e.g. VALUES (..., ${tenantId})) and validate the referenced memory ids belong to that tenant before inserting.
- **server/memory-graph.ts:393** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is missing a tenant_id filter. Although the SELECT that produced mem was tenant-scoped, the UPDATE can affect an entry with the same id in another tenant.
  - _Fix:_ Always include tenant_id in the WHERE clause for updates (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or perform the write within a tenant-scoped transaction/tx that enforces the tenant filter.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in async callback) lacks tenant scoping. This update can modify rows belonging to other tenants if ids collide.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or use a storage helper that enforces tenant ownership for the id before updating.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in another async path) is missing tenant filtering, risking cross-tenant modification.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or use tenant-aware storage APIs to perform the update after verifying ownership.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} lacks a tenant_id constraint. The initial SELECT of contradictions filtered by tenant_id, but the UPDATE can still affect other tenants' rows if ids collide.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) to ensure only the intended tenant's triple is modified.
- **server/minerva-planner.ts:373** — Probe SELECT (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}) lacks tenant scoping. It can disclose or act on another tenant's plan state.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause when probing plan state, or require tenantId input and validate ownership before probing.
- **server/minerva-planner.ts:391** — Child-plan lookup (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1) does not constrain by tenant_id; it can return a child plan from another tenant.
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}) so only child plans of the same tenant are considered.
- **server/minerva-planner.ts:460** — Rollback UPDATE on plans (UPDATE plans SET status = 'awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising') lacks tenant scoping. This can roll back a plan in another tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause when reverting decision metadata so only the intended tenant's plan is modified.
- **server/oauth-subscriptions.ts:956** — UPDATE on oauth_subscriptions uses WHERE id = ${sub.id} without also constraining tenant_id. Writing to a tenant-scoped table without tenant_id increases risk of modifying another tenant's row if IDs collide or are guessed.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} or WHERE provider = ${provider} AND tenant_id = ${tenantId}) to enforce tenant ownership on updates.
- **server/oauth-subscriptions.ts:981** — UPDATE on oauth_subscriptions uses WHERE id = ${sub.id} (no tenant_id constraint) when deactivating stale subscriptions.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or use provider+tenant_id to ensure the update affects only the intended tenant's row.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} reads a persona row by id without constraining tenant_id, even though tenantId is available to the function. This can expose persona data belonging to other tenants.
  - _Fix:_ Include tenant ownership verification in the query (e.g. SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). Always verify that any foreign id passed from callers belongs to the caller's tenant before reading or using it.
- **server/plan-executor.ts:116** — appendExecutionLog updates plans without any tenant_id constraint: `UPDATE plans SET ... WHERE id = ${planId}`. This lets callers append/modify execution_log for any tenant's plan by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${planId} AND tenant_id = ${callerTenantId}`), or fetch and verify the plan row's tenant first. Pass the tenant into appendExecutionLog or otherwise assert authorization before writing.
- **server/plan-executor.ts:527** — Code sets a plan to 'failed' using `UPDATE plans SET status = 'failed' WHERE id = ${planId}` without restricting by tenant_id, enabling cross-tenant state changes.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. `AND tenant_id = ${callerTenantId}`) or validate the plan's tenant prior to updating. Ensure all status transitions are tenant-scoped.
- **server/plan-executor.ts:759** — Error-path recovery updates plans with `UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing'` without scoping to tenant_id, allowing cross-tenant modifications on exception handling paths.
  - _Fix:_ Add `AND tenant_id = ${callerTenantId}` to the WHERE clause or otherwise validate the plan's tenant before marking failed in the catch path.
- **server/plan-executor.ts:784** — Query selecting pending plans reads across all tenants: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`. This returns plans from any tenant to be re-fired without tenant scoping.
  - _Fix:_ Limit the SELECT to a tenant context (e.g. `WHERE status = 'approved' AND tenant_id = ${tenantId}`) or iterate per-tenant to pick approved plans to execute. Ensure executePlan is invoked only for plans authorized to the executor's context.
- **server/process-governor.ts:121** — Subqueries count heartbeat_logs without constraining tenant_id. The SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ... does not include tenant_id, so it can read logs across tenants.
  - _Fix:_ Include tenant scoping in the subqueries (e.g. AND tenant_id = ${tenantId}) or join heartbeat_logs to heartbeat_tasks with an explicit tenant filter so every read on heartbeat_logs is constrained to the caller's tenant.
- **server/process-governor.ts:155** — Subquery counts heartbeat_tasks for a persona without tenant scoping: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) as pending_tasks. This reads a tenant-scoped table without tenant_id constraint.
  - _Fix:_ Add tenant constraint to the subquery (e.g. AND tenant_id = ${tenantId}) or rewrite to join heartbeat_tasks with ad and include ad.tenant_id = ${tenantId} so results are scoped to the tenant.
- **server/process-governor.ts:198** — SELECT AVG(duration_ms) FROM heartbeat_logs lacks a tenant filter. This reads performance logs across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent) to the WHERE clause so the aggregation only uses rows owned by the tenant invoking the evaluation.
- **server/process-governor.ts:240** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... does not constrain tenant_id. This can count persona failures across tenants.
  - _Fix:_ Include tenant scoping (e.g. AND tenant_id = ${tenantId}) in the heartbeat_logs WHERE clause or join to a tenant-scoped source to ensure only the caller's tenant rows are read.
- **server/process-governor.ts:1183** — Query selects governance_actions and LEFT JOINs governance_rules ON gr.id = ga.rule_id but does not constrain governance_rules by tenant_id. This reads governance_rules (a tenant-scoped table) without a tenant filter.
  - _Fix:_ Ensure joined tenant-scoped tables are constrained to the caller's tenant (e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ${tenantId}) or add gr.tenant_id = ${tenantId} in the WHERE clause to avoid cross-tenant reads.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table agent_knowledge without tenant_id constraint (global scan of 'recurring_message' rows).
  - _Fix:_ Avoid unscoped reads against tenant-scoped tables. Either query per-tenant (iterate tenants and include WHERE tenant_id = <tenant>) or, if a global scan is required, ensure every subsequent read/write on the returned rows is strictly guarded by tenant_id checks. Prefer restricting the initial query with tenant context where possible.
- **server/research-engine.ts:450** — SELECT from tenant-scoped table research_experiments WHERE session_id = ${sessionId} with NO tenant_id constraint — reading potentially other tenants' experiment data.
  - _Fix:_ Constrain the query by tenant_id (e.g. AND tenant_id = ${session.tenantId}) or join through the session row to ensure the session belongs to the expected tenant before selecting experiments. Always include the caller's tenant in WHERE for tenant-scoped tables.
- **server/research-engine.ts:459** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} — query against a tenant-scoped table without tenant_id constraint.
  - _Fix:_ Restrict the select to the session's tenant (e.g. WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}). If research_programs is global, document that explicitly; otherwise require tenant scoping.
- **server/research-engine.ts:464** — SELECT summary FROM research_sessions WHERE id = ${sessionId} — reading a tenant-scoped table row without filtering by tenant_id.
  - _Fix:_ Add tenant protection to the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}) or validate the session row's tenant before using it. All selects against tenant-scoped tables must include the caller's tenant.
- **server/research-engine.ts:987** — SELECT name FROM research_programs WHERE id = ${session.programId} inside injectKeepedFinding — no tenant_id constraint when reading a (likely) tenant-scoped table.
  - _Fix:_ Query research_programs with tenant context (e.g. WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}) or otherwise validate ownership before using program metadata. Ensure every read against tenant-scoped tables includes tenant_id.
- **server/routes.ts:4337** — INSERT into project_notes does not include tenant_id. The code inserts `INSERT INTO project_notes (project_id, note, author) VALUES (...)` using a project_id retrieved without tenant validation, which can create rows missing tenant context or associate notes with another tenant's project.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ...)`). Also ensure projId was obtained from a conversation verified to belong to the caller's tenant (see fix for the SELECT above). If project_notes is created via storage APIs, prefer a tenant-aware storage method that enforces tenant ownership.
- **server/routes/conversations.ts:98** — UPDATE against tenant-scoped table 'conversations' omits tenant_id in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}). Even if you validated project ownership earlier, enforce tenant_id in every SQL write to prevent cross-tenant modification if IDs are guessed.
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})), or otherwise derive/validate and set tenantId from the caller before inserting.
- **server/routes/conversations.ts:104** — UPDATE against tenant-scoped table 'projects' omits tenant_id in WHERE clause (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}).
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so only projects owned by the caller's tenant can be modified.
- **server/routes/conversations.ts:227** — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)).
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) or otherwise ensure tenant ownership is set on the new row.
- **server/routes/heartbeat.ts:102** — UPDATE against tenant-scoped table 'heartbeat_tasks' omits tenant_id in WHERE clause (UPDATE heartbeat_tasks ... WHERE id = ${taskId}) in the approval path.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${taskId} AND tenant_id = ${task.tenantId || tenantId}) so one tenant's admin cannot update another tenant's tasks by guessing IDs. Always include tenant_id on writes to tenant-scoped tables.
- **server/routes/heartbeat.ts:116** — UPDATE against tenant-scoped table 'heartbeat_tasks' omits tenant_id in WHERE clause (UPDATE heartbeat_tasks ... WHERE id = ${taskId}) in the rejection path.
  - _Fix:_ Add tenant_id to the WHERE clause (WHERE id = ${taskId} AND tenant_id = ${task.tenantId || tenantId}) to enforce tenant scoping on the update.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table 'inbox_messages' omits tenant_id in WHERE clause (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}) when marking a message read while serving GET /api/inbox/:id.
  - _Fix:_ Include tenant constraint on the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or perform the update via a tenant-scoped API so only the owning tenant's rows are modified.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ... runs without an explicit tenant_id predicate. Although the code checks the project belongs to the caller earlier, the query itself lacks a tenant_id filter and therefore violates the invariant that tenant-scoped tables must be constrained by tenant_id at the SQL level.
  - _Fix:_ Add an explicit tenant predicate to the query (e.g. AND tenant_id = ${tenantId}), or join to projects and include WHERE p.tenant_id = ${tenantId} so the tenant predicate is enforced in the DB-level query.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 has no tenant_id constraint. The query could return a row from a wrong tenant if project-file FKs are malformed or not enforced by the DB.
  - _Fix:_ Constrain the SELECT with tenant_id = ${tenantId} (or join to projects and check projects.tenant_id = ${tenantId}) to ensure tenant isolation is enforced at SQL level.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} omits tenant_id. The deletion relies on a prior application-level project check but does not include tenant_id in the DELETE itself, violating the requirement that tenant-scoped table mutations include tenant predicates.
  - _Fix:_ Include tenant_id in the DELETE predicate (DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}). Alternatively, perform DELETE ... USING projects JOIN to ensure the tenant predicate is enforced by the DB.
- **server/routes/oauth-subscriptions.ts:325** — SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE reads tenant-scoped table across all tenants without a tenant_id constraint. This is used in the cron 'heal' flow and enumerates tenant IDs globally.
  - _Fix:_ If the cron/heal flow must scan all tenants, ensure it runs under a tightly controlled platform-admin process and the endpoint is protected accordingly (the current cron secret check helps, but document and restrict access). If this should be tenant-scoped, add WHERE tenant_id = <tenantId>. Prefer moving global tenant enumeration into an offline admin-only job or otherwise ensure platform-level authorization and auditability.
- **server/seed.ts:34** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. This reads a tenant-scoped table across all tenants.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope the count to a specific tenant before running the query. If the intent is to operate per-tenant, iterate per tenant and pass the tenant_id explicitly.
- **server/seed.ts:2854** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id constraint and therefore reads research_programs across tenants.
  - _Fix:_ Restrict the SELECT to the caller's tenant (e.g. WHERE is_active = true AND tenant_id = ${tenantId}) or perform the operation per-tenant. Avoid scanning tenant-scoped tables without an explicit tenant filter.
- **server/seed.ts:2859** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only id, without verifying tenant ownership.
  - _Fix:_ Include tenant ownership in the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${p.tenantId}) or first verify the program belongs to the expected tenant before updating. Never rely on id alone when tenant isolation must be enforced at application level.
- **server/seed.ts:3204** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant filter. Conversations are tenant-scoped, so this query can return rows from other tenants.
  - _Fix:_ Add a tenant constraint (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or join/compare tenant_id with the related project to ensure both rows belong to the same tenant before acting on them.
- **server/seed.ts:4032** — Bulk db.insert(heartbeatTasks).values([...]) calls insert multiple heartbeat_tasks rows but the inserted objects do not include tenant_id. heartbeat_tasks is tenant-scoped, so these inserts will rely on a default or create rows with incorrect tenant ownership.
  - _Fix:_ Always pass tenant_id explicitly in inserted rows (e.g. { ..., tenant_id: tenantId }). If seeding global defaults for a specific tenant use explicit tenant=1 for demo/deploy scripts, or modify the seed to iterate tenants and insert per-tenant.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` (detectStagnation) can execute without a tenant_id filter when tenantId is not supplied — the constructed WHERE may omit tenant constraint.
  - _Fix:_ Require a tenantId (throw if absent) or ALWAYS include eq(experiments.tenantId, tenantId) in the WHERE. Avoid building a WHERE that can end up missing the tenant predicate (do not run unscoped queries across tenants).
- **server/self-improvement.ts:511** — SELECT from tenant-scoped table `experiments` (getExperimentHistory) may be executed without tenant_id when tenantId argument is omitted — function falls back to returning an unscoped select().from(experiments).
  - _Fix:_ Fail-closed: require tenantId or add explicit tenant constraint. Do not allow the branch that issues an unscoped SELECT; either throw if tenantId not provided or default to the caller's tenant and always include eq(experiments.tenantId, tenantId) in the WHERE.
- **server/skill-seeker.ts:759** — listGaps issues multiple SELECTs against tenant-scoped table `capability_gaps` that can run without a tenant_id filter (the code has branches that execute unscoped SELECTs when tenantId is not supplied).
  - _Fix:_ Do not run unscoped queries. Require tenantId or explicitly include AND tenant_id = ${tenantId} in every branch. If cross-tenant admin views are needed, require an explicit admin check and a dedicated API that documents the cross-tenant access.
- **server/skillify.ts:196** — SELECT from `conversations` is performed without scoping by tenant_id (no WHERE tenant_id = <caller tenant>). The code reads a conversation by id and only checks tenant ownership after the SELECT, violating the requirement that every read against a tenant-scoped table include an explicit tenant filter.
  - _Fix:_ Include tenant scoping in the query itself (e.g. .where(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))) or use a canonical guard before the SELECT (e.g. storage-helpers/project-tenant-guard.assertConversationInTenant) so the DB never returns rows from other tenants.
- **server/skillify.ts:203** — SELECT from `messages` uses only conversationId (eq(messages.conversationId, conversationId)) with NO tenant_id constraint. This performs a read on a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or validate the conversation ownership up front and then include the tenant_id in the query via tenantScope/requireTenantScope.
- **server/skillify.ts:91** — In distillIntent, recent messages are queried from `messages` by conversationId without a tenant_id constraint. This allows returning messages across tenants if conversationId is attacker-controlled or misused.
  - _Fix:_ Scope the query to the caller tenant (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or validate conversation ownership before fetching messages (assertConversationInTenant). Use tenantScope/assertValidTenantId helpers when building Drizzle predicates.
- **server/skillify.ts:331** — In skillifyConversation, recentMsgs are selected from `messages` using only conversationId (no tenant_id constraint). This violates the requirement that reads against tenant-scoped tables include an explicit tenant filter.
  - _Fix:_ Include the tenant constraint on messages (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or verify conversation ownership first via the project/conversation tenant-guard helpers prior to the SELECT.
- **server/stability-watchdog.ts:177** — SELECT from `heartbeat_logs` aggregates error counts across rows but does not include a tenant_id WHERE clause. This reads tenant-scoped data across all tenants without explicit scoping.
  - _Fix:_ If this query should act per-tenant, add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant and run scoped queries. If it must be global admin-only, make the admin intent explicit and ensure the code path is restricted to authorized operator processes and documented; prefer explicit tenant-aware loops over unscoped multi-tenant queries.
- **server/storage.ts:245** — getConversationUnscoped performs a SELECT on the tenant-scoped conversations table without constraining by tenant_id.
  - _Fix:_ Avoid unscoped reads in general. If an unscoped fetch is absolutely required, keep it internal-only and ensure every caller MUST validate tenant ownership before using the row. Prefer adding a documented explicit parameter and require assertValidTenantId or otherwise limit callsites; or provide a separate controlled admin path guarded by authorization.
- **server/storage.ts:289** — attachCitationMetadata executes a raw SELECT FROM messages WHERE id IN (...) but does not include a tenant_id constraint — allowing reads of message rows across tenants by id.
  - _Fix:_ Include tenant scoping in the query (e.g. add AND tenant_id = ${tenantId} or join via conversation → tenant) or pass the validated tenantId into this function and use it in the WHERE clause. Avoid raw selects that rely solely on id if ids may come from request-derived data.
- **server/storage.ts:390** — attachCitations runs an UPDATE on messages (UPDATE messages SET citations = ... WHERE id = ${messageId}) with no tenant_id constraint, allowing cross-tenant updates if messageId is attacker-controlled.
  - _Fix:_ Require and validate the tenantId for the update (or verify the message's tenant before updating) and include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}).
- **server/storage.ts:474** — deletePersona updates conversations (UPDATE conversations SET personaId = null WHERE personaId = id) without tenant scoping — this mutates conversation rows across all tenants.
  - _Fix:_ Scope the update by tenant (e.g. add AND tenant_id = ${tenantId}) and/or require the caller to pass the tenantId and validate it with assertValidTenantId. If persona deletion is intended to be tenant-scoped, add tenant param; if global, restrict to admin-only code paths and document it.
- **server/storage.ts:475** — deletePersona updates memory_entries (SET status='superseded' WHERE personaId = id) with no tenant constraint — can affect memory entries across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) and require/validate tenantId for this operation. If this action is intentionally global, ensure it is protected by admin-only authorization and audit logging.
- **server/storage.ts:476** — deletePersona deletes dailyNotes WHERE personaId = id with no tenant constraint — this will delete notes across tenants.
  - _Fix:_ Restrict the DELETE by tenant (AND tenant_id = ${tenantId}) and require the tenantId to be passed/validated when deleting a persona. If global behavior is intended, gate behind admin checks and document the rationale.
- **server/storage.ts:539** — updateMemoryEntry performs an UPDATE on memory_entries using a WHERE that may lack tenant scoping (tenantId optional; code only warns when omitted) — permitting cross-tenant updates if tenantId is not passed.
  - _Fix:_ Require tenantId (assertValidTenantId) for the update path and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause. Do not allow a silent warn-then-run pattern for data-modifying operations against tenant-scoped tables.
- **server/storage.ts:547** — deleteMemoryEntry updates memory_entries.set(status='superseded') with a WHERE that can be unscoped if tenantId is not supplied (tenantId optional + only a warning when omitted).
  - _Fix:_ Require and validate tenantId and include tenant scoping in the WHERE clause. Avoid executing cross-tenant mutations even if the code only emits a console warning for missing tenantId.
- **server/storage.ts:556** — touchMemoryEntries issues an UPDATE on memory_entries with a WHERE built from ids and an optional tenantScope; if tenantId is omitted this becomes unscoped and will touch rows across tenants.
  - _Fix:_ Require tenantId (use assertValidTenantId) for bulk touch/update operations, or otherwise ensure the function is only callable from trusted contexts and include tenant scoping in the WHERE clause unconditionally.
- **server/storage.ts:833** — archiveExpiredMemories runs a raw UPDATE on memory_entries that archives all expired rows without any tenant_id constraint — bulk cross-tenant mutation by background job.
  - _Fix:_ If intended to be tenant-scoped, add tenant filtering (and call per-tenant or loop per-tenant with authorization). If this is an intentional global archive, restrict execution to an admin/maintenance context and document the risk; prefer per-tenant scheduled jobs to avoid cross-tenant side effects.
- **server/storage.ts:849** — archiveStaleMemories executes a raw UPDATE on memory_entries to archive stale rows with no tenant_id filtering — background job will affect all tenants' rows.
  - _Fix:_ Either scope the UPDATE by tenant_id (and run per-tenant) or ensure the job runs in an admin-only context and is explicitly authorized/audited. Prefer failing closed and requiring explicit tenant scoping for lifecycle mutations.
- **server/storage.ts:797** — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} without scoping by tenant_id — allows modification of another tenant's memory row by id.
  - _Fix:_ Require the tenantId (or verify the row's tenant before updating) and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause to enforce tenant isolation.
- **server/storage.ts:809** — updateKnowledgeEmbedding updates agent_knowledge SET embedding WHERE id = ${id} with no tenant constraint — this can overwrite another tenant's knowledge embedding by id.
  - _Fix:_ Require/accept tenantId and include eq(agent_knowledge.tenant_id, tenantId) in the WHERE clause, or verify ownership of the row before performing the update to ensure tenant isolation.
- **server/storage.ts:1421** — supersedeAdr updates architecture_decisions (marking the old ADR superseded) using WHERE id = oldId without including tenant_id in the UPDATE — even though ownership was checked earlier, the UPDATE itself is not tenant-scoped.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) as defense-in-depth. Even after prior ownership checks, always include tenant_id equality in write WHERE clauses to prevent accidental cross-tenant mutation if logic changes.
- **server/storage.ts:1424** — supersedeAdr updates the new ADR row with WHERE id = newId but without tenant scoping — write is not constrained to tenant_id even though earlier selects checked ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) to ensure defense-in-depth. Always include tenant_id in UPDATE/DELETE WHERE clauses for tenant-scoped tables.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (eq(customTools.tenantId, tenantId)). This allows a cross-tenant lookup/leak or logic acting on the wrong tenant's row.
  - _Fix:_ Include an explicit tenant filter on the SELECT, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). If the code intends to check global uniqueness, make that explicit and document why; otherwise scope to the caller's tenant.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without filtering by tenant_id (eq(customTools.tenantId, tenantId)). Reading rows by id across tenants can leak or return the wrong tenant's data.
  - _Fix:_ Add a tenant constraint to the query, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Alternatively, fetch by id+tenantId or pass tenantId into this validation path and assert ownership before returning the row.
- **server/tool-learning.ts:292** — UPDATE against tenant-scoped table `custom_tools` (.where(eq(customTools.id, tool.id))) lacks a tenant_id constraint. This can update a row belonging to a different tenant if IDs collide or are guessed.
  - _Fix:_ Include the tenant guard in the WHERE clause, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId))). Ensure the function has the tenantId available (or derive it from the tool object) and validate ownership before updating.
- **server/tool-learning.ts:367** — UPDATE to increment usageCount on tenant-scoped `custom_tools` uses only id in WHERE (where(eq(customTools.id, tool.id))) and omits tenant_id, allowing cross-tenant modification.
  - _Fix:_ Scope the update by tenant, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the code has or derives the tenantId for the tool and validate ownership before incrementing usageCount.
- **server/tools.ts:3129** — Query builds per-project counts using subselects like `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)` / `project_notes` / `project_conversations` WITHOUT constraining those tenant-scoped tables by tenant_id. Even though the outer projects row is tenant-limited, these unscoped reads against other tenant-scoped tables violate the invariant and can leak cross-tenant data.
  - _Fix:_ Add an explicit tenant filter to each subselect, e.g. `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId})`, or join through the projects row that is already tenant-checked and include `AND pf.tenant_id = ${tenantId}`. Ensure every read against project_files/project_notes/project_conversations includes `tenant_id = <caller tenant>`.
- **server/tools.ts:3191** — Duplicate-check for add_file uses `SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ...` WITHOUT `tenant_id` — reading a tenant-scoped table without tenant constraint.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the duplicate-check SELECT (or use a storage helper that enforces tenant scoping) so the check is limited to the caller's tenant.
- **server/tools.ts:3232** — Lookups against `project_conversations` use `SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId}` WITHOUT a tenant filter on project_conversations.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or otherwise scope the read to the caller's tenant) to this SELECT to prevent reading another tenant's link records.
- **server/tools.ts:3246** — Search query for projects includes subselects `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)` and `(SELECT COUNT(*) FROM project_notes WHERE project_id = p.id)` without tenant constraints. These are reads against tenant-scoped tables with no tenant_id in the predicate.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to each subselect (or rewrite as joins that include tenant checks) so counts are computed only for the caller's tenant. Consistently enforce tenant scoping in all subqueries.
- **server/tools.ts:4404** — SELECT from tenant-scoped table 'memory_entries' without constraining by tenant_id. The query: SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ... has no tenant filter.
  - _Fix:_ Scope the query to the caller's tenant. Pass the tenant id and add WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(memoryEntries.tenantId, tenantId))). Example: db.execute(sql`SELECT fact FROM memory_entries WHERE tenant_id = ${params._tenantId} AND persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ...`).
- **server/tools.ts:4753** — SELECT from tenant-scoped table 'presenter_sessions' without a tenant_id constraint: SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ... — missing tenant scoping.
  - _Fix:_ Restrict the query to the caller's tenant. Either include AND tenant_id = ${params._tenantId} (or equivalent Drizzle .where) or verify ownership with an assertion helper that checks the presenter_session / presentation belongs to params._tenantId before querying. Prefer adding the tenant filter in the SQL.
- **server/tools.ts:7113** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The code inserts a row for a project file without persisting the tenant_id column even though tenant-isolation is enforced only at the application layer (no RLS).
  - _Fix:_ Include the tenant id explicitly in the INSERT (and/or use a Drizzle insert with the tenantId property). For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Alternatively use db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: params.project_id, ... }). Ensure tenant_id comes from the authenticated caller and keep (or keep) the assertProjectInTenant check as defense-in-depth.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE on tenant-scoped table 'browser_workflows' without tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}) — row update is scoped only by id, allowing cross-tenant modification if an attacker can guess an id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well. Example: UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId} (use the trusted ctx.tenantId). Alternatively use Drizzle's .where(eq(table.id, id), eq(table.tenantId, tenantId)). Always include tenant_id in WHERE for updates against tenant-scoped tables.
- **server/tools/domains/files/handlers.ts:839** — UPDATE on tenant-scoped table 'projects' without tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}) — even though projId was derived earlier, this UPDATE lacks an explicit tenant filter and could modify a project id from a different tenant if validation changes or is bypassed.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}, or use Drizzle .where(eq(projects.id, projId), eq(projects.tenantId, tenantId)). Even when projId was previously validated, always include tenant_id in write/modify statements against tenant-scoped tables to enforce isolation at each operation.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE on tenant-scoped table `knowledge_triples` uses WHERE id = ${row.id} with NO tenant_id constraint. This UPDATE can mutate rows without explicitly constraining to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the calling tenant. For example: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}. Or use Drizzle's .where(eq(table.tenantId, tenantId)) so every write is scoped to the tenant.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys executed without a tenant_id filter. The query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` can return rows for all tenants (sensitive provider API key presence/length), but no application-level tenant constraint is enforced.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add `AND tenant_id = ${tenantId}`) so only the intended tenant's provider keys are inspected. If this is intended admin-only global audit behavior, make that explicit and protect the path (keep the existing admin check and/or move to a clearly privileged admin-only routine); otherwise add `WHERE tenant_id = ${tenantId}` and use parameterized drizzle/sql templates.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT/DISTINCT on provider_keys executed without a tenant_id filter. The query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` runs under the admin branch but lacks a tenant constraint and can aggregate across tenants.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to restrict aggregation to the caller's tenant, or explicitly document and centralize admin-global audits behind a dedicated, well-audited admin endpoint. Prefer parameterized drizzle/sql templates to avoid accidental cross-tenant exposure.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without a tenant_id constraint. Any SELECT against a tenant-scoped table must include tenant scoping or explicit ownership checks.
  - _Fix:_ Include tenant scoping in the query or otherwise verify ownership in a single DB operation. For example: SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${expectedTenantId} LIMIT 1 — or fetch the conversation row by id and assert row.tenant_id === info.tenantId before trusting the pairing.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} updates a tenant-scoped table by id only, without constraining tenant_id. This could allow a caller to affect rows belonging to other tenants if reservationId is attacker-controlled or leaked.
  - _Fix:_ Require and use tenantId when settling a reservation (add tenantId param or verify ownership). Update the row with both id and tenant_id (WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or SELECT the row first and assert its tenant_id matches the expected tenant before UPDATE. Reject/ignore when the reservation does not belong to the caller's tenant.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (releaseVentureReservation) modifies a tenant-scoped table without constraining tenant_id (same risk as settleVentureReservation).
  - _Fix:_ As above, require tenant-scoped checks before modifying the ledger row. Change the function to accept tenantId (or look up reservation and validate tenant_id) and include AND tenant_id = ${tenantId} in the WHERE clause when updating, or perform a guarded SELECT+UPDATE after verifying ownership.
- **server/video-job-runner.ts:202** — SELECT on tenant-scoped table videoJobs (isCancelRequested reads by jobId only) lacks tenant scoping. isCancelRequested(jobId) queries cancelRequested WHERE jobId = ${jobId} without verifying tenant ownership.
  - _Fix:_ Pass the expected tenantId into isCancelRequested and include it in the WHERE clause (e.g. WHERE jobId = ${jobId} AND tenant_id = ${tenantId}). Alternatively, SELECT the row and assert row.tenantId === expectedTenantId before honoring its cancelRequested value.

### MEDIUM (38)

- **server/agentic-features.ts:599** — Duplicate-enrollment check SELECT FROM `outreach_enrollments` does not constrain by tenant_id. This can cause cross-tenant enrollment collisions or false positives/negatives when determining whether a contact is already enrolled.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' AND tenant_id = ${params.tenantId}).
- **server/business-tools.ts:84** — Inserting invoice line items does not set tenant_id on invoice_items. The INSERT INTO invoice_items(...) VALUES(${invoiceId}, ...) omits tenant scoping; if invoice_items is a tenant-scoped table this allows creation of rows without an explicit tenant_id and may break isolation.
  - _Fix:_ Ensure invoice_items is tenant-scoped and include tenant_id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)), or if invoice_items intentionally omits tenant_id, enforce ownership by validating the parent invoice belongs to the tenant before inserting.
- **server/channel-routing.ts:30** — Query joins global channel_routes to personas without a tenant constraint: SELECT ... FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ... returns persona data (p.name) from personas across tenants.
  - _Fix:_ Either make channel_routes tenant-scoped (add tenant_id and include it in INSERT/SELECT), or restrict the JOIN to the caller's tenant (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = <callerTenant>). Ensure persona names are only resolved for the requesting tenant.
- **server/channel-routing.ts:63** — getPersonaForChannel SELECTs persona_id from global channel_routes without any tenant context. Returning a persona_id from a global table can lead to using that id across tenant boundaries without verification.
  - _Fix:_ If channel_routes is intentionally global, verify ownership of the returned persona_id before any tenant-scoped use. Prefer making channel_routes tenant-scoped or filter the lookup by tenant_id to ensure the returned persona_id belongs to the requesting tenant.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` is executed without any tenant_id constraint (scans all tenants).
  - _Fix:_ If this cross-tenant scan is intentional for a platform-admin job, ensure the code executes under an explicit, auditable admin context and enforce strict redaction/authorization for any surfaced data. If it is accidental, restrict the query with a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or require an explicit admin-only flag and assert caller identity before reading tenant-scoped rows.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` with no tenant_id constraint — reading tasks across all tenants (potential cross-tenant impact)
  - _Fix:_ Confirm whether heartbeat_tasks is global or per-tenant. If per-tenant, add WHERE tenant_id = ${tenantId} and operate only on that tenant's tasks. If global, document and protect this admin-only behavior (require explicit admin context).
- **server/health-audit.ts:368** — UPDATE on `heartbeat_tasks` (disabling stuck tasks) with no tenant_id constraint — may disable tasks for other tenants
  - _Fix:_ If heartbeat_tasks is tenant-scoped, include tenant_id in the WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or validate each task's tenant before mutating. If global admin action, require explicit admin authorization and logging.
- **server/job-queue.ts:177** — completeJob updates `agent_jobs` by id without verifying tenant ownership (no tenant_id in the WHERE clause). A caller could complete a job for another tenant if they obtain the id.
  - _Fix:_ Add tenant fencing to the WHERE clause (AND tenant_id = ${tenantId}) or require callers to pass/verify the job's tenant before marking it complete. Use the job row's tenant_id (from claim time) as the authoritative source for tenant context rather than untrusted input.
- **server/job-queue.ts:273** — cancelJob updates `agent_jobs` by id without a tenant_id predicate. This can cancel jobs across tenants if misused.
  - _Fix:_ Require / verify tenant ownership before updating: UPDATE ... WHERE id = ${id} AND tenant_id = ${tenantId} AND status IN (...), or restrict the API that calls cancelJob to operate in the correct tenant context.
- **server/job-queue.ts:282** — retryJob updates `agent_jobs` by id without tenant scoping (no tenant_id check). This can reset/ retry jobs belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or otherwise validate the job belongs to the requesting tenant before performing the reset.
- **server/job-queue.ts:385** — getJobStats runs a global SELECT on `agent_jobs` (WHERE created_at > ...) without tenant filtering. This returns aggregate counts across tenants.
  - _Fix:_ If this API is tenant-scoped, add WHERE tenant_id = ${tenantId}. If global stats are intended, ensure the calling surface is admin-only and clearly documented; avoid exposing cross-tenant aggregates to tenant-level callers.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor issues a SELECT over `agent_knowledge` grouping by tenant_id/persona_id and iterates all tenant/persona pairs (no tenant_id WHERE clause). This scans tenant-scoped data across all tenants.
  - _Fix:_ If this monitor is intended to run globally as an admin process, ensure it is only executed by a privileged background worker and that any outputs are tenant-separated. If it should be run per-tenant, add a tenant_id filter or run per-tenant iteration to avoid accidentally exposing or mutating cross-tenant data.
- **server/lib/action-ledger.ts:268** — Cross-tenant SELECT on tenant-scoped table `action_attempts` with no tenant_id predicate (listUnknownAttemptsAllTenants).
  - _Fix:_ If this is intended to be global maintenance, document and guard it (require a reconciler/admin role) and keep the returned tenant_id column; otherwise restrict the query to a single tenant (add WHERE tenant_id = ${tenantId} or accept a tenantId param). Prefer explicit, audited code paths for cross-tenant reads rather than silent unscoped SELECTs.
- **server/lib/agent-trace.ts:124** — UPDATE on tenant-scoped table `agent_trace_spans` uses WHERE span_id = ${spanId} with no tenant_id predicate (persistSpanClose).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) or otherwise assert/lookup the tenant for the span before updating. Track tenantId together with spanId in the in-memory context used to perform the close so the UPDATE can be tenant-scoped.
- **server/lib/restraint-trust.ts:90** — Dynamic SQL identifier via sql.raw(column) where `column` is derived from the caller-supplied `outcome` parameter (even though whitelisted). Using sql.raw with a runtime value risks SQL-injection or accidental misuse if validation logic changes.
  - _Fix:_ Avoid emitting dynamic SQL identifiers. Replace the dynamic UPDATE with an explicit, static branch (switch/case) that issues one of four fixed, parameterized UPDATE statements (one per allowed outcome column). This keeps all identifiers static and uses only parameterized values. Example: switch on outcome → run UPDATE trust_scores SET action_alpha = action_alpha + $1 WHERE ... (bound params). If dynamic identifiers are unavoidable, centralize and strictly assert the allowlist and consider using a server-side mapping to pre-built prepared statements.
- **server/minds-engine.ts:214** — SQL value constructed via string interpolation from request-derived dependsOn array: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. Building SQL fragments from caller input risks injection or mis-scoping.
  - _Fix:_ Avoid building array literals via string interpolation. Instead pass arrays as parameters or use parameterized constructs supported by the driver (e.g. pass an int[] parameter if supported) or validate and sanitize each id explicitly before composing the array.
- **server/minds-engine.ts:327** — UPDATE using id = ANY(${`{${eventIds.join(',')}}`}::int[]) constructs a SQL array literal by interpolating request-derived eventIds into a string. This pattern can lead to injection or scoping issues if eventIds are not strictly validated.
  - _Fix:_ Use parameterized array binding (driver/ORM array parameter) or validate/escape every id before composing the SQL. Prefer passing eventIds as a parameterized array rather than injecting a formatted literal into the query.
- **server/minerva-planner.ts:508** — getPlan fallback branch allows SELECT * FROM plans WHERE id = ${planId} with no tenant restriction when tenantId is omitted. Although documented as 'trusted internal' usage, any accidental external call or misuse could read cross-tenant data.
  - _Fix:_ Avoid exposing an unscoped code path. Require tenantId or explicitly assert internal-trusted context before executing an unscoped SELECT. Prefer explicit tenant scoping even for internal callers or clearly limit and audit all call sites that use the unscoped form.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(${lockKey}) inside SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). sql.raw is being used with a value derived from caller-provided ledgerKey (even though hashed). This is string-injected into SQL and can be an injection/isolation risk.
  - _Fix:_ Avoid sql.raw with caller-derived data. Compute the numeric lock value in JS (as a BigInt) and pass it as a parameter to the query so it is parameterized (e.g. const lock = BigInt(...); await tx.execute(sql`SELECT pg_advisory_xact_lock(${lock}::bigint)`)). Ensure ledgerKey is validated/normalized before use.
- **server/routes.ts:163** — SELECT from tenant-scoped table `file_storage` without tenant constraint in restoreUploadsFromDb startup routine. The query db.select(...).from(fileStorage).where(and(like(fileStorage.mimeType, "image/%"), isNull(fileStorage.storageKey))) reads rows across all tenants.
  - _Fix:_ Either scope this restore to a single tenant (add a WHERE tenant_id = <tenant>), iterate per-tenant under proper platform-admin context, or explicitly run as a trusted admin-only background job. Avoid doing unscoped reads of tenant data on public startup paths; if the operation must touch all tenants, ensure it runs with explicit platform-admin authorization and logs/controls are in place.
- **server/routes.ts:1536** — Public endpoint /api/public/stats issues SELECTs against tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filter — this returns cross-tenant aggregates to unauthenticated callers.
  - _Fix:_ If these global aggregates are intentional, restrict the endpoint to admin callers or aggregate only metrics that are explicitly allowed to be public. Otherwise, scope queries by tenant (WHERE tenant_id = <caller tenant>) or compute per-tenant summaries and only return the caller's tenant data.
- **server/routes.ts:1565** — Public endpoint /api/public/architecture performs multiple unscoped SELECTs against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions, etc.) without tenant_id constraints, leaking cross-tenant counts and lists.
  - _Fix:_ Restrict this endpoint or its queries: either require admin authentication for full global stats or scope queries to the requesting tenant with WHERE tenant_id = <caller tenant>. For persona/project lists that must be global, move them to explicitly global/shared tables or mark and protect them via admin-only gates.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table 'agent_jobs' does not constrain by tenant_id (SELECT ... FROM agent_jobs j LEFT JOIN tenants t ON t.id = j.tenant_id WHERE j.status IN ('pending','running')). This returns active jobs across all tenants.
  - _Fix:_ If the intent is to show only the caller's tenant jobs, add WHERE j.tenant_id = ${tenantId}. If the intent is to show cross-tenant data, explicitly document and gate it (platform-admin only) and ensure the caller's tenant context is validated before returning cross-tenant results.
- **server/routes/metrics.ts:63** — Global SELECT over tenant-scoped table `agent_cost_ledger` (SELECT tenant_id, SUM(cost_usd...) FROM agent_cost_ledger ...) executes with no tenant_id constraint. The query returns per-tenant aggregates and reads across all tenants without a caller-scoped tenant predicate.
  - _Fix:_ If this endpoint is intended to be operator-only, ensure it is strongly authenticated/authorized (platform-admin) and that exposing per-tenant aggregates is intended and documented. If it is meant to run in the context of a single tenant, add WHERE tenant_id = <tenantId>. Prefer explicit platform-admin gating and audit logging for any global reads of tenant-scoped tables.
- **server/routes/metrics.ts:79** — SELECT tool_name, COUNT(*) FROM agent_cost_ledger ... has no tenant_id constraint and aggregates across all tenants. This reads a tenant-scoped ledger table globally.
  - _Fix:_ Same as above: either add tenant_id filtering for tenant-scoped use, or ensure the endpoint is strictly operator-only (platform-admin) and review whether exposing aggregated cross-tenant data is acceptable. Add platform-level auth if required.
- **server/routes/metrics.ts:95** — SELECT SUM(tokens_in), SUM(tokens_out) FROM agent_cost_ledger ... executes without a tenant_id predicate and aggregates globally across tenants.
  - _Fix:_ Add a tenant_id WHERE clause when this data should be scoped, or restrict the endpoint to platform operators and ensure the global read is an explicit, authorized action. Consider moving cross-tenant aggregation to a separate admin-only report path.
- **server/skillify.ts:223** — SELECT from `personas` queries persona by id without any tenant scoping. If `personas` is tenant-scoped this can leak another tenant's persona data.
  - _Fix:_ If personas are tenant-scoped, add a tenant filter (e.g. .where(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))) or verify the persona belongs to the tenant before reading. If personas are intentionally global, add a code comment to justify the global access.
- **server/storage.ts:513** — getMemoryEntry can perform an unscoped SELECT on memory_entries when tenantId is not supplied (tenant scoping is optional and only warned about), allowing accidental cross-tenant reads.
  - _Fix:_ Fail closed: require tenantId (use assertValidTenantId) or make the function explicitly named/unexported for unscoped reads and restrict its callers. If unscoped reads are allowed for specific orchestration, centralize and audit those call sites and ensure they only run in trusted contexts.
- **server/tools.ts:2970** — System health probe issues a DB count over messages without a tenant filter: `db.select({ count: sql`count(*)::int` }).from(messagesTable)` — this reads a tenant-scoped table globally.
  - _Fix:_ If intent is per-tenant metrics, constrain the query with `WHERE tenant_id = ${tenantId}` (or call a storage-layer helper that scopes by tenant). If the intent is a global system-wide metric, ensure this is an explicit, audited global read and does not surface tenant data to tenant-scoped contexts.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files is executed without explicitly supplying tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)). If project_files is a tenant-scoped table (no DB default for tenant_id), this will create rows not tied to the caller's tenant.
  - _Fix:_ If project_files is tenant-scoped, include tenant_id in the inserted columns and pass the trusted tenant (e.g. VALUES (${projId}, ${fileName}, ${absPath}, ${driveUrl||null}, ${fileType}, ${stat.size}, ${'agent'}, ${tenantId})). If project_files is intentionally keyed by project_id only, ensure that project_id was validated against ctx.tenantId immediately before insert (and document this invariant). Prefer explicit tenant_id columns for clarity and safety.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs updates videoJobs rows across tenants with no tenant constraint (UPDATE ... WHERE status IN (...) AND updatedAt < staleCutoff). This writes to tenant-scoped rows for all tenants in one statement.
  - _Fix:_ Restrict stale-job recovery to an explicitly authorized admin operation or to a per-tenant execution. Either (a) accept an explicit admin mode and document/permit cross-tenant writes only under admin control, or (b) run the sweep per-tenant (loop tenants and run update WHERE tenant_id = ${tenantId} AND ...) so that each DB write is explicitly scoped to a tenant. Add access control checks if cross-tenant updates must be retained.
- **server/wiring-invariants.ts:244** — Querying tool_performance without tenant scoping — SELECT count(*) FROM tool_performance WHERE last_success_at > ... has no tenant_id filter.
  - _Fix:_ If tool_performance is tenant-scoped, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant. If this is an intentional platform-level aggregate, make that explicit (require an admin context) and document/guard it so it cannot be invoked with arbitrary request credentials.
- **server/wiring-invariants.ts:258** — SELECT count(*)::int AS n FROM tool_performance executed with no tenant constraint (reads a tenant-scoped table across all tenants).
  - _Fix:_ Either add tenant scoping or require/verify an admin context. For safe per-tenant usage, pass tenantId into the function and include WHERE tenant_id = ${tenantId}.
- **server/wiring-invariants.ts:260** — SELECT count(*)::int AS n FROM messages WHERE created_at > ... reads the messages table with no tenant filter.
  - _Fix:_ messages is typically tenant-scoped — add a tenant_id predicate (WHERE tenant_id = ${tenantId}) or explicitly run this as a privileged admin-only aggregate. Document and gate platform-wide queries so they cannot be called using normal tenant credentials.
- **server/wiring-invariants.ts:366** — Raw SQL reading subsystem table without tenant filter: `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}` — no tenant_id constraint.
  - _Fix:_ If these subsystem tables are tenant-scoped, include tenant_id in the WHERE clause or otherwise iterate per-tenant. If the checks intentionally examine global state, require and verify admin-only execution and clearly document that behavior.
- **server/wiring-invariants.ts:417** — Querying tool_performance for drift with no tenant scoping (`FROM tool_performance WHERE last_failure_at > ...`) — reads tenant-scoped data across all tenants.
  - _Fix:_ Add an explicit tenant constraint (WHERE tenant_id = ${tenantId}) or run the drift check per-tenant. Alternatively, restrict this routine to an admin context and require explicit confirmation that cross-tenant aggregation is intended and safe.
- **server/wiring-invariants.ts:535** — Raw SQL reads COUNT/size for memory tables (SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}) with no tenant filter.
  - _Fix:_ For tenant-scoped memory tables, add tenant_id filters or perform per-tenant scans. If this is intended as a global platform-level health check, enforce admin-only invocation and document the rationale to avoid accidental cross-tenant data exposure.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*)::int AS cnt FROM graph_memory executed with no tenant scope — reads a likely tenant-scoped table across all tenants.
  - _Fix:_ If graph_memory is tenant-scoped, include WHERE tenant_id = ${tenantId} or run the check iteratively per tenant. If this is intended to be a global check, require admin privileges and explicitly document/guard that behavior.

### LOW (1)

- **server/routes.ts:771** — Startup/backfill backfillProjectDriveFolders() selects projects across all tenants (SELECT p.id, p.name, p.tenant_id FROM projects p JOIN tenants t ...) without scoping to a caller tenant. This reads tenant-scoped project rows without a tenant filter.
  - _Fix:_ If this backfill is intended to run system-wide, ensure it executes only in a platform-admin context (not in a public request) and document the privileged nature. Alternatively, iterate tenants explicitly and run per-tenant queries (WHERE tenant_id = <tid>), or otherwise enforce an explicit admin gate so tenant data isn't read accidentally by less-privileged code paths.

### Suppressed — confirmed false positives (120)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL — this UPDATE does not include tenant_id in the WHERE clause and may modify a conversation belonging to a different tenant.
  - _Why false positive:_ c.project_id and c.tenant_id come from a prior join enforcing conversations and projects share tenant ownership; update targets that previously verified conversation id.
- **server/chat-engine.ts:571** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` uses only project_id and lacks an explicit tenant_id constraint in the WHERE clause. Relying on prior project validation is not sufficient per the invariant — every tenant-scoped read must include the tenant filter.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by verified parent project selected with id and tenant_id above.
- **server/chat-engine.ts:582** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` uses only project_id and lacks an explicit tenant_id constraint in the WHERE clause.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by the already verified parent project.
- **server/data-protection.ts:73** _(upstream-verified)_ — UPDATE on conversations performed without constraining by tenant_id. The code SELECTs the conversation with tenant filter but then issues UPDATE conversations SET ... WHERE id = ${conversationId} (no tenant_id predicate), allowing a TOCTOU or accidental cross-tenant modification.
  - _Why false positive:_ same function immediately selects the conversation by id and tenant_id before updating that verified row; ownership is enforced earlier in the call path shown.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only eq(tenantVoiceProfiles.id, existing.id) in the WHERE clause and does not also constrain by tenant_id. This can allow a caller to update another tenant's row if the id is known or reused.
  - _Why false positive:_ existing row is fetched immediately above via getVoiceProfile with opts.tenantId, so update targets that tenant-owned row
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The code checks ownership via assertProjectInTenant but still writes the row without setting tenant_id, violating the invariant that every INSERT must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is guarded by assertProjectInTenant via tenant-scoped parent project
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. As above, assertProjectInTenant is called but the actual INSERT does not set tenant_id, breaking the tenant-isolation invariant.
  - _Why false positive:_ project_files has no tenant_id and insert is guarded by assertProjectInTenant via tenant-scoped parent project
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim query updates the plans row without constraining by tenant_id: `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...`. This allows the executor to claim/operate on plans belonging to any tenant by id.
  - _Why false positive:_ server-side executor claims plan by server-scheduled numeric PK, not directly from untrusted request input
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final plan status update writes to plans with `WHERE id = ${planId}` and no tenant filter: `UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}`. This can flip status for plans across tenants.
  - _Why false positive:_ same call path claimed and returned the specific plans row earlier; final update operates on that already-claimed plan id
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates a tenant-scoped table by id only, with no tenant_id constraint. That can modify another tenant's task with the same id.
  - _Why false positive:_ t.id comes from prior SELECT of heartbeat_tasks filtered by tenant_id in same call path
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} mutates a tenant-scoped table using only the id column and lacks a tenant_id constraint, allowing cross-tenant writes if ids collide.
  - _Why false positive:_ desk.id comes from prior SELECT of agent_desks filtered by tenant_id in same call path
- **server/routes.ts:4331** _(upstream-verified)_ — SELECT from tenant-scoped table 'conversations' is missing a tenant_id constraint. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` does not restrict results to the caller's tenant, allowing cross-tenant data leakage.
  - _Why false positive:_ same request path already carries tenantId and later code uses conv.tenantId, indicating conversation ownership is established earlier before this helper read
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The statement inserts (project_id, note, author) without explicitly setting tenant_id (project_notes rows are tenant-scoped and tenant_id is NOT NULL/no default).
  - _Why false positive:_ project_notes has no tenant_id by schema and insert is preceded by project ownership check on tenant-scoped parent projects
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload handler) does not include tenant_id. The INSERT omits tenant_id and writes project_files rows with only project_id / file metadata.
  - _Why false positive:_ project_files has no tenant_id by schema and insert is preceded by tenant-validated parent project lookup
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload handler) does not include tenant_id. The INSERT omits tenant_id and records file rows without an explicit tenant column value.
  - _Why false positive:_ project_files has no tenant_id by schema and multipart upload first verifies parent project belongs to tenant
- **server/sculptor.ts:94** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` is performed without constraining by tenant_id (WHERE id = ${session.id} only). This allows an update to a session row belonging to a different tenant.
  - _Why false positive:_ row was just inserted in same function with params.tenantId and returned as session, so updating by that freshly created session.id is ownership-established earlier in same call path
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` (setting conversation_id) lacks a tenant_id constraint (WHERE id = ${session.id} only).
  - _Why false positive:_ same function just created and returned this sculptor_sessions row for params.tenantId, so update by the fresh session.id is already tied to that tenant in-path
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — settleFusionReservation issues an UPDATE on agent_cost_ledger WHERE id = ${reservationId} but does not constrain by tenant_id. agent_cost_ledger is tenant-scoped; updating by id alone can modify another tenant's ledger row.
  - _Why false positive:_ Internal helper settles a server-created reservation row by serial PK; reservationId is not from a request boundary in the shown call path.
- **server/second-opinion.ts:425** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger SET cost_usd='0' WHERE id = ${reservationId} without a tenant_id constraint. This may allow clearing reservations for other tenants.
  - _Why false positive:_ Internal best-effort release helper updates a server-created reservation by PK only; id is not shown as untrusted tenant input.
- **server/seed.ts:3212** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts into a (likely) tenant-scoped table without providing tenant_id. Per the codebase invariant, tenant_id must be explicitly set on INSERTs.
  - _Why false positive:_ project_conversations has no tenant_id by schema and links tenant-scoped parent entities transitively via project_id/conversation_id.
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` updates by experiments.id only (where(eq(experiments.id, experiment.id))) with NO tenant_id constraint — allows modifying another tenant's row if id known.
  - _Why false positive:_ update is by server-side experiment.id inside internal self-improvement worker flow, not from untrusted request boundary
- **server/self-improvement.ts:413** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` in the catch path uses only experiments.id in the WHERE (where(eq(experiments.id, experiment.id))) with NO tenant filter — cross-tenant write risk.
  - _Why false positive:_ catch-path update also targets server-generated experiment primary key in internal worker code
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table `custom_tools` without a tenant_id constraint (db.delete(...).where(eq(customTools.id, tool.id))). Unscoped DELETEs can remove another tenant's data and are a serious tenant-isolation breach.
  - _Why false positive:_ revert helper deletes just-inserted custom_tools row by server-held primary key in internal validation flow
- **server/tools.ts:3198** _(fk-transitive)_ — INSERT into `project_files` does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`. Tenant_id columns are required by policy and must be explicitly set on INSERT.
  - _Why false positive:_ project_files has no tenant_id and is transitively isolated via prior tenant-scoped project ownership check on project_id
- **server/tools.ts:3212** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (...)`. This can cause cross-tenant contamination because tenant_id is not written.
  - _Why false positive:_ project_notes has no tenant_id and insert is guarded by tenant-scoped parent project ownership check
- **server/tools.ts:3236** _(fk-transitive)_ — INSERT into `project_conversations` does not include tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)`. Creating rows without tenant_id can produce cross-tenant links and contamination.
  - _Why false positive:_ project_conversations has no tenant_id and both parent project and conversation are verified in-tenant before insert
- **server/tools.ts:4728** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT uses (project_id, note, author, created_at) but omits tenant_id, violating the rule that tenant_id must be explicitly set on tenant-scoped inserts.
  - _Why false positive:_ project_notes has no tenant_id column and is inserted via tenant-verified project_id after assertConversationInTenant/assertProjectInTenant.
- **server/tools.ts:4732** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT uses (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) but omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and is attached to a tenant-verified project via project_id; child table is transitively isolated.
- **server/tools.ts:6579** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' (from generate_audio) omits tenant_id. The INSERT uses project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by but does not set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column; insert is gated by assertProjectInTenant and uses the verified project_id parent.
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The UPDATE (SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id}) can modify rows across tenants if `id` is controlled or reused.
  - _Why false positive:_ id being updated comes directly from a prior SELECT on memory_entries constrained by tenant_id, persona_id, and category in the same flow.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The batch UPDATE (SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids})) updates rows by id only, without ensuring those ids belong to the caller's tenant.
  - _Why false positive:_ ids array is derived from rows just selected with tenant_id = tid in the same call path before the batch UPDATE.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` lacks a tenant_id constraint. The UPDATE (SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id}) does not include tenant_id in the WHERE clause, allowing cross-tenant modification if ids are mis-bound.
  - _Why false positive:_ updated id comes from a preceding SELECT on memory_entries filtered by tenant_id and persona_id in the same handler.
- **server/ab-optimizer.ts:110** _(global-table)_ — SELECT FROM tenant-scoped table `experiments` without restricting to a tenant (no tenant_id in WHERE). This reads experiment rows across all tenants.
  - _Why false positive:_ this is an explicit heartbeat/background sweep over running experiments across tenants, and subsequent updates use each row's tenant_id.
- **server/agentic-engines.ts:160** _(global-table)_ — SELECT FROM tenant-scoped table `ai_insights` without any tenant filter (durability sweep reads rows across all tenants).
  - _Why false positive:_ this is an explicit background durability sweep intended to process pending high-priority insights platform-wide, validating each row's tenant_id before routing.
- **server/agentic-engines.ts:342** _(global-table)_ — SELECT against `heartbeat_logs` omits tenant scoping (no tenant_id filter). This reads log data across all tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and appears to be a platform-global operational log table.
- **server/agentic-engines.ts:448** _(global-table)_ — SELECT FROM `heartbeat_logs` (recentLogs) lacks tenant scoping (no tenant_id in WHERE).
  - _Why false positive:_ heartbeat_logs is schema-noted as non-tenant-scoped; reading recent success logs matches intentional global operational telemetry.
- **server/agentic-engines.ts:516** _(global-table)_ — SELECT aggregating `heartbeat_logs` (heartbeatPerformance) runs without tenant restriction (reads/aggregates across tenants).
  - _Why false positive:_ heartbeat_logs lacks tenant_id and this aggregate is over global heartbeat telemetry by design.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — UPDATE on agent_runs (appendStep) uses WHERE id = <runId> with NO tenant_id constraint. This allows modifying runs belonging to other tenants when called with an arbitrary runId.
  - _Why false positive:_ appendStep updates agent_runs by server-generated run id in an internal worker helper; no untrusted request boundary is shown here.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — UPDATE on agent_runs (updateRunState) uses WHERE id = <runId> with NO tenant_id constraint, allowing cross-tenant state mutation.
  - _Why false positive:_ updateRunState is an internal server-side run-state mutation by globally unique run id, not shown as sourced from caller input.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — UPDATE on agent_runs (replaceRunState) updates by run id only (WHERE id = <runId>) with NO tenant_id constraint, enabling cross-tenant updates.
  - _Why false positive:_ replaceRunState is an internal helper updating by server-generated primary key with no visible untrusted tenant boundary.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — UPDATE on agent_runs (completeRun) uses WHERE id = <runId> with NO tenant_id constraint; this can mark runs for other tenants completed.
  - _Why false positive:_ completeRun is a server-invoked state transition on agent_runs by globally unique run id, matching internal worker PK usage.
- **server/agentic/runs.ts:67** _(internal-worker-pk)_ — UPDATE on agent_runs (failRun) uses WHERE id = <runId> with NO tenant_id constraint, allowing an attacker or bug to fail runs across tenants.
  - _Why false positive:_ failRun is an internal state-transition helper updating by server-generated run id, not a tenant-facing selector.
- **server/agentic/runs.ts:76** _(internal-worker-pk)_ — UPDATE on agent_runs (pauseRun) updates runs by id only (WHERE id = <runId>) with NO tenant_id constraint, enabling cross-tenant status changes.
  - _Why false positive:_ pauseRun is likewise an internal run lifecycle update by globally unique primary key with no shown untrusted source.
- **server/auto-asset-capture.ts:207** _(upstream-verified)_ — SELECT content FROM `messages` (by conversation_id) is executed without an explicit tenant_id filter. Although the conversation rows were enumerated earlier, the messages SELECT itself lacks a tenant constraint.
  - _Why false positive:_ messages are read only for conversation ids obtained from the immediately preceding trusted conversations query in the same function
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT of messages for a conversation reads from `messages` without an explicit tenant_id constraint (WHERE conversation_id = ${conversationId} ...). The query relies on conversation_id but does not enforce tenant_id equality in the WHERE clause.
  - _Why false positive:_ same call path first verifies the conversation belongs to tenantId with SELECT ... FROM conversations WHERE id = conversationId AND tenant_id = tenantId
- **server/ceo-orchestrator.ts:821** _(global-table)_ — Unrestricted SELECT from personas table: cachedPersonas = await db.select().from(personasTable) reads all persona rows with no tenant_id constraint. This exposes/uses persona rows across tenants.
  - _Why false positive:_ schema says personas has no tenant_id, so reading all personas matches a platform-global table pattern
- **server/channel-routing.ts:54** _(global-table)_ — setChannelRoute inserts/updates a global channel_routes row using a caller-supplied personaId with no ownership check. This allows a caller to register a persona_id they do not own (cross-tenant reference) into a global routing table.
  - _Why false positive:_ personas has no tenant_id and channel_routes is used as a global routing table, so no tenant ownership check applies here
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in the WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — missing tenant constraint.
  - _Why false positive:_ getCredential(id, tenantId) is called immediately above and returns null unless the row matches the tenant; UPDATE uses that verified row id in same call path.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in the WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — missing tenant constraint.
  - _Why false positive:_ getCredentialForSite(siteUrl, tenantId) verifies tenant ownership before this last_used_at update on the returned cred.id.
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM `doc_chunks` in addDocument uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but does NOT include tenant_id. This is a DELETE against a tenant-scoped table without explicit tenant scoping in the WHERE clause.
  - _Why false positive:_ A few lines above, doc_collections is checked with id + tenant_id; delete is constrained by that verified collection_id in the same call path.
- **server/email.ts:424** _(upstream-verified)_ — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} is executed without tenant_id in the WHERE clause, allowing an update to affect another tenant's row if id collisions occur or if row.id belongs to a different tenant.
  - _Why false positive:_ UPDATE uses id from immediately prior unscoped SELECT in same function; no new cross-tenant boundary at this line beyond finding 1.
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with a string-interpolated vector literal built from the caller's query: sql.raw(`'${vec}'::vector`). The vector literal is constructed from request-derived input (via generateEmbedding(query)) and injected into raw SQL via string interpolation.
  - _Why false positive:_ Query is explicitly constrained by tenant_id = tenantId a few lines below in same statement.
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw() with a string-interpolated vector literal built from the caller's query: sql.raw(`'${vec}'::vector`) in vectorSearchKnowledge. This injects a request-derived value into raw SQL.
  - _Why false positive:_ Knowledge vector query includes AND tenant_id = tenantId in same SQL statement.
- **server/firecrawl.ts:192** _(upstream-verified)_ — UPDATE on scrapedPages uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint. This allows updating another tenant's row if an id from a different tenant is used or if race conditions occur.
  - _Why false positive:_ Row id being updated comes from prior SELECT scoped by url and tenantId in same call path.
- **server/firecrawl.ts:316** _(upstream-verified)_ — UPDATE on scrapedPages inside the crawl loop uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint.
  - _Why false positive:_ Row id being updated comes from prior SELECT scoped by pageUrl and tenantId in same loop iteration.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from projects queries the project by id (where eq(projects.id, projectId)) without scoping the read to the caller's tenant (no WHERE projects.tenant_id = tenantId). Reading an arbitrary project id can leak another tenant's data and violates the tenant-isolation invariant.
  - _Why false positive:_ Function immediately checks fetched project's tenantId against caller tenantId and fails closed before any return/use.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) with no tenant_id constraint. Without an explicit tenant filter this UPDATE could modify another tenant's project record.
  - _Why false positive:_ UPDATE occurs only after same-function ownership check project.tenantId !== tenantId throws on mismatch.
- **server/google-drive.ts:829** _(upstream-verified)_ — SELECT to load a project in uploadAndShare uses where(eq(projects.id, params.projectId)) with no tenant_id constraint. This reads a project row by id without scoping to the caller's tenant (even though ownership is checked after the read).
  - _Why false positive:_ Caller immediately validates proj.tenantId against params.tenantId and aborts on mismatch before proceeding.
- **server/google-drive.ts:1004** _(upstream-verified)_ — searchDriveFiles: early ownership verification SELECT reads projects by id via where(eq(projects.id, params.projectId)) without also restricting to tenant_id. The query itself is not tenant-scoped.
  - _Why false positive:_ Query is immediately followed by tenantId comparison and abort before any tenant-scoped use; serves as ownership check in same call path.
- **server/google-drive.ts:1066** _(upstream-verified)_ — searchDriveFiles pass-2 Drive fallback selects projects.driveFolderId with where(eq(projects.id, params.projectId)) and no tenant_id constraint. This read can return another tenant's folder id if projectId is untrusted.
  - _Why false positive:_ Earlier same function verifies project ownership against params.tenantId before this folder-id lookup when projectId is present.
- **server/heartbeat.ts:101** _(upstream-verified)_ — UPDATEs against tenant-scoped table "research_schedules" use WHERE id = ${sched.id} but do not include tenant_id in the WHERE clause (other similar updates at lines 106, 112, 120). This allows updating rows across tenants or otherwise bypassing application-level tenant isolation.
  - _Why false positive:_ sched rows come from prior SELECT on research_schedules; updates target the same fetched row id/tenant_id in an internal worker loop, not a request boundary.
- **server/heartbeat.ts:257** _(upstream-verified)_ — Direct UPDATE on tenant-scoped table "heartbeat_tasks" (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}) without tenant_id in the WHERE clause. This occurs at multiple startup-fix points (e.g. lines 257, 261) and can modify another tenant's row if ids overlap or if row ownership is not enforced by DB.
  - _Why false positive:_ t comes from storage.getHeartbeatTasks() in internal startup cleanup; update targets previously fetched row by its server-derived id.
- **server/heartbeat.ts:1646** _(global-table)_ — SELECT from tenant-scoped table "heartbeat_logs" (SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ...) without tenant_id filtering. This reads logs across tenants and can leak timing/state between tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this is not a tenant-scoped table.
- **server/heartbeat.ts:2955** _(upstream-verified)_ — Direct UPDATE on tenant-scoped table "heartbeat_tasks" to set approval_status (UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}) without tenant_id in WHERE. Same pattern also used at line 3407. Writes may affect rows outside the caller's tenant context.
  - _Why false positive:_ newTask.id comes from immediately preceding storage.createHeartbeatTask with trusted tenantId in same flow; follow-up update targets that just-created row.
- **server/heartbeat.ts:2609** _(global-table)_ — Call to storage.getPersonas() without passing tenantId inside buildTaskContext. storage.getPersonas likely returns tenant-scoped personas; calling it without tenant scoping can leak personas from other tenants into the task context.
  - _Why false positive:_ personas schema shows no tenant_id; read appears intentionally global, so no tenant-isolation boundary on this table
- **server/heartbeat.ts:2726** _(global-table)_ — Call to storage.getPersonas() without passing tenantId inside processTaskOutput (used to find "Proof" persona). This can read personas across tenants and allow cross-tenant delegation or data leakage.
  - _Why false positive:_ personas has no tenant_id and Proof lookup is against a global persona registry, not a tenant-scoped table
- **server/heartbeat.ts:2861** _(global-table)_ — Call to storage.getPersonas() without passing tenantId inside processDelegations. Delegation logic uses persona lookup/validation but does not verify persona ownership by tenant, risking delegating tasks to personas from other tenants.
  - _Why false positive:_ personas is not tenant-scoped per schema, so resolving delegation targets from it is not a cross-tenant row-isolation issue
- **server/heartbeat.ts:3053** _(global-table)_ — delegateTaskFromChat calls storage.getPersonas() without passing tenantId even though tenantId is provided to the function. This reads personas across tenants and then uses the result to resolve target persona, risking cross-tenant impersonation/leak.
  - _Why false positive:_ personas table has no tenant_id; using global persona definitions here is consistent with platform-global data
- **server/internal-resolver.ts:91** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `event_log` (finalizeEvent) is executed without any tenant_id constraint. This can allow a resolver to finalize/modify events belonging to other tenants.
  - _Why false positive:_ server-invoked resolver updates event_log by server-generated eventId primary key, not from a request boundary
- **server/internal-resolver.ts:106** _(internal-worker-pk)_ — SELECT from `plans` by id (resolvePlanProposed) is performed with no tenant_id filter. The code may read and act on a plan belonging to a different tenant.
  - _Why false positive:_ resolver reads plans by server-supplied planId tied to an internal event-processing path, not caller-controlled tenant input
- **server/internal-resolver.ts:170** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `code_proposals` (resolveExperimentFailed) uses WHERE id = ${proposalId} AND status = 'pending' with no tenant_id check. This may archive or modify another tenant's proposal.
  - _Why false positive:_ internal resolver archives code_proposals by proposalId extracted from internal event data; this is a server worker PK path, not direct untrusted input
- **server/job-queue.ts:98** _(global-table)_ — claimDueJobs performs a global UPDATE/SELECT on `agent_jobs` without scoping by tenant_id. This atomically claims jobs across all tenants, which can lead to cross-tenant execution or races with tenant-specific expectations.
  - _Why false positive:_ claiming jobs is a platform-global worker-queue maintenance operation; agent_jobs is processed intentionally across tenants and returned with tenant_id for downstream context
- **server/job-queue.ts:131** _(global-table)_ — reclaimExpiredLeases updates `agent_jobs` across all tenants (two global UPDATEs) with no tenant_id restriction. This mutates tenant-scoped rows for all tenants in one statement.
  - _Why false positive:_ lease reclamation is a global queue maintenance sweep across agent_jobs, an intentionally cross-tenant worker operation
- **server/memory-graph.ts:181** _(fk-transitive)_ — SELECT from memory_links to check existence of a link lacks tenant scoping (SELECT 1 FROM memory_links WHERE ...). If memory_links is tenant-scoped this can read other tenants' relations.
  - _Why false positive:_ schema note says memory_links has no tenant_id; link rows are transitively tenant-scoped through source/target memory_entries selected from the caller tenant.
- **server/memory-graph.ts:195** _(fk-transitive)_ — INSERT INTO memory_links does not set tenant_id. If memory_links is a tenant-scoped table this creates rows with no tenant binding (or defaults), allowing cross-tenant link pollution.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries by FK-like ids; isolation is transitive through memory_entries
- **server/oauth-subscriptions.ts:956** _(internal-worker-pk)_ — UPDATE on oauth_subscriptions uses WHERE id = ${sub.id} without also constraining tenant_id. Writing to a tenant-scoped table without tenant_id increases risk of modifying another tenant's row if IDs collide or are guessed.
  - _Why false positive:_ startup validation loop is internal server worker logic operating on previously selected subscription rows by server-side primary key
- **server/oauth-subscriptions.ts:981** _(internal-worker-pk)_ — UPDATE on oauth_subscriptions uses WHERE id = ${sub.id} (no tenant_id constraint) when deactivating stale subscriptions.
  - _Why false positive:_ internal refresh/validation worker deactivates the selected subscription row by server-obtained primary key, not untrusted request input
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} reads a persona row by id without constraining tenant_id, even though tenantId is available to the function. This can expose persona data belonging to other tenants.
  - _Why false positive:_ schema marks personas with no tenant_id; persona row itself is platform-global while tenant-scoped companion data is filtered separately
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog updates plans without any tenant_id constraint: `UPDATE plans SET ... WHERE id = ${planId}`. This lets callers append/modify execution_log for any tenant's plan by id.
  - _Why false positive:_ appendExecutionLog is internal executor code updating a plan by server-driven planId, not a direct untrusted tenant boundary
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — Code sets a plan to 'failed' using `UPDATE plans SET status = 'failed' WHERE id = ${planId}` without restricting by tenant_id, enabling cross-tenant state changes.
  - _Why false positive:_ Internal executor updates by server-generated planId after claiming the same row and obtaining row.tenant_id; not from untrusted request boundary.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path recovery updates plans with `UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing'` without scoping to tenant_id, allowing cross-tenant modifications on exception handling paths.
  - _Why false positive:_ Catch-path status update is in the same internal worker flow keyed by server-invoked planId, not caller-supplied tenant context.
- **server/plan-executor.ts:784** _(global-table)_ — Query selecting pending plans reads across all tenants: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`. This returns plans from any tenant to be re-fired without tenant scoping.
  - _Why false positive:_ Boot-time recovery is a platform-wide internal scheduler intentionally scanning approved plans across tenants for execution.
- **server/process-governor.ts:121** _(fk-transitive)_ — Subqueries count heartbeat_logs without constraining tenant_id. The SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ... does not include tenant_id, so it can read logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted via task_id = ht.id where parent heartbeat_tasks is tenant-scoped in outer query.
- **server/process-governor.ts:198** _(global-table)_ — SELECT AVG(duration_ms) FROM heartbeat_logs lacks a tenant filter. This reads performance logs across all tenants.
  - _Why false positive:_ heartbeat_logs is a non-tenant-scoped platform log table; this computes a global response-time metric by design.
- **server/process-governor.ts:240** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... does not constrain tenant_id. This can count persona failures across tenants.
  - _Why false positive:_ heartbeat_logs is a non-tenant-scoped platform log table; this query measures global cascading failures across agents.
- **server/routes.ts:4337** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The code inserts `INSERT INTO project_notes (project_id, note, author) VALUES (...)` using a project_id retrieved without tenant validation, which can create rows missing tenant context or associate notes with another tenant's project.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects via project_id
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE against tenant-scoped table 'conversations' omits tenant_id in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Why false positive:_ conv was just created with tenantId and projectId was verified by SELECT on projects with tenant_id = tenantId
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project and conversation already validated/created in this tenant
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE against tenant-scoped table 'projects' omits tenant_id in WHERE clause (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}).
  - _Why false positive:_ projectId was immediately verified in the same block with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/conversations.ts:227** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)).
  - _Why false positive:_ project_notes has no tenant_id and insert uses projId selected by join to projects constrained to conv.tenantId/tenantId
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table 'inbox_messages' omits tenant_id in WHERE clause (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}) when marking a message read while serving GET /api/inbox/:id.
  - _Why false positive:_ same handler selected msg by id and tenant_id before updating that exact row by msg.id
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ... runs without an explicit tenant_id predicate. Although the code checks the project belongs to the caller earlier, the query itself lacks a tenant_id filter and therefore violates the invariant that tenant-scoped tables must be constrained by tenant_id at the SQL level.
  - _Why false positive:_ project_files has no tenant_id and access is transitively scoped through prior verified tenant-owned project
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 has no tenant_id constraint. The query could return a row from a wrong tenant if project-file FKs are malformed or not enforced by the DB.
  - _Why false positive:_ project_files has no tenant_id and query is scoped by project_id after verifying the project belongs to tenant
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} omits tenant_id. The deletion relies on a prior application-level project check but does not include tenant_id in the DELETE itself, violating the requirement that tenant-scoped table mutations include tenant predicates.
  - _Why false positive:_ project_files has no tenant_id and delete targets child rows of a project already verified as tenant-owned
- **server/routes/oauth-subscriptions.ts:325** _(upstream-verified)_ — SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE reads tenant-scoped table across all tenants without a tenant_id constraint. This is used in the cron 'heal' flow and enumerates tenant IDs globally.
  - _Why false positive:_ Cron handler is gated by CRON_SECRET Authorization check and intentionally performs platform-wide heal enumeration.
- **server/seed.ts:3204** _(fk-transitive)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant filter. Conversations are tenant-scoped, so this query can return rows from other tenants.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table isolated through tenant-scoped conversations/projects by foreign keys.
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from `messages` uses only conversationId (eq(messages.conversationId, conversationId)) with NO tenant_id constraint. This performs a read on a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ Same call path verifies conversation tenant immediately above before loading child messages for that validated conversation.
- **server/skillify.ts:91** _(upstream-verified)_ — In distillIntent, recent messages are queried from `messages` by conversationId without a tenant_id constraint. This allows returning messages across tenants if conversationId is attacker-controlled or misused.
  - _Why false positive:_ distillIntent first calls analyzeConversation(conversationId, tenantId), which verifies conversation ownership before this message read.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from `heartbeat_logs` aggregates error counts across rows but does not include a tenant_id WHERE clause. This reads tenant-scoped data across all tenants without explicit scoping.
  - _Why false positive:_ heartbeat_logs is schema-noted as having no tenant_id and is used by a global watchdog/heartbeat facility.
- **server/storage.ts:289** _(upstream-verified)_ — attachCitationMetadata executes a raw SELECT FROM messages WHERE id IN (...) but does not include a tenant_id constraint — allowing reads of message rows across tenants by id.
  - _Why false positive:_ attachCitationMetadata only re-reads rows for ids taken from msgs already fetched by tenant-scoped getMessages/getMessagesPaginated immediately above.
- **server/storage.ts:1421** _(upstream-verified)_ — supersedeAdr updates architecture_decisions (marking the old ADR superseded) using WHERE id = oldId without including tenant_id in the UPDATE — even though ownership was checked earlier, the UPDATE itself is not tenant-scoped.
  - _Why false positive:_ same function first selects oldAdr/newAdr with id+tenantId and only updates if both tenant-owned rows exist.
- **server/storage.ts:1424** _(upstream-verified)_ — supersedeAdr updates the new ADR row with WHERE id = newId but without tenant scoping — write is not constrained to tenant_id even though earlier selects checked ownership.
  - _Why false positive:_ newId ownership is verified a few lines above via select on id+tenantId before this update runs.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (eq(customTools.tenantId, tenantId)). This allows a cross-tenant lookup/leak or logic acting on the wrong tenant's row.
  - _Why false positive:_ name is first rewritten with tenantToolName(parsed.name, tenantId), so lookup is already tenant-derived and checks only that scoped name for uniqueness.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` by id without filtering by tenant_id (eq(customTools.tenantId, tenantId)). Reading rows by id across tenants can leak or return the wrong tenant's data.
  - _Why false positive:_ row was just inserted in the same call with tenantId and the reread is by returned inserted.id, not untrusted tenant-crossing input.
- **server/tool-learning.ts:292** _(upstream-verified)_ — UPDATE against tenant-scoped table `custom_tools` (.where(eq(customTools.id, tool.id))) lacks a tenant_id constraint. This can update a row belonging to a different tenant if IDs collide or are guessed.
  - _Why false positive:_ tool comes from the just-inserted/selected row in the same flow, so tool.id is already the validated target row for this internal auto-fix update.
- **server/tool-learning.ts:367** _(upstream-verified)_ — UPDATE to increment usageCount on tenant-scoped `custom_tools` uses only id in WHERE (where(eq(customTools.id, tool.id))) and omits tenant_id, allowing cross-tenant modification.
  - _Why false positive:_ tool was fetched immediately above with name plus tenant_id, so updating by that retrieved tool.id is within the same verified tenant-owned row.
- **server/tools.ts:3129** _(fk-transitive)_ — Query builds per-project counts using subselects like `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)` / `project_notes` / `project_conversations` WITHOUT constraining those tenant-scoped tables by tenant_id. Even though the outer projects row is tenant-limited, these unscoped reads against other tenant-scoped tables violate the invariant and can leak cross-tenant data.
  - _Why false positive:_ project_conversations has no tenant_id and is joined through tenant-scoped projects; conversation row also has explicit c.tenant_id filter.
- **server/tools.ts:3191** _(fk-transitive)_ — Duplicate-check for add_file uses `SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ...` WITHOUT `tenant_id` — reading a tenant-scoped table without tenant constraint.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; owner check on projects enforces tenant before duplicate lookup by project_id.
- **server/tools.ts:3232** _(fk-transitive)_ — Lookups against `project_conversations` use `SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId}` WITHOUT a tenant filter on project_conversations.
  - _Why false positive:_ project_conversations has no tenant_id and link rows are isolated transitively via tenant-checked parent project_id; conversation tenant is separately verified before use.
- **server/tools.ts:3246** _(fk-transitive)_ — Search query for projects includes subselects `(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)` and `(SELECT COUNT(*) FROM project_notes WHERE project_id = p.id)` without tenant constraints. These are reads against tenant-scoped tables with no tenant_id in the predicate.
  - _Why false positive:_ project_notes/project_files/project_conversations have no tenant_id and are transitively scoped via tenant-checked projects/project_id
- **server/tools.ts:7113** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The code inserts a row for a project file without persisting the tenant_id column even though tenant-isolation is enforced only at the application layer (no RLS).
  - _Why false positive:_ project_files has no tenant_id column; insert is correct via tenant-scoped parent project validated by assertProjectInTenant
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table 'browser_workflows' without tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}) — row update is scoped only by id, allowing cross-tenant modification if an attacker can guess an id.
  - _Why false positive:_ workflow row was loaded immediately above with tenant_id = tenantId, and update uses that verified row id
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' without tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}) — even though projId was derived earlier, this UPDATE lacks an explicit tenant filter and could modify a project id from a different tenant if validation changes or is bypassed.
  - _Why false positive:_ projId comes from same-path SELECT joining conversations to projects with p.tenant_id = tenantId before the update
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` uses WHERE id = ${row.id} with NO tenant_id constraint. This UPDATE can mutate rows without explicitly constraining to the caller's tenant.
  - _Why false positive:_ rows being updated were selected just above from knowledge_triples with tenant_id = tenantId and valid_until IS NULL
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys executed without a tenant_id filter. The query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` can return rows for all tenants (sensitive provider API key presence/length), but no application-level tenant constraint is enforced.
  - _Why false positive:_ provider_keys schema note shows no tenant_id column and code intentionally restricts this audit to admin tenant only
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT/DISTINCT on provider_keys executed without a tenant_id filter. The query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` runs under the admin branch but lacks a tenant constraint and can aggregate across tenants.
  - _Why false positive:_ provider_keys has no tenant_id column and this aggregate runs only in the admin-only branch
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without a tenant_id constraint. Any SELECT against a tenant-scoped table must include tenant scoping or explicit ownership checks.
  - _Why false positive:_ query is intentionally verifying whether conversation tenant_id matches claimed pairing tenantId; ownership is checked immediately after fetch
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} updates a tenant-scoped table by id only, without constraining tenant_id. This could allow a caller to affect rows belonging to other tenants if reservationId is attacker-controlled or leaked.
  - _Why false positive:_ server-side helper updates by reservation serial PK only; id is an internal reservation handle, not shown as coming from request input
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (releaseVentureReservation) modifies a tenant-scoped table without constraining tenant_id (same risk as settleVentureReservation).
  - _Why false positive:_ best-effort internal worker cleanup by server-generated reservation PK, not an untrusted cross-tenant request id
- **server/video-job-runner.ts:202** _(internal-worker-pk)_ — SELECT on tenant-scoped table videoJobs (isCancelRequested reads by jobId only) lacks tenant scoping. isCancelRequested(jobId) queries cancelRequested WHERE jobId = ${jobId} without verifying tenant ownership.
  - _Why false positive:_ Server worker helper reads by globally-unique jobId for runner cancel polling; shown call path is internal, not from req/body/params.
