# Tenant-Isolation Audit Report

_Generated 2026-09-14T01:14:16.020Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

_Revalidated 2026-09-15 without a model rerun: focused executable regressions and independent review checked every previously kept/deferred severe finding against current code. Seven stale findings are closed by tenant-authority fixes; three are documented capability/upstream-authority false positives. The only later video-editor change makes external Drive upload explicit opt-in; an incomplete path-authority experiment was removed after review. The full 673-file corpus and historical finding trail are retained._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **673/673** file(s) audited · chunks 62/62 OK (0 failed)
- Findings: **234** (CRITICAL 37 · HIGH 155 · MEDIUM 38 · LOW 4)
- Severe dispositions: **0 kept** · 148 precision false-positive · 44 allowlist false-positive · 0 deferred real risk

### CRITICAL (37)

- **server/agent-eval.ts:221** — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with NO tenant_id check — write can affect another tenant's row if evalId is guessed or reused.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}. Always include tenant_id on updates/deletes for tenant-scoped tables.
- **server/agentic/approvals.ts:135** — Bulk UPDATE in expireStaleApprovals updates agentApprovals rows across all tenants (no tenant_id constraint), then returns rows for downstream per-run updates. This can modify approvals for other tenants.
  - _Fix:_ Scope the expiry to a specific tenant (accept/require tenantId) or perform the expiry in a per-tenant loop. Ensure the initial UPDATE's WHERE includes tenant_id = <tenant> (or otherwise guarantees only the caller's tenant rows are modified).
- **server/agentic/resume-worker.ts:65** — SELECT against tenant-scoped table(s) (agent_runs/agent_approvals) executed without constraining to a caller/worker tenant_id. The initial candidate query (db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`)) reads rows across all tenants.
  - _Fix:_ Do not perform cross-tenant reads. Constrain the query to a specific tenant_id (e.g. WHERE r.tenant_id = ${tenantId}) or run a per-tenant sweep. Replace the raw SQL with a parameterized Drizzle query using .where(eq(agentRuns.tenantId, tenantId)) (or otherwise ensure the caller's tenant is applied). If the worker must operate globally, explicitly iterate tenants with proper authorization and per-tenant filtering so no unbounded cross-tenant read occurs.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table 'conversations' lacks tenant_id in the WHERE clause (writes project_id WHERE id = ${c.conversation_id} AND project_id IS NULL). This can modify rows belonging to other tenants if ids collide.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL or WHERE id = ${c.conversation_id} AND tenant_id = ${tenantId} AND project_id IS NULL) so writes are limited to the caller's tenant.
- **server/commitment-drafter.ts:234** — UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} — missing tenant_id constraint. This can modify a commitment belonging to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id to enforce tenant isolation. For example: UPDATE commitments SET draft_status='open', drafted_at=NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}. Prefer using the tenantId already available in the claimed row to ensure the revert only affects that tenant's row.
- **server/email.ts:429** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} has no tenant_id constraint. This write can modify rows belonging to other tenants (cross-tenant persistence).
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or derive/associate the tenant for the row before updating. Prefer tenant-scoped update APIs to avoid missing tenant filters.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed with no tenant_id constraint. This write will update memory rows regardless of tenant ownership (cross-tenant write risk).
  - _Fix:_ Change the UPDATE to include AND tenant_id = ${tenantId} (and ensure tenantId is obtained from the selected row) or use a tenant-scoped update API. Never perform UPDATEs on tenant-scoped tables by id only.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed with no tenant_id constraint. This write can change rows belonging to other tenants (cross-tenant persistence).
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (derive tenantId from the selected row) or use storeEmbeddingVec with a validated tenantId so only the owning tenant's rows are updated.
- **server/embeddings.ts:806** — storeEmbeddingVec contains a fallback branch that runs UPDATE ${sql.raw(table)} ... WHERE id = ${id} when options.tenantId is undefined. That performs an unconditional write to a tenant-scoped table (memory_entries or agent_knowledge) without tenant filtering.
  - _Fix:_ Do not allow the unconditional-update branch. Require a positive tenantId for writes, or always include a tenant_id constraint in the UPDATE. Replace sql.raw(table) + id-only WHERE with tenant-scoped updates (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}).
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot performs a SELECT from the tenant-scoped table `agent_runs` without constraining rows to a specific tenant (no WHERE tenant_id = ...). This reads rows across all tenants and then uses each row's tenant_id to populate in-memory task state, violating the invariant that every read against tenant-scoped tables must include an application-level tenant filter.
  - _Fix:_ Restrict the query to an explicit tenant context. Either accept a tenantId parameter (or iterate an explicit, authorized tenant list) and run the SELECT per-tenant with WHERE tenant_id = ${tenantId}, or otherwise enforce an admin-only, audited path that explicitly documents and approves cross-tenant reads. Validate tenant_id before using rows to populate taskStateStore and avoid any blind multi-tenant SELECTs that return other tenants' data.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` (setting status = 'archived_stale') uses WHERE id = ANY(${ids}) with no tenant_id constraint — may update rows belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform per-tenant updates after verifying ownership. Alternatively, scope the initial SELECT by tenant and carry the tenant_id through to the UPDATE.
- **server/health-audit.ts:366** — UPDATE on tenant-scoped table `heartbeat_tasks` (disabling tasks) uses WHERE id = ANY(${ids}) with no tenant_id guard — can modify tasks for other tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or perform per-tenant updates after verifying each row's tenant ownership.
- **server/google-workspace.ts:2287** — INSERT into `presenter_slide_images` omits tenant scoping (no tenant_id column/value supplied). If `presenter_slide_images` is tenant-scoped this allows writes not bound to a tenant and can associate images with sessions across tenants.
  - _Fix:_ Include the tenant_id explicitly in the INSERT values (e.g. INSERT ... (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)), or validate that session_id belongs to the expected tenant before inserting. Prefer per-tenant DB constraints in the WHERE/VALUES used by the app layer.
- **server/google-workspace.ts:2293** — Second INSERT into `presenter_slide_images` (thumb variant) also omits tenant scoping (no tenant_id supplied).
  - _Fix:_ Same fix: include tenant_id in the INSERT values or verify the session_id's tenant before persisting thumbnails so writes cannot cross tenant boundaries.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id filter. This UPDATE can modify a presenter's session data regardless of tenant ownership.
  - _Fix:_ Constrain the UPDATE to the owning tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or otherwise verify session ownership before updating. Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/job-queue.ts:154** — claimDueJobs updates agent_jobs rows (SET status='running' ...) using id IN (SELECT id FROM agent_jobs WHERE status='pending' ...) but does not constrain the SELECT or UPDATE by tenant_id. This lets the worker atomically claim and mutate pending jobs across all tenants by id without any tenant_id WHERE clause.
  - _Fix:_ Restrict claims to a tenant scope (e.g. include AND tenant_id = <tenant> in the SELECT/UPDATE) or run one worker per-tenant. Alternatively, make the claim path require the caller to provide an explicit, verified tenantId and add WHERE tenant_id = ${tenantId} to both the inner SELECT and the outer UPDATE so jobs are only claimed within the intended tenant.
- **server/job-queue.ts:192** — reclaimExpiredLeases performs two UPDATEs over agent_jobs (marking reclaimed/terminal rows) without any tenant_id filter. This will mutate running/expired leases across all tenants in a single global operation.
  - _Fix:_ Scope reclaimed/terminal transitions to an explicit tenant (or iterate tenants atomically). Add AND tenant_id = ${tenantId} to the UPDATEs, or run the reclaim logic per-tenant so a bug or malicious id can't cause cross-tenant state changes.
- **server/job-queue.ts:272** — failJob reads and updates agent_jobs by id without scoping to tenant_id. It SELECTs max_attempts FROM agent_jobs WHERE id = ${id} and then issues UPDATEs (to failed_terminal or pending) using only id/status/attempts guards — no tenant_id. This allows cross-tenant status transitions and error writes if id is misused.
  - _Fix:_ Include tenant_id in the SELECT/UPDATE guards or require the caller to provide a verified tenantId and add AND tenant_id = ${tenantId} to every UPDATE/SELECT. Avoid mutating rows by id-only without tenant fencing; perform a tenant-checked lookup first and fail if tenant mismatch is detected.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id predicate (CTE selects due rows across all tenants and the subsequent UPDATE updates rows across tenants). This allows cross-tenant reads/updates.
  - _Fix:_ Restrict the CTE and the UPDATE to a single tenant context (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant (use withTenantTx or equivalent) so every read/write against scheduled_posts is explicitly filtered by the caller's tenant_id. If the runner intentionally must operate across tenants, make that explicit and perform per-tenant scoped transactions to preserve the tenant-isolation invariant.
- **server/plan-executor.ts:557** — CAS claim UPDATE on plans omits a tenant_id constraint (UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved'). This allows an attacker who can supply a planId to flip status across tenants (cross-tenant write) because the claim is not scoped to the plan's tenant.
  - _Fix:_ Include the plan's tenant in the WHERE clause when doing the CAS claim (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved'), or first SELECT the row, validate the tenant ownership against the caller's tenant, then do a tenant-scoped CAS UPDATE. Ensure the caller's tenantId is derived from a validated server-side context, not client input.
- **server/project-brain.ts:332** — Backfill query selects projects/conversations across the entire DB without tenant scoping: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... WHERE (SELECT COUNT(*) FROM messages ... ). This will enumerate projects from all tenants.
  - _Fix:_ Restrict the backfill to a single tenant at a time: accept/derive tenantId and add WHERE p.tenant_id = ${tenantId} (and similarly filter conversations/messages by tenant_id) so no cross-tenant reads occur.
- **server/project-brain.ts:359** — Query that reads messages for a conversation lacks tenant scoping: SELECT role, content FROM messages WHERE conversation_id = ${conv.id}. Messages are tenant-scoped and contain sensitive content; reading them without tenant filters leaks cross-tenant data.
  - _Fix:_ Add tenant filtering (e.g. AND tenant_id = ${tenantId}) and/or verify conversation ownership before selecting messages. Ensure backfill/update routines accept and enforce a tenantId parameter so message reads are restricted to that tenant.
- **server/research-engine.ts:169** — UPDATE against tenant-scoped table research_sessions issued without any tenant_id constraint (cleanupZombieSessions). This will mark sessions across all tenants as completed/ended, violating tenant isolation.
  - _Fix:_ Constrain the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId} in the WHERE) or perform the cleanup per-tenant. If intent is global, re-evaluate design (row-level security or explicit tenant scoping). Ensure any id lists are parameterized and that only the correct tenant's rows can be updated.
- **server/routes/conversations.ts:119** — UPDATE on conversations omits tenant scoping: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates a tenant-scoped table by id only (no `AND tenant_id = ${tenantId}`).
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. `WHERE id = ${conv.id} AND tenant_id = ${tenantId}`) or perform the update via a storage/ORM helper that enforces tenant ownership. Always enforce WHERE tenant_id = <caller tenant> on writes to tenant-scoped tables.
- **server/routes/conversations.ts:123** — INSERT into project_conversations omits tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id. Per invariant, every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Persist tenant_id explicitly in the insert (e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`) or use a storage API that adds tenant_id. Also validate ownership of referenced foreign IDs before inserting.
- **server/routes/conversations.ts:125** — UPDATE on projects omits tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` has no `AND tenant_id = ${tenantId}`. This can modify another tenant's project row if ids are guessed or misused.
  - _Fix:_ Add tenant restriction to the UPDATE (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) or use a storage/ORM function that enforces tenant ownership. Always validate and include tenant_id on updates to tenant-scoped tables.
- **server/routes/conversations.ts:256** — INSERT into project_notes omits tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` does not include tenant_id on the inserted row.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`) or call storage APIs that enforce tenant attribution. Ensure all inserts into tenant-scoped tables set tenant_id and that foreign IDs are validated to belong to the caller's tenant before use.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks tenant constraint — will disable delegation tasks across all tenants.
  - _Fix:_ Constrain the UPDATE to the specific tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or iterate per-tenant after validating intent; destructive cross-tenant updates must be avoided.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant constraint — will disable matching tasks for all tenants.
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or run per-tenant) to ensure only the intended tenant's tasks are modified.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true lacks tenant constraint — will disable these 'examine repository' tasks for every tenant.
  - _Fix:_ Include AND tenant_id = ${ADMIN_TENANT_ID} (or loop tenants intentionally) to avoid unintended cross-tenant updates; require explicit admin-scoped intent before making global changes.
- **server/seed.ts:2239** — A backfill SELECT over conversations (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)) is performed with no tenant_id constraint, and then project_conversations rows are inserted for each result. The code does not verify that the conversation and project belong to the same tenant (and project_conversations has no tenant_id), enabling cross-tenant linking/corruption.
  - _Fix:_ Only select conversations belonging to the intended tenant (e.g. WHERE c.tenant_id = ${tenantId}) and verify the project's tenant as well (join projects p ON p.id = c.project_id AND p.tenant_id = c.tenant_id). Either add tenant_id to project_conversations and populate it, or guard inserts by confirming ownership before inserting. Use parameterized Drizzle queries (eq(table.tenantId, tenantId)) rather than global selects.
- **server/storage.ts:1351** — getAllDataForExport performs numerous SELECTs across many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, deliveryLogs, fileStorage, etc.) with no tenant_id constraints — this exposes entire platform data in one call.
  - _Fix:_ Restrict this function to admin-only contexts and require an explicit, strongly-audited admin token. Prefer adding a required tenantId parameter (or explicit ALL_TENANTS sentinel only usable by super-admins) and enforce validation. Log and audit exports and ensure callsites cannot be invoked by regular tenants.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table `custom_tools` without tenant_id constraint (await db.delete(customTools).where(eq(customTools.id, tool.id))). This can remove another tenant's tool if ids collide or are guessed.
  - _Fix:_ Always include tenant scoping on DELETE: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Pass and verify tenantId to revertToolRow and any deletion paths to enforce ownership.
- **server/surprise-scorer.ts:93** — UPDATE felix_proposals sets args_embedding without a tenant_id constraint (UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL). `felix_proposals` is tenant-scoped; this update can modify another tenant's row.
  - _Fix:_ Require and pass tenantId into stampArgsEmbedding, and include tenant filtering on the UPDATE: WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL (or use Drizzle .where(eq(felixProposals.tenantId, tenantId))). Ensure all writes to tenant-scoped tables include tenant ownership checks.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table `browser_workflows` has no tenant filter — `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` updates by id only, allowing a caller who can supply a workflow id to modify rows belonging to other tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. add AND tenant_id = ${tenantId} to the WHERE clause: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); or use Drizzle's fluent API with .where(eq(browserWorkflows.tenantId, tenantId)) after loading the workflow id. Always ensure all UPDATE/DELETE/SELECT statements against tenant-scoped tables include an explicit tenant_id constraint (or verify ownership before modifying).
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger missing tenant_id constraint. settleVentureReservation updates a ledger row by id only (WHERE id = ${reservationId}) with no tenant check.
  - _Fix:_ Require tenantId when settling and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Verify the UPDATE affected exactly one row and fail safely if not. Alternatively SELECT the row with tenant check before updating.
- **server/venture-discovery/budget.ts:169** — UPDATE against tenant-scoped table agent_cost_ledger missing tenant_id constraint. releaseVentureReservation updates a ledger row by id only (WHERE id = ${reservationId}) with no tenant check.
  - _Fix:_ Require tenantId when releasing and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Check the affected row count and avoid mutating rows that do not belong to the caller's tenant.

### HIGH (155)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without tenant_id constraint (scans all tenants).
  - _Fix:_ Scope the query to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate a validated tenant list and run per-tenant. Avoid multi-tenant reads that return other tenants' metadata.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting is not tenant-scoped (no tenant_id filter) and iterates results across tenants.
  - _Fix:_ Run the sweep per-tenant (accept a tenantId parameter) or explicitly constrain the SELECT to a validated tenant_id. If cross-tenant processing is intended, require an explicit operator-approved flow and strong audit/logging; otherwise limit to a single tenant per invocation.
- **server/agentic-features.ts:861** — SELECT id FROM outreach_enrollments omits tenant_id in WHERE clause — checking for existing enrollment can match rows from other tenants.
  - _Fix:_ Include AND tenant_id = ${params.tenantId} in the query so duplicate-detection and EXISTS checks are limited to the caller's tenant.
- **server/agentic/department-budgets.ts:143** — SELECT against tenant-scoped table `department_budgets` is executed without constraining by tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This performs an unconstrained cross-tenant read on a tenant-scoped table.
  - _Fix:_ Restrict the query to a single tenant (WHERE tenant_id = ${tenantId}) or explicitly document and gate this cross-tenant admin action behind a clearly checked ADMIN context. If the intent is to sweep all tenants, perform the sweep from an explicit admin-only job that requires an admin/owner tenant id or uses a dedicated global table, and ensure the code path is hardened (audit/authorization) so ordinary callers cannot trigger unconstrained reads.
- **server/agentic/wake-scheduler.ts:142** — UPDATE claims pending agent_wake_schedules using an inner SELECT that lacks any tenant_id constraint (UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE)). This can atomically claim and mutate rows across all tenants.
  - _Fix:_ Restrict the claim to a single tenant at a time. Either run claims per-tenant (loop tenants or accept a tenantId) or add AND tenant_id = ${tenantId} to the inner SELECT and include tenant_id = ${tenantId} in the outer UPDATE so rows are only locked/updated for the intended tenant.
- **server/auth.ts:197** — validateApiKey performs a db.select() FROM api_keys filtering only by keyHash/isRevoked and not by tenant_id. This reads a tenant-scoped table without constraining tenant ownership.
  - _Fix:_ Ensure API-key lookup is scoped safely: include tenant_id in the WHERE when the tenant context is known, or derive the tenant from a tenant-encoded key prefix, or otherwise ensure the lookup cannot disclose/operate on keys from other tenants. Failing that, minimize returned fields and audit access.
- **server/auth.ts:206** — Updating api_keys.lastUsedAt via db.update(...).where(eq(apiKeys.id, key.id)) does not include a tenant_id constraint (update on a tenant-scoped table without tenant scoping).
  - _Fix:_ Add a tenant_id constraint to the WHERE (e.g. AND eq(apiKeys.tenantId, key.tenantId)) when updating tenant-scoped rows, or perform the update via an API that already enforces tenant ownership at lookup time.
- **server/auth.ts:305** — getSession SELECTs from auth_sessions by token only (WHERE token = ${tokenHash}) with no tenant_id constraint. This reads tenant-scoped session rows without tenant scoping.
  - _Fix:_ Design session lookup so the token encodes or is accompanied by tenant context, or require the caller to provide tenant context and add AND tenant_id = ${tenantId} to the WHERE. At minimum, ensure this code path is used only for secure lookup and audited.
- **server/auth.ts:1335** — loadSessionsFromDb issues DELETE FROM auth_sessions WHERE expires_at < ${now} (deleting expired sessions across all tenants) without a tenant scope — a write against a tenant-scoped table with no tenant constraint.
  - _Fix:_ Run expiration cleanup per-tenant or under a platform-admin-only maintenance path (and clearly document/audit it). If global cleanup is intended, ensure it's behind an explicit platform-admin gate and logged/audited; otherwise add tenant scoping.
- **server/auth.ts:1339** — loadSessionsFromDb issues DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' (migration purge) without tenant scoping — global delete on a tenant-scoped table.
  - _Fix:_ Perform migration purges under an explicit, auditable platform-admin maintenance operation and/or include tenant scoping when appropriate. Ensure the operation is intentionally gated and logged so it cannot be triggered inadvertently by non-admin code paths.
- **server/auth.ts:1327** — clearAllSessionsAsPlatformAdmin executes DELETE FROM auth_sessions (removes all sessions across tenants) without tenant scoping. Although this function is intended as a platform-admin action, it performs a global write on a tenant-scoped table.
  - _Fix:_ Retain the platform-admin guard but additionally: require an explicit one-time authority token (already present in code), log and audit the operation, and consider restricting the function to an internal-only admin path. Document that this is an intentional cross-tenant operation and keep strict access controls.
- **server/auto-project.ts:135** — SELECT from tenant-scoped table 'messages' omits tenant_id predicate (reads messages for a conversation without constraining to the caller's tenant).
  - _Fix:_ Constrain the query by tenant, e.g. add AND tenant_id = ${tenantId} to the WHERE clause or join via conversations ensuring m.tenant_id = ${tenantId} so all reads are scoped to the caller's tenant.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table 'messages' omits tenant_id predicate (reading conversation messages without tenant constraint).
  - _Fix:_ Add an explicit tenant filter (AND tenant_id = ${tenantId}) or join messages to conversations with a tenant_id check so the read is constrained to the caller's tenant.
- **server/auto-transcript.ts:128** — Query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without constraining messages by tenant_id. This reads tenant-scoped rows without a tenant filter.
  - _Fix:_ Include tenant scoping in the subquery, e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id (or otherwise ensure the subquery is constrained by the conversation's tenant) so message counts are tenant-scoped.
- **server/auto-transcript.ts:153** — Query with JOINs includes a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) that reads the tenant-scoped 'messages' table without a tenant_id constraint.
  - _Fix:_ Constrain the subquery by tenant (for example AND tenant_id = c.tenant_id) or rewrite the query to join messages through conversations/projects with explicit tenant checks to prevent cross-tenant reads.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this writes a row without the caller's tenant context and breaks the invariant that all tenant-scoped writes must set tenant_id explicitly.
  - _Fix:_ Include tenant_id on the invoice_items INSERT (and populate it from the transaction's tid). For example: INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ${item.description}, ...). Alternatively, if the table is intentionally not tenant-scoped, document that explicitly; otherwise add tenant_id to the schema and all INSERT/UPDATE/SELECTs against invoice_items must constrain on tenant_id.
- **server/channel-routing.ts:31** — SELECT joins a likely tenant-scoped table (personas) without any tenant_id constraint — query returns persona rows across tenants (no WHERE p.tenant_id = <caller tenant>).
  - _Fix:_ Require a tenantId parameter and constrain the query by tenant (e.g. add tenant_id to channel_routes or at least add WHERE p.tenant_id = ${tenantId} and cr.tenant_id = ${tenantId}). If channel_routes is intended to be per-tenant, add a tenant_id column and include it in all queries; if it must remain global, validate each persona_id by SELECTing FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} before returning/using the persona row.
- **server/channel-routing.ts:54** — INSERT INTO channel_routes writes a caller-supplied personaId without validating that the persona belongs to the caller's tenant (and channel_routes has no tenant_id). This allows a caller to associate another tenant's persona with a channel.
  - _Fix:_ Validate ownership before inserting: first SELECT personas WHERE id = ${personaId} AND tenant_id = ${tenantId} (reject if not found). Prefer adding tenant_id to channel_routes and include tenant_id in the INSERT/ON CONFLICT so the mapping is always tenant-scoped (use parameterized Drizzle .where(eq(table.tenantId, tenantId))).
- **server/chat-engine.ts:3875** — Calling recordErrorForRetryStormDetection(conversationId, ...) with only conversationId and no tenant information. This uses a foreign conversation identifier from the request and may cause the module to read/write tenant-scoped diagnostics/state without verifying tenant ownership.
  - _Fix:_ Modify recordErrorForRetryStormDetection to accept tenantId (and require it) or perform an explicit ownership check on conversationId before calling. Ensure all DB operations inside stuck-diagnostics use tenant-scoped WHERE tenant_id = ? filters derived from the caller's tenant.
- **server/chat-engine.ts:4096** — Calling recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) without tenantId. A handler-supplied conversationId is forwarded to a diagnostics module which may read or write rows tied to that conversation without validating tenant ownership.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection (and postDiagnosticReport) and update the stuck-diagnostics module to enforce tenant scoping on every DB access. If accepting tenantId is not possible, perform an ownership verification of conversationId in the caller before invoking the helper.
- **server/chat-engine/project-context.ts:27** — Query reads from project_conversations (pc) without an explicit tenant_id constraint. The WHERE clause filters p.tenant_id but does not constrain pc. This relies on indirect validation (project join) rather than the required explicit tenant filter on every tenant-scoped table.
  - _Fix:_ Add an explicit tenant filter for project_conversations, e.g. WHERE pc.conversation_id = ${conversationId} AND pc.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId} (or use Drizzle: .where(and(eq(project_conversations.tenantId, tenantId), eq(project_conversations.conversationId, conversationId)))).
- **server/chat-engine/project-context.ts:45** — Inner EXISTS() references project_conversations (pc) without constraining its tenant_id. Even though the outer query filters projects by p.tenant_id, every access to a tenant-scoped table must include an explicit tenant filter.
  - _Fix:_ Constrain the EXISTS() to the caller tenant, e.g. EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.conversation_id = ${conversationId} AND pc.tenant_id = ${tenantId} AND pc.project_id = p.id). Use Drizzle .where(eq(project_conversations.tenantId, tenantId)) for parameterized protection.
- **server/chat-engine/project-context.ts:159** — Selecting from project_files uses WHERE project_id = ${projectId} but does NOT include tenant_id = ${tenantId}. The code relies on prior project ownership checks instead of the required explicit tenant constraint on this tenant-scoped table.
  - _Fix:_ Add an explicit tenant constraint to the query, e.g. SELECT ... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} (or Drizzle .where(and(eq(project_files.projectId, projectId), eq(project_files.tenantId, tenantId)))).
- **server/chat-engine/project-context.ts:171** — Selecting from project_notes uses WHERE project_id = ${projectId} without an explicit tenant_id constraint. Every read against a tenant-scoped table must include the caller's tenant filter.
  - _Fix:_ Add tenant_id to the WHERE clause: SELECT ... FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId} (or use Drizzle .where(and(eq(project_notes.projectId, projectId), eq(project_notes.tenantId, tenantId)))).
- **server/chat-engine/project-context.ts:184** — Query joining project_conversations (pc) and conversations (c) filters c.tenant_id but does not explicitly constrain pc.tenant_id. Accessing project_conversations without an explicit tenant filter violates the per-query tenant-scope requirement.
  - _Fix:_ Add an explicit tenant filter for project_conversations (pc), e.g. WHERE pc.project_id = ${projectId} AND pc.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId} (or Drizzle .where(and(eq(project_conversations.tenantId, tenantId), eq(project_conversations.projectId, projectId)))).
- **server/coinbase-commerce.ts:73** — SELECT from tenants (coinbase_commerce_webhook_secret) is executed without any tenant_id constraint — this reads decrypted webhook secrets across all tenants.
  - _Fix:_ Avoid scanning the tenants table and decrypting every tenant webhook secret. Either require the webhook to include a trusted tenant selector (and then SELECT WHERE id = $tenantId), maintain a separate indexed table mapping safe verification hashes to tenant_id so you can match/verify without decrypting all rows, or restrict this operation to privileged offline/admin code. At minimum avoid returning/decrypting secrets for all tenants in a single unscoped SELECT.
- **server/data-protection.ts:149** — SELECT from tenant-scoped table "conversations" without a tenant_id predicate. The query SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} can return rows for all tenants and exposes tenant-specific metadata prior to per-row scoped deletes.
  - _Fix:_ Scope the initial scan by tenant (e.g. iterate tenants and SELECT WHERE tenant_id = <that tenant> AND deleted_at < ...), or require an explicit admin/maintenance context before running a global scan. Alternatively run this inside a withTenantTx or add an explicit WHERE tenant_id = ${desiredTenant} so reads never return other tenants' rows.
- **server/doc-collections.ts:166** — DELETE from tenant-scoped table doc_chunks is missing a tenant_id constraint. The statement `DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}` can affect rows across tenants if collection_id is mis-bound or row ownership is ambiguous.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. add `AND tenant_id = ${tenantId}`) or otherwise verify the collection belongs to the caller's tenant before deleting. Use the bound tenantId parameter rather than relying on collection_id alone.
- **server/doc-collections.ts:294** — UPDATE against tenant-scoped table doc_chunks uses only the primary id in the WHERE clause: `UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}`. This allows updating a doc_chunk row that may belong to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`), or include tenant_id in the identifying predicate to ensure the row belongs to the caller's tenant.
- **server/email.ts:370** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} is executed with no tenant_id constraint. inbox_messages is tenant-scoped, so this read can return rows from other tenants.
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or otherwise resolve the tenant for the message before querying. Use a tenant-scoped API instead of a global lookup.
- **server/email.ts:415** — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... is executed with no tenant_id constraint. This reads tenant-scoped rows across all tenants.
  - _Fix:_ Limit the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or perform per-tenant iteration. Ensure all SELECTs against inbox_messages include tenant_id filtering.
- **server/embeddings.ts:811** — SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... is executed with no tenant_id constraint. agent_knowledge is tenant-scoped, so this read can return entries across tenants.
  - _Fix:_ Add an explicit tenant_id filter (e.g. AND tenant_id = ${tenantId}) or iterate backfill per-tenant. Ensure all SELECTs against agent_knowledge include tenant scoping.
- **server/embeddings.ts:842** — SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200 is executed with no tenant_id constraint. memory_entries is tenant-scoped, so this read can touch rows across tenants.
  - _Fix:_ Perform this backfill per-tenant (include AND tenant_id = ${tenantId}) or otherwise limit the query to a safe tenant context. Never run cross-tenant SELECTs on tenant-scoped tables without explicit intent and safeguards.
- **server/embeddings.ts:858** — SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... is executed with no tenant_id constraint. This reads tenant-scoped agent_knowledge rows across tenants.
  - _Fix:_ Restrict the query with AND tenant_id = ${tenantId} or run the backfill per-tenant. Ensure tenant scoping on all reads of agent_knowledge.
- **server/firecrawl.ts:239** — UPDATE on scrapedPages uses only the row id in WHERE (.where(eq(scrapedPages.id, existing[0].id))) and omits a tenant_id constraint. This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include the tenant filter on updates even when the id was previously selected under a tenant-scoped query.
- **server/firecrawl.ts:372** — UPDATE on scrapedPages in the crawl loop uses only the row id in WHERE (.where(eq(scrapedPages.id, existing[0].id))) and omits a tenant_id constraint. This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Enforce tenant scoping on every UPDATE to tenant-scoped tables.
- **server/google-drive.ts:879** — UPDATE on projects sets driveFolderId/driveFolderUrl and matches only on projects.id (.where(eq(projects.id, projectId))) without an explicit tenant filter. This is a write against a tenant-scoped table without tenant_id in the WHERE clause.
  - _Fix:_ Add the tenant constraint to the UPDATE to ensure tenant isolation. For example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). If tenantId is validated earlier, still include it in the SQL WHERE to prevent cross-tenant updates.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` is executed without a tenant_id filter (reads across all tenants).
  - _Fix:_ If this query is intended to be per-tenant, add a WHERE tenant_id = ${tenantId} (or iterate DISTINCT tenant ids and run per-tenant). If it must be global, ensure it's only callable by an administrative process and document/guard that explicitly.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` is executed with no tenant_id filter (reads tasks across all tenants).
  - _Fix:_ If the scan is intended to operate per-tenant, query per tenant (WHERE tenant_id = ${tenantId}) or enumerate tenant IDs first then run tenant-scoped queries. If truly global admin work, restrict the API to admin-only contexts and document the intent.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules executed with no tenant_id filter (reads schedules across all tenants).
  - _Fix:_ If the heartbeat is intended to run per-tenant, enumerate tenant IDs and run a tenant-scoped SELECT (WHERE tenant_id = ${tenantId}). If the scan must be cross-tenant, ensure the caller is an admin-only context and add explicit authorization checks and comments documenting the global intent.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks performed via raw SQL without constraining by tenant_id (WHERE id = ${t.id} only). heartbeat_tasks is tenant-scoped; this update can modify another tenant's row.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${t.tenantId}) or use the storage API (storage.updateHeartbeatTask) which enforces tenant context/validation. Prefer assertTenantContext before any raw SQL update.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks performed via raw SQL without constraining by tenant_id (WHERE id = ${t.id} only). heartbeat_tasks is tenant-scoped; this update can modify another tenant's row.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${t.tenantId}) or use the storage API (storage.updateHeartbeatTask) which enforces tenant context/validation. Prefer assertTenantContext before any raw SQL update.
- **server/heartbeat-task-scheduler-storage.ts:12** — SELECT * FROM heartbeat_tasks executed with no tenant_id constraint. heartbeat_tasks is tenant-scoped and this read is unscoped.
  - _Fix:_ If this function truly must operate across tenants, ensure it is strictly isolated/authorized and documented (name already suggests across-tenants). Otherwise add a WHERE tenant_id = ${tenantId} (or pass ALL_TENANTS via a controlled boundary). Prefer using a storage-layer API that explicitly accepts ALL_TENANTS and documents the cross-tenant intent.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE heartbeat_tasks SET last_run_at/next_run_at WHERE id = ${id} without a tenant_id constraint. This can update rows belonging to other tenants by id alone.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${expectedTenantId}), or call into a storage method that validates tenant context. If this is intentionally cross-tenant, restrict its use to a vetted scheduler boundary and validate caller privileges explicitly.
- **server/heartbeat.ts:256** — SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10 executed with no tenant_id constraint. event_log is tenant-scoped (tenant_id used elsewhere) — unscoped reads can expose data across tenants.
  - _Fix:_ Scope the SELECT by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status = 'pending') or iterate per-tenant through an explicit, auditable cross-tenant scheduler boundary. Use assertTenantContext or otherwise validate that cross-tenant access is intentional and authorized.
- **server/heartbeat.ts:1574** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... executed without any tenant_id constraint. heartbeat_logs is tenant-scoped; this read can leak cross-tenant information.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND task_name = ...) or explicitly bind to ADMIN_TENANT_ID if this check is intended to be admin-scoped. Prefer using storage APIs that enforce tenant context.
- **server/heartbeat.ts:2672** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint. This can change approval_status for another tenant's task with the same id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or use the storage layer's updateHeartbeatTask which should enforce tenant ownership. Validate tenant context before performing raw SQL updates.
- **server/heartbeat.ts:3142** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint (in delegateTaskFromChat). This may affect tasks from other tenants.
  - _Fix:_ Add a WHERE tenant_id = ${tenantId} clause or use storage.updateHeartbeatTask to set approval_status so tenant ownership is enforced. Ensure any raw SQL includes the tenant filter or runs inside an auditable cross-tenant boundary.
- **server/job-queue.ts:233** — completeJob updates agent_jobs (SET status='succeeded' ...) WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but does not include tenant_id in the WHERE. A caller that knows an id could potentially complete a job for another tenant if they can influence that call.
  - _Fix:_ Require and validate tenant context when completing a job. Add an extra tenant_id = ${tenantId} predicate (and require the caller/worker to supply/derive the tenantId), or fetch and check the job's tenant before performing the UPDATE to ensure the completing actor is authorized for that tenant.
- **server/job-queue.ts:334** — cancelJob updates agent_jobs (SET status='cancelled' ...) WHERE id = ${id} AND status IN (...) without a tenant_id predicate, allowing cancelling jobs across tenants by id.
  - _Fix:_ Require tenant context and add AND tenant_id = ${tenantId} to the WHERE clause, or perform an initial tenant-checked lookup before changing status to ensure only the owning tenant (or an authorized admin) can cancel the job.
- **server/job-worker.ts:147** — The "research_code_proposal" job handler reconstructs a session from job.payload (including payload.tenantId) and calls generateCodeProposal using that payload tenantId without verifying that job.tenantId (the job's persisted tenant scope) matches payload.tenantId. This allows an attacker who can control job.payload to cause the handler to read/write data under a different tenant id.
  - _Fix:_ Do not trust tenant identifiers inside job.payload. Validate that any tenantId passed inside payload equals job.tenantId (the authoritative persisted scope) before calling tenant-scoped services. Prefer using job.tenantId exclusively for tenant-scoped operations and ignore/strip tenantId from payloads.
- **server/job-worker.ts:168** — The "research_digest" job handler reads tenantId from job.payload and passes it to generateResearchDigest without validating that payload.tenantId matches the job.tenantId. Using a tenant id from payload (untrusted) can result in cross-tenant reads/writes.
  - _Fix:_ Assert that job.payload.tenantId === job.tenantId (or otherwise derive tenantId from job.tenantId) before invoking tenant-scoped functionality. Reject or fail the job if there is a mismatch so handlers cannot act on foreign tenant data.
- **server/lib/action-ledger.ts:268** — Cross-tenant SELECT from tenant-scoped table `action_attempts` with no tenant_id filter (listUnknownAttemptsAllTenants). This returns rows for all tenants instead of restricting to a caller tenant.
  - _Fix:_ If this is not intended, add a tenant_id WHERE predicate (e.g. WHERE tenant_id = ${tenantId}) or change the API to accept an explicit tenantId parameter and use a parameterized filter (drizzle eq). If the cross-tenant scan is intentional for a reconciler/maintenance process, document/mark it clearly and ensure the caller runs with an explicit maintenance-only, auditable privilege or runs in a separated service/process so normal tenant-code cannot invoke it.
- **server/lib/action-ledger.ts:304** — UPDATE of tenant-scoped table `action_attempts` (sweepStaleAttempts) performed without any tenant_id constraint — inner SELECT and UPDATE operate across all tenants.
  - _Fix:_ Either scope the sweep to a tenant by accepting/using tenantId and adding WHERE tenant_id = ${tenantId}, or, if the global sweep is intended, ensure it only runs from a privileged maintenance context (separate service/process) and add explicit comments/guards documenting the intent. Prefer explicit parameterized filters (e.g. AND tenant_id = ${tenantId}) when tenant isolation is required.
- **server/lib/agent-trace.ts:125** — UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} — no tenant_id constraint. This may update span rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE predicate (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). To obtain tenantId at close time, persist the tenantId alongside the spanId (in-memory map keyed by spanId or read tenant_id in the open INSERT and store it) or derive it from currentTenantId(); ensure the close path uses that trusted tenantId and uses a parameterized condition.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT on tenant-scoped table `projects` performs a global aggregation across all tenants (SELECT tenant_id, count(*) FROM projects WHERE tags && ... GROUP BY tenant_id) with NO tenant_id filter. This violates the invariant that reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Avoid scanning all tenants. Restrict the query to only the tenant(s) you intend to evaluate (e.g. WHERE tenant_id = ${tenantId}) or, if you must compare against a snapshot of multiple specific tenants, pass an explicit list of tenant ids and use WHERE tenant_id = ANY(${tenantIdList}::int[]) so the SQL only touches those tenants. Alternatively compute per-tenant counts by iterating the snapshot tenants and querying each tenant individually. Ensure every read on tenant-scoped tables includes an explicit tenant_id constraint.
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey performs a SELECT FROM mcp_api_keys WHERE key_prefix = ${prefix} with NO tenant_id constraint. This reads a tenant-scoped table without constraining by tenant, which violates the project invariant that every read against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ If the intent is to resolve the tenant from the key (as the code comment indicates), make that design explicit and enforce it safely: ensure key_prefix is enforced UNIQUE at the DB level (global index) and document that this global lookup is the only exception. Alternatively, change the flow to require the caller's tenant and add AND tenant_id = ${tenantId} to the WHERE clause. At minimum harden the lookup by selecting only non-revoked rows (AND revoked_at IS NULL), assert the presence of a global UNIQUE constraint on key_prefix, and add an in-code comment explaining why this cross-tenant lookup is safe and audited. Implementing a separate global lookup table for prefix→tenant mapping is a clearer, safer option.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without a tenant_id filter (reads all tenants' running rows).
  - _Fix:_ Add a tenant_id predicate to the query or run the sweep per-tenant (e.g. accept a tenantId parameter and filter WHERE tenant_id = ${tenantId} or use withTenantTx) so tenant-scoped tables are never read across tenants without explicit app-level authorization.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without a tenant_id filter (reads across tenants).
  - _Fix:_ As with agent_runs, constrain the query with WHERE tenant_id = ${tenantId} or run the audit per-tenant using an explicit tenant context. Avoid global scans of tenant-scoped tables unless explicitly intended and guarded.
- **server/lib/system-probes.ts:163** — Direct DB query counting rows from tenant-scoped `messages` table has no tenant_id constraint (SELECT COUNT(*) FROM messages). This probes tenant data across all tenants.
  - _Fix:_ Constrain the query by tenant (WHERE tenant_id = ${tenantId}) or make the cross-tenant intent explicit and perform per-tenant queries in a controlled admin flow. Prefer doing per-tenant counts under an explicit admin/maintenance path that iterates tenants so the app-level tenant filter invariant is preserved.
- **server/martech-bundle.ts:326** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses WHERE id = existing.id without constraining tenant_id. This can allow cross-tenant row modifications if id provenance is ever untrusted.
  - _Fix:_ Include the tenant predicate in the UPDATE WHERE clause. For example, add AND tenant_id = ${opts.tenantId} (or in Drizzle: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) so the UPDATE is scoped to the caller's tenant.
- **server/memory-graph.ts:137** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — missing tenant_id constraint. This mutates a tenant-scoped table by id only, violating the tenant-isolation invariant.
  - _Fix:_ Restrict the UPDATE to the tenant: add AND tenant_id = ${tenantId} (or use a combined WHERE with eq(memory_categories.id, catId) AND eq(memory_categories.tenantId, tenantId) in Drizzle) so the increment only affects the calling tenant's category.
- **server/memory-graph.ts:708** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id constraint. Updating a tenant-scoped memory row by id only can cross tenant boundaries if id is ever mis-bound.
  - _Fix:_ Scope the UPDATE to the tenant by adding AND tenant_id = ${tenantId} (or use Drizzle .where(and(eq(memory_entries.id, mem.id), eq(memory_entries.tenantId, tenantId)))) to ensure only the caller's tenant row is modified.
- **server/memory-intelligence.ts:399** — UPDATE on knowledge_triples omits tenant constraint. The code executes: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}` without constraining by tenant_id, allowing accidental/unauthorized updates across tenants.
  - _Fix:_ Include the tenant_id in the WHERE clause (or otherwise verify ownership) when updating triples. Example fix: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} AND tenant_id = ${tenantId}`. Alternatively, query and assert the row belongs to the caller's tenant before updating.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code relies on assertProjectInTenant(...) for ownership but the INSERT itself omits tenant_id (columns are expected NOT NULL per invariant), breaking the project-level guard that tenant isolation must be enforced by SQL filters/values on every write.
  - _Fix:_ Include tenant_id explicitly in the INSERT and bind it as a parameter (e.g. add tenant_id to the column list and VALUES (${options.tenantId}, ...)) after validating options.tenantId is a positive integer. Do not rely solely on an ownership check; persist tenant_id on the row so application-level SQL filtering can enforce isolation consistently.
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id. Like the single-job path, this uses assertProjectInTenant(...) but then writes the row without tenant_id, violating the invariant that all tenant-scoped INSERTs must supply tenant_id explicitly.
  - _Fix:_ Modify this INSERT to supply tenant_id (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${options.projectId}, ${options.tenantId}, ${safeTitle + ".mp4"}, ...)) after validating options.tenantId. Ensure both single- and parallel-path inserts consistently record tenant_id so downstream reads/filters can enforce isolation.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions without tenant_id constraint (global scan). validateSubscriptionsOnStartup queries all active subscriptions across tenants.
  - _Fix:_ Avoid global SELECT on a tenant-scoped table. Either scope the query to an explicit tenant_id or explicitly iterate per-tenant (e.g. query tenant list and run per-tenant validation) so every read is constrained to a caller tenant.
- **server/oauth-subscriptions.ts:915** — SELECT FROM oauth_subscriptions (provider = 'google-workspace') omits tenant constraint — reads tenant-scoped rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or otherwise restrict to a known set of tenant IDs before operating on the results.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — missing tenant_id constraint. This updates a tenant-scoped row by id only, derived from a prior global SELECT.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or ensure the update runs in a per-tenant context so you never update another tenant's rows by id alone.
- **server/oauth-subscriptions.ts:981** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — missing tenant_id constraint (second occurrence).
  - _Fix:_ Same fix: constrain by tenant_id in the WHERE clause or perform updates only within a per-tenant loop where tenant_id is enforced.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh: SELECT provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE — global scan across tenants without tenant_id filtering.
  - _Fix:_ Do not read all tenants in a single unscoped query. Either require an explicit tenant parameter, or iterate over tenant IDs and run a per-tenant query (WHERE tenant_id = ${tenantId}) so every read is tenant-scoped.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — global read across tenants.
  - _Fix:_ As above, avoid unscoped reads on tenant-scoped tables. Run per-tenant refresh cycles or otherwise restrict queries to tenant_id to preserve tenant isolation and avoid cross-tenant effects.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} has no tenant_id constraint. A caller-provided personaId is used to read a tenant-scoped table without verifying ownership/tenant association.
  - _Fix:_ Require and enforce tenantId when loading a persona: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. If personaId comes from an external request, first verify the persona belongs to the caller's tenant before exporting.
- **server/persona-routing-outcomes.ts:44** — refreshTenant background query joins action_outcomes (scoped by ao.tenant_id = ${tenantId}) to personas p ON p.id = ao.persona_id but does not constrain personas by tenant. This reads the tenant-scoped personas table without a tenant_id constraint in the JOIN/WHERE.
  - _Fix:_ Constrain the joined personas table to the same tenant (e.g. JOIN personas p ON p.id = ao.persona_id AND p.tenant_id = ${tenantId}) to ensure persona rows belong to the tenant and avoid cross-tenant joins/leakage.
- **server/plan-executor.ts:880** — Boot-time recovery UPDATE resets all stale 'executing' plans back to 'approved' without any tenant_id constraint (UPDATE plans SET status='approved' WHERE status='executing' AND updated_at < ... RETURNING id). This performs cross-tenant writes and breaks the per-tenant isolation invariant.
  - _Fix:_ Restrict the recovery to a single tenant at a time (e.g. run per-tenant loop) or include an explicit tenant_id filter for the admin/owner tenant only. If global recovery is required, ensure the operation is gated to an explicit system/admin context and audited; otherwise perform the UPDATE per-tenant using a verified tenant id source.
- **server/plan-executor.ts:887** — Selecting pending approved plans uses an unrestricted query (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50) with no tenant_id filter. The code then schedules executePlan for each id, potentially causing cross-tenant reads/executes without tenant scoping.
  - _Fix:_ When enumerating pending plans, filter by tenant_id (or iterate per-tenant) so only plans belonging to the intended tenant are selected. If this is intended as a global dispatcher, require an explicit and validated admin/system context and document the authority; otherwise change the query to WHERE tenant_id = ${tenantId} AND status = 'approved'.
- **server/project-brain.ts:135** — SELECT projects query does not constrain by tenant_id (SELECT name, status, description FROM projects WHERE id = ${projectId}). This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Require and pass a tenantId into updateProjectBrain and add AND tenant_id = ${tenantId} (or equivalent Drizzle where) to the SELECT. Validate the caller's ownership of the project before reading/writing any tenant-scoped resources.
- **server/project-brain.ts:349** — Query that loads conversations for a project lacks tenant constraint: SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}. conversation rows are not scoped to tenant.
  - _Fix:_ Include tenant scoping (e.g. AND c.tenant_id = ${tenantId}) or join through a project row that is already verified to belong to the tenant; require tenantId in the function and validate ownership before enumerating conversations.
- **server/process-governor.ts:897** — UPDATE agent_desks SET ... WHERE id = ${desk.id} (in restart_agent) does not include tenant_id in the WHERE clause. This updates a tenant-scoped table without confirming the row's tenant, allowing cross-tenant modification if ids collide.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use a parameterized Drizzle .where(eq(...)) check) to ensure only the caller's tenant rows are modified.
- **server/process-governor.ts:1234** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} is missing a tenant_id constraint. This mutates another tenant's governance_rules row if IDs collide.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE (or otherwise verify the rule belongs to the tenant before updating) so trigger_count/last_triggered_at can only be updated for the caller's tenant.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} with no tenant_id filter — allows modifying rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) so the update is constrained to the owning tenant. Prefer passing an explicit tenantId variable rather than trusting row-derived values when composing the WHERE clause.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ${storedId} with no tenant_id constraint — allows cross-tenant modification if id is tampered with or mis-sourced.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}). Always include tenant_id in WHERE for writes to tenant-scoped tables, even when the id was obtained from a prior tenant-scoped SELECT.
- **server/research-engine.ts:456** — INSERT into project_notes (auto-deposit summary) does not include tenant_id. Per project/DB schema conventions tenant_id must be explicitly set on tenant-scoped tables to enforce isolation.
  - _Fix:_ Include tenant_id in the INSERT columns/VALUES (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)). Also ensure project ownership was validated (assertProjectInTenant) prior to insert (it is, but still pass tenant_id explicitly).
- **server/research-engine.ts:465** — INSERT into project_notes inside the findings loop does not include tenant_id. Writing tenant-scoped artifacts without tenant_id breaks the invariant that all tenant-scoped inserts must pass tenantId explicitly.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ${noteContent}, ...)). Confirm project ownership (assertProjectInTenant) before inserting and validate values are parameterized.
- **server/routes.ts:182** — SELECT from tenant-scoped table `file_storage` with no tenant_id constraint (restoreUploadsFromDb). This reads file blobs for all tenants on startup.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE file_storage.tenant_id = ${tenantId}) or iterate per-known-tenant. If this is intended as a global admin operation, gate it behind an explicit platform-admin check and document the intent.
- **server/routes.ts:1919** — SELECT FROM tenant-scoped table `presenter_sessions` by token with no tenant_id constraint (GET /api/presenter/:token). A raw token lookup returns a presenter session row for any tenant if token matches, potentially exposing tenant-specific metadata.
  - _Fix:_ Treat the token as an explicit capability (documented) or, safer, verify/derive the tenant from a validated token payload and include eq(presenter_sessions.tenant_id, tenantId) in the WHERE clause. Avoid returning tenant-identifying fields unless the token validation explicitly binds the token to that tenant and the operation is intended as a capability-based cross-tenant access.
- **server/routes.ts:1947** — SELECT of `presenter_sessions` by token (sessionResult) in /api/presenter/:token/slide/:index is performed without scoping to a caller tenant. Subsequent image selects rely on the returned tenant_id, but the initial lookup itself reads a tenant-scoped table unrestricted.
  - _Fix:_ Validate the token as a capability before the DB lookup or include tenant scoping in the lookup (e.g. only select sessions where token = ${token} AND tenant_id = ${expectedTenant}). If the token is the authorization capability, ensure it's treated and stored/validated securely (e.g. HMAC/hash) and document that behavior.
- **server/routes.ts:2054** — SELECT FROM tenant-scoped table `presenter_sessions` by token in /api/presenter/:token/repair-images is done without tenant_id constraint. This reads session rows across tenants based solely on token.
  - _Fix:_ Same as above: either require/derive tenant context and add WHERE tenant_id = ${tenantId} or ensure the token is a properly-scoped capability (and validate it) before performing the lookup. Prefer explicit tenant-scoped queries or an admin-only maintenance path for cross-tenant operations.
- **server/routes.ts:4571** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The code executes: INSERT INTO project_notes (project_id, note, author) VALUES (...) without passing tenant_id, violating the invariant that every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (and bind it as a parameter). Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Prefer using the storage/Drizzle insert helper that enforces tenant scoping and/or validate project ownership before insert. Ensure the tenant_id column is non-null and populated for all tenant-scoped inserts.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table inbox_messages lacks tenant_id predicate. The statement `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` will mark a message read by id without verifying it belongs to the caller's tenant, allowing cross-tenant modification if an attacker supplies/guesses an id.
  - _Fix:_ Constrain the UPDATE to the requester's tenant. e.g. use: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}` (or call a storage-layer method that enforces tenant scope). Always include tenant_id (or equivalent Drizzle eq(table.tenantId, tenantId)) on mutating queries against tenant-scoped tables.
- **server/routes/projects.ts:165** — The SELECT at /api/projects builds subqueries counting project_files, project_notes, and project_conversations without constraining those dependent tables to the caller's tenant (the inner subqueries lack a tenant filter).
  - _Fix:_ Constrain the dependent table reads to the tenant. Either add tenant_id predicates on the dependent tables (e.g. pf.tenant_id = ${tenantId}) or JOIN the dependent table to projects and check the project's tenant_id (e.g. FROM project_files pf JOIN projects p2 ON p2.id = pf.project_id WHERE p2.tenant_id = ${tenantId} AND pf.project_id = p.id). Ensure every read against tenant-scoped tables includes an explicit tenant constraint.
- **server/routes/projects.ts:201** — The UPDATE at line 616: 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' executes against the projects table without a tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. 'WHERE id = ${projectId} AND tenant_id = ${tenantId}', or otherwise verify tenant ownership inside the same SQL statement prior to mutation.
- **server/routes/projects.ts:616** — The UPDATE at /api/projects/:id/files-base64 (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. 'WHERE id = ${projectId} AND tenant_id = ${tenantId}'. Do not rely solely on prior application checks — the SQL must include the tenant predicate.
- **server/routes/projects.ts:684** — The UPDATE at /api/projects/:id/files (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. 'WHERE id = ${projectId} AND tenant_id = ${tenantId}'. Ensure all updates to tenant-scoped tables include an explicit tenant filter.
- **server/routes/projects.ts:568** — INSERT INTO project_notes omits tenant scoping / tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)).
  - _Fix:_ Include tenant_id explicitly in the INSERT (INSERT INTO project_notes (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). If the table is intended to be scoped via project_id, still add a tenant_id column and set it, or use a single INSERT that joins/validates the project belongs to the tenant before inserting.
- **server/routes/projects.ts:608** — INSERT INTO project_files in /api/projects/:id/files-base64 omits tenant scoping / tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)).
  - _Fix:_ Include tenant_id explicitly in the INSERT (INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)) or otherwise constrain the write in the same SQL. Do not rely only on an earlier application-level project check.
- **server/routes/projects.ts:676** — INSERT INTO project_files in multipart upload handler omits tenant scoping / tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)).
  - _Fix:_ Include tenant_id explicitly in the INSERT (INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)) or otherwise ensure the DB write is constrained to the caller's tenant in the same statement.
- **server/routes/projects.ts:401** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not include tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${targetProjectId}, ${conversationId}, ${tenantId})) or otherwise ensure the write is constrained to the tenant. If project_conversations relies on project FK for isolation, add an explicit tenant column/check to avoid risk from stale/malformed cross-tenant FKs.
- **server/routes/projects.ts:703** — SELECT * FROM project_files WHERE project_id = ${projectId} (listing project files) lacks an explicit tenant constraint on project_files.
  - _Fix:_ Constrain the SELECT to tenant ownership. Either add a tenant_id predicate on project_files or JOIN projects and check p.tenant_id = ${tenantId} in the WHERE clause (e.g. SELECT pf.* FROM project_files pf JOIN projects p ON p.id = pf.project_id WHERE pf.project_id = ${projectId} AND p.tenant_id = ${tenantId}).
- **server/routes/projects.ts:726** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 has no tenant constraint on project_files.
  - _Fix:_ Constrain the SELECT by tenant, e.g. add AND EXISTS (SELECT 1 FROM projects p WHERE p.id = pf.project_id AND p.tenant_id = ${tenantId}) or include tenant_id in project_files and filter tenant_id = ${tenantId} to ensure the file belongs to the caller's tenant.
- **server/routes/projects.ts:755** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} performs a deletion on a tenant-scoped table without an explicit tenant_id constraint in the DELETE statement.
  - _Fix:_ Include tenant scoping in the DELETE clause (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) or JOIN to projects to ensure p.tenant_id = ${tenantId} in the same SQL statement. Avoid relying solely on prior application checks.
- **server/routes/projects.ts:472** — The conversation ownership check uses EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = ${id} AND pc.conversation_id = ${primaryConversationId}) but that subquery does not constrain project_conversations by tenant. This is a check that reads a referenced (foreign) row without tenant scoping.
  - _Fix:_ Constrain the EXISTS subquery to the tenant by joining/adding tenant predicates, for example: EXISTS (SELECT 1 FROM project_conversations pc JOIN projects p2 ON p2.id = pc.project_id WHERE pc.project_id = ${id} AND pc.conversation_id = ${primaryConversationId} AND p2.tenant_id = ${tenantId}) or include pc.tenant_id = ${tenantId} if the table has tenant_id. Always validate ownership of foreign IDs within the SQL used for authorization.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions without tenant constraint — updates session status by id only (WHERE id = ${session.id}) but does not ensure tenant_id = caller tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${params.tenantId} or AND tenant_id = ${tenantId}) so the update cannot affect sessions in other tenants.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions setting conversation_id does not restrict by tenant — WHERE id = ${session.id} has no tenant_id check.
  - _Fix:_ Include AND tenant_id = ${params.tenantId} (or the appropriate tenantId variable) in the WHERE clause when updating sculptor_sessions.
- **server/sculptor.ts:168** — UPDATE sculptor_sessions in monitorSession (finalization path) lacks tenant scoping — WHERE id = ${sessionId} without tenant_id allows cross-tenant modification.
  - _Fix:_ Constrain the UPDATE to the session's tenant: add AND tenant_id = ${tenantId} (use the tenantId passed to monitorSession) to the WHERE clause.
- **server/sculptor.ts:358** — UPDATE sculptor_sessions setting review_result does not include tenant constraint — writes by session id only (WHERE id = ${sessionId}).
  - _Fix:_ Restrict the UPDATE to the owning tenant, e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}, or fetch and validate the session's tenant before writing.
- **server/second-opinion.ts:416** — settleFusionReservation UPDATE agent_cost_ledger uses WHERE id = ${reservationId} only — no tenant_id check on a tenant-scoped ledger table.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or pass/verify the reservation's tenant before updating to prevent cross-tenant ledger modifications.
- **server/second-opinion.ts:438** — releaseFusionReservation UPDATE agent_cost_ledger sets cost_usd/operation WHERE id = ${reservationId} with no tenant constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise verify reservation ownership) so releases cannot touch other tenants' ledger rows.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks has no tenant filter — heartbeat_tasks is tenant-scoped elsewhere (tenant_id used in other statements), so this read is cross-tenant.
  - _Fix:_ Scope the query to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) or otherwise document and intentionally handle cross-tenant semantics.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant filter while heartbeat_tasks is tenant-scoped elsewhere.
  - _Fix:_ Add tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant) to the WHERE clause to avoid counting across tenants.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) is missing tenant scoping while later seed logic expects admin-scoped changes.
  - _Fix:_ Restrict the SELECT to the admin tenant (WHERE tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the query's cross-tenant semantics are deliberate and safe.
- **server/seed.ts:2171** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... executes against the tenant-scoped projects table without constraining tenant_id. This writes to a project row regardless of its tenant, risking cross-tenant data modification.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise verify ownership of project id=15 before updating. Prefer Drizzle .where(eq(projects.tenantId, tenantId)) to enforce application-level tenant isolation.
- **server/seed.ts:3662** — UPDATE against tenant-scoped table `memory_entries` has no tenant_id constraint (mass-update across all tenants).
  - _Fix:_ Constrain the UPDATE to a specific tenant (or iterate tenants). e.g. add AND tenant_id = <tenantId> to the WHERE clause or perform per-tenant updates. In Drizzle use .where(eq(memoryEntries.tenantId, tenantId)). Avoid blanket updates that modify rows for all tenants.
- **server/seed.ts:3688** — UPDATE on `conversations` (SET model = 'gpt-5' WHERE model = 'gpt-4.1') lacks any tenant_id restriction — it will modify conversations across all tenants.
  - _Fix:_ Restrict the UPDATE to the target tenant(s). Add WHERE tenant_id = <tenantId> (or run the migration per-tenant). In Drizzle use .where(eq(conversations.tenantId, tenantId)) or include tenant_id in the SQL WHERE to ensure tenant isolation.
- **server/seed.ts:3634** — DELETE FROM tenant-scoped table `heartbeat_tasks` is executed without a tenant_id filter, potentially deleting rows for all tenants.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = <tenantId>) or perform deletion per-tenant after verifying ownership. Ensure any seed/cleanup that touches tenant-scoped tables is explicitly limited to intended tenant(s).
- **server/seed.ts:3881** — INSERT INTO `project_notes` does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are non-null/no default).
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., <tenantId>)). If deriving tenant_id from project_id, first fetch/verify the project's tenant and use that verified tenant_id in the INSERT.
- **server/seed.ts:3948** — UPDATE on `agent_knowledge` uses only id in WHERE (UPDATE ... WHERE id = $id) after an earlier SELECT that filtered tenant_id; the UPDATE itself is not tenant-scoped and could affect a row with the same id in a different tenant context.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = $id AND tenant_id = <tenantId>) or re-query and use both id and tenant_id when issuing the UPDATE. Always verify ownership before updating tenant-scoped rows.
- **server/self-improvement.ts:427** — UPDATE against tenant-scoped table `experiments` is missing a tenant_id constraint — the .where(eq(experiments.id, experiment.id)) does not verify the caller's tenant, allowing an experiment row belonging to another tenant to be updated.
  - _Fix:_ Restrict the UPDATE to the caller's tenant. Add a tenant filter to the WHERE, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId!))). Always validate config.tenantId before use.
- **server/self-reflection.ts:374** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = <id> without also constraining tenant_id. This allows updating another tenant's memory entry by id.
  - _Fix:_ Include tenant scoping in the UPDATE. Add AND tenant_id = ${tenantId} to the WHERE clause (or use a WHERE id = ... AND tenant_id = ${tenantId}). Validate tenantId before performing the update.
- **server/skillify.ts:91** — SELECT from messages without tenant scoping: recentMsgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)) (no tenant_id constraint). This reads tenant-scoped rows by conversationId only.
  - _Fix:_ Constrain message reads to the caller's tenant. Add tenant filtering to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or otherwise include WHERE tenant_id = ${tenantId}) so only the caller's tenant rows are returned.
- **server/skillify.ts:201** — analyzeConversation reads messages without tenant scoping: const msgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)).orderBy(...). This selects from a tenant-scoped table with no tenant_id constraint.
  - _Fix:_ Include tenant scoping on every messages query. Use a combined WHERE that enforces messages.tenant_id = tenantId (or the Drizzle equivalent) so message rows are always constrained to the caller's tenant. Example: .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))).
- **server/skillify.ts:221** — Selecting persona rows without tenant scoping: personaRows = await db.select(...).from(personas).where(eq(personas.id, conv[0].personaId)) — no tenant_id constraint. Personas are tenant-scoped and this can read another tenant's persona metadata.
  - _Fix:_ Constrain persona lookup by tenant as well (e.g. add eq(personas.tenantId, tenantId) to the WHERE), or retrieve persona via a storage API that enforces ownership. Always verify ownership of referenced foreign IDs before using their data.
- **server/stability-watchdog.ts:177** — Query aggregates heartbeat_logs across tenants with no tenant constraint: SELECT task_id, task_name, COUNT(*) ... FROM heartbeat_logs WHERE status = 'error' AND created_at > ... GROUP BY ... — no tenant_id filter. This reads tenant-scoped audit/log rows globally.
  - _Fix:_ If the watchdog intends to act per-tenant, scope the query by tenant_id or iterate tenants explicitly. If the intent is a global system-level aggregate, make the global intent explicit and ensure proper authorization; otherwise add WHERE tenant_id = ${tenantId} (or consult a multi-tenant-safe storage API) to avoid cross-tenant data access.
- **server/stability-watchdog.ts:211** — DELETE from heartbeat_tasks without tenant scoping: DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' AND created_at < ... RETURNING id — no tenant_id constraint. This can delete tenant-scoped rows across all tenants.
  - _Fix:_ Restrict DELETEs to a specific tenant (WHERE tenant_id = ${tenantId} ...) or perform tenant-scoped cleanup per-tenant. Alternatively, use a storage-layer API that enforces tenant boundaries for deletions to prevent cross-tenant data modification.
- **server/stability-watchdog.ts:225** — DELETE from event_log without tenant scoping: DELETE FROM event_log WHERE (status = 'processed' OR status = 'failed') AND created_at < ... RETURNING id — no tenant_id constraint. This deletes tenant-scoped log rows across all tenants.
  - _Fix:_ Scope the deletion by tenant_id or iterate per-tenant when purging logs. Use a tenant-safe storage API or add WHERE tenant_id = ${tenantId} to the DELETE to avoid cross-tenant deletion.
- **server/storage.ts:276** — Raw SELECT from tenant-scoped table `messages` in attachCitationMetadata lacks any tenant_id constraint (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL).
  - _Fix:_ Include tenant scoping in the query (e.g., add AND tenant_id = ${tenantId} or join to a tenant-validated set) or require/accept a tenantId and validate it before selecting citations. Ensure the tenantId is bound as a parameter (use Drizzle eq(messages.tenantId, tenantId)).
- **server/storage.ts:299** — createMessage reads the parent conversation by conversationId and writes a message tied to that conversation without verifying the acting caller's tenant owns the conversation. The function will accept an arbitrary conversation_id from a caller and insert a message into that conversation's tenant context.
  - _Fix:_ Require an explicit acting tenantId parameter (or derive it from authenticated request context) and validate ownership before writing — e.g. use assertConversationInTenant(conversationId, tenantId) or include eq(conversations.tenantId, tenantId) when selecting the conversation. Fail closed if the tenant does not match.
- **server/storage.ts:619** — deleteMemoryEntry issues an UPDATE on tenant-scoped table `memory_entries` using only id when tenantId is omitted (tenantId optional). This allows an unscoped update if tenantId is not supplied.
  - _Fix:_ Require tenantId for delete/update operations (use requiredTenantScope or assertValidTenantId) or always include eq(memoryEntries.tenantId, tenantId) in the WHERE. If an explicit platform-wide operation is needed, accept an explicit ALL_TENANTS sentinel and handle it securely.
- **server/storage.ts:626** — touchMemoryEntries performs an UPDATE on `memory_entries` with an inArray(id) condition but only optionally scopes by tenantId. If tenantId is undefined the update is unscoped across tenants.
  - _Fix:_ Require and validate tenantId for this operation (use requiredTenantScope or assertValidTenantId) and include eq(memoryEntries.tenantId, tenantId) in the WHERE. Do not allow a silent unscoped path.
- **server/storage.ts:683** — evictOldestConversationFacts updates `conversation_facts` using only an id list in the WHERE (where(inArray(conversationFacts.id, victims.map(...)))), but the victims were selected under a tenant filter earlier. The UPDATE is performed without re-applying the tenant constraint, allowing possible cross-tenant updates if IDs overlap.
  - _Fix:_ Include the tenant constraint in the UPDATE's WHERE (e.g., AND eq(conversationFacts.tenantId, tenantId)) or pass the tenant-scoped ids through and update using a combined condition to ensure the update cannot affect other tenants.
- **server/storage.ts:891** — updateMemoryEmbedding updates `memory_entries` and sets embedding WHERE id = {id} with no tenant_id filter. This writes a tenant-scoped table without validating the caller's tenant.
  - _Fix:_ Require the caller to pass a tenantId (or validate ownership) and include eq(memoryEntries.tenantId, tenantId) in the WHERE. Alternatively accept an explicit ALL_TENANTS sentinel for legitimate admin operations and validate that only admins can call it.
- **server/storage.ts:914** — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` without any tenant filter (returns rows across all tenants).
  - _Fix:_ Require a tenant scope parameter (or explicit ALL_TENANTS sentinel) and use requiredTenantScope/assertValidTenantId to enforce/validate tenant scoping. If this is an admin-only task, gate it to admin contexts explicitly.
- **server/storage.ts:919** — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` without a tenant filter, returning rows across tenants.
  - _Fix:_ Require and validate a tenant scope parameter (use requiredTenantScope/ALL_TENANTS) or otherwise restrict this function to explicit admin-only usage and enforce that at the callsite.
- **server/storage.ts:929** — archiveExpiredMemories runs UPDATE memory_entries SET status = 'archived' WHERE ... without any tenant_id predicate — this mutates all tenants' data in one statement.
  - _Fix:_ Either iterate per-tenant (and require/validate tenantId) or require an explicit ALL_TENANTS sentinel for global maintenance operations and ensure only privileged/admin code can invoke it. Prefer per-tenant scoped updates to avoid accidental cross-tenant mutations.
- **server/storage.ts:946** — archiveStaleMemories runs an UPDATE on `memory_entries` using timestamp predicates but no tenant_id filter, affecting all tenants' rows.
  - _Fix:_ Require tenant scoping (per-tenant execution) or make this an explicit admin-only global operation with appropriate access checks. Always include eq(memoryEntries.tenantId, tenantId) when intended to act on a single tenant.
- **server/storage.ts:1057** — updateHeartbeatTask builds conditions and only applies a tenant-scoping subquery when tenantId !== undefined; if tenantId is omitted it updates heartbeat_tasks by id only, allowing unscoped updates to heartbeat_tasks which are parent to tenant-scoped logs.
  - _Fix:_ Require and validate tenantId for tenant-scoped mutations or use requiredTenantScope to ensure callers explicitly opt into ALL_TENANTS. Include a direct eq(heartbeatTasks.tenantId, tenantId) clause (or assert caller is admin) instead of a subquery bypass.
- **server/storage.ts:1062** — deleteHeartbeatTask similarly may DELETE heartbeat_tasks by id without tenant scoping when tenantId is omitted, allowing unscoped deletions.
  - _Fix:_ Require tenantId or an explicit ALL_TENANTS sentinel and validate caller privileges. Ensure DELETE WHERE includes tenant constraint (eq(heartbeatTasks.tenantId, tenantId)) to prevent cross-tenant deletion.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (existing = await db.select().from(customTools).where(eq(customTools.name, parsed.name))). This can leak existence of or collide with another tenant's custom tool names.
  - _Fix:_ Include the tenant filter when querying tenant-scoped tables. e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Alternatively, pass tenantId into this function and enforce eq(customTools.tenantId, tenantId) on all reads.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without tenant constraint (finalTool = await db.select().from(customTools).where(eq(customTools.id, inserted.id))). This reads tenant-scoped JS/implementation without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the SELECT: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Ensure functions which examine inserted rows accept/verify tenantId and apply it on all reads.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` without tenant_id guard (await db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id))). A row with the same id but different tenant could be modified.
  - _Fix:_ Include tenant constraint in the WHERE clause: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure `tool` provenance is validated and tenantId is passed through to the updater.
- **server/tool-learning.ts:365** — UPDATE of tenant-scoped table `custom_tools` to bump usageCount uses WHERE by id only (await db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(eq(customTools.id, tool.id))). Missing tenant constraint allows writes across tenants.
  - _Fix:_ Constrain the update by tenant as well: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure executeCustomTool either carries tenantId or derives it from the previously fetched tool and uses it in the WHERE clause.
- **server/tools.ts:6932** — INSERT into tenant-scoped table 'project_files' does not include tenant_id (INSERT INTO project_files (...) VALUES (...)). Tenant_id is required by the platform invariant and columns have no default.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)) or use your Drizzle model with tenantId set. Also keep the existing assertProjectInTenant check (good) but do NOT rely on it as a substitute for persisting tenant_id in the row.
- **server/tools.ts:8907** — UPDATE against proposed_skills uses WHERE eq(proposed_skills.id, row.id) but does NOT constrain by tenant (no WHERE tenant_id = <caller tenant>). This allows one tenant to affect another tenant's proposed_skills row if ids collide or are guessed.
  - _Fix:_ Add a tenant constraint to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or otherwise ensure the update includes tenantId === calling tenant. Prefer Drizzle-style eq on the tenant column rather than relying on implicit protections.
- **server/tools.ts:8937** — UPDATE against proposed_skills in the managed-skill-safety error-path uses WHERE eq(proposed_skills.id, row.id) without a tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. include eq(proposedSkills.tenantId, tid)) so updates only affect rows in the caller's tenant.
- **server/tools.ts:8952** — UPDATE against proposed_skills (mark accepted/promoted) filters only by id (eq(proposed_skills.id, row.id)) and omits tenant_id, violating tenant isolation requirements.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant equality.
- **server/tools.ts:8968** — UPDATE against proposed_skills (mark rejected) uses WHERE eq(proposed_skills.id, row.id) without tenant_id filter, allowing cross-tenant mutation risk.
  - _Fix:_ Add tenant filtering to the WHERE clause (e.g. include eq(proposedSkills.tenantId, tid)) so only the owning tenant's row can be updated. Audit remaining proposed_skills queries for the same issue.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table knowledge_triples uses WHERE id = ${row.id} with no tenant_id constraint.
  - _Fix:_ Include tenant filtering in the UPDATE to enforce tenant isolation (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or use the Drizzle ORM update builder with an explicit tenant_id predicate so writes cannot affect rows belonging to other tenants.
- **server/tools/domains/files/handlers.ts:937** — INSERT into project_files omits tenant_id and writes request-derived values (file path/name) via a raw SQL template. If project_files is tenant-scoped this violates the requirement to set tenant_id on INSERT; using interpolated raw SQL with request-derived values increases injection/isolation risk.
  - _Fix:_ Ensure project_files (if tenant-scoped) includes tenant_id in the INSERT (e.g. INSERT ... (project_id, ..., tenant_id) VALUES (..., ${tenantId})). Avoid building raw SQL with request-derived values — use the Drizzle ORM insert/update helpers (db.insert(...).values({...})) or parameterized ORM methods so values are bound safely and tenant_id is always set from the trusted ctx.tenantId.
- **server/tools/domains/files/handlers.ts:927** — Use of db.execute(sql`...`) with interpolated values derived from request/context (e.g. ${tenantId}, ${ctx.conversationId}, ${absPath}, ${fileName}) — raw/template SQL is being composed with request-derived inputs.
  - _Fix:_ Avoid composing raw SQL templates with request-derived values. Use the ORM query-builder / parameterized insert/update methods (Drizzle's db.insert/db.update or prepared queries) which bind parameters safely and make tenant predicates explicit. Where raw SQL must be used, ensure only trusted values are interpolated and add explicit tenant_id predicates to every read/write against tenant-scoped tables.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE on tenant-scoped table (memory_entries) uses WHERE id = ${id} without constraining to the caller's tenant (tenant_id missing). This allows a row id from another tenant to be updated (TOCTOU/race possible since id was discovered earlier).
  - _Fix:_ Include the tenant context in the UPDATE WHERE clause (and persona_id if relevant). Example: WHERE id = ${id} AND tenant_id = ${tid} (or WHERE id = ${id} AND tenant_id = ${tid} AND persona_id = ${personaId}). Prefer using a single parameterized statement (or a transaction) that verifies tenant_id at update time to avoid TOCTOU races.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE on tenant-scoped table (memory_entries) uses WHERE id = ANY(${ids}) without constraining to the caller's tenant (tenant_id missing). This will update rows across tenants if ids include rows from other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (and persona_id if applicable). For example: UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Alternatively, perform the update with WHERE id = ANY(${ids}) AND tenant_id = ${tid} inside a single parameterized query/transaction and/or use RETURNING to confirm only rows from the tenant were affected.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE on tenant-scoped table (memory_entries) uses WHERE id = ${id} without constraining to the caller's tenant (tenant_id missing). The SELECT that discovered the id filtered by tenant, but the UPDATE omits tenant_id, enabling cross-tenant modification if an attacker supplies or guesses an id.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${id} AND tenant_id = ${tid} (and include persona_id when appropriate). Use a parameterized update that includes tenant check (or perform the SELECT+UPDATE in a transaction and update by both id and tenant_id) to prevent cross-tenant writes.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without constraining by tenant_id (reads all matching rows across tenants using a request-derived phone/title).
  - _Fix:_ Constrain the query to a specific tenant (WHERE tenant_id = <expectedTenant>) or otherwise avoid scanning tenant-scoped tables across tenants. If cross-tenant discovery is required, move pairings to an operator-only/global table or perform a two-step check that verifies conversation ownership first and then queries only the owning tenant's rows (e.g. query conversations by id+tenant_id and then select agent_knowledge restricted to that tenant). Ensure all SELECTs against tenant-scoped tables include an explicit tenant_id filter.
- **server/video-job-runner.ts:275** — SELECT from tenant-scoped table video_jobs performed with no tenant_id constraint (listStaleVideoJobs). This returns rows across tenants without per-tenant filtering.
  - _Fix:_ If this user-facing code should only see a single tenant, add AND eq(videoJobs.tenantId, tenantId) to the WHERE. If this is an intentional system-wide recovery job that must operate across tenants, restrict access to a privileged and audited path, or explicitly document and tightly control the privilege; still prefer to minimize exposed columns and perform any cross-tenant actions only after a tenant-aware claim/update that enforces tenant equality.
- **server/webhook-triggers.ts:223** — processTriggerEvent() queries the tenant-scoped table `webhook_triggers` by webhook_key without any tenant_id constraint: `WHERE w.webhook_key = ${webhookKey} AND w.enabled = true`. The webhookKey is caller-controlled/untrusted input, so this read can map an arbitrary webhook key to its tenant and allow acting in that tenant's scope without an explicit ownership check.
  - _Fix:_ Constrain the lookup to the intended tenant or otherwise validate ownership before using the row. Options: (a) require and validate an explicit tenantId/claim from the caller and include `AND w.tenant_id = ${tenantId}` in the WHERE, (b) encode the tenant id inside the webhook key and verify the decoded tenant matches the stored row, or (c) store webhook keys as tenant-scoped secrets and look up via both key+tenant. Ensure the code never acts on a trigger row derived solely from an untrusted webhook_key without an explicit tenant ownership check.

### MEDIUM (38)

- **server/auth.ts:336** — deleteSession deletes from auth_sessions WHERE token = ${hashSessionToken(token)} with no tenant_id restriction (writes to a tenant-scoped table without tenant scoping).
  - _Fix:_ Include tenant_id in the DELETE WHERE clause when possible (e.g. DELETE ... WHERE token = ${tokenHash} AND tenant_id = ${tenantId}), or validate the token-to-tenant mapping before deleting to avoid cross-tenant effects.
- **server/auto-asset-capture.ts:184** — backfillProjectAssets SELECTs conversations with: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ... — this background query scans conversations across all tenants (no tenant filter) and reads tenant-scoped rows without tenant scoping.
  - _Fix:_ Scope the backfill to a single tenant at a time (accept a tenantId parameter or iterate tenants) or add an explicit tenant filter to the WHERE clause. If a cross-tenant sweep is required, run it as a controlled, platform-admin maintenance job with auditing.
- **server/auto-asset-capture.ts:207** — Later in backfillProjectAssets the code SELECTs messages by conversation_id without tenant_id: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' ... — reading a tenant-scoped messages table without enforcing tenant ownership in the query.
  - _Fix:_ Include tenant_id in the WHERE (e.g. AND tenant_id = ${c.tenant_id}) or join messages to conversations and filter by conversations.tenant_id to ensure reads are constrained to the owning tenant before processing.
- **server/channel-routing.ts:64** — getPersonaForChannel returns persona_id from a global channel_routes table without any tenant context — consumer can receive a persona id belonging to another tenant.
  - _Fix:_ Require caller tenantId and filter channel_routes by tenant (e.g. WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId}). Additionally, after retrieving persona_id, verify the persona row belongs to that tenant (SELECT FROM personas WHERE id = persona_id AND tenant_id = ${tenantId}) before returning it.
- **server/chat-engine.ts:3758** — Calling markReviewReused(conversationId, toolName) (capability-review) with only conversationId — a foreign identifier from the request — and no tenant context. The callee may read/write state keyed by conversationId without verifying the caller's tenant, enabling cross-tenant leakage or contamination.
  - _Fix:_ Pass the caller's tenantId into markReviewReused or otherwise validate that the conversationId belongs to the caller's tenant before invoking. Prefer changing the capability-review API to accept tenantId and enforce tenant-scoped filters on all DB reads/writes.
- **server/chat-engine.ts:4103** — Calling postDiagnosticReport([stuckPattern]) without any tenant context. The diagnostics reporting path may create persistent records without associating them with the caller's tenant, risking cross-tenant contamination.
  - _Fix:_ Add tenantId as an explicit parameter to postDiagnosticReport (or include tenantId in stuckPattern) and ensure postDiagnosticReport applies tenant-scoped filters/inserts (e.g. set tenant_id on INSERTs and filter by tenant_id on reads).
- **server/chat-engine.ts:4190** — Using hasFiredThisSession(sessionKey) / markFired(sessionKey) with sessionKey = `${conversationId||'anon'}-${persona?.id||0}` that does not include tenantId. If the tool-pick-validator persists or queries session state by this key, it may be operating on a foreign conversation without tenant validation.
  - _Fix:_ Include tenantId in the session key or change hasFiredThisSession/markFired signatures to accept tenantId explicitly. Ensure the tool-pick-validator enforces tenant scoping on any persisted/session state (filter or store with tenant_id).
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments is executed with no tenant_id filter (cross-tenant read of commitments table).
  - _Fix:_ If this enumeration is intentional (background job that schedules work per-tenant), ensure it runs in a properly privileged context and cannot be abused by request-level callers. Prefer maintaining a separate per-tenant queue/index (or tenants table flag) so you can discover tenants with work without scanning tenant-scoped data, or otherwise document and restrict this operation to a trusted background process.
- **server/commitments.ts:135** — SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL ... is executed without a tenant_id constraint — the scanner reads across all tenants.
  - _Fix:_ If the scanner intentionally fans-in across tenants for operator visibility, ensure it runs only in a privileged background/operator context and that any included content is redacted (as documented). If tenant-scoped processing is required, run per-tenant queries (SELECT ... WHERE tenant_id = ${tenantId}) or maintain an indexed table of flagged commitments per tenant to avoid unscoped reads.
- **server/data-protection.ts:403** — COUNT(*) query against tenant-scoped table "messages" lacks tenant_id filter: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}. Even though conversation id was fetched earlier for this tenant, the query does not include tenant_id = <tenant> and relies on implicit relationship rather than enforcing tenant scoping.
  - _Fix:_ Include the tenant predicate in the COUNT: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id} (or the caller's tenantId). Prefer always threading tenant_id into queries rather than relying on foreign id invariants.
- **server/doc-collections.ts:136** — SELECT in listCollections includes correlated subqueries counting rows in doc_chunks but those subqueries do not filter by tenant_id: `(SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id)`. This reads tenant-scoped rows without explicit tenant scoping in the subquery.
  - _Fix:_ Add tenant scoping in each subquery, e.g. `... FROM doc_chunks WHERE collection_id = c.id AND tenant_id = ${tenantId}` (and similarly for the DISTINCT doc_path/counts) so counts are computed only for the caller's tenant.
- **server/doc-collections.ts:457** — SELECT in getCollectionStatus uses several correlated subqueries over doc_chunks (chunk_count, doc_count, embedded_count, total_tokens) but the subqueries lack tenant_id filters. These queries read tenant-scoped data without explicit tenant scoping.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to each doc_chunks subquery (e.g. `SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id AND tenant_id = ${tenantId}` and the other aggregates) so results are scoped to the requested tenant.
- **server/dfy-intake.ts:50** — Lookup by token queries tenant-scoped dfy_intake_forms without constraining tenant_id: `SELECT ... FROM dfy_intake_forms WHERE token = $token`. The code relies on the unguessable token as the sole authorization check; this is a read against a tenant-scoped table with no tenant constraint (potential IDOR if tokens are misused or leaked).
  - _Fix:_ Either require and enforce a tenantId argument (add `AND tenant_id = ${tenantId}`), or explicitly document and justify that token-only access is intended and mitigate risk (e.g. ensure tokens are single-tenant and cannot be abused). Prefer adding tenant scoping where the caller has a tenant context.
- **server/glasses-gateway.ts:174** — UPDATE on apiKeys sets lastUsedAt and matches only on apiKeys.id (.where(eq(apiKeys.id, key.id))) without an explicit tenant filter. If apiKeys is a tenant-scoped table, this write lacks a tenant constraint.
  - _Fix:_ Include the tenant constraint in the WHERE clause when updating apiKeys. For example: .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))). Prefer always scoping writes to the owning tenant even when the row was fetched earlier.
- **server/job-queue.ts:375** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} without any tenant restriction. While intended as an admin helper, this performs an unscoped read on a tenant-scoped table by bare id.
  - _Fix:_ Document and centralize admin-only helpers; require an explicit admin-guard before calling this function. As a safety improvement, accept and validate an expected caller role or make the lookup part of an admin-only path that is access-controlled. Consider returning null if caller is not authorized rather than performing an unconstrained lookup.
- **server/job-queue.ts:391** — listJobs builds a WHERE clause conditionally and will run SELECT FROM agent_jobs with no tenant_id restriction if the caller omits tenantId. This enables unscoped listing of jobs across tenants by default.
  - _Fix:_ Require tenantId for tenant-facing callers and enforce it at the API boundary. If admin/global listing is intended, make that explicit via a separate admin method and protect it with authorization checks. Otherwise always add AND tenant_id = ${tenantId} when tenantId is provided and disallow empty/implicit global queries in tenant-facing code paths.
- **server/job-queue.ts:470** — getJobStats may run an aggregate SELECT across agent_jobs without a tenant_id filter when scope === ALL_TENANTS (tenantFilter is empty). This performs a cross-tenant aggregation on a tenant-scoped table without an explicit authorization/guard in this function.
  - _Fix:_ If ALL_TENANTS is intended only for platform-admins, require an explicit admin authorization check before running the query. Otherwise restrict the aggregation to the provided tenant via AND tenant_id = ${scope}. Make cross-tenant aggregation a clearly documented admin-only operation with enforced access control.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes a SELECT that scans agent_knowledge grouping by tenant_id and persona_id without restricting to a single tenant: SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge WHERE embedding_vec IS NOT NULL GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES}. This enumerates and then processes tenant-scoped data across all tenants in one operation (unscoped reads).
  - _Fix:_ If the monitor intentionally scans all tenants, ensure the code path is admin-only and protected by strong authorization. Otherwise, restrict the scan to a specific tenant (AND tenant_id = ${tenantId}) or iterate explicit tenant ids fetched from an admin-only list. Add application-level checks so tenant-scoped queries are never run unscoped from tenant-facing contexts.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions executes a SELECT over deliverable_grade_events without any tenant_id WHERE filter (aggregation across all tenants). This reads tenant-scoped data for all tenants in one query, violating the invariant that tenant-scoped tables must be constrained to a caller's tenant via an application-level filter.
  - _Fix:_ Run promotion scans per-tenant (iterate known tenant IDs and apply WHERE tenant_id = <tenant> inside each query) or otherwise make the intent explicit (admin-only job) and guard it accordingly. At minimum add an explicit tenant_id predicate when invoked in a tenant-context, or restrict this maintenance job to an admin process with documented approval and audited access. Ensure any generated artifacts do not leak cross-tenant contents.
- **server/lib/restraint-trust.ts:90** — Dynamic SQL via sql.raw(column) is used to update a column name derived from the request (outcome). Even though the code whitelists column names, using sql.raw with runtime-controlled identifiers is fragile and can lead to SQL injection or other safety/maintenance issues.
  - _Fix:_ Avoid embedding runtime-derived SQL identifiers with sql.raw. Instead use an explicit, server-side mapping from outcome → parameterized query (e.g. switch on outcome and execute one of four fixed UPDATE statements that reference a concrete column name), or use the query builder/Drizzle APIs that accept column references safely. This eliminates use of sql.raw for request-derived values and preserves parameterization of all data inputs.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() to inject a computed lockKey into SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). lockKey originates from ledgerKey/tenantId and may be caller-provided, risking SQL injection or unexpected behavior.
  - _Fix:_ Avoid sql.raw() with caller-derived input. Pass the lock value as a bound parameter (e.g. compute numeric BigInt and use parameter binding: sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), or validate/sanitize ledgerKey strictly before constructing the lock value.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL — no tenant constraint. If skills is a tenant-scoped table, this can leak or mix tenant data.
  - _Fix:_ If skills are tenant-scoped, include tenant_id = ${tenantId} in the WHERE clause (and still allow persona_id IS NULL global entries if intended). If skills are intentionally global, document that and keep as-is; otherwise enforce tenant scoping.
- **server/process-governor.ts:396** — SELECT in desk_status left-joins personas without constraining the personas table by tenant: LEFT JOIN personas p ON p.id = ad.persona_id. If personas is tenant-scoped this can read another tenant's persona rows via the join.
  - _Fix:_ Scope the joined personas to the tenant (e.g. LEFT JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId} or include p.tenant_id = ad.tenant_id) so the join cannot return rows from other tenants.
- **server/process-governor.ts:890** — In restart_agent the SELECT from agent_desks left-joins personas without a tenant constraint on personas: LEFT JOIN personas p ON p.id = ad.persona_id. This reads personas without assuring tenant ownership.
  - _Fix:_ Add tenant scoping to the join (e.g. ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) or otherwise verify persona rows belong to the tenant before using their data.
- **server/recurring-messages.ts:259** — SELECT from tenant-scoped table `agent_knowledge` without any tenant_id constraint (global read).
  - _Fix:_ Avoid unscoped reads against tenant-scoped tables. Either run a per-tenant query (add WHERE tenant_id = ${tenantId}) or explicitly document + centralize the trusted global scan path and minimize the returned columns. If a global scan is required, ensure every subsequent write/UPDATE uses tenant_id in the WHERE clause and the global access is limited to an authorized system-only context.
- **server/routes.ts:480** — DELETE FROM tenant-scoped table `order_lookup_codes` without tenant_id constraint (pruneOrderLookupCodes). This will delete rows across all tenants.
  - _Fix:_ Perform per-tenant cleanup (delete WHERE tenant_id = <tid> AND expires_at < NOW()) or run the deletion only in a controlled admin context. Alternatively, iterate tenants and prune each tenant's rows individually.
- **server/routes.ts:500** — SELECT uses subqueries on tenant-scoped tables (`conversations`, `projects`) with no tenant_id constraint in cleanupTestTenants. These subqueries read tenant-scoped data across all tenants.
  - _Fix:_ Make the intended scope explicit: if this must scan across all tenants require a platform-admin check and log the intent. Otherwise constrain the subqueries to the caller/target tenant (e.g. add WHERE tenant_id = ${tenantId}) or use an admin-only maintenance job that handles tenant isolation deliberately.
- **server/routes.ts:621** — SELECT from tenant-scoped table `projects` (backfillProjectDriveFolders) with no tenant_id constraint. This reads project rows for all tenants.
  - _Fix:_ If this backfill must operate across tenants, gate it behind explicit platform-admin authorization and ensure operators understand it touches all tenant data. If not, restrict to a single tenant (WHERE projects.tenant_id = ${tenantId}) or iterate tenants one-by-one and process only that tenant's projects.
- **server/seed.ts:41** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id filter (reads a tenant-scoped table globally).
  - _Fix:_ Constrain the count to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant. Use parameterized Drizzle filters (e.g. .where(eq(research_sessions.tenantId, tenantId))) so the check only considers the caller/target tenant's rows.
- **server/self-improvement.ts:576** — INSERT into tenant-scoped table `experiments` uses tenantId: data.tenantId! but the function accepts an optional tenantId and does not validate it. This can lead to inserts without a valid tenant_id or with an untrusted value.
  - _Fix:_ Require and validate a positive integer tenantId at the API boundary (or within logExperiment). Do not use the non-checked `!` operator. e.g. assert Number.isInteger(data.tenantId) && data.tenantId>0 before calling db.insert, or change the function signature to require tenantId.
- **server/storage.ts:698** — getDailyNotes accepts an optional tenantId and will SELECT daily_notes without any tenant constraint when tenantId is omitted — allowing reads across tenants if callers omit the tenant parameter.
  - _Fix:_ Make tenantId required for tenant-scoped reads or require callers to pass the explicit ALL_TENANTS sentinel when a global read is intended; validate via requiredTenantScope/tenantScope to avoid silent unscoped behavior.
- **server/storage.ts:707** — getDailyNote accepts optional tenantId and may SELECT daily_notes without a tenant filter when tenantId is omitted, enabling cross-tenant reads.
  - _Fix:_ Require and validate tenantId or force callers to use the explicit ALL_TENANTS sentinel for platform-wide behavior. Use tenantScope/requiredTenantScope to build the WHERE clause so invalid values cannot silently bypass filtering.
- **server/storage.ts:569** — getMemoryEntries accepts an optional tenantId and will perform a SELECT from `memory_entries` without a tenant filter if tenantId is omitted (silent unscoped read).
  - _Fix:_ Require tenantId (or explicit ALL_TENANTS sentinel) for tenant-scoped reads or make the permissive omission an explicit, audited admin path. Use tenantScope/requiredTenantScope to ensure validity.
- **server/tools/domains/files/handlers.ts:940** — UPDATE projects SET updated_at = ... WHERE id = ${projId} executes without an explicit tenant_id WHERE clause.
  - _Fix:_ Also constrain this UPDATE by tenant (e.g. WHERE id = ${projId} AND tenant_id = ${tenantId}) to make tenant-scoping explicit in the SQL, even if project ownership was verified earlier — prefer app-level tenant predicates on every write to tenant-scoped tables.
- **server/twilio.ts:116** — SELECT FROM `conversations` by id is executed without a tenant_id constraint before using the result to validate a pairing (reads a tenant-scoped table row without narrowing to a tenant).
  - _Fix:_ Use an ownership-check that queries conversations with both id and tenant_id (e.g. SELECT 1 FROM conversations WHERE id = ${conversationId} AND tenant_id = ${claimedTenant} LIMIT 1) so you never perform unscoped reads of tenant-scoped tables. Prefer verifying existence via an existence/boolean query rather than returning full rows when validating cross-tenant references.
- **server/watchlist.ts:16** — INSERT into watchlist_items accepts createdByPersonaId and writes it directly as created_by_persona_id without validating that the persona belongs to the provided tenantId (foreign-id write without owner verification).
  - _Fix:_ Before inserting, validate that createdByPersonaId (if provided) exists and is owned by the same tenant (SELECT persona WHERE id = $id AND tenant_id = $tenantId). Reject or null it if ownership doesn't match, or pass only persona ids confirmed to belong to the tenant.
- **server/watchlist.ts:44** — UPDATE writes escalate_to_persona_id from caller input directly (escalateToPersonaId) without verifying that the referenced persona belongs to the tenant. This is a foreign-id write without ownership validation.
  - _Fix:_ Validate the provided escalateToPersonaId belongs to the tenant before writing (e.g. SELECT id FROM personas WHERE id = ${escalateToPersonaId} AND tenant_id = ${tenantId}). Only set the column when ownership is confirmed; otherwise reject the change or set to NULL.
- **server/watchlist.ts:95** — INSERT into watchlist_alerts writes watchlist_item_id provided by caller without first validating that the referenced watchlist_item belongs to the same tenant (foreign-id write without ownership validation).
  - _Fix:_ Ensure watchlist_item_id belongs to tenantId before inserting (e.g. SELECT id FROM watchlist_items WHERE id = ${watchlistItemId} AND tenant_id = ${tenantId}). Reject or refuse to insert if the referenced item is absent or owned by another tenant.

### LOW (4)

- **server/data-protection.ts:37** — Admin/migration SELECT against tenant-scoped table "memory_categories" without tenant predicate: SELECT count(*)::int AS n FROM memory_categories WHERE tenant_id IS NULL. This reads tenant-scoped data globally (even if the intent is a migration/backfill).
  - _Fix:_ Restrict such checks to an explicit admin-only migration path, or document and gate them so they're not callable from normal runtime paths. When possible prefer per-tenant backfill loops or run under an explicit admin transaction/context so the operation's scope and auditability are clear.
- **server/data-protection.ts:71** — Admin/migration SELECT against tenant-scoped table "agent_knowledge" without tenant predicate: SELECT count(*)::int AS n FROM agent_knowledge WHERE tenant_id IS NULL. Similar to the memory_categories check, this reads tenant-scoped rows globally during schema migration/backfill.
  - _Fix:_ Perform this check in a dedicated, auditable migration step (not in regular runtime code) or run per-tenant. Ensure migrations that inspect/write tenant-scoped tables are executed with explicit admin authorization and logged, or add safeguards to avoid accidental cross-tenant reads in normal application runtime.
- **server/routes.ts:1388** — COUNT queries over tenant-scoped tables (conversations/messages/heartbeat_tasks/memory_entries/heartbeat_logs) in /api/public/stats are executed without tenant_id WHERE clauses.
  - _Fix:_ If the route is intended to return global platform-wide totals, keep the platform-admin guard and document why global reads are required. If the route should be tenant-scoped, add eq(table.tenant_id, tenantId) filters. Prefer explicit admin-only gating for cross-tenant reads.
- **server/routes.ts:1418** — Multiple db.execute() COUNT queries in /api/public/architecture read tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, etc.) without tenant_id constraints.
  - _Fix:_ If this is intentionally a platform-admin global architecture report, ensure the endpoint is strictly admin-gated (and log access). If it should be tenant-scoped, add explicit tenant_id filters (eq(..., tenantId)). Consider centralizing/annotating admin-only global queries to avoid accidental exposure.

### Resolved — confirmed false positives (182)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/agent-eval.ts:221** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with NO tenant_id check — write can affect another tenant's row if evalId is guessed or reused.
  - _Why false positive:_ evalId is obtained from the immediately preceding INSERT of this tenant's agent_evals row in the same flow, not from an untrusted boundary.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE in expireStaleApprovals updates agentApprovals rows across all tenants (no tenant_id constraint), then returns rows for downstream per-run updates. This can modify approvals for other tenants.
  - _Why false positive:_ Global expiry sweep intentionally updates stale approvals across tenants and carries each row's tenant_id for per-row follow-up; no tenant data is returned to a tenant-facing surface.
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — SELECT against tenant-scoped table(s) (agent_runs/agent_approvals) executed without constraining to a caller/worker tenant_id. The initial candidate query (db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`)) reads rows across all tenants.
  - _Why false positive:_ Boot/worker sweep intentionally scans pending approval runs across tenants and processes each candidate with its own row-carried tenant_id rather than mixing tenant output.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE on tenant-scoped table 'conversations' lacks tenant_id in the WHERE clause (writes project_id WHERE id = ${c.conversation_id} AND project_id IS NULL). This can modify rows belonging to other tenants if ids collide.
  - _Why false positive:_ Backfill maintenance loop derives conversation_id/project_id/tenant_id from a cross-checked SELECT joining tenant-consistent project/conversation rows, then repairs housekeeping linkage/transcript state.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} — missing tenant_id constraint. This can modify a commitment belonging to another tenant.
  - _Why false positive:_ row.id comes from the immediately prior claim query constrained by c.tenant_id = tenantId; this branch only reverts that already-claimed row when its carried tenantId is invalid.
- **server/email.ts:429** _(system-sweep)_ — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} has no tenant_id constraint. This write can modify rows belonging to other tenants (cross-tenant persistence).
  - _Why false positive:_ Backfill worker globally fills missing email bodies as housekeeping only; it mutates per-row content fields and does not expose mixed tenant data.
- **server/embeddings.ts:852** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed with no tenant_id constraint. This write will update memory rows regardless of tenant ownership (cross-tenant write risk).
  - _Why false positive:_ Backfill loop globally repairs missing vector columns for selected rows and only mutates housekeeping/derived embedding fields per row, with no tenant-facing mixed output.
- **server/embeddings.ts:869** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed with no tenant_id constraint. This write can change rows belonging to other tenants (cross-tenant persistence).
  - _Why false positive:_ Global embedding backfill updates derived vector data per row across tenants as maintenance only, without returning cross-tenant content to users.
- **server/felix-brain.ts:23** _(system-sweep)_ — rehydrateTaskStateOnBoot performs a SELECT from the tenant-scoped table `agent_runs` without constraining rows to a specific tenant (no WHERE tenant_id = ...). This reads rows across all tenants and then uses each row's tenant_id to populate in-memory task state, violating the invariant that every read against tenant-scoped tables must include an application-level tenant filter.
  - _Why false positive:_ Boot-time rehydration intentionally reads running/pending runs across tenants and stores each row under its own tenant_id in memory, not in a combined tenant-facing response.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table `code_proposals` (setting status = 'archived_stale') uses WHERE id = ANY(${ids}) with no tenant_id constraint — may update rows belonging to other tenants.
  - _Why false positive:_ Health-audit TTL pass is a platform-wide maintenance action archiving stale proposals across tenants; it only mutates status fields and emits audit counts/findings.
- **server/health-audit.ts:366** _(system-sweep)_ — UPDATE on tenant-scoped table `heartbeat_tasks` (disabling tasks) uses WHERE id = ANY(${ids}) with no tenant_id guard — can modify tasks for other tenants.
  - _Why false positive:_ Platform-wide housekeeping disables stale heartbeat tasks across tenants, mutating only enabled status and not mixing tenant content into a tenant-facing output.
- **server/google-workspace.ts:2287** _(fk-transitive)_ — INSERT into `presenter_slide_images` omits tenant scoping (no tenant_id column/value supplied). If `presenter_slide_images` is tenant-scoped this allows writes not bound to a tenant and can associate images with sessions across tenants.
  - _Why false positive:_ presenter_slide_images is keyed by session_id to tenant-scoped presenter_sessions; child table insert without tenant_id is by-design transitive scoping
- **server/google-workspace.ts:2293** _(fk-transitive)_ — Second INSERT into `presenter_slide_images` (thumb variant) also omits tenant scoping (no tenant_id supplied).
  - _Why false positive:_ thumbnail row is another insert into child table presenter_slide_images scoped via tenant-owned presenter_sessions.session_id
- **server/job-queue.ts:154** _(system-sweep)_ — claimDueJobs updates agent_jobs rows (SET status='running' ...) using id IN (SELECT id FROM agent_jobs WHERE status='pending' ...) but does not constrain the SELECT or UPDATE by tenant_id. This lets the worker atomically claim and mutate pending jobs across all tenants by id without any tenant_id WHERE clause.
  - _Why false positive:_ global worker claim loop intentionally operates across tenants and returns row-carried tenant_id per claimed job rather than mixing tenant data
- **server/job-queue.ts:192** _(system-sweep)_ — reclaimExpiredLeases performs two UPDATEs over agent_jobs (marking reclaimed/terminal rows) without any tenant_id filter. This will mutate running/expired leases across all tenants in a single global operation.
  - _Why false positive:_ expired-lease reclaim is platform-wide housekeeping that only mutates job status/lease fields and returns ids/counts, not tenant content
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id predicate (CTE selects due rows across all tenants and the subsequent UPDATE updates rows across tenants). This allows cross-tenant reads/updates.
  - _Why false positive:_ runner is a platform-wide scheduler that claims due rows across tenants but returns/handles each row with its own tenant_id
- **server/research-engine.ts:169** _(system-sweep)_ — UPDATE against tenant-scoped table research_sessions issued without any tenant_id constraint (cleanupZombieSessions). This will mark sessions across all tenants as completed/ended, violating tenant isolation.
  - _Why false positive:_ zombie cleanup is platform-wide housekeeping updating stale session status only; no tenant content is returned or mixed
- **server/routes/conversations.ts:119** _(upstream-verified)_ — UPDATE on conversations omits tenant scoping: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates a tenant-scoped table by id only (no `AND tenant_id = ${tenantId}`).
  - _Why false positive:_ conv was just created with tenantId and project ownership was verified with WHERE id=projectId AND tenant_id=tenantId earlier in same path
- **server/routes/conversations.ts:123** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id. Per invariant, every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project/conversation rows already ownership-checked upstream
- **server/routes/conversations.ts:125** _(upstream-verified)_ — UPDATE on projects omits tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` has no `AND tenant_id = ${tenantId}`. This can modify another tenant's project row if ids are guessed or misused.
  - _Why false positive:_ same block first verifies project id belongs to tenant via SELECT ... FROM projects WHERE id=? AND tenant_id=tenantId
- **server/routes/conversations.ts:256** _(fk-transitive)_ — INSERT into project_notes omits tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` does not include tenant_id on the inserted row.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through tenant-checked project_id
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks tenant constraint — will disable delegation tasks across all tenants.
  - _Why false positive:_ boot-time cleanup intentionally disables runaway housekeeping tasks across tenants; status-only mutation, no tenant data exposure
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant constraint — will disable matching tasks for all tenants.
  - _Why false positive:_ boot-time cleanup intentionally disables duplicate housekeeping tasks across tenants; status-only mutation, no tenant data exposure
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true lacks tenant constraint — will disable these 'examine repository' tasks for every tenant.
  - _Why false positive:_ boot-time cleanup intentionally disables orphaned repo-examination housekeeping tasks across tenants; status-only mutation, no tenant data exposure
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE against tenant-scoped table `custom_tools` without tenant_id constraint (await db.delete(customTools).where(eq(customTools.id, tool.id))). This can remove another tenant's tool if ids collide or are guessed.
  - _Why false positive:_ internal revert helper deletes just-inserted custom tool by server-side row id, not direct request-supplied id
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table `browser_workflows` has no tenant filter — `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` updates by id only, allowing a caller who can supply a workflow id to modify rows belonging to other tenants.
  - _Why false positive:_ workflow row is first loaded with WHERE id/name and tenant_id=tenantId, then updated using that verified row id
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger missing tenant_id constraint. settleVentureReservation updates a ledger row by id only (WHERE id = ${reservationId}) with no tenant check.
  - _Why false positive:_ best-effort settlement updates server-created reservation row by primary key in internal workflow, not from untrusted tenant request boundary
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without tenant_id constraint (scans all tenants).
  - _Why false positive:_ heartbeat maintenance sweep intentionally scans running experiments across tenants and processes each row with its own tenant_id on update/event flow.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting is not tenant-scoped (no tenant_id filter) and iterates results across tenants.
  - _Why false positive:_ durability sweep intentionally retries pending rows across tenants and carries each row's tenant_id forward per-row after validation, without mixed tenant output.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT against tenant-scoped table `department_budgets` is executed without constraining by tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This performs an unconstrained cross-tenant read on a tenant-scoped table.
  - _Why false positive:_ heartbeat enforcement sweep intentionally iterates active budgets across tenants and emits per-row tenant-scoped events using each row's tenant_id.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE claims pending agent_wake_schedules using an inner SELECT that lacks any tenant_id constraint (UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE)). This can atomically claim and mutate rows across all tenants.
  - _Why false positive:_ scheduler sweep intentionally claims due wakes across tenants, then emits and finalizes each row using that row's tenant_id; housekeeping only, no mixed tenant response.
- **server/auth.ts:197** _(upstream-verified)_ — validateApiKey performs a db.select() FROM api_keys filtering only by keyHash/isRevoked and not by tenant_id. This reads a tenant-scoped table without constraining tenant ownership.
  - _Why false positive:_ API key lookup by globally unique secret-derived keyHash is the authentication step that establishes req.tenantId from the matched row; tenant scope cannot be supplied earlier in this call path.
- **server/auth.ts:206** _(upstream-verified)_ — Updating api_keys.lastUsedAt via db.update(...).where(eq(apiKeys.id, key.id)) does not include a tenant_id constraint (update on a tenant-scoped table without tenant scoping).
  - _Why false positive:_ lastUsedAt update acts on the exact api_keys row just looked up by unique keyHash and uses that row's server-derived id; ownership was established immediately above.
- **server/auth.ts:305** _(upstream-verified)_ — getSession SELECTs from auth_sessions by token only (WHERE token = ${tokenHash}) with no tenant_id constraint. This reads tenant-scoped session rows without tenant scoping.
  - _Why false positive:_ session lookup by bearer-derived token hash is the authentication primitive that establishes tenant context from the matched session row; no prior tenant input exists to constrain it.
- **server/auth.ts:1335** _(system-sweep)_ — loadSessionsFromDb issues DELETE FROM auth_sessions WHERE expires_at < ${now} (deleting expired sessions across all tenants) without a tenant scope — a write against a tenant-scoped table with no tenant constraint.
  - _Why false positive:_ boot-time expiry cleanup is platform-wide housekeeping deleting expired sessions only; it does not return or mix tenant data.
- **server/auth.ts:1339** _(system-sweep)_ — loadSessionsFromDb issues DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' (migration purge) without tenant scoping — global delete on a tenant-scoped table.
  - _Why false positive:_ boot-time migration purge removes legacy insecure session rows globally as housekeeping, without exposing tenant data.
- **server/auth.ts:1327** _(system-sweep)_ — clearAllSessionsAsPlatformAdmin executes DELETE FROM auth_sessions (removes all sessions across tenants) without tenant scoping. Although this function is intended as a platform-admin action, it performs a global write on a tenant-scoped table.
  - _Why false positive:_ explicit platform-wide session reset is guarded by one-time platform-admin authority and audit logging; intentional global housekeeping action.
- **server/auto-project.ts:135** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' omits tenant_id predicate (reads messages for a conversation without constraining to the caller's tenant).
  - _Why false positive:_ conversation ownership is proven earlier via conversations WHERE id = conversationId AND tenant_id = tenantId in the same transaction before reading its messages
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' omits tenant_id predicate (reading conversation messages without tenant constraint).
  - _Why false positive:_ assertConversationInTenant plus scoped conversation/project checks earlier in the same call path prove tenant ownership before reading messages for that conversation
- **server/auto-transcript.ts:128** _(system-sweep)_ — Query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without constraining messages by tenant_id. This reads tenant-scoped rows without a tenant filter.
  - _Why false positive:_ backfillProjectTranscripts is a platform-wide maintenance sweep; it only enumerates conversation ids with their own tenant_id and then processes each row via autoSaveProjectTranscript(c.id, c.tenant_id)
- **server/auto-transcript.ts:153** _(system-sweep)_ — Query with JOINs includes a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) that reads the tenant-scoped 'messages' table without a tenant_id constraint.
  - _Why false positive:_ maintenance backfill loop processes each linked conversation with its row-carried tenant_id and hands off to tenant-checked autoSaveProjectTranscript; no mixed tenant output is returned
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this writes a row without the caller's tenant context and breaks the invariant that all tenant-scoped writes must set tenant_id explicitly.
  - _Why false positive:_ invoice_items has no tenant_id and is a child of tenant-scoped invoices; parent invoice row is inserted with tenant_id = tid in the same transaction
- **server/channel-routing.ts:31** _(global-table)_ — SELECT joins a likely tenant-scoped table (personas) without any tenant_id constraint — query returns persona rows across tenants (no WHERE p.tenant_id = <caller tenant>).
  - _Why false positive:_ schema notes show personas has no tenant_id and channel_routes is created here without tenant_id with UNIQUE(channel), indicating an intentionally global routing registry
- **server/channel-routing.ts:54** _(global-table)_ — INSERT INTO channel_routes writes a caller-supplied personaId without validating that the persona belongs to the caller's tenant (and channel_routes has no tenant_id). This allows a caller to associate another tenant's persona with a channel.
  - _Why false positive:_ channel_routes is an intentionally global table and personas is also non-tenant-scoped per schema, so no tenant ownership check applies here
- **server/chat-engine/project-context.ts:27** _(fk-transitive)_ — Query reads from project_conversations (pc) without an explicit tenant_id constraint. The WHERE clause filters p.tenant_id but does not constrain pc. This relies on indirect validation (project join) rather than the required explicit tenant filter on every tenant-scoped table.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped through joined projects filtered by p.tenant_id = tenantId
- **server/chat-engine/project-context.ts:45** _(fk-transitive)_ — Inner EXISTS() references project_conversations (pc) without constraining its tenant_id. Even though the outer query filters projects by p.tenant_id, every access to a tenant-scoped table must include an explicit tenant filter.
  - _Why false positive:_ project_conversations has no tenant_id; EXISTS ties pc.project_id to outer tenant-scoped projects row p.id where p.tenant_id = tenantId
- **server/chat-engine/project-context.ts:159** _(fk-transitive)_ — Selecting from project_files uses WHERE project_id = ${projectId} but does NOT include tenant_id = ${tenantId}. The code relies on prior project ownership checks instead of the required explicit tenant constraint on this tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by previously verified tenant-owned projectId
- **server/chat-engine/project-context.ts:171** _(fk-transitive)_ — Selecting from project_notes uses WHERE project_id = ${projectId} without an explicit tenant_id constraint. Every read against a tenant-scoped table must include the caller's tenant filter.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by tenant-verified project_id
- **server/chat-engine/project-context.ts:184** _(fk-transitive)_ — Query joining project_conversations (pc) and conversations (c) filters c.tenant_id but does not explicitly constrain pc.tenant_id. Accessing project_conversations without an explicit tenant filter violates the per-query tenant-scope requirement.
  - _Why false positive:_ project_conversations has no tenant_id; join is scoped via project_id and conversations tenant filter
- **server/coinbase-commerce.ts:73** _(global-table)_ — SELECT from tenants (coinbase_commerce_webhook_secret) is executed without any tenant_id constraint — this reads decrypted webhook secrets across all tenants.
  - _Why false positive:_ tenants is not tenant-scoped and webhook verification intentionally scans configured tenant secrets for shared endpoint selection
- **server/data-protection.ts:149** _(system-sweep)_ — SELECT from tenant-scoped table "conversations" without a tenant_id predicate. The query SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} can return rows for all tenants and exposes tenant-specific metadata prior to per-row scoped deletes.
  - _Why false positive:_ global purge sweep reads expired rows across tenants and then mutates each row using its own tenant_id without tenant data exposure to users
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE against tenant-scoped table doc_chunks uses only the primary id in the WHERE clause: `UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}`. This allows updating a doc_chunk row that may belong to another tenant.
  - _Why false positive:_ row.id comes from immediately prior SELECT on doc_chunks constrained by collection_id and tenant_id in same call path
- **server/email.ts:370** _(system-sweep)_ — SELECT id FROM inbox_messages WHERE message_id = ${msgId} is executed with no tenant_id constraint. inbox_messages is tenant-scoped, so this read can return rows from other tenants.
  - _Why false positive:_ shared inbox poller is platform-wide housekeeping dedupe over inbound messages, not tenant-facing data return
- **server/email.ts:415** _(system-sweep)_ — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... is executed with no tenant_id constraint. This reads tenant-scoped rows across all tenants.
  - _Why false positive:_ backfill loop is maintenance across all tenants and updates each row by its own id without mixing tenant output
- **server/embeddings.ts:811** _(system-sweep)_ — SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... is executed with no tenant_id constraint. agent_knowledge is tenant-scoped, so this read can return entries across tenants.
  - _Why false positive:_ embedding backfill intentionally scans all tenants and updates each knowledge row using its carried tenant_id
- **server/embeddings.ts:842** _(system-sweep)_ — SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200 is executed with no tenant_id constraint. memory_entries is tenant-scoped, so this read can touch rows across tenants.
  - _Why false positive:_ vector backfill is cross-tenant maintenance updating housekeeping field embedding_vec only
- **server/embeddings.ts:858** _(system-sweep)_ — SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... is executed with no tenant_id constraint. This reads tenant-scoped agent_knowledge rows across tenants.
  - _Why false positive:_ vector backfill is cross-tenant maintenance updating housekeeping field embedding_vec only
- **server/firecrawl.ts:239** _(upstream-verified)_ — UPDATE on scrapedPages uses only the row id in WHERE (.where(eq(scrapedPages.id, existing[0].id))) and omits a tenant_id constraint. This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ updated id is selected just above from scrapedPages under same tenant_id constraint in same function
- **server/firecrawl.ts:372** _(upstream-verified)_ — UPDATE on scrapedPages in the crawl loop uses only the row id in WHERE (.where(eq(scrapedPages.id, existing[0].id))) and omits a tenant_id constraint. This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ updated row id comes from prior SELECT scoped by url and tenantId in same path
- **server/google-drive.ts:879** _(upstream-verified)_ — UPDATE on projects sets driveFolderId/driveFolderUrl and matches only on projects.id (.where(eq(projects.id, projectId))) without an explicit tenant filter. This is a write against a tenant-scoped table without tenant_id in the WHERE clause.
  - _Why false positive:_ same function first loads project and rejects when project.tenantId !== tenantId
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` is executed without a tenant_id filter (reads across all tenants).
  - _Why false positive:_ admin health-audit stale-proposal TTL sweep operates across tenants for housekeeping only
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table `heartbeat_tasks` is executed with no tenant_id filter (reads tasks across all tenants).
  - _Why false positive:_ admin health-audit disables stale heartbeat rows cross-tenant as housekeeping without tenant data mixing
- **server/heartbeat-schedules.ts:60** _(system-sweep)_ — SELECT * FROM research_schedules executed with no tenant_id filter (reads schedules across all tenants).
  - _Why false positive:_ heartbeat scheduler intentionally scans due research schedules across tenants and processes each row with its own tenant_id
- **server/heartbeat-startup.ts:81** _(system-sweep)_ — UPDATE heartbeat_tasks performed via raw SQL without constraining by tenant_id (WHERE id = ${t.id} only). heartbeat_tasks is tenant-scoped; this update can modify another tenant's row.
  - _Why false positive:_ startup maintenance loop normalizes heartbeat task model values across tenants by per-row id only
- **server/heartbeat-startup.ts:85** _(system-sweep)_ — UPDATE heartbeat_tasks performed via raw SQL without constraining by tenant_id (WHERE id = ${t.id} only). heartbeat_tasks is tenant-scoped; this update can modify another tenant's row.
  - _Why false positive:_ startup maintenance loop normalizes reflection task model values across tenants by per-row id only
- **server/heartbeat-task-scheduler-storage.ts:12** _(system-sweep)_ — SELECT * FROM heartbeat_tasks executed with no tenant_id constraint. heartbeat_tasks is tenant-scoped and this read is unscoped.
  - _Why false positive:_ dedicated scheduler-only boundary intentionally reads due heartbeat tasks across tenants for per-row processing
- **server/heartbeat-task-scheduler-storage.ts:44** _(system-sweep)_ — UPDATE heartbeat_tasks SET last_run_at/next_run_at WHERE id = ${id} without a tenant_id constraint. This can update rows belonging to other tenants by id alone.
  - _Why false positive:_ scheduler-only claim step updates due heartbeat tasks cross-tenant by selected ids as housekeeping
- **server/heartbeat.ts:256** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10 executed with no tenant_id constraint. event_log is tenant-scoped (tenant_id used elsewhere) — unscoped reads can expose data across tenants.
  - _Why false positive:_ heartbeat event processor intentionally sweeps pending events across tenants and routes each using row-carried tenant_id
- **server/heartbeat.ts:1574** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... executed without any tenant_id constraint. heartbeat_logs is tenant-scoped; this read can leak cross-tenant information.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id and this cooldown check is against a non-tenant-scoped log table
- **server/heartbeat.ts:3142** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint (in delegateTaskFromChat). This may affect tasks from other tenants.
  - _Why false positive:_ Row was just created via createHeartbeatTask with the same tenantId; update targets fresh server-created newTask.id, not cross-tenant caller input.
- **server/job-queue.ts:233** _(internal-worker-pk)_ — completeJob updates agent_jobs (SET status='succeeded' ...) WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but does not include tenant_id in the WHERE. A caller that knows an id could potentially complete a job for another tenant if they can influence that call.
  - _Why false positive:_ Worker completion updates by server-managed job id plus attempts lease fence; function is for internal worker state transition, not tenant request input.
- **server/lib/action-ledger.ts:268** _(system-sweep)_ — Cross-tenant SELECT from tenant-scoped table `action_attempts` with no tenant_id filter (listUnknownAttemptsAllTenants). This returns rows for all tenants instead of restricting to a caller tenant.
  - _Why false positive:_ Commented cross-tenant reconciler helper; rows carry own tenant_id and are for maintenance, not tenant-facing mixed output.
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — UPDATE of tenant-scoped table `action_attempts` (sweepStaleAttempts) performed without any tenant_id constraint — inner SELECT and UPDATE operate across all tenants.
  - _Why false positive:_ Cross-tenant stale-row sweep is maintenance housekeeping only, mutating status/error fields without tenant-facing data mixing.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT on tenant-scoped table `projects` performs a global aggregation across all tenants (SELECT tenant_id, count(*) FROM projects WHERE tags && ... GROUP BY tenant_id) with NO tenant_id filter. This violates the invariant that reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ Boot/backfill import intentionally evaluates snapshot coverage across tenants as maintenance logic, not tenant-facing query handling.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` without a tenant_id filter (reads all tenants' running rows).
  - _Why false positive:_ Read-only cross-tenant audit sweep for stuck tasks; returns bounded maintenance findings carrying each row's own tenant_id.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT from tenant-scoped table `mind_tickets` without a tenant_id filter (reads across tenants).
  - _Why false positive:_ Read-only cross-tenant stuck-task audit over mind_tickets is maintenance reporting, bounded and per-row tenant-carried.
- **server/lib/system-probes.ts:163** _(system-sweep)_ — Direct DB query counting rows from tenant-scoped `messages` table has no tenant_id constraint (SELECT COUNT(*) FROM messages). This probes tenant data across all tenants.
  - _Why false positive:_ Self-health probe intentionally does platform-wide aggregate count and does not return tenant row data or mix content.
- **server/martech-bundle.ts:326** _(upstream-verified)_ — UPDATE against tenant-scoped table `tenant_voice_profiles` uses WHERE id = existing.id without constraining tenant_id. This can allow cross-tenant row modifications if id provenance is ever untrusted.
  - _Why false positive:_ Row id comes from prior getVoiceProfile({ tenantId, profileName }) lookup in same flow, so update targets tenant-owned row.
- **server/memory-graph.ts:708** _(system-sweep)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id constraint. Updating a tenant-scoped memory row by id only can cross tenant boundaries if id is ever mis-bound.
  - _Why false positive:_ Batch categorization is a maintenance loop scoped by prior SELECT tenant_id = tenantId; update uses ids from that per-tenant result and only sets housekeeping category_id.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE on knowledge_triples omits tenant constraint. The code executes: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}` without constraining by tenant_id, allowing accidental/unauthorized updates across tenants.
  - _Why false positive:_ c.id is sourced from immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId.
- **server/mpeg-engine.ts:1058** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code relies on assertProjectInTenant(...) for ownership but the INSERT itself omits tenant_id (columns are expected NOT NULL per invariant), breaking the project-level guard that tenant isolation must be enforced by SQL filters/values on every write.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; assertProjectInTenant verifies parent ownership before insert.
- **server/mpeg-engine.ts:1445** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id. Like the single-job path, this uses assertProjectInTenant(...) but then writes the row without tenant_id, violating the invariant that all tenant-scoped INSERTs must supply tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; assertProjectInTenant verifies parent ownership before insert.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions without tenant_id constraint (global scan). validateSubscriptionsOnStartup queries all active subscriptions across tenants.
  - _Why false positive:_ Startup validation intentionally scans all tenants and processes each subscription with its own row-carried tenant_id in maintenance flow.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT FROM oauth_subscriptions (provider = 'google-workspace') omits tenant constraint — reads tenant-scoped rows across all tenants.
  - _Why false positive:_ Boot-time auto-reconnect enumerates tenant ids across subscriptions for maintenance; per-tenant actions use each row's tenant_id.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — missing tenant_id constraint. This updates a tenant-scoped row by id only, derived from a prior global SELECT.
  - _Why false positive:_ Maintenance loop updates status only for rows selected in global startup sweep, using row-carried tenant context and not exposing tenant data.
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — missing tenant_id constraint (second occurrence).
  - _Why false positive:_ Maintenance loop deactivates stale subscriptions during startup sweep; status-only mutation on rows processed per row tenant context.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh: SELECT provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE — global scan across tenants without tenant_id filtering.
  - _Why false positive:_ Proactive refresh is a platform-wide housekeeping loop over active subscriptions, operating per row using tenant_id and not mixing tenant content into outputs.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — global read across tenants.
  - _Why false positive:_ Daily cron force-refresh intentionally scans all tenants and processes each row with its own tenant_id; only housekeeping refresh/status effects, no cross-tenant data returned or mixed.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} has no tenant_id constraint. A caller-provided personaId is used to read a tenant-scoped table without verifying ownership/tenant association.
  - _Why false positive:_ Schema notes personas has no tenant_id column, so loading persona by global id is not a tenant-scoped read.
- **server/persona-routing-outcomes.ts:44** _(global-table)_ — refreshTenant background query joins action_outcomes (scoped by ao.tenant_id = ${tenantId}) to personas p ON p.id = ao.persona_id but does not constrain personas by tenant. This reads the tenant-scoped personas table without a tenant_id constraint in the JOIN/WHERE.
  - _Why false positive:_ Joined personas table is globally scoped per schema (no tenant_id); tenant isolation comes from action_outcomes filtered by tenant_id.
- **server/plan-executor.ts:880** _(system-sweep)_ — Boot-time recovery UPDATE resets all stale 'executing' plans back to 'approved' without any tenant_id constraint (UPDATE plans SET status='approved' WHERE status='executing' AND updated_at < ... RETURNING id). This performs cross-tenant writes and breaks the per-tenant isolation invariant.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping reset of stale executing plans; mutates status fields only and does not expose or mix tenant data.
- **server/plan-executor.ts:887** _(system-sweep)_ — Selecting pending approved plans uses an unrestricted query (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50) with no tenant_id filter. The code then schedules executePlan for each id, potentially causing cross-tenant reads/executes without tenant scoping.
  - _Why false positive:_ Global dispatcher intentionally enumerates approved plans across tenants by id and schedules execution; this is maintenance/orchestration rather than tenant-facing mixed output.
- **server/project-brain.ts:349** _(system-sweep)_ — Query that loads conversations for a project lacks tenant constraint: SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}. conversation rows are not scoped to tenant.
  - _Why false positive:_ Backfill is a maintenance sweep creating per-project brain files; it iterates rows individually and does not return or combine multiple tenants' content into a tenant-facing response.
- **server/process-governor.ts:897** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} (in restart_agent) does not include tenant_id in the WHERE clause. This updates a tenant-scoped table without confirming the row's tenant, allowing cross-tenant modification if ids collide.
  - _Why false positive:_ desk.id comes from a prior SELECT on agent_desks scoped by ad.tenant_id = tenantId in the same call path, so the update targets rows already tenant-verified.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} with no tenant_id filter — allows modifying rows belonging to other tenants.
  - _Why false positive:_ Scheduler sweep processes due jobs across tenants using each row's carried tenant_id for downstream actions; this update only advances housekeeping content/timestamps on the current row.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ${storedId} with no tenant_id constraint — allows cross-tenant modification if id is tampered with or mis-sourced.
  - _Why false positive:_ storedId is obtained immediately from a prior SELECT on memory_entries constrained by input.tenantId and personaId in the same path.
- **server/research-engine.ts:456** _(fk-transitive)_ — INSERT into project_notes (auto-deposit summary) does not include tenant_id. Per project/DB schema conventions tenant_id must be explicitly set on tenant-scoped tables to enforce isolation.
  - _Why false positive:_ project_notes has no tenant_id column per schema and is isolated transitively via project_id; ownership is also checked with assertProjectInTenant before insert.
- **server/research-engine.ts:465** _(fk-transitive)_ — INSERT into project_notes inside the findings loop does not include tenant_id. Writing tenant-scoped artifacts without tenant_id breaks the invariant that all tenant-scoped inserts must pass tenantId explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id to tenant-checked parent project/session flow.
- **server/routes.ts:182** _(system-sweep)_ — SELECT from tenant-scoped table `file_storage` with no tenant_id constraint (restoreUploadsFromDb). This reads file blobs for all tenants on startup.
  - _Why false positive:_ startup restore is a platform-wide housekeeping loop restoring blobs to disk only; it does not return mixed tenant data to any tenant-facing surface.
- **server/routes.ts:2054** _(upstream-verified)_ — SELECT FROM tenant-scoped table `presenter_sessions` by token in /api/presenter/:token/repair-images is done without tenant_id constraint. This reads session rows across tenants based solely on token.
  - _Why false positive:_ internal maintenance endpoint is gated by x-internal-key constant-time check before lookup, so tenant ownership is enforced by the same call path's internal authorization.
- **server/routes.ts:4571** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The code executes: INSERT INTO project_notes (project_id, note, author) VALUES (...) without passing tenant_id, violating the invariant that every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and note is attached via project_id selected from conversations already scoped by tenant_id.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table inbox_messages lacks tenant_id predicate. The statement `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` will mark a message read by id without verifying it belongs to the caller's tenant, allowing cross-tenant modification if an attacker supplies/guesses an id.
  - _Why false positive:_ msg was selected just above with WHERE id = msgId AND tenant_id = tenantId before updating by that trusted row id.
- **server/routes/projects.ts:165** _(fk-transitive)_ — The SELECT at /api/projects builds subqueries counting project_files, project_notes, and project_conversations without constraining those dependent tables to the caller's tenant (the inner subqueries lack a tenant filter).
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and counts are scoped transitively from already tenant-filtered parent projects row p.
- **server/routes/projects.ts:201** _(upstream-verified)_ — The UPDATE at line 616: 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' executes against the projects table without a tenant_id constraint.
  - _Why false positive:_ shown line is a tenant-constrained dependent read joined through projects with p.tenant_id = tenantId; first-pass issue text does not match snippet.
- **server/routes/projects.ts:616** _(upstream-verified)_ — The UPDATE at /api/projects/:id/files-base64 (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ same handler verifies project ownership just above with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before this timestamp-only update.
- **server/routes/projects.ts:684** _(upstream-verified)_ — The UPDATE at /api/projects/:id/files (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ same handler verifies project ownership just above with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before this timestamp-only update.
- **server/routes/projects.ts:568** _(fk-transitive)_ — INSERT INTO project_notes omits tenant scoping / tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)).
  - _Why false positive:_ project_notes has no tenant_id column and insert is for a project already verified in-tenant immediately above.
- **server/routes/projects.ts:608** _(fk-transitive)_ — INSERT INTO project_files in /api/projects/:id/files-base64 omits tenant scoping / tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)).
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by prior verified tenant-owned projects row via project_id
- **server/routes/projects.ts:676** _(fk-transitive)_ — INSERT INTO project_files in multipart upload handler omits tenant scoping / tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)).
  - _Why false positive:_ project_files has no tenant_id and insert targets already tenant-verified project_id
- **server/routes/projects.ts:401** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not include tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project/conversation created and verified in same transaction
- **server/routes/projects.ts:703** _(upstream-verified)_ — SELECT * FROM project_files WHERE project_id = ${projectId} (listing project files) lacks an explicit tenant constraint on project_files.
  - _Why false positive:_ same handler first verifies project id belongs to tenant, then reads child rows by that verified project_id
- **server/routes/projects.ts:726** _(upstream-verified)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 has no tenant constraint on project_files.
  - _Why false positive:_ download handler first verifies tenant ownership of project, then fetches child file row by that project_id and path
- **server/routes/projects.ts:755** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} performs a deletion on a tenant-scoped table without an explicit tenant_id constraint in the DELETE statement.
  - _Why false positive:_ delete handler first verifies tenant-owned project, then deletes child project_files row constrained to that project_id
- **server/routes/projects.ts:472** _(upstream-verified)_ — The conversation ownership check uses EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = ${id} AND pc.conversation_id = ${primaryConversationId}) but that subquery does not constrain project_conversations by tenant. This is a check that reads a referenced (foreign) row without tenant scoping.
  - _Why false positive:_ outer query already constrains conversation to tenant and same project id; EXISTS only checks link presence for that verified project
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE sculptor_sessions without tenant constraint — updates session status by id only (WHERE id = ${session.id}) but does not ensure tenant_id = caller tenant.
  - _Why false positive:_ updates newly inserted sculptor_sessions row by server-generated session.id within internal server flow, not untrusted request id
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE sculptor_sessions setting conversation_id does not restrict by tenant — WHERE id = ${session.id} has no tenant_id check.
  - _Why false positive:_ updates newly created session by internal server-generated primary key after launch result, not caller-controlled id
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE sculptor_sessions in monitorSession (finalization path) lacks tenant scoping — WHERE id = ${sessionId} without tenant_id allows cross-tenant modification.
  - _Why false positive:_ monitorSession is internal worker finalizing a specific session by server-originated sessionId/runId pair, not cross-tenant request input
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks has no tenant filter — heartbeat_tasks is tenant-scoped elsewhere (tenant_id used in other statements), so this read is cross-tenant.
  - _Why false positive:_ boot-time housekeeping intentionally counts/disables runaway heartbeat tasks across tenants and only mutates enabled/status fields
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant filter while heartbeat_tasks is tenant-scoped elsewhere.
  - _Why false positive:_ seed cleanup intentionally finds/disables duplicate session-logger heartbeat tasks platform-wide; no tenant data returned
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) is missing tenant scoping while later seed logic expects admin-scoped changes.
  - _Why false positive:_ maintenance sweep counts/disables orphaned repo-examination heartbeat tasks across tenants and only changes housekeeping state
- **server/seed.ts:3662** _(system-sweep)_ — UPDATE against tenant-scoped table `memory_entries` has no tenant_id constraint (mass-update across all tenants).
  - _Why false positive:_ idempotent seed normalizer intentionally fixes text in memory_entries across tenants without returning mixed tenant data
- **server/seed.ts:3688** _(system-sweep)_ — UPDATE on `conversations` (SET model = 'gpt-5' WHERE model = 'gpt-4.1') lacks any tenant_id restriction — it will modify conversations across all tenants.
  - _Why false positive:_ platform-wide migration updates legacy model ids across all conversation rows as maintenance/backfill only
- **server/seed.ts:3634** _(system-sweep)_ — DELETE FROM tenant-scoped table `heartbeat_tasks` is executed without a tenant_id filter, potentially deleting rows for all tenants.
  - _Why false positive:_ boot-time cleanup purges sentinel-marked disabled heartbeat tasks across tenants as housekeeping only
- **server/seed.ts:3881** _(fk-transitive)_ — INSERT INTO `project_notes` does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are non-null/no default).
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by parent project_id selected from tenant-scoped projects row
- **server/skillify.ts:201** _(upstream-verified)_ — analyzeConversation reads messages without tenant scoping: const msgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)).orderBy(...). This selects from a tenant-scoped table with no tenant_id constraint.
  - _Why false positive:_ analyzeConversation first verifies the conversation row by id and tenantId, then reads messages for that verified conversationId in the same call path.
- **server/skillify.ts:221** _(fk-transitive)_ — Selecting persona rows without tenant scoping: personaRows = await db.select(...).from(personas).where(eq(personas.id, conv[0].personaId)) — no tenant_id constraint. Personas are tenant-scoped and this can read another tenant's persona metadata.
  - _Why false positive:_ schema note says personas has no tenant_id; personaId is read from a tenant-validated conversation row, so lookup is transitively scoped.
- **server/stability-watchdog.ts:177** _(global-table)_ — Query aggregates heartbeat_logs across tenants with no tenant constraint: SELECT task_id, task_name, COUNT(*) ... FROM heartbeat_logs WHERE status = 'error' AND created_at > ... GROUP BY ... — no tenant_id filter. This reads tenant-scoped audit/log rows globally.
  - _Why false positive:_ heartbeat_logs schema has no tenant_id and watchdog is doing platform-level stability aggregation over global logs.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE from heartbeat_tasks without tenant scoping: DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' AND created_at < ... RETURNING id — no tenant_id constraint. This can delete tenant-scoped rows across all tenants.
  - _Why false positive:_ this is a housekeeping purge of old disabled delegation tasks across tenants; it only deletes stale rows and returns aggregate counts.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE from event_log without tenant scoping: DELETE FROM event_log WHERE (status = 'processed' OR status = 'failed') AND created_at < ... RETURNING id — no tenant_id constraint. This deletes tenant-scoped log rows across all tenants.
  - _Why false positive:_ this is a platform-wide retention cleanup of processed/failed event logs; it mutates housekeeping state only and exposes only aggregate counts.
- **server/storage.ts:276** _(upstream-verified)_ — Raw SELECT from tenant-scoped table `messages` in attachCitationMetadata lacks any tenant_id constraint (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL).
  - _Why false positive:_ attachCitationMetadata only receives msgs already fetched through tenant-scoped callers here, and the id list is derived from those validated rows in the same flow.
- **server/storage.ts:683** _(upstream-verified)_ — evictOldestConversationFacts updates `conversation_facts` using only an id list in the WHERE (where(inArray(conversationFacts.id, victims.map(...)))), but the victims were selected under a tenant filter earlier. The UPDATE is performed without re-applying the tenant constraint, allowing possible cross-tenant updates if IDs overlap.
  - _Why false positive:_ victim ids are selected in the same function under conversationId+tenantId filtering before the update; ids come from that verified tenant-scoped result set.
- **server/storage.ts:929** _(system-sweep)_ — archiveExpiredMemories runs UPDATE memory_entries SET status = 'archived' WHERE ... without any tenant_id predicate — this mutates all tenants' data in one statement.
  - _Why false positive:_ global housekeeping update archives expired rows across tenants and only returns ids/count, not tenant data
- **server/storage.ts:946** _(system-sweep)_ — archiveStaleMemories runs an UPDATE on `memory_entries` using timestamp predicates but no tenant_id filter, affecting all tenants' rows.
  - _Why false positive:_ platform-wide stale-memory archival is housekeeping-only across tenants and returns only ids/count
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (existing = await db.select().from(customTools).where(eq(customTools.name, parsed.name))). This can leak existence of or collide with another tenant's custom tool names.
  - _Why false positive:_ name is transformed with tenantToolName(parsed.name, tenantId) before lookup, making existence check tenant-derived
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id without tenant constraint (finalTool = await db.select().from(customTools).where(eq(customTools.id, inserted.id))). This reads tenant-scoped JS/implementation without verifying tenant ownership.
  - _Why false positive:_ read-back is by freshly inserted server-generated primary key from same flow, not untrusted tenant-crossing input
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `custom_tools` without tenant_id guard (await db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id))). A row with the same id but different tenant could be modified.
  - _Why false positive:_ auto-fix updates custom_tools by trusted row id from same server-side creation/validation flow
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE of tenant-scoped table `custom_tools` to bump usageCount uses WHERE by id only (await db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(eq(customTools.id, tool.id))). Missing tenant constraint allows writes across tenants.
  - _Why false positive:_ tool row was fetched immediately above with tenant_id constraint, so id used for usageCount bump is already tenant-verified
- **server/tools.ts:6932** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id (INSERT INTO project_files (...) VALUES (...)). Tenant_id is required by the platform invariant and columns have no default.
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped through tenant-checked parent project_id
- **server/tools.ts:8937** _(upstream-verified)_ — UPDATE against proposed_skills in the managed-skill-safety error-path uses WHERE eq(proposed_skills.id, row.id) without a tenant_id constraint.
  - _Why false positive:_ row.id is from an already-loaded proposed_skills row in the same flow; this update targets that verified row, not request-supplied id
- **server/tools.ts:8952** _(upstream-verified)_ — UPDATE against proposed_skills (mark accepted/promoted) filters only by id (eq(proposed_skills.id, row.id)) and omits tenant_id, violating tenant isolation requirements.
  - _Why false positive:_ accept path updates the same previously-selected proposal row; ownership was established earlier in the call path via row context
- **server/tools.ts:8968** _(upstream-verified)_ — UPDATE against proposed_skills (mark rejected) uses WHERE eq(proposed_skills.id, row.id) without tenant_id filter, allowing cross-tenant mutation risk.
  - _Why false positive:_ reject path also updates the same in-scope proposal row already verified upstream, not a fresh untrusted identifier
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples uses WHERE id = ${row.id} with no tenant_id constraint.
  - _Why false positive:_ contradictionRows were selected immediately above with tenant_id = tenantId; update iterates those verified ids only
- **server/tools/domains/files/handlers.ts:937** _(fk-transitive)_ — INSERT into project_files omits tenant_id and writes request-derived values (file path/name) via a raw SQL template. If project_files is tenant-scoped this violates the requirement to set tenant_id on INSERT; using interpolated raw SQL with request-derived values increases injection/isolation risk.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped through project_id; projId comes from tenant-verified project join
- **server/tools/domains/files/handlers.ts:927** _(upstream-verified)_ — Use of db.execute(sql`...`) with interpolated values derived from request/context (e.g. ${tenantId}, ${ctx.conversationId}, ${absPath}, ${fileName}) — raw/template SQL is being composed with request-derived inputs.
  - _Why false positive:_ SELECT joins conversations to projects with p.tenant_id = tenantId, so conversation/project lookup is tenant-scoped; project_files insert is fk-transitive
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE on tenant-scoped table (memory_entries) uses WHERE id = ${id} without constraining to the caller's tenant (tenant_id missing). This allows a row id from another tenant to be updated (TOCTOU/race possible since id was discovered earlier).
  - _Why false positive:_ id was selected just above from memory_entries with tenant_id and persona_id filters in the same call path
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE on tenant-scoped table (memory_entries) uses WHERE id = ANY(${ids}) without constraining to the caller's tenant (tenant_id missing). This will update rows across tenants if ids include rows from other tenants.
  - _Why false positive:_ ids array is derived only from rows already selected with tenant_id = tid and persona_id = personaId in the same function
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE on tenant-scoped table (memory_entries) uses WHERE id = ${id} without constraining to the caller's tenant (tenant_id missing). The SELECT that discovered the id filtered by tenant, but the UPDATE omits tenant_id, enabling cross-tenant modification if an attacker supplies or guesses an id.
  - _Why false positive:_ update uses id obtained from immediately preceding tenant/persona-scoped SELECT on memory_entries
- **server/video-job-runner.ts:275** _(system-sweep)_ — SELECT from tenant-scoped table video_jobs performed with no tenant_id constraint (listStaleVideoJobs). This returns rows across tenants without per-tenant filtering.
  - _Why false positive:_ this is a platform-wide stale-job recovery sweep; rows are later claimed and processed with each row's own tenantId, not mixed into one tenant response
- **server/embeddings.ts:806** _(system-sweep)_ — storeEmbeddingVec contains a fallback branch that runs UPDATE ${sql.raw(table)} ... WHERE id = ${id} when options.tenantId is undefined. That performs an unconditional write to a tenant-scoped table (memory_entries or agent_knowledge) without tenant filtering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/google-workspace.ts:2307** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id filter. This UPDATE can modify a presenter's session data regardless of tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/job-queue.ts:272** _(internal-worker-pk)_ — failJob reads and updates agent_jobs by id without scoping to tenant_id. It SELECTs max_attempts FROM agent_jobs WHERE id = ${id} and then issues UPDATEs (to failed_terminal or pending) using only id/status/attempts guards — no tenant_id. This allows cross-tenant status transitions and error writes if id is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): failJob (server/job-queue.ts:211-268) reads max_attempts by the worker-owned job.id; sole callers are job-worker.ts:62 and :82 which pass job.id from claimDueJobs (server-generated PK).
- **server/plan-executor.ts:557** _(upstream-verified)_ — CAS claim UPDATE on plans omits a tenant_id constraint (UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved'). This allows an attacker who can supply a planId to flip status across tenants (cross-tenant write) because the claim is not scoped to the plan's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): CAS claim UPDATE runs inside executePlan whose planId ownership is validated upstream by getPlan(id, tenantId) at server/routes/agentic-policy.ts:113 (route gated requirePlatformAdmin at :95).
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selects projects/conversations across the entire DB without tenant scoping: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... WHERE (SELECT COUNT(*) FROM messages ... ). This will enumerate projects from all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:359** _(system-sweep)_ — Query that reads messages for a conversation lacks tenant scoping: SELECT role, content FROM messages WHERE conversation_id = ${conv.id}. Messages are tenant-scoped and contain sensitive content; reading them without tenant filters leaks cross-tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/storage.ts:1351** _(upstream-verified)_ — getAllDataForExport performs numerous SELECTs across many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, deliveryLogs, fileStorage, etc.) with no tenant_id constraints — this exposes entire platform data in one call.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — UPDATE felix_proposals sets args_embedding without a tenant_id constraint (UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL). `felix_proposals` is tenant-scoped; this update can modify another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger missing tenant_id constraint. releaseVentureReservation updates a ledger row by id only (WHERE id = ${reservationId}) with no tenant check.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agentic-features.ts:861** _(upstream-verified)_ — SELECT id FROM outreach_enrollments omits tenant_id in WHERE clause — checking for existing enrollment can match rows from other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/chat-engine.ts:3875** _(upstream-verified)_ — Calling recordErrorForRetryStormDetection(conversationId, ...) with only conversationId and no tenant information. This uses a foreign conversation identifier from the request and may cause the module to read/write tenant-scoped diagnostics/state without verifying tenant ownership.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): recordErrorForRetryStormDetection is an in-memory diagnostics counter keyed by conversationId (stuck-diagnostics); it performs no DB read/write and returns no tenant data.
- **server/chat-engine.ts:4096** _(internal-worker-pk)_ — Calling recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) without tenantId. A handler-supplied conversationId is forwarded to a diagnostics module which may read or write rows tied to that conversation without validating tenant ownership.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): recordToolCallForStuckDetection is in-memory loop diagnostics keyed by conversationId; no DB write occurs.
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE from tenant-scoped table doc_chunks is missing a tenant_id constraint. The statement `DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}` can affect rows across tenants if collection_id is mis-bound or row ownership is ambiguous.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/heartbeat.ts:2672** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint. This can change approval_status for another tenant's task with the same id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/job-queue.ts:334** _(internal-worker-pk)_ — cancelJob updates agent_jobs (SET status='cancelled' ...) WHERE id = ${id} AND status IN (...) without a tenant_id predicate, allowing cancelling jobs across tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:125** _(internal-worker-pk)_ — UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} — no tenant_id constraint. This may update span rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — verifyApiKey performs a SELECT FROM mcp_api_keys WHERE key_prefix = ${prefix} with NO tenant_id constraint. This reads a tenant-scoped table without constraining by tenant, which violates the project invariant that every read against tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/mcp-api-keys.ts:166 — verifyApiKey compares the sha256 of the FULL presented key against key_hash in the same function (mcp-api-keys.ts:163-180); the credential itself is the access control and selects the tenant.
- **server/memory-graph.ts:137** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — missing tenant_id constraint. This mutates a tenant-scoped table by id only, violating the tenant-isolation invariant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): catId is produced by ensureCategory (server/memory-graph.ts:79) which SELECTs/INSERTs scoped to tenantId (:51 'AND tenant_id = ${tenantId}', :57-58 INSERT with tenant_id); the id is server-derived within the same tenant call path, not request-supplied.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT projects query does not constrain by tenant_id (SELECT name, status, description FROM projects WHERE id = ${projectId}). This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/process-governor.ts:1234** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} is missing a tenant_id constraint. This mutates another tenant's governance_rules row if IDs collide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/routes.ts:1919** _(signed-url)_ — SELECT FROM tenant-scoped table `presenter_sessions` by token with no tenant_id constraint (GET /api/presenter/:token). A raw token lookup returns a presenter session row for any tenant if token matches, potentially exposing tenant-specific metadata.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2057 — presenter token is crypto.randomBytes(16).hex (routes.ts:2140) — an unguessable capability URL for public slide viewing; the token IS the access control by design.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE sculptor_sessions setting review_result does not include tenant constraint — writes by session id only (WHERE id = ${sessionId}).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/second-opinion.ts:416** _(internal-worker-pk)_ — settleFusionReservation UPDATE agent_cost_ledger uses WHERE id = ${reservationId} only — no tenant_id check on a tenant-scoped ledger table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:438** _(internal-worker-pk)_ — releaseFusionReservation UPDATE agent_cost_ledger sets cost_usd/operation WHERE id = ${reservationId} with no tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/seed.ts:2171** _(system-sweep)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... executes against the tenant-scoped projects table without constraining tenant_id. This writes to a project row regardless of its tenant, risking cross-tenant data modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:3131-3138 inside seedDatabase(); guarded by isOwnerDeployment() (3133) and uses fully hardcoded literals (id=15, primary_conversation_id=272) — a one-time owner-deployment migration, not reachable from any request input.
- **server/self-improvement.ts:427** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `experiments` is missing a tenant_id constraint — the .where(eq(experiments.id, experiment.id)) does not verify the caller's tenant, allowing an experiment row belonging to another tenant to be updated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-reflection.ts:374** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = <id> without also constraining tenant_id. This allows updating another tenant's memory entry by id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): UPDATE id comes from the tenant-scoped SELECT (WHERE tenant_id = ${tenantId}) directly above.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from messages without tenant scoping: recentMsgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)) (no tenant_id constraint). This reads tenant-scoped rows by conversationId only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): distillIntent calls analyzeConversation(conversationId, tenantId) at skillify.ts:89 first, which asserts conversation ownership via WHERE id AND tenantId and throws if not found (skillify.ts:196-199). The messages read at 91-94 is by conversationId whose tenant ownership is already proven; messages FK-cascade to conversations.
- **server/storage.ts:891** _(internal-worker-pk)_ — updateMemoryEmbedding updates `memory_entries` and sets embedding WHERE id = {id} with no tenant_id filter. This writes a tenant-scoped table without validating the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:914** _(system-sweep)_ — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` without any tenant filter (returns rows across all tenants).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getMemoriesWithoutEmbeddings only caller is the background embedding backfill heartbeat.ts:1912; rows feed embedding generation (updateMemoryEmbedding) and are never returned to a tenant-facing surface.
- **server/storage.ts:919** _(system-sweep)_ — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` without a tenant filter, returning rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getKnowledgeWithoutEmbeddings only caller is heartbeat.ts:1913 background embedding worker; rows only drive updateKnowledgeEmbedding, not returned to any tenant surface.
- **server/storage.ts:1062** _(upstream-verified)_ — deleteHeartbeatTask similarly may DELETE heartbeat_tasks by id without tenant scoping when tenantId is omitted, allowing unscoped deletions.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): deleteHeartbeatTask only caller is routes/heartbeat.ts:81 which always passes tenantId → tenant subselect scope applied (storage.ts:966); no unscoped caller exists.
- **server/tools.ts:8907** _(internal-worker-pk)_ — UPDATE against proposed_skills uses WHERE eq(proposed_skills.id, row.id) but does NOT constrain by tenant (no WHERE tenant_id = <caller tenant>). This allows one tenant to affect another tenant's proposed_skills row if ids collide or are guessed.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is executed without constraining by tenant_id (reads all matching rows across tenants using a request-derived phone/title).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified

### Deferred — accepted real risks (3)

_These are genuine findings under dated deferral. They still block republish readiness._

- **server/storage.ts:619** — deleteMemoryEntry issues an UPDATE on tenant-scoped table `memory_entries` using only id when tenantId is omitted (tenantId optional). This allows an unscoped update if tenantId is not supplied.
  - _Deferral:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): deleteMemoryEntry scopes tenant only when tenantId provided (warns via _warnUnscoped otherwise) — optional-tenant fail-open; hardening = make tenantId required.
- **server/storage.ts:626** — touchMemoryEntries performs an UPDATE on `memory_entries` with an inArray(id) condition but only optionally scopes by tenantId. If tenantId is undefined the update is unscoped across tenants.
  - _Deferral:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): touchMemoryEntries scopes tenant only when tenantId provided (warns via _warnUnscoped otherwise) — optional-tenant fail-open; hardening = make tenantId required.
- **server/storage.ts:1057** — updateHeartbeatTask builds conditions and only applies a tenant-scoping subquery when tenantId !== undefined; if tenantId is omitted it updates heartbeat_tasks by id only, allowing unscoped updates to heartbeat_tasks which are parent to tenant-scoped logs.
  - _Deferral:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): updateHeartbeatTask falls back to an unscoped UPDATE when tenantId omitted — same optional-tenant fail-open family as getMemoryStats/getHeartbeatTasks (new deferral, campaign 2026-08-01 pass 2).
