# Tenant-Isolation Audit Report

_Generated 2026-06-18T17:18:17.440Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **400/400** file(s) audited · chunks 48/48 OK (0 failed)
- Findings: **299** (CRITICAL 78 · HIGH 183 · MEDIUM 35 · LOW 3)

### CRITICAL (78)

- **server/agent-activity.ts:122** — UPDATE against tenant-scoped table `agent_activity` uses only activity id in WHERE (WHERE id = activityId) and lacks a tenant_id constraint. This allows modifying another tenant's activity row by id.
  - _Fix:_ Constrain the UPDATE by tenant: include the tenantId in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) and require tenantId as a parameter to the function.
- **server/agentic-engines.ts:147** — UPDATE ai_insights SET action_taken ... WHERE id = ${insight.insightId} lacks a tenant_id constraint. This can overwrite another tenant's insight action_taken field.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId}) so only the owning tenant's row is modified.
- **server/agentic-engines.ts:242** — Bulk UPDATE on tenant-scoped table `ai_insights` (SET status='applied' WHERE status='new' AND category IN (...)) does not filter by tenant_id, and will update rows across all tenants.
  - _Fix:_ Perform auto-apply per-tenant (add WHERE tenant_id = ${tenantId}) or iterate tenants and apply the update scoped to each tenant to avoid cross-tenant modifications.
- **server/agentic-engines.ts:262** — UPDATE ai_insights SET action_taken = ${PENDING_PLAN_MARKER} WHERE id = ${Number(r.id)} does not include tenant_id in the WHERE clause, allowing modification of rows belonging to other tenants if id collisions occur.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE id = ${Number(r.id)} AND tenant_id = ${r.tenant_id}) or pass the tenant into the update call so only the owning tenant's row is modified.
- **server/agentic-features.ts:671** — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} does not include a tenant_id constraint and will update the enrollment row by id across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to ensure only the owning tenant's enrollment is modified.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments SET reply_classification = ..., status = ... WHERE id = ${row.id} is missing tenant_id in the WHERE clause, allowing cross-tenant updates by id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or include tenant in the update filter to prevent modifying another tenant's enrollment.
- **server/agentic/approvals.ts:96** — Bulk UPDATE on tenant-scoped table `agent_approvals` to mark stale approvals expired uses WHERE (status = 'pending' AND expiresAt < now()) but has no tenant_id filter, so it will expire approvals across all tenants in one operation.
  - _Fix:_ Scope expiry runs per-tenant (add WHERE tenant_id = ${tenantId}) or iterate tenants explicitly. Ensure any follow-up run update uses the tenantId from the expired approval row when marking linked runs to avoid cross-tenant effects.
- **server/business-tools.ts:84** — INSERT into invoice_items does not include tenant_id. Tenant-scoped tables must have tenant_id set on INSERTs — currently invoice_items rows are created without an explicit tenant_id value.
  - _Fix:_ Include the tenant id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)) or otherwise derive+validate the tenant before inserting. Ensure invoice_items is populated with the caller's tenant_id and/or validate the invoice belongs to the tenant before inserting items.
- **server/chat-engine.ts:1781** — buildMemorySection declares tenantId with a default of 1 (tenantId: number = 1). If a caller omits the tenantId the function will run under tenant 1 (admin) and may surface or act on memory data for the wrong tenant, enabling cross-tenant data exposure.
  - _Fix:_ Fail-closed: remove the insecure default. Require callers to pass an explicit tenantId (e.g. tenantId: number) and validate it at runtime (assertTenantContext or throw if null/invalid). Audit all callers to ensure they pass the resolved tenantId. If a default is absolutely needed, change it to null and throw immediately when tenantId == null so accidental omission cannot fall back to admin.
- **server/chat-engine.ts:4344** — storage.createMemoryEntry(...) is called without passing tenantId — inserts into a tenant-scoped memory table with no explicit tenant_id set.
  - _Fix:_ Include the tenantId when creating memory entries (e.g. storage.createMemoryEntry({ ..., tenantId }) ), or ensure the storage API enforces or fills tenant_id server-side. Additionally validate personaId/other ownership context before persisting to prevent cross-tenant memory leaks.
- **server/chat-engine.ts:4085** — storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is invoked without an explicit tenantId — potential INSERT into a tenant-scoped citations table missing tenant_id.
  - _Fix:_ Pass tenantId into attachCitations (e.g. storage.attachCitations(messageId, citations, tenantId)) or have the storage API derive and enforce tenant_id from the message row server-side. At minimum, validate that the messageId belongs to the caller's tenant before attaching citations.
- **server/data-protection.ts:127** — DELETE from tenant-scoped table `conversations` without tenant_id predicate — permanentlyPurgeSoftDeleted deletes conversation rows by id but lacks `AND tenant_id = ...`. This can remove another tenant's conversation record.
  - _Fix:_ Never DELETE a tenant-scoped primary record without including tenant scoping. Change to `DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id} RETURNING id` (or run in an admin-authorized multi-tenant purge routine that explicitly accepts/validates tenant context). Prefer withTenantTx or explicit tenant_id parameter for safety.
- **server/delivery-pipeline.ts:184** — updateDeliveryLog performs UPDATE on delivery_logs by id with no tenant_id constraint. This allows one tenant (or an unauthenticated caller that can reach this code path) to modify another tenant's delivery rows.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. add a tenantId parameter and use .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))) or at least verify ownership before calling the generic updater). Prefer making the function signature require tenantId so callers cannot skip scoping.
- **server/delivery-pipeline.ts:709** — UPDATE that clears stripePaymentId uses WHERE id = prior.id with no tenant_id constraint. This mutates a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = <owner tenant>) or perform the update inside a transaction after verifying the prior row belongs to the expected tenant.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} has no tenant_id predicate. This DELETE can remove chunks belonging to other tenants sharing the same collection_id/doc_path.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = ${tenantId}). Ensure collectionId is always validated as belonging to the tenant before deleting, or derive collection_id from a tenant-scoped select.
- **server/email.ts:406** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} updates a tenant-scoped table row by id with no tenant_id constraint. Without tenant scoping this can mutate another tenant's row.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. AND tenant_id = ${tenantId}) or fetch+verify the tenant before performing the UPDATE. Prefer making backfill iterate per-tenant and include tenant_id in all DML.
- **server/embeddings.ts:680** — storeEmbeddingVec executes UPDATE ${sql.raw(table)} ... WHERE id = ${id} without a tenant_id predicate. This updates a tenant-scoped row by id only and can overwrite another tenant's embedding vector.
  - _Fix:_ Require and pass tenantId into storeEmbeddingVec and include AND tenant_id = ${tenantId} in the UPDATE; or use a tenant-scoped API/storage layer that verifies ownership before updating rows.
- **server/felix-brain.ts:19** — Unbounded SELECT from tenant-scoped table 'agent_runs' with no tenant filter. rehydrateTaskStateOnBoot() reads rows for ALL tenants (SELECT ... FROM agent_runs WHERE status IN (...) AND created_at > ...), then materializes them into a global in-memory store — allowing cross-tenant data exposure and mixing of task state.
  - _Fix:_ Restrict the query to a single tenant (or iterate per-tenant). Add a tenantId parameter and include a WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq(agent_runs.tenantId, tenantId)). Ensure only rows belonging to the intended tenant are fetched and rehydrated.
- **server/google-drive.ts:560** — UPDATE projects SET driveFolderId/driveFolderUrl uses WHERE eq(projects.id, projectId) but does not include a tenant_id constraint. projects is tenant-scoped (has tenantId), so this UPDATE could modify another tenant's project if projectId is valid.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or equivalent SQL WHERE tenant_id = ${tenantId}). Even if you validated ownership earlier in the function, the SQL itself should enforce tenant scoping to prevent accidental cross-tenant writes.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` (SET status = 'archived_stale') without any tenant_id constraint — may modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform the update through a storage/service function that asserts the task caller's tenant. Never perform cross-tenant UPDATEs without verifying tenant ownership.
- **server/health-audit.ts:368** — UPDATE on tenant-scoped table `heartbeat_tasks` (disabling stuck tasks) without tenant_id constraint — can disable other tenants' tasks.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or invoke a storage/api that enforces tenant ownership before modifying rows.
- **server/heartbeat.ts:101** — UPDATE on tenant-scoped table `research_schedules` (SET next_run_at) using id only and no tenant_id constraint — can advance/reset schedules for other tenants.
  - _Fix:_ Include tenant_id in WHERE clause (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via the trusted storage API that asserts tenant ownership before writing.
- **server/heartbeat.ts:106** — UPDATE on tenant-scoped table `research_schedules` (SET last_run_at/next_run_at) without tenant_id constraint — may affect schedules belonging to other tenants.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a tenant-aware storage function that enforces the caller's tenant context.
- **server/heartbeat.ts:120** — UPDATE on tenant-scoped table `research_schedules` (recovery path) without tenant_id constraint.
  - _Fix:_ Constrain UPDATE by tenant (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call into a storage layer that validates tenant ownership before mutating the row.
- **server/heartbeat.ts:248** — UPDATE on tenant-scoped table `heartbeat_tasks` (normalization fix: SET model = ...) using id only with no tenant_id constraint — can modify another tenant's task row.
  - _Fix:_ Include tenant_id in the UPDATE (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the change via storage.updateHeartbeatTask which enforces tenant scoping. Always validate tenant ownership on writes.
- **server/heartbeat.ts:252** — UPDATE on tenant-scoped table `heartbeat_tasks` (model normalization) without tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use a tenant-aware storage API to prevent cross-tenant writes.
- **server/heartbeat.ts:542** — UPDATE on tenant-scoped table `event_log` (SET status = 'failed' ...) using id only and no tenant_id constraint — can update another tenant's event row.
  - _Fix:_ Include tenant filter in the UPDATE (WHERE id = ${event.id} AND tenant_id = ${event.tenant_id}) or perform the update via a storage method that verifies tenant ownership. Never update tenant-scoped rows using id alone without confirming tenant_id.
- **server/heartbeat.ts:2772** — UPDATE on tenant-scoped table `heartbeat_tasks` (SET approval_status = 'pending') using id only and no tenant_id constraint in processDelegations — may change approval status for tasks of other tenants.
  - _Fix:_ Constrain UPDATE by tenant (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or set approval_status via storage.updateHeartbeatTask which enforces tenant context. Always validate tenant ownership for writes coming from task output processing.
- **server/heartbeat.ts:3130** — UPDATE on tenant-scoped table `heartbeat_tasks` (SET approval_status = 'pending') using id only and no tenant_id constraint in delegateTaskFromChat — can affect tasks across tenants.
  - _Fix:_ Include tenant filtering in the UPDATE (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage API to set approval_status. Ensure all DB writes include the caller/target tenant to prevent cross-tenant modification.
- **server/internal-resolver.ts:93** — UPDATE event_log executed without constraining to tenant_id — finalizes arbitrary event rows by id only.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId} to the WHERE) or fetch and validate the event's tenant_id before mutating. Ensure the resolver has caller tenant context and never updates event_log rows across tenants by id alone.
- **server/internal-resolver.ts:107** — SELECT FROM plans by id without any tenant check — reads a plan referenced by an external eventId/planId without verifying the caller owns the plan.
  - _Fix:_ Require or derive the tenantId for the event and constrain the SELECT (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}), or first query event_log to get tenant_id and validate ownership before reading/modifying plans. Do not act on planId-only lookups from untrusted event data.
- **server/lib/inbox-ingest.ts:491** — UPDATE on tenant-scoped table `inbox_classifications` uses only `WHERE inbox_message_id = ${storeResult.id}` with no tenant_id check — a write can affect another tenant's row if ids are guessed or race conditions occur.
  - _Fix:_ Constrain the update by tenant (e.g. `WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}`) or join via the owning inbox_messages row and check its tenant_id. Always include tenant_id on UPDATEs against tenant-scoped tables to enforce isolation.
- **server/memory-graph.ts:195** — INSERT INTO memory_links omits tenant_id entirely (INSERT INTO memory_links (... ) VALUES (...)). The invariant requires every INSERT into a tenant-scoped table to set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT column list and bind it from the caller (e.g. INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId})). Validate the source/target memory rows belong to the tenant before inserting.
- **server/oauth-subscriptions.ts:935** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update on tenant-scoped table uses only id and lacks tenant_id constraint. This can modify/deactivate rows belonging to any tenant (cross-tenant write).
  - _Fix:_ Always include tenant_id in the WHERE clause for updates on tenant-scoped tables (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} or pass the tenantId variable explicitly). If this is part of a global job, perform per-tenant updates while validating caller is an admin.
- **server/oauth-subscriptions.ts:959** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — same issue repeated: write to a tenant-scoped table without tenant_id in WHERE, risking cross-tenant mutation.
  - _Fix:_ Require AND tenant_id = ${sub.tenant_id} (or equivalent) on the WHERE clause. Ensure the code computes/uses the tenant id for the targeted row rather than relying solely on id.
- **server/plan-executor.ts:327** — CAS claim UPDATE on plans omits any tenant_id constraint (UPDATE plans ... WHERE id = ${planId} AND status = 'approved'). This lets a caller claim/transition a plan belonging to another tenant if they know the plan id, breaking tenant isolation and allowing cross-tenant execution/billing.
  - _Fix:_ Include the owning tenant in the CAS predicate (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved'), or accept a tenantId parameter to executePlan and use it in the WHERE clause. Ensure the claim is atomic and parameterized so a caller cannot flip a plan from another tenant.
- **server/plan-executor.ts:338** — Setting plan status to 'failed' uses WHERE id = ${planId} without validating tenant_id. This can change the state of plans belonging to other tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (use the plan's tenant or pass tenantId into the function) so state transitions are constrained to the plan's owning tenant.
- **server/plan-executor.ts:435** — Final status UPDATE for plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint and can modify plans across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}) to ensure only the owning tenant's plan is updated. Prefer using the tenant_id fetched with the row rather than trusting an external id.
- **server/plan-executor.ts:458** — Error-path update flips a plan to 'failed' WHERE id = ${planId} AND status = 'executing' with no tenant_id check, allowing cross-tenant state changes on failure paths.
  - _Fix:_ Restrict the UPDATE to the plan's tenant (AND tenant_id = ${tenantId}). Use the tenant_id returned when claiming the plan or require caller-supplied tenantId validated against the row to avoid cross-tenant effects.
- **server/plan-executor.ts:476** — resumeStuckPlans performs a global UPDATE resetting any 'executing' plan older than the threshold back to 'approved' with no tenant_id restriction. This affects all tenants in a single sweep and can leak cross-tenant recovery behavior.
  - _Fix:_ Run recovery per-tenant or add tenant_id filtering. For multi-tenant deployments, iterate tenants and run the rollback per-tenant, or add scoping (e.g. WHERE status='executing' AND tenant_id = ${tenantId} AND updated_at < ...) so only that tenant's plans are touched.
- **server/plan-executor.ts:483** — resumeStuckPlans selects pending approved plans (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50) with no tenant filter and then schedules execution for each id. This will cause the executor to pick up and run plans across tenants.
  - _Fix:_ Filter the SELECT by tenant_id (or iterate per-tenant) so only approved plans for the intended tenant are enqueued. Require executePlan to validate tenant ownership on claim as an additional safeguard.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks executed without constraining by tenant_id (UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id}). This allows disabling a task by global id across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId} (or use a parameterized Drizzle .where(eq(table.tenantId, tenantId))). Never rely on a prior SELECT's tenant filter alone for subsequent UPDATEs.
- **server/process-governor.ts:977** — UPDATE governance_rules executed without tenant constraint (UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id}). This mutates a tenant-scoped table by id only.
  - _Fix:_ Constrain the UPDATE to the caller tenant: UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} AND tenant_id = ${tenantId}. Validate/normalize tenantId before use.
- **server/process-governor.ts:641** — UPDATE agent_desks executed without tenant constraint (UPDATE agent_desks SET ... WHERE id = ${desk.id}). The SELECT that discovered the id was tenant-scoped, but the subsequent UPDATE lacks an explicit tenant_id guard.
  - _Fix:_ Include tenant filtering on the UPDATE, e.g. UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId}. Always include tenant_id for writes to tenant-scoped tables even if the id originated from a prior tenant-scoped SELECT.
- **server/project-brain.ts:332** — Backfill query scans projects/conversations/messages across the DB with no tenant restrictions (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0). This will iterate across all tenants and create files for projects from other tenants.
  - _Fix:_ Constrain the backfill to a single tenant (add AND p.tenant_id = ${tenantId} / c.tenant_id = ${tenantId} etc.), or require a tenantId parameter and apply it consistently to all queries (projects, conversations, messages) involved in the backfill process.
- **server/proposal-verifier.ts:246** — persist() updates code_proposals without tenant constraint (UPDATE code_proposals SET verification_status = ..., verified_at = NOW() WHERE id = ${proposalId}). This mutates tenant-scoped rows by id only.
  - _Fix:_ Constrain the UPDATE by tenant_id as well: UPDATE code_proposals SET ... WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Add tenantId to the persist() function signature or resolve tenant from caller context and validate ownership before applying updates.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift, the code updates code_proposals (SET status = 'rejected', verification_status = 'skipped', ...) using WHERE id = ${proposalId} only. No tenant constraint is present.
  - _Fix:_ Include tenant scoping in these UPDATEs (AND tenant_id = ${tenantId}) and ensure attemptRebaseOnDrift accepts/validates tenantId (or resolves it from authenticated context) before persisting rebased diffs or status changes.
- **server/proposal-verifier.ts:408** — In attemptRebaseOnDrift the rebased diff is persisted via UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} — missing tenant constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${proposalId} AND tenant_id = ${tenantId}), and require/validate tenantId in attemptRebaseOnDrift and any callers to prevent cross-tenant modification of proposals.
- **server/proposal-verifier.ts:396** — When LLM hallucination is detected the code updates code_proposals validation_result using WHERE id = ${proposalId} only (no tenant constraint).
  - _Fix:_ Constrain this UPDATE with tenant_id (AND tenant_id = ${tenantId}) and ensure attemptRebaseOnDrift has tenant validation so a proposal from one tenant cannot be modified by processes handling another tenant's proposal id.
- **server/recurring-messages.ts:294** — UPDATE agent_knowledge SET content = ... WHERE id = ${r.id} — missing tenant_id constraint. This can modify another tenant's row if ids collide or are attacker-controlled.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or otherwise verify ownership before updating. Never rely solely on an earlier select without an explicit parameterized tenant filter for subsequent writes.
- **server/reference-learner.ts:283** — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} — no tenant_id constraint on an update to a tenant-scoped table.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) or perform an ownership check before the update to ensure the storedId belongs to the caller's tenant.
- **server/research-engine.ts:156** — UPDATE research_sessions ... WHERE status = 'running' AND started_at < ${staleThreshold} — no tenant_id constraint. This updates sessions across all tenants.
  - _Fix:_ Restrict the cleanup to a specific tenant or run per-tenant. Add AND tenant_id = ${tenantId} (or otherwise ensure the function is invoked with and enforces a tenant context) so rows from other tenants are not modified.
- **server/research-engine.ts:309** — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${programId} AND tenant_id = ${tenantId}) so you only modify the program belonging to the expected tenant.
- **server/research-engine.ts:388** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() — no tenant_id constraint; deletes across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or run the delete per-tenant) so expired autoresearch entries are removed only for the intended tenant.
- **server/research-engine.ts:468** — INSERT INTO project_notes (project_id, note, author, created_at) ... — INSERT does not explicitly set tenant_id. Tenant-scoped tables must receive tenant_id on INSERT.
  - _Fix:_ Include tenant_id in the INSERT columns/values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, ...) VALUES (${session.tenantId}, ${projectId}, ...)). If project_notes is intentionally keyed only by project_id, ensure project_id is validated to belong to the tenant and prefer adding tenant_id to the schema and all inserts for defense-in-depth.
- **server/research-engine.ts:1468** — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL — missing tenant_id constraint on an update to a tenant-scoped table.
  - _Fix:_ Include AND tenant_id = ${tenantId} (the caller's tenant) in the WHERE clause so the update cannot touch another tenant's row. Use the same tenant context used when selecting the row.
- **server/research-engine.ts:1510** — UPDATE research_experiments SET replayed_at = NULL WHERE id = ${row.id} AND replayed_proposal_id IS NULL — missing tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId} AND replayed_proposal_id IS NULL) to avoid cross-tenant modifications.
- **server/research-engine.ts:545** — UPDATE research_sessions SET status = ${reason}, ... WHERE id = ${sessionId} — no tenant_id constraint on a tenant-scoped table update.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or equivalent) so only the session belonging to the tenant is updated. Avoid relying solely on in-memory ownership without a DB-level WHERE tenant_id guard.
- **server/research-engine.ts:810** — UPDATE research_experiments SET ... WHERE id = ${experimentId} — update on tenant-scoped table with no tenant_id constraint.
  - _Fix:_ Include tenant_id = ${session.tenantId} in the WHERE clause (or other ownership validation) before updating experiment rows to prevent modifying another tenant's data.
- **server/research-engine.ts:1583** — UPDATE code_proposals SET status = 'needs_review', validation_result = ... WHERE id = ${proposalId} — missing tenant_id constraint when updating a tenant-scoped table.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE so only proposals owned by the tenant can be modified. Even if the proposal was previously read with a tenant filter, writes should also include tenant_id in the WHERE to prevent TOCTOU or id-reuse issues.
- **server/routes.ts:3889** — Unscoped SELECT from tenant-scoped file_storage used to restore missing upload files into the uploads directory. The code selects file_storage rows for arbitrary filenames (inArray on fileStorage.filename) with NO tenant_id constraint and then writes those rows to the shared UPLOADS_DIR and later inlines them into conversation context — enabling cross-tenant data exfiltration and context injection.
  - _Fix:_ Restrict the lookup to the conversation's tenant (e.g. add .where(and(inArray(fileStorage.filename, [...missingFiles]), eq(fileStorage.tenantId, convTenantId)))) or otherwise verify ownership for each filename before restoring/writing to disk. Prefer using a storage API that enforces tenant ownership rather than selecting raw rows. Do not write files belonging to other tenants into the shared UPLOADS_DIR or expose them into a conversation's prompt/context.
- **server/routes.ts:4404** — SELECT against tenant-scoped table 'conversations' omits tenant constraint. Query: SELECT project_id FROM conversations WHERE id = ${conversationId} ...
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add AND tenant_id = ${tenantId}), or fetch the conversation via the storage layer which enforces tenant scoping. Validate that the conversation belongs to the caller before using its project_id.
- **server/seed.ts:2859** — Unscoped UPDATE on tenant-scoped table `research_programs`: `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}` updates rows by id without constraining tenant_id — this can modify another tenant's data if ids collide or are guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${p.id} AND tenant_id = ${tenantId}`) or otherwise validate ownership before updating. Prefer Drizzle's `.where(and(eq(research_programs.id, id), eq(research_programs.tenantId, tenantId)))` pattern.
- **server/seed.ts:3136** — Unscoped UPDATE on tenant-scoped table `projects`: `UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL` lacks a tenant_id constraint and may modify a project belonging to another tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. add `AND tenant_id = ${tenantId}`) or otherwise verify the project's tenant ownership before performing the update. Use parameterized Drizzle `.where(eq(projects.tenantId, tenantId))`.
- **server/seed.ts:3212** — INSERT into `project_conversations` omits tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id})` — if `project_conversations` is tenant-scoped, this writes rows without tenant affiliation, risking cross-tenant data mixing.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `VALUES (${tenantId}, ${r.project_id}, ${r.id})`) and ensure the tenant value is trusted/validated. If using Drizzle, use `.values({ tenantId, projectId, conversationId })` and/or verify that both project and conversation belong to the same tenant before inserting.
- **server/seed.ts:4402** — UPDATE memory_entries ... WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ... runs without any tenant_id constraint. This can modify memory rows across all tenants (mass, cross-tenant data corruption risk).
  - _Fix:_ Add AND tenant_id = ${tenantId} (or run the update once per-tenant) to ensure the normalizer only updates rows for the intended tenant(s). Avoid global updates on tenant-scoped tables.
- **server/self-reflection.ts:366** — storeLesson updates memory_entries by id without constraining the UPDATE to the calling tenant (UPDATE ... WHERE id = <id>). This allows modifying another tenant's memory entry if an id is known.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Always include tenant_id in WHERE clauses for updates/deletes on tenant-scoped tables as a defense-in-depth measure.
- **server/storage.ts:280** — attachCitationMetadata issues a SELECT on messages by id without constraining tenant_id. Tenant-scoped messages table is read without a tenant filter, enabling cross-tenant data exposure.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or accept the tenantId as an explicit parameter and validate it before querying. If ids are only trusted when derived from a tenant-scoped prior query, ensure the caller passes/validates tenantId or re-check tenant ownership in this function before selecting citations.
- **server/storage.ts:385** — attachCitations updates messages (UPDATE ... WHERE id = ${messageId}) with no tenant_id constraint. This can modify rows across tenants if messageId is abused.
  - _Fix:_ Require and verify the tenantId (or fetch the message and confirm its tenant) before issuing the UPDATE, or include tenant scoping in the UPDATE WHERE clause: WHERE id = ${messageId} AND tenant_id = ${tenantId}.
- **server/storage.ts:499** — getAllMemoriesForBackup selects all memory_entries without any tenant filter. This returns tenant-scoped rows for all tenants in one call (mass data exfiltration risk).
  - _Fix:_ Restrict this function to admin-only contexts and require an explicit tenantId (or an explicit admin authorization check). Prefer separating per-tenant export endpoints that require tenantId, or require an admin flag and log access strongly. If this is intended for admin export, make the function name and ACL explicit and enforce it.
- **server/storage.ts:1206** — getAllDataForExport performs many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with no tenant scoping. This exports entire platform data in one call.
  - _Fix:_ Restrict this API to authenticated admin contexts and require explicit authorization. Prefer per-tenant export functions that accept a tenantId and filter every tenant-scoped table by tenant_id. At minimum, add an explicit admin-only check and strong audit logging; avoid silent global exports.
- **server/tool-learning.ts:283** — UPDATE on tenant-scoped table `custom_tools` lacks tenant ownership filter: `.where(eq(customTools.id, tool.id))`. Without `tenant_id` this can modify another tenant's tool row.
  - _Fix:_ Include tenant check in the WHERE clause for all updates. For example: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Ensure `tenantId` is the authenticated caller's tenant and propagated into tryAndRevert (or otherwise validated) before performing the update.
- **server/tool-learning.ts:300** — DELETE on tenant-scoped table `custom_tools` without tenant filter: `.where(eq(customTools.id, tool.id))`. This can delete rows owned by other tenants.
  - _Fix:_ Restrict deletions to the caller's tenant by adding `tenant_id` to the WHERE: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Ensure the function has a trusted `tenantId` and use it for the check.
- **server/tool-learning.ts:350** — UPDATE on tenant-scoped table `custom_tools` to increment usageCount uses `.where(eq(customTools.id, tool.id))` with no tenant constraint. A caller could increment or modify another tenant's row.
  - _Fix:_ Add tenant scoping to the update: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Ensure `tenantId` is the caller's tenant and passed into executeCustomTool or fetched/validated before performing writes.
- **server/tools.ts:10773** — INSERT into project_notes omits tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL/no default).
  - _Fix:_ Include tenant_id in the INSERT and pass the verified tenant id. Example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ${note}, 'system', NOW()). Ensure ownership was validated (assertProjectInTenant) before inserting.
- **server/tools.ts:10777** — INSERT into project_files omits tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL/no default).
  - _Fix:_ Include tenant_id in the INSERT and pass the verified tenant id. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ${fileName}, 'presentation', ${editUrl}, ${editUrl}, 'Felix', NOW()). Ensure ownership was validated (assertProjectInTenant) before inserting.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped ledger row by id only with no tenant_id constraint. An attacker or bug could modify another tenant's ledger row if reservationId is misused.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include AND tenant_id = ${tenantId} (or eq table filter) in the UPDATE so the row is only modified if it belongs to the expected tenant. Use a parameterized tenant filter in Drizzle rather than relying solely on id.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseVentureReservation) clears a reservation without constraining by tenant_id. This permits cross-tenant modification of ledger rows by id alone.
  - _Fix:_ Accept tenantId as an argument and include a tenant_id filter in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) so only the owning tenant's reservation can be released.

### HIGH (183)

- **server/ab-optimizer.ts:110** — Global SELECT from tenant-scoped table `experiments` with no tenant_id constraint (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads rows across all tenants.
  - _Fix:_ Restrict the query by tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate tenants explicitly and process each tenant's experiments separately so no cross-tenant rows are returned.
- **server/agent-channels.ts:194** — Query joins `channel_subscriptions` (cs) but does not constrain cs.tenant_id. The unread-count SELECT joins cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} and only filters cm.tenant_id = ${tenantId}, allowing cs rows from other tenants to be considered.
  - _Fix:_ Add tenant scoping to the join or WHERE (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}) to ensure subscription rows are tenant-scoped.
- **server/agentic-engines.ts:114** — SELECT FROM `plans` to check for an existing plan is not tenant-scoped (WHERE source = ... AND source_ref = ...). This can find/reuse plans belonging to other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${insight.tenantId}) or perform plan lookup per-tenant to avoid cross-tenant reuse.
- **server/agentic-engines.ts:160** — Durability sweep SELECT from tenant-scoped table `ai_insights` filters by status/priority/action_taken but has no tenant_id constraint. It reads pending rows across all tenants in a single operation.
  - _Fix:_ Either run the sweep per-tenant (iterate tenants and include tenant_id = ...) or add tenant scoping to the query to ensure only a single tenant's rows are returned at a time.
- **server/agentic-engines.ts:342** — SELECT COUNT(...) FROM heartbeat_logs to compute toolCalls is executed without tenant filtering (WHERE task_name != 'Self-Reflection' only). This reads global heartbeat_logs across tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND task_name != 'Self-Reflection') or otherwise aggregate per-tenant to avoid leaking cross-tenant telemetry.
- **server/agentic-engines.ts:448** — SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' LIMIT 10 is not constrained by tenant_id and returns recent logs across tenants.
  - _Fix:_ Filter by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status = 'success') so only this tenant's heartbeat logs are returned.
- **server/agentic-engines.ts:516** — SELECT from heartbeat_logs to compute heartbeatPerformance groups by task_name/status but has no tenant_id filter. This will aggregate logs across all tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to the query so performance metrics are tenant-scoped, or run the aggregation per-tenant explicitly.
- **server/agentic-engines.ts:530** — SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true is missing tenant scoping and will return tasks from every tenant.
  - _Fix:_ Constrain queries on heartbeat_tasks by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND enabled = true) so only tenant-specific tasks are processed.
- **server/agentic-features.ts:274** — Subquery in prevSnaps SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY url, created_at DESC lacks tenant_id filter. The outer query filters s1.tenant_id but the inner subquery can pick ids from other tenants, causing incorrect exclusion logic.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the inner subquery (and all subqueries) so the DISTINCT/NOT IN logic is limited to the tenant's own snapshots.
- **server/agentic-features.ts:599** — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' does not include tenant_id, so it may match enrollments from other tenants when preventing duplicates.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${params.tenantId}) in the SELECT to ensure duplicate checks are limited to the tenant's data.
- **server/agentic/harness-adaptation.ts:71** — Query reads from agent_trace_spans without any tenant_id constraint (SELECT metadata->>'modelId' ... FROM agent_trace_spans ...). This performs an unscoped cross-tenant read from a tenant-scoped table.
  - _Fix:_ Scope the query to an explicit tenant (e.g. add WHERE tenant_id = ${tenantId} or WHERE tenant_id = ${ADMIN_TENANT_ID} if this must be an admin-only operation). If the intention is a global admin scan, require and check an explicit admin context/privilege and document it; avoid implicit cross-tenant reads. Parameterize any tenant selector through the call-site rather than leaving the table unfiltered.
- **server/agentic/department-budgets.ts:143** — Query enumerates department_budgets across all tenants (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active') with no tenant_id filter — unscoped cross-tenant read.
  - _Fix:_ If this is intended as an administrative sweep, require an explicit admin context and/or run under a clearly-marked admin tenant (filter to that tenant_id), or otherwise restrict the query to a specific tenant by adding WHERE tenant_id = ${tenantId}. Avoid leaving tenant-scoped tables unfiltered; enforce an explicit tenant parameter for cross-tenant operations and audit/authorize them.
- **server/agentic/resume-worker.ts:66** — Broad SELECT across agent_runs JOIN agent_approvals with no tenant_id filter — the query scans rows for all tenants.
  - _Fix:_ Restrict the initial candidate query to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId}) or otherwise iterate/claim by tenant. Never run cross-tenant SELECTs on tenant-scoped tables; require caller tenant context and include tenant_id in the WHERE clause.
- **server/agentic/runs.ts:34** — appendStep updates agent_runs by id only (WHERE eq(agentRuns.id, runId)) with no tenant_id constraint — allows cross-tenant modification of run rows.
  - _Fix:_ Require tenant context (tenantId) in appendStep and include AND eq(agentRuns.tenantId, tenantId) in the WHERE clause. All updates to tenant-scoped agent_runs must include the tenant_id filter.
- **server/agentic/runs.ts:48** — updateRunState updates agent_runs by id only (WHERE eq(agentRuns.id, runId)) with no tenant_id constraint — tenant-scoped write missing tenant filter.
  - _Fix:_ Add tenantId parameter and include AND eq(agentRuns.tenantId, tenantId) in the WHERE clause so state merges only affect the caller's tenant's run.
- **server/agentic/runs.ts:54** — replaceRunState replaces agent_runs.state by id only (WHERE eq(agentRuns.id, runId)) without constraining tenant_id — cross-tenant overwrite risk.
  - _Fix:_ Require tenantId and add a tenant filter (AND eq(agentRuns.tenantId, tenantId)) to the WHERE clause before performing the update.
- **server/agentic/runs.ts:58** — completeRun updates agent_runs status/result by run id only (WHERE eq(agentRuns.id, runId)) with no tenant_id constraint — can complete runs across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (AND eq(agentRuns.tenantId, tenantId)) or accept tenantId as a parameter so this write only affects the intended tenant's run.
- **server/agentic/runs.ts:67** — failRun updates agent_runs by run id only (WHERE eq(agentRuns.id, runId)) without tenant_id filtering — enables failing other tenants' runs by id.
  - _Fix:_ Require tenantId and add AND eq(agentRuns.tenantId, tenantId) to the WHERE clause to enforce per-tenant write scoping.
- **server/agentic/runs.ts:76** — pauseRun updates agent_runs by run id only (WHERE eq(agentRuns.id, runId)) with no tenant_id constraint — cross-tenant pause possible.
  - _Fix:_ Add tenantId argument and include AND eq(agentRuns.tenantId, tenantId) in the WHERE clause to ensure only the tenant's runs are modified.
- **server/agentic/wake-scheduler.ts:79** — runDueWakes performs a bulk UPDATE/SELECT to claim due agent_wake_schedules without constraining tenant_id — this can claim schedules across tenants.
  - _Fix:_ Scope the claim to a specific tenant (e.g. process wakes per-tenant) or add tenant_id filters to the WHERE/inner SELECT so the UPDATE only affects schedules belonging to the intended tenant. Ensure all reads/updates against agent_wake_schedules include tenant_id.
- **server/auto-memorize.ts:100** — SELECT from memory_entries performed without a tenant_id constraint (global scan used for dedup). This reads other tenants' memory rows and performs cross-tenant deduping, risking data leakage and mixing tenants' data.
  - _Fix:_ Scope the dedupe query to a single tenant (add WHERE tenant_id = ${tenantId} or accept a tenantId parameter). If cross-tenant analysis is required, do it explicitly under a documented admin-only path and isolate results (e.g. use an admin-only dataset or aggregate hashes rather than raw facts).
- **server/business-tools.ts:332** — SELECT in listContracts LEFT JOINs customers (cust) without constraining the joined customers row to the caller's tenant. The JOIN condition uses cust.id = c.customer_id but does not ensure cust.tenant_id matches the contract's tenant (or the request tenant), potentially exposing other tenants' customer data if referential integrity isn't enforced.
  - _Fix:_ Constrain the joined customers rows to the tenant, e.g. include the tenant check in the JOIN: LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = c.tenant_id (or AND cust.tenant_id = ${tid}), or add an explicit WHERE condition ensuring cust.tenant_id = ${tid}. This guarantees customer_name comes only from rows owned by the caller's tenant.
- **server/ceo-orchestrator.ts:815** — Unscoped SELECT from `personas` table: `const cachedPersonas = await db.select().from(personasTable);` fetches all persona rows with no tenant constraint. If `personas` is tenant-scoped, this leaks cross-tenant rows and allows later logic to map or use persona ids that belong to other tenants.
  - _Fix:_ Restrict the query to the caller/plan tenant (e.g. add .where(eq(personasTable.tenant_id, plan.tenantId)) or pass an explicit tenantId into this helper). Alternatively, if personas are global, ensure that is documented and enforced; otherwise add tenant_id to the personas schema and always filter by it before using persona ids in tenant-scoped operations.
- **server/channel-routing.ts:53** — INSERT uses caller-supplied personaId directly when creating/updating a channel route: `VALUES (${channel}, ${personaId})` / `ON CONFLICT ... DO UPDATE SET persona_id = ${personaId}`. There is no validation that the provided personaId belongs to the caller's tenant.
  - _Fix:_ Validate ownership before writing: look up the persona row and confirm persona.tenant_id === callerTenant (or require admin-only access). Alternatively, make channel_routes tenant-scoped (include tenant_id in the table) and require tenant_id in INSERT/UPDATE so the DB-level WHERE can enforce isolation.
- **server/chat-engine.ts:4352** — storage.updateMemoryEmbedding(entry.id, emb) updates a memory/embedding row using only the entry id with no tenant scoping/verification.
  - _Fix:_ Constrain the update by tenant (pass tenantId into updateMemoryEmbedding or have the storage layer apply WHERE tenant_id = ?) and/or verify the entry belongs to the caller's tenant before performing the update to prevent accidental cross-tenant modification.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` is executed without any tenant_id filter (reads all tenants).
  - _Fix:_ Constrain the SELECT to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or require/verify an explicit admin context before scanning across tenants. If platform-wide scanning is needed, perform it with an explicit, auditable admin path that enforces authorization and minimizes returned tenant data (or iterate tenants and query per-tenant with tenant_id filters).
- **server/compaction.ts:463** — INSERT into tenant-scoped table `memory_entries` uses ${tenantId} which is an optional parameter and is not validated — this can insert entries with a null/incorrect tenant or misattribute memories across tenants.
  - _Fix:_ Require and validate a non-null, integer tenantId before inserting (throw or abort if missing). Ensure the caller provides a trusted tenantId (or resolve it from the active session) and include it explicitly in the INSERT. e.g. if (!tenantId) throw; then INSERT ... VALUES (..., ${tenantId}).
- **server/credential-vault.ts:169** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} executes without scoping by tenant_id — this can modify rows belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}. Alternatively resolve/verify the credential belongs to the caller's tenant before updating.
- **server/credential-vault.ts:176** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} executes without scoping by tenant_id — this can update another tenant's row if id is guessed or ambiguous.
  - _Fix:_ Include tenant scoping on the UPDATE: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}. Ensure `cred` was resolved for the same tenant before updating as well.
- **server/data-protection.ts:73** — UPDATE on tenant-scoped table `conversations` without tenant_id predicate — softDeleteConversation updates a conversation by id but does not include AND tenant_id = ${tenantId}. This can mutate another tenant's row if ids collide or the caller is mistaken.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. `WHERE id = ${conversationId} AND tenant_id = ${tenantId}`, or verify ownership up-front and/or run the operation in a tenant-scoped transaction (withTenantTx) so the change is constrained to the caller's tenant.
- **server/data-protection.ts:112** — SELECT from tenant-scoped table `conversations` without tenant filtering — permanentlyPurgeSoftDeleted queries expired conversations across all tenants (`WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}`) with no tenant_id filter.
  - _Fix:_ If this is intended to be multi-tenant system maintenance, make the behavior explicit (require an admin-only code path). Otherwise restrict the SELECT by tenant (accept a tenantId parameter) or enumerate tenants and process per-tenant within a tenant-scoped transaction; avoid blind cross-tenant SELECTs.
- **server/data-protection.ts:125** — DELETE from tenant-scoped table `messages` lacks tenant scoping — permanentlyPurgeSoftDeleted deletes messages by conversation_id with no tenant_id predicate.
  - _Fix:_ Include tenant scoping in the DELETE, e.g. `DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}`, or perform the purge within an explicit tenant-scoped transaction and verify ownership before destructive operations.
- **server/data-protection.ts:126** — DELETE from tenant-scoped table `compaction_archives` lacks tenant scoping — permanentlyPurgeSoftDeleted deletes compaction_archives by conversation_id with no tenant_id predicate.
  - _Fix:_ Add tenant filtering to the DELETE, e.g. `DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}`, or otherwise ensure the delete is executed only for the target tenant (verify ownership / use withTenantTx).
- **server/data-protection.ts:144** — SELECT from tenant-scoped table `messages` without tenant scoping — verifyMessageSaved queries messages by conversation_id (and role) but the function does not accept/verify tenantId, so this reads a tenant-scoped table without tenant constraint or ownership verification.
  - _Fix:_ Require and pass tenantId into verifyMessageSaved and include `AND tenant_id = ${tenantId}` in the WHERE, or first verify the conversation belongs to the caller's tenant and only then query messages by conversation_id. Alternatively, perform the check inside a withTenantTx(tenantId, ...) so RLS / txn-scoped enforcement applies.
- **server/delivery-pipeline.ts:672** — SELECT prior delivery by stripePaymentId queries delivery_logs without a tenant_id filter. This can leak or mix deliveries across tenants when deduping by payment id.
  - _Fix:_ Scope the lookup to the caller's tenant (add tenant as a parameter and include eq(deliveryLogs.tenantId, tenantId) in the WHERE). If global lookup is intended, document and protect it explicitly (and restrict to admin paths only).
- **server/delivery-pipeline.ts:1002** — getDeliveryByStripePayment selects from delivery_logs WHERE stripe_payment_id = $1 with no tenant_id constraint, returning potentially another tenant's delivery row.
  - _Fix:_ Require and apply a tenantId (or only allow admin/internal callers) and add eq(deliveryLogs.tenantId, tenantId) to the WHERE clause. If multi-tenant lookup by payment id is required, restrict to an admin-only API or add explicit authorization checks.
- **server/dream-consolidation.ts:83** — Query SELECT role, content FROM messages WHERE conversation_id = ${conv.id} does not include tenant_id. Although conversation rows were fetched scoped to tenant earlier, reading messages without tenant filter relies on the conversation_id alone and can risk cross-tenant access in absence of FK/RLS.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause when selecting messages, or fetch messages through a tenant-scoped API/storage layer that enforces ownership before returning content.
- **server/email.ts:392** — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... runs without tenant_id filtering. This enumerates tenant-scoped messages across all tenants for backfill.
  - _Fix:_ Run the backfill per-tenant (add tenant_id = ${tenantId}) or restrict this maintenance job to an admin process with explicit authorization. Avoid unscoped scans of tenant tables.
- **server/event-bus.ts:167** — UPDATE on tenant-scoped table event_log without tenant constraint (UPDATE event_log ... WHERE id = ${eventId}). This can update rows belonging to other tenants if ids collide.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId}) or otherwise ensure the UPDATE is executed within a transaction/scope that guarantees the id belongs to the caller's tenant. Always bind and check tenant_id on mutating statements.
- **server/event-bus.ts:201** — UPDATE on tenant-scoped table event_log without tenant constraint (UPDATE event_log SET ... WHERE id = ${eventId}). Same as above — mutates a tenant-scoped row by id only.
  - _Fix:_ Add tenant scoping to the WHERE clause (WHERE id = ${eventId} AND tenant_id = ${tenantId}) or validate ownership before updating. Make all UPDATE/DELETE statements against tenant-scoped tables include tenant filter.
- **server/event-bus.ts:280** — createEventSubscription inserts caller-supplied personaId into event_subscriptions without verifying that the persona belongs to the caller's tenant. Inserting a foreign-id from input without ownership validation can link cross-tenant rows.
  - _Fix:_ Validate the personaId before INSERT (e.g. SELECT id FROM personas WHERE id = ${params.personaId} AND tenant_id = ${tenantId}) and reject if not found. Alternatively, enforce insertion only with persona ids known to belong to the tenant.
- **server/firecrawl.ts:184** — UPDATE on tenant-scoped table scraped_pages uses only the row id in WHERE (WHERE id = existing[0].id) after selecting the row; it does not include tenant_id in the UPDATE WHERE clause. That allows accidental cross-tenant updates if ids collide or selection logic is compromised.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or use the same tenant-bound WHERE used in the initial SELECT. Always include tenant constraint on UPDATE/DELETE for tenant-scoped tables.
- **server/firecrawl.ts:307** — UPDATE on tenant-scoped table scraped_pages inside crawl loop uses only id in WHERE (WHERE id = existing[0].id) without tenant constraint. Same risk: cross-tenant mutation by id only.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise ensure the update is restricted to the tenant. Ensure every write against scraped_pages includes tenant filtering.
- **server/glasses-gateway.ts:156** — SELECT from apiKeys performed without scoping by tenant (no WHERE tenant_id = ...). This reads a tenant-scoped table (apiKeys contains tenantId) with no tenant constraint.
  - _Fix:_ Either treat apiKeys as a global table (document and enforce a unique constraint on keyHash) or include tenant scoping in the query. If the code must look up by keyHash during auth (before tenant is known), ensure the schema makes keyHash globally unique and document that apiKeys is not tenant-scoped. If the table is tenant-scoped, restrict the query with the caller's tenant (eq(apiKeys.tenantId, <tenantId>)) or otherwise ensure the lookup cannot return rows from other tenants.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images executed with session_id and no tenant scoping. presenter_slide_images updates/inserts are not constrained by tenant_id or validated against the session's tenant in the SQL, allowing potential cross-tenant persistence if session_id is abused.
  - _Fix:_ Either add tenant_id to presenter_slide_images and populate it on INSERT, or perform the INSERT via a query that verifies the session belongs to the caller's tenant (e.g. INSERT ... SELECT ... FROM presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure every write is constrained to the caller's tenant in the SQL itself.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant constraint. Updating presenter_sessions by id without scoping to tenant_id can modify another tenant's session record.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or include tenant_id in the UPDATE predicate. If presenter_sessions is multi-tenant, always enforce tenant_id in the SQL to prevent cross-tenant writes.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` with no tenant_id constraint (reads proposals across all tenants).
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise assert an explicit admin/tenant context before scanning. Prefer using the storage layer or Drizzle .where(eq(table.tenantId, tenantId)) so reads are scoped to a single tenant.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` (stuck heartbeat check) with no tenant_id constraint — reads tasks across tenants.
  - _Fix:_ Restrict the query to a tenant (WHERE tenant_id = ${tenantId}) or, if the intention is an admin-wide scan, explicitly run per-tenant with an asserted admin context and document that behavior. Prefer using storage methods that enforce tenant scoping.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table `research_schedules` with no tenant_id constraint (iterates schedules across tenants).
  - _Fix:_ When reading tenant-scoped rows, either filter by a single tenant_id or explicitly iterate per-tenant under an asserted admin/system context. If the scheduler must examine all tenants, do so by enumerating tenants and executing tenant-scoped queries for each (or otherwise document and guard the admin intent). Use Drizzle .where(eq(..., tenantId)).
- **server/heartbeat.ts:526** — SELECT from tenant-scoped table `event_log` (pending events) with no tenant_id constraint — reads events across tenants.
  - _Fix:_ Scope the query by tenant (WHERE tenant_id = ${tenantId}) or, if processing events for all tenants is intended, first enumerate tenants and run tenant-scoped queries per tenant under an explicit admin flow. Ensure every read of tenant-scoped data uses the caller's asserted tenant context.
- **server/heartbeat.ts:1503** — SELECT from tenant-scoped table `heartbeat_logs` (last successful Dream Memory Consolidation) without tenant_id constraint — reads logs across tenants.
  - _Fix:_ Add tenant scope to the SELECT (e.g. WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' AND tenant_id = ${tenantId}) or otherwise run the check per-tenant with an explicit admin context. Use tenant-aware storage helpers.
- **server/internal-resolver.ts:171** — UPDATE code_proposals modifies a proposal row by id without scoping to tenant_id — may archive or affect proposals across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending') or otherwise verify the caller/event's tenant owns the proposal before performing the state transition. Ensure all writes to tenant-scoped tables include tenant_id guards.
- **server/lib/agent-trace.ts:124** — UPDATE against tenant-scoped table agent_trace_spans omits tenant_id in WHERE (UPDATE ... WHERE span_id = ${spanId}). This updates rows by span_id only and can cross tenant boundaries.
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Obtain tenantId (currentTenantId()) or record tenantId at span-open time so persistSpanClose can use a trusted tenant id when updating.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on procedure_evolution_runs omits tenant predicate (WHERE id = ${runId}). Mutating a tenant-scoped table without constraining tenant_id can affect other tenants' runs.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) and ensure tenantId is passed into the error/cleanup paths that perform these updates.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on procedure_evolution_runs omits tenant predicate (WHERE id = ${runId}) when marking noop/done. Tenant constraint missing allows cross-tenant mutation.
  - _Fix:_ Include eq(procedureEvolutionRuns.tenantId, tenantId) in the WHERE and propagate tenantId into this control flow before updating the row.
- **server/lib/aevo-meta-editor.ts:465** — UPDATE on procedure_evolution_runs omits tenant predicate (WHERE id = ${runId}) in validation-failed path. Tenant-scoped update lacks tenant_id guard.
  - _Fix:_ Constrain the update by tenant (AND tenant_id = ${tenantId}) — use and(eq(...id...), eq(...tenantId...)) so only the caller's tenant row is touched.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE on procedure_evolution_runs omits tenant predicate (WHERE id = ${runId}) after inserting procedure_edits — status flip lacks tenant scoping.
  - _Fix:_ Add tenant predicate to the WHERE clause (eq(procedureEvolutionRuns.tenantId, tenantId)). Ensure all code paths that update procedureEvolutionRuns use the tenantId in the WHERE.
- **server/lib/bwb-job-progress.ts:74** — The scope(jobId, ...) helper can omit the tenant_id predicate when env-threaded tenant (BWB_TENANT_ID) is missing, causing subsequent SELECT/UPDATE/DELETE on video_jobs to run without tenant scoping (pinned only by jobId).
  - _Fix:_ Don’t permit dropping the tenant predicate: require a valid tenantId and fail/abort when missing, or have callers pass an explicit tenantId and enforce eq(videoJobs.tenantId, tenantId) in every WHERE. If job-level secrecy is relied upon, document and enforce it at the callsite rather than silently falling back to jobId-only predicates.
- **server/lib/inbox-ingest.ts:431** — SELECT from tenant-scoped table `inbox_messages` lacks a tenant_id constraint (reads m.id without restricting to caller tenant).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. add `AND m.tenant_id = ${tenantId}`) or otherwise verify the row belongs to the caller's tenant before returning it. Prefer parameterized tenant filters rather than trusting message_id alone.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only id in WHERE clause (no tenant_id check). This allows modifying another tenant's profile if an id is known.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well, e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Always include tenant_id in WHERE for updates/deletes on tenant-scoped tables as a belt-and-suspenders check.
- **server/memory-graph.ts:66** — UPDATE on tenant-scoped table memory_categories omits tenant_id in WHERE clause (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This can modify another tenant's row with the same id.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. add AND tenant_id = ${tenantId} (or use tx/context-bound tenant clause) so the update only affects rows for the caller's tenant.
- **server/memory-graph.ts:126** — refreshCategoryCount performs an UPDATE on memory_categories without constraining tenant_id and the inner SELECT COUNT(*) FROM memory_entries also omits tenant_id. Both reads/writes lack tenant scoping.
  - _Fix:_ Filter the inner COUNT by tenant_id (AND tenant_id = ${tenantId}) and constrain the outer UPDATE to the tenant (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Always bind tenant via parameter, not by trusting id alone.
- **server/memory-graph.ts:182** — SELECT 1 FROM memory_links ... does not constrain by tenant_id when checking for existing links. If memory_links is tenant-scoped, this can return rows from other tenants.
  - _Fix:_ Add tenant filtering to the existence check (e.g. AND tenant_id = ${tenantId}) or otherwise ensure memory_links rows are always matched to the caller's tenant before reads.
- **server/memory-graph.ts:393** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks tenant constraint. This can update a row from a different tenant if ids collide.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or use the transaction tenant context) to ensure the update only affects the caller's tenant rows.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom} ... WHERE id = ${c.id} omits tenant_id in the WHERE clause. This updates rows by id without verifying tenant ownership.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) so only the tenant's triples are modified.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} is executed without a tenant_id filter, allowing cross-tenant modification by id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or perform the update via a tenant-scoped API/tx) so only the tenant's row is updated.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in the update path) is missing tenant scoping and may update another tenant's row.
  - _Fix:_ Add tenant filtering (AND tenant_id = ${tenantId}) to the UPDATE or use a tenant-aware helper that enforces ownership before modifying rows.
- **server/memory-intelligence.ts:438** — storage.updateMemoryEntry(remove.id, { status: 'superseded' }) is called without passing tenantId. A write helper that requires tenant context may be bypassed, allowing cross-tenant updates if the helper expects an explicit tenant parameter.
  - _Fix:_ Pass the tenantId to storage.updateMemoryEntry (or ensure the storage API obtains the active tenant context) so the update is performed only on the caller's tenant rows and ownership is validated.
- **server/minds-engine.ts:214** — INSERT into mind_tickets builds a Postgres array literal via string interpolation: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(',')}}` : null}. This is string-interpolated SQL using request-derived values (injection/isolation risk).
  - _Fix:_ Avoid constructing SQL fragments via string interpolation. Use parameterized array binding (pass the array as a bound parameter or use Drizzle's array helpers / sql.array) so the DB driver binds values safely and prevents SQL injection/isolation issues.
- **server/minds-engine.ts:262** — checkDependenciesMet uses id = ANY(${`{${ticket.dependsOn.join(',')}}`}::int[]) — building an array literal via string interpolation from potentially caller-owned values (injection + tenant/isolation risk).
  - _Fix:_ Use parameterized array binding (e.g. WHERE id = ANY(${ticket.dependsOn}::int[]) with proper driver support) or pass the array as a bound parameter via Drizzle to avoid string interpolation of request-derived data.
- **server/minds-engine.ts:327** — markEventsHandled updates WHERE id = ANY(${`{${eventIds.join(',')}}`}::int[]) — this injects a request-derived list into SQL via string interpolation (SQL injection / isolation risk).
  - _Fix:_ Use parameterized array binding for eventIds (bind the array as a parameter) instead of building a literal string. Ensure tenant_id is also included (it already is) and that ownership is validated.
- **server/minerva-planner.ts:356** — CAS UPDATE on plans (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING id, tenant_id, ...) does not constrain tenant_id. This allows a caller with a planId to change a plan belonging to another tenant.
  - _Fix:_ Constrain the CAS UPDATE by tenant_id (AND tenant_id = ${expectedTenantId}) or require the caller to supply/authorize the tenant and bind it. Always include tenant scoping on UPDATE/DELETE for tenant-scoped tables.
- **server/minerva-planner.ts:373** — Probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} lacks tenant filtering. Reading plan state by id without tenant scope can cause cross-tenant inference or incorrect idempotency checks.
  - _Fix:_ Add tenant constraint (AND tenant_id = ${expectedTenantId}) to the probe SELECT, or fetch the row only from a tenant-scoped query to ensure the row belongs to the caller's tenant.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 is executed without tenant filtering. This can find a child plan in a different tenant.
  - _Fix:_ Include tenant filtering (AND tenant_id = ${expectedTenantId}) when probing for child plans, so the check is limited to the caller's tenant.
- **server/minerva-planner.ts:460** — Rollback UPDATE (RESET decision metadata) uses WHERE id = ${args.planId} AND status = 'revising' but does not constrain tenant_id. This may rollback/alter a plan in another tenant.
  - _Fix:_ Constrain the rollback UPDATE by tenant_id (AND tenant_id = ${expectedTenantId}) so the corrective rollback only touches rows belonging to the caller's tenant.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code calls assertProjectInTenant to check ownership but the INSERT omits tenant_id (columns are NOT NULL with no default), so the row may be written without correct tenant scoping.
  - _Fix:_ Include tenant_id explicitly in the INSERT and bind it from the verified caller (e.g. VALUES (${options.tenantId}, ...)) or use a central helper that always sets tenant_id. Keep the assertProjectInTenant check (or equivalent) to verify ownership, but still set tenant_id on every insert into tenant-scoped tables.
- **server/mpeg-engine.ts:1487** — INSERT into tenant-scoped table `project_files` (in parallel path) does not include tenant_id. Even though assertProjectInTenant is called, the INSERT omits tenant_id, violating the invariant that every insert must pass tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT values (e.g. INSERT INTO project_files (tenant_id, project_id, ... ) VALUES (${options.tenantId}, ${options.projectId}, ...)). Use a shared insert helper or enforce a lint rule to prevent inserts into tenant-scoped tables without tenant_id.
- **server/oauth-subscriptions.ts:884** — SELECT FROM tenant-scoped table `oauth_subscriptions` without scoping by tenant_id (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows across all tenants.
  - _Fix:_ If this must run across tenants, make it an explicit admin-only job with additional access controls/audit logging. Otherwise restrict the query with a WHERE tenant_id = ${tenantId} (or iterate tenants explicitly) so each read is limited to a single tenant's rows.
- **server/oauth-subscriptions.ts:893** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes against a tenant-scoped table with no tenant_id filter — returns rows across all tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or explicitly document and gate this as an admin/global operation. Prefer per-tenant iteration to preserve isolation.
- **server/oauth-subscriptions.ts:1236** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — reads all active subscriptions across tenants with no tenant filter.
  - _Fix:_ Restrict to a single tenant (WHERE tenant_id = ${tenantId}) or, if a global scan is required, make the global intent explicit and run with strict admin privileges and careful auditing. Prefer per-tenant queries to maintain isolation.
- **server/oauth-subscriptions.ts:1306** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — forceRefreshAllSubscriptions scans active subscriptions across all tenants without tenant scoping.
  - _Fix:_ Either accept and gate this as an admin/global operation, or change to per-tenant enumeration (e.g. query distinct tenant_ids and run per-tenant refreshes) and ensure each refresh call uses a tenant-scoped query.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(lockKey) inside SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). sql.raw() with a value derived from caller-provided ledgerKey/tenantId bypasses parameter binding and risks SQL injection or accidental misuse of unvalidated inputs.
  - _Fix:_ Avoid sql.raw with request-derived values. Validate and canonicalize ledgerKey and tenantId, convert to a numeric BigInt safely in JS (e.g. const k = BigInt(validatedLockKey)) and pass it as a parameter: await tx.execute(sql`SELECT pg_advisory_xact_lock(${k}::bigint)`). If ledgerKey is user-supplied, enforce strict validation and require tenant scoping before using it.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} — reading a persona by id without constraining tenant_id/validating tenant ownership allows cross-tenant data disclosure.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or first verify the persona row belongs to the requesting tenant. Reject or require elevated/admin privileges if a global export is intended.
- **server/plan-executor.ts:106** — appendExecutionLog updates the plans table by id without constraining by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows mutation of another tenant's plan log if an id is supplied.
  - _Fix:_ Require the caller to provide/derive tenantId and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. Alternatively fetch the plan row first (with tenant check) and then perform the update using the known tenant_id in the WHERE predicate to avoid cross-tenant writes.
- **server/personality-files.ts:8** — personality_files table defines tenant_id INTEGER NOT NULL DEFAULT 1. A default tenant_id allows INSERTs that omit tenant_id to silently assign another tenant (default=1), enabling accidental or malicious cross-tenant writes.
  - _Fix:_ Remove the DEFAULT and require callers to explicitly pass tenant_id on every INSERT (tenant_id INTEGER NOT NULL, no default). If a default is necessary for legacy reasons, add strict code-side checks that always provide tenantId, and audit existing INSERTs to ensure tenant_id is always supplied; better: enforce NOT NULL without default and validate in tests/linters.
- **server/process-governor.ts:1184** — SELECT with LEFT JOIN reads governance_rules (a tenant-scoped table) without constraining the joined table by tenant_id. Query: FROM governance_actions ga LEFT JOIN governance_rules gr ON gr.id = ga.rule_id WHERE ga.tenant_id = ${tenantId}. The join may return governance_rules rows from other tenants if id collisions exist.
  - _Fix:_ Restrict the joined governance_rules rows to the tenant as well, e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ga.tenant_id (or add WHERE gr.tenant_id = ${tenantId}). Ensure every read of a tenant-scoped table includes an explicit tenant filter.
- **server/project-brain.ts:135** — SELECT projects by id without any tenant filter (SELECT name, status, description FROM projects WHERE id = ${projectId}). The function has no tenant parameter and reads tenant-scoped data by id only.
  - _Fix:_ Require/accept a tenantId parameter and constrain queries, e.g. SELECT ... FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}. If the caller cannot provide tenantId, resolve it from an authenticated context before querying.
- **server/project-brain.ts:350** — Selecting conversations for a project without tenant scope (SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}) inside the backfill loop. No tenant_id constraint is applied to conversations or subsequent messages queries.
  - _Fix:_ Include tenant scoping on these queries (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) and likewise require tenant_id on the messages SELECT: WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}.
- **server/project-brain.ts:360** — Selecting messages without tenant constraint (SELECT role, content FROM messages WHERE conversation_id = ${conv.id}). This is part of the backfill flow that currently lacks tenant scoping and can read messages across tenants.
  - _Fix:_ Add tenant scoping to the messages query, e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}. Ensure updateProjectBrain and backfill flows accept/verify tenant ownership before reading messages.
- **server/proposal-verifier.ts:33** — Selecting code_proposals by id without tenant constraint (SELECT ... FROM code_proposals WHERE id = ${proposalId}). The verifier lacks any tenant parameter and reads tenant-scoped proposals by id only.
  - _Fix:_ Require a tenantId (or resolve it from request/auth context) and include tenant_id = ${tenantId} in the SELECT. E.g. SELECT ... FROM code_proposals WHERE id = ${proposalId} AND tenant_id = ${tenantId}.
- **server/recurring-messages.ts:252** — SELECT from tenant-scoped table agent_knowledge without tenant_id filter (reads across tenants).
  - _Fix:_ Restrict this query by tenant (e.g. add WHERE tenant_id = ${tenantId} or run per-tenant queries). If an intentional global scan is required, document and gate it behind an explicit system-only flag and audited code path, and ensure any later writes include tenant checks.
- **server/research-engine.ts:449** — SELECT id,... FROM research_experiments WHERE session_id = ${sessionId} AND status = 'keep' — no tenant_id filter on a tenant-scoped table read.
  - _Fix:_ Restrict the query by tenant (AND tenant_id = ${session.tenantId}) or ensure session_id is validated to belong to the session. Prefer explicit tenant_id WHERE clauses for clarity and safety.
- **server/research-engine.ts:458** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} — missing tenant_id constraint when reading a tenant-scoped table.
  - _Fix:_ Add tenant_id = ${session.tenantId} to the WHERE clause to ensure the program read belongs to the session's tenant.
- **server/research-engine.ts:463** — SELECT summary FROM research_sessions WHERE id = ${sessionId} — no tenant_id constraint on a tenant-scoped table read.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} to limit the read to the owning tenant, or otherwise validate ownership before using the summary.
- **server/routes.ts:160** — Startup restoreUploadsFromDb performs a SELECT FROM file_storage without scoping by tenant_id. It iterates and writes files from DB to UPLOADS_DIR for all rows matching mimeType and storageKey criteria, potentially restoring other tenants' private files into the shared uploads folder.
  - _Fix:_ Filter the SELECT by tenant (or otherwise restrict to a safe admin-held tenant or a migration-specific allowlist). If the intent is per-tenant restore, perform the operation per-tenant and only write files for the tenant(s) being restored. Audit and gate this startup job under an explicit admin-only mode if cross-tenant access is intended for maintenance.
- **server/routes.ts:4410** — INSERT into tenant-scoped table 'project_notes' omits tenant_id. Query: INSERT INTO project_notes (project_id, note, author) VALUES (...)
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ...)), or call a storage helper that requires/validates tenantId to ensure the row is created under the caller's tenant.
- **server/routes.ts:6669** — Admin import calls storage.createPersona(rest) without passing an explicit tenantId for tenant-scoped data insertion.
  - _Fix:_ When creating tenant-scoped rows during import, pass an explicit tenantId (or otherwise ensure the storage API requires and enforces tenantId). Avoid relying on implicit/default tenancy during inserts.
- **server/routes.ts:6679** — Admin import calls storage.createMemoryEntry({...}) without passing tenantId for memory_entries (tenant-scoped insert).
  - _Fix:_ Pass tenantId explicitly into createMemoryEntry or ensure the storage API enforces tenant ownership at the call site. Validate ownership and that the tenant_id column is set on all inserts.
- **server/routes.ts:6689** — Admin import calls storage.createKnowledge({...}) without supplying tenantId for agent_knowledge (tenant-scoped insert).
  - _Fix:_ Include tenantId in calls that create tenant-scoped knowledge rows, or use storage APIs that require tenantId to prevent accidental creation under the wrong tenant or DB errors.
- **server/routes.ts:6699** — Admin import calls storage.createConversation(rest) without an explicit tenantId for conversations (tenant-scoped insert).
  - _Fix:_ Provide tenantId explicitly when creating conversations during import, or ensure storage.createConversation requires tenantId and validates it against the intended tenant.
- **server/routes.ts:6705** — Admin import calls storage.createMessage({... conversationId ...}) without passing tenantId for messages (tenant-scoped insert).
  - _Fix:_ When inserting messages, either include tenantId explicitly or resolve/validate the message's conversation tenant before insert (e.g. fetch conversation with tenant check and pass tenantId into storage.createMessage).
- **server/routes.ts:6717** — Admin import calls storage.createHeartbeatTask(rest) without providing tenantId for heartbeat_tasks if those are tenant-scoped.
  - _Fix:_ Ensure tenantId is supplied when creating tenant-scoped heartbeat_tasks during import, or use an API that enforces tenant scoping at the storage layer.
- **server/routes/admin-zombie-detector.ts:54** — Querying conversations (tenant-scoped) without constraining by tenant_id. The personasRes CTE builds `used` from `conversations` with no tenant filter, then joins into `personas`, causing cross-tenant exposure even though this is an admin-only endpoint.
  - _Fix:_ Add an explicit tenant filter to the CTE so the aggregation only considers the admin caller's tenant (e.g. WHERE persona_id IS NOT NULL AND tenant_id = ${tenantId} AND updated_at > ...). Ensure the transaction receives the admin tenantId and pass it into the sql tag (or use parameterized query helpers) to enforce tenant-scoping.
- **server/routes/admin.ts:587** — Selecting ai_insights rows without a tenant filter (SELECT ... FROM ai_insights WHERE status = 'new' ...). ai_insights is tenant-scoped elsewhere; this query will return incidents across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or include tenant_id via the constructed `where` variable) so only the intended tenant's ai_insights are returned.
- **server/routes/admin.ts:609** — Querying ai_insights for pending Minerva routing without tenant constraint (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' ...). This reads tenant-scoped data across all tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tenantId}) so the admin endpoint only scans the tenant the caller is authorized for (or explicitly limit to the platform-owner tenant if intended). Validate that every ai_insights access in admin paths includes tenant_id filtering.
- **server/routes/conversations.ts:98** — UPDATE on conversations lacks tenant constraint. The query `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` does not enforce tenant_id, so a row belonging to another tenant could be modified.
  - _Fix:_ Restrict the UPDATE by tenant, e.g. `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use the storage layer / Drizzle .where(eq(conversations.tenantId, tenantId))). Always include tenant_id in UPDATE/DELETE WHERE clauses and/or verify ownership before writing.
- **server/routes/conversations.ts:102** — INSERT into project_conversations omits tenant context. The INSERT `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id for a tenant-scoped table.
  - _Fix:_ Include tenant_id in the inserted columns and values (e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`), or ensure the storage API populates tenant_id and validates ownership before inserting.
- **server/routes/conversations.ts:104** — UPDATE on projects lacks tenant constraint. The query `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` does not include tenant_id, allowing cross-tenant modification.
  - _Fix:_ Restrict the UPDATE by tenant, e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Always include tenant_id filters for tenant-scoped tables even after in-handler ownership checks.
- **server/routes/conversations.ts:227** — INSERT into project_notes omits tenant context. The INSERT `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id on what is likely a tenant-scoped table.
  - _Fix:_ Add tenant_id to the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`) or use a storage API that sets and enforces tenant ownership.
- **server/routes/inbox-notifications.ts:101** — UPDATE on inbox_messages missing tenant constraint. After selecting message, the handler runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without restricting by tenant_id.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}` (or use the storage layer / Drizzle .where(eq(inbox_messages.tenantId, tenantId))). Even when the row was previously selected, enforce tenant_id in the write query.
- **server/routes/heartbeat.ts:102** — Admin-guarded UPDATE on heartbeat_tasks lacks tenant constraint. The query `UPDATE heartbeat_tasks SET approval_status = 'approved', enabled = true, next_run_at = ${nextRun} WHERE id = ${taskId}` does not include tenant_id, allowing modification of tasks across tenants.
  - _Fix:_ Include tenant filtering in the UPDATE, for example `WHERE id = ${taskId} AND tenant_id = ${task.tenantId ?? tenantId}` (ensure tenantId is derived/validated), or use a storage API that enforces tenant ownership before mutating rows.
- **server/routes/heartbeat.ts:116** — Admin-guarded UPDATE on heartbeat_tasks lacks tenant constraint. The query `UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId}` does not enforce tenant_id.
  - _Fix:_ Restrict the UPDATE by tenant, e.g. `UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId} AND tenant_id = ${task.tenantId ?? tenantId}` or call a storage layer that validates tenant ownership before performing the update.
- **server/routes/stripe-checkout.ts:106** — Request-derived value interpolated directly into a sql`...` template: `WHERE pr.id = ${priceId}`. This uses string/interpolated SQL with a request-sourced value, which risks SQL injection and violates the rule disallowing sql/raw or interpolated SQL with request-derived values.
  - _Fix:_ Avoid interpolating request-derived values into raw SQL templates. Use the Drizzle query builder or parameterized queries instead (e.g. db.select().from(prices).where(eq(prices.id, priceId)) or pass priceId as a bound parameter). Additionally validate/normalize priceId server-side before use (pattern/length) and prefer explicit Drizzle .where(eq(...)) calls to eliminate injection and ensure correct query semantics.
- **server/safety/transactional-snapshot.ts:408** — Rollback path updates action_snapshots without scoping by tenant_id: `UPDATE action_snapshots SET undone_at = NULL WHERE id = $1` (no tenant check). This can clear the undone_at flag for a snapshot belonging to another tenant.
  - _Fix:_ Include tenant scoping in the rollback update and parameterize the tenant id, e.g. `UPDATE action_snapshots SET undone_at = NULL WHERE id = $1 AND tenant_id = $2` with [row.id, ctx.tenantId] (or otherwise pass the original tenant) so undo-claims cannot be reset across tenants.
- **server/sculptor.ts:94** — UPDATE to sculptor_sessions omits tenant constraint: `UPDATE sculptor_sessions SET status = 'failed', ... WHERE id = ${session.id}`. Without AND tenant_id = <tenant> an attacker (or bug) could modify another tenant's session row.
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. `WHERE id = ${session.id} AND tenant_id = ${params.tenantId}` (or the caller's tenantId) and parameterize the tenant value to enforce per-tenant isolation.
- **server/sculptor.ts:101** — UPDATE to sculptor_sessions omits tenant constraint: `UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}`. Missing tenant check allows cross-tenant modification.
  - _Fix:_ Add tenant scoping to the UPDATE: `WHERE id = ${session.id} AND tenant_id = ${params.tenantId}` (or use the explicit tenantId variable) to ensure only the owning tenant's row is modified.
- **server/sculptor.ts:156** — MonitorSession updates sculptor_sessions without tenant scoping: `UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}`. This writes tenant-scoped rows without verifying the tenant.
  - _Fix:_ Constrain the UPDATE to the tenant, e.g. `WHERE id = ${sessionId} AND tenant_id = ${tenantId}` (and pass tenantId from the monitor context) so only the tenant's own session row can be updated.
- **server/sculptor.ts:347** — Update to sculptor_sessions.review_result omits tenant constraint: `UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}`. This can overwrite another tenant's review_result.
  - _Fix:_ Add tenant scoping to the WHERE clause: `WHERE id = ${sessionId} AND tenant_id = ${tenantId}` (use the caller's tenantId) to enforce per-tenant writes.
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger by id without tenant constraint: `UPDATE agent_cost_ledger ... WHERE id = ${reservationId}`. This may modify ledger rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. `WHERE id = ${reservationId} AND tenant_id = ${tenantId}` (and pass/verify the tenantId associated with the reservation) to ensure only the owning tenant's ledger row is modified. If tenantId isn't available here, fetch/verify the row's tenant_id first.
- **server/second-opinion.ts:424** — releaseFusionReservation updates agent_cost_ledger by id without tenant constraint: `UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId}`. This can release/zero out another tenant's reservation.
  - _Fix:_ Constrain the UPDATE to the tenant, e.g. `WHERE id = ${reservationId} AND tenant_id = ${tenantId}` (pass/lookup tenantId tied to the reservation) or otherwise verify reservation ownership before updating to preserve tenant isolation.
- **server/seed.ts:34** — Unscoped SELECT on tenant-scoped table `research_sessions`: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id filter, allowing cross-tenant data exposure.
  - _Fix:_ Scope this query to a specific tenant (e.g. `WHERE tenant_id = ${tenantId}` or use Drizzle `.where(eq(research_sessions.tenantId, tenantId))`). If the intent is to operate only on a specific tenant during seeding, make the tenant explicit and document it.
- **server/seed.ts:2854** — Unscoped SELECT on tenant-scoped table `research_programs`: `SELECT id, name, model FROM research_programs WHERE is_active = true` lacks a tenant_id constraint and returns rows for all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. `WHERE tenant_id = ${tenantId} AND is_active = true`) or otherwise limit the query to the intended tenant. Use Drizzle's `.where(eq(research_programs.tenantId, tenantId))` to enforce isolation.
- **server/seed.ts:3204** — Unscoped SELECT on tenant-scoped table `conversations`: `SELECT DISTINCT c.id, c.project_id FROM conversations c ...` has no tenant_id filter and can return conversations across tenants.
  - _Fix:_ Add a tenant filter (e.g. `WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...`) or validate ownership of returned rows before acting on them. Use Drizzle `.where(eq(conversations.tenantId, tenantId))`.
- **server/seed.ts:3204** — SELECT from tenant-scoped table `conversations` is executed without any tenant_id constraint (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). This reads data across all tenants.
  - _Fix:_ Constrain the query to a specific tenant (e.g. add AND c.tenant_id = ${tenantId}) or otherwise scope it to the intended tenant context before performing the SELECT/iterating rows.
- **server/seed.ts:4374** — DELETE FROM heartbeat_tasks WHERE enabled = false executes without a tenant_id filter — this will delete disabled heartbeat tasks across all tenants.
  - _Fix:_ Add a tenant filter to the DELETE (e.g. DELETE ... WHERE enabled = false AND tenant_id = ${tenantId}), or run this cleanup per-tenant explicitly so you do not affect other tenants' tasks.
- **server/seed.ts:4492** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' is executed without tenant_id — reads aggregate across all tenants.
  - _Fix:_ Scope the SELECT by tenant (e.g. WHERE ... AND tenant_id = ${tenantId}) or otherwise perform per-tenant counting to avoid cross-tenant effects.
- **server/seed.ts:4501** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true is executed without tenant_id — will disable delegation tasks across all tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause, or run the update per-tenant to avoid disabling other tenants' tasks.
- **server/seed.ts:4512** — UPDATE heartbeat_tasks SET cron_expression = '0 * * * *' WHERE name = 'Self-Reflection' ... has no tenant_id filter and will modify the schedule for all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} so schedule modifications are applied only to the intended tenant.
- **server/seed.ts:4517** — UPDATE heartbeat_tasks SET cron_expression = '0 */2 * * *' WHERE name = 'Memory Consolidation' ... is executed without tenant scoping, affecting all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or perform per-tenant reconciliation) to avoid cross-tenant changes.
- **server/seed.ts:4521** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' runs without tenant_id — reads counts across all tenants.
  - _Fix:_ Filter by tenant_id (e.g. AND tenant_id = ${tenantId}) or perform per-tenant queries to limit scope to the caller's tenant.
- **server/seed.ts:4528** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is executed without tenant filtering; it will disable matching tasks across all tenants.
  - _Fix:_ Add a tenant constraint (AND tenant_id = ${tenantId}) to the WHERE clause so only the current tenant's tasks are affected.
- **server/seed.ts:4535** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) executes without tenant scoping — it inspects tasks across all tenants.
  - _Fix:_ Restrict this SELECT by tenant_id (AND tenant_id = ${tenantId}) or perform per-tenant evaluation to avoid reading unrelated tenants' tasks.
- **server/seed.ts:4551** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true runs without tenant_id, disabling tasks across all tenants that match the name conditions.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or apply the update per-tenant to prevent cross-tenant impact.
- **server/seed.ts:4032** — db.insert(heartbeatTasks).values([...]) inserts multiple heartbeat_tasks rows via Drizzle without explicitly setting tenant_id on the inserted objects. Insertion into a tenant-scoped table must include the tenant identifier.
  - _Fix:_ Ensure each inserted heartbeat task object includes the explicit tenantId (e.g. tenantId: currentTenant) or use a per-tenant insert path so every INSERT sets tenant_id explicitly.
- **server/seed.ts:4099** — db.insert(heartbeatTasks).values({ name: 'Model Scout', ... }) inserts a heartbeat_tasks row without an explicit tenant_id.
  - _Fix:_ Pass tenantId explicitly in the inserted object (e.g. tenantId: ${tenantId}) or otherwise scope inserts to the intended tenant.
- **server/seed.ts:4158** — db.insert(heartbeatTasks).values({ name: 'Daily Cloud Backup', ... }) inserts a heartbeat task without tenant_id.
  - _Fix:_ Include tenantId in the inserted object or perform per-tenant inserts so the row is associated with the correct tenant.
- **server/seed.ts:4174** — db.insert(heartbeatTasks).values({ name: 'Memory Snapshot Backup', ... }) inserts without tenant_id.
  - _Fix:_ Add tenantId to the inserted data (tenantId: ${tenantId}) to ensure tenant isolation.
- **server/seed.ts:4190** — db.insert(heartbeatTasks).values({ name: 'Decision Analysis Engine', ... }) inserts a tenant-scoped row without tenant_id.
  - _Fix:_ Set tenantId explicitly on the inserted object or scope insertion per-tenant.
- **server/seed.ts:4204** — db.insert(heartbeatTasks).values({ name: 'Trend Forecasting Engine', ... }) inserts a heartbeat_tasks row without tenant_id.
  - _Fix:_ Include tenantId in the values to avoid creating rows that are not bound to the caller's tenant.
- **server/seed.ts:4220** — db.insert(heartbeatTasks).values({ name: 'Process Optimization Engine', ... }) inserts without an explicit tenant_id.
  - _Fix:_ Ensure the insert includes tenantId or is executed under an explicit per-tenant context.
- **server/seed.ts:4236** — db.insert(heartbeatTasks).values({ name: 'Community Fork Scanner', ... }) inserts without tenant_id.
  - _Fix:_ Provide tenantId explicitly in the inserted object to keep the row scoped to the correct tenant.
- **server/seed.ts:4252** — db.insert(heartbeatTasks).values({ name: 'Memory Hygiene Sweep', ... }) inserts a tenant-scoped row without tenant_id.
  - _Fix:_ Add tenantId to the inserted object or perform per-tenant seeding to ensure tenant isolation.
- **server/seed.ts:4268** — db.insert(heartbeatTasks).values({ name: 'Embedding Backfill', ... }) inserts without tenant_id.
  - _Fix:_ Explicitly set tenantId on the inserted row (tenantId: ${tenantId}) or run seeding per-tenant so no rows are created without an explicit tenant association.
- **server/self-improvement.ts:165** — detectStagnation performs a SELECT FROM experiments without requiring or enforcing a tenant_id constraint when tenantId is omitted — this can return experiments across all tenants.
  - _Fix:_ Require a tenantId for this function (remove optional/default) or explicitly fail when tenantId is not provided. Always include eq(experiments.tenantId, tenantId) in the WHERE clause so queries never scan other tenants' rows.
- **server/self-improvement.ts:511** — getExperimentHistory will return experiments across all tenants if tenantId is not supplied — the function constructs an unscoped query when tenantId is omitted.
  - _Fix:_ Make tenantId required (or enforce a fail-closed behavior) and always add eq(experiments.tenantId, tenantId) to the WHERE clause. Do not issue unscoped reads against tenant-scoped tables.
- **server/skill-seeker.ts:300** — detectGap defaults tenantId to 1 (ADMIN) when the caller omits it. That means unseen/unspecified tenant context will cause inserts/updates to be recorded under the admin tenant, creating cross-tenant write risk.
  - _Fix:_ Remove the default tenantId=1; require callers to pass an explicit tenantId and fail-closed if missing. Ensure all INSERTs/UPDATEs use the provided tenantId.
- **server/skill-seeker.ts:743** — listGaps has branches that will return capability_gaps across all tenants when caller does not supply tenantId (or status-only path). Unscoped SELECTs on a tenant-scoped table can leak data.
  - _Fix:_ Require tenantId or restrict the API so unscoped reads are disallowed. Ensure every SELECT on capability_gaps includes WHERE tenant_id = ${tenantId} (unless the caller is an explicitly authorized admin and this is documented/audited).
- **server/skillify.ts:91** — SELECT from tenant-scoped table "messages" without constraining tenant_id. recentMsgs is queried by conversationId only (no WHERE tenant_id = tenantId).
  - _Fix:_ Include tenant scoping in the query (e.g. .where(eq(messages.conversationId, conversationId)).where(eq(messages.tenantId, tenantId))) or validate/require tenant ownership in the SQL itself (use tenantScope/requireTenantScope helper) so the DB never returns rows from other tenants.
- **server/skillify.ts:196** — SELECT from tenant-scoped table "conversations" without constraining tenant_id. The conversation is fetched by id only and tenant ownership is checked in application logic after the query rather than in the SQL WHERE clause.
  - _Fix:_ Add a tenant filter to the query (e.g. .where(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))) or use tenantScope/requireTenantScope so the SELECT never returns rows from other tenants.
- **server/skillify.ts:203** — SELECT from tenant-scoped table "messages" (analyzeConversation) without tenant_id in WHERE clause — messages are fetched by conversationId only.
  - _Fix:_ Constrain the query by tenant (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or otherwise use a validated tenant-scoping predicate so results cannot cross tenants.
- **server/skillify.ts:331** — SELECT from tenant-scoped table "messages" (skillifyConversation) without tenant_id in WHERE clause — recentMsgs is fetched by conversationId only.
  - _Fix:_ Include tenant scoping in the SQL (e.g. add .where(eq(messages.tenantId, tenantId))) or use the requireTenantScope/tenantScope helpers to fail-closed when tenantId is missing so no cross-tenant reads are possible.
- **server/storage.ts:235** — getConversationUnscoped performs an unscoped SELECT on conversations (no tenant_id filter).
  - _Fix:_ Avoid unscoped reads or require an explicit tenantId parameter. If an unscoped fetch is truly necessary, restrict its usage to well-audited internal code paths and log/require post-hoc validation of the row's tenant before any downstream use. Prefer adding and using a parametrized tenant filter: .where(and(eq(conversations.id, id), eq(conversations.tenantId, tenantId))).
- **server/storage.ts:502** — getMemoryEntry allows tenantId to be omitted and will perform a SELECT without tenant scoping (only emits a warning). Unscoped reads of tenant-scoped rows are allowed when tenantId is not provided.
  - _Fix:_ Make tenantId required (fail-closed) or enforce assertValidTenantId(tenantId) at function entry. If unscoped access is required for very limited internal callers, provide a clearly named separate function and audit all call sites.
- **server/storage.ts:529** — touchMemoryEntries updates memory_entries by id list without any tenant filter (WHERE id IN (...)). This will update rows across tenants when ids are not validated for ownership.
  - _Fix:_ Require a tenantId parameter and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause, or filter the id list to only ids that belong to the caller's tenant before updating.
- **server/storage.ts:516** — updateMemoryEntry builds conditions using an optional tenantId; if tenantId is omitted the UPDATE runs without a tenant_id constraint, allowing cross-tenant updates.
  - _Fix:_ Make tenantId required (fail-closed) or call assertValidTenantId(tenantId) and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause. Avoid optional/implicit scoping that defaults to unscoped operations.
- **server/storage.ts:523** — deleteMemoryEntry may perform an UPDATE (status change) without tenant scoping if tenantId is omitted (conds only contain id), enabling cross-tenant mutation.
  - _Fix:_ Require tenantId and include it in the WHERE clause (AND tenant_id = ${tenantId}) or validate ownership before mutating. Do not rely on warning-only behavior for fail-closed security.
- **server/subagents.ts:218** — _spawnSubagent calls storage.getConversation(parentConversationId, params.tenantId ?? ADMIN_TENANT_ID). If params.tenantId is omitted the code falls back to an ADMIN_TENANT_ID, causing the conversation read to be performed as admin and potentially bypassing caller tenant ownership checks.
  - _Fix:_ Require callers to provide the correct tenantId (fail-closed) or derive tenantId from a validated authenticated context. Avoid defaulting to an admin tenant. If admin-level behavior is required, make the call site explicitly admin-only and strongly audit it.
- **server/task-planner.ts:202** — executeStep (task-planner) injects ADMIN_TENANT_ID into step.toolArgs when args._tenantId is undefined. This causes downstream tool executions to run as the admin tenant by default, enabling cross-tenant reads/writes if caller-provided steps omit _tenantId.
  - _Fix:_ Do not implicitly inject an admin tenant. Require an explicit tenantId for planner-executed tools or pass the real caller/owner tenant. If planner actions must run in admin context, enforce strict ACL checks and make the admin elevation explicit in the API surface. Prefer fail-closed behavior instead of implicitly using an admin tenant.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant ownership. The query checks only name: `where(eq(customTools.name, parsed.name))` — this can return rows belonging to other tenants.
  - _Fix:_ Filter by tenant_id as well. Use a combined where: `.where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))` (or at minimum add `eq(customTools.tenantId, tenantId)`) so reads are scoped to the caller's tenant.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without tenant constraint: `.where(eq(customTools.id, inserted.id))`. This may expose a row belonging to another tenant if ids collide or are guessed.
  - _Fix:_ Add tenant scoping to the query: `.where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))` so the row is verified to belong to the caller's tenant.
- **server/tools.ts:7350** — UPDATE conversations SET project_id = ${newProject.id} WHERE id = ${params._conversationId} — this UPDATE writes a tenant-scoped resource (conversations) using a caller-supplied conversation id but does NOT constrain by tenant_id or otherwise verify ownership. An attacker supplying a foreign conversation id can cause cross-tenant linkage/metadata modification (IDOR).
  - _Fix:_ Require and verify conversation ownership before mutating: either include AND tenant_id = ${tenantId} in the WHERE clause, or call a helper (e.g. assertConversationInTenant(params._conversationId, tenantId)) and abort if it fails. Always enforce tenant_id on UPDATE/DELETE against tenant-scoped tables.
- **server/tools.ts:7351** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (${newProject.id}, ${params._conversationId}) — inserting a project_conversations link using an unvalidated foreign conversation id. The handler does not verify the conversation belongs to the caller's tenant before creating the association, enabling cross-tenant linking and potential data leakage.
  - _Fix:_ Validate the referenced conversation belongs to the calling tenant before inserting the join row. Use an ownership assertion (e.g. assertConversationInTenant(params._conversationId, tenantId)) or perform the INSERT via a SELECT/INSERT that joins conversations on tenant_id = ${tenantId} so the DB enforces locality; reject the operation if validation fails.
- **server/tools.ts:10452** — SELECT from tenant-scoped table memory_entries without constraining by tenant_id. The query reads memory_entries (fact) but does not include WHERE tenant_id = <caller tenant>, so a caller could see other tenants' memory.
  - _Fix:_ Restrict the SELECT to the caller's tenant. Use the router-injected tenant id (e.g. params._tenantId or a local __tid) in the WHERE clause or via Drizzle .where(eq(memory_entries.tenant_id, tenantId)). Example: SELECT fact FROM memory_entries WHERE tenant_id = ${params._tenantId} AND persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ORDER BY created_at DESC LIMIT 5. Alternatively assert tenant context first and then include tenant_id in the query parameters.
- **server/tools.ts:16880** — UPDATE on tenant-scoped table 'browser_workflows' performs a write without constraining by tenant_id: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. This can allow cross-tenant modification if ownership isn't enforced elsewhere.
  - _Fix:_ Constrain the UPDATE by tenant id (defense-in-depth) instead of relying solely on prior SELECT. Example: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (use the same tenantId used to fetch the workflow). Alternatively verify workflow.tenant_id === tenantId before the UPDATE and include tenantId as a parameter in the WHERE clause so all writes are explicitly tenant-scoped.
- **server/tools.ts:18860** — UPDATE on tenant-scoped table 'memory_entries' uses WHERE id = ${id} without constraining tenant_id — missing tenant isolation check.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}), or use Drizzle's eq(table.tenantId, tenantId) in the WHERE clause so only the caller's tenant row(s) can be modified.
- **server/tools.ts:19033** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) lacks tenant_id constraint — can update rows across tenants by id.
  - _Fix:_ Restrict the UPDATE to the tenant's rows, e.g. UPDATE memory_entries SET ... WHERE tenant_id = ${tid} AND id = ANY(${ids}) (or compute ids already filtered to the tenant and still add tenant_id guard). Use Drizzle .where(eq(table.tenantId, tenantId), in(table.id, ids)).
- **server/tools.ts:19840** — UPDATE on tenant-scoped table 'memory_entries' uses WHERE id = ${id} with no tenant_id check — allows cross-tenant modification.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${id} AND tenant_id = ${tid}, or use Drizzle eq(table.tenantId, tenantId) alongside the id equality to enforce caller ownership.
- **server/tools.ts:19491** — db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) updates a tenant-scoped proposed_skills row by id only — missing tenant_id restriction.
  - _Fix:_ Add tenant scoping to the update (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid})) or include AND tenant_id = ${tid} in raw SQL) so updates can only affect rows owned by the caller's tenant.
- **server/tools.ts:19516** — db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) — update performed without tenant_id constraint (promote/accept path).
  - _Fix:_ Require tenant check in the WHERE clause (e.g. where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid}))). Ensure the tenantId used is the authenticated caller's tenant to prevent cross-tenant updates.
- **server/tools.ts:19533** — db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) — rejection path updates proposed_skills without tenant scoping.
  - _Fix:_ Include tenant isolation in the WHERE (e.g. WHERE id = ${row.id} AND tenant_id = ${tid}) or use Drizzle .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid})) to prevent modifying other tenants' proposals.
- **server/tools.ts:20517** — Calls getOrCreateTenantInbox(tenantId || 1) and later sendEmail using tenantId derived from params (fallback to tenant 1). A caller-controlled params._tenantId can cause reads/writes and email delivery to an arbitrary tenant's inbox (or to the admin tenant via fallback) without validating the caller's authority.
  - _Fix:_ Do not use a caller-provided tenant id to perform tenant-scoped actions. Use the tenant id from the authenticated context. If you must accept a tenant id parameter, explicitly validate that the caller is authorized for that tenant before calling getOrCreateTenantInbox/sendEmail. Remove the silent fallback to tenant 1; fail-closed when tenant context is missing or unverified.
- **server/twilio.ts:87** — SELECT from agent_knowledge (messaging_pairing) is performed without constraining by tenant_id. This reads a tenant-scoped table using only category+title, allowing a pairing lookup across tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId} or include tenantId in the query parameters) or otherwise scope reads to the resolved/expected tenant before returning pairing info. Validate ownership server-side rather than relying on an unscoped agent_knowledge lookup.
- **server/twilio.ts:102** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed with no tenant_id constraint. Reading the conversations table by id without tenant scoping violates the tenant-isolation invariant.
  - _Fix:_ Query the conversation using both id and tenant_id, e.g. WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} (or the caller's tenant). Alternatively, accept a tenantId parameter and use eq(conversations.tenantId, tenantId) in the Drizzle where clause to ensure the lookup is confined to the expected tenant.
- **server/video-job-runner.ts:178** — Upsert (insert ... onConflictDoUpdate) into videoJobs uses jobId as the conflict target but the onConflict update does not constrain the existing row by tenant_id. The update 'where' clause filters only on updatedAt, so it may update a job row regardless of tenant ownership.
  - _Fix:_ Include tenant_id in the onConflict/update WHERE clause (e.g. WHERE lt(videoJobs.updatedAt, row.updatedAt) AND eq(videoJobs.tenantId, ${row.tenantId})) or otherwise ensure upserts only affect rows belonging to the same tenant. Alternatively make the conflict target include tenant_id if appropriate.
- **server/video-job-runner.ts:202** — isCancelRequested reads videoJobs.cancelRequested using WHERE jobId = ${jobId} with no tenant_id constraint. This is an unscoped SELECT against a tenant-scoped table.
  - _Fix:_ Require the caller to supply tenantId (or resolve it) and include eq(videoJobs.tenantId, tenantId) in the where clause so cancel checks are constrained to the requesting tenant's jobs only.
- **server/video-job-runner.ts:225** — recoverStaleVideoJobs performs a db.update(videoJobs) that updates rows across all tenants (no tenant_id constraint). This global UPDATE can mark other tenants' jobs failed and leaks/modifies cross-tenant state without per-tenant scoping.
  - _Fix:_ Run stale-job recovery in a tenant-aware manner: either iterate tenants and run the update scoped to each tenant_id, or add an admin-only guard and explicitly document/limit this global operation. At minimum include tenant filtering when invoked from tenant-scoped contexts (e.g. WHERE tenant_id = ${tenantId} AND ...).
- **server/wiring-invariants.ts:180** — checkOutcomeCanary queries tenant-scoped tables (research_experiments, code_proposals, research_sessions) without constraining by tenant_id — reads may cross tenants and violate tenant isolation.
  - _Fix:_ Pass an explicit tenantId and add WHERE clauses (e.g. AND e.tenant_id = ${tenantId}, AND rs.tenant_id = ${tenantId}, AND cp.tenant_id = ${tenantId}) or run the check per-tenant. If the intent is a global admin aggregate, make that explicit and ensure the code uses a vetted admin tenant identifier and documents the cross-tenant nature.
- **server/wiring-invariants.ts:244** — checkDormantTools SELECT count(*) FROM tool_performance runs without a tenant_id filter — reads tool performance across all tenants.
  - _Fix:_ Restrict counts to a tenant (WHERE tenant_id = ${tenantId}) or iterate tenants explicitly for per-tenant health checks. If the query must be global for admin telemetry, use an explicit admin/aggregate path and make the use of a trusted admin tenant explicit.
- **server/wiring-invariants.ts:258** — everResult SELECT count(*) FROM tool_performance lacks tenant filtering, returning cross-tenant totals.
  - _Fix:_ Add tenant_id constraint or perform per-tenant aggregation; avoid silently mixing tenant data. If global totals are required, centralize them in a controlled admin query path with explicit admin credentials/intent.
- **server/wiring-invariants.ts:260** — chatResult SELECT count(*) FROM messages WHERE created_at > ... executes without tenant_id constraint — reading message counts across tenants.
  - _Fix:_ Filter messages by tenant_id (WHERE tenant_id = ${tenantId}) or run the metric per-tenant. Do not rely on implicit global queries for tenant-scoped tables.
- **server/wiring-invariants.ts:293** — usedResult SELECT DISTINCT tool_name FROM tool_performance filters by time but not tenant_id — returns data across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} or iterate tenants. If the intent is global telemetry, require and document an explicit admin path that consciously aggregates across tenants.
- **server/wiring-invariants.ts:417** — checkToolDrift aggregates from tool_performance (SUM/ MAX/ GROUP BY) without any tenant_id condition — may mix metrics across tenants and leak/contaminate tenant data.
  - _Fix:_ Include tenant_id in the WHERE/GROUP BY (e.g. WHERE tenant_id = ${tenantId} GROUP BY tool_name) or perform per-tenant aggregation. If a system-wide view is desired, implement it explicitly in an admin-only routine with audit/consent.
- **server/workflow-templates.ts:66** — Hardcoded project_id: 14 is passed into a tool invocation along with context.tenantId but there is no ownership verification — a tenant-scoped write/read using this project_id could affect a project that does not belong to context.tenantId.
  - _Fix:_ Do not pass unvalidated foreign IDs. Derive project_id from the caller's tenant, or validate that project_id belongs to context.tenantId before invoking the tool. Prefer APIs that accept only tenant-scoped identifiers or that enforce tenant ownership server-side.

### MEDIUM (35)

- **server/auto-memorize.ts:159** — Query selects messages from messages (tenant-scoped) with no tenant_id constraint (reads messages across ALL tenants). This lets the routine ingest data from all tenants without explicit per-tenant scoping.
  - _Fix:_ If the intent is per-tenant processing, add a tenant_id filter. If the intent is deliberate cross-tenant admin analytics, make this explicit in code (require admin context/authorization) and minimize exposure (sanitize/redact, record audit logs). Prefer an explicit ADMIN_TENANT_ID usage or a dedicated admin-only pipeline rather than an implicit global SELECT.
- **server/auto-memorize.ts:47** — getWatermark() selects agent_knowledge rows without scoping by tenant_id. The watermark lookup may return rows from any tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND category = ...), or clearly document and protect that the watermark is intentionally stored/queried under an admin tenant (and use ADMIN_TENANT_ID constant explicitly).
- **server/auto-memorize.ts:74** — setWatermark() inserts an agent_knowledge row with tenant_id hard-coded to 1. Writing per-tenant state into a fixed admin tenant can misattribute data and mixes tenant data under the admin tenant.
  - _Fix:_ Avoid hardcoding tenant_id. Either accept a tenantId parameter and write the watermark under that tenant, or—if this is an intentional platform-level watermark—use a clearly named flag/namespace and require that the caller is platform-admin. Document and gate admin-only writes with explicit checks.
- **server/auto-memorize.ts:246** — enqueueMemoryFact(...) is called with tenantId: 1 (hard-coded). Auto-memorize synthesised lessons from multiple tenants are being enqueued/attributed to tenant #1 (admin) rather than their origin tenant.
  - _Fix:_ Either enqueue resulting memory facts under the originating tenant (pass tenantId per fact) or intentionally isolate platform-wide facts into a clearly documented admin datastore. If attributing to admin is intended, add explicit authorization and auditing and document the design choice.
- **server/channel-routing.ts:31** — SELECT joins `channel_routes` -> `personas` with no tenant scoping: `FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id` returns persona rows without verifying tenant ownership. If `personas` rows are tenant-scoped this can surface or join across tenants.
  - _Fix:_ Make channel routing tenant-aware (add tenant_id to channel_routes) and include an explicit tenant filter in the JOIN (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = cr.tenant_id AND cr.tenant_id = <caller tenant>). If channel_routes is intentionally global, validate the persona's tenant before joining/returning persona data.
- **server/channel-routing.ts:64** — Query to read persona mapping `SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true` returns a persona_id without validating the persona's tenant. Consumers may then use that persona_id in tenant-scoped operations without verifying ownership.
  - _Fix:_ When returning a persona_id for use in tenant-scoped operations, either return only tenant-local persona ids (store tenant_id in channel_routes and filter WHERE tenant_id = <caller tenant>), or validate the persona row's tenant before handing its id to callers.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages WHERE created_at > ... (user message traffic snapshot) lacks any tenant_id filter. Aggregating global per-tenant-scoped messages risks mixing tenant signals and violates per-tenant isolation.
  - _Fix:_ If the traffic snapshot is intended to be per-tenant, add tenant_id filtering to the query. If the metric must be global/admin, make that explicit and ensure only admin callers run it; otherwise require a tenantId parameter and scope queries.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... runs without tenant_id constraint. This reads tenant-scoped tool_performance across all tenants.
  - _Fix:_ Scope the tool_performance queries to a tenant_id when computing per-tenant gates; if global system-wide metrics are required, restrict to an admin-only path and document the difference.
- **server/dormant-deprecation.ts:203** — SELECT DISTINCT tool_name FROM tool_performance executes with no tenant filtering (getEverInvokedTools). This returns tool names across tenants rather than per-tenant history.
  - _Fix:_ Add a tenant_id parameter and include AND tenant_id = ${tenantId} in the query, or ensure this admin/global operation is explicitly gated and documented.
- **server/dormant-deprecation.ts:213** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (getActiveTools14d) lacks tenant_id filtering and thus mixes activity from all tenants.
  - _Fix:_ Apply tenant scoping (tenant_id = ${tenantId}) for per-tenant computations, or require an explicit admin-only path for global aggregation.
- **server/email.ts:348** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without tenant scoping. The code checks dedupe across the shared inbox but does not constrain by tenant.
  - _Fix:_ If dedupe must be per-tenant, include tenant_id in the WHERE. If global dedupe across tenants is intentional, make it explicit and ensure only the appropriate admin/system code runs this query; otherwise determine tenant before dedupe and scope appropriately.
- **server/embeddings.ts:684** — backfillMissingKnowledgeEmbeddings SELECTs agent_knowledge rows with no tenant_id filter (SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50). This backfill runs across all tenants without explicit tenant scoping/authorization.
  - _Fix:_ Run backfill per-tenant (accept tenantId) and include AND tenant_id = ${tenantId} in queries, or perform backfill in an admin context that is explicitly documented and protected.
- **server/embeddings.ts:712** — backfillEmbeddingVecs selects memory_entries rows (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200) without tenant_id, thus touching multiple tenants' rows.
  - _Fix:_ Perform backfill per-tenant and include tenant_id = ${tenantId} in the WHERE, or gate the operation as an explicit multi-tenant admin job.
- **server/embeddings.ts:728** — backfillEmbeddingVecs selects agent_knowledge rows (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ...) without tenant_id, potentially processing rows from all tenants.
  - _Fix:_ Scope the SELECT by tenant_id or run the backfill per-tenant in a controlled/admin context; avoid unscoped scans of tenant-scoped tables.
- **server/event-bus.ts:141** — SELECT joins personas without constraining the personas table by tenant. Query: SELECT es.*, p.name as persona_name FROM event_subscriptions es JOIN personas p ON p.id = es.persona_id WHERE es.tenant_id = ${tenantId} ... — the join does not verify p.tenant_id = es.tenant_id. This can read a persona row that doesn't belong to the tenant if IDs overlap.
  - _Fix:_ Constrain the joined table by tenant as well (e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = es.tenant_id) or add WHERE p.tenant_id = ${tenantId}. Ensure all JOINs that read tenant-scoped tables enforce tenant ownership.
- **server/event-bus.ts:264** — SELECT (getEventSubscriptions) joins personas without constraining the personas table by tenant (SELECT es.*, p.name as persona_name FROM event_subscriptions es JOIN personas p ON p.id = es.persona_id WHERE es.tenant_id = ${tenantId}). The persona row is read without an explicit tenant filter.
  - _Fix:_ Include tenant scoping on the joined personas table (JOIN personas p ON p.id = es.persona_id AND p.tenant_id = es.tenant_id) or add p.tenant_id = ${tenantId} to the WHERE clause. Always ensure joins to tenant-scoped tables include tenant checks.
- **server/google-drive.ts:939** — INSERT INTO project_files is executed without setting/including a tenant_id. If project_files is a tenant-scoped table (or expected to be constrained by tenant), this INSERT lacks explicit tenant affiliation.
  - _Fix:_ If project_files is tenant-scoped, include tenant_id in the INSERT values. If the table is intentionally scoped by project_id only, ensure project_id was validated to belong to the caller's tenant before inserting and consider adding a DB constraint or inserting tenant_id redundantly to make isolation explicit (and to simplify later reads).
- **server/lib/restraint-trust.ts:88** — Uses sql.raw() to inject a column name derived from the caller-provided `outcome` (even though validated) into an UPDATE statement: `SET ${sql.raw(column)} = ${sql.raw(column)} + ${delta}`. This is a code-path that performs SQL composition from request-derived data.
  - _Fix:_ Avoid sql.raw() with request-derived values. Replace with an explicit whitelist switch/case that maps outcomes to concrete, hard-coded parameterized UPDATE statements (e.g. `if (outcome === 'action_ok') UPDATE ... SET action_alpha = action_alpha + $1 ...`). If a dynamic column is unavoidable, ensure the mapping is fully internal (no direct propagation of caller strings) and keep the whitelist+mapping logic server-side, then use parameterized queries (no sql.raw of user input).
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` has no tenant_id constraint (reads all tenants).
  - _Fix:_ If this scan is intended to be tenant-scoped, add a tenantId parameter and filter WHERE tenant_id = ${tenantId}. If it is intentionally a system-wide admin audit, document that clearly and require an explicit admin context/flag (or check current tenant context is an admin) so cross-tenant reads are explicit and auditable.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` has no tenant_id constraint (reads all tenants).
  - _Fix:_ If this scan is intended to be tenant-scoped, add a tenantId parameter and filter WHERE tenant_id = ${tenantId}. If it is intentionally a system-wide admin audit, require an explicit admin invocation or guard so cross-tenant reads are explicit and logged.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL — this query lacks tenant scoping. If the skills table is tenant-scoped, this will return skills from other tenants.
  - _Fix:_ If skills are tenant-scoped, add tenant filtering (e.g. AND (tenant_id = ${tenantId} OR persona_id IS NULL) or include tenant_id in WHERE appropriately). If skills are intentionally global, document this clearly; otherwise restrict reads to the caller's tenant.
- **server/routes.ts:1107** — /uploads middleware issues a db.select(...) FROM file_storage WHERE filename = ${baseName} without tenant_id constraint, returning owners across all tenants. Although later logic checks whether any owner matches the authenticated tenant, the initial read enumerates other tenants' owner rows and thus queries a tenant-scoped table unscoped to the caller.
  - _Fix:_ Avoid querying all tenants' rows. Instead, query for the caller's tenant first (eq(fileStorage.filename, baseName) AND eq(fileStorage.tenantId, authedTenantId)) and, if none found and a signed URL is presented, verify the signed URL separately. If a global basename collision check is required, make the logic explicit and restrict it to admin-only operations, or redact results so that no cross-tenant metadata is read unless running with explicit admin privileges.
- **server/routes.ts:651** — cleanupTestTenants runs a SELECT that uses nested SELECT DISTINCT tenant_id FROM conversations and projects without scoping those inner queries by tenant. These tenant-scoped tables are queried without tenant filters in a context that could lead to unintended cross-tenant checks/side-effects.
  - _Fix:_ If this is an admin-only maintenance routine, ensure it explicitly runs under an admin context and document it. Otherwise, constrain the inner queries or rework the cleanup so it does not scan tenant-scoped tables across all tenants (for example, check ownership using explicit admin-only APIs or perform per-tenant checks gated by admin authorization).
- **server/routes.ts:767** — backfillProjectDriveFolders selects projects (JOIN tenants) WHERE drive_folder_id IS NULL without any tenant filter, enumerating projects across all tenants for Drive-folder backfill. This reads tenant-scoped projects data unscoped to the caller.
  - _Fix:_ Run this backfill per-tenant (loop tenant IDs and run per-tenant queries scoped by tenant_id) or gate the operation to an explicit admin-only path and comment/approve the cross-tenant behavior. Prefer using admin-only database helpers that make the cross-tenant intent explicit.
- **server/routes/research.ts:105** — POST /api/research/sessions/start accepts a programId from the caller and calls startResearchSession({ programId, tenantId }) without first verifying the program row belongs to the caller's tenant.
  - _Fix:_ Before starting a session, validate ownership of the referenced program: SELECT id FROM research_programs WHERE id = <programId> AND tenant_id = <caller tenant>. Reject if not found. Alternatively, ensure startResearchSession performs and documents this tenant check server-side and returns an error if the program does not belong to the tenant.
- **server/self-improvement.ts:536** — logExperiment inserts into experiments using data.tenantId! even though tenantId is optional in the input — there's no validation that tenantId was provided, which can lead to a missing/incorrect tenant_id on insert or runtime DB errors.
  - _Fix:_ Make tenantId a required parameter for logExperiment (or validate and throw if missing). Do not use non-null assertion on an optional tenantId; explicitly validate and set the correct tenant_id value before INSERT.
- **server/skill-seeker.ts:833** — setupGapDetectionHook's handler calls detectGap with tenantId pulled from message data that may be undefined; combined with detectGap's default of tenantId=1, this can route auto-detected gaps into the admin tenant.
  - _Fix:_ Validate tenantId in the hook before calling detectGap; if tenantId is missing discard or log the event instead of defaulting to admin. Prefer propagating an explicit tenant context and fail-closed when it's absent.
- **server/storage.ts:584** — evictOldestConversationFacts updates conversation_facts using a list of victim ids with WHERE id IN (...), but the UPDATE is not explicitly tenant-scoped. Although victims were selected earlier with tenant scoping, the UPDATE lacks an explicit tenant_id guard.
  - _Fix:_ Include the tenant constraint in the UPDATE as defense-in-depth: .where(and(inArray(conversation_facts.id, victims.map(...)), eq(conversation_facts.tenantId, tenantId))). Always prefer explicit tenant filters on each write even when ids were derived from prior queries.
- **server/storage.ts:862** — getHeartbeatTask selects heartbeat_tasks by id without tenant filtering. heartbeat_tasks is tenant-scoped; returning a task by id without tenant guard can expose other tenants' tasks to callers that shouldn't see them.
  - _Fix:_ Require a tenantId parameter or otherwise scope this fetch to the caller's tenant (e.g. .where(and(eq(heartbeat_tasks.id, id), eq(heartbeat_tasks.tenantId, tenantId))). If this is intended as an admin/unscoped fetch, make that explicit and restrict its callers.
- **server/storage.ts:295** — createMessage performs an unscoped read to fetch the conversation's tenant (SELECT ... FROM conversations WHERE id = ${data.conversationId}) without a tenant filter. This is an unscoped read of a tenant-scoped table to derive tenant ownership.
  - _Fix:_ Either require the caller to pass tenantId and validate it, or call a clearly named 'getConversationUnscoped' (with explicit logging/audit) and ensure downstream callers validate the derived tenant before using it. Consider making this explicit by using storage.getConversation(id, tenantId) or asserting caller privileges.
- **server/tools.ts:20483** — Reads tenant row via storage.getTenant(tenantId) where tenantId is taken directly from params._tenantId/params. This lets a caller supply an arbitrary tenant id and read tenant-scoped metadata (e.g., email) without any server-side verification of the caller's authority over that tenant.
  - _Fix:_ Do NOT trust params._tenantId. Resolve the tenant id from the authenticated request/session context (or otherwise validate that the caller is authorized for the requested tenant) before calling storage.getTenant. If a tenant id must be accepted from clients, verify ownership explicitly (e.g., query with WHERE id = <tenantId> AND owner_user_id = <caller user>) and fail-closed if the check fails.
- **server/tools.ts:20350** — Calls enforceToolPolicy(..., { tenantId: typeof params._tenantId === "number" ? params._tenantId : 1, ... }) using params._tenantId without verifying it. Passing an unvalidated tenant id into a policy check can misattribute or bypass tenant-specific policy enforcement; defaulting to tenant 1 is also dangerous.
  - _Fix:_ Do not pass an untrusted params._tenantId into policy enforcement. Derive tenantId from the authenticated caller/session and pass that to enforceToolPolicy. Remove the silent default to tenant 1; instead require an explicit, verified tenant context and fail if absent or unverified.
- **server/wiring-invariants.ts:366** — checkSubsystemFreshness uses sql.raw to SELECT COUNT/MAX FROM ${spec.table} without a tenant_id constraint — rows for tenant-scoped subsystem tables (e.g. knowledge_nudges, tool_performance) are aggregated across tenants.
  - _Fix:_ When probing tenant-scoped subsystem tables, include tenant_id filtering or run the probe per-tenant. Avoid building sql.raw queries that return cross-tenant state unless explicitly intended and protected by an admin-only code path.
- **server/wiring-invariants.ts:535** — checkMemoryGrowth executes sql.raw SELECT COUNT/... FROM ${tbl.name} (memory tables) with no tenant_id filtering — counts (including messages and other tenant-scoped tables) are across all tenants.
  - _Fix:_ Make memory-growth checks tenant-scoped by filtering WHERE tenant_id = ${tenantId} or iterate through tenants. If global system-wide sizing is intended, require an explicit admin/aggregate path and clearly document that it reads cross-tenant data.
- **server/wiring-invariants.ts:561** — Final graph_memory probe SELECT COUNT(*) FROM graph_memory runs without tenant filtering; this reports 'empty across all tenants' but performs an unfiltered read of a tenant-scoped table.
  - _Fix:_ Either run this check per-tenant (WHERE tenant_id = ${tenantId}) or, if the check must be global, make the admin intent explicit and restrict access to admin-only code paths. Avoid implicit cross-tenant reads.

### LOW (3)

- **server/auth.ts:165** — validateApiKey() queries apiKeys without a tenant_id constraint (SELECT ... FROM apiKeys WHERE keyHash = ...). The query returns the key row and then sets req.tenantId from that row.
  - _Fix:_ This lookup is often necessary to identify the key's tenant, but ensure the apiKeys table and this code path are intended to be globally searchable. Add comments documenting the rationale and ensure rate-limiting/monitoring to prevent abuse. If you want stricter scoping, consider a two-step flow (lookup by keyHash, then validate tenant context) or additional protections (e.g. limit results, index and uniqueness, logging).
- **server/routes.ts:1599** — /api/public/stats executes SELECT count(*) FROM tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) without tenant filters, returning global platform totals. This is an unscoped read of tenant tables.
  - _Fix:_ If global aggregated, cross-tenant stats are intentionally public, keep but document that these counts aggregate across tenants and ensure this endpoint is intended to be public. Otherwise, scope counts by tenant where needed (e.g. provide per-tenant stats endpoints) and avoid raw queries that read tenant tables without restriction.
- **server/routes.ts:1627** — /api/public/architecture issues multiple db.execute COUNT queries against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) without tenant filters, exposing global counts and lists of personas.
  - _Fix:_ If platform-wide architecture stats are intended to be public, restrict sensitive details (e.g. persona names) or require admin auth for detailed exports. Otherwise, scope queries to a tenant when returning tenant-specific views. Prefer using dedicated admin-only endpoints for cross-tenant introspection and annotate them clearly.
