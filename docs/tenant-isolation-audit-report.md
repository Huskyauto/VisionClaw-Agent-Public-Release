# Tenant-Isolation Audit Report

_Generated 2026-09-04T02:57:04.938Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 472 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **190/662** file(s) audited · chunks 20/61 OK (0 failed, 41 pending)
- Findings: **82** (CRITICAL 20 · HIGH 49 · MEDIUM 11 · LOW 2)
- Severe dispositions: **69 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (20)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` is executed without a tenant_id constraint (reads experiments across all tenants).
  - _Fix:_ Constrain the query to a tenant (e.g. add AND tenant_id = ${someTenantId} or accept a tenantId parameter). If this function must operate per-tenant, iterate tenants explicitly and run a tenant-scoped query for each; do not read multi-tenant rows into a single process-wide loop.
- **server/ab-optimizer.ts:141** — INSERT into tenant-scoped table `proposedSkills` sets tenantId from a previously-read row (row.tenant_id) that was selected by an unconstrained query. This can cause cross-tenant writes derived from data read without ownership validation.
  - _Fix:_ Do not insert tenant_id derived from rows read by a tenant-agnostic query. Either (a) ensure the source SELECT is tenant-scoped, or (b) validate ownership explicitly before inserting (e.g. only process rows belonging to the caller's tenant), or require privileged/system context for any cross-tenant operation and restrict its invocation.
- **server/agentic/approvals.ts:134** — Bulk UPDATE on tenant-scoped table `agent_approvals` (expireStaleApprovals) is executed without a tenant_id constraint — it will expire pending approvals across all tenants.
  - _Fix:_ Scope expiry to a tenant or require that this function be an explicit operator action. If the intent is to run a global sweep, require explicit operator authorization and/or iterate tenants one-by-one running tenant-scoped updates; in any case ensure tenant_id is included in the WHERE clause for per-tenant operations.
- **server/agentic/resume-worker.ts:65** — Unrestricted SELECT across tenant-scoped tables: the query at line ~65 selects from agent_runs (joined to agent_approvals) with no WHERE tenant_id = <tenant> constraint. This reads tenant-scoped rows across all tenants (tenant isolation not enforced for this read).
  - _Fix:_ Constrain the query to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId}) or change the worker to iterate per-tenant and bind tenant_id as a parameter. Avoid scanning tenant-scoped tables without an explicit tenant_id predicate; ensure all reads/updates/deletes include eq(<table>.tenant_id, tenantId) (or equivalent Drizzle .where(eq(...))) so tenant isolation is enforced at the application layer.
- **server/channel-routing.ts:53** — INSERT/UPSERT into channel_routes occurs with no tenant_id scoping (INSERT INTO channel_routes (channel, persona_id) ...). If channel_routes is tenant-scoped, this allows writes that place rows into the wrong tenant or create cross-tenant mappings.
  - _Fix:_ Require caller to pass tenantId into setChannelRoute; include tenant_id in the INSERT and the ON CONFLICT key (e.g. UNIQUE(channel, tenant_id)). Validate that the provided persona_id belongs to the same tenant before inserting/updating. If channel_routes is global, explicitly enforce that persona_id points to a global persona or perform an ownership check.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' without tenant_id predicate — unclaim path uses `WHERE id = ${row.id}` allowing modification of a row regardless of tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) or perform the unclaim inside a tenant-scoped transaction/withTenantTx after validating tenantId. Always bind tenantId as a parameter; never issue cross-tenant updates without explicit tenant predicates.
- **server/email.ts:370** — SELECT from tenant-scoped table `inbox_messages` without a tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can cause cross-tenant visibility/logic errors because the check is global.
  - _Fix:_ Constrain the lookup to the relevant tenant. Resolve the tenant context first (e.g. map inboxId/from/to to a tenantId) and use WHERE tenant_id = ${tenantId} AND message_id = ${msgId} or include inbox_id/tenant_id in the predicate. If the code relies on a global uniqueness invariant, make that explicit in schema and enforce it at DB level; otherwise always add the tenant filter.
- **server/email.ts:415** — SELECT from tenant-scoped table `inbox_messages` without a tenant_id constraint (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) AND ... ORDER BY id). This scans rows across all tenants.
  - _Fix:_ Restrict the query to a specific tenant or inbox (e.g. WHERE tenant_id = ${tenantId} OR WHERE inbox_id = ${inboxId}). If this backfill is intended to be multi-tenant admin work, run it with explicit admin/maintenance tenant context and document that; otherwise perform per-tenant queries to avoid cross-tenant data access.
- **server/email.ts:428** — UPDATE against tenant-scoped table `inbox_messages` without a tenant_id constraint (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). Updating by id alone can modify rows belonging to any tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or first fetch/validate the tenant for the row before updating. Ensure backfill code carries tenant context and only updates rows for that tenant.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to executing a raw UPDATE using a runtime table name without any tenant constraint: UPDATE ${sql.raw(table)} ... WHERE id = ${id}. When tenantId is not supplied the call updates rows without tenant scoping.
  - _Fix:_ Do NOT perform global updates on tenant-scoped tables. Require and validate a positive tenantId for all updates to tenant-scoped tables and include AND tenant_id = ${tenantId} in the WHERE clause. Remove the permissive fallback branch or make it an admin-only operation that explicitly documents and requires elevated context.
- **server/google-drive.ts:1665** — INSERT INTO tenant-scoped table 'project_files' does not include a tenant_id column/value. Per project/DB design tenant_id is required for tenant-scoped tables, and omission can lead to broken invariants or cross-tenant rows.
  - _Fix:_ Include the tenant_id explicitly in the INSERT (e.g. VALUES (..., ${resolvedTenantId}, ...)) and ensure resolvedTenantId is validated via assertProjectInTenant before inserting. If project_files is intentionally tenant-less, document and review that design; otherwise mark tenant_id NOT NULL and populate it on every insert.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} performs a write to a tenant-scoped resource without any tenant_id constraint or ownership verification. This allows cross-tenant modification if sessionId is forged or ambiguous.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first SELECT the presenter_sessions row and verify its tenant_id equals the caller's tenant before performing the update. Use Drizzle's where(eq(table.tenantId, tenantId)) or equivalent to guarantee tenant-scoped updates.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) performs cross-tenant writes without constraining by tenant_id. This can archive proposals belonging to other tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant by adding AND tenant_id = ${tenantId} or resolve and include per-row tenant_id checks before updating. Better: run per-tenant archival passes (SELECT ids WITH tenant_id = X, then UPDATE WHERE id = ANY(...) AND tenant_id = X) to avoid accidental cross-tenant writes.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) performs writes on tenant-scoped rows without a tenant_id filter, allowing disabling tasks for other tenants.
  - _Fix:_ Constrain updates by tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or run the archival/disable path per-tenant after confirming ownership. Avoid global UPDATEs that affect tenant-scoped tables without explicit tenant filtering.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE on tenant-scoped table `heartbeat_tasks` (setting last_run_at/next_run_at) uses WHERE id = ${id} without constraining tenant_id. This can modify other tenants' rows by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or require the caller to pass an explicit tenantId/ADMIN marker. Alternatively, perform the update through a storage-layer function that asserts tenant ownership before updating and/or returns/validates the tenant_id in a RETURNING clause before committing changes.
- **server/heartbeat-startup.ts:76** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — write to tenant-scoped table without tenant_id constraint. This may update tasks belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${t.tenantId} or similar) or use a storage API that takes/validates tenant context before issuing the UPDATE. Avoid raw cross-tenant UPDATEs by id alone.
- **server/heartbeat-startup.ts:80** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — write to tenant-scoped table without tenant_id constraint.
  - _Fix:_ Same fix: include AND tenant_id = ${t.tenantId} (or assert tenant context) or route through a storage-layer function that enforces tenant ownership before updating.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — updates tenant-scoped table without tenant_id constraint. Can affect arbitrary task rows by id.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${newTask.tenantId}) or use a storage API that requires/validates tenant context. If using raw SQL, validate the row's tenant_id first (e.g., SELECT tenant_id FOR UPDATE) and assert it matches expected tenant before updating.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) — write to tenant-scoped table without tenant_id constraint.
  - _Fix:_ Same as above: add AND tenant_id = ${newTask.tenantId} or perform the update via a storage-layer helper that asserts the caller's tenant ownership before changing approval_status. Ensure all writes to tenant-scoped tables include tenant filters or prior ownership validation.
- **server/job-queue.ts:393** — String-building of an SQL array literal from caller-controlled excludeKinds (excludeLiteral = `{...}`) and interpolation into the query. This composes SQL text from input instead of passing a parameterized array and risks injection and isolation bypass.
  - _Fix:_ Avoid constructing SQL array literals by string concatenation. Use a parameterized SQL array (e.g. pass the array as a parameter or use drizzle's helpers to bind an array) so user input is never directly concatenated into SQL text. Ensure the exclusion clause is expressed via safe parameterization (AND kind <> ALL(${sqlArrayParam}::text[])).

### HIGH (49)

- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` (retryPendingMinervaRouting) is executed without a tenant_id constraint — the sweep reads rows across all tenants and then acts on them.
  - _Fix:_ Scope the SELECT by tenant (accept a tenantId parameter or run the sweep per-tenant) or otherwise require elevated/explicit operator privileges. If the function intentionally scans all tenants, document and gate it behind an operator-only path and add strict auditing/authorization checks.
- **server/agentic-engines.ts:247** — UPDATE ai_insights (autoApplyEligibleInsights) may run without a tenant_id constraint when called with no tenantId, performing tenant-wide updates across all tenants.
  - _Fix:_ Avoid performing status/tenant-scoped updates without an explicit tenant scope. Require a tenantId parameter or otherwise restrict this operation (e.g. run per-tenant). If a system-level bulk sweep is required, ensure it is explicitly invoked with operator privileges and audited.
- **server/agentic-features.ts:861** — SELECT against tenant-scoped table `outreach_enrollments` to detect existing enrollment omits tenant_id in the WHERE clause (may match rows from other tenants).
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause when checking for an existing enrollment to ensure the check is scoped to the caller's tenant.
- **server/agentic-features.ts:1020** — UPDATE outreach_enrollments updates a row by id but omits tenant_id in the WHERE clause (classifyReply). Even though the row was previously selected with tenant-scoped filters, the UPDATE lacks the tenant constraint, allowing accidental cross-tenant modification if ids are guessed or the row ownership changed.
  - _Fix:_ Always include tenant_id = ${params.tenantId} (or the tenant value you validated earlier) in UPDATE/DELETE WHERE clauses for tenant-scoped tables to enforce ownership at write time.
- **server/agentic/wake-scheduler.ts:141** — UPDATE on agent_wake_schedules claims/advances pending wakes using a subquery that does not filter by tenant_id. The statement (UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status='pending' AND wake_at <= now() ...)) can select and mutate rows belonging to any tenant.
  - _Fix:_ Constrain the claim/select to the intended tenant context. Either run the heartbeat per-tenant and add AND tenant_id = ${tenantId} in the inner SELECT, or add an explicit tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}) so the UPDATE can only affect rows for the target tenant. If the worker must operate across tenants, perform explicit, audited per-tenant iterations or require platform-admin authority and document the intent.
- **server/auto-project.ts:135** — SELECT from messages filters only on conversation_id and does NOT include tenant_id (messages WHERE conversation_id = ${conversationId} ...). This is a tenant-isolation risk because messages is a tenant-scoped table and the SQL relies on implicit trust of conversation_id rather than an explicit tenant filter.
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise join to/validate the conversation row in the same statement so the DB read is explicitly scoped to the caller's tenant.
- **server/auto-transcript.ts:46** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} has no tenant_id constraint. messages is tenant-scoped and the query reads rows without an explicit tenant filter.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or join messages to the conversations table and include AND c.tenant_id = ${tenantId} so the DB enforces tenant scoping in the SQL.
- **server/auto-transcript.ts:132** — Subquery uses SELECT COUNT(*) FROM messages WHERE conversation_id = c.id without constraining messages by tenant. This appears inside a global backfill scan and can return cross-tenant message counts.
  - _Fix:_ Add tenant scoping to the subquery, for example: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0, or otherwise ensure the subquery filters messages by the intended tenant_id to enforce isolation at SQL level.
- **server/auto-transcript.ts:160** — Another subquery uses (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 with no tenant constraint. This can surface messages from other tenants when scanning project_conversations/backfilling transcripts.
  - _Fix:_ Include tenant filtering in the subquery (e.g. AND tenant_id = c.tenant_id) or rewrite the query to JOIN messages with an explicit tenant_id = c.tenant_id check so reads against the tenant-scoped messages table are always constrained by tenant.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id — tenant-scoped rows must be explicitly attributed to the caller's tenant.
  - _Fix:_ Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly. Modify the invoice_items INSERT to include tenant_id (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or otherwise enforce tenant ownership (for example, add tenant_id FK on invoice_items and populate it from the surrounding transaction). Also ensure any later reads/updates on invoice_items are constrained by tenant_id (or joined to invoices with tenant_id filtering) to prevent cross-tenant leakage.
- **server/channel-routing.ts:15** — channel_routes table is created with no tenant_id column — schema provides no per-tenant scoping.
  - _Fix:_ If channel_routes should be tenant-scoped, add a tenant_id column (NOT NULL) and a multi-column UNIQUE(channel, tenant_id) or appropriate constraint; enforce tenant_id on all DML. If channel_routes is intentionally global, document that decision and ensure referenced rows (e.g. personas) are global or ownership is otherwise validated.
- **server/channel-routing.ts:30** — SELECT joins channel_routes -> personas without any tenant constraint (no WHERE tenant_id = ...). This can return/resolve personas across tenants.
  - _Fix:_ Require a tenantId parameter for listChannelRoutes and include WHERE cr.tenant_id = ${tenantId} (and/or p.tenant_id = ${tenantId}) in the query. Alternatively, verify persona ownership per-row before returning and avoid joining to tenant-scoped personas without an ownership check.
- **server/channel-routing.ts:63** — getPersonaForChannel SELECT filters only by channel/enabled and returns persona_id with no tenant constraint — can reveal or return a persona from the wrong tenant.
  - _Fix:_ Add a tenantId parameter and filter: SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId} AND enabled = true. If channel_routes is global, validate that the returned persona_id is owned by the caller's tenant before using it.
- **server/channel-routing.ts:71** — DELETE FROM channel_routes WHERE channel = ${channel} is executed with no tenant scoping — may delete routes for other tenants if table is tenant-scoped.
  - _Fix:_ Require tenantId for removeChannelRoute and include WHERE tenant_id = ${tenantId} AND channel = ${channel}. If channel_routes is intended to be global, document this and ensure callers are authorized to modify global routing entries.
- **server/cmmc-assessments.ts:150** — getCmmcAccessByToken performs a SELECT joining tenant-scoped tables (cmmcAssessmentInvitations / cmmcAssessments) using only the token hash and invitation state — there is no explicit WHERE clause constraining the read to the caller's tenant. This reads tenant-scoped rows without an explicit tenant_id filter (caller tenant is not enforced in the query).
  - _Fix:_ Either require a caller tenant and add an explicit tenant filter (e.g. WHERE cmmc_assessments.tenant_id = <callerTenant>), or if this endpoint is intended to authorize by token alone, make that policy explicit and harden it: ensure the token handling is the only auth boundary, audit its lifecycle, and avoid treating the returned rows as if scoped to an authenticated tenant. Prefer returning/using the tenant_id derived from the invitation row and explicitly applying it to all subsequent reads/writes to tenant-scoped tables.
- **server/coinbase-commerce.ts:594** — handleCoinbaseWebhook verifies the incoming webhook against the set of all configured tenant webhook secrets but does not record which tenant's secret matched. Later the code reads visionclaw_tenant_id from the webhook metadata and logs/attributes the payment to that tenant without verifying that the matched secret belongs to that tenant. This allows ambiguous attribution between a verified signature and the tenant id present in the payload.
  - _Fix:_ When verifying the HMAC, track which specific tenant secret matched (map secrets back to tenant IDs when loading them). Only accept/attribute a visionclaw_tenant_id that matches the tenant owning the secret which verified the signature. If you must test multiple secrets in one request for timing safety, still compute all HMACs but afterwards determine the matching index and map it to the tenant id atomically before trusting metadata-derived tenant ids.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' (scanAndEscalate) executes `SELECT id, tenant_id AS "tenantId", description, due_at ...` with no tenant_id filter — reads tenant data across all tenants.
  - _Fix:_ If this is intended as a global admin job, restrict use to an explicitly-authorized admin context and avoid selecting free-text fields (e.g. omit `description`) or redact them immediately. Otherwise, add a tenant_id predicate (e.g. `WHERE tenant_id = ${tenantId} ...`) or iterate per-tenant (enumerate tenants then run tenant-scoped queries). Consider using withTenantTx for tenant-scoped operations to enforce the tenant context.
- **server/debate-engine.ts:125** — Call to storage.getPersonas() reads persona data without constraining by the caller's tenant. If personas are tenant-scoped this will return cross-tenant data (no WHERE tenant_id = <tenant> filter).
  - _Fix:_ Require tenant-scoped persona access: pass the tenantId into the storage call (e.g. storage.getPersonas(tenantId) or storage.getPersonasForTenant(tenantId)) or filter the returned personas server-side to only include those whose tenant_id matches the caller. Ensure every DB read against tenant-scoped tables uses an explicit tenant_id WHERE clause (or Drizzle .where(eq(table.tenantId, tenantId))).
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table dfy_intake_forms is performed without constraining by tenant_id (lookup by token only). This allows cross-tenant access to forms by token.
  - _Fix:_ Require a tenant context when looking up intake forms or include tenant_id in the WHERE clause (e.g. WHERE token = $1 AND tenant_id = $2). If the token lookup is intentionally public, document the decision and ensure the token entropy and threat model are acceptable; otherwise restrict the query to the caller's tenant.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed without a tenant_id filter (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can affect rows belonging to other tenants.
  - _Fix:_ Add an explicit tenant filter to the DELETE (AND tenant_id = ${tenantId}) so operations on doc_chunks are scoped to the caller's tenant. Ensure every mutation against tenant-scoped tables includes tenant_id in the WHERE clause.
- **server/dormant-deprecation.ts:170** — SELECT COUNT(*) FROM messages ... is executed without scoping by tenant_id. This reads tenant-scoped messages across all tenants.
  - _Fix:_ Either add tenant_id = ${tenantId} to the WHERE clause when running tenant-specific checks, or explicitly document and isolate this code as a platform-global admin operation (and restrict who can call it). Prefer parameterized tenant-scoped queries for per-tenant behavior.
- **server/dormant-deprecation.ts:181** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance ... is executed without tenant_id filtering. This aggregates tenant-scoped tool_performance data across tenants.
  - _Fix:_ Scope the query to a tenant via WHERE tenant_id = ${tenantId} when making tenant-scoped decisions. If a global aggregation is intended, make that explicit and restrict the operation to trusted admin context.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance is executed without tenant_id filtering (getEverInvokedTools). This reads tenant-scoped tool_performance data across all tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the query to limit results to the intended tenant, or clearly separate and protect any global cross-tenant analysis operations.
- **server/dormant-deprecation.ts:215** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... is executed without tenant_id filtering (getActiveTools14d). This reads tenant-scoped tool usage across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause for per-tenant computation, or require an explicit admin/global context for cross-tenant operations and document/guard that path.
- **server/embeddings.ts:842** — Selecting memory_entries for backfill without tenant scoping: SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200. This reads rows across all tenants.
  - _Fix:_ Perform backfill per-tenant (pass a tenantId) or otherwise include a tenant filter (WHERE tenant_id = ${tenantId}). If this is an intentional global admin job, make the admin intent explicit and ensure the subsequent UPDATEs include tenant checks before mutating rows.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} performed without tenant_id constraint. Combined with the preceding global SELECT this can modify memory rows for any tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or call an update helper that validates the tenant (for example, reuse storeEmbeddingVec with a validated tenantId). Avoid updating rows by id alone when the table is tenant-scoped.
- **server/embeddings.ts:858** — Selecting agent_knowledge rows for backfill without tenant scoping: SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200. This reads rows across all tenants.
  - _Fix:_ As with memory backfill, run per-tenant or add WHERE tenant_id = ${tenantId}. If this is intended as an administrative multi-tenant operation, ensure the operation has explicit admin context and audit controls.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed without tenant_id constraint, allowing modification of agent_knowledge rows across tenants.
  - _Fix:_ Include tenant filtering in the UPDATE (AND tenant_id = ${tenantId}) or use the tenant-aware storeEmbeddingVec path (which requires a validated tenantId). Ensure every write to tenant-scoped tables is constrained to that tenant.
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot executes a SELECT on the tenant-scoped table `agent_runs` without constraining by tenant_id (no WHERE tenant_id = ...). This reads in-flight runs across all tenants, violating the invariant that every read against a tenant-scoped table must be scoped to the caller's tenant.
  - _Fix:_ Restrict the query to a specific tenant or iterate per-tenant and filter each query by tenant_id. Require an explicit admin/elevated context if truly cross-tenant rehydration is needed. Example fixes: (a) accept a tenantId parameter and run `SELECT ... FROM agent_runs WHERE tenant_id = ${tenantId} AND status IN (...) ...`, or (b) retrieve a list of tenant ids and loop: `for (const tenantId of tenantIds) { const res = await db.execute(sql`SELECT ... FROM agent_runs WHERE tenant_id = ${tenantId} AND status IN (...) ...`); ... }`. Also add an explicit admin-check/audit log if this operation must run across tenants.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' lacks an explicit tenant_id constraint in the WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the write cannot affect another tenant's rows.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' lacks an explicit tenant_id constraint in the WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to enforce tenant isolation.
- **server/google-drive.ts:867** — SELECT from tenant-scoped table 'projects' uses .where(eq(projects.id, projectId)) with no tenant_id equality check (ownership is asserted after the SELECT rather than enforced in SQL).
  - _Fix:_ Include tenant scoping in the query itself (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so reads cannot cross tenant boundaries and to keep authorization enforced at SQL-level as required by the invariant.
- **server/google-drive.ts:880** — UPDATE on tenant-scoped table 'projects' sets driveFolderId using .where(eq(projects.id, projectId)) without scoping by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) to prevent cross-tenant modifications.
- **server/google-drive.ts:1474** — SELECT from tenant-scoped table 'projects' inside uploadAndShare is performed with .where(eq(projects.id, params.projectId)) and no tenant_id WHERE clause before using project info.
  - _Fix:_ Constrain the SELECT by tenant (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or otherwise verify tenant ownership in the WHERE to avoid cross-tenant reads/logic branching before Drive calls.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant context/tenant_id. The INSERTs use only session_id/slide_index as the uniqueness key and write image blobs without asserting or persisting the owning tenant.
  - _Fix:_ Ensure tenant is set / validated on insert. Either include tenant_id in the INSERT values (and schema), or first validate the session_id belongs to the expected tenant and use a constrained INSERT (or INSERT ... SELECT) so the row is annotated with the correct tenant. Prefer using Drizzle/parametrized queries with an explicit tenantId column or a JOIN against presenter_sessions WHERE tenant_id = ${tenantId} to enforce ownership.
- **server/health-audit.ts:314** — SELECT FROM code_proposals lacks a tenant_id WHERE clause. This reads rows from a likely tenant-scoped table across all tenants.
  - _Fix:_ If this scanner is intended to operate per-tenant, enumerate tenant IDs and run the query scoped to each tenant (WHERE tenant_id = ${tenantId}). If this is an admin/global operation, make the admin intent explicit and document it, but avoid returning or processing tenant-scoped data without an explicit tenant-bound filter. Prefer queries that include tenant_id in the WHERE clause or use SQL that joins/filters by tenant_id.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks lacks a tenant_id constraint; it reads tasks across tenants without scoping.
  - _Fix:_ If the intent is a system-wide audit, make that explicit and ensure all subsequent write operations include tenant constraints. Otherwise, perform per-tenant scans (WHERE tenant_id = ${tenantId}) or enumerate tenants first and then query that tenant's heartbeat_tasks to avoid cross-tenant reads.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW() queries a tenant-scoped table across all tenants with no tenant_id filter. This returns schedules for many tenants in a single, unscoped read.
  - _Fix:_ If this is a system scheduler intended to scan all tenants, make the admin intent explicit and ensure the code performing subsequent updates includes tenant_id in WHERE clauses (the updates later do include tenant_id, but the initial SELECT is still an unscoped read). Prefer enumerating tenant IDs and then querying research_schedules per-tenant, or add tenant_id to the initial query when processing a specific tenant to avoid accidental cross-tenant exposure.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT against tenant-scoped table `heartbeat_tasks` with no tenant_id constraint (SELECT * FROM heartbeat_tasks ...). This reads rows across all tenants.
  - _Fix:_ Either restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or explicitly document/centralize this as an ADMIN-only cross-tenant operation and enforce that only a trusted scheduler path can call it. Prefer using a storage-layer helper that requires an explicit tenant context or an explicit ADMIN marker, and audit all callers.
- **server/heartbeat.ts:255** — SELECT * FROM event_log WHERE status = 'pending' ... — reads event_log rows across tenants with no tenant_id filter. event_log includes tenant_id and this SELECT fetches pending events for all tenants.
  - _Fix:_ Either scope to a tenant (WHERE tenant_id = ${tenantId}) or make this an explicit admin/scheduler operation that is clearly audited and only callable by a trusted cross-tenant scheduler. If it must be cross-tenant, ensure the code treats rows strictly with their tenant_id and enforce stricter access controls/audit logging; otherwise add the tenant filter.
- **server/job-queue.ts:100** — UPDATE/SELECT on tenant-scoped table `agent_jobs` performed without any tenant_id constraint (claimDueJobs updates pending jobs across all tenants).
  - _Fix:_ Restrict claims to a specific tenant or otherwise ensure caller is authorized for cross-tenant operations. If the worker must operate across tenants, ensure authorization is enforced at a higher level and consider selecting by tenant_id or adding an explicit tenant parameter to the claim call to avoid accidental cross-tenant effects.
- **server/job-queue.ts:135** — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 1) lacks a tenant_id constraint — terminaling running jobs across all tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or execute reclamation per-tenant. If a global reclaim is intentional, ensure this is restricted to a trusted admin-only context and audited.
- **server/job-queue.ts:149** — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 2) performed without tenant_id filtering — re-queuing expired leases across all tenants.
  - _Fix:_ Scope this UPDATE to a tenant (e.g. run per-tenant) or otherwise ensure only an authorized cross-tenant actor can run it. Prefer parameterized tenant_id conditions instead of unscoped writes.
- **server/job-queue.ts:178** — UPDATE on tenant-scoped table `agent_jobs` (completeJob) executes without constraining by tenant_id — allows completing jobs by id regardless of tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (and/or verify the caller's tenant matches the job.tenant_id before updating). Use the job's tenant to fence the write and prevent cross-tenant completes.
- **server/job-queue.ts:221** — SELECT max_attempts FROM agent_jobs WHERE id = ${id} executed without tenant scoping — reads tenant-scoped table row by id with no tenant filter.
  - _Fix:_ When reading job metadata by id in a context that is not cross-tenant admin, include tenant_id in the WHERE clause or otherwise verify the caller is authorized to inspect that job id. For admin-only APIs, require an explicit admin guard and document the exception.
- **server/job-queue.ts:238** — UPDATE on tenant-scoped table `agent_jobs` (failJob → terminal) updates by id without tenant_id constraint — can mark another tenant's job failed_terminal.
  - _Fix:_ Include tenant_id in the WHERE clause (or re-query and verify the job's tenant_id matches the expected tenant) so writes cannot affect other tenants' rows by id alone.
- **server/job-queue.ts:258** — UPDATE on tenant-scoped table `agent_jobs` (failJob → retry path) executed without tenant_id constraint — requeues jobs by id across tenants.
  - _Fix:_ Add tenant_id to the UPDATE WHERE clause or otherwise perform a tenant-checked read-then-update to ensure only the intended tenant's job rows are modified.
- **server/job-queue.ts:275** — Operator cancelJob updates `agent_jobs` by id without any tenant_id filter — risk of cancelling jobs from other tenants by id.
  - _Fix:_ Require tenant_id (or an admin-only guard) for cancel operations and include tenant_id in the WHERE clause to enforce tenant isolation for cancellation requests.
- **server/knowledge-diversity-monitor.ts:266** — SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id executed without a tenant filter — scans agent_knowledge across all tenants.
  - _Fix:_ If intended as a background multi-tenant scan, ensure this code runs only in a trusted, audited admin context. Otherwise, rework to iterate per-tenant (query tenant list then run per-tenant queries scoped with WHERE tenant_id = ${tenantId}) to keep each data access tenant-scoped and reduce blast radius.

### MEDIUM (11)

- **server/agentic-features.ts:901** — Dynamic SQL builds an ANY(...) array by string-interpolating seqIds into the SQL (`ANY(${`{${seqIds.join(",")}}`}::int[])`), which is brittle and can lead to injection/isolation risks if seqIds are not strictly validated.
  - _Fix:_ Use parameterized array binding instead of interpolating a constructed string. Example: WHERE sequence_id = ANY(${sql.array(seqIds, 'int')}) or pass seqIds as a query parameter supported by the DB driver, ensuring seqIds are validated as integers before use.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT FROM conversations scans conversations across all tenants (no tenant_id WHERE clause). This reads a tenant-scoped table without constraining to a tenant, allowing a background job to enumerate rows for other tenants.
  - _Fix:_ Either add a WHERE tenant_id = ${tenantId} (run per-tenant), or run the backfill under an explicit platform-admin context and document/guard it. Prefer enumerating tenants and scoping each SELECT to one tenant at a time to preserve the application-level tenant isolation invariant.
- **server/auto-asset-capture.ts:207** — SELECT FROM messages is executed with only conversation_id = ${c.id} and no tenant_id filter. While conversation_id was obtained earlier, the messages query itself is not explicitly tenant-scoped and thus reads a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Include an explicit tenant predicate in the messages query (e.g. AND tenant_id = ${c.tenant_id}) or join messages to conversations and assert the tenant match in the WHERE clause to ensure the DB read is explicitly constrained to the owning tenant.
- **server/auth.ts:1127** — DELETE FROM password_reset_tokens WHERE token = ${tokenHash} removes reset tokens without also restricting tenant_id. The read path (getResetToken) checks tenant_id, but this DELETE can affect rows across tenants if token formats/hashes collide or an attacker crafts tokens.
  - _Fix:_ Include tenant_id in the DELETE WHERE clause (e.g. DELETE ... WHERE token = ${tokenHash} AND tenant_id = ${embeddedTenantId}) so deletions operate only on the intended tenant's rows. Ensure all mutating operations on tenant-scoped tables include an explicit tenant predicate.
- **server/data-protection.ts:149** — SELECT in permanentlyPurgeSoftDeleted reads tenant-scoped table 'conversations' across all tenants (`SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}`) without scoping to a tenant.
  - _Fix:_ If this maintenance job must operate across tenants, ensure the code path is strictly admin-only and that any subsequent operations use the returned tenant_id to scope deletes (as already done). If not admin-only, restrict the initial SELECT to a single tenant or enumerate tenants deliberately and run tenant-scoped queries per-tenant. Avoid selecting sensitive free-text fields (e.g. `title`) unless required and ensure values are handled safely.
- **server/glasses-gateway.ts:158** — SELECT from tenant-scoped table 'apiKeys' is performed without an explicit tenant_id constraint (lookup by keyHash only).
  - _Fix:_ Either make apiKeys a global (non-tenant) lookup table explicitly, or (preferable) ensure tenant scoping is explicit where appropriate. If you must look up by keyHash, validate the returned row's tenantId and use that tenantId for downstream queries, and/or include tenant scoping where possible for subsequent updates.
- **server/glasses-gateway.ts:173** — UPDATE on tenant-scoped table 'apiKeys' (updating lastUsedAt) uses .where(eq(apiKeys.id, key.id)) with no tenant_id constraint.
  - _Fix:_ Include the tenant check in the WHERE clause (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) to ensure the update cannot be used to touch another tenant's row.
- **server/heartbeat-schedules.ts:26** — SELECT DISTINCT tenant_id FROM messages reads tenant_id values from a tenant-scoped table without an explicit tenant filter. While used here to enumerate tenants for a cross-tenant sweep, the SELECT itself is an unscoped read of a tenant-scoped table.
  - _Fix:_ Document and gate this cross-tenant enumeration as an admin/system-only operation. Alternatively, use a dedicated tenant metadata table (or an indexed tenants list) for enumeration, or clearly assert system-level context before running the query. Ensure any follow-up operations are executed per-tenant with tenant_id constraints.
- **server/job-queue.ts:317** — SELECT tenant_id FROM agent_jobs WHERE id = ${id} performed without tenant constraint in getJobTenantForAdmin. While this function's purpose is to reveal the tenant, it reads a tenant-scoped table by id with no caller-scope checks.
  - _Fix:_ Guard this admin-oriented helper so only platform-admin callers can invoke it, or require an explicit admin-context parameter. Prefer performing reads with both id AND tenant_id when used from tenant-scoped callers.
- **server/job-queue.ts:346** — Generic listJobs SELECT from `agent_jobs` will run unscoped when tenantId is omitted (the function supports listing across tenants). This performs SELECT on a tenant-scoped table without tenant_id filter in non-admin contexts if misused.
  - _Fix:_ Require an explicit tenantId for tenant-facing callers, or guard the no-tenant path behind an admin permission. If cross-tenant listing is only for admins, enforce and document that constraint and add explicit authorization checks.
- **server/job-queue.ts:420** — getJobStats executes a grouped SELECT on `agent_jobs` where tenant filtering is optional (ALL_TENANTS path leaves the query unscoped).
  - _Fix:_ Ensure the ALL_TENANTS branch is only callable by admins or otherwise restrict access. For tenant-scoped callers, always add AND tenant_id = ${tenantId} to the WHERE clause.

### LOW (2)

- **server/auth.ts:1335** — Startup migration deletes legacy auth_sessions rows with DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' without a tenant_id filter. This is a global mutation against a table that contains tenant-scoped session rows.
  - _Fix:_ If this global migration is intentional, ensure it runs only during a controlled boot where platform-admin privilege is assured and document it. Alternatively, restrict to a per-tenant cleanup (DELETE ... WHERE tenant_id = ...) or require an explicit platform-admin safety check before executing global DML.
- **server/auth.ts:1323** — clearAllSessionsAsPlatformAdmin executes DELETE FROM auth_sessions without WHERE tenant_id, revoking sessions across all tenants. While the function is intended as a platform-admin action, it performs an unscoped delete on a tenant-scoped table.
  - _Fix:_ Keep the platform-admin guard, but make the global scope explicit in code/comments and audit logs. Optionally consider deleting per-tenant in a loop or require an explicit parameter/consent token to make the global nature explicit. Ensure the caller is strictly platform-admin and that the authority check cannot be bypassed.
