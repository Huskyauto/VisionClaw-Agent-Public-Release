# Tenant-Isolation Audit Report

_Generated 2026-09-19T09:49:09.969Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **686/686** file(s) audited · chunks 64/64 OK (0 failed)
- Findings: **230** (CRITICAL 55 · HIGH 138 · MEDIUM 36 · LOW 1)
- Severe dispositions: **3 kept** · 156 precision false-positive · 34 allowlist false-positive · 0 deferred real risk

### CRITICAL (55)

- **server/agentic-features.ts:1021** — classifyReply performs an UPDATE on `outreach_enrollments` (UPDATE outreach_enrollments SET ... WHERE id = ${row.id}) without including tenant_id in the WHERE clause. This is a write to a tenant-scoped table that lacks a tenant constraint.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId} (or AND tenant_id = ${row.tenant_id}), so the update cannot affect rows outside the caller's tenant.
- **server/auth.ts:1327** — DELETE FROM auth_sessions executes a global wipe of auth sessions across all tenants (no tenant filter). While this function is intended as a platform-admin operation, the SQL itself performs an unrestricted cross-tenant mutation.
  - _Fix:_ Keep the platform-admin guard, but additionally (a) make the platform-admin authority check and intent explicit in code paths that call this function, (b) ensure the authority object is validated (already done) and thoroughly audited, and (c) add an explicit comment + audit log entry (persisted) before/after the DELETE. If this must run non-interactively in some contexts, require an explicit platform-admin flag and protect it with additional operational controls.
- **server/channel-routing.ts:53** — INSERT INTO channel_routes (...) VALUES (${channel}, ${personaId}) / ON CONFLICT ... inserts a persona_id provided by the caller without validating that the referenced persona belongs to the caller's tenant and without setting tenant_id on the new row. This allows cross-tenant association or privilege escalation.
  - _Fix:_ Do not accept a foreign personaId from the caller without server-side ownership verification. Before inserting, verify the persona row belongs to the caller's tenant (SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). Make channel_routes tenant-scoped by adding tenant_id NOT NULL and set tenant_id = ${tenantId} in the INSERT/ON CONFLICT. Use tenant_id in the conflict target/unique constraint (e.g. UNIQUE(channel, tenant_id)).
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' performed without constraining to the row's tenant (missing AND tenant_id = ... in WHERE). The statement `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` can modify a row across tenants if `row.id` is provided from an unverified source.
  - _Fix:_ Add an explicit tenant filter to the WHERE clause using the claimed row's tenant (or the validated tenantId). For example: `WHERE id = ${row.id} AND tenant_id = ${row.tenantId}` (or AND tenant_id = ${tenantId} after validating it). Prefer using the tenant value returned by the claim statement and always validate tenantId before issuing any cross-row updates. Also audit other error/rollback paths to ensure every UPDATE/DELETE/SELECT on tenant-scoped tables includes tenant_id = <caller tenant>.
- **server/doc-collections.ts:166** — DELETE FROM `doc_chunks` is executed without a tenant_id constraint (addDocument). This delete can remove chunks belonging to other tenants that share the same collection_id/doc_path.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}). Ensure addDocument receives/uses the caller's tenantId (or validate the collection belongs to that tenant before deleting).
- **server/embeddings.ts:809** — UPDATE to tenant-scoped table (`memory_entries`) performs an unconditional write when tenantId is not supplied: UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}. This allows writing a memory row without verifying tenant ownership.
  - _Fix:_ Require a positive tenantId for all writes to tenant-scoped tables. Remove the branch that performs the unconditional update without tenant filtering; either throw when tenantId is missing or require callers to call the tenant-aware code path (e.g. the branch that includes AND tenant_id = ${tenantId}). Ensure all INSERT/UPDATE/DELETE statements include tenant_id in the WHERE/VALUES as appropriate.
- **server/embeddings.ts:855** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} executed inside backfillEmbeddingVecs without tenant_id constraint. This backfill loop can modify rows across tenants.
  - _Fix:_ Perform backfill updates in a tenant-scoped manner: include tenant_id = ${row.tenant_id} (or pass the tenantId into the backfill routine) in the WHERE clause, or call the tenant-aware storeEmbeddingVec(...) path that enforces tenant ownership. Never update tenant-scoped rows by id only unless tenant ownership has been verified.
- **server/embeddings.ts:872** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed inside backfillEmbeddingVecs without tenant_id constraint. This writes tenant-scoped rows without ensuring tenant ownership.
  - _Fix:_ Include AND tenant_id = ${row.tenant_id} (or otherwise require/derive the tenantId) in the WHERE clause, or reuse the tenant-aware storeEmbeddingVec path which enforces tenant ownership. Ensure backfill queries that iterate rows also carry the tenant context or only update rows with an explicit tenant filter.
- **server/google-drive.ts:1664** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without an explicit tenant_id column value. Per invariant every INSERT into tenant-scoped tables must explicitly pass the tenantId (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (..., ${resolvedTenantId}, ...)). If the code cannot prove ownership of the project/tenant, fail-closed and do not perform the INSERT.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not set tenant_id (tenant-scoped table may require tenant_id). The INSERT uses session_id only and does not validate that the session belongs to the current tenant.
  - _Fix:_ Include the canonical tenantId on the inserted row (e.g. add tenant_id column and pass ${tenantId}) and/or verify session ownership before inserting. Example: INSERT INTO presenter_slide_images (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...) or verify session belongs to tenant_id=${tenantId} before inserting.
- **server/google-workspace.ts:2293** — INSERT into presenter_slide_images (thumb variant) does not set tenant_id and does not verify ownership of the referenced session_id.
  - _Fix:_ Same fix as above — ensure tenant_id is explicitly inserted and/or verify that session_id belongs to this tenant before persisting thumbnails. Avoid relying solely on session_id without tenant scoping.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — no tenant_id constraint. This updates a tenant-scoped resource by id only, allowing cross-tenant mutation if sessionId is unverified.
  - _Fix:_ Constrain the update to the caller's tenant. Add AND tenant_id = ${tenantId} to the WHERE clause (or use a Drizzle update with eq(presenter_sessions.tenantId, tenantId)). Alternatively, verify ownership of sessionId prior to the UPDATE and fail if it doesn't belong to tenantId.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — no tenant_id constraint. This mutates proposals across tenants by id only.
  - _Fix:_ Constrain the UPDATE to the intended tenant(s). Prefer UPDATE code_proposals SET ... WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}. If this is intended as a global admin action, require and check explicit admin privileges and document that intent.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — no tenant_id constraint. This disables tasks by id across tenants without scoping.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. AND tenant_id = ${tenantId}), or perform an ownership check for each task id before mutating. If this is intended to be a global admin action, require explicit admin authorization and document it.
- **server/memory-graph.ts:130** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} executes without constraining tenant_id. This allows a caller to increment memory_count for a category belonging to a different tenant.
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or perform the update inside a transaction that scopes the tenant (withTenantTx) and/or verify ownership of the category id before updating.
- **server/memory-graph.ts:708** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} executes without constraining tenant_id. This can modify a memory entry belonging to another tenant if the id value is forged/incorrect.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or verify the memory row belongs to the tenant before updating. Prefer using helper wrappers (withTenantTx or parameterized queries that include tenant_id) to enforce the check consistently.
- **server/memory-intelligence.ts:413** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} runs without a tenant_id predicate. This can expire/alter a knowledge_triple row owned by another tenant.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}), or verify ownership of the triple row inside the same transaction before updating. Ensure all updates/deletes on tenant-scoped tables include tenant_id checks to enforce isolation.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code verifies project ownership via assertProjectInTenant(...) but the INSERT itself omits tenant_id, violating the invariant that every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT columns/values (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${options.tenantId}, ...)). Keep the assertProjectInTenant(projectId, tenantId) check (good), but also store tenant_id on the row so DB-level queries that rely on tenant_id filters are correct and future code cannot accidentally access rows across tenants.
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table `project_files` does not include tenant_id. This is the parallel-render code path mirroring the earlier insert: project ownership is checked, but the INSERT omits tenant_id.
  - _Fix:_ Same fix as other path: add tenant_id to the INSERT columns and pass ${options.tenantId} as the value. Ensure both single and parallel code paths write tenant_id consistently so tenant-scoped rows are always labeled with the owning tenant.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} lacks a tenant_id constraint — reading a tenant-scoped personas table without scoping to the caller's tenant. This can expose other tenants' persona definitions.
  - _Fix:_ Include tenant scoping in the query, e.g. SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. Ensure personaId is validated as belonging to tenantId before exporting.
- **server/plan-executor.ts:613** — CAS claim UPDATE on plans lacks a tenant_id constraint (UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved'). This allows claiming/executing a plan across any tenant.
  - _Fix:_ Restrict the claim to the plan's tenant by including tenant_id = <caller/validated tenant> in the WHERE clause (e.g. WHERE id = ${planId} AND status = 'approved' AND tenant_id = ${callerTenantId}). If this function runs as a multi-tenant background worker, require an explicit, validated tenant parameter or run a per-tenant claim loop as an admin-only operation. Never infer tenant ownership from plan id or untrusted input.
- **server/plan-executor.ts:1149** — SELECT fetching pending approved plans (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50) has no tenant_id filter and the code subsequently kicks off executePlan(r.id) for each id. Combined with the claim UPDATE bug, this can cause cross-tenant plan execution.
  - _Fix:_ Filter pending queries by tenant_id (WHERE tenant_id = ${tenantId}) or otherwise ensure only plans belonging to an explicitly validated tenant are enqueued for execution. Do not enumerate/trigger execution on plan ids across all tenants without strict admin authorization and per-tenant scoping.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without any tenant constraint. updateProjectBrain accepts no tenantId and reads a tenant-scoped table by id only, enabling cross-tenant data access.
  - _Fix:_ Require and pass an explicit tenantId into updateProjectBrain and validate ownership. e.g. SELECT ... FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}; if the caller's tenant does not own the project, abort. Never read tenant-scoped rows by id alone from code paths that lack tenant context.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... has NO tenant filtering. This enumerates projects (and later conversations/messages) across all tenants, causing cross-tenant data exposure.
  - _Fix:_ Restrict the backfill to a specific tenant (accept a tenantId) or otherwise add WHERE p.tenant_id = ${tenantId} (and scope the joined conversation/message queries similarly). Do not scan global projects table without tenant scoping.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping. Conversations are tenant-scoped and this read can return rows for other tenants' conversations.
  - _Fix:_ Include tenant scoping on this query, e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}, or derive tenant_id from the project and enforce it before querying conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant filtering. Messages are tenant-scoped and this can leak message contents across tenants.
  - _Fix:_ Always scope message queries to the tenant: add AND tenant_id = ${tenantId} (or ensure conv.id is validated to belong to a project owned by the tenant) before reading messages. Prefer verifying ownership of the conversation/project before selecting messages.
- **server/routes.ts:187** — SELECT from tenant-scoped table `file_storage` without any tenant_id constraint (restoreUploadsFromDb startup restore). This reads raw file data for all tenants (fileStorage.data) using only filename/mime filters.
  - _Fix:_ Scope the restore to a specific tenant or iterate per-tenant. Add an explicit tenant filter (e.g. .where(and(eq(fileStorage.tenantId, tenantId), ...)) or query tenant ids first and run the restore per-tenant so every read from file_storage is constrained by tenant_id. If this is an intentional global maintenance migration, run it as a controlled offline migration executed with platform-admin privileges, not in unscoped runtime logic.
- **server/routes/projects.ts:401** — INSERT into project_conversations omits tenant_id. 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT and pass the caller's tenant (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${targetProjectId}, ${conversationId}, ${tenantId})). Validate tenant ownership before insert as well.
- **server/routes/projects.ts:569** — INSERT into project_notes omits tenant_id. 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT and pass the caller's tenant (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId})). Ensure project ownership was checked (it is) and persist tenant_id explicitly.
- **server/routes/projects.ts:609** — INSERT into project_files (base64 path) omits tenant_id. 'INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenantId used is from the authenticated session and validated against the project before inserting.
- **server/routes/projects.ts:677** — INSERT into project_files (multipart upload) omits tenant_id. 'INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenant ownership of the project has been validated prior to insertion.
- **server/routes/projects.ts:616** — UPDATE projects sets updated_at without constraining tenant_id. 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' lacks 'AND tenant_id = ${tenantId}'.
  - _Fix:_ Scope the UPDATE to the caller's tenant: 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}'. Always include tenant_id in WHERE for updates to tenant-scoped tables.
- **server/routes/projects.ts:684** — UPDATE projects sets updated_at without constraining tenant_id. 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' lacks 'AND tenant_id = ${tenantId}'.
  - _Fix:_ Scope the UPDATE to the caller's tenant: 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}'. Always include tenant_id in WHERE for updates to tenant-scoped tables.
- **server/routes/projects.ts:755** — DELETE from project_files omits tenant constraint. 'DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}' does not include tenant_id, allowing deletion of rows outside the caller's tenant if ids collide.
  - _Fix:_ Include tenant_id in the DELETE: 'DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}'. Always include tenant_id when mutating tenant-scoped tables.
- **server/sculptor.ts:105** — UPDATE on sculptor_sessions omits tenant constraint: `UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id}` updates by id only without verifying tenant_id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. `... WHERE id = ${session.id} AND tenant_id = ${params.tenantId}`) and check the affected row count to detect unexpected cross-tenant modifications.
- **server/sculptor.ts:113** — UPDATE on sculptor_sessions omits tenant constraint: `UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}` lacks tenant scoping.
  - _Fix:_ Add tenant isolation to the WHERE clause (e.g. `WHERE id = ${session.id} AND tenant_id = ${params.tenantId}`) and validate the update affected exactly one row.
- **server/sculptor.ts:168** — UPDATE on sculptor_sessions in monitorSession omits tenant constraint: `UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}` does not verify tenant ownership (monitorSession is passed tenantId but not used in this WHERE).
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the WHERE clause (or otherwise verify ownership before writing). Use RETURNING/rowCount to confirm only the intended tenant row was modified.
- **server/sculptor.ts:359** — UPDATE on sculptor_sessions sets review_result without tenant constraint: `UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}` can update a row belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${sessionId} AND tenant_id = ${tenantId}`) and verify the update affected exactly one row; accept/reject based on that verification.
- **server/second-opinion.ts:501** — settleFusionReservation updates agent_cost_ledger without scoping by tenant: `UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}` can modify another tenant's ledger row because tenant_id is not constrained and the function lacks tenant context.
  - _Fix:_ Pass tenantId into settleFusionReservation (or otherwise obtain it) and constrain the UPDATE: `WHERE id = ${reservationId} AND tenant_id = ${tenantId}`. Also verify exactly one row was updated and fail/alert if not.
- **server/seed-cleanup.ts:7** — SELECT counts heartbeat_tasks without tenant constraint: `SELECT count(*) as cnt FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'` reads across tenants (and subsequent UPDATE disables tasks globally).
  - _Fix:_ Limit the SELECT to the intended tenant (e.g. `AND tenant_id = ${ADMIN_TENANT_ID}`) or explicitly document/declare this is a global administration action against a global table. If the intent is to act only on the admin tenant, add `tenant_id = ${ADMIN_TENANT_ID}` to the WHERE and the corresponding UPDATE.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks disables delegation tasks without tenant scoping: `UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true` will affect all tenants' tasks.
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. `WHERE type = 'delegation' AND enabled = true AND tenant_id = ${ADMIN_TENANT_ID}`), or if it must be global, make table ownership explicit and review for intended global operation.
- **server/seed-cleanup.ts:44** — SELECT counts session-logger heartbeat_tasks without tenant constraint: `SELECT count(*) as cnt FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%'` reads across tenants.
  - _Fix:_ Add `AND tenant_id = ${ADMIN_TENANT_ID}` (or appropriate tenantId) to both the SELECT and the subsequent UPDATE to avoid affecting other tenants, or clearly mark the table as global if global changes are intentional.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks disables session-logger tasks without tenant scoping: `UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true` lacks tenant isolation.
  - _Fix:_ Constrain the UPDATE to the admin tenant (e.g. `AND tenant_id = ${ADMIN_TENANT_ID}`) and confirm the affected row count; if the intent is to operate globally, document and harden that intent explicitly.
- **server/seed-cleanup.ts:58** — SELECT counts various 'examine repository' heartbeat_tasks without tenant constraint: `SELECT count(*) as cnt FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... OR ...)` reads across tenants and the subsequent UPDATE disables matching tasks across all tenants.
  - _Fix:_ Add `AND tenant_id = ${ADMIN_TENANT_ID}` (or otherwise scope to the intended tenant) to the SELECT and UPDATE, or explicitly treat heartbeat_tasks as a global table and document that design decision.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks disables 'examine repository' tasks without tenant scoping: `UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true` will update rows for all tenants.
  - _Fix:_ Add tenant filtering (e.g. `AND tenant_id = ${ADMIN_TENANT_ID}`) to the UPDATE to ensure only the intended tenant's rows are modified, or change the code to explicitly manage global vs tenant-scoped semantics.
- **server/seed.ts:2240** — SELECT DISTINCT ... FROM conversations JOIN projects queries tenant-scoped tables (conversations, projects) without restricting to a specific tenant_id. The query iterates over data for all tenants and then inserts links (project_conversations) without tenant context, risking cross-tenant data mixing.
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. WHERE c.tenant_id = ${tenantId}) or iterate per-tenant (select tenant ids then run the backfill per tenant). Ensure any inserted link rows include tenant_id (or are written to a tenant-scoped table) so data cannot be mixed across tenants, and require an explicit admin/owner check if this operation must run across all tenants.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks without tenant constraint — this deletes disabled delegation tasks across all tenants (no tenant_id in WHERE), causing destructive cross-tenant modifications.
  - _Fix:_ Do not run destructive queries without tenant scoping. Either run the cleanup per-tenant (passing tenantId into the WHERE clause), or include tenant_id = ${tenantId} in the DELETE. If system-wide deletion is intentional, document and implement an explicit ALL_TENANTS-safe API in storage with proper authorization checks instead of an unscoped DELETE.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs WHERE created_at < ... with NO tenant constraint — this bulk delete removes heartbeat logs for all tenants.
  - _Fix:_ Scope deletions to a tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...) and run per-tenant, or implement a controlled, audited system-level purge API that explicitly authorizes cross-tenant operations. Avoid unscoped destructive SQL against tenant-scoped tables.
- **server/storage.ts:937** — UPDATE on tenant-scoped table `memory_entries` (update(memoryEntries).set({ embedding }).where(eq(memoryEntries.id, id))) lacks tenant scoping — no tenant_id constraint.
  - _Fix:_ Require and validate the tenantId for this operation and include it in the WHERE clause (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId))) or use requiredTenantScope). Failing to scope updates by tenant allows modifying other tenants' rows.
- **server/storage.ts:1409** — getAllDataForExport performs broad SELECTs from many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with NO tenant_id constraints, exporting data for every tenant.
  - _Fix:_ Restrict export to a single tenant OR require an explicit admin/ALL_TENANTS sentinel and strong authorization. Prefer an API that takes tenantId and uses requiredTenantScope on each tenant-scoped table, or implement an explicit, well-audited admin export flow and ensure it's gated to authorized admin contexts only.
- **server/tools.ts:2806** — INSERT into project_conversations omits tenant_id. The insert: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` does not set tenant_id on a tenant-scoped relationship table, allowing cross-tenant linkage/contamination.
  - _Fix:_ Include tenant_id in the insert (and value it from the caller's tenantId). Example: `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ON CONFLICT DO NOTHING`. Also ensure any reads against project_conversations include WHERE tenant_id = ${tenantId}.
- **server/tools.ts:2932** — INSERT into project_files does not include tenant_id. The INSERT: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` omits tenant_id, so new file rows may not be tenant-scoped.
  - _Fix:_ Add tenant_id to the columns and set it from the caller context: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by, tenant_id) VALUES (..., ${tenantId}) RETURNING *`. Ensure project_files has tenant_id NOT NULL and all writes always set it.
- **server/tools.ts:2946** — INSERT into project_notes omits tenant_id. The INSERT: `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id, enabling tenant cross-contamination in notes.
  - _Fix:_ Include tenant_id in the insert, e.g. `INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author || 'agent'}, ${tenantId}) RETURNING *`. Also ensure subsequent reads/updates on project_notes are filtered by tenant_id.
- **server/tools.ts:2970** — INSERT into project_conversations (second occurrence) omits tenant_id. The code inserts `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` without tenant scoping when linking a conversation to a project.
  - _Fix:_ As above, persist tenant_id with the row: `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})`. Also ensure any reads/joins over project_conversations enforce `tenant_id = ${tenantId}`.
- **server/tools.ts:7006** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the inserted columns. (No tenant_id column/value in INSERT INTO project_files ... VALUES (...)).
  - _Fix:_ Include tenant_id explicitly in the INSERT. For example, insert tenant_id = params._tenantId (or use Drizzle's insert API with the tenantId field). Even though ownership of project_id is asserted earlier, you must still persist tenant_id on the row (columns are NOT NULL with no default) and avoid relying on implicit guarantees. Use parameterized Drizzle insert or sql`...` with ${params._tenantId} to avoid injection.

### HIGH (138)

- **server/ab-optimizer.ts:110** — runDueAbExperiments executes a SELECT on the tenant-scoped `experiments` table without scoping the query to a tenant (no WHERE tenant_id = ...). This returns rows across all tenants.
  - _Fix:_ Restrict the query to a tenant (e.g. add WHERE tenant_id = ${tenantId}) or, if the intent is a global sweep, explicitly iterate tenants and run per-tenant queries. Ensure any subsequent use of returned rows enforces tenant ownership before acting.
- **server/agentic-engines.ts:325** — retryPendingMinervaRouting executes a SELECT on the tenant-scoped `ai_insights` table without a tenant filter (WHERE tenant_id = ... is missing), returning insights across tenants.
  - _Fix:_ Scope the SELECT by tenant (add AND tenant_id = ${tenantId} when called with a tenant), or change the API to iterate tenants and validate tenant_id for each row before acting. Avoid operating on rows from other tenants.
- **server/agentic/approvals.ts:135** — expireStaleApprovals issues a bulk UPDATE on the tenant-scoped `agent_approvals` table without a tenant filter (UPDATE ... WHERE status = 'pending' AND expiresAt < ...), updating rows across all tenants.
  - _Fix:_ Scope the bulk expiry to a tenant (accept a tenantId parameter and add AND tenant_id = ${tenantId}), or explicitly iterate tenants and run a per-tenant expiry to ensure no accidental cross-tenant writes occur.
- **server/agentic/department-budgets.ts:143** — Query selects from tenant-scoped table `department_budgets` without constraining by tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads across all tenants and violates the project invariant that every read/write to tenant-scoped tables must include a tenant_id filter.
  - _Fix:_ Restrict the query to a single tenant or make the multi-tenant intent explicit and enforce an admin-only guard. Options: (a) change the function to accept a tenantId and add WHERE tenant_id = ${tenantId}; (b) if this is meant to be an admin/global sweep, require an explicit ADMIN context and document that the caller must be an authorized system job (or use a well-audited ADMIN_TENANT_ID constant) and add an authorization check before running the query. Avoid un-scoped reads across tenant tables in code paths callable by non-admin contexts.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT on tenant-scoped table `agent_runs` (raw SQL at lines ~65-90) — the query returns rows across tenants because it lacks a WHERE tenant_id = <tenant> constraint. This allows cross-tenant reads/processing of tenant-scoped data and violates the invariant that every read against a tenant-scoped table must be constrained to the caller's tenant.
  - _Fix:_ Restrict the query to a single tenant (or iterate per-tenant) so every read is scoped. For example, accept/derive a tenantId and add AND r.tenant_id = ${tenantId} to the WHERE clause (or use Drizzle helpers: .where(eq(agentRuns.tenantId, tenantId))). If this worker genuinely must scan all tenants, make that explicit and ensure only an authorized admin process with audited intent runs it; otherwise avoid scanning without tenant scoping.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table `agent_wake_schedules` performs a claim (and SELECT subquery) with no tenant_id filter — the query updates/returns rows across all tenants.
  - _Fix:_ Either (a) run this job per-tenant (accept a tenantId param) and add WHERE tenant_id = ${tenantId}, or (b) explicitly document+gate this as a privileged global maintenance worker and ensure it runs only in a trusted platform context. Prefer iterating tenants and claiming rows scoped to each tenant (or include tenant_id in the outer/subquery WHERE clause) to preserve tenant isolation.
- **server/auto-project.ts:135** — SELECT from tenant-scoped table `messages` is missing an explicit tenant filter. The query `SELECT role, LEFT(content, 300) as content FROM messages WHERE conversation_id = ${conversationId} ...` does not constrain rows by tenant_id.
  - _Fix:_ Constrain the messages read to the caller's tenant. For example: add `AND tenant_id = ${tenantId}` to the WHERE clause (or join through conversations and require `m.tenant_id = ${tenantId}`). Do not rely on previously-read conversation rows without also applying an explicit tenant-level filter on this query.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table `messages` is missing an explicit tenant filter. The query `SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ...` does not include `tenant_id`.
  - _Fix:_ Restrict the messages query to the tenant, e.g. `WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}` or join to conversations and ensure `messages.tenant_id = ${tenantId}`. Even after asserting conversation ownership, every query to a tenant-scoped table must include an application-level tenant filter.
- **server/auto-transcript.ts:128** — Backfill query uses a subquery against tenant-scoped table `messages` without a tenant constraint: `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`. This can read message counts across tenants.
  - _Fix:_ Include tenant scoping inside the subquery, e.g. `(SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) > 0`, or otherwise add an explicit tenant filter to ensure the messages read are limited to the conversation's tenant.
- **server/auto-transcript.ts:153** — Linked-conversations query includes a subquery against `messages` without tenant scoping: `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`. This can count messages across tenants.
  - _Fix:_ Add tenant scoping to the subquery, e.g. `(SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) > 0`, or otherwise ensure the messages check explicitly filters by the correct tenant to enforce isolation.
- **server/business-tools.ts:96** — INSERT into invoice_items does not set tenant_id. The invoice_items INSERT uses only invoice_id and item fields (INSERT INTO invoice_items (invoice_id, ...) VALUES (${newId}, ...)) so if invoice_items is a tenant-scoped table (tenant_id NOT NULL) new rows will not be tied to the caller's tenant — allowing cross-tenant data leakage or orphaned rows.
  - _Fix:_ Include tenant_id on invoice_items (and pass the current tenant id into the INSERT). e.g. INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). If invoice_items is intentionally global, document that and ensure all reads/join logic enforce the invoice -> invoice_items tenant relationship consistently.
- **server/channel-routing.ts:30** — SELECT joins channel_routes -> personas and returns persona_name without any tenant filtering. If personas is tenant-scoped, this leaks or mixes persona rows across tenants because there's no WHERE p.tenant_id = <tenant> (or equivalent).
  - _Fix:_ Make channel_routes tenant-scoped (add tenant_id NOT NULL and a UNIQUE(channel, tenant_id) index) and include tenant_id in the SELECT WHERE clause. Alternatively, if channel_routes is intentionally global, filter the joined personas by tenant (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}) and only return rows for the caller's tenant. Always enforce tenant ownership on any join to a tenant-scoped table.
- **server/channel-routing.ts:51** — DELETE FROM channel_routes WHERE channel = ${channel} executes without any tenant constraint. This can delete or affect other tenants' routing records if channel_routes should be tenant-scoped.
  - _Fix:_ Require and pass a tenantId, add tenant_id to channel_routes, and restrict deletes to that tenant (DELETE ... WHERE channel = ${channel} AND tenant_id = ${tenantId}). If channel_routes is per-tenant, change schema to include tenant_id NOT NULL and include it in all DML.
- **server/channel-routing.ts:63** — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true returns persona_id without scoping by tenant. If channel_routes is tenant-scoped, this read may return another tenant's mapping.
  - _Fix:_ Require tenantId and restrict the query by tenant (WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId}). If channel_routes is intended to be global, revisit design to avoid exposing tenant-scoped persona IDs; otherwise add tenant_id to the table and queries.
- **server/channel-routing.ts:71** — DELETE FROM channel_routes WHERE channel = ${channel} (removeChannelRoute) runs without tenant constraint and will remove mappings across tenants if the table is not tenant-scoped.
  - _Fix:_ Require tenantId in removeChannelRoute and delete only the caller's tenant mapping (DELETE ... WHERE channel = ${channel} AND tenant_id = ${tenantId}). Prefer making channel_routes tenant-scoped (tenant_id NOT NULL) so all CRUD operations must include tenant filtering.
- **server/chat-engine.ts:3805** — Call to capability-review.markReviewReused(conversationId, toolName) passes only conversationId (a foreign identifier) and toolName — no tenantId is provided. If markReviewReused reads/writes DB rows keyed by conversationId, that call may perform tenant-scoped access without enforcing tenant_id filtering, allowing cross-tenant reads/writes or missing tenant_id on inserts.
  - _Fix:_ Include tenantId in the call (e.g. markReviewReused(tenantId, conversationId, toolName)) or ensure markReviewReused immediately validates ownership by checking the conversation's tenant matches the caller. All DB reads/writes inside markReviewReused must include WHERE tenant_id = ? (or Drizzle eq(table.tenantId, tenantId)) and all INSERTs must explicitly set tenant_id.
- **server/chat-engine.ts:3926** — Call to stuck-diagnostics.recordErrorForRetryStormDetection(conversationId, ...) is invoked with only conversationId and error text — no tenantId is provided. If that function persists or queries diagnostic/storm records by conversationId without tenant scoping, tenant isolation may be bypassed.
  - _Fix:_ Pass tenantId into recordErrorForRetryStormDetection (e.g. recordErrorForRetryStormDetection(tenantId, conversationId, ...)) or have the function validate that the conversation belongs to the tenant before any read/write. Ensure all DB queries use WHERE tenant_id = ? and all INSERTs include tenant_id explicitly.
- **server/chat-engine.ts:4161** — Call to stuck-diagnostics.recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) does not include tenantId. If the module reads/writes per-conversation diagnostic/state rows, it may perform operations without tenant_id constraints, violating tenant isolation.
  - _Fix:_ Include tenantId in the call (e.g. recordToolCallForStuckDetection(tenantId, conversationId, ...)) or ensure the called function first verifies that the conversation belongs to the tenant and uses tenant-scoped WHERE clauses for all queries and sets tenant_id on any INSERTs. Audit stuck-diagnostics for DB access using conversationId-only keys and fix to require tenant scoping.
- **server/chat-engine/project-context.ts:44** — Subquery reads project_conversations (pc) without constraining it to the caller's tenant (EXISTS(...) uses pc.conversation_id and pc.project_id but no tenant filter). This can allow cross-tenant linking via project_conversations.
  - _Fix:_ Add an explicit tenant restriction on project_conversations (e.g. AND pc.tenant_id = ${tenantId}), or otherwise ensure pc rows are filtered by tenant (or join via a tenant-scoped projects row). Do not rely on p.tenant_id alone — apply a WHERE/AND on pc.tenant_id.
- **server/chat-engine/project-context.ts:23** — Query selecting from project_conversations (pc) JOIN projects (p) WHERE pc.conversation_id = ${conversationId} does not constrain pc by tenant_id. project_conversations appears tenant-scoped but is not filtered — risk of returning cross-tenant project_id.
  - _Fix:_ Include tenant scoping for pc, e.g. WHERE pc.conversation_id = ${conversationId} AND pc.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}, or join via a tenant-scoped join condition eq(pc.tenant_id, ${tenantId}). Ensure every tenant-scoped table in the query has an explicit tenant_id filter.
- **server/chat-engine/project-context.ts:159** — Selecting project_files by project_id only (SELECT ... FROM project_files WHERE project_id = ${projectId}) has no tenant_id constraint. There is no DB-enforced FK/tenant guard, so this read may leak files across tenants if project_id mapping is not perfectly enforced at DB level.
  - _Fix:_ Also filter project_files by tenant_id: e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}. Alternatively, join projects and require p.tenant_id = ${tenantId} in the same query, but still include an explicit tenant filter on project_files per the project's isolation invariant.
- **server/chat-engine/project-context.ts:171** — Selecting project_notes filtered only by project_id (SELECT note,... FROM project_notes WHERE project_id = ${projectId}) lacks tenant_id constraint and can return notes from other tenants for the same project_id value if DB foreign keys/cascades are not enforcing tenant ownership.
  - _Fix:_ Add tenant scoping to project_notes queries, e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}, or validate project ownership explicitly before reading notes.
- **server/chat-engine/project-context.ts:181** — Query joining project_conversations (pc) and conversations (c) uses pc.project_id = ${projectId} but does not constrain pc by tenant_id (only c.tenant_id is checked). The project_conversations table is read without an explicit tenant filter.
  - _Fix:_ Constrain project_conversations by tenant: add AND pc.tenant_id = ${tenantId} (or include eq(pc.tenant_id, ${tenantId}) in the where). Ensure every tenant-scoped table referenced in the join includes an explicit tenant_id check rather than relying on the other joined table's tenant filter.
- **server/data-protection.ts:149** — SELECT from tenant-scoped table `conversations` lacks a tenant_id predicate. The query `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` will read rows across all tenants (tenant_id not constrained).
  - _Fix:_ Restrict the SELECT to a single tenant or otherwise ensure explicit admin authorization. Options: (1) run the purge per-tenant and add `AND tenant_id = ${tenantId}` to the WHERE clause; (2) require and validate an explicit admin context before running cross-tenant queries; or (3) use a tenant-bound transaction (withTenantTx) so RLS enforces isolation. Avoid any unfiltered reads of tenant-scoped tables.
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table `dfy_intake_forms` without constraining by tenant_id (getDfyIntakeByToken). This returns a form by token across all tenants.
  - _Fix:_ Require the caller to supply the tenantId (or otherwise derive it from authenticated context) and add a tenant filter to the WHERE clause (e.g. AND eq(dfyIntakeForms.tenantId, tenantId)). If the design intentionally allows token-only global lookup, explicitly document and enforce that the table/column is global (no tenant_id) instead of having tenant_id populated.
- **server/dormant-deprecation.ts:180** — SELECT COUNT(DISTINCT tool_name) FROM `tool_performance` lacks tenant scoping (getTrafficSnapshot). `tool_performance` is used elsewhere as tenant-scoped; this returns cross-tenant data and can break tenant isolation.
  - _Fix:_ Make the function tenant-aware and add WHERE tenant_id = ${tenantId} (or pass tenantId into the function). If this logic is intended to be global, ensure `tool_performance` is truly global and does not carry tenant_id, or move platform-global aggregation to a designated global-only path.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM `tool_performance` (getEverInvokedTools) is executed with no tenant filter. `tool_performance` elsewhere is tenant-scoped, so this leaks/uses data across tenants.
  - _Fix:_ Scope the query by tenant (e.g. SELECT DISTINCT tool_name FROM tool_performance WHERE tenant_id = ${tenantId}) and accept a tenantId parameter. If this is intended to be a platform-global report, ensure the table schema and intent are global and explicitly documented.
- **server/dormant-deprecation.ts:215** — SELECT DISTINCT tool_name FROM `tool_performance` WHERE last_success_at ... (getActiveTools14d) lacks a tenant filter. This returns active tools across all tenants rather than for a specific tenant.
  - _Fix:_ If this should be per-tenant, accept/derive tenantId and add WHERE tenant_id = ${tenantId}. If global aggregation is required, ensure `tool_performance` is deliberately global (no tenant_id) or run these global aggregations via a dedicated global path that does not touch tenant-scoped rows.
- **server/email.ts:370** — SELECT from tenant-scoped table `inbox_messages` without constraining tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This allows cross-tenant reads/logic based on a row from the wrong tenant.
  - _Fix:_ Include the tenant context in the WHERE clause (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1) or otherwise ensure the lookup is intentionally global and safe. If this check is intended to be tenant-scoped, require and pass the tenantId to the function and filter by it.
- **server/email.ts:415** — SELECT from tenant-scoped table `inbox_messages` without tenant filter (SELECT id, message_id FROM inbox_messages WHERE ...). This iterates rows across all tenants.
  - _Fix:_ Restrict the query to a tenant (e.g. WHERE tenant_id = ${tenantId} AND ...). If this job intentionally runs globally, make that explicit and ensure operator safeguards are acceptable; otherwise accept a tenantId parameter and filter.
- **server/email.ts:429** — UPDATE against tenant-scoped table `inbox_messages` without tenant filter (UPDATE inbox_messages SET ... WHERE id = ${row.id}). A write by id alone can modify rows belonging to other tenants if IDs collide or attacker-controlled IDs are used.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise derive tenantId for each row before updating. Do not perform tenant-scoped writes by id-only unless you have another guaranteed per-row tenant check.
- **server/embeddings.ts:259** — Use of sql.raw() with a vector literal derived from user/model input in a query against tenant-scoped table `memory_entries` (embedding vector literal injected via sql.raw(`'${vec}'::vector`)). Using sql.raw on request-derived/model-derived data is injection-prone and bypasses Drizzle parameterization.
  - _Fix:_ Avoid embedding unescaped string literals into raw SQL. Prefer fully-parameterized queries or a safe driver-level binding for pgvector values. If you must interpolate a vector literal, strictly validate/sanitize the numeric array (already done by vecLiteral) and centralize this conversion in a single audited helper; better: pass the embedding as a parameter and use a DB-side function to construct/compare vectors or use prepared statements that accept numeric arrays and cast server-side.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` without any tenant_id filter (reads rows across all tenants). This rehydrate routine performs a cross-tenant read of in-flight runs and then materializes per-tenant in-memory state, violating the requirement that every read against a tenant-scoped table be constrained to the caller's tenant.
  - _Fix:_ Restrict the query to a specific tenant or otherwise ensure the operation is performed with explicit admin authorization. Options: (1) add a WHERE tenant_id = ${tenantId} clause and run rehydration per-tenant (driven by an authorized list), (2) perform rehydration only for tenants explicitly passed/authorized to the function, or (3) move this boot-time rehydration into an admin-only process with appropriate access controls and auditing. Avoid global, unfiltered SELECTs on tenant-scoped tables.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) but does not include a tenant_id constraint. Writes to tenant-scoped tables must always be scoped to the caller's tenant in the WHERE clause.
  - _Fix:_ Include the tenant filter in the UPDATE predicate (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the update is constrained to the authenticated tenant. Alternatively, use the row's tenantId from the previously-selected row and assert it matches caller tenant before updating, but prefer adding the tenant equality to the WHERE to satisfy the invariant.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant_id constraint (in crawl loop). This allows a write that is not explicitly scoped to the caller's tenant.
  - _Fix:_ Add the tenant constraint to the WHERE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or include tenantId in the update predicate so the update can only affect rows belonging to the caller's tenant.
- **server/google-drive.ts:866** — SELECT from tenant-scoped table 'projects' is performed with WHERE eq(projects.id, projectId) with no tenant_id constraint. Per policy every read against a tenant-scoped table must be constrained to the caller's tenant via tenant_id in the WHERE clause.
  - _Fix:_ Add a tenant filter to the SELECT (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, expectedTenantId)))) or otherwise fail if the project does not belong to the caller before proceeding. Prefer including the tenant equality in the DB WHERE so the query is tenant-scoped.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) but omits tenant_id in the WHERE clause. Writes must include tenant scoping to prevent cross-tenant modification.
  - _Fix:_ Include the tenant constraint in the UPDATE predicate (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) to ensure only the owning tenant's project row is modified.
- **server/google-drive.ts:1473** — SELECT from tenant-scoped table 'projects' performed with WHERE eq(projects.id, params.projectId) and no tenant_id constraint before using project data. Although a tenant-ownership check is later performed, the DB read itself is not tenant-scoped via WHERE tenant_id = ... as required by the invariant.
  - _Fix:_ Scope the SELECT by tenant (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the initial lookup is constrained to the caller's tenant. If that is not possible, immediately verify tenant ownership and fail before any side-effects; better is to include the tenant equality in the WHERE clause.
- **server/health-audit.ts:314** — SELECT FROM code_proposals is executed without a tenant_id filter. This reads pending/unverified code proposals across all tenants.
  - _Fix:_ If this audit is intended to run per-tenant, include WHERE tenant_id = ${tenantId} and accept tenantId in the caller context. If it is an admin/global audit, make the admin intent explicit (e.g. require an admin flag and document it). Avoid silently scanning tenant-scoped tables globally without explicit admin authorization.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks is executed without tenant_id filtering. The query lists enabled tasks that never ran across the entire DB.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, include tenant_id = ${tenantId} and run per-tenant (or require explicit admin intent). If the intent is to run a global admin sweep, require explicit admin checks and make that explicit in code/comments.
- **server/heartbeat.ts:2679** — UPDATE on tenant-scoped table heartbeat_tasks executed without tenant_id constraint: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}. This updates approval state without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or, better, use storage.updateHeartbeatTask/newTask metadata so the tenant context is applied server-side. Ensure any direct SQL writes include tenant_id in the WHERE to enforce isolation.
- **server/heartbeat.ts:3152** — UPDATE on tenant-scoped table heartbeat_tasks executed without tenant_id constraint: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}. This write occurs in a chat-driven handler and lacks explicit tenant scoping.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call the storage wrapper that enforces tenant ownership. All writes coming from request/caller contexts must include the caller's tenant_id to prevent cross-tenant modification.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without scoping by tenant_id — it claims pending jobs across all tenants.
  - _Fix:_ Restrict claims to a specific tenant (e.g. pass tenantId into claimDueJobs and add WHERE tenant_id = ${tenantId}) or perform per-tenant claiming loops. If global claiming is required, add an explicit, auditable justification and ensure the callsite runs with appropriate platform privileges and logging.
- **server/job-queue.ts:196** — reclaimExpiredLeases updates agent_jobs (marking terminal/requeued) without any tenant_id WHERE clause — affects rows across all tenants.
  - _Fix:_ Make reclaimExpiredLeases tenant-scoped (accept a tenantId and include AND tenant_id = ${tenantId} in the UPDATEs) or iterate tenants and run the reclaim per-tenant. If global reclaim is necessary, limit scope to system-owned jobs or add explicit admin-only guard and audit logging.
- **server/job-queue.ts:238** — completeJob updates agent_jobs status to 'succeeded' by id/attempts but does not include tenant_id in the WHERE clause, allowing cross-tenant completion if an id is abused.
  - _Fix:_ Add tenant fencing to completeJob (accept tenantId or tenant_id from the job context) and include AND tenant_id = ${tenantId} in the WHERE clause, or otherwise verify the caller/worker is authorized to modify that tenant's job before updating.
- **server/job-queue.ts:281** — failJob reads max_attempts and later updates agent_jobs without constraining by tenant_id (SELECT and UPDATE are unscoped), enabling cross-tenant mutation by id.
  - _Fix:_ Require/accept tenantId in failJob or re-read the row with a tenant constraint before deciding retry vs terminal, and include AND tenant_id = ${tenantId} in the UPDATE to enforce tenant isolation. Avoid relying solely on id/attempts for fencing without tenant scoping.
- **server/job-queue.ts:336** — cancelJob updates agent_jobs status by id without a tenant_id filter, allowing cancellation of jobs across tenants by id.
  - _Fix:_ Require a tenantId parameter for cancelJob and include AND tenant_id = ${tenantId} in the WHERE clause, or otherwise verify operator privileges and log the action when performing cross-tenant cancellations.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes a SELECT grouping agent_knowledge by tenant_id/persona_id (scanning all tenants) and subsequently loads embeddings per tenant — the initial SELECT touches tenant-scoped data without restricting to a caller tenant.
  - _Fix:_ If this is an intentional system-wide diagnostic, ensure it runs under a tightly-controlled admin context and is auditable. If tenant-scoped execution is desired, iterate per-tenant from a trusted tenant list and use WHERE tenant_id = ${tenantId} on loadEmbeddings and other mutations. Avoid leaving unscoped reads/writes against tenant tables in code paths callable from tenant-scoped contexts.
- **server/lib/action-ledger.ts:268** — SELECT from tenant-scoped table `action_attempts` in listUnknownAttemptsAllTenants() has no tenant_id constraint (cross-tenant read).
  - _Fix:_ If this is intended only for a reconciler/maintenance job, document and restrict execution to an admin-only context; otherwise add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) so the query is tenant-scoped. Consider an explicit 'all-tenants' API name and additional authorization guard if cross-tenant access is required.
- **server/lib/action-ledger.ts:304** — UPDATE of `action_attempts` in sweepStaleAttempts() does not constrain by tenant_id and will modify rows across tenants.
  - _Fix:_ Either scope the sweep to a specific tenant (add tenant_id = ${tenantId} to the WHERE) or ensure this cross-tenant operation runs only under a tightly controlled, audited maintenance identity. Prefer explicit tenant-scoped updates when possible to avoid accidental cross-tenant writes.
- **server/lib/agent-trace.ts:125** — UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} (persistSpanClose) lacks a tenant_id constraint — allows closing/updating spans without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) and pass the tenant context into persistSpanClose. If span_id is globally unique but tenant checks are still required by policy, ensure the function validates the current tenant before performing the UPDATE.
- **server/lib/ideabrowser-backfill.ts:49** — Unscoped read over tenant-scoped `projects` table: the aggregate query SELECT tenant_id, count(*) ... FROM projects has no WHERE tenant_id = <...> filter and thus reads counts across all tenants.
  - _Fix:_ Scope this aggregation to a specific tenant or perform the logic per-tenant. If the intention is to decide whether to backfill a particular tenant, change the query to include WHERE tenant_id = ${tenantId} (or run the query inside a per-tenant loop). If a multi-tenant global aggregation is required, move it to an explicit admin-only path and document the reason; ensure the code path cannot be invoked using an unprivileged caller's context.
- **server/lib/mcp-api-keys.ts:166** — SELECT against tenant-scoped table mcp_api_keys is done WITHOUT a tenant_id filter (lookup by key_prefix only). This reads a tenant-scoped table without constraining to a tenant, violating the invariant that every tenant-scoped read must be pinned by tenant_id.
  - _Fix:_ Avoid querying a tenant-scoped table without a tenant filter. Options: (1) Make key_prefix a global, unique lookup table (prefix → tenant_id) that is not tenant-scoped and query that; or (2) change the verification flow so the lookup is performed against a global index/table or using a parameterized mapping, then use the resolved tenant_id for any subsequent tenant-scoped queries. If you intend key_prefix to be globally unique, document/enforce that invariant at the schema level and move the prefix-lookup out of a tenant-scoped table so the initial lookup is not a tenant-scoped read.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions runs a SELECT on the tenant-scoped table `deliverable_grade_events` without constraining by tenant_id (reads across all tenants). This violates the invariant that every read against a tenant-scoped table must be constrained to the caller's tenant and risks cross-tenant data exposure.
  - _Fix:_ Scope the aggregation to a single tenant per invocation (e.g. loop tenants and run the query with WHERE tenant_id = <tenant>) or otherwise perform per-tenant queries. e.g. SELECT ... FROM deliverable_grade_events WHERE tenant_id = ${tenantId} AND created_at > ... GROUP BY format. If this cross-tenant scan is intentional for an admin job, ensure it runs under an explicit admin context and is clearly documented/authorized rather than left as an unconstrained application query.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a request-derived value (the chosen Beta column name) in the UPDATE statement. Although the code whitelists column names earlier, passing a runtime-derived value into sql.raw() is a SQL-injection/isolation risk and violates the rule forbidding sql.raw() or string-interpolated SQL with request-derived values.
  - _Fix:_ Avoid using sql.raw() with any request-derived input. Replace the dynamic-column UPDATE with an explicit, fixed query per outcome (e.g. switch on outcome and run one of four parameterized UPDATE statements that reference trust_scores.action_alpha, action_beta, restraint_alpha, restraint_beta directly), or use the Drizzle API to reference columns (table.column) rather than raw SQL. Keep the tenant_id/persona_id/category WHERE clause as-is. If a mapping is required, perform the whitelist-to-static-SQL mapping server-side and never pass an unchecked runtime string into sql.raw().
- **server/lib/stuck-task-sweep.ts:56** — SELECT against tenant-scoped table `agent_runs` is executed without a tenant_id WHERE clause (queries all tenants).
  - _Fix:_ Restrict the query to a tenant by adding a tenant_id predicate (e.g. add WHERE tenant_id = ${tenantId} or accept a tenantId parameter). If the cross-tenant scan is intentional, explicitly gate this function behind a documented admin-only context and strong auditing/authorization and clearly state that behavior in code comments; avoid silent multi-tenant reads in regular runtime paths.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against tenant-scoped table `mind_tickets` is executed without a tenant_id WHERE clause (queries all tenants).
  - _Fix:_ Restrict the query to a tenant by adding a tenant_id predicate (e.g. add WHERE tenant_id = ${tenantId} or accept a tenantId parameter). If a cross-tenant audit is required, require explicit admin authorization and audit logging, and document the cross-tenant behavior; otherwise enforce per-tenant filtering to preserve tenant isolation.
- **server/lib/system-probes.ts:163** — Unscoped SELECT against tenant-scoped table `messagesTable` — db.select(...).from(messagesTable) executes without a WHERE tenant_id = ... filter, violating tenant isolation invariant.
  - _Fix:_ Constrain this query to an explicit tenant context (e.g. add .where(eq(messagesTable.tenantId, tenantId)) or WHERE tenant_id = ${tenantId}) or, if the intent is an admin-wide aggregate, call a clearly-audited admin-only function that explicitly documents and enforces that cross-tenant aggregation. Avoid raw unscoped selects on tenant-scoped tables; require explicit ALL_TENANTS handling and authorization checks before performing cross-tenant reads.
- **server/minds-engine.ts:501** — Query reads from tenant-scoped table `messages` without an explicit tenant_id constraint. The SELECT: `SELECT m.role, m.content FROM messages m JOIN conversations c ON c.id = m.conversation_id AND c.tenant_id = ${tenantId} WHERE m.conversation_id = ${run.conversationId}` does not include `m.tenant_id = ${tenantId}` (or equivalent), violating the project invariant that every access to tenant-scoped tables must include an application-level tenant filter.
  - _Fix:_ Constrain the messages read to the caller's tenant explicitly. For example add `AND m.tenant_id = ${tenantId}` to the JOIN/WHERE or otherwise verify the conversation/messages row ownership before selecting. Alternatively fetch/validate the conversation row for the given conversationId and tenantId first, then query messages for that conversation while also filtering on m.tenant_id = tenantId to ensure full defense-in-depth.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table `oauth_subscriptions` without tenant_id filter: this startup validation queries id, provider, tenant_id, expires_at, refresh_token, consecutive_failures across all tenants (WHERE is_active = TRUE). Reading sensitive columns (refresh_token) across tenants risks leaking or mis-scoping tenant data.
  - _Fix:_ Restrict the query to the specific tenant(s) intended, or gate this logic to an explicit, well-audited admin-only path. If cross-tenant iteration is required for maintenance, avoid selecting sensitive columns (like refresh_token) unless strictly necessary and ensure the operation runs under a documented admin-only context and audit trail. Alternatively, fetch tenant IDs from a safe admin API and process tenants one-by-one with tenant_id in the WHERE clause.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' (no tenant_id filter). This reads rows across tenants to build a tenant list — a cross-tenant read on a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ If the intent is to enumerate tenants for an admin maintenance task, document & gate the function as admin-only. Otherwise, restrict the query to a single tenant context. Prefer an explicit list of tenant IDs from a safe source rather than scanning oauth_subscriptions across tenants without authorization.
- **server/oauth-subscriptions.ts:1258** — SELECT FROM tenant-scoped table `oauth_subscriptions` without tenant_id filter in proactiveTokenRefresh: it queries provider, tenant_id, expires_at, is_active, consecutive_failures WHERE is_active = TRUE (global scan across tenants).
  - _Fix:_ Avoid unbounded cross-tenant scans. If proactive refresh must run for all tenants, ensure the routine is explicitly admin-only and minimize sensitive columns returned. Prefer iterating a vetted tenant list and performing per-tenant queries with WHERE tenant_id = ${tenantId} to keep each DB access tenant-scoped.
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE in forceRefreshAllSubscriptions performs a global scan of a tenant-scoped table without tenant scoping. This enumerates tenants and then refreshes tokens across them.
  - _Fix:_ Require an explicit admin guard for forceRefreshAllSubscriptions, and/or change the implementation to iterate a pre-authorized tenant list and perform per-tenant queries that include WHERE tenant_id = ${tenantId}. If a global scan is necessary, limit returned columns to non-sensitive fields and ensure only admin callers may invoke this code.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw() with a value derived from caller-controlled ledgerKey/tenantId when invoking pg_advisory_xact_lock (possible SQL injection / unsafe interpolation).
  - _Fix:_ Avoid sql.raw() with request-derived data. Pass the lock key as a parameterized value (bound parameter) and validate it first. Example: await tx.execute(sql`SELECT pg_advisory_xact_lock(${lockKey}::bigint)`); or use $1 parameter binding. Also validate/limit ledgerKey input before deriving the lock key.
- **server/orchestrator-ledger.ts:466** — UPDATE against tenant-scoped table memory_entries does not constrain the update by tenant_id (WHERE clause is only WHERE id = ${row.id}). Per-invariant, every write against tenant-scoped tables must include tenant filtering.
  - _Fix:_ Add the tenant constraint to the UPDATE to ensure tenant isolation, e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer parameterized queries and keep the tenant_id check even if the row was selected earlier.
- **server/pace-control.ts:85** — SELECT against heartbeat_logs in checkPace() has no tenant_id filter; this reads a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Require or accept a tenantId in checkPace() and include AND tenant_id = ${tenantId} in the WHERE clause so counts are isolated per tenant. If this is intentionally global, document it and ensure heartbeat_logs is truly global/shared.
- **server/pace-control.ts:140** — SELECT against heartbeat_logs in getPaceSnapshot() has no tenant_id filter; dashboard/debug snapshot will surface cross-tenant data.
  - _Fix:_ Either accept a tenantId parameter and filter WHERE tenant_id = ${tenantId}, or explicitly document and enforce that this query is for global admin only. Prefer explicit tenant scoping for multi-tenant deployments.
- **server/pace-control.ts:147** — SELECT grouping by persona_name in getPaceSnapshot() queries heartbeat_logs without tenant scoping (no tenant_id in WHERE).
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (and add tenantId parameter to the function) so the per-tenant persona breakdown cannot leak other tenants' usage.
- **server/persona-export.ts:39** — SELECT from skills: `SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ...` lacks tenant_id filtering. As written it may return skills from other tenants.
  - _Fix:_ Restrict skills to the tenant by adding tenant filter, e.g. WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR persona_id IS NULL. Ensure tenant_id is included when fetching tenant-scoped rows.
- **server/plan-executor.ts:1024** — SELECT scanning for stale 'executing' plans (SELECT id, tenant_id, status, plan_json, execution_log FROM plans WHERE status = 'executing' AND updated_at < ...) has no tenant_id filter and enumerates rows across all tenants.
  - _Fix:_ If this is intended to be per-tenant, add a tenant_id = ${tenantId} constraint or iterate tenants explicitly. If this is an admin/global recovery path, make the cross-tenant intent explicit, limit scope (e.g. admin-only, audit log), and avoid returning/operating on rows without an explicit, validated admin authority. Prefer per-tenant queries or require an admin-authenticated tenant parameter.
- **server/plan-executor.ts:1090** — SELECT searching for recent transientFailures (SELECT ... FROM plans WHERE status='failed' AND ceo_decision='approved' ... ) lacks a tenant_id constraint and may read/consider failed plans from all tenants.
  - _Fix:_ Constrain this query by tenant_id (WHERE tenant_id = ${tenantId}) or make the admin/global nature explicit and perform necessary authorization checks before enumerating cross-tenant rows. Avoid any broad tenant-less SELECTs on tenant-scoped tables.
- **server/platform-admin-conversations.ts:18** — Cross-tenant SELECT from conversations is performed without a tenant_id constraint (db.select().from(conversations).where(and(eq(conversations.id, conversationId), isNull(conversations.deletedAt))). Although guarded by isPlatformAdmin(req), the SQL itself reads a tenant-scoped table without tenant scoping.
  - _Fix:_ Either add an explicit tenant_id = ${tenantId} filter when the caller supplies/requests a tenant, or, if true cross-tenant admin access is required, centralize and document that this boundary is admin-only and ensure a strong admin authorization check occurs prior to the SQL. Prefer making tenant intent explicit (e.g. pass tenantId and include it in the WHERE) to avoid accidental cross-tenant leakage.
- **server/process-governor.ts:898** — UPDATE to agent_desks uses WHERE id = ${desk.id} with NO tenant filter. This mutates a tenant-scoped table by id only and can update rows belonging to other tenants if ids collide or are global.
  - _Fix:_ Include tenant scoping on the UPDATE. Use WHERE id = ${desk.id} AND tenant_id = ${tenantId} (or WHERE id = ${desk.id} AND tenant_id = ad.tenant_id if available) to ensure the update only touches the caller's tenant rows.
- **server/process-governor.ts:1235** — UPDATE governance_rules increments trigger_count using WHERE id = ${rule.id} but does not include tenant_id = ${tenantId}. This allows cross-tenant modification of governance rules by id.
  - _Fix:_ Scope the UPDATE to the tenant. Change the WHERE clause to: WHERE id = ${rule.id} AND tenant_id = ${tenantId} (logAction already receives tenantId so pass and use it in the query).
- **server/recurring-messages.ts:265** — Unscoped SELECT against tenant-scoped table `agent_knowledge` in `_runDueInner`. The heartbeat runner issues `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` with no tenant_id constraint, returning rows across all tenants.
  - _Fix:_ Do not scan tenant-scoped tables without scoping by tenant. Change the runner to query per-tenant (e.g. fetch tenant ids then run `SELECT ... WHERE tenant_id = <tid> AND category = 'recurring_message' LIMIT N`), or add an explicit, internal-only guard and logging/auditing. Ensure any global scan is limited, documented, and authorized, and avoid returning sensitive columns when scanning across tenants.
- **server/recurring-messages.ts:307** — UPDATE to tenant-scoped table `agent_knowledge` lacks tenant constraint. The update `UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}` does not include `AND tenant_id = <tenant>` allowing a write by id that is not additionally restricted to the owning tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause for writes, e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}` (or otherwise validate ownership before the update). This prevents accidental or malicious cross-tenant modifications when id values are reused or mis-bound.
- **server/reference-learner.ts:296** — UPDATE to tenant-scoped table `memory_entries` missing tenant constraint. The code runs `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}` without verifying tenant_id/persona_id ownership.
  - _Fix:_ Add tenant (and persona) scoping to the update: `WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}` (or verify ownership earlier and include tenant_id in the WHERE). Always include tenant_id on UPDATE/DELETE for tenant-scoped tables to enforce isolation at the SQL layer.
- **server/research-engine.ts:171** — UPDATE against tenant-scoped table 'research_sessions' performed without any tenant_id constraint (cleanupZombieSessions uses a global UPDATE to mark running sessions as completed). This can modify rows across tenants, violating tenant isolation.
  - _Fix:_ Constrain the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or, if this is intended as an admin-global maintenance operation, require an explicit trusted/admin context and perform the operation per-tenant (iterate tenant ids) so app-level tenant isolation rules are preserved and accidental cross-tenant changes cannot occur.
- **server/research-engine.ts:460** — INSERT into project_notes omits tenant_id. If project_notes is a tenant-scoped table (tenant_id NOT NULL), this write either fails or risks misattributing/omitting the tenant context.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and bind it from the verified owner tenant). For example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...). Also ensure ownership was verified (assertProjectInTenant was already used earlier) before inserting.
- **server/research-engine.ts:468** — INSERT into project_notes (within the findings loop) does not set tenant_id. Same risk as above — writes into a tenant-scoped table without the caller's tenant context.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. VALUES (${session.tenantId}, ${projectId}, ${note}, ...)). Ensure project ownership verification (assertProjectInTenant) is applied before these inserts so the tenant is authoritative.
- **server/routes.ts:476** — UPDATE on tenant-scoped table `order_lookup_codes` runs without qualifying tenant_id for the caller: `UPDATE order_lookup_codes SET tenant_id = ${ownerTenantId()} WHERE tenant_id IS NULL`. This mutates rows across tenants in a blanket way.
  - _Fix:_ Avoid runtime blanket updates against tenant-scoped data. Run this as an explicit, platform-admin migration script (separate from request/handler code) or otherwise scope it to a known tenant context. If it must run in-process, enumerate and validate the target tenant(s) and include WHERE tenant_id = <explicit tenant> (or run per-tenant updates) so every write to a tenant-scoped table carries an explicit tenant_id constraint.
- **server/routes.ts:485** — DELETE on tenant-scoped table `order_lookup_codes` performed without tenant scoping: `DELETE FROM order_lookup_codes WHERE expires_at < NOW()`. This removes rows for all tenants rather than only the intended tenant context.
  - _Fix:_ Scope pruning by tenant (e.g. delete per-tenant with WHERE tenant_id = <tid> AND expires_at < NOW()) or iterate tenants and prune per-tenant. If global pruning is required, perform it as an offline/admin migration with documented platform-admin intent; otherwise always include an explicit tenant_id constraint on DELETEs against tenant-scoped tables.
- **server/routes.ts:626** — SELECT from tenant-scoped table `projects` (joined to tenants) without a tenant_id WHERE clause: the startup backfill query scans all projects (`SELECT p.id, p.name, p.tenant_id ... WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = ''`).
  - _Fix:_ When operating against tenant-scoped tables do per-tenant processing. Either add an explicit tenant filter (eq(p.tenant_id, tenantId)) and call the backfill for each tenant, or ensure this code runs as a controlled migration under platform-admin/operator supervision. Prefer explicit per-tenant loops so every read is constrained by tenant_id in application code.
- **server/routes.ts:978** — SELECT from tenant-scoped table `file_storage` without tenant filter in the uploads ownership-check middleware: all owner rows for a basename are fetched (`.from(fileStorage).where(eq(fileStorage.filename, baseName))`) and then ownership checked in application logic. This reads across tenants.
  - _Fix:_ Avoid returning rows for other tenants. Replace the single unscoped SELECT with tenant-scoped checks: (1) a query that checks existence for the authenticated tenant (eq(filename, baseName) AND eq(tenant_id, authedTenantId)); (2) a separate existence check for any owner if strictly needed, returning only booleans (not full rows), or perform a controlled two-step check that never materializes other tenants' data. If the intent is to support signed capability URLs that are global, consider storing signed-delivery metadata in a non-tenant-scoped capability table or include the tenant in the signed capability and filter by tenant_id.
- **server/routes.ts:1926** — SELECT from tenant-scoped table `presenter_sessions` by token with NO tenant_id constraint (GET /api/presenter/:token). Reading presenter_sessions without tenant scoping relies entirely on the token capability and returns tenant-scoped rows unchecked by tenant_id.
  - _Fix:_ Either require the caller to supply the tenant context (and validate it) or include tenant in the signed token/URL so the SELECT can include WHERE tenant_id = <tenant>. Alternatively, move capability tokens into a separate non-tenant-scoped lookup table (capability -> session_id + tenant_id) and ensure any read against presenter_sessions itself is constrained by tenant_id. Ensure every SELECT/UPDATE/DELETE on presenter_sessions includes an explicit tenant filter or is performed after verifying ownership.
- **server/routes.ts:4652** — INSERT into tenant-scoped table (project_notes) does not set tenant_id. The code inserts project notes via raw SQL: INSERT INTO project_notes (project_id, note, author) VALUES (...) without including tenant_id, violating the invariant that tenant-scoped writes must include the caller's tenant.
  - _Fix:_ Include the tenant_id column and bind the caller's tenant value in the INSERT (or call a storage helper that enforces tenant scoping). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Also ensure projId is owned by the tenant (ownership was checked for the conversation earlier, but add an explicit project ownership check or derive tenantId from the verified conversation) and prefer using a storage layer function (e.g. storage.createProjectNote(projectId, note, author, tenantId)) to centralize tenant enforcement.
- **server/routes/admin.ts:910** — Query against `heartbeat_tasks` (SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3 ...) has no tenant_id constraint, but `heartbeat_tasks` is used elsewhere as tenant-scoped (queries/deletes with tenant_id). This endpoint therefore reads heartbeat tasks across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant by adding WHERE tenant_id = ${tenantId} (or explicitly include tenant_id = ${ADMIN_TENANT_ID} when the caller is the platform admin and cross-tenant access is intended). If cross-tenant inspection is required, ensure the route is strictly platform-admin gated (use requirePlatformAdmin) and log/audit access; otherwise enforce tenant-scoped filters to maintain isolation.
- **server/routes/conversations.ts:119** — UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} executes without constraining by tenant_id. This updates a tenant-scoped table (conversations) using only the conversation id, allowing cross-tenant modification if an attacker can supply or guess conv.id.
  - _Fix:_ Include the tenant check in the UPDATE (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or use a tenant-scoped helper (storage.updateConversation) that enforces tenant ownership. Consider doing the update inside a transaction and/or SELECT ... FOR UPDATE to avoid TOCTOU races.
- **server/routes/conversations.ts:123** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id}) does not set tenant_id. If project_conversations is tenant-scoped (likely), this both violates the schema expectation and allows cross-tenant associations or SQL errors.
  - _Fix:_ Explicitly include tenant_id in the INSERT, e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}). Ensure the tenant value used is the authenticated caller's tenant and/or verify ownership of project_id before inserting.
- **server/routes/conversations.ts:125** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is performed without a tenant_id constraint. Projects are tenant-scoped elsewhere in the code, so this write can affect another tenant's project rows.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}. Prefer doing writes only after verifying ownership and within a transaction if multiple related writes occur.
- **server/routes/conversations.ts:254** — INSERT INTO project_notes (project_id, note, author) VALUES (...) is executed without a tenant_id column/value. project_notes appears to be tenant-scoped elsewhere, so this INSERT writes tenant-scoped data with no tenant_id, risking cross-tenant data leakage or DB constraint failures.
  - _Fix:_ Include tenant_id in the INSERT, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'}). Alternatively, derive and validate tenant from the project row and use that tenant value in the INSERT.
- **server/routes/inbox-notifications.ts:101** — UPDATE statement marks a message read without scoping by tenant_id (missing tenant predicate). This allows an authenticated tenant to change rows belonging to other tenants by ID.
  - _Fix:_ Add tenant scoping to the UPDATE so the row is only modified if it belongs to the caller's tenant. For example, include AND tenant_id = ${tenantId}: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`); or use a storage-layer helper that accepts tenantId to enforce ownership. Ensure any similar UPDATEs/DELETEs always include tenant_id predicates.
- **server/routes/projects.ts:165** — Subqueries counting related rows (project_files, project_notes, project_conversations) do not constrain the tenant on the child tables. E.g. '(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)' lacks 'AND tenant_id = ${tenantId}'.
  - _Fix:_ Add tenant scoping to the subqueries so counts only reflect rows for the caller's tenant, e.g. '(SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId})'.
- **server/routes/projects.ts:703** — SELECT from project_files omits tenant constraint. 'SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC' does not filter by tenant_id and can leak cross-tenant rows if project_files rows with that project_id exist for other tenants.
  - _Fix:_ Add tenant scoping to the SELECT: 'SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC'. Even when the parent project was checked earlier, explicitly constrain child table reads by tenant_id as defense-in-depth.
- **server/routes/projects.ts:726** — SELECT file record for download omits tenant constraint. 'SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1' does not include tenant_id.
  - _Fix:_ Include tenant_id in the WHERE: '... WHERE project_id = ${projectId} AND file_path = ${...} AND tenant_id = ${tenantId} LIMIT 1'. This prevents cross-tenant file records from being accessed if present.
- **server/seed.ts:42** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped but the query aggregates across all tenants.
  - _Fix:_ Filter the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId} or use Drizzle: .where(eq(research_sessions.tenantId, tenantId))). If an aggregate across all tenants is truly required, perform it only with an explicit admin/owner privilege check and document the reason.
- **server/seed.ts:3639** — DELETE from tenant-scoped table `heartbeat_tasks` executed without constraining by tenant_id. This DELETE will run across all tenants because there's no WHERE tenant_id = ... clause.
  - _Fix:_ Add a tenant filter to the DELETE (e.g. AND tenant_id = <tenantId>) or explicitly restrict the operation to the platform owner (ADMIN_TENANT_ID) if intended. Alternatively, iterate per-tenant and perform the cleanup for each tenant separately to preserve tenant isolation.
- **server/seed.ts:3667** — UPDATE on tenant-scoped table `memory_entries` (REPLACE on fact) executed without a tenant_id constraint. This will modify rows for all tenants.
  - _Fix:_ Restrict the UPDATE with a tenant_id filter (e.g. AND tenant_id = <tenantId>) or run the normalization per-tenant. If the change is intended only for the platform owner, explicitly include tenant_id = ADMIN_TENANT_ID in the WHERE clause.
- **server/seed.ts:3693** — UPDATE on tenant-scoped table `conversations` (setting model) executed without a tenant_id constraint, affecting all tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = <tenantId> (or loop per-tenant) so only the intended tenant(s) are updated. If it's intended as a global platform-level migration, perform the operation in a way that verifies and documents that cross-tenant changes are safe (or target only ADMIN_TENANT_ID).
- **server/self-improvement.ts:434** — UPDATE on tenant-scoped table `experiments` lacks a tenant_id constraint. The code updates an experiment by id only: .where(eq(experiments.id, experiment.id)) — this allows a row to be modified across tenants if ids collide or in case of incorrect context.
  - _Fix:_ Include the tenant constraint in the WHERE clause (or use a Drizzle eq on experiments.tenantId). Example: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Ensure the tenantId used is the authenticated caller's tenant and not an untrusted input.
- **server/self-reflection.ts:382** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = <id> without also constraining tenant_id. Although the id was discovered via a prior tenant-scoped SELECT, the UPDATE should still enforce tenant scoping to prevent race/TOCTOU or misuse.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tenantId}. Prefer multi-column WHERE checks rather than relying on a prior lookup alone.
- **server/skillify.ts:91** — SELECT from messages without tenant constraint — recentMsgs are queried by conversationId but the query omits tenant_id, allowing potential cross-tenant disclosure if conversationId is misused.
  - _Fix:_ Include the tenant filter on messages (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)) or SQL WHERE tenant_id = ${tenantId}) so reads are strictly scoped to the caller's tenant.
- **server/skillify.ts:201** — SELECT from messages without tenant constraint in analyzeConversation — messages for a conversation are fetched without constraining tenant_id.
  - _Fix:_ Constrain the messages query by tenant (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)) or add WHERE tenant_id = ${tenantId}). Even if the conversation row was previously validated, every tenant-scoped table access must include the tenant filter.
- **server/skillify.ts:221** — SELECT from personas by id with NO tenant constraint — personaRows are looked up using persona id without verifying the persona belongs to the caller's tenant.
  - _Fix:_ Constrain the personas query with tenant ownership (e.g. .where(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)) or otherwise verify persona belongs to the same tenant before trusting its data.)
- **server/skillify.ts:329** — SELECT from messages without tenant constraint — recentMsgs fetched for skillifyConversation lack tenant scoping.
  - _Fix:_ Add tenant filtering to the messages SELECT (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) to ensure message reads are tenant-scoped.
- **server/skills-marketplace.ts:209** — SELECT id FROM skills WHERE name = ${template.name} with NO tenant filter — may return or treat another tenant's installed skill as existing (cross-tenant lookup).
  - _Fix:_ If skills are tenant-scoped, require a tenantId and add WHERE tenant_id = ${tenantId} to the query (or otherwise confirm skills is intentionally global and document that explicitly). Do not perform unscoped lookups on tenant-scoped tables.
- **server/skills-marketplace.ts:235** — SELECT * FROM skills WHERE id = ${skillId} with NO tenant constraint — exporting a skill by id can leak another tenant's skill data if skills is tenant-scoped.
  - _Fix:_ Require the caller's tenant context and constrain the query with WHERE tenant_id = ${tenantId} AND id = ${skillId}, or otherwise make explicit that skills is a global table and safe to read without tenant scoping.
- **server/stability-watchdog.ts:177** — SELECT FROM heartbeat_logs for error counts lacks tenant constraint — query aggregates error counts across all tenants, then acts based on results, which may disclose cross-tenant data or trigger cross-tenant remediation.
  - _Fix:_ Either restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) per-run, or explicitly iterate per-tenant and run tenant-scoped queries. If the watchdog intentionally runs global maintenance, make the design explicit and ensure it's safe to operate across tenants.
- **server/storage.ts:300** — createMessage reads the referenced conversation by id without validating the caller's tenant owns that conversation (no tenant_id predicate on the SELECT).
  - _Fix:_ When accepting a foreign id (conversationId) from the caller, verify ownership before using it. Query conversations with both id and tenant_id (or assert the conversation belongs to the caller's tenant via a guard like assertConversationInTenant). Do not accept a conversationId and then insert/update rows based solely on that id without verifying tenant ownership.
- **server/storage.ts:959** — SELECT on tenant-scoped table `memory_entries` (getMemoriesWithoutEmbeddings) returns rows across all tenants with no tenant_id filter.
  - _Fix:_ If this is intended to be an admin/global job, require an explicit ALL_TENANTS sentinel or an authorized admin context; otherwise accept a tenant scope parameter and use requiredTenantScope(memoryEntries.tenantId, tenantId) (or tenantScope) to limit results to a tenant. Make the all-tenant intent explicit in the function signature and code.
- **server/storage.ts:969** — SELECT on tenant-scoped table `agent_knowledge` (getKnowledgeWithoutEmbeddings) runs without any tenant_id constraint, exposing knowledge rows for all tenants.
  - _Fix:_ Either require an explicit ALL_TENANTS sentinel for global background jobs or add a tenantId parameter and use requiredTenantScope(agentKnowledge.tenantId, tenantId) so queries are scoped. If truly intended as a global admin read, document and gate it behind an admin-only path and an explicit ALL_TENANTS argument.
- **server/storage.ts:979** — UPDATE statement in archiveExpiredMemories updates `memory_entries` across all tenants (no tenant_id constraint).
  - _Fix:_ Make tenant scope explicit. If archiving must be tenant-scoped, add a tenantId parameter and include WHERE tenant_id = ${tenantId}. If performing a global maintenance task, require explicit admin authorization and an ALL_TENANTS sentinel, and document that this is an intentional global operation.
- **server/storage.ts:995** — UPDATE statement in archiveStaleMemories updates `memory_entries` without any tenant_id filter (applies to all tenants).
  - _Fix:_ Same remediation as archiveExpiredMemories: require explicit tenant scoping via a tenant parameter and requiredTenantScope, or gate the global update behind an explicit admin/ALL_TENANTS sentinel and authorization.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table 'felix_proposals' without constraining tenant_id in WHERE clause (stampArgsEmbedding updates args_embedding by id only). This allows a row from another tenant (or ambiguity) to be modified.
  - _Fix:_ Include tenant scoping in the UPDATE. Pass tenantId into stampArgsEmbedding and change the statement to: UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL. Prefer parameterized drizzle/drizzle-orm usage to ensure tenant filter is always present.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table 'custom_tools' uses WHERE id = <tool.id> only (setting implementation) with no tenant_id constraint. This updates a row by id without verifying tenant ownership in SQL.
  - _Fix:_ Add tenant constraint to the UPDATE: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). If the tool.id was just created in the same flow, ensure the routine carries the tenantId and always includes it in WHERE for updates.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table 'custom_tools' (revertToolRow) is performed with WHERE id = <tool.id> only and lacks a tenant_id constraint. Deleting by id without tenant scoping can affect the wrong tenant row if ids are ever misaligned.
  - _Fix:_ Constrain deletes by tenant_id as well: db.delete(customTools).where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure revertToolRow receives/derives the tenantId of the tool being reverted and uses it in the WHERE clause.
- **server/tool-learning.ts:365** — UPDATE on tenant-scoped table 'custom_tools' increments usageCount using WHERE id = <tool.id> only, without tenant_id in the WHERE clause. This mutates a tenant-scoped row without an explicit tenant constraint.
  - _Fix:_ Include tenant scoping on the UPDATE: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the caller passes tenantId and that all updates/deletes/selects on tenant-scoped tables include tenant_id filters.
- **server/tools.ts:2926** — SELECT against project_files lacks an explicit tenant filter. The duplicate-check query `SELECT id FROM project_files WHERE project_id = ${params.id} ...` does not constrain by tenant_id (even though project ownership was checked earlier), violating the invariant that every read on tenant-scoped tables must include tenant scoping.
  - _Fix:_ Constrain the SELECT to the caller's tenant, e.g. `SELECT id FROM project_files WHERE project_id = ${params.id} AND tenant_id = ${tenantId} ...`, or JOIN through projects and require `p.tenant_id = ${tenantId}` (the latter pattern is used elsewhere in this module). Ensure all reads on project_files include the tenant filter.
- **server/tools.ts:4546** — INSERT into tenant-scoped table `project_notes` without passing tenant_id. The INSERT statement (VALUES ...) does not include tenant_id even though project_notes is per-project/tenant and DB columns are NOT NULL with no default.
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Also keep the existing assertProjectInTenant check (or equivalent) to ensure ownership before inserting.
- **server/tools.ts:4550** — INSERT into tenant-scoped table `project_files` without passing tenant_id. The code inserts a project_files row (project_id, file_name, ...) but does not set tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Ensure assertProjectInTenant ran before the insert (it does), and use the caller's tenantId parameter (coerced/validated) as the tenant value.
- **server/tools.ts:6465** — INSERT into tenant-scoped table `project_files` without passing tenant_id. The project_files INSERT performed in generate_audio omits tenant_id even though the insert may create tenant-scoped rows.
  - _Fix:_ Include tenant_id in the VALUES (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${filename + '.' + ext}, ${outPath}, ${driveUrl || null}, 'audio', ${audioBuffer.length}, 'system')). Keep/verify the prior assertProjectInTenant call before inserting.
- **server/tools.ts:8978** — UPDATE on tenant-scoped table `proposed_skills` filters only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)). Missing tenant_id constraint in WHERE, allowing cross-tenant updates.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure 'tid' (tenant id) is the same tenant that created/owns the proposed_skill. Prefer using Drizzle update with both id and tenantId predicates to enforce application-level isolation.
- **server/tools.ts:9006** — UPDATE on tenant-scoped table `proposed_skills` filters only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)). Missing tenant_id constraint in WHERE, allowing cross-tenant updates.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Confirm the tenantId value used is the same 'tid' inserted earlier when the row was created and never rely on row-level security.
- **server/tools.ts:9021** — UPDATE on tenant-scoped table `proposed_skills` filters only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)). Missing tenant_id constraint in WHERE, allowing cross-tenant updates.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Consider consolidating updates behind helper that always enforces tenant scoping to avoid repeated omissions.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' lacks tenant_id constraint in WHERE clause (`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`). Every UPDATE on tenant-scoped tables must include a tenant filter to enforce isolation (no RLS present).
  - _Fix:_ Add an explicit tenant filter to the UPDATE statement (e.g. `WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`) or otherwise scope the update with the caller's tenant (use a parameterized Drizzle .where(eq(..., tenantId))). Ensure every write/update/delete against tenant-scoped tables includes the tenant_id check.
- **server/tools/domains/files/handlers.ts:937** — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files omits any tenant_id column, so a project_files row could be written without an explicit tenant ownership column.
  - _Fix:_ Include the tenant column in the INSERT and set it from the trusted context (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)), or write this row via a DAO that enforces tenant_id. Prefer using Drizzle's parameterized APIs (e.g. .insert(...).values({... , tenantId })) so tenant ownership is always explicit.
- **server/tools/domains/files/handlers.ts:940** — UPDATE against tenant-scoped table projects lacks an explicit tenant constraint. The UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} does not include tenant_id in the WHERE, so it can affect a project belonging to a different tenant if projId were tampered with or mis-validated upstream.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, for example: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}. Using Drizzle's .where(eq(projects.id, projId), eq(projects.tenantId, tenantId)) is preferred to ensure parameterization and tenant isolation.
- **server/tools/domains/knowledge/handlers.ts:91** — UPDATE to tenant-scoped table knowledge_triples missing tenant constraint. In the contradictions loop the code runs UPDATE knowledge_triples SET valid_until = ${validFrom} WHERE id = ${row.id} without also constraining tenant_id, violating the requirement that writes must be scoped to caller tenant.
  - _Fix:_ Add tenant filtering to the UPDATE, e.g. UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer using Drizzle's parameterized .where(eq(knowledge_triples.id, row.id), eq(knowledge_triples.tenantId, tenantId)) or include tenant_id in the UPDATE statement to enforce isolation.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys executed without any tenant_id filter (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). This reads tenant-scoped key material across all tenants.
  - _Fix:_ Apply tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId}) or otherwise explicitly restrict/authorize the operation. If the table is intentionally global, document that fact and add owner-only checks and auditing; otherwise parameterize the query to include the caller's tenantId.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM provider_keys executed without tenant_id constraint. Non-filtered aggregation over provider_keys can expose cross-tenant data.
  - _Fix:_ Limit this aggregation to the caller's tenant by adding WHERE tenant_id = ${tenantId}, or if a cross-tenant admin view is required, make the admin-only intent explicit and audit/log the access. Prefer per-tenant counts instead of an unscoped global query.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without a tenant_id constraint (loads candidate pairings across all tenants). This violates the invariant that all reads against tenant-scoped tables must be constrained to the caller's tenant and allows cross-tenant data exposure / ambiguous-pairing logic to leak or be abused.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId} to the WHERE clause) and only perform cross-tenant/administrative scans from an operator-only path with explicit authorization. Alternatively, change the pairing schema so pairings are strictly tenant-scoped and include a robust phone-ownership/verification field, then query only rows for the tenant and detect ambiguity without scanning other tenants. Ensure any deliberate cross-tenant lookup is performed under an admin guard and audited.
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (settleVentureReservation updates by id only). This allows a caller that supplies/obtains a reservation id from another tenant to modify another tenant's ledger row.
  - _Fix:_ Require and pass the tenantId into settleVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Alternatively, verify the row's tenant before updating and fail if it doesn't match the caller's tenant.
- **server/venture-discovery/budget.ts:168** — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (releaseVentureReservation updates by id only). This may allow releasing/altering another tenant's reservation row by id.
  - _Fix:_ Require and pass the tenantId into releaseVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Or perform a prior lookup to confirm the row belongs to the caller's tenant before performing the UPDATE.
- **server/wiring-invariants.ts:366** — SELECT COUNT/MAX executed against subsystem table (${spec.table}) via sql.raw without any tenant_id constraint — tenant-scoped tables (e.g. knowledge_nudges, tool_performance) are being aggregated across all tenants.
  - _Fix:_ If this is intended to be an admin-only, cross-tenant aggregate, make that explicit and guard the function with an admin-only authorization check; otherwise restrict the query to a specific tenant (add WHERE tenant_id = ${tenantId}) or iterate tenants and run per-tenant queries. Avoid embedding table/column names into raw SQL from non-constant sources.
- **server/wiring-invariants.ts:535** — SELECT COUNT and pg_total_relation_size against memory tables (e.g. messages, agent_knowledge, memory_entries) uses sql.raw(`... FROM ${tbl.name}`) with no tenant_id filtering. This reads tenant-scoped data across all tenants.
  - _Fix:_ Run these freshness/size probes per-tenant (pass an explicit tenantId and add WHERE tenant_id = ${tenantId}), or require admin-only execution and clearly document/authorize cross-tenant aggregation. Prefer parameterized queries instead of injecting table names when possible, and ensure any cross-tenant reads are intentional and access-controlled.
- **server/wiring-invariants.ts:244** — Query against tool_performance (SELECT count(*) FROM tool_performance WHERE last_success_at > ...) has no tenant_id constraint — may aggregate/observe tenant-scoped metrics across all tenants.
  - _Fix:_ If tool_performance is tenant-scoped, add WHERE tenant_id = ${tenantId} or iterate tenants and aggregate per-tenant; if the intent is global telemetry, restrict this code to admin-only contexts and document that it intentionally runs cross-tenant queries.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages WHERE created_at > ... runs across the entire messages table with no tenant_id filter. messages is tenant-scoped, so this is an unscoped cross-tenant read.
  - _Fix:_ Restrict message-count queries to a tenant via WHERE tenant_id = ${tenantId} or perform the check in an admin-authorized context and explicitly document/authorize the cross-tenant aggregation. Avoid blanket table scans that touch tenant data without tenant scoping.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... omits any tenant_id filtering and therefore aggregates tenant-scoped tool performance across all tenants.
  - _Fix:_ If tool_performance rows are tenant-scoped, add a tenant_id predicate (WHERE tenant_id = ${tenantId}) or run the check per-tenant. If global aggregation is intended, gate this routine to an admin-only path and document the cross-tenant nature of the query.

### MEDIUM (36)

- **server/agentic-features.ts:861** — enrollInSequence runs a SELECT against `outreach_enrollments` (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active') without constraining by tenant_id. This lets the code read tenant-scoped rows without explicit tenant scoping.
  - _Fix:_ Include tenant scoping in the query, e.g. AND tenant_id = ${params.tenantId}, to ensure the lookup is limited to the caller's tenant before deciding whether the contact is already enrolled.
- **server/auth.ts:1131** — DELETE FROM password_reset_tokens WHERE token = ${tokenHash} deletes from a tenant-scoped table without constraining tenant_id. The function takes only a token and does not verify the owning tenant in the DELETE statement.
  - _Fix:_ Parse/verify the embedded tenant id from the token (as done in getResetToken) and include AND tenant_id = ${embeddedTenantId} in the DELETE WHERE clause (or reuse getResetToken to resolve the tenant then delete with both token hash and tenant_id). This prevents accidental cross-tenant deletion and enforces the tenant ownership invariant.
- **server/auth.ts:1136** — DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} removes rows across all tenants from a tenant-scoped table with no tenant_id constraint.
  - _Fix:_ Perform expiration cleanup per-tenant (iterate tenants and delete rows WHERE tenant_id = ${tenantId} AND expires_at < ...), or run the job under a clearly-documented platform-admin/maintenance context with an explicit audit/authorization guard. Ensure code and operational controls make it explicit this is a privileged global operation.
- **server/auth.ts:1242** — DELETE FROM auth_sessions WHERE expires_at < ${now} deletes auth session rows across all tenants without a tenant_id filter.
  - _Fix:_ Either run per-tenant (delete expired sessions for each tenantId) or gate this cleanup as a platform-maintenance task with explicit authorization and audit logging. Prefer scoping deletes by tenant_id when invoked from non-privileged contexts.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... queries the tenant-scoped `conversations` table without narrowing by tenant_id — this enumerates conversations across all tenants.
  - _Fix:_ If backfill is intended to be a global, platform-level job, gate it behind an explicit platform-admin context and document that it is privileged. Otherwise, iterate over tenants and run the SELECT with WHERE tenant_id = ${tenantId} (or pass tenantId into the job) so rows are always processed under the correct tenant scope.
- **server/business-tools.ts:235** — addCustomer inserts assigned_to directly from caller input without validating ownership of that foreign id. The INSERT INTO customers includes ${params.assigned_to || null} which can reference a user from another tenant.
  - _Fix:_ Validate any foreign id supplied by the caller (assigned_to) belongs to the caller's tenant before storing it. For example, SELECT id FROM users WHERE id = ${params.assigned_to} AND tenant_id = ${tid} LIMIT 1 and fail if not found, or disallow assigning cross-tenant user ids entirely.
- **server/business-tools.ts:269** — updateCustomer allows caller-controlled foreign ids (e.g. assigned_to) to be written into customers without verifying the referenced row's tenant ownership. The code applies sql.raw(key) = ${val} for request-supplied fields and then updates WHERE id = ... AND tenant_id = ${tid}, but does not validate that assigned_to (if provided) references a user in the same tenant.
  - _Fix:_ When accepting foreign ids in updates (assigned_to or other FK columns), explicitly validate the referenced resource belongs to the tenant before applying the update (e.g. SELECT id FROM users WHERE id = ${params.assigned_to} AND tenant_id = ${tid} LIMIT 1). Alternatively, restrict which FK fields callers can set and derive/resolve them server-side after validation.
- **server/dormant-deprecation.ts:170** — SELECT COUNT(*) FROM `messages` does not include a tenant filter (getTrafficSnapshot). If `messages` is tenant-scoped this returns global counts instead of per-tenant counts.
  - _Fix:_ If the intent is per-tenant traffic gating, accept/derive a tenantId and add WHERE tenant_id = ${tenantId}. If the query is intentionally global, verify `messages` is not a tenant-scoped table and document the global behavior so it is not accidentally left tenant-scoped.
- **server/episode-playbooks.ts:135** — String-interpolated SQL array literal used in WHERE id = ANY(${`{${ids.join(",")}}`}::int[]) for tenant-scoped `episode_playbooks`. Although ids come from DB, building SQL array text via string interpolation is brittle and can lead to injection risks and DB/driver incompatibilities.
  - _Fix:_ Use parameterized array binding or Drizzle helpers (e.g. pass an array parameter or use sql.join/sql.array-like constructs) so ids are passed as parameters rather than interpolated into SQL text. Example: WHERE id = ANY(${sql`ARRAY[${sql.join(ids.map(i => sql`${i}`), sql`, `)}]::int[]`}) or use a parameterized helper that produces an int[] safely.
- **server/glasses-gateway.ts:173** — UPDATE against 'apiKeys' uses WHERE eq(apiKeys.id, key.id) but does not include a tenant_id constraint. Even though the row was fetched by keyHash, every write to tenant-scoped tables must include explicit tenant scoping in the WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) to ensure the write cannot affect rows outside the authenticated tenant context.
- **server/heartbeat-startup.ts:81** — UPDATE on tenant-scoped table heartbeat_tasks executed without constraining by tenant_id (WHERE id = ${t.id} only). This mutates tenant-scoped rows without an explicit tenant filter.
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage helper that accepts an explicit tenant context (storage.updateHeartbeatTask) so all writes remain scoped to the correct tenant.
- **server/heartbeat-startup.ts:85** — UPDATE on tenant-scoped table heartbeat_tasks executed without constraining by tenant_id (WHERE id = ${t.id} only). This normalizes a task model across the table but lacks explicit tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}), or call the storage wrapper that enforces tenant context to avoid cross-tenant mutations.
- **server/heartbeat.ts:1579** — SELECT from heartbeat_logs (tenant-scoped table) without restricting by tenant_id: the query SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... is cross-tenant and may cause tenant A runs to be skipped based on tenant B logs.
  - _Fix:_ Restrict the query to the task's tenant (e.g. WHERE tenant_id = ${tenantId} AND task_name = ...) or otherwise scope it to the admin tenant if this is intended platform-level behavior. Prefer using storage APIs that accept tenant context so the filter cannot be accidentally omitted.
- **server/job-queue.ts:378** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} without scoping. Although intended for admin use, it reads a tenant-scoped table by id with no tenant guard.
  - _Fix:_ Keep this function admin-only and audit its callers. Prefer to explicitly mark it as admin-only and ensure route-level checks restrict access; alternatively, return tenant only after enforcing caller authorization. Consider parameterizing and validating the id input before use.
- **server/job-queue.ts:406** — listJobs builds and executes a SELECT FROM agent_jobs that may omit a tenant_id WHERE clause when tenantId is not provided, returning rows across tenants.
  - _Fix:_ Require an explicit tenantId for tenant-facing callers; if admin users need cross-tenant listing, ensure the API enforces strict admin auth and auditing. Alternatively offer separate admin endpoints and enforce authorization checks there.
- **server/job-queue.ts:480** — getJobStats permits ALL_TENANTS (no tenant filter) and runs an aggregate SELECT on agent_jobs without tenant scoping when scope is ALL_TENANTS.
  - _Fix:_ If cross-tenant aggregation is an admin-only operation, ensure the caller is an authorized platform admin and add explicit audit logging. Otherwise require a tenant-scoped call and add AND tenant_id = ${tenantId} to the query.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on table `procedure_evolution_runs` filters only by id (WHERE id = runId) without constraining tenant_id — tenant-scoped row update lacks tenant predicate.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or otherwise ensure the update runs under a secure admin-only context and that runId cannot be guessed by another tenant.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on table `procedure_evolution_runs` (marking noop/done) uses only run id in WHERE; no tenant_id constraint present.
  - _Fix:_ Add tenant-scoped condition to the WHERE (eq(procedureEvolutionRuns.tenantId, tenantId)) or gate this operation to an authorized internal/admin flow to prevent cross-tenant status mutation.
- **server/lib/aevo-meta-editor.ts:500** — UPDATE on table `procedure_evolution_runs` (finalizing run) filters only by id — missing tenant_id guard on a tenant-scoped table.
  - _Fix:_ Constrain the UPDATE by tenant (include eq(procedureEvolutionRuns.tenantId, tenantId) in the WHERE) to enforce tenant isolation, or otherwise ensure only a trusted admin context can perform this cross-tenant update.
- **server/lib/ideabrowser-score.ts:185** — SQL uses a hand-built Postgres array literal (`const literal = `{${ids.join(',')}}`;`) and interpolates it into the query (rm.project_id = ANY(${literal}::int[])). This is a string-interpolated SQL value derived from caller input (projectIds) rather than using a parameterized array binding, increasing risk of SQL/format issues and violating the 'no string-interpolated SQL with request-derived value' rule.
  - _Fix:_ Use a parameterized binding for the integer array instead of constructing a textual array literal. For example, pass the ids as a parameterized JS array (or use unnest/VALUES parameterization) so the DB driver binds elements safely: e.g. WHERE rm.project_id = ANY(${ids}::int[]) with ids provided as a parameterized array, or use a join against unnest($1::int[]) to avoid manual string construction. Keep the existing tenant_id constraints in place.
- **server/lib/jury-experience.ts:174** — A SQL fragment for the vector embedding is constructed from request-derived input and interpolated into the query: embeddingFragment = embedding ? sql`${`[${embedding.join(',')}]`}::vector` : sql`NULL`. This builds SQL text from data derived from the caller question and injects it into the SQL template rather than using a safe parameter binding for the embedding, i.e. string-interpolated SQL with request-derived content.
  - _Fix:_ Avoid constructing raw SQL text from the embedding array. Instead pass the embedding as a parameterized value (or use the DB driver's native support for vector/array parameters) so elements are bound safely. If the DB/driver doesn't support the vector type directly, store embeddings as parameterized bytea/JSONB or use a dedicated global mapping table and parameter binding rather than joining numbers into a SQL literal. Ensure the tenant_id is still passed as a parameterized field (it already is).
- **server/process-governor.ts:399** — Query JOINs to personas without scoping the joined table by tenant. The SELECT that reads agent_desks LEFT JOIN personas ON p.id = ad.persona_id lacks a p.tenant_id constraint, so persona rows from other tenants could be returned.
  - _Fix:_ Constrain the join to the tenant. e.g. JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId} (or p.tenant_id = ad.tenant_id) so every read from the personas table is scoped to the tenant.
- **server/recurring-messages.ts:189** — SELECT from tenant-scoped table `agent_knowledge` may omit tenant filter. The query in listScheduledMessages conditionally omits `AND tenant_id = ${tenantId}` when the caller passes the `_allowGlobalSystemUseOnly` flag, resulting in a tenant-scoped read with no tenant_id constraint.
  - _Fix:_ Avoid issuing unscoped SELECTs on tenant-scoped tables from a public API. If a global/system scan is required, restrict this path to an internal-only, well-audited codepath (not an exported function), or prefer iterating tenants and issuing per-tenant queries (e.g. `WHERE tenant_id = <tenant>`). If returning multi-tenant results is necessary, ensure the caller is an internal, authenticated system process and that the returned columns are strictly limited and scrubbed of PII/secrets.
- **server/routes.ts:1395** — SELECTs aggregating counts from tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) are executed without tenant_id constraints in the admin `/api/public/stats` handler, returning global per-table counts.
  - _Fix:_ If these are intended global admin metrics, ensure the endpoint is tightly platform-admin gated (it already is) and clearly document the global nature. Otherwise, add tenant scoping to the queries (e.g. .where(eq(table.tenantId, tenantId))) or offer per-tenant stats. Prefer explicit tenant filters for any queries that touch tenant-scoped tables unless the query is a deliberate and documented platform-admin global operation executed under safe controls.
- **server/routes.ts:1425** — Multiple db.execute(s`SELECT count(*) FROM <tenant-scoped table>`) calls in `/api/public/architecture` run without tenant_id constraints, fetching counts from tenant-scoped tables globally.
  - _Fix:_ As above: either restrict these queries to a tenant_id when per-tenant metrics are desired, or ensure the endpoint is strictly platform-admin-only and document the global reads. Safer approach: accept an explicit tenant parameter (and validate admin privileges) and add WHERE tenant_id = <tenant> to each count query, or run platform-global checks only from an offline/admin migration.
- **server/routes/admin.ts:881** — Query selects from `tool_performance` without any tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance). If `tool_performance` is tenant-scoped this reads data across tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}) or, if this endpoint intentionally must scan all tenants, make that explicit and ensure the route is strictly platform-admin gated and audited. Prefer parameterized tenant-scoped reads where possible.
- **server/routes/admin.ts:899** — Query against `ai_insights` (SELECT id, engine_type, category, ... FROM ai_insights WHERE status = 'new' ...) has no tenant_id filter. If `ai_insights` is tenant-scoped this will return rows from all tenants.
  - _Fix:_ Add a tenant_id constraint (e.g. WHERE tenant_id = ${tenantId}) or validate that `ai_insights` is a global table and that cross-tenant access here is intentional and only exposed to properly-authorized platform admin sessions. Prefer least-privilege, tenant-scoped queries.
- **server/sentiment-detector.ts:167** — logSentimentEvent inserts a row referencing conversation_id provided by the caller without verifying that the referenced conversation belongs to the same tenant. Writing events tied to a foreign conversation id can leak cross-tenant associations or allow inconsistent references.
  - _Fix:_ Validate ownership of the referenced conversation before inserting (e.g. SELECT 1 FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} LIMIT 1), or include an explicit tenant constraint on any downstream operations and reject/ignore if the conversation does not belong to the caller's tenant.
- **server/storage.ts:289** — Raw SELECT from tenant-scoped table `messages` (SELECT id, citations FROM messages WHERE id IN (...)) has NO tenant_id constraint.
  - _Fix:_ Include tenant scoping in the query. Pass the caller's tenantId into attachCitationMetadata (or derive it) and add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle predicate). Alternatively, only query for citations of message IDs that were already validated to belong to the caller's tenant and enforce that in the code path prior to this query.
- **server/storage.ts:1135** — markHeartbeatTaskRun updates heartbeat_tasks by id with NO tenant_id constraint (WHERE eq(heartbeat_tasks.id, id) only). heartbeat_tasks is tenant-scoped.
  - _Fix:_ Require/accept tenantId in the API and include it in the WHERE clause (e.g. .where(and(eq(heartbeatTasks.id, id), eq(heartbeatTasks.tenantId, tenantId)))). If this is a scheduler operation that needs to act across tenants, make that explicit and use an admin-only path with a documented ALL_TENANTS sentinel.
- **server/tenant-fork.ts:226** — UPDATE on a tenant-scoped config table performed with WHERE id = <newId> only (reparent step) and no tenant_id constraint. All reads/writes against tenant-scoped tables must include tenant_id in WHERE to avoid cross-tenant effects.
  - _Fix:_ Add an explicit tenant constraint to the update that reparents self-ref rows. For example: .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId))). This enforces the operation is limited to rows for the newly-created tenant.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table 'custom_tools' filters only on name (eq(customTools.name, parsed.name)) but does NOT include tenant_id. Relying on name normalization alone is brittle and breaks the invariant that every read must be tenant-scoped.
  - _Fix:_ Include tenant scoping in the lookup. For example: db.select().from(customTools).where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). This prevents cross-tenant name collisions and enforces explicit tenant ownership checks.
- **server/tools/domains/strategic-memory/handlers.ts:98** — UPDATE memory_entries SET ... WHERE id = ${id} is executed without constraining by tenant_id. Although id came from a prior tenant-scoped SELECT, the UPDATE itself lacks an explicit tenant filter and could act on rows from other tenants if ids are manipulated.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) so every destructive/modifying statement explicitly enforces the caller's tenant boundary.
- **server/tools/domains/strategic-memory/handlers.ts:159** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) is executed without a tenant_id constraint. The ids array originates from a previous tenant-scoped SELECT, but the UPDATE statement itself does not enforce tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ANY(${ids}) AND tenant_id = ${tid}), or include tenant_id in the WHERE clause to make the tenant-boundary explicit for the mutation.
- **server/tools/domains/strategic-memory/handlers.ts:258** — UPDATE memory_entries SET ... WHERE id = ${id} is executed without an explicit tenant_id filter (in recordStrategicWin). The UPDATE should explicitly constrain by tenant to prevent accidental cross-tenant modifications.
  - _Fix:_ Modify the UPDATE to include AND tenant_id = ${tid} (or otherwise ensure tenant scoping in the statement). Always include tenant_id in WHERE for updates against tenant-scoped tables to enforce isolation at the application layer.
- **server/video-job-runner.ts:275** — SELECT from tenant-scoped table videoJobs (listStaleVideoJobs) is performed without a tenant_id constraint. This enumerates job rows across tenants in the initial candidate listing.
  - _Fix:_ If global scanning is not intended, restrict the SELECT to a specific tenant by adding eq(videoJobs.tenantId, <tenantId>) to the WHERE clause. If global recovery is intended, document and guard this function so only a privileged/system process may run it, or at minimum limit returned fields and ensure the subsequent claim/update enforces tenant equality (claimStaleVideoJob already includes tenant checks). Consider moving tenant-less logic into an explicit admin path to make the privilege requirement explicit.

### LOW (1)

- **server/auth.ts:1339** — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' deletes rows from the auth_sessions table across all tenants without tenant scoping.
  - _Fix:_ This boot-time migration/cleanup should be explicitly documented as a platform-global operation and run only during secure startup. Alternatively, narrow the deletion criteria to include tenant scoping or otherwise assert platform-admin authority before executing.

### Resolved — confirmed false positives (190)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/agentic-features.ts:1021** _(upstream-verified)_ — classifyReply performs an UPDATE on `outreach_enrollments` (UPDATE outreach_enrollments SET ... WHERE id = ${row.id}) without including tenant_id in the WHERE clause. This is a write to a tenant-scoped table that lacks a tenant constraint.
  - _Why false positive:_ row.id comes from the immediately preceding SELECT constrained by e.tenant_id = params.tenantId, so the updated enrollment was already tenant-verified in the same call path.
- **server/auth.ts:1327** _(global-table)_ — DELETE FROM auth_sessions executes a global wipe of auth sessions across all tenants (no tenant filter). While this function is intended as a platform-admin operation, the SQL itself performs an unrestricted cross-tenant mutation.
  - _Why false positive:_ This is an explicitly exceptional platform-admin global session reset with one-use authority check at the function boundary and audit logging; cross-tenant scope is intentional.
- **server/channel-routing.ts:53** _(global-table)_ — INSERT INTO channel_routes (...) VALUES (${channel}, ${personaId}) / ON CONFLICT ... inserts a persona_id provided by the caller without validating that the referenced persona belongs to the caller's tenant and without setting tenant_id on the new row. This allows cross-tenant association or privilege escalation.
  - _Why false positive:_ channel_routes is defined here as a single global routing table with UNIQUE(channel) and personas are schema-noted as not tenant-scoped.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE against tenant-scoped table 'commitments' performed without constraining to the row's tenant (missing AND tenant_id = ... in WHERE). The statement `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` can modify a row across tenants if `row.id` is provided from an unverified source.
  - _Why false positive:_ row.id was returned by the claim statement under c.id = e.id AND c.tenant_id = tenantId; this rollback update is acting on a row already claimed from a tenant-scoped query in the same path.
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE FROM `doc_chunks` is executed without a tenant_id constraint (addDocument). This delete can remove chunks belonging to other tenants that share the same collection_id/doc_path.
  - _Why false positive:_ Immediately before the DELETE, the code verifies the collection exists with id = collectionId AND tenant_id = tenantId; doc_chunks are then deleted only for that validated collection_id/doc_path pair.
- **server/embeddings.ts:855** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} executed inside backfillEmbeddingVecs without tenant_id constraint. This backfill loop can modify rows across tenants.
  - _Why false positive:_ This is a platform-wide backfill updating only housekeeping/vector fields in a maintenance loop; it does not mix tenant data into outputs.
- **server/embeddings.ts:872** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed inside backfillEmbeddingVecs without tenant_id constraint. This writes tenant-scoped rows without ensuring tenant ownership.
  - _Why false positive:_ This is a platform-wide backfill updating only embedding_vec on existing rows in a maintenance loop; cross-tenant iteration is intentional housekeeping.
- **server/google-drive.ts:1664** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without an explicit tenant_id column value. Per invariant every INSERT into tenant-scoped tables must explicitly pass the tenantId (columns are NOT NULL with no default).
  - _Why false positive:_ Referenced schema notes project_files has no tenant_id column; insert is gated by assertProjectInTenant(projectId, resolvedTenantId), so isolation is via the tenant-scoped parent project.
- **server/google-workspace.ts:2287** _(fk-transitive)_ — INSERT into presenter_slide_images does not set tenant_id (tenant-scoped table may require tenant_id). The INSERT uses session_id only and does not validate that the session belongs to the current tenant.
  - _Why false positive:_ presenter_slide_images is not shown as tenant-scoped, and the row is a child of presenter_sessions via session_id; isolation is transitive through the tenant-scoped parent session.
- **server/google-workspace.ts:2293** _(fk-transitive)_ — INSERT into presenter_slide_images (thumb variant) does not set tenant_id and does not verify ownership of the referenced session_id.
  - _Why false positive:_ presenter_slide_images is a child table keyed by session_id; with tenant-scoped presenter_sessions, storing thumbnail variants without tenant_id is consistent with transitive isolation.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — no tenant_id constraint. This mutates proposals across tenants by id only.
  - _Why false positive:_ Global TTL housekeeping sweep intentionally archives stale proposals across tenants; mutates status only and does not return mixed tenant data to a tenant-facing surface.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — no tenant_id constraint. This disables tasks by id across tenants without scoping.
  - _Why false positive:_ Global maintenance sweep disables stuck heartbeat tasks across tenants; per-row housekeeping/status mutation only, no tenant data mixing or tenant-facing disclosure.
- **server/memory-graph.ts:130** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} executes without constraining tenant_id. This allows a caller to increment memory_count for a category belonging to a different tenant.
  - _Why false positive:_ catId comes from ensureCategory(categoryName, tenantId, personaId), which first selects/inserts memory_categories scoped by tenant_id = tenantId in the same call path.
- **server/memory-graph.ts:708** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} executes without constraining tenant_id. This can modify a memory entry belonging to another tenant if the id value is forged/incorrect.
  - _Why false positive:_ mem.id is sourced from a prior SELECT on memory_entries constrained by tenant_id = tenantId in the same function, so the update targets rows already ownership-verified upstream.
- **server/memory-intelligence.ts:413** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} runs without a tenant_id predicate. This can expire/alter a knowledge_triple row owned by another tenant.
  - _Why false positive:_ Each c.id comes from the immediately preceding contradictions query filtered by tenant_id = tenantId, so the updated knowledge_triples rows were already tenant-scoped in the same path.
- **server/mpeg-engine.ts:1058** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code verifies project ownership via assertProjectInTenant(...) but the INSERT itself omits tenant_id, violating the invariant that every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; assertProjectInTenant verifies the parent project belongs to the tenant before insert.
- **server/mpeg-engine.ts:1445** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. This is the parallel-render code path mirroring the earlier insert: project ownership is checked, but the INSERT omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated through project_id to tenant-scoped projects; parent ownership is checked by assertProjectInTenant.
- **server/plan-executor.ts:613** _(system-sweep)_ — CAS claim UPDATE on plans lacks a tenant_id constraint (UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved'). This allows claiming/executing a plan across any tenant.
  - _Why false positive:_ This is a platform-wide worker claim loop for approved plans; it claims rows globally and then carries forward the row-returned tenant_id for scoped downstream handling, not a tenant-facing mixed-data read.
- **server/plan-executor.ts:1149** _(system-sweep)_ — SELECT fetching pending approved plans (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50) has no tenant_id filter and the code subsequently kicks off executePlan(r.id) for each id. Combined with the claim UPDATE bug, this can cause cross-tenant plan execution.
  - _Why false positive:_ Boot recovery intentionally enumerates approved plans across tenants for background execution; this maintenance dispatcher does not return mixed tenant content and each plan is handled with its own row-carried tenant context.
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping. Conversations are tenant-scoped and this read can return rows for other tenants' conversations.
  - _Why false positive:_ Boot-time backfill loops across all projects intentionally; output is per-project housekeeping/backfill, not tenant-facing mixed response.
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant filtering. Messages are tenant-scoped and this can leak message contents across tenants.
  - _Why false positive:_ Part of the same global backfill/maintenance loop processing each conversation/messages set for its project, not a tenant-request read path.
- **server/routes.ts:187** _(system-sweep)_ — SELECT from tenant-scoped table `file_storage` without any tenant_id constraint (restoreUploadsFromDb startup restore). This reads raw file data for all tenants (fileStorage.data) using only filename/mime filters.
  - _Why false positive:_ Startup restore is a platform-wide maintenance job restoring image blobs to disk; it does not return tenant data to callers.
- **server/routes/projects.ts:401** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table scoped transitively by tenant-scoped project/conversation parents.
- **server/routes/projects.ts:569** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_notes has no tenant_id; insert is correctly scoped via previously verified tenant-owned project_id.
- **server/routes/projects.ts:609** _(fk-transitive)_ — INSERT into project_files (base64 path) omits tenant_id. 'INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id; insert is correctly scoped through tenant-verified parent project.
- **server/routes/projects.ts:677** _(fk-transitive)_ — INSERT into project_files (multipart upload) omits tenant_id. 'INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id; insert is correctly scoped through tenant-verified parent project.
- **server/routes/projects.ts:616** _(upstream-verified)_ — UPDATE projects sets updated_at without constraining tenant_id. 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' lacks 'AND tenant_id = ${tenantId}'.
  - _Why false positive:_ Earlier in same handler project ownership is verified with SELECT ... WHERE id = projectId AND tenant_id = tenantId before this update by id.
- **server/routes/projects.ts:684** _(upstream-verified)_ — UPDATE projects sets updated_at without constraining tenant_id. 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' lacks 'AND tenant_id = ${tenantId}'.
  - _Why false positive:_ Earlier in same handler project ownership is verified with SELECT ... WHERE id = projectId AND tenant_id = tenantId before this update by id.
- **server/routes/projects.ts:755** _(fk-transitive)_ — DELETE from project_files omits tenant constraint. 'DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}' does not include tenant_id, allowing deletion of rows outside the caller's tenant if ids collide.
  - _Why false positive:_ project_files has no tenant_id; delete is against child rows under a tenant-verified parent project_id.
- **server/sculptor.ts:105** _(internal-worker-pk)_ — UPDATE on sculptor_sessions omits tenant constraint: `UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id}` updates by id only without verifying tenant_id.
  - _Why false positive:_ Server-internal flow updates freshly created sculptor_sessions row by its server-generated primary key, not an untrusted cross-tenant request id.
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE on sculptor_sessions omits tenant constraint: `UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}` lacks tenant scoping.
  - _Why false positive:_ Server-internal follow-up update of the newly inserted sculptor_sessions row by server-generated primary key only.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT counts heartbeat_tasks without tenant constraint: `SELECT count(*) as cnt FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'` reads across tenants (and subsequent UPDATE disables tasks globally).
  - _Why false positive:_ Boot-time cleanup intentionally scans heartbeat_tasks across tenants for runaway rows and only performs housekeeping disable/count actions, not tenant data disclosure.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks disables delegation tasks without tenant scoping: `UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true` will affect all tenants' tasks.
  - _Why false positive:_ Global maintenance disables runaway delegation tasks across tenants as housekeeping; no tenant content is returned or mixed.
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT counts session-logger heartbeat_tasks without tenant constraint: `SELECT count(*) as cnt FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%'` reads across tenants.
  - _Why false positive:_ Cross-tenant count of malformed session-logger tasks is part of boot-time housekeeping sweep and only drives global disable action.
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks disables session-logger tasks without tenant scoping: `UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true` lacks tenant isolation.
  - _Why false positive:_ Global disable of stray session-logger tasks is maintenance-only status change across tenants, not tenant-facing data access.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT counts various 'examine repository' heartbeat_tasks without tenant constraint: `SELECT count(*) as cnt FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... OR ...)` reads across tenants and the subsequent UPDATE disables matching tasks across all tenants.
  - _Why false positive:_ Cross-tenant scan for orphaned repo-examination heartbeat tasks is a maintenance sweep using only aggregate count/cleanup.
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks disables 'examine repository' tasks without tenant scoping: `UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true` will update rows for all tenants.
  - _Why false positive:_ Disabling matching examine-repository tasks across tenants is intentional housekeeping and does not expose or combine tenant content.
- **server/seed.ts:2240** _(system-sweep)_ — SELECT DISTINCT ... FROM conversations JOIN projects queries tenant-scoped tables (conversations, projects) without restricting to a specific tenant_id. The query iterates over data for all tenants and then inserts links (project_conversations) without tenant context, risking cross-tenant data mixing.
  - _Why false positive:_ Boot-time backfill iterates all tenants' conversation/project links but writes only missing join rows per matched tenant-carried pair; project_conversations is transitively scoped and no tenant data is surfaced.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks without tenant constraint — this deletes disabled delegation tasks across all tenants (no tenant_id in WHERE), causing destructive cross-tenant modifications.
  - _Why false positive:_ Purging old disabled delegation tasks is platform-wide housekeeping delete across tenants with no tenant-facing output beyond aggregate count.
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs WHERE created_at < ... with NO tenant constraint — this bulk delete removes heartbeat logs for all tenants.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id, so unscoped retention purge targets a non-tenant-scoped table by design.
- **server/tools.ts:2806** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. The insert: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` does not set tenant_id on a tenant-scoped relationship table, allowing cross-tenant linkage/contamination.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively isolated via tenant-scoped projects/conversations already validated before insert
- **server/tools.ts:2932** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The INSERT: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` omits tenant_id, so new file rows may not be tenant-scoped.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by prior project ownership check on tenant-scoped parent projects
- **server/tools.ts:2946** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The INSERT: `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id, enabling tenant cross-contamination in notes.
  - _Why false positive:_ project_notes has no tenant_id and insert is gated by prior project ownership check on tenant-scoped parent projects
- **server/tools.ts:2970** _(fk-transitive)_ — INSERT into project_conversations (second occurrence) omits tenant_id. The code inserts `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` without tenant scoping when linking a conversation to a project.
  - _Why false positive:_ project_conversations has no tenant_id and both project ownership and conversation tenant ownership are verified earlier before insert
- **server/tools.ts:7006** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the inserted columns. (No tenant_id column/value in INSERT INTO project_files ... VALUES (...)).
  - _Why false positive:_ project_files has no tenant_id and insert occurs only after assertProjectInTenant validates tenant-scoped parent project
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments executes a SELECT on the tenant-scoped `experiments` table without scoping the query to a tenant (no WHERE tenant_id = ...). This returns rows across all tenants.
  - _Why false positive:_ heartbeat maintenance sweep intentionally scans all tenants but handles each row with its own tenant_id and updates same-row status only
- **server/agentic-engines.ts:325** _(system-sweep)_ — retryPendingMinervaRouting executes a SELECT on the tenant-scoped `ai_insights` table without a tenant filter (WHERE tenant_id = ... is missing), returning insights across tenants.
  - _Why false positive:_ durability sweep intentionally scans pending rows across tenants and processes each with row-carried tenant_id, not returning mixed data to tenants
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals issues a bulk UPDATE on the tenant-scoped `agent_approvals` table without a tenant filter (UPDATE ... WHERE status = 'pending' AND expiresAt < ...), updating rows across all tenants.
  - _Why false positive:_ global expiry housekeeping updates stale approvals across tenants and then handles linked runs per returned row tenant_id
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Query selects from tenant-scoped table `department_budgets` without constraining by tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads across all tenants and violates the project invariant that every read/write to tenant-scoped tables must include a tenant_id filter.
  - _Why false positive:_ heartbeat enforcement sweep intentionally iterates active budgets across tenants and emits per-tenant events using each row's tenant_id
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — Unscoped SELECT on tenant-scoped table `agent_runs` (raw SQL at lines ~65-90) — the query returns rows across tenants because it lacks a WHERE tenant_id = <tenant> constraint. This allows cross-tenant reads/processing of tenant-scoped data and violates the invariant that every read against a tenant-scoped table must be constrained to the caller's tenant.
  - _Why false positive:_ resume worker is a cross-tenant housekeeping sweep; joined rows retain tenant_id and are processed per row rather than exposed to tenant-facing output
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_wake_schedules` performs a claim (and SELECT subquery) with no tenant_id filter — the query updates/returns rows across all tenants.
  - _Why false positive:_ Privileged heartbeat maintenance loop intentionally claims due rows across tenants and emits each row with its own tenant_id; no mixed tenant output.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is missing an explicit tenant filter. The query `SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ...` does not include `tenant_id`.
  - _Why false positive:_ Same call path fail-closed checks assert conversation ownership and re-reads conversation/project with tenant_id before reading messages for that conversation.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Backfill query uses a subquery against tenant-scoped table `messages` without a tenant constraint: `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`. This can read message counts across tenants.
  - _Why false positive:_ Backfill is a platform-wide maintenance sweep; it only enumerates conversation ids/tenant_ids and then processes each row with its own tenant_id via autoSaveProjectTranscript.
- **server/auto-transcript.ts:153** _(system-sweep)_ — Linked-conversations query includes a subquery against `messages` without tenant scoping: `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`. This can count messages across tenants.
  - _Why false positive:_ Linked-conversation backfill is a maintenance sweep; rows carry c.tenant_id and each conversation is handled separately, with tenant checks inside autoSaveProjectTranscript.
- **server/business-tools.ts:96** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. The invoice_items INSERT uses only invoice_id and item fields (INSERT INTO invoice_items (invoice_id, ...) VALUES (${newId}, ...)) so if invoice_items is a tenant-scoped table (tenant_id NOT NULL) new rows will not be tied to the caller's tenant — allowing cross-tenant data leakage or orphaned rows.
  - _Why false positive:_ invoice_items has no tenant_id by schema and is a child table isolated transitively through tenant-scoped invoices via invoice_id.
- **server/channel-routing.ts:30** _(global-table)_ — SELECT joins channel_routes -> personas and returns persona_name without any tenant filtering. If personas is tenant-scoped, this leaks or mixes persona rows across tenants because there's no WHERE p.tenant_id = <tenant> (or equivalent).
  - _Why false positive:_ channel_routes is created ad hoc without tenant_id and UNIQUE(channel), indicating an intentionally global per-channel routing registry; joined personas are also non-tenant-scoped by schema.
- **server/channel-routing.ts:51** _(global-table)_ — DELETE FROM channel_routes WHERE channel = ${channel} executes without any tenant constraint. This can delete or affect other tenants' routing records if channel_routes should be tenant-scoped.
  - _Why false positive:_ channel_routes has no tenant_id and UNIQUE(channel), matching an intentionally global routing table rather than per-tenant data.
- **server/channel-routing.ts:63** _(global-table)_ — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true returns persona_id without scoping by tenant. If channel_routes is tenant-scoped, this read may return another tenant's mapping.
  - _Why false positive:_ Lookup is against global channel_routes and returns persona_id from a non-tenant-scoped personas table.
- **server/channel-routing.ts:71** _(global-table)_ — DELETE FROM channel_routes WHERE channel = ${channel} (removeChannelRoute) runs without tenant constraint and will remove mappings across tenants if the table is not tenant-scoped.
  - _Why false positive:_ removeChannelRoute operates on the intentionally global channel_routes table keyed globally by channel.
- **server/chat-engine/project-context.ts:44** _(fk-transitive)_ — Subquery reads project_conversations (pc) without constraining it to the caller's tenant (EXISTS(...) uses pc.conversation_id and pc.project_id but no tenant filter). This can allow cross-tenant linking via project_conversations.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table; query is anchored to tenant-scoped projects/conversations.
- **server/chat-engine/project-context.ts:23** _(fk-transitive)_ — Query selecting from project_conversations (pc) JOIN projects (p) WHERE pc.conversation_id = ${conversationId} does not constrain pc by tenant_id. project_conversations appears tenant-scoped but is not filtered — risk of returning cross-tenant project_id.
  - _Why false positive:_ project_conversations has no tenant_id; join to projects with p.tenant_id scopes the linked child rows transitively.
- **server/chat-engine/project-context.ts:159** _(fk-transitive)_ — Selecting project_files by project_id only (SELECT ... FROM project_files WHERE project_id = ${projectId}) has no tenant_id constraint. There is no DB-enforced FK/tenant guard, so this read may leak files across tenants if project_id mapping is not perfectly enforced at DB level.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; projectId was verified against projects.tenant_id earlier in same function.
- **server/chat-engine/project-context.ts:171** _(fk-transitive)_ — Selecting project_notes filtered only by project_id (SELECT note,... FROM project_notes WHERE project_id = ${projectId}) lacks tenant_id constraint and can return notes from other tenants for the same project_id value if DB foreign keys/cascades are not enforcing tenant ownership.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; project ownership was verified before this read.
- **server/chat-engine/project-context.ts:181** _(fk-transitive)_ — Query joining project_conversations (pc) and conversations (c) uses pc.project_id = ${projectId} but does not constrain pc by tenant_id (only c.tenant_id is checked). The project_conversations table is read without an explicit tenant filter.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table; joined conversations are explicitly filtered by c.tenant_id.
- **server/data-protection.ts:149** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` lacks a tenant_id predicate. The query `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` will read rows across all tenants (tenant_id not constrained).
  - _Why false positive:_ This is a cross-tenant expiry purge loop; each row carries its own tenant_id and subsequent deletes are per-row tenant-scoped housekeeping.
- **server/dfy-intake.ts:52** _(signed-url)_ — SELECT from tenant-scoped table `dfy_intake_forms` without constraining by tenant_id (getDfyIntakeByToken). This returns a form by token across all tenants.
  - _Why false positive:_ Lookup is by high-entropy random token; the token itself is the access secret for this intake form flow.
- **server/dormant-deprecation.ts:180** _(system-sweep)_ — SELECT COUNT(DISTINCT tool_name) FROM `tool_performance` lacks tenant scoping (getTrafficSnapshot). `tool_performance` is used elsewhere as tenant-scoped; this returns cross-tenant data and can break tenant isolation.
  - _Why false positive:_ Global aggregate count for maintenance/traffic gating; returns only platform-wide counts, not tenant row data.
- **server/dormant-deprecation.ts:204** _(system-sweep)_ — SELECT DISTINCT tool_name FROM `tool_performance` (getEverInvokedTools) is executed with no tenant filter. `tool_performance` elsewhere is tenant-scoped, so this leaks/uses data across tenants.
  - _Why false positive:_ Platform-wide maintenance logic intentionally aggregates distinct tool names globally and does not expose tenant content.
- **server/dormant-deprecation.ts:215** _(system-sweep)_ — SELECT DISTINCT tool_name FROM `tool_performance` WHERE last_success_at ... (getActiveTools14d) lacks a tenant filter. This returns active tools across all tenants rather than for a specific tenant.
  - _Why false positive:_ Global 14-day active-tool aggregation for maintenance gating; aggregate set only, no tenant content returned.
- **server/email.ts:415** _(system-sweep)_ — SELECT from tenant-scoped table `inbox_messages` without tenant filter (SELECT id, message_id FROM inbox_messages WHERE ...). This iterates rows across all tenants.
  - _Why false positive:_ Global backfill job scans tenant rows only to repair empty body fields; no tenant-facing data return or cross-tenant mixing.
- **server/email.ts:429** _(system-sweep)_ — UPDATE against tenant-scoped table `inbox_messages` without tenant filter (UPDATE inbox_messages SET ... WHERE id = ${row.id}). A write by id alone can modify rows belonging to other tenants if IDs collide or attacker-controlled IDs are used.
  - _Why false positive:_ Per-row housekeeping update in a global backfill loop; updates only body fields on rows selected by the sweep, with no tenant-facing mixing.
- **server/embeddings.ts:259** _(upstream-verified)_ — Use of sql.raw() with a vector literal derived from user/model input in a query against tenant-scoped table `memory_entries` (embedding vector literal injected via sql.raw(`'${vec}'::vector`)). Using sql.raw on request-derived/model-derived data is injection-prone and bypasses Drizzle parameterization.
  - _Why false positive:_ The flagged raw vector expression is inside a query already constrained by AND tenant_id = ${tenantId} a few lines below/within the same WHERE.
- **server/felix-brain.ts:23** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` without any tenant_id filter (reads rows across all tenants). This rehydrate routine performs a cross-tenant read of in-flight runs and then materializes per-tenant in-memory state, violating the requirement that every read against a tenant-scoped table be constrained to the caller's tenant.
  - _Why false positive:_ Boot-time rehydration intentionally scans all in-flight runs and stores each row under its own row-carried tenant_id in isolated in-memory state.
- **server/firecrawl.ts:231** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) but does not include a tenant_id constraint. Writes to tenant-scoped tables must always be scoped to the caller's tenant in the WHERE clause.
  - _Why false positive:_ The row id being updated came from an immediately prior SELECT scoped by url and tenantId, so the update target was ownership-checked in the same call path.
- **server/firecrawl.ts:372** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant_id constraint (in crawl loop). This allows a write that is not explicitly scoped to the caller's tenant.
  - _Why false positive:_ The updated id comes from the immediately preceding SELECT filtered by pageUrl and tenantId, so tenant ownership is verified earlier in the same path.
- **server/google-drive.ts:879** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) but omits tenant_id in the WHERE clause. Writes must include tenant scoping to prevent cross-tenant modification.
  - _Why false positive:_ This update occurs only after the function fetched the project and explicitly checked project.tenantId === tenantId in the same call path.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals is executed without a tenant_id filter. This reads pending/unverified code proposals across all tenants.
  - _Why false positive:_ Admin health-audit sweep intentionally scans stale proposals globally for maintenance/reporting and optional archival, not for tenant-facing mixed output.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks is executed without tenant_id filtering. The query lists enabled tasks that never ran across the entire DB.
  - _Why false positive:_ Global maintenance audit checks stuck heartbeat tasks across tenants and only flags/disables housekeeping state per row, without tenant data mixing.
- **server/heartbeat.ts:2679** _(upstream-verified)_ — UPDATE on tenant-scoped table heartbeat_tasks executed without tenant_id constraint: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}. This updates approval state without verifying tenant ownership.
  - _Why false positive:_ newTask.id comes from storage.createHeartbeatTask in the same flow with tenantId supplied, so this immediate status update targets the just-created row.
- **server/heartbeat.ts:3152** _(upstream-verified)_ — UPDATE on tenant-scoped table heartbeat_tasks executed without tenant_id constraint: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}. This write occurs in a chat-driven handler and lacks explicit tenant scoping.
  - _Why false positive:_ Row is created immediately above with caller tenantId and update targets the returned server-created newTask.id in the same call path.
- **server/job-queue.ts:160** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT on agent_jobs without scoping by tenant_id — it claims pending jobs across all tenants.
  - _Why false positive:_ Global worker claim loop intentionally operates across tenants and returns rows with their own tenant_id for per-row handling.
- **server/job-queue.ts:196** _(system-sweep)_ — reclaimExpiredLeases updates agent_jobs (marking terminal/requeued) without any tenant_id WHERE clause — affects rows across all tenants.
  - _Why false positive:_ Platform-wide lease-reclaim housekeeping mutates only job status/lease fields across tenants and does not expose mixed tenant data.
- **server/job-queue.ts:238** _(internal-worker-pk)_ — completeJob updates agent_jobs status to 'succeeded' by id/attempts but does not include tenant_id in the WHERE clause, allowing cross-tenant completion if an id is abused.
  - _Why false positive:_ Worker completion updates a server-generated job row by id plus attempts fencing; id is from claimed job context, not untrusted request input.
- **server/job-queue.ts:281** _(internal-worker-pk)_ — failJob reads max_attempts and later updates agent_jobs without constraining by tenant_id (SELECT and UPDATE are unscoped), enabling cross-tenant mutation by id.
  - _Why false positive:_ Worker failure path reads/updates server-generated job row by id with attempts fencing; not called from an untrusted tenant request boundary.
- **server/knowledge-diversity-monitor.ts:266** _(system-sweep)_ — runKnowledgeDiversityMonitor executes a SELECT grouping agent_knowledge by tenant_id/persona_id (scanning all tenants) and subsequently loads embeddings per tenant — the initial SELECT touches tenant-scoped data without restricting to a caller tenant.
  - _Why false positive:_ Explicit periodic all-tenant monitor iterates tenant/persona pairs and snapshots each pair with its own row-carried tenant_id.
- **server/lib/action-ledger.ts:268** _(system-sweep)_ — SELECT from tenant-scoped table `action_attempts` in listUnknownAttemptsAllTenants() has no tenant_id constraint (cross-tenant read).
  - _Why false positive:_ Function is explicitly cross-tenant for reconciler maintenance; rows carry tenant_id and downstream writes use each row's own tenant.
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — UPDATE of `action_attempts` in sweepStaleAttempts() does not constrain by tenant_id and will modify rows across tenants.
  - _Why false positive:_ Cross-tenant stale-attempt sweep is bounded housekeeping that only flips state/error fields and does not mix tenant data into outputs.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Unscoped read over tenant-scoped `projects` table: the aggregate query SELECT tenant_id, count(*) ... FROM projects has no WHERE tenant_id = <...> filter and thus reads counts across all tenants.
  - _Why false positive:_ Boot-time/backfill maintenance intentionally aggregates per-tenant counts across snapshot tenants and does not return tenant data to tenant-facing surfaces.
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — scanOutcomePromotions runs a SELECT on the tenant-scoped table `deliverable_grade_events` without constraining by tenant_id (reads across all tenants). This violates the invariant that every read against a tenant-scoped table must be constrained to the caller's tenant and risks cross-tenant data exposure.
  - _Why false positive:_ Nightly optimizer scan intentionally aggregates across tenants, keeps tenant_id per row, and writes per-tenant eval files/manifest rather than returning mixed tenant-facing data.
- **server/lib/restraint-trust.ts:88** _(upstream-verified)_ — Use of sql.raw() with a request-derived value (the chosen Beta column name) in the UPDATE statement. Although the code whitelists column names earlier, passing a runtime-derived value into sql.raw() is a SQL-injection/isolation risk and violates the rule forbidding sql.raw() or string-interpolated SQL with request-derived values.
  - _Why false positive:_ Update is explicitly scoped by tenant_id/persona_id/category; finding is about sql.raw injection, not cross-tenant isolation.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT against tenant-scoped table `agent_runs` is executed without a tenant_id WHERE clause (queries all tenants).
  - _Why false positive:_ Read-only housekeeping audit intentionally scans all tenants and carries tenant_id per row in bounded report; no tenant-facing mixed content prompt/response shown.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT against tenant-scoped table `mind_tickets` is executed without a tenant_id WHERE clause (queries all tenants).
  - _Why false positive:_ Read-only housekeeping audit intentionally scans all tenants and carries tenant_id per row in bounded report; no tenant-facing mixed content prompt/response shown.
- **server/minds-engine.ts:501** _(upstream-verified)_ — Query reads from tenant-scoped table `messages` without an explicit tenant_id constraint. The SELECT: `SELECT m.role, m.content FROM messages m JOIN conversations c ON c.id = m.conversation_id AND c.tenant_id = ${tenantId} WHERE m.conversation_id = ${run.conversationId}` does not include `m.tenant_id = ${tenantId}` (or equivalent), violating the project invariant that every access to tenant-scoped tables must include an application-level tenant filter.
  - _Why false positive:_ Messages are joined through conversations constrained to c.tenant_id = tenantId, and filtered by that conversation_id in the same query path.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` without tenant_id filter: this startup validation queries id, provider, tenant_id, expires_at, refresh_token, consecutive_failures across all tenants (WHERE is_active = TRUE). Reading sensitive columns (refresh_token) across tenants risks leaking or mis-scoping tenant data.
  - _Why false positive:_ Startup-wide subscription validation is a maintenance loop across tenants; it processes each row with its own tenant_id and does not return tenant data to tenant-facing output.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' (no tenant_id filter). This reads rows across tenants to build a tenant list — a cross-tenant read on a tenant-scoped table without an explicit tenant constraint.
  - _Why false positive:_ Boot-time auto-reconnect intentionally enumerates tenant_ids for provider maintenance and handles each tenant separately.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` without tenant_id filter in proactiveTokenRefresh: it queries provider, tenant_id, expires_at, is_active, consecutive_failures WHERE is_active = TRUE (global scan across tenants).
  - _Why false positive:_ Proactive token refresh is a platform-wide maintenance sweep that iterates rows using each row's tenant_id and performs per-tenant refreshes.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE in forceRefreshAllSubscriptions performs a global scan of a tenant-scoped table without tenant scoping. This enumerates tenants and then refreshes tokens across them.
  - _Why false positive:_ Force-refresh is an explicit daily cron maintenance sweep across tenants, using per-row tenant_id without mixing tenant content into one output.
- **server/orchestrator-ledger.ts:451** _(upstream-verified)_ — Use of sql.raw() with a value derived from caller-controlled ledgerKey/tenantId when invoking pg_advisory_xact_lock (possible SQL injection / unsafe interpolation).
  - _Why false positive:_ Advisory lock call is not a tenant-table access; surrounding persistence operations are scoped by tenant_id, so this is not a cross-tenant isolation issue.
- **server/orchestrator-ledger.ts:466** _(internal-worker-pk)_ — UPDATE against tenant-scoped table memory_entries does not constrain the update by tenant_id (WHERE clause is only WHERE id = ${row.id}). Per-invariant, every write against tenant-scoped tables must include tenant filtering.
  - _Why false positive:_ Update uses row.id obtained server-side from a prior tenant-scoped SELECT inside the same transaction; id is not taken from an untrusted request boundary.
- **server/pace-control.ts:85** _(global-table)_ — SELECT against heartbeat_logs in checkPace() has no tenant_id filter; this reads a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is described as platform-global pace/heartbeat logging
- **server/pace-control.ts:140** _(global-table)_ — SELECT against heartbeat_logs in getPaceSnapshot() has no tenant_id filter; dashboard/debug snapshot will surface cross-tenant data.
  - _Why false positive:_ heartbeat_logs is a non-tenant-scoped global table; snapshot reads global counters by design
- **server/pace-control.ts:147** _(global-table)_ — SELECT grouping by persona_name in getPaceSnapshot() queries heartbeat_logs without tenant scoping (no tenant_id in WHERE).
  - _Why false positive:_ persona grouping is over heartbeat_logs, which has no tenant_id and is global by schema
- **server/persona-export.ts:39** _(global-table)_ — SELECT from skills: `SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ...` lacks tenant_id filtering. As written it may return skills from other tenants.
  - _Why false positive:_ skills has no tenant_id by schema, so tenant scoping is not applicable here
- **server/plan-executor.ts:1024** _(system-sweep)_ — SELECT scanning for stale 'executing' plans (SELECT id, tenant_id, status, plan_json, execution_log FROM plans WHERE status = 'executing' AND updated_at < ...) has no tenant_id filter and enumerates rows across all tenants.
  - _Why false positive:_ boot-time recovery intentionally scans stale plans across tenants and then mutates rows using each row's own tenant_id
- **server/plan-executor.ts:1090** _(system-sweep)_ — SELECT searching for recent transientFailures (SELECT ... FROM plans WHERE status='failed' AND ceo_decision='approved' ... ) lacks a tenant_id constraint and may read/consider failed plans from all tenants.
  - _Why false positive:_ global recovery sweep over failed plans; candidates carry tenant_id and follow-on handling is per-row housekeeping/recovery
- **server/platform-admin-conversations.ts:18** _(upstream-verified)_ — Cross-tenant SELECT from conversations is performed without a tenant_id constraint (db.select().from(conversations).where(and(eq(conversations.id, conversationId), isNull(conversations.deletedAt))). Although guarded by isPlatformAdmin(req), the SQL itself reads a tenant-scoped table without tenant scoping.
  - _Why false positive:_ function enforces isPlatformAdmin(req) immediately before the cross-tenant admin lookup
- **server/process-governor.ts:898** _(upstream-verified)_ — UPDATE to agent_desks uses WHERE id = ${desk.id} with NO tenant filter. This mutates a tenant-scoped table by id only and can update rows belonging to other tenants if ids collide or are global.
  - _Why false positive:_ desk ids come from a prior SELECT constrained by ad.tenant_id = tenantId in the same call path
- **server/recurring-messages.ts:265** _(system-sweep)_ — Unscoped SELECT against tenant-scoped table `agent_knowledge` in `_runDueInner`. The heartbeat runner issues `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` with no tenant_id constraint, returning rows across all tenants.
  - _Why false positive:_ heartbeat runner intentionally scans due jobs across tenants and processes each row using its carried tenant_id
- **server/recurring-messages.ts:307** _(system-sweep)_ — UPDATE to tenant-scoped table `agent_knowledge` lacks tenant constraint. The update `UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}` does not include `AND tenant_id = <tenant>` allowing a write by id that is not additionally restricted to the owning tenant.
  - _Why false positive:_ update occurs inside per-row maintenance sweep over rows already selected with their tenant_id; no tenant-mixed output
- **server/reference-learner.ts:296** _(upstream-verified)_ — UPDATE to tenant-scoped table `memory_entries` missing tenant constraint. The code runs `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}` without verifying tenant_id/persona_id ownership.
  - _Why false positive:_ storedId is obtained just above from a SELECT on memory_entries scoped by tenant_id and persona_id in the same path
- **server/research-engine.ts:171** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_sessions' performed without any tenant_id constraint (cleanupZombieSessions uses a global UPDATE to mark running sessions as completed). This can modify rows across tenants, violating tenant isolation.
  - _Why false positive:_ Startup/maintenance cleanup globally marks stale running sessions completed; it only mutates housekeeping/status fields and returns counts/ids to logs, not tenant-facing mixed data.
- **server/research-engine.ts:460** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. If project_notes is a tenant-scoped table (tenant_id NOT NULL), this write either fails or risks misattributing/omitting the tenant context.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively through project_id; ownership is verified just above with assertProjectInTenant(projectId, session.tenantId).
- **server/research-engine.ts:468** _(fk-transitive)_ — INSERT into project_notes (within the findings loop) does not set tenant_id. Same risk as above — writes into a tenant-scoped table without the caller's tenant context.
  - _Why false positive:_ project_notes has no tenant_id column and inserts are correctly scoped through verified parent project_id after assertProjectInTenant.
- **server/routes.ts:476** _(system-sweep)_ — UPDATE on tenant-scoped table `order_lookup_codes` runs without qualifying tenant_id for the caller: `UPDATE order_lookup_codes SET tenant_id = ${ownerTenantId()} WHERE tenant_id IS NULL`. This mutates rows across tenants in a blanket way.
  - _Why false positive:_ One-time bootstrap/backfill for legacy single-store rows sets missing tenant_id to the trusted owner tenant only; admin maintenance, not tenant-request handling.
- **server/routes.ts:485** _(system-sweep)_ — DELETE on tenant-scoped table `order_lookup_codes` performed without tenant scoping: `DELETE FROM order_lookup_codes WHERE expires_at < NOW()`. This removes rows for all tenants rather than only the intended tenant context.
  - _Why false positive:_ Global expiry prune is a housekeeping sweep deleting expired lookup codes across tenants and does not return or mix tenant data.
- **server/routes.ts:626** _(system-sweep)_ — SELECT from tenant-scoped table `projects` (joined to tenants) without a tenant_id WHERE clause: the startup backfill query scans all projects (`SELECT p.id, p.name, p.tenant_id ... WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = ''`).
  - _Why false positive:_ Startup drive-folder backfill intentionally scans all projects and processes each row with its own carried tenant_id via ensureProjectFolder(..., p.tenant_id, ...).
- **server/routes.ts:978** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` without tenant filter in the uploads ownership-check middleware: all owner rows for a basename are fetched (`.from(fileStorage).where(eq(fileStorage.filename, baseName))`) and then ownership checked in application logic. This reads across tenants.
  - _Why false positive:_ This middleware authorizes upload/delivery asset access via signed URL or authenticated tenant, and the unscoped read fetches only tenantId owners for basename to validate capability/ownership without exposing row content.
- **server/routes.ts:4652** _(fk-transitive)_ — INSERT into tenant-scoped table (project_notes) does not set tenant_id. The code inserts project notes via raw SQL: INSERT INTO project_notes (project_id, note, author) VALUES (...) without including tenant_id, violating the invariant that tenant-scoped writes must include the caller's tenant.
  - _Why false positive:_ project_notes has no tenant_id column; parent conversation is tenant-verified and project_id is derived from a tenant-scoped conversations query before insert.
- **server/routes/conversations.ts:119** _(internal-worker-pk)_ — UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} executes without constraining by tenant_id. This updates a tenant-scoped table (conversations) using only the conversation id, allowing cross-tenant modification if an attacker can supply or guess conv.id.
  - _Why false positive:_ UPDATE targets the freshly server-created conversation by its server-generated primary key conv.id; id is not from an untrusted cross-tenant boundary.
- **server/routes/conversations.ts:123** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id}) does not set tenant_id. If project_conversations is tenant-scoped (likely), this both violates the schema expectation and allows cross-tenant associations or SQL errors.
  - _Why false positive:_ project_conversations has no tenant_id column and is a join/child row scoped transitively by verified tenant-owned project_id and server-created conversation_id.
- **server/routes/conversations.ts:125** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is performed without a tenant_id constraint. Projects are tenant-scoped elsewhere in the code, so this write can affect another tenant's project rows.
  - _Why false positive:_ project ownership was verified just above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before this update
- **server/routes/conversations.ts:254** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author) VALUES (...) is executed without a tenant_id column/value. project_notes appears to be tenant-scoped elsewhere, so this INSERT writes tenant-scoped data with no tenant_id, risking cross-tenant data leakage or DB constraint failures.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through validated tenant-owned projects/project_id
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE statement marks a message read without scoping by tenant_id (missing tenant predicate). This allows an authenticated tenant to change rows belonging to other tenants by ID.
  - _Why false positive:_ same handler first selects the message by id and tenant_id, then updates that fetched row by its server-read primary key
- **server/routes/projects.ts:165** _(fk-transitive)_ — Subqueries counting related rows (project_files, project_notes, project_conversations) do not constrain the tenant on the child tables. E.g. '(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)' lacks 'AND tenant_id = ${tenantId}'.
  - _Why false positive:_ project_files, project_notes, and project_conversations have no tenant_id and are child tables counted via tenant-scoped parent projects rows
- **server/routes/projects.ts:703** _(fk-transitive)_ — SELECT from project_files omits tenant constraint. 'SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC' does not filter by tenant_id and can leak cross-tenant rows if project_files rows with that project_id exist for other tenants.
  - _Why false positive:_ project_files has no tenant_id and rows are read only after verifying the parent project belongs to tenantId
- **server/routes/projects.ts:726** _(fk-transitive)_ — SELECT file record for download omits tenant constraint. 'SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1' does not include tenant_id.
  - _Why false positive:_ project_files has no tenant_id and the parent project is tenant-checked immediately above; child access is transitively scoped by project_id
- **server/seed.ts:42** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped but the query aggregates across all tenants.
  - _Why false positive:_ boot-time snapshot import intentionally checks global session count during a maintenance seed path, not a tenant-facing surface
- **server/seed.ts:3639** _(system-sweep)_ — DELETE from tenant-scoped table `heartbeat_tasks` executed without constraining by tenant_id. This DELETE will run across all tenants because there's no WHERE tenant_id = ... clause.
  - _Why false positive:_ boot-time housekeeping purges stale heartbeat task rows platform-wide and does not expose tenant data
- **server/seed.ts:3667** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` (REPLACE on fact) executed without a tenant_id constraint. This will modify rows for all tenants.
  - _Why false positive:_ boot-time normalization updates housekeeping content across rows platform-wide and does not return or mix tenant data
- **server/seed.ts:3693** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` (setting model) executed without a tenant_id constraint, affecting all tenants.
  - _Why false positive:_ idempotent boot-time migration of legacy model ids across all conversation rows is platform maintenance, not tenant-facing data mixing
- **server/self-reflection.ts:382** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = <id> without also constraining tenant_id. Although the id was discovered via a prior tenant-scoped SELECT, the UPDATE should still enforce tenant scoping to prevent race/TOCTOU or misuse.
  - _Why false positive:_ the row id being updated was selected immediately above from memory_entries with tenant_id = tenantId
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from messages without tenant constraint in analyzeConversation — messages for a conversation are fetched without constraining tenant_id.
  - _Why false positive:_ analyzeConversation first verifies the conversation row with id + tenantId, then reads child messages by that validated conversationId in the same call path.
- **server/skillify.ts:221** _(global-table)_ — SELECT from personas by id with NO tenant constraint — personaRows are looked up using persona id without verifying the persona belongs to the caller's tenant.
  - _Why false positive:_ schema note says personas has no tenant_id column, so this lookup is on a non-tenant-scoped table.
- **server/skills-marketplace.ts:209** _(global-table)_ — SELECT id FROM skills WHERE name = ${template.name} with NO tenant filter — may return or treat another tenant's installed skill as existing (cross-tenant lookup).
  - _Why false positive:_ skills schema shows no tenant_id, so checking for an existing installed skill by name is against a global table.
- **server/skills-marketplace.ts:235** _(global-table)_ — SELECT * FROM skills WHERE id = ${skillId} with NO tenant constraint — exporting a skill by id can leak another tenant's skill data if skills is tenant-scoped.
  - _Why false positive:_ skills has no tenant_id per schema, so exporting by id is a read from a global table rather than cross-tenant access.
- **server/stability-watchdog.ts:177** _(system-sweep)_ — SELECT FROM heartbeat_logs for error counts lacks tenant constraint — query aggregates error counts across all tenants, then acts based on results, which may disclose cross-tenant data or trigger cross-tenant remediation.
  - _Why false positive:_ heartbeat_logs has no tenant_id and this is a platform-wide watchdog aggregate that disables tasks using each task's own tenantId, not returning mixed tenant data.
- **server/storage.ts:959** _(system-sweep)_ — SELECT on tenant-scoped table `memory_entries` (getMemoriesWithoutEmbeddings) returns rows across all tenants with no tenant_id filter.
  - _Why false positive:_ this helper appears to support global embedding backfill/maintenance over tenant rows and only selects rows for housekeeping, not tenant-facing output.
- **server/storage.ts:969** _(system-sweep)_ — SELECT on tenant-scoped table `agent_knowledge` (getKnowledgeWithoutEmbeddings) runs without any tenant_id constraint, exposing knowledge rows for all tenants.
  - _Why false positive:_ this helper likewise appears to be a global embedding backfill selector over tenant rows for housekeeping only, with no mixed tenant response surface shown.
- **server/storage.ts:979** _(system-sweep)_ — UPDATE statement in archiveExpiredMemories updates `memory_entries` across all tenants (no tenant_id constraint).
  - _Why false positive:_ archiveExpiredMemories is a platform-wide lifecycle housekeeping update that only changes status on expired rows and returns counts/ids, not tenant content.
- **server/storage.ts:995** _(system-sweep)_ — UPDATE statement in archiveStaleMemories updates `memory_entries` without any tenant_id filter (applies to all tenants).
  - _Why false positive:_ archiveStaleMemories is another global maintenance sweep mutating only archival status across rows, not exposing mixed tenant data.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE on tenant-scoped table 'custom_tools' increments usageCount using WHERE id = <tool.id> only, without tenant_id in the WHERE clause. This mutates a tenant-scoped row without an explicit tenant constraint.
  - _Why false positive:_ tool row was selected immediately above with tenant_id = tenantId, so updating that fetched row by its id is within same call path
- **server/tools.ts:2926** _(fk-transitive)_ — SELECT against project_files lacks an explicit tenant filter. The duplicate-check query `SELECT id FROM project_files WHERE project_id = ${params.id} ...` does not constrain by tenant_id (even though project ownership was checked earlier), violating the invariant that every read on tenant-scoped tables must include tenant scoping.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by tenant-owned parent project; owner check on projects is done just above
- **server/tools.ts:4546** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` without passing tenant_id. The INSERT statement (VALUES ...) does not include tenant_id even though project_notes is per-project/tenant and DB columns are NOT NULL with no default.
  - _Why false positive:_ project_notes has no tenant_id and insert is guarded by assertProjectInTenant on the parent project
- **server/tools.ts:4550** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` without passing tenant_id. The code inserts a project_files row (project_id, file_name, ...) but does not set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and insert is guarded by assertProjectInTenant on the parent project
- **server/tools.ts:6465** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` without passing tenant_id. The project_files INSERT performed in generate_audio omits tenant_id even though the insert may create tenant-scoped rows.
  - _Why false positive:_ project_files has no tenant_id and insert occurs only after assertProjectInTenant on parent project
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' lacks tenant_id constraint in WHERE clause (`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`). Every UPDATE on tenant-scoped tables must include a tenant filter to enforce isolation (no RLS present).
  - _Why false positive:_ workflow was selected immediately above with tenant_id = tenantId, then updated by that fetched row id in same call path
- **server/tools/domains/files/handlers.ts:937** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files omits any tenant_id column, so a project_files row could be written without an explicit tenant ownership column.
  - _Why false positive:_ project_files has no tenant_id and is linked via project_id to tenant-scoped projects; projId was derived from a tenant-filtered projects join.
- **server/tools/domains/files/handlers.ts:940** _(upstream-verified)_ — UPDATE against tenant-scoped table projects lacks an explicit tenant constraint. The UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} does not include tenant_id in the WHERE, so it can affect a project belonging to a different tenant if projId were tampered with or mis-validated upstream.
  - _Why false positive:_ projId comes from a prior SELECT joining conversations to projects with p.tenant_id = tenantId, so the updated project was already tenant-verified in this call path.
- **server/tools/domains/knowledge/handlers.ts:91** _(upstream-verified)_ — UPDATE to tenant-scoped table knowledge_triples missing tenant constraint. In the contradictions loop the code runs UPDATE knowledge_triples SET valid_until = ${validFrom} WHERE id = ${row.id} without also constraining tenant_id, violating the requirement that writes must be scoped to caller tenant.
  - _Why false positive:_ row.id values come only from the immediately preceding contradictions SELECT filtered by tenant_id = tenantId, so each updated row was tenant-scoped earlier in the same path.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys executed without any tenant_id filter (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). This reads tenant-scoped key material across all tenants.
  - _Why false positive:_ provider_keys schema shows no tenant_id; this admin-tenant-only audit is querying a platform-global table.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM provider_keys executed without tenant_id constraint. Non-filtered aggregation over provider_keys can expose cross-tenant data.
  - _Why false positive:_ provider_keys has no tenant_id and is treated as platform-global; query is additionally limited to tenantId === 1 admin path.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — SELECT COUNT/MAX executed against subsystem table (${spec.table}) via sql.raw without any tenant_id constraint — tenant-scoped tables (e.g. knowledge_nudges, tool_performance) are being aggregated across all tenants.
  - _Why false positive:_ This is a platform-wide observability/maintenance aggregate over subsystem tables, returning only counts/timestamps and not tenant content.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — SELECT COUNT and pg_total_relation_size against memory tables (e.g. messages, agent_knowledge, memory_entries) uses sql.raw(`... FROM ${tbl.name}`) with no tenant_id filtering. This reads tenant-scoped data across all tenants.
  - _Why false positive:_ Memory-growth check is a platform-wide housekeeping/observability aggregate that reads only row counts and relation sizes, not tenant data content.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Query against tool_performance (SELECT count(*) FROM tool_performance WHERE last_success_at > ...) has no tenant_id constraint — may aggregate/observe tenant-scoped metrics across all tenants.
  - _Why false positive:_ Dormant-tool detector is a platform-wide observability check using aggregate counts only, with no tenant content returned or mixed.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM messages WHERE created_at > ... runs across the entire messages table with no tenant_id filter. messages is tenant-scoped, so this is an unscoped cross-tenant read.
  - _Why false positive:_ This message-count probe is part of a global wiring/health check and only reads an aggregate count, not tenant row data.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... omits any tenant_id filtering and therefore aggregates tenant-scoped tool performance across all tenants.
  - _Why false positive:_ Boot-time invariant/housekeeping check aggregates distinct tool names over 14d to emit platform-level findings, not tenant-facing row data; this is a cross-tenant maintenance diagnostic.
- **server/embeddings.ts:809** _(internal-worker-pk)_ — UPDATE to tenant-scoped table (`memory_entries`) performs an unconditional write when tenantId is not supplied: UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}. This allows writing a memory row without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/embeddings.ts:684 — storeEmbeddingVec is only used by embedding/backfill workers (server/embeddings.ts:693-706; server/research-engine.ts:518,1135; server/dream-consolidation.ts:363,422; server/storage.ts:806,818), with IDs obtained from database-created rows rather than request input. The UPDATE targets a globally unique serial primary key, and this worker path does not expose an attac
- **server/google-workspace.ts:2307** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — no tenant_id constraint. This updates a tenant-scoped resource by id only, allowing cross-tenant mutation if sessionId is unverified.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} lacks a tenant_id constraint — reading a tenant-scoped personas table without scoping to the caller's tenant. This can expose other tenants' persona definitions.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): personas table has no tenant_id column (platform-global personas); tenant linkage lives in trust/skills rows fetched separately.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without any tenant constraint. updateProjectBrain accepts no tenantId and reads a tenant-scoped table by id only, enabling cross-tenant data access.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... has NO tenant filtering. This enumerates projects (and later conversations/messages) across all tenants, causing cross-tenant data exposure.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE on sculptor_sessions in monitorSession omits tenant constraint: `UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}` does not verify tenant ownership (monitorSession is passed tenantId but not used in this WHERE).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:106 monitorSession is called only with session.id freshly created at 74-82 (server-generated PK); no caller passes a request-derived id
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE on sculptor_sessions sets review_result without tenant constraint: `UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}` can update a row belonging to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:501** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger without scoping by tenant: `UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}` can modify another tenant's ledger row because tenant_id is not constrained and the function lacks tenant context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/storage.ts:937** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `memory_entries` (update(memoryEntries).set({ embedding }).where(eq(memoryEntries.id, id))) lacks tenant scoping — no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:1409** _(upstream-verified)_ — getAllDataForExport performs broad SELECTs from many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with NO tenant_id constraints, exporting data for every tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/auto-project.ts:135** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is missing an explicit tenant filter. The query `SELECT role, LEFT(content, 300) as content FROM messages WHERE conversation_id = ${conversationId} ...` does not constrain rows by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-project.ts:95-100 verifies the conversation belongs to the tenant (SELECT ... FROM conversations WHERE id=conversationId AND tenant_id=tenantId; returns null if not found) before the messages read at 112-116 scoped by that same conversation_id.
- **server/chat-engine.ts:3926** _(upstream-verified)_ — Call to stuck-diagnostics.recordErrorForRetryStormDetection(conversationId, ...) is invoked with only conversationId and error text — no tenantId is provided. If that function persists or queries diagnostic/storm records by conversationId without tenant scoping, tenant isolation may be bypassed.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): recordErrorForRetryStormDetection is an in-memory diagnostics counter keyed by conversationId (stuck-diagnostics); it performs no DB read/write and returns no tenant data.
- **server/chat-engine.ts:4161** _(internal-worker-pk)_ — Call to stuck-diagnostics.recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) does not include tenantId. If the module reads/writes per-conversation diagnostic/state rows, it may perform operations without tenant_id constraints, violating tenant isolation.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): recordToolCallForStuckDetection is in-memory loop diagnostics keyed by conversationId; no DB write occurs.
- **server/email.ts:370** _(system-sweep)_ — SELECT from tenant-scoped table `inbox_messages` without constraining tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This allows cross-tenant reads/logic based on a row from the wrong tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): platform primary-inbox poller (single system account); message_id dedupe against its own inbox rows.
- **server/google-drive.ts:866** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' is performed with WHERE eq(projects.id, projectId) with no tenant_id constraint. Per policy every read against a tenant-scoped table must be constrained to the caller's tenant via tenant_id in the WHERE clause.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): ensureProjectFolder selects only tenantId/driveFolderId and immediately throws on project.tenantId!==tenantId at google-drive.ts:551-553 before returning or writing anything.
- **server/job-queue.ts:336** _(internal-worker-pk)_ — cancelJob updates agent_jobs status by id without a tenant_id filter, allowing cancellation of jobs across tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:125** _(internal-worker-pk)_ — UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} (persistSpanClose) lacks a tenant_id constraint — allows closing/updating spans without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — SELECT against tenant-scoped table mcp_api_keys is done WITHOUT a tenant_id filter (lookup by key_prefix only). This reads a tenant-scoped table without constraining to a tenant, violating the invariant that every tenant-scoped read must be pinned by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/mcp-api-keys.ts:166 — verifyApiKey compares the sha256 of the FULL presented key against key_hash in the same function (mcp-api-keys.ts:163-180); the credential itself is the access control and selects the tenant.
- **server/lib/system-probes.ts:163** _(upstream-verified)_ — Unscoped SELECT against tenant-scoped table `messagesTable` — db.select(...).from(messagesTable) executes without a WHERE tenant_id = ... filter, violating tenant isolation invariant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Reclassified by main-agent review: only callsite (tools.ts check_system_status) is admin-gated (ADMIN_TENANT_ID) two lines above the call. server/lib/system-probes.ts:163 db.select(count(*)).from(messagesTable) has no tenant filter; messages has tenant_id (shared/schema.ts:142-150). checkSystemStatus (119) is exposed as the check_system_status tool (server/tools.ts:3501, definitions.ts:24-31, 
- **server/process-governor.ts:1235** _(upstream-verified)_ — UPDATE governance_rules increments trigger_count using WHERE id = ${rule.id} but does not include tenant_id = ${tenantId}. This allows cross-tenant modification of governance rules by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/routes.ts:1926** _(signed-url)_ — SELECT from tenant-scoped table `presenter_sessions` by token with NO tenant_id constraint (GET /api/presenter/:token). Reading presenter_sessions without tenant scoping relies entirely on the token capability and returns tenant-scoped rows unchecked by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/routes.ts:2057 — presenter token is crypto.randomBytes(16).hex (routes.ts:2140) — an unguessable capability URL for public slide viewing; the token IS the access control by design.
- **server/routes/admin.ts:910** _(upstream-verified)_ — Query against `heartbeat_tasks` (SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3 ...) has no tenant_id constraint, but `heartbeat_tasks` is used elsewhere as tenant-scoped (queries/deletes with tenant_id). This endpoint therefore reads heartbeat tasks across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/routes/admin.ts:562 admin-only guard; heartbeat_tasks read is platform-admin diagnostics
- **server/self-improvement.ts:434** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` lacks a tenant_id constraint. The code updates an experiment by id only: .where(eq(experiments.id, experiment.id)) — this allows a row to be modified across tenants if ids collide or in case of incorrect context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from messages without tenant constraint — recentMsgs are queried by conversationId but the query omits tenant_id, allowing potential cross-tenant disclosure if conversationId is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): distillIntent calls analyzeConversation(conversationId, tenantId) at skillify.ts:89 first, which asserts conversation ownership via WHERE id AND tenantId and throws if not found (skillify.ts:196-199). The messages read at 91-94 is by conversationId whose tenant ownership is already proven; messages FK-cascade to conversations.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from messages without tenant constraint — recentMsgs fetched for skillifyConversation lack tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): distillIntent calls analyzeConversation(conversationId, tenantId) at skillify.ts:89 first, which asserts conversation ownership via WHERE id AND tenantId and throws if not found (skillify.ts:196-199). The messages read at 91-94 is by conversationId whose tenant ownership is already proven; messages FK-cascade to conversations.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — UPDATE on tenant-scoped table 'felix_proposals' without constraining tenant_id in WHERE clause (stampArgsEmbedding updates args_embedding by id only). This allows a row from another tenant (or ambiguity) to be modified.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'custom_tools' uses WHERE id = <tool.id> only (setting implementation) with no tenant_id constraint. This updates a row by id without verifying tenant ownership in SQL.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table 'custom_tools' (revertToolRow) is performed with WHERE id = <tool.id> only and lacks a tenant_id constraint. Deleting by id without tenant scoping can affect the wrong tenant row if ids are ever misaligned.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/tools.ts:8978** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `proposed_skills` filters only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)). Missing tenant_id constraint in WHERE, allowing cross-tenant updates.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9006** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `proposed_skills` filters only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)). Missing tenant_id constraint in WHERE, allowing cross-tenant updates.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9021** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `proposed_skills` filters only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)). Missing tenant_id constraint in WHERE, allowing cross-tenant updates.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` without a tenant_id constraint (loads candidate pairings across all tenants). This violates the invariant that all reads against tenant-scoped tables must be constrained to the caller's tenant and allows cross-tenant data exposure / ambiguous-pairing logic to leak or be abused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (settleVentureReservation updates by id only). This allows a caller that supplies/obtains a reservation id from another tenant to modify another tenant's ledger row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:168** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger does not constrain by tenant_id (releaseVentureReservation updates by id only). This may allow releasing/altering another tenant's reservation row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
