# Tenant-Isolation Audit Report

_Generated 2026-08-02T00:41:06.553Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **584/584** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **297** (CRITICAL 54 · HIGH 198 · MEDIUM 45 · LOW 0)
- Precision second-pass (`gpt-5.4`): **236** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (54)

- **server/agentic-engines.ts:243** — autoApplyEligibleInsights performs an UPDATE on ai_insights (status → 'applied') for category IN (...) without any tenant filter — this can modify rows across all tenants.
  - _Fix:_ Do not run an unscoped cross-tenant UPDATE. Either run the sweep per-tenant (adding WHERE tenant_id = ${tenantId}) or otherwise ensure each UPDATE is tenant-scoped. Break the multi-tenant update into per-tenant operations.
- **server/agentic/runs.ts:36** — appendStep updates agent_runs but the UPDATE .where(...) only filters by run id (eq(agentRuns.id, runId)) and does NOT include tenant scoping. This allows modifying another tenant's run if runId is known.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or require/validate the tenantId in the function signature and enforce it on every UPDATE/DELETE/SELECT against tenant-scoped tables.
- **server/agentic/runs.ts:51** — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) but does not constrain by tenant_id; this is an unscoped write to a tenant-scoped table.
  - _Fix:_ Require the caller to provide tenantId and add it to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). All updates must verify tenant ownership before mutating rows.
- **server/agentic/runs.ts:55** — replaceRunState performs an UPDATE on agent_runs using only run id in the WHERE clause (eq(agentRuns.id, runId)) and lacks a tenant_id constraint.
  - _Fix:_ Add a tenant_id filter to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or otherwise verify the run belongs to the caller's tenant before writing. Ensure the API surfaces tenantId where needed.
- **server/agentic/runs.ts:64** — completeRun updates agent_runs and clears/completes the run using only the run id in the WHERE clause; there is no tenant_id constraint, enabling cross-tenant writes.
  - _Fix:_ Constrain the UPDATE with the tenant id (e.g. where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or fetch and validate the run's tenant before performing the update. Apply this pattern to all mutating run APIs.
- **server/agentic/runs.ts:94** — failRun updates agent_runs to set status='failed' using only the run id in the WHERE clause (eq(agentRuns.id, runId)) with no tenant_id filter.
  - _Fix:_ Require and enforce tenant scoping in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or verify ownership prior to updating. All write operations on tenant-scoped tables must include tenant_id checks.
- **server/agentic/runs.ts:102** — pauseRun updates agent_runs to set status='paused' using only the run id in the WHERE clause; tenant_id is not enforced, allowing cross-tenant mutation.
  - _Fix:_ Add tenant filtering to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or accept/require tenantId in the function and validate it. Ensure every UPDATE/DELETE/SELECT on agent_runs is tenant-scoped.
- **server/auto-memorize.ts:74** — INSERT into `agent_knowledge` hardcodes tenant_id = 1 (creates watermark always for tenant 1).
  - _Fix:_ Do NOT hardcode tenant_id. Pass the correct tenantId into setWatermark and INSERT with tenant_id = ${tenantId}. If this watermark is intended to be per-tenant, make the function require tenantId; if global, document explicitly and move to a global table.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL has no tenant_id constraint — can modify another tenant's conversation row.
  - _Fix:_ Include tenant restriction in the UPDATE (e.g. AND tenant_id = ${c.tenant_id}) or perform this update inside a transaction after validating that the conversation belongs to the expected tenant. Always include tenant_id on UPDATE/DELETE to preserve isolation.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table, this INSERT allows creating rows without an explicit tenant_id, breaking the invariant that every tenant-scoped INSERT must include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT into invoice_items (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)). If invoice_items is intentionally not tenant-scoped, document that and ensure any code relying on invoice_items cannot be used to cross-tenant reference; otherwise fail closed and require tenant_id parameter.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table `commitments` executes without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can mutate another tenant's row if row.id is from a different tenant.
  - _Fix:_ Include the tenant guard in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the revert inside the same transactional/locked context where tenant ownership is validated. Prefer re-checking the row's tenant_id and only updating when it matches the expected tenant to enforce fail-closed isolation.
- **server/delivery-pipeline.ts:195** — updateDeliveryLog performs UPDATE on tenant-scoped table `delivery_logs` with WHERE eq(delivery_logs.id, id) but no tenant_id constraint. This allows updating another tenant's delivery row if an attacker or caller supplies an id from a different tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or require/accept tenantId and validate caller ownership before updating. Prefer passing caller tenantId into updateDeliveryLog and always enforce it in the SQL WHERE.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped tables (memory_entries / agent_knowledge) without constraining tenant_id. The UPDATE in storeEmbeddingVec uses WHERE id = ${id} but does not restrict by tenant_id, allowing cross-tenant writes.
  - _Fix:_ Require and pass an explicit tenantId to storeEmbeddingVec (or otherwise validate row ownership) and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Validate that the caller is allowed to update that tenant before performing the UPDATE.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter. This background backfill reads rows across all tenants and then writes (via storeEmbeddingVec) without scoping to a tenant.
  - _Fix:_ Do not enumerate or update tenant-scoped tables without a tenant filter. Either accept an explicit tenantId and filter WHERE tenant_id = ${tenantId}, or iterate per-tenant in a controlled loop (authorized to run per-tenant). Ensure any subsequent updates also include tenant_id checks.
- **server/embeddings.ts:716** — SELECT from memory_entries in backfillEmbeddingVecs lacks tenant_id scoping. The code reads memory_entries across all tenants and then performs UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} with no tenant restriction.
  - _Fix:_ Scope the backfill to a tenant (accept tenantId or iterate tenants), and include tenant_id = ${tenantId} on both the SELECT and the UPDATE. Validate ownership before updating rows to prevent cross-tenant modification.
- **server/embeddings.ts:732** — SELECT from agent_knowledge in backfillEmbeddingVecs has no tenant_id filter and later UPDATE agent_knowledge SET embedding_vec WHERE id = ${row.id} also lacks tenant scoping. This touches tenant-scoped data without ownership checks.
  - _Fix:_ Same as above: run backfill per-tenant or add WHERE tenant_id = ${tenantId} to the SELECT and UPDATE. Ensure the background job cannot touch rows from other tenants.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' performed at boot without scoping by tenant_id — reads rows for all tenants into in-memory taskStateStore.
  - _Fix:_ Add a tenant filter to the query (or run per-tenant): e.g. WHERE tenant_id = ${tenantId} AND status IN (...) or otherwise limit to the tenant(s) this process is allowed to rehydrate. If this boot rehydration must be per-tenant, iterate tenants and query per-tenant rather than selecting all rows.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — update lacks any tenant_id constraint or ownership check. This allows modifying another tenant's session if an attacker supplies a sessionId.
  - _Fix:_ Enforce tenant scoping on the UPDATE. Either add AND tenant_id = ${tenantId} to the WHERE clause, or first SELECT the session by id and ensure its tenant_id matches the caller's tenant before updating. Fail closed if the ownership check fails.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT INTO `project_files` does not include tenant_id. Per-platform invariant every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (and bind it via the parameterized query) and verify the target project_id belongs to that tenant before inserting. Example: INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...). If project_files is not tenant-scoped, document that fact; otherwise make tenant ownership explicit and non-null.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT INTO `project_notes` does not include tenant_id. Every INSERT into tenant-scoped tables must pass tenant_id explicitly (columns are NOT NULL with no default).
  - _Fix:_ Add tenant_id to the INSERT and verify the referenced project belongs to the tenant (e.g. WHERE id = ${newProjectId} AND tenant_id = ${tenantId}) before writing. Example: INSERT INTO project_notes (project_id, note, author, tenant_id, created_at) VALUES (${newProjectId}, ${n.note}, ${n.author ?? 'system'}, ${tenantId}, ${n.createdAt}).
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey() queries the tenant-scoped table `mcp_api_keys` by key_prefix without constraining by tenant_id and later updates last_used_at by id with no tenant check. This reads/writes a tenant-scoped table without an explicit tenant_id WHERE clause (tenant ownership is not enforced in the SQL), violating the app-level tenant-isolation invariant.
  - _Fix:_ When a caller-supplied tenant is available, always include AND tenant_id = <caller tenant> in queries. For the API-key verification path (where caller tenant is not known a priori): ensure key_prefix is globally unguessable and protected by a UNIQUE constraint, then after selecting the row verify and use the returned tenant_id for any subsequent writes. Change the touch update to: UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} (or include the resolved tenantId variable) so every write is constrained to the owning tenant. Audit other auth paths for similar patterns.
- **server/lib/scheduled-post-runner.ts:320** — Postgres array literal (`arrLit`) is constructed by string interpolation from caller-supplied `platforms` and then injected into the SQL (`${arrLit}::text[]`). This is effectively building SQL from request-derived data and risks SQL injection and isolation issues.
  - _Fix:_ Do not build SQL literals from user input. Use parameterized queries (pass an actual JS array to the DB driver or Drizzle helper), or cast a JSON/JSONB parameter to text[] on the server side. If you must build a literal, fully validate+whitelist each element and use parameter placeholders for each element rather than string concatenation.
- **server/plan-executor.ts:117** — Unscoped UPDATE against tenant-scoped table: appendExecutionLog executes `UPDATE plans ... WHERE id = ${planId}` with no tenant_id constraint. This can allow updating a plan belonging to a different tenant if a planId from another tenant is passed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${planId} AND tenant_id = ${expectedTenantId}`) or fetch/validate the plan row's tenant_id first and use it in the UPDATE. Prefer parameterized tenantId derived from the authoritative execution context (not from planner-supplied data).
- **server/plan-executor.ts:513** — Unscoped CAS update on plans: claiming the plan uses `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...` but does not constrain by tenant_id. Without a tenant constraint this may claim and transition plans from any tenant.
  - _Fix:_ Constrain the CAS by tenant (e.g. `WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved'`) or otherwise verify the caller has authority to operate on that tenant's plan. Use the plan's tenant as part of the conditional update to prevent cross-tenant claims.
- **server/plan-executor.ts:527** — Unscoped UPDATE on plans: on empty/invalid plan_json path the code runs `UPDATE plans SET status = 'failed' ... WHERE id = ${planId}` with no tenant filter. This can flip status for a plan belonging to another tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `WHERE id = ${planId} AND tenant_id = ${expectedTenantId}`) or validate plan ownership before performing the update.
- **server/plan-executor.ts:728** — Unscoped final status UPDATE: the final `UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}` lacks a tenant_id constraint. This writes terminal state to a plan without verifying tenant ownership.
  - _Fix:_ Include `AND tenant_id = ${expectedTenantId}` in the WHERE clause (or otherwise ensure the update only affects the plan row belonging to the intended tenant). Validate the plan row's tenant before mutating it.
- **server/plan-executor.ts:778** — Global UPDATE that resets stuck plans: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id` is unscoped and will affect executing plans across all tenants. There is no tenant constraint, so background recovery can modify other tenants' plans.
  - _Fix:_ If intent is to operate per-tenant, restrict the update to a specific tenant (e.g. `AND tenant_id = ${tenantId}`). If the operation must run system-wide, document and gate it (require an explicit admin/system context) and ensure the caller is trusted (explicit ADMIN_TENANT_ID use). Otherwise scope the recovery to the appropriate tenant(s).
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not constrain by tenant_id — a rule could disable a task belonging to another tenant if ids collide.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or use an explicit check of the task's tenant) in the WHERE clause to ensure updates only affect the intended tenant's rows.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} is missing a tenant_id constraint and can modify another tenant's desks if id values overlap.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause (or use an UPDATE referencing the tenant-constrained SELECT) so only the current tenant's rows are modified.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant scoping and may update rules belonging to other tenants.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (or include tenant_id in the update predicate) to ensure only the rule for the current tenant is modified.
- **server/proposal-verifier.ts:108** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) updates a tenant-scoped table without a tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or otherwise verify ownership) in all UPDATE statements against code_proposals to ensure only the intended tenant's rows are modified.
- **server/proposal-verifier.ts:246** — persist() runs UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} without tenant scoping — verification metadata updates could affect other tenants' proposals.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) to the UPDATE, and accept/derive tenantId from a validated server-side context rather than trusting caller-supplied ids.
- **server/proposal-verifier.ts:396** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking rebase_outcome 'hallucinated') lacks tenant scoping and can modify proposals across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or validate ownership) for this and all other updates to code_proposals.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} (NO_LONGER_APPLICABLE branch) updates tenant-scoped rows without a tenant_id constraint, potentially affecting other tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (and require/verify tenant context) before mutating proposal rows.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes the rebased diff without any tenant_id restriction — cross-tenant modification possible.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (or validate the proposal's tenant) to ensure only the current tenant's proposal row is modified.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` uses only the row id in WHERE (WHERE id = ${r.id}) and does not include tenant_id. In a multi-tenant DB this can modify another tenant's row if ids collide or are misrouted.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}` (or use the caller/runner's tenant variable). Always include tenant_id in WHERE for UPDATE/DELETE on tenant-scoped tables, even when the row id was previously read from an unscoped query.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses only id in WHERE (UPDATE memory_entries SET ... WHERE id = ${storedId}) after a tenant-scoped SELECT. The UPDATE lacks tenant_id, allowing cross-tenant modification if ids are misused.
  - _Fix:_ Include tenant_id in the WHERE clause to bind the update to the correct tenant, e.g. `WHERE id = ${storedId} AND tenant_id = ${input.tenantId}`. Never perform UPDATE/DELETE on tenant-scoped tables using id alone — always enforce tenant_id in the predicate.
- **server/research-engine.ts:165** — Global UPDATE on tenant-scoped table research_sessions without any tenant_id constraint (UPDATE research_sessions ... WHERE status = 'running' ...). This can modify sessions across all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}), or iterate per-tenant and run a tenant-scoped UPDATE. If this is intended as an admin/global job, make that explicit and protect it with an admin-only guard and an audit log.
- **server/research-engine.ts:400** — DELETE from tenant-scoped table agent_knowledge without tenant_id (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will remove rows for all tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE tenant_id = ${session.tenantId} AND source = 'autoresearch' AND expires_at < NOW()) or run deletion per-tenant. If the intent is global cleanup, require an explicit admin context and safety checks before running across tenants.
- **server/routes.ts:167** — SELECT from tenant-scoped table `file_storage` with no tenant_id restriction (restoreUploadsFromDb). Startup routine selects all file_storage rows matching mime-type without scoping to a tenant, which can expose or restore files for every tenant to local disk.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE file_storage.tenant_id = <tenant>) or run this per-tenant with an authenticated/authorized tenant context. If the intent is admin-wide maintenance, require a platform-admin guard and audit/limit which tenants are processed; do NOT run an unscoped select that touches all tenants' files.
- **server/routes/conversations.ts:102** — INSERT into `project_conversations` does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES ...) — tenant_id is required and missing, allowing cross-tenant linkage.
  - _Fix:_ Include tenant_id in the INSERT. Example: INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}). Ensure the tenantId used is the caller's trusted tenant (or conv.tenantId if validated).
- **server/routes/projects.ts:185** — INSERT into tenant-scoped table project_notes does not include tenant_id. The route inserts (project_id, note, author) without explicitly setting tenant_id (table columns are NOT NULL with no default per invariant).
  - _Fix:_ Include tenant_id in the INSERT values (and use the authenticated tenantId, not any client-provided value). e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) RETURNING *.
- **server/routes/projects.ts:225** — INSERT into tenant-scoped table project_files (base64 upload path) does not include tenant_id. The query inserts project file rows without explicitly setting tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (use the tenantId resolved from the request). e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}) RETURNING *.
- **server/routes/projects.ts:293** — INSERT into tenant-scoped table project_files (multipart upload path) does not include tenant_id. project_files rows are created without tenant_id column set.
  - _Fix:_ Include tenant_id in the INSERT using the authenticated tenantId. e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) RETURNING *.
- **server/seed.ts:3771** — DELETE from tenant-scoped table `heartbeat_tasks` is executed without any tenant constraint (no WHERE tenant_id = ...). This will delete matching rows across all tenants.
  - _Fix:_ Add an explicit tenant predicate to the DELETE (e.g. DELETE FROM heartbeat_tasks WHERE tenant_id = <tenant> AND ...). If global cleanup is required, perform it deliberately and with operator confirmation; otherwise scope to the system/admin tenant with a WHERE tenant_id = 1 or use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)).
- **server/seed.ts:3799** — UPDATE on tenant-scoped table `memory_entries` (REPLACE on fact column) is executed without a tenant_id filter. This will modify rows for all tenants.
  - _Fix:_ Limit the UPDATE to the intended tenant by adding AND tenant_id = <tenant> to the WHERE clause, or loop per tenant. Prefer Drizzle's .where(eq(memoryEntries.tenantId, tenantId)) to ensure tenant isolation and avoid accidental cross-tenant data modification.
- **server/seed.ts:3825** — UPDATE on tenant-scoped table `conversations` (model backfill) runs without a tenant constraint, affecting conversations for all tenants.
  - _Fix:_ Scope the UPDATE to the intended tenant (e.g. WHERE tenant_id = <tenant> AND model = 'gpt-4.1') or perform the update per-tenant. Use Drizzle .where(eq(conversations.tenantId, tenantId)) to enforce tenant isolation and prevent cross-tenant changes.
- **server/self-improvement.ts:394** — runExperiment updates the `experiments` row using only eq(experiments.id, experiment.id) (same in the error path at ~413). The UPDATE has no tenant_id constraint, so it could modify another tenant's row if an id is supplied / misused.
  - _Fix:_ Include the tenant guard in the UPDATE/WHERE clause (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId)))). Never issue UPDATE/DELETE against tenant-scoped tables without constraining by tenant_id in the same statement.
- **server/self-reflection.ts:366** — storeLesson executes an UPDATE on memory_entries using WHERE id = <id> with no tenant_id constraint. Although the row was discovered by a prior tenant-scoped SELECT, the UPDATE itself is not constrained and could update rows across tenants if ids are misused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) and bind tenantId. Prefer verifying the row belongs to the tenant in the same atomic statement to avoid TOCTOU and cross-tenant modification risks.
- **server/storage.ts:530** — createMemoryEntry inserts into memory_entries with values(data) but does not validate that data.tenantId is present and owned by caller. The schema requires tenant_id NOT NULL with no default — an insert without an explicit validated tenantId can create cross-tenant rows or rely on unsafe defaults.
  - _Fix:_ Require and validate an explicit positive tenantId on all memory entry inserts (assertValidTenantId(data.tenantId) or similar) before calling db.insert. Do not accept unvalidated tenantId from untrusted input.
- **server/storage.ts:838** — archiveExpiredMemories executes a raw UPDATE on memory_entries to mark expired rows as archived with NO tenant scoping — this will update rows across all tenants (global destructive write).
  - _Fix:_ If the maintenance task is intended to act across all tenants, ensure it is run under an explicit admin context and document that. Prefer adding tenant scoping where appropriate or run per-tenant loops with validated tenant ids. At minimum, make the admin-only nature explicit and protect it with authorization checks.
- **server/storage.ts:854** — archiveStaleMemories executes a raw UPDATE on memory_entries to archive stale rows without tenant scoping — this updates rows across tenants.
  - _Fix:_ Same as above: either perform this per-tenant with an explicit validated tenantId or ensure the operation is an authorized admin-only maintenance action. Avoid unscoped multi-tenant updates.
- **server/storage.ts:1296** — getAllDataForExport performs SELECTs from many tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with NO tenant scoping — it returns platform-wide data and will leak all tenants' data if called by a non-admin.
  - _Fix:_ Restrict this function to admin-only contexts and add explicit authorization checks. Prefer adding an option to export per-tenant data (require tenantId) and fail if called without admin credentials. Ensure callers are audited and access is tightly controlled.
- **server/tools.ts:4563** — Raw SELECT against presenter_sessions uses only presentation_id (SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...) with no tenant_id constraint or ownership check. This can leak tokens/sensitive session data across tenants.
  - _Fix:_ Constrain the query to the caller's tenant or assert ownership first. Either add AND tenant_id = ${params._tenantId} to the WHERE clause or call/implement an assertPresentationInTenant(presentationId, tenantId) guard before reading presenter_sessions. Never read tenant-scoped rows by foreign id alone.

### HIGH (198)

- **server/ab-optimizer.ts:110** — Global SELECT from experiments in runDueAbExperiments is not tenant-scoped (missing WHERE tenant_id = <caller tenant>) — this reads metadata across all tenants.
  - _Fix:_ Add tenant filtering to the SELECT (e.g. WHERE tenant_id = ${tenantId} or pass a tenantId param and use eq(experiments.tenantId, tenantId)). If the heartbeat must scan per-tenant, iterate tenants and scope each query to that tenant.
- **server/agent-activity.ts:122** — UPDATE agentActivity in completeActivity is scoped only by id (WHERE id = activityId) and lacks a tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}) or use the typed drizzle eq(agentActivity.tenantId, tenantId) guard.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting selects ai_insights WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER but WITHOUT tenant_id — reads rows across all tenants.
  - _Fix:_ Scope the SELECT by tenant_id (or iterate tenants) so you only process insights belonging to the intended tenant (WHERE tenant_id = ${tenantId}).
- **server/agentic-features.ts:638** — advanceSequence updates outreach_enrollments to mark completed (UPDATE ... WHERE id = ${enrollment.id}) without a tenant_id constraint, allowing cross-tenant writes by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to ensure only the caller's tenant row is modified.
- **server/agentic-features.ts:671** — advanceSequence updates outreach_enrollments (SET current_step, last_sent_at, next_send_at ...) using WHERE id = ${enrollment.id} with no tenant filter — write is not tenant-scoped.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause when updating enrollments to prevent cross-tenant modifications.
- **server/agentic-features.ts:721** — classifyReply updates outreach_enrollments (UPDATE ... WHERE id = ${row.id}) without constraining tenant_id — this can modify another tenant's enrollment row by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to ensure only the caller's tenant row is updated.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to set status='expired' based purely on pending/expiresAt with no tenant constraint — this updates approvals across all tenants in one operation.
  - _Fix:_ Make expiry tenant-scoped (e.g. run per-tenant and include WHERE tenant_id = ${tenantId}) or otherwise constrain the UPDATE to a specific tenant to avoid cross-tenant writes.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table without restricting by tenant_id.
  - _Fix:_ Either add an explicit tenant_id WHERE clause when this function is called on behalf of a single tenant, or make the admin intent explicit (e.g. require an admin-only gate/parameter and document that this is a cross-tenant sweep). If this query must enumerate tenants, ensure it runs in a privileged/admin context with strict auditing/authorization, or move cross-tenant logic to a dedicated admin-only path so normal request handlers never run unfiltered tenant-scoped queries.
- **server/agentic/resume-worker.ts:66** — Multi-row SELECT joins agent_runs and agent_approvals without restricting to a specific tenant (no WHERE tenant_id = <caller tenant>). This scans/returns tenant-scoped rows across tenants.
  - _Fix:_ Filter the query by a caller/owner tenant (e.g. add WHERE r.tenant_id = ${tenantId} or accept a tenantId parameter), or otherwise iterate per-tenant explicitly. Never perform cross-tenant SELECTs on tenant-scoped tables; ensure any returned rows are limited to the caller's tenant before further processing.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table agent_wake_schedules performs a row selection without any tenant_id constraint (inner SELECT in WHERE id IN (...)). This can claim/modify rows across all tenants.
  - _Fix:_ Run the claim per-tenant or include tenant_id in the inner SELECT/outer UPDATE. For example iterate tenants and use: UPDATE agent_wake_schedules SET ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE tenant_id = ${tenantId} AND status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED). Alternatively, select ids per-tenant first and then update with a WHERE id = ${id} AND tenant_id = ${tenantId} guard.
- **server/auth.ts:196** — Lookup against apiKeys does a SELECT by keyHash/isRevoked with NO tenant_id restriction. This reads a tenant-scoped table without applying tenant scoping and can return keys for any tenant.
  - _Fix:_ Either treat api_keys as an explicit global/credential table (document that as an allowed exception) or restrict lookups when caller context is available. If caller-supplied tenant context exists, add AND tenant_id = ${tenantId}. If the lookup must remain global for authentication, ensure the table is clearly documented as a global credential table and enforce additional application checks after lookup (e.g. strict scope checks and least-privilege behavior).
- **server/auto-asset-capture.ts:184** — Backfill query SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... runs without a tenant_id constraint and scans conversations across all tenants.
  - _Fix:_ Perform backfill on a per-tenant basis (iterate tenant ids and add WHERE tenant_id = ${tenantId}) or otherwise restrict the query to an explicit tenant whitelist. Ensure any subsequent processing of conversation rows enforces ownership before writes (e.g. keep the existing assertConversationInTenant guard) and avoid global scans over tenant-scoped tables.
- **server/auto-memorize.ts:47** — SELECT from tenant-scoped table `agent_knowledge` with no tenant_id constraint (getWatermark).
  - _Fix:_ Include a tenant filter (e.g. AND tenant_id = ${tenantId}) or otherwise scope reads to a verified tenant. If this is a system-wide per-tenant watermark, change API to accept a tenantId and read the watermark per-tenant.
- **server/auto-memorize.ts:62** — SELECT id FROM tenant-scoped table `agent_knowledge` with no tenant_id constraint (setWatermark).
  - _Fix:_ Constrain the SELECT by tenant_id (e.g. WHERE ... AND tenant_id = ${tenantId}) and thread tenantId into setWatermark so you never lookup an unrelated tenant's row.
- **server/auto-memorize.ts:68** — UPDATE on `agent_knowledge` uses id only and does not verify tenant ownership (no tenant_id condition).
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) to prevent updating rows belonging to other tenants.
- **server/auto-memorize.ts:163** — SELECT from tenant-scoped table `messages` with no tenant_id WHERE clause — reads messages across all tenants and relies on post-query partitioning.
  - _Fix:_ Either run this job per-tenant (pass tenantId) and add WHERE m.tenant_id = ${tenantId}, or add a tenant filter to the query. Avoid scanning multiple tenants in a single unscoped query.
- **server/auto-project.ts:112** — SELECT from tenant-scoped table `messages` WHERE conversation_id = ${conversationId} has no tenant_id constraint.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the messages query (or otherwise ensure tenant ownership before the SELECT). Even if conversation ownership was checked earlier, add the tenant filter for defense-in-depth and to meet the app-level isolation invariant.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table `messages` WHERE conversation_id = ${conversationId} has no tenant_id constraint (autoSaveProjectTranscript).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages SELECT (or ensure conversation ownership is validated and include tenant guard in the query) so reads cannot cross tenant boundaries.
- **server/auto-transcript.ts:128** — Backfill SELECT queries scan `conversations` (tenant-scoped) without constraining tenant_id (backfillProjectTranscripts convRes) — enumerates conversations across all tenants.
  - _Fix:_ Run backfill per-tenant (accept a tenantId argument) or add a WHERE c.tenant_id = ${tenantId} clause. Avoid global scans of tenant-scoped data without explicit tenant scoping.
- **server/auto-transcript.ts:171** — SELECT from tenant-scoped table `conversations` (missingLinks query) is executed without a tenant_id filter — backfill step iterates across tenants.
  - _Fix:_ Scope this query to a single tenant or add WHERE c.tenant_id = ${tenantId}. For multi-tenant background jobs, iterate tenants explicitly rather than selecting unscoped tenant-scoped rows.
- **server/build-video-from-brief.ts:714** — projectId from caller input is passed into the startVideoJob/startInput without verifying the project belongs to the caller's tenant. This allows a caller to reference another tenant's project_id and have it used in a job (ownership not validated).
  - _Fix:_ Validate ownership of input.projectId (if provided) before using it: look up the project row and confirm project.tenant_id === input.tenantId (or perform an equivalent authorization check). Fail closed if the project is not found for the tenant. Alternatively, reject projectId from untrusted sources unless validated by the application.
- **server/chat-engine.ts:4365** — Call to storage.attachCitations(...) does not include tenant context. This likely performs an INSERT/UPDATE against a tenant-scoped citations/messages table without explicitly supplying tenant_id, violating the invariant that every write must include tenant_id.
  - _Fix:_ Pass the tenant id explicitly to attachCitations (e.g. storage.attachCitations(messageId, collectedCitations, tenantIdForScope)) or have attachCitations validate ownership by resolving the message id and asserting its tenant_id matches tenantIdForScope before writing. Ensure any INSERTs set tenant_id column and any UPDATE/DELETE include WHERE tenant_id = <caller tenant>.
- **server/commitments.ts:136** — SELECT from tenant-scoped table `commitments` lacks a tenant_id constraint (SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...). This reads rows across all tenants, risking cross-tenant data exposure.
  - _Fix:_ If cross-tenant scanning is intentional, ensure it is strictly limited to an admin-only code path with explicit authorization and that any exposed content is redacted. Otherwise, add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND ...) or otherwise scope the query to the caller's tenant. If admin-wide behavior is required, document and gate it behind a verified admin principal and minimize returned columns to avoid leaking tenant data.
- **server/credential-vault.ts:169** — UPDATE against tenant-scoped table `credential_vault` updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). Even though the code previously fetched the credential, the subsequent UPDATE is not guarded by tenant_id, permitting potential cross-tenant mutation in race scenarios.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or perform the UPDATE using the previously-validated credential row's tenant_id inside a transaction to eliminate the race. Prefer parameterized tenant checks rather than relying solely on prior SELECTs.
- **server/credential-vault.ts:176** — UPDATE against tenant-scoped table `credential_vault` updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}). The callsite earlier validated the credential, but the unconditional UPDATE is missing an explicit tenant_id guard.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${cred.id} AND tenant_id = ${tenantId}) or perform the update within the same transaction/authorization context that validated the credential's tenant to avoid cross-tenant side effects.
- **server/data-protection.ts:19** — CREATE TABLE memory_categories defines tenant_id INTEGER DEFAULT 1. This introduces a default tenant and allows INSERTs to omit tenant_id, violating the invariant that tenant_id must be supplied explicitly on tenant-scoped rows.
  - _Fix:_ Remove the DEFAULT 1 and make tenant_id explicit (e.g. tenant_id INTEGER NOT NULL). Ensure all INSERTs into memory_categories always pass the caller's tenantId from the application layer (and add application-level validation). Consider a migration to backfill/convert any rows that relied on the default tenant before deploying.
- **server/data-protection.ts:48** — ALTER TABLE agent_knowledge ADD COLUMN IF NOT EXISTS tenant_id INTEGER DEFAULT 1. Adding a tenant_id with a default of 1 means future INSERTs can implicitly land in tenant 1 when tenant_id is not provided, breaking the rule that INSERTs must explicitly set tenantId.
  - _Fix:_ Remove the DEFAULT 1 and add tenant_id as a NOT NULL column (or at least NULLable but with no default) so application code must supply tenant_id explicitly. Audit places that INSERT into agent_knowledge to ensure they pass tenantId, and run a migration to fix or annotate any rows that used the implicit default.
- **server/data-protection.ts:131** — Permanently-purge path issues: DELETE statements remove messages/compaction_archives/conversations using only conversation_id or id and do not include an explicit tenant_id predicate. Deletions against tenant-scoped tables without an explicit tenant_id in the WHERE clause risk accidental cross-tenant effects if IDs are misused or assumptions about uniqueness change.
  - _Fix:_ Include tenant scoping in destructive statements. For example: DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Alternatively run the purge inside a tenant-aware transaction (withTenantTx) or otherwise bind the tenant context before performing destructive queries.
- **server/delivery-pipeline.ts:1050** — getDeliveryByStripePayment SELECTs from tenant-scoped `delivery_logs` using only stripePaymentId (eq(delivery_logs.stripe_payment_id, paymentId)) with NO tenant filter. This can leak or return another tenant's delivery row.
  - _Fix:_ Require and apply tenant scoping for this lookup (e.g. accept tenantId and add AND eq(deliveryLogs.tenantId, tenantId)). If the lookup must be global, restrict its use to trusted admin-only code paths and document the risk; otherwise run per-tenant queries only.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken SELECTs from tenant-scoped `dfy_intake_forms` by token alone (eq(token, token)) with NO tenant_id constraint. A token lookup can return a form belonging to another tenant.
  - _Fix:_ Scope token lookups to tenant context: accept an explicit tenantId or validate the returned row's tenantId against the caller's tenant before returning. Avoid global token lookups without tenant verification.
- **server/dfy-intake.ts:96** — submitDfyIntake updates dfy_intake_forms via WHERE eq(dfy_intake_forms.id, form.id) but does NOT include tenant_id in the WHERE. If the form was resolved cross-tenant (or an attacker supplies an id), this can modify another tenant's row.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId)))) or require and pass the tenantId into submitDfyIntake and use it in the WHERE to ensure ownership before updating.
- **server/doc-collections.ts:166** — addDocument deletes existing doc_chunks with SQL DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but omits tenant_id. This can remove chunks belonging to other tenants sharing the same doc_path/collection_id (or if collection ids collide across tenants).
  - _Fix:_ Include tenant scoping in the DELETE: add AND tenant_id = ${tenantId}. Alternatively validate collection ownership first and perform per-tenant deletes (you already check the collection earlier; use that tenant context in the DELETE).
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks embedding with WHERE id = ${row.id} but omits tenant_id. Updating by id without tenant scoping can modify another tenant's chunk if ids collide or an attacker manipulates ids.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise ensure the update is performed within a per-tenant context (the function already receives tenantId — use it).
- **server/email.ts:516** — backfillEmptyBodies selects inbox_messages rows with no tenant filter: SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id. This scans all tenants' inbox_messages. Later the code updates rows by id (see below) also without tenant constraint.
  - _Fix:_ Run backfill per-tenant (require tenant context) or add tenant_id to the WHERE clause so the operation only touches a single tenant at a time. Avoid cross-tenant table scans and ensure subsequent UPDATEs include tenant_id in the WHERE.
- **server/email.ts:529** — backfillEmptyBodies updates inbox_messages via UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} with NO tenant_id constraint. This can modify rows belonging to other tenants if ids overlap or the initial SELECT was global.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the backfill in a per-tenant loop where tenantId is known and enforced on both SELECT and UPDATE statements.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' by project id is performed without scoping to the caller's tenant (WHERE eq(projects.id, projectId) only). This reads another tenant's project row before ownership is enforced in code.
  - _Fix:_ Scope the SELECT to the caller's tenant in the query itself: add AND p.tenant_id = ${tenantId} (or use .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))). Avoid reading cross-tenant rows and prefer SELECT ... WHERE id = X AND tenant_id = callerTenant so ownership is proven by the DB predicate.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' uses only project id in WHERE clause (where(eq(projects.id, projectId))) — write is not constrained by tenant_id.
  - _Fix:_ Include tenant scoping on the UPDATE: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Even if ownership was checked earlier, ensure the UPDATE's WHERE enforces tenant_id to prevent cross-tenant modifications.
- **server/google-drive.ts:826** — SELECT from tenant-scoped table 'projects' (resolving params.projectId) is executed without including tenant_id in the WHERE clause — the code reads a foreign project row before checking tenant ownership in JS.
  - _Fix:_ Use a tenant-scoped SELECT (WHERE id = ${projectId} AND tenant_id = ${callerTenant}) or perform the ownership check in the DB predicate so you never read another tenant's row unscoped.
- **server/google-drive.ts:1003** — SELECT from tenant-scoped table 'projects' by project id in searchDriveFiles is performed without scoping by tenant_id; the code reads the project row and only then checks tenant ownership in JS.
  - _Fix:_ Change the SELECT to include tenant scoping (e.g. WHERE id = ${projectId} AND tenant_id = ${params.tenantId}) so the DB enforces ownership and no unscoped cross-tenant read occurs.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images performed without including or validating tenant_id. This writes tenant-scoped data (presenter_slide_images tied to a presenter session) using only session_id, with no tenant constraint.
  - _Fix:_ Include tenant_id in the INSERT (and ON CONFLICT/UPDATE) or verify the session belongs to the expected tenant before inserting. Prefer: validate sessionId → fetch session row WHERE id = ${sessionId} AND tenant_id = ${tenantId} (fail if not found), then INSERT presenter_slide_images with an explicit tenant_id column value.
- **server/health-audit.ts:314** — SELECT FROM code_proposals without tenant_id filtering. code_proposals is tenant-scoped but the query pulls stale rows across all tenants.
  - _Fix:_ Scope the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND ...), or, if this is an admin/global maintenance operation, iterate per-tenant explicitly while running under a verified admin context. Never read tenant-scoped tables across tenants in a tenant-context function without explicit admin intent and audit.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — update has no tenant_id constraint and will affect rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}), or restrict ids to rows owned by the intended tenant after a verified SELECT. Alternatively, run the archival per-tenant (with tenant context) to avoid cross-tenant modifications.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks WHERE enabled = true ... (stuck heartbeat detection) has no tenant_id filter. heartbeat_tasks are often tenant-scoped; this will surface tasks for all tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or explicitly restrict this maintenance action to an admin-only job that iterates per-tenant. Otherwise the scan can leak or act upon other tenants' tasks.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — disables tasks by id without any tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Constrain the UPDATE to tenant-owned rows (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or perform a prior verification step that ensures each id to be disabled belongs to the expected tenant. If this is intended as a global admin action, make the admin context explicit and audit/log the tenant impact.
- **server/heartbeat.ts:53** — SELECT from research_schedules has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads tenant-scoped rows without restricting to a specific tenant.
  - _Fix:_ Query per-tenant or include an explicit tenant_id WHERE clause. For system-wide loops, iterate getActiveTenantIds() and run tenant-scoped queries like WHERE tenant_id = <tenant> (or use assertTenantContext) so every read is bound to a particular tenant.
- **server/heartbeat.ts:102** — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — no tenant_id constraint. This can modify another tenant's schedule by id alone.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}, or perform the update inside a tenant-bound context (assertTenantContext) to ensure you only update rows belonging to the intended tenant.
- **server/heartbeat.ts:107** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${sched.tenant_id}) or perform this update using a tenant-bound storage helper so row writes are always validated against the tenant.
- **server/heartbeat.ts:121** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id constraint on update.
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or execute the update through a tenant-aware storage function/assertion to prevent cross-tenant modification.
- **server/heartbeat.ts:378** — SELECT from event_log (SELECT * FROM event_log WHERE status = 'pending' ...) has no tenant_id constraint. This reads pending events across all tenants without scoping.
  - _Fix:_ Either query per-tenant (iterate tenant ids) or add WHERE tenant_id = ${tenant} so you only read events for the intended tenant. System processors that operate across tenants must still enforce tenant scoping per-row when making changes.
- **server/heartbeat.ts:266** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — missing tenant_id constraint. This updates a tenant-scoped heartbeat_tasks row by id only.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use a tenant-bound storage helper/assertTenantContext so writes can't affect other tenants' tasks.
- **server/heartbeat.ts:270** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — no tenant_id constraint on the UPDATE.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the update via tenant-aware storage APIs / assertTenantContext to prevent cross-tenant changes.
- **server/heartbeat.ts:1534** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 — missing tenant_id constraint. heartbeat_logs is tenant-scoped and this reads across tenants.
  - _Fix:_ Constrain the SELECT to the task's tenant (e.g. WHERE tenant_id = ${tenantId} AND task_name = ...). Use the asserted tenantId (assertTenantContext) so log reads are scoped to the correct tenant.
- **server/heartbeat.ts:2628** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint after creating a delegated task. This can change approval_status for a task id regardless of tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage helper to set approval_status so updates cannot cross tenant boundaries.
- **server/heartbeat.ts:3080** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint (in delegateTaskFromChat).
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage layer / assertTenantContext to perform tenant-scoped updates and avoid modifying other tenants' rows.
- **server/internal-resolver.ts:184** — finalizeEvent is called without a tenant_id constraint when the event row has no tenant_id. This issues an unscoped UPDATE against tenant-scoped event_log (UPDATE event_log ... WHERE id = ${eventId} ... with no tenant filter), potentially mutating another tenant's event_row by id alone.
  - _Fix:_ Do NOT perform unscoped finalization. Fail closed: log and return without updating event_log when the event has no tenant_id. Prefer requiring the caller to pass tenantId (or derive it first) and always include AND tenant_id = ${tenantId} in the UPDATE, or change finalizeEvent to require tenantId non-optional so callers cannot accidentally run an unscoped UPDATE.
- **server/job-queue.ts:98** — claimDueJobs issues a global UPDATE on agent_jobs to claim rows (UPDATE agent_jobs SET ... WHERE id IN (SELECT id FROM agent_jobs WHERE status='pending' ... ) ) with no tenant_id constraint. This mutates tenant-scoped rows without scoping to a tenant.
  - _Fix:_ Restrict claims to a tenant (include tenant_id = ${tenantId} filter) or make the global intent explicit and document/guard it. If the worker must operate per-tenant, accept a tenantId parameter and add WHERE tenant_id = ${tenantId}. If it is intentionally global, audit and justify that this system actor is allowed to modify rows across tenants and add additional guards/ACLs.
- **server/job-queue.ts:134** — reclaimExpiredLeases STEP 1 performs UPDATE agent_jobs SET status='failed_terminal' ... WHERE status = 'running' AND lease_until < NOW() AND attempts >= max_attempts with no tenant_id constraint — terminalizes running jobs across all tenants without tenant scoping.
  - _Fix:_ Scope terminalization by tenant where appropriate (add AND tenant_id = ${tenantId}) or ensure this operation is explicitly intended to run globally and is protected by operator-only privileges. Prefer separate per-tenant reclaim paths or an explicit administrative guard to avoid cross-tenant side-effects.
- **server/job-queue.ts:148** — reclaimExpiredLeases STEP 2 re-queues expired `running` jobs back to pending via an UPDATE with no tenant_id constraint (UPDATE agent_jobs SET status='pending' ... WHERE status = 'running' AND lease_until < NOW()). This affects jobs for all tenants.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or otherwise restrict this operation to the intended tenant(s). If the reclaim is global by design, document and protect it with explicit operator authorization; otherwise make it per-tenant.
- **server/job-queue.ts:177** — completeJob updates agent_jobs (SET status='succeeded' ...) using only id/attempts guards, but no tenant_id constraint (UPDATE agent_jobs ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts}). This allows a caller with just a job id to complete a job from another tenant.
  - _Fix:_ Require tenant context when completing jobs. Pass tenantId into completeJob and add AND tenant_id = ${tenantId} to the UPDATE to fence completion to the owning tenant (or ensure callers run with a verified system context and add explicit authorization checks).
- **server/job-queue.ts:220** — failJob first SELECTs max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — reads tenant-scoped row by id without verifying tenant ownership.
  - _Fix:_ Read the job row scoping by tenant (SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId}) or require a tenantId parameter and validate ownership before using the row. Avoid unscoped reads by id.
- **server/job-queue.ts:237** — failJob's terminal-path UPDATE sets status='failed_terminal' WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} without tenant_id filtering. This can mark jobs from other tenants as failed terminal by id alone.
  - _Fix:_ Add tenantId to the call and include AND tenant_id = ${tenantId} in the WHERE clause (and similarly for all other UPDATEs on agent_jobs) to prevent cross-tenant mutation. Alternatively enforce an operator-only flow with additional authorization if global updates are required.
- **server/job-queue.ts:257** — failJob's retry-path UPDATE (requeue with next_run_at/backoff) updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint, enabling cross-tenant mutation.
  - _Fix:_ Include tenant scoping in this UPDATE (AND tenant_id = ${tenantId}) or require the caller to supply and verify tenantId before performing the update. Ensure lease-fencing + tenant-fencing together to prevent stale or cross-tenant writes.
- **server/job-queue.ts:274** — cancelJob issues UPDATE agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint, allowing cancellation of jobs belonging to other tenants by id alone.
  - _Fix:_ Require tenantId and include AND tenant_id = ${tenantId} in the WHERE clause. If this is intended as an operator/admin action, add explicit authorization checks and audit logging to prevent misuse.
- **server/job-queue.ts:283** — retryJob does an UPDATE resetting job state WHERE id = ${id} AND status IN (...) without any tenant_id filter. This allows resetting jobs from other tenants by id alone.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) or require tenantId in the retryJob API and validate ownership before updating. For operator actions, add explicit privilege checks and require tenant selection to avoid accidental cross-tenant resets.
- **server/job-queue.ts:320** — listJobs builds and runs a SELECT from agent_jobs but if no tenantId is provided it performs an unscoped SELECT across all tenant rows (SELECT ... FROM agent_jobs ${whereClause}). That returns tenant-scoped rows to callers without tenant filtering.
  - _Fix:_ Require callers to explicitly pass tenantId or enforce caller-scoped filtering by default. If an administrative global listing is needed, separate the admin API (with strict authorization) from per-tenant listing. Always prefer explicit tenant scoping in the WHERE clause for tenant-level APIs.
- **server/job-queue.ts:385** — getJobStats runs SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > NOW() - INTERVAL '7 days' GROUP BY status with no tenant filter — aggregates job counts across all tenants and returns global stats from a tenant-scoped table.
  - _Fix:_ If per-tenant stats are required, add WHERE tenant_id = ${tenantId}. If a global operator metric is intended, restrict this function to admin context only and ensure appropriate access controls and audit trails. Avoid silently returning cross-tenant aggregates to tenant-scoped callers.
- **server/lib/action-ledger.ts:304** — sweepStaleAttempts() runs an UPDATE on tenant-scoped table `action_attempts` using WHERE id IN (SELECT id FROM action_attempts ... ) with no tenant_id constraint — this can transition rows across tenants in bulk.
  - _Fix:_ Add tenant scoping to the UPDATE (and the inner SELECT) so only rows belonging to a single tenant are updated, or iterate per-tenant and call transition(id, tenantId, ...) so every write is performed with an explicit tenant_id WHERE clause. If reconciler must sweep across tenants, fetch tenant_id with each id and perform per-row tenant-guarded transitions (i.e. call the tenant-aware transition function for each row).
- **server/lib/agent-trace.ts:124** — persistSpanClose() issues UPDATE agent_trace_spans ... WHERE span_id = ${spanId} with no tenant_id predicate. agent_trace_spans is tenant-scoped; the UPDATE can modify a span belonging to another tenant if span_id is known or leaked.
  - _Fix:_ Include tenant_id in the WHERE clause for the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) and pass the tenant id from the span context/currentTenantId() into persistSpanClose. If spans are closed asynchronously, ensure the saved open context includes the tenant id so the close can assert and constrain by tenant before updating the DB.
- **server/lib/bwb-job-progress.ts:74** — scope(jobId, ...) builds WHERE clauses that may omit the tenant predicate when env-threaded tenant (BWB_TENANT_ID) is missing; callers use .where(scope(...)) for updates to tenant-scoped table `video_jobs`. If env tenant is absent the code warns once but proceeds and performs updates without tenant_id filtering — allowing cross-tenant updates (writes) pinned only to a jobId.
  - _Fix:_ Do not perform updates to tenant-scoped rows without an explicit tenant_id. Change scope(...) to require tenantId and throw or return a predicate that always fails when tenant is unknown, or make all callers pass tenantId explicitly. Failing closed (reject the operation) when BWB_TENANT_ID is not set is safer than silently proceeding without tenant scoping. Audit all call sites (setBwbPhase, updateBwbChapters, completeBwbJob, bumpBwbHeartbeat, failBwbJob) to ensure tenantId is always provided and used in the WHERE clause.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped table `projects` groups/counts across tenants with NO tenant_id WHERE clause (reads tenant-scoped rows for all tenants).
  - _Fix:_ Restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) or explicitly iterate per-tenant when the operation must aggregate across tenants. Ensure any logic that decides to short-circuit/backfill is computed per-tenant, never from an unscoped global read of tenant-scoped tables.
- **server/lib/mention-injection.ts:121** — fetchRadioMessages() joins channel_messages cm to personas p using only p.id = cm.from_persona_id but does not constrain the joined personas row by tenant_id. Because there is no DB-level FK/ownership enforcement, this can leak persona fields across tenants if ids collide or are attacker-controlled.
  - _Fix:_ Constrain the JOIN to the same tenant (or fetch the persona separately after verifying ownership). Example: LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = cm.tenant_id — or, after selecting the message rows, query personas with WHERE id = <from_persona_id> AND tenant_id = ${params.tenantId}. Ensure all joins against tenant-scoped tables always include tenant_id ownership checks.
- **server/lib/scheduled-post-runner.ts:61** — UPDATE of tenant-scoped table `scheduled_posts` (CTE update that flips pending→publishing) does not include an explicit tenant_id constraint in the WHERE clause.
  - _Fix:_ When updating tenant-scoped rows, include the tenant_id in the predicate (e.g. join the CTE to include tenant_id and use WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id), or otherwise ensure the update is parameterized by the intended tenant(s). Even for system/cron runners, make the tenant binding explicit so writes cannot accidentally affect the wrong tenant's rows.
- **server/lib/scheduled-post-runner.ts:135** — UPDATE scheduled_posts in the quality-gate blocked branch uses WHERE id = ${row.id} but no tenant_id predicate. This is a write against a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) to ensure the update is scoped to the intended tenant, or otherwise bind the CTE/update to the tenant returned from the earlier SELECT.
- **server/lib/scheduled-post-runner.ts:217** — UPDATE scheduled_posts (persisting per_platform_results / next_attempt_at / status) uses WHERE id = ${row.id} with no tenant_id check, performing a tenant-scoped write without an explicit tenant constraint.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} (or include tenant in the CTE) to the WHERE clause so the update is explicitly scoped to the tenant associated with the row being processed.
- **server/lib/scheduled-post-runner.ts:244** — Error-path UPDATE scheduled_posts (release on exception) updates WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id. This performs a tenant-scoped write without an explicit tenant constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} AND status = 'publishing') so exception handling cannot accidentally affect another tenant's data.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` with no tenant_id filter — cross-tenant read.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or require an explicit admin-only code path and an authorization check. If the intention is an admin/global audit, make that explicit (authorize only admin callers) and document the exception; otherwise accept a tenantId parameter and filter all reads.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` with no tenant_id filter — cross-tenant read.
  - _Fix:_ Same fix as above: either add a tenant_id WHERE clause (pass in tenantId) or gate this function to an authorized admin-only path and clearly document the privileged exception. Avoid blanket cross-tenant SELECTs in normal request paths.
- **server/lib/system-probes.ts:163** — COUNT(*) query against tenant-scoped `messages` table with no tenant_id constraint — aggregates across all tenants.
  - _Fix:_ Limit the count to a tenant by adding WHERE tenant_id = ${tenantId} (and accept/require tenantId), or ensure this probe runs only in an explicitly authorized admin context and document that exception. Prefer parameterized tenant-scoped queries rather than global scans from request-exposed code.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table tenant_voice_profiles uses only id in WHERE (eq(tenantVoiceProfiles.id, existing.id)) with no tenant_id constraint. This allows an attacker or bug that supplies/guesses an id to update a profile belonging to a different tenant.
  - _Fix:_ Add an explicit tenant check in the UPDATE WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) or use the tenant-scoped row returned earlier to enforce ownership. Prefer parameterized Drizzle eq(...) checks rather than relying solely on id.
- **server/memory-graph.ts:141** — refreshCategoryCount runs an UPDATE on memory_categories with a subquery that counts memory_entries but neither the subquery nor the outer UPDATE includes a tenant_id filter. This can produce incorrect counts and update other tenants' rows.
  - _Fix:_ Restrict both the inner COUNT and the outer UPDATE by tenant (e.g. inner: SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ...; outer: WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Alternatively run this inside a tenant-scoped tx and bind tenantId explicitly.
- **server/memory-graph.ts:210** — INSERT INTO memory_links (...) VALUES (...) does not set tenant_id. Per DB invariant, tenant-scoped tables must receive tenant_id in INSERTs; omitting it will create links not bound to any tenant or to the wrong tenant.
  - _Fix:_ Include tenant_id in the INSERT column list and values (e.g. INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count, tenant_id) VALUES (..., ${tenantId})). Also validate that both memory ids belong to the same tenant before inserting.
- **server/memory-graph.ts:408** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} has no tenant_id constraint. This can update a memory entry in another tenant if mem.id is guessed or misused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or perform the update via a tenant-scoped helper/storage method that validates ownership.
- **server/minds-engine.ts:368** — CAS UPDATE on plans (to flip status) conditionally omits tenant_id when args.tenantId is not provided (SQL contains ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}). When called without tenantId this UPDATE will act across tenants.
  - _Fix:_ Require a tenantId for this operation at the function boundary (fail closed) or always include a tenant constraint derived from the caller context (e.g. runtime tenant), not make it optional. If omission is genuinely internal, ensure internal callers cannot be reached from untrusted paths and document/assert that invariant.
- **server/minds-engine.ts:386** — Probe SELECT (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}) may execute without tenant_id when args.tenantId is absent, allowing reads of plans across tenants.
  - _Fix:_ Always include tenant_id in the WHERE clause for tenant-scoped plan reads or require the caller to provide a trusted internal flag and assert it. Prefer failing when tenantId is omitted for externally-invoked code paths.
- **server/minds-engine.ts:472** — Rollback UPDATE that restores a plan to awaiting_approval uses WHERE id = ${args.planId} AND status = 'revising' with no tenant_id constraint. That update can affect a plan belonging to another tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${args.tenantId}) or perform the rollback in a tenant-scoped transaction so the scope is enforced. Validate tenant ownership before performing the UPDATE.
- **server/minds-engine.ts:547** — getPlan() contains a SELECT path that executes SELECT * FROM plans WHERE id = ${planId} when tenantId is omitted. That produces an un-scoped read on a tenant-scoped table whenever caller omits tenantId.
  - _Fix:_ Avoid the un-scoped SELECT branch for non-trusted callers. Either require tenantId and fail if missing, or ensure the call is only ever used by fully-trusted internal code paths. Prefer a deterministic API: always bind tenant_id via eq(plans.tenant_id, tenantId) or run inside a withTenantTx context.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list. The code relies on assertProjectInTenant(...) but still writes the row without an explicit tenant_id column.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)) after you have validated ownership, or ensure the DB schema/constraints intentionally allow omission. Prefer explicit parameterized tenant_id to preserve the app-level tenant isolation invariant.
- **server/oauth-subscriptions.ts:906** — SELECT FROM oauth_subscriptions (`SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE`) runs without a tenant_id filter, reading rows across all tenants.
  - _Fix:_ If this is intended as a global admin/maintenance operation, require/verify an admin context before running and document it clearly. Otherwise add an explicit tenant_id filter (WHERE tenant_id = ${tenantId} AND ...) or iterate over an explicit safe tenant list. Avoid ad-hoc queries that expose all tenants' subscription rows to request-scoped code paths.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh executes SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter (reads subscriptions across all tenants).
  - _Fix:_ This global scan should only run from a trusted background job with explicit authorization. If it is not intended to touch all tenants from arbitrary callers, add tenant scoping or require an explicit admin context; otherwise document and gate this system-level operation.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions runs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant filtering, iterating all tenants' subscriptions.
  - _Fix:_ As a global operation this must be explicitly privileged and separated from request-scoped code. If used in non-admin contexts, add tenant filters or an explicit tenant list. Otherwise ensure only authorized background/admin jobs call this function and document the cross-tenant behavior.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(lockKey) in SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). sql.raw() injects raw SQL text derived (via ledgerKey) from caller-supplied input, creating an injection/isolation risk.
  - _Fix:_ Avoid sql.raw() with values that can be influenced by requests. Instead pass the numeric lock key as a parameter so the driver parameterizes it (for example compute the numeric BigInt and use tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`)). Validate/normalize ledgerKey server-side and only use parameter interpolation, not raw SQL fragments.
- **server/plan-executor.ts:787** — Unscoped SELECT for pending plans: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50` returns approved plans across all tenants. This exposes plan ids from other tenants to the executor loop and schedules execution without tenant scoping.
  - _Fix:_ Filter the SELECT by tenant_id (e.g. `WHERE status = 'approved' AND tenant_id = ${tenantId}`) or iterate per-tenant when picking up pending plans. If a global worker must process all tenants, explicitly enumerate tenants and query per-tenant to ensure explicit tenant scoping on every DB operation.
- **server/process-governor.ts:155** — SELECT subquery in desk_status computes pending_tasks via: SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true — this subquery lacks a tenant_id constraint and can leak cross-tenant counts.
  - _Fix:_ Include tenant scoping in the subquery (e.g. AND tenant_id = ${tenantId}) or otherwise join on tenant_id to ensure counts are restricted to the current tenant.
- **server/process-governor.ts:199** — SELECT AVG(duration_ms) FROM heartbeat_logs(...) lacks a tenant_id filter — response time is aggregated across all tenants.
  - _Fix:_ Add a WHERE tenant_id = ${tenantId} condition (or equivalent) to restrict the aggregation to the caller's tenant.
- **server/process-governor.ts:121** — Subqueries in task_failure_rate use heartbeat_logs filtered only by task_id and time (e.g. SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...) but do not include tenant_id, allowing cross-tenant leakage.
  - _Fix:_ Add tenant scoping to those subqueries (e.g. AND tenant_id = ${tenantId}) to ensure logs counted belong to the same tenant as the task.
- **server/process-governor.ts:239** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... is missing tenant_id filtering — counts failing agents across tenants.
  - _Fix:_ Restrict the query with AND tenant_id = ${tenantId} to avoid aggregating logs from other tenants.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a tenant-scoped projects row without any tenant_id constraint or ownership verification — caller-supplied projectId may reference another tenant.
  - _Fix:_ Require tenant context and include WHERE tenant_id = ${tenantId} in the SELECT, or verify the project belongs to the caller's tenant before reading and using project data.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 has no tenant restrictions — it can enumerate projects across tenants.
  - _Fix:_ Scope the query by tenant (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}) or join only tenant-scoped tables filtered by the caller's tenant to avoid cross-tenant enumeration.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping — conversations can belong to other tenants sharing the project id value.
  - _Fix:_ Include tenant_id = ${tenantId} (or ensure proj was validated to belong to the tenant) when selecting conversations to restrict results to the caller's tenant.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant filter — message content may be read across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or otherwise verify the conversation belongs to the tenant) to ensure messages fetched are limited to the caller's tenant.
- **server/proposal-verifier.ts:33** — SELECT id, target_file, ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id without constraining by tenant_id or validating ownership — caller-supplied proposalId can access others' proposals.
  - _Fix:_ Require tenant context and include AND tenant_id = ${tenantId} in the SELECT (or validate the proposal belongs to the caller's tenant) before using its fields.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id filter (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 1/500). This reads tenant-scoped rows across tenants with no per-tenant constraint.
  - _Fix:_ If this is not intended to be a global scan, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}). If the intent is a global cron scan, make that explicit and ensure every subsequent write/update uses the per-row tenant_id to constrain writes (see next finding). Prefer the safer pattern: query per-tenant or pass an explicit `_allowGlobalSystemUseOnly` flag and document/guard the global scan.
- **server/research-engine.ts:331** — SELECT on personas (SELECT name FROM personas WHERE id = ${program.persona_id}) is executed without constraining by tenant_id. Personas appear to be tenant-scoped elsewhere, so this can leak or resolve persona data across tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. SELECT name FROM personas WHERE id = ${program.persona_id} AND tenant_id = ${tenantId}), or otherwise validate that the persona belongs to the same tenant before using its data.
- **server/research-engine.ts:1053** — resolvePersonaId(personaSlug, _tenantId) issues a lookup (SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1) but ignores the tenantId parameter and has no tenant_id filter — cross-tenant read possible.
  - _Fix:_ Honor the tenantId parameter and add WHERE tenant_id = ${tenantId} to the query. Change the signature to require tenantId and use it to scope the lookup so persona ids are resolved only within the caller's tenant.
- **server/routes.ts:1118** — SELECT from tenant-scoped table `file_storage` without a tenant_id WHERE clause in /uploads middleware. The code fetches all owners for a basename and relies on post-query JS to check ownership instead of constraining the SQL to the caller's tenant.
  - _Fix:_ Scope the DB query to the caller's tenant (e.g. .where(eq(file_storage.tenant_id, authedTenantId))). If you intentionally need to fetch all owners to detect collisions, do that only under a platform-admin/authenticated internal path and document the reason; otherwise enforce tenant filtering in SQL and only return rows for the requesting tenant.
- **server/routes.ts:2091** — SELECT from `presenter_slide_images` uses session_id derived from an earlier presenter_sessions lookup that did not validate tenant ownership. The slide-image read is not tenant-scoped (no tenant_id filter), so a crafted token or session_id could be used to read another tenant's images.
  - _Fix:_ Verify tenant ownership of the presenter_session before using its id to read presenter_slide_images. Prefer adding tenant_id to presenter_slide_images and include WHERE presenter_slide_images.tenant_id = <caller tenant> (or join to presenter_sessions with tenant_id = caller tenant) to enforce DB-side scoping.
- **server/routes.ts:2174** — repair-images route reads presenter_sessions (tenant_id present) and then reads/writes presenter_slide_images by session_id without enforcing tenant ownership in SQL. The initial presenter_sessions SELECT is not tenant-scoped nor validated against the caller, allowing cross-tenant access/modification.
  - _Fix:_ Before operating on presenter_slide_images, validate that the presenter_session belongs to a tenant the caller is authorized for. Add tenant_id equality checks in the SQL (e.g. WHERE token = ${token} AND tenant_id = ${callerTenant}) and ensure subsequent queries on presenter_slide_images also include tenant scoping or are done via a join that enforces the session's tenant_id.
- **server/routes.ts:4333** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The raw INSERT: INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id, violating the invariant that tenant-scoped rows must be created with an explicit tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})). Alternatively, use a storage helper that enforces tenant scoping and validation before persisting.
- **server/routes/admin.ts:701** — SELECT from tool_performance executed without any tenant_id constraint — reads a likely tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId} or include tenant_id = ${tenantId} in the query) or explicitly document/implement that tool_performance is a true platform-global table. Prefer parameterized SQL with the resolved tenantId.
- **server/routes/admin.ts:706** — SELECT from tool_performance (failingRows query) lacks tenant_id filtering — can return cross-tenant data.
  - _Fix:_ Add tenant scoping to this query (e.g. WHERE tenant_id = ${tenantId} AND last_failure_at > ...) or otherwise ensure the table is intentionally global and safe for cross-tenant admin reads. Use the request's tenantId parameterized into the SQL.
- **server/routes/admin.ts:719** — SELECT from ai_insights (stuckInsights query) has no tenant_id constraint — reads tenant-scoped insights across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'new' ...). If the intent is to operate platform-wide only for the admin tenant, include tenant_id = ${tenantId} and ensure tenantId equals ADMIN_TENANT_ID.
- **server/routes/admin.ts:731** — SELECT from heartbeat_tasks (disabledTasks query) lacks tenant_id filtering — may return heartbeat tasks from all tenants.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND (enabled = false OR consecutive_failures >= 3) ...) or otherwise document that heartbeat_tasks is global; prefer scoping by tenantId for safety.
- **server/routes/admin.ts:741** — SELECT from ai_insights (stuckPending query) executed without tenant_id constraint — can leak tenant-scoped pending insights across tenants.
  - _Fix:_ Scope the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status = 'applied' AND priority = 'high' ...). If platform-wide reads are intended only for platform-admin sessions, still explicitly filter by the admin caller's tenantId or implement a clear, audited cross-tenant access pattern.
- **server/routes/admin-zombie-detector.ts:41** — Query on agent_trace_spans (used CTE) has no tenant_id constraint — reads a likely tenant-scoped trace table without scoping to the caller's tenant.
  - _Fix:_ Add tenant scoping to the subquery (e.g. FROM agent_trace_spans WHERE tenant_id = ${tenantId} AND tool_name IS NOT NULL AND started_at > ...) or, if the table is intentionally global, explicitly document that and ensure the admin gate plus auditing is sufficient. Use parameterized tenantId in the SQL.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table `conversations` lacks a tenant_id constraint (UPDATE conversations SET project_id = ... WHERE id = ...)
  - _Fix:_ Constrain the UPDATE to the conversation's tenant. Example: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId} (or use conv.tenantId if available).
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table `projects` lacks a tenant_id constraint (UPDATE projects SET updated_at = ... WHERE id = ...)
  - _Fix:_ Constrain the UPDATE to the tenant: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. This prevents touching projects belonging to other tenants.
- **server/routes/conversations.ts:230** — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES ...). If project_notes is tenant-scoped, tenant_id must be supplied.
  - _Fix:_ Include tenant_id in the INSERT: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, 'system:archive'). Use the conversation/project tenant that was validated earlier.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` has no tenant_id constraint in its WHERE clause (race/ownership bypass risk). The code runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` after selecting the row; a missing tenant predicate can allow cross-tenant mutation in race conditions.
  - _Fix:_ Include the tenant predicate on the UPDATE (e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) or use the canonical storage/db update API that enforces tenant scoping. Prefer parameterized Drizzle update calls (db.update(...).set(...).where(and(eq(inboxMessages.id, msg.id), eq(inboxMessages.tenantId, tenantId)))) to ensure tenant isolation.
- **server/routes/projects.ts:33** — SELECT subqueries count rows from tenant-scoped tables (project_files, project_notes, project_conversations) without constraining tenant_id. These subqueries (SELECT COUNT(*) FROM project_files WHERE project_id = p.id etc.) can return cross-tenant counts because there is no tenant_id filter on those tables.
  - _Fix:_ Restrict each subquery to the caller's tenant. Example: SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}. Alternatively join to projects p and filter on p.tenant_id = ${tenantId} for the dependent table as well.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — missing tenant_id constraint. This update can touch a project record outside the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — missing tenant_id constraint (duplicate in multipart upload path).
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC — querying tenant-scoped table project_files without constraining tenant_id.
  - _Fix:_ Constrain the query to the caller's tenant. e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 — missing tenant_id filter on tenant-scoped project_files table.
  - _Fix:_ Add tenant scoping: SELECT ... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} AND tenant_id = ${tenantId} LIMIT 1.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} — deletion against tenant-scoped table project_files but no tenant_id constraint in WHERE clause.
  - _Fix:_ Include tenant scoping in DELETE: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId} (or use eq(project_files.tenantId, tenantId) via Drizzle).
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without constraining tenant_id (UPDATE ... WHERE id = ${session.id}). This allows modifying a session row belonging to another tenant.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the session's tenant before updating. Prefer using Drizzle eq(table.tenantId, tenantId) or pass the tenantId into the update context.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions sets conversation_id without constraining tenant_id (UPDATE ... WHERE id = ${session.id}). This can update another tenant's session.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the WHERE clause (or validate ownership first). Ensure every UPDATE/DELETE on sculptor_sessions includes tenant_id to enforce tenant isolation.
- **server/sculptor.ts:174** — monitorSession updates sculptor_sessions (UPDATE ... WHERE id = ${sessionId}) without restricting tenant_id. monitorSession is passed tenantId but the UPDATE lacks it, enabling cross-tenant writes.
  - _Fix:_ Restrict the UPDATE to the tenant: e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Always include tenant_id (or verify ownership) on any write to tenant-scoped tables.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions sets review_result without constraining tenant_id (UPDATE ... WHERE id = ${sessionId}). reviewSessionWork accepts tenantId but the UPDATE omits it, allowing cross-tenant modification.
  - _Fix:_ Include tenant scoping in the WHERE clause: WHERE id = ${sessionId} AND tenant_id = ${tenantId}, or fetch-and-verify the session's tenant before updating. Ensure all writes to sculptor_sessions enforce tenant_id.
- **server/second-opinion.ts:404** — settleFusionReservation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}) with no tenant_id constraint. This can modify another tenant's ledger row if an id is misused.
  - _Fix:_ Constrain the UPDATE to the owning tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or read the row's tenant_id first and verify ownership before updating. Modify settleFusionReservation to accept/verify tenantId or perform a safe two-step check.
- **server/second-opinion.ts:423** — releaseFusionReservation updates agent_cost_ledger by id only (UPDATE ... WHERE id = ${reservationId}) without tenant scoping, allowing cross-tenant ledger mutation.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or fetch the row and verify its tenant before updating. Ensure reservation-release/settle code always enforces tenant ownership.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks runs without any tenant_id filter. heartbeat_tasks appears to be tenant-scoped elsewhere (some queries use tenant_id), so this read can cross tenant boundaries.
  - _Fix:_ Add an explicit tenant filter (WHERE tenant_id = ${tenantId}) or otherwise restrict the query to the intended tenant(s). If the intention is to operate only on the admin tenant, use ADMIN_TENANT_ID explicitly.
- **server/seed-cleanup.ts:15** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks a tenant_id constraint and will disable tasks across all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant(s) (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or loop per-tenant). Never perform updates on tenant-scoped tables without an explicit tenant_id WHERE clause.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant_id filter, allowing cross-tenant reads.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId} AND ...), or explicitly use ADMIN_TENANT_ID if the operation is admin-only. Make tenant ownership explicit for reads on tenant-scoped tables.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is missing tenant_id and can affect every tenant's rows.
  - _Fix:_ Constrain the UPDATE to the correct tenant(s) (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or pass a tenantId and use it in WHERE). Avoid blanket updates on tenant-scoped tables.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) executes without tenant scoping, enabling cross-tenant enumeration.
  - _Fix:_ Include tenant_id in the WHERE clause or otherwise restrict the query to the intended tenant scope. If admin-only changes are intended, explicitly use ADMIN_TENANT_ID.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has no tenant_id restriction and will disable matching tasks across all tenants.
  - _Fix:_ Limit the UPDATE to the specific tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or iterate per-tenant. Ensure every UPDATE/DELETE on heartbeat_tasks includes tenant_id to preserve tenant isolation.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped but this query reads across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise pass an explicit tenant id. In Drizzle use .where(eq(research_sessions.tenantId, tenantId)) or include tenant_id = ${tenantId} in the SQL literal.
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true reads research_programs across all tenants (no tenant_id filter). research_programs is tenant-scoped.
  - _Fix:_ Add a tenant constraint to the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise iterate only over programs owned by the intended tenant. In Drizzle use .where(eq(research_programs.tenantId, tenantId)).
- **server/seed.ts:2163** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only the id without constraining tenant_id. This can modify rows belonging to other tenants sharing the same id value.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}). Prefer using Drizzle .where(and(eq(research_programs.id, p.id), eq(research_programs.tenantId, tenantId))).
- **server/seed.ts:2446** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL performs a write on a likely tenant-scoped table without constraining to tenant_id.
  - _Fix:_ Constrain the UPDATE to the appropriate tenant (e.g. AND tenant_id = ${tenantId}) or avoid hardcoding global ids. Use parameterized tenant id (Drizzle .where(eq(projects.tenantId, tenantId)) ) so the update cannot affect other tenants' rows.
- **server/seed.ts:2516** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id filter on conversations. conversations is tenant-scoped, so this reads across all tenants.
  - _Fix:_ Add a tenant constraint to the query (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or otherwise restrict the operation to the caller's tenant. Use eq(conversations.tenantId, tenantId) in Drizzle or include tenant_id = ${tenantId} in the SQL.
- **server/seed.ts:3343** — SELECT from tenant-scoped table `heartbeat_tasks` without constraining to a tenant (no WHERE tenant_id = ...). This reads rows across all tenants.
  - _Fix:_ Constrain reads to the intended tenant (e.g. WHERE tenant_id = 1) or use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)). If the intent is to only operate on system/admin tenant rows, explicitly filter by that tenant id to avoid cross-tenant visibility.
- **server/seed.ts:4038** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} but does not include tenant_id = ... — a TOCTOU/race could update another tenant's row with the same id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = $id AND tenant_id = <tenant>) or use a single upsert/UPDATE that was selected under the same tenant context to avoid TOCTOU issues. Prefer Drizzle .where(eq(agentKnowledge.tenantId, tenantId)) in addition to id checks.
- **server/self-improvement.ts:170** — detectStagnation builds and executes a SELECT on `experiments` that can run without a tenant_id constraint when tenantId is omitted. This allows cross-tenant reads of a tenant-scoped table.
  - _Fix:_ Require a tenantId for this function (or fail closed when tenantId is not provided) and always include eq(experiments.tenantId, tenantId) in the WHERE clause. Avoid branching into an unscoped query path; prefer an explicit error if tenant context is missing.
- **server/self-improvement.ts:521** — getExperimentHistory will perform db.select().from(experiments) with no tenant_id constraint when no tenantId is passed, returning experiment rows across all tenants.
  - _Fix:_ Make tenantId mandatory for tenant-scoped history reads (or explicitly fail when tenantId is absent). Always include a tenant filter (e.g. .where(eq(experiments.tenantId, tenantId))) for any SELECT on tenant-scoped tables.
- **server/storage.ts:239** — getConversationUnscoped performs a SELECT FROM conversations WITHOUT any tenant_id constraint (returns conversation row by id unscoped).
  - _Fix:_ Avoid unscoped reads in shared HTTP/handler code. Either require caller to pass tenantId and use eq(conversations.tenantId, tenantId) or restrict this function to internal orchestration code and audit every caller to ensure they derive and validate tenant ownership before using the returned row. If an unscoped read is absolutely required, add strong logging/authorization and a code comment pointing to the single approved callsites.
- **server/storage.ts:294** — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL without scoping by tenant_id. The messages table is tenant-scoped, so this can return other tenants' message data if ids are attacker-controlled.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) and pass in/validate a tenantId parameter. If caller-derived msgs are known to be tenant-scoped, require the caller to pass the tenantId explicitly and assert it before running the raw query.
- **server/storage.ts:395** — attachCitations runs raw SQL UPDATE messages SET citations = ... WHERE id = ${messageId} without constraining by tenant_id — a write to a tenant-scoped table with no tenant filter.
  - _Fix:_ Require and validate tenantId for this call and include AND tenant_id = ${tenantId} in the WHERE clause (or use Drizzle .where(eq(messages.tenantId, tenantId))). Avoid raw updates without tenant scoping.
- **server/storage.ts:306** — createMessage reads conversations row by conversationId with no tenant filter (SELECT ... FROM conversations WHERE id = ${data.conversationId}) while conversationId is caller-supplied. This derives tenant from the row but does not verify the caller's tenant owns the conversation first — potential cross-tenant object-confusion/leak.
  - _Fix:_ Either require the caller to provide tenantId and validate eq(conversations.tenantId, tenantId) in the SELECT or perform the SELECT with an explicit tenant filter (WHERE id = ${conversationId} AND tenant_id = ${tenantId}). If the function is intended to derive tenantId from the conversation row, document and restrict its use to trusted internal callers and avoid exposing it from HTTP handlers.
- **server/storage.ts:512** — getMemoryEntry can perform an unscoped SELECT from memory_entries when tenantId is omitted (function warns but still issues the SELECT). memory_entries is tenant-scoped, so omitting tenantId allows cross-tenant reads.
  - _Fix:_ Make tenantId mandatory (or fail-closed) for public callsites, and always include eq(memory_entries.tenant_id, tenantId) in the WHERE clause. Do not rely on a runtime warning — enforce via types or throw when tenantId is undefined.
- **server/storage.ts:533** — updateMemoryEntry builds a WHERE that may lack tenant scoping if tenantId is omitted (it only adds tenantScope when provided). This allows UPDATEs to memory_entries by id without a tenant filter.
  - _Fix:_ Fail-closed: require tenantId for updateMemoryEntry (or assert and throw if undefined) and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause. Avoid optional tenant scoping that can be omitted at runtime.
- **server/storage.ts:548** — deleteMemoryEntry updates memory_entries (set status='superseded') using a WHERE that may omit tenant scoping when tenantId is not provided. This lets callers mark rows in other tenants.
  - _Fix:_ Require tenantId and include tenant scoping in the WHERE. If unscoped calls are required for internal tooling, provide a clearly-named explicit function and restrict its usage.
- **server/storage.ts:555** — touchMemoryEntries updates memory_entries for a list of ids and only conditionally adds tenant scoping; when tenantId is undefined the update will act across tenants for those ids.
  - _Fix:_ Require tenantId (fail-closed) or otherwise validate ownership of every id before updating. Always include eq(memory_entries.tenant_id, tenantId) in the WHERE clause.
- **server/storage.ts:802** — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} with NO tenant filter — a write to a tenant-scoped table unscoped by tenant_id.
  - _Fix:_ Require/accept a tenantId argument or otherwise validate ownership and add AND tenant_id = ${tenantId} (or use Drizzle .where(eq(memoryEntries.tenantId, tenantId))). Prevent embedding writes that are not tenant-scoped.
- **server/storage.ts:814** — updateKnowledgeEmbedding updates agent_knowledge WHERE id = ${id} without any tenant constraint. agent_knowledge is tenant-scoped, so this can update rows across tenants.
  - _Fix:_ Require tenant context for embedding updates and include tenant scoping in the WHERE clause (eq(agent_knowledge.tenant_id, tenantId)). Do not perform global updates by id alone.
- **server/storage.ts:874** — getMemoryStats performs SELECTs over memory_entries and agent_knowledge and will return all rows when tenantId is undefined. This can leak counts and content across tenants if callers omit tenantId.
  - _Fix:_ Make tenantId required for this API or fail-closed when tenantId is omitted. Always include tenant filters in the SELECT queries (eq(...tenant_id, tenantId)).
- **server/storage.ts:650** — upsertDailyNote may INSERT into daily_notes using data without validating/ensuring data.tenantId. daily_notes has a tenant_id column historically defaulted; inserting without an explicit validated tenantId can write into the wrong tenant.
  - _Fix:_ Require and validate tenantId on upsert paths and include tenant scoping on the SELECT/UPDATE. For INSERTs, ensure caller-supplied data includes an explicit, validated tenantId or derive tenantId from a validated parent object.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding executes UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} without scoping by tenant_id — a write to a tenant-scoped table with no tenant filter.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) and require tenantId to be passed in and validated. If proposalId is used to derive tenant, perform a prior tenant-scoped SELECT and assert ownership before updating.
- **server/surprise-scorer.ts:139** — scoreProposalSurprise runs UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId} without constraining by tenant_id even though the function takes tenantId — later queries used tenantId but this UPDATE does not.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) so that outcome embeddings are only written for the intended tenant.
- **server/surprise-scorer.ts:212** — scoreProposalSurprise executes UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} WITHOUT tenant scoping — this writes scoring/band to a tenant-scoped row without ensuring tenant ownership in the WHERE clause.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use Drizzle .where(eq(felix_proposals.tenantId, tenantId))). Ensure all writes to felix_proposals are tenant-scoped.
- **server/tenant-fork.ts:225** — UPDATE against a tenant-scoped table (entry.table) uses only the PK filter (eq(entry.table.id, p.newId)) and does NOT include a tenant_id constraint. The invariant requires every read/write on tenant-scoped tables to be constrained to the caller's tenant.
  - _Fix:_ Add a tenant filter to the UPDATE where clause (e.g. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId)))) so the update is scoped to the new tenant and cannot affect rows belonging to other tenants.
- **server/tool-learning.ts:291** — UPDATE on custom_tools (.where(eq(customTools.id, tool.id))) mutates a tenant-scoped table without constraining by tenant_id — a cross-tenant update risk.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId)))) so the update only affects the expected tenant's row. Use the known tenantId from the tool/operation rather than trusting derived inputs.
- **server/tool-learning.ts:316** — DELETE from custom_tools (.where(eq(customTools.id, tool.id))) removes a tenant-scoped row without a tenant_id constraint, violating tenant isolation guarantees.
  - _Fix:_ Constrain the DELETE with tenantId as well (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId)))) to ensure only the owning-tenant's row can be deleted. Always verify ownership before destructive operations.
- **server/tool-learning.ts:365** — UPDATE incrementing usageCount (where(eq(customTools.id, tool.id))) mutates a tenant-scoped table but omits a tenant_id constraint.
  - _Fix:_ Include the tenant filter on the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId)))) so counters/metrics updates are scoped to the correct tenant and cannot modify another tenant's rows.
- **server/tools.ts:2878** — INSERT into tenant-scoped table project_conversations does not set tenant_id. The code inserts (project_id, conversation_id) without recording tenant ownership, relying on other updates for scoping.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})) or explicitly constrain the write by joining/validating ownership and then write tenant_id. Always set the tenant_id column on INSERTs to prevent cross-tenant writes or orphan rows.
- **server/tools.ts:2981** — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files only provides project_id, file metadata and uploaded_by, but not tenant_id.
  - _Fix:_ Pass the caller's tenant id explicitly in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). If project_files is intended to be scoped by project_id only, still include tenant_id to enforce tenant isolation at the application layer and fail fast when tenant context is missing.
- **server/tools.ts:2995** — INSERT into tenant-scoped table project_notes does not set tenant_id. The code inserts (project_id, note, author) but omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). Alternatively, validate ownership and explicitly populate tenant_id to ensure writes cannot cross tenant boundaries.
- **server/tools.ts:3020** — INSERT into tenant-scoped table project_conversations (link_conversation path) does not set tenant_id. The INSERT writes (project_id, conversation_id) without tenant_id even though tenant context is available and earlier ownership was checked.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Even when ownership was validated earlier, the row must carry the tenant_id explicitly to satisfy the application-only isolation invariant.
- **server/tools.ts:4541** — INSERT into project_notes omits tenant_id: INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) — tenant_id is not passed explicitly.
  - _Fix:_ Every INSERT into tenant-scoped tables must include the tenant_id column explicitly. Add tenant_id to the column list and pass the caller's tenant (e.g. VALUES (${projectId}, ${params._tenantId}, ...)) or otherwise ensure the tenant_id column is set by the application at insert time. Do not rely solely on assertProjectInTenant for row attribution.
- **server/tools.ts:4545** — INSERT into project_files omits tenant_id: INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) — tenant_id is not passed explicitly.
  - _Fix:_ Include tenant_id in the INSERT column list and supply params._tenantId (or the authoritative tenantId) as a value. Example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${projectId}, ...). Ensure this is done consistently for all project_files inserts.
- **server/tools.ts:6392** — INSERT into project_files (audio upload path) omits tenant_id: db.execute(sql`INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`).
  - _Fix:_ Add tenant_id to the INSERT (tenant_id, project_id, ...) and pass the caller's tenant (params._tenantId). Even when assertProjectInTenant was called, the tenant must still be written explicitly into tenant-scoped tables.
- **server/tools.ts:6926** — INSERT into project_files after slideshow/video production omits tenant_id: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) — tenant_id is not passed explicitly.
  - _Fix:_ Include tenant_id in the INSERT (tenant_id, project_id, ...) and supply the caller's tenant (params._tenantId). Audit all other project_* and project_files insertion sites and update them to explicitly set tenant_id during INSERT.
- **server/tools.ts:6926** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT SQL omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`.
  - _Fix:_ Include tenant_id from a trusted source in the INSERT (do not rely on implicit or derived values). e.g. use parameterized Drizzle insert or add tenant_id to the VALUES clause: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Ensure tenantId comes from the authenticated context (params._tenantId) and not an untrusted request field. Alternatively use db.insert(projectFiles).values({ tenantId, projectId: ..., ... }) so the tenant column is always set.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table `browser_workflows` lacks a tenant constraint (no WHERE tenant_id = ...). The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` can modify another tenant's row if the id is reused or attacker-controlled.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}; or perform the update via a JOIN that ensures the project/row belongs to ctx.tenantId. Always bind a trusted tenantId (ctx.tenantId) rather than relying solely on an id-derived provenance check.
- **server/tools/domains/files/handlers.ts:835** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` writes a tenant-scoped table without explicitly setting tenant_id (columns are NOT NULL with no default per invariant).
  - _Fix:_ Include the tenant_id column in the INSERT using the verified tenant (e.g. tenant_id column set to ctx.tenantId). Example: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ${absPath}, ...). If relying on project_id -> project.tenant_id relationship, still write tenant_id explicitly to satisfy application-level isolation rules and prevent orphaned/misattributed rows.
- **server/tools/domains/files/handlers.ts:839** — UPDATE against tenant-scoped table `projects` lacks tenant constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` does not include a tenant_id condition and could update a project belonging to another tenant if projId is not strongly provenance-checked for this SQL execution.
  - _Fix:_ Restrict the UPDATE to the caller's tenant. Add AND tenant_id = ${tenantId} to the WHERE clause (or perform an UPDATE ... FROM join that enforces p.tenant_id = ${tenantId}). Prefer explicit tenant-scoped WHERE clauses even when projId was previously derived from a tenant-checked query to enforce per-statement isolation.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE executed against tenant-scoped table `knowledge_triples` without constraining by tenant_id (UPDATE knowledge_triples SET valid_until = ${validFrom} WHERE id = ${row.id}). This allows a caller to affect rows belonging to other tenants if they can guess an id.
  - _Fix:_ Include the caller tenant in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or perform the mutation via a storage/DAO function that enforces tenant scoping. Consider SELECTing and verifying the row's tenant before mutating as defense-in-depth.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` with no tenant_id constraint (admin-path). This returns rows across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise explicitly scope results to the intended tenant(s). If the intent is an admin/global audit, make that explicit and perform an authorized per-tenant iteration (or add an explicit admin-only guard) rather than selecting all provider_keys without a tenant filter.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed with no tenant_id constraint (admin-path). This aggregates across all tenants without an explicit tenant scope.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or perform an explicit, authorized per-tenant aggregation. If truly intended to read global provider configuration only for owner/admin, require/verify an explicit admin gate and document/limit the operation; otherwise enforce application-level tenant filtering.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} but does NOT constrain by tenant_id (tenant filter omitted). This violates the invariant that every write against tenant-scoped tables must include tenant scoping.
  - _Fix:_ Include the tenant constraint in the UPDATE. For example: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}. Or use Drizzle-style equality: .where(eq(memoryEntries.tenantId, tid)). This ensures the row update is constrained to the caller's tenant and prevents cross-tenant updates.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) without also constraining tenant_id. Even though ids were fetched earlier with a tenant-scoped SELECT, the UPDATE itself lacks an explicit tenant filter.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Alternatively, include tenant checks in the WHERE clause using the tenant id or use a parameterized Drizzle .where(eq(memoryEntries.tenantId, tid)) to enforce tenant isolation on the update.
- **server/tools/domains/strategic-memory/handlers.ts:241** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without an accompanying tenant_id constraint. This violates the requirement to scope all writes to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}. Or use the equivalent Drizzle .where(eq(table.tenantId, tid)) to ensure the update cannot affect rows belonging to other tenants.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without any tenant_id constraint (query loads all pairing rows for the phone across tenants).
  - _Fix:_ Restrict the query to the expected tenant (e.g. add AND tenant_id = ${tenantId}) or require elevated/admin privileges for cross-tenant scans. If the intent is to detect contested pairings, make that explicit and perform an auditable admin-only query, or otherwise ensure you never return or act on rows belonging to other tenants.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} is executed without constraining by tenant_id. This reads a tenant-scoped table by id only.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${info.conversationId} AND tenant_id = ${expectedTenantId}) or otherwise enforce that reads against `conversations` are performed only for the caller's tenant. If the code needs to validate a claimed tenant, pass the claimed tenant into the query and check equality in SQL rather than performing an unrestricted read.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped table by id only, without tenant_id filtering. A caller could potentially affect another tenant's ledger row by supplying an id.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise verify ownership before updating). Never update tenant-scoped rows by id alone.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table by id only, without tenant_id filtering.
  - _Fix:_ Add a tenantId parameter and include tenant_id = ${tenantId} in the WHERE clause (or verify the row belongs to the tenant before releasing). Avoid touching tenant-scoped rows by id alone.
- **server/video-job-runner.ts:206** — SELECT cancelRequested FROM `video_jobs` WHERE jobId = ${jobId} is executed without tenant_id constraint (isCancelRequested). This reads a tenant-scoped table by jobId only.
  - _Fix:_ Require tenant scoping on the read (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}) or accept tenantId as an argument and include it in the WHERE clause to ensure callers cannot read/act on other tenants' job state.
- **server/video-job-runner.ts:236** — Global UPDATE in recoverStaleVideoJobs updates `video_jobs` rows across all tenants (no tenant_id filter) when marking stale jobs failed. It modifies tenant-scoped data without tenant scoping.
  - _Fix:_ If this is intended to be an admin/global maintenance operation ensure it is only callable by an operator/admin path. Otherwise restrict the update to a specific tenant (add tenant_id = ${tenantId}) or iterate tenants individually with explicit scoping. Avoid blanket updates across tenant-scoped tables.
- **server/webhook-triggers.ts:122** — processTriggerEvent() selects webhook trigger rows and LEFT JOINs to `personas` (tenant-scoped) without a tenant filter, then uses the persona info to drive processing. Reading tenant-scoped persona rows without verifying tenant ownership can leak or misuse other tenants' personas during webhook-driven processing.
  - _Fix:_ Do not SELECT from tenant-scoped tables (e.g. `personas`) without enforcing tenant ownership. Either store explicit tenant scoping on webhook triggers and join on matching tenant_id, or fetch the persona via a tenant-aware API (storage.getPersona) and verify the persona. Reject or sanitize triggers that reference personas from other tenants.
- **server/webhook-triggers.ts:167** — getOrCreateTriggerConversation() reads trigger.persona_id and later calls storage.createConversation({ tenantId: ADMIN_TENANT_ID, ..., personaId }) without first verifying that the personaId belongs to the ADMIN_TENANT_ID (or otherwise intended tenant). Using a foreign persona id across tenant boundaries can lead to cross-tenant references and privilege/ownership issues.
  - _Fix:_ Before passing a personaId into tenant-scoped operations, verify ownership: fetch the persona with a tenant-aware function and confirm its tenant_id matches the conversation tenant. If the persona belongs to a different tenant, clear the personaId, map to an appropriate local persona, or reject the operation. Prefer using storage APIs that take tenantId and enforce ownership server-side.
- **server/wiring-invariants.ts:244** — Query against tenant-scoped table `tool_performance` without any tenant_id constraint (SELECT count(*) FROM tool_performance ...). This reads across all tenants.
  - _Fix:_ If this is intended to run per-tenant, add a WHERE tenant_id = <tenantId> (or pass tenantId and use a parameterized Drizzle filter). If this is intentionally a global/system-level aggregate, document that the table is global and restrict this code to an admin-only context; prefer explicit tenant scoping rather than implicit cross-tenant reads.
- **server/wiring-invariants.ts:258** — Query counts rows in `tool_performance` (SELECT count(*)::int AS n FROM tool_performance) with no tenant_id filter — reads data across tenants.
  - _Fix:_ Constrain by tenant_id when evaluating a tenant-scoped table, or explicitly run this aggregation only as a trusted system operation and make that intent explicit. Use parameterized queries / Drizzle .where(eq(table.tenant_id, tenantId)).
- **server/wiring-invariants.ts:260** — Query against `messages` table (SELECT count(*)::int AS n FROM messages WHERE created_at > ...) without tenant_id constraint — this reads message counts across tenants.
  - _Fix:_ Add a tenant_id filter (WHERE tenant_id = <tenantId>) when measuring per-tenant metrics. If the metric must be global, ensure the caller is an explicit admin process and document/guard the cross-tenant intent.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance ... executed with no tenant_id predicate — returns tool usage across all tenants.
  - _Fix:_ Apply tenant scoping (WHERE tenant_id = <tenantId>) for tenant-scoped telemetry. For cross-tenant/system aggregation, require an explicit admin-only path and parameterize the query to avoid accidental data exposure.
- **server/wiring-invariants.ts:361** — Use of sql.raw() to query information_schema and then later selecting COUNT/MAX FROM ${spec.table} (e.g. knowledge_nudges, tool_performance) performs table-level checks without tenant_id constraints — tenant-scoped subsystem tables are probed across all tenants.
  - _Fix:_ When probing tenant-scoped tables, include tenant_id filters (or iterate tenants explicitly). Avoid relying on raw unscoped SELECTs against tenant tables; if the intent is to check global schema existence that's fine, but any data-read must be tenant-scoped or explicitly guarded in an admin-only context.
- **server/wiring-invariants.ts:366** — sql.raw(`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`) reads tenant-scoped subsystem tables (e.g. knowledge_nudges, tool_performance) without WHERE tenant_id = ... — cross-tenant read.
  - _Fix:_ Add tenant scoping to these probes (pass tenantId and include WHERE tenant_id = ${tenantId}) or explicitly run system-wide aggregations only under a protected admin context. Prefer Drizzle's parameterized APIs instead of string-interpolated raw SQL for data queries.
- **server/wiring-invariants.ts:535** — sql.raw(`SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`) runs against memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) with no tenant filter — counts rows across tenants.
  - _Fix:_ For tenant-scoped memory tables, add WHERE tenant_id = <tenantId> or run this check per-tenant. If an aggregate across tenants is truly required, make that explicit and restrict execution to an admin-only routine. Use parameterized queries where possible rather than interpolating table names into raw SQL for data reads.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*)::int AS cnt FROM graph_memory executed with no tenant_id constraint — reads graph_memory across all tenants (message text even references "across all tenants").
  - _Fix:_ If the intent is to check per-tenant emptiness, add WHERE tenant_id = <tenantId> and run per-tenant. If this is intended to be a global admin check, ensure the operation is only callable by an admin process and document that it aggregates across tenants; prefer explicit parameterized Drizzle queries over unscoped SQL.

### MEDIUM (45)

- **server/agent-channels.ts:198** — Query in getUnreadCount joins channel_subscriptions (cs) without constraining cs.tenant_id — the join condition lacks tenant scoping for the subscription row.
  - _Fix:_ Constrain the join to the caller tenant (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}) so rows from other tenants cannot be joined.
- **server/agentic-engines.ts:344** — gatherOperationalData queries heartbeat_logs (COUNT of calls) without tenant scoping (WHERE task_name != 'Self-Reflection' only), allowing cross-tenant reads.
  - _Fix:_ Add tenant_id filter to the heartbeat_logs query (e.g. WHERE tenant_id = ${tenantId} AND task_name != ...) or run the aggregation per-tenant.
- **server/agentic-engines.ts:451** — runPredictiveEngine selects recent heartbeat_logs (status='success') without tenant_id filtering — reads logs across tenants.
  - _Fix:_ Scope the SELECT to the tenant (WHERE tenant_id = ${tenantId}) or otherwise ensure the query only returns rows belonging to the target tenant.
- **server/agentic-engines.ts:519** — runOptimizationEngine queries heartbeat_logs for performance metrics without constraining tenant_id — this reads cross-tenant operational data.
  - _Fix:_ Add a tenant filter to the heartbeat_logs SELECT (e.g. WHERE tenant_id = ${tenantId}) so metrics are tenant-scoped.
- **server/agentic-engines.ts:533** — runOptimizationEngine selects heartbeat_tasks WHERE enabled = true but does not filter by tenant_id, reading tasks across tenants.
  - _Fix:_ Constrain the query by tenant_id (WHERE tenant_id = ${tenantId} AND enabled = true) or enumerate tenants and run per-tenant.
- **server/agentic-features.ts:279** — In detectCompetitorChanges the subquery used in prevSnaps (SELECT DISTINCT ON (url) id FROM competitor_snapshots ... ORDER BY url, created_at DESC) is missing tenant scoping, which can cause cross-tenant results to influence the outer query.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the inner subquery so both the outer and inner snapshot selections are tenant-scoped.
- **server/agentic-features.ts:599** — enrollInSequence checks for an existing enrollment via SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant_id, potentially matching enrollments from other tenants.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause to ensure the existing-check is limited to the caller's tenant.
- **server/auth.ts:276** — SELECT from auth_sessions by token does not include tenant_id. This reads the tenant-scoped session table without an explicit tenant_id constraint.
  - _Fix:_ When possible, bind session lookups to a tenant (e.g. when the expected tenant is known) or otherwise ensure auth_sessions is explicitly designed/treated as a global session table. As a fix: add tenant-aware checks where caller context exists or validate that subsequent logic always enforces tenant_id taken from the session for every downstream DB operation.
- **server/auth.ts:1209** — Startup loadSessionsFromDb issues SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions with no tenant filter (loads all sessions across tenants into memory).
  - _Fix:_ Avoid scanning all tenants' sessions. Restrict the query to non-expired rows and limit scope (e.g. to admin tenant or a configurable safe set), or load sessions on-demand. If a full load is required, document and justify that auth_sessions is a global table and ensure strict operational controls and ACLs around this code path.
- **server/chat-engine.ts:4038** — recordToolExecution(conversationId, ...) is invoked without an explicit tenant context. The function likely writes/read rows keyed by conversation_id; without verifying that the conversation belongs to the caller's tenant (or passing tenant_id) this can break tenant isolation.
  - _Fix:_ Either pass the tenantId to recordToolExecution (e.g. recordToolExecution(tenantId, conversationId, ...)) or perform an explicit ownership check (confirm conversation.conversationId.tenant_id === tenantId) before calling it. All write/read paths that accept foreign ids from the request must validate tenant ownership or include tenant_id in the DB predicates.
- **server/chat-engine.ts:4050** — recordToolCallForStuckDetection(conversationId, ...) is called without tenant context. If that routine persisted diagnostics keyed by conversation id, it may read/write tenant-scoped tables without enforcing tenant_id.
  - _Fix:_ Modify calls to include tenantId (e.g. recordToolCallForStuckDetection(tenantId, conversationId, ...)) or ensure the function internally validates that the provided conversationId belongs to the caller's tenant before any DB access. Ensure all diagnostic/logging writes include tenant_id or verify ownership to enforce tenant isolation.
- **server/email.ts:471** — startInboxPolling checks for existing message rows with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without scoping by tenant_id or inbox_id. This global lookup can incorrectly skip or misattribute messages across tenants.
  - _Fix:_ Scope the check by inbox_id or tenant_id (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} OR AND tenant_id = ${tenantId}) so existence checks are per-tenant/inbox and cannot affect other tenants.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from generateEmbedding(query) inside vectorSearchMemory (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} ). The vector value is derived from caller input (query) and is interpolated into raw SQL.
  - _Fix:_ Avoid injecting request-derived values into sql.raw(). Use parameterized bindings or a proper driver/Drizzle API that passes the vector as a parameter rather than building raw SQL text. If raw interpolation is unavoidable, perform strict validation (as already present) and also bind the value as a parameter (or use pg's parameterized array/vector syntax) and clearly document why it is safe.
- **server/embeddings.ts:252** — Second use of sql.raw() with the interpolated vector literal in vectorSearchMemory (ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is interpolating a request-derived embedding into raw SQL.
  - _Fix:_ Same as above: switch to parameterized queries or Drizzle constructs to avoid string-interpolated SQL. If you must use sql.raw, ensure the vector is strictly validated and passed as a SQL parameter instead of string interpolation.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). The vector was derived from generateEmbedding(query) (request-derived) and is injected into raw SQL text.
  - _Fix:_ Replace raw interpolation with a parameterized approach (pass the vector as a bound parameter) or use Drizzle APIs to construct the query safely. Retain strict numeric validation of the embedding before usage.
- **server/embeddings.ts:565** — Second use of sql.raw() with interpolated vector literal in vectorSearchKnowledge (ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). Request-derived embedding is injected into raw SQL.
  - _Fix:_ As above, avoid sql.raw interpolation of request-derived data; use parameter binding or a safe query-building API. If interpolation is necessary, ensure robust validation and consider limiting the feature or running under trusted contexts only.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec = ${sql.raw(`'${vec}'::vector`)} WHERE id = ${row.id} in backfillEmbeddingVecs injects a vec literal via sql.raw. The vec value is derived from DB-stored embedding but still interpolated into raw SQL.
  - _Fix:_ Prefer parameterized updates instead of string interpolation. Either bind the vector as a parameter or use sanitized/validated conversions that Drizzle can parameterize; keep validation of numeric shape and length and restrict who can run the backfill.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec = ${sql.raw(`'${vec}'::vector`)} WHERE id = ${row.id} in backfillEmbeddingVecs interpolates the vec into raw SQL. Although emb came from DB, using sql.raw with interpolated content risks injection if inputs are malformed.
  - _Fix:_ Use parameterized queries or Drizzle APIs to pass the vector as a parameter instead of embedding into SQL text. Maintain strict validation of embedding arrays before any interpolation, and scope the operation to a tenant context.
- **server/firecrawl.ts:214** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) — no tenant_id constraint on the write.
  - _Fix:_ Include tenant scoping in the UPDATE predicate (and/or carry tenantId from the prior select explicitly). Example: .where(and(eq(scrapedPages.id, id), eq(scrapedPages.tenantId, tenantId))) so every write is constrained to the caller's tenant.
- **server/firecrawl.ts:344** — UPDATE on tenant-scoped table 'scraped_pages' in crawl loop uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) — missing tenant_id constraint on the write.
  - _Fix:_ Add tenant scoping to the UPDATE (AND eq(scrapedPages.tenantId, tenantId)) or otherwise ensure the id you update is proven to belong to the caller's tenant in the same statement.
- **server/lib/action-ledger.ts:268** — listUnknownAttemptsAllTenants() issues a cross-tenant SELECT from tenant-scoped table `action_attempts` with no tenant_id WHERE predicate (returns rows across all tenants).
  - _Fix:_ If this cross-tenant read is truly required (reconciler), make the cross-tenant intent explicit and minimise exposed columns; otherwise restrict the SELECT by tenant_id. Prefer returning only ids + tenant_id (as you already do) and ensure any subsequent writes use the row's tenant_id and perform per-tenant guarded updates (i.e. call tenant-scoped transition()/markReconcilerDigested with the row's tenant). Consider adding an audit/authorization gate around cross-tenant reads.
- **server/lib/decline-events.ts:63** — recordDecline accepts personaId and conversationId from the caller and inserts them into `decline_events` without verifying those foreign IDs actually belong to the provided tenantId (no ownership checks). This can allow cross-tenant foreign-id references being recorded for a different tenant.
  - _Fix:_ Validate ownership of any caller-supplied foreign id before inserting. For example, SELECT 1 FROM personas WHERE id = ${opts.personaId} AND tenant_id = ${opts.tenantId} (and likewise for conversation_id) and reject/clear the foreign id if the check fails. Alternatively, only accept server-resolved foreign ids instead of trusting caller-supplied numeric ids.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions() aggregates rows from the tenant-scoped table `deliverable_grade_events` across all tenants (SELECT tenant_id, format, count(*) ... GROUP BY tenant_id, format) without being constrained to a caller tenant. This performs cross-tenant reads/writes without an explicit per-caller tenant filter.
  - _Fix:_ If this behavior is intended (nightly global admin job), ensure the job runs under an explicitly authorized admin context and document that it legitimately scans all tenants. Otherwise, restrict the scan to a specific tenant by adding WHERE tenant_id = ${tenantId} or iterate explicit tenant IDs (select distinct tenant_id first and process each tenant in an authorized context). Add audit/authorization checks so only an admin/runnable background job can perform cross-tenant aggregates.
- **server/memory-graph.ts:80** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} has no tenant_id constraint. This can modify a category row belonging to another tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or perform the UPDATE within a tenant-scoped transaction (withTenantTx) and/or derive catId only after validating the tenant.
- **server/memory-graph.ts:196** — SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR (...) LIMIT 1 checks memory_links existence without any tenant_id filter. If memory_links is tenant-scoped, this leaks cross-tenant existence information.
  - _Fix:_ Constrain existence checks by tenant (e.g. add AND tenant_id = ${tenantId}) or join to a tenant-scoped memory_entries table to validate ownership before probing links.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes without tenant filtering, enumerating tenant rows.
  - _Fix:_ If enumerating tenants is required (startup/maintenance), ensure this runs in a trusted, non-request context and is access-controlled. If not intended, add a tenant_id WHERE clause or otherwise limit the scope to an explicit tenant set.
- **server/plan-replay.ts:149** — INSERT into plan_replay_cache uses plan.tenantId (derived from the passed plan object) without validating that the caller is authorized for that tenant.
  - _Fix:_ Do not accept tenant_id from an unvalidated plan object. Require an explicit server-side tenant context or a tenantId parameter and verify the caller owns that tenant before inserting. Alternatively, validate plan.tenantId against the authenticated request's tenant.
- **server/routes.ts:766** — SELECT over `projects` (tenant-scoped) in backfillProjectDriveFolders has no tenant_id constraint. The backfill iterates all projects across tenants and calls ensureProjectFolder for each row, performing cross-tenant operations without per-tenant gating.
  - _Fix:_ If this is intended as an admin background job, ensure it runs under a platform-admin context and is auditable. Otherwise, add a WHERE clause (e.g. WHERE projects.tenant_id = <tenant>) and process only that tenant. Prefer explicit per-tenant iteration with authorization checks instead of an unscoped SELECT.
- **server/routes.ts:1532** — Public stats endpoint issues global counts by SELECTing from tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filter, exposing aggregated cross-tenant data without tenant scoping.
  - _Fix:_ If global aggregates are acceptable, ensure this is an explicitly-audited design decision and that no sensitive per-tenant detail is returned. Otherwise, add tenant scoping to return per-tenant stats only for the authenticated tenant, or gate this endpoint behind an admin check.
- **server/routes.ts:1561** — /api/public/architecture performs many unscoped COUNT/SELECT queries on tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, research_sessions) without WHERE tenant_id = <caller tenant>, leaking global metrics.
  - _Fix:_ Restrict queries to the caller's tenant (WHERE tenant_id = ...) for tenant-level metrics. If platform-wide metrics are required, make them available only to platform admins and document/secure the admin-only path accordingly.
- **server/routes.ts:2059** — SELECT from tenant-scoped `presenter_sessions` by token returns row including tenant_id without verifying caller tenant ownership. The handler returns session details based only on token, no tenant verification is applied in SQL or afterwards.
  - _Fix:_ If the token is intended as an unguessable capability, document that explicitly and ensure the token entropy/lifecycle is hardened. Otherwise, enforce tenant scoping by validating the session token maps to a tenant the caller is authorized for (or include WHERE tenant_id = <caller tenant>) before returning presenter session rows.
- **server/routes.ts:2315** — SELECT from tenant-scoped table `delivery_logs` by session id (order lookup) has no tenant_id constraint. The code looks up delivery_logs by order/session id without verifying tenant ownership (relies on the session id being unguessable), opening risk if session IDs are exposed.
  - _Fix:_ If the order/session ID is intended to be a capability token, ensure its entropy and issuance semantics are robust and document that design. Otherwise enforce tenant scoping (WHERE delivery_logs.tenant_id = <caller tenant>) or require additional authorization to avoid accidental cross-tenant data disclosure.
- **server/routes.ts:5480** — Potential tenant-scoping fallback to ADMIN_TENANT_ID when updating a conversation title: storage.updateConversation(conversationId, { title: newTitle }, conv.tenantId ?? ADMIN_TENANT_ID). If conv.tenantId is missing or falsy this will perform the update under the admin tenant, which can cause cross-tenant writes.
  - _Fix:_ Avoid defaulting to ADMIN_TENANT_ID for tenant-scoped writes. Determine the caller's tenant explicitly (e.g. getTenantFromRequest(req) or ensure conv.tenantId is validated) and fail if tenant context is missing. Pass the verified tenantId to storage.updateConversation so every write is scoped to the correct tenant.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table `agent_jobs` has no tenant_id WHERE clause (SELECT ... FROM agent_jobs j ... WHERE j.status IN (...)). This returns jobs across all tenants.
  - _Fix:_ If the caller should be restricted to a specific tenant, add AND j.tenant_id = ${tenantId}. If the cross-tenant view is intentional, ensure the route is strictly platform-admin gated (and auditable). Prefer explicit admin gating at the route-level and/or make the cross-tenant intent obvious in code (eg. pass an explicit admin flag) rather than relying on an unfiltered SELECT.
- **server/sentiment-detector.ts:167** — logSentimentEvent inserts a sentiment_events row referencing a caller-provided conversation_id but does not verify that the conversation_id belongs to the given tenant. This can allow events to be created that reference another tenant's conversation (data-integrity / confusion risk).
  - _Fix:_ Before inserting, validate ownership of conversation_id (e.g. SELECT or use storage.getConversation(conversationId, tenantId)) and ensure the conversation belongs to the tenant. Or derive the conversation's tenant from a trusted source and reject mismatched inputs.
- **server/storage.ts:934** — getHeartbeatTask selects heartbeat_tasks by id without any tenant filter. heartbeat_tasks is tenant-scoped (tenant_id column exists) so this read can return tasks from other tenants.
  - _Fix:_ Accept tenantId or otherwise include tenant scoping (eq(heartbeat_tasks.tenant_id, tenantId)) or validate caller's rights to access the task before returning it.
- **server/storage.ts:986** — getDueHeartbeatTasks executes a raw SELECT FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() without tenant scoping — it returns due tasks across tenants. This allows cross-tenant task claiming if subsequent claim logic is also unscoped.
  - _Fix:_ If the worker is intended to process tasks across all tenants, ensure the caller is an authorized system worker. Otherwise, scope to tenant or include an explicit admin-only guard. At minimum, ensure subsequent claim/update logic validates tenant ownership before acting on a task.
- **server/storage.ts:1019** — claimHeartbeatTasks issues an UPDATE on heartbeat_tasks WHERE id = ${id} ... without tenant_id constraint. This can claim tasks across tenants if an attacker supplies task ids from other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or validate that each task id belongs to the expected tenant before updating. If multi-tenant claiming across tenants is intentional, require an explicit admin/system credential.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table custom_tools filters only on name (eq(customTools.name, parsed.name)) and does NOT include tenant_id. This can produce cross-tenant lookups or block creation based on another tenant's tools.
  - _Fix:_ Constrain the lookup to the intended tenant (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))). If you need to check both bare and namespaced names, include tenantId in the predicate for both checks.
- **server/tool-learning.ts:155** — SELECT by custom_tools.id (eq(customTools.id, inserted.id)) does not include tenant_id. Reads against tenant-scoped tables must include the tenant constraint.
  - _Fix:_ Include tenant scoping on selects too (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) or pass/derive the tenantId from the inserted row and use it in the WHERE to ensure tenant-only reads.
- **server/tools.ts:3016** — SELECT against tenant-scoped table project_conversations lacks an explicit tenant_id constraint. The query SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} does not include tenant_id filtering.
  - _Fix:_ Constrain reads to the caller tenant (e.g. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) or JOIN against projects/conversations that are already tenant-filtered to ensure the engine never reads rows belonging to other tenants.
- **server/tools.ts:2975** — SELECT against tenant-scoped table project_files (dup check) lacks an explicit tenant_id constraint. The duplication check queries project_files WHERE project_id = ${params.id} ... but does not include tenant_id.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND ...), or JOIN to projects filtered by tenant_id to make the tenant binding explicit. Avoid relying solely on project_id correlation without an explicit tenant filter.
- **server/tools.ts:2913** — Correlated subqueries count rows from tenant-scoped tables (project_files, project_notes, project_conversations) without an explicit tenant_id predicate on those tables. Example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) is not restricting project_files by tenant_id.
  - _Fix:_ Make tenant scoping explicit in the subqueries (e.g. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or JOIN the tenant-filtered projects table to enforce the tenant constraint). Ensure every read against tenant-scoped tables includes an explicit tenant constraint or a join to a tenant-filtered table so isolation cannot be bypassed.
- **server/webhook-triggers.ts:58** — Query joins the tenant-scoped `personas` table without constraining by tenant (SELECT w.*, p.name as persona_name FROM webhook_triggers w LEFT JOIN personas p ON p.id = w.persona_id ...). This can leak persona rows across tenants.
  - _Fix:_ When exposing trigger listings, do not join directly to the personas table unscoped. Either (a) join with an explicit tenant constraint (e.g. ON p.id = w.persona_id AND p.tenant_id = <expected tenant>), or (b) resolve persona names via a tenant-aware accessor (storage.getPersona) that verifies the persona belongs to the intended tenant (e.g. ADMIN_TENANT_ID for admin hooks). Ensure any persona reads are filtered by tenant_id.
- **server/webhook-triggers.ts:81** — createTrigger() inserts a caller-supplied personaId into the global `webhook_triggers` table without validating that the persona belongs to the caller/admin tenant. This enables creation of triggers that reference personas from arbitrary tenants.
  - _Fix:_ Validate any personaId supplied by the caller before persisting it. Look up the persona with a tenant-aware accessor (e.g. storage.getPersona) and ensure persona.tenant_id === expectedTenantId (ADMIN_TENANT_ID for global hooks) before inserting. If the persona does not belong to the expected tenant, reject the request.

### Suppressed — confirmed false positives (236)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — autoApplyEligibleInsights performs an UPDATE on ai_insights (status → 'applied') for category IN (...) without any tenant filter — this can modify rows across all tenants.
  - _Why false positive:_ Platform-wide housekeeping sweep marks eligible insights applied and processes returned rows with their own tenant_id; no tenant-facing mixed output at this line.
- **server/agentic/runs.ts:36** _(internal-worker-pk)_ — appendStep updates agent_runs but the UPDATE .where(...) only filters by run id (eq(agentRuns.id, runId)) and does NOT include tenant scoping. This allows modifying another tenant's run if runId is known.
  - _Why false positive:_ Server-internal worker helper updates agent_runs by globally unique run id created server-side, not shown crossing an untrusted tenant boundary.
- **server/agentic/runs.ts:51** _(internal-worker-pk)_ — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) but does not constrain by tenant_id; this is an unscoped write to a tenant-scoped table.
  - _Why false positive:_ Internal run-state worker mutation by server-generated primary key; code context shows agent run lifecycle helpers, not request-scoped direct access.
- **server/agentic/runs.ts:55** _(internal-worker-pk)_ — replaceRunState performs an UPDATE on agent_runs using only run id in the WHERE clause (eq(agentRuns.id, runId)) and lacks a tenant_id constraint.
  - _Why false positive:_ Internal helper replaces run state by server-issued run id in agent workflow code; fits worker-by-PK benign pattern.
- **server/agentic/runs.ts:64** _(internal-worker-pk)_ — completeRun updates agent_runs and clears/completes the run using only the run id in the WHERE clause; there is no tenant_id constraint, enabling cross-tenant writes.
  - _Why false positive:_ Completion update is part of internal agent run lifecycle using server-generated run id; not an untrusted cross-tenant request boundary in shown path.
- **server/agentic/runs.ts:94** _(internal-worker-pk)_ — failRun updates agent_runs to set status='failed' using only the run id in the WHERE clause (eq(agentRuns.id, runId)) with no tenant_id filter.
  - _Why false positive:_ Failure-state update is internal worker lifecycle code keyed by globally unique run id, matching known benign worker-by-PK pattern.
- **server/agentic/runs.ts:102** _(internal-worker-pk)_ — pauseRun updates agent_runs to set status='paused' using only the run id in the WHERE clause; tenant_id is not enforced, allowing cross-tenant mutation.
  - _Why false positive:_ Pause mutation occurs in same internal agent run helper set operating on server-created run ids, not caller-owned tenant input in evidence shown.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL has no tenant_id constraint — can modify another tenant's conversation row.
  - _Why false positive:_ Backfill loop is platform-wide maintenance; source rows were selected with joined tenant-consistent conversation/project linkage and each row is handled with its own tenant_id.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table, this INSERT allows creating rows without an explicit tenant_id, breaking the invariant that every tenant-scoped INSERT must include tenant_id.
  - _Why false positive:_ invoice_items has no tenant_id column and is a child of tenant-scoped invoices via invoice_id, so omitting tenant_id is correct by design.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE against tenant-scoped table `commitments` executes without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can mutate another tenant's row if row.id is from a different tenant.
  - _Why false positive:_ row.id comes from claimedRows produced by a prior claim statement constrained with c.tenant_id and RETURNING tenantId; this revert only unclaims that previously verified row.
- **server/embeddings.ts:684** _(system-sweep)_ — UPDATE against tenant-scoped tables (memory_entries / agent_knowledge) without constraining tenant_id. The UPDATE in storeEmbeddingVec uses WHERE id = ${id} but does not restrict by tenant_id, allowing cross-tenant writes.
  - _Why false positive:_ Boot/background embedding backfill updates housekeeping fields only; row ids come from prior sweep selection, not tenant-facing input.
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter. This background backfill reads rows across all tenants and then writes (via storeEmbeddingVec) without scoping to a tenant.
  - _Why false positive:_ Platform-wide backfill sweep reads rows across tenants only to compute/store embeddings and log counts; no tenant data is returned or mixed into one response.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT from memory_entries in backfillEmbeddingVecs lacks tenant_id scoping. The code reads memory_entries across all tenants and then performs UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} with no tenant restriction.
  - _Why false positive:_ BackfillEmbeddingVecs is a maintenance sweep updating embedding_vec housekeeping per selected row, not a tenant-facing cross-tenant access path.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT from agent_knowledge in backfillEmbeddingVecs has no tenant_id filter and later UPDATE agent_knowledge SET embedding_vec WHERE id = ${row.id} also lacks tenant scoping. This touches tenant-scoped data without ownership checks.
  - _Why false positive:_ Global maintenance sweep over agent_knowledge updates per-row vector metadata only and does not expose mixed tenant content to users.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT INTO `project_files` does not include tenant_id. Per-platform invariant every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via project_id to tenant-scoped projects.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT INTO `project_notes` does not include tenant_id. Every INSERT into tenant-scoped tables must pass tenant_id explicitly (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id to tenant-scoped projects.
- **server/plan-executor.ts:117** _(upstream-verified)_ — Unscoped UPDATE against tenant-scoped table: appendExecutionLog executes `UPDATE plans ... WHERE id = ${planId}` with no tenant_id constraint. This can allow updating a plan belonging to a different tenant if a planId from another tenant is passed.
  - _Why false positive:_ appendExecutionLog is invoked after executePlan claims the plan row and resolves its tenant_id in the same call path; planId is server-controlled worker state, not fresh tenant input here.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Unscoped CAS update on plans: claiming the plan uses `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...` but does not constrain by tenant_id. Without a tenant constraint this may claim and transition plans from any tenant.
  - _Why false positive:_ executePlan is an internal server worker operating by globally unique plan primary key; claim step returns the row's own tenant_id for subsequent scoped processing.
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — Unscoped UPDATE on plans: on empty/invalid plan_json path the code runs `UPDATE plans SET status = 'failed' ... WHERE id = ${planId}` with no tenant filter. This can flip status for a plan belonging to another tenant.
  - _Why false positive:_ Server-invoked executor operates on a plan already claimed by unique id; this follow-up update is on that same server-side planId, not a request-bound tenant selector.
- **server/plan-executor.ts:728** _(internal-worker-pk)_ — Unscoped final status UPDATE: the final `UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}` lacks a tenant_id constraint. This writes terminal state to a plan without verifying tenant ownership.
  - _Why false positive:_ Final status write is inside the same internal worker run for a previously claimed plan row identified by server-side planId.
- **server/plan-executor.ts:778** _(system-sweep)_ — Global UPDATE that resets stuck plans: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id` is unscoped and will affect executing plans across all tenants. There is no tenant constraint, so background recovery can modify other tenants' plans.
  - _Why false positive:_ Boot-time recovery intentionally sweeps stale/pending plans across all tenants and only resets status/queues by per-row ids, without returning mixed tenant data.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not constrain by tenant_id — a rule could disable a task belonging to another tenant if ids collide.
  - _Why false positive:_ t.id comes from a prior SELECT constrained by ht.tenant_id = tenantId, so the update targets rows already ownership-filtered in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} is missing a tenant_id constraint and can modify another tenant's desks if id values overlap.
  - _Why false positive:_ desk.id is sourced from a preceding SELECT with ad.tenant_id = tenantId in the same branch, so tenant ownership was established earlier.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant scoping and may update rules belonging to other tenants.
  - _Why false positive:_ rule.id is used in logAction alongside tenantId and originates from the tenant-scoped governor rule processing path; this helper is updating the same verified rule.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses only id in WHERE (UPDATE memory_entries SET ... WHERE id = ${storedId}) after a tenant-scoped SELECT. The UPDATE lacks tenant_id, allowing cross-tenant modification if ids are misused.
  - _Why false positive:_ storedId comes from immediately preceding tenant-scoped SELECT on memory_entries with tenant_id=input.tenantId and persona/category filters
- **server/research-engine.ts:165** _(system-sweep)_ — Global UPDATE on tenant-scoped table research_sessions without any tenant_id constraint (UPDATE research_sessions ... WHERE status = 'running' ...). This can modify sessions across all tenants.
  - _Why false positive:_ startup/maintenance cleanup globally auto-completes stale running sessions, only mutates housekeeping fields and returns ids for server logs
- **server/research-engine.ts:400** _(system-sweep)_ — DELETE from tenant-scoped table agent_knowledge without tenant_id (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will remove rows for all tenants.
  - _Why false positive:_ global expiry cleanup of expired agent_knowledge rows; deletes housekeeping-expired data across tenants without returning mixed tenant content
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into `project_conversations` does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES ...) — tenant_id is required and missing, allowing cross-tenant linkage.
  - _Why false positive:_ project_conversations has no tenant_id and is a child/link table isolated via tenant-scoped parent project validated just above
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not include tenant_id. The route inserts (project_id, note, author) without explicitly setting tenant_id (table columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_notes has no tenant_id and insert is under prior tenant-scoped project ownership check
- **server/routes/projects.ts:225** _(fk-transitive)_ — INSERT into tenant-scoped table project_files (base64 upload path) does not include tenant_id. The query inserts project file rows without explicitly setting tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is under prior tenant-scoped project ownership check
- **server/routes/projects.ts:293** _(fk-transitive)_ — INSERT into tenant-scoped table project_files (multipart upload path) does not include tenant_id. project_files rows are created without tenant_id column set.
  - _Why false positive:_ project_files has no tenant_id and insert is under prior tenant-scoped project ownership check
- **server/seed.ts:3771** _(system-sweep)_ — DELETE from tenant-scoped table `heartbeat_tasks` is executed without any tenant constraint (no WHERE tenant_id = ...). This will delete matching rows across all tenants.
  - _Why false positive:_ boot-time housekeeping purge of stale auto-disabled heartbeat tasks across tenants; status/cleanup only, no tenant data returned
- **server/seed.ts:3799** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` (REPLACE on fact column) is executed without a tenant_id filter. This will modify rows for all tenants.
  - _Why false positive:_ boot-time canonicalization backfill across tenants updates text in place only and does not expose or mix tenant data to users
- **server/seed.ts:3825** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` (model backfill) runs without a tenant constraint, affecting conversations for all tenants.
  - _Why false positive:_ boot-time global model-id backfill updates conversation metadata in place across tenants; housekeeping migration, no tenant data returned
- **server/self-reflection.ts:366** _(upstream-verified)_ — storeLesson executes an UPDATE on memory_entries using WHERE id = <id> with no tenant_id constraint. Although the row was discovered by a prior tenant-scoped SELECT, the UPDATE itself is not constrained and could update rows across tenants if ids are misused.
  - _Why false positive:_ id comes from immediately prior SELECT scoped by tenant_id = tenantId in same call path; update targets that discovered row.
- **server/storage.ts:838** _(system-sweep)_ — archiveExpiredMemories executes a raw UPDATE on memory_entries to mark expired rows as archived with NO tenant scoping — this will update rows across all tenants (global destructive write).
  - _Why false positive:_ platform-wide housekeeping update only archives expired memory rows and returns aggregate count/ids, not tenant data to another tenant-facing surface.
- **server/storage.ts:854** _(system-sweep)_ — archiveStaleMemories executes a raw UPDATE on memory_entries to archive stale rows without tenant scoping — this updates rows across tenants.
  - _Why false positive:_ platform-wide stale-row archival is housekeeping only, mutating status fields across tenants without mixing tenant content.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — Global SELECT from experiments in runDueAbExperiments is not tenant-scoped (missing WHERE tenant_id = <caller tenant>) — this reads metadata across all tenants.
  - _Why false positive:_ heartbeat maintenance sweep intentionally scans running experiments across tenants and then processes each row using its own row-carried tenant_id.
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting selects ai_insights WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER but WITHOUT tenant_id — reads rows across all tenants.
  - _Why false positive:_ durability sweep intentionally reads pending rows across tenants and routes each using the row's validated tenant_id; no mixed tenant output.
- **server/agentic-features.ts:638** _(upstream-verified)_ — advanceSequence updates outreach_enrollments to mark completed (UPDATE ... WHERE id = ${enrollment.id}) without a tenant_id constraint, allowing cross-tenant writes by id.
  - _Why false positive:_ enrollment.id is obtained from immediately prior SELECT on outreach_enrollments filtered by e.tenant_id = params.tenantId in same function.
- **server/agentic-features.ts:671** _(upstream-verified)_ — advanceSequence updates outreach_enrollments (SET current_step, last_sent_at, next_send_at ...) using WHERE id = ${enrollment.id} with no tenant filter — write is not tenant-scoped.
  - _Why false positive:_ update uses enrollment row selected moments earlier from outreach_enrollments under e.tenant_id = params.tenantId in same call path.
- **server/agentic-features.ts:721** _(upstream-verified)_ — classifyReply updates outreach_enrollments (UPDATE ... WHERE id = ${row.id}) without constraining tenant_id — this can modify another tenant's enrollment row by id.
  - _Why false positive:_ row.id comes from immediately prior SELECT on outreach_enrollments constrained by e.tenant_id = params.tenantId and contact email in same function.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals to set status='expired' based purely on pending/expiresAt with no tenant constraint — this updates approvals across all tenants in one operation.
  - _Why false positive:_ Global expiry sweep intentionally updates stale approvals across tenants and returns per-row tenant_id for isolated follow-up run updates.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table without restricting by tenant_id.
  - _Why false positive:_ Heartbeat sweep enumerates active budgets across tenants, processes each row with its own tenant_id, and emits per-tenant events only.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Multi-row SELECT joins agent_runs and agent_approvals without restricting to a specific tenant (no WHERE tenant_id = <caller tenant>). This scans/returns tenant-scoped rows across tenants.
  - _Why false positive:_ Worker sweep scans pending approval runs across tenants but carries tenant_id per row and resumes each run individually without mixing tenant content.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on tenant-scoped table agent_wake_schedules performs a row selection without any tenant_id constraint (inner SELECT in WHERE id IN (...)). This can claim/modify rows across all tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due wake rows across tenants, then emits and updates each row using its own tenant_id.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — Backfill query SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... runs without a tenant_id constraint and scans conversations across all tenants.
  - _Why false positive:_ Backfill scans conversations across tenants for maintenance and carries c.tenant_id for per-conversation processing rather than tenant-facing mixed output.
- **server/auto-memorize.ts:163** _(system-sweep)_ — SELECT from tenant-scoped table `messages` with no tenant_id WHERE clause — reads messages across all tenants and relies on post-query partitioning.
  - _Why false positive:_ Cross-tenant maintenance read partitions messages by tenant before LLM use, explicitly avoiding mixed-tenant prompts.
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` WHERE conversation_id = ${conversationId} has no tenant_id constraint.
  - _Why false positive:_ Conversation ownership is proven just above with conversations WHERE id = conversationId AND tenant_id = tenantId before reading its messages.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` WHERE conversation_id = ${conversationId} has no tenant_id constraint (autoSaveProjectTranscript).
  - _Why false positive:_ assertConversationInTenant plus scoped conversation/project checks in the same call path prove tenant ownership before the messages query.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Backfill SELECT queries scan `conversations` (tenant-scoped) without constraining tenant_id (backfillProjectTranscripts convRes) — enumerates conversations across all tenants.
  - _Why false positive:_ Boot/backfill maintenance scan intentionally iterates all tenants and passes each row's own tenant_id to per-row handling; no mixed tenant-facing output.
- **server/auto-transcript.ts:171** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` (missingLinks query) is executed without a tenant_id filter — backfill step iterates across tenants.
  - _Why false positive:_ Global maintenance backfill for missing links; processes each conversation row individually and only inserts housekeeping links, not tenant-facing mixed data.
- **server/chat-engine.ts:4365** _(fk-transitive)_ — Call to storage.attachCitations(...) does not include tenant context. This likely performs an INSERT/UPDATE against a tenant-scoped citations/messages table without explicitly supplying tenant_id, violating the invariant that every write must include tenant_id.
  - _Why false positive:_ Citations are attached to a tenant-scoped message identified by messageId; child records are isolated transitively through the parent message.
- **server/commitments.ts:136** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` lacks a tenant_id constraint (SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...). This reads rows across all tenants, risking cross-tenant data exposure.
  - _Why false positive:_ Platform-wide overdue scanner intentionally fans in across tenants, but uses each row's tenant_id separately and emits redacted owner-digest housekeeping only.
- **server/data-protection.ts:131** _(system-sweep)_ — Permanently-purge path issues: DELETE statements remove messages/compaction_archives/conversations using only conversation_id or id and do not include an explicit tenant_id predicate. Deletions against tenant-scoped tables without an explicit tenant_id in the WHERE clause risk accidental cross-tenant effects if IDs are misused or assumptions about uniqueness change.
  - _Why false positive:_ Expired-row purge is a platform housekeeping sweep; it handles each selected conversation with its own tenant_id and performs non-tenant-facing deletion/backup cleanup only.
- **server/dfy-intake.ts:52** _(signed-url)_ — getDfyIntakeByToken SELECTs from tenant-scoped `dfy_intake_forms` by token alone (eq(token, token)) with NO tenant_id constraint. A token lookup can return a form belonging to another tenant.
  - _Why false positive:_ High-entropy intake token is the bearer authorization secret for the form lookup; access is controlled by possession of the token rather than tenant_id.
- **server/dfy-intake.ts:96** _(upstream-verified)_ — submitDfyIntake updates dfy_intake_forms via WHERE eq(dfy_intake_forms.id, form.id) but does NOT include tenant_id in the WHERE. If the form was resolved cross-tenant (or an attacker supplies an id), this can modify another tenant's row.
  - _Why false positive:_ form.id comes from getDfyIntakeByToken(token), not caller input, and nearby lookup already resolves the same row before update.
- **server/doc-collections.ts:166** _(upstream-verified)_ — addDocument deletes existing doc_chunks with SQL DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but omits tenant_id. This can remove chunks belonging to other tenants sharing the same doc_path/collection_id (or if collection ids collide across tenants).
  - _Why false positive:_ same call path first verifies doc_collections ownership with id+tenant_id; delete is then scoped by that owned collectionId.
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks embedding with WHERE id = ${row.id} but omits tenant_id. Updating by id without tenant scoping can modify another tenant's chunk if ids collide or an attacker manipulates ids.
  - _Why false positive:_ rows are selected immediately above from doc_chunks with collection_id and tenant_id; update uses those verified row ids within same loop.
- **server/email.ts:516** _(system-sweep)_ — backfillEmptyBodies selects inbox_messages rows with no tenant filter: SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id. This scans all tenants' inbox_messages. Later the code updates rows by id (see below) also without tenant constraint.
  - _Why false positive:_ global backfill sweep only fills missing body fields per row and emits counts/logs; it does not return or mix tenant data to tenant-facing surfaces.
- **server/email.ts:529** _(system-sweep)_ — backfillEmptyBodies updates inbox_messages via UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} with NO tenant_id constraint. This can modify rows belonging to other tenants if ids overlap or the initial SELECT was global.
  - _Why false positive:_ per-row housekeeping update inside global backfill sweep; mutates only empty-body fields and does not expose cross-tenant content.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' uses only project id in WHERE clause (where(eq(projects.id, projectId))) — write is not constrained by tenant_id.
  - _Why false positive:_ same function verifies fetched project's tenantId matches caller before reaching the update.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images performed without including or validating tenant_id. This writes tenant-scoped data (presenter_slide_images tied to a presenter session) using only session_id, with no tenant constraint.
  - _Why false positive:_ presenter_slide_images is inserted via session_id under tenant-scoped presenter_sessions; child table is isolated transitively through the parent session.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals without tenant_id filtering. code_proposals is tenant-scoped but the query pulls stale rows across all tenants.
  - _Why false positive:_ platform-wide stale proposal audit intentionally scans across tenants for TTL housekeeping and produces admin audit findings, not tenant-facing mixed output.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — update has no tenant_id constraint and will affect rows across tenants.
  - _Why false positive:_ global maintenance archival updates only proposal status for stale rows selected by the sweep; housekeeping mutation across tenants is intentional.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks WHERE enabled = true ... (stuck heartbeat detection) has no tenant_id filter. heartbeat_tasks are often tenant-scoped; this will surface tasks for all tenants.
  - _Why false positive:_ Global maintenance scan for stale heartbeat rows; not tenant-facing and only flags/housekeeps rows.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — disables tasks by id without any tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ Follow-on housekeeping update over rows selected by the global stale-task sweep; disables stale tasks only.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from research_schedules has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads tenant-scoped rows without restricting to a specific tenant.
  - _Why false positive:_ Heartbeat worker intentionally scans due research schedules across tenants and processes each row with its own sched.tenant_id.
- **server/heartbeat.ts:102** _(upstream-verified)_ — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — no tenant_id constraint. This can modify another tenant's schedule by id alone.
  - _Why false positive:_ Update targets sched.id from the immediately preceding SELECT of that same schedule row, carrying sched.tenant_id through processing.
- **server/heartbeat.ts:107** _(upstream-verified)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Update uses sched.id from the prior due-schedules SELECT in the same loop; row ownership already established upstream.
- **server/heartbeat.ts:121** _(upstream-verified)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id constraint on update.
  - _Why false positive:_ Recovery update still targets the already-selected sched row from the same call path, not untrusted input.
- **server/heartbeat.ts:378** _(system-sweep)_ — SELECT from event_log (SELECT * FROM event_log WHERE status = 'pending' ...) has no tenant_id constraint. This reads pending events across all tenants without scoping.
  - _Why false positive:_ Global event processor intentionally reads pending events across tenants and routes each using the row-carried event.tenant_id.
- **server/heartbeat.ts:266** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — missing tenant_id constraint. This updates a tenant-scoped heartbeat_tasks row by id only.
  - _Why false positive:_ Startup normalization pass over heartbeat tasks is cross-tenant housekeeping that only adjusts model metadata per row.
- **server/heartbeat.ts:270** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — no tenant_id constraint on the UPDATE.
  - _Why false positive:_ Startup cleanup intentionally mutates task model metadata across tenants as maintenance, without mixing tenant data.
- **server/heartbeat.ts:1534** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 — missing tenant_id constraint. heartbeat_logs is tenant-scoped and this reads across tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this log table is not tenant-scoped.
- **server/heartbeat.ts:2628** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint after creating a delegated task. This can change approval_status for a task id regardless of tenant.
  - _Why false positive:_ Server-created newTask.id from storage.createHeartbeatTask is immediately updated by PK inside internal worker flow, not from untrusted request input.
- **server/heartbeat.ts:3080** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint (in delegateTaskFromChat).
  - _Why false positive:_ Server-created newTask.id is updated immediately in the same internal flow; PK is not sourced from an untrusted cross-tenant boundary.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs issues a global UPDATE on agent_jobs to claim rows (UPDATE agent_jobs SET ... WHERE id IN (SELECT id FROM agent_jobs WHERE status='pending' ... ) ) with no tenant_id constraint. This mutates tenant-scoped rows without scoping to a tenant.
  - _Why false positive:_ global worker queue claim intentionally operates across tenants and returns row-carried tenant_id for per-row processing
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases STEP 1 performs UPDATE agent_jobs SET status='failed_terminal' ... WHERE status = 'running' AND lease_until < NOW() AND attempts >= max_attempts with no tenant_id constraint — terminalizes running jobs across all tenants without tenant scoping.
  - _Why false positive:_ maintenance reclaim pass updates only housekeeping/status fields across tenants and does not mix tenant data
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases STEP 2 re-queues expired `running` jobs back to pending via an UPDATE with no tenant_id constraint (UPDATE agent_jobs SET status='pending' ... WHERE status = 'running' AND lease_until < NOW()). This affects jobs for all tenants.
  - _Why false positive:_ global lease-expiry requeue is housekeeping across tenants, mutating only job state fields
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs (SET status='succeeded' ...) using only id/attempts guards, but no tenant_id constraint (UPDATE agent_jobs ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts}). This allows a caller with just a job id to complete a job from another tenant.
  - _Why false positive:_ completeJob is a server worker completion path keyed by claimed job id plus attempts fencing, not shown as request-derived
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob first SELECTs max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — reads tenant-scoped row by id without verifying tenant ownership.
  - _Why false positive:_ failJob is internal worker logic reading by server-held job id to decide retry policy, not a tenant-facing read path
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob's terminal-path UPDATE sets status='failed_terminal' WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} without tenant_id filtering. This can mark jobs from other tenants as failed terminal by id alone.
  - _Why false positive:_ terminal fail update is internal worker state transition by claimed job primary key with attempts fence
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob's retry-path UPDATE (requeue with next_run_at/backoff) updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint, enabling cross-tenant mutation.
  - _Why false positive:_ retry-path update is internal worker state transition by claimed job primary key with attempts fence
- **server/job-queue.ts:385** _(global-table)_ — getJobStats runs SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > NOW() - INTERVAL '7 days' GROUP BY status with no tenant filter — aggregates job counts across all tenants and returns global stats from a tenant-scoped table.
  - _Why false positive:_ operator dashboard aggregate returns only cross-tenant counts, not tenant row data; this is a global admin metric
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — sweepStaleAttempts() runs an UPDATE on tenant-scoped table `action_attempts` using WHERE id IN (SELECT id FROM action_attempts ... ) with no tenant_id constraint — this can transition rows across tenants in bulk.
  - _Why false positive:_ Global crash-recovery sweep intentionally updates stale rows across tenants and only mutates housekeeping/status fields; no tenant data is returned or mixed.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped table `projects` groups/counts across tenants with NO tenant_id WHERE clause (reads tenant-scoped rows for all tenants).
  - _Why false positive:_ Boot/backfill maintenance aggregates per-tenant counts across all tenants to decide snapshot import; it does not expose tenant rows to another tenant or mix tenant content into one response.
- **server/lib/mention-injection.ts:121** _(global-table)_ — fetchRadioMessages() joins channel_messages cm to personas p using only p.id = cm.from_persona_id but does not constrain the joined personas row by tenant_id. Because there is no DB-level FK/ownership enforcement, this can leak persona fields across tenants if ids collide or are attacker-controlled.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so the joined table is not tenant-scoped and lack of tenant predicate on personas is expected.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — UPDATE of tenant-scoped table `scheduled_posts` (CTE update that flips pending→publishing) does not include an explicit tenant_id constraint in the WHERE clause.
  - _Why false positive:_ Cron runner intentionally claims due rows across tenants, returns each row with its own tenant_id, and processes them per-row without cross-tenant output mixing.
- **server/lib/scheduled-post-runner.ts:135** _(upstream-verified)_ — UPDATE scheduled_posts in the quality-gate blocked branch uses WHERE id = ${row.id} but no tenant_id predicate. This is a write against a tenant-scoped table without an explicit tenant constraint.
  - _Why false positive:_ The shown UPDATE already includes AND tenant_id = row.tenant_id in the WHERE clause.
- **server/lib/scheduled-post-runner.ts:217** _(upstream-verified)_ — UPDATE scheduled_posts (persisting per_platform_results / next_attempt_at / status) uses WHERE id = ${row.id} with no tenant_id check, performing a tenant-scoped write without an explicit tenant constraint.
  - _Why false positive:_ The shown UPDATE already constrains by both id and tenant_id for the row being processed.
- **server/lib/scheduled-post-runner.ts:244** _(upstream-verified)_ — Error-path UPDATE scheduled_posts (release on exception) updates WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id. This performs a tenant-scoped write without an explicit tenant constraint.
  - _Why false positive:_ The error-path UPDATE already includes tenant_id in the WHERE clause along with id and status.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` with no tenant_id filter — cross-tenant read.
  - _Why false positive:_ Read-only maintenance audit intentionally scans across tenants and carries tenant_id per row in bounded results; no tenant content is merged or mutated.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT from tenant-scoped table `mind_tickets` with no tenant_id filter — cross-tenant read.
  - _Why false positive:_ Read-only cross-tenant stuck-task audit is a bounded maintenance sweep with per-row tenant_id, not a tenant-facing mixed-content response.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table tenant_voice_profiles uses only id in WHERE (eq(tenantVoiceProfiles.id, existing.id)) with no tenant_id constraint. This allows an attacker or bug that supplies/guesses an id to update a profile belonging to a different tenant.
  - _Why false positive:_ UPDATE uses existing.id from getVoiceProfile({ tenantId, profileName }) immediately above, so ownership is established in the same call path.
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) does not set tenant_id. Per DB invariant, tenant-scoped tables must receive tenant_id in INSERTs; omitting it will create links not bound to any tenant or to the wrong tenant.
  - _Why false positive:_ memory_links has no tenant_id column; links are scoped transitively through source/target memory_entries, which are tenant-scoped and selected from tenant-filtered rows.
- **server/memory-graph.ts:408** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} has no tenant_id constraint. This can update a memory entry in another tenant if mem.id is guessed or misused.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries filtered by tenant_id = tenantId in the same function, so the row being updated was already tenant-scoped.
- **server/minds-engine.ts:472** _(upstream-verified)_ — Rollback UPDATE that restores a plan to awaiting_approval uses WHERE id = ${args.planId} AND status = 'revising' with no tenant_id constraint. That update can affect a plan belonging to another tenant.
  - _Why false positive:_ Shown query is tenant-scoped for both mind_tickets and joined minds; the cited unscoped rollback UPDATE is not present at this location.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list. The code relies on assertProjectInTenant(...) but still writes the row without an explicit tenant_id column.
  - _Why false positive:_ project_files has no tenant_id column and is isolated transitively via tenant-owned project_id; assertProjectInTenant confirms parent ownership before insert.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT FROM oauth_subscriptions (`SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE`) runs without a tenant_id filter, reading rows across all tenants.
  - _Why false positive:_ validateSubscriptionsOnStartup is a startup-wide maintenance loop over all subscriptions, handling each row with its own tenant_id rather than returning mixed tenant data.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh executes SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter (reads subscriptions across all tenants).
  - _Why false positive:_ proactiveTokenRefresh is a background maintenance sweep across tenants; each subscription is processed independently using its row-carried tenant_id.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions runs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant filtering, iterating all tenants' subscriptions.
  - _Why false positive:_ forceRefreshAllSubscriptions is explicitly a cron-style global refresh loop, processing each tenant subscription separately with its own tenant_id.
- **server/orchestrator-ledger.ts:450** _(upstream-verified)_ — Use of sql.raw(lockKey) in SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). sql.raw() injects raw SQL text derived (via ledgerKey) from caller-supplied input, creating an injection/isolation risk.
  - _Why false positive:_ Flagged line is advisory-lock SQL, not tenant data access; surrounding memory_entries reads/writes are explicitly scoped by tenant_id = tenantId.
- **server/plan-executor.ts:787** _(system-sweep)_ — Unscoped SELECT for pending plans: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50` returns approved plans across all tenants. This exposes plan ids from other tenants to the executor loop and schedules execution without tenant scoping.
  - _Why false positive:_ Boot-time recovery worker intentionally scans approved plans across tenants and only schedules per-plan execution by row id; this is housekeeping, not tenant-facing data mixing.
- **server/process-governor.ts:199** _(global-table)_ — SELECT AVG(duration_ms) FROM heartbeat_logs(...) lacks a tenant_id filter — response time is aggregated across all tenants.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column, so this response-time aggregate is over a non-tenant-scoped global table.
- **server/process-governor.ts:121** _(global-table)_ — Subqueries in task_failure_rate use heartbeat_logs filtered only by task_id and time (e.g. SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...) but do not include tenant_id, allowing cross-tenant leakage.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column; subqueries count from a globally scoped log table rather than bypassing a tenant_id column.
- **server/process-governor.ts:239** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... is missing tenant_id filtering — counts failing agents across tenants.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column; aggregate is from a global log table, not an omitted tenant filter on a tenant-scoped table.
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 has no tenant restrictions — it can enumerate projects across tenants.
  - _Why false positive:_ Backfill is a platform-wide maintenance loop creating per-project local brain files; it is non-tenant-facing housekeeping rather than mixed output to tenants.
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping — conversations can belong to other tenants sharing the project id value.
  - _Why false positive:_ Part of the same maintenance backfill loop processing each project/conversation independently; no tenant-facing mixed response is produced here.
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant filter — message content may be read across tenants.
  - _Why false positive:_ Part of the same non-tenant-facing backfill that reads messages per conversation to build per-project artifacts; this is housekeeping, not direct cross-tenant exposure.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT id, target_file, ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id without constraining by tenant_id or validating ownership — caller-supplied proposalId can access others' proposals.
  - _Why false positive:_ Verifier is a server-side worker operating on a globally unique proposal id and not shown as taking proposalId from a request boundary in this snippet.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id filter (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 1/500). This reads tenant-scoped rows across tenants with no per-tenant constraint.
  - _Why false positive:_ Recurring-message runner is a global scheduler scan across tenants and subsequent operations use each row's tenant_id for per-row scoped conversation lookup/insert.
- **server/research-engine.ts:331** _(global-table)_ — SELECT on personas (SELECT name FROM personas WHERE id = ${program.persona_id}) is executed without constraining by tenant_id. Personas appear to be tenant-scoped elsewhere, so this can leak or resolve persona data across tenants.
  - _Why false positive:_ personas schema shows no tenant_id; lookup is on a non-tenant-scoped table
- **server/research-engine.ts:1053** _(global-table)_ — resolvePersonaId(personaSlug, _tenantId) issues a lookup (SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1) but ignores the tenantId parameter and has no tenant_id filter — cross-tenant read possible.
  - _Why false positive:_ personas has no tenant_id in schema, so resolving by name is on a global table
- **server/routes.ts:1118** _(upstream-verified)_ — SELECT from tenant-scoped table `file_storage` without a tenant_id WHERE clause in /uploads middleware. The code fetches all owners for a basename and relies on post-query JS to check ownership instead of constraining the SQL to the caller's tenant.
  - _Why false positive:_ caller tenant is established first and result rows are immediately checked in JS to require a matching owner; nonmatching rows are never returned
- **server/routes.ts:2091** _(upstream-verified)_ — SELECT from `presenter_slide_images` uses session_id derived from an earlier presenter_sessions lookup that did not validate tenant ownership. The slide-image read is not tenant-scoped (no tenant_id filter), so a crafted token or session_id could be used to read another tenant's images.
  - _Why false positive:_ access is authorized by prior lookup on unguessable presenter_sessions.token; slide_images are then scoped by that verified session_id
- **server/routes.ts:2174** _(upstream-verified)_ — repair-images route reads presenter_sessions (tenant_id present) and then reads/writes presenter_slide_images by session_id without enforcing tenant ownership in SQL. The initial presenter_sessions SELECT is not tenant-scoped nor validated against the caller, allowing cross-tenant access/modification.
  - _Why false positive:_ route is gated by x-internal-key server secret before token-based session lookup, so not a tenant request-boundary bypass
- **server/routes.ts:4333** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The raw INSERT: INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id, violating the invariant that tenant-scoped rows must be created with an explicit tenant_id.
  - _Why false positive:_ project_notes has no tenant_id per schema and is inserted only after tenant-scoped conversation lookup yields project_id
- **server/routes/admin.ts:701** _(global-table)_ — SELECT from tool_performance executed without any tenant_id constraint — reads a likely tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ admin-only diagnostic over tool_performance, which is treated here as platform-wide aggregated usage data
- **server/routes/admin.ts:706** _(global-table)_ — SELECT from tool_performance (failingRows query) lacks tenant_id filtering — can return cross-tenant data.
  - _Why false positive:_ admin-only failing-tools report reads platform-wide tool_performance aggregates rather than tenant rows
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` lacks a tenant_id constraint (UPDATE conversations SET project_id = ... WHERE id = ...)
  - _Why false positive:_ UPDATE uses newly created conv.id in same flow; project ownership already checked for tenant, so no untrusted cross-tenant target.
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` lacks a tenant_id constraint (UPDATE projects SET updated_at = ... WHERE id = ...)
  - _Why false positive:_ Just above, project existence is verified with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before this timestamp update.
- **server/routes/conversations.ts:230** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES ...). If project_notes is tenant-scoped, tenant_id must be supplied.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via validated project_id/tenant-owned conversation.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` has no tenant_id constraint in its WHERE clause (race/ownership bypass risk). The code runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` after selecting the row; a missing tenant predicate can allow cross-tenant mutation in race conditions.
  - _Why false positive:_ Same handler first selects inbox_messages row by id and tenant_id, then updates that retrieved row id.
- **server/routes/projects.ts:33** _(fk-transitive)_ — SELECT subqueries count rows from tenant-scoped tables (project_files, project_notes, project_conversations) without constraining tenant_id. These subqueries (SELECT COUNT(*) FROM project_files WHERE project_id = p.id etc.) can return cross-tenant counts because there is no tenant_id filter on those tables.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are counted only for parent projects already filtered by p.tenant_id = tenantId.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — missing tenant_id constraint. This update can touch a project record outside the caller's tenant.
  - _Why false positive:_ Earlier in same path, project row is checked with WHERE id = projectId AND tenant_id = tenantId before updating updated_at.
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — missing tenant_id constraint (duplicate in multipart upload path).
  - _Why false positive:_ Earlier in same multipart upload path, project ownership is verified with WHERE id = projectId AND tenant_id = tenantId.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC — querying tenant-scoped table project_files without constraining tenant_id.
  - _Why false positive:_ project_files has no tenant_id; access is scoped by prior verified parent project ownership and query uses that project_id.
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 — missing tenant_id filter on tenant-scoped project_files table.
  - _Why false positive:_ project_files has no tenant_id; download lookup is by validated tenant-owned project_id plus file_path under that parent.
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} — deletion against tenant-scoped table project_files but no tenant_id constraint in WHERE clause.
  - _Why false positive:_ project_files has no tenant_id; delete is scoped by prior verified tenant-owned project and matching project_id.
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}) with no tenant_id constraint. This can modify another tenant's ledger row if an id is misused.
  - _Why false positive:_ Best-effort internal helper settles a server-created reservation row by global PK; reservationId is not shown coming from request input.
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger by id only (UPDATE ... WHERE id = ${reservationId}) without tenant scoping, allowing cross-tenant ledger mutation.
  - _Why false positive:_ Internal helper releases a server-created reservation by global PK only; no untrusted cross-tenant boundary is shown for reservationId.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks runs without any tenant_id filter. heartbeat_tasks appears to be tenant-scoped elsewhere (some queries use tenant_id), so this read can cross tenant boundaries.
  - _Why false positive:_ Boot/maintenance cleanup intentionally scans platform-wide task rows for housekeeping counts and returns no tenant data.
- **server/seed-cleanup.ts:15** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks a tenant_id constraint and will disable tasks across all tenants.
  - _Why false positive:_ Platform-wide housekeeping disables runaway delegation tasks across tenants; mutates status only and is not tenant-facing.
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant_id filter, allowing cross-tenant reads.
  - _Why false positive:_ Maintenance count across all heartbeat_tasks is used only for cleanup logic, with no tenant data exposure.
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is missing tenant_id and can affect every tenant's rows.
  - _Why false positive:_ Housekeeping update disables duplicate session-logger tasks across tenants; status-only mutation in maintenance flow.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) executes without tenant scoping, enabling cross-tenant enumeration.
  - _Why false positive:_ Platform-wide maintenance query counts orphaned repo-examination tasks and does not expose mixed tenant content.
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has no tenant_id restriction and will disable matching tasks across all tenants.
  - _Why false positive:_ Maintenance cleanup disables matching orphaned tasks across tenants; only housekeeping fields are changed.
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped but this query reads across all tenants.
  - _Why false positive:_ Dev snapshot import performs global existence check during seed/sync flow and does not expose tenant data to another tenant.
- **server/seed.ts:2158** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true reads research_programs across all tenants (no tenant_id filter). research_programs is tenant-scoped.
  - _Why false positive:_ Seed maintenance iterates all active research programs to fix invalid model values; platform-wide housekeeping only.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only the id without constraining tenant_id. This can modify rows belonging to other tenants sharing the same id value.
  - _Why false positive:_ Boot-time seed maintenance loop fixes model field across rows and only mutates housekeeping data; no tenant-facing mixed output.
- **server/seed.ts:2446** _(upstream-verified)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL performs a write on a likely tenant-scoped table without constraining to tenant_id.
  - _Why false positive:_ Write is gated by isOwnerDeployment() and targets the owner/admin deployment's fixed seed row only, not request-driven tenant data.
- **server/seed.ts:2516** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id filter on conversations. conversations is tenant-scoped, so this reads across all tenants.
  - _Why false positive:_ Boot-time backfill scans conversations and inserts per-row join records into tenantless child table project_conversations; no tenant data returned or mixed.
- **server/seed.ts:4038** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} but does not include tenant_id = ... — a TOCTOU/race could update another tenant's row with the same id.
  - _Why false positive:_ The row id was selected immediately above under tenant_id = 1; update reuses that vetted row id in same call path.
- **server/storage.ts:294** _(upstream-verified)_ — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL without scoping by tenant_id. The messages table is tenant-scoped, so this can return other tenants' message data if ids are attacker-controlled.
  - _Why false positive:_ ids come from msgs already fetched by tenant-scoped getMessages/getMessagesPaginated in the same path, so this batched citation lookup is bounded to vetted rows.
- **server/storage.ts:802** _(system-sweep)_ — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} with NO tenant filter — a write to a tenant-scoped table unscoped by tenant_id.
  - _Why false positive:_ embedding updater is paired with global backfill helpers selecting rows without embeddings across tenants; this is housekeeping/batch enrichment by row id, not tenant-facing mixing.
- **server/storage.ts:814** _(system-sweep)_ — updateKnowledgeEmbedding updates agent_knowledge WHERE id = ${id} without any tenant constraint. agent_knowledge is tenant-scoped, so this can update rows across tenants.
  - _Why false positive:_ knowledge embedding update is used with global getKnowledgeWithoutEmbeddings backfill across tenants; housekeeping write by row id only, no tenant data returned or mixed.
- **server/tenant-fork.ts:225** _(upstream-verified)_ — UPDATE against a tenant-scoped table (entry.table) uses only the PK filter (eq(entry.table.id, p.newId)) and does NOT include a tenant_id constraint. The invariant requires every read/write on tenant-scoped tables to be constrained to the caller's tenant.
  - _Why false positive:_ same call path inserted cloned rows with tenantId set to newTenantId and updates the freshly returned newId inside one transaction; p.newId is server-derived.
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE on custom_tools (.where(eq(customTools.id, tool.id))) mutates a tenant-scoped table without constraining by tenant_id — a cross-tenant update risk.
  - _Why false positive:_ update targets tool.id from an already-loaded tool row in the same worker flow; tenant ownership is carried by that row rather than request input at this line.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE incrementing usageCount (where(eq(customTools.id, tool.id))) mutates a tenant-scoped table but omits a tenant_id constraint.
  - _Why false positive:_ tool is selected immediately above with name and tenant_id = tenantId, then updated by that retrieved row id in the same call path.
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations does not set tenant_id. The code inserts (project_id, conversation_id) without recording tenant ownership, relying on other updates for scoping.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table; insert occurs only after conversation update succeeds with tenant_id and project was just created with tenant_id.
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files only provides project_id, file metadata and uploaded_by, but not tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by tenant-owned projects; ownership of project_id is checked immediately above.
- **server/tools.ts:2995** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id. The code inserts (project_id, note, author) but omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by tenant-owned projects; ownership of project_id is checked immediately above.
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations (link_conversation path) does not set tenant_id. The INSERT writes (project_id, conversation_id) without tenant_id even though tenant context is available and earlier ownership was checked.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-owned project and tenant-verified conversation after explicit ownership checks.
- **server/tools.ts:4541** _(fk-transitive)_ — INSERT into project_notes omits tenant_id: INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) — tenant_id is not passed explicitly.
  - _Why false positive:_ project_notes has no tenant_id and write is guarded by assertConversationInTenant plus assertProjectInTenant before inserting child row for that project.
- **server/tools.ts:4545** _(fk-transitive)_ — INSERT into project_files omits tenant_id: INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) — tenant_id is not passed explicitly.
  - _Why false positive:_ project_files has no tenant_id and write is guarded by assertProjectInTenant before inserting child row under that tenant-owned project.
- **server/tools.ts:6392** _(fk-transitive)_ — INSERT into project_files (audio upload path) omits tenant_id: db.execute(sql`INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`).
  - _Why false positive:_ project_files has no tenant_id and insert is performed only if assertProjectInTenant(project_id, params._tenantId) passes.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table `browser_workflows` lacks a tenant constraint (no WHERE tenant_id = ...). The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` can modify another tenant's row if the id is reused or attacker-controlled.
  - _Why false positive:_ workflow row is fetched immediately above with tenant_id = tenantId, and the update targets that already tenant-scoped row id in the same path.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` writes a tenant-scoped table without explicitly setting tenant_id (columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_files has no tenant_id column and is linked via project_id to tenant-scoped projects already verified by tenant-scoped join
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE against tenant-scoped table `projects` lacks tenant constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` does not include a tenant_id condition and could update a project belonging to another tenant if projId is not strongly provenance-checked for this SQL execution.
  - _Why false positive:_ projId comes from immediately preceding SELECT joining projects with p.tenant_id = tenantId, so this update targets a tenant-verified project
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE executed against tenant-scoped table `knowledge_triples` without constraining by tenant_id (UPDATE knowledge_triples SET valid_until = ${validFrom} WHERE id = ${row.id}). This allows a caller to affect rows belonging to other tenants if they can guess an id.
  - _Why false positive:_ row.id values come from contradictionRows selected just above with tenant_id = tenantId, and only those ids are updated in the loop
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` with no tenant_id constraint (admin-path). This returns rows across all tenants.
  - _Why false positive:_ schema notes provider_keys has no tenant_id column, so this is a platform-global/admin audit query not a tenant-scoped read
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed with no tenant_id constraint (admin-path). This aggregates across all tenants without an explicit tenant scope.
  - _Why false positive:_ provider_keys is schema-global with no tenant_id column; counting configured providers is an admin/global aggregation
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} but does NOT constrain by tenant_id (tenant filter omitted). This violates the invariant that every write against tenant-scoped tables must include tenant scoping.
  - _Why false positive:_ id is taken from the immediately preceding SELECT on memory_entries constrained by tenant_id = tid and persona/category filters
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) without also constraining tenant_id. Even though ids were fetched earlier with a tenant-scoped SELECT, the UPDATE itself lacks an explicit tenant filter.
  - _Why false positive:_ ids array is built solely from rows selected just above with tenant_id = tid and persona/category filters
- **server/tools/domains/strategic-memory/handlers.ts:241** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without an accompanying tenant_id constraint. This violates the requirement to scope all writes to the caller's tenant.
  - _Why false positive:_ id comes from the prior tenant-scoped SELECT on memory_entries with tenant_id = tid and matching persona/category
- **server/video-job-runner.ts:236** _(system-sweep)_ — Global UPDATE in recoverStaleVideoJobs updates `video_jobs` rows across all tenants (no tenant_id filter) when marking stale jobs failed. It modifies tenant-scoped data without tenant scoping.
  - _Why false positive:_ Periodic stale-job recovery is a platform housekeeping sweep that only updates status/error/completed timestamps per matched row and returns aggregate/job ids to logs, not tenant-facing mixed content.
- **server/webhook-triggers.ts:122** _(global-table)_ — processTriggerEvent() selects webhook trigger rows and LEFT JOINs to `personas` (tenant-scoped) without a tenant filter, then uses the persona info to drive processing. Reading tenant-scoped persona rows without verifying tenant ownership can leak or misuse other tenants' personas during webhook-driven processing.
  - _Why false positive:_ Schema note says personas has no tenant_id, so this join is not a tenant-scoped read under the provided schema evidence.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Query against tenant-scoped table `tool_performance` without any tenant_id constraint (SELECT count(*) FROM tool_performance ...). This reads across all tenants.
  - _Why false positive:_ This is a global wiring/health invariant check computing aggregate counts for maintenance/observability, not returning tenant data to a tenant-facing surface.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — Query counts rows in `tool_performance` (SELECT count(*)::int AS n FROM tool_performance) with no tenant_id filter — reads data across tenants.
  - _Why false positive:_ Global historical row count used only for internal invariant diagnostics/aggregate findings; no per-tenant data exposure.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Query against `messages` table (SELECT count(*)::int AS n FROM messages WHERE created_at > ...) without tenant_id constraint — this reads message counts across tenants.
  - _Why false positive:_ Cross-tenant message count is used as an internal aggregate health signal in a maintenance check, not to serve mixed tenant content.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance ... executed with no tenant_id predicate — returns tool usage across all tenants.
  - _Why false positive:_ Distinct tool names over recent executions are aggregated for internal dormant-tool diagnostics, not exposed as tenant-facing mixed content.
- **server/wiring-invariants.ts:361** _(system-sweep)_ — Use of sql.raw() to query information_schema and then later selecting COUNT/MAX FROM ${spec.table} (e.g. knowledge_nudges, tool_performance) performs table-level checks without tenant_id constraints — tenant-scoped subsystem tables are probed across all tenants.
  - _Why false positive:_ information_schema existence probe is part of a platform-wide maintenance diagnostic sweep, not tenant data access.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — sql.raw(`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`) reads tenant-scoped subsystem tables (e.g. knowledge_nudges, tool_performance) without WHERE tenant_id = ... — cross-tenant read.
  - _Why false positive:_ COUNT/MAX over subsystem tables is a housekeeping freshness check emitting only aggregate metadata, not mixing tenant row content into outputs.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — sql.raw(`SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`) runs against memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) with no tenant filter — counts rows across tenants.
  - _Why false positive:_ Memory-growth tripwire reads only aggregate row counts and table size for internal observability across tenants, with no tenant-facing content exposure.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*)::int AS cnt FROM graph_memory executed with no tenant_id constraint — reads graph_memory across all tenants (message text even references "across all tenants").
  - _Why false positive:_ Global graph_memory emptiness count is an internal bootstrap/health check using only an aggregate count across tenants.
- **server/auto-memorize.ts:74** _(system-sweep)_ — INSERT into `agent_knowledge` hardcodes tenant_id = 1 (creates watermark always for tenant 1).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:60-81 setWatermark writes a single global housekeeping watermark row (category='auto_memorize_watermark') for the platform-wide runAutoMemorize sweep; it is never returned to a tenant surface (only read by getWatermark:45-58). tenant_id=1 is the sweep's fixed housekeeping bucket, not acting-tenant data
- **server/delivery-pipeline.ts:195** _(internal-worker-pk)_ — updateDeliveryLog performs UPDATE on tenant-scoped table `delivery_logs` with WHERE eq(delivery_logs.id, id) but no tenant_id constraint. This allows updating another tenant's delivery row if an attacker or caller supplies an id from a different tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateDeliveryLog is only ever called with deliveryId returned by createDeliveryLog within deliverDigitalProduct itself (server/delivery-pipeline.ts:653,755,813,836,847,896,960,729); no caller passes a request/tool-arg-derived id.
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' performed at boot without scoping by tenant_id — reads rows for all tenants into in-memory taskStateStore.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): rehydrateTaskStateOnBoot is a boot rehydration loop over running/pending agent_runs that rebuilds in-memory TaskState keyed by each row's own conversationId (server/felix-brain.ts:13-45); state is surfaced per-conversation, not cross-tenant.
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — update lacks any tenant_id constraint or ownership check. This allows modifying another tenant's session if an attacker supplies a sessionId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/proposal-verifier.ts:108** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) updates a tenant-scoped table without a tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist() runs UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} without tenant scoping — verification metadata updates could affect other tenants' proposals.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking rebase_outcome 'hallucinated') lacks tenant scoping and can modify proposals across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} (NO_LONGER_APPLICABLE branch) updates tenant-scoped rows without a tenant_id constraint, potentially affecting other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes the rebased diff without any tenant_id restriction — cross-tenant modification possible.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` uses only the row id in WHERE (WHERE id = ${r.id}) and does not include tenant_id. In a multi-tenant DB this can modify another tenant's row if ids collide or are misrouted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/routes.ts:167** _(system-sweep)_ — SELECT from tenant-scoped table `file_storage` with no tenant_id restriction (restoreUploadsFromDb). Startup routine selects all file_storage rows matching mime-type without scoping to a tenant, which can expose or restore files for every tenant to local disk.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): startup restoreUploadsFromDb rehydrates the servable uploads dir from file_storage; boot-time system context, not a request path.
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — runExperiment updates the `experiments` row using only eq(experiments.id, experiment.id) (same in the error path at ~413). The UPDATE has no tenant_id constraint, so it could modify another tenant's row if an id is supplied / misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:339 the experiment row is created via db.insert(experiments) with a server-generated serial PK earlier in the same runExperiment() call; the UPDATE at 394 targets that same experiment.id. All callers (runSelfImprovementCycle:272) create the row internally; no request/tool-arg id.
- **server/storage.ts:530** _(upstream-verified)_ — createMemoryEntry inserts into memory_entries with values(data) but does not validate that data.tenantId is present and owned by caller. The schema requires tenant_id NOT NULL with no default — an insert without an explicit validated tenantId can create cross-tenant rows or rely on unsafe defaults.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/storage.ts:1296** _(upstream-verified)_ — getAllDataForExport performs SELECTs from many tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with NO tenant scoping — it returns platform-wide data and will leak all tenants' data if called by a non-admin.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tools.ts:4563** _(internal-worker-pk)_ — Raw SELECT against presenter_sessions uses only presentation_id (SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...) with no tenant_id constraint or ownership check. This can leak tokens/sensitive session data across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:4537 — The presenter_sessions SELECT keys on slideResult.presentationId, which was just created by this same create_slides tool invocation for the caller's own tenant (conversation ownership is asserted upstream at ~4500 via 'conversation not owned by tenant' and assertProjectInTenant). The presentation id is not attacker-supplied cross-tenant input; it is a self-generated art
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE agentActivity in completeActivity is scoped only by id (WHERE id = activityId) and lacks a tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` with no tenant_id constraint (getWatermark).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-memorize.ts:68** _(internal-worker-pk)_ — UPDATE on `agent_knowledge` uses id only and does not verify tenant ownership (no tenant_id condition).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): singleton watermark row; id from its own category+title SELECT one statement above (system row, tenant 1 by design).
- **server/build-video-from-brief.ts:714** _(upstream-verified)_ — projectId from caller input is passed into the startVideoJob/startInput without verifying the project belongs to the caller's tenant. This allows a caller to reference another tenant's project_id and have it used in a job (ownership not validated).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): tenantId is explicitly threaded into StartVideoJobInput on the adjacent line; downstream project_files writes are gated by assertProjectInTenant.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE against tenant-scoped table `credential_vault` updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). Even though the code previously fetched the credential, the subsequent UPDATE is not guarded by tenant_id, permitting potential cross-tenant mutation in race scenarios.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getDecryptedPassword calls getCredential(id, tenantId) at credential-vault.ts:167, which SELECTs WHERE id = id AND tenant_id = tenantId (credential-vault.ts:75-82) and returns null (function bails) if not owned. The last_used_at UPDATE at 169 runs only after ownership is proven.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE against tenant-scoped table `credential_vault` updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}). The callsite earlier validated the credential, but the unconditional UPDATE is missing an explicit tenant_id guard.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getDecryptedPassword calls getCredential(id, tenantId) at credential-vault.ts:167, which SELECTs WHERE id = id AND tenant_id = tenantId (credential-vault.ts:75-82) and returns null (function bails) if not owned. The last_used_at UPDATE at 169 runs only after ownership is proven.
- **server/delivery-pipeline.ts:1050** _(global-table)_ — getDeliveryByStripePayment SELECTs from tenant-scoped `delivery_logs` using only stripePaymentId (eq(delivery_logs.stripe_payment_id, paymentId)) with NO tenant filter. This can leak or return another tenant's delivery row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' by project id is performed without scoping to the caller's tenant (WHERE eq(projects.id, projectId) only). This reads another tenant's project row before ownership is enforced in code.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): ensureProjectFolder selects only tenantId/driveFolderId and immediately throws on project.tenantId!==tenantId at google-drive.ts:551-553 before returning or writing anything.
- **server/internal-resolver.ts:184** _(internal-worker-pk)_ — finalizeEvent is called without a tenant_id constraint when the event row has no tenant_id. This issues an unscoped UPDATE against tenant-scoped event_log (UPDATE event_log ... WHERE id = ${eventId} ... with no tenant filter), potentially mutating another tenant's event_row by id alone.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): fail-closed branch: event id came from the resolver's own dropped-event sweep; archives the event without touching tenant data.
- **server/job-queue.ts:274** _(internal-worker-pk)_ — cancelJob issues UPDATE agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint, allowing cancellation of jobs belonging to other tenants by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:283** _(upstream-verified)_ — retryJob does an UPDATE resetting job state WHERE id = ${id} AND status IN (...) without any tenant_id filter. This allows resetting jobs from other tenants by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:283 — retryJob is only reachable via POST /api/admin/agent-jobs/:id/retry (server/routes/agent-jobs.ts:62-70), gated by requirePlatformAdmin. Same admin-only operator inbox as cancelJob — cross-tenant retry by id is deliberate platform-admin capability, not attacker-influenced.
- **server/job-queue.ts:320** _(system-sweep)_ — listJobs builds and runs a SELECT from agent_jobs but if no tenantId is provided it performs an unscoped SELECT across all tenant rows (SELECT ... FROM agent_jobs ${whereClause}). That returns tenant-scoped rows to callers without tenant filtering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/lib/agent-trace.ts:124** _(internal-worker-pk)_ — persistSpanClose() issues UPDATE agent_trace_spans ... WHERE span_id = ${spanId} with no tenant_id predicate. agent_trace_spans is tenant-scoped; the UPDATE can modify a span belonging to another tenant if span_id is known or leaked.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/bwb-job-progress.ts:74** _(internal-worker-pk)_ — scope(jobId, ...) builds WHERE clauses that may omit the tenant predicate when env-threaded tenant (BWB_TENANT_ID) is missing; callers use .where(scope(...)) for updates to tenant-scoped table `video_jobs`. If env tenant is absent the code warns once but proceeds and performs updates without tenant_id filtering — allowing cross-tenant updates (writes) pinned only to a jobId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): scope() always pins the unguessable server-generated jobId (vj_<base36>_<6-byte-hex>, newBwbJobId at bwb-job-progress.ts:44-46, validated by JOB_ID_RE at :28,50); jobId comes only from BWB_JOB_ID env/explicit args threaded through the spawn chain, never a request. Tenant predicate is defense-in-depth, and a random-jobId collision is not reachable from an untrusted boundary.
- **server/memory-graph.ts:141** _(upstream-verified)_ — refreshCategoryCount runs an UPDATE on memory_categories with a subquery that counts memory_entries but neither the subquery nor the outer UPDATE includes a tenant_id filter. This can produce incorrect counts and update other tenants' rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/process-governor.ts:155** _(global-table)_ — SELECT subquery in desk_status computes pending_tasks via: SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true — this subquery lacks a tenant_id constraint and can leak cross-tenant counts.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): heartbeat-task pace counting is a platform-wide throttle by design (same family as checkPace/heartbeat_logs); per-tenant fairness would need a schema migration and is tracked separately.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a tenant-scoped projects row without any tenant_id constraint or ownership verification — caller-supplied projectId may reference another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/routes/admin.ts:719** _(system-sweep)_ — SELECT from ai_insights (stuckInsights query) has no tenant_id constraint — reads tenant-scoped insights across all tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): admin-gated self-heal status endpoint; platform-wide stuck-insight scan is its purpose.
- **server/routes/admin.ts:731** _(upstream-verified)_ — SELECT from heartbeat_tasks (disabledTasks query) lacks tenant_id filtering — may return heartbeat tasks from all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/routes/admin.ts:562 admin-only guard; heartbeat_tasks read is platform-admin diagnostics
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE sculptor_sessions executed without constraining tenant_id (UPDATE ... WHERE id = ${session.id}). This allows modifying a session row belonging to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:74-82 session.id is server-generated PK from the tenant-scoped insert in this same call
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id without constraining tenant_id (UPDATE ... WHERE id = ${session.id}). This can update another tenant's session.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:74-82 session.id is server-generated PK from the tenant-scoped insert in this same call
- **server/sculptor.ts:174** _(internal-worker-pk)_ — monitorSession updates sculptor_sessions (UPDATE ... WHERE id = ${sessionId}) without restricting tenant_id. monitorSession is passed tenantId but the UPDATE lacks it, enabling cross-tenant writes.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions sets review_result without constraining tenant_id (UPDATE ... WHERE id = ${sessionId}). reviewSessionWork accepts tenantId but the UPDATE omits it, allowing cross-tenant modification.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/self-improvement.ts:170** _(upstream-verified)_ — detectStagnation builds and executes a SELECT on `experiments` that can run without a tenant_id constraint when tenantId is omitted. This allows cross-tenant reads of a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — getExperimentHistory will perform db.select().from(experiments) with no tenant_id constraint when no tenantId is passed, returning experiment rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:239** _(upstream-verified)_ — getConversationUnscoped performs a SELECT FROM conversations WITHOUT any tenant_id constraint (returns conversation row by id unscoped).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:395** _(internal-worker-pk)_ — attachCitations runs raw SQL UPDATE messages SET citations = ... WHERE id = ${messageId} without constraining by tenant_id — a write to a tenant-scoped table with no tenant filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:306** _(upstream-verified)_ — createMessage reads conversations row by conversationId with no tenant filter (SELECT ... FROM conversations WHERE id = ${data.conversationId}) while conversationId is caller-supplied. This derives tenant from the row but does not verify the caller's tenant owns the conversation first — potential cross-tenant object-confusion/leak.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): createMessage lookup only reads conversations.tenantId (int) and the message is stamped with the conversation's real tenant (storage.ts:325); a caller-supplied tenantId that mismatches throws (storage.ts:320-323). No cross-tenant data returned or wrong-tenant write.
- **server/storage.ts:512** _(system-sweep)_ — getMemoryEntry can perform an unscoped SELECT from memory_entries when tenantId is omitted (function warns but still issues the SELECT). memory_entries is tenant-scoped, so omitting tenantId allows cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/storage.ts:874** _(upstream-verified)_ — getMemoryStats performs SELECTs over memory_entries and agent_knowledge and will return all rows when tenantId is undefined. This can leak counts and content across tenants if callers omit tenantId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:878 — getMemoryStats scopes with eq(memoryEntries.tenantId, tenantId) and eq(agentKnowledge.tenantId, tenantId) whenever tenantId is supplied (lines 877/889). All tenant-facing callers pass tenantId (routes/memory.ts:174, routes/briefings.ts:164). The unscoped fallback is only hit by trusted system/admin callers (system-probes.ts:159 health probe, backup.ts:119 platform back
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — stampArgsEmbedding executes UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} without scoping by tenant_id — a write to a tenant-scoped table with no tenant filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — scoreProposalSurprise runs UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId} without constraining by tenant_id even though the function takes tenantId — later queries used tenantId but this UPDATE does not.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — scoreProposalSurprise executes UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} WITHOUT tenant scoping — this writes scoring/band to a tenant-scoped row without ensuring tenant ownership in the WHERE clause.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from custom_tools (.where(eq(customTools.id, tool.id))) removes a tenant-scoped row without a tenant_id constraint, violating tenant isolation guarantees.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into project_files after slideshow/video production omits tenant_id: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) — tenant_id is not passed explicitly.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column (isolated via project_id FK); server/tools.ts:2969 owner check SELECT id FROM projects WHERE id=params.id AND tenant_id=tenantId gates the insert
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT SQL omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column (isolated via project_id FK); server/tools.ts:2969 owner check SELECT id FROM projects WHERE id=params.id AND tenant_id=tenantId gates the insert
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` without any tenant_id constraint (query loads all pairing rows for the phone across tenants).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} is executed without constraining by tenant_id. This reads a tenant-scoped table by id only.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped table by id only, without tenant_id filtering. A caller could potentially affect another tenant's ledger row by supplying an id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table by id only, without tenant_id filtering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/video-job-runner.ts:206** _(internal-worker-pk)_ — SELECT cancelRequested FROM `video_jobs` WHERE jobId = ${jobId} is executed without tenant_id constraint (isCancelRequested). This reads a tenant-scoped table by jobId only.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): isCancelRequested reads by the unguessable random jobId (vj_<base36>_<hex>) generated by the runner itself.
- **server/lib/scheduled-post-runner.ts:320** _(deferred)_ — Postgres array literal (`arrLit`) is constructed by string interpolation from caller-supplied `platforms` and then injected into the SQL (`${arrLit}::text[]`). This is effectively building SQL from request-derived data and risks SQL injection and isolation issues.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): hand-built text[] literal from allowlist-checked platforms — injection-resistant today; refactor to parameterized bind (deferred item 5, campaign 2026-07-31).
- **server/data-protection.ts:19** _(deferred)_ — CREATE TABLE memory_categories defines tenant_id INTEGER DEFAULT 1. This introduces a default tenant and allows INSERTs to omit tenant_id, violating the invariant that tenant_id must be supplied explicitly on tenant-scoped rows.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): legacy CREATE TABLE memory_categories tenant_id DEFAULT 1 — schema-level decision needed (deferred item 1, campaign 2026-07-31; HARD tier, owner sign-off).
- **server/storage.ts:548** _(deferred)_ — deleteMemoryEntry updates memory_entries (set status='superseded') using a WHERE that may omit tenant scoping when tenantId is not provided. This lets callers mark rows in other tenants.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): deleteMemoryEntry scopes tenant only when tenantId provided (warns via _warnUnscoped otherwise) — optional-tenant fail-open; hardening = make tenantId required.
- **server/storage.ts:555** _(deferred)_ — touchMemoryEntries updates memory_entries for a list of ids and only conditionally adds tenant scoping; when tenantId is undefined the update will act across tenants for those ids.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): touchMemoryEntries scopes tenant only when tenantId provided (warns via _warnUnscoped otherwise) — optional-tenant fail-open; hardening = make tenantId required.
