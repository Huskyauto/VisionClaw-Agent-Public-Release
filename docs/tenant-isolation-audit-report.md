# Tenant-Isolation Audit Report

_Generated 2026-07-22T09:27:05.209Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **548/548** file(s) audited · chunks 52/52 OK (0 failed)
- Findings: **300** (CRITICAL 107 · HIGH 155 · MEDIUM 38 · LOW 0)
- Precision second-pass (`gpt-5.4`): **152** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (107)

- **server/agentic/runs.ts:34** — appendStep() performs an UPDATE on agent_runs without constraining by tenant_id (where clause is eq(agentRuns.id, runId) only). This allows cross-tenant modification of run rows.
  - _Fix:_ Include the tenant check in the WHERE clause (and/or accept tenantId as an explicit parameter). e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Update all callers to pass/verify tenantId before invoking appendStep.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) only — no tenant_id constraint. This can overwrite another tenant's run state.
  - _Fix:_ Require and pass tenantId into updateRunState and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers supply/verify the tenant context before calling.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) only — missing tenant_id constraint.
  - _Fix:_ Add tenant scoping: accept tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Change all callsites accordingly.
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs (status/result/completedAt) using WHERE eq(agentRuns.id, runId) only — no tenant_id constraint.
  - _Fix:_ Scope the update to the tenant: add tenantId parameter and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Propagate tenant checks to callers.
- **server/agentic/runs.ts:67** — failRun() updates agent_runs with WHERE eq(agentRuns.id, runId) only — missing tenant_id check allowing cross-tenant status changes.
  - _Fix:_ Require tenantId and add it to the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Update callsites to pass tenantId.
- **server/agentic/runs.ts:76** — pauseRun() updates agent_runs with WHERE eq(agentRuns.id, runId) only — no tenant_id constraint present.
  - _Fix:_ Add tenant scoping: accept tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Update all callers to supply tenantId.
- **server/ceo-orchestrator.ts:821** — Unscoped SELECT on tenant-scoped 'personas' table — cachedPersonas is loaded with db.select().from(personasTable) with no tenant_id constraint, so personas from all tenants will be returned and may be used to route or assign work across tenants.
  - _Fix:_ Restrict the query to the caller's tenant: pass the tenantId in and filter (e.g. db.select().from(personasTable).where(eq(personasTable.tenant_id, tenantId))). If you need a global cache, maintain a per-tenant cache keyed by tenantId; never load all tenants' personas into a single process-wide cache.
- **server/chat-engine.ts:632** — SELECT from `compaction_archives` uses only conversation_id = ${c.id} with no tenant_id constraint. Compaction archives can contain compacted conversation history; returning these without tenant scoping risks cross-tenant data exposure.
  - _Fix:_ Include tenant scoping on compaction_archives queries. If compaction_archives has tenant_id, add AND tenant_id = ${tenantId}. If it doesn't, change the schema or join via the conversations table to ensure the referenced conversation belongs to the tenant (e.g. JOIN conversations conv ON conv.id = compaction_archives.conversation_id AND conv.tenant_id = ${tenantId}). Always enforce tenant ownership server-side before returning archived content.
- **server/chat-engine.ts:650** — SELECT from `messages` uses only conversation_id = ${c.id} with no tenant_id constraint. Messages include sensitive chat content; fetching them without an explicit tenant filter can leak other tenants' messages if conversation IDs are mis-bound.
  - _Fix:_ Constrain message reads by tenant. Add AND tenant_id = ${tenantId} to the WHERE clause, or join to conversations ensuring the conversation row is tenant-owned (e.g. SELECT ... FROM messages m JOIN conversations c ON m.conversation_id = c.id WHERE m.conversation_id = ${c.id} AND c.tenant_id = ${tenantId}). Prefer Drizzle .where(eq(messages.tenantId, tenantId)) where possible.
- **server/chat-engine.ts:4583** — INSERT into memory/entries performed without passing tenant context. storage.createMemoryEntry({...}) is invoked with fact, category, personaId but no tenantId, so the new row may be created without the required tenant_id constraint (or rely on storage internals that are not shown).
  - _Fix:_ Include the caller's tenant id when creating memory entries (e.g. storage.createMemoryEntry({..., tenantId})) or change the storage API to require tenantId explicitly. Also validate that any provided personaId belongs to that tenant before inserting.
- **server/chat-engine.ts:4591** — UPDATE to a memory embedding performed without tenant scoping. storage.updateMemoryEmbedding(entry.id, emb) updates by id only and does not pass/validate tenant ownership, allowing cross-tenant modification if storage does not enforce tenant_id checks internally.
  - _Fix:_ Require and pass tenantId to updateMemoryEmbedding (or have the implementation constrain updates by tenant_id). Alternatively, include the tenant_id in the update call and/or re-query the entry with tenant filtering before updating to verify ownership.
- **server/commitment-drafter.ts:233** — UPDATE on tenant-scoped table 'commitments' performed without constraining tenant_id (unclaim path uses: UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can mutate rows belonging to other tenants.
  - _Fix:_ Include the tenant guard in the WHERE clause (or use the already-validated tenantId). E.g. UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}; ensure the tenantId used is the one asserted for the row. Prefer using RETURNING and re-checking tenant ownership inside the transaction before side-effects.
- **server/data-protection.ts:125** — permanentlyPurgeSoftDeleted deletes rows from messages, compaction_archives and conversations using only conversation_id (DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}) with no tenant_id constraint. Because the initial SELECT was run across all tenants, these DELETEs can affect multiple tenants' data.
  - _Fix:_ Scope each DELETE by tenant_id as well (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}). Better: perform the whole purge operation inside a transaction and/or require an explicit admin context; always include tenant_id predicates when deleting from tenant-scoped tables to avoid accidental cross-tenant data removal.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Do not scan tenant-scoped tables globally. Either restrict the query to a specific tenant (pass tenantId) or run the backfill per-tenant (or explicitly bind ADMIN_TENANT_ID if the intention is to operate on an admin-owned global partition). Parameterize tenantId via Drizzle (WHERE tenant_id = ${tenantId}).
- **server/embeddings.ts:716** — SELECT FROM memory_entries in backfillEmbeddingVecs lacks a tenant_id constraint — reads rows for all tenants.
  - _Fix:_ Restrict the backfill SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. Avoid global, unscoped reads of tenant-scoped tables.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge in backfillEmbeddingVecs has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Restrict to a tenant (WHERE tenant_id = ${tenantId}) or run backfill per-tenant. Never run unscoped reads against tenant-scoped tables.
- **server/felix-brain.ts:19** — SELECT FROM agent_runs in rehydrateTaskStateOnBoot omits tenant_id in WHERE clause and scans running/pending runs across all tenants. This reads tenant-scoped rows without scoping and can expose or mix cross-tenant data into in-memory task state.
  - _Fix:_ Scope the rehydration to a tenant (pass tenantId or run per-tenant), or explicitly bind to an admin tenant and document it. At minimum add WHERE tenant_id = ${tenantId} (or otherwise ensure the operation runs under an approved admin/maintenance context) and avoid unscoped reads of tenant-scoped tables.
- **server/felix-loop.ts:684** — UPDATE on tenant-scoped table 'felix_loop_runs' omits tenant_id in WHERE (UPDATE ... WHERE id = ${runId}). This allows a runId to be used to update a row belonging to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant as well as id. e.g. WHERE id = ${runId} AND tenant_id = ${tenantId} (or the Drizzle equivalent) so the write is limited to the caller's tenant.
- **server/felix-loop.ts:690** — UPDATE on tenant-scoped table 'felix_loop_runs' in the error recovery path omits tenant_id in WHERE (UPDATE ... WHERE id = ${runId}).
  - _Fix:_ Include the tenant_id constraint on the UPDATE/WHERE (WHERE id = ${runId} AND tenant_id = ${tenantId}) to ensure only the caller's tenant row is modified.
- **server/firecrawl.ts:190** — UPDATE of scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) but omits tenant scoping (tenantId). scrapedPages is tenant-scoped and the update is not constrained by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE: include eq(scrapedPages.tenantId, tenantId) in the WHERE (or use and(eq(scrapedPages.id, id), eq(scrapedPages.tenantId, tenantId))). Ensure all UPDATEs/DELETES on tenant-scoped tables include tenant_id.
- **server/firecrawl.ts:316** — UPDATE of scrapedPages inside crawl loop uses WHERE eq(scrapedPages.id, existing[0].id) but omits tenant scoping. This is a write to a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Constrain the update by tenant: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include tenant_id in WHERE for tenant-scoped tables.
- **server/google-drive.ts:560** — UPDATE on projects (setting driveFolderId/driveFolderUrl) uses WHERE eq(projects.id, projectId) without tenant scoping. projects is tenant-scoped and the UPDATE can affect another tenant's row.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId}) to ensure the write is restricted to the caller's tenant.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images omits tenant context / tenant_id column. The INSERT uses only session_id and can write images for any session id, permitting cross-tenant writes if session ids are misused.
  - _Fix:_ Include tenant_id explicitly in the INSERT into presenter_slide_images (and ensure the value comes from the trusted execution context), or first verify ownership by selecting presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId} and fail if no row. Never trust sessionId from an external source without an ownership check, and always set tenant_id on tenant-scoped tables.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} performs an unscoped update on a tenant-scoped table (no WHERE tenant_id = ...). This can overwrite another tenant's session data if sessionId is controlled or guessed.
  - _Fix:_ Verify the session belongs to the tenant before updating (SELECT id FROM presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or include tenant_id in the WHERE clause (WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure all UPDATE/DELETE paths on tenant-scoped tables enforce WHERE tenant_id = <caller tenant>.
- **server/heartbeat.ts:52** — SELECT from research_schedules has no tenant_id filter (reads rows for all tenants). research_schedules is tenant-scoped (rows are later used via sched.tenant_id).
  - _Fix:_ Limit the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate tenants explicitly after resolving an asserted admin/system tenant context. Prefer using a storage helper that enforces tenant scoping or call assertTenantContext and pass the tenant into the query.
- **server/heartbeat.ts:102** — UPDATE research_schedules ... WHERE id = ${sched.id} executes without tenant_id constraint. This updates a tenant-scoped table by id only and can modify other tenants' rows.
  - _Fix:_ Add an explicit tenant guard on the UPDATE, e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id} (or the asserted tenant variable). Alternatively, use a storage/update helper that enforces tenant scoping.
- **server/heartbeat.ts:106** — UPDATE research_schedules ... WHERE id = ${sched.id} executes without tenant_id constraint (last_run_at,next_run_at update).
  - _Fix:_ Include AND tenant_id = ${sched.tenant_id} (or the asserted tenant) in the WHERE clause, or use a tenant-aware storage function to perform the update so writes cannot affect other tenants.
- **server/heartbeat.ts:112** — UPDATE research_schedules ... WHERE id = ${sched.id} executed without tenant_id constraint (rescheduling path).
  - _Fix:_ Restrict the UPDATE to the owning tenant: add AND tenant_id = ${sched.tenant_id} or call a storage API that enforces tenant scoping.
- **server/heartbeat.ts:120** — UPDATE research_schedules ... WHERE id = ${sched.id} in the error-recovery block lacks tenant scoping. Writing to a tenant-scoped table by id only can cross-tenant-affect rows.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use the storage layer that validates tenant ownership before persisting changes.
- **server/heartbeat.ts:257** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — this mutates heartbeat_tasks without any tenant_id guard. heartbeat_tasks are tenant-scoped (tenantId used elsewhere).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call storage.updateHeartbeatTask which should enforce tenant ownership; do not perform direct DB updates without tenant constraints.
- **server/heartbeat.ts:261** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} (normalization branch) executes without tenant_id filter.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or delegate to a tenant-aware storage helper to avoid modifying another tenant's task row.
- **server/heartbeat.ts:632** — SELECT * FROM event_log WHERE status = 'pending' ... issues a multi-tenant read from a tenant-scoped table (event_log has tenant_id) with no tenant filter. This fetches pending events for all tenants in one query.
  - _Fix:_ Either query per-tenant (resolve the tenant context for the worker) or add tenant_id filtering if the worker is intended to process only a single tenant. If the worker must process across tenants, make the multi-tenant intent explicit and ensure the code documents/authorizes this, but prefer processing with an asserted admin tenant and explicit tenant-aware read APIs.
- **server/heartbeat.ts:2837** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id filtering (processDelegations path). This can change approval_status on another tenant's task if ids collide or are guessed.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenant}) or use a tenant-aware storage helper to set approval_status while validating ownership.
- **server/heartbeat.ts:3289** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id filtering (delegateTaskFromChat path).
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform this via the storage layer which validates tenant ownership and prevents cross-tenant mutations.
- **server/internal-resolver.ts:93** — finalizeEvent performs UPDATE on event_log without constraining by tenant_id. This lets the resolver finalize (mutate) event_log rows across tenants (race-prone and breaks tenant isolation).
  - _Fix:_ Require and pass the event's tenant_id into finalizeEvent (or first SELECT the event's tenant_id) and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. Always parameterize the tenant id rather than relying on global context.
- **server/internal-resolver.ts:107** — SELECT FROM plans by id has no tenant_id constraint. The resolver reads and then acts on a plan row without verifying it belongs to the caller/tenant, enabling cross-tenant access and mutation.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}), or first fetch and verify the plan's tenant_id matches the expected tenant before taking action. All code paths that mutate the plan must enforce the same check.
- **server/internal-resolver.ts:170** — UPDATE code_proposals transitions rows to archived_verification_failed without restricting by tenant_id. This can archive proposals belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending'), and require the resolver to know/verify the tenant context for the proposal before mutating it.
- **server/lib/action-ledger.ts:304** — UPDATE on tenant-scoped table `action_attempts` (sweepStaleAttempts) runs without any tenant_id constraint — this will transition rows across all tenants to 'unknown'.
  - _Fix:_ Avoid blind cross-tenant UPDATEs. Either (a) include tenant_id in the WHERE clause, or (b) enumerate affected ids per-tenant and call a tenant-scoped transition function per-row, or restrict this function to a privileged reconciler that performs per-tenant writes after fetching each row's tenant_id (and then uses a tenant-scoped update). Add explicit access control and an audit trail for cross-tenant maintenance jobs.
- **server/lib/inbox-ingest.ts:444** — SELECT from inbox_messages / left-join inbox_classifications lacks tenant_id constraint (WHERE m.message_id = ${id} AND c.id IS NULL). This reads a tenant-scoped table without scoping to the caller tenant.
  - _Fix:_ Add an explicit tenant filter (e.g. AND m.tenant_id = ${tenantId}) to the WHERE clause so the query cannot return rows belonging to other tenants.
- **server/lib/inbox-ingest.ts:505** — UPDATE on inbox_classifications omits tenant_id in WHERE (UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}). This can update rows across tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise verify ownership before updating. Always constrain UPDATEs to the current tenant.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} — UPDATE on a tenant-scoped table without any tenant_id constraint. A row id alone can collide across tenants and allow cross-tenant modification.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or run updates in a per-tenant transaction after validating the row belongs to the targeted tenant.
- **server/lib/scheduled-post-runner.ts:191** — Fallback UPDATE in the catch block updates scheduled_posts WHERE id = ${row.id} AND status = 'publishing' but does not filter by tenant_id — a cross-tenant write risk.
  - _Fix:_ Add tenant_id to the WHERE predicate (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} AND status = 'publishing') so only the owning tenant's row can be modified.
- **server/minerva-planner.ts:355** — decidePlan runs an UPDATE on plans without scoping by tenant_id: WHERE id = ${args.planId} AND status = 'awaiting_approval'. This allows altering a plan belonging to another tenant by id (cross-tenant modification).
  - _Fix:_ Constrain the UPDATE to the caller's tenant: include AND tenant_id = ${callerTenantId}. Ensure decidePlan receives and validates the tenantId of the actor (or derives it from context) before performing the UPDATE.
- **server/minerva-planner.ts:373** — Probe SELECT used to detect concurrent decision retries queries plans by id only: SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}. No tenant_id filter, allowing information disclosure or logic mistakes across tenants.
  - _Fix:_ Include tenant scoping in the probe: WHERE id = ${args.planId} AND tenant_id = ${callerTenantId}. Ensure all reads that are not explicitly trusted internal contexts are tenant-constrained.
- **server/minerva-planner.ts:390** — When handling 'revise' idempotency, code queries child plans: SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 — lacks tenant constraint and can return cross-tenant child plans.
  - _Fix:_ Add tenant filtering: WHERE parent_plan_id = ${args.planId} AND tenant_id = ${callerTenantId} ORDER BY id DESC LIMIT 1. Always pass and enforce tenantId in these control flows.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions executed without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads all tenants' subscription rows and can expose sensitive tokens/status across tenants.
  - _Fix:_ Do not run unrestricted SELECTs over a tenant-scoped table. Either (a) scope the query to a specific tenant via WHERE tenant_id = ${tenantId}, or (b) if an admin-wide sweep is required, enumerate an explicit, validated list of tenant IDs and perform parameterized per-tenant queries (or use a dedicated admin-only code path with strong access controls). Avoid returning raw token fields for other tenants; process each tenant row in-situ with tenant-scoped parameter binding.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table oauth_subscriptions executed without tenant filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This reads data across all tenants.
  - _Fix:_ Add explicit tenant scoping, or enumerate/validate tenant IDs before reading. If the startup logic truly needs a global scan, ensure this runs in a controlled admin context and iterate per-tenant using parameterized queries rather than a single unbounded SELECT; avoid leaking or operating on other tenants' rows without explicit per-tenant handling.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh() queries oauth_subscriptions with WHERE is_active = TRUE but no tenant_id filter — it scans active subscriptions across all tenants (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE).
  - _Fix:_ If you must refresh tokens across tenants, explicitly iterate a validated list of tenant IDs and run tenant-scoped queries per tenant (e.g. WHERE tenant_id = ${tenantId}). Avoid blanket SELECTs that return rows from all tenants; ensure any background task uses parameterized per-tenant operations and minimal returned sensitive fields.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions() performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without restricting by tenant_id — it returns rows for all tenants.
  - _Fix:_ Restrict to an explicit tenant list or perform per-tenant parameterized queries. If an admin/global sweep is required, separate that code path and protect it with additional admin-only checks and iterate per-tenant rather than selecting an unbounded cross-tenant result set.
- **server/plan-executor.ts:116** — appendExecutionLog updates the tenant-scoped plans table using only WHERE id = ${planId} (no tenant_id constraint). This can modify other tenants' plan rows if planId is guessable or attacker-controlled.
  - _Fix:_ Change appendExecutionLog to accept/require the plan's tenantId (or the caller's tenant context) and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise scope the update by tenant). Use a CAS/RETURNING pattern that verifies the tenant before writing.
- **server/plan-executor.ts:513** — Claiming a plan uses UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status = 'approved' RETURNING ... — there is no tenant_id constraint. This allows claiming/executing plans across tenants.
  - _Fix:_ Include tenant scoping in the claim: WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved'. Require the caller/auth context supply expectedTenantId (or otherwise derive it safely) so the DB enforces tenant ownership at claim time.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} is executed without a tenant_id constraint, allowing cross-tenant writes to the plans table.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${planId} AND tenant_id = ${tenantId}. Ensure the code has the correct tenantId in scope (pass it through the call or read it from the claimed row) before performing the update.
- **server/plan-executor.ts:725** — Final status update (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint, permitting cross-tenant modifications of plan status.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause. Propagate/require the plan's tenantId throughout executePlan and use it when updating the DB to enforce tenant isolation.
- **server/plan-executor.ts:759** — Error-handling update (UPDATE plans SET status = 'failed' ... WHERE id = ${planId} AND status = 'executing') does not include tenant_id in the WHERE clause, enabling an attacker to flip other tenants' plan states.
  - _Fix:_ Restrict this update by tenant: WHERE id = ${planId} AND tenant_id = ${tenantId} AND status = 'executing'. Obtain tenantId from the claimed/returned plan row or require it from the caller context.
- **server/process-governor.ts:119** — Subquery reads heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This allows cross-tenant data leakage because heartbeat_logs is tenant-scoped but not constrained.
  - _Fix:_ Include tenant isolation in the subquery (e.g. AND tenant_id = ${tenantId}) or join via tenant-scoped parent row. Always add AND tenant_id = ${tenantId} to any reads against tenant-scoped tables.
- **server/process-governor.ts:198** — Query on heartbeat_logs (SELECT AVG(duration_ms) ...) lacks a tenant_id filter. Reading tenant-scoped heartbeat logs without tenant constraint can leak cross-tenant metrics.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent .where(eq(...))) to the WHERE clause so only the caller's tenant data is aggregated.
- **server/process-governor.ts:239** — Query counting distinct persona_id from heartbeat_logs (cascading_failures) has no tenant_id constraint. This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Constrain the query with tenant_id = ${tenantId} (or use a tenant-scoped join) to ensure results are limited to the caller's tenant.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} executes without a tenant_id condition. This can modify rows belonging to other tenants if IDs collide or are tampered with.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE (or use a parameterized .where(eq(...))) to ensure only tasks for the caller's tenant are modified.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} has no tenant_id restriction. Although desk.id was selected earlier for this tenant, the UPDATE lacks an explicit tenant filter and can affect rows beyond the tenant boundary.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause to enforce tenant isolation on writes.
- **server/process-governor.ts:977** — UPDATE governance_rules SET ... WHERE id = ${rule.id} is executed without scoping by tenant_id. This increments trigger_count / last_triggered_at across tenant boundaries if ids collide.
  - _Fix:_ Restrict the UPDATE with WHERE id = ${rule.id} AND tenant_id = ${tenantId} (or equivalent) to guarantee the write applies only to the caller's tenant.
- **server/process-governor.ts:1183** — Query joins governance_actions (scoped to tenant via WHERE ga.tenant_id = ${tenantId}) to governance_rules using only gr.id = ga.rule_id but does not constrain governance_rules by tenant_id. This can expose or pull governance_rules rows from other tenants if ids are not globally unique.
  - _Fix:_ Constrain the join to the same tenant (e.g. JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ga.tenant_id) or add WHERE gr.tenant_id = ${tenantId} to ensure governance_rules columns are tenant-scoped.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} lacks a tenant_id filter. Reading project data by id without verifying tenant ownership risks returning another tenant's project.
  - _Fix:_ Require tenant context and add AND tenant_id = ${tenantId} (or use a tenant-scoped lookup) before reading project rows. Validate that the caller owns the projectId prior to loading any project data.
- **server/project-brain.ts:332** — Backfill SELECT DISTINCT FROM projects JOIN conversations does not filter by tenant_id. This can enumerate projects/conversations across tenants.
  - _Fix:_ Scope the query to a tenant (e.g. FROM projects p WHERE p.tenant_id = ${tenantId} ...) or accept/require a tenantId parameter and add appropriate WHERE clauses to every tenant-scoped table in the query.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} does not include tenant_id. Reading conversations by project id without confirming tenant ownership can leak other tenants' conversation data.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or JOIN via a tenant-scoped project row) and validate the project belongs to the tenant before querying conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks a tenant_id constraint. This reads tenant-scoped messages without scoping to the caller's tenant.
  - _Fix:_ Ensure conversation_id is validated as belonging to the tenant and include AND tenant_id = ${tenantId} (or equivalent) when selecting messages, or otherwise restrict via a tenant-scoped join.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} is missing a tenant_id constraint. Reading a proposal by id without verifying ownership can return another tenant's proposal.
  - _Fix:_ Require tenant context and add WHERE id = ${proposalId} AND tenant_id = ${tenantId}, or first validate that the proposalId belongs to the caller's tenant before selecting.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) executes without tenant scoping. This can modify proposals belonging to other tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE (or otherwise verify proposal ownership) before persisting any changes.
- **server/proposal-verifier.ts:246** — persist() performs UPDATE code_proposals SET verification_status... WHERE id = ${proposalId} without scoping by tenant_id. This writes tenant-scoped state without enforcing tenant isolation.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (and require the caller's tenantId be passed/verified). Ensure every UPDATE/INSERT/DELETE against code_proposals sets or checks tenant_id explicitly.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} (marking NO_LONGER_APPLICABLE) has no tenant_id condition. This mutates tenant-scoped proposals without tenant scoping.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (or verify proposal ownership) before modifying proposal state.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} (persist rebased diff) lacks tenant scoping. Writing a rebased diff without tenant restriction risks cross-tenant modification.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause and validate the proposal belongs to the tenant prior to updating code_proposals.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table 'agent_knowledge' omits tenant_id in the WHERE clause (UPDATE ... WHERE id = ${r.id}). This can update rows from any tenant that share the id.
  - _Fix:_ Constrain the UPDATE to the tenant, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id} (or the caller's validated tenantId). Always include tenant_id in WHERE for writes to tenant-scoped tables to prevent cross-tenant modification.
- **server/research-engine.ts:158** — UPDATE against tenant-scoped table research_sessions in cleanupZombieSessions has no tenant_id constraint — this will mark running sessions as completed across all tenants.
  - _Fix:_ Scope the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant and run per-tenant UPDATEs. Derive tenant(s) from activeSessions or accept a tenant parameter so the statement only affects the caller's tenant.
- **server/routes.ts:4330** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The statement: INSERT INTO project_notes (project_id, note, author) VALUES (...) — missing tenant_id column. Without tenant_id, a note could be recorded against a project id belonging to another tenant.
  - _Fix:_ Include tenant_id in the INSERT and ensure the project belongs to that tenant. Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}). Also validate ownership: verify the project (projId) is owned by tenantId before inserting, or use a storage API that enforces tenant scoping.
- **server/sculptor.ts:93** — UPDATE to sculptor_sessions lacks tenant scoping (no WHERE tenant_id = ${tenantId}). The INSERT that created the session included tenant_id, but subsequent UPDATE uses only id and can modify another tenant's row if ids are guessed or leaked.
  - _Fix:_ Always include tenant_id in the WHERE clause for updates against tenant-scoped tables. Change to e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}. Prefer parameterized tenantId from the caller, and/or validate the session belongs to the caller before updating.
- **server/sculptor.ts:100** — UPDATE to sculptor_sessions (setting conversation_id) uses WHERE id = ${session.id} with no tenant_id constraint. This writes to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${session.id} AND tenant_id = ${params.tenantId}. Alternatively, perform the update inside a transaction and re-check that the session row's tenant_id matches the caller's tenant before making changes.
- **server/sculptor.ts:155** — monitorSession updates sculptor_sessions (SET status, result, tool_calls_count, tokens_used, completed_at) using WHERE id = ${sessionId} with no tenant_id filter. monitorSession receives tenantId but the UPDATE doesn't enforce it, allowing cross-tenant modification.
  - _Fix:_ Include tenant scoping on the UPDATE: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Ensure all periodic/background updaters always constrain by tenant_id when mutating tenant-scoped tables.
- **server/sculptor.ts:346** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} — no tenant filter. This writes tenant-scoped data without verifying the caller's tenant owns the session row.
  - _Fix:_ Constrain the UPDATE by tenant_id: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. If tenantId is not available at call-site, fetch the session with a tenant check first and refuse if it doesn't belong to the caller.
- **server/second-opinion.ts:404** — settleFusionReservation issues UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} with no tenant_id constraint. agent_cost_ledger is tenant-scoped; updating by id alone can affect another tenant's ledger row.
  - _Fix:_ Require and pass tenantId into settleFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause. Before updating, verify the reservation row belongs to the intended tenant (SELECT .. WHERE id=$1 AND tenant_id=$2) or perform the update guarded by tenant_id to prevent cross-tenant mutation.
- **server/second-opinion.ts:423** — releaseFusionReservation does UPDATE agent_cost_ledger SET cost_usd='0', operation=... WHERE id = ${reservationId} without tenant scoping. This can clear/modify another tenant's reservation row by id alone.
  - _Fix:_ Require tenantId in releaseFusionReservation and include tenant_id = ${tenantId} in the WHERE clause. Validate that the reservationId belongs to the tenant before releasing, or perform the update within a transaction that checks tenant ownership first.
- **server/seed.ts:2860** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without constraining tenant_id — a cross-tenant write risk.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or verify the row's tenant matches the caller before updating. Always enforce eq(table.tenantId, tenantId) for writes to tenant-scoped tables.
- **server/seed.ts:3137** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... performs a write to a tenant-scoped table using only id (no tenant_id constraint).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or validate the project's tenant ownership before applying the update. All UPDATEs/DELETES on tenant-scoped tables must include tenant_id in the WHERE clause.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id}) inserts into a tenant-scoped join table without setting tenant_id (and derived values came from an unscoped query).
  - _Fix:_ Include tenant_id in the INSERT (tenant_id column) populated from a trusted tenantId, and ensure you only insert links for rows owned by that tenant. Prefer validating ownership (e.g. SELECT ... WHERE id = ${r.id} AND tenant_id = ${tenantId}) before inserting and use INSERT ... VALUES (${tenantId}, ${projectId}, ${conversationId}).
- **server/seed.ts:4431** — DELETE FROM `heartbeat_tasks` executed without a tenant_id constraint (DELETE FROM heartbeat_tasks WHERE ...), which can delete rows across all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${tenantId}), or otherwise limit the deletion to explicit task ids known to belong to the intended tenant. Review whether this operation should run per-tenant and avoid global deletes without explicit tenant validation.
- **server/self-improvement.ts:243** — getExperimentHistory (and other code paths) perform unscoped SELECTs against the experiments table when no tenantId condition is supplied, returning data across tenants.
  - _Fix:_ Make tenantId required for history reads or return an empty result / throw when absent. Ensure every SELECT on experiments includes eq(experiments.tenantId, tenantId) (or equivalent Drizzle where) to enforce tenant isolation.
- **server/self-improvement.ts:394** — db.update(experiments).where(eq(experiments.id, experiment.id)) updates the experiments row by id only and does NOT constrain by tenant_id — a caller could update another tenant's experiment by id.
  - _Fix:_ Constrain UPDATEs with tenant_id as well. For example: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Alternatively, use a single INSERT ... RETURNING id/tenant_id and then use both id+tenant in subsequent updates to ensure the record belongs to the calling tenant.
- **server/self-improvement.ts:413** — Error-path UPDATE (setting status='error') uses WHERE eq(experiments.id, experiment.id) without tenant_id constraint — same cross-tenant write risk as above.
  - _Fix:_ Add tenant constraint to the UPDATE: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Ensure both normal and error code paths include the tenant check.
- **server/self-reflection.ts:366** — storeLesson performs an UPDATE on memory_entries using WHERE id = <id> but does NOT include tenant_id in the WHERE clause. Although the SELECT that found the id used tenant_id, the UPDATE path still lacks an explicit tenant constraint allowing a mis-scoped write if ids are guessed or race conditions occur.
  - _Fix:_ Include tenant scoping on the UPDATE: WHERE id = ${id} AND tenant_id = ${tenantId}. Prefer always constraining writes by tenant_id (defense-in-depth) even if the id was previously selected with tenant scope.
- **server/skillify.ts:196** — SELECT from `conversations` is executed without constraining by tenant_id (WHERE tenant_id = <caller tenant>). The code fetches the conversation by id only and performs an in-memory tenant check after the SELECT, which violates the requirement that isolation be enforced via SQL filters.
  - _Fix:_ Add a tenant filter to the query (do not rely only on post-fetch checks). Example: include eq(conversations.tenantId, tenantId) in the .where() or use the tenantScope / requireTenantScope helper so the DB never returns rows from other tenants. Alternatively, use assertConversationInTenant(projectId, tenantId) before reading child rows.
- **server/skillify.ts:203** — SELECT from `messages` uses only conversation_id in the WHERE clause and does not include tenant scoping. Reading tenant-scoped message rows without a tenant_id constraint allows cross-tenant data exposure (even if the conversation was validated in-memory).
  - _Fix:_ Scope message queries to the caller's tenant in SQL. Either add eq(messages.tenantId, tenantId) to the .where() or join via conversations and require conversation.tenant_id = tenantId in the query. Use tenantScope/assertValidTenantId or requireTenantScope helpers to enforce this at the SQL level.
- **server/skillify.ts:91** — SELECT from `messages` (recentMsgs in distillIntent) filters only by conversationId and omits tenant_id. This performs a tenant-scoped table read without an SQL-level tenant constraint.
  - _Fix:_ Include the tenant constraint in the SELECT (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise ensure the DB query enforces tenant ownership before returning rows. Prefer tenantScope/requireTenantScope helper to validate tenantId at call sites.
- **server/skillify.ts:331** — SELECT from `messages` (recentMsgs in skillifyConversation) filters only by conversationId and omits tenant_id. This allows reading messages from other tenants if an attacker supplies a foreign conversation id.
  - _Fix:_ Add tenant scoping to the query. For example, .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). Alternatively, validate ownership with an SQL check helper (assertConversationInTenant or tenantScope) prior to selecting messages, but the SQL must include tenant_id in the WHERE clause.
- **server/skillify.ts:223** — SELECT from `personas` fetches a persona by id only (eq(personas.id, conv[0].personaId)) without constraining by tenant_id. If personas is tenant-scoped, this is a cross-tenant read risk.
  - _Fix:_ Constrain persona lookups to the caller's tenant in SQL (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))). If personas can be shared, document that explicitly; otherwise use tenantScope/requireTenantScope or assert the persona belongs to the tenant before using it.
- **server/storage.ts:832** — Mass UPDATE on tenant-scoped table `memory_entries` with no tenant filter (archiveExpiredMemories updates all expired memory_entries across tenants).
  - _Fix:_ Do not run global, unscoped DML against tenant-scoped rows. Either run the archival per-tenant (iterate tenants and scope by tenant_id) or add tenant scoping criteria. If this job intentionally spans tenants, restrict execution to an admin-only path and document/guard it heavily.
- **server/storage.ts:848** — Mass UPDATE on tenant-scoped table `memory_entries` with no tenant filter (archiveStaleMemories updates rows by created_at/last_accessed without tenant scoping).
  - _Fix:_ Same as above — add tenant scoping (run per-tenant) or otherwise ensure only authorized admin contexts can perform cross-tenant updates and make the action explicit in code and audit logs.
- **server/storage.ts:1296** — Multiple reads across many tenant-scoped tables with NO tenant filter in getAllDataForExport (selects conversations, messages, memoryEntries, agentKnowledge, dailyNotes, heartbeatTasks, deliveryLogs, fileStorage, etc.).
  - _Fix:_ This function performs a full-platform export — restrict it to admin-only contexts, and require/validate an explicit tenantId to export a single tenant. If a global export is necessary, gate it behind an audited, authenticated admin operation and make that explicit in the function name and callsites.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (no eq(customTools.tenantId, tenantId)).
  - _Fix:_ Restrict the query to the caller's tenant. e.g. include .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). If tenantId is not available, require it as a parameter and validate it before issuing the SELECT.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id only (eq(customTools.id, inserted.id)) with NO tenant_id constraint.
  - _Fix:_ Add a tenant check to the WHERE clause (and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))) or fetch using a transaction/context that verifies the inserted row belongs to the expected tenant. Ensure caller provides/validates tenantId before this select.
- **server/tool-learning.ts:294** — UPDATE on tenant-scoped table `custom_tools` (.where(eq(customTools.id, tool.id))) without constraining by tenant_id — missing tenant ownership check.
  - _Fix:_ Require and use the tenantId for the caller/tool row in the WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))). If caller code cannot provide tenantId, read-and-verify the row's tenant_id first and abort if it doesn't match.
- **server/tool-learning.ts:316** — DELETE on tenant-scoped table `custom_tools` (db.delete(...).where(eq(customTools.id, tool.id))) with NO tenant_id constraint — may delete another tenant's row if IDs are guessed or reused.
  - _Fix:_ Constrain deletes by tenant as well: db.delete(customTools).where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure revertToolRow accepts/validates the tenantId of the tool being reverted or performs a guarded lookup before deleting.
- **server/tool-learning.ts:367** — UPDATE to increment usageCount on `custom_tools` (.where(eq(customTools.id, tool.id))) without a tenant_id filter.
  - _Fix:_ Include tenant verification in the update: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Pass the tenantId into executeCustomTool (or fetch and assert the row's tenant_id matches the caller) before performing the update to enforce tenant isolation.
- **server/tools.ts:4184** — Unscoped SELECT against tenant-scoped table 'memory_entries' — query omits tenant_id filter, which can leak other tenants' memory entries.
  - _Fix:_ Enforce tenant scoping on this read. Add a WHERE tenant_id = ${tenantId} (or the Drizzle equivalent .where(eq(memoryEntries.tenant_id, tenantId))) and pass tenantId from the caller context (e.g. params._tenantId). Fail closed when tenantId is missing. Prefer using existing tenant-assert helpers or parameterized Drizzle queries to avoid accidental omission.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE against tenant-scoped table `memory_entries` omits a tenant_id constraint (WHERE id = ${id} only). This permits updating a row by id without asserting it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. add AND tenant_id = ${tid} to the WHERE clause (or use Drizzle's .where(eq(table.tenantId, tid)) / parameterized binding). Alternatively verify ownership when selecting the id and perform the update in the same statement with tenant check to avoid race conditions.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` omits a tenant_id constraint (WHERE id = ANY(${ids}) only). This updates rows by id without asserting tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE: e.g. UPDATE memory_entries SET ... WHERE tenant_id = ${tid} AND id = ANY(${ids}). Prefer single parameterized statements that enforce tenant_id to avoid cross-tenant updates.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` omits a tenant_id constraint (WHERE id = ${id} only). This can update a row by id without verifying it belongs to the caller's tenant.
  - _Fix:_ Add an explicit tenant_id constraint to the UPDATE: WHERE tenant_id = ${tid} AND id = ${id} (or use Drizzle helpers to eq on tenantId). Ensure ownership is enforced in the same statement to prevent TOCTOU/race issues.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id filter (SELECT id, content::jsonb FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ${channel:phone}).
  - _Fix:_ Avoid querying a tenant-scoped table across tenants. Add an explicit tenant_id filter to the WHERE clause (or move pairing rows required to be globally visible into a dedicated global table with strong access controls). If cross-tenant enumeration is intentionally required, document and centralize that behaviour and perform robust ownership verification before any routing; prefer explicit WHERE tenant_id = ${tenantId} whenever possible.
- **server/twilio.ts:116** — SELECT on `conversations` is issued without constraining tenant_id (SELECT tenant_id FROM conversations WHERE id = ${info.conversationId}).
  - _Fix:_ Constrain the query to the expected tenant (e.g. WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId}) so the DB enforces the ownership check rather than relying solely on application-side comparison. If you must select by id first, immediately validate that the returned tenant_id matches the claimed tenant and fail closed when ambiguous.
- **server/wiring-invariants.ts:260** — Query against messages has no tenant_id constraint: SELECT count(*)::int AS n FROM messages WHERE created_at > NOW() - INTERVAL '14 days' — this reads tenant-scoped chat data across all tenants.
  - _Fix:_ Do not run unfiltered queries against messages. Add WHERE tenant_id = ${tenantId} for per-tenant checks or explicitly iterate tenants and aggregate per-tenant. If an admin-global metric is required, ensure this path is admin-only and documented; otherwise scope the query to the caller's tenant using parameterized filters (or Drizzle's .where(eq(table.tenantId, tenantId))).

### HIGH (155)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects rows from experiments without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This can cause processing of other tenants' experiments.
  - _Fix:_ Constrain the query to the caller's tenant. Either iterate per-tenant or add WHERE tenant_id = ${tenantId} (and pass tenantId into the function) so only that tenant's experiments are read.
- **server/agent-activity.ts:122** — completeActivity performs an UPDATE on agentActivity without any tenant constraint (UPDATE agent_activity ... WHERE id = activityId). This can modify another tenant's row if IDs collide.
  - _Fix:_ Require and accept tenantId in completeActivity and include it in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}), or otherwise verify ownership before updating.
- **server/agentic-engines.ts:114** — routeInsightToMinerva runs a SELECT on plans without constraining by tenant_id (SELECT id FROM plans WHERE source = ... AND source_ref = ...). This can match plans belonging to other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${insight.tenantId}) or otherwise ensure the SELECT is limited to the intended tenant before deciding on idempotency.
- **server/agentic-engines.ts:160** — retryPendingMinervaRouting SELECTs ai_insights rows across all tenants (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ...). Reading tenant-scoped rows without tenant filtering risks cross-tenant processing.
  - _Fix:_ Either run the sweep per-tenant and constrain queries with tenant_id = ${tenantId}, or explicitly document/implement safe multi-tenant processing with owner validation prior to any tenant-scoped operations. Prefer adding tenant_id filtering before acting on rows.
- **server/agentic-engines.ts:242** — autoApplyEligibleInsights executes UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) without any tenant_id constraint, updating rows across all tenants.
  - _Fix:_ Scope the UPDATE to a specific tenant (WHERE tenant_id = ${tenantId} AND ...), or perform per-tenant updates so you never bulk-update tenant-scoped tables across tenants without explicit intent and ownership checks.
- **server/agentic-engines.ts:343** — gatherOperationalData issues a heartbeat_logs query without tenant scoping (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads operational logs across tenants into a per-tenant analysis.
  - _Fix:_ Add tenant_id = ${tenantId} to the heartbeat_logs query (or otherwise ensure heartbeat_logs are intended to be global). All reads of tenant-scoped logs must be constrained to the caller's tenant.
- **server/agentic-engines.ts:450** — runPredictiveEngine queries recent heartbeat_logs without a tenant filter (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...). This can surface other tenants' logs.
  - _Fix:_ Filter heartbeat_logs by the tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'success') or run the query per-tenant to ensure isolation.
- **server/agentic-engines.ts:518** — runOptimizationEngine aggregates heartbeat_logs without tenant filtering (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This reads multi-tenant data into tenant-specific optimization logic.
  - _Fix:_ Restrict the heartbeat_logs aggregation to the tenant (e.g. add WHERE tenant_id = ${tenantId}) or otherwise partition analysis by tenant before aggregating.
- **server/agentic-engines.ts:532** — runOptimizationEngine selects heartbeat_tasks WHERE enabled = true with no tenant scope (SELECT ... FROM heartbeat_tasks WHERE enabled = true ...). heartbeat_tasks are tenant-scoped elsewhere; reading them without tenant_id risks cross-tenant exposure.
  - _Fix:_ Include tenant_id = ${tenantId} in the heartbeat_tasks query, or explicitly document that heartbeat_tasks are global. Prefer adding the tenant filter to enforce isolation.
- **server/agentic-features.ts:525** — scoreLeads updates lead_enrichments using WHERE id = ${lead.id} but does not include tenant_id in the WHERE clause. An attacker or bug could cause cross-tenant updates if an id collides.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or verify the row's tenant ownership before updating.
- **server/agentic-features.ts:672** — advanceSequence updates outreach_enrollments (UPDATE ... WHERE id = ${enrollment.id}) without including tenant_id in the WHERE clause. This can update another tenant's enrollment row with the same id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to ensure only the calling tenant's rows are modified.
- **server/agentic-features.ts:721** — classifyReply updates outreach_enrollments using WHERE id = ${row.id} but omits tenant_id. This can modify enrollments belonging to other tenants if ids collide.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or verify ownership before updating.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE agentApprovals SET status='expired' ... WHERE status = 'pending' AND expiresAt < now() without any tenant constraint. This will expire pending approvals across all tenants in one shot.
  - _Fix:_ Either run expiry per-tenant (add tenant filter) or accept a tenant parameter and include WHERE tenant_id = ${tenantId} to avoid modifying approvals for other tenants. Ensure subsequent run updates likewise include tenant scoping.
- **server/agentic/department-budgets.ts:143** — SELECT from tenant-scoped table `department_budgets` without tenant_id constraint: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads rows across all tenants.
  - _Fix:_ If this is intended to be a cross-tenant admin sweep, make the admin intent explicit (e.g. name the function admin-only, require an explicit admin credential/flag, and document/approve the cross-tenant access). Otherwise restrict the query to a single tenant by adding `WHERE tenant_id = ${tenantId}` (or pass an explicit tenantId parameter) so every read is constrained to the caller's tenant as required by the tenancy isolation invariant.
- **server/agentic/harness-adaptation.ts:72** — Query reads `agent_trace_spans` with no tenant filter: `SELECT metadata->>'modelId' AS model_id, tool_name, summary, status FROM agent_trace_spans ...` — likely a tenant-scoped table, so this returns cross-tenant trace data.
  - _Fix:_ Either scope the mining to a single tenant by accepting/using a tenantId parameter and adding `WHERE tenant_id = ${tenantId}`, or make this an explicit admin-only operation that documents and enforces cross-tenant access (require explicit admin credential/flag). Avoid silent cross-tenant reads to preserve tenant isolation.
- **server/agentic/resume-worker.ts:66** — resumePendingApprovalRuns() issues a SELECT that reads agent_runs (and joins agent_approvals) without constraining by a caller tenant_id — the query can return rows across tenants.
  - _Fix:_ Restrict the initial SELECT to a specific tenant (pass tenantId in) or run per-tenant sweeps. If the worker intentionally scans across tenants, make that explicit and ensure it's an admin-only job; otherwise add WHERE r.tenant_id = ${tenantId} (or parameterize) and only operate on rows for that tenant.
- **server/agentic/task-forces.ts:40** — createTaskForce() builds an SQL array literal for persona_ids (e.g. `{1,2}`) from request-derived personaIds and injects it into a sql fragment (sql`${literal}::int[]`). This is string-interpolated SQL with request-derived content (injection / isolation risk).
  - _Fix:_ Avoid injecting pre-composed SQL fragments. Use parameterized arrays instead (pass a sanitized array to the driver / Drizzle insert API) or use safe parameter binding, e.g. .values({ personaIds: p.personaIds ? p.personaIds.filter(Number.isInteger) : null }) or use sql`ARRAY[...]` with parameterized elements. Ensure any literal built from input is fully validated and parameterized rather than injected as raw SQL.
- **server/agentic/wake-scheduler.ts:79** — UPDATE/SELECT on tenant-scoped table agent_wake_schedules performed without constraining tenant_id. The claim/selection query that moves pending wakes to 'firing' runs across all tenants (no WHERE tenant_id = ...).
  - _Fix:_ Run the scheduler per-tenant (loop tenants or query distinct tenant_ids then claim per-tenant), or add an explicit tenant_id constraint to the UPDATE/inner SELECT. Ensure any row you later emit/update is always verified to belong to that tenant.
- **server/auth.ts:1019** — SELECT from tenant-scoped table password_reset_tokens uses only token hash in WHERE (no tenant_id constraint). A request-derived token is used to look up the row without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the SELECT WHERE clause (e.g. WHERE token = ${tokenHash} AND tenant_id = ${expectedTenantId}) or require/provide the tenant context when resolving the token. If the token is the sole bearer credential, document/design this explicitly; otherwise bind lookups to tenant context to enforce isolation.
- **server/auto-memorize.ts:159** — SELECT from messages (JOIN conversations) pulls recent messages across all tenants (WHERE m.created_at > ... AND role IN ('user','assistant')) with no tenant_id constraint. This routine processes tenant messages without scoping to a tenant.
  - _Fix:_ Process messages per-tenant (add WHERE c.tenant_id = ${tenantId} or iterate tenants). If cross-tenant aggregation is intended, require this to run under an explicit, auditable admin context and ensure privacy review.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table 'conversations' lacks a tenant_id constraint in the WHERE clause. The code runs: UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — missing tenant guard risks modifying a conversation belonging to another tenant if IDs collide or are forged.
  - _Fix:_ Include the tenant filter in the UPDATE WHERE clause using the trusted tenant from the earlier query, e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}. Alternatively, perform the UPDATE inside a transaction after re-validating ownership (SELECT ... FOR UPDATE) to ensure the row belongs to the intended tenant before mutating it.
- **server/business-tools.ts:84** — INSERT into invoice_items does not include tenant_id. invoice_items appears to be a tenant-scoped table but the INSERT only sets invoice_id and other columns, so tenant ownership is not enforced on insert.
  - _Fix:_ Include tenant_id in the invoice_items INSERT (e.g. add tenant_id column and VALUES (${tid}, ...) or set it from a verified invoice row). Alternatively validate that invoice_id belongs to the caller's tenant before inserting. Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly.
- **server/business-tools.ts:334** — LEFT JOIN on customers omits a tenant constraint for the joined row (cust). The query selects cust.company_name without ensuring the customer row is scoped to the same tenant, risking cross-tenant data exposure via the join.
  - _Fix:_ Constrain the joined customers row to the caller's tenant in the JOIN condition (e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}) or explicitly add AND cust.tenant_id = ${tid} where appropriate. Always ensure reads against tenant-scoped tables include tenant_id filters even for joined tables.
- **server/capability-review.ts:490** — UPDATE capability_reviews sets reused = true WHERE id = ${pending.reviewId} AND reused = false without constraining tenant_id. The write may affect a row belonging to a different tenant since tenant context is not enforced in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${expectedTenantId}). To do that you should persist tenantId alongside pending.reviewId when recording the review and use it here, or otherwise resolve and verify the tenant before updating. Always include tenant_id in WHERE for writes to tenant-scoped tables.
- **server/ceo-orchestrator.ts:1806** — getActivePlan(planId) returns an in-memory OrchestrationPlan by id without checking the caller's tenant. A caller can read an active plan belonging to another tenant.
  - _Fix:_ Require and enforce the caller's tenantId (or an auth context) in this API and verify plan.tenantId === callerTenantId before returning the plan. Alternatively, expose a tenant-scoped accessor (getActivePlanForTenant(planId, tenantId)).
- **server/ceo-orchestrator.ts:1810** — getActivePlansForConversation(conversationId) iterates activePlans and returns plans matching conversationId without verifying tenant ownership. This can leak other tenants' active plans tied to the same conversationId.
  - _Fix:_ Require the caller's tenantId (or obtain it from auth context) and filter returned plans by plan.tenantId === callerTenantId. Provide a tenant-scoped API signature (getActivePlansForConversationForTenant(conversationId, tenantId)).
- **server/channel-routing.ts:30** — SQL SELECT joins channel_routes to personas (JOIN personas p ON p.id = cr.persona_id) without constraining the personas table by tenant_id. If personas is tenant-scoped, this reads persona rows across tenants and can leak or misuse persona data.
  - _Fix:_ Scope the join by tenant: ensure channel_routes is tenant-scoped (add tenant_id to the table) and include WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId} (or add .where(eq(personas.tenant_id, tenantId)) when using Drizzle). Always pass the caller's tenantId into these functions and enforce it in the SQL.
- **server/channel-routing.ts:16** — channel_routes table is created with no tenant_id column (CREATE TABLE IF NOT EXISTS channel_routes ... UNIQUE(channel)). In a multi-tenant deployment this global table will lead to cross-tenant collisions and/or require joins to tenant-scoped tables without tenant filters.
  - _Fix:_ Make channel_routes tenant-scoped by adding a tenant_id column (NOT NULL), include tenant_id in the UNIQUE constraint (e.g. UNIQUE(tenant_id, channel)), and update all channel_routes queries to filter by tenant_id = <caller tenant>. If the table is intentionally global, document that choice and avoid joining it to tenant-scoped tables without explicit ownership checks.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (no WHERE tenant_id = ${tenantId}). Relying on prior project ownership checks is brittle and violates the invariant that every tenant-scoped read must include an application-level tenant filter.
  - _Fix:_ Constrain the query to the caller's tenant. Example: add AND tenant_id = ${tenantId} to the WHERE clause, or join to projects and filter on p.tenant_id = ${tenantId} (e.g. SELECT ... FROM project_files pf JOIN projects p ON p.id = pf.project_id WHERE pf.project_id = ${projectId} AND p.tenant_id = ${tenantId}). Use parameterized Drizzle .where(eq(...)) where possible.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (WHERE project_id = ${projectId} only). This allows potential cross-tenant access if project_id can be influenced.
  - _Fix:_ Add an explicit tenant filter to the query. For example: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY ... LIMIT 20. Alternatively, join to projects and verify p.tenant_id = ${tenantId} before selecting.
- **server/chat-engine.ts:4320** — Attachment/write related to a message (storage.attachCitations(messageId, citations)) is invoked without an explicit tenant id. If attachCitations writes to a tenant-scoped table (or queries by message id without tenant filtering), this can break tenant isolation.
  - _Fix:_ Pass the tenant id (or tenant-scoped message id object) into attachCitations, or modify attachCitations to accept and enforce tenantId, and/or ensure it validates that the referenced messageId belongs to the caller's tenant before writing citations.
- **server/chat-engine.ts:4004** — Recording tool execution uses only conversationId (recordToolExecution(conversationId, toolName, ...)) with no tenant provided. If recordToolExecution writes to tenant-scoped tables keyed by conversationId, doing so without verifying the conversation's tenant-owner may allow cross-tenant writes/reads.
  - _Fix:_ Require and pass tenantId into recordToolExecution and/or verify the conversation row belongs to the caller's tenant before recording tool execution. Ensure storage writes/queries always include WHERE tenant_id = <tenant>.
- **server/chat-engine.ts:4008** — Second call to recordToolExecution that records ids (recordToolExecution(conversationId, toolName, true, ids)) is made without tenant context. Same tenant-isolation risk as above.
  - _Fix:_ Include tenantId when calling recordToolExecution, and validate that any referenced identifiers (conversationId, recorded ids) are owned by the caller's tenant before writing. Ensure storage layer enforces tenant filtering on these operations.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table 'credential_vault' performed without tenant_id constraint (last_used_at update: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}).
  - _Fix:_ Constrain the update by tenant to avoid touching another tenant's row: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}. Prefer using the previously-fetched credential's tenant_id or include tenantId parameter validated by caller.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table 'credential_vault' performed without tenant_id constraint (last_used_at update: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}).
  - _Fix:_ Constrain the update by tenant to avoid cross-tenant mutation: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}. Ensure getCredentialForSite validated ownership and propagate tenantId into the update.
- **server/crews-engine.ts:874** — addFlowStep inserts flow_steps.crew_id from caller-supplied params without verifying that the referenced crew belongs to the caller's tenant.
  - _Fix:_ Before inserting a crewId into flow_steps, verify ownership: e.g. if params.crewId is provided, call getCrew(params.crewId, params.tenantId) (or SELECT ... FROM crews WHERE id = $1 AND tenant_id = $2) and reject the request if no row. Alternatively, require the crewId to be null or derive it from a tenant-scoped lookup. Always enforce tenant ownership for foreign IDs before writing them.
- **server/data-protection.ts:72** — softDeleteConversation updates conversations.deleted_at without constraining the UPDATE by tenant_id (UPDATE ... WHERE id = ${conversationId} missing tenant_id), relying only on an earlier SELECT that included tenant_id.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (AND tenant_id = ${tenantId}) so the write is explicitly scoped to the caller's tenant. Prefer doing read+write in a transaction or using withTenantTx to avoid TOCTOU between the verification SELECT and the UPDATE.
- **server/data-protection.ts:112** — permanentlyPurgeSoftDeleted reads conversations across all tenants (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) — tenant-scoped table accessed with no tenant filter.
  - _Fix:_ If this is meant to run per-tenant, require and pass a tenantId and add WHERE tenant_id = ${tenantId}. If it's an admin/global purge, run it under a clearly-audited admin path (and document/secure it). Avoid ad-hoc tenant-scoped queries that touch all tenants without explicit privilege checks; prefer withTenantTx or an explicit admin guard.
- **server/delivery-pipeline.ts:188** — UPDATE on tenant-scoped table `delivery_logs` without any tenant constraint (updateDeliveryLog uses .where(eq(deliveryLogs.id, id)) only). This lets code update another tenant's row if given an id.
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog (or otherwise ensure ownership) and add it to the WHERE clause, e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). Audit all call sites to propagate tenant context or perform the update inside a transaction that verifies ownership first.
- **server/delivery-pipeline.ts:683** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId with NO tenant constraint (.where(eq(deliveryLogs.stripePaymentId, req.stripePaymentId))). This can find another tenant's delivery for the same payment id and leak/act across tenants.
  - _Fix:_ Scope the lookup to the caller's tenant (include eq(deliveryLogs.tenantId, req.tenantId) or require a tenantId parameter). If stripePaymentId is intended to be unique per-tenant, make it part of the WHERE (stripePaymentId + tenant_id) or enforce uniqueness differently.
- **server/delivery-pipeline.ts:720** — UPDATE against `delivery_logs` using .where(eq(deliveryLogs.id, prior.id)) with NO tenant constraint when clearing stripePaymentId on a prior delivery. This mutates a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = <owner tenant>) or perform the change inside a transaction that first verifies the row belongs to the expected tenant before updating.
- **server/delivery-pipeline.ts:1014** — SELECT from `delivery_logs` by stripePaymentId in getDeliveryByStripePayment has NO tenant constraint. This can return a delivery belonging to another tenant.
  - _Fix:_ Require a tenantId or otherwise scope the SELECT to a tenant: WHERE stripe_payment_id = $1 AND tenant_id = $2, or document and enforce that stripePaymentId is namespaced per-tenant and validate ownership before returning.
- **server/doc-collections.ts:165** — DELETE FROM `doc_chunks` uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but has NO tenant_id constraint. Although collection existence was checked earlier, the DELETE itself lacks an explicit tenant filter.
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}) or derive and assert the collection's tenant_id inside the same transaction before deleting so every write against the tenant-scoped table includes the tenant_id equality predicate.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM `messages` (r1) with no tenant constraint: the traffic gate query counts user messages across all tenants. This reads a tenant-scoped table globally and may cause cross-tenant signal leakage.
  - _Fix:_ If this routine must operate per-tenant, add a tenant filter (WHERE tenant_id = ...). If it is intended to be platform-global, ensure this behavior is deliberate and documented; otherwise run these stats per-tenant or on an explicitly privileged global-only table.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM `tool_performance` lacks a tenant filter. This reads tenant-scoped usage data globally and can surface cross-tenant telemetry.
  - _Fix:_ Add tenant scoping (AND tenant_id = ...) to the query if the gate is supposed to be tenant-specific. If a global aggregate is required, ensure only an appropriately privileged platform process runs it and document the exception.
- **server/dormant-deprecation.ts:202** — SELECT DISTINCT tool_name FROM `tool_performance` (getEverInvokedTools) has NO tenant constraint and returns tool names across all tenants.
  - _Fix:_ Restrict to a tenant by adding WHERE tenant_id = ${tenantId} (and accept tenantId as a parameter), or explicitly document and isolate global usage if intended. Avoid reading tenant-scoped tables without tenant_id.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM `tool_performance` WHERE last_success_at > ... (getActiveTools14d) has NO tenant filter; it's reading tenant-scoped usage data globally.
  - _Fix:_ Add tenant scoping to the query (WHERE tenant_id = ${tenantId} AND ...), or run this only from a trusted global job with documented justification. Prefer explicit tenant filters for multi-tenant isolation.
- **server/dream-consolidation.ts:83** — SELECT role, content FROM `messages` WHERE conversation_id = ${conv.id} (inside getRecentSessionSummaries) lacks a tenant_id constraint. Although conversation rows were selected for a tenant, the messages SELECT does not include tenant_id explicitly.
  - _Fix:_ Include tenant scoping in the messages query (AND tenant_id = ${tenantId}) or perform the messages lookup JOINed to conversations (which are already tenant-scoped) so the DB enforces tenant ownership in the same statement/transaction.
- **server/email.ts:365** — SELECT id FROM `inbox_messages` WHERE message_id = ${msgId} LIMIT 1 (pollSharedInbox) has NO tenant constraint. This existence check can match another tenant's stored message_id and cause cross-tenant processing differences.
  - _Fix:_ Include tenant scoping in the existence check (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} OR tenant_id = ${tenantId}) so operations act only on messages belonging to the expected tenant/inbox.
- **server/email.ts:410** — SELECT id, message_id FROM `inbox_messages` WHERE (body_text IS NULL OR ...) AND (body_html IS NULL OR ...) ORDER BY id (backfillEmptyBodies) is executed without a tenant filter and will iterate messages across tenants.
  - _Fix:_ Run backfill per-tenant (add AND tenant_id = ${tenantId}) or otherwise scope the query by inbox_id so the job updates only messages for the intended tenant. Ensure maintenance/cron jobs either accept an explicit tenant parameter or run with privileged intention documented and controlled.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped table (memory_entries | agent_knowledge via dynamic table param) omits tenant_id in WHERE clause. This allows updating rows belonging to other tenants by id.
  - _Fix:_ Require and pass a tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Prefer using Drizzle's parameterized update API (db.update(...).set(...).where(eq(table.id, id), eq(table.tenantId, tenantId))) rather than raw SQL interpolation.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id in WHERE, allowing cross-tenant modifications.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${row.id} AND tenant_id = ${tenantId}). Prefer using Drizzle parameterized updates and pass the tenantId to the backfill routine so updates are tenant-scoped.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id in WHERE, allowing cross-tenant modifications.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${row.id} AND tenant_id = ${tenantId}) and run backfills/updates in a tenant-scoped context or under an explicit, documented admin tenant if intended.
- **server/google-drive.ts:547** — SELECT from projects by project id omits tenant filter: db.select(...).from(projects).where(eq(projects.id, projectId)). This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Scope the SELECT to the caller's tenant (e.g. WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId}), or use a single query that selects only rows belonging to the caller and fail-closed when none is returned.
- **server/google-drive.ts:826** — SELECT of a project by id (uploadAndShare) is performed without tenant scoping: .where(eq(projects.id, params.projectId)). This reads a tenant-scoped table before ownership is validated.
  - _Fix:_ Add tenant scoping to the SELECT (WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId}) or otherwise ensure the query is parameterized to return only rows owned by the calling tenant before exposing the row to further logic.
- **server/google-drive.ts:1004** — SELECT used to validate a project ownership in searchDriveFiles reads projects by id without adding tenant_id to the WHERE (where(eq(projects.id, params.projectId))). Reads against tenant-scoped tables must include tenant constraints.
  - _Fix:_ Constrain the SELECT with the caller's tenant: WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId} (or the Drizzle equivalent). This avoids reading foreign-tenant rows.
- **server/google-drive.ts:1066** — SELECT to fetch projects.driveFolderId (Drive API pass) uses WHERE eq(projects.id, params.projectId) with no tenant constraint, allowing reading tenant-scoped projects rows without scoping.
  - _Fix:_ Include the tenant filter (e.g. WHERE projects.id = ${projectId} AND projects.tenant_id = ${tenantId}) so Drive-folder resolution only touches rows owned by the caller's tenant.
- **server/health-audit.ts:314** — Query against code_proposals omits tenant scoping. The SELECT pulls pending/unverified proposals across the entire DB without a WHERE tenant_id = ... filter.
  - _Fix:_ If code_proposals is tenant-scoped, restrict the query to the intended tenant (e.g. add AND tenant_id = ${tenantId}) or require/validate an explicit admin-only context before running a global scan. Do NOT rely on implicit ownership. If this is intentionally global, document and gate it to an admin-only pathway.
- **server/health-audit.ts:333** — UPDATE that archives stale code_proposals runs without tenant scoping (UPDATE code_proposals ... WHERE id = ANY(...)). This can modify rows for any tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform an ownership check first. Alternatively, run archival only in an explicit admin context and ensure the operation is intended to be global and documented as such.
- **server/health-audit.ts:346** — Query selecting 'stuck' heartbeat_tasks is not tenant-scoped. It reads tasks across all tenants (and later the code updates heartbeat_tasks.enabled=false without tenant constraint).
  - _Fix:_ If heartbeat_tasks are tenant-scoped, filter by tenant_id (AND tenant_id = ${tenantId}) or otherwise ensure this scanner runs only in an admin context. When disabling tasks, include tenant verification in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}).
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) is executed without tenant scoping, allowing disabling tasks for any tenant.
  - _Fix:_ Add tenant ownership checks to the UPDATE (e.g. AND tenant_id = ${tenantId}) or verify each task belongs to the expected tenant before toggling enabled=false. Restrict this mutation to explicit admin workflows with audit/logging.
- **server/heartbeat.ts:1743** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant constraint. heartbeat_logs is likely tenant-scoped, so this reads logs across tenants.
  - _Fix:_ Restrict this query to the admin tenant or the intended tenant by adding AND tenant_id = ${tenantId} (or use storage.getHeartbeatLogs with a tenant parameter). Avoid cross-tenant reads without explicit authorization.
- **server/job-queue.ts:98** — claimDueJobs updates agent_jobs (claims jobs) using a SELECT/UPDATE that lacks any tenant_id constraint — it can claim or mutate jobs for any tenant indiscriminately.
  - _Fix:_ Make job claiming tenant-scoped: either run the job worker with an explicit tenantId and add AND tenant_id = ${tenantId} in the inner SELECT/outer UPDATE, or otherwise ensure the claim operation enforces the intended tenant boundary and requires an explicit tenant parameter for non-admin code paths.
- **server/job-queue.ts:134** — reclaimExpiredLeases updates agent_jobs to failed_terminal for expired leases without scoping by tenant_id. This can mark jobs for other tenants as failed-terminal incorrectly.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or run reclaim per-tenant with an explicit tenant parameter) so lease-reclaim operations cannot affect other tenants' jobs.
- **server/job-queue.ts:148** — reclaimExpiredLeases re-queues expired agent_jobs (running→pending) without a tenant_id constraint, allowing cross-tenant mutation of job rows.
  - _Fix:_ Scope the UPDATE by tenant_id (or perform requeueing in a per-tenant pass) so only jobs belonging to the intended tenant are requeued.
- **server/job-queue.ts:177** — completeJob updates agent_jobs to 'succeeded' by id/attempts only, with no tenant_id constraint. A stale or forged caller could affect another tenant's job row by id.
  - _Fix:_ Require tenant_id as an additional guard in the UPDATE WHERE clause (AND tenant_id = ${tenantId}), and make callers supply/verify tenantId when completing jobs.
- **server/job-queue.ts:237** — failJob updates agent_jobs to 'failed_terminal' using WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} without checking tenant_id, which can mutate jobs across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause and require callers to pass the tenantId (or run these operations within a verified tenant context).
- **server/job-queue.ts:258** — failJob's retry-path UPDATE (set pending/next_run_at) does not include a tenant_id constraint, permitting cross-tenant state changes to job retry scheduling.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or perform class-aware backoff in a per-tenant operation) so retry scheduling cannot affect other tenants' jobs.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on procedureEvolutionRuns uses only the run id in the WHERE clause (where(eq(procedureEvolutionRuns.id, runId))) — no tenant_id predicate. This allows a caller to flip/modify another tenant's run row by id.
  - _Fix:_ Always include the tenant filter in the WHERE clause (e.g. and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Capture/propagate the run's tenantId at insert/lookup time and use it in subsequent updates, or fetch-and-verify ownership before updating.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on procedureEvolutionRuns uses only the run id in the WHERE clause to mark a run done — missing tenant_id predicate (concurrent noop/claim update).
  - _Fix:_ Add tenant scoping to the WHERE clause (require eq(procedureEvolutionRuns.tenantId, tenantId)) so a tenant cannot affect another tenant's run. Prefer a claim-then-write pattern that includes tenant_id in the CAS predicate.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE on procedureEvolutionRuns to mark 'done' (post-insert) uses only id in WHERE; tenant_id is not constrained.
  - _Fix:_ Include the tenant predicate in the WHERE (and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Ensure all follow-up updates to run rows verify ownership before writing.
- **server/lib/agent-trace.ts:124** — persistSpanClose issues an UPDATE against agent_trace_spans with WHERE span_id = ${spanId} but no tenant_id filter. agent_trace_spans is tenant-scoped, so this can close/modify spans belonging to another tenant if span_id is known or abused.
  - _Fix:_ Record/propagate tenantId when opening the span (e.g. store tenantId keyed by spanId) and include tenant_id = ${tenantId} in the UPDATE WHERE clause. Alternatively change persistSpanClose to accept tenantId (or fetch it from a trusted mapping) and use it in the WHERE to enforce per-tenant closure.
- **server/lib/bwb-job-progress.ts:74** — scope(jobId, ...) may return a WHERE clause lacking tenant predicate when BWB_TENANT_ID is missing. Multiple UPDATE/SELECT calls use .where(scope(...)) which can therefore operate without tenant_id, allowing writes/reads to affect rows across tenants (pinned only to jobId).
  - _Fix:_ Require and validate tenantId explicitly for these operations (fail if env/threaded tenant is missing), or have scope() throw/return an explicit error when tenant is unset instead of silently producing a jobId-only predicate. At a minimum, ensure callers pass tenantId explicitly and assert it before performing updates; consider making tenant filtering mandatory for any write to tenant-scoped tables.
- **server/lib/ideabrowser-backfill.ts:104** — INSERT INTO project_files omits tenant_id in the VALUES list. Every INSERT into a tenant-scoped table must set tenant_id explicitly (columns are NOT NULL with no default in this deployment model).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Ensure the inserted rows are explicitly tied to the tenant.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT INTO project_notes omits tenant_id in the VALUES list. Tenant-scoped INSERTs must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)) and validate ownership of project_id before inserting.
- **server/lib/scheduled-post-runner.ts:60** — SELECT ... FROM scheduled_posts (CTE 'due') is executed without constraining by tenant_id. This reads tenant-scoped rows across all tenants.
  - _Fix:_ Restrict the query to a single tenant (WHERE tenant_id = <tenant>) or iterate per-tenant. If the runner must operate cross-tenant, make the admin intent explicit and audit-authorize it; otherwise enforce tenant_id in the WHERE clause (or pass tenant context and run per-tenant).
- **server/lib/scheduled-post-runner.ts:266** — Platform array literal (`arrLit`) is constructed by stringifying request-derived platforms and then embedded into SQL as ${arrLit}::text[]. Building a SQL literal from request data risks injection and/or type/semantics errors.
  - _Fix:_ Use a parameterized approach or Drizzle helpers to pass arrays (e.g. db.insert(...).values({ platforms: lowered }) or use sql.array with proper bindings) instead of serializing into a SQL array literal. Always avoid raw string interpolation of request-derived values into SQL.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from agent_runs (tenant-scoped table) is executed without any tenant_id filter — reads rows across all tenants.
  - _Fix:_ If the tool is intended to be per-tenant, add WHERE tenant_id = <caller tenant>. If it is an admin cross-tenant audit, make that explicit and ensure admin-only authorization and logging; otherwise run the scan per-tenant with explicit tenant context.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from mind_tickets (tenant-scoped table) has no tenant_id constraint — returns rows across tenants.
  - _Fix:_ Add tenant_id filtering (WHERE tenant_id = <caller tenant>) or run a controlled, admin-approved cross-tenant audit with explicit checks. Avoid unguarded multi-tenant reads in normal request handlers.
- **server/lib/system-probes.ts:163** — SELECT count(*) FROM messages table is executed without filtering by tenant_id — this aggregates/reads tenant-scoped data across tenants.
  - _Fix:_ Supply and enforce a tenant_id filter for per-tenant probes. If a global count is required only for admin, gate the probe behind admin-only authorization and make cross-tenant intent explicit (and logged). Prefer per-tenant queries when invoked from tenant-scoped contexts.
- **server/lib/session-fact-extractor.ts:136** — extractAndPersistSessionFacts accepts a conversationId + tenantId and then calls storage.createConversationFact(row) without first verifying that the conversationId actually belongs to the supplied tenant. This can allow writing facts against a conversation owned by another tenant if conversation ownership isn't validated by storage.
  - _Fix:_ Before persisting, validate the referenced conversation: load the conversation by id and confirm its tenant_id matches the caller's tenant (or rely on a storage API that enforces ownership). Never accept an arbitrary foreign id from a caller without verifying ownership/tenant binding.
- **server/memory-graph.ts:123** — refreshCategoryCount updates memory_categories and computes its count without scoping to tenant_id. The inner SELECT counting memory_entries also omits tenant filtering, allowing cross-tenant count/updates.
  - _Fix:_ Require and pass tenantId into refreshCategoryCount and constrain both the inner count and the UPDATE with WHERE tenant_id = ${tenantId} (e.g. WHERE category_id = ${categoryId} AND tenant_id = ${tenantId}), and UPDATE memory_categories ... WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:194** — INSERT into memory_links does not set tenant_id. If memory_links is tenant-scoped, this creates links without an explicit tenant owner, allowing cross-tenant linking.
  - _Fix:_ Include tenant_id in the INSERT (and in the memory_links schema if missing). Pass the caller's tenantId and insert it explicitly: INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId}). If memory_links is intentionally global, document and enforce why cross-tenant links are safe.
- **server/memory-graph.ts:392** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks tenant_id scoping. Updating by id alone can affect rows belonging to other tenants (depending on id allocation).
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Alternatively, pass tenantId into this call and validate ownership before the update.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} does not include tenant_id. This may update triples belonging to other tenants if ids are global.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${c.id} AND tenant_id = ${tenantId}. Ensure tenantId is passed into extractTriples and used for all modifying statements.
- **server/minds-engine.ts:214** — createTicket builds a Postgres array literal via string interpolation for dependsOn: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. This interpolates request-derived values directly into SQL (injection + isolation risk).
  - _Fix:_ Avoid manual string construction of SQL array literals. Use parameterized array binding or Drizzle helpers (e.g. pass a JS array as a parameter or use sql.array/sql.join with parameterized elements). For example, use WHERE id = ANY(${sql.array(params.dependsOn, 'int')}) or let the ORM bind the array safely.
- **server/minds-engine.ts:262** — checkDependenciesMet uses string-interpolated array literal: WHERE id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. Even if ticket.dependsOn originates from DB, interpolating it into SQL is unsafe and can break isolation/parsing.
  - _Fix:_ Use parameterized array binding rather than string interpolation. Example: WHERE id = ANY(${sql.array(ticket.dependsOn, 'int')}) AND tenant_id = ${tenantId}, or use Drizzle's parameterized constructs to pass the array safely.
- **server/minds-engine.ts:325** — markEventsHandled uses string-interpolated array literal for eventIds: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. This interpolates caller-provided eventIds into SQL directly (injection + isolation risk).
  - _Fix:_ Bind the array parameter safely instead of string interpolation. Use a parameterized array binding (e.g. WHERE id = ANY(${sql.array(eventIds, 'int')}) AND tenant_id = ${tenantId}) or iterate/upsert using prepared statements provided by the ORM.
- **server/minerva-planner.ts:506** — getPlan's fallback path performs SELECT * FROM plans WHERE id = ${planId} with no tenant_id constraint when tenantId is omitted. Even if intended for trusted internal callers, this is an unguarded read on a tenant-scoped table.
  - _Fix:_ Avoid unscoped SELECTs. Require an explicit tenantId for all callers or, if an unscoped read is necessary, document and centralize a clearly-audited internal-only API path. Prefer adding tenant_id = ${tenantId} when possible.
- **server/pace-control.ts:85** — checkPace() queries heartbeat_logs without any tenant_id predicate (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND <laneFilter>), so global activity from all tenants is counted together and could allow one tenant's activity to affect others or leak usage patterns.
  - _Fix:_ Make pace checks tenant-aware. Accept an explicit tenantId (or persona-scoped tenant context) and include WHERE tenant_id = ${tenantId} in the query. If the intention is a global platform cap, document and gate that behavior behind an explicit admin-only path; otherwise always restrict by tenant to enforce isolation.
- **server/pace-control.ts:140** — getPaceSnapshot() queries heartbeat_logs across all tenants (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... ) with no tenant_id filter. This produces cross-tenant aggregates and may leak tenant-specific activity data or cause cross-tenant enforcement issues.
  - _Fix:_ Require a tenant parameter for snapshots and add WHERE tenant_id = ${tenantId} to the queries. If a multi-tenant operator view is needed, implement a dedicated admin endpoint that explicitly requests cross-tenant aggregates and enforce admin authorization before running unscoped queries.
- **server/orchestrator-ledger.ts:444** — Raw SQL injection point: pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint) uses sql.raw() with a value (lockKey) derived from ledgerKey and tenantId. ledgerKey is caller-supplied in LedgerRunOptions and therefore can be request-derived, making this use of sql.raw() an injection and tenant-isolation risk.
  - _Fix:_ Avoid sql.raw() with request-derived strings. Compute a numeric lock value server-side and pass it as a parameter so it is bound safely (e.g. derive a BigInt and use parameter binding: tx.execute(sql`SELECT pg_advisory_xact_lock(${BigIntValue}::bigint)`), or use the driver API with parameter placeholders). Validate/sanitize ledgerKey inputs and keep advisory-lock arguments parameterized rather than injected as raw SQL text.
- **server/personality-files.ts:6** — DDL creates personality_files.tenant_id with a DEFAULT (DEFAULT 1). This allows inserts that do not explicitly supply tenant_id (implicitly assigning ADMIN), violating the invariant that tenant_id must be supplied explicitly (NOT NULL with no default).
  - _Fix:_ Remove the DEFAULT on tenant_id and ensure tenant_id is NOT NULL with no default. Add a migration to backfill any existing rows with a correct tenant_id, and update callers so every INSERT/UPSERT always passes tenantId explicitly (no implicit admin fallback).
- **server/plan-executor.ts:775** — resumeStuckPlans runs UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id without any tenant_id constraint — this globally modifies plans across all tenants.
  - _Fix:_ Either limit this recovery to an explicit ADMIN/maintenance tenant context (and document it), or iterate per-tenant and apply the UPDATE with WHERE tenant_id = ${tenantId} so each tenant's plans are only touched by an authorized actor. Make the tenant scope explicit in the code and add logging of the tenant(s) affected.
- **server/plan-executor.ts:784** — resumeStuckPlans selects pending plans with SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 without tenant scoping, potentially reading other tenants' plans to schedule execution.
  - _Fix:_ Scope this selection by tenant (e.g. WHERE status = 'approved' AND tenant_id = ${tenantId}) or require an explicit admin/maintenance context. If this function should act globally, ensure it runs under a verified ADMIN tenant and document/guard that behavior; otherwise iterate tenants and select per-tenant.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table 'agent_knowledge' executed without any tenant_id filter (global scan). This reads rows across all tenants without an explicit tenant constraint.
  - _Fix:_ If a global/system scan is intended, make the intent explicit and ensure downstream code enforces per-row tenant isolation before any side-effects. Otherwise, add a WHERE tenant_id = ${tenantId} clause (or reuse listScheduledMessages with an explicit tenantId) so the query only returns rows for the caller's tenant.
- **server/quarterly-intelligence.ts:344** — CREATE TABLE model_registry_updates defines tenant_id INTEGER NOT NULL DEFAULT 1. A default tenant_id allows INSERTs to omit an explicit tenant and unintentionally attribute changes to the default tenant.
  - _Fix:_ Remove the DEFAULT value so tenant_id must be provided explicitly (e.g. tenant_id INTEGER NOT NULL). Enforce callers always pass tenantId in INSERTs and avoid any schema defaults that mask missing tenant context.
- **server/research-engine.ts:390** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' ... has no tenant_id constraint — will delete autoresearch knowledge across all tenants.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = ${tenantId}) or otherwise restrict the deletion to the session/tenant context. Ensure the tenantId in startResearchSession (or an explicit param) is used to scope deletes.
- **server/research-engine.ts:470** — INSERT into project_notes does not set tenant_id. project_notes appears tenant-scoped, so missing tenant_id allows cross-tenant writes or orphan rows tied only by project_id.
  - _Fix:_ Add tenant_id to the INSERT column list and pass the validated tenant id (e.g. ${session.tenantId} or the project's tenant). Also ensure project ownership was validated (assertProjectInTenant) before inserting.
- **server/research-engine.ts:478** — Second INSERT into project_notes (for each finding) omits tenant_id, allowing project notes to be written without tenant scoping.
  - _Fix:_ Same fix as above: include tenant_id in the INSERT and supply the validated tenant (e.g. ${session.tenantId}). Keep the prior assertProjectInTenant check to ensure correct ownership.
- **server/routes.ts:2312** — SELECT from delivery_logs in /api/store/order/:sessionId uses only orderId/stripePaymentId matching (capability) and does not include any tenant_id constraint in SQL. This reads tenant-scoped rows with no tenant-scoped WHERE clause.
  - _Fix:_ If this endpoint relies on the sessionId as a true capability token, explicitly enforce that the returned delivery row belongs to the expected tenant before returning tenant-identifying fields. Otherwise, add tenant scoping to the query (e.g. .where(and(eq(deliveryLogs.orderId, ${sessionId}), eq(deliveryLogs.tenantId, ${tenantId}))) or gate the endpoint to a context that guarantees the tenant.) Avoid raw/unguarded selects across delivery_logs.
- **server/routes.ts:2420** — Async background lookup in setImmediate (store order-recovery) queries delivery_logs by lower(customer_email) with no tenant_id filter, selecting from a tenant-scoped table without a tenant constraint.
  - _Fix:_ Scope this lookup to the correct tenant (e.g. include eq(deliveryLogs.tenantId, ${tenantId})) or re-design so the code does not perform unscoped searches across tenant data. If cross-tenant search is required for business reasons, restrict it to a platform-admin-only flow and ensure any returned data is minimized and safe (do not leak tenant identifiers or PII).
- **server/routes.ts:2564** — /api/store/verify-orders uses db.select().from(deliveryLogs).where(lower(customerEmail) = ${rawEmail}) and later returns rows; the SELECT/DELETE operate on delivery_logs (tenant-scoped) without a tenant_id constraint in SQL.
  - _Fix:_ Constrain the DELETE/SELECT to the appropriate tenant (e.g. include eq(deliveryLogs.tenantId, ${tenantId})) or otherwise ensure the verify flow cannot be used to enumerate or retrieve cross-tenant data. If this is intended as a global capability tied to the emailed code, validate and bind the code to a tenant at creation and require that binding in the verification SQL.
- **server/routes.ts:3436** — Handler POST /api/messages/:id/feedback executes a SELECT from messages WHERE id = ${messageId} with NO tenant_id clause. A tenant-scoped table (messages) is queried without SQL-level tenant scoping.
  - _Fix:_ Add SQL-level tenant scoping to the SELECT (e.g. .where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId)))) so the DB only returns rows belonging to the caller's tenant. Do not rely solely on application-layer post-hoc checks — include the tenant filter in the WHERE clause.
- **server/routes.ts:4324** — SELECT from tenant-scoped table 'conversations' without constraining by tenant_id. The query: SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL — lacks AND tenant_id = ${tenantId}. This allows reading project_id for an arbitrary conversation id across tenants.
  - _Fix:_ Restrict the SELECT to the caller's tenant. For example: SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL. Better: reuse the already-loaded conv object (conv.projectId) or call a storage helper that enforces tenant scoping/ownership before using the project id.
- **server/routes/admin-zombie-detector.ts:38** — Query reads agent_trace_spans without a tenant_id constraint. The SQL in the transaction aggregates agent_trace_spans (used AS ...) but does not filter by tenant_id, allowing cross-tenant data exposure.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId} or use Drizzle .where(eq(table.tenantId, tenantId))). If the intent is to run a cross-tenant platform-level audit, make that explicit (require requirePlatformAdmin) and centralize/justify the cross-tenant access path.
- **server/routes/admin-zombie-detector.ts:54** — Query aggregates conversations across all tenants without tenant_id filtering. The WITH used AS (...) selects from conversations (GROUP BY persona_id) but omits tenant_id, exposing per-tenant conversation activity globally.
  - _Fix:_ Add a tenant filter inside the WITH clause (e.g. WHERE tenant_id = ${tenantId}) or use a Drizzle eq(...) filter on conversations to ensure only the caller's tenant data is read. If a global cross-tenant scan is required, restrict it to an auditable platform-admin-only flow and document the justification.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This can mutate a conversation belonging to another tenant.
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or use the storage layer helper that enforces tenant ownership.
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Fix:_ Include tenant_id in the INSERT columns and values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})) or use storage/ORM helpers that set tenant_id.
- **server/routes/conversations.ts:228** — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) or call a storage API that enforces tenant assignment.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table 'inbox_messages' lacks tenant_id constraint (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}).
  - _Fix:_ Restrict the UPDATE by tenant as well (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) to prevent modifying messages in other tenants.
- **server/routes/projects.ts:185** — INSERT into project_notes omits tenant_id (INSERT INTO project_notes (project_id, note, author) ...). If project_notes is tenant-scoped this allows inserting rows not explicitly tied to the caller's tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${id}, ...)) or derive tenant_id in the same statement from a verified project row. Ensure the tenant_id value is the resolved tenant from getTenantFromRequest, not any client-supplied value.
- **server/routes/projects.ts:224** — INSERT into project_files (base64 upload handler) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...). If project_files is tenant-scoped this will create rows without an explicit tenant association.
  - _Fix:_ Add tenant_id into the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ...)) or otherwise ensure tenant_id is set from the verified tenant before persisting.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — the UPDATE lacks a tenant predicate. Without a WHERE tenant_id = ${tenantId} this could update a project from another tenant if projectId is misused.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or perform the update via a join that enforces the project belongs to the resolved tenant.
- **server/routes/projects.ts:292** — INSERT into project_files (multipart upload handler) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...). Same class of issue as other file inserts.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${projectId}, ...)) or otherwise ensure tenant_id comes from the verified tenant context.
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — this update in the multipart upload flow lacks a tenant constraint, allowing cross-tenant updates if misused.
  - _Fix:_ Add tenant scoping to the WHERE clause (WHERE id = ${projectId} AND tenant_id = ${tenantId}) or update via a tenant-verified join.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ... executed after a separate projCheck but the SELECT itself has no tenant_id constraint. Per the tenant-isolation rule every read against a tenant-scoped table must include an explicit tenant predicate (or equivalent Drizzle eq).
  - _Fix:_ Either include tenant_id in the WHERE (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}), or perform the SELECT via a JOIN to projects with an explicit WHERE p.tenant_id = ${tenantId} to ensure the DB-level predicate is present in every query.
- **server/routes/projects.ts:342** — SELECT file_name,... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 lacks an explicit tenant predicate. Although project ownership is validated earlier, this query itself does not constrain by tenant_id.
  - _Fix:_ Add an explicit tenant constraint (e.g. AND tenant_id = ${tenantId}) or rewrite to join projects in the same statement (ensuring WHERE p.tenant_id = ${tenantId}) so the DB enforces tenant scoping on this read.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} has no tenant predicate. Without tenant scoping the DELETE could remove rows belonging to other tenants if project_id validation is bypassed or incorrect.
  - _Fix:_ Constrain the DELETE by tenant (for example DELETE FROM project_files USING projects p WHERE project_files.id = ${fileId} AND project_files.project_id = ${projectId} AND p.id = project_files.project_id AND p.tenant_id = ${tenantId}), or add tenant_id to project_files and use WHERE tenant_id = ${tenantId} in the DELETE.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id constraint (reads tenant-scoped table across all tenants).
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}). If the intent is per-tenant logic, pass in the tenantId and use it in the WHERE clause or use Drizzle's .where(eq(research_sessions.tenantId, tenantId)).
- **server/seed.ts:2855** — SELECT id, name, model FROM research_programs WHERE is_active = true is run with no tenant_id constraint (reads tenant-scoped table across all tenants).
  - _Fix:_ Add an explicit tenant filter (WHERE tenant_id = ${tenantId}) or otherwise scope this maintenance query to an admin-approved tenant context. Avoid scanning tenant-scoped rows across tenants without an explicit tenant constraint.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... runs against conversations with no tenant_id constraint (reads tenant-scoped rows across tenants).
  - _Fix:_ Scope the SELECT to a tenant (WHERE c.tenant_id = ${tenantId}) or otherwise ensure this maintenance query is executed in an appropriate tenant context. Avoid cross-tenant reads without explicit tenant filters.
- **server/seed.ts:3137** — UPDATE against tenant-scoped table `projects` performed without constraining by tenant_id (no WHERE tenant_id = ...).
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND primary_conversation_id IS NULL), or perform the update by selecting the project row joined to projects.owner/tenant and validating ownership before updating.
- **server/seed.ts:3205** — SELECT from tenant-scoped table `conversations` (and the correlated NOT EXISTS on `project_conversations`) is executed with no tenant_id constraint, allowing cross-tenant discovery of conversation→project links.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or join through the projects table and ensure both the conversation and project belong to the intended tenant before acting. Never read tenant-scoped rows without validating tenant ownership.
- **server/seed.ts:3213** — INSERT into `project_conversations` uses project_id and conversation_id derived from an unfiltered query (above), potentially creating links across tenants; `project_conversations` is not tenant-tagged in the seed and the source ids were not tenant-validated.
  - _Fix:_ Ensure the source rows are tenant-validated before inserting (filter the SELECT by tenant_id or join projects to confirm tenant). Consider adding a tenant_id column to project_conversations and set it explicitly on INSERT, or otherwise enforce ownership checks (SELECT project.tenant_id = conversation.tenant_id) prior to inserting.
- **server/seed.ts:4460** — UPDATE on tenant-scoped table `memory_entries` is executed without any tenant_id filter (UPDATE memory_entries SET ... WHERE ...), which will modify memory rows across all tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. AND tenant_id = ${tenantId}) or process per-tenant. If this is intended to be tenant-agnostic maintenance, run it per-tenant with explicit tenant context; otherwise add WHERE tenant_id = ${tenantId} to prevent cross-tenant data modifications.
- **server/self-improvement.ts:165** — detectStagnation builds and executes a SELECT on the experiments table without guaranteeing a tenant_id constraint when tenantId is omitted — the query will scan all tenants if tenantId is not provided.
  - _Fix:_ Require caller tenantId (make it mandatory) or fail-closed when tenantId is not supplied. Always include eq(experiments.tenantId, tenantId) in the WHERE clause (or throw) so the query cannot return rows from other tenants.
- **server/storage.ts:239** — Unscoped SELECT against tenant-scoped table `conversations` (getConversationUnscoped reads by id with no tenant_id filter).
  - _Fix:_ Avoid unrestricted reads on tenant-scoped tables. Either require caller-supplied tenantId and use eq(conversations.tenantId, tenantId), or keep this helper private and force all callsites to call a tenant-scoped getter after deriving/validating tenant. If an unscoped read is truly required, make the risk explicit in the API and audit every callsite that uses the returned row to derive/validate tenant ownership.
- **server/storage.ts:248** — INSERT into tenant-scoped table `conversations` without enforcing tenant_id (createConversation allows tenantId?: number and inserts raw data).
  - _Fix:_ Require and validate tenantId before inserting (e.g. assertValidTenantId(data.tenantId)), or refuse to insert without an explicit tenantId. Use parameterized Drizzle insert with tenantId guaranteed non-null.
- **server/storage.ts:289** — SELECT from tenant-scoped table `messages` without a tenant_id constraint in attachCitationMetadata (raw SQL SELECT id,citations FROM messages ... ).
  - _Fix:_ Scope the select by tenant (pass tenantId into this helper or JOIN via conversation → tenant) so the lookup only returns messages owned by the caller's tenant. Avoid unscoped raw SQL against tenant-scoped tables.
- **server/storage.ts:390** — UPDATE on tenant-scoped table `messages` without tenant_id constraint in attachCitations (UPDATE messages SET citations = ... WHERE id = ...).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or use Drizzle's update with an explicit eq(messages.tenantId, tenantId). Validate the caller owns the message before mutating.
- **server/storage.ts:796** — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint (updateMemoryEmbedding updates by id only).
  - _Fix:_ Require tenantId (or validate ownership) and include eq(memory_entries.tenantId, tenantId) in the WHERE clause when updating embedding. Alternatively, fetch the row with tenant scoping first and then update.
- **server/storage.ts:808** — UPDATE on tenant-scoped table `agent_knowledge` without tenant_id constraint (updateKnowledgeEmbedding updates by id only).
  - _Fix:_ Require tenantId or validate ownership; include eq(agent_knowledge.tenantId, tenantId) in the WHERE clause to prevent cross-tenant embedding writes.
- **server/storage.ts:615** — UPDATE on tenant-scoped table `conversation_facts` without tenant_id constraint (evictOldestConversationFacts updates by id list only).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(...) AND tenant_id = ${tenantId}) or perform the update using the same tenant-scoped predicate that produced the victim ids to avoid accidental cross-tenant mutation.
- **server/storage.ts:928** — SELECT from tenant-scoped table `heartbeat_tasks` with no tenant constraint (getHeartbeatTask selects by id only).
  - _Fix:_ If this function is intended to be tenant-scoped, accept tenantId and add eq(heartbeat_tasks.tenantId, tenantId). If it must be unscoped for internal scheduling, make that explicit and audit/limit callsites.
- **server/storage.ts:985** — SELECT from tenant-scoped table `heartbeat_tasks` with no tenant constraint (getDueHeartbeatTasks queries all enabled/approved tasks across tenants).
  - _Fix:_ Scheduler code that iterates per-tenant should either include tenantId in the query or explicitly document that it is performing cross-tenant work under a trusted background context. Prefer returning tenant_id with each row and making any downstream actions tenant-aware and audited.
- **server/storage.ts:1013** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant scoping in claimHeartbeatTasks (UPDATE ... WHERE id = ${id} ... no tenant_id check).
  - _Fix:_ Add tenantId scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or ensure the claim loop fetches tasks with tenant_id and uses that when updating to avoid claiming tasks belonging to other tenants.
- **server/storage.ts:868** — SELECTs against tenant-scoped tables can be performed unscoped in getMemoryStats when tenantId is omitted (selects all memoryEntries / agentKnowledge if tenantId undefined).
  - _Fix:_ Require tenantId or explicit admin flag; avoid returning platform-wide tenant-scoped rows by default. If optional tenantId is necessary, fail closed (throw) when tenantId is not provided for non-admin callers.
- **server/storage.ts:506** — Potentially unscoped SELECT against tenant-scoped table `memory_entries` in getMemoryEntry: when tenantId is undefined the query selects by id only (caller warned but the read still happens unscoped).
  - _Fix:_ Require tenantId for tenant-scoped reads (fail-closed) or explicitly expose and document an audited unscoped getter. Prefer always scoping with eq(memory_entries.tenantId, tenantId) for HTTP/handler callsites.
- **server/storage.ts:516** — INSERT into tenant-scoped table `memory_entries` without validating/forcing tenant_id (createMemoryEntry inserts provided data directly).
  - _Fix:_ Require and validate data.tenantId via assertValidTenantId(data.tenantId) before insert. Do not accept inserts that leave tenant_id NULL.
- **server/storage.ts:542** — UPDATE on tenant-scoped table `memory_entries` in deleteMemoryEntry can run unscoped when tenantId is not supplied (tenantId optional; function warns but proceeds).
  - _Fix:_ Require tenantId or include tenant scoping in the WHERE clause. Fail closed when tenantId is missing for mutation operations on tenant-scoped tables.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table `felix_proposals` without tenant filter in stampArgsEmbedding (UPDATE ... WHERE id = ${proposalId} ... no tenant_id constraint).
  - _Fix:_ Include tenant scoping when updating proposal rows (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), or accept tenantId as a parameter to the stamping function so writes cannot affect other tenants' rows.
- **server/tools.ts:6893** — INSERT into tenant-scoped table 'project_files' does not include tenant_id in the columns/VALUES. The INSERT uses project_id and other fields but omits tenant_id even though tenant isolation is enforced only at the app layer and tenant_id columns are NOT NULL with no default.
  - _Fix:_ Include the tenant id explicitly in the INSERT (e.g. add tenant_id to the column list and ${params._tenantId} to the VALUES). Prefer using Drizzle's insert helpers with the tenantId property (or sql/eq helpers) so all tenant-scoped writes always set tenant_id. Also keep the prior ownership check (assertProjectInTenant) but do NOT rely on it alone — always persist tenant_id with the row to maintain isolation and future queries' correctness.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files does not include tenant_id. The code inserts a row referencing project_id but does not set tenant_id on the INSERT; if project_files is tenant-scoped this violates the invariant that every INSERT must set tenant_id explicitly.
  - _Fix:_ Include the tenant_id column in the INSERT and pass the caller's tenant (e.g. VALUES (${projId}, ${fileName}, ${absPath}, ${driveUrl || null}, ${fileType}, ${stat.size}, ${"agent"}, ${tenantId}) or explicitly list tenant_id in the INSERT columns). Alternatively, derive/validate tenant from the project and set tenant_id = tenantId to ensure the new row is scoped to the caller's tenant.
- **server/tools/domains/files/handlers.ts:838** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} executes without constraining the update to the caller's tenant (no WHERE tenant_id = ...). Writing to a tenant-scoped table without a tenant constraint can modify another tenant's row if IDs collide or were previously validated separately.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}), or use a Drizzle .where(eq(projects.tenantId, tenantId)) so the write is explicitly constrained to the caller's tenant. If projId was derived from a prior tenant-scoped query, still include the tenant filter as defense-in-depth.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} executes without an explicit tenant filter. Although workflow was fetched earlier with a tenant WHERE, this UPDATE writes to a tenant-scoped table without verifying tenant_id in the WHERE clause, violating the invariant that every write must be constrained by tenant_id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}) or use the Drizzle .where(eq(browserWorkflows.tenantId, tenantId)). Even if workflow was previously fetched, include the tenant predicate on subsequent writes as defense-in-depth.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE against tenant-scoped table knowledge_triples omits tenant filter. The loop updates rows with `WHERE id = ${row.id}` but does not constrain by tenant_id, allowing a row id to be updated across tenants.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) or use Drizzle's .where(eq(table.tenantId, tenantId)) so every update is explicitly scoped to the caller's tenant. Ensure all other DML statements similarly include tenant_id.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys executed without constraining by tenant_id (reads tenant-scoped provider_keys table with no tenant filter).
  - _Fix:_ If provider_keys is tenant-scoped, add an application-level WHERE tenant_id = ${tenantId} (or parameterize accordingly) to restrict results to the caller's tenant. If the intention is an admin-wide audit, make that explicit (guard by an admin-only check) and document/confirm the table is global or that admin access to all rows is acceptable.
- **server/tools/domains/security/handlers.ts:260** — COUNT(DISTINCT provider) query against provider_keys runs without a tenant_id filter (potential cross-tenant read of provider keys).
  - _Fix:_ Restrict the query to the caller's tenant via WHERE tenant_id = ${tenantId} (or otherwise ensure the table is truly global and that admin-only access is enforced and audited). If an admin-wide aggregate is required, ensure it's explicitly gated to admin contexts and reviewed for data-leak risk.
- **server/venture-discovery/budget.ts:155** — UPDATE on tenant-scoped table `agent_cost_ledger` updates by id only (WHERE id = ${reservationId}) with no tenant_id constraint in the WHERE clause (settleVentureReservation). This can modify rows belonging to other tenants if reservationId is mis-bound.
  - _Fix:_ Include tenant scope in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) and pass tenantId into settleVentureReservation so the DB enforces tenant ownership. Validate reservation rows belong to the expected tenant before updating.
- **server/venture-discovery/budget.ts:169** — UPDATE on tenant-scoped table `agent_cost_ledger` releases a reservation by id only (WHERE id = ${reservationId}) with no tenant_id constraint (releaseVentureReservation).
  - _Fix:_ Require and check tenantId when releasing reservations. Update using WHERE id = ${reservationId} AND tenant_id = ${tenantId} (propagate tenantId into the function), or verify the reservation row belongs to the expected tenant before changing it.
- **server/video-job-runner.ts:178** — Upsert (INSERT ... ON CONFLICT DO UPDATE) into tenant-scoped table `video_jobs` uses an ON CONFLICT DO UPDATE with a WHERE clause that only compares updatedAt (where: lt(videoJobs.updatedAt, row.updatedAt)) but does not constrain by tenant_id — the UPDATE part can therefore affect rows without an explicit tenant check.
  - _Fix:_ Ensure the ON CONFLICT DO UPDATE only updates rows for the same tenant. Add tenant-scoping to the where clause (e.g. where: and(eq(videoJobs.tenantId, row.tenantId), lt(videoJobs.updatedAt, row.updatedAt))). Alternatively ensure jobId is globally namespaced per-tenant or validate existing row.tenantId == row.tenantId before performing the UPDATE.
- **server/video-job-runner.ts:226** — UPDATE across `video_jobs` marks stale jobs failed using only updatedAt/status predicates (WHERE status IN (...) AND updatedAt < staleCutoff) with no tenant_id constraint (recoverStaleVideoJobs). This will update jobs across all tenants.
  - _Fix:_ Scope the stale-recovery update to tenants as appropriate (e.g. run per-tenant or include tenant_id predicate). If the sweep must operate globally, ensure it is an intentional, well-documented operator action; otherwise include tenant scoping or run separate per-tenant recovery with explicit tenant context.
- **server/wiring-invariants.ts:535** — Raw SQL counts memory tables without tenant filter: `SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}` — memory tables (messages, memory_entries, agent_knowledge, etc.) are likely tenant-scoped and this reads across all tenants.
  - _Fix:_ Replace this with per-tenant queries (WHERE tenant_id = ${tenantId}) executed in an admin-controlled loop, or otherwise ensure the intent is an admin-global scan and restrict the call to admin-only bootstrap checks. Validate that tbl.name is from a trusted constant whitelist before interpolating into sql.raw to avoid injection risks.
- **server/wiring-invariants.ts:561** — Unscoped SELECT COUNT(*) FROM graph_memory (tenant-scoped memory store) with no tenant_id constraint: SELECT COUNT(*)::int AS cnt FROM graph_memory
  - _Fix:_ Scope the read to a tenant (WHERE tenant_id = ${tenantId}) or iterate tenants and compute per-tenant counts. If a global check is intended, document and lock this operation to explicit admin context; avoid blanket unfiltered reads of tenant data.

### MEDIUM (38)

- **server/agentic-features.ts:600** — enrollInSequence checks for existing enrollments with SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant_id, causing the uniqueness check to consider enrollments across tenants.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the SELECT so duplicate checks are tenant-scoped, preventing cross-tenant interference.
- **server/auth.ts:1036** — DELETE FROM password_reset_tokens WHERE expires_at < ... deletes across tenants (no tenant_id). This maintenance delete can affect rows for all tenants without an explicit tenant-bound guard.
  - _Fix:_ If intended as a global admin cleanup, ensure the code runs under a controlled admin path and is auditable; otherwise, run per-tenant cleanup (DELETE ... WHERE tenant_id = X AND expires_at < ...) or add an explicit tenant filter/guard before running global deletions.
- **server/auth.ts:1150** — SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions selects all rows from a tenant-scoped table without a tenant_id filter and loads sessions into process cache for all tenants.
  - _Fix:_ Restrict this load to a specific tenant (or only admin session rows) or run under a tightly-scoped admin job that is explicitly authorized/audited. Alternatively, paginate/partition by tenant_id and ensure this operation is intended and safe for cross-tenant data access.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... reads conversations across all tenants (no WHERE tenant_id = ...).
  - _Fix:_ If backfill is intended to operate per-tenant, iterate per tenant and include WHERE c.tenant_id = ${tenantId}. If a cross-tenant admin job is intended, ensure it runs only with platform-admin privileges and is auditable.
- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} lacks an explicit tenant_id constraint. While conversation_id may be unique, the query does not re-assert the conversation's tenant ownership here.
  - _Fix:_ Add an explicit tenant check (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}) or ensure this code only runs after verifying the conversation belongs to the intended tenant to prevent accidental cross-tenant reads.
- **server/auto-memorize.ts:47** — SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ... does not include tenant_id. Watermark reads/writes mix tenant contexts (writes later use tenant_id=1).
  - _Fix:_ Scope watermark reads to the intended tenant (e.g. WHERE tenant_id = ${tenantId} AND ...), or document and enforce that the watermark is intentionally on a single admin tenant. Avoid implicit cross-tenant reads.
- **server/auto-memorize.ts:100** — SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ORDER BY id DESC LIMIT 200 reads memory_entries across tenants (no tenant_id filter) when checking for duplicates.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or pass tenant context into isDuplicate so dedup only compares within the tenant's data.
- **server/auto-consolidation.ts:335** — SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL queries tenant-scoped memory_entries without constraining tenant_id (it intentionally enumerates tenants), which is a cross-tenant read.
  - _Fix:_ If the intent is to discover tenants to run consolidation for, use a vetted tenant-listing API/table and ensure the consolidation runner runs under an audited admin process. Otherwise, perform per-tenant queries scoped by tenant_id to avoid inadvertent cross-tenant access.
- **server/auto-transcript.ts:128** — Backfill SELECT reads conversations (and uses a subquery counting messages) without constraining by tenant_id. This query returns rows across all tenants and the nested messages subquery also lacks a tenant filter, which risks cross-tenant exposure/processing.
  - _Fix:_ Scope the query to a tenant (or make it explicit admin-only). Add tenant scoping to the messages subquery or join so rows are filtered by tenant_id, e.g. (SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) > 0, or add WHERE c.tenant_id = ${tenantId} if the backfill should run per-tenant. If this is an admin/global job, require an explicit admin context and make that obvious in code/comments.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' is executed without a tenant_id constraint (scanAndEscalate reads all commitments across tenants).
  - _Fix:_ If this is intended only for platform-admin use, make the admin authorization explicit at the call site and avoid returning tenant-sensitive columns, or add an explicit tenant_id filter when called in tenant-contexts. Otherwise, scope the SELECT with WHERE tenant_id = $tenantId (or pass a list of allowed tenant ids) to enforce tenant isolation at the application layer.
- **server/data-protection.ts:144** — verifyMessageSaved reads messages by conversation_id (SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} ...) but the function has no tenantId parameter and does not verify the caller's tenant owns the conversation, so the read is not explicitly tenant-scoped.
  - _Fix:_ Add a tenantId parameter and include tenant_id = ${tenantId} in the WHERE clause, or verify ownership of conversationId before reading messages. If conversation ids are globally unique and verified elsewhere, make that explicit in code/comments and perform an ownership check to satisfy the tenant-isolation invariant.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal built from a request-derived embedding (vec from generateEmbedding(query)). Even though vecLiteral does shape validation, interpolating user/provider-derived content into sql.raw risks SQL-injection or other malformed-literal issues.
  - _Fix:_ Avoid string-interpolating vector literals into sql.raw. Use parameterized binding where possible (e.g. pass the vector as a parameter and cast in SQL: embedding_vec <=> $1::vector) or use Drizzle/pg driver parameter placeholders. If raw is unavoidable, ensure strict validation and document the trust boundary; prefer storing vectors as pgvector parameters rather than injecting literal SQL.
- **server/embeddings.ts:252** — Use of sql.raw() with an interpolated vector literal (same vec) in ORDER BY — vector originated from request/LLM output.
  - _Fix:_ As above: replace string interpolation with a parameterized query (pass the vector as a parameter and cast it in SQL), or use Drizzle bindings so user/provider-derived values are not embedded into raw SQL strings.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated vector literal (vec from generateEmbedding(query)) in vectorSearchKnowledge — raw interpolation of a request-derived value.
  - _Fix:_ Parameterize the vector rather than embedding it into sql.raw. Use prepared/parameterized statements (e.g. $1::vector) or Drizzle parameter binding to avoid raw string interpolation of untrusted input.
- **server/embeddings.ts:565** — Use of sql.raw() with an interpolated vector literal (vec) in ORDER BY — the vector originates from user input/LLM output.
  - _Fix:_ Use parameter binding for the vector and avoid constructing SQL via string interpolation. Ensure embeddings are validated and passed as query parameters to the DB.
- **server/embeddings.ts:726** — (Also) sql.raw(`'${vec}'::vector`) used when updating memory_entries.embedding_vec — vec is derived from DB or provider data and is interpolated into raw SQL.
  - _Fix:_ Use parameterized casting (e.g. SET embedding_vec = $1::vector) and pass the vector as a parameter, or use Drizzle parameter binding; avoid injecting the literal into sql.raw even after validation.
- **server/embeddings.ts:743** — (Also) sql.raw(`'${vec}'::vector`) used when updating agent_knowledge.embedding_vec — raw interpolation of a derived vector literal.
  - _Fix:_ As above: use parameterized query placeholders for the vector or Drizzle's parameter binding to prevent constructing SQL with embedded literal content from external sources.
- **server/job-queue.ts:220** — failJob first SELECTs max_attempts FROM agent_jobs WHERE id = ${id} without any tenant_id constraint, exposing metadata about jobs from other tenants and enabling subsequent decisions to be made without tenant-scoped verification.
  - _Fix:_ SELECT max_attempts using a tenant-scoped query (WHERE id = ${id} AND tenant_id = ${tenantId}) or otherwise fetch and verify tenant ownership before using the row's values.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs by id without tenant_id filtering, allowing cancellation of jobs belonging to other tenants if job id is known or misused.
  - _Fix:_ Require tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status IN (...)) and make cancelJob accept/verify tenant context.
- **server/job-queue.ts:283** — retryJob resets a job to pending by id without scoping by tenant_id. This can re-enable jobs from other tenants unintentionally.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause and require tenant verification when retrying jobs (or restrict retry operations to admin-only code paths with explicit tenant checks).
- **server/job-queue.ts:320** — listJobs issues a SELECT FROM agent_jobs with an optional whereClause. When tenantId is not provided, the query returns rows across all tenants (no tenant scoping enforced by default).
  - _Fix:_ Make tenantId a required parameter for non-admin callers, or ensure the default behavior scopes by the caller's tenant (add WHERE tenant_id = ${tenantId} when caller lacks elevated privileges). Avoid returning multi-tenant job data to tenant-scoped callers.
- **server/job-queue.ts:385** — getJobStats aggregates counts from agent_jobs over the last 7 days without constraining by tenant_id, exposing cross-tenant job statistics and breaking tenant isolation.
  - _Fix:_ Provide a tenant-scoped variant (WHERE tenant_id = ${tenantId}) or require an explicit tenantId parameter; if a global operator view is needed, restrict to admin-only paths with explicit authorization and auditing.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor queries agent_knowledge grouped by tenant_id/persona_id across the whole table (no tenant-level restriction) and then iterates over all tenant/persona pairs. This reads tenant-scoped data across all tenants without per-call tenant scoping.
  - _Fix:_ If intended to run multi-tenant diagnostics, make this an admin-only background job with explicit authorization and audit trail; otherwise, provide a tenant-scoped path that accepts tenantId and only scans that tenant's data (add WHERE tenant_id = ${tenantId}). Ensure any handler that mutates or emits notifications includes tenant checks.
- **server/lib/action-ledger.ts:266** — Tenant-scoped table `action_attempts` is read without any tenant_id predicate (listUnknownAttemptsAllTenants performs a cross-tenant SELECT).
  - _Fix:_ If this is intended, ensure the callsite is tightly access-controlled (platform-maintenance/reconciler only). Otherwise scope the SELECT to a specific tenant (WHERE tenant_id = $1) or expose an admin-only API and document the trusted caller requirements.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT id FROM project_files WHERE project_id = ${newProjectId} ... does not include an explicit tenant_id constraint. Reads against a tenant-scoped table must be scoped to the caller tenant.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the query is constrained to the intended tenant. Do not rely solely on project_id unless the application verifies project→tenant ownership above the DB call.
- **server/lib/ideabrowser-backfill.ts:116** — SELECT id FROM project_notes WHERE project_id = ${newProjectId} AND note = ${n.note} LIMIT 1 lacks an explicit tenant_id constraint. Reads against tenant-scoped tables must be scoped to the caller tenant.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent) to the WHERE clause, or otherwise validate that project_id belongs to the caller tenant before running the query.
- **server/research-engine.ts:955** — SELECT * FROM research_sessions WHERE id = ${sessionId} is executed without a tenant_id constraint when getResearchSessionStatus is called without tenantId. This can return a session belonging to another tenant to callers that omit tenantId.
  - _Fix:_ Require and use caller tenantId for this query (e.g. always include AND tenant_id = ${tenantId}), or restrict the no-tenant branch to authorized admin paths only. Prefer requiring tenantId or validating caller privileges before returning tenant-scoped rows.
- **server/routes.ts:1530** — Multiple SELECTs in /api/public/stats query tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) without any WHERE tenant_id = ... constraint.
  - _Fix:_ Either restrict this endpoint to an admin only route or add explicit tenant scoping to each query (e.g. .where(eq(table.tenantId, tenantId)) or sql`WHERE tenant_id = ${tenantId}`) so counts cannot leak cross-tenant data.
- **server/routes.ts:1558** — Raw db.execute(...) SELECTs in /api/public/architecture perform global COUNT queries against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, etc.) with no tenant_id filter.
  - _Fix:_ Limit these summary queries to an admin-only route OR add explicit tenant filters to each count/select. If the intention is to return per-tenant values, include WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq()) using the caller's tenant; if global platform metrics are intended, gate the route to platform admins.
- **server/routes/conversations.ts:99** — SELECT from tenant-scoped table 'project_conversations' omits tenant_id (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}).
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}) to ensure tenant-scoped lookup.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table 'projects' lacks tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to prevent accidental cross-tenant mutation.
- **server/routes/conversations.ts:208** — SELECT reads tenant-scoped table 'conversations' without tenant predicate (SELECT c.project_id FROM conversations c JOIN projects p ... WHERE c.id = ${id} ...).
  - _Fix:_ Add an explicit conversation tenant check (e.g. AND c.tenant_id = ${tenantId}) in the WHERE clause, even if ownership was previously validated in application logic.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table 'agent_jobs' is executed without a tenant_id filter (SELECT ... FROM agent_jobs j ... WHERE j.status IN ('pending','running') ...).
  - _Fix:_ If the intent is per-tenant listing, add a tenant predicate (e.g. WHERE j.tenant_id = ${tenantId} AND ...). If a global admin view is intended, make the global intent explicit and ensure the route is strictly platform-admin gated and logged/audited.
- **server/storage.ts:300** — Reads the `conversations` row by conversationId without a tenant constraint in createMessage (conversation lookup uses id only). This reads a foreign row derived from caller input without verifying caller tenant owns it.
  - _Fix:_ Either require the caller to provide a validated tenantId and use getConversation(id, tenantId), or explicitly call a single audited unscoped function (e.g. getConversationUnscoped) and then enforce/derive and validate the tenant before proceeding. Make the ownership derivation explicit and documented.
- **server/storage.ts:549** — UPDATE on tenant-scoped table `memory_entries` in touchMemoryEntries can run unscoped when tenantId is not supplied (tenantId is optional and only warned).
  - _Fix:_ Require tenantId (fail-closed) or ensure the update includes eq(memory_entries.tenantId, tenantId). If callers intentionally omit tenantId, make that explicit and audited.
- **server/video-job-runner.ts:203** — SELECT reads cancelRequested from `video_jobs` by jobId only (WHERE jobId = ${jobId}) without tenant_id constraint (isCancelRequested). This reads a tenant-scoped table without tenant filtering.
  - _Fix:_ Take tenantId as an explicit parameter (or otherwise resolve owner) and add AND tenant_id = ${tenantId} to the WHERE clause so cancel-checks are properly scoped to the caller's tenant. If the function must be tenant-agnostic, document why and make the ownership boundary explicit and safe.
- **server/wiring-invariants.ts:244** — Query against tool_performance uses no tenant filter: SELECT count(*)::int AS n FROM tool_performance WHERE last_success_at > NOW() - INTERVAL '14 days'
  - _Fix:_ If tool_performance is tenant-scoped, add an explicit tenant constraint (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate per-tenant. If this is intended as a global admin aggregate, make that explicit and restrict this code to admin-only context and audit the rationale.
- **server/wiring-invariants.ts:366** — sql.raw used to SELECT counts/MAX timestamp from dynamic subsystem tables without tenant filters: `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}` — reads tenant-scoped subsystem tables across all tenants.
  - _Fix:_ Avoid unscoped reads on tenant-scoped subsystem tables. If per-tenant freshness is required, query each tenant explicitly (WHERE tenant_id = ${tenantId}). If the table is intentionally global, document that and ensure this admin-only code path is protected. Also avoid building raw SQL from variable table/column names without validating them against a whitelist.

### Suppressed — confirmed false positives (152)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() performs an UPDATE on agent_runs without constraining by tenant_id (where clause is eq(agentRuns.id, runId) only). This allows cross-tenant modification of run rows.
  - _Why false positive:_ Server-side worker helper updates agent_runs by server-generated PK runId; shown code has no untrusted request boundary and matches internal run-state transition pattern.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) only — no tenant_id constraint. This can overwrite another tenant's run state.
  - _Why false positive:_ updateRunState is an internal agent run worker function operating on globally unique run id, not caller-scoped tenant input in the shown path.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) only — missing tenant_id constraint.
  - _Why false positive:_ replaceRunState is part of internal run lifecycle management keyed by server-issued run id, matching benign internal-worker-pk usage.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs (status/result/completedAt) using WHERE eq(agentRuns.id, runId) only — no tenant_id constraint.
  - _Why false positive:_ completeRun performs internal status transition on agent_runs by server-generated primary key; no evidence of untrusted cross-tenant id source here.
- **server/agentic/runs.ts:67** _(internal-worker-pk)_ — failRun() updates agent_runs with WHERE eq(agentRuns.id, runId) only — missing tenant_id check allowing cross-tenant status changes.
  - _Why false positive:_ This finding refers to failRun below; it is another internal worker state update by server-side run PK, fitting the internal-worker-pk pattern.
- **server/agentic/runs.ts:76** _(internal-worker-pk)_ — pauseRun() updates agent_runs with WHERE eq(agentRuns.id, runId) only — no tenant_id constraint present.
  - _Why false positive:_ This finding refers to pauseRun below; it updates an agent run by internal run id as part of worker orchestration, not direct tenant request input.
- **server/ceo-orchestrator.ts:821** _(global-table)_ — Unscoped SELECT on tenant-scoped 'personas' table — cachedPersonas is loaded with db.select().from(personasTable) with no tenant_id constraint, so personas from all tenants will be returned and may be used to route or assign work across tenants.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so loading all personas is from a non-tenant-scoped table rather than a cross-tenant leak.
- **server/data-protection.ts:125** _(global-table)_ — permanentlyPurgeSoftDeleted deletes rows from messages, compaction_archives and conversations using only conversation_id (DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}) with no tenant_id constraint. Because the initial SELECT was run across all tenants, these DELETEs can affect multiple tenants' data.
  - _Why false positive:_ Global maintenance purge intentionally scans and deletes expired soft-deleted conversations across all tenants; not user-scoped cross-tenant access.
- **server/embeddings.ts:688** _(global-table)_ — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ Background embedding backfill is a platform-wide maintenance job operating across tenant partitions, not a caller-controlled tenant boundary.
- **server/embeddings.ts:716** _(global-table)_ — SELECT FROM memory_entries in backfillEmbeddingVecs lacks a tenant_id constraint — reads rows for all tenants.
  - _Why false positive:_ Embedding vector backfill is a global maintenance sweep over stored rows, not tenant-mixing via request input.
- **server/embeddings.ts:732** _(global-table)_ — SELECT FROM agent_knowledge in backfillEmbeddingVecs has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ Knowledge embedding backfill is a platform-wide maintenance task rather than a tenant-scoped request path.
- **server/felix-brain.ts:19** _(global-table)_ — SELECT FROM agent_runs in rehydrateTaskStateOnBoot omits tenant_id in WHERE clause and scans running/pending runs across all tenants. This reads tenant-scoped rows without scoping and can expose or mix cross-tenant data into in-memory task state.
  - _Why false positive:_ Boot-time rehydration intentionally reloads in-flight runs across tenants into server memory; internal maintenance, not untrusted tenant access.
- **server/felix-loop.ts:684** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' omits tenant_id in WHERE (UPDATE ... WHERE id = ${runId}). This allows a runId to be used to update a row belonging to another tenant.
  - _Why false positive:_ Update targets a server-created run row by its primary key runId within the same internal worker flow; id is not from an untrusted request boundary.
- **server/felix-loop.ts:690** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' in the error recovery path omits tenant_id in WHERE (UPDATE ... WHERE id = ${runId}).
  - _Why false positive:_ Error-path update uses the same server-generated runId in an internal worker context, not caller-controlled cross-tenant input.
- **server/firecrawl.ts:190** _(upstream-verified)_ — UPDATE of scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) but omits tenant scoping (tenantId). scrapedPages is tenant-scoped and the update is not constrained by tenant_id.
  - _Why false positive:_ Row id being updated comes from an immediately preceding SELECT constrained by url and tenantId, so ownership is established in the same call path.
- **server/firecrawl.ts:316** _(upstream-verified)_ — UPDATE of scrapedPages inside crawl loop uses WHERE eq(scrapedPages.id, existing[0].id) but omits tenant scoping. This is a write to a tenant-scoped table without constraining tenant_id.
  - _Why false positive:_ existing[0].id is obtained from a prior tenant-scoped SELECT on scrapedPages, so the update inherits verified tenant ownership.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on projects (setting driveFolderId/driveFolderUrl) uses WHERE eq(projects.id, projectId) without tenant scoping. projects is tenant-scoped and the UPDATE can affect another tenant's row.
  - _Why false positive:_ Function first loads project by id and explicitly checks project.tenantId === tenantId before updating the same projectId.
- **server/heartbeat.ts:52** _(global-table)_ — SELECT from research_schedules has no tenant_id filter (reads rows for all tenants). research_schedules is tenant-scoped (rows are later used via sched.tenant_id).
  - _Why false positive:_ heartbeat is a system worker intentionally sweeping due schedules across tenants; multi-tenant read is by design and uses each row's tenant_id downstream
- **server/heartbeat.ts:102** _(upstream-verified)_ — UPDATE research_schedules ... WHERE id = ${sched.id} executes without tenant_id constraint. This updates a tenant-scoped table by id only and can modify other tenants' rows.
  - _Why false positive:_ sched.id comes from the prior internal SELECT of research_schedules rows in the same worker loop, not from request input; update applies to that fetched row
- **server/heartbeat.ts:106** _(upstream-verified)_ — UPDATE research_schedules ... WHERE id = ${sched.id} executes without tenant_id constraint (last_run_at,next_run_at update).
  - _Why false positive:_ same call path fetched sched from research_schedules first; this update is on that previously selected row, not an untrusted tenant boundary
- **server/heartbeat.ts:112** _(upstream-verified)_ — UPDATE research_schedules ... WHERE id = ${sched.id} executed without tenant_id constraint (rescheduling path).
  - _Why false positive:_ same internal loop over previously fetched schedule rows; id is already bound to the selected sched row
- **server/heartbeat.ts:120** _(upstream-verified)_ — UPDATE research_schedules ... WHERE id = ${sched.id} in the error-recovery block lacks tenant scoping. Writing to a tenant-scoped table by id only can cross-tenant-affect rows.
  - _Why false positive:_ recovery update still targets the schedule row already fetched earlier in the same loop; no external tenant-controlled id source
- **server/heartbeat.ts:632** _(global-table)_ — SELECT * FROM event_log WHERE status = 'pending' ... issues a multi-tenant read from a tenant-scoped table (event_log has tenant_id) with no tenant filter. This fetches pending events for all tenants in one query.
  - _Why false positive:_ system heartbeat worker intentionally drains pending event_log entries across tenants and routes each using event.tenant_id
- **server/heartbeat.ts:2837** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id filtering (processDelegations path). This can change approval_status on another tenant's task if ids collide or are guessed.
  - _Why false positive:_ newTask.id comes from storage.createHeartbeatTask in the same call path with tenantId supplied; update targets that just-created row
- **server/heartbeat.ts:3289** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id filtering (delegateTaskFromChat path).
  - _Why false positive:_ newTask.id is returned from same-path storage.createHeartbeatTask with tenantId set, so the subsequent approval_status update is on the newly created row
- **server/internal-resolver.ts:93** _(internal-worker-pk)_ — finalizeEvent performs UPDATE on event_log without constraining by tenant_id. This lets the resolver finalize (mutate) event_log rows across tenants (race-prone and breaks tenant isolation).
  - _Why false positive:_ finalizeEvent is an internal resolver helper updating event_log by server-generated eventId within worker flow, not from request params/body
- **server/internal-resolver.ts:107** _(internal-worker-pk)_ — SELECT FROM plans by id has no tenant_id constraint. The resolver reads and then acts on a plan row without verifying it belongs to the caller/tenant, enabling cross-tenant access and mutation.
  - _Why false positive:_ resolver reads plan by internal planId from server-side event-processing path, not from an untrusted cross-tenant request boundary
- **server/internal-resolver.ts:170** _(internal-worker-pk)_ — UPDATE code_proposals transitions rows to archived_verification_failed without restricting by tenant_id. This can archive proposals belonging to other tenants.
  - _Why false positive:_ Internal resolver worker updates by server-derived proposalId from event data, not a request-bound tenant context.
- **server/lib/inbox-ingest.ts:505** _(upstream-verified)_ — UPDATE on inbox_classifications omits tenant_id in WHERE (UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}). This can update rows across tenants.
  - _Why false positive:_ Classification row was just claimed/inserted in the same call path with tenant_id and unique inbox_message_id, so update by inbox_message_id targets the owned row.
- **server/oauth-subscriptions.ts:1328** _(global-table)_ — forceRefreshAllSubscriptions() performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without restricting by tenant_id — it returns rows for all tenants.
  - _Why false positive:_ Daily cron force-refresh explicitly sweeps all active subscriptions platform-wide; this is a server global maintenance path, not tenant-request data access.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog updates the tenant-scoped plans table using only WHERE id = ${planId} (no tenant_id constraint). This can modify other tenants' plan rows if planId is guessable or attacker-controlled.
  - _Why false positive:_ appendExecutionLog is an internal executor helper keyed by server-generated planId in worker flow, not directly from an untrusted request boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Claiming a plan uses UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status = 'approved' RETURNING ... — there is no tenant_id constraint. This allows claiming/executing plans across tenants.
  - _Why false positive:_ executePlan is an internal worker/executor claim by globally unique plan primary key; this is server-side queue processing, not caller-scoped tenant access.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} is executed without a tenant_id constraint, allowing cross-tenant writes to the plans table.
  - _Why false positive:_ This update occurs only after the prior claim RETURNING fetched the specific plan row by id and established its identity in the same call path.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final status update (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint, permitting cross-tenant modifications of plan status.
  - _Why false positive:_ Final status write is in the same executePlan call after the earlier successful claim of that exact plan row; ownership/row identity is established upstream.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-handling update (UPDATE plans SET status = 'failed' ... WHERE id = ${planId} AND status = 'executing') does not include tenant_id in the WHERE clause, enabling an attacker to flip other tenants' plan states.
  - _Why false positive:_ Catch-path failure transition is internal executor cleanup on the same server-side planId worker flow, not user-controlled cross-tenant access.
- **server/process-governor.ts:119** _(fk-transitive)_ — Subquery reads heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This allows cross-tenant data leakage because heartbeat_logs is tenant-scoped but not constrained.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted via task_id against tenant-scoped heartbeat_tasks rows, so isolation is transitive through the parent task.
- **server/process-governor.ts:198** _(global-table)_ — Query on heartbeat_logs (SELECT AVG(duration_ms) ...) lacks a tenant_id filter. Reading tenant-scoped heartbeat logs without tenant constraint can leak cross-tenant metrics.
  - _Why false positive:_ heartbeat_logs has no tenant_id per schema and this response-time check is a platform-wide health metric, not per-tenant scoped data.
- **server/process-governor.ts:239** _(global-table)_ — Query counting distinct persona_id from heartbeat_logs (cascading_failures) has no tenant_id constraint. This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ heartbeat_logs has no tenant_id per schema and this distinct failing-agents count is used as a global cascading-failure signal.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} executes without a tenant_id condition. This can modify rows belonging to other tenants if IDs collide or are tampered with.
  - _Why false positive:_ t.id values come from the immediately preceding SELECT of heartbeat_tasks constrained by ht.tenant_id = tenantId in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} has no tenant_id restriction. Although desk.id was selected earlier for this tenant, the UPDATE lacks an explicit tenant filter and can affect rows beyond the tenant boundary.
  - _Why false positive:_ desk.id is sourced from the preceding SELECT on agent_desks with ad.tenant_id = tenantId, so the target rows were tenant-verified earlier in the same flow.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} is missing a tenant_id constraint. Reading a proposal by id without verifying ownership can return another tenant's proposal.
  - _Why false positive:_ Server-invoked verifier reads code_proposals by server-supplied proposalId primary key, with no untrusted request boundary shown.
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) executes without tenant scoping. This can modify proposals belonging to other tenants.
  - _Why false positive:_ Internal verifier worker updates code_proposals by the same server-managed proposalId primary key, not shown as caller-controlled.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist() performs UPDATE code_proposals SET verification_status... WHERE id = ${proposalId} without scoping by tenant_id. This writes tenant-scoped state without enforcing tenant isolation.
  - _Why false positive:_ persist() is an internal helper in the verifier call path updating by server-invoked proposalId primary key.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} (marking NO_LONGER_APPLICABLE) has no tenant_id condition. This mutates tenant-scoped proposals without tenant scoping.
  - _Why false positive:_ Rebase path is internal worker logic mutating a proposal by trusted proposalId already loaded in same server flow.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} (persist rebased diff) lacks tenant scoping. Writing a rebased diff without tenant restriction risks cross-tenant modification.
  - _Why false positive:_ Persisting rebased diff occurs inside internal verifier worker using server-supplied proposalId primary key.
- **server/routes.ts:4330** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The statement: INSERT INTO project_notes (project_id, note, author) VALUES (...) — missing tenant_id column. Without tenant_id, a note could be recorded against a project id belonging to another tenant.
  - _Why false positive:_ project_notes has no tenant_id by schema and is linked via tenant-scoped parent project_id
- **server/sculptor.ts:93** _(internal-worker-pk)_ — UPDATE to sculptor_sessions lacks tenant scoping (no WHERE tenant_id = ${tenantId}). The INSERT that created the session included tenant_id, but subsequent UPDATE uses only id and can modify another tenant's row if ids are guessed or leaked.
  - _Why false positive:_ update uses server-created session.id from same function after insert, not untrusted request input
- **server/sculptor.ts:100** _(internal-worker-pk)_ — UPDATE to sculptor_sessions (setting conversation_id) uses WHERE id = ${session.id} with no tenant_id constraint. This writes to a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ update uses server-created session.id from same function after insert, not caller-controlled
- **server/sculptor.ts:155** _(internal-worker-pk)_ — monitorSession updates sculptor_sessions (SET status, result, tool_calls_count, tokens_used, completed_at) using WHERE id = ${sessionId} with no tenant_id filter. monitorSession receives tenantId but the UPDATE doesn't enforce it, allowing cross-tenant modification.
  - _Why false positive:_ background monitor updates by server-originated sessionId established at creation time, not cross-tenant request input
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id}) inserts into a tenant-scoped join table without setting tenant_id (and derived values came from an unscoped query).
  - _Why false positive:_ project_conversations has no tenant_id by schema and links tenant-scoped conversation/project parents transitively
- **server/self-improvement.ts:243** _(upstream-verified)_ — getExperimentHistory (and other code paths) perform unscoped SELECTs against the experiments table when no tenantId condition is supplied, returning data across tenants.
  - _Why false positive:_ the shown query already filters by experiments.tenantId = tenantId and tenantId is required above
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — db.update(experiments).where(eq(experiments.id, experiment.id)) updates the experiments row by id only and does NOT constrain by tenant_id — a caller could update another tenant's experiment by id.
  - _Why false positive:_ Update is by server-held experiment.id inside internal self-improvement workflow, not from request boundary.
- **server/self-improvement.ts:413** _(internal-worker-pk)_ — Error-path UPDATE (setting status='error') uses WHERE eq(experiments.id, experiment.id) without tenant_id constraint — same cross-tenant write risk as above.
  - _Why false positive:_ Error-path state update uses same internal server-generated experiment.id in worker flow.
- **server/self-reflection.ts:366** _(upstream-verified)_ — storeLesson performs an UPDATE on memory_entries using WHERE id = <id> but does NOT include tenant_id in the WHERE clause. Although the SELECT that found the id used tenant_id, the UPDATE path still lacks an explicit tenant constraint allowing a mis-scoped write if ids are guessed or race conditions occur.
  - _Why false positive:_ UPDATE id comes directly from prior SELECT scoped by tenant_id in same call path.
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from `messages` uses only conversation_id in the WHERE clause and does not include tenant scoping. Reading tenant-scoped message rows without a tenant_id constraint allows cross-tenant data exposure (even if the conversation was validated in-memory).
  - _Why false positive:_ Messages are fetched only after same-function conversation ownership check against tenantId.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from `messages` (recentMsgs in distillIntent) filters only by conversationId and omits tenant_id. This performs a tenant-scoped table read without an SQL-level tenant constraint.
  - _Why false positive:_ distillIntent first calls analyzeConversation(conversationId, tenantId), which verifies conversation tenant ownership in same call path.
- **server/skillify.ts:331** _(upstream-verified)_ — SELECT from `messages` (recentMsgs in skillifyConversation) filters only by conversationId and omits tenant_id. This allows reading messages from other tenants if an attacker supplies a foreign conversation id.
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), establishing conversation ownership earlier in same path.
- **server/skillify.ts:223** _(global-table)_ — SELECT from `personas` fetches a persona by id only (eq(personas.id, conv[0].personaId)) without constraining by tenant_id. If personas is tenant-scoped, this is a cross-tenant read risk.
  - _Why false positive:_ Schema note says personas has no tenant_id, so lookup is against non-tenant-scoped table.
- **server/storage.ts:832** _(global-table)_ — Mass UPDATE on tenant-scoped table `memory_entries` with no tenant filter (archiveExpiredMemories updates all expired memory_entries across tenants).
  - _Why false positive:_ Global maintenance job intentionally archives expired rows across all tenants, not request-scoped tenant access.
- **server/storage.ts:848** _(global-table)_ — Mass UPDATE on tenant-scoped table `memory_entries` with no tenant filter (archiveStaleMemories updates rows by created_at/last_accessed without tenant scoping).
  - _Why false positive:_ Global lifecycle maintenance job archives stale memories platform-wide rather than serving tenant caller data.
- **server/storage.ts:1296** _(global-table)_ — Multiple reads across many tenant-scoped tables with NO tenant filter in getAllDataForExport (selects conversations, messages, memoryEntries, agentKnowledge, dailyNotes, heartbeatTasks, deliveryLogs, fileStorage, etc.).
  - _Why false positive:_ Function is explicitly full-platform export (getAllDataForExport), indicating intentional global admin-style aggregation.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (no eq(customTools.tenantId, tenantId)).
  - _Why false positive:_ Tool name is first transformed with tenantToolName(parsed.name, tenantId), embedding tenant scoping before lookup.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` by id only (eq(customTools.id, inserted.id)) with NO tenant_id constraint.
  - _Why false positive:_ Read is by inserted.id from the row just created with tenantId in the same call path, not from untrusted input.
- **server/tool-learning.ts:294** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` (.where(eq(customTools.id, tool.id))) without constraining by tenant_id — missing tenant ownership check.
  - _Why false positive:_ Update uses tool.id from the newly inserted/validated tool object created earlier in the same flow with tenantId set.
- **server/tool-learning.ts:316** _(upstream-verified)_ — DELETE on tenant-scoped table `custom_tools` (db.delete(...).where(eq(customTools.id, tool.id))) with NO tenant_id constraint — may delete another tenant's row if IDs are guessed or reused.
  - _Why false positive:_ Delete reverts the same just-inserted tool row passed through same call path, not caller-selected arbitrary id.
- **server/tool-learning.ts:367** _(upstream-verified)_ — UPDATE to increment usageCount on `custom_tools` (.where(eq(customTools.id, tool.id))) without a tenant_id filter.
  - _Why false positive:_ Tool row was selected immediately above with tenantId constraint, so updating by that fetched id is ownership-verified in the same path.
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits a tenant_id constraint (WHERE id = ${id} only). This permits updating a row by id without asserting it belongs to the caller's tenant.
  - _Why false positive:_ id comes from a prior SELECT on memory_entries scoped by tenant_id and persona in the same call path.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits a tenant_id constraint (WHERE id = ANY(${ids}) only). This updates rows by id without asserting tenant ownership.
  - _Why false positive:_ ids array is derived from rows selected just above with tenant_id and persona filters in the same function.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits a tenant_id constraint (WHERE id = ${id} only). This can update a row by id without verifying it belongs to the caller's tenant.
  - _Why false positive:_ id comes from earlier tenant-scoped SELECT on memory_entries in the same function.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT on `conversations` is issued without constraining tenant_id (SELECT tenant_id FROM conversations WHERE id = ${info.conversationId}).
  - _Why false positive:_ This select is immediately followed by tenant comparison against claimed tenantId and rows failing the check are dropped.
- **server/ab-optimizer.ts:110** _(internal-worker-pk)_ — runDueAbExperiments selects rows from experiments without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This can cause processing of other tenants' experiments.
  - _Why false positive:_ Heartbeat-callable internal worker processes experiment rows across tenants by server-side batch job, not caller-supplied tenant/id input.
- **server/agentic-engines.ts:160** _(upstream-verified)_ — retryPendingMinervaRouting SELECTs ai_insights rows across all tenants (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ...). Reading tenant-scoped rows without tenant filtering risks cross-tenant processing.
  - _Why false positive:_ This is an intentional cross-tenant internal sweep; rows carry tenant_id forward and downstream write back is scoped by id and tenant_id.
- **server/agentic-engines.ts:242** _(upstream-verified)_ — autoApplyEligibleInsights executes UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) without any tenant_id constraint, updating rows across all tenants.
  - _Why false positive:_ Intentional platform-wide worker backfill over ai_insights; returned rows include tenant_id and subsequent per-row actions validate tenant and use tenant-scoped updates.
- **server/agentic-engines.ts:343** _(global-table)_ — gatherOperationalData issues a heartbeat_logs query without tenant scoping (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads operational logs across tenants into a per-tenant analysis.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column NO, matching a platform-global operational log table.
- **server/agentic-engines.ts:450** _(global-table)_ — runPredictiveEngine queries recent heartbeat_logs without a tenant filter (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...). This can surface other tenants' logs.
  - _Why false positive:_ heartbeat_logs has no tenant_id and appears intentionally used as global operational activity data.
- **server/agentic-engines.ts:518** _(global-table)_ — runOptimizationEngine aggregates heartbeat_logs without tenant filtering (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This reads multi-tenant data into tenant-specific optimization logic.
  - _Why false positive:_ heartbeat_logs has no tenant_id column, so this aggregation is over a global operational log table by design.
- **server/agentic-features.ts:525** _(upstream-verified)_ — scoreLeads updates lead_enrichments using WHERE id = ${lead.id} but does not include tenant_id in the WHERE clause. An attacker or bug could cause cross-tenant updates if an id collides.
  - _Why false positive:_ lead.id comes from leadRows selected earlier for the same tenant-scoped operation, so the update targets rows already filtered to params.tenantId.
- **server/agentic-features.ts:672** _(upstream-verified)_ — advanceSequence updates outreach_enrollments (UPDATE ... WHERE id = ${enrollment.id}) without including tenant_id in the WHERE clause. This can update another tenant's enrollment row with the same id.
  - _Why false positive:_ enrollment.id is from an earlier tenant-scoped enrollment query in the same call path, so ownership was verified before this update.
- **server/agentic-features.ts:721** _(upstream-verified)_ — classifyReply updates outreach_enrollments using WHERE id = ${row.id} but omits tenant_id. This can modify enrollments belonging to other tenants if ids collide.
  - _Why false positive:_ row is selected immediately above with e.tenant_id = params.tenantId, so the subsequent id-based update operates on a tenant-verified row.
- **server/agentic/approvals.ts:135** _(upstream-verified)_ — expireStaleApprovals performs a bulk UPDATE agentApprovals SET status='expired' ... WHERE status = 'pending' AND expiresAt < now() without any tenant constraint. This will expire pending approvals across all tenants in one shot.
  - _Why false positive:_ This is an internal expiry sweep intended to process all tenants; returned rows include tenantId and linked run updates are tenant-scoped.
- **server/agentic/department-budgets.ts:143** _(global-table)_ — SELECT from tenant-scoped table `department_budgets` without tenant_id constraint: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads rows across all tenants.
  - _Why false positive:_ Heartbeat-callable enforcement sweep intentionally iterates active budgets across tenants and emits per-tenant events; this is a server-side cross-tenant worker path.
- **server/agentic/resume-worker.ts:66** _(global-table)_ — resumePendingApprovalRuns() issues a SELECT that reads agent_runs (and joins agent_approvals) without constraining by a caller tenant_id — the query can return rows across tenants.
  - _Why false positive:_ Server-side sweep worker intentionally scans pending approvals across tenants to resume runs; not request-scoped tenant access.
- **server/agentic/task-forces.ts:40** _(upstream-verified)_ — createTaskForce() builds an SQL array literal for persona_ids (e.g. `{1,2}`) from request-derived personaIds and injects it into a sql fragment (sql`${literal}::int[]`). This is string-interpolated SQL with request-derived content (injection / isolation risk).
  - _Why false positive:_ Not a tenant-isolation finding; tenant_id is explicitly inserted from p.tenantId and the array literal is built only from integers.
- **server/agentic/wake-scheduler.ts:79** _(upstream-verified)_ — UPDATE/SELECT on tenant-scoped table agent_wake_schedules performed without constraining tenant_id. The claim/selection query that moves pending wakes to 'firing' runs across all tenants (no WHERE tenant_id = ...).
  - _Why false positive:_ Shown code path already enforces tenant scope with WHERE tenant_id = ${tenantId}; first-pass claim does not match snippet.
- **server/auth.ts:1019** _(signed-url)_ — SELECT from tenant-scoped table password_reset_tokens uses only token hash in WHERE (no tenant_id constraint). A request-derived token is used to look up the row without verifying tenant ownership.
  - _Why false positive:_ Password reset token hash is the bearer secret used as the row key; lookup by token alone is the access control, tenant_id is payload context.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' lacks a tenant_id constraint in the WHERE clause. The code runs: UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — missing tenant guard risks modifying a conversation belonging to another tenant if IDs collide or are forged.
  - _Why false positive:_ project_id and tenant_id were derived from prior join requiring projects p.tenant_id = c.tenant_id, and update targets the fetched conversation id from same worker path.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. invoice_items appears to be a tenant-scoped table but the INSERT only sets invoice_id and other columns, so tenant ownership is not enforced on insert.
  - _Why false positive:_ invoice_items has no tenant_id column and is transitively scoped through tenant-scoped invoices via invoice_id.
- **server/channel-routing.ts:30** _(global-table)_ — SQL SELECT joins channel_routes to personas (JOIN personas p ON p.id = cr.persona_id) without constraining the personas table by tenant_id. If personas is tenant-scoped, this reads persona rows across tenants and can leak or misuse persona data.
  - _Why false positive:_ personas schema shows no tenant_id and channel_routes is a single global routing table by design
- **server/channel-routing.ts:16** _(global-table)_ — channel_routes table is created with no tenant_id column (CREATE TABLE IF NOT EXISTS channel_routes ... UNIQUE(channel)). In a multi-tenant deployment this global table will lead to cross-tenant collisions and/or require joins to tenant-scoped tables without tenant filters.
  - _Why false positive:_ channel_routes is defined as a platform-global channel map with UNIQUE(channel), not a tenant-scoped table
- **server/chat-engine.ts:572** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (no WHERE tenant_id = ${tenantId}). Relying on prior project ownership checks is brittle and violates the invariant that every tenant-scoped read must include an application-level tenant filter.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via project_id; parent project ownership is verified just above with tenant_id
- **server/chat-engine.ts:583** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (WHERE project_id = ${projectId} only). This allows potential cross-tenant access if project_id can be influenced.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped via project_id; parent project ownership is verified just above with tenant_id
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on tenant-scoped table 'credential_vault' performed without tenant_id constraint (last_used_at update: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}).
  - _Why false positive:_ getCredential(id, tenantId) verifies tenant ownership immediately before this last_used_at update on that fetched row
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on tenant-scoped table 'credential_vault' performed without tenant_id constraint (last_used_at update: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}).
  - _Why false positive:_ getCredentialForSite(siteUrl, tenantId) verifies tenant ownership immediately before updating last_used_at for cred.id
- **server/data-protection.ts:72** _(upstream-verified)_ — softDeleteConversation updates conversations.deleted_at without constraining the UPDATE by tenant_id (UPDATE ... WHERE id = ${conversationId} missing tenant_id), relying only on an earlier SELECT that included tenant_id.
  - _Why false positive:_ same function selects conversations by id and tenant_id just above and only then performs the soft-delete update
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM `doc_chunks` uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but has NO tenant_id constraint. Although collection existence was checked earlier, the DELETE itself lacks an explicit tenant filter.
  - _Why false positive:_ Same call path first verifies doc_collections.id belongs to tenantId; delete is scoped by that verified collection_id.
- **server/dream-consolidation.ts:83** _(upstream-verified)_ — SELECT role, content FROM `messages` WHERE conversation_id = ${conv.id} (inside getRecentSessionSummaries) lacks a tenant_id constraint. Although conversation rows were selected for a tenant, the messages SELECT does not include tenant_id explicitly.
  - _Why false positive:_ Messages are fetched only for conversation ids already selected with tenant_id = tenantId in the same function.
- **server/embeddings.ts:684** _(internal-worker-pk)_ — UPDATE against tenant-scoped table (memory_entries | agent_knowledge via dynamic table param) omits tenant_id in WHERE clause. This allows updating rows belonging to other tenants by id.
  - _Why false positive:_ server-side helper updates by server-generated row id from internal backfill path, not request-derived input
- **server/embeddings.ts:726** _(internal-worker-pk)_ — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id in WHERE, allowing cross-tenant modifications.
  - _Why false positive:_ internal maintenance backfill updates memory_entries by selected primary key ids, not untrusted caller input
- **server/embeddings.ts:743** _(internal-worker-pk)_ — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id in WHERE, allowing cross-tenant modifications.
  - _Why false positive:_ internal maintenance backfill updates agent_knowledge by selected primary key ids, not untrusted caller input
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from projects by project id omits tenant filter: db.select(...).from(projects).where(eq(projects.id, projectId)). This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ row is immediately checked against caller tenantId before use; mismatch throws fail-closed
- **server/google-drive.ts:826** _(upstream-verified)_ — SELECT of a project by id (uploadAndShare) is performed without tenant scoping: .where(eq(projects.id, params.projectId)). This reads a tenant-scoped table before ownership is validated.
  - _Why false positive:_ selected project row is immediately ownership-checked against params.tenantId before further logic
- **server/google-drive.ts:1004** _(upstream-verified)_ — SELECT used to validate a project ownership in searchDriveFiles reads projects by id without adding tenant_id to the WHERE (where(eq(projects.id, params.projectId))). Reads against tenant-scoped tables must include tenant constraints.
  - _Why false positive:_ query purpose is ownership verification and code immediately rejects tenant mismatch before proceeding
- **server/heartbeat.ts:1743** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant constraint. heartbeat_logs is likely tenant-scoped, so this reads logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is used as platform-global heartbeat telemetry
- **server/job-queue.ts:98** _(global-table)_ — claimDueJobs updates agent_jobs (claims jobs) using a SELECT/UPDATE that lacks any tenant_id constraint — it can claim or mutate jobs for any tenant indiscriminately.
  - _Why false positive:_ claimDueJobs is a platform worker claiming from a shared queue table across tenants by design
- **server/job-queue.ts:134** _(global-table)_ — reclaimExpiredLeases updates agent_jobs to failed_terminal for expired leases without scoping by tenant_id. This can mark jobs for other tenants as failed-terminal incorrectly.
  - _Why false positive:_ reclaimExpiredLeases is shared queue maintenance over global worker state, not request-tenant access
- **server/job-queue.ts:148** _(global-table)_ — reclaimExpiredLeases re-queues expired agent_jobs (running→pending) without a tenant_id constraint, allowing cross-tenant mutation of job rows.
  - _Why false positive:_ requeue of expired leases is shared queue maintenance on global job scheduler state
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs to 'succeeded' by id/attempts only, with no tenant_id constraint. A stale or forged caller could affect another tenant's job row by id.
  - _Why false positive:_ completeJob updates by server-generated job id plus attempts lease-fencing inside worker flow, not an untrusted request boundary
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob updates agent_jobs to 'failed_terminal' using WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} without checking tenant_id, which can mutate jobs across tenants.
  - _Why false positive:_ failJob terminal update is by server-generated job id plus attempts in internal worker path
- **server/job-queue.ts:258** _(internal-worker-pk)_ — failJob's retry-path UPDATE (set pending/next_run_at) does not include a tenant_id constraint, permitting cross-tenant state changes to job retry scheduling.
  - _Why false positive:_ failJob retry update is by server-generated job id plus attempts in internal worker path
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns uses only the run id in the WHERE clause (where(eq(procedureEvolutionRuns.id, runId))) — no tenant_id predicate. This allows a caller to flip/modify another tenant's run row by id.
  - _Why false positive:_ run row was just inserted with tenantId and subsequent update uses that server-generated runId within same function
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns uses only the run id in the WHERE clause to mark a run done — missing tenant_id predicate (concurrent noop/claim update).
  - _Why false positive:_ same-function follow-up update of freshly inserted procedureEvolutionRuns row by server-generated runId
- **server/lib/aevo-meta-editor.ts:499** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns to mark 'done' (post-insert) uses only id in WHERE; tenant_id is not constrained.
  - _Why false positive:_ post-insert completion update targets the same newly created run row by internal runId in same call path
- **server/lib/ideabrowser-backfill.ts:104** _(fk-transitive)_ — INSERT INTO project_files omits tenant_id in the VALUES list. Every INSERT into a tenant-scoped table must set tenant_id explicitly (columns are NOT NULL with no default in this deployment model).
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via project_id to projects, which were inserted/looked up with tenant_id
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT INTO project_notes omits tenant_id in the VALUES list. Tenant-scoped INSERTs must explicitly set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id to tenant-scoped projects
- **server/memory-graph.ts:194** _(fk-transitive)_ — INSERT into memory_links does not set tenant_id. If memory_links is tenant-scoped, this creates links without an explicit tenant owner, allowing cross-tenant linking.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries by foreign keys rather than direct tenant column
- **server/memory-graph.ts:392** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks tenant_id scoping. Updating by id alone can affect rows belonging to other tenants (depending on id allocation).
  - _Why false positive:_ row ids come from a prior SELECT on memory_entries constrained by tenant_id in the same function
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} does not include tenant_id. This may update triples belonging to other tenants if ids are global.
  - _Why false positive:_ c.id values come from the immediately prior SELECT on knowledge_triples constrained by tenant_id in the same loop
- **server/minds-engine.ts:214** _(upstream-verified)_ — createTicket builds a Postgres array literal via string interpolation for dependsOn: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. This interpolates request-derived values directly into SQL (injection + isolation risk).
  - _Why false positive:_ This INSERT is preceded by getMind(params.mindId, params.tenantId), tying the operation to the caller's tenant; flagged issue is SQL injection style, not a missing tenant isolation check.
- **server/minds-engine.ts:262** _(upstream-verified)_ — checkDependenciesMet uses string-interpolated array literal: WHERE id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. Even if ticket.dependsOn originates from DB, interpolating it into SQL is unsafe and can break isolation/parsing.
  - _Why false positive:_ Query includes tenant_id = ${tenantId}; dependsOn values come from the already tenant-scoped ticket/listTickets path, so no cross-tenant read is shown here.
- **server/minds-engine.ts:325** _(upstream-verified)_ — markEventsHandled uses string-interpolated array literal for eventIds: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. This interpolates caller-provided eventIds into SQL directly (injection + isolation risk).
  - _Why false positive:_ UPDATE is explicitly constrained by tenant_id = ${tenantId}; eventIds are additionally bounded by that predicate, so no cross-tenant modification path is shown.
- **server/pace-control.ts:85** _(global-table)_ — checkPace() queries heartbeat_logs without any tenant_id predicate (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND <laneFilter>), so global activity from all tenants is counted together and could allow one tenant's activity to affect others or leak usage patterns.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column: NO; this module enforces platform-wide pace caps over a genuinely global log table.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot() queries heartbeat_logs across all tenants (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... ) with no tenant_id filter. This produces cross-tenant aggregates and may leak tenant-specific activity data or cause cross-tenant enforcement issues.
  - _Why false positive:_ heartbeat_logs has no tenant_id and getPaceSnapshot is an operator/debug aggregate over that global table by design.
- **server/orchestrator-ledger.ts:444** _(upstream-verified)_ — Raw SQL injection point: pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint) uses sql.raw() with a value (lockKey) derived from ledgerKey and tenantId. ledgerKey is caller-supplied in LedgerRunOptions and therefore can be request-derived, making this use of sql.raw() an injection and tenant-isolation risk.
  - _Why false positive:_ The only tenant-scoped table access here is memory_entries and every read/write is constrained by tenant_id = ${tenantId}; the raw advisory lock expression does not itself access tenant data across tenants.
- **server/research-engine.ts:470** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. project_notes appears tenant-scoped, so missing tenant_id allows cross-tenant writes or orphan rows tied only by project_id.
  - _Why false positive:_ project_notes has no tenant_id by schema and is scoped transitively via project_id; assertProjectInTenant(projectId, session.tenantId) verifies parent ownership first.
- **server/research-engine.ts:478** _(fk-transitive)_ — Second INSERT into project_notes (for each finding) omits tenant_id, allowing project notes to be written without tenant scoping.
  - _Why false positive:_ project_notes is non-tenant table isolated through tenant-scoped parent project; same upstream assertProjectInTenant check confirms project belongs to session tenant.
- **server/routes.ts:2312** _(signed-url)_ — SELECT from delivery_logs in /api/store/order/:sessionId uses only orderId/stripePaymentId matching (capability) and does not include any tenant_id constraint in SQL. This reads tenant-scoped rows with no tenant-scoped WHERE clause.
  - _Why false positive:_ Public order lookup is explicitly capability-based on unguessable Stripe session id and returns minimized public-safe fields; tenant_id is not the access-control mechanism here.
- **server/routes.ts:3436** _(upstream-verified)_ — Handler POST /api/messages/:id/feedback executes a SELECT from messages WHERE id = ${messageId} with NO tenant_id clause. A tenant-scoped table (messages) is queried without SQL-level tenant scoping.
  - _Why false positive:_ Handler immediately checks returned message tenantId against requester tenantId and storage.upsertMessageFeedback re-verifies ownership before write; same call path enforces tenant match.
- **server/routes/admin-zombie-detector.ts:38** _(upstream-verified)_ — Query reads agent_trace_spans without a tenant_id constraint. The SQL in the transaction aggregates agent_trace_spans (used AS ...) but does not filter by tenant_id, allowing cross-tenant data exposure.
  - _Why false positive:_ Route is explicitly restricted to platform admin tenant plus admin request check; cross-tenant aggregate is intentional in this admin-only call path.
- **server/routes/admin-zombie-detector.ts:54** _(upstream-verified)_ — Query aggregates conversations across all tenants without tenant_id filtering. The WITH used AS (...) selects from conversations (GROUP BY persona_id) but omits tenant_id, exposing per-tenant conversation activity globally.
  - _Why false positive:_ Same admin-only route path (ADMIN_TENANT_ID and isAdminRequest) intentionally performs cross-tenant usage aggregation for platform audit.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This can mutate a conversation belonging to another tenant.
  - _Why false positive:_ conv.id comes from storage.createConversation with tenantId just above, so update targets a freshly created conversation already owned by the caller's tenant.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Why false positive:_ project_conversations has no tenant_id by schema and is a join/child table linking tenant-scoped parent rows; both project ownership is checked and conversation was just created for this tenant.
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)).
  - _Why false positive:_ project_notes has no tenant_id and is inserted via verified tenant-scoped project_id from projects join/check
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table 'inbox_messages' lacks tenant_id constraint (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}).
  - _Why false positive:_ msg was selected immediately above with id and tenant_id, so update by msg.id is on a tenant-verified row in same path
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes omits tenant_id (INSERT INTO project_notes (project_id, note, author) ...). If project_notes is tenant-scoped this allows inserting rows not explicitly tied to the caller's tenant.
  - _Why false positive:_ project_notes has no tenant_id and parent project was verified with tenant_id before insert
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (base64 upload handler) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...). If project_files is tenant-scoped this will create rows without an explicit tenant association.
  - _Why false positive:_ project_files has no tenant_id and insert uses projectId already verified against tenant-scoped projects row
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — the UPDATE lacks a tenant predicate. Without a WHERE tenant_id = ${tenantId} this could update a project from another tenant if projectId is misused.
  - _Why false positive:_ same handler verified project id belongs to tenant just above before updating that project row
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into project_files (multipart upload handler) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...). Same class of issue as other file inserts.
  - _Why false positive:_ project_files has no tenant_id and insert uses tenant-verified parent project_id
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — this update in the multipart upload flow lacks a tenant constraint, allowing cross-tenant updates if misused.
  - _Why false positive:_ same handler already checked project belongs to tenant before updating that project id
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ... executed after a separate projCheck but the SELECT itself has no tenant_id constraint. Per the tenant-isolation rule every read against a tenant-scoped table must include an explicit tenant predicate (or equivalent Drizzle eq).
  - _Why false positive:_ project_files has no tenant_id; read is scoped transitively by prior verified project_id ownership check
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file_name,... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 lacks an explicit tenant predicate. Although project ownership is validated earlier, this query itself does not constrain by tenant_id.
  - _Why false positive:_ project_files has no tenant_id; download lookup is by verified tenant-owned project_id
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} has no tenant predicate. Without tenant scoping the DELETE could remove rows belonging to other tenants if project_id validation is bypassed or incorrect.
  - _Why false positive:_ project_files has no tenant_id; delete is limited to file within previously verified tenant-owned project_id
- **server/storage.ts:289** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without a tenant_id constraint in attachCitationMetadata (raw SQL SELECT id,citations FROM messages ... ).
  - _Why false positive:_ attachCitationMetadata only re-reads messages for ids taken from msgs already fetched with tenant-scoped getMessages/getMessagesPaginated filters.
- **server/storage.ts:615** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversation_facts` without tenant_id constraint (evictOldestConversationFacts updates by id list only).
  - _Why false positive:_ Victim ids are selected immediately above with conversation_id and tenant_id filters, and update uses only that scoped id set.
- **server/storage.ts:985** _(internal-worker-pk)_ — SELECT from tenant-scoped table `heartbeat_tasks` with no tenant constraint (getDueHeartbeatTasks queries all enabled/approved tasks across tenants).
  - _Why false positive:_ getDueHeartbeatTasks is background scheduler code intentionally enumerating due tasks across tenants and returns tenantId for downstream worker handling.
- **server/storage.ts:1013** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant scoping in claimHeartbeatTasks (UPDATE ... WHERE id = ${id} ... no tenant_id check).
  - _Why false positive:_ claimHeartbeatTasks updates scheduler-selected task rows by server-side task ids in internal worker flow, not ids from an untrusted request boundary.
- **server/tools.ts:6893** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id in the columns/VALUES. The INSERT uses project_id and other fields but omits tenant_id even though tenant isolation is enforced only at the app layer and tenant_id columns are NOT NULL with no default.
  - _Why false positive:_ project_files has no tenant_id column and is isolated transitively through tenant-scoped parent project after assertProjectInTenant check.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The code inserts a row referencing project_id but does not set tenant_id on the INSERT; if project_files is tenant-scoped this violates the invariant that every INSERT must set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and insert uses projId derived from prior tenant-scoped project join.
- **server/tools/domains/files/handlers.ts:838** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} executes without constraining the update to the caller's tenant (no WHERE tenant_id = ...). Writing to a tenant-scoped table without a tenant constraint can modify another tenant's row if IDs collide or were previously validated separately.
  - _Why false positive:_ projId is obtained from the immediately preceding query joining projects with p.tenant_id = tenantId, so the update target was already tenant-verified in the same call path.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} executes without an explicit tenant filter. Although workflow was fetched earlier with a tenant WHERE, this UPDATE writes to a tenant-scoped table without verifying tenant_id in the WHERE clause, violating the invariant that every write must be constrained by tenant_id.
  - _Why false positive:_ workflow is fetched immediately above with tenant_id = tenantId and update uses that verified row's id in the same call path
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples omits tenant filter. The loop updates rows with `WHERE id = ${row.id}` but does not constrain by tenant_id, allowing a row id to be updated across tenants.
  - _Why false positive:_ row.id values come from the contradictions query scoped by tenant_id = tenantId earlier in the same function
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys executed without constraining by tenant_id (reads tenant-scoped provider_keys table with no tenant filter).
  - _Why false positive:_ schema note says provider_keys has no tenant_id column and access is explicitly gated to admin tenantId === 1
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — COUNT(DISTINCT provider) query against provider_keys runs without a tenant_id filter (potential cross-tenant read of provider keys).
  - _Why false positive:_ provider_keys is schema-global with no tenant_id and this aggregate is explicitly restricted to admin tenantId === 1
