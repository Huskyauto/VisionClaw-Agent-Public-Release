# Tenant-Isolation Audit Report

_Generated 2026-09-09T05:53:35.945Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 0 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **667/667** file(s) audited · chunks 61/61 OK (0 failed)
- Findings: **243** (CRITICAL 51 · HIGH 139 · MEDIUM 53 · LOW 0)
- Severe dispositions: **8 kept** · 145 precision false-positive · 37 allowlist false-positive · 0 deferred real risk

### CRITICAL (51)

- **server/auth.ts:1323** — DELETE FROM auth_sessions is executed with no tenant filter (DELETE FROM auth_sessions). This function (clearAllSessionsAsPlatformAdmin) performs a cross-tenant DELETE against a tenant-scoped table without an explicit tenant_id WHERE clause.
  - _Fix:_ Restrict destructive operations to explicit, intentional platform-admin flows and make the cross-tenant intent explicit in the API surface. Require and validate a platform-admin authority at the callsite (already present) but also (1) log and gate this operation behind an additional explicit parameter/approval, and (2) if possible, scope to tenant_id when the intention is not to affect every tenant. Keep a narrowly-scoped, well-documented admin-only path for full wipes and avoid silent unfiltered DELETEs in shared code.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL performs a write to the tenant-scoped `conversations` table without constraining by tenant_id. This can modify another tenant's conversation row if IDs are reused or if project/conversation linkage is inconsistent.
  - _Fix:_ Include an explicit tenant constraint in the UPDATE (e.g. AND tenant_id = ${c.tenant_id}) or perform the UPDATE inside a transaction that first verifies ownership and then updates using the verified tenant_id. Never perform writes to tenant-scoped tables without the tenant_id WHERE clause (or equivalent application-level ownership check used directly in the same DB operation).
- **server/chat-engine/project-context.ts:23** — Query reads from tenant-scoped table `project_conversations` (alias pc) without constraining pc.tenant_id to the caller's tenant. Relying on a joined `projects.p.tenant_id` is not an allowed replacement under the project's invariant.
  - _Fix:_ Add an explicit tenant filter on the project_conversations table (e.g. AND pc.tenant_id = ${tenantId} or .where(eq(projectConversations.tenantId, tenantId))) or otherwise ensure the SQL itself includes eq(pc.tenant_id, tenantId) so every access to a tenant-scoped table carries the tenant constraint.
- **server/chat-engine/project-context.ts:44** — EXISTS subquery reads `project_conversations` (pc) without a tenant_id constraint. This touches a tenant-scoped table without an explicit tenant filter, enabling cross-tenant inference/IDOR.
  - _Fix:_ Constrain the EXISTS subquery to the caller's tenant (e.g. add AND pc.tenant_id = ${tenantId} or `AND pc.tenant_id = p.tenant_id`) so the query includes an application-level tenant_id filter on every tenant-scoped table referenced.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` uses only project_id and omits tenant_id. The code comments assert safety because projectId was validated earlier, but the invariant requires each SQL against a tenant-scoped table to include an explicit tenant_id constraint.
  - _Fix:_ Add an explicit tenant filter to this query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId} or use the Drizzle eq(fileStorage.tenantId, tenantId)). Even if project ownership was previously verified, the SQL must include the tenant constraint itself.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` uses project_id only and omits tenant_id. This violates the rule that every read/write on tenant-scoped tables must include an application-level tenant filter.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(projectNotes.tenantId, tenantId)) so the query enforces tenant isolation at the SQL level.
- **server/chat-engine/project-context.ts:181** — Query joins `project_conversations pc` but does not constrain pc.tenant_id. While the joined `conversations` row is tenant-filtered, the project_conversations table itself is referenced without an explicit tenant filter, breaching the invariant that every tenant-scoped table access must include tenant_id.
  - _Fix:_ Add an explicit tenant constraint on pc (e.g. AND pc.tenant_id = ${tenantId}) or include eq(projectConversations.tenantId, tenantId) in the WHERE clause so every tenant-scoped table referenced is directly constrained to the caller's tenant.
- **server/commitment-drafter.ts:234** — Unscoped UPDATE on tenant-scoped table `commitments`. The code performs `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` without constraining by tenant_id, allowing a caller (or a buggy code path) to update rows belonging to other tenants.
  - _Fix:_ Include the tenant_id constraint in the WHERE clause so the revert/unclaim only affects the intended tenant's row. For example: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Prefer also checking draft_status (as other code does) to ensure idempotent/race-safe updates (e.g. add `AND draft_status = 'draft_pending'`).
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks executed without tenant constraint (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can delete rows belonging to other tenants.
  - _Fix:_ Always include tenant_id in DELETE predicates on tenant-scoped tables. Change the statement to include AND tenant_id = ${tenantId} (and pass tenantId into the function). Example: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}.
- **server/email.ts:429** — UPDATE inbox_messages sets body_text/body_html WHERE id = ${row.id} without constraining tenant_id. This allows updating rows across tenants by id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the backfill per-tenant and bind tenantId to the update to ensure updates cannot affect other tenants' rows.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to an unconditional UPDATE using sql.raw(table) and updates memory_entries/agent_knowledge by id without a tenant_id WHERE clause when options.tenantId is not provided (UPDATE ${sql.raw(table)} ... WHERE id = ${id}). This writes to tenant-scoped tables without tenant scoping.
  - _Fix:_ Require and validate a positive tenantId for all updates to tenant-scoped tables. Remove the unconditional branch or change it to require a tenantId and include AND tenant_id = ${tenantId} in the WHERE. Avoid using sql.raw(table) for unscoped writes.
- **server/embeddings.ts:852** — backfillEmbeddingVecs issues UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} with no tenant_id constraint. This can modify embedding vectors for rows belonging to other tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE or perform the update inside a per-tenant loop so writes are always bound to the correct tenant. Never update tenant-scoped rows by id alone in multi-tenant environment.
- **server/embeddings.ts:869** — backfillEmbeddingVecs updates agent_knowledge rows with UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without tenant scoping, allowing writes across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in UPDATE statements or run the backfill per-tenant. Ensure all writes to tenant-scoped tables include tenant_id checks to enforce isolation.
- **server/felix-brain.ts:23** — SQL SELECT from tenant-scoped table `agent_runs` is executed without a tenant_id filter. The query at startup (rehydrateTaskStateOnBoot) selects running/pending agent_runs across all tenants, relying on post-query JS checks rather than an application-level WHERE tenant_id = <tenant> filter.
  - _Fix:_ Restrict the query to a single tenant at the SQL level. Either: (a) run rehydration per-tenant and include WHERE tenant_id = ${tenantId} in the SELECT, or (b) enumerate known tenant IDs and query each with an explicit tenant_id filter. Do NOT read cross-tenant rows in a global query even if you later validate row.tenant_id in JS. Ensure all reads from tenant-scoped tables include an application-level tenant_id constraint.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks executed with no tenant_id constraint (returns rows across all tenants). This reads tenant-scoped rows without constraining to a caller tenant.
  - _Fix:_ If this is intended cross-tenant scheduler work, centralize and document it as a privileged scheduler boundary. Otherwise restrict the query with a tenant_id WHERE clause (or accept an explicit ALL_TENANTS sentinel only from a controlled path). Prefer using a storage API that enforces the intended tenant-scoping rather than raw cross-tenant SELECTs.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE heartbeat_tasks SET last_run_at = ..., next_run_at = ... WHERE id = ${id} has no tenant_id constraint. This updates tenant-scoped rows by id only and can modify rows across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or call a storage-layer helper that enforces tenant scoping/authorization. If this function is intentionally cross-tenant, restrict the caller surface and add explicit audit/authorization checks.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE against tenant-scoped table `scheduled_posts` performed without any tenant_id predicate (CTE selects due rows across all tenants and the subsequent UPDATE joins only on id). This updates tenant rows without constraining by tenant_id.
  - _Fix:_ If this is not intentionally multi-tenant admin work, add a tenant_id predicate to the CTE and to the UPDATE (e.g. SELECT id, tenant_id FROM scheduled_posts WHERE tenant_id = ${tenantId} ...; UPDATE scheduled_posts sp SET ... FROM due WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id). If the runner must operate across tenants, explicitly document and gate it to an admin context and make the UPDATE include the tenant_id from the selected rows (sp.tenant_id = due.tenant_id) to preserve the application-level tenant filter invariant.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the primary id in its WHERE clause (`.where(eq(tenantVoiceProfiles.id, existing.id))`) and does not constrain by tenant_id. This lets an update target a row id across tenants.
  - _Fix:_ Include the tenant predicate in the WHERE clause (defensive ownership check). E.g. `.where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, ${opts.tenantId})))` (or equivalent Drizzle `eq(tenantVoiceProfiles.tenantId, tenantId)`).
- **server/memory-graph.ts:129** — UPDATE on tenant-scoped table `memory_categories` lacks a tenant predicate: `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}`. A category id is not a substitute for an explicit tenant check.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}` (or use Drizzle `.where(and(eq(memoryCategories.id, catId), eq(memoryCategories.tenantId, tenantId)))`).
- **server/memory-graph.ts:708** — UPDATE on tenant-scoped table `memory_entries` uses only the entry id (`WHERE id = ${mem.id}`) and omits the tenant_id predicate. This allows modifying another tenant's memory if ids collide or are leaked.
  - _Fix:_ Add an explicit tenant ownership predicate to the UPDATE, e.g. `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}` (or Drizzle `.where(and(eq(memoryEntries.id, mem.id), eq(memoryEntries.tenantId, tenantId)))`).
- **server/oauth-subscriptions.ts:906** — Startup validation reads all rows from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE). This returns data across tenants (includes sensitive tokens/metadata).
  - _Fix:_ Don't perform unscoped SELECTs on a tenant-scoped table. Either iterate known tenant IDs and query each tenant separately, or restrict this startup validation to a single explicit admin/maintenance tenant and require an explicit admin flag. At minimum add a WHERE tenant_id = ${tenantId} parameter (or pass a list of tenantIds) so every query is parameterized to a concrete tenant.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh queries `oauth_subscriptions` without a tenant filter (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE), iterating all tenants. This reads across tenants and may expose sensitive subscription data.
  - _Fix:_ Either run proactive refresh per-tenant (accept tenantId and restrict the query to that tenant) or move this job to a privileged administrative routine that explicitly accepts a tenant whitelist. Never run unscoped queries on tenant-scoped tables; add WHERE tenant_id = ${tenantId} or iterate tenant ids from a safe tenant registry.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant constraint, enumerating subscriptions across all tenants.
  - _Fix:_ Do not issue global unscoped queries on tenant-scoped data. Require an explicit admin operation with an explicit tenant list or iterate tenants and call the per-tenant refresh routine. Add WHERE tenant_id = ${tenantId} when running per-tenant or otherwise restrict to an explicit, validated tenant set.
- **server/project-brain.ts:125** — updateProjectBrain accepts projectId/conversationId and reads the projects row but does not verify tenant ownership — the SELECT against projects uses only id (no tenant_id).
  - _Fix:_ Require/accept a tenantId parameter (or resolve caller tenant from auth) and verify ownership on reads (SELECT ... FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}) before reading or writing any tenant-scoped data.
- **server/project-brain.ts:135** — SELECT from projects without tenant constraint (SELECT name, status, description FROM projects WHERE id = ${projectId}).
  - _Fix:_ Always scope reads to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise validate the project belongs to the tenant before proceeding.
- **server/project-brain.ts:333** — Backfill query selects projects/conversations/messages across all tenants (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...). There is no tenant_id filtering — this will enumerate data for every tenant.
  - _Fix:_ Limit backfill to a specific tenant context or add tenant filters to the query (e.g. WHERE p.tenant_id = ${tenantId} and ensure conversations/messages are scoped) and/or require an explicit tenantId parameter for backfill operations.
- **server/routes/conversations.ts:118** — UPDATE on tenant-scoped table `conversations` omits tenant_id in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This allows an update by id alone and can modify rows belonging to other tenants.
  - _Fix:_ Include the tenant filter in the UPDATE, e.g. WHERE id = ${conv.id} AND tenant_id = ${conv.tenantId ?? tenantId} (or use ${tenantId}). Prefer using the conversation's tenantId value you've already validated to make the WHERE clause atomic and avoid race conditions.
- **server/routes/conversations.ts:124** — UPDATE on tenant-scoped table `projects` omits tenant_id in WHERE clause (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This allows updating a project by id across tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Alternatively, perform the update via a prior select that returned the tenant and reuse that tenant id in the WHERE to ensure tenant-scoped enforcement.
- **server/routes/conversations.ts:256** — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). Per project schema conventions tenant_id is expected to be provided explicitly — omitting it violates the invariant and can create cross-tenant or malformed rows.
  - _Fix:_ Include tenant_id in the INSERT columns/values, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'}). Ensure every INSERT into tenant-scoped tables sets tenant_id from the validated caller tenant (or the owned project tenant) rather than deriving it from request data.
- **server/sculptor.ts:106** — UPDATE against tenant-scoped table `sculptor_sessions` uses WHERE id = ${session.id} with NO tenant_id constraint. This can update a row belonging to another tenant if an id is forged or mistaken.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or verify the session belongs to the caller's tenant before updating. Prefer parameterized tenant checks rather than relying on id-only updates.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions SET conversation_id = … WHERE id = ${session.id} has no tenant_id constraint. Missing tenant filter allows cross-tenant modification by id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or fetch+verify ownership prior to the update.
- **server/sculptor.ts:168** — UPDATE on `sculptor_sessions` (setting status/result/completed_at/etc) uses WHERE id = ${sessionId} without tenant_id constraint. This risks modifying another tenant's session.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or perform an ownership check in the same transaction before making the mutation.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result = … WHERE id = ${sessionId} contains NO tenant_id filter. An attacker-supplied sessionId could cause cross-tenant writes.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or verify the session row belongs to the caller's tenant before writing review_result.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' runs with NO tenant_id constraint against a tenant-scoped table. This reads across tenants and is a potential tenant-isolation risk.
  - _Fix:_ If the intent is to operate per-tenant, add tenant_id = ${tenantId} (or ADMIN_TENANT_ID where appropriate). If global/owner-only intent, document and constrain explicitly to the intended admin tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID}).
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has NO tenant_id constraint and will disable delegation tasks across tenants.
  - _Fix:_ Scope the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or other tenant param). If the goal is to touch only admin/owner tasks, add explicit tenant_id checks.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' executes without tenant_id constraint and will count across tenants.
  - _Fix:_ Restrict the query to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...), or explicitly document why a global scan is required and guard it accordingly.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant scoping and will disable matching tasks for every tenant.
  - _Fix:_ Add tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant parameter) to the WHERE clause, or perform per-tenant updates after validating the target tenant.
- **server/seed-cleanup.ts:59** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) runs without tenant_id constraint and may return counts across tenants.
  - _Fix:_ If only admin/owner tasks should be affected, add WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...; otherwise iterate tenants explicitly and apply scoped updates per tenant.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has NO tenant_id constraint and will disable matching tasks for all tenants.
  - _Fix:_ Constrain the UPDATE to the intended tenant_id (for example ADMIN_TENANT_ID) or perform ownership checks before performing the update. Avoid broad, id-only or pattern-only updates on tenant-scoped tables.
- **server/self-improvement.ts:430** — UPDATE on tenant-scoped table `experiments` uses only experiments.id in the WHERE clause (.where(eq(experiments.id, experiment.id))) — there is no tenant_id constraint, so a row from another tenant could be updated.
  - _Fix:_ Constrain the update to the caller's tenant. Add tenant filtering to the WHERE clause, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId!))) or equivalent EXISTS check to ensure the row belongs to the tenant before updating.
- **server/stability-watchdog.ts:211** — DELETE FROM 'heartbeat_tasks' is executed without any tenant_id constraint, potentially deleting disabled delegation tasks across all tenants.
  - _Fix:_ Do not run destructive SQL across tenants. Constrain the DELETE with WHERE tenant_id = ${tenantId}, or enumerate tenants and perform per-tenant deletes. If the watchdog must operate as a platform admin, require an explicit admin path and/or use a storage API that accepts an explicit ALL_TENANTS flag and documents the destructive scope.
- **server/stability-watchdog.ts:225** — DELETE FROM 'event_log' is performed with no tenant_id constraint (deletes processed/failed events older than 3 days across all tenants).
  - _Fix:_ Add tenant scoping to the DELETE (WHERE tenant_id = ${tenantId}) or loop per-tenant to avoid cross-tenant data deletion. If global pruning is intended, require an auditable admin approval path and clearly document that this is an admin-only global operation.
- **server/stability-watchdog.ts:259** — PruneOldLogs DELETE on 'heartbeat_logs' (old log pruning) runs with no tenant_id constraint — this removes heartbeat logs across all tenants.
  - _Fix:_ Limit the DELETE to a tenant (WHERE tenant_id = ${tenantId}) or perform pruning in a per-tenant loop. For intended global/admin maintenance, require an explicit admin-only pathway and use explicit flags rather than unscoped SQL.
- **server/storage.ts:289** — SELECT from tenant-scoped table `messages` without constraining by tenant_id (SELECT id,citations FROM messages WHERE id IN (...) AND citations IS NOT NULL). This can return citations for messages belonging to other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. add AND tenant_id = <callerTenant> or JOIN to messages → conversations to validate tenant) or pass the tenantId into attachCitationMetadata and use a parameterized Drizzle predicate (eq(messages.tenantId, tenantId)).
- **server/storage.ts:1358** — getAllDataForExport executes many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with no tenant filtering and returns a platform-wide dump. This allows exfiltration of all tenants' data if misused.
  - _Fix:_ Restrict this routine to explicit admin-only flows and require authenticated admin context. Require the caller to pass an explicit ALL_TENANTS sentinel and ensure tight runtime checking; ideally split exports into per-tenant exports that require a tenantId parameter.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table custom_tools updates implementation using only the PK (`.where(eq(customTools.id, tool.id))`) with NO tenant_id constraint. This can modify rows across tenants.
  - _Fix:_ Ensure the update is constrained to the tool's tenant. Pass tenantId into tryAndRevert (or obtain/validate tool.tenantId) and update with both id and tenant_id: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Also validate that the caller is authorized for that tenant before modifying rows.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table custom_tools deletes by id only (`.where(eq(customTools.id, tool.id))`) with NO tenant_id constraint. This can delete another tenant's tool row.
  - _Fix:_ Require and enforce tenant scoping on delete operations. Delete using both id and tenant_id: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Ensure revertToolRow receives or validates the tenantId before performing the DELETE.
- **server/tools.ts:2929** — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT INTO project_files (... ) VALUES (...) omits tenant_id, violating the invariant that tenant_id must be explicitly provided on writes.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})), and ensure the caller's tenantId is used. Also keep the pre-check (project ownership) but do not rely on it as a substitute for an explicit tenant column value.
- **server/tools.ts:2943** — INSERT into tenant-scoped table project_notes does not set tenant_id. The INSERT INTO project_notes (...) VALUES (...) omits tenant_id.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Ensure the tenantId is the caller's tenant and that ownership of the referenced project was validated before the insert.
- **server/tools.ts:2803** — INSERT into tenant-scoped table project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without storing tenant_id on the new row.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId})) so the new row is explicitly tenant-scoped. Validate the conversation and project ownership before inserting.
- **server/tools.ts:2967** — INSERT into tenant-scoped table project_conversations (link_conversation path) does not set tenant_id. The VALUES clause lacks tenant_id.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Also ensure assertConversationInTenant was used (already present) and keep the explicit tenant column to enforce isolation at the application level.

### HIGH (139)

- **server/ab-optimizer.ts:110** — runDueAbExperiments performs a SELECT on tenant-scoped table `experiments` without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This returns rows from all tenants.
  - _Fix:_ Scope the read to the intended tenant(s). Either run the heartbeat per-tenant (SELECT ... WHERE tenant_id = <tenant> ...) or include explicit tenant_id filtering. If this function is intended to operate across tenants, make the multi-tenant intent explicit and ensure callers are authorized; otherwise change query to include WHERE tenant_id = ${tenantId}.
- **server/agent-activity.ts:122** — completeActivity updates agentActivity with WHERE eq(agentActivity.id, activityId) but does not include tenant_id filtering. This allows updating a row that may belong to another tenant.
  - _Fix:_ Include tenant scoping in the update, e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))). Require the caller to provide tenantId and enforce it in the WHERE clause to prevent cross-tenant writes.
- **server/agent-eval.ts:218** — runEval updates agent_evals with WHERE id = ${evalId} but does not constrain by tenant_id, allowing an update to an eval row belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE agent_evals ... WHERE id = ${evalId} AND tenant_id = ${tenantId}. Ensure tenantId is validated and passed through the call path so updates are always tenant-scoped.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs from tenant-scoped table `ai_insights` without a tenant_id filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = PENDING_PLAN_MARKER LIMIT 50). This reads insights for all tenants.
  - _Fix:_ If the sweep must operate per-tenant, iterate over tenants and run the query with WHERE tenant_id = ${tenantId}. If the sweep must legitimately operate globally, add explicit justification and strong access control; otherwise add a tenant_id filter to the SELECT to enforce tenant isolation.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights runs UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope} but when tenantId is undefined the UPDATE lacks tenant scoping and will update rows across all tenants.
  - _Fix:_ Avoid executing tenant-ungoverned UPDATEs. Require a tenantId argument or explicitly loop per-tenant and include WHERE tenant_id = ${tenantId}. If a global sweep is intended, ensure it's restricted to an operator-only path and document the privileged nature of the operation.
- **server/agentic-features.ts:901** — mission_experiments query builds an ARRAY literal via string interpolation: sequence_id = ANY(${`{${seqIds.join(",")}}`}::int[]). The array is embedded as a raw string expression constructed from runtime values, which can lead to injection or unescaped SQL if the values are not fully trusted/sanitized.
  - _Fix:_ Avoid building Postgres array literals via string concatenation. Use a parameterized int[] bound by the driver (e.g. SQL parameter for an int[] or drizzle's sql.array helper), e.g. AND sequence_id = ANY(${sql.array(seqIds, 'int')}). This ensures values are passed as parameters instead of being inlined into SQL text.
- **server/agentic-features.ts:1021** — classifyReply updates outreach_enrollments with WHERE id = ${row.id} but does not include tenant_id in the WHERE clause, enabling cross-tenant updates if an id is guessed or ownership changed.
  - _Fix:_ Include tenant scoping on the UPDATE: add AND tenant_id = ${params.tenantId} to the WHERE clause. Always require and validate tenant context for updates to tenant-scoped tables.
- **server/agentic/approvals.ts:134** — expireStaleApprovals performs a bulk UPDATE on agentApprovals WHERE status = 'pending' AND expiresAt < now() without a tenant_id filter, which will expire pending approvals across all tenants in one statement.
  - _Fix:_ Scope expiry per-tenant or iterate tenants and apply the UPDATE with WHERE tenant_id = ${tenantId} AND status = 'pending' AND expiresAt < ... . If a global sweep is intended, ensure it's run with proper operator privilege and audit logging; otherwise require tenant scoping for the UPDATE.
- **server/agentic/department-budgets.ts:143** — SELECT queries tenant-scoped table without a tenant filter. The code executes `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` (no WHERE tenant_id = ...) which reads rows across all tenants from a tenant-scoped table.
  - _Fix:_ If this is unintended, add a tenant filter (e.g. `WHERE tenant_id = ${tenantId} AND ...`) so every read is constrained to the caller's tenant. If a cross-tenant admin sweep is truly required, make the admin-only nature explicit (validate that the caller is an authorized platform admin), document/justify the global scan, and ensure appropriate auditing — or iterate per-tenant and call the existing per-tenant budget check to avoid an unconstrained SELECT.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT on tenant-scoped tables — the candidate query scans agent_runs (joined to agent_approvals) without constraining by tenant_id. This allows a background sweep to read rows for all tenants (cross-tenant data exposure / isolation violation).
  - _Fix:_ Constrain the query to a specific tenant_id (or otherwise enforce an explicit admin-only, audited path). For example, add a WHERE r.tenant_id = ${tenantId} (or iterate tenants and run per-tenant queries), or implement Postgres RLS for strong enforcement. If the intent is to safely scan across tenants, make that explicit, restrict to an admin context, and add auditing/authorization checks so the scan cannot leak tenant data.
- **server/agentic/wake-scheduler.ts:142** — Global UPDATE on tenant-scoped table `agent_wake_schedules` uses an unfiltered subselect to pick ids: the inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE SKIP LOCKED) does not constrain by tenant_id. This updates rows across all tenants without an app-level tenant filter.
  - _Fix:_ Process wakes per-tenant (accept a tenantId and add WHERE tenant_id = ${tenantId}) or include tenant_id in both the inner SELECT and the outer UPDATE. If this function must be multi-tenant, explicitly iterate tenants and claim rows scoped to each tenant, or add an explicit tenant column predicate to the subselect/UPDATE so every row mutation is guarded by an application-level tenant filter.
- **server/auth.ts:1331** — Maintenance DELETE on tenant-scoped table `auth_sessions` deletes expired sessions across all tenants (DELETE FROM auth_sessions WHERE expires_at < ${now}) with no tenant_id filter.
  - _Fix:_ If the maintenance task should be global, ensure it runs only in a trusted platform-admin context and the caller is clearly authorized. Prefer adding a tenant_id predicate when called from tenant-scoped contexts, or centralize global maintenance into an explicitly documented and auditable platform-admin job. At minimum, add an audit/event-log entry recording scope and initiator before performing global deletes.
- **server/auth.ts:1335** — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' deletes rows across all tenants with no tenant_id filter (global cleanup of legacy sessions).
  - _Fix:_ As above: run this only from a clearly privileged platform maintenance path, or scope by tenant_id when invoked from tenant-scoped code. Prefer an explicit migration script run under ops control rather than an unattended cross-tenant DELETE hidden in runtime boot code.
- **server/auto-transcript.ts:128** — SELECT DISTINCT ... WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 runs a subquery against tenant-scoped table `messages` without constraining by tenant_id. This can read message rows across tenants because the subquery does not enforce the conversation's tenant.
  - _Fix:_ Constrain the subquery to the conversation's tenant (e.g. AND tenant_id = c.tenant_id) or rewrite as a join that ensures messages.tenant_id = c.tenant_id. Always include an explicit tenant filter when querying tenant-scoped tables, even in subqueries.
- **server/auto-transcript.ts:153** — Query selects rows where (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 but the subquery against `messages` lacks a tenant_id constraint. This allows cross-tenant message reads when evaluating linked conversations.
  - _Fix:_ Add an explicit tenant constraint in the messages subquery (e.g. WHERE conversation_id = c.id AND tenant_id = c.tenant_id) or join messages with conversations and enforce messages.tenant_id = c.tenant_id. Ensure every SELECT against tenant-scoped tables includes tenant scoping.
- **server/channel-routing.ts:30** — listChannelRoutes performs a JOIN against the personas table (tenant-scoped) but does not constrain the join by tenant_id. This SELECT (cr.*, p.name ...) can expose persona rows from other tenants because there is no WHERE p.tenant_id = <caller tenant>.
  - _Fix:_ Add tenant scoping to the query. Either make channel_routes tenant-scoped and filter by channel_routes.tenant_id = tenantId, or constrain the join to only personas owned by the caller (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}). Ensure every read of personas includes an explicit tenant_id filter.
- **server/channel-routing.ts:53** — setChannelRoute inserts/updates a persona_id provided by the caller into a global channel_routes table without verifying that the persona belongs to the caller's tenant. A tenant could associate another tenant's persona_id with their channel.
  - _Fix:_ Before inserting/updating, verify ownership of the persona_id: SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. Alternatively, make channel_routes tenant-scoped (add tenant_id column, require tenant_id on INSERT) and always include tenant_id in WHERE clauses for reads/writes. Do not trust caller-supplied foreign IDs without explicit ownership checks.
- **server/data-protection.ts:149** — SELECT from conversations lacks a tenant_id constraint: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` queries a tenant-scoped table across all tenants.
  - _Fix:_ Scope the query to a tenant (or iterate per-tenant). For example, add `AND tenant_id = ${tenantId}` or run the purge inside a tenant-scoped transaction (withTenantTx) so every read is explicitly constrained to a single tenant. Do NOT rely on downstream logic to re-check ownership — every read against tenant-scoped tables must include the tenant predicate.
- **server/dfy-intake.ts:50** — SELECT from tenant-scoped table dfy_intake_forms without constraining tenant_id (getDfyIntakeByToken queries by token only). This allows retrieving another tenant's intake row by token.
  - _Fix:_ Include tenant scoping on the lookup or require the caller to supply/derive tenantId. e.g. .where(and(eq(dfyIntakeForms.token, token), eq(dfyIntakeForms.tenantId, tenantId))). If the token is intentionally public, document the risk and enforce additional checks (e.g. bind token to tenant at caller level).
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table doc_chunks uses WHERE id = ${row.id} without tenant_id. This allows updating another tenant's row by id.
  - _Fix:_ Constrain updates to the tenant: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer binding tenantId from the surrounding context to avoid accidental cross-tenant mutations.
- **server/email.ts:370** — SELECT against inbox_messages omits tenant constraint (SELECT id FROM inbox_messages WHERE message_id = ... LIMIT 1). This can cause cross-tenant deduping/leakage (a message for another tenant will stop storing a message for the current tenant).
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1) or otherwise partition the check by resolved tenantId. Ensure the poll loop resolves the tenant context before querying inbox_messages.
- **server/email.ts:415** — SELECT across inbox_messages omits tenant constraint (SELECT id, message_id FROM inbox_messages WHERE body_text IS NULL ... ORDER BY id). This reads rows for all tenants.
  - _Fix:_ Query only for a specific tenant (e.g. add AND tenant_id = ${tenantId}). If this backfill is intended to operate per-tenant, iterate tenants and run the backfill per-tenant; do not scan/update all tenants in a single unscoped query.
- **server/embeddings.ts:811** — backfillMissingKnowledgeEmbeddings SELECTs agent_knowledge rows across all tenants (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE ... LIMIT 50) without restricting tenant_id.
  - _Fix:_ Perform backfill on a per-tenant basis (iterate tenants or include a tenantId parameter) and include tenant_id = ${tenantId} in the WHERE. Never run cross-tenant SELECTs on tenant-scoped tables without explicit intent and safeguards.
- **server/embeddings.ts:842** — backfillEmbeddingVecs selects memory_entries rows across all tenants (SELECT id, embedding FROM memory_entries WHERE ...) without tenant scoping.
  - _Fix:_ Make the backfill operate per-tenant or add tenant_id = ${tenantId} to the WHERE. Ensure any maintenance read only returns rows for the intended tenant context.
- **server/embeddings.ts:858** — backfillEmbeddingVecs selects agent_knowledge rows across all tenants (SELECT id, embedding FROM agent_knowledge WHERE ...) without tenant scoping.
  - _Fix:_ Run this backfill per-tenant or add tenant_id = ${tenantId} to the WHERE. Ensure read operations on tenant-scoped tables are always scoped to a tenant in application code.
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scrapedPages' lacks a tenant_id constraint in WHERE (uses only id). This can update another tenant's row if IDs are global.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or perform the UPDATE in the same transaction with the initial tenant-scoped SELECT to guarantee ownership.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' lacks a tenant_id constraint in WHERE (uses only id). This can update another tenant's row if IDs are global.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or perform the UPDATE in the same transaction with the initial tenant-scoped SELECT to guarantee ownership.
- **server/glasses-gateway.ts:173** — UPDATE to apiKeys table (setting lastUsedAt) uses only apiKeys.id in WHERE and does not include tenant_id. apiKeys is tenant-scoped — without tenant constraint this write could affect another tenant's API key row in races or ID reuse scenarios.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or use an UPDATE that includes both id and tenantId to enforce ownership at SQL level.
- **server/google-drive.ts:880** — UPDATE on tenant-scoped table 'projects' (setting driveFolderId/driveFolderUrl) uses only projects.id in WHERE and lacks tenant_id constraint. This can modify another tenant's project row.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the UPDATE is strictly scoped to the caller's tenant.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` has no tenant_id constraint.
  - _Fix:_ Scope this query to a specific tenant or explicitly perform per-tenant work. Example: add WHERE tenant_id = ${tenantId} (and pass tenantId into runFullAudit/archiveStaleProposals), or otherwise restrict to an explicit admin-only multi-tenant code path with proper auditing/authorization before scanning/updating cross-tenant rows.
- **server/health-audit.ts:333** — UPDATE on `code_proposals` uses id = ANY(...) without constraining tenant_id — allows modifying proposals across tenants.
  - _Fix:_ Include tenant isolation in the UPDATE, e.g. UPDATE code_proposals SET ... WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}. Alternatively, resolve ids per-tenant and run the UPDATE per-tenant with tenant_id in the WHERE clause.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` lacks any tenant_id constraint (reads tenant-scoped rows globally).
  - _Fix:_ If heartbeat_tasks are tenant-scoped, query them per-tenant (WHERE tenant_id = ${tenantId}) or enumerate tenants explicitly from a trusted tenants table and then select per-tenant. Do not run a global SELECT on tenant-scoped tables without explicit admin authorization and audit trail.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a cross-tenant update with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}, or perform per-tenant updates after selecting tasks for that tenant only.
- **server/google-workspace.ts:2287** — INSERT INTO presenter_slide_images does not set or include tenant_id when writing a (likely) tenant-scoped table.
  - _Fix:_ Include the tenant identifier in the INSERT (e.g. add tenant_id column and pass tenantId into this function) or otherwise ensure the session_id is validated to belong to the caller's tenant before inserting. Use parameterized Drizzle insert API so tenantId is explicitly persisted.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a session row without constraining tenant_id — allows changing another tenant's session.
  - _Fix:_ Constrain the UPDATE by tenant_id: UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Or first validate that the sessionId belongs to the expected tenant before performing the update.
- **server/heartbeat-startup.ts:76** — Direct SQL update: UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — no tenant_id filter. This mutates a tenant-scoped table without constraining to the task's tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage API (e.g. storage.updateHeartbeatTask) which should enforce tenant scoping and authorization.
- **server/heartbeat-startup.ts:80** — Direct SQL update: UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — no tenant_id filter. Mutates tenant-scoped rows without tenant constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${t.tenantId}) or use a storage-layer update helper that enforces tenant scoping and prevents accidental cross-tenant writes.
- **server/heartbeat.ts:256** — SELECT * FROM event_log WHERE status = 'pending' ... executed with no tenant_id constraint. event_log rows contain tenant_id and this reads pending events across all tenants without an application-level tenant filter.
  - _Fix:_ Either: 1) iterate per-tenant and SELECT WHERE tenant_id = ${tenantId} so processing is explicitly scoped, or 2) if truly intended to be cross-tenant, ensure the function runs only in a secured scheduler context and add explicit auditing/authorization and comments documenting the privileged cross-tenant intent.
- **server/heartbeat.ts:1510** — SELECT created_at FROM heartbeat_logs ... executed without tenant_id constraint. heartbeat_logs are tenant-scoped and this read will return rows across tenants.
  - _Fix:_ Restrict the query to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) or use a storage helper that enforces tenant scoping. Document and protect any deliberate cross-tenant reads.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint. This writes a tenant-scoped table by id only and can affect another tenant's row with the same id.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or invoke a storage API (e.g. storage.updateHeartbeatTask) that enforces tenant ownership checks before updating approval_status.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint in delegateTaskFromChat. This modifies tenant-scoped data by id only, lacking tenant scoping/ownership verification.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a storage-layer method that enforces tenant ownership and avoids raw cross-tenant updates.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without scoping by tenant_id (no tenant filter) — the atomic claim can touch jobs from any tenant.
  - _Fix:_ If the worker must only operate within a tenant scope, add an explicit tenant_id filter (or accept a tenant parameter) so the UPDATE/SELECT is constrained. If this is intended to be a cross-tenant system worker, document and isolate the authority, and ensure handlers always validate any tenant identifiers inside job.payload against the job row's tenant_id before performing tenant-scoped reads/writes.
- **server/job-queue.ts:195** — reclaimExpiredLeases step 1 updates agent_jobs (marking rows failed_terminal) without any tenant_id constraint — affects rows across all tenants.
  - _Fix:_ Include tenant_id scoping or otherwise ensure this reclamation is authorized (e.g. run as a privileged platform operation only). Prefer scoping the UPDATE to a specific tenant when called from tenant-scoped contexts, or require an explicit admin scope parameter for global operations.
- **server/job-queue.ts:209** — reclaimExpiredLeases step 2 re-queues expired running jobs via UPDATE agent_jobs ... WHERE status='running' AND lease_until < NOW() without tenant_id filtering.
  - _Fix:_ Same as above — add tenant_id filtering for tenant-scoped calls or require explicit admin-level execution for cross-tenant bulk operations. Ensure the caller's authority is validated and document why cross-tenant updates are safe if intentional.
- **server/job-queue.ts:238** — completeJob updates agent_jobs (status -> 'succeeded') using id/status/attempts but no tenant_id in WHERE clause, allowing a caller to potentially complete a job from another tenant if ID/attempts are known.
  - _Fix:_ Include tenant_id in the WHERE clause (and/or pass tenantId as an explicit parameter) so lease-fencing includes tenant scope. Alternatively validate the job's tenant before writing terminal state.
- **server/job-queue.ts:281** — failJob reads max_attempts with SELECT max_attempts FROM agent_jobs WHERE id = ${id} without tenant scoping — a malicious call or abused job id could reveal or affect other-tenant rows.
  - _Fix:_ When looking up per-job metadata, scope queries by tenant_id or require an explicit admin-only path for unscoped reads. Prefer SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} (or have the caller supply/derive tenantId and validate it).
- **server/job-queue.ts:298** — failJob terminal UPDATE moves a job to failed_terminal (and writes error/failure_class) using id/status/attempts but does not include tenant_id in the WHERE clause.
  - _Fix:_ Add tenant_id to the UPDATE WHERE clause or otherwise verify that the writer is authorized to mutate that job's tenant. Use the job row's tenant_id (from a prior tenant-scoped read) when performing terminal updates.
- **server/job-queue.ts:318** — failJob retry/update path sets job back to pending with next_run_at but the UPDATE lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause or require the caller to pass/verify the tenant. Ensure lease-fencing and retry logic can't be abused to affect other tenants' jobs.
- **server/job-queue.ts:335** — cancelJob updates agent_jobs to status = 'cancelled' WHERE id = ${id} AND status IN (...) with NO tenant_id constraint (can cancel other-tenant jobs by id).
  - _Fix:_ Require tenant_id in the WHERE clause (or return an explicit admin-only API) so cancellation is scoped to the tenant making the request. Validate tenant ownership before mutating job rows.
- **server/job-queue.ts:452** — cancelPendingJobsForTenant builds an SQL array literal via string interpolation from user-supplied excludeKinds (excludeLiteral) and then injects it into the query text — this is a string-interpolated SQL clause built from request-derived values (SQL injection + isolation risk).
  - _Fix:_ Avoid building SQL fragments by interpolation. Use parameterized queries / pass the array as a bound parameter (e.g. an explicit Postgres text[] parameter) or use sql.join/sql.array helpers so excludeKinds are bound values rather than inlined text. If constructing a literal is unavoidable, rigorously validate/escape and prefer safer alternatives.
- **server/job-worker.ts:147** — research_code_proposal job handler reconstructs a session from job.payload including payload.tenantId and passes it to generateCodeProposal without verifying that payload.tenantId matches the job row's tenant (job.tenantId). This allows a job with a forged payload tenantId to cause actions as another tenant.
  - _Fix:_ Do not trust tenant identifiers in job.payload. Validate that any tenantId inside payload equals job.tenantId (or ignore payload.tenantId entirely and use job.tenantId). Enforce this check before any tenant-scoped reads/writes, and fail the job if mismatched.
- **server/job-worker.ts:168** — research_digest handler extracts tenantId from job.payload and calls generateResearchDigest(tenantId) without verifying it matches job.tenantId (the worker sets tenant context from job.tenantId). This lets a manipulated payload cause cross-tenant operations.
  - _Fix:_ Validate payload.tenantId === job.tenantId (or use job.tenantId only). Always derive tenant scope from the job row (trusted) rather than payload (untrusted) before invoking tenant-scoped operations.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on procedureEvolutionRuns uses only id in WHERE clause (no tenant check). This writes a tenant-scoped table row without constraining by tenant_id, allowing a runId to be updated across tenants.
  - _Fix:_ Include the tenant predicate in the WHERE clause. For example: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Use the local tenantId variable (already available in this function) when updating the run row so the update is scoped to the caller's tenant.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on procedureEvolutionRuns (noop_proposal path) uses only id in WHERE clause (no tenant check). This mutates a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Scope the update by tenant. Change the WHERE to include tenantId, e.g.: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Ensure every update to procedureEvolutionRuns includes the calling tenant's id.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE on procedureEvolutionRuns after inserting procedureEdits uses only id in WHERE clause (no tenant check). This can flip run state for a different tenant if runId collides or is attacker-controlled.
  - _Fix:_ Add a tenant guard to the WHERE clause. Example: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Always include the tenant predicate on updates to tenant-scoped tables (procedureEvolutionRuns).
- **server/lib/agent-trace.ts:125** — persistSpanClose issues UPDATE agent_trace_spans WHERE span_id = ${spanId} with no tenant_id constraint. This updates a tenant-scoped table row without verifying the tenant, violating tenant isolation (span_id alone is used to identify the row).
  - _Fix:_ Record the tenantId at span-open time and use it when closing. For example, when opening a span store tenant in a map: _openSpanTenant.set(ctx.spanId, tenantId). Then in persistSpanClose retrieve it and UPDATE with WHERE span_id = ${spanId} AND tenant_id = ${tenantId}. Remove the map entry after the close. Alternatively change persistSpanClose to accept tenantId explicitly and use it in the WHERE clause.
- **server/lib/chronicle-precision.ts:90** — Query reads columns from the `skills` table (alias `s`) via LEFT JOIN without constraining `s` to the caller's tenant. This allows disclosure of skill rows belonging to other tenants because tenant ownership is enforced only at the application level.
  - _Fix:_ Restrict the joined `skills` rows to the caller's tenant. For example, change the join to include tenant filter: LEFT JOIN skills s ON s.id = ps.promoted_skill_id AND s.tenant_id = ${tenantId}. Alternatively add a WHERE/AND condition ensuring s.tenant_id = ${tenantId}. Ensure the parameter is passed as a bound value (or use Drizzle's eq(skills.tenantId, tenantId)) so the tenant constraint is always applied.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped table `projects` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This aggregates counts across all tenants and violates the invariant that every read against tenant-scoped tables must be tenant-scoped at the application level.
  - _Fix:_ Restrict the query to the relevant tenant(s). For example, compute counts per tenant only for the snapshot tenants: SELECT tenant_id, count(*)::int AS c FROM projects WHERE tenant_id = ANY(${Array.from(wantedByTenant.keys())}::int[]) AND tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id. Or loop per tenant and query WHERE tenant_id = ${tenantId} to keep every read scoped to a caller-controlled tenant.
- **server/lib/mcp-api-keys.ts:69** — Function builds a Postgres array literal string (scopesLit) from request-derived input (params.scopes) then interpolates that string into a SQL template (VALUES (...) ${scopesLit}::text[] ...). Constructing SQL array literals via string concatenation from caller data risks injection or subtle parsing errors.
  - _Fix:_ Avoid building SQL literals by string-concatenation. Pass the scopes as a typed parameter/array to the query (use Drizzle/driver parameterization for arrays) or use a proper parameterized array cast (e.g. pass the JS array and let the driver encode it as a text[]), so user input is never embedded into raw SQL text.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions performs a SELECT on tenant-scoped table deliverable_grade_events without constraining by tenant_id (SELECT tenant_id, format, count(*) ... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format). This reads across all tenants and violates the tenant-isolation invariant.
  - _Fix:_ Restrict the aggregation to a single tenant (add WHERE tenant_id = ${tenantId}) or change the routine to iterate per-tenant and run the aggregation under each tenant context. Ensure any downstream queries use the tenant_id from a trusted source rather than relying on a global, multi-tenant SELECT.
- **server/memory-intelligence.ts:399** — UPDATE to tenant-scoped table knowledge_triples lacks a tenant_id constraint in the WHERE clause (writes row by id only).
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} AND tenant_id = ${tenantId}`) — or use an explicit Drizzle .where(eq(table.tenantId, tenantId)) check so every write is scoped to the tenant.
- **server/oauth-subscriptions.ts:915** — Query selects tenant_id and is_active from `oauth_subscriptions` for provider='google-workspace' without scoping by tenant (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This can enumerate tenant rows and leak cross-tenant information.
  - _Fix:_ Avoid unscoped reads. If the intent is to discover tenants to auto-reconnect, fetch tenant IDs from an explicit tenant registry table (that you control) or require an explicit admin/repair flow. Otherwise add a tenant filter (WHERE tenant_id = ${tenantId}) or require an explicit list of tenant IDs passed by a trusted operator.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() to interpolate a lock key: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). The argument is derived from ledgerKey and tenantId and is being injected via sql.raw(), which bypasses parameterization and risks SQL injection or other unsafe interpolation of request-derived values.
  - _Fix:_ Avoid sql.raw() with values derived from callers. Compute a numeric BigInt server-side and bind it as a parameterized value, e.g. tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), or otherwise pass a securely validated numeric param. Also validate/normalize ledgerKey and require tenantId; never directly raw-inject unvalidated user input into SQL.
- **server/pace-control.ts:85** — checkPace queries the tenant-scoped table `heartbeat_logs` without any tenant_id filter (COUNT(...) FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). This returns global counts across tenants, violating tenant isolation.
  - _Fix:_ Require a tenantId parameter for pace checks or add a WHERE tenant_id = ${tenantId} clause. If the intent is a global system-wide cap, make the administrative intent explicit and restrict the call to a privileged admin path; otherwise scope reads to the calling tenant.
- **server/pace-control.ts:140** — getPaceSnapshot queries `heartbeat_logs` without scoping by tenant (SELECT COUNT(...) FROM heartbeat_logs WHERE created_at > ...). This returns cross-tenant usage metrics and breaches the invariant that tenant-scoped tables must be filtered per caller tenant.
  - _Fix:_ Scope the snapshot to a tenant by adding WHERE tenant_id = ${tenantId} (and require tenantId as an argument). If an operator-level global snapshot is required, make it an explicitly privileged endpoint and do not reuse tenant-facing functions for global queries.
- **server/persona-export.ts:33** — exportPersona performs SELECT * FROM personas WHERE id = ${personaId} with NO tenant constraint. This reads a persona row without verifying ownership by the caller's tenant (foreign id taken from caller input but no tenant check).
  - _Fix:_ Always verify that the persona belongs to the caller's tenant before returning or exporting it. Add AND tenant_id = ${tenantId} to the SELECT, or perform a join/ownership check and reject if the persona does not belong to the provided tenantId.
- **server/plan-executor.ts:792** — resumeStuckPlans() performs a global UPDATE on the tenant-scoped plans table without constraining by tenant_id (UPDATE plans SET status='approved' ... WHERE status='executing' AND updated_at < ...). This writes across all tenants' rows.
  - _Fix:_ Restrict the UPDATE to a specific tenant context (e.g. WHERE tenant_id = <admin/target tenant> AND ...) or iterate per-tenant and run the UPDATE per-tenant. If this is intended to be a global admin operation, require an explicit admin authorization check and document it; prefer explicit tenant-scoped operations to avoid accidental cross-tenant writes.
- **server/plan-rollout-simulator.ts:246** — When persisting a simulation, the code embeds JSON via nested sql templates: ${sql`${JSON.stringify(steps)}::jsonb`} and ${sql`${JSON.stringify(weak)}::jsonb`}. This creates a SQL fragment that can effectively inject request-derived data into the query rather than using a safe parameterized value.
  - _Fix:_ Use parameterized bindings for JSON values (e.g. VALUES (..., ${JSON.stringify(steps)}::jsonb, ..., ${JSON.stringify(weak)}::jsonb) within the outer sql template) instead of constructing nested sql fragments. Ensure any user/request-derived data is passed as query parameters (not raw SQL) so the DB driver parameterizes the content and prevents injection and unintended interpretation.
- **server/process-governor.ts:898** — UPDATE agent_desks executed without constraining by tenant_id (UPDATE ... WHERE id = ${desk.id}). This updates a tenant-scoped table using only the row id.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or otherwise verify the row belongs to the tenant before modifying it.
- **server/process-governor.ts:1235** — UPDATE governance_rules executed without tenant constraint (UPDATE governance_rules ... WHERE id = ${rule.id}). This mutates a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) so that only rules belonging to the caller's tenant are modified.
- **server/project-brain.ts:349** — SELECT conversations WHERE c.project_id = ${proj.id} is executed without tenant constraints (conversations likely tenant-scoped or should be validated through project tenant).
  - _Fix:_ Ensure conversation reads are tenant-scoped — either join on/require conversation.tenant_id = project.tenant_id or include AND c.tenant_id = ${tenantId}, and validate project/conv ownership before using their ids.
- **server/project-brain.ts:359** — SELECT messages WHERE conversation_id = ${conv.id} is executed without tenant scoping/ownership verification for the conversation (reads message rows across tenants).
  - _Fix:_ Constrain messages reads by tenant (e.g. join conversations and add c.tenant_id = ${tenantId} or WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise verify the conversation belongs to the caller's tenant before reading messages.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` without constraining by tenant_id. The update uses only WHERE id = ${r.id}, which can modify a row belonging to a different tenant if ids are discovered via an unscoped SELECT.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) to enforce per-tenant isolation. If the code legitimately processes rows across tenants (global runner), ensure the runner proves/records privilege and still always includes tenant_id in WHERE for any modifying statements to prevent cross-tenant tampering.
- **server/research-engine.ts:169** — UPDATE against tenant-scoped table 'research_sessions' performed without any tenant_id constraint. The cleanupZombieSessions() query (UPDATE research_sessions ... WHERE status = 'running' AND started_at < $staleThreshold) will modify rows across all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant and run the cleanup in each tenant context. If the intent is global maintenance, require an explicit, authenticated admin tenant-scoped operation and log/authorize it. Never issue cross-tenant UPDATE/DELETE/SELECTs without an explicit tenant_id filter; ensure any Map-derived ids are validated/parameterized (as done with sql.join) and avoid relying on unscoped global updates.
- **server/routes.ts:179** — Unscoped SELECT from tenant-scoped table `file_storage` during startup restore (no tenant_id constraint).
  - _Fix:_ Scope the restore to a specific tenant or iterate tenants and perform per-tenant restores. Add a tenant filter (e.g. .where(and(eq(file_storage.tenantId, tenantId), ...))) or otherwise ensure only data belonging to an explicitly-authorised tenant is read and restored to disk.
- **server/routes.ts:970** — SELECT from tenant-scoped table `file_storage` without tenant constraint to fetch ALL owners of a basename (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))).
  - _Fix:_ Avoid returning or scanning rows across all tenants. Prefer checking ownership with a tenant-constrained query (e.g. where filename = X AND tenant_id = authedTenantId) or otherwise use a capability (signed URL) that binds the filename to the tenant. If you truly need to detect basename collisions, ensure the operation is performed under strong platform-admin authority and/or uses a safe, audited migration process rather than an unauthenticated or per-request lookup.
- **server/routes.ts:4542** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The code inserts: INSERT INTO project_notes (project_id, note, author) VALUES (...) without passing tenant_id; project records are tenant-owned and project_notes is expected to be tenant-scoped, so this violates the invariant that every INSERT into tenant-scoped tables must set tenant_id.
  - _Fix:_ Include the tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})). Alternatively use a storage-layer helper that enforces and populates tenant_id after verifying ownership of projId. Ensure the tenantId used is the authenticated caller's tenant and that ownership of the referenced projectId was validated beforehand.
- **server/routes/conversations.ts:236** — SELECT joins `conversations` and `projects` but omits tenant filter for the `conversations` table (SELECT c.project_id FROM conversations c JOIN projects p ... WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}). The query constrains only projects by tenant_id, not conversations.
  - _Fix:_ Add an explicit tenant filter on conversations, e.g. WHERE c.id = ${id} AND c.tenant_id = ${conv.tenantId ?? tenantId} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}. Always enforce tenant scope on every referenced tenant-scoped table in SQL.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` is missing a tenant_id predicate. The code does `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `AND tenant_id = ${tenantId}`, allowing a write to a row outside the caller's tenant context.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: `await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`);` — or use Drizzle's db.update(...).where(eq(inbox_messages.id, msg.id), eq(inbox_messages.tenantId, tenantId)). This ensures tenant-scoped enforcement and prevents cross-tenant modification.
- **server/routes/projects.ts:596** — UPDATE projects statement omits tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) — SQL against tenant-scoped table has no tenant predicate.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. add AND tenant_id = ${tenantId}: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:664** — UPDATE projects statement omits tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) — SQL against tenant-scoped table has no tenant predicate in the multipart file-upload handler.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. add AND tenant_id = ${tenantId}: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:548** — INSERT INTO project_notes does not include tenant_id in VALUES (INSERT INTO project_notes (project_id, note, author) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'})) — tenant_id must be set explicitly on inserts to tenant-scoped tables.
  - _Fix:_ Include tenant_id explicitly in the INSERT and/or derive it server-side from the authenticated tenant: INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ${req.body.note}, ${req.body.author || 'user'}) RETURNING *.
- **server/routes/projects.ts:589** — INSERT INTO project_files (base64 path) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)) — tenant-scoped row inserted without tenant ownership asserted in the row.
  - _Fix:_ Pass tenant_id explicitly in the INSERT and derive from authenticated tenant: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ... ) RETURNING *.
- **server/routes/projects.ts:656** — INSERT INTO project_files (multipart upload handler) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)) — row may be created without explicit tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT and derive it from the authenticated tenant: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ... ) RETURNING *.
- **server/routes/projects.ts:735** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks an explicit tenant_id constraint — performing DELETE on tenant-scoped table without tenant predicate.
  - _Fix:_ Include tenant-based guarding in the DELETE statement, e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND EXISTS (SELECT 1 FROM projects p WHERE p.id = ${projectId} AND p.tenant_id = ${tenantId}) or add tenant_id = ${tenantId} if project_files stores tenant_id, to ensure the row belongs to the caller's tenant.
- **server/second-opinion.ts:417** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} lacks a tenant_id constraint. Even though reservationId originates from a prior insert, future callers could attempt to settle an id from another tenant if not double-checked.
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or first SELECT the reservation by id+tenant and then UPDATE by id and tenant in the same transaction to ensure the reservation belongs to the expected tenant.
- **server/second-opinion.ts:438** — UPDATE agent_cost_ledger SET cost_usd='0', operation='second_opinion:released' WHERE id = ${reservationId} has NO tenant_id constraint. Releasing by id only can affect other tenants' ledger rows.
  - _Fix:_ Include tenant scoping (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or verify ownership before performing the release update.
- **server/seed.ts:2169** — UPDATE against likely tenant-scoped table `projects` without tenant constraint: `UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL` will modify rows regardless of tenant_id.
  - _Fix:_ Restrict the UPDATE to the intended tenant, e.g. add `AND tenant_id = ${tenantId}` or use Drizzle update with .where(eq(projects.id, id), eq(projects.tenantId, tenantId)). If the intent is to change only the platform owner project, use the explicit admin tenant id constant and enforce it in the WHERE clause.
- **server/seed.ts:2239** — Unscoped SELECT from `conversations`: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)` reads a tenant-scoped table with no tenant filter, then acts on the results (inserting links), which can cross tenant boundaries.
  - _Fix:_ Add a tenant filter to the SELECT (e.g. `WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...`) and validate that the conversation and project belong to the same tenant before inserting into `project_conversations`. Prefer using Drizzle .where(eq(conversations.tenantId, tenantId)) for clarity and safety.
- **server/seed.ts:3634** — DELETE FROM heartbeat_tasks is executed without constraining by tenant_id. heartbeat_tasks is tenant-scoped (idx_heartbeat_tasks_tenant_id exists), so this DELETE can remove rows across all tenants.
  - _Fix:_ Constrain the DELETE to the intended tenant. For example: DELETE FROM heartbeat_tasks WHERE tenant_id = <tenantId> AND enabled = false AND description LIKE ... AND type != 'maintenance_script'. In seed/system-only paths, explicitly use the system/ADMIN tenant id (e.g. tenant_id = ADMIN_TENANT_ID). Prefer parameterized Drizzle filters (where(eq(heartbeatTasks.tenantId, tenantId))) rather than raw SQL interpolation.
- **server/seed.ts:3662** — UPDATE memory_entries replaces text (REPLACE(fact, 'Manjaro', 'wellness-program')) without any tenant_id restriction. memory_entries is tenant-scoped (idx_memory_entries_tenant_id exists), so this will run across all tenants and violate tenant isolation.
  - _Fix:_ Add an explicit tenant_id constraint to the UPDATE (e.g. AND tenant_id = <tenantId>) or run this per-tenant in a loop. Use Drizzle/parameterized filters (where(eq(memoryEntries.tenantId, tenantId))) to ensure the change only affects the intended tenant(s). Avoid blanket updates across tenant-scoped tables.
- **server/seed.ts:3688** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped (idx_conversations_tenant_id exists); this updates rows for all tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE tenant_id = <tenantId> AND model = 'gpt-4.1') or iterate per tenant. Use Drizzle's where(eq(conversations.tenantId, tenantId)) to enforce application-level tenant isolation.
- **server/seed.ts:3947** — UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id} executes without a tenant_id constraint. agent_knowledge is tenant-scoped (idx_agent_knowledge_tenant_id exists). Even if the row was selected earlier with tenant_id = 1, subsequent writes must still include the tenant check to prevent cross-tenant modification.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = <tenantId>) or use Drizzle's where(eq(agentKnowledge.tenantId, tenantId)). Always verify ownership / include tenant constraint on any write that references a row id obtained earlier.
- **server/self-reflection.ts:366** — UPDATE on tenant-scoped table `memory_entries` updates WHERE id = ... with no tenant_id check (UPDATE memory_entries SET ... WHERE id = <id>) — this can modify another tenant's memory entry if the id is known or ownership changed.
  - _Fix:_ Include the tenant constraint on the UPDATE (for example: WHERE id = ${id} AND tenant_id = ${tenantId}) or use an EXISTS(...) check that confirms the record belongs to the tenant before updating. Ensure all writes to tenant-scoped tables always include tenant_id in the predicate.
- **server/skillify.ts:221** — Query reads from the 'personas' table using persona id without constraining by tenant_id (missing tenant ownership check). This allows cross-tenant disclosure of persona records.
  - _Fix:_ Verify the persona belongs to the same tenant before reading it. Add a tenant constraint to the query (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))) or use a tenant-scoped accessor that enforces ownership. If this lookup must be cross-tenant for an admin flow, make the admin intent explicit and protect the code path with an admin guard.
- **server/stability-watchdog.ts:177** — SELECT from 'heartbeat_logs' (recent error aggregation) is executed with no tenant_id constraint — this reads logs across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate tenants and run per-tenant, tenant-scoped queries. If this is intended to be a system-wide admin operation, make that explicit (admin-only guard) and use an API that documents and audits cross-tenant reads.
- **server/storage.ts:300** — Handler `createMessage` reads the `conversations` row by id without scoping the SELECT to the caller's tenant. A caller could supply a foreign conversation_id and cause an out-of-tenant read.
  - _Fix:_ Require/validate the caller's tenant and include it in the WHERE clause (AND conversations.tenant_id = <callerTenant>) before using the row. Alternatively require the API layer to pass a validated tenantId and use eq(conversations.tenantId, tenantId) in the Drizzle query (fail-closed if missing).
- **server/storage.ts:601** — INSERT into tenant-scoped table `memory_entries` with db.insert(memoryEntries).values(data) without validating that `data.tenantId` is an explicit positive integer. The schema is NOT NULL with no default; missing/invalid tenantId can lead to unscoped writes or DB errors.
  - _Fix:_ Fail-closed: validate data.tenantId at runtime (Number.isInteger && > 0) and throw if missing/invalid before inserting. Prefer strongly typed callers but enforce at DB boundary.
- **server/storage.ts:624** — UPDATE on tenant-scoped table `memory_entries` (set status='superseded') may be executed without a tenant_id constraint when tenantId is omitted (tenantScope can be undefined). This produces an unscoped mutation risk.
  - _Fix:_ Require tenantId for destructive/semantic operations or use requiredTenantScope(memoryEntries.tenantId, tenantId) so the update always includes eq(memory_entries.tenant_id, tenantId). If intentional, make this explicit and admin-only.
- **server/storage.ts:656** — INSERT into tenant-scoped table `conversation_facts` is performed (db.insert(conversationFacts).values(data)) without validating that `data.tenantId` is present/valid. This can create unscoped writes if caller omits tenantId.
  - _Fix:_ Validate data.tenantId is a positive integer before inserting and fail-closed if missing/invalid. Ensure all write paths into tenant-scoped tables enforce tenant ownership at the storage boundary.
- **server/storage.ts:694** — UPDATE on tenant-scoped table `conversation_facts` uses WHERE id IN (<victim ids>) without including tenant_id in the UPDATE predicate. Although victims were selected earlier with tenant-scoped filters, the UPDATE itself is unqualified and may affect rows incorrectly if id semantics change.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or use a JOIN/update with the tenant validated in the same statement. Prefer always including tenant predicates in UPDATE/DELETE statements.
- **server/storage.ts:892** — updateMemoryEmbedding executes db.update(memoryEntries).set({ embedding }).where(eq(memoryEntries.id, id)) with no tenant_id constraint. This allows updating a memory row by id regardless of tenant ownership.
  - _Fix:_ Require caller to pass tenantId and include it in the WHERE clause (and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId))). Use assertValidTenantId(tenantId) to validate before the update.
- **server/storage.ts:915** — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` without a tenant filter (returns rows across all tenants).
  - _Fix:_ Restrict this to admin-only with explicit admin tenant or accept a TenantScope param. Prefer requiredTenantScope(memoryEntries.tenantId, tenantId) so callers must opt-in to ALL_TENANTS explicitly.
- **server/storage.ts:921** — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` without any tenant constraint, returning cross-tenant rows.
  - _Fix:_ Require a tenant scope (or explicit ALL_TENANTS sentinel) and use requiredTenantScope(agentKnowledge.tenantId, tenantId) in the WHERE clause to enforce tenant isolation.
- **server/storage.ts:929** — archiveExpiredMemories performs a raw UPDATE on `memory_entries` with no tenant scoping (UPDATE memory_entries SET status='archived' WHERE ...). This will archive rows for all tenants.
  - _Fix:_ Either run per-tenant (loop over tenants) or add AND tenant_id = ${tenantId} and require admin intent. Prefer explicit, auditable admin-only operations rather than silent cross-tenant mutations.
- **server/storage.ts:945** — archiveStaleMemories runs a raw UPDATE on `memory_entries` without tenant_id constraint, possibly affecting all tenants' rows.
  - _Fix:_ Same as above: require explicit tenant scoping or restrict to admin-only with explicit checks. Use parameterized queries that include eq(memory_entries.tenant_id, tenantId) when appropriate.
- **server/storage.ts:1068** — fixStaleBackupSchedules executes a global UPDATE on `heartbeat_tasks` without any tenant scoping, modifying rows across all tenants.
  - _Fix:_ This maintenance operation should be explicitly admin-only or, if intended to be global, be documented and gated. Prefer performing tenant-scoped updates per-tenant or requiring an explicit ALL_TENANTS sentinel and admin authorization.
- **server/storage.ts:1085** — markHeartbeatTaskRun updates heartbeat_tasks .where(eq(heartbeatTasks.id, id)) with no tenant predicate. If heartbeat_tasks is tenant-scoped, this allows modifying any tenant's task by id without validating caller's tenant.
  - _Fix:_ Require and assert the tenantId or use a WHERE clause combining task id and tenant id (and(eq(heartbeatTasks.id, id), eq(heartbeatTasks.tenantId, tenantId))).
- **server/surprise-scorer.ts:93** — UPDATE against tenant-scoped table felix_proposals omits tenant_id in WHERE clause (`WHERE id = ${proposalId} AND args_embedding IS NULL`). This allows updating a row by id without constraining to the caller's tenant.
  - _Fix:_ Pass tenantId into stampArgsEmbedding (or otherwise derive/validate the tenant) and include tenant_id in the WHERE clause. e.g. `UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL` (or the Drizzle equivalent `where(and(eq(table.id, proposalId), eq(table.tenantId, tenantId), isNull(table.argsEmbedding)))`).
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table custom_tools filters only on name and omits tenant_id (`.where(eq(customTools.name, parsed.name))`). Reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Include tenant scoping in the query (e.g. `.where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))`). If name is already namespaced, still enforce tenant_id to follow the single-source-of-truth tenant-guard invariant.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table custom_tools by id (`.where(eq(customTools.id, inserted.id))`) lacks a tenant_id constraint. Reads by id must still be tenant-scoped to enforce isolation.
  - _Fix:_ Constrain by tenant_id as well: `.where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))`, or look up by (id, tenantId) or use a transaction/returned row that contains tenantId and validate it before further reads.
- **server/tool-learning.ts:365** — UPDATE to increment usageCount on custom_tools uses only the PK in WHERE (`.where(eq(customTools.id, tool.id))`) without scoping by tenant_id.
  - _Fix:_ Constrain the update to the tenant: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Ensure executeCustomTool has/uses the tenantId when performing any read/write on custom_tools.
- **server/tools.ts:2923** — SELECT against tenant-scoped table project_files (dupCheck) lacks an explicit tenant_id filter. Queries such as SELECT id FROM project_files WHERE project_id = ${params.id} ... do not include tenant_id = ${tenantId}.
  - _Fix:_ Add an explicit tenant_id constraint to the WHERE clause (e.g. WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND ...). If you rely on validated project_id, still include tenant_id to follow the invariant and avoid accidental cross-tenant reads.
- **server/tools.ts:2963** — SELECT against tenant-scoped table project_conversations (existing check) lacks an explicit tenant_id filter. The query SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} has no tenant_id condition.
  - _Fix:_ Constrain the select with tenant_id (e.g. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) or join through a project/conversation row already scoped to the tenant. Always add tenant_id checks on reads of tenant-scoped tables.
- **server/tools.ts:4496** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id in the inserted columns/VALUES.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, ...) VALUES (${tenantId}, ${projectId}, ...)) and ensure tenantId is the verified caller tenant (e.g. params._tenantId) — even if ownership was asserted, always write tenant_id to the row.
- **server/tools.ts:4499** — INSERT into tenant-scoped table 'project_files' does not include tenant_id in the inserted columns/VALUES.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ...)) and use the verified tenant (params._tenantId) after an ownership check (assertProjectInTenant).
- **server/tools.ts:6389** — INSERT into tenant-scoped table 'project_files' (audio upload path) does not include tenant_id in the inserted columns/VALUES.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${params.project_id}, ...)). Continue to verify project ownership (assertProjectInTenant) but still write tenant_id as required.
- **server/tools.ts:6930** — INSERT into tenant-scoped table 'project_files' (video production path) does not include tenant_id in the inserted columns/VALUES.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${params.project_id}, ...)). Verify project ownership before inserting and always set tenant_id on tenant-scoped tables.
- **server/tools.ts:6930** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT INTO project_files(...) VALUES(...) call omits tenant_id (even though caller asserted project ownership earlier).
  - _Fix:_ Include tenant_id in the inserted columns and set it to the caller's tenant (e.g. tenant_id = ${params._tenantId}). Prefer using Drizzle insert helpers (or add tenant_id in the SQL template) and keep the prior assertProjectInTenant check as defense-in-depth.
- **server/tools.ts:8902** — UPDATE on tenant-scoped table 'proposed_skills' is constrained only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) — missing tenant_id filter. This can allow cross-tenant updates if ids collide or an attacker obtains another tenant's id.
  - _Fix:_ Add the tenant constraint to the WHERE clause, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) (or the equivalent Drizzle condition using the current caller tenant variable). Always include tenant_id in UPDATE/DELETE WHERE conditions for tenant-scoped tables.
- **server/tools.ts:8945** — UPDATE on tenant-scoped table 'proposed_skills' (marking accepted) lacks a tenant_id constraint. The call db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates by id only.
  - _Fix:_ Restrict the UPDATE to the caller's tenant as well, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Ensure all updates to tenant-scoped tables include tenant_id equality.
- **server/tools.ts:8962** — UPDATE on tenant-scoped table 'proposed_skills' (marking rejected) is missing a tenant_id WHERE clause. The update uses only the row id in the WHERE predicate.
  - _Fix:_ Add tenant isolation to the WHERE clause, for example .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Enforce this pattern (id + tenant_id) for all UPDATE/DELETE statements on tenant-scoped tables.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' omits tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This allows modifying a row by id without restricting the operation to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Include tenant_id = ${tenantId} (ctx.tenantId) in the WHERE clause, e.g. wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); and preferably use RETURNING or check affected row count to detect/fail when no row was updated (indicating mismatch/ownership failure). Ensure all UPDATE/DELETE statements across the codebase follow the same pattern.
- **server/tools/domains/knowledge/handlers.ts:84** — Unscoped UPDATE against tenant-scoped table knowledge_triples — the code updates rows by id without constraining by tenant_id (UPDATE knowledge_triples SET valid_until = ${validFrom} ... WHERE id = ${row.id}). Although the id came from a prior tenant-scoped SELECT, the invariant requires every write to include an explicit tenant filter to prevent cross-tenant modifications.
  - _Fix:_ Include the tenant constraint on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or perform the UPDATE via Drizzle's query builder with eq(knowledge_triples.tenant_id, tenantId). Always ensure every UPDATE/DELETE on tenant-scoped tables includes AND tenant_id = <caller tenant>.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` is performed with WHERE id = ${id} but omits a tenant_id constraint.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or use Drizzle's .where(eq(table.tenantId, tid)) so every write is explicitly scoped to the caller's tenant and cannot affect rows from other tenants even if an id is guessed or reused.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) executes without any tenant_id constraint, allowing cross-tenant modification if ids are tampered with or mis-derived.
  - _Fix:_ Restrict the UPDATE by tenant as well (e.g. UPDATE ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}) or filter the ids per-tenant before issuing the UPDATE. Prefer parameterized Drizzle APIs with an explicit tenant equality predicate to ensure every write includes tenant scoping.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` is performed with WHERE id = ${id} but omits a tenant_id constraint (record_strategic_win path).
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or use Drizzle .where(eq(table.tenantId, tid)). Do not rely solely on a previously-selected id — always assert tenant ownership in the write statement.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without any tenant_id constraint (query returns pairing rows across all tenants).
  - _Fix:_ Do not scan tenant-scoped tables across all tenants. Constrain the query to the owning tenant at the SQL level (e.g. add WHERE tenant_id = ${tenantId} or join against conversations to validate and filter by conversation. If the function needs to discover the tenant for a phone, perform a single SQL query that both validates the pairing and asserts that the referenced conversation belongs to the same tenant (for example: join agent_knowledge → conversations and require conversations.tenant_id = agent_knowledge.tenant_id or content->>'tenantId'), or introduce/require an explicit tenant_id column on pairings and use it in the WHERE clause. Always enforce tenant scoping in SQL rather than relying on later application-side filtering.
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` by id with NO tenant_id filter. Reading a tenant-scoped row without scoping allows cross-tenant data exposure if unchecked.
  - _Fix:_ When reading a referenced conversation, include tenant scoping in the WHERE clause (e.g. SELECT tenant_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${expectedTenantId} LIMIT 1) or otherwise perform the ownership check atomically in SQL (join or a single query that ensures the conversation belongs to the expected tenant). This ensures the DB read is constrained by tenant at the SQL level instead of returning arbitrary tenant rows to be validated later in application code.
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger omits tenant constraint — WHERE id = ${reservationId} has no tenant_id check. This allows updating another tenant's reservation row by id.
  - _Fix:_ Require and pass the owning tenantId into settleVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or use Drizzle: .where(and(eq(agent_cost_ledger.id, reservationId), eq(agent_cost_ledger.tenantId, tenantId))). Validate the reservation belongs to the tenant before updating.
- **server/venture-discovery/budget.ts:169** — UPDATE against tenant-scoped table agent_cost_ledger omits tenant constraint — releaseVentureReservation runs UPDATE ... WHERE id = ${reservationId} with no tenant_id filter.
  - _Fix:_ Accept/derive the tenantId for the reservation and include tenant_id in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Prefer passing tenantId from the caller and checking ownership before making the UPDATE.
- **server/wiring-invariants.ts:244** — SELECT from tenant-scoped table `tool_performance` (count of recent successes) executed without any tenant_id constraint.
  - _Fix:_ If this query is intended to be tenant-scoped, add an explicit WHERE tenant_id = ${tenantId} (or pass ADMIN_TENANT_ID when performing global admin checks). If it is intentionally global, make that explicit in code/comments and ensure callers are admin-only. Prefer parameterized/explicitly-scoped queries rather than unscoped scans of tenant tables.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM `tool_performance` executed without tenant_id filtering (global scan of a tenant-scoped table).
  - _Fix:_ Restrict this read to a tenant (WHERE tenant_id = ${tenantId}) or, if performing an admin/global health check, explicitly aggregate by tenant_id and ensure only an admin context performs it. Avoid unscoped reads on tenant tables.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM `messages` (last 14 days) executed with no tenant_id constraint — reads tenant-scoped chat history across all tenants.
  - _Fix:_ Either include WHERE tenant_id = ${tenantId} to scope to a tenant, or explicitly document/require admin context and aggregate per-tenant (e.g. GROUP BY tenant_id). Do not perform unscoped reads of tenant tables in multi-tenant deployments.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM `tool_performance` executed without tenant_id constraint (reads tenant-scoped usage data globally).
  - _Fix:_ Scope the query by tenant_id, or if the intent is a global admin check, explicitly aggregate by tenant_id and ensure the code only runs in an admin/global context. Avoid blind scans of tenant-scoped tables.
- **server/wiring-invariants.ts:366** — sql.raw SELECT COUNT/MAX FROM ${spec.table} executes against subsystem tables (e.g. knowledge_nudges, tool_performance) with the table name interpolated and no tenant_id WHERE clause — unscoped reads against tenant-scoped tables and use of raw SQL interpolation.
  - _Fix:_ Avoid raw/interpolated SQL for tenant-scoped tables. Either parameterize and add a tenant_id filter (WHERE tenant_id = ${tenantId}), or validate that spec.table is an allowed constant and, for admin-only checks, iterate per-tenant or aggregate by tenant_id. Replace sql.raw with parameterized drizzle queries when possible to avoid injection and isolation risks.
- **server/wiring-invariants.ts:417** — SELECT ... FROM `tool_performance` in tool-drift check runs without a tenant_id filter (reads per-tool aggregates across all tenants).
  - _Fix:_ Either add a tenant_id predicate to make this tenant-scoped, or explicitly perform per-tenant aggregation in admin mode. Ensure only an admin/auditing context can run unscoped diagnostics, and document/guard that requirement.
- **server/wiring-invariants.ts:535** — sql.raw SELECT COUNT() and pg_total_relation_size FROM ${tbl.name} executes against memory tables (including `messages`) without tenant_id filters — unscoped scans of tenant-scoped tables and raw string interpolation of table names.
  - _Fix:_ Do not run unscoped reads on tenant-scoped tables. Scope queries with WHERE tenant_id = ${tenantId} or perform per-tenant aggregation for admin checks. Replace raw-interpolated table names with a validated whitelist if dynamic table names are required, and prefer parameterized queries to avoid injection/isolation issues.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM `graph_memory` runs without tenant_id constraint — reads a tenant-scoped memory store globally.
  - _Fix:_ Scope to tenant_id (WHERE tenant_id = ${tenantId}) or explicitly aggregate per-tenant for admin diagnostics. Ensure the check is only run in an admin/global context and avoid unscoped reads of tenant-scoped tables.

### MEDIUM (53)

- **server/agentic-features.ts:861** — enrollInSequence checks for existing outreach_enrollments with SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but does not include tenant_id in the WHERE clause. This can produce a false positive/negative when same sequence_id or contact_email values exist across tenants.
  - _Fix:_ Include tenant scoping in the existence check: add AND tenant_id = ${params.tenantId}. Always constrain SELECTs on tenant-scoped tables to the caller's tenant to avoid cross-tenant interference.
- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' reads a tenant-scoped table (messages) using only conversation_id and no tenant_id predicate. The query is executed in a global backfill loop that enumerated conversations from multiple tenants earlier in the same function, so this read is not explicitly tenant-filtered at the SQL level.
  - _Fix:_ Either join messages to conversations and include AND m.tenant_id = ${c.tenant_id} (or a join on c.id with tenant check) or add an explicit WHERE tenant_id = ${c.tenant_id}. Even if conversation ids were obtained from an earlier tenant-scoped query, enforce the tenant in the messages query to avoid accidental cross-tenant reads if DB referential integrity is ever inconsistent.
- **server/data-protection.ts:403** — COUNT query on messages omits tenant predicate: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}` reads a tenant-scoped table without `tenant_id = <tenant>` in the WHERE clause.
  - _Fix:_ Include the tenant filter in the COUNT query, e.g. `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}` (or pass and bind the tenantId) so the existence check cannot become a cross-tenant oracle.
- **server/doc-collections.ts:136** — Subqueries in listCollections compute counts from doc_chunks without constraining tenant_id (e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id)). These subqueries leak or aggregate other tenants' data.
  - _Fix:_ Include tenant filtering inside the subqueries, e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id AND tenant_id = ${tenantId}) and similarly for the DISTINCT doc_path count.
- **server/doc-collections.ts:456** — Subqueries in getCollectionStatus aggregate doc_chunks (COUNT, SUM, etc.) without tenant constraint (e.g. SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id). This can surface metrics across tenants.
  - _Fix:_ Add tenant scoping inside each subquery: WHERE collection_id = c.id AND tenant_id = ${tenantId}. Ensure all aggregations / sub-selects on tenant-scoped tables include tenant_id.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' runs without tenant_id. messages is tenant-scoped, so this global query mixes traffic from all tenants.
  - _Fix:_ If the intent is per-tenant traffic gating, add a tenant_id predicate (AND tenant_id = ${tenantId}) and accept tenantId as input. If this is intentionally global admin telemetry, clearly separate it from tenant-scoped code and ensure it does not reuse tenant-scoped tables without explicit global intent and safeguards.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... executes without tenant constraint. tool_performance is tenant-scoped, so this counts across tenants.
  - _Fix:_ If the traffic gate should be per-tenant, add AND tenant_id = ${tenantId}. Otherwise, make the global intent explicit and ensure this code cannot be invoked in a tenant context that expects isolation.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance (in getEverInvokedTools) runs without tenant constraint. This returns names across tenants from a tenant-scoped table.
  - _Fix:_ Scope the query by tenant_id (e.g. WHERE tenant_id = ${tenantId}) when used for tenant-scoped decisions. If the goal is system-wide computation, keep it in a clearly separated admin/global path and avoid mixing with tenant-scoped flows.
- **server/glasses-gateway.ts:157** — SELECT from tenant-scoped apiKeys table is performed without a tenant_id constraint (lookup by keyHash only). Per policy every read against tenant-scoped tables must include tenant scoping at SQL level.
  - _Fix:_ Where feasible add tenant scoping to the query (e.g. include tenantId if available) or otherwise ensure post-query that the row belongs to the expected tenant and avoid leaking details. Consider storing a separate index keyed by (tenant_id, keyHash) or otherwise limiting the exposure of tenant rows during lookup.
- **server/google-drive.ts:867** — SELECT from tenant-scoped table 'projects' is performed by project id only (no tenant_id filter). Although code checks project.tenantId after the SELECT, the raw SELECT still reads another tenant's row without SQL-level scoping.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so reads are constrained at SQL level and avoid exposing another tenant's data.
- **server/google-drive.ts:1477** — SELECT from tenant-scoped table 'projects' (resolving project for uploadAndShare) is done using project id only (no tenant_id filter). The code validates project.tenantId later, but the SELECT itself is not tenant-scoped.
  - _Fix:_ Include tenant scoping in the SELECT (for example .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) to prevent cross-tenant reads and meet the requirement that every read against tenant-scoped tables is constrained by tenant_id.
- **server/google-drive.ts:1724** — SELECT from tenant-scoped table 'projects' (in searchDriveFiles when projectId given) is performed by project id only and lacks a tenant_id constraint. The code later compares proj.tenantId, but the initial read is unscoped.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the DB read is restricted to the caller's tenant and avoids any cross-tenant exposure.
- **server/heartbeat-schedules.ts:27** — SELECT DISTINCT tenant_id FROM messages scans a tenant-scoped table without being scoped to the caller tenant (reads tenant metadata by scanning content table).
  - _Fix:_ Use an explicit tenants metadata source (SELECT id FROM tenants WHERE is_active = true) to enumerate tenants rather than scanning a tenant-scoped content table. If scanning messages is intentional, run it only in a clearly-audited system/admin context and document/guard that it is a cross-tenant operation.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE ... has no tenant_id filter — reads schedules for all tenants in one query.
  - _Fix:_ Either enumerate tenants and process schedules per-tenant (add WHERE tenant_id = ${tenantId} per pass) or add tenant_id to the WHERE clause returned by the tenant enumeration. If this is intended as a global scheduler, make that explicit and gate the code behind an admin-only privilege and clear audit logging.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} performs an unscoped read on a tenant-scoped table (no tenant_id filter). Although this function is documented as an admin helper, an unscoped SELECT can be abused if invoked from a non-privileged path.
  - _Fix:_ Constrain to tenant_id when used in tenant-facing contexts. For admin use, ensure the caller is strongly authenticated/authorized and mark the function clearly as admin-only. Prefer an explicit admin-only API for cross-tenant inspection and audit logging.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor runs a cross-tenant aggregation: SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ... — this scans agent_knowledge across all tenants without per-caller tenant scoping.
  - _Fix:_ If this is intended as a privileged/platform audit job, ensure it only runs in an operator/privileged context and is not callable from tenant-scoped code paths. Alternatively, iterate tenants explicitly (and require authorization) or accept a tenant parameter so tenant-facing calls cannot enumerate other tenants' data.
- **server/lib/inbox-ingest.ts:536** — Query reads inbox_messages (alias m) in a JOIN without an explicit tenant_id constraint on m. The SELECT joins inbox_classifications c (which is filtered by c.tenant_id) to inbox_messages m but does not constrain m.tenant_id = ${tenantId}, violating the invariant that every read against a tenant-scoped table must include the caller's tenant filter.
  - _Fix:_ Add an explicit tenant filter for the joined table. e.g. include AND m.tenant_id = ${tenantId} in the WHERE clause or change the JOIN to ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId} so the inbox_messages rows are always scoped to the caller tenant.
- **server/lib/mention-injection.ts:121** — Query selects from channel_messages (cm) and LEFT JOINs personas p ON p.id = cm.from_persona_id but does not constrain personas by tenant_id. personas is a tenant-scoped table; reading p without scoping can leak persona data from other tenants.
  - _Fix:_ Restrict the joined personas rows to the tenant. For example, change the JOIN to LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId}, or add an explicit WHERE clause condition p.tenant_id = ${params.tenantId} (or fetch the persona in a separate, tenant-scoped query after verifying cm.tenant_id).
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey queries mcp_api_keys by key_prefix without a tenant_id WHERE clause. This SELECT touches a tenant-scoped table without constraining by tenant and therefore reads rows across tenants to resolve the tenant from the key.
  - _Fix:_ If this is an intended auth lookup (resolve tenant by key), document the exception and harden access patterns (minimize returned columns, avoid revealing sensitive metadata). Otherwise, restrict the lookup by tenant_id. If resolving tenant from the key is required, ensure subsequent code treats this code path as a privileged operation and perform careful auditing/logging; prefer a separate indexed lookup table keyed by prefix that is explicitly allowed to be global, or add rate-limiting/monitoring to detect cross-tenant probing.
- **server/lib/restraint-trust.ts:90** — Use of sql.raw() with a column identifier derived from request input (outcome) to build an UPDATE statement. Although the code validates the outcome against a whitelist, embedding a runtime identifier via sql.raw() risks SQL-injection / maintenance pitfalls and is flagged by policy.
  - _Fix:_ Avoid injecting identifiers into SQL at runtime. Implement the column update with explicit, parameterized branches (one UPDATE per allowed outcome/column) or a switch that maps outcomes to static SQL templates so no runtime identifier is passed to sql.raw(). Alternatively, use a well-tested library API to quote identifiers from a fixed allowlist, and keep the tenant_id WHERE clause as-is to ensure tenant-scoped updates.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is executed without a tenant_id filter (reads running rows across all tenants).
  - _Fix:_ Either restrict the query by tenant_id (WHERE tenant_id = ${tenantId}) or run the sweep explicitly in an admin-only context and ensure the caller is authorized. Prefer per-tenant queries (or an explicit admin-only code path) so application-level tenant predicates are always present for tenant-scoped tables.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` is executed without a tenant_id filter (reads in_progress rows across all tenants).
  - _Fix:_ Add tenant_id filtering (WHERE tenant_id = ${tenantId}) or otherwise ensure this cross-tenant read is strictly admin-operated and audited. Prefer per-tenant scoped queries to maintain application-enforced tenant isolation.
- **server/lib/system-probes.ts:163** — COUNT(*) query against `messages` table is executed without tenant_id filtering (global count across tenants).
  - _Fix:_ If the probe should be tenant-scoped, add WHERE tenant_id = ${tenantId}. If a global/admin metric is intended, ensure this API is admin-only and clearly documented; otherwise avoid exposing global tenant-scoped data to non-admin callers and prefer using explicitly-scoped storage helpers that accept tenant context.
- **server/persona-export.ts:39** — Skills query SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL has no tenant scoping. Tenant-specific skills may be mixed across tenants or leaked. The current query returns skills without checking tenant_id.
  - _Fix:_ Filter skills by tenant: include AND (tenant_id = ${tenantId} OR persona_id IS NULL) or otherwise ensure skill rows returned are either explicitly global (persona_id IS NULL) or belong to the requesting tenant (tenant_id = ${tenantId}). Ensure every read of a tenant-scoped table includes a tenant_id check.
- **server/plan-executor.ts:802** — resumeStuckPlans() selects pending approved plans with no tenant filter (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50). This reads plan ids across tenants and then schedules executePlan() for each id.
  - _Fix:_ Either restrict the SELECT to a specific tenant (WHERE tenant_id = <tenant>) or enumerate tenants and fetch per-tenant approved plans. If this is an intentional global boot-recovery, gate it behind an explicit admin-only path and ensure audit/logging and safeguards are in place.
- **server/process-governor.ts:399** — SELECT joins personas without constraining the personas table by tenant_id (JOIN ON p.id = ad.persona_id has no p.tenant_id = ${tenantId}).
  - _Fix:_ When joining tenant-scoped tables (e.g. personas), include an explicit tenant filter (e.g. ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) so every read against a tenant-scoped table is constrained to the caller's tenant.
- **server/process-governor.ts:891** — SELECT in restart_agent left-joins personas without constraining the personas table by tenant_id (LEFT JOIN personas p ON p.id = ad.persona_id).
  - _Fix:_ Add tenant scoping to the join (e.g. ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) to ensure persona rows are only read for the caller's tenant.
- **server/recurring-messages.ts:258** — Unscoped SELECT against tenant-scoped table `agent_knowledge` (no tenant_id filter). The cron/runner query selects rows across all tenants without a WHERE tenant_id = <tenant> constraint.
  - _Fix:_ If the intent is a global scan, make the global nature explicit and restrict subsequent operations by validating tenant ownership before any writes. Otherwise, add an application-level filter to the SELECT (e.g. WHERE tenant_id = ${tenantId}) so the query only returns rows for the caller's tenant. Prefer using an explicit flag proving the caller is a privileged system runner and document it, and keep the scan window bounded.
- **server/routes.ts:477** — DELETE from tenant-scoped table `order_lookup_codes` without tenant_id constraint (DELETE FROM order_lookup_codes WHERE expires_at < NOW()).
  - _Fix:_ Run expiration/cleanup on a per-tenant basis or run as an explicitly trusted platform/admin operation that documents and authorises global purges. Prefer: DELETE ... WHERE tenant_id = <tenant> AND expires_at < NOW(), or iterate tenants and delete per-tenant to avoid accidental cross-tenant effects.
- **server/routes.ts:468** — UPDATE on tenant-scoped table `order_lookup_codes` setting tenant_id for rows with tenant_id IS NULL but without scoping to a caller tenant. (UPDATE order_lookup_codes SET tenant_id = <...> WHERE tenant_id IS NULL)
  - _Fix:_ If this is an intentional one-time migration run by a trusted operator, ensure it runs under an explicit migration path, documented and gated. Otherwise, avoid global UPDATEs and instead perform safe per-tenant migration steps or filter rows to those you intend to modify (and log/backup changes). Consider running this via a DB migration tool rather than application runtime code.
- **server/routes.ts:618** — SELECT across `projects` (tenant-scoped) with no tenant_id constraint when backfilling Drive folders (SELECT p.id, p.name, p.tenant_id ... WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = '').
  - _Fix:_ Perform the backfill per-tenant or otherwise explicitly constrain which tenant(s) are being processed in each operation. Add eq(projects.tenant_id, tenantId) when running in the context of a single tenant, or iterate tenants and call ensureProjectFolder for each tenant after validating authorization.
- **server/routes.ts:1385** — Multiple unscoped aggregate SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) for /api/public/stats — no tenant_id filters present.
  - _Fix:_ If these global aggregates are intended only for platform admins, ensure the admin gate is strict (already present) and document the privileged nature. Otherwise, to respect tenant isolation, either add tenant scoping (WHERE tenant_id = <tenant>) for tenant-scoped views or only allow these global queries to run as an audited platform-admin maintenance operation.
- **server/routes.ts:1415** — Multiple unscoped SELECTs against tenant-scoped tables (conversations, messages, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) in /api/public/architecture — queries lack tenant_id constraints.
  - _Fix:_ Limit these queries to an explicit platform-admin context and keep that gate, or scope them per-tenant. Prefer explicit WHERE tenant_id = <tenant> when returning tenant data; for global architecture/metrics require platform admin and log/audit the use, or compute per-tenant stats in a safe, controlled maintenance job rather than ad-hoc unscoped queries.
- **server/routes.ts:497** — SELECT on `tenants` uses subqueries referencing tenant-scoped tables (conversations, projects) without tenant scoping (subqueries SELECT DISTINCT tenant_id FROM conversations/projects). These unscoped reads can cross tenant boundaries.
  - _Fix:_ When checking whether a tenant has related rows, prefer per-tenant checks (e.g. existence queries filtered by tenant) or run explicit, audited maintenance operations. If the intent is to find tenants with no associated resources, ensure the logic is run in a trusted migration/maintenance context and/or rewrite to be explicit about the intended tenant scope to avoid accidental cross-tenant effects.
- **server/routes/projects.ts:401** — INSERT INTO project_conversations inside transaction omits tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${targetProjectId}, ${conversationId})) — if project_conversations is tenant-scoped, tenant_id must be set on insert.
  - _Fix:_ If project_conversations has a tenant_id column, include it explicitly: INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${targetProjectId}, ${conversationId}, ${tenantId}). If the table relies on project FK, consider adding tenant_id for defense-in-depth and to satisfy the single-row tenant predicate invariant.
- **server/routes/projects.ts:683** — SELECT * FROM project_files WHERE project_id = ${projectId} has no tenant_id constraint — reading a tenant-scoped resource without a tenant predicate in SQL.
  - _Fix:_ Constrain the SELECT to the caller's tenant (add AND tenant_id = ${tenantId} if project_files has tenant_id), or join to projects and assert p.tenant_id = ${tenantId} (as done in other handlers) in the same query for defense-in-depth: SELECT * FROM project_files pf JOIN projects p ON p.id = pf.project_id WHERE pf.project_id = ${projectId} AND p.tenant_id = ${tenantId} ORDER BY pf.created_at DESC.
- **server/routes/projects.ts:706** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 omits tenant_id predicate — tenant ownership not enforced in SQL.
  - _Fix:_ Add tenant scoping to the query, e.g. JOIN projects p ON p.id = project_files.project_id AND p.tenant_id = ${tenantId}, or add tenant_id = ${tenantId} to the WHERE clause if project_files stores tenant_id.
- **server/seed.ts:41** — Unscoped read from tenant-scoped table `research_sessions`: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id filter.
  - _Fix:_ Constrain the query to a tenant (e.g. `WHERE tenant_id = ${tenantId}`) or otherwise scope the check to the caller's tenant. Use Drizzle's .where(eq(table.tenantId, tenantId)) or pass an explicit tenant_id parameter so the count operates only on one tenant's rows.
- **server/storage.ts:632** — touchMemoryEntries updates memory_entries with inArray(ids) and optionally omits tenant scoping when tenantId is undefined (warn-only path). This allows cross-tenant updates if callers omit tenantId.
  - _Fix:_ Require tenantId or use requiredTenantScope to fail-closed; if permissive behavior is needed, restrict to admin paths and document the risk. Prefer using eq(memoryEntries.tenantId, tenantId) in the WHERE clause.
- **server/storage.ts:1002** — getRecentDailyNotes will return daily_notes across tenants when tenantId is undefined (tenant filter is optional). This is a read of a tenant-scoped table without tenant constraint.
  - _Fix:_ Require tenantId or use requiredTenantScope for admin flows. If optional behavior is required, make it explicit and protect the API so only trusted callers can omit tenantId.
- **server/storage.ts:1045** — createHeartbeatTask uses db.execute with an interpolated SQL template containing many caller-supplied fields (including tenant_id). Use of raw SQL templates with request-derived values increases risk of SQL mishandling and obfuscates Drizzle's parameterization guarantees.
  - _Fix:_ Prefer Drizzle's insert(...) API or at minimum ensure strict validation of all interpolated values. Pass tenantId explicitly and validate it; avoid constructing long raw SQL blocks with many interpolated fields from caller input.
- **server/storage.ts:1058** — updateHeartbeatTask builds a raw SQL subselect using sql`... WHERE tenant_id = ${tenantId}` via sql template interpolation. While tenantId is validated elsewhere, using raw SQL fragments with interpolated values is brittle and flagged by policy.
  - _Fix:_ Use Drizzle predicates (requiredTenantScope/tenantScope) or parameterized Drizzle expressions instead of embedding subselect text in sql`...`. Validate tenantId and prefer eq(heartbeatTasks.tenantId, tenantId).
- **server/storage.ts:1064** — deleteHeartbeatTask includes a raw SQL subselect with interpolated tenantId (`... WHERE tenant_id = ${tenantId}`) to scope deletions. Use of raw template with request-derived values is a potential injection/ownership risk.
  - _Fix:_ Use Drizzle's API and explicit tenant predicates (e.g., .where(and(eq(heartbeatTasks.id, id), eq(heartbeatTasks.tenantId, tenantId)))) or require caller to provide validated tenantId and avoid stringy subselect fragments.
- **server/storage.ts:1099** — getHeartbeatLogs constructs a raw SQL `IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId})` fragment using sql`...` interpolation. While the code enforces requiredTenantScope earlier, embedding raw SQL with interpolated values is discouraged and flagged.
  - _Fix:_ Use Drizzle API to express the same predicate or at least ensure tenantId is validated with requiredTenantScope; prefer moving this logic into a parameterized join/condition rather than a text subselect.
- **server/storage.ts:369** — upsertMessageFeedback uses a raw SQL INSERT/ON CONFLICT statement built via db.execute(sql`...`) with many interpolated caller fields (tenant_id, conversation_id, message_id, comment, topicHint). Although tenantId is validated earlier, raw SQL with request-derived values is flagged by the policy.
  - _Fix:_ Use Drizzle's insert(...).onConflict(...) APIs or parameterize the insert via Drizzle query builders. Ensure all interpolated fields are validated and sanitized before embedding in raw SQL.
- **server/storage.ts:391** — attachCitations uses db.execute with an interpolated JSON string (JSON.stringify(citations)) and tenantId in a raw UPDATE: UPDATE messages SET citations = ${JSON.stringify(citations)}::jsonb WHERE id = ${messageId} AND tenant_id = ${tenantId}. Raw SQL with request-derived JSON and ids is risky.
  - _Fix:_ Use Drizzle's parameterized update API (db.update(...).set({ citations }).where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId)))) so values are passed as parameters and tenant scoping is explicit.
- **server/storage-helpers/project-tenant-guard.ts:32** — assertProjectInTenant builds a raw SQL query via db.execute(sql`SELECT 1 FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId} LIMIT 1`) using sql-tag interpolation of caller-provided values. Policy flags raw SQL templates carrying request-derived values.
  - _Fix:_ Prefer using Drizzle's select/from/where API with parameterized eq(projects.id, projectId) and eq(projects.tenantId, tenantId) after validating inputs. If using db.execute is required, validate inputs strictly (already done) and document rationale.
- **server/storage-helpers/project-tenant-guard.ts:44** — assertConversationInTenant similarly uses a raw sql template (SELECT 1 FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} LIMIT 1) with interpolated request-derived values; this pattern is flagged.
  - _Fix:_ Use Drizzle's parameterized select/where or at minimum ensure inputs are strictly validated (they are), and prefer not to embed raw SQL fragments with interpolated values for ownership checks.
- **server/storefront-rate-limit-store.ts:138** — recordStorefrontHit executes a db.execute(sql`SELECT ... WHERE rate_key = ${rateKey} AND expires_at > NOW()`) with `rateKey` coming from the caller. The code uses sql-tag interpolation of request-derived strings (potential injection pitfalls) and policy flags raw templates with request-derived values.
  - _Fix:_ Use Drizzle's parameterized select() API to build this query (db.select(...).from(...).where(and(eq(table.rateKey, rateKey), gt(table.expiresAt, new Date())))). Validate/normalize rateKey before using it.
- **server/storefront-rate-limit-store.ts:163** — The CTE insert uses a long raw SQL statement with interpolated values (${rateKey}, ${expiresIso}) to insert and evict rows. This is a large raw SQL blob interpolating request-derived values, flagged by policy.
  - _Fix:_ If the single-statement CTE semantics are required for correctness, validate and sanitize all interpolated values and document why raw SQL is necessary; otherwise prefer building parameterized Drizzle queries or using a prepared statement with parameters.
- **server/tools.ts:2843** — Subqueries counting rows in tenant-scoped tables (project_files, project_notes, project_conversations) do not include tenant_id filters. Example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) — no tenant constraint on the referenced table.
  - _Fix:_ Constrain these subqueries by tenant_id as well, e.g. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) (or join via projects p that are already tenant-scoped and explicitly include tenant_id in the subquery). Ensure every read against tenant-scoped tables includes tenant_id = callerTenant.
- **server/tools/domains/files/handlers.ts:941** — UPDATE against tenant-scoped table projects lacks an explicit tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Even though projId was obtained earlier via a tenant-scoped join, the code issues a write against the projects table without AND tenant_id = <caller tenant>.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well (e.g. WHERE id = ${projId} AND tenant_id = ${tenantId}), or use Drizzle's update API with eq(projects.id, projId) and eq(projects.tenantId, tenantId). Ensure all writes to tenant-scoped tables include the tenant_id constraint even when the id was previously validated.
- **server/video-job-runner.ts:273** — SELECT from tenant-scoped table video_jobs in listStaleVideoJobs has no tenant filter — the query lists jobs across all tenants (no WHERE tenant_id = ...).
  - _Fix:_ If this function is intended to operate per-tenant, add a tenantId parameter and filter on videoJobs.tenantId. If it must run as a global/system recovery process, make the system privilege explicit and harden access (audit/logging) and ensure no user-controlled inputs influence the query; alternatively implement per-tenant enumeration or an explicit admin-only path to avoid accidental cross-tenant reads.

### Resolved — confirmed false positives (182)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/auth.ts:1323** _(upstream-verified)_ — DELETE FROM auth_sessions is executed with no tenant filter (DELETE FROM auth_sessions). This function (clearAllSessionsAsPlatformAdmin) performs a cross-tenant DELETE against a tenant-scoped table without an explicit tenant_id WHERE clause.
  - _Why false positive:_ Function boundary explicitly requires one-use PlatformAdminSessionResetAuthority for intentional global reset; cross-tenant delete is gated and documented at this call path.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL performs a write to the tenant-scoped `conversations` table without constraining by tenant_id. This can modify another tenant's conversation row if IDs are reused or if project/conversation linkage is inconsistent.
  - _Why false positive:_ Boot-time/backfill maintenance loop selects rows with row-carried tenant_id via tenant-consistent joins, then only repairs housekeeping linkage on that row before per-row processing.
- **server/chat-engine/project-context.ts:23** _(fk-transitive)_ — Query reads from tenant-scoped table `project_conversations` (alias pc) without constraining pc.tenant_id to the caller's tenant. Relying on a joined `projects.p.tenant_id` is not an allowed replacement under the project's invariant.
  - _Why false positive:_ project_conversations has no tenant_id; join to tenant-scoped projects with p.tenant_id=tenantId transitively scopes the link table.
- **server/chat-engine/project-context.ts:44** _(fk-transitive)_ — EXISTS subquery reads `project_conversations` (pc) without a tenant_id constraint. This touches a tenant-scoped table without an explicit tenant filter, enabling cross-tenant inference/IDOR.
  - _Why false positive:_ project_conversations has no tenant_id; EXISTS ties pc.project_id to outer tenant-scoped projects row already constrained by p.tenant_id=tenantId.
- **server/chat-engine/project-context.ts:159** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` uses only project_id and omits tenant_id. The code comments assert safety because projectId was validated earlier, but the invariant requires each SQL against a tenant-scoped table to include an explicit tenant_id constraint.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by previously verified tenant-owned projectId.
- **server/chat-engine/project-context.ts:171** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` uses project_id only and omits tenant_id. This violates the rule that every read/write on tenant-scoped tables must include an application-level tenant filter.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; query is scoped through verified tenant-owned projectId.
- **server/chat-engine/project-context.ts:181** _(fk-transitive)_ — Query joins `project_conversations pc` but does not constrain pc.tenant_id. While the joined `conversations` row is tenant-filtered, the project_conversations table itself is referenced without an explicit tenant filter, breaching the invariant that every tenant-scoped table access must include tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id; joined conversations row is tenant-filtered and pc is transitively scoped by project_id/conversation_id linkage.
- **server/email.ts:429** _(system-sweep)_ — UPDATE inbox_messages sets body_text/body_html WHERE id = ${row.id} without constraining tenant_id. This allows updating rows across tenants by id alone.
  - _Why false positive:_ Platform-wide backfill loop updates only housekeeping/content fields on each selected inbox_messages row by its own primary key and does not return or mix tenant data.
- **server/embeddings.ts:852** _(system-sweep)_ — backfillEmbeddingVecs issues UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} with no tenant_id constraint. This can modify embedding vectors for rows belonging to other tenants.
  - _Why false positive:_ Backfill is a maintenance loop that updates only embedding_vec on rows selected from each table by their own primary key; no tenant-facing data mixing occurs.
- **server/embeddings.ts:869** _(system-sweep)_ — backfillEmbeddingVecs updates agent_knowledge rows with UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without tenant scoping, allowing writes across tenants.
  - _Why false positive:_ Boot/maintenance backfill sweeps rows across tenants and only updates housekeeping field embedding_vec on each row by its own id; no tenant-facing data mixing or return.
- **server/felix-brain.ts:23** _(system-sweep)_ — SQL SELECT from tenant-scoped table `agent_runs` is executed without a tenant_id filter. The query at startup (rehydrateTaskStateOnBoot) selects running/pending agent_runs across all tenants, relying on post-query JS checks rather than an application-level WHERE tenant_id = <tenant> filter.
  - _Why false positive:_ Boot-time rehydration intentionally scans running/pending agent_runs across all tenants and stores each row under its own tenant_id key in memory; no cross-tenant response surface shown.
- **server/heartbeat-task-scheduler-storage.ts:11** _(system-sweep)_ — SELECT * FROM heartbeat_tasks executed with no tenant_id constraint (returns rows across all tenants). This reads tenant-scoped rows without constraining to a caller tenant.
  - _Why false positive:_ Explicit scheduler-only cross-tenant function selects due heartbeat tasks for platform-wide dispatch and carries tenantId per row for later scoped handling.
- **server/heartbeat-task-scheduler-storage.ts:44** _(system-sweep)_ — UPDATE heartbeat_tasks SET last_run_at = ..., next_run_at = ... WHERE id = ${id} has no tenant_id constraint. This updates tenant-scoped rows by id only and can modify rows across tenants.
  - _Why false positive:_ Scheduler claim loop intentionally updates due heartbeat tasks across tenants as housekeeping state only (last_run_at/next_run_at) and does not expose mixed tenant data.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE against tenant-scoped table `scheduled_posts` performed without any tenant_id predicate (CTE selects due rows across all tenants and the subsequent UPDATE joins only on id). This updates tenant rows without constraining by tenant_id.
  - _Why false positive:_ Global runner intentionally locks and flips due scheduled_posts across tenants for background publishing, returning each row with its own tenant_id for per-row processing.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the primary id in its WHERE clause (`.where(eq(tenantVoiceProfiles.id, existing.id))`) and does not constrain by tenant_id. This lets an update target a row id across tenants.
  - _Why false positive:_ existing row is first resolved by getVoiceProfile with tenantId and profileName, then updated by that retrieved server-side primary key; ownership established earlier in same call path.
- **server/memory-graph.ts:129** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_categories` lacks a tenant predicate: `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}`. A category id is not a substitute for an explicit tenant check.
  - _Why false positive:_ catId comes from ensureCategory(categoryName, tenantId, ...) which selects/inserts memory_categories scoped by tenant_id immediately beforehand, so the updated row was already tenant-verified.
- **server/memory-graph.ts:708** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` uses only the entry id (`WHERE id = ${mem.id}`) and omits the tenant_id predicate. This allows modifying another tenant's memory if ids collide or are leaked.
  - _Why false positive:_ Batch categorization first selects uncategorized memory_entries for the supplied tenantId, then updates those same selected rows by id as maintenance enrichment.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — Startup validation reads all rows from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE). This returns data across tenants (includes sensitive tokens/metadata).
  - _Why false positive:_ Startup validation is a platform-wide maintenance routine over active oauth_subscriptions; it processes each row with its own tenant_id and does not return mixed tenant data to a tenant-facing surface.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh queries `oauth_subscriptions` without a tenant filter (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE), iterating all tenants. This reads across tenants and may expose sensitive subscription data.
  - _Why false positive:_ Proactive token refresh is a cross-tenant housekeeping job over active subscriptions, handled per row using row-carried tenant_id and mutating refresh state only.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant constraint, enumerating subscriptions across all tenants.
  - _Why false positive:_ Force-refresh-all is an explicit cron/admin maintenance sweep across active subscriptions, iterating per-row tenant_id and updating auth housekeeping only.
- **server/project-brain.ts:333** _(system-sweep)_ — Backfill query selects projects/conversations/messages across all tenants (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...). There is no tenant_id filtering — this will enumerate data for every tenant.
  - _Why false positive:_ Backfill is a platform-wide maintenance loop creating per-project brain files; not tenant-facing and processes each project separately.
- **server/routes/conversations.ts:118** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` omits tenant_id in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}). This allows an update by id alone and can modify rows belonging to other tenants.
  - _Why false positive:_ Conversation row was just server-created with tenantId and conv.id is server-generated; project ownership was validated immediately above.
- **server/routes/conversations.ts:124** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` omits tenant_id in WHERE clause (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This allows updating a project by id across tenants.
  - _Why false positive:_ Same call path already verified project id belongs to tenant via SELECT ... WHERE id = projectId AND tenant_id = tenantId.
- **server/routes/conversations.ts:256** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). Per project schema conventions tenant_id is expected to be provided explicitly — omitting it violates the invariant and can create cross-tenant or malformed rows.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively scoped through tenant-validated parent project_id.
- **server/sculptor.ts:106** _(upstream-verified)_ — UPDATE against tenant-scoped table `sculptor_sessions` uses WHERE id = ${session.id} with NO tenant_id constraint. This can update a row belonging to another tenant if an id is forged or mistaken.
  - _Why false positive:_ Updates the exact row just inserted in this function; session.id is server-generated from RETURNING *, not request-controlled.
- **server/sculptor.ts:113** _(upstream-verified)_ — UPDATE sculptor_sessions SET conversation_id = … WHERE id = ${session.id} has no tenant_id constraint. Missing tenant filter allows cross-tenant modification by id.
  - _Why false positive:_ Updates the same newly inserted session row by server-generated primary key within the same function.
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' runs with NO tenant_id constraint against a tenant-scoped table. This reads across tenants and is a potential tenant-isolation risk.
  - _Why false positive:_ Boot-time cleanup counts runaway heartbeat tasks across tenants for housekeeping only; no tenant data returned.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has NO tenant_id constraint and will disable delegation tasks across tenants.
  - _Why false positive:_ Maintenance cleanup intentionally disables runaway delegation tasks across tenants as housekeeping, not tenant-facing data access.
- **server/seed-cleanup.ts:45** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' executes without tenant_id constraint and will count across tenants.
  - _Why false positive:_ Global duplicate-task detector is a maintenance count across tenants and does not expose tenant content.
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant scoping and will disable matching tasks for every tenant.
  - _Why false positive:_ Boot-time cleanup globally disables housekeeping heartbeat tasks by pattern; maintenance mutation only, no tenant data returned or mixed.
- **server/seed-cleanup.ts:59** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) runs without tenant_id constraint and may return counts across tenants.
  - _Why false positive:_ Global count in maintenance reconciler is aggregate-only for deciding cleanup/logging, not exposed per tenant or mixing content.
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has NO tenant_id constraint and will disable matching tasks for all tenants.
  - _Why false positive:_ Boot-time orphaned-task cleanup intentionally disables matching heartbeat tasks across tenants; housekeeping-only mutation.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM 'heartbeat_tasks' is executed without any tenant_id constraint, potentially deleting disabled delegation tasks across all tenants.
  - _Why false positive:_ Watchdog purges stale disabled delegation tasks platform-wide as housekeeping; only deletes old rows and reports aggregate count.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM 'event_log' is performed with no tenant_id constraint (deletes processed/failed events older than 3 days across all tenants).
  - _Why false positive:_ Global pruning of old processed/failed event_log rows is housekeeping-only and returns only aggregate count to actions.
- **server/stability-watchdog.ts:259** _(global-table)_ — PruneOldLogs DELETE on 'heartbeat_logs' (old log pruning) runs with no tenant_id constraint — this removes heartbeat logs across all tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id, so old-log pruning is on a non-tenant-scoped table.
- **server/storage.ts:289** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant_id (SELECT id,citations FROM messages WHERE id IN (...) AND citations IS NOT NULL). This can return citations for messages belonging to other tenants.
  - _Why false positive:_ attachCitationMetadata only re-reads ids from msgs already fetched with tenant-scoped getMessages/getMessagesPaginated filters in same call path.
- **server/tools.ts:2929** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT INTO project_files (... ) VALUES (...) omits tenant_id, violating the invariant that tenant_id must be explicitly provided on writes.
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated through tenant-scoped projects after owner check.
- **server/tools.ts:2943** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id. The INSERT INTO project_notes (...) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id by schema; insert is gated by tenant-scoped project ownership check on projects.id + tenant_id
- **server/tools.ts:2803** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without storing tenant_id on the new row.
  - _Why false positive:_ project_conversations has no tenant_id by schema; conversation link only occurs after tenant-scoped conversations update succeeds
- **server/tools.ts:2967** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations (link_conversation path) does not set tenant_id. The VALUES clause lacks tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id by schema; both project ownership and conversation tenant membership are validated before insert
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments performs a SELECT on tenant-scoped table `experiments` without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This returns rows from all tenants.
  - _Why false positive:_ heartbeat maintenance sweep intentionally scans all tenants and processes each row with its own tenant_id for scoped updates
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — completeActivity updates agentActivity with WHERE eq(agentActivity.id, activityId) but does not include tenant_id filtering. This allows updating a row that may belong to another tenant.
  - _Why false positive:_ completeActivity updates by server-generated activityId primary key in internal worker/helper, not shown crossing an untrusted tenant request boundary
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — runEval updates agent_evals with WHERE id = ${evalId} but does not constrain by tenant_id, allowing an update to an eval row belonging to another tenant.
  - _Why false positive:_ evalId comes from prior INSERT RETURNING in same function and update is internal by server-generated primary key
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting SELECTs from tenant-scoped table `ai_insights` without a tenant_id filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = PENDING_PLAN_MARKER LIMIT 50). This reads insights for all tenants.
  - _Why false positive:_ durability sweep intentionally scans all tenants and routes each row using that row's validated tenant_id without mixing tenant outputs
- **server/agentic-engines.ts:247** _(system-sweep)_ — autoApplyEligibleInsights runs UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope} but when tenantId is undefined the UPDATE lacks tenant scoping and will update rows across all tenants.
  - _Why false positive:_ bulk housekeeping sweep intentionally applies status/action_taken changes across tenants and carries row tenant_id forward for per-row follow-up
- **server/agentic-features.ts:901** _(upstream-verified)_ — mission_experiments query builds an ARRAY literal via string interpolation: sequence_id = ANY(${`{${seqIds.join(",")}}`}::int[]). The array is embedded as a raw string expression constructed from runtime values, which can lead to injection or unescaped SQL if the values are not fully trusted/sanitized.
  - _Why false positive:_ query already constrains mission_experiments by params.tenantId; finding is not a tenant-isolation issue
- **server/agentic-features.ts:1021** _(upstream-verified)_ — classifyReply updates outreach_enrollments with WHERE id = ${row.id} but does not include tenant_id in the WHERE clause, enabling cross-tenant updates if an id is guessed or ownership changed.
  - _Why false positive:_ row.id is sourced from immediately prior SELECT filtered by e.tenant_id = params.tenantId in same call path
- **server/agentic/approvals.ts:134** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals WHERE status = 'pending' AND expiresAt < now() without a tenant_id filter, which will expire pending approvals across all tenants in one statement.
  - _Why false positive:_ expiry housekeeping intentionally updates expired pending approvals across tenants and returns per-row tenantId/runId for scoped follow-up only
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT queries tenant-scoped table without a tenant filter. The code executes `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` (no WHERE tenant_id = ...) which reads rows across all tenants from a tenant-scoped table.
  - _Why false positive:_ heartbeat sweep intentionally scans active budgets across tenants and emits per-tenant events using each row's tenant_id
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — Unscoped SELECT on tenant-scoped tables — the candidate query scans agent_runs (joined to agent_approvals) without constraining by tenant_id. This allows a background sweep to read rows for all tenants (cross-tenant data exposure / isolation violation).
  - _Why false positive:_ Global maintenance sweep scans across tenants but carries tenant_id per row and resumes/rejects runs individually; no combined tenant-facing output shown.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — Global UPDATE on tenant-scoped table `agent_wake_schedules` uses an unfiltered subselect to pick ids: the inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE SKIP LOCKED) does not constrain by tenant_id. This updates rows across all tenants without an app-level tenant filter.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due rows platform-wide and processes each returned row with its own tenant_id when emitting/finalizing.
- **server/auth.ts:1331** _(system-sweep)_ — Maintenance DELETE on tenant-scoped table `auth_sessions` deletes expired sessions across all tenants (DELETE FROM auth_sessions WHERE expires_at < ${now}) with no tenant_id filter.
  - _Why false positive:_ Boot-time expiry cleanup deletes expired sessions globally as housekeeping only; no tenant data returned or mixed.
- **server/auth.ts:1335** _(system-sweep)_ — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' deletes rows across all tenants with no tenant_id filter (global cleanup of legacy sessions).
  - _Why false positive:_ One-time legacy-session invalidation is global housekeeping on session rows, not tenant-facing data access.
- **server/auto-transcript.ts:128** _(upstream-verified)_ — SELECT DISTINCT ... WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 runs a subquery against tenant-scoped table `messages` without constraining by tenant_id. This can read message rows across tenants because the subquery does not enforce the conversation's tenant.
  - _Why false positive:_ messages subquery is correlated by conversation_id to a conversations row already carrying c.tenant_id; this checks existence for that conversation before per-row tenant-scoped processing.
- **server/auto-transcript.ts:153** _(upstream-verified)_ — Query selects rows where (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 but the subquery against `messages` lacks a tenant_id constraint. This allows cross-tenant message reads when evaluating linked conversations.
  - _Why false positive:_ messages subquery is correlated to joined conversation c.id, while projects join enforces p.tenant_id = c.tenant_id; rows are then processed with c.tenant_id.
- **server/channel-routing.ts:30** _(global-table)_ — listChannelRoutes performs a JOIN against the personas table (tenant-scoped) but does not constrain the join by tenant_id. This SELECT (cr.*, p.name ...) can expose persona rows from other tenants because there is no WHERE p.tenant_id = <caller tenant>.
  - _Why false positive:_ channel_routes is defined here as a platform-global routing table (unique by channel, no tenant_id); personas schema note shows no tenant_id column.
- **server/channel-routing.ts:53** _(global-table)_ — setChannelRoute inserts/updates a persona_id provided by the caller into a global channel_routes table without verifying that the persona belongs to the caller's tenant. A tenant could associate another tenant's persona_id with their channel.
  - _Why false positive:_ Writes target a global channel_routes registry keyed by channel; referenced persona table is not tenant-scoped per schema note.
- **server/data-protection.ts:149** _(system-sweep)_ — SELECT from conversations lacks a tenant_id constraint: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` queries a tenant-scoped table across all tenants.
  - _Why false positive:_ Global purge loop selects expired soft-deleted conversations across tenants, then uses each row's tenant_id to back up and delete child/parent rows individually.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE on tenant-scoped table doc_chunks uses WHERE id = ${row.id} without tenant_id. This allows updating another tenant's row by id.
  - _Why false positive:_ Updated row ids come from the immediately preceding SELECT constrained by collection_id and tenant_id, so row.id set is already tenant-scoped in the same call path.
- **server/email.ts:415** _(system-sweep)_ — SELECT across inbox_messages omits tenant constraint (SELECT id, message_id FROM inbox_messages WHERE body_text IS NULL ... ORDER BY id). This reads rows for all tenants.
  - _Why false positive:_ Global backfill/housekeeping loop updates each inbox_messages row by its own id and only logs aggregate counts; no tenant-facing mixed output.
- **server/embeddings.ts:811** _(system-sweep)_ — backfillMissingKnowledgeEmbeddings SELECTs agent_knowledge rows across all tenants (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE ... LIMIT 50) without restricting tenant_id.
  - _Why false positive:_ Platform-wide embedding backfill intentionally scans expired/missing rows across tenants and handles each row with its own tenant_id on update.
- **server/embeddings.ts:842** _(system-sweep)_ — backfillEmbeddingVecs selects memory_entries rows across all tenants (SELECT id, embedding FROM memory_entries WHERE ...) without tenant scoping.
  - _Why false positive:_ Maintenance backfill for memory_entries only repairs embedding_vec housekeeping state and returns aggregate counts, not tenant data.
- **server/embeddings.ts:858** _(system-sweep)_ — backfillEmbeddingVecs selects agent_knowledge rows across all tenants (SELECT id, embedding FROM agent_knowledge WHERE ...) without tenant scoping.
  - _Why false positive:_ Maintenance backfill for agent_knowledge only repairs embedding_vec housekeeping state and returns aggregate counts, not tenant-facing mixed content.
- **server/firecrawl.ts:239** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' lacks a tenant_id constraint in WHERE (uses only id). This can update another tenant's row if IDs are global.
  - _Why false positive:_ Updated row id comes from an immediately preceding SELECT scoped by scrapedPages.tenantId = tenantId in the same call path.
- **server/firecrawl.ts:372** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' lacks a tenant_id constraint in WHERE (uses only id). This can update another tenant's row if IDs are global.
  - _Why false positive:_ Updated row id comes from an immediately preceding SELECT scoped by scrapedPages.tenantId = tenantId in the same loop iteration.
- **server/glasses-gateway.ts:173** _(upstream-verified)_ — UPDATE to apiKeys table (setting lastUsedAt) uses only apiKeys.id in WHERE and does not include tenant_id. apiKeys is tenant-scoped — without tenant constraint this write could affect another tenant's API key row in races or ID reuse scenarios.
  - _Why false positive:_ apiKeys row is first loaded by unique keyHash and key.id is then reused only to stamp lastUsedAt on that same authenticated row.
- **server/google-drive.ts:880** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' (setting driveFolderId/driveFolderUrl) uses only projects.id in WHERE and lacks tenant_id constraint. This can modify another tenant's project row.
  - _Why false positive:_ Same function first loads project, verifies project.tenantId matches caller tenantId, then updates that verified project id.
- **server/google-workspace.ts:2287** _(fk-transitive)_ — INSERT INTO presenter_slide_images does not set or include tenant_id when writing a (likely) tenant-scoped table.
  - _Why false positive:_ presenter_slide_images appears child-scoped by session_id to tenant-scoped presenter_sessions; child table itself is not shown tenant-scoped.
- **server/heartbeat-startup.ts:76** _(system-sweep)_ — Direct SQL update: UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — no tenant_id filter. This mutates a tenant-scoped table without constraining to the task's tenant.
  - _Why false positive:_ boot-time startup normalization intentionally sweeps heartbeat tasks across tenants and only mutates housekeeping/model fields.
- **server/heartbeat-startup.ts:80** _(system-sweep)_ — Direct SQL update: UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — no tenant_id filter. Mutates tenant-scoped rows without tenant constraint.
  - _Why false positive:_ boot-time startup normalization intentionally sweeps heartbeat tasks across tenants and only mutates housekeeping/model fields.
- **server/heartbeat.ts:256** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... executed with no tenant_id constraint. event_log rows contain tenant_id and this reads pending events across all tenants without an application-level tenant filter.
  - _Why false positive:_ scheduler selects pending events globally but processes each row with its own event.tenant_id via routeEventToSubscribers(event.id, eventTenantId).
- **server/heartbeat.ts:1510** _(global-table)_ — SELECT created_at FROM heartbeat_logs ... executed without tenant_id constraint. heartbeat_logs are tenant-scoped and this read will return rows across tenants.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id, so this is not a tenant-scoped read.
- **server/job-queue.ts:160** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT on agent_jobs without scoping by tenant_id (no tenant filter) — the atomic claim can touch jobs from any tenant.
  - _Why false positive:_ queue claim is a platform-wide worker loop intentionally claiming jobs across tenants and returns each job with row-carried tenant_id for per-job handling.
- **server/job-queue.ts:195** _(system-sweep)_ — reclaimExpiredLeases step 1 updates agent_jobs (marking rows failed_terminal) without any tenant_id constraint — affects rows across all tenants.
  - _Why false positive:_ lease-reclamation is cross-tenant housekeeping on job status fields only, not tenant-facing data mixing.
- **server/job-queue.ts:209** _(system-sweep)_ — reclaimExpiredLeases step 2 re-queues expired running jobs via UPDATE agent_jobs ... WHERE status='running' AND lease_until < NOW() without tenant_id filtering.
  - _Why false positive:_ lease-reclamation is cross-tenant housekeeping on job status fields only, not tenant-facing data mixing.
- **server/job-queue.ts:238** _(internal-worker-pk)_ — completeJob updates agent_jobs (status -> 'succeeded') using id/status/attempts but no tenant_id in WHERE clause, allowing a caller to potentially complete a job from another tenant if ID/attempts are known.
  - _Why false positive:_ completeJob is internal worker state transition by server-generated global job id plus attempts lease fence, not from request boundary.
- **server/job-queue.ts:281** _(internal-worker-pk)_ — failJob reads max_attempts with SELECT max_attempts FROM agent_jobs WHERE id = ${id} without tenant scoping — a malicious call or abused job id could reveal or affect other-tenant rows.
  - _Why false positive:_ failJob is a server worker helper operating on a server-generated job PK with lease-fenced attempt state, not a tenant request boundary
- **server/job-queue.ts:298** _(internal-worker-pk)_ — failJob terminal UPDATE moves a job to failed_terminal (and writes error/failure_class) using id/status/attempts but does not include tenant_id in the WHERE clause.
  - _Why false positive:_ worker-only failure transition by server-generated job id/attempts under lease fencing; not driven by untrusted tenant input
- **server/job-queue.ts:318** _(internal-worker-pk)_ — failJob retry/update path sets job back to pending with next_run_at but the UPDATE lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ worker retry-state update uses internal claimed job PK and attempt fence, not cross-tenant request data
- **server/job-queue.ts:452** _(upstream-verified)_ — cancelPendingJobsForTenant builds an SQL array literal via string interpolation from user-supplied excludeKinds (excludeLiteral) and then injects it into the query text — this is a string-interpolated SQL clause built from request-derived values (SQL injection + isolation risk).
  - _Why false positive:_ query is tenant-scoped by WHERE tenant_id = tenantId; finding is about SQL construction, not missing cross-tenant isolation
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns uses only id in WHERE clause (no tenant check). This writes a tenant-scoped table row without constraining by tenant_id, allowing a runId to be updated across tenants.
  - _Why false positive:_ runId is the freshly inserted server-generated PK in the same function; update targets that internal row, not caller-supplied cross-tenant input
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns (noop_proposal path) uses only id in WHERE clause (no tenant check). This mutates a tenant-scoped row without verifying tenant ownership.
  - _Why false positive:_ same-function update of newly created procedureEvolutionRuns row by server-generated runId only
- **server/lib/aevo-meta-editor.ts:499** _(internal-worker-pk)_ — UPDATE on procedureEvolutionRuns after inserting procedureEdits uses only id in WHERE clause (no tenant check). This can flip run state for a different tenant if runId collides or is attacker-controlled.
  - _Why false positive:_ completion update uses the same freshly created internal runId, not an untrusted tenant-controlled identifier
- **server/lib/chronicle-precision.ts:90** _(fk-transitive)_ — Query reads columns from the `skills` table (alias `s`) via LEFT JOIN without constraining `s` to the caller's tenant. This allows disclosure of skill rows belonging to other tenants because tenant ownership is enforced only at the application level.
  - _Why false positive:_ schema note says skills has no tenant_id; joined by promoted_skill_id from tenant-scoped proposed_skills row so isolation is transitive
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped table `projects` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This aggregates counts across all tenants and violates the invariant that every read against tenant-scoped tables must be tenant-scoped at the application level.
  - _Why false positive:_ Boot/backfill routine intentionally scans across tenants, groups by row-carried tenant_id, and only uses aggregate counts for housekeeping short-circuiting.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE to tenant-scoped table knowledge_triples lacks a tenant_id constraint in the WHERE clause (writes row by id only).
  - _Why false positive:_ Updated ids come only from prior SELECT constrained by tenant_id = tenantId, so the write target is already ownership-verified in the same call path.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — Query selects tenant_id and is_active from `oauth_subscriptions` for provider='google-workspace' without scoping by tenant (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This can enumerate tenant rows and leak cross-tenant information.
  - _Why false positive:_ Startup auto-reconnect is a platform-wide maintenance loop over subscription rows; it uses each row's own tenant_id and does not return mixed tenant data to a tenant-facing surface.
- **server/pace-control.ts:85** _(global-table)_ — checkPace queries the tenant-scoped table `heartbeat_logs` without any tenant_id filter (COUNT(...) FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). This returns global counts across tenants, violating tenant isolation.
  - _Why false positive:_ Schema note shows heartbeat_logs has no tenant_id column; this is an intentionally global throttle/heartbeat log used for platform-wide pace caps.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries `heartbeat_logs` without scoping by tenant (SELECT COUNT(...) FROM heartbeat_logs WHERE created_at > ...). This returns cross-tenant usage metrics and breaches the invariant that tenant-scoped tables must be filtered per caller tenant.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column, and this snapshot reports global operator/debugging metrics by design.
- **server/persona-export.ts:33** _(global-table)_ — exportPersona performs SELECT * FROM personas WHERE id = ${personaId} with NO tenant constraint. This reads a persona row without verifying ownership by the caller's tenant (foreign id taken from caller input but no tenant check).
  - _Why false positive:_ Schema note shows personas has no tenant_id column; the flagged SELECT is against a non-tenant-scoped table.
- **server/plan-executor.ts:792** _(system-sweep)_ — resumeStuckPlans() performs a global UPDATE on the tenant-scoped plans table without constraining by tenant_id (UPDATE plans SET status='approved' ... WHERE status='executing' AND updated_at < ...). This writes across all tenants' rows.
  - _Why false positive:_ Boot-time recovery intentionally updates stale plans across tenants as housekeeping, then restarts by id; this is a platform-wide maintenance sweep rather than tenant-facing data mixing.
- **server/process-governor.ts:898** _(upstream-verified)_ — UPDATE agent_desks executed without constraining by tenant_id (UPDATE ... WHERE id = ${desk.id}). This updates a tenant-scoped table using only the row id.
  - _Why false positive:_ desk.id values come from an immediately prior SELECT on agent_desks constrained by ad.tenant_id = tenantId, so the updated rows were already tenant-scoped.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_knowledge` without constraining by tenant_id. The update uses only WHERE id = ${r.id}, which can modify a row belonging to a different tenant if ids are discovered via an unscoped SELECT.
  - _Why false positive:_ global scheduler processes due rows across tenants using row-carried tenant_id; this UPDATE only advances housekeeping fields on the same row
- **server/research-engine.ts:169** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_sessions' performed without any tenant_id constraint. The cleanupZombieSessions() query (UPDATE research_sessions ... WHERE status = 'running' AND started_at < $staleThreshold) will modify rows across all tenants.
  - _Why false positive:_ global stale-session cleanup intentionally operates across tenants and only mutates status/ended_at/summary housekeeping fields, returning ids for logs only
- **server/routes.ts:179** _(system-sweep)_ — Unscoped SELECT from tenant-scoped table `file_storage` during startup restore (no tenant_id constraint).
  - _Why false positive:_ startup restore is a platform-wide maintenance loop restoring blobs to disk; no tenant-facing mixed response is produced
- **server/routes.ts:970** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` without tenant constraint to fetch ALL owners of a basename (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))).
  - _Why false positive:_ query checks all owner rows to validate access to a file/signed capability and deny on collision; signed URL or matched tenant ownership is the access control
- **server/routes.ts:4542** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The code inserts: INSERT INTO project_notes (project_id, note, author) VALUES (...) without passing tenant_id; project records are tenant-owned and project_notes is expected to be tenant-scoped, so this violates the invariant that every INSERT into tenant-scoped tables must set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id to tenant-owned projects
- **server/routes/conversations.ts:236** _(upstream-verified)_ — SELECT joins `conversations` and `projects` but omits tenant filter for the `conversations` table (SELECT c.project_id FROM conversations c JOIN projects p ... WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}). The query constrains only projects by tenant_id, not conversations.
  - _Why false positive:_ same handler already loaded conv via tenant-scoped storage.getConversation/getConversationForPlatformAdmin and verifies access before this project lookup
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` is missing a tenant_id predicate. The code does `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `AND tenant_id = ${tenantId}`, allowing a write to a row outside the caller's tenant context.
  - _Why false positive:_ same call path first SELECTs inbox_messages by id and tenant_id, then updates that fetched row by its server-read pk
- **server/routes/projects.ts:596** _(upstream-verified)_ — UPDATE projects statement omits tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) — SQL against tenant-scoped table has no tenant predicate.
  - _Why false positive:_ same handler previously verified project ownership with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:664** _(upstream-verified)_ — UPDATE projects statement omits tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) — SQL against tenant-scoped table has no tenant predicate in the multipart file-upload handler.
  - _Why false positive:_ multipart upload handler first checks project id under tenant_id before touching updated_at on that project
- **server/routes/projects.ts:548** _(fk-transitive)_ — INSERT INTO project_notes does not include tenant_id in VALUES (INSERT INTO project_notes (project_id, note, author) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'})) — tenant_id must be set explicitly on inserts to tenant-scoped tables.
  - _Why false positive:_ project_notes has no tenant_id and is inserted only after verifying parent projects.id belongs to tenantId
- **server/routes/projects.ts:589** _(fk-transitive)_ — INSERT INTO project_files (base64 path) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)) — tenant-scoped row inserted without tenant ownership asserted in the row.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped transitively via prior tenant-checked parent project
- **server/routes/projects.ts:656** _(fk-transitive)_ — INSERT INTO project_files (multipart upload handler) omits tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...)) — row may be created without explicit tenant_id.
  - _Why false positive:_ project_files has no tenant_id and multipart insert follows project ownership check on tenant-scoped projects row
- **server/routes/projects.ts:735** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks an explicit tenant_id constraint — performing DELETE on tenant-scoped table without tenant predicate.
  - _Why false positive:_ delete is preceded in same handler by SELECT on projects with id and tenant_id, and child table is keyed by that verified project_id
- **server/second-opinion.ts:417** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} lacks a tenant_id constraint. Even though reservationId originates from a prior insert, future callers could attempt to settle an id from another tenant if not double-checked.
  - _Why false positive:_ reservationId is a server-generated ledger PK used by internal settlement helper, not taken from request input
- **server/second-opinion.ts:438** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd='0', operation='second_opinion:released' WHERE id = ${reservationId} has NO tenant_id constraint. Releasing by id only can affect other tenants' ledger rows.
  - _Why false positive:_ release helper updates internal reservation row by server-generated PK only, not an untrusted tenant boundary value
- **server/seed.ts:2169** _(global-table)_ — UPDATE against likely tenant-scoped table `projects` without tenant constraint: `UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL` will modify rows regardless of tenant_id.
  - _Why false positive:_ flagged line is ALTER TABLE DDL on projects schema, not a tenant row mutation
- **server/seed.ts:2239** _(system-sweep)_ — Unscoped SELECT from `conversations`: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)` reads a tenant-scoped table with no tenant filter, then acts on the results (inserting links), which can cross tenant boundaries.
  - _Why false positive:_ boot-time backfill scans all conversations and inserts missing project_conversations links per row without returning or mixing tenant data
- **server/seed.ts:3634** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without constraining by tenant_id. heartbeat_tasks is tenant-scoped (idx_heartbeat_tasks_tenant_id exists), so this DELETE can remove rows across all tenants.
  - _Why false positive:_ seed cleanup intentionally purges stale disabled heartbeat_tasks across tenants as housekeeping only, with no tenant-facing data flow
- **server/seed.ts:3662** _(system-sweep)_ — UPDATE memory_entries replaces text (REPLACE(fact, 'Manjaro', 'wellness-program')) without any tenant_id restriction. memory_entries is tenant-scoped (idx_memory_entries_tenant_id exists), so this will run across all tenants and violate tenant isolation.
  - _Why false positive:_ boot-time normalizer updates memory_entries in place across tenants as maintenance only and does not expose mixed tenant content
- **server/seed.ts:3688** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped (idx_conversations_tenant_id exists); this updates rows for all tenants.
  - _Why false positive:_ global backfill rewrites legacy model ids in conversations in place across tenants as housekeeping only, with no cross-tenant data output
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` updates WHERE id = ... with no tenant_id check (UPDATE memory_entries SET ... WHERE id = <id>) — this can modify another tenant's memory entry if the id is known or ownership changed.
  - _Why false positive:_ Updated id comes directly from prior SELECT constrained by tenant_id = tenantId in same function.
- **server/skillify.ts:221** _(fk-transitive)_ — Query reads from the 'personas' table using persona id without constraining by tenant_id (missing tenant ownership check). This allows cross-tenant disclosure of persona records.
  - _Why false positive:_ personas table has no tenant_id; persona id is read from tenant-scoped conversation already filtered by tenant.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from 'heartbeat_logs' (recent error aggregation) is executed with no tenant_id constraint — this reads logs across all tenants.
  - _Why false positive:_ heartbeat_logs schema is global (no tenant_id) and watchdog is platform-wide stability logic.
- **server/storage.ts:300** _(upstream-verified)_ — Handler `createMessage` reads the `conversations` row by id without scoping the SELECT to the caller's tenant. A caller could supply a foreign conversation_id and cause an out-of-tenant read.
  - _Why false positive:_ createMessage immediately derives conversation tenant and rejects mismatched caller tenant before insert; read is used only for ownership validation.
- **server/storage.ts:694** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversation_facts` uses WHERE id IN (<victim ids>) without including tenant_id in the UPDATE predicate. Although victims were selected earlier with tenant-scoped filters, the UPDATE itself is unqualified and may affect rows incorrectly if id semantics change.
  - _Why false positive:_ Victim ids are selected earlier in same call path with conversationId and tenantId filters, then updated by those exact ids.
- **server/storage.ts:915** _(system-sweep)_ — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` without a tenant filter (returns rows across all tenants).
  - _Why false positive:_ Unscoped select is for platform-wide embedding backfill/maintenance and not tenant-facing output.
- **server/storage.ts:921** _(system-sweep)_ — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` without any tenant constraint, returning cross-tenant rows.
  - _Why false positive:_ Unscoped select is for platform-wide embedding backfill/maintenance and not tenant-facing output.
- **server/storage.ts:929** _(system-sweep)_ — archiveExpiredMemories performs a raw UPDATE on `memory_entries` with no tenant scoping (UPDATE memory_entries SET status='archived' WHERE ...). This will archive rows for all tenants.
  - _Why false positive:_ archiveExpiredMemories is a platform-wide housekeeping update of status fields only, with no tenant data returned to users.
- **server/storage.ts:945** _(system-sweep)_ — archiveStaleMemories runs a raw UPDATE on `memory_entries` without tenant_id constraint, possibly affecting all tenants' rows.
  - _Why false positive:_ Global expiry/archive housekeeping updates status only across tenants and returns only ids/counts, not tenant data.
- **server/storage.ts:1068** _(system-sweep)_ — fixStaleBackupSchedules executes a global UPDATE on `heartbeat_tasks` without any tenant scoping, modifying rows across all tenants.
  - _Why false positive:_ Platform-wide maintenance reschedules stale backup tasks across tenants; housekeeping field update only.
- **server/storage.ts:1085** _(internal-worker-pk)_ — markHeartbeatTaskRun updates heartbeat_tasks .where(eq(heartbeatTasks.id, id)) with no tenant predicate. If heartbeat_tasks is tenant-scoped, this allows modifying any tenant's task by id without validating caller's tenant.
  - _Why false positive:_ Server worker marks a task run by server-side task PK; no untrusted tenant boundary shown.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table custom_tools filters only on name and omits tenant_id (`.where(eq(customTools.name, parsed.name))`). Reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ Name is rewritten with tenantToolName(parsed.name, tenantId) immediately above, making lookup tenant-derived in same flow.
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table custom_tools by id (`.where(eq(customTools.id, inserted.id))`) lacks a tenant_id constraint. Reads by id must still be tenant-scoped to enforce isolation.
  - _Why false positive:_ Re-read is by freshly inserted server-generated custom_tools PK from same function, not attacker-controlled input.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment usageCount on custom_tools uses only the PK in WHERE (`.where(eq(customTools.id, tool.id))`) without scoping by tenant_id.
  - _Why false positive:_ Tool row was fetched just above with tenant_id = tenantId; subsequent usageCount update uses that verified row's PK.
- **server/tools.ts:2923** _(fk-transitive)_ — SELECT against tenant-scoped table project_files (dupCheck) lacks an explicit tenant_id filter. Queries such as SELECT id FROM project_files WHERE project_id = ${params.id} ... do not include tenant_id = ${tenantId}.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via verified parent project ownership check just above.
- **server/tools.ts:2963** _(fk-transitive)_ — SELECT against tenant-scoped table project_conversations (existing check) lacks an explicit tenant_id filter. The query SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} has no tenant_id condition.
  - _Why false positive:_ project_conversations has no tenant_id and insert/read is transitively scoped by verified project and conversation tenant checks above.
- **server/tools.ts:4496** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id in the inserted columns/VALUES.
  - _Why false positive:_ project_notes has no tenant_id; insert is correct via tenant-verified parent project.
- **server/tools.ts:4499** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id in the inserted columns/VALUES.
  - _Why false positive:_ project_files has no tenant_id; insert is correct via tenant-verified parent project.
- **server/tools.ts:6389** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' (audio upload path) does not include tenant_id in the inserted columns/VALUES.
  - _Why false positive:_ project_files has no tenant_id and insert occurs only after assertProjectInTenant on the parent project.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' omits tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This allows modifying a row by id without restricting the operation to the caller's tenant.
  - _Why false positive:_ workflow row was loaded immediately above with tenant_id = tenantId, and update uses that verified row id
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — Unscoped UPDATE against tenant-scoped table knowledge_triples — the code updates rows by id without constraining by tenant_id (UPDATE knowledge_triples SET valid_until = ${validFrom} ... WHERE id = ${row.id}). Although the id came from a prior tenant-scoped SELECT, the invariant requires every write to include an explicit tenant filter to prevent cross-tenant modifications.
  - _Why false positive:_ each updated row.id comes from contradictionRows selected just above with tenant_id = tenantId
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` is performed with WHERE id = ${id} but omits a tenant_id constraint.
  - _Why false positive:_ id being updated was selected immediately above from memory_entries constrained by tenant_id and persona_id
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) executes without any tenant_id constraint, allowing cross-tenant modification if ids are tampered with or mis-derived.
  - _Why false positive:_ ids array is derived solely from rows selected just above with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` is performed with WHERE id = ${id} but omits a tenant_id constraint (record_strategic_win path).
  - _Why false positive:_ updated id was fetched immediately above from memory_entries with tenant_id = tid and persona_id = personaId
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT from tenant-scoped table `conversations` by id with NO tenant_id filter. Reading a tenant-scoped row without scoping allows cross-tenant data exposure if unchecked.
  - _Why false positive:_ conversation read returns only tenant_id for verification against claimed tenant and is used to fail closed on mismatch
- **server/wiring-invariants.ts:244** _(system-sweep)_ — SELECT from tenant-scoped table `tool_performance` (count of recent successes) executed without any tenant_id constraint.
  - _Why false positive:_ global wiring/health diagnostic aggregates only count(*) across tenants for maintenance observability and does not return tenant row data.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT count(*) FROM `tool_performance` executed without tenant_id filtering (global scan of a tenant-scoped table).
  - _Why false positive:_ global maintenance check intentionally counts total tool_performance rows across tenants; aggregate-only housekeeping output, no tenant content mixed.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM `messages` (last 14 days) executed with no tenant_id constraint — reads tenant-scoped chat history across all tenants.
  - _Why false positive:_ global health probe uses only aggregate message count for observability; no tenant messages or mixed content are returned.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM `tool_performance` executed without tenant_id constraint (reads tenant-scoped usage data globally).
  - _Why false positive:_ diagnostic reads distinct tool names globally to detect dormant tools; emits platform-wide maintenance findings, not tenant data.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — sql.raw SELECT COUNT/MAX FROM ${spec.table} executes against subsystem tables (e.g. knowledge_nudges, tool_performance) with the table name interpolated and no tenant_id WHERE clause — unscoped reads against tenant-scoped tables and use of raw SQL interpolation.
  - _Why false positive:_ subsystem freshness loop is a platform-wide housekeeping probe over whitelisted tables producing only counts/timestamps, not exposing tenant rows.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — SELECT ... FROM `tool_performance` in tool-drift check runs without a tenant_id filter (reads per-tool aggregates across all tenants).
  - _Why false positive:_ tool-drift detector aggregates success/fail totals by tool for admin diagnostics; outputs only cross-tenant maintenance summaries, not tenant-specific content.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — sql.raw SELECT COUNT() and pg_total_relation_size FROM ${tbl.name} executes against memory tables (including `messages`) without tenant_id filters — unscoped scans of tenant-scoped tables and raw string interpolation of table names.
  - _Why false positive:_ memory-growth tripwire is a platform-wide observability check over row counts and relation sizes only; no tenant records are returned or mixed into tenant-facing output.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM `graph_memory` runs without tenant_id constraint — reads a tenant-scoped memory store globally.
  - _Why false positive:_ empty-table bootstrap check performs only a global count for housekeeping and reports no tenant data.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table `commitments`. The code performs `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` without constraining by tenant_id, allowing a caller (or a buggy code path) to update rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM doc_chunks executed without tenant constraint (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can delete rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/embeddings.ts:806** _(system-sweep)_ — storeEmbeddingVec falls back to an unconditional UPDATE using sql.raw(table) and updates memory_entries/agent_knowledge by id without a tenant_id WHERE clause when options.tenantId is not provided (UPDATE ${sql.raw(table)} ... WHERE id = ${id}). This writes to tenant-scoped tables without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT from projects without tenant constraint (SELECT name, status, description FROM projects WHERE id = ${projectId}).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE on `sculptor_sessions` (setting status/result/completed_at/etc) uses WHERE id = ${sessionId} without tenant_id constraint. This risks modifying another tenant's session.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = … WHERE id = ${sessionId} contains NO tenant_id filter. An attacker-supplied sessionId could cause cross-tenant writes.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/self-improvement.ts:430** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` uses only experiments.id in the WHERE clause (.where(eq(experiments.id, experiment.id))) — there is no tenant_id constraint, so a row from another tenant could be updated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:1358** _(upstream-verified)_ — getAllDataForExport executes many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with no tenant filtering and returns a platform-wide dump. This allows exfiltration of all tenants' data if misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table custom_tools updates implementation using only the PK (`.where(eq(customTools.id, tool.id))`) with NO tenant_id constraint. This can modify rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE against tenant-scoped table custom_tools deletes by id only (`.where(eq(customTools.id, tool.id))`) with NO tenant_id constraint. This can delete another tenant's tool row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/dfy-intake.ts:50** _(signed-url)_ — SELECT from tenant-scoped table dfy_intake_forms without constraining tenant_id (getDfyIntakeByToken queries by token only). This allows retrieving another tenant's intake row by token.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/email.ts:370** _(system-sweep)_ — SELECT against inbox_messages omits tenant constraint (SELECT id FROM inbox_messages WHERE message_id = ... LIMIT 1). This can cause cross-tenant deduping/leakage (a message for another tenant will stop storing a message for the current tenant).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): platform primary-inbox poller (single system account); message_id dedupe against its own inbox rows.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` has no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): archiveStaleProposals is a platform TTL maintenance sweep; it selects code_proposals older than PROPOSAL_TTL_DAYS and (apply) updates status='archived_stale' by id set (server/health-audit.ts:314-338) — housekeeping-only status change, not a tenant-facing surface.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on `code_proposals` uses id = ANY(...) without constraining tenant_id — allows modifying proposals across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): self-heal sweep archives stale proposals whose ids came from its own stale-scan immediately above.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from `heartbeat_tasks` lacks any tenant_id constraint (reads tenant-scoped rows globally).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Same TTL sweep flags stuck heartbeat_tasks and disables them (enabled=false) by id set (server/health-audit.ts:346-369); housekeeping mutation of a maintenance loop.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a cross-tenant update with no tenant_id constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): self-heal sweep disables stuck heartbeat tasks whose ids came from its own scan immediately above.
- **server/google-workspace.ts:2307** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a session row without constraining tenant_id — allows changing another tenant's session.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/heartbeat.ts:2609** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint. This writes a tenant-scoped table by id only and can affect another tenant's row with the same id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3065** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint in delegateTaskFromChat. This modifies tenant-scoped data by id only, lacking tenant scoping/ownership verification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/job-queue.ts:335** _(internal-worker-pk)_ — cancelJob updates agent_jobs to status = 'cancelled' WHERE id = ${id} AND status IN (...) with NO tenant_id constraint (can cancel other-tenant jobs by id).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:125** _(internal-worker-pk)_ — persistSpanClose issues UPDATE agent_trace_spans WHERE span_id = ${spanId} with no tenant_id constraint. This updates a tenant-scoped table row without verifying the tenant, violating tenant isolation (span_id alone is used to identify the row).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/orchestrator-ledger.ts:450** _(upstream-verified)_ — Use of sql.raw() to interpolate a lock key: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). The argument is derived from ledgerKey and tenantId and is being injected via sql.raw(), which bypasses parameterization and risks SQL injection or other unsafe interpolation of request-derived values.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): pg_advisory_xact_lock is a lock, not a tenant table. lockKey is a purely numeric BigInt(sha1).toString() (server/orchestrator-ledger.ts:35-38) — no injectable text. The ledger read/write in the same tx are tenant-scoped ('AND tenant_id = ${tenantId}' at :448).
- **server/process-governor.ts:1235** _(upstream-verified)_ — UPDATE governance_rules executed without tenant constraint (UPDATE governance_rules ... WHERE id = ${rule.id}). This mutates a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT conversations WHERE c.project_id = ${proj.id} is executed without tenant constraints (conversations likely tenant-scoped or should be validated through project tenant).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT messages WHERE conversation_id = ${conv.id} is executed without tenant scoping/ownership verification for the conversation (reads message rows across tenants).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/storage.ts:601** _(upstream-verified)_ — INSERT into tenant-scoped table `memory_entries` with db.insert(memoryEntries).values(data) without validating that `data.tenantId` is an explicit positive integer. The schema is NOT NULL with no default; missing/invalid tenantId can lead to unscoped writes or DB errors.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/storage.ts:656** _(upstream-verified)_ — INSERT into tenant-scoped table `conversation_facts` is performed (db.insert(conversationFacts).values(data)) without validating that `data.tenantId` is present/valid. This can create unscoped writes if caller omits tenantId.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): conversation_facts.tenant_id NOT NULL with no default; InsertConversationFact requires tenantId at compile time.
- **server/storage.ts:892** _(internal-worker-pk)_ — updateMemoryEmbedding executes db.update(memoryEntries).set({ embedding }).where(eq(memoryEntries.id, id)) with no tenant_id constraint. This allows updating a memory row by id regardless of tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — UPDATE against tenant-scoped table felix_proposals omits tenant_id in WHERE clause (`WHERE id = ${proposalId} AND args_embedding IS NULL`). This allows updating a row by id without constraining to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/tools.ts:6930** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' (video production path) does not include tenant_id in the inserted columns/VALUES.
  - _Why false positive:_ allowlist (independent completed-audit path review 2026-09-03 2026-09-03): shared/schema.ts:1483-1492 defines project_files without a tenant_id column, so ownership is transitive through project_id. Immediately before this video INSERT, server/tools.ts:6905-6907 calls assertProjectInTenant(params.project_id, params._tenantId) and inserts only if that same-tenant ownership check succeeds.
- **server/tools.ts:6930** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT INTO project_files(...) VALUES(...) call omits tenant_id (even though caller asserted project ownership earlier).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column; server/tools.ts:6898 assertProjectInTenant(params.project_id, params._tenantId) guard
- **server/tools.ts:8902** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' is constrained only by id: db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) — missing tenant_id filter. This can allow cross-tenant updates if ids collide or an attacker obtains another tenant's id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8945** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' (marking accepted) lacks a tenant_id constraint. The call db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8962** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' (marking rejected) is missing a tenant_id WHERE clause. The update uses only the row id in the WHERE predicate.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` without any tenant_id constraint (query returns pairing rows across all tenants).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger omits tenant constraint — WHERE id = ${reservationId} has no tenant_id check. This allows updating another tenant's reservation row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger omits tenant constraint — releaseVentureReservation runs UPDATE ... WHERE id = ${reservationId} with no tenant_id filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
