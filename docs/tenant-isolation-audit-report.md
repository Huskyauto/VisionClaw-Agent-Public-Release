# Tenant-Isolation Audit Report

_Generated 2026-07-23T00:58:31.868Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **556/556** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **286** (CRITICAL 80 · HIGH 168 · MEDIUM 36 · LOW 2)
- Precision second-pass (`gpt-5.4`): **149** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (80)

- **server/agentic-engines.ts:242** — UPDATE ai_insights (autoApplyEligibleInsights) updates rows across all tenants — the UPDATE/RETURNING has no tenant_id constraint. This can apply/modify other tenants' insights.
  - _Fix:_ Never perform bulk UPDATE/RETURNING across tenants. Restrict the UPDATE to a single tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. Ensure any auto-apply sweep is executed per-tenant with tenant-scoped WHERE clauses.
- **server/agentic/approvals.ts:134** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to mark pending approvals expired without scoping by tenant_id — this updates approvals across all tenants.
  - _Fix:_ Scope expiry to a tenant (add tenant_id = ${tenantId}) or perform expiry per-tenant. Avoid committing cross-tenant state changes in a single bulk update; require explicit tenant scoping.
- **server/agentic/resume-worker.ts:66** — SELECT across tenant-scoped tables (agent_runs JOIN agent_approvals) has no tenant_id WHERE constraint — this query can read rows for all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE r.tenant_id = ${tenantId} or otherwise parameterize by tenant) or redesign the sweep to iterate per-tenant. Never run cross-tenant SELECTs without an explicit tenant_id equality predicate.
- **server/agentic/wake-scheduler.ts:146** — The scheduler's inner SELECT that claims pending wakes does not filter by tenant_id. The UPDATE uses WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status='pending' AND wake_at <= now() ...), allowing rows from any tenant to be claimed/fired across tenants.
  - _Fix:_ Partition the claim by tenant or otherwise include tenant scoping in the selection. For example, iterate per-tenant and include WHERE tenant_id = <tenantId> in the inner SELECT (or SELECT DISTINCT tenant_id first and process each tenant separately). Ensure any returned rows are always updated/processed with tenant_id checks (e.g. UPDATE ... WHERE id = <id> AND tenant_id = <tenantId>) to prevent cross-tenant claims.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id ... WHERE id = ${c.conversation_id} has no tenant_id restriction — writing tenant-scoped conversation rows without verifying tenant ownership in the UPDATE clause.
  - _Fix:_ Include AND tenant_id = ${c.tenant_id} (or the caller's tenant) in the WHERE of the UPDATE so the update can only modify a conversation belonging to the intended tenant. Never rely solely on previously-read rows for write scoping; enforce tenant_id in the write statement.
- **server/auto-transcript.ts:179** — INSERT INTO project_conversations (...) VALUES (...) is executed without any tenant scoping — can create cross-tenant links because the INSERT lacks tenant context and the table has no tenant_id column.
  - _Fix:_ Only insert links after verifying both project_id and conversation_id belong to the same tenant. If project_conversations is intentionally tenant-agnostic, enforce scoping by validating ownership (e.g. check projects.tenant_id = ${tenantId} and conversations.tenant_id = ${tenantId}) before inserting. Prefer adding tenant_id to the link table and inserting it, or perform the operation per-tenant with explicit tenant checks.
- **server/business-tools.ts:84** — INSERT into invoice_items omits tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this write will create tenant-ambiguous rows or fail — the code never sets tenant_id on child line items.
  - _Fix:_ Include the tenant_id when inserting invoice_items (e.g. add tenant_id column and VALUES (${tid}, ...)), or explicitly validate ownership by looking up the invoice and use its tenant_id. Ensure every INSERT into tenant-scoped tables passes the calling tenant's id.
- **server/ceo-orchestrator.ts:841** — Reading personas via db.select().from(personasTable) with no tenant filter. personas is a tenant-scoped resource; selecting all personas and later using those ids to create conversations (storage.createConversation with tenantId=plan.tenantId but personaId from the unfiltered list) can result in cross-tenant persona usage and unauthorized access.
  - _Fix:_ Query personas scoped to the plan's tenant (e.g. db.select().from(personasTable).where(eq(personasTable.tenant_id, plan.tenantId))). Alternatively load only personas for the tenant when caching, and always verify that any personaId used belongs to the same tenant before passing it into createConversation or other operations.
- **server/data-protection.ts:72** — UPDATE on conversations omits tenant scoping. The softDeleteConversation function verifies the conversation belongs to the caller via a prior SELECT with tenant_id, but the subsequent UPDATE uses WHERE id = ${conversationId} with no tenant_id predicate, allowing an update by id that could affect a different tenant if ids collide or a future code change bypasses the earlier check.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. add AND tenant_id = ${tenantId}), or perform the UPDATE inside a tenant-scoped transaction (withTenantTx) so the DB operation is explicitly constrained to the caller's tenant. Example: UPDATE conversations SET ... WHERE id = ${conversationId} AND tenant_id = ${tenantId}.
- **server/data-protection.ts:125** — Deletes in permanentlyPurgeSoftDeleted are not tenant-scoped. The purge loop deletes from messages, compaction_archives, and conversations using only conversation id (DELETE FROM messages WHERE conversation_id = ${conv.id}; etc.) without constraining by tenant_id. This can delete or affect rows across tenants if conversation ids are ambiguous or if those tables contain tenant-scoped columns that are not checked.
  - _Fix:_ Constrain deletes to the conversation's tenant. If child tables have tenant_id, add AND tenant_id = ${conv.tenant_id}. If child tables lack tenant_id, delete via a safe join to the conversations row, or run the purge per-tenant via withTenantTx(conv.tenant_id, tx => tx.execute(...)) so operations are executed in a tenant-scoped transaction. Example safe form: DELETE FROM messages USING conversations WHERE messages.conversation_id = conversations.id AND conversations.id = ${conv.id} AND conversations.tenant_id = ${conv.tenant_id}.
- **server/delivery-pipeline.ts:683** — SELECT from delivery_logs by stripePaymentId is executed without a tenant_id filter (where eq(delivery_logs.stripePaymentId, req.stripePaymentId)). A request-provided stripePaymentId can be used to enumerate or access other tenants' deliveries.
  - _Fix:_ Always include tenant scoping in the SELECT (e.g. add AND tenant_id = ${tenantId} or pass tenantId into the function). If this lookup must be global, ensure it is only callable from a trusted/internal context and enforce authorization at the callsite; otherwise use eq(deliveryLogs.tenantId, tenantId) in the WHERE.
- **server/embeddings.ts:688** — SELECT from tenant-scoped table `agent_knowledge` without a tenant_id filter (backfillMissingKnowledgeEmbeddings). This reads tenant data across all tenants.
  - _Fix:_ Add explicit tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId}) or only run this backfill under an explicit admin context that documents and enforces cross-tenant intent. Prefer passing an explicit tenantId and limiting the SELECT to that tenant.
- **server/embeddings.ts:716** — SELECT from tenant-scoped table `memory_entries` without a tenant_id filter (backfillEmbeddingVecs). This reads tenant data across all tenants.
  - _Fix:_ Add explicit tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId}) or restrict this background backfill to a safe admin-only path that documents cross-tenant behavior. Prefer passing an explicit tenantId and limiting the SELECT to that tenant.
- **server/embeddings.ts:732** — SELECT from tenant-scoped table `agent_knowledge` without a tenant_id filter (backfillEmbeddingVecs knRows). This reads tenant data across all tenants.
  - _Fix:_ Add explicit tenant scoping (WHERE tenant_id = ${tenantId}) or ensure this function runs only under an audited admin context that intentionally touches all tenants. Prefer per-tenant backfill with a tenantId parameter.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped table (`memory_entries` or `agent_knowledge`) in storeEmbeddingVec does not constrain by tenant_id (WHERE id = ${id} only). This allows modifying rows belonging to any tenant.
  - _Fix:_ Require and pass tenantId into storeEmbeddingVec and include AND tenant_id = ${tenantId} in the WHERE clause. Never perform updates on tenant-scoped tables using only id without verifying tenant ownership.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} (backfillEmbeddingVecs) has no tenant_id constraint. This updates tenant-scoped rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or run the backfill per-tenant with an explicit tenantId parameter and validate ownership before updating.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} (backfillEmbeddingVecs) has no tenant_id constraint. This updates tenant-scoped rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${row.id} AND tenant_id = ${tenantId}), or make the backfill run per-tenant with an explicit tenantId to avoid cross-tenant writes.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images does not include tenant_id (tenant-scoped table writes must include tenant identifier). The INSERT uses session_id only and does not validate that the session belongs to the caller tenant.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. tenant_id = ${tenantId}) or otherwise derive/validate tenant ownership before inserting. Best practice: fetch the presenter_sessions row by id AND tenant_id to verify ownership, then insert presenter_slide_images with tenant_id set to that verified tenant. This prevents cross-tenant writes and enforces the invariant that every INSERT into a tenant-scoped table carries the tenant id explicitly.
- **server/heartbeat.ts:52** — SELECT * FROM research_schedules is executed with no tenant_id filter. This reads rows across all tenants from what appears to be a tenant-scoped table.
  - _Fix:_ Restrict the query to an explicit tenant context (e.g. add WHERE tenant_id = ${tenantId}) or otherwise ensure only permitted/ADMIN tenant rows are read (and document/centralize that intent). If the code needs to iterate schedules per-tenant, enumerate tenant IDs and query per-tenant via a parameterized WHERE tenant_id = ${tid}.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — update lacks tenant_id constraint. This can modify a schedule row belonging to another tenant.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via a storage API that enforces tenant ownership before persisting.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — update lacks tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call into a tenant-aware storage helper to perform the update atomically with tenant scoping.
- **server/heartbeat.ts:112** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — update lacks tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or use a tenant-aware storage function to prevent cross-tenant modification.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — update lacks tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the owning tenant (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform this through a storage API that validates ownership before update.
- **server/heartbeat.ts:631** — SELECT * FROM event_log WHERE status = 'pending' ... — the SELECT does not filter by tenant_id. This reads pending events across tenants and could process events outside an intended tenant context.
  - _Fix:_ Either include tenant_id in the WHERE clause (process per-tenant) or ensure this cross-tenant read is intentional and protected (e.g. only admin-run code with explicit authorization). Prefer per-tenant selection (SELECT ... WHERE status='pending' AND tenant_id = ${tid}) or a tenant-aware storage API.
- **server/internal-resolver.ts:93** — UPDATE event_log executed without constraining by tenant_id — finalizes/ mutates event rows across all tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. add AND tenant_id = ${tenantId}) or require the caller to pass the event's tenantId and verify it. Alternatively, SELECT the event row first to obtain tenant_id and only UPDATE when it matches the resolver's caller context.
- **server/internal-resolver.ts:170** — UPDATE code_proposals mutates proposal rows without constraining by tenant_id — can archive proposals across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending') or perform a prior tenant-validated SELECT and only update when tenant matches the event owner.
- **server/job-queue.ts:98** — claimDueJobs performs a global UPDATE on agent_jobs (status -> 'running') without any tenant_id constraint — can claim/modify jobs across tenants.
  - _Fix:_ Require and pass an explicit tenantId into claimDueJobs (or otherwise scope the claim), and include tenant_id = ${tenantId} in the inner SELECT/outer UPDATE. If the worker must process multiple tenants, call the function per-tenant under the proper tenant context rather than doing a cross-tenant UPDATE.
- **server/job-queue.ts:134** — reclaimExpiredLeases step1 UPDATE sets status='failed_terminal' on agent_jobs without tenant scoping — may affect jobs for other tenants.
  - _Fix:_ Scope lease-reclaim updates to a tenant (require tenantId param) or ensure the operation is intentionally global and gated by an admin-only path. Prefer per-tenant updates: WHERE tenant_id = ${tenantId} AND ... .
- **server/job-queue.ts:148** — reclaimExpiredLeases step2 requeues expired agent_jobs (status -> 'pending') without tenant_id filtering — can requeue jobs from other tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or split the reclaim operation to run per-tenant under an authenticated/system context that has explicit authorization for cross-tenant operations.
- **server/job-queue.ts:177** — completeJob updates agent_jobs to 'succeeded' using only id/attempts guards and no tenant_id constraint — allows completing jobs for other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}), and require the caller to provide/validate the tenantId for the job being completed.
- **server/job-queue.ts:237** — failJob branch updates agent_jobs to 'failed_terminal' using id/attempts but no tenant_id filter — can mark other tenants' jobs as failed terminal.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) and require the caller to supply/validate tenantId. Enforce lease-fencing including tenant_id to avoid cross-tenant side effects.
- **server/job-queue.ts:257** — failJob retry branch updates agent_jobs back to 'pending' (with next_run_at) without tenant scoping — requeues jobs across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause or make the function require/validate tenantId before performing the update; avoid global, unscoped requeues.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs WHERE id = ${id} AND status IN (...) with no tenant filter — can cancel jobs belonging to other tenants.
  - _Fix:_ Enforce tenant scoping in the cancellation (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status IN (...)) and require tenant validation in caller context.
- **server/job-queue.ts:283** — retryJob resets status/attempts for a job id without constraining by tenant_id — allows operator to affect other tenants' jobs.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause and require tenantId to be passed in and validated before performing retry operations.
- **server/minerva-planner.ts:355** — UPDATE on tenant-scoped table `plans` performs a CAS without constraining by tenant_id (WHERE id = ${args.planId} AND status = 'awaiting_approval'). This allows cross-tenant modification/decision of plans (IDOR / unauthorized tenant action).
  - _Fix:_ Include tenant scoping in the CAS UPDATE (e.g. WHERE id = ${args.planId} AND tenant_id = <callerTenantId> AND status = 'awaiting_approval'). Pass the caller's tenantId into decidePlan or resolve/verify the plan's tenant before performing the UPDATE. Ensure all RETURNING columns come from a tenant-scoped row.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` without a tenant_id filter (SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows across all tenants (sensitive tokens stored here).
  - _Fix:_ If this is an admin-level multi-tenant sweep, ensure the codepath is explicitly admin-only and audited. Otherwise, add a tenant_id WHERE clause (WHERE tenant_id = ${tenantId} AND ...) or iterate a vetted list of tenant IDs with explicit intent. Never perform unconstrained reads of tenant-scoped secrets from normal request-path code.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' — unconstrained read across all tenants. This exposes tenant-scoped rows.
  - _Fix:_ Restrict this query to a specific tenant_id when running in tenant-scoped contexts. If the operation must enumerate tenants (startup auto-reconnect), ensure it runs only in a trusted admin startup context and documents/limits access, or change to iterate an explicit, validated list of tenant IDs rather than SELECTing all oauth_subscriptions rows.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh uses SELECT provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE without tenant_id filtering — unconstrained scan of all tenants' oauth_subscriptions (contains secrets).
  - _Fix:_ If this background refresh is intended to act cross-tenant, require an explicit, privileged admin-only execution context and narrowly audit/limit its scope. Otherwise, change to refresh tokens per-tenant by selecting only for a given tenant_id or accepting an explicit tenant list to operate on. Never run unconstrained selects of tenant-scoped secret tables from regular tenant-facing code paths.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions does SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant_id filter — unconstrained cross-tenant read of tenant-scoped table.
  - _Fix:_ Make this an explicit admin-only action or require a validated tenant list. If the intent is per-tenant refresh, change the query to include WHERE tenant_id = ${tenantId} or iterate explicit tenant IDs. Avoid unconstrained reads on tenant-scoped tables containing secrets.
- **server/plan-executor.ts:117** — UPDATE against tenant-scoped table `plans` without tenant_id constraint in WHERE clause (appendExecutionLog updates plans by id only). This allows modifying a plan row belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. accept/derive tenantId and run WHERE id = ${planId} AND tenant_id = ${tenantId}), or first SELECT the plan and validate its tenant before updating. Consider changing appendExecutionLog signature to take tenantId or resolve it from a trusted source and enforce it in the WHERE clause.
- **server/plan-executor.ts:513** — CAS UPDATE claiming a plan (UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved') omits tenant_id in the WHERE clause. This can cause claiming/executing an approved plan belonging to another tenant.
  - _Fix:_ Add tenant scoping to the CAS UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved'). Ensure the caller's tenant (or an explicitly validated admin tenant) is used for the tenant check.
- **server/plan-executor.ts:527** — UPDATE against tenant-scoped table `plans` (setting status='failed') lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}), obtaining tenantId from the claimed/returned plan row or caller's trusted context before performing the write.
- **server/plan-executor.ts:725** — Final UPDATE to `plans` (setting final status and appending execution_log) does not constrain by tenant_id in the WHERE clause, allowing cross-tenant modification.
  - _Fix:_ Use the plan's authoritative tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${planTenantId}). If the code has the plan row (row.tenant_id), pass that into the update to enforce the tenant boundary.
- **server/plan-executor.ts:759** — Error-path UPDATE resetting plan status to 'failed' includes WHERE id = ${planId} AND status = 'executing' but omits tenant_id, allowing altering another tenant's plan in error handling.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) when flipping status on error. Obtain tenantId from the earlier claimed plan row and use it for all subsequent writes to `plans`.
- **server/plan-executor.ts:775** — resumeStuckPlans performs a global UPDATE on `plans` resetting status='approved' for any stale executing plan without tenant scoping. This updates rows across tenants with no tenant filter.
  - _Fix:_ Either run this recovery per-tenant (iterate tenants and scope the UPDATE by tenant_id) or explicitly perform the operation under a verified admin context and document that intent. Prefer scoping by tenant_id (e.g. UPDATE plans SET ... WHERE status='executing' AND tenant_id = ${tenantId} AND updated_at < ...) to maintain application-level tenant isolation.
- **server/plan-executor.ts:784** — SELECT id FROM plans WHERE status = 'approved' ... fetches approved plans across all tenants with no tenant_id filter, then kicks executePlan for each — this may start execution for plans of other tenants without tenant scoping or authorization checks.
  - _Fix:_ Restrict the pending-plan selection to the intended tenant (e.g. SELECT id FROM plans WHERE status = 'approved' AND tenant_id = ${tenantId} ORDER BY id ASC LIMIT 50) or ensure the caller is an explicit admin and that the loop explicitly handles/authorizes multi-tenant recovery. Ideally enumerate tenants and schedule per-tenant recovery jobs rather than a global unscoped SELECT.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads project data without constraining by tenant_id. The function does not accept a tenant context, allowing cross-tenant project access.
  - _Fix:_ Require and pass an explicit tenantId into updateProjectBrain and add WHERE tenant_id = ${tenantId} to the SELECT. Never read tenant-scoped rows by id alone without verifying tenant ownership.
- **server/project-brain.ts:332** — SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 runs without any tenant_id constraint. This can enumerate projects across all tenants.
  - _Fix:_ Scope this query by tenant (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}) and require a tenantId parameter for backfill operations to prevent cross-tenant data access.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed without tenant filtering. Conversations are tenant-scoped and must be constrained.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (and require tenantId in the surrounding function), or join/ensure project -> conversation ownership is checked before reading conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping. Message rows are tenant-scoped and this allows reading messages across tenants if conversation ids are global or uncontrolled.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or otherwise verify conversation belongs to the tenant) when querying messages. Ensure backfill/update flows accept/validate tenant context.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped table (code_proposals) by id only, with no tenant_id constraint or tenant context. This allows cross-tenant access to proposals.
  - _Fix:_ Require an explicit tenantId parameter for verification and include WHERE id = ${proposalId} AND tenant_id = ${tenantId} (or otherwise verify proposal ownership) before returning proposal data.
- **server/proposal-verifier.ts:112** — UPDATE code_proposals ... WHERE id = ${proposalId} clears rebased_at / writes validation_result without constraining by tenant_id. This may modify another tenant's proposal rows.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and require tenant context in the verifier operations.
- **server/proposal-verifier.ts:250** — UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} persists verification results without tenant scoping. This allows updating proposals from other tenants if ids are abused.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause and require the verifier to be called with an explicit tenantId to ensure only owned proposals are modified.
- **server/proposal-verifier.ts:379** — UPDATE code_proposals SET status = 'rejected', verification_status = 'skipped', ... WHERE id = ${proposalId} marks a proposal rejected without a tenant filter. This can change status of proposals across tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and require the rebase/verifier flows to validate tenant ownership before mutating proposals.
- **server/proposal-verifier.ts:399** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking 'hallucinated' rebase outcome) is executed without tenant scoping, enabling cross-tenant updates.
  - _Fix:_ Require tenantId and add AND tenant_id = ${tenantId} to the WHERE clause when updating code_proposals to ensure only the tenant's proposals are modified.
- **server/proposal-verifier.ts:412** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without constraining by tenant_id. That can overwrite or leak other tenants' proposal content.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and make attemptRebaseOnDrift require/validate tenant context before persisting changes.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} with NO tenant_id constraint — this can modify rows belonging to other tenants with the same id if called in an unscoped context.
  - _Fix:_ Always include the tenant constraint in writes. Change the UPDATE to include the row's tenant_id (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or require an explicit caller tenant and use WHERE id = ${id} AND tenant_id = ${tenantId}. Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/research-engine.ts:145** — UPDATE research_sessions in cleanupZombieSessions executes without any tenant_id constraint — this will mark running sessions as completed across all tenants.
  - _Fix:_ Scope the UPDATE to a tenant (e.g. add WHERE tenant_id = ${tenantId} or iterate per-tenant). If intended to act globally, add explicit justification and an admin-only safeguard; otherwise parameterize the tenant_id and validate it before running the statement.
- **server/research-engine.ts:390** — DELETE FROM agent_knowledge (source = 'autoresearch' AND expires_at < NOW()) is executed with no tenant_id filter — this will remove agent_knowledge rows for all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${tenantId}), or run per-tenant cleanup. Ensure the caller provides/validates the tenantId before deleting shared rows.
- **server/routes.ts:2139** — INSERT into tenant-scoped table `presenter_sessions` uses tenantId taken directly from request body (tenantId) without validating that the caller is authorized for that tenant. This allows creation of rows attributed to arbitrary tenants.
  - _Fix:_ Do not accept tenantId from arbitrary request bodies. Derive tenantId from the authenticated session or otherwise strongly-validated context (or require platform-admin and verify it). Validate that the caller owns or is allowed to act for the specified tenant before inserting, e.g. set tenantId = getTenantFromRequest(req) or verify the authenticated caller is permitted to create sessions for the supplied tenantId.
- **server/routes/conversations.ts:98** — UPDATE conversations SET project_id ... WHERE id = ${conv.id} executes without constraining by tenant_id. This is a tenant-scoped table but the UPDATE lacks a tenant predicate.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or perform the update via a storage helper that enforces tenant ownership. Also ensure the conversation belongs to the caller's tenant before updating.
- **server/routes/conversations.ts:102** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not set tenant_id. If project_conversations is tenant-scoped, the INSERT omits tenant_id entirely, violating the invariant that INSERTs must include tenantId.
  - _Fix:_ Add the tenant_id column to the INSERT values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})). Also validate that both the project and conversation belong to tenantId before inserting.
- **server/routes/conversations.ts:104** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} lacks a tenant_id predicate. Projects is tenant-scoped and this UPDATE can affect a project across tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}, and/or verify project ownership before performing the update.
- **server/routes/conversations.ts:228** — INSERT INTO project_notes (project_id, note, author) VALUES (...) does not include tenant_id. project_notes is likely tenant-scoped but the INSERT omits tenant_id, allowing notes to be written without tenant context.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) and verify the project belongs to tenantId before inserting the archive note.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} is executed without constraining by tenant_id. This is a tenant-scoped table and the UPDATE can modify rows belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or perform the mark-read via an existing storage helper that enforces tenant ownership. Ensure all write statements always include tenant_id in the WHERE clause.
- **server/sculptor.ts:93** — UPDATE to tenant-scoped table `sculptor_sessions` performed with WHERE id = ${session.id} but NO tenant_id constraint. This can allow cross-tenant modification if an attacker supplies or guesses an id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${params.tenantId} or use an equivalent Drizzle .where(eq(table.tenant_id, tenantId))). Always constrain writes to the caller's tenant even when a prior SELECT fetched the row.
- **server/sculptor.ts:100** — UPDATE to tenant-scoped table `sculptor_sessions` (setting conversation_id) uses WHERE id = ${session.id} with NO tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${params.tenantId}) or use a parameterized tenant equality check to ensure the update cannot affect rows from other tenants.
- **server/sculptor.ts:155** — UPDATE to tenant-scoped table `sculptor_sessions` (final status/result update) uses WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) so only the caller's tenant row can be modified. Prefer always passing tenantId into the SQL and asserting equality in the WHERE clause.
- **server/sculptor.ts:346** — UPDATE to tenant-scoped table `sculptor_sessions` (setting review_result) uses WHERE id = ${sessionId} without a tenant_id filter.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or use a Drizzle eq check). Even if a prior SELECT verified ownership, every UPDATE must still explicitly enforce tenant scoping in the SQL.
- **server/second-opinion.ts:404** — UPDATE to tenant-scoped table `agent_cost_ledger` (settling a reservation) uses WHERE id = ${reservationId} but NO tenant_id constraint. This can permit modifying another tenant's ledger row by id collision/guessing.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) and pass the tenantId through the settle function so the update can only affect the reservation belonging to that tenant.
- **server/second-opinion.ts:423** — UPDATE to tenant-scoped table `agent_cost_ledger` (releasing a reservation) uses WHERE id = ${reservationId} with NO tenant_id constraint.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise validate ownership) before clearing a reservation. Propagate tenantId to this function and always constrain ledger writes to the caller's tenant.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts into a (likely) tenant-scoped relationship table without setting tenant_id. Per invariant, tenant_id must be provided on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id}) ). Additionally, verify that the referenced project_id and conversation_id belong to the tenant before inserting.
- **server/seed.ts:4459** — UPDATE on tenant-scoped table 'memory_entries' performs a mass replacement (REPLACE 'Manjaro' -> 'wellness-program') without any tenant_id constraint. This will modify memory entries for all tenants, corrupting cross-tenant data.
  - _Fix:_ Constrain the UPDATE to a specific tenant (e.g. AND tenant_id = <tenantId>) or run the migration once per-tenant. If this must be global, require an explicit, audited admin path; otherwise parameterize tenantId and apply safely.
- **server/self-improvement.ts:394** — UPDATE on experiments is constrained only by experiments.id (WHERE eq(experiments.id, experiment.id)) and does not include tenant_id. This allows modifying another tenant's experiment row by id.
  - _Fix:_ Constrain UPDATEs to the caller's tenant as well, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Always include tenant_id in UPDATE/DELETE WHERE clauses for tenant-scoped tables.
- **server/self-improvement.ts:413** — Error-path UPDATE on experiments likewise uses only experiments.id in the WHERE clause and lacks a tenant_id constraint, enabling cross-tenant modification on error handling.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Ensure all error/cleanup updates on tenant-scoped tables also include tenant_id.
- **server/storage.ts:248** — createConversation inserts into conversations but accepts data where tenantId is optional. The invariant states every INSERT into a tenant-scoped table MUST set tenant_id explicitly; allowing omission risks NULL/default or cross-tenant insertion.
  - _Fix:_ Make tenantId mandatory in the function signature and validate it (use assertValidTenantId or tenantScope helper) before inserting. Fail closed if tenantId is missing or invalid.
- **server/storage.ts:472** — deletePersona updates/deletes tenant-scoped tables (conversations, memory_entries, daily_notes) without any tenant_id restriction (e.g. tx.update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id))). This will affect rows across all tenants.
  - _Fix:_ Require a tenant context (tenantId) for persona deletion or include tenant-scoped WHERE clauses on each mutation (e.g. AND eq(conversations.tenantId, tenantId)). If personas are global but referenced by tenant-scoped rows, ensure you only clear references for the relevant tenant(s) or perform this as an authorized global admin operation with explicit checks.
- **server/storage.ts:1296** — getAllDataForExport runs many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with no tenant filtering — it returns full platform data across all tenants.
  - _Fix:_ This must be converted into an explicit admin-only export function with strong authorization checks (and ideally audit logging). If per-tenant export is needed, require tenantId and filter every query by tenant_id (or provide an explicit admin flow that clearly documents global access).
- **server/tools.ts:4533** — SELECT from 'presenter_sessions' (SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...) is performed without any tenant check or ownership verification. If presenter_sessions is tenant-scoped this can leak session tokens/URLs across tenants.
  - _Fix:_ Verify ownership before reading presenter_sessions (for example: assert that the presentation_id belongs to the caller's tenant via an ownership guard function like assertPresentationInTenant(presentationId, tenantId)), or include tenant_id = ${params._tenantId} in the WHERE clause. Never return session tokens/links without first confirming the caller's tenant owns the related presentation.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id constraint (reads pairing rows across all tenants). This violates tenant-isolation invariant and allows cross-tenant discovery/exfiltration.
  - _Fix:_ Restrict the query to the relevant tenant (WHERE tenant_id = ${expectedTenantId}) or require an explicit admin-level operation. If the intent is to detect cross-tenant collisions, perform only a scoped lookup (e.g. lookup by tenant first) and/or move the cross-tenant reconciliation into a carefully-audited admin path that does not return tenant-owned content to unprivileged callers. Always avoid returning full rows from other tenants to request context.

### HIGH (168)

- **server/ab-optimizer.ts:110** — SELECT from `experiments` (runDueAbExperiments) is missing a tenant filter — it reads experiments across all tenants.
  - _Fix:_ Scope the query to a single tenant (e.g. add a WHERE tenant_id = ${tenantId} or accept/iterate per-tenant). Never scan tenant-scoped tables without an explicit tenant_id filter.
- **server/agent-channels.ts:216** — subscribeToChannel inserts/UPSERTs into `channel_subscriptions` using a caller-supplied channelId without verifying the channel belongs to the caller's tenant.
  - _Fix:_ Verify ownership of the referenced channel (SELECT agent_channels WHERE id = channelId AND tenant_id = tenantId) before inserting. Also ensure the upsert/unique constraint includes tenant_id or make the ON CONFLICT target tenant-scoped to avoid touching other tenants' rows.
- **server/agentic-engines.ts:114** — SELECT FROM `plans` (routeInsightToMinerva) queries by source/source_ref without a tenant filter — may match plans from other tenants.
  - _Fix:_ Include tenant_id = ${insight.tenantId} in the WHERE clause (or otherwise scope the lookup by tenant) so idempotency checks operate only within the intended tenant.
- **server/agentic-engines.ts:160** — SELECT FROM `ai_insights` (retryPendingMinervaRouting) is scanning ai_insights across all tenants (no tenant_id WHERE clause).
  - _Fix:_ Run the durability sweep tenant-scoped (accept a tenant parameter or iterate tenants explicitly) and include WHERE tenant_id = ${tenantId} to avoid cross-tenant reads/operations.
- **server/agentic-engines.ts:344** — In gatherOperationalData the heartbeat_logs query SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection' has no tenant filter and thus reads logs across tenants.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND task_name != ...) or otherwise scope the aggregation to the target tenant.
- **server/agentic-engines.ts:518** — heartbeatPerformance query in runOptimizationEngine queries `heartbeat_logs` without a tenant_id WHERE clause (aggregates across tenants).
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) so performance metrics come only from the target tenant.
- **server/agentic-features.ts:599** — enrollInSequence checks for existing enrollments with SELECT id FROM outreach_enrollments WHERE sequence_id = ... AND contact_email = ... AND status = 'active' but does not filter by tenant_id — a collision check crosses tenant boundaries.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the SELECT to ensure duplication checks are performed only within the caller's tenant before inserting an enrollment.
- **server/agentic/harness-adaptation.ts:71** — Query in mineFailures() selects from agent_trace_spans without any tenant_id filter: SELECT ... FROM agent_trace_spans ... — this reads across tenants from a tenant-scoped table.
  - _Fix:_ If this is meant to be an admin/nightly job, make that explicit and gate the function behind an admin-only execution path (and/or use ADMIN_TENANT_ID consistently). Otherwise, add a WHERE tenant_id = ${tenantId} (or accept/derive a tenantId) to limit the query to the caller's tenant. Avoid unscoped SELECTs on tenant-scoped tables to prevent cross-tenant data exposure.
- **server/agentic/runs.ts:36** — appendStep() updates agent_runs with WHERE id = runId but no tenant_id filter — write into another tenant's run row is possible.
  - _Fix:_ Require/accept the caller's tenantId and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) so updates are scoped to the caller's tenant.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs using only runId (no tenant_id constraint).
  - _Fix:_ Add a tenantId parameter and include eq(agentRuns.tenantId, tenantId) in the WHERE clause to ensure the state merge only affects the caller's tenant row.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs with .where(eq(agentRuns.id, runId)) but no tenant_id filter.
  - _Fix:_ Require/accept tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs.status/result/completedAt using only runId in WHERE — missing tenant_id constraint.
  - _Fix:_ Add tenant scoping to the update (include eq(agentRuns.tenantId, tenantId) in WHERE) or accept tenantId from caller and enforce it before updating.
- **server/agentic/runs.ts:88** — failRun() updates agent_runs setting status='failed' etc. but the WHERE clause only uses runId; no tenant_id constraint present.
  - _Fix:_ Require tenantId and include it in the WHERE (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) to prevent cross-tenant writes.
- **server/agentic/runs.ts:97** — pauseRun() updates agent_runs.status/state with WHERE id = runId but no tenant_id filter.
  - _Fix:_ Include tenant_id in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or accept tenantId param to scope the pause to the caller's tenant.
- **server/auto-memorize.ts:160** — The bulk messages read scans the messages table across ALL rows without any tenant_id constraint (SELECT ... FROM messages m LEFT JOIN conversations c ... WHERE m.created_at > ...). This allows cross-tenant message content to be read and processed.
  - _Fix:_ Apply explicit tenant scoping to the query (e.g. add WHERE m.tenant_id = ${tenantId} or join on conversations and filter by c.tenant_id = ${tenantId}) or else explicitly and auditablely restrict this job to operate only on an admin-intended global index with documented consent. If the intent is platform-level processing, explicitly limit/annotate the query and move it to a clearly auditable admin-only path and ensure LLM calls and outputs are constrained and logged.
- **server/auto-memorize.ts:100** — isDuplicate() queries memory_entries across all tenants (SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ... ) with no tenant_id constraint, enabling cross-tenant memory comparisons and potential leakage of tenant data.
  - _Fix:_ Restrict the dedupe query to the relevant tenant (e.g. add WHERE tenant_id = ${tenantId} or otherwise pass an explicit tenant context). If global dedupe is required by design, make that explicit, run under an auditable admin context, and document/admin-consent this cross-tenant operation.
- **server/auto-transcript.ts:128** — SELECT over conversations (backfill) without tenant_id constraint — reads tenant-scoped rows across all tenants.
  - _Fix:_ Run this backfill per-tenant or add an explicit WHERE c.tenant_id = ${tenantId} (or accept a tenantId parameter) so the query never selects conversations from other tenants. Ensure any downstream processing only uses rows for the intended tenant.
- **server/auto-transcript.ts:153** — SELECT joining project_conversations, conversations, projects lacks a tenant_id filter — may return cross-tenant rows.
  - _Fix:_ Restrict the query by tenant (e.g. add WHERE c.tenant_id = ${tenantId} or pass tenantId into the function and filter). Alternatively iterate per-tenant and run the join scoped to that tenant to avoid leaking or acting on other tenants' data.
- **server/auto-transcript.ts:171** — SELECT to find missing project_conversations links reads conversations across all tenants (no tenant_id filter).
  - _Fix:_ Scope this SELECT by tenant (e.g. WHERE c.tenant_id = ${tenantId}) or run as a per-tenant operation so you only discover/insert links for a single tenant at a time.
- **server/business-tools.ts:334** — SELECT in listContracts LEFT JOINs customers WITHOUT constraining the joined customers row by tenant_id. The query returns cust.company_name via cust ON cust.id = c.customer_id but does not ensure cust.tenant_id = c.tenant_id (or = caller tenant), potentially exposing another tenant's customer data.
  - _Fix:_ Constrain the join to the tenant context (for example: LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = c.tenant_id) or explicitly AND cust.tenant_id = ${tid}. Always ensure joined tenant-scoped tables are filtered by tenant_id to preserve isolation.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` is executed by id only, without constraining to the owning tenant_id. This allows marking/altering another tenant's review row if an attacker can guess an id or if pending.reviewId becomes tainted.
  - _Fix:_ Include tenant ownership in the UPDATE (e.g. add AND tenant_id = ${tenantId}) and ensure markReviewReused has access to/validates the tenant context (store tenantId in the pending entry or accept tenantId in the function). Use application-level checks to confirm the caller owns the row before updating.
- **server/chat-engine.ts:4332** — Call to storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is invoked without any tenant context. If attachCitations inserts into a tenant-scoped citations table, the INSERT may omit tenant_id (or otherwise not constrain by tenant), allowing cross-tenant citation records or write failures.
  - _Fix:_ Ensure attachCitations is passed an explicit tenant identifier (tenantId or tenantIdForScope) or resolve the message->tenant server-side and include tenant_id in the INSERT. At minimum, change the call to storage.attachCitations(messageId, collectedCitations, tenantIdForScope) and update the implementation to include WHERE/INSERT tenant_id checks and validate the message belongs to that tenant before writing.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, toolName, !hasError) (and the subsequent call recordToolExecution(conversationId, toolName, true, ids)) is invoked with only a foreign id (conversationId) and no tenant information. If recordToolExecution writes to a tenant-scoped table or uses conversationId to associate rows, it may perform writes without verifying the caller's tenant owns that conversation.
  - _Fix:_ Pass explicit tenant context to recordToolExecution (e.g. recordToolExecution(tenantIdForScope, conversationId, ...)) or resolve and verify that the conversationId belongs to the caller's tenant before any write. Update recordToolExecution to require tenantId and enforce WHERE tenant_id = :tenantId for any reads/updates and set tenant_id explicitly on INSERTs.
- **server/commitment-drafter.ts:234** — Unscoped UPDATE on tenant-scoped table 'commitments' — `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` lacks a tenant_id constraint. This can revert a row belonging to another tenant if ids collide.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${expectedTenantId}`) or otherwise validate ownership before mutating. Ensure every UPDATE/DELETE on commitments is scoped to the caller's tenant_id.
- **server/compaction.ts:463** — INSERT into 'memory_entries' uses a possibly-undefined tenantId: `INSERT INTO memory_entries (..., tenant_id) VALUES (..., ${tenantId})`. The function accepts tenantId? and does not validate it, so tenant_id may be NULL or incorrect, violating tenant-scoped insert invariant.
  - _Fix:_ Require and validate tenantId before writing. Fail-closed if tenantId is missing/invalid. Always pass an explicit, trusted tenantId to INSERTs into tenant-scoped tables (e.g. throw or return without inserting when tenantId is not a positive integer).
- **server/credential-vault.ts:169** — Unscoped UPDATE on tenant-scoped table 'credential_vault' — `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` omits tenant_id constraint. This can update another tenant's credential if ids overlap.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. `WHERE id = ${id} AND tenant_id = ${tenantId}`) or perform ownership verification prior to the update. Ensure every UPDATE/DELETE includes tenant_id for tenant-scoped tables.
- **server/credential-vault.ts:176** — Unscoped UPDATE on tenant-scoped table 'credential_vault' — `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` omits tenant_id constraint when updating last_used_at after returning credentials.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${cred.id} AND tenant_id = ${tenantId}`), or derive and validate tenantId from a trusted source before mutating. Every write to tenant-scoped tables must be explicitly tenant-scoped.
- **server/data-protection.ts:144** — verifyMessageSaved reads messages by conversation_id without tenant scoping. The function signature lacks a tenantId and does a SELECT FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} — a caller could query messages for a conversation id that belongs to another tenant.
  - _Fix:_ Require and pass tenantId to verifyMessageSaved and constrain the SELECT to the tenant (e.g. WHERE conversation_id = ${conversationId} AND role = ${role} AND tenant_id = ${tenantId}), or verify the conversation belongs to the tenant first (SELECT FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId}) before reading messages.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog performs UPDATE on tenant-scoped delivery_logs without constraining by tenant_id (WHERE eq(delivery_logs.id, id) only). This allows one tenant to update another tenant's delivery row.
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog and include it in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or otherwise validate ownership before updating.
- **server/delivery-pipeline.ts:720** — UPDATE on delivery_logs that nulls stripePaymentId uses WHERE eq(delivery_logs.id, prior.id) without tenant_id constraint. This updates a delivery row regardless of tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE (AND tenant_id = ${tenantId}) or validate the prior.id belongs to the caller's tenant before performing the UPDATE. Prefer using Drizzle eq(deliveryLogs.tenantId, tenantId).
- **server/delivery-pipeline.ts:955** — RetryDelivery SELECT from delivery_logs uses a dynamic WHERE that can be eq(delivery_logs.id, deliveryId) when tenantId is omitted — resulting in a tenant-unscoped SELECT/possible access when tenantId is not provided.
  - _Fix:_ Avoid allowing an unscoped path or ensure the caller always supplies a validated tenantId. If internal callers need to bypass scoping, restrict this API to internal-only code paths and document/guard that explicitly; otherwise require and apply eq(deliveryLogs.tenantId, tenantId).
- **server/delivery-pipeline.ts:980** — getDeliveryStatus SELECT from delivery_logs can execute without tenant constraint when tenantId is omitted (WHERE may be eq(delivery_logs.id, deliveryId)). This returns tenant-scoped rows without enforcing caller tenant.
  - _Fix:_ Require tenantId and include eq(deliveryLogs.tenantId, tenantId) in the WHERE, or gate the function so only trusted internal callers can omit tenant scoping. Prefer explicit tenant parameter and use Drizzle .where(and(...)).
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks executed without tenant_id filter (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can delete chunks belonging to other tenants.
  - _Fix:_ Include tenant scoping in the DELETE (AND tenant_id = ${tenantId}) or otherwise validate that collectionId belongs to the caller's tenant before removing chunks.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} is executed without tenant_id constraint. Updating by id only can modify another tenant's chunk if id is global.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or look up the tenant_id for that id and validate ownership before updating.
- **server/dream-consolidation.ts:83** — Query for messages inside getRecentSessionSummaries: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ... lacks tenant scoping. This can read messages from other tenants' conversations.
  - _Fix:_ Include tenant scoping in the messages SELECT (AND tenant_id = ${tenantId}) or otherwise validate the conversation belongs to tenantId before fetching messages.
- **server/dormant-deprecation.ts:169** — Traffic gate query counts user messages across the messages table without tenant_id (SELECT COUNT(*) FROM messages WHERE ... AND role = 'user'). If messages is tenant-scoped, this aggregates across all tenants rather than scoping to a tenant.
  - _Fix:_ If this operation should be per-tenant, add tenant scoping (AND tenant_id = ${tenantId}). If it is intentionally global, ensure this function is only runnable by authorized platform operators and document that it queries global tables. Prefer explicit separation of tenant vs platform-level metrics.
- **server/dormant-deprecation.ts:179** — Query counting distinct tools active uses tool_performance without tenant filter (SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ...). This may aggregate across tenants if tool_performance is tenant-scoped.
  - _Fix:_ Scope by tenant (AND tenant_id = ${tenantId}) for tenant-level decisions, or restrict this module to platform-admin operations and document that it intentionally queries global metrics. Prefer explicit tenant parameter and eq(tool_performance.tenantId, tenantId) in WHERE.
- **server/email.ts:365** — Polling loop checks for existing inbox_messages using SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 with no tenant_id. This may cause cross-tenant detection/duplication or store messages incorrectly across tenants.
  - _Fix:_ Include tenant scoping in the existence check (AND tenant_id = ${tenantId}) or compute existence using message_id + inbox_id + tenant_id. Ensure the poller determines the proper tenant before the lookup.
- **server/email.ts:410** — backfillEmptyBodies runs SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... without tenant_id, iterating over all inbox_messages possibly across tenants.
  - _Fix:_ Restrict the query to a tenant (AND tenant_id = ${tenantId}) or otherwise run backfill in a tenant-scoped manner. If the operation is intended to be global, require platform-admin authorization and document it.
- **server/embeddings.ts:244** — Use of sql.raw with an interpolated vector literal: sql.raw(`'${vec}'::vector`) in vectorSearchMemory. The vector is derived from the caller's query (request-derived) and is being injected into raw SQL.
  - _Fix:_ Avoid building SQL via string interpolation. Parameterize the vector value via the query parameter binding provided by Drizzle (do not use sql.raw with request-derived strings). If a raw literal is unavoidable, validate and bind it safely (and include tenant scope), or use a prepared statement / Drizzle-native approach that keeps the value parameterized rather than string-interpolated.
- **server/embeddings.ts:252** — Use of sql.raw with an interpolated vector literal in ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} — vector is request-derived.
  - _Fix:_ Parameterize the vector value instead of using sql.raw interpolation. Use driver/Drizzle parameter binding or a supported pgvector binding mechanism so untrusted input isn't injected into raw SQL.
- **server/embeddings.ts:559** — Use of sql.raw with an interpolated vector literal: sql.raw(`'${vec}'::vector`) in vectorSearchKnowledge. The vector is derived from the caller's query (request-derived) and is being injected into raw SQL.
  - _Fix:_ Replace sql.raw interpolation with parameterized binding of the vector value (use Drizzle parameter placeholders or a proper pgvector binding). Validate shape/contents server-side and avoid string-interpolating request-derived data into SQL.
- **server/embeddings.ts:565** — Use of sql.raw with an interpolated vector literal in ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} — vector is request-derived.
  - _Fix:_ Avoid sql.raw interpolation of request-derived vectors; use parameterized queries/prepared statements or Drizzle-native facilities to bind the vector value safely. Also ensure tenant scoping is present (it is here) and maintain validation of the embedding shape before binding.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' is executed without constraining by tenant_id. This boot-time rehydration pulls rows for all tenants into process memory.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant and run the rehydration per-tenant context. Never select tenant-scoped rows across all tenants in a global query.
- **server/firecrawl.ts:184** — UPDATE against tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) and lacks an explicit tenant_id constraint — a write that can affect other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. AND eq(scrapedPages.tenantId, tenantId)) or perform the update using the same tenant-scoped selector (update where id AND tenantId) to ensure the caller cannot update another tenant's row.
- **server/firecrawl.ts:307** — UPDATE of scraped_pages in the crawl loop uses only the page id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) without constraining tenant_id — cross-tenant write risk.
  - _Fix:_ Require tenant scoping on the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so updates cannot modify rows belonging to other tenants.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' fetches driveFolderId/driveFolderUrl by project id using WHERE eq(projects.id, projectId) but does not include tenant scoping (tenant_id). Sensitive project Drive metadata is therefore read without constraining to caller tenant.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise resolve the project via a helper that enforces tenant ownership before returning Drive folder fields.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' (setting driveFolderId/driveFolderUrl) uses WHERE eq(projects.id, projectId) without constraining by tenant_id — this can update another tenant's project row.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so only the owning tenant's project can be modified.
- **server/google-drive.ts:826** — Handler uploadAndShare selects project row by id (WHERE eq(projects.id, params.projectId)) without filtering by tenant. This reads a tenant-scoped row based on a caller-supplied project_id without the required tenant filter.
  - _Fix:_ Either include eq(projects.tenantId, params.tenantId) in the WHERE, or call an assertProjectInTenant helper that verifies ownership and performs tenant-scoped reads to avoid leaking another tenant's project info.
- **server/google-drive.ts:1003** — searchDriveFiles does an initial SELECT of projects by project id with WHERE eq(projects.id, params.projectId) and then checks proj.tenantId against params.tenantId — but the SELECT itself lacks an explicit tenant_id constraint (tenant-scoped read without WHERE tenant_id).
  - _Fix:_ Perform the initial verification with a tenant-scoped WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${params.tenantId}) so the database enforces tenant isolation at query time rather than relying on post-query comparison.
- **server/google-drive.ts:1066** — Later in searchDriveFiles the code selects projects.driveFolderId using WHERE eq(projects.id, params.projectId) without tenant scoping. Even though an earlier check validated ownership, the DB query itself is not constrained to the tenant.
  - _Fix:_ Include tenant scoping on this SELECT (e.g. WHERE id = ${params.projectId} AND tenant_id = ${params.tenantId}) or reuse the previously-validated tenant-scoped row to avoid any non-scoped DB access.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} does not constrain by tenant_id or verify that the session belongs to the caller's tenant.
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or first SELECT the session by id and assert session.tenant_id === tenantId before performing the update. Always verify ownership and include tenant_id in WHERE clauses for updates on tenant-scoped tables.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) performs writes across proposals without constraining by tenant_id — may modify rows belonging to other tenants.
  - _Fix:_ When archiving proposals, include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or resolve the tenant for each id and validate ownership before changing status. If this is meant to be a global admin action, require and verify an explicit admin authorization context before performing cross-tenant writes.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id without restricting by tenant_id — this can affect tasks across tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or verify, per id, that the task belongs to the intended tenant before disabling. For system-wide operations, require explicit admin authorization and make the cross-tenant mutation explicit and auditable.
- **server/heartbeat.ts:257** — UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} executes without a tenant_id constraint — this can update another tenant's task row.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage layer (which should enforce tenant ownership) to perform the update.
- **server/heartbeat.ts:261** — UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} (second normalization) lacks tenant_id filter.
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use tenant-aware storage APIs.
- **server/heartbeat.ts:2836** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id in the WHERE clause. This write can affect approval_state across tenants.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update through a storage function that enforces tenant ownership/validation.
- **server/heartbeat.ts:3289** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat path) — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage API to set approval_status, ensuring the caller's tenant actually owns the task row before updating.
- **server/internal-resolver.ts:106** — SELECT FROM plans by id has no tenant_id filter — may read plan rows belonging to other tenants.
  - _Fix:_ Restrict the SELECT to the caller's tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId} LIMIT 1) or retrieve the plan and assert its tenant before taking any action. Ensure the function receives or resolves the tenant id for the event before reading.
- **server/job-queue.ts:220** — SELECT max_attempts FROM agent_jobs WHERE id = ${id} runs without tenant_id constraint — reads job metadata across tenants.
  - _Fix:_ SELECT should include tenant_id = ${tenantId} or the function should first validate ownership of the job by id/tenant before reading/updating; require tenantId parameter for the lookup.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor SELECTs tenant_id, persona_id, COUNT(*) FROM agent_knowledge without WHERE tenant_id = ... — it enumerates all tenants/personas using a tenant-scoped table without per-tenant authorization.
  - _Fix:_ If this monitor legitimately needs to iterate all tenants, ensure the reconciler runs in a strictly-admin context with explicit audit and authorization. Otherwise, scope scans per-tenant (require tenantId param) and run per-tenant snapshots under the tenant context. At minimum document and gate any global cross-tenant reads with owner/admin checks.
- **server/lib/aevo-meta-editor.ts:447** — UPDATE on procedureEvolutionRuns performed with only id predicate (where(eq(procedureEvolutionRuns.id, runId))) — no tenant_id constraint. This can mutate another tenant's row if an attacker knows/guesses a run id.
  - _Fix:_ Add an explicit tenant predicate to the WHERE clause. For example: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Ensure the function uses the same tenantId that created the runRow (or otherwise validates ownership) before updating.
- **server/lib/aevo-meta-editor.ts:459** — UPDATE on procedureEvolutionRuns performed with only id predicate (where(eq(procedureEvolutionRuns.id, runId))) — no tenant_id constraint. Allows cross-tenant modification of the run row.
  - _Fix:_ Include the tenant predicate in the WHERE to enforce tenant isolation, e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Prefer defensive checks that runId was created for this tenant before issuing updates.
- **server/lib/revenue-missions.ts:89** — createMission inserts `project_id` from caller input directly into revenue_missions without validating that the referenced project belongs to the same tenant.
  - _Fix:_ Validate any provided projectId before inserting: SELECT the project by id and ensure its tenant_id matches input.tenantId (or reject/NULL it). Alternatively require callers to provide only tenant-scoped references (or remove projectId from the public API) and always pass tenant_id explicitly in the INSERT.
- **server/lib/revenue-missions.ts:161** — addEvidence writes caller-supplied experimentId into mission_evidence (experiment_id) without verifying the experiment row belongs to the same tenant/mission. This lets a caller attach evidence referencing an experiment from another tenant.
  - _Fix:_ If experimentId is provided, SELECT the experiment row and verify its tenant_id (and ideally its mission_id) match input.tenantId and input.missionId before inserting. Reject the request (fail-closed) if ownership does not match.
- **server/lib/pinned-hypotheses.ts:141** — pinHypothesis inserts conversation_id and persona_id from caller input directly into pinned_hypotheses without checking those foreign ids belong to the caller's tenant.
  - _Fix:_ Before inserting, validate conversationId and personaId (if provided) by querying their tables with a WHERE id = ? AND tenant_id = ?. Reject or NULL-out values that don't belong to the tenant to prevent cross-tenant references.
- **server/lib/scheduled-post-runner.ts:60** — SELECT ... FROM scheduled_posts (CTE 'due') is executed with no tenant_id filter — the runner reads tenant-scoped rows across all tenants.
  - _Fix:_ If this operation must be per-tenant, iterate tenants and include WHERE tenant_id = <tenantId> in the CTE (or accept an explicit tenantId parameter). If it is intentionally cross-tenant, restrict execution to an explicit admin-only path and document/authorize it; otherwise add an application-level tenant_id constraint to the SELECT so rows are always limited to the caller's tenant.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} is executed without also constraining tenant_id — this allows updates to a scheduled_posts row regardless of tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} (or ${tenantId} from the caller) so writes are constrained to the owning tenant. If the runner legitimately operates system-wide, require an explicit admin authorization and make that explicit in the callsite.
- **server/lib/scheduled-post-runner.ts:191** — UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' (error/release path) lacks a tenant_id filter — a row could be modified across tenants.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} (or caller tenantId) to the WHERE clause to ensure the update is applied only to the intended tenant's row. If cross-tenant behavior is required, make it explicit and gated to admin-only code paths.
- **server/lib/stuck-task-sweep.ts:56** — SELECT ... FROM agent_runs WHERE status = 'running' ... is executed with no tenant_id filter — tenant-scoped agent_runs are scanned across all tenants.
  - _Fix:_ Either accept a tenantId parameter and add WHERE tenant_id = ${tenantId}, or explicitly document and gate the admin/system-level intent. Avoid silent cross-tenant reads by scoping the query to the caller's tenant.
- **server/lib/stuck-task-sweep.ts:84** — SELECT ... FROM mind_tickets WHERE status = 'in_progress' ... is executed with no tenant_id filter — this reads tenant-scoped mind_tickets across all tenants.
  - _Fix:_ Require an explicit tenantId filter in the WHERE clause (WHERE tenant_id = ${tenantId}) or otherwise restrict this audit to an authorized admin context with an explicit, reviewed justification for cross-tenant access.
- **server/memory-graph.ts:66** — UPDATE against tenant-scoped table memory_categories omits tenant_id in WHERE clause (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This allows modifying a category row without constraining to the caller's tenant.
  - _Fix:_ Include the tenant guard in the UPDATE. For example, pass tenantId through to this call and run: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Prefer using the same tx/context that enforced the original SELECT so the change is scoped to the correct tenant.
- **server/memory-graph.ts:126** — UPDATE against tenant-scoped table memory_categories omits tenant_id in WHERE clause (UPDATE memory_categories SET memory_count = (...) WHERE id = ${categoryId}). The function refreshCategoryCount only takes categoryId and updates by id alone, violating the tenant-isolation invariant.
  - _Fix:_ Make refreshCategoryCount accept tenantId (or obtain it from the tx) and include tenant filtering in both the inner count and outer UPDATE. Example: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status='active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:393** — UPDATE against tenant-scoped table memory_entries omits tenant_id in WHERE clause (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). Even though mem.id was selected earlier with a tenant filter, the UPDATE itself is not constrained to tenant and violates the application-level isolation invariant.
  - _Fix:_ Constrain the UPDATE to the tenant. Either use the storage API that accepts tenantId or change the UPDATE to: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Pass tenantId through to this scope (do not rely on prior selects alone).
- **server/minerva-planner.ts:373** — SELECT probe (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}) reads a tenant-scoped row without a tenant_id constraint, allowing unauthorized reads of other tenants' plan state.
  - _Fix:_ Constrain the SELECT by tenant_id (e.g. WHERE id = ${args.planId} AND tenant_id = <callerTenantId>) or require the caller to provide/verify tenantId before probing. Never query tenant-scoped tables by id alone in externally-invokable handlers.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} (child plan probe) lacks any tenant_id constraint. This can expose or return cross-tenant child plan ids.
  - _Fix:_ Add tenant scoping to this query (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = <callerTenantId>) or fetch the candidate child and verify its tenant_id matches the caller before returning success. All plan lookups must be tenant-scoped.
- **server/minds-engine.ts:327** — markEventsHandled builds a Postgres array literal via string interpolation: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. The eventIds array is caller-supplied and string-interpolated into SQL, creating injection risk and weakening tenant-isolation guarantees if values are not validated.
  - _Fix:_ Avoid constructing SQL array literals by string concatenation. Use parameterized array binding (pass eventIds as an actual array parameter) or Drizzle helpers (e.g. sql-array utilities) and validate each id is an integer belonging to the tenant. Example: use WHERE id = ANY(${eventIds}::int[]) with eventIds provided as a parameter, or use a safe helper that binds arrays, after validating IDs.
- **server/minds-engine.ts:214** — createTicket interpolates params.dependsOn into an array literal string for INSERT: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. The dependsOn value is request-derived and is being string-constructed into SQL, risking injection and broken tenant isolation.
  - _Fix:_ Do not stringify client-supplied arrays into SQL array literals. Validate that params.dependsOn is an array of integers and pass it as a parameterized int[] value (or use Drizzle's typed bindings). Example: bind dependsOn as a JS array and insert with a parameter placeholder or use sql`... VALUES (..., ${params.dependsOn}, ...)` where the library binds arrays safely.
- **server/minds-engine.ts:262** — checkDependenciesMet constructs a Postgres array literal with ticket.dependsOn via string interpolation: WHERE id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. While ticket.dependsOn originates from the DB, it may have been initially populated from request data (createTicket). Using string-interpolated SQL for values that can be request-derived risks injection/isolation issues.
  - _Fix:_ Instead of building an array literal string, bind the integer array as a parameter. Validate array contents are integers and belong to the tenant (or re-derive dependencies via a safe join). Use parameterized array bindings or Drizzle helpers (e.g. WHERE id = ANY(${ticket.dependsOn}::int[])) to avoid string concatenation in SQL.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES. The table appears tenant-scoped but the INSERT omits tenant_id (relying only on assertProjectInTenant prior to insert).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Keep/assert ownership (assertProjectInTenant) but still write tenant_id to the row to enforce isolation at the data level and satisfy the application-level tenant filter invariant.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id in the VALUES. Same pattern as above in the parallel render path.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Verify ownership first (assertProjectInTenant) and ensure the tenant_id column is populated on every insert into tenant-scoped tables.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(lockKey) inside a parameterized SQL template to call pg_advisory_xact_lock with a value derived from ledgerKey (a caller-supplied value). Using sql.raw with request-derived data can introduce injection/isolation risks because it embeds raw SQL text instead of parameterizing.
  - _Fix:_ Avoid sql.raw for values derived from callers. Compute a numeric lock id and pass it as a bound parameter so the driver parameterizes it (e.g. const lockVal = BigInt(lockKey); await tx.execute(sql`SELECT pg_advisory_xact_lock(${lockVal}::bigint)`);). Alternatively, validate/sanitize the value strictly to a numeric BigInt and then pass it as a parameter rather than via sql.raw.
- **server/proactive-engine.ts:139** — SELECT persona_id FROM proactive_actions WHERE id = ${actionId} executes without constraining by tenant_id. This reads tenant-scoped data (persona_id) for an arbitrary actionId and can leak cross-tenant information.
  - _Fix:_ Add tenant scoping to the SELECT, e.g. SELECT persona_id FROM proactive_actions WHERE id = ${actionId} AND tenant_id = ${tenantId}, or otherwise verify the actionId belongs to the caller's tenant before reading it.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates a tenant-scoped table using only id and no tenant_id constraint. An attacker or wrong id could modify another tenant's rows.
  - _Fix:_ Include tenant scoping on the UPDATE: UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId}. Prefer using parameterized tenantId everywhere rather than relying on prior SELECT provenance.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} performs a write without restricting by tenant_id. The SELECT that produced desk came from a tenant-scoped query, but the UPDATE must still enforce tenant scoping to prevent cross-tenant modification.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or use a safe parameter) to the WHERE clause of the UPDATE to ensure only the intended tenant's row is modified.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant_id in the WHERE clause. This increments trigger_count on a rule id without verifying ownership.
  - _Fix:_ Include tenant filtering in the UPDATE, e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}, so rule updates cannot affect other tenants.
- **server/recurring-messages.ts:258** — Runner SELECT: SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500 is unscoped — it reads rows for all tenants.
  - _Fix:_ If the runner must enumerate all tenants, make that explicit and restrict the capability to a trusted internal job. Otherwise, add a tenant filter (WHERE tenant_id = ${tenantId}). If a global scan is required, ensure the subsequent processing always enforces tenant-scoped updates (see next finding) and record tight audit logs; better: query per-tenant batches and process per-tenant.
- **server/reference-learner.ts:290** — UPDATE memory_entries SET ... WHERE id = ${storedId} executes without constraining tenant_id (tenant-scoped table). This allows cross-tenant modification if IDs collide or if function is called outside tenant context.
  - _Fix:_ Constrain the UPDATE by tenant (and persona if appropriate). For example: WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}. Prefer passing and asserting the tenant context explicitly for all writes to tenant-scoped tables.
- **server/quarterly-intelligence.ts:343** — CREATE TABLE model_registry_updates defines tenant_id INTEGER NOT NULL DEFAULT 1. The codebase invariant requires INSERTs to pass tenant_id explicitly (no default) to avoid silent misattribution to admin tenant.
  - _Fix:_ Do not define a tenant_id default. Create the table with tenant_id INTEGER NOT NULL (no DEFAULT) so callers must supply tenant_id on every INSERT. If a default is intentionally required, document and audit its use; prefer explicit tenant arguments to every INSERT to avoid accidental cross-tenant attribution.
- **server/research-engine.ts:320** — SELECT name FROM personas WHERE id = ${program.persona_id} executes without constraining personas by tenant_id. If personas are tenant-scoped, this permits reads of other tenants' persona rows.
  - _Fix:_ Restrict the query to the session/program tenant (e.g. SELECT name FROM personas WHERE id = ${program.persona_id} AND tenant_id = ${tenantId}). If personas are intentionally global, document that explicitly.
- **server/research-engine.ts:979** — resolvePersonaId performs SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 with no tenant_id constraint — it can return persona ids from other tenants.
  - _Fix:_ Accept and use a tenantId parameter in resolvePersonaId and add WHERE tenant_id = ${tenantId} to the query, or otherwise validate that the returned persona id belongs to the caller's tenant before using it.
- **server/research-engine.ts:1001** — injectKeepedFinding calls resolvePersonaId (which is unscoped) and then INSERTs agent_knowledge with persona_id set from that result without verifying persona ownership — a foreign id from an unvalidated source is used when writing tenant-scoped rows.
  - _Fix:_ Ensure resolvePersonaId is tenant-scoped (use tenantId) or explicitly verify the resolved persona_id belongs to session.tenantId before inserting. If persona lookup fails or returns a different tenant, set persona_id NULL or abort the insert.
- **server/research-engine.ts:1446** — The replayHighValueFindings query JOINs research_programs rp ON rp.id = re.program_id but does not constrain rp.tenant_id. Since foreign-key tenant ownership is not enforced at DB level, this can read program rows not owned by the caller's tenant.
  - _Fix:_ Add tenant scoping to the JOIN (e.g. AND rp.tenant_id = re.tenant_id or include rp.tenant_id = ${tenantId} in the WHERE/JOIN) or explicitly verify rp.tenant_id matches the expected tenant before using its fields.
- **server/research-engine.ts:1807** — generateResearchDigest sessionsResult SELECT joins research_programs rp ON rs.program_id = rp.id but does not assert rp.tenant_id = rs.tenant_id (or = ${tenantId}). This can surface program metadata from other tenants when building digests.
  - _Fix:_ Constrain the join to the tenant (e.g. JOIN research_programs rp ON rs.program_id = rp.id AND rp.tenant_id = rs.tenant_id) or add rp.tenant_id = ${tenantId} to the WHERE clause so only tenant-owned programs are read.
- **server/research-engine.ts:1818** — generateResearchDigest findingsResult SELECT joins research_programs via rs.program_id without validating the program's tenant_id. This reads program metadata (rp.name) for sessions/reports without ensuring program ownership.
  - _Fix:_ Ensure the JOIN includes rp.tenant_id = rs.tenant_id (or add WHERE rp.tenant_id = ${tenantId}) so the joined research_programs rows are verified to belong to the digest's tenant.
- **server/routes.ts:165** — SELECT from tenant-scoped table `file_storage` without tenant constraint (restoreUploadsFromDb). This reads raw file blobs across all tenants and writes them to disk without verifying ownership.
  - _Fix:_ Restrict the query to a specific tenant or iterate per-tenant. Add a WHERE clause filtering file_storage.tenant_id = <tenantId> (or equivalent Drizzle eq(fileStorage.tenantId, tenantId)) and ensure any disk writes are only for files owned by the intended tenant. If this is a global admin job, run it explicitly as an admin-only task and document the privileged intent.
- **server/routes.ts:1116** — SELECT from tenant-scoped table `file_storage` by filename alone (owners lookup) — no tenant_id filter. The code fetches all owners of a basename across tenants.
  - _Fix:_ Avoid unscoped lookups. If the intention is to check ownership for the requesting tenant, add eq(fileStorage.tenantId, authedTenantId) in the WHERE clause. If you must detect collisions across tenants, ensure this operation is restricted to a privileged/admin context and do not return tenant-identifying data to unprivileged callers.
- **server/routes.ts:2056** — Public GET selects presenter_sessions by token and returns tenant_id and other fields without verifying caller ownership. Reading a tenant-scoped table without tenant scoping can leak tenant information.
  - _Fix:_ Treat the token as a capability and avoid returning tenant-identifying fields. If the endpoint must be public, ensure the token is a true capability that was cryptographically bound to the resource and do not expose tenant_id. If the endpoint is meant for authenticated callers, enforce tenant checks (WHERE tenant_id = callerTenant) before returning rows.
- **server/routes.ts:4324** — SELECT from tenant-scoped table 'conversations' is missing a tenant_id constraint. The query selects project_id by conversation id without ensuring the conversation belongs to the caller's tenant.
  - _Fix:_ Constrain the SELECT to the caller's tenant and/or verify ownership before using the result. Example: include AND tenant_id = ${tenantId} (or use Drizzle .where(eq(conversations.tenantId, tenantId))). Alternatively, fetch the conversation row earlier from a trusted storage layer that already enforces tenant scoping and validate ownership before proceeding.
- **server/routes.ts:4331** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts a project note (project_id, note, author) without supplying tenant_id, and the project_id was derived from a prior SELECT that lacked tenant scoping/ownership verification.
  - _Fix:_ Always include tenant_id explicitly in INSERTs for tenant-scoped tables and validate that the referenced project (project_id) belongs to the caller's tenant before inserting. E.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}). Also ensure the earlier lookup of conversation/project enforces tenant ownership (see previous finding) to avoid cross-tenant writes.
- **server/routes/projects.ts:184** — INSERT into project_notes does not set tenant_id. Tenant-scoped tables require tenant_id to be explicitly stored on INSERT to enforce isolation (FKs / app checks alone are insufficient).
  - _Fix:_ Include tenant_id in the INSERT (and bind it as a parameter). Example: INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ${req.body.note}, ${author}) and ensure tenantId is validated from the session before use.
- **server/routes/projects.ts:224** — INSERT into project_files (base64 upload path) does not set tenant_id. Tenant-scoped files must record tenant_id on creation.
  - _Fix:_ Add tenant_id to the INSERT columns and pass the authenticated tenantId. Example: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ...). Validate project ownership before inserting.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at ... WHERE id = ${projectId} lacks a tenant_id predicate. Updating a tenant-scoped row without constraining by tenant_id can allow cross-tenant updates if an ID is incorrect or attacker-controlled.
  - _Fix:_ Constrain the UPDATE by tenant_id as well: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Use the authenticated tenantId parameter.
- **server/routes/projects.ts:292** — INSERT into project_files (multipart upload path) does not set tenant_id. New project_files rows are created without explicit tenant ownership recorded.
  - _Fix:_ Include tenant_id in the INSERT (project_files should store tenant_id) and bind the tenantId derived from the authenticated session: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (..., ${tenantId}, ...).
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at ... WHERE id = ${projectId} (after multipart uploads) lacks tenant_id in WHERE clause, allowing updates without explicit tenant scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so only the caller's tenant row can be updated.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks tenant_id in the WHERE clause. Deletions on tenant-scoped tables must include tenant_id to prevent cross-tenant deletion if an ID is tampered with.
  - _Fix:_ Include tenant_id in the DELETE predicate: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}, after verifying tenant ownership of the project.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions executed without restricting by tenant_id. This reads across all tenants for a tenant-scoped table.
  - _Fix:_ Constrain the query to the intended tenant (e.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}) or otherwise pass the tenantId parameter to the query to enforce tenant isolation.
- **server/seed.ts:2855** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter — reads tenant-scoped rows for all tenants.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise scope the select to the intended tenant before iterating and modifying rows.
- **server/seed.ts:2860** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without constraining by tenant_id. Relying only on id is insufficient for tenant isolation.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) to ensure only the caller's tenant row is modified. If id comes from an untrusted source, validate ownership first.
- **server/seed.ts:3137** — UPDATE projects SET primary_conversation_id = ... WHERE id = 15 ... is executed without tenant_id constraint. This can modify project rows across tenants.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise verify the project belongs to the target tenant before updating.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations ... checks for missing project_conversations links but does not restrict by tenant_id — reads conversations across all tenants.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL AND NOT EXISTS (... AND pc.tenant_id = ${tenantId} ...)) to ensure the operation only inspects rows for the intended tenant.
- **server/seed.ts:3137** — UPDATE on tenant-scoped table 'projects' lacks a tenant_id constraint (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL). This will update the row regardless of tenant, violating tenant isolation.
  - _Fix:_ Include a tenant filter in the WHERE clause (e.g. AND tenant_id = <tenantId>) or otherwise ensure the row belongs to the intended tenant before updating. Prefer parameterized tenantId rather than hardcoding.
- **server/seed.ts:3205** — SELECT from tenant-scoped table 'conversations' has no tenant_id filter (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)). This reads rows across all tenants.
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. add WHERE c.tenant_id = <tenantId>) or join on project/tenant and enforce tenant ownership before processing. Avoid scanning cross-tenant data.
- **server/seed.ts:3213** — INSERT into project_conversations uses project_id and conversation_id derived from an earlier unfiltered query; the code does not verify tenant ownership of those foreign ids. This can create cross-tenant links.
  - _Fix:_ Only insert links for conversations/projects that belong to the target tenant. Either (a) ensure the source SELECT is tenant-scoped, or (b) validate each project_id/conversation_id pair by querying their tenant_id and confirming ownership before inserting. Consider adding tenant_id to project_conversations and populate it atomically.
- **server/seed.ts:4431** — DELETE from tenant-scoped table 'heartbeat_tasks' has no tenant_id constraint (DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE ... AND type != 'maintenance_script'). This will delete rows across all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = <tenantId>) or otherwise restrict the delete to rows owned by the intended tenant. Always include tenant filters for destructive operations.
- **server/self-improvement.ts:170** — detectStagnation builds a WHERE clause that can omit tenant filtering. If tenantId is not passed, the query (db.select().from(experiments).where(...)) may execute with only category constraint and return experiments across all tenants.
  - _Fix:_ Require a tenantId or fail-closed when none is supplied, or always add eq(experiments.tenantId, tenantId) to the conditions before executing the query. Avoid executing a cross-tenant SELECT when tenantId is missing.
- **server/self-improvement.ts:521** — getExperimentHistory returns db.select().from(experiments) unscoped when no tenantId is provided, allowing cross-tenant reads of the experiments table.
  - _Fix:_ Make tenantId required or fail-closed when absent. If tenantId is optional, explicitly deny/empty-result when not provided, or always include eq(experiments.tenantId, tenantId) in the WHERE clause.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` without constraining by tenant_id — query uses only conversationId. Per invariant, reads against tenant-scoped tables must include tenant filter in SQL.
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)) or use the tenantScope/assertRequire helper) so the DB only returns rows for the caller's tenant. Alternatively, join to conversations and filter by conversations.tenant_id = tenantId in the same SQL.
- **server/skillify.ts:196** — SELECT from tenant-scoped table `conversations` does not include tenant_id in the WHERE clause. The code fetches the row by id and then checks conv[0].tenantId !== tenantId in application logic — tenant isolation must be enforced by SQL filters only.
  - _Fix:_ Scope the SELECT to the caller's tenant in SQL (e.g. .where(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId)) or use requireTenantScope(conversations.tenantId, tenantId)) instead of relying on a post-fetch ownership check.
- **server/skillify.ts:203** — SELECT from tenant-scoped table `messages` (by conversationId) lacks a tenant_id constraint. The messages query relies on a prior in-app check rather than enforcing tenant scoping in SQL.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)) or join to conversations and filter by conversations.tenant_id = tenantId) to ensure the DB only returns rows belonging to the caller's tenant.
- **server/skillify.ts:223** — SELECT from `personas` uses persona id only (eq(personas.id, conv[0].personaId)) with no tenant filter. If `personas` is tenant-scoped, this allows cross-tenant reads.
  - _Fix:_ Constrain the query to the tenant (e.g. .where(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)) or verify ownership via an assert helper before selecting) so persona rows are only read for the caller's tenant.
- **server/skillify.ts:331** — Another SELECT from tenant-scoped table `messages` (recentMsgs in skillifyConversation) omits tenant_id in the WHERE clause — it only filters by conversationId.
  - _Fix:_ Include tenant scoping in the messages query (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)) or use tenantScope/assertConversationInTenant before querying) to enforce DB-level isolation.
- **server/storage.ts:276** — attachCitationMetadata performs a raw SELECT from messages by id without constraining tenant_id. messages is tenant-scoped; this can read citations belonging to other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) and/or accept a tenantId parameter from the caller. Prefer using Drizzle predicate helpers or tenantScope(messages.tenantId, tenantId) to fail-closed if tenantId is missing.
- **server/storage.ts:386** — attachCitations issues a raw UPDATE to messages (UPDATE messages SET citations = ...) filtering only on id and not tenant_id. messages is tenant-scoped so this can modify another tenant's row if ids collide.
  - _Fix:_ Require and validate tenantId in this function and add AND tenant_id = ${tenantId} to the WHERE clause (or use .where(and(eq(messages.id, id), eq(messages.tenantId, tenantId)))). Avoid blind raw SQL without tenant constraint.
- **server/storage.ts:796** — updateMemoryEmbedding updates memory_entries by id only (WHERE id = ${id}) and does not constrain by tenant_id. memory_entries is tenant-scoped; this can update another tenant's embedding row.
  - _Fix:_ Require and pass tenantId (or otherwise validate ownership) and add tenant filter to the WHERE clause (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))).
- **server/storage.ts:808** — updateKnowledgeEmbedding updates agent_knowledge by id only (WHERE id = ${id}) without tenant scoping. agent_knowledge is tenant-scoped and must be constrained to the caller's tenant.
  - _Fix:_ Require tenantId or otherwise verify ownership and include eq(agentKnowledge.tenantId, tenantId) in the WHERE clause (or use tenantScope(agentKnowledge.tenantId, tenantId)).
- **server/storage.ts:832** — archiveExpiredMemories executes a raw UPDATE on memory_entries without constraining tenant_id, potentially archiving rows across all tenants.
  - _Fix:_ If this is intended to be tenant-scoped, add tenant_id filtering (or accept tenantId). If it's an admin/global maintenance operation, require explicit admin authorization and document that it's multi-tenant. Prefer per-tenant invocations rather than unscoped mass updates.
- **server/storage.ts:849** — archiveStaleMemories executes a raw UPDATE on memory_entries without a tenant_id constraint, modifying tenant-scoped rows across all tenants.
  - _Fix:_ Same as archiveExpiredMemories: require tenant scoping or explicit admin authorization, or run per-tenant with tenant_id in the WHERE clause to avoid cross-tenant mutation.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding issues an UPDATE on felix_proposals filtering only by id (WHERE id = ${proposalId}) and does not include tenant_id. felix_proposals is tenant-scoped; this can modify another tenant's row.
  - _Fix:_ Require tenantId and include eq(felix_proposals.tenant_id, tenantId) (or use tenantScope) in the WHERE clause. Pass tenantId through callers or validate ownership before updating.
- **server/surprise-scorer.ts:139** — scoreProposalSurprise updates actual_outcome_embedding on felix_proposals with WHERE id = ${proposalId} but no tenant_id constraint. This writes tenant-scoped data without tenant scoping.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE and validate tenantId is provided/valid before making the change.
- **server/surprise-scorer.ts:181** — When not enough neighbors are found, the code sets surprise_band = 'no_history' with UPDATE felix_proposals WHERE id = ${proposalId} — lacking tenant scoping and thus touching tenant-scoped rows across tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${tenantId} and ensure tenantId is passed to the function and validated; avoid unscoped writes to tenant-scoped tables.
- **server/surprise-scorer.ts:212** — The final write that sets surprise_score and surprise_band performs UPDATE felix_proposals WHERE id = ${proposalId} without tenant_id constraint, writing tenant-scoped state across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), and require/validate tenantId for this operation to enforce isolation.
- **server/tool-learning.ts:129** — SELECT against tenant-scoped table `custom_tools` without constraining by tenant_id. The query `db.select().from(customTools).where(eq(customTools.name, parsed.name))` can read rows across tenants.
  - _Fix:_ Constrain reads to the caller's tenant. Example: add `and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))` or use the tenant-aware lookup already used elsewhere so queries only return rows for the supplied tenantId.
- **server/tool-learning.ts:155** — SELECT by id against tenant-scoped table `custom_tools` without tenant_id constraint. `db.select().from(customTools).where(eq(customTools.id, inserted.id))` may return a row belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))`) or re-query by (id, tenant_id) to ensure tenant isolation.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` without tenant_id constraint. `db.update(customTools).set(...).where(eq(customTools.id, tool.id))` can update another tenant's row if id collides or is misused.
  - _Fix:_ Add tenant guard to the UPDATE: `where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId || tool.tenantId)))`. Ensure the function has/receives a verified tenantId and use it for all UPDATEs.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table `custom_tools` without tenant_id constraint. `db.delete(customTools).where(eq(customTools.id, tool.id))` will delete across tenants if not scoped.
  - _Fix:_ Scope DELETEs by tenant: `where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId || tool.tenantId)))`. Pass an authoritative tenantId into revertToolRow and enforce it in the WHERE clause.
- **server/tool-learning.ts:365** — UPDATE (usageCount increment) against tenant-scoped table `custom_tools` without tenant_id constraint. `db.update(customTools).set({ usageCount: ... }).where(eq(customTools.id, tool.id))` lacks tenant scoping.
  - _Fix:_ Include tenant constraint in the WHERE clause (e.g. `where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId || tool.tenantId)))`). Always pair id-based updates with tenant_id to enforce isolation.
- **server/tools.ts:2874** — INSERT into project_conversations does not include tenant_id. This inserts a tenant-scoped association row without explicitly stamping the caller's tenant, relying on prior updates/ownership checks instead of writing tenant_id into the row.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) or add tenant_id = ${tenantId}) so every persisted row is explicitly tenant-scoped. Alternatively, derive and bind the tenantId from the already-validated project/conversation ownership and ensure the schema requires tenant_id NOT NULL.
- **server/tools.ts:2978** — INSERT into project_files does not set tenant_id. The code inserts a project_files row with project_id but does not explicitly write tenant_id into the new row.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) and bind the caller's tenantId. Ensure the tenantId is derived from a verified ownerCheck (or the project row) and stored with the file record so reads/writes can be constrained reliably.
- **server/tools.ts:2992** — INSERT into project_notes does not include tenant_id. A project-scoped note row is inserted without explicitly stamping the tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author||'agent'}, ${tenantId})) using the validated tenantId. Enforce tenant_id NOT NULL at schema level if intended.
- **server/tools.ts:3016** — INSERT into project_conversations (link_conversation path) omits tenant_id when creating the association row.
  - _Fix:_ Write tenant_id explicitly in this INSERT as well (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Ensure tenantId is validated from ownership checks prior to insert.
- **server/tools.ts:4184** — SELECT from tenant-scoped table 'memory_entries' is executed without constraining to the caller's tenant. The query (SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ...) lacks any tenant_id filter and will return rows from other tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE tenant_id = ${params._tenantId} AND ...), or verify ownership before the read. Prefer using Drizzle eq(table.tenantId, tenantId) in the .where() or include ${params._tenantId} in the template so only rows for the current tenant are returned. Failing closed on missing params._tenantId is recommended.
- **server/tools.ts:6893** — INSERT into tenant-scoped table `project_files` omits tenant_id. The raw SQL INSERT at line 6893 inserts project file metadata but does not set the tenant_id column (even though tenant ownership was checked earlier). Because tenant_id columns are NOT defaulted/enforced at the DB level, this will create a row with no tenant association and can break isolation/auditing.
  - _Fix:_ Include the tenant id explicitly in the INSERT. Prefer using the Drizzle insert API or add tenant_id to the column list and pass the verified tenant value (e.g. ${params._tenantId} or tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Alternatively, use db.insert(projectFiles).values({ tenantId: tenantId, projectId: ..., fileName: ..., ... }) so every row is explicitly tenant-scoped.
- **server/tools.ts:8853** — UPDATEs against tenant-scoped table `proposed_skills` lack tenant constraint in WHERE clause. Multiple db.update(...) calls update proposed_skills using only eq(proposedSkills.id, row.id) (lines ~8853, 8878, 8895) without restricting by tenant_id. This allows one tenant to update another tenant's proposed_skills row.
  - _Fix:_ Restrict UPDATEs to the caller's tenant by adding a tenant_id condition to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Or, first verify ownership (e.g. SELECT tenant_id FROM proposed_skills WHERE id = ... and compare to tid) before performing the update. Prefer the Drizzle fluent API with an explicit tenant filter to avoid accidental cross-tenant writes.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE on tenant-scoped table 'browser_workflows' omits tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId} (use the trusted ctx.tenantId). Alternatively, perform the UPDATE via a parameterized API that verifies the workflow belongs to the tenant before modifying it.
- **server/tools/domains/files/handlers.ts:838** — INSERT into project_files does not include tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)). This writes a tenant-scoped artifact without explicitly setting tenant ownership.
  - _Fix:_ Include the tenant_id column in the INSERT and set it from the trusted context (ctx.tenantId). e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ...). Validate ctx.tenantId is present before inserting.
- **server/tools/domains/files/handlers.ts:841** — UPDATE on projects table omits tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Even though projId was discovered via a tenant-scoped join earlier, the UPDATE is not constrained and could modify a project id belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}, using the trusted ctx.tenantId. Always include tenant_id on UPDATEs of tenant-scoped tables to enforce isolation.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table 'knowledge_triples' lacks a tenant_id constraint in the WHERE clause: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}`. This could allow cross-tenant row modification if ids are misused or in future code paths.
  - _Fix:_ Include the tenant filter in the UPDATE to enforce tenant scoping, e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}` (and pass tenantId as a parameter). Additionally, check the number of affected rows (RETURNING or rowCount) and error if zero to detect and fail when the target row doesn't belong to the caller's tenant.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table 'provider_keys' without constraining tenant_id. The query: SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' — no WHERE tenant_id = ${tenantId}.
  - _Fix:_ Restrict this query to the caller's tenant, e.g. add AND tenant_id = ${tenantId} (use the parameterized scanSql interpolation), or explicitly document and enforce that this code must only run in a controlled/admin context and cannot leak cross-tenant data. Prefer passing ctx.tenantId into the query parameter rather than doing a non-filtered, cross-tenant read.
- **server/tools/domains/security/handlers.ts:260** — SELECT from tenant-scoped table 'provider_keys' without constraining tenant_id. The query: SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' — no WHERE tenant_id = ${tenantId}.
  - _Fix:_ Restrict this query to the caller's tenant by adding WHERE tenant_id = ${tenantId} (parameterized). If the intent is an admin-global audit, make that explicit and ensure only an authorized admin path can execute this cross-tenant query; otherwise, filter by tenant_id to enforce isolation.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint. This violates the invariant that every write to tenant-scoped tables must be constrained to the caller's tenant and could allow cross-tenant modifications if the id is ever derived from an untrusted source.
  - _Fix:_ Include the tenant filter on the UPDATE. e.g. add `AND tenant_id = ${tid}` (or use Drizzle .where(eq(table.tenantId, tid))) so the statement becomes: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} — ensure tenantId (tid) is the caller's trusted ctx.tenantId.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with no tenant_id constraint. Even though ids were derived from a prior tenant-scoped SELECT in this function, the invariant requires every write to include an explicit tenant_id constraint to prevent accidental cross-tenant updates in future refactors or if ids are influenced externally.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE. For example: UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Alternatively, restrict the ids to tenant-scoped rows in a single UPDATE: UPDATE memory_entries SET ... WHERE tenant_id = ${tid} AND id = ANY(${ids}). Use parameterized SQL/Drizzle helpers (eq(table.tenantId, tid)) rather than string interpolation.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint (in recordStrategicWinHandler). This lacks the required tenant scoping on writes to tenant-scoped tables.
  - _Fix:_ Constrain the UPDATE by tenant. e.g. UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id} AND tenant_id = ${tid} (or use Drizzle .where(eq(table.tenantId, tid))). Ensure tid is ctx.tenantId (trusted) and always include tenant_id in WHERE for tenant-scoped tables.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} executed without a tenant_id constraint. Reading a tenant-scoped `conversations` row by id alone can be used to infer/validate or be abused across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId}) or otherwise fetch the conversation via a tenant-scoped accessor that enforces ownership before trusting/using the returned value.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} performs an update on a tenant-scoped table using only the reservation id, with no tenant_id constraint. This allows a caller who can influence reservationId to modify another tenant's ledger row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or first SELECT/VERIFY that the reservation row belongs to the expected tenant before performing the UPDATE. Ensure settlement paths accept tenantId and enforce it in the query.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table by id without verifying tenant ownership.
  - _Fix:_ Restrict the UPDATE by tenant (WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or verify ownership (SELECT tenant_id FROM agent_cost_ledger WHERE id = ${reservationId}) before performing the release. Accept tenantId as a parameter to this function and use it in the WHERE clause.
- **server/webhook-triggers.ts:60** — listTriggers SELECT LEFT JOINs into the personas table without any tenant constraint. This reads tenant-scoped personas (p.name) with no WHERE p.tenant_id = ... filter and can leak persona data across tenants.
  - _Fix:_ Avoid joining directly into a tenant-scoped table without scoping. Either: (a) join with an explicit tenant filter (e.g. LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = <expectedTenantId>), or (b) resolve persona names via a server-side API that verifies ownership (e.g. storage.getPersona) before exposing them, or (c) store persona tenant context alongside webhook_triggers and enforce it at query time.
- **server/webhook-triggers.ts:89** — createTrigger INSERT writes persona_id from caller-supplied config.personaId directly into webhook_triggers without validating that the referenced persona belongs to an allowed tenant. This allows creating triggers that reference personas from other tenants (ownership/spoofing risk).
  - _Fix:_ Validate personaId before inserting. Look up the persona and ensure its tenant_id matches the expected tenant (e.g. admin tenant for global triggers) or reject/normalize personaId. Alternatively restrict this API to admin-only callers and perform an ownership check (storage.getPersona and compare persona.tenantId) prior to INSERT.
- **server/webhook-triggers.ts:122** — processTriggerEvent SELECT queries webhook_triggers and LEFT JOINs personas (p.name) without scoping the joined personas to a tenant. The code then uses trigger.persona_id for downstream processing, potentially using a persona from another tenant without ownership verification.
  - _Fix:_ When resolving persona data for a webhook trigger, verify the persona's tenant before using it. Either add an explicit tenant predicate to the join (e.g. AND p.tenant_id = <expectedTenantId>), or fetch the persona via a storage helper that enforces tenant ownership (storage.getPersona) and only proceed if the persona belongs to the permitted tenant. Also consider persisting persona tenant context when creating triggers to make checks simpler.
- **server/wiring-invariants.ts:57** — SELECT from research_schedules (joined to research_programs) is executed without constraining to a tenant. If research_schedules is tenant-scoped, this reads across tenants and can break the invariant that all tenant-scoped reads must include tenant_id.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE s.tenant_id = ${tenantId}) or run the check per-tenant using a provided tenantId/Admin tenant constant. In Drizzle use .where(eq(research_schedules.tenantId, tenantId)) or parameterize the SQL with the intended tenant (or explicitly document that the table is global).
- **server/wiring-invariants.ts:180** — Outcome canary query aggregates recent experiments and proposals (research_experiments, code_proposals, research_sessions) without any tenant_id constraint. These tables are typically tenant-scoped — querying them across tenants violates tenant isolation assumptions.
  - _Fix:_ Restrict the subqueries and main query to a specific tenant (e.g. add AND e.tenant_id = ${tenantId}, AND cp.tenant_id = ${tenantId}, AND rs.tenant_id = ${tenantId}, and p.tenant_id = ${tenantId}), or execute the canary per-tenant. Use Drizzle-style .where(eq(table.tenantId, tenantId)) rather than unparameterized SQL.
- **server/wiring-invariants.ts:244** — Query against tool_performance (SELECT count(*) WHERE last_success_at > ...) is executed with no tenant_id filter. tool_performance is likely tenant-scoped, so this reads across tenants.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND last_success_at > ...) or run the check per tenant. Use parameterized Drizzle .where(eq(tool_performance.tenantId, tenantId)).
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tool_performance (everResult) is executed without tenant scoping — it returns a global count across tenants. For tenant-scoped tables every read must include tenant_id.
  - _Fix:_ Limit to a tenant (e.g. SELECT count(*) FROM tool_performance WHERE tenant_id = ${tenantId}) or perform the probe per-tenant. If the intent is a global health-metric, make that explicit and restrict access to admin-only code paths with documented rationale.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages WHERE created_at > ... has no tenant_id constraint. messages are almost always tenant-scoped, so this aggregates across tenants incorrectly.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or run this probe per-tenant. Use Drizzle .where(eq(messages.tenantId, tenantId)).
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... lacks tenant scoping. This will report tools used across all tenants, violating the requirement that reads against tenant-scoped tables include tenant_id.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId} AND last_success_at > ...) or run the detector per-tenant. Prefer Drizzle's .where(eq(tool_performance.tenantId, tenantId)).
- **server/wiring-invariants.ts:417** — Tool-drift query selects from tool_performance (aggregations, HAVING) without any tenant_id constraint. This inspects invocation counts/failures across all tenants, violating tenant isolation rules for tenant-scoped tables.
  - _Fix:_ Add tenant restriction to the query (e.g. WHERE tenant_id = ${tenantId} AND last_failure_at > ...) or run the drift detector per-tenant. Use Drizzle .where(eq(tool_performance.tenantId, tenantId)) and include tenant_id in GROUP BY/HAVING predicates as needed.
- **server/wiring-invariants.ts:535** — Memory-growth probes use sql.raw and query COUNT/pg_total_relation_size on tables such as agent_knowledge, memory_entries, knowledge_triples, messages without tenant scoping. These tables are typically tenant-scoped and must be queried per-tenant or filtered by tenant_id.
  - _Fix:_ Perform counts per-tenant (SELECT COUNT(*) FROM <table> WHERE tenant_id = ${tenantId}) or otherwise restrict these probes to a specific tenant. If the intent is a global metric, document/explicitly justify this and ensure only admin contexts run it.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory is executed without a tenant_id WHERE clause while the code treats the result as 'empty across all tenants'. Querying a tenant-scoped table without filtering violates the requirement that all reads include tenant_id (or be explicitly global).
  - _Fix:_ If the check is intended to be a per-tenant seed check, add WHERE tenant_id = ${tenantId} and run it per tenant. If truly global, document that graph_memory is a global/shared table; otherwise restrict access and add explicit tenant filtering.

### MEDIUM (36)

- **server/agentic-engines.ts:450** — recentLogs query in runPredictiveEngine selects from `heartbeat_logs` WHERE status='success' without restricting by tenant_id (returns global rows).
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or otherwise scope the query) so returned logs are tenant-local.
- **server/agentic-engines.ts:532** — taskConfig SELECT from `heartbeat_tasks` WHERE enabled = true does not include tenant scoping and may return tasks from other tenants.
  - _Fix:_ Restrict the query to the tenant (WHERE tenant_id = ${tenantId} AND enabled = true) or iterate tenants explicitly when a cross-tenant sweep is intended (and do so intentionally and carefully).
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes a query over a tenant-scoped table without constraining to a specific tenant_id.
  - _Fix:_ If this is intended as an admin-wide sweep, require an explicit admin-only code path (or a clear comment/authorization guard) and document it. Otherwise, add a WHERE tenant_id = ${tenantId} filter (or accept a tenantId parameter) so ordinary callers only read their own tenant's rows. Prefer explicit scoping over implicit cross-tenant reads.
- **server/agentic/self-initiative.ts:100** — Telemetry query for heartbeat_logs uses a condition on task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}) but does not include an explicit WHERE tenant_id = ${tenantId} on heartbeat_logs itself — no direct tenant_id equality on the tenant-scoped table.
  - _Fix:_ Add an explicit tenant_id = ${tenantId} predicate on heartbeat_logs (or otherwise ensure the SELECT has an explicit tenant equality check). Prefer the clear pattern WHERE tenant_id = ${tenantId} (or eq(..)) so tenant-scoping is obvious and enforced.
- **server/auto-memorize.ts:47** — getWatermark() selects from agent_knowledge without a tenant_id constraint. agent_knowledge is typically tenant-scoped, so this may return another tenant's watermark rather than the intended admin/global watermark.
  - _Fix:_ If the watermark is intended as a global/admin artifact, add an explicit tenant filter (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID}) when reading it. If it must remain tenant-agnostic, document the reason and ensure writes/reads consistently target the same tenant id (and that this behavior is auditable).
- **server/auto-memorize.ts:62** — setWatermark() looks up an existing agent_knowledge row via SELECT id FROM agent_knowledge WHERE category = ... AND title = ... LIMIT 1 without filtering by tenant_id, then updates/inserts rows with tenant_id = 1. The lack of a tenant filter can cause mismatches (reading another tenant's row) before the write.
  - _Fix:_ Include an explicit tenant filter when selecting the existing watermark row (e.g. WHERE category = ... AND title = ... AND tenant_id = ${ADMIN_TENANT_ID}) so the lookup/update is unambiguous and cannot accidentally touch or observe other tenants' rows.
- **server/business-tools.ts:320** — createContract writes contracts.customer_id from caller input without first verifying that the referenced customer belongs to the caller's tenant. Writing a foreign id without ownership validation can create cross-tenant links or stale references.
  - _Fix:_ Validate foreign IDs supplied by callers before use. For example SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} and reject if not found, or otherwise refuse to accept customer_id values that are not owned by the tenant. Apply the same ownership checks for all foreign IDs (project_id, assigned_to, etc.).
- **server/chat-engine.ts:4015** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is invoked without tenant context. If this function persists detection/telemetry into tenant-scoped tables, it may write/read without tenant constraints or assume ownership of conversationId.
  - _Fix:_ Add explicit tenantId parameter when calling recordToolCallForStuckDetection (or have the function resolve and validate tenant ownership of conversationId). Ensure any DB writes include tenant_id and any reads filter by tenant_id to prevent cross-tenant access.
- **server/chat-engine.ts:3589** — logDecision(conversationId, ...) is called without passing tenantId. If logDecision writes a tenant-scoped record (decisions/annotations) tied to conversationId, the implementation could write without tenant_id or fail to verify conversation ownership.
  - _Fix:_ Include tenantId (tenantId or tenantIdForScope) when calling logDecision, or perform an explicit ownership check (select conversation where id=:conversationId AND tenant_id=:tenantId) before writing. Update logDecision to require tenant context and ensure any INSERT/UPDATE sets tenant_id or filters by it.
- **server/chat-engine.ts:3810** — recordErrorForRetryStormDetection(conversationId, String(result.error).slice(0,200)) is called without tenant context. If this routine persists error telemetry to tenant-scoped tables, it may write/aggregate data without tenant_id constraints.
  - _Fix:_ Pass tenantId into recordErrorForRetryStormDetection, or make the function lookup and validate the conversation's tenant before persisting. Ensure all telemetry/error INSERTs/updates include tenant_id and any SELECTs filter by tenant_id.
- **server/doc-collections.ts:136** — listCollections SQL includes subqueries that count doc_chunks using WHERE collection_id = c.id but omit tenant_id in those subqueries. Those counts can leak or mix other-tenant chunks if collection ids are not sufficiently constrained.
  - _Fix:_ Add tenant scoping to the subqueries (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) or compute counts via joined/filtered queries that include dc.tenant_id = ${tenantId}.
- **server/doc-collections.ts:458** — getCollectionStatus SQL uses subqueries that count doc_chunks with WHERE collection_id = c.id (and other aggregates) but omit tenant_id in those subqueries, risking cross-tenant mixing of counts.
  - _Fix:_ Add tenant filters to each subquery (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) so aggregate counts are scoped to the caller's tenant.
- **server/dormant-deprecation.ts:203** — getEverInvokedTools selects DISTINCT tool_name FROM tool_performance with no tenant_id filter — may return tools invoked by other tenants.
  - _Fix:_ If intended to be tenant-specific, add WHERE tenant_id = ${tenantId}. Otherwise ensure the caller is authorized to view global tool usage before running this query.
- **server/dormant-deprecation.ts:214** — getActiveTools14d selects DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant_id. This may surface other tenants' tool activity when the table is tenant-scoped.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) if the computation should be per-tenant, or restrict the function to platform-level operations with proper authorization.
- **server/health-audit.ts:314** — SELECT from code_proposals (stale scan) is executed without any tenant_id constraint — this reads tenant-scoped rows across all tenants.
  - _Fix:_ If this scanner is intended to operate per-tenant, restrict queries with WHERE tenant_id = ${tenantId} (and accept a tenantId param). If this is an admin/system-wide operation, ensure it runs under a trusted admin context and explicitly documents that it is allowed to read across tenants. Prefer explicit admin gating and logging instead of silent global reads.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks (stuck check) is run without tenant_id scoping and returns enabled tasks across all tenants.
  - _Fix:_ If scanning per-tenant, add WHERE tenant_id = ${tenantId}. If this is an intentional cross-tenant admin audit, require an explicit admin context and document the privileged nature of the operation. Prefer explicit tenant scoping where possible to avoid accidental exposure of tenant data.
- **server/heartbeat.ts:1743** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 — query has no tenant_id filter. If heartbeat_logs are tenant-scoped, this reads logs across tenants.
  - _Fix:_ If heartbeat_logs are per-tenant, add a tenant_id filter (e.g. AND tenant_id = ${tenantId}) or otherwise scope the query to the admin tenant explicitly. If logs are intentionally global, document that and ensure only non-sensitive data is read cross-tenant.
- **server/job-queue.ts:385** — getJobStats runs a global aggregation over agent_jobs (7-day window) with no tenant_id filter — exposes cross-tenant counts if used in tenant-facing UI.
  - _Fix:_ If stats are intended to be tenant-scoped, add tenant_id = ${tenantId}. If a global admin view is intended, gate the call behind an explicit admin permission and make that explicit in the code / function name (e.g. getGlobalJobStats).
- **server/lib/agent-trace.ts:129** — UPDATE agent_trace_spans uses only span_id in the WHERE clause (WHERE span_id = ${spanId}) and does not constrain by tenant_id. An attacker who knows a span_id could cause cross-tenant updates to trace rows.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Capture the tenant id at span-open time (or call currentTenantId() in the calling context) and pass it into persistSpanClose so the closure can include the tenant predicate when performing the UPDATE.
- **server/lib/restraint-trust.ts:88** — recordReputationOutcome uses sql.raw(${column}) to build a dynamic UPDATE column name derived from the caller-provided outcome. Although the column name is checked against a whitelist, using sql.raw with runtime-derived identifiers is error-prone and can lead to injection/logic mistakes if the allowlist or mapping changes.
  - _Fix:_ Avoid injecting raw SQL identifiers. Implement the column-specific UPDATE with an explicit branch per outcome (e.g. switch on outcome and run a parameterized UPDATE that references the column literally), or use a safe mapping to pre-built parameterized queries. Keep any dynamic identifier handling strictly server-controlled and unit-tested.
- **server/lib/system-probes.ts:163** — db.select(count(*)) FROM messagesTable is executed with no tenant_id constraint — the probe counts messages across all tenants and can leak multi-tenant aggregates.
  - _Fix:_ Scope the count by tenant (e.g. .from(messagesTable).where(eq(messagesTable.tenant_id, ${tenantId})) or call a storage helper that enforces tenant scoping). If an aggregate across tenants is intended, ensure the probe runs only in an authorized admin context and that the behavior is explicitly documented and audited.
- **server/recurring-messages.ts:182** — SELECT from tenant-scoped table `agent_knowledge` performed without a tenant_id constraint when _allowGlobalSystemUseOnly is not set — the query may return rows across tenants.
  - _Fix:_ Avoid unscoped reads on tenant-scoped tables. Require the caller's tenant and add an explicit WHERE tenant_id = ${tenantId}. If a global scan is required for internal system use, isolate that functionality behind an internal-only path with strict audit/authorization and ensure the code that acts on rows enforces per-row tenant checks before any write.
- **server/routes.ts:2312** — SELECT from tenant-scoped table `delivery_logs` by order/session id without constraining tenant_id. The code later uses the returned row's tenantId when signing a play link — selecting without tenant filtering can leak or be abused if session identifiers are discoverable.
  - _Fix:_ Constrain the query when possible (e.g. require additional proof or limit to the owning tenant). If the sessionId is intended as an unguessable capability, ensure that capability handling is well-documented and avoid returning or using tenant_id in ways that could be abused; otherwise add WHERE tenant_id = <tenant> or otherwise validate ownership before returning rows.
- **server/routes.ts:764** — SELECT over `projects` JOIN `tenants` reads all projects where drive_folder_id is NULL without tenant filtering. This reads tenant-scoped rows across all tenants.
  - _Fix:_ If this backfill is intended to run as a privileged admin job, make the admin context explicit and restrict invocation to admin-only. Otherwise, process projects per-tenant or add a WHERE clause (eq(projects.tenantId, <tenant>)) to avoid unscoped cross-tenant reads.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} does not include tenant_id in the WHERE clause. Even though the handler verifies the project first, the query itself has no tenant predicate as required by the invariant.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join to projects and filter by projects.tenant_id = ${tenantId} for defense-in-depth.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not restrict by tenant_id. The SELECT against a tenant-scoped table must include tenant_id.
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId} AND file_path = ${...} LIMIT 1), or join to projects and verify projects.tenant_id = ${tenantId}.
- **server/seed.ts:4655** — UPDATE on tenant-scoped table 'agent_knowledge' uses WHERE id = ${rows[0].id} but omits tenant_id. Relying on id alone does not satisfy the codebase's tenant-filtering invariant.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = <tenantId>) or re-query using tenant scoping before updating to ensure the row belongs to the intended tenant.
- **server/storage.ts:817** — getMemoriesWithoutEmbeddings selects from memory_entries without any tenant filter. This reads tenant-scoped data across tenants when tenantId is not constrained.
  - _Fix:_ Accept/require a tenantId parameter or make this an explicit admin-only function. If it must run multi-tenant (background worker), ensure it enforces tenant context per-row or runs with explicit admin authorization and logging.
- **server/storage.ts:868** — getMemoryStats may select memory_entries and agent_knowledge without tenant constraints (it falls back to a full-table select when tenantId is undefined). These tables are tenant-scoped; calling this without tenantId exposes or aggregates data across tenants.
  - _Fix:_ Require tenantId or make it explicit that callers must pass tenantId. If admin aggregation is needed, create a clearly documented admin-only path and gate it behind proper authorization.
- **server/storage.ts:928** — getHeartbeatTask fetches heartbeat_tasks by id without scoping by tenant_id. heartbeat_tasks is tenant-scoped elsewhere; this read can return another tenant's task.
  - _Fix:_ Add tenant scoping (accept tenantId param and include eq(heartbeatTasks.tenantId, tenantId) or use tenantScope) or require callers to use a tenant-aware API that validates ownership before calling this helper.
- **server/tools.ts:2971** — SELECT id FROM project_files used for duplicate-check lacks an explicit tenant filter (no tenant_id in WHERE). While the code does an earlier ownerCheck on the project, the SELECT itself reads a tenant-scoped table without a tenant constraint.
  - _Fix:_ Add an explicit tenant-scoped constraint to the duplicate check (e.g. WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND file_name = ${fname} ...), or join through projects and filter by p.tenant_id = ${tenantId}. Prefer binding tenant_id on every read against tenant-scoped tables to avoid any accidental cross-tenant lookups.
- **server/tools.ts:3012** — SELECT id FROM project_conversations (exists check) does not include tenant_id in the WHERE clause. The code relies on earlier ownership assertions, but the query itself reads a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Constrain the check to the caller's tenant (e.g. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) or join to projects/conversations and require p.tenant_id = ${tenantId} (or c.tenant_id = ${tenantId}). Ensure all existence checks on tenant-scoped tables include tenant scoping.
- **server/video-job-runner.ts:204** — SELECT cancelRequested FROM video_jobs WHERE job_id = ${jobId} is executed without restricting by tenant_id. Reading tenant-scoped video_jobs rows by job_id alone can leak or act on other tenants' job state.
  - _Fix:_ Scope the SELECT by tenant (WHERE job_id = ${jobId} AND tenant_id = ${tenantId}), or validate ownership of the jobId prior to reading the cancel flag. Pass tenantId into isCancelRequested or otherwise derive and enforce owner constraint before the read.
- **server/video-job-runner.ts:226** — UPDATE video_jobs ... WHERE status IN (...) AND updated_at < staleCutoff is run without any tenant_id constraint. This modifies rows across all tenants (marks stale jobs failed) without per-tenant filtering.
  - _Fix:_ If the intention is to perform a global cleanup, confine this operation to an admin-only maintenance path and document the behavior. Otherwise, add tenant scoping to the UPDATE or iterate per-tenant (SELECT tenants → per-tenant update) so tenant isolation guarantees are preserved. At minimum, ensure the caller is authorized to perform cross-tenant updates.
- **server/wiring-invariants.ts:361** — sql.raw is used to query information_schema and to select COUNT/MAX from subsystem tables using string-interpolated table names (e.g. SELECT COUNT(*)... FROM ${spec.table}) without tenant filters. While spec.table is a code constant, the COUNT/MAX queries against tables like knowledge_nudges and tool_performance do not filter by tenant_id and thus read tenant-scoped data across tenants.
  - _Fix:_ Avoid global string-interpolated queries for tenant-scoped tables. Execute per-tenant queries (SELECT COUNT(*) FROM <table> WHERE tenant_id = ${tenantId}) or add tenant predicates. If table existence checks are needed, keep table name interpolation limited to trusted constants but ensure row-level probes are tenant-scoped.
- **server/workflow-templates.ts:66** — Hardcoded project_id=14 is passed into produce_video without verifying that project 14 belongs to context.tenantId. If project records are tenant-scoped, using a foreign id without ownership verification can cause cross-tenant writes/associations.
  - _Fix:_ Do not pass raw foreign IDs without ownership checks. Either look up the project by a tenant-scoped identifier (e.g. SELECT id FROM projects WHERE id=${projectId} AND tenant_id=${context.tenantId}) and fail if not owned, or create a project scoped to context.tenantId. Ensure downstream tools validate tenant ownership before mutating tenant-scoped rows.

### LOW (2)

- **server/routes.ts:1529** — Public aggregated SELECTs over tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) are executed without tenant filtering. While aggregated, these queries still read tenant-scoped tables unscoped by tenant.
  - _Fix:_ If global aggregates are intentional, explicitly document and restrict these endpoints to admin roles, or mark the underlying tables as global. Otherwise, scope the queries to the caller's tenant (WHERE tenant_id = <tenant>) or provide safe, pre-aggregated counters stored in a global table.
- **server/routes.ts:1558** — Multiple SELECT count(*) queries over tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, etc.) are executed without tenant scoping in a public architecture endpoint.
  - _Fix:_ Same as above: either restrict this endpoint to admins, use dedicated global statistics tables, or add tenant scoping to queries so tenant data is only read in the appropriate tenant context.

### Suppressed — confirmed false positives (149)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id ... WHERE id = ${c.conversation_id} has no tenant_id restriction — writing tenant-scoped conversation rows without verifying tenant ownership in the UPDATE clause.
  - _Why false positive:_ The preceding SELECT joins conversations and projects on matching tenant ownership and returns c.tenant_id, so project_id being written was already verified for that conversation in the same call path.
- **server/auto-transcript.ts:179** _(fk-transitive)_ — INSERT INTO project_conversations (...) VALUES (...) is executed without any tenant scoping — can create cross-tenant links because the INSERT lacks tenant context and the table has no tenant_id column.
  - _Why false positive:_ project_conversations has no tenant_id and is a child/link table isolated transitively through tenant-scoped projects and conversations selected earlier from the same row.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into invoice_items omits tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this write will create tenant-ambiguous rows or fail — the code never sets tenant_id on child line items.
  - _Why false positive:_ invoice_items has no tenant_id and is a child table of tenant-scoped invoices via invoice_id, so omitting tenant_id on insert is by design.
- **server/ceo-orchestrator.ts:841** _(global-table)_ — Reading personas via db.select().from(personasTable) with no tenant filter. personas is a tenant-scoped resource; selecting all personas and later using those ids to create conversations (storage.createConversation with tenantId=plan.tenantId but personaId from the unfiltered list) can result in cross-tenant persona usage and unauthorized access.
  - _Why false positive:_ Referenced schema notes personas has no tenant_id column, so this is treated as a platform-global registry rather than a tenant-scoped table.
- **server/data-protection.ts:72** _(upstream-verified)_ — UPDATE on conversations omits tenant scoping. The softDeleteConversation function verifies the conversation belongs to the caller via a prior SELECT with tenant_id, but the subsequent UPDATE uses WHERE id = ${conversationId} with no tenant_id predicate, allowing an update by id that could affect a different tenant if ids collide or a future code change bypasses the earlier check.
  - _Why false positive:_ A prior SELECT in the same function already enforces id + tenant_id + deleted_at IS NULL before this UPDATE on the same conversation id.
- **server/heartbeat.ts:101** _(upstream-verified)_ — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — update lacks tenant_id constraint. This can modify a schedule row belonging to another tenant.
  - _Why false positive:_ sched.id comes from the immediately preceding SELECT of research_schedules rows, so this update advances the same row already loaded in this call path
- **server/heartbeat.ts:106** _(upstream-verified)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — update lacks tenant_id constraint.
  - _Why false positive:_ sched.id is taken from the schedule row selected a few lines earlier in the same worker loop, not from an untrusted request boundary
- **server/heartbeat.ts:112** _(upstream-verified)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — update lacks tenant_id constraint.
  - _Why false positive:_ updates the same in-memory schedule row selected earlier in the same call path; no separate tenant lookup is needed here
- **server/heartbeat.ts:120** _(upstream-verified)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — update lacks tenant_id constraint.
  - _Why false positive:_ recovery update still targets the already-selected sched row from the same loop, so ownership/source was established upstream in this worker path
- **server/internal-resolver.ts:93** _(internal-worker-pk)_ — UPDATE event_log executed without constraining by tenant_id — finalizes/ mutates event rows across all tenants.
  - _Why false positive:_ Internal resolver finalizes event_log by server-side numeric eventId, not a request-derived tenant boundary; worker-style PK update.
- **server/internal-resolver.ts:170** _(internal-worker-pk)_ — UPDATE code_proposals mutates proposal rows without constraining by tenant_id — can archive proposals across tenants.
  - _Why false positive:_ Internal resolver archives code_proposals by server-provided proposalId from event payload in a server worker flow, not direct untrusted request input.
- **server/job-queue.ts:98** _(global-table)_ — claimDueJobs performs a global UPDATE on agent_jobs (status -> 'running') without any tenant_id constraint — can claim/modify jobs across tenants.
  - _Why false positive:_ claimDueJobs is the queue scheduler/worker claim path and is intentionally global across tenants for platform job processing.
- **server/job-queue.ts:134** _(global-table)_ — reclaimExpiredLeases step1 UPDATE sets status='failed_terminal' on agent_jobs without tenant scoping — may affect jobs for other tenants.
  - _Why false positive:_ Lease reclamation is part of global platform queue maintenance over agent_jobs, intentionally sweeping expired jobs across tenants.
- **server/job-queue.ts:148** _(global-table)_ — reclaimExpiredLeases step2 requeues expired agent_jobs (status -> 'pending') without tenant_id filtering — can requeue jobs from other tenants.
  - _Why false positive:_ Requeueing expired leases is global queue maintenance on agent_jobs, not a tenant-request data access path.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs to 'succeeded' using only id/attempts guards and no tenant_id constraint — allows completing jobs for other tenants.
  - _Why false positive:_ completeJob updates a claimed job by server-generated job id plus attempts fencing in worker flow, not request-derived tenant input.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob branch updates agent_jobs to 'failed_terminal' using id/attempts but no tenant_id filter — can mark other tenants' jobs as failed terminal.
  - _Why false positive:_ failJob terminal update is a worker-internal state transition by server-generated job id with attempts fence, not an untrusted cross-tenant boundary.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob retry branch updates agent_jobs back to 'pending' (with next_run_at) without tenant scoping — requeues jobs across tenants.
  - _Why false positive:_ failJob retry update is a worker-internal requeue by server-generated job id with attempts fence, not caller-controlled tenant access.
- **server/oauth-subscriptions.ts:906** _(global-table)_ — SELECT from tenant-scoped table `oauth_subscriptions` without a tenant_id filter (SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows across all tenants (sensitive tokens stored here).
  - _Why false positive:_ validateSubscriptionsOnStartup is an explicit startup validation sweep over all active subscriptions, a platform-global maintenance path.
- **server/oauth-subscriptions.ts:915** _(upstream-verified)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' — unconstrained read across all tenants. This exposes tenant-scoped rows.
  - _Why false positive:_ Startup-only validator function enumerates subscriptions globally in same call path for auto-reconnect; not a tenant-request path.
- **server/oauth-subscriptions.ts:1258** _(upstream-verified)_ — proactiveTokenRefresh uses SELECT provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE without tenant_id filtering — unconstrained scan of all tenants' oauth_subscriptions (contains secrets).
  - _Why false positive:_ proactiveTokenRefresh is a background refresh loop over all active subscriptions, not caller-scoped tenant access.
- **server/oauth-subscriptions.ts:1328** _(upstream-verified)_ — forceRefreshAllSubscriptions does SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant_id filter — unconstrained cross-tenant read of tenant-scoped table.
  - _Why false positive:_ forceRefreshAllSubscriptions is explicitly for daily cron/global maintenance across active subscriptions.
- **server/plan-executor.ts:117** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `plans` without tenant_id constraint in WHERE clause (appendExecutionLog updates plans by id only). This allows modifying a plan row belonging to a different tenant.
  - _Why false positive:_ appendExecutionLog is internal worker code updating by server-generated plan primary key, not request-derived tenant input.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE claiming a plan (UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved') omits tenant_id in the WHERE clause. This can cause claiming/executing an approved plan belonging to another tenant.
  - _Why false positive:_ executePlan is internal executor claiming a plan by primary key; planId comes from worker scheduling/recovery, not untrusted request boundary here.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE against tenant-scoped table `plans` (setting status='failed') lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ This write follows the successful claim RETURNING row for the same plan id inside executePlan; ownership is already fixed by that claimed row.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final UPDATE to `plans` (setting final status and appending execution_log) does not constrain by tenant_id in the WHERE clause, allowing cross-tenant modification.
  - _Why false positive:_ Final status update occurs in same executePlan call after the plan row was claimed and bound by id; no new tenant boundary is crossed.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE resetting plan status to 'failed' includes WHERE id = ${planId} AND status = 'executing' but omits tenant_id, allowing altering another tenant's plan in error handling.
  - _Why false positive:_ Error-path reset is internal executor cleanup by in-flight plan primary key, not driven by tenant-controlled input.
- **server/plan-executor.ts:775** _(upstream-verified)_ — resumeStuckPlans performs a global UPDATE on `plans` resetting status='approved' for any stale executing plan without tenant scoping. This updates rows across tenants with no tenant filter.
  - _Why false positive:_ resumeStuckPlans is boot-time recovery for the internal executor, intentionally operating globally rather than from a tenant request context.
- **server/plan-executor.ts:784** _(upstream-verified)_ — SELECT id FROM plans WHERE status = 'approved' ... fetches approved plans across all tenants with no tenant_id filter, then kicks executePlan for each — this may start execution for plans of other tenants without tenant scoping or authorization checks.
  - _Why false positive:_ Pending-plan scan is part of boot-time internal recovery/scheduler and not exposed to tenant-scoped callers.
- **server/project-brain.ts:349** _(upstream-verified)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed without tenant filtering. Conversations are tenant-scoped and must be constrained.
  - _Why false positive:_ conversations are selected by project_id from proj.id obtained from prior projects/conversations query in same internal backfill flow
- **server/project-brain.ts:359** _(upstream-verified)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping. Message rows are tenant-scoped and this allows reading messages across tenants if conversation ids are global or uncontrolled.
  - _Why false positive:_ messages are read by conversation_id from conv.id fetched immediately above from conversations already tied to selected project
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped table (code_proposals) by id only, with no tenant_id constraint or tenant context. This allows cross-tenant access to proposals.
  - _Why false positive:_ server-side verifier worker reads code_proposals by server-invoked proposalId primary key, not from request boundary in shown path
- **server/proposal-verifier.ts:112** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} clears rebased_at / writes validation_result without constraining by tenant_id. This may modify another tenant's proposal rows.
  - _Why false positive:_ internal verifier updates proposal by server-side proposalId primary key during worker processing
- **server/proposal-verifier.ts:250** _(internal-worker-pk)_ — UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} persists verification results without tenant scoping. This allows updating proposals from other tenants if ids are abused.
  - _Why false positive:_ persist() is part of internal verifier worker and writes by server-side proposalId primary key
- **server/proposal-verifier.ts:379** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', verification_status = 'skipped', ... WHERE id = ${proposalId} marks a proposal rejected without a tenant filter. This can change status of proposals across tenants.
  - _Why false positive:_ rebase worker marks proposal by server-side proposalId primary key within internal processing flow
- **server/proposal-verifier.ts:399** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking 'hallucinated' rebase outcome) is executed without tenant scoping, enabling cross-tenant updates.
  - _Why false positive:_ internal rebase/verifier worker records hallucinated outcome by trusted proposal primary key
- **server/proposal-verifier.ts:412** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without constraining by tenant_id. That can overwrite or leak other tenants' proposal content.
  - _Why false positive:_ internal rebase worker persists rebased diff by trusted server-side proposal primary key
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE conversations SET project_id ... WHERE id = ${conv.id} executes without constraining by tenant_id. This is a tenant-scoped table but the UPDATE lacks a tenant predicate.
  - _Why false positive:_ conv was just created with tenantId and project ownership was verified by SELECT on projects with tenant_id = tenantId in the same path
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not set tenant_id. If project_conversations is tenant-scoped, the INSERT omits tenant_id entirely, violating the invariant that INSERTs must include tenantId.
  - _Why false positive:_ project_conversations has no tenant_id and is a link table isolated transitively via tenant-scoped projects/conversations
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} lacks a tenant_id predicate. Projects is tenant-scoped and this UPDATE can affect a project across tenants.
  - _Why false positive:_ same call path already verified project exists with id = projectId AND tenant_id = tenantId before this update
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author) VALUES (...) does not include tenant_id. project_notes is likely tenant-scoped but the INSERT omits tenant_id, allowing notes to be written without tenant context.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; query derives projId from join enforcing project tenant ownership
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} is executed without constraining by tenant_id. This is a tenant-scoped table and the UPDATE can modify rows belonging to other tenants.
  - _Why false positive:_ msg was fetched immediately above with SELECT * FROM inbox_messages WHERE id = msgId AND tenant_id = tenantId; update uses that verified row id
- **server/sculptor.ts:93** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `sculptor_sessions` performed with WHERE id = ${session.id} but NO tenant_id constraint. This can allow cross-tenant modification if an attacker supplies or guesses an id.
  - _Why false positive:_ updates newly inserted sculptor_sessions row by server-generated session.id within internal worker flow, not from request params/body
- **server/sculptor.ts:100** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `sculptor_sessions` (setting conversation_id) uses WHERE id = ${session.id} with NO tenant_id constraint.
  - _Why false positive:_ sets conversation_id on the just-created session using internal server-generated session.id in same function
- **server/sculptor.ts:155** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `sculptor_sessions` (final status/result update) uses WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Why false positive:_ background monitor updates sculptor_sessions by internal sessionId/runId created server-side, not an untrusted cross-tenant request boundary
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_cost_ledger` (settling a reservation) uses WHERE id = ${reservationId} but NO tenant_id constraint. This can permit modifying another tenant's ledger row by id collision/guessing.
  - _Why false positive:_ settles a server-created reservation row by internal reservationId in helper flow, not from untrusted request input
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_cost_ledger` (releasing a reservation) uses WHERE id = ${reservationId} with NO tenant_id constraint.
  - _Why false positive:_ releases a server-created reservation row by internal reservationId in helper flow, not from untrusted request input
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) inserts into a (likely) tenant-scoped relationship table without setting tenant_id. Per invariant, tenant_id must be provided on INSERTs.
  - _Why false positive:_ project_conversations has no tenant_id and this seed backfill populates a transitive link table between tenant-scoped parent records
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — UPDATE on experiments is constrained only by experiments.id (WHERE eq(experiments.id, experiment.id)) and does not include tenant_id. This allows modifying another tenant's experiment row by id.
  - _Why false positive:_ Server-side self-improvement worker updates experiments by server-generated primary key experiment.id, not from an untrusted request boundary.
- **server/self-improvement.ts:413** _(internal-worker-pk)_ — Error-path UPDATE on experiments likewise uses only experiments.id in the WHERE clause and lacks a tenant_id constraint, enabling cross-tenant modification on error handling.
  - _Why false positive:_ Error-path status update is in the same internal worker flow, keyed by server-held experiment.id rather than caller-controlled input.
- **server/ab-optimizer.ts:110** _(internal-worker-pk)_ — SELECT from `experiments` (runDueAbExperiments) is missing a tenant filter — it reads experiments across all tenants.
  - _Why false positive:_ Heartbeat-callable internal scheduler scans due experiments across tenants and then updates each row with its own tenant_id; this is server-invoked background processing, not request-driven cross-tenant access.
- **server/agentic-engines.ts:160** _(internal-worker-pk)_ — SELECT FROM `ai_insights` (retryPendingMinervaRouting) is scanning ai_insights across all tenants (no tenant_id WHERE clause).
  - _Why false positive:_ Durability sweep is a server-run background worker intentionally iterating pending insights across tenants; rows carry tenant_id forward into scoped follow-up updates.
- **server/agentic-engines.ts:344** _(global-table)_ — In gatherOperationalData the heartbeat_logs query SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection' has no tenant filter and thus reads logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is described as platform-global logging/telemetry.
- **server/agentic-engines.ts:518** _(global-table)_ — heartbeatPerformance query in runOptimizationEngine queries `heartbeat_logs` without a tenant_id WHERE clause (aggregates across tenants).
  - _Why false positive:_ heartbeat_logs has no tenant_id column; aggregate performance query matches global operational logs pattern.
- **server/agentic/runs.ts:36** _(internal-worker-pk)_ — appendStep() updates agent_runs with WHERE id = runId but no tenant_id filter — write into another tenant's run row is possible.
  - _Why false positive:_ appendStep is an internal helper updating agent_runs by server-generated primary key runId, not shown as coming from request input.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs using only runId (no tenant_id constraint).
  - _Why false positive:_ updateRunState updates by internal runId primary key for server-managed worker state transitions.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs with .where(eq(agentRuns.id, runId)) but no tenant_id filter.
  - _Why false positive:_ replaceRunState targets a server-created agent_runs row by primary key in internal worker flow.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs.status/result/completedAt using only runId in WHERE — missing tenant_id constraint.
  - _Why false positive:_ completeRun is an internal lifecycle transition on agent_runs addressed by server-generated runId.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs setting status='failed' etc. but the WHERE clause only uses runId; no tenant_id constraint present.
  - _Why false positive:_ failRun is internal run-state mutation by globally unique server-generated runId.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() updates agent_runs.status/state with WHERE id = runId but no tenant_id filter.
  - _Why false positive:_ pauseRun is an internal worker/state helper operating on agent_runs via server-generated primary key.
- **server/auto-transcript.ts:128** _(global-table)_ — SELECT over conversations (backfill) without tenant_id constraint — reads tenant-scoped rows across all tenants.
  - _Why false positive:_ Backfill function is a server-side global maintenance sweep across all tenants, not a request-scoped tenant boundary.
- **server/auto-transcript.ts:153** _(global-table)_ — SELECT joining project_conversations, conversations, projects lacks a tenant_id filter — may return cross-tenant rows.
  - _Why false positive:_ Server backfill/repair job intentionally scans all tenants to reconcile project conversation links and transcripts.
- **server/auto-transcript.ts:171** _(global-table)_ — SELECT to find missing project_conversations links reads conversations across all tenants (no tenant_id filter).
  - _Why false positive:_ Global repair pass for missing project_conversations links; not a caller-controlled per-tenant read path.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` is executed by id only, without constraining to the owning tenant_id. This allows marking/altering another tenant's review row if an attacker can guess an id or if pending.reviewId becomes tainted.
  - _Why false positive:_ Update is by server-generated pending.reviewId from internal in-memory worker state, not directly from an untrusted request boundary.
- **server/credential-vault.ts:169** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table 'credential_vault' — `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` omits tenant_id constraint. This can update another tenant's credential if ids overlap.
  - _Why false positive:_ Same call path first fetches the credential with getCredential(id, tenantId); subsequent last_used_at update targets that already tenant-verified row id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table 'credential_vault' — `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` omits tenant_id constraint when updating last_used_at after returning credentials.
  - _Why false positive:_ Same call path first fetches cred via getCredentialForSite(siteUrl, tenantId); update is on the id of that tenant-verified row.
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM doc_chunks executed without tenant_id filter (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). This can delete chunks belonging to other tenants.
  - _Why false positive:_ Immediately above, collection ownership is verified with SELECT id FROM doc_collections WHERE id = collectionId AND tenant_id = tenantId before deleting matching chunks for that collection.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} is executed without tenant_id constraint. Updating by id only can modify another tenant's chunk if id is global.
  - _Why false positive:_ Rows being updated were selected just above from doc_chunks with WHERE collection_id = collectionId AND tenant_id = tenantId; row.id comes from that tenant-scoped result set.
- **server/dream-consolidation.ts:83** _(upstream-verified)_ — Query for messages inside getRecentSessionSummaries: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ... lacks tenant scoping. This can read messages from other tenants' conversations.
  - _Why false positive:_ Messages are fetched for conv.id values obtained from conversations already constrained by tenant_id = tenantId in the same function.
- **server/dormant-deprecation.ts:169** _(global-table)_ — Traffic gate query counts user messages across the messages table without tenant_id (SELECT COUNT(*) FROM messages WHERE ... AND role = 'user'). If messages is tenant-scoped, this aggregates across all tenants rather than scoping to a tenant.
  - _Why false positive:_ This function computes platform-wide traffic gate metrics; the code aggregates across all messages intentionally rather than serving a tenant request.
- **server/dormant-deprecation.ts:179** _(global-table)_ — Query counting distinct tools active uses tool_performance without tenant filter (SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ...). This may aggregate across tenants if tool_performance is tenant-scoped.
  - _Why false positive:_ The distinct tool activity count is part of the same platform-level traffic snapshot and is intentionally global, not tenant-scoped.
- **server/email.ts:365** _(global-table)_ — Polling loop checks for existing inbox_messages using SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 with no tenant_id. This may cause cross-tenant detection/duplication or store messages incorrectly across tenants.
  - _Why false positive:_ This is a poller for a single shared inbox using globally unique provider message_id values; dedupe is intentionally global across inbox_messages.
- **server/email.ts:410** _(global-table)_ — backfillEmptyBodies runs SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') ... without tenant_id, iterating over all inbox_messages possibly across tenants.
  - _Why false positive:_ backfillEmptyBodies is a global maintenance job over the shared inbox_messages store, not a tenant-request path.
- **server/embeddings.ts:252** _(upstream-verified)_ — Use of sql.raw with an interpolated vector literal in ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} — vector is request-derived.
  - _Why false positive:_ Query is explicitly scoped with WHERE tenant_id = ${tenantId}; flagged raw vector interpolation is not a cross-tenant isolation issue.
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw with an interpolated vector literal: sql.raw(`'${vec}'::vector`) in vectorSearchKnowledge. The vector is derived from the caller's query (request-derived) and is being injected into raw SQL.
  - _Why false positive:_ SELECT on agent_knowledge includes AND tenant_id = ${tenantId}; issue is SQL injection style, not tenant isolation.
- **server/embeddings.ts:565** _(upstream-verified)_ — Use of sql.raw with an interpolated vector literal in ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} — vector is request-derived.
  - _Why false positive:_ ORDER BY uses raw vector but same query already constrains agent_knowledge by tenant_id = ${tenantId}.
- **server/firecrawl.ts:184** _(upstream-verified)_ — UPDATE against tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) and lacks an explicit tenant_id constraint — a write that can affect other tenants' rows.
  - _Why false positive:_ Updated row id comes from an immediately prior tenant-scoped SELECT on scraped_pages by url and tenantId.
- **server/firecrawl.ts:307** _(upstream-verified)_ — UPDATE of scraped_pages in the crawl loop uses only the page id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) without constraining tenant_id — cross-tenant write risk.
  - _Why false positive:_ Updated row id is obtained from the preceding SELECT constrained by scrapedPages.tenantId = tenantId.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' fetches driveFolderId/driveFolderUrl by project id using WHERE eq(projects.id, projectId) but does not include tenant scoping (tenant_id). Sensitive project Drive metadata is therefore read without constraining to caller tenant.
  - _Why false positive:_ Code immediately verifies project.tenantId === tenantId before returning or using project Drive metadata.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' (setting driveFolderId/driveFolderUrl) uses WHERE eq(projects.id, projectId) without constraining by tenant_id — this can update another tenant's project row.
  - _Why false positive:_ UPDATE occurs only after same-call-path ownership check that loaded the project and rejected tenant mismatch.
- **server/google-drive.ts:826** _(upstream-verified)_ — Handler uploadAndShare selects project row by id (WHERE eq(projects.id, params.projectId)) without filtering by tenant. This reads a tenant-scoped row based on a caller-supplied project_id without the required tenant filter.
  - _Why false positive:_ Caller-side check immediately after SELECT enforces proj.tenantId matches params.tenantId before use.
- **server/google-drive.ts:1003** _(upstream-verified)_ — searchDriveFiles does an initial SELECT of projects by project id with WHERE eq(projects.id, params.projectId) and then checks proj.tenantId against params.tenantId — but the SELECT itself lacks an explicit tenant_id constraint (tenant-scoped read without WHERE tenant_id).
  - _Why false positive:_ The purpose of this SELECT is immediate ownership verification; code fail-closes if proj.tenantId does not equal params.tenantId.
- **server/google-drive.ts:1066** _(upstream-verified)_ — Later in searchDriveFiles the code selects projects.driveFolderId using WHERE eq(projects.id, params.projectId) without tenant scoping. Even though an earlier check validated ownership, the DB query itself is not constrained to the tenant.
  - _Why false positive:_ Earlier in the same function, project ownership is already verified against params.tenantId before this driveFolderId lookup.
- **server/heartbeat.ts:257** _(upstream-verified)_ — UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} executes without a tenant_id constraint — this can update another tenant's task row.
  - _Why false positive:_ t.id comes from earlier in-function iteration over allTasks, so the update targets the row just read rather than untrusted request input
- **server/heartbeat.ts:261** _(upstream-verified)_ — UPDATE heartbeat_tasks SET model = '...' WHERE id = ${t.id} (second normalization) lacks tenant_id filter.
  - _Why false positive:_ t.id is sourced from the previously loaded task row in the same loop, so this normalization update is on the already-selected row
- **server/heartbeat.ts:2836** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id in the WHERE clause. This write can affect approval_state across tenants.
  - _Why false positive:_ newTask.id is the server-returned id from createHeartbeatTask called with tenantId in the same path; update applies to that freshly created row
- **server/heartbeat.ts:3289** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat path) — missing tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ newTask.id comes from same-call createHeartbeatTask with tenantId, so approval_status update is for the just-created tenant-scoped row
- **server/job-queue.ts:220** _(internal-worker-pk)_ — SELECT max_attempts FROM agent_jobs WHERE id = ${id} runs without tenant_id constraint — reads job metadata across tenants.
  - _Why false positive:_ agent_jobs lookup is by server-side worker job primary key inside internal queue handling, not from a request tenant boundary
- **server/lib/aevo-meta-editor.ts:447** _(upstream-verified)_ — UPDATE on procedureEvolutionRuns performed with only id predicate (where(eq(procedureEvolutionRuns.id, runId))) — no tenant_id constraint. This can mutate another tenant's row if an attacker knows/guesses a run id.
  - _Why false positive:_ runId is the id returned from the immediately preceding insert of procedureEvolutionRuns with the same tenantId, so update targets that created row
- **server/lib/aevo-meta-editor.ts:459** _(upstream-verified)_ — UPDATE on procedureEvolutionRuns performed with only id predicate (where(eq(procedureEvolutionRuns.id, runId))) — no tenant_id constraint. Allows cross-tenant modification of the run row.
  - _Why false positive:_ runId was just created by prior insert in the same function under tenantId, so this status update is on that same run row
- **server/lib/scheduled-post-runner.ts:60** _(global-table)_ — SELECT ... FROM scheduled_posts (CTE 'due') is executed with no tenant_id filter — the runner reads tenant-scoped rows across all tenants.
  - _Why false positive:_ system heartbeat/runner intentionally processes due scheduled_posts across tenants; this is platform-wide worker behavior, not request-scoped access
- **server/lib/scheduled-post-runner.ts:164** _(upstream-verified)_ — UPDATE scheduled_posts ... WHERE id = ${row.id} is executed without also constraining tenant_id — this allows updates to a scheduled_posts row regardless of tenant ownership.
  - _Why false positive:_ row ids come only from the prior CTE that selected and locked the exact scheduled_posts rows being processed, so this update is bound to those worker-owned rows
- **server/lib/scheduled-post-runner.ts:191** _(upstream-verified)_ — UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' (error/release path) lacks a tenant_id filter — a row could be modified across tenants.
  - _Why false positive:_ error-path release updates the same row ids previously claimed by the runner CTE; status='publishing' further narrows to the worker-selected row
- **server/memory-graph.ts:66** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_categories omits tenant_id in WHERE clause (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This allows modifying a category row without constraining to the caller's tenant.
  - _Why false positive:_ catId is obtained in the same call path from tenant-scoped select/insert using tenantId, so this increment targets that verified category row
- **server/memory-graph.ts:393** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries omits tenant_id in WHERE clause (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). Even though mem.id was selected earlier with a tenant filter, the UPDATE itself is not constrained to tenant and violates the application-level isolation invariant.
  - _Why false positive:_ mem.id was selected earlier in the same function with tenant_id = tenantId before this update, so the row identity is already tenant-bound in this call path
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES. The table appears tenant-scoped but the INSERT omits tenant_id (relying only on assertProjectInTenant prior to insert).
  - _Why false positive:_ project_files has no tenant_id column and is scoped transitively via tenant-owned project_id; assertProjectInTenant verifies parent ownership.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id in the VALUES. Same pattern as above in the parallel render path.
  - _Why false positive:_ project_files has no tenant_id column and is scoped through project_id to the tenant-owned parent project; ownership checked before insert.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates a tenant-scoped table using only id and no tenant_id constraint. An attacker or wrong id could modify another tenant's rows.
  - _Why false positive:_ t.id comes from an immediately prior SELECT restricted to ht.tenant_id = tenantId in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} performs a write without restricting by tenant_id. The SELECT that produced desk came from a tenant-scoped query, but the UPDATE must still enforce tenant scoping to prevent cross-tenant modification.
  - _Why false positive:_ desk.id is sourced from the preceding SELECT on agent_desks filtered by ad.tenant_id = tenantId in the same loop path.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${storedId} executes without constraining tenant_id (tenant-scoped table). This allows cross-tenant modification if IDs collide or if function is called outside tenant context.
  - _Why false positive:_ storedId is obtained immediately above from a SELECT on memory_entries constrained by tenant_id and persona_id in the same path.
- **server/research-engine.ts:320** _(global-table)_ — SELECT name FROM personas WHERE id = ${program.persona_id} executes without constraining personas by tenant_id. If personas are tenant-scoped, this permits reads of other tenants' persona rows.
  - _Why false positive:_ schema note says personas has no tenant_id column, so lookup is against a non-tenant-scoped table
- **server/research-engine.ts:979** _(global-table)_ — resolvePersonaId performs SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 with no tenant_id constraint — it can return persona ids from other tenants.
  - _Why false positive:_ personas is documented with no tenant_id column; resolving by name is not a cross-tenant read
- **server/research-engine.ts:1001** _(global-table)_ — injectKeepedFinding calls resolvePersonaId (which is unscoped) and then INSERTs agent_knowledge with persona_id set from that result without verifying persona ownership — a foreign id from an unvalidated source is used when writing tenant-scoped rows.
  - _Why false positive:_ persona_id comes from a lookup in personas, which schema marks as non-tenant-scoped/global
- **server/routes.ts:1116** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` by filename alone (owners lookup) — no tenant_id filter. The code fetches all owners of a basename across tenants.
  - _Why false positive:_ unscoped basename lookup is used only to verify whether the authenticated tenant owns the file, while delivery assets rely on signed-URL authorization and non-owners get 404
- **server/routes.ts:2056** _(signed-url)_ — Public GET selects presenter_sessions by token and returns tenant_id and other fields without verifying caller ownership. Reading a tenant-scoped table without tenant scoping can leak tenant information.
  - _Why false positive:_ presenter token is used as the capability selector and response does not expose tenant_id despite selecting it internally
- **server/routes.ts:4331** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts a project note (project_id, note, author) without supplying tenant_id, and the project_id was derived from a prior SELECT that lacked tenant scoping/ownership verification.
  - _Why false positive:_ project_notes has no tenant_id column per schema and is tenant-isolated transitively through tenant-scoped parent project_id
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. Tenant-scoped tables require tenant_id to be explicitly stored on INSERT to enforce isolation (FKs / app checks alone are insufficient).
  - _Why false positive:_ project_notes has no tenant_id and insert is preceded by tenant-scoped parent project check on projects.id + tenant_id
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (base64 upload path) does not set tenant_id. Tenant-scoped files must record tenant_id on creation.
  - _Why false positive:_ project_files has no tenant_id and row is inserted only after verifying parent project belongs to tenant
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at ... WHERE id = ${projectId} lacks a tenant_id predicate. Updating a tenant-scoped row without constraining by tenant_id can allow cross-tenant updates if an ID is incorrect or attacker-controlled.
  - _Why false positive:_ same handler already verified projectId belongs to requester tenant via SELECT on projects with tenant_id before this update
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into project_files (multipart upload path) does not set tenant_id. New project_files rows are created without explicit tenant ownership recorded.
  - _Why false positive:_ project_files has no tenant_id and insert occurs after tenant-scoped parent project ownership check
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at ... WHERE id = ${projectId} (after multipart uploads) lacks tenant_id in WHERE clause, allowing updates without explicit tenant scoping.
  - _Why false positive:_ same multipart-upload path verifies project ownership with id + tenant_id before updating updated_at
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks tenant_id in the WHERE clause. Deletions on tenant-scoped tables must include tenant_id to prevent cross-tenant deletion if an ID is tampered with.
  - _Why false positive:_ project_files has no tenant_id and delete is limited to child row under already-verified tenant-owned parent project
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT into project_conversations uses project_id and conversation_id derived from an earlier unfiltered query; the code does not verify tenant ownership of those foreign ids. This can create cross-tenant links.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table; insert is into tenantless FK table by design, with risk stemming from source SELECT not this insert itself.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant_id — query uses only conversationId. Per invariant, reads against tenant-scoped tables must include tenant filter in SQL.
  - _Why false positive:_ distillIntent first calls analyzeConversation(conversationId, tenantId), which verifies conversation ownership before this messages-by-conversation query in the same call path.
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` (by conversationId) lacks a tenant_id constraint. The messages query relies on a prior in-app check rather than enforcing tenant scoping in SQL.
  - _Why false positive:_ Immediately preceded in same function by conversation fetch and tenant ownership check; messages are then read only for that verified conversationId.
- **server/skillify.ts:223** _(global-table)_ — SELECT from `personas` uses persona id only (eq(personas.id, conv[0].personaId)) with no tenant filter. If `personas` is tenant-scoped, this allows cross-tenant reads.
  - _Why false positive:_ Schema note shows personas has no tenant_id column, so this is not a tenant-scoped table read.
- **server/skillify.ts:331** _(upstream-verified)_ — Another SELECT from tenant-scoped table `messages` (recentMsgs in skillifyConversation) omits tenant_id in the WHERE clause — it only filters by conversationId.
  - _Why false positive:_ skillifyConversation calls analyzeConversation(conversationId, tenantId) before this query, and that helper enforces conversation ownership in the same call path.
- **server/storage.ts:276** _(upstream-verified)_ — attachCitationMetadata performs a raw SELECT from messages by id without constraining tenant_id. messages is tenant-scoped; this can read citations belonging to other tenants.
  - _Why false positive:_ attachCitationMetadata only re-reads message rows for ids sourced from already tenant-scoped getMessages/getMessagesPaginated queries in the shown call path.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT against tenant-scoped table `custom_tools` without constraining by tenant_id. The query `db.select().from(customTools).where(eq(customTools.name, parsed.name))` can read rows across tenants.
  - _Why false positive:_ name is transformed with tenantToolName(parsed.name, tenantId) immediately above, making the lookup tenant-specific by namespacing.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT by id against tenant-scoped table `custom_tools` without tenant_id constraint. `db.select().from(customTools).where(eq(customTools.id, inserted.id))` may return a row belonging to a different tenant.
  - _Why false positive:_ select re-reads the row just inserted in the same flow using returned inserted.id; id is server-derived from that insert, not a cross-tenant request boundary.
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE against tenant-scoped table `custom_tools` without tenant_id constraint. `db.update(customTools).set(...).where(eq(customTools.id, tool.id))` can update another tenant's row if id collides or is misused.
  - _Why false positive:_ update targets tool.id from the just-inserted/validated tool object within same internal flow, not attacker-controlled lookup across tenants.
- **server/tool-learning.ts:316** _(upstream-verified)_ — DELETE against tenant-scoped table `custom_tools` without tenant_id constraint. `db.delete(customTools).where(eq(customTools.id, tool.id))` will delete across tenants if not scoped.
  - _Why false positive:_ delete targets tool.id from the same just-inserted validation flow, so ownership is established by prior insert in this call path.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE (usageCount increment) against tenant-scoped table `custom_tools` without tenant_id constraint. `db.update(customTools).set({ usageCount: ... }).where(eq(customTools.id, tool.id))` lacks tenant scoping.
  - _Why false positive:_ tool row was selected immediately above with tenant_id = tenantId; update uses that verified row.id
- **server/tools.ts:2874** _(fk-transitive)_ — INSERT into project_conversations does not include tenant_id. This inserts a tenant-scoped association row without explicitly stamping the caller's tenant, relying on prior updates/ownership checks instead of writing tenant_id into the row.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped by tenant-checked project/conversation parents
- **server/tools.ts:2978** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. The code inserts a project_files row with project_id but does not explicitly write tenant_id into the new row.
  - _Why false positive:_ project_files has no tenant_id and insert is preceded by tenant-scoped project ownership check
- **server/tools.ts:2992** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. A project-scoped note row is inserted without explicitly stamping the tenant.
  - _Why false positive:_ project_notes has no tenant_id and insert is preceded by tenant-scoped project ownership check
- **server/tools.ts:3016** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) omits tenant_id when creating the association row.
  - _Why false positive:_ project_conversations has no tenant_id; both project ownership and conversation tenant membership are verified before insert
- **server/tools.ts:6893** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The raw SQL INSERT at line 6893 inserts project file metadata but does not set the tenant_id column (even though tenant ownership was checked earlier). Because tenant_id columns are NOT defaulted/enforced at the DB level, this will create a row with no tenant association and can break isolation/auditing.
  - _Why false positive:_ project_files has no tenant_id and insert occurs only after assertProjectInTenant passes
- **server/tools.ts:8853** _(upstream-verified)_ — UPDATEs against tenant-scoped table `proposed_skills` lack tenant constraint in WHERE clause. Multiple db.update(...) calls update proposed_skills using only eq(proposedSkills.id, row.id) (lines ~8853, 8878, 8895) without restricting by tenant_id. This allows one tenant to update another tenant's proposed_skills row.
  - _Why false positive:_ update targets proposedSkills row.id returned from same-function insert for this tenant, not caller-supplied id
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table 'browser_workflows' omits tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}).
  - _Why false positive:_ workflow was loaded just above with id/name plus tenant_id = tenantId; update uses that verified workflow.id
- **server/tools/domains/files/handlers.ts:838** _(fk-transitive)_ — INSERT into project_files does not include tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)). This writes a tenant-scoped artifact without explicitly setting tenant ownership.
  - _Why false positive:_ project_files has no tenant_id and project_id comes from tenant-scoped conversation→project join
- **server/tools/domains/files/handlers.ts:841** _(upstream-verified)_ — UPDATE on projects table omits tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Even though projId was discovered via a tenant-scoped join earlier, the UPDATE is not constrained and could modify a project id belonging to another tenant.
  - _Why false positive:_ projId was derived from prior join to projects constrained by p.tenant_id = tenantId
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table 'knowledge_triples' lacks a tenant_id constraint in the WHERE clause: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}`. This could allow cross-tenant row modification if ids are misused or in future code paths.
  - _Why false positive:_ rows being updated were selected immediately above from knowledge_triples with tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table 'provider_keys' without constraining tenant_id. The query: SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' — no WHERE tenant_id = ${tenantId}.
  - _Why false positive:_ Schema note says provider_keys has no tenant_id; admin-gated global audit query on non-tenant-scoped table.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT from tenant-scoped table 'provider_keys' without constraining tenant_id. The query: SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != '' — no WHERE tenant_id = ${tenantId}.
  - _Why false positive:_ provider_keys has no tenant_id per schema and this count is only run for tenantId===1 admin path.
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint. This violates the invariant that every write to tenant-scoped tables must be constrained to the caller's tenant and could allow cross-tenant modifications if the id is ever derived from an untrusted source.
  - _Why false positive:_ id comes directly from prior SELECT on memory_entries constrained by tenant_id, persona_id, and category in same function.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with no tenant_id constraint. Even though ids were derived from a prior tenant-scoped SELECT in this function, the invariant requires every write to include an explicit tenant_id constraint to prevent accidental cross-tenant updates in future refactors or if ids are influenced externally.
  - _Why false positive:_ ids are derived from rows selected just above with WHERE tenant_id = tid and persona_id/category filters in same call path.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint (in recordStrategicWinHandler). This lacks the required tenant scoping on writes to tenant-scoped tables.
  - _Why false positive:_ id being updated was fetched immediately above from memory_entries with explicit tenant_id and persona_id constraints.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} executed without a tenant_id constraint. Reading a tenant-scoped `conversations` row by id alone can be used to infer/validate or be abused across tenants.
  - _Why false positive:_ Read by conversation id is intentionally verifying the claimed tenant by comparing returned tenant_id to info.tenantId before use; fail-closed on mismatch.
- **server/webhook-triggers.ts:60** _(global-table)_ — listTriggers SELECT LEFT JOINs into the personas table without any tenant constraint. This reads tenant-scoped personas (p.name) with no WHERE p.tenant_id = ... filter and can leak persona data across tenants.
  - _Why false positive:_ Schema notes personas has no tenant_id, and webhook_triggers is created here without tenant_id, indicating platform-global trigger registry.
- **server/webhook-triggers.ts:89** _(global-table)_ — createTrigger INSERT writes persona_id from caller-supplied config.personaId directly into webhook_triggers without validating that the referenced persona belongs to an allowed tenant. This allows creating triggers that reference personas from other tenants (ownership/spoofing risk).
  - _Why false positive:_ Both webhook_triggers and referenced personas appear global/non-tenant-scoped here; no tenant ownership invariant applies from provided schema.
- **server/webhook-triggers.ts:122** _(global-table)_ — processTriggerEvent SELECT queries webhook_triggers and LEFT JOINs personas (p.name) without scoping the joined personas to a tenant. The code then uses trigger.persona_id for downstream processing, potentially using a persona from another tenant without ownership verification.
  - _Why false positive:_ Joined personas table has no tenant_id per schema and webhook_triggers lookup is by global webhook_key in a global trigger system.
- **server/wiring-invariants.ts:57** _(global-table)_ — SELECT from research_schedules (joined to research_programs) is executed without constraining to a tenant. If research_schedules is tenant-scoped, this reads across tenants and can break the invariant that all tenant-scoped reads must include tenant_id.
  - _Why false positive:_ This is a platform wiring/invariant check over research schedules/programs with no tenant-scoped table evidence in provided schema.
- **server/wiring-invariants.ts:244** _(global-table)_ — Query against tool_performance (SELECT count(*) WHERE last_success_at > ...) is executed with no tenant_id filter. tool_performance is likely tenant-scoped, so this reads across tenants.
  - _Why false positive:_ This is a boot-time wiring invariant/health probe computing platform-wide tool tracking totals, not a tenant request path.
- **server/wiring-invariants.ts:258** _(global-table)_ — SELECT count(*) FROM tool_performance (everResult) is executed without tenant scoping — it returns a global count across tenants. For tenant-scoped tables every read must include tenant_id.
  - _Why false positive:_ Global historical count for tool_performance used for system health diagnostics; intentional platform-wide observability check.
- **server/wiring-invariants.ts:260** _(global-table)_ — SELECT count(*) FROM messages WHERE created_at > ... has no tenant_id constraint. messages are almost always tenant-scoped, so this aggregates across tenants incorrectly.
  - _Why false positive:_ Global chat-volume proxy inside wiring diagnostics to assess tracker health across the platform, not tenant data access for a caller.
- **server/wiring-invariants.ts:293** _(global-table)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... lacks tenant scoping. This will report tools used across all tenants, violating the requirement that reads against tenant-scoped tables include tenant_id.
  - _Why false positive:_ Distinct used-tools query is part of platform-wide dormant-tool detector/registry observability, not a tenant-scoped endpoint.
- **server/wiring-invariants.ts:417** _(global-table)_ — Tool-drift query selects from tool_performance (aggregations, HAVING) without any tenant_id constraint. This inspects invocation counts/failures across all tenants, violating tenant isolation rules for tenant-scoped tables.
  - _Why false positive:_ Tool drift detector aggregates tool_performance globally for operational health monitoring; explicit observability/admin logic.
- **server/wiring-invariants.ts:535** _(global-table)_ — Memory-growth probes use sql.raw and query COUNT/pg_total_relation_size on tables such as agent_knowledge, memory_entries, knowledge_triples, messages without tenant scoping. These tables are typically tenant-scoped and must be queried per-tenant or filtered by tenant_id.
  - _Why false positive:_ Memory-growth tripwire intentionally measures whole-table row counts and relation size as global capacity telemetry.
- **server/wiring-invariants.ts:561** _(global-table)_ — SELECT COUNT(*) FROM graph_memory is executed without a tenant_id WHERE clause while the code treats the result as 'empty across all tenants'. Querying a tenant-scoped table without filtering violates the requirement that all reads include tenant_id (or be explicitly global).
  - _Why false positive:_ Global empty-table bootstrap check explicitly asks whether graph_memory is empty across all tenants for platform readiness.
