# Tenant-Isolation Audit Report

_Generated 2026-09-20T09:49:10.393Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **689/689** file(s) audited · chunks 64/64 OK (0 failed)
- Findings: **218** (CRITICAL 44 · HIGH 137 · MEDIUM 37 · LOW 0)
- Severe dispositions: **12 kept** · 141 precision false-positive · 26 allowlist false-positive · 2 deferred real risk

### CRITICAL (44)

- **server/agentic/wake-scheduler.ts:145** — Global claim/update of agent_wake_schedules selects ids without constraining to a tenant. The inner SELECT used by the UPDATE (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) has no tenant_id predicate, allowing rows across all tenants to be selected/locked/updated in one statement.
  - _Fix:_ Restrict the row selection to a single tenant (or explicitly iterate tenants). Either add AND tenant_id = ${tenantId} to the inner WHERE (and accept a tenantId parameter), or change the sweep to perform a per-tenant claim (e.g. query tenant list and run a tenant-scoped FOR UPDATE SELECT per tenant). Ensure every SELECT/UPDATE against agent_wake_schedules includes tenant_id in the WHERE clause so no cross-tenant rows are touched.
- **server/auto-asset-capture.ts:145** — INSERT into project_files (project-scoped table with no tenant_id column) is performed using a project_id derived from a conversation row without explicitly verifying that the referenced project row belongs to the caller's tenant. Because project_files has no tenant_id, a poisoned conversation.project_id could let one tenant write files against another tenant's project.
  - _Fix:_ Before inserting into project_files, verify the project ownership explicitly (e.g. SELECT id FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}) and fail if no row is returned. Alternatively, include an additional check/transaction that ensures the project_id belongs to tenantId, or add tenant_id to project_files and set it on insert.
- **server/auto-asset-capture.ts:164** — INSERT into project_notes uses project_id (project-scoped table) without first verifying the referenced project belongs to the calling tenant. project_notes has no tenant_id column, so relying on a conversation-derived project_id without verifying the project's tenant enables cross-tenant writes if conversation.project_id is poisoned.
  - _Fix:_ As above, explicitly verify ownership of the project (SELECT id FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}) before writing project_notes. Prefer explicit server-side checks rather than trusting a transitive project_id value from another table.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id. Tenant isolation invariant requires every INSERT into tenant-scoped tables to include the caller's tenant (columns are NOT NULL with no default). As written, invoice_items rows can be created without an explicit tenant_id, risking cross-tenant data leakage or failed invariants.
  - _Fix:_ Include tenant_id in the invoice_items insert (and other line-item writes). For example: INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). Also ensure any later reads/updates on invoice_items include WHERE tenant_id = ${tid}. Consider adding a schema migration to make tenant_id NOT NULL and add a foreign-key/index, and add tests that verify invoice_items are always written/read with the correct tenant filter.
- **server/chat-engine.ts:3805** — Call to capability-review.markReviewReused(conversationId, toolName) is invoked with only conversation-derived values and NO tenant/tenantId context. If markReviewReused persists telemetry it may INSERT/UPDATE tenant-scoped rows without setting tenant_id or validating ownership.
  - _Fix:_ Pass the caller's tenantId explicitly (e.g. markReviewReused(tenantId, conversationId, toolName)) or have the callee validate the conversation->tenant ownership before any DB writes. Ensure all DB INSERTs set tenant_id and all SELECT/UPDATE/DELETE use a tenant_id WHERE clause (or equivalent Drizzle .where(eq(table.tenantId, tenantId))).
- **server/chat-engine.ts:3926** — Call to stuck-diagnostics.recordErrorForRetryStormDetection(conversationId, ...) is invoked without tenant context. If that function persists diagnostics it may write tenant-scoped rows without tenant_id filtering or without setting tenant_id on INSERT.
  - _Fix:_ Include tenantId when calling recordErrorForRetryStormDetection or require the callee to accept/validate tenant context and enforce tenant_id on any DB writes. All persistence paths must set tenant_id explicitly and validate referenced foreign IDs' ownership before accessing/updating related rows.
- **server/chat-engine.ts:4161** — Call to stuck-diagnostics.recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is made without tenant/tenantId. The subsequent postDiagnosticReport([stuckPattern]) call also runs without tenant context. These telemetry/diagnostic functions may persist data without tenant_id or read referenced rows without verifying ownership.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection and postDiagnosticReport (or ensure the callee validates tenant ownership of conversationId) and ensure any INSERT/UPDATE/DELETE include tenant_id in the values/WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))). Audit the callee modules to guarantee they never write tenant-scoped tables without an explicit tenant parameter or ownership verification.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' executed without any tenant_id constraint (unscoped UPDATE: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`). This can mutate a row belonging to a different tenant if IDs collide or if row.id is attacker-controlled, breaking tenant isolation.
  - _Fix:_ Constrain the UPDATE to the owning tenant. Use the row's tenant value or the caller's tenant in the WHERE clause. Example: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}` (or AND tenant_id = ${tenantId} where appropriate). Also validate row.tenantId before running any mutation and fail-closed if tenantId cannot be derived.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but does not include tenant_id. This can delete chunks belonging to other tenants sharing the same collection_id/doc_path.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}) so the operation only affects the caller's tenant.
- **server/embeddings.ts:809** — Fallback path in storeEmbeddingVec performs an UPDATE using `sql.raw(table)` without any tenant constraint: `UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}`. That is an unscoped write against a tenant-scoped table (memory_entries or agent_knowledge) and can modify rows across tenants.
  - _Fix:_ Never perform writes against tenant-scoped tables without an explicit tenant_id filter. Remove the unscoped fallback. Require a positive tenantId parameter and include `AND tenant_id = ${tenantId}` in the WHERE clause (or otherwise run under a documented admin maintenance pathway that explicitly documents and bounds cross-tenant intent). Also avoid building table identifiers via sql.raw from untrusted inputs; keep an allowlist and map to identifiers server-side.
- **server/embeddings.ts:845** — backfillEmbeddingVecs selects from memory_entries without tenant scoping (`SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200`) and then performs unscoped UPDATEs: `UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}`. Both the SELECT and UPDATE operate across all tenants and perform writes with no tenant_id constraint.
  - _Fix:_ Do not read or write tenant-scoped tables without an explicit tenant filter. Refactor backfillEmbeddingVecs to iterate per-tenant (require tenantId) or to record and enforce an admin execution context that uses a documented ADMIN_TENANT_ID and strong access controls. Ensure every UPDATE includes `AND tenant_id = ${tenantId}`.
- **server/embeddings.ts:861** — backfillEmbeddingVecs also selects agent_knowledge rows without tenant filtering and updates them with `UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}` — unscoped reads and writes across tenant-scoped table agent_knowledge.
  - _Fix:_ Same fix as above: run backfill per-tenant or under a documented admin path, and include `tenant_id = ${tenantId}` in both SELECT and UPDATE statements. Avoid unscoped UPDATE/SELECT on tenant-scoped tables; require tenant context.
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot issues a SELECT from agent_runs without constraining tenant_id. This reads running/pending agent_runs across all tenants and then materializes them into in-memory taskStateStore, violating the invariant that every read against a tenant-scoped table must be filtered by the caller's tenant.
  - _Fix:_ Restrict the query to a single tenant or otherwise enforce tenant scoping. Options: (a) change rehydrateTaskStateOnBoot to accept an explicit tenantId and add WHERE tenant_id = ${tenantId} to the SELECT; (b) iterate over an authorized list of tenant IDs and run the query per-tenant inside a parameterized query; or (c) if global rehydration is intended for an admin-only process, gate the function behind an explicit, auditable admin-only path and ensure the code documents and enforces that fact. In all cases, ensure the SQL uses parameterized tenant_id filtering (e.g. WHERE tenant_id = ${tenantId}) and never returns rows from other tenants into runtime memory.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` (SET status = 'archived_stale' WHERE id = ANY(...)) has no tenant_id constraint — this can modify rows for other tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant, e.g. add AND tenant_id = ${tenantId}, or join against a SELECT that enforces tenant_id. If this is intended as an admin-wide action, make that explicit and run it per-tenant with an explicit admin context; do not perform unbounded cross-tenant updates.
- **server/health-audit.ts:368** — UPDATE on tenant-scoped table `heartbeat_tasks` (SET enabled = false WHERE id = ANY(...)) has no tenant_id constraint — this may disable tasks for other tenants.
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. AND tenant_id = ${tenantId}) or otherwise ensure updates are performed only on rows owned by the intended tenant. If global admin disabling is required, explicitly require an admin tenant context and iterate per-tenant to avoid accidental cross-tenant writes.
- **server/heartbeat-task-scheduler-storage.ts:43** — claimDueHeartbeatTasksAcrossTenants() runs UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... without a tenant_id equality check. This allows claiming/updating heartbeat_task rows across tenants based solely on id, enabling cross-tenant mutation.
  - _Fix:_ Require and pass the task's tenant id into this routine and include AND tenant_id = ${tenantId} in the WHERE clause (or use a single atomic UPDATE ... WHERE id = ${id} AND tenant_id = ${tenantId} RETURNING id). Alternatively, perform the claim via a scheduler-only API that verifies caller privileges before modifying cross-tenant rows.
- **server/heartbeat.ts:2681** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint. This mutates a tenant-scoped row without verifying tenant ownership, risking modification of another tenant's task.
  - _Fix:_ Use the storage API that accepts tenantId or add an explicit tenant guard to the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}). Ensure the code asserts/derives the tenant from a trusted source (assertTenantContext) before performing mutations.
- **server/heartbeat.ts:3155** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat path) executes without tenant_id scoping. This writes tenant-scoped state (approval_status) without an ownership check.
  - _Fix:_ Pass the tenantId into the update and include AND tenant_id = ${tenantId} in the WHERE clause, or call storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }, tenantId) so the storage layer enforces tenant ownership. Always derive tenantId from an asserted/trusted context (assertTenantContext) before updating tenant-scoped rows.
- **server/lib/scheduled-post-runner.ts:61** — The CTE+UPDATE that picks and locks due rows from `scheduled_posts` executes without any tenant_id predicate — it selects/updates tenant-scoped rows across all tenants (SELECT id FROM scheduled_posts ... FOR UPDATE SKIP LOCKED / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id).
  - _Fix:_ If this runner is intended to operate per-tenant, add an explicit tenant_id filter to the CTE (and the UPDATE WHERE) so every row is processed only in the correct tenant context (e.g. WHERE tenant_id = ${tenantId}). If the cross-tenant sweep is intentional (system/admin runner), make the privileged admin intent explicit and auditable: require a documented admin-only code path, perform explicit authorization checks, and/or iterate tenants and run per-tenant transactions (or use an administrative API that sets app.current_tenant) so tenant-scoped invariants remain explicit and auditable.
- **server/memory-graph.ts:130** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} executes without a tenant_id predicate, allowing a category row belonging to another tenant to be modified.
  - _Fix:_ Constrain the update to the caller's tenant. For example: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Or call the existing refreshCategoryCount(catId, tenantId) helper which applies the tenant predicate.
- **server/memory-graph.ts:708** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} executes without a tenant_id predicate, so it may modify a memory entry belonging to a different tenant.
  - _Fix:_ Add the tenant filter to the UPDATE. For example: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Alternatively use a tenant-scoped helper (or withTenantTx / Drizzle .where(eq(table.tenantId, tenantId))) to ensure the row belongs to the caller's tenant.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — write against tenant-scoped table using only id without tenant_id constraint. This can modify another tenant's row if ids collide or if subs were read without tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause for writes (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} or WHERE id = ${sub.id} AND tenant_id = ${tenantId}). Always enforce tenant_id on UPDATE/DELETE to prevent cross-tenant modifications.
- **server/oauth-subscriptions.ts:966** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — write without tenant_id constraint (same pattern repeated).
  - _Fix:_ As above, require tenant_id in the WHERE clause for any UPDATE/DELETE on tenant-scoped tables. Use the tenant value from the row or the current tenant context to scope the update.
- **server/oauth-subscriptions.ts:981** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — write against tenant-scoped table lacking tenant_id constraint (another un-scoped update).
  - _Fix:_ Ensure every destructive/modifying statement includes tenant_id (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} or equivalent parameterized tenant check). Avoid relying on id alone for isolation.
- **server/recurring-messages.ts:307** — UPDATE agent_knowledge SET ... WHERE id = ${r.id} — write to tenant-scoped table without including tenant_id in the WHERE clause. This can result in modifying another tenant's row if ids collide or if the row context changes between read and update.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}, or otherwise re-validate ownership before the UPDATE. For global scanner code, always bind the tenant id from the previously fetched row into the WHERE clause when performing writes to tenant-scoped tables.
- **server/reference-learner.ts:296** — UPDATE memory_entries SET ... WHERE id = ${storedId} — write to tenant-scoped table memory_entries without tenant_id constraint. Although storedId was selected earlier under a tenant filter, the UPDATE by id alone can allow cross-tenant modification in races or misuse.
  - _Fix:_ Constrain updates to the tenant context, e.g. UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId}. Prefer double-checking ownership before mutations or use parameterized APIs that include tenant in the predicate.
- **server/research-engine.ts:171** — cleanupZombieSessions performs an UPDATE on research_sessions without constraining by tenant_id. This will mark running sessions as completed across all tenants (cross-tenant mutation).
  - _Fix:_ Restrict the UPDATE to a specific tenant or iterate per-tenant. Add a tenantId parameter (or enumerate tenants) and include AND tenant_id = ${tenantId} in the WHERE clause; or otherwise compute and apply the UPDATE per-tenant so no single call mutates rows across tenants. Ensure any dynamic IN lists remain parameterized (sql.join) as already done.
- **server/routes.ts:4653** — INSERT into project_notes does not include tenant_id. The code inserts a note for a project (INSERT INTO project_notes (project_id, note, author) VALUES (...)) without setting tenant_id, allowing a conversation/request scoped to tenant A to create notes that end up associated with projects from other tenants.
  - _Fix:_ Include tenant_id in the INSERT and ensure the project belongs to the caller's tenant. For example, verify ownership earlier (SELECT project_id FROM conversations WHERE id = ... AND tenant_id = ${tenantId}) then INSERT with tenant_id: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, use a storage layer helper that requires tenantId and validates project ownership before persisting.
- **server/routes/admin-zombie-detector.ts:41** — Query against tenant-scoped table agent_trace_spans has no tenant_id constraint — CTE `used` selects from agent_trace_spans without filtering by tenant, allowing cross-tenant data exposure.
  - _Fix:_ Constrain the CTE to the caller's tenant (e.g. add AND tenant_id = ${tenantId} in the WHERE) or otherwise limit the query to the resolved tenant. Wrap tenantId as a parameterized value (sql`... ${tenantId} ...`) and ensure the transaction uses the caller's tenant context.
- **server/routes/admin-zombie-detector.ts:56** — Query against tenant-scoped table conversations has no tenant_id constraint — CTE `used` selects from conversations without filtering by tenant, allowing cross-tenant aggregation.
  - _Fix:_ Add a tenant filter to the CTE (e.g. WHERE persona_id IS NOT NULL AND tenant_id = ${tenantId} AND updated_at > ...). Pass tenantId as a parameter to the query (sql`... ${tenantId} ...`) so results are restricted to the caller's tenant.
- **server/sculptor.ts:106** — UPDATE to sculptor_sessions performed without constraining by tenant_id (WHERE id = ${session.id} only) — write to a tenant-scoped table missing tenant filter.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the row belongs to the caller's tenant before updating.
- **server/sculptor.ts:113** — UPDATE to sculptor_sessions sets conversation_id WHERE id = ${session.id} without tenant_id constraint — write to a tenant-scoped table missing tenant filter.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}), or re-check the session row's tenant before performing the update.
- **server/sculptor.ts:168** — UPDATE to sculptor_sessions (status/result/tool_calls_count/tokens_used/completed_at) uses WHERE id = ${sessionId} with no tenant_id constraint — allows modifying another tenant's session by id.
  - _Fix:_ Include tenant check in the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or perform the update within an earlier-validated tenant-scoped transaction to ensure isolation.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result WHERE id = ${sessionId} has no tenant_id constraint — write to a tenant-scoped table missing tenant filter.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or verify ownership of session prior to the write.
- **server/second-opinion.ts:502** — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without scoping by tenant_id. The function lacks a tenant parameter, so it may settle/modify ledger rows for other tenants by id alone.
  - _Fix:_ Require and pass tenantId into settleFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause. Alternatively, fetch and verify the reservation row belongs to the expected tenant before updating.
- **server/storage.ts:979** — UPDATE memory_entries SET status = 'archived' runs without a tenant_id predicate — a write to a tenant-scoped table affecting all tenants.
  - _Fix:_ Scope the UPDATE by tenant (e.g. add requiredTenantScope(memoryEntries.tenantId, tenantId) or pass an explicit tenantId parameter). If this is intended as a global maintenance job, require an explicit admin callsite with the ALL_TENANTS sentinel and proper authorization before performing cross-tenant writes.
- **server/storage.ts:995** — UPDATE memory_entries SET status = 'archived' (archiveStaleMemories) executed without tenant_id filter — performing cross-tenant writes.
  - _Fix:_ Accept and validate a tenantId (or require explicit ALL_TENANTS sentinel) and apply requiredTenantScope(memoryEntries.tenantId, tenantId) in the WHERE clause. Ensure callers are explicitly privileged for platform-wide operations.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table felix_proposals is executed without constraining by tenant_id (WHERE id = ${proposalId} AND args_embedding IS NULL). This can overwrite another tenant's row if ids collide or be abused to stamp embeddings cross-tenant.
  - _Fix:_ Include tenant isolation in the UPDATE. Either accept a tenantId parameter to stampArgsEmbedding and add AND tenant_id = ${tenantId} to the WHERE, or resolve the proposal row first with tenant context and use that tenant_id when updating. Example: WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped custom_tools filters only by name (eq(customTools.name, parsed.name)) and omits tenant_id, allowing cross-tenant discovery or conflict checks across tenants.
  - _Fix:_ Scope the lookup to the creating tenant. Add eq(customTools.tenantId, tenantId) to the WHERE so you only check for existing tools within that tenant (or otherwise pass/derive tenantId in this context).
- **server/tool-learning.ts:155** — SELECT by custom_tools.id is performed without a tenant_id predicate (where(eq(customTools.id, inserted.id))). This reads a tenant-scoped table row without explicit tenant scoping.
  - _Fix:_ Include tenant scoping in the SELECT, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Ensure tenantId is available/propagated into this code path before re-reading rows.
- **server/tool-learning.ts:291** — UPDATE of custom_tools (setting implementation) uses only the row id in the WHERE clause (.where(eq(customTools.id, tool.id))) and omits tenant_id, performing a tenant-scoped write without tenant filtering.
  - _Fix:_ Constrain updates to the tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Propagate tenantId through tryAndRevert so modifications always include the tenant guard.
- **server/tool-learning.ts:316** — DELETE on custom_tools in revertToolRow uses only the row id (where(eq(customTools.id, tool.id))) and lacks a tenant_id filter, risking deletion of a row outside the caller's tenant.
  - _Fix:_ Add tenant scoping to the DELETE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure revertToolRow receives/derives the tenantId for safe deletion.
- **server/tool-learning.ts:365** — UPDATE that increments usageCount on custom_tools uses only eq(customTools.id, tool.id) in the WHERE and omits tenant_id, performing a tenant-scoped write without tenant filtering.
  - _Fix:_ Constrain the UPDATE by tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure executeCustomTool has the tenantId available and uses it for all writes against tenant-scoped tables.
- **server/webhookHandlers.ts:557** — Writes to the `tenants` row (UPDATE tenants SET plan=..., stripe_customer_id=..., ...) use tenant id taken directly from untrusted Stripe webhook metadata (session.metadata.tenantId) and mutate the tenant record without a robust ownership check. An attacker could spoof metadata to change another tenant's plan or bind a Stripe customer to a tenant.
  - _Fix:_ Do not trust metadata. Before mutating a tenant record identified by metadata.tenantId, verify ownership: require that the Stripe customer/subscription in the webhook matches an existing binding on that tenant (e.g. tenants.stripe_customer_id === session.customer or tenants.stripe_subscription_id === subscriptionId). If there is no existing binding, require an out-of-band admin confirmation or other strong proof of ownership (or refuse automatic first-bind in high-risk flows). Fail closed (throw) on provenance mismatches rather than proceeding to UPDATE the tenant.

### HIGH (137)

- **server/ab-optimizer.ts:110** — runDueAbExperiments issues a SELECT from experiments without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant and run the logic within a tenant-scoped transaction. Ensure any global sweep explicitly validates tenant ownership before making writes on behalf of a tenant.
- **server/agent-activity.ts:122** — completeActivity issues an UPDATE on agent_activity without a tenant_id constraint (UPDATE agentActivity ... WHERE id = activityId). This can modify a row belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId} OR use eq(agentActivity.tenantId, tenantId) with Drizzle). Always require and check tenantId for per-row updates.
- **server/agent-eval.ts:221** — runEval updates agent_evals by id only (UPDATE agent_evals ... WHERE id = ${evalId}) after running an eval. There is no tenant_id constraint on the UPDATE, allowing cross-tenant writes if evalId is guessed or rebinding occurs.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}). Validate evalId ownership before modifying the row.
- **server/agentic-engines.ts:686** — autoApplyEligibleInsights issues an UPDATE on ai_insights that may run without a tenant scope when tenantId is omitted: UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope}. When tenantScope is empty this updates across tenants.
  - _Fix:_ Require an explicit tenantId or, if global sweep behavior is intended, iterate per-tenant and run tenant-scoped updates inside a transaction. Never execute mass UPDATE/DELETE/SELECT on tenant-scoped tables without an explicit tenant_id constraint or validated per-tenant loop.
- **server/agentic-engines.ts:500** — retryPendingMinervaRouting SELECTs ai_insights rows without a tenant constraint (SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ... LIMIT 50). This reads tenant-scoped insight rows across tenants.
  - _Fix:_ If this is a system-wide sweep, iterate tenants explicitly and SELECT rows scoped to each tenant_id; otherwise add WHERE tenant_id = ${tenantId}. After selecting rows for a tenant, always perform updates with tenant_id in the WHERE clause.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agent_approvals without scoping by tenant_id (UPDATE agentApprovals SET status='expired' WHERE status='pending' AND expiresAt < now()). This updates pending approvals across all tenants in one statement.
  - _Fix:_ Scope the bulk expiry per-tenant (e.g. accept tenantId and include WHERE tenant_id = ${tenantId}) or perform a tenant-by-tenant sweep: select tenants, then update agent_approvals WHERE tenant_id = ${tid} AND ... inside a transaction. Ensure subsequent per-row actions validate tenant ownership before updating linked rows.
- **server/agentic/department-budgets.ts:143** — runBudgetEnforcement executes a cross-tenant query: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` has no tenant_id constraint. This reads a tenant-scoped table across all tenants without an explicit admin/authorization guard, violating the requirement that tenant-scoped reads be constrained to the caller's tenant.
  - _Fix:_ Either restrict this query to a specific tenant by adding `WHERE tenant_id = ${tenantId}` (and call per-tenant), or make the function explicitly an ADMIN-only operation with an explicit admin context check and audit trail. If a cross-tenant sweep is required, ensure it only runs under a well-audited, privileged service account (documented in code) rather than as an unguarded background job, or iterate tenants from a vetted tenant list and constrain subsequent work by tenant_id on each iteration.
- **server/agentic/resume-worker.ts:65** — SELECT query reads tenant-scoped tables (agent_runs, agent_approvals) without constraining to a specific tenant_id — the query scans rows across all tenants.
  - _Fix:_ Constrain the SELECT to a caller/operational tenant (e.g. add WHERE r.tenant_id = ${tenantId}) or, if the intent is to process tenants one-by-one, iterate tenants and run a per-tenant query binding tenant_id. If an operator/global scan is required, centralize and harden that as an explicit admin-only job and document the exception; never rely on unbounded cross-tenant reads in ordinary request paths.
- **server/agentic/task-forces.ts:40** — createTaskForce inserts caller-supplied foreign IDs (project_id and persona_ids) into task_forces without verifying those referenced rows belong to the caller's tenant. personaIds are mass-inserted via personaIdArrayLiteral and projectId is accepted as-is.
  - _Fix:_ Validate referenced foreign IDs before persisting. For projectId: SELECT the project by id and confirm its tenant_id === p.tenantId (or reject/null it). For personaIds: SELECT the persona rows WHERE id IN (...) AND tenant_id = p.tenantId, and either reject if any are missing or filter to only those owned by the tenant. Do not trust caller-supplied foreign ids without an ownership check.
- **server/agentic/wake-scheduler.ts:66** — scheduleWake inserts persona_id, conversation_id, project_id provided by the caller into agent_wake_schedules without validating that the referenced conversation/project/persona belong to the tenant.
  - _Fix:_ Before inserting, verify each optional foreign id (personaId, conversationId, projectId) references a row owned by the same tenant. For example, SELECT 1 FROM conversations WHERE id = ${p.conversationId} AND tenant_id = ${p.tenantId} LIMIT 1 (and similarly for projects/personas) and reject or null-out invalid/foreign ids. Never persist foreign ids from the request without an explicit ownership check.
- **server/auto-asset-capture.ts:184** — Backfill query selects conversations with an existence check on messages using a subquery: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 — the subquery does not include a tenant_id filter. This reads a tenant-scoped table (messages) without constraining by tenant_id.
  - _Fix:_ Add tenant scoping to the subquery, e.g. AND tenant_id = c.tenant_id (or AND tenant_id = ${c.tenant_id} when parameterized), so the COUNT only considers messages belonging to the same tenant.
- **server/auto-asset-capture.ts:207** — Selecting messages: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' — query omits tenant_id constraint. This reads the tenant-scoped messages table without scoping to the tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${c.tenant_id}) or perform the SELECT via the conversation/tenant context already established so every read includes tenant_id = <caller tenant>.
- **server/auto-project.ts:135** — Querying messages for a conversation: SELECT role, LEFT(content, 300) ... WHERE conversation_id = ${conversationId} ORDER BY id ASC LIMIT 20 — the query lacks an explicit tenant_id constraint. Reads against the tenant-scoped messages table must include tenant_id filtering.
  - _Fix:_ Add tenant scoping to the messages query (AND tenant_id = ${tenantId}) or join to conversations with tenant_id to ensure only this tenant's messages are read.
- **server/auto-transcript.ts:46** — Selecting messages for transcript: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ORDER BY created_at ASC — the query omits tenant_id. This reads the tenant-scoped messages table without constraining to the caller's tenant.
  - _Fix:_ Constrain the query by tenant_id (AND tenant_id = ${tenantId}) or join to conversations on id and tenant_id so the messages read are scoped to the same tenant.
- **server/auto-transcript.ts:128** — Backfill conv query contains an existence/count check that uses: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 — that subquery does not include tenant_id, so it reads the tenant-scoped messages table without tenant scoping.
  - _Fix:_ Add tenant scoping to the subquery (e.g. WHERE conversation_id = c.id AND tenant_id = c.tenant_id) so the count only considers messages for the same tenant.
- **server/auto-transcript.ts:160** — In the 'alsoLinked' backfill query the subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 is used without tenant_id constraint, causing an unscope read from the tenant-scoped messages table.
  - _Fix:_ Include tenant scoping in the subquery (e.g. AND tenant_id = c.tenant_id) or rewrite the existence check to join messages with a tenant check to ensure the condition only considers this tenant's messages.
- **server/ceo-orchestrator.ts:661** — Unscoped read from tenant-scoped table: db.select().from(personasTable) loads all persona rows with no tenant_id filter. This can expose or mix persona records across tenants.
  - _Fix:_ Scope the query to the plan owner: filter by tenant_id (e.g. db.select().from(personasTable).where(eq(personasTable.tenant_id, plan.tenantId)) or equivalent Drizzle .where call). If you only need a subset, select only required rows and/or limit columns to reduce blast radius.
- **server/channel-routing.ts:30** — SELECT joins tenant-scoped personas table without any tenant_id constraint (SELECT cr.*, p.name as persona_name FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ...). This reads persona rows across tenants and can leak or mix tenant data.
  - _Fix:_ Make channel routing tenant-aware: either add tenant_id to channel_routes and accept a tenantId parameter for listChannelRoutes, then include WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}; or, if channel_routes is intentionally global, ensure persona_id refers to a global persona table or otherwise resolve persona names with an explicit tenant-scoped lookup (JOIN ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}). Do not query personas without constraining tenant_id.
- **server/chat-engine.ts:1624** — buildMemorySection declares tenantId with a default of 1 (tenantId: number = 1). This creates a silent admin/default tenant fallback and can cause tenant-scoped DB reads to run under tenant 1 if callers omit the tenant, leading to cross-tenant data exposure.
  - _Fix:_ Do not default tenantId to an admin tenant. Make tenantId required (no default) or default to null and fail-closed at runtime. Validate the tenantId is a positive integer before any DB calls and ensure all downstream calls use the caller's tenant. Audit callers to confirm they always pass the proper tenantId or adjust the signature so omission is a type/ runtime error.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id.
  - _Fix:_ Add an explicit tenant constraint (e.g. AND tenant_id = ${tenantId}) or include the tenant check in the JOIN (e.g. join projects and require projects.tenant_id = ${tenantId}) to ensure the row belongs to the caller's tenant.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id.
  - _Fix:_ Include an explicit tenant filter (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or otherwise ensure the tenant ownership is enforced in the same SQL statement.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' performed without a tenant_id filter (cross-tenant scan in scanAndEscalate: `SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`). This reads tenant data across all tenants and may expose tenant-scoped rows to contexts not authorized for those tenants.
  - _Fix:_ If this cross-tenant scan is intentional for a platform-admin feature, ensure it's guarded by strict admin-only authorization and auditing. Otherwise, restrict the query by tenant (e.g. add `AND tenant_id = ${tenantId}`) or enumerate tenant IDs first and run tenant-scoped queries per-tenant to preserve tenant isolation. Log and document the admin-only exception if truly required.
- **server/data-protection.ts:149** — Background purge selects from tenant-scoped table `conversations` without restricting by tenant_id (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants, creating an opportunity to leak cross-tenant information and violates the invariant that tenant-scoped reads must be constrained.
  - _Fix:_ Restrict the initial SELECT to a single tenant at a time (e.g. iterate tenant IDs and query WHERE tenant_id = <tenant> AND deleted_at < ...), or run the purge inside an explicit admin-only context (and document that it is a privileged operation). Alternatively use per-tenant transactions (withTenantTx) or otherwise ensure the caller is a verified platform-admin and the read is auditable. Do not leave an unconstrained SELECT on a tenant-scoped table.
- **server/delegation-events.ts:26** — Events are stored and keyed solely by conversationId in a shared recentEvents map (convEvents keyed by event.conversationId) without tenant separation. Conversation IDs can collide across tenants, so storing/serving events by conversationId only may mix events from different tenants and leak information.
  - _Fix:_ Include tenant context in the event storage key (e.g. use `${tenantId}:${conversationId}`) or maintain per-tenant maps. Ensure all event emission and retrieval paths always require and validate the tenantId to avoid cross-tenant mixing.
- **server/delegation-events.ts:44** — subscribeToDelegation(conversationId, callback) registers a subscription to events for a conversationId with no tenant check. A caller can subscribe to a conversationId originating from another tenant and receive those events, violating tenant isolation.
  - _Fix:_ Require an explicit tenantId when subscribing (e.g. subscribeToDelegation(conversationId, tenantId, callback)) and enforce that incoming events are filtered to only deliver events whose tenantId matches the subscriber. Alternatively include the tenant in the event channel name (e.g. `delegation:${tenantId}:${conversationId}`) so subscriptions are tenant-scoped.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken performs a SELECT on dfy_intake_forms by token with no tenant_id constraint (returns a tenant-scoped row without scoping to caller tenant).
  - _Fix:_ Constrain the lookup to the caller's tenant (e.g. add AND eq(dfyIntakeForms.tenantId, tenantId) or accept a tenantId parameter and include eq(dfyIntakeForms.tenantId, tenantId)). If the token is intentionally global, document and enforce that only a server/admin context can call this path; otherwise require tenant-scoped filtering to prevent cross-tenant access.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates by id alone with no tenant_id constraint. An attacker or bug could update another tenant's chunk by ID.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update using the same tenant-scoped SELECT result (passing tenantId) to ensure you only update rows owned by the caller's tenant.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages ... (user messages) executes against the messages table with no tenant_id filter. messages appears tenant-scoped elsewhere; this reads cross-tenant data for traffic gating.
  - _Fix:_ If this logic is intended to be tenant-scoped, accept/derive a tenantId and add WHERE tenant_id = ${tenantId}. If this is intentionally global/platform-wide, call out that explicitly and restrict access to an admin/global context rather than running tenant-scoped queries without a tenant filter.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance ... has no tenant_id constraint. tool_performance is tenant-scoped elsewhere, so this aggregates across tenants and can mix signals.
  - _Fix:_ Make this query tenant-scoped by filtering on tenant_id = ${tenantId} (or otherwise ensure it runs in an explicit global/admin context). Avoid reading tenant-scoped tables without an explicit tenant filter.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance (getEverInvokedTools) runs with no tenant_id filter and will return tools across all tenants.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) if the operation should be per-tenant. If a global view is required, restrict the API so only a well-audited admin/global operation can execute it and document the reason.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (getActiveTools14d) executes without tenant_id filtering, returning cross-tenant results despite tool_performance being tenant-scoped elsewhere.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause to scope activity to a single tenant, or ensure this code is explicitly an admin/global operation and cannot be called in a tenant context.
- **server/email.ts:370** — Query checks inbox_messages by message_id without constraining tenant_id: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1`. inbox_messages is tenant-scoped (INSERTs include tenant_id), so this global existence check can cause cross-tenant dedupe/skips and data mis-attribution.
  - _Fix:_ Include a tenant restriction (or inbox_id) in the WHERE clause: e.g. `WHERE tenant_id = ${tenantId} AND message_id = ${msgId}` (or `AND inbox_id = ${inboxId}` if appropriate). Pass tenant context into the polling flow so each poll only considers that tenant's rows.
- **server/email.ts:415** — Backfill query reads inbox_messages without tenant scoping: `SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...)` — no tenant_id or inbox_id filter. This potentially processes rows across all tenants.
  - _Fix:_ Restrict the backfill to a specific tenant or inbox (e.g. add `WHERE tenant_id = ${tenantId}` or `AND inbox_id = ${inboxId}`). If the intent is to run an admin/global job, make that explicit and use a documented admin/maintenance path (and/or ADMIN_TENANT_ID) rather than an unscoped query.
- **server/embeddings.ts:259** — Use of sql.raw with an interpolated vector literal derived from the request/model embedding: `embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}`. The vec string originates from generateEmbedding(query) (request-derived / untrusted). Using string-interpolated sql.raw with request-derived content raises injection and isolation risks.
  - _Fix:_ Avoid sql.raw interpolation of request/model-derived strings. Use parameterized bindings and safe casting (pass the embedding as a parameter and cast on the DB side), or a dedicated safe helper that validates and binds the vector without injecting raw text. If raw interpolation is unavoidable, perform strict validation and document the trust boundary, but prefer parameterization to eliminate injection risk.
- **server/embeddings.ts:269** — ORDER BY uses sql.raw with the same interpolated vector literal: `ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}` — raw interpolation of a request-derived value.
  - _Fix:_ Same as above: replace string-interpolated sql.raw with a parameterized approach (bind the embedding as a parameter) or otherwise avoid injecting untrusted/model-derived text into raw SQL.
- **server/embeddings.ts:585** — Vector retrieval for agent_knowledge uses sql.raw with an interpolated vector literal derived from model embeddings: `1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) AS similarity` and `ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}`. This injects a request/model-derived value into raw SQL.
  - _Fix:_ Use parameterized queries / prepared statements rather than sql.raw interpolation. Pass the embedding as a bound parameter and cast it safely, or use a DB-side helper that accepts an array parameter to construct the vector, avoiding direct string interpolation.
- **server/embeddings.ts:814** — Backfill selects agent_knowledge rows without tenant scoping: `SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50`. This reads tenant-scoped rows across all tenants without an application-level tenant filter.
  - _Fix:_ Make backfill operate per-tenant (accept a tenantId and add `WHERE tenant_id = ${tenantId}`) or run under a documented admin/maintenance path that explicitly uses an admin tenant constant and access control. Avoid ad-hoc global selects over tenant-scoped tables.
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scrapedPages' omits tenant_id constraint in WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Fix:_ Add an explicit tenant filter to the UPDATE to enforce tenant isolation. Example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include tenant scoping in UPDATE/DELETE WHERE clauses even if you previously selected the row.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' omits tenant_id constraint in WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Fix:_ Add an explicit tenant filter to the UPDATE to enforce tenant isolation. Example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include tenant scoping in UPDATE/DELETE WHERE clauses even if you previously selected the row.
- **server/google-drive.ts:866** — SELECT from tenant-scoped table 'projects' uses only project id in WHERE (where(eq(projects.id, projectId))) — SQL lacks tenant_id constraint.
  - _Fix:_ Constrain the SELECT to the caller's tenant in SQL rather than relying solely on a later in-code check. Example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). This prevents accidental cross-tenant reads.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' omits tenant_id constraint in WHERE clause (.where(eq(projects.id, projectId))).
  - _Fix:_ Include the tenant filter in the UPDATE WHERE clause to enforce tenant-scoped writes. Example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Do not rely only on earlier JS-level checks; the SQL must be scoped.
- **server/google-drive.ts:1476** — SELECT from tenant-scoped table 'projects' (resolving params.projectId) omits tenant_id in the SQL WHERE (where(eq(projects.id, params.projectId))).
  - _Fix:_ Add tenant scoping at the SQL level: .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))). Always perform ownership checks via SQL filters before further processing or Drive calls.
- **server/google-drive.ts:1721** — SELECT from tenant-scoped table 'projects' used to verify project ownership does not include tenant filter in SQL (where(eq(projects.id, params.projectId))).
  - _Fix:_ Constrain this SELECT by tenant in the SQL so the DB only returns rows belonging to the caller: .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))). Prefer SQL-level tenant scoping over post-query JS checks.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` is executed without any tenant_id constraint (reads across all tenants).
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly document/implement an admin-only sweep that iterates tenants and runs tenant-scoped queries per-tenant. Never perform broad tenant-scoped reads without an explicit, trusted tenant context.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` is executed without any tenant_id constraint (reads tasks across all tenants).
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) or perform the scan in a controlled admin routine that iterates tenants and applies tenant-scoped predicates. Avoid returning or acting on tenant-scoped rows without restricting to the caller's tenant.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules is executed without any tenant_id constraint. This reads tenant-scoped rows across all tenants in an un-scoped query.
  - _Fix:_ Either iterate per-tenant using a vetted cross-tenant scheduler boundary or add an explicit tenant filter (WHERE tenant_id = ${tenantId}) before selecting. If the code is intended to run cross-tenant, centralize and review the implementation (and keep it isolated from request handlers) so the cross-tenant read is deliberate and auditable.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executes without constraining tenant_id. heartbeat_tasks is tenant-scoped, so this update can modify rows belonging to other tenants if id is ambiguous or misused.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call the storage layer that accepts tenantId (e.g. storage.updateHeartbeatTask) to perform the update under asserted tenant context (use assertTenantContext where appropriate).
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} has no tenant_id constraint. This mutates a tenant-scoped table without asserting ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${t.tenantId}) or use the storage helper that enforces tenant ownership to perform the model normalization update under the correct tenant context.
- **server/heartbeat-task-scheduler-storage.ts:11** — getDueHeartbeatTasksAcrossTenants() issues SELECT * FROM heartbeat_tasks ... without any tenant_id constraint. This returns tenant-scoped rows across all tenants from a function that is not request-scoped.
  - _Fix:_ If this function is intentionally scheduler-level and must enumerate across tenants, mark it clearly and restrict its use to scheduler-only code paths. Otherwise add an explicit tenant filter parameter and apply WHERE tenant_id = ${tenantId}. Consider returning only metadata needed for cross-tenant operations (e.g. tenant_id + id) rather than full tenant-scoped rows if you need to minimize exposure.
- **server/heartbeat.ts:260** — SELECT * FROM event_log WHERE status = 'pending' ... executes without a tenant_id constraint. event_log rows are tenant-scoped (tenant_id exists in the row interface), so this is a cross-tenant read of pending events.
  - _Fix:_ Either scope the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or implement a dedicated scheduler-only event-processor that explicitly documents and centralizes cross-tenant event consumption. Avoid returning tenant-scoped event rows into code paths that assume a single caller tenant without asserting it.
- **server/job-queue.ts:160** — UPDATE on tenant-scoped table `agent_jobs` performs a global claim without scoping to a tenant: the UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) will select and move pending jobs for all tenants into 'running'.
  - _Fix:_ Scope claims to a tenant (e.g. add WHERE tenant_id = ${tenantId} to the inner SELECT / outer UPDATE) or require an explicit tenant argument for per-tenant workers. If the worker must operate globally, enforce a strict admin-only boundary and add per-row tenant checks before performing any tenant-affecting side-effects so tenant-scoped mutations cannot be widened by a malicious caller.
- **server/job-queue.ts:195** — UPDATE on tenant-scoped table `agent_jobs` reclassifies expired leases to `failed_terminal` without any tenant_id filter. Similarly, the second UPDATE that requeues expired leases is unscoped. These mutations affect rows across all tenants.
  - _Fix:_ Include tenant scoping in these UPDATEs (or iterate per-tenant) so reclaimed/terminal transitions are applied only within the intended tenant context. If global reclaim is required, run it in a trusted, admin-only context and log/authorize the action accordingly.
- **server/job-queue.ts:335** — cancelJob updates `agent_jobs` by id without restricting by tenant_id. Cancelling by id alone can mutate another tenant's job if misused.
  - _Fix:_ Require and validate the caller's tenantId (AND include tenant_id = ${tenantId} in the WHERE clause) or otherwise enforce an admin-only guard before allowing id-only cancellation. Use lease/attempt fencing as an additional safety check.
- **server/lib/agent-trace.ts:125** — UPDATE against tenant-scoped table `agent_trace_spans` performs WHERE span_id = ${spanId} with no tenant_id constraint — write may affect another tenant's rows.
  - _Fix:_ Constrain the UPDATE by tenant. For example, have persistSpanClose include/derive the current tenantId and use WHERE span_id = ${spanId} AND tenant_id = ${tenantId}. Update all callsites accordingly (or call currentTenantId() inside persistSpanClose) so every UPDATE/DELETE/SELECT on agent_trace_spans is guarded by tenant_id = <caller tenant>.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped table `projects` is performed without constraining to a specific tenant (no WHERE tenant_id = ...). This query aggregates counts across all tenants: `SELECT tenant_id, count(*)... FROM projects WHERE tags && ... GROUP BY tenant_id`.
  - _Fix:_ Avoid cross-tenant scans. Scope the aggregation to only the tenants you intend to backfill (e.g. add WHERE tenant_id = ${tenantId} or WHERE tenant_id = ANY(${tenantIdList}) if you compute per-tenant), or run the check per-target-tenant inside the import loop. Ensure every read over a tenant-scoped table is constrained to the caller's tenant or an explicit, auditable admin tenant list.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` performed without constraining by tenant_id (lookup by key_prefix only). This reads a tenant-scoped row without an application-level tenant filter, violating the invariant that all reads/writes must be constrained to the caller's tenant.
  - _Fix:_ Resolve the presented key hash first and restrict the DB lookup to only rows that actually match the computed hash (or otherwise ensure the query cannot return rows for other tenants). For example, compute expectedHash = sha256(presented) in-app and change the query to: SELECT id, tenant_id, scopes, revoked_at FROM mcp_api_keys WHERE key_prefix = ${prefix} AND key_hash = ${expectedHash} AND revoked_at IS NULL LIMIT 1. This way the DB will only return a row when the full key matches, preventing unconstrained reads across tenants. Alternatively, redesign the lookup so the tenant is determined in a tenant-constrained lookup (or make key_prefix globally unique and ensure the SQL still includes tenant_id or a key_hash equality guard). Always parameterize inputs and avoid returning rows for other tenants.
- **server/lib/outcome-promotion.ts:158** — Global SELECT from tenant-scoped table deliverable_grade_events without a tenant_id filter (reads across all tenants).
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly document/limit this as an admin/global operation and ensure only an authorized admin context can run it. If the intent is to perform per-tenant scans, iterate tenants and run tenant-scoped queries inside that loop. Avoid unscoped reads of tenant-scoped tables to preserve tenant isolation.
- **server/lib/scheduled-post-runner.ts:320** — Code constructs a Postgres text[] literal string (arrLit = `{...}`) from caller-supplied platforms and injects it into a SQL template. Manually building an array literal from request-derived values risks SQL/formatting injection and brittle parsing.
  - _Fix:_ Avoid manually composing SQL literals from user input. Validate/normalize platform values strictly, then pass the platform array as a parameterized value rather than building a literal. Use Drizzle / parameter binding for arrays (e.g. pass `lowered` as a parameter and cast to text[] on the server side) or use prepared statements that accept JS arrays. If you must serialize, ensure thorough escaping and prefer DB-side array construction APIs to eliminate injection risk.
- **server/lib/system-probes.ts:163** — Unscoped SELECT against tenant-scoped table `messages` (db.select(...).from(messagesTable) has no WHERE tenant_id = ...). This reads cross-tenant data without an application-level tenant filter.
  - _Fix:_ Constrain the query by tenant (e.g. add WHERE tenant_id = ${tenantId} or use the storage API that enforces tenant scoping). If a global/system-wide aggregate is intentionally required, gate this code behind an explicit admin-only path and document the justification; otherwise change to per-tenant counting or an explicitly-authorized admin routine that asserts operator context before reading all tenants.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the row id in WHERE (where(eq(tenantVoiceProfiles.id, existing.id))) and does not include a tenant_id constraint. This can bypass tenant scoping checks.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (for example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, ${opts.tenantId})))) so every mutating statement is explicitly constrained to the caller's tenant. Alternatively, validate ownership of the row before updating and assert the tenant matches the authenticated tenant context.
- **server/minds-engine.ts:501** — SELECT from tenant-scoped table `messages` is executed without an explicit tenant_id constraint on the messages table. The query relies on a JOIN to conversations with c.tenant_id = ${tenantId}, but it does not include m.tenant_id = ${tenantId}. Per the project's invariant every read against a tenant-scoped table must include an explicit tenant_id filter.
  - _Fix:_ Constrain the messages table explicitly to the caller's tenant. For example, verify the conversation belongs to the tenant first (SELECT id FROM conversations WHERE id = ${run.conversationId} AND tenant_id = ${tenantId}) then SELECT messages WHERE conversation_id = ${run.conversationId} AND tenant_id = ${tenantId} ORDER BY ...; or add the tenant filter to the original query (e.g. add AND m.tenant_id = ${tenantId} in the JOIN or WHERE). Ensure any foreign-id (conversation_id) is validated against tenant ownership before reading messages.
- **server/mpeg-engine.ts:1058** — INSERT into project_files does not include tenant_id. Tenant-scoped rows must carry tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT and pass the caller's tenant context (e.g. VALUES (..., ${options.tenantId}, ...)) or otherwise ensure the project_files schema is not tenant-scoped. Keep the assertProjectInTenant check, then add tenant_id to the column list and VALUES to enforce application-level isolation.
- **server/mpeg-engine.ts:1445** — INSERT into project_files does not include tenant_id. Tenant-scoped rows must carry tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT and pass the caller's tenant context (e.g. VALUES (..., ${options.tenantId}, ...)) or otherwise ensure the project_files schema is not tenant-scoped. Keep the assertProjectInTenant check, then add tenant_id to the column list and VALUES to enforce application-level isolation.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads across all tenants.
  - _Fix:_ If this is intended to operate per-tenant, restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND is_active = TRUE) or explicitly iterate a trusted tenant list and run per-tenant queries. Avoid blind cross-tenant SELECTs on tenant-scoped tables.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — reads all tenants' rows.
  - _Fix:_ Add a tenant constraint (WHERE tenant_id = ${tenantId} AND provider = 'google-workspace') or make the cross-tenant intent explicit and special-case with strict admin-only checks and logging. Prefer per-tenant queries.
- **server/oauth-subscriptions.ts:1258** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — reads all tenants' subscriptions (no tenant_id filter).
  - _Fix:_ If this should target a single tenant, add WHERE tenant_id = ${tenantId}. If it is intended as a cross-tenant background job, make the cross-tenant intent explicit and handle per-tenant operations safely (e.g., enumerate tenants and operate per-tenant while always including tenant_id in subsequent writes).
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — no tenant_id filter; reads rows across all tenants.
  - _Fix:_ Restrict to a tenant with WHERE tenant_id = ${tenantId} or explicitly enumerate tenants and operate per-tenant. Never perform un-scoped reads of tenant-scoped tables from request-derived contexts.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw(${lockKey}) inside SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). sql.raw() with a value derived from caller inputs (ledgerKey/tenantId) can lead to SQL injection or unexpected SQL execution.
  - _Fix:_ Avoid sql.raw with request-derived values. Pass the lock key as a parameterized value instead, e.g. compute a numeric BigInt and use a parameter placeholder: await tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`); additionally validate/normalize ledgerKey before use. If raw SQL is unavoidable, strictly validate and sanitize the string and prefer numeric parameters.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} — no tenant_id constraint when reading a persona. This reads a persona row without ensuring it belongs to the caller's tenant.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) and/or verify the persona belongs to the tenant before exporting. Always validate ownership of referenced rows coming from caller input.
- **server/pace-control.ts:85** — Query against heartbeat_logs without tenant_id filter (COUNT FROM heartbeat_logs ... WHERE created_at > ... AND ${laneFilter}). This reads global heartbeat logs rather than scoping to a tenant.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId} AND created_at > ...) and require/accept a tenantId parameter for pace checks. If the intent is a system-wide metric, restrict it to admin-only code paths and document explicitly; otherwise operate per-tenant.
- **server/pace-control.ts:140** — Dashboard snapshot query SELECT COUNT(...) FROM heartbeat_logs WHERE created_at > ... — missing tenant constraint; it aggregates across tenants.
  - _Fix:_ Require tenant context and add WHERE tenant_id = ${tenantId} to all heartbeat_logs queries (including the persona breakdown query). If global metrics are required, ensure only trusted admin code paths can call the function and consider separating global metrics from tenant-scoped code paths.
- **server/plan-executor.ts:741** — SELECT from tenant-scoped table `plans` without tenant_id constraint (SELECT source FROM plans WHERE id=${planId} LIMIT 1). This reads a tenant-scoped row by id with no tenant filter and can leak or allow cross-tenant interaction before ownership is validated.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId}) or first resolve/validate the caller's tenant identity and use it in the query. If the code needs to discover tenant_id by planId, perform a single parameterized SELECT that returns tenant_id and immediately validate it against the caller (or fail-closed), rather than issuing unauthenticated queries across tenants.
- **server/plan-executor.ts:744** — UPDATE of tenant-scoped table `plans` without tenant_id constraint (UPDATE plans SET ... WHERE id = ${planId} AND status = 'approved' RETURNING ...). The update claims a plan for execution without constraining to a tenant, allowing cross-tenant modification if invoked with an arbitrary planId.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved') or validate the plan's tenant_id (via a prior tenant-scoped SELECT) and then perform the update within a transaction or CAS using that tenant_id to ensure the caller is authorized for that tenant.
- **server/plan-executor.ts:1279** — claimStaleRecoveryChildCore performs UPDATE ... WHERE id=${planId} AND source='stale-plan-recovery' AND status='approved' RETURNING ... on tenant-scoped `plans` without a tenant_id filter. This can claim/return rows across tenants.
  - _Fix:_ Require and pass an expected tenantId into claimStaleRecoveryChildCore and include tenant_id=${tenantId} in the WHERE clause. If tenantId is not known, first fetch and validate the plan's tenant_id in a single guarded step and fail-closed if it does not match the caller's tenant.
- **server/plan-executor.ts:1404** — resumeStuckPlans issues a broad SELECT over `plans` (SELECT id, tenant_id, status, plan_json, execution_log, version FROM plans WHERE status = 'executing' AND ... ) with no tenant filter. This iterates across all tenants' executing plans and may read/update tenant-scoped data without per-tenant authorization checks.
  - _Fix:_ If this is intended as an admin/maintenance operation, ensure it runs only in an admin context and documents that explicitly; otherwise, add tenant scoping or run per-tenant with explicit tenant parameter. At minimum, require an explicit admin guard and audit the operation; prefer iterating per-tenant and scoping every SELECT/UPDATE by tenant_id.
- **server/plan-executor.ts:1710** — reconcileStaleRecoveryChildren selects tenant-scoped rows without tenant filtering (SELECT id, tenant_id, parent_plan_id FROM plans WHERE source = 'stale-plan-recovery' AND status = 'executing' AND updated_at < ...). This reads plans across tenants and then performs tenant-scoped transactional updates later, creating a window for cross-tenant access.
  - _Fix:_ Limit the initial SELECT by tenant_id or require the function to be invoked with an explicit tenantId/admin context. If the operation must sweep multiple tenants, require explicit admin privileges and perform per-tenant transactions that include tenant_id in WHERE clauses; otherwise add WHERE tenant_id = ${tenantId} to the SELECT.
- **server/process-governor.ts:899** — UPDATE agent_desks executes WHERE id = ${desk.id} with no tenant_id constraint — allows updating a row belonging to another tenant if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId} (or validate desk.tenant_id before updating). Always add tenant_id to mutating statements.
- **server/process-governor.ts:1235** — UPDATE governance_rules SET last_triggered_at ... WHERE id = ${rule.id} lacks tenant_id constraint — may modify rules across tenants.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}, or persist + use the rule's tenantId when updating to ensure scoped updates only.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant_id filter; handler does not verify project ownership before using project data.
  - _Fix:_ Require and check tenant context or include tenant_id in the WHERE clause (e.g. AND tenant_id = <caller tenant>) and/or verify the caller is authorized for the project before reading/updating related artifacts.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has no tenant scoping — will iterate projects across tenants.
  - _Fix:_ Scope the query by tenant (e.g. WHERE p.tenant_id = ${tenantId} ...) or otherwise restrict the backfill to a single tenant so no cross-tenant data is read or processed.
- **server/project-brain.ts:352** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is not scoped by tenant_id — may return conversations from other tenants for the same project id.
  - _Fix:_ Add tenant filtering (e.g. AND c.tenant_id = ${tenantId}) or validate the project's tenant before selecting conversations; ensure every read of tenant-scoped tables includes tenant_id.
- **server/project-brain.ts:361** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks tenant scoping — message reads may cross tenant boundaries.
  - _Fix:_ Include tenant_id constraint in the query (e.g. AND tenant_id = ${tenantId}) or verify that the conversation belongs to the expected tenant prior to selecting messages. Ensure all tenant-scoped reads enforce tenant ownership.
- **server/recurring-messages.ts:266** — SELECT from tenant-scoped table agent_knowledge performed without tenant_id constraint (global scan). This returns rows across all tenants (called from heartbeat runner) and violates the invariant that every read against a tenant-scoped table must be constrained to the caller's tenant.
  - _Fix:_ Either add an explicit tenant_id WHERE clause (WHERE tenant_id = ${tenantId}) for tenant-scoped callers, or if a global scan is required for a trusted system job, restrict this query to an internal-only path and perform per-row tenant checks before any processing. Prefer iterating per-tenant (or pass an explicit system-only flag that is guarded by auth) and always include tenant_id when selecting tenant data.
- **server/recurring-messages.ts:190** — SELECT from tenant-scoped table agent_knowledge in listScheduledMessages may be executed without a tenant_id constraint when called with the `_allowGlobalSystemUseOnly` option, returning rows across tenants.
  - _Fix:_ Avoid returning tenant-scoped rows unfiltered. Require a numeric tenantId for public callers. If a global/system scan is needed, place it behind a dedicated, strongly-authenticated internal API and still prefer fetching rows per-tenant or otherwise ensure subsequent operations enforce tenant scoping (e.g. always use tenant_id in downstream UPDATE/DELETE statements).
- **server/research-engine.ts:466** — INSERT into project_notes (project_id, note, author, created_at) omits tenant_id for a tenant-scoped resource. The table is project-scoped and should be tied to a tenant; inserting without tenant_id may mis-attribute notes across tenants.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ...)). Alternatively, ensure project_notes has a foreign key that enforces tenant ownership and pass the session's tenantId explicitly. Keep the earlier assertProjectInTenant guard (already present) and persist tenant_id to the row.
- **server/research-engine.ts:474** — Looped INSERTs into project_notes for each finding omit tenant_id. These per-finding INSERTs write project-scoped data without recording tenant ownership.
  - _Fix:_ Add tenant_id to the INSERT columns and values (VALUES (${projectId}, ${session.tenantId}, ${noteContent}, ${'Research Engine'}, NOW())). Keep the assertProjectInTenant check and ensure the tenant_id value is taken from the validated session.tenantId (not from untrusted input).
- **server/routes.ts:979** — SELECT from tenant-scoped table `file_storage` without tenant_id constraint (owners lookup by filename uses no WHERE tenant_id = ...). This returns rows across tenants and then checks ownership in application logic, risking cross-tenant disclosure if the logic is changed or misused.
  - _Fix:_ Scope the DB query to the authenticated tenant (or to the explicit signed-url tenant) in SQL, e.g. include AND file_storage.tenant_id = <authedTenantId> (or use Drizzle eq(fileStorage.tenantId, authedTenantId)). If the intent is to detect any owners across tenants, make that explicit and protect it behind platform-admin-only access or a vetted audit path; otherwise restrict the query to the caller's tenant and/or limit returned columns.
- **server/routes/conversations.ts:119** — UPDATE on tenant-scoped table 'conversations' performed without constraining by tenant_id (missing AND tenant_id = ${tenantId}).
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}, or use the storage API that enforces tenant ownership.
- **server/routes/conversations.ts:120** — SELECT from 'project_conversations' omits tenant constraint (no WHERE tenant_id = <tenant>). If project_conversations is tenant-scoped this allows cross-tenant reads.
  - _Fix:_ Constrain the query to the caller's tenant, e.g. SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}.
- **server/routes/conversations.ts:123** — INSERT into 'project_conversations' does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERTs.
  - _Fix:_ Include tenant_id in the inserted row, e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}), or use an application API that sets tenant_id.
- **server/routes/conversations.ts:125** — UPDATE on tenant-scoped table 'projects' performed without constraining by tenant_id (missing AND tenant_id = ${tenantId}).
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/conversations.ts:256** — INSERT into 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) ...). If project_notes is tenant-scoped this will create rows with no tenant ownership recorded.
  - _Fix:_ Include tenant_id in the INSERT, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'}), or use an API that enforces/sets tenant ownership.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` lacks tenant_id constraint (missing WHERE tenant_id = <caller tenant>). This allows a requester to mark messages in other tenants as read.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`); Ensure tenantId is derived from the authenticated request and used as a parameter (do not inline raw strings).
- **server/routes/projects.ts:401** — INSERT into project_conversations does not include tenant_id. The table is a project-scoped relation and INSERT lacks an explicit tenant_id column/value.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${targetProjectId}, ${conversationId}, ${tenantId})) or otherwise ensure the table is not tenant-scoped. Always pass the caller's tenantId explicitly for tenant-scoped tables.
- **server/routes/projects.ts:609** — INSERT into project_files does not set tenant_id. The handler inserts a file record referenced to project_id but does not explicitly set tenant_id on the insert statement.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}) ). If project_files is intentionally not tenant-scoped, document this design; otherwise, always persist tenant_id from the authenticated tenant.
- **server/routes/projects.ts:616** — UPDATE projects SET updated_at ... WHERE id = ${projectId} — missing tenant_id constraint. This UPDATE modifies a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant to enforce isolation, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId} RETURNING *; or use Drizzle's eq(projects.tenantId, tenantId). Always include tenant_id in WHERE for tenant-scoped updates.
- **server/routes/projects.ts:676** — INSERT into project_files (multipart upload) does not set tenant_id. File records are inserted with project_id but tenant_id is not passed in the INSERT.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) so that the new row is explicitly attributed to the caller's tenant. If the table is not tenant-scoped, make that explicit in code/comments; otherwise always persist tenant_id.
- **server/routes/projects.ts:684** — UPDATE projects SET updated_at ... WHERE id = ${projectId} — missing tenant_id constraint (multipart upload path). This mutating query lacks the required tenant filter.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}; ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/routes/projects.ts:755** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} — DELETE lacks explicit tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) or include tenant_id in the WHERE clause. Even if a prior projCheck verified ownership, the mutation SQL should still include tenant_id to prevent any accidental cross-tenant modifications.
- **server/seed.ts:42** — Unfiltered read on tenant-scoped table: `SELECT COUNT(*) as count FROM research_sessions` executes across all tenants (no tenant_id constraint).
  - _Fix:_ Constrain the query to a specific tenant. Pass the caller/admin tenant id explicitly and add a WHERE clause (or use Drizzle eq): e.g. `SELECT COUNT(*) as count FROM research_sessions WHERE tenant_id = ${tenantId}` or use db.select().from(researchSessions).where(eq(researchSessions.tenantId, tenantId)). If the intent is a global system check ensure it runs under an explicit operator/admin path and is clearly documented as privileged.
- **server/seed.ts:2240** — Cross-tenant SELECT without tenant scoping: the query selecting conversations joined to projects does not constrain tenant_id to a caller tenant (no WHERE c.tenant_id = <tenant>). This will enumerate/act on conversations/projects across tenants.
  - _Fix:_ Limit the operation to a single tenant by adding an explicit tenant filter (e.g. `WHERE c.tenant_id = ${tenantId}`) or otherwise pass and use the intended tenant id. Ensure downstream INSERTs/updates are only performed for that tenant. If this is an admin-only maintenance action, require an explicit admin/tenant context and document/guard it accordingly.
- **server/seed.ts:3639** — DELETE FROM heartbeat_tasks executed without tenant_id constraint against a tenant-scoped table (heartbeat_tasks has tenant_id index). This will delete matching rows across all tenants.
  - _Fix:_ Restrict the DELETE to an explicit tenant scope. If the intent is to affect only the platform/system tenant, add AND tenant_id = <ADMIN_TENANT_ID> (or use Drizzle .where(eq(heartbeatTasks.tenantId, ADMIN_TENANT_ID))). If the intent is per-tenant cleanup, iterate tenants and run a constrained DELETE per tenant after validating ownership.
- **server/seed.ts:3667** — UPDATE memory_entries performed without any tenant_id constraint (memory_entries is tenant-scoped). This will modify memory rows for all tenants.
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. AND tenant_id = <ADMIN_TENANT_ID>), or iterate per-tenant and run the UPDATE with WHERE tenant_id = <tenantId>. Prefer using Drizzle .where(eq(memoryEntries.tenantId, tenantId)) to avoid cross-tenant changes.
- **server/seed.ts:3693** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint while conversations is tenant-scoped. This will update conversations across all tenants.
  - _Fix:_ Constrain the UPDATE to a tenant (e.g. AND tenant_id = <ADMIN_TENANT_ID>) or perform the migration per-tenant with an explicit WHERE tenant_id = <tenantId>. Use Drizzle's .where(eq(conversations.tenantId, tenantId)) or an equivalent to ensure tenant-scoped updates.
- **server/self-improvement.ts:434** — UPDATE on tenant-scoped table `experiments` is executed without constraining by tenant_id. The db.update(...) call uses .where(eq(experiments.id, experiment.id)) but omits tenant ownership check, allowing cross-tenant modification if an id is guessed or mis-bound.
  - _Fix:_ Include tenant isolation in the WHERE clause. e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId!))). Alternatively, fetch the experiment row with tenant filter and use that DB-verified row id for updates, or enforce an admin-only guard before running unconstrained updates.
- **server/self-reflection.ts:380** — UPDATE on tenant-scoped table `memory_entries` uses only the row id in the WHERE clause (WHERE id = ...), with no tenant_id constraint. This permits updating another tenant's memory row if the id is known.
  - _Fix:_ Add tenant ownership check to the UPDATE WHERE clause, e.g. WHERE id = ${id} AND tenant_id = ${tenantId}. Prefer using the same tenant-scoped condition used in the SELECT that found the row, or perform the update via a single parameterized statement that includes tenant_id.
- **server/self-improvement.ts:561** — Unconstrained SELECT from tenant-scoped table `experiments`. When no category and tenant-scoped conditions are absent the function falls back to db.select().from(experiments).orderBy(...).limit(limit) returning rows across all tenants.
  - _Fix:_ Ensure tenant scoping is always applied. If an 'ALL_TENANTS' admin view is intentionally supported, require an explicit, well-audited admin authorization check before allowing an unconstrained query; otherwise always add .where(eq(experiments.tenantId, tenantId)).
- **server/source-repair-handoff.ts:272** — Unscoped SELECT from the tenant-scoped plans table (SELECT id, tenant_id, source_ref, plan_json, execution_log FROM plans WHERE status = 'handoff_pending' ...). This reads rows across all tenants with no tenant_id filter, violating the requirement that reads against tenant-scoped tables must be tenant-constrained.
  - _Fix:_ Avoid unscoped cross-tenant reads or make the tenant scope explicit. If this reconciliation job must operate per-tenant, iterate tenants and run the query per-tenant with WHERE tenant_id = ${tenantId}. If it truly must run across tenants, centralize and harden access (and document/justify), or otherwise require an explicit admin-only path and protect it accordingly.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks executed without any tenant_id constraint — deleting from a tenant-scoped table across all tenants.
  - _Fix:_ Restrict the DELETE to a specific tenant (e.g. add WHERE tenant_id = ${tenantId} / requiredTenantScope(heartbeat_tasks.tenant_id, tenantId)). If this is intended to run platform-wide, require an explicit admin path and explicit ALL_TENANTS sentinel and audit/log the action.
- **server/storage.ts:936** — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} with no tenant_id constraint — modifying a tenant-scoped row without validating ownership.
  - _Fix:_ Require a tenantId parameter (assertValidTenantId) and include a tenant predicate in the WHERE (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))). If the operation is intended to be admin/system-scoped, require explicit ALL_TENANTS sentinel and authorization checks.
- **server/storage.ts:1119** — fixStaleBackupSchedules runs an UPDATE on heartbeat_tasks without scoping by tenant_id — modifying tenant-scoped rows across tenants.
  - _Fix:_ Apply tenant scoping or require an explicit ALL_TENANTS sentinel and strong admin authorization. Prefer performing per-tenant updates (pass tenantId) or run a controlled admin job that is explicitly authorized and logged.
- **server/storage.ts:1136** — markHeartbeatTaskRun updates heartbeat_tasks WHERE id = ${id} with no tenant_id constraint — mutating a tenant-scoped table without validating tenant ownership.
  - _Fix:_ Require a tenantId argument or lookup the task's tenant and include eq(heartbeatTasks.tenantId, tenantId) in the WHERE. For internal task-run bookkeeping, ensure the caller proves it owns the task before updating.
- **server/storage.ts:1409** — getAllDataForExport reads many tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, etc.) with no tenant scoping — exporting cross-tenant data without an explicit ALL_TENANTS sentinel or authorization.
  - _Fix:_ Require an explicit admin-only code path and an explicit ALL_TENANTS sentinel or a tenantId parameter. Add authorization checks and logging, and prevent accidental use from non-admin contexts. If intended, document and gate the function clearly.
- **server/tenant-fork.ts:226** — UPDATE against a tenant-scoped table uses only the PK (WHERE id = p.newId) when re-pointing the self-ref. The UPDATE lacks a tenant_id filter.
  - _Fix:_ Add an explicit tenant filter to the UPDATE to enforce application-level tenant isolation. e.g. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId))). Even though ids were just created in this transaction, always include tenant_id to satisfy the project invariant and avoid accidental cross-tenant writes.
- **server/tools.ts:2932** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT columns (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) omit tenant_id, so a row can be created without explicit tenant ownership (or will fail if tenant_id is NOT NULL).
  - _Fix:_ Include tenant_id in the INSERT (and pass the caller's tenantId). Example: INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}). Also keep the prior owner/project ownership check, but do not rely on it as a substitute for writing tenant_id explicitly.
- **server/tools.ts:2946** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) lacks tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Even if project ownership was validated, always write tenant_id explicitly to enforce isolation and make later reads unambiguous.
- **server/tools.ts:2806** — INSERT into `project_conversations` (ON CONFLICT DO NOTHING) during project creation does not set tenant_id. The row linking project↔conversation is created without an explicit tenant_id column/value.
  - _Fix:_ Add tenant_id to the insert (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ON CONFLICT DO NOTHING). Ensure the tenantId used is the caller's tenant and that prior checks verify the project/conversation ownership.
- **server/tools.ts:2970** — INSERT into `project_conversations` in link_conversation handler does not set tenant_id. The code INSERTs (project_id, conversation_id) without tenant scoping.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). The handler already asserts the conversation belongs to the tenant — still write tenant_id explicitly to enforce isolation.
- **server/tools.ts:7006** — INSERT into tenant-scoped table project_files does not set tenant_id.
  - _Fix:_ Always include tenant_id explicitly in INSERTs to tenant-scoped tables. For example, add tenant_id to the column list and bind params._tenantId (or resolved tenantId) in the VALUES clause, or use Drizzle insert with tenantId: params._tenantId. Even if you previously validated project ownership, the tenant_id column must still be written to avoid cross-tenant ambiguity.
- **server/tools.ts:8978** — UPDATE on tenant-scoped table proposed_skills uses only WHERE id = ... without constraining tenant_id.
  - _Fix:_ Constrain updates to the caller's tenant. Add an additional predicate (e.g. AND tenant_id = ${tid}) or use Drizzle's .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). Failing to include tenant_id allows cross-tenant row modifications if an id is guessed or leaked.
- **server/tools.ts:9007** — UPDATE on tenant-scoped table proposed_skills (error handler) uses only WHERE id = ... without constraining tenant_id.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tid}) or use Drizzle eq() on the tenantId column so the update only affects rows owned by the caller's tenant.
- **server/tools.ts:9021** — UPDATE on tenant-scoped table proposed_skills (promotion path) uses only WHERE id = ... without constraining tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (or use Drizzle .where with eq(proposedSkills.tenantId, tid)) to ensure only the tenant's own proposed_skills row is updated when promoting/accepting a proposal.
- **server/tools.ts:9038** — UPDATE on tenant-scoped table proposed_skills (reject path) uses only WHERE id = ... without constraining tenant_id.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${tid}) or use Drizzle's eq(proposedSkills.tenantId, tid) in the .where call to prevent modifying other tenants' rows.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE statement modifies browser_workflows row by id without constraining tenant scope (missing WHERE tenant_id = <tenant>), allowing cross-tenant modification of another tenant's workflow record.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); Alternatively, verify ownership first in a transaction (SELECT ... WHERE id = ${workflow.id} AND tenant_id = ${tenantId}) and only then perform the UPDATE.
- **server/tools/domains/knowledge/handlers.ts:90** — UPDATE against tenant-scoped table 'knowledge_triples' uses only 'WHERE id = ${row.id}' without constraining by tenant_id — allows a caller (or race) to modify rows belonging to other tenants.
  - _Fix:_ Include the tenant filter in the UPDATE so the row is modified only if it belongs to the caller's tenant. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}`); or use Drizzle-style .where(eq(knowledgeTriples.tenantId, tenantId)). Ensure all UPDATE/DELETE statements on tenant-scoped tables include tenant_id in the WHERE clause.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys performed without constraining by tenant_id (no WHERE tenant_id = ...). This reads provider key rows across all tenants (even if only run for admin tenant), risking cross-tenant exposure of provider configuration.
  - _Fix:_ If provider_keys is tenant-scoped, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) and/or restrict returned columns to non-sensitive aggregates. If the intent is an admin-wide audit, make the cross-tenant intent explicit and gate it to a verified admin flow and avoid returning raw key material (or return only aggregated/redacted info).
- **server/tools/domains/security/handlers.ts:260** — COUNT/aggregation on provider_keys performed without tenant_id constraint. Query enumerates provider configurations across all tenants with no tenant scoping.
  - _Fix:_ If provider_keys is tenant-scoped, include tenant_id = ${tenantId} in the WHERE clause. If this is an explicit admin-only aggregation, ensure the code documents and enforces the admin-only authorization and that results do not expose per-tenant sensitive details (or restrict to safe aggregates).
- **server/tools/domains/strategic-memory/handlers.ts:99** — UPDATE memory_entries SET ... WHERE id = ${id} — the UPDATE lacks an explicit tenant_id constraint. This allows modifying rows by id without ensuring the row belongs to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or otherwise verify ownership before updating. Always combine primary-key updates with tenant_id checks in multi-tenant tables to prevent cross-tenant modification.
- **server/tools/domains/strategic-memory/handlers.ts:159** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) — the UPDATE uses id list but omits tenant_id scoping, allowing updates to rows that may belong to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE tenant_id = ${tid} AND id = ANY(${ids})) or use a parameterized update that includes tenant_id to ensure only this tenant's rows are modified.
- **server/tools/domains/strategic-memory/handlers.ts:258** — UPDATE memory_entries SET ... WHERE id = ${id} — missing tenant_id constraint on the UPDATE (same pattern repeats for strategic_win updates).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or otherwise validate row ownership before updating to prevent cross-tenant writes.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without tenant constraint (no WHERE tenant_id = ...) — loads pairing rows across all tenants.
  - _Fix:_ Avoid selecting tenant-scoped rows across tenants. Constrain the query to a specific tenant (WHERE tenant_id = <trusted tenant>) or redesign pairing lookup so only verified/tenant-scoped rows are read. For example, store/maintain an indexed, operator-provisioned pairing table with an explicit tenant_id column and query WHERE tenant_id = $tenant AND title = $title. If you must detect contested pairings, perform a JOIN with conversations and restrict to rows whose conversation tenant matches the claimed tenant, or perform an explicit, audited admin-only scan — do NOT read arbitrary agent_knowledge rows across tenants in normal webhook flow.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} executed without constraining to caller/expected tenant. This allows cross-tenant lookup driven by untrusted data from agent_knowledge content.
  - _Fix:_ Do not perform unconstrained reads on tenant-scoped tables using untrusted IDs. Instead, verify ownership in a single constrained query (e.g. SELECT id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${expectedTenantId} LIMIT 1) or JOIN conversations to agent_knowledge in a query that enforces c.tenant_id = ak_expected_tenant. Treat data parsed from agent_knowledge as untrusted and bound all reads to an authoritative tenant context to prevent cross-tenant information leaks.
- **server/venture-discovery/budget.ts:156** — UPDATE against tenant-scoped table agent_cost_ledger without tenant_id constraint (updates by id only). This allows modifying reservation rows across tenants if an id collides or is attacker-controlled.
  - _Fix:_ Require and accept tenantId for settleVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Alternatively verify the row belongs to the tenant before updating and fail if not.
- **server/venture-discovery/budget.ts:170** — UPDATE against tenant-scoped table agent_cost_ledger without tenant_id constraint (release updates by id only). This can clear or change other tenants' reservation rows.
  - _Fix:_ Require and accept tenantId for releaseVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Or first SELECT to verify the reservation's tenant_id matches the caller, then perform the UPDATE.
- **server/webhook-triggers.ts:255** — SELECT from tenant-scoped table `webhook_triggers` is performed without constraining by tenant_id (WHERE tenant_id = ...). This lookup uses only webhook_key and then proceeds to act on the returned row, which can leak or mis-authorize cross-tenant rows.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE webhook_key = ${webhookKey} AND tenant_id = ${expectedTenantId}) or otherwise resolve/verify the owning tenant before reading or using the row. If webhook_key must be globally unique, explicitly document and enforce that invariant and validate ownership before performing tenant-scoped side effects (assert the row.tenant_id before using it).
- **server/webhookHandlers.ts:1268** — Subscription cancellation handler updates tenants (UPDATE tenants SET plan='trial' WHERE id = ${tenantId}) using tenantId parsed from Stripe metadata (subscription.metadata.tenantId) without verifying that the webhook caller is authorized to affect that tenant.
  - _Fix:_ Validate ownership before mutating: if metadata provides a tenant id, confirm the subscription/customer in the webhook is already associated with that tenant (e.g. tenants.stripe_customer_id or tenants.stripe_subscription_id matches). If not, look up the tenant via a trusted binding (customer/subscription id) or reject the operation. Treat metadata-derived tenant ids as untrusted input.
- **server/webhookHandlers.ts:1343** — Subscription update handler updates tenant plan (UPDATE tenants SET plan = ${plan} WHERE id = ${tenantId}) using tenantId from subscription.metadata without verifying the webhook's authority over that tenant.
  - _Fix:_ Before applying plan changes based on metadata.tenantId, re-verify ownership by ensuring the subscription/customer in the webhook matches the tenant's recorded Stripe customer/subscription. Require price/price-item verification (already present) plus an explicit ownership check (stripe_customer_id/stripe_subscription_id) and fail closed on mismatch. Alternatively, derive tenant identity from server-controlled bindings rather than untrusted metadata.

### MEDIUM (37)

- **server/channels/kernel.ts:177** — Handler trusts an adapter-supplied conversationId (returned by resolveConversation) and calls processMessage(conversationId, ..., { tenantId: turn.tenantId }) without validating that the conversationId actually belongs to turn.tenantId. If processMessage (or downstream reads) does not enforce tenant scoping, this can allow cross-tenant access.
  - _Fix:_ Validate ownership of resolved conversationId before using it: either have resolveConversation return a tenant-verified conversation record (including tenant_id) or perform an explicit lookup that verifies WHERE id = ${conversationId} AND tenant_id = ${turn.tenantId} (or use Drizzle eq(table.tenant_id, turn.tenantId)) prior to calling processMessage. Ensure all downstream conversation reads use tenant-scoped SQL filters.
- **server/chat-engine/project-context.ts:23** — SELECT from `project_conversations` (pc) in the link lookup does not include an explicit tenant_id constraint on pc. The query relies on joined `projects p` having p.tenant_id = ${tenantId} but pc itself is not constrained.
  - _Fix:_ Restrict the project_conversations row to the tenant explicitly (e.g. WHERE pc.conversation_id = ${conversationId} AND pc.tenant_id = ${tenantId}) or add pc.tenant_id = p.tenant_id in the join/WHERE so every tenant-scoped table is directly constrained.
- **server/chat-engine/project-context.ts:45** — EXISTS subquery reads from tenant-scoped table `project_conversations` (pc) without a tenant_id constraint. The EXISTS clause checks pc.conversation_id and pc.project_id but does not ensure pc.tenant_id equals the caller tenant.
  - _Fix:_ Add an explicit tenant predicate inside the EXISTS subquery (e.g. AND pc.tenant_id = ${tenantId}) or correlate pc through a project row already scoped to the tenant so the SQL itself enforces ownership.
- **server/chat-engine/project-context.ts:181** — Query joins `project_conversations` (pc) but does not constrain pc.tenant_id. The WHERE filters c.tenant_id but not pc, so reads against the tenant-scoped project_conversations table lack an explicit tenant_id filter.
  - _Fix:_ Include an explicit tenant filter for pc (e.g. WHERE pc.project_id = ${projectId} AND pc.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}) or otherwise ensure pc rows are constrained in the same statement to prevent cross-tenant access.
- **server/delegation-events.ts:59** — getRecentEvents(conversationId, since?, tenantId?) will return events across tenants if caller omits tenantId. Filtering is applied only when tenantId is provided; callers that forget to pass tenantId get unscoped results, enabling cross-tenant information leakage.
  - _Fix:_ Make tenantId a required parameter for getRecentEvents (and similar read APIs) or fail-closed when tenantId is omitted. If optionality is required, document and enforce stricter defaults (e.g. return empty or throw when tenantId is not supplied) and always filter events by tenant before returning them.
- **server/heartbeat-schedules.ts:26** — Cross-tenant SELECT on tenant-scoped table `messages` without asserting caller tenant. The query SELECT DISTINCT tenant_id FROM messages ... has no caller-tenant equality filter (e.g. WHERE tenant_id = <tenant>).
  - _Fix:_ If this is intended to be a cross-tenant enumeration, make that explicit and document/centralize it in a scheduler-only boundary. Otherwise restrict the query to the caller's tenant (add WHERE tenant_id = ${tenantId} or use a storage API that enforces tenant context). Avoid returning tenant-scoped rows to request-scoped handlers without explicit authorization.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECTs tenant_id FROM `agent_jobs` by id with no tenant filter. This exposes tenant resolution by id without scoping; while intended for admin lookups, it's an unscoped read against a tenant-scoped table.
  - _Fix:_ Document and enforce that this helper is admin-only (and require explicit admin authorization at call sites), or require a caller-provided tenant context and verify it matches the returned tenant_id before proceeding. Prefer an admin-only API surface for cross-tenant lookups.
- **server/job-queue.ts:406** — listJobs will run a SELECT from `agent_jobs` without a tenant_id WHERE clause when filter.tenantId is not provided, returning rows across all tenants. This is an unscoped read from a tenant-scoped table.
  - _Fix:_ Require an explicit tenantId for tenant-facing callers or enforce admin-only permissions for cross-tenant listing. Alternatively, always apply a tenant scope derived from the request context and fail closed if no tenant is available.
- **server/knowledge-diversity-monitor.ts:266** — Query selects tenant_id, persona_id, COUNT(*) FROM `agent_knowledge` grouping by tenant/persona without restricting by a specific tenant: it enumerates and reads embedding rows across all tenants.
  - _Fix:_ If the monitor must run per-tenant, iterate tenants and call per-tenant queries with WHERE tenant_id = ${tenantId}. If it is intentionally cross-tenant, ensure the routine runs in a tightly controlled, admin-only context and that any downstream actions (notifications, snapshots) are written with explicit tenant_id constraints and authorization checks.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a request-derived value when updating trust_scores: the column name (`column`) is derived from the caller-supplied `outcome` and then injected into the SQL via sql.raw(column). Even though the code validates and whitelist-checks the outcome, using sql.raw with any runtime-derived value poses injection and maintenance risk.
  - _Fix:_ Avoid injecting dynamic SQL identifiers at runtime. Replace the raw interpolation with explicit, branch-based updates (e.g. switch on outcome and execute one of four prepared update statements that reference a fixed column identifier), or use the query builder API to set the specific column in a safe manner. This removes the need for sql.raw and eliminates the risk of accidental injection or future changes bypassing the whitelist.
- **server/lib/stuck-task-sweep.ts:56** — The agent_runs query reads tenant-scoped rows without a tenant_id filter (SELECT ... FROM agent_runs WHERE status = 'running' AND updated_at < ...). This returns rows across all tenants and exposes tenant_id values without enforcing caller scoping.
  - _Fix:_ Scope the sweep by tenant (accept a tenantId parameter and add WHERE tenant_id = ${tenantId}), or make explicit that this is an admin audit and restrict/authorize callers accordingly. If the intent is to run a cross-tenant admin audit, ensure it executes on a documented and restricted admin-only path and logs/audits access; otherwise add tenant predicates to the query.
- **server/lib/stuck-task-sweep.ts:84** — The mind_tickets query reads tenant-scoped rows without a tenant_id filter (SELECT ... FROM mind_tickets WHERE status = 'in_progress' AND updated_at < ...). This may surface rows across tenants in a single call.
  - _Fix:_ Either add tenant_id = ${tenantId} filtering (and/or accept a tenantId param) or restrict this function to an authenticated admin-only audit path and make that intention explicit. Prefer per-tenant queries or an explicit, auditable admin flow to avoid accidental cross-tenant exposure.
- **server/plan-executor.ts:1619** — backfillHistoricalRecoveryBlockedPlansCore builds a WHERE clause that allows an unbounded scan when tenantId is not supplied ( ... AND (${tenantId ?? null}::integer IS NULL OR tenant_id=${tenantId ?? null}) ). When called without tenantId this selects across tenants on the tenant-scoped `plans` table.
  - _Fix:_ Avoid SQL constructs that implicitly remove tenant filtering. Require a tenantId parameter for non-admin calls or guard the function so it only runs in an explicit admin/maintenance context. When scanning across tenants is necessary, ensure the caller is an authorized admin and that the operation is auditable and safe; otherwise always include tenant_id = ${tenantId} in the WHERE clause.
- **server/platform-admin-conversations.ts:21** — SELECT from tenant-scoped table `conversations` without scoping by tenant_id (cross-tenant read).
  - _Fix:_ Add an explicit tenant filter (e.g. AND conversations.tenant_id = <platform-admin-target-tenant>) or otherwise ensure this cross-tenant access is audited/guarded. If the function must allow cross-tenant reads, make that explicit and log/authorize accordingly.
- **server/routes.ts:1927** — SELECT presenter_sessions by token without scoping to tenant_id. The query returns tenant_id but the SQL does not constrain by tenant, relying on token as capability only.
  - _Fix:_ Either include tenant scoping in the WHERE clause (WHERE token = ${token} AND tenant_id = ${expectedTenantId}) when a tenant context is available, or document and harden the capability model (long unguessable token) and consider additionally storing and validating tenant-bound HMACs. Prefer explicit tenant checks where possible.
- **server/routes.ts:1955** — SELECT presenter_sessions by token (session lookup) performed without a tenant_id WHERE filter. The code later uses the returned tenant_id, but the initial lookup is unscoped SQL using a request-derived token.
  - _Fix:_ Constrain the SELECT by tenant when the tenant is known, e.g. WHERE token = ${token} AND tenant_id = ${expectedTenantId}. If the token is intended to be a capability (unguessable secret), ensure the capability's scope and lifetime are well-documented and consider adding an explicit tenant-binding check in SQL to reduce risk.
- **server/routes.ts:1998** — SELECT id FROM presenter_sessions WHERE token = ${token} executed without tenant constraint in a request handler. Token is request-derived and the SQL is unscoped.
  - _Fix:_ Add tenant scoping to this lookup where possible (e.g. WHERE token = ${token} AND tenant_id = ${expectedTenantId}) or otherwise centralize and harden capability checks so the SQL does not return rows across tenants based solely on a token parameter.
- **server/routes.ts:2062** — SELECT presenter_sessions WHERE token = ${token} without tenant filter in /api/presenter/:token/repair-images. The query is request-driven and returns tenant_id but is not constrained at SQL level.
  - _Fix:_ Restrict the initial SELECT by tenant_id when the tenant context is available, or require and validate an internal auth credential that guarantees caller authorization before performing cross-tenant SQL. Prefer explicit WHERE tenant_id = ${sessionTenantId}.
- **server/routes.ts:628** — Startup backfill SELECT across projects and tenants (SELECT p.id, p.name, p.tenant_id, t.name ... WHERE p.drive_folder_id IS NULL) does not restrict by tenant_id. This reads a tenant-scoped table without a tenant filter.
  - _Fix:_ If this backfill must operate across all tenants, run it in a controlled administrative job (verify it already does so) and ensure access is limited to trusted startup/maintenance code. If it should operate per-tenant only, add a tenant_id filter or iterate tenants and run per-tenant queries with WHERE p.tenant_id = <tid> to avoid unscoped multi-tenant reads.
- **server/routes.ts:1396** — Admin endpoint /api/public/stats issues SELECTs on tenant-scoped tables (conversations, messages, heartbeat tasks/logs, memory_entries) without tenant_id WHERE filters — global counts are computed by unscoped queries.
  - _Fix:_ If the endpoint is platform-admin-only and global counts are intentional, document this explicitly and restrict the route to platform admins (it already is). If per-tenant counts are expected, scope each query by tenant_id using the caller's tenantId (or accept a tenant parameter validated for admin use). Always prefer explicit tenant scoping in SQL rather than implicit assumptions.
- **server/routes.ts:1426** — Admin endpoint /api/public/architecture runs many unscoped SELECT COUNT(*) queries against tenant-scoped tables (conversations, messages, projects, skills, etc.) without tenant_id filters.
  - _Fix:_ If global aggregation for platform admins is intended, ensure the route remains strictly platform-admin gated and audited. Otherwise, change queries to include tenant scoping (WHERE tenant_id = …) or perform per-tenant aggregation in controlled admin jobs. Prefer explicit SQL-level tenant constraints to avoid accidental cross-tenant exposure.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table 'agent_jobs' is executed with no tenant_id filter (no WHERE tenant_id = ...). This returns cross-tenant rows without an explicit ALL_TENANTS/intent marker.
  - _Fix:_ If this is intended only for platform admins, make the intent explicit (e.g. use a named ALL_TENANTS parameter or document the admin-only cross-tenant access) and gate the route with requirePlatformAdmin. Otherwise restrict the query with WHERE j.tenant_id = ${tenantId} (or accept a tenant selector passed after explicit admin check).
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant filter — reading a tenant-scoped table globally.
  - _Fix:_ If this is intended to be tenant-scoped, add a tenant_id predicate (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...). If it is intentionally global, document the intent in code comments to avoid accidental cross-tenant effects.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks across all tenants (no tenant_id filter).
  - _Fix:_ Restrict the UPDATE with an explicit tenant constraint (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or the intended tenant variable), or add a clear comment/approval if global changes are intended.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' is not scoped by tenant_id — reads tenant-scoped table globally.
  - _Fix:_ Add a tenant_id predicate (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) if the operation should be tenant-scoped, or explicitly document deliberate global intent.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant_id constraint and will affect rows across tenants.
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant) to the WHERE clause, or otherwise scope the update to the intended tenant.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND ( name ILIKE ... ) has no tenant filter — unscoped read of a tenant-scoped table.
  - _Fix:_ Restrict the query with tenant_id = ${ADMIN_TENANT_ID} (or intended tenant) or explicitly document/approve the global scope.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE ( ... ) AND enabled = true is missing tenant_id, potentially disabling tasks across tenants.
  - _Fix:_ Constrain the UPDATE to the appropriate tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise make global intent explicit and audited.
- **server/skillify.ts:221** — Query reads from the personas table without constraining by tenant_id. The code does a lookup by persona id (conv[0].personaId) but does not verify the persona row belongs to the calling tenant, allowing a conv row to reference a persona from another tenant and leak that persona's data.
  - _Fix:_ Constrain the SELECT to the calling tenant (or otherwise verify ownership). Example: include AND persona.tenant_id = ${tenantId} (or eq(personas.tenantId, tenantId)) in the WHERE clause, or explicitly fetch and assert the persona belongs to conv[0].tenantId before using it.
- **server/skillify.ts:534** — Creating a skill with a caller-supplied personaId without verifying that the persona belongs to the caller's tenant. Passing an unvalidated persona_id into storage.createSkill can associate a skill with a persona from another tenant.
  - _Fix:_ Validate the personaId before creating the skill (e.g. SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} and fail if not found). Alternatively, disallow caller-provided personaId or require the storage layer to enforce tenant ownership.
- **server/storage.ts:289** — Raw SELECT on messages: SELECT id, citations FROM messages WHERE id IN (...) is executed without an explicit tenant_id clause — reading a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId} or use a join/subselect constrained by the caller's tenant). If ids are derived from a prior tenant-scoped query, still enforce tenant equality in this follow-up query as defense-in-depth.
- **server/storage.ts:959** — getMemoriesWithoutEmbeddings selects memory_entries across all rows with no tenant filter — reading tenant-scoped data without scoping.
  - _Fix:_ Require and validate a tenantId param (or explicit ALL_TENANTS sentinel) and apply requiredTenantScope(memoryEntries.tenantId, tenantId). If intended for platform-wide background jobs, make that explicit and gate with admin authorization.
- **server/storage.ts:969** — getKnowledgeWithoutEmbeddings queries agent_knowledge with no tenant predicate — returning tenant-scoped rows across tenants.
  - _Fix:_ Require a tenantId or explicit ALL_TENANTS sentinel and use requiredTenantScope(agentKnowledge.tenantId, tenantId) in the WHERE. Gate any platform-wide access behind an explicit admin path and audit logging.
- **server/tools.ts:2926** — SELECT against tenant-scoped table `project_files` (dupCheck) lacks an explicit tenant_id filter. Query: SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ... (no tenant_id constraint).
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or otherwise scope the read via a join that enforces tenant_id. Never read a tenant-scoped table using only a foreign id supplied by the caller without tenant scoping/ownership verification.
- **server/tools.ts:2966** — SELECT against tenant-scoped table `project_conversations` (existing link check) lacks tenant_id constraint. Query: SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId}
  - _Fix:_ Constrain the read by tenant: add AND tenant_id = ${tenantId} or join to projects/conversations with tenant_id checks. Even though project/conversation ownership is validated elsewhere, each query should include explicit tenant scoping to prevent accidental cross-tenant exposure.
- **server/tools.ts:2844** — Correlated subqueries counting rows in tenant-scoped tables do not include tenant_id in their WHERE clauses. Example subqueries at lines 2844-2847: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id), (SELECT COUNT(*) FROM project_notes WHERE project_id = p.id), (SELECT COUNT(*) FROM project_conversations WHERE project_id = p.id) — none include tenant_id constraint.
  - _Fix:_ Add tenant scoping to the subqueries, e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}, or rewrite the counts using JOINs that include p.tenant_id = ${tenantId}. Ensure every read against tenant-scoped tables contains an explicit tenant filter.
- **server/video-job-runner.ts:275** — SELECT from tenant-scoped table video_jobs with no tenant_id filter (listStaleVideoJobs returns rows across all tenants). This reads potentially sensitive cross-tenant job state and can surface or act on other tenants' jobs.
  - _Fix:_ If intent is to operate per-tenant, add tenant_id filtering to the query. If this is a global/system recovery process, add an explicit authorization/intent comment and minimize leaked data (or move this logic to a privileged admin-only path). Consider adding an explicit tenant parameter or an allowlist and ensure any subsequent UPDATEs always require and verify tenant_id equality.

### Resolved — confirmed false positives (167)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/agentic/wake-scheduler.ts:145** _(system-sweep)_ — Global claim/update of agent_wake_schedules selects ids without constraining to a tenant. The inner SELECT used by the UPDATE (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) has no tenant_id predicate, allowing rows across all tenants to be selected/locked/updated in one statement.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due rows across tenants and processes each row with its own tenant_id; housekeeping/event dispatch only, no mixed tenant output.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. Tenant isolation invariant requires every INSERT into tenant-scoped tables to include the caller's tenant (columns are NOT NULL with no default). As written, invoice_items rows can be created without an explicit tenant_id, risking cross-tenant data leakage or failed invariants.
  - _Why false positive:_ invoice_items has no tenant_id and is a child of tenant-scoped invoices; inserting without tenant_id is correct by design.
- **server/embeddings.ts:845** _(system-sweep)_ — backfillEmbeddingVecs selects from memory_entries without tenant scoping (`SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200`) and then performs unscoped UPDATEs: `UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}`. Both the SELECT and UPDATE operate across all tenants and perform writes with no tenant_id constraint.
  - _Why false positive:_ Backfill is a platform-wide maintenance sweep that mutates only embedding housekeeping fields and does not return or mix tenant data.
- **server/embeddings.ts:861** _(system-sweep)_ — backfillEmbeddingVecs also selects agent_knowledge rows without tenant filtering and updates them with `UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}` — unscoped reads and writes across tenant-scoped table agent_knowledge.
  - _Why false positive:_ Knowledge embedding backfill is a maintenance sweep updating per-row housekeeping fields only, with no tenant-facing mixed output.
- **server/felix-brain.ts:23** _(system-sweep)_ — rehydrateTaskStateOnBoot issues a SELECT from agent_runs without constraining tenant_id. This reads running/pending agent_runs across all tenants and then materializes them into in-memory taskStateStore, violating the invariant that every read against a tenant-scoped table must be filtered by the caller's tenant.
  - _Why false positive:_ Boot-time global rehydration intentionally scans all tenants but stores each row under its own tenant_id key and does not return mixed tenant data.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table `code_proposals` (SET status = 'archived_stale' WHERE id = ANY(...)) has no tenant_id constraint — this can modify rows for other tenants.
  - _Why false positive:_ Global stale-proposal housekeeping sweep selects stale rows platform-wide and archives only those picked ids; maintenance/status-only action, not tenant-facing data mixing.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE on tenant-scoped table `heartbeat_tasks` (SET enabled = false WHERE id = ANY(...)) has no tenant_id constraint — this may disable tasks for other tenants.
  - _Why false positive:_ Global stuck-heartbeat housekeeping sweep disables only rows it previously selected by id; status-only maintenance across tenants.
- **server/heartbeat-task-scheduler-storage.ts:43** _(system-sweep)_ — claimDueHeartbeatTasksAcrossTenants() runs UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... without a tenant_id equality check. This allows claiming/updating heartbeat_task rows across tenants based solely on id, enabling cross-tenant mutation.
  - _Why false positive:_ Cross-tenant scheduler claim loop intentionally operates platform-wide on due heartbeat tasks and only updates per-row scheduling fields.
- **server/heartbeat.ts:2681** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint. This mutates a tenant-scoped row without verifying tenant ownership, risking modification of another tenant's task.
  - _Why false positive:_ newTask is created immediately above with trusted tenantId passed into createHeartbeatTask; follow-up update targets that freshly created row by returned id.
- **server/heartbeat.ts:3155** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat path) executes without tenant_id scoping. This writes tenant-scoped state (approval_status) without an ownership check.
  - _Why false positive:_ newTask is created immediately above with trusted tenantId in the same flow; this update applies to that just-created row by id.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — The CTE+UPDATE that picks and locks due rows from `scheduled_posts` executes without any tenant_id predicate — it selects/updates tenant-scoped rows across all tenants (SELECT id FROM scheduled_posts ... FOR UPDATE SKIP LOCKED / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id).
  - _Why false positive:_ Platform-wide scheduled-post runner intentionally claims due rows across tenants and returns each row with its own tenant_id for per-row processing.
- **server/memory-graph.ts:130** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} executes without a tenant_id predicate, allowing a category row belonging to another tenant to be modified.
  - _Why false positive:_ catId comes from ensureCategory in the same call path, which selects/inserts memory_categories scoped by the provided tenantId.
- **server/memory-graph.ts:708** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} executes without a tenant_id predicate, so it may modify a memory entry belonging to a different tenant.
  - _Why false positive:_ mem.id is sourced from a preceding SELECT on memory_entries filtered by tenant_id = tenantId in the same function.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — write against tenant-scoped table using only id without tenant_id constraint. This can modify another tenant's row if ids collide or if subs were read without tenant scoping.
  - _Why false positive:_ Startup validation is a platform-wide maintenance loop over subscription rows already loaded per-row with tenant_id; update only flips is_active on the current row.
- **server/oauth-subscriptions.ts:966** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — write without tenant_id constraint (same pattern repeated).
  - _Why false positive:_ Same startup maintenance sweep processes each subscription row individually with row-carried tenant_id and only mutates housekeeping status.
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — write against tenant-scoped table lacking tenant_id constraint (another un-scoped update).
  - _Why false positive:_ Same platform-wide subscription expiry/refresh sweep; per-row status deactivation only, no tenant-facing data mixing.
- **server/recurring-messages.ts:307** _(system-sweep)_ — UPDATE agent_knowledge SET ... WHERE id = ${r.id} — write to tenant-scoped table without including tenant_id in the WHERE clause. This can result in modifying another tenant's row if ids collide or if the row context changes between read and update.
  - _Why false positive:_ Global scheduler sweep updates housekeeping fields only (lastRunAt/nextRunAt in content) per row and does not mix tenant data into output.
- **server/research-engine.ts:171** _(system-sweep)_ — cleanupZombieSessions performs an UPDATE on research_sessions without constraining by tenant_id. This will mark running sessions as completed across all tenants (cross-tenant mutation).
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping update of status fields across stale rows, not a tenant-facing mixed-data operation.
- **server/routes.ts:4653** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The code inserts a note for a project (INSERT INTO project_notes (project_id, note, author) VALUES (...)) without setting tenant_id, allowing a conversation/request scoped to tenant A to create notes that end up associated with projects from other tenants.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through tenant-scoped projects/conversations; project_id was resolved from a conversation filtered by tenant_id.
- **server/routes/admin-zombie-detector.ts:41** _(global-table)_ — Query against tenant-scoped table agent_trace_spans has no tenant_id constraint — CTE `used` selects from agent_trace_spans without filtering by tenant, allowing cross-tenant data exposure.
  - _Why false positive:_ Route is restricted to ADMIN tenant/admin requests and intentionally computes platform-wide aggregate tool-usage stats.
- **server/routes/admin-zombie-detector.ts:56** _(global-table)_ — Query against tenant-scoped table conversations has no tenant_id constraint — CTE `used` selects from conversations without filtering by tenant, allowing cross-tenant aggregation.
  - _Why false positive:_ Admin-only endpoint intentionally aggregates conversations platform-wide for zombie detection; output is cross-tenant operational telemetry, not tenant-scoped data.
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE to sculptor_sessions performed without constraining by tenant_id (WHERE id = ${session.id} only) — write to a tenant-scoped table missing tenant filter.
  - _Why false positive:_ Update targets the freshly inserted sculptor_sessions row by server-generated primary key within the same internal flow, not from an untrusted request id boundary.
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE to sculptor_sessions sets conversation_id WHERE id = ${session.id} without tenant_id constraint — write to a tenant-scoped table missing tenant filter.
  - _Why false positive:_ Update uses the just-created session's server-generated primary key in the same internal function, so this is an internal worker/local state transition pattern.
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE to sculptor_sessions (status/result/tool_calls_count/tokens_used/completed_at) uses WHERE id = ${sessionId} with no tenant_id constraint — allows modifying another tenant's session by id.
  - _Why false positive:_ Background monitor updates one internally created session identified by server-held sessionId/runId, not caller-supplied cross-tenant input.
- **server/storage.ts:979** _(system-sweep)_ — UPDATE memory_entries SET status = 'archived' runs without a tenant_id predicate — a write to a tenant-scoped table affecting all tenants.
  - _Why false positive:_ archiveExpiredMemories is a platform-wide maintenance sweep archiving expired rows via housekeeping status updates only.
- **server/storage.ts:995** _(system-sweep)_ — UPDATE memory_entries SET status = 'archived' (archiveStaleMemories) executed without tenant_id filter — performing cross-tenant writes.
  - _Why false positive:_ Platform-wide housekeeping update archives stale rows across tenants, returns only ids/count, and does not expose or mix tenant data.
- **server/surprise-scorer.ts:93** _(internal-worker-pk)_ — UPDATE on tenant-scoped table felix_proposals is executed without constraining by tenant_id (WHERE id = ${proposalId} AND args_embedding IS NULL). This can overwrite another tenant's row if ids collide or be abused to stamp embeddings cross-tenant.
  - _Why false positive:_ Best-effort internal worker stamps a row by server-generated proposalId primary key, not from an untrusted request boundary.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped custom_tools filters only by name (eq(customTools.name, parsed.name)) and omits tenant_id, allowing cross-tenant discovery or conflict checks across tenants.
  - _Why false positive:_ Name is transformed with tenantToolName(parsed.name, tenantId) immediately above, making lookup tenant-derived and not cross-tenant discovery.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT by custom_tools.id is performed without a tenant_id predicate (where(eq(customTools.id, inserted.id))). This reads a tenant-scoped table row without explicit tenant scoping.
  - _Why false positive:_ Re-read is by inserted.id from the immediately preceding insert in the same flow, so ownership is established earlier in the same call path.
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE of custom_tools (setting implementation) uses only the row id in the WHERE clause (.where(eq(customTools.id, tool.id))) and omits tenant_id, performing a tenant-scoped write without tenant filtering.
  - _Why false positive:_ Update targets tool.id from the just-inserted/validated row passed through the same flow, not an untrusted tenant-crossing selector.
- **server/tool-learning.ts:316** _(upstream-verified)_ — DELETE on custom_tools in revertToolRow uses only the row id (where(eq(customTools.id, tool.id))) and lacks a tenant_id filter, risking deletion of a row outside the caller's tenant.
  - _Why false positive:_ Delete reverts the same tool row created earlier in this call path; tool.id comes from that inserted row, with ownership already established.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE that increments usageCount on custom_tools uses only eq(customTools.id, tool.id) in the WHERE and omits tenant_id, performing a tenant-scoped write without tenant filtering.
  - _Why false positive:_ Tool row was selected just above with tenant_id = tenantId; subsequent update uses that verified row's id in the same call path.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments issues a SELECT from experiments without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads tenant-scoped rows across all tenants.
  - _Why false positive:_ Global maintenance sweep reads due experiments across tenants and then updates each row using its own row-carried tenant_id.
- **server/agent-eval.ts:221** _(internal-worker-pk)_ — runEval updates agent_evals by id only (UPDATE agent_evals ... WHERE id = ${evalId}) after running an eval. There is no tenant_id constraint on the UPDATE, allowing cross-tenant writes if evalId is guessed or rebinding occurs.
  - _Why false positive:_ Update is on evalId returned from a server-side INSERT in the same internal eval runner flow, not from an untrusted request boundary.
- **server/agentic-engines.ts:686** _(system-sweep)_ — autoApplyEligibleInsights issues an UPDATE on ai_insights that may run without a tenant scope when tenantId is omitted: UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope}. When tenantScope is empty this updates across tenants.
  - _Why false positive:_ Intentional cross-tenant housekeeping/backfill updates status fields and processes returned rows with their own tenant_id, without returning mixed tenant data to users.
- **server/agentic-engines.ts:500** _(system-sweep)_ — retryPendingMinervaRouting SELECTs ai_insights rows without a tenant constraint (SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ... LIMIT 50). This reads tenant-scoped insight rows across tenants.
  - _Why false positive:_ Background durability sweep intentionally scans across tenants, then processes each row with its own row-carried tenant_id and skips invalid tenant ids.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agent_approvals without scoping by tenant_id (UPDATE agentApprovals SET status='expired' WHERE status='pending' AND expiresAt < now()). This updates pending approvals across all tenants in one statement.
  - _Why false positive:_ Global expiry housekeeping updates only approval status fields and returns per-row tenant_id for follow-up run updates; no tenant data is returned to a tenant-facing surface.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — runBudgetEnforcement executes a cross-tenant query: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` has no tenant_id constraint. This reads a tenant-scoped table across all tenants without an explicit admin/authorization guard, violating the requirement that tenant-scoped reads be constrained to the caller's tenant.
  - _Why false positive:_ Heartbeat sweep intentionally checks active budgets across tenants and emits separate per-tenant events using each row's tenant_id without mixing tenant content.
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — SELECT query reads tenant-scoped tables (agent_runs, agent_approvals) without constraining to a specific tenant_id — the query scans rows across all tenants.
  - _Why false positive:_ Resume worker is a platform-wide maintenance sweep over pending approvals/runs; each candidate carries tenant_id and later handling is per-row, not mixed into one tenant response.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — Backfill query selects conversations with an existence check on messages using a subquery: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 — the subquery does not include a tenant_id filter. This reads a tenant-scoped table (messages) without constraining by tenant_id.
  - _Why false positive:_ Boot/backfill maintenance sweep scans conversations across tenants; the messages count subquery is only used as housekeeping eligibility and results are processed per conversation with row-carried tenant_id.
- **server/auto-project.ts:135** _(upstream-verified)_ — Querying messages for a conversation: SELECT role, LEFT(content, 300) ... WHERE conversation_id = ${conversationId} ORDER BY id ASC LIMIT 20 — the query lacks an explicit tenant_id constraint. Reads against the tenant-scoped messages table must include tenant_id filtering.
  - _Why false positive:_ Same transaction first proves the conversation belongs to tenantId via conversations.id + tenant_id, so the subsequent messages-by-conversation read is scoped by that verified parent.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — Selecting messages for transcript: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ORDER BY created_at ASC — the query omits tenant_id. This reads the tenant-scoped messages table without constraining to the caller's tenant.
  - _Why false positive:_ Function first asserts conversation ownership and re-selects the conversation with tenant_id plus project ownership before reading messages for that conversation.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Backfill conv query contains an existence/count check that uses: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 — that subquery does not include tenant_id, so it reads the tenant-scoped messages table without tenant scoping.
  - _Why false positive:_ Backfill is a platform-wide housekeeping sweep; the count subquery is only eligibility for per-conversation processing and each row is handled with its own tenant_id.
- **server/auto-transcript.ts:160** _(system-sweep)_ — In the 'alsoLinked' backfill query the subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 is used without tenant_id constraint, causing an unscope read from the tenant-scoped messages table.
  - _Why false positive:_ Also-linked transcript backfill is a maintenance sweep using row-carried tenant_id per conversation/project and does not combine tenants' content in one output.
- **server/ceo-orchestrator.ts:661** _(global-table)_ — Unscoped read from tenant-scoped table: db.select().from(personasTable) loads all persona rows with no tenant_id filter. This can expose or mix persona records across tenants.
  - _Why false positive:_ schema note shows personas has no tenant_id; this read is from a non-tenant-scoped/global table
- **server/channel-routing.ts:30** _(global-table)_ — SELECT joins tenant-scoped personas table without any tenant_id constraint (SELECT cr.*, p.name as persona_name FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ...). This reads persona rows across tenants and can leak or mix tenant data.
  - _Why false positive:_ channel_routes is global by design and joined personas also has no tenant_id per schema note
- **server/chat-engine/project-context.ts:159** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id.
  - _Why false positive:_ project_files has no tenant_id and is child-scoped by project_id after project ownership was verified above
- **server/chat-engine/project-context.ts:171** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id.
  - _Why false positive:_ project_notes has no tenant_id and is child-scoped by project_id after project ownership was verified above
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table 'commitments' performed without a tenant_id filter (cross-tenant scan in scanAndEscalate: `SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`). This reads tenant data across all tenants and may expose tenant-scoped rows to contexts not authorized for those tenants.
  - _Why false positive:_ platform-wide overdue scan processes each row with its own tenant_id and emits only redacted admin digest data
- **server/data-protection.ts:149** _(system-sweep)_ — Background purge selects from tenant-scoped table `conversations` without restricting by tenant_id (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants, creating an opportunity to leak cross-tenant information and violates the invariant that tenant-scoped reads must be constrained.
  - _Why false positive:_ purge sweep intentionally scans expired rows across tenants and then handles each row using its carried tenant_id for scoped deletes/backup
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates by id alone with no tenant_id constraint. An attacker or bug could update another tenant's chunk by ID.
  - _Why false positive:_ updated ids come from the immediately preceding SELECT scoped by collection_id and tenant_id in the same call path
- **server/dormant-deprecation.ts:169** _(system-sweep)_ — SELECT COUNT(*) FROM messages ... (user messages) executes against the messages table with no tenant_id filter. messages appears tenant-scoped elsewhere; this reads cross-tenant data for traffic gating.
  - _Why false positive:_ global traffic-gate count is a platform-wide aggregate only and does not return or mix tenant content
- **server/dormant-deprecation.ts:179** _(system-sweep)_ — SELECT COUNT(DISTINCT tool_name) FROM tool_performance ... has no tenant_id constraint. tool_performance is tenant-scoped elsewhere, so this aggregates across tenants and can mix signals.
  - _Why false positive:_ Global maintenance gating uses aggregate counts only; no tenant content returned or mixed into tenant-facing output.
- **server/dormant-deprecation.ts:204** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance (getEverInvokedTools) runs with no tenant_id filter and will return tools across all tenants.
  - _Why false positive:_ Auto-deprecation candidate computation is a platform-wide maintenance routine over tool names, not tenant-facing row data.
- **server/dormant-deprecation.ts:214** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (getActiveTools14d) executes without tenant_id filtering, returning cross-tenant results despite tool_performance being tenant-scoped elsewhere.
  - _Why false positive:_ Global dormant-tool analysis intentionally scans recent activity across tenants for housekeeping and returns only aggregate/tool-name sets.
- **server/email.ts:370** _(global-table)_ — Query checks inbox_messages by message_id without constraining tenant_id: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1`. inbox_messages is tenant-scoped (INSERTs include tenant_id), so this global existence check can cause cross-tenant dedupe/skips and data mis-attribution.
  - _Why false positive:_ Polling a single shared inbox uses provider-global message ids for dedupe; flow is not scoped per tenant inbox.
- **server/email.ts:415** _(system-sweep)_ — Backfill query reads inbox_messages without tenant scoping: `SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...)` — no tenant_id or inbox_id filter. This potentially processes rows across all tenants.
  - _Why false positive:_ Backfill is a platform-wide housekeeping job filling empty body fields and does not expose mixed tenant data.
- **server/embeddings.ts:259** _(upstream-verified)_ — Use of sql.raw with an interpolated vector literal derived from the request/model embedding: `embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}`. The vec string originates from generateEmbedding(query) (request-derived / untrusted). Using string-interpolated sql.raw with request-derived content raises injection and isolation risks.
  - _Why false positive:_ Query is explicitly constrained by tenant_id = tenantId a few lines below; finding is injection-related, not cross-tenant isolation.
- **server/embeddings.ts:269** _(upstream-verified)_ — ORDER BY uses sql.raw with the same interpolated vector literal: `ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}` — raw interpolation of a request-derived value.
  - _Why false positive:_ ORDER BY uses raw vector literal but the same query already enforces tenant_id = tenantId; no cross-tenant read path shown.
- **server/embeddings.ts:585** _(upstream-verified)_ — Vector retrieval for agent_knowledge uses sql.raw with an interpolated vector literal derived from model embeddings: `1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) AS similarity` and `ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}`. This injects a request/model-derived value into raw SQL.
  - _Why false positive:_ Vector knowledge retrieval includes tenant_id = tenantId in the WHERE clause, so this is not a tenant-isolation issue.
- **server/embeddings.ts:814** _(system-sweep)_ — Backfill selects agent_knowledge rows without tenant scoping: `SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50`. This reads tenant-scoped rows across all tenants without an application-level tenant filter.
  - _Why false positive:_ Embedding backfill is a maintenance sweep across tenants, and each row carries its own tenant_id into the per-row update.
- **server/firecrawl.ts:239** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' omits tenant_id constraint in WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Why false positive:_ The row id being updated was selected immediately above with url + tenantId filter, then updated by that row's primary key.
- **server/firecrawl.ts:372** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scrapedPages' omits tenant_id constraint in WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Why false positive:_ The updated row id comes from the preceding tenant-scoped select on url + tenantId in the same call path.
- **server/google-drive.ts:866** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' uses only project id in WHERE (where(eq(projects.id, projectId))) — SQL lacks tenant_id constraint.
  - _Why false positive:_ Tenant ownership is checked immediately after the select and before any folder is returned or created for the caller.
- **server/google-drive.ts:879** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' omits tenant_id constraint in WHERE clause (.where(eq(projects.id, projectId))).
  - _Why false positive:_ Project row is read immediately above and explicit JS tenant ownership check rejects mismatches before this update.
- **server/google-drive.ts:1476** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' (resolving params.projectId) omits tenant_id in the SQL WHERE (where(eq(projects.id, params.projectId))).
  - _Why false positive:_ Same call path immediately checks proj.tenantId against params.tenantId and fails closed before Drive/file actions.
- **server/google-drive.ts:1721** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' used to verify project ownership does not include tenant filter in SQL (where(eq(projects.id, params.projectId))).
  - _Why false positive:_ This select is used only to fetch tenantId for an immediate equality check against params.tenantId before proceeding.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` is executed without any tenant_id constraint (reads across all tenants).
  - _Why false positive:_ Admin health-audit TTL sweep intentionally scans stale proposals across tenants and only archives/statuses them plus emits audit findings.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table `heartbeat_tasks` is executed without any tenant_id constraint (reads tasks across all tenants).
  - _Why false positive:_ Admin housekeeping sweep intentionally scans stuck heartbeat tasks across tenants and only disables stale tasks / reports findings.
- **server/heartbeat-schedules.ts:60** _(system-sweep)_ — SELECT * FROM research_schedules is executed without any tenant_id constraint. This reads tenant-scoped rows across all tenants in an un-scoped query.
  - _Why false positive:_ Scheduler loop intentionally enumerates due research_schedules across tenants and processes each row with its own tenant_id.
- **server/heartbeat-startup.ts:81** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executes without constraining tenant_id. heartbeat_tasks is tenant-scoped, so this update can modify rows belonging to other tenants if id is ambiguous or misused.
  - _Why false positive:_ Boot-time housekeeping normalizes heartbeat task model values across all tenants by row id; internal maintenance only.
- **server/heartbeat-startup.ts:85** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} has no tenant_id constraint. This mutates a tenant-scoped table without asserting ownership.
  - _Why false positive:_ Boot-time housekeeping normalizes reflection-task model values across all tenants by row id; internal maintenance only.
- **server/heartbeat-task-scheduler-storage.ts:11** _(system-sweep)_ — getDueHeartbeatTasksAcrossTenants() issues SELECT * FROM heartbeat_tasks ... without any tenant_id constraint. This returns tenant-scoped rows across all tenants from a function that is not request-scoped.
  - _Why false positive:_ Function is explicitly scheduler-only cross-tenant boundary returning due heartbeat tasks for per-row processing with carried tenantId.
- **server/heartbeat.ts:260** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... executes without a tenant_id constraint. event_log rows are tenant-scoped (tenant_id exists in the row interface), so this is a cross-tenant read of pending events.
  - _Why false positive:_ Heartbeat worker intentionally consumes pending events across tenants and routes each using the row's own tenant_id; not tenant-facing aggregation.
- **server/job-queue.ts:160** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_jobs` performs a global claim without scoping to a tenant: the UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) will select and move pending jobs for all tenants into 'running'.
  - _Why false positive:_ Global worker queue intentionally claims due agent_jobs across tenants; rows carry tenant_id for per-job isolated processing.
- **server/job-queue.ts:195** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_jobs` reclassifies expired leases to `failed_terminal` without any tenant_id filter. Similarly, the second UPDATE that requeues expired leases is unscoped. These mutations affect rows across all tenants.
  - _Why false positive:_ Lease-reclaim is cross-tenant worker housekeeping updating only job state fields, with no tenant-facing data mixing.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped table `projects` is performed without constraining to a specific tenant (no WHERE tenant_id = ...). This query aggregates counts across all tenants: `SELECT tenant_id, count(*)... FROM projects WHERE tags && ... GROUP BY tenant_id`.
  - _Why false positive:_ boot/backfill routine intentionally scans all tenants, aggregates counts by tenant_id only, and does not mix tenant content into one tenant-facing output
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — Global SELECT from tenant-scoped table deliverable_grade_events without a tenant_id filter (reads across all tenants).
  - _Why false positive:_ nightly optimizer scan intentionally groups by tenant_id and processes each tenant separately for maintenance/promotion generation
- **server/lib/system-probes.ts:163** _(system-sweep)_ — Unscoped SELECT against tenant-scoped table `messages` (db.select(...).from(messagesTable) has no WHERE tenant_id = ...). This reads cross-tenant data without an application-level tenant filter.
  - _Why false positive:_ system health probe performs global count only, returns aggregate count not tenant row data, and is an intentional platform-wide check
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the row id in WHERE (where(eq(tenantVoiceProfiles.id, existing.id))) and does not include a tenant_id constraint. This can bypass tenant scoping checks.
  - _Why false positive:_ update targets row returned by getVoiceProfile({ tenantId, profileName }); ownership is established earlier in same call path before id-only update
- **server/minds-engine.ts:501** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed without an explicit tenant_id constraint on the messages table. The query relies on a JOIN to conversations with c.tenant_id = ${tenantId}, but it does not include m.tenant_id = ${tenantId}. Per the project's invariant every read against a tenant-scoped table must include an explicit tenant_id filter.
  - _Why false positive:_ messages are joined through conversations with c.tenant_id = tenantId and filtered by that conversation, so tenant ownership is enforced in the same query
- **server/mpeg-engine.ts:1058** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. Tenant-scoped rows must carry tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ project_files has no tenant_id and is isolated transitively via tenant-scoped parent project after assertProjectInTenant check
- **server/mpeg-engine.ts:1445** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. Tenant-scoped rows must carry tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ project_files has no tenant_id and is isolated transitively via tenant-scoped parent project after assertProjectInTenant check
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads across all tenants.
  - _Why false positive:_ startup validation is a platform-wide maintenance loop over active subscriptions, handling each row with its own tenant_id rather than returning mixed tenant data
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — reads all tenants' rows.
  - _Why false positive:_ Boot-time startup validation intentionally sweeps active/global subscription state across tenants and then handles each row by its own tenant_id.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — reads all tenants' subscriptions (no tenant_id filter).
  - _Why false positive:_ Proactive token refresh is a cross-tenant maintenance loop over active subscriptions with per-row tenant_id used for subsequent refresh/update.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — no tenant_id filter; reads rows across all tenants.
  - _Why false positive:_ Daily force-refresh is an explicit platform-wide housekeeping sweep; each subscription is processed independently using its own tenant_id.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} — no tenant_id constraint when reading a persona. This reads a persona row without ensuring it belongs to the caller's tenant.
  - _Why false positive:_ Schema says personas has no tenant_id column, so reading by id is from a non-tenant-scoped table; tenant-scoped joins are filtered separately.
- **server/pace-control.ts:85** _(global-table)_ — Query against heartbeat_logs without tenant_id filter (COUNT FROM heartbeat_logs ... WHERE created_at > ... AND ${laneFilter}). This reads global heartbeat logs rather than scoping to a tenant.
  - _Why false positive:_ heartbeat_logs is explicitly non-tenant-scoped in schema and used for platform-wide pace gating metrics.
- **server/pace-control.ts:140** _(global-table)_ — Dashboard snapshot query SELECT COUNT(...) FROM heartbeat_logs WHERE created_at > ... — missing tenant constraint; it aggregates across tenants.
  - _Why false positive:_ Dashboard snapshot reads heartbeat_logs, which is a global table with no tenant_id by design.
- **server/plan-executor.ts:741** _(internal-worker-pk)_ — SELECT from tenant-scoped table `plans` without tenant_id constraint (SELECT source FROM plans WHERE id=${planId} LIMIT 1). This reads a tenant-scoped row by id with no tenant filter and can leak or allow cross-tenant interaction before ownership is validated.
  - _Why false positive:_ executePlan is an internal worker claiming a plan by server-side planId, then derives tenant from the claimed row rather than request input.
- **server/plan-executor.ts:744** _(internal-worker-pk)_ — UPDATE of tenant-scoped table `plans` without tenant_id constraint (UPDATE plans SET ... WHERE id = ${planId} AND status = 'approved' RETURNING ...). The update claims a plan for execution without constraining to a tenant, allowing cross-tenant modification if invoked with an arbitrary planId.
  - _Why false positive:_ This CAS update is part of an internal server worker operating on a globally unique plan primary key, not a tenant-facing request boundary.
- **server/plan-executor.ts:1279** _(internal-worker-pk)_ — claimStaleRecoveryChildCore performs UPDATE ... WHERE id=${planId} AND source='stale-plan-recovery' AND status='approved' RETURNING ... on tenant-scoped `plans` without a tenant_id filter. This can claim/return rows across tenants.
  - _Why false positive:_ claimStaleRecoveryChildCore is a server-invoked worker helper updating by globally unique plan id for internal recovery flow.
- **server/plan-executor.ts:1404** _(system-sweep)_ — resumeStuckPlans issues a broad SELECT over `plans` (SELECT id, tenant_id, status, plan_json, execution_log, version FROM plans WHERE status = 'executing' AND ... ) with no tenant filter. This iterates across all tenants' executing plans and may read/update tenant-scoped data without per-tenant authorization checks.
  - _Why false positive:_ Boot-time recovery intentionally sweeps stale executing plans across tenants and then carries tenant_id per row into subsequent scoped operations.
- **server/plan-executor.ts:1710** _(system-sweep)_ — reconcileStaleRecoveryChildren selects tenant-scoped rows without tenant filtering (SELECT id, tenant_id, parent_plan_id FROM plans WHERE source = 'stale-plan-recovery' AND status = 'executing' AND updated_at < ...). This reads plans across tenants and then performs tenant-scoped transactional updates later, creating a window for cross-tenant access.
  - _Why false positive:_ Recovery-child reconciler is a maintenance sweep over stale rows; each candidate carries its own tenant_id and later transactional updates are tenant-scoped per row.
- **server/process-governor.ts:899** _(upstream-verified)_ — UPDATE agent_desks executes WHERE id = ${desk.id} with no tenant_id constraint — allows updating a row belonging to another tenant if ids collide.
  - _Why false positive:_ desk.id comes from prior SELECT on agent_desks filtered by ad.tenant_id = tenantId in same call path.
- **server/recurring-messages.ts:266** _(system-sweep)_ — SELECT from tenant-scoped table agent_knowledge performed without tenant_id constraint (global scan). This returns rows across all tenants (called from heartbeat runner) and violates the invariant that every read against a tenant-scoped table must be constrained to the caller's tenant.
  - _Why false positive:_ heartbeat runner intentionally scans due jobs across tenants and carries each row's tenant_id for per-row scoped delivery.
- **server/recurring-messages.ts:190** _(system-sweep)_ — SELECT from tenant-scoped table agent_knowledge in listScheduledMessages may be executed without a tenant_id constraint when called with the `_allowGlobalSystemUseOnly` option, returning rows across tenants.
  - _Why false positive:_ explicit _allowGlobalSystemUseOnly path is documented internal cron use and returns rows with tenant_id for downstream per-row scoping.
- **server/research-engine.ts:466** _(fk-transitive)_ — INSERT into project_notes (project_id, note, author, created_at) omits tenant_id for a tenant-scoped resource. The table is project-scoped and should be tied to a tenant; inserting without tenant_id may mis-attribute notes across tenants.
  - _Why false positive:_ project_notes has no tenant_id and inserts are preceded by assertProjectInTenant(projectId, session.tenantId); isolation is via tenant-scoped parent project.
- **server/research-engine.ts:474** _(fk-transitive)_ — Looped INSERTs into project_notes for each finding omit tenant_id. These per-finding INSERTs write project-scoped data without recording tenant ownership.
  - _Why false positive:_ project_notes has no tenant_id and per-finding inserts attach to validated tenant-owned project via project_id.
- **server/routes.ts:979** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` without tenant_id constraint (owners lookup by filename uses no WHERE tenant_id = ...). This returns rows across tenants and then checks ownership in application logic, risking cross-tenant disclosure if the logic is changed or misused.
  - _Why false positive:_ this ownership lookup supports signed-URL/file delivery auth; code verifies authenticated tenant or valid signature and only checks filename owners, denying on mismatch.
- **server/routes/conversations.ts:119** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' performed without constraining by tenant_id (missing AND tenant_id = ${tenantId}).
  - _Why false positive:_ conv.id was just created with tenantId via storage.createConversation and project ownership was checked with tenant_id before update.
- **server/routes/conversations.ts:120** _(fk-transitive)_ — SELECT from 'project_conversations' omits tenant constraint (no WHERE tenant_id = <tenant>). If project_conversations is tenant-scoped this allows cross-tenant reads.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project/conversation rows already verified in this flow
- **server/routes/conversations.ts:123** _(fk-transitive)_ — INSERT into 'project_conversations' does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERTs.
  - _Why false positive:_ project_conversations has no tenant_id; insert is correctly scoped transitively via verified project and newly created tenant-scoped conversation
- **server/routes/conversations.ts:125** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' performed without constraining by tenant_id (missing AND tenant_id = ${tenantId}).
  - _Why false positive:_ same path already verified projects.id belongs to tenant via SELECT ... WHERE id = projectId AND tenant_id = tenantId
- **server/routes/conversations.ts:256** _(fk-transitive)_ — INSERT into 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) ...). If project_notes is tenant-scoped this will create rows with no tenant ownership recorded.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; projId was derived from tenant-checked conversation/project join
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_messages` lacks tenant_id constraint (missing WHERE tenant_id = <caller tenant>). This allows a requester to mark messages in other tenants as read.
  - _Why false positive:_ msg was loaded immediately above with WHERE id = msgId AND tenant_id = tenantId; update uses that verified row id
- **server/routes/projects.ts:401** _(fk-transitive)_ — INSERT into project_conversations does not include tenant_id. The table is a project-scoped relation and INSERT lacks an explicit tenant_id column/value.
  - _Why false positive:_ project_conversations has no tenant_id and relation is transitively scoped by tenant-owned project and conversation inserted with tenant_id
- **server/routes/projects.ts:609** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. The handler inserts a file record referenced to project_id but does not explicitly set tenant_id on the insert statement.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects after prior project ownership check
- **server/routes/projects.ts:616** _(upstream-verified)_ — UPDATE projects SET updated_at ... WHERE id = ${projectId} — missing tenant_id constraint. This UPDATE modifies a tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ same handler already verified project ownership with SELECT ... WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:676** _(fk-transitive)_ — INSERT into project_files (multipart upload) does not set tenant_id. File records are inserted with project_id but tenant_id is not passed in the INSERT.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped through previously verified tenant-owned project
- **server/routes/projects.ts:684** _(upstream-verified)_ — UPDATE projects SET updated_at ... WHERE id = ${projectId} — missing tenant_id constraint (multipart upload path). This mutating query lacks the required tenant filter.
  - _Why false positive:_ same handler already verified project ownership with SELECT ... WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:755** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} — DELETE lacks explicit tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id and delete is limited by tenant-verified project_id parent
- **server/seed.ts:42** _(system-sweep)_ — Unfiltered read on tenant-scoped table: `SELECT COUNT(*) as count FROM research_sessions` executes across all tenants (no tenant_id constraint).
  - _Why false positive:_ boot-time seed/import logic intentionally checks global table emptiness and does not expose mixed tenant data to any tenant-facing surface
- **server/seed.ts:2240** _(system-sweep)_ — Cross-tenant SELECT without tenant scoping: the query selecting conversations joined to projects does not constrain tenant_id to a caller tenant (no WHERE c.tenant_id = <tenant>). This will enumerate/act on conversations/projects across tenants.
  - _Why false positive:_ Boot-time backfill intentionally scans all tenants and only inserts missing child-link rows per conversation/project pair; no tenant data is returned or mixed across tenants.
- **server/seed.ts:3639** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without tenant_id constraint against a tenant-scoped table (heartbeat_tasks has tenant_id index). This will delete matching rows across all tenants.
  - _Why false positive:_ Boot-time housekeeping purge of stale auto-disabled heartbeat tasks runs platform-wide and only deletes housekeeping rows, with no tenant-facing data exposure or mixing.
- **server/seed.ts:3667** _(system-sweep)_ — UPDATE memory_entries performed without any tenant_id constraint (memory_entries is tenant-scoped). This will modify memory rows for all tenants.
  - _Why false positive:_ Idempotent boot-time data normalizer intentionally updates matching memory rows across tenants in place; no cross-tenant read surface or mixed output.
- **server/seed.ts:3693** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint while conversations is tenant-scoped. This will update conversations across all tenants.
  - _Why false positive:_ Global migration/backfill of legacy model ids is a platform-wide maintenance update on persisted rows, not a tenant-request path or mixed-tenant output.
- **server/self-reflection.ts:380** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses only the row id in the WHERE clause (WHERE id = ...), with no tenant_id constraint. This permits updating another tenant's memory row if the id is known.
  - _Why false positive:_ The row id being updated comes from the immediately preceding SELECT constrained by tenant_id = tenantId in the same call path.
- **server/self-improvement.ts:561** _(upstream-verified)_ — Unconstrained SELECT from tenant-scoped table `experiments`. When no category and tenant-scoped conditions are absent the function falls back to db.select().from(experiments).orderBy(...).limit(limit) returning rows across all tenants.
  - _Why false positive:_ Function explicitly requires either a positive tenantId or the ALL_TENANTS sentinel; the unscoped branch is the intentional explicit ALL_TENANTS path.
- **server/source-repair-handoff.ts:272** _(system-sweep)_ — Unscoped SELECT from the tenant-scoped plans table (SELECT id, tenant_id, source_ref, plan_json, execution_log FROM plans WHERE status = 'handoff_pending' ...). This reads rows across all tenants with no tenant_id filter, violating the requirement that reads against tenant-scoped tables must be tenant-constrained.
  - _Why false positive:_ Bounded reconciliation job intentionally reads pending plans across tenants, then processes each row with its own row-carried tenant_id and updates by id+tenant_id without mixing tenant outputs.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without any tenant_id constraint — deleting from a tenant-scoped table across all tenants.
  - _Why false positive:_ Watchdog cleanup is platform-wide housekeeping deleting old disabled delegation tasks only; it does not expose or combine tenant data.
- **server/storage.ts:1119** _(system-sweep)_ — fixStaleBackupSchedules runs an UPDATE on heartbeat_tasks without scoping by tenant_id — modifying tenant-scoped rows across tenants.
  - _Why false positive:_ Platform maintenance function repairs stale backup schedules across tenants by updating only scheduling fields on heartbeat tasks; no tenant data is returned or mixed.
- **server/tenant-fork.ts:226** _(internal-worker-pk)_ — UPDATE against a tenant-scoped table uses only the PK (WHERE id = p.newId) when re-pointing the self-ref. The UPDATE lacks a tenant_id filter.
  - _Why false positive:_ update targets freshly inserted rows by server-generated PK within same transaction; id is not from untrusted request boundary.
- **server/tools.ts:2932** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT columns (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) omit tenant_id, so a row can be created without explicit tenant ownership (or will fail if tenant_id is NOT NULL).
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped through owned parent project_id after prior project ownership check.
- **server/tools.ts:2946** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) lacks tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and is isolated transitively via project_id after prior tenant-scoped project ownership check.
- **server/tools.ts:2806** _(fk-transitive)_ — INSERT into `project_conversations` (ON CONFLICT DO NOTHING) during project creation does not set tenant_id. The row linking project↔conversation is created without an explicit tenant_id column/value.
  - _Why false positive:_ project_conversations has no tenant_id column; link row is transitively scoped by tenant-owned project/conversation and created only after tenant-scoped conversation update succeeds.
- **server/tools.ts:2970** _(fk-transitive)_ — INSERT into `project_conversations` in link_conversation handler does not set tenant_id. The code INSERTs (project_id, conversation_id) without tenant scoping.
  - _Why false positive:_ project_conversations has no tenant_id column; project and conversation ownership are verified in-branch before insert.
- **server/tools.ts:7006** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert is guarded by assertProjectInTenant on the parent project.
- **server/tools.ts:8978** _(upstream-verified)_ — UPDATE on tenant-scoped table proposed_skills uses only WHERE id = ... without constraining tenant_id.
  - _Why false positive:_ update uses row.id from the proposal row just inserted/returned in the same flow for tenant tid, not an independent untrusted lookup.
- **server/tools.ts:9007** _(upstream-verified)_ — UPDATE on tenant-scoped table proposed_skills (error handler) uses only WHERE id = ... without constraining tenant_id.
  - _Why false positive:_ same call path updates the just-created proposed_skills row by its returned id; tenant ownership is established when the row is created for tid.
- **server/tools.ts:9021** _(upstream-verified)_ — UPDATE on tenant-scoped table proposed_skills (promotion path) uses only WHERE id = ... without constraining tenant_id.
  - _Why false positive:_ acceptance update targets the same newly inserted proposal row carried in local variable row for the current tenant flow.
- **server/tools.ts:9038** _(upstream-verified)_ — UPDATE on tenant-scoped table proposed_skills (reject path) uses only WHERE id = ... without constraining tenant_id.
  - _Why false positive:_ rejection update also targets the same locally created proposal row; no separate attacker-controlled row id boundary is shown.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE statement modifies browser_workflows row by id without constraining tenant scope (missing WHERE tenant_id = <tenant>), allowing cross-tenant modification of another tenant's workflow record.
  - _Why false positive:_ workflow is selected immediately above with tenant_id = tenantId, and update uses workflow.id from that verified row in the same call path.
- **server/tools/domains/knowledge/handlers.ts:90** _(upstream-verified)_ — UPDATE against tenant-scoped table 'knowledge_triples' uses only 'WHERE id = ${row.id}' without constraining by tenant_id — allows a caller (or race) to modify rows belonging to other tenants.
  - _Why false positive:_ row.id comes from immediately prior SELECT on knowledge_triples scoped by tenant_id = tenantId, so updated ids are already tenant-owned in this call path
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys performed without constraining by tenant_id (no WHERE tenant_id = ...). This reads provider key rows across all tenants (even if only run for admin tenant), risking cross-tenant exposure of provider configuration.
  - _Why false positive:_ schema note says provider_keys has no tenant_id; query is admin-tenant gated and returns only provider/key length metadata
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — COUNT/aggregation on provider_keys performed without tenant_id constraint. Query enumerates provider configurations across all tenants with no tenant scoping.
  - _Why false positive:_ provider_keys is explicitly non-tenant-scoped per schema; admin-only aggregate over global provider config
- **server/tools/domains/strategic-memory/handlers.ts:99** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} — the UPDATE lacks an explicit tenant_id constraint. This allows modifying rows by id without ensuring the row belongs to the caller's tenant.
  - _Why false positive:_ id is loaded just above from memory_entries with tenant_id = tid and matching persona/category, then updated in same flow
- **server/tools/domains/strategic-memory/handlers.ts:159** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) — the UPDATE uses id list but omits tenant_id scoping, allowing updates to rows that may belong to other tenants.
  - _Why false positive:_ ids array is derived from rows selected immediately above with tenant_id = tid and persona/category filters before the bulk UPDATE
- **server/tools/domains/strategic-memory/handlers.ts:258** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} — missing tenant_id constraint on the UPDATE (same pattern repeats for strategic_win updates).
  - _Why false positive:_ id is obtained from prior SELECT on memory_entries constrained by tenant_id = tid, persona_id, and category in the same call path
- **server/webhook-triggers.ts:255** _(upstream-verified)_ — SELECT from tenant-scoped table `webhook_triggers` is performed without constraining by tenant_id (WHERE tenant_id = ...). This lookup uses only webhook_key and then proceeds to act on the returned row, which can leak or mis-authorize cross-tenant rows.
  - _Why false positive:_ webhook_key itself is the bearer capability; code intentionally loads all duplicates by key, fails closed unless exactly one row, then uses returned tenant_id for scoped side effects
- **server/chat-engine.ts:3926** _(upstream-verified)_ — Call to stuck-diagnostics.recordErrorForRetryStormDetection(conversationId, ...) is invoked without tenant context. If that function persists diagnostics it may write tenant-scoped rows without tenant_id filtering or without setting tenant_id on INSERT.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): recordErrorForRetryStormDetection is an in-memory diagnostics counter keyed by conversationId (stuck-diagnostics); it performs no DB read/write and returns no tenant data.
- **server/chat-engine.ts:4161** _(internal-worker-pk)_ — Call to stuck-diagnostics.recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is made without tenant/tenantId. The subsequent postDiagnosticReport([stuckPattern]) call also runs without tenant context. These telemetry/diagnostic functions may persist data without tenant_id or read referenced rows without verifying ownership.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): recordToolCallForStuckDetection is in-memory loop diagnostics keyed by conversationId; no DB write occurs.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE against tenant-scoped table 'commitments' executed without any tenant_id constraint (unscoped UPDATE: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`). This can mutate a row belonging to a different tenant if IDs collide or if row.id is attacker-controlled, breaking tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE FROM doc_chunks uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but does not include tenant_id. This can delete chunks belonging to other tenants sharing the same collection_id/doc_path.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/embeddings.ts:809** _(system-sweep)_ — Fallback path in storeEmbeddingVec performs an UPDATE using `sql.raw(table)` without any tenant constraint: `UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}`. That is an unscoped write against a tenant-scoped table (memory_entries or agent_knowledge) and can modify rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/reference-learner.ts:296** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${storedId} — write to tenant-scoped table memory_entries without tenant_id constraint. Although storedId was selected earlier under a tenant filter, the UPDATE by id alone can allow cross-tenant modification in races or misuse.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storedId used in the UPDATE at server/reference-learner.ts:290 is taken from the existing SELECT at reference-learner.ts:281-287 which filters WHERE tenant_id = ${input.tenantId} AND persona_id = ${personaId}. Ownership is proven in the same call path immediately before the write.
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result WHERE id = ${sessionId} has no tenant_id constraint — write to a tenant-scoped table missing tenant filter.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/second-opinion.ts:502** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without scoping by tenant_id. The function lacks a tenant parameter, so it may settle/modify ledger rows for other tenants by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — completeActivity issues an UPDATE on agent_activity without a tenant_id constraint (UPDATE agentActivity ... WHERE id = activityId). This can modify a row belonging to a different tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/auto-asset-capture.ts:207** _(system-sweep)_ — Selecting messages: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' — query omits tenant_id constraint. This reads the tenant-scoped messages table without scoping to the tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The message read is inside the per-conversation backfill loop; conversation_id=c.id (auto-asset-capture.ts:209) belongs to the row whose tenant_id was selected at line 185 and passed to captureProjectAssets at line 217. Maintenance sweep, not tenant-facing.
- **server/dfy-intake.ts:52** _(signed-url)_ — getDfyIntakeByToken performs a SELECT on dfy_intake_forms by token with no tenant_id constraint (returns a tenant-scoped row without scoping to caller tenant).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/job-queue.ts:335** _(internal-worker-pk)_ — cancelJob updates `agent_jobs` by id without restricting by tenant_id. Cancelling by id alone can mutate another tenant's job if misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:125** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `agent_trace_spans` performs WHERE span_id = ${spanId} with no tenant_id constraint — write may affect another tenant's rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — SELECT from tenant-scoped table `mcp_api_keys` performed without constraining by tenant_id (lookup by key_prefix only). This reads a tenant-scoped row without an application-level tenant filter, violating the invariant that all reads/writes must be constrained to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/mcp-api-keys.ts:166 — verifyApiKey compares the sha256 of the FULL presented key against key_hash in the same function (mcp-api-keys.ts:163-180); the credential itself is the access control and selects the tenant.
- **server/orchestrator-ledger.ts:451** _(upstream-verified)_ — Use of sql.raw(${lockKey}) inside SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). sql.raw() with a value derived from caller inputs (ledgerKey/tenantId) can lead to SQL injection or unexpected SQL execution.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): pg_advisory_xact_lock is a lock, not a tenant table. lockKey is a purely numeric BigInt(sha1).toString() (server/orchestrator-ledger.ts:35-38) — no injectable text. The ledger read/write in the same tx are tenant-scoped ('AND tenant_id = ${tenantId}' at :448).
- **server/process-governor.ts:1235** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at ... WHERE id = ${rule.id} lacks tenant_id constraint — may modify rules across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant_id filter; handler does not verify project ownership before using project data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has no tenant scoping — will iterate projects across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:352** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is not scoped by tenant_id — may return conversations from other tenants for the same project id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:361** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks tenant scoping — message reads may cross tenant boundaries.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/self-improvement.ts:434** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` is executed without constraining by tenant_id. The db.update(...) call uses .where(eq(experiments.id, experiment.id)) but omits tenant ownership check, allowing cross-tenant modification if an id is guessed or mis-bound.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:936** _(internal-worker-pk)_ — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} with no tenant_id constraint — modifying a tenant-scoped row without validating ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant constraint (no WHERE tenant_id = ...) — loads pairing rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} executed without constraining to caller/expected tenant. This allows cross-tenant lookup driven by untrusted data from agent_knowledge content.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT is itself the tenant-verification step — it compares the row's tenant_id to the claimed pairing tenantId and rejects mismatches.
- **server/venture-discovery/budget.ts:156** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger without tenant_id constraint (updates by id only). This allows modifying reservation rows across tenants if an id collides or is attacker-controlled.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:170** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger without tenant_id constraint (release updates by id only). This can clear or change other tenants' reservation rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.

### Deferred — accepted real risks (2)

_These are genuine findings under dated deferral. They still block republish readiness._

- **server/agentic/task-forces.ts:40** — createTaskForce inserts caller-supplied foreign IDs (project_id and persona_ids) into task_forces without verifying those referenced rows belong to the caller's tenant. personaIds are mass-inserted via personaIdArrayLiteral and projectId is accepted as-is.
  - _Deferral:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): createTaskForce inserts caller-supplied projectId without verifying project→tenant ownership; hardening = assertProjectInTenant before insert (row itself is tenant-stamped; reads are tenant-filtered, so exposure is a dangling FK association).
- **server/lib/scheduled-post-runner.ts:320** — Code constructs a Postgres text[] literal string (arrLit = `{...}`) from caller-supplied platforms and injects it into a SQL template. Manually building an array literal from request-derived values risks SQL/formatting injection and brittle parsing.
  - _Deferral:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): hand-built text[] literal from allowlist-checked platforms — injection-resistant today; refactor to parameterized bind (deferred item 5, campaign 2026-07-31).
