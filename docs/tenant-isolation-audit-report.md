# Tenant-Isolation Audit Report

_Generated 2026-07-15T01:51:23.541Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **536/536** file(s) audited · chunks 51/51 OK (0 failed)
- Findings: **295** (CRITICAL 87 · HIGH 172 · MEDIUM 36 · LOW 0)
- Precision second-pass (`gpt-5.4`): **139** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (87)

- **server/agentic/wake-scheduler.ts:79** — UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules ...) runs without any tenant_id constraint — this mutates agent_wake_schedules across all tenants.
  - _Fix:_ Constrain the UPDATE to the intended tenant(s). Either include tenant_id = ${tenantId} in the subquery/WHERE clause or iterate per-tenant. Ensure any returned rows are processed only for their tenant and subsequent UPDATEs use WHERE id = ${w.id} AND tenant_id = ${w.tenant_id} (as you already do later). Avoid wide cross-tenant UPDATEs.
- **server/agentic/runs.ts:39** — appendStep updates agent_runs using WHERE eq(agentRuns.id, runId) with NO tenant_id constraint — allows updating a run by id across tenants.
  - _Fix:_ Require and pass tenantId into appendStep and add a tenant constraint to the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). This prevents cross-tenant mutation by run id alone.
- **server/agentic/runs.ts:51** — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) but lacks tenant_id constraint — cross-tenant update risk.
  - _Fix:_ Add tenantId to the function signature and include eq(agentRuns.tenantId, tenantId) in the WHERE clause to ensure the update is scoped to the caller's tenant.
- **server/agentic/runs.ts:55** — replaceRunState calls .where(eq(agentRuns.id, runId)) without filtering by tenant_id — allows replacing state for a run id in any tenant.
  - _Fix:_ Add an explicit tenantId parameter and include a tenant filter (AND agent_runs.tenant_id = ${tenantId}) in the WHERE clause to enforce tenant scoping.
- **server/agentic/runs.ts:64** — completeRun updates agent_runs status/result WHERE id = runId but missing tenant_id in WHERE — unscoped mutation across tenants.
  - _Fix:_ Require tenantId in the call or fetch the run under tenant ownership before updating. Use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) to scope the update.
- **server/agentic/runs.ts:73** — failRun updates agent_runs (status, error, completedAt) using WHERE id = runId with NO tenant filter — cross-tenant write allowed.
  - _Fix:_ Include tenantId in the function and add eq(agentRuns.tenantId, tenantId) to the WHERE clause to ensure writes only modify rows belonging to the caller's tenant.
- **server/agentic/runs.ts:81** — pauseRun updates agent_runs status/state with WHERE id = runId but does not filter by tenant_id — allows pausing runs of other tenants by id.
  - _Fix:_ Add tenantId parameter and add tenant scoping to the WHERE (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all mutating helpers in this module enforce tenant ownership.
- **server/auto-memorize.ts:245** — Writes memory facts using a hard-coded tenantId = 1 (admin) when enqueueing LLM-proposed lessons: `enqueueMemoryFact({ tenantId: 1, ... })`. This causes tenant-specific memories to be written into the admin tenant instead of the originating tenant, resulting in cross-tenant data leakage/wrong ownership.
  - _Fix:_ Do NOT hardcode tenantId. Derive the tenant for each lesson from the source messages (or run the synthesizer per-tenant) and pass the correct tenantId into enqueueMemoryFact. If the LLM cost must be billed to an admin account, attribute cost separately (e.g. via runLlmTask tenantId param) but ensure the stored memory entries use the originating tenant's id and validate ownership before writes.
- **server/chat-engine.ts:4560** — extractMemory(...) is declared without any tenant context and proceeds to create/update memory-related rows. The helper generates DB inserts (createMemoryEntry) and embedding updates but never receives or propagates tenantId, so tenant-scoped writes cannot include an explicit tenant_id.
  - _Fix:_ Add tenantId as an explicit parameter to extractMemory(…) and ensure callers pass the caller's tenant id. All downstream storage calls in this function must include that tenantId (or perform a guarded lookup by id+tenant) so the INSERT/UPDATE SQL includes WHERE tenant_id = <tenantId> / explicit tenant_id column on INSERT.
- **server/chat-engine.ts:4586** — storage.createMemoryEntry({...}) inserts a memory entry but the object does not include tenantId. Per the project's invariant, every INSERT into tenant-scoped tables must set tenant_id explicitly (there is no RLS).
  - _Fix:_ Pass tenantId explicitly into the createMemoryEntry payload (e.g. { ..., tenantId }) or change the storage API to require tenantId and assert ownership. Ensure the underlying SQL INSERT sets the tenant_id column (or uses Drizzle .values with tenantId) rather than relying on derived/implicit ownership.
- **server/compaction.ts:400** — INSERT into tenant-scoped archive table (compaction_archives) omits tenant_id. The code inserts conversation_id/message_count/total_messages/content without supplying or validating a tenant_id, allowing archives to be associated with no tenant or a wrong tenant.
  - _Fix:_ Include and set tenant_id on the INSERT (or resolve the conversation→tenant mapping and insert that tenant_id). Validate the caller-supplied tenant/context before writing. Example fixes: add tenantId parameter to archiveMessages and INSERT tenant_id = ${tenantId}, or lookup the conversation row and write its tenant_id in the INSERT. Ensure tenant_id is non-null and validated before DB write.
- **server/data-protection.ts:72** — UPDATE on conversations omits tenant scoping — SQL updates deleted_at/deleted_by WHERE id = ${conversationId} with no tenant_id predicate. This can modify a conversation belonging to a different tenant if IDs are misused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}). Prefer using withTenantTx(tenantId, ...) for these operations so the transaction enforces tenant context, or at minimum pass tenant_id into the WHERE clause to fail-closed on ownership mismatches.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog performs UPDATE on tenant-scoped table delivery_logs without constraining by tenant_id (WHERE only checks id). This allows an attacker or buggy caller who can supply an id to modify another tenant's delivery row.
  - _Fix:_ Require and propagate the caller's tenantId into updateDeliveryLog and include it in the WHERE clause, e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). Validate caller ownership before updating.
- **server/delivery-pipeline.ts:682** — SELECT prior delivery by stripePaymentId queries delivery_logs without tenant_id filter (where eq(deliveryLogs.stripePaymentId, req.stripePaymentId)). Stripe payment id is caller-controlled/derived; this can return another tenant's delivery row and leak data or cause cross-tenant actions (re-delivery).
  - _Fix:_ Include tenant scoping in the query (e.g. add AND eq(deliveryLogs.tenantId, req.tenantId)) or otherwise validate that the returned row belongs to the caller's tenant before acting on it. Treat stripePaymentId lookups as tenant-scoped.
- **server/delivery-pipeline.ts:720** — UPDATE that nulls stripePaymentId on a prior delivery (db.update(deliveryLogs).set(...).where(eq(deliveryLogs.id, prior.id))) lacks a tenant_id constraint. This can modify another tenant's delivery row if prior.id is not tenant-checked.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND eq(deliveryLogs.tenantId, <callerTenant>)) and/or perform a prior ownership check before updating. Thread tenantId through this flow.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment SELECTs from delivery_logs where stripePaymentId equals paymentId with no tenant filter, returning a tenant-scoped row without validating caller ownership.
  - _Fix:_ Make this lookup tenant-scoped (require tenantId param or validate tenant ownership before returning). Add WHERE tenant_id = <callerTenant> to the query.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} without tenant_id. This will delete chunks across tenants that share collection_id/doc_path if tenant_id is not enforced elsewhere.
  - _Fix:_ Add tenant scoping to the DELETE: DELETE ... WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Ensure tenantId is validated and passed into the function.
- **server/embeddings.ts:684** — storeEmbeddingVec performs an UPDATE on tenant-scoped tables (memory_entries or agent_knowledge) without constraining by tenant_id (WHERE id = ${id} only). This allows cross-tenant writes.
  - _Fix:_ Require and propagate tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Prefer using Drizzle's typed builders (e.g. .where(eq(table.id, id), eq(table.tenantId, tenantId))) so writes are always scoped and callers must prove ownership before calling.
- **server/google-drive.ts:547** — SELECT from tenant-scoped 'projects' table is missing tenant_id constraint (reads project by id without WHERE tenant_id = ...). This can leak another tenant's project row to the caller.
  - _Fix:_ Constrain the SELECT by the caller's tenant (e.g. .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Alternatively, perform an assertProjectInTenant(projectId, tenantId) check before issuing the query and ensure every DB call that touches tenant-scoped tables includes tenant_id in the WHERE clause.
- **server/google-drive.ts:560** — UPDATE on 'projects' table lacks tenant scoping (UPDATE projects SET driveFolderId... WHERE id = <projectId> with no tenant_id filter). Even if ownership was checked earlier, every write against a tenant-scoped table must include tenant_id in the WHERE to prevent cross-tenant writes.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or use an assertion helper to verify ownership and then include tenant_id in the WHERE for the update to fail-closed on mismatch.
- **server/google-drive.ts:829** — SELECT from 'projects' to resolve project metadata is executed without constraining by tenant_id (reads a caller-supplied projectId with no tenant filter). This allows reading another tenant's project row.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId || callerTenantId)))) or call a helper that asserts project ownership before selecting sensitive fields.
- **server/google-drive.ts:1004** — SELECT from 'projects' (in searchDriveFiles) reads project by id without adding tenant_id constraint before relying on proj.tenantId for ownership checks. The raw SELECT itself must be tenant-scoped to avoid cross-tenant reads.
  - _Fix:_ Include tenant constraint on the SELECT (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or use a dedicated assertProjectInTenant(...) helper that performs a tenant-scoped lookup and fails if not owned.
- **server/health-audit.ts:333** — UPDATE on `code_proposals` (SET status = 'archived_stale') uses WHERE id = ANY(${ids}) with no tenant_id constraint — this can modify rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or ensure the operation is explicitly authorized as a global admin action and narrow the id set to the intended tenant before applying any mutation. Never perform cross-tenant UPDATEs without an explicit and auditable admin gate.
- **server/heartbeat.ts:52** — SELECT from research_schedules has no tenant_id filter (SELECT * FROM research_schedules WHERE ...). This returns schedules across tenants and allows cross-tenant reads/processing.
  - _Fix:_ Constrain the query to an explicit tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant via a storage API that asserts tenant context. If the intention is an admin/global scan, gate it explicitly (ADMIN_TENANT_ID) and document why a cross-tenant read is required.
- **server/internal-resolver.ts:170** — resolveExperimentFailed() performs an UPDATE on code_proposals WHERE id = ${proposalId} AND status = 'pending' with NO tenant_id constraint — this can modify/ archive proposals belonging to other tenants.
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. AND tenant_id = ${tenantId}) and/or fetch and validate the proposal's tenant prior to the state transition. Always include tenant_id in the WHERE clause for mutating tenant-scoped tables and treat the event-sourced proposalId as untrusted until ownership is confirmed.
- **server/lib/action-ledger.ts:305** — sweepStaleAttempts issues a cross-tenant UPDATE on action_attempts without a tenant_id predicate (UPDATE ... WHERE id IN (SELECT id FROM action_attempts ...)). This directly mutates rows across all tenants instead of constraining writes to a specific tenant or using per-row tenant-scoped transition(), violating tenant isolation.
  - _Fix:_ Do not perform a single multi-tenant UPDATE. Either (a) add an explicit tenant_id predicate to the UPDATE (and the inner SELECT) so only the caller's tenant rows are touched, or (b) implement the sweep as a paged, cross-tenant reconciler loop that selects candidate rows including tenant_id and then invokes the tenant-scoped transition(id, tenantId, ...) for each row (or calls markReconcilerDigested per-row). This preserves per-tenant scoping and the state-machine guards.
- **server/lib/inbox-ingest.ts:431** — SELECT on inbox_messages to find an orphan row omits any tenant_id constraint (WHERE tenant_id = ${tenantId}). The query `SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ON c.inbox_message_id = m.id WHERE m.message_id = ${id} AND c.id IS NULL LIMIT 1` can return a message row owned by a different tenant, allowing this runner to claim/process another tenant's inbox_message (and later insert an inbox_classifications row linking that message id to the current tenant).
  - _Fix:_ Constrain the lookup to the caller's tenant. For example: `... WHERE m.message_id = ${id} AND m.tenant_id = ${tenantId} AND c.id IS NULL ...` (or include tenant check in the JOIN). Ensure every read used to derive an id is filtered by tenant_id before any subsequent writes that reference that id.
- **server/lib/scheduled-post-runner.ts:62** — SELECT from tenant-scoped table `scheduled_posts` with no tenant_id constraint (CTE selects candidate rows across all tenants).
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant in admin code. If this is intentionally a system-wide runner, make the admin intent explicit and enforce an allowlist/check that the caller is an admin context; otherwise add tenant_id filtering so each query only touches a single tenant's rows.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts WHERE id = ${row.id} performs a write against a tenant-scoped table but omits tenant_id in the WHERE clause, allowing accidental modification of rows in another tenant if ids collide.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) to ensure the update only affects the intended tenant's row, or derive/verify the tenant from the prior SELECT and bind it into the UPDATE.
- **server/lib/scheduled-post-runner.ts:191** — UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' omits tenant_id in the WHERE clause; this can modify another tenant's row with the same id.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} to the WHERE clause (or otherwise verify the row's tenant before updating) so writes are constrained to the correct tenant.
- **server/memory-graph.ts:195** — INSERT into tenant-scoped table `memory_links` does not set tenant_id: `INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count) VALUES (...)`. Per project invariant, every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and set it from the caller's trusted tenant (e.g. `... (source_memory_id, target_memory_id, link_type, strength, confidence, source_count, tenant_id) VALUES (..., ${tenantId})`). Also ensure the prior existence check is tenant-scoped so you don't create links across tenants.
- **server/plan-executor.ts:116** — UPDATE plans in appendExecutionLog does not constrain by tenant_id (WHERE id = ${planId} only). This can mutate another tenant's plan if planId is mixed across tenants.
  - _Fix:_ Pass the plan's tenant_id into appendExecutionLog (or resolve it within a prior tenant-scoped SELECT) and add AND tenant_id = ${tenantId} to the WHERE clause. e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}.
- **server/plan-executor.ts:513** — CAS UPDATE on plans (claiming plan: UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved') is not scoped by tenant_id. A planId from another tenant could be claimed by this process.
  - _Fix:_ Include tenant scoping on the CAS update (AND tenant_id = ${expectedTenant}). If executePlan is called without tenant context, change the API to accept/verify tenantId or first SELECT the row and assert tenant ownership before performing any UPDATE.
- **server/plan-executor.ts:527** — UPDATE plans SET status='failed' WHERE id = ${planId} is executed without a tenant_id constraint (clears status for a plan by id only).
  - _Fix:_ Add tenant constraint: WHERE id = ${planId} AND tenant_id = ${tenantId}. Ensure executePlan has the tenantId available (from the claimed row) and use it for subsequent updates.
- **server/plan-executor.ts:725** — Final UPDATE of plans to set terminal status uses WHERE id = ${planId} only (no tenant_id constraint).
  - _Fix:_ Use the plan's tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) so updates cannot affect other tenants' plans.
- **server/plan-executor.ts:759** — Error-path UPDATE (set status='failed' WHERE id = ${planId} AND status = 'executing') lacks tenant_id constraint.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or otherwise gate the update by the plan's trusted tenant id obtained earlier.
- **server/process-governor.ts:239** — Query for cascading failures selects from heartbeat_logs without a tenant_id filter: SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ... . heartbeat_logs is tenant-scoped; missing tenant constraint allows cross-tenant aggregation/leakage.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so the failing-agents count is computed only within the caller's tenant.
- **server/process-governor.ts:595** — Within disable_task action, code runs UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without tenant constraint. Although t.id was selected from tenant-scoped rows earlier, the UPDATE by id alone can affect rows belonging to other tenants.
  - _Fix:_ Always include tenant_id in mutating statements: UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId}.
- **server/process-governor.ts:641** — restart_agent action updates agent_desks using WHERE id = ${desk.id} without tenant_id scoping (UPDATE agent_desks SET ... WHERE id = ${desk.id}). This can modify another tenant's agent_desks row if ids collide.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause. Make sure any state-changing UPDATEs always include tenant_id even if the row was read earlier in the same function.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant_id filter. Reading a project by id only without validating tenant ownership violates tenant isolation and can leak other tenants' project data.
  - _Fix:_ Require and pass an explicit tenantId into updateProjectBrain (and any callers) and add WHERE id = ${projectId} AND tenant_id = ${tenantId} when querying projects. Verify ownership before reading or writing related data.
- **server/project-brain.ts:332** — backfillProjectBrains: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 is unscoped and will enumerate projects across tenants.
  - _Fix:_ Restrict the backfill query to a tenant (e.g. WHERE p.tenant_id = ${tenantId}) or rework the interface to accept tenantId and process per-tenant to avoid cross-tenant enumeration.
- **server/proposal-verifier.ts:33** — verifyProposalById SELECTs from code_proposals WHERE id = ${proposalId} with no tenant_id constraint. The handler accepts a foreign id and reads the proposal without verifying that the caller/operation has rights to that tenant's proposal (cross-tenant exposure).
  - _Fix:_ Require tenant context for verification operations and constrain queries: SELECT ... FROM code_proposals WHERE id = ${proposalId} AND tenant_id = ${tenantId}. If the API cannot get tenantId, first SELECT tenant_id from the row and verify against the caller's tenant before proceeding.
- **server/proposal-verifier.ts:241** — persist() updates code_proposals SET verification_status/verification_details WHERE id = ${proposalId} with no tenant_id filter — it mutates the proposal by id only.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause. Persist should accept tenantId (or derive and verify it) and always scope updates to that tenant.
- **server/proposal-verifier.ts:372** — attemptRebaseOnDrift writes back to code_proposals (UPDATE ... WHERE id = ${proposalId}) without tenant scoping when marking a proposal rejected or when persisting the rebased diff. This allows cross-tenant mutation by id only.
  - _Fix:_ Add tenant_id = ${tenantId} to these UPDATE statements. Ensure attemptRebaseOnDrift receives or validates tenant ownership before mutating the proposal row.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge SET content = ... WHERE id = ${r.id} — the UPDATE uses only id and lacks tenant_id (or persona/category) constraint. This lets an attacker or misbehaving job modify another tenant's row by ID.
  - _Fix:_ Include tenant ownership conditions in the UPDATE (e.g. AND tenant_id = ${r.tenant_id}) — or better, include tenant_id AND category AND any other scoping columns. Never perform tenant-scoped updates using id-only unless the code has already strongly verified ownership prior to the update.
- **server/research-engine.ts:389** — DELETE FROM agent_knowledge is executed without constraining by tenant_id (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will delete autoresearch rows across all tenants.
  - _Fix:_ Scope the DELETE to the session/caller tenant (e.g. add AND tenant_id = ${tenantId}) or otherwise pass the tenantId into this routine so only that tenant's agent_knowledge rows are affected.
- **server/routes.ts:163** — SELECT from tenant-scoped table file_storage without any tenant_id constraint (restoreUploadsFromDb). This reads raw file data (fileStorage.data) for all tenants and writes files to a shared uploads directory, allowing cross-tenant data exposure.
  - _Fix:_ Scope the query to a specific tenant (e.g. .where(eq(fileStorage.tenantId, tenantId))) or iterate tenants explicitly. Avoid restoring all tenants' file blobs into a single shared uploads directory; restore per-tenant into tenant-scoped subdirs or perform this operation only under an explicit admin/maintenance context with validated admin identity and audited actions.
- **server/routes.ts:4332** — SELECT from tenant-scoped table `conversations` uses conversationId but omits tenant constraint. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` can return data for a conversation owned by another tenant because tenant_id is not checked.
  - _Fix:_ Constrain the read to the caller's tenant. Either include `AND tenant_id = ${tenantId}` in the SQL or, better, use the storage/Drizzle accessor that enforces tenant scoping (e.g. db.select(...).from(conversations).where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))). Also validate ownership before deriving related IDs (project_id).
- **server/routes.ts:4338** — INSERT into `project_notes` does not set tenant_id. The raw insert `INSERT INTO project_notes (project_id, note, author) VALUES (...)` writes to a tenant-scoped table without associating the row with the caller's tenant, allowing records to be created without correct tenant attribution.
  - _Fix:_ Include tenant_id in the INSERT and ensure the referenced project belongs to the caller's tenant. For example: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})`. Prefer using the storage layer or Drizzle ORM methods that require and validate tenantId and verify ownership of project_id before inserting.
- **server/routes/conversations.ts:98** — UPDATE on conversations omits tenant constraint. The handler updates conversations.project_id with `WHERE id = ${conv.id}` but does not include `AND tenant_id = ${tenantId}`.
  - _Fix:_ Constrain the UPDATE by tenant: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use the conversation's tenantId). Prefer using storage layer helpers that accept tenantId to ensure ownership checks.
- **server/routes/conversations.ts:99** — SELECT from project_conversations omits tenant constraint. The code checks for an existing project_conversations row using `WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` with no tenant filter, allowing cross-tenant information disclosure if IDs are guessed.
  - _Fix:_ Add tenant scoping to the SELECT: `WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}` or perform this check via a storage function that enforces tenant ownership.
- **server/routes/conversations.ts:102** — INSERT into project_conversations omits tenant_id. The INSERT `VALUES (${projectId}, ${conv.id})` does not set tenant_id, so a row may be created without tenant scoping (or assigned to a default), violating tenant isolation.
  - _Fix:_ Include tenant_id in the INSERT: `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conv.id}, ${tenantId})`. Prefer using a typed storage helper/Drizzle API that enforces tenant_id.
- **server/routes/conversations.ts:104** — UPDATE on projects omits tenant constraint. The handler runs `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` with no tenant check, allowing an attacker to update another tenant's project timestamp by guessing project_id.
  - _Fix:_ Scope the UPDATE to tenant: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}` (or use conv.tenantId). Use centralized storage/update functions that require tenantId.
- **server/routes/conversations.ts:228** — INSERT into project_notes omits tenant scoping. The archive insert `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id and thus writes into a tenant-scoped table without the caller's tenant context.
  - _Fix:_ Include tenant_id in the INSERT: `INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${projId}, ${note.slice(0,8000)}, ${'system:archive'}, ${conv.tenantId ?? tenantId})` or call a storage helper that takes tenantId and enforces ownership.
- **server/routes/heartbeat.ts:103** — UPDATE heartbeat_tasks to approve a task omits tenant constraint. The query `UPDATE heartbeat_tasks SET approval_status = 'approved' ... WHERE id = ${taskId}` lacks `AND tenant_id = ...`, allowing approval of tasks across tenants by id guessing.
  - _Fix:_ Restrict the UPDATE to the task's tenant: `WHERE id = ${taskId} AND tenant_id = ${task.tenantId || tenantId}` (or pass tenantId into the query). Prefer using storage APIs that accept tenantId and enforce ownership checks.
- **server/routes/heartbeat.ts:116** — UPDATE heartbeat_tasks to reject a task omits tenant constraint. The query `UPDATE heartbeat_tasks SET approval_status = 'rejected' ... WHERE id = ${taskId}` lacks tenant filtering and can affect other tenants' tasks.
  - _Fix:_ Add `AND tenant_id = ${task.tenantId || tenantId}` to the WHERE clause, or use storage-layer functions that require tenantId and ensure ownership validation.
- **server/routes/projects.ts:185** — INSERT into tenant-scoped table `project_notes` does not include tenant_id (values: project_id, note, author).
  - _Fix:_ Include the caller's tenant id on INSERTs into tenant-scoped tables (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})) or use a storage helper that attaches tenantId server-side. Do not rely on prior existence checks alone — persist tenant_id explicitly.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload path) does not include tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...).
  - _Fix:_ Add tenant_id to the INSERT values (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Prefer using a storage layer helper that enforces tenant_id for all writes to tenant-scoped tables.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multer upload path) does not include tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...).
  - _Fix:_ Same fix as above — include tenant_id in the INSERT or call a storage API that sets tenant_id. Ensure all file rows are written with the owning tenant attached.
- **server/sculptor.ts:94** — UPDATE on tenant-scoped table `sculptor_sessions` performed with WHERE id = ${session.id} but no tenant_id constraint — write is only guarded by the local session id, not the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or include tenantId in the query parameters). Prefer passing the tenantId explicitly into the function and require AND tenant_id = ${tenantId} on all writes to tenant-scoped tables.
- **server/sculptor.ts:101** — UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id} has no tenant_id filter — missing tenant-scoping on a write to a tenant-scoped table.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Always include tenant_id on UPDATEs to tenant-scoped tables or otherwise verify ownership before writing.
- **server/sculptor.ts:162** — UPDATE sculptor_sessions ... WHERE id = ${sessionId} in monitorSession lacks tenant_id constraint — a background monitor can update rows across tenants if id collisions occur.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (the function already receives tenantId). Ensure the UPDATE only affects rows for the intended tenant to enforce tenant isolation.
- **server/sculptor.ts:347** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} is missing a tenant_id constraint — write to tenant-scoped table without tenant-scoping.
  - _Fix:_ Restrict the UPDATE with tenant_id (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Pass tenantId into the updater and assert ownership before modifying tenant rows.
- **server/second-opinion.ts:407** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleFusionReservation) updates a tenant-scoped ledger row by id only, with no tenant_id constraint or tenant context — allows cross-tenant modification if reservationId is guessed or leaked.
  - _Fix:_ Require tenant_id for settlement operations (add tenantId to the function signature) and include AND tenant_id = ${tenantId} in the WHERE clause when updating agent_cost_ledger. Validate the reservationId belongs to the calling tenant before modifying it.
- **server/second-opinion.ts:426** — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseFusionReservation) performs a write to a tenant-scoped table without scoping by tenant_id or verifying the reservation's tenant.
  - _Fix:_ Include tenant scoping: accept tenantId in releaseFusionReservation and use WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Verify the reservation belongs to the tenant before updating to prevent cross-tenant ledger tampering.
- **server/seed.ts:3212** — INSERT INTO project_conversations (project_id, conversation_id) ... is executed without setting tenant_id into a tenant-scoped table — new rows may be created with no tenant association.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id}) ...) or derive and validate tenant ownership before inserting. Per the invariant, INSERTs must explicitly set tenant_id and not rely on defaults.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` may run without tenant_id constraint (detectStagnation builds a WHERE that can be only `category` when tenantId not supplied). This allows reading experiments across tenants.
  - _Fix:_ Always require and/or enforce tenantId for this query. Add `AND tenant_id = <caller tenant>` (or add eq(experiments.tenantId, tenantId) into the conditions and throw if tenantId is missing) so the SELECT cannot return rows from other tenants.
- **server/self-improvement.ts:521** — Unscoped SELECT from tenant-scoped table `experiments` in getExperimentHistory: when tenantId is not provided the function returns experiments across all tenants (db.select().from(experiments)... without tenant filter).
  - _Fix:_ Fail-closed or require tenantId. If tenantId is optional by design, return only non-tenant-scoped/global results or throw when tenantId omitted. Otherwise always include `WHERE tenant_id = <caller tenant>` in this query.
- **server/skill-seeker.ts:768** — listGaps contains branches that execute SELECTs against tenant-scoped `capability_gaps` without a tenant_id constraint (e.g. when only `status` is provided or when neither status nor tenantId is provided). These return rows across tenants.
  - _Fix:_ Require tenantId or restrict the code path to only return tenant-scoped rows. Always include `AND tenant_id = <caller tenant>` in SELECTs against capability_gaps, or throw / fail-closed when tenantId is not supplied.
- **server/storage.ts:390** — attachCitations() executes UPDATE messages SET citations = ... WHERE id = ${messageId} without any tenant_id constraint — this allows cross-tenant mutation by id alone.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}). Require the caller to supply/derive and validate tenantId before performing the update. Fail-closed if tenantId is missing.
- **server/storage.ts:472** — deletePersona() clears personaId on `conversations` and updates `memory_entries`/`daily_notes` using only personaId in the WHERE clause — no tenant_id scoping, so it affects all tenants.
  - _Fix:_ Scope these updates by tenant. Either accept/require a tenantId param and add eq(conversations.tenantId, tenantId) (and equivalents for memory_entries/daily_notes), or iterate per-tenant explicitly if the intent is global admin action. Do not perform tenant-scoped mutations without tenant filtering.
- **server/storage.ts:832** — archiveExpiredMemories() executes raw SQL UPDATE on `memory_entries` with no tenant_id constraint, archiving data across all tenants.
  - _Fix:_ If this job must run per-tenant, iterate tenants and run the update per tenant with WHERE tenant_id = ${tenantId}. Otherwise require an explicit admin context and audit/log it heavily. Do not run global updates on tenant-scoped tables without an explicit tenant filter.
- **server/storage.ts:849** — archiveStaleMemories() executes raw SQL UPDATE on `memory_entries` without tenant_id scoping, affecting all tenants.
  - _Fix:_ Same as above — run per-tenant with a tenant_id WHERE clause or require an explicit, auditable admin operation. Add tenant scoping to the SQL.
- **server/storage.ts:1272** — getAllDataForExport() selects entire tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) without tenant filters — exports data across all tenants.
  - _Fix:_ Restrict this function to an admin-only/export path and require explicit tenant filtering for tenant-scoped tables. If per-tenant exports are needed, accept tenantId and filter each SELECT by tenant_id. Add strong authorization and audit logging for any global export.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. The query `db.select().from(customTools).where(eq(customTools.name, parsed.name))` lacks a tenant filter and can return rows from other tenants.
  - _Fix:_ Include an explicit tenant filter on all reads of tenant-scoped tables. e.g. add `.where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))`. Alternatively, require/propagate the tenantId and use it in the query.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without tenant constraint. `db.select().from(customTools).where(eq(customTools.id, inserted.id))` can return a row from a different tenant if IDs collide or if attacker can guess an id.
  - _Fix:_ Always include tenant_id in WHERE clauses for tenant-scoped tables. Change to `.where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))` or otherwise validate ownership before returning the row.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` without tenant constraint. `db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id))` updates by id only and may affect another tenant's row.
  - _Fix:_ Restrict updates to the tenant. Add a tenant predicate: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. If tenantId is not available in this context, require it or derive/validate ownership before performing the UPDATE.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table `custom_tools` without tenant constraint. `db.delete(customTools).where(eq(customTools.id, tool.id))` deletes by id only and could remove another tenant's tool row.
  - _Fix:_ Include tenant_id in the DELETE predicate: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))`. Ensure revertToolRow receives/validates the tenantId or uses the tool's tenantId when deleting.
- **server/tool-learning.ts:365** — UPDATE against tenant-scoped table `custom_tools` without tenant constraint. `db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(eq(customTools.id, tool.id))` lacks tenant filtering and may increment usage for the wrong tenant's row.
  - _Fix:_ Always include tenant_id in UPDATE WHERE clauses. Change to `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))` or pass/validate the tenantId from the previously queried tool row before performing the update.
- **server/tools.ts:4687** — INSERT into `project_notes` omits tenant_id. Per invariant, tenant-scoped table INSERTs must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Ensure project ownership is asserted (assertProjectInTenant) before inserting and always pass the validated tenantId as a parameter.
- **server/tools.ts:4691** — INSERT into `project_files` omits tenant_id when saving presentation links. Tenant_id must be set on INSERT to avoid cross-tenant contamination.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${projectId}, ...)). Confirm project ownership via assertProjectInTenant before the insert and use parameterized Drizzle/sql templates to bind tenantId.
- **server/tools.ts:6465** — INSERT into `project_files` (audio upload path) omits tenant_id despite being a tenant-scoped artifact. The call relies on assertProjectInTenant but still fails the requirement that every INSERT set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Continue to assert project ownership before inserting.
- **server/tools.ts:6999** — INSERT into `project_files` (slideshow video) omits tenant_id. Every write to tenant-scoped tables must include the caller's tenant_id explicitly.
  - _Fix:_ Pass tenant_id in the INSERT (INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Ensure assertProjectInTenant validated ownership prior to the insert and use parameterized SQL/Drizzle expressions for tenant scoping.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` omits tenant constraint. The UPDATE uses WHERE id = ${id} alone, allowing cross-tenant row modification if an id from another tenant is supplied or discovered.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Include tenant_id (and persona_id if applicable) in the WHERE clause, e.g. WHERE id = ${id} AND tenant_id = ${tid} AND persona_id = ${personaId} (or at least AND tenant_id = ${tid}). Use the trusted ctx values (tid/personaId) already in scope.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` omits tenant constraint. The batch UPDATE uses WHERE id = ANY(${ids}) without scoping to tenant_id, allowing last_accessed/access_count bumps across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ANY(${ids}) AND tenant_id = ${tid} (and persona_id = ${personaId} if appropriate). Ensure ids are filtered/validated to the caller's tenant before performing the UPDATE.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` omits tenant constraint. The UPDATE uses WHERE id = ${id} alone (after finding an existing row with tenant filter), enabling cross-tenant modification if id is misused.
  - _Fix:_ Include tenant constraint on the UPDATE to enforce isolation, e.g. WHERE id = ${id} AND tenant_id = ${tid} AND persona_id = ${personaId}. Prefer double-checking tenant/persona when updating rows discovered earlier.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id constraint (loads pairing rows across all tenants). This allows cross-tenant enumeration/processing of pairings and is a direct cross-tenant isolation violation.
  - _Fix:_ Filter the query by tenant_id (or otherwise ensure the query is scoped to an explicit owning tenant). If the design requires checking candidate rows across tenants, replace the query with a safe, auditable flow: (1) avoid scanning all tenants, (2) validate ownership via a constrained JOIN or additional WHERE tenant_id = <expected tenant>, or (preferably) enforce pairing lookup only against a single trusted tenant context after an ownership proof (OTP) or operator-controlled provisioning path.

### HIGH (172)

- **server/ab-optimizer.ts:110** — SELECT from experiments without any tenant_id constraint (reads running ab_test rows across all tenants).
  - _Fix:_ Constrain the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or run the heartbeat per-tenant. Avoid scanning/operating on other tenants' rows without explicit tenant scoping/validation.
- **server/agentic-engines.ts:341** — SELECT from heartbeat_logs (gatherOperationalData) has no tenant_id filter — it reads heartbeat logs across all tenants.
  - _Fix:_ Add tenant scoping to heartbeat_logs queries (WHERE tenant_id = ${tenantId}) or otherwise ensure the operation intentionally and safely aggregates across tenants after explicit validation.
- **server/agentic-engines.ts:448** — SELECT recent heartbeat_logs in runPredictiveEngine (recentLogs) lacks tenant_id filtering — reads other tenants' logs.
  - _Fix:_ Include WHERE tenant_id = ${tenantId} in the query or scope the aggregation to the intended tenant before parsing/using results.
- **server/agentic-engines.ts:516** — SELECT heartbeat_logs (heartbeatPerformance) in runOptimizationEngine has no tenant_id constraint — aggregates task performance across all tenants.
  - _Fix:_ Filter heartbeat_logs by tenant_id (WHERE tenant_id = ${tenantId}) or otherwise restrict the dataset to the tenant being processed.
- **server/agentic-engines.ts:530** — SELECT heartbeat_tasks (taskConfig) in runOptimizationEngine does not include tenant_id — reads task configuration globally rather than per-tenant.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (or otherwise ensure per-tenant scoping) so task configuration is restricted to the tenant's rows.
- **server/agentic-engines.ts:160** — SELECT in retryPendingMinervaRouting queries ai_insights without a tenant_id filter (scans applied HIGH insights across all tenants).
  - _Fix:_ Either scope the sweep to a supplied tenant or validate tenant_id on each row and/or include a tenant_id predicate when appropriate. If a multi-tenant sweep is intended, make the behavior explicit and safe (e.g. operator-only job with auditing).
- **server/agentic-engines.ts:242** — UPDATE ai_insights in autoApplyEligibleInsights runs without any tenant_id constraint (updates 'new' insights across all tenants).
  - _Fix:_ Restrict the update to a tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. If a multi-tenant bulk operation is required, make it an explicit operator action and ensure correct authorization and auditing.
- **server/agentic-engines.ts:114** — SELECT id FROM plans WHERE source = ... AND source_ref = ... lacks tenant scoping — can match plans belonging to other tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT to ensure the idempotency check is performed within the intended tenant scope.
- **server/agentic-engines.ts:147** — UPDATE ai_insights SET action_taken ... WHERE id = ${insight.insightId} does not constrain by tenant_id — a caller-provided tenantId was available but not enforced in the UPDATE.
  - _Fix:_ Use WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId} (or pass and check the tenantId) so the update cannot affect rows from other tenants.
- **server/agentic/approvals.ts:123** — Bulk UPDATE agentApprovals to mark pending approvals expired has no tenant_id constraint — it will expire approvals across all tenants.
  - _Fix:_ Constrain the expiry update to a tenant (WHERE tenant_id = ${tenantId}) or implement per-tenant sweeps. If the intention is a global sweep, make that explicit and ensure proper authorization/auditing to avoid cross-tenant side effects.
- **server/agentic/department-budgets.ts:143** — Query scans department_budgets without constraining tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads tenant-scoped data across all tenants.
  - _Fix:_ If this is intended as an admin-wide sweep, make the admin intent explicit and protect the call (require an explicit admin credential/context) or move to a dedicated global store. Otherwise restrict the query with an explicit tenant filter (WHERE tenant_id = ${tenantId}) or accept an explicit tenantId parameter so the query cannot accidentally expose/process other tenants' budgets.
- **server/agentic/harness-adaptation.ts:71** — Query reads agent_trace_spans without a tenant_id constraint (SELECT ... FROM agent_trace_spans WHERE status IN (...) AND metadata->>'modelId' IS NOT NULL ...). If agent_trace_spans is tenant-scoped, this exposes/aggregates data across tenants.
  - _Fix:_ Either constrain the query to a specific tenant_id (WHERE tenant_id = ${tenantId}) passed explicitly, or (if this is an intentional cross-tenant admin operation) require an explicit admin scope and document/guard the action. Alternatively move tenant-agnostic metrics to a dedicated global table so tenant isolation is not violated.
- **server/agentic/resume-worker.ts:66** — SELECT joins tenant-scoped tables (agent_runs, agent_approvals) without constraining to a caller/worker tenant_id — the query can return rows across tenants.
  - _Fix:_ Limit the initial candidate query to a specific tenant (WHERE r.tenant_id = ${tenantId}) or change the design to iterate per-tenant explicitly. If this is an intentional cross-tenant system job, make that explicit and review access controls; otherwise add an application-level tenant filter (and pass tenantId into the function) so every returned row is constrained to the expected tenant.
- **server/auto-memorize.ts:159** — SELECT from messages (tenant-scoped) without any tenant_id constraint. The query pulls messages across all tenants: `SELECT m.role, m.content, m.created_at, c.title AS conv_title FROM messages m LEFT JOIN conversations c ON c.id = m.conversation_id WHERE m.created_at > ${windowStart} ...`
  - _Fix:_ Restrict the query to a single tenant before reading tenant-scoped rows. For example join/guard by conversation tenant: add `AND c.tenant_id = ${tenantId}` (and pass tenantId into the function) or run the job per-tenant. If the cross-tenant scan is intentional, document and gate it behind a platform-admin assertion and an explicit admin-mode flag.
- **server/auto-memorize.ts:100** — SELECT from memory_entries (tenant-scoped) without any tenant_id constraint in isDuplicate(). The function reads recent memory_entries across all tenants: `SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ...`
  - _Fix:_ Make isDuplicate tenant-aware: accept a tenantId argument and filter `WHERE tenant_id = ${tenantId} AND created_at > ...`. If the function must operate globally for admin analytics, explicitly separate that code path and gate it to an admin-only routine.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table conversations is performed without an explicit tenant_id constraint in the WHERE clause (UPDATE conversations SET project_id = ... WHERE id = ...).
  - _Fix:_ Include the tenant in the UPDATE WHERE clause to prevent cross-tenant writes. For example: UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id} (or AND tenant_id = ${tenantId}). Ensure any code-path that derives ids from earlier queries passes a trusted tenant and uses it in the WHERE clause.
- **server/business-tools.ts:84** — INSERT into tenant-scoped table `invoice_items` does not include tenant_id — new rows are created without an explicit tenant context.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)), or otherwise derive/validate the tenant before inserting so every invoice_item row is explicitly scoped to the correct tenant.
- **server/business-tools.ts:337** — SELECT in listContracts LEFT JOINs customers (cust) ON cust.id = c.customer_id but does not restrict the joined customers row to the tenant (no cust.tenant_id filter). This can expose another tenant's customer data if a contract references an id from a different tenant.
  - _Fix:_ Ensure the join enforces tenant isolation, e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}, or otherwise only select customer fields after verifying the referenced customer belongs to the same tenant.
- **server/capability-review.ts:491** — UPDATE capability_reviews sets reused=true WHERE id = ${pending.reviewId} … but the WHERE clause lacks a tenant_id constraint. This updates a tenant-scoped table row by id without confirming tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${pending.reviewId} AND tenant_id = ${tenantId} AND reused = false). To do that, persist tenantId in the pendingReuse structure when recording the review and accept/compute tenantId in markReviewReused so the DB update always verifies tenant ownership.
- **server/ceo-orchestrator.ts:821** — Unscoped SELECT on tenant-scoped table `personas` — cachedPersonas = await db.select().from(personasTable) reads personas for ALL tenants without filtering by plan.tenantId.
  - _Fix:_ Constrain the query to the current tenant (or cache per-tenant). e.g. use Drizzle/SQL WHERE tenant_id = :tenantId (or .where(eq(personasTable.tenantId, plan.tenantId))). Alternatively, pass tenantId into this routine and only load personas for that tenant. Also validate later uses of cachedPersonas (e.g. mapping persona → id) to ensure the chosen persona id actually belongs to plan.tenantId before using it to create/update tenant-scoped rows.
- **server/ceo-orchestrator.ts:1818** — getAllActivePlans() returns active plans for all tenants (no tenant restriction). This exposes multi-tenant in-memory data across tenants.
  - _Fix:_ Do not return cross-tenant data. Either remove this function from any tenant-visible surface, require admin privileges, or accept a tenantId and filter to only return plans where plan.tenantId === tenantId. Consider splitting activePlans by tenant to avoid accidental cross-tenant leaks.
- **server/channel-routing.ts:30** — listChannelRoutes() performs a JOIN to the tenant-scoped `personas` table (JOIN personas p ON p.id = cr.persona_id) without constraining personas by tenant_id. That returns persona names/ids across tenants.
  - _Fix:_ Ensure persona rows are resolved in the context of a tenant. Options: make channel_routes tenant-scoped (add tenant_id column) and filter both channel_routes and personas by that tenant; or accept a tenantId parameter to listChannelRoutes and add WHERE p.tenant_id = ${tenantId} (and/or cr.tenant_id = ${tenantId}). Also validate that any persona_id returned belongs to the requesting tenant before exposing it to callers.
- **server/chat-engine.ts:571** — SELECT from project_files uses only project_id (`WHERE project_id = ${projectId}`) and lacks an explicit tenant_id constraint. Under the codebase invariant tenant isolation must be enforced on every tenant-scoped table, not only via ownership provenance of projectId.
  - _Fix:_ Include an explicit tenant filter on the query (e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`) or join to projects and assert p.tenant_id = ${tenantId}. Prefer using Drizzle's where(eq(table.tenantId, tenantId)) for consistency.
- **server/chat-engine.ts:582** — SELECT from project_notes uses only project_id (`WHERE project_id = ${projectId}`) and does not include tenant_id. Relying on prior verification of projectId is not sufficient per the invariant that every access to tenant-scoped tables must include the tenant constraint.
  - _Fix:_ Constrain the query to the caller tenant (e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`) or explicitly join to a tenant-scoped projects row already validated. Use Drizzle's eq(projectNotes.tenantId, tenantId) where possible.
- **server/chat-engine.ts:631** — Query against compaction_archives: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id}` lacks tenant scoping. Even though conversation id was obtained earlier, every read against tenant-scoped tables must include an explicit tenant constraint.
  - _Fix:_ Add tenant scoping to the query (for example `AND tenant_id = ${tenantId}`) or join compaction_archives to conversations and require conversations.tenant_id = ${tenantId}. Use Drizzle's where(eq(compactionArchives.tenantId, tenantId)).
- **server/chat-engine.ts:649** — Query against messages: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id}` has no tenant_id constraint. Per the invariant, reads from tenant-scoped tables must include tenant scoping even when using an owner id obtained previously.
  - _Fix:_ Constrain the messages query to the tenant (e.g. `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}`) or join through conversations with an explicit tenant predicate. Prefer Drizzle-style where(eq(messages.tenantId, tenantId)).
- **server/chat-engine.ts:759** — Complex recent-work query (LEFT JOIN LATERAL subqueries) contains subqueries reading messages and compaction_archives without explicit tenant_id constraints (e.g. `(SELECT content FROM messages WHERE conversation_id = c.id ...)` and `(SELECT summary FROM compaction_archives WHERE conversation_id = c.id ...)`). The outer WHERE restricts conversations by tenant, but the invariant requires explicit tenant scoping on every tenant-scoped table access.
  - _Fix:_ Add tenant predicates inside the lateral/subqueries (e.g. `... FROM messages WHERE conversation_id = c.id AND tenant_id = ${tid}` and `... FROM compaction_archives WHERE conversation_id = c.id AND tenant_id = ${tid}`), or rewrite the subqueries to join tables with an explicit tenant filter. Use Drizzle eq(table.tenantId, tid) to enforce the constraint.
- **server/chat-engine.ts:4594** — storage.updateMemoryEmbedding(entry.id, emb) updates memory/embedding data using only entry.id and no tenant context. Without constraining by tenant_id in the UPDATE, a malicious or buggy entry.id could modify another tenant's row.
  - _Fix:_ Modify updateMemoryEmbedding to accept tenantId (or perform an initial verified lookup) and ensure UPDATE statements include WHERE id = <entryId> AND tenant_id = <tenantId> (or use Drizzle .where(eq(table.tenantId, tenantId))). If relying on entry.id, first SELECT the entry by id+tenant_id to validate ownership before any update.
- **server/chat-engine.ts:4328** — storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is called without passing tenantId. If attachCitations performs INSERTs into tenant-scoped citation tables, those writes are not explicitly constrained by tenant_id here.
  - _Fix:_ Pass tenantId to attachCitations or ensure attachCitations internally requires and uses tenantId. The implementation must include tenant_id on any INSERT or use WHERE tenant_id = <tenantId> when updating rows associated with a message id. If it derives tenant from message id, add an explicit verification step (lookup by id+tenant) before writes.
- **server/compaction.ts:465** — INSERT into memory_entries uses tenant_id from an optional/unchecked parameter (tenantId?: number). If tenantId is undefined/invalid this will insert NULL or an incorrect value into tenant-scoped memory_entries, violating tenant isolation and NOT NULL constraints.
  - _Fix:_ Require and validate tenantId before inserting memories. If tenantId is optional at call sites, change the signature to require a valid tenantId (number > 0) or skip persisting memories when tenantId is missing. Always pass a validated tenantId into the INSERT (and fail closed if not present).
- **server/credential-vault.ts:169** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} executes without constraining tenant_id. This updates a tenant-scoped row by id only, enabling cross-tenant updates if called with an id from another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause: WHERE id = ${id} AND tenant_id = ${tenantId}. Prefer verifying ownership via getCredential(id, tenantId) first (which already enforces tenant) and then perform the scoped UPDATE.
- **server/credential-vault.ts:176** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} executes without constraining tenant_id. Called after resolving a credential by site/tenant, but the UPDATE lacks tenant check and could affect another tenant's row if id values collide or are spoofed.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${cred.id} AND tenant_id = ${tenantId}. Alternatively, perform the UPDATE using the previously-validated credential row (e.g. use getCredential result) and ensure the tenantId is passed and validated.
- **server/data-protection.ts:125** — DELETE statements in permanentlyPurgeSoftDeleted remove messages/compaction_archives/conversations using only conversation_id (DELETE FROM messages WHERE conversation_id = ${conv.id} / DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} / DELETE FROM conversations WHERE id = ${conv.id}) with no tenant_id predicate.
  - _Fix:_ Include tenant scoping in the DELETEs (e.g. AND tenant_id = ${conv.tenant_id}) or run the purge per-tenant within a tenant-scoped transaction (withTenantTx) so deletes cannot affect rows outside the intended tenant. Ensure audit/logging and backups are performed before destructive multi-tenant operations.
- **server/delivery-pipeline.ts:984** — listDeliveries constructs a base query on delivery_logs and only applies tenant filtering if an optional tenantId is provided; when tenantId is omitted the query returns rows across all tenants.
  - _Fix:_ Avoid returning tenant-scoped rows when no tenantId is supplied from untrusted callers. Require an explicit tenant context for list operations or ensure only trusted internal callers can omit tenantId. Prefer enforcing tenant_id filter by default and make omission explicit and auditable.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot queries messages with SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' without tenant_id scoping — this computes platform-wide user message counts and may mix tenants.
  - _Fix:_ Scope traffic metrics per-tenant by adding AND tenant_id = ${tenantId} (or otherwise run per-tenant snapshots). If the intent is a global metric, make that explicit and ensure only trusted admin contexts run it.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot queries tool_performance with SELECT COUNT(DISTINCT tool_name) ... WHERE last_success_at > ... but no tenant filter, producing cross-tenant aggregation.
  - _Fix:_ Add tenant scoping (e.g. WHERE last_success_at > ... AND tenant_id = ${tenantId}) or only run global aggregations under a trusted admin context. Ensure tenant_id is required when this function is used for tenant-scoped decisions.
- **server/dormant-deprecation.ts:203** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with no tenant filter; it returns global tool usage history rather than tenant-scoped data.
  - _Fix:_ Include tenant_id in the WHERE clause (or accept tenantId param) so candidate computation works on a per-tenant basis. If global behavior is intended, restrict to admin-only use and document it.
- **server/dormant-deprecation.ts:214** — getActiveTools14d selects DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant scoping. This yields global active-tool lists rather than tenant-specific, breaking isolation assumptions.
  - _Fix:_ Add tenant_id = ${tenantId} to the query or require this function to be called only in a trusted/global admin context. Prefer per-tenant scoping for auto-deprecation logic.
- **server/email.ts:366** — pollSharedInbox checks for existence with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without tenant scoping. inbox_messages is tenant-scoped (tenant_id used on insert), so this read can cross tenants or return rows for other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause when probing inbox_messages (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}), or otherwise derive tenant from the inbox context and enforce it before querying.
- **server/email.ts:410** — backfillEmptyBodies selects id,message_id FROM inbox_messages WHERE body_text IS NULL OR body_html IS NULL ORDER BY id with no tenant filter — this scans inbox messages across all tenants and will operate on other tenants' messages.
  - _Fix:_ Restrict the backfill to a specific tenant (add AND tenant_id = ${tenantId}) or iterate per-tenant. Ensure background jobs that touch tenant-scoped tables always include tenant_id filtering or run under explicit admin-only global logic with proper safeguards.
- **server/embeddings.ts:688** — backfillMissingKnowledgeEmbeddings selects rows from agent_knowledge without any tenant_id filter. Reading tenant-scoped data without a tenant constraint can leak cross-tenant data to the backfill process.
  - _Fix:_ Either run backfills per-tenant (accept a tenantId and add WHERE tenant_id = ${tenantId}) or explicitly document and hardbind the intent (e.g. only operate on ADMIN_TENANT_ID) and guard the code path. Never SELECT tenant-scoped tables globally without an explicit deliberate admin/maintenance boundary and audit trail.
- **server/embeddings.ts:716** — backfillEmbeddingVecs selects memory_entries rows (SELECT id, embedding FROM memory_entries ...) without any tenant_id constraint, then later UPDATEs those rows without tenant scoping. Both the SELECT and UPDATE operate on tenant-scoped rows without tenant filtering.
  - _Fix:_ Perform these backfills scoped to a tenant (accept tenantId) or iterate tenants and run per-tenant. Ensure both SELECT and UPDATE include tenant_id = ${tenantId} (use Drizzle .where(eq(memory_entries.tenantId, tenantId))). Do not run ad-hoc cross-tenant SELECT/UPDATE on tenant-scoped tables.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id filter — a write to a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and ensure the surrounding flow knows which tenant it's operating on. Prefer Drizzle builder methods to avoid accidental raw SQL updates.
- **server/embeddings.ts:732** — backfillEmbeddingVecs selects agent_knowledge rows (SELECT id, embedding FROM agent_knowledge ...) without tenant_id in the WHERE clause, then performs updates — reading/writing tenant-scoped data without tenant scoping.
  - _Fix:_ Run backfill per-tenant or otherwise ensure tenant ownership is enforced (add WHERE tenant_id = ${tenantId}). Avoid global scans of tenant-scoped tables unless explicitly running a documented admin migration with appropriate safeguards.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without constraining by tenant_id. This updates tenant-scoped rows without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${row.id} AND tenant_id = ${tenantId}), and ensure callers supply and validate tenantId. Use Drizzle's typed API for updates to make tenant filters explicit.
- **server/felix-brain.ts:19** — rehydrateTaskStateOnBoot executes a global SELECT over agent_runs (SELECT ... FROM agent_runs WHERE status IN (...) AND created_at > ...) with no tenant_id filter, then hydrates an in-memory taskStateStore keyed only by conversationId. This reads other tenants' running runs and can mix cross-tenant state into an in-memory store (and lead to conversation id collisions across tenants).
  - _Fix:_ Scope the rehydration per-tenant: either accept a tenantId and include WHERE tenant_id = ${tenantId}, or iterate known tenants and rehydrate per-tenant, and store taskStateStore keys that include tenantId (tenantId + conversationId) to avoid collisions. Ensure callers/boot flow run this with explicit admin intent and audit logging if a global scan is required.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` is executed without any tenant_id constraint.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or, if this audit must run across tenants, explicitly gate it to an admin/system context and document that it is a global operation. Avoid unscoped selects on tenant-scoped tables in multi-tenant deployments.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — missing tenant_id constraint. Updating by id without also filtering on tenant_id can modify another tenant's row if ids collide or are mis-attributed.
  - _Fix:_ Include tenant protection in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage helper that enforces/asserts tenant context (assertTenantContext) before the update.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or perform the update via a tenant-aware storage layer that validates ownership/asserts tenant context.
- **server/heartbeat.ts:112** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Add tenant_id filtering (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call into storage APIs that enforce tenant scoping.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id constraint in recovery path as well.
  - _Fix:_ Ensure recovery updates include tenant scoping (AND tenant_id = ${sched.tenant_id}) or use a tenant-asserting storage method to persist next_run_at.
- **server/heartbeat.ts:257** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. heartbeat_tasks are tenant-scoped; updating by id without tenant guard risks touching another tenant's task.
  - _Fix:_ Use storage.updateHeartbeatTask or include AND tenant_id = ${t.tenantId} (or the asserted tenant) in the WHERE clause. Prefer a tenant-aware helper that calls assertTenantContext before executing SQL.
- **server/heartbeat.ts:261** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — repeated model-normalization UPDATE lacks tenant guard.
  - _Fix:_ Same as above: include tenant_id filtering (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call a tenant-aware storage API to perform the update safely.
- **server/heartbeat.ts:586** — SELECT * FROM event_log WHERE status = 'pending' ... — no tenant_id constraint. event_log rows are tenant-scoped; selecting pending events across tenants allows cross-tenant reads/processing without explicit tenant scoping.
  - _Fix:_ Either add a tenant_id filter (WHERE tenant_id = ${tenantId} AND status = ...) and process per-tenant, or make the cross-tenant intent explicit and gate it to an admin/runtime-check. Prefer using a storage API that enforces or validates tenant context for event processing.
- **server/heartbeat.ts:1642** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 — no tenant_id filter. heartbeat_logs are tenant-scoped and this read can surface another tenant's logs.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE task_name = ... AND status = ... AND tenant_id = ${tenantId}) or call a tenant-aware storage function. If this query is intentionally global (admin task), require an explicit admin tenant and document/guard it.
- **server/heartbeat.ts:2948** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id filter. This write can affect tasks across tenants if ids are not uniquely guarded by tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or invoke a storage/update helper that asserts tenant ownership before changing approval_status.
- **server/heartbeat.ts:3324** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — same missing tenant guard in delegateTaskFromChat path.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or use a tenant-aware storage API to set approval_status so updates cannot cross tenant boundaries.
- **server/internal-resolver.ts:93** — finalizeEvent() updates event_log without constraining by tenant_id — the UPDATE can modify event rows across tenants because there's no WHERE tenant_id = ... guard.
  - _Fix:_ Require and pass the tenant context into finalizeEvent and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise derive/validate the event's tenant before writing). Use a parameterized tenant check as part of the CAS guard so the resolver cannot finalize events belonging to other tenants.
- **server/internal-resolver.ts:107** — resolvePlanProposed() SELECTs plans by id with no tenant_id check. A foreign planId taken from the event is read without verifying the caller/event tenant owns that plan, allowing cross-tenant reads.
  - _Fix:_ Load/validate the plan row using a tenant-scoped query (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or first resolve the event's tenant and assert ownership before reading the plans table. Ensure decision paths only operate on plan rows after tenant ownership is verified.
- **server/lib/agent-trace.ts:129** — persistSpanClose issues UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} with no tenant_id constraint. agent_trace_spans is tenant-scoped and the UPDATE may affect another tenant's row if span_id is known/leaked.
  - _Fix:_ Include the tenant predicate in the UPDATE, e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}. Obtain tenantId from the same trusted context used at open time (currentTenantId() or store it with the span context) to ensure the close only updates the owning tenant's row.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE procedureEvolutionRuns WHERE eq(procedureEvolutionRuns.id, runId) is executed without constraining by tenant_id. procedureEvolutionRuns is tenant-scoped but the update uses only id, allowing cross-tenant modifications if an id is mis-used.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or pass and validate tenantId to the function and bind it into the WHERE. Always scope updates by tenant for tenant-scoped tables.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE procedureEvolutionRuns .where(eq(procedureEvolutionRuns.id, runId)) is used without tenant scoping when marking noop/done. Missing tenant predicate allows cross-tenant row mutation.
  - _Fix:_ Add tenant-level predicate (eq(procedureEvolutionRuns.tenantId, tenantId)) to the WHERE or carry runRow/tenantId and assert the tenant before updating. Prefer ANDing tenantId into all UPDATE/DELETE/SELECTs on tenant-scoped tables.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE procedureEvolutionRuns .where(eq(procedureEvolutionRuns.id, runId)) when finishing the run lacks a tenant_id condition. This allows changing a run row belonging to another tenant if runId is mis-bound.
  - _Fix:_ Add a tenant predicate to the WHERE (and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Require and validate tenantId for these operations; never update tenant-scoped rows by id alone.
- **server/lib/bwb-job-progress.ts:144** — setBwbPhase calls db.update(videoJobs).where(scope(jobId,...)). The scope() helper may omit a tenant_id predicate when BWB_TENANT_ID is not present, resulting in updates that are only pinned by jobId (unguarded cross-tenant write if env tenant ID is missing).
  - _Fix:_ Require a validated tenantId at callsite or make scope() fail/throw when tenant is unknown instead of silently warning. Ensure the WHERE always includes eq(videoJobs.tenantId, tenantId). If the code must support env-threading, fail-fast when BWB_TENANT_ID is absent rather than emitting tenant-free UPDATEs.
- **server/lib/bwb-job-progress.ts:169** — updateBwbChapters updates videoJobs using .where(scope(jobId,...)) which can omit tenant_id when BWB_TENANT_ID is not set, allowing cross-tenant mutation keyed only by jobId.
  - _Fix:_ Require/validate the tenantId and include eq(videoJobs.tenantId, tenantId) in the WHERE. Alternatively, reject the call when envTenantId() is null so no tenant-free writes occur.
- **server/lib/bwb-job-progress.ts:203** — completeBwbJob selects the current job row using .where(scope(jobId)) where scope may omit tenant_id if BWB_TENANT_ID is not present. A tenant-less SELECT on a tenant-scoped table is a tenant-isolation violation.
  - _Fix:_ Include tenant scoping in the SELECT (eq(videoJobs.tenantId, tenantId)). Require the caller to supply a tenantId or fail the operation when env tenant info is missing; do not perform tenant-ambiguous queries.
- **server/lib/bwb-job-progress.ts:211** — completeBwbJob performs db.update(videoJobs).set(set).where(scope(jobId)) without guaranteeing scope includes tenant_id. This can flip rows across tenants if BWB_TENANT_ID is absent.
  - _Fix:_ Ensure the UPDATE WHERE includes eq(videoJobs.tenantId, tenantId). Refactor to require an explicit tenantId parameter or abort when envTenantId() is null so updates are always tenant-scoped.
- **server/lib/bwb-job-progress.ts:230** — bumpBwbHeartbeat updates videoJobs with .where(scope(jobId,...)) which may lack tenant_id when BWB_TENANT_ID is not set, enabling tenant-ambiguous updates.
  - _Fix:_ Require and validate tenantId and include eq(videoJobs.tenantId, tenantId) in scope(), or make scope() throw/return an explicit error when tenant is unknown. Never issue UPDATEs on tenant-scoped tables without an explicit tenant constraint.
- **server/lib/bwb-job-progress.ts:254** — failBwbJob updates videoJobs with .where(scope(jobId, { notDone: true })) where scope may omit tenant_id, allowing the error/fail status to be applied across tenants keyed only by jobId if BWB_TENANT_ID is missing.
  - _Fix:_ Add an explicit tenant_id predicate to the WHERE clause. Enforce that envTenantId() returns a valid tenant id (or require a tenantId parameter) and fail/return rather than performing tenant-less writes.
- **server/lib/inbox-ingest.ts:521** — Query joins inbox_classifications (aliased c) to inbox_messages (aliased m) but only filters by c.tenant_id; the JOIN does not ensure the joined inbox_messages row belongs to the same tenant. The SELECT `FROM inbox_classifications c JOIN inbox_messages m ON m.id = c.inbox_message_id WHERE c.tenant_id = ${tenantId} ...` can surface inbox_messages from other tenants if IDs are mismatched (no FK/RLS enforced).
  - _Fix:_ Add an explicit tenant constraint on the joined table (either in the JOIN condition or the WHERE clause). Example: `JOIN inbox_messages m ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId}` or `WHERE c.tenant_id = ${tenantId} AND m.tenant_id = ${tenantId} ...`. Audit similar joins to ensure every tenant-scoped table is constrained to the caller's tenant.
- **server/lib/scheduled-post-runner.ts:267** — Building a Postgres array literal (arrLit) by interpolating user-supplied `platforms` into a string and then injecting it into SQL risks SQL-injection/isolation issues (string-interpolated SQL with request-derived data).
  - _Fix:_ Do not inline raw array literals built from request data. Use parameterized array binding (e.g. build the array from parameterized elements via sql.join/sql.array or use Drizzle's proper array binding/helpers) or insert platforms as a JSON/parameterized value and cast server-side. If inlining is unavoidable, rigorously escape and validate each element and prefer sql.raw only after safe escaping/allowlisting.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without a tenant_id constraint — the sweep reads rows across all tenants (tenant_id present in returned rows) which can leak cross-tenant information.
  - _Fix:_ Either restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or ensure this utility only runs in a privileged/admin context with explicit authorization. If the intent is to perform cross-tenant maintenance, document and gate it behind an admin-only pathway; otherwise add tenant filters to the SELECT.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without a tenant_id constraint — reads rows across all tenants and may expose tenant-specific data.
  - _Fix:_ Apply a tenant_id filter to the query (e.g. WHERE tenant_id = ${tenantId}) or ensure the function is explicitly executed only in an admin/trusted context. Prefer per-tenant scanning or an explicit admin-only bulk-scan path that is tightly controlled and audited.
- **server/memory-graph.ts:66** — UPDATE on tenant-scoped table `memory_categories` is performed without constraining to the caller's tenant (WHERE tenant_id = ...). The query `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}` can modify another tenant's row if id collisions occur or if called with an id from a different tenant.
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. `WHERE id = ${catId} AND tenant_id = ${tenantId}`), or pass tenantId into this function and validate ownership before updating. Ensure every UPDATE/DELETE against tenant-scoped tables includes an explicit tenant filter.
- **server/memory-graph.ts:126** — refreshCategoryCount executes an UPDATE that is not scoped to a tenant. The SQL `UPDATE memory_categories SET memory_count = (SELECT count(*)::int FROM memory_entries WHERE category_id = ${categoryId} AND status = 'active' AND deleted_at IS NULL) WHERE id = ${categoryId}` lacks tenant constraints both in the inner SELECT and the outer UPDATE.
  - _Fix:_ Accept and use tenantId in refreshCategoryCount and add tenant restrictions in both places, e.g. inner: `... FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND ...` and outer: `WHERE id = ${categoryId} AND tenant_id = ${tenantId}`. This prevents cross-tenant counting/updates.
- **server/memory-graph.ts:181** — SELECT against `memory_links` is executed without a tenant constraint: `SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ... LIMIT 1`. If `memory_links` is tenant-scoped this can reveal cross-tenant existence and/or allow cross-tenant checks.
  - _Fix:_ Constrain the SELECT to the tenant (e.g. include `AND tenant_id = ${tenantId}`) or join via a tenant-scoped `memory_entries` row to ensure the candidate link belongs to the expected tenant. Always scope read queries on tenant-scoped tables to the caller's tenant.
- **server/memory-graph.ts:393** — UPDATE on `memory_entries` is performed without constraining to tenant: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}`. This can update another tenant's entry if ids collide or if called with a wrong id.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. `WHERE id = ${mem.id} AND tenant_id = ${tenantId}`) or perform the update via an API that verifies the row belongs to the caller's tenant before mutating. Ensure all UPDATE/DELETE operations on tenant-scoped tables include tenant_id checks.
- **server/martech-bundle.ts:315** — UPDATE against tenant-scoped `tenant_voice_profiles` omits a tenant constraint: `.where(eq(tenantVoiceProfiles.id, existing.id))`. Relying on id alone can allow cross-tenant modification if ids are abused or mis-bound.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. `.where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, ${opts.tenantId})))` (or use the existing.id and tenantId pair in the WHERE). Alternatively, pass tenantId through and validate ownership before updating.
- **server/memory-intelligence.ts:399** — UPDATE on tenant-scoped `knowledge_triples` is performed without a tenant constraint: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}`. Although the id was selected earlier with tenant filtering, the UPDATE lacks an explicit tenant filter and could modify rows across tenants if misused.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. `WHERE id = ${c.id} AND tenant_id = ${tenantId}`) or otherwise ensure the update is executed only after verifying the row's tenant. Always include tenant_id in UPDATEs for tenant-scoped tables.
- **server/memory-intelligence.ts:273** — Unscoped UPDATE to `memory_entries`: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}` executed after classifyMemoryCategory. The UPDATE lacks a tenant filter and may modify entries from another tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `WHERE id = ${entry.id} AND tenant_id = ${tenantId}`) or use a storage API that enforces tenant ownership. Ensure every UPDATE/DELETE targets the caller's tenant.
- **server/memory-intelligence.ts:316** — Another unscoped UPDATE to `memory_entries`: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}` (in the update-path). This mutation is not constrained by tenant_id.
  - _Fix:_ Same fix as above: include `AND tenant_id = ${tenantId}` in the WHERE clause or perform the update via a tenant-aware storage method that asserts ownership before applying changes.
- **server/minerva-planner.ts:355** — UPDATE on tenant-scoped table `plans` performed without restricting to a caller tenant (missing AND tenant_id = ... in WHERE). This CAS/decision write can modify plans across tenants.
  - _Fix:_ Require/accept a tenantId for decidePlan (or otherwise resolve caller tenant) and include `AND tenant_id = ${tenantId}` in the UPDATE WHERE clause (and in the RETURNING/locking CAS). Verify ownership before changing status; fail if the plan does not belong to the caller's tenant.
- **server/minerva-planner.ts:459** — Rollback UPDATE `UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'` is performed without tenant scoping. This can rollback/modify a plan in a different tenant.
  - _Fix:_ Include tenant scoping on the rollback (e.g. `WHERE id = ${args.planId} AND status = 'revising' AND tenant_id = ${tenantId}`) and ensure the calling context supplies/validates tenantId before performing the rollback.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id constraint (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows across all tenants.
  - _Fix:_ If this is intended admin/maintenance code, make the cross-tenant nature explicit and ensure only an operator/admin context can run it. Otherwise restrict the query with WHERE tenant_id = ${tenantId} (or iterate per-tenant explicitly). Consider auditing/ID-ing this background task and documenting why cross-tenant access is required; prefer doing per-tenant queries to avoid leaking other tenants' data.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh performs SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE (no tenant_id filter) and then iterates refreshes across tenants. This reads and operates across all tenant rows without tenant scoping.
  - _Fix:_ If the background refresh must touch multiple tenants, run it in an explicit admin/maintenance context with strict access control and audit logging. Otherwise change the query to operate per-tenant (e.g. accept a tenantId and filter WHERE tenant_id = ${tenantId}) or iterate over a vetted tenant list retrieved from a safe source. Ensure the operation is authorized to touch other tenants.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions executes SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant scoping and then refreshes all tenants. This performs cross-tenant reads/writes on a tenant-scoped table.
  - _Fix:_ Same mitigation as above: require this routine to run only in an admin/maintenance mode, or change implementation to refresh only a specified tenantId or a vetted list. If global behavior is required, document and gate it behind strong operator-only authorization and audit trails.
- **server/pace-control.ts:85** — COUNT query against heartbeat_logs (global pacing check) has no tenant filter — the read aggregates across all tenants (no WHERE tenant_id = ...). This can leak cross-tenant activity and lets one tenant's activity affect another's pace enforcement.
  - _Fix:_ Make pacing tenant-aware. Accept a tenantId (or an admin flag) on checkPace and add a WHERE tenant_id = ${tenantId} clause to the FROM heartbeat_logs query (and any other aggregates). If the function is meant to be global/admin-only, enforce and document that explicitly (e.g. require an admin context) and do not use it for tenant-level decisioning.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs for totals and per-persona counts without any tenant scoping — it returns cross-tenant aggregates that can leak tenant activity and misrepresent per-tenant usage.
  - _Fix:_ If this is intended as an admin/global snapshot, restrict the API surface to admin-only callers and document it. For per-tenant dashboards, add a tenantId parameter and include WHERE tenant_id = ${tenantId} in the queries (and similarly scope the persona breakdown queries). Avoid returning cross-tenant metrics to non-admin callers.
- **server/plan-executor.ts:775** — resumeStuckPlans: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id runs without tenant scoping and will operate across all tenants.
  - _Fix:_ Either iterate per-tenant and apply the recovery per-tenant_id or add a tenant filter when called in a tenant-scoped context. If global recovery is required, document and harden the mechanism (audit, owner approval) — do not rely on row updates without tenant checks.
- **server/plan-executor.ts:785** — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is unscoped and returns plans from all tenants.
  - _Fix:_ Scope this listing to a tenant context or iterate by tenant. If the executor is intended to poll per-tenant work, change the query to WHERE tenant_id = ${tenantId} AND status = 'approved' or perform a secure cross-tenant orchestration with explicit tenant boundaries.
- **server/proactive-engine.ts:138** — SELECT persona_id FROM proactive_actions WHERE id = ${actionId} is executed without constraining tenant_id. This reads a tenant-scoped row by id only and can leak or return a persona from another tenant.
  - _Fix:_ Constrain reads by tenant: SELECT persona_id FROM proactive_actions WHERE id = ${actionId} AND tenant_id = ${tenantId}. Verify tenant ownership before using returned persona_id.
- **server/process-governor.ts:976** — logAction updates governance_rules LAST_TRIGGERED/trigger_count using WHERE id = ${rule.id} with no tenant_id constraint. This mutates a governance rule by id only and may affect a different tenant's rule.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (and verify the rule belongs to the tenant before mutating). All governance_rules mutators must include tenant scoping.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} does not include tenant scoping. Conversations are tenant-scoped and should be filtered by tenant_id.
  - _Fix:_ When selecting conversations, include tenant_id (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or ensure proj was loaded with tenant ownership and propagate tenantId into these queries.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without a tenant filter. Messages are tenant-scoped; reading by conversation_id only may cross tenant boundaries if conversation ids are not globally unique per-tenant.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or ensure the conversation lookup is tenant-scoped and pass tenantId explicitly when querying messages.
- **server/recurring-messages.ts:258** — Broad SELECT on agent_knowledge (LIMIT 500) without any tenant_id restriction returns rows across all tenants. This enumerates tenant-scoped data and can lead to cross-tenant actions if further code updates rows without owner checks.
  - _Fix:_ Scope the query by tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly in the cron runner. If a cross-tenant scan is necessary, keep it in an isolated internal-only path and ensure every subsequent write/update enforces tenant ownership in its WHERE clause.
- **server/reference-learner.ts:290** — UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId} — update uses id only and does not constrain by tenant_id and persona_id, allowing cross-tenant modification if id is known or deduplicated row matched incorrectly.
  - _Fix:_ Constrain the UPDATE by tenant_id and persona_id (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}). Alternatively, verify ownership earlier and perform updates via a function that always binds tenant_id to prevent accidental cross-tenant writes.
- **server/quarterly-intelligence.ts:343** — CREATE TABLE model_registry_updates defines tenant_id INTEGER NOT NULL DEFAULT 1. Introducing a DEFAULT (fallback to ADMIN tenant) breaks the project invariant that INSERTs must explicitly pass tenant_id (no default) and risks silent misattribution / cross-tenant writes when tenantId is omitted.
  - _Fix:_ Remove the DEFAULT and require tenant_id to be specified explicitly in every INSERT: define tenant_id INTEGER NOT NULL (no default). Also audit any code paths that INSERT into this table to ensure they always bind the correct tenantId parameter.
- **server/research-engine.ts:450** — SELECT from research_experiments uses only session_id and status (SELECT ... FROM research_experiments WHERE session_id = ${sessionId} AND status = 'keep') with no tenant_id constraint. research_experiments is tenant-scoped.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${session.tenantId}) or otherwise verify the session belongs to the caller's tenant before querying to enforce isolation.
- **server/research-engine.ts:459** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} is missing a tenant_id constraint. research_programs is tenant-scoped.
  - _Fix:_ Include tenant_id = ${session.tenantId} in the WHERE clause (or fetch the program earlier with tenant scoping and reuse that row) to ensure you only read the tenant's program row.
- **server/research-engine.ts:464** — SELECT summary FROM research_sessions WHERE id = ${sessionId} is not constrained by tenant_id. research_sessions is tenant-scoped.
  - _Fix:_ Constrain by tenant_id = ${session.tenantId} (e.g. WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}) or require/re-verify the caller's tenant before selecting session data.
- **server/research-engine.ts:468** — INSERT INTO project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...)). Project-scoped tables must receive explicit tenant_id per the codebase invariant.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)). Do the same for the similar INSERT inside the findings loop (around line 476).
- **server/research-engine.ts:310** — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} is executed without a tenant_id constraint. This can update another tenant's program row if ids collide or are misused.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE id = ${programId} AND tenant_id = ${tenantId}) to ensure only the caller's program row is modified.
- **server/research-engine.ts:820** — Multiple UPDATEs to research_experiments use only id (e.g. UPDATE research_experiments SET ... WHERE id = ${experimentId}) without verifying tenant ownership. Several such updates appear (line ~820 and in error branches), which can modify other tenants' experiment rows.
  - _Fix:_ Always include tenant_id = ${session.tenantId} in the WHERE clause for experiment updates (e.g. WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}), or re-query & validate ownership before performing writes.
- **server/research-engine.ts:549** — UPDATE research_sessions SET ... WHERE id = ${sessionId} (storing summary/status/etc.) is executed without a tenant_id constraint. research_sessions is tenant-scoped.
  - _Fix:_ Add tenant_id = ${session.tenantId} to the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}) to avoid cross-tenant updates.
- **server/research-engine.ts:951** — getResearchSessionStatus falls back to SELECT * FROM research_sessions WHERE id = ${sessionId} with no tenant_id check and the function has no tenantId parameter. This exposes/returns session rows without verifying caller ownership.
  - _Fix:_ Require the caller to pass tenantId (or otherwise determine caller tenant) and add AND tenant_id = ${tenantId} to the query. Do not query tenant-scoped tables by id alone without verifying tenant ownership.
- **server/research-engine.ts:1482** — Replay claim UPDATE uses UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL without constraining tenant_id, even though tenantId is available in the context. This can alter rows outside the intended tenant.
  - _Fix:_ Include tenant_id = ${tenantId} in the conditional UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId} AND replayed_at IS NULL) to ensure the atomic claim only affects the selected tenant's rows.
- **server/routes.ts:771** — SELECT over projects JOIN tenants (backfillProjectDriveFolders) queries the projects table without a tenant_id WHERE constraint. This reads all tenant projects and then performs per-project actions without restricting to an authorized caller/tenant.
  - _Fix:_ If this backfill is intended to run across all tenants, require an explicit platform-admin context (authenticated/admin-only action) and document/guard it. Otherwise, add a tenant filter (WHERE p.tenant_id = ${tenantId}) or iterate tenants under explicit admin authorization. Ensure any subsequent actions (creating Drive folders) are performed only for tenants authorized by the caller or under a verified platform-admin operation.
- **server/routes/admin-zombie-detector.ts:38** — Query reads from `agent_trace_spans` (a tenant-scoped usage/trace table) without constraining by tenant_id. This allows cross-tenant aggregation/exposure of usage data even though the handler is admin-gated.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise constrain the CTE to the requested tenant. If this endpoint must aggregate across all tenants, implement an explicit, audited aggregation path (with admin-only authorization and logging) rather than querying the tenant-scoped table without a tenant_id predicate.
- **server/routes/admin-zombie-detector.ts:54** — Query computes recent persona usage from `conversations` (tenant-scoped) without any tenant_id constraint. This reads cross-tenant conversation metadata and can leak tenant-specific activity to the caller.
  - _Fix:_ Constrain the `used` CTE (and the outer query) to the appropriate tenant (e.g. add AND tenant_id = ${tenantId} in the CTE WHERE clause) or otherwise validate/derive the tenant context before querying. If a global cross-tenant view is required, create an explicit, auditable aggregate and avoid direct selects from tenant-scoped tables without tenant_id.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages to mark message read omits tenant constraint. After selecting the message, the handler runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `AND tenant_id = ${tenantId}` — the SQL write is not tenant-scoped.
  - _Fix:_ Include tenant scope in the UPDATE: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`. Even if the row was loaded earlier, enforce tenant in SQL to satisfy the single-source-of-truth constraint and prevent accidental cross-tenant writes.
- **server/routes/projects.ts:319** — SELECT * FROM `project_files` WHERE project_id = ${projectId} has no explicit tenant_id constraint on the tenant-scoped table.
  - _Fix:_ Constrain reads against tenant-scoped tables with the tenant predicate (e.g. AND tenant_id = ${tenantId}) or use a join that guarantees the tenant predicate on the tenant-scoped table. Prefer an explicit tenant_id WHERE clause to avoid accidental cross-tenant reads.
- **server/routes/projects.ts:342** — SELECT file metadata FROM `project_files` (used by download) does not include a tenant_id constraint (WHERE project_id = ${projectId} AND file_path = ...).
  - _Fix:_ Add an explicit tenant_id = ${tenantId} predicate to the SELECT to ensure only files owned by the caller's tenant are accessible, even if the parent project was checked earlier.
- **server/routes/projects.ts:371** — DELETE FROM `project_files` WHERE id = ${fileId} AND project_id = ${projectId} lacks an explicit tenant_id predicate on the tenant-scoped table.
  - _Fix:_ Include tenant_id = ${tenantId} in the DELETE WHERE clause (or delete via a storage helper that enforces tenant scope) to prevent accidental cross-tenant deletes if IDs collide or checks are bypassed.
- **server/seed.ts:34** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id — this reads aggregate data across all tenants.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope the query to a specific tenant. If the intent is a per-tenant count, pass the tenantId explicitly and use a parameterized where clause (or Drizzle eq(table.tenantId, tenantId)).
- **server/seed.ts:2854** — SELECT id, name, model FROM research_programs WHERE is_active = true executes against a tenant-scoped table without a tenant_id constraint.
  - _Fix:_ Restrict the query to a tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or iterate per-tenant. Ensure any subsequent operations on returned rows also verify the tenant_id before making updates.
- **server/seed.ts:2859** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table by id only, with no tenant_id check — this can modify another tenant's row with the same id.
  - _Fix:_ Include tenant ownership in the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or fetch the row by id and validate its tenant_id before updating. Prefer using Drizzle's .where(eq(table.id, id), eq(table.tenantId, tenantId)).
- **server/seed.ts:3136** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... updates a projects row without scoping by tenant_id.
  - _Fix:_ Add a tenant constraint (e.g. AND tenant_id = ${tenantId}) or otherwise verify the project's tenant ownership before making the update to avoid cross-tenant modification.
- **server/seed.ts:3204** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... checks conversations (tenant-scoped) without any tenant_id constraint, potentially enumerating other tenants' data.
  - _Fix:_ Restrict the SELECT to the target tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or run the backfill per-tenant. Always include tenant_id in WHERE for tenant-scoped reads.
- **server/seed.ts:3204** — SELECT from tenant-scoped table 'conversations' with no tenant_id constraint (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...))
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. AND c.tenant_id = <tenantId>) or iterate per-tenant. Also ensure the correlated subquery checks tenant ownership (project_conversations/project rows) before acting.
- **server/seed.ts:3212** — INSERT into project_conversations uses project_id and conversation_id derived from an unscoped SELECT without validating tenant ownership (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id}))
  - _Fix:_ Before inserting, verify that both the project and conversation belong to the same tenant (SELECT ... WHERE id = ? AND tenant_id = <tenantId>). Prefer storing tenant_id on project_conversations and include it in the INSERT, or scope the INSERT to the tenant explicitly.
- **server/seed.ts:4402** — UPDATE against tenant-scoped table 'memory_entries' (normalizer) has no tenant_id filter (UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND fact LIKE ... )
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = <tenantId>) or run the update per-tenant to avoid modifying rows belonging to other tenants.
- **server/seed.ts:4492** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id constraint
  - _Fix:_ Scope metric/cleanup queries by tenant (e.g. WHERE tenant_id = <tenantId> AND ...) or iterate through tenants; do not run cross-tenant counts without explicit multi-tenant coordination.
- **server/seed.ts:4501** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks without tenant scoping
  - _Fix:_ Include tenant_id = <tenantId> in the WHERE clause (or otherwise ensure the operation is performed per-tenant) so tasks for other tenants are not changed inadvertently.
- **server/seed.ts:4512** — UPDATE heartbeat_tasks SET cron_expression = ... WHERE name = 'Self-Reflection' ... executes against heartbeat_tasks with no tenant constraint
  - _Fix:_ Add AND tenant_id = <tenantId> (or run per-tenant) so you only modify the intended tenant's heartbeat_tasks.
- **server/seed.ts:4528** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is not scoped by tenant_id
  - _Fix:_ Restrict the UPDATE by tenant_id or run the cleanup per-tenant to avoid disabling other tenants' tasks.
- **server/seed.ts:4551** — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE '%examine repository%' OR ...) AND enabled = true lacks tenant scoping
  - _Fix:_ Add tenant_id filtering to the WHERE clause or perform the operation only after verifying tenant ownership of matched rows.
- **server/seed.ts:4032** — db.insert(heartbeatTasks).values([...]) bulk-inserts default heartbeat tasks without setting tenant_id
  - _Fix:_ When inserting tenant-scoped rows, include tenant_id explicitly for each inserted object (or use a per-tenant insert path). Do not rely on implicit/global defaults; ensure each inserted row is tied to the correct tenant.
- **server/seed.ts:4099** — db.insert(heartbeatTasks).values({...}) (Model Scout insert) does not set tenant_id for a tenant-scoped table
  - _Fix:_ Include tenant_id in the object passed to db.insert, or run the seed in a context that ensures tenant scoping; validate that heartbeat_tasks is intended to be global before omitting tenant_id.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` uses `WHERE eq(experiments.id, experiment.id)` with no tenant_id constraint. This can update a row belonging to another tenant if IDs collide or are guessed.
  - _Fix:_ Constrain the UPDATE by tenant: include `AND tenant_id = <caller tenant>` (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) or load and verify the experiment's tenant before updating.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table `experiments` uses `WHERE eq(experiments.id, experiment.id)` with no tenant_id constraint (same pattern as main update in success path).
  - _Fix:_ Add tenant constraint to the WHERE clause on error updates as well (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) or otherwise verify ownership before updating status to avoid cross-tenant modification.
- **server/self-reflection.ts:366** — UPDATE memory_entries uses `WHERE id = <existing id>` without also constraining tenant_id. Although the id was discovered from a prior tenant-scoped SELECT, the UPDATE should still include tenant_id in its WHERE to prevent accidental cross-tenant modification if IDs are reused or logic changes.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. `WHERE id = <id> AND tenant_id = ${tenantId}`) or otherwise verify the row's tenant before performing the UPDATE.
- **server/skillify.ts:196** — SELECT from tenant-scoped table 'conversations' is executed without scoping to the caller's tenant (no WHERE tenant_id = ...). The code fetches the row by id and only afterwards checks conv[0].tenantId !== tenantId in application logic — this violates the requirement that every read must include an application-level SQL tenant filter.
  - _Fix:_ Add an explicit tenant filter to the query. For example: db.select().from(conversations).where(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId)).limit(1). Alternatively use the project's tenant-scoping helper (requireTenantScope/tenantScope) so the DB never returns rows from other tenants.
- **server/skillify.ts:203** — SELECT from tenant-scoped table 'messages' is executed without scoping to the caller's tenant (WHERE only filters by conversationId). This allows reading messages across tenants if conversationId is attacker-controlled or misused.
  - _Fix:_ Include the tenant constraint on the messages query as well, e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)). If the design relies on the conversation ownership check, still enforce the tenant filter at SQL layer to meet the invariant (use tenantScope or requireTenantScope helpers).
- **server/skillify.ts:223** — SELECT from tenant-scoped table 'personas' is executed without scoping to the caller's tenant (only filters by personas.id). If personas are tenant-scoped this permits cross-tenant reads.
  - _Fix:_ Constrain the lookup to the tenant: .where(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)). If personas are global, document that clearly; otherwise enforce tenant ownership prior to or within the SQL query (use tenantScope/assertProjectInTenant-style checks).
- **server/skillify.ts:91** — SELECT from tenant-scoped table 'messages' in distillIntent is executed without a tenant_id constraint (.where only checks conversationId). This allows cross-tenant reads of message content.
  - _Fix:_ Add tenant scoping to the query, e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)). Prefer using tenantScope/assertTenantContext helpers to ensure the SQL always includes tenant filters for tenant-scoped tables.
- **server/skillify.ts:331** — SELECT from tenant-scoped table 'messages' in skillifyConversation is executed without a tenant_id constraint (.where only checks conversationId). This permits reading messages across tenants.
  - _Fix:_ Include tenant scoping in the SQL: .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)). Enforce tenant scoping at the SQL layer (use tenantScope/requireTenantScope) rather than relying solely on subsequent application-side checks.
- **server/storage.ts:239** — getConversationUnscoped() performs a SELECT on tenant-scoped table `conversations` with no tenant_id constraint.
  - _Fix:_ Avoid unscoped reads in HTTP handlers. Require and pass tenantId everywhere or restrict this method to internal orchestration code only, and audit all callers. If an unscoped read is absolutely required, log and require a clear justification and ensure the caller enforces ownership before using the row.
- **server/storage.ts:248** — createConversation() inserts into tenant-scoped table `conversations` without enforcing that tenantId is provided; values(data) may omit tenant_id.
  - _Fix:_ Require and validate tenantId on the API/type level (assertValidTenantId) and ensure the insert always sets tenantId explicitly (e.g. .values({ ...data, tenantId })). Reject or fail-closed if tenantId is missing.
- **server/storage.ts:289** — attachCitationMetadata() runs a raw SELECT on `messages` (tenant-scoped) using id list but with NO tenant_id constraint, returning rows across tenants.
  - _Fix:_ Include a tenant filter in the query (e.g. AND tenant_id = ${tenantId}), or rework the caller to pass the message tenantId and use it in the WHERE clause. Avoid relying on raw unscoped selects against tenant-scoped tables.
- **server/storage.ts:524** — createMemoryEntry() inserts into tenant-scoped table `memory_entries` via values(data) without asserting tenantId is present.
  - _Fix:_ Require tenantId (assertValidTenantId) and ensure the insert uses the validated tenantId (e.g. .values({ ...data, tenantId })). Reject writes that omit tenantId.
- **server/storage.ts:506** — getMemoryEntry() can SELECT from tenant-scoped `memory_entries` with only eq(id) when tenantId is undefined (the tenant filter is optional and only warned about), enabling unscoped reads.
  - _Fix:_ Make tenantId mandatory for this API or fail-closed if tenantId is undefined. Always include eq(memory_entries.tenantId, tenantId) in the WHERE clause (or require callers to explicitly opt into an audited unscoped path with strict logging and access controls).
- **server/storage.ts:527** — updateMemoryEntry() updates `memory_entries` and will run without tenant scoping when tenantId is omitted (only warns via _warnUnscoped). This allows cross-tenant updates by id.
  - _Fix:_ Require tenantId and include eq(memory_entries.tenantId, tenantId) in the WHERE clause. Convert the warning mode to fail-closed for mutation paths (or assertValidTenantId) to prevent accidental cross-tenant writes.
- **server/storage.ts:542** — deleteMemoryEntry() updates status on `memory_entries` but, if tenantId is omitted, the WHERE clause lacks a tenant filter (only eq(id)), enabling cross-tenant mutation.
  - _Fix:_ Require tenantId for delete operations and include tenant scoping in the WHERE clause. Fail if tenantId is not provided or invalid.
- **server/storage.ts:549** — touchMemoryEntries() updates `memory_entries` based on id array and only conditionally adds tenant scoping. If tenantId is omitted, the update affects rows across tenants matching the ids.
  - _Fix:_ Require tenantId and include eq(memory_entries.tenantId, tenantId) when updating. Do not allow bulk updates on tenant-scoped tables without a tenant constraint.
- **server/storage.ts:579** — createConversationFact() inserts into tenant-scoped table `conversation_facts` via values(data) without asserting tenantId is present.
  - _Fix:_ Require and validate tenantId on the create path (assertValidTenantId) and always insert with the validated tenantId. Reject writes missing tenantId.
- **server/storage.ts:868** — getMemoryStats() will select all memory_entries and agent_knowledge across tenants when tenantId is undefined (does not enforce tenant scoping by default).
  - _Fix:_ Require tenantId on this stats API or fail-closed when tenantId is not provided. Add eq(...tenantId, tenantId) filters to both memory_entries and agent_knowledge queries so results are tenant-scoped.
- **server/storage.ts:893** — getRecentDailyNotes() will return daily_notes across tenants if tenantId is undefined (tenant filter is optional), leaking other tenants' notes.
  - _Fix:_ Make tenantId required for this API or include eq(daily_notes.tenantId, tenantId) unconditionally. Fail-closed if tenantId is missing.
- **server/storage.ts:910** — getHeartbeatTasks() returns heartbeat_tasks for all tenants when tenantId is not passed (tenant scoping optional), exposing other tenants' scheduled tasks.
  - _Fix:_ Require tenantId for tenant-scoped reads or always include eq(heartbeat_tasks.tenantId, tenantId) when operating in user contexts. If an admin needs cross-tenant visibility, provide a clearly labelled admin-only API with proper authorization.
- **server/storage.ts:928** — getHeartbeatTask(id) SELECTs heartbeat_tasks by id with NO tenant filter, returning a task across tenants by id alone.
  - _Fix:_ Add tenant scoping to the query (accept and require tenantId, then WHERE id = ${id} AND tenant_id = ${tenantId}). If an unscoped internal lookup is required, provide an auditable/unscoped method and restrict callers.
- **server/storage.ts:948** — updateHeartbeatTask() can UPDATE heartbeat_tasks without tenant scoping if tenantId is omitted — the conditional tenant check uses an IN(subselect) expression only when tenantId provided; otherwise the update is unscoped.
  - _Fix:_ Require tenantId and always include eq(heartbeat_tasks.tenantId, tenantId) (or equivalent) in the WHERE clause for updates. Avoid conditional subselect patterns that allow unscoped mutations.
- **server/storage.ts:958** — deleteHeartbeatTask() can DELETE heartbeat_tasks without tenant scoping if tenantId is omitted (same conditional pattern as updateHeartbeatTask).
  - _Fix:_ Require tenantId and include it in the WHERE clause (eq(heartbeat_tasks.tenantId, tenantId)). Do not perform deletes by id only on tenant-scoped tables.
- **server/storage.ts:980** — getDueHeartbeatTasks() SELECTs heartbeat_tasks (tenant-scoped) without any tenant filter, returning due tasks across all tenants.
  - _Fix:_ If callers must process tasks per-tenant, add tenant_id filtering. For multi-tenant workers, claim/processing should be per-tenant or run under an explicit admin context with strong audit/logging.
- **server/storage.ts:995** — claimHeartbeatTasks() updates heartbeat_tasks WHERE id = ${id} ... with NO tenant filter, allowing claiming tasks across tenants by id.
  - _Fix:_ Require and include tenantId when claiming tasks (WHERE id = ${id} AND tenant_id = ${tenantId}) or scope the worker to a tenant. Avoid updating tenant-scoped rows by id without tenant verification.
- **server/storage.ts:1009** — markHeartbeatTaskRun() UPDATEs heartbeat_tasks WHERE id = ${id} with no tenant_id constraint, allowing cross-tenant mutation by id.
  - _Fix:_ Add tenant scoping to the WHERE clause (eq(heartbeat_tasks.tenantId, tenantId)). Require tenant context for marking runs, or restrict to an admin context with appropriate checks.
- **server/storage.ts:1421** — supersedeAdr() performs UPDATEs on architecture_decisions without including tenant_id in the WHERE clause even though earlier SELECTs verified ownership; the writes themselves are unscoped.
  - _Fix:_ Add tenant scoping to the UPDATE statements (e.g. WHERE id = ${oldId} AND tenant_id = ${tenantId}) to make each write self-contained and safe against TOCTOU or caller mistakes. Do not rely solely on earlier SELECTs for ownership enforcement.
- **server/tools.ts:2967** — SELECT from tenant-scoped messages table executed without constraining by tenant_id (db.select(...).from(messagesTable) with no WHERE tenant_id = ...). This can return cross-tenant counts/data and violates the tenant-isolation invariant.
  - _Fix:_ Restrict the query to the caller's tenant. Pass the caller's tenantId into checkSystemStatus (or call a storage helper that is tenant-aware) and add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(messagesTable.tenantId, tenantId))). Example: db.select({count: sql`count(*)::int`}).from(messagesTable).where(eq(messagesTable.tenantId, tenantId)).then(...) or use an existing storage accessor like storage.getMessageCount(tenantId) so the DB never returns multi-tenant data.
- **server/tools.ts:4363** — SELECT from tenant-scoped table `memory_entries` without constraining by tenant_id (reads persona_id=2 slides QA lessons across all tenants).
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${params._tenantId} AND ...) or otherwise restrict to a validated tenant context. Prefer using Drizzle helpers (e.g. .where(eq(memoryEntries.tenantId, tenantId))) or pass params._tenantId as a parameter so results cannot leak across tenants.
- **server/tools.ts:4712** — SELECT from `presenter_sessions` uses only presentation_id and does not verify the session belongs to the caller's tenant. Missing tenant constraint allows cross-tenant session token reads.
  - _Fix:_ Verify ownership before reading (e.g. assertPresentationInTenant/presentation→tenant mapping) or include tenant_id in the WHERE clause. Use the project's tenant-guard helpers and parameterized SQL/Drizzle .where(eq(table.tenant_id, tenantId)) to ensure only same-tenant rows are returned.
- **server/tools.ts:6999** — INSERT into tenant-scoped table `project_files` omits tenant identifier. The INSERT SQL does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) ...).
  - _Fix:_ Include the tenant id explicitly in the INSERT (e.g. add tenant_id column with params._tenantId), or use the Drizzle schema object and set tenantId: params._tenantId. Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Do not rely solely on assertProjectInTenant for isolation — always persist tenant_id on tenant-scoped tables.
- **server/tools.ts:8960** — UPDATE against tenant-scoped table `proposed_skills` lacks a tenant filter. The code calls db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id)) without constraining by tenant_id.
  - _Fix:_ Add tenant scoping to the WHERE clause when updating tenant-scoped rows. For example: .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). If using raw SQL, include AND tenant_id = ${tid}. Always require the tenant_id match for UPDATE/DELETE on tenant-scoped tables.
- **server/tools.ts:8984** — UPDATE against tenant-scoped table `proposed_skills` lacks a tenant filter. db.update(...).where(eq(proposedSkills.id, row.id)) used to mark a row accepted/promoted without tenant constraint.
  - _Fix:_ Constrain updates by tenant. Use a two-column WHERE (id + tenantId) or a Drizzle where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) so only the owning tenant's row can be modified. Audit all updates/inserts to tenant-scoped tables to ensure tenant_id is present.
- **server/tools.ts:9001** — UPDATE against tenant-scoped table `proposed_skills` lacks a tenant filter. db.update(proposedSkills).set({status:'rejected', ...}).where(eq(proposedSkills.id, row.id)) runs without tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or include tenant_id in raw SQL). Ensure every read/update/delete on tenant-scoped tables includes tenant_id equality to enforce isolation.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files does not set tenant_id — tenant-scoped rows must include explicit tenant context on write.
  - _Fix:_ Include the caller's tenant id when inserting tenant-scoped rows (e.g. add tenant_id: ${ctx.tenantId} to the INSERT columns/values). Alternatively, validate ownership and write the tenant_id explicitly, or enforce in a DB constraint and populate from the verified project context before INSERT.
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects executed without scoping by tenant_id (WHERE id = ${projId} only). Without an application-level tenant filter this can update another tenant's row if projId is manipulated.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${projId} AND tenant_id = ${tenantId}). Even if projId was looked up earlier, always include tenant_id in UPDATE/DELETE statements (or perform the UPDATE inside a verification+transaction that guarantees the row belongs to the tenant).
- **server/tools/domains/browser/handlers.ts:165** — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} lacks a tenant filter. Application-level isolation requires WHERE tenant_id = caller_tenant for tenant-scoped tables.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${workflow.id} AND tenant_id = ${tenantId}). As a rule, ensure all UPDATE/DELETE/SELECT statements against tenant-scoped tables include an explicit tenant_id = ctx.tenantId guard or equivalent Drizzle .where(eq(table.tenantId, tenantId)).
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE against tenant-scoped table `knowledge_triples` omits tenant constraint. The code executes: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} — it does not include AND tenant_id = ${tenantId}, so a row id from another tenant could be updated if an id is guessed or leaked.
  - _Fix:_ Include the tenant filter in the UPDATE (defense-in-depth). Change the statement to include tenant_id in the WHERE clause, e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}. Alternatively, perform the UPDATE via a storage layer helper that enforces tenant scoping, and audit all writes to tenant-scoped tables to ensure tenant_id is always constrained.
- **server/tools/domains/security/handlers.ts:163** — Direct SELECT from provider_keys without constraining by tenant_id. The query SELECT provider, LENGTH(api_key) ... FROM provider_keys omits any WHERE tenant_id = ${tenantId}, meaning it can enumerate provider key records across tenants (even though executed inside an admin branch). This violates the project invariant that tenant-scoped tables must always be filtered by the caller's tenant.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId} to the WHERE clause) or, if the intent is an admin-global audit, make that explicit and enforce strong controls: verify admin privilege with an auditable check, avoid returning raw key material (or even key lengths), redact sensitive fields, and log the access. Prefer per-tenant queries wherever possible.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM provider_keys executed without a tenant_id filter. This aggregates provider key info across all tenants (admin-only branch aside) and thus reads a tenant-scoped table without constraining to a tenant.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) for non-admin callers. If the code is intentionally performing an admin-global audit, make the admin-only check explicit and documented, and ensure the operation is strictly limited/audited or performed by a dedicated admin-only routine that redacts or summarizes sensitive data rather than enumerating tenant rows.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` is executed without constraining by tenant_id when verifying a claimed conversation. Reads against a tenant-scoped table lack the required tenant filter.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${conversationId} AND tenant_id = ${expectedTenantId}) or pass tenantId into the query and use eq(conversations.tenantId, tenantId). Validate ownership in the DB query itself rather than relying only on comparing returned values in application code.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} updates a tenant-scoped table by id only, with no tenant_id constraint. A caller-supplied reservationId could cause an update to another tenant's ledger row.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or use a composite WHERE with eq(agent_cost_ledger.tenantId, tenantId)). Always constrain updates by tenant to enforce isolation.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} performs a tenant-scoped table update by id only, without tenant_id filtering, allowing a reservation release to affect another tenant's row.
  - _Fix:_ Require tenantId and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Validate that the reservation belongs to the expected tenant before modifying it.
- **server/video-job-runner.ts:228** — UPDATE video_jobs SET status = 'failed', ... WHERE status IN (...) AND updatedAt < <cutoff> updates tenant-scoped rows across all tenants with no tenant_id filter. This can mark other tenants' active jobs as failed and is a cross-tenant modification risk.
  - _Fix:_ Scope the stale-job recovery update to a single tenant at a time (include AND tenant_id = ${tenantId}), or iterate tenants explicitly and run the stale recovery per-tenant. If this is intended as an operator/global maintenance operation, require an operator-only path that is clearly separated from tenant-facing code and/or add explicit safeguards and logging.

### MEDIUM (36)

- **server/auth.ts:170** — Lookup of apiKeys uses a SELECT against the tenant-scoped api_keys table without constraining by tenant: `db.select().from(apiKeys).where(and(eq(apiKeys.keyHash, keyHash), eq(apiKeys.isRevoked, false)))`. This reads tenant-scoped rows without an explicit tenant_id filter.
  - _Fix:_ If possible, include the tenant constraint when the calling context can supply/derive it. If the lookup must be global (authenticating a presented API key), ensure this code path is explicitly reviewed and limited (e.g. index on keyHash, DB access audit, and strict handling of the returned key to avoid inadvertently exposing other tenant metadata). Prefer returning only the matched key and immediately scoping subsequent queries by the resolved key. Document this global lookup as an intentional and reviewed exception.
- **server/auth.ts:1139** — loadSessionsFromDb runs `SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions` and reads all auth_sessions across tenants into an in-process cache (no tenant constraint). This is a cross-tenant read of a tenant-scoped table.
  - _Fix:_ Avoid bulk-loading all sessions across tenants in normal operation. If this is an admin/maintenance operation, gate it behind a platform-admin check and an explicit admin-only code path. Alternatively, restrict the load to only sessions for tenants the process should serve, or page/stream by tenant to avoid uncontrolled cross-tenant exposure.
- **server/auto-asset-capture.ts:184** — Backfill query selects conversations across all tenants without filtering by tenant: `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...`. This reads tenant-scoped rows for all tenants in one sweep.
  - _Fix:_ Run backfill per-tenant or add a tenant filter (e.g. `WHERE c.tenant_id = ${tenantId}`) when called in a tenant-scoped context. If the backfill is intentionally cross-tenant maintenance, make the admin intent explicit and gate the operation to an admin-only maintenance job.
- **server/auto-memorize.ts:47** — getWatermark selects agent_knowledge rows without a tenant constraint: `SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} ...`. The corresponding setWatermark writes with tenant_id = 1, but the read is not scoped to that tenant.
  - _Fix:_ Scope the watermark read to the intended tenant (for example `AND tenant_id = ${ADMIN_TENANT_ID}`) or otherwise make explicit that the watermark is global and stored/read only under a documented admin tenant id. Ensure the read and write use the same tenant scoping to avoid picking up another tenant's row.
- **server/auto-transcript.ts:128** — SELECT subquery reads messages table without constraining by tenant_id (no WHERE tenant_id = ...). This is a tenant-scoped table read that lacks an explicit tenant filter.
  - _Fix:_ Constrain the subquery to the conversation's tenant. E.g. add AND tenant_id = c.tenant_id to the subquery: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0 ... — or otherwise pass the caller tenant and use WHERE tenant_id = ${tenantId}.
- **server/auto-transcript.ts:153** — SELECT (used to discover linked project_conversations) includes a subquery that reads messages without a tenant_id constraint. The subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) lacks tenant scoping.
  - _Fix:_ Scope the messages existence check by tenant (e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0), or rewrite the query to join through a tenant-scoped key/parameter so every access to messages includes tenant_id = <tenant>.
- **server/business-tools.ts:74** — INSERT INTO invoices sets customer_id from caller-supplied params.customer_id without validating that the referenced customer belongs to the caller's tenant.
  - _Fix:_ Validate ownership of params.customer_id before using it (e.g. SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} and fail if not found), or clear/disallow cross-tenant customer_id values. Never persist a foreign id supplied by the caller without verifying tenant ownership.
- **server/ceo-orchestrator.ts:1806** — getActivePlan(planId) returns an in-memory OrchestrationPlan without verifying the caller's tenant; callers can retrieve plans belonging to other tenants by ID.
  - _Fix:_ Require and enforce tenant context when returning active plans. For example change signature to getActivePlan(planId, tenantId) and return the plan only if plan.tenantId === tenantId (or otherwise fail). Alternatively store activePlans keyed/nested by tenant and only return per-tenant entries.
- **server/ceo-orchestrator.ts:1810** — getActivePlansForConversation(conversationId) returns plans matching conversationId across all tenants (no tenant check). A caller can enumerate or request conversation-scoped data from other tenants.
  - _Fix:_ Add tenant scoping (e.g. accept tenantId param and filter plans by plan.tenantId === tenantId) or otherwise ensure callers are authenticated/authorized and the conversationId is validated to belong to the caller's tenant before returning results.
- **server/chat-engine.ts:4012** — recordToolExecution(conversationId, toolName, !hasError) is invoked without tenantId. If that function writes to a tenant-scoped table (tool execution / telemetry), the write may lack an explicit tenant filter.
  - _Fix:_ Pass tenantId into recordToolExecution (or have recordToolExecution require tenantId) and ensure any INSERT/UPDATE includes tenant_id (or uses id+tenant verification). Audit lightweight telemetry helpers to require tenant context so application-level SQL filters are always used.
- **server/data-protection.ts:112** — SELECT on conversations in permanentlyPurgeSoftDeleted queries tenant-scoped table without a tenant_id predicate: SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}.
  - _Fix:_ If this is intended as a global admin purge, make that explicit and ensure only a privileged admin context can call it (and audit it). Otherwise constrain the query by tenant_id (or iterate per-tenant) or run under withTenantTx to ensure tenant isolation. Avoid broad SELECTs that return other tenants' rows unless explicitly required and access-controlled.
- **server/embeddings.ts:244** — vectorSearchMemory uses sql.raw with an interpolated vector literal (sql.raw(`'${vec}'::vector`)), where vec is derived from generateEmbedding(query) — a request-derived value. Interpolating request-derived values into raw SQL risks injection/isolation problems.
  - _Fix:_ Avoid interpolating request-derived strings into sql.raw. Prefer parameterized queries or Drizzle bindings; if raw interpolation is unavoidable (pgvector casting), validate/normalize thoroughly (as vecLiteral does) and then bind the value as a parameter or use a safe helper. Document and centralize the validation, and ensure the code path cannot be supplied arbitrary user strings.
- **server/embeddings.ts:559** — vectorSearchKnowledge uses sql.raw with an interpolated vector literal (sql.raw(`'${vec}'::vector`)) where vec originates from generateEmbedding(query) — a request-derived embedding. This is raw string interpolation into SQL with request-derived data.
  - _Fix:_ Replace string interpolation with a parameterized approach or a safe binding mechanism for pgvector. At minimum, centralize and prove the vec validation (vecLiteral) and avoid constructing SQL fragments by string concatenation; prefer Drizzle/driver parameter binding for dynamic values.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` is done with no tenant_id filter — if heartbeat_tasks is tenant-scoped this will enumerate tasks across tenants.
  - _Fix:_ If `heartbeat_tasks` is tenant-scoped, add WHERE tenant_id = ${tenantId} to limit results. If the table is intentionally global, document that fact and ensure this code is running under an appropriate system/admin context. Avoid ambiguous unscoped queries.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}) has no tenant_id constraint — mutates tasks without tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) if tasks are per-tenant. If the intent is a global operation, require an explicit admin/system authorization check and clearly document the global nature of the mutation to avoid accidental cross-tenant changes.
- **server/minerva-planner.ts:373** — SELECT probe `SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}` reads a tenant-scoped row without tenant scoping. This may leak plan state across tenants or be used to bypass tenant checks.
  - _Fix:_ Add tenant scoping to the probe SELECT (e.g. `WHERE id = ${args.planId} AND tenant_id = ${tenantId}`), or fetch the row via a verified call that ensures caller ownership before returning status/decision fields.
- **server/minerva-planner.ts:390** — SELECT for child plan `SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1` is missing tenant filtering. This can return a child plan from another tenant.
  - _Fix:_ Filter by tenant (e.g. `WHERE parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}`) so the child-plan probe only returns rows that belong to the caller's tenant.
- **server/minds-engine.ts:214** — INSERT into `mind_tickets` uses a string-built Postgres array literal for depends_on from params.dependsOn: `${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}` — this builds SQL-input from caller-derived values in-line.
  - _Fix:_ Avoid constructing SQL array literals by string-joining caller input. Bind a typed array parameter (e.g. pass params.dependsOn as a parameter/typed array), use driver/ORM array binding or JSONB storage, or validate/sanitize each element strictly as integer before building the parameter. Prefer Drizzle/parameterized array operations or use `= ANY($1::int[])` with a safe array bind.
- **server/minds-engine.ts:262** — Query uses `WHERE id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[])` where the array literal is built via string-join from ticket.dependsOn (which may ultimately be derived from user input). This is string-interpolated SQL content derived from data, increasing injection / parsing risk.
  - _Fix:_ Pass an array parameter rather than building a literal string. Use parameterized array binding or ORM helpers (e.g. `WHERE id = ANY(${ticket.dependsOn}::int[])` where ticket.dependsOn is passed as a bound array) or explicitly validate/normalize each id to an integer before binding.
- **server/minds-engine.ts:327** — markEventsHandled builds an array literal via `${`{${eventIds.join(",")}}`}::int[]` where eventIds come from caller input and is interpolated into the SQL template, creating a string-interpolated SQL fragment derived from request data.
  - _Fix:_ Avoid creating SQL array literals by concatenation. Bind eventIds as a proper array parameter, use ORM-provided array binding (or `WHERE id = ANY($1::int[])` with $1 bound to an integer array), or validate and sanitize every id before use to eliminate injection/parsing risk.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is performed without a tenant_id predicate and used to enumerate tenant IDs. This enumerates tenants with subscriptions.
  - _Fix:_ Avoid enumerating tenant rows without an explicit admin boundary. If enumeration is necessary for startup repair, restrict to a known list of tenants or require this code path to run under an explicit admin/maintenance mode. Alternatively include additional safeguards (explicit allowlist, environment flag) and log/alert when cross-tenant operations run.
- **server/mpeg-engine.ts:1132** — INSERT into project_files does not include an explicit tenant_id column (INSERT INTO project_files (...project_id, file_name, file_path, ...) VALUES (${options.projectId}, ...)). Per project-scoped/tenant-scoped invariant, every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and bind options.tenantId) rather than relying only on assertProjectInTenant for ownership. If project_files is intended to be scoped only by project_id, consider adding tenant_id as a NOT NULL column (and set it on insert) to make tenant ownership explicit and avoid accidental cross-tenant inserts.
- **server/mpeg-engine.ts:1498** — INSERT into project_files in produceVideoParallel similarly omits tenant_id in the VALUES clause when recording uploaded files for a project. This can lead to missing tenant scoping on writes to a tenant-scoped resource.
  - _Fix:_ Same as above: include tenant_id explicitly in the INSERT (use the validated tenantId after assertProjectInTenant) or change schema to require tenant_id on project_files and populate it on insert. Ensure inserts into any tenant-scoped table always provide tenant_id rather than inferring ownership only via foreign keys.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(${lockKey}) in the advisory-lock SELECT embeds a computed/raw SQL fragment derived from caller-provided ledgerKey via sql.raw(). Embedding request-derived values with sql.raw() can lead to SQL-injection-like risks or accidental SQL syntax issues.
  - _Fix:_ Avoid sql.raw for request-derived data. Pass the lock key as a bound parameter (or validate/convert to a numeric type first) instead of raw SQL. Example: compute a validated numeric BigInt lock value and bind it, e.g. await tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`); or use a parameterized tx.execute with explicit parameters so the DB driver performs safe binding. Ensure ledgerKey is canonicalized/hashed and validated before any interpolation.
- **server/recurring-messages.ts:182** — SELECT from tenant-scoped table agent_knowledge may execute without a tenant_id filter (the query conditionally omits AND tenant_id = ${tenantId}). This allows an unscoped/global result set when the caller passes the `_allowGlobalSystemUseOnly` flag or if the conditional logic is bypassed.
  - _Fix:_ Always require and bind tenant_id for reads against tenant-scoped tables, or make the global/unscoped path explicit & internal-only. If a global scan is required (cron runner), implement a separate internal-only function and strongly gate it (e.g. internal-only API, explicit callsite with audit/logging) and ensure callers cannot pass arbitrary request-derived values to trigger the unscoped path.
- **server/routes.ts:1536** — Public stats endpoint (/api/public/stats) issues SELECT count(*) FROM tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) without tenant_id constraints, aggregating data across all tenants.
  - _Fix:_ If global/aggregated platform metrics are intentional, restrict access to admin users or aggregate counts via a safe, audited background job that records non-identifying platform-level metrics. Otherwise, add tenant_id constraints to return per-tenant stats (e.g. WHERE tenant_id = ${tenantId}). Avoid exposing raw multi-tenant counts on public endpoints.
- **server/routes.ts:1565** — /api/public/architecture runs multiple SELECT/COUNT queries against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, etc.) without tenant_id filters, exposing aggregated cross-tenant counts and lists.
  - _Fix:_ Restrict this endpoint to platform admins or change the queries to be tenant-scoped (WHERE tenant_id = ${tenantId}). If platform-wide aggregates are required, compute them in a controlled background job and expose only sanitized, non-identifying aggregates via an admin-only API.
- **server/routes.ts:2320** — GET /api/store/order/:sessionId queries delivery_logs by orderId/stripePaymentId without any tenant_id constraint. Although sessionId is a capability token, this reads delivery logs across all tenants and returns delivery metadata (including tenantId used later).
  - _Fix:_ Treat sessionId as a capability but still verify the returned row's tenant ownership before exposing tenant-sensitive fields. Add an explicit tenant_id check where appropriate, or validate that the sessionId is scoped to an intended tenant and fail-closed if not. Consider returning only public-safe fields and avoid implicitly trusting records from other tenants.
- **server/routes.ts:2428** — Async order-lookup flow (setImmediate) queries delivery_logs by lower(customer_email) without tenant scoping, retrieving order rows for any tenant matching the email.
  - _Fix:_ If the feature needs to search across tenants, require an explicit admin operation or otherwise limit scope (e.g. per-tenant lookup via authentication). If cross-tenant lookup is intentional for public recovery, ensure the downstream behavior doesn't leak tenant-internal metadata and document the privacy implications. Prefer tenant-scoped queries when handling authenticated tenant requests.
- **server/routes.ts:2572** — POST /api/store/verify-orders selects delivery_logs by lower(customer_email) to return a list of orders for the provided email without any tenant_id restriction — returns up to 50 rows across tenants.
  - _Fix:_ Either require tenant authentication and filter by tenant_id, or scope returned results to a single tenant context (e.g. only orders associated with the verified email and a signed capability). If global email-based recovery is intended, ensure this behavior is acceptable from a privacy standpoint and consider limiting returned fields and access to an authenticated or rate-limited flow.
- **server/routes/projects.ts:33** — Correlated subqueries counting rows from tenant-scoped tables (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id) lack an explicit tenant_id predicate on the tenant-scoped table.
  - _Fix:_ Add tenant_id predicates to the correlated subqueries (e.g. WHERE project_id = p.id AND tenant_id = ${tenantId}) or otherwise ensure the tenant is explicitly enforced in the subquery to meet the app-level isolation invariant.
- **server/storage.ts:964** — fixStaleBackupSchedules() executes a raw UPDATE on heartbeat_tasks (tenant-scoped) with no tenant filter, changing schedules for all tenants.
  - _Fix:_ If this maintenance job should operate per-tenant, iterate tenants and run the update per tenant with WHERE tenant_id = ${tenantId}; otherwise restrict to an admin-only, audited path. Add tenant scoping to the SQL.
- **server/video-job-runner.ts:204** — SELECT cancelRequested FROM video_jobs WHERE jobId = ${jobId} is executed without a tenant_id constraint. Reads from a tenant-scoped table are not scoped to a tenant here.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}) where possible, or otherwise ensure the caller's tenant is known and enforced when reading the flag. If the runner legitimately needs to read across tenants, isolate that privilege to an operator-only flow with strict auditing and protections.
- **server/wiring-invariants.ts:366** — Raw SQL reads from tenant-scoped tables without any tenant_id filter (SELECT COUNT/MAX FROM ${spec.table}). This performs cross-tenant reads against tables that appear to be per-tenant (e.g. memory_entries, messages, agent_knowledge), violating the invariant that tenant-scoped tables must be constrained by tenant_id.
  - _Fix:_ If the intent is an admin/global audit, make the cross-tenant intent explicit and restrict execution to an administrative context (and document/guard it). Otherwise, scope the queries to a specific tenant (e.g. add WHERE tenant_id = ${ADMIN_TENANT_ID} or accept a tenantId parameter and use it in the WHERE clause). Avoid un-scoped SELECTs on tenant-scoped tables.
- **server/wiring-invariants.ts:535** — sql.raw executes SELECT COUNT(...) / pg_total_relation_size(...) FROM ${tbl.name} without a tenant_id WHERE clause. This reads message/ memory tables across all tenants rather than constraining to a tenant.
  - _Fix:_ Limit the query to a tenant by adding WHERE tenant_id = ${tenantId} (or run explicitly as an admin-only operation with appropriate access controls). Prefer parameterized drizzle queries rather than sql.raw for table access, and ensure any cross-tenant aggregation is only performed in an audited/admin-only path.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory is executed without filtering by tenant_id, causing a cross-tenant read on a table that appears tenant-scoped.
  - _Fix:_ If this check must inspect all tenants for platform-wide observability, ensure it runs only in a strictly-admin context and is clearly documented/audited. If per-tenant checks are expected, add a tenant_id filter (WHERE tenant_id = ${tenantId}) or accept tenantId as input and pass it through the query using parameterized SQL.

### Suppressed — confirmed false positives (139)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/wake-scheduler.ts:79** _(global-table)_ — UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules ...) runs without any tenant_id constraint — this mutates agent_wake_schedules across all tenants.
  - _Why false positive:_ heartbeat worker intentionally claims due wakes across tenants and later processes each row with its own tenant_id
- **server/agentic/runs.ts:39** _(internal-worker-pk)_ — appendStep updates agent_runs using WHERE eq(agentRuns.id, runId) with NO tenant_id constraint — allows updating a run by id across tenants.
  - _Why false positive:_ helper updates agent_runs by server-generated primary key inside internal run orchestration, not shown as request-derived input
- **server/agentic/runs.ts:51** _(internal-worker-pk)_ — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) but lacks tenant_id constraint — cross-tenant update risk.
  - _Why false positive:_ internal run-state mutation by globally unique run id within server agent worker flow
- **server/agentic/runs.ts:55** _(internal-worker-pk)_ — replaceRunState calls .where(eq(agentRuns.id, runId)) without filtering by tenant_id — allows replacing state for a run id in any tenant.
  - _Why false positive:_ server-side helper replaces state for internally created run identified by unique pk
- **server/agentic/runs.ts:64** _(internal-worker-pk)_ — completeRun updates agent_runs status/result WHERE id = runId but missing tenant_id in WHERE — unscoped mutation across tenants.
  - _Why false positive:_ completion helper mutates internally managed agent run by server-issued pk, not caller tenant selector
- **server/agentic/runs.ts:73** _(internal-worker-pk)_ — failRun updates agent_runs (status, error, completedAt) using WHERE id = runId with NO tenant filter — cross-tenant write allowed.
  - _Why false positive:_ failure transition uses internal run id in agent worker lifecycle
- **server/agentic/runs.ts:81** _(internal-worker-pk)_ — pauseRun updates agent_runs status/state with WHERE id = runId but does not filter by tenant_id — allows pausing runs of other tenants by id.
  - _Why false positive:_ pause helper is part of internal run lifecycle using server-created unique run id
- **server/data-protection.ts:72** _(upstream-verified)_ — UPDATE on conversations omits tenant scoping — SQL updates deleted_at/deleted_by WHERE id = ${conversationId} with no tenant_id predicate. This can modify a conversation belonging to a different tenant if IDs are misused.
  - _Why false positive:_ same function first selects conversation by id and tenant_id, proving ownership before the update
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} without tenant_id. This will delete chunks across tenants that share collection_id/doc_path if tenant_id is not enforced elsewhere.
  - _Why false positive:_ same function first verifies the collection belongs to tenantId via doc_collections WHERE id = collectionId AND tenant_id = tenantId; deleting chunks by that validated collection_id/doc_path is scoped through the owned parent collection.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped 'projects' table is missing tenant_id constraint (reads project by id without WHERE tenant_id = ...). This can leak another tenant's project row to the caller.
  - _Why false positive:_ the same code immediately checks project.tenantId !== tenantId and throws on mismatch before using the row, so ownership is enforced in this call path.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on 'projects' table lacks tenant scoping (UPDATE projects SET driveFolderId... WHERE id = <projectId> with no tenant_id filter). Even if ownership was checked earlier, every write against a tenant-scoped table must include tenant_id in the WHERE to prevent cross-tenant writes.
  - _Why false positive:_ the UPDATE is preceded in the same function by a project fetch and explicit tenant mismatch check that fails closed before this write.
- **server/google-drive.ts:829** _(upstream-verified)_ — SELECT from 'projects' to resolve project metadata is executed without constraining by tenant_id (reads a caller-supplied projectId with no tenant filter). This allows reading another tenant's project row.
  - _Why false positive:_ same path immediately validates proj.tenantId against params.tenantId when provided, and then ensureProjectFolder performs its own tenant ownership check before proceeding.
- **server/google-drive.ts:1004** _(upstream-verified)_ — SELECT from 'projects' (in searchDriveFiles) reads project by id without adding tenant_id constraint before relying on proj.tenantId for ownership checks. The raw SELECT itself must be tenant-scoped to avoid cross-tenant reads.
  - _Why false positive:_ this select is used solely to read tenantId for the immediate ownership check in the same function; subsequent DB search is tenant-scoped via JOIN on projects.tenant_id.
- **server/health-audit.ts:333** _(global-table)_ — UPDATE on `code_proposals` (SET status = 'archived_stale') uses WHERE id = ANY(${ids}) with no tenant_id constraint — this can modify rows across tenants.
  - _Why false positive:_ health-audit is a system-wide maintenance/admin audit routine archiving stale proposals across the platform, not a tenant-request path.
- **server/heartbeat.ts:52** _(global-table)_ — SELECT from research_schedules has no tenant_id filter (SELECT * FROM research_schedules WHERE ...). This returns schedules across tenants and allows cross-tenant reads/processing.
  - _Why false positive:_ heartbeat is a global scheduler/worker scanning due research_schedules across tenants for background processing, an intentional platform-wide internal job.
- **server/internal-resolver.ts:170** _(internal-worker-pk)_ — resolveExperimentFailed() performs an UPDATE on code_proposals WHERE id = ${proposalId} AND status = 'pending' with NO tenant_id constraint — this can modify/ archive proposals belonging to other tenants.
  - _Why false positive:_ Internal resolver worker updates by server-generated proposalId from event data, not a tenant request boundary.
- **server/lib/scheduled-post-runner.ts:62** _(global-table)_ — SELECT from tenant-scoped table `scheduled_posts` with no tenant_id constraint (CTE selects candidate rows across all tenants).
  - _Why false positive:_ This is an intentional system-wide heartbeat runner selecting due work across tenants, not a tenant-request path.
- **server/lib/scheduled-post-runner.ts:164** _(upstream-verified)_ — UPDATE scheduled_posts WHERE id = ${row.id} performs a write against a tenant-scoped table but omits tenant_id in the WHERE clause, allowing accidental modification of rows in another tenant if ids collide.
  - _Why false positive:_ Row ids come from the same function's prior locked claim CTE returning sp.id and sp.tenant_id; update is on the claimed row, not caller-supplied input.
- **server/lib/scheduled-post-runner.ts:191** _(upstream-verified)_ — UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' omits tenant_id in the WHERE clause; this can modify another tenant's row with the same id.
  - _Why false positive:_ Error-path release updates the row previously claimed and locked in the same call path; row identity was established by the prior claim CTE.
- **server/memory-graph.ts:195** _(fk-transitive)_ — INSERT into tenant-scoped table `memory_links` does not set tenant_id: `INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count) VALUES (...)`. Per project invariant, every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Why false positive:_ memory_links has no tenant_id column; isolation is transitive through tenant-scoped memory_entries referenced by source/target ids.
- **server/plan-executor.ts:116** _(upstream-verified)_ — UPDATE plans in appendExecutionLog does not constrain by tenant_id (WHERE id = ${planId} only). This can mutate another tenant's plan if planId is mixed across tenants.
  - _Why false positive:_ appendExecutionLog is used after executePlan's claim fetches the plan row and tenant in the same call path; planId is established internally from that claimed row.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE on plans (claiming plan: UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved') is not scoped by tenant_id. A planId from another tenant could be claimed by this process.
  - _Why false positive:_ Executor claims a plan by server-invoked globally unique plan id, not from an untrusted tenant request boundary.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status='failed' WHERE id = ${planId} is executed without a tenant_id constraint (clears status for a plan by id only).
  - _Why false positive:_ This failure update runs only after the prior claim RETURNING row established the exact plan and tenant in the same function.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final UPDATE of plans to set terminal status uses WHERE id = ${planId} only (no tenant_id constraint).
  - _Why false positive:_ Final status write is for the plan already claimed earlier in executePlan; ownership and row identity were established upstream in the same call path.
- **server/plan-executor.ts:759** _(upstream-verified)_ — Error-path UPDATE (set status='failed' WHERE id = ${planId} AND status = 'executing') lacks tenant_id constraint.
  - _Why false positive:_ Catch-path failure update targets the same internally claimed executing plan from earlier in executePlan, not a caller-derived cross-tenant selector.
- **server/process-governor.ts:239** _(global-table)_ — Query for cascading failures selects from heartbeat_logs without a tenant_id filter: SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ... . heartbeat_logs is tenant-scoped; missing tenant constraint allows cross-tenant aggregation/leakage.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; this aggregate is over a non-tenant-scoped table
- **server/process-governor.ts:595** _(upstream-verified)_ — Within disable_task action, code runs UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without tenant constraint. Although t.id was selected from tenant-scoped rows earlier, the UPDATE by id alone can affect rows belonging to other tenants.
  - _Why false positive:_ t.id comes from earlier SELECT restricted to heartbeat_tasks.tenant_id = tenantId in same call path
- **server/process-governor.ts:641** _(upstream-verified)_ — restart_agent action updates agent_desks using WHERE id = ${desk.id} without tenant_id scoping (UPDATE agent_desks SET ... WHERE id = ${desk.id}). This can modify another tenant's agent_desks row if ids collide.
  - _Why false positive:_ desk.id is selected immediately above from agent_desks filtered by ad.tenant_id = tenantId
- **server/proposal-verifier.ts:241** _(internal-worker-pk)_ — persist() updates code_proposals SET verification_status/verification_details WHERE id = ${proposalId} with no tenant_id filter — it mutates the proposal by id only.
  - _Why false positive:_ persist is an internal helper invoked from server-side verifier flow using server-supplied proposalId, not a request boundary
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — attemptRebaseOnDrift writes back to code_proposals (UPDATE ... WHERE id = ${proposalId}) without tenant scoping when marking a proposal rejected or when persisting the rebased diff. This allows cross-tenant mutation by id only.
  - _Why false positive:_ attemptRebaseOnDrift mutates by server-managed proposalId inside internal verifier/rebase workflow, not directly from untrusted caller input
- **server/recurring-messages.ts:300** _(upstream-verified)_ — UPDATE agent_knowledge SET content = ... WHERE id = ${r.id} — the UPDATE uses only id and lacks tenant_id (or persona/category) constraint. This lets an attacker or misbehaving job modify another tenant's row by ID.
  - _Why false positive:_ r.id/r.tenant_id come from rows already loaded for the scheduled job; update applies to that selected row in same loop
- **server/routes.ts:4338** _(fk-transitive)_ — INSERT into `project_notes` does not set tenant_id. The raw insert `INSERT INTO project_notes (project_id, note, author) VALUES (...)` writes to a tenant-scoped table without associating the row with the caller's tenant, allowing records to be created without correct tenant attribution.
  - _Why false positive:_ project_notes has no tenant_id per schema and is attached via project_id to tenant-scoped projects/conversations
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on conversations omits tenant constraint. The handler updates conversations.project_id with `WHERE id = ${conv.id}` but does not include `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ conv was just created with tenantId via storage.createConversation, so updating by its fresh server-generated id is already tenant-owned in this call path
- **server/routes/conversations.ts:99** _(fk-transitive)_ — SELECT from project_conversations omits tenant constraint. The code checks for an existing project_conversations row using `WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` with no tenant filter, allowing cross-tenant information disclosure if IDs are guessed.
  - _Why false positive:_ project_conversations has no tenant_id per schema and links already tenant-validated projectId with newly created tenant-owned conv.id
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. The INSERT `VALUES (${projectId}, ${conv.id})` does not set tenant_id, so a row may be created without tenant scoping (or assigned to a default), violating tenant isolation.
  - _Why false positive:_ project_conversations has no tenant_id per schema and is a join row isolated through tenant-scoped projects/conversations
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on projects omits tenant constraint. The handler runs `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` with no tenant check, allowing an attacker to update another tenant's project timestamp by guessing project_id.
  - _Why false positive:_ projectId was verified immediately above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT into project_notes omits tenant scoping. The archive insert `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id and thus writes into a tenant-scoped table without the caller's tenant context.
  - _Why false positive:_ project_notes has no tenant_id per schema and projId is selected only through tenant-verified conversation/project join
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id (values: project_id, note, author).
  - _Why false positive:_ project_notes has no tenant_id per schema and insert is for a project already checked with tenant_id = tenantId
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload path) does not include tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...).
  - _Why false positive:_ project_files has no tenant_id per schema and file row is attached to a project already verified for tenant ownership
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multer upload path) does not include tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...).
  - _Why false positive:_ project_files has no tenant_id per schema and file row is attached to a project already verified for tenant ownership
- **server/sculptor.ts:94** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` performed with WHERE id = ${session.id} but no tenant_id constraint — write is only guarded by the local session id, not the caller's tenant.
  - _Why false positive:_ update uses freshly inserted server-generated session.id inside internal workflow, not an untrusted request boundary id
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id} has no tenant_id filter — missing tenant-scoping on a write to a tenant-scoped table.
  - _Why false positive:_ session.id comes from the row just inserted with params.tenantId in the same function, so this update targets that owned row.
- **server/sculptor.ts:162** _(internal-worker-pk)_ — UPDATE sculptor_sessions ... WHERE id = ${sessionId} in monitorSession lacks tenant_id constraint — a background monitor can update rows across tenants if id collisions occur.
  - _Why false positive:_ background monitor updates by server-generated sessionId passed from createAgentSession, not from an untrusted request boundary.
- **server/second-opinion.ts:407** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleFusionReservation) updates a tenant-scoped ledger row by id only, with no tenant_id constraint or tenant context — allows cross-tenant modification if reservationId is guessed or leaked.
  - _Why false positive:_ reservationId is an internal ledger reservation row id used by server-side settlement logic, not shown as coming from request input.
- **server/second-opinion.ts:426** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseFusionReservation) performs a write to a tenant-scoped table without scoping by tenant_id or verifying the reservation's tenant.
  - _Why false positive:_ reservationId is an internal server-managed reservation primary key for release logic, not an untrusted cross-tenant input.
- **server/seed.ts:3212** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) ... is executed without setting tenant_id into a tenant-scoped table — new rows may be created with no tenant association.
  - _Why false positive:_ project_conversations has no tenant_id column and links tenant-scoped conversation/project parents, so isolation is transitive through the referenced rows.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. The query `db.select().from(customTools).where(eq(customTools.name, parsed.name))` lacks a tenant filter and can return rows from other tenants.
  - _Why false positive:_ name is first transformed with tenantToolName(parsed.name, tenantId), making lookup tenant-scoped by derived unique name before insert
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id without tenant constraint. `db.select().from(customTools).where(eq(customTools.id, inserted.id))` can return a row from a different tenant if IDs collide or if attacker can guess an id.
  - _Why false positive:_ reads back the just-inserted row by server-generated primary key inserted.id within same internal flow, not from untrusted request input
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `custom_tools` without tenant constraint. `db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id))` updates by id only and may affect another tenant's row.
  - _Why false positive:_ updates custom_tools by tool.id carried from the just-created/internal validation flow, not attacker-supplied
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE against tenant-scoped table `custom_tools` without tenant constraint. `db.delete(customTools).where(eq(customTools.id, tool.id))` deletes by id only and could remove another tenant's tool row.
  - _Why false positive:_ deletes the just-inserted tool row by internal tool.id during revert path, not from an untrusted boundary
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE against tenant-scoped table `custom_tools` without tenant constraint. `db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(eq(customTools.id, tool.id))` lacks tenant filtering and may increment usage for the wrong tenant's row.
  - _Why false positive:_ tool row is selected immediately above with tenant_id = tenantId; update targets that verified row id in same call path
- **server/tools.ts:4687** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id. Per invariant, tenant-scoped table INSERTs must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id column and insert is gated by assertProjectInTenant on tenant-scoped parent project
- **server/tools.ts:4691** _(fk-transitive)_ — INSERT into `project_files` omits tenant_id when saving presentation links. Tenant_id must be set on INSERT to avoid cross-tenant contamination.
  - _Why false positive:_ project_files has no tenant_id column and insert is gated by assertProjectInTenant on tenant-scoped parent project
- **server/tools.ts:6465** _(fk-transitive)_ — INSERT into `project_files` (audio upload path) omits tenant_id despite being a tenant-scoped artifact. The call relies on assertProjectInTenant but still fails the requirement that every INSERT set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and insert occurs only after assertProjectInTenant validates parent ownership
- **server/tools.ts:6999** _(fk-transitive)_ — INSERT into `project_files` (slideshow video) omits tenant_id. Every write to tenant-scoped tables must include the caller's tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and insert occurs only after assertProjectInTenant validates parent ownership
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits tenant constraint. The UPDATE uses WHERE id = ${id} alone, allowing cross-tenant row modification if an id from another tenant is supplied or discovered.
  - _Why false positive:_ id comes from immediately prior SELECT on memory_entries constrained by tenant_id and persona_id in same function
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits tenant constraint. The batch UPDATE uses WHERE id = ANY(${ids}) without scoping to tenant_id, allowing last_accessed/access_count bumps across tenants.
  - _Why false positive:_ ids are derived from rows just selected with tenant_id = tid and persona_id = personaId in the same flow
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits tenant constraint. The UPDATE uses WHERE id = ${id} alone (after finding an existing row with tenant filter), enabling cross-tenant modification if id is misused.
  - _Why false positive:_ id comes from prior SELECT on memory_entries scoped by tenant_id and persona_id in same call path
- **server/agentic-engines.ts:341** _(global-table)_ — SELECT from heartbeat_logs (gatherOperationalData) has no tenant_id filter — it reads heartbeat logs across all tenants.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column: NO, so this is a platform-global log table
- **server/agentic-engines.ts:448** _(global-table)_ — SELECT recent heartbeat_logs in runPredictiveEngine (recentLogs) lacks tenant_id filtering — reads other tenants' logs.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is treated as a global operational log
- **server/agentic-engines.ts:516** _(global-table)_ — SELECT heartbeat_logs (heartbeatPerformance) in runOptimizationEngine has no tenant_id constraint — aggregates task performance across all tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column; aggregate is over a global log table
- **server/agentic/approvals.ts:123** _(global-table)_ — Bulk UPDATE agentApprovals to mark pending approvals expired has no tenant_id constraint — it will expire approvals across all tenants.
  - _Why false positive:_ Cross-tenant expiry sweep is explicit system-wide maintenance; returned rows carry tenantId for per-row scoped follow-up updates.
- **server/agentic/department-budgets.ts:143** _(global-table)_ — Query scans department_budgets without constraining tenant_id (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads tenant-scoped data across all tenants.
  - _Why false positive:_ Function is a heartbeat-callable sweep explicitly iterating all active tenant budgets for enforcement/events, not tenant-request data access.
- **server/agentic/resume-worker.ts:66** _(global-table)_ — SELECT joins tenant-scoped tables (agent_runs, agent_approvals) without constraining to a caller/worker tenant_id — the query can return rows across tenants.
  - _Why false positive:_ Explicit worker sweep over pending approvals across tenants; join preserves tenant alignment via a.tenant_id = r.tenant_id.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table conversations is performed without an explicit tenant_id constraint in the WHERE clause (UPDATE conversations SET project_id = ... WHERE id = ...).
  - _Why false positive:_ Updated conversation ids come from prior query joining conversations to projects with matched tenant_id, so ownership is established earlier in same call path.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into tenant-scoped table `invoice_items` does not include tenant_id — new rows are created without an explicit tenant context.
  - _Why false positive:_ invoice_items has no tenant_id column and is tenant-scoped transitively through invoice_id -> invoices.
- **server/capability-review.ts:491** _(internal-worker-pk)_ — UPDATE capability_reviews sets reused=true WHERE id = ${pending.reviewId} … but the WHERE clause lacks a tenant_id constraint. This updates a tenant-scoped table row by id without confirming tenant ownership.
  - _Why false positive:_ Internal server-side best-effort worker updates by cached reviewId primary key, not from untrusted request input.
- **server/ceo-orchestrator.ts:821** _(global-table)_ — Unscoped SELECT on tenant-scoped table `personas` — cachedPersonas = await db.select().from(personasTable) reads personas for ALL tenants without filtering by plan.tenantId.
  - _Why false positive:_ Referenced schema marks personas as not tenant-scoped, so loading all personas is intentional global data access.
- **server/channel-routing.ts:30** _(global-table)_ — listChannelRoutes() performs a JOIN to the tenant-scoped `personas` table (JOIN personas p ON p.id = cr.persona_id) without constraining personas by tenant_id. That returns persona names/ids across tenants.
  - _Why false positive:_ channel_routes is created here as a global routing table with UNIQUE(channel), and referenced personas has no tenant_id; this is platform-global routing, not tenant-scoped data access
- **server/chat-engine.ts:571** _(fk-transitive)_ — SELECT from project_files uses only project_id (`WHERE project_id = ${projectId}`) and lacks an explicit tenant_id constraint. Under the codebase invariant tenant isolation must be enforced on every tenant-scoped table, not only via ownership provenance of projectId.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; project ownership was verified just above with id+tenant_id
- **server/chat-engine.ts:582** _(fk-transitive)_ — SELECT from project_notes uses only project_id (`WHERE project_id = ${projectId}`) and does not include tenant_id. Relying on prior verification of projectId is not sufficient per the invariant that every access to tenant-scoped tables must include the tenant constraint.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-isolated transitively via project_id after verified projects lookup by id+tenant_id
- **server/chat-engine.ts:759** _(upstream-verified)_ — Complex recent-work query (LEFT JOIN LATERAL subqueries) contains subqueries reading messages and compaction_archives without explicit tenant_id constraints (e.g. `(SELECT content FROM messages WHERE conversation_id = c.id ...)` and `(SELECT summary FROM compaction_archives WHERE conversation_id = c.id ...)`). The outer WHERE restricts conversations by tenant, but the invariant requires explicit tenant scoping on every tenant-scoped table access.
  - _Why false positive:_ the same query's outer WHERE constrains conversations by tenant_id, and subqueries are correlated on c.id derived only from those tenant-scoped conversation rows
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} executes without constraining tenant_id. This updates a tenant-scoped row by id only, enabling cross-tenant updates if called with an id from another tenant.
  - _Why false positive:_ getCredential(id, tenantId) verifies ownership immediately before this update, and id is a validated row id from the same call path
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} executes without constraining tenant_id. Called after resolving a credential by site/tenant, but the UPDATE lacks tenant check and could affect another tenant's row if id values collide or are spoofed.
  - _Why false positive:_ getCredentialForSite(siteUrl, tenantId) resolves the credential for the tenant immediately before updating last_used_at by that validated row id
- **server/data-protection.ts:125** _(upstream-verified)_ — DELETE statements in permanentlyPurgeSoftDeleted remove messages/compaction_archives/conversations using only conversation_id (DELETE FROM messages WHERE conversation_id = ${conv.id} / DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} / DELETE FROM conversations WHERE id = ${conv.id}) with no tenant_id predicate.
  - _Why false positive:_ the purge first selects expired conversations including tenant_id, then deletes child rows by conversation_id and the parent by id for those already enumerated rows in the same call path
- **server/dormant-deprecation.ts:169** _(global-table)_ — getTrafficSnapshot queries messages with SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' without tenant_id scoping — this computes platform-wide user message counts and may mix tenants.
  - _Why false positive:_ getTrafficSnapshot computes platform-wide traffic gate metrics for dormant-deprecation logic, not per-tenant row access.
- **server/dormant-deprecation.ts:179** _(global-table)_ — getTrafficSnapshot queries tool_performance with SELECT COUNT(DISTINCT tool_name) ... WHERE last_success_at > ... but no tenant filter, producing cross-tenant aggregation.
  - _Why false positive:_ tool_performance aggregation here is used as a global platform activity metric, not tenant-scoped access.
- **server/dormant-deprecation.ts:203** _(global-table)_ — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with no tenant filter; it returns global tool usage history rather than tenant-scoped data.
  - _Why false positive:_ SELECT DISTINCT tool_name is global tool-usage history for platform-wide deprecation decisions.
- **server/dormant-deprecation.ts:214** _(global-table)_ — getActiveTools14d selects DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant scoping. This yields global active-tool lists rather than tenant-specific, breaking isolation assumptions.
  - _Why false positive:_ active-tools query is part of global dormant-deprecation preview logic rather than tenant data retrieval.
- **server/email.ts:366** _(global-table)_ — pollSharedInbox checks for existence with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without tenant scoping. inbox_messages is tenant-scoped (tenant_id used on insert), so this read can cross tenants or return rows for other tenants.
  - _Why false positive:_ shared inbox polling deduplicates provider message_id across a single platform-wide inbox; this is an internal global mailbox flow.
- **server/email.ts:410** _(global-table)_ — backfillEmptyBodies selects id,message_id FROM inbox_messages WHERE body_text IS NULL OR body_html IS NULL ORDER BY id with no tenant filter — this scans inbox messages across all tenants and will operate on other tenants' messages.
  - _Why false positive:_ backfillEmptyBodies is a maintenance job over the shared inbox_messages store for one global inbox, not caller-scoped tenant access.
- **server/embeddings.ts:726** _(internal-worker-pk)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id filter — a write to a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ update is by server-selected primary key within an internal backfill worker, not from an untrusted request boundary.
- **server/embeddings.ts:743** _(internal-worker-pk)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without constraining by tenant_id. This updates tenant-scoped rows without verifying tenant ownership.
  - _Why false positive:_ update is by server-selected primary key within an internal backfill worker, not from an untrusted request boundary.
- **server/heartbeat.ts:101** _(internal-worker-pk)_ — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — missing tenant_id constraint. Updating by id without also filtering on tenant_id can modify another tenant's row if ids collide or are mis-attributed.
  - _Why false positive:_ Background heartbeat worker updates research_schedules by server-derived sched.id from its own loaded schedule rows, not from request input.
- **server/heartbeat.ts:106** _(internal-worker-pk)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Same internal scheduler path; update is by server-selected primary key sched.id within worker logic, not an untrusted tenant boundary.
- **server/heartbeat.ts:112** _(internal-worker-pk)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Same heartbeat recovery/advance flow uses server-owned sched.id from prior internal selection, matching internal-worker-pk.
- **server/heartbeat.ts:120** _(internal-worker-pk)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id constraint in recovery path as well.
  - _Why false positive:_ Recovery-path update still targets server-derived research_schedules PK inside internal worker code, not caller-controlled input.
- **server/heartbeat.ts:257** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. heartbeat_tasks are tenant-scoped; updating by id without tenant guard risks touching another tenant's task.
  - _Why false positive:_ Startup cleanup worker normalizes heartbeat_tasks rows it already loaded internally; update uses server-derived task PK, not request data.
- **server/heartbeat.ts:261** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — repeated model-normalization UPDATE lacks tenant guard.
  - _Why false positive:_ Same internal startup normalization path for heartbeat_tasks by server-selected PK only.
- **server/heartbeat.ts:1642** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 — no tenant_id filter. heartbeat_logs are tenant-scoped and this read can surface another tenant's logs.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this log table is not tenant-scoped and global read is expected.
- **server/heartbeat.ts:2948** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id filter. This write can affect tasks across tenants if ids are not uniquely guarded by tenant.
  - _Why false positive:_ approval_status update is immediately after creating newTask with tenantId in server code; PK is server-generated and not taken from untrusted input.
- **server/heartbeat.ts:3324** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — same missing tenant guard in delegateTaskFromChat path.
  - _Why false positive:_ Same create-then-update pattern on newly created heartbeat task; update uses server-generated newTask.id within same internal flow.
- **server/internal-resolver.ts:93** _(internal-worker-pk)_ — finalizeEvent() updates event_log without constraining by tenant_id — the UPDATE can modify event rows across tenants because there's no WHERE tenant_id = ... guard.
  - _Why false positive:_ Internal resolver updates event_log by server-side eventId primary key; no untrusted request boundary shown.
- **server/internal-resolver.ts:107** _(internal-worker-pk)_ — resolvePlanProposed() SELECTs plans by id with no tenant_id check. A foreign planId taken from the event is read without verifying the caller/event tenant owns that plan, allowing cross-tenant reads.
  - _Why false positive:_ Internal resolver reads plans by server-derived planId from event processing, not directly from req/body/params.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE procedureEvolutionRuns WHERE eq(procedureEvolutionRuns.id, runId) is executed without constraining by tenant_id. procedureEvolutionRuns is tenant-scoped but the update uses only id, allowing cross-tenant modifications if an id is mis-used.
  - _Why false positive:_ runId was just created by server-side INSERT in same function and then updated by that generated primary key.
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE procedureEvolutionRuns .where(eq(procedureEvolutionRuns.id, runId)) is used without tenant scoping when marking noop/done. Missing tenant predicate allows cross-tenant row mutation.
  - _Why false positive:_ Marks the same newly inserted procedureEvolutionRuns row by server-generated runId within same call path.
- **server/lib/aevo-meta-editor.ts:499** _(internal-worker-pk)_ — UPDATE procedureEvolutionRuns .where(eq(procedureEvolutionRuns.id, runId)) when finishing the run lacks a tenant_id condition. This allows changing a run row belonging to another tenant if runId is mis-bound.
  - _Why false positive:_ Finishes the run using runId created earlier in this function, not an untrusted tenant-crossing input.
- **server/memory-graph.ts:66** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_categories` is performed without constraining to the caller's tenant (WHERE tenant_id = ...). The query `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}` can modify another tenant's row if id collisions occur or if called with an id from a different tenant.
  - _Why false positive:_ catId comes from ensureCategory in the same call path, which selects/inserts memory_categories for the provided tenantId before this update.
- **server/memory-graph.ts:181** _(fk-transitive)_ — SELECT against `memory_links` is executed without a tenant constraint: `SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ... LIMIT 1`. If `memory_links` is tenant-scoped this can reveal cross-tenant existence and/or allow cross-tenant checks.
  - _Why false positive:_ Referenced schema says memory_links has no tenant_id; links are between tenant-scoped memory_entries selected for the same tenant.
- **server/memory-graph.ts:393** _(upstream-verified)_ — UPDATE on `memory_entries` is performed without constraining to tenant: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}`. This can update another tenant's entry if ids collide or if called with a wrong id.
  - _Why false positive:_ mem.id is taken from uncategorized rows selected just above with tenant_id = tenantId in the same function.
- **server/martech-bundle.ts:315** _(upstream-verified)_ — UPDATE against tenant-scoped `tenant_voice_profiles` omits a tenant constraint: `.where(eq(tenantVoiceProfiles.id, existing.id))`. Relying on id alone can allow cross-tenant modification if ids are abused or mis-bound.
  - _Why false positive:_ existing is loaded via getVoiceProfile({ tenantId, profileName }) immediately above, so the updated id was tenant-verified in the same call path.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE on tenant-scoped `knowledge_triples` is performed without a tenant constraint: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}`. Although the id was selected earlier with tenant filtering, the UPDATE lacks an explicit tenant filter and could modify rows across tenants if misused.
  - _Why false positive:_ c.id values come from the immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId.
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — Unscoped UPDATE to `memory_entries`: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}` executed after classifyMemoryCategory. The UPDATE lacks a tenant filter and may modify entries from another tenant.
  - _Why false positive:_ entry.id is the freshly created row from storage.createMemoryEntry with tenantId supplied in the same call path.
- **server/memory-intelligence.ts:316** _(upstream-verified)_ — Another unscoped UPDATE to `memory_entries`: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}` (in the update-path). This mutation is not constrained by tenant_id.
  - _Why false positive:_ entry.id is the freshly created replacement row from storage.createMemoryEntry with tenantId supplied in the same call path.
- **server/minerva-planner.ts:459** _(upstream-verified)_ — Rollback UPDATE `UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'` is performed without tenant scoping. This can rollback/modify a plan in a different tenant.
  - _Why false positive:_ Rollback occurs in the catch after the prior CAS UPDATE RETURNING captured the specific row including tenant_id; this is same-call-path cleanup of that already-selected plan.
- **server/pace-control.ts:85** _(global-table)_ — COUNT query against heartbeat_logs (global pacing check) has no tenant filter — the read aggregates across all tenants (no WHERE tenant_id = ...). This can leak cross-tenant activity and lets one tenant's activity affect another's pace enforcement.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; this pacing log table is intentionally platform-global.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries heartbeat_logs for totals and per-persona counts without any tenant scoping — it returns cross-tenant aggregates that can leak tenant activity and misrepresent per-tenant usage.
  - _Why false positive:_ heartbeat_logs has no tenant_id and this snapshot is over a global pacing table, matching the global-table pattern.
- **server/proactive-engine.ts:138** _(upstream-verified)_ — SELECT persona_id FROM proactive_actions WHERE id = ${actionId} is executed without constraining tenant_id. This reads a tenant-scoped row by id only and can leak or return a persona from another tenant.
  - _Why false positive:_ Immediately before, the function updates proactive_actions with WHERE id = actionId AND tenant_id = tenantId; the follow-on SELECT is in the same call path for that same row.
- **server/project-brain.ts:350** _(upstream-verified)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} does not include tenant scoping. Conversations are tenant-scoped and should be filtered by tenant_id.
  - _Why false positive:_ This query is constrained by project_id from projects selected earlier; conversations are being traversed via the already-chosen parent project in the same call path.
- **server/recurring-messages.ts:258** _(upstream-verified)_ — Broad SELECT on agent_knowledge (LIMIT 500) without any tenant_id restriction returns rows across all tenants. This enumerates tenant-scoped data and can lead to cross-tenant actions if further code updates rows without owner checks.
  - _Why false positive:_ This is an internal heartbeat runner intentionally scanning across tenants; rows include tenant_id and subsequent per-row operations use that tenant_id for tenant-scoped reads/writes.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId} — update uses id only and does not constrain by tenant_id and persona_id, allowing cross-tenant modification if id is known or deduplicated row matched incorrectly.
  - _Why false positive:_ storedId comes directly from a prior SELECT on memory_entries constrained by tenant_id and persona_id in the same path.
- **server/research-engine.ts:468** _(fk-transitive)_ — INSERT INTO project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...)). Project-scoped tables must receive explicit tenant_id per the codebase invariant.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects via project_id; insertion is transitively isolated after assertProjectInTenant(projectId, session.tenantId).
- **server/research-engine.ts:310** _(upstream-verified)_ — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} is executed without a tenant_id constraint. This can update another tenant's program row if ids collide or are misused.
  - _Why false positive:_ The same function loaded the program with WHERE id = programId AND tenant_id = tenantId immediately above, so this update is operating on a verified tenant-owned row.
- **server/routes/admin-zombie-detector.ts:38** _(global-table)_ — Query reads from `agent_trace_spans` (a tenant-scoped usage/trace table) without constraining by tenant_id. This allows cross-tenant aggregation/exposure of usage data even though the handler is admin-gated.
  - _Why false positive:_ endpoint is explicitly restricted to ADMIN_TENANT_ID plus isAdminRequest and intentionally aggregates platform-wide admin telemetry
- **server/routes/admin-zombie-detector.ts:54** _(global-table)_ — Query computes recent persona usage from `conversations` (tenant-scoped) without any tenant_id constraint. This reads cross-tenant conversation metadata and can leak tenant-specific activity to the caller.
  - _Why false positive:_ admin-only zombie-detector is intentionally producing cross-tenant usage aggregates for platform administration
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages to mark message read omits tenant constraint. After selecting the message, the handler runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `AND tenant_id = ${tenantId}` — the SQL write is not tenant-scoped.
  - _Why false positive:_ same handler first selects inbox_messages by id and tenant_id, then updates that verified row by its server-read primary key
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM `project_files` WHERE project_id = ${projectId} has no explicit tenant_id constraint on the tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id and is isolated through prior verified parent project ownership check on projects.id + tenant_id
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file metadata FROM `project_files` (used by download) does not include a tenant_id constraint (WHERE project_id = ${projectId} AND file_path = ...).
  - _Why false positive:_ project_files has no tenant_id and access is transitively scoped by the earlier projects tenant check for the same projectId
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM `project_files` WHERE id = ${fileId} AND project_id = ${projectId} lacks an explicit tenant_id predicate on the tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id and delete is limited to a child row of a project whose tenant ownership was verified just above
- **server/seed.ts:3212** _(fk-transitive)_ — INSERT into project_conversations uses project_id and conversation_id derived from an unscoped SELECT without validating tenant ownership (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id}))
  - _Why false positive:_ project_conversations has no tenant_id and is a child/link table; insert is tenant-isolated transitively via tenant-scoped parent rows.
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE memory_entries uses `WHERE id = <existing id>` without also constraining tenant_id. Although the id was discovered from a prior tenant-scoped SELECT, the UPDATE should still include tenant_id in its WHERE to prevent accidental cross-tenant modification if IDs are reused or logic changes.
  - _Why false positive:_ updated id is selected immediately above from memory_entries with WHERE tenant_id = tenantId in same call path
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' is executed without scoping to the caller's tenant (WHERE only filters by conversationId). This allows reading messages across tenants if conversationId is attacker-controlled or misused.
  - _Why false positive:_ messages are fetched only after same-function ownership check that loaded conversation and verified conv[0].tenantId === tenantId
- **server/skillify.ts:223** _(global-table)_ — SELECT from tenant-scoped table 'personas' is executed without scoping to the caller's tenant (only filters by personas.id). If personas are tenant-scoped this permits cross-tenant reads.
  - _Why false positive:_ schema note says personas has no tenant_id, so tenant scoping is not applicable here
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' in distillIntent is executed without a tenant_id constraint (.where only checks conversationId). This allows cross-tenant reads of message content.
  - _Why false positive:_ distillIntent first calls analyzeConversation(conversationId, tenantId), which verifies conversation ownership before this message read
- **server/skillify.ts:331** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' in skillifyConversation is executed without a tenant_id constraint (.where only checks conversationId). This permits reading messages across tenants.
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), which verifies conversation ownership before this message read
- **server/storage.ts:239** _(upstream-verified)_ — getConversationUnscoped() performs a SELECT on tenant-scoped table `conversations` with no tenant_id constraint.
  - _Why false positive:_ explicit dangerous internal unscoped fetcher documented for orchestration only, with downstream tenant validation requirement and audit logging
- **server/storage.ts:289** _(upstream-verified)_ — attachCitationMetadata() runs a raw SELECT on `messages` (tenant-scoped) using id list but with NO tenant_id constraint, returning rows across tenants.
  - _Why false positive:_ ids come only from msgs already fetched through tenant-scoped getMessages/getMessagesPaginated, so this helper re-reads same rows for citations metadata
- **server/storage.ts:980** _(internal-worker-pk)_ — getDueHeartbeatTasks() SELECTs heartbeat_tasks (tenant-scoped) without any tenant filter, returning due tasks across all tenants.
  - _Why false positive:_ getDueHeartbeatTasks is a server worker scheduler query over approved due tasks across tenants, not a tenant-bounded request path
- **server/storage.ts:995** _(internal-worker-pk)_ — claimHeartbeatTasks() updates heartbeat_tasks WHERE id = ${id} ... with NO tenant filter, allowing claiming tasks across tenants by id.
  - _Why false positive:_ claimHeartbeatTasks is an internal scheduler worker state-transition by server-selected task ids, not ids from an untrusted cross-tenant request boundary
- **server/storage.ts:1009** _(global-table)_ — markHeartbeatTaskRun() UPDATEs heartbeat_tasks WHERE id = ${id} with no tenant_id constraint, allowing cross-tenant mutation by id.
  - _Why false positive:_ heartbeat task processing is platform-global worker functionality; surrounding code claims due tasks across all tenants and heartbeat_logs are explicitly noted global/transitively scoped
- **server/storage.ts:1421** _(upstream-verified)_ — supersedeAdr() performs UPDATEs on architecture_decisions without including tenant_id in the WHERE clause even though earlier SELECTs verified ownership; the writes themselves are unscoped.
  - _Why false positive:_ both old/new ADR rows are selected immediately above with id+tenant_id and updates use those verified ids in same call path
- **server/tools.ts:4712** _(upstream-verified)_ — SELECT from `presenter_sessions` uses only presentation_id and does not verify the session belongs to the caller's tenant. Missing tenant constraint allows cross-tenant session token reads.
  - _Why false positive:_ conversation ownership is asserted just above and project ownership is re-asserted before any project-linked writes
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into project_files does not set tenant_id — tenant-scoped rows must include explicit tenant context on write.
  - _Why false positive:_ project_files has no tenant_id and is scoped through verified tenant-owned project_id
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE projects executed without scoping by tenant_id (WHERE id = ${projId} only). Without an application-level tenant filter this can update another tenant's row if projId is manipulated.
  - _Why false positive:_ projId is derived from a query joining projects with p.tenant_id = tenantId immediately above in the same flow
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} lacks a tenant filter. Application-level isolation requires WHERE tenant_id = caller_tenant for tenant-scoped tables.
  - _Why false positive:_ workflow is selected just above with id/name plus tenant_id, and the update uses that verified workflow.id in the same call path
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE against tenant-scoped table `knowledge_triples` omits tenant constraint. The code executes: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} — it does not include AND tenant_id = ${tenantId}, so a row id from another tenant could be updated if an id is guessed or leaked.
  - _Why false positive:_ row.id comes only from contradictionRows selected just above with tenant_id = tenantId, so this update is scoped by prior verification in the same call path.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — Direct SELECT from provider_keys without constraining by tenant_id. The query SELECT provider, LENGTH(api_key) ... FROM provider_keys omits any WHERE tenant_id = ${tenantId}, meaning it can enumerate provider key records across tenants (even though executed inside an admin branch). This violates the project invariant that tenant-scoped tables must always be filtered by the caller's tenant.
  - _Why false positive:_ schema note says provider_keys has no tenant_id column, so this is not a tenant-scoped table query.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM provider_keys executed without a tenant_id filter. This aggregates provider key info across all tenants (admin-only branch aside) and thus reads a tenant-scoped table without constraining to a tenant.
  - _Why false positive:_ provider_keys is explicitly marked as having no tenant_id column; global aggregate is consistent with schema.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` is executed without constraining by tenant_id when verifying a claimed conversation. Reads against a tenant-scoped table lack the required tenant filter.
  - _Why false positive:_ the query intentionally verifies ownership by reading tenant_id for a specific conversation id and then comparing it to the claimed tenant; mismatches are rejected in the same flow.
