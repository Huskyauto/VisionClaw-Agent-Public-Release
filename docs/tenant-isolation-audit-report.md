# Tenant-Isolation Audit Report

_Generated 2026-07-27T00:07:14.405Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **571/571** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **323** (CRITICAL 81 · HIGH 197 · MEDIUM 45 · LOW 0)
- Precision second-pass (`gpt-5.4`): **206** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (81)

- **server/agentic-engines.ts:243** — autoApplyEligibleInsights performs a bulk UPDATE on ai_insights (UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...)) without any tenant_id constraint — this writes across all tenants.
  - _Fix:_ Do not run blanket multi-tenant writes. Either accept a tenantId and scope the UPDATE (WHERE tenant_id = ${tenantId} AND ...), or iterate tenants explicitly and run per-tenant updates. Ensure any mass-update affecting tenant-scoped rows always includes tenant_id.
- **server/agentic/resume-worker.ts:66** — Cross-tenant SELECT: the candidate query reads agent_runs and agent_approvals without restricting to a single tenant (no WHERE tenant_id = <caller tenant>). This allows the background sweep to enumerate/observe runs and approvals across all tenants.
  - _Fix:_ Run the sweep scoped-per-tenant or accept a tenant parameter and add explicit tenant filters (e.g. WHERE r.tenant_id = ${tenantId} AND a.tenant_id = ${tenantId}). Do not run a global JOIN across tenant-scoped tables; restrict reads to the caller's tenant or iterate tenants in a controlled, audited loop.
- **server/agentic/wake-scheduler.ts:142** — runDueWakes performs a cross-tenant claim: the inner SELECT used by the UPDATE that marks wakes 'firing' does not restrict by tenant_id. This can claim and update pending wakes from any tenant in one transaction (mass cross-tenant write).
  - _Fix:_ Scope the claim by tenant. Either run the claim per-tenant (SELECT ... WHERE tenant_id = ${tenantId} ... FOR UPDATE SKIP LOCKED) or add an explicit tenant_id filter to both the inner SELECT and the outer UPDATE (and/or pass the tenant context into this scheduler loop). Ensure every UPDATE/WHERE includes AND tenant_id = <caller tenant> so claims cannot touch other tenants' rows.
- **server/auto-memorize.ts:62** — setWatermark writes to agent_knowledge using a hardcoded tenant_id value: INSERT ... VALUES (..., 1, 'auto_memorize', ...) — this inserts a tenant-scoped row into the admin tenant (id=1) regardless of which tenant is being processed. Also the SELECT/UPDATE path used to detect/update existing watermark rows does not constrain by tenant_id.
  - _Fix:_ Do not hardcode tenant_id. Pass the correct tenantId into setWatermark and use it in both SELECT/UPDATE and INSERT (WHERE tenant_id = ${tenantId} and VALUES (..., ${tenantId}, ...)). Alternatively, if this is intended to be global, move the watermark into a dedicated global table that is explicitly non-tenant-scoped and document its use. Ensure all UPDATE/DELETE statements also include tenant_id to avoid modifying other tenants' rows.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ... WHERE id = ... (no tenant constraint). This writes a tenant-scoped table without verifying tenant ownership in the WHERE clause, allowing potential cross-tenant modification if ids are misused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL) or perform an explicit ownership check before updating so an attacker cannot update rows belonging to another tenant.
- **server/data-protection.ts:131** — DELETE FROM messages WHERE conversation_id = ${conv.id} — DELETE against tenant-scoped table `messages` with NO tenant_id predicate. This can delete another tenant's rows if IDs collide or if conversation_id is ambiguous.
  - _Fix:_ Include tenant scoping in destructive queries. Example: DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Prefer parameterizing tenant_id from the validated conversation row to avoid races.
- **server/data-protection.ts:132** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} — destructive DELETE on tenant-scoped table without tenant_id predicate.
  - _Fix:_ Add tenant predicate to DELETE: DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Ensure every destructive statement includes tenant_id or is performed inside an explicit tenant-scoped transaction.
- **server/data-protection.ts:133** — DELETE FROM conversations WHERE id = ${conv.id} — deleting tenant-scoped `conversations` row without tenant_id predicate.
  - _Fix:_ Include tenant_id in DELETE: DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Require the tenant be passed/checked or perform deletion inside a tenant-scoped transaction to fail-closed on mismatch.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped tables (memory_entries / agent_knowledge) performed without constraining to tenant_id. storeEmbeddingVec updates embedding_vec / embedding WHERE id = ${id} with no tenant_id filter.
  - _Fix:_ Require and pass tenantId into storeEmbeddingVec and include an explicit tenant filter in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Avoid relying on id-only updates for tenant-scoped rows; always validate ownership before writing.
- **server/embeddings.ts:688** — SELECT from agent_knowledge (backfillMissingKnowledgeEmbeddings) with no tenant_id constraint — reads rows across all tenants.
  - _Fix:_ Make backfillMissingKnowledgeEmbeddings operate per-tenant (accept tenantId) or add a WHERE tenant_id = ${tenantId} clause. If the intent is to backfill global/system rows, document and use a dedicated global table; otherwise always scope by tenant.
- **server/embeddings.ts:716** — SELECT from memory_entries (backfillEmbeddingVecs) has no tenant_id constraint — reads embeddings across all tenants.
  - _Fix:_ Run backfill per-tenant (pass tenantId) or add WHERE tenant_id = ${tenantId} to the SELECT. Ensure subsequent UPDATEs also include tenant predicates so you cannot modify another tenant's rows by id alone.
- **server/embeddings.ts:726** — UPDATE memory_entries setting embedding_vec WHERE id = ${row.id} without tenant_id constraint — write may affect other tenants' rows.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the backfill in a per-tenant loop that binds tenantId as a parameter to both SELECT and UPDATE.
- **server/embeddings.ts:732** — SELECT from agent_knowledge (backfillEmbeddingVecs) without tenant_id constraint — reads rows across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} or refactor backfillEmbeddingVecs to operate per-tenant and accept tenantId parameter. Do not perform multi-tenant scans that read tenant data without explicit scoping.
- **server/embeddings.ts:743** — UPDATE agent_knowledge setting embedding_vec WHERE id = ${row.id} without tenant_id constraint — write may overwrite another tenant's row.
  - _Fix:_ Ensure the UPDATE includes AND tenant_id = ${tenantId} or run the update within a per-tenant loop where tenantId is provably bound for both the SELECT and UPDATE.
- **server/felix-brain.ts:19** — Unscoped SELECT from tenant-scoped table `agent_runs` performed at boot without any tenant_id filter. This reads all tenants' running/pending agent_runs into an in-memory store.
  - _Fix:_ Limit the query to a single tenant or iterate per-tenant under an explicit tenant context. e.g. add WHERE tenant_id = ${tenantId} (or run per-tenant with runWithTenant) so rows from other tenants are never read into the same in-memory store.
- **server/internal-resolver.ts:91** — UPDATE event_log finalization (finalizeEvent) mutates event_log rows by id with no tenant_id constraint. This allows the resolver to finalize (update) event_log rows belonging to any tenant.
  - _Fix:_ Include tenant scoping in the CAS UPDATE. Either accept/lookup the event's tenant_id and add AND tenant_id = ${tenantId} to the WHERE clause (or fetch the row first and validate tenant ownership), or change finalizeEvent to accept the event's tenantId and use it in the WHERE to ensure you never mutate another tenant's rows.
- **server/job-queue.ts:177** — completeJob updates agent_jobs to status='succeeded' WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but does not restrict by tenant_id. This allows marking another tenant's job succeeded by id.
  - _Fix:_ Require and use tenant_id when completing a job (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}), or validate the job's tenant before applying the update to enforce tenant isolation.
- **server/job-queue.ts:237** — failJob (terminal branch) updates agent_jobs to 'failed_terminal' WHERE id = ${id} ... without scoping by tenant_id, enabling cross-tenant mutation of job status/error.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or validate ownership via a prior SELECT) so a caller cannot mark jobs for other tenants as failed_terminal. Ensure all single-job writes incorporate tenant checks.
- **server/job-queue.ts:257** — failJob (retry branch) updates agent_jobs (status='pending', next_run_at, error, failure_class) WHERE id = ${id} ... with no tenant_id constraint. This can re-queue or change state of jobs from other tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id (AND tenant_id = ${tenantId}) or validate the job belongs to the caller's tenant before changing it. All job state transitions that target a single id must include tenant scoping to prevent cross-tenant state tampering.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT INTO tenant-scoped table `project_files` does not set tenant_id. Per platform invariant tenant_id columns are NOT NULL with no default; inserting without tenant_id will misattribute rows or fail and breaks tenant isolation.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Derive tenantId from the new project's tenant and ensure idMap/newProjectId mapping carries tenant context so every insert into tenant-scoped tables sets tenant_id.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT INTO tenant-scoped table `project_notes` does not set tenant_id. Writing tenant-scoped rows without tenant_id violates the invariant and can create cross-tenant contamination or insert failures.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, created_at, tenant_id) VALUES (${newProjectId}, ${n.note}, ${n.author ?? 'system'}, ${n.createdAt}, ${tenantId})). Ensure all tenant-scoped tables are populated with tenant_id on every INSERT and consider adding a code-level helper to enforce this across backfill logic.
- **server/lib/scheduled-post-runner.ts:266** — Postgres array literal `arrLit` is constructed by string-concatenating user-supplied platform names and then injected into an SQL template (VALUES (${params.tenantId}, ${arrLit}::text[], ...)). This is string-interpolated SQL using request-derived content and can lead to SQL injection or parsing surprises (even with naive escaping).
  - _Fix:_ Do not build SQL array literals by hand. Use parameterized arrays or Drizzle/pg features that bind arrays safely. For example, pass the JS array directly as a parameter and use a parameter cast to text[] (or use Drizzle's array helpers): VALUES (${params.tenantId}, ${lowered}::text[], ...) or use db.insert with the platform field as an Array value. Validate/normalize items and avoid manual template injection altogether.
- **server/minds-engine.ts:356** — decidePlan performs a CAS-style UPDATE on plans without scoping by tenant_id (UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval'). This allows flipping the status of a plan owned by another tenant (cross-tenant mutation) and violates tenant isolation.
  - _Fix:_ Require an explicit tenantId or ensure the UPDATE constrains tenant ownership (e.g. AND tenant_id = ${expectedTenantId}, where expectedTenantId is derived from the authenticated/admin context). Validate caller privileges and fail if tenant is missing or mismatched. Avoid blind updates by id only on tenant-scoped tables.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table project_files does not include tenant_id. The code calls assertProjectInTenant(...) but the VALUES list for INSERT omits tenant_id (columns: project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by). Per invariant, every INSERT into a tenant-scoped table must explicitly set tenant_id (NOT NULL, no default).
  - _Fix:_ Include tenant_id as an explicit column/value in the INSERT and pass the verified tenant (e.g. ${options.tenantId}). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep/assert the ownership check (assertProjectInTenant) before inserting to ensure tenant owns the project.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table project_files (in parallel path) does not include tenant_id. Even though assertProjectInTenant(...) is called, the INSERT's VALUES omit tenant_id, violating the requirement that tenant_id be passed explicitly on every insert to tenant-scoped tables.
  - _Fix:_ Add tenant_id to the INSERT column list and values (use ${options.tenantId} or equivalent) and ensure assertProjectInTenant(...) succeeded before executing the INSERT. For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...).
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions without any tenant_id filter (reads all tenants' active subscriptions).
  - _Fix:_ Restrict this query to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or run per-tenant in an explicitly-authorized administrative routine. Avoid global scans of oauth_subscriptions that leak cross-tenant data (tokens/metadata).
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh: SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE reads all active subscriptions across tenants (no tenant_id filter).
  - _Fix:_ Either run proactive refresh per-tenant with an explicit tenantId parameter, or restrict the query with WHERE tenant_id = ${tenantId}. Ensure any background task that touches tenant-scoped rows is explicitly scoped and authorized.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE has no tenant_id constraint and iterates/allows cross-tenant reads.
  - _Fix:_ If this is intended to operate on all tenants, require explicit admin privileges and document the risk. Prefer per-tenant operations (pass tenantId) or use an authorized maintenance path; avoid unscoped reads of tenant-scoped sensitive tables.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} is not constrained by tenant_id — can return a persona from another tenant.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or verify tenant ownership before returning persona data. Ensure callers must provide/confirm the tenantId or that this export path is authorized for cross-tenant access only for admins.
- **server/plan-executor.ts:116** — UPDATE against tenant-scoped table 'plans' does not include tenant_id in WHERE (appendExecutionLog uses WHERE id = ${planId} only). This allows cross-tenant modification by planId.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or otherwise bind/verify the plan's tenant before the update. Pass the authoritative tenant id into appendExecutionLog (or load+validate the plan row) and use it for the WHERE filter.
- **server/plan-executor.ts:513** — CAS UPDATE to claim a plan (UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status='approved') has no tenant_id constraint — it can claim plans across tenants.
  - _Fix:_ Constrain the claim to the plan's tenant (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved') or validate the returned row. Ensure executePlan is invoked with/validates the caller tenant context before performing updates.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — missing tenant_id filter when marking a plan failed (no tenant scoping).
  - _Fix:_ Apply tenant scoping on the UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or validate the plan row's tenant before performing status changes.
- **server/plan-executor.ts:725** — Final status UPDATE on plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks tenant_id constraint — writes to a tenant-scoped table without scoping.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}) or otherwise verify the plan's tenant prior to updating. Prefer parameterizing and passing the authoritative tenant id.
- **server/plan-executor.ts:759** — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id — allows cross-tenant state changes in plans.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or fetch+verify the plan's tenant before updating. Ensure all plan-state transitions include tenant scoping.
- **server/plan-executor.ts:775** — Boot-time recovery UPDATE resets any 'executing' plans to 'approved' globally (UPDATE plans SET status='approved' WHERE status='executing' AND updated_at < ...). No tenant filter is applied — this writes across all tenants.
  - _Fix:_ If intended to be per-tenant, run this recovery per-tenant or include a tenant_id filter (and restrict execution to an admin context). If it must operate cluster-wide, gate the function behind an explicit admin context and document that it touches all tenants; otherwise include tenant scoping.
- **server/plan-executor.ts:784** — SELECT id FROM plans WHERE status = 'approved' ... LIMIT 50 is read against tenant-scoped 'plans' table with no tenant_id filter — returns plans across tenants.
  - _Fix:_ Restrict the pending selection to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'approved') or iterate tenants explicitly. Ensure the executor runs with a known tenant context or an explicit admin scope before reading approved plans.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks issued without tenant constraint: UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — this mutates a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId}. Prefer using Drizzle .where(eq(table.id, t.id), eq(table.tenantId, tenantId)).
- **server/process-governor.ts:641** — UPDATE agent_desks performed without tenant constraint: UPDATE agent_desks SET ... WHERE id = ${desk.id} — writing to a tenant-scoped table without tenant_id filter.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or use a Drizzle .where with both id and tenantId) to ensure only the caller's tenant rows are updated.
- **server/process-governor.ts:977** — UPDATE governance_rules performed without tenant constraint: UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — this updates a tenant-scoped table by id only.
  - _Fix:_ Constrain the UPDATE by tenant_id as well: WHERE id = ${rule.id} AND tenant_id = ${tenantId}. Always include tenant_id on updates to tenant-scoped tables.
- **server/project-brain.ts:332** — Backfill query scans projects and conversations without tenant scoping: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... — no tenant_id filter on projects/conversations, so backfill iterates across all tenants' data.
  - _Fix:_ Run backfill per-tenant or add tenant filters (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}). Never perform cross-tenant scans without explicit operator-level authorization and separation.
- **server/project-brain.ts:349** — Selecting conversations by project_id with no tenant constraint: SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — conversation rows are not scoped to a tenant in this query.
  - _Fix:_ Include tenant scoping: WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId} (and ensure proj is resolved within the same tenant). Prefer requiring tenantId in the API and enforce it on all queries.
- **server/project-brain.ts:359** — Selecting messages for a conversation without tenant constraint: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — messages table likely tenant-scoped but is read without tenant_id filtering.
  - _Fix:_ Constrain by tenant: WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}, and ensure the function is driven by an explicit tenant context to avoid cross-tenant data exposure.
- **server/proposal-verifier.ts:106** — UPDATE code_proposals is executed without tenant constraint when clearing rebased_at/race info: UPDATE code_proposals ... WHERE id = ${proposalId} — this mutates a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Require tenantId in the verifier flow or validate ownership before updating proposals.
- **server/proposal-verifier.ts:246** — persist() updates code_proposals without tenant constraint: UPDATE code_proposals SET verification_status = ${status} ... WHERE id = ${proposalId} — this allows cross-tenant mutation of proposals' verification state.
  - _Fix:_ Add tenant restriction to the UPDATE (AND tenant_id = ${tenantId}) and change persist() to accept/require tenantId so verification updates cannot affect other tenants' proposals.
- **server/proposal-verifier.ts:372** — attemptRebaseOnDrift updates code_proposals to mark rejected without tenant constraint: UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} — mutates tenant-scoped rows without tenant verification.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}), and ensure attemptRebaseOnDrift is passed/validates the proposal's tenant before persisting changes.
- **server/proposal-verifier.ts:408** — attemptRebaseOnDrift persists rebased code_diff back to code_proposals via UPDATE ... WHERE id = ${proposalId} without tenant scoping — this writes to a tenant-scoped table without validating ownership.
  - _Fix:_ Constrain the UPDATE by tenant_id: WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Ensure all code_proposals mutations verify tenant ownership and accept tenantId in the flow.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` uses `WHERE id = ${r.id}` without constraining `tenant_id`. This can modify rows belonging to other tenants.
  - _Fix:_ Include the tenant guard in the WHERE clause (e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}`) or otherwise ensure the update is executed in an authenticated/authorized tenant context and parameterize the tenant id to prevent cross-tenant writes.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses `WHERE id = ${storedId}` with no `tenant_id` constraint after a tenant-scoped SELECT. This allows updating another tenant's row if `id` collides.
  - _Fix:_ Add tenant scoping to the UPDATE (for example `WHERE id = ${storedId} AND tenant_id = ${input.tenantId}`) or perform the UPDATE using a known tenant-scoped identifier to ensure the row belongs to the caller's tenant before mutation.
- **server/research-engine.ts:163** — UPDATE on tenant-scoped table `research_sessions` (cleanupZombieSessions) is executed without any `tenant_id` constraint — the statement can change sessions across all tenants.
  - _Fix:_ Run cleanup per-tenant or add an explicit tenant filter (e.g. `AND tenant_id = ${tenantId}`) or otherwise restrict this maintenance operation to a system-only maintenance context that is explicitly authorized. Never run non-tenant-scoped destructive updates on tenant-scoped tables in general request paths.
- **server/research-engine.ts:398** — DELETE FROM tenant-scoped table `agent_knowledge` (`DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()`) is executed without constraining `tenant_id`, which will remove rows for all tenants.
  - _Fix:_ Restrict deletion to a specific tenant (e.g. `DELETE ... WHERE source = 'autoresearch' AND expires_at < NOW() AND tenant_id = ${tenantId}`), or iterate per-tenant in a controlled system maintenance job. Ensure any global cleanup runs in a clearly documented, privileged maintenance context and records which tenant(s) were affected.
- **server/routes.ts:166** — Startup restore query selects from tenant-scoped table `file_storage` without any tenant_id constraint (db.select(...).from(fileStorage).where(...)). This will read rows for all tenants and writes files to the shared uploads directory, potentially exposing other tenants' files to the public uploads mount.
  - _Fix:_ Scope the restore to a specific tenant or run the restore per-tenant under an explicit admin context. E.g. add eq(fileStorage.tenantId, <admin-or-target-tenant>) to the WHERE or iterate tenants and restore into tenant-scoped subdirectories, or restrict the operation to ADMIN_TENANT_ID and ensure the restored files are written to a tenant-isolated path not globally served.
- **server/routes.ts:4325** — SELECT from tenant-scoped table (conversations) without constraining by tenant_id. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} ...` can return project_id for a conversation belonging to a different tenant, enabling cross-tenant data access.
  - _Fix:_ Include the tenant filter in the WHERE clause or use the already-loaded conversation object. Example: `SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL`. Better: avoid an extra DB hit and use the existing `conv.projectId` (which should have been loaded in tenant context) after verifying `conv.tenantId === tenantId`.
- **server/routes.ts:4331** — INSERT into tenant-scoped table (project_notes) does not set tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, so the new row will not be associated with the caller's tenant (or may be inserted with NULL), breaking isolation and enabling cross-tenant corruption/leakage.
  - _Fix:_ Always insert tenant_id explicitly and validate ownership of referenced foreign id (project_id) first. For example: ensure the project belongs to the caller's tenant, then `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'})`. Alternatively, use storage-layer helper that enforces tenant scoping and ownership validation.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes statement only supplies project_id, note, author and omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})) or derive it from a validated tenant (not from request body). Ensure the tenantId value is the authenticated caller's tenant and is passed as a bound parameter.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` does not set tenant_id (base64 upload path). The INSERT INTO project_files statement omits tenant_id.
  - _Fix:_ Add tenant_id to the INSERT columns and values (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Do not take tenant identifiers from request body; use the authenticated tenantId and bind it as a parameter.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` does not set tenant_id (multipart upload path). The INSERT INTO project_files statement omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Use the authenticated tenantId rather than any request-derived value.
- **server/sculptor.ts:94** — UPDATE sculptor_sessions sets status/result but the WHERE clause filters only by id (WHERE id = ${session.id}) and does NOT constrain by tenant_id. This allows cross-tenant modification of sessions by id.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or use Drizzle's .where(eq(sculptor_sessions.tenant_id, tenantId)). Ensure params.tenantId (or the canonical tenant id) is used here to enforce tenant isolation.
- **server/sculptor.ts:101** — UPDATE sculptor_sessions sets conversation_id but the WHERE clause filters only by id (WHERE id = ${session.id}) and does NOT constrain by tenant_id. Tenant isolation can be bypassed if an attacker knows another session id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or use a Drizzle .where(eq(..., tenantId)). Use the caller's tenant id from function scope when updating session rows.
- **server/sculptor.ts:156** — monitorSession performs UPDATE sculptor_sessions SET ... WHERE id = ${sessionId} without constraining tenant_id. monitorSession receives tenantId but the UPDATE omits it, enabling cross-tenant writes to session rows by id.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(sculptor_sessions.tenant_id, tenantId)). Always include the tenantId parameter when updating tenant-scoped tables in background/async monitors.
- **server/sculptor.ts:346** — UPDATE sculptor_sessions sets review_result WHERE id = ${sessionId} but does not include tenant_id in the WHERE clause. This can overwrite another tenant's session review_result if the id is known/guessable.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). If tenantId isn't available in this scope, fetch and verify the session row belongs to the caller's tenant before updating, or pass tenantId into the function and use Drizzle's .where(eq(..., tenantId)).
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} but does NOT limit the UPDATE to the reservation's tenant (no tenant_id filter). A reservation id alone is being used to update ledger rows across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Because this function currently lacks tenant context, either accept/derive tenantId as a parameter, or first SELECT the reservation row and verify its tenant_id matches the caller before performing the UPDATE. Prefer Drizzle .where(eq(agent_cost_ledger.tenant_id, tenantId)).
- **server/second-opinion.ts:423** — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without constraining tenant_id. This can release or alter ledger rows belonging to other tenants if reservation ids are known or guessable.
  - _Fix:_ Require and use tenantId when updating ledger rows (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or verify ownership by selecting the reservation row first. Update the function signature to accept tenantId or otherwise ensure the reservation belongs to the caller before calling UPDATE; use Drizzle .where(eq(..., tenantId)).
- **server/seed.ts:3213** — INSERT into tenant-scoped table project_conversations without tenant_id: INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) — tenant_id is not provided. Per invariant tenant_id is required on INSERTs and must be validated/explicit.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conversationId})). Ensure tenant ownership is validated for project_id and conversation_id before inserting to prevent cross-tenant linking.
- **server/seed.ts:3205** — SELECT reads from tenant-scoped table `conversations` (and checks `project_conversations`) without any tenant_id filter: SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id). This can enumerate and operate on rows for other tenants and enables cross-tenant writes later in the script.
  - _Fix:_ Add explicit tenant scoping to the query (e.g. WHERE c.tenant_id = ${tenantId} AND ... and ensure the inner EXISTS uses tenant checks or joins to tenant-scoped projects). Use Drizzle .where(eq(conversations.tenantId, tenantId)) and ensure project_conversations is checked/filtered by tenant ownership before taking action.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` lacks a tenant_id constraint: db.update(experiments).set(...).where(eq(experiments.id, experiment.id)). An attacker or buggy caller could update another tenant's row by id.
  - _Fix:_ Constrain updates by tenant as well as id, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Always include the caller's tenant_id in the WHERE for UPDATE/DELETE on tenant-scoped tables.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table `experiments` also lacks a tenant_id constraint: .where(eq(experiments.id, experiment.id)) in the catch handler. Same privilege/isolation risk as the normal-path update.
  - _Fix:_ Add tenant-scoped constraint to this UPDATE as well (include eq(experiments.tenantId, tenantId)). Ensure all code paths updating tenant-scoped rows enforce tenant ownership.
- **server/self-reflection.ts:366** — UPDATE on tenant-scoped table `memory_entries` performed without scoping by tenant_id: the code updates WHERE id = <existing id> only. This can modify another tenant's memory entry by id.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = <id> AND tenant_id = ${tenantId}) or obtain the row using tenant-scoped lookup first. Always constrain UPDATE/DELETE by tenant_id on tenant-scoped tables.
- **server/stability-watchdog.ts:211** — DELETE FROM `heartbeat_tasks` performed without any tenant constraint — this can delete rows across all tenants (cross-tenant destructive write).
  - _Fix:_ Fail-closed: add tenant scoping to the DELETE (e.g. WHERE tenant_id = ${tenantId} AND enabled = false AND type = 'delegation' AND created_at < ...) or enumerate tenants and delete per-tenant after verifying ownership. Ensure destructive operations always require explicit tenantId or an explicit admin/maintenance mode guarded and audited.
- **server/stability-watchdog.ts:225** — DELETE FROM `event_log` executed with no tenant filter — deletes may affect all tenants' event logs.
  - _Fix:_ Add a tenant filter (WHERE tenant_id = ${tenantId} ...) or perform per-tenant deletes with explicit tenantId. If this is intended to be global, require an explicit admin flag and comprehensive audit logging to avoid accidental cross-tenant deletion.
- **server/stability-watchdog.ts:259** — DELETE FROM `heartbeat_logs` uses an INTERVAL but no tenant constraint — this will prune logs for every tenant rather than being scoped to a tenant.
  - _Fix:_ Scope the prune to a tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...) or iterate tenants explicitly. Do not perform destructive cleanups across tenants without an explicit, auditable admin intent.
- **server/storage.ts:480** — UPDATE on tenant-scoped table `conversations` without tenant_id constraint in deletePersona transaction (.update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id))). This will clear personaId across all tenants' conversations.
  - _Fix:_ Scope these cross-table updates by tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant. Require the caller to pass a tenantId and enforce WHERE tenant_id = ${tenantId} for each update/delete touching tenant-scoped tables.
- **server/storage.ts:481** — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint in deletePersona (.update(memory_entries).set({ status: 'superseded' }).where(eq(memory_entries.personaId, id))). This will modify memory entries across tenants.
  - _Fix:_ Require and enforce tenant scoping (e.g. .where(and(eq(memory_entries.personaId, id), eq(memory_entries.tenantId, tenantId)))). Do not perform tenant-crossing updates without an explicit admin workflow.
- **server/storage.ts:838** — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint in archiveExpiredMemories (UPDATE memory_entries SET status = 'archived' WHERE ...). This will archive memories across all tenants indiscriminately.
  - _Fix:_ If archiving must be per-tenant, iterate per-tenant or add a tenantId parameter and include WHERE tenant_id = ${tenantId}. If the job intentionally runs globally, make that explicit and run under an admin-only path that is reviewed and audited.
- **server/storage.ts:855** — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint in archiveStaleMemories (UPDATE memory_entries SET status = 'archived' WHERE ...). This will affect all tenants.
  - _Fix:_ Same mitigation as archiveExpiredMemories: add tenant scoping or make the global admin intent explicit and gated. Prefer per-tenant scoped updates to prevent cross-tenant data mutation.
- **server/storage.ts:1303** — Multiple SELECTs in getAllDataForExport read tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) without tenant filters and return full platform data. This exposes all tenants' data in one export.
  - _Fix:_ Restrict this endpoint to an explicit admin-only flow. Require an admin authentication check and/or a tenant parameter to export only a single tenant; otherwise ensure audited, privileged access and consider regenerating per-tenant exports only.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` (setting implementation) uses WHERE id = tool.id with NO tenant_id constraint. This can modify another tenant's tool row if ids are guessed or misused, allowing cross-tenant modification of code (RCE risk).
  - _Fix:_ Always constrain mutating updates to the tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, /* owning tenant id */))). Ensure the function has/uses the authoritative tenantId for the target row (validate ownership before update).
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` uses WHERE id = tool.id with NO tenant_id constraint. This can delete another tenant's tool row (cross-tenant destructive operation).
  - _Fix:_ Constrain deletes by tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, /* owning tenant id */))). Verify caller ownership before performing DELETE and fail if tenant mismatch.
- **server/tools.ts:2876** — INSERT into tenant-scoped table project_conversations does not include tenant_id. The code inserts (project_id, conversation_id) without explicitly setting tenant scope, relying on implicit project/conversation ownership checks elsewhere.
  - _Fix:_ Include tenant_id in the INSERT columns and values (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conversationId}, ${tenantId})). Alternatively, assert both project and conversation belong to the caller's tenant and store tenant_id on the row so reads/writes can be explicitly scoped. Do NOT rely solely on foreign keys or separate updates for tenant scoping.
- **server/tools.ts:2980** — INSERT into tenant-scoped table project_files omits tenant_id. The INSERT populates project_id and file metadata but does not set tenant_id on the new row.
  - _Fix:_ Add tenant_id to the INSERT columns and supply the caller's tenantId (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by, tenant_id) VALUES (..., ${tenantId}) ). Also keep or add an explicit ownership check (SELECT on projects) before the insert to ensure the project belongs to the tenant.
- **server/tools.ts:2994** — INSERT into tenant-scoped table project_notes omits tenant_id. The code inserts (project_id, note, author) without explicit tenant scoping on the new row.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). Also ensure you validated project ownership before inserting; do not rely on implicit FK constraints alone.
- **server/tools.ts:3018** — INSERT into tenant-scoped table project_conversations (link_conversation flow) omits tenant_id. After asserting the conversation belongs to the tenant, the code inserts project_conversations without recording tenant_id on the new row.
  - _Fix:_ Record tenant_id on the inserted project_conversations row (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). Keep the assertConversationInTenant guard, but still persist tenant_id to every tenant-scoped row so future reads/writes may reliably filter by tenant.

### HIGH (197)

- **server/ab-optimizer.ts:110** — runDueAbExperiments reads the experiments table without scoping by tenant (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'), exposing/processing other tenants' experiments.
  - _Fix:_ Constrain the query to a single tenant (e.g. add AND tenant_id = ${tenantId}) or change the design so the heartbeat runs per-tenant and accepts a tenantId. Avoid scanning tenant-scoped tables across tenants in a single un-scoped query.
- **server/agent-eval.ts:218** — UPDATE against tenant-scoped table agent_evals uses WHERE id = ${evalId} with NO tenant_id constraint. This can allow cross-tenant UPDATE if evalId is misused.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or otherwise validate ownership before updating. Prefer using the typed drizzle where(eq(agent_evals.tenantId, tenantId)) pattern.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting selects ai_insights rows WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER with NO tenant_id constraint, reading tenant-scoped rows across all tenants.
  - _Fix:_ Scope the SELECT to a specific tenant, or iterate tenants and validate tenant_id before operating. If global sweep is intended, make that explicit and ensure the operation runs under a privileged admin path with explicit safeguards — otherwise require tenant_id in the query.
- **server/agentic-engines.ts:344** — gatherOperationalData queries heartbeat_logs without a tenant filter (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'), reading logs across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to heapbeat_logs queries (and any other telemetry tables) so operational data is gathered per-tenant only, or explicitly accept & validate a tenant-scoped parameter for the sweep.
- **server/agentic-engines.ts:451** — runPredictiveEngine queries recent heartbeat_logs (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10) without filtering by tenant_id, exposing other tenants' heartbeat logs.
  - _Fix:_ Constrain the query with AND tenant_id = ${tenantId} (or accept tenantId and iterate per-tenant). Ensure all reads from tenant-scoped log tables include tenant_id.
- **server/agentic-engines.ts:519** — runOptimizationEngine selects heartbeat_logs performance aggregated across all rows (SELECT ... FROM heartbeat_logs GROUP BY ...) with NO tenant_id filter — reading/aggregating cross-tenant data.
  - _Fix:_ Include tenant scoping (WHERE tenant_id = ${tenantId}) or otherwise restrict the aggregation to a single tenant. Avoid global aggregation on tenant-scoped tables unless run as an explicit admin operation with safeguards.
- **server/agentic-engines.ts:533** — runOptimizationEngine selects heartbeat_tasks with WHERE enabled = true but NO tenant_id constraint (SELECT ... FROM heartbeat_tasks WHERE enabled = true), which reads tasks across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} or otherwise scope this query per-tenant. If the intention is to query global tasks, document that the table is global — otherwise require tenant filtering.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agent_approvals to set status='expired' for pending approvals whose expiresAt has passed, but the UPDATE lacks a tenant_id filter and will affect approvals across tenants.
  - _Fix:_ Scope the bulk expiry by tenant (e.g. accept a tenantId param and add eq(agentApprovals.tenantId, tenantId) to the WHERE), or iterate tenants and run per-tenant expiries. Never perform tenant-scoped state changes without a tenant filter.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' — tenant-scoped table queried with no tenant_id constraint.
  - _Fix:_ Restrict this query to a single tenant or make the multi-tenant intent explicit and safe. If this function should operate per-tenant, add a tenantId parameter and use WHERE tenant_id = ${tenantId}. If it must scan all tenants, ensure it runs only in a trusted admin/owner context (documented and access-restricted), and add auditing/authorization checks; alternatively enumerate tenants via a trusted admin API and call the tenant-scoped check for each to preserve explicit tenant-level filters.
- **server/agentic/runs.ts:34** — Missing tenant constraint on agent_runs UPDATE: appendStep() updates agent_runs WHERE id = runId but does not also require tenant_id = <caller tenant>. This permits mutation of another tenant's run by runId alone.
  - _Fix:_ Require and check the tenant context. Change appendStep signature to accept tenantId (or fetch and verify ownership) and include .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Validate the run belongs to the tenant before mutating it.
- **server/agentic/runs.ts:45** — Missing tenant constraint on agent_runs UPDATE: updateRunState() updates run state by runId only, without constraining tenant_id = <caller tenant>. This can overwrite state for runs belonging to other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and require tenantId be passed in or verified before updating state.
- **server/agentic/runs.ts:54** — Missing tenant constraint on agent_runs UPDATE: replaceRunState() performs an update by runId only and lacks a tenant_id filter, allowing cross-tenant state replacement.
  - _Fix:_ Require tenant verification and add tenant_id to the WHERE condition or otherwise assert run ownership before performing the update.
- **server/agentic/runs.ts:58** — Missing tenant constraint on agent_runs UPDATE: completeRun() marks a run completed by runId without constraining tenant_id, enabling completion of runs from other tenants if only runId is known.
  - _Fix:_ Add tenant scoping to the update (AND eq(agentRuns.tenantId, tenantId)) and require the caller to supply/verify tenantId to prevent cross-tenant mutation.
- **server/agentic/runs.ts:88** — Missing tenant constraint on agent_runs UPDATE: failRun() updates a run to failed by runId without filtering by tenant_id, permitting cross-tenant writes.
  - _Fix:_ Require tenantId context and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Verify the run belongs to the tenant before changing status.
- **server/agentic/runs.ts:97** — Missing tenant constraint on agent_runs UPDATE: pauseRun() updates status/state by runId only and does not ensure the run belongs to the caller's tenant.
  - _Fix:_ Add tenant_id to the WHERE clause and require/verify tenantId from the caller to enforce tenant-scoped updates (e.g. where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))).
- **server/auto-asset-capture.ts:184** — backfillProjectAssets queries conversations without a tenant filter: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... has no WHERE tenant_id = ... and will enumerate conversations across all tenants.
  - _Fix:_ Avoid scanning tenant-scoped tables without tenant constraints. Either restrict the query to a single tenant at a time (e.g. SELECT ... WHERE c.tenant_id = ${tenantId}) and iterate tenants explicitly from a trusted tenant list, or ensure this backfill runs in a privileged, audited maintenance job that intentionally and safely enumerates tenants. Prefer per-tenant loops to preserve application-enforced isolation.
- **server/auto-memorize.ts:47** — getWatermark reads agent_knowledge without a tenant filter: SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} — this reads a tenant-scoped table across tenants (global watermark).
  - _Fix:_ Make the watermark tenant-scoped: include tenant_id in the SELECT (e.g. AND tenant_id = ${tenantId}) so each tenant has its own watermark row, or maintain a separate global, non-tenant-scoped config table for process-level watermarks. Ensure any reads/writes to agent_knowledge always include the appropriate tenant_id.
- **server/auto-memorize.ts:163** — runAutoMemorize pulls messages across tenants in one query: SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ${windowStart} AND m.created_at <= ${windowEnd} AND m.role IN ('user','assistant') — there is no tenant_id constraint, so the code reads multiple tenants' messages in a single unscoped query.
  - _Fix:_ Avoid global reads from tenant-scoped messages table. Instead, iterate per-tenant: determine tenants to process (from a trusted tenants table or per-tenant watermark), then query messages WHERE tenant_id = ${tenantId} AND created_at BETWEEN ... for each tenant. If the goal is to batch across tenants for efficiency, make that an explicit, audited maintenance operation and ensure you never mix messages from different tenants in a single LLM prompt.
- **server/auto-transcript.ts:128** — SELECT from tenant-scoped table 'conversations' without scoping by tenant_id — backfill query reads conversations across all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE c.tenant_id = ${tenantId}) or iterate per-tenant and pass tenantId into the query so the backfill cannot return rows from other tenants. If this is intended to be a global admin operation, ensure it runs under a privileged/admin-only code path and not in logic that can be invoked by non-admin tenants.
- **server/auto-transcript.ts:153** — SELECT joining project_conversations / conversations / projects returns rows across tenants (no tenant filter). This reads tenant-scoped tables without constraining by tenant_id.
  - _Fix:_ Scope the join by tenant (e.g. add WHERE c.tenant_id = ${tenantId} or p.tenant_id = ${tenantId}) or perform the operation per-tenant. Ensure any returned conversation/project pairs are validated for the calling tenant before further processing.
- **server/auto-transcript.ts:171** — SELECT from conversations (missingLinks query) with no tenant filter — selects c.id, c.project_id, c.tenant_id for all conversations with project_id that are missing project_conversations links, across all tenants.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or run this backfill per-tenant after enumerating tenant ids. If this is a global maintenance job, ensure it runs in a trusted admin context and not in tenant-exposed code paths.
- **server/capability-review.ts:491** — UPDATE to tenant-scoped table `capability_reviews` performed without constraining by tenant_id. The code updates a row by id only: `UPDATE capability_reviews SET reused = true ... WHERE id = ${pending.reviewId} AND reused = false`. This allows touching another tenant's row if the id is known or the in-memory mapping is poisoned.
  - _Fix:_ Include tenant scoping in the UPDATE. Record the tenantId alongside the pending review when inserting (extend PendingReuse to store tenantId) and execute the update with an explicit tenant check, e.g. `WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false`. This prevents cross-tenant updates even if ids are guessed or reused.
- **server/ceo-orchestrator.ts:841** — Reading the `personas` table with `db.select().from(personasTable)` without any tenant filter. Persona rows appear to be tenant-scoped; fetching all personas (across tenants) and later selecting an id by name can expose or mix personas from other tenants and cause incorrect cross-tenant assignments.
  - _Fix:_ Restrict persona lookups to the plan's tenant. Query `personas` with an explicit tenant filter (e.g. `.where(eq(personasTable.tenant_id, ${plan.tenantId}))`) or otherwise load a per-tenant persona cache. Ensure any persona id used when creating conversations or delegations belongs to the same tenant as the operation.
- **server/channels/kernel.ts:180** — dispatchInbound calls processMessage(conversationId, ...) using a conversationId returned by the adapter-provided resolveConversation without verifying that the conversation belongs to turn.tenantId. The kernel does not check tenant ownership before invoking code that will read/write tenant-scoped rows, so a malicious or buggy adapter could pass an ID from another tenant and cause cross-tenant access.
  - _Fix:_ Before passing a conversationId (or any foreign ID from adapter input) into processMessage, validate ownership against the inbound turn. Options: (a) have resolveConversation return the conversation record or {conversationId, tenantId} and assert returned tenantId === turn.tenantId; (b) have the kernel fetch the conversation with an explicit tenant filter (e.g. SELECT ... WHERE id = ? AND tenant_id = ? or Drizzle .where(eq(table.tenantId, tenantId))) and fail if not found; or (c) change processMessage to require tenantId and ensure every DB query inside it includes WHERE tenant_id = tenantId. Do not trust adapter-supplied IDs without explicit tenant checks.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` lacks a tenant_id constraint. The query: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC` does not include `tenant_id`.
  - _Fix:_ Constrain every read to the caller's tenant. Either add `AND tenant_id = ${tenantId}` to the WHERE clause, or JOIN via a tenant-scoped parent (e.g. join projects p on p.id = project_files.project_id AND p.tenant_id = ${tenantId}). Alternatively, use Drizzle .where(eq(projectFiles.tenantId, tenantId)) so the tenant is enforced at query time.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` lacks a tenant_id constraint. The query: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ORDER BY created_at DESC LIMIT 20` does not include `tenant_id`.
  - _Fix:_ Add an explicit tenant filter (e.g. `AND tenant_id = ${tenantId}`) or JOIN to a tenant-checked parent (projects) that includes `p.tenant_id = ${tenantId}`. Prefer using Drizzle `.where(eq(projectNotes.tenantId, tenantId))` to make tenant scoping explicit and robust.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table `compaction_archives` lacks a tenant_id constraint. The query: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ORDER BY archived_at DESC LIMIT 1` does not include `tenant_id`.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or equivalent) to the WHERE clause to ensure the archive read is limited to the caller's tenant. If relying on conversation ownership, still enforce tenant_id in the query (or JOIN conversations with `tenant_id = ${tenantId}`) so every access is explicitly tenant-scoped.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` lacks a tenant_id constraint. The query: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC` does not include `tenant_id`.
  - _Fix:_ Constrain message reads to the tenant by adding `AND tenant_id = ${tenantId}` (or using Drizzle `.where(eq(messages.tenantId, tenantId))`). Even when selecting by conversation_id, include the tenant filter explicitly (or join conversations and assert `conversations.tenant_id = ${tenantId}`) so tenant isolation is enforced on every query.
- **server/chat-engine.ts:4332** — storage.attachCitations(...) is invoked without passing tenant context. If attachCitations inserts into or updates a tenant-scoped table it may perform an INSERT/UPDATE without an explicit tenant_id, or rely on a lookup by message id that lacks an application-level tenant filter.
  - _Fix:_ Change attachCitations to accept an explicit tenantId (or message owner tenant id) and use it in all writes/queries (e.g. INSERT ... tenant_id = $tenantId, SELECT ... WHERE id = $id AND tenant_id = $tenantId). Alternatively, load & validate the message row first with a WHERE tenant_id = tenantId check before attaching citations so cross-tenant writes are impossible.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, ...) is called without tenantId. The helper likely writes tool-execution rows (or reads conversation metadata) using only conversationId — risking cross-tenant writes/reads if the underlying implementation does not enforce tenant ownership.
  - _Fix:_ Propagate and pass tenantId into recordToolExecution and ensure the implementation enforces tenant scoping on all queries and INSERTs (e.g. include tenant_id in INSERT columns and add WHERE tenant_id = $tenantId to any SELECT/UPDATE/DELETE). If the call takes a foreign id (conversationId), verify ownership first via SELECT ... WHERE id = conversationId AND tenant_id = tenantId before performing any mutations.
- **server/chat-engine.ts:4008** — Second call to recordToolExecution(conversationId, ...) (with ids) is also invoked without tenant context. Same risk as above for the follow-up success-recording path.
  - _Fix:_ Same fix: pass tenantId into recordToolExecution for this call path and ensure recordToolExecution uses the tenantId to constrain all reads/writes, or perform an explicit ownership check on the conversation id before recording execution details.
- **server/commitment-drafter.ts:234** — Unscoped UPDATE on tenant-scoped table commits without tenant filter: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — no `AND tenant_id = ...` present.
  - _Fix:_ Constrain the UPDATE to the owning tenant. e.g. `UPDATE commitments SET ... WHERE id = ${row.id} AND tenant_id = ${row.tenantId}` (and verify row.tenantId is a valid positive integer before executing). Prefer verifying ownership first and fail-closed if tenant is invalid.
- **server/commitments.ts:135** — SELECT from tenant-scoped `commitments` lacks tenant restriction: `SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' ...` reads rows across all tenants.
  - _Fix:_ If this query must be tenant-scoped, add `AND tenant_id = ${tenantId}`. If an admin/global scan is intended, make the intent explicit and gate the routine behind an admin-only path and redact any tenant-sensitive fields; or enumerate tenants and run a per-tenant query so every statement that touches tenant-scoped tables is executed with an explicit tenant filter.
- **server/credential-vault.ts:169** — UPDATE on credential_vault is missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — allows updating another tenant's row if id matches.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}`. Ensure the function caller supplies and validates tenantId (and/or confirm ownership via a prior SELECT) before performing the UPDATE.
- **server/credential-vault.ts:176** — UPDATE on credential_vault is missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — no `tenant_id` condition.
  - _Fix:_ Constrain the UPDATE to the tenant: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}` (ensure tenantId is validated/derived from the authenticated request). Avoid performing updates keyed only by id on tenant-scoped tables.
- **server/data-protection.ts:118** — SELECT on tenant-scoped table `conversations` without scoping to a caller tenant (no WHERE tenant_id = ...). This enumerates soft-deleted conversations across all tenants.
  - _Fix:_ When enumerating expired soft-deletes, either accept an explicit tenantId and scope the query (WHERE tenant_id = ${tenantId}), or make it an explicit multi-tenant admin job that iterates tenants and scopes each query. Never select tenant-scoped rows without an explicit tenant predicate.
- **server/data-protection.ts:182** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} — read from tenant-scoped `messages` without an explicit tenant_id predicate. Reads should never rely solely on a foreign id for tenant isolation per project invariant.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause, or join to the validated conversation row and add an explicit tenant predicate. Example: WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}.
- **server/data-protection.ts:189** — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} — reading tenant-scoped `compaction_archives` without tenant_id constraint.
  - _Fix:_ Scope reads by tenant: WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}, or join via conversation -> ensure explicit tenant check before querying archive rows.
- **server/data-protection.ts:24** — Schema migration creates memory_categories with `tenant_id INTEGER DEFAULT 1`. This introduces a default tenant value and violates the rule that INSERTs must pass tenant_id explicitly (no implicit defaults).
  - _Fix:_ Remove the DEFAULT. Ensure tenant_id is NULL/NOT NULL as appropriate and require the application to always supply tenant_id on INSERT. Example migration: ALTER TABLE memory_categories ALTER COLUMN tenant_id DROP DEFAULT; and consider ALTER TABLE ... ALTER COLUMN tenant_id SET NOT NULL (only if all inserts will provide a value). Audit code paths that insert into this table to pass tenant_id explicitly.
- **server/data-protection.ts:48** — ALTER TABLE agent_knowledge ADD COLUMN IF NOT EXISTS tenant_id INTEGER DEFAULT 1 — adding tenant_id with DEFAULT 1 creates implicit assignment to tenant 1 for inserts that don't specify tenant_id.
  - _Fix:_ Avoid defaulting tenant_id. Remove the DEFAULT (ALTER TABLE agent_knowledge ALTER COLUMN tenant_id DROP DEFAULT) and make tenant_id required to be set by application code. If a migration must backfill values, do it explicitly and then remove the default.
- **server/data-protection.ts:325** — SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} — read from tenant-scoped `project_notes` without scoping by tenant_id. Using project_id alone is not sufficient per project invariant (no FK-based enforcement).
  - _Fix:_ Restrict the query to the tenant: add AND tenant_id = ${tenantId} or join through projects with p.tenant_id = ${tenantId}. Example: WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}.
- **server/data-protection.ts:336** — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} — reading tenant-scoped `project_files` without tenant_id in WHERE clause.
  - _Fix:_ Add tenant scoping: WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}, or perform a join to verify project -> tenant and include explicit tenant_id predicate to enforce isolation.
- **server/data-protection.ts:369** — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} — counting/reading tenant-scoped `messages` without tenant_id predicate.
  - _Fix:_ Include tenant scoping: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}. Prefer always passing tenantId through and asserting it in the WHERE to avoid cross-tenant inference or races.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog performs UPDATE on delivery_logs without scoping the WHERE clause to tenant_id (where(eq(deliveryLogs.id, id))). This lets an attacker or mis-scoped caller modify another tenant's delivery row by id.
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog and include it in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or verify ownership in the same transaction before applying updates. Audit all callsites to ensure they provide the tenantId / ownership proof.
- **server/delivery-pipeline.ts:1018** — getDeliveryByStripePayment SELECTs from delivery_logs using stripePaymentId with NO tenant_id filter. This can return another tenant's delivery row for the same payment id.
  - _Fix:_ Accept a tenantId (or require caller context) and include eq(delivery_logs.tenant_id, tenantId) in the WHERE clause, or otherwise validate that the returned row belongs to the calling tenant before returning sensitive delivery fields.
- **server/doc-collections.ts:165** — addDocument executes DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} WITHOUT a tenant_id constraint. This can delete chunks belonging to other tenants sharing the same collection_id/doc_path (or permit cross-tenant deletion in multi-tenant setups).
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = ${tenantId}) or otherwise verify the collection belongs to the tenant before deleting. Use parameterized tenantId in the same SQL statement/transaction.
- **server/email.ts:366** — startInboxPolling checks for existence with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without tenant/inbox scoping. This can incorrectly consider a message already-stored based on another tenant's row and may allow cross-tenant interference or omission.
  - _Fix:_ Include tenant identification (tenant_id or inbox_id) in the WHERE clause (for example: WHERE message_id = ${msgId} AND inbox_id = ${inboxId} LIMIT 1) so the existence check is scoped to the tenant/inbox before skipping/storing messages.
- **server/email.ts:410** — backfillEmptyBodies queries inbox_messages with SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id with NO tenant/inbox scoping. This reads rows for all tenants and can leak or modify cross-tenant data during backfill.
  - _Fix:_ Scope the SELECT to a specific tenant or inbox (e.g. WHERE tenant_id = ${tenantId} OR inbox_id = ${inboxId}) or run the backfill per-tenant/inbox in an admin-only job. Ensure any updates are done within a tenant-scoped WHERE.
- **server/email.ts:424** — backfillEmptyBodies updates inbox_messages: UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} without confirming tenant_id. Updating by id alone can affect another tenant's row if the calling context is not tenant-limited.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or fetch the tenant/inbox for each row earlier and use that to constrain the UPDATE. Run backfill per-tenant/inbox to avoid cross-tenant updates.
- **server/embeddings.ts:244** — Use of sql.raw() with a vector literal derived from user/request input: 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) — vector comes from generateEmbedding(query) (request-derived). Even though vecLiteral validates numbers, using sql.raw with request-derived content increases injection risk and bypasses DB parameterization.
  - _Fix:_ Avoid injecting string-interpolated SQL. Instead pass the vector as a parameter in a parameterized query or use Drizzle primitives that bind values (or a placeholder with proper casting). If raw is unavoidable, ensure strict validation (as already done) and document why parameterization is not feasible; preferably change to a parameterized form that avoids string interpolation.
- **server/embeddings.ts:252** — ORDER BY uses sql.raw() with an interpolated vector literal (embedding) derived from caller input (ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is string-interpolated SQL with request-derived content.
  - _Fix:_ Parameterize the vector value instead of interpolating it into sql.raw(), or use Drizzle support for binary/vector parameters. Keep validation, but prefer parameter binding to eliminate any possibility of SQL injection and to preserve isolation semantics.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) ). The vec is derived from generateEmbedding(query) (request-derived).
  - _Fix:_ Replace the sql.raw interpolation with a parameterized bind or other Drizzle-safe mechanism to pass the vector value to Postgres. Validate the embedding shape and use parameter binding/casting rather than injecting a string literal.
- **server/embeddings.ts:565** — ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} uses sql.raw() with a request-derived vector literal — injection/isolation risk.
  - _Fix:_ Avoid sql.raw() injection of dynamic, request-derived payloads. Use parameterized queries or Drizzle's binding approach for vector types, and ensure embeddings are validated before use.
- **server/episode-playbooks.ts:68** — String-interpolated embedding literal (${embeddingLiteral}::vector) is embedded directly into SQL for similarity calculation/ORDER BY using template interpolation. embeddingLiteral is derived from a runtime embedding (request-derived). This bypasses parameterized binding and risks injection if embedding data is poisoned.
  - _Fix:_ Use parameterized binding for the vector value or a Drizzle-supported mechanism to pass the vector safely (do not inline an embedding string into the SQL text). If you must construct a vector literal, validate it strictly and prefer sql.raw with safe placeholders and parameters rather than injecting interpolated text.
- **server/episode-playbooks.ts:135** — SQL uses string-interpolated array literal for id ANY(...) clause: WHERE tenant_id = ${tenantId} AND id = ANY(${`{${ids.join(",")}}`}::int[]). Building an SQL array string via interpolation is unsafe and bypasses parameterization.
  - _Fix:_ Pass ids as a parameterized array instead of building '{...}' via string concatenation. Use a parameterized ANY/IN (e.g. WHERE id = ANY(${ids}::int[])) where ids is an array parameter, or use Drizzle's inArray helper to safely bind the list. This prevents injection and keeps queries properly parameterized.
- **server/felix-loop.ts:676** — UPDATE of tenant-scoped table `felix_loop_runs` uses WHERE id = ${runId} with no tenant_id constraint. This can allow modifying another tenant's run if an id is reused/guessed.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}. Always constrain updates on tenant-scoped tables to the caller's tenant.
- **server/felix-loop.ts:690** — Error-path UPDATE of `felix_loop_runs` uses WHERE id = ${runId} with no tenant_id constraint (same missing tenant scoping as above).
  - _Fix:_ Constrain the UPDATE to the tenant as well: WHERE id = ${runId} AND tenant_id = ${tenantId}. Ensure all writes/updates against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/firecrawl.ts:214** — db.update(scrapedPages) uses .where(eq(scrapedPages.id, existing[0].id)) without also constraining tenant_id. The existing row was discovered via a tenant-scoped select, but the UPDATE itself lacks an explicit tenant filter.
  - _Fix:_ Add tenant scoping to the UPDATE condition (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or carry the tenant check through to the update to prevent cross-tenant writes/updates.
- **server/firecrawl.ts:344** — Inside crawl logic, existing scraped page rows are updated via .where(eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint. This updates by id only (missing explicit tenant scoping).
  - _Fix:_ Include tenant scoping in the UPDATE (AND eq(scrapedPages.tenantId, tenantId)) or reuse the original tenant-qualified selector to avoid touching rows belonging to other tenants.
- **server/google-drive.ts:547** — ensureProjectFolder selects project row by project id only (where eq(projects.id, projectId)) without first constraining or verifying tenant ownership before reading. The code later checks project.tenantId, but the initial read still exposes tenant data without prior scoping/authorization.
  - _Fix:_ Query projects with tenant scoping up-front: WHERE id = ${projectId} AND tenant_id = ${tenantId} (or perform an explicit ownership-check before returning any project fields). Avoid selecting cross-tenant rows and then checking ownership — use tenant-scoped queries to prevent information exposure.
- **server/google-drive.ts:560** — db.update(projects) sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) and no tenant_id constraint. Even though the code earlier validated ownership, the UPDATE should still be scoped to the tenant to avoid accidental cross-tenant writes.
  - _Fix:_ Add tenant scoping to the UPDATE: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Always include tenant_id in WHERE on updates to tenant-scoped tables.
- **server/google-drive.ts:826** — uploadAndShare reads a project row by id (where eq(projects.id, params.projectId)) without first restricting by tenant_id; the code then uses proj.tenantId to validate/compare. Reading the referenced row without a tenant-scoped WHERE is an information-exposure/ownership-verification gap.
  - _Fix:_ Select the project using a tenant-scoped WHERE (e.g. WHERE id = ${projectId} AND tenant_id = ${params.tenantId}) or perform an explicit ownership check before returning any project fields. Avoid querying cross-tenant rows and then relying on post-hoc checks.
- **server/google-drive.ts:946** — INSERT INTO project_files does not include tenant_id in the inserted columns/values. Per project_files being tenant-scoped, every INSERT must set tenant_id explicitly — otherwise rows can be created without an explicit tenant binding.
  - _Fix:_ Add tenant_id to the INSERT (and bind the caller's tenant id). e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${tenantId}, ...). If tenantId cannot be derived, fail-closed rather than insert without tenant ownership.
- **server/google-workspace.ts:2358** — INSERT into tenant-scoped table presenter_slide_images without setting tenant_id (no tenant scoping on insert).
  - _Fix:_ Ensure presenter_slide_images is written with an explicit tenant_id column value (e.g. include tenantId in the INSERT). Alternatively, verify the session ownership first (SELECT presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId}) and fail if it doesn't match, then insert with the verified tenant_id.
- **server/google-workspace.ts:2364** — INSERT into tenant-scoped table presenter_slide_images (thumbnail row) without setting tenant_id.
  - _Fix:_ Same fix as above: verify the presenter session belongs to the expected tenant before persisting thumbnails, and include tenant_id in the INSERT values (or use an INSERT that derives tenant_id from a verified session row).
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — update on a tenant-scoped resource performed using only the foreign id with NO tenant_id constraint or ownership check.
  - _Fix:_ Verify ownership of the session (SELECT id, tenant_id FROM presenter_sessions WHERE id = ${sessionId}) and ensure tenant_id matches the caller's tenant before updating. Prefer updating with an explicit tenant-scoped WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}).
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — modifying tenant-scoped rows with no tenant_id constraint (mass update by id only).
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or perform the update only for IDs that were selected for the expected tenant after an explicit ownership check. If this is an intended global admin sweep, require explicit admin authorization and make that explicit in code.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — disabling tasks by id with NO tenant_id constraint (write across tenants).
  - _Fix:_ Only disable tasks after verifying they belong to the expected tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or run the archive/disable logic per-tenant and/or behind an explicit admin guard so tenant isolation is preserved.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table research_schedules is missing a tenant_id constraint (SELECT * FROM research_schedules ...). This reads schedules across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant (getActiveTenantIds + per-tenant query) so reads are scoped to a caller/tenant context.
- **server/heartbeat.ts:101** — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint. Writes to a tenant-scoped table must also ensure the row belongs to the expected tenant.
  - _Fix:_ Include tenant guard in WHERE clause (e.g. AND tenant_id = ${sched.tenant_id}) or perform the UPDATE via a storage helper that asserts tenant context before modifying rows.
- **server/heartbeat.ts:106** — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint. This advances next_run_at/last_run_at without scoping to tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage method that enforces tenant ownership.
- **server/heartbeat.ts:112** — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint (reschedules without tenant scoping).
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via storage API that validates tenant ownership.
- **server/heartbeat.ts:120** — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint when writing recovery next_run_at.
  - _Fix:_ Include AND tenant_id = ${sched.tenant_id} in the WHERE clause (or use a tenant-aware storage wrapper) to avoid touching other tenants' schedule rows.
- **server/heartbeat.ts:658** — SELECT from tenant-scoped table event_log: SELECT * FROM event_log WHERE status = 'pending' ... has no tenant_id filter. This pulls pending events across all tenants.
  - _Fix:_ Scope the selection per-tenant (e.g. include WHERE tenant_id = ${tenantId}) or iterate active tenants and query event_log for each tenant separately so event processing cannot mix tenant contexts.
- **server/heartbeat.ts:260** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} lacks a tenant_id constraint. heartbeat_tasks is tenant-scoped and updating by id only can act across tenants.
  - _Fix:_ Add tenant constraint (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId} or AND tenant_id = ${ADMIN_TENANT_ID} when seeding admin tasks) or use storage.updateHeartbeatTask which enforces tenant scoping.
- **server/heartbeat.ts:264** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} is performed without a tenant_id filter (tenant-scoped table).
  - _Fix:_ Include tenant scoping (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the change through a tenant-aware storage API to guarantee the correct tenant owns the row.
- **server/heartbeat.ts:2828** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id constraint. This reads heartbeat_logs across tenants when deciding to skip a tenant-scoped task.
  - _Fix:_ Scope this lookup to the task's tenant (e.g. WHERE task_name = ... AND status = 'success' AND tenant_id = ${tenantId}) so the decision uses only the tenant's logs.
- **server/heartbeat.ts:2922** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without a tenant_id constraint after creating a new tenant-scoped task.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or set approval_status as part of the storage.createHeartbeatTask call to avoid an extra un-scoped UPDATE.
- **server/heartbeat.ts:3374** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (after creating a chat-delegation task) lacks a tenant_id constraint.
  - _Fix:_ Ensure the UPDATE includes AND tenant_id = ${tenantId} or avoid the extra raw UPDATE by setting approval_status when creating the task via the tenant-aware storage API.
- **server/internal-resolver.ts:106** — SELECT id, objective, status FROM plans WHERE id = ${planId} executes without constraining by tenant_id. The resolver reads a plan row by id without verifying the tenant ownership of that plan.
  - _Fix:_ Restrict the SELECT to the owning tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or first fetch the event row to obtain the tenant_id and then validate that the plan belongs to that tenant before reading/mutating. Never read plan rows across tenants without an explicit tenant filter and ownership check.
- **server/internal-resolver.ts:170** — UPDATE code_proposals sets status/reviewed_by etc. WHERE id = ${proposalId} ... but does not include tenant_id. This can archive or otherwise mutate another tenant's code_proposal rows by id.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or fetch/validate the proposal's tenant before updating. Ensure the resolver only mutates code_proposals that belong to the event's tenant.
- **server/job-queue.ts:98** — claimDueJobs performs an UPDATE on agent_jobs (setting status='running', attempts++, lease_until) over rows selected without a tenant_id filter. This updates rows across tenants without tenant scoping on the write.
  - _Fix:_ If this operation is intended to be tenant-scoped, include tenant_id = ${tenantId} in the selection/update or ensure the caller provides/validates tenant context. If it must run globally, document and guard it as an explicit system/global operation; for single-job operations prefer including tenant_id in the WHERE to avoid accidental cross-tenant writes.
- **server/job-queue.ts:134** — reclaimExpiredLeases updates agent_jobs (marking failed_terminal / re-queued) without tenant_id constraint, affecting jobs across tenants.
  - _Fix:_ When transitioning individual tenant jobs prefer adding tenant_id = ${tenantId} to the UPDATE WHERE clause or otherwise ensure only legitimate system-level code can perform global reclaim. For per-tenant bulk cancels, require an explicit tenant parameter and use it in the WHERE clause.
- **server/job-queue.ts:275** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) without tenant_id in WHERE. An operator or code path could cancel a job belonging to another tenant by id.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or require an explicit tenant parameter and validate ownership before cancelling. Avoid operating on agent_jobs by id alone without tenant validation.
- **server/job-queue.ts:283** — retryJob resets a job's state via UPDATE ... WHERE id = ${id} AND status IN (...) but does not include a tenant_id constraint, allowing cross-tenant job retries.
  - _Fix:_ Scope the UPDATE by tenant_id (AND tenant_id = ${tenantId}) or validate the job's tenant prior to resetting attempts/next_run_at. Require tenant context for any write that targets a job id.
- **server/lib/agent-trace.ts:129** — UPDATE against tenant-scoped table agent_trace_spans uses only span_id in WHERE (WHERE span_id = ${spanId}) with NO tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE. Thread the tenant id into persistSpanClose (or resolve currentTenantId() reliably at close time) and add AND tenant_id = ${tenantId} to the WHERE clause (or use a parameterized .where(eq(agent_trace_spans.tenantId, tenantId))). Do not update cross-tenant rows by span id alone.
- **server/lib/bwb-job-progress.ts:141** — UPDATE against tenant-scoped table video_jobs uses .where(scope(jobId,...)) but scope() may omit the tenant predicate when BWB_TENANT_ID is missing, resulting in an unscoped UPDATE that can affect rows across tenants.
  - _Fix:_ Ensure the WHERE always includes tenant_id. Require the caller to provide tenantId (or fail if env BWB_TENANT_ID is not set), or change scope() to always require/validate a tenantId and fail closed if missing. Use an explicit clause like AND video_jobs.tenant_id = ${tenantId}.
- **server/lib/bwb-job-progress.ts:166** — UPDATE against tenant-scoped table video_jobs uses .where(scope(jobId,...)) where scope() can drop tenant_id, causing possible cross-tenant writes when env tenant is absent.
  - _Fix:_ Always constrain updates by tenant_id. Pass tenantId explicitly into these progress update functions and add eq(video_jobs.tenantId, tenantId) to the WHERE (or make scope() fail if tenant is not available), rather than relying on jobId-only scoping.
- **server/lib/bwb-job-progress.ts:200** — SELECT FROM tenant-scoped table video_jobs uses .where(scope(jobId)) — scope() may omit tenant_id, so this read can return another tenant's row if BWB_TENANT_ID is missing.
  - _Fix:_ Require and pass a tenantId into the select, or ensure scope() always includes tenant predicate. Replace scope(jobId) with a predicate that includes eq(video_jobs.tenantId, tenantId) (or fail if tenantId is not available) to enforce tenant isolation.
- **server/lib/bwb-job-progress.ts:227** — UPDATE against tenant-scoped table video_jobs (heartbeat bump) uses .where(scope(jobId,...)) which can omit tenant_id, allowing cross-tenant updates when tenant context is not threaded.
  - _Fix:_ Enforce tenant scoping on heartbeats: accept tenantId explicitly or fail when the env tenant is missing, and add eq(video_jobs.tenantId, tenantId) to the WHERE clause instead of relying solely on jobId.
- **server/lib/bwb-job-progress.ts:245** — UPDATE against tenant-scoped table video_jobs (failBwbJob) uses .where(scope(jobId,...)) where scope() may drop tenant_id, resulting in an unscoped failure mark that could affect other tenants' rows.
  - _Fix:_ Prevent unscoped failure writes by requiring a tenantId and adding it to the WHERE clause (e.g. AND video_jobs.tenant_id = ${tenantId}), or make scope() fail-closed when env tenant is not set. Avoid relying on jobId-only randomness for tenant isolation.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped table `projects` without constraining to a specific tenant (reads all tenants). The query groups by tenant_id but still scans tenant-scoped rows for every tenant in the DB.
  - _Fix:_ Restrict the query to only the tenants relevant to the backfill snapshot (e.g. WHERE tenant_id = ANY(${Array.ofTenantIds}::int[])) or perform the check per-tenant: iterate wantedByTenant and run a tenant-scoped COUNT SELECT with WHERE tenant_id = ${tenantId}. Avoid scanning all tenants in a multi-tenant environment.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT from tenant-scoped table `project_files` uses only project_id/file_name and omits tenant_id. Reading a tenant-scoped table without an explicit tenant filter can leak or mix rows across tenants.
  - _Fix:_ Add an explicit tenant filter to the SELECT, e.g. WHERE project_id = ${newProjectId} AND file_name = ${f.fileName} AND tenant_id = ${tenantId}. Alternatively derive tenant_id from the mapped project and always include it in WHERE clauses when querying tenant-scoped tables.
- **server/lib/ideabrowser-backfill.ts:116** — SELECT from tenant-scoped table `project_notes` uses only project_id and note and omits tenant_id. Reading tenant-scoped rows without tenant constraint risks returning or touching other tenants' data.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE project_id = ${newProjectId} AND note = ${n.note} AND tenant_id = ${tenantId}). Always include tenant_id in WHERE clauses when accessing tenant-scoped tables, even when using project_id.
- **server/lib/mcp-api-keys.ts:69** — String construction of a Postgres array literal from caller-provided `scopes` (scopesLit) is inserted into the SQL template. This builds SQL array text via string concatenation of request-derived values rather than using a proper parameterized array binding — risk of injection or malformed array parsing and breaks the intended safe parameterization pattern.
  - _Fix:_ Do not build SQL array literals by concatenating user input. Bind the scopes as a parameterized array (or use Drizzle/pg helpers) so the DB driver performs safe escaping/typing. Example fixes: (a) use Drizzle/pg array helper such as sql.array(scopes, 'text') inside the sql template, or (b) pass a JSON/JSONB parameter and convert to text[] server-side (e.g. use a parameterized SELECT to unnest into text[]), e.g. INSERT ... VALUES (${tenantId}, ${name}, ${prefix}, ${hash}, ${sql.array(scopes, 'text')}, ${createdBy || null}) — or otherwise use parameter binding APIs instead of constructing a literal string.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a runtime-derived column name (constructed from the caller-supplied `outcome`) in the UPDATE statement. Even though the code validates the outcome and whitelist-checks the column, this pattern uses a request-derived value to build SQL via sql.raw(), which risks SQL-injection / maintenance mistakes and violates the “no string-interpolated SQL from request data” invariant.
  - _Fix:_ Avoid dynamic sql.raw() injection. Replace with explicit, static SQL branches: switch on `outcome` and run one of four parameterized UPDATEs where the column name is a compile-time literal (e.g. UPDATE trust_scores SET action_alpha = action_alpha + $1 ...). Alternatively use the Drizzle API to update fixed column identifiers (one query per allowed outcome). Keep tenant_id/persona_id/category in the WHERE clause (they already are).
- **server/lib/scheduled-post-runner.ts:60** — CTE selects/updates rows from tenant-scoped table `scheduled_posts` without filtering by tenant_id. The initial SELECT ... FOR UPDATE and the subsequent UPDATE in the same CTE operate across all tenants (no tenant_id filter), violating the requirement that reads/writes to tenant-scoped tables include tenant constraints tied to the caller’s tenant.
  - _Fix:_ If this runner must operate per-tenant, add an explicit tenant_id filter (e.g. WHERE tenant_id = ${someTenantId}) or iterate tenants and lock per-tenant. If it is intentionally a system-scoped job that must act across tenants, document and centralize that exception and ensure the code runs only in an authorized admin context and that every follow-up UPDATE/side-effect also explicitly uses the returned tenant_id to limit changes (e.g. include AND tenant_id = ${row.tenant_id} in the UPDATE WHERE clause).
- **server/lib/scheduled-post-runner.ts:174** — UPDATE of scheduled_posts uses WHERE id = ${row.id} with no tenant_id constraint. Mutating a tenant-scoped row without constraining by tenant_id can allow cross-tenant effects if the caller context expects tenant-scoped enforcement.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) so that updates are explicitly constrained to the tenant owning the row. If the runner is system-scoped and intends cross-tenant updates, require and pass the tenant_id explicitly in the operation and document the privileged context.
- **server/lib/scheduled-post-runner.ts:191** — Error-path UPDATE of scheduled_posts (catch block) performs UPDATE ... WHERE id = ${row.id} AND status = 'publishing' without constraining by tenant_id. This will persist per-platform results/last_error without an explicit tenant filter.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. AND tenant_id = ${row.tenant_id}) when writing per-row recovery/failure state. Prefer the same defender pattern as the normal success path (explicit tenant binding) so all writes are unambiguous and tenant-scoped.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the row id in WHERE (.where(eq(tenantVoiceProfiles.id, existing.id))) and does not include a tenant_id constraint. Per invariant, writes against tenant-scoped tables must be constrained by tenant_id.
  - _Fix:_ Include the tenant filter in the UPDATE WHERE clause to ensure the row belongs to the caller's tenant, e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively, perform the UPDATE via a parameterized UPSERT that keys on (tenant_id, profile_name) or otherwise ensure tenant_id is part of the WHERE to avoid cross-tenant writes.
- **server/memory-graph.ts:141** — UPDATE memory_categories sets memory_count using a subquery that counts memory_entries without scoping by tenant_id (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} ...). This can aggregate counts across tenants.
  - _Fix:_ Add tenant scoping to the subquery, e.g. include AND tenant_id = ${tenantId} (and pass tenantId into refreshCategoryCount), or join memory_categories→memory_entries and filter by memory_categories. Ensure every SELECT in the subquery is parameterized with the caller's tenantId.
- **server/memory-graph.ts:81** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — no tenant_id constraint. This will modify a category row regardless of tenant, enabling cross-tenant mutation/confusion.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}. Pass tenantId through and use parameterized binding.
- **server/memory-graph.ts:196** — SELECT 1 FROM memory_links ... LIMIT 1 queries memory_links with no tenant filter. Reading a tenant-scoped table without tenant_id allows cross-tenant information leaks/logic errors.
  - _Fix:_ Include tenant scope in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or join through an owned row (memory_entries) that is tenant-scoped to ensure the link belongs to the caller's tenant before proceeding.
- **server/memory-graph.ts:210** — INSERT INTO memory_links (...) VALUES (...) does not include tenant_id. Tenant-scoped tables must have tenant_id passed on INSERT; omitting it writes tenant-ambiguous rows and breaks tenant isolation.
  - _Fix:_ Add tenant_id to the INSERT columns and values (INSERT INTO memory_links (tenant_id, source_memory_id, target_memory_id, ...) VALUES (${tenantId}, ${newMemoryId}, ${target.id}, ...)). Also validate that both source and target memory_entries belong to the same tenant before inserting.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id constraint. Updating by id alone can modify another tenant's row if ids collide or are guessed.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Prefer verifying ownership earlier and always include tenant_id in all DML against tenant-scoped tables.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — missing tenant_id constraint after creating a new memory entry. This update writes a tenant-scoped table by id only.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId} (pass tenantId into the callback). Alternatively, use storage APIs that enforce tenant ownership.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — missing tenant check in the update path for superseded/replacement entries.
  - _Fix:_ Constrain the UPDATE by tenant_id: WHERE id = ${entry.id} AND tenant_id = ${tenantId}. Ensure every update on memory_entries includes the caller's tenant parameter.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — no tenant_id constraint. Even though c was selected earlier with tenant_id, the UPDATE itself lacks explicit tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE to enforce defense-in-depth (UPDATE ... WHERE id = ${c.id} AND tenant_id = ${tenantId}). Do not rely solely on prior SELECTs for isolation guarantees.
- **server/minds-engine.ts:460** — Rollback UPDATE of plans (UPDATE plans SET status = 'awaiting_approval', ceo_decision = NULL, ... WHERE id = ${args.planId} AND status = 'revising') omits tenant_id. This may roll back a plan in another tenant.
  - _Fix:_ Constrain the rollback UPDATE with AND tenant_id = ${tenantId} (use the same tenant context used to create the revise attempt). Ensure all corrective/rollback DML also include tenant scoping.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id constraint (enumerates tenant rows).
  - _Fix:_ If the intent is to operate across tenants, require an explicit admin/maintenance path and protect it with authorization. Otherwise, add WHERE tenant_id = ${tenantId} or iterate a validated list of tenant IDs obtained from a safe, authorized source.
- **server/orchestrator-ledger.ts:445** — Using sql.raw(lockKey) inside SQL: SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint). lockKey is derived from ledgerKey (caller-provided) and tenantId — using sql.raw with a value that can come from callers risks SQL injection or unexpected SQL syntax.
  - _Fix:_ Do not inject caller-controlled values via sql.raw. Use a parameterized binding and proper validation/casting (e.g. compute a numeric BigInt server-side and pass it as a parameter: sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), and validate/normalize ledgerKey before use. Alternatively derive the lock in application code and pass a numeric parameter rather than raw SQL text.
- **server/pace-control.ts:85** — checkPace queries heartbeat_logs without any tenant_id filter (SELECT COUNT(*) ... FROM heartbeat_logs ... AND ${laneFilter}) — unscoped reads against what appears to be a tenant-scoped activity table.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or accept an explicit tenantId parameter so pacing checks only use the caller's tenant data. If this is intentionally global, restrict to an admin-only maintenance path with explicit authorization and auditing.
- **server/pace-control.ts:140** — getPaceSnapshot aggregates heartbeat_logs over a time window without filtering by tenant (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...).
  - _Fix:_ Scope snapshot queries to a tenant (WHERE tenant_id = ${tenantId}) or require an explicit admin/maintenance execution context. Avoid returning cross-tenant usage breakdowns unless the caller is authorized to view all tenants.
- **server/process-governor.ts:121** — Subselects count heartbeat_logs without scoping by tenant. The inner queries (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...) lack a tenant_id = ${tenantId} filter and thus can read logs across tenants.
  - _Fix:_ Add tenant scoping to the subselects, e.g. AND tenant_id = ${tenantId} (or use Drizzle .where(eq(table.tenant_id, tenantId))). Ensure EVERY read from heartbeat_logs is restricted to the caller's tenant.
- **server/process-governor.ts:155** — Subselect on heartbeat_tasks in desk_status lacks tenant scoping: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) has no tenant_id filter.
  - _Fix:_ Constrain the subselect by tenant, e.g. (SELECT COUNT(*) FROM heartbeat_tasks WHERE tenant_id = ${tenantId} AND persona_id = ad.persona_id AND enabled = true). Always include tenant_id for tenant-scoped tables.
- **server/process-governor.ts:199** — Global aggregation over heartbeat_logs without tenant filter: SELECT AVG(duration_ms) FROM heartbeat_logs WHERE ... has no tenant_id constraint and will aggregate across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))). Ensure all heartbeat_logs reads are tenant-scoped.
- **server/process-governor.ts:240** — cascading_failures query counts distinct persona_id from heartbeat_logs but omits tenant scoping: WHERE status != 'success' ... has no tenant_id filter.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or equivalent Drizzle filtering) to prevent cross-tenant aggregation/leakage.
- **server/project-brain.ts:135** — SELECT projects WHERE id = ${projectId} without tenant filter. updateProjectBrain reads project metadata by id only and does not verify that the caller/context owns the project, allowing cross-tenant access to project details.
  - _Fix:_ Require and pass a tenantId to updateProjectBrain and add WHERE id = ${projectId} AND tenant_id = ${tenantId} (or use Drizzle .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Always verify ownership before reading/writing tenant-scoped rows.
- **server/proposal-verifier.ts:33** — SELECT FROM code_proposals by id without tenant scoping: SELECT ... FROM code_proposals WHERE id = ${proposalId} — proposal ownership is not validated, enabling access to other tenants' proposals.
  - _Fix:_ Require tenantId (or verify ownership) before verifying a proposal, and scope the read: WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Use Drizzle's typed where helpers where possible.
- **server/recurring-messages.ts:258** — SELECT against tenant-scoped table `agent_knowledge` performs an unscoped read (no `tenant_id` WHERE clause).
  - _Fix:_ Avoid blanket scans across tenant data. If this is intended as a system-wide scheduler, make the intent explicit (e.g. a `_allowGlobalSystemUseOnly` code path) and restrict execution to a privileged system context; otherwise add `AND tenant_id = ${tenantId}` (or parameterize the tenant) so each caller only reads their own rows.
- **server/research-engine.ts:478** — INSERT into `project_notes` omits `tenant_id` (INSERT INTO project_notes (project_id, note, author, created_at) ...). Per design all tenant-scoped INSERTs must include tenant_id explicitly.
  - _Fix:_ Include the tenant id in the INSERT columns/values (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)`) or otherwise derive and pass the owning tenant_id explicitly when persisting tenant-scoped records.
- **server/research-engine.ts:486** — INSERT into `project_notes` inside the findings loop omits `tenant_id`, creating project notes without explicit tenant scoping.
  - _Fix:_ Add `tenant_id` to the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)`) so each inserted note is explicitly tied to the correct tenant; ensure the tenant value is validated/derived from the project ownership check.
- **server/routes.ts:1531** — Public stats endpoint performs SELECT COUNT(*) FROM tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) without any tenant_id WHERE clause. This publicly exposes aggregate counts across all tenants.
  - _Fix:_ Either require admin authentication for these cross-tenant aggregates or limit the response to truly global/shared tables. If per-tenant counts are intended, add WHERE eq(<table>.tenantId, tenantId) using the request's tenant. If global aggregates are intended, ensure the route is admin-only and clearly documented as global.
- **server/routes.ts:1559** — Public architecture endpoint runs multiple db.execute() SELECTs against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) with no tenant_id filters and returns names/rows (e.g. persona list). This leaks tenant-scoped data to a public endpoint.
  - _Fix:_ Restrict this endpoint to admin-only access or sanitize/aggregate the data so tenant-private rows and names are not exposed. If per-tenant data is required, add WHERE eq(<table>.tenantId, tenantId) and require authentication. Avoid returning raw tenant-scoped rows (names, roles) from a public route.
- **server/routes/admin-zombie-detector.ts:38** — Query against agent_trace_spans (tx.execute) has no tenant_id constraint — it aggregates tool usage across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId} in the WHERE or otherwise .where(eq(table.tenantId, tenantId))). If the intent is a platform-wide admin view, make the intent explicit (requirePlatformAdmin) and document that this endpoint intentionally returns cross-tenant data; otherwise enforce tenant scoping.
- **server/routes/admin-zombie-detector.ts:54** — Query over conversations (used AS (...) FROM conversations ...) lacks any tenant_id constraint and thus aggregates persona usage across all tenants.
  - _Fix:_ Constrain the subquery to the caller's tenant (e.g. WHERE tenant_id = ${tenantId} AND persona_id IS NOT NULL ...) so results are scoped to the session's tenant. If the endpoint is intended to be platform-global, make that explicit and protect it with a canonical platform-admin guard.
- **server/routes/admin.ts:587** — SELECT FROM ai_insights (stuckInsights) is executed without a tenant_id constraint — returns insights across tenants.
  - _Fix:_ If ai_insights is tenant-scoped, add WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq filter) to limit to the caller's tenant. If the handler is intentionally platform-global, require/verify platform-admin authority unambiguously and document the design decision.
- **server/routes/admin.ts:598** — Query selecting heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3 has no tenant_id constraint — can expose tenant-scoped heartbeat task rows across tenants.
  - _Fix:_ Add a tenant_id = ${tenantId} filter to the WHERE clause when returning tenant-scoped heartbeat tasks. If the endpoint intentionally reports platform-wide heartbeat task status, ensure the route is strictly guarded by the platform-admin check and document this behavior.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table `conversations` lacks a tenant_id constraint. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates by id only and does not include `AND tenant_id = <caller tenant>`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use conv.tenantId if available). Alternatively use a storage-layer helper that enforces tenant scope.
- **server/routes/conversations.ts:102** — INSERT into `project_conversations` omits tenant_id. The statement `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT and set it to the authenticated tenant. Example: `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`. Ensure the tenant value is the caller's, not user-supplied.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table `projects` lacks a tenant_id constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` updates by id only and omits `AND tenant_id = <caller tenant>`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Prefer verifying ownership earlier and applying tenant-scoped WHERE clauses on all writes.
- **server/routes/conversations.ts:228** — INSERT into `project_notes` omits tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` does not include tenant_id for a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT and set it to the conversation/project owner tenant (e.g. `${conv.tenantId ?? tenantId}`). Example: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`. Ensure tenant is derived from the authenticated context and not from client input.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` performed without tenant_id predicate. The code sets is_read true with WHERE id = ${msg.id} only, which can mutate any tenant's row if message id is guessed or an attacker can control msg.id.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or otherwise verify ownership before mutating. Change to: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:232** — UPDATE against tenant-scoped table `projects` lacks a tenant_id constraint. The UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} does not include AND tenant_id = ${tenantId}.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. This prevents cross-tenant mutation if project_id is corrupted.
- **server/routes/projects.ts:300** — UPDATE against tenant-scoped table `projects` lacks a tenant_id constraint. The UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} does not include AND tenant_id = ${tenantId}.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:319** — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint. The query SELECT * FROM project_files WHERE project_id = ${projectId} ... does not include tenant_id filtering on project_files.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY ...), or explicitly JOIN to projects and assert projects.tenant_id = ${tenantId} in the WHERE clause.
- **server/routes/projects.ts:342** — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint in download handler. Query is SELECT ... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 without filtering on tenant_id.
  - _Fix:_ Constrain the SELECT to the caller's tenant, e.g. SELECT ... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} AND tenant_id = ${tenantId} LIMIT 1. Do not rely solely on earlier parent checks; enforce tenant filter on the table being queried.
- **server/routes/projects.ts:371** — DELETE against tenant-scoped table `project_files` lacks tenant_id constraint. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not include tenant_id.
  - _Fix:_ Add tenant scoping to the DELETE, e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}. Even if the parent project was checked earlier, each operation on a tenant-scoped table must explicitly constrain tenant_id.
- **server/routes/stats.ts:55** — SELECT counting messages does not include an explicit tenant_id constraint on the messages table. The query uses messages.conversationId IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId}) instead of explicitly constraining messages.tenant_id.
  - _Fix:_ Constrain the tenant on the target table directly. Either add WHERE messages.tenant_id = ${tenantId} (or equivalent eq(messages.tenantId, tenantId)) or JOIN messages to conversations and include an explicit messages.tenant_id = ${tenantId} condition. Per policy, every read against a tenant-scoped table must include an explicit tenant_id constraint.
- **server/seed.ts:2860** — UPDATE on tenant-scoped table without tenant constraint: UPDATE research_programs SET model = ... WHERE id = ${p.id} updates by id only and lacks WHERE tenant_id = <tenant>. This can modify another tenant's data if ids collide or are global.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId} or .where(eq(research_programs.tenant_id, tenantId))). Validate that the row belongs to the expected tenant before applying updates.
- **server/seed.ts:3135** — UPDATE on tenant-scoped table without tenant constraint: UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... has no tenant_id filter, so it may modify a project belonging to a different tenant.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Always validate ownership before making changes to tenant-scoped rows.
- **server/seed.ts:3137** — UPDATE on tenant-scoped table `projects` has no tenant_id constraint (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL). This can modify rows across tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. add AND tenant_id = ${tenantId} (or WHERE id = ... AND tenant_id = ...) or lookup the project's tenant and include it. Prefer using parameterized Drizzle .where(eq(projects.tenantId, tenantId)).
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) occurs after a cross-tenant-unscoped SELECT. `project_conversations` has no tenant_id column in schema; inserting links without ensuring tenant ownership can create cross-tenant relationships or corrupt tenant isolation.
  - _Fix:_ Either add tenant_id to project_conversations and populate it explicitly on INSERT, or validate ownership before inserting by ensuring both project_id and conversation_id belong to the caller's tenant. Always include tenant_id in INSERTs to tenant-scoped relations or derive/verify it from trusted lookups.
- **server/seed.ts:4431** — DELETE FROM heartbeat_tasks ... runs without a tenant_id constraint. `heartbeat_tasks` is tenant-scoped (there are tenant indexes), so this DELETE may remove rows across all tenants.
  - _Fix:_ Constrain the DELETE to the intended tenant, e.g. DELETE ... WHERE tenant_id = ${tenantId} AND ... or use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)). Ensure any global cleanup logic explicitly scopes by tenant or runs per-tenant after determining the correct tenantId.
- **server/seed.ts:4459** — UPDATE memory_entries SET fact = REPLACE(...) WHERE ... executes on `memory_entries` without any tenant_id restriction. `memory_entries` is tenant-scoped (index exists), so this mass UPDATE will affect all tenants' memories.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}), or run the update per-tenant by selecting target tenant ids first. Use Drizzle .where(eq(memoryEntries.tenantId, tenantId)) so only the caller's tenant rows are modified.
- **server/seed.ts:4033** — Bulk db.insert(heartbeatTasks).values([...]) inserts default heartbeat_tasks entries without specifying tenant_id. `heartbeat_tasks` is tenant-scoped (tenant index exists); inserting without explicit tenant_id relies on a default and will seed data into the wrong tenant or multiple tenants.
  - _Fix:_ Include tenant_id explicitly in each inserted object (e.g. tenantId: ${tenantId}) or perform per-tenant inserts. Alternatively, if these rows are meant to be global, move them to a genuinely global table; otherwise ensure the seed sets tenant_id explicitly (and does not rely on a DEFAULT).
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` executed without a tenant_id constraint when tenantId is not provided. The query at db.select().from(experiments) (detectStagnation) can return rows across all tenants.
  - _Fix:_ Always include a tenant filter. Make tenantId required for this function or add a guard that forces eq(experiments.tenantId, tenantId) in the WHERE clause; never issue an unscoped read from a tenant-scoped table.
- **server/self-improvement.ts:521** — Unscoped SELECT from tenant-scoped table `experiments` in getExperimentHistory: when no category/tenant conditions are passed the function returns db.select().from(experiments) (all rows across tenants).
  - _Fix:_ Require and enforce a tenantId parameter for this API or, at minimum, refuse to run without tenantId. Add WHERE tenant_id = <tenantId> to all queries against tenant-scoped tables.
- **server/self-improvement.ts:536** — INSERT into tenant-scoped table `experiments` (logExperiment) uses data.tenantId! but the function signature allows tenantId to be optional. This can lead to inserts with an undefined/null tenant or runtime errors and potential cross-tenant misrouting.
  - _Fix:_ Make tenantId a required parameter (not optional), validate it before INSERT, and fail closed if missing. Ensure the INSERT always sets tenant_id explicitly from a verified tenant context.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` without constraining by tenant (distillIntent recentMsgs query uses only conversationId).
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or use tenantScope(messageTable.tenantId, tenantId) so reads are limited to the caller's tenant.
- **server/skillify.ts:201** — SELECT from tenant-scoped table `messages` without constraining by tenant (analyzeConversation reads all messages for conversationId with no tenant filter).
  - _Fix:_ Constrain message reads to the tenant (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or validate/require tenant ownership via assertConversationInTenant and add tenantScope to the query.)
- **server/skillify.ts:221** — SELECT from `personas` lacks tenant scoping (personaRows lookup uses only persona id). If `personas` is tenant-scoped this allows cross-tenant reads.
  - _Fix:_ Include tenant constraint on the personas query (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))) or otherwise verify persona ownership before reading.)
- **server/skillify.ts:329** — SELECT from tenant-scoped table `messages` without tenant constraint (skillifyConversation recentMsgs query uses only conversationId).
  - _Fix:_ Add explicit tenant scoping to this query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or use tenantScope to produce the equality predicate.)
- **server/stability-watchdog.ts:177** — SELECT aggregation from `heartbeat_logs` (error counts) has no tenant filter — this aggregates across all tenants and can lead to cross-tenant actions/decisions.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE tenant_id = ${tenantId}) or run per-tenant loops; if the watchdog must operate globally, explicitly enumerate tenants and scope each query to that tenant to avoid accidental cross-tenant aggregation.
- **server/storage.ts:295** — SELECT FROM tenant-scoped table `messages` without tenant_id constraint in attachCitationMetadata (SELECT id, citations FROM messages WHERE id IN (...)) — can leak citations across tenants.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. add AND tenant_id = ${tenantId}) and pass the calling tenantId into attachCitationMetadata. If callers legitimately need to fetch without tenant, limit and log/centralize that code path explicitly.
- **server/storage.ts:396** — UPDATE on tenant-scoped table `messages` without tenant_id constraint in attachCitations (UPDATE messages SET citations = ... WHERE id = ...). This can overwrite another tenant's message row.
  - _Fix:_ Require and validate tenantId in attachCitations and include it in the WHERE clause (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}).
- **server/storage.ts:245** — SELECT from tenant-scoped table `conversations` without tenant_id constraint in getConversationUnscoped (WHERE id = ...). Unscoped read of a tenant-scoped table can leak conversation data across tenants.
  - _Fix:_ Restrict use of getConversationUnscoped to tightly controlled internal callers only, and require callers to validate ownership. Prefer adding an explicit tenantId parameter and enforcing WHERE tenant_id = ${tenantId} where possible; add audit/guarding wrappers if unscoped reads are unavoidable.
- **server/storage.ts:341** — SELECT from tenant-scoped table `messages` without tenant_id constraint in upsertMessageFeedback (.from(messages).where(eq(messages.id, data.messageId))). This reads a tenant-scoped row without tenant scoping.
  - _Fix:_ Either accept tenantId as an explicit parameter or JOIN against messages with a tenant check, or include AND tenant_id = ${data.tenantId} in the WHERE clause after validating data.tenantId is trusted.
- **server/storage.ts:803** — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint in updateMemoryEmbedding (.where(eq(memory_entries.id, id))). This can modify another tenant's embedding row.
  - _Fix:_ Require a tenantId parameter and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), or re-fetch/verify the row's tenant before updating.
- **server/storage.ts:815** — UPDATE on tenant-scoped table `agent_knowledge` without tenant_id constraint in updateKnowledgeEmbedding (.where(eq(agent_knowledge.id, id))). This can update knowledge rows across tenants.
  - _Fix:_ Require and validate tenantId, and add the tenant check to the WHERE clause (.where(and(eq(agent_knowledge.id, id), eq(agent_knowledge.tenantId, tenantId)))).
- **server/storage.ts:824** — SELECT from tenant-scoped table `memory_entries` without tenant filter in getMemoriesWithoutEmbeddings (.where(eq(status,'active'), isNull(embedding))). This returns rows across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(and(eq(memory_entries.status,'active'), isNull(memory_entries.embedding), eq(memory_entries.tenantId, tenantId))) and require tenantId as an argument. If this is an admin/global job, mark and gate it explicitly.
- **server/storage.ts:830** — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in getKnowledgeWithoutEmbeddings (.from(agent_knowledge).where(...)). This returns rows across tenants.
  - _Fix:_ Add a tenantId parameter and include eq(agent_knowledge.tenantId, tenantId) in the WHERE clause; if intended as global maintenance, restrict to an explicit admin context and document it.
- **server/storage.ts:880** — SELECT from tenant-scoped table `memory_entries` without tenant filter in getMemoryStats (code path where tenantId is undefined uses full-table select). This returns other tenants' memories if called without tenantId.
  - _Fix:_ Make tenantId required for this function or default to explicit admin contexts only. Always include eq(memory_entries.tenantId, tenantId) when tenantId is provided and fail-closed when it is not.
- **server/storage.ts:921** — SELECT from tenant-scoped table `heartbeat_tasks` without tenant filter in getHeartbeatTasks (returns all rows when tenantId is not provided). This exposes heartbeat task metadata across tenants.
  - _Fix:_ Require tenantId or add per-tenant filtering. If global worker behavior is intended, gate it behind an admin-only API and document the cross-tenant behavior.
- **server/storage.ts:1445** — UPDATE on tenant-scoped table `architecture_decisions` without tenant filter in supersedeAdr (UPDATE architecture_decisions SET ... WHERE id = oldId / newId). Although rows were previously selected with tenant checks, the UPDATE itself lacks tenant scoping and could affect rows if state changed between select/update.
  - _Fix:_ Include tenant scoping on the UPDATEs (e.g. WHERE id = ${oldId} AND tenant_id = ${tenantId}) as a defense-in-depth check even after prior selects/validations.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table `felix_proposals` without tenant_id constraint in stampArgsEmbedding (UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL). This updates rows without verifying tenant ownership.
  - _Fix:_ Accept and validate tenantId in stampArgsEmbedding and add AND tenant_id = ${tenantId} to the WHERE clause to avoid cross-tenant writes.
- **server/surprise-scorer.ts:141** — UPDATE on tenant-scoped table `felix_proposals` without tenant_id constraint in scoreProposalSurprise (UPDATE felix_proposals SET actual_outcome_embedding = ... WHERE id = ${proposalId}). This can modify another tenant's row.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) in the UPDATE and require tenantId be passed/validated by the caller.
- **server/surprise-scorer.ts:149** — SELECT args_embedding FROM tenant-scoped `felix_proposals` WHERE id = ${proposalId} without tenant filter. Re-fetching the row by id without tenant scoping can expose/return another tenant's data.
  - _Fix:_ Add tenantId to the SELECT conditions (AND tenant_id = ${tenantId}) or otherwise validate ownership prior to the re-fetch.
- **server/surprise-scorer.ts:201** — SELECT AVG(...) FROM tenant-scoped `felix_proposals` WHERE id = ANY(${neighborIds}::int[]) without tenant scoping. Even though neighborIds were produced from a tenant-scoped query, this AVG uses arbitrary ids and lacks an explicit tenant check.
  - _Fix:_ Use a parameterized tenant-scoped query (e.g. WHERE id = ANY(${neighborIds}::int[]) AND tenant_id = ${tenantId}) to ensure the aggregation only touches same-tenant rows.
- **server/surprise-scorer.ts:213** — UPDATE felix_proposals SET surprise_score = ..., surprise_band = ... WHERE id = ${proposalId} without tenant constraint. This writes back scoring state without tenant scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE and require the caller to supply/validate tenantId. Maintain defense-in-depth even when prior checks existed.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (checks for existing tool name use `WHERE name = $name` only). This can leak or reason about other tenants' tool names and allows name-collision logic to operate cross-tenant.
  - _Fix:_ Scope the query to the caller's tenant. e.g. add tenant filter: .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Alternatively, explicitly check global/admin semantics only when intended and document it.
- **server/tool-learning.ts:155** — SELECT by id from tenant-scoped `custom_tools` without constraining tenant_id (reads inserted row by id only). Missing tenant filter can expose or return rows across tenants.
  - _Fix:_ Include tenant scoping in the lookup: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Prefer returning needed columns from the initial insert or propagate tenantId from the insert to avoid an unscoped select.
- **server/tool-learning.ts:365** — UPDATE to increment `usageCount` in tenant-scoped `custom_tools` uses WHERE id = tool.id with NO tenant_id constraint. This mutates a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Scope the update with tenant constraint: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, /* owning tenant id */))). Prefer passing tenantId into execute paths or resolving the row via a tenant-scoped lookup before mutating.
- **server/tools.ts:2911** — SELECT subqueries in project list omit tenant scoping for child tables. The list query uses (SELECT COUNT(*) FROM project_files WHERE project_id = p.id), (SELECT COUNT(*) FROM project_notes WHERE project_id = p.id), (SELECT COUNT(*) FROM project_conversations WHERE project_id = p.id) without restricting those counts to the caller's tenant.
  - _Fix:_ Constrain these subqueries to the tenant (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or join to projects and rely on p.tenant_id = ${tenantId}. Ensure every read against tenant-scoped tables includes an explicit tenant_id filter rather than relying on project_id alone.
- **server/tools.ts:2973** — Duplication-check SELECT against project_files does not include tenant constraint. The code checks for existing files via SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ... but does not include tenant_id in the WHERE clause.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or join to projects and verify p.tenant_id = ${tenantId}. Even if the project ownership was validated earlier, queries against tenant-scoped tables must still explicitly constrain by tenant_id per the project's invariant.
- **server/tools.ts:3014** — SELECT id FROM project_conversations used to detect existing link lacks tenant scoping. The query checks project_conversations by project_id and conversation_id but does not filter by tenant_id.
  - _Fix:_ Constrain this SELECT to the calling tenant (e.g. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}). Alternatively, join to projects and conversations and require p.tenant_id = ${tenantId} and c.tenant_id = ${tenantId}.
- **server/tools.ts:4550** — DB read from presenter_sessions selects by presentation_id without scoping by tenant. The query (SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...) lacks a tenant_id constraint and can return presenter session tokens belonging to other tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${params._tenantId}), or perform an explicit ownership check (assertPresentationInTenant or equivalent) before reading presenter_sessions. Ensure every SELECT/UPDATE/DELETE against tenant-scoped tables includes a tenant_id constraint or a verified ownership assertion.
- **server/tools.ts:6912** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) call omits tenant_id, so new rows may not be associated with the caller's tenant (columns are NOT NULL with no default per invariant).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)) or use a Drizzle insert with tenantId set. Also keep the existing assertProjectInTenant check (or similar) to validate ownership before inserting.
- **server/tools.ts:8872** — UPDATE against tenant-scoped table `proposed_skills` is missing a tenant constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) does not include tenant_id in the WHERE clause, allowing modification of a row with the same id from another tenant.
  - _Fix:_ Restrict the UPDATE by tenant, e.g. .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid}))) or otherwise include tenantId in the WHERE clause (use Drizzle's eq with the calling tenant id). Ensure tid is validated and comes from trusted context.
- **server/tools.ts:8914** — UPDATE against tenant-scoped table `proposed_skills` is missing a tenant constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) is executed in the REJECT branch without filtering by tenant_id.
  - _Fix:_ Add a tenant check to the UPDATE, e.g. .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, ${tid}))) or otherwise ensure the update only affects rows owned by the caller's tenant. Validate tenantId from trusted context before performing the update.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' missing tenant constraint — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} has no tenant_id filter.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: await wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); or use Drizzle's where(eq(browserWorkflows.tenantId, tenantId)) so the row update cannot affect other tenants.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files does not set tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (${projId}, ...) omits tenant_id which violates the rule that every INSERT into a tenant-scoped table must include tenant_id explicitly.
  - _Fix:_ Include tenant_id as an explicit column/value derived from the trusted context. Example: await db.execute(sql`INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${projId}, ${tenantId}, ${fileName}, ${absPath}, ${driveUrl || null}, ${fileType}, ${stat.size}, ${'agent'})`); Validate tenantId (ctx.tenantId) is present/positive beforehand.
- **server/tools/domains/files/handlers.ts:839** — UPDATE against tenant-scoped table 'projects' missing tenant constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} lacks a tenant_id filter, allowing an update without ensuring the project belongs to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE. Example: await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`); or use a parameterized Drizzle update with where(eq(projects.tenantId, tenantId)) to ensure only the caller's tenant rows are modified.
- **server/tools/domains/memory/handlers.ts:272** — Potential IDOR: the handler accepts a caller-supplied conversationId (params.conversationId) and later uses it to read archives/recall data without first verifying the conversation belongs to the caller's tenant. This allows an attacker who can supply arbitrary conversation IDs to attempt to access other tenants' conversation data.
  - _Fix:_ Do not trust params.conversationId directly. Either require and use the router-injected ctx.conversationId only, or explicitly validate ownership before using a caller-supplied conversationId — e.g. run a parameterized check: SELECT id FROM conversations WHERE id = $1 AND tenant_id = $2; if no row found, return an error. Apply this check before calling recallCompactionArchive or any DB reads that use the conversationId (also when handling non-projectWide paths).
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` executed without any tenant_id constraint. This returns rows across tenants (even though guarded by tenantId===1), violating the invariant that every tenant-scoped read must include an application-level tenant filter.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE tenant_id = ${tenantId}) and pass tenantId as a parameter. If the query is intentionally global for admin use, make that explicit (audit/approve the cross-tenant access) and ensure it is confined to a verified admin-only code path and logged/audited; otherwise restrict to the caller's tenant via tenant_id.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed without a tenant_id constraint. This aggregates provider keys across all tenants, exposing cross-tenant data to the caller (even if only run for admin tenant).
  - _Fix:_ Restrict the aggregation to a specific tenant by adding WHERE tenant_id = ${tenantId} (and pass tenantId as a parameter). If true cross-tenant aggregation by an owner/admin is required, make the cross-tenant intent explicit, ensure the code path is strongly admin-gated, and document/audit this exception; otherwise enforce per-tenant filtering.
- **server/tools/domains/strategic-memory/handlers.ts:95** — UPDATE memory_entries executed without scoping by tenant_id (WHERE id = ${id}) — writes to a tenant-scoped table are not constrained to the caller's tenant.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (and persona if appropriate). e.g. WHERE id = ${id} AND tenant_id = ${tid} (and AND persona_id = ${personaId} if per-persona scoping is required). Always ensure every UPDATE/DELETE against tenant-scoped tables includes tenant_id = <caller tenant>.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) executed without scoping by tenant_id — batch update may affect rows across tenants.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE, e.g. WHERE id = ANY(${ids}) AND tenant_id = ${tid} (or filter ids to those belonging to the tenant in the query). Ensure all batched UPDATE/DELETE statements include tenant_id = <caller tenant>.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries executed without scoping by tenant_id (WHERE id = ${id}) in recordStrategicWinHandler — write is not constrained to the caller's tenant.
  - _Fix:_ Restrict the UPDATE by tenant (and persona if applicable): WHERE id = ${id} AND tenant_id = ${tid} (and AND persona_id = ${personaId} if needed). All writes/updates/deletes against tenant-scoped tables must include the caller's tenant_id in the WHERE clause.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without filtering by tenant_id (SELECT id, content::jsonb FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ...). This loads pairing rows across all tenants and risks cross-tenant information exposure or misuse.
  - _Fix:_ Restrict the query to the caller/expected tenant where possible (e.g. add AND tenant_id = ${tenantId}). If intentionally enumerating across tenants for ambiguity detection, record a clear justification in code, minimize returned columns (avoid returning full content), and perform strict server-side validation of any returned content before using it. Prefer an explicit, audited admin-only path for cross-tenant scans or design a dedicated, auditable mechanism (e.g. one-time ownership-proof handshake) that does not expose tenant-scoped rows to general request data.
- **server/watchlist.ts:111** — Query selects from watchlist_alerts (aliased wa) and LEFT JOINs watchlist_items (aliased wi) but does not constrain the joined watchlist_items rows by tenant_id. This reads a tenant-scoped table (watchlist_items) without a tenant_id guard on the joined table.
  - _Fix:_ Include the tenant guard on the joined table. Example: change the JOIN to include tenant scoping (e.g. LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id AND wi.tenant_id = ${tenantId}) or add AND wi.tenant_id = ${tenantId} to the WHERE clause so every read of watchlist_items is constrained to the caller's tenant.
- **server/wiring-invariants.ts:180** — checkOutcomeCanary builds and runs a multi-subquery SQL that reads tenant-scoped tables (research_experiments, code_proposals via research_sessions, research_schedules) without constraining by tenant_id.
  - _Fix:_ Either scope these subqueries to a specific tenant via WHERE <table>.tenant_id = :tenantId (use Drizzle .where(eq(table.tenantId, tenantId))) or explicitly iterate over all tenants and run the checks per-tenant. If the intent is an auditable cross-tenant admin scan, require an explicit ADMIN tenant param and document/authorize the cross-tenant access; do not leave unconstrained SELECTs against tenant-scoped tables.
- **server/wiring-invariants.ts:244** — checkDormantTools runs SELECT count(*) FROM tool_performance WHERE last_success_at > ... without any tenant_id filter (reads tenant-scoped telemetry across all tenants).
  - _Fix:_ Add tenant scoping (WHERE tenant_id = :tenantId) or perform the aggregation per-tenant by enumerating tenant IDs. If an intentional global admin aggregation is required, make the admin tenant explicit and audit/log the operation.
- **server/wiring-invariants.ts:258** — checkDormantTools executes SELECT count(*) FROM tool_performance (everResult) without a tenant constraint, reading total rows across all tenants.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = :tenantId) or run it per-tenant. For legitimate global diagnostics, require an explicit admin-authorized path that documents cross-tenant reads and uses safe parameterization.
- **server/wiring-invariants.ts:260** — checkDormantTools executes SELECT count(*) FROM messages WHERE created_at > ... with no tenant_id filter, reading user chat messages across tenants.
  - _Fix:_ Constrain message counts to a tenant via WHERE tenant_id = :tenantId or iterate tenants. Do not run unconstrained queries against message tables; if a global metric is required, build an explicit, authorized aggregation pipeline that records intent and access.
- **server/wiring-invariants.ts:293** — checkDormantTools runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant scoping, returning tool usage across all tenants.
  - _Fix:_ Add WHERE tenant_id = :tenantId or execute the query per-tenant. If cross-tenant aggregation is needed for ops, make the cross-tenant scope explicit and only run it under an admin-authorized path.
- **server/wiring-invariants.ts:417** — checkToolDrift runs an aggregate SELECT FROM tool_performance (SUM/MAX/GROUP BY) filtering only by last_failure_at > ... but with no tenant_id constraint — it aggregates tenant-scoped telemetry across all tenants.
  - _Fix:_ Scope the aggregation to a tenant (WHERE tenant_id = :tenantId) or run the aggregation per tenant. For legitimate global monitoring, require an explicit admin-scoped pathway that documents cross-tenant access and uses parameterized queries.

### MEDIUM (45)

- **server/agentic-features.ts:599** — enrollInSequence checks for an existing enrollment with SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but does not include tenant_id. Although the sequence was validated earlier, the EXIST check itself is unscoped.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the SELECT for existing enrollments to avoid any risk from ID reuse or logic changes; prefer always scoping direct table reads/updates with tenant_id.
- **server/agentic-features.ts:672** — advanceSequence updates outreach_enrollments with UPDATE outreach_enrollments SET ... WHERE id = ${enrollment.id} but the UPDATE statement does not include tenant_id. The enrollment row was selected earlier under a tenant filter, but the write itself is not explicitly tenant-scoped.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} (or use the tenant id from the selected enrollment row) to the UPDATE WHERE clause to make the write explicitly tenant-scoped and robust to future changes.
- **server/agentic/task-forces.ts:40** — Accepting and persisting a foreign id (projectId) from caller input without validating ownership: createTaskForce() inserts project_id submitted by the caller directly into task_forces without verifying the referenced project belongs to the caller's tenant.
  - _Fix:_ Validate any foreign identifiers from caller input before persisting — e.g. SELECT the project row and ensure project.tenant_id === p.tenantId (or otherwise enforce ownership). Alternatively enforce cross-tenant-safe foreign keys or reject external ids not proven to belong to the tenant.
- **server/auto-consolidation.ts:335** — checkAllTenants issues a SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL — reading the tenant-scoped memory_entries table without scoping to the caller's tenant.
  - _Fix:_ If the goal is to enumerate tenants for scheduled consolidation, derive tenant ids from a dedicated global tenants table (trusted list) or explicitly run this code in a controlled maintenance context. If this code is running in a tenant-scoped context it must filter by tenant_id. Document and gate any cross-tenant scans and ensure they run only with explicit platform-admin privileges.
- **server/chat-engine.ts:4016** — recordToolCallForStuckDetection(conversationId, ...) is invoked without tenantId. If the stuck-diagnostics functions read or write tenant-scoped tables based only on conversationId they may perform operations without tenant scoping/ownership validation.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection (and any other stuck-diagnostics APIs) or ensure those functions validate the conversation belongs to the caller's tenant (SELECT ... WHERE id = conversationId AND tenant_id = tenantId) before reading/writing tenant-scoped tables.
- **server/chat-engine.ts:4022** — postDiagnosticReport([stuckPattern]) is called without tenantId. If postDiagnosticReport persists diagnostics/events to tenant-scoped storage, it may do so without an explicit tenant filter.
  - _Fix:_ Update postDiagnosticReport API to accept tenantId (or ensure stuckPattern includes tenant context) and enforce tenant_id in all DB operations. If posting diagnostics for a conversation, validate the conversation's tenant ownership prior to persisting diagnostics.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} without constraining by tenant_id. Although the rows were selected via a tenant-scoped query earlier, the UPDATE uses only id and could update an id that doesn't belong to the tenant in some DB setups or if id collisions/assumptions change.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or update via collection_id + id to guarantee the change is limited to the intended tenant's rows.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot runs SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' with NO tenant_id filter. This reads aggregated tenant-scoped message data across all tenants, violating per-tenant isolation assumptions.
  - _Fix:_ If this is intended to be per-tenant, add a tenant_id filter parameter (e.g. AND tenant_id = ${tenantId}). If it's a global/admin metric, make the admin-only intent explicit and ensure only privileged code paths call it.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot runs SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... with NO tenant_id filter. This reads tenant-scoped tool performance data across tenants.
  - _Fix:_ Scope the query to a tenant via AND tenant_id = ${tenantId} when used in tenant-scoped contexts, or clearly separate/mark global admin metrics and restrict their callers to admin contexts.
- **server/dormant-deprecation.ts:202** — getEverInvokedTools executes SELECT DISTINCT tool_name FROM tool_performance with NO tenant_id filter. This reads the tool_performance table across all tenants rather than per-tenant.
  - _Fix:_ If the intention is per-tenant, add WHERE tenant_id = ${tenantId} (and accept tenantId as a parameter). Otherwise, restrict this function to admin-only code paths and document that it intentionally aggregates global data.
- **server/dormant-deprecation.ts:214** — getActiveTools14d executes SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... with NO tenant_id filter — returning tools active across all tenants rather than scoping to a tenant.
  - _Fix:_ Add tenant scoping (tenant_id = ${tenantId}) for tenant-aware behavior, or ensure this is only invoked in global/admin contexts and is not used to make tenant-local decisions.
- **server/health-audit.ts:314** — SELECT from code_proposals without any tenant_id filter — reading tenant-scoped proposals across tenants.
  - _Fix:_ If this query is intended to operate per-tenant, add WHERE tenant_id = ${tenantId}. If it is an explicit global/admin operation, make the admin intent explicit (guard it) and document/limit the scope, or iterate per-tenant rather than doing an unscoped SELECT so tenant isolation is explicit in the code.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks without tenant_id filtering — reading potentially tenant-scoped tasks across tenants.
  - _Fix:_ Scope this scanner to a specific tenant by adding AND tenant_id = ${tenantId} to the WHERE clause or perform per-tenant scans. If heartbeat_tasks are intentionally global, document that and ensure calling code is authorized for global operations.
- **server/job-queue.ts:385** — getJobStats executes SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > NOW() - INTERVAL '7 days' GROUP BY status without tenant_id filtering, returning aggregated counts across all tenants.
  - _Fix:_ If these stats are intended per-tenant, add WHERE tenant_id = ${tenantId} (or accept a tenantId parameter). If global counts are intentional, make the global nature explicit and restrict this to admin-only callers or expose a separate admin API; avoid using tenant-scoped tables without explicit tenant scoping in code paths that could be called in a tenant context.
- **server/lib/stuck-task-sweep.ts:56** — SELECT against tenant-scoped table `agent_runs` (scanning for stuck runs) omits any tenant_id filter — it returns rows across all tenants. The invariant expects tenant-scoped queries to be constrained to a tenant unless explicitly and safely intended.
  - _Fix:_ If this is intended to be a global admin audit, ensure the function runs only in an authorized admin/system context and explicitly document that exception. Otherwise, restrict the query by tenant_id (accept tenantId as a parameter) or iterate tenants and run per-tenant queries. In any global-audit case, minimize returned tenant data and make permission checks explicit.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against tenant-scoped table `mind_tickets` omits a tenant_id constraint, returning rows across tenants (the table may be absent in some environments but when present the query is cross-tenant).
  - _Fix:_ Same mitigation as for agent_runs: either add tenant_id filtering (accept tenantId param) or document/admin-only global-scan expectation and gate the function so it only runs in a privileged/system context. Ensure returned results preserve tenant_id and that any downstream writes would include tenant constraints.
- **server/lib/system-probes.ts:163** — Unfiltered read from tenant-scoped table `messages` (db.select(...).from(messagesTable)) — no tenant_id WHERE constraint.
  - _Fix:_ If this probe must be tenant-scoped, add an explicit tenant filter (e.g. .where(eq(messagesTable.tenantId, tenantId)) or include a tenantId parameter and use it). If the intent is an admin/global metric, make that explicit and restrict access to an admin-only path or aggregate counts per-tenant rather than returning unscoped tenant rows.
- **server/minds-engine.ts:373** — Probe SELECT (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}) lacks tenant_id scoping. Reading a tenant-scoped plans row by id only can leak cross-tenant state.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT or otherwise require/derive tenantId before querying plans. Make probe queries tenant-aware to avoid IDORs.
- **server/minds-engine.ts:326** — markEventsHandled builds an array literal via string interpolation: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}. eventIds is request-derived and is interpolated into the SQL string before binding, risking injection/isolation issues.
  - _Fix:_ Avoid constructing SQL array literals via string concatenation. Use proper parameterized array binding (e.g. pass eventIds as a parameterized int[] or use a driver/ORM helper such as sql.array(eventIds, 'int')/ANY($1::int[])). Validate/normalize eventIds to integers before use and include tenant_id in the WHERE clause as already present.
- **server/process-governor.ts:1184** — LEFT JOIN to governance_rules in getActionHistory lacks tenant scoping on the joined table: LEFT JOIN governance_rules gr ON gr.id = ga.rule_id — gr.tenant_id is not constrained, which can surface another tenant's rule.description.
  - _Fix:_ Constrain the join to the same tenant, e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ${tenantId}, or select the description via a tenant-scoped lookup. Ensure every read of governance_rules includes tenant_id = ${tenantId}.
- **server/routes.ts:648** — Cleanup SQL (SELECT id FROM tenants … AND id NOT IN (SELECT DISTINCT tenant_id FROM conversations ...) AND id NOT IN (SELECT DISTINCT tenant_id FROM projects ...)) contains subqueries selecting tenant_id from tenant-scoped tables (conversations, projects) with no tenant filtering. These unscoped reads access tenant-scoped data without an application-level tenant constraint.
  - _Fix:_ Run cleanup under an explicit administrative context (documented) or add explicit tenant scoping in subqueries if applicable. If the intent is a platform-wide maintenance job, ensure it executes only on a maintenance/admin node and cannot be triggered by unprivileged requests; otherwise add explicit filters or require ADMIN_TENANT_ID and admin-only invocation.
- **server/routes.ts:765** — Drive backfill SELECT queries (e.g. SELECT p.id, p.name, p.tenant_id, t.name as tenant_name FROM projects p JOIN tenants t ON t.id = p.tenant_id WHERE p.drive_folder_id IS NULL ...) read tenant-scoped tables without scoping to a caller tenant. This exposes project and tenant names across tenants when run during startup/backfill.
  - _Fix:_ Run backfill tasks explicitly under an administrative/maintenance context and ensure they are not accessible or triggerable from normal request paths. Alternatively, add explicit tenant scoping or redact tenant identifiers/names unless the operation is intended to run as a platform admin. Consider writing results to admin-only logs rather than public filesystem areas.
- **server/routes.ts:2142** — POST /api/presenter inserts into tenant-scoped table `presenter_sessions` using tenantId taken directly from req.body (INSERT ... VALUES (${tenantId}, ...)). The code trusts the provided tenantId without validating the caller is authorized to create sessions for that tenant.
  - _Fix:_ Validate the tenantId before inserting: ensure the caller is an authorized internal caller (you already require SESSION_SECRET) and explicitly verify the tenant exists and that the internal caller is permitted to create sessions for the given tenant. Prefer deriving tenantId from a trusted server-side mapping or require an admin-level credential rather than accepting arbitrary tenantId values from the request body.
- **server/routes/admin.ts:569** — SELECT DISTINCT tool_name FROM tool_performance executes without a tenant_id filter — may return cross-tenant tool usage rows.
  - _Fix:_ If tool_performance is tenant-scoped, include a tenant filter (WHERE tenant_id = ${tenantId}) to limit results to the caller's tenant. If this is intentionally a platform-global report, require and assert platform-admin privileges explicitly and document the cross-tenant nature.
- **server/routes/admin.ts:574** — Query selecting from tool_performance (failingRows) has no tenant_id constraint and can surface tenant data across all tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) to these queries. If the endpoint should aggregate platform-wide data, ensure it's protected by the platform-admin gate and make that explicit in code/comments.
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` without constraining to the caller's tenant (aggregates per-tenant over all tenants). This is a read against tenant-scoped data with no tenant_id WHERE clause.
  - _Fix:_ If the multi-tenant aggregation is intentional (operator/metrics use), ensure strict operator-only gating (already uses METRICS_TOKEN) and document the intentional global access. If not intentional, add a WHERE tenant_id = ${tenantId} predicate or otherwise scope the query to the requester's tenant.
- **server/routes/metrics.ts:79** — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) without a tenant_id predicate — reads across tenants.
  - _Fix:_ If this cross-tenant aggregation is intentional for operator metrics, enforce and log operator-only access (METRICS_TOKEN). Otherwise, scope to the caller's tenant by adding WHERE tenant_id = ${tenantId}.
- **server/routes/metrics.ts:95** — SELECT SUM(tokens_in/tokens_out) FROM tenant-scoped `agent_cost_ledger` performed without tenant_id constraint (reads across tenants).
  - _Fix:_ Restrict to the requester's tenant with WHERE tenant_id = ${tenantId} if the data should be tenant-scoped, or ensure the endpoint is strictly operator-only and documented as a deliberate cross-tenant aggregation.
- **server/routes/metrics.ts:113** — SELECT from `repair_incidents` (tenant-scoped table) without a tenant_id WHERE clause — returns incidents across all tenants.
  - _Fix:_ Either add a tenant_id predicate to scope results to the caller's tenant, or ensure and document that this endpoint is operator-only (and additionally enforce platform-admin gating) if global aggregation is required.
- **server/routes/oauth-subscriptions.ts:325** — SELECT DISTINCT tenant_id FROM `oauth_subscriptions` executed without scoping to a tenant. This reads tenant-scoped rows across all tenants during the cron heal step.
  - _Fix:_ If the cron heal step legitimately needs to iterate tenants, keep it but ensure the handler is protected (it uses CRON_SECRET — keep and validate that). Otherwise, restrict the query by tenant_id or rework to enumerate tenants via a platform-admin-only path so ordinary tenants cannot trigger or observe cross-tenant data.
- **server/seed.ts:35** — Unscoped SELECT on tenant-scoped table: SELECT COUNT(*) FROM research_sessions without a tenant_id WHERE clause. This reads global/session counts across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId} or eq(research_sessions.tenant_id, tenantId)). If this check must be global, ensure it runs only in a trusted/admin context and cannot be triggered by tenant-scoped code paths.
- **server/seed.ts:2855** — Unscoped SELECT on tenant-scoped table: SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id constraint and will return rows for all tenants.
  - _Fix:_ Filter by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise restrict this operation to an explicitly authorized admin/human-only code path. Avoid scanning all tenants in routine code.
- **server/seed.ts:3205** — SELECT on tenant-scoped table 'conversations' without tenant constraint: SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no WHERE tenant_id filter and will enumerate conversations across tenants.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...). If this is an admin-only fix path, ensure it cannot be invoked by tenant-level actors.
- **server/seed.ts:4591** — INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note}, 'system:import') does not include tenant_id. If project_notes is tenant-scoped, the INSERT will not explicitly set tenant ownership and may rely on a default tenant_id or leave rows unscoped.
  - _Fix:_ Explicitly include tenant_id in the INSERT (tenant_id: <proj tenant>) or derive/verify the project tenant and set tenant_id accordingly. If project_notes is intended to be per-tenant, modify the table/seed to require and set tenant_id for every INSERT.
- **server/storage.ts:971** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant filter in fixStaleBackupSchedules (global maintenance update WHERE type IN (...)). This mutates tasks across tenants without tenant scoping.
  - _Fix:_ If intended to run globally by a scheduler, mark as admin-only and audit; otherwise include tenant scoping or iterate tenants and apply per-tenant updates.
- **server/storage.ts:991** — SELECT from tenant-scoped table `heartbeat_tasks` without tenant filter in getDueHeartbeatTasks (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ...). This returns due tasks across tenants.
  - _Fix:_ If this is a queue worker intentionally processing tasks for all tenants, ensure the caller identity and execution context are privileged and hardened. Otherwise, add tenant scoping or explicitly document and secure the global behavior.
- **server/storage.ts:1019** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant filter in claimHeartbeatTasks (UPDATE heartbeat_tasks SET last_run_at = ..., WHERE id = ${id} ...). This can claim tasks across tenants.
  - _Fix:_ Include tenantId in the claim condition (e.g. AND tenant_id = ${tenantId}) or ensure the worker runs with explicit admin privilege and that claims are audited and tenant-aware.
- **server/storage.ts:1034** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant filter in markHeartbeatTaskRun (.where(eq(heartbeat_tasks.id, id))). This updates tasks across tenants by id alone.
  - _Fix:_ Include tenant scoping (e.g. AND tenant_id = ${tenantId}) or validate task ownership prior to updating. Prefer explicit tenant parameter for admin operations.
- **server/video-job-runner.ts:204** — SELECT from tenant-scoped table `video_jobs` reads cancelRequested WHERE jobId = ${jobId} without scoping by tenant_id. A request or background actor that knows a jobId could probe or infer state for another tenant's job.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE job_id = ${jobId} AND tenant_id = ${expectedTenantId}) or otherwise verify job ownership before reading cancel flags. If the background runner legitimately cannot easily pass tenant id, derive tenant id from a trusted source (e.g. the job-state mirror) and use it in the WHERE clause to avoid cross-tenant reads.
- **server/video-job-runner.ts:228** — UPDATE on tenant-scoped table `video_jobs` (marking stale jobs failed) performs a blanket update across all tenants: UPDATE video_jobs SET ... WHERE status IN (...) AND updatedAt < staleCutoff — no tenant_id constraint. This operation will affect jobs across tenants and may unintentionally change other tenants' job states.
  - _Fix:_ If the operation is intended to be global/administrative, ensure it runs only as a privileged admin/maintenance task and is auditable. Otherwise, restrict updates to a specific tenant by adding tenant_id = ${tenantId} to the WHERE clause. Alternatively, run the recovery sweep in a controlled maintenance context that explicitly documents and authorizes cross-tenant state changes.
- **server/webhook-triggers.ts:60** — listTriggers() selects webhook_triggers and LEFT JOINs personas (p) but does not restrict personas by tenant_id. Personas is a tenant-scoped table; the query reads that tenant-scoped table without a tenant filter.
  - _Fix:_ Constrain the personas join to the appropriate tenant (or to ADMIN_TENANT_ID where appropriate). For example: LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = ${desiredTenantId} (or explicitly filter p.tenant_id = ${ADMIN_TENANT_ID} if triggers are admin-scoped). Ensure the chosen tenantId is the correct, trusted tenant context.
- **server/webhook-triggers.ts:122** — processTriggerEvent() SELECTs webhook_triggers and LEFT JOINs personas (p) to fetch persona name but does not include a tenant_id constraint on personas. This reads a tenant-scoped table (personas) without tenant scoping, potentially exposing persona data from other tenants.
  - _Fix:_ Add a tenant constraint for the joined personas row (e.g. LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = ${expectedTenantId}) or otherwise ensure persona lookup is performed in the correct tenant context (such as ADMIN_TENANT_ID for admin-scoped triggers).
- **server/wiring-invariants.ts:361** — checkSubsystemFreshness uses sql.raw() with string-interpolated table names and issues SELECT COUNT/MAX FROM ${spec.table} with no tenant filter for subsystem tables (e.g. tool_performance, knowledge_nudges). Even if spec.table is from code constants, the query reads tenant-scoped tables without tenant constraints.
  - _Fix:_ Avoid raw string interpolation for table access where possible. Use Drizzle helpers or parameterized queries and add tenant scoping (WHERE tenant_id = :tenantId). If these probes must run across tenants, explicitly enumerate tenants and run per-tenant queries; otherwise document and restrict/authorize the cross-tenant read.
- **server/wiring-invariants.ts:535** — checkMemoryGrowth uses sql.raw() with string-interpolated table names (pg_total_relation_size('${tbl.name}') FROM ${tbl.name}) and issues an unconstrained COUNT(*) against memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) without tenant scoping.
  - _Fix:_ Avoid unconstrained COUNTs on tenant-scoped memory tables. Add WHERE tenant_id = :tenantId (use Drizzle .where(eq(...))) or run the check per-tenant. When using sql.raw for table-level stats, validate table names against a safe whitelist and make tenant intent explicit.
- **server/wiring-invariants.ts:561** — checkMemoryGrowth runs SELECT COUNT(*) FROM graph_memory (and treats it as 'empty across all tenants') without tenant_id filtering — reading tenant-scoped graph memory across the entire DB.
  - _Fix:_ Scope the query to a tenant via WHERE tenant_id = :tenantId, or explicitly iterate tenants to detect per-tenant emptiness. If the goal is a global seed check, perform an admin-authorized, audited action instead of unconstrained reads of tenant data.

### Suppressed — confirmed false positives (206)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — autoApplyEligibleInsights performs a bulk UPDATE on ai_insights (UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...)) without any tenant_id constraint — this writes across all tenants.
  - _Why false positive:_ Global maintenance backfill intentionally sweeps all tenants; each returned row carries tenant_id and is handled per-row, not exposed to a tenant-facing mixed response.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Cross-tenant SELECT: the candidate query reads agent_runs and agent_approvals without restricting to a single tenant (no WHERE tenant_id = <caller tenant>). This allows the background sweep to enumerate/observe runs and approvals across all tenants.
  - _Why false positive:_ Background sweep intentionally scans pending approvals across tenants for housekeeping/resume logic; joined rows retain row tenant_id and are processed individually.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — runDueWakes performs a cross-tenant claim: the inner SELECT used by the UPDATE that marks wakes 'firing' does not restrict by tenant_id. This can claim and update pending wakes from any tenant in one transaction (mass cross-tenant write).
  - _Why false positive:_ Heartbeat scheduler globally claims due wake rows across tenants for maintenance/event dispatch; each claimed row carries its own tenant_id and subsequent handling is per-row.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id = ... WHERE id = ... (no tenant constraint). This writes a tenant-scoped table without verifying tenant ownership in the WHERE clause, allowing potential cross-tenant modification if ids are misused.
  - _Why false positive:_ The source SELECT already joins conversations and projects on matching tenant_id and returns conversation_id/project_id from that verified pair; UPDATE only applies that derived link when project_id is NULL.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} — DELETE against tenant-scoped table `messages` with NO tenant_id predicate. This can delete another tenant's rows if IDs collide or if conversation_id is ambiguous.
  - _Why false positive:_ Platform-wide purge loop selects each expired conversation with its tenant_id first, then performs housekeeping deletion of that conversation's related rows; no tenant-facing mixed output.
- **server/data-protection.ts:132** _(system-sweep)_ — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} — destructive DELETE on tenant-scoped table without tenant_id predicate.
  - _Why false positive:_ Platform-wide purge loop processes expired conversations row-by-row using the selected conversation context; this is housekeeping deletion, not tenant-facing data mixing.
- **server/data-protection.ts:133** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} — deleting tenant-scoped `conversations` row without tenant_id predicate.
  - _Why false positive:_ Platform-wide purge of expired soft-deleted conversations is a housekeeping sweep over rows selected earlier; deletes are per conversation, with no cross-tenant response surface.
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT from agent_knowledge (backfillMissingKnowledgeEmbeddings) with no tenant_id constraint — reads rows across all tenants.
  - _Why false positive:_ Embedding backfill is a maintenance sweep over all tenants; rows are processed individually for housekeeping updates and not returned or mixed into tenant-facing output.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT from memory_entries (backfillEmbeddingVecs) has no tenant_id constraint — reads embeddings across all tenants.
  - _Why false positive:_ Vector backfill is a platform-wide maintenance job reading rows across tenants only to repair embedding_vec fields; no tenant-facing mixed output.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries setting embedding_vec WHERE id = ${row.id} without tenant_id constraint — write may affect other tenants' rows.
  - _Why false positive:_ Update occurs inside a maintenance backfill loop over rows selected for housekeeping; it only stamps derived embedding_vec on each row and does not expose mixed tenant data.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT from agent_knowledge (backfillEmbeddingVecs) without tenant_id constraint — reads rows across tenants.
  - _Why false positive:_ Boot/maintenance backfill scans across tenants only to repair embedding_vec housekeeping; no tenant-facing return or mixed output.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge setting embedding_vec WHERE id = ${row.id} without tenant_id constraint — write may overwrite another tenant's row.
  - _Why false positive:_ Per-row housekeeping update during global embedding backfill keyed by selected row id; mutates only embedding_vec, not cross-tenant exposure.
- **server/internal-resolver.ts:91** _(internal-worker-pk)_ — UPDATE event_log finalization (finalizeEvent) mutates event_log rows by id with no tenant_id constraint. This allows the resolver to finalize (update) event_log rows belonging to any tenant.
  - _Why false positive:_ Internal resolver finalizes event_log by server-generated event id in async worker flow, not from an untrusted tenant request boundary.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs to status='succeeded' WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but does not restrict by tenant_id. This allows marking another tenant's job succeeded by id.
  - _Why false positive:_ Worker state transition by job primary key plus attempts fence is internal queue machinery, not caller-controlled cross-tenant input.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob (terminal branch) updates agent_jobs to 'failed_terminal' WHERE id = ${id} ... without scoping by tenant_id, enabling cross-tenant mutation of job status/error.
  - _Why false positive:_ Internal worker fail path updates agent_jobs by server-held id and attempts; primary key is not taken from tenant request input.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob (retry branch) updates agent_jobs (status='pending', next_run_at, error, failure_class) WHERE id = ${id} ... with no tenant_id constraint. This can re-queue or change state of jobs from other tenants.
  - _Why false positive:_ Retry-state update is internal queue processing by server-tracked job id with attempt fencing, not an untrusted boundary.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT INTO tenant-scoped table `project_files` does not set tenant_id. Per platform invariant tenant_id columns are NOT NULL with no default; inserting without tenant_id will misattribute rows or fail and breaks tenant isolation.
  - _Why false positive:_ project_files has no tenant_id column and is transitively tenant-scoped through project_id -> projects.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT INTO tenant-scoped table `project_notes` does not set tenant_id. Writing tenant-scoped rows without tenant_id violates the invariant and can create cross-tenant contamination or insert failures.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively tenant-scoped through project_id -> projects.
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not include tenant_id. The code calls assertProjectInTenant(...) but the VALUES list for INSERT omits tenant_id (columns: project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by). Per invariant, every INSERT into a tenant-scoped table must explicitly set tenant_id (NOT NULL, no default).
  - _Why false positive:_ project_files has no tenant_id column and insert is correctly isolated via verified parent project ownership.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table project_files (in parallel path) does not include tenant_id. Even though assertProjectInTenant(...) is called, the INSERT's VALUES omit tenant_id, violating the requirement that tenant_id be passed explicitly on every insert to tenant-scoped tables.
  - _Why false positive:_ project_files has no tenant_id column; insert is correctly isolated via tenant-verified project_id after assertProjectInTenant(...)
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions without any tenant_id filter (reads all tenants' active subscriptions).
  - _Why false positive:_ startup validation is a platform-wide maintenance sweep over active subscriptions using each row's own tenant_id; no tenant-facing mixed output
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh: SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE reads all active subscriptions across tenants (no tenant_id filter).
  - _Why false positive:_ proactive token refresh is a background maintenance loop processing each subscription with its own tenant_id and only mutating housekeeping/token state
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE has no tenant_id constraint and iterates/allows cross-tenant reads.
  - _Why false positive:_ force-refresh is an explicit cron-style maintenance sweep across active subscriptions using per-row tenant_id and not returning mixed tenant data
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'plans' does not include tenant_id in WHERE (appendExecutionLog uses WHERE id = ${planId} only). This allows cross-tenant modification by planId.
  - _Why false positive:_ appendExecutionLog is an internal worker helper updating by server-side planId, not directly from an untrusted request boundary
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE to claim a plan (UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status='approved') has no tenant_id constraint — it can claim plans across tenants.
  - _Why false positive:_ executePlan is an internal executor claiming a plan by server-side primary key and immediately returns the row's tenant_id for subsequent scoped behavior
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} — missing tenant_id filter when marking a plan failed (no tenant scoping).
  - _Why false positive:_ the same call path just claimed and loaded this exact plan row via the CAS update/RETURNING, so this failure update targets the already-verified row id
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final status UPDATE on plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks tenant_id constraint — writes to a tenant-scoped table without scoping.
  - _Why false positive:_ final status write occurs after the same function successfully claimed and held the specific plan row, so tenant ownership for this row was established earlier in-path
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id — allows cross-tenant state changes in plans.
  - _Why false positive:_ fatal-error cleanup is an internal worker state transition by server-side planId/status, not a direct cross-tenant request operation
- **server/plan-executor.ts:775** _(system-sweep)_ — Boot-time recovery UPDATE resets any 'executing' plans to 'approved' globally (UPDATE plans SET status='approved' WHERE status='executing' AND updated_at < ...). No tenant filter is applied — this writes across all tenants.
  - _Why false positive:_ boot-time recovery is a platform-wide housekeeping sweep that only resets stale status fields and does not expose mixed tenant data
- **server/plan-executor.ts:784** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ... LIMIT 50 is read against tenant-scoped 'plans' table with no tenant_id filter — returns plans across tenants.
  - _Why false positive:_ boot-time pending-plan scan is part of a maintenance dispatcher selecting ids to process individually, with no tenant-facing mixed output
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks issued without tenant constraint: UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — this mutates a tenant-scoped table without verifying tenant ownership.
  - _Why false positive:_ t.id comes from prior SELECT on heartbeat_tasks scoped by ht.tenant_id = tenantId in same case block.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks performed without tenant constraint: UPDATE agent_desks SET ... WHERE id = ${desk.id} — writing to a tenant-scoped table without tenant_id filter.
  - _Why false positive:_ desk.id is sourced from immediately preceding SELECT on agent_desks with ad.tenant_id = tenantId.
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query scans projects and conversations without tenant scoping: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... — no tenant_id filter on projects/conversations, so backfill iterates across all tenants' data.
  - _Why false positive:_ Boot/backfill maintenance loop intentionally scans all tenants to create per-project files; not tenant-facing query and no mixed response/prompt output at this line.
- **server/project-brain.ts:349** _(system-sweep)_ — Selecting conversations by project_id with no tenant constraint: SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — conversation rows are not scoped to a tenant in this query.
  - _Why false positive:_ Conversation scan is part of global backfill maintenance over each project row; processing stays per project rather than request-scoped tenant access.
- **server/proposal-verifier.ts:106** _(internal-worker-pk)_ — UPDATE code_proposals is executed without tenant constraint when clearing rebased_at/race info: UPDATE code_proposals ... WHERE id = ${proposalId} — this mutates a tenant-scoped row without verifying tenant ownership.
  - _Why false positive:_ Verifier is an internal worker flow operating on server-supplied proposalId primary key, not directly from a tenant request boundary here.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist() updates code_proposals without tenant constraint: UPDATE code_proposals SET verification_status = ${status} ... WHERE id = ${proposalId} — this allows cross-tenant mutation of proposals' verification state.
  - _Why false positive:_ persist() is internal verifier bookkeeping on code_proposals by server-invoked proposalId primary key.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — attemptRebaseOnDrift updates code_proposals to mark rejected without tenant constraint: UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} — mutates tenant-scoped rows without tenant verification.
  - _Why false positive:_ attemptRebaseOnDrift is internal worker logic mutating a proposal by server-controlled proposalId PK.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — attemptRebaseOnDrift persists rebased code_diff back to code_proposals via UPDATE ... WHERE id = ${proposalId} without tenant scoping — this writes to a tenant-scoped table without validating ownership.
  - _Why false positive:_ Rebase persistence updates code_proposals in internal verifier pipeline using server-side proposalId PK.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_knowledge` uses `WHERE id = ${r.id}` without constraining `tenant_id`. This can modify rows belonging to other tenants.
  - _Why false positive:_ Scheduler loop is platform maintenance processing rows across tenants with each row carrying r.tenant_id; update only advances job housekeeping content for that row.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses `WHERE id = ${storedId}` with no `tenant_id` constraint after a tenant-scoped SELECT. This allows updating another tenant's row if `id` collides.
  - _Why false positive:_ storedId comes from immediately preceding SELECT on memory_entries scoped by input.tenantId and persona/category/fact prefix.
- **server/research-engine.ts:163** _(system-sweep)_ — UPDATE on tenant-scoped table `research_sessions` (cleanupZombieSessions) is executed without any `tenant_id` constraint — the statement can change sessions across all tenants.
  - _Why false positive:_ Global stale-session cleanup updates housekeeping/status fields across tenants and only returns aggregate ids for logs, not tenant-facing mixed data.
- **server/research-engine.ts:398** _(system-sweep)_ — DELETE FROM tenant-scoped table `agent_knowledge` (`DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()`) is executed without constraining `tenant_id`, which will remove rows for all tenants.
  - _Why false positive:_ Global expiry purge of expired autoresearch knowledge is maintenance cleanup across tenants; it deletes expired rows only and does not expose mixed tenant data.
- **server/routes.ts:4331** _(fk-transitive)_ — INSERT into tenant-scoped table (project_notes) does not set tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, so the new row will not be associated with the caller's tenant (or may be inserted with NULL), breaking isolation and enabling cross-tenant corruption/leakage.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated transitively through tenant-scoped parent project_id.
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes statement only supplies project_id, note, author and omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and insert follows verified ownership of tenant-scoped parent project via project_id.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id (base64 upload path). The INSERT INTO project_files statement omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert uses project_id after verifying the parent project belongs to the request tenant.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id (multipart upload path). The INSERT INTO project_files statement omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert uses project_id after verifying the parent project belongs to the request tenant.
- **server/sculptor.ts:94** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets status/result but the WHERE clause filters only by id (WHERE id = ${session.id}) and does NOT constrain by tenant_id. This allows cross-tenant modification of sessions by id.
  - _Why false positive:_ Update is in server-internal flow immediately after inserting the session row; id is server-generated and not taken from an untrusted request boundary.
- **server/sculptor.ts:101** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id but the WHERE clause filters only by id (WHERE id = ${session.id}) and does NOT constrain by tenant_id. Tenant isolation can be bypassed if an attacker knows another session id.
  - _Why false positive:_ Update by freshly created server-generated session primary key in internal launch flow, not attacker-controlled id input.
- **server/sculptor.ts:156** _(internal-worker-pk)_ — monitorSession performs UPDATE sculptor_sessions SET ... WHERE id = ${sessionId} without constraining tenant_id. monitorSession receives tenantId but the UPDATE omits it, enabling cross-tenant writes to session rows by id.
  - _Why false positive:_ Background monitor updates a session by server-managed sessionId/runId established internally, not from untrusted cross-tenant request input.
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} but does NOT limit the UPDATE to the reservation's tenant (no tenant_id filter). A reservation id alone is being used to update ledger rows across tenants.
  - _Why false positive:_ Internal helper updates by server-generated reservationId only; no untrusted request boundary shown.
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without constraining tenant_id. This can release or alter ledger rows belonging to other tenants if reservation ids are known or guessable.
  - _Why false positive:_ Internal best-effort release helper uses server-generated reservationId, not caller-supplied tenant-crossing input.
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations without tenant_id: INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) — tenant_id is not provided. Per invariant tenant_id is required on INSERTs and must be validated/explicit.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table scoped transitively via tenant-scoped parent rows.
- **server/seed.ts:3205** _(system-sweep)_ — SELECT reads from tenant-scoped table `conversations` (and checks `project_conversations`) without any tenant_id filter: SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id). This can enumerate and operate on rows for other tenants and enables cross-tenant writes later in the script.
  - _Why false positive:_ Boot-time backfill intentionally scans all tenants and only repairs per-row conversation→project links without returning mixed tenant data.
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` performed without scoping by tenant_id: the code updates WHERE id = <existing id> only. This can modify another tenant's memory entry by id.
  - _Why false positive:_ The row id being updated was selected immediately above with WHERE tenant_id = tenantId, so ownership is established in the same call path.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM `heartbeat_tasks` performed without any tenant constraint — this can delete rows across all tenants (cross-tenant destructive write).
  - _Why false positive:_ Global housekeeping purge of stale disabled tasks; only deletes old rows and returns aggregate count, no tenant data mixing.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM `event_log` executed with no tenant filter — deletes may affect all tenants' event logs.
  - _Why false positive:_ Platform-wide cleanup of old processed/failed event logs is housekeeping only and exposes only aggregate counts.
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM `heartbeat_logs` uses an INTERVAL but no tenant constraint — this will prune logs for every tenant rather than being scoped to a tenant.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id, so pruning is on a non-tenant-scoped global table.
- **server/storage.ts:838** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint in archiveExpiredMemories (UPDATE memory_entries SET status = 'archived' WHERE ...). This will archive memories across all tenants indiscriminately.
  - _Why false positive:_ Global expiry housekeeping updates status only and returns counts/ids internally; no tenant data is returned or mixed.
- **server/storage.ts:855** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint in archiveStaleMemories (UPDATE memory_entries SET status = 'archived' WHERE ...). This will affect all tenants.
  - _Why false positive:_ Global stale-memory archival is a maintenance sweep mutating only status fields across rows, not exposing tenant content.
- **server/tools.ts:2876** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations does not include tenant_id. The code inserts (project_id, conversation_id) without explicitly setting tenant scope, relying on implicit project/conversation ownership checks elsewhere.
  - _Why false positive:_ project_conversations has no tenant_id by schema and is transitively isolated through tenant-scoped project/conversation parents, with prior tenant-scoped conversation update guard.
- **server/tools.ts:2980** _(fk-transitive)_ — INSERT into tenant-scoped table project_files omits tenant_id. The INSERT populates project_id and file metadata but does not set tenant_id on the new row.
  - _Why false positive:_ project_files has no tenant_id by schema and is a child of tenant-scoped projects; ownership is checked on project before insert.
- **server/tools.ts:2994** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes omits tenant_id. The code inserts (project_id, note, author) without explicit tenant scoping on the new row.
  - _Why false positive:_ project_notes has no tenant_id by schema and is a child of tenant-scoped projects; project ownership is verified before insert.
- **server/tools.ts:3018** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations (link_conversation flow) omits tenant_id. After asserting the conversation belongs to the tenant, the code inserts project_conversations without recording tenant_id on the new row.
  - _Why false positive:_ project_conversations has no tenant_id by schema and link is between tenant-scoped parents after explicit conversation-in-tenant and project ownership checks.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments reads the experiments table without scoping by tenant (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'), exposing/processing other tenants' experiments.
  - _Why false positive:_ Heartbeat maintenance loop intentionally scans across tenants but processes each row with its own tenant_id and updates only that row's status/result.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_evals uses WHERE id = ${evalId} with NO tenant_id constraint. This can allow cross-tenant UPDATE if evalId is misused.
  - _Why false positive:_ Update targets eval row by server-generated id returned from immediately preceding insert in same worker flow, not from untrusted request input.
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting selects ai_insights rows WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER with NO tenant_id constraint, reading tenant-scoped rows across all tenants.
  - _Why false positive:_ Durability sweep intentionally scans all tenants, carries row tenant_id forward, validates it, and processes each insight separately without mixing tenant data into one output.
- **server/agentic-engines.ts:344** _(global-table)_ — gatherOperationalData queries heartbeat_logs without a tenant filter (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'), reading logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and appears to be platform-global telemetry
- **server/agentic-engines.ts:519** _(global-table)_ — runOptimizationEngine selects heartbeat_logs performance aggregated across all rows (SELECT ... FROM heartbeat_logs GROUP BY ...) with NO tenant_id filter — reading/aggregating cross-tenant data.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and query is aggregate operational telemetry only
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agent_approvals to set status='expired' for pending approvals whose expiresAt has passed, but the UPDATE lacks a tenant_id filter and will affect approvals across tenants.
  - _Why false positive:_ platform-wide expiry sweep updates housekeeping status across tenants and carries tenantId per returned row for follow-up
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' — tenant-scoped table queried with no tenant_id constraint.
  - _Why false positive:_ trusted sweep intentionally scans all tenants, processes each row with its own tenant_id, and emits per-tenant events only
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — Missing tenant constraint on agent_runs UPDATE: appendStep() updates agent_runs WHERE id = runId but does not also require tenant_id = <caller tenant>. This permits mutation of another tenant's run by runId alone.
  - _Why false positive:_ server-side worker helper updates agent_runs by server-generated run primary key, not shown crossing an untrusted request boundary
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — Missing tenant constraint on agent_runs UPDATE: updateRunState() updates run state by runId only, without constraining tenant_id = <caller tenant>. This can overwrite state for runs belonging to other tenants.
  - _Why false positive:_ server-side worker state mutation by run primary key generated internally
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — Missing tenant constraint on agent_runs UPDATE: replaceRunState() performs an update by runId only and lacks a tenant_id filter, allowing cross-tenant state replacement.
  - _Why false positive:_ internal helper replaces run state by server-generated run id, not a tenant request lookup
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — Missing tenant constraint on agent_runs UPDATE: completeRun() marks a run completed by runId without constraining tenant_id, enabling completion of runs from other tenants if only runId is known.
  - _Why false positive:_ internal completion helper mutates run by server-generated primary key
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — Missing tenant constraint on agent_runs UPDATE: failRun() updates a run to failed by runId without filtering by tenant_id, permitting cross-tenant writes.
  - _Why false positive:_ internal failure helper updates run by server-generated primary key
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — Missing tenant constraint on agent_runs UPDATE: pauseRun() updates status/state by runId only and does not ensure the run belongs to the caller's tenant.
  - _Why false positive:_ internal pause helper mutates run by server-generated primary key
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — backfillProjectAssets queries conversations without a tenant filter: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... has no WHERE tenant_id = ... and will enumerate conversations across all tenants.
  - _Why false positive:_ Global backfill/maintenance loop across tenants; processes each conversation with its own tenant/project context and does not return mixed tenant data to a tenant-facing surface.
- **server/auto-memorize.ts:163** _(system-sweep)_ — runAutoMemorize pulls messages across tenants in one query: SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ${windowStart} AND m.created_at <= ${windowEnd} AND m.role IN ('user','assistant') — there is no tenant_id constraint, so the code reads multiple tenants' messages in a single unscoped query.
  - _Why false positive:_ Privileged maintenance pass scans messages across tenants but explicitly partitions by m.tenant_id before downstream synthesis so prompts are per-tenant, not mixed.
- **server/auto-transcript.ts:128** _(system-sweep)_ — SELECT from tenant-scoped table 'conversations' without scoping by tenant_id — backfill query reads conversations across all tenants.
  - _Why false positive:_ Backfill job intentionally enumerates conversations across tenants and handles each row separately via autoSaveProjectTranscript(c.id, c.tenant_id).
- **server/auto-transcript.ts:153** _(system-sweep)_ — SELECT joining project_conversations / conversations / projects returns rows across tenants (no tenant filter). This reads tenant-scoped tables without constraining by tenant_id.
  - _Why false positive:_ Cross-tenant maintenance join is used to repair/link rows and then processes each returned row with its own tenant_id; no tenant-facing mixed output.
- **server/auto-transcript.ts:171** _(system-sweep)_ — SELECT from conversations (missingLinks query) with no tenant filter — selects c.id, c.project_id, c.tenant_id for all conversations with project_id that are missing project_conversations links, across all tenants.
  - _Why false positive:_ Missing-link reconciliation is a housekeeping sweep across all tenants inserting per-row linkage only, with no cross-tenant content exposure.
- **server/ceo-orchestrator.ts:841** _(global-table)_ — Reading the `personas` table with `db.select().from(personasTable)` without any tenant filter. Persona rows appear to be tenant-scoped; fetching all personas (across tenants) and later selecting an id by name can expose or mix personas from other tenants and cause incorrect cross-tenant assignments.
  - _Why false positive:_ Referenced schema says personas has no tenant_id column, so selecting all personas is from a non-tenant-scoped table.
- **server/chat-engine.ts:572** _(upstream-verified)_ — SELECT from tenant-scoped table `project_files` lacks a tenant_id constraint. The query: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC` does not include `tenant_id`.
  - _Why false positive:_ projects row is fetched immediately above with id and tenant_id, and project_files has no tenant_id so access is transitively scoped by verified projectId.
- **server/chat-engine.ts:583** _(upstream-verified)_ — SELECT from tenant-scoped table `project_notes` lacks a tenant_id constraint. The query: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ORDER BY created_at DESC LIMIT 20` does not include `tenant_id`.
  - _Why false positive:_ projects row is verified above with tenant_id and project_notes has no tenant_id; query is scoped through the already-validated parent projectId.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped `commitments` lacks tenant restriction: `SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' ...` reads rows across all tenants.
  - _Why false positive:_ Boot/scheduled heartbeat scanner intentionally scans all tenants and emits redacted owner-digest output without tenant content mixing.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on credential_vault is missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — allows updating another tenant's row if id matches.
  - _Why false positive:_ This function first loads the credential with getCredential(id, tenantId); the subsequent last_used_at update is on that verified row id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on credential_vault is missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — no `tenant_id` condition.
  - _Why false positive:_ This function first resolves the credential via getCredentialForSite(siteUrl, tenantId); the follow-up update uses that verified credential id.
- **server/data-protection.ts:118** _(system-sweep)_ — SELECT on tenant-scoped table `conversations` without scoping to a caller tenant (no WHERE tenant_id = ...). This enumerates soft-deleted conversations across all tenants.
  - _Why false positive:_ Global purge job intentionally enumerates expired soft-deleted conversations across tenants and processes each row with its own tenant_id.
- **server/data-protection.ts:182** _(upstream-verified)_ — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} — read from tenant-scoped `messages` without an explicit tenant_id predicate. Reads should never rely solely on a foreign id for tenant isolation per project invariant.
  - _Why false positive:_ Just above, conversation is loaded with WHERE id = conversationId AND tenant_id = tenantId; message export is within that validated call path.
- **server/data-protection.ts:189** _(upstream-verified)_ — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} — reading tenant-scoped `compaction_archives` without tenant_id constraint.
  - _Why false positive:_ Same validated backup path: conversation ownership is checked immediately above before reading archives for that conversation.
- **server/data-protection.ts:325** _(fk-transitive)_ — SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} — read from tenant-scoped `project_notes` without scoping by tenant_id. Using project_id alone is not sufficient per project invariant (no FK-based enforcement).
  - _Why false positive:_ project_notes has no tenant_id and is read only for projects already scoped by tenant_id
- **server/data-protection.ts:336** _(fk-transitive)_ — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} — reading tenant-scoped `project_files` without tenant_id in WHERE clause.
  - _Why false positive:_ project_files has no tenant_id and is read only for projects already scoped by tenant_id
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument executes DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} WITHOUT a tenant_id constraint. This can delete chunks belonging to other tenants sharing the same collection_id/doc_path (or permit cross-tenant deletion in multi-tenant setups).
  - _Why false positive:_ same function first verifies doc_collections.id belongs to tenantId before deleting chunks for that collectionId
- **server/email.ts:410** _(system-sweep)_ — backfillEmptyBodies queries inbox_messages with SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id with NO tenant/inbox scoping. This reads rows for all tenants and can leak or modify cross-tenant data during backfill.
  - _Why false positive:_ backfillEmptyBodies is a maintenance sweep filling empty body fields only and does not return tenant data or mix content into one tenant-facing output
- **server/email.ts:424** _(system-sweep)_ — backfillEmptyBodies updates inbox_messages: UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} without confirming tenant_id. Updating by id alone can affect another tenant's row if the calling context is not tenant-limited.
  - _Why false positive:_ update by row id occurs inside maintenance backfill and only mutates housekeeping/content fields for each row, not a tenant-facing mixed output
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with a vector literal derived from user/request input: 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) — vector comes from generateEmbedding(query) (request-derived). Even though vecLiteral validates numbers, using sql.raw with request-derived content increases injection risk and bypasses DB parameterization.
  - _Why false positive:_ this query already enforces tenant_id = tenantId on memory_entries; finding is SQL injection style, not cross-tenant isolation
- **server/embeddings.ts:252** _(upstream-verified)_ — ORDER BY uses sql.raw() with an interpolated vector literal (embedding) derived from caller input (ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is string-interpolated SQL with request-derived content.
  - _Why false positive:_ ORDER BY uses same tenant-scoped query with tenant_id = tenantId; not a tenant-isolation issue
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) ). The vec is derived from generateEmbedding(query) (request-derived).
  - _Why false positive:_ Query is explicitly tenant-scoped with WHERE tenant_id = ${tenantId}; issue is SQL injection, not cross-tenant isolation.
- **server/embeddings.ts:565** _(upstream-verified)_ — ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} uses sql.raw() with a request-derived vector literal — injection/isolation risk.
  - _Why false positive:_ ORDER BY uses raw vector literal, but same SELECT is constrained by tenant_id = ${tenantId}, so no tenant-isolation gap shown.
- **server/episode-playbooks.ts:68** _(upstream-verified)_ — String-interpolated embedding literal (${embeddingLiteral}::vector) is embedded directly into SQL for similarity calculation/ORDER BY using template interpolation. embeddingLiteral is derived from a runtime embedding (request-derived). This bypasses parameterized binding and risks injection if embedding data is poisoned.
  - _Why false positive:_ Similarity query reads episode_playbooks with WHERE tenant_id = ${tenantId}; flagged interpolation is not a cross-tenant isolation issue here.
- **server/episode-playbooks.ts:135** _(upstream-verified)_ — SQL uses string-interpolated array literal for id ANY(...) clause: WHERE tenant_id = ${tenantId} AND id = ANY(${`{${ids.join(",")}}`}::int[]). Building an SQL array string via interpolation is unsafe and bypasses parameterization.
  - _Why false positive:_ UPDATE is scoped by tenant_id = ${tenantId} and ids come from rows already selected under that same tenant filter.
- **server/firecrawl.ts:214** _(upstream-verified)_ — db.update(scrapedPages) uses .where(eq(scrapedPages.id, existing[0].id)) without also constraining tenant_id. The existing row was discovered via a tenant-scoped select, but the UPDATE itself lacks an explicit tenant filter.
  - _Why false positive:_ Row id being updated was fetched immediately above with url + tenantId filter, so update targets a row already proven in-tenant in same call path.
- **server/firecrawl.ts:344** _(upstream-verified)_ — Inside crawl logic, existing scraped page rows are updated via .where(eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint. This updates by id only (missing explicit tenant scoping).
  - _Why false positive:_ existing[0].id comes from prior SELECT constrained by scrapedPages.tenantId = tenantId in the same loop iteration.
- **server/google-drive.ts:560** _(upstream-verified)_ — db.update(projects) sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) and no tenant_id constraint. Even though the code earlier validated ownership, the UPDATE should still be scoped to the tenant to avoid accidental cross-tenant writes.
  - _Why false positive:_ Function checks fetched project's tenantId matches caller tenantId immediately above before performing the UPDATE.
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT INTO project_files does not include tenant_id in the inserted columns/values. Per project_files being tenant-scoped, every INSERT must set tenant_id explicitly — otherwise rows can be created without an explicit tenant binding.
  - _Why false positive:_ project_files has no tenant_id column and is transitively tenant-scoped through validated project_id ownership via assertProjectInTenant.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into tenant-scoped table presenter_slide_images without setting tenant_id (no tenant scoping on insert).
  - _Why false positive:_ presenter_slide_images is keyed by session_id to tenant-scoped presenter_sessions; child row is isolated transitively via parent session
- **server/google-workspace.ts:2364** _(fk-transitive)_ — INSERT into tenant-scoped table presenter_slide_images (thumbnail row) without setting tenant_id.
  - _Why false positive:_ thumbnail row is inserted into child table via session_id; tenant scope comes from presenter_sessions parent
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — modifying tenant-scoped rows with no tenant_id constraint (mass update by id only).
  - _Why false positive:_ stale-proposal archival is a platform-wide housekeeping sweep updating status only; no tenant data returned or mixed into one tenant surface
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — disabling tasks by id with NO tenant_id constraint (write across tenants).
  - _Why false positive:_ heartbeat task disable is a cross-tenant maintenance sweep mutating only housekeeping enabled state
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT from tenant-scoped table research_schedules is missing a tenant_id constraint (SELECT * FROM research_schedules ...). This reads schedules across all tenants.
  - _Why false positive:_ global scheduler intentionally scans due research_schedules across tenants and later processes each row with its own tenant_id
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint. Writes to a tenant-scoped table must also ensure the row belongs to the expected tenant.
  - _Why false positive:_ heartbeat maintenance updates housekeeping timing fields on each schedule row selected by the sweep; per-row tenant_id is carried and not mixed
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint. This advances next_run_at/last_run_at without scoping to tenant.
  - _Why false positive:_ scheduler sweep updates last_run_at/next_run_at housekeeping fields for the specific row being processed
- **server/heartbeat.ts:112** _(system-sweep)_ — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint (reschedules without tenant scoping).
  - _Why false positive:_ rescheduling next_run_at is part of platform-wide maintenance loop and only changes housekeeping fields per row
- **server/heartbeat.ts:120** _(system-sweep)_ — UPDATE research_schedules ... WHERE id = ${sched.id} lacks a tenant_id constraint when writing recovery next_run_at.
  - _Why false positive:_ error-recovery reschedule is a housekeeping write in the same cross-tenant scheduler sweep, not tenant-facing data mixing
- **server/heartbeat.ts:658** _(system-sweep)_ — SELECT from tenant-scoped table event_log: SELECT * FROM event_log WHERE status = 'pending' ... has no tenant_id filter. This pulls pending events across all tenants.
  - _Why false positive:_ event processor intentionally sweeps pending events across tenants and routes each event using its own tenant_id; no multi-tenant content aggregation shown
- **server/heartbeat.ts:260** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} lacks a tenant_id constraint. heartbeat_tasks is tenant-scoped and updating by id only can act across tenants.
  - _Why false positive:_ startup cleanup pass normalizes heartbeat task model as housekeeping across shared task rows, not user-scoped content access
- **server/heartbeat.ts:264** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} is performed without a tenant_id filter (tenant-scoped table).
  - _Why false positive:_ startup cleanup/seed logic intentionally sweeps heartbeat tasks globally and only mutates housekeeping config on each row by its own id
- **server/heartbeat.ts:2828** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id constraint. This reads heartbeat_logs across tenants when deciding to skip a tenant-scoped task.
  - _Why false positive:_ heartbeat_logs schema has no tenant_id and is intentionally platform-global
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs an UPDATE on agent_jobs (setting status='running', attempts++, lease_until) over rows selected without a tenant_id filter. This updates rows across tenants without tenant scoping on the write.
  - _Why false positive:_ global worker claim loop intentionally operates across all tenants and returns per-job tenant_id for downstream per-row handling
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases updates agent_jobs (marking failed_terminal / re-queued) without tenant_id constraint, affecting jobs across tenants.
  - _Why false positive:_ lease-reclaim is platform-wide maintenance updating only job lifecycle fields across tenants
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped table `projects` without constraining to a specific tenant (reads all tenants). The query groups by tenant_id but still scans tenant-scoped rows for every tenant in the DB.
  - _Why false positive:_ boot/backfill job intentionally scans snapshot tenants across the platform and only computes per-tenant counts; no tenant-facing mixed output.
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` uses only project_id/file_name and omits tenant_id. Reading a tenant-scoped table without an explicit tenant filter can leak or mix rows across tenants.
  - _Why false positive:_ project_files has no tenant_id and is transitively isolated through tenant-scoped parent projects via project_id.
- **server/lib/ideabrowser-backfill.ts:116** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` uses only project_id and note and omits tenant_id. Reading tenant-scoped rows without tenant constraint risks returning or touching other tenants' data.
  - _Why false positive:_ project_notes has no tenant_id and is transitively isolated through tenant-scoped parent projects via project_id.
- **server/lib/scheduled-post-runner.ts:60** _(system-sweep)_ — CTE selects/updates rows from tenant-scoped table `scheduled_posts` without filtering by tenant_id. The initial SELECT ... FOR UPDATE and the subsequent UPDATE in the same CTE operate across all tenants (no tenant_id filter), violating the requirement that reads/writes to tenant-scoped tables include tenant constraints tied to the caller’s tenant.
  - _Why false positive:_ global scheduler intentionally claims due rows across all tenants and returns each row with its own tenant_id for per-row handling; maintenance workflow not tenant-facing aggregation.
- **server/lib/scheduled-post-runner.ts:174** _(system-sweep)_ — UPDATE of scheduled_posts uses WHERE id = ${row.id} with no tenant_id constraint. Mutating a tenant-scoped row without constraining by tenant_id can allow cross-tenant effects if the caller context expects tenant-scoped enforcement.
  - _Why false positive:_ system runner updates the exact row it previously locked and carried with tenant_id; per-row housekeeping/state transition across tenants is intentional.
- **server/lib/scheduled-post-runner.ts:191** _(system-sweep)_ — Error-path UPDATE of scheduled_posts (catch block) performs UPDATE ... WHERE id = ${row.id} AND status = 'publishing' without constraining by tenant_id. This will persist per-platform results/last_error without an explicit tenant filter.
  - _Why false positive:_ error-path release/update is part of the same platform-wide runner and only mutates row status/housekeeping for the locked row, not tenant-facing mixed data.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only the row id in WHERE (.where(eq(tenantVoiceProfiles.id, existing.id))) and does not include a tenant_id constraint. Per invariant, writes against tenant-scoped tables must be constrained by tenant_id.
  - _Why false positive:_ existing row is fetched immediately before via getVoiceProfile({ tenantId: opts.tenantId, profileName }); update uses that owned row's server-derived id
- **server/memory-graph.ts:81** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — no tenant_id constraint. This will modify a category row regardless of tenant, enabling cross-tenant mutation/confusion.
  - _Why false positive:_ catId comes from ensureCategory(categoryName, tenantId, ...) which selects/inserts the category under the same tenant immediately beforehand
- **server/memory-graph.ts:196** _(fk-transitive)_ — SELECT 1 FROM memory_links ... LIMIT 1 queries memory_links with no tenant filter. Reading a tenant-scoped table without tenant_id allows cross-tenant information leaks/logic errors.
  - _Why false positive:_ memory_links has no tenant_id and is isolated through source/target memory_ids, both drawn from tenant-scoped memory_entries already filtered to tenantId
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) does not include tenant_id. Tenant-scoped tables must have tenant_id passed on INSERT; omitting it writes tenant-ambiguous rows and breaks tenant isolation.
  - _Why false positive:_ memory_links has no tenant_id by schema; inserted link references only same-tenant memory_entries selected/created in this flow
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id constraint. Updating by id alone can modify another tenant's row if ids collide or are guessed.
  - _Why false positive:_ mem.id comes from a SELECT on memory_entries scoped by tenant_id = tenantId a few lines above in the same function
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — missing tenant_id constraint after creating a new memory entry. This update writes a tenant-scoped table by id only.
  - _Why false positive:_ entry.id is from storage.createMemoryEntry with tenantId passed in this same branch; update targets that newly created tenant-owned row
- **server/memory-intelligence.ts:316** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — missing tenant check in the update path for superseded/replacement entries.
  - _Why false positive:_ entry.id is from storage.createMemoryEntry with tenantId in the same branch, so the updated row is the just-created tenant-owned row
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — no tenant_id constraint. Even though c was selected earlier with tenant_id, the UPDATE itself lacks explicit tenant scoping.
  - _Why false positive:_ each c.id comes from an immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId and valid_until IS NULL
- **server/minds-engine.ts:460** _(upstream-verified)_ — Rollback UPDATE of plans (UPDATE plans SET status = 'awaiting_approval', ceo_decision = NULL, ... WHERE id = ${args.planId} AND status = 'revising') omits tenant_id. This may roll back a plan in another tenant.
  - _Why false positive:_ messages are joined through conversations c with c.tenant_id = tenantId, so transcript rows are tenant-scoped via the parent conversation
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id constraint (enumerates tenant rows).
  - _Why false positive:_ startup validation intentionally scans subscriptions across tenants for auto-reconnect maintenance and handles each row by its own tenant_id
- **server/pace-control.ts:85** _(global-table)_ — checkPace queries heartbeat_logs without any tenant_id filter (SELECT COUNT(*) ... FROM heartbeat_logs ... AND ${laneFilter}) — unscoped reads against what appears to be a tenant-scoped activity table.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is described as heartbeat/pace-gate throttle data used for global pacing caps
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot aggregates heartbeat_logs over a time window without filtering by tenant (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...).
  - _Why false positive:_ heartbeat_logs has no tenant_id and snapshot is over global pace/heartbeat log data by design
- **server/process-governor.ts:121** _(fk-transitive)_ — Subselects count heartbeat_logs without scoping by tenant. The inner queries (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...) lack a tenant_id = ${tenantId} filter and thus can read logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted via tenant-scoped heartbeat_tasks ht.id, so scope is inherited from the parent task rows
- **server/process-governor.ts:199** _(global-table)_ — Global aggregation over heartbeat_logs without tenant filter: SELECT AVG(duration_ms) FROM heartbeat_logs WHERE ... has no tenant_id constraint and will aggregate across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and this computes a platform-wide response-time metric from global heartbeat log data
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures query counts distinct persona_id from heartbeat_logs but omits tenant scoping: WHERE status != 'success' ... has no tenant_id filter.
  - _Why false positive:_ heartbeat_logs has no tenant_id and query computes a global cascading-failure signal rather than tenant row access
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT against tenant-scoped table `agent_knowledge` performs an unscoped read (no `tenant_id` WHERE clause).
  - _Why false positive:_ heartbeat-driven scheduler intentionally scans due jobs across tenants and carries each row's tenant_id for per-row scoped handling
- **server/research-engine.ts:478** _(fk-transitive)_ — INSERT into `project_notes` omits `tenant_id` (INSERT INTO project_notes (project_id, note, author, created_at) ...). Per design all tenant-scoped INSERTs must include tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id and inserts are transitively scoped through project_id; project ownership is verified just above with assertProjectInTenant
- **server/research-engine.ts:486** _(fk-transitive)_ — INSERT into `project_notes` inside the findings loop omits `tenant_id`, creating project notes without explicit tenant scoping.
  - _Why false positive:_ project_notes has no tenant_id and inserts are transitively scoped through project_id; parent project tenant is checked earlier
- **server/routes/admin-zombie-detector.ts:38** _(system-sweep)_ — Query against agent_trace_spans (tx.execute) has no tenant_id constraint — it aggregates tool usage across all tenants.
  - _Why false positive:_ Route is explicitly restricted to platform admin and intentionally provides platform-wide aggregate tool-usage/last-used diagnostics across tenants.
- **server/routes/admin-zombie-detector.ts:54** _(system-sweep)_ — Query over conversations (used AS (...) FROM conversations ...) lacks any tenant_id constraint and thus aggregates persona usage across all tenants.
  - _Why false positive:_ Platform-admin-only diagnostic intentionally aggregates conversation/persona usage across tenants for zombie detection rather than serving tenant-facing data.
- **server/routes/admin.ts:587** _(system-sweep)_ — SELECT FROM ai_insights (stuckInsights) is executed without a tenant_id constraint — returns insights across tenants.
  - _Why false positive:_ Admin-only silent-failures dashboard intentionally scans platform-wide housekeeping/status rows and returns ops diagnostics, not tenant-scoped user-facing data.
- **server/routes/admin.ts:598** _(system-sweep)_ — Query selecting heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3 has no tenant_id constraint — can expose tenant-scoped heartbeat task rows across tenants.
  - _Why false positive:_ Admin-only operational endpoint intentionally reports failing heartbeat tasks across tenants for platform maintenance.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` lacks a tenant_id constraint. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates by id only and does not include `AND tenant_id = <caller tenant>`.
  - _Why false positive:_ Conversation row was just created with caller tenantId server-side, and project ownership was verified by WHERE id = projectId AND tenant_id = tenantId before this update.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into `project_conversations` omits tenant_id. The statement `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_conversations has no tenant_id and is a join/child table isolated transitively by verified tenant-scoped project and newly created tenant-scoped conversation.
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` lacks a tenant_id constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` updates by id only and omits `AND tenant_id = <caller tenant>`.
  - _Why false positive:_ Project ownership is explicitly verified a few lines above with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before touching updated_at.
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` does not include tenant_id for a tenant-scoped table.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; insert is transitively scoped via projId derived from joined conversation/project check with p.tenant_id enforced.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_messages` performed without tenant_id predicate. The code sets is_read true with WHERE id = ${msg.id} only, which can mutate any tenant's row if message id is guessed or an attacker can control msg.id.
  - _Why false positive:_ The message is fetched immediately above with WHERE id = msgId AND tenant_id = tenantId, and the update uses that verified row's id in the same request path.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE against tenant-scoped table `projects` lacks a tenant_id constraint. The UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} does not include AND tenant_id = ${tenantId}.
  - _Why false positive:_ Project ownership is checked earlier in the same handler with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before updating updated_at.
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE against tenant-scoped table `projects` lacks a tenant_id constraint. The UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} does not include AND tenant_id = ${tenantId}.
  - _Why false positive:_ Project ownership is checked earlier in the same handler with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before updating updated_at.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint. The query SELECT * FROM project_files WHERE project_id = ${projectId} ... does not include tenant_id filtering on project_files.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by prior tenant-checked parent project_id
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint in download handler. Query is SELECT ... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 without filtering on tenant_id.
  - _Why false positive:_ project_files has no tenant_id; handler first verifies projects.id belongs to tenant, then reads child by project_id
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE against tenant-scoped table `project_files` lacks tenant_id constraint. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not include tenant_id.
  - _Why false positive:_ project_files is child table without tenant_id and delete is scoped by tenant-verified project_id
- **server/seed.ts:3137** _(system-sweep)_ — UPDATE on tenant-scoped table `projects` has no tenant_id constraint (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL). This can modify rows across tenants.
  - _Why false positive:_ boot-time owner-deployment seed fix updates a single hardcoded row as maintenance, not tenant-facing mixed data access
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) occurs after a cross-tenant-unscoped SELECT. `project_conversations` has no tenant_id column in schema; inserting links without ensuring tenant ownership can create cross-tenant relationships or corrupt tenant isolation.
  - _Why false positive:_ project_conversations has no tenant_id and backfill links conversation.project_id to child relation transitively scoped by parent records
- **server/seed.ts:4431** _(system-sweep)_ — DELETE FROM heartbeat_tasks ... runs without a tenant_id constraint. `heartbeat_tasks` is tenant-scoped (there are tenant indexes), so this DELETE may remove rows across all tenants.
  - _Why false positive:_ platform-wide housekeeping purge of stale auto-disabled heartbeat rows only mutates maintenance status and returns no tenant data
- **server/seed.ts:4459** _(system-sweep)_ — UPDATE memory_entries SET fact = REPLACE(...) WHERE ... executes on `memory_entries` without any tenant_id restriction. `memory_entries` is tenant-scoped (index exists), so this mass UPDATE will affect all tenants' memories.
  - _Why false positive:_ platform-wide canonicalization sweep updates memory text in place and does not return or mix tenant data into any output surface
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant (distillIntent recentMsgs query uses only conversationId).
  - _Why false positive:_ analyzeConversation is called first and verifies the conversation belongs to tenantId; recentMsgs are then fetched by that validated conversationId
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant (analyzeConversation reads all messages for conversationId with no tenant filter).
  - _Why false positive:_ same function first loads conversations row with id + tenantId, proving the conversation is in the tenant before reading its messages by conversationId
- **server/skillify.ts:221** _(global-table)_ — SELECT from `personas` lacks tenant scoping (personaRows lookup uses only persona id). If `personas` is tenant-scoped this allows cross-tenant reads.
  - _Why false positive:_ schema note says personas has no tenant_id column, so this lookup is not tenant-scoped
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without tenant constraint (skillifyConversation recentMsgs query uses only conversationId).
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), which validates tenant ownership of the conversation before this message fetch
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT aggregation from `heartbeat_logs` (error counts) has no tenant filter — this aggregates across all tenants and can lead to cross-tenant actions/decisions.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is an intentionally platform-global watchdog table
- **server/storage.ts:295** _(upstream-verified)_ — SELECT FROM tenant-scoped table `messages` without tenant_id constraint in attachCitationMetadata (SELECT id, citations FROM messages WHERE id IN (...)) — can leak citations across tenants.
  - _Why false positive:_ attachCitationMetadata only re-reads ids from msgs already fetched by tenant-scoped callers, so ownership is established earlier in the same path
- **server/storage.ts:824** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` without tenant filter in getMemoriesWithoutEmbeddings (.where(eq(status,'active'), isNull(embedding))). This returns rows across tenants.
  - _Why false positive:_ getMemoriesWithoutEmbeddings is a cross-tenant maintenance selector for embedding backfill and only feeds housekeeping processing, not tenant-facing output
- **server/storage.ts:830** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in getKnowledgeWithoutEmbeddings (.from(agent_knowledge).where(...)). This returns rows across tenants.
  - _Why false positive:_ getKnowledgeWithoutEmbeddings is a global backlog selector for embedding maintenance and does not itself mix content into a tenant response
- **server/storage.ts:1445** _(upstream-verified)_ — UPDATE on tenant-scoped table `architecture_decisions` without tenant filter in supersedeAdr (UPDATE architecture_decisions SET ... WHERE id = oldId / newId). Although rows were previously selected with tenant checks, the UPDATE itself lacks tenant scoping and could affect rows if state changed between select/update.
  - _Why false positive:_ same function first selects both ADR rows with matching tenant_id and only then updates those verified ids
- **server/surprise-scorer.ts:141** _(upstream-verified)_ — UPDATE on tenant-scoped table `felix_proposals` without tenant_id constraint in scoreProposalSurprise (UPDATE felix_proposals SET actual_outcome_embedding = ... WHERE id = ${proposalId}). This can modify another tenant's row.
  - _Why false positive:_ scoreProposalSurprise first fetches the proposal with id and tenant_id in the same call path before stamping outcome embedding
- **server/surprise-scorer.ts:149** _(upstream-verified)_ — SELECT args_embedding FROM tenant-scoped `felix_proposals` WHERE id = ${proposalId} without tenant filter. Re-fetching the row by id without tenant scoping can expose/return another tenant's data.
  - _Why false positive:_ same function already loaded the target proposal under id plus tenant_id, and this re-fetch is for the same proposal after lazy backfill
- **server/surprise-scorer.ts:201** _(upstream-verified)_ — SELECT AVG(...) FROM tenant-scoped `felix_proposals` WHERE id = ANY(${neighborIds}::int[]) without tenant scoping. Even though neighborIds were produced from a tenant-scoped query, this AVG uses arbitrary ids and lacks an explicit tenant check.
  - _Why false positive:_ neighborIds come only from the immediately preceding tenant-scoped neighbor query in the same function
- **server/surprise-scorer.ts:213** _(upstream-verified)_ — UPDATE felix_proposals SET surprise_score = ..., surprise_band = ... WHERE id = ${proposalId} without tenant constraint. This writes back scoring state without tenant scoping.
  - _Why false positive:_ same call path first verifies the proposal exists under the supplied tenant_id before writing back score/band to that proposal id
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (checks for existing tool name use `WHERE name = $name` only). This can leak or reason about other tenants' tool names and allows name-collision logic to operate cross-tenant.
  - _Why false positive:_ tool names are first transformed with tenantToolName(parsed.name, tenantId), making the existence check tenant-derived rather than cross-tenant
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT by id from tenant-scoped `custom_tools` without constraining tenant_id (reads inserted row by id only). Missing tenant filter can expose or return rows across tenants.
  - _Why false positive:_ Row was just inserted with tenantId and selected back by returned server-generated inserted.id in same flow; no untrusted cross-tenant boundary.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment `usageCount` in tenant-scoped `custom_tools` uses WHERE id = tool.id with NO tenant_id constraint. This mutates a tenant-scoped row without verifying tenant ownership.
  - _Why false positive:_ tool.id comes from prior tenant-scoped lookup by name and tenantId in same function, so update targets an already tenant-verified row.
- **server/tools.ts:2911** _(fk-transitive)_ — SELECT subqueries in project list omit tenant scoping for child tables. The list query uses (SELECT COUNT(*) FROM project_files WHERE project_id = p.id), (SELECT COUNT(*) FROM project_notes WHERE project_id = p.id), (SELECT COUNT(*) FROM project_conversations WHERE project_id = p.id) without restricting those counts to the caller's tenant.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are transitively scoped by parent projects row already filtered with p.tenant_id = tenantId.
- **server/tools.ts:2973** _(fk-transitive)_ — Duplication-check SELECT against project_files does not include tenant constraint. The code checks for existing files via SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ... but does not include tenant_id in the WHERE clause.
  - _Why false positive:_ project_files has no tenant_id; ownerCheckF first verifies the parent project belongs to tenant, so duplicate check by project_id is transitively tenant-scoped.
- **server/tools.ts:3014** _(upstream-verified)_ — SELECT id FROM project_conversations used to detect existing link lacks tenant scoping. The query checks project_conversations by project_id and conversation_id but does not filter by tenant_id.
  - _Why false positive:_ Same path verifies project ownership and calls assertConversationInTenant before the existence check; project_conversations has no tenant_id and link is bounded by verified parent/child ownership.
- **server/tools.ts:6912** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) call omits tenant_id, so new rows may not be associated with the caller's tenant (columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_files has no tenant_id; insert is correct by design after assertProjectInTenant verifies parent project ownership.
- **server/tools.ts:8872** _(upstream-verified)_ — UPDATE against tenant-scoped table `proposed_skills` is missing a tenant constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) does not include tenant_id in the WHERE clause, allowing modification of a row with the same id from another tenant.
  - _Why false positive:_ row.id is from the just-inserted proposed_skills row in the same flow with tenantId tid set on insert, so update is against that already-owned row.
- **server/tools.ts:8914** _(upstream-verified)_ — UPDATE against tenant-scoped table `proposed_skills` is missing a tenant constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) is executed in the REJECT branch without filtering by tenant_id.
  - _Why false positive:_ This update also targets row.id from the same newly inserted tenant-scoped proposed_skills record, already bound to tid earlier in call path.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' missing tenant constraint — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} has no tenant_id filter.
  - _Why false positive:_ workflow is fetched immediately above with tenant_id = tenantId, and update uses workflow.id from that verified row.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (${projId}, ...) omits tenant_id which violates the rule that every INSERT into a tenant-scoped table must include tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id; projId is obtained through join to projects scoped by tenantId, so child insert is transitively isolated.
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE against tenant-scoped table 'projects' missing tenant constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} lacks a tenant_id filter, allowing an update without ensuring the project belongs to the caller's tenant.
  - _Why false positive:_ projId comes from prior JOIN selecting only projects with p.tenant_id = tenantId in the same path, so project ownership was already verified.
- **server/tools/domains/memory/handlers.ts:272** _(upstream-verified)_ — Potential IDOR: the handler accepts a caller-supplied conversationId (params.conversationId) and later uses it to read archives/recall data without first verifying the conversation belongs to the caller's tenant. This allows an attacker who can supply arbitrary conversation IDs to attempt to access other tenants' conversation data.
  - _Why false positive:_ caller-supplied conversationId is immediately ownership-checked via conversations WHERE id = safeConvId AND tenant_id = __tid before any projectWide archive enumeration
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` executed without any tenant_id constraint. This returns rows across tenants (even though guarded by tenantId===1), violating the invariant that every tenant-scoped read must include an application-level tenant filter.
  - _Why false positive:_ schema note says provider_keys has no tenant_id; this admin-only audit reads a platform-global table by design
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed without a tenant_id constraint. This aggregates provider keys across all tenants, exposing cross-tenant data to the caller (even if only run for admin tenant).
  - _Why false positive:_ provider_keys is non-tenant-scoped per schema, so distinct-provider aggregation is over a global table
- **server/tools/domains/strategic-memory/handlers.ts:95** _(upstream-verified)_ — UPDATE memory_entries executed without scoping by tenant_id (WHERE id = ${id}) — writes to a tenant-scoped table are not constrained to the caller's tenant.
  - _Why false positive:_ id comes from a prior SELECT on memory_entries scoped by tenant_id and persona_id, then updated by that selected PK
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) executed without scoping by tenant_id — batch update may affect rows across tenants.
  - _Why false positive:_ ids array is derived only from rows previously selected from memory_entries with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries executed without scoping by tenant_id (WHERE id = ${id}) in recordStrategicWinHandler — write is not constrained to the caller's tenant.
  - _Why false positive:_ updated id was first selected from memory_entries under tenant_id = tid and persona_id = personaId dedup query
- **server/wiring-invariants.ts:244** _(system-sweep)_ — checkDormantTools runs SELECT count(*) FROM tool_performance WHERE last_success_at > ... without any tenant_id filter (reads tenant-scoped telemetry across all tenants).
  - _Why false positive:_ platform-wide wiring diagnostic aggregates housekeeping telemetry counts only and does not return tenant row data or mix tenant content into one response
- **server/wiring-invariants.ts:258** _(system-sweep)_ — checkDormantTools executes SELECT count(*) FROM tool_performance (everResult) without a tenant constraint, reading total rows across all tenants.
  - _Why false positive:_ global dormant-tools diagnostic uses total counts for maintenance/health checking only, with no tenant content returned
- **server/wiring-invariants.ts:260** _(system-sweep)_ — checkDormantTools executes SELECT count(*) FROM messages WHERE created_at > ... with no tenant_id filter, reading user chat messages across tenants.
  - _Why false positive:_ cross-tenant message count is used only as an aggregate health signal in a maintenance check, not to expose or mix message content
- **server/wiring-invariants.ts:293** _(system-sweep)_ — checkDormantTools runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant scoping, returning tool usage across all tenants.
  - _Why false positive:_ Boot/self-check invariant scans tool_performance globally to emit maintenance findings about dormant tools; aggregate tool-name list only, intentional cross-tenant ops telemetry, not tenant-facing row data.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — checkToolDrift runs an aggregate SELECT FROM tool_performance (SUM/MAX/GROUP BY) filtering only by last_failure_at > ... but with no tenant_id constraint — it aggregates tenant-scoped telemetry across all tenants.
  - _Why false positive:_ Global maintenance drift detector aggregates tool_performance for platform health warnings; operates across tenants for housekeeping/ops and returns only summarized findings, not tenant-scoped data exposure.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules performed without tenant constraint: UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — this updates a tenant-scoped table by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/agentic-engines.ts:533** _(global-table)_ — runOptimizationEngine selects heartbeat_tasks with WHERE enabled = true but NO tenant_id constraint (SELECT ... FROM heartbeat_tasks WHERE enabled = true), which reads tasks across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/commitment-drafter.ts:234** _(system-sweep)_ — Unscoped UPDATE on tenant-scoped table commits without tenant filter: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — no `AND tenant_id = ...` present.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/commitment-drafter.ts:234 — This UPDATE is the fail-CLOSED revert branch inside the background commitment-drafter loop. row.id comes from claimedRows, which were claimed with an explicit AND c.tenant_id = ${tenantId} predicate (line 204). The rows are server-derived from a tenant-scoped claim, not attacker input, and this only reverts draft_status back to 'open' for a row this loop jus
- **server/google-drive.ts:826** _(upstream-verified)_ — uploadAndShare reads a project row by id (where eq(projects.id, params.projectId)) without first restricting by tenant_id; the code then uses proj.tenantId to validate/compare. Reading the referenced row without a tenant-scoped WHERE is an information-exposure/ownership-verification gap.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): uploadAndShare selects only id/name/tenantId and enforces proj.tenantId!==params.tenantId → returns error at google-drive.ts:840-842 before any Drive/DB write; ensureProjectFolder re-checks at 551.
- **server/heartbeat.ts:2922** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without a tenant_id constraint after creating a new tenant-scoped task.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3374** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (after creating a chat-delegation task) lacks a tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/seed.ts:2860** _(system-sweep)_ — UPDATE on tenant-scoped table without tenant constraint: UPDATE research_programs SET model = ... WHERE id = ${p.id} updates by id only and lacks WHERE tenant_id = <tenant>. This can modify another tenant's data if ids collide or are global.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:2841-2865 fixResearchProgramModels() is a boot-time housekeeping loop; iterates rows from SELECT ...research_programs WHERE is_active (2855) and only rewrites the 'model' housekeeping field for rows whose model is not in MODEL_REGISTRY. No untrusted boundary; never returns row data to a tenant surface.
- **server/seed.ts:3135** _(system-sweep)_ — UPDATE on tenant-scoped table without tenant constraint: UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... has no tenant_id filter, so it may modify a project belonging to a different tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:3131-3138 inside seedDatabase(); guarded by isOwnerDeployment() (3133) and uses fully hardcoded literals (id=15, primary_conversation_id=272) — a one-time owner-deployment migration, not reachable from any request input.
