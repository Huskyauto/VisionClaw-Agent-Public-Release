# Tenant-Isolation Audit Report

_Generated 2026-07-21T02:17:25.312Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **548/548** file(s) audited · chunks 52/52 OK (0 failed)
- Findings: **309** (CRITICAL 94 · HIGH 168 · MEDIUM 47 · LOW 0)
- Precision second-pass (`gpt-5.4`): **147** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (94)

- **server/agentic-engines.ts:242** — autoApplyEligibleInsights runs an UPDATE on `ai_insights` (SET status='applied' ...) without scoping by tenant_id. This can change statuses for rows across all tenants in bulk.
  - _Fix:_ Constrain the UPDATE to a tenant (include tenant_id = ${tenantId}) or perform the sweep on a per-tenant basis. If a global sweep is intended, restrict access to an authorized admin path and audit changes carefully. Never perform cross-tenant writes from tenant-scoped background logic.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on `agentApprovals` (setting status='expired' for pending approvals past expiry) without a tenant constraint, affecting rows across tenants, then returns rows for per-run handling. This is a cross-tenant write sweep.
  - _Fix:_ Restrict the bulk expiry to a single tenant (pass tenantId into expireStaleApprovals and add eq(agentApprovals.tenantId, tenantId) to the WHERE) or make the function an explicit admin operation with strict authorization and logging. Never perform unscoped UPDATEs on tenant-scoped tables in code that runs with regular privileges.
- **server/agentic/runs.ts:34** — appendStep() performs an UPDATE on the tenant-scoped table `agent_runs` using WHERE eq(agentRuns.id, runId) with NO tenant_id constraint — this allows a caller/process to modify another tenant's run row.
  - _Fix:_ Require and pass the tenantId into appendStep (or verify ownership) and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Audit all callers to pass the tenantId and fail closed if missing. Also ensure the step payload is parameterized (already JSON.stringified) but the tenant check must be enforced server-side.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs via .where(eq(agentRuns.id, runId)) with NO tenant_id constraint — cross-tenant row modification is possible.
  - _Fix:_ Add tenantId as an explicit parameter and include eq(agentRuns.tenantId, tenantId) in the WHERE clause. Update all call sites to pass the tenantId and fail if the caller cannot prove ownership.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs with .where(eq(agentRuns.id, runId)) and omits tenant scoping, enabling cross-tenant writes.
  - _Fix:_ Require tenantId and scope the UPDATE with AND eq(agentRuns.tenantId, tenantId). Update callers to supply tenantId and/or validate ownership before invoking this function.
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs.status/result/completedAt using .where(eq(agentRuns.id, runId)) with NO tenant_id filter — this can mark another tenant's run as completed.
  - _Fix:_ Add tenantId parameter and include eq(agentRuns.tenantId, tenantId) in the WHERE clause. Ensure callers pass the tenantId and that the function rejects attempts without ownership.
- **server/agentic/runs.ts:67** — failRun() updates agent_runs.status/error/completedAt using .where(eq(agentRuns.id, runId)) without constraining tenant_id — allows cross-tenant row mutation.
  - _Fix:_ Require tenantId and include AND eq(agentRuns.tenantId, tenantId) in the WHERE. Ensure callers pass tenantId and audit that no code invokes failRun without tenant-scoped validation.
- **server/agentic/runs.ts:76** — pauseRun() updates agent_runs.status/state using .where(eq(agentRuns.id, runId)) with NO tenant_id check — permits pausing runs from other tenants.
  - _Fix:_ Add tenantId to the function signature and scope the UPDATE with eq(agentRuns.tenantId, tenantId). Update all callers to provide tenantId and verify ownership before calling.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL — missing tenant_id constraint in WHERE clause. This UPDATE can modify a conversation belonging to a different tenant if row ids are manipulated or results are poisoned.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id} or AND tenant_id = ${tenantId}). Always verify ownership before updating tenant-scoped rows.
- **server/data-protection.ts:112** — SELECT from conversations fetches soft-deleted rows across ALL tenants (no tenant_id filter). This administrative purge iterates over returned rows and then deletes related data; running this without explicit admin scoping or protections can lead to cross-tenant purges/leakage if misused.
  - _Fix:_ If this is intended to be a true multi-tenant global purge, require an explicit admin-only code path/flag and document it clearly; otherwise restrict the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND deleted_at IS NOT NULL AND deleted_at < ${cutoff}). Consider using withTenantTx or otherwise enforcing RLS for this operation if it must run per-tenant safely.
- **server/data-protection.ts:125** — DELETE FROM messages executed without tenant-scoping predicate. The purge loop deletes rows using conversation_id only (no tenant_id constraint). Per the tenant-isolation invariant, DELETEs against tenant-scoped tables must include tenant_id in WHERE to prevent accidental cross-tenant modifications.
  - _Fix:_ Include tenant scoping in these DELETEs (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or perform deletes within a transaction that has explicitly-verified tenant context for conv. Do the same for compaction_archives and conversations deletes below.
- **server/data-protection.ts:126** — DELETE FROM compaction_archives executed without tenant-scoping predicate (no tenant_id in WHERE). This violates the requirement that writes/deletes on tenant-scoped tables must be constrained by tenant_id.
  - _Fix:_ Restrict the delete to the conversation's tenant (e.g. DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or run the purge with an explicit, documented admin-only scope and additional safeguards.
- **server/data-protection.ts:127** — DELETE FROM conversations executed without tenant_id predicate (DELETE WHERE id = ${conv.id}). Even though conv.id came from an earlier SELECT, the DELETE lacks the tenant predicate and thus violates the invariant requiring explicit tenant-level filtering on writes/deletes.
  - _Fix:_ Add tenant scoping to the delete (e.g. DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or ensure the operation runs in a privileged, auditable admin context that is intentionally global and cannot be invoked by untrusted callers.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog() issues an UPDATE against tenant-scoped table `delivery_logs` without constraining the row by tenant_id (WHERE tenant_id = ...). This lets code update any delivery row by id regardless of tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE. Either accept/require a tenantId parameter and use .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))) or otherwise verify the caller owns the delivery before updating.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks is executed with WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but WITHOUT tenant_id. This can delete another tenant's chunks if collection ids/doc_paths collide or are misused.
  - _Fix:_ Include tenant scoping in the DELETE: add AND tenant_id = ${tenantId}, or otherwise validate the collection belongs to the tenant before deleting.
- **server/embeddings.ts:684** — UPDATE on tenant-scoped tables (memory_entries / agent_knowledge) performed by storeEmbeddingVec does not constrain by tenant_id — UPDATE ${sql.raw(table)} SET ... WHERE id = ${id} (no tenant filter). This can modify rows across tenants.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec, and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Prefer using Drizzle parameterized updates (not sql.raw for table names unless validated) and verify ownership before touching rows.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} (in backfillEmbeddingVecs) lacks any tenant constraint — write can affect rows in any tenant.
  - _Fix:_ Include tenant ownership in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id || tenantId}) or pass tenantId through and use WHERE id = ${id} AND tenant_id = ${tenantId}. Treat maintenance writes as tenant-scoped or run under an explicit, auditable admin flow.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} (in backfillEmbeddingVecs) has no tenant_id constraint and will update rows across tenants.
  - _Fix:_ Require tenant context and include WHERE tenant_id = ${tenantId} in the UPDATE. Prefer parameterized Drizzle updates that include tenant_id to prevent accidental cross-tenant writes.
- **server/firecrawl.ts:184** — UPDATE on scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant constraint. This allows updating a scraped page row by id without scoping to the caller's tenant_id.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or add tenant_id = ${tenantId} to the SQL) so the write is always scoped to the caller's tenant.
- **server/firecrawl.ts:307** — UPDATE on scrapedPages inside the crawl loop uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant constraint. Same cross-tenant write risk as above.
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the update is constrained to the calling tenant.
- **server/google-drive.ts:946** — INSERT INTO project_files does not include tenant_id in the column list or values. Per the invariant, INSERTs into tenant-scoped tables must explicitly set tenant_id — otherwise this either fails (if tenant_id is NOT NULL) or creates uncategorized/incorrectly-scoped rows.
  - _Fix:_ Pass the tenant id explicitly in the INSERT (add tenant_id column and value, e.g. VALUES (..., ${resolvedTenantId}, ...)) after verifying ownership. Prefer using parameterized Drizzle helpers or sql`` with ${resolvedTenantId} to ensure the inserted row is correctly scoped to the caller's tenant.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(...) executes without constraining tenant_id — this mutates rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. UPDATE code_proposals SET ... WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform the archive per-tenant after verifying admin authorization to operate cross-tenant. Never perform global writes on tenant-scoped tables without an explicit tenant filter and authorization check.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) executes without tenant constraint — this can disable tasks across tenants.
  - _Fix:_ Add tenant-based protection (e.g. UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform per-tenant updates only after verifying admin authorization. Avoid global writes against tenant-scoped tables without explicit tenant scoping.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images is executed without setting tenant_id nor verifying the session's tenant ownership — write into a likely tenant-scoped table without tenant scoping.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. tenant_id = ${tenantId}) or validate that session_id belongs to the current tenant before inserting. If presenter_slide_images is scoped indirectly via session_id, first SELECT/verify that the session belongs to the same tenant and include tenant_id in the write or add AND tenant_id = ${tenantId} constraints on subsequent queries.
- **server/google-workspace.ts:2364** — INSERT (thumb) into presenter_slide_images for quality='thumb' occurs without tenant_id or ownership check — another write to a tenant-scoped table lacking tenant scoping.
  - _Fix:_ Same fix as above: include tenant_id in the INSERT or verify session ownership (SELECT presenter_sessions WHERE id=${sessionId} AND tenant_id=${tenantId}) before writing. Ensure all INSERTs into tenant-scoped tables set tenant_id explicitly.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint — updating a tenant-scoped row without verifying ownership.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first validate that the session belongs to the tenant calling this code. If this code runs with admin privileges intentionally, require an explicit admin check and document that this is an admin-only cross-tenant operation.
- **server/internal-resolver.ts:91** — finalizeEvent() performs UPDATE event_log ... WHERE id = ${eventId} ... without constraining by tenant_id. This mutates tenant-scoped event_log rows without verifying the target row belongs to the caller/actor tenant.
  - _Fix:_ Require and use the event's tenant_id when finalizing: either accept tenantId as an argument or first SELECT the event row (with id) and verify its tenant_id, then UPDATE with an explicit WHERE id = ${eventId} AND tenant_id = ${tenantId} AND status IN (...). Do not rely on id-only updates for tenant-scoped tables.
- **server/internal-resolver.ts:170** — resolveExperimentFailed() runs UPDATE code_proposals ... WHERE id = ${proposalId} AND status = 'pending' (RETURNING ...) without constraining by tenant_id. This can archive/modify another tenant's code_proposal row identified only by id.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or first SELECT the proposal by id and tenant, verify ownership, then perform the state transition. All writes to tenant-scoped tables must include an explicit tenant_id guard to enforce isolation.
- **server/lib/scheduled-post-runner.ts:61** — SELECT from tenant-scoped table `scheduled_posts` performed without any tenant_id filter (CTE that picks due rows is unbounded across tenants).
  - _Fix:_ Constrain the SELECT to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly iterate/dispatch per tenant. If this is intentionally system-scoped, make the privilege explicit and restrict this runner to an admin-only context and audit/ACL the callsite; otherwise require application-level tenant predicates on tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:70** — UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id updates tenant-scoped rows without including tenant_id in the WHERE clause — writes are not constrained to a tenant.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. AND sp.tenant_id = ${tenantId}) or otherwise ensure the update only affects rows owned by the intended tenant(s). If this runner must operate cross-tenant, perform an explicit per-tenant loop and enforce admin-only privileges with strong audit logging.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} is a tenant-scoped write without any tenant_id guard — row.id alone is used to identify the row, allowing cross-tenant updates if called in wrong context.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${row.tenant_id} or AND tenant_id = ${tenantId} from the caller context). Never rely solely on id for tenant-scoped writes unless the caller is an audited admin process that intentionally operates across tenants and additional controls are in place.
- **server/memory-graph.ts:194** — INSERT INTO memory_links does not set tenant_id. memory_links is a tenant-scoped table, so inserting without tenant_id will create unscoped links (cross-tenant isolation violation).
  - _Fix:_ Pass tenant_id explicitly in the INSERT values (e.g. VALUES (..., ${tenantId}, ...)) or use a transaction bound to the tenant and an INSERT that explicitly sets tenant_id. Every INSERT into tenant-scoped tables must set tenant_id.
- **server/plan-executor.ts:116** — appendExecutionLog updates plans row by id without constraining tenant_id (UPDATE plans ... WHERE id = ${planId}). This writes to a tenant-scoped table without scoping to the plan's tenant.
  - _Fix:_ Require and pass the plan's tenant_id into appendExecutionLog and add it to the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Validate the tenant matches the execution context before updating to prevent cross-tenant modifications.
- **server/plan-executor.ts:513** — CAS claim that flips plan to 'executing' does not constrain by tenant (UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status = 'approved'). A caller-supplied planId could cause a caller to claim/modify another tenant's plan.
  - _Fix:_ Include tenant_id in the CAS WHERE clause (e.g. AND tenant_id = ${callerTenantId}) or first SELECT/VERIFY the plan belongs to the expected tenant and only then perform the CAS update. Never rely solely on id without tenant scoping for tenant-scoped tables.
- **server/plan-executor.ts:527** — Plain UPDATE marking a plan failed does not include a tenant constraint (UPDATE plans SET status = 'failed' WHERE id = ${planId}). This allows writes to plans across tenants by id only.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or otherwise ensure the code has verified the plan's tenant before performing the update.
- **server/plan-executor.ts:725** — Final status update of the plan (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint; execution_log appends similarly without tenant scoping.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause for the UPDATE, and ensure the execution_log append uses a tenant-constrained update. Derive tenantId from the claimed row (returned by the CAS) and thread it to the update.
- **server/plan-executor.ts:759** — Error-path UPDATE resetting plan to 'failed' (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') is missing tenant scoping.
  - _Fix:_ Constrain this UPDATE with tenant_id = ${tenantId} or verify plan ownership before updating to avoid cross-tenant state changes.
- **server/plan-executor.ts:775** — resumeStuckPlans performs a global recovery UPDATE that flips all stale 'executing' plans back to 'approved' without any tenant constraint (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...). This writes across all tenants.
  - _Fix:_ Either restrict boot-time recovery to admin-only context explicitly (and document/authorize it), or iterate per-tenant and include tenant_id in the WHERE clause. At minimum, add tenant scoping or require an explicit ADMIN_TENANT_ID filter and strong admin authorization for this operation.
- **server/plan-executor.ts:784** — resumeStuckPlans selects pending approved plans across all tenants (SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50) without tenant scoping — the subsequent executePlan calls will run across tenants.
  - _Fix:_ Query and process approved plans per-tenant (e.g. WHERE tenant_id = ${tenantId}) or explicitly restrict this scheduler to an administrative tenant context. Ensure downstream executePlan uses the plan's tenant and enforces tenant-scoped updates as described above.
- **server/proactive-engine.ts:138** — SELECT from proactive_actions by id omits tenant scoping — `SELECT persona_id FROM proactive_actions WHERE id = ${actionId}` can leak cross-tenant data / return rows not owned by caller.
  - _Fix:_ Include tenant filtering or verify ownership before selecting: e.g. `SELECT persona_id FROM proactive_actions WHERE id = ${actionId} AND tenant_id = ${tenantId}` or first assert the row belongs to tenantId before using its values.
- **server/process-governor.ts:199** — Query against heartbeat_logs has no tenant filter: `SELECT AVG(duration_ms) as avg_ms FROM heartbeat_logs ...` will aggregate across all tenants.
  - _Fix:_ Scope the query by tenant, e.g. `SELECT AVG(duration_ms) as avg_ms FROM heartbeat_logs WHERE tenant_id = ${tenantId} AND ...` or otherwise ensure tenant ownership is enforced.
- **server/process-governor.ts:239** — Query counting distinct failing persona_ids from heartbeat_logs lacks tenant constraint: `SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ...` can include other tenants' logs.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. `... FROM heartbeat_logs WHERE tenant_id = ${tenantId} AND ...`.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks uses only id in WHERE (`UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id}`) without tenant scoping — risk of modifying another tenant's row if ids overlap or attacker supplies crafted state.
  - _Fix:_ Constrain updates by tenant_id as well: `UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId}` (or validate ownership before issuing the UPDATE).
- **server/process-governor.ts:641** — UPDATE agent_desks uses only id in WHERE (`UPDATE agent_desks SET ... WHERE id = ${desk.id}`) with no tenant filter.
  - _Fix:_ Include tenant scoping: `UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId}` or verify the desk belongs to the tenant before updating.
- **server/process-governor.ts:977** — UPDATE governance_rules increments trigger_count without tenant constraint (`UPDATE governance_rules SET ... WHERE id = ${rule.id}`) allowing cross-tenant mutation if id is supplied from an unscoped source.
  - _Fix:_ Constrain the UPDATE to the tenant: `WHERE id = ${rule.id} AND tenant_id = ${tenantId}` or validate ownership prior to mutating.
- **server/project-brain.ts:135** — SELECT project by id omits tenant scoping: `SELECT name, status, description FROM projects WHERE id = ${projectId}` — function accepts only projectId and never verifies tenant ownership, enabling cross-tenant data access.
  - _Fix:_ Require and pass a tenantId into updateProjectBrain and scope the query: `WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Alternatively, first verify the project's tenant_id equals the caller's tenant before proceeding.
- **server/project-brain.ts:332** — Backfill query selects projects and conversations across all tenants: `SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0` has no tenant filter.
  - _Fix:_ Filter by tenant: add `WHERE p.tenant_id = ${tenantId}` (and ensure conversations/messages are scoped similarly), and require a tenantId parameter for backfillProjectBrains to avoid cross-tenant enumeration.
- **server/project-brain.ts:349** — Selecting conversations by project id omits tenant scoping: `SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}` — if projects are not tenant-checked earlier, this can traverse other tenants' data.
  - _Fix:_ Include tenant constraint (e.g. `AND c.tenant_id = ${tenantId}` or only select conversations for projects validated to belong to the tenant) and require tenantId context for the backfill flow.
- **server/project-brain.ts:359** — Selecting messages by conversation id lacks tenant scoping: `SELECT role, content FROM messages WHERE conversation_id = ${conv.id}` — allows reading messages from other tenants when run unscoped.
  - _Fix:_ Add tenant filter (e.g. `AND tenant_id = ${tenantId}`) or ensure conversation ownership is verified before selecting messages.
- **server/proposal-verifier.ts:33** — SELECT from code_proposals by id omits tenant scoping: `SELECT ... FROM code_proposals WHERE id = ${proposalId}` — verifyProposalById takes only proposalId and does not validate tenant ownership, enabling cross-tenant reads.
  - _Fix:_ Require caller tenantId (or resolve the proposal's tenant and validate against caller) and/or include `AND tenant_id = ${tenantId}` in the WHERE clause before reading proposal contents.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals used to clear rebased_at is not scoped by tenant: `UPDATE code_proposals ... WHERE id = ${proposalId}` — allows mutating proposals from other tenants.
  - _Fix:_ Add tenant constraint: `WHERE id = ${proposalId} AND tenant_id = ${tenantId}` or verify ownership prior to performing the UPDATE. Ensure the function has tenant context.
- **server/proposal-verifier.ts:246** — persist() updates code_proposals verification fields without tenant scoping: `UPDATE code_proposals SET ... WHERE id = ${proposalId}` — this can modify another tenant's proposal record.
  - _Fix:_ Include tenant filtering in the UPDATE, e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`, and require persist/verify callers pass/validate tenantId before calling.
- **server/proposal-verifier.ts:372** — Rebase path updates code_proposals status without tenant scoping: `UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId}` — no tenant constraint present.
  - _Fix:_ Scope the UPDATE to the tenant (`AND tenant_id = ${tenantId}`) or validate proposal ownership before mutating. Ensure attemptRebaseOnDrift receives tenant context or verifies ownership prior to updates.
- **server/proposal-verifier.ts:396** — When LLM hallucination detected, code updates validation_result on code_proposals without tenant scoping: `UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId}`.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` or otherwise verify the proposal belongs to the caller's tenant before updating the record.
- **server/proposal-verifier.ts:408** — Persisting rebased code_diff writes to code_proposals with `WHERE id = ${proposalId}` but no tenant filter: `UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId}`.
  - _Fix:_ Constrain the UPDATE to the tenant: `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`, and require attemptRebaseOnDrift to accept/validate tenantId to prevent cross-tenant mutation.
- **server/recurring-messages.ts:300** — UPDATE to tenant-scoped table agent_knowledge performed without a tenant_id constraint (UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}). This allows updating a row by id without ensuring it belongs to the intended tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause to prevent cross-tenant writes, e.g. WHEN updating rows discovered from a global read use WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. Prefer passing a tenantId into the function and using that explicit value for the WHERE clause.
- **server/reference-learner.ts:290** — UPDATE to tenant-scoped table memory_entries performed without a tenant_id/persona_id constraint (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This updates by id only and can modify another tenant's row.
  - _Fix:_ Ensure the UPDATE includes tenant-scoping (and persona scoping if applicable), for example: WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}. Always validate ownership before updating rows that are tenant-scoped.
- **server/routes.ts:4324** — SELECT from tenant-scoped table 'conversations' is performed without constraining by tenant_id. The code runs: SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL — there is no tenant_id filter, so it can leak or return data from another tenant.
  - _Fix:_ Require and pass the caller's tenantId into the query (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL), or verify ownership first (select the conversation row scoped to tenant_id and reject if not owned). Always validate that the referenced conversation belongs to the caller's tenant before using its project_id.
- **server/routes.ts:4330** — INSERT into tenant-scoped table 'project_notes' omits tenant_id. The code inserts: INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${snapshot.slice(0, 5000)}, ${'system:context-guard'}) — tenant_id is not provided, and the table requires tenant_id (no DB-level RLS/default).
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})). Also ensure projId was resolved from a conversation owned by the caller's tenant (see fix for the SELECT) before inserting.
- **server/sculptor.ts:94** — UPDATE sculptor_sessions executes without a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id}). This allows an attacker or bug that supplies/guesses an id to mutate a session belonging to another tenant.
  - _Fix:_ Always include tenant scoping in UPDATE/DELETE queries on tenant-scoped tables. e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} (or use the session.tenantId from the inserted row). Ensure the tenantId used comes from the authenticated/trusted caller context, not untrusted input.
- **server/sculptor.ts:101** — UPDATE sculptor_sessions sets conversation_id without a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). Missing tenant filter can allow cross-tenant mutation if an id is misused.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the session belongs to the caller's tenant before updating. Prefer using a parameterized, trusted tenantId.
- **server/sculptor.ts:162** — UPDATE sculptor_sessions (monitorSession) updates status/result/completed_at with WHERE id = ${sessionId} but no tenant_id constraint. This permits changing another tenant's session record if sessionId is guessed or leaked.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause and ensure tenantId is the trusted caller tenant. Alternatively, SELECT the session by id+tenant first and use its id/tenant when updating to guarantee tenant-bound writes.
- **server/sculptor.ts:347** — UPDATE sculptor_sessions sets review_result WHERE id = ${sessionId} with no tenant_id constraint. This write can modify another tenant's session review result if sessionId is attacker-controlled or mis-bound.
  - _Fix:_ Constrain the UPDATE by tenant: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Verify tenantId from the authenticated context and never derive it from client-provided session data without ownership checks.
- **server/second-opinion.ts:407** — settleFusionReservation updates agent_cost_ledger by id (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}) without verifying tenant ownership (no tenant_id in WHERE). This can allow tampering with another tenant's cost ledger row by reservationId.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}. Since settleFusionReservation currently lacks tenantId, change its signature to accept a tenantId (or look up+verify the reservation row's tenant before updating) so you never update ledger rows across tenants.
- **server/second-opinion.ts:426** — releaseFusionReservation updates agent_cost_ledger (SET cost_usd = '0', operation = ...) WHERE id = ${reservationId} with no tenant_id constraint, allowing a caller to clear/release reservations belonging to other tenants by id.
  - _Fix:_ Require and use tenantId when releasing reservations and include AND tenant_id = ${tenantId} in the WHERE clause. Alternatively, perform a verified SELECT by id+tenant before doing a mutation to ensure the reservation belongs to the caller's tenant.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is inserting into a tenant-scoped join table without setting tenant_id. The table likely requires tenant_id (NOT NULL) and this will create tenant-ambiguous/incorrect rows.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id})) and ensure tenantId is derived from the owning project/conversation and validated to prevent cross-tenant linkage.
- **server/seed.ts:4033** — INSERTs into tenant-scoped table `heartbeat_tasks` are performed (batch insert of default heartbeat tasks) without setting tenant_id. The inserted objects lack tenant_id, so rows may land in the DB with NULL/default tenant or leak into other tenants.
  - _Fix:_ Always include tenant_id in INSERTs to tenant-scoped tables (e.g. set tenant_id: tenantId or include it in the SQL VALUES). If seeding for system/tenant 1, explicitly set tenant_id = 1. Prefer per-tenant seeding paths that accept a tenantId parameter.
- **server/seed.ts:4100** — INSERT into tenant-scoped table `heartbeat_tasks` (Model Scout) is done without tenant_id. The insert object does not include tenant_id, so rows will not be tenant-scoped as required.
  - _Fix:_ Include tenant_id in the inserted object (e.g. tenant_id: tenantId or tenantId: tenantId depending on ORM mapping). Ensure every INSERT into tenant-scoped tables passes an explicit, validated tenant id.
- **server/seed.ts:4159** — INSERT into tenant-scoped table `heartbeat_tasks` (Daily Cloud Backup) is performed without tenant_id. Multiple subsequent heartbeat_tasks inserts in this block likewise omit tenant scoping.
  - _Fix:_ Add tenant_id to each inserted heartbeat task record (or use a helper that attaches the tenant id). Do not rely on defaults — tenant_id must be explicitly provided and validated for every INSERT into tenant-scoped tables.
- **server/seed.ts:4419** — DELETE FROM heartbeat_tasks WHERE enabled = false is executed without a tenant filter. This will delete disabled heartbeat tasks across all tenants instead of being scoped to a single tenant.
  - _Fix:_ Add a tenant constraint to the DELETE (e.g. DELETE ... WHERE enabled = false AND tenant_id = ${tenantId}). If the operation must be global, ensure it is explicitly authorized and documented; otherwise run per-tenant deletes only.
- **server/self-improvement.ts:394** — UPDATE on experiments uses WHERE eq(experiments.id, experiment.id) without also constraining by tenant_id. This allows updating another tenant's experiment row by id.
  - _Fix:_ Include tenant ownership in the UPDATE WHERE clause, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Alternatively, SELECT the row by id+tenant before updating and fail if tenant mismatch.
- **server/self-improvement.ts:413** — Error-path UPDATE on experiments (in catch) updates status/outcome WHERE id = experiment.id with no tenant constraint — same cross-tenant modification risk as above.
  - _Fix:_ Restrict the UPDATE to the owning tenant like .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Ensure experiment.id is only updated when the row belongs to the caller's tenant.
- **server/self-reflection.ts:366** — storeLesson() issues an UPDATE memory_entries ... WHERE id = <existing id> without constraining by tenant_id. This allows modifying another tenant's memory entry by ID.
  - _Fix:_ Include tenant check in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or fetch+verify tenant before updating. Ensure every UPDATE/DELETE on tenant-scoped tables verifies tenant_id.
- **server/skillify.ts:196** — SELECT from tenant-scoped table `conversations` is executed without scoping to the caller's tenant (no tenant_id constraint in WHERE).
  - _Fix:_ Fail-closed: include the tenant filter in the query. e.g. use a combined where predicate (eq(conversations.id, conversationId) AND eq(conversations.tenantId, tenantId)) or use the tenant-scope helpers (requireTenantScope/tenantScope) so the DB SELECT can only return rows for the caller's tenant.
- **server/skillify.ts:203** — SELECT from tenant-scoped table `messages` is executed using only conversationId (WHERE messages.conversationId = ... ) with NO tenant_id constraint.
  - _Fix:_ Add explicit tenant scoping to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or otherwise validate ownership at the SQL level using tenantScope/requireTenantScope. Every read on a tenant-scoped table must include tenant_id in the WHERE clause.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` in distillIntent() (recentMsgs) uses only conversationId in WHERE and lacks tenant_id constraint.
  - _Fix:_ Scope the query to the calling tenant as well (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or fetch messages via a helper that enforces tenant ownership. All reads of tenant-scoped tables must include tenant_id in the SQL predicate.
- **server/skillify.ts:331** — SELECT from tenant-scoped table `messages` in skillifyConversation() (recentMsgs) uses only conversationId and orders by date — no tenant_id constraint in the SQL.
  - _Fix:_ Add explicit tenant scoping to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise validate ownership at the SQL level before returning rows. Every tenant-scoped read/write must include tenant_id filtering.
- **server/storage.ts:832** — archiveExpiredMemories() executes a raw UPDATE memory_entries SET status='archived' WHERE ... with no tenant_id filter — mass-updates all tenants' memory_entries.
  - _Fix:_ Run archival per-tenant (require tenantId parameter) or iterate tenants and scope the UPDATE to each tenant (e.g. WHERE tenant_id = ${tenantId} AND ...). Do not run global UPDATEs against tenant-scoped tables without explicit admin/ops authorization and audit.
- **server/storage.ts:849** — archiveStaleMemories() performs a raw UPDATE on memory_entries to archive rows older than a cutoff with no tenant_id constraint (global write across tenants).
  - _Fix:_ Scope archival updates to a tenant (require tenantId or loop tenants) or otherwise ensure the operation runs with explicit admin intent and appropriate tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}).
- **server/storage.ts:1279** — getAllDataForExport() performs many SELECTs across tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, dailyNotes, etc.) with no tenant filter — this exports data for all tenants.
  - _Fix:_ Restrict this function to privileged admin use only and require an explicit tenantId if exporting tenant data. Alternatively, make it explicitly an admin export that is gated/authenticated and logged; if not intended, add tenant-scoping WHERE clauses to each SELECT.
- **server/subagents.ts:248** — _spawnSubagentImpl() calls storage.getConversation(parentConversationId, params.tenantId ?? ADMIN_TENANT_ID) — defaulting to ADMIN_TENANT_ID when tenantId is missing can cause the parent conversation lookup to run under the admin tenant, potentially bypassing ownership checks.
  - _Fix:_ Do not default to ADMIN_TENANT_ID. Require the caller to supply tenantId (or derive it from a trusted session/context) and assert ownership before spawning; use assertValidTenantId and pass the real tenantId to storage.getConversation to prevent escalation to admin tenant.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. Query: db.select().from(customTools).where(eq(customTools.name, parsed.name))
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))) so reads are scoped to the caller's tenant.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without constraining tenant_id. Query: db.select().from(customTools).where(eq(customTools.id, inserted.id))
  - _Fix:_ Add tenant scoping to the lookup (e.g. and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))) to ensure the read is limited to the intended tenant.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` uses only PK filter (eq(customTools.id, tool.id)) with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantIdOrToolTenant))) ), or derive/validate the tenantId from the tool row and include eq(customTools.tenantId, <tenantId>).
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` uses only PK filter (eq(customTools.id, tool.id)) with no tenant_id constraint.
  - _Fix:_ Constrain the DELETE by tenant (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) so only the intended tenant's row can be removed.
- **server/tool-learning.ts:365** — UPDATE increments usageCount on tenant-scoped `custom_tools` using only eq(customTools.id, tool.id) with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) to prevent cross-tenant mutation of usage counters.
- **server/tools.ts:2978** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The row is created without explicitly setting tenant scope, allowing cross-tenant data contamination or orphaned rows.
  - _Fix:_ Include tenant_id in the INSERT columns/values (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenantId is validated earlier (use the caller's params._tenantId) and passed as a bound parameter rather than injected into raw SQL.
- **server/tools.ts:2992** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The note row is created without tenant scoping, risking cross-tenant writes.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenantId is the caller's verified tenant (params._tenantId) and use parameter binding to set it.
- **server/tools.ts:2874** — INSERT into tenant-scoped table `project_conversations` (create path) does not include tenant_id. The project_conversations row is inserted without an explicit tenant_id column/value.
  - _Fix:_ When inserting into project_conversations, include tenant_id (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ON CONFLICT DO NOTHING). Ensure tenantId is validated and comes from the caller context (params._tenantId).
- **server/tools.ts:3016** — INSERT into tenant-scoped table `project_conversations` (link_conversation path) does not include tenant_id. The link is created without explicitly recording tenant ownership.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Confirm project and conversation ownership first (as currently done) and bind tenantId from params._tenantId to the query.
- **server/tools.ts:4507** — INSERT into tenant-scoped table `project_notes` omits tenant_id column entirely. Per the invariant every INSERT into tenant-scoped tables must explicitly set tenant_id (no DB defaults / RLS).
  - _Fix:_ Include tenant_id in the INSERT values using the caller's tenant (params._tenantId) and validate ownership before insert. E.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Alternatively, use a data-layer helper that automatically injects and parameterizes tenant_id.
- **server/tools.ts:4511** — INSERT into tenant-scoped table `project_files` omits tenant_id column and thus does not record the tenant in the row. This violates the requirement that every INSERT set tenant_id explicitly.
  - _Fix:_ Add tenant_id to the inserted columns and set it from the verified caller tenant (params._tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_type, ...) VALUES (${params._tenantId}, ${projectId}, ...). Ensure assertProjectInTenant ran before the insert and keep the SQL parameterized.
- **server/tools.ts:6359** — INSERT into tenant-scoped table `project_files` (from generate_audio) omits tenant_id. The write uses params.project_id but does not include tenant_id in the inserted row.
  - _Fix:_ Include tenant_id = ${params._tenantId} in the INSERT (and ensure assertProjectInTenant validated ownership first). Use parameterized SQL or Drizzle insert helpers to set the tenant_id column explicitly.
- **server/tools.ts:6892** — INSERT into tenant-scoped table `project_files` (from create_slideshow_video) omits tenant_id in the VALUES. Every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT columns and values (tenant_id = ${params._tenantId}) and keep the insert parameterized. Prefer using a shared data-layer helper that enforces tenant_id on all writes to tenant-scoped tables.

### HIGH (168)

- **server/ab-optimizer.ts:110** — SELECT from `experiments` performed without restricting to a tenant (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. add AND tenant_id = ${tenantId}) or change the design to iterate per-tenant (pass tenantId into runDueAbExperiments or run a per-tenant loop). Avoid global scans of tenant-scoped tables unless the function is explicitly an admin operation and enforces proper authorization.
- **server/agent-activity.ts:122** — UPDATE on `agentActivity` uses WHERE eq(agentActivity.id, activityId) with no tenant constraint. This allows updating activity rows belonging to other tenants by id.
  - _Fix:_ Include the tenant in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, callerTenantId)))) so updates are always limited to the caller's tenant.
- **server/agentic-engines.ts:114** — SELECT id FROM `plans` used as an idempotency check omits tenant filtering (WHERE source = ... AND source_ref = ...). `plans` is tenant-scoped; this may return a plan for another tenant, breaking idempotency and routing semantics.
  - _Fix:_ Include tenant_id = ${insight.tenantId} (or pass tenantId explicitly) in the WHERE clause so the idempotency check only considers plans belonging to the same tenant.
- **server/agentic-engines.ts:148** — UPDATE ai_insights SET action_taken = ... WHERE id = ${insight.insightId} omits tenant constraint. This updates a tenant-scoped row by id without verifying tenant ownership.
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId}) to ensure only the intended tenant's row is modified.
- **server/agentic-engines.ts:160** — retryPendingMinervaRouting performs a SELECT on `ai_insights` for status='applied' AND priority='high' AND action_taken = ... without a tenant_id filter. This reads tenant-scoped rows across all tenants.
  - _Fix:_ Either make this function accept an explicit tenantId and filter by tenant_id, or ensure it is only run with elevated/admin rights and keep an explicit tenant_id predicate in the query. Do not scan tenant-scoped tables across tenants in regular background code.
- **server/agentic-features.ts:599** — enrollInSequence checks for an existing enrollment with SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' without constraining tenant_id. That allows enrollment-duplication logic to cross tenants.
  - _Fix:_ Include tenant filtering in the query (e.g. AND tenant_id = ${params.tenantId}) before deciding 'Contact already enrolled'. Always scope lookups/exists checks against tenant-scoped tables to the caller's tenant.
- **server/agentic/department-budgets.ts:143** — Query scans the tenant-scoped table `department_budgets` without a tenant_id constraint (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads across tenants and violates the app-only isolation invariant.
  - _Fix:_ If this function must only operate for one tenant, add a tenantId parameter and include WHERE tenant_id = ${tenantId}. If it is intended as a global/admin sweep, make that explicit: run only under a validated admin context (no implicit caller tenant), document/label it as an admin-only job, and enforce admin-only authorization. Avoid unbounded cross-tenant SELECTs from tenant-scoped tables unless the caller is an audited admin process; prefer parameterized tenant filters (e.g. WHERE tenant_id = ${tid}).
- **server/agentic/harness-adaptation.ts:71** — Query against `agent_trace_spans` omits any tenant filter (SELECT ... FROM agent_trace_spans WHERE status IN (...) AND metadata->>'modelId' IS NOT NULL ...). This reads failure traces across all tenants and can leak cross-tenant data.
  - _Fix:_ Either add an explicit tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}) or require this nightly miner to run as an explicitly-authorized admin process (use ADMIN_TENANT_ID and a documented admin-only execution path). If the intent is per-tenant mining, accept a tenantId param and parameterize the query. Ensure any global sweep is audited and runs only with admin privileges.
- **server/agentic/runs.ts:98** — withRun() creates a run (correctly sets tenantId) but then calls appendStep/completeRun/failRun which themselves do NOT enforce tenant scoping — the combined call path lets in-process logic mutate run rows without validated tenant checks.
  - _Fix:_ Either change appendStep/completeRun/failRun/etc to accept tenantId and enforce WHERE tenant_id = tenantId, or have withRun perform explicit ownership-scoped updates (e.g. use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, params.tenantId))). Audit all call sites that invoke the run-update helpers and update them to pass and enforce tenantId.
- **server/agentic/wake-scheduler.ts:79** — UPDATE on tenant-scoped table agent_wake_schedules performed without any tenant_id constraint (claims pending wakes across all tenants in a single statement).
  - _Fix:_ Avoid a cross-tenant UPDATE. Either iterate distinct tenant_id values and claim wakes per-tenant, or add an explicit tenant_id filter to the UPDATE/subselect so only rows for a specific tenant are modified. Example fixes: (a) SELECT DISTINCT tenant_id FROM agent_wake_schedules WHERE status='pending'... then run the UPDATE/claim per tenant, or (b) include AND tenant_id = ${tenantId} in the inner SELECT and outer UPDATE when processing a particular tenant.
- **server/auth.ts:180** — Lookup of apiKeys by keyHash queries the apiKeys table without scoping by tenant_id. This selects across all tenants to find a matching API key.
  - _Fix:_ Scope api key lookup so it cannot read across tenants absent explicit admin intent. If callers must authenticate by global key, ensure keys are uniquely namespaced or encoded with tenant context. Prefer: include tenant identification (if available) in the lookup (WHERE tenant_id = ${tenantId} AND keyHash = ...), or otherwise validate the key in a way that does not expose or act on rows from other tenants. If global lookup is necessary, ensure the code path is audited and strongly isolated and that subsequent operations are executed only after explicit tenant resolution and checks.
- **server/auto-asset-capture.ts:207** — Selecting messages by conversation_id without tenant_id (SELECT content FROM messages WHERE conversation_id = ${c.id} ...) — the query reads messages across tenants by conversation id with no tenant-scoped WHERE clause.
  - _Fix:_ Before reading messages, verify ownership of the conversation id (assert conversation belongs to the tenant) or include tenant_id in the WHERE clause (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}). If this is an admin backfill that must scan all tenants, iterate per-tenant and include tenant_id in inner queries to preserve explicit application-level scoping.
- **server/auto-memorize.ts:159** — The main auto-memorize batch SELECT reads messages (and joins conversations) across all tenants (WHERE m.created_at > ... AND m.created_at <= ... AND m.role IN ('user','assistant')) with no tenant_id constraint. This pulls user/assistant messages from multiple tenants without explicit per-tenant scoping.
  - _Fix:_ If this is intended to be a global/admin cross-tenant analytics job, make that explicit and ensure it runs only under a trusted admin context. Otherwise, change the pattern to process a single tenant at a time: e.g. enumerate tenant_ids, then run the SELECT with WHERE m.created_at > ... AND m.created_at <= ... AND m.role IN (...) AND m.tenant_id = ${tenantId} (or join on conversations and include c.tenant_id = ${tenantId}). Ensure all downstream writes are attributed to the correct tenant and that any cross-tenant reads are removed or guarded by explicit admin intent.
- **server/auto-transcript.ts:128** — SELECT across the conversations table with no tenant_id constraint (backshell/backfill query). This reads conversation rows for all tenants instead of scoping to a single tenant.
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE c.tenant_id = ${tenantId}) or iterate tenants explicitly with proper authorization. Ensure any downstream work uses the verified tenant context rather than trusting tenant_id values returned from an unscoped scan.
- **server/auto-transcript.ts:153** — SELECT joining project_conversations/conversations/projects runs across tenants with no top-level tenant constraint. The query enumerates conversation/project links for all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE c.tenant_id = ${tenantId}) or perform this backfill per-tenant with an authorized tenantId. Avoid wide cross-tenant scans unless performed by a privileged admin job that enforces per-tenant isolation and audit logging.
- **server/auto-transcript.ts:171** — SELECT c.id, c.project_id, c.tenant_id FROM conversations c ... WHERE c.project_id IS NOT NULL AND NOT EXISTS (...) — this selects conversation rows across all tenants (no tenant_id filter) for backfill.
  - _Fix:_ Constrain the SELECT to the intended tenant (WHERE c.tenant_id = ${tenantId}) or perform the backfill on a per-tenant basis with explicit authorization checks before touching tenant-scoped tables. Ensure subsequent INSERTs/updates use the same verified tenant context.
- **server/business-tools.ts:84** — INSERT into `invoice_items` does not set tenant_id. If `invoice_items` is a tenant-scoped table (columns are NOT NULL with no default), this will either fail or create a row not explicitly tied to the caller's tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. VALUES (${tid}, ...)) or derive and validate tenant ownership from the parent invoice before inserting. Prefer passing ${tid} explicitly to invoice_items to enforce tenant scoping.
- **server/business-tools.ts:334** — SELECT in listContracts LEFT JOINs customers (cust) with ON cust.id = c.customer_id but does NOT constrain the joined `customers` row by tenant_id. This reads a tenant-scoped table (`customers`) without a tenant_id filter on that table.
  - _Fix:_ Constrain the joined customers row to the contract's tenant (e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}) or include WHERE cust.tenant_id = c.tenant_id. Never select tenant-scoped rows without an explicit tenant_id constraint.
- **server/capability-review.ts:490** — UPDATE capability_reviews sets reused/reused_capability WHERE id = ${pending.reviewId} but does not include tenant_id in the WHERE clause. This writes a tenant-scoped table row by id only, without ensuring the caller's tenant owns that row.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${pending.reviewId} AND tenant_id = ${ctxTenantId} AND reused = false). Ensure markReviewReused receives/uses the tenant context (or validates the review's tenant) before updating capability_reviews.
- **server/ceo-orchestrator.ts:821** — Unconstrained read of personas table: `await db.select().from(personasTable)` fetches all personas with NO tenant_id filter, so cachedPersonas contains rows for every tenant.
  - _Fix:_ Restrict the query to the current tenant (e.g. .where(eq(personasTable.tenantId, plan.tenantId)) or otherwise pass a tenantId to the lookup). Avoid caching a global personas list that mixes tenants; cache per-tenant results only.
- **server/ceo-orchestrator.ts:1153** — Using a persona id derived from a global cachedPersonas list when creating conversations: `storage.createConversation({... personaId: targetPersona?.id ..., tenantId: plan.tenantId})` — the code uses a persona id that may belong to another tenant without validating ownership.
  - _Fix:_ Before using a persona id provided from a shared cache, verify the persona row belongs to plan.tenantId (or re-query personas with a tenant filter). Do not accept or reuse persona ids from other tenants when creating tenant-scoped resources.
- **server/channel-routing.ts:31** — SELECT joins personas without tenant filtering: the query joins `personas p ON p.id = cr.persona_id` and returns p.name with NO WHERE p.tenant_id = <tenant>. If personas is tenant-scoped this leaks cross-tenant data.
  - _Fix:_ Make channel routing tenant-aware (or explicitly document it as global). If personas are tenant-scoped, filter the join by the caller's tenant_id (or store channel_routes per-tenant). e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = <callerTenantId> or add tenant_id to channel_routes and filter accordingly.
- **server/channels/kernel.ts:176** — dispatchInbound accepts a conversationId via the adapter callback `resolveConversation(turn)` and then calls processMessage(conversationId, ...) without verifying that the conversationId belongs to turn.tenantId. This uses a foreign conversation id from input without tenant-ownership validation.
  - _Fix:_ Either make resolveConversation return a tenant-validated conversationId (and document that contract) or have dispatchInbound explicitly verify the conversation's tenant (e.g. SELECT conversation WHERE id = conversationId AND tenant_id = turn.tenantId) before calling processMessage. Always ensure conversation ids are validated against the caller's tenant.
- **server/chat-engine.ts:632** — SELECT from compaction_archives executed with only conversation_id filter (no tenant_id). This reads tenant-scoped compaction_archives rows without an explicit tenant_id constraint.
  - _Fix:_ Constrain the query by tenant (e.g. AND tenant_id = ${tenantId}) or JOIN through a parent row already verified to belong to the tenant. Prefer explicit WHERE tenant_id = ${tenantId} to ensure application-level tenant isolation is enforced.
- **server/chat-engine.ts:651** — SELECT from messages executed with only conversation_id filter (no tenant_id). This reads messages (a tenant-scoped table) without an explicit tenant_id constraint.
  - _Fix:_ Add an explicit tenant constraint (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or otherwise ensure the conversation ownership check is enforced immediately prior to this read. Prefer explicit WHERE eq(messages.tenantId, tenantId) for clarity and defense-in-depth.
- **server/chat-engine.ts:4583** — INSERT into tenant-scoped memory data via storage.createMemoryEntry(...) is performed without supplying a tenant identifier. The created memory row is not explicitly scoped to the caller's tenant_id, violating the invariant that every INSERT into tenant-scoped tables must include tenant_id (or equivalent validation).
  - _Fix:_ Pass the caller's tenantId into storage.createMemoryEntry (e.g. include tenantId in the payload) or change storage.createMemoryEntry to require tenantId. Additionally, validate that the personaId (if provided) belongs to the tenant before creating the memory. If the storage layer cannot be changed, add an explicit ownership check (lookup persona/tenant mapping) before inserting.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` omits tenant_id in WHERE clause: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This can mutate rows across tenants if id is known.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise validate row ownership before updating). Prefer using the same tenant-scoped guard used elsewhere in this module: `WHERE id = ${row.id} AND tenant_id = ${tenantId}` so rollbacks/unclaims cannot affect other tenants.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant_id constraint. scanAndEscalate selects overdue commitments across all tenants: `SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`.
  - _Fix:_ Scope the scan to a specific tenant_id or explicitly require/admin-authenticate this operation. If the intent is a global admin digest, make that explicit and ensure the downstream handling redacts tenant content and that only admin principals can invoke it. Otherwise iterate per-tenant and add `WHERE tenant_id = ${tenantId}` to the query.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}`. This can update rows across tenants.
  - _Fix:_ Constrain updates with the tenant id: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}` (and validate tenantId earlier). Ensure every UPDATE/DELETE/SELECT on credential_vault enforces tenant scoping.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` called from getLoginCredentials.
  - _Fix:_ As above, include tenant scoping: `WHERE id = ${cred.id} AND tenant_id = ${tenantId}`. Always verify the tenant owns the credential before updating metadata to prevent cross-tenant mutation.
- **server/data-protection.ts:144** — SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} ... is executed without any tenant validation or tenant_id predicate. The function verifyMessageSaved accepts conversationId but does not accept or verify tenant ownership, enabling a caller who supplies another tenant's conversation id to read/count messages.
  - _Fix:_ Require and pass tenantId into verifyMessageSaved, or first verify that the conversation belongs to the caller's tenant (SELECT ... FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId}) before querying messages. Then include AND tenant_id = ${tenantId} on the messages query (or otherwise scope via a previously-verified conversation row).
- **server/delivery-pipeline.ts:683** — SELECT on `delivery_logs` by stripePaymentId is executed with no tenant filter (.where(eq(deliveryLogs.stripePaymentId, req.stripePaymentId))). This can return another tenant's delivery record.
  - _Fix:_ Add a tenant filter to the query (e.g. .where(and(eq(deliveryLogs.stripePaymentId, req.stripePaymentId), eq(deliveryLogs.tenantId, req.tenantId)))) or require/propagate tenant context for idempotency checks. If this is an admin/global query, ensure it's only callable in trusted admin context and document the intent.
- **server/delivery-pipeline.ts:720** — UPDATE on `delivery_logs` (clearing stripePaymentId / updating metadata) uses .where(eq(deliveryLogs.id, prior.id)) with no tenant_id constraint, allowing modification of another tenant's row.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ...), or perform the update via a transaction that first verifies the row belongs to the expected tenant before mutation.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment() SELECTs from `delivery_logs` WHERE stripePaymentId = paymentId with no tenant filter — returns delivery rows across tenants.
  - _Fix:_ Accept a tenantId or otherwise restrict the lookup: WHERE stripe_payment_id = $1 AND tenant_id = $2, or ensure this function is only used in trusted admin contexts with appropriate access controls.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} is executed without constraining by tenant_id. Although the row.id was obtained from a tenant-scoped SELECT earlier, the UPDATE lacks explicit tenant verification and could update the wrong tenant's row in some race scenarios.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the UPDATE in the same transaction/statement that discovered the id to ensure the tenant constraint is enforced server-side.
- **server/dream-consolidation.ts:83** — When assembling recent session summaries the code SELECTs messages: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ... without also constraining messages by tenant_id. This reads message rows from the tenant-scoped `messages` table without an explicit tenant filter.
  - _Fix:_ Add tenant scoping to the messages query, e.g. AND tenant_id = ${tenantId}, or otherwise verify the conversation->tenant ownership before reading messages. Always include tenant_id in WHERE for tenant-scoped tables.
- **server/email.ts:366** — In inbox polling code the query SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without tenant_id. `inbox_messages` is tenant-scoped, so this read can return rows from other tenants.
  - _Fix:_ Include tenant scoping in the lookup (AND tenant_id = ${tenantId}) or use the inboxId (and/or derived tenantId) as part of the WHERE so lookups are limited to the intended tenant/inbox. Ensure the polling code can determine the appropriate tenant context for each message before touching the DB.
- **server/email.ts:410** — backfillEmptyBodies() performs SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id with NO tenant filter, iterating over all tenants' rows.
  - _Fix:_ Run backfill per-tenant (add tenant_id filter) or otherwise restrict this maintenance job to a controlled admin context. Iterating all rows across tenants risks exposing or modifying other tenants' data.
- **server/email.ts:424** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} updates a tenant-scoped table row by id without verifying tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or perform the SELECT and UPDATE in a transaction that enforces the tenant ownership. Ensure all UPDATE/DELETE statements on tenant-scoped tables include tenant_id in WHERE.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings reads rows without any tenant_id constraint: SELECT id, title, content FROM agent_knowledge WHERE ... LIMIT 50. This can leak rows across tenants.
  - _Fix:_ Scope backfill queries to a specific tenant (pass tenantId in and add AND tenant_id = ${tenantId}), or iterate per-tenant explicitly. Never perform cross-tenant SELECTs on tenant-scoped tables.
- **server/embeddings.ts:716** — SELECT FROM memory_entries in backfillEmbeddingVecs is not tenant-scoped: SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200. This reads tenant data across all tenants.
  - _Fix:_ Run backfill per-tenant or add a tenantId filter (WHERE tenant_id = ${tenantId}) so only the caller's tenant rows are read. If this is an intentional multi-tenant maintenance, perform it with explicit admin tooling and separate safeguards.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge in backfillEmbeddingVecs reads rows without tenant filtering: SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200. This can return data for other tenants.
  - _Fix:_ Add tenant_id filtering (WHERE tenant_id = ${tenantId}) or otherwise restrict the backfill to a single tenant at a time. If this is intended to run globally, isolate it behind privileged admin tooling and auditing.
- **server/felix-brain.ts:19** — Boot-time rehydration selects agent_runs across all tenants without a tenant filter: SELECT ... FROM agent_runs WHERE status IN (...) AND created_at > ... . Reading agent_runs without tenant_id will return rows for all tenants (tenant_id exists on that table).
  - _Fix:_ Scope the rehydration query to the intended tenant(s). If rehydrating all tenants is intended, require an explicit, auditable admin path; otherwise add WHERE tenant_id = ${tenantId} or iterate tenants and rehydrate on a per-tenant basis to avoid cross-tenant data access.
- **server/google-drive.ts:547** — SELECT from projects is performed with WHERE eq(projects.id, projectId) but no tenant scoping. The code later compares proj.tenantId to the caller, but the initial read occurred without tenant-scoped SQL (risk of unauthorized read and violates the invariant requiring SQL-level tenant filters).
  - _Fix:_ Scope the SELECT to the caller's tenant at SQL level (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise ensure authenticated tenantId is required and applied in the query so the DB read is tenant-scoped.
- **server/google-drive.ts:560** — UPDATE projects sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) but no tenant constraint. Even though ownership was checked in application logic, the SQL update itself is not tenant-scoped (race/IDOR risk).
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so writes cannot affect rows belonging to other tenants.
- **server/google-drive.ts:825** — uploadAndShare reads projects via WHERE eq(projects.id, params.projectId) without including a tenant constraint BEFORE using the row. The handler verifies tenant ownership after the SELECT rather than performing a tenant-scoped read, violating the rule that referenced rows must be verified/scoped prior to use.
  - _Fix:_ Perform the project lookup with tenant scoping (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, callerTenantId)))) or require/obtain the caller's tenantId up-front and include it in the SELECT so you never read an unscoped foreign row.
- **server/google-drive.ts:1003** — searchDriveFiles does a SELECT on projects to fetch driveFolderId via WHERE eq(projects.id, params.projectId) without a tenant filter before using the result. The code then uses that value to scope Drive queries — reading the project without SQL tenant scoping is a cross-tenant read risk.
  - _Fix:_ Include tenant_id in the SELECT predicate (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or otherwise require and apply the caller's tenantId in the DB query so the lookup is guaranteed tenant-scoped.
- **server/health-audit.ts:314** — SELECT from code_proposals is executed with no tenant filter (no WHERE tenant_id = ...) — reading a tenant-scoped table across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or explicitly run the audit only in an admin context after validating admin privileges. If the intent is to perform a cross-tenant admin sweep, make that explicit and gated (e.g. require an admin flag and audit-per-tenant loop), otherwise scope to tenant_id.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks is executed with no tenant filter (no WHERE tenant_id = ...) — reading a tenant-scoped table across all tenants.
  - _Fix:_ Scope the SELECT to the intended tenant (add AND tenant_id = ${tenantId}) or require explicit admin privileges and iterate per-tenant. If a global scan is intended, make it explicit and protected by an admin-only gate.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table research_schedules without any tenant_id constraint (SELECT * FROM research_schedules ...). This can read rows across all tenants.
  - _Fix:_ Constrain the query to a specific tenant (e.g. WHERE tenant_id = ${someTenantId}) or iterate per-tenant (get active tenant ids and query per-tenant) so you never select tenant-scoped rows without an explicit tenant_id filter.
- **server/heartbeat.ts:101** — UPDATE on tenant-scoped table research_schedules uses WHERE id = ${sched.id} but does not include tenant_id. An UPDATE by id alone can modify rows across tenants if ids collide or are enumerated.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform updates through a storage helper that enforces the tenant constraint.
- **server/heartbeat.ts:107** — UPDATE on tenant-scoped table research_schedules lacks tenant_id in WHERE clause (UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id}).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage function that enforces the tenant context.
- **server/heartbeat.ts:112** — UPDATE on tenant-scoped table research_schedules (SET next_run_at = ${nextDate} WHERE id = ${sched.id}) has no tenant_id constraint.
  - _Fix:_ Restrict the UPDATE to the owning tenant: WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}, or use a tenant-aware storage API.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id filter (tenant-scoped table).
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or perform the update through a storage layer that asserts tenant ownership.
- **server/heartbeat.ts:612** — SELECT * FROM event_log WHERE status = 'pending' without tenant_id constraint. event_log rows include tenant_id and this read pulls pending events across all tenants.
  - _Fix:_ Either restrict the SELECT to a tenant (WHERE tenant_id = ${tenantId} AND status='pending') or explicitly iterate/claim per-tenant (e.g. getActiveTenantIds and query/claim per tenant) so tenant boundaries are enforced at the SQL level.
- **server/heartbeat.ts:257** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — heartbeat_tasks is tenant-scoped but the UPDATE omits tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${t.id} AND tenant_id = ${t.tenantId} (or equivalent), or use the storage layer (updateHeartbeatTask) which enforces tenant ownership.
- **server/heartbeat.ts:261** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use a tenant-aware storage function to perform the update.
- **server/heartbeat.ts:2818** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint. This updates a tenant-scoped table without verifying the row belongs to the expected tenant.
  - _Fix:_ Add tenant filtering (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update via storage.updateHeartbeatTask/newTask helper that enforces tenant context.
- **server/heartbeat.ts:3270** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id in WHERE. This write may affect rows across tenants.
  - _Fix:_ Constrain the UPDATE to the correct tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or rely on a tenant-aware storage API to perform the update safely.
- **server/internal-resolver.ts:107** — resolvePlanProposed() SELECTs plans by id (SELECT id, objective, status FROM plans WHERE id = ${planId}) without any tenant_id constraint or ownership verification. A foreign plan_id coming from event data is used directly.
  - _Fix:_ When resolving an event that references a plan, verify the plan belongs to the intended tenant before acting. Query plans with an explicit tenant filter (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or select the row and validate its tenant_id before reading/modifying it. Do not trust raw ids from events without tenant ownership checks.
- **server/lib/action-ledger.ts:304** — UPDATE in sweepStaleAttempts performs a cross-tenant write — the WHERE clause (id IN (SELECT id FROM action_attempts ...)) has no tenant_id predicate, so rows from all tenants can be updated.
  - _Fix:_ Ensure every UPDATE on tenant-scoped table is constrained by tenant_id. Either (a) add tenant_id = <tenant> predicates (or run per-tenant loops that call the tenant-scoped transition/markReconcilerDigested helpers), or (b) select tenant_id in the subquery and then perform per-tenant guarded transitions so no single UPDATE can touch rows across tenants.
- **server/lib/agent-trace.ts:124** — persistSpanClose issues an UPDATE against tenant-scoped table agent_trace_spans but the WHERE clause filters only on span_id (no tenant_id). This allows updates to span rows without verifying the caller's tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) or require and pass the tenantId into persistSpanClose (assert it from currentTenantId()) so the update is always tenant-constrained.
- **server/lib/bwb-job-progress.ts:74** — scope() may omit tenant predicate when BWB_TENANT_ID is unset, and several UPDATE/SELECT statements call .where(scope(...)). That results in writes/reads against video_jobs that may lack tenant_id filtering (writes can touch rows across tenants).
  - _Fix:_ Do not allow scope() to return a clause that omits tenant_id for tenant-scoped tables. Require a valid tenantId (throw or no-op) when envTenantId is missing, or fetch the job's tenant_id first and then perform tenant-scoped updates. Always include eq(videoJobs.tenantId, <tenant>) in WHERE for any update/delete/select on tenant-scoped tables.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT aggregates tenant-scoped `projects` without constraining to a caller tenant (`WHERE tenant_id = ...`). This query reads counts across all tenants: `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ... GROUP BY tenant_id`.
  - _Fix:_ Restrict the aggregation to the snapshot's tenant set or run the check per-tenant. e.g. add `WHERE tenant_id = ANY(${Array.from(wantedByTenant.keys())}::int[])` or iterate wantedByTenant and run `SELECT count(*) ... WHERE tenant_id = ${t} AND tags && ...` so every read is constrained to an explicit tenant_id.
- **server/lib/inbox-ingest.ts:444** — Orphan lookup SELECT against tenant-scoped `inbox_messages` lacks a tenant constraint. Query `SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ON c.inbox_message_id = m.id WHERE m.message_id = ${id} AND c.id IS NULL LIMIT 1` can return rows from other tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. `WHERE m.message_id = ${id} AND m.tenant_id = ${tenantId} AND c.id IS NULL LIMIT 1`. This ensures the orphan check only examines rows owned by the expected tenant.
- **server/lib/inbox-ingest.ts:504** — UPDATE on tenant-scoped table `inbox_classifications` is missing a tenant constraint: `UPDATE inbox_classifications SET routed_to = ${JSON.stringify(routedTo)}::jsonb WHERE inbox_message_id = ${storeResult.id}`. Without `AND tenant_id = ...` this could modify another tenant's row sharing the same inbox_message_id.
  - _Fix:_ Constrain updates with tenant_id, e.g. `UPDATE inbox_classifications SET routed_to = ${JSON.stringify(routedTo)}::jsonb WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}`. Always include tenant_id in UPDATE/DELETE WHERE clauses on tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:266** — Constructing a Postgres array literal string (arrLit) from request-derived platforms and interpolating it into sql``: this is a string-built SQL fragment derived from user input and risks injection + tenant/isolation issues.
  - _Fix:_ Avoid building SQL array literals by hand. Use proper parameterization or Drizzle array binding for text[] (pass a JS array as a parameter or use an explicit array parameterization helper). Validate and canonicalize platform inputs and pass them as bound parameters rather than embedding escaped strings into SQL text.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is executed without filtering by tenant_id — read across all tenants returned in a single result set.
  - _Fix:_ If this is intended to be a system-wide audit, make that explicit and restrict the caller to an admin context. Otherwise, constrain the SELECT by tenant_id (or run the sweep per-tenant) to maintain application-level tenant isolation (WHERE tenant_id = ${tenantId}).
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` is executed without filtering by tenant_id — allowing cross-tenant discovery of active rows.
  - _Fix:_ Add a tenant_id predicate to the query (e.g. WHERE tenant_id = ${tenantId}) or run the sweep per-tenant under an explicit admin context. If the absence of tenant filtering is intentional for system audits, enforce admin-only access and auditing.
- **server/lib/system-probes.ts:163** — COUNT(*) FROM messages (messagesTable) is executed without a tenant_id WHERE clause — this aggregates messages across tenants and returns a global count in a probe intended to run per-system/tenant context.
  - _Fix:_ Scope the message count query to a tenant (e.g. add WHERE tenant_id = ${tenantId}) or run the probe per-tenant. If a global count is required, ensure the function is strictly admin-only and documented/audited; otherwise enforce application-level tenant predicates for tenant-scoped tables.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table tenantVoiceProfiles uses WHERE id = <existing.id> but does NOT include tenant_id in the WHERE clause.
  - _Fix:_ Include the tenant filter in the UPDATE (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) so the update is constrained to the caller's tenant.
- **server/memory-graph.ts:392** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id constraint on UPDATE.
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or perform the update via a tenant-scoped transaction so the row cannot be modified across tenants.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — missing tenant_id constraint (called after classifyMemoryCategory), allowing cross-tenant updates.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or have the storage helper enforce tenant ownership before updating.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — same missing tenant constraint in the update path for 'update' actions.
  - _Fix:_ Ensure every UPDATE includes tenant_id (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or use a storage API that enforces tenant ownership atomically.
- **server/minerva-planner.ts:355** — UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval' — the UPDATE does not include a tenant_id constraint, allowing state changes to plans belonging to other tenants.
  - _Fix:_ Constrain the CAS UPDATE by tenant (e.g. add AND tenant_id = ${expectedTenantId}) or require the caller to supply/validate tenantId and include eq(plans.tenant_id, tenantId) in the WHERE clause so only the caller's tenant row can be modified.
- **server/minerva-planner.ts:459** — On createPlan failure the code rolls back the original decision with UPDATE plans SET status = 'awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising' — UPDATE lacks tenant_id constraint.
  - _Fix:_ Include tenant scoping in the rollback UPDATE (e.g. WHERE id = ${args.planId} AND status = 'revising' AND tenant_id = ${row.tenant_id}) so rollback only affects the intended tenant's plan.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table 'project_files' omits tenant_id. The INSERT INTO project_files(...) VALUES(...) does not include tenant_id even though tenant-scoped tables must be constrained/annotated.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. add tenant_id column and pass ${options.tenantId}). Preferably keep the prior assertProjectInTenant check, then INSERT with tenant_id = ${options.tenantId} so the row is recorded under the correct tenant and cannot leak or fail silently.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table 'project_files' omits tenant_id. The parallel-produce path performs INSERT INTO project_files(...) VALUES(...) without setting tenant_id.
  - _Fix:_ Add tenant_id to the INSERT values (e.g. VALUES (${options.projectId}, ${safeTitle + ".mp4"}, ..., ${options.tenantId}, ...)) and ensure assertProjectInTenant returned true before inserting. Always bind tenant_id rather than relying on implicit ownership.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table 'oauth_subscriptions' without a tenant_id filter. validateSubscriptionsOnStartup runs SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE, which reads rows across all tenants.
  - _Fix:_ If this operation must be global/admin-only, document and gate it explicitly (require an admin context) and avoid accidental exposure in tenant-scoped code. Otherwise, restrict the query to a single tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant in a controlled admin flow. Prefer explicit per-tenant operations to satisfy tenant isolation invariant.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id. connectGoogleViaReplit executes SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' (no tenant constraint).
  - _Fix:_ Scope this query to a tenant when run in a tenant context or perform an explicit, authorized admin-level enumeration. Prefer an explicit parameter (tenantId) and WHERE tenant_id = ${tenantId}, or add an admin-only code path that clearly documents and enforces privileges.
- **server/oauth-subscriptions.ts:1258** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id. proactiveTokenRefresh SELECTs all active subscriptions (WHERE is_active = TRUE) and processes them across tenants.
  - _Fix:_ If proactiveTokenRefresh is intentionally multi-tenant (an admin background job), ensure it's only invoked in an appropriately privileged background process and document that it operates across tenants. Otherwise restrict to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate tenants via an explicit, audited admin loop. Avoid implicit cross-tenant SELECTs in tenant-scoped code paths.
- **server/oauth-subscriptions.ts:1328** — SELECT from tenant-scoped table 'oauth_subscriptions' without tenant_id. forceRefreshAllSubscriptions runs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE and will touch rows for all tenants.
  - _Fix:_ Same mitigations as above: either require/verify admin privileges for this operation, or restrict the query to a single tenant passed in by the caller. If global enumeration is required, centralize and harden this logic in an admin-only module and log/audit its runs.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw() with a request-derived value. saveLedgerState calls tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`) where lockKey is derived from ledgerKey and tenantId — this inserts a derived string into raw SQL rather than as a bound parameter, creating an injection and isolation risk.
  - _Fix:_ Avoid sql.raw() for data-derived values. Compute a numeric BigInt (or a parameterizable value) in JS and pass it as a bound parameter, e.g. tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), or better: compute the numeric advisory lock key as a BigInt in code and bind it via the templating API so the DB driver parameterizes it. Validate/strictly constrain ledgerKey/tenantId inputs before deriving the lock key.
- **server/pace-control.ts:85** — SELECT from tenant-scoped table 'heartbeat_logs' with no tenant constraint. checkPace queries heartbeat_logs over a time window but does not filter by tenant_id, so usage counts are aggregated across tenants.
  - _Fix:_ If the pace check is intended to be per-tenant, add WHERE tenant_id = ${tenantId} (and accept tenantId as a parameter). If it is intentionally global, ensure the function is only callable by a privileged/admin context and clearly document that it enumerates across tenants. Prefer explicit per-tenant scoping in tenant-facing code paths.
- **server/pace-control.ts:140** — SELECTs from tenant-scoped table 'heartbeat_logs' without tenant constraint. getPaceSnapshot aggregates recent heartbeat_logs but lacks tenant_id filtering, returning cross-tenant data in what appears to be a tenant-scoped helper.
  - _Fix:_ Scope the snapshot to a tenant by accepting a tenantId parameter and adding WHERE tenant_id = ${tenantId} to the queries. If an operator-level global snapshot is required, move this to an admin-only endpoint and harden/authorize it accordingly.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table agent_knowledge executed without a tenant_id constraint (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This reads rows across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or, if a global system runner must enumerate all tenants, change the code to iterate tenants explicitly and query each tenant's rows with a tenant_id WHERE clause. Avoid unscoped SELECTs on tenant-scoped tables unless strong, reviewed justification exists and additional access controls are in place.
- **server/research-engine.ts:158** — UPDATE research_sessions in cleanupZombieSessions does not constrain by tenant_id — it will mark running sessions across all tenants as completed.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or iterate/execute per-tenant. Parameterize tenant(s) rather than performing a global UPDATE to avoid cross-tenant side-effects.
- **server/research-engine.ts:311** — UPDATE research_programs SET model = ... WHERE id = ${programId} lacks a tenant_id constraint — it can modify another tenant's program record.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise verify program ownership before updating). Use parameterized tenant context for all UPDATEs on tenant-scoped tables.
- **server/research-engine.ts:390** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' ... executes without tenant scoping — will delete autoresearch knowledge for all tenants.
  - _Fix:_ Add tenant filtering (e.g. AND tenant_id = ${tenantId}) or run the cleanup per-tenant. Never run destructive statements on tenant-scoped tables without an explicit tenant_id constraint.
- **server/research-engine.ts:451** — SELECT FROM research_experiments WHERE session_id = ${sessionId} ... lacks tenant_id filtering — reads experiments across tenants for the same session id value.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or join/validate the session row's tenant before selecting). Always include the caller's tenant_id when reading tenant-scoped tables.
- **server/research-engine.ts:460** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} omits tenant_id — may return a program belonging to a different tenant.
  - _Fix:_ Filter by tenant_id (e.g. WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}) or validate program ownership before trusting program fields.
- **server/research-engine.ts:465** — SELECT summary FROM research_sessions WHERE id = ${sessionId} does not include tenant_id — may read another tenant's session summary.
  - _Fix:_ Include AND tenant_id = ${session.tenantId} in the WHERE clause or fetch session via a JOIN that enforces tenant ownership before using its fields.
- **server/research-engine.ts:550** — UPDATE research_sessions SET ... WHERE id = ${sessionId} (session end) lacks tenant_id filtering — will update sessions across tenants if ids collide or are misused.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} to the WHERE clause (or include tenant in the UPDATE key) to ensure only the owning tenant's session is modified.
- **server/research-engine.ts:821** — UPDATE research_experiments ... WHERE id = ${experimentId} updates an experiment row by id without verifying tenant_id — writes may affect another tenant's experiment.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}) for all UPDATE/DELETE/SELECT statements on tenant-scoped tables, even when addressing rows by id.
- **server/research-engine.ts:888** — UPDATE research_sessions SET model = ${fallbackModel} WHERE id = ${session.sessionId} lacks tenant scoping and can change another tenant's session.
  - _Fix:_ Restrict the UPDATE with AND tenant_id = ${session.tenantId} or update via a transaction that first selects and validates the session belongs to the tenant.
- **server/research-engine.ts:952** — SELECT * FROM research_sessions WHERE id = ${sessionId} in getResearchSessionStatus does not restrict by tenant_id — may leak session data across tenants.
  - _Fix:_ Require and apply the caller's tenant_id (e.g. WHERE id = ${sessionId} AND tenant_id = ${callerTenantId}) or validate session ownership from activeSessions before returning DB rows.
- **server/research-engine.ts:988** — SELECT name FROM research_programs WHERE id = ${session.programId} (inside injectKeepedFinding) omits tenant_id and may fetch a program from another tenant.
  - _Fix:_ Include tenant scoping: WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}, or validate program ownership earlier and only use trusted program data.
- **server/research-engine.ts:1483** — In replayHighValueFindings, UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL does not include tenant_id — it can mark experiments for other tenants as replayed.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (use the tenantId parameter passed into replayHighValueFindings) to ensure claims/locks only affect the intended tenant's rows.
- **server/research-engine.ts:1514** — UPDATE research_experiments SET replayed_proposal_id = ${newProposalId} WHERE id = ${row.id} updates without tenant_id filtering — could associate a proposal with another tenant's experiment.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause, or perform the update in a transaction that verifies ownership before writing.
- **server/research-engine.ts:1526** — UPDATE research_experiments SET replayed_at = NULL WHERE id = ${row.id} AND replayed_proposal_id IS NULL resets claims without tenant scoping — risks cross-tenant modifications.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause to ensure resets only affect this tenant's experiments. Always apply tenant constraints to UPDATEs that modify tenant-scoped rows.
- **server/routes.ts:764** — SELECT from tenant-scoped table `projects` without constraining by tenant_id (SELECT p.id, p.name, p.tenant_id, ... FROM projects p JOIN tenants t ... WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = '')
  - _Fix:_ Restrict this backfill query to a specific tenant or iterate per-tenant. Add a WHERE p.tenant_id = ${tenantId} (or pass a list of tenant ids to process) or require platform-admin context and document the privileged cross-tenant operation. Avoid unscoped scans of tenant-scoped tables in general code paths.
- **server/routes/admin.ts:569** — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant_id filter — reading from a likely tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId} or equivalent), or explicitly aggregate/authorize cross-tenant access only after a deliberate platform-admin guard and audit logging. Never read tenant-scoped tables without an application-level tenant_id constraint.
- **server/routes/admin.ts:574** — Query against tool_performance (failingRows) lacks any tenant_id constraint — possible cross-tenant data exposure.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE tenant_id = ${tenantId}) or perform an explicit, well-audited platform-admin aggregation that does not return per-tenant rows. Ensure the route is guarded and the SQL always enforces tenant isolation unless a deliberate, logged cross-tenant access path exists.
- **server/routes/admin.ts:587** — SELECT from ai_insights (stuckInsights) is executed without a tenant_id constraint — ai_insights appears to be tenant-scoped elsewhere but here is queried globally.
  - _Fix:_ Constrain ai_insights reads to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or, if platform-wide inspection is required, implement an explicit platform-admin path that aggregates safely and documents/authorizes cross-tenant reads. Do not rely solely on route-level admin gating.
- **server/routes/admin.ts:598** — Query against heartbeat_tasks (disabledTasks) is executed with no tenant_id filter — heartbeat_tasks is used as a tenant-scoped table elsewhere but is queried globally here.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) to the heartbeat_tasks query, or implement a deliberate platform-admin aggregation that avoids returning raw per-tenant rows. Ensure tenant isolation is enforced at the SQL level for tenant-scoped tables.
- **server/routes/admin.ts:609** — SELECT from ai_insights (stuckPending) runs without a tenant_id constraint, exposing tenant-scoped incident rows across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or equivalent Drizzle eq(table.tenantId, tenantId)) to restrict results to the caller's tenant, or create an audited platform-admin aggregation endpoint with explicit authorization for cross-tenant access.
- **server/routes/admin-zombie-detector.ts:38** — Querying agent_trace_spans (tools usage CTE) with no tenant_id constraint — likely a tenant-scoped activity/log table being read globally inside the admin zombie detector.
  - _Fix:_ Add tenant scoping to the agent_trace_spans query (e.g. WHERE tenant_id = ${tenantId}) or otherwise ensure only an explicit, auditable platform-admin path may aggregate cross-tenant trace data. Always enforce tenant_id at the SQL level for tenant-scoped tables.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table `conversations` lacks tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or use a storage-layer API that enforces tenant scope. Always include tenant_id in WHERE for tenant-scoped tables.
- **server/routes/conversations.ts:101** — SELECT from `project_conversations` lacks tenant scope (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}). If `project_conversations` is tenant-scoped this can leak/affect other tenants.
  - _Fix:_ Include tenant filtering in the SELECT (AND tenant_id = ${tenantId}) and in subsequent INSERT/UPDATEs, or ensure the table is global by design. Prefer using storage layer helpers that enforce tenant_id.
- **server/routes/conversations.ts:102** — INSERT into `project_conversations` omits tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). If this table is tenant-scoped, the tenant_id must be set on INSERT.
  - _Fix:_ Include tenant_id in the INSERT values (tenant_id, project_id, conversation_id) with the caller's tenant, or use a storage function that populates and enforces tenant_id.
- **server/routes/conversations.ts:228** — INSERT into `project_notes` omits tenant context (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})). If project_notes is tenant-scoped, tenant_id is missing from the INSERT.
  - _Fix:_ Set tenant_id explicitly when inserting project-scoped rows (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ...)), or use a storage API that enforces tenant_id.
- **server/routes/heartbeat.ts:102** — UPDATE on tenant-scoped table `heartbeat_tasks` lacks tenant_id constraint in approval path (UPDATE heartbeat_tasks SET approval_status = 'approved', ... WHERE id = ${taskId}).
  - _Fix:_ Constrain the UPDATE to the task's tenant (e.g. AND tenant_id = ${task.tenantId} or AND tenant_id = ${callerTenantId}). Even for admin flows, prefer explicit tenant scoping to avoid accidental cross-tenant writes.
- **server/routes/heartbeat.ts:116** — UPDATE on tenant-scoped table `heartbeat_tasks` lacks tenant_id constraint in rejection path (UPDATE heartbeat_tasks SET approval_status = 'rejected', ... WHERE id = ${taskId}).
  - _Fix:_ Add AND tenant_id = ${task.tenantId} (or caller's tenant id) to the WHERE clause so approvals/rejections cannot modify tasks belonging to other tenants.
- **server/routes/inbox-notifications.ts:101** — UPDATE statement that marks a message read omits tenant_id in WHERE (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). The SELECT earlier constrained by tenant_id, but the UPDATE itself is not scoped.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}) to enforce tenant isolation at the SQL layer.
- **server/routes/projects.ts:185** — INSERT into project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author) ...).
  - _Fix:_ Include tenant_id in the INSERT and pass the resolved tenantId (or derive/validate tenant ownership before inserting). E.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ${note}, ${author}) to ensure new rows are tenant-scoped.
- **server/routes/projects.ts:225** — INSERT into project_files (base64 upload path) does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...).
  - _Fix:_ Add tenant_id to the INSERT and supply the caller's tenantId (or derive it from the verified project). Example: INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${projectId}, ${tenantId}, ${fileName}, ...).
- **server/routes/projects.ts:293** — INSERT into project_files (multipart upload path) does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...).
  - _Fix:_ Add tenant_id to the INSERT and supply the resolved tenantId. Ensure every INSERT into tenant-scoped tables includes tenant_id explicitly, e.g. VALUES (${projectId}, ${tenantId}, ${fileName}, ...).
- **server/routes/slack.ts:124** — Raw/interpolated SQL (db.execute(sql`... ${token} ... ${tenantId} ...`)) uses request-derived values (token, tenantId) inside a sql template. This both mixes protection & filtering into raw SQL fragments and risks injection / isolation issues instead of using Drizzle's typed query builder.
  - _Fix:_ Replace the raw sql`...` call with Drizzle query-builder APIs (db.select().from(...).leftJoin(...).where(...)) using eq(...) for tenant_id and token comparisons so values are passed as parameters. Validate/sanitize token early and avoid any string interpolation into SQL; prefer eq(personas.name, token) OR eq(tpn.display_name, token) with proper parameter binding.
- **server/routes/slack.ts:191** — Raw/interpolated SQL (db.execute(sql`... tpn.tenant_id = ${tenantId} ...`)) used to enumerate persona names with tenantId injected into the template. This builds SQL via template interpolation with a request-derived tenant identifier rather than using Drizzle's eq() predicates.
  - _Fix:_ Use Drizzle's query builder to select persona names with an explicit .where(eq(tenant_persona_names.tenantId, tenantId), eq(personas.isActive, true)) or equivalent. Avoid sql`...${tenantId}` interpolation; use parameterized Drizzle predicates to enforce tenant scoping and prevent injection/isolation mistakes.
- **server/routes/stripe-checkout.ts:99** — Raw/interpolated SQL (db.execute(sql`... WHERE pr.id = ${priceId} ... AND (${isAnonymous}::boolean = false OR p.metadata->>'kind' = 'audit')`)) interpolates priceId and isAnonymous (derived from the request) into an SQL template. This blends request-derived values directly into SQL and bypasses the Drizzle .where(eq(...)) pattern required for clear tenant/parameter binding and isolation checks.
  - _Fix:_ Replace this db.execute(sql`...`) with Drizzle query-builder calls (db.select().from(prices).join(products).where(eq(prices.id, priceId), eq(prices.active, true), eq(products.active, true), ...)). Ensure priceId is validated server-side and use proper parameterized predicates rather than interpolated sql templates. If anonymous/tenant gating is required, express it explicitly with typed predicates rather than string interpolation.
- **server/routes/stats.ts:54** — Use of sql template interpolation with a request-derived tenantId inside .where (s`${conversations.tenantId} = ${tenantId}` and s`${messages.conversationId} IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`). This constructs SQL via template interpolation with request data instead of using Drizzle's eq()/subquery APIs, increasing injection/isolation risk.
  - _Fix:_ Replace these templated conditions with Drizzle predicates: e.g. db.select(...).from(conversations).where(eq(conversations.tenantId, tenantId)) and use a parameterized subquery or join for messages (or .where(inArray(messages.conversationId, db.select({id: conversations.id}).from(conversations).where(eq(conversations.tenantId, tenantId))))). Avoid embedding request-derived values inside sql`` templates; use typed eq/inArray helpers so tenant filtering is explicit and parameterized.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id filter. This reads aggregate session data across all tenants.
  - _Fix:_ Constrain the query to the intended tenant (e.g. WHERE tenant_id = ${tenantId} or WHERE tenant_id = 1 for owner/seed data). Use a parameterized tenantId variable rather than a global count to enforce tenant isolation.
- **server/seed.ts:2855** — SELECT id, name, model FROM research_programs WHERE is_active = true lacks a tenant_id constraint — it will return programs for all tenants.
  - _Fix:_ Add a tenant predicate (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise scope the query to the intended tenant (tenant_id = 1 for seed). Always include tenant_id on reads from tenant-scoped tables.
- **server/seed.ts:2860** — UPDATE research_programs SET model = ... WHERE id = ${p.id} does not restrict by tenant_id — it can modify rows across tenants if ids collide.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${p.tenantId} or tenant_id = 1). Ensure updates always validate tenant ownership before writing.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id constraint. This reads conversation rows across tenants.
  - _Fix:_ Filter the SELECT by tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) to limit results to the intended tenant before any backfill/insert operations.
- **server/seed.ts:3205** — SELECT from tenant-scoped table `conversations` is executed without constraining by tenant_id. This query (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)) will inspect rows across all tenants.
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. add AND c.tenant_id = ${tenantId} or .where(eq(conversations.tenantId, tenantId))). If the backfill must be tenant-local, iterate per-tenant or pass an explicit tenant id for the operation so you do not touch other tenants' data.
- **server/seed.ts:4031** — SELECT from tenant-scoped table `heartbeat_tasks` is issued without any tenant filter: `await db.select().from(heartbeatTasks)`. This reads all tenants' heartbeat tasks and is used to determine what seeds to insert.
  - _Fix:_ Scope the query to the caller/system tenant by adding a WHERE tenant_id = <tenant> (or use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId))). If you intend to seed only a specific tenant, pass that tenant id explicitly. Avoid global selects on tenant-scoped tables.
- **server/seed.ts:4447** — UPDATE on tenant-scoped table `memory_entries` (normalizer replacing 'Manjaro' → 'wellness-program') is executed without any tenant constraint. This UPDATE will modify rows for all tenants.
  - _Fix:_ Scope the UPDATE by tenant (e.g. AND tenant_id = ${tenantId} or use Drizzle .where(eq(memoryEntries.tenantId, tenantId))). If the intent is to run this migration across all tenants, iterate over tenant ids explicitly and run constrained updates for each tenant to avoid accidental cross-tenant changes.
- **server/self-improvement.ts:170** — detectStagnation() issues a SELECT FROM experiments that can omit a tenant filter when tenantId is not provided — the query may scan all tenants' experiments.
  - _Fix:_ Always constrain queries on tenant-scoped tables. Either make tenantId required (throw when absent) or always push eq(experiments.tenantId, tenantId) into the conditions array before building the WHERE clause so the SELECT cannot run unscoped.
- **server/self-improvement.ts:511** — getExperimentHistory() will return experiments across all tenants when tenantId is omitted (the fallback path does db.select().from(experiments) with no tenant filter).
  - _Fix:_ Fail-closed or require tenantId. If tenantId is optional for some callers, explicitly document and enforce which callers may get cross-tenant data; otherwise always add eq(experiments.tenantId, tenantId) to the WHERE or throw if tenantId not provided.
- **server/skill-seeker.ts:766** — listGaps(status) branch issues SELECT * FROM capability_gaps WHERE status = ${status} with no tenant filter — returns rows for all tenants when caller did not provide tenantId.
  - _Fix:_ Require a tenantId for tenant-scoped reads or add a tenant filter in this branch (AND tenant_id = ${tenantId}). If the function must support global admin queries, make that explicit and gated by admin-only checks.
- **server/skill-seeker.ts:777** — listGaps() fallback branch performs SELECT * FROM capability_gaps with no tenant filter when neither status nor tenantId supplied — unchecked cross-tenant read.
  - _Fix:_ Do not allow an unscoped read. Require tenantId (fail-closed to an error/empty list), or restrict this path to admin callers only and document the guard. Always include tenant_id in WHERE for tenant-scoped tables.
- **server/storage.ts:239** — getConversationUnscoped() performs a SELECT on conversations without constraining by tenant_id (unscoped read).
  - _Fix:_ Avoid unscoped reads in general. If this helper is required, restrict use to trusted internal callers only and document the responsibility clearly; otherwise require a tenantId and add and(eq(conversations.tenantId, tenantId)) or use tenantScope(conversations.tenantId, tenantId). Consider removing or renaming to make misuse less likely.
- **server/storage.ts:248** — createConversation() inserts into conversations with an optional tenantId on the passed data (values(data)), allowing an INSERT with no explicit tenant_id.
  - _Fix:_ Require tenantId on the function signature and assert/validate it (e.g. assertValidTenantId(tenantId) or use tenantScope helper). Populate tenantId explicitly in the inserted object rather than accepting optional tenantId from callers.
- **server/storage.ts:289** — attachCitationMetadata() runs raw SELECT FROM messages WHERE id IN (...) without constraining by tenant_id, reading a tenant-scoped table unscoped.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or re-use the tenantId available from the msgs input. If message rows were already validated to belong to the caller, pass that tenantId into this helper and assert it before running the SELECT.
- **server/storage.ts:390** — attachCitations() issues a raw UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id constraint (unscoped write to tenant-scoped table).
  - _Fix:_ Require a tenantId parameter (or derive/validate it from the conversation/message row) and include AND tenant_id = ${tenantId} in the WHERE clause to ensure the update cannot modify another tenant's row.
- **server/storage.ts:524** — createMemoryEntry() inserts into memory_entries using values(data) without ensuring data includes tenantId (inserts may be written without tenant scoping).
  - _Fix:_ Make tenantId required when creating memory entries (function signature), validate it (assertValidTenantId), and ensure the INSERT explicitly sets tenant_id = tenantId (or values({ ...data, tenantId })). Do not accept undefined tenantId for tenant-scoped tables.
- **server/storage.ts:474** — deletePersona() updates conversations (clearing personaId) using WHERE conversations.personaId = id but without tenant filtering — this will affect conversations across all tenants.
  - _Fix:_ If personas are tenant-scoped, require tenantId and add eq(conversations.tenantId, tenantId) to the WHERE. If personas are global but this operation should only affect a single tenant, make that explicit and pass/validate tenantId; otherwise ensure this admin-level operation is restricted to appropriate privilege and clearly audited.
- **server/storage.ts:797** — updateMemoryEmbedding() updates memory_entries SET embedding WHERE id = id with no tenant filter (write against tenant-scoped table unscoped).
  - _Fix:_ Require and validate tenantId (or otherwise verify ownership) and include eq(memoryEntries.tenantId, tenantId) in the WHERE clause. Alternatively, perform a JOIN/subselect to validate the row belongs to the caller before updating.
- **server/storage.ts:809** — updateKnowledgeEmbedding() updates agent_knowledge SET embedding WHERE id = id without constraining by tenant_id (unscoped write).
  - _Fix:_ Require tenantId or validate ownership prior to updating, and include AND agent_knowledge.tenant_id = ${tenantId} (or use tenantScope) in the WHERE clause to ensure the update is tenant-scoped.
- **server/storage.ts:818** — getMemoriesWithoutEmbeddings() selects from memory_entries WHERE status='active' AND embedding IS NULL with no tenant filter, returning rows across tenants.
  - _Fix:_ Require tenantId and add eq(memoryEntries.tenantId, tenantId) to the WHERE clause (or iterate per-tenant). Ensure background embedding workers receive a tenant context and do not process rows from other tenants.
- **server/storage.ts:824** — getKnowledgeWithoutEmbeddings() selects agent_knowledge rows without restricting by tenant_id (reads across tenants).
  - _Fix:_ Require tenantId and include eq(agentKnowledge.tenantId, tenantId) in the WHERE, or otherwise ensure the caller has admin privileges and the fetch is intentionally cross-tenant and logged.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding() updates felix_proposals SET args_embedding WHERE id = ${proposalId} AND args_embedding IS NULL with no tenant_id filter — write to a tenant-scoped table without tenant scoping.
  - _Fix:_ Require tenantId as a parameter (or validate proposal ownership before stamping) and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. Use parameterized tenant checks rather than relying on id alone.
- **server/surprise-scorer.ts:139** — scoreProposalSurprise() updates actual_outcome_embedding on felix_proposals WHERE id = ${proposalId} without including tenant_id — unscoped write to tenant-scoped table.
  - _Fix:_ Include tenant checking in the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), or validate the proposal row's tenant before performing the update to ensure cross-tenant writes cannot occur.
- **server/surprise-scorer.ts:181** — When not enough neighbors exist the code UPDATEs felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} with no tenant constraint.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) to the UPDATE or make tenantId required and assert ownership before updating proposal metadata.
- **server/surprise-scorer.ts:212** — After computing the mean distance the code UPDATEs felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} without tenant_id filtering.
  - _Fix:_ Use a tenant-scoped WHERE clause (e.g. AND tenant_id = ${tenantId}) or validate ownership of the proposal row before performing the UPDATE to prevent accidental cross-tenant modification.
- **server/tool-learning.ts:308** — Select/update/delete patterns in tryAndRevert/revert paths operate on custom_tools rows by id without explicit tenant_id checks — revertToolRow and related code delete/update rows without tenant filtering.
  - _Fix:_ Ensure every revert/update/delete path includes an explicit tenant_id condition (use the tenantId passed into the API or the tool row's tenantId after validation) to enforce tenant isolation before mutating or removing rows.
- **server/tenant-fork.ts:226** — UPDATE on tenant-scoped table (re-pointing self-ref) uses only id filter (.where(eq(entry.table.id, p.newId))) with no tenant_id constraint during cross-tenant cloning.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId)))) so reparenting cannot affect rows belonging to other tenants.
- **server/tools.ts:4184** — SELECT from tenant-scoped table `memory_entries` executed without constraining to the caller tenant (no WHERE tenant_id = ...). This can leak cross-tenant memory facts (slides QA lessons).
  - _Fix:_ Add tenant scoping to the query. Pass the caller tenantId and include it in the WHERE clause (or use Drizzle eq(table.tenant_id, tenantId)). Example: SELECT fact FROM memory_entries WHERE tenant_id = ${params._tenantId} AND persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ...; or use the Drizzle/sql API with a parameterized tenant filter.
- **server/tools.ts:4496** — SELECT that reads project_id from `conversations` / `project_conversations` is executed without constraining by tenant (no WHERE tenant_id = ...). Even though ownership is asserted earlier via assertConversationInTenant, the raw SQL itself lacks the required tenant filter.
  - _Fix:_ Include tenant scoping in the SQL and/or use a parameterized read helper that enforces tenant ownership in the same DB call. For example: SELECT COALESCE((SELECT project_id FROM conversations WHERE id = ${convId} AND tenant_id = ${params._tenantId} AND project_id IS NOT NULL), (SELECT project_id FROM project_conversations WHERE conversation_id = ${convId} AND tenant_id = ${params._tenantId} LIMIT 1)) AS pid. Prefer using Drizzle helpers to add eq(table.tenant_id, tenantId) rather than relying only on prior in-memory assertions.
- **server/tools.ts:6893** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The query inserts project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by but omits tenant_id even though the code relies on application-level filtering only.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. VALUES (${params._tenantId}, ${params.project_id}, ...)) or use Drizzle's insert with the project_files table object and set tenantId: params._tenantId. Ensure the tenant passed has been validated (assertProjectInTenant already called) and make tenant_id NOT NULL enforcement explicit in the app-level insert.
- **server/tools.ts:8853** — UPDATE on tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE; it lacks a tenant_id constraint (no WHERE tenant_id = <caller tenant>). This allows updating a row by id without confirming it belongs to the caller's tenant.
  - _Fix:_ Constrain UPDATEs to the caller's tenant, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or add an explicit ownership check before performing the update (and fail-closed if ownership cannot be confirmed). Use Drizzle's eq(proposedSkills.tenantId, tenantId) helper to enforce this everywhere.
- **server/tools.ts:8895** — UPDATE on tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE; it lacks a tenant_id constraint (no WHERE tenant_id = <caller tenant>). This appears when marking a proposal rejected — it can modify proposals across tenants by id.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or perform an ownership assertion before the UPDATE. Audit other updates on tenant-scoped tables to ensure they always include tenant_id checks.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table (browser_workflows) has no tenant_id constraint in the WHERE clause. The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` can modify a row belonging to another tenant if an id from another tenant is supplied.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example, include tenant_id in the WHERE clause: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or the equivalent Drizzle .where(eq(table.tenantId, tenantId))). Always ensure the tenantId used is the trusted ctx.tenantId and fail closed if missing.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files does not set tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` omits tenant_id, violating the invariant that INSERTs into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include the tenant_id column/value in the INSERT (sourced from the trusted ctx.tenantId) or otherwise ensure the inserted row is explicitly attributed to the tenant. Example: `INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ${fileName}, ...)`. Validate ctx.tenantId is present and a number before inserting.
- **server/tools/domains/files/handlers.ts:838** — UPDATE on projects table lacks tenant scoping. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` can update a project belonging to a different tenant.
  - _Fix:_ Add tenant scoping to the UPDATE to ensure it only affects the caller's tenant, e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(projects.tenantId, tenantId))). Prefer failing the operation if ctx.tenantId is missing.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE on tenant-scoped table `knowledge_triples` is executed without constraining by tenant_id (missing `AND tenant_id = ${tenantId}` in the WHERE clause). Writes must be explicitly tenant-scoped to prevent cross-tenant modification.
  - _Fix:_ Add an explicit tenant filter to the UPDATE, e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Prefer using Drizzle's query builder (e.g. .where(eq(table.tenantId, tenantId))) or parameterized sql templates for all UPDATE/DELETE operations on tenant-scoped tables to ensure tenant isolation.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table memory_entries lacks tenant_id constraint. The code updates memory_entries WHERE id = ${id} without also constraining tenant_id to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller tenant as well, e.g. add AND tenant_id = ${tid} to the WHERE clause (or use Drizzle's .where(eq(table.tenantId, tid)) combined with the id). This prevents cross-tenant row modification if an id from another tenant is referenced.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table memory_entries lacks tenant_id constraint. The code updates memory_entries WHERE id = ANY(${ids}) and does not include tenant_id = <caller tenant>.
  - _Fix:_ Include the tenant constraint in the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}, or use a Drizzle .where(...) call that ensures tenant_id equals the caller's tenant. This guarantees the update only touches rows belonging to the tenant.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table memory_entries lacks tenant_id constraint. The code updates memory_entries WHERE id = ${id} without constraining tenant_id to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} (or use Drizzle .where(eq(table.tenantId, tid))). Always require tenant_id in write predicates to enforce tenant isolation.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` performed without filtering by tenant_id. The query returns pairing rows across all tenants (`WHERE category = 'messaging_pairing' AND title = ...`) which violates the invariant that every read against a tenant-scoped table must include a tenant_id constraint.
  - _Fix:_ Avoid scanning the entire agent_knowledge table across tenants. Include tenant_id in the WHERE clause (or otherwise restrict by an explicitly-validated owning tenant) when possible. If the code needs to detect contested pairings across tenants, implement an explicit, auditable cross-tenant check (separate admin-safe query path) rather than an unscoped SELECT in normal request flow. At minimum, document and gate this behavior to an admin-only maintenance path or change the data model so pairings include a canonical tenant_id column indexed for safe tenant-scoped lookups.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` by conversation id is executed without a tenant_id filter. Reading the conversations table without scoping by tenant allows cross-tenant lookups and violates the requirement that tenant-scoped table reads include the caller's tenant constraint.
  - _Fix:_ Constrain the SELECT to the claimed tenant: add AND tenant_id = ${info.tenantId} (or supply the expected tenantId as a parameter and validate it) so the query only returns rows belonging to the claimed tenant. Alternatively, perform a parameterized lookup that checks both id and tenant_id in a single query and fail if mismatch.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger WHERE id = ${reservationId} is executed without constraining by tenant_id. This allows updating an arbitrary ledger row by id across tenants if an attacker can supply/guess a reservationId.
  - _Fix:_ Require tenantId in settleVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or verify the reservation row belongs to the expected tenant before updating. Make settleVentureReservation accept tenantId (or validate ownership via a guarded lookup) and enforce tenant-scoped WHERE filters on the UPDATE.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger WHERE id = ${reservationId} (releaseVentureReservation) is executed without tenant scoping. This allows releasing (or modifying) reservation rows belonging to other tenants by id.
  - _Fix:_ Require and pass tenantId into releaseVentureReservation and include tenant_id in the WHERE clause (WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or first SELECT the row and verify its tenant before performing the UPDATE. Always enforce tenant-scoped filters on UPDATE/DELETE against tenant-scoped tables.
- **server/video-job-runner.ts:226** — System recovery routine recoverStaleVideoJobs performs UPDATE on `videoJobs` across all rows matching stale criteria without scoping by tenant_id. This writes to tenant-scoped rows for all tenants in a single unscoped operation, violating the 'must include tenant filter' invariant and risking unintended cross-tenant modifications.
  - _Fix:_ Either make recoverStaleVideoJobs explicitly an admin-only maintenance routine (document + restrict execution context) or iterate per-tenant and apply tenant-scoped updates (e.g. WHERE tenant_id = ${tenantId} AND ...). If a global sweep is required, ensure it runs in a trusted admin context and clearly documents why unscoped writes are acceptable; ideally add an additional guard (e.g. require an env flag and run only from a job runner process with elevated privileges).
- **server/wiring-invariants.ts:367** — COUNT/MAX query against subsystem table uses direct table interpolation and queries the entire table without any tenant_id constraint (e.g. `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`). If that table is tenant-scoped this reads data across all tenants.
  - _Fix:_ If the table is tenant-scoped, restrict the check to an explicit tenant (e.g. add WHERE tenant_id = ${ADMIN_TENANT_ID} or accept a tenantId parameter) or otherwise run this only within an authenticated admin context and document that this is an intentional cross-tenant diagnostic. Avoid interpolating table/column names from untrusted input; keep them from constants and, when possible, use parameterized queries for tenant values.
- **server/wiring-invariants.ts:535** — Global COUNT/size probe over memory tables uses raw SQL without a tenant filter (e.g. `SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`). Several entries in MEMORY_TABLES (messages, memory_entries, graph_memory, etc.) are typically tenant-scoped — this reads counts across all tenants.
  - _Fix:_ If the intent is per-tenant monitoring, add an explicit tenant_id WHERE clause (and pass an ADMIN_TENANT_ID when run as a platform admin). If global aggregation is required, ensure the code is only executable by a trusted admin process and document the cross-tenant intent. Prefer parameterized queries and avoid string-interpolating table names from any external input.
- **server/wiring-invariants.ts:561** — COUNT query against graph_memory runs without scoping by tenant (SELECT COUNT(*)::int AS cnt FROM graph_memory). If graph_memory is tenant-scoped, this reads data across all tenants.
  - _Fix:_ Scope this query to a tenant when used in per-tenant checks (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} for admin diagnostics) or ensure it runs only in an admin-only diagnostic path. Document and protect intentional cross-tenant reads; otherwise add tenant filtering to enforce isolation.

### MEDIUM (47)

- **server/agent-channels.ts:194** — Query joining channel_messages to channel_subscriptions does not constrain the subscription row by tenant (JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}). If channel_subscriptions is tenant-scoped, the join can pull subscriptions from other tenants.
  - _Fix:_ Ensure the join restricts channel_subscriptions to the caller's tenant (e.g. add AND cs.tenant_id = ${tenantId} in the JOIN condition or include cs.tenant_id = ${tenantId} in the WHERE clause). Always include tenant predicates for every tenant-scoped table in multi-table queries.
- **server/agentic-engines.ts:341** — gatherOperationalData issues a COUNT query against `heartbeat_logs` without filtering by tenant_id (SELECT COUNT(...) FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads tenant-scoped telemetry for all tenants.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND ...) or change the function to explicitly operate as an admin-only aggregator with proper authorization. Avoid unscoped reads of tenant-scoped tables.
- **server/agentic-engines.ts:448** — runPredictiveEngine selects recent heartbeat logs (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...) without a tenant_id filter, returning logs across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or scope the retrieval to the targeted tenant. Ensure every access to heartbeat_logs includes the tenant predicate.
- **server/agentic-engines.ts:516** — runOptimizationEngine queries `heartbeat_logs` aggregated by task_name/status without a tenant filter (SELECT ... FROM heartbeat_logs GROUP BY ...). This can mix data across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the query (or run per-tenant) so aggregated metrics are tenant-scoped. Avoid global aggregation over tenant-scoped tables unless done by an authorized multi-tenant admin routine.
- **server/agentic-engines.ts:531** — runOptimizationEngine selects heartbeat_tasks (SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ...) without a tenant_id filter; heartbeat_tasks are tenant-scoped in other places and must be filtered.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause so only the caller's tenant tasks are returned, or make the function explicitly admin-only and enforce authorization.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT FROM conversations scans the conversations table across all tenants without a tenant_id filter.
  - _Fix:_ If this backfill is intended to be multi-tenant admin work that's fine in principle, but mark it explicitly and ensure it runs in an admin context. Otherwise, constrain the query per-tenant (e.g. SELECT ... WHERE tenant_id = ${tenantId}) or iterate tenant IDs and process each tenant separately to preserve the explicit per-tenant filtering invariant.
- **server/auto-memorize.ts:100** — isDuplicate reads recent memory_entries via SELECT ... FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' without any tenant_id constraint, scanning memories across tenants.
  - _Fix:_ Constrain dedupe checks to the tenant being processed by adding tenant_id = ${tenantId} to the WHERE clause, or run the dedupe per-tenant (e.g. iterate tenants or accept an explicit tenant context). If the design intentionally dedupes across tenants, document this as an explicit admin operation and run it under admin context with clear justification and protections.
- **server/business-tools.ts:75** — INSERT INTO `invoices` accepts params.customer_id and inserts it directly without first verifying that the referenced customer row belongs to the caller's tenant.
  - _Fix:_ Before inserting an invoice that references a customer_id, verify ownership (e.g. SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid}) and fail if it doesn't exist/own. Alternatively, omit customer_id or require a validated customer lookup API that asserts tenant ownership.
- **server/business-tools.ts:321** — INSERT INTO `contracts` includes params.customer_id directly without validating that the referenced customer belongs to the caller's tenant.
  - _Fix:_ Validate the customer_id belongs to the tenant (SELECT ... WHERE id = ${customer_id} AND tenant_id = ${tid}) prior to inserting. If the project/customer FK must be enforced, include tenant checks and reject/use null when ownership is not confirmed.
- **server/business-tools.ts:152** — INSERT INTO `expenses` saves project_id from caller input without verifying the referenced project belongs to the caller's tenant.
  - _Fix:_ Verify project ownership before writing (SELECT id FROM projects WHERE id = ${project_id} AND tenant_id = ${tid}) or strip/ignore project_id when it is not owned by the tenant. Enforce explicit tenant checks for any foreign id stored on tenant-scoped rows.
- **server/business-tools.ts:277** — INSERT INTO `customer_interactions` uses params.customer_id provided by the caller without first verifying that the referenced customer belongs to the caller's tenant.
  - _Fix:_ Validate customer ownership prior to inserting (SELECT id FROM customers WHERE id = ${customer_id} AND tenant_id = ${tid}) and reject if it does not match the tenant. Ensure all foreign ids are validated before persisting.
- **server/channel-routing.ts:48** — setChannelRoute inserts a persona_id provided by the caller into a global channel_routes table without validating that the persona belongs to the caller's tenant.
  - _Fix:_ Require tenant context when creating/updating channel routing. Validate that the supplied persona_id belongs to the caller's tenant before inserting into the mapping, or add tenant_id to channel_routes and INSERT/UPDATE with tenant_id = <callerTenantId> to prevent cross-tenant association.
- **server/chat-engine.ts:572** — SELECT from project_files uses project_id but does not include tenant_id in the WHERE clause. Although projectId was validated earlier in this function, the table is still queried without an explicit tenant filter.
  - _Fix:_ Add an explicit tenant constraint (e.g. AND tenant_id = ${tenantId}) or perform the SELECT via a JOIN with projects WHERE p.id = project_id AND p.tenant_id = ${tenantId}. Prefer explicit tenant_id checks on every tenant-scoped table access for consistent protection.
- **server/chat-engine.ts:583** — SELECT from project_notes uses project_id but omits tenant_id. The query relies on earlier project ownership verification but does not include an explicit tenant_id clause on the project_notes table.
  - _Fix:_ Include tenant scoping on the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join to projects with a tenant_id check. Use explicit tenant filters on tenant-scoped tables to avoid accidental cross-tenant access if calling context changes.
- **server/chat-engine.ts:4591** — Subsequent update storage.updateMemoryEmbedding(entry.id, emb) updates a memory row by id without any tenant context or ownership validation. If the initial create did not enforce tenant scoping, this update could affect rows across tenants.
  - _Fix:_ Modify updateMemoryEmbedding to accept and enforce tenantId (e.g. updateMemoryEmbedding(entryId, emb, tenantId)) or perform an explicit ownership check before calling it (ensure the entry.id returned from create belongs to the expected tenant). Prefer making storage-layer methods require tenantId and validate ownership server-side to avoid reliance on application callers.
- **server/commitment-drafter.ts:146** — SELECT from tenant-scoped table `commitments` without tenant_id constraint (enumerates commitments across all tenants).
  - _Fix:_ Avoid querying tenant-scoped tables across tenants. If you truly need to enumerate tenants, read the tenant list from the authoritative tenants table (or require admin context) and ensure any subsequent reads/writes are explicitly scoped to the target tenant_id. Add explicit access checks or restrict this query to an admin-only path.
- **server/data-protection.ts:72** — UPDATE on conversations performed without constraining by tenant_id (missing AND tenant_id = ${tenantId}). Although the function earlier selected the conversation with tenant filtering, the subsequent UPDATE uses only WHERE id = ${conversationId}, which breaks the invariant that every write against tenant-scoped tables must include the caller's tenant predicate.
  - _Fix:_ Include the tenant predicate on the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise perform the UPDATE inside a transaction/context that enforces tenant scope (with explicit verification of ownership before the write).
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot() runs SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' with NO tenant filter. `messages` is tenant-scoped; this query aggregates across all tenants and may leak cross-tenant activity counts into logic that should be per-tenant or explicitly admin-only.
  - _Fix:_ If this routine must operate per-tenant, add tenant_id filter parameters. If it's intentionally global, restrict execution to a trusted admin/global-only path and document why reading cross-tenant data is acceptable. Prefer explicit tenant-scoped aggregation when used to make automated decisions.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot() runs SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... with NO tenant filter. `tool_performance` appears tenant-scoped; this query aggregates across tenants.
  - _Fix:_ Scope the query to a tenant (add tenant_id = ...) if the decision logic is per-tenant. If the operation intentionally needs global stats, ensure it runs only in a trusted global/admin context and document the rationale.
- **server/dormant-deprecation.ts:203** — getEverInvokedTools() executes SELECT DISTINCT tool_name FROM tool_performance with NO tenant filter. This reads tenant-scoped usage data for all tenants.
  - _Fix:_ Either add a tenant filter to compute per-tenant candidates, or restrict this function to admin-only/global usage and clearly document the requirement and access controls.
- **server/dormant-deprecation.ts:214** — getActiveTools14d() SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... executes without tenant scoping, reading across tenants.
  - _Fix:_ Add tenant_id filter when used in per-tenant flows, or gate the function to admin-only/global flows with explicit justification. Prefer explicit WHERE tenant_id = ${tenantId} for tenant-scoped tables.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from the request/query in vectorSearchMemory: 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}. This interpolates a request-derived value into raw SQL (injection + safety risk).
  - _Fix:_ Avoid sql.raw interpolation of request-derived values. Use parameterized bindings / Drizzle placeholders where possible, or a safe binary representation binding supported by the pgvector integration. Validate and escape thoroughly and, better, pass the vector as a parameter rather than embedding a string literal into raw SQL.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated query-derived vector in vectorSearchKnowledge: 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}. This interpolates request-derived data into raw SQL.
  - _Fix:_ Replace sql.raw interpolation with parameterized query bindings for the vector, or use a driver/ORM mechanism that can bind pgvector values safely. If raw is unavoidable, ensure strict validation and consider binding via prepared statements rather than string interpolation.
- **server/heartbeat.ts:1724** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id filter. heartbeat_logs are likely tenant-scoped so this reads logs across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE task_name = ... AND tenant_id = ${tenantId}) or call a storage API that enforces the task's tenant context before reading logs.
- **server/lib/restraint-trust.ts:88** — Dynamic SQL identifier injected via sql.raw(column) where `column` is derived (indirectly) from caller input (`opts.outcome`). Although the value is validated/allowlisted earlier, using sql.raw for a dynamic column name introduces an injection/safety surface and is fragile.
  - _Fix:_ Avoid interpolating SQL identifiers with sql.raw. Instead, map the allowed outcomes to fixed, prebuilt parameterized UPDATE statements (e.g. switch/case for each allowed column) or use a safe identifier helper provided by your SQL builder (e.g. sql.identifier(...) if available) after strict allowlisting. Example: switch on outcome and run `UPDATE trust_scores SET action_alpha = action_alpha + $1 ... WHERE tenant_id = $2 AND persona_id = $3 AND category = $4` with delta and tenant/persona/category as parameters. Keep the tenant_id WHERE clause to enforce tenant isolation.
- **server/memory-graph.ts:125** — refreshCategoryCount issues an UPDATE on memory_categories with WHERE id = ${categoryId} but does not constrain by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}) or accept/require tenantId as parameter and use it in the WHERE clause so a category count update cannot affect other tenants.
- **server/memory-graph.ts:181** — SELECT existence check on memory_links (SELECT 1 FROM memory_links WHERE (source_memory_id = ... OR target_memory_id = ...)) lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Constrain the existence check by tenant (e.g. add AND tenant_id = ${tenantId}) or otherwise ensure the query uses a tx bound to the tenant and includes an explicit tenant filter so the check cannot cross tenants.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — missing tenant_id constraint on UPDATE (the earlier SELECT filtered by tenant, but the UPDATE itself has no tenant condition).
  - _Fix:_ Include tenant filtering on the UPDATE (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) to avoid modifying another tenant's triples if ids collide or are leaked.
- **server/minds-engine.ts:455** — SELECT messages m JOIN conversations c ON c.id = m.conversation_id AND c.tenant_id = ${tenantId} WHERE m.conversation_id = ${run.conversationId} — the query lacks an explicit tenant_id constraint on the messages table in the WHERE clause.
  - _Fix:_ Add an explicit tenant constraint for messages (e.g. AND m.tenant_id = ${tenantId}) or include an explicit WHERE clause equating the conversation's tenant and/or the message tenant to the caller tenant. Don’t rely solely on the JOIN condition for tenant scoping; use an explicit tenant filter on the tenant-scoped table being selected.
- **server/minerva-planner.ts:373** — Probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} — missing tenant_id filter on this SELECT used to determine idempotency/result of the attempted CAS.
  - _Fix:_ Use a tenant-scoped probe (e.g. WHERE id = ${args.planId} AND tenant_id = ${tenantId}) so status checks do not reveal or act on other tenants' plans.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 — missing tenant_id filter when checking for child revision existence.
  - _Fix:_ Restrict the query to the tenant (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}) so existence checks for child plans cannot cross-tenant.
- **server/research-engine.ts:470** — INSERT INTO project_notes (project_id, note, author, created_at) does not set tenant_id. If project_notes is tenant-scoped with a NOT NULL tenant_id column, this insert will violate the invariant or create orphan/misattributed rows.
  - _Fix:_ If project_notes is tenant-scoped, include tenant_id in the INSERT (VALUES (${projectId}, ${note}, ${author}, ${createdAt}, ${tenantId})) and/or set tenant_id from a validated/owned project (using assertProjectInTenant). Ensure tenant_id is derived from a trusted source (project ownership assertion) not from untrusted input.
- **server/routes.ts:2141** — INSERT into tenant-scoped table `presenter_sessions` uses tenantId from request body without validating caller's authority over that tenant (INSERT INTO presenter_sessions (tenant_id, ...) VALUES (${tenantId}, ...))
  - _Fix:_ Do not accept arbitrary tenantId from callers. Derive tenantId from the authenticated session (or otherwise verify the caller is a platform-admin allowed to create sessions for the supplied tenant). At minimum validate the tenant exists and the caller is authorized for that tenant before inserting.
- **server/routes.ts:2171** — SELECT presenter session by token (SELECT id, presentation_id, slides FROM presenter_sessions WHERE token = ${token}) in the repair-images endpoint is performed without any tenant ownership check before proceeding to mutate presenter_slide_images and presenter_sessions — the handler relies only on an internal key for protection
  - _Fix:_ Enforce an explicit tenant ownership check for the looked-up presenter_sessions row (e.g. check session.tenant_id equals an authenticated/authorized tenant or ADMIN_TENANT_ID) before performing inserts/updates. If this endpoint is intended to be admin-only, ensure that the authentication mechanism maps to an admin tenant or perform an explicit platform-admin gate and log/monitor use.
- **server/routes/conversations.ts:104** — UPDATE on `projects` lacks tenant filter (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). Although ownership was checked earlier, the SQL statement itself has no tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause to enforce tenant-scoped updates at the DB layer, even after prior ownership checks.
- **server/routes/projects.ts:35** — SELECT subqueries read project_files, project_notes, and project_conversations without an explicit tenant_id predicate.
  - _Fix:_ Constrain these subqueries to the caller's tenant. Example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or JOIN to projects and assert p.tenant_id = ${tenantId} so the query includes an explicit tenant_id filter.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} executes against a tenant-scoped table without an explicit tenant_id predicate in the query.
  - _Fix:_ Constrain the SELECT with the tenant, e.g. SELECT * FROM project_files pf JOIN projects p ON p.id = pf.project_id WHERE pf.project_id = ${projectId} AND p.tenant_id = ${tenantId} OR add AND pf.tenant_id = ${tenantId} if project_files stores tenant_id. Always include tenant_id in the WHERE clause for tenant-scoped tables.
- **server/routes/projects.ts:342** — SELECT file metadata from project_files (SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...}) lacks an explicit tenant_id constraint on the tenant-scoped table.
  - _Fix:_ Include tenant scoping in the query, for example: JOIN conversations/projects to assert tenant_id or add AND tenant_id = ${tenantId} to the WHERE clause (or derive tenant_id from the previously-verified project and include it in the SELECT predicate).
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} deletes from a tenant-scoped table without an explicit tenant_id predicate.
  - _Fix:_ Constrain the DELETE to the caller's tenant by adding tenant_id to the WHERE clause or joining to projects and checking p.tenant_id = ${tenantId}. Example: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}.
- **server/skillify.ts:223** — SELECT from tenant-scoped table `personas` is executed without tenant scoping (WHERE personas.id = ... but no tenant_id check). This can leak persona data from another tenant.
  - _Fix:_ Verify persona ownership before using it: add tenant filter in the query (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))) or call a helper that asserts the persona belongs to tenantId. Do not rely solely on later JS checks — enforce at SQL level.
- **server/storage.ts:615** — evictOldestConversationFacts() updates conversation_facts with WHERE id IN (...) but does not include tenant_id in the UPDATE WHERE clause (write against tenant-scoped table without tenant filter).
  - _Fix:_ Add a tenant constraint to the UPDATE, e.g. .where(and(inArray(conversationFacts.id, victims.map(v=>v.id)), eq(conversationFacts.tenantId, tenantId))), or compute victim ids per-tenant and include tenant_id to avoid accidental cross-tenant updates.
- **server/storage.ts:918** — getHeartbeatTask(id) performs SELECT FROM heartbeat_tasks WHERE id = ${id} with no tenant filter; heartbeat_tasks is tenant-scoped so this returns a task regardless of tenant ownership.
  - _Fix:_ Require caller-supplied tenantId (or derive tenant from parent context) and add eq(heartbeatTasks.tenantId, tenantId) to the WHERE clause, or otherwise verify ownership before returning the task.
- **server/storage.ts:981** — getDueHeartbeatTasks() SELECTs rows FROM heartbeat_tasks (tenant-scoped) with no tenant_id constraint — the scheduler reads tasks across all tenants without explicit tenant scoping.
  - _Fix:_ If the scheduler must operate across tenants, ensure the code that executes tasks enforces tenant-level isolation when performing task actions. If this function is intended for a single-tenant execution path, require tenantId and add eq(heartbeat_tasks.tenant_id, tenantId); otherwise document and restrict this admin/global behavior.
- **server/storage.ts:872** — getMemoryStats() falls back to a full-table select on memory_entries when tenantId is not provided, returning data across all tenants.
  - _Fix:_ Fail closed: require tenantId (or clearly restrict the API) or only allow full-table admin queries behind an explicit admin flag. Prefer making tenantId mandatory for tenant-scoped statistics APIs.
- **server/storage.ts:917** — getHeartbeatTasks() returns all heartbeat_tasks when no tenantId is provided (reads tenant-scoped table unscoped).
  - _Fix:_ Require tenantId or otherwise restrict the API to admin contexts; when a tenantId is supplied use eq(heartbeatTasks.tenantId, tenantId) so normal handlers cannot read other tenants' tasks.
- **server/tools.ts:4531** — SELECT from `presenter_sessions` is executed without an explicit tenant filter or ownership check. The query reads token for a presentation_id, but no tenant scoping is applied in SQL.
  - _Fix:_ If `presenter_sessions` is tenant-scoped, include tenant_id = ${params._tenantId} in the WHERE clause. If ownership is guaranteed elsewhere, still prefer adding tenant scoping to the SQL to satisfy the invariant and avoid relying solely on out-of-band checks.
- **server/video-job-runner.ts:204** — SELECT cancelRequested FROM `videoJobs` is executed filtering only by jobId (no tenant filter) in isCancelRequested(jobId). Reading tenant-scoped job rows without a tenant constraint violates the invariant and may leak or allow cross-tenant control signals.
  - _Fix:_ Change isCancelRequested to accept tenantId (or otherwise validate caller identity) and include tenant filtering in the query (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}). If the function must remain tenant-agnostic for internal system use, restrict its callers to an admin-only context and audit usage; prefer explicit tenant-scoped reads for request-handling paths.

### Suppressed — confirmed false positives (147)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() performs an UPDATE on the tenant-scoped table `agent_runs` using WHERE eq(agentRuns.id, runId) with NO tenant_id constraint — this allows a caller/process to modify another tenant's run row.
  - _Why false positive:_ agent_runs is updated by globally unique server-generated runId in internal worker helpers; no untrusted request boundary is shown here.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs via .where(eq(agentRuns.id, runId)) with NO tenant_id constraint — cross-tenant row modification is possible.
  - _Why false positive:_ state update uses internal server-generated runId primary key in worker-run helper, matching the internal-worker-pk pattern.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs with .where(eq(agentRuns.id, runId)) and omits tenant scoping, enabling cross-tenant writes.
  - _Why false positive:_ replaceRunState targets a server-generated agent_runs PK in internal orchestration code rather than caller-supplied tenant-crossing input.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs.status/result/completedAt using .where(eq(agentRuns.id, runId)) with NO tenant_id filter — this can mark another tenant's run as completed.
  - _Why false positive:_ completeRun updates a run by globally unique internal runId in server worker flow, fitting internal-worker-pk.
- **server/agentic/runs.ts:67** _(internal-worker-pk)_ — failRun() updates agent_runs.status/error/completedAt using .where(eq(agentRuns.id, runId)) without constraining tenant_id — allows cross-tenant row mutation.
  - _Why false positive:_ failRun is an internal run-lifecycle helper keyed by server-created runId, not a request-exposed tenant selector.
- **server/agentic/runs.ts:76** _(internal-worker-pk)_ — pauseRun() updates agent_runs.status/state using .where(eq(agentRuns.id, runId)) with NO tenant_id check — permits pausing runs from other tenants.
  - _Why false positive:_ pauseRun is another internal lifecycle mutation by server-generated run PK, matching internal-worker-pk.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL — missing tenant_id constraint in WHERE clause. This UPDATE can modify a conversation belonging to a different tenant if row ids are manipulated or results are poisoned.
  - _Why false positive:_ the prior SELECT joins projects and conversations on matching tenant_id and returns c.tenant_id for the exact conversation_id/project_id pair being updated.
- **server/data-protection.ts:125** _(upstream-verified)_ — DELETE FROM messages executed without tenant-scoping predicate. The purge loop deletes rows using conversation_id only (no tenant_id constraint). Per the tenant-isolation invariant, DELETEs against tenant-scoped tables must include tenant_id in WHERE to prevent accidental cross-tenant modifications.
  - _Why false positive:_ each delete is driven by conv.id values selected from conversations immediately above; messages are child rows of that verified parent conversation.
- **server/data-protection.ts:126** _(upstream-verified)_ — DELETE FROM compaction_archives executed without tenant-scoping predicate (no tenant_id in WHERE). This violates the requirement that writes/deletes on tenant-scoped tables must be constrained by tenant_id.
  - _Why false positive:_ each delete uses conversation_id from the immediately preceding verified conversations selection, so ownership is established by the parent row in the same call path.
- **server/data-protection.ts:127** _(upstream-verified)_ — DELETE FROM conversations executed without tenant_id predicate (DELETE WHERE id = ${conv.id}). Even though conv.id came from an earlier SELECT, the DELETE lacks the tenant predicate and thus violates the invariant requiring explicit tenant-level filtering on writes/deletes.
  - _Why false positive:_ DELETE uses conv.id from same-function SELECT of expired conversations that also fetched conv.tenant_id; this is an internal purge loop over rows already selected by DB, not caller input.
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM doc_chunks is executed with WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but WITHOUT tenant_id. This can delete another tenant's chunks if collection ids/doc_paths collide or are misused.
  - _Why false positive:_ Immediately above, code verifies doc_collections.id belongs to tenantId before deleting chunks for that collectionId; same call path proves tenant ownership of the parent collection.
- **server/embeddings.ts:726** _(internal-worker-pk)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} (in backfillEmbeddingVecs) lacks any tenant constraint — write can affect rows in any tenant.
  - _Why false positive:_ Backfill job is internal maintenance code selecting rows server-side then updating by globally unique row.id, not using request-derived input.
- **server/embeddings.ts:743** _(internal-worker-pk)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} (in backfillEmbeddingVecs) has no tenant_id constraint and will update rows across tenants.
  - _Why false positive:_ Backfill job is internal maintenance code selecting rows server-side then updating by globally unique row.id, not using request-derived input.
- **server/firecrawl.ts:184** _(upstream-verified)_ — UPDATE on scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant constraint. This allows updating a scraped page row by id without scoping to the caller's tenant_id.
  - _Why false positive:_ The row id comes from an immediately preceding SELECT constrained by url and tenantId in the same function, so the update target was already tenant-verified in this call path.
- **server/firecrawl.ts:307** _(upstream-verified)_ — UPDATE on scrapedPages inside the crawl loop uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant constraint. Same cross-tenant write risk as above.
  - _Why false positive:_ The row id comes from an immediately preceding SELECT constrained by pageUrl and tenantId in the same loop, so the updated row was already tenant-verified.
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT INTO project_files does not include tenant_id in the column list or values. Per the invariant, INSERTs into tenant-scoped tables must explicitly set tenant_id — otherwise this either fails (if tenant_id is NOT NULL) or creates uncategorized/incorrectly-scoped rows.
  - _Why false positive:_ Schema note says project_files has no tenant_id; code also re-validates project ownership with assertProjectInTenant before insert, so isolation is via tenant-scoped parent project.
- **server/google-workspace.ts:2364** _(fk-transitive)_ — INSERT (thumb) into presenter_slide_images for quality='thumb' occurs without tenant_id or ownership check — another write to a tenant-scoped table lacking tenant scoping.
  - _Why false positive:_ presenter_slide_images is keyed by session_id to tenant-scoped presenter_sessions; child table insert without tenant_id is by-design transitive isolation
- **server/google-workspace.ts:2378** _(upstream-verified)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint — updating a tenant-scoped row without verifying ownership.
  - _Why false positive:_ sessionId comes from presData.id of the presenter session just created in the same flow, so the id is already the owned target row for this update
- **server/lib/scheduled-post-runner.ts:61** _(global-table)_ — SELECT from tenant-scoped table `scheduled_posts` performed without any tenant_id filter (CTE that picks due rows is unbounded across tenants).
  - _Why false positive:_ heartbeat-style scheduled runner is an intentional system worker that polls due scheduled_posts across tenants for platform-wide execution
- **server/lib/scheduled-post-runner.ts:70** _(upstream-verified)_ — UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id updates tenant-scoped rows without including tenant_id in the WHERE clause — writes are not constrained to a tenant.
  - _Why false positive:_ UPDATE only affects ids selected and locked by the same CTE, and returns tenant_id for each claimed row; target set is established in the immediately preceding query
- **server/lib/scheduled-post-runner.ts:164** _(upstream-verified)_ — UPDATE scheduled_posts ... WHERE id = ${row.id} is a tenant-scoped write without any tenant_id guard — row.id alone is used to identify the row, allowing cross-tenant updates if called in wrong context.
  - _Why false positive:_ row.id/row.tenant_id come from the runner's prior claim query RETURNING scheduled_posts rows, so this follow-up update is operating on already-claimed rows in the same worker path
- **server/memory-graph.ts:194** _(fk-transitive)_ — INSERT INTO memory_links does not set tenant_id. memory_links is a tenant-scoped table, so inserting without tenant_id will create unscoped links (cross-tenant isolation violation).
  - _Why false positive:_ schema note says memory_links has no tenant_id; links are between tenant-scoped memory_entries and are isolated transitively through source/target memory foreign keys
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog updates plans row by id without constraining tenant_id (UPDATE plans ... WHERE id = ${planId}). This writes to a tenant-scoped table without scoping to the plan's tenant.
  - _Why false positive:_ appendExecutionLog is an internal executor helper updating plans by server-side planId, not a direct untrusted request boundary
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim that flips plan to 'executing' does not constrain by tenant (UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status = 'approved'). A caller-supplied planId could cause a caller to claim/modify another tenant's plan.
  - _Why false positive:_ executePlan is internal worker logic claiming a plan by server-side planId; this is not shown as coming from req/body/tool args and returns tenant_id for subsequent work
- **server/plan-executor.ts:527** _(upstream-verified)_ — Plain UPDATE marking a plan failed does not include a tenant constraint (UPDATE plans SET status = 'failed' WHERE id = ${planId}). This allows writes to plans across tenants by id only.
  - _Why false positive:_ the plan row was just claimed via RETURNING at line 513 in the same function, so this failure update targets the same already-verified claimed row
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final status update of the plan (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint; execution_log appends similarly without tenant scoping.
  - _Why false positive:_ final status update occurs after the same-function claim RETURNING the plan row and tenant_id; it is the terminal write on that already-claimed plan
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE resetting plan to 'failed' (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') is missing tenant scoping.
  - _Why false positive:_ Error-path update is inside server-invoked executePlan flow keyed by server-side planId, not from an untrusted request boundary.
- **server/proactive-engine.ts:138** _(upstream-verified)_ — SELECT from proactive_actions by id omits tenant scoping — `SELECT persona_id FROM proactive_actions WHERE id = ${actionId}` can leak cross-tenant data / return rows not owned by caller.
  - _Why false positive:_ Immediately prior UPDATE uses id and tenant_id; subsequent SELECT by same id is in same call path after ownership-scoped mutation.
- **server/process-governor.ts:199** _(global-table)_ — Query against heartbeat_logs has no tenant filter: `SELECT AVG(duration_ms) as avg_ms FROM heartbeat_logs ...` will aggregate across all tenants.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column: NO and is used here as platform-wide performance telemetry.
- **server/process-governor.ts:239** _(global-table)_ — Query counting distinct failing persona_ids from heartbeat_logs lacks tenant constraint: `SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ...` can include other tenants' logs.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column: NO; query is over a platform-global log table rather than a tenant-scoped table.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks uses only id in WHERE (`UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id}`) without tenant scoping — risk of modifying another tenant's row if ids overlap or attacker supplies crafted state.
  - _Why false positive:_ t.id comes from a prior SELECT of heartbeat_tasks scoped by ht.tenant_id = tenantId in the same loop.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks uses only id in WHERE (`UPDATE agent_desks SET ... WHERE id = ${desk.id}`) with no tenant filter.
  - _Why false positive:_ desk.id comes from a prior SELECT of agent_desks scoped by ad.tenant_id = tenantId in the same call path.
- **server/project-brain.ts:359** _(upstream-verified)_ — Selecting messages by conversation id lacks tenant scoping: `SELECT role, content FROM messages WHERE conversation_id = ${conv.id}` — allows reading messages from other tenants when run unscoped.
  - _Why false positive:_ messages are read only for conv.id values just selected from conversations under the current proj.id in the same loop; no untrusted cross-tenant id boundary shown
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT from code_proposals by id omits tenant scoping: `SELECT ... FROM code_proposals WHERE id = ${proposalId}` — verifyProposalById takes only proposalId and does not validate tenant ownership, enabling cross-tenant reads.
  - _Why false positive:_ server-side verifier worker reads by globally unique proposalId argument, not from req/body in shown code; function is queued internally
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals used to clear rebased_at is not scoped by tenant: `UPDATE code_proposals ... WHERE id = ${proposalId}` — allows mutating proposals from other tenants.
  - _Why false positive:_ internal verifier worker updates proposal row by server-side proposalId primary key during race handling
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist() updates code_proposals verification fields without tenant scoping: `UPDATE code_proposals SET ... WHERE id = ${proposalId}` — this can modify another tenant's proposal record.
  - _Why false positive:_ persist() is only used by internal proposal verifier flow and updates by server-side proposalId pk
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — Rebase path updates code_proposals status without tenant scoping: `UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId}` — no tenant constraint present.
  - _Why false positive:_ attemptRebaseOnDrift is part of internal verifier worker path mutating one proposal by server-side pk
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — When LLM hallucination detected, code updates validation_result on code_proposals without tenant scoping: `UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId}`.
  - _Why false positive:_ hallucination bookkeeping update is within internal verifier worker acting on trusted proposalId pk
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — Persisting rebased code_diff writes to code_proposals with `WHERE id = ${proposalId}` but no tenant filter: `UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId}`.
  - _Why false positive:_ rebased diff persistence is internal worker mutation keyed by trusted proposalId pk
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE to tenant-scoped table memory_entries performed without a tenant_id/persona_id constraint (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This updates by id only and can modify another tenant's row.
  - _Why false positive:_ storedId comes from immediately preceding SELECT on memory_entries constrained by tenant_id and persona_id in the same call path
- **server/routes.ts:4330** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' omits tenant_id. The code inserts: INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${snapshot.slice(0, 5000)}, ${'system:context-guard'}) — tenant_id is not provided, and the table requires tenant_id (no DB-level RLS/default).
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated transitively via project_id parent
- **server/sculptor.ts:94** _(upstream-verified)_ — UPDATE sculptor_sessions executes without a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id}). This allows an attacker or bug that supplies/guesses an id to mutate a session belonging to another tenant.
  - _Why false positive:_ session.id is from the row just inserted with params.tenantId in the same function, so this update targets that known-owned session
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE sculptor_sessions sets conversation_id without a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). Missing tenant filter can allow cross-tenant mutation if an id is misused.
  - _Why false positive:_ Updates the freshly inserted session using server-generated session.id from the same call path; not sourced from an untrusted request boundary.
- **server/sculptor.ts:162** _(internal-worker-pk)_ — UPDATE sculptor_sessions (monitorSession) updates status/result/completed_at with WHERE id = ${sessionId} but no tenant_id constraint. This permits changing another tenant's session record if sessionId is guessed or leaked.
  - _Why false positive:_ Background monitor updates a row by the server-created sessionId captured at session creation, not attacker-controlled input.
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is inserting into a tenant-scoped join table without setting tenant_id. The table likely requires tenant_id (NOT NULL) and this will create tenant-ambiguous/incorrect rows.
  - _Why false positive:_ project_conversations has no tenant_id column and links tenant-scoped parent/child records transitively via project_id and conversation_id.
- **server/self-reflection.ts:366** _(upstream-verified)_ — storeLesson() issues an UPDATE memory_entries ... WHERE id = <existing id> without constraining by tenant_id. This allows modifying another tenant's memory entry by ID.
  - _Why false positive:_ updated row id comes from prior SELECT on memory_entries scoped by tenant_id = tenantId in same call path
- **server/skillify.ts:196** _(upstream-verified)_ — SELECT from tenant-scoped table `conversations` is executed without scoping to the caller's tenant (no tenant_id constraint in WHERE).
  - _Why false positive:_ same function immediately verifies conv[0].tenantId === tenantId before using conversation data
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed using only conversationId (WHERE messages.conversationId = ... ) with NO tenant_id constraint.
  - _Why false positive:_ messages are fetched only after parent conversation ownership is checked in same function
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` in distillIntent() (recentMsgs) uses only conversationId in WHERE and lacks tenant_id constraint.
  - _Why false positive:_ distillIntent first calls analyzeConversation(conversationId, tenantId), which verifies the conversation belongs to tenantId
- **server/skillify.ts:331** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` in skillifyConversation() (recentMsgs) uses only conversationId and orders by date — no tenant_id constraint in the SQL.
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), which verifies tenant ownership of the conversation
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id. Query: db.select().from(customTools).where(eq(customTools.name, parsed.name))
  - _Why false positive:_ tool name is transformed by tenantToolName(parsed.name, tenantId), so lookup is tenant-derived name rather than arbitrary cross-tenant selector
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id without constraining tenant_id. Query: db.select().from(customTools).where(eq(customTools.id, inserted.id))
  - _Why false positive:_ reads back the just-inserted custom_tools row by server-generated inserted.id within same server-side flow
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `custom_tools` uses only PK filter (eq(customTools.id, tool.id)) with no tenant_id constraint.
  - _Why false positive:_ updates custom_tools by tool.id on internally passed just-created row during server-side validation/fix flow, not from untrusted request input
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE increments usageCount on tenant-scoped `custom_tools` using only eq(customTools.id, tool.id) with no tenant_id constraint.
  - _Why false positive:_ tool row is selected just above with name and tenant_id = tenantId, then updated by its server-fetched PK in same call path
- **server/tools.ts:2978** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The row is created without explicitly setting tenant scope, allowing cross-tenant data contamination or orphaned rows.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via verified parent project_id after ownerCheckF on projects.tenant_id
- **server/tools.ts:2992** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The note row is created without tenant scoping, risking cross-tenant writes.
  - _Why false positive:_ project_notes has no tenant_id and insert is tied to verified tenant-owned parent project via ownerCheckN
- **server/tools.ts:2874** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` (create path) does not include tenant_id. The project_conversations row is inserted without an explicit tenant_id column/value.
  - _Why false positive:_ project_conversations has no tenant_id and link row is transitively scoped through tenant-owned project and conversation already constrained to tenant before insert
- **server/tools.ts:3016** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` (link_conversation path) does not include tenant_id. The link is created without explicitly recording tenant ownership.
  - _Why false positive:_ project_conversations has no tenant_id and insert occurs only after verifying both project and conversation belong to the tenant
- **server/tools.ts:4507** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` omits tenant_id column entirely. Per the invariant every INSERT into tenant-scoped tables must explicitly set tenant_id (no DB defaults / RLS).
  - _Why false positive:_ project_notes has no tenant_id and projectId is checked with assertProjectInTenant before insert
- **server/tools.ts:4511** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id column and thus does not record the tenant in the row. This violates the requirement that every INSERT set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and projectId is verified tenant-owned before insert
- **server/tools.ts:6359** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (from generate_audio) omits tenant_id. The write uses params.project_id but does not include tenant_id in the inserted row.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by assertProjectInTenant on the parent project
- **server/tools.ts:6892** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (from create_slideshow_video) omits tenant_id in the VALUES. Every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and insert is allowed only after assertProjectInTenant validates the parent project
- **server/agentic-engines.ts:148** _(upstream-verified)_ — UPDATE ai_insights SET action_taken = ... WHERE id = ${insight.insightId} omits tenant constraint. This updates a tenant-scoped row by id without verifying tenant ownership.
  - _Why false positive:_ The shown UPDATE already includes AND tenant_id = ${insight.tenantId}, so tenant ownership is enforced at this line.
- **server/agentic/runs.ts:98** _(internal-worker-pk)_ — withRun() creates a run (correctly sets tenantId) but then calls appendStep/completeRun/failRun which themselves do NOT enforce tenant scoping — the combined call path lets in-process logic mutate run rows without validated tenant checks.
  - _Why false positive:_ Run mutation helpers operate by server-generated run primary key within an internal helper call path, not directly from an untrusted tenant request boundary.
- **server/auth.ts:180** _(upstream-verified)_ — Lookup of apiKeys by keyHash queries the apiKeys table without scoping by tenant_id. This selects across all tenants to find a matching API key.
  - _Why false positive:_ API-key auth intentionally resolves tenant from a globally presented secret; the matched row's tenantId is then bound onto req for subsequent scoped access.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into `invoice_items` does not set tenant_id. If `invoice_items` is a tenant-scoped table (columns are NOT NULL with no default), this will either fail or create a row not explicitly tied to the caller's tenant.
  - _Why false positive:_ invoice_items has no tenant_id and is transitively scoped through tenant-scoped parent invoices via invoice_id.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE capability_reviews sets reused/reused_capability WHERE id = ${pending.reviewId} but does not include tenant_id in the WHERE clause. This writes a tenant-scoped table row by id only, without ensuring the caller's tenant owns that row.
  - _Why false positive:_ Best-effort internal worker updates capability_reviews by server-stored pending.reviewId from in-memory map, not directly from request boundary.
- **server/ceo-orchestrator.ts:821** _(global-table)_ — Unconstrained read of personas table: `await db.select().from(personasTable)` fetches all personas with NO tenant_id filter, so cachedPersonas contains rows for every tenant.
  - _Why false positive:_ Referenced schema says personas has no tenant_id, so reading all personas is from a platform-global table.
- **server/ceo-orchestrator.ts:1153** _(global-table)_ — Using a persona id derived from a global cachedPersonas list when creating conversations: `storage.createConversation({... personaId: targetPersona?.id ..., tenantId: plan.tenantId})` — the code uses a persona id that may belong to another tenant without validating ownership.
  - _Why false positive:_ personaId comes from personas table, which schema marks as non-tenant-scoped/global; no cross-tenant ownership concept applies.
- **server/channel-routing.ts:31** _(global-table)_ — SELECT joins personas without tenant filtering: the query joins `personas p ON p.id = cr.persona_id` and returns p.name with NO WHERE p.tenant_id = <tenant>. If personas is tenant-scoped this leaks cross-tenant data.
  - _Why false positive:_ personas has no tenant_id per schema, and channel_routes is a global routing table by design.
- **server/commitments.ts:135** _(global-table)_ — SELECT from tenant-scoped table `commitments` without tenant_id constraint. scanAndEscalate selects overdue commitments across all tenants: `SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`.
  - _Why false positive:_ scanner is an internal platform-wide admin digest by design; comments explicitly state cross-tenant fan-in with redacted output for owner visibility.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}`. This can update rows across tenants.
  - _Why false positive:_ getDecryptedPassword first loads the credential via getCredential(id, tenantId); the subsequent last_used_at update targets that verified row id in the same call path.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` omits tenant_id in WHERE clause: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` called from getLoginCredentials.
  - _Why false positive:_ getLoginCredentials first resolves cred via getCredentialForSite(siteUrl, tenantId); the later metadata update uses that verified credential id in the same function.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} is executed without constraining by tenant_id. Although the row.id was obtained from a tenant-scoped SELECT earlier, the UPDATE lacks explicit tenant verification and could update the wrong tenant's row in some race scenarios.
  - _Why false positive:_ row.id comes from an immediately prior SELECT on doc_chunks constrained by collection_id and tenant_id in the same function.
- **server/dream-consolidation.ts:83** _(fk-transitive)_ — When assembling recent session summaries the code SELECTs messages: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ... without also constraining messages by tenant_id. This reads message rows from the tenant-scoped `messages` table without an explicit tenant filter.
  - _Why false positive:_ messages are read via conversation_id values obtained from conversations already filtered by tenant_id; access is transitively scoped through the tenant-owned parent conversation rows.
- **server/email.ts:366** _(global-table)_ — In inbox polling code the query SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without tenant_id. `inbox_messages` is tenant-scoped, so this read can return rows from other tenants.
  - _Why false positive:_ this is shared-inbox polling against a single platform inbox; de-dup by provider message_id is a platform-global maintenance flow rather than tenant-requested access.
- **server/email.ts:410** _(global-table)_ — backfillEmptyBodies() performs SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id with NO tenant filter, iterating over all tenants' rows.
  - _Why false positive:_ backfillEmptyBodies is a platform-wide maintenance job over the shared inbox store, not a tenant-facing query path.
- **server/email.ts:424** _(upstream-verified)_ — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} updates a tenant-scoped table row by id without verifying tenant_id in the WHERE clause.
  - _Why false positive:_ Row.id comes from prior same-function SELECT over inbox_messages; this is a server maintenance backfill updating the exact rows it just enumerated, not request-scoped tenant access.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) but no tenant constraint. Even though ownership was checked in application logic, the SQL update itself is not tenant-scoped (race/IDOR risk).
  - _Why false positive:_ Same call path immediately verifies project.tenantId === tenantId before this update; write uses the previously verified row.
- **server/heartbeat.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table research_schedules uses WHERE id = ${sched.id} but does not include tenant_id. An UPDATE by id alone can modify rows across tenants if ids collide or are enumerated.
  - _Why false positive:_ sched row already carries tenant_id from earlier selection/processing in same worker loop; this is internal scheduler advancement on that fetched row, not request-driven cross-tenant access.
- **server/heartbeat.ts:107** _(upstream-verified)_ — UPDATE on tenant-scoped table research_schedules lacks tenant_id in WHERE clause (UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id}).
  - _Why false positive:_ same fetched sched object is being updated within the same internal heartbeat flow after tenant-aware processing; no untrusted tenant boundary here.
- **server/heartbeat.ts:112** _(upstream-verified)_ — UPDATE on tenant-scoped table research_schedules (SET next_run_at = ${nextDate} WHERE id = ${sched.id}) has no tenant_id constraint.
  - _Why false positive:_ same internal schedule-processing path uses the already-fetched sched row and its tenant_id; this update is tied to that row in the same flow.
- **server/heartbeat.ts:120** _(upstream-verified)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id filter (tenant-scoped table).
  - _Why false positive:_ recovery update is on the same sched row already loaded and processed in this call path; not a fresh caller-controlled lookup.
- **server/heartbeat.ts:612** _(upstream-verified)_ — SELECT * FROM event_log WHERE status = 'pending' without tenant_id constraint. event_log rows include tenant_id and this read pulls pending events across all tenants.
  - _Why false positive:_ global worker intentionally sweeps pending events across tenants, then routes each using event.tenant_id; this is per-tenant processing by design, not a tenant data leak endpoint.
- **server/heartbeat.ts:257** _(upstream-verified)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — heartbeat_tasks is tenant-scoped but the UPDATE omits tenant_id in the WHERE clause.
  - _Why false positive:_ t comes from prior internal iteration over fetched heartbeat task rows; update is a startup normalization on that exact row, not a cross-tenant request lookup.
- **server/heartbeat.ts:261** _(upstream-verified)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ same startup cleanup loop updates the already-fetched task row t; no untrusted tenant boundary is crossed in this path.
- **server/heartbeat.ts:2818** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint. This updates a tenant-scoped table without verifying the row belongs to the expected tenant.
  - _Why false positive:_ newTask was just created in the same call with explicit tenantId, so updating by its returned server-generated id is on a row whose tenancy was established earlier in the path.
- **server/heartbeat.ts:3270** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id in WHERE. This write may affect rows across tenants.
  - _Why false positive:_ newTask is immediately updated after creation with the same tenantId in the same flow; row ownership was established by createHeartbeatTask.
- **server/lib/action-ledger.ts:304** _(global-table)_ — UPDATE in sweepStaleAttempts performs a cross-tenant write — the WHERE clause (id IN (SELECT id FROM action_attempts ...)) has no tenant_id predicate, so rows from all tenants can be updated.
  - _Why false positive:_ reconciler sweep is a platform-wide internal recovery job over action_attempts across tenants, bounded and not driven by request-scoped tenant input.
- **server/lib/inbox-ingest.ts:504** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_classifications` is missing a tenant constraint: `UPDATE inbox_classifications SET routed_to = ${JSON.stringify(routedTo)}::jsonb WHERE inbox_message_id = ${storeResult.id}`. Without `AND tenant_id = ...` this could modify another tenant's row sharing the same inbox_message_id.
  - _Why false positive:_ storeResult.id comes from a tenant-scoped insert/select in the same call path and the classification row was just claimed with tenant_id and inbox_message_id for that same message.
- **server/lib/scheduled-post-runner.ts:266** _(upstream-verified)_ — Constructing a Postgres array literal string (arrLit) from request-derived platforms and interpolating it into sql``: this is a string-built SQL fragment derived from user input and risks injection + tenant/isolation issues.
  - _Why false positive:_ this is a SQL-construction concern, not a tenant-isolation finding; the insert writes tenant_id from params and no cross-tenant read/write scope bypass is shown here.
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE against tenant-scoped table tenantVoiceProfiles uses WHERE id = <existing.id> but does NOT include tenant_id in the WHERE clause.
  - _Why false positive:_ existing is fetched immediately above via getVoiceProfile({ tenantId: opts.tenantId, profileName }), so the updated id was already resolved through a tenant-scoped lookup in the same path.
- **server/memory-graph.ts:392** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id constraint on UPDATE.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries constrained by tenant_id = ${tenantId} in the same function, so the update targets rows already tenant-verified upstream.
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — missing tenant_id constraint (called after classifyMemoryCategory), allowing cross-tenant updates.
  - _Why false positive:_ entry.id is from storage.createMemoryEntry with tenantId supplied in the same path, so the async category update is to the newly created row already bound to that tenant.
- **server/memory-intelligence.ts:316** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — same missing tenant constraint in the update path for 'update' actions.
  - _Why false positive:_ entry.id here is likewise the newly created replacement row from storage.createMemoryEntry with tenantId, so the follow-on category update is tenant-bound upstream.
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' omits tenant_id. The INSERT INTO project_files(...) VALUES(...) does not include tenant_id even though tenant-scoped tables must be constrained/annotated.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; ownership is enforced by assertProjectInTenant before insert.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' omits tenant_id. The parallel-produce path performs INSERT INTO project_files(...) VALUES(...) without setting tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert is gated by assertProjectInTenant on the parent project.
- **server/orchestrator-ledger.ts:444** _(upstream-verified)_ — Use of sql.raw() with a request-derived value. saveLedgerState calls tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`) where lockKey is derived from ledgerKey and tenantId — this inserts a derived string into raw SQL rather than as a bound parameter, creating an injection and isolation risk.
  - _Why false positive:_ The flagged raw advisory-lock key is not a tenant-row access; surrounding persistence queries on memory_entries are tenant-scoped by tenant_id, so this is not a cross-tenant isolation finding.
- **server/pace-control.ts:85** _(global-table)_ — SELECT from tenant-scoped table 'heartbeat_logs' with no tenant constraint. checkPace queries heartbeat_logs over a time window but does not filter by tenant_id, so usage counts are aggregated across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id column and is described as a platform-global throttle/pace log.
- **server/pace-control.ts:140** _(global-table)_ — SELECTs from tenant-scoped table 'heartbeat_logs' without tenant constraint. getPaceSnapshot aggregates recent heartbeat_logs but lacks tenant_id filtering, returning cross-tenant data in what appears to be a tenant-scoped helper.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and this snapshot operates on a global pacing log by design.
- **server/research-engine.ts:311** _(upstream-verified)_ — UPDATE research_programs SET model = ... WHERE id = ${programId} lacks a tenant_id constraint — it can modify another tenant's program record.
  - _Why false positive:_ Same call path first selects research_programs by id and tenant_id, so program ownership is verified before fallback update.
- **server/research-engine.ts:451** _(upstream-verified)_ — SELECT FROM research_experiments WHERE session_id = ${sessionId} ... lacks tenant_id filtering — reads experiments across tenants for the same session id value.
  - _Why false positive:_ sessionId comes from the in-memory ActiveSession created after tenant-scoped session creation; experiments are tied to that trusted session context.
- **server/research-engine.ts:460** _(upstream-verified)_ — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} omits tenant_id — may return a program belonging to a different tenant.
  - _Why false positive:_ session.programId is from a previously tenant-validated research_programs row in this session flow, not untrusted input.
- **server/research-engine.ts:465** _(upstream-verified)_ — SELECT summary FROM research_sessions WHERE id = ${sessionId} does not include tenant_id — may read another tenant's session summary.
  - _Why false positive:_ sessionId is the trusted active session id carried with the ActiveSession object, not caller-controlled in this helper path.
- **server/research-engine.ts:550** _(upstream-verified)_ — UPDATE research_sessions SET ... WHERE id = ${sessionId} (session end) lacks tenant_id filtering — will update sessions across tenants if ids collide or are misused.
  - _Why false positive:_ endSession operates on an ActiveSession looked up from the server-managed activeSessions map; sessionId targets that trusted in-memory session.
- **server/research-engine.ts:821** _(upstream-verified)_ — UPDATE research_experiments ... WHERE id = ${experimentId} updates an experiment row by id without verifying tenant_id — writes may affect another tenant's experiment.
  - _Why false positive:_ experimentId is server-generated within the same experiment/session workflow, not taken from a cross-tenant request boundary.
- **server/research-engine.ts:888** _(upstream-verified)_ — UPDATE research_sessions SET model = ${fallbackModel} WHERE id = ${session.sessionId} lacks tenant scoping and can change another tenant's session.
  - _Why false positive:_ session.sessionId comes from the trusted ActiveSession state in the same worker flow, not untrusted input.
- **server/research-engine.ts:988** _(upstream-verified)_ — SELECT name FROM research_programs WHERE id = ${session.programId} (inside injectKeepedFinding) omits tenant_id and may fetch a program from another tenant.
  - _Why false positive:_ session.programId is from trusted ActiveSession state established from a tenant-scoped program lookup.
- **server/routes.ts:764** _(upstream-verified)_ — SELECT from tenant-scoped table `projects` without constraining by tenant_id (SELECT p.id, p.name, p.tenant_id, ... FROM projects p JOIN tenants t ... WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = '')
  - _Why false positive:_ This is an internal startup backfill/admin maintenance scan that deliberately selects tenant_id and passes it onward for per-tenant handling, not a tenant request path.
- **server/routes/admin.ts:569** _(upstream-verified)_ — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant_id filter — reading from a likely tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ Route is explicitly gated to ADMIN_TENANT_ID and isAdminRequest; this is a deliberate platform-admin cross-tenant diagnostic path.
- **server/routes/admin.ts:574** _(upstream-verified)_ — Query against tool_performance (failingRows) lacks any tenant_id constraint — possible cross-tenant data exposure.
  - _Why false positive:_ Same admin-only silent-failures endpoint; cross-tenant tool_performance read is explicitly authorized earlier in the same call path.
- **server/routes/admin.ts:587** _(upstream-verified)_ — SELECT from ai_insights (stuckInsights) is executed without a tenant_id constraint — ai_insights appears to be tenant-scoped elsewhere but here is queried globally.
  - _Why false positive:_ Same platform-admin-only endpoint; earlier ADMIN_TENANT_ID plus isAdminRequest guard authorizes this global inspection.
- **server/routes/admin.ts:598** _(upstream-verified)_ — Query against heartbeat_tasks (disabledTasks) is executed with no tenant_id filter — heartbeat_tasks is used as a tenant-scoped table elsewhere but is queried globally here.
  - _Why false positive:_ Same admin-only diagnostic route with explicit platform-admin guard before the query.
- **server/routes/admin.ts:609** _(upstream-verified)_ — SELECT from ai_insights (stuckPending) runs without a tenant_id constraint, exposing tenant-scoped incident rows across tenants.
  - _Why false positive:_ Same platform-admin-only call path; earlier guard authorizes deliberate cross-tenant ai_insights inspection.
- **server/routes/admin-zombie-detector.ts:38** _(upstream-verified)_ — Querying agent_trace_spans (tools usage CTE) with no tenant_id constraint — likely a tenant-scoped activity/log table being read globally inside the admin zombie detector.
  - _Why false positive:_ Zombie-detector endpoint is explicitly restricted by ADMIN_TENANT_ID and isAdminRequest before running global aggregate queries.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` lacks tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Why false positive:_ conv.id comes from storage.createConversation with tenantId just above, and project ownership is verified with projects.id plus tenant_id before linking.
- **server/routes/conversations.ts:101** _(fk-transitive)_ — SELECT from `project_conversations` lacks tenant scope (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}). If `project_conversations` is tenant-scoped this can leak/affect other tenants.
  - _Why false positive:_ project_conversations has no tenant_id and is an association child isolated by verified tenant-scoped parents project and conversation.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into `project_conversations` omits tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). If this table is tenant-scoped, the tenant_id must be set on INSERT.
  - _Why false positive:_ INSERT into project_conversations without tenant_id is correct because the table has no tenant_id and isolation is transitive via tenant-scoped project/conversation rows.
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT into `project_notes` omits tenant context (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})). If project_notes is tenant-scoped, tenant_id is missing from the INSERT.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects; project_id was resolved via tenant-scoped project lookup.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE statement that marks a message read omits tenant_id in WHERE (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). The SELECT earlier constrained by tenant_id, but the UPDATE itself is not scoped.
  - _Why false positive:_ same call path first selects inbox_messages by id and tenant_id, then updates that resolved row id only.
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author) ...).
  - _Why false positive:_ project_notes has no tenant_id column and insert is gated by prior tenant-scoped projects check on the same project id.
- **server/routes/projects.ts:225** _(fk-transitive)_ — INSERT into project_files (base64 upload path) does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...).
  - _Why false positive:_ project_files has no tenant_id column and insert uses project_id already verified against tenant-scoped projects.
- **server/routes/projects.ts:293** _(fk-transitive)_ — INSERT into project_files (multipart upload path) does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) ...).
  - _Why false positive:_ project_files has no tenant_id column and multipart upload path verifies tenant ownership of parent project before insert.
- **server/routes/slack.ts:124** _(upstream-verified)_ — Raw/interpolated SQL (db.execute(sql`... ${token} ... ${tenantId} ...`)) uses request-derived values (token, tenantId) inside a sql template. This both mixes protection & filtering into raw SQL fragments and risks injection / isolation issues instead of using Drizzle's typed query builder.
  - _Why false positive:_ query scopes tenant_persona_names with tenant_id = tenantId and personas is global; sql template interpolation here is parameterized and tenant filtering is present.
- **server/routes/slack.ts:191** _(upstream-verified)_ — Raw/interpolated SQL (db.execute(sql`... tpn.tenant_id = ${tenantId} ...`)) used to enumerate persona names with tenantId injected into the template. This builds SQL via template interpolation with a request-derived tenant identifier rather than using Drizzle's eq() predicates.
  - _Why false positive:_ enumeration is explicitly limited to tenant-specific display names via join condition tpn.tenant_id = tenantId; personas table is global.
- **server/routes/stripe-checkout.ts:99** _(global-table)_ — Raw/interpolated SQL (db.execute(sql`... WHERE pr.id = ${priceId} ... AND (${isAnonymous}::boolean = false OR p.metadata->>'kind' = 'audit')`)) interpolates priceId and isAnonymous (derived from the request) into an SQL template. This blends request-derived values directly into SQL and bypasses the Drizzle .where(eq(...)) pattern required for clear tenant/parameter binding and isolation checks.
  - _Why false positive:_ stripe products/prices are platform-global commerce tables, not tenant-scoped data; no cross-tenant isolation issue shown.
- **server/routes/stats.ts:54** _(upstream-verified)_ — Use of sql template interpolation with a request-derived tenantId inside .where (s`${conversations.tenantId} = ${tenantId}` and s`${messages.conversationId} IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`). This constructs SQL via template interpolation with request data instead of using Drizzle's eq()/subquery APIs, increasing injection/isolation risk.
  - _Why false positive:_ both stats queries explicitly filter by request tenantId, including subquery on conversations tenant_id.
- **server/storage.ts:239** _(upstream-verified)_ — getConversationUnscoped() performs a SELECT on conversations without constraining by tenant_id (unscoped read).
  - _Why false positive:_ Explicitly dangerous internal helper documented for orchestration to derive tenantId then validate downstream; not for HTTP handlers.
- **server/storage.ts:289** _(upstream-verified)_ — attachCitationMetadata() runs raw SELECT FROM messages WHERE id IN (...) without constraining by tenant_id, reading a tenant-scoped table unscoped.
  - _Why false positive:_ Helper is only called with msgs already selected using tenant_id in same call path; query re-reads by those validated message ids.
- **server/storage.ts:474** _(global-table)_ — deletePersona() updates conversations (clearing personaId) using WHERE conversations.personaId = id but without tenant filtering — this will affect conversations across all tenants.
  - _Why false positive:_ personas is explicitly non-tenant-scoped, so clearing personaId on all conversations using that global persona is intentional platform-wide admin behavior.
- **server/tenant-fork.ts:226** _(upstream-verified)_ — UPDATE on tenant-scoped table (re-pointing self-ref) uses only id filter (.where(eq(entry.table.id, p.newId))) with no tenant_id constraint during cross-tenant cloning.
  - _Why false positive:_ p.newId is the freshly inserted row id created in the same transaction after copying a source-tenant row and setting row.tenantId = newTenantId; update targets that trusted new row only.
- **server/tools.ts:4496** _(upstream-verified)_ — SELECT that reads project_id from `conversations` / `project_conversations` is executed without constraining by tenant (no WHERE tenant_id = ...). Even though ownership is asserted earlier via assertConversationInTenant, the raw SQL itself lacks the required tenant filter.
  - _Why false positive:_ assertConversationInTenant(convId, params._tenantId) is called immediately before the raw SQL, and project ownership is rechecked with assertProjectInTenant before use.
- **server/tools.ts:6893** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The query inserts project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by but omits tenant_id even though the code relies on application-level filtering only.
  - _Why false positive:_ project_files has no tenant_id column and is isolated through tenant-scoped parent project validated by assertProjectInTenant.
- **server/tools.ts:8853** _(upstream-verified)_ — UPDATE on tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE; it lacks a tenant_id constraint (no WHERE tenant_id = <caller tenant>). This allows updating a row by id without confirming it belongs to the caller's tenant.
  - _Why false positive:_ row.id comes from the row just inserted into proposed_skills in this same function with tenantId = tid; update is on that trusted row, not attacker-chosen id.
- **server/tools.ts:8895** _(upstream-verified)_ — UPDATE on tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE; it lacks a tenant_id constraint (no WHERE tenant_id = <caller tenant>). This appears when marking a proposal rejected — it can modify proposals across tenants by id.
  - _Why false positive:_ same call path updates the freshly inserted proposed_skills row by returned row.id; not a cross-tenant request-boundary id.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table (browser_workflows) has no tenant_id constraint in the WHERE clause. The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` can modify a row belonging to another tenant if an id from another tenant is supplied.
  - _Why false positive:_ workflow is first loaded with WHERE id/name ... AND tenant_id = tenantId, then updated by workflow.id from that verified row.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` omits tenant_id, violating the invariant that INSERTs into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert uses projId obtained via join to projects constrained by tenant_id = tenantId.
- **server/tools/domains/files/handlers.ts:838** _(upstream-verified)_ — UPDATE on projects table lacks tenant scoping. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` can update a project belonging to a different tenant.
  - _Why false positive:_ projId is sourced from the prior SELECT joining projects with p.tenant_id = tenantId, so the subsequent update targets a project already proven in-tenant.
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` is executed without constraining by tenant_id (missing `AND tenant_id = ${tenantId}` in the WHERE clause). Writes must be explicitly tenant-scoped to prevent cross-tenant modification.
  - _Why false positive:_ row.id values come only from the immediately preceding SELECT on knowledge_triples filtered by tenant_id = tenantId, so the updates apply only to already tenant-scoped rows.
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries lacks tenant_id constraint. The code updates memory_entries WHERE id = ${id} without also constraining tenant_id to the caller's tenant.
  - _Why false positive:_ id is taken from the immediately preceding SELECT on memory_entries filtered by tenant_id = tid, persona_id, and category; update follows that verified lookup.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries lacks tenant_id constraint. The code updates memory_entries WHERE id = ANY(${ids}) and does not include tenant_id = <caller tenant>.
  - _Why false positive:_ ids come only from rows just selected with WHERE tenant_id = tid, so the update set is already tenant-scoped in this call path
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries lacks tenant_id constraint. The code updates memory_entries WHERE id = ${id} without constraining tenant_id to the caller's tenant.
  - _Why false positive:_ id is taken from an immediately prior SELECT constrained by tenant_id = tid and persona/category filters in the same path
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` by conversation id is executed without a tenant_id filter. Reading the conversations table without scoping by tenant allows cross-tenant lookups and violates the requirement that tenant-scoped table reads include the caller's tenant constraint.
  - _Why false positive:_ this read is a verification step for a previously loaded claimed tenant; code compares returned tenant_id to info.tenantId and rejects mismatches/ambiguity
- **server/video-job-runner.ts:226** _(global-table)_ — System recovery routine recoverStaleVideoJobs performs UPDATE on `videoJobs` across all rows matching stale criteria without scoping by tenant_id. This writes to tenant-scoped rows for all tenants in a single unscoped operation, violating the 'must include tenant filter' invariant and risking unintended cross-tenant modifications.
  - _Why false positive:_ recoverStaleVideoJobs is a trusted system-wide recovery sweeper/job runner performing intentional global maintenance, not tenant-request data access
- **server/wiring-invariants.ts:367** _(global-table)_ — COUNT/MAX query against subsystem table uses direct table interpolation and queries the entire table without any tenant_id constraint (e.g. `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`). If that table is tenant-scoped this reads data across all tenants.
  - _Why false positive:_ wiring-invariants is a platform-wide diagnostic/observability check that intentionally aggregates whole-table freshness across the system
- **server/wiring-invariants.ts:535** _(global-table)_ — Global COUNT/size probe over memory tables uses raw SQL without a tenant filter (e.g. `SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`). Several entries in MEMORY_TABLES (messages, memory_entries, graph_memory, etc.) are typically tenant-scoped — this reads counts across all tenants.
  - _Why false positive:_ memory-growth tripwire is an intentional platform-global observability probe over table counts/sizes, not tenant request handling
- **server/wiring-invariants.ts:561** _(global-table)_ — COUNT query against graph_memory runs without scoping by tenant (SELECT COUNT(*)::int AS cnt FROM graph_memory). If graph_memory is tenant-scoped, this reads data across all tenants.
  - _Why false positive:_ the graph_memory count is an intentional global diagnostic to detect whether the table is empty across all tenants
