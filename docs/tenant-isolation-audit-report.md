# Tenant-Isolation Audit Report

_Generated 2026-07-28T03:03:16.377Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **574/574** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **300** (CRITICAL 106 · HIGH 166 · MEDIUM 27 · LOW 1)
- Precision second-pass (`gpt-5.4`): **201** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (106)

- **server/agentic-engines.ts:243** — UPDATE ai_insights SET status='applied', action_taken=... WHERE status='new' AND category IN (...) — this bulk UPDATE is not tenant-scoped and will modify rows across all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant scope (e.g. add AND tenant_id = ${tenantId}, or iterate tenants and update per-tenant). Bulk cross-tenant writes must be avoided; require an explicit tenant context when mutating tenant-scoped tables.
- **server/agentic/approvals.ts:135** — Bulk UPDATE in expireStaleApprovals updates agentApprovals rows to status='expired' using only status/expiresAt conditions and no tenant_id constraint — this will expire approvals across all tenants.
  - _Fix:_ Make the expiry tenant-scoped (require tenantId parameter and add eq(agentApprovals.tenantId, tenantId) to the WHERE), or iterate per-tenant so expiries only affect the intended tenant. Avoid cross-tenant bulk writes on tenant-scoped tables.
- **server/agentic/resume-worker.ts:66** — Tenant-scoped tables scanned without constraining to a caller tenant: the candidate query SELECTs from agent_runs JOIN agent_approvals across all tenants (no WHERE tenant_id = <caller tenant>).
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE r.tenant_id = ${tenantId} or run the sweep per-tenant). If this background worker must operate across tenants, implement an explicit per-tenant iteration or otherwise ensure the caller's tenant context is applied before reading tenant-scoped rows.
- **server/agentic/runs.ts:34** — appendStep updates agent_runs without constraining by tenant_id (where clause is only WHERE id = runId). This allows modifying another tenant's run row.
  - _Fix:_ Require and pass tenantId into appendStep and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) so updates are scoped to the tenant.
- **server/agentic/runs.ts:45** — updateRunState updates agent_runs without constraining by tenant_id (WHERE id = runId only).
  - _Fix:_ Add a tenantId parameter and include a tenant_id equality check in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))).
- **server/agentic/runs.ts:54** — replaceRunState updates agent_runs without constraining by tenant_id (WHERE id = runId only).
  - _Fix:_ Require tenantId and add it to the WHERE clause to enforce tenant scoping (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:58** — completeRun updates agent_runs (setting status/result/completedAt) using WHERE id = runId only — missing tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause or require caller to pass tenantId. Use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) to avoid cross-tenant mutations.
- **server/agentic/runs.ts:88** — failRun updates agent_runs (marking a run failed) using WHERE id = runId only — no tenant_id constraint.
  - _Fix:_ Require tenantId and include it in the WHERE clause so only the owning tenant's run row can be modified (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:97** — pauseRun updates agent_runs to set status = 'paused' with WHERE id = runId only — missing tenant_id constraint.
  - _Fix:_ Add tenantId parameter and include tenant filtering in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/wake-scheduler.ts:145** — runDueWakes claims pending rows across tenants: the inner SELECT used in the UPDATE that moves rows to 'firing' does not constrain agent_wake_schedules by tenant_id, allowing a single scheduler run to claim/modify wakes for all tenants.
  - _Fix:_ Apply tenant scoping to the claim query. Either iterate tenants and run a tenant-scoped claim (e.g. WHERE tenant_id = ${tenantId} ...) or include tenant_id in the inner SELECT/outer UPDATE so rows are only selected/updated for the intended tenant. Avoid a global unscoped SELECT FOR UPDATE on a tenant-scoped table.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL executes an UPDATE on a tenant-scoped table without constraining tenant_id. This can modify rows across tenants if ids are manipulated.
  - _Fix:_ Include an explicit tenant filter in the UPDATE WHERE clause, e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id} (or the caller's tenant variable). Always include tenant_id on UPDATE/DELETE against tenant-scoped tables.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table `compaction_archives` uses only conversation_id (query: SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ... ) without an explicit tenant_id constraint. This reads message history/compacted content that can contain sensitive data.
  - _Fix:_ Constrain the query to the tenant (e.g. AND tenant_id = ${tenantId}) or join to conversations/projects to assert the conversation belongs to the tenant in the same SQL. Always add tenant_id to selects retrieving historical message/content data.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` uses only conversation_id (query: SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...) without an explicit tenant_id constraint. This can leak other tenants' message content if conversation_id validation is bypassed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or join with conversations ensuring c.tenant_id = ${tenantId} in the same query. Always enforce tenant_id on message reads.
- **server/chat-engine.ts:766** — Inline LATERAL subqueries select from `messages` using WHERE conversation_id = c.id (e.g. (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ORDER BY ...) ) but do NOT include tenant_id in those subqueries. Tenant scoping is missing at the subquery level.
  - _Fix:_ Add tenant constraints to the subqueries (e.g. WHERE conversation_id = c.id AND tenant_id = ${tenantId}) or ensure the outer query enforces tenant scoping and that the DB optimizer cannot return cross-tenant rows. Prefer explicit tenant checks in every access to tenant-scoped tables.
- **server/data-protection.ts:131** — DELETEs in the purge loop remove rows without tenant predicates (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}). These DELETEs operate on tenant-scoped tables without constraining tenant_id and can delete another tenant's rows if IDs collide or data was mis-attributed.
  - _Fix:_ Include tenant predicates on all destructive statements, e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Avoid relying solely on conversation_id as the guard; always include tenant_id in WHERE clauses for deletes/updates.
- **server/embeddings.ts:684** — UPDATE to tenant-scoped table (memory_entries|agent_knowledge via dynamic table param) performed without any tenant_id constraint in the WHERE clause (storeEmbeddingVec). This allows modifying rows across tenants.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Validate caller ownership before updating. Avoid one-off, global UPDATEs that only match by id.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} executed with no tenant_id filter in backfillEmbeddingVecs — writes can affect rows across tenants.
  - _Fix:_ Scope the update to the owning tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). If backfill runs as a global admin job, enumerate tenants and run per-tenant or otherwise ensure explicit, intentional admin semantics are documented and protected.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed with no tenant_id filter in backfillEmbeddingVecs — writes can affect rows across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause, and perform backfill per-tenant (or require an explicit admin-scoped operation that is auditable). Do not perform cross-tenant UPDATEs by id alone.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images does not set tenant_id (or otherwise constrain the write to the caller's tenant). This writes image data tied to a session_id without verifying the session belongs to the provided tenant.
  - _Fix:_ Make the insert tenant-aware: include tenant_id in the INSERT columns (VALUES (${tenantId}, ...)) or SELECT/VERIFY the presenter session belongs to the tenant before inserting. Prefer: query presenter_sessions for id = ${sessionId} AND tenant_id = ${tenantId} (fail if no row), then insert presenter_slide_images with an explicit tenant_id column (or otherwise enforce tenant ownership in the DB statement).
- **server/google-workspace.ts:2364** — INSERT into presenter_slide_images (thumbnail) does not set tenant_id (or otherwise constrain the write to the caller's tenant). Same pattern as the full-size image INSERT above.
  - _Fix:_ As above: verify the session belongs to the tenant (SELECT presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId}) and include tenant_id in the presenter_slide_images INSERT. Avoid relying solely on session_id without an explicit tenant constraint in the SQL.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks any tenant_id constraint. This allows modifying a session row by id without ensuring it belongs to the caller's tenant.
  - _Fix:_ Constrain the update to the tenant: UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, first SELECT the session by id and assert its tenant_id === tenantId before performing the UPDATE.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(...) updates proposals by id without a tenant_id constraint. This can modify proposals belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}. Alternatively, perform the update using a join or run the operation per-tenant after verifying ownership.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) disables tasks by id without tenant scoping, which can affect tasks belonging to other tenants.
  - _Fix:_ Constrain updates with tenant_id: UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}. Alternatively, verify each task's tenant matches the intended tenant before changing its enabled flag, or run the archival per-tenant.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — update misses tenant_id constraint. This can modify another tenant's schedule row by id.
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id} (or use the hoisted/validated tenantId). Avoid relying on id-only updates in a multi-tenant DB.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} (or equivalent assertTenantContext/parameter) to the WHERE clause so only the owning tenant's row is updated.
- **server/heartbeat.ts:112** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id in WHERE, risking updating another tenant's row.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use storage helper that enforces tenant scoping.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — write without tenant scope (tenant_id) in WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform this via a storage API that asserts tenant ownership.
- **server/heartbeat.ts:260** — UPDATE heartbeat_tasks SET model = 'claude-sonnet-4-5' WHERE id = ${t.id} — updating heartbeat_tasks by id only, missing tenant_id scoping.
  - _Fix:_ Add tenant filtering (AND tenant_id = ${t.tenantId} or equivalent) to the UPDATE, or call a storage-layer helper that enforces the tenant context before persisting changes.
- **server/heartbeat.ts:264** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — write lacks tenant_id constraint and can affect other tenants' tasks.
  - _Fix:_ Constrain the UPDATE by tenant_id (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or route via a tenant-aware storage function that asserts ownership.
- **server/heartbeat.ts:3222** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — update by id only, without tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform via a storage API that enforces tenant ownership validation.
- **server/heartbeat.ts:3374** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Constrain updates by tenant_id (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use tenant-aware helpers in storage to enforce ownership before updating.
- **server/internal-resolver.ts:93** — UPDATE on tenant-scoped table `event_log` (finalizeEvent) has no tenant_id constraint — resolver can finalize events across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause and pass the event's tenant into finalizeEvent (e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId} AND status IN (...)). Thread tenantId through callers so you only mutate rows owned by that tenant and preserve the CAS race protections.
- **server/internal-resolver.ts:107** — SELECT from `plans` (resolvePlanProposed) fetches plan by id without constraining by tenant_id — allows reading another tenant's plan.
  - _Fix:_ Query plans with an explicit tenant_id filter (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId} LIMIT 1) or resolve the tenant from the event payload and pass it into resolvePlanProposed before any reads, rejecting if tenant mismatch.
- **server/job-queue.ts:99** — claimDueJobs issues an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs ...) that claims pending jobs without any tenant_id constraint — worker may claim/modify jobs for any tenant.
  - _Fix:_ Either scope claims explicitly per-tenant (add a tenantId param and include WHERE tenant_id = ${tenantId}) or otherwise implement a well-reviewed, intentional global-sweep design that enforces safe separation. Add tenant_id to the outer WHERE/SELECT so only jobs for the intended tenant are claimed.
- **server/job-queue.ts:134** — reclaimExpiredLeases step 1 UPDATE moves running jobs to failed_terminal across all tenants (no tenant_id constraint).
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or provide an explicit all-tenants justification and protect with an explicit admin-only code path. Prefer passing tenantId into reclaim functions and restricting updates to that tenant.
- **server/job-queue.ts:148** — reclaimExpiredLeases step 2 UPDATE requeues expired running jobs to pending without any tenant_id constraint — affects jobs from all tenants.
  - _Fix:_ Same as above: scope UPDATE to tenant_id or make it an explicit, audited global operation accessible only to an operator path. Prefer per-tenant updates to avoid cross-tenant mutation.
- **server/job-queue.ts:177** — completeJob UPDATE on `agent_jobs` does not restrict by tenant_id — a caller can mark any job id (potentially from another tenant) succeeded.
  - _Fix:_ Require and validate tenant context (tenant_id) when completing a job, and include AND tenant_id = ${tenantId} in the WHERE clause to preserve tenant isolation and enforce lease-fencing.
- **server/job-queue.ts:237** — failJob UPDATE on `agent_jobs` writes failure state without tenant_id constraint — may mark jobs from other tenants failed or change their state.
  - _Fix:_ Include tenant_id in the WHERE clause and require tenant context for failJob. Ensure the attempts fencing check remains in place while adding tenant scoping to prevent cross-tenant state mutations.
- **server/job-queue.ts:274** — cancelJob UPDATE on `agent_jobs` can cancel jobs across tenants because it lacks a tenant_id condition.
  - _Fix:_ Require tenant scoping (AND tenant_id = ${tenantId}) or expose this as an explicit operator/admin-only global action with additional safeguards. Prefer enforcing tenant ownership in the WHERE clause.
- **server/job-queue.ts:283** — retryJob UPDATE resets job rows without a tenant_id constraint — can reset jobs belonging to other tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause and require the caller to provide and validate tenant context before allowing a retry.
- **server/lib/bwb-job-progress.ts:144** — UPDATE on video_jobs uses scope(jobId, ...) which can omit tenant predicate when BWB_TENANT_ID (envTenantId) is missing, resulting in a write without `tenant_id` scoping (`WHERE job_id = ...` only).
  - _Fix:_ Make tenant scoping mandatory. Require an explicit tenantId parameter (fail closed if missing) and change scope() to always include `eq(videoJobs.tenantId, tenantId)`. If env-threading is used, assert/throw when envTenantId is absent instead of falling back to jobId-only writes. Document and enforce BWB_TENANT_ID propagation in spawn chains.
- **server/lib/bwb-job-progress.ts:169** — UPDATE on video_jobs (updateBwbChapters) uses scope(jobId, ...) which may omit tenant predicate when env tenant is not present, allowing cross-tenant updates.
  - _Fix:_ Same fix: require/accept an explicit tenantId and ensure scope() always adds `eq(videoJobs.tenantId, tenantId)`. If tenantId cannot be derived, abort the update; log and surface an error rather than performing an unconstrained UPDATE.
- **server/lib/bwb-job-progress.ts:211** — UPDATE in completeBwbJob uses scope(jobId) which may omit tenant predicate and therefore can update video_jobs rows across tenants.
  - _Fix:_ Ensure completeBwbJob has a verified tenantId and that scope() always includes `eq(videoJobs.tenantId, tenantId)`. Prefer an explicit tenantId argument rather than relying on BWB_TENANT_ID env threading; fail closed if tenant is unknown.
- **server/lib/bwb-job-progress.ts:230** — UPDATE in bumpBwbHeartbeat uses scope(jobId, ...) which can omit tenant predicate when tenant ID is not available, allowing cross-tenant writes.
  - _Fix:_ Require tenantId for heartbeat writes (or assert envTenantId is present). Change scope() to throw or return an error when tenant is missing so the update never runs unconstrained. Always include `AND tenant_id = <tenant>` in the WHERE clause.
- **server/lib/bwb-job-progress.ts:254** — UPDATE in failBwbJob uses scope(jobId, { notDone: true }) which may omit tenant predicate and therefore may set `failed` across tenants when env tenant is absent.
  - _Fix:_ Do not allow unconstrained fail writes. Require/validate tenantId and ensure scope() always builds `eq(videoJobs.tenantId, tenantId)` into the WHERE. If envTenantId is missing, abort and surface an error instead of performing the update.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into `project_files` does not set tenant_id. Per project convention tenant_id columns are NOT NULL with no default — inserting without tenant_id violates the tenant-isolation invariant and may misattribute or fail to enforce ownership.
  - _Fix:_ Add tenant_id to the INSERT values (e.g. VALUES (..., ${tenantId}, ...)) or otherwise ensure the row is written with an explicit tenant_id. Validate the project_id belongs to the tenant before inserting.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into `project_notes` does not include tenant_id. This violates the invariant that tenant-scoped tables must receive tenant_id explicitly on INSERTs, risking wrong-tenant writes or schema errors.
  - _Fix:_ Include tenant_id in the INSERT (VALUES (..., ${tenantId}, ...)), and ensure the target project_id is validated as belonging to the tenant before inserting. Audit other project-scoped child tables for the same pattern.
- **server/lib/system-probes.ts:163** — SELECT against tenant-scoped table `messages` without any tenant_id filter. The code runs `db.select({ count: sql<number>`count(*)::int` }).from(messagesTable)` and aggregates across all rows regardless of tenant.
  - _Fix:_ Constrain the query to an explicit tenant. For example, accept/derive the tenantId and use a WHERE: db.select({ count: sql`count(*)::int` }).from(messagesTable).where(eq(messagesTable.tenantId, tenantId)). Alternatively, if this is an admin-only global metric, make that explicit in code/audit and ensure the table is documented as global — but do NOT leave tenant-scoped tables readable without an explicit tenant filter.
- **server/memory-graph.ts:210** — INSERT INTO memory_links does not set tenant_id. Tenant_id is required for tenant-scoped tables and omitting it will create links that are not associated with the caller's tenant (or may violate NOT NULL constraints).
  - _Fix:_ Set tenant_id explicitly in the INSERT (e.g. INSERT ... (source_memory_id, target_memory_id, link_type, strength, confidence, source_count, tenant_id) VALUES (..., ${tenantId})). Prefer verifying both source and target memory rows belong to the tenant before inserting.
- **server/orchestrator-ledger.ts:445** — Use of sql.raw() with a value derived from ledgerKey/tenantId in this statement: `SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)` — the inserted raw token is request/argument-derived and can lead to SQL injection or malformed SQL.
  - _Fix:_ Avoid sql.raw() with untrusted inputs. Validate and coerce lockKey to a numeric type, then bind it as a parameter instead of injecting raw SQL. Example: compute a BigInt/number server-side (after strict validation) and use a parameterized expression (e.g. `tx.execute(sql`SELECT pg_advisory_xact_lock(${validatedBigInt}::bigint)`) or the driver's parameter API). If casting is required, perform it via a safe, validated binding rather than sql.raw().
- **server/plan-executor.ts:116** — UPDATE on tenant-scoped table 'plans' omits tenant_id guard. appendExecutionLog executes UPDATE plans ... WHERE id = ${planId} without constraining by tenant_id, allowing accidental/unauthorized writes to another tenant's plan row.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId}). Pass or derive the authoritative tenantId into appendExecutionLog (from the claimed plan row) and assert it before the UPDATE to enforce per-tenant isolation.
- **server/plan-executor.ts:513** — CAS claim UPDATE flips plan status without tenant constraint. The initial claim uses UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' but does not constrain by tenant_id — a plan from another tenant could be claimed/modified.
  - _Fix:_ Add tenant scoping to the CAS UPDATE (e.g. WHERE id = ${planId} AND status = 'approved' AND tenant_id = ${expectedTenantId}). If this executor runs as a multi-tenant service, require/accept an explicit caller tenantId or validate the returned row. Always verify the plan's tenant before proceeding.
- **server/plan-executor.ts:527** — UPDATE sets plan status = 'failed' for an id without tenant constraint when plan has no steps (UPDATE plans SET status = 'failed' WHERE id = ${planId}). This can modify another tenant's plan if id is misused.
  - _Fix:_ Include tenant guard in the UPDATE (WHERE id = ${planId} AND tenant_id = ${expectedTenantId}). Obtain the plan's tenant_id from the earlier claim or require the caller to provide the tenant context and assert it before updating.
- **server/plan-executor.ts:725** — Final-status UPDATE on plans omits tenant constraint. The executor writes final status and execution_log via UPDATE plans ... WHERE id = ${planId} with no tenant_id filter, allowing cross-tenant mutation of plan state/logs.
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}). Make tenant_id available to the function (from the claimed row) and use it in all subsequent updates to enforce isolation.
- **server/plan-executor.ts:759** — Error-path UPDATE resets plan status to 'failed' using WHERE id = ${planId} AND status = 'executing' with no tenant constraint. This can alter plans across tenants during error handling.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${expectedTenantId}) or verify the plan's tenant before performing the UPDATE. Ensure all error-path writes include tenant checks.
- **server/plan-executor.ts:775** — Boot-recovery UPDATE flips 'executing'→'approved' for stale plans without tenant scoping: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This operates across all tenants.
  - _Fix:_ Limit recovery to a specific tenant context or iterate per-tenant. Add tenant scoping (WHERE tenant_id = ${tenantId} AND status = 'executing' ...) or require an explicit admin-level intent with documented justification. At minimum, record/log affected tenant_ids and ensure recovery runs under a trusted admin process with explicit tenant-awareness.
- **server/plan-executor.ts:784** — SELECT pending plans for restart does not filter by tenant: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50. This will queue plans across all tenants for execution in the current process context.
  - _Fix:_ Only select pending plans for the tenant(s) this executor is authorized to process. Add WHERE tenant_id = ${tenantId} (or iterate tenants explicitly under a trusted admin path). Avoid blind cross-tenant polling; require explicit tenant-scoped work-claiming.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} executes a write on a tenant-scoped table using only the row id and no tenant_id constraint. Without tenant scoping this can modify rows belonging to other tenants.
  - _Fix:_ Add tenant_id to the UPDATE WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}), or validate ownership of the task id before updating.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates a tenant-scoped table by id alone with no tenant_id condition, enabling cross-tenant mutation if IDs collide or are misused.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}), or verify that the desk belongs to the tenant before updating.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance_rules without scoping by tenant_id, allowing rule counts/metadata for other tenants to be modified.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or validate rule ownership prior to performing the update.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} modifies a tenant-scoped table without a tenant_id condition. This can mutate proposals for other tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause or otherwise verify proposal ownership before performing updates. Ensure proposal-facing functions accept tenantId from a trusted source.
- **server/proposal-verifier.ts:245** — persist() runs UPDATE code_proposals ... WHERE id = ${proposalId} without tenant scoping, persisting verification state across tenants if invoked without ownership checks.
  - _Fix:_ Change persist() to accept tenantId and use WHERE id = ${proposalId} AND tenant_id = ${tenantId}. Validate the caller's tenant before updating proposal rows.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift, UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} is executed with no tenant constraint, allowing cross-tenant mutation.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE, and require tenantId be passed into attemptRebaseOnDrift (or validate proposal belongs to caller's tenant before changing status).
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without scoping by tenant_id, enabling cross-tenant data changes.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${tenantId} (pass tenantId into the function) or verify proposal ownership before persisting rebased content.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` missing tenant_id constraint: `UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}` — allows modifying a row by id regardless of tenant ownership.
  - _Fix:_ Include the tenant_id in the WHERE clause to enforce ownership at the DB query level, e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}` (use the tenant value from the row or context). Never rely solely on in-memory checks; always parameterize the tenant_id on UPDATE/DELETE to prevent cross-tenant updates.
- **server/reference-learner.ts:290** — UPDATE against tenant-scoped table `memory_entries` missing tenant_id constraint: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}` — updates a row by id without verifying it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id as well, e.g. `WHERE id = ${storedId} AND tenant_id = ${input.tenantId}` (or validate ownership before updating). Ensure every UPDATE/DELETE/SELECT on tenant-scoped tables includes `tenant_id = <caller tenant>` (or equivalent parameterized Drizzle filter) to enforce tenant isolation.
- **server/routes.ts:4331** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id (columns are noted as NOT NULL with no default), leading to tenant-isolation violation (or DB errors) and possible cross-tenant writes.
  - _Fix:_ Always include tenant_id explicitly in INSERTs to tenant-scoped tables and validate ownership of referenced foreign IDs. Example: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'})`. Alternatively, use a storage function that enforces tenant ownership of the project before inserting.
- **server/routes/conversations.ts:97** — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Fix:_ Constrain the update to the caller's tenant. e.g. `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use conv.tenantId). Prefer prepared/Drizzle methods that include tenant scope.
- **server/routes/conversations.ts:101** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Fix:_ Always supply tenant_id on INSERTs to tenant-scoped tables and validate ownership: `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`. Ensure tenantId is from the authenticated context (not user-controlled).
- **server/routes/conversations.ts:103** — UPDATE on tenant-scoped table 'projects' lacks tenant_id constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Fix:_ Constrain the UPDATE by tenant to avoid cross-tenant modification: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Prefer verifying project ownership before mutating.
- **server/routes/conversations.ts:229** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})`
  - _Fix:_ Include tenant_id in the INSERT and verify the project belongs to the tenant: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`. Ensure tenantId is the authenticated tenant and not derived from client input.
- **server/routes/projects.ts:184** — INSERT into project_notes omits tenant_id. The INSERT INTO project_notes (project_id, note, author) does not set tenant_id (tenant-scoped column).
  - _Fix:_ Include tenant_id in the INSERT explicitly (and derive it from the authenticated request). Example: INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) RETURNING *; or validate ownership and set tenantId via Drizzle values including tenantId.
- **server/routes/projects.ts:224** — INSERT into project_files (base64 upload) omits tenant_id. The INSERT INTO project_files (...) VALUES (...) does not set tenant_id.
  - _Fix:_ Add tenant_id to the inserted columns and set it from the authenticated tenant (e.g. tenantId). Example: INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) RETURNING *; ensure tenantId is validated from the request and not taken from client input.
- **server/routes/projects.ts:292** — INSERT into project_files (multipart upload) omits tenant_id. The INSERT INTO project_files (...) VALUES (...) does not set tenant_id.
  - _Fix:_ Same fix as above: include tenant_id in the column list and pass the server-derived tenantId (e.g. ${tenantId}) when inserting. Prefer using Drizzle's .values({ tenantId }) to avoid SQL templating errors.
- **server/sculptor.ts:94** — UPDATE to sculptor_sessions without constraining tenant_id. The update sets status/result for a session by id only (WHERE id = ${session.id}) allowing cross-tenant modification if an attacker supplies/guesses a session id.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or verify the session belongs to params.tenantId before updating. Prefer using Drizzle helpers like .where(eq(table.tenantId, tenantId)).
- **server/sculptor.ts:101** — UPDATE sculptor_sessions (set conversation_id) uses WHERE id = ${session.id} with no tenant_id constraint, allowing modification of another tenant's session if the id is known or guessed.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}, or first verify that the session row belongs to the tenant and then update within the same transactional context.
- **server/sculptor.ts:156** — monitorSession updates sculptor_sessions (status/result/tool_calls_count/tokens_used/completed_at) using WHERE id = ${sessionId} without a tenant_id constraint. The function has tenantId available but does not use it, enabling cross-tenant updates.
  - _Fix:_ Restrict the UPDATE to the tenant: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, read/validate the session row under the tenant first and then perform the update in a transaction.
- **server/sculptor.ts:347** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} executes without constraining tenant_id. This allows writing review data into sessions belonging to other tenants.
  - _Fix:_ Include tenant filtering on the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or fetch-and-verify ownership of the session before performing the update.
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without verifying tenant ownership. agent_cost_ledger rows are tenant-scoped elsewhere, so updating by id only risks affecting another tenant's ledger entry.
  - _Fix:_ Ensure the UPDATE includes tenant scoping (e.g. add AND tenant_id = ${tenantId}) or change the API to accept/validate tenantId and verify that the reservationId belongs to that tenant (SELECT ... WHERE id=${reservationId} AND tenant_id=${tenantId}) before updating.
- **server/second-opinion.ts:424** — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without a tenant_id constraint, allowing a caller to modify a reservation row belonging to a different tenant.
  - _Fix:_ Require and use tenant context when settling/releasing reservations, or include tenant_id in the WHERE clause (WHERE id = ${reservationId} AND tenant_id = ${tenantId}). At minimum, SELECT the row and verify its tenant_id matches the caller before performing the UPDATE.
- **server/seed.ts:3096** — UPDATE research_programs SET model = ... WHERE id = ${p.id} performs a write to a tenant-scoped table without constraining by tenant_id — this can modify other tenants' data.
  - _Fix:_ Include the tenant_id constraint in the UPDATE (e.g. UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} AND tenant_id = ${tenantId}) or verify ownership of the row before updating. Always require and check tenant ownership on writes to tenant-scoped tables to prevent cross-tenant modification.
- **server/seed.ts:3442** — SELECT from tenant-scoped table `conversations` without any tenant_id constraint (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). This reads rows across all tenants and is used to create project↔conversation links, enabling cross-tenant data access/corruption.
  - _Fix:_ Restrict the query to the intended tenant. e.g. add AND c.tenant_id = ${tenantId} (or tenant_id = 1 for owner seed):
SELECT DISTINCT c.id, c.project_id FROM conversations c
WHERE c.project_id IS NOT NULL
  AND c.tenant_id = 1
  AND NOT EXISTS (...)
Also consider joining to projects and ensure pc/project rows are only for the same tenant before inserting links.
- **server/seed.ts:4272** — Inserts into tenant-scoped table `heartbeat_tasks` are performed via db.insert(heartbeatTasks).values(...) without setting tenant_id on the inserted rows. Tenant_id must be provided explicitly; otherwise rows may be created with NULL/default tenant or assigned incorrectly.
  - _Fix:_ Include tenant_id in every inserted object (or use a parameterized tenantId):
{ tenant_id: 1, name: 'Self-Reflection', description: ..., ... }
Or use a helper that merges tenant_id into each object before db.insert().values(...). Review all subsequent heartbeat_tasks inserts in this block and add tenant_id accordingly.
- **server/seed.ts:4339** — Additional INSERT into `heartbeat_tasks` (Model Scout) performed without tenant_id. Multiple later inserts in this file also omit tenant_id, creating tenant-ambiguous rows.
  - _Fix:_ Same fix as above: ensure every heartbeat_tasks insert includes tenant_id (e.g. tenant_id: 1 or tenantId variable). Audit the rest of the file for similar inserts and enforce tenant_id consistently.
- **server/seed.ts:4686** — DELETE FROM tenant-scoped table `heartbeat_tasks` without a tenant_id constraint (DELETE FROM heartbeat_tasks WHERE enabled = false ...). This can remove rows for all tenants, not just the intended tenant.
  - _Fix:_ Add a tenant filter to the DELETE: DELETE FROM heartbeat_tasks WHERE tenant_id = ${tenantId} AND enabled = false AND description LIKE ... AND type != 'maintenance_script'. Prefer explicit tenantId parameter rather than relying on implicit defaults.
- **server/seed.ts:4714** — UPDATE memory_entries (tenant-scoped) without tenant_id constraint (UPDATE memory_entries SET fact = REPLACE(...) WHERE ...). This will modify memory entries across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or tenant_id = 1 for owner seed). Example:
UPDATE memory_entries SET fact = REPLACE(fact,'Manjaro','wellness-program') WHERE tenant_id = 1 AND deleted_at IS NULL AND fact LIKE '%Manjaro%' AND ...
Ensure large multi-tenant migrations are run per-tenant or under an operator-approved single-tenant context.
- **server/seed.ts:4740** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' — updates `conversations` (tenant-scoped) across all tenants because no tenant_id constraint is present.
  - _Fix:_ Constrain the update to the intended tenant: UPDATE conversations SET model = 'gpt-5' WHERE tenant_id = ${tenantId} AND model = 'gpt-4.1'. If this migration must run globally, iterate tenants explicitly and run per-tenant updates to avoid accidental cross-tenant changes.
- **server/self-improvement.ts:394** — runExperiment issues UPDATE statements against the tenant-scoped `experiments` table using .where(eq(experiments.id, experiment.id)) with NO tenant_id constraint. This allows updating any experiment row by id across tenants.
  - _Fix:_ Constrain UPDATEs (and any DELETEs) to the caller's tenant by adding the tenant predicate, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Ensure the function has access to and uses the experiment owner's tenantId when updating. Apply same fix to the error-path UPDATE at lines ~413-415.
- **server/skill-seeker.ts:749** — seekAndLearn has a default parameter tenantId: number = 1 (admin tenant). If callers omit tenantId, the function will insert/operate in the admin tenant, causing cross-tenant writes into admin data.
  - _Fix:_ Do NOT default tenantId to 1. Require an explicit tenantId and fail-closed (throw or return an error) when it's missing/invalid. If an admin sweep is required, provide an explicit admin-only API that callers must opt into (and authenticate) rather than using a default parameter.
- **server/stability-watchdog.ts:177** — SELECT from `heartbeat_logs` (recentLogs) aggregates error counts but has NO tenant_id filter. This reads across all tenants and is later used to disable tasks; it can cause cross-tenant impact and data leakage.
  - _Fix:_ Scope the query to a specific tenant_id when performing tenant-targeted remediation, or explicitly perform this work under an authorized administrative context with documented intent. Prefer per-tenant iteration (SELECT DISTINCT tenant_id ... then run tenant-scoped query) or add AND tenant_id = ${tenantId} to the WHERE clause. Ensure any subsequent disabling/updating of tasks is performed with tenant-scoped checks (use assertTenantContext/assertProjectInTenant etc.).
- **server/stability-watchdog.ts:211** — DELETE FROM `heartbeat_tasks` deletes rows matching criteria without any tenant_id constraint. This can remove data for all tenants (cross-tenant data deletion).
  - _Fix:_ Add tenant scoping to destructive queries (e.g. WHERE tenant_id = ${tenantId} AND ...), or run cleanup on a per-tenant basis. If this is intended to be a global system maintenance operation, require and assert an explicit admin context and authorization before executing. Avoid un-scoped DELETEs on tenant-scoped tables.
- **server/stability-watchdog.ts:225** — DELETE FROM `event_log` deletes old event log entries without any tenant_id filter. This will remove event_log rows across all tenants.
  - _Fix:_ Restrict deletions to a tenant (WHERE tenant_id = ${tenantId} AND ...), or iterate per-tenant. If global pruning is intended, perform it under an explicit admin-only path and document/authorize it. Never perform blind tenant-scoped table deletions without explicit tenant scoping or admin guard.
- **server/stability-watchdog.ts:259** — DELETE FROM `heartbeat_logs` in pruneOldLogs runs without any tenant_id constraint (uses INTERVAL with sql.raw but does not scope by tenant). This can prune logs across all tenants unintentionally.
  - _Fix:_ Add a tenant_id predicate to the DELETE (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...), or execute the pruning per-tenant in a controlled loop. If pruning must be global, require explicit admin authorization and a deliberate, documented code path.
- **server/storage.ts:480** — Transaction deletes/updates tenant-scoped tables without tenant filtering: update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id)) — no tenant_id constraint. This will clear persona associations across all tenants.
  - _Fix:_ Scope these cleanup statements to the relevant tenant(s). Either accept a tenantId parameter (and assert it) or join to/where by tenant_id (e.g. .where(and(eq(conversations.personaId, id), eq(conversations.tenantId, tenantId)))). Ensure atomic multi-table cleanup is still tenant-scoped.
- **server/storage.ts:481** — Transaction updates `memory_entries` without tenant scoping: update(memoryEntries).set({ status: 'superseded' }).where(eq(memory_entries.persona_id, id)) — no tenant_id constraint, affecting all tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or pass/validate tenantId in the deletePersona API so the cleanup only touches the intended tenant's rows.
- **server/storage.ts:482** — Transaction deletes from tenant-scoped `daily_notes` without tenant filter: delete(dailyNotes).where(eq(daily_notes.persona_id, id)). This will remove notes across tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}), and require/validate tenantId on persona deletion flows so deletions only affect the correct tenant.
- **server/storage.ts:1303** — getAllDataForExport performs many unscoped SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.), returning platform-wide data with no tenant filters.
  - _Fix:_ Export is a high-risk operation — require an explicit tenantId or an admin-only pathway and validate the caller's authorization. Prefer per-tenant exports by passing tenantId and scoping every SELECT (eq(table.tenantId, tenantId)). Do not perform blanket unscoped selects in code accessible to non-admin contexts.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` without constraining tenant_id (.delete(...).where(eq(custom_tools.id, tool.id))). This can remove another tenant's row if an id is guessed or leaked.
  - _Fix:_ Always include tenant scoping on DELETE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, <expectedTenantId>)))) or require/validate tenant ownership before deleting. Change revertToolRow signature to accept/validate tenantId if necessary.
- **server/tools/domains/files/handlers.ts:835** — INSERT into tenant-scoped table `project_files` does not set tenant_id. Per invariant, tenant-scoped tables must be explicitly populated with tenant_id on INSERT (columns are NOT NULL w/ no default).
  - _Fix:_ Include tenant_id explicitly in the INSERT values and bind it from a trusted source (ctx.tenantId or the already-verified proj owner). e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ...). Ensure tenantId is derived from a verified trusted context and not from untrusted request params.
- **server/wiring-invariants.ts:57** — Query selects from tenant-scoped research_schedules (and joins research_programs) with no tenant_id constraint — reads all tenants' schedules.
  - _Fix:_ Constrain the query to a single tenant (or iterate per-tenant). e.g. use a parameterized WHERE s.tenant_id = $1 (or Drizzle .where(eq(research_schedules.tenantId, tenantId))). If this is intentionally a global/admin cross-tenant audit, make that explicit and restrict invocation to an admin context only (or use ADMIN_TENANT_ID and document the exception).
- **server/wiring-invariants.ts:180** — Complex query in checkOutcomeCanary reads research_programs, research_experiments, code_proposals, research_sessions and research_schedules with no tenant_id constraints — aggregates/joins across all tenants.
  - _Fix:_ Restrict these subqueries to the caller's tenant (e.g. WHERE e.tenant_id = $1, rs.tenant_id = $1, s.tenant_id = $1), or run the check per-tenant. If this is intended to be an admin/global audit, require admin-only execution and document the exception; otherwise use Drizzle .where(eq(..., tenantId)).
- **server/wiring-invariants.ts:244** — SELECT count(*) FROM tool_performance WHERE last_success_at... has no tenant filter — aggregates tool performance across all tenants.
  - _Fix:_ If tool_performance is tenant-scoped, add WHERE tenant_id = $1 (or Drizzle .where(eq(tool_performance.tenantId, tenantId))). For admin/global behaviour, ensure only admin contexts run this and explicitly pass ADMIN_TENANT_ID if appropriate.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tool_performance executed without tenant constraint (counts rows across all tenants).
  - _Fix:_ Add tenant scoping (WHERE tenant_id = $1) or make this an explicit admin/global-only query with proper access controls and documentation.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages WHERE created_at > ... runs without filtering by tenant — message rows are typically tenant-scoped and this reads across tenants.
  - _Fix:_ Restrict the query by tenant_id (WHERE tenant_id = $1) or run per-tenant to avoid cross-tenant data aggregation. If counting global messages is intentional, add explicit admin gating and document the exception.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE ... has no tenant constraint — exposes per-tenant tool usage across all tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = $1) or run this per-tenant. If an admin/global view is required, require admin privileges and document the rationale.
- **server/wiring-invariants.ts:417** — Tool-drift query aggregates FROM tool_performance with no tenant filter (GROUP BY tool_name ...) — reads/factors across all tenants.
  - _Fix:_ If tool_performance is tenant-scoped, include a WHERE tenant_id = $1 (or Drizzle .where(eq(tool_performance.tenantId, tenantId))). Otherwise, make the cross-tenant nature explicit and gate behind admin-only execution.

### HIGH (166)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (SELECT id, tenant_id, hypothesis, ...) — reads experiments across all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = <tenantId>), or explicitly iterate per-tenant and pass tenantId into this function so every SELECT against experiments is tenant-scoped. Avoid cross-tenant reads on tenant-scoped tables.
- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` uses WHERE id = activityId only, with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the update (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) or accept/require tenantId as a parameter and verify ownership before updating to enforce tenant isolation.
- **server/agent-channels.ts:198** — Query joins `channel_subscriptions` (tenant-scoped) without constraining its tenant_id in the JOIN condition (JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}). This can surface rows from other tenants.
  - _Fix:_ Constrain the join by tenant_id (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${params.tenantId}) or otherwise ensure every reference to channel_subscriptions is filtered by the caller's tenant.
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} without constraining tenant_id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or otherwise verify the row belongs to the caller's tenant before updating.
- **server/agentic-engines.ts:161** — SELECT from `ai_insights` in retryPendingMinervaRouting omits tenant_id filter — reads pending insights across all tenants.
  - _Fix:_ Query per-tenant (add WHERE tenant_id = ${tenantId}) or accept a tenantId parameter and restrict the SELECT to that tenant. Ensure downstream routing uses the owning tenant only.
- **server/agentic-engines.ts:347** — In gatherOperationalData a SELECT against `heartbeat_logs` (COUNT) lacks tenant_id filter and therefore aggregates logs across tenants.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = ${tenantId} ...) so operational telemetry is scoped per-tenant, or change the function to iterate per-tenant if it must run across all tenants in a controlled way.
- **server/agentic-engines.ts:451** — SELECT recent heartbeat_logs (recentLogs) in runPredictiveEngine omits tenant_id — reads global logs instead of tenant-scoped logs.
  - _Fix:_ Filter heartbeat_logs by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status = 'success') so only the caller's tenant data is read.
- **server/agentic-engines.ts:519** — SELECT grouping heartbeat_logs in runOptimizationEngine has no tenant filter — aggregates global task performance across tenants.
  - _Fix:_ Restrict the query by tenant_id in the WHERE clause so only the tenant's heartbeat_logs are aggregated, or run the aggregation per-tenant explicitly.
- **server/agentic-engines.ts:533** — SELECT from `heartbeat_tasks` (taskConfig) lacks tenant_id filtering — reads task configurations across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to scope configuration data to the tenant, or otherwise ensure this function is invoked separately for each tenant and the query is tenant-scoped.
- **server/agentic-features.ts:524** — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} is executed without a tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or validate ownership before updating to prevent cross-tenant modification.
- **server/agentic-features.ts:600** — SELECT existing enrollment (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active') omits tenant_id, so existing enrollments from other tenants can cause false positives/negatives.
  - _Fix:_ Include tenant scoping in the check (add AND tenant_id = ${params.tenantId}) to ensure the existence check is performed only within the caller's tenant.
- **server/agentic-features.ts:638** — UPDATE outreach_enrollments SET status='completed' WHERE id = ${enrollment.id} is executed without tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) or require the function to pass tenantId for safety.
- **server/agentic-features.ts:672** — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} lacks tenant scoping.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to ensure only the caller's tenant row is modified.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments SET reply_classification = ..., status = ... WHERE id = ${row.id} is missing tenant_id constraint.
  - _Fix:_ Include tenant filtering (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) so the update cannot affect rows belonging to other tenants.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' performs a tenant-scoped table read with no tenant_id constraint, allowing cross-tenant enumeration/leakage.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or refactor to iterate per-tenant under an explicit admin-only operation. If this sweep must run across all tenants, make the admin nature explicit (run only in a privileged, audited process) and ensure the SQL still enforces tenant scoping on per-tenant operations (or enumerate tenants from a secure admin source and call tenant-scoped queries for each). Avoid unconstrained SELECTs on tenant-scoped tables.
- **server/agentic/task-forces.ts:40** — String-interpolated SQL with request-derived value: personaIdArrayLiteral builds an array literal string from caller-supplied personaIds and that string is injected into the SQL (sql`${literal}::int[]`). This is injection-prone and mixes raw SQL generation with user input, even if filtered to integers.
  - _Fix:_ Avoid constructing raw SQL array literals. Use parameterized array binding or Drizzle's parameterization for arrays (pass personaIds as a parameter and cast to int[] in SQL), or build the query with parameter placeholders so the DB driver handles escaping (e.g. VALUES (..., ${p.personaIds ?? null}::int[])). Validate/normalize inputs and prefer parameterized queries instead of embedding pre-built SQL fragments.
- **server/auto-asset-capture.ts:184** — backfillProjectAssets scans conversations across all tenants: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations has no tenant_id filter, so it reads tenant-scoped rows for every tenant in one query.
  - _Fix:_ Either run the backfill on a per-tenant basis (iterate known tenant_ids) or add an explicit tenant_id WHERE clause for each tenant you intend to process. Ensure subsequent processing only touches rows for that tenant.
- **server/auto-asset-capture.ts:207** — Reading messages for a conversation without tenant constraint: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' lacks tenant_id, so message rows (tenant-scoped) can be read without verifying tenant ownership in the query.
  - _Fix:_ Include tenant scoping on the messages SELECT (e.g. AND tenant_id = ${c.tenant_id}) or otherwise ensure the query is executed in a per-tenant context so only rows belonging to the expected tenant are returned. Do not rely solely on later application-level checks after an unscoped read.
- **server/auto-memorize.ts:47** — getWatermark reads agent_knowledge without tenant scoping: SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} LIMIT 1 has no tenant_id filter, returning a global/other-tenant watermark from a tenant-scoped table.
  - _Fix:_ Store and read watermarks per-tenant (or use a dedicated global table for truly global watermarks). Include WHERE tenant_id = ${tenantId} (or equivalent) when querying agent_knowledge so watermark reads/writes are scoped to the correct tenant.
- **server/auto-memorize.ts:163** — runAutoMemorize scans messages across tenants in a single unscoped SELECT: SELECT m.tenant_id, m.role, m.content, ... FROM messages ... has no tenant_id WHERE clause. Although the code later partitions by tenant, the raw read touches tenant-scoped rows for all tenants in one shot without an explicit per-tenant constraint.
  - _Fix:_ Avoid blanket unscoped selects on tenant-scoped tables. Either run a per-tenant query (WHERE m.tenant_id = ${tenantId}) or otherwise ensure reads are executed in a context that guarantees the tenant boundary (e.g. iterate tenants and run tenant-scoped queries). This prevents accidental cross-tenant data exposure if later logic changes.
- **server/auto-project.ts:112** — SELECT from messages uses conversation_id but does not include a tenant_id constraint. Reads against tenant-scoped tables must include an explicit tenant filter.
  - _Fix:_ Constrain the messages query to the caller's tenant. For example: WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} — or JOIN messages to conversations and check conversations.tenant_id = ${tenantId} to ensure tenant isolation.
- **server/auto-project.ts:145** — INSERT into project_notes does not include tenant_id. If project_notes is tenant-scoped (no default tenant_id), this writes a row without assigning the tenant, violating tenant isolation.
  - _Fix:_ Include tenant_id when inserting into project_notes (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (...) or SELECT id, ${noteText}, 'system', ${tenantId} FROM new_project). If project_notes is intentionally global, document that explicitly and enforce ownership checks before writing.
- **server/auto-transcript.ts:46** — SELECT from messages uses conversation_id but omits tenant_id. Even though conversation ownership was checked earlier, every read against a tenant-scoped table must include an explicit tenant filter per policy.
  - _Fix:_ Add tenant scoping to the messages query, e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}, or join messages to conversations and require conversations.tenant_id = ${tenantId} in the query to make the enforcement explicit.
- **server/auto-transcript.ts:128** — Backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without scoping messages by tenant. This reads a tenant-scoped table without a tenant_id constraint.
  - _Fix:_ Scope the subquery to the conversation's tenant, e.g. (SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) > 0, or otherwise include tenant_id = c.tenant_id in the subquery to ensure isolation.
- **server/auto-transcript.ts:153** — the alsoLinked query contains (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 — the subquery reads messages without a tenant filter, allowing cross-tenant reads if messages are tenant-scoped.
  - _Fix:_ Add tenant scoping to the subquery: (SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) > 0, or otherwise join messages with tenant check to guarantee tenant isolation.
- **server/business-tools.ts:96** — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table this allows creation of rows without an explicit tenant_id and can break tenant isolation (orphan/foreign rows tied only by invoice_id).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or otherwise ensure invoice_items is not tenant-scoped. Additionally, validate that the invoice_id belongs to the caller's tenant before inserting (or derive tenant from invoice and use that tenant_id) so items cannot be attached to another tenant's invoice.
- **server/capability-review.ts:492** — UPDATE against tenant-scoped table `capability_reviews` is executed without constraining `tenant_id` (UPDATE capability_reviews SET reused = true ... WHERE id = ${pending.reviewId} AND reused = false). This allows a row belonging to another tenant to be modified if the id is known or reachable via the in-memory map, violating the tenant-isolation invariant.
  - _Fix:_ Require and check tenant ownership in the update. Persist the tenantId when creating the pending entry (pendingReuseByConversation) and/or accept a caller tenantId, then include it in the WHERE clause (e.g. WHERE id = ${pending.reviewId} AND tenant_id = ${tenantId} AND reused = false). Do not rely solely on an in-memory map for authorization — validate ownership server-side before mutating tenant-scoped rows.
- **server/channels/kernel.ts:180** — dispatchInbound calls processMessage(conversationId, ...) with a conversationId returned from the adapter `resolveConversation` but does not verify that the conversation belongs to turn.tenantId. This lets an adapter (or a malicious caller) cause cross-tenant access if a conversationId from another tenant is supplied.
  - _Fix:_ Ensure tenant ownership is validated before any read/write that uses a foreign id. Options:
1) Have resolveConversation perform and guarantee tenant validation (and document/contract that it must only return conversationIds owned by turn.tenantId). Also pass turn.tenantId into resolveConversation so it can validate.
2) Or, in dispatchInbound, load the conversation row and assert it has tenant_id = turn.tenantId before calling processMessage. Example: const conv = await db.select().from(conversations).where(eq(conversations.id, conversationId), eq(conversations.tenant_id, turn.tenantId)); if (!conv) throw new Error('conversation not found or not owned by tenant');
3) Alternatively, modify processMessage to accept tenantId and enforce tenant-scoped checks internally. Whichever approach is chosen, ensure every downstream DB query uses an application-level WHERE tenant_id = <tenant> (or Drizzle eq on tenantId) so no cross-tenant reads/writes occur.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (query: SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...).
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) or otherwise enforce ownership in the same SQL (JOIN to projects and require p.tenant_id = ${tenantId}). Don’t rely solely on caller-validated projectId — include tenant scoping in the query for defense in depth.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (query: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...).
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or join against projects with a tenant filter to ensure the notes returned belong to the caller's tenant.
- **server/chat-engine.ts:831** — Scalar subquery counts rows in tenant-scoped `project_files` (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) without an explicit tenant_id constraint in the subquery.
  - _Fix:_ Include tenant scoping in the subquery (e.g. WHERE project_id = p.id AND tenant_id = ${tenantId}) or compute the count via a JOIN that includes p.tenant_id = ${tenantId}. Ensure every access to tenant-scoped tables includes tenant_id filtering.
- **server/chat-engine.ts:4332** — storage.attachCitations is called without any tenant context. The call storage.attachCitations((assistantMsgRow as any).id, collectedCitations) does not pass tenantId nor validate that the message id belongs to the caller's tenant, risking cross-tenant citation writes/updates.
  - _Fix:_ Ensure tenant isolation when attaching citations. Options: (1) Modify the call to include the tenant id (e.g. storage.attachCitations(messageId, citations, tenantIdForScope)) and enforce tenant_id in the underlying SQL; (2) Before calling attachCitations, fetch and validate that the message row (assistantMsgRow.id) belongs to tenantIdForScope; or (3) change attachCitations to require and enforce tenantId in its API and underlying queries. All underlying SQL must include a WHERE tenant_id = <tenant> guard for tenant-scoped tables.
- **server/commitment-drafter.ts:234** — UPDATE commits to revert a claimed row executes without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can unclaim or modify rows across tenants if row.id is re-used or attacker manipulates input.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise validate ownership before updating. Prefer parameterized Drizzle .where(eq(...)) patterns and fail-closed when tenantId is missing.
- **server/credential-vault.ts:169** — UPDATE on credential_vault updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). This can update another tenant's credential record if id overlaps.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or derive tenantId from an already-validated credential and include it in the WHERE clause to ensure tenant ownership.
- **server/credential-vault.ts:176** — UPDATE on credential_vault updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}). Missing tenant filter allows cross-tenant modification.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${cred.id} AND tenant_id = ${tenantId}). Ensure every UPDATE/DELETE against tenant-scoped tables includes tenant_id in the WHERE clause (or use Drizzle .where(eq(...))).
- **server/data-protection.ts:24** — Schema creation sets tenant_id DEFAULT 1 on memory_categories (tenant_id INTEGER DEFAULT 1). This creates implicit tenant attribution when INSERTs omit tenant_id and contradicts the invariant that tenant-scoped rows must require an explicit tenant_id.
  - _Fix:_ Remove the DEFAULT and make tenant_id NOT NULL (no default). Ensure all application INSERTs explicitly pass the tenant_id. If a default is needed for legacy-migration, perform an explicit migration that backfills and then drop the default immediately.
- **server/data-protection.ts:48** — ALTER TABLE adds agent_knowledge.tenant_id with DEFAULT 1 (tenant_id INTEGER DEFAULT 1). A default tenant value can accidentally attribute new rows to tenant 1 when tenant_id is not explicitly provided by the app.
  - _Fix:_ Do not add a DEFAULT for tenant_id. Add the column as nullable only if necessary, backfill rows explicitly, and then alter it to NOT NULL with no default. Ensure application code always INSERTs tenant_id explicitly.
- **server/data-protection.ts:117** — SELECT across conversations scans deleted_at rows for all tenants (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) with no tenant_id predicate. This reads tenant-scoped data across tenants.
  - _Fix:_ Add explicit tenant scoping or run this purge under a controlled administrative tenant-aware path. For multi-tenant purge jobs include WHERE tenant_id = ${tenantId} or make the operation explicit admin-only and log/authorize it. Prefer per-tenant purge runs to avoid cross-tenant access.
- **server/delivery-pipeline.ts:195** — updateDeliveryLog performs UPDATE on tenant-scoped table delivery_logs without constraining by tenant_id (WHERE only checks id).
  - _Fix:_ Require/accept a tenantId on updateDeliveryLog and include tenant scoping in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or validate ownership before updating. Avoid blanket updates by id alone.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed without tenant_id constraint (WHERE doc_path = ... AND collection_id = ... but no tenant_id), which can delete another tenant's chunks if collection_id/doc_path collide.
  - _Fix:_ Include tenant_id in the DELETE WHERE clause (AND tenant_id = ${tenantId}) or verify the collection belongs to the caller's tenant before deleting rows.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding ... WHERE id = ${row.id} is executed without tenant scoping. An UPDATE by id without tenant filter can modify another tenant's row if misrouted or if ids collide across tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or pass a tenant-validated row id to the updater to ensure ownership.
- **server/email.ts:490** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} performs an UPDATE on a tenant-scoped table without tenant_id in the WHERE clause, allowing cross-tenant modification if a row id is misattributed.
  - _Fix:_ Include tenant_id (and/or inbox_id) in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) to ensure only the owning tenant's row is updated.
- **server/embeddings.ts:716** — SELECT id, embedding FROM memory_entries ... LIMIT 200 executed without filtering by tenant_id in backfillEmbeddingVecs — reads can return rows across tenants.
  - _Fix:_ Add tenant scoping to the SELECT (WHERE tenant_id = ${tenantId} AND ...). If this is an admin-only backfill, make the admin intent explicit and audit/limit it; otherwise run backfills per-tenant.
- **server/embeddings.ts:732** — SELECT id, embedding FROM agent_knowledge ... LIMIT 200 executed without filtering by tenant_id in backfillEmbeddingVecs — reads can return rows across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause or perform the backfill per-tenant. Ensure any admin/global backfill is intentional, logged, and access-restricted.
- **server/embeddings.ts:688** — SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... in backfillMissingKnowledgeEmbeddings has no tenant_id filter — this reads data across tenants.
  - _Fix:_ Scope this query by tenant_id (WHERE tenant_id = ${tenantId} AND ...), or iterate tenants explicitly. Ensure that any global research backfill that deliberately touches all tenants is tightly controlled and documented.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal built from an embedding (sql.raw(`'${vec}'::vector`)) in vectorSearchMemory. The vec value is derived from model/provider output (request-derived) — interpolation risks injection and breaks the ‘application-level filters only’ assurance model.
  - _Fix:_ Avoid sql.raw() with interpolated, request-derived strings. Use parameterized binding for the embedding (pass a numeric array/parameter and cast safely), or use driver/pgvector-safe helpers that accept array parameters instead of building a quoted literal. Also validate embedding shape and contents and, where possible, pass tenantId as a parameter rather than interpolating SQL strings.
- **server/embeddings.ts:252** — Use of sql.raw() with an interpolated vector literal (ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) in vectorSearchMemory — vec is request/provider-derived and being inlined into SQL.
  - _Fix:_ Parameterize the vector value instead of inlining the string literal. For example, pass the embedding as a parameterized array and use an appropriate cast or pgvector binding so no SQL string interpolation is needed. Keep tenant filters parameterized as well.
- **server/embeddings.ts:559** — Use of sql.raw() with interpolated vector literal ('${vec}'::vector) in vectorSearchKnowledge — embedding is request-derived and being inlined into raw SQL, creating injection/isolation risk.
  - _Fix:_ Replace inlined sql.raw usage with a parameterized query that binds the numeric vector (or driver-supported pgvector parameter) rather than concatenating a string literal. Validate the embedding shape and avoid direct string interpolation into SQL.
- **server/embeddings.ts:565** — Use of sql.raw() with interpolated vector literal in ORDER BY (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) in vectorSearchKnowledge — request-derived data is being inlined into SQL via sql.raw.
  - _Fix:_ Use parameter binding for the query embedding (or a safe pgvector helper) instead of sql.raw string interpolation. Ensure queries remain parameterized and tenant-scoped.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' without tenant_id constraint (reads all tenants). The boot-time rehydrator queries agent_runs across tenants: SELECT ... FROM agent_runs WHERE status IN (...)
  - _Fix:_ Scope the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate tenants and run the query per-tenant inside the appropriate tenant context. Never run a cross-tenant SELECT against a tenant-scoped table at boot.
- **server/firecrawl.ts:214** — UPDATE on tenant-scoped table 'scraped_pages' uses only id in WHERE clause (no tenant_id constraint). After finding an existing row filtered by tenant, the code updates by id alone: db.update(scrapedPages).set(...).where(eq(scrapedPages.id, existing[0].id))
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise assert ownership immediately prior to the UPDATE and include the tenant_id in the WHERE to enforce isolation at write time.
- **server/firecrawl.ts:344** — UPDATE on tenant-scoped table 'scraped_pages' in crawl loop uses only id in WHERE clause (no tenant_id constraint). The code updates existing row by id: db.update(scrapedPages).set(...).where(eq(scrapedPages.id, existing[0].id))
  - _Fix:_ Same fix as above: include tenant scoping in the WHERE (e.g. AND eq(scrapedPages.tenantId, tenantId)) or validate ownership and include tenant_id in the UPDATE's WHERE clause to prevent cross-tenant writes.
- **server/glasses-gateway.ts:156** — SELECT from apiKeys does not include tenant scoping. The code fetches an API key row by hash without constraining by tenant (db.select().from(apiKeys).where(...)). If apiKeys is a tenant-scoped table this is a cross-tenant read risk.
  - _Fix:_ If apiKeys is tenant-scoped, include tenant filtering when possible or otherwise treat apiKeys as a global (non-tenant-scoped) resource in schema. At minimum ensure subsequent operations use both id AND tenant_id (e.g. eq(apiKeys.id, key.id) AND eq(apiKeys.tenantId, key.tenantId)) to avoid accidental cross-tenant access; consider making the schema explicit about apiKeys scoping.
- **server/glasses-gateway.ts:172** — UPDATE on tenant-scoped table 'api_keys' (lastUsedAt) uses only id in WHERE clause with no tenant_id constraint: db.update(apiKeys).set(...).where(eq(apiKeys.id, key.id))
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or otherwise ensure the update is qualified by the key's tenant before writing. Never update a tenant-scoped row by id alone without confirming tenant ownership.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' modifies driveFolderId/driveFolderUrl using only project id in WHERE clause (no tenant_id constraint). Even though ownership was checked earlier, the UPDATE itself is not tenant-scoped: db.update(projects).set(...).where(eq(projects.id, projectId))
  - _Fix:_ Add the tenant constraint to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or use an atomic UPDATE that includes tenant_id to both verify ownership and perform the write in one statement. This prevents accidental cross-tenant updates if ownership checks are bypassed or stale.
- **server/health-audit.ts:314** — SELECT FROM code_proposals filters by status and age but does not constrain by tenant_id. code_proposals appears tenant-scoped, so this can return rows for all tenants.
  - _Fix:_ Scope the stale-proposals query to a tenant or run the TTL logic per-tenant. For example, accept or iterate tenantId(s) and add WHERE tenant_id = ${tenantId} to the SELECT. If this operation is intentionally global, document and confirm that code_proposals is a global/shared table.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks for stuck tasks does not include tenant scoping. heartbeat_tasks are used elsewhere as tenant-scoped (many APIs accept tenantId for heartbeat tasks), so this may surface tasks across tenants.
  - _Fix:_ If heartbeat_tasks are per-tenant, add tenant_id = ${tenantId} to the WHERE clause or execute this scan per-tenant. If heartbeat_tasks are intentionally global/shared, document that clearly; otherwise enforce tenant scoping before selecting rows to inspect.
- **server/heartbeat.ts:52** — SELECT from research_schedules without any tenant_id constraint (SELECT * FROM research_schedules ...). This reads schedule rows across all tenants.
  - _Fix:_ Query per-tenant or include an explicit tenant_id filter (e.g. WHERE tenant_id = ${sched.tenant_id} or iterate active tenant ids and query per-tenant). Ensure any processing that must be tenant-aware only touches rows for that tenant.
- **server/heartbeat.ts:658** — SELECT from event_log WHERE status = 'pending' ... (no tenant_id filter). This pulls pending events across all tenants into a single query.
  - _Fix:_ If the processing loop must scan all tenants, iterate per-tenant and run tenant-scoped queries, or include tenant_id in the WHERE so each query is limited to a single tenant. Avoid unscoped SELECTs on tenant-scoped tables.
- **server/internal-resolver.ts:170** — UPDATE on `code_proposals` (resolveExperimentFailed) uses id but has no tenant_id constraint — may archive/modify proposals for other tenants.
  - _Fix:_ Restrict the UPDATE with AND tenant_id = ${tenantId} (and/or pass tenantId into resolveExperimentFailed). Validate the proposal belongs to the event's tenant before mutating, to enforce tenant isolation.
- **server/job-queue.ts:220** — SELECT max_attempts FROM agent_jobs WHERE id = ${id} reads job metadata without a tenant filter — leaks cross-tenant information and can lead to incorrect decisions.
  - _Fix:_ Select by id AND tenant_id = ${tenantId} (or require tenantId param), or otherwise ensure the caller has authority to read that job. Validate ownership before using returned values.
- **server/job-queue.ts:320** — listJobs SELECT builds WHERE dynamically and only filters by tenant_id when a tenantId arg is provided — the default/all-tenant behavior issues a plain SELECT from `agent_jobs` returning rows across tenants.
  - _Fix:_ Audit call-sites that call listJobs without tenantId; if the intent is operator/global visibility require an explicit admin flag (avoid implicit global queries). Consider making tenantId required for non-admin callers or add an explicit parameter to indicate cross-tenant intent and require admin authorization.
- **server/job-queue.ts:385** — getJobStats SELECT aggregates counts from `agent_jobs` over the last 7 days with no tenant_id constraint — returns cross-tenant aggregates.
  - _Fix:_ If stats are intended per-tenant, add an argument to scope by tenant_id and include WHERE tenant_id = ${tenantId}. If global stats are intentionally required, make the admin-only intent explicit and document/guard it with authorization checks.
- **server/knowledge-diversity-monitor.ts:266** — Query in runKnowledgeDiversityMonitor SELECT tenant_id, persona_id, COUNT(*) FROM `agent_knowledge` groups by tenant_id and persona_id without restricting to a tenant — enumerates tenant/persona pairs across the entire DB.
  - _Fix:_ If the monitor must iterate all tenants as a system job, make that explicit and ensure it's run only in a privileged system context. Otherwise, restrict queries to a single tenant by adding WHERE tenant_id = ${tenantId} or accept a tenantId parameter and enforce it in the SQL.
- **server/lib/agent-trace.ts:129** — UPDATE to tenant-scoped table agent_trace_spans omits tenant predicate: `UPDATE agent_trace_spans ... WHERE span_id = ${spanId}`. This can update rows across tenants because the WHERE clause lacks `tenant_id = ...`.
  - _Fix:_ Include a tenant filter on the UPDATE. Track the tenantId at persistSpanOpen (e.g. map spanId -> tenantId) or pass the tenantId into persistSpanClose, and change the WHERE to `WHERE span_id = ${spanId} AND tenant_id = ${tenantId}`. Alternatively, store tenant on open in a map and use that stored tenant when closing. Do not rely on implicit in-process context alone.
- **server/lib/bwb-job-progress.ts:201** — SELECT from video_jobs uses scope(jobId) which may omit tenant predicate if envTenantId is missing, returning rows across tenants (`SELECT ... WHERE job_id = ...` without tenant filter).
  - _Fix:_ Require and pass tenantId into the function, and make scope() assert the tenant presence and include `eq(videoJobs.tenantId, tenantId)` in the WHERE. Failing to find a tenant should error/return instead of performing an unconstrained SELECT.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT against tenant-scoped table `projects` is missing a tenant_id WHERE filter (returns counts across all tenants).
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. Never run cross-tenant aggregations without explicit owner intent and operator approval.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT from `project_files` uses only project_id/file_name and lacks an explicit tenant_id filter. This is a tenant-isolation risk if `project_files` is tenant-scoped and tenant_id is required by policy.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause, or validate that the referenced project_id belongs to the caller's tenant before reading project_files. Do not rely solely on project_id foreign keys for tenant isolation.
- **server/lib/ideabrowser-backfill.ts:115** — SELECT from `project_notes` uses only project_id and note to check existence and lacks an explicit tenant_id filter. This bypasses the required application-level tenant constraint.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or verify ownership of project_id before querying project_notes to ensure reads are scoped to the caller's tenant.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE locks rows across all tenants). This UPDATE/RETURNING updates rows across all tenants in a single statement (runner is system-scoped but the query touches tenant-scoped data with no tenant filter).
  - _Fix:_ If this behavior is intentional (system-wide runner), make the admin intent explicit and restrict access to this code path (audit + operator-only). Otherwise scope the CTE to a single tenant (add `WHERE tenant_id = <tenant>`), or iterate per-tenant: query tenants with due rows and process each tenant separately. At minimum record and assert the admin context clearly and add logging/audit trail tying each mutation to an authorized system account.
- **server/lib/scheduled-post-runner.ts:204** — UPDATE on tenant-scoped table `scheduled_posts` uses `WHERE id = ${row.id}` with NO tenant_id constraint. This can update a row belonging to a different tenant if ids collide or if the row context was tampered with in memory.
  - _Fix:_ Include the tenant_id in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}`) so every mutation is explicitly constrained to the expected tenant. Alternatively, fetch and verify the row's tenant before updating.
- **server/lib/scheduled-post-runner.ts:231** — Error-path UPDATE on `scheduled_posts` uses `WHERE id = ${row.id} AND status = 'publishing'` but omits tenant_id. In exception handling this updates tenant-scoped state with no tenant constraint.
  - _Fix:_ Add `AND tenant_id = ${row.tenant_id}` to the WHERE clause (or otherwise assert the row belongs to the expected tenant) before performing the recovery/update to avoid cross-tenant state changes.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` is performed with .where(eq(tenantVoiceProfiles.id, existing.id)) but does not include an explicit tenant_id constraint. Every UPDATE against a tenant-scoped table must also constrain by tenant_id to enforce isolation.
  - _Fix:_ Include the tenant_id in the WHERE clause to avoid cross-tenant updates. Example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Prefer always checking tenantId (not relying solely on a previously fetched row id) to prevent TOCTOU or misuse of ids from other tenants.
- **server/memory-graph.ts:81** — UPDATE memory_categories executed without constraining by tenant_id (WHERE id = ${catId} only). This can modify a category belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or perform the update inside a tenant-scoped transaction after verifying the category belongs to the tenant.
- **server/memory-graph.ts:141** — refreshCategoryCount: UPDATE memory_categories SET memory_count = (...) WHERE id = ${categoryId} — missing tenant_id filter. This may update another tenant's row with the same id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}) or compute and update counts inside a tenant-scoped transaction.
- **server/memory-graph.ts:196** — SELECT 1 FROM memory_links ... LIMIT 1 is performed without any tenant_id constraint. memory_links appears to be a tenant-scoped table, so this read can return rows from other tenants.
  - _Fix:_ Constrain the existence check to the caller's tenant (e.g. JOIN/WHERE to ensure linked rows belong to the tenant or add AND tenant_id = ${tenantId}). If tenant_id is stored on memory_links, include it in the WHERE clause; otherwise derive tenant from memory_entries and join on it.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is executed without tenant filtering. This can update a memory entry belonging to another tenant if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}), or perform the update only after verifying the memory entry belongs to the tenant in a tenant-scoped transaction.
- **server/memory-intelligence.ts:273** — After creating a memory entry the code runs UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} without tenant scoping. The update could affect a row with the same id in another tenant.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or run this in a tenant-scoped transaction and/or verify ownership before update.
- **server/memory-intelligence.ts:316** — During the 'update' action the code executes UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} with no tenant filter, which may modify another tenant's row.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}), or verify the existingId belongs to the tenant before changing state.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom} ... WHERE id = ${c.id} is executed without tenant scoping even though the contradictions selection targeted tenant_id earlier. Missing tenant filter on the UPDATE allows cross-tenant modification if ids collide.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause, or perform the UPDATE via a join that ensures the row belongs to the tenant.
- **server/minds-engine.ts:327** — markEventsHandled uses id = ANY(${`{${eventIds.join(",")}}`}::int[]) where the array literal is constructed via string interpolation from request-derived eventIds. This is a SQL construction with request-controlled content (injection + isolation risk).
  - _Fix:_ Use proper parameterized array binding instead of embedding a joined string (e.g. WHERE id = ANY(${eventIds}::int[]) using a parameterized array or Drizzle helpers). Also ensure tenant scoping is applied (tenant_id = ${tenantId}) — which is present — and validate input eventIds before use.
- **server/minerva-planner.ts:355** — decidePlan performs a CAS UPDATE on plans: UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'awaiting_approval' with NO tenant_id constraint. This allows modifying a plan belonging to another tenant.
  - _Fix:_ Require and enforce tenant scoping on this UPDATE (e.g. add AND tenant_id = ${expectedTenantId}) or require the caller to supply/derive the tenant and verify authorization before updating. All plan mutations must include tenant_id in the WHERE clause.
- **server/minerva-planner.ts:373** — Probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} is executed without tenant scoping, allowing information disclosure across tenants.
  - _Fix:_ Include tenant_id = ${expectedTenantId} in the SELECT or only probe rows after verifying the caller is authorized for the tenant.
- **server/minerva-planner.ts:391** — When handling 'revise' idempotency the code SELECTs child plans with SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 with NO tenant filter. This can return a child plan from another tenant.
  - _Fix:_ Restrict the child plan lookup by tenant (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${expectedTenantId}) so the idempotency check only inspects plans owned by the same tenant.
- **server/minerva-planner.ts:459** — On revise error handling the rollback UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising' is executed without tenant scoping, which may roll back a plan in another tenant.
  - _Fix:_ Constrain the rollback UPDATE with AND tenant_id = ${expectedTenantId} (and preferably perform the entire revise/create/rollback sequence inside a tenant-validated context or transaction).
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code calls assertProjectInTenant(...) to verify ownership but the INSERT omits tenant_id in the VALUES list, violating the invariant that tenant-scoped tables must have tenant_id explicitly set on INSERT.
  - _Fix:_ Include tenant_id in the INSERT columns and bind the caller's tenant id (e.g. VALUES (${options.tenantId}, ...)) so the row is written under the verified tenant. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ${safeTitle + ".mp4"}, ${outPath}, ${driveUrl || null}, ${"video"}, ${stats.size}, ${"mpeg-engine"}). Keep the ownership check (assertProjectInTenant) but always record tenant_id explicitly.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` does not include tenant_id. Even though assertProjectInTenant(...) is called to confirm ownership, the INSERT omits tenant_id in the VALUES list, violating the requirement that every INSERT must explicitly set tenant_id.
  - _Fix:_ Add tenant_id to the INSERT and bind options.tenantId. For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ${safeTitle + ".mp4"}, ${concatResult.filePath}, ${driveUrl || null}, ${"video"}, ${concatResult.sizeBytes || 0}, ${"mpeg-engine-parallel"}). Ensure the tenant id used matches the ownership verification and is always provided.
- **server/oauth-subscriptions.ts:906** — Global read from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id: `SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`
  - _Fix:_ Restrict this query to a specific tenant (e.g. add `AND tenant_id = ${tenantId}`) or explicitly document/implement an admin-only multi-tenant flow that enumerates tenants safely. Never select active OAuth subscriptions across all tenants without an explicit, validated tenant context.
- **server/oauth-subscriptions.ts:915** — Unconstrained read from tenant-scoped table `oauth_subscriptions`: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'` (no tenant_id filter)
  - _Fix:_ If you intend to operate per-tenant, add `WHERE tenant_id = ${tenantId}`. If you truly need a global scan (admin operation), require an explicit admin-only path and validate caller privileges; avoid mixing global scans with normal application code that assumes single-tenant scope.
- **server/oauth-subscriptions.ts:1258** — Background proactive refresh reads `oauth_subscriptions` without tenant scoping: `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`
  - _Fix:_ If this is intended as a multi-tenant maintenance job, make the multi-tenant intent explicit and ensure only an authorized admin job runs it. Otherwise restrict to a specific tenant (e.g. `WHERE tenant_id = ${tenantId}`). Prefer an explicit tenant iteration mechanism that validates tenant boundaries when performing per-tenant refreshes.
- **server/oauth-subscriptions.ts:1328** — Force-refresh code queries `oauth_subscriptions` for all active rows without a tenant filter: `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE`
  - _Fix:_ Avoid reading tenant-scoped rows globally in application code. Either require an explicit admin-only action to iterate tenants, or pass a tenantId and include `WHERE tenant_id = ${tenantId}`. Always make cross-tenant operations explicit and authenticated.
- **server/persona-export.ts:33** — Reading from tenant-scoped table `personas` without tenant restriction: `SELECT * FROM personas WHERE id = ${personaId}` — no tenant_id filter, so caller can export personas from other tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. `WHERE id = ${personaId} AND tenant_id = ${tenantId}`) or require and validate an admin context for global persona exports. Always enforce tenant_id when reading tenant-scoped entities.
- **server/process-governor.ts:121** — Subqueries against heartbeat_logs do not include tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This allows reading other tenants' heartbeat/log data since there is no row-level security.
  - _Fix:_ Add tenant scoping to these subqueries, e.g. AND tenant_id = ${tenantId} (or use eq(table.tenantId, tenantId) in Drizzle). Ensure every reference to heartbeat_logs filters by the caller's tenant_id.
- **server/process-governor.ts:155** — Inner SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true lacks tenant_id. Even though outer query scopes agent_desks by tenant, the inner query can count tasks across tenants.
  - _Fix:_ Constrain the inner query by tenant (e.g. AND tenant_id = ad.tenant_id or AND tenant_id = ${tenantId}). Always include the tenant_id filter in nested queries against tenant-scoped tables.
- **server/process-governor.ts:198** — SELECT AVG(duration_ms) FROM heartbeat_logs ... has no tenant_id filter — this aggregates logs across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(...))) to scope the aggregation to the caller's tenant.
- **server/process-governor.ts:240** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... does not filter by tenant_id, allowing cross-tenant aggregation of recent failures.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause to limit results to the invoking tenant's logs.
- **server/process-governor.ts:1184** — SELECT ... LEFT JOIN governance_rules gr ON gr.id = ga.rule_id ... fetches governance_rules.description without verifying gr.tenant_id = ga.tenant_id (no tenant filter on the joined governance_rules). This reads a tenant-scoped table via a foreign id without enforcing ownership.
  - _Fix:_ Constrain the join to the same tenant (e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ga.tenant_id) or otherwise verify the referenced rule belongs to the tenant before returning its fields.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project row by id with no tenant scoping. projects are tenant-scoped and must be constrained to the caller's tenant.
  - _Fix:_ Require and pass tenantId into updateProjectBrain, and include WHERE tenant_id = ${tenantId} in the SELECT (or verify project.tenant_id matches the caller's tenant before proceeding).
- **server/project-brain.ts:332** — SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 performs discovery across projects/conversations/messages with no tenant filter, potentially exposing other tenants' data.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId} AND messages.tenant_id = ${tenantId}) or accept a tenantId parameter and constrain all involved tables accordingly.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} does not include tenant_id. conversations are tenant-scoped and fetching them without tenant constraint can traverse other tenants' conversations.
  - _Fix:_ Include tenant filtering (e.g. AND c.tenant_id = ${tenantId}) or resolve conversations only for projects verified to belong to the caller's tenant.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} queries messages without tenant_id. messages are tenant-scoped and must be filtered by tenant_id to prevent cross-tenant reads.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or ensure conversation ownership is validated and used to scope the message query.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id without tenant scoping. code_proposals are tenant-scoped and must be constrained to the caller's tenant.
  - _Fix:_ Require/accept tenantId for verification operations and include AND tenant_id = ${tenantId} in all SELECTs against code_proposals, or validate that the proposal belongs to the tenant before proceeding.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint: `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` — this reads rows for all tenants.
  - _Fix:_ Do not perform an unscoped read against a tenant-scoped table. Either (a) add an explicit tenant_id = ${tenantId} WHERE clause for the tenant context, or (b) make the global scan explicit and gated behind an _allowGlobalSystemUseOnly flag AND ensure you still constrain every subsequent per-row mutation by tenant_id at the SQL level (preferred: perform per-tenant queries to avoid any cross-tenant reads).
- **server/routes.ts:166** — SELECT from tenant-scoped table `file_storage` without tenant_id constraint. The startup restoreUploadsFromDb() reads all file_storage rows (all tenants) and restores files to disk, violating tenant isolation enforced only by application-level filters.
  - _Fix:_ Scope the query to a specific tenant (e.g. include WHERE file_storage.tenant_id = <tenantId>) or iterate per-tenant explicitly under an admin-only code path. If this is intended as a cross-tenant admin maintenance task, ensure it runs only with explicit platform-admin privileges/auditing and does not write tenant files into a public path accessible without verifying tenant ownership. Prefer explicit per-tenant queries instead of a global unscoped SELECT.
- **server/routes.ts:1117** — SELECT from tenant-scoped table `file_storage` without tenant_id constraint in uploads middleware. The code fetches all owners for a basename (db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))) and then checks membership in application logic, which performs an unscoped read across tenants.
  - _Fix:_ Avoid unscoped reads. First check for an owner row scoped to the authenticated tenant: SELECT ... FROM file_storage WHERE filename = <baseName> AND tenant_id = <authedTenantId> LIMIT 1. If found, allow. If not found, return 404 (do not probe other tenants). If you must detect basename collisions for metrics, perform that as an admin-only operation or aggregate in a way that does not expose other tenants' data to arbitrary requesters.
- **server/routes.ts:2142** — INSERT into tenant-scoped table `presenter_sessions` uses tenantId directly from req.body without validating that the caller is authorized for that tenant. This allows an authenticated caller (who passes the internal key) to create rows attributed to any tenant id supplied in the request.
  - _Fix:_ Do not accept arbitrary tenantId from request body. Derive tenantId from the authenticated session (getTenantFromRequest) or require and verify platform-admin privileges before allowing an explicit tenantId. If an internal/shared key is used, additionally validate that the caller is authorized to act for the supplied tenant (e.g. check membership/ownership or disallow arbitrary tenant assignment).
- **server/routes.ts:4325** — SELECT from tenant-scoped table `conversations` is missing a tenant_id constraint. The code queries `SELECT project_id FROM conversations WHERE id = ${conversationId} ...` without verifying the tenant, which can leak or return data belonging to other tenants.
  - _Fix:_ Restrict the query to the caller's tenant. For example: `SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL`. Prefer using the storage layer or Drizzle helpers that enforce tenant scoping and verify the conversation belongs to the tenant before using returned IDs.
- **server/routes/admin.ts:693** — SELECT from `tool_performance` executed with no tenant_id constraint — this table appears to be tenant-scoped elsewhere (tenant-specific heartbeats/tools are used in other queries). Querying tenant-scoped data without constraining by tenant_id can leak cross-tenant data.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise restrict this admin query to the intended tenant(s). If the intent is to scan across all tenants, require an explicit, strongly-authorized parameter (and validate it) or run under a controlled system account and document that this table is intentionally global.
- **server/routes/admin.ts:706** — SELECT from `ai_insights` (stuckInsights) executed with no tenant_id constraint. `ai_insights` is used elsewhere in tenant-scoped contexts and must not be read across tenants without an explicit tenant filter or ownership check.
  - _Fix:_ Constrain the query by tenant_id (e.g. AND tenant_id = ${tenantId}) or require an explicit admin-scoped tenant parameter that is validated. Ensure any admin cross-tenant access is deliberate and auditable.
- **server/routes/admin.ts:716** — SELECT from `heartbeat_tasks` (disabledTasks) executed with no tenant_id constraint. Other code in the repo queries heartbeat_tasks with tenant filtering — this inconsistent usage can leak tenant data.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (or other appropriate tenant restriction) to the query, or explicitly document and gate cross-tenant reads with a validated tenant selector for platform admins.
- **server/routes/admin.ts:728** — SELECT from `ai_insights` (stuckPending) executed with no tenant_id constraint. This reads potentially sensitive per-tenant incident records without verifying tenant ownership.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or accept and validate an explicit tenant id param) so only records belonging to the intended tenant are returned. For platform-level inspections, require a validated tenant argument and stronger authorization checks.
- **server/routes/conversations.ts:98** — SELECT from tenant-scoped table 'project_conversations' missing tenant filter: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`
  - _Fix:_ Add tenant scoping to the WHERE clause: `... AND tenant_id = ${tenantId}` so the existence check is limited to the caller's tenant.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` omits tenant predicate. The code executes `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` after fetching a message — the UPDATE does not include `tenant_id = ${tenantId}` and can modify another tenant's row if an ID is guessed or the prior fetch is bypassed.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`. Alternatively use Drizzle's update API with a where(eq(inboxMessages.id, msg.id), eq(inboxMessages.tenantId, tenantId)). Ensure every SELECT/UPDATE/DELETE/INSERT on tenant-scoped tables always includes an explicit tenant_id check (or validates ownership) before mutating rows.
- **server/routes/projects.ts:33** — Tenant-scoped dependent subqueries in the projects list lack explicit tenant constraint. The SELECT uses subselects (project_files, project_notes, project_conversations) without filtering those tables by tenant_id.
  - _Fix:_ Constrain each subquery to the caller's tenant. For example: SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}. Alternatively JOIN those tables to projects and ensure the WHERE includes p.tenant_id = ${tenantId} or use Drizzle eq(table.tenantId, tenantId).
- **server/routes/projects.ts:232** — UPDATE projects sets updated_at WHERE id = ${projectId} but lacks tenant constraint. This UPDATE can modify a project owned by another tenant if projectId is manipulated.
  - _Fix:_ Add tenant scoping to the UPDATE. Example: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}; or use Drizzle .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))).
- **server/routes/projects.ts:300** — UPDATE projects sets updated_at WHERE id = ${projectId} but lacks tenant constraint (multipart upload path).
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause for the UPDATE, or use Drizzle .where with eq(projects.tenantId, tenantId) to ensure only the caller's tenant row is modified.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} has no tenant filter. Although there was a prior projCheck, every read against a tenant-scoped table must include an explicit tenant constraint.
  - _Fix:_ Add tenant filtering to the SELECT, e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC (or use a JOIN to projects and constrain by projects.tenant_id = ${tenantId}).
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} has no tenant constraint. This allows lookup of files by path across tenants when project_id/file_path are manipulated.
  - _Fix:_ Constrain the SELECT by tenant_id. Example: SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} AND tenant_id = ${tenantId} LIMIT 1. Verify tenantId is derived from the authenticated session.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} omits tenant constraint. Deleting without tenant scoping risks removing another tenant's file if IDs are manipulated.
  - _Fix:_ Include tenant_id in the DELETE where clause. Example: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}; or perform a scoped delete via Drizzle with eq(fileStorage.tenantId, tenantId).
- **server/sculptor.ts:76** — INSERT into sculptor_sessions sets parent_session_id directly from caller-supplied params.parentSessionId without verifying ownership. A caller can link a new session to a parent session that belongs to a different tenant.
  - _Fix:_ If parentSessionId is provided, first SELECT the referenced sculptor_sessions row and verify it has tenant_id = params.tenantId (or reject). Only insert parent_session_id after confirming the referenced session belongs to the caller's tenant.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id constraint — this reads aggregate data across all tenants for a tenant-scoped table.
  - _Fix:_ Scope the query to a specific tenant. Example: SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(research_sessions.tenantId, tenantId))). Ensure tenantId is a trusted value appropriate for the operation (or iterate per-tenant if this is an administrative action).
- **server/seed.ts:3092** — SELECT id, name, model FROM research_programs WHERE is_active = true is querying a tenant-scoped table with no tenant_id filter — it may read other tenants' rows.
  - _Fix:_ Add a tenant constraint to the SELECT, e.g. SELECT id, name, model FROM research_programs WHERE is_active = true AND tenant_id = ${tenantId} (or use Drizzle .where(eq(research_programs.tenantId, tenantId))). If the intent is to operate across tenants, make that explicit and ensure the caller is authorized to do cross-tenant operations.
- **server/seed.ts:4270** — Selecting all rows from tenant-scoped table `heartbeat_tasks` via db.select().from(heartbeatTasks) with no tenant filter. This loads heartbeats across tenants into memory and drives subsequent logic (insertion checks), causing cross-tenant effects.
  - _Fix:_ Limit the selection to the target tenant: db.select().from(heartbeatTasks).where(eq(heartbeatTasks.tenantId, tenantId)) (or .where(eq(heartbeatTasks.tenantId, 1)) for owner seed). Use the tenant-scoped result for per-tenant decisions.
- **server/seed.ts:4925** — UPDATE agent_knowledge SET content = ..., priority = 10, updated_at = NOW() WHERE id = ${rows[0].id} — writing to tenant-scoped `agent_knowledge` by id only, with no tenant_id constraint. While id was obtained from a tenant-filtered SELECT, the UPDATE query itself lacks an explicit tenant guard.
  - _Fix:_ Include tenant_id in the WHERE clause as well (defensive check):
UPDATE agent_knowledge SET content = ${platformBriefingContent}, priority = 10, updated_at = NOW()
WHERE id = ${rows[0].id} AND tenant_id = 1
This prevents accidental writes if ids are reused or inputs change; prefer explicit tenant checks on all UPDATE/DELETE/SELECT statements.
- **server/self-improvement.ts:170** — detectStagnation builds and executes a SELECT on the tenant-scoped `experiments` table without requiring a tenant constraint when tenantId is not provided (the .where() can end up being just eq(experiments.category, ...)). This allows cross-tenant reads if called without tenantId.
  - _Fix:_ Require an explicit tenantId (fail-closed) or always include a tenant filter in the WHERE (e.g. include eq(experiments.tenantId, tenantId)). If the function must support global queries, introduce a distinct, explicit API for admin/global scans and document it clearly.
- **server/self-improvement.ts:511** — getExperimentHistory can return db.select().from(experiments) with no WHERE clause when no category/tenantId conditions are passed — allowing an unscoped read of the tenant-scoped `experiments` table.
  - _Fix:_ Fail-closed: require tenantId as a mandatory parameter (or return an error/empty list when tenantId is missing). Always include AND eq(experiments.tenantId, tenantId) in the query to scope reads to the caller's tenant.
- **server/skillify.ts:91** — SELECT from `messages` (recentMsgs) is executed with only messages.conversationId = conversationId and NO tenant constraint. This allows reading messages across tenants if conversationId is guessable.
  - _Fix:_ Constrain the query to the caller's tenant. Either add an explicit tenant check (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or join to conversations and filter by conversations.tenant_id = tenantId. Alternatively, call/verify assertConversationInTenant(conversationId, tenantId) and still include a tenant-scoped WHERE to satisfy the invariant.
- **server/skillify.ts:201** — SELECT from `messages` inside analyzeConversation is using only messages.conversationId = conversationId with NO tenant constraint. Messages are tenant-scoped and this reads them without scoping to caller tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or join messages to conversations and filter by conversations.tenant_id = tenantId. At minimum, verify ownership via assertConversationInTenant and add the tenant filter to the messages query.
- **server/skillify.ts:221** — SELECT from `personas` (personaRows) queries personas by id but does NOT verify tenant ownership (no personas.tenantId = tenantId filter). Reading persona records referenced by a conversation without validating tenant ownership violates the tenant-isolation invariant.
  - _Fix:_ Restrict persona lookups to the caller's tenant or otherwise validate ownership. For example add eq(personas.tenantId, tenantId) to the WHERE clause or call a guard that asserts the persona belongs to tenantId before reading it. If personas are intentionally global, document that explicitly and use a different, clearly-named global table.
- **server/skillify.ts:329** — SELECT from `messages` (recentMsgs in skillifyConversation) uses only messages.conversationId = conversationId and lacks any tenant constraint. This reads conversation messages without scoping to the caller's tenant.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or join to conversations and filter by conversations.tenant_id = tenantId. Also consider using assertConversationInTenant before reading messages, and always include the tenant_id predicate in the SQL.
- **server/storage.ts:295** — Read from tenant-scoped table `messages` via raw SQL lacks tenant_id constraint (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL). This can return citations across tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. add AND tenant_id = ${tenantId}) or fetch citations via Drizzle with an explicit tenant filter. Prefer passing the caller's tenantId into attachCitationMetadata and using a parameterized WHERE eq(messages.tenantId, tenantId). Avoid raw SQL without tenant filters for tenant-scoped tables.
- **server/storage.ts:396** — UPDATE on tenant-scoped table `messages` (UPDATE messages SET citations = ... WHERE id = ${messageId}) has no tenant_id constraint, allowing cross-tenant mutation.
  - _Fix:_ Require and validate tenantId for attachCitations, then include AND tenant_id = ${tenantId} in the WHERE clause (or use Drizzle .where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId)))). Do not perform unscoped updates to tenant-scoped tables.
- **server/storage.ts:530** — INSERT into tenant-scoped table `memory_entries` (db.insert(memoryEntries).values(data)) does not assert or validate that data.tenantId is present/owned by caller. Inserts may proceed with missing/incorrect tenant_id or rely on callers to supply it without validation.
  - _Fix:_ Fail-closed: require & validate a positive integer tenantId before inserting (assertValidTenantId or similar). If tenantId must be passed by caller, explicitly check it and throw if missing/invalid. Prefer typed signatures that mandate tenantId for tenant-scoped inserts.
- **server/storage.ts:803** — UPDATE on tenant-scoped table `memory_entries` (UPDATE memory_entries SET embedding = ... WHERE id = ${id}) lacks tenant filtering — can modify rows across tenants by id.
  - _Fix:_ Require and validate tenantId and include tenant scoping in the WHERE clause (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))) or otherwise ensure the caller's tenant owns the row before updating.
- **server/storage.ts:815** — UPDATE on tenant-scoped table `agent_knowledge` (UPDATE agent_knowledge SET embedding = ... WHERE id = ${id}) lacks tenant constraint and can overwrite other tenants' rows.
  - _Fix:_ Require tenantId and scope the UPDATE with AND tenant_id = ${tenantId} (or Drizzle eq(agentKnowledge.tenantId, tenantId)). Validate tenant ownership before mutation.
- **server/storage.ts:824** — SELECT from tenant-scoped table `memory_entries` (getMemoriesWithoutEmbeddings) is unscoped: SELECT ... FROM memory_entries WHERE status='active' AND embedding IS NULL LIMIT ... with no tenant filter, returning data across tenants.
  - _Fix:_ Accept/require tenantId (or otherwise determine caller tenant) and add eq(memoryEntries.tenantId, tenantId) to the WHERE clause. Do not return tenant-scoped data without tenant scoping.
- **server/storage.ts:830** — SELECT from tenant-scoped table `agent_knowledge` (getKnowledgeWithoutEmbeddings) lacks tenant scoping: .where(and(notExpired, isNull(agentKnowledge.embedding))). No tenant filter present.
  - _Fix:_ Require tenantId or pass tenant scoping into this background helper and include eq(agentKnowledge.tenantId, tenantId) in the WHERE clause before returning results.
- **server/storage.ts:838** — Global UPDATE on tenant-scoped `memory_entries` (archiveExpiredMemories uses UPDATE memory_entries SET status='archived' WHERE status='active' AND expires_at <= NOW()) has no tenant scoping and will archive rows across all tenants.
  - _Fix:_ If this operation is intended to be per-tenant, iterate tenants or accept a tenantId and include tenant filtering in the UPDATE (e.g. AND tenant_id = ${tenantId}). If it must run globally for maintenance, ensure it's executed under an admin context with explicit design review — but still consider per-tenant scoping to avoid accidental cross-tenant side effects.
- **server/storage.ts:855** — Global UPDATE on tenant-scoped `memory_entries` (archiveStaleMemories) runs without tenant constraint and can affect all tenants' rows.
  - _Fix:_ Scope the archival operation by tenant (require tenantId or loop tenants) or add tenant_id = ${tenantId} to the WHERE clause. Ensure background maintenance is implemented with explicit tenant scoping or safe admin-only semantics.
- **server/storage.ts:880** — getMemoryStats falls back to SELECTing all rows from tenant-scoped `memory_entries` and `agent_knowledge` when tenantId is not provided (db.select(...).from(memoryEntries) / db.select(...).from(agentKnowledge)). These unscoped reads return platform-wide data.
  - _Fix:_ Require tenantId for stats or fail-closed when tenantId is omitted. If admin usage is intended, explicitly restrict the API to admin roles and document the behavior; otherwise add tenant filters to the SELECTs.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding updates tenant-scoped table `felix_proposals` without tenant scoping (UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL). This can write across tenants.
  - _Fix:_ Require caller to provide/validate tenantId (or re-query to confirm the proposal's tenant) and include AND tenant_id = ${tenantId} in the WHERE clause. Always scope writes to tenant-scoped tables by tenant_id.
- **server/surprise-scorer.ts:139** — UPDATE actual_outcome_embedding on `felix_proposals` uses WHERE id = ${proposalId} with no tenant constraint, allowing cross-tenant writes.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or verify ownership before updating actual_outcome_embedding. Ensure the tenantId used is trusted and validated.
- **server/surprise-scorer.ts:212** — UPDATE that writes surprise_score and surprise_band to `felix_proposals` uses WHERE id = ${proposalId} with no tenant_id constraint, permitting cross-tenant mutation.
  - _Fix:_ Scope the UPDATE with tenant_id (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or validate ownership prior to the update. All writes to tenant-scoped tables must include an explicit tenant filter.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (checks existing tool name across all tenants).
  - _Fix:_ Include tenant filtering in the query (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) so lookups are scoped to the caller's tenant. Enforce name-uniqueness per-tenant, not globally.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without constraining tenant_id (reads the inserted row without verifying tenant ownership).
  - _Fix:_ Constrain reads to the tenant (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) or pass/verify tenantId on the inserted row before returning it.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` (.update(...).where(eq(custom_tools.id, tool.id))) performed without tenant_id constraint — can update a row belonging to another tenant.
  - _Fix:_ Add tenant scoping to updates, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, <expectedTenantId>))). Ensure the function has or validates the tenantId before mutating rows.
- **server/tool-learning.ts:365** — UPDATE on tenant-scoped table `custom_tools` to increment usageCount is constrained only by id (.where(eq(custom_tools.id, tool.id))) and lacks tenant_id filtering.
  - _Fix:_ Restrict the update by tenant as well (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) — use the tenantId parameter available to executeCustomTool to ensure the operation cannot affect other tenants' rows.
- **server/tools.ts:4553** — SELECT from tenant-scoped table `presenter_sessions` executed without constraining to caller tenant (no WHERE tenant_id = ...) — reads presenter token for a presentation (presentation_id = slideResult.presentationId) with no tenant ownership check.
  - _Fix:_ Enforce tenant scoping before querying presenter_sessions. Either (a) include tenant_id = ${params._tenantId} in the WHERE clause (e.g. WHERE presentation_id = ${presentationId} AND tenant_id = ${params._tenantId}), or (b) call and verify an ownership guard (e.g. assertPresentationInTenant(presentationId, params._tenantId)) and only then select the token. Ensure any subsequent INSERT/SELECT/UPDATE/DELETE against presenter_sessions is always tenant-constrained.
- **server/tools.ts:8883** — UPDATE on tenant-scoped table `proposed_skills` uses WHERE eq(proposed_skills.id, row.id) but does NOT constrain by tenant_id. This can allow cross-tenant updates if an attacker can influence row.id or otherwise cause the update to target another tenant's row.
  - _Fix:_ Include the tenant constraint in the WHERE clause. For example, use .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or equivalent SQL (e.g. WHERE id = $1 AND tenant_id = $2). Always enforce tenant_id on updates to tenant-scoped tables.
- **server/tools.ts:8909** — UPDATE on tenant-scoped table `proposed_skills` (marking accepted/promoted) uses .where(eq(proposed_skills.id, row.id)) with no tenant_id guard. Tenant isolation is not enforced in this update.
  - _Fix:_ Add tenant scoping to the update: e.g. .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure every update to proposed_skills (and other tenant-scoped tables) includes tenant_id filtering or uses a helper that enforces it.
- **server/tools.ts:8924** — UPDATE on tenant-scoped table `proposed_skills` (marking rejected) uses .where(eq(proposed_skills.id, row.id)) without constraining tenant_id, violating tenant isolation.
  - _Fix:_ Restrict the update to the caller's tenant by adding tenant_id to the WHERE: .where(and(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or equivalent. Audit other updates to proposed_skills to ensure tenant checks are always present.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table `browser_workflows` without tenant_id constraint (no WHERE tenant_id = ...). This UPDATE uses only id in the WHERE clause, allowing a cross-tenant update if ids collide or context is spoofed.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. include tenant_id in the WHERE: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); or use Drizzle's .where(eq(browserWorkflows.tenantId, tenantId)) so every write/read is explicitly scoped to ctx.tenantId.
- **server/tools/domains/files/handlers.ts:838** — UPDATE on `projects` table omits tenant_id in the WHERE clause (UPDATE projects SET updated_at = ... WHERE id = ${projId}). This is an UPDATE against a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId} (or use Drizzle .where(eq(projects.tenantId, tenantId))). Even if projId was obtained from a prior tenant-scoped query, include tenant_id in the WHERE for defense in depth and to satisfy the invariant.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE on tenant-scoped table `knowledge_triples` does not constrain by tenant_id. The statement `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` can modify a row by id across tenants if an id is guessed/leaked.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (defense-in-depth even if the selected rows were tenant-scoped). Example: `WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Or, when using Drizzle, use .where(and(eq(table.id, row.id), eq(table.tenantId, tenantId))). Ensure all UPDATE/DELETE statements against tenant-scoped tables always include tenant_id (or equivalent Drizzle predicate) and prefer parameterized queries rather than relying on previously-selected rows for scoping.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` is executed without any tenant_id constraint (reads provider keys across all tenants).
  - _Fix:_ Add an application-level tenant filter to the query (e.g. WHERE tenant_id = ${tenantId}) or otherwise restrict/aggregate the data returned so raw per-tenant key material or tenant-scoped counts cannot be enumerated. If this is intended admin-only behavior, make it explicit, audit it, and return only aggregated/sanitized metrics (never raw keys or per-tenant rows) or require an explicit admin API that documents the cross-tenant scope.
- **server/tools/domains/security/handlers.ts:260** — COUNT/DISTINCT query on `provider_keys` runs without tenant constraint (enumerates provider configurations across tenants).
  - _Fix:_ Constrain the query to the caller's tenant_id (e.g. WHERE tenant_id = ${tenantId}), or compute admin-appropriate aggregates in a controlled/explicit admin-only path that does not expose per-tenant details. Ensure any cross-tenant scans are logged/audited and return only aggregated, non-sensitive metrics.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with NO tenant_id constraint. Every write to a tenant-scoped table must also constrain by tenant_id to prevent cross-tenant modification.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. add `AND tenant_id = ${tid}` to the WHERE clause (or use Drizzle's .where(eq(table.tenantId, tid)). Prefer composing the predicate as id+tenant_id rather than relying on a prior SELECT alone.
- **server/tools/domains/strategic-memory/handlers.ts:147** — UPDATE against tenant-scoped table `memory_entries` uses `WHERE id = ANY(${ids})` with NO tenant_id constraint. This updates rows by id only and can cross tenant boundaries if an id is reused or exposed.
  - _Fix:_ Restrict the UPDATE to the caller's tenant, e.g. `WHERE id = ANY(${ids}) AND tenant_id = ${tid}` (or use a parameterized Drizzle update with a combined where clause eq(table.tenantId, tid) AND id IN ids). Avoid relying solely on previously-selected ids without an explicit tenant filter on the write.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} in a one-line UPDATE without a tenant_id constraint. The write is not explicitly scoped to ctx.tenantId.
  - _Fix:_ Add tenant scoping to the UPDATE: `WHERE id = ${id} AND tenant_id = ${tid}`, or use Drizzle's .where(eq(table.id, id), eq(table.tenantId, tid)). Ensure all writes include the tenant constraint even when the id was obtained from a prior constrained query.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` executed without a tenant_id filter. This code queries all pairing rows across tenants by title, violating the invariant that every read against tenant-scoped tables must be constrained to a tenant.
  - _Fix:_ Restrict the query to the intended tenant (e.g. add AND tenant_id = ${tenantId}) or else require this routine be run only by an operator/privileged process and make the cross-tenant intent explicit and auditable. Prefer fetching rows by both title AND tenant_id, or accept a tenantId parameter and enforce db-level filtering: SELECT ... FROM agent_knowledge WHERE category='messaging_pairing' AND title=${...} AND tenant_id=${tenantId} ...
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` executed without constraining by tenant_id. The code reads conversation rows by id but does not include tenant filtering to verify ownership, risking cross-tenant data access or mis-verification.
  - _Fix:_ Constrain the query to the claimed tenant when verifying pairing ownership. Use: SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 (or the equivalent Drizzle eq checks) so the check confirms the conversation row belongs to the claimed tenant rather than returning arbitrary rows across tenants.
- **server/video-job-runner.ts:228** — UPDATE on tenant-scoped table `video_jobs` in recoverStaleVideoJobs() runs without any tenant_id filter — it will mark stale jobs across all tenants. This violates the invariant and can cause jobs from unrelated tenants to be failed/modified by this sweep.
  - _Fix:_ Limit stale-job recovery to a specific tenant scope or make the sweep per-tenant. Options: (1) iterate tenants and update only rows WHERE tenant_id = ${tenantId} AND updated_at < cutoff, or (2) restrict this operation to an admin-only service account with explicit acceptance of cross-tenant semantics. Prefer adding tenant_id to the WHERE clause to avoid unintended cross-tenant modifications.
- **server/watchlist.ts:16** — addWatchlistItem inserts persona IDs (created_by_persona_id, escalate_to_persona_id) provided by the caller without verifying those persona IDs belong to the same tenant. This lets a caller reference foreign persona rows from other tenants when creating watchlist items.
  - _Fix:_ Before inserting, validate any incoming persona id(s) belong to the tenant (e.g. SELECT id FROM personas WHERE id = $1 AND tenant_id = $2) and reject or null them if not-owned. Alternatively derive persona ids from the authenticated context rather than accepting raw ids from callers.
- **server/watchlist.ts:95** — createAlert inserts a watchlist_alerts row with watchlist_item_id taken from caller parameters without verifying the referenced watchlist_item belongs to the same tenant. Although tenant_id is set on the alert, the foreign id can point to an item owned by another tenant.
  - _Fix:_ Validate the watchlist_item_id before inserting (e.g. SELECT id FROM watchlist_items WHERE id = $1 AND tenant_id = $2). If no match, return an error. Enforce ownership checks for all foreign ids provided by callers rather than trusting the identifier alone.
- **server/wiring-invariants.ts:535** — Memory-growth probe uses sql.raw to SELECT counts and pg_total_relation_size FROM various memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without tenant filters — potentially aggregates tenant data across all tenants.
  - _Fix:_ If these tables are tenant-scoped, run the probe per-tenant or add tenant-aware WHERE clauses (and avoid raw string interpolation with tenant-derived values). If some tables are global, explicitly document which ones are global and only exempt those from filtering.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory is performed without a tenant filter; the code even treats this as "across all tenants" which bypasses per-tenant isolation.
  - _Fix:_ If graph_memory is tenant-scoped, add WHERE tenant_id = $1 or iterate per-tenant. If intentionally global, document the exception and ensure only admin contexts can run this check. In general avoid un-scoped SELECTs on tenant-scoped tables.

### MEDIUM (27)

- **server/auth.ts:1032** — Password reset token lookup does not include tenant scoping: getResetToken queries password_reset_tokens WHERE token = ${tokenHash} without constraining tenant_id. Although token is a secret, the table is tenant-scoped (has tenant_id) and the lookup should not rely solely on token uniqueness to enforce isolation.
  - _Fix:_ Constrain reset-token lookups and deletions by tenant when possible (e.g. WHERE token = ${tokenHash} AND tenant_id = ${expectedTenantId}), or encode tenant context into the token lookup (store/lookup by a composite key or include tenant in the presented reset flow). At minimum document and review the threat model for token-only lookups and ensure tokens cannot be abused to operate across tenants.
- **server/data-protection.ts:182** — Backup query reads messages without scoping by tenant (SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}). Even though the conversation row was validated earlier, queries against tenant-scoped tables should also include tenant_id to enforce isolation at every access point.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages SELECT (and similar selects) so every read is explicitly scoped to the caller's tenant. This prevents depending on implicit foreign-key semantics for tenant isolation.
- **server/data-protection.ts:190** — Archive SELECT reads compaction_archives without tenant scoping (SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId}). Tenant scope is not applied to this tenant-scoped table.
  - _Fix:_ Constrain by tenant_id as well (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise ensure the query is executed in a tenant-scoped transaction context.
- **server/data-protection.ts:325** — When backing up projects the code selects project_notes by project_id only (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) with no tenant predicate. This reads tenant-scoped rows without explicit tenant scoping.
  - _Fix:_ Add tenant_id scoping (AND tenant_id = ${tenantId}) when selecting project_notes. Even when you obtained the project row previously, enforce tenant_id on all subsequent reads to avoid accidental cross-tenant access.
- **server/data-protection.ts:336** — project_files are read by project_id alone (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}) without tenant scoping. This violates the rule that every read against tenant-scoped tables must include tenant_id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause for project_files selects, or otherwise ensure the query runs in a tenant-scoped transaction that enforces tenant isolation.
- **server/data-protection.ts:369** — Conversation message count is checked with SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} — no tenant_id predicate. This reads tenant-scoped messages without explicit tenant scoping.
  - _Fix:_ Use WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId} when counting messages, or validate tenant ownership before performing the count. Ensure every SELECT/COUNT against tenant-scoped tables includes tenant_id.
- **server/delivery-pipeline.ts:1049** — getDeliveryByStripePayment SELECTs from tenant-scoped table delivery_logs by stripePaymentId without tenant_id constraint, returning a row globally.
  - _Fix:_ Add a tenantId parameter and filter on tenant_id (WHERE stripe_payment_id = $1 AND tenant_id = $2). If this helper is intentionally global, document and protect callers; otherwise require caller-supplied tenant context.
- **server/email.ts:431** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 runs during inbox polling without tenant scoping. This read can return a match from another tenant and cause logic/branching mistakes (skip/consider duplicate) across tenants.
  - _Fix:_ Filter by tenant_id or inbox_id in the SELECT (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId}) so existence checks are tenant-scoped.
- **server/email.ts:476** — backfillEmptyBodies SELECTs rows FROM inbox_messages WHERE body_text IS NULL ... without a tenant filter; this will iterate rows across tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (AND tenant_id = ${tenantId}) or to a specific inbox_id. If this is meant to run globally, add explicit safeguards and operator consent in code/comments.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot queries messages (SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user') without tenant scoping. messages appears to be tenant-scoped, so this aggregates across tenants.
  - _Fix:_ If the deprecation logic is intended to be tenant-specific, add a tenant_id filter to the queries. If it is intentionally global, add comments/authorization checks to make that explicit and document the intended global scope.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot and related helpers query tool_performance (COUNT DISTINCT, SELECT DISTINCT tool_name, etc.) without tenant_id constraints, touching a likely tenant-scoped table globally.
  - _Fix:_ Scope these queries by tenant_id when running per-tenant deprecation logic; otherwise document and protect the intended global behavior. Prefer explicit tenant param and include WHERE tenant_id = ${tenantId}.
- **server/dormant-deprecation.ts:204** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with no tenant filter, returning results across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) if this computation should be per-tenant, or gate this routine behind an admin/global-only execution path and document it.
- **server/lib/scheduled-post-runner.ts:306** — Manual construction of a Postgres array literal (`arrLit = '{"a","b"}'`) from request-derived `params.platforms` and interpolation into a SQL template (`... VALUES (${params.tenantId}, ${arrLit}::text[], ...)`). Building SQL array literals by string composition from request data is fragile and risks SQL/semantic injection or casing/escaping errors.
  - _Fix:_ Use parameterized array binding instead of composing a `'{...}'` literal. For example, pass the JS array as a query parameter and use the DB driver's array parameter support or Drizzle's array helpers (or explicit parameter placeholders) so the platform array is bound safely (e.g. use `$n::text[]` with a parameterized array). If you must stringify, centralize and hard-validate/whitelist inputs and prefer to construct arrays from strongly-validated constants (which you already do) — but prefer parameter binding to manual literal construction.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is executed with no tenant_id filter (SELECT ... FROM agent_runs WHERE status = 'running' ...). This scans/returns rows across all tenants and exposes tenant_id values in the report without per-caller tenant scoping.
  - _Fix:_ If this is intended as a global admin audit, restrict access to this routine to an admin/operator role and document it clearly. If it should be tenant-scoped, add `AND tenant_id = ${tenantId}` and accept a tenant parameter. Otherwise, minimize returned tenant detail and ensure operators calling this have explicit authorization and audit logging.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` runs without tenant_id filtering (reads rows across tenants). Even if informational, it exposes multi-tenant data to this caller.
  - _Fix:_ Same remediation: either make this function explicitly admin-only with audit controls, or scope the query to a single tenant by adding `AND tenant_id = ${tenantId}` (and accept tenantId as an argument). If table absence is expected in some environments, keep the existing guard but still limit tenant exposure when present.
- **server/minds-engine.ts:214** — createTicket builds a Postgres array literal from caller-supplied params.dependsOn (`{${params.dependsOn.join(",")}}`) and embeds it in the SQL VALUES. This is string-interpolated SQL using request-derived values and also stores foreign ticket IDs without verifying ownership.
  - _Fix:_ Avoid constructing SQL array literals by string interpolation. Use proper parameterized array binding (pass deps as an array parameter) or Drizzle's array helpers. Additionally, validate/verify each dependsOn id belongs to the same tenant before persisting, or reject cross-tenant references.
- **server/persona-export.ts:39** — Query against `skills` omits tenant scoping: `SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ...` — the tenant_id is not included, so tenant-specific skill rows may be read across tenants.
  - _Fix:_ Include tenant filtering. For example: `WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR persona_id IS NULL` (or otherwise ensure the intended semantics for global vs tenant-scoped skills are correctly enforced). Always include tenant_id for tenant-scoped rows.
- **server/personality-files.ts:8** — personality_files table defines tenant_id INTEGER NOT NULL DEFAULT 1. A DEFAULT can mask missing tenant handling and cause writes to be attributed to tenant 1 when caller omitted tenant_id, enabling accidental cross-tenant data leakage.
  - _Fix:_ Remove the DEFAULT on tenant_id and keep tenant_id NOT NULL with no default so every INSERT must explicitly supply tenant_id. Audit any existing INSERTs to ensure tenant_id is always passed from an authoritative, validated source (caller auth context).
- **server/routes/admin-zombie-detector.ts:38** — Query over `agent_trace_spans` (used to build tool usage 'used' CTE) runs without any tenant_id constraint. If `agent_trace_spans` is tenant-scoped, this returns cross-tenant usage data.
  - _Fix:_ If `agent_trace_spans` is tenant-scoped, add AND tenant_id = ${tenantId} (or equivalent) to the query. If the endpoint intentionally aggregates across tenants for platform admins, make that explicit, require and validate the admin tenant/authorization, and document that cross-tenant aggregation is intended.
- **server/storage.ts:934** — getHeartbeatTask selects from tenant-scoped `heartbeat_tasks` by id without any tenant_id constraint (SELECT ... FROM heartbeat_tasks WHERE id = ${id}).
  - _Fix:_ If this function is intended to be tenant-scoped, add a tenantId parameter and use eq(heartbeatTasks.tenantId, tenantId) or otherwise validate ownership. If it's intentionally global for maintenance, make that explicit and limit access to admin contexts.
- **server/storage.ts:991** — getDueHeartbeatTasks runs a raw SELECT on tenant-scoped `heartbeat_tasks` without tenant filtering (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ...).
  - _Fix:_ If the worker is intended to process tasks across tenants, ensure this is an admin-only background operation and audit access. Otherwise, scope by tenant and/or only return tasks for the caller's tenant. Consider adding explicit tenant scoping or clearly documenting and restricting global behavior.
- **server/storage.ts:1020** — claimHeartbeatTasks updates tenant-scoped `heartbeat_tasks` (UPDATE heartbeat_tasks SET last_run_at = ..., next_run_at = ...) without any tenant constraint in the WHERE clause.
  - _Fix:_ Include tenant scoping when claiming tasks (e.g. AND tenant_id = ${tenantId}), or ensure taskIds passed are already validated to belong to the expected tenant. Prefer explicit tenant ownership checks before mutating tenant-scoped rows.
- **server/video-job-runner.ts:204** — SELECT from tenant-scoped table `video_jobs` (cancelRequested) is performed by isCancelRequested(jobId) without constraining by tenant_id. This reads a tenant-scoped flag by jobId only, lacking tenant scoping.
  - _Fix:_ Require the caller to provide tenantId (or otherwise derive tenant context) and query with both jobId AND tenantId: SELECT cancel_requested FROM video_jobs WHERE job_id = ${jobId} AND tenant_id = ${tenantId} LIMIT 1. If the runner truly lacks tenant context, first fetch the row and assert ownership before interpreting tenant-sensitive fields, or make the function explicitly admin-only and document the reasoning.
- **server/watchlist.ts:44** — updateWatchlistItem updates escalate_to_persona_id directly from caller-supplied data without verifying that the persona belongs to the tenant. This allows setting the escalate_to_persona_id to an id owned by another tenant.
  - _Fix:_ When updating persona references, check ownership first (SELECT id FROM personas WHERE id = $1 AND tenant_id = $2) and only write the value if the check passes; otherwise reject the update or set the column to NULL/leave unchanged.
- **server/wiring-invariants.ts:108** — SELECT name FROM research_programs is executed with no tenant filter — potentially reading program rows across tenants.
  - _Fix:_ If research_programs is tenant-scoped, add a tenant_id WHERE clause (or use Drizzle .where(eq(research_programs.tenantId, tenantId))). If programs are truly global/shared, document that this table is global and exempt from per-tenant filtering.
- **server/wiring-invariants.ts:111** — SELECT name FROM personas is executed with no tenant filter — potentially reading persona rows across tenants.
  - _Fix:_ If personas are tenant-scoped, restrict the query with WHERE tenant_id = $1 (or Drizzle .where(eq(personas.tenantId, tenantId))). If personas are intentionally global, document that and centralize access via a global registry API to avoid accidental tenant leaks.
- **server/wiring-invariants.ts:142** — Duplicate SELECT name FROM research_programs with no tenant constraint (same concern as earlier).
  - _Fix:_ Apply the same fix — add tenant filtering or explicitly treat the table as global with documented justification.

### LOW (1)

- **server/lib/restraint-trust.ts:88** — Use of sql.raw(column) to inject a dynamic column identifier into an UPDATE (SET ${sql.raw(column)} = ${sql.raw(column)} + ${delta}). Even though `column` is validated/whitelisted earlier, using sql.raw() for identifiers is risky and makes the statement construct semi-dynamic SQL at runtime.
  - _Fix:_ Replace the dynamic-identifier path with an explicit switch/if that maps the allowed outcomes to concrete, parameterized updates (e.g. `if (outcome === 'action_ok') await update(..., { action_alpha: sql`action_alpha + ${delta}` })`) or use per-case db.update() calls so the column name never comes from raw SQL. Keep the whitelist check and retain tenant_id WHERE filtering (already present).

### Suppressed — confirmed false positives (201)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights SET status='applied', action_taken=... WHERE status='new' AND category IN (...) — this bulk UPDATE is not tenant-scoped and will modify rows across all tenants.
  - _Why false positive:_ Platform-wide backfill/housekeeping sweep over ai_insights; processes returned rows per-row with row-carried tenant_id and does not serve mixed tenant data to a tenant-facing caller.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE in expireStaleApprovals updates agentApprovals rows to status='expired' using only status/expiresAt conditions and no tenant_id constraint — this will expire approvals across all tenants.
  - _Why false positive:_ Global expiry housekeeping for stale approvals; bulk status mutation across tenants with per-row tenantId used for linked run updates, not a tenant-facing mixed-data path.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Tenant-scoped tables scanned without constraining to a caller tenant: the candidate query SELECTs from agent_runs JOIN agent_approvals across all tenants (no WHERE tenant_id = <caller tenant>).
  - _Why false positive:_ Background sweep intentionally scans pending approval runs across tenants and carries each row's tenant_id forward for isolated handling.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep updates agent_runs without constraining by tenant_id (where clause is only WHERE id = runId). This allows modifying another tenant's run row.
  - _Why false positive:_ Helper updates agent_runs by globally unique server-generated run id; no evidence here that runId comes from an untrusted request boundary.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState updates agent_runs without constraining by tenant_id (WHERE id = runId only).
  - _Why false positive:_ Internal state-transition helper keyed by server-generated agent_runs primary key, not shown as taking id from req/body/tool args.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState updates agent_runs without constraining by tenant_id (WHERE id = runId only).
  - _Why false positive:_ Internal replace-state helper uses globally unique run PK generated server-side; no untrusted cross-tenant boundary shown.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun updates agent_runs (setting status/result/completedAt) using WHERE id = runId only — missing tenant_id constraint.
  - _Why false positive:_ Run completion helper updates by server-issued run id in internal workflow code; matches internal worker PK pattern.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun updates agent_runs (marking a run failed) using WHERE id = runId only — no tenant_id constraint.
  - _Why false positive:_ Failure transition is an internal update by globally unique run id, with no caller-controlled tenant crossing shown.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun updates agent_runs to set status = 'paused' with WHERE id = runId only — missing tenant_id constraint.
  - _Why false positive:_ Pause helper is internal workflow code operating on server-generated run PK, not a demonstrated tenant-facing id boundary.
- **server/agentic/wake-scheduler.ts:145** _(system-sweep)_ — runDueWakes claims pending rows across tenants: the inner SELECT used in the UPDATE that moves rows to 'firing' does not constrain agent_wake_schedules by tenant_id, allowing a single scheduler run to claim/modify wakes for all tenants.
  - _Why false positive:_ Scheduler is a global due-row claim/dispatch loop; each claimed wake carries its own tenant_id into emit/update handling and is not returned as mixed tenant data.
- **server/chat-engine.ts:650** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` uses only conversation_id (query: SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...) without an explicit tenant_id constraint. This can leak other tenants' message content if conversation_id validation is bypassed.
  - _Why false positive:_ messages are read by conversation_id from prior validated conversation context; same call path has tenant-gated conversation scoping and aborts when tenant invalid
- **server/chat-engine.ts:766** _(upstream-verified)_ — Inline LATERAL subqueries select from `messages` using WHERE conversation_id = c.id (e.g. (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ORDER BY ...) ) but do NOT include tenant_id in those subqueries. Tenant scoping is missing at the subquery level.
  - _Why false positive:_ outer query scopes conversations with c.tenant_id = tid before correlated message subqueries, so subqueries inherit tenant-owned c.id only
- **server/data-protection.ts:131** _(system-sweep)_ — DELETEs in the purge loop remove rows without tenant predicates (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}). These DELETEs operate on tenant-scoped tables without constraining tenant_id and can delete another tenant's rows if IDs collide or data was mis-attributed.
  - _Why false positive:_ global purge loop intentionally sweeps expired soft-deleted conversations selected with row-carried tenant_id and only performs housekeeping deletes, no tenant-facing data mixing
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} executed with no tenant_id filter in backfillEmbeddingVecs — writes can affect rows across tenants.
  - _Why false positive:_ boot/backfill job globally repairs missing embedding_vec housekeeping field; per-row id updates do not expose or mix tenant data
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed with no tenant_id filter in backfillEmbeddingVecs — writes can affect rows across tenants.
  - _Why false positive:_ global backfill updates only embedding_vec housekeeping field for selected rows and returns aggregate counts, not tenant data
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images does not set tenant_id (or otherwise constrain the write to the caller's tenant). This writes image data tied to a session_id without verifying the session belongs to the provided tenant.
  - _Why false positive:_ presenter_slide_images appears child-scoped by session_id to tenant-scoped presenter_sessions; insert without tenant_id is consistent with transitive isolation
- **server/google-workspace.ts:2364** _(fk-transitive)_ — INSERT into presenter_slide_images (thumbnail) does not set tenant_id (or otherwise constrain the write to the caller's tenant). Same pattern as the full-size image INSERT above.
  - _Why false positive:_ thumbnail row is same child table pattern, isolated via tenant-scoped presenter_sessions parent through session_id
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(...) updates proposals by id without a tenant_id constraint. This can modify proposals belonging to other tenants.
  - _Why false positive:_ stale-proposal archival is a platform-wide maintenance sweep updating status only and not returning tenant content to another tenant
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) disables tasks by id without tenant scoping, which can affect tasks belonging to other tenants.
  - _Why false positive:_ heartbeat disablement is cross-tenant housekeeping of task enabled flags only, with no tenant-facing data exposure or mixing
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} — update misses tenant_id constraint. This can modify another tenant's schedule row by id.
  - _Why false positive:_ scheduler loop processes rows carrying sched.tenant_id and only advances housekeeping timestamps on research_schedules across tenants
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ heartbeat cron sweep updates housekeeping fields on due schedules across tenants using each row's own id/tenant context; no tenant-facing data mixing
- **server/heartbeat.ts:112** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id in WHERE, risking updating another tenant's row.
  - _Why false positive:_ global heartbeat maintenance loop advancing next_run_at only; per-row housekeeping across tenants, no cross-tenant output
- **server/heartbeat.ts:120** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — write without tenant scope (tenant_id) in WHERE clause.
  - _Why false positive:_ error-recovery branch of the same platform-wide schedule sweep, only mutates next_run_at for the current row
- **server/heartbeat.ts:260** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'claude-sonnet-4-5' WHERE id = ${t.id} — updating heartbeat_tasks by id only, missing tenant_id scoping.
  - _Why false positive:_ startup cleanup loop intentionally normalizes heartbeat task config across tenants; housekeeping-only field update, no tenant data exposure
- **server/heartbeat.ts:264** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — write lacks tenant_id constraint and can affect other tenants' tasks.
  - _Why false positive:_ startup maintenance normalizes reflection task model globally; status/config housekeeping only
- **server/heartbeat.ts:3374** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ row was just created via createHeartbeatTask with tenantId in same call path; immediate update by returned server-generated newTask.id is already ownership-bound
- **server/job-queue.ts:99** _(system-sweep)_ — claimDueJobs issues an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs ...) that claims pending jobs without any tenant_id constraint — worker may claim/modify jobs for any tenant.
  - _Why false positive:_ queue worker intentionally claims due jobs globally and returns each job with its own tenant_id for per-job processing; standard cross-tenant maintenance queue
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases step 1 UPDATE moves running jobs to failed_terminal across all tenants (no tenant_id constraint).
  - _Why false positive:_ lease-expiry reclaim is platform-wide housekeeping changing only job status/error metadata across tenants
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases step 2 UPDATE requeues expired running jobs to pending without any tenant_id constraint — affects jobs from all tenants.
  - _Why false positive:_ requeue of expired leases is global queue housekeeping only, with no tenant-facing data mixing
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob UPDATE on `agent_jobs` does not restrict by tenant_id — a caller can mark any job id (potentially from another tenant) succeeded.
  - _Why false positive:_ Worker completion updates server-generated job row by PK with attempt fencing; no untrusted tenant boundary shown.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob UPDATE on `agent_jobs` writes failure state without tenant_id constraint — may mark jobs from other tenants failed or change their state.
  - _Why false positive:_ Worker failure path reads/updates agent_jobs by server-generated PK plus attempts guard; no request-derived id shown.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into `project_files` does not set tenant_id. Per project convention tenant_id columns are NOT NULL with no default — inserting without tenant_id violates the tenant-isolation invariant and may misattribute or fail to enforce ownership.
  - _Why false positive:_ project_files has no tenant_id and is tenant-isolated transitively through tenant-scoped projects via project_id.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into `project_notes` does not include tenant_id. This violates the invariant that tenant-scoped tables must receive tenant_id explicitly on INSERTs, risking wrong-tenant writes or schema errors.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects via project_id, so omitting tenant_id is by design.
- **server/lib/system-probes.ts:163** _(system-sweep)_ — SELECT against tenant-scoped table `messages` without any tenant_id filter. The code runs `db.select({ count: sql<number>`count(*)::int` }).from(messagesTable)` and aggregates across all rows regardless of tenant.
  - _Why false positive:_ Global health probe reads only aggregate count(*) across all messages for system diagnostics; no tenant content returned or mixed.
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT INTO memory_links does not set tenant_id. Tenant_id is required for tenant-scoped tables and omitting it will create links that are not associated with the caller's tenant (or may violate NOT NULL constraints).
  - _Why false positive:_ memory_links has no tenant_id column and links tenant-scoped memory_entries by FK-like ids; isolation is transitive through memory_entries ownership.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim UPDATE flips plan status without tenant constraint. The initial claim uses UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' but does not constrain by tenant_id — a plan from another tenant could be claimed/modified.
  - _Why false positive:_ Internal server executor claims a plan by globally unique planId and returns the row's tenant_id for subsequent processing; not from a tenant-facing query in this snippet.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE sets plan status = 'failed' for an id without tenant constraint when plan has no steps (UPDATE plans SET status = 'failed' WHERE id = ${planId}). This can modify another tenant's plan if id is misused.
  - _Why false positive:_ This update occurs only after the preceding claim UPDATE returned the specific row, including tenant_id, so ownership of the targeted plan row was established earlier in the same call path.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final-status UPDATE on plans omits tenant constraint. The executor writes final status and execution_log via UPDATE plans ... WHERE id = ${planId} with no tenant_id filter, allowing cross-tenant mutation of plan state/logs.
  - _Why false positive:_ Final status write targets the same plan row successfully claimed earlier in executePlan; tenant_id was already bound from that row in the same path.
- **server/plan-executor.ts:775** _(system-sweep)_ — Boot-recovery UPDATE flips 'executing'→'approved' for stale plans without tenant scoping: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This operates across all tenants.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping sweep resetting stale status fields across tenants and only returns ids for reprocessing, not mixed tenant data.
- **server/plan-executor.ts:784** _(system-sweep)_ — SELECT pending plans for restart does not filter by tenant: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50. This will queue plans across all tenants for execution in the current process context.
  - _Why false positive:_ Selecting approved plan ids for boot-time restart is part of a platform-wide maintenance dispatcher; it queues per-row work and does not mix tenant data into one output.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} executes a write on a tenant-scoped table using only the row id and no tenant_id constraint. Without tenant scoping this can modify rows belonging to other tenants.
  - _Why false positive:_ Tasks are selected immediately above with ht.tenant_id = tenantId, so each updated id was already constrained to the tenant in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates a tenant-scoped table by id alone with no tenant_id condition, enabling cross-tenant mutation if IDs collide or are misused.
  - _Why false positive:_ Agent desks are selected immediately above with ad.tenant_id = tenantId, so the subsequent update by id operates on rows already tenant-scoped in the same path.
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} modifies a tenant-scoped table without a tenant_id condition. This can mutate proposals for other tenants.
  - _Why false positive:_ server-side verifier worker updates code_proposals by server-supplied proposalId, not a request-boundary tenant input
- **server/proposal-verifier.ts:245** _(internal-worker-pk)_ — persist() runs UPDATE code_proposals ... WHERE id = ${proposalId} without tenant scoping, persisting verification state across tenants if invoked without ownership checks.
  - _Why false positive:_ persist() is only a helper for verifier worker state updates keyed by server-invoked proposalId
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — In attemptRebaseOnDrift, UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} is executed with no tenant constraint, allowing cross-tenant mutation.
  - _Why false positive:_ attemptRebaseOnDrift is internal verifier logic mutating one proposal by server-generated id
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without scoping by tenant_id, enabling cross-tenant data changes.
  - _Why false positive:_ rebased diff persisted by internal verification worker using trusted proposal primary key
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` missing tenant_id constraint: `UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}` — allows modifying a row by id regardless of tenant ownership.
  - _Why false positive:_ scheduled maintenance loop processes rows across tenants using each row's own tenant_id; update only advances housekeeping payload on that same row id
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` missing tenant_id constraint: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}` — updates a row by id without verifying it belongs to the caller's tenant.
  - _Why false positive:_ storedId comes from prior SELECT on memory_entries constrained by tenant_id and persona/category/fact dedupe
- **server/routes.ts:4331** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id (columns are noted as NOT NULL with no default), leading to tenant-isolation violation (or DB errors) and possible cross-tenant writes.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively through tenant-owned project_id
- **server/routes/conversations.ts:97** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Why false positive:_ project ownership was checked with tenant_id, and conv.id is from just-created conversation for this tenant
- **server/routes/conversations.ts:101** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Why false positive:_ project_conversations has no tenant_id and links already-verified tenant-owned project and newly created conversation
- **server/routes/conversations.ts:103** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' lacks tenant_id constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Why false positive:_ same call path already verified project id belongs to tenant via SELECT ... projects ... tenant_id = tenantId
- **server/routes/conversations.ts:229** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})`
  - _Why false positive:_ project_notes has no tenant_id and insert is isolated through project_id after tenant-scoped project join check
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The INSERT INTO project_notes (project_id, note, author) does not set tenant_id (tenant-scoped column).
  - _Why false positive:_ project_notes has no tenant_id and parent project ownership is verified by tenant-scoped project lookup before insert
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (base64 upload) omits tenant_id. The INSERT INTO project_files (...) VALUES (...) does not set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via prior verified tenant-owned projects row
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into project_files (multipart upload) omits tenant_id. The INSERT INTO project_files (...) VALUES (...) does not set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is under previously verified project ownership
- **server/sculptor.ts:94** _(upstream-verified)_ — UPDATE to sculptor_sessions without constraining tenant_id. The update sets status/result for a session by id only (WHERE id = ${session.id}) allowing cross-tenant modification if an attacker supplies/guesses a session id.
  - _Why false positive:_ session.id comes from the immediately preceding INSERT of sculptor_sessions with params.tenantId, not untrusted input
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE sculptor_sessions (set conversation_id) uses WHERE id = ${session.id} with no tenant_id constraint, allowing modification of another tenant's session if the id is known or guessed.
  - _Why false positive:_ updates the same freshly inserted session row identified by returned session.id in same call path
- **server/sculptor.ts:156** _(internal-worker-pk)_ — monitorSession updates sculptor_sessions (status/result/tool_calls_count/tokens_used/completed_at) using WHERE id = ${sessionId} without a tenant_id constraint. The function has tenantId available but does not use it, enabling cross-tenant updates.
  - _Why false positive:_ background monitor updates a server-created session by its own serial id passed from creator, not from request input
- **server/seed.ts:3096** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} performs a write to a tenant-scoped table without constraining by tenant_id — this can modify other tenants' data.
  - _Why false positive:_ boot-time maintenance sweep fixes model field across tenants and only mutates housekeeping config values
- **server/seed.ts:3442** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` without any tenant_id constraint (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). This reads rows across all tenants and is used to create project↔conversation links, enabling cross-tenant data access/corruption.
  - _Why false positive:_ boot-time backfill intentionally scans all tenants and inserts per-row project_conversations links without returning mixed tenant data
- **server/seed.ts:4686** _(system-sweep)_ — DELETE FROM tenant-scoped table `heartbeat_tasks` without a tenant_id constraint (DELETE FROM heartbeat_tasks WHERE enabled = false ...). This can remove rows for all tenants, not just the intended tenant.
  - _Why false positive:_ boot-time cleanup sweep intentionally purges stale disabled heartbeat tasks across tenants and only logs aggregate count
- **server/seed.ts:4714** _(system-sweep)_ — UPDATE memory_entries (tenant-scoped) without tenant_id constraint (UPDATE memory_entries SET fact = REPLACE(...) WHERE ...). This will modify memory entries across all tenants.
  - _Why false positive:_ idempotent global seed/backfill normalizes memory text across tenants without returning mixed tenant data
- **server/seed.ts:4740** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' — updates `conversations` (tenant-scoped) across all tenants because no tenant_id constraint is present.
  - _Why false positive:_ global boot-time model-id backfill updates all conversations uniformly and only emits aggregate counts
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from `heartbeat_logs` (recentLogs) aggregates error counts but has NO tenant_id filter. This reads across all tenants and is later used to disable tasks; it can cause cross-tenant impact and data leakage.
  - _Why false positive:_ heartbeat_logs is schema-noted as not tenant-scoped, so global aggregation is by design
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM `heartbeat_tasks` deletes rows matching criteria without any tenant_id constraint. This can remove data for all tenants (cross-tenant data deletion).
  - _Why false positive:_ watchdog housekeeping purge deletes stale disabled heartbeat tasks globally and only reports aggregate counts
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM `event_log` deletes old event log entries without any tenant_id filter. This will remove event_log rows across all tenants.
  - _Why false positive:_ watchdog housekeeping purge deletes old processed/failed event logs globally and only reports aggregate counts
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM `heartbeat_logs` in pruneOldLogs runs without any tenant_id constraint (uses INTERVAL with sql.raw but does not scope by tenant). This can prune logs across all tenants unintentionally.
  - _Why false positive:_ heartbeat_logs is not tenant-scoped per schema, so global log pruning is intended
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table `custom_tools` without constraining tenant_id (.delete(...).where(eq(custom_tools.id, tool.id))). This can remove another tenant's row if an id is guessed or leaked.
  - _Why false positive:_ Server-internal revert helper deletes just-inserted tool row by server-held PK, not from untrusted request boundary.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. Per invariant, tenant-scoped tables must be explicitly populated with tenant_id on INSERT (columns are NOT NULL w/ no default).
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via verified tenant-owned projects row.
- **server/wiring-invariants.ts:57** _(system-sweep)_ — Query selects from tenant-scoped research_schedules (and joins research_programs) with no tenant_id constraint — reads all tenants' schedules.
  - _Why false positive:_ Boot/admin-style invariant audit intentionally scans schedules platform-wide and emits findings only, not tenant data.
- **server/wiring-invariants.ts:180** _(system-sweep)_ — Complex query in checkOutcomeCanary reads research_programs, research_experiments, code_proposals, research_sessions and research_schedules with no tenant_id constraints — aggregates/joins across all tenants.
  - _Why false positive:_ Global outcome canary is a maintenance audit across tenants producing only health findings/aggregates.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — SELECT count(*) FROM tool_performance WHERE last_success_at... has no tenant filter — aggregates tool performance across all tenants.
  - _Why false positive:_ Dormant-tools invariant check is platform-wide housekeeping using aggregate counts only, not tenant-facing data mixing.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT count(*) FROM tool_performance executed without tenant constraint (counts rows across all tenants).
  - _Why false positive:_ Historical count over tool_performance is part of global maintenance audit and returns only aggregate health info.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM messages WHERE created_at > ... runs without filtering by tenant — message rows are typically tenant-scoped and this reads across tenants.
  - _Why false positive:_ Cross-tenant message count is aggregate-only within a maintenance check; no row data returned to tenants.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE ... has no tenant constraint — exposes per-tenant tool usage across all tenants.
  - _Why false positive:_ Distinct tool-name usage is used for platform health/dormancy detection, not exposed as tenant content.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — Tool-drift query aggregates FROM tool_performance with no tenant filter (GROUP BY tool_name ...) — reads/factors across all tenants.
  - _Why false positive:_ Tool-drift detector is a global housekeeping query producing warning summaries, not tenant-facing mixed content.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (SELECT id, tenant_id, hypothesis, ...) — reads experiments across all tenants.
  - _Why false positive:_ Heartbeat worker intentionally sweeps running experiments across tenants and updates each row using its own tenant_id.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} without constraining tenant_id.
  - _Why false positive:_ evalId is obtained from the immediately preceding INSERT RETURNING id in the same server flow, not from an untrusted request boundary.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from `ai_insights` in retryPendingMinervaRouting omits tenant_id filter — reads pending insights across all tenants.
  - _Why false positive:_ platform-wide durability sweep intentionally scans across tenants and processes each row with its own row-carried tenant_id.
- **server/agentic-engines.ts:347** _(global-table)_ — In gatherOperationalData a SELECT against `heartbeat_logs` (COUNT) lacks tenant_id filter and therefore aggregates logs across tenants.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column NO, so counting it globally matches an intentionally platform-global table.
- **server/agentic-features.ts:524** _(upstream-verified)_ — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} is executed without a tenant_id constraint.
  - _Why false positive:_ the updated id comes from leadRows selected earlier in the same function from lead_enrichments scoped to the tenant, so ownership is established upstream.
- **server/agentic-features.ts:638** _(upstream-verified)_ — UPDATE outreach_enrollments SET status='completed' WHERE id = ${enrollment.id} is executed without tenant_id constraint.
  - _Why false positive:_ enrollment.id comes from rows selected just above with e.tenant_id = params.tenantId, so the row was already tenant-scoped in the same call path.
- **server/agentic-features.ts:672** _(upstream-verified)_ — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} lacks tenant scoping.
  - _Why false positive:_ enrollment.id is from the same tenant-filtered advanceSequence rowset, so ownership was verified earlier in the same function.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE outreach_enrollments SET reply_classification = ..., status = ... WHERE id = ${row.id} is missing tenant_id constraint.
  - _Why false positive:_ row.id comes from the immediately preceding SELECT with e.tenant_id = params.tenantId, establishing tenant ownership before the UPDATE.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' performs a tenant-scoped table read with no tenant_id constraint, allowing cross-tenant enumeration/leakage.
  - _Why false positive:_ Heartbeat maintenance sweep processes each row with its own tenant_id and only emits per-tenant events/counts; no tenant-facing mixed output.
- **server/agentic/task-forces.ts:40** _(upstream-verified)_ — String-interpolated SQL with request-derived value: personaIdArrayLiteral builds an array literal string from caller-supplied personaIds and that string is injected into the SQL (sql`${literal}::int[]`). This is injection-prone and mixes raw SQL generation with user input, even if filtered to integers.
  - _Why false positive:_ Not a tenant-isolation finding; insert explicitly sets tenant_id from caller-scoped param and no cross-tenant read/write bypass is shown.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — backfillProjectAssets scans conversations across all tenants: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations has no tenant_id filter, so it reads tenant-scoped rows for every tenant in one query.
  - _Why false positive:_ Backfill is a platform-wide housekeeping loop over conversations, carrying each row's tenant_id forward per row rather than mixing tenants in one response.
- **server/auto-memorize.ts:163** _(system-sweep)_ — runAutoMemorize scans messages across tenants in a single unscoped SELECT: SELECT m.tenant_id, m.role, m.content, ... FROM messages ... has no tenant_id WHERE clause. Although the code later partitions by tenant, the raw read touches tenant-scoped rows for all tenants in one shot without an explicit per-tenant constraint.
  - _Why false positive:_ Code explicitly partitions message rows by tenant before any prompt creation, avoiding mixed-tenant prompts in this maintenance pass.
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT from messages uses conversation_id but does not include a tenant_id constraint. Reads against tenant-scoped tables must include an explicit tenant filter.
  - _Why false positive:_ Conversation ownership is proven a few lines earlier with WHERE id = conversationId AND tenant_id = tenantId before this messages read on that conversation.
- **server/auto-project.ts:145** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. If project_notes is tenant-scoped (no default tenant_id), this writes a row without assigning the tenant, violating tenant isolation.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects via project_id; insert from new_project is isolated transitively.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from messages uses conversation_id but omits tenant_id. Even though conversation ownership was checked earlier, every read against a tenant-scoped table must include an explicit tenant filter per policy.
  - _Why false positive:_ Same call path first asserts conversation in tenant and verifies owning project tenant before reading messages for that conversation.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without scoping messages by tenant. This reads a tenant-scoped table without a tenant_id constraint.
  - _Why false positive:_ Backfill is housekeeping across tenants and each conversation is later handled with its own tenant_id via autoSaveProjectTranscript(c.id, c.tenant_id).
- **server/auto-transcript.ts:153** _(system-sweep)_ — the alsoLinked query contains (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 — the subquery reads messages without a tenant filter, allowing cross-tenant reads if messages are tenant-scoped.
  - _Why false positive:_ AlsoLinked is a maintenance reconciliation loop; rows carry c.tenant_id and are processed per-row without mixed tenant output.
- **server/business-tools.ts:96** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table this allows creation of rows without an explicit tenant_id and can break tenant isolation (orphan/foreign rows tied only by invoice_id).
  - _Why false positive:_ invoice_items has no tenant_id and is a line-item child of tenant-scoped invoices; parent invoice is created in-transaction with tenant_id and customer ownership checked.
- **server/chat-engine.ts:572** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (query: SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...).
  - _Why false positive:_ project_files has no tenant_id and is scoped through previously verified parent project id+tenant_id
- **server/chat-engine.ts:583** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (query: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...).
  - _Why false positive:_ project_notes has no tenant_id and is scoped through previously verified parent project id+tenant_id
- **server/chat-engine.ts:831** _(fk-transitive)_ — Scalar subquery counts rows in tenant-scoped `project_files` (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) without an explicit tenant_id constraint in the subquery.
  - _Why false positive:_ project_files has no tenant_id and count is over child rows of tenant-filtered parent projects
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE commits to revert a claimed row executes without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This can unclaim or modify rows across tenants if row.id is re-used or attacker manipulates input.
  - _Why false positive:_ row.id comes from same-path claim query returning c.id with c.tenant_id, and this branch only reverts that already selected row
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on credential_vault updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). This can update another tenant's credential record if id overlaps.
  - _Why false positive:_ getCredential(id, tenantId) is called immediately above and must return only tenant-owned credential before last_used_at update
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on credential_vault updates last_used_at without tenant scoping (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}). Missing tenant filter allows cross-tenant modification.
  - _Why false positive:_ cred is obtained immediately above via getCredentialForSite(siteUrl, tenantId), so ownership is verified in same call path before update
- **server/data-protection.ts:117** _(system-sweep)_ — SELECT across conversations scans deleted_at rows for all tenants (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) with no tenant_id predicate. This reads tenant-scoped data across tenants.
  - _Why false positive:_ global purge job intentionally scans expired rows across tenants and processes each row with its own tenant_id for housekeeping
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE FROM doc_chunks is executed without tenant_id constraint (WHERE doc_path = ... AND collection_id = ... but no tenant_id), which can delete another tenant's chunks if collection_id/doc_path collide.
  - _Why false positive:_ same call path verifies doc_collections ownership with collectionId and tenantId immediately before delete
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding ... WHERE id = ${row.id} is executed without tenant scoping. An UPDATE by id without tenant filter can modify another tenant's row if misrouted or if ids collide across tenants.
  - _Why false positive:_ row.id values come from prior SELECT scoped by collection_id and tenant_id in same function
- **server/email.ts:490** _(system-sweep)_ — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} performs an UPDATE on a tenant-scoped table without tenant_id in the WHERE clause, allowing cross-tenant modification if a row id is misattributed.
  - _Why false positive:_ boot/backfill maintenance loop updates housekeeping body fields per row and does not expose mixed tenant data
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT id, embedding FROM memory_entries ... LIMIT 200 executed without filtering by tenant_id in backfillEmbeddingVecs — reads can return rows across tenants.
  - _Why false positive:_ global backfill sweep reads rows across tenants only to populate embedding_vec on each same row by id
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT id, embedding FROM agent_knowledge ... LIMIT 200 executed without filtering by tenant_id in backfillEmbeddingVecs — reads can return rows across tenants.
  - _Why false positive:_ global backfill sweep reads rows across tenants only to populate embedding_vec on each same row by id
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... in backfillMissingKnowledgeEmbeddings has no tenant_id filter — this reads data across tenants.
  - _Why false positive:_ maintenance backfill processes expired-safe autoresearch rows individually to add embeddings and returns only aggregate counts/logs
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal built from an embedding (sql.raw(`'${vec}'::vector`)) in vectorSearchMemory. The vec value is derived from model/provider output (request-derived) — interpolation risks injection and breaks the ‘application-level filters only’ assurance model.
  - _Why false positive:_ query is tenant-scoped with tenant_id = tenantId; finding is SQLi-related not cross-tenant isolation
- **server/embeddings.ts:252** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal (ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) in vectorSearchMemory — vec is request/provider-derived and being inlined into SQL.
  - _Why false positive:_ query remains tenant-scoped with tenant_id = tenantId; cited issue is raw SQL handling not tenant isolation
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw() with interpolated vector literal ('${vec}'::vector) in vectorSearchKnowledge — embedding is request-derived and being inlined into raw SQL, creating injection/isolation risk.
  - _Why false positive:_ query enforces tenant_id = tenantId; finding concerns raw interpolation, not cross-tenant access
- **server/embeddings.ts:565** _(upstream-verified)_ — Use of sql.raw() with interpolated vector literal in ORDER BY (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) in vectorSearchKnowledge — request-derived data is being inlined into SQL via sql.raw.
  - _Why false positive:_ query enforces tenant_id = tenantId; ORDER BY raw vector does not remove tenant scoping
- **server/firecrawl.ts:214** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' uses only id in WHERE clause (no tenant_id constraint). After finding an existing row filtered by tenant, the code updates by id alone: db.update(scrapedPages).set(...).where(eq(scrapedPages.id, existing[0].id))
  - _Why false positive:_ Row id comes from an immediately prior SELECT on scraped_pages scoped by tenantId, so ownership is verified in the same call path.
- **server/firecrawl.ts:344** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' in crawl loop uses only id in WHERE clause (no tenant_id constraint). The code updates existing row by id: db.update(scrapedPages).set(...).where(eq(scrapedPages.id, existing[0].id))
  - _Why false positive:_ Update uses id obtained from the preceding tenant-scoped SELECT on scraped_pages within the same loop iteration.
- **server/glasses-gateway.ts:156** _(upstream-verified)_ — SELECT from apiKeys does not include tenant scoping. The code fetches an API key row by hash without constraining by tenant (db.select().from(apiKeys).where(...)). If apiKeys is a tenant-scoped table this is a cross-tenant read risk.
  - _Why false positive:_ API key auth intentionally resolves tenant by globally unique keyHash, then derives req.tenantId from the matched row; tenant scoping cannot precede authentication here.
- **server/glasses-gateway.ts:172** _(upstream-verified)_ — UPDATE on tenant-scoped table 'api_keys' (lastUsedAt) uses only id in WHERE clause with no tenant_id constraint: db.update(apiKeys).set(...).where(eq(apiKeys.id, key.id))
  - _Why false positive:_ The row was just fetched by keyHash and key.id is taken from that verified row, so lastUsedAt update targets the authenticated key.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' modifies driveFolderId/driveFolderUrl using only project id in WHERE clause (no tenant_id constraint). Even though ownership was checked earlier, the UPDATE itself is not tenant-scoped: db.update(projects).set(...).where(eq(projects.id, projectId))
  - _Why false positive:_ The function explicitly loads the project, checks project.tenantId === tenantId, and only then updates that same projectId in the same flow.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals filters by status and age but does not constrain by tenant_id. code_proposals appears tenant-scoped, so this can return rows for all tenants.
  - _Why false positive:_ This is a global TTL/health audit sweep that inspects stale rows across tenants and only archives status fields; no tenant-facing data mixing is shown.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks for stuck tasks does not include tenant scoping. heartbeat_tasks are used elsewhere as tenant-scoped (many APIs accept tenantId for heartbeat tasks), so this may surface tasks across tenants.
  - _Why false positive:_ This is a platform housekeeping scan for stuck heartbeat tasks that globally disables stale tasks by id only; it is maintenance, not tenant-facing data access.
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT from research_schedules without any tenant_id constraint (SELECT * FROM research_schedules ...). This reads schedule rows across all tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due research_schedules across tenants and processes each row using its own tenant_id.
- **server/heartbeat.ts:658** _(system-sweep)_ — SELECT from event_log WHERE status = 'pending' ... (no tenant_id filter). This pulls pending events across all tenants into a single query.
  - _Why false positive:_ Pending events are swept platform-wide, and each event is processed with its row-carried tenant_id without combining tenant data into one output.
- **server/knowledge-diversity-monitor.ts:266** _(system-sweep)_ — Query in runKnowledgeDiversityMonitor SELECT tenant_id, persona_id, COUNT(*) FROM `agent_knowledge` groups by tenant_id and persona_id without restricting to a tenant — enumerates tenant/persona pairs across the entire DB.
  - _Why false positive:_ heartbeat-style monitor intentionally scans all tenant/persona pairs and processes each row with its own tenant_id
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT against tenant-scoped table `projects` is missing a tenant_id WHERE filter (returns counts across all tenants).
  - _Why false positive:_ boot/backfill import intentionally aggregates per-tenant counts across snapshot tenants for housekeeping, not tenant-facing data return
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT from `project_files` uses only project_id/file_name and lacks an explicit tenant_id filter. This is a tenant-isolation risk if `project_files` is tenant-scoped and tenant_id is required by policy.
  - _Why false positive:_ project_files has no tenant_id and is isolated transitively by tenant-scoped parent project_id
- **server/lib/ideabrowser-backfill.ts:115** _(fk-transitive)_ — SELECT from `project_notes` uses only project_id and note to check existence and lacks an explicit tenant_id filter. This bypasses the required application-level tenant constraint.
  - _Why false positive:_ project_notes has no tenant_id and is isolated transitively by tenant-scoped parent project_id
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE locks rows across all tenants). This UPDATE/RETURNING updates rows across all tenants in a single statement (runner is system-scoped but the query touches tenant-scoped data with no tenant filter).
  - _Why false positive:_ system-wide scheduler intentionally claims due rows across tenants and carries tenant_id per row for isolated processing
- **server/lib/scheduled-post-runner.ts:204** _(upstream-verified)_ — UPDATE on tenant-scoped table `scheduled_posts` uses `WHERE id = ${row.id}` with NO tenant_id constraint. This can update a row belonging to a different tenant if ids collide or if the row context was tampered with in memory.
  - _Why false positive:_ row.id comes from the immediately preceding locked UPDATE RETURNING of the same scheduled_posts row, so tenant ownership is established earlier in the same call path
- **server/lib/scheduled-post-runner.ts:231** _(upstream-verified)_ — Error-path UPDATE on `scheduled_posts` uses `WHERE id = ${row.id} AND status = 'publishing'` but omits tenant_id. In exception handling this updates tenant-scoped state with no tenant constraint.
  - _Why false positive:_ error-path update targets the same row previously claimed and materialized from the runner's locked RETURNING result in this call path
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table `tenant_voice_profiles` is performed with .where(eq(tenantVoiceProfiles.id, existing.id)) but does not include an explicit tenant_id constraint. Every UPDATE against a tenant-scoped table must also constrain by tenant_id to enforce isolation.
  - _Why false positive:_ existing row was fetched just above via getVoiceProfile with tenantId and profileName, so the updated id is already tenant-verified in this flow
- **server/memory-graph.ts:81** _(upstream-verified)_ — UPDATE memory_categories executed without constraining by tenant_id (WHERE id = ${catId} only). This can modify a category belonging to a different tenant.
  - _Why false positive:_ catId was obtained in the same flow from tenant-scoped SELECT/INSERT using tenantId, so the subsequent increment targets that tenant-verified row
- **server/memory-graph.ts:196** _(fk-transitive)_ — SELECT 1 FROM memory_links ... LIMIT 1 is performed without any tenant_id constraint. memory_links appears to be a tenant-scoped table, so this read can return rows from other tenants.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries already filtered to the same tenant.
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is executed without tenant filtering. This can update a memory entry belonging to another tenant if ids collide.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries constrained by tenant_id = tenantId in the same function.
- **server/memory-intelligence.ts:273** _(internal-worker-pk)_ — After creating a memory entry the code runs UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} without tenant scoping. The update could affect a row with the same id in another tenant.
  - _Why false positive:_ entry.id is the server-created PK returned by createMemoryEntry for this tenant-scoped insert, not request-supplied.
- **server/memory-intelligence.ts:316** _(internal-worker-pk)_ — During the 'update' action the code executes UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} with no tenant filter, which may modify another tenant's row.
  - _Why false positive:_ entry.id is the server-created PK of the newly inserted replacement row, not taken from an untrusted boundary.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom} ... WHERE id = ${c.id} is executed without tenant scoping even though the contradictions selection targeted tenant_id earlier. Missing tenant filter on the UPDATE allows cross-tenant modification if ids collide.
  - _Why false positive:_ c.id values come from an immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId.
- **server/minds-engine.ts:327** _(upstream-verified)_ — markEventsHandled uses id = ANY(${`{${eventIds.join(",")}}`}::int[]) where the array literal is constructed via string interpolation from request-derived eventIds. This is a SQL construction with request-controlled content (injection + isolation risk).
  - _Why false positive:_ the UPDATE includes tenant_id = tenantId; finding's SQL-construction concern is not a cross-tenant isolation issue.
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code calls assertProjectInTenant(...) to verify ownership but the INSERT omits tenant_id in the VALUES list, violating the invariant that tenant-scoped tables must have tenant_id explicitly set on INSERT.
  - _Why false positive:_ project_files has no tenant_id by schema and is a child table isolated through tenant-verified project_id via assertProjectInTenant.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. Even though assertProjectInTenant(...) is called to confirm ownership, the INSERT omits tenant_id in the VALUES list, violating the requirement that every INSERT must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by assertProjectInTenant on tenant-scoped parent project
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — Global read from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id: `SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`
  - _Why false positive:_ startup validation intentionally scans active subscriptions across tenants for maintenance and processes each row with its own tenant_id
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — Unconstrained read from tenant-scoped table `oauth_subscriptions`: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'` (no tenant_id filter)
  - _Why false positive:_ boot-time auto-reconnect sweep enumerates tenant_ids platform-wide and handles each tenant separately
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — Background proactive refresh reads `oauth_subscriptions` without tenant scoping: `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`
  - _Why false positive:_ proactive refresh is a background maintenance loop over all active subscriptions using row-carried tenant_id per subscription
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — Force-refresh code queries `oauth_subscriptions` for all active rows without a tenant filter: `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE`
  - _Why false positive:_ force-refresh-all is an explicit cron-style maintenance sweep over active subscriptions with per-row tenant handling
- **server/persona-export.ts:33** _(global-table)_ — Reading from tenant-scoped table `personas` without tenant restriction: `SELECT * FROM personas WHERE id = ${personaId}` — no tenant_id filter, so caller can export personas from other tenants.
  - _Why false positive:_ schema note says personas has no tenant_id, so this base persona row is not tenant-scoped by design
- **server/process-governor.ts:121** _(fk-transitive)_ — Subqueries against heartbeat_logs do not include tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This allows reading other tenants' heartbeat/log data since there is no row-level security.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is scoped transitively by task_id to tenant-scoped heartbeat_tasks filtered by ht.tenant_id
- **server/process-governor.ts:198** _(global-table)_ — SELECT AVG(duration_ms) FROM heartbeat_logs ... has no tenant_id filter — this aggregates logs across tenants.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id, so direct tenant filter requirement does not apply
- **server/process-governor.ts:240** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... does not filter by tenant_id, allowing cross-tenant aggregation of recent failures.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id, so this aggregate is over a non-tenant-scoped table
- **server/project-brain.ts:332** _(system-sweep)_ — SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 performs discovery across projects/conversations/messages with no tenant filter, potentially exposing other tenants' data.
  - _Why false positive:_ Boot/backfill maintenance loop intentionally scans all tenants and processes each project/conversation separately to build per-project files, not tenant-facing mixed output.
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} does not include tenant_id. conversations are tenant-scoped and fetching them without tenant constraint can traverse other tenants' conversations.
  - _Why false positive:_ Conversation read is inside the same global backfill maintenance loop and scoped per project row selected by the sweep.
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} queries messages without tenant_id. messages are tenant-scoped and must be filtered by tenant_id to prevent cross-tenant reads.
  - _Why false positive:_ Message read is part of the same housekeeping backfill, handled per conversation and used only to build that project's brain artifact.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id without tenant scoping. code_proposals are tenant-scoped and must be constrained to the caller's tenant.
  - _Why false positive:_ Server-invoked verifier reads by globally unique proposal id parameter, with no request-boundary evidence here.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint: `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` — this reads rows for all tenants.
  - _Why false positive:_ Scheduled runner intentionally scans due jobs across tenants, carrying each row's tenant_id forward for per-row scoped conversation operations.
- **server/routes.ts:166** _(system-sweep)_ — SELECT from tenant-scoped table `file_storage` without tenant_id constraint. The startup restoreUploadsFromDb() reads all file_storage rows (all tenants) and restores files to disk, violating tenant isolation enforced only by application-level filters.
  - _Why false positive:_ Startup restore is a platform-wide maintenance task restoring image blobs to disk and not returning cross-tenant row data to tenant-facing surfaces.
- **server/routes.ts:1117** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` without tenant_id constraint in uploads middleware. The code fetches all owners for a basename (db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))) and then checks membership in application logic, which performs an unscoped read across tenants.
  - _Why false positive:_ Upload delivery path is authorized by signed URL or authenticated tenant ownership check; tenant id in file_storage lookup is used to verify owner membership, not expose content.
- **server/routes/admin.ts:693** _(system-sweep)_ — SELECT from `tool_performance` executed with no tenant_id constraint — this table appears to be tenant-scoped elsewhere (tenant-specific heartbeats/tools are used in other queries). Querying tenant-scoped data without constraining by tenant_id can leak cross-tenant data.
  - _Why false positive:_ Endpoint is gated to platform admin tenant and intentionally aggregates recent failing tools across tenants for admin diagnostics.
- **server/routes/admin.ts:706** _(system-sweep)_ — SELECT from `ai_insights` (stuckInsights) executed with no tenant_id constraint. `ai_insights` is used elsewhere in tenant-scoped contexts and must not be read across tenants without an explicit tenant filter or ownership check.
  - _Why false positive:_ Admin-only diagnostic endpoint intentionally lists stuck insights across tenants for platform maintenance, not tenant-facing access.
- **server/routes/admin.ts:716** _(system-sweep)_ — SELECT from `heartbeat_tasks` (disabledTasks) executed with no tenant_id constraint. Other code in the repo queries heartbeat_tasks with tenant filtering — this inconsistent usage can leak tenant data.
  - _Why false positive:_ Admin-only diagnostic query intentionally scans heartbeat tasks across tenants for platform-wide housekeeping visibility.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — SELECT from tenant-scoped table 'project_conversations' missing tenant filter: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`
  - _Why false positive:_ Project ownership is verified immediately above via projects.id + tenant_id, and conv.id is the newly created conversation for this tenant; project_conversations is non-tenant child linkage.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` omits tenant predicate. The code executes `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` after fetching a message — the UPDATE does not include `tenant_id = ${tenantId}` and can modify another tenant's row if an ID is guessed or the prior fetch is bypassed.
  - _Why false positive:_ The same handler first selects the inbox_messages row by id and tenant_id, then updates that fetched row by its primary key.
- **server/routes/projects.ts:33** _(fk-transitive)_ — Tenant-scoped dependent subqueries in the projects list lack explicit tenant constraint. The SELECT uses subselects (project_files, project_notes, project_conversations) without filtering those tables by tenant_id.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are tenant-isolated through tenant-scoped parent projects filtered by p.tenant_id.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects sets updated_at WHERE id = ${projectId} but lacks tenant constraint. This UPDATE can modify a project owned by another tenant if projectId is manipulated.
  - _Why false positive:_ This upload path already verified the project exists with id + tenant_id before inserting child files and touching updated_at on that same project id.
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects sets updated_at WHERE id = ${projectId} but lacks tenant constraint (multipart upload path).
  - _Why false positive:_ This multipart upload path verifies the project with id + tenant_id just above, so the updated_at touch is on the already-owned project.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} has no tenant filter. Although there was a prior projCheck, every read against a tenant-scoped table must include an explicit tenant constraint.
  - _Why false positive:_ project_files has no tenant_id and is read only after verifying the parent project belongs to the tenant.
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} has no tenant constraint. This allows lookup of files by path across tenants when project_id/file_path are manipulated.
  - _Why false positive:_ project_files has no tenant_id; file lookup is scoped by verified tenant-owned parent project_id plus file_path.
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} omits tenant constraint. Deleting without tenant scoping risks removing another tenant's file if IDs are manipulated.
  - _Why false positive:_ project_files has no tenant_id and deletion is constrained to a project_id whose ownership was verified via projects.tenant_id immediately above.
- **server/seed.ts:35** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id constraint — this reads aggregate data across all tenants for a tenant-scoped table.
  - _Why false positive:_ Boot-time dev snapshot import is a platform-wide seeding/reconciliation path; the count is aggregate housekeeping, not tenant-facing data exposure.
- **server/seed.ts:3092** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true is querying a tenant-scoped table with no tenant_id filter — it may read other tenants' rows.
  - _Why false positive:_ Seed maintenance iterates active research_programs across tenants only to normalize model values; it is a platform-wide housekeeping loop, not tenant-facing output.
- **server/seed.ts:4270** _(system-sweep)_ — Selecting all rows from tenant-scoped table `heartbeat_tasks` via db.select().from(heartbeatTasks) with no tenant filter. This loads heartbeats across tenants into memory and drives subsequent logic (insertion checks), causing cross-tenant effects.
  - _Why false positive:_ Boot-time seed/housekeeping scan intentionally checks global heartbeat seed state; no tenant-facing data returned or mixed output.
- **server/seed.ts:4925** _(upstream-verified)_ — UPDATE agent_knowledge SET content = ..., priority = 10, updated_at = NOW() WHERE id = ${rows[0].id} — writing to tenant-scoped `agent_knowledge` by id only, with no tenant_id constraint. While id was obtained from a tenant-filtered SELECT, the UPDATE query itself lacks an explicit tenant guard.
  - _Why false positive:_ Updated row id comes directly from prior SELECT constrained to tenant_id = 1 in the same call path.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from `messages` (recentMsgs) is executed with only messages.conversationId = conversationId and NO tenant constraint. This allows reading messages across tenants if conversationId is guessable.
  - _Why false positive:_ Same call path first calls analyzeConversation, which asserts the conversation belongs to tenantId before this message read by conversationId.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from `messages` inside analyzeConversation is using only messages.conversationId = conversationId with NO tenant constraint. Messages are tenant-scoped and this reads them without scoping to caller tenant.
  - _Why false positive:_ Immediately above, conversation is fetched with id + tenantId and execution aborts if not found; message read is scoped by verified parent conversation.
- **server/skillify.ts:221** _(global-table)_ — SELECT from `personas` (personaRows) queries personas by id but does NOT verify tenant ownership (no personas.tenantId = tenantId filter). Reading persona records referenced by a conversation without validating tenant ownership violates the tenant-isolation invariant.
  - _Why false positive:_ personas schema note shows no tenant_id column, so persona lookup is against a non-tenant-scoped table.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from `messages` (recentMsgs in skillifyConversation) uses only messages.conversationId = conversationId and lacks any tenant constraint. This reads conversation messages without scoping to the caller's tenant.
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), which verifies tenant ownership of the conversation in the same call path.
- **server/storage.ts:295** _(upstream-verified)_ — Read from tenant-scoped table `messages` via raw SQL lacks tenant_id constraint (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL). This can return citations across tenants.
  - _Why false positive:_ attachCitationMetadata only re-reads ids from msgs already fetched by tenant-scoped callers in the shown path; no new untrusted ids enter here.
- **server/storage.ts:838** _(system-sweep)_ — Global UPDATE on tenant-scoped `memory_entries` (archiveExpiredMemories uses UPDATE memory_entries SET status='archived' WHERE status='active' AND expires_at <= NOW()) has no tenant scoping and will archive rows across all tenants.
  - _Why false positive:_ platform-wide housekeeping update only archives expired rows and returns ids/count, not tenant data
- **server/storage.ts:855** _(system-sweep)_ — Global UPDATE on tenant-scoped `memory_entries` (archiveStaleMemories) runs without tenant constraint and can affect all tenants' rows.
  - _Why false positive:_ global stale-archive maintenance only mutates status and returns ids/count, with no tenant-facing data mixing
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — UPDATE actual_outcome_embedding on `felix_proposals` uses WHERE id = ${proposalId} with no tenant constraint, allowing cross-tenant writes.
  - _Why false positive:_ same call path first selects proposal by id and tenant_id, establishing ownership before this write
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — UPDATE that writes surprise_score and surprise_band to `felix_proposals` uses WHERE id = ${proposalId} with no tenant_id constraint, permitting cross-tenant mutation.
  - _Why false positive:_ proposal row was already fetched with matching tenant_id earlier in the same function before scoring update
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (checks existing tool name across all tenants).
  - _Why false positive:_ name is transformed with tenantToolName(parsed.name, tenantId), making lookup tenant-derived/per-tenant before select
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` by id without constraining tenant_id (reads the inserted row without verifying tenant ownership).
  - _Why false positive:_ row was just inserted in same call with tenantId and returned as inserted; reread by that server-generated pk is same-row follow-up
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` (.update(...).where(eq(custom_tools.id, tool.id))) performed without tenant_id constraint — can update a row belonging to another tenant.
  - _Why false positive:_ update operates on the just-inserted tool object passed through same call path, not a cross-tenant request lookup
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` to increment usageCount is constrained only by id (.where(eq(custom_tools.id, tool.id))) and lacks tenant_id filtering.
  - _Why false positive:_ tool row is selected immediately above with tenant_id = tenantId, and update uses that verified row.id
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table `browser_workflows` without tenant_id constraint (no WHERE tenant_id = ...). This UPDATE uses only id in the WHERE clause, allowing a cross-tenant update if ids collide or context is spoofed.
  - _Why false positive:_ workflow is fetched just above with id/name plus tenant_id, and update targets that verified workflow.id
- **server/tools/domains/files/handlers.ts:838** _(fk-transitive)_ — UPDATE on `projects` table omits tenant_id in the WHERE clause (UPDATE projects SET updated_at = ... WHERE id = ${projId}). This is an UPDATE against a tenant-scoped table without an explicit tenant constraint.
  - _Why false positive:_ project_files has no tenant_id and is inserted via tenant-scoped parent project joined through conversation/project ownership
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` does not constrain by tenant_id. The statement `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` can modify a row by id across tenants if an id is guessed/leaked.
  - _Why false positive:_ rows being updated come from contradictionRows selected just above with tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` is executed without any tenant_id constraint (reads provider keys across all tenants).
  - _Why false positive:_ provider_keys schema shows no tenant_id and code exposes only admin-tenant aggregate metadata, not tenant-scoped rows
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — COUNT/DISTINCT query on `provider_keys` runs without tenant constraint (enumerates provider configurations across tenants).
  - _Why false positive:_ provider_keys has no tenant_id and this is an admin-only aggregate count over a global table
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with NO tenant_id constraint. Every write to a tenant-scoped table must also constrain by tenant_id to prevent cross-tenant modification.
  - _Why false positive:_ id comes from prior SELECT on memory_entries constrained by tenant_id, persona_id, and category in same path
- **server/tools/domains/strategic-memory/handlers.ts:147** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses `WHERE id = ANY(${ids})` with NO tenant_id constraint. This updates rows by id only and can cross tenant boundaries if an id is reused or exposed.
  - _Why false positive:_ ids array is derived from rows selected immediately above with tenant_id, persona_id, and category filters
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} in a one-line UPDATE without a tenant_id constraint. The write is not explicitly scoped to ctx.tenantId.
  - _Why false positive:_ id is loaded immediately above from memory_entries with WHERE tenant_id = ctx.tenantId; update uses that previously tenant-scoped row id in same call path.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT from tenant-scoped table `conversations` executed without constraining by tenant_id. The code reads conversation rows by id but does not include tenant filtering to verify ownership, risking cross-tenant data access or mis-verification.
  - _Why false positive:_ this query is itself the ownership verification step and only returns tenant_id for the specific conversation id to compare against claimed tenantId, with no tenant data returned onward absent a match.
- **server/video-job-runner.ts:228** _(system-sweep)_ — UPDATE on tenant-scoped table `video_jobs` in recoverStaleVideoJobs() runs without any tenant_id filter — it will mark stale jobs across all tenants. This violates the invariant and can cause jobs from unrelated tenants to be failed/modified by this sweep.
  - _Why false positive:_ recoverStaleVideoJobs is a platform-wide housekeeping sweep marking stale jobs failed by heartbeat age; it only mutates status fields and logs recovered ids, not tenant-facing mixed content.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Memory-growth probe uses sql.raw to SELECT counts and pg_total_relation_size FROM various memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without tenant filters — potentially aggregates tenant data across all tenants.
  - _Why false positive:_ memory-growth probe is observability-only platform-wide counting/size telemetry; it emits aggregate counts and sizes, not tenant row data or mixed tenant content.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory is performed without a tenant filter; the code even treats this as "across all tenants" which bypasses per-tenant isolation.
  - _Why false positive:_ global empty-table check is a maintenance/observability probe that returns only an aggregate count and warning, not tenant data.
- **server/heartbeat.ts:3222** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — update by id only, without tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance_rules without scoping by tenant_id, allowing rule counts/metadata for other tenants to be modified.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/sculptor.ts:347** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} executes without constraining tenant_id. This allows writing review data into sessions belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — runExperiment issues UPDATE statements against the tenant-scoped `experiments` table using .where(eq(experiments.id, experiment.id)) with NO tenant_id constraint. This allows updating any experiment row by id across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:339 the experiment row is created via db.insert(experiments) with a server-generated serial PK earlier in the same runExperiment() call; the UPDATE at 394 targets that same experiment.id. All callers (runSelfImprovementCycle:272) create the row internally; no request/tool-arg id.
- **server/delivery-pipeline.ts:195** _(upstream-verified)_ — updateDeliveryLog performs UPDATE on tenant-scoped table delivery_logs without constraining by tenant_id (WHERE only checks id).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:188 — updateDeliveryLog is a private helper only ever called with the deliveryId returned by createDeliveryLog within the same deliverDigitalProduct flow (callsites 653,729,760,818,841,848,852,901,965). The id is a freshly-minted PK owned by the current tenant, never derived from request/tool/LLM input, so no caller can reference another tenant's delivery row.
- **server/minerva-planner.ts:373** _(upstream-verified)_ — Probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} is executed without tenant scoping, allowing information disclosure across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Probe SELECT is in decidePlan's CAS-miss branch, reached only after agentic-policy.ts:113 getPlan(planId, tenantId) proved ownership (404 otherwise). No cross-tenant read possible for a caller who does not own the plan.
- **server/storage.ts:824** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` (getMemoriesWithoutEmbeddings) is unscoped: SELECT ... FROM memory_entries WHERE status='active' AND embedding IS NULL LIMIT ... with no tenant filter, returning data across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getMemoriesWithoutEmbeddings only caller is the background embedding backfill heartbeat.ts:1912; rows feed embedding generation (updateMemoryEmbedding) and are never returned to a tenant-facing surface.
- **server/storage.ts:830** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` (getKnowledgeWithoutEmbeddings) lacks tenant scoping: .where(and(notExpired, isNull(agentKnowledge.embedding))). No tenant filter present.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getKnowledgeWithoutEmbeddings only caller is heartbeat.ts:1913 background embedding worker; rows only drive updateKnowledgeEmbedding, not returned to any tenant surface.
