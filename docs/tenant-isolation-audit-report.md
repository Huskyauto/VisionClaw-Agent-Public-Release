# Tenant-Isolation Audit Report

_Generated 2026-09-11T20:33:43.819Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 495 file(s) were NOT audited this run (1/62 chunk(s) failed to run or parse). Findings below are PARTIAL — do not read "few findings" as "all clear."
> - chunk 20: model-call-failed: tenant audit chunk 20/62 timed out after 120000ms

- Coverage: **175/670** file(s) audited · chunks 19/62 OK (1 failed, 42 pending)
- Findings: **64** (CRITICAL 5 · HIGH 39 · MEDIUM 19 · LOW 1)
- Severe dispositions: **44 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (5)

- **server/ab-optimizer.ts:110** — runDueAbExperiments performs a SELECT on `experiments` without constraining by tenant_id (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads experiments across all tenants.
  - _Fix:_ Scope the query to a specific tenant or iterate tenants explicitly. For example, add WHERE tenant_id = ${tenantId} (or run the function per-tenant). If the intent is a global sweep, make that explicit and add additional safeguards (audit, admin-only) — but do not leave unscoped tenant reads in production code.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' is executed without constraining by tenant_id (WHERE id = ${row.id} only). This can unclaim/modify a commitment row belonging to a different tenant.
  - _Fix:_ Require tenant scoping on this UPDATE. Include AND tenant_id = ${tenantId} (or the appropriate tenant variable) in the WHERE clause. Example: await db.execute(sql`UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}`);
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` is executed without a tenant_id filter. The rehydrate query pulls all in-flight `agent_runs` across tenants and then uses tenant_id from the rows to populate in-memory state — this violates the project's tenant-isolation invariant (reads against tenant-scoped tables must be constrained to the caller's tenant).
  - _Fix:_ Limit the query to an explicit tenant scope or otherwise restrict this bootstrap to an admin-only, audited path. Options: (a) require an explicit list/argument of tenantId(s) and add WHERE tenant_id = ${tenantId} to the SELECT; (b) run rehydration per-tenant in a controlled loop that passes each tenantId in and uses WHERE tenant_id = ${tenantId}; or (c) move this cross-tenant rehydration into a privileged, documented maintenance tool that is separate from normal app code and audited. Do NOT perform an unbounded SELECT of tenant-scoped rows at boot in normal multi-tenant runtime.
- **server/health-audit.ts:333** — UPDATE on `code_proposals` (tenant-scoped) uses WHERE id = ANY(...) only and lacks a tenant_id constraint — this can modify rows across other tenants.
  - _Fix:_ Constrain updates to the owning tenant (e.g. add AND tenant_id = ${tenantId}) or resolve and validate tenant ownership before updating. If updating many tenants intentionally, require an explicit, auditable admin action and validate each id's tenant before writing.
- **server/health-audit.ts:366** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) — mutates tenant-scoped rows without constraining by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ANY(...) AND tenant_id = ${tenantId}) or validate each id belongs to the intended tenant before disabling. Require explicit admin authorization if disabling tasks across tenants is intended.

### HIGH (39)

- **server/agent-activity.ts:122** — completeActivity issues an UPDATE on `agent_activity` without scoping by tenant_id (UPDATE agentActivity ... WHERE id = activityId). This allows modifying another tenant's row by id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}) or otherwise validate that the activity row belongs to the caller's tenant before updating.
- **server/agent-eval.ts:218** — runEval updates `agent_evals` by id only (UPDATE agent_evals SET ... WHERE id = ${evalId}) without verifying tenant ownership. A malicious or buggy evalId could cause cross-tenant writes/overwrites.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}), or fetch/validate the row's tenant before performing the update.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs rows from `ai_insights` with no tenant filter (SELECT id, tenant_id, category, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER LIMIT 50). This reads insights across tenants.
  - _Fix:_ Scope the SELECT by tenant (WHERE tenant_id = ${tenantId}) or otherwise iterate explicit tenant contexts. If intended as a multi-tenant sweep, make it explicit and ensure additional protections so individual tenant data cannot be read/processed by an unauthorised caller.
- **server/agentic-features.ts:861** — enrollInSequence runs an existence check SELECT on `outreach_enrollments` without tenant scoping (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'). That can observe/affect enrollments from other tenants and incorrectly block or influence behavior.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE sequence_id = ${params.sequenceId} AND tenant_id = ${params.tenantId} AND contact_email = ${params.contactEmail} ...), or validate ownership of the enrollment rows before deciding.
- **server/agentic/approvals.ts:134** — expireStaleApprovals performs a bulk UPDATE on `agent_approvals` to mark pending approvals expired without a tenant_id constraint (UPDATE agent_approvals SET status = 'expired' WHERE status = 'pending' AND expiresAt < now()). This updates approvals across all tenants in one statement.
  - _Fix:_ If the sweep is intended to be global, make that explicit and ensure the caller has proper operational privileges; otherwise restrict the UPDATE to a tenant (e.g. include AND tenant_id = ${tenantId}) or run per-tenant iterations. Also ensure subsequent per-row run updates use the tenant from the expired row when updating linked runs (they currently do but the initial bulk UPDATE should still be considered).
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT on tenant-scoped tables: the initial db.execute SELECT reads from agent_runs (and joins agent_approvals) without constraining to a single tenant. This returns rows across tenants and can expose/operate on other tenants' run/approval data before later per-row checks.
  - _Fix:_ Restrict the initial query to a single tenant (e.g. add WHERE r.tenant_id = ${tenantId} or iterate per-tenant) or otherwise ensure the SELECT is explicitly parameterized by the caller's tenant. If the worker must scan all tenants, perform a safe, audited per-tenant loop (select tenant ids first, then query each tenant-scoped table with WHERE tenant_id = <that tenant>) so every read uses an explicit tenant_id filter.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table `agent_wake_schedules` performs an unscoped row claim via WHERE id IN (SELECT id FROM agent_wake_schedules ... FOR UPDATE SKIP LOCKED) with no tenant_id filter — this can claim/modify pending wakes across tenants.
  - _Fix:_ Limit the claim to a single tenant (add AND tenant_id = ${tenantId} to the inner SELECT / outer UPDATE), or run the waiter per-tenant. Ensure any cross-tenant maintenance is gated by platform-admin authorization and documented as an explicit global operation.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... executes without a tenant_id constraint, returning conversations across all tenants for backfill processing.
  - _Fix:_ Run backfill per-tenant or add an explicit tenant_id filter to the query. If cross-tenant maintenance is required, restrict execution to a platform-admin context and document the operation; otherwise ensure all reads/writes against tenant-scoped tables include WHERE tenant_id = <caller tenant>.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this write can create rows without the caller's tenant context and breaks the invariant that every INSERT must pass tenantId explicitly.
  - _Fix:_ Include the tenant ID in the invoice_items INSERT and/or validate ownership before inserting. For example, add tenant_id to the INSERT columns and pass ${tid} in the VALUES, or explicitly verify the invoice (newId) belongs to tid before inserting. Ensure all child rows that are tenant-scoped are written with tenant_id = ${tid} (and consider adding a DB constraint to make this explicit).
- **server/channel-routing.ts:30** — Query selects from personas (JOIN personas p ON p.id = cr.persona_id) with no tenant_id constraint on the personas table — this can expose or link persona rows belonging to other tenants.
  - _Fix:_ Constrain the JOIN by tenant (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}) or make channel_routes tenant-scoped and include tenant_id in the WHERE clause. At minimum, always filter any read of tenant-scoped tables by the caller's tenant_id.
- **server/channel-routing.ts:53** — INSERT into global channel_routes uses a caller-supplied personaId with no verification that the persona belongs to the caller's tenant. This lets a caller associate channels with personas from other tenants.
  - _Fix:_ Validate ownership before writing: either add tenant_id to channel_routes and include tenantId in the INSERT/UPDATE, or perform a server-side check (e.g. SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}) and reject the operation if it doesn't match. Ensure all updates/inserts of references to tenant-scoped rows verify tenant ownership.
- **server/chat-engine.ts:1605** — buildMemorySection declares a default tenantId = 1 (fail-open admin default). If this function is ever called without an explicit tenantId, tenant-scoped reads/operations inside (embeddings, ranking, telemetry like recordHydeAttempt/recordHydeComparisonFailure/recordHydeShadow, proactiveContextLoad, etc.) will run under tenant 1 and can leak data or perform cross-tenant operations.
  - _Fix:_ Remove the default tenantId = 1. Require an explicit tenantId parameter (fail-closed) and/or assert/throw when tenantId is missing or invalid. Audit all call sites to ensure they always pass the caller's tenantId. If a backward-compatible change is needed, add a runtime guard that rejects calls with missing/invalid tenantId rather than defaulting to admin tenant 1.
- **server/chat-engine/project-context.ts:158** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (`WHERE tenant_id = ${tenantId}`) — query only filters by project_id.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` or use Drizzle `.where(eq(project_files.tenantId, tenantId))`) so every read is explicitly constrained to the caller's tenant.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` lacks an explicit tenant_id constraint (`WHERE tenant_id = ${tenantId}`) — query only filters by project_id.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` or use Drizzle `.where(eq(project_notes.tenantId, tenantId))`) to ensure tenant isolation is enforced on every read.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` without tenant_id filter (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Fix:_ Limit the SELECT to a single tenant or iterate per-tenant. Example fixes: add a tenant predicate (e.g. WHERE tenant_id = ${tenantId} AND ...) or run inside a tenant-scoped transaction (withTenantTx) so RLS (or app-level WHERE) enforces isolation. Do not perform a global SELECT that exposes other tenants' rows.
- **server/deep-interview.ts:338** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The statement inserts (project_id, content, created_at) via SELECT but does not supply tenant_id explicitly.
  - _Fix:_ Include tenant_id explicitly in the INSERT so the new row is properly scoped, e.g.: INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() FROM ... WHERE p.tenant_id = ${state.tenantId} LIMIT 1. Alternatively, run the INSERT inside a tenant-scoped transaction (withTenantTx) and explicitly set tenant_id from a trusted source.
- **server/doc-collections.ts:166** — addDocument issues a DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete rows belonging to other tenants.
  - _Fix:_ Constrain the DELETE by tenant_id (AND tenant_id = ${tenantId}). Ensure collectionId is validated against the caller's tenant and include tenant_id in every row-level mutation against tenant-scoped tables.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks via UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} without verifying tenant_id. This is a write to a tenant-scoped table with no tenant constraint.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or perform the UPDATE via collection_id + tenant_id or otherwise ensure the row belongs to the caller's tenant before mutating it. Pass tenantId into the function if needed.
- **server/embeddings.ts:842** — SELECT from tenant-scoped table `memory_entries` with no tenant_id filter (backfill selects all tenants' rows).
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or run the backfill per-tenant and pass tenantId through to the update path so rows for other tenants are not read/modified.
- **server/embeddings.ts:852** — UPDATE memory_entries sets embedding_vec WHERE id = ${row.id} but contains no tenant_id constraint — this can update rows across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or call storeEmbeddingVec with a validated tenantId) so the update only affects the intended tenant's row.
- **server/embeddings.ts:858** — SELECT from tenant-scoped table `agent_knowledge` with no tenant_id filter (backfill selects all tenants' rows).
  - _Fix:_ Restrict the SELECT to a specific tenant (add WHERE tenant_id = ${tenantId}) or iterate per-tenant and propagate tenantId to subsequent updates to avoid touching other tenants' data.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} has no tenant_id constraint — may update rows across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or call storeEmbeddingVec with a validated tenantId) so updates are scoped to the owning tenant only.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to executing an UPDATE using a raw table name when options.tenantId is undefined: UPDATE ${sql.raw(table)} ... WHERE id = ${id} — this performs tenant-unguarded updates on tenant-scoped tables.
  - _Fix:_ Disallow the no-tenantPath for tenant-scoped tables: require a positive tenantId for memory_entries/agent_knowledge and include tenant_id = ${tenantId} in the WHERE clause. If a global maintenance path is needed, make it explicit and ensure operator/maintenance safeguards and auditing are in place.
- **server/embeddings.ts:811** — SELECT id, tenant_id, title, content FROM agent_knowledge ... LIMIT 50 executes without a tenant_id filter (global read across tenants) in backfillMissingKnowledgeEmbeddings.
  - _Fix:_ Run this backfill per-tenant (filter by tenant_id) or otherwise ensure you only process rows belonging to a tenant context; propagate the tenant_id into subsequent updates to guarantee isolation.
- **server/email.ts:370** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 — no tenant_id or inbox_id filter. This will treat an existing message_id in any tenant as a blocker and can prevent storing a legitimately different-tenant message.
  - _Fix:_ Include a tenant-scoped predicate (e.g. AND tenant_id = ${tenantId}) or at minimum qualify by inbox_id (which is known in the polling context) so existence checks are per-tenant and cannot cause cross-tenant skipping.
- **server/email.ts:415** — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') AND (body_html IS NULL OR body_html = '') ORDER BY id — missing tenant_id filter; iterates all tenants' inbox messages.
  - _Fix:_ Restrict the SELECT to a tenant (WHERE tenant_id = ${tenantId}) or run the backfill per-tenant; avoid scanning/updating rows for tenants outside the intended context.
- **server/email.ts:428** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} — no tenant_id constraint on UPDATE, allowing cross-tenant updates if id collisions or mistaken rows are processed.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise ensure row.id was fetched within the same tenant-scoped query) so updates can only modify rows belonging to the intended tenant.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with no explicit tenant_id constraint.
  - _Fix:_ Include the tenant filter in the WHERE clause (fail-closed). For example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Alternatively use Drizzle eq(scrapedPages.tenantId, tenantId) to ensure the row belongs to the caller's tenant before updating.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with no explicit tenant_id constraint.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always constrain writes by tenant_id even if the row was previously selected.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) without an explicit tenant_id constraint.
  - _Fix:_ Include the caller's tenant in the WHERE clause, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Even when you validated ownership earlier, always include tenant_id in the UPDATE to enforce isolation at the SQL level.
- **server/google-drive.ts:1664** — INSERT into project_files is executed via raw SQL without supplying an explicit tenant_id value. If project_files is a tenant-scoped table this will create a row with no tenant scoping and enable cross-tenant exposures.
  - _Fix:_ Either include tenant_id explicitly in the INSERT values or ensure the insert is done via a safe path that validates and binds tenant ownership. For example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${resolvedTenantId}, ${params.projectId}, ...). If project_files intentionally lacks tenant_id, make sure every consumer queries it joined to projects.tenant_id and document the design; otherwise add tenant_id and make it NOT NULL.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` without any tenant_id constraint (cross-tenant read).
  - _Fix:_ If this is intended to run per-tenant, iterate tenants and add WHERE tenant_id = ${tenantId} to the query (or otherwise scope by tenant). If it truly must be global (admin-only), make that explicit and put an admin-only guard around the operation and/or document why cross-tenant access is required; avoid unscoped queries that can leak tenant data.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` (likely tenant-scoped) without any tenant_id filter — enumerates tasks across tenants.
  - _Fix:_ If this scan must be tenant-scoped, query per-tenant with WHERE tenant_id = ${tenantId}. If the scheduler needs to act cross-tenant, enumerate tenants via a trusted global tenants table and then operate per-tenant while applying tenant_id filters for any read/write operations.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at <= NOW() executes against `research_schedules` with no tenant_id filter (cross-tenant read).
  - _Fix:_ Either run this query per-tenant (add WHERE tenant_id = ${tenantId}) or explicitly enumerate tenants from a trusted global source and then scope all reads/updates to that tenant. Avoid unscoped SELECTs/operations on tenant-scoped tables; ensure updates always include tenant_id in the WHERE clause.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE heartbeat_tasks SET last_run_at = ..., next_run_at = ... WHERE id = ${id} ... executes without constraining by tenant_id. This can update a heartbeat_tasks row belonging to another tenant if an id is abused.
  - _Fix:_ Bind the update to the task's tenant (e.g., AND tenant_id = ${tenantId}) or accept/require (and validate) a tenantId parameter for this scheduler boundary. Alternatively, select the row first and verify its tenant before updating.
- **server/heartbeat-startup.ts:76** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} executed with no tenant_id constraint — an update by id only can modify another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${t.tenantId}) or perform a guarded lookup that asserts ownership/tenant before applying the change. If this mutation must be cross-tenant, restrict it to an audited admin path only.
- **server/heartbeat-startup.ts:80** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} executed without a tenant_id constraint and may modify rows from other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${t.tenantId}) or verify the task's tenant in application logic prior to updating. Limit cross-tenant updates to an explicit, auditable admin boundary.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint — this can change approval state on another tenant's task if ids are mixed.
  - _Fix:_ Constrain the UPDATE by tenant (AND tenant_id = ${tenantId}) or perform a prior lookup to verify the task belongs to the expected tenant before changing approval_status. Prefer using storage APIs that enforce tenant scoping.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint (second occurrence) and may affect a task from a different tenant.
  - _Fix:_ Same fix: include AND tenant_id = ${tenantId} on the UPDATE or validate task ownership prior to updating. Use tenant-scoped storage helpers rather than raw cross-tenant UPDATEs.

### MEDIUM (19)

- **server/agentic/self-initiative.ts:100** — Potential tenant-scoping gap in telemetry query: the heartbeat_logs COUNT query filters by task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}) but does not include an explicit WHERE heartbeat_logs.tenant_id = ${tenantId}. If heartbeat_logs is tenant-scoped, this read lacks the required tenant_id equality guard.
  - _Fix:_ Constrain the heartbeat_logs read explicitly to the tenant, for example add AND heartbeat_logs.tenant_id = ${tenantId}, or JOIN heartbeat_logs to heartbeat_tasks with an explicit WHERE heartbeat_tasks.tenant_id = ${tenantId} and heartbeat_logs.tenant_id = ${tenantId} as appropriate so every read against a tenant-scoped table includes a direct tenant_id filter.
- **server/auth.ts:1128** — DELETE FROM password_reset_tokens WHERE token = ${tokenHash} (deleteResetToken) omits tenant_id in the WHERE clause on a tenant-scoped table (password_reset_tokens has tenant_id).
  - _Fix:_ Constrain the DELETE by tenant (e.g. parse/require the embedded tenant id and use AND tenant_id = ${tenantId}), or change the API to accept a (token, tenantId) pair so the deletion is always scoped to the tenant.
- **server/auth.ts:1134** — DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} (cleanExpiredResetTokens) deletes rows across all tenants without a tenant_id constraint.
  - _Fix:_ Perform expiration cleanup per-tenant (iterate tenants or accept an admin-only global migration path), or ensure the maintenance path runs with explicit platform-admin authority and is audited. Prefer adding tenant scoping where appropriate to avoid accidental cross-tenant mutation.
- **server/auth.ts:1242** — DELETE FROM auth_sessions WHERE expires_at < ${now} (clearExpiredSessions/load path) removes expired sessions across all tenants with no tenant_id constraint on the tenant-scoped auth_sessions table.
  - _Fix:_ Scope deletion to a tenant when called in a tenant context (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ...), or require and assert platform-admin authority for global cleanup jobs and audit the action. Avoid unscoped deletes on tenant-scoped tables in normal request paths.
- **server/auth.ts:1339** — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' (loadSessionsFromDb) deletes session rows across tenants without tenant filtering when purging legacy tokens.
  - _Fix:_ Perform this one-time migration/cleanup under an explicit, auditable platform-admin migration path, or include tenant scoping where appropriate. If the operation must be global, ensure it runs only during a controlled maintenance window and cannot be triggered by normal tenant-context code.
- **server/cmmc-assessments.ts:150** — Public token lookup SELECT on tenant-scoped tables (`cmmc_assessment_invitations` / `cmmc_assessments`) does not include an explicit tenant_id constraint in the WHERE clause — the query locates rows solely by token hash/expiry/revoked state.
  - _Fix:_ If the token is intentionally global, document and harden the design (rotate/expire tokens, audit usage). Otherwise require/accept an expected tenantId and include `AND <table>.tenant_id = ${tenantId}` in the WHERE clause (or validate the retrieved row's tenant_id against an expected tenant) so reads are explicitly tenant-scoped.
- **server/crews-engine.ts:326** — Request-derived array of tool names is interpolated into a Postgres array literal string (toolsArr) before being passed into the SQL template. Constructing SQL-format literals from untrusted input risks injection/semantics mistakes and weakens binding semantics.
  - _Fix:_ Avoid building SQL array literals by string concatenation. Pass the array as a parameter or use proper typed array/sql helpers (e.g. sql.array(params.tools, 'text') or supply JSON/JSONB). Example: const toolsArr = params.tools && params.tools.length > 0 ? params.tools : null; ... VALUES (..., ${toolsArr}, ...) and let the driver/ORM bind the array, or use sql.array to build a safe SQL ARRAY expression.
- **server/crews-engine.ts:391** — Request-derived arrays (tools/contextTaskIds → toolsArr, ctxArr) are constructed as Postgres array literal strings via string interpolation before being injected into the SQL template. This is string-interpolated SQL with caller-controlled values.
  - _Fix:_ Do not manually craft array literal strings from untrusted inputs. Instead pass arrays as parameters so the DB driver/ORM binds them safely, or use sql.array/sql.raw with sanitized/validated elements. Example: const ctxArr = params.contextTaskIds?.length ? params.contextTaskIds : null; ... VALUES (..., ${ctxArr}, ...) and ensure proper typing/casting on the DB side.
- **server/crews-engine.ts:947** — Request-derived arrays (listenTo/routerOutputs → listenArr/routerArr) are turned into SQL array literal strings via string interpolation and inserted into a SQL template. This constructs SQL-formatted strings from untrusted input, which is an injection/semantics risk.
  - _Fix:_ Avoid building SQL array literals by concatenation. Pass the arrays as parameters or use Drizzle/driver helpers to bind them safely (e.g. sql.array(params.listenTo, 'text') or supply JSONB). Validate/normalize elements before persistence and use parameterized binding rather than embedding preformatted SQL strings.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` without tenant_id constraint (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} ...). Missing tenant predicate allows cross-tenant reads if project_id is manipulated or FK anomalies exist.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId}) to the query, or join to projects and assert p.tenant_id = ${tenantId} in the WHERE clause. Always include tenant_id when selecting from tenant-scoped tables.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` without tenant_id constraint (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} ...).
  - _Fix:_ Scope the query by tenant (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the read happens in a tenant-scoped transaction. Never rely solely on a prior derived project_id without also asserting tenant ownership in the query.
- **server/data-protection.ts:404** — SELECT from tenant-scoped table `messages` without tenant_id constraint (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}). This is an existence/oracle read not scoped to tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the COUNT() query (or run the check inside a tenant-scoped transaction). Even when conversation IDs were selected earlier for this tenant, every child-table read must explicitly constrain tenant_id to prevent isolation leaks.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken performs a SELECT on dfy_intake_forms without constraining by tenant_id (SELECT ... WHERE token = ...). This reads a tenant-scoped table with no tenant filter.
  - _Fix:_ Include tenant scoping in the lookup (e.g. accept/derive tenantId and add AND tenant_id = ${tenantId}) or otherwise enforce that the token search is safely tenant-scoped (document/ensure token uniqueness per-tenant and/or require tenant context).
- **server/dormant-deprecation.ts:169** — Multiple queries read tenant-scoped tables without tenant constraints (e.g. SELECT COUNT(*) FROM messages WHERE ... at line ~169; SELECT COUNT(DISTINCT tool_name) FROM tool_performance ... at ~179; SELECT DISTINCT tool_name FROM tool_performance ... at ~203 and ~214). These aggregate reads on tenant-scoped tables lack tenant_id filters.
  - _Fix:_ If these computations must be tenant-specific, accept/derive a tenantId and add WHERE tenant_id = ${tenantId} to each query. If they are intentionally global, document why these tables are global/shared; otherwise scope them to avoid cross-tenant data leakage or incorrect statistics.
- **server/falsified-routes.ts:174** — SQL string-interpolation used to build a Postgres array literal for the id list: id = ANY({...}::int[]). The code constructs the array text via `${`{${ids.join(",")}}`}` and inlines it into the sql`...` template. Constructing SQL array literals by string concatenation is error-prone and can lead to injection/semantics issues even if ids originate from DB rows. It also bypasses safe parameterized-array binding patterns.
  - _Fix:_ Avoid creating an array literal string. Pass the ids as a parameterized array (or use Drizzle helpers) so the driver can bind the array safely. Example: use a parameterized form such as WHERE id = ANY(${ids}::int[]) (binding `ids` as an array) or use a proper helper like an `inArray`/array binding utility provided by your DB/ORM rather than interpolating `'{...}'` text.
- **server/glasses-gateway.ts:173** — UPDATE on tenant-scoped table 'apiKeys' sets lastUsedAt using WHERE eq(apiKeys.id, key.id) but does not include a tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the API key's tenant to avoid accidental cross-tenant modification. For example: .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))). If you derive tenant from the authenticated key, bind and use that tenant value explicitly in the WHERE clause.
- **server/heartbeat-schedules.ts:26** — SELECT DISTINCT tenant_id FROM messages reads the tenant-scoped `messages` table without a tenant constraint to discover tenant IDs (cross-tenant scan).
  - _Fix:_ Use a global tenant registry (e.g. tenants table) to enumerate active tenant IDs, or otherwise restrict permissions and justify admin-only cross-tenant enumeration. When performing per-tenant work, always run subsequent queries with WHERE tenant_id = ${tenantId}.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks is executed with no tenant_id constraint (cross-tenant read of a tenant-scoped table).
  - _Fix:_ If this is intended only for a privileged scheduler boundary, document and restrict it to a well-audited scheduler process; otherwise add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(table.tenantId, tenantId))) or require an explicit admin/ALL_TENANTS guard so ordinary code paths cannot read other tenants' tasks.
- **server/heartbeat.ts:255** — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id constraint, returning pending events across all tenants into the heartbeat process.
  - _Fix:_ If the intention is per-tenant processing, add WHERE tenant_id = ${tenantId}. If a cross-tenant scheduler is intended, ensure this code path is only used by a trusted scheduler and add explicit documentation/authorization checks; consider selecting tenant_id along with rows and validating each row's tenant before processing.

### LOW (1)

- **server/auth.ts:338** — DELETE FROM auth_sessions WHERE token = ${tokenHash} (deleteSession) deletes from a tenant-scoped table without an explicit tenant_id constraint — the token is used as the sole selector.
  - _Fix:_ Where possible include tenant_id in the DELETE predicate (DELETE ... WHERE token = ${tokenHash} AND tenant_id = ${tenantId}) or verify the token embeds tenant information and that the caller is authorized for that tenant. This makes the intent explicit and reduces risk of accidental cross-tenant mutation.
