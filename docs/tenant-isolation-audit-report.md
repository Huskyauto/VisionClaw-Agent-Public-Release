# Tenant-Isolation Audit Report

_Generated 2026-08-01T02:23:28.590Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **581/581** file(s) audited · chunks 53/53 OK (0 failed)
- Findings: **277** (CRITICAL 77 · HIGH 156 · MEDIUM 43 · LOW 1)
- Precision second-pass (`gpt-5.4`): **161** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (77)

- **server/agentic-engines.ts:243** — autoApplyEligibleInsights performs a global UPDATE on tenant-scoped table `ai_insights` without tenant_id scoping: `UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...`. This updates rows across all tenants in one operation.
  - _Fix:_ Do not run an unscoped cross-tenant UPDATE. Either perform this per-tenant (adding `WHERE tenant_id = ${tenantId}`) or otherwise ensure the operation is explicitly authorized to mutate all tenants. Prefer iterating tenants or queuing per-tenant jobs so each UPDATE includes tenant_id = <target tenant>.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on tenant-scoped table `agent_approvals` without tenant scoping: `UPDATE agent_approvals SET status = 'expired', decidedAt = new Date() WHERE status = 'pending' AND expiresAt < now() RETURNING id, runId, tenantId`. This updates approvals for all tenants in one statement.
  - _Fix:_ Do not run an unscoped cross-tenant expiry. Either run this per-tenant (add `AND tenant_id = ${tenantId}`) or explicitly authorize and audit the global operation. At minimum, ensure per-row tenant validation before applying downstream per-run updates.
- **server/agentic/resume-worker.ts:66** — SELECT query pulls agent_runs JOIN agent_approvals without constraining to a specific tenant (no WHERE tenant_id = <caller tenant>). This returns rows across tenants and allows the worker to observe/process approvals/runs for other tenants.
  - _Fix:_ Restrict the query to a single tenant context. Either iterate per-tenant and include WHERE r.tenant_id = ${tenantId} (parameterized) or add an explicit tenant filter (e.g. WHERE r.tenant_id = ${someTrustedTenantId}). Avoid wide cross-tenant selects; ensure any rows processed are additionally verified with a tenant_id check before taking action.
- **server/auto-memorize.ts:75** — INSERT into agent_knowledge (setWatermark) sets tenant_id to a hardcoded value (1) rather than the acting caller's tenant — this writes a tenant-scoped table with an incorrect/implicit tenant and can misattribute or leak data across tenants.
  - _Fix:_ Do not hardcode tenant_id. Pass the caller's tenantId into setWatermark and use that value in the INSERT (VALUES (..., ${tenantId}, ...)). Ensure every INSERT into tenant-scoped tables explicitly sets tenant_id from a verified/trusted source and validate ownership where appropriate.
- **server/data-protection.ts:24** — memory_categories table is created with tenant_id INTEGER DEFAULT 1, allowing inserts to omit tenant_id (violates invariant that tenant_id MUST be provided explicitly and NOT NULL).
  - _Fix:_ Remove the DEFAULT 1 and make tenant_id NOT NULL (or otherwise require explicit tenant_id on INSERT). Prefer: tenant_id INTEGER NOT NULL with no default, and update any migration/code to always pass tenantId explicitly.
- **server/data-protection.ts:48** — ALTER TABLE agent_knowledge adds tenant_id INTEGER DEFAULT 1, permitting rows to be inserted without an explicit tenant_id (violates requirement that tenant_id be supplied explicitly).
  - _Fix:_ Remove the DEFAULT 1. Add tenant_id as NOT NULL with no default, and ensure all INSERTs into agent_knowledge explicitly pass the caller's tenantId (or run a migration that backfills safely and then set NOT NULL).
- **server/data-protection.ts:131** — DELETE FROM messages WHERE conversation_id = ${conv.id} is executed without constraining by tenant_id — destructive write may remove another tenant's messages if IDs collide or attack is possible.
  - _Fix:_ Include tenant scoping on all DELETEs (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}). Prefer passing the tenantId into the statement (or run per-tenant) to ensure the write is constrained to the intended tenant.
- **server/data-protection.ts:132** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks a tenant_id predicate — deletion may affect other tenants' archives.
  - _Fix:_ Constrain deletes by tenant (e.g. AND tenant_id = ${conv.tenant_id}), or perform deletes via a JOIN that enforces the parent conversation's tenant_id. Never DELETE tenant-scoped rows by id alone.
- **server/data-protection.ts:133** — DELETE FROM conversations WHERE id = ${conv.id} is executed without tenant_id predicate — may delete a conversation for the wrong tenant in case of ID confusion or race.
  - _Fix:_ Include tenant scoping: DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Consider using WITH tenant tx or explicit tenant param binding for background jobs that touch multiple tenants.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped table (memory_entries or agent_knowledge) performed without scoping to tenant_id — storeEmbeddingVec updates rows by id only (no tenant filter). This allows cross-tenant modification.
  - _Fix:_ Require and accept a tenantId parameter to storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Prefer using the typed Drizzle query builder rather than sql.raw for table names; if sql.raw is unavoidable for table identifiers keep the allowlist but always enforce tenant_id in the UPDATE.
- **server/embeddings.ts:716** — SELECT FROM memory_entries (backfillEmbeddingVecs) with no tenant_id filter (SELECT id, embedding FROM memory_entries ...). This reads tenant-scoped data across all tenants.
  - _Fix:_ Scope the SELECT to a single tenant (add WHERE tenant_id = ${tenantId}) or iterate per-tenant. If this backfill is intended to be multi-tenant, run per-tenant worker that passes the tenantId and enforces it in all queries/updates.
- **server/embeddings.ts:726** — UPDATE memory_entries in backfillEmbeddingVecs updates rows by id only (WHERE id = ${row.id}) with no tenant_id constraint, enabling modification across tenants.
  - _Fix:_ Include tenant_id in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the backfill within a per-tenant context so the tenant is explicitly passed and enforced for each UPDATE.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge (backfillEmbeddingVecs) executed without tenant_id filter (SELECT id, embedding FROM agent_knowledge ...). This reads knowledge rows across all tenants.
  - _Fix:_ Add tenant scoping to the select (WHERE tenant_id = ${tenantId}) or run the backfill per-tenant so every read is constrained to that tenant.
- **server/embeddings.ts:743** — UPDATE agent_knowledge in backfillEmbeddingVecs updates rows by id only (WHERE id = ${row.id}) with no tenant_id constraint, allowing cross-tenant writes.
  - _Fix:_ Include tenant_id in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform backfill using a per-tenant loop that passes tenantId and enforces it in the UPDATE.
- **server/embeddings.ts:688** — SELECT in backfillMissingKnowledgeEmbeddings queries agent_knowledge without tenant scoping (SELECT id, title, content FROM agent_knowledge WHERE ...). This can return rows for other tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or run the backfill per-tenant and pass a tenantId so the query is strictly constrained to that tenant's rows.
- **server/lib/ideabrowser-backfill.ts:104** — INSERT INTO project_files does not set tenant_id. Platform invariant requires explicit tenant_id on INSERT into tenant-scoped tables (no defaults).
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. tenant_id = ${tenantId}) or otherwise ensure project_files is not a tenant-scoped table. Validate that the inserted row is associated with the correct tenant before inserting.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT INTO project_notes does not set tenant_id. Every INSERT into tenant-scoped tables must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Add tenant_id to the INSERT (VALUES (..., ${tenantId}, ...)) or otherwise ensure project_notes is not tenant-scoped. If project_notes should be tenant-scoped, update the insert to include tenant_id and validate the tenant association of project_id.
- **server/memory-graph.ts:210** — INSERT into tenant-scoped table memory_links does not include tenant_id (tenant_id column omitted from VALUES). Inserts into tenant-scoped tables must always set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO memory_links (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Also ensure the surrounding logic validates that the target/source memories belong to the same tenant before inserting.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — UPDATE on tenant-scoped table uses id only and lacks an explicit tenant_id constraint. This can modify another tenant's row if ids are guessed or misaligned.
  - _Fix:_ Always include tenant_id in the WHERE clause for updates (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}). Validate the row's tenant before performing destructive updates to ensure tenants cannot affect each other's data.
- **server/oauth-subscriptions.ts:981** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — same pattern as above: UPDATE without tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) and/or derive and validate tenantId from the row before changing it. Never issue UPDATE/DELETE on tenant-scoped tables without confirming tenant ownership.
- **server/persona-export.ts:33** — exportPersona reads persona by id using SELECT * FROM personas WHERE id = ${personaId} without constraining by tenant_id. The function accepts tenantId but does not validate that the persona row belongs to that tenant — allows cross-tenant data export/leak.
  - _Fix:_ Add tenant scoping to the persona lookup and validate ownership (e.g. SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). If the personaId is global, explicitly document and enforce that behavior; otherwise always require tenant_id in queries that return tenant-scoped records.
- **server/plan-executor.ts:116** — appendExecutionLog executes UPDATE plans ... WHERE id = ${planId} without constraining by tenant_id. This allows a caller that can invoke this function with an arbitrary planId to modify another tenant's plan row.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. AND tenant_id = ${expectedTenantId}). Pass the authoritative tenant id (from the claimed/returned plan row) into appendExecutionLog so every UPDATE is restricted to that tenant's row.
- **server/plan-executor.ts:513** — Claim step performs UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' RETURNING ... with NO tenant_id constraint. This allows changing status of any plan by id across tenants (cross-tenant mutation).
  - _Fix:_ Restrict the claim to the correct tenant: add AND tenant_id = ${callerTenantId} (or include the caller/admin tenant context) in the WHERE clause so only plans belonging to the caller's tenant can be claimed. Always combine id + tenant_id when modifying tenant-scoped rows.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} is executed without a tenant_id constraint when a plan has no steps. This updates a plan by id across tenants.
  - _Fix:_ Add AND tenant_id = ${expectedTenantId} to the WHERE clause (use the tenantId returned from the claimed plan) so the update only affects the plan within the intended tenant.
- **server/plan-executor.ts:725** — Final status update (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) is performed without scoping to tenant_id, allowing cross-tenant status changes and log appends.
  - _Fix:_ Include tenant_id = ${expectedTenantId} in the WHERE clause (use the plan row's tenant_id). Ensure every UPDATE/DELETE on plans includes the tenant guard.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — the UPDATE is not constrained by tenant_id. Although t.id was selected earlier for this tenant, the UPDATE without tenant filter could affect a row id colliding in another tenant.
  - _Fix:_ Always include tenant scoping on UPDATEs: UPDATE heartbeat_tasks SET ... WHERE id = ${t.id} AND tenant_id = ${tenantId}. Prefer binding the tenant (safeTenantId) rather than relying on prior selection provenance.
- **server/process-governor.ts:641** — UPDATE agent_desks ... WHERE id = ${desk.id} — missing tenant_id in WHERE; this mutates agent_desks by id without re-checking tenant ownership.
  - _Fix:_ Constrain the UPDATE to the tenant: UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId} (or verify desk.tenant_id equals tenantId before updating). Always validate ownership for writes by id.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} — updateProjectBrain takes only projectId (no tenant context) and reads a tenant-scoped projects row without verifying tenant ownership.
  - _Fix:_ Require and accept tenantId for updateProjectBrain (or resolve caller tenant from request context), and add WHERE tenant_id = ${tenantId} to the SELECT. Before operating on project data or writing derived artifacts, verify the project belongs to the caller's tenant.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id} — this write uses id only (no tenant_id) after selecting rows across tenants, allowing the updater to modify other tenants' rows.
  - _Fix:_ Include tenant scoping on the UPDATE: UPDATE agent_knowledge SET ... WHERE id = ${r.id} AND tenant_id = ${r.tenant_id} (or the caller's tenant). For global runners that process many tenants, ensure each mutating statement also constrains by the row's tenant_id to prevent cross-tenant writes.
- **server/routes.ts:166** — Unscoped SELECT from tenant-scoped table `file_storage` in startup restoreUploadsFromDb — no tenant_id filter. This reads all tenants' stored files and may write them to disk, breaking tenant isolation.
  - _Fix:_ Scope the query per-tenant (e.g. iterate tenants and query WHERE tenant_id = <tid>) or include a tenant_id constraint. Avoid scanning all file_storage rows in a single unscoped query; if a global maintenance job is required, run it under an explicit admin context and ensure additional safeguards and audit logging.
- **server/routes.ts:1117** — SELECT from tenant-scoped table `file_storage` by filename with no tenant constraint (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This reads ownership rows across all tenants, enabling cross-tenant enumeration of who owns a filename.
  - _Fix:_ Avoid unscoped lookups. If collision-detection across tenants is required, perform safe checks that don't return or touch rows for other tenants (e.g. check only for a same-tenant owner first, or require the signed URL to include tenant info and validate with that tenant_id). If a cross-tenant check is truly needed, run it under a tightly controlled admin flow and ensure no leakage of other tenants' metadata.
- **server/routes.ts:4331** — Direct INSERT into project_notes omits tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) is executed without providing tenant_id, violating tenant isolation assumptions for tenant-scoped tables.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ...)). Ensure tenantId is the authenticated caller's tenant and not derived from untrusted input; validate ownership of projectId before inserting.
- **server/routes/conversations.ts:101** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id — tenant_id is NOT provided in VALUES, allowing creation of rows with no tenant ownership.
  - _Fix:_ Include tenant_id in the INSERT values, e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}); or use a storage/ORM helper that always sets tenant_id from the authenticated context.
- **server/routes/conversations.ts:103** — UPDATE on tenant-scoped table 'projects' performed without constraining by tenant_id (WHERE clause lacks tenant scope).
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}; to prevent cross-tenant modifications.
- **server/routes/projects.ts:185** — INSERT into project_notes does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT values (and bind it from the authenticated tenant). Example: INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) RETURNING *; validate that tenantId was derived from a trusted authenticated session.
- **server/routes/projects.ts:225** — INSERT into project_files (files-base64 route) does not include tenant_id. Inserting into a tenant-scoped table without tenant_id violates tenant isolation invariant.
  - _Fix:_ Add tenant_id to the INSERT column list and pass ${tenantId} as the value. Example: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}) RETURNING *; ensure tenantId comes from getTenantFromRequest(req).
- **server/routes/projects.ts:293** — INSERT into project_files (multipart upload route) does not include tenant_id. Tenant_id must be explicitly set on INSERT into tenant-scoped tables.
  - _Fix:_ Include tenant_id in the INSERT (and bind ${tenantId}). Example: INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) RETURNING *; continue to verify project ownership before inserting.
- **server/sculptor.ts:105** — UPDATE sculptor_sessions sets status/result by session id without constraining tenant_id (UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id}). This can modify another tenant's session if an attacker supplies/guesses an id.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the session row belongs to the caller's tenant before updating.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions sets conversation_id by session id without constraining tenant_id (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). This can link a conversation into a session of another tenant.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or explicitly validate ownership prior to the write.
- **server/sculptor.ts:168** — monitorSession() updates sculptor_sessions (UPDATE sculptor_sessions ... WHERE id = ${sessionId}) without constraining tenant_id even though tenantId is available. This allows cross-tenant modification by session id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE's WHERE clause or re-query the session under the tenant before updating to ensure the row belongs to the calling tenant.
- **server/sculptor.ts:358** — reviewSessionWork() updates sculptor_sessions.review_result by session id (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}) without constraining tenant_id, despite tenantId being available and used earlier to fetch the session.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or perform an ownership-verified UPDATE to ensure only the tenant's session is modified.
- **server/second-opinion.ts:404** — settleFusionReservation() updates agent_cost_ledger by reservation id (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}) with no tenant_id constraint. A reservation id could be misused to alter another tenant's ledger row.
  - _Fix:_ Require and use the tenant id when settling (e.g. accept tenantId param and add AND tenant_id = ${tenantId}), or first SELECT the reservation row to verify tenant ownership before performing the UPDATE.
- **server/second-opinion.ts:423** — releaseFusionReservation() updates agent_cost_ledger by reservation id (UPDATE agent_cost_ledger SET cost_usd='0' ... WHERE id = ${reservationId}) without a tenant_id constraint, allowing potential cross-tenant ledger modification.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or fetch-and-verify the reservation's tenant before updating; change function signature to accept/derive tenantId if needed.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' is executed without any tenant_id filter. heartbeat_tasks appears tenant-scoped, so this reads across tenants (and subsequent logic acts on that global count).
  - _Fix:_ Scope the SELECT to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true ...) or make explicit that the operation is intentionally global and gated to a trusted admin path. Prefer adding tenant_id filter to enforce per-tenant isolation.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true executes without tenant_id filter, disabling delegation tasks across all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure this administrative operation explicitly targets only authorized tenant(s). Avoid blanket updates without tenant scoping.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' is run without tenant_id filter, reading tasks across tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or parameterize tenantId) before counting, or document and gate an intentional global operation to a trusted admin-only code path.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true runs without tenant_id filter, disabling matching tasks for all tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${ADMIN_TENANT_ID} (or the intended tenant) or otherwise ensure the script only modifies rows for the authorized tenant. Never perform mass tenant-scoped updates without explicit tenant filtering.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) executes without tenant_id filter; the subsequent UPDATE (lines 74-86) also misses tenant scoping and will disable matching tasks across tenants.
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or parameterize the tenant) in both the SELECT and the UPDATE WHERE clauses, or ensure these admin changes are explicitly constrained to the target tenant to avoid cross-tenant impacts.
- **server/seed.ts:3722** — DELETE FROM heartbeat_tasks is executed without a tenant_id constraint, which can remove rows across all tenants.
  - _Fix:_ Add an explicit tenant filter to the DELETE, e.g. DELETE ... WHERE tenant_id = <tenant> AND ... (or use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId))). Ensure destructive operations never run without tenant scoping.
- **server/seed.ts:3750** — UPDATE memory_entries runs without any tenant_id constraint (updates may affect all tenants' memory_entries).
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = <tenant>) or use Drizzle .where(eq(memoryEntries.tenantId, tenantId)). Never run UPDATE on a tenant-scoped table without constraining to the caller tenant.
- **server/seed.ts:3776** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint and will update conversations across all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant by adding WHERE tenant_id = <tenant> (or Drizzle .where(eq(conversations.tenantId, tenantId))). For cross-tenant-safe maintenance, run per-tenant updates or run as a controlled migration with explicit admin intent.
- **server/self-improvement.ts:401** — UPDATE on tenant-scoped table `experiments` uses only experiments.id in WHERE. This update can modify a row belonging to any tenant if an attacker supplies or guesses an experiment id.
  - _Fix:_ Constrain the UPDATE by tenant as well. e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) where tenantId is the caller's tenant. Failing that, include the tenant id from the originally-inserted row in the WHERE clause to enforce isolation.
- **server/self-improvement.ts:415** — UPDATE on tenant-scoped table `experiments` in the error-handling path uses only experiments.id in WHERE. This write lacks a tenant_id constraint and can affect other tenants' rows.
  - _Fix:_ As above, include a tenant constraint in the WHERE (AND tenant_id = <caller tenant>) — e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Ensure tenantId is available in this catch path or read it from the inserted experiment row and use it in the WHERE.
- **server/self-reflection.ts:366** — UPDATE memory_entries executes with WHERE id = <row id> but no tenant_id constraint. Updating by id alone can modify another tenant's entry if ids collide or are guessed.
  - _Fix:_ Include tenant isolation in the UPDATE, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or use both id and tenant_id in the WHERE). Also ensure tenantId is the caller's validated tenant and fail-closed if it's not available.
- **server/storage.ts:295** — SELECT from tenant-scoped table `messages` without constraining tenant_id (SELECT id, citations FROM messages WHERE id IN (...)) in attachCitationMetadata
  - _Fix:_ Add tenant scoping to this query (e.g. include AND tenant_id = <callerTenant> or JOIN via the parent conversation's tenant_id). Alternatively change the call-site to pass a validated tenantId and require attachCitationMetadata to accept/ enforce it, or make this function explicitly 'unscoped' and emit a loud audit log/throw when used from HTTP handlers.
- **server/storage.ts:396** — UPDATE on tenant-scoped table `messages` without tenant filter (UPDATE messages SET citations = ... WHERE id = ${messageId}) in attachCitations
  - _Fix:_ Require and validate tenantId at the API boundary and include eq(messages.tenantId, tenantId) in the WHERE clause. If this helper must update a specific tenant's row, accept tenantId as a parameter and fail closed if absent or invalid.
- **server/storage.ts:803** — UPDATE on tenant-scoped table `memory_entries` missing tenant constraint (updateMemoryEmbedding: WHERE id = ${id})
  - _Fix:_ Require a tenantId parameter and include tenant-scoping in the WHERE clause (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))). If this is a background job that must process across tenants, iterate per-tenant with explicit tenant context rather than issuing an unscoped UPDATE.
- **server/storage.ts:815** — UPDATE on tenant-scoped table `agent_knowledge` missing tenant constraint (updateKnowledgeEmbedding: WHERE id = ${id})
  - _Fix:_ Require tenantId and include eq(agentKnowledge.tenantId, tenantId) in the WHERE clause, or otherwise make the unscoped nature explicit and guarded (audit/log/require admin context) so it's not callable from ordinary HTTP handlers.
- **server/storage.ts:838** — UPDATE that archives expired memories runs across the entire tenant-scoped table `memory_entries` with no tenant filter (UPDATE memory_entries SET status='archived' WHERE ... expires_at <= NOW())
  - _Fix:_ Run archival per-tenant (accept tenantId or iterate tenants) or add a tenant filter to the statement. If this is intentionally global maintenance, restrict it to a privileged admin path and document/guard it strongly; do NOT leave a silent, unscoped UPDATE that can alter all tenants' data.
- **server/storage.ts:855** — UPDATE that archives stale memories runs across entire tenant-scoped table `memory_entries` with no tenant filter (archiveStaleMemories)
  - _Fix:_ Same as for archiveExpiredMemories: perform per-tenant operations or add an explicit tenant constraint. Fail closed if tenant context isn't supplied to avoid cross-tenant mutations.
- **server/storage.ts:1303** — getAllDataForExport performs many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, etc.) with no tenant constraint — full platform-wide dump
  - _Fix:_ This is a privileged admin operation and must be clearly gated. Add an explicit admin-only guard and/or require an admin tenant/context parameter. If per-tenant export is desired, require tenantId and apply tenant scoping on each SELECT. Avoid leaving an unguarded global export that any caller can trigger.
- **server/storage.ts:530** — INSERT into tenant-scoped table `memory_entries` (db.insert(memoryEntries).values(data)) without validating/ensuring data.tenantId is present and owned — unscoped write risk
  - _Fix:_ Fail closed: require and validate a positive integer tenantId on the data before inserting (e.g. assertValidTenantId(data.tenantId) or accept tenantId param and inject it from a validated context). Never insert into tenant-scoped tables without an explicit, validated tenant id.
- **server/tool-learning.ts:291** — UPDATE of `custom_tools` sets implementation by matching only id (where(eq(customTools.id, tool.id))) with no tenant_id constraint. This allows cross-tenant modification of another tenant's tool row.
  - _Fix:_ Enforce tenant scoping on updates. Add a tenant filter (for example .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId)))) or require the tenantId and verify ownership before performing the update.
- **server/tool-learning.ts:316** — DELETE on tenant-scoped table `custom_tools` is executed without constraining by tenant_id (await db.delete(customTools).where(eq(customTools.id, tool.id))). This can delete another tenant's tool row if IDs collide or are guessed.
  - _Fix:_ Always include the tenant in DELETE predicates, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId))). Ensure revertToolRow receives/validates tenantId or verifies ownership before deleting.
- **server/tool-learning.ts:365** — UPDATE to increment usageCount on `custom_tools` is performed by id only (.where(eq(customTools.id, tool.id))) with no tenant_id constraint. This permits cross-tenant writes.
  - _Fix:_ Include tenant scoping on usage updates, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). If tool objects include tenantId, use it; otherwise require and validate the caller's tenant before writing.
- **server/tools.ts:4540** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id in the VALUES list. The code inserts project_notes for a project without explicitly setting tenant_id even though tenant isolation is enforced only at the app layer.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Also keep/ensure the prior assertProjectInTenant(projectId, params._tenantId) check (or equivalent ownership assertion) before performing the INSERT to verify the caller owns the project.
- **server/tools.ts:4544** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The insert stores a project file row but omits tenant_id in the column list/VALUES.
  - _Fix:_ Add tenant_id to the column list and supply the caller's tenant (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${projectId}, ...)). Maintain the assertProjectInTenant(projectId, params._tenantId) guard (or equivalent) before the INSERT to validate ownership.
- **server/tools.ts:6392** — INSERT into tenant-scoped table 'project_files' (audio upload path) omits tenant_id in the VALUES. The code inserts a project_files row for params.project_id without setting tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Ensure the code verifies ownership (assertProjectInTenant) before inserting — which it does here — and also pass the tenant id into the inserted row.
- **server/tools.ts:6926** — INSERT into tenant-scoped table 'project_files' (video upload) omits tenant_id in the VALUES. The code saves the produced video to project_files for params.project_id but does not set tenant_id on insert.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Keep the assertProjectInTenant(params.project_id, params._tenantId) ownership check prior to the INSERT and ensure tenant_id is persisted with the row.
- **server/tools.ts:6926** — INSERT into project_files does not include tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) via a raw SQL template without supplying tenant_id, relying only on assertProjectInTenant for an ownership check.
  - _Fix:_ Include tenant_id explicitly in the INSERT and bind it from the authenticated tenant context (e.g. VALUES (..., ${params._tenantId})). Alternatively, add tenant_id to the column list and pass the verified tenantId variable. Do not rely solely on a prior assertion — every INSERT into tenant-scoped tables must set tenant_id in the statement (and avoid assuming DB defaults).
- **server/tools/domains/files/handlers.ts:835** — INSERT into tenant-scoped table `project_files` does not set tenant_id (INSERT INTO project_files (... ) VALUES (...)) — tenant_id column is omitted, so new row may not be tied to the caller's tenant.
  - _Fix:_ Include tenant_id in the INSERT and set it from the trusted context (e.g. tenant_id: ctx.tenantId). Example: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ${absPath}, ...). Also ensure tenantId is validated (non-null, number) before the INSERT and prefer schema-level NOT NULL/default constraints where appropriate.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint. Application-level isolation invariant requires every write to be constrained to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant. e.g. include tenant_id in the WHERE clause: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}. Or use Drizzle's .where(eq(memoryEntries.tenantId, tenantId)) so updates are always tenant-scoped.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with no tenant_id constraint. This can update rows from other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE. For example: UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE tenant_id = ${tid} AND id = ANY(${ids}). Prefer Drizzle .where(eq(table.tenantId, tenantId)).
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without any tenant_id constraint (after finding the id via a tenant-scoped SELECT). Application-level invariant requires explicit tenant filter on every write.
  - _Fix:_ Include tenant scoping on the UPDATE to avoid cross-tenant writes. E.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}. Using Drizzle .where(eq(table.tenantId, tenantId)) is recommended.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` performed without any tenant_id constraint (SELECT id, content::jsonb AS info FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ...). This reads rows across all tenants.
  - _Fix:_ Do not scan the tenant table across tenants. Either add a tenant_id filter to the query, or move messaging_pairing into an operator-controlled/global registry table that is explicitly audited and queried by admin-only code. If the cross-tenant scan is required for ambiguity detection, implement this logic against a dedicated, explicitly-global operator table (not tenant-scoped agent_knowledge), or ensure the query is run only by a trusted operator process with explicit justification and auditing.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} performed without verifying tenant ownership. This can modify another tenant's ledger row if an attacker supplies a reservationId from a different tenant.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Additionally, verify the reservation row belongs to the tenant before updating, and fail closed if the check fails.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} executed without tenant constraint in releaseVentureReservation. This allows releasing/updating a reservation row belonging to another tenant.
  - _Fix:_ Accept tenantId in releaseVentureReservation and add tenant ownership to the WHERE clause (WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Confirm the row's tenant before making changes and fail closed on mismatches.

### HIGH (156)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects from tenant-scoped table `experiments` without constraining by tenant_id (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads metadata for all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. pass tenantId into the function and add WHERE tenant_id = ${tenantId}), or iterate per-tenant in a safe, authorized sweep so you never read other tenants' rows.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting selects from tenant-scoped table `ai_insights` without a tenant filter: `SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50`. This reads multiple tenants' rows.
  - _Fix:_ Scope the sweep to a specific tenant (pass tenantId) or run the sweep per-tenant; add `AND tenant_id = ${tenantId}` or similar validation before operating on returned rows.
- **server/agentic-engines.ts:344** — gatherOperationalData issues a tenant-unscoped query against `heartbeat_logs`: `SELECT COUNT(*) as total_calls, ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` — no tenant_id filter, so it aggregates logs across tenants.
  - _Fix:_ Add tenant scoping (e.g. `WHERE tenant_id = ${tenantId} AND task_name != ...`) or otherwise ensure the call only reads data for the intended tenant.
- **server/agentic-engines.ts:451** — runPredictiveEngine performs `SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10` with no tenant constraint, reading logs across tenants.
  - _Fix:_ Restrict the query by tenant_id (e.g. `WHERE tenant_id = ${tenantId} AND status = 'success'`) or run the pull per-tenant.
- **server/agentic-engines.ts:519** — runOptimizationEngine queries `heartbeat_logs` without tenant scoping: `SELECT task_name, status, COUNT(*) as count, AVG(duration_ms) as avg_duration FROM heartbeat_logs GROUP BY task_name, status ...`. This aggregates data across tenants.
  - _Fix:_ Add `WHERE tenant_id = ${tenantId}` to scope the aggregation to the target tenant, or perform per-tenant aggregation jobs.
- **server/agentic-engines.ts:533** — runOptimizationEngine queries `heartbeat_tasks` with `WHERE enabled = true` but no tenant filter (`SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ORDER BY name`). heartbeat_tasks is tenant-scoped elsewhere; this reads other tenants' task configs.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or otherwise scope by tenant) to avoid leaking/operating on other tenants' task definitions.
- **server/agentic-features.ts:525** — scoreLeads updates tenant-scoped table `lead_enrichments` using `WHERE id = ${lead.id}` without confirming tenant ownership in the WHERE clause. The update can affect rows outside the caller's tenant if IDs collide or are misused.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. `WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}`) to ensure only the intended tenant's row is modified.
- **server/agentic-features.ts:671** — advanceSequence updates outreach_enrollments with `UPDATE outreach_enrollments SET current_step = ${nextStep}, ... WHERE id = ${enrollment.id}` but omits tenant_id in the WHERE clause. Although enrollment was selected under the tenant, the UPDATE should still enforce tenant scoping to avoid accidental cross-tenant writes.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}`) to ensure the write cannot affect rows from other tenants.
- **server/agentic-features.ts:721** — classifyReply updates outreach_enrollments using `WHERE id = ${row.id}` without scoping by tenant (`UPDATE outreach_enrollments SET ... WHERE id = ${row.id}`). This write lacks an explicit tenant_id guard.
  - _Fix:_ Add `AND tenant_id = ${params.tenantId}` to the WHERE clause of the UPDATE so only the caller's tenant row is modified.
- **server/agentic/runs.ts:34** — appendStep updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with no tenant_id constraint. An update-by-id without tenant scoping can modify another tenant's run record.
  - _Fix:_ Include tenant scoping on updates. Change appendStep signature to accept tenantId (or fetch the run and verify tenant ownership) and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure every update to agent_runs always verifies tenant_id.
- **server/agentic/runs.ts:45** — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) and no tenant_id condition, allowing cross-tenant mutation if runId is guessed or misused.
  - _Fix:_ Require and pass tenantId into updateRunState (or fetch/verify ownership) and constrain the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Apply the same pattern to all state-updating helpers.
- **server/agentic/runs.ts:54** — replaceRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) without tenant scoping. This permits updating runs belonging to other tenants.
  - _Fix:_ Constrain replaceRunState with tenant ownership: accept tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or otherwise validate the run's tenant before updating.
- **server/agentic/runs.ts:58** — completeRun updates agent_runs (status/result/completedAt) using only the run id in WHERE (.where(eq(agentRuns.id, runId))) with no tenant_id constraint.
  - _Fix:_ Require tenant scoping on completion. Either read+verify run.tenantId before updating or accept tenantId as an argument and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure the completion path cannot modify another tenant's row.
- **server/agentic/runs.ts:88** — failRun sets a run to failed using only run id in the WHERE clause (.where(eq(agentRuns.id, runId))) and lacks a tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Add tenant verification to failRun. Pass tenantId in or first select the run and confirm ownership, then update with .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:97** — pauseRun updates agent_runs.status/state using only the run id in the WHERE (.where(eq(agentRuns.id, runId))) with no tenant_id constraint, so a caller could affect runs from other tenants.
  - _Fix:_ Require tenant scoping for pauseRun. Either accept tenantId and include it in the WHERE clause (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or validate run ownership before applying updates.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table `agent_wake_schedules` is executed without constraining to a tenant. The statement `UPDATE agent_wake_schedules SET status = 'firing' ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE SKIP LOCKED)` does not include `tenant_id` in the WHERE clause, so rows from any tenant can be claimed/modified by this scheduler run.
  - _Fix:_ Either run the claim per-tenant (pass a tenantId and include `WHERE tenant_id = ${tenantId}` in both the inner SELECT and outer UPDATE) or add an explicit tenant_id predicate to the subquery and outer UPDATE. Ensure every UPDATE/SELECT/DELETE against agent_wake_schedules includes `tenant_id = <caller tenant>` (or equivalent Drizzle eq(table.tenantId, tenantId)).
- **server/auto-asset-capture.ts:184** — Global SELECT on tenant-scoped tables without tenant scoping. The query `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0` does not constrain rows to a tenant_id. The correlated subquery also queries `messages` with no tenant filter.
  - _Fix:_ If this is intended to operate per-tenant, add `WHERE c.tenant_id = ${tenantId}` to the outer query and include `AND tenant_id = c.tenant_id` (or similar) in the subquery. If it must scan all tenants, run it only from an explicit admin context and document/guard that choice; otherwise always include tenant predicates when reading tenant-scoped tables.
- **server/auto-asset-capture.ts:207** — SELECT from tenant-scoped `messages` table without tenant constraint: `SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'` does not include `tenant_id`. This allows reading messages across tenants when conversation ids collide or if other parts of the code supply ids without ownership checks.
  - _Fix:_ Include the tenant predicate (e.g. `AND tenant_id = ${c.tenant_id}`) or join via the conversations table to ensure the conversation belongs to the expected tenant before selecting messages. Ensure every read from `messages` is scoped to the caller's tenant.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge (getWatermark) has no tenant_id constraint — reading a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Include tenant scoping in the query (e.g. add WHERE tenant_id = ${tenantId}) or pass a tenantId into getWatermark and restrict the SELECT to that tenant. Avoid global reads from tenant-scoped tables.
- **server/auto-memorize.ts:68** — UPDATE against agent_knowledge (setWatermark) uses WHERE id = ${existing.rows[0].id} with no tenant_id constraint — an UPDATE on a tenant-scoped table is not validated against the caller's tenant.
  - _Fix:_ When updating tenant-scoped rows, include tenant ownership in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), and require/validate a tenantId parameter for setWatermark so updates cannot affect other tenants' rows.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this INSERT violates the project invariant that every write to tenant-scoped tables must set tenant_id. Even if the invoice_id was just created in the same transaction, absence of an explicit tenant_id on the child row relies on implicit schema choices or FK semantics (which the codebase says are NOT relied upon).
  - _Fix:_ Explicitly set tenant_id when inserting invoice line items, e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...). If invoice_items is intentionally global, document that and ensure any reads/updates that join to invoices always filter by tenant_id. Alternatively, enforce tenant ownership by selecting/locking the parent invoice and/or adding a NOT NULL tenant_id column and application-level checks to guarantee isolation.
- **server/chat-engine.ts:4331** — Call to storage.attachCitations((assistantMsgRow as any).id, collectedCitations) does not pass tenant context. If attachCitations performs INSERT/UPDATE against tenant-scoped tables it must be constrained by tenant_id.
  - _Fix:_ Modify attachCitations to accept and use tenantId (or pass tenantId here). Ensure the implementation applies an application-level filter (e.g. WHERE tenant_id = ?) or Drizzle .where(eq(table.tenantId, tenantId)). Do NOT rely on implicit FK ownership; always include tenant_id in the write filter or validate ownership of the referenced message id before mutating.
- **server/chat-engine.ts:3738** — recordMutation({... , conversationId, personaId: persona?.id, ...}) is recorded without any tenantId parameter. If recordMutation writes to a tenant-scoped audit/mutation table, the operation lacks an explicit tenant constraint.
  - _Fix:_ Change recordMutation to accept tenantId and pass the current tenantId when calling. Inside recordMutation ensure all DB writes include tenant_id filters (Drizzle .where(eq(...)) or explicit param binding). If using a conversationId foreign key, VERIFY ownership (SELECT ... WHERE id = X AND tenant_id = ?) before writing.
- **server/chat-engine.ts:4015** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is called without tenantId. The function likely records tool-call diagnostic entries tied to a conversation but no tenant context is provided.
  - _Fix:_ Pass tenantId explicitly to recordToolCallForStuckDetection (and related stuck-diagnostics APIs). Inside that module, ensure any queries/writes include tenant_id filters or first verify conversation ownership with a tenant-scoped SELECT prior to mutating.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, toolName, !hasError) is invoked with a foreign id (conversationId) but no tenantId/ownership verification. If recordToolExecution writes tenant-scoped rows, there is no explicit tenant constraint.
  - _Fix:_ Include tenantId when calling recordToolExecution or validate that conversationId is owned by the current tenant before recording. All DB writes inside recordToolExecution must include WHERE tenant_id = <tenant> (Drizzle .where(eq(...))). Avoid deriving tenant implicitly from foreign keys without explicit application-level checks.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` without tenant_id constraint (UPDATE commitments SET draft_status = 'open' ... WHERE id = ${row.id}). This can modify rows across tenants if `id` is guessed or collides.
  - _Fix:_ Include the tenant filter in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or verify ownership before performing the mutation. Always scope UPDATE/DELETE statements against tenant-scoped tables to the caller's tenant.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant_id filter (scanning overdue commitments across all tenants).
  - _Fix:_ This cross-tenant SELECT exposes tenant-scoped rows. If this is an intentional admin/ops job, ensure it runs under an explicit admin role and that returned data is redacted appropriately; otherwise add WHERE tenant_id = ${tenantId} or iterate per-tenant so ordinary code paths never perform unscoped reads.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table `credential_vault` without tenant_id constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). This mutates a credential row without verifying the tenant owner.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or fetch-and-verify ownership before updating. All writes to tenant-scoped tables must be owner-scoped to prevent cross-tenant tampering.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table `credential_vault` without tenant_id constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}).
  - _Fix:_ Same fix: scope the UPDATE to the tenant (WHERE id = ${cred.id} AND tenant_id = ${tenantId}) or verify the credential belongs to the tenant before mutating. Ensure all code paths that write tenant-scoped rows include the tenant_id guard.
- **server/data-protection.ts:118** — SELECT on conversations (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) has no tenant_id predicate — reads rows across all tenants.
  - _Fix:_ If the purge is intended to be tenant-scoped, add AND tenant_id = ${tenantId} (or iterate per-tenant). If the job must run globally, ensure it runs under a controlled admin context and that subsequent destructive operations enforce tenant predicates; prefer explicit per-tenant queries to avoid accidental cross-tenant data access.
- **server/data-protection.ts:183** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} has no tenant_id constraint — reading messages for a conversation without verifying tenant ownership in the SELECT.
  - _Fix:_ Add tenant scoping to the query (AND tenant_id = ${tenantId}). Even if the parent conversation was validated earlier, every tenant-scoped table access must include an explicit tenant predicate to enforce isolation.
- **server/data-protection.ts:190** — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} lacks tenant_id filtering — archive rows are read without tenant constraint.
  - _Fix:_ Include tenant predicate (AND tenant_id = ${tenantId}), or join against the conversations table that has tenant_id = ${tenantId} to ensure compaction_archives read is scoped to the caller's tenant.
- **server/data-protection.ts:326** — SELECT ... FROM project_notes WHERE project_id = ${proj.id} is executed without also scoping by tenant_id — reading notes for a project without explicit tenant predicate.
  - _Fix:_ Constrain by tenant (e.g. AND tenant_id = ${tenantId}) or join project_notes to projects (which are already tenant-scoped) and verify p.tenant_id = ${tenantId} in the WHERE clause to ensure the project_notes belong to the caller's tenant.
- **server/data-protection.ts:336** — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} lacks tenant_id predicate — project files are read without explicit tenant scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the query or join to projects with p.tenant_id = ${tenantId} to ensure project_files queries are scoped to the tenant that owns the project.
- **server/data-protection.ts:369** — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} omits tenant_id — counting messages for a conversation without tenant scoping may leak or conflate other tenants' messages.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or conv.tenant_id) in the COUNT query. Always scope message queries by tenant_id even when iterating over tenant-scoped conversation rows obtained earlier.
- **server/delivery-pipeline.ts:195** — updateDeliveryLog performs UPDATE on delivery_logs without scoping by tenant_id (WHERE only checks id). This lets code update another tenant's delivery row by id.
  - _Fix:_ Require and pass the caller tenant into updateDeliveryLog (e.g. updateDeliveryLog(id, updates, tenantId)) and include it in the WHERE clause: .where(and(eq(delivery_logs.id, id), eq(delivery_logs.tenant_id, tenantId))). Alternatively, validate the row's tenant before updating and fail when it doesn't match the caller.
- **server/delivery-pipeline.ts:1049** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with NO tenant filter. This can return another tenant's delivery row when only a payment id is provided.
  - _Fix:_ Add a tenant-scoped variant (e.g. getDeliveryByStripePayment(paymentId, tenantId)) and include eq(delivery_logs.tenant_id, tenantId) in the WHERE clause. If a tenant context isn't available, ensure callers perform an explicit tenant check before using the returned row.
- **server/dfy-intake.ts:93** — submitDfyIntake updates dfy_intake_forms via .where(eq(dfy_intake_forms.id, form.id)) without constraining by tenant_id. An UPDATE by id without tenant scoping can modify another tenant's row if IDs are misused.
  - _Fix:_ Scope the UPDATE to the tenant as well (use .where(and(eq(dfy_intake_forms.id, form.id), eq(dfy_intake_forms.tenant_id, form.tenantId)))). Alternatively require and pass tenantId into submitDfyIntake and use it in the WHERE clause to ensure updates only affect the caller's tenant.
- **server/embeddings.ts:244** — Use of sql.raw() with a request-derived vector literal in vectorSearchMemory (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). The vector value originates from the request/query embedding and is interpolated into raw SQL.
  - _Fix:_ Avoid building SQL by string-interpolating request-derived values. Instead pass the vector as a parameter/bound value (use Drizzle/pg parameter binding or a prepared statement) so the vector is not injected into raw SQL text. Also keep tenant_id filtering (already present) and validate the vector shape before binding (as done with vecLiteral).
- **server/embeddings.ts:252** — Use of sql.raw() with a request-derived vector literal in vectorSearchMemory ORDER BY clause (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This interpolates a request-derived value into raw SQL.
  - _Fix:_ Replace sql.raw interpolation with parameterized binding of the vector value (use Drizzle/pg parameter binding) to avoid string interpolation of request-derived data. Maintain tenant_id constraint and validate vector contents before binding.
- **server/embeddings.ts:559** — Use of sql.raw() with a request-derived vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) AS similarity). The vector is derived from the caller's query embedding and is interpolated into raw SQL.
  - _Fix:_ Do not interpolate the vector into raw SQL text. Use parameterized queries / Drizzle bindings to pass the vector safely (e.g. pass a parameter as $1::vector or equivalent) and continue to validate the embedding shape (vecLiteral).
- **server/embeddings.ts:565** — Use of sql.raw() with a request-derived vector literal in vectorSearchKnowledge ORDER BY clause (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is string-interpolating request-derived data into SQL.
  - _Fix:_ Avoid sql.raw interpolation of request-derived vectors. Use parameterized binding for the vector value or Drizzle native constructs so the vector is passed as data rather than injected SQL text. Ensure tenant_id remains in the WHERE clause (already present).
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' with no tenant_id filter (reads tenant_id but does not constrain by it). This can leak/rehydrate data from all tenants into a single in-memory store.
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant and run the rehydration inside the correct tenant context. If the intent is to rehydrate only a single tenant at boot, require/derive that tenantId and bind it into the query (parameterized). Avoid wide, cross-tenant reads.
- **server/google-drive.ts:561** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) without also constraining by tenant_id. Although the code validated ownership earlier, the write itself lacks an explicit tenant constraint (TOCTOU/race risk).
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Even after validating ownership earlier, include tenant_id in the WHERE to make writes atomic and avoid cross-tenant updates under race conditions.
- **server/health-audit.ts:314** — SELECT from code_proposals executes without constraining tenant_id — reads tenant-scoped rows globally.
  - _Fix:_ If code_proposals is tenant-scoped, add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope the audit to a specific tenant. If this is an intentional global/admin scan, require and validate an explicit admin context and document it; do not leave unconstrained reads against tenant data.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' uses id ANY(...) without any tenant_id constraint — allows modifying proposals across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or join the ids with tenant_id to ensure only rows belonging to the intended tenant are updated. Alternatively, run this archive operation per-tenant with an explicit admin-approved path.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks queries enabled/stuck tasks with no tenant_id restriction — reads tenant-scoped scheduled tasks globally.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, add tenant_id = ${tenantId} to the WHERE clause or otherwise limit the query to an explicit admin-scoped operation. Avoid broad tenant-less reads of task records.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) updates tasks without constraining by tenant_id — can disable tasks across tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${tenantId} (or equivalent) to ensure only tasks for the intended tenant are modified. If this is an admin-wide action, require an explicit admin context and make that explicit in the call path.
- **server/google-workspace.ts:2361** — INSERT INTO presenter_slide_images omits tenant_id (and uses only session_id) — if presenter_slide_images is tenant-scoped, this write will create/modify rows without recording or enforcing tenant ownership.
  - _Fix:_ Include tenant_id in the INSERT values (VALUES (${sessionId}, ${idx}, ..., ${tenantId}) and corresponding column list) or ensure presenter_slide_images is a non-tenant/global table. Prefer explicit tenantId parameterization (use the function’s tenantId) and validate session ownership before writing.
- **server/google-workspace.ts:2383** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} does not include tenant_id, allowing cross-tenant updates of presenter_sessions.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or otherwise verify the session belongs to the calling tenant before updating. Always include tenant_id filter on tenant-scoped UPDATEs to enforce isolation.
- **server/heartbeat.ts:53** — SELECT from tenant-scoped table `research_schedules` without constraining by tenant_id (SELECT * FROM research_schedules ...). This reads rows for all tenants.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE tenant_id = <tenant> AND ...) or iterate per-tenant. If this is a system-wide operation, explicitly enumerate/derive the tenant(s) to operate on and include tenant_id in the WHERE clause (or use a storage helper that enforces tenant context).
- **server/heartbeat.ts:102** — UPDATE on tenant-scoped table `research_schedules` uses WHERE id = ${sched.id} with no tenant_id check (UPDATE research_schedules SET next_run_at = ... WHERE id = ...).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update through a storage helper that enforces the expected tenant context.
- **server/heartbeat.ts:108** — UPDATE on tenant-scoped table `research_schedules` updates last_run_at/next_run_at by id only (no tenant_id constraint).
  - _Fix:_ Add tenant filtering to the WHERE clause (e.g. AND tenant_id = ${sched.tenant_id}) or use a tenant-aware storage API to avoid cross-tenant writes.
- **server/heartbeat.ts:113** — UPDATE on tenant-scoped table `research_schedules` sets next_run_at WHERE id = ${sched.id} without tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the schedule's tenant (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a tenant-asserting helper to perform the update.
- **server/heartbeat.ts:121** — In recovery path: UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id filter on tenant-scoped table.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or call a storage-layer function that enforces tenant ownership before updating.
- **server/heartbeat.ts:378** — SELECT from tenant-scoped table `event_log` without a tenant_id constraint (SELECT * FROM event_log WHERE status = 'pending' ...). This returns events across all tenants.
  - _Fix:_ Query pending events per-tenant (e.g. WHERE status='pending' AND tenant_id = <tenant>) or enumerate tenant ids and SELECT with tenant_id, or use a tenant-aware storage API. Avoid global SELECTs on tenant-scoped tables.
- **server/heartbeat.ts:266** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — updating a tenant-scoped table row by id without constraining tenant_id. This can modify another tenant's row if mis-bound.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use storage.updateHeartbeatTask / a tenant-aware API that asserts the task belongs to the expected tenant.
- **server/heartbeat.ts:270** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — no tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or perform updates through a tenant-asserting storage helper to ensure isolation.
- **server/heartbeat.ts:2628** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint — writing to a tenant-scoped table by id only.
  - _Fix:_ Constrain the UPDATE to the task's tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage function to set approval_status so the write cannot affect other tenants' rows.
- **server/heartbeat.ts:3080** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) lacks a tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call a storage-layer update that enforces tenant ownership before mutating approval_status.
- **server/internal-resolver.ts:184** — finalizeEvent called WITHOUT scoping tenant_id — UPDATE event_log is executed with no tenant filter when event row lacked tenant_id, allowing a write to event_log without tenant constraint.
  - _Fix:_ Derive and pass the event's tenant_id into finalizeEvent (or add a safe guard inside finalizeEvent) so the UPDATE includes AND tenant_id = <tenant>. If the event truly has no tenant, handle that case explicitly (e.g. write to a separate admin/audit table) instead of doing an un-scoped UPDATE.
- **server/internal-resolver.ts:229** — finalizeEvent called WITHOUT tenantId — performs UPDATE on event_log without adding tenant_id constraint, allowing cross-tenant modification of event_log rows.
  - _Fix:_ Lookup the event's tenant_id (SELECT tenant_id FROM event_log WHERE id = ...) and pass it to finalizeEvent so the UPDATE includes AND tenant_id = <tenant>. Alternatively change finalizeEvent to always derive and enforce the tenant before updating.
- **server/internal-resolver.ts:259** — finalizeEvent invoked with no tenantId in default branch — UPDATE on event_log executed without tenant constraint, permitting cross-tenant state transitions.
  - _Fix:_ Ensure every finalizeEvent invocation passes a validated tenantId (derive from the event row) or make finalizeEvent fetch and enforce the tenant_id internally so the WHERE clause always contains tenant_id = <tenant>.
- **server/job-queue.ts:98** — claimDueJobs executes UPDATE ... FROM agent_jobs and inner SELECT over agent_jobs without any tenant_id filtering — mass claim/transition of jobs could affect rows across tenants.
  - _Fix:_ Scope claims to a tenant (e.g. pass tenantId into claimDueJobs and add WHERE tenant_id = ${tenantId}) or document and enforce that the caller is an operator process authorized to act across tenants. Prefer per-tenant claim paths where tenant isolation is required.
- **server/job-queue.ts:134** — reclaimExpiredLeases: UPDATE agent_jobs ... WHERE status='running' AND lease_until < NOW() — no tenant_id constraint; this will mutate running jobs across all tenants.
  - _Fix:_ Add tenant scoping (accept an optional tenantId parameter and include AND tenant_id = ${tenantId}) or ensure the operation runs under an operator/admin context that is explicitly authorized to sweep all tenants. If the operation must be tenant-scoped, require an explicit tenantId argument.
- **server/job-queue.ts:148** — reclaimExpiredLeases second UPDATE re-queues expired running jobs without any tenant_id filter — affects agent_jobs for all tenants.
  - _Fix:_ Same as above: add an explicit tenant filter or require an admin-level guard. Ensure application-level tenant scoping is applied before mutating tenant-scoped tables.
- **server/job-queue.ts:177** — completeJob updates agent_jobs (SET status='succeeded' ...) by id without constraining tenant_id — a caller could mark another tenant's job succeeded by id.
  - _Fix:_ Require tenantId as a parameter and include AND tenant_id = ${tenantId} in the WHERE clause, or validate ownership of the job id before updating.
- **server/job-queue.ts:237** — failJob UPDATE that marks a job failed_terminal writes to agent_jobs WHERE id = ${id} ... with no tenant_id constraint — allows cross-tenant mutation of job state.
  - _Fix:_ Require and validate tenantId and add AND tenant_id = ${tenantId} to the UPDATE WHERE clause, or perform an ownership check prior to updating.
- **server/job-queue.ts:257** — failJob UPDATE that re-queues a failed job (sets status='pending', next_run_at=...) lacks tenant_id filtering — affects agent_jobs across tenants.
  - _Fix:_ Scope the UPDATE by tenant_id (require tenantId parameter) or otherwise ensure only the owning tenant's rows are touched.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} without tenant_id constraint — an operator or caller could cancel jobs across tenants by id.
  - _Fix:_ Add tenantId parameter and include AND tenant_id = ${tenantId} in the WHERE, or validate job ownership before mutating.
- **server/job-queue.ts:283** — retryJob updates agent_jobs to reset status/attempts WHERE id = ${id} with no tenant_id constraint — permits cross-tenant job resets.
  - _Fix:_ Require tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or verify the job belongs to the tenant before changing it.
- **server/lib/agent-trace.ts:129** — UPDATE to tenant-scoped table `agent_trace_spans` uses WHERE span_id = ${spanId} with NO tenant_id predicate. This allows cross-tenant updates (closing a span) based only on span_id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Change persistSpanClose to accept or derive the tenantId (or look it up from the span open context) and propagate it from the callers (withRootSpan/withSpan/withSpanOrRoot). Verify callers pass the correct tenant and add an assert to fail closed if tenantId is missing.
- **server/lib/bwb-job-progress.ts:141** — UPDATE on tenant-scoped table `video_jobs` performed via .where(scope(jobId, ...)) can omit tenant_id when BWB_TENANT_ID/env tenant is missing; scope() will then build a WHERE that only pins jobId, allowing cross-tenant writes.
  - _Fix:_ Ensure tenant scoping is mandatory for these mutating queries. Either require the tenantId be passed explicitly to the API (and assert it), or have scope() throw/return an error when envTenantId is missing. Replace implicit jobId-only updates with WHERE job_id = ... AND tenant_id = <tenant> and fail closed if tenant is unknown.
- **server/lib/bwb-job-progress.ts:166** — UPDATE on tenant-scoped table `video_jobs` (updateBwbChapters) uses .where(scope(jobId,...)) which may omit tenant_id when env tenant is not set, enabling cross-tenant updates keyed only by jobId.
  - _Fix:_ Require/validate tenantId before performing the update and include tenant_id in the WHERE clause. Make the tenant binding explicit (pass tenantId into the function) or make scope() fail when BWB_TENANT_ID is not present.
- **server/lib/bwb-job-progress.ts:203** — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope may omit tenant_id (env threading missing), allowing a cross-tenant read/update to be performed based only on jobId.
  - _Fix:_ Include tenant_id in the WHERE (WHERE job_id = ... AND tenant_id = ...) or require and validate tenantId is present before issuing the query. If tenant is not available, fail or no-op rather than performing an unscoped cross-tenant operation.
- **server/lib/bwb-job-progress.ts:227** — UPDATE on tenant-scoped table `video_jobs` (bumpBwbHeartbeat) uses .where(scope(jobId,...)) which can omit tenant_id when env tenant is missing, leading to tenant-ambiguous writes.
  - _Fix:_ Make tenant scoping mandatory for heartbeat writes. Pass tenantId explicitly or have scope() assert/fail when tenant is not available; include tenant_id in the WHERE clause to enforce isolation.
- **server/lib/bwb-job-progress.ts:245** — UPDATE on tenant-scoped table `video_jobs` (failBwbJob) uses .where(scope(jobId,...)) and can omit tenant_id when BWB_TENANT_ID is not set, allowing a failure marker to be applied across tenants based only on jobId.
  - _Fix:_ Require tenantId for fail writes and include tenant_id in the WHERE clause. Change the code to accept an explicit tenantId or make scope() throw/stop when envTenantId is missing so unscoped updates cannot occur.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from projects groups by tenant_id but does not constrain the query to a specific tenant (no WHERE tenant_id = ...). This reads a tenant-scoped table across all tenants.
  - _Fix:_ If this routine should operate per-tenant, add an explicit WHERE tenant_id = ${tenantId} (or iterate per-target-tenant) so rows for other tenants cannot be read. If the cross-tenant aggregation is intentional, make the admin-only nature explicit and audit callers so it cannot run in a tenant-scoped context.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT id FROM project_files WHERE project_id = ${newProjectId} ... reads project_files without constraining by tenant_id. If project_files is tenant-scoped, this can return rows belonging to other tenants.
  - _Fix:_ Ensure reads against project_files include tenant scoping (e.g. AND tenant_id = ${tenantId}) or otherwise validate ownership of project_id before reading. Prefer joining via a tenant-scoped parent check or include tenant_id column on project_files and filter it.
- **server/lib/ideabrowser-backfill.ts:115** — SELECT id FROM project_notes WHERE project_id = ${newProjectId} ... reads project_notes without constraining by tenant_id. If project_notes is tenant-scoped, this can surface other tenants' rows.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. AND tenant_id = ${tenantId}) or validate ownership of the referenced project_id before querying project_notes. If project_notes lacks tenant_id, add/maintain that column and enforce it on reads/writes.
- **server/lib/inbox-ingest.ts:536** — Query JOINs inbox_classifications c to inbox_messages m but only c is filtered by c.tenant_id = ${tenantId}; the joined inbox_messages rows (m.*) are read without an explicit tenant_id constraint. Without RLS/foreign-key enforcement this can expose or mix other tenants' inbox_messages.
  - _Fix:_ Constrain the joined inbox_messages with the same tenant filter (e.g. JOIN inbox_messages m ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId}) or add WHERE m.tenant_id = ${tenantId}. Always ensure every read from tenant-scoped tables includes an explicit tenant_id constraint.
- **server/lib/scheduled-post-runner.ts:319** — A Postgres array literal string (arrLit) is built by concatenating caller-supplied platform names and then embedded into the SQL VALUES clause. Constructing SQL array literals by string concatenation from request-derived input risks injection/formatting errors and bypasses safer parameterized array binding.
  - _Fix:_ Stop constructing a text[] literal by hand. Use parameterized array binding or Drizzle's array/JSON support so the DB driver handles escaping and typing. Options: pass the JS array as a single parameter and cast (e.g. VALUES ($1::text[]) with [lowered]) or build the array expression with sql.join of parameterized elements (sql`ARRAY[${sql.join(lowered.map(p => sql`${p}`), sql`, `)}]::text[]`). Alternatively use Drizzle's insert API and supply the platforms field as an array (let Drizzle/pg handle the conversion).
- **server/lib/system-probes.ts:163** — SELECT against tenant-scoped table `messages` (messagesTable) is executed without any tenant_id constraint.
  - _Fix:_ Constrain the query to the caller's tenant. E.g. use the current tenant context or accept tenantId and add WHERE tenant_id = ${tenantId} (or Drizzle: .where(eq(messagesTable.tenantId, tenantId))). If the intent is a global system-wide count, make that explicit (document/rename) and ensure it's allowed by policy; otherwise scope to the tenant to preserve isolation.
- **server/martech-bundle.ts:325** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the row id in the WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) and does not constrain by tenant_id.
  - _Fix:_ Enforce ownership in the UPDATE by adding the tenant filter. For example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Alternatively, always include tenantId in the WHERE (or re-fetch/validate the row's tenant before updating) to guarantee you never update another tenant's row.
- **server/memory-graph.ts:197** — SELECT against memory_links to check existence does not constrain by tenant (no WHERE tenant_id = ${tenantId}). This can leak or act on cross-tenant links.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add AND tenant_id = ${tenantId}), or join to memory_entries/memory_categories and verify ownership of both endpoints before checking/creating links.
- **server/memory-graph.ts:141** — UPDATE memory_categories SET memory_count uses a subquery counting memory_entries but the subquery does not filter by tenant_id, and the outer UPDATE also lacks a tenant_id WHERE clause. Both read and write touch tenant-scoped tables without tenant scoping.
  - _Fix:_ Scope the subquery and the UPDATE to the tenant: e.g. SET memory_count = (SELECT count(*)::int FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status = 'active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks a tenant_id filter. This updates a tenant-scoped table without verifying the row belongs to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or perform an ownership check before updating.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} is executed without a tenant_id constraint. This writes to a tenant-scoped table without verifying ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or ensure storage layer validates ownership before updating.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in the 'update' flow) lacks a tenant_id filter, allowing cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or verify ownership prior to performing the update.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} is executed without a tenant_id WHERE clause although the contradiction rows were selected by tenant. The UPDATE could modify rows across tenants if ids collide.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) to ensure only the intended tenant's rows are modified.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table project_files does not include tenant_id. The code asserts project ownership via assertProjectInTenant(options.projectId, options.tenantId) but the INSERT omits tenant_id, violating the invariant that every INSERT into a tenant-scoped table must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT column list and pass the caller's tenant (e.g. ${options.tenantId}). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep the prior assertProjectInTenant check, but do not rely on it as a substitute for writing tenant_id.
- **server/mpeg-engine.ts:1417** — INSERT into tenant-scoped table project_files (in produceVideoParallel) omits tenant_id. Even though ownership is verified via assertProjectInTenant(options.projectId, options.tenantId), the INSERT itself does not set tenant_id, violating the requirement that every INSERT include tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT columns and bind the caller's tenant value (e.g. ${options.tenantId}). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Maintain the ownership assertion, but always write tenant_id on INSERT.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions without tenant_id constraint (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = TRUE) or iterate tenants explicitly from a safe, audited tenant-list. Avoid broad, cross-tenant reads from tenant-scoped tables in multi-tenant deployments.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' — no tenant filter. This enumerates tenant rows for a tenant-scoped table.
  - _Fix:_ Add tenant scoping (e.g. WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or otherwise fetch only the tenant ids you explicitly intend to operate on after validating caller privileges. Avoid unbounded selects on oauth_subscriptions.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — unscoped read across all tenants.
  - _Fix:_ If this operation is intended to act per-tenant, accept/resolve a tenantId parameter and scope the SELECT by tenant_id = ${tenantId}. If the process must iterate all tenants, implement a secure enumeration of tenant IDs from a trusted source and ensure operations for each tenant are executed with explicit tenant scoping and proper authorization/audit.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — unscoped read across all tenants.
  - _Fix:_ Avoid unbounded selects on tenant-scoped tables. Either require an explicit tenantId or use a vetted tenant enumeration mechanism, and always include tenant_id in subsequent queries/updates (e.g. WHERE provider = ${provider} AND tenant_id = ${tenantId}).
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(${lockKey}) to inject a computed advisory-lock key into SQL: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). sql.raw() is being used with a value derived from caller-provided inputs (ledgerKey / tenantId) — risk of SQL injection or unintended SQL literal injection.
  - _Fix:_ Avoid sql.raw() with request-derived values. Compute a numeric lock key server-side and pass it as a parameterized value (e.g. sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`) or use parameter binding supported by the DB library. Ensure ledgerKey is hashed and the numeric value is bound as a parameter rather than injected as raw SQL text.
- **server/pace-control.ts:85** — checkPace runs a COUNT(*) FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter} with no tenant_id constraint. If heartbeat_logs is tenant-scoped, this returns counts across tenants and can leak usage metrics.
  - _Fix:_ Add tenant scoping (accept a tenantId or otherwise determine tenant context) and include WHERE tenant_id = ${tenantId} in the query. If the design intentionally aggregates cross-tenant telemetry, isolate that code path, restrict access to operators, and audit the operation.
- **server/pace-control.ts:140** — getPaceSnapshot SELECTs heartbeat_logs over a time window (COUNT and per-persona GROUP BY) without any tenant_id filter — potential cross-tenant data exposure if heartbeat_logs is tenant-scoped.
  - _Fix:_ Require a tenantId parameter or explicitly document and protect any operator-only cross-tenant telemetry endpoints. Prefer adding WHERE tenant_id = ${tenantId} for per-tenant snapshots and ensure only authorized callers can request global snapshots.
- **server/personality-files.ts:8** — Table personality_files defines tenant_id INTEGER NOT NULL DEFAULT 1. The DEFAULT allows inserts that omit tenant_id to be attributed to tenant 1, breaking tenant isolation and letting writes land in the wrong tenant context.
  - _Fix:_ Remove the DEFAULT and require tenant_id to be supplied by callers (tenant_id INTEGER NOT NULL with no default). Enforce at the application boundary that every INSERT/UPSERT passes the tenantId explicitly. Add code reviews/linters to prevent schema defaults that imply a tenant.
- **server/plan-executor.ts:775** — resumeStuckPlans performs a global UPDATE to flip all 'executing' plans older than the stale threshold back to 'approved' with no tenant_id filtering. This will touch other tenants' rows without per-tenant scoping.
  - _Fix:_ Run recovery per-tenant or include an explicit tenant filter (e.g. loop tenants or accept an admin tenantId and include WHERE tenant_id = ${tenantId}). Document and enforce that global maintenance operations either run with an explicit admin tenant context or iterate per-tenant while honoring isolation.
- **server/plan-executor.ts:784** — resumeStuckPlans selects pending plans via SELECT id FROM plans WHERE status = 'approved' ... with NO tenant filter, then schedules executePlan on each id. That will cause executePlan to operate on plan ids from any tenant (and other functions in this module lack tenant guards).
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'approved') or iterate tenants and kick per-tenant pending plans. Never select pending work across all tenants without explicitly scoping to the caller/admin tenant context.
- **server/plan-rollout-simulator.ts:240** — The persistence INSERT nests sql`` with JSON.stringify of request-derived data: ${sql`${JSON.stringify(steps)}::jsonb`} and ${sql`${JSON.stringify(weak)}::jsonb`}. Using nested/interpolated sql like this with user/agent-provided JSON risks injection or improper binding and bypasses safe parameter binding semantics.
  - _Fix:_ Avoid embedding raw SQL fragments built from request data. Use proper parameter binding for JSONB values (pass JSON as a parameter and let the driver bind it, e.g. VALUES (..., ${JSON.stringify(steps)}, ...) and cast safely on the server side or use to_jsonb($1) with parameterized queries). Ensure the JSON is passed as a bound parameter (not inlined into a raw SQL fragment) and always include tenant_id in the INSERT (already present) and enforce parameterization to eliminate injection risk.
- **server/process-governor.ts:121** — Subqueries counting heartbeat_logs (e.g. SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...) do not constrain by tenant_id, allowing cross-tenant log counts/exfiltration.
  - _Fix:_ Add tenant scoping to the subqueries, e.g. AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(table.tenant_id, tenantId))) so counts only include this tenant's heartbeat_logs.
- **server/process-governor.ts:198** — Query computing average response time reads heartbeat_logs without tenant scoping (SELECT AVG(duration_ms) ... FROM heartbeat_logs WHERE ...), exposing cross-tenant telemetry.
  - _Fix:_ Scope the query by tenant: add AND tenant_id = ${tenantId} to the WHERE clause so only this tenant's logs are measured.
- **server/process-governor.ts:240** — Cascading-failures check counts distinct persona_id in heartbeat_logs but omits tenant filter (SELECT COUNT(DISTINCT persona_id) ... FROM heartbeat_logs WHERE ...). This can mix tenants' persona failures.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}) so the metric only considers the tenant's heartbeat_logs.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW() ... WHERE id = ${rule.id} — missing tenant_id in WHERE, allowing trigger counts to be updated across tenants if id collides or if ids are globally unique but should still be tenant-scoped.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${rule.id} AND tenant_id = ${tenantId} (or pass a safeTenantId). Ensure all mutating queries include tenant_id checks.
- **server/project-brain.ts:332** — Backfill query selects projects/joined conversations across the DB: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... — no tenant filter. This enumerates projects from all tenants.
  - _Fix:_ Scope discovery by tenant (e.g. WHERE p.tenant_id = ${tenantId}) or make the routine explicitly accept a tenant-scoped parameter and only operate on that tenant. Do not run cross-tenant scans without strong admin authorization and explicit flagging.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} — no tenant constraint on conversations read during backfill, enabling cross-tenant conversation scanning.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or ensure proj was resolved to a tenant and pass that tenantId into this query to restrict results to the tenant's conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — messages read without tenant filter, permitting reading messages from conversations potentially belonging to other tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${tenantId}) or otherwise verify conversation/ project ownership before fetching messages. Functions that process tenant data must accept tenantId and validate ownership before reads.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` omits tenant filter — the UPDATE uses `WHERE id = ${storedId}` without `AND tenant_id = ${input.tenantId}`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: `UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId}` (or use drizzle's .where(eq(memory_entries.tenant_id, input.tenantId))). Alternatively, re-query/validate the row ownership before updating and always include tenant_id in the WHERE clause to prevent cross-tenant writes.
- **server/routes.ts:765** — Unscoped SELECT from tenant-scoped table `projects` (JOIN tenants) in backfillProjectDriveFolders — no tenant filter. This enumerates projects across all tenants.
  - _Fix:_ Limit the backfill to a single tenant at a time (iterate tenant list and run per-tenant queries using WHERE p.tenant_id = <tid>), or otherwise restrict to an explicit admin-only maintenance path that enforces extra safeguards and auditable scope.
- **server/routes.ts:6485** — Import handler calls storage.createMemoryEntry({ ...rest, personaId: ... }) without supplying tenantId. Memory entries are tenant-scoped and must be created with an explicit tenant_id.
  - _Fix:_ Pass the correct tenantId to createMemoryEntry (e.g. await storage.createMemoryEntry({ ...rest, personaId: rest.personaId || null, tenantId })). If importing data across tenants, require and validate explicit tenant assignment and/or require admin confirmation and explicit mapping.
- **server/routes.ts:6495** — Import handler calls storage.createKnowledge({ ...rest, personaId: ... }) without supplying tenantId. Knowledge rows are tenant-scoped elsewhere in the app (other code passes tenantId), so omitting tenantId risks creating tenant-less or cross-tenant rows.
  - _Fix:_ Provide tenantId explicitly when creating knowledge entries (e.g. await storage.createKnowledge({ ...rest, personaId: rest.personaId || null, tenantId })). Validate or map imported items to the intended tenant.
- **server/routes.ts:6505** — Import handler calls storage.createConversation(rest) without an explicit tenantId. Conversations are tenant-scoped and other code paths require tenantId; creating conversations without tenant_id can break isolation.
  - _Fix:_ Ensure createConversation is called with an explicit tenantId (e.g. await storage.createConversation({ ...rest, tenantId })). For import flows, require the admin to specify target tenant mapping and validate ownership.
- **server/routes.ts:6511** — Import handler calls storage.createMessage({ ...msgRest, conversationId: newConv.id }) without passing tenantId. Message rows are tenant-scoped elsewhere and must be persisted with tenant_id set explicitly.
  - _Fix:_ Pass tenantId when creating messages (e.g. await storage.createMessage({ ...msgRest, conversationId: newConv.id, tenantId })). If storage.createMessage relies on conversationId to infer tenant, make that explicit and document/verify that inference; otherwise require the caller to supply tenantId and validate it matches the conversation's tenant.
- **server/routes/admin.ts:701** — Query selects from tool_performance without constraining by tenant_id. If tool_performance is tenant-scoped this returns cross-tenant data.
  - _Fix:_ If tool_performance is tenant-scoped, add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise constrain the query to the caller's tenant. If the intent is to show global metrics only to platform admins, ensure the table is truly global and document that global-read behavior and gate the route with requirePlatformAdmin (and/or ADMIN_TENANT_ID) explicitly.
- **server/routes/admin.ts:719** — Query against ai_insights (SELECT ... FROM ai_insights WHERE status = 'new' ...) omits tenant_id filtering. ai_insights is likely tenant-scoped, so this can expose other tenants' insights.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND ...). If cross-tenant visibility is intentional, make the table explicitely global and require platform-admin-only access with an auditable justification.
- **server/routes/admin.ts:730** — Query reads heartbeat_tasks without a tenant_id constraint (SELECT ... FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3). heartbeat_tasks is used elsewhere as tenant-scoped.
  - _Fix:_ Restrict the query with WHERE tenant_id = ${tenantId} (or otherwise scope to the caller's tenant). If a platform-global view is required, explicitly document and enforce platform-admin-only access and confirm the schema is global.
- **server/routes/admin.ts:741** — Query selecting stuck pending ai_insights rows (WHERE status = 'applied' AND ...) lacks tenant_id filtering and can leak cross-tenant data.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) in the WHERE clause, or if the intent is to scan across tenants only for platform admins, make that explicit (global table design or admin-only gating) and audit that decision.
- **server/routes/conversations.ts:97** — UPDATE on tenant-scoped table 'conversations' performed without constraining by tenant_id (WHERE clause lacks tenant scope).
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}; or use storage/update helper that enforces tenant ownership before mutating.
- **server/routes/conversations.ts:98** — SELECT from 'project_conversations' performed without tenant constraint — reads tenant-scoped table without filtering by tenant_id.
  - _Fix:_ Add tenant scoping to the query, e.g. SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}; so the lookup is limited to the caller's tenant.
- **server/routes/inbox-notifications.ts:101** — UPDATE on inbox_messages performed without tenant_id constraint: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` (no tenant predicate). This allows a caller who can control `id` (or trigger this path) to modify messages belonging to other tenants.
  - _Fix:_ Include the tenant predicate in the UPDATE (and/or verify ownership before mutating). For example: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`. Alternatively, use a storage-layer helper that accepts tenantId and enforces the tenant-scoped WHERE clause, and always validate message ownership before making destructive updates.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions executed without a tenant_id constraint — reads tenant-scoped table globally.
  - _Fix:_ Constrain the query to the intended tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise limit the scope (use Drizzle .where(eq(researchSessions.tenantId, tenantId))). Avoid global scans of tenant-scoped tables.
- **server/seed.ts:2116** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter — reads research_programs across all tenants.
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or use Drizzle .where(eq(researchPrograms.tenantId, tenantId)). Ensure all reads against tenant-scoped tables are scoped to the caller tenant.
- **server/seed.ts:2466** — SELECT DISTINCT ... FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations ...) performs cross-tenant reads/joins without any tenant_id constraints on conversations or project_conversations.
  - _Fix:_ Restrict both sides of the query to the same tenant (e.g. WHERE c.tenant_id = ${tenantId} AND pc.tenant_id = ${tenantId}) or otherwise verify ownership before operating. Never query tenant-scoped tables without verifying tenant_id.
- **server/seed.ts:3294** — db.select().from(heartbeatTasks) reads heartbeat tasks without filtering by tenant_id — returns records across tenants.
  - _Fix:_ Use a tenant-scoped query (e.g. db.select().from(heartbeatTasks).where(eq(heartbeatTasks.tenantId, tenantId))) or otherwise restrict to the admin/system tenant when intentional. Ensure all Drizzle selects against tenant-scoped tables include an explicit tenant filter.
- **server/seed.ts:3294** — SELECT on tenant-scoped table `heartbeat_tasks` is executed without any tenant_id constraint (db.select().from(heartbeatTasks)).
  - _Fix:_ Restrict reads to the caller tenant. e.g. use a WHERE tenant_id = <tenant> (or Drizzle .where(eq(heartbeatTasks.tenantId, tenantId))). Avoid selecting across all tenants from tenant-scoped tables.
- **server/seed.ts:3961** — UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id} omits a tenant_id check. agent_knowledge is tenant-scoped; updating by id alone can modify a row that does not belong to the intended tenant context.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = <tenant> or .where(and(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId)) in Drizzle). Always enforce tenant ownership on writes even when the row id was sourced from a prior tenant-filtered SELECT.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` may execute without a tenant constraint. detectStagnation builds a conditions array where tenant filter is optional and then calls db.select().from(experiments).where(...). If tenantId is omitted the query will return rows across tenants.
  - _Fix:_ Always include a tenant constraint. Make tenantId required for this function or fail-closed when tenantId is not provided, and add eq(experiments.tenantId, tenantId) to the WHERE (or otherwise ensure the caller's tenant is applied). Avoid queries that can run unscoped across tenant rows.
- **server/self-improvement.ts:521** — SELECT from tenant-scoped table `experiments` in getExperimentHistory can run unscoped when tenantId is not provided. The function returns db.select().from(experiments)... when no conditions are supplied, causing cross-tenant reads.
  - _Fix:_ Require a tenantId or fail-closed when tenantId is not supplied. If supporting optional category filtering, still enforce tenant scoping by default (add eq(experiments.tenantId, tenantId) to conditions or throw when tenantId is missing).
- **server/storage.ts:510** — SELECT all rows from tenant-scoped table `memory_entries` with no tenant filter (getAllMemoriesForBackup) — returns data across tenants
  - _Fix:_ Require caller to provide a tenantId or advertise this function as an admin-only/unscoped export and gate it behind a privileged check. Prefer providing a per-tenant export API or iterating tenants explicitly so ordinary callers cannot read all tenants' memories.
- **server/storage.ts:878** — getMemoryStats falls back to selecting all rows from tenant-scoped `memory_entries` (and later `agent_knowledge`) when tenantId is not provided; this returns cross-tenant data
  - _Fix:_ Require tenantId (fail-closed) or ensure tenantScope(eq(...tenantId)) is applied before any select. If global stats are needed, make the function explicit and restrict it to admin callers.
- **server/storage.ts:916** — getHeartbeatTasks returns all heartbeat_tasks when tenantId is omitted (no tenant filter) — tenant-scoped table can be read platform-wide
  - _Fix:_ Require tenantId or add an explicit unscoped variant with loud audit logging. Prefer fail-closed: throw if tenantId is not provided from an HTTP handler, or apply tenantScope(heartbeatTasks.tenantId, tenantId) consistently.
- **server/storage.ts:823** — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` with no tenant filter (reads all tenants' memories)
  - _Fix:_ Require tenantId or iterate per-tenant in maintenance worker. If this function is part of a global background job, ensure it runs under a trusted admin context and is documented; otherwise apply tenant scoping.
- **server/storage.ts:828** — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` with no tenant filter (reads across tenants)
  - _Fix:_ Require tenantId in the API or process embeddings per-tenant. If this is an intentional global worker, ensure it's executed by a privileged job and make that explicit; otherwise apply tenant scoping.
- **server/storage.ts:585** — INSERT into tenant-scoped table `conversation_facts` (db.insert(conversationFacts).values(data)) without explicit validation of data.tenantId/ownership
  - _Fix:_ Require and validate tenantId on the provided data (or resolve the conversation's tenant and assert match) before inserting. Fail closed if tenant ownership cannot be proven.
- **server/tool-learning.ts:129** — SELECT on tenant-scoped table `custom_tools` performed without restricting by tenant_id (checks `eq(customTools.name, parsed.name)` only). This can leak cross-tenant existence information and cause incorrect conflicts.
  - _Fix:_ Constrain the SELECT to the caller's tenant, e.g. add eq(customTools.tenantId, tenantId) to the WHERE clause (or otherwise namespace the lookup to the validated tenant).
- **server/tool-learning.ts:155** — SELECT by id against tenant-scoped table `custom_tools` performed without tenant_id filter (`where(eq(customTools.id, inserted.id))`). Reading tenant-scoped rows must be scoped to the tenant.
  - _Fix:_ Include tenant scope in the query, e.g. where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))) or pass/verify the tenant before selecting the row.
- **server/tools.ts:2878** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) lacks an explicit tenant_id column/value.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})) or otherwise ensure the INSERT is scoped to the caller's tenant. If deriving tenant from the referenced project, assert and use that tenantId in the INSERT so the row cannot be created without tenant scope.
- **server/tools.ts:2981** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) RETURNING * omits tenant_id.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) ) and ensure project ownership was validated before inserting. Never rely on implicit FK semantics — write tenant_id explicitly on insert.
- **server/tools.ts:2995** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) RETURNING * omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})) and ensure the project ownership check runs prior to insert. Always persist tenant_id explicitly for tenant-scoped rows.
- **server/tools.ts:3020** — INSERT into tenant-scoped table 'project_conversations' (link_conversation path) does not set tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id.
  - _Fix:_ Set tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})) and/or derive tenantId from verified project ownership before inserting. Ensure every write to tenant-scoped tables includes an explicit tenant_id.
- **server/tools.ts:8891** — UPDATE against proposed_skills omits a tenant_id constraint in the WHERE clause. The code updates status/review fields using WHERE eq(proposedSkills.id, row.id) only, allowing modification of a row by id without scoping to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add a tenant equality predicate to the WHERE clause (for example: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) and ensure tid is the verified tenantId in scope before performing the update.
- **server/tools.ts:8916** — UPDATE against proposed_skills omits a tenant_id constraint in the WHERE clause. The code promotes a proposal and sets reviewed/promoted fields via WHERE eq(proposedSkills.id, row.id) only, lacking tenant scoping.
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure the tid used is the authenticated caller's tenant id so the update cannot affect rows from other tenants.
- **server/tools.ts:8933** — UPDATE against proposed_skills omits a tenant_id constraint in the WHERE clause. The code marks a proposal as rejected using WHERE eq(proposedSkills.id, row.id) only, without verifying tenant ownership in the SQL.
  - _Fix:_ Add an explicit tenant equality condition to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) and ensure tid is the verified tenant id; always scope UPDATE/DELETE/SELECT on tenant-scoped tables to tenant_id.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table knowledge_triples omits tenant_id in WHERE clause (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}). This allows mutating rows by id across tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add tenant_id to the WHERE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(table.tenantId, tenantId)) so the mutation is strictly scoped to the calling tenant. Also consider verifying ownership before updating as an extra defense-in-depth.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table 'provider_keys' executed without tenant_id constraint. This query enumerates provider API keys across all tenants (no WHERE tenant_id = ...), potentially exposing cross-tenant secrets even though branch is guarded by tenantId === 1.
  - _Fix:_ Restrict the query to a tenant scope (e.g. add WHERE tenant_id = ${tenantId}) or otherwise ensure explicit, auditable owner-admin authorization before enumerating cross-tenant secrets. If the intent is to run cross-tenant for platform admin, make the authorization explicit and log/justify the global access; avoid returning raw key material and prefer aggregated metrics or per-tenant scoped results.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM 'provider_keys' executed without tenant_id constraint. This aggregates provider configuration across all tenants (no WHERE tenant_id = ...), leaking multi-tenant configuration information to the caller even though the branch checks tenantId === 1.
  - _Fix:_ Add a tenant_id filter (e.g. WHERE tenant_id = ${tenantId}) when the intent is per-tenant reporting. If a global admin metric is required, perform this only in an explicitly authorized admin path with clear justification and ensure results cannot leak sensitive per-tenant details; prefer aggregated/admin-only dashboards rather than ad-hoc tool queries.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} executed with no tenant filter. This reads a tenant-scoped table (conversations) by id without constraining to the expected tenant, which can leak/expose cross-tenant info.
  - _Fix:_ Constrain the lookup to the claimed tenant (e.g. WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1) or otherwise validate ownership using a query that includes tenant_id. If you need to validate the mapping, pass tenantId into the check and verify existence in a single tenant-scoped query to avoid global lookups.
- **server/video-job-runner.ts:236** — recoverStaleVideoJobs performs db.update(videoJobs) to mark stale jobs without any tenant_id constraint (updates all matching rows across tenants). This mutates tenant-scoped video job rows globally.
  - _Fix:_ Scope recovery updates per-tenant or run this maintenance under an explicit operator/admin context with clear authorization and auditing. Alternatively, iterate per-tenant (or accept tenantId parameter) and include AND tenant_id = ${tenantId} in the WHERE clause so rows for other tenants are not updated inadvertently.
- **server/wiring-invariants.ts:244** — Query against tenant-scoped table `tool_performance` without any tenant_id constraint (SELECT count(*) FROM tool_performance WHERE last_success_at > ...). This reads across all tenants.
  - _Fix:_ If the intent is per-tenant analysis, run the check per-tenant and include WHERE tenant_id = :tenantId (use parameterized queries / drizzle's sql`` with eq(...)). If this is meant to be a global admin-only aggregate, make that explicit (use ADMIN_TENANT_ID or an explicit admin-only path) and document/guard the call; still prefer explicit tenant scoping to avoid accidental cross-tenant reads.
- **server/wiring-invariants.ts:258** — COUNT(*) on `tool_performance` executed without tenant filtering (SELECT count(*) FROM tool_performance). This can leak cross-tenant telemetry/statistics.
  - _Fix:_ Add a tenant_id filter or iterate tenants and restrict each query to WHERE tenant_id = :tenantId. If a global check is required, require explicit admin authorization and a clearly named function for global aggregates.
- **server/wiring-invariants.ts:260** — Query against `messages` table without tenant filter (SELECT count(*) FROM messages WHERE created_at > ...). `messages` is tenant-scoped and this reads across all tenants.
  - _Fix:_ Restrict the query to a specific tenant with WHERE tenant_id = :tenantId, or perform the check in a per-tenant loop. Avoid any implicit global scans of tenant-scoped data.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM `tool_performance` WHERE ... is executed with no tenant_id constraint, allowing cross-tenant aggregation of tool usage.
  - _Fix:_ Add WHERE tenant_id = :tenantId or iterate tenants and run per-tenant queries. Use parameterized Drizzle queries (e.g. .where(eq(table.tenantId, tenantId))).
- **server/wiring-invariants.ts:417** — Tool-drift query aggregates `tool_performance` (SUM success/fail, HAVING ...) with no tenant_id filter, producing cross-tenant rollups and potential data leakage.
  - _Fix:_ Scope the aggregation to a tenant with WHERE tenant_id = :tenantId or perform the aggregation per-tenant. Use parameterized queries (drizzle sql`` with parameters or query builders) to avoid accidental cross-tenant aggregations.

### MEDIUM (43)

- **server/agent-channels.ts:194** — getUnreadCount joins channel_subscriptions (alias cs) but does not constrain cs.tenant_id. The JOIN condition is `JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}` while the WHERE only scopes cm to the caller tenant.
  - _Fix:_ Add tenant scoping for the channel_subscriptions table (e.g. include `AND cs.tenant_id = ${tenantId}` in the JOIN or WHERE) so subscription rows from other tenants cannot be joined.
- **server/agentic-features.ts:599** — enrollInSequence checks for an existing enrollment with `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` but does not include tenant_id. This can read enrollments across tenants.
  - _Fix:_ Constrain the existence check by tenant (e.g. add `AND tenant_id = ${params.tenantId}`) so the check is performed only within the caller's tenant.
- **server/agentic/department-budgets.ts:143** — SELECT against tenant-scoped table `department_budgets` is executed without constraining by tenant_id (query: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`). This reads rows across all tenants without an explicit tenant filter.
  - _Fix:_ Either scope the query to an explicit tenant (add `WHERE tenant_id = ${tenantId}`) when intended to operate per-tenant, or if the sweep is intentionally cross-tenant, make the admin intent explicit: require/validate an admin role or an explicit admin flag in the caller, document the privilege, and centralize this cross-tenant operation behind a tightly-audited, server-only admin API. Avoid blind unscoped reads on tenant-scoped tables to prevent accidental tenant data exposure or unauthorized cross-tenant operations.
- **server/auto-consolidation.ts:335** — Administrative/global scan queries tenant-scoped table without tenant predicate: `SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL` enumerates tenant IDs by scanning a tenant-scoped table with no tenant_id filtering.
  - _Fix:_ If this is intended as a global scheduler enumerating tenants, explicitly document and guard this administrative operation; otherwise avoid unscoped selects on tenant-scoped tables. For per-tenant operations, always include `WHERE tenant_id = ${tenantId}`. Consider moving global tenant enumeration to a dedicated, intentionally-global registry table or add explicit admin-only guards around this scan.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` omits a tenant_id constraint. The code updates `capability_reviews` by id (WHERE id = ${pending.reviewId} AND reused = false) without ensuring the caller/operation is scoped to the owning tenant, allowing a cross-tenant write if an attacker can trigger markReviewReused with a conversationId that maps to another tenant's pending review.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId} to the WHERE clause). Persist the review's tenantId into the in-memory pendingReuseByConversation entry when recording the review and verify it in markReviewReused before performing the DB UPDATE. Alternatively, fetch the review row and validate its tenant_id matches the caller's tenant prior to updating. Always constrain writes to tenant-scoped tables with an explicit tenant_id filter.
- **server/chat-engine.ts:3697** — markReviewReused(conversationId, toolName) is invoked without tenant context. If markReviewReused persists telemetry/records in tenant-scoped tables, it lacks tenant_id scoping.
  - _Fix:_ Pass tenantId into markReviewReused or make the function derive and enforce tenant ownership by validating the conversationId belongs to the current tenant. Ensure any DB writes include WHERE tenant_id = <tenant>.
- **server/chat-engine.ts:4022** — postDiagnosticReport([stuckPattern]) is invoked without tenant context. If this routine persists diagnostic records into tenant-scoped storage, no tenant_id is supplied.
  - _Fix:_ If postDiagnosticReport stores tenant-scoped data, add a tenantId parameter and pass tenantId here; enforce application-level WHERE tenant_id = tenantId in all queries. If the function is global telemetry, make that explicit and ensure no tenant-scoped table is written without the tenant filter.
- **server/commitment-drafter.ts:146** — SELECT from tenant-scoped table `commitments` without tenant_id filter (SELECT DISTINCT tenant_id ... FROM commitments).
  - _Fix:_ Avoid ad-hoc cross-tenant reads. If you must enumerate tenants, run this code in an explicit admin context and document/guard it, or derive the tenant list from an explicit tenants table with access checks. Otherwise restrict the SELECT to a single tenant (WHERE tenant_id = $tenantId) so ordinary code never reads unscoped tenant rows.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks is executed without an explicit tenant_id WHERE clause (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). The code previously validated the collection belongs to the tenant, but the raw DELETE does not include tenant scoping.
  - _Fix:_ Include tenant_id in the DELETE predicate (AND tenant_id = ${tenantId}) to enforce the app-level isolation invariant. e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}.
- **server/firecrawl.ts:214** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) and omits tenant scoping. Writes against tenant-scoped tables must include tenant constraint.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Even when the row was just read with a tenant filter, add the tenant constraint on subsequent writes to prevent TOCTOU/race and IDOR risks.
- **server/firecrawl.ts:344** — UPDATE on tenant-scoped table 'scraped_pages' inside the crawl loop uses only the row id in the WHERE clause and omits tenant scoping.
  - _Fix:_ Add the tenant constraint to the UPDATE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include tenant filters on writes to tenant-scoped tables.
- **server/job-queue.ts:220** — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — reading per-job data without ownership check.
  - _Fix:_ Accept tenantId in failJob (or re-read the row and validate its tenant_id) before acting; add WHERE tenant_id = ${tenantId} to the SELECT to enforce tenant isolation.
- **server/job-queue.ts:320** — listJobs runs a SELECT FROM agent_jobs with a dynamically-built whereClause that may be empty (no tenant_id filter) allowing listing jobs across all tenants when tenantId is not supplied.
  - _Fix:_ Require callers to explicitly supply tenantId for tenant-facing APIs or add authorization checks for operator/admin use. If cross-tenant listing is necessary, make it an explicit admin-only API and document the privilege.
- **server/job-queue.ts:385** — getJobStats SELECT aggregates counts FROM agent_jobs over last 7 days with NO tenant_id filter — returns cross-tenant stats without scoping to a tenant.
  - _Fix:_ Provide an optional tenantId parameter to return per-tenant stats or make this endpoint clearly admin-only. Add WHERE tenant_id = ${tenantId} when tenant-scoped stats are required.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge GROUP BY tenant_id, persona_id with no tenant filter — scans agent_knowledge across all tenants.
  - _Fix:_ If this job is intended to run per-tenant, add tenant scoping; otherwise ensure this code runs only in a privileged/operator context and that appropriate access controls and audit logs exist. Consider iterating tenants via a safe, explicit list rather than an un-scoped GROUP BY.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` without a tenant_id WHERE filter (lookup by key_prefix). This query reads a tenant-scoped table across all tenants.
  - _Fix:_ If this global lookup is intentional (to resolve a presented key into its tenant), ensure this path is strictly limited to the key-verification flow only and document it. Otherwise, constrain the query by tenant_id (or otherwise verify ownership) to avoid accidental cross-tenant reads. For example, include AND tenant_id = ${expectedTenantId} or ensure the surrounding logic derives and enforces the intended tenant prior to exposing any tenant-specific data.
- **server/lib/mcp-api-keys.ts:182** — UPDATE to tenant-scoped table `mcp_api_keys` (touching last_used_at) is executed with WHERE id = ${row.id} but no tenant_id constraint. This is a write against a tenant-scoped table without explicitly constraining by tenant.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause or otherwise validate ownership before the write. E.g. UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} so the write is guaranteed to affect only the resolved tenant's row.
- **server/lib/restraint-trust.ts:88** — Dynamic SQL via sql.raw() is used with a column name derived from caller input (`outcome` → mapped `column`). Although the code allowlists the column name, using sql.raw() with any runtime-derived identifier is fragile and increases risk of SQL-injection or accidental misuse and is flagged by the tenant-isolation + query-safety rules.
  - _Fix:_ Avoid passing identifiers into raw SQL. Replace the dynamic raw fragment with an explicit, compile-time-safe branch that issues one of a small set of parameterized updates. Example: switch on outcome and call db.update(trust_scores).set({ action_alpha: sql`action_alpha + ${delta}` }) (or the equivalent Drizzle column reference) for each allowed outcome, or use Drizzle's typed column references / builder APIs instead of sql.raw(). Keep tenant_id/persona_id/category in the WHERE clause as now.
- **server/memory-graph.ts:81** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} is missing a tenant filter. This write to a tenant-scoped table is constrained only by id and could affect another tenant's row with the same id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. ... WHERE id = ${catId} AND tenant_id = ${tenantId}) or otherwise verify the category belongs to the caller's tenant before updating.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL — no tenant filter. If skills are tenant-scoped, this will return rows across tenants.
  - _Fix:_ If skills are tenant-scoped, include tenant_id = ${tenantId} OR tenant_id IS NULL (for global skills) in the WHERE clause. Explicitly scope tenant-specific rows and allow NULL tenant rows only when intended (and safe). Validate schema semantics and add the appropriate tenant constraint.
- **server/recurring-messages.ts:258** — Background runner selects agent_knowledge rows without tenant filter (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This reads rows for all tenants from a tenant-scoped table.
  - _Fix:_ If the runner is intentionally global, document the privileged global intent and ensure it only runs in a trusted system context. Otherwise restrict the query by tenant (AND tenant_id = ${tenantId}) or require an explicit admin/global flag to avoid accidental cross-tenant reads.
- **server/routes.ts:1531** — Public stats endpoint runs SELECT count(*) FROM conversations/messages/heartbeatTasks/memory_entries/heartbeat_logs without tenant_id filters — reading tenant-scoped tables across all tenants.
  - _Fix:_ If per-tenant isolation is required, restrict these queries to the caller's tenant. If the intent is to publish global aggregates, compute and store aggregated metrics in a dedicated global table via a background job (so per-tenant rows are not queried ad-hoc) and document that these are intentionally global metrics.
- **server/routes.ts:1559** — Public architecture endpoint executes multiple raw SELECTs (counts, persona list) against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_* etc.) without tenant filters, exposing cross-tenant aggregate data.
  - _Fix:_ Either restrict these queries to admin-only contexts, or maintain a separate pre-aggregated global metrics table updated by a safe background job. Avoid ad-hoc unscoped scans of tenant-scoped tables in public endpoints.
- **server/routes.ts:2313** — Order lookup: SELECT FROM delivery_logs WHERE orderId/stripePaymentId = ${sessionId} is executed without scoping by tenant_id. delivery_logs appears to be tenant-scoped and this read is unscoped.
  - _Fix:_ Treat delivery records as tenant-scoped or as capability-protected resources. If relying on an unguessable sessionId as a capability, document that clearly and ensure the query does not expose tenant identifiers. Prefer adding an explicit tenant_id constraint where possible or store capability tokens in a separate global safe table so reads don't query tenant-scoped tables unscoped.
- **server/routes.ts:2421** — Async order-recovery job queries delivery_logs by lower(customer_email) = ${rawEmail} with no tenant_id constraint — this searches delivery records across all tenants by email address (PII).
  - _Fix:_ Restrict lookups to a single tenant context when appropriate or ensure this cross-tenant behavior is a deliberate admin/global feature with explicit privacy review. Prefer storing per-tenant delivery indexes or performing the lookup only within a tenant scope; if emailing the owner globally is required, ensure proper logging, consent, and rate-limiting are in place.
- **server/routes.ts:2565** — Order verify returns delivery_logs rows by lower(customer_email) = ${rawEmail} without tenant scoping (again reading tenant-scoped delivery_logs across tenants) when consuming a lookup code.
  - _Fix:_ As above: scope queries to tenant where possible, or move to a design that uses capability tokens or precomputed per-tenant indexes. Ensure PII access across tenants is intentionally allowed and documented; otherwise add WHERE tenant_id = <tenant>.
- **server/routes.ts:2057** — Presenter session lookup: SELECT ... FROM presenter_sessions WHERE token = ${token} does not include tenant scoping or verification. The endpoint returns session fields (including tenant_id) based solely on token.
  - _Fix:_ If presenter tokens are intended to act as unguessable capabilities, explicitly document and validate token entropy and expiration. Otherwise, bind tokens to tenant context (include tenant in the signed capability) or verify that the requester is authorized for the tenant before returning tenant-scoped data. Avoid returning tenant_id or other tenant-specific fields when not necessary.
- **server/routes.ts:2089** — Presenter slide fetch queries presenter_slide_images by session_id without enforcing tenant ownership. session_id was derived from a token earlier and no tenant check is performed before reading image rows.
  - _Fix:_ Ensure the session token lookup also verifies or binds tenant ownership, or include tenant_id in the slide-image query (JOIN through presenter_sessions with tenant check) so image rows cannot be read across tenants even if session_id is misused.
- **server/routes/projects.ts:35** — Project list subqueries (file_count, note_count, conversation_count) query tenant-scoped tables (project_files, project_notes, project_conversations) without constraining tenant_id. This can leak cross-tenant counts if FK integrity is missing.
  - _Fix:_ Constrain the subqueries to the caller tenant. Example: (SELECT COUNT(*) FROM project_files pf WHERE pf.project_id = p.id AND pf.tenant_id = ${tenantId}) or join through projects and include tenant_id = ${tenantId} in the WHERE clause.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (files-base64 flow) lacks a tenant_id constraint. Although the handler earlier checks project ownership, the SQL itself is not scoped to tenant and could update the wrong row if checks are bypassed or stale.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}; prefer always embedding tenant constraint in the statement as defense-in-depth.
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (multipart upload flow) lacks tenant_id in the WHERE clause. The DB write is not explicitly tenant-scoped.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause. Example: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId};
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC does not include tenant_id. Dependent reads against tenant-scoped tables must also be constrained to the caller tenant.
  - _Fix:_ Constrain the query to the tenant, e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC, or JOIN projects and require p.tenant_id = ${tenantId} in the WHERE clause.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${'uploads/' + filename} LIMIT 1 lacks an explicit tenant_id constraint. A file record could belong to another tenant if tenant checks are bypassed elsewhere.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause, or select via a JOIN to projects with p.tenant_id = ${tenantId} as defense-in-depth.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not include tenant_id. Although project ownership is checked earlier, the DELETE statement itself is not tenant-scoped.
  - _Fix:_ Include tenant scoping in the delete: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}; prefer always adding tenant_id checks in SQL statements rather than relying only on prior application-level checks.
- **server/storage.ts:935** — getHeartbeatTask fetches a heartbeat_tasks row by id with NO tenant restriction (where id = ${id})
  - _Fix:_ If callers should be tenant-restricted, add a tenantId parameter and include eq(heartbeatTasks.tenantId, tenantId) in the WHERE. If an unscoped fetch is legitimately required, rename to getHeartbeatTaskUnscoped and emit an explicit warning/log so use in HTTP handlers is avoided.
- **server/storage.ts:991** — getDueHeartbeatTasks selects FROM heartbeat_tasks across all tenants with no tenant filter (raw SQL selects tasks WHERE enabled=true AND next_run_at <= NOW())
  - _Fix:_ If the scheduler legitimately needs to scan tasks across tenants, ensure the caller is a trusted service principal and document that this is an intentional global operation. Otherwise constrain by tenant or provide a safe per-tenant interface and/or admin guard.
- **server/storage.ts:1019** — claimHeartbeatTasks issues UPDATE heartbeat_tasks ... WHERE id = ${id} ... with NO tenant constraint — potential cross-tenant mutation if id is attacker-supplied
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) and require a validated tenantId parameter in claimHeartbeatTasks, or ensure the caller is privileged and cannot be driven by an untrusted request parameter.
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} executes without constraining by tenant_id. An UPDATE against a tenant-scoped table lacks an explicit tenant_id filter in the WHERE clause.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (or re-validate ownership) e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}. Alternatively, perform the update via a JOIN that validates project belongs to ctx.tenantId before modifying or re-query/authorize the project id first and then include tenant_id in the WHERE.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} lacks a tenant_id constraint — performing an UPDATE on a tenant-scoped table without WHERE tenant_id = <caller tenant>.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}, or otherwise verify ownership immediately prior to the UPDATE and include the tenant check in the statement to prevent cross-tenant modification.
- **server/video-job-runner.ts:208** — SELECT cancelRequested from videoJobs WHERE jobId = ${jobId} is executed without a tenant_id filter in isCancelRequested. Reading a tenant-scoped flag by jobId alone can leak state across tenants or be used to act on jobs not owned by the runner's context.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE jobId = ${jobId} AND tenant_id = ${expectedTenantId}) or otherwise determine and validate the job's owning tenant (for example by reading the local job state file which contains tenant_id) before reading/modifying DB flags. Ensure any global reads are only done by an authorized admin path with explicit justification.
- **server/wiring-invariants.ts:361** — Use of sql.raw to query subsystem tables (information_schema + SELECT COUNT / MAX FROM ${spec.table}) with table/column names from SUBSYSTEM_TABLES, but the queries lack tenant scoping (counts over entire table). Subsystem tables like `knowledge_nudges` and `tool_performance` are likely tenant-scoped.
  - _Fix:_ When checking per-tenant freshness, run the existence/count/MAX checks per tenant and include WHERE tenant_id = :tenantId. If table names must be injected, ensure they come from trusted constants (they do) but still include tenant_id in the generated SQL and use parameter binding for tenant values rather than string interpolation.
- **server/wiring-invariants.ts:535** — Memory-growth probe uses sql.raw to SELECT COUNT and pg_total_relation_size FROM memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without tenant_id constraints — these tables are commonly tenant-scoped and are being scanned globally.
  - _Fix:_ Run memory-size checks per-tenant and include WHERE tenant_id = :tenantId. If pg_total_relation_size must be used for global table size, clearly separate per-tenant row counts (with tenant filter) from aggregate-only operations and document admin-only usage.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory is executed without tenant scoping; it returns a global count across tenants though graph_memory is likely tenant-scoped.
  - _Fix:_ Restrict to a tenant via WHERE tenant_id = :tenantId or perform the check per-tenant. If this is an intentional global check, require explicit admin intent and document the exception.

### LOW (1)

- **server/lib/outcome-promotion.ts:158** — SELECT over tenant-scoped table `deliverable_grade_events` with no tenant_id filter (global aggregation grouped by tenant_id). This reads all tenants' events in one query.
  - _Fix:_ If this is intended (a nightly cross-tenant promotion scan), ensure the job is only run under a controlled admin context and that the code and operational processes accept the cross-tenant read. If not intended, add a tenant_id WHERE filter to scope reads to a single tenant or run separate per-tenant queries to preserve tenant isolation.

### Suppressed — confirmed false positives (161)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — autoApplyEligibleInsights performs a global UPDATE on tenant-scoped table `ai_insights` without tenant_id scoping: `UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...`. This updates rows across all tenants in one operation.
  - _Why false positive:_ Global maintenance backfill intentionally updates eligible insights across tenants and returns row-carried tenant_id for per-row downstream routing; no mixed tenant-facing output shown.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on tenant-scoped table `agent_approvals` without tenant scoping: `UPDATE agent_approvals SET status = 'expired', decidedAt = new Date() WHERE status = 'pending' AND expiresAt < now() RETURNING id, runId, tenantId`. This updates approvals for all tenants in one statement.
  - _Why false positive:_ Expiry sweep intentionally bulk-expires stale approvals across tenants, then handles linked runs per returned row with tenantId carried from each approval.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query pulls agent_runs JOIN agent_approvals without constraining to a specific tenant (no WHERE tenant_id = <caller tenant>). This returns rows across tenants and allows the worker to observe/process approvals/runs for other tenants.
  - _Why false positive:_ Background worker sweep scans pending approval runs across tenants with join enforcing a.tenant_id = r.tenant_id and processes rows per row-carried tenant_id, not a tenant-facing mixed response.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} is executed without constraining by tenant_id — destructive write may remove another tenant's messages if IDs collide or attack is possible.
  - _Why false positive:_ Housekeeping purge iterates expired conversations selected with their tenant_id and deletes child rows for each parent conversation; maintenance deletion only, no tenant data returned or mixed.
- **server/data-protection.ts:132** _(system-sweep)_ — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks a tenant_id predicate — deletion may affect other tenants' archives.
  - _Why false positive:_ Same purge loop deletes per-conversation child archives as housekeeping during global sweep, with parent rows processed individually and no tenant-facing data mixing.
- **server/data-protection.ts:133** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} is executed without tenant_id predicate — may delete a conversation for the wrong tenant in case of ID confusion or race.
  - _Why false positive:_ Global purge deletes expired soft-deleted conversations as maintenance only after selecting each row; this is housekeeping rather than tenant-facing cross-tenant access.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT FROM memory_entries (backfillEmbeddingVecs) with no tenant_id filter (SELECT id, embedding FROM memory_entries ...). This reads tenant-scoped data across all tenants.
  - _Why false positive:_ Backfill sweep reads rows across tenants solely to repair missing embedding_vec values; maintenance job does not expose mixed tenant content to users.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries in backfillEmbeddingVecs updates rows by id only (WHERE id = ${row.id}) with no tenant_id constraint, enabling modification across tenants.
  - _Why false positive:_ Per-row embedding_vec backfill is a platform-wide housekeeping update derived from rows selected in the same maintenance sweep, not a tenant-facing mixed operation.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT FROM agent_knowledge (backfillEmbeddingVecs) executed without tenant_id filter (SELECT id, embedding FROM agent_knowledge ...). This reads knowledge rows across all tenants.
  - _Why false positive:_ Boot/maintenance backfill intentionally scans across tenants and only updates per-row embedding_vec housekeeping fields; no tenant-facing data mixing/output.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge in backfillEmbeddingVecs updates rows by id only (WHERE id = ${row.id}) with no tenant_id constraint, allowing cross-tenant writes.
  - _Why false positive:_ Per-row maintenance update during global embedding backfill; row id comes from the sweep's own select and only mutates housekeeping vector fields.
- **server/lib/ideabrowser-backfill.ts:104** _(fk-transitive)_ — INSERT INTO project_files does not set tenant_id. Platform invariant requires explicit tenant_id on INSERT into tenant-scoped tables (no defaults).
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated through tenant-scoped projects via project_id.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT INTO project_notes does not set tenant_id. Every INSERT into tenant-scoped tables must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id column and is transitively isolated through tenant-scoped projects via project_id.
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT into tenant-scoped table memory_links does not include tenant_id (tenant_id column omitted from VALUES). Inserts into tenant-scoped tables must always set tenant_id explicitly.
  - _Why false positive:_ memory_links has no tenant_id column; isolation is via referenced tenant-scoped memory_entries rows.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — UPDATE on tenant-scoped table uses id only and lacks an explicit tenant_id constraint. This can modify another tenant's row if ids are guessed or misaligned.
  - _Why false positive:_ Startup validation loop is platform-wide maintenance over subscriptions; it uses each row's own tenant_id and only flips is_active housekeeping state.
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — same pattern as above: UPDATE without tenant_id constraint.
  - _Why false positive:_ Same startup maintenance sweep; per-row deactivation uses row-selected subscription and does not expose or mix tenant data.
- **server/persona-export.ts:33** _(global-table)_ — exportPersona reads persona by id using SELECT * FROM personas WHERE id = ${personaId} without constraining by tenant_id. The function accepts tenantId but does not validate that the persona row belongs to that tenant — allows cross-tenant data export/leak.
  - _Why false positive:_ Schema notes show personas has no tenant_id column, so base persona row is platform-global rather than tenant-scoped.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog executes UPDATE plans ... WHERE id = ${planId} without constraining by tenant_id. This allows a caller that can invoke this function with an arbitrary planId to modify another tenant's plan row.
  - _Why false positive:_ Internal executor helper updates a plan by server-side planId in worker flow, not directly from an untrusted request boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Claim step performs UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' RETURNING ... with NO tenant_id constraint. This allows changing status of any plan by id across tenants (cross-tenant mutation).
  - _Why false positive:_ Claim step is in internal server worker executePlan operating on a server-invoked planId primary key, not a tenant-facing arbitrary id parameter.
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} is executed without a tenant_id constraint when a plan has no steps. This updates a plan by id across tenants.
  - _Why false positive:_ Follow-on status update inside same internal worker after claim; planId is the server-controlled worker key, not direct untrusted cross-tenant input.
- **server/routes.ts:166** _(system-sweep)_ — Unscoped SELECT from tenant-scoped table `file_storage` in startup restoreUploadsFromDb — no tenant_id filter. This reads all tenants' stored files and may write them to disk, breaking tenant isolation.
  - _Why false positive:_ Boot-time restore scans all tenants for housekeeping only, writing files to local disk and not returning or mixing tenant data to tenant-facing output.
- **server/routes.ts:1117** _(signed-url)_ — SELECT from tenant-scoped table `file_storage` by filename with no tenant constraint (owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This reads ownership rows across all tenants, enabling cross-tenant enumeration of who owns a filename.
  - _Why false positive:_ Cross-tenant filename owner lookup is part of signed/session file authorization and only checks whether authenticated tenant owns the basename; non-owners get 404 and no row data is exposed.
- **server/routes.ts:4331** _(fk-transitive)_ — Direct INSERT into project_notes omits tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) is executed without providing tenant_id, violating tenant isolation assumptions for tenant-scoped tables.
  - _Why false positive:_ project_notes has no tenant_id and is inserted only after verifying conversation and derived project belong to tenant-scoped parent project.
- **server/routes/conversations.ts:101** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id — tenant_id is NOT provided in VALUES, allowing creation of rows with no tenant ownership.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped parent entities already checked for tenant ownership.
- **server/routes/conversations.ts:103** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' performed without constraining by tenant_id (WHERE clause lacks tenant scope).
  - _Why false positive:_ Immediately preceded by SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId in same call path.
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id and insert occurs only after verifying parent project belongs to authenticated tenant.
- **server/routes/projects.ts:225** _(fk-transitive)_ — INSERT into project_files (files-base64 route) does not include tenant_id. Inserting into a tenant-scoped table without tenant_id violates tenant isolation invariant.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped transitively through verified tenant-owned parent project.
- **server/routes/projects.ts:293** _(fk-transitive)_ — INSERT into project_files (multipart upload route) does not include tenant_id. Tenant_id must be explicitly set on INSERT into tenant-scoped tables.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via project_id to tenant-scoped projects; route verifies project ownership before insert
- **server/sculptor.ts:105** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets status/result by session id without constraining tenant_id (UPDATE sculptor_sessions SET status = 'failed', result = ... WHERE id = ${session.id}). This can modify another tenant's session if an attacker supplies/guesses an id.
  - _Why false positive:_ update is by freshly inserted server-generated session.id inside same function, not from an untrusted request boundary
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id by session id without constraining tenant_id (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). This can link a conversation into a session of another tenant.
  - _Why false positive:_ conversation_id update targets the newly created server-side session row by its own generated primary key
- **server/sculptor.ts:168** _(internal-worker-pk)_ — monitorSession() updates sculptor_sessions (UPDATE sculptor_sessions ... WHERE id = ${sessionId}) without constraining tenant_id even though tenantId is available. This allows cross-tenant modification by session id.
  - _Why false positive:_ monitorSession is an internal server worker operating on sessionId passed from launch of the same newly created session, not caller-controlled
- **server/sculptor.ts:358** _(upstream-verified)_ — reviewSessionWork() updates sculptor_sessions.review_result by session id (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}) without constraining tenant_id, despite tenantId being available and used earlier to fetch the session.
  - _Why false positive:_ same function fetches sculptor_sessions by id and tenant_id immediately before this update, proving ownership in the call path
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' is executed without any tenant_id filter. heartbeat_tasks appears tenant-scoped, so this reads across tenants (and subsequent logic acts on that global count).
  - _Why false positive:_ boot/maintenance cleanup intentionally counts rows across tenants for housekeeping only and does not expose tenant data
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true executes without tenant_id filter, disabling delegation tasks across all tenants.
  - _Why false positive:_ maintenance reconciler intentionally disables runaway delegation tasks across tenants as housekeeping, with no tenant data returned or mixed
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' is run without tenant_id filter, reading tasks across tenants.
  - _Why false positive:_ global cleanup count over heartbeat_tasks is maintenance-only aggregate logic across tenants, not tenant-facing data access
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true runs without tenant_id filter, disabling matching tasks for all tenants.
  - _Why false positive:_ housekeeping update intentionally disables duplicate session-logger tasks across tenants and only mutates enabled status
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) executes without tenant_id filter; the subsequent UPDATE (lines 74-86) also misses tenant scoping and will disable matching tasks across tenants.
  - _Why false positive:_ maintenance sweep counts and disables orphaned repo-examination tasks across tenants as status housekeeping only
- **server/seed.ts:3750** _(global-table)_ — UPDATE memory_entries runs without any tenant_id constraint (updates may affect all tenants' memory_entries).
  - _Why false positive:_ Shown UPDATE targets tenants table, which is intentionally platform-global and not tenant-scoped.
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE memory_entries executes with WHERE id = <row id> but no tenant_id constraint. Updating by id alone can modify another tenant's entry if ids collide or are guessed.
  - _Why false positive:_ Same call path first selects the row with tenant_id = tenantId, then updates that selected id; ownership is enforced earlier in the function.
- **server/storage.ts:295** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining tenant_id (SELECT id, citations FROM messages WHERE id IN (...)) in attachCitationMetadata
  - _Why false positive:_ attachCitationMetadata only queries ids taken from msgs already fetched by tenant-scoped callers in shown path, so ids are pre-scoped upstream.
- **server/storage.ts:838** _(system-sweep)_ — UPDATE that archives expired memories runs across the entire tenant-scoped table `memory_entries` with no tenant filter (UPDATE memory_entries SET status='archived' WHERE ... expires_at <= NOW())
  - _Why false positive:_ archiveExpiredMemories is a platform-wide housekeeping update that only flips status on expired rows and returns aggregate count, not tenant data.
- **server/storage.ts:855** _(system-sweep)_ — UPDATE that archives stale memories runs across entire tenant-scoped table `memory_entries` with no tenant filter (archiveStaleMemories)
  - _Why false positive:_ archiveStaleMemories is a global maintenance archival that only updates housekeeping status and returns count, with no tenant-facing data mixing.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment usageCount on `custom_tools` is performed by id only (.where(eq(customTools.id, tool.id))) with no tenant_id constraint. This permits cross-tenant writes.
  - _Why false positive:_ Tool row is fetched immediately above with tenant-scoped lookup (name and tenantId), then updated by that retrieved row id in same call path.
- **server/tools.ts:4540** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id in the VALUES list. The code inserts project_notes for a project without explicitly setting tenant_id even though tenant isolation is enforced only at the app layer.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively scoped via project_id; assertProjectInTenant verifies parent ownership before insert.
- **server/tools.ts:4544** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The insert stores a project file row but omits tenant_id in the column list/VALUES.
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped via project_id; assertProjectInTenant verifies parent ownership before insert.
- **server/tools.ts:6392** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' (audio upload path) omits tenant_id in the VALUES. The code inserts a project_files row for params.project_id without setting tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is child-scoped by project_id; insert is preceded by assertProjectInTenant.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id (INSERT INTO project_files (... ) VALUES (...)) — tenant_id column is omitted, so new row may not be tied to the caller's tenant.
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated through tenant-scoped projects; preceding SELECT joins projects with p.tenant_id = tenantId before insert.
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint. Application-level isolation invariant requires every write to be constrained to the caller's tenant.
  - _Why false positive:_ The id being updated comes from the immediately preceding SELECT constrained by tenant_id = tid, persona_id, and category in the same flow.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with no tenant_id constraint. This can update rows from other tenants.
  - _Why false positive:_ ids are derived from rows just selected with WHERE tenant_id = tid, so this write is scoped by same-call-path selection
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without any tenant_id constraint (after finding the id via a tenant-scoped SELECT). Application-level invariant requires explicit tenant filter on every write.
  - _Why false positive:_ updated id comes from immediately prior tenant-scoped SELECT on memory_entries for the same tenant/persona/category
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} performed without verifying tenant ownership. This can modify another tenant's ledger row if an attacker supplies a reservationId from a different tenant.
  - _Why false positive:_ server-side helper updates a ledger row by reservationId primary key produced by prior reservation logic, not from a request boundary
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} executed without tenant constraint in releaseVentureReservation. This allows releasing/updating a reservation row belonging to another tenant.
  - _Why false positive:_ server-side release helper mutates reservation row by server-generated reservationId, not caller-supplied tenant-crossing input
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments selects from tenant-scoped table `experiments` without constraining by tenant_id (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads metadata for all tenants.
  - _Why false positive:_ heartbeat-callable maintenance sweep intentionally iterates running experiments across tenants and uses each row's tenant_id for per-row updates
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting selects from tenant-scoped table `ai_insights` without a tenant filter: `SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50`. This reads multiple tenants' rows.
  - _Why false positive:_ durability sweep intentionally scans all tenants' pending insights and processes each row with its own tenant_id without mixing outputs across tenants
- **server/agentic-engines.ts:344** _(global-table)_ — gatherOperationalData issues a tenant-unscoped query against `heartbeat_logs`: `SELECT COUNT(*) as total_calls, ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` — no tenant_id filter, so it aggregates logs across tenants.
  - _Why false positive:_ heartbeat_logs is schema-noted as not tenant-scoped, so global aggregate query is by design
- **server/agentic-engines.ts:451** _(global-table)_ — runPredictiveEngine performs `SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10` with no tenant constraint, reading logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is an intentionally global log table
- **server/agentic-engines.ts:519** _(global-table)_ — runOptimizationEngine queries `heartbeat_logs` without tenant scoping: `SELECT task_name, status, COUNT(*) as count, AVG(duration_ms) as avg_duration FROM heartbeat_logs GROUP BY task_name, status ...`. This aggregates data across tenants.
  - _Why false positive:_ heartbeat_logs is intentionally non-tenant-scoped, so cross-tenant aggregation is expected
- **server/agentic-features.ts:525** _(upstream-verified)_ — scoreLeads updates tenant-scoped table `lead_enrichments` using `WHERE id = ${lead.id}` without confirming tenant ownership in the WHERE clause. The update can affect rows outside the caller's tenant if IDs collide or are misused.
  - _Why false positive:_ lead.id comes from leadRows loaded earlier via tenant-scoped SELECT for params.tenantId, so the update targets rows already scoped
- **server/agentic-features.ts:671** _(upstream-verified)_ — advanceSequence updates outreach_enrollments with `UPDATE outreach_enrollments SET current_step = ${nextStep}, ... WHERE id = ${enrollment.id}` but omits tenant_id in the WHERE clause. Although enrollment was selected under the tenant, the UPDATE should still enforce tenant scoping to avoid accidental cross-tenant writes.
  - _Why false positive:_ Row being updated was selected just above with e.tenant_id = params.tenantId; update uses that verified row id in same call path.
- **server/agentic-features.ts:721** _(upstream-verified)_ — classifyReply updates outreach_enrollments using `WHERE id = ${row.id}` without scoping by tenant (`UPDATE outreach_enrollments SET ... WHERE id = ${row.id}`). This write lacks an explicit tenant_id guard.
  - _Why false positive:_ row.id comes from immediately preceding SELECT scoped by e.tenant_id = params.tenantId in same function.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with no tenant_id constraint. An update-by-id without tenant scoping can modify another tenant's run record.
  - _Why false positive:_ Helper updates agent_runs by server-generated serial PK runId inside internal worker/run orchestration, not directly from untrusted tenant request input.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) and no tenant_id condition, allowing cross-tenant mutation if runId is guessed or misused.
  - _Why false positive:_ Internal state-transition helper for agent_runs keyed by server-created runId, matching internal worker PK pattern.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) without tenant scoping. This permits updating runs belonging to other tenants.
  - _Why false positive:_ replaceRunState is an internal helper mutating a run by server-generated run id, not a caller-controlled cross-tenant boundary.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun updates agent_runs (status/result/completedAt) using only the run id in WHERE (.where(eq(agentRuns.id, runId))) with no tenant_id constraint.
  - _Why false positive:_ completeRun is an internal completion path on server-generated agent_runs PKs; subsequent read uses same internal id.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun sets a run to failed using only run id in the WHERE clause (.where(eq(agentRuns.id, runId))) and lacks a tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ failRun is an internal worker helper updating by globally unique server-side run id.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun updates agent_runs.status/state using only the run id in the WHERE (.where(eq(agentRuns.id, runId))) with no tenant_id constraint, so a caller could affect runs from other tenants.
  - _Why false positive:_ pauseRun is part of internal run orchestration and updates by server-generated run PK, not request-supplied tenant object id.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_wake_schedules` is executed without constraining to a tenant. The statement `UPDATE agent_wake_schedules SET status = 'firing' ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... FOR UPDATE SKIP LOCKED)` does not include `tenant_id` in the WHERE clause, so rows from any tenant can be claimed/modified by this scheduler run.
  - _Why false positive:_ Platform-wide scheduler intentionally claims due rows across tenants, then processes each row with its own tenant_id and updates per-row housekeeping state only.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — Global SELECT on tenant-scoped tables without tenant scoping. The query `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0` does not constrain rows to a tenant_id. The correlated subquery also queries `messages` with no tenant filter.
  - _Why false positive:_ Backfill scans all tenants intentionally and processes each conversation with row-carried tenant_id; no mixed multi-tenant output is returned to tenants.
- **server/auto-asset-capture.ts:207** _(system-sweep)_ — SELECT from tenant-scoped `messages` table without tenant constraint: `SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'` does not include `tenant_id`. This allows reading messages across tenants when conversation ids collide or if other parts of the code supply ids without ownership checks.
  - _Why false positive:_ Per-conversation message fetch occurs inside global backfill loop using conversations selected with their own tenant_id; maintenance job processes rows one tenant at a time.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (tenant_id NOT NULL), this INSERT violates the project invariant that every write to tenant-scoped tables must set tenant_id. Even if the invoice_id was just created in the same transaction, absence of an explicit tenant_id on the child row relies on implicit schema choices or FK semantics (which the codebase says are NOT relied upon).
  - _Why false positive:_ invoice_items has no tenant_id and is a child table isolated through tenant-scoped parent invoices created in the same transaction
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` without tenant_id filter (scanning overdue commitments across all tenants).
  - _Why false positive:_ this is a platform-wide heartbeat scanner that processes each row with its own tenant_id and emits only redacted owner-digest output, not tenant-facing mixed content
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` without tenant_id constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). This mutates a credential row without verifying the tenant owner.
  - _Why false positive:_ getCredential(id, tenantId) is called immediately before and returns only a credential already scoped to the tenant; this follow-on update is for that verified row
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` without tenant_id constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}).
  - _Why false positive:_ getCredentialForSite(siteUrl, tenantId) resolves the credential under tenant scope first; the subsequent last_used_at update targets that verified row
- **server/data-protection.ts:118** _(system-sweep)_ — SELECT on conversations (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) has no tenant_id predicate — reads rows across all tenants.
  - _Why false positive:_ global soft-delete purge intentionally scans across tenants, handles each row with its own tenant_id, and performs housekeeping/deletion rather than returning mixed tenant data
- **server/data-protection.ts:326** _(fk-transitive)_ — SELECT ... FROM project_notes WHERE project_id = ${proj.id} is executed without also scoping by tenant_id — reading notes for a project without explicit tenant predicate.
  - _Why false positive:_ project_notes has no tenant_id and is read by project_id from projects already scoped by tenant_id = tenantId.
- **server/data-protection.ts:336** _(fk-transitive)_ — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} lacks tenant_id predicate — project files are read without explicit tenant scoping.
  - _Why false positive:_ project_files has no tenant_id and is read by project_id from projects already scoped by tenant_id = tenantId.
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with a request-derived vector literal in vectorSearchMemory (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). The vector value originates from the request/query embedding and is interpolated into raw SQL.
  - _Why false positive:_ memory_entries query already enforces AND tenant_id = tenantId; finding is about raw SQL injection, not cross-tenant isolation.
- **server/embeddings.ts:252** _(upstream-verified)_ — Use of sql.raw() with a request-derived vector literal in vectorSearchMemory ORDER BY clause (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This interpolates a request-derived value into raw SQL.
  - _Why false positive:_ memory_entries ORDER BY occurs in a query already scoped with tenant_id = tenantId; no tenant-isolation issue here.
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw() with a request-derived vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) AS similarity). The vector is derived from the caller's query embedding and is interpolated into raw SQL.
  - _Why false positive:_ agent_knowledge vector query includes AND tenant_id = tenantId; flagged raw interpolation is not a tenant-isolation defect.
- **server/embeddings.ts:565** _(upstream-verified)_ — Use of sql.raw() with a request-derived vector literal in vectorSearchKnowledge ORDER BY clause (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is string-interpolating request-derived data into SQL.
  - _Why false positive:_ agent_knowledge ORDER BY is within a tenant-scoped query filtered by tenant_id = tenantId.
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' with no tenant_id filter (reads tenant_id but does not constrain by it). This can leak/rehydrate data from all tenants into a single in-memory store.
  - _Why false positive:_ boot-time rehydration intentionally scans all running/pending agent_runs and only rebuilds in-memory status state; it does not return mixed tenant data to a tenant-facing surface in this code.
- **server/google-drive.ts:561** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) without also constraining by tenant_id. Although the code validated ownership earlier, the write itself lacks an explicit tenant constraint (TOCTOU/race risk).
  - _Why false positive:_ Project ownership is checked immediately above by loading project.tenantId and throwing on mismatch before the update.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from code_proposals executes without constraining tenant_id — reads tenant-scoped rows globally.
  - _Why false positive:_ Health-audit performs a platform-wide TTL maintenance scan; rows are handled individually for findings/housekeeping, not exposed to tenant surfaces.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' uses id ANY(...) without any tenant_id constraint — allows modifying proposals across tenants.
  - _Why false positive:_ Platform-wide stale-proposal archival updates only a housekeeping status on ids selected by the same maintenance sweep.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from heartbeat_tasks queries enabled/stuck tasks with no tenant_id restriction — reads tenant-scoped scheduled tasks globally.
  - _Why false positive:_ Health-audit intentionally scans heartbeat tasks across tenants for maintenance; results are per-row audit findings/housekeeping.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) updates tasks without constraining by tenant_id — can disable tasks across tenants.
  - _Why false positive:_ Platform-wide maintenance disables stuck heartbeat tasks as housekeeping only, using ids gathered by the same sweep.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from tenant-scoped table `research_schedules` without constraining by tenant_id (SELECT * FROM research_schedules ...). This reads rows for all tenants.
  - _Why false positive:_ Heartbeat scheduler is a platform-wide maintenance loop selecting due schedules across tenants and then processing each row with its own sched.tenant_id.
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` uses WHERE id = ${sched.id} with no tenant_id check (UPDATE research_schedules SET next_run_at = ... WHERE id = ...).
  - _Why false positive:_ Heartbeat updates only housekeeping timing fields for each selected schedule row in a system-wide sweep using the row-carried sched context.
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` updates last_run_at/next_run_at by id only (no tenant_id constraint).
  - _Why false positive:_ System heartbeat advances per-row schedule timestamps only; no tenant data is returned or mixed, and processing uses each row's sched context.
- **server/heartbeat.ts:113** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` sets next_run_at WHERE id = ${sched.id} without tenant_id constraint.
  - _Why false positive:_ Platform-wide scheduler sweep updates next_run_at as housekeeping for the individual selected schedule row.
- **server/heartbeat.ts:121** _(system-sweep)_ — In recovery path: UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id filter on tenant-scoped table.
  - _Why false positive:_ Recovery-path update is part of the same maintenance sweep and only adjusts housekeeping timing on the current schedule row.
- **server/heartbeat.ts:378** _(system-sweep)_ — SELECT from tenant-scoped table `event_log` without a tenant_id constraint (SELECT * FROM event_log WHERE status = 'pending' ...). This returns events across all tenants.
  - _Why false positive:_ Global heartbeat sweep processes pending rows across tenants but handles each row with its own tenant_id and does not mix tenant data into one response.
- **server/heartbeat.ts:266** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — updating a tenant-scoped table row by id without constraining tenant_id. This can modify another tenant's row if mis-bound.
  - _Why false positive:_ Startup housekeeping normalizes heartbeat task config across the shared table; this is a platform-wide maintenance mutation, not tenant-facing data access.
- **server/heartbeat.ts:270** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — no tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ Startup housekeeping sweep updates reflection-task model settings across tenants as maintenance only, with no cross-tenant data exposure.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs executes UPDATE ... FROM agent_jobs and inner SELECT over agent_jobs without any tenant_id filtering — mass claim/transition of jobs could affect rows across tenants.
  - _Why false positive:_ Job claiming is a platform-wide worker operation over all tenants; claimed rows retain row-carried tenant_id for per-job handling rather than being returned to a tenant surface.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases: UPDATE agent_jobs ... WHERE status='running' AND lease_until < NOW() — no tenant_id constraint; this will mutate running jobs across all tenants.
  - _Why false positive:_ Expired-lease reclamation is cross-tenant housekeeping that only mutates job status/lease metadata and does not mix tenant content.
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases second UPDATE re-queues expired running jobs without any tenant_id filter — affects agent_jobs for all tenants.
  - _Why false positive:_ Requeueing expired jobs is a maintenance sweep over all tenants affecting only operational status fields.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs (SET status='succeeded' ...) by id without constraining tenant_id — a caller could mark another tenant's job succeeded by id.
  - _Why false positive:_ completeJob is an internal worker state transition by server-generated job primary key plus lease-fencing attempts guard, not a request-bound tenant selector.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob UPDATE that marks a job failed_terminal writes to agent_jobs WHERE id = ${id} ... with no tenant_id constraint — allows cross-tenant mutation of job state.
  - _Why false positive:_ Worker fail path updates by server-side job PK with lease-fencing attempts/status; not from request boundary.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob UPDATE that re-queues a failed job (sets status='pending', next_run_at=...) lacks tenant_id filtering — affects agent_jobs across tenants.
  - _Why false positive:_ Internal worker requeue of server-generated job id under running/attempt fencing, not tenant-facing input.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from projects groups by tenant_id but does not constrain the query to a specific tenant (no WHERE tenant_id = ...). This reads a tenant-scoped table across all tenants.
  - _Why false positive:_ Boot/backfill routine intentionally aggregates counts across tenants for per-tenant maintenance, not tenant-facing data return.
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT id FROM project_files WHERE project_id = ${newProjectId} ... reads project_files without constraining by tenant_id. If project_files is tenant-scoped, this can return rows belonging to other tenants.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by tenant-owned projects via project_id.
- **server/lib/ideabrowser-backfill.ts:115** _(fk-transitive)_ — SELECT id FROM project_notes WHERE project_id = ${newProjectId} ... reads project_notes without constraining by tenant_id. If project_notes is tenant-scoped, this can surface other tenants' rows.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by tenant-owned projects via project_id
- **server/lib/system-probes.ts:163** _(system-sweep)_ — SELECT against tenant-scoped table `messages` (messagesTable) is executed without any tenant_id constraint.
  - _Why false positive:_ global health probe only counts messages across tenants and does not return tenant content
- **server/martech-bundle.ts:325** _(upstream-verified)_ — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the row id in the WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) and does not constrain by tenant_id.
  - _Why false positive:_ row id comes from getVoiceProfile({tenantId, profileName}) so existing.id was already tenant-scoped in same flow
- **server/memory-graph.ts:197** _(fk-transitive)_ — SELECT against memory_links to check existence does not constrain by tenant (no WHERE tenant_id = ${tenantId}). This can leak or act on cross-tenant links.
  - _Why false positive:_ memory_links has no tenant_id and links are between memory_entries already selected within one tenant
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks a tenant_id filter. This updates a tenant-scoped table without verifying the row belongs to the caller's tenant.
  - _Why false positive:_ mem.id comes from prior SELECT on memory_entries filtered by tenant_id in same function
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} is executed without a tenant_id constraint. This writes to a tenant-scoped table without verifying ownership.
  - _Why false positive:_ entry.id was just created with tenantId in same flow so update targets the same tenant-owned row
- **server/memory-intelligence.ts:316** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in the 'update' flow) lacks a tenant_id filter, allowing cross-tenant modification.
  - _Why false positive:_ entry.id was just created with tenantId in same flow so update targets the same tenant-owned row
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} is executed without a tenant_id WHERE clause although the contradiction rows were selected by tenant. The UPDATE could modify rows across tenants if ids collide.
  - _Why false positive:_ c.id comes from contradictions SELECT filtered by tenant_id in same call path
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not include tenant_id. The code asserts project ownership via assertProjectInTenant(options.projectId, options.tenantId) but the INSERT omits tenant_id, violating the invariant that every INSERT into a tenant-scoped table must set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and insert is correctly scoped through assertProjectInTenant on parent project_id
- **server/mpeg-engine.ts:1417** _(fk-transitive)_ — INSERT into tenant-scoped table project_files (in produceVideoParallel) omits tenant_id. Even though ownership is verified via assertProjectInTenant(options.projectId, options.tenantId), the INSERT itself does not set tenant_id, violating the requirement that every INSERT include tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and insert is gated by assertProjectInTenant on tenant-scoped parent project
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions without tenant_id constraint (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants.
  - _Why false positive:_ startup validation intentionally iterates active subscriptions across tenants using each row's tenant_id for per-row handling
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' — no tenant filter. This enumerates tenant rows for a tenant-scoped table.
  - _Why false positive:_ boot-time auto-reconnect enumerates google-workspace subscription tenant_ids for per-tenant maintenance, not tenant-facing data return
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — unscoped read across all tenants.
  - _Why false positive:_ proactive refresh is a cross-tenant maintenance loop over active subscriptions with per-row tenant_id-scoped refresh actions
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — unscoped read across all tenants.
  - _Why false positive:_ daily force-refresh intentionally sweeps all active subscriptions and processes each provider/tenant row separately
- **server/pace-control.ts:85** _(global-table)_ — checkPace runs a COUNT(*) FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter} with no tenant_id constraint. If heartbeat_logs is tenant-scoped, this returns counts across tenants and can leak usage metrics.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column; global pace counting matches platform-global telemetry
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot SELECTs heartbeat_logs over a time window (COUNT and per-persona GROUP BY) without any tenant_id filter — potential cross-tenant data exposure if heartbeat_logs is tenant-scoped.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and snapshot is global telemetry over a global table
- **server/plan-executor.ts:775** _(system-sweep)_ — resumeStuckPlans performs a global UPDATE to flip all 'executing' plans older than the stale threshold back to 'approved' with no tenant_id filtering. This will touch other tenants' rows without per-tenant scoping.
  - _Why false positive:_ boot-time stale-plan recovery is cross-tenant housekeeping updating status fields only, with no tenant data returned or mixed
- **server/process-governor.ts:121** _(fk-transitive)_ — Subqueries counting heartbeat_logs (e.g. SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...) do not constrain by tenant_id, allowing cross-tenant log counts/exfiltration.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted via tenant-scoped heartbeat_tasks rows (ht.tenant_id = tenantId) through task_id = ht.id
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selects projects/joined conversations across the DB: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... — no tenant filter. This enumerates projects from all tenants.
  - _Why false positive:_ backfillProjectBrains is a platform-wide maintenance backfill operating across all tenants, processing each project/conversation separately rather than serving mixed tenant output
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} — no tenant constraint on conversations read during backfill, enabling cross-tenant conversation scanning.
  - _Why false positive:_ conversation scan occurs inside the global backfill loop for one project at a time; housekeeping backfill, not tenant-facing mixed output
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — messages read without tenant filter, permitting reading messages from conversations potentially belonging to other tenants.
  - _Why false positive:_ message reads are part of the same per-project maintenance backfill and are not returned to another tenant-facing surface
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` omits tenant filter — the UPDATE uses `WHERE id = ${storedId}` without `AND tenant_id = ${input.tenantId}`.
  - _Why false positive:_ storedId is obtained immediately above from memory_entries with tenant_id = input.tenantId, so the subsequent UPDATE by that id is ownership-verified in the same call path
- **server/routes.ts:765** _(system-sweep)_ — Unscoped SELECT from tenant-scoped table `projects` (JOIN tenants) in backfillProjectDriveFolders — no tenant filter. This enumerates projects across all tenants.
  - _Why false positive:_ drive-folder backfill is an admin maintenance sweep across tenants; each row carries its own tenant_id and is handled per-project without mixing tenant content
- **server/routes/admin.ts:701** _(global-table)_ — Query selects from tool_performance without constraining by tenant_id. If tool_performance is tenant-scoped this returns cross-tenant data.
  - _Why false positive:_ Admin-only diagnostics route reads tool_performance as platform-wide operational metrics; table appears intentionally global from usage/comments.
- **server/routes/conversations.ts:97** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' performed without constraining by tenant_id (WHERE clause lacks tenant scope).
  - _Why false positive:_ Conversation row was just created with tenantId and project ownership is verified by tenant-scoped SELECT before update; id is server-generated.
- **server/routes/conversations.ts:98** _(fk-transitive)_ — SELECT from 'project_conversations' performed without tenant constraint — reads tenant-scoped table without filtering by tenant_id.
  - _Why false positive:_ project_conversations has tenant_id=NO and links tenant-scoped project and newly created conversation, so isolation is transitive through parent rows.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on inbox_messages performed without tenant_id constraint: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` (no tenant predicate). This allows a caller who can control `id` (or trigger this path) to modify messages belonging to other tenants.
  - _Why false positive:_ Same path first SELECTs inbox_messages by id AND tenant_id, then updates that fetched row by its PK.
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions executed without a tenant_id constraint — reads tenant-scoped table globally.
  - _Why false positive:_ Boot-time dev snapshot seed intentionally checks global emptiness before importing; maintenance/seed logic, not tenant-facing data return.
- **server/seed.ts:2116** _(global-table)_ — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter — reads research_programs across all tenants.
  - _Why false positive:_ Shown line is static seed document text, not a tenant-scoped database query.
- **server/seed.ts:2466** _(global-table)_ — SELECT DISTINCT ... FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations ...) performs cross-tenant reads/joins without any tenant_id constraints on conversations or project_conversations.
  - _Why false positive:_ Code shown is owner-deployment seed migration hard-coded to tenant 1, not the alleged cross-tenant conversations/project_conversations query.
- **server/seed.ts:3294** _(global-table)_ — db.select().from(heartbeatTasks) reads heartbeat tasks without filtering by tenant_id — returns records across tenants.
  - _Why false positive:_ Shown line is comment text and surrounding code updates personas, which schema marks non-tenant-scoped; no heartbeat_tasks read is present.
- **server/seed.ts:3294** _(global-table)_ — SELECT on tenant-scoped table `heartbeat_tasks` is executed without any tenant_id constraint (db.select().from(heartbeatTasks)).
  - _Why false positive:_ Flagged line is a personas startup backfill; personas has no tenant_id and first-pass table mismatch to heartbeat_tasks is unsupported by shown code.
- **server/seed.ts:3961** _(global-table)_ — UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id} omits a tenant_id check. agent_knowledge is tenant-scoped; updating by id alone can modify a row that does not belong to the intended tenant context.
  - _Why false positive:_ This function updates personas by fixed ids; personas is platform-global with no tenant_id, so no tenant isolation issue.
- **server/storage.ts:823** _(system-sweep)_ — getMemoriesWithoutEmbeddings selects from tenant-scoped `memory_entries` with no tenant filter (reads all tenants' memories)
  - _Why false positive:_ Unscoped read is for embedding backfill/maintenance queue selection, a platform-wide worker pattern over per-row processing rather than tenant-facing mixed output.
- **server/storage.ts:828** _(system-sweep)_ — getKnowledgeWithoutEmbeddings selects from tenant-scoped `agent_knowledge` with no tenant filter (reads across tenants)
  - _Why false positive:_ Unscoped agent_knowledge selection is part of embedding maintenance/backfill processing, not tenant-facing data return or mixed prompt generation.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT on tenant-scoped table `custom_tools` performed without restricting by tenant_id (checks `eq(customTools.name, parsed.name)` only). This can leak cross-tenant existence information and cause incorrect conflicts.
  - _Why false positive:_ Name is transformed via tenantToolName(parsed.name, tenantId) before lookup, so existence check is namespaced by validated tenant-derived name.
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT by id against tenant-scoped table `custom_tools` performed without tenant_id filter (`where(eq(customTools.id, inserted.id))`). Reading tenant-scoped rows must be scoped to the tenant.
  - _Why false positive:_ Read is by freshly inserted server-generated primary key inserted.id within same internal flow, not from an untrusted request boundary.
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) lacks an explicit tenant_id column/value.
  - _Why false positive:_ project_conversations has no tenant_id; link insert is transitively scoped via tenant-owned project and tenant-verified conversation update just above
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) RETURNING * omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id; insert follows owner check on tenant-scoped projects row
- **server/tools.ts:2995** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) RETURNING * omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id; insert follows owner check on tenant-scoped projects row
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' (link_conversation path) does not set tenant_id. The INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id; project ownership and conversation tenant membership are both verified before insert
- **server/tools.ts:8891** _(upstream-verified)_ — UPDATE against proposed_skills omits a tenant_id constraint in the WHERE clause. The code updates status/review fields using WHERE eq(proposedSkills.id, row.id) only, allowing modification of a row by id without scoping to the caller's tenant.
  - _Why false positive:_ row.id refers to the proposal row just inserted/returned in this same call path for tenant tid; update is on that same row, not caller-selected id
- **server/tools.ts:8916** _(upstream-verified)_ — UPDATE against proposed_skills omits a tenant_id constraint in the WHERE clause. The code promotes a proposal and sets reviewed/promoted fields via WHERE eq(proposedSkills.id, row.id) only, lacking tenant scoping.
  - _Why false positive:_ updates the same freshly created proposal row from this flow; row.id is not taken from untrusted request id lookup
- **server/tools.ts:8933** _(upstream-verified)_ — UPDATE against proposed_skills omits a tenant_id constraint in the WHERE clause. The code marks a proposal as rejected using WHERE eq(proposedSkills.id, row.id) only, without verifying tenant ownership in the SQL.
  - _Why false positive:_ reject path mutates the same proposal row created earlier in the same request context for tenant tid
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples omits tenant_id in WHERE clause (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}). This allows mutating rows by id across tenants.
  - _Why false positive:_ row.id comes from immediately prior SELECT on knowledge_triples constrained by tenant_id = tenantId and valid_until IS NULL
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table 'provider_keys' executed without tenant_id constraint. This query enumerates provider API keys across all tenants (no WHERE tenant_id = ...), potentially exposing cross-tenant secrets even though branch is guarded by tenantId === 1.
  - _Why false positive:_ provider_keys schema note shows no tenant_id and access is explicitly restricted to admin tenant path
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM 'provider_keys' executed without tenant_id constraint. This aggregates provider configuration across all tenants (no WHERE tenant_id = ...), leaking multi-tenant configuration information to the caller even though the branch checks tenantId === 1.
  - _Why false positive:_ provider_keys has no tenant_id; this is an admin-only global aggregate over a platform-global table
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} executed with no tenant filter. This reads a tenant-scoped table (conversations) by id without constraining to the expected tenant, which can leak/expose cross-tenant info.
  - _Why false positive:_ global read by conversation id is used only to verify claimed tenant ownership and fail closed on mismatch/ambiguity, returning no cross-tenant content
- **server/video-job-runner.ts:236** _(system-sweep)_ — recoverStaleVideoJobs performs db.update(videoJobs) to mark stale jobs without any tenant_id constraint (updates all matching rows across tenants). This mutates tenant-scoped video job rows globally.
  - _Why false positive:_ periodic stale-job recovery is platform-wide maintenance updating housekeeping status fields only, with no tenant data returned or mixed
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Query against tenant-scoped table `tool_performance` without any tenant_id constraint (SELECT count(*) FROM tool_performance WHERE last_success_at > ...). This reads across all tenants.
  - _Why false positive:_ Global maintenance/invariant check computes only aggregate count across tenants and emits internal housekeeping findings, not tenant-facing mixed content.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — COUNT(*) on `tool_performance` executed without tenant filtering (SELECT count(*) FROM tool_performance). This can leak cross-tenant telemetry/statistics.
  - _Why false positive:_ Boot-time wiring check intentionally counts total rows platform-wide for housekeeping diagnostics only; no per-tenant data returned or mixed content exposed.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Query against `messages` table without tenant filter (SELECT count(*) FROM messages WHERE created_at > ...). `messages` is tenant-scoped and this reads across all tenants.
  - _Why false positive:_ Cross-tenant message count is used only as a global traffic heuristic inside a maintenance check and outputs aggregate count only, without exposing tenant data.
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — settleFusionReservation() updates agent_cost_ledger by reservation id (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}) with no tenant_id constraint. A reservation id could be misused to alter another tenant's ledger row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE of `custom_tools` sets implementation by matching only id (where(eq(customTools.id, tool.id))) with no tenant_id constraint. This allows cross-tenant modification of another tenant's tool row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE on tenant-scoped table `custom_tools` is executed without constraining by tenant_id (await db.delete(customTools).where(eq(customTools.id, tool.id))). This can delete another tenant's tool row if IDs collide or are guessed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) via a raw SQL template without supplying tenant_id, relying only on assertProjectInTenant for an ownership check.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column (isolated via project_id FK); server/tools.ts:2969 owner check SELECT id FROM projects WHERE id=params.id AND tenant_id=tenantId gates the insert
- **server/agentic-engines.ts:533** _(global-table)_ — runOptimizationEngine queries `heartbeat_tasks` with `WHERE enabled = true` but no tenant filter (`SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ORDER BY name`). heartbeat_tasks is tenant-scoped elsewhere; this reads other tenants' task configs.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/heartbeat.ts:2628** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint — writing to a tenant-scoped table by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3080** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) lacks a tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/orchestrator-ledger.ts:450** _(upstream-verified)_ — Use of sql.raw(${lockKey}) to inject a computed advisory-lock key into SQL: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). sql.raw() is being used with a value derived from caller-provided inputs (ledgerKey / tenantId) — risk of SQL injection or unintended SQL literal injection.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/orchestrator-ledger.ts:445 — lockKey is not caller-supplied text: ledgerLockKey() (line 35-38) computes BigInt('0x'+sha1(ledgerKey:tenantId).slice(0,15)).toString(), i.e. a pure decimal-digit numeric string. sql.raw() embeds only [0-9] characters, so no SQL injection is possible. The advisory lock is keyed on (ledgerKey, tenantId) and the surrounding ledger SELECT/UPDATE/INSERT are all
