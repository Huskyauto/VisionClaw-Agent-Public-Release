# Tenant-Isolation Audit Report

_Generated 2026-07-18T23:03:08.832Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **539/539** file(s) audited · chunks 51/51 OK (0 failed)
- Findings: **297** (CRITICAL 61 · HIGH 185 · MEDIUM 50 · LOW 1)
- Precision second-pass (`gpt-5.4`): **141** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (61)

- **server/agentic/resume-worker.ts:66** — SELECT reads agent_runs JOIN agent_approvals without constraining tenant_id — the sweep pulls rows across all tenants (no WHERE tenant_id = <tenant>), exposing cross-tenant data.
  - _Fix:_ Restrict the initial candidate SELECT to a single tenant (e.g. WHERE r.tenant_id = ${tenantId}) or iterate tenants explicitly. Do NOT perform an unscoped read of tenant-scoped tables; propagate an explicit tenantId parameter and use it in the query or otherwise ensure the process only enumerates rows for authorized tenants.
- **server/agentic/runs.ts:34** — appendStep() updates agent_runs using WHERE eq(agentRuns.id, runId) but does NOT include a tenant_id constraint. This allows modifying another tenant's run row by id.
  - _Fix:_ Require and pass the caller's tenantId into appendStep (or resolve+verify tenant ownership first) and add it to the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Avoid relying on id-only updates for tenant-scoped tables.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs WHERE id = runId but omits tenant_id constraint, enabling cross-tenant state mutation by run id.
  - _Fix:_ Change the API to accept tenantId (or otherwise verify ownership) and add AND eq(agentRuns.tenantId, tenantId) to the .where() call to enforce per-tenant scope.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs WHERE id = runId with no tenant_id filter — permits cross-tenant overwrites of run state.
  - _Fix:_ Require tenantId and include it in the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all helpers that mutate tenant-scoped tables validate tenant ownership.
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs status/result by run id but lacks a tenant_id constraint in the WHERE clause, allowing completion of runs belonging to other tenants.
  - _Fix:_ Add tenant scoping to the update: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Modify the function signature to accept tenantId if necessary and always enforce tenant filtering on writes.
- **server/agentic/runs.ts:67** — failRun() updates agent_runs WHERE id = runId with no tenant_id condition, enabling cross-tenant failure markings.
  - _Fix:_ Require tenantId (or validate run ownership) and include eq(agentRuns.tenantId, tenantId) in the WHERE clause to prevent cross-tenant mutations.
- **server/agentic/runs.ts:76** — pauseRun() updates agent_runs WHERE id = runId but omits tenant_id filter; this lets callers pause runs in other tenants by id.
  - _Fix:_ Include tenant scoping in the update: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Update function signature to accept tenantId and audit call sites to pass it through.
- **server/agentic/wake-scheduler.ts:80** — UPDATE on tenant-scoped table agent_wake_schedules (claiming due schedules) does not restrict by tenant_id. The inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...) lacks any tenant_id constraint, so rows from all tenants may be claimed/updated and cause cross-tenant wake events.
  - _Fix:_ Run the scheduler per-tenant or include a tenant filter in the query. Either iterate tenants and constrain each UPDATE/SELECT with WHERE tenant_id = <tenantId>, or otherwise scope the inner SELECT/UPDATE by tenant_id. Avoid global updates across tenant-scoped tables without explicit admin intent and authorization.
- **server/auto-memorize.ts:159** — Query pulling recent messages for auto-memorize (SELECT m.role, m.content, m.created_at, c.title AS conv_title FROM messages m LEFT JOIN conversations c ON c.id = m.conversation_id WHERE m.created_at > ${windowStart} AND m.created_at <= ${windowEnd} AND m.role IN ('user','assistant') ORDER BY m.created_at ASC LIMIT 400) has no tenant_id constraint. This reads message content across all tenants and then sends transcripts to an LLM (admin tenant), which is a high-risk cross-tenant data exposure.
  - _Fix:_ Either run auto-memorize per-tenant (iterate tenants and add WHERE m.tenant_id = ${tenantId} or c.tenant_id = ${tenantId}), or otherwise implement strict admin authorization and explicit per-tenant gating. Never send multi-tenant message contents to an LLM without explicit tenant scoping and admin approval. Add explicit tenant_id WHERE clauses or redesign to process tenant-by-tenant.
- **server/channels/kernel.ts:180** — dispatchInbound calls processMessage(conversationId, ...) using conversationId returned by adapter resolveConversation(turn) without verifying that the conversation belongs to turn.tenantId. This allows a resolver to return a conversation id from another tenant and enables cross-tenant reads/writes.
  - _Fix:_ After resolveConversation returns a conversationId, verify ownership before calling processMessage. For example, fetch the conversation row and ensure its tenant_id === turn.tenantId (or have resolveConversation return both id and tenant and validate). Reject/throw if the conversation is not owned by the caller's tenant.
- **server/chat-engine.ts:4328** — Call to storage.attachCitations(...) does not include tenant context. If attachCitations inserts/updates tenant-scoped rows, it must receive the tenant_id explicitly; currently it is invoked with only the message id and citations, risking cross-tenant writes or writes that lack the required tenant_id column.
  - _Fix:_ Pass the tenant id (e.g. tenantIdForScope or tenantId) into attachCitations and ensure the storage implementation includes WHERE tenant_id = ? (or Drizzle .where(eq(table.tenantId, tenantId))) on any writes. Alternatively, validate message ownership first and have attachCitations enforce that tenant_id on the inserted rows is set to the owning tenant.
- **server/chat-engine.ts:4591** — storage.createMemoryEntry(...) is called without a tenant identifier. Per the DB contract, INSERTs into tenant-scoped tables must include tenant_id (NOT NULL, no default). This will produce tenant-less memory rows or fail and/or allow cross-tenant data leakage.
  - _Fix:_ Change extractMemory to accept and pass an explicit tenantId to storage.createMemoryEntry (e.g. include tenantId: tenantId). Ensure createMemoryEntry inserts tenant_id and the storage layer enforces the tenant filter on subsequent reads/writes.
- **server/chat-engine.ts:4599** — storage.updateMemoryEmbedding(entry.id, emb) performs an update using only the entry id and no tenant constraint. Updates to tenant-scoped rows must be constrained by tenant_id to prevent accidental cross-tenant modification.
  - _Fix:_ Modify updateMemoryEmbedding (and callers) to accept and use tenantId (or perform an ownership-checked lookup first) and ensure the SQL UPDATE includes WHERE tenant_id = ? (or Drizzle .where(eq(table.tenantId, tenantId))). If updateMemoryEmbedding cannot be changed, perform an explicit ownership check (SELECT ... WHERE id = ? AND tenant_id = ?) before updating.
- **server/embeddings.ts:684** — storeEmbeddingVec performs an UPDATE on tenant-scoped tables (memory_entries / agent_knowledge) without constraining by tenant_id (WHERE id = ${id} only). This lets a caller update rows across tenants.
  - _Fix:_ Require and pass an explicit tenantId to storeEmbeddingVec, and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(table.tenantId, tenantId)). Also avoid mixing sql.raw for value interpolation; use parameterized binding for the vector value or a typed parameter cast instead.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images without tenant constraint or ownership verification. This writes presenter slide images tied to a session_id but does not ensure the session belongs to the current tenant nor include a tenant_id column.
  - _Fix:_ Ensure tenant isolation by either (A) including tenant_id in the INSERT (and make it NOT NULL in schema) using the trusted tenantId, or (B) verify the session_id belongs to the current tenant before writing (eg. SELECT tenant_id FROM presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId} and fail if none). Do not trust sessionId from untrusted sources.
- **server/google-workspace.ts:2364** — INSERT into presenter_slide_images (thumbnail/‘thumb’) without tenant constraint or ownership verification (same pattern as prior INSERT).
  - _Fix:_ Same fix as above: include tenant_id in the INSERT or verify the presenter session belongs to the caller's tenant before persisting thumbnails. Ensure all inserts to presenter_slide_images are constrained to the tenant or otherwise validated.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — no tenant_id filter or ownership check. This allows updating a session belonging to another tenant if sessionId is attacker-controlled.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, load and validate the presenter session belongs to the caller's tenant before performing the UPDATE (and fail CLOSED if not).
- **server/heartbeat.ts:101** — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint — possible cross-tenant modification.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}), or use a storage-layer function that enforces tenant ownership (or call assertTenantContext(task.tenantId) prior to the update). Always verify tenant ownership when modifying tenant-scoped rows.
- **server/heartbeat.ts:106** — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint — possible cross-tenant modification.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a tenant-aware storage API to perform the update after asserting tenant context.
- **server/heartbeat.ts:112** — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint — possible cross-tenant modification.
  - _Fix:_ Restrict the UPDATE by tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}), or call a storage method that enforces tenant ownership.
- **server/heartbeat.ts:119** — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint in recovery path — possible cross-tenant modification.
  - _Fix:_ Include AND tenant_id = ${sched.tenant_id} in the WHERE clause (or use a storage-layer method) to ensure only the owning tenant's schedule row is updated.
- **server/heartbeat.ts:257** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} performed without tenant_id constraint — mutates heartbeat_tasks across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the change via storage.updateHeartbeatTask which should enforce tenant ownership / assertTenantContext before modifying the row.
- **server/heartbeat.ts:261** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} performed without tenant_id constraint — mutates heartbeat_tasks across tenants.
  - _Fix:_ Restrict the update with AND tenant_id = ${t.tenantId}, or use the tenant-aware storage API (after assertTenantContext) to avoid cross-tenant writes.
- **server/heartbeat.ts:2955** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint — may set approval_status on a task belonging to a different tenant if id collisions occur or storage is inconsistent.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${newTask.tenantId}), or call a tenant-aware storage method (e.g. storage.updateHeartbeatTask) after asserting tenant context to enforce ownership before mutating the row.
- **server/heartbeat.ts:3331** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint in delegateTaskFromChat path — cross-tenant write risk.
  - _Fix:_ Restrict the UPDATE with AND tenant_id = ${newTask.tenantId} or replace the direct SQL with a storage-layer call that enforces tenant ownership (assertTenantContext) before updating approval_status.
- **server/internal-resolver.ts:91** — UPDATE on tenant-scoped table `event_log` (finalizeEvent) omits a tenant_id constraint in the WHERE clause — the resolver can finalize events across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. require and pass tenantId into finalizeEvent and add `AND tenant_id = ${tenantId}`) or otherwise assert/validate the event belongs to the expected tenant before mutating. Do not allow cross-tenant finalization without explicit operator-level intent and audit.
- **server/internal-resolver.ts:106** — SELECT from tenant-scoped table `plans` by id has no tenant_id filter. The resolver reads a foreign plan row (planId from event data) without verifying the caller/actor owns that plan's tenant.
  - _Fix:_ Require tenant context/tenantId for resolving events or include `AND tenant_id = ${tenantId}` in the SELECT. Validate ownership before taking any action (or fail/park the event when tenant cannot be determined).
- **server/internal-resolver.ts:170** — UPDATE on `code_proposals` (resolveExperimentFailed) mutates a tenant-scoped row by id without constraining by tenant_id — can archive proposals across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending'`) or fetch and verify the row's tenant before mutating. Ensure finalizeEvent and other follow-up operations also respect the same tenant check.
- **server/memory-graph.ts:194** — INSERT into memory_links does not set tenant_id. The code runs: INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count) VALUES (...). Every INSERT into tenant-scoped tables must include tenant_id — omitting it lets links be created without tenant ownership and breaches tenant isolation.
  - _Fix:_ Include tenant_id in the INSERT and bind the caller's tenant (this function already accepts tenantId). Example: INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId}). Ensure the memory_links schema has tenant_id and enforce/validate it at insert time.
- **server/minerva-planner.ts:356** — UPDATE on tenant-scoped table `plans` performed without constraining by tenant_id (CAS flip from awaiting_approval). This allows modifying a plan row across tenants if called with an id only.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId} AND status = 'awaiting_approval'), or require/accept an explicit tenantId and validate the caller's authorization (or run inside a withTenantTx that enforces tenant context). Treat CAS updates as multi-tenant-aware.
- **server/minerva-planner.ts:460** — UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising' rolls back decision state but omits tenant_id scoping. This can alter plan state across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId} AND status = 'revising') or require the tenant context be enforced by the caller or a tenant transaction before rolling back fields.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code verifies project ownership via assertProjectInTenant, but the INSERT itself omits tenant_id (per project_files schema the tenant_id column is required/no default). This can break tenant isolation or create records not tied to the caller tenant.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. VALUES (${options.projectId}, ${options.tenantId}, ... ) or INSERT the tenant_id column in the column list). Keep the assertProjectInTenant check, but also set tenant_id on every row written to tenant-scoped tables.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` (in produceVideoParallel) omits tenant_id. Although the code calls assertProjectInTenant before the insert, the INSERT statement does not set tenant_id.
  - _Fix:_ Add tenant_id to the INSERT column list and pass the validated options.tenantId value. Always include tenant_id on INSERTs into tenant-scoped tables even when the caller holds a verified foreign id.
- **server/orchestrator-ledger.ts:452** — UPDATE memory_entries ... WHERE id = ${row.id} lacks a tenant_id constraint. This is a write to a tenant-scoped table without constraining to the caller's tenant, violating the tenant-isolation invariant.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) so writes are always constrained to the caller's tenant. Prefer double-checking tenant_id in the WHERE clause even if row.id was selected earlier under a tenant filter.
- **server/plan-executor.ts:775** — UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — boot recovery flips all stale 'executing' plans across ALL tenants because no tenant_id filter is applied.
  - _Fix:_ Do not run a global cross-tenant UPDATE. Either iterate per-tenant (pass tenantId) or include tenant_id = ${tenantId} in the WHERE clause. If this must be global (admin operation), gate it with explicit admin intent and audit, but never silently mutate tenant-scoped rows without explicit tenant scope.
- **server/plan-executor.ts:785** — SELECT id FROM plans WHERE status = 'approved' ... — enumerates approved plans across all tenants without tenant scoping.
  - _Fix:_ Scope pending/resumable plan enumeration by tenant_id or run the recovery per-tenant. If intended to operate across tenants, require an explicit admin-mode and document/audit it; otherwise add WHERE tenant_id = ${tenantId}.
- **server/project-brain.ts:332** — SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 — enumerates projects across all tenants (no tenant scoping).
  - _Fix:_ Scope the backfill to a specific tenant (add WHERE p.tenant_id = ${tenantId} or run the job per-tenant). Do NOT run global project discovery without explicit admin privileges and auditing.
- **server/sculptor.ts:94** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: `UPDATE sculptor_sessions SET status = 'failed', ... WHERE id = ${session.id}` — write is not constrained by tenant_id.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. `WHERE id = ${session.id} AND tenant_id = ${params.tenantId}`) or perform the update within a transaction that first verifies the row belongs to the caller's tenant. Always bind tenant_id as a parameter rather than relying on id uniqueness alone.
- **server/sculptor.ts:101** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: `UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}`.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause (pass the correct tenantId into scope) or verify the session row belongs to the caller's tenant before updating. Ensure all writes to tenant-scoped tables include an explicit tenant_id constraint.
- **server/sculptor.ts:156** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: finalization UPDATE uses `WHERE id = ${sessionId}` with no tenant_id filter.
  - _Fix:_ Constrain the update to the caller tenant (e.g. `WHERE id = ${sessionId} AND tenant_id = ${tenantId}`) so one tenant cannot modify another tenant's session rows. Prefer parameterized tenant checks or a SELECT+assert before performing the UPDATE.
- **server/sculptor.ts:347** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: `UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}`.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the WHERE clause (use the tenantId passed into reviewSessionWork) or first assert the row belongs to the tenant. All writes to tenant-scoped tables must be tenant-scoped in SQL.
- **server/second-opinion.ts:404** — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant constraint: `UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}` — this can modify another tenant's ledger row.
  - _Fix:_ Ensure the reservation update is tenant-scoped. Either accept/propagate tenantId into settleFusionReservation and use `WHERE id = ${reservationId} AND tenant_id = ${tenantId}`, or perform a guarded SELECT to verify the row's tenant_id before updating. Never update ledger rows by id alone across tenants.
- **server/second-opinion.ts:423** — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant constraint: `UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId}`.
  - _Fix:_ Constrain the release update to the reservation's tenant (e.g. `WHERE id = ${reservationId} AND tenant_id = ${tenantId}`) or verify tenant ownership before updating. Modify releaseFusionReservation to accept tenantId or otherwise assert ownership so reservations can't be released/modified across tenants.
- **server/seed.ts:2859** — UPDATE research_programs SET model = ... WHERE id = ${p.id} — UPDATE on tenant-scoped table without any tenant_id constraint. This can modify another tenant's data.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or validate the row's tenant before updating to ensure tenant isolation.
- **server/seed.ts:3136** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... — UPDATE on a tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or resolve and verify the target project's tenant before making the change.
- **server/seed.ts:3212** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) — INSERT into a tenant-scoped linking table without passing tenant_id explicitly.
  - _Fix:_ Include tenant_id explicitly in the INSERT (VALUES (${tenantId}, ${projectId}, ${conversationId}) or similar) and/or validate that both project_id and conversation_id belong to the caller's tenant before inserting to enforce tenant isolation.
- **server/seed.ts:3204** — SELECT on tenant-scoped table 'conversations' executed without constraining tenant_id. The query (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...project_conversations...)) reads conversation rows across all tenants and then inserts links using those project_id values, enabling cross-tenant data mixing.
  - _Fix:_ Add a tenant filter to the SELECT (and any subsequent INSERTs using its results). E.g. WHERE c.project_id IS NOT NULL AND c.tenant_id = ${tenantId} AND NOT EXISTS(...) and ensure any INSERT into project_conversations is derived only from rows whose tenant_id has been validated (or include tenant_id on project_conversations and set it explicitly). Validate project ownership before linking.
- **server/seed.ts:4497** — Multiple SELECT/UPDATE statements against tenant-scoped table 'heartbeat_tasks' are executed without any tenant_id constraint (count, disabling delegation tasks, cron-expression normalizations, disabling by name patterns). These writes will affect rows for all tenants, not just the intended tenant.
  - _Fix:_ Constrain all SELECT/UPDATE/DELETE on heartbeat_tasks to the caller tenant (e.g. WHERE tenant_id = ${tenantId}). If the migration is intended to run only for a specific system tenant, pass that tenant id explicitly in WHERE clauses. Never run global updates without an explicit tenant filter unless operating on a known-global table.
- **server/seed.ts:4407** — UPDATE against tenant-scoped table 'memory_entries' performed without tenant_id constraint (UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND fact LIKE ...). This will modify memory entries across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so only the intended tenant's rows are updated. If this is a system-wide normalization that must run per-tenant, iterate tenants and run the update scoped to each tenant_id.
- **server/self-improvement.ts:394** — runExperiment updates the `experiments` row via db.update(...).where(eq(experiments.id, experiment.id)) without constraining by tenant_id. This UPDATE (and the similar UPDATE in the catch block) can modify another tenant's experiment row if ids collide.
  - _Fix:_ Constrain updates to the caller's tenant. Include the tenant predicate in the WHERE clause, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) (and ensure tenantId is available in this scope). Apply the same change to the error-path UPDATE.
- **server/storage.ts:248** — createConversation inserts into tenant-scoped `conversations` with data whose tenantId is optional (values(data)). An INSERT into a tenant-scoped table may occur without an explicit tenant_id value.
  - _Fix:_ Require and validate tenantId before inserting (e.g. change signature to require tenantId and call assertValidTenantId(tenantId)). Explicitly set tenantId on the inserted row rather than relying on caller-supplied optional data. Fail closed if tenantId is missing/invalid.
- **server/storage.ts:1279** — getAllDataForExport executes many SELECTs over tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, etc.) and returns platform-wide data without any tenant constraint. This exports data across all tenants.
  - _Fix:_ This must be an explicit, strongly-gated admin-only operation. Require explicit admin authorization (not just calling code) and/or a required admin credential; or change the API to export only a single tenant (require tenantId). Add audit logging and require a distinct admin method to avoid accidental invocation from non-admin code paths.
- **server/surprise-scorer.ts:107** — scoreProposalSurprise defaults tenantId to 1 (function signature: scoreProposalSurprise(proposalId: number, tenantId: number = 1)). If callers omit tenantId, the function will operate as tenant 1 (admin) and will read/write tenant-scoped `felix_proposals` rows under that tenant by default.
  - _Fix:_ Do not default tenantId to 1. Require an explicit tenantId parameter and validate it (assertValidTenantId). Ensure all reads/writes use the provided tenantId and fail if none is supplied. Avoid implicit admin defaults that can cause cross-tenant operations.
- **server/tool-learning.ts:316** — DELETE in revertToolRow removes a custom_tools row using only the PK (eq(customTools.id, tool.id)) with no tenant_id constraint—this can delete another tenant's tool row.
  - _Fix:_ Always scope destructive operations to the tenant. Change the DELETE to include tenant_id, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId))). Ensure the function receives/validates the tenantId of the tool being reverted.
- **server/tools.ts:3197** — INSERT into project_files omits tenant_id. The statement INSERT INTO project_files (...) VALUES (...) does not set tenant_id (nor otherwise enforce tenant scope) which violates the rule that tenant-scoped writes must include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (VALUES (..., ${tenantId})) or derive it from the owning project (INSERT ... SELECT ... FROM projects p WHERE p.id = ${params.id} AND p.tenant_id = ${tenantId}) to ensure the new row is scoped to the caller's tenant. Alternatively, if project_files is not tenant-scoped, document that explicitly; otherwise make the tenant column NOT NULL and always supply it.
- **server/tools.ts:3112** — INSERT into project_notes omits tenant_id. The statement INSERT INTO project_notes (project_id, note, author) VALUES (...) does not set tenant_id or otherwise record tenant ownership.
  - _Fix:_ Set tenant_id on insert (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})) or insert via SELECT from projects to inherit/validate the project's tenant (INSERT ... SELECT ... FROM projects p WHERE p.id = ${params.id} AND p.tenant_id = ${tenantId}). Ensure project ownership is validated before inserting.
- **server/tools.ts:3094** — INSERT into project_conversations during project creation omits tenant_id. The code INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not include tenant_id, so the created linkage may not be tenant-scoped.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId})) or use INSERT ... SELECT that validates the project/conversation belong to the caller's tenant. Always write tenant_id explicitly for tenant-scoped tables.
- **server/tools.ts:3236** — INSERT into project_conversations (link_conversation path) omits tenant_id. The code INSERT INTO project_conversations (project_id, conversation_id) VALUES (${params.id}, ${params.conversationId}) is executed after project existence check but does not set tenant_id on the inserted row.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})) or perform an INSERT ... SELECT from projects to inherit/validate the tenant. Ensure all writes to tenant-scoped tables carry the caller's tenant id.
- **server/tools/domains/files/handlers.ts:836** — INSERT into tenant-scoped table project_files does not set tenant_id (INSERT INTO project_files (...) VALUES (...)) — tenant_id column is omitted, violating the requirement that every tenant-scoped INSERT include the caller's tenant id.
  - _Fix:_ Include tenant_id in the INSERT columns and pass the trusted tenant id (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ${fileName}, ...)) or use Drizzle-insert helpers with an explicit tenantId field. Validate ctx.tenantId is present and numeric before inserting.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` executed without constraining by tenant_id (SELECT id, content::jsonb FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = $1 ...). This reads rows across tenants.
  - _Fix:_ Avoid SELECTing tenant-scoped rows across all tenants. Either (a) add an explicit tenant_id filter (WHERE tenant_id = <expected tenant>) and change design accordingly, (b) store phone→conversation pairings in a global, explicitly-scoped table designed for cross-tenant lookups, or (c) enforce Postgres RLS for this use-case. If you must load multiple tenants for special contest-resolution logic, make that explicit and ensure the code runs under an audited admin-only path and performs strict verification (and minimize returned columns).
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} executed without any tenant scoping. This exposes conversation tenant mapping and is a cross-tenant read.
  - _Fix:_ Constrain the query to the expected tenant (e.g. WHERE id = ${conversationId} AND tenant_id = ${claimedTenantId}) and use the DB result only to verify ownership. Alternatively, perform this check using a single constrained existence check (SELECT 1 FROM conversations WHERE id = ${id} AND tenant_id = ${tenantId}) so you never leak other tenants' metadata. If this is an admin-only check, ensure it runs in an admin context and is auditable.

### HIGH (185)

- **server/ab-optimizer.ts:110** — Unscoped SELECT on tenant-scoped `experiments` table: the query reads experiments across all tenants (`WHERE category = 'ab_test' AND status = 'running'`) without constraining by tenant_id.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add `AND tenant_id = ${tenantId}`) or iterate per-tenant in the caller. Ensure any sweep over tenants is explicit and audited, or move per-tenant filtering into the calling heartbeat so no cross-tenant reads occur.
- **server/agent-activity.ts:122** — UPDATE against tenant-scoped `agentActivity` table without tenant constraint: `db.update(agentActivity).set(...).where(eq(agentActivity.id, activityId))` can update rows in other tenants if ids collide.
  - _Fix:_ Require the caller to pass tenantId (or otherwise resolve ownership) and include `AND tenant_id = ${tenantId}` in the WHERE clause (e.g. `.where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))`). Validate the row belongs to the caller before updating.
- **server/agent-channels.ts:194** — JOIN against `channel_subscriptions` omits tenant constraint on the subscriptions side: the query joins `channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}` but never constrains `cs.tenant_id` to the caller's tenant, allowing cross-tenant subscription rows to influence results.
  - _Fix:_ Include tenant scoping for channel_subscriptions (e.g. `AND cs.tenant_id = ${tenantId}` in the JOIN or in the WHERE). Also validate that personaId belongs to the tenant if persona ids are not globally unique.
- **server/agentic-engines.ts:114** — Unscoped SELECT on tenant-scoped `plans` table: `SELECT id FROM plans WHERE source = ${ROUTE_SOURCE} AND source_ref = ${String(insight.insightId)} LIMIT 1` lacks a tenant predicate and may return plans from other tenants.
  - _Fix:_ Add tenant filtering (e.g. `AND tenant_id = ${insight.tenantId}`) to the SELECT. Ensure idempotency checks always include tenant_id so the existence test is scoped correctly to the insight's tenant.
- **server/agentic-engines.ts:242** — UPDATE sweep on tenant-scoped `ai_insights` table without tenant constraint: `UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...` modifies rows across all tenants.
  - _Fix:_ If this operation is intended to be per-tenant, add `AND tenant_id = ${tenantId}` and run per-tenant. If it must be a global sweep, make that explicit and ensure the caller has authorization and auditing; otherwise limit to a single tenant context or perform a safe multi-tenant loop that enforces tenant scoping for each update.
- **server/agentic-engines.ts:344** — Unscoped SELECT on tenant-scoped `heartbeat_logs` table in gatherOperationalData: `SELECT COUNT(...) FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` has no tenant filter and will aggregate across tenants.
  - _Fix:_ Add `WHERE tenant_id = ${tenantId} AND ...` so the collected operational data is tenant-scoped. Avoid including global heartbeat logs unless that is explicitly intended and safe.
- **server/agentic-engines.ts:449** — Unscoped SELECT on tenant-scoped `heartbeat_logs` table in runPredictiveEngine: `SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10` lacks tenant scoping.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or an appropriate WHERE clause) so only logs for the target tenant are read. For any cross-tenant aggregation, require explicit authorization and logging.
- **server/agentic-engines.ts:516** — Unscoped aggregation on tenant-scoped `heartbeat_logs` in runOptimizationEngine: `SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...` has no tenant filter and will return multi-tenant data.
  - _Fix:_ Limit the aggregation to the target tenant by adding `WHERE tenant_id = ${tenantId}` (and any other predicates). Ensure aggregated metrics are always computed within tenant scope.
- **server/agentic-engines.ts:531** — SELECT on `heartbeat_tasks` (likely tenant-scoped) without tenant filter: `SELECT name, cron_expression, enabled, last_run_at, run_all FROM research_schedules WHERE tenant_id = ${tenantId}` followed by `SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ORDER BY name` — the heartbeat_tasks query is not scoped to tenant.
  - _Fix:_ Add `WHERE tenant_id = ${tenantId}` to the heartbeat_tasks query so task configuration is read only for the intended tenant. If heartbeat_tasks are intentionally global, document that and ensure it is safe.
- **server/agentic-features.ts:274** — Subquery against `competitor_snapshots` omits tenant filter: inner `SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY url, created_at DESC` lacks `tenant_id` and can reference snapshots from other tenants, causing incorrect NOT IN logic and cross-tenant reads.
  - _Fix:_ Include `AND tenant_id = ${params.tenantId}` in the subquery (and any other subqueries touching tenant-scoped tables). Always scope subqueries by tenant to avoid cross-tenant interactions.
- **server/agentic-features.ts:599** — Existence check on `outreach_enrollments` lacks tenant scoping: `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` does not ensure the enrollment belongs to the caller's tenant.
  - _Fix:_ Add `AND tenant_id = ${params.tenantId}` to the SELECT so the check only considers enrollments within the tenant. Validate the sequence belongs to the tenant as well before checking enrollments.
- **server/agentic-features.ts:721** — UPDATE on `outreach_enrollments` performed with `WHERE id = ${row.id}` only; no tenant constraint is included. This can update an enrollment row in another tenant if ids collide or if `row.id` came from untrusted source.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${row.id} AND tenant_id = ${params.tenantId}`) or verify the enrollment's tenant before updating. Prefer parameterizing tenantId in all updates to tenant-scoped tables.
- **server/agentic/approvals.ts:122** — Bulk UPDATE in expireStaleApprovals misses tenant scoping: `db.update(agentApprovals).set({...}).where(and(eq(agentApprovals.status, 'pending'), lt(agentApprovals.expiresAt, new Date())))` updates pending approvals across all tenants.
  - _Fix:_ If expiry should be per-tenant, add `AND tenant_id = ${tenantId}` and perform per-tenant sweeps. If this is an intentional global sweep, make it explicit and ensure only properly authorized system code executes it; otherwise restrict the update to a tenant context to avoid cross-tenant state changes.
- **server/agentic/department-budgets.ts:143** — Query selects across the tenant-scoped table `department_budgets` without constraining to a specific tenant (missing WHERE tenant_id = ...). This reads tenant data for all tenants.
  - _Fix:_ If this is intended as an administrative multi-tenant sweep, make the intent explicit and restrict execution to a privileged admin job (validate caller is allowed) and/or use an explicit admin tenant constant. Otherwise, add a WHERE tenant_id = ${tenantId} filter (and pass the tenantId) so the query only returns rows for the calling tenant. Ensure any cross-tenant operations are only performed by audited, authorized admin processes.
- **server/agentic/harness-adaptation.ts:71** — Query selects from `agent_trace_spans` with no tenant filter (missing WHERE tenant_id = ...). This mines trace spans across all tenants, exposing cross-tenant data.
  - _Fix:_ Either restrict the query to a specific tenant (add WHERE tenant_id = ${tenantId} and pass the tenant id) or, if a global admin scan is required, run it under an explicit admin context and document/authorize that behavior. Avoid unscoped reads on tenant-scoped tables from non-admin code paths; require explicit admin credentialing and auditing for any cross-tenant queries.
- **server/agentic/task-forces.ts:40** — createTaskForce() inserts project_id (projectId) supplied by the caller directly into task_forces without validating that the referenced project belongs to the caller's tenant. Because there are no DB FK/cascade protections, this can associate a task force with a foreign-tenant project.
  - _Fix:_ Before inserting, validate ownership of the foreign id: SELECT the project by id AND tenant_id = ${p.tenantId} (or otherwise verify the project belongs to the tenant). Reject or clear projectId when the project does not belong to the tenant. Prefer performing this check server-side and include the tenant_id constraint in the verification query.
- **server/auto-asset-capture.ts:184** — Backfill query SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... scans the conversations table with no tenant_id constraint. This reads rows across all tenants in one query.
  - _Fix:_ If this is intended to be per-tenant work, change the scan to iterate tenants (or accept a tenantId parameter) and add WHERE tenant_id = ${tenantId}. If it's an admin/global maintenance, make the cross-tenant intent explicit and ensure only authorised admin contexts execute it and that subsequent processing never mixes tenant data across tenants.
- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} ... queries the messages table without a tenant_id constraint. Even though conversation rows were previously selected, the messages read itself has no tenant_id filter, violating the requirement that tenant-scoped reads include tenant constraints.
  - _Fix:_ Include tenant scoping on messages reads, e.g. add AND tenant_id = ${c.tenant_id} (or iterate per-tenant) or ensure an explicit ownership assertion (and code-level invariant) before reading. Prefer explicit WHERE tenant_id checks rather than relying purely on foreign-key ids.
- **server/auto-memorize.ts:100** — SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ... reads recent memory_entries without any tenant_id constraint for duplication checks. This can compare and dedupe facts across tenants, leaking cross-tenant signals to the dedup logic.
  - _Fix:_ Scope the dedupe probe by tenant (WHERE tenant_id = ${tenantId}) or run the dedupe per-tenant. If a global dedupe is intentional, require explicit admin-only context and strong justification; otherwise enforce tenant_id filtering.
- **server/auto-transcript.ts:165** — UPDATE against tenant-scoped table 'conversations' uses only the row id in the WHERE clause (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL). There's no tenant_id constraint, so a poisoned/foreign conversation id could cause a cross-tenant update.
  - _Fix:_ Constrain the UPDATE to the owning tenant. For example: UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}. Alternatively, re-check/Assert ownership (e.g. assertConversationInTenant) immediately before the UPDATE so the write cannot affect another tenant's rows.
- **server/auto-transcript.ts:179** — INSERT into project_conversations (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id}) ON CONFLICT DO NOTHING) uses project_id and conversation_id derived from a prior query but does not verify the project actually belongs to the same tenant. The insert can create cross-tenant links if the project_id is poisoned or stale.
  - _Fix:_ Validate ownership before inserting. Either (a) ensure the project belongs to the same tenant (SELECT 1 FROM projects WHERE id = ${r.project_id} AND tenant_id = ${r.tenant_id} and only then insert), or (b) include an explicit tenant identifier on the association (if schema permits) and constrain/INSERT via a SELECT that joins projects ON tenant_id = ${r.tenant_id}. Do not insert using foreign ids without an explicit tenant-ownership check.
- **server/business-tools.ts:84** — INSERT into invoice_items does not include tenant_id for a likely tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT into invoice_items (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)). If invoice_items is intentionally not tenant-scoped, document that explicitly; otherwise fail-closed by requiring and passing the caller's tenant id.
- **server/business-tools.ts:334** — LEFT JOIN to customers in listContracts does not constrain the joined customers row by tenant_id — customers.company_name can leak data from another tenant.
  - _Fix:_ Restrict the joined customers row to the same tenant (e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}) or SELECT customer fields via a separate tenant-scoped query. Always enforce tenant_id on joined tenant-scoped tables to prevent cross-tenant leaks.
- **server/ceo-orchestrator.ts:821** — SELECT from tenant-scoped personas table without restricting by tenant. cachedPersonas = await db.select().from(personasTable) reads all personas across tenants.
  - _Fix:_ Filter the query by the plan/ caller tenant (e.g. .where(eq(personasTable.tenant_id, plan.tenantId)) or add a tenantId parameter). Only load personas for the specific tenant, or scope cachedPersonas per-tenant.
- **server/channel-routing.ts:30** — listChannelRoutes performs a JOIN against personas (tenant-scoped) with no tenant constraint: SELECT ... FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ... — this reads persona rows across tenants.
  - _Fix:_ Make channel routing tenant-aware (add tenant_id to channel_routes and filter by it) or, if channel_routes is global by design, explicitly join/filter personas by the intended tenant (e.g. WHERE p.tenant_id = ${tenantId}). Add a tenantId parameter to the API so queries can enforce tenant isolation.
- **server/compaction.ts:400** — archiveMessages inserts into compaction_archives without supplying tenant_id. If compaction_archives is a tenant-scoped table (tenant_id non-null), this INSERT omits the tenant context and may violate tenant isolation or fail/associate archives incorrectly.
  - _Fix:_ Include and validate tenant_id when inserting compaction archives (e.g. INSERT ... (tenant_id, conversation_id, ...) VALUES (${tenantId}, ${convId}, ...)). If compaction_archives is intentionally global, document that and ensure subsequent reads always enforce tenant ownership (JOIN on conversations.tenant_id) and that the schema/constraints reflect the intended model.
- **server/data-protection.ts:72** — UPDATE against tenant-scoped table `conversations` executed without scoping by tenant_id (WHERE id = ${conversationId} but no AND tenant_id = ${tenantId}). This allows modifying a conversation row even if the caller's tenant doesn't own it.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or perform the UPDATE inside a transaction that asserts the conversation belongs to the caller before mutating it.
- **server/data-protection.ts:125** — DELETE statements in purge loop run without tenant_id scoping (DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}). These DELETEs touch tenant-scoped tables without an explicit tenant predicate.
  - _Fix:_ Include tenant_id in the WHERE clauses (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}) to ensure the deleted rows belong to the expected tenant, or perform the deletes in a transaction after verifying tenant ownership.
- **server/delivery-pipeline.ts:188** — UPDATE on tenant-scoped table `delivery_logs` executed without any tenant_id constraint (db.update(deliveryLogs).set(...).where(eq(deliveryLogs.id, id))). This allows modifying a delivery record by id regardless of caller tenant.
  - _Fix:_ Require and apply a tenant filter when updating delivery_logs. Either accept a tenantId parameter or look up+verify ownership first, e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). Refuse or verify the operation if tenantId is not available.
- **server/delivery-pipeline.ts:683** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId has no tenant_id constraint (.where(eq(deliveryLogs.stripePaymentId, req.stripePaymentId))). This can leak or mix deliveries across tenants.
  - _Fix:_ Scope this lookup to the caller's tenant (or require an explicit trusted internal context). Add tenant filtering: .where(and(eq(deliveryLogs.stripePaymentId, req.stripePaymentId), eq(deliveryLogs.tenantId, req.tenantId))). If cross-tenant/global behavior is intended, make it explicit and only callable by a trusted internal path.
- **server/delivery-pipeline.ts:720** — UPDATE that clears stripePaymentId on a prior delivery uses .where(eq(deliveryLogs.id, prior.id)) with no tenant_id constraint, allowing cross-tenant mutation of delivery rows.
  - _Fix:_ Include tenant scoping in the UPDATE: .where(and(eq(deliveryLogs.id, prior.id), eq(deliveryLogs.tenantId, prior.tenantId || <expectedTenant>))). Ensure you verify the tenant before releasing/altering stripePaymentId.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment SELECTs from tenant-scoped `delivery_logs` using only stripePaymentId (WHERE stripe_payment_id = ${paymentId}) with no tenant filter, so it can return another tenant's delivery.
  - _Fix:_ Add tenant scoping to this function (accept a tenantId param) and filter by tenant_id in the WHERE clause, or otherwise restrict this API to trusted internal callers only and document the trust boundary.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks executed without tenant filter (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). doc_chunks is tenant-scoped; this can delete rows belonging to other tenants if collection_id is reused or mismatched.
  - _Fix:_ Include tenant scoping in the DELETE: add AND tenant_id = ${tenantId}. Alternatively validate collection ownership first and run the delete under that tenant context.
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table `doc_chunks` sets embedding by id only (UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}) with no tenant_id constraint. This can overwrite another tenant's chunk if ids collide or are guessed.
  - _Fix:_ Include tenant filtering in the UPDATE (AND tenant_id = ${tenantId}) or perform the UPDATE within a transaction after verifying chunk ownership. Prefer parametrized Drizzle update patterns that accept tenantId.
- **server/email.ts:366** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 queries tenant-scoped `inbox_messages` without tenant_id constraint when polling shared inbox. This can cause cross-tenant message visibility or incorrect deduplication.
  - _Fix:_ Include tenant scoping in the lookup, e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}. If tenant is not yet known (shared inbox), resolve tenant from the message metadata first, then filter by tenant_id.
- **server/email.ts:410** — Backfill SELECT across inbox_messages: SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) has no tenant condition, scanning all tenants' messages.
  - _Fix:_ Limit backfill to a specific tenant (add AND tenant_id = ${tenantId}) or run per-tenant batches after resolving tenant context. Avoid scanning tenant-scoped tables across tenants.
- **server/email.ts:424** — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} is executed without a tenant_id constraint. Updating by id alone can modify another tenant's row if ids are misused or guessed.
  - _Fix:_ Include tenant scoping in UPDATEs as well (AND tenant_id = ${tenantId}), or re-query and verify the tenant ownership before updating. Prefer parameterized updates that combine id + tenant_id in the WHERE clause.
- **server/embeddings.ts:688** — backfillMissingKnowledgeEmbeddings selects rows from agent_knowledge without any tenant_id filter (SELECT ... FROM agent_knowledge WHERE embedding IS NULL ...). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Run backfill per-tenant or otherwise scope the query to a specific tenant (WHERE tenant_id = ${tenantId}). If this job must be global, run it in an explicit admin context with strong audit/consent and ensure updates also include tenant scoping to avoid cross-tenant writes.
- **server/embeddings.ts:716** — backfillEmbeddingVecs selects from and updates memory_entries without a tenant_id constraint (SELECT id, embedding FROM memory_entries ...; UPDATE memory_entries SET ... WHERE id = ${row.id}). This reads and writes tenant-scoped rows across all tenants.
  - _Fix:_ Scope both the SELECT and UPDATE to tenant_id (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant and include tenant_id in the UPDATE (WHERE id = ${id} AND tenant_id = ${tenantId}). Avoid global queries against tenant-scoped tables.
- **server/embeddings.ts:732** — backfillEmbeddingVecs selects from and updates agent_knowledge without a tenant_id constraint (SELECT id, embedding FROM agent_knowledge ...; UPDATE agent_knowledge SET ... WHERE id = ${row.id}). This touches tenant-scoped knowledge rows across tenants.
  - _Fix:_ As above, iterate per-tenant or include tenant_id in both SELECT and UPDATE (WHERE id = ${id} AND tenant_id = ${tenantId}). Ensure background jobs do not perform un-scoped writes on tenant tables.
- **server/felix-brain.ts:19** — rehydrateTaskStateOnBoot runs a SELECT against agent_runs without scoping by tenant_id (SELECT ... FROM agent_runs WHERE status IN ('running','pending') ...). This reads tenant-scoped rows across all tenants into process memory and may leak cross-tenant data into the in-memory taskStateStore.
  - _Fix:_ Restrict the query to a single tenant at a time (e.g. iterate tenant ids and add WHERE tenant_id = ${tenantId}), or explicitly document and enforce that this admin boot-time job runs in an authorized admin context and that no per-tenant data is mixed. Prefer per-tenant rehydration to avoid cross-tenant state leakage.
- **server/firecrawl.ts:192** — UPDATE on scraped_pages uses WHERE id = <existing id> but does NOT include tenant_id constraint. This allows an attacker who knows or guesses a global row id to update another tenant's row because the UPDATE isn't explicitly scoped to the caller's tenant.
  - _Fix:_ Add an explicit tenant filter to the UPDATE WHERE clause (or include the tenant_id in the equality), e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Alternatively run the SELECT+UPDATE in a transaction and re-check tenant_id before update to ensure ownership.
- **server/firecrawl.ts:316** — UPDATE on scraped_pages uses WHERE id = <existing id> but omits tenant_id scoping. Although the row was selected earlier with a tenant filter, the subsequent UPDATE by id alone lacks an explicit tenant constraint and could modify a row outside the caller's tenant if ids are predictable or leaked.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Prefer always including tenant_id in WHERE clauses for tenant-scoped tables rather than relying solely on prior SELECTs.
- **server/health-audit.ts:314** — SELECT FROM code_proposals without tenant filter. code_proposals appears to be tenant-scoped but the query scans stale proposals globally (no WHERE tenant_id = ...).
  - _Fix:_ If this scanner is intended to be an admin-only global job, make that explicit (require an admin-only guard) and document it. Otherwise restrict the query to the intended tenant with WHERE tenant_id = ${tenantId} (or iterate tenants server-side with explicit admin privileges). Avoid blanket SELECTs across tenant-scoped tables from non-admin paths.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — no tenant constraint. This may archive proposals across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the UPDATE only after verifying each proposal id belongs to the intended tenant. If this is an admin-only batch archive, require explicit admin authorization and audit/log the operation.
- **server/heartbeat.ts:52** — Unscoped SELECT from tenant-scoped table research_schedules — query selects across all tenants (no tenant_id filter).
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE tenant_id = ${someTenantId} AND ...) or iterate per-tenant and call a tenant-scoped storage helper. If this is intended to be multi-tenant system work, make the multi-tenant nature explicit and ensure subsequent actions verify and assert tenant context (assertTenantContext) before any row mutation.
- **server/heartbeat.ts:588** — Unscoped SELECT from tenant-scoped table event_log (SELECT * FROM event_log WHERE status = 'pending') — reads pending events across all tenants without explicit tenant filter.
  - _Fix:_ If the worker must process events per-tenant, query per-tenant (WHERE tenant_id = ${tenantId} AND status = 'pending') or otherwise clearly document/authorize cross-tenant processing and ensure every subsequent write/update is guarded by tenant_id checks (e.g. use WHERE id = ${event.id} AND tenant_id = ${event.tenant_id}). Prefer a tenant-scoped fetch interface in the storage layer when possible.
- **server/job-queue.ts:98** — Atomic claim query (claimDueJobs) updates `agent_jobs` across all rows without a tenant_id constraint. This will claim jobs for any tenant even when running in a tenant-specific context or when callers expect tenant separation.
  - _Fix:_ If claims should be tenant-scoped, accept a tenantId and add `AND tenant_id = ${tenantId}` to the inner SELECT/outer UPDATE. If this is intentionally global system behaviour, document and restrict which callers may invoke it and add audit/tracing to prove explicit intent. Prefer explicit tenant parameters where possible.
- **server/job-queue.ts:134** — reclaimExpiredLeases updates `agent_jobs` (marking expired leases terminal / requeued) without any tenant_id constraint — touches jobs for all tenants in bulk.
  - _Fix:_ If reclaim should operate per-tenant, accept tenantId and include `AND tenant_id = ${tenantId}`; otherwise ensure this global operation is intentional and explicitly audited/guarded. Avoid blind, unscoped updates to tenant-scoped tables.
- **server/job-queue.ts:177** — completeJob updates `agent_jobs` to 'succeeded' by id but without verifying tenant_id. A stale/forged id could allow cross-tenant row transitions.
  - _Fix:_ Require and check tenantId (or include an attempts+tenant_id fence) in the WHERE clause (e.g. `WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}`). Add caller-provided tenant validation to preserve isolation.
- **server/job-queue.ts:220** — failJob reads `agent_jobs` max_attempts via SELECT without scoping by tenant_id, then later updates the same table without tenant_id. Reads/updates are not tenant-scoped.
  - _Fix:_ Pass tenantId into failJob and constrain both the SELECT and subsequent UPDATEs with `AND tenant_id = ${tenantId}` (or use a SELECT ... FOR UPDATE that includes tenant_id) to ensure only the intended tenant's job row is observed/modified.
- **server/job-queue.ts:237** — failJob terminal-update writes `agent_jobs` to 'failed_terminal' without tenant_id in WHERE clause — risk of cross-tenant mutation.
  - _Fix:_ Include tenant_id in the WHERE clause (and/or require tenantId param) when transitioning to failed_terminal. Enforce lease-fencing together with tenant_id to avoid races and cross-tenant impact.
- **server/job-queue.ts:257** — failJob retry-update writes `agent_jobs` back to pending (with next_run_at) without tenant_id in the WHERE clause — unscoped updates across tenants.
  - _Fix:_ Constrain the UPDATE by tenant_id and attempts (and require tenantId parameter) so retries only affect the job's tenant. Prefer parameterized tenant scoping for all updates to tenant-scoped tables.
- **server/job-queue.ts:273** — cancelJob updates `agent_jobs` by id to 'cancelled' without any tenant_id constraint — may cancel jobs belonging to other tenants.
  - _Fix:_ Require tenantId and include `AND tenant_id = ${tenantId}` in the WHERE clause, or verify ownership before the update. Prevent mass cancellation of unrelated tenant jobs from a single call that lacks tenant scoping.
- **server/job-queue.ts:282** — retryJob resets terminal/failed job rows to pending without constraining by tenant_id — unscoped cross-tenant mutation is possible.
  - _Fix:_ Add a tenantId parameter and include `AND tenant_id = ${tenantId}` in the WHERE clause so retries are tenant-scoped, or enforce operator-only privileges and strong audit for intentional global retries.
- **server/job-queue.ts:385** — getJobStats aggregates counts from `agent_jobs` over the last 7 days without tenant_id filtering, returning cross-tenant global stats that may be used incorrectly by tenant-scoped callers.
  - _Fix:_ If this API is intended to return global (operator) stats, ensure it is guarded and documented as an admin-only endpoint. Otherwise add an optional tenantId parameter and use `WHERE tenant_id = ${tenantId} AND created_at > ...` for tenant-scoped stats.
- **server/lib/agent-trace.ts:129** — UPDATE to tenant-scoped table agent_trace_spans omits tenant_id predicate. persistSpanClose() issues an UPDATE WHERE span_id = ${spanId} with no tenant_id filter, allowing a close operation to modify another tenant's span row.
  - _Fix:_ Bind and check the tenant on close. Persist the tenantId at open time (e.g. store spanId→tenantId in the open-tracking map) and include AND tenant_id = <that tenant> in the UPDATE WHERE clause. Alternatively, change persistSpanClose to accept the tenantId and assert it matches the row before updating. Do NOT rely on span_id uniqueness alone.
- **server/lib/inbox-ingest.ts:444** — SELECT from inbox_messages (orphan lookup) lacks tenant_id constraint. The query `SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ON c.inbox_message_id = m.id WHERE m.message_id = ${id} AND c.id IS NULL LIMIT 1` can return a row belonging to a different tenant because it does not restrict by tenant_id.
  - _Fix:_ Add an explicit tenant filter to the WHERE clause (e.g. `AND m.tenant_id = ${tenantId}`) so the orphan lookup only returns rows for the caller's tenant.
- **server/lib/inbox-ingest.ts:504** — UPDATE to inbox_classifications omits tenant_id constraint. The `UPDATE inbox_classifications SET routed_to = ${...} WHERE inbox_message_id = ${storeResult.id}` updates solely by inbox_message_id without verifying tenant, allowing cross-tenant modification if an ID is known.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}`) to ensure only the target tenant's row is modified.
- **server/lib/scheduled-post-runner.ts:60** — SELECT ... FROM scheduled_posts (CTE) pulls and locks rows across all tenants with no tenant_id constraint. This allows the system runner to read tenant-scoped rows without an explicit tenant scoping check.
  - _Fix:_ Restrict the poll to an explicit tenant (WHERE tenant_id = ${tenantId}) when running on behalf of a tenant, or if the runner must operate across tenants, iterate per-tenant (or otherwise record and pass the targeted tenant_id) so every SELECT is explicitly scoped. At minimum, document and centralize the admin/system privilege and require an explicit admin flag rather than silently omitting tenant_id.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} executes without also constraining by tenant_id. Updating by id alone can modify a tenant-scoped row without verifying the tenant context.
  - _Fix:_ Include tenant scoping in UPDATE WHERE clauses (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or otherwise assert the runner's tenant context explicitly before performing the update. Ensure the tenant_id value used comes from a trusted source (the row returned by the locked SELECT) and is used in the UPDATE predicate.
- **server/lib/scheduled-post-runner.ts:191** — Error-path UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' lacks tenant_id filtering. This can persist status/result changes to rows without verifying tenant ownership.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} (or equivalent) to the WHERE clause so the update is constrained to the intended tenant. Prefer using the tenant_id previously read from the locked row rather than any ambient context.
- **server/memory-graph.ts:125** — UPDATE query computes memory_count from memory_entries but the subquery lacks a tenant_id filter: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND status = 'active' AND deleted_at IS NULL) WHERE id = ${categoryId}. This reads a tenant-scoped table without constraining to the intended tenant.
  - _Fix:_ Pass/derive the tenantId and include it in the subquery (or join via memory_categories to scope by tenant). Example fixes: add a tenantId parameter and use AND tenant_id = ${tenantId} in the subquery, or SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = (SELECT tenant_id FROM memory_categories WHERE id = ${categoryId}) AND ... .
- **server/memory-graph.ts:181** — SELECT existence check against memory_links omits tenant scoping: SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR (...). This reads a tenant-scoped table without a tenant_id constraint.
  - _Fix:_ Constrain the SELECT to the current tenant (e.g. AND tenant_id = ${tenantId}), or join through a tenant-scoped table (memory_entries) to ensure the link belongs to the expected tenant. Always bind tenantId explicitly instead of relying on prior context.
- **server/memory-graph.ts:66** — UPDATE memory_categories increments memory_count by id only (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}) without a tenant_id clause. This updates a tenant-scoped table row without verifying tenant ownership in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE. Either pass tenantId into this function and add AND tenant_id = ${tenantId} to the WHERE clause, or perform the update via a join that ensures the category belongs to the expected tenant (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}).
- **server/memory-graph.ts:392** — UPDATE to memory_entries in categorizeExistingMemories uses only id in the WHERE clause: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}. This performs a tenant-scoped write without constraining to tenant_id.
  - _Fix:_ Restrict the UPDATE to the caller tenant. Add the tenantId into the function signature and update using WHERE id = ${mem.id} AND tenant_id = ${tenantId} (or otherwise derive tenant from context) so writes cannot affect other tenants' rows.
- **server/minerva-planner.ts:373** — SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} executes against tenant-scoped table `plans` with no tenant_id constraint. This probe can leak or read plan state across tenants.
  - _Fix:_ Restrict the probe by tenant (e.g. SELECT ... FROM plans WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId}) or require callers to pass a tenantId and/or execute inside a tenant-scoped transaction. Avoid global reads on tenant tables.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ... reads tenant-scoped `plans` without tenant_id. This can discover or return child plans that belong to other tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${expectedTenantId}) or derive tenant from the parent row (which itself must be tenant-validated) before querying children.
- **server/minerva-planner.ts:508** — getPlan(): the branch that runs `SELECT * FROM plans WHERE id = ${planId}` performs an unconstrained read against tenant-scoped `plans` when tenantId is omitted. Allowing an unconstrained read opens possibility of cross-tenant data access if invoked incorrectly.
  - _Fix:_ Remove the unconstrained branch or make omission only possible under a clearly authenticated server-internal context and document plus restrict its use. Prefer requiring tenantId or executing the read inside a tenant-scoped transaction (withTenantTx). If the unconstrained path is required, add an explicit guard (e.g. only callable from internal/privileged code paths) and audit calls.
- **server/minds-engine.ts:214** — INSERT INTO mind_tickets builds a Postgres array literal via string interpolation: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. The dependsOn array is request-derived and is embedded into SQL text context (even though placed inside a ${} interpolation), which is fragile and could lead to injection or isolation issues.
  - _Fix:_ Avoid constructing SQL array literals by concatenating user input. Use proper parameterized array binding (pass the JS array directly if the driver/ORM supports it, or use UNNEST with parameterized values). E.g. use WHERE id = ANY(${someArray}::int[]) with someArray as a bound parameter or use Drizzle's array helpers to safely bind arrays.
- **server/minds-engine.ts:327** — UPDATE mind_events ... WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) uses a string-built array literal from request-provided eventIds. Constructing an array literal via string interpolation from caller input is an injection and isolation risk.
  - _Fix:_ Bind the array as a parameter instead of building '{...}' text. Use parameterized array binding (e.g. WHERE id = ANY(${eventIds}::int[]) with eventIds as a bound parameter or use an unnest-based parameterized approach) or use Drizzle's typed array support so the DB driver sends the array safely rather than embedding text.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` with no tenant_id constraint (startup validation: `SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE`). This reads rows across all tenants (including sensitive token fields) without per-tenant filtering.
  - _Fix:_ If this is intended (admin/global maintenance), document the privilege boundary and limit exposed columns (avoid selecting refresh_token/access_token unless strictly needed). Otherwise add a tenant_id filter to scope the query to a single tenant. Prefer iterating tenants via a controlled admin pathway that explicitly acknowledges cross-tenant access and minimizes returned sensitive fields.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint (checking for existing google-workspace rows: `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'`). This returns data across tenants.
  - _Fix:_ If scanning across tenants is necessary for an admin startup routine, restrict the operation to admin-only context, or otherwise limit and validate the returned data. If the intent is to act per-tenant, add WHERE tenant_id = ${tenantId} or enumerate tenants in a controlled, auditable manner.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh reads all rows from tenant-scoped `oauth_subscriptions` (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) without scoping to a specific tenant. Background jobs enumerating tenant rows can surface or act on sensitive tokens across tenants.
  - _Fix:_ Either scope these refresh operations explicitly per-tenant or ensure the background job runs with an explicit admin privilege, documents the reasons for cross-tenant reads, and avoids selecting sensitive token columns unnecessarily. Add auditing and strict access controls if cross-tenant enumeration is required.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions issues a SELECT across tenants (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE) without a tenant_id constraint. This enumerates tenant-scoped rows for a global operation.
  - _Fix:_ If global refresh is required, run this as a clearly privileged admin job and avoid returning sensitive columns; if not, rework to only refresh subscriptions for an explicitly-specified tenant. Add safeguards (authorization checks, audit logging) and avoid implicit cross-tenant reads in code paths that might be reachable from less-privileged contexts.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(${lockKey}) in advisory-lock SELECT. lockKey is derived from ledgerKey/tenantId (caller-supplied) and is interpolated via sql.raw(), allowing SQL-injection-like risks and violating the rule prohibiting raw templated SQL with request-derived values.
  - _Fix:_ Avoid sql.raw() with values derived from caller input. Validate/normalize ledgerKey/tenantId, convert to a numeric BigInt, and pass it as a bound parameter (e.g. sql`SELECT pg_advisory_xact_lock(${BigInt(safeLockKey)}::bigint)`), or use parameterized driver APIs. Add strict validation that produces a numeric BigInt before binding.
- **server/pace-control.ts:85** — Query against heartbeat_logs omits tenant_id. The COUNT(*) query on heartbeat_logs runs without scoping to a tenant, allowing cross-tenant observation/behavior and violating the requirement that tenant-scoped tables must be filtered by tenant_id.
  - _Fix:_ Require a tenantId parameter (or explicitly restrict this to admin contexts) and add AND tenant_id = ${tenantId} to the WHERE clause. If global aggregation is intended, make the intent explicit and restrict the API to privileged/admin users only.
- **server/pace-control.ts:140** — getPaceSnapshot's aggregation query against heartbeat_logs has no tenant_id filter, reading tenant-scoped rows globally.
  - _Fix:_ Scope the query by tenant_id (AND tenant_id = ${tenantId}) or provide an admin-only path for global metrics. Ensure all heartbeat_logs reads include tenant scoping unless intentionally global and access-controlled.
- **server/pace-control.ts:147** — Persona breakdown query (by persona_name) against heartbeat_logs is not tenant-scoped. It can expose per-tenant usage cross-tenant if heartbeat_logs is tenant-scoped.
  - _Fix:_ Add tenant scoping to the WHERE clause (WHERE tenant_id = ${tenantId} AND created_at > ...) or restrict the endpoint to admin-only / explicitly non-tenant data aggregation scenarios.
- **server/plan-executor.ts:117** — UPDATE plans ... WHERE id = ${planId} (appendExecutionLog) updates a tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Pass the plan's tenantId into appendExecutionLog or fetch it safely before updating. Prefer Drizzle-style predicates (e.g. .where(eq(plans.tenantId, tenantId))).
- **server/plan-executor.ts:513** — UPDATE plans SET status='executing' WHERE id = ${planId} ... RETURNING ... — claiming/executing a plan by id without scoping to tenant_id.
  - _Fix:_ Include AND tenant_id = ${expectedTenantId} in the WHERE clause or resolve/verify tenant ownership immediately before the UPDATE. Ensure executePlan receives or verifies the caller/owner tenant and uses that value in the SQL filter (or use Drizzle eq on tenantId).
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — writes to tenant-scoped plans table without tenant_id constraint.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or accept tenantId as a parameter). Always constrain plan mutations to the owning tenant to avoid cross-tenant state changes.
- **server/plan-executor.ts:725** — UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} — final status update lacks tenant_id constraint.
  - _Fix:_ Include the plan's tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}). Pass tenantId through the call path so every plans mutation is tenant-scoped.
- **server/plan-executor.ts:759** — UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' — error-path write missing tenant_id constraint.
  - _Fix:_ Constrain the update to the plan's tenant (AND tenant_id = ${tenantId}). Don't rely on id-only filters for tenant-scoped tables.
- **server/process-governor.ts:239** — SELECT COUNT(DISTINCT persona_id) as failing_agents FROM heartbeat_logs ... — reads heartbeat_logs without filtering by tenant_id (case 'cascading_failures'), potentially aggregating across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause. All queries against tenant-scoped audit/log tables must include tenant_id to avoid cross-tenant leakage.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — disabling a task by id without constraining tenant_id.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}). Even when id was obtained from a previous tenant-scoped query, writes must re-check tenant ownership in the WHERE clause to prevent races or ID confusion.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} — mutating agent_desks by id with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId}. Ensure executeAction receives tenantId and uses it on all writes to tenant-scoped tables.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — updates governance_rules without tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (the rule object should carry tenantId or the caller must provide it). All mutating operations on governance_rules must verify tenant ownership in SQL.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} — reading project metadata by id without tenant_id filter.
  - _Fix:_ Require and pass tenantId into updateProjectBrain and add WHERE id = ${projectId} AND tenant_id = ${tenantId}. Always verify project rows belong to the caller's tenant before reading/writing associated resources or filesystem artifacts.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — enumerating conversations without tenant filter (inside backfill), may cross tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or run per-tenant). Ensure conversation/project selects are always tenant-scoped.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — reading messages without tenant constraint (tenant-scoped messages table).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or otherwise ensure conversation belongs to the tenant before querying messages.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} — fetching a code proposal by id without tenant_id filtering (tenant-scoped table).
  - _Fix:_ Require tenantId (or validate ownership after fetch) and include AND tenant_id = ${tenantId} in the SELECT. Avoid operating on proposals without verifying they belong to the caller's tenant.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... - 'clear rebased_at' update uses WHERE id = ${proposalId} with no tenant constraint, mutating proposals across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE. Pass tenantId into the verifier or fetch and confirm ownership before applying any updates.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} — attemptRebaseOnDrift marks proposals rejected without tenant scoping.
  - _Fix:_ Constrain the UPDATE by tenant_id and ensure the operation only touches proposals owned by the tenant (WHERE id = ${proposalId} AND tenant_id = ${tenantId}).
- **server/proposal-verifier.ts:396** — UPDATE code_proposals ... SET validation_result = ... WHERE id = ${proposalId} — marking 'hallucinated' rebases without tenant filter.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause. All updates to code_proposals must verify tenant ownership in SQL.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ... WHERE id = ${proposalId} — writing rebased diff back to proposal without tenant constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE and require a tenantId parameter for attemptRebaseOnDrift so changes are applied only to proposals belonging to that tenant.
- **server/proposal-verifier.ts:246** — UPDATE code_proposals SET verification_status/verification_details ... WHERE id = ${proposalId} — persist() writes verification metadata without tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or require and pass tenantId into persist(), ensuring verification metadata updates are scoped to the owning tenant.
- **server/recurring-messages.ts:299** — UPDATE to tenant-scoped table agent_knowledge omits tenant_id in the WHERE clause (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}). This can update a row from a different tenant if an id collides or is manipulated.
  - _Fix:_ Include tenant ownership in the WHERE clause to enforce tenant isolation, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id} (or otherwise validate that the caller/process is authorized for that tenant) before updating. Prefer double-checking tenant_id derived from the earlier row so the update is atomic to the tenant context.
- **server/reference-learner.ts:289** — UPDATE to tenant-scoped table memory_entries omits tenant_id in the WHERE clause (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This allows cross-tenant modification if a row id is guessed or collides.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}, or first verify ownership of the row matches input.tenantId before performing the update. Always include tenant_id in WHERE for writes to tenant-scoped tables.
- **server/research-engine.ts:157** — UPDATE research_sessions in cleanupZombieSessions omits any tenant filter — updates/completes sessions across all tenants.
  - _Fix:_ Constrain the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant. Ensure the caller provides a trusted tenantId and use parameterized Drizzle/.where(eq(...)) semantics.
- **server/research-engine.ts:310** — UPDATE research_programs SET model = ... WHERE id = ${programId} lacks tenant constraint — can modify another tenant's program.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or use a parameterized .where(eq(research_programs.tenant_id, tenantId)) to ensure only the caller's tenant row is updated.
- **server/research-engine.ts:389** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' ... has no tenant filter — will delete agent_knowledge across all tenants.
  - _Fix:_ Add tenant_id = ${tenantId} constraint to the DELETE or execute per-tenant. Always pass the caller's tenantId when deleting tenant-scoped rows.
- **server/research-engine.ts:450** — SELECT FROM research_experiments WHERE session_id = ${sessionId} ... omits tenant_id — reads experiments potentially across tenants.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or equivalent Drizzle .where(eq(table.tenantId, session.tenantId))) to restrict reads to the session's tenant. Validate session ownership before querying if needed.
- **server/research-engine.ts:459** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} omits tenant filter — may read another tenant's program.
  - _Fix:_ Include AND tenant_id = ${session.tenantId} (or parameterized equivalent) when selecting tenant-scoped research_programs.
- **server/research-engine.ts:464** — SELECT summary FROM research_sessions WHERE id = ${sessionId} lacks tenant constraint — can leak other tenants' session summaries.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or use sessionId+tenantId pair) to the WHERE clause to enforce tenant isolation on reads.
- **server/research-engine.ts:469** — INSERT INTO project_notes (project_id, note, author, created_at) does not set tenant_id — tenant-scoped table inserts must provide tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT columns and values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, ...) VALUES (${session.tenantId}, ${projectId}, ...)). Even if project_id was validated, the tenant_id must still be stored explicitly per schema invariant.
- **server/research-engine.ts:477** — Second INSERT INTO project_notes (project_id, note, author, created_at) also omits tenant_id.
  - _Fix:_ Set tenant_id explicitly in the INSERT values (tenant_id = ${session.tenantId}) to ensure records are scoped to the correct tenant.
- **server/research-engine.ts:549** — UPDATE research_sessions SET ... WHERE id = ${sessionId} (session end) has no tenant filter — modifies session rows across tenants.
  - _Fix:_ Include AND tenant_id = ${session.tenantId} in the WHERE clause (or use a parameterized Drizzle update with .where(eq(table.tenantId, session.tenantId))). Verify session.tenantId before updating.
- **server/research-engine.ts:859** — UPDATE research_sessions in DB-timeout handling (setting totals) omits tenant constraint — may update other tenants' sessions.
  - _Fix:_ Restrict the UPDATE by AND tenant_id = ${session.tenantId} or use a safe two-column WHERE (id AND tenant_id) to enforce ownership.
- **server/research-engine.ts:887** — UPDATE research_sessions SET model = ${fallbackModel} WHERE id = ${session.sessionId} omits tenant filter — changes another tenant's session potentially.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} to the WHERE clause; always include tenant guard on updates to tenant-scoped tables.
- **server/research-engine.ts:926** — UPDATE research_sessions SET total_experiments... WHERE id = ${session.sessionId} (end of run) lacks tenant constraint.
  - _Fix:_ Add tenant_id condition (e.g. AND tenant_id = ${session.tenantId}) to prevent cross-tenant writes.
- **server/research-engine.ts:820** — UPDATE research_experiments SET ... WHERE id = ${experimentId} (main persist) omits tenant filter — writes experiment rows without verifying tenant ownership.
  - _Fix:_ Include AND tenant_id = ${session.tenantId} (or the known tenant) in the WHERE clause. Prefer updating by (id, tenant_id) pair to enforce isolation.
- **server/research-engine.ts:851** — UPDATE research_experiments SET status = 'crash' ... WHERE id = ${experimentId} (timeout handler) lacks tenant constraint.
  - _Fix:_ Add tenant_id = ${session.tenantId} to the WHERE clause when updating research_experiments in error branches.
- **server/research-engine.ts:878** — UPDATE research_experiments SET ... WHERE id = ${experimentId} (auth error branch) is missing tenant constraint.
  - _Fix:_ Constrain the UPDATE by tenant_id (AND tenant_id = ${session.tenantId}) to avoid cross-tenant modification.
- **server/research-engine.ts:894** — UPDATE research_experiments SET ... WHERE id = ${experimentId} (retry/transient branch) omits tenant guard.
  - _Fix:_ Include tenant_id in the WHERE clause for these error-handling updates.
- **server/research-engine.ts:910** — UPDATE research_experiments SET ... WHERE id = ${experimentId} (final error branch) lacks tenant constraint.
  - _Fix:_ Use WHERE id = ${experimentId} AND tenant_id = ${session.tenantId} (or paramized Drizzle .where) for all updates to research_experiments.
- **server/research-engine.ts:1482** — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL (replayHighValueFindings) lacks tenant constraint — the surrounding SELECT filtered by tenant, but the UPDATE must also include tenant guard.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE to ensure only rows owned by the replaying tenant are modified.
- **server/research-engine.ts:1524** — UPDATE research_experiments SET replayed_at = NULL WHERE id = ${row.id} AND replayed_proposal_id IS NULL (error rollback) omits tenant filter.
  - _Fix:_ Include AND tenant_id = ${tenantId} to avoid affecting experiments in other tenants when resetting the claim.
- **server/research-engine.ts:1597** — UPDATE code_proposals SET status = 'needs_review', validation_result = ... WHERE id = ${proposalId} (safeApplyProposal) omits tenant constraint — modifying proposals without verifying tenant ownership.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause for updates to code_proposals, or re-query proposal with tenant and use that tenant when performing updates.
- **server/research-engine.ts:1669** — UPDATE code_proposals SET status = 'failed', ... WHERE id = ${proposalId} (compile failure revert) lacks tenant filter.
  - _Fix:_ Restrict the UPDATE to the proposal's tenant (AND tenant_id = ${tenantId}) to prevent cross-tenant writes.
- **server/research-engine.ts:1712** — UPDATE code_proposals SET status = 'applied', applied_at = NOW(), ... WHERE id = ${proposalId} omits tenant constraint — may mark another tenant's proposal applied.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE to ensure only the caller's tenant rows are modified.
- **server/research-engine.ts:1773** — UPDATE code_proposals SET status = 'reverted', reviewed_at = NOW() WHERE id = ${proposalId} (git checkout revert path) omits tenant filter.
  - _Fix:_ Include AND tenant_id = ${proposal.tenant_id || tenantId} or otherwise ensure tenant ownership in the WHERE clause before updating code_proposals.
- **server/research-engine.ts:1783** — UPDATE code_proposals SET status = 'reverted' WHERE id = ${proposalId} (file-replace revert path) lacks tenant constraint.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause to ensure the revert only affects proposals belonging to the caller's tenant.
- **server/research-engine.ts:951** — SELECT * FROM research_sessions WHERE id = ${sessionId} in getResearchSessionStatus omits tenant constraint — returns session data without checking tenant ownership.
  - _Fix:_ Require tenant context and add AND tenant_id = ${tenantId} to the SELECT (or accept tenantId parameter) so callers only fetch their own sessions.
- **server/routes.ts:163** — Startup routine restoreUploadsFromDb selects rows from tenant-scoped file_storage with no tenant constraint and writes file data to the shared uploads directory. This reads across all tenants and can restore another tenant's files to a globally-accessible path.
  - _Fix:_ Scope the SELECT to a specific tenant (e.g. add WHERE tenant_id = <admin/target tenant>) or iterate tenants explicitly and restore files into per-tenant locations. At minimum, ensure the operation runs under an audited admin-only context and avoid writing other-tenants' files into a shared public uploads directory. Prefer restoring only files belonging to the service/tenant being operated on and preserve/validate tenant ownership before writing to disk.
- **server/routes.ts:2148** — POST /api/presenter inserts a presenter_sessions row using tenantId taken directly from the request body (INSERT ... VALUES (${tenantId}, ...)). The tenantId is unvalidated/unverified against the caller's session/context, allowing creation of presenter sessions for arbitrary tenants if the caller or SESSION_SECRET is abused.
  - _Fix:_ Do NOT accept tenantId from the caller. Derive tenantId from the authenticated/authorized context (session, internal binding) or validate that the caller is explicitly authorized to act for the supplied tenant. If this endpoint must accept tenantId (internal-only), bind/verify it cryptographically to the internal key (or map the internal key to an allowed tenant list) and log the action. Prefer server-side determination of tenantId and enforce ownership/authorization before INSERT.
- **server/routes.ts:4331** — SELECT from tenant-scoped table 'conversations' is missing a tenant_id constraint. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` can return data for a conversation ID that does not belong to the caller's tenant.
  - _Fix:_ Restrict the SELECT to the caller's tenant (e.g. add AND tenant_id = ${tenantId}). Prefer using the storage layer or Drizzle helpers that accept/validate tenantId so ownership is enforced in a single place.
- **server/routes.ts:4337** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts `project_id, note, author` but omits tenant_id, and the project_id used was looked up without a tenant check, so this can create notes associated with projects across tenants.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ...)). Additionally, ensure the projectId is validated as belonging to the requesting tenant before inserting (or use a storage helper that enforces tenant ownership).
- **server/routes/admin-extra.ts:128** — Raw/interpolated SQL (drizzle sql`... ${tenantId}`) used with a request-derived tenantId in the decline_events SELECT. This mixes templated SQL with request data and deviates from the enforced Drizzle .where(eq(...)) pattern — increasing isolation/injection risk.
  - _Fix:_ Avoid embedding request-derived values directly into sql`` templates. Use the Drizzle query builder (db.select().from(...).where(eq(table.tenantId, tenantId))) or other parameterized builder methods so tenant scoping is explicit and consistent. Validate tenantId before use and centralize tenant scoping checks.
- **server/routes/admin-extra.ts:216** — Multiple raw sql template queries in account deletion-summary (e.g. SELECT count(*) FROM conversations WHERE tenant_id = ${tenantId}) embed the request-derived tenantId via sql``/string interpolation instead of using Drizzle's typed query API.
  - _Fix:_ Replace these sql`` interpolations with Drizzle query-builder calls (e.g. db.select().from(conversations).where(eq(conversations.tenantId, tenantId))). This both ensures parameterization and makes the tenant constraint explicit and auditable. Avoid ad-hoc sql templating for request values.
- **server/routes/admin-wedges.ts:45** — Raw sql interpolation used with request-derived tenantId in the projects SELECT (WHERE tenant_id = ${tenantId}). Using sql`` with request-derived values bypasses the preferred Drizzle .where(eq(...)) pattern and risks injection/isolation mistakes.
  - _Fix:_ Use Drizzle query builder (db.select().from(projects).where(and(eq(projects.tenantId, tenantId), ...))) or otherwise ensure parameterized bindings and an explicit eq(tenantId) predicate. Do not inline request-derived tenantId into raw SQL templates.
- **server/routes/admin-zombie-detector.ts:38** — SQL executed inside the transaction interpolates a request-derived windowDays value (e.g. started_at > NOW() - (${windowDays}::int * INTERVAL '1 day')) via sql``. Even though executed in a transaction this is string-interpolated SQL using request input.
  - _Fix:_ Bind request-derived numeric parameters through the query builder (or validate/normalize them and pass as typed parameters), or use Drizzle's APIs that produce eq/gt expressions. Avoid embedding request parameters directly into sql`` templates; ensure the query remains parameterized and tenant/global intent is explicit.
- **server/routes/admin.ts:661** — The repair_incidents SELECT is constructed using sql`` fragments where tenant_id = ${tenantId} (and later WHERE ${where}). This builds SQL via interpolated sql pieces that include a request-derived tenantId rather than using the Drizzle query API.
  - _Fix:_ Construct the WHERE clause with Drizzle expression helpers (eq(table.tenantId, tenantId), sql builders via drizzle) or at minimum use db.select().from(repair_incidents).where(eq(repair_incidents.tenantId, tenantId)). Avoid composing sql`` fragments that interpolate request values — prefer explicit query-builder predicates for tenant scoping.
- **server/routes/api-v1.ts:395** — Agents list query uses a raw sql`` with an interpolated request-derived tenantId in the LEFT JOIN condition (tpn.tenant_id = ${tenantId}). This relies on templated SQL interpolation instead of an explicit Drizzle .where(eq(...)) constraint.
  - _Fix:_ Replace the raw sql`` with a Drizzle query (db.select().from(personas).leftJoin(tenant_persona_names).on(...).where(eq(personas.isActive, true)).and(eq(tenant_persona_names.tenantId, tenantId))). Use table/column references and eq(...) so tenant scoping is explicit and consistently enforced.
- **server/routes/api-v1.ts:653** — AG-UI /agui/run persona lookup uses sql`` embedding request body.agent (SELECT id FROM personas WHERE LOWER(name) = LOWER(${body.agent})). This interpolates a request-derived string into raw SQL rather than relying on the query builder, increasing injection/isolation risk.
  - _Fix:_ Use the query builder (e.g. db.select().from(personas).where(and(eq(personas.is_active, true), sql`LOWER(${personas.name}) = LOWER(${agent})`)) or better: normalize agent on the application side and use eq(personas.name, normalized) with parameter binding. Never build SQL by interpolating raw request strings.
- **server/routes/api-v1.ts:780** — Conversation poll uses a raw sql`` query with interpolated conversationId and tenantId (WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId}). Although tenant checks exist elsewhere, this is still templated SQL embedding request-derived values instead of using Drizzle's typed .where(eq(...)) expressions.
  - _Fix:_ Switch to Drizzle query-builder APIs (db.select().from(messages).join(conversations, ...).where(and(eq(messages.conversationId, conversationId), eq(conversations.tenantId, tenantId)))) so the tenant constraint is explicit and parameterized. Avoid direct sql`` interpolation of request parameters.
- **server/routes/billing.ts:109** — Tenant update uses raw sql`` interpolation with request-derived tenantId (UPDATE tenants SET plan = ${plan} WHERE id = ${tenantId}). This mixes request data into templated SQL rather than an explicit Drizzle update with eq(id, tenantId).
  - _Fix:_ Use Drizzle's update builder (e.g. db.update(tenants).set({ plan }).where(eq(tenants.id, tenantId))) or otherwise ensure parameterized bindings via the query builder. This makes tenant scoping explicit and consistent with the application's tenant-isolation invariant.
- **server/routes/conversations.ts:227** — INSERT into `project_notes` performed without tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` — no tenant_id passed/checked
  - _Fix:_ Ensure `project_notes` is written with explicit tenant ownership (e.g. include tenant_id in VALUES or add `tenant_id` column and set it), or write via a storage function that enforces/derives tenant_id from the referenced project and validates ownership before inserting.
- **server/routes/heartbeat.ts:102** — UPDATE on tenant-scoped table `heartbeat_tasks` performed without tenant constraint in WHERE when approving a task: `UPDATE heartbeat_tasks SET approval_status = 'approved', enabled = true, next_run_at = ${nextRun} WHERE id = ${taskId}`
  - _Fix:_ Constrain the UPDATE to the task's tenant (e.g. `WHERE id = ${taskId} AND tenant_id = ${task.tenantId}`) or perform approval via a tenant-aware storage function. Also verify fetched task belongs to the caller's tenant before mutating.
- **server/routes/heartbeat.ts:116** — UPDATE on tenant-scoped table `heartbeat_tasks` performed without tenant constraint in WHERE when rejecting a task: `UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId}`
  - _Fix:_ Constrain the UPDATE to include tenant_id (e.g. `WHERE id = ${taskId} AND tenant_id = ${task.tenantId}`) or use a storage API that enforces tenant ownership. Ensure the handler verifies the task's tenant before mutating.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) ... is missing tenant_id, so the new row may be created without the required tenant ownership column (or fail if tenant_id is NOT NULL).
  - _Fix:_ Include the calling tenant's id in the INSERT (and/or validate ownership). Example: INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ${req.body.note}, ${req.body.author || 'user'}) RETURNING *; alternatively use a storage helper that injects and validates tenantId before persisting.
- **server/routes/projects.ts:225** — INSERT into tenant-scoped table `project_files` (base64 upload path) does not set tenant_id. The INSERT INTO project_files (...) VALUES (${projectId}, ...) omits tenant_id.
  - _Fix:_ Pass the tenant id when inserting project files and verify project ownership first. Example: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ${f.fileName}, ${'uploads/'+uniqueName}, ${fileUrl || downloadPath}, ${f.mimeType || 'application/octet-stream'}, ${fileBuffer.length}) RETURNING *; or perform insertion via a storage API that enforces tenant_id.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload path) does not set tenant_id. The INSERT INTO project_files (...) VALUES (${projectId}, ...) omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and ensure the caller owns the project. Example: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ${file.originalname}, ${filePath}, ${fileUrl || downloadPath}, ${file.mimetype}, ${file.size}) RETURNING *; or centralize file writes through a storage layer that injects tenantId.
- **server/seed.ts:34** — SELECT COUNT(*) FROM research_sessions executed with no tenant_id filter — reads tenant-scoped table across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope by tenant before counting to avoid cross-tenant data access.
- **server/seed.ts:2854** — SELECT id, name, model FROM research_programs WHERE is_active = true — reads research_programs across all tenants (no tenant_id constraint).
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise iterate per-tenant to avoid exposing or processing other tenants' rows.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... — selecting conversations with no tenant_id constraint, potentially returning rows for other tenants.
  - _Fix:_ Restrict the SELECT to the current tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or otherwise join/verify tenant ownership before operating on the results.
- **server/seed.ts:4032** — INSERTs into tenant-scoped table 'heartbeat_tasks' are performed via db.insert(...).values(...) without explicitly setting tenant_id. Relying on implicit defaults (if any) can result in incorrect tenant ownership or violate the rule that every INSERT must pass tenantId explicitly.
  - _Fix:_ Always include tenant_id explicitly in INSERT values for tenant-scoped tables (e.g. { ..., tenantId: tenantId }). If inserting seed/system rows intended for tenant 1, set tenant_id: 1 explicitly. Audit all db.insert(...) calls for tenant-scoped tables and add tenant_id where missing.
- **server/self-improvement.ts:170** — detectStagnation builds and executes a SELECT on the tenant-scoped `experiments` table without enforcing tenant_id when tenantId is omitted. This allows cross-tenant reads if called without a tenant filter.
  - _Fix:_ Always require and use a tenantId for queries against tenant-scoped tables. Either make tenantId a required parameter (throw if missing) or fail-closed (return empty) and add eq(experiments.tenantId, tenantId) to the WHERE clause instead of issuing an unscoped SELECT.
- **server/self-improvement.ts:521** — getExperimentHistory may execute an unscoped SELECT on the tenant-scoped `experiments` table when no tenantId is provided, returning rows across tenants.
  - _Fix:_ Require tenantId or fail-closed when tenantId is not supplied. If multi-tenant listing is needed only for admin roles, gate it behind explicit admin checks. Ensure all SELECTs against `experiments` include eq(experiments.tenantId, tenantId).
- **server/skill-seeker.ts:777** — listGaps has branches that perform SELECTs on the tenant-scoped `capability_gaps` table without a tenant_id filter (e.g. when only `status` is passed or when neither status nor tenantId is passed). These unscoped queries can leak other tenants' data.
  - _Fix:_ Require tenantId for tenant-scoped listings or restrict unscoped queries to admin-only code paths. Always include AND tenant_id = ${tenantId} (or equivalent) in WHERE clauses when reading `capability_gaps`, or explicitly document and enforce that only authorized admin callers may perform cross-tenant reads.
- **server/skillify.ts:196** — SELECT from tenant-scoped table `conversations` is performed without constraining by tenant_id (WHERE tenant_id = <caller tenant>). The query fetches a conversation by id only, then checks ownership in application code — this performs an unscoped read of another tenant's row before the app-level check.
  - _Fix:_ Add tenant-level SQL scoping to the query itself. e.g. include a tenant predicate in the WHERE clause (drizzle: .where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))) ) or use the requireTenantScope/tenantScope helper to fail-closed at the SQL level before the SELECT.
- **server/skillify.ts:223** — SELECT from `personas` by id is done without a tenant_id constraint. If `personas` is tenant-scoped, this reads another tenant's persona row (IDOR) because the SQL itself lacks tenant scoping.
  - _Fix:_ Scope the persona lookup to the caller's tenant in SQL. Example: .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))). Alternatively validate ownership with a dedicated assertConversationInTenant/assertProjectInTenant-style helper prior to the query or use tenantScope(requireTenantScope) for the personas. If personas are global intentionally, document it clearly.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` (recentMsgs in distillIntent) uses conversationId only and lacks an explicit tenant_id filter in the SQL. This performs an unscoped read of messages that may belong to another tenant.
  - _Fix:_ Constrain the SELECT at the DB level. Include tenant scoping in the WHERE (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or use tenantScope(requireTenantScope) for the messages table so the DB enforces isolation rather than relying solely on post-query application checks.
- **server/skillify.ts:203** — SELECT from tenant-scoped table `messages` (in analyzeConversation) uses only conversationId in the SQL and omits an explicit tenant_id condition. This allows reading messages across tenants until an application-level check runs.
  - _Fix:_ Add tenant-level SQL scoping to the messages query. For example: .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or use the tenantScope/requireTenantScope helper to ensure fail-closed DB-level isolation.
- **server/skillify.ts:331** — SELECT from tenant-scoped table `messages` (recentMsgs in skillifyConversation) is performed without an explicit tenant_id constraint. The query only filters by conversationId, so it reads potentially cross-tenant data in SQL before any app-level validation.
  - _Fix:_ Include the tenant predicate in the SQL WHERE clause (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise use the tenant-scoping helpers so the DB-level query enforces tenant isolation rather than relying solely on later application checks.
- **server/storage.ts:239** — getConversationUnscoped performs a SELECT on the tenant-scoped `conversations` table without any tenant_id constraint (WHERE tenant_id = ...). This returns a conversation row regardless of tenant and relies on callers to validate ownership.
  - _Fix:_ Remove or strongly gate unscoped helpers. Require an explicit tenantId parameter (or an explicit admin flag) and enforce eq(conversations.tenantId, tenantId) in the WHERE clause. If an unscoped accessor is absolutely required, make it private/internal, document it clearly, and ensure callers perform an ownership check immediately after fetch (and audit all callsites).
- **server/storage.ts:289** — attachCitationMetadata issues a raw SELECT from `messages` (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL) without scoping by tenant_id. messages is tenant-scoped, so this may leak citations across tenants.
  - _Fix:_ Pass tenantId into attachCitationMetadata (or derive it from the messages passed in) and include eq(messages.tenantId, tenantId) in the WHERE clause (or join via conversations to confirm ownership). Always scope selects on tenant-scoped tables to the calling tenant.
- **server/storage.ts:390** — attachCitations issues a raw UPDATE against tenant-scoped `messages` (UPDATE messages SET citations = ... WHERE id = ${messageId}) with no tenant constraint. This allows updating a message row without verifying tenant ownership.
  - _Fix:_ Require the caller to provide tenantId (or validate ownership via a prior SELECT that checks the message's tenant_id). Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use Drizzle eq(...) tenant scoping) to ensure the update cannot cross tenants.
- **server/storage.ts:474** — deletePersona transaction updates the tenant-scoped `conversations` table (tx.update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id))) without a tenant filter. This will clear persona references across all conversations for all tenants.
  - _Fix:_ Add tenant scoping to these updates (accept/derive a tenantId and include eq(conversations.tenantId, tenantId) in the WHERE). If personas are global but conversation cleanup must be per-tenant, ensure the operation only touches rows belonging to the intended tenant(s); require an explicit tenant context for destructive multi-row updates.
- **server/storage.ts:504** — getAllMemoriesForBackup selects from tenant-scoped `memory_entries` without any tenant_id constraint and returns data across all tenants.
  - _Fix:_ Make this an explicit admin-only operation that requires an explicit admin authorization and/or tenantId parameter, or change it to export per-tenant. At minimum, document and gate it and require an explicit admin flag; prefer forcing a tenantId or an admin-only codepath that cannot be called from regular request handlers.
- **server/storage.ts:524** — createMemoryEntry inserts into tenant-scoped `memory_entries` with values(data) but does not validate that data.tenantId is present/owned. An INSERT can therefore be made without an explicit tenant_id.
  - _Fix:_ Require and validate tenantId before inserting (assertValidTenantId or similar). If tenantId must come from caller context, accept it explicitly and set it server-side rather than trusting client-supplied data.
- **server/storage.ts:797** — updateMemoryEmbedding updates tenant-scoped `memory_entries` with WHERE id = ${id} and no tenant_id constraint. This can modify memory rows across tenants if called with just an id.
  - _Fix:_ Require a tenantId parameter or verify ownership of the memory entry before updating. Use a WHERE that includes eq(memoryEntries.tenantId, tenantId) (or fetch-and-verify the row first).
- **server/storage.ts:809** — updateKnowledgeEmbedding updates tenant-scoped `agent_knowledge` with WHERE id = ${id} and no tenant_id constraint. This allows cross-tenant embedding updates by id alone.
  - _Fix:_ Require tenantId or verify the tenant on the target row before updating. Use tenant-scoped WHERE clauses (eq(agentKnowledge.tenantId, tenantId)) when mutating tenant-scoped tables.
- **server/storage.ts:928** — getHeartbeatTask performs SELECT FROM `heartbeat_tasks` by id with no tenant scoping. heartbeat_tasks is tenant-scoped (tenant_id column present), so this read can return a task from any tenant.
  - _Fix:_ Require caller to pass tenantId and include eq(heartbeatTasks.tenantId, tenantId) in the WHERE clause, or explicitly document and gate this as an admin-only unscoped accessor.
- **server/storage.ts:953** — updateHeartbeatTask conditionally adds a tenant check only when tenantId is provided; if caller omits tenantId the UPDATE runs unscoped (WHERE id = ... only) against the tenant-scoped `heartbeat_tasks` table.
  - _Fix:_ Either make tenantId required for this operation or always resolve and verify the task's tenant before mutating. Do not perform mutating operations on tenant-scoped tables unless tenant ownership is verified in the WHERE clause.
- **server/storage.ts:959** — deleteHeartbeatTask similarly performs DELETE on tenant-scoped `heartbeat_tasks` without tenant filtering if tenantId is not supplied (conditional tenant check only when tenantId provided).
  - _Fix:_ Require tenantId or verify ownership before deleting. Always include tenant scoping in DELETE/UPDATE queries for tenant-scoped tables.
- **server/subagents.ts:248** — spawnSubagent fetches the parent conversation using storage.getConversation(params.parentConversationId, params.tenantId ?? ADMIN_TENANT_ID). Defaulting an omitted tenantId to ADMIN_TENANT_ID means missing tenant context results in an admin-scoped fetch and may allow cross-tenant access.
  - _Fix:_ Do not default to ADMIN_TENANT_ID. Require the caller to provide tenantId and verify ownership of the parent conversation before spawning a subagent. If admin behavior is needed, require an explicit admin flag and additional authorization checks.
- **server/surprise-scorer.ts:315** — getSurpriseStats defaults tenantId to 1 (getSurpriseStats(tenantId: number = 1)) and runs several tenant-scoped SELECTs using that default. Omitting tenantId causes the call to return tenant-1 data.
  - _Fix:_ Make tenantId a required parameter; do not default to tenant 1. Require callers to pass the correct tenantId and validate access rights before returning tenant-scoped statistics.
- **server/tool-learning.ts:291** — UPDATE of custom_tools (setting implementation during auto-fix) uses only eq(customTools.id, tool.id) with no tenant_id constraint.
  - _Fix:_ Constrain UPDATE to the tenant, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure you pass the tenantId into the auto-fix flow so the guard can be applied.
- **server/tools.ts:2970** — SELECT from messages table without tenant constraint — db.select({ count: sql`count(*)::int` }).from(messagesTable) is run inside checkSystemStatus with no WHERE tenant_id = <tenant> filter. This exposes cross-tenant data (total message counts) to callers.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. include WHERE tenant_id = ${tenantId}), or call a storage helper that returns tenant-scoped counts. If the tool is intended to be tenant-scoped, accept/require a tenantId and pass it into the query; if it must show global platform metrics, restrict access to an operator/admin channel and document that explicitly.
- **server/tools.ts:4394** — SELECT from tenant-scoped table `memory_entries` is missing a tenant_id constraint. The query: SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ... will return rows across all tenants.
  - _Fix:_ Constrain reads to the caller's tenant. Add an explicit tenant filter (e.g. WHERE tenant_id = ${params._tenantId} AND ...) or bind the tenant id into the Drizzle query (eg. sql`... WHERE tenant_id = ${tenantId} AND persona_id = 2 ...`). Validate that tenantId is present and numeric before running the query.
- **server/tools.ts:7030** — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files omits tenant_id and instead only checks ownership via assertProjectInTenant, but the row will be created without an explicit tenant column value.
  - _Fix:_ Include tenant_id in the INSERT (or use the Drizzle typed insert API) so the new project_files row is explicitly tied to the caller's tenant. Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Prefer using db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: params.project_id, ... }) to avoid raw SQL mistakes.
- **server/tools.ts:8990** — UPDATE on proposed_skills omits tenant constraint. db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) updates by id only and does not restrict by tenant_id, allowing cross-tenant modification if ids collide or are known.
  - _Fix:_ Add a tenant filter to the WHERE clause to enforce ownership. For example: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) where tid is the current tenant id (params._tenantId). Alternatively use a Drizzle helper that scopes by tenant before updating.
- **server/tools.ts:9015** — UPDATE on proposed_skills omits tenant constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates without verifying the tenant, violating tenant-isolation requirement.
  - _Fix:_ Restrict the UPDATE to the tenant by adding tenant equality in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Prefer using typed Drizzle updates that include tenant scoping.
- **server/tools.ts:9032** — UPDATE on proposed_skills omits tenant constraint. The rejection path updates proposed_skills by id only, with no tenant_id guard, which can affect other tenants' rows.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or otherwise ensure the update uses a tenant-scoped query (use db.execute/sql with AND tenant_id = ${tid} or the Drizzle equivalent).
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against tenant-scoped table browser_workflows without constraining by tenant_id (no WHERE tenant_id = <caller tenant>).
  - _Fix:_ Add a tenant filter to the UPDATE (e.g. WHERE id = ${workflow.id} AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(table.tenantId, tenantId)) so updates cannot affect other tenants' rows.
- **server/tools/domains/files/handlers.ts:839** — UPDATE executed against projects table (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}) without constraining by tenant_id — can update another tenant's project by id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(projects.tenantId, tenantId)) after validating the caller's tenant owns the project row.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE on tenant-scoped table `knowledge_triples` lacks a tenant_id constraint — the UPDATE uses `WHERE id = ${row.id}` without `AND tenant_id = ${tenantId}`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}` — or use Drizzle's .where(eq(table.tenantId, tenantId)) to enforce tenant scoping as a defense-in-depth measure.
- **server/tools/domains/security/handlers.ts:163** — Query against provider_keys without tenant constraint — SELECT provider, LENGTH(api_key) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''. This reads rows from a likely tenant-scoped table without filtering by tenant_id, enabling cross-tenant enumeration of API keys/metadata (even though the branch is admin-only).
  - _Fix:_ If provider_keys is tenant-scoped, add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) and parameterize tenantId. If the intent is an admin-wide audit, make that explicit in code and authorization (and avoid returning sensitive key material — return counts/metrics only), or move provider_keys to a documented global table. Ensure all reads of tenant-owned secrets are explicitly scoped to a tenant_id or guarded by an auditable admin-only pathway.
- **server/tools/domains/security/handlers.ts:260** — Query against provider_keys without tenant constraint — SELECT COUNT(DISTINCT provider) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''. This aggregates across provider_keys with no tenant_id filter, potentially exposing cross-tenant provider configuration (executed under an admin path).
  - _Fix:_ If provider_keys is tenant-scoped, restrict the query to the calling tenant (e.g. WHERE api_key IS NOT NULL AND api_key != '' AND tenant_id = ${tenantId}) or otherwise ensure this admin-only audit path is intentionally authorized and documented. Prefer returning tenant-scoped metrics only after explicit admin consent and with least-privilege results (avoid returning raw key-related metadata).
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses only `WHERE id = ${id}` with no tenant_id (or persona_id) constraint — allows updating rows belonging to other tenants by id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (and persona if applicable). Example: `WHERE id = ${id} AND tenant_id = ${tid}` (or include `AND persona_id = ${personaId}`) or use Drizzle .where(eq(memoryEntries.tenantId, tid)).
- **server/tools/domains/strategic-memory/handlers.ts:148** — Bulk UPDATE on `memory_entries` (WHERE id = ANY(${ids})) omits tenant_id constraint — will bump last_accessed/access_count for rows across tenants if ids include other-tenant rows.
  - _Fix:_ Include tenant scoping in the WHERE clause. Example: `UPDATE memory_entries SET ... WHERE id = ANY(${ids as any}) AND tenant_id = ${tid}` (and/or `AND persona_id = ${personaId}`), or select ids scoped to the tenant before updating.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against `memory_entries` uses `WHERE id = ${id}` without tenant_id/persona_id constraint (in recordStrategicWin flow) — permits cross-tenant row modification by id.
  - _Fix:_ Add the tenant (and persona) constraint to the UPDATE: `WHERE id = ${id} AND tenant_id = ${tid}` (and optionally `AND persona_id = ${personaId}`), or use parameterized Drizzle filters (e.g. .where(eq(table.tenantId, tid))).
- **server/video-job-runner.ts:228** — UPDATE videoJobs ... WHERE status IN (...) AND updatedAt < <cutoff> executes without tenant_id constraint — this global maintenance update will modify rows across all tenants.
  - _Fix:_ If this is intended to be a global system sweep, restrict it to a privileged/operational context (document & audit it) or add a tenant filter / per-tenant sweep mode. Safer options: iterate tenants and run the update per-tenant, or add AND tenant_id = ${tenantId} where the tenant is explicit. Alternatively, run the reclamation as a DB-side scheduled job with appropriate admin privileges and logging.
- **server/watchlist.ts:111** — Query reads columns from tenant-scoped table `watchlist_items` via a LEFT JOIN (wi) without constraining that joined table to the caller's tenant. The WHERE only restricts wa.tenant_id = ${tenantId}, but wi is not filtered — this can expose another tenant's watchlist item metadata if alert rows reference items across tenants.
  - _Fix:_ Constrain the joined watchlist_items rows to the same tenant. For example, include the tenant check in the JOIN: LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id AND wi.tenant_id = ${tenantId} (or add AND wi.tenant_id = ${tenantId} in the WHERE). Ensure every read of a tenant-scoped table includes an explicit tenant_id filter (or fetch the item separately via a tenant-scoped getWatchlistItem call).

### MEDIUM (50)

- **server/auto-memorize.ts:47** — SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} ... has no tenant_id constraint. agent_knowledge is tenant-scoped elsewhere (has tenant_id), so this read may unintentionally mix tenant-scoped rows.
  - _Fix:_ Add tenant scoping to this query (e.g. WHERE tenant_id = ${tenantId}) or explicitly document and enforce that this watermark is intentionally stored/queried for a specific admin tenant (and then include that tenant id in the WHERE). Avoid unscoped selects on tenant-scoped tables.
- **server/auto-memorize.ts:62** — SELECT id FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} LIMIT 1 performs a lookup on a tenant-scoped table with no tenant_id filter before deciding whether to update/insert.
  - _Fix:_ Constrain the lookup by tenant_id (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the operation is explicitly targeted at the admin/owner tenant. Use per-tenant keys or explicitly parametrize admin-tenant id in these maintenance queries.
- **server/auto-project.ts:112** — SELECT role, LEFT(content, 300) as content FROM messages WHERE conversation_id = ${conversationId} ... reads messages without an explicit tenant_id constraint. Although the conversation was checked for tenant ownership earlier, the messages read is not explicitly constrained to tenant_id, violating the rule that tenant-scoped reads include tenant filters.
  - _Fix:_ Add an explicit tenant filter on messages reads (e.g. AND tenant_id = ${tenantId}), or derive/verify conversation ownership and use an explicit assert helper immediately before the messages SELECT. Prefer explicit WHERE tenant_id checks rather than relying solely on previously-read foreign rows.
- **server/business-tools.ts:74** — createInvoice inserts customer_id into invoices without verifying the caller's tenant actually owns that customer_id (no ownership check). This can create invoices referencing foreign customers.
  - _Fix:_ Validate customer ownership before using customer_id: e.g. SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1 and reject if not found. Alternatively, set customer_id to null unless ownership is verified. Ensure any foreign id provided by callers is validated to belong to the caller's tenant.
- **server/capability-review.ts:490** — markReviewReused updates capability_reviews WHERE id = ${pending.reviewId} WITHOUT scoping by tenant_id. A pending review id could belong to another tenant and be updated incorrectly.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${pending.reviewId} AND tenant_id = ${tenantId} AND reused = false). To do that, persist tenantId in the pendingReuseByConversation entries when recording the review so markReviewReused can assert tenant ownership before updating.
- **server/ceo-orchestrator.ts:1806** — getActivePlan(planId) returns an active plan object from a global in-memory map without checking the caller's tenant. This can expose another tenant's plan data if the function is called with an arbitrary planId.
  - _Fix:_ Require and check the caller's tenant (e.g. accept tenantId and verify plan.tenantId === tenantId) before returning the plan, or only expose plan data via tenant-scoped accessors.
- **server/ceo-orchestrator.ts:1810** — getActivePlansForConversation(conversationId) scans a global activePlans map and returns plans matching conversationId without enforcing tenant ownership, allowing cross-tenant plan access.
  - _Fix:_ Require tenant context and filter returned plans by plan.tenantId === callerTenantId (or add tenantId param), so only plans belonging to the caller's tenant are returned.
- **server/ceo-orchestrator.ts:1818** — getAllActivePlans() returns all active plans across tenants (filtering only by status) from a global in-memory map — leaking other tenants' in-flight orchestration data.
  - _Fix:_ Do not return global activePlans to callers without tenant scoping. Either accept a tenantId and filter plans by tenantId, or restrict this API to internal/admin code with proper access controls; consider storing per-tenant active plan lists instead.
- **server/commitments.ts:135** — scanAndEscalate performs a SELECT FROM commitments with no tenant_id constraint — it reads active commitments across all tenants (includes description field).
  - _Fix:_ If this is intended as an admin/global scanner, make the intent explicit and minimize selected columns to non-sensitive fields (e.g. SELECT id, tenant_id, due_at, heartbeat_interval_ms) and ensure only an authenticated admin process can run it. Otherwise, restrict the query to a specific tenant (add WHERE tenant_id = ${tenantId}) before reading tenant-scoped rows.
- **server/credential-vault.ts:169** — getDecryptedPassword updates credential_vault.last_used_at using only WHERE id = ${id} — the UPDATE lacks a tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Guard the UPDATE by tenant as well (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or propagate tenant_id into the query. Even if you previously loaded the row, include tenant_id in the WHERE to avoid accidental cross-tenant updates.
- **server/credential-vault.ts:176** — getLoginCredentials updates credential_vault.last_used_at with WHERE id = ${cred.id} but no tenant_id filter — this UPDATE touches tenant-scoped data without constraining to the caller's tenant.
  - _Fix:_ Change the UPDATE to include tenant_id (e.g. WHERE id = ${cred.id} AND tenant_id = ${tenantId}) or otherwise ensure the update is scoped to the same tenant that owns the credential to prevent cross-tenant modifications.
- **server/data-protection.ts:111** — SELECT across tenant-scoped `conversations` for purge: SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} — no tenant filter. Reads rows for all tenants without an application-level tenant constraint.
  - _Fix:_ If this job is intended to be cross-tenant, document that clearly and restrict access to an admin-only path. Otherwise, add tenant scoping (AND tenant_id = ...) or iterate tenants explicitly and run per-tenant SQL to avoid accidental cross-tenant operations.
- **server/data-protection.ts:169** — SELECT from `messages` is executed without an explicit tenant_id predicate: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}. Reads tenant-scoped data without the required tenant filter.
  - _Fix:_ Add tenant scoping to the query (AND tenant_id = ${tenantId}) or ensure the conversation ownership check is enforced and used to scope subsequent queries (prefer explicit tenant_id predicates for defense-in-depth).
- **server/data-protection.ts:176** — SELECT from `compaction_archives` is executed without an explicit tenant_id predicate: SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId}. This reads tenant-scoped data without tenant filtering.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or otherwise validate that the conversation belongs to the tenant and then include that tenant_id in the query) to enforce tenant isolation on reads.
- **server/data-protection.ts:144** — verifyMessageSaved issues a SELECT against `messages` without tenant scoping: SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} ... No tenant_id predicate is present.
  - _Fix:_ Either accept tenantId as a parameter and filter WHERE tenant_id = ${tenantId}, or lookup/verify the conversation->tenant mapping first and include tenant_id in the query to ensure the read is scoped to the caller's tenant.
- **server/data-protection.ts:311** — Within full-tenant backup, SELECTs for project-related child tables do not constrain by tenant: SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} and SELECT file_name,... FROM project_files WHERE project_id = ${proj.id} lack tenant_id predicates. These reads rely on project_id only and do not include tenant scoping at the SQL level.
  - _Fix:_ Add tenant predicates (e.g. AND project_id = ${proj.id} AND tenant_id = ${tenantId}) or join back to the projects row that was tenant-scoped to ensure records belong to the expected tenant before reading/writing them.
- **server/data-protection.ts:356** — COUNT query against `messages` uses conversation_id only (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) and omits tenant scoping. Reads tenant-scoped data without explicit tenant filtering.
  - _Fix:_ Include tenant scoping (e.g. AND tenant_id = ${tenantId}) or ensure the conversation ownership is asserted and used to scope the query; prefer explicit WHERE tenant_id = ${tenantId} to satisfy the application's isolation invariant.
- **server/crews-engine.ts:874** — addFlowStep inserts params.crewId into flow_steps without validating that the supplied crewId belongs to the caller's tenant. This writes a foreign key referencing another tenant's entity without an ownership check.
  - _Fix:_ Before inserting, validate ownership (e.g. if params.crewId is provided call getCrew(params.crewId, params.tenantId) and fail if not found). Alternatively, enforce a DB-side check by inserting only when the crew row matches tenant_id, or include comment/documentation clarifying why cross-tenant references are disallowed.
- **server/doc-collections.ts:136** — Subqueries in listCollections count rows in tenant-scoped table `doc_chunks` but do not include tenant filtering (e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id)). This can produce counts that cross tenant boundaries.
  - _Fix:_ Constrain the subqueries by tenant_id (e.g. WHERE collection_id = c.id AND tenant_id = c.tenant_id) so counts are restricted to the same tenant as the collection row.
- **server/dormant-deprecation.ts:169** — COUNT query over `messages` omits tenant scoping (SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user'). If messages is tenant-scoped, this aggregates across tenants and may cause platform-wide decisions based on mixed data.
  - _Fix:_ If this computation should be tenant-specific, add tenant_id filtering (or accept a tenant param). If it is intentionally global, make that explicit in comments and ensure only trusted operator code can call this path.
- **server/dormant-deprecation.ts:179** — COUNT DISTINCT query over `tool_performance` omits tenant filter (SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ...). If tool_performance is tenant-scoped, this produces cross-tenant counts.
  - _Fix:_ Scope the query by tenant_id when computing tenant-scoped metrics (e.g. WHERE tenant_id = ${tenantId} AND last_success_at > ...). If the intent is a global metric, restrict access to trusted operator contexts and document it.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance in getEverInvokedTools has no tenant filter, potentially returning tool usage across tenants rather than per-tenant usage.
  - _Fix:_ Add tenant scoping to the query (WHERE tenant_id = ${tenantId}) or make getEverInvokedTools explicitly operate per-tenant (accept tenantId). Ensure the auto-deprecation decisions are computed from tenant-scoped data only.
- **server/dormant-deprecation.ts:214** — getActiveTools14d SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant filter and can return global tool activity rather than per-tenant activity.
  - _Fix:_ Add tenant_id constraint to the query (WHERE tenant_id = ${tenantId} AND last_success_at > ...), or otherwise ensure this aggregation is intentionally global and protected by admin-only access.
- **server/embeddings.ts:244** — vectorSearchMemory uses sql.raw() with a string-interpolated vector literal derived from a request (query → embedding → vec) inside the SQL (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). Using sql.raw() with request-derived data risks SQL injection / injection-like issues even if there is some validation.
  - _Fix:_ Avoid sql.raw interpolation for user/request-derived values. Pass the vector as a parameter and cast in SQL (e.g. embedding_vec <=> ${vec}::vector with proper parameter binding), or use Drizzle parameterization. If sql.raw must be used, ensure strict validation and prefer binary/bytea binding rather than manual string injection.
- **server/embeddings.ts:559** — vectorSearchKnowledge similarly uses sql.raw() with an interpolated vector literal derived from a user query's embedding (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}), ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is sql.raw() with request-derived data.
  - _Fix:_ Use parameterized queries for the vector (bind the vector value) rather than interpolating a string into sql.raw(). Ensure embeddings are validated and passed as SQL parameters or use a prepared statement / Drizzle parameter binding for the vector cast.
- **server/lib/bwb-job-progress.ts:74** — scope() can return a WHERE clause with only jobId (no tenant predicate) when BWB_TENANT_ID is missing, and several UPDATE/SELECT calls use where(scope(...)). That allows writes/reads against video_jobs without a tenant_id constraint when the environment/threading lacks the tenant — violating tenant isolation.
  - _Fix:_ Do not permit omission of tenant_id. Require and validate tenantId for all progress operations (fail closed or return/no-op when tenant missing) or make tenantId an explicit mandatory parameter for these functions. Ensure scope() always adds eq(videoJobs.tenantId, tid) (or throw) so every SELECT/UPDATE/DELETE includes tenant_id in the WHERE clause.
- **server/lib/mcp-api-keys.ts:182** — Best-effort UPDATE to mcp_api_keys uses only id in WHERE (`UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id}`) and lacks an explicit tenant_id constraint. Although the row was loaded just before, the write is not explicitly tenant-anchored.
  - _Fix:_ Constrain the UPDATE by tenant as well, using the tenant_id from the previously retrieved row (e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}`) so even best-effort touches are tenant-scoped.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from agent_runs (status='running') does not include tenant_id filter and scans rows across tenants. Reads against a tenant-scoped table are not constrained to a caller tenant.
  - _Fix:_ Scope the audit to a single tenant (add WHERE tenant_id = ${tenantId}) or explicitly run the sweep per-tenant, passing the tenantId into the query. If the tool must be cross-tenant (admin), require an explicit admin mode and audit that this is an intentional cross-tenant operation with appropriate controls.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from mind_tickets (status='in_progress') queries rows across tenants with no tenant_id predicate. This reads tenant-scoped data without tenant scoping.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) or run the sweep per-tenant (loop tenants and run the query with the tenant filter). If run as an admin cross-tenant job, make the cross-tenant intent explicit and document safe usage, but never omit tenant_id in ad-hoc tenant-scoped UPDATE/DELETE without a clear, auditable admin context.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} reads the persona row without constraining by tenant_id. exportPersona receives tenantId but does not verify the persona row belongs to that tenant (or otherwise validate ownership).
  - _Fix:_ Include tenant filtering or ownership validation when reading persona rows (e.g. SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}) or explicitly document that personas are global; if personas are tenant-scoped, always include AND tenant_id = ${tenantId} and reject cross-tenant access.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table agent_knowledge is executed without a tenant_id filter (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This returns rows across all tenants into application context.
  - _Fix:_ If this global scan is intentional for a system cron runner, clearly limit/authorize the caller (e.g. require an explicit internal flag) and document it; otherwise add an application-level tenant filter (e.g. WHERE tenant_id = ${tenantId}). If the global scan must remain, ensure any per-row actions always validate and constrain by the row's tenant_id before performing sensitive operations.
- **server/routes.ts:771** — backfillProjectDriveFolders executes a SELECT over projects (SELECT p.id, p.name, p.tenant_id, ...) with NO WHERE tenant_id = ... filter. This reads all tenant projects in a multi-tenant DB during startup.
  - _Fix:_ If the backfill is intended to operate per-tenant, iterate tenants and run the SELECT per-tenant with a WHERE p.tenant_id = <tenantId>. If the operation must run over all tenants, ensure it is executed only by a trusted admin process (not exposed to request-derived context), and log/audit actions. Explicitly document and gate cross-tenant admin operations so they are not mistaken for tenant-scoped application logic.
- **server/routes.ts:3443** — Feedback handler selects from messages with only WHERE id = ${messageId} (db.select(...).from(messages).where(eq(messages.id, messageId))). There is no tenant_id filter on the SELECT. Although code checks the returned tenantId afterwards, the raw SELECT still queries a tenant-scoped table without constraining by the caller's tenant, which can leak existence of rows across tenants.
  - _Fix:_ Include tenant scoping in the DB query itself, e.g. add AND eq(messages.tenantId, tenantId) (or equivalent Drizzle .where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId)))). That ensures the DB enforces tenant isolation and avoids any cross-tenant leakage even if later checks are bypassed.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table `conversations` performed without constraining by tenant_id: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. include `AND tenant_id = ${tenantId}` in the WHERE) or perform the change via a storage API that enforces ownership so a request cannot modify another tenant's conversation.
- **server/routes/conversations.ts:99** — SELECT from `project_conversations` performed without an explicit tenant filter: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`
  - _Fix:_ If `project_conversations` is tenant-scoped, include tenant ownership in the WHERE (e.g. `AND tenant_id = ${tenantId}`) or read it via a tenant-aware storage function.
- **server/routes/conversations.ts:103** — INSERT into `project_conversations` does not include tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`
  - _Fix:_ If `project_conversations` is tenant-scoped, the INSERT must supply tenant_id explicitly (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})), or use the storage layer that enforces tenant ownership.
- **server/routes/conversations.ts:105** — UPDATE to `projects` performed without constraining by tenant_id: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) to avoid updating a project in another tenant, or use a tenant-aware API to perform the update.
- **server/routes/inbox-notifications.ts:101** — UPDATE to `inbox_messages` performed without tenant constraint: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` executed when returning a message (missing `AND tenant_id = ${tenantId}`)
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) or use a tenant-aware storage function to mark messages read so a caller cannot affect another tenant's messages.
- **server/seed.ts:4684** — UPDATE on tenant-scoped table 'agent_knowledge' uses only id in WHERE clause (UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id}) after an earlier SELECT that scoped tenant_id = 1. The UPDATE lacks an explicit tenant_id constraint, violating the requirement that writes be tenant-scoped.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) even when the id was obtained from a prior tenant-scoped SELECT. This defends against unexpected id collisions or logic errors and enforces tenant isolation.
- **server/storage.ts:833** — archiveExpiredMemories issues a raw UPDATE on tenant-scoped `memory_entries` (UPDATE memory_entries SET status = 'archived' WHERE ... AND expires_at <= NOW()) with no tenant filter — it operates across all tenants.
  - _Fix:_ If this is intended maintenance, require explicit admin privilege and document it, or iterate per-tenant and scope the UPDATE with tenantId. Prefer per-tenant maintenance or an admin-only migration/cron that cannot be invoked from request handlers.
- **server/storage.ts:849** — archiveStaleMemories issues a raw UPDATE on tenant-scoped `memory_entries` using time-based predicates without any tenant scope, touching all tenants' rows.
  - _Fix:_ As with archiveExpiredMemories, restrict this to admin-only and/or run per-tenant with explicit tenantId scoping. Avoid global unscoped writes to tenant-scoped tables from code reachable by non-admin contexts.
- **server/subagents.ts:771** — announceResult creates a message on the parent conversation with tenantId: run.tenantId ?? 1 — falling back to tenant 1 (admin) if run.tenantId is missing. Using a hardcoded admin fallback risks writing under the wrong tenant.
  - _Fix:_ Remove the hardcoded fallback. Ensure run.tenantId is always set when the run is created; assert/throw if not. If an admin fallback is genuinely required, gate it behind explicit admin checks and audit logging.
- **server/tenant-fork.ts:226** — UPDATE against tenant-scoped config table is performed using only the row PK (entry.table.id) with no tenant_id constraint.
  - _Fix:_ Include the tenant filter on the UPDATE so the write is scoped to the new tenant. Example: .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId))).
- **server/tool-learning.ts:129** — SELECT on tenant-scoped table custom_tools checks only name (eq(customTools.name, parsed.name)) and omits tenant_id in the WHERE clause.
  - _Fix:_ Scope reads to the tenant by adding eq(customTools.tenantId, tenantId) to the WHERE clause (or otherwise validate ownership) to prevent cross-tenant lookup.
- **server/tool-learning.ts:155** — SELECT by id from tenant-scoped table custom_tools is done without constraining tenant_id.
  - _Fix:_ Add a tenant filter to the SELECT, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))).
- **server/tool-learning.ts:365** — UPDATE incrementing usageCount on custom_tools uses only eq(customTools.id, tool.id) without a tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Pass and validate tenantId where required so the operation is properly constrained.
- **server/tools.ts:4718** — INSERT into `project_notes` does not include tenant_id. The INSERT is executed after deriving projectId from conversation but the SQL omits tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) ...). If `project_notes` is tenant-scoped, this will violate the requirement to stamp tenant_id at insert time.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, ...) VALUES (${tenantId}, ${projectId}, ...)). Ensure project ownership was validated (assertProjectInTenant) and use the same validated tenantId when inserting.
- **server/tools.ts:4722** — INSERT into `project_files` does not include tenant_id. The code inserts project file metadata (project_id, file_name, file_type, file_url, ...) without stamping tenant_id. If `project_files` is tenant-scoped this breaks the invariant that tenant_id must be supplied on writes.
  - _Fix:_ Add tenant_id to the INSERT (INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ...)). Ensure the tenantId used is validated (e.g. via assertProjectInTenant) before performing the insert.
- **server/tools.ts:6496** — INSERT into `project_files` (in generate_audio) omits tenant_id. Although the code calls assertProjectInTenant before inserting, the actual INSERT statement does not include tenant_id which violates the rule that tenant-scoped tables must be written with tenant_id set.
  - _Fix:_ Even when ownership was asserted, explicitly include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)) or confirm the table is intentionally global. Prefer using Drizzle's typed insert helpers to ensure tenant_id is always supplied.
- **server/video-job-runner.ts:204** — SELECT cancelRequested FROM videoJobs WHERE jobId = ${jobId} is executed without a tenant_id constraint. Reading a tenant-scoped table row without scoped WHERE allows cross-tenant access if jobId is guessable/faulty.
  - _Fix:_ Include tenant scoping in the WHERE clause (WHERE jobId = ${jobId} AND tenantId = ${tenantId}) when possible, or perform this read only after verifying ownership via a constrained lookup. If jobId is globally-unique and you rely on that property, still add an explicit tenant check to follow the project's tenant-isolation invariant or restrict this helper to internal code only and document the admin-level requirement.

### LOW (1)

- **server/research-engine.ts:559** — (Context) same UPDATE block spans lines 549-559 — ensure tenant constraint applied to this update too.
  - _Fix:_ See previous suggestion: add tenant_id constraint to this UPDATE.

### Suppressed — confirmed false positives (141)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/resume-worker.ts:66** _(internal-worker-pk)_ — SELECT reads agent_runs JOIN agent_approvals without constraining tenant_id — the sweep pulls rows across all tenants (no WHERE tenant_id = <tenant>), exposing cross-tenant data.
  - _Why false positive:_ Server-side worker sweep joins tenant-matched approvals and processes internal run ids, not caller-supplied tenant input.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() updates agent_runs using WHERE eq(agentRuns.id, runId) but does NOT include a tenant_id constraint. This allows modifying another tenant's run row by id.
  - _Why false positive:_ Helper updates agent_runs by server-generated primary key; no evidence runId comes from an untrusted request boundary here.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs WHERE id = runId but omits tenant_id constraint, enabling cross-tenant state mutation by run id.
  - _Why false positive:_ State update helper targets a server-generated run primary key in internal agent workflows, not request-derived cross-tenant input.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs WHERE id = runId with no tenant_id filter — permits cross-tenant overwrites of run state.
  - _Why false positive:_ Internal helper replaces state by globally unique run id; no untrusted request boundary shown for runId.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs status/result by run id but lacks a tenant_id constraint in the WHERE clause, allowing completion of runs belonging to other tenants.
  - _Why false positive:_ Completion helper mutates internal agent run row by server-generated primary key, not tenant-controlled selector.
- **server/agentic/runs.ts:67** _(internal-worker-pk)_ — failRun() updates agent_runs WHERE id = runId with no tenant_id condition, enabling cross-tenant failure markings.
  - _Why false positive:_ Failure helper is an internal state-transition on agent_runs by unique run id, with no request-sourced id shown.
- **server/agentic/runs.ts:76** _(internal-worker-pk)_ — pauseRun() updates agent_runs WHERE id = runId but omits tenant_id filter; this lets callers pause runs in other tenants by id.
  - _Why false positive:_ Pause helper updates internal run state via server-generated PK; no evidence of untrusted cross-tenant id input.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images without tenant constraint or ownership verification. This writes presenter slide images tied to a session_id but does not ensure the session belongs to the current tenant nor include a tenant_id column.
  - _Why false positive:_ presenter_slide_images is a child of tenant-scoped presenter_sessions via session_id and has no tenant_id by design
- **server/google-workspace.ts:2364** _(fk-transitive)_ — INSERT into presenter_slide_images (thumbnail/‘thumb’) without tenant constraint or ownership verification (same pattern as prior INSERT).
  - _Why false positive:_ presenter_slide_images thumbnail row is a child of tenant-scoped presenter_sessions via session_id and has no tenant_id
- **server/heartbeat.ts:101** _(internal-worker-pk)_ — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint — possible cross-tenant modification.
  - _Why false positive:_ background heartbeat worker updating its own loaded schedule row by server-derived primary key, not request input
- **server/heartbeat.ts:106** _(internal-worker-pk)_ — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint — possible cross-tenant modification.
  - _Why false positive:_ internal scheduler advances loaded research_schedules row by server-derived id
- **server/heartbeat.ts:112** _(internal-worker-pk)_ — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint — possible cross-tenant modification.
  - _Why false positive:_ internal heartbeat path updates already-loaded schedule by server-owned primary key
- **server/heartbeat.ts:119** _(internal-worker-pk)_ — UPDATE research_schedules WHERE id = ${sched.id} without tenant_id constraint in recovery path — possible cross-tenant modification.
  - _Why false positive:_ recovery update is in internal worker flow using loaded schedule id, not untrusted caller input
- **server/heartbeat.ts:257** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} performed without tenant_id constraint — mutates heartbeat_tasks across tenants.
  - _Why false positive:_ startup maintenance worker updates heartbeat_tasks by ids from server-loaded task rows, not cross-tenant request data
- **server/heartbeat.ts:261** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} performed without tenant_id constraint — mutates heartbeat_tasks across tenants.
  - _Why false positive:_ internal startup normalization updates loaded heartbeat task row by server-derived primary key
- **server/heartbeat.ts:2955** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint — may set approval_status on a task belonging to a different tenant if id collisions occur or storage is inconsistent.
  - _Why false positive:_ row was just created in same call path with trusted tenantId; follow-up update targets that newly created task id
- **server/heartbeat.ts:3331** _(global-table)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint in delegateTaskFromChat path — cross-tenant write risk.
  - _Why false positive:_ Flagged line creates heartbeat_logs row; referenced schema marks heartbeat_logs as not tenant-scoped and comments describe platform-global pacing telemetry.
- **server/internal-resolver.ts:91** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `event_log` (finalizeEvent) omits a tenant_id constraint in the WHERE clause — the resolver can finalize events across tenants.
  - _Why false positive:_ finalizeEvent is an internal resolver helper updating event_log by server-side eventId in deferred worker flow, not from a request boundary.
- **server/internal-resolver.ts:106** _(internal-worker-pk)_ — SELECT from tenant-scoped table `plans` by id has no tenant_id filter. The resolver reads a foreign plan row (planId from event data) without verifying the caller/actor owns that plan's tenant.
  - _Why false positive:_ Internal resolver reads plans by server-derived planId from event processing, not directly from untrusted tenant request input.
- **server/internal-resolver.ts:170** _(internal-worker-pk)_ — UPDATE on `code_proposals` (resolveExperimentFailed) mutates a tenant-scoped row by id without constraining by tenant_id — can archive proposals across tenants.
  - _Why false positive:_ Internal resolver archives code_proposals by server-processed proposalId from event data in worker path, not caller-controlled request params/body.
- **server/memory-graph.ts:194** _(fk-transitive)_ — INSERT into memory_links does not set tenant_id. The code runs: INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count) VALUES (...). Every INSERT into tenant-scoped tables must include tenant_id — omitting it lets links be created without tenant ownership and breaches tenant isolation.
  - _Why false positive:_ memory_links has no tenant_id column per schema; isolation is via source/target memory_entries which are tenant-scoped and later joined through tenant-filtered memory_entries.
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code verifies project ownership via assertProjectInTenant, but the INSERT itself omits tenant_id (per project_files schema the tenant_id column is required/no default). This can break tenant isolation or create records not tied to the caller tenant.
  - _Why false positive:_ project_files has no tenant_id column per schema; assertProjectInTenant verifies parent project ownership and child row is isolated transitively by project_id.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (in produceVideoParallel) omits tenant_id. Although the code calls assertProjectInTenant before the insert, the INSERT statement does not set tenant_id.
  - _Why false positive:_ project_files is not tenant-scoped in schema and insert is guarded by assertProjectInTenant on the parent project, matching transitive isolation.
- **server/orchestrator-ledger.ts:452** _(upstream-verified)_ — UPDATE memory_entries ... WHERE id = ${row.id} lacks a tenant_id constraint. This is a write to a tenant-scoped table without constraining to the caller's tenant, violating the tenant-isolation invariant.
  - _Why false positive:_ Same transaction selects the row with tenant_id = tenantId immediately above; update uses that selected primary key under advisory lock.
- **server/sculptor.ts:94** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: `UPDATE sculptor_sessions SET status = 'failed', ... WHERE id = ${session.id}` — write is not constrained by tenant_id.
  - _Why false positive:_ Updates the same sculptor_sessions row just inserted in this call with tenant_id from params; row ownership is established by the preceding INSERT RETURNING.
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: `UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}`.
  - _Why false positive:_ Updates the newly created session row returned from the tenant-scoped INSERT in the same function, so ownership is already established.
- **server/sculptor.ts:156** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant constraint: finalization UPDATE uses `WHERE id = ${sessionId}` with no tenant_id filter.
  - _Why false positive:_ Background monitorSession worker updates by server-generated sessionId passed from the creator path, not from an untrusted request boundary.
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant constraint: `UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}` — this can modify another tenant's ledger row.
  - _Why false positive:_ Best-effort internal helper settles a reservation row by server-generated reservationId, not directly from request input.
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant constraint: `UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId}`.
  - _Why false positive:_ Internal helper releases a reservation by server-generated reservationId; not shown as caller-controlled cross-tenant input.
- **server/seed.ts:3136** _(upstream-verified)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... — UPDATE on a tenant-scoped table without constraining by tenant_id.
  - _Why false positive:_ Guarded by isOwnerDeployment() and targets a specific owner-deployment row id=15; explicit one-off owner seed path, not general tenant access.
- **server/seed.ts:3212** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) — INSERT into a tenant-scoped linking table without passing tenant_id explicitly.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped conversation/project via foreign keys; insert without tenant_id matches transitive isolation design.
- **server/tools.ts:3197** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The statement INSERT INTO project_files (...) VALUES (...) does not set tenant_id (nor otherwise enforce tenant scope) which violates the rule that tenant-scoped writes must include tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insertion is gated by prior owned-project check; isolation is via project_id -> projects tenant scope.
- **server/tools.ts:3112** _(upstream-verified)_ — INSERT into project_notes omits tenant_id. The statement INSERT INTO project_notes (project_id, note, author) VALUES (...) does not set tenant_id or otherwise record tenant ownership.
  - _Why false positive:_ this is a tenant-scoped read of project_notes via join to projects with p.tenant_id = tenantId.
- **server/tools.ts:3094** _(fk-transitive)_ — INSERT into project_conversations during project creation omits tenant_id. The code INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not include tenant_id, so the created linkage may not be tenant-scoped.
  - _Why false positive:_ project_conversations has no tenant_id and row is only inserted after conversation update scoped to tenant and project creation in same tenant.
- **server/tools.ts:3236** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) omits tenant_id. The code INSERT INTO project_conversations (project_id, conversation_id) VALUES (${params.id}, ${params.conversationId}) is executed after project existence check but does not set tenant_id on the inserted row.
  - _Why false positive:_ project_conversations has no tenant_id; project ownership and conversation-in-tenant are verified before insert, so isolation is transitive via parent rows.
- **server/tools/domains/files/handlers.ts:836** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id (INSERT INTO project_files (...) VALUES (...)) — tenant_id column is omitted, violating the requirement that every tenant-scoped INSERT include the caller's tenant id.
  - _Why false positive:_ project_files has no tenant_id and project is derived by joining conversation to projects scoped to ctx.tenantId before insert.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE against tenant-scoped `agentActivity` table without tenant constraint: `db.update(agentActivity).set(...).where(eq(agentActivity.id, activityId))` can update rows in other tenants if ids collide.
  - _Why false positive:_ update is by server-generated activityId primary key in internal helper, not shown crossing an untrusted request boundary
- **server/agentic-engines.ts:344** _(global-table)_ — Unscoped SELECT on tenant-scoped `heartbeat_logs` table in gatherOperationalData: `SELECT COUNT(...) FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` has no tenant filter and will aggregate across tenants.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id column, indicating an intentionally global table
- **server/agentic-engines.ts:449** _(global-table)_ — Unscoped SELECT on tenant-scoped `heartbeat_logs` table in runPredictiveEngine: `SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10` lacks tenant scoping.
  - _Why false positive:_ heartbeat_logs has no tenant_id column per schema, so reading recent logs is from a global table
- **server/agentic-engines.ts:516** _(global-table)_ — Unscoped aggregation on tenant-scoped `heartbeat_logs` in runOptimizationEngine: `SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...` has no tenant filter and will return multi-tenant data.
  - _Why false positive:_ heartbeat_logs is explicitly non-tenant-scoped in schema, so global aggregation is expected
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE on `outreach_enrollments` performed with `WHERE id = ${row.id}` only; no tenant constraint is included. This can update an enrollment row in another tenant if ids collide or if `row.id` came from untrusted source.
  - _Why false positive:_ row.id comes from the immediately preceding SELECT on outreach_enrollments scoped by e.tenant_id = params.tenantId; update targets that verified row.
- **server/agentic/approvals.ts:122** _(global-table)_ — Bulk UPDATE in expireStaleApprovals misses tenant scoping: `db.update(agentApprovals).set({...}).where(and(eq(agentApprovals.status, 'pending'), lt(agentApprovals.expiresAt, new Date())))` updates pending approvals across all tenants.
  - _Why false positive:_ expireStaleApprovals is an intentional global sweep/worker that expires all pending approvals system-wide, then updates linked runs per returned tenantId.
- **server/agentic/department-budgets.ts:143** _(global-table)_ — Query selects across the tenant-scoped table `department_budgets` without constraining to a specific tenant (missing WHERE tenant_id = ...). This reads tenant data for all tenants.
  - _Why false positive:_ runBudgetEnforcement is explicitly a heartbeat-callable cross-tenant enforcement sweep selecting tenant_id,department for all active budgets.
- **server/auto-asset-capture.ts:207** _(upstream-verified)_ — SELECT content FROM messages WHERE conversation_id = ${c.id} ... queries the messages table without a tenant_id constraint. Even though conversation rows were previously selected, the messages read itself has no tenant_id filter, violating the requirement that tenant-scoped reads include tenant constraints.
  - _Why false positive:_ Messages are read by conversation_id from conversation rows selected earlier in the same path, carrying c.tenant_id used downstream; access is chained from verified parent conversation rows.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'conversations' uses only the row id in the WHERE clause (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL). There's no tenant_id constraint, so a poisoned/foreign conversation id could cause a cross-tenant update.
  - _Why false positive:_ c.project_id and c.conversation_id come from prior join enforcing p.tenant_id = c.tenant_id; update applies to that verified conversation row.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id for a likely tenant-scoped table.
  - _Why false positive:_ invoice_items has no tenant_id column and is a child table of tenant-scoped invoices via invoice_id; insert without tenant_id is by design.
- **server/ceo-orchestrator.ts:821** _(global-table)_ — SELECT from tenant-scoped personas table without restricting by tenant. cachedPersonas = await db.select().from(personasTable) reads all personas across tenants.
  - _Why false positive:_ schema note shows personas has no tenant_id, so this read is from a non-tenant-scoped table
- **server/channel-routing.ts:30** _(global-table)_ — listChannelRoutes performs a JOIN against personas (tenant-scoped) with no tenant constraint: SELECT ... FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ... — this reads persona rows across tenants.
  - _Why false positive:_ personas has no tenant_id and channel_routes is created as global with UNIQUE(channel), indicating platform-global routing
- **server/data-protection.ts:72** _(upstream-verified)_ — UPDATE against tenant-scoped table `conversations` executed without scoping by tenant_id (WHERE id = ${conversationId} but no AND tenant_id = ${tenantId}). This allows modifying a conversation row even if the caller's tenant doesn't own it.
  - _Why false positive:_ same function first SELECTs conversation by id and tenant_id, then updates that verified row
- **server/data-protection.ts:125** _(upstream-verified)_ — DELETE statements in purge loop run without tenant_id scoping (DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}). These DELETEs touch tenant-scoped tables without an explicit tenant predicate.
  - _Why false positive:_ rows are selected from conversations first, and child deletes are by verified conversation_id; final conversation delete is by that verified id
- **server/delivery-pipeline.ts:188** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `delivery_logs` executed without any tenant_id constraint (db.update(deliveryLogs).set(...).where(eq(deliveryLogs.id, id))). This allows modifying a delivery record by id regardless of caller tenant.
  - _Why false positive:_ helper updates delivery_logs by server-generated id within internal delivery pipeline, not from a request boundary here
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM doc_chunks executed without tenant filter (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). doc_chunks is tenant-scoped; this can delete rows belonging to other tenants if collection_id is reused or mismatched.
  - _Why false positive:_ same call path verifies doc_collections ownership with id and tenant_id before deleting chunks for that collection
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE on tenant-scoped table `doc_chunks` sets embedding by id only (UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}) with no tenant_id constraint. This can overwrite another tenant's chunk if ids collide or are guessed.
  - _Why false positive:_ rows to update were selected just above with collection_id and tenant_id, so row.id comes from a tenant-scoped query in the same function
- **server/email.ts:424** _(upstream-verified)_ — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} is executed without a tenant_id constraint. Updating by id alone can modify another tenant's row if ids are misused or guessed.
  - _Why false positive:_ UPDATE uses row.id sourced from the immediately preceding untrusted-independent SELECT in the same function; not a request-boundary cross-tenant write.
- **server/firecrawl.ts:192** _(upstream-verified)_ — UPDATE on scraped_pages uses WHERE id = <existing id> but does NOT include tenant_id constraint. This allows an attacker who knows or guesses a global row id to update another tenant's row because the UPDATE isn't explicitly scoped to the caller's tenant.
  - _Why false positive:_ existing[0].id comes from an immediately prior SELECT filtered by tenantId in the same call path.
- **server/firecrawl.ts:316** _(upstream-verified)_ — UPDATE on scraped_pages uses WHERE id = <existing id> but omits tenant_id scoping. Although the row was selected earlier with a tenant filter, the subsequent UPDATE by id alone lacks an explicit tenant constraint and could modify a row outside the caller's tenant if ids are predictable or leaked.
  - _Why false positive:_ UPDATE id is obtained from the immediately preceding tenant-scoped SELECT on scraped_pages.
- **server/health-audit.ts:333** _(upstream-verified)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — no tenant constraint. This may archive proposals across tenants.
  - _Why false positive:_ UPDATE targets ids collected from the immediately preceding SELECT in the same function; not directly caller-controlled.
- **server/job-queue.ts:98** _(global-table)_ — Atomic claim query (claimDueJobs) updates `agent_jobs` across all rows without a tenant_id constraint. This will claim jobs for any tenant even when running in a tenant-specific context or when callers expect tenant separation.
  - _Why false positive:_ Global worker queue claim logic intentionally operates across all tenant-scoped jobs; this module is a system job runner, not a tenant request path.
- **server/job-queue.ts:134** _(global-table)_ — reclaimExpiredLeases updates `agent_jobs` (marking expired leases terminal / requeued) without any tenant_id constraint — touches jobs for all tenants in bulk.
  - _Why false positive:_ Lease reclamation is a global background maintenance operation over the shared job queue, intentionally not tenant-filtered.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates `agent_jobs` to 'succeeded' by id but without verifying tenant_id. A stale/forged id could allow cross-tenant row transitions.
  - _Why false positive:_ completeJob mutates by server-generated job primary key plus attempts fence inside internal worker flow, not from an untrusted tenant request boundary.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob reads `agent_jobs` max_attempts via SELECT without scoping by tenant_id, then later updates the same table without tenant_id. Reads/updates are not tenant-scoped.
  - _Why false positive:_ failJob reads by server-generated job id within internal worker handling; id is paired with attempts fencing and not shown as caller-controlled.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob terminal-update writes `agent_jobs` to 'failed_terminal' without tenant_id in WHERE clause — risk of cross-tenant mutation.
  - _Why false positive:_ Terminal fail transition is an internal worker state update by server-generated job pk with attempts fence, not tenant-user supplied.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob retry-update writes `agent_jobs` back to pending (with next_run_at) without tenant_id in the WHERE clause — unscoped updates across tenants.
  - _Why false positive:_ Retry requeue transition is an internal worker state update by trusted job pk with attempts fence.
- **server/lib/inbox-ingest.ts:504** _(upstream-verified)_ — UPDATE to inbox_classifications omits tenant_id constraint. The `UPDATE inbox_classifications SET routed_to = ${...} WHERE inbox_message_id = ${storeResult.id}` updates solely by inbox_message_id without verifying tenant, allowing cross-tenant modification if an ID is known.
  - _Why false positive:_ The classification row was just claimed earlier in the same call path with INSERT tenant_id=${tenantId}, inbox_message_id=${storeResult.id}; this update continues that owned row by inbox_message_id.
- **server/lib/scheduled-post-runner.ts:164** _(upstream-verified)_ — UPDATE scheduled_posts ... WHERE id = ${row.id} executes without also constraining by tenant_id. Updating by id alone can modify a tenant-scoped row without verifying the tenant context.
  - _Why false positive:_ row.id comes from the immediately prior locked/updated SELECT RETURNING of scheduled_posts in the same runner flow
- **server/lib/scheduled-post-runner.ts:191** _(upstream-verified)_ — Error-path UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' lacks tenant_id filtering. This can persist status/result changes to rows without verifying tenant ownership.
  - _Why false positive:_ error-path update targets the same row.id previously claimed by the locked SELECT/UPDATE in this call path
- **server/memory-graph.ts:181** _(fk-transitive)_ — SELECT existence check against memory_links omits tenant scoping: SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR (...). This reads a tenant-scoped table without a tenant_id constraint.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries selected from the same tenant; isolation is transitive via source/target memory ids
- **server/memory-graph.ts:66** _(upstream-verified)_ — UPDATE memory_categories increments memory_count by id only (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}) without a tenant_id clause. This updates a tenant-scoped table row without verifying tenant ownership in the WHERE clause.
  - _Why false positive:_ catId is returned by ensureCategory(categoryName, tenantId, ...) which looks up/inserts only within the provided tenant before this update
- **server/memory-graph.ts:392** _(upstream-verified)_ — UPDATE to memory_entries in categorizeExistingMemories uses only id in the WHERE clause: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}. This performs a tenant-scoped write without constraining to tenant_id.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries constrained by tenant_id in the same function
- **server/pace-control.ts:85** _(global-table)_ — Query against heartbeat_logs omits tenant_id. The COUNT(*) query on heartbeat_logs runs without scoping to a tenant, allowing cross-tenant observation/behavior and violating the requirement that tenant-scoped tables must be filtered by tenant_id.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id column, so this is an intentionally global table query.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot's aggregation query against heartbeat_logs has no tenant_id filter, reading tenant-scoped rows globally.
  - _Why false positive:_ heartbeat_logs has no tenant_id column per schema; global aggregation matches platform-global table pattern.
- **server/pace-control.ts:147** _(global-table)_ — Persona breakdown query (by persona_name) against heartbeat_logs is not tenant-scoped. It can expose per-tenant usage cross-tenant if heartbeat_logs is tenant-scoped.
  - _Why false positive:_ Persona breakdown reads heartbeat_logs, which schema marks as non-tenant-scoped; global metrics are benign here.
- **server/plan-executor.ts:117** _(upstream-verified)_ — UPDATE plans ... WHERE id = ${planId} (appendExecutionLog) updates a tenant-scoped table without constraining by tenant_id.
  - _Why false positive:_ appendExecutionLog is only invoked after executePlan claims the plan row and obtains its tenant_id; subsequent writes target that same server-selected plan id in the same call path.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — UPDATE plans SET status='executing' WHERE id = ${planId} ... RETURNING ... — claiming/executing a plan by id without scoping to tenant_id.
  - _Why false positive:_ executePlan is an internal worker operating by globally unique plan primary key, not shown as taking tenant-controlled request input at this line.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} — writes to tenant-scoped plans table without tenant_id constraint.
  - _Why false positive:_ Immediately follows the claim UPDATE that selected the exact plan row and returned row.tenant_id in the same function; this status change applies to that already-verified plan.
- **server/plan-executor.ts:725** _(upstream-verified)_ — UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} — final status update lacks tenant_id constraint.
  - _Why false positive:_ Final status update occurs in the same call path after the plan was claimed and its tenant_id loaded from the database; ownership was established earlier.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' — error-path write missing tenant_id constraint.
  - _Why false positive:_ Worker error-path update uses server-side planId in internal executor flow, not request-derived input.
- **server/process-governor.ts:239** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) as failing_agents FROM heartbeat_logs ... — reads heartbeat_logs without filtering by tenant_id (case 'cascading_failures'), potentially aggregating across tenants.
  - _Why false positive:_ heartbeat_logs schema has no tenant_id; query targets platform-global log table by design.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — disabling a task by id without constraining tenant_id.
  - _Why false positive:_ t.id comes from immediately preceding SELECT scoped by ht.tenant_id = tenantId in same flow.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} — mutating agent_desks by id with no tenant_id constraint.
  - _Why false positive:_ desk.id comes from immediately preceding SELECT on agent_desks filtered by ad.tenant_id = tenantId.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} — fetching a code proposal by id without tenant_id filtering (tenant-scoped table).
  - _Why false positive:_ Verifier is an internal server worker operating on globally unique proposalId, not a request-bound tenant query.
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals ... - 'clear rebased_at' update uses WHERE id = ${proposalId} with no tenant constraint, mutating proposals across tenants.
  - _Why false positive:_ Internal verifier worker updates proposal by server-invoked proposalId primary key, not untrusted tenant input.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} — attemptRebaseOnDrift marks proposals rejected without tenant scoping.
  - _Why false positive:_ Rebase helper is internal worker logic mutating proposal by server-side proposalId primary key.
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — UPDATE code_proposals ... SET validation_result = ... WHERE id = ${proposalId} — marking 'hallucinated' rebases without tenant filter.
  - _Why false positive:_ Hallucination mark is internal verifier worker update keyed by server-side proposalId primary key.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ... WHERE id = ${proposalId} — writing rebased diff back to proposal without tenant constraint.
  - _Why false positive:_ Background verifier/queue updates code_proposals by server-side proposalId, not from a request boundary.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — UPDATE code_proposals SET verification_status/verification_details ... WHERE id = ${proposalId} — persist() writes verification metadata without tenant scoping.
  - _Why false positive:_ persist() is used by internal verifier flow to update a proposal by server-generated id, not caller-controlled tenant context.
- **server/recurring-messages.ts:299** _(upstream-verified)_ — UPDATE to tenant-scoped table agent_knowledge omits tenant_id in the WHERE clause (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}). This can update a row from a different tenant if an id collides or is manipulated.
  - _Why false positive:_ Update uses r.id from rows already loaded in tenant context; same flow also uses r.tenant_id for scoped conversation operations.
- **server/reference-learner.ts:289** _(upstream-verified)_ — UPDATE to tenant-scoped table memory_entries omits tenant_id in the WHERE clause (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This allows cross-tenant modification if a row id is guessed or collides.
  - _Why false positive:_ storedId comes from an immediately preceding SELECT on memory_entries constrained by input.tenantId and persona/category/dedup key.
- **server/research-engine.ts:310** _(upstream-verified)_ — UPDATE research_programs SET model = ... WHERE id = ${programId} lacks tenant constraint — can modify another tenant's program.
  - _Why false positive:_ Program row was fetched just above with WHERE id = programId AND tenant_id = tenantId before fallback model update.
- **server/research-engine.ts:450** _(upstream-verified)_ — SELECT FROM research_experiments WHERE session_id = ${sessionId} ... omits tenant_id — reads experiments potentially across tenants.
  - _Why false positive:_ Experiments are read within an internal session flow using sessionId from the active session object already bound to session.tenantId.
- **server/research-engine.ts:459** _(upstream-verified)_ — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} omits tenant filter — may read another tenant's program.
  - _Why false positive:_ session.programId comes from startResearchSession after loading the program with tenant_id = tenantId; same call path binds session to that tenant.
- **server/research-engine.ts:464** _(upstream-verified)_ — SELECT summary FROM research_sessions WHERE id = ${sessionId} lacks tenant constraint — can leak other tenants' session summaries.
  - _Why false positive:_ sessionId is from the in-memory ActiveSession created in a tenant-bound startResearchSession flow, not arbitrary caller input.
- **server/research-engine.ts:469** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author, created_at) does not set tenant_id — tenant-scoped table inserts must provide tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated transitively through validated project_id ownership.
- **server/research-engine.ts:477** _(fk-transitive)_ — Second INSERT INTO project_notes (project_id, note, author, created_at) also omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and insert is scoped by project_id after assertProjectInTenant validation.
- **server/research-engine.ts:549** _(internal-worker-pk)_ — UPDATE research_sessions SET ... WHERE id = ${sessionId} (session end) has no tenant filter — modifies session rows across tenants.
  - _Why false positive:_ Server-side worker updates a session tracked in activeSessions by server-generated sessionId/session.tenantId, not directly from request input.
- **server/research-engine.ts:859** _(internal-worker-pk)_ — UPDATE research_sessions in DB-timeout handling (setting totals) omits tenant constraint — may update other tenants' sessions.
  - _Why false positive:_ Internal error-handling path updates research_sessions by server-held session.sessionId from in-memory worker state, not an untrusted tenant boundary.
- **server/research-engine.ts:887** _(internal-worker-pk)_ — UPDATE research_sessions SET model = ${fallbackModel} WHERE id = ${session.sessionId} omits tenant filter — changes another tenant's session potentially.
  - _Why false positive:_ Fallback-model update is inside internal research worker flow using server-controlled session.sessionId, not caller-supplied id.
- **server/research-engine.ts:926** _(internal-worker-pk)_ — UPDATE research_sessions SET total_experiments... WHERE id = ${session.sessionId} (end of run) lacks tenant constraint.
  - _Why false positive:_ End-of-run status write targets the current worker's server-tracked session primary key, not a request-derived identifier.
- **server/research-engine.ts:820** _(internal-worker-pk)_ — UPDATE research_experiments SET ... WHERE id = ${experimentId} (main persist) omits tenant filter — writes experiment rows without verifying tenant ownership.
  - _Why false positive:_ Experiment row is updated by internal worker using experimentId created/held server-side for the current run, not from untrusted input.
- **server/research-engine.ts:851** _(internal-worker-pk)_ — UPDATE research_experiments SET status = 'crash' ... WHERE id = ${experimentId} (timeout handler) lacks tenant constraint.
  - _Why false positive:_ Timeout-handler update uses internal experimentId from the worker's own DB-created experiment row, not a cross-tenant request parameter.
- **server/research-engine.ts:878** _(internal-worker-pk)_ — UPDATE research_experiments SET ... WHERE id = ${experimentId} (auth error branch) is missing tenant constraint.
  - _Why false positive:_ Auth-error branch updates experiment by server-managed primary key within internal worker execution.
- **server/research-engine.ts:894** _(internal-worker-pk)_ — UPDATE research_experiments SET ... WHERE id = ${experimentId} (retry/transient branch) omits tenant guard.
  - _Why false positive:_ Retry-branch experiment update is keyed by server-side experimentId in internal processing, not untrusted input.
- **server/research-engine.ts:910** _(internal-worker-pk)_ — UPDATE research_experiments SET ... WHERE id = ${experimentId} (final error branch) lacks tenant constraint.
  - _Why false positive:_ Crash-branch experiment update uses worker-owned experimentId generated earlier server-side.
- **server/research-engine.ts:1482** _(upstream-verified)_ — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL (replayHighValueFindings) lacks tenant constraint — the surrounding SELECT filtered by tenant, but the UPDATE must also include tenant guard.
  - _Why false positive:_ The row.id being claimed comes from the immediately preceding SELECT already filtered to tenantId, so this UPDATE operates on tenant-scoped rows selected in the same call path.
- **server/research-engine.ts:1524** _(upstream-verified)_ — UPDATE research_experiments SET replayed_at = NULL WHERE id = ${row.id} AND replayed_proposal_id IS NULL (error rollback) omits tenant filter.
  - _Why false positive:_ Rollback UPDATE uses row.id from the same tenant-filtered SELECT/claim loop, so ownership was established earlier in this call path.
- **server/research-engine.ts:1597** _(upstream-verified)_ — UPDATE code_proposals SET status = 'needs_review', validation_result = ... WHERE id = ${proposalId} (safeApplyProposal) omits tenant constraint — modifying proposals without verifying tenant ownership.
  - _Why false positive:_ safeApplyProposal first SELECTs the proposal by id and tenant_id and returns if absent, so later status update is on an already tenant-verified proposal in the same flow.
- **server/research-engine.ts:1773** _(upstream-verified)_ — UPDATE code_proposals SET status = 'reverted', reviewed_at = NOW() WHERE id = ${proposalId} (git checkout revert path) omits tenant filter.
  - _Why false positive:_ revertProposal first loads the proposal with WHERE id = proposalId AND tenant_id = tenantId, so the later status update operates on an already tenant-verified row in the same call path.
- **server/research-engine.ts:1783** _(upstream-verified)_ — UPDATE code_proposals SET status = 'reverted' WHERE id = ${proposalId} (file-replace revert path) lacks tenant constraint.
  - _Why false positive:_ same function previously enforces proposal ownership via SELECT * FROM code_proposals WHERE id = proposalId AND tenant_id = tenantId before this update.
- **server/routes.ts:4337** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts `project_id, note, author` but omits tenant_id, and the project_id used was looked up without a tenant check, so this can create notes associated with projects across tenants.
  - _Why false positive:_ project_notes has no tenant_id by schema, so omitting tenant_id on INSERT is expected child-table behavior scoped transitively via project_id.
- **server/routes/admin-extra.ts:128** _(upstream-verified)_ — Raw/interpolated SQL (drizzle sql`... ${tenantId}`) used with a request-derived tenantId in the decline_events SELECT. This mixes templated SQL with request data and deviates from the enforced Drizzle .where(eq(...)) pattern — increasing isolation/injection risk.
  - _Why false positive:_ the query explicitly constrains decline_events by tenant_id = tenantId, and tenantId is derived from req session/context, not caller-supplied query/body.
- **server/routes/admin-extra.ts:216** _(upstream-verified)_ — Multiple raw sql template queries in account deletion-summary (e.g. SELECT count(*) FROM conversations WHERE tenant_id = ${tenantId}) embed the request-derived tenantId via sql``/string interpolation instead of using Drizzle's typed query API.
  - _Why false positive:_ each account-summary query uses tenantId obtained from getTenantFromRequest(req) and applies it in tenant predicates; this is not an isolation miss.
- **server/routes/admin-wedges.ts:45** _(upstream-verified)_ — Raw sql interpolation used with request-derived tenantId in the projects SELECT (WHERE tenant_id = ${tenantId}). Using sql`` with request-derived values bypasses the preferred Drizzle .where(eq(...)) pattern and risks injection/isolation mistakes.
  - _Why false positive:_ the projects query includes WHERE tenant_id = tenantId, and tenantId is taken from authenticated admin request context after access checks.
- **server/routes/admin-zombie-detector.ts:38** _(global-table)_ — SQL executed inside the transaction interpolates a request-derived windowDays value (e.g. started_at > NOW() - (${windowDays}::int * INTERVAL '1 day')) via sql``. Even though executed in a transaction this is string-interpolated SQL using request input.
  - _Why false positive:_ admin-only/global analytics over platform-wide tables; not a tenant-isolation flaw
- **server/routes/admin.ts:661** _(upstream-verified)_ — The repair_incidents SELECT is constructed using sql`` fragments where tenant_id = ${tenantId} (and later WHERE ${where}). This builds SQL via interpolated sql pieces that include a request-derived tenantId rather than using the Drizzle query API.
  - _Why false positive:_ route is restricted to ADMIN_TENANT_ID and query explicitly filters repair_incidents by tenant_id = tenantId
- **server/routes/api-v1.ts:395** _(upstream-verified)_ — Agents list query uses a raw sql`` with an interpolated request-derived tenantId in the LEFT JOIN condition (tpn.tenant_id = ${tenantId}). This relies on templated SQL interpolation instead of an explicit Drizzle .where(eq(...)) constraint.
  - _Why false positive:_ LEFT JOIN constrains tenant_persona_names.tenant_id to caller tenant; personas is global by schema
- **server/routes/api-v1.ts:653** _(global-table)_ — AG-UI /agui/run persona lookup uses sql`` embedding request body.agent (SELECT id FROM personas WHERE LOWER(name) = LOWER(${body.agent})). This interpolates a request-derived string into raw SQL rather than relying on the query builder, increasing injection/isolation risk.
  - _Why false positive:_ personas has no tenant_id and is looked up as a platform-global registry
- **server/routes/api-v1.ts:780** _(upstream-verified)_ — Conversation poll uses a raw sql`` query with interpolated conversationId and tenantId (WHERE m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId}). Although tenant checks exist elsewhere, this is still templated SQL embedding request-derived values instead of using Drizzle's typed .where(eq(...)) expressions.
  - _Why false positive:_ same call path first fetches conversation via storage.getConversation(conversationId, tenantId) and query also filters c.tenant_id = tenantId
- **server/routes/billing.ts:109** _(upstream-verified)_ — Tenant update uses raw sql`` interpolation with request-derived tenantId (UPDATE tenants SET plan = ${plan} WHERE id = ${tenantId}). This mixes request data into templated SQL rather than an explicit Drizzle update with eq(id, tenantId).
  - _Why false positive:_ tenants table is keyed by caller tenant id and Stripe session metadata is verified to match tenantId before update
- **server/routes/conversations.ts:227** _(fk-transitive)_ — INSERT into `project_notes` performed without tenant scoping: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` — no tenant_id passed/checked
  - _Why false positive:_ project_notes has no tenant_id and insert is transitively scoped through previously verified tenant-owned project_id
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) ... is missing tenant_id, so the new row may be created without the required tenant ownership column (or fail if tenant_id is NOT NULL).
  - _Why false positive:_ project_notes has no tenant_id and parent project ownership is verified immediately before insert
- **server/routes/projects.ts:225** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload path) does not set tenant_id. The INSERT INTO project_files (...) VALUES (${projectId}, ...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped through tenant-verified parent project_id
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload path) does not set tenant_id. The INSERT INTO project_files (...) VALUES (${projectId}, ...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped through tenant-verified parent project_id
- **server/seed.ts:34** _(upstream-verified)_ — SELECT COUNT(*) FROM research_sessions executed with no tenant_id filter — reads tenant-scoped table across all tenants.
  - _Why false positive:_ boot-time seed/dev snapshot import script, not caller-driven tenant access; global emptiness check only gates one-time seeding
- **server/seed.ts:2854** _(upstream-verified)_ — SELECT id, name, model FROM research_programs WHERE is_active = true — reads research_programs across all tenants (no tenant_id constraint).
  - _Why false positive:_ seed maintenance routine scans all active programs to normalize invalid model ids; offline boot-time fix, not request-bound tenant exposure
- **server/seed.ts:3205** _(fk-transitive)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... — selecting conversations with no tenant_id constraint, potentially returning rows for other tenants.
  - _Why false positive:_ project_conversations has no tenant_id and is a child link table; backfill derives links from tenant-scoped conversations/project_id relationship transitively
- **server/skillify.ts:223** _(global-table)_ — SELECT from `personas` by id is done without a tenant_id constraint. If `personas` is tenant-scoped, this reads another tenant's persona row (IDOR) because the SQL itself lacks tenant scoping.
  - _Why false positive:_ schema says personas has no tenant_id, so persona lookup is from a global table
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` (recentMsgs in distillIntent) uses conversationId only and lacks an explicit tenant_id filter in the SQL. This performs an unscoped read of messages that may belong to another tenant.
  - _Why false positive:_ same call path first calls analyzeConversation(conversationId, tenantId), which verifies conversation ownership before reading its messages
- **server/skillify.ts:203** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` (in analyzeConversation) uses only conversationId in the SQL and omits an explicit tenant_id condition. This allows reading messages across tenants until an application-level check runs.
  - _Why false positive:_ messages are fetched only after analyzeConversation verified the conversation belongs to tenantId in the same function
- **server/skillify.ts:331** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` (recentMsgs in skillifyConversation) is performed without an explicit tenant_id constraint. The query only filters by conversationId, so it reads potentially cross-tenant data in SQL before any app-level validation.
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), enforcing conversation ownership earlier in the same call path
- **server/storage.ts:289** _(upstream-verified)_ — attachCitationMetadata issues a raw SELECT from `messages` (SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL) without scoping by tenant_id. messages is tenant-scoped, so this may leak citations across tenants.
  - _Why false positive:_ Query only re-reads messages by ids taken from msgs already fetched with tenant-scoped WHERE clauses in the same call path.
- **server/tools.ts:7030** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT into project_files omits tenant_id and instead only checks ownership via assertProjectInTenant, but the row will be created without an explicit tenant column value.
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated through tenant-scoped parent projects via project_id ownership check.
- **server/tools.ts:8990** _(upstream-verified)_ — UPDATE on proposed_skills omits tenant constraint. db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) updates by id only and does not restrict by tenant_id, allowing cross-tenant modification if ids collide or are known.
  - _Why false positive:_ Shown line is only a tenant-context guard; the flagged missing update is not at this line and tenantId is already required here.
- **server/tools.ts:9015** _(upstream-verified)_ — UPDATE on proposed_skills omits tenant constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates without verifying the tenant, violating tenant-isolation requirement.
  - _Why false positive:_ This line is inside tenant-scoped insert values for proposed_skills, not the update; tenantId is explicitly set from validated tid.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against tenant-scoped table browser_workflows without constraining by tenant_id (no WHERE tenant_id = <caller tenant>).
  - _Why false positive:_ workflow row was selected immediately above with id/name plus tenant_id, and update uses that verified row's id in same call path.
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE executed against projects table (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}) without constraining by tenant_id — can update another tenant's project by id.
  - _Why false positive:_ projId comes from prior SELECT joining conversations to projects with p.tenant_id = tenantId, so ownership is verified before update.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE on tenant-scoped table `knowledge_triples` lacks a tenant_id constraint — the UPDATE uses `WHERE id = ${row.id}` without `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ Rows being updated were selected just above from knowledge_triples with tenant_id = tenantId in the same flow.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — Query against provider_keys without tenant constraint — SELECT provider, LENGTH(api_key) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''. This reads rows from a likely tenant-scoped table without filtering by tenant_id, enabling cross-tenant enumeration of API keys/metadata (even though the branch is admin-only).
  - _Why false positive:_ Schema says provider_keys has no tenant_id and code restricts this audit to admin tenant 1, indicating platform-global secret registry usage.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — Query against provider_keys without tenant constraint — SELECT COUNT(DISTINCT provider) ... FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''. This aggregates across provider_keys with no tenant_id filter, potentially exposing cross-tenant provider configuration (executed under an admin path).
  - _Why false positive:_ provider_keys schema shows no tenant_id; this admin-tenant-only aggregate is over a non-tenant-scoped table by design
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses only `WHERE id = ${id}` with no tenant_id (or persona_id) constraint — allows updating rows belonging to other tenants by id.
  - _Why false positive:_ id comes from an immediately prior SELECT on memory_entries constrained by tenant_id and persona_id in the same flow
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — Bulk UPDATE on `memory_entries` (WHERE id = ANY(${ids})) omits tenant_id constraint — will bump last_accessed/access_count for rows across tenants if ids include other-tenant rows.
  - _Why false positive:_ ids are derived from rows selected just above with tenant_id and persona_id filters, so the bulk update only targets already-scoped rows
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against `memory_entries` uses `WHERE id = ${id}` without tenant_id/persona_id constraint (in recordStrategicWin flow) — permits cross-tenant row modification by id.
  - _Why false positive:_ id is obtained from the preceding tenant_id/persona_id-scoped SELECT on memory_entries in the same call path
