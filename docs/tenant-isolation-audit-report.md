# Tenant-Isolation Audit Report

_Generated 2026-07-30T11:25:31.013Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **576/576** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **272** (CRITICAL 66 · HIGH 169 · MEDIUM 36 · LOW 1)
- Precision second-pass (`gpt-5.4`): **186** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (66)

- **server/agentic-engines.ts:243** — Bulk UPDATE across tenants: UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ... — this updates rows for all tenants without tenant_id constraints.
  - _Fix:_ This write must be tenant-scoped. Either run the update per-tenant (provide tenantId) or add a tenant filter to the WHERE clause. Avoid any bulk write that touches multiple tenants' rows without explicit tenant scoping and operator approval.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ... WHERE id = ... (project backfill) executes against tenant-scoped `conversations` rows with NO tenant_id constraint. This can modify another tenant's conversation if id is poisoned.
  - _Fix:_ Always include the tenant in the WHERE clause on updates (e.g. AND tenant_id = ${c.tenant_id} or AND tenant_id = ${tenantId}) or perform the update inside a transaction after verifying ownership (like assertConversationInTenant) to enforce per-tenant isolation.
- **server/commitment-drafter.ts:235** — UNSCOPED UPDATE against tenant-scoped table `commitments` — the handler unclaims a row using only WHERE id = ${row.id} (no tenant_id filter). This allows accidental cross-tenant mutation if IDs collide or are forged.
  - _Fix:_ Constrain the UPDATE by tenant. Use the tenant id returned with the claimed row (row.tenantId) or otherwise-validated tenantId: e.g. await db.execute(sql`UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}`); also check the returned row count to ensure only the intended row was modified.
- **server/commitments.ts:135** — Cross-tenant SELECT against tenant-scoped table `commitments` — scanAndEscalate performs a SELECT across all commitments with no WHERE tenant_id = ... clause, returning rows for every tenant.
  - _Fix:_ Either (a) scope the query to a single tenant (add WHERE tenant_id = ${tenantId}) and run per-tenant, or (b) if this is intended as an admin/global job, explicitly gate the function with admin-only authorization and document that it is allowed to read all tenants. Prefer the safer per-tenant iteration pattern so all reads/writes are explicitly tenant-scoped by application-level filters.
- **server/data-protection.ts:131** — DELETE FROM messages WHERE conversation_id = ${conv.id} lacks tenant_id predicate — a DELETE against a tenant-scoped table without tenant scoping can remove another tenant's rows.
  - _Fix:_ Include the conversation's tenant in the DELETE predicate. e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Alternatively run the purge inside a tenant-scoped transaction (withTenantTx) or otherwise ensure tenant_id is enforced for all destructive statements.
- **server/data-protection.ts:132** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks tenant_id predicate — destructive cross-tenant delete possible.
  - _Fix:_ Add tenant predicate: DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Prefer using the conv.tenant_id you selected earlier or perform the whole purge inside a tenant-scoped transaction to guarantee isolation.
- **server/data-protection.ts:133** — DELETE FROM conversations WHERE id = ${conv.id} is not constrained by tenant_id — can delete another tenant's conversation row.
  - _Fix:_ Constrain deletes by tenant: DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Ensure every DELETE/UPDATE/SELECT operating on tenant-scoped tables includes tenant_id or is executed under an explicit tenant-scoped transaction (withTenantTx).
- **server/delivery-pipeline.ts:1049** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with no tenant filter (WHERE stripe_payment_id = ...). This can leak another tenant's delivery row/links if a payment id from another tenant is supplied.
  - _Fix:_ Either require a tenantId parameter for the lookup and include eq(deliveryLogs.tenantId, tenantId) in the WHERE, or clearly document and rework this helper so callers perform a tenant-scoped lookup (and never return tenant-scoped PII/links from an unscoped query).
- **server/embeddings.ts:684** — UPDATE on tenant-scoped tables (memory_entries | agent_knowledge) performed without any tenant_id constraint — storeEmbeddingVec updates rows by id only.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec; include a tenant filter in the UPDATE (e.g. WHERE tenant_id = ${tenantId} AND id = ${id}). Also verify ownership (SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId}) before updating to avoid cross-tenant writes. Avoid relying on process-wide allowlists alone.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Scope the backfill to a specific tenant (accept a tenantId parameter) or iterate per-tenant and include AND tenant_id = ${tenantId} in the WHERE clause. Do not run a global backfill that reads tenant-scoped data without explicit tenant scoping/authorization.
- **server/embeddings.ts:716** — SELECT FROM memory_entries in backfillEmbeddingVecs is missing tenant scoping (no tenant_id filter) — reads tenant-scoped rows globally.
  - _Fix:_ Run the backfill per-tenant or accept a tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause. Ensure any background maintenance job explicitly bounds its work to a tenant before reading tenant-scoped tables.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} has no tenant_id constraint — can modify rows from any tenant by id.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE tenant_id = ${tenantId} AND id = ${row.id}) and/or verify ownership before updating. Change backfillEmbeddingVecs to operate per-tenant so you can pass tenantId into the UPDATE.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge in backfillEmbeddingVecs lacks tenant scoping — reads rows across tenants.
  - _Fix:_ As with memory_entries, perform per-tenant backfills or accept a tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause before reading tenant-scoped tables.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} has no tenant_id constraint — updates rows without verifying tenant ownership.
  - _Fix:_ Require tenantId and include WHERE tenant_id = ${tenantId} AND id = ${row.id} in the UPDATE. Prefer doing a SELECT to confirm the row belongs to the tenant (or perform the UPDATE with the tenant filter and check affected-rows) to prevent cross-tenant writes.
- **server/heartbeat.ts:52** — SELECT * FROM research_schedules is executed with no tenant_id constraint. research_schedules rows contain tenant_id (used later) so this read can return rows for all tenants, violating tenant isolation.
  - _Fix:_ Restrict the query to an explicit tenant context (e.g. WHERE tenant_id = ${tenantId}) or use an admin/asserted tenant flow. If the intention is to run cross-tenant admin work, require an explicit ADMIN_TENANT_ID assertion and document it; otherwise use the storage wrapper that enforces tenant scoping (or assertTenantContext) before selecting.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} has no tenant_id constraint. Updating by id only may modify another tenant's row if ids collide or attacker influences id values.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or assert admin tenant intent before running cross-tenant updates. Prefer using a storage helper that requires tenantId and enforces it.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} lacks tenant_id filtering — write against tenant-scoped table without tenant constraint.
  - _Fix:_ Add tenant filtering to the UPDATE (AND tenant_id = ${sched.tenant_id}) or call a storage-layer helper that enforces/validates the tenant before mutating the row.
- **server/heartbeat.ts:112** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} is executed without tenant_id in the WHERE clause, allowing cross-tenant writes.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via a tenant-aware storage API after asserting tenant context.
- **server/heartbeat.ts:119** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} (error recovery path) has no tenant_id constraint — can advance/reserve slots across tenants.
  - _Fix:_ Include AND tenant_id = ${sched.tenant_id} in the WHERE clause or delegate the update to a storage function that requires/validates the tenant. Be explicit about admin vs tenant-scoped operations.
- **server/heartbeat.ts:265** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} is executed without a tenant_id constraint. heartbeat_tasks are tenant-scoped; this write can affect a task in another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call a tenant-aware storage/update helper after asserting the task's tenant context.
- **server/heartbeat.ts:269** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} lacks tenant filtering. This mutates a tenant-scoped table by id only, risking cross-tenant modification.
  - _Fix:_ Constrain the update with tenant_id (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage abstraction that enforces tenant ownership/validation.
- **server/heartbeat.ts:686** — SELECT * FROM event_log WHERE status = 'pending' ... is executed with no tenant_id filter. event_log rows include tenant_id and subsequent processing expects a tenant per-row — reading without scoping can expose other tenants' events to this tick processing.
  - _Fix:_ Either restrict the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate active tenant IDs and select per-tenant (or use a tenant-aware storage API). Always enforce/assert tenant context before reading tenant-scoped event rows.
- **server/heartbeat.ts:2950** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause. Modifies a tenant-scoped table by id only (no tenant validation).
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or perform the update through a tenant-aware storage function that validates the caller's tenant ownership of the task.
- **server/heartbeat.ts:3402** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) is executed without tenant_id constraint. This writes to a tenant-scoped table without validating tenant ownership.
  - _Fix:_ Use a tenant-scoped WHERE condition (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call a storage-layer updater that requires/validates tenantId to enforce isolation.
- **server/lib/action-ledger.ts:304** — UPDATE (sweepStaleAttempts) performs a cross-tenant write on tenant-scoped table `action_attempts` with no tenant_id constraint (UPDATE ... WHERE id IN (SELECT id FROM action_attempts ...)).
  - _Fix:_ Do not perform bulk cross-tenant UPDATEs. Either scope the inner SELECT to a single tenant_id or enumerate tenants and run the sweep per-tenant (or use the existing tenant-scoped transition() helper per-row). Ensure every UPDATE includes an explicit tenant_id predicate (e.g. WHERE tenant_id = ${tenantId} AND id = ${id}) so writes cannot affect other tenants.
- **server/lib/scheduled-post-runner.ts:306** — Manual construction of a Postgres array literal (arrLit) from caller-supplied platforms and interpolation into SQL. This is string-interpolated SQL using request-derived data and is an injection/isolation risk.
  - _Fix:_ Stop building SQL literals by hand. Use parameterized queries or Drizzle's array/JSON support (pass the JS array as a parameter and let the driver handle casting) or use sql.array/sql`...` properly. Validate/normalize platform elements server-side and bind them as a parameter (e.g. VALUES (${params.tenantId}, ${lowered}::text[], ...) or use db.insert with typed values). Avoid any manual quote-escaping.
- **server/memory-graph.ts:210** — INSERT INTO memory_links (...) VALUES (...) does not set tenant_id — inserting into a tenant-scoped table without tenant_id (or validating ownership) can create cross-tenant links and/or violate NOT NULL invariant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId})). Before inserting, verify both source_memory_id and target_memory_id belong to the caller's tenant. Prefer using a tenant-scoped tx or explicit ownership checks.
- **server/oauth-subscriptions.ts:906** — SELECT from oauth_subscriptions performed without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads all tenants' OAuth subscriptions (sensitive tokens) in one unscoped query.
  - _Fix:_ Avoid scanning tenant-scoped tables unbounded. Either (a) restrict the query to a single tenant_id parameter, or (b) if a multi-tenant background job must iterate all tenants, first query an explicit tenant-list and then run per-tenant parameterized queries (e.g. WHERE tenant_id = ${tid}). Do NOT run unscoped SELECTs that return other tenants' rows unless running in an auditable admin context with additional safeguards.
- **server/oauth-subscriptions.ts:1258** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — full-table read across all tenants in proactiveTokenRefresh (background auto-refresh). This loads sensitive subscription rows for all tenants without per-tenant scoping.
  - _Fix:_ Do not perform unscoped reads on tenant-scoped tables. Either iterate tenant-by-tenant (query tenant registry then refresh per-tenant using WHERE tenant_id = ${tid}) or restrict this background job to an explicit admin context with strong audit/ACL and explicit justification. Parameterize and scope all queries by tenant_id.
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE in forceRefreshAllSubscriptions — unscoped read across all tenants. This enumerates active subscriptions across tenants and drives per-tenant refresh actions without explicit tenant scoping in the initial query.
  - _Fix:_ Same as above: avoid unscoped multi-tenant selects. If you must process all tenants, first obtain an explicit list of tenant IDs from a trusted tenant registry and then perform per-tenant, parameterized queries (WHERE tenant_id = ${tid}) to operate on that tenant's rows.
- **server/plan-executor.ts:117** — UPDATE on tenant-scoped 'plans' table without tenant_id constraint (appendExecutionLog uses WHERE id = ${planId} only). This allows modifying another tenant's plan/log if planId is attacker-controlled or leaked.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or require the caller to provide/derive the authoritative tenantId and validate it against the claimed plan before performing the UPDATE. Prefer passing tenantId into appendExecutionLog from the caller (derived from a prior verified SELECT/claim) and use parameterized queries.
- **server/plan-executor.ts:513** — CAS claim: UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' — no tenant_id constraint. This lets a caller claim and execute a plan belonging to any tenant by ID alone.
  - _Fix:_ Add tenant scoping to the CAS update (e.g. AND tenant_id = ${callerTenantId}) or enforce that the caller is an admin-system actor. Ensure executePlan receives and verifies the caller's tenant context and only claims plans where tenant_id matches that context.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' WHERE id = ${planId} — no tenant_id constraint when marking plan failed due to no steps. This can flip another tenant's plan state.
  - _Fix:_ Constrain updates to the owning tenant: include AND tenant_id = ${tenantId} (the plan's tenant). Derive tenantId from a prior trusted/returned row (the CAS claim) rather than accepting it implicitly.
- **server/plan-executor.ts:725** — Final status UPDATE on plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) has no tenant_id constraint. This can finalize plans for other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Use the tenant_id from the claimed/returned plan row and never from untrusted input.
- **server/plan-executor.ts:759** — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') lacks tenant scoping — can change another tenant's plan on error handling.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) and ensure any error recovery updates validate the plan belongs to the expected tenant before mutating state.
- **server/plan-executor.ts:775** — resumeStuckPlans: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — no tenant_id filter. This globally flips executing plans across all tenants back to 'approved'.
  - _Fix:_ Restrict recovery to a single tenant context or require an explicit admin/system-mode guard. If intended to be cross-tenant, require this function to run as an admin-only operation and log/authorize it; otherwise add WHERE tenant_id = ${tenantId} or iterate tenants explicitly after verifying operator privileges.
- **server/plan-executor.ts:784** — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 — no tenant_id constraint when locating pending plans to schedule. This can pick up plans from other tenants.
  - _Fix:_ Limit the SELECT to a tenant scope (WHERE tenant_id = ${tenantId} AND status = 'approved') or require admin-mode execution. Ensure executePlan is only enqueued for plans that belong to the executor's tenant context or run under an authorized system/admin tenant identity.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks sets enabled = false WHERE id = ${t.id} but does not include tenant_id in the WHERE clause. Even if id was derived from a previous tenant-scoped select, the UPDATE can affect a row belonging to another tenant if ids are tampered or reused.
  - _Fix:_ Always include tenant scoping in mutating statements, e.g. `WHERE id = ${t.id} AND tenant_id = ${tenantId}` to ensure updates cannot cross tenants.
- **server/process-governor.ts:641** — UPDATE agent_desks ... WHERE id = ${desk.id} lacks tenant_id filter. This mutates agent_desks by id only and risks modifying another tenant's row.
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${desk.id} AND tenant_id = ${tenantId}` (or otherwise verify ownership before updating).
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant scoping. This increments trigger counts for a rule id without verifying it belongs to the caller's tenant.
  - _Fix:_ Include tenant constraint in the UPDATE, e.g. `WHERE id = ${rule.id} AND tenant_id = ${tenantId}`, so the rule update cannot affect other tenants' rules.
- **server/project-brain.ts:135** — updateProjectBrain reads project row via `SELECT name, status, description FROM projects WHERE id = ${projectId}` but does not take/verify a tenantId or ensure the project belongs to the caller's tenant. The function accepts only projectId and may be invoked with IDs from arbitrary callers, enabling cross-tenant data access.
  - _Fix:_ Require and use the caller's tenantId (pass it into updateProjectBrain), and verify ownership (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) before reading project metadata or proceeding. If the caller is not authorized, abort.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` uses only WHERE id = ${r.id} — no tenant_id constraint. This is a tenant-isolation write missing the required tenant scoping.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}), or otherwise verify the row's tenant before updating. Never perform tenant-scoped writes keyed only by id without also asserting tenant_id = callerTenant.
- **server/reference-learner.ts:290** — UPDATE memory_entries SET ... WHERE id = ${storedId} — missing tenant_id constraint on a tenant-scoped table. This updates a memory entry by id without asserting tenant ownership.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) or re-load and validate the row's tenant before updating to ensure no cross-tenant writes occur.
- **server/research-engine.ts:160** — Mass UPDATE on `research_sessions` (UPDATE research_sessions SET ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}) lacks any tenant_id constraint — this can modify sessions across tenants.
  - _Fix:_ Scope the cleanup to a tenant (e.g. add AND tenant_id = ${tenantId} or iterate per-tenant) or require an explicit admin/system authorization path that is documented and audited. Prefer per-tenant cleanup queries or an explicit, well-audited global-mode that enforces additional safeguards before touching tenant-scoped rows.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` is executed without constraining to the caller's tenant (missing tenant_id predicate). This allows one tenant to mark another tenant's message as read.
  - _Fix:_ Constrain the UPDATE to the requester's tenant. For example, include AND tenant_id = ${tenantId} in the WHERE clause (or use Drizzle `.where(eq(inboxMessages.tenantId, tenantId))`). E.g.: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`); Also consider using a storage helper that enforces tenant-scoping to avoid future regressions.
- **server/routes/projects.ts:185** — INSERT into project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author) ...). Tenant_id is required by schema and must be set from the authenticated tenant.
  - _Fix:_ Include tenant_id in the INSERT column list and values (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})), or derive and validate tenant ownership server-side before inserting.
- **server/routes/projects.ts:224** — INSERT into project_files (files-base64 path) omits tenant_id (INSERT INTO project_files (...)).
  - _Fix:_ When inserting project_files, include tenant_id in columns/values (e.g. tenant_id = ${tenantId}) or use the storage layer to set tenant_id based on the verified project owner. Never rely solely on prior checks — write the tenant_id explicitly.
- **server/routes/projects.ts:292** — INSERT into project_files (multipart upload path) omits tenant_id (INSERT INTO project_files (...)).
  - _Fix:_ Add tenant_id to the INSERT values (e.g. VALUES (..., ${tenantId})) or use Drizzle insert with tenantId set from getTenantFromRequest(req) after verifying project ownership.
- **server/seed.ts:3097** — Unconstrained UPDATE on tenant-scoped table 'research_programs' (UPDATE research_programs SET model = ... WHERE id = ${p.id}) with no tenant_id constraint. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership validation in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or perform a prior ownership check (SELECT tenant_id FROM research_programs WHERE id = ${p.id}) and verify it matches the expected tenant before performing the UPDATE. Prefer Drizzle-style filtering: .where(and(eq(research_programs.id, p.id), eq(research_programs.tenantId, tenantId))).
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` uses only experiments.id in WHERE clause (no tenant_id constraint). This allows updating an experiment row without verifying it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) or in raw SQL add AND tenant_id = ${tenantId}. Always include tenant_id on UPDATE/DELETE for tenant-scoped tables.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table `experiments` uses only experiments.id in WHERE clause (no tenant_id constraint). This can modify another tenant's row in the error handling path.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Ensure the catch path has access to the same tenantId and enforces it before updating.
- **server/self-improvement.ts:521** — Unscoped SELECT: getExperimentHistory falls back to db.select().from(experiments) with no tenant_id filter when caller does not supply category or tenantId, exposing experiments from all tenants.
  - _Fix:_ Disallow or fail on unscoped reads. Require tenantId and include WHERE tenant_id = ${tenantId} in the query, or only return when a tenant filter is present. Example: if (!tenantId) throw new Error('tenantId required'); then .where(eq(experiments.tenantId, tenantId)).
- **server/self-reflection.ts:366** — UPDATE on tenant-scoped table `memory_entries` uses only id in WHERE clause (no tenant_id constraint) after an initial tenant-scoped SELECT. This can update a row belonging to a different tenant if IDs are guessed or conflict.
  - _Fix:_ Include tenant scoping in the UPDATE: add AND tenant_id = ${tenantId} to the WHERE clause (or use eq(memoryEntries.tenantId, tenantId) with Drizzle). Always use tenant_id in both the lookup and the subsequent UPDATE/DELETE.
- **server/storage.ts:838** — archiveExpiredMemories() executes a raw UPDATE on memory_entries across all rows (WHERE status = 'active' AND expires_at <= NOW()) with NO tenant_id constraint. This performs mass mutation across tenants and can corrupt or archive other tenants' data.
  - _Fix:_ Run tenant-scoped archival: either iterate tenants and perform the UPDATE scoped per tenant, or require an admin context and make that explicit. At minimum add tenant_id constraint(s) or provide a separate admin-only codepath with strong access controls and audit logging.
- **server/storage.ts:855** — archiveStaleMemories() executes a raw UPDATE on memory_entries (WHERE status='active' AND created_at <= ${cutoff} ...) with NO tenant_id constraint, causing cross-tenant mutations.
  - _Fix:_ Make this tenant-aware: perform per-tenant updates (or accept a tenantId and enforce it), or restrict this job to a privileged admin flow with explicit checks and audit logging. Never perform blanket updates on tenant-scoped tables without tenant scoping.
- **server/storage.ts:1303** — getAllDataForExport() selects entire tables (conversations, messages, memory_entries, agent_knowledge, etc.) across all tenants and returns them. This effectively enables platform-wide data exfiltration with no tenant scoping or access checks.
  - _Fix:_ Restrict this function to an admin-only API guarded by strong authorization, or accept a tenantId and limit exports to a single tenant. Add explicit checks (and logging/audit) to ensure only authorized principals can call an all-tenant export.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` modifies implementation without constraining by tenant_id (.where(eq(customTools.id, tool.id))). This could overwrite another tenant's tool row if IDs are guessed or collided.
  - _Fix:_ Add an explicit tenant filter to the UPDATE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the function has the authoritative tenantId and verify ownership before performing any UPDATEs on tenant-scoped tables.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table `custom_tools` uses only the PK filter (db.delete(customTools).where(eq(customTools.id, tool.id))) with no tenant_id constraint. This can delete another tenant's tool row if the id is known or collisions occur.
  - _Fix:_ Constrain deletes to the tenant as well, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Require and validate the caller's tenantId for any delete and double-check ownership before removing tenant-scoped rows.
- **server/tools.ts:2981** — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT statement into project_files omits tenant_id, violating tenant-isolation invariant (rows may be created without explicit tenant ownership).
  - _Fix:_ Include tenant_id in the INSERT (e.g. VALUES (..., ${tenantId})) or otherwise derive/validate tenant ownership server-side before insert. Prefer: INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) and validate project_id belongs to that tenant first.
- **server/tools.ts:2995** — INSERT into tenant-scoped table project_notes does not set tenant_id. The INSERT INTO project_notes omits tenant_id, creating notes without explicit tenant scoping.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Validate that the referenced project_id belongs to the caller's tenant before inserting.
- **server/tools.ts:2878** — INSERT into tenant-scoped junction table project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without recording tenant_id, risking cross-tenant linkage or missing ownership metadata.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). Ensure the conversation and project both belong to the tenant before inserting.
- **server/tools.ts:3020** — INSERT into tenant-scoped junction table project_conversations omits tenant_id (second occurrence). Same problem: tenant ownership not recorded on insert.
  - _Fix:_ Same fix: include tenant_id in the INSERT and verify ownership of both project_id and conversation_id prior to inserting.
- **server/tools.ts:6914** — INSERT into project_files does not include tenant_id. The INSERT uses project_id only (even though assertProjectInTenant was called earlier), so the project_files row may be missing an explicit tenant_id column/value required by the tenant-scoped-table invariant.
  - _Fix:_ Include tenant_id in the INSERT (and bind it from the authenticated tenant context or the verified project row). Example: INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...) or use Drizzle insert with .values({ tenantId: params._tenantId, projectId: params.project_id, ... }). If tenant_id must be derived, SELECT the project's tenant_id after verifying ownership and use that value in the INSERT. Always explicitly set tenant_id on tenant-scoped tables.
- **server/tools/domains/files/handlers.ts:836** — INSERT into tenant-scoped table 'project_files' does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...))
  - _Fix:_ Pass the tenant id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ...)) or use Drizzle's insert API with the tenantId column set from ctx.tenantId. Every INSERT into tenant-scoped tables must include tenant_id (NOT NULL) to prevent cross-tenant attribution/leaks.
- **server/twilio.ts:102** — Query reads agent_knowledge rows across all tenants without constraining tenant_id (SELECT id, content::jsonb FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ...). This violates the project invariant that every read against tenant-scoped tables must be constrained to the caller's tenant and allows cross-tenant enumeration/inspection of pairing rows.
  - _Fix:_ Avoid scanning tenant-scoped tables without a tenant filter. If the code truly needs to detect contested pairings across tenants, move this to an explicit, audited admin-only operation or a dedicated global index/table designed for cross-tenant lookups. Alternatively, include tenant_id in the WHERE clause (or return only rows for the caller tenant) and/or redesign pairings to store a separate global ownership index so application-level checks do not rely on unconstrained SELECTs.
- **server/twilio.ts:116** — Query SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without constraining by tenant_id. This reads a tenant-scoped table by id only and therefore can leak or be used to infer cross-tenant data and breaks the invariant that every read must include the caller's tenant filter.
  - _Fix:_ Constrain this lookup to the claimed tenant (e.g. WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1). If the intent is to verify that the pairing's claimed tenant matches the conversation, perform the lookup with both id and tenant_id so the DB enforces the ownership check rather than returning data from other tenants.

### HIGH (169)

- **server/ab-optimizer.ts:110** — Scanning experiments across tenants: SELECT id, tenant_id, hypothesis, metric, persona_id, metadata, created_at FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100 — no tenant_id constraint.
  - _Fix:_ Restrict the query to a single tenant (WHERE tenant_id = <tenant>) or iterate per-tenant. If this job must operate per-tenant, run the SELECT per tenantId or include an explicit tenant filter; never read tenant-scoped rows across all tenants in a single unscoped query.
- **server/agent-activity.ts:122** — UPDATE agentActivity without tenant scope: await db.update(agentActivity).set(updates).where(eq(agentActivity.id, activityId)) — missing tenant_id constraint on the WHERE clause.
  - _Fix:_ Include the tenant in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) or first verify ownership before updating to ensure the caller can only modify rows in their tenant.
- **server/agentic-engines.ts:161** — Reading ai_insights across tenants in retryPendingMinervaRouting: SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50 — no tenant_id constraint.
  - _Fix:_ Either accept a tenantId parameter and restrict the SELECT to that tenant, or iterate per-tenant. Never perform unscoped reads of tenant-scoped ai_insights; add WHERE tenant_id = ${tenantId} or otherwise scope the sweep.
- **server/agentic-features.ts:525** — UPDATE lead_enrichments performed with WHERE id = ${lead.id} but no tenant filter: await db.execute(sql`UPDATE lead_enrichments SET ... WHERE id = ${lead.id}`). This can modify rows in other tenants if ids are guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or verify the row's tenant before updating.
- **server/agentic-features.ts:638** — UPDATE outreach_enrollments SET status = 'completed' WHERE id = ${enrollment.id} — missing tenant scoping on the update that finalizes an enrollment.
  - _Fix:_ Include tenant in WHERE (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to ensure only the tenant's rows are modified.
- **server/agentic-features.ts:672** — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} — update lacks tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to prevent cross-tenant modification.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments SET reply_classification = ..., status = ${newStatus} WHERE id = ${row.id} — missing tenant constraint when writing classification/status back to an enrollment.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or verify ownership before applying updates.
- **server/agentic/approvals.ts:135** — expireStaleApprovals updates agentApprovals in bulk without tenant scoping: the UPDATE sets status='expired' WHERE status='pending' AND expiresAt < now() — no tenant_id filter, touching approvals across tenants.
  - _Fix:_ Perform expiry per-tenant (accept tenantId or iterate tenants) or include tenant_id = ${tenantId} in the WHERE clause so rows are only expired in the intended tenant. Avoid committing cross-tenant state changes in a single unscoped statement.
- **server/agentic/department-budgets.ts:143** — Query against tenant-scoped table `department_budgets` without a tenant_id constraint (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads rows for all tenants in a tenant-scoped table, violating the rule that every read/write must be constrained to a caller's tenant unless intentionally using an audited/admin cross-tenant process.
  - _Fix:_ Either restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) or require and document an explicit admin/audited context for cross-tenant scans. If this function must scan all tenants, make that explicit (rename to admin-only), add an authorization check (only run when running as a trusted admin process), and ensure audit/logging and least-privilege controls are in place. Alternatively, accept a tenantId parameter and only query that tenant.
- **server/agentic/resume-worker.ts:66** — SELECT query scans tenant-scoped tables (agent_runs, agent_approvals) without constraining to a caller/worker tenant id. The initial db.execute(...) returns rows across all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE r.tenant_id = ${tenantId} or accept a tenantId parameter and filter by it), or iterate per-tenant in a controlled way. Never perform unscoped reads of tenant-scoped tables in a multi-tenant context.
- **server/agentic/runs.ts:36** — appendStep updates agent_runs by id only: db.update(agentRuns).set(...).where(eq(agentRuns.id, runId)). This update lacks a tenant_id constraint and can modify rows belonging to other tenants if a cross-tenant runId is supplied.
  - _Fix:_ Include tenant scoping in the WHERE clause and require caller to pass tenantId (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or otherwise ensure the update is performed only for the owning tenant.
- **server/agentic/runs.ts:48** — updateRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) without constraining by tenant_id, allowing cross-tenant state mutation.
  - _Fix:_ Add tenant scoping to the WHERE clause and require the tenantId be provided, e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:54** — replaceRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with no tenant_id filter, enabling cross-tenant updates if runId is from another tenant.
  - _Fix:_ Require and check tenantId; add it to the WHERE clause (AND agent_runs.tenant_id = <tenantId>) to enforce tenant isolation on the update.
- **server/agentic/runs.ts:59** — completeRun updates agent_runs (.where(eq(agentRuns.id, runId))) without a tenant_id constraint, permitting cross-tenant modifications to run rows.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND agent_runs.tenant_id = <tenantId>) and change the API to accept/validate the tenantId for the caller.
- **server/agentic/runs.ts:88** — failRun updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with no tenant_id check, which can affect runs across tenants.
  - _Fix:_ Require tenantId and add it to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all callers pass the tenantId.
- **server/agentic/runs.ts:97** — pauseRun updates agent_runs (.where(eq(agentRuns.id, runId))) without constraining by tenant_id, enabling cross-tenant state changes.
  - _Fix:_ Add tenant scoping to the update (AND agent_runs.tenant_id = <tenantId>) and accept/validate tenantId from the caller; apply this pattern to all run-modifying functions to enforce tenant isolation.
- **server/agentic/wake-scheduler.ts:142** — Claiming due wakes updates/SELECTs agent_wake_schedules without constraining to a tenant. The UPDATE/inner SELECT will atomically claim pending wakes across all tenants (no WHERE tenant_id = ...), violating tenant-isolation-by-filter invariant.
  - _Fix:_ Either run the claim per-tenant (iterate tenants and include WHERE tenant_id = <tenant>) or add tenant_id predicates to the inner SELECT and outer UPDATE so each claim only touches rows belonging to a single tenant. Ensure any later UPDATE/RETURNING uses the same tenant-bound value.
- **server/auto-memorize.ts:45** — getWatermark and setWatermark operate on agent_knowledge without tenant scoping. getWatermark SELECTs agent_knowledge with no tenant_id filter; setWatermark INSERTs a watermark row with tenant_id hardcoded to 1. Both read/write across tenant-scoped agent_knowledge without properly scoping to the active tenant.
  - _Fix:_ Store and read the auto_memorize watermark per-tenant: add tenant_id to the WHERE clauses for reads and set tenant_id to the current tenant when inserting/updating. Do NOT hardcode tenant_id=1 (admin) unless deliberately global and access-controlled; if a global watermark is required, document and gate it behind strict platform-admin checks.
- **server/auto-memorize.ts:163** — runAutoMemorize pulls messages across all tenants in a single query (SELECT m.tenant_id, m.role, m.content, ... WHERE m.created_at > ... AND m.role IN ('user','assistant') ORDER BY m.tenant_id ASC ...). This returns raw message content spanning tenants into memory and then partitions in-process — an LLM prompt is built per-tenant but the initial unscoped SELECT reads multi-tenant data without tenant_id constraints.
  - _Fix:_ Avoid scanning messages from all tenants in one unscoped query. Either run the pipeline per-tenant (query messages WHERE tenant_id = <tenant>) or use a safe admin-only job that enumerates tenant ids first and then selects messages per-tenant with tenant_id predicates. Ensure no prompt ever contains or intermixes data from multiple tenants.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (schema convention: tenant_id NOT NULL, no default), this write will either fail or create/allow cross-tenant data (missing explicit tenant attribution).
  - _Fix:_ Fail-closed: make invoice_items tenant-aware. Include tenant_id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)). Alternatively, read/derive the parent invoice's tenant and verify it matches tid before inserting. Add a DB-level NOT NULL tenant_id column (and an app-level check) if the table is intended to be tenant-scoped.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` omits a tenant_id constraint. The query `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` can modify rows belonging to another tenant because it does not include `AND tenant_id = <caller tenant>` (and the pending in-memory entry does not store tenantId).
  - _Fix:_ Record the tenantId when you push pendingReuseByConversation (include tenantId in the PendingReuse object), and require it in the UPDATE. Change the query to `WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false`. Alternatively, pass the caller's tenantId into markReviewReused and always include `AND tenant_id = ${tenantId}` in the WHERE clause to enforce per-tenant scoping.
- **server/chat-engine.ts:4332** — storage.attachCitations(...) is invoked without an explicit tenant context. If attachCitations performs inserts/updates on a tenant-scoped table it does not receive tenant_id here, violating the invariant that every tenant-scoped write must include tenant_id.
  - _Fix:_ Include the caller's tenant id when calling attachCitations (e.g. storage.attachCitations(messageId, citations, { tenantId })) or change attachCitations signature to accept tenantId and ensure it uses WHERE tenant_id = :tenantId in its DB writes. Alternatively, validate the message belongs to the tenant before attaching citations.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, ...) is called with only conversationId (and later with ids) but no tenant id. If recordToolExecution writes to tenant-scoped tables, it lacks an explicit tenant constraint. Also this call uses a foreign id (conversationId) without an explicit ownership check.
  - _Fix:_ Pass tenantId into recordToolExecution (e.g. recordToolExecution(tenantId, conversationId, ...)) or have recordToolExecution validate ownership (e.g. verify conversation belongs to tenantId before any DB write). Ensure all DB writes inside that function include WHERE tenant_id = :tenantId or set tenant_id explicitly on INSERT.
- **server/chat-engine.ts:4016** — recordToolCallForStuckDetection(conversationId, toolName, ...) is invoked without any tenant id. If that routine writes/reads tenant-scoped rows it will not be constrained to the caller's tenant.
  - _Fix:_ Change the call to include tenantId (e.g. recordToolCallForStuckDetection(tenantId, conversationId, ...)) or ensure the callee validates that conversationId is owned by tenantId before performing any DB operations. All internal DB queries must include tenant_id checks.
- **server/credential-vault.ts:169** — UNSCOPED UPDATE against tenant-scoped table `credential_vault` — getDecryptedPassword updates last_used_at using WHERE id = ${id} with no tenant_id filter, despite the table being tenant-scoped.
  - _Fix:_ Add tenant scoping to the UPDATE: await db.execute(sql`UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}`); (Even if ownership was previously checked in application logic, every DB mutation must include tenant_id in the WHERE clause.)
- **server/credential-vault.ts:176** — UNSCOPED UPDATE against tenant-scoped table `credential_vault` — getLoginCredentials updates last_used_at using WHERE id = ${cred.id} without filtering by tenant_id.
  - _Fix:_ Constrain the UPDATE by tenant: await db.execute(sql`UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}`); ensure the tenantId passed in matches the credential's tenant and verify the UPDATE affected exactly one row.
- **server/data-protection.ts:118** — SELECT FROM conversations (expired) queries tenant-scoped table without scoping to a specific tenant (no WHERE tenant_id = ...). This reads soft-deleted conversations across all tenants.
  - _Fix:_ Scope the query to a tenant or run each tenant separately. e.g. remove the open cross-tenant SELECT and instead SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} AND tenant_id = ${tenantId} (or iterate tenants explicitly / use withTenantTx for system jobs). Avoid any unscoped reads of tenant-scoped tables unless explicitly intended and access-controlled.
- **server/data-protection.ts:183** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} is missing tenant_id predicate. Reading tenant-scoped messages without tenant filtering can expose other tenants' data.
  - _Fix:_ Include tenant scoping on messages queries, for example: SELECT ... FROM messages WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} ORDER BY created_at ASC. Even if the conversation was previously validated, always include tenant_id in subsequent queries against tenant-scoped tables.
- **server/data-protection.ts:190** — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} lacks tenant_id predicate — reads tenant-scoped archive rows without tenant scoping.
  - _Fix:_ Add tenant constraint: SELECT ... FROM compaction_archives WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} ORDER BY archived_at ASC. Ensure all reads from tenant-scoped tables include tenant_id.
- **server/data-protection.ts:369** — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} is missing tenant_id — counting/reading messages without tenant scoping can reveal cross-tenant counts and cause logic errors.
  - _Fix:_ Include tenant filter in the COUNT: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}. Prefer passing and using the conv.tenant_id you retrieved earlier to guarantee the operation stays within the conversation's tenant.
- **server/delivery-pipeline.ts:195** — updateDeliveryLog performs an UPDATE on the tenant-scoped delivery_logs table without constraining by tenant_id (WHERE eq(delivery_logs.id, id) only). This allows cross-tenant updates if callers provide an id that belongs to another tenant.
  - _Fix:_ Require and accept the caller's tenantId in updateDeliveryLog (e.g. updateDeliveryLog(id, updates, tenantId)) and include eq(deliveryLogs.tenantId, tenantId) in the WHERE. Alternatively fetch the row first and validate its tenant before updating.
- **server/dfy-intake.ts:93** — submitDfyIntake performs an UPDATE on dfy_intake_forms using WHERE id = form.id without a tenant_id constraint. If form.id can be influenced or a token lookup returns a cross-tenant id, this allows updating another tenant's form.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${form.id} AND tenant_id = ${form.tenantId} or accept the tenantId as an explicit parameter) so only the owning tenant's row can be modified.
- **server/felix-brain.ts:19** — Boot rehydration queries agent_runs without scoping by tenant. The SELECT (lines 19-24) reads rows across all tenants (no WHERE tenant_id = ...), exposing cross-tenant state at startup.
  - _Fix:_ Scope the rehydration to a tenant context (e.g. iterate tenants or run under the server's tenant-aware context) and add an explicit tenant filter to the query (WHERE tenant_id = ${tenantId} or equivalent Drizzle .where(eq(agent_runs.tenantId, tenantId))). If global/administrative access is required, document and gate it explicitly; otherwise fail-closed and only rehydrate rows for the intended tenant(s).
- **server/google-drive.ts:547** — Query against projects uses WHERE eq(projects.id, projectId) without restricting by tenant. The SELECT reads a tenant-scoped row by id but does not include a tenant_id filter (no .where(eq(projects.tenantId, tenantId))). This allows reading another tenant's project record before ownership is enforced in application logic.
  - _Fix:_ Constrain the SELECT to the caller's tenant at the DB level (e.g. .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId)) or add AND p.tenant_id = ${tenantId} in the SQL). Never read tenant-scoped rows by id alone — always push the tenant check into the WHERE clause so untrusted ids cannot leak cross-tenant data.
- **server/google-drive.ts:560** — UPDATE on projects sets driveFolderId/driveFolderUrl WHERE eq(projects.id, projectId) without including a tenant filter. The UPDATE could modify a project belonging to a different tenant if called with a foreign projectId.
  - _Fix:_ Include tenant scoping in the UPDATE's WHERE clause (e.g. .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Even if ownership was checked earlier in code, always include the tenant constraint on the UPDATE to avoid TOCTOU and privilege-escalation risks.
- **server/google-drive.ts:826** — uploadAndShare resolves a project by id with a SELECT that lacks a tenant filter (.where(eq(projects.id, params.projectId))). This reads a tenant-scoped row before application-level ownership checks, enabling cross-tenant reads.
  - _Fix:_ Add tenant scoping to the query that resolves the project (e.g. .where(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)) or otherwise ensure the tenant is part of the WHERE). Prefer verifying ownership in the SQL rather than selecting by id then checking in application code.
- **server/google-drive.ts:1005** — searchDriveFiles performs a SELECT on projects to validate project ownership using .where(eq(projects.id, params.projectId)) but does not include tenant scoping. The code checks proj.tenantId after the read rather than constraining the query itself.
  - _Fix:_ Constrain the ownership check in the SQL (e.g. .where(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))). That way the DB only returns rows owned by the caller's tenant and avoids exposing other tenants' metadata.
- **server/google-drive.ts:1066** — Drive-search pass reads projects.driveFolderId with a SELECT .where(eq(projects.id, params.projectId)) and no tenant filter, then uses the folder id to scope a Drive API query. The initial DB read can disclose another tenant's driveFolderId.
  - _Fix:_ Include tenant_id scoping in the SELECT (e.g. .where(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))). Prevent using project-derived values from rows that haven't been DB-scoped to the caller's tenant.
- **server/google-workspace.ts:2358** — INSERT into tenant-scoped table `presenter_slide_images` does not include tenant_id (no tenant constraint).
  - _Fix:_ Include the tenant_id column when inserting (e.g. VALUES (${tenantId}, ...) or use a Drizzle insert that sets tenantId). Alternatively validate the session_id belongs to the expected tenant before inserting and fail if it does not. Never rely on implicit ownership from a prior service call — always persist/verify tenant_id in the same DB statement or add a WHERE tenant_id = ${tenantId} guard when updating/selecting the session.
- **server/google-workspace.ts:2364** — INSERT into tenant-scoped table `presenter_slide_images` (thumb row) does not include tenant_id (no tenant constraint).
  - _Fix:_ Same fix as above: supply tenant_id in the INSERT or verify session ownership first. Use Drizzle/parameterized SQL to set tenantId explicitly, or join back to presenter_sessions to ensure the session belongs to this tenant before writing thumbnails.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id filter — mutates a tenant-scoped row by id only.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). If this code is running as an admin operation that intentionally touches all tenants, make that explicit and check an admin authorization flag; otherwise always require the tenant_id constraint to prevent accidental cross-tenant writes.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(...) updates proposals without constraining by tenant_id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId} or UPDATE ... WHERE id = ANY(...) AND tenant_id = ${tenantId}). If the operation must affect multiple tenants, require a vetted admin-mode path and explicit authorization, but do not perform cross-tenant updates silently.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs writes with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ANY(...) AND tenant_id = ${tenantId}) or only modify tasks after verifying ownership. If run as an admin sweep, require explicit admin authorization and make the tenant impact explicit rather than updating rows by id alone.
- **server/internal-resolver.ts:91** — UPDATE event_log is executed without constraining by tenant_id — finalizing events by id alone can mutate rows belonging to other tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. AND tenant_id = ${tenantId}) and pass/validate the expected tenant into finalizeEvent. Alternatively read the event first and assert ownership before performing terminal updates.
- **server/internal-resolver.ts:107** — SELECT from plans (SELECT id, objective, status FROM plans WHERE id = ${planId}) is performed without verifying the plan's tenant ownership.
  - _Fix:_ Query plans with an explicit tenant filter (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or fetch event_log first to determine the tenant and validate that the caller is authorized to act on that tenant's plan before reading/updating the row.
- **server/internal-resolver.ts:170** — UPDATE on code_proposals (UPDATE code_proposals SET ... WHERE id = ${proposalId} ...) does not include tenant_id — this may modify a proposal from another tenant.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or otherwise ensure tenant ownership is validated before performing the UPDATE. Pass tenant context into the resolver call paths so ownership can be enforced.
- **server/knowledge-diversity-monitor.ts:266** — Query aggregates agent_knowledge across all tenants (SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... HAVING COUNT(*) >= ${MIN_ENTRIES}) with no tenant_id constraint — per-tenant data is being read without tenant scoping.
  - _Fix:_ Run the monitor per-tenant (iterate tenant ids and call tenant-scoped queries with WHERE tenant_id = ${tenantId}) or clearly mark and restrict this job to an operator-only maintenance path with explicit audit/authorization. Ensure any multi-tenant scans are deliberate and protected, and that application-level tenant filters are used when reading tenant-scoped data.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE against tenant-scoped table `procedure_evolution_runs` uses only the run id in the WHERE clause (.where(eq(procedureEvolutionRuns.id, runId))) with no tenant_id constraint.
  - _Fix:_ Include the tenantId in the WHERE clause for all updates (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) so a runId cannot be used to modify another tenant's row. Apply the same tenant guard to all subsequent updates that reference runId.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE against tenant-scoped table `procedure_evolution_runs` (noop case) uses only run id in WHERE clause with no tenant predicate.
  - _Fix:_ Add tenant scoping to the UPDATE (include eq(procedureEvolutionRuns.tenantId, tenantId)) or otherwise validate the run belongs to the calling tenant before applying the update. Treat runId as untrusted input unless proven owned by the caller.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE against tenant-scoped table `procedure_evolution_runs` (mark done) uses only run id in WHERE clause with no tenant predicate.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) to ensure only the owner tenant can flip the run state. Audit all places that re-query/update rows by id to ensure tenant ownership is enforced.
- **server/lib/agent-trace.ts:124** — persistSpanClose issues an UPDATE on tenant-scoped table `agent_trace_spans` using only span_id in the WHERE clause (no tenant_id predicate). This can allow closing/updating spans belonging to other tenants if span_id is guessed or leaked.
  - _Fix:_ Include tenant scoping in the UPDATE (add AND tenant_id = ${tenantId}) — capture and pass the tenant id into persistSpanClose (or store it at open time and ensure it is bound to the span) so every UPDATE is guarded by the owning tenant_id. If span_id is globally unique but not secret, do not rely on it alone for tenant isolation.
- **server/lib/bwb-job-progress.ts:141** — Multiple UPDATE/SELECT statements (setBwbPhase, updateBwbChapters, completeBwbJob, bumpBwbHeartbeat, failBwbJob) use scope(jobId) which may omit tenant_id when BWB_TENANT_ID is missing — resulting in tenant-unscoped writes to tenant-scoped `video_jobs` rows.
  - _Fix:_ Require and assert a valid tenantId for any operation that writes/updates tenant-scoped rows. Always include eq(videoJobs.tenantId, tenantId) in WHERE clauses. If the design relies on jobId secrecy, replace that with explicit tenant scoping (fail fast if BWB_TENANT_ID is not set) or iterate per-tenant so the DB writes are always filtered by tenant_id.
- **server/lib/ideabrowser-backfill.ts:51** — SELECT from tenant-scoped table `projects` without constraining to a caller tenant (no WHERE tenant_id = <tenant>). This query aggregates/counts across ALL tenants (`SELECT tenant_id, count(*) ... FROM projects WHERE tags && ... GROUP BY tenant_id`).
  - _Fix:_ Don't query tenant-scoped tables across tenants from a per-tenant backfill routine. Either (a) restrict the query to the specific tenant you are operating on (add `WHERE tenant_id = ${tenantId}`), or (b) explicitly iterate tenants you intend to backfill and run the per-tenant query, or (c) move this global aggregation to a properly authorized admin path that is explicit about cross-tenant scope. Ensure every read of `projects` in tenant-sensitive flows includes a tenant_id filter.
- **server/lib/jury-experience.ts:175** — String-interpolated SQL fragment is built from a request-derived value (`embedding`) and injected into a drizzle `sql` template: `sql`${`[${embedding.join(",")}]`}::vector``. This constructs SQL text from runtime data and risks injection / broken casting and undermines tenant isolation/auditing semantics.
  - _Fix:_ Avoid constructing raw SQL fragments from model-derived or request-derived data. Instead pass the embedding as a parameterized value (let the DB client bind the array) or use driver-supported parameterization for vector types. Example: use a parameter placeholder and pass the embedding array as a bound parameter (or use a safe helper that converts an array to a parameterized Postgres array/vector), e.g. `question_embedding = ${embedding}` or use a prepared statement / driver API that supports vector binding. Do not stringify/inline the embedding into the SQL text.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (the CTE selects due rows across all tenants).
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) for tenant-scoped API calls, or if this runner must operate system-wide, ensure every subsequent UPDATE/DELETE uses both id AND tenant_id in the WHERE clause and that the code explicitly documents and audits the system-scope privilege. Prefer per-tenant worker instances to avoid accidental cross-tenant effects.
- **server/lib/scheduled-post-runner.ts:121** — UPDATE on `scheduled_posts` (quality-gate failure path) uses WHERE id = ${row.id} but omits tenant_id. Writes to a tenant-scoped table are not constrained to the row's tenant in the WHERE clause.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) so that any write is explicitly constrained to the intended tenant. This prevents accidental cross-tenant modifications if ids are guessed or a different context is used.
- **server/lib/scheduled-post-runner.ts:204** — UPDATE on `scheduled_posts` (normal result-path) sets status/per_platform_results using WHERE id = ${row.id} with no tenant_id filter.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} to the WHERE clause (or otherwise bind the tenant) so updates are explicitly scoped to the owning tenant. Prefer fail-closed checks that verify the row's tenant matches the context before applying changes.
- **server/lib/scheduled-post-runner.ts:231** — Error-path UPDATE on `scheduled_posts` uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id. Even when checking status, the update is not constrained by tenant_id.
  - _Fix:_ Constrain the write by tenant (AND tenant_id = ${row.tenant_id}) to avoid cross-tenant impacts in exception handling. Always include tenant_id in WHERE for tenant-scoped tables.
- **server/lib/system-probes.ts:163** — Query counts messagesTable rows without constraining by tenant_id. The db.select(...).from(messagesTable) has no WHERE tenant_id filter and will aggregate across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant (or explicitly declare the intent if this must be global). For example, accept/derive a tenantId and use a filtered query: db.select({ count: sql<number>`count(*)::int` }).from(messagesTable).where(eq(messagesTable.tenantId, tenantId)).then(...). If this is intended only for admin/global probes, document and gate it appropriately; do NOT leave tenant-scoped tables queried without tenant_id.
- **server/martech-bundle.ts:324** — UPDATE on tenantVoiceProfiles uses only the row id in the WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) and does not also constrain by tenant_id. This allows a (malicious or buggy) holder of another profile id to update a row belonging to a different tenant.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (compound check). E.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Always enforce eq(table.tenantId, tenantId) on UPDATE/DELETE against tenant-scoped tables to preserve tenant isolation.
- **server/memory-graph.ts:81** — UPDATE memory_categories ... WHERE id = ${catId} executes without constraining tenant_id — cross-tenant update risk.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. AND tenant_id = ${tenantId}) or pass an explicit tenantId into this function / run inside a tenant-scoped tx. Verify ownership of the category before mutating it.
- **server/memory-graph.ts:141** — refreshCategoryCount: UPDATE memory_categories ... WHERE id = ${categoryId} has no tenant_id filter — tenant-scoped table updated without tenant constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (or accept a tenantId parameter and use WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Ensure the function is executed with the caller's tenant context (withTenantTx) to enforce isolation.
- **server/memory-graph.ts:196** — SELECT 1 FROM memory_links WHERE ... LIMIT 1 queries memory_links without any tenant constraint — tenant-scoped read not filtered by tenant_id.
  - _Fix:_ Restrict this existence check to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or join through the memory_entries rows (which are tenant-scoped) to ensure the link belongs to the same tenant before assuming existence.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} executes without tenant_id filtering — updates a tenant-scoped table only by id (no owner check).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or perform the update within a tenant-scoped transaction and/or verify the row belongs to the caller before updating.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} runs without constraining tenant_id — mutates tenant-scoped rows by id only.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. AND tenant_id = ${tenantId}) or perform the mutation with an explicit ownership check (the SELECT that discovered c used tenant_id, but the UPDATE must still include tenant_id to enforce application-level isolation).
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} lacks tenant_id in the WHERE clause — tenant-scoped update without tenant constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or run this update inside a tenant-scoped transaction / verify the entry belongs to the tenant before updating.
- **server/memory-intelligence.ts:315** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (second occurrence) missing tenant_id filter — same cross-tenant update risk as above.
  - _Fix:_ Same fix: include tenant_id = ${tenantId} in the WHERE clause or perform this update via a tenant-scoped storage API that enforces ownership.
- **server/minerva-planner.ts:355** — decidePlan: UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'awaiting_approval' performs a write against plans without constraining tenant_id — allows modifying plans across tenants.
  - _Fix:_ Include tenant scoping in the CAS (e.g. WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId} AND status = 'awaiting_approval'), or require/derive the caller's tenant and run the CAS inside a tenant-scoped transaction to enforce ownership.
- **server/minerva-planner.ts:373** — Probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} executes without tenant_id constraint — reading tenant-scoped row without verifying ownership.
  - _Fix:_ Add AND tenant_id = ${expectedTenantId} to the SELECT or fetch the row after verifying the caller's tenant, to avoid leaking or acting on another tenant's plan state.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 runs without tenant scoping — finding child plans by parent id may cross tenant boundaries.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${expectedTenantId}) or otherwise validate that the child plan belongs to the same tenant before returning it.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` omits tenant_id. The code verifies project ownership via assertProjectInTenant(options.projectId, options.tenantId) but the subsequent SQL INSERT does not include tenant_id, violating the invariant that tenant-scoped rows must be written with an explicit tenant_id.
  - _Fix:_ Add tenant_id to the INSERT column list and bind the caller's tenant id (e.g. include tenant_id in the columns and VALUES (${options.tenantId}, ...)). Prefer using the same db/Drizzle APIs used elsewhere and ensure the tenantId was already validated by assertProjectInTenant before inserting.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` omits tenant_id in the parallel-video path. Even though assertProjectInTenant(...) is called to validate ownership, the INSERT does not set tenant_id, violating the tenancy-enforcement invariant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure ownership check ran and pass the validated tenantId into the INSERT to enforce application-level tenant scoping.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' — no tenant filter. This reveals tenant_id/is_active across tenants and is used to drive reconnection logic.
  - _Fix:_ Restrict to a specific tenant or make the intent explicit: if you need to inspect all tenant rows for startup auto-reconnect, enumerate tenant IDs from a trusted tenant registry and then perform per-tenant queries with WHERE tenant_id = ${tid}. If this must be global, require an admin-only guard and log the action.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update uses id only and omits tenant_id. An attacker who can influence or guess an id could affect other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sub.id} AND tenant_id = ${tenantId}). When updating by id, still require tenant_id to be matched to prevent cross-tenant mutation.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} is executed without constraining by tenant_id. A caller-provided personaId can be used to read a persona record belonging to another tenant.
  - _Fix:_ Enforce tenant ownership before exposing persona data: include tenant_id in the WHERE clause (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or first fetch and validate persona. If personas are global by design, document and enforce that explicitly; otherwise require callers to provide tenantId and validate it before returning persona data.
- **server/process-governor.ts:121** — Subqueries against heartbeat_logs do not include tenant_id. The SELECTs counting successes/failures for a heartbeat task (task_failure_rate) use `WHERE task_id = ht.id ...` without scoping by tenant_id, allowing cross-tenant reads (and inaccurate metrics).
  - _Fix:_ Add explicit tenant scoping to the heartbeat_logs subqueries (e.g. `AND tenant_id = ${tenantId}`) so every read of tenant-scoped heartbeat_logs is constrained to the caller's tenant.
- **server/process-governor.ts:155** — Subquery counting pending_tasks for each agent desk uses `SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id ...` but lacks tenant_id filter. This can count tasks from other tenants.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the subquery (or join to heartbeat_tasks with tenant filter) to ensure pending_tasks are computed only for the same tenant.
- **server/process-governor.ts:199** — Query computing average response time reads from heartbeat_logs without scoping by tenant_id (`WHERE created_at > ... AND status = 'success'`) — this returns cross-tenant aggregates.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause so the performance aggregation is limited to the caller's tenant.
- **server/process-governor.ts:240** — cascading_failures aggregation queries heartbeat_logs (`WHERE status != 'success' ...`) but does not include tenant_id, producing cross-tenant counts.
  - _Fix:_ Filter heartbeat_logs by `tenant_id = ${tenantId}` (or equivalent Drizzle .where(eq(...))) to confine the aggregation to the tenant.
- **server/process-governor.ts:121** — (Related) Several places in evaluateCondition use subqueries against tenant-scoped tables (heartbeat_logs, heartbeat_tasks) without tenant_id filters (same root cause across multiple case branches).
  - _Fix:_ Audit all subqueries in evaluateCondition and add `AND tenant_id = ${tenantId}` (or equivalent) to every read against tenant-scoped tables to enforce isolation consistently.
- **server/project-brain.ts:332** — backfillProjectBrains SELECTs projects/conversations/messages without tenant scoping: `SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0`. This unscoped query can traverse data across tenants.
  - _Fix:_ Run backfill per-tenant or add explicit tenant filters to projects, conversations and messages (e.g. `p.tenant_id = ${tenantId}`, `c.tenant_id = ${tenantId}`, and messages.`tenant_id = ${tenantId}`), and ensure the background job is invoked with an approved tenant context.
- **server/project-brain.ts:350** — Querying conversations for a project uses `SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}` with no tenant constraint. That can return conversations from other tenants if project IDs collide or are tampered.
  - _Fix:_ Constrain the SELECT with tenant scoping (e.g. `AND c.tenant_id = ${tenantId}`) or resolve conversations via a project->tenant verified mapping before querying.
- **server/project-brain.ts:359** — Selecting messages (`SELECT role, content FROM messages WHERE conversation_id = ${conv.id}`) is performed without a tenant filter. Messages are tenant-scoped and must be read only within the owning tenant's context.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` on message queries or otherwise verify the conversation belongs to the tenant before reading messages.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` without constraining by tenant_id (global scan).
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or require an explicit, well-audited system-global flag parameter (_allowGlobalSystemUseOnly) and document/limit its use. If a global scan is necessary, ensure strict per-row tenant enforcement on every subsequent write/update and log/authorize the operation.
- **server/routes.ts:166** — SELECT from tenant-scoped table `file_storage` without a tenant_id constraint (restoreUploadsFromDb reads all matching filenames across tenants).
  - _Fix:_ Scope the query to a specific tenant or run as an explicit admin-only operation. For example, add a tenant filter: .where(and(like(fileStorage.mimeType, 'image/%'), isNull(fileStorage.storageKey), eq(fileStorage.tenantId, :tenantId))) or ensure this startup job runs with an explicit, auditable admin context and does not write tenant files to a shared uploads dir without per-tenant isolation.
- **server/routes.ts:765** — SELECT from tenant-scoped table `projects` (backfillProjectDriveFolders) with no tenant_id constraint — iterates over all projects across tenants without per-tenant isolation.
  - _Fix:_ If this must run across all tenants, make it explicit and run with an audited admin context; otherwise scope the selection to a specific tenant (WHERE p.tenant_id = ${tenantId}) or iterate tenants and process each tenant separately while passing the tenant id into each operation. Ensure any downstream writes use the same tenant id.
- **server/routes.ts:1531** — Public endpoint `/api/public/stats` performs counts on tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) with no tenant filter, exposing aggregate per-tenant data across the entire platform.
  - _Fix:_ Restrict these queries to admin-only endpoints, or scope counts to a specific tenant (e.g. require authentication and use WHERE tenant_id = ${tenantId}). If intended as global metrics, ensure they do not leak tenant-identifying details and are computed by a trusted admin-only path with explicit approval.
- **server/routes.ts:1559** — Public endpoint `/api/public/architecture` issues multiple SELECT COUNT(...) queries against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) with no tenant_id constraint, leaking platform-wide tenant-scoped counts and lists.
  - _Fix:_ Make the endpoint admin-only or scope results per-tenant. Add tenant filters (e.g. WHERE <table>.tenant_id = ${tenantId}) when returning tenant data, or aggregate via an explicit, audited admin job that ensures no per-tenant leakage to unauthenticated callers.
- **server/routes.ts:1117** — Uploads middleware selects all `file_storage` rows matching a filename without a tenant_id constraint (owners = SELECT tenant_id FROM file_storage WHERE filename = ...). This reads across tenants and returns rows later checked against the requester's tenant.
  - _Fix:_ Restrict the initial lookup to the caller's tenant: .where(and(eq(fileStorage.filename, baseName), eq(fileStorage.tenantId, authedTenantId))). If the intent is to detect collisions across tenants, perform that logic in an explicit admin-only flow and avoid returning or iterating over rows from other tenants in the request path.
- **server/routes.ts:3437** — Message-feedback endpoint SELECTs from `messages` by id without scoping by tenant_id (reads message row(s) across tenants) and then compares returned tenantId to the caller. The initial unscoped read can expose cross-tenant data and relies on application logic to check ownership afterwards.
  - _Fix:_ Add a tenant constraint to the SQL so the DB only returns rows belonging to the caller: .where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId))). Alternatively, use storage layer helpers that perform the ownership check inside the DB query to avoid any race/leak.
- **server/routes.ts:4325** — SELECT from tenant-scoped table 'conversations' without constraining by tenant_id (no WHERE tenant_id = ${tenantId}). This reads conversation.project_id by id but does not verify the caller's tenant owns that conversation.
  - _Fix:_ Constrain the SELECT to the caller's tenant, e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL, or use the storage layer (e.g. storage.getConversation) which enforces tenant ownership before reading project_id. Always validate ownership of referenced rows before using their values.
- **server/routes.ts:4332** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. Tenant ownership is not recorded/checked for the new note (INSERT INTO project_notes (project_id, note, author) ...).
  - _Fix:_ Include the tenant_id column in the INSERT and bind the caller's tenant id (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'})). Alternatively use a storage helper that enforces tenant scoping and validates that the project belongs to the tenant before inserting.
- **server/routes/admin-zombie-detector.ts:38** — Querying agent_trace_spans in CTE `used` with no tenant_id constraint — reads platform trace data across all tenants.
  - _Fix:_ Constrain the CTE to the caller's tenant (or explicit owner tenant) — e.g. add `AND tenant_id = ${tenantId}` to the WHERE inside the CTE and pass tenantId into the transaction so the heavy query only returns rows for that tenant.
- **server/routes/admin-zombie-detector.ts:54** — Querying conversations in CTE `used` (personasRes) with no tenant_id constraint — persona usage is derived from conversations across all tenants.
  - _Fix:_ Filter the conversations CTE by tenant_id (e.g. `AND tenant_id = ${tenantId}`) so persona last-used/conversation counts are scoped to the caller's tenant. Pass tenantId into the transaction and parameterize it.
- **server/routes/admin.ts:719** — SELECT from ai_insights (stuckInsights) has no tenant_id constraint — returns sensitive incident records across tenants.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause (and ensure tenantId is the current request's tenant or the intended admin-owned tenant). Do not read ai_insights without scoping by tenant_id.
- **server/routes/admin.ts:741** — SELECT from ai_insights (stuckPending) executed without tenant_id constraint — may leak high-priority incident rows across tenants.
  - _Fix:_ Constrain this query by tenant (e.g. `AND tenant_id = ${tenantId}`) or otherwise explicitly limit it to the admin/owner tenant. Ensure all reads from tenant-scoped tables include an explicit tenant_id predicate.
- **server/routes/conversations.ts:98** — UPDATE against tenant-scoped table 'conversations' issued without constraining tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId} (or use conv.tenantId) to ensure the caller can only modify their own tenant's row.
- **server/routes/conversations.ts:102** — INSERT into (likely tenant-scoped) table 'project_conversations' does not set tenant_id explicitly.
  - _Fix:_ Set the tenant_id column on INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conv.id}, ${tenantId})) or otherwise ensure the table row is explicitly annotated with the owning tenant to avoid cross-tenant writes.
- **server/routes/conversations.ts:104** — UPDATE against tenant-scoped table 'projects' issued without constraining tenant_id in the WHERE clause.
  - _Fix:_ Add tenant filtering to the UPDATE, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}, so only the tenant's project row can be modified.
- **server/routes/conversations.ts:229** — INSERT into 'project_notes' omits tenant_id (table appears tenant-scoped), creating a cross-tenant write risk when archiving conversation summaries.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${projId}, ${note...}, ${'system:archive'}, ${tenantId})) or otherwise ensure the inserted note is tied to the correct tenant.
- **server/routes/projects.ts:33** — SELECT subqueries count project_files, project_notes, and project_conversations without constraining those tenant-scoped tables to the caller's tenant (subselects like `SELECT COUNT(*) FROM project_files WHERE project_id = p.id`).
  - _Fix:_ Add explicit tenant filtering to every subselect (e.g. `... FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}`), or use Drizzle eq(table.tenantId, tenantId) so every read against tenant-scoped tables includes the caller's tenant_id.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is missing a tenant constraint (no tenant_id filter).
  - _Fix:_ Constrain the UPDATE to the authenticated tenant (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) to avoid touching another tenant's row; prefer using Drizzle .where(eq(projects.tenantId, tenantId)).
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (in multipart upload path) is missing tenant_id constraint.
  - _Fix:_ Same fix as above — include `AND tenant_id = ${tenantId}` (or Drizzle eq) on the UPDATE so every write to projects is explicitly tenant-scoped.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC lacks an explicit tenant_id filter on the tenant-scoped table.
  - _Fix:_ Either include `AND tenant_id = ${tenantId}` in the WHERE clause or use an explicit JOIN to projects with tenant_id = ${tenantId}. Prefer always adding the tenant_id predicate on tenant-scoped tables.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not include tenant_id filtering on project_files.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause (or join through projects with tenant_id = ${tenantId}) so the file lookup is explicitly scoped to the caller's tenant.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks tenant_id constraint on project_files.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the DELETE WHERE clause (or perform deletion via a join that includes projects.tenant_id = ${tenantId}). Always constrain DELETEs on tenant-scoped tables to the authenticated tenant.
- **server/sculptor.ts:93** — UPDATE sculptor_sessions SET status = ... WHERE id = ${session.id} is executed without constraining by tenant_id. sculptor_sessions is tenant-scoped, so this can modify another tenant's row if id is guessed or manipulated.
  - _Fix:_ Include tenant constraint on the UPDATE: e.g. UPDATE sculptor_sessions SET ... WHERE id = ${session.id} AND tenant_id = ${params.tenantId} (or use Drizzle .where(eq(table.tenantId, tenantId))). If you don't have the tenant in scope here, fetch/verify it first and only update rows that match the caller's tenant.
- **server/sculptor.ts:101** — UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id} is executed without constraining by tenant_id — allows modifying sessions belonging to other tenants.
  - _Fix:_ Add tenant constraint to the UPDATE (AND tenant_id = ${params.tenantId}) or verify the session's tenant before updating. Use parameterized Drizzle .where(eq(table.tenantId, tenantId)) to enforce the invariant.
- **server/sculptor.ts:155** — monitorSession updates sculptor_sessions (SET status/result/tool_calls_count/tokens_used/completed_at) with WHERE id = ${sessionId} but no tenant_id constraint. Background monitor code can therefore update sessions across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or query the session's tenant first and verify it matches before performing the update. Any background worker updating tenant-scoped rows must constrain by tenant_id.
- **server/sculptor.ts:346** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} is executed without tenant_id in the WHERE clause. This writes tenant-scoped data without verifying ownership.
  - _Fix:_ Restrict the UPDATE to the caller's tenant: UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, fetch the session row with tenant_id verification before updating.
- **server/seed-cleanup.ts:7** — SELECT/UPDATE operations on heartbeat_tasks (e.g. counting and disabling 'delegation' tasks) are performed without a tenant_id filter. heartbeat_tasks elsewhere is treated as tenant-scoped (tenant_id used in other queries), so these global UPDATEs/SELECTs will affect all tenants.
  - _Fix:_ If the operation is intended to operate per-tenant, add tenant_id constraints (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} or loop per-tenant). If it truly must be global admin work, make this explicit and guard it so only an admin-run process can execute it (and document the admin-only nature). Prefer explicit WHERE tenant_id = ... to avoid accidental cross-tenant changes.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' (and the subsequent UPDATE) run without tenant scoping — they will act across tenants despite heartbeat_tasks containing tenant_id elsewhere.
  - _Fix:_ Add tenant_id filtering (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) or perform per-tenant logic. Ensure maintenance/cleanup code does not implicitly modify tenant-scoped rows across all tenants.
- **server/seed-cleanup.ts:58** — SELECT/UPDATE disabling various 'examine repository' heartbeat_tasks is executed without tenant_id constraints while the same table is used with tenant_id elsewhere. This can disable tasks for all tenants unintentionally.
  - _Fix:_ Constrain maintenance updates with an explicit tenant_id (or iterate tenants) or restrict these operations to an admin-scoped run that intentionally targets ADMIN_TENANT_ID. Always include tenant_id when modifying tenant-scoped tables.
- **server/second-opinion.ts:404** — settleFusionReservation executes UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} without including tenant_id. agent_cost_ledger is queried elsewhere by tenant_id; updating by id only allows tampering with another tenant's ledger row if reservationId is misused.
  - _Fix:_ Require and check tenant context when settling a reservation. Options: (1) add tenantId parameter and run UPDATE ... WHERE id = ${reservationId} AND tenant_id = ${tenantId}; or (2) first SELECT tenant_id FROM agent_cost_ledger WHERE id = ${reservationId} and verify it matches the caller's tenant before updating. Never update tenant-scoped rows by id alone.
- **server/second-opinion.ts:423** — releaseFusionReservation executes UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId} without any tenant_id constraint, allowing modification of another tenant's ledger row by id alone.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or verify the reservation row's tenant before performing the release. Ensure all writes to agent_cost_ledger are constrained to the correct tenant.
- **server/seed.ts:35** — Unfiltered read from tenant-scoped table 'research_sessions' (SELECT COUNT(*) FROM research_sessions). This query counts sessions across all tenants with no tenant_id constraint.
  - _Fix:_ Restrict the query to a specific tenant. Pass the caller/target tenantId into the function and add a WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(research_sessions.tenantId, tenantId))). Example: SELECT COUNT(*) as count FROM research_sessions WHERE tenant_id = ${tenantId}.
- **server/seed.ts:3092** — Unfiltered read from tenant-scoped table 'research_programs' (SELECT id, name, model FROM research_programs WHERE is_active = true). No tenant_id constraint, so it returns programs for all tenants.
  - _Fix:_ Filter by tenant_id before reading tenant-scoped rows. Accept or derive a tenantId in this context and add WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(research_programs.tenantId, tenantId))). If the operation must touch all tenants, iterate per-tenant and enforce per-tenant scoping for each iteration.
- **server/seed.ts:3374** — UPDATE on tenant-scoped table `projects` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This updates project row id=15 across all tenants.
  - _Fix:_ Constrain the UPDATE to the intended tenant (e.g. add AND tenant_id = 1 or use a parameterized tenant variable). Prefer using Drizzle .where(eq(projects.tenantId, tenantId)) or an explicit WHERE tenant_id = $1 to avoid cross-tenant modification.
- **server/seed.ts:3442** — SELECT from tenant-scoped table `conversations` is executed without restricting by tenant_id. The code then inserts links into `project_conversations` (which has no tenant_id), allowing cross-tenant linking/visibility.
  - _Fix:_ Restrict the SELECT to the target tenant (e.g. WHERE c.tenant_id = 1). Ensure project_conversations is tenant-scoped (add tenant_id) and include tenant_id on INSERTs, or otherwise explicitly validate ownership of both conversation.project_id and conversation.id before creating links.
- **server/seed.ts:4714** — UPDATE on tenant-scoped table `memory_entries` is run without a tenant_id constraint. This will modify memory rows for all tenants.
  - _Fix:_ Add a tenant filter (e.g. AND tenant_id = 1 or WHERE tenant_id = $1) or iterate per-tenant. Use Drizzle's .where(eq(memoryEntries.tenantId, tenantId)) to scope updates to the caller's tenant.
- **server/seed.ts:4740** — UPDATE on tenant-scoped table `conversations` (converting model 'gpt-4.1' → 'gpt-5') is executed without any tenant_id WHERE clause, affecting all tenants' conversations.
  - _Fix:_ Constrain the UPDATE to the appropriate tenant (e.g. WHERE model = 'gpt-4.1' AND tenant_id = 1) or perform per-tenant updates using a parameterized tenantId to avoid changing other tenants' data.
- **server/seed.ts:4686** — DELETE FROM `heartbeat_tasks` is executed without a tenant_id constraint, potentially deleting heartbeat tasks across all tenants.
  - _Fix:_ Limit the DELETE to the intended tenant (e.g. add AND tenant_id = 1) or validate per-tenant ownership before deleting. Prefer Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) for safe scoped deletions.
- **server/seed.ts:4270** — Read from tenant-scoped table `heartbeat_tasks` via db.select().from(heartbeatTasks) without scoping by tenant_id; subsequent code inserts default heartbeat rows without explicitly setting tenant_id.
  - _Fix:_ When selecting, add a tenant constraint (e.g. .where(eq(heartbeatTasks.tenantId, tenantId))). When inserting seed rows, pass tenant_id explicitly (tenantId: <id>) rather than relying on implicit defaults so inserts are always scoped to the correct tenant.
- **server/seed.ts:4272** — Multiple db.insert(heartbeatTasks).values(...) calls insert heartbeat tasks without explicitly setting tenant_id. Inserting into a tenant-scoped table must supply tenant_id (columns are NOT NULL with no safe default).
  - _Fix:_ Include tenant_id (or tenantId) in every inserted heartbeat task object (e.g. tenantId: 1 or tenantId: tenantId). Alternatively, use parameterized SQL or Drizzle insert with .values({ tenantId: tenantId, ... }) and ensure any reads/repairs are also tenant-scoped.
- **server/skillify.ts:91** — SELECT from `messages` without tenant constraint. recentMsgs = db.select().from(messages).where(eq(messages.conversationId, conversationId)) does not include eq(messages.tenantId, tenantId). This lets a conversation foreign-id be used to read messages across tenants if conversation ownership isn't enforced at this call site.
  - _Fix:_ Constrain the query to the caller's tenant, e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). Alternatively, explicitly verify the conversation belongs to tenantId immediately prior to this query using a canonical assert (like assertConversationInTenant) and document that invariant at the call site.
- **server/skillify.ts:201** — SELECT from `messages` without tenant constraint. msgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)) omits tenant scoping. Per project policy every read against a tenant-scoped table must be tenant-scoped in SQL.
  - _Fix:_ Add tenant scoping to the WHERE clause (and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or call/require an ownership assertion (assertConversationInTenant) immediately before this read so the code cannot accidentally return cross-tenant data.
- **server/skillify.ts:329** — SELECT from `messages` without tenant constraint. In skillifyConversation recentMsgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)) lacks an explicit tenant_id filter.
  - _Fix:_ Scope the read to the caller's tenant by adding eq(messages.tenantId, tenantId) to the WHERE clause or perform an explicit ownership check for the conversation (assertConversationInTenant) immediately before retrieving messages.
- **server/storage.ts:239** — getConversationUnscoped() performs an unscoped SELECT from conversations (no tenant_id filter). This allows cross-tenant reads if called from HTTP handlers or other places that fail to validate ownership.
  - _Fix:_ Remove or restrict this API: require an explicit tenantId parameter, enforce a tenant equality WHERE clause, or move to a clearly internal-only codepath protected by review/ACLs. If an unscoped read is truly necessary, make callers explicitly acknowledge and validate tenant ownership immediately after the fetch, and document and audit every callsite.
- **server/storage.ts:295** — attachCitationMetadata() issues a raw SELECT FROM messages WHERE id IN (...) without constraining by tenant_id. messages is tenant-scoped; this can leak citations from other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or accept/require the tenantId as an explicit argument and assert the message ids belong to that tenant before querying the messages table.
- **server/storage.ts:396** — attachCitations() runs UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id filter. This is a write to a tenant-scoped table without enforcing tenant ownership.
  - _Fix:_ Require and validate the tenantId (or verify the message row's tenant_id first) and include it in the WHERE clause (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}) before performing the UPDATE.
- **server/storage.ts:530** — createMemoryEntry() inserts into memory_entries without validating or asserting the presence/validity of tenantId. Per schema, tenant_id is NOT NULL with no default; an insert missing tenantId or with an incorrect tenantId can create cross-tenant rows.
  - _Fix:_ Validate tenantId explicitly (e.g. assertValidTenantId(data.tenantId)) before inserting. Require callers to pass tenantId or derive + verify tenant ownership server-side and fail closed if not present.
- **server/storage.ts:647** — upsertDailyNote() updates daily_notes using WHERE id = existing.id without also constraining by tenant_id. daily_notes is tenant-scoped, so this UPDATE could affect another tenant's row if the function is misused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${existing.id} AND tenant_id = ${data.tenantId}) or require read+update within the same tenant-scoped transaction and assert the tenant before updating.
- **server/storage.ts:803** — updateMemoryEmbedding() runs UPDATE memory_entries SET embedding = ... WHERE id = ${id} with no tenant_id constraint. This mutates tenant-scoped rows without verifying tenant ownership.
  - _Fix:_ Require tenantId as an argument or validate the row's tenant_id before updating and include tenant_id in the WHERE clause to ensure the update is constrained to the correct tenant.
- **server/storage.ts:815** — updateKnowledgeEmbedding() runs UPDATE agent_knowledge SET embedding = ... WHERE id = ${id} without a tenant_id condition, allowing cross-tenant mutations.
  - _Fix:_ Require and validate tenant ownership before updating agent_knowledge rows; include eq(agentKnowledge.tenantId, tenantId) in the WHERE clause or accept tenantId and assert it first.
- **server/storage.ts:961** — updateHeartbeatTask() allows updating a heartbeat_tasks row by id when tenantId is undefined (the tenant filter is only added when tenantId !== undefined). That permits unscoped updates to tenant-scoped rows.
  - _Fix:_ Make tenantId required for this operation or fail closed when tenantId is omitted. Include eq(heartbeatTasks.tenantId, tenantId) in the WHERE clause rather than an optional subquery, so updates are always tenant-scoped unless an explicit admin override is used with strict checks.
- **server/storage.ts:967** — deleteHeartbeatTask() deletes heartbeat_tasks rows by id without requiring tenantId (tenant filter is only applied when tenantId !== undefined). This permits unscoped deletes across tenants.
  - _Fix:_ Require tenantId and include it in the DELETE's WHERE clause (eq(heartbeatTasks.tenantId, tenantId)). If an admin global delete is required, provide an explicit admin-only API protected by ACLs and audit logs.
- **server/storage.ts:1445** — supersedeAdr() performs UPDATEs on architecture_decisions using WHERE id = oldId/newId without also requiring tenant_id in the WHERE clause. Although the function verifies both rows belong to tenantId earlier, the UPDATE statements do not include tenant scoping.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clauses (e.g. WHERE id = ${oldId} AND tenant_id = ${tenantId}) to prevent accidental cross-tenant updates if the prior verification is bypassed or race conditions occur.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding() runs UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL but does NOT include tenant_id in the WHERE clause. This updates a tenant-scoped table without tenant constraint.
  - _Fix:_ Require tenantId as an explicit parameter or include AND tenant_id = ${tenantId} in the WHERE clause. Validate that the proposal belongs to the intended tenant before performing the UPDATE.
- **server/surprise-scorer.ts:141** — scoreProposalSurprise() updates actual_outcome_embedding with WHERE id = ${proposalId} but omits tenant_id scoping. This writes to felix_proposals (tenant-scoped) without enforcing tenant ownership on the UPDATE.
  - _Fix:_ Add tenant_id to the WHERE clause of the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or accept/verify tenantId explicitly before writing.
- **server/surprise-scorer.ts:214** — scoreProposalSurprise() updates surprise_score and surprise_band with WHERE id = ${proposalId} but without a tenant_id condition, enabling cross-tenant mutation of felix_proposals.
  - _Fix:_ Constrain the UPDATE with tenant_id (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or require a prior verified tenant ownership check before recording scores/bands.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` is missing a tenant_id constraint (db.select().from(customTools).where(eq(customTools.name, parsed.name))). This reads a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Restrict the lookup to the caller's tenant (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or otherwise validate the tenant owning the row before using the result. Ensure the function requires/accepts the authoritative tenantId and uses it in all reads of tenant-scoped tables.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id is missing a tenant_id constraint (db.select().from(customTools).where(eq(customTools.id, inserted.id))). This can leak tenant-scoped implementation/code across tenants.
  - _Fix:_ Include the tenant filter when reading the inserted row (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))). Always validate ownership before returning tenant-scoped fields such as implementation code.
- **server/tool-learning.ts:365** — UPDATE against tenant-scoped table `custom_tools` to increment usageCount is missing a tenant_id constraint (.where(eq(customTools.id, tool.id))). This is a write to a tenant-scoped table without enforcing tenant isolation.
  - _Fix:_ Include the tenant filter in the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))). Prefer using the authoritative tenantId parameter and validate ownership before modifying rows in tenant-scoped tables.
- **server/tools.ts:2916** — SELECT subqueries in project listing read tenant-scoped tables (project_files, project_notes, project_conversations) without an explicit tenant_id constraint. The COUNT subqueries (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id) lack tenant scoping.
  - _Fix:_ Constrain these reads to the caller's tenant. For example, SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId} (or AND tenant_id = p.tenant_id). Ensure every SELECT against tenant-scoped tables includes tenant_id in the WHERE clause.
- **server/tools.ts:2976** — Duplicate-check SELECT against project_files lacks tenant constraint. The dupCheck SELECT (SELECT id FROM project_files WHERE project_id = ${params.id} AND ...) does not include tenant_id, allowing a lookup across tenants.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or join to projects and check p.tenant_id = ${tenantId} to ensure the checked file belongs to the caller's tenant before proceeding.
- **server/tools.ts:2903** — SELECT from project_conversations (pc) reads a tenant-scoped table without an explicit tenant_id constraint. The query joins projects and conversations but does not include a direct tenant_id filter on project_conversations.
  - _Fix:_ Restrict the project_conversations read by tenant as well (e.g. WHERE pc.project_id = ${params.id} AND pc.tenant_id = ${tenantId}) or include pc.tenant_id = p.tenant_id in the WHERE clause. Ensure every table read includes an explicit tenant_id filter or equivalent validated join condition.
- **server/tools.ts:8880** — UPDATE on proposed_skills uses only proposed_skills.id in WHERE and omits a tenant_id constraint. This allows an attacker (or cross-tenant bug) to update another tenant's proposed skill by id.
  - _Fix:_ Constrain updates to the caller's tenant. Use a composite WHERE that includes tenantId, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or the SQL equivalent UPDATE ... WHERE id = $1 AND tenant_id = $2. Alternatively, verify the row belongs to tid before updating and fail if not.
- **server/tools.ts:8904** — UPDATE on proposed_skills (mark accepted + set promotedSkillId) uses only proposed_skills.id in WHERE and omits tenant_id. This permits cross-tenant modification of proposed_skills rows.
  - _Fix:_ Add tenant scoping to the WHERE clause: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or UPDATE ... WHERE id=$1 AND tenant_id=$2. Ensure the tid used is the authenticated caller's tenant. Consider wrapping in a transaction and re-checking ownership before promotion.
- **server/tools.ts:8921** — UPDATE on proposed_skills (mark rejected) uses only proposed_skills.id in WHERE and omits tenant_id. This lacks tenant scoping and can update rows across tenants by id.
  - _Fix:_ Require tenant scoping on the UPDATE: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or use SQL UPDATE ... WHERE id = $1 AND tenant_id = $2. As a defense-in-depth, verify the row's tenant matches the caller before making any state changes.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' omits tenant filter (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id})
  - _Fix:_ Include the caller's tenant in the UPDATE to enforce isolation (e.g. UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}) or use a parameterized Drizzle .where(eq(table.tenantId, tenantId)) so the write is constrained to the current tenant.
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} performs an update without constraining by tenant_id
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}) or use Drizzle with a .where(eq(projects.tenantId, tenantId)). Ensure all UPDATE/DELETE against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table 'knowledge_triples' uses only WHERE id = ${row.id} and does NOT constrain by tenant_id. This allows altering rows belonging to other tenants if an attacker can guess an id.
  - _Fix:_ Add tenant scoping to the UPDATE (for example: WHERE id = ${row.id} AND tenant_id = ${tenantId}) or first verify the row belongs to the caller's tenant before mutating. Better: use the storage/data-access helper that enforces tenant_id, or include an explicit ownership check prior to UPDATE. Add unit/integration tests asserting cross-tenant ids cannot be updated.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with NO tenant_id constraint. The prior SELECT limited by tenant, but the UPDATE by id alone can update a row from another tenant if ids collide or an attacker supplies an id.
  - _Fix:_ Constrain the UPDATE to the tenant as well. Example: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}. Or, when using Drizzle, include eq(memoryEntries.tenantId, tid) in the .where() call. Also consider doing the SELECT+UPDATE in a transaction (or SELECT ... FOR UPDATE) to avoid race conditions.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with NO tenant_id constraint. This may update rows belonging to other tenants if ids are not globally unique across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE. Example: UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Or use a parameterized Drizzle query that adds eq(memoryEntries.tenantId, tid) to the WHERE clause. Ensure the update happens under the same tenant-limited selection (or in a transaction) to avoid cross-tenant updates.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with NO tenant_id constraint. Although the id came from a tenant-scoped SELECT, performing the UPDATE without tenant filtering risks cross-tenant modification (id collision/race).
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${id} AND tenant_id = ${tid}), or use Drizzle's .where(eq(table.id, id), eq(table.tenantId, tid)). Consider wrapping SELECT+UPDATE in a transaction or using SELECT ... FOR UPDATE to prevent races.
- **server/video-job-runner.ts:204** — isCancelRequested() reads video_jobs by jobId only (SELECT cancelRequested WHERE job_id = ...) without a tenant_id constraint. This is a read against a tenant-scoped table without scoping to the caller tenant.
  - _Fix:_ Require and pass the tenantId to isCancelRequested and add a tenant filter to the query (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}). Update callers (runChaptersInBackground and any other callers) to pass the job's tenant and ensure the check only reads rows owned by that tenant.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs() performs an UPDATE on the tenant-scoped video_jobs table without constraining tenant_id, potentially modifying job rows across all tenants when marking stale jobs (UPDATE video_jobs SET ... WHERE status IN (...) AND updated_at < staleCutoff).
  - _Fix:_ Limit this operation to an appropriate scope. Options: run recovery per-tenant (iterate tenants and include WHERE tenant_id = ...), restrict to an admin-only maintenance context, or add additional filters (for example instanceId comparisons or an explicit tenant list) so the update cannot accidentally affect unrelated tenants. At minimum, document and gate the operation as an authorized cross-tenant maintenance step and prefer per-tenant updates where possible.
- **server/webhook-triggers.ts:60** — Query selects from global webhook_triggers and LEFT JOINs into personas (tenant-scoped) using `LEFT JOIN personas p ON p.id = w.persona_id` with NO tenant_id constraint on personas. This can return persona rows from any tenant and leak tenant-scoped data.
  - _Fix:_ Restrict the join to the correct tenant before returning persona data. Options: (1) If triggers should only reference admin personas, add an explicit tenant filter in the join (e.g. `LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = ${ADMIN_TENANT_ID}`), (2) if triggers may reference personas from a specific tenant, store the persona's tenant_id on webhook_triggers and join with `AND p.tenant_id = w.persona_tenant_id`, or (3) avoid the SQL join and resolve the persona name via the storage layer (e.g. `storage.getPersona(personaId, expectedTenantId)`) so you can enforce ownership checks in application logic before exposing persona_name.
- **server/webhook-triggers.ts:122** — processTriggerEvent queries webhook_triggers and again LEFT JOINs personas with `LEFT JOIN personas p ON p.id = w.persona_id` (WHERE w.webhook_key = ${webhookKey}). There is no tenant_id constraint on the tenant-scoped personas table, allowing disclosure of persona rows across tenants.
  - _Fix:_ Same fix as above: ensure any read of personas is constrained to the intended tenant. For example, join with `AND p.tenant_id = ${ADMIN_TENANT_ID}` if triggers should only use admin personas, or store+use the persona's tenant_id on webhook_triggers and join with that, or fetch the persona via the storage API with an explicit tenantId to validate ownership before returning persona_name.
- **server/wiring-invariants.ts:57** — SELECT from research_schedules (joined to research_programs) runs without constraining tenant_id. If research_schedules is tenant-scoped this will read across all tenants.
  - _Fix:_ Scope the query to an explicit tenant (or iterate tenants). e.g. add AND s.tenant_id = ${ADMIN_TENANT_ID} (or parameterize with sql`${tenantId}` / Drizzle .where(eq(table.tenantId, tenantId))). Avoid relying on global reads for tenant-scoped tables.
- **server/wiring-invariants.ts:180** — Complex query (research_programs plus subqueries reading research_experiments, code_proposals, research_sessions, research_schedules) has no tenant_id restrictions. Tenant-scoped experiment/proposal/session/schedule rows will be aggregated across tenants.
  - _Fix:_ Run the canary per-tenant or add tenant predicates to every tenant-scoped subquery (e.g. WHERE e.tenant_id = ${tenantId}, rs.tenant_id = ${tenantId}, s.tenant_id = ${tenantId}) or use Drizzle .where(eq(..., tenantId)). For admin/global checks, explicitly pass ADMIN_TENANT_ID in WHERE so reads are intentionally scoped.
- **server/wiring-invariants.ts:244** — SELECT count(*) FROM tool_performance WHERE last_success_at > ... executes without tenant filtering. tool_performance is tenant-scoped and this reads across tenants.
  - _Fix:_ Include tenant_id predicate (e.g. WHERE tenant_id = ${tenantId} AND last_success_at > ...) or iterate all tenants and run the check per-tenant. Use parameterized values via Drizzle/sql templating rather than concatenation.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tool_performance (total rows) runs with no tenant constraint; this aggregates across tenants.
  - _Fix:_ Scope to a tenant with WHERE tenant_id = ${tenantId} or run per-tenant. If intent is to measure global state, make that explicit and document why cross-tenant reads are acceptable; otherwise add tenant predicates.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages WHERE created_at > ... runs without tenant_id restriction. messages is tenant-scoped so this counts messages across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or run per-tenant). Use parameterized queries (sql`${tenantId}` or Drizzle where) so results are correctly scoped to the caller/admin tenant.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant predicate and will return tools used across all tenants.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = ${tenantId}) or execute this logic once per tenant. Prefer Drizzle or sql templating for the tenant parameter to avoid manual interpolation.
- **server/wiring-invariants.ts:366** — Raw SELECT COUNT/MAX FROM ${spec.table} is performed without any tenant_id constraint. Specified subsystem tables (e.g. knowledge_nudges, tool_performance) are tenant-scoped and the probe therefore observes cross-tenant state.
  - _Fix:_ When probing tenant-scoped subsystem tables, include tenant_id in the query (e.g. SELECT COUNT(*) FROM table WHERE tenant_id = ${tenantId}). If you must inspect schema-level state across all tenants, make that explicit and separate from per-tenant health checks.
- **server/wiring-invariants.ts:417** — SELECT aggregation from tool_performance (fail/success counts, last_failure_at) is executed without tenant filtering. This will compute drift across all tenants instead of per-tenant.
  - _Fix:_ Scope the aggregation to a tenant (WHERE tenant_id = ${tenantId}) or run the drift detection per-tenant. Use parameterized Drizzle filters rather than unscoped raw queries.
- **server/wiring-invariants.ts:535** — sql.raw SELECT COUNT/... pg_total_relation_size FROM ${tbl.name} reads memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) without tenant filters. Those tables are tenant-scoped and this inspects aggregate across tenants.
  - _Fix:_ Run per-tenant counts (WHERE tenant_id = ${tenantId}) or otherwise explicitly document and limit to ADMIN_TENANT_ID via a parameterized predicate. Avoid constructing queries with unscoped table reads for tenant-scoped data.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory executes with no tenant constraint. graph_memory is tenant-scoped and the check (which comments about emptiness ‘‘across all tenants’’) will return an aggregate instead of a per-tenant result.
  - _Fix:_ Scope the check to a tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate tenants. If the goal is to detect missing seed per-tenant, run the seed check per-tenant and use parameterized queries rather than unscoped global reads.

### MEDIUM (36)

- **server/agent-eval.ts:218** — UPDATE agent_evals uses WHERE id = ${evalId} without scoping by tenant_id (UPDATE agent_evals ... WHERE id = ${evalId}).
  - _Fix:_ Scope the update by tenant (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or validate that the eval row belongs to the caller's tenant before updating.
- **server/agent-channels.ts:198** — Query joins channel_subscriptions (cs) without constraining cs.tenant_id: JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} ... WHERE cm.tenant_id = ${tenantId} — the join reads channel_subscriptions rows without an explicit tenant_id filter.
  - _Fix:_ Add tenant scoping to the join (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}) so every read of tenant-scoped channel_subscriptions is explicit to the caller's tenant.
- **server/agentic-features.ts:600** — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Restrict this lookup to the caller's tenant (add AND tenant_id = ${params.tenantId}) so the existence check cannot leak or collide across tenants.
- **server/auto-asset-capture.ts:184** — Backfill query selects DISTINCT conversations (id, project_id, tenant_id) with no tenant_id filter. This reads conversation rows across all tenants and then processes them in the same process, violating the requirement that tenant-scoped reads be filtered by tenant_id.
  - _Fix:_ Limit the backfill to a single tenant per run (add WHERE tenant_id = ${tenantId} and run per-tenant) or explicitly partition the query by tenant and enforce ownership checks before acting on each conversation. Avoid global SELECTs over tenant-scoped tables without explicit tenant constraints.
- **server/auto-consolidation.ts:335** — Scheduler code selects DISTINCT tenant_id from memory_entries (SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL) to enumerate tenants. This is a global scan of a tenant-scoped table without per-call tenant constraint.
  - _Fix:_ If the operation must enumerate tenants, restrict its use to platform-admin/system context and ensure all subsequent writes/reads are performed with tenant_id filters. Alternatively, schedule per-tenant consolidation runs (query by tenant_id) or otherwise ensure the scan cannot be influenced by request-derived values and that each per-tenant operation re-checks tenant ownership before any writes.
- **server/auto-transcript.ts:128** — SELECT from tenant-scoped table `conversations` without constraining by tenant_id (backfill query).
  - _Fix:_ Scope the query to a tenant (e.g. add WHERE c.tenant_id = ${tenantId}) or iterate per-tenant and call autoSaveProjectTranscript per-tenant so the SELECT never reads other tenants' conversation rows.
- **server/auto-transcript.ts:153** — SELECT joining project_conversations / conversations / projects reads conversation rows without a tenant_id WHERE clause (search for conversations needing linking).
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE c.tenant_id = ${tenantId}) or constrain the whole operation to a single tenant when backfilling, to avoid reading other tenants' rows.
- **server/auto-transcript.ts:171** — SELECT from tenant-scoped table `conversations` (missingLinks query) lacks a tenant_id filter.
  - _Fix:_ Restrict the SELECT by tenant (WHERE c.tenant_id = ${tenantId}) or perform the backfill per-tenant so no tenant-scoped table is read without an explicit tenant_id constraint.
- **server/chat-engine.ts:3589** — logDecision(conversationId, ...) is called with only a conversationId and no tenant context. If logDecision persists data into a tenant-scoped table it may perform writes/reads without tenant_id filtering or without verifying the conversation belongs to the caller's tenant.
  - _Fix:_ Require and pass tenantId into logDecision (e.g. logDecision(tenantId, conversationId, ...)) or perform an explicit ownership check before calling it (fetch conversation with tenant constraint). Ensure logDecision's DB operations include WHERE tenant_id = :tenantId or set tenant_id on INSERTs.
- **server/data-protection.ts:325** — SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} does not include tenant_id. Reading notes by project_id alone can leak or mix data if project_notes are tenant-scoped and project_id reuse is possible.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${tenantId}) or join project_notes to projects and require project.tenant_id = ${tenantId}. Always include tenant predicates when querying tenant-scoped tables.
- **server/data-protection.ts:336** — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} lacks tenant_id constraint. This reads tenant-scoped project files without an explicit tenant predicate.
  - _Fix:_ Constrain the query by tenant: SELECT ... FROM project_files WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} ORDER BY created_at ASC (or join via projects with tenant filter). Ensure all reads against tenant-scoped tables include tenant_id.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken queries dfy_intake_forms by token only (WHERE token = ...) without any tenant_id constraint. dfy_intake_forms are created with tenantId set, so an unscoped SELECT can return rows belonging to other tenants.
  - _Fix:_ If the public token is intentionally the sole auth, document that explicitly and ensure token entropy/rotation is sufficient; otherwise add an optional tenantId parameter and enforce WHERE tenant_id = <tenantId> (or verify the returned form.tenantId matches the expected tenant before returning).
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with no tenant_id constraint. Although collection_id was validated earlier for tenant membership, the DELETE itself does not include tenant scoping and could affect rows with the same collection_id if data integrity assumptions change.
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}) or derive tenantId from the validated collection row and use it in the WHERE to ensure only the intended tenant's chunks are deleted.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot counts messages with SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' without scoping by tenant_id. messages is likely a tenant-scoped table; an unscoped aggregate reads cross-tenant data.
  - _Fix:_ If the traffic gate is meant to operate per-tenant, accept a tenantId and add WHERE tenant_id = ${tenantId}. If it is intentionally global, document that clearly and ensure this global access is an approved design choice.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot queries tool_performance to COUNT DISTINCT tool_name over the last 14 days without a tenant_id filter. tool_performance appears to include tenant-specific data; this unscoped query aggregates across tenants.
  - _Fix:_ Scope the query to a tenant when the algorithm runs per-tenant (WHERE tenant_id = ${tenantId}), or explicitly document/justify the global aggregation if intended.
- **server/dormant-deprecation.ts:203** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance with no tenant filter, reading tenant-scoped performance data globally.
  - _Fix:_ If candidates should be computed per-tenant, include tenant_id in the WHERE. If global behavior is intentional, make the global nature explicit and audit for privacy/isolation implications.
- **server/dormant-deprecation.ts:213** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant scoping, returning active tools across all tenants rather than per-tenant.
  - _Fix:_ Add tenant scoping (WHERE last_success_at > ... AND tenant_id = ${tenantId}) when computing per-tenant activity, or document and justify the global aggregation if that is the intended design.
- **server/health-audit.ts:314** — SELECT FROM code_proposals is executed without a tenant_id WHERE clause — reading tenant-scoped rows across all tenants.
  - _Fix:_ If this scan is intended to run as a system-level/admin job, require an explicit admin authorization check and document that it intentionally traverses tenants. If not, scope the query to a tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate tenants explicitly and run tenant-scoped queries per tenant, ensuring the caller context is authorized for each tenant.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks queries tasks without a tenant_id filter — could read tenant-scoped tasks for all tenants.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, scope the SELECT to the tenant (WHERE tenant_id = ${tenantId}). If this is an intentional global/system scan, require an explicit admin-mode guard and log that the action will iterate across tenants; otherwise avoid unscoped reads.
- **server/job-queue.ts:368** — User-supplied exclude kinds are interpolated into an array literal string (excludeLiteral) which is injected directly into the SQL template — this constructs SQL text from caller-controlled values and risks injection/semantics issues.
  - _Fix:_ Avoid constructing SQL literals manually. Use parameterized array binding instead (pass the exclude array as a parameter) or Drizzle's array helpers so the array is a bound parameter (e.g. use a parameterized text[] value or sql`kind <> ALL(${excludeArray})`), and never splice user values into raw SQL text.
- **server/job-queue.ts:385** — Aggregation over agent_jobs (SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > ... GROUP BY status) is executed with no tenant_id constraint, exposing counts across tenants.
  - _Fix:_ If the intent is per-tenant stats, add WHERE tenant_id = ${tenantId} and expose an admin-only global aggregate separately with explicit authorization. Otherwise ensure this global read is confined to an operator-only path and documented/audited; prefer explicit per-tenant queries for user-facing endpoints.
- **server/lib/action-ledger.ts:266** — Cross-tenant SELECT (listUnknownAttemptsAllTenants) reads tenant-scoped table `action_attempts` with no tenant_id filter.
  - _Fix:_ If this must be cross-tenant for a reconciler, document and explicitly justify it in audit records; otherwise restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) or implement a controlled per-tenant paging loop that queries one tenant at a time. Avoid broad, unauthenticated cross-tenant SELECTs on tenant-scoped tables.
- **server/lib/outcome-promotion.ts:158** — Cross-tenant SELECT against tenant-scoped table `deliverable_grade_events` with no tenant_id filter. The query aggregates rows across all tenants (GROUP BY tenant_id, format) without an explicit tenant selector — this is a tenant-scoping omission per the project invariant.
  - _Fix:_ If this is intended as an admin/system-wide job, make that explicit and gate it to an authorized admin runtime (document + assert admin context). Otherwise, restrict the query to a single tenant (accept a tenantId param and add WHERE tenant_id = ${tenantId}) or run the aggregation in an admin-only background context with careful auditing and least-privilege checks. Avoid unfettered cross-tenant reads from tenant-scoped tables unless explicitly approved and protected.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` filters only by status/updated_at and DOES NOT include tenant_id. This reads across tenants without an explicit tenant constraint.
  - _Fix:_ If this audit is intended to be system-wide, document that explicitly and ensure the caller has system privilege. Otherwise, add WHERE tenant_id = ${tenantId} (or accept tenantId as an argument) so reads are scoped to a tenant. Prefer parameterized tenant scoping rather than global queries to preserve isolation invariants.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` has no tenant_id constraint; it queries status='in_progress' across all tenants.
  - _Fix:_ Either require a tenantId input and filter WHERE tenant_id = ${tenantId}, or, if system-wide auditing is required, ensure the behavior is deliberate and covered by privileged system processes and add explicit checks/logging. Do not rely on implicit expectations—explicit tenant scoping avoids accidental cross-tenant disclosure/changes.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(${lockKey}) inside tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`) where lockKey is derived from ledgerKey (caller-supplied). Using sql.raw with request-derived values can lead to SQL injection or unexpected SQL evaluation and bypass of parameterization.
  - _Fix:_ Do not inject untrusted values via sql.raw. Compute/validate the numeric lock key server-side (e.g. BigInt/Number) and pass it as a parameter so the query is parameterized: tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`). Also validate ledgerKey/derived value format (numeric range) before use. If Drizzle doesn't accept BigInt as a param, explicitly cast/validate on the JS side and pass a Number safely.
- **server/recurring-messages.ts:287** — INSERT INTO `conversations` sets persona_id from `p.expandViaPersona` (untrusted/derived input) without verifying that the persona belongs to the same tenant.
  - _Fix:_ Before using a caller-/payload-supplied persona_id, verify ownership (e.g. SELECT persona row WHERE id = ${p.expandViaPersona} AND tenant_id = ${r.tenant_id}) or otherwise confirm the persona is allowed for the tenant. Reject or map foreign persona ids that are not owned/allowed by the tenant.
- **server/routes.ts:2057** — Presenter lookup by token queries `presenter_sessions` without a tenant_id constraint (SELECT ... FROM presenter_sessions WHERE token = ${token}). While token is a capability, the query still reads the tenant-scoped table without tenant scoping.
  - _Fix:_ If the token is intended as a scoped capability, document that and keep the token entropy/TTL audited. Optionally include tenant_id in the query when available (e.g. WHERE token = ${token} AND tenant_id = ${expectedTenantId}) or ensure this code path is hardened and rate-limited (and that tokens are treated as single-tenant capability keys).
- **server/routes/admin.ts:701** — SELECT DISTINCT tool_name FROM tool_performance executed with no tenant_id constraint — may return/aggregate cross-tenant tool performance data.
  - _Fix:_ If tool_performance is tenant-scoped, add `WHERE tenant_id = ${tenantId}` (or equivalent Drizzle predicate) so the listing is restricted to the caller's tenant; if the route intentionally needs cross-tenant visibility, make that explicit and audit the data-access policy.
- **server/routes/admin.ts:706** — Query on tool_performance (failingRows) lacks any tenant_id filter — aggregates recent failures platform-wide.
  - _Fix:_ If tool_performance is tenant-scoped, include `AND tenant_id = ${tenantId}` in the WHERE clause (or pass ADMIN_TENANT_ID explicitly if you intend only admin-tenant data). Avoid querying tenant-scoped tables without an explicit tenant filter.
- **server/routes/admin.ts:730** — SELECT from heartbeat_tasks (disabledTasks) omits tenant_id filtering — enumerates disabled heartbeat tasks for all tenants.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, include `AND tenant_id = ${tenantId}` in the WHERE so results are limited to the caller's tenant; otherwise document and intentionally justify cross-tenant access with explicit checks.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table 'agent_jobs' returns rows across tenants (no tenant_id WHERE filter). This exposes cross-tenant job data in the admin goal-ledger read path.
  - _Fix:_ If cross-tenant visibility is not intended, add a WHERE j.tenant_id = ${tenantId} filter. If this is intentionally cross-tenant for platform admins, ensure the caller is strongly gated (requirePlatformAdmin) and document/centralize that admin-only cross-tenant query policy; alternatively add an explicit admin-only marker and audit logging for these queries.
- **server/sculptor.ts:76** — INSERT into sculptor_sessions sets parent_session_id from caller input (params.parentSessionId) without validating that the referenced parent session belongs to the caller's tenant. This allows creating cross-tenant links/references.
  - _Fix:_ Before inserting, verify the parent session (if provided) belongs to the same tenant. e.g. SELECT id FROM sculptor_sessions WHERE id = ${params.parentSessionId} AND tenant_id = ${params.tenantId} and reject or null the parent_session_id if not found. Always validate foreign ids that reference tenant-scoped tables prior to using them in INSERTs.
- **server/situation-room.ts:156** — SELECT joins tenant-scoped table `messages` as alias `m` but the WHERE clause only explicitly filters c.tenant_id = ${tenantId}; the query references m.created_at in WHERE without adding m.tenant_id = ${tenantId}. This reads from `messages` without an explicit tenant_id condition.
  - _Fix:_ Add an explicit tenant constraint on the messages side (e.g. AND m.tenant_id = ${tenantId} OR AND m.tenant_id = c.tenant_id) to make the tenant scope explicit for every table referenced. Prefer explicit WHERE tenant_id filters on every tenant-scoped table rather than relying on join semantics.
- **server/skillify.ts:221** — SELECT from `personas` without tenant constraint. The persona lookup uses .where(eq(personas.id, conv[0].personaId)) and does not restrict by tenant. If `personas` is tenant-scoped this can leak persona names across tenants.
  - _Fix:_ If personas are tenant-scoped, include a tenant equality in the WHERE (eq(personas.tenantId, tenantId)) or otherwise ensure the persona row is validated as belonging to the tenant before reading. If personas are intentionally global/shared, add a comment documenting that and why no tenant filter is required.
- **server/storage.ts:935** — getHeartbeatTask(id) SELECTs heartbeat_tasks by id without a tenant_id constraint. heartbeat_tasks has tenant_id and this read is unscoped.
  - _Fix:_ Require caller to pass tenantId and add eq(heartbeatTasks.tenantId, tenantId) to the WHERE clause, or otherwise document and restrict this unscoped accessor to internal trusted codepaths only.

### LOW (1)

- **server/lib/mcp-api-keys.ts:166** — Lookup in `verifyApiKey` selects from tenant-scoped table `mcp_api_keys` by key_prefix alone (no tenant_id in WHERE). Although this is an auth-path that resolves tenant from the key, it is a SELECT against a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ If this design is intentional (resolve tenant from key), keep but document the rationale and harden: ensure key_prefix uniqueness (unique index), limit returned columns, avoid any error messages that could be used to enumerate prefixes/tenants, and ensure this code path is the only place that legitimately performs cross-tenant key resolution. If not intentional, require tenant context and add WHERE tenant_id = ${tenantId}.

### Suppressed — confirmed false positives (186)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — Bulk UPDATE across tenants: UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ... — this updates rows for all tenants without tenant_id constraints.
  - _Why false positive:_ Boot/backfill sweep intentionally processes rows across tenants and uses each row's tenant_id for per-row follow-on routing; this mutation is housekeeping/status-only.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id = ... WHERE id = ... (project backfill) executes against tenant-scoped `conversations` rows with NO tenant_id constraint. This can modify another tenant's conversation if id is poisoned.
  - _Why false positive:_ The row being updated comes from a prior join proving conversation/project tenant match (c.tenant_id with p.tenant_id = c.tenant_id), so ownership is established in the same call path.
- **server/commitments.ts:135** _(system-sweep)_ — Cross-tenant SELECT against tenant-scoped table `commitments` — scanAndEscalate performs a SELECT across all commitments with no WHERE tenant_id = ... clause, returning rows for every tenant.
  - _Why false positive:_ Platform-wide heartbeat scanner is an internal maintenance loop; output is redacted admin digest and per-row tenant context is not exposed to tenant-facing surfaces.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} lacks tenant_id predicate — a DELETE against a tenant-scoped table without tenant scoping can remove another tenant's rows.
  - _Why false positive:_ Purge is a platform-wide housekeeping sweep over expired soft-deleted conversations; it deletes per selected row and does not return/mix tenant data.
- **server/data-protection.ts:132** _(system-sweep)_ — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks tenant_id predicate — destructive cross-tenant delete possible.
  - _Why false positive:_ Same expired-row purge maintenance loop; compaction_archives delete is housekeeping only, not tenant-facing data mixing.
- **server/data-protection.ts:133** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} is not constrained by tenant_id — can delete another tenant's conversation row.
  - _Why false positive:_ Same maintenance purge of expired soft-deleted conversations; deletes selected rows only and emits aggregate count/logging.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT FROM memory_entries in backfillEmbeddingVecs is missing tenant scoping (no tenant_id filter) — reads tenant-scoped rows globally.
  - _Why false positive:_ Backfill is a platform-wide maintenance job updating vector columns only; it does not return or mix tenant content into one response/prompt.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} has no tenant_id constraint — can modify rows from any tenant by id.
  - _Why false positive:_ Embedding vector backfill mutates only housekeeping/index fields in a maintenance loop and is not tenant-facing or content-mixing.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillEmbeddingVecs lacks tenant scoping — reads rows across tenants.
  - _Why false positive:_ Boot/maintenance backfill sweeps all tenants to populate embedding_vec and only performs housekeeping updates/counts, not tenant-facing data mixing.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} has no tenant_id constraint — updates rows without verifying tenant ownership.
  - _Why false positive:_ Update is the paired housekeeping mutation for the cross-tenant embedding backfill sweep, writing only embedding_vec by per-row id.
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT * FROM research_schedules is executed with no tenant_id constraint. research_schedules rows contain tenant_id (used later) so this read can return rows for all tenants, violating tenant isolation.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due schedules across tenants and then processes each row with its own tenant_id; no mixed tenant output returned.
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} has no tenant_id constraint. Updating by id only may modify another tenant's row if ids collide or attacker influences id values.
  - _Why false positive:_ Cross-tenant heartbeat maintenance loop advances only scheduling metadata for the schedule row it just loaded; housekeeping-only mutation.
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} lacks tenant_id filtering — write against tenant-scoped table without tenant constraint.
  - _Why false positive:_ Same scheduler sweep updates last_run_at/next_run_at housekeeping fields on the current schedule row, not tenant content exposure.
- **server/heartbeat.ts:112** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} is executed without tenant_id in the WHERE clause, allowing cross-tenant writes.
  - _Why false positive:_ Same maintenance sweep updates next_run_at only for the due schedule row being processed; housekeeping-only cross-tenant work.
- **server/heartbeat.ts:119** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} (error recovery path) has no tenant_id constraint — can advance/reserve slots across tenants.
  - _Why false positive:_ Error-recovery branch still only advances schedule timing metadata during the platform-wide scheduler sweep.
- **server/heartbeat.ts:265** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} is executed without a tenant_id constraint. heartbeat_tasks are tenant-scoped; this write can affect a task in another tenant.
  - _Why false positive:_ Startup cleanup iterates heartbeat tasks platform-wide and normalizes only model metadata on each task row; housekeeping/admin maintenance.
- **server/heartbeat.ts:269** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} lacks tenant filtering. This mutates a tenant-scoped table by id only, risking cross-tenant modification.
  - _Why false positive:_ Startup maintenance normalizes reflection task model per row across tenants; metadata-only housekeeping, not tenant data mixing.
- **server/heartbeat.ts:686** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... is executed with no tenant_id filter. event_log rows include tenant_id and subsequent processing expects a tenant per-row — reading without scoping can expose other tenants' events to this tick processing.
  - _Why false positive:_ Heartbeat event processor intentionally sweeps pending events across tenants and routes each using the row-carried tenant_id; not a tenant-facing mixed response.
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — UPDATE (sweepStaleAttempts) performs a cross-tenant write on tenant-scoped table `action_attempts` with no tenant_id constraint (UPDATE ... WHERE id IN (SELECT id FROM action_attempts ...)).
  - _Why false positive:_ Bounded crash-recovery sweep intentionally mutates housekeeping/state across tenants and returns only rowCount, not tenant data.
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) does not set tenant_id — inserting into a tenant-scoped table without tenant_id (or validating ownership) can create cross-tenant links and/or violate NOT NULL invariant.
  - _Why false positive:_ memory_links has no tenant_id and links memory_entries rows; isolation is transitive through tenant-scoped memory_entries parents.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from oauth_subscriptions performed without any tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads all tenants' OAuth subscriptions (sensitive tokens) in one unscoped query.
  - _Why false positive:_ Startup validation is a platform-wide maintenance loop over active subscriptions; rows are handled per row by carried tenant_id and not returned to tenant-facing output.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE — full-table read across all tenants in proactiveTokenRefresh (background auto-refresh). This loads sensitive subscription rows for all tenants without per-tenant scoping.
  - _Why false positive:_ Proactive token refresh is a background maintenance sweep across tenants using each row's tenant_id for per-row refresh, with only aggregate labels/logging.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE in forceRefreshAllSubscriptions — unscoped read across all tenants. This enumerates active subscriptions across tenants and drives per-tenant refresh actions without explicit tenant scoping in the initial query.
  - _Why false positive:_ Force-refresh-all is an intentional cron-style maintenance sweep processing each subscription with its own tenant_id, not mixing tenant data into one response.
- **server/plan-executor.ts:117** _(internal-worker-pk)_ — UPDATE on tenant-scoped 'plans' table without tenant_id constraint (appendExecutionLog uses WHERE id = ${planId} only). This allows modifying another tenant's plan/log if planId is attacker-controlled or leaked.
  - _Why false positive:_ appendExecutionLog is an internal executor helper updating by server-generated planId, not directly from an untrusted request boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim: UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' — no tenant_id constraint. This lets a caller claim and execute a plan belonging to any tenant by ID alone.
  - _Why false positive:_ CAS claim is inside internal executePlan worker operating on a server-invoked plan id, not a caller-scoped tenant request path.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' WHERE id = ${planId} — no tenant_id constraint when marking plan failed due to no steps. This can flip another tenant's plan state.
  - _Why false positive:_ This update occurs immediately after the claim RETURNING row for the same plan id; ownership/state was already established in the same call path.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final status UPDATE on plans (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) has no tenant_id constraint. This can finalize plans for other tenants.
  - _Why false positive:_ Finalization updates the same claimed plan row after prior CAS RETURNING established the exact target plan/tenant in this execution path.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') lacks tenant scoping — can change another tenant's plan on error handling.
  - _Why false positive:_ Error recovery runs inside the same internal executor invocation for the already-claimed plan id, not from an untrusted tenant boundary.
- **server/plan-executor.ts:775** _(system-sweep)_ — resumeStuckPlans: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — no tenant_id filter. This globally flips executing plans across all tenants back to 'approved'.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping sweep that only flips status fields and requeues internal work; it does not expose or mix tenant data.
- **server/plan-executor.ts:784** _(system-sweep)_ — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 — no tenant_id constraint when locating pending plans to schedule. This can pick up plans from other tenants.
  - _Why false positive:_ Boot-time recovery intentionally sweeps all tenants' plans and only enqueues per-row ids for background execution; no tenant data is returned or mixed in one output.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks sets enabled = false WHERE id = ${t.id} but does not include tenant_id in the WHERE clause. Even if id was derived from a previous tenant-scoped select, the UPDATE can affect a row belonging to another tenant if ids are tampered or reused.
  - _Why false positive:_ t.id comes from an immediately prior SELECT scoped by ht.tenant_id = tenantId, so the update target was already ownership-verified in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks ... WHERE id = ${desk.id} lacks tenant_id filter. This mutates agent_desks by id only and risks modifying another tenant's row.
  - _Why false positive:_ desk.id is sourced from the preceding SELECT on agent_desks constrained by ad.tenant_id = tenantId, proving ownership earlier in the same branch.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${storedId} — missing tenant_id constraint on a tenant-scoped table. This updates a memory entry by id without asserting tenant ownership.
  - _Why false positive:_ storedId is obtained from the immediately preceding SELECT on memory_entries constrained by input.tenantId and persona/category filters.
- **server/research-engine.ts:160** _(system-sweep)_ — Mass UPDATE on `research_sessions` (UPDATE research_sessions SET ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}) lacks any tenant_id constraint — this can modify sessions across tenants.
  - _Why false positive:_ This is stale-session housekeeping across tenants, only flipping status/ended_at/summary on old running rows and returning aggregate ids for logs, not exposing mixed tenant content.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` is executed without constraining to the caller's tenant (missing tenant_id predicate). This allows one tenant to mark another tenant's message as read.
  - _Why false positive:_ msg.id was loaded just above with WHERE id = msgId AND tenant_id = tenantId before being marked read.
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id (INSERT INTO project_notes (project_id, note, author) ...). Tenant_id is required by schema and must be set from the authenticated tenant.
  - _Why false positive:_ project_notes has no tenant_id by schema and is inserted only after verifying the parent project belongs to tenantId.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (files-base64 path) omits tenant_id (INSERT INTO project_files (...)).
  - _Why false positive:_ project_files has no tenant_id by schema and the parent project ownership is checked with tenant_id before insert.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into project_files (multipart upload path) omits tenant_id (INSERT INTO project_files (...)).
  - _Why false positive:_ project_files has no tenant_id by schema and this multipart path also verifies the parent project under tenantId before insert.
- **server/seed.ts:3097** _(system-sweep)_ — Unconstrained UPDATE on tenant-scoped table 'research_programs' (UPDATE research_programs SET model = ... WHERE id = ${p.id}) with no tenant_id constraint. This can modify rows belonging to other tenants.
  - _Why false positive:_ Boot/seed maintenance loop fixes invalid model values across all rows; only mutates housekeeping/config field and does not expose mixed tenant data.
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses only id in WHERE clause (no tenant_id constraint) after an initial tenant-scoped SELECT. This can update a row belonging to a different tenant if IDs are guessed or conflict.
  - _Why false positive:_ The row id being updated comes directly from a prior SELECT on memory_entries constrained by tenant_id in the same call path.
- **server/storage.ts:838** _(system-sweep)_ — archiveExpiredMemories() executes a raw UPDATE on memory_entries across all rows (WHERE status = 'active' AND expires_at <= NOW()) with NO tenant_id constraint. This performs mass mutation across tenants and can corrupt or archive other tenants' data.
  - _Why false positive:_ Platform-wide expiry sweep archives expired memory rows by status/timestamp only and returns counts/ids, not tenant data to another tenant.
- **server/storage.ts:855** _(system-sweep)_ — archiveStaleMemories() executes a raw UPDATE on memory_entries (WHERE status='active' AND created_at <= ${cutoff} ...) with NO tenant_id constraint, causing cross-tenant mutations.
  - _Why false positive:_ Platform-wide stale-row archival only mutates status housekeeping fields across rows and does not mix or expose tenant content.
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The INSERT statement into project_files omits tenant_id, violating tenant-isolation invariant (rows may be created without explicit tenant ownership).
  - _Why false positive:_ project_files has no tenant_id by schema and is a child of tenant-scoped projects; ownership is checked on parent project before insert.
- **server/tools.ts:2995** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id. The INSERT INTO project_notes omits tenant_id, creating notes without explicit tenant scoping.
  - _Why false positive:_ project_notes has no tenant_id by schema and is transitively scoped through tenant-owned projects, with parent ownership verified before insert.
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into tenant-scoped junction table project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without recording tenant_id, risking cross-tenant linkage or missing ownership metadata.
  - _Why false positive:_ project_conversations has no tenant_id by schema; insert is gated by prior UPDATE conversations ... WHERE id = _conversationId AND tenant_id = tenantId and project was just created in-tenant.
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT into tenant-scoped junction table project_conversations omits tenant_id (second occurrence). Same problem: tenant ownership not recorded on insert.
  - _Why false positive:_ project_conversations has no tenant_id by schema; upstream checks verify project ownership and assertConversationInTenant verifies conversation belongs to tenant before insert.
- **server/tools.ts:6914** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The INSERT uses project_id only (even though assertProjectInTenant was called earlier), so the project_files row may be missing an explicit tenant_id column/value required by the tenant-scoped-table invariant.
  - _Why false positive:_ project_files has no tenant_id by schema and is transitively scoped through tenant-checked parent project via assertProjectInTenant.
- **server/tools/domains/files/handlers.ts:836** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...))
  - _Why false positive:_ project_files has no tenant_id by schema; projId is sourced from conversation joined to projects with p.tenant_id = tenantId before insert.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — Scanning experiments across tenants: SELECT id, tenant_id, hypothesis, metric, persona_id, metadata, created_at FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100 — no tenant_id constraint.
  - _Why false positive:_ platform-wide maintenance sweep over running experiments; each row carries its own tenant_id and subsequent mutation is scoped per-row by id and tenant_id.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE agentActivity without tenant scope: await db.update(agentActivity).set(updates).where(eq(agentActivity.id, activityId)) — missing tenant_id constraint on the WHERE clause.
  - _Why false positive:_ server-side completion helper updates by activityId primary key passed internally, not from a shown untrusted request boundary.
- **server/agentic-engines.ts:161** _(system-sweep)_ — Reading ai_insights across tenants in retryPendingMinervaRouting: SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50 — no tenant_id constraint.
  - _Why false positive:_ durability sweep intentionally scans pending insights across tenants and processes each using the row-carried tenant_id; no mixed tenant output returned.
- **server/agentic-features.ts:525** _(upstream-verified)_ — UPDATE lead_enrichments performed with WHERE id = ${lead.id} but no tenant filter: await db.execute(sql`UPDATE lead_enrichments SET ... WHERE id = ${lead.id}`). This can modify rows in other tenants if ids are guessed.
  - _Why false positive:_ lead.id comes from prior SELECT of lead_enrichments constrained by tenant_id = params.tenantId in the same function.
- **server/agentic-features.ts:638** _(upstream-verified)_ — UPDATE outreach_enrollments SET status = 'completed' WHERE id = ${enrollment.id} — missing tenant scoping on the update that finalizes an enrollment.
  - _Why false positive:_ enrollment.id comes from earlier SELECT on outreach_enrollments with e.tenant_id = params.tenantId in the same call path.
- **server/agentic-features.ts:672** _(upstream-verified)_ — UPDATE outreach_enrollments SET current_step = ..., next_send_at = ... WHERE id = ${enrollment.id} — update lacks tenant_id constraint.
  - _Why false positive:_ enrollment.id is sourced from the tenant-scoped enrollment query above (e.tenant_id = params.tenantId), so update operates only on rows already selected for that tenant.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE outreach_enrollments SET reply_classification = ..., status = ${newStatus} WHERE id = ${row.id} — missing tenant constraint when writing classification/status back to an enrollment.
  - _Why false positive:_ Updated row.id comes from immediately preceding SELECT on outreach_enrollments constrained by e.tenant_id = params.tenantId.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals updates agentApprovals in bulk without tenant scoping: the UPDATE sets status='expired' WHERE status='pending' AND expiresAt < now() — no tenant_id filter, touching approvals across tenants.
  - _Why false positive:_ Global expiry sweep intentionally processes pending approvals across tenants and returns per-row tenantId for follow-on scoped handling.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Query against tenant-scoped table `department_budgets` without a tenant_id constraint (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads rows for all tenants in a tenant-scoped table, violating the rule that every read/write must be constrained to a caller's tenant unless intentionally using an audited/admin cross-tenant process.
  - _Why false positive:_ Heartbeat sweep scans all active budgets but handles each row with its own tenant_id and emits per-tenant events only.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query scans tenant-scoped tables (agent_runs, agent_approvals) without constraining to a caller/worker tenant id. The initial db.execute(...) returns rows across all tenants.
  - _Why false positive:_ Worker sweep reads cross-tenant pending approval runs, keeps tenant_id per row, and resumes each run individually rather than mixing tenant data into one response.
- **server/agentic/runs.ts:36** _(internal-worker-pk)_ — appendStep updates agent_runs by id only: db.update(agentRuns).set(...).where(eq(agentRuns.id, runId)). This update lacks a tenant_id constraint and can modify rows belonging to other tenants if a cross-tenant runId is supplied.
  - _Why false positive:_ Internal helper updates agent_runs by server-generated primary key runId; no evidence runId comes from req/body/tool boundary here.
- **server/agentic/runs.ts:48** _(internal-worker-pk)_ — updateRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) without constraining by tenant_id, allowing cross-tenant state mutation.
  - _Why false positive:_ Internal state-transition helper writes by globally unique server-generated runId, not a shown untrusted tenant-crossing input.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with no tenant_id filter, enabling cross-tenant updates if runId is from another tenant.
  - _Why false positive:_ Internal run-state replacement by server-generated runId matches worker PK pattern; no request-derived id shown.
- **server/agentic/runs.ts:59** _(internal-worker-pk)_ — completeRun updates agent_runs (.where(eq(agentRuns.id, runId))) without a tenant_id constraint, permitting cross-tenant modifications to run rows.
  - _Why false positive:_ Completion helper mutates a run by internal server-generated PK runId; shown usage pattern is internal orchestration.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with no tenant_id check, which can affect runs across tenants.
  - _Why false positive:_ Failure helper updates agent_runs via internal runId primary key, with no evidence of untrusted external selection.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun updates agent_runs (.where(eq(agentRuns.id, runId))) without constraining by tenant_id, enabling cross-tenant state changes.
  - _Why false positive:_ Pause helper is an internal server workflow mutation by globally unique runId, matching internal-worker-pk.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — Claiming due wakes updates/SELECTs agent_wake_schedules without constraining to a tenant. The UPDATE/inner SELECT will atomically claim pending wakes across all tenants (no WHERE tenant_id = ...), violating tenant-isolation-by-filter invariant.
  - _Why false positive:_ Due-wake claimer is a heartbeat maintenance loop across tenants; each claimed row carries tenant_id and subsequent emit/update stays per-row scoped.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (schema convention: tenant_id NOT NULL, no default), this write will either fail or create/allow cross-tenant data (missing explicit tenant attribution).
  - _Why false positive:_ invoice_items has no tenant_id by schema and is a child table of tenant-scoped invoices via invoice_id; inserting without tenant_id is correct by design.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UNSCOPED UPDATE against tenant-scoped table `credential_vault` — getDecryptedPassword updates last_used_at using WHERE id = ${id} with no tenant_id filter, despite the table being tenant-scoped.
  - _Why false positive:_ Function first loads credential via getCredential(id, tenantId); subsequent UPDATE only touches housekeeping field last_used_at on that verified row id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UNSCOPED UPDATE against tenant-scoped table `credential_vault` — getLoginCredentials updates last_used_at using WHERE id = ${cred.id} without filtering by tenant_id.
  - _Why false positive:_ Function first loads credential via getCredentialForSite(siteUrl, tenantId); subsequent UPDATE only touches housekeeping field last_used_at for the verified credential id.
- **server/data-protection.ts:118** _(system-sweep)_ — SELECT FROM conversations (expired) queries tenant-scoped table without scoping to a specific tenant (no WHERE tenant_id = ...). This reads soft-deleted conversations across all tenants.
  - _Why false positive:_ This is a platform-wide purge job over expired soft-deleted conversations; each row carries its own tenant_id and is processed per-row for housekeeping/purge rather than mixed into one tenant-facing output.
- **server/data-protection.ts:183** _(upstream-verified)_ — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} is missing tenant_id predicate. Reading tenant-scoped messages without tenant filtering can expose other tenants' data.
  - _Why false positive:_ Immediately above, conversations row is verified with WHERE id = conversationId AND tenant_id = tenantId before reading messages for that same conversation backup path.
- **server/data-protection.ts:190** _(upstream-verified)_ — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} lacks tenant_id predicate — reads tenant-scoped archive rows without tenant scoping.
  - _Why false positive:_ Immediately above, conversation ownership is verified with tenant_id for this backup path before reading archives for that same conversation.
- **server/data-protection.ts:369** _(upstream-verified)_ — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} is missing tenant_id — counting/reading messages without tenant scoping can reveal cross-tenant counts and cause logic errors.
  - _Why false positive:_ Conversation ids are enumerated from conversations WHERE tenant_id = tenantId just above; subsequent message count is for those verified conversation ids in same call path.
- **server/felix-brain.ts:19** _(system-sweep)_ — Boot rehydration queries agent_runs without scoping by tenant. The SELECT (lines 19-24) reads rows across all tenants (no WHERE tenant_id = ...), exposing cross-tenant state at startup.
  - _Why false positive:_ boot-time rehydration intentionally scans running/pending rows across tenants into internal in-memory state and does not return mixed tenant data to callers
- **server/google-drive.ts:547** _(upstream-verified)_ — Query against projects uses WHERE eq(projects.id, projectId) without restricting by tenant. The SELECT reads a tenant-scoped row by id but does not include a tenant_id filter (no .where(eq(projects.tenantId, tenantId))). This allows reading another tenant's project record before ownership is enforced in application logic.
  - _Why false positive:_ same function immediately verifies project.tenantId matches caller tenantId before using the row
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on projects sets driveFolderId/driveFolderUrl WHERE eq(projects.id, projectId) without including a tenant filter. The UPDATE could modify a project belonging to a different tenant if called with a foreign projectId.
  - _Why false positive:_ same call path fetched the project and checked project.tenantId against tenantId just above before this update
- **server/google-drive.ts:826** _(upstream-verified)_ — uploadAndShare resolves a project by id with a SELECT that lacks a tenant filter (.where(eq(projects.id, params.projectId))). This reads a tenant-scoped row before application-level ownership checks, enabling cross-tenant reads.
  - _Why false positive:_ same function checks proj.tenantId against params.tenantId immediately after the select before proceeding
- **server/google-drive.ts:1005** _(upstream-verified)_ — searchDriveFiles performs a SELECT on projects to validate project ownership using .where(eq(projects.id, params.projectId)) but does not include tenant scoping. The code checks proj.tenantId after the read rather than constraining the query itself.
  - _Why false positive:_ the select is solely for ownership validation and the same block immediately rejects tenant mismatch before any further use
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into tenant-scoped table `presenter_slide_images` does not include tenant_id (no tenant constraint).
  - _Why false positive:_ presenter_slide_images is scoped transitively by session_id to tenant-scoped presenter_sessions; child insert without tenant_id is by design
- **server/google-workspace.ts:2364** _(fk-transitive)_ — INSERT into tenant-scoped table `presenter_slide_images` (thumb row) does not include tenant_id (no tenant constraint).
  - _Why false positive:_ presenter_slide_images thumbnail row is a child of presenter_sessions via session_id and does not need its own tenant_id
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(...) updates proposals without constraining by tenant_id.
  - _Why false positive:_ stale proposal archival is a platform-wide housekeeping sweep updating status only across rows and not exposing mixed tenant data
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs writes with no tenant_id constraint.
  - _Why false positive:_ platform-wide health audit disables stale heartbeat tasks in a maintenance sweep; action is housekeeping-only and not tenant-facing
- **server/internal-resolver.ts:91** _(internal-worker-pk)_ — UPDATE event_log is executed without constraining by tenant_id — finalizing events by id alone can mutate rows belonging to other tenants.
  - _Why false positive:_ server-invoked resolver finalizes event_log by server-side eventId primary key, not from a tenant request boundary
- **server/internal-resolver.ts:107** _(internal-worker-pk)_ — SELECT from plans (SELECT id, objective, status FROM plans WHERE id = ${planId}) is performed without verifying the plan's tenant ownership.
  - _Why false positive:_ internal resolver reads plans by server-derived planId from event processing, not caller-controlled cross-tenant input
- **server/internal-resolver.ts:170** _(internal-worker-pk)_ — UPDATE on code_proposals (UPDATE code_proposals SET ... WHERE id = ${proposalId} ...) does not include tenant_id — this may modify a proposal from another tenant.
  - _Why false positive:_ internal resolver archives code_proposals by server-derived proposalId from event data within worker flow, not direct tenant request input
- **server/knowledge-diversity-monitor.ts:266** _(system-sweep)_ — Query aggregates agent_knowledge across all tenants (SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... HAVING COUNT(*) >= ${MIN_ENTRIES}) with no tenant_id constraint — per-tenant data is being read without tenant scoping.
  - _Why false positive:_ scheduled cross-tenant monitor iterates tenant/persona pairs and processes each row with its own tenant_id, emitting only aggregate counts
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` uses only the run id in the WHERE clause (.where(eq(procedureEvolutionRuns.id, runId))) with no tenant_id constraint.
  - _Why false positive:_ runId is created by this function from a fresh insert and subsequent update targets that same server-generated primary key
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` (noop case) uses only run id in WHERE clause with no tenant predicate.
  - _Why false positive:_ noop-case update uses the same server-generated runId created earlier in the same function
- **server/lib/aevo-meta-editor.ts:499** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` (mark done) uses only run id in WHERE clause with no tenant predicate.
  - _Why false positive:_ mark-done update uses runId generated by prior insert in same internal flow, not untrusted input
- **server/lib/ideabrowser-backfill.ts:51** _(system-sweep)_ — SELECT from tenant-scoped table `projects` without constraining to a caller tenant (no WHERE tenant_id = <tenant>). This query aggregates/counts across ALL tenants (`SELECT tenant_id, count(*) ... FROM projects WHERE tags && ... GROUP BY tenant_id`).
  - _Why false positive:_ boot/backfill routine intentionally scans counts across tenants for maintenance short-circuiting and does not return tenant data to users
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (the CTE selects due rows across all tenants).
  - _Why false positive:_ Global scheduler intentionally picks due rows across tenants and returns each row with its own tenant_id for per-row handling.
- **server/lib/scheduled-post-runner.ts:121** _(upstream-verified)_ — UPDATE on `scheduled_posts` (quality-gate failure path) uses WHERE id = ${row.id} but omits tenant_id. Writes to a tenant-scoped table are not constrained to the row's tenant in the WHERE clause.
  - _Why false positive:_ row.id comes from the prior system-scope CTE that selected and locked this exact scheduled_posts row including tenant_id; update is on that verified row, not request input.
- **server/lib/scheduled-post-runner.ts:204** _(upstream-verified)_ — UPDATE on `scheduled_posts` (normal result-path) sets status/per_platform_results using WHERE id = ${row.id} with no tenant_id filter.
  - _Why false positive:_ Normal-path update uses row.id from the earlier locked/returned scheduled_posts row in the same call path, so ownership/context was already established.
- **server/lib/scheduled-post-runner.ts:231** _(upstream-verified)_ — Error-path UPDATE on `scheduled_posts` uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id. Even when checking status, the update is not constrained by tenant_id.
  - _Why false positive:_ Error-path release update targets the same previously selected locked row; id is not caller-controlled and status='publishing' further ties it to this runner state.
- **server/martech-bundle.ts:324** _(upstream-verified)_ — UPDATE on tenantVoiceProfiles uses only the row id in the WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) and does not also constrain by tenant_id. This allows a (malicious or buggy) holder of another profile id to update a row belonging to a different tenant.
  - _Why false positive:_ existing row was fetched immediately above via getVoiceProfile({ tenantId: opts.tenantId, profileName }), so existing.id is already tenant-scoped in the same call path.
- **server/memory-graph.ts:81** _(upstream-verified)_ — UPDATE memory_categories ... WHERE id = ${catId} executes without constraining tenant_id — cross-tenant update risk.
  - _Why false positive:_ catId is produced by ensureCategory for the same tenantId in this call path after tenant-filtered lookup/insert, so the increment targets that verified category row.
- **server/memory-graph.ts:196** _(fk-transitive)_ — SELECT 1 FROM memory_links WHERE ... LIMIT 1 queries memory_links without any tenant constraint — tenant-scoped read not filtered by tenant_id.
  - _Why false positive:_ Referenced schema says memory_links has no tenant_id; links are between tenant-scoped memory_entries already selected for the same tenant, so isolation is transitive through the entry ids.
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} executes without tenant_id filtering — updates a tenant-scoped table only by id (no owner check).
  - _Why false positive:_ mem.id comes from a preceding SELECT on memory_entries filtered by tenant_id = tenantId in the same function, so the update targets already-verified tenant rows.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} runs without constraining tenant_id — mutates tenant-scoped rows by id only.
  - _Why false positive:_ Each c.id comes from the immediately preceding contradictions SELECT constrained by tenant_id = tenantId, so the updated knowledge_triples rows were tenant-verified earlier in the same path.
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} lacks tenant_id in the WHERE clause — tenant-scoped update without tenant constraint.
  - _Why false positive:_ entry.id was just created with tenantId in the same call path, so the async category update applies to that newly created tenant-owned row, not untrusted input.
- **server/memory-intelligence.ts:315** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (second occurrence) missing tenant_id filter — same cross-tenant update risk as above.
  - _Why false positive:_ entry.id comes from createMemoryEntry called just above with tenantId; update targets that newly created tenant-scoped row, not caller-selected id
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The code verifies project ownership via assertProjectInTenant(options.projectId, options.tenantId) but the subsequent SQL INSERT does not include tenant_id, violating the invariant that tenant-scoped rows must be written with an explicit tenant_id.
  - _Why false positive:_ project_files schema note says tenant_id column NO; row is tenant-scoped transitively via validated project_id parent
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id in the parallel-video path. Even though assertProjectInTenant(...) is called to validate ownership, the INSERT does not set tenant_id, violating the tenancy-enforcement invariant.
  - _Why false positive:_ project_files has no tenant_id and insert is tied to validated project_id parent, so tenant isolation is transitive
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' — no tenant filter. This reveals tenant_id/is_active across tenants and is used to drive reconnection logic.
  - _Why false positive:_ startup validation intentionally scans subscriptions across tenants for maintenance auto-reconnect, using per-row tenant_id and not returning mixed tenant data to tenants
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update uses id only and omits tenant_id. An attacker who can influence or guess an id could affect other tenants' rows.
  - _Why false positive:_ boot-time maintenance loop processes rows selected globally and deactivates each row by its own carried id/tenant context; housekeeping-only mutation
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} is executed without constraining by tenant_id. A caller-provided personaId can be used to read a persona record belonging to another tenant.
  - _Why false positive:_ personas schema note shows no tenant_id column, so base persona row is platform-global by design; tenant-scoped data is filtered in subsequent queries
- **server/process-governor.ts:121** _(global-table)_ — Subqueries against heartbeat_logs do not include tenant_id. The SELECTs counting successes/failures for a heartbeat task (task_failure_rate) use `WHERE task_id = ht.id ...` without scoping by tenant_id, allowing cross-tenant reads (and inaccurate metrics).
  - _Why false positive:_ heartbeat_logs schema note shows no tenant_id column, so lack of tenant filter on log counts is consistent with global log table
- **server/process-governor.ts:199** _(global-table)_ — Query computing average response time reads from heartbeat_logs without scoping by tenant_id (`WHERE created_at > ... AND status = 'success'`) — this returns cross-tenant aggregates.
  - _Why false positive:_ heartbeat_logs schema note shows no tenant_id column, so global aggregate over logs matches non-tenant-scoped table design
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures aggregation queries heartbeat_logs (`WHERE status != 'success' ...`) but does not include tenant_id, producing cross-tenant counts.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is used here as platform-wide failure telemetry aggregate
- **server/process-governor.ts:121** _(fk-transitive)_ — (Related) Several places in evaluateCondition use subqueries against tenant-scoped tables (heartbeat_logs, heartbeat_tasks) without tenant_id filters (same root cause across multiple case branches).
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted via task_id from tenant-scoped heartbeat_tasks rows
- **server/project-brain.ts:332** _(system-sweep)_ — backfillProjectBrains SELECTs projects/conversations/messages without tenant scoping: `SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0`. This unscoped query can traverse data across tenants.
  - _Why false positive:_ boot/backfill job intentionally scans all tenants and processes each project/conversation separately without tenant-mixed output surface
- **server/project-brain.ts:350** _(system-sweep)_ — Querying conversations for a project uses `SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}` with no tenant constraint. That can return conversations from other tenants if project IDs collide or are tampered.
  - _Why false positive:_ conversation scan is inside global backfill over per-project rows and feeds per-project file updates only
- **server/project-brain.ts:359** _(system-sweep)_ — Selecting messages (`SELECT role, content FROM messages WHERE conversation_id = ${conv.id}`) is performed without a tenant filter. Messages are tenant-scoped and must be read only within the owning tenant's context.
  - _Why false positive:_ message read is part of maintenance backfill and content is handled per conversation/project, not returned in cross-tenant response
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without constraining by tenant_id (global scan).
  - _Why false positive:_ scheduler intentionally scans due jobs across tenants and carries each row tenant_id into downstream tenant-scoped operations
- **server/routes.ts:166** _(system-sweep)_ — SELECT from tenant-scoped table `file_storage` without a tenant_id constraint (restoreUploadsFromDb reads all matching filenames across tenants).
  - _Why false positive:_ startup restore is platform-wide housekeeping restoring each file row to disk and not exposing mixed tenant data to a tenant-facing surface
- **server/routes.ts:765** _(system-sweep)_ — SELECT from tenant-scoped table `projects` (backfillProjectDriveFolders) with no tenant_id constraint — iterates over all projects across tenants without per-tenant isolation.
  - _Why false positive:_ drive backfill is explicit platform-wide maintenance and passes each project's own tenant_id to downstream folder creation
- **server/routes.ts:4332** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. Tenant ownership is not recorded/checked for the new note (INSERT INTO project_notes (project_id, note, author) ...).
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects via project_id; omission is by design.
- **server/routes/admin-zombie-detector.ts:38** _(global-table)_ — Querying agent_trace_spans in CTE `used` with no tenant_id constraint — reads platform trace data across all tenants.
  - _Why false positive:_ Admin-only route intentionally reports platform-wide tool usage from trace spans joined to global capabilities registry.
- **server/routes/admin-zombie-detector.ts:54** _(global-table)_ — Querying conversations in CTE `used` (personasRes) with no tenant_id constraint — persona usage is derived from conversations across all tenants.
  - _Why false positive:_ Admin-only route intentionally derives platform-wide persona usage; personas are global and endpoint is restricted to admin tenant.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE against tenant-scoped table 'conversations' issued without constraining tenant_id in the WHERE clause.
  - _Why false positive:_ conv was just created with tenantId server-side, so updating by its new primary key is already bound to the caller's tenant.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into (likely tenant-scoped) table 'project_conversations' does not set tenant_id explicitly.
  - _Why false positive:_ project_conversations has no tenant_id column and links tenant-scoped parent rows project and conversation by FK-style ids.
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE against tenant-scoped table 'projects' issued without constraining tenant_id in the WHERE clause.
  - _Why false positive:_ Project ownership was verified immediately above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId.
- **server/routes/conversations.ts:229** _(fk-transitive)_ — INSERT into 'project_notes' omits tenant_id (table appears tenant-scoped), creating a cross-tenant write risk when archiving conversation summaries.
  - _Why false positive:_ project_notes has no tenant_id column and is inserted under a project already tenant-checked via joined projects row.
- **server/routes/projects.ts:33** _(fk-transitive)_ — SELECT subqueries count project_files, project_notes, and project_conversations without constraining those tenant-scoped tables to the caller's tenant (subselects like `SELECT COUNT(*) FROM project_files WHERE project_id = p.id`).
  - _Why false positive:_ Subcounts are over child tables with no tenant_id, scoped transitively by parent projects p already filtered to tenant_id.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is missing a tenant constraint (no tenant_id filter).
  - _Why false positive:_ Same handler verifies project ownership just above with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before touching updated_at.
- **server/routes/projects.ts:319** _(upstream-verified)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC lacks an explicit tenant_id filter on the tenant-scoped table.
  - _Why false positive:_ project ownership is verified immediately above via projects.id + tenant_id, and project_files is tenantless child data keyed by project_id.
- **server/routes/projects.ts:342** _(upstream-verified)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 does not include tenant_id filtering on project_files.
  - _Why false positive:_ same handler first verifies the parent project belongs to tenantId; project_files is then safely read by that verified project_id.
- **server/routes/projects.ts:371** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks tenant_id constraint on project_files.
  - _Why false positive:_ DELETE is gated by prior SELECT on projects with tenant_id, and project_files is a tenantless child table scoped by project_id.
- **server/sculptor.ts:93** _(internal-worker-pk)_ — UPDATE sculptor_sessions SET status = ... WHERE id = ${session.id} is executed without constraining by tenant_id. sculptor_sessions is tenant-scoped, so this can modify another tenant's row if id is guessed or manipulated.
  - _Why false positive:_ updates the freshly inserted session row by server-generated session.id within internal flow, not from an untrusted request boundary.
- **server/sculptor.ts:101** _(internal-worker-pk)_ — UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id} is executed without constraining by tenant_id — allows modifying sessions belonging to other tenants.
  - _Why false positive:_ conversation_id update targets the same server-created session by internal primary key after launch; id is not caller-controlled here.
- **server/sculptor.ts:155** _(internal-worker-pk)_ — monitorSession updates sculptor_sessions (SET status/result/tool_calls_count/tokens_used/completed_at) with WHERE id = ${sessionId} but no tenant_id constraint. Background monitor code can therefore update sessions across tenants.
  - _Why false positive:_ background monitor updates one known session by server-originated sessionId/runId pair from the same launch flow, not by untrusted external id input.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT/UPDATE operations on heartbeat_tasks (e.g. counting and disabling 'delegation' tasks) are performed without a tenant_id filter. heartbeat_tasks elsewhere is treated as tenant-scoped (tenant_id used in other queries), so these global UPDATEs/SELECTs will affect all tenants.
  - _Why false positive:_ boot/maintenance cleanup intentionally scans and disables runaway heartbeat task rows across tenants, only mutating housekeeping fields and returning no tenant data.
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' (and the subsequent UPDATE) run without tenant scoping — they will act across tenants despite heartbeat_tasks containing tenant_id elsewhere.
  - _Why false positive:_ global cleanup disables duplicate session-logger heartbeat tasks across tenants as maintenance only; no tenant content is read or mixed into outputs.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT/UPDATE disabling various 'examine repository' heartbeat_tasks is executed without tenant_id constraints while the same table is used with tenant_id elsewhere. This can disable tasks for all tenants unintentionally.
  - _Why false positive:_ maintenance sweep disables orphaned repo-examination heartbeat tasks platform-wide, mutating enabled flags only and not exposing tenant data.
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — settleFusionReservation executes UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} without including tenant_id. agent_cost_ledger is queried elsewhere by tenant_id; updating by id only allows tampering with another tenant's ledger row if reservationId is misused.
  - _Why false positive:_ settles a server-created reservation row by internal reservationId in best-effort postprocessing, not from an untrusted cross-tenant request parameter.
- **server/seed.ts:35** _(system-sweep)_ — Unfiltered read from tenant-scoped table 'research_sessions' (SELECT COUNT(*) FROM research_sessions). This query counts sessions across all tenants with no tenant_id constraint.
  - _Why false positive:_ boot-time seed/import logic does a global COUNT only to decide whether to import snapshot; no tenant data returned or mixed
- **server/seed.ts:3092** _(system-sweep)_ — Unfiltered read from tenant-scoped table 'research_programs' (SELECT id, name, model FROM research_programs WHERE is_active = true). No tenant_id constraint, so it returns programs for all tenants.
  - _Why false positive:_ seed maintenance scans all active research_programs to repair invalid model ids in place; platform-wide housekeeping only
- **server/seed.ts:3374** _(system-sweep)_ — UPDATE on tenant-scoped table `projects` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This updates project row id=15 across all tenants.
  - _Why false positive:_ owner-deployment seed mutation targets one fixed row during boot-time migration and is gated by existingDeploy; housekeeping, not tenant-facing access
- **server/seed.ts:3442** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` is executed without restricting by tenant_id. The code then inserts links into `project_conversations` (which has no tenant_id), allowing cross-tenant linking/visibility.
  - _Why false positive:_ boot-time backfill links conversations to projects using each row's own project_id/conversation_id; no tenant-facing data exposure or cross-tenant output
- **server/seed.ts:4714** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` is run without a tenant_id constraint. This will modify memory rows for all tenants.
  - _Why false positive:_ global boot-time content normalizer updates memory_entries in place across tenants; housekeeping only, no returned data or mixing
- **server/seed.ts:4740** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` (converting model 'gpt-4.1' → 'gpt-5') is executed without any tenant_id WHERE clause, affecting all tenants' conversations.
  - _Why false positive:_ global backfill repairs legacy model ids across conversations in place; housekeeping only and not request-scoped
- **server/seed.ts:4686** _(system-sweep)_ — DELETE FROM `heartbeat_tasks` is executed without a tenant_id constraint, potentially deleting heartbeat tasks across all tenants.
  - _Why false positive:_ boot-time purge of stale auto-disabled heartbeat rows is platform-wide housekeeping and does not expose tenant data
- **server/seed.ts:4270** _(system-sweep)_ — Read from tenant-scoped table `heartbeat_tasks` via db.select().from(heartbeatTasks) without scoping by tenant_id; subsequent code inserts default heartbeat rows without explicitly setting tenant_id.
  - _Why false positive:_ seed checks whether any heartbeat tasks exist globally before inserting defaults; boot-time initialization, not tenant-facing read
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from `messages` without tenant constraint. recentMsgs = db.select().from(messages).where(eq(messages.conversationId, conversationId)) does not include eq(messages.tenantId, tenantId). This lets a conversation foreign-id be used to read messages across tenants if conversation ownership isn't enforced at this call site.
  - _Why false positive:_ same call path first calls analyzeConversation(conversationId, tenantId), which asserts the conversation belongs to tenant via WHERE id AND tenant_id
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from `messages` without tenant constraint. msgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)) omits tenant scoping. Per project policy every read against a tenant-scoped table must be tenant-scoped in SQL.
  - _Why false positive:_ immediately above, analyzeConversation verifies conversations.id with tenant_id before reading messages for that conversation
- **server/storage.ts:295** _(upstream-verified)_ — attachCitationMetadata() issues a raw SELECT FROM messages WHERE id IN (...) without constraining by tenant_id. messages is tenant-scoped; this can leak citations from other tenants.
  - _Why false positive:_ message ids come only from msgs already fetched with tenant-scoped getMessages/getMessagesPaginated, so the follow-on citation lookup is limited to those verified rows.
- **server/storage.ts:647** _(upstream-verified)_ — upsertDailyNote() updates daily_notes using WHERE id = existing.id without also constraining by tenant_id. daily_notes is tenant-scoped, so this UPDATE could affect another tenant's row if the function is misused.
  - _Why false positive:_ existing is loaded immediately before via getDailyNote(..., data.tenantId), so the update target row was already tenant-scoped in the same call path.
- **server/storage.ts:1445** _(upstream-verified)_ — supersedeAdr() performs UPDATEs on architecture_decisions using WHERE id = oldId/newId without also requiring tenant_id in the WHERE clause. Although the function verifies both rows belong to tenantId earlier, the UPDATE statements do not include tenant scoping.
  - _Why false positive:_ both ADR rows are fetched just above with id plus tenantId checks, and only those verified ids are updated in the same function.
- **server/surprise-scorer.ts:141** _(upstream-verified)_ — scoreProposalSurprise() updates actual_outcome_embedding with WHERE id = ${proposalId} but omits tenant_id scoping. This writes to felix_proposals (tenant-scoped) without enforcing tenant ownership on the UPDATE.
  - _Why false positive:_ Row is fetched just above with WHERE id = proposalId AND tenant_id = tenantId; update acts on that verified row in same call path.
- **server/surprise-scorer.ts:214** _(upstream-verified)_ — scoreProposalSurprise() updates surprise_score and surprise_band with WHERE id = ${proposalId} but without a tenant_id condition, enabling cross-tenant mutation of felix_proposals.
  - _Why false positive:_ proposalId ownership is established earlier by SELECT on felix_proposals with tenant_id in the same function before this score write.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` is missing a tenant_id constraint (db.select().from(customTools).where(eq(customTools.name, parsed.name))). This reads a tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ parsed.name is rewritten via tenantToolName(parsed.name, tenantId), making lookup tenant-derived namespaced input rather than cross-tenant read.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` by id is missing a tenant_id constraint (db.select().from(customTools).where(eq(customTools.id, inserted.id))). This can leak tenant-scoped implementation/code across tenants.
  - _Why false positive:_ This reads back the row just inserted with tenantId in the same call path using server-generated inserted.id, not untrusted cross-tenant input.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE against tenant-scoped table `custom_tools` to increment usageCount is missing a tenant_id constraint (.where(eq(customTools.id, tool.id))). This is a write to a tenant-scoped table without enforcing tenant isolation.
  - _Why false positive:_ tool is first selected with name and tenantId; subsequent update by tool.id mutates that already tenant-verified row.
- **server/tools.ts:2916** _(fk-transitive)_ — SELECT subqueries in project listing read tenant-scoped tables (project_files, project_notes, project_conversations) without an explicit tenant_id constraint. The COUNT subqueries (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id) lack tenant scoping.
  - _Why false positive:_ project_notes/project_files/project_conversations have no tenant_id and are child tables scoped through project_id to tenant-scoped projects.
- **server/tools.ts:2976** _(fk-transitive)_ — Duplicate-check SELECT against project_files lacks tenant constraint. The dupCheck SELECT (SELECT id FROM project_files WHERE project_id = ${params.id} AND ...) does not include tenant_id, allowing a lookup across tenants.
  - _Why false positive:_ project_files has no tenant_id; owner project is verified just above with SELECT id FROM projects ... AND tenant_id = tenantId, so duplicate check is transitively scoped by project_id.
- **server/tools.ts:2903** _(fk-transitive)_ — SELECT from project_conversations (pc) reads a tenant-scoped table without an explicit tenant_id constraint. The query joins projects and conversations but does not include a direct tenant_id filter on project_conversations.
  - _Why false positive:_ project_conversations has no tenant_id and is joined to tenant-scoped projects plus conversations constrained to tenantId, so isolation is transitive.
- **server/tools.ts:8880** _(upstream-verified)_ — UPDATE on proposed_skills uses only proposed_skills.id in WHERE and omits a tenant_id constraint. This allows an attacker (or cross-tenant bug) to update another tenant's proposed skill by id.
  - _Why false positive:_ Update targets row.id from a row created/held earlier in the same flow for tid; id is not taken directly from caller input here.
- **server/tools.ts:8904** _(upstream-verified)_ — UPDATE on proposed_skills (mark accepted + set promotedSkillId) uses only proposed_skills.id in WHERE and omits tenant_id. This permits cross-tenant modification of proposed_skills rows.
  - _Why false positive:_ Accepted-state update uses row.id from the same tenant-scoped proposal flow, not an unverified external id.
- **server/tools.ts:8921** _(upstream-verified)_ — UPDATE on proposed_skills (mark rejected) uses only proposed_skills.id in WHERE and omits tenant_id. This lacks tenant scoping and can update rows across tenants by id.
  - _Why false positive:_ Rejected-state update also operates on same in-memory proposal row established earlier for tid in this call path.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' omits tenant filter (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id})
  - _Why false positive:_ workflow is selected immediately above with id/name plus tenant_id, and the update only touches that verified workflow.id in the same handler.
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} performs an update without constraining by tenant_id
  - _Why false positive:_ projId comes from a prior JOIN enforcing p.tenant_id = ctx.tenantId in the same call path, so the updated project was already tenant-verified.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table 'knowledge_triples' uses only WHERE id = ${row.id} and does NOT constrain by tenant_id. This allows altering rows belonging to other tenants if an attacker can guess an id.
  - _Why false positive:_ row.id values come exclusively from the immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId.
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with NO tenant_id constraint. The prior SELECT limited by tenant, but the UPDATE by id alone can update a row from another tenant if ids collide or an attacker supplies an id.
  - _Why false positive:_ id is taken from the prior SELECT on memory_entries filtered by tenant_id = tid and persona_id in the same function.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with NO tenant_id constraint. This may update rows belonging to other tenants if ids are not globally unique across tenants.
  - _Why false positive:_ ids array is derived only from rows selected just above with tenant_id = tid and persona_id, so the batch update targets that verified tenant-scoped set.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with NO tenant_id constraint. Although the id came from a tenant-scoped SELECT, performing the UPDATE without tenant filtering risks cross-tenant modification (id collision/race).
  - _Why false positive:_ id comes from the immediately preceding tenant-scoped SELECT on memory_entries (tenant_id = tid, persona scoped).
- **server/video-job-runner.ts:204** _(internal-worker-pk)_ — isCancelRequested() reads video_jobs by jobId only (SELECT cancelRequested WHERE job_id = ...) without a tenant_id constraint. This is a read against a tenant-scoped table without scoping to the caller tenant.
  - _Why false positive:_ This is an internal runner helper reading by globally unique server-generated jobId, not from an untrusted cross-tenant request boundary.
- **server/video-job-runner.ts:228** _(system-sweep)_ — recoverStaleVideoJobs() performs an UPDATE on the tenant-scoped video_jobs table without constraining tenant_id, potentially modifying job rows across all tenants when marking stale jobs (UPDATE video_jobs SET ... WHERE status IN (...) AND updated_at < staleCutoff).
  - _Why false positive:_ recoverStaleVideoJobs is a platform-wide maintenance sweep that only flips housekeeping/status fields on stale jobs and does not mix or return tenant data.
- **server/webhook-triggers.ts:60** _(global-table)_ — Query selects from global webhook_triggers and LEFT JOINs into personas (tenant-scoped) using `LEFT JOIN personas p ON p.id = w.persona_id` with NO tenant_id constraint on personas. This can return persona rows from any tenant and leak tenant-scoped data.
  - _Why false positive:_ webhook_triggers is created here as a platform-global table with no tenant_id; the listing is global/admin infrastructure rather than tenant-scoped data access.
- **server/webhook-triggers.ts:122** _(global-table)_ — processTriggerEvent queries webhook_triggers and again LEFT JOINs personas with `LEFT JOIN personas p ON p.id = w.persona_id` (WHERE w.webhook_key = ${webhookKey}). There is no tenant_id constraint on the tenant-scoped personas table, allowing disclosure of persona rows across tenants.
  - _Why false positive:_ processTriggerEvent resolves a platform-global webhook trigger by signed secret key from a global table; the persona join is part of global trigger processing, not tenant-scoped listing.
- **server/wiring-invariants.ts:57** _(system-sweep)_ — SELECT from research_schedules (joined to research_programs) runs without constraining tenant_id. If research_schedules is tenant-scoped this will read across all tenants.
  - _Why false positive:_ This is a platform-wide invariant/health check over schedules/program bindings that emits findings, not tenant-facing mixed content or per-tenant data.
- **server/wiring-invariants.ts:180** _(system-sweep)_ — Complex query (research_programs plus subqueries reading research_experiments, code_proposals, research_sessions, research_schedules) has no tenant_id restrictions. Tenant-scoped experiment/proposal/session/schedule rows will be aggregated across tenants.
  - _Why false positive:_ Outcome canary is an admin maintenance/invariant aggregation across programs; it emits counts/findings only and does not return tenant row data to tenants.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — SELECT count(*) FROM tool_performance WHERE last_success_at > ... executes without tenant filtering. tool_performance is tenant-scoped and this reads across tenants.
  - _Why false positive:_ Dormant tool detector is a platform-wide health check using aggregate counts for maintenance, with no tenant-facing disclosure or mixed content output.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT count(*) FROM tool_performance (total rows) runs with no tenant constraint; this aggregates across tenants.
  - _Why false positive:_ Boot-time wiring invariant check intentionally reads global tool_performance totals for maintenance/health diagnostics only; emits aggregate findings and does not expose tenant row data or mix content.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM messages WHERE created_at > ... runs without tenant_id restriction. messages is tenant-scoped so this counts messages across all tenants.
  - _Why false positive:_ Global housekeeping diagnostic counts recent messages only as an aggregate traffic proxy; no per-message data returned and no tenant content mixed into outputs.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant predicate and will return tools used across all tenants.
  - _Why false positive:_ Maintenance check computes globally used tool names to detect dormant registrations; result is aggregate registry health, not tenant-facing row disclosure or content mixing.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — Raw SELECT COUNT/MAX FROM ${spec.table} is performed without any tenant_id constraint. Specified subsystem tables (e.g. knowledge_nudges, tool_performance) are tenant-scoped and the probe therefore observes cross-tenant state.
  - _Why false positive:_ Subsystem freshness probe intentionally scans whole tables for row counts/last-write timestamps as platform-wide health telemetry; only housekeeping findings are emitted.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — SELECT aggregation from tool_performance (fail/success counts, last_failure_at) is executed without tenant filtering. This will compute drift across all tenants instead of per-tenant.
  - _Why false positive:_ Tool drift detector aggregates fail/success metrics across all tenants for platform-wide operational monitoring and emits only summarized warnings, not tenant data.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — sql.raw SELECT COUNT/... pg_total_relation_size FROM ${tbl.name} reads memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) without tenant filters. Those tables are tenant-scoped and this inspects aggregate across tenants.
  - _Why false positive:_ Memory growth tripwire is a platform-wide observability/housekeeping scan of table counts and sizes; it reports only aggregate thresholds and does not return tenant rows.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory executes with no tenant constraint. graph_memory is tenant-scoped and the check (which comments about emptiness ‘‘across all tenants’’) will return an aggregate instead of a per-tenant result.
  - _Why false positive:_ Graph-memory bootstrap check intentionally tests global emptiness and emits a maintenance warning only; no tenant-scoped content is read or exposed.
- **server/heartbeat.ts:2950** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause. Modifies a tenant-scoped table by id only (no tenant validation).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3402** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) is executed without tenant_id constraint. This writes to a tenant-scoped table without validating tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant scoping. This increments trigger counts for a rule id without verifying it belongs to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `custom_tools` modifies implementation without constraining by tenant_id (.where(eq(customTools.id, tool.id))). This could overwrite another tenant's tool row if IDs are guessed or collided.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE against tenant-scoped table `custom_tools` uses only the PK filter (db.delete(customTools).where(eq(customTools.id, tool.id))) with no tenant_id constraint. This can delete another tenant's tool row if the id is known or collisions occur.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/delivery-pipeline.ts:195** _(internal-worker-pk)_ — updateDeliveryLog performs an UPDATE on the tenant-scoped delivery_logs table without constraining by tenant_id (WHERE eq(delivery_logs.id, id) only). This allows cross-tenant updates if callers provide an id that belongs to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateDeliveryLog is only ever called with deliveryId returned by createDeliveryLog within deliverDigitalProduct itself (server/delivery-pipeline.ts:653,755,813,836,847,896,960,729); no caller passes a request/tool-arg-derived id.
- **server/sculptor.ts:346** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} is executed without tenant_id in the WHERE clause. This writes tenant-scoped data without verifying ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from `messages` without tenant constraint. In skillifyConversation recentMsgs = await db.select().from(messages).where(eq(messages.conversationId, conversationId)) lacks an explicit tenant_id filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Within analyzeConversation, the conversation ownership is asserted immediately before at skillify.ts:196-199 (WHERE eq(id) AND eq(tenantId), throws if absent); the messages SELECT at 201-203 reads the same owned conversation's messages.
