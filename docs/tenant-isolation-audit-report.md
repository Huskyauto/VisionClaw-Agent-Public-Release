# Tenant-Isolation Audit Report

_Generated 2026-09-06T15:59:12.021Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 335 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **331/666** file(s) audited · chunks 29/61 OK (0 failed, 32 pending)
- Findings: **95** (CRITICAL 17 · HIGH 67 · MEDIUM 11 · LOW 0)
- Severe dispositions: **84 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (17)

- **server/agent-activity.ts:122** — UPDATE against tenant-scoped table `agent_activity` without tenant constraint: completeActivity does `db.update(agentActivity).set(...).where(eq(agentActivity.id, activityId))` and can update rows in other tenants if id is guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) so updates only affect the caller's tenant. Validate callers provide the tenantId to this function.
- **server/agentic-engines.ts:239** — UPDATE against tenant-scoped table `ai_insights` without tenant constraint when called without tenantId: autoApplyEligibleInsights performs UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) with no tenant_id when tenantId is omitted, risking cross-tenant writes.
  - _Fix:_ Require a tenantId or always include a tenant filter in the UPDATE. If the function is intentionally multi-tenant, ensure it's only invoked by an operator process with explicit authority; otherwise change signature to require tenantId and use WHERE tenant_id = ${tenantId} in the UPDATE to prevent cross-tenant modifications.
- **server/agentic-features.ts:1020** — UPDATE against tenant-scoped table `outreach_enrollments` without tenant constraint: classifyReply updates an enrollment `WHERE id = ${row.id}` but does not require tenant_id, allowing an attacker who knows an id to update another tenant's row.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) and/or re-fetch the enrollment with tenant filter before updating to ensure ownership. Failing to do so permits cross-tenant writes.
- **server/agentic/approvals.ts:134** — Bulk UPDATE on tenant-scoped table `agent_approvals` without tenant constraint: expireStaleApprovals updates pending approvals that are expired but does not include tenant_id in the WHERE, resulting in expirations across all tenants.
  - _Fix:_ Either scope the expiry to a specific tenant (require tenantId param and add AND tenant_id = ${tenantId}) or explicitly document that this cron job intentionally touches all tenants and ensure operator-level authorization and auditing. Prefer per-tenant updates or carefully vetted global maintenance paths with strict controls.
- **server/auto-transcript.ts:128** — Cross-tenant exposure risk: the backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without scoping messages by tenant_id. This scans message existence across all tenants and can leak cross-tenant metadata/content.
  - _Fix:_ Restrict the subquery to the conversation's tenant (e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) or otherwise join messages through a tenant-scoped predicate so the DB enforces tenant isolation during the backfill scan.
- **server/auto-transcript.ts:153** — Cross-tenant exposure risk: the alsoLinked backfill query contains (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without tenant scoping. This can enumerate messages across tenants.
  - _Fix:_ Add tenant scoping to the subquery (e.g. AND tenant_id = c.tenant_id) or otherwise ensure the messages count is constrained by the conversation/project tenant to prevent cross-tenant reads.
- **server/auto-transcript.ts:165** — UPDATE on conversations does not include tenant scoping. The backfill calls UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL without AND tenant_id = ${c.tenant_id}, allowing potential cross-tenant modification if c.conversation_id were tampered.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}) so writes are constrained to the calling tenant and cannot modify rows belonging to other tenants.
- **server/channel-routing.ts:54** — setChannelRoute INSERTs a caller-supplied persona_id into channel_routes without verifying that the referenced persona belongs to the caller's tenant and without storing a tenant_id on the route. This lets a caller create routes that reference personas from other tenants (cross-tenant write/authorization bypass).
  - _Fix:_ Validate ownership before writing: require the caller's tenantId, verify the persona belongs to that tenant (e.g. SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} and abort if not found), and persist tenant_id on the channel_routes row (INSERT INTO channel_routes (channel, persona_id, tenant_id) VALUES (..., ${tenantId})). Perform the ownership check and insert in a transaction to avoid race conditions.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant_id restriction in scanAndEscalate — the scanner queries overdue commitments across all tenants (no WHERE tenant_id = ...), allowing cross-tenant reads.
  - _Fix:_ Do not perform global SELECTs on commitment rows. Instead enumerate tenants and run a tenant-scoped query per tenant (like the pattern used in commitment-drafter), or add an explicit WHERE tenant_id = ${tenantId} when invoked in a tenant context. If a global admin scan is required, ensure it runs in a tightly controlled admin-only process, and that returned data is strictly redacted and audited. Prefer per-tenant processing to enforce isolation.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` without a tenant_id filter. The rehydrateTaskStateOnBoot query pulls rows across all tenants: `SELECT id, tenant_id, ... FROM agent_runs WHERE status IN ('running', 'pending') ...` which can load other tenants' in-flight run state into memory (cross-tenant data exposure).
  - _Fix:_ Constrain the query to a specific tenant (e.g. add `AND tenant_id = ${tenantId}`) or explicitly iterate per-tenant with an authoritative tenant list. If the intent is an admin/global operation, require an explicit, auditable admin-only path and clearly document/guard it. Never read tenant-scoped rows unbounded in production.
- **server/google-drive.ts:1665** — INSERT into tenant-scoped table 'project_files' writes (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) but does NOT set tenant_id. Invariant requires every INSERT into tenant-scoped tables to include tenant_id explicitly (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ..., uploaded_by) VALUES (${resolvedTenantId}, ${params.projectId}, ...)) and ensure resolvedTenantId is validated to belong to the caller. If the table is not tenant-scoped, document that explicitly; otherwise fail-closed until tenant is provided and validated.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. This updates presenter_sessions by id without verifying the session belongs to the caller's tenant, allowing cross-tenant modification.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, select/verify the session row's tenant_id first and fail if it does not match the trusted tenantId before performing the UPDATE.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows without tenant_id restriction. This can modify proposals for any tenant.
  - _Fix:_ Restrict UPDATE by tenant_id as well (e.g. AND tenant_id = ${tenantId}), or group ids by tenant and run the UPDATE per-tenant after verifying the caller is authorized to modify that tenant's proposals. Never perform cross-tenant writes without explicit admin authorization and audited safeguards.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) performs cross-tenant writes with no tenant_id constraint, disabling tasks for any tenant arbitrarily.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or perform per-tenant updates. If this operation must act cross-tenant, require explicit admin authorization and verify ownership before disabling rows.
- **server/job-queue.ts:238** — completeJob updates agent_jobs to status='succeeded' WHERE id=${id} AND status='running' AND attempts=${attempts} with no tenant_id constraint. A stale/rogue caller could complete a job belonging to another tenant if job id is known.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or require callers to fetch and verify the job's tenant before calling completeJob. Preserve lease-fencing (attempts) but add tenant-scoped fencing to prevent cross-tenant mutation.
- **server/job-queue.ts:281** — failJob reads and then updates agent_jobs (SELECT max_attempts FROM agent_jobs WHERE id=${id}; later UPDATE ... WHERE id=${id} AND status='running' AND attempts=${attempts}) without any tenant_id constraint. Both the lookup and mutation can affect another tenant's job.
  - _Fix:_ Include tenant_id in the SELECT/UPDATE WHERE clauses or require the caller to pass a verified tenantId and assert it in the WHERE (e.g. WHERE id=${id} AND tenant_id=${tenantId} ...). Ensure all failure/terminal transitions are tenant-scoped to prevent cross-tenant effects.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` is executed without a tenant_id predicate (CTE selects due rows across all tenants). This runner changes status/locks rows across tenants in one statement without application-level tenant scoping.
  - _Fix:_ Limit the CTE to a single tenant (add WHERE tenant_id = ${tenantId}) or iterate per-tenant under an explicit admin/auth guard. If the runner must operate cross-tenant, make the admin intent explicit and authorize it centrally (e.g. run with ADMIN_TENANT_ID and auditably verify that this code is allowed to modify all tenants). Otherwise, scope the SELECT/UPDATE by tenant_id to enforce tenant isolation.

### HIGH (67)

- **server/ab-optimizer.ts:110** — Unscoped SELECT on tenant-scoped table `experiments`: SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100 reads rows across all tenants (no tenant_id filter).
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId} or run per-tenant loops). Avoid scanning/processing rows for other tenants; ensure any downstream actions only operate on rows belonging to the caller's tenant.
- **server/agent-activity.ts:60** — SELECT from `personas` without tenant scoping: getPersonality queries personas by id but does not constrain by tenant_id, potentially exposing persona data across tenants.
  - _Fix:_ Constrain the lookup by tenant (e.g. JOIN or add WHERE personas.tenant_id = ${tenantId}) or otherwise ensure personas is intentionally global. If personas are per-tenant, change signature to accept tenantId and include eq(personas.tenantId, tenantId) in the WHERE.
- **server/agentic-engines.ts:161** — Unscoped SELECT from tenant-scoped table `ai_insights`: retryPendingMinervaRouting selects insights WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER with no tenant constraint, reading other tenants' rows.
  - _Fix:_ Scope the SELECT by tenant_id (add AND tenant_id = ${tenantId}) or iterate per-tenant and validate tenantId before routing. Avoid reading rows for other tenants.
- **server/agentic-features.ts:861** — Unscoped SELECT on tenant-scoped table `outreach_enrollments`: enrollInSequence checks for existing enrollment with `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` but omits tenant_id, allowing cross-tenant reads/fingerprinting.
  - _Fix:_ Include tenant scoping in the check (e.g. AND tenant_id = ${params.tenantId}). Always filter enrollments by tenant before deciding on business logic such as duplicates or blocking.
- **server/agentic/department-budgets.ts:143** — Query against tenant-scoped table `department_budgets` executed without scoping to a specific tenant (missing WHERE tenant_id = ...). The code selects DISTINCT tenant_id, department FROM department_budgets with no tenant filter, allowing cross-tenant enumeration.
  - _Fix:_ If this is intended only for an admin/global sweep, require and validate an explicit admin-only code path (and document it), or else restrict the query by adding a tenant_id parameter (e.g. WHERE tenant_id = ${tenantId}). If the sweep must operate across tenants, ensure it runs only under a properly authorized privileged process and avoid exposing results to unprivileged callers; otherwise change the query to operate per-tenant to enforce tenant isolation.
- **server/agentic/resume-worker.ts:65** — SELECT query reads agent_runs/agent_approvals without constraining to a tenant_id — the sweep pulls rows across tenants (no WHERE r.tenant_id = <tenant> or equivalent).
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE r.tenant_id = ${tenantId} or use eq(agentRuns.tenantId, tenantId)), or if this must be an admin cross-tenant job, make that explicit (admin-only code path) and ensure proper authorization/audit. Do NOT run this query using unscoped caller context.
- **server/agentic/self-initiative.ts:100** — Query on heartbeat_logs lacks an explicit WHERE tenant_id = <caller tenant> constraint. It filters by task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}) but does not include heartbeat_logs.tenant_id = ${tenantId}, which violates the requirement that tenant-scoped tables be explicitly constrained by tenant_id.
  - _Fix:_ Add an explicit tenant constraint on heartbeat_logs (e.g. AND heartbeat_logs.tenant_id = ${tenantId}) or rewrite the query to JOIN heartbeat_tasks and require heartbeat_logs.tenant_id = ${tenantId} (or use drizzle eq(heartbeatLogs.tenantId, tenantId)). Ensure every read on tenant-scoped tables includes an explicit tenant filter.
- **server/agentic/wake-scheduler.ts:142** — Claim/update of agent_wake_schedules selects pending rows without constraining by tenant_id. The UPDATE/SELECT block that claims due wakes uses a subquery (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...) with no tenant_id filter, so a single heartbeat tick can claim rows belonging to any tenant.
  - _Fix:_ Restrict the claim to a single tenant (preferred) or otherwise include tenant_id in the WHERE clause. Options: (a) accept a tenantId and run the claim per-tenant: SELECT/UPDATE ... WHERE tenant_id = ${tenantId} AND status = 'pending' ...; (b) add AND tenant_id = agent_wake_schedules.tenant_id (or similar) so the UPDATE only touches rows for a defined tenant context; or (c) run a per-tenant loop that claims wake rows per tenant. Ensure every SELECT/UPDATE that touches agent_wake_schedules includes an explicit tenant_id filter (or is guarded by a platform-admin-only operation with explicit authorization).
- **server/auto-project.ts:135** — SELECT from messages lacks a tenant_id constraint. The query selects messages WHERE conversation_id = ${conversationId} without ensuring messages.tenant_id = caller tenant.
  - _Fix:_ Constrain the query to the caller tenant (e.g. add AND tenant_id = ${tenantId}) or otherwise verify/derive the tenant in SQL. Even if the conversation row was validated earlier, every SELECT against a tenant-scoped table must include an explicit tenant_id filter per the tenancy invariant.
- **server/auto-transcript.ts:46** — SELECT from messages lacks a tenant_id constraint. The code reads all messages for a conversation using WHERE conversation_id = ${conversationId} but does not include AND tenant_id = ${tenantId}.
  - _Fix:_ Add an explicit tenant filter to the messages query (AND tenant_id = ${tenantId}) or include the tenant constraint via a JOIN so the DB enforces the tenant scoping for this read, even though the conversation was validated earlier.
- **server/channel-routing.ts:31** — listChannelRoutes performs a JOIN to the personas table (p) but does not constrain the query by tenant_id. If personas is a tenant-scoped table this SELECT can expose persona rows from other tenants.
  - _Fix:_ Make channel routing tenant-aware: either (1) add tenant_id to channel_routes and include WHERE cr.tenant_id = ${tenantId} (and/or WHERE p.tenant_id = ${tenantId}) and pass tenantId into listChannelRoutes, or (2) accept a tenantId parameter and add an explicit filter in the JOIN (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}). Ensure every read of personas includes a tenant_id predicate.
- **server/chat-engine.ts:1605** — buildMemorySection declares a default tenantId = 1 (function signature: buildMemorySection(..., tenantId: number = 1)). If this function is ever invoked without an explicit tenantId it will silently operate under tenant 1 (admin) and perform tenant-scoped reads/ranking under the wrong tenant, creating a cross-tenant data-leak / authorization bypass risk.
  - _Fix:_ Remove the safe-seeming default and require an explicit tenantId parameter (fail-closed). Validate at runtime that tenantId is a positive integer and throw if absent/invalid. Audit all callers to ensure they always pass the caller's tenantId. If a default is absolutely needed for internal-only callers, replace numeric default with explicit runtime guard (e.g. throw when tenantId is undefined or <=0) and document the internal-only usage clearly.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` omits an explicit tenant constraint. The query `SELECT ... FROM project_files WHERE project_id = ${projectId} ...` relies on a previously-read project but does not include `tenant_id = ${tenantId}` in the WHERE clause, allowing potential cross-tenant data exposure if project_id validation ever changes or is bypassed.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. `... WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`) or join to `projects` and constrain by `projects.tenant_id = ${tenantId}`. Never rely solely on a prior lookup — every access to a tenant-scoped table must include the tenant_id constraint in the SQL.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` omits an explicit tenant constraint. The query `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...` does not include `tenant_id = ${tenantId}`, risking cross-tenant disclosure if project_id ownership isn't enforced at every access.
  - _Fix:_ Constrain the query by tenant (e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`) or join to `projects` with `projects.tenant_id = ${tenantId}`. Ensure every read against tenant-scoped tables includes an explicit tenant_id equality condition.
- **server/coinbase-commerce.ts:73** — SELECT against tenant-scoped table `tenants` without tenant_id restriction — function getConfiguredTenantWebhookSecrets reads all tenants' coinbase_commerce_webhook_secret values (decrypts them) and returns them for webhook verification.
  - _Fix:_ Avoid scanning the tenants table across all tenants. If the code genuinely needs to verify webhook signatures for multiple tenant-configured secrets, move webhook secrets into a dedicated global/administrative table (e.g. webhook_secrets) with explicit access controls, or restrict this function to an admin-only codepath and ensure only trusted server contexts call it. If the intent is per-tenant access, change the query to SELECT ... FROM tenants WHERE id = ${tenantId} (or otherwise scope by tenant) and only decrypt secrets for the calling tenant.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Fix:_ Add an application-level tenant filter (e.g. WHERE tenant_id = ${tenantId} OR iterate per-tenant) so the query only returns rows for the intended tenant(s). If this is an intentional global admin purge, make that explicit and run per-tenant within a controlled admin transaction (or use withTenantTx for RLS-backed enforcement).
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` uses only project_id and omits tenant scoping (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}). If project_notes is tenant-scoped, this can return another tenant's rows.
  - _Fix:_ Constrain the read by tenant (e.g. AND tenant_id = ${tenantId}) or join through a project row already verified to belong to tenant and include that tenant predicate in the WHERE clause. Always include an explicit tenant predicate on tenant-scoped tables rather than relying solely on foreign IDs.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` uses only project_id and omits tenant scoping (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}). This can expose files belonging to other tenants if an FK anomaly exists.
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or otherwise validate ownership before reading. Treat foreign IDs as untrusted unless tenant ownership is enforced in the same query.
- **server/data-protection.ts:404** — SELECT from tenant-scoped table `messages` without tenant_id constraint (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}). Even though conv.id was read earlier with tenant scoping, every read/write against tenant-scoped tables must include an explicit tenant predicate.
  - _Fix:_ Scope this query to the tenant as well (e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or use a join to a tenant-validated conversation row. Ensure all reads against tenant-scoped tables include tenant_id predicates.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken() queries dfy_intake_forms by token without constraining tenant_id. This reads a tenant-scoped table with no tenant filter.
  - _Fix:_ Require a tenant identifier or otherwise validate ownership before returning the row. Add a WHERE tenant_id = ${tenantId} (or equivalent) to the SELECT, or provide a separate public-access path that documents the security model and ensures tokens cannot be used to access other tenants' records.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed as: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} — the statement lacks an explicit tenant_id constraint when deleting from the tenant-scoped doc_chunks table.
  - _Fix:_ Include the tenant filter in the DELETE, e.g. AND tenant_id = ${tenantId}, or otherwise verify the collection belongs to the caller's tenant and use a tenant-scoped predicate to make the intent explicit in SQL.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates a tenant-scoped table by id only, without constraining tenant_id. This could modify rows belonging to another tenant if ids are misused.
  - _Fix:_ Constrain updates to the caller's tenant by including tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or resolve the row via a tenant-scoped select before updating to ensure ownership.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot() runs SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' without any tenant_id constraint. messages appears to be tenant-scoped but is queried globally.
  - _Fix:_ If this logic must be tenant-specific, accept a tenantId and add WHERE tenant_id = ${tenantId}. If the intent is global platform statistics, separate the implementation and ensure this global access is acceptable; otherwise scope to tenant to preserve isolation.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot() runs SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... with no tenant_id constraint, reading a tenant-scoped table globally.
  - _Fix:_ Scope the query by tenant (WHERE tenant_id = ${tenantId}) or change the design so the function explicitly operates per-tenant (accept tenantId). Avoid global reads of tenant-scoped tables unless documented and safe.
- **server/dormant-deprecation.ts:204** — getEverInvokedTools() SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant filter — reading tenant-scoped tool_performance across all tenants.
  - _Fix:_ Add tenant scoping (e.g. SELECT DISTINCT tool_name FROM tool_performance WHERE tenant_id = ${tenantId}) or require a tenantId parameter and enforce it, so queries do not cross tenant boundaries.
- **server/dormant-deprecation.ts:214** — getActiveTools14d() runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... but omits tenant_id, returning active tools across tenants.
  - _Fix:_ Accept and use a tenantId to restrict the query (WHERE tenant_id = ${tenantId}) or otherwise ensure this global access is deliberate and safe; prefer per-tenant scoping to enforce isolation.
- **server/email.ts:371** — SELECT from tenant-scoped table inbox_messages without tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This read can return rows across tenants.
  - _Fix:_ Include the tenant_id in the WHERE clause (or otherwise scope the query to the resolved tenant). E.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1, or supply tenantId into the function so all queries are tenant-scoped.
- **server/email.ts:416** — SELECT from tenant-scoped table inbox_messages without tenant_id constraint (SELECT id, message_id FROM inbox_messages ...). This reads rows across all tenants.
  - _Fix:_ Restrict the SELECT to a tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. Ensure you obtain the correct tenant context before selecting inbox_messages.
- **server/email.ts:429** — UPDATE against tenant-scoped table inbox_messages without tenant_id constraint (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). This can update rows in any tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) when updating inbox_messages, or otherwise validate ownership of the row before performing the UPDATE.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to executing UPDATE ${sql.raw(table)} ... WHERE id = ${id} when tenantId is undefined. This writes to tenant-scoped tables (memory_entries/agent_knowledge) without any tenant_id constraint.
  - _Fix:_ Do not perform tenant-unscoped updates. Require a positive tenantId for tenant-scoped tables and add AND tenant_id = ${tenantId} to the WHERE clause, or refuse the operation when tenantId is not provided.
- **server/embeddings.ts:843** — SELECT from tenant-scoped table memory_entries without tenant_id constraint (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL ... LIMIT 200). This reads rows across tenants.
  - _Fix:_ Scope backfill queries to a specific tenant (add WHERE tenant_id = ${tenantId}) and run per-tenant, or otherwise ensure the caller has global/admin authority and document/handle that explicitly. Avoid global reads from tenant-scoped tables without an explicit tenant filter.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} with no tenant_id constraint. This can overwrite rows across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause when updating embedding_vec (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} or the per-tenant id captured earlier). Ensure the tenant is verified before the update.
- **server/embeddings.ts:859** — SELECT from tenant-scoped table agent_knowledge without tenant_id constraint (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL ... LIMIT 200). This reads rows across tenants.
  - _Fix:_ Run the backfill per-tenant (WHERE tenant_id = ${tenantId}) or otherwise explicitly restrict the query to the intended tenant. Avoid global selects on tenant-scoped tables.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} with no tenant_id constraint. This writes to a tenant-scoped table without scoping by tenant.
  - _Fix:_ Include tenant_id in the WHERE clause for updates (AND tenant_id = ${row.tenant_id} or a supplied tenantId). Verify ownership before modifying rows.
- **server/felix-loop.ts:676** — UPDATE to tenant-scoped table `felix_loop_runs` omits tenant_id in WHERE clause. The statement `UPDATE felix_loop_runs SET ... WHERE id = ${runId}` can modify a run row belonging to another tenant if runId is mis-bound or guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${runId} AND tenant_id = ${tenantId}`) so every UPDATE is constrained to the caller's tenant. Prefer defensive checks that verify the row's tenant_id before mutating, and keep the transaction/advisory-lock pattern to avoid races.
- **server/felix-loop.ts:689** — UPDATE to tenant-scoped table `felix_loop_runs` in the error-path omits tenant_id in WHERE clause. The error-recording statement `UPDATE felix_loop_runs SET ended_at = NOW(), error = ${...} WHERE id = ${runId}` lacks tenant scoping and may affect other tenants' rows.
  - _Fix:_ Add tenant filtering to the WHERE clause (e.g. `WHERE id = ${runId} AND tenant_id = ${tenantId}`). Ensure all error/recovery writes that touch tenant-scoped tables include the same tenant constraint as the originating operation to prevent cross-tenant writes.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with NO tenant_id constraint. This update can affect rows outside the caller's tenant if IDs are abused or assumptions change.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or re-query/lock ownership and use a single parameterized statement that enforces tenantId before updating.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with NO tenant_id constraint inside the crawl loop. Every write must be explicitly scoped to the caller's tenant.
  - _Fix:_ Add the tenant constraint to the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise ensure the UPDATE is executed only when ownership has been validated and enforced in the SQL statement itself.
- **server/google-drive.ts:867** — SELECT from tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) but does NOT include a tenant_id constraint. Every read against tenant-scoped objects must be tenant-scoped in SQL to prevent cross-tenant access.
  - _Fix:_ Add the tenant filter to the SELECT (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise ensure the SQL itself enforces project ownership before returning any project data.
- **server/google-drive.ts:880** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) and NO tenant_id constraint. Writes must be scoped to the tenant at the SQL level.
  - _Fix:_ Include tenantId in the WHERE clause for the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) to ensure the update cannot affect another tenant's project row.
- **server/google-drive.ts:1474** — SELECT from tenant-scoped table 'projects' during uploadAndShare resolves a project by id using WHERE eq(projects.id, params.projectId) without constraining tenant_id. This allows a project lookup across tenants prior to ownership enforcement.
  - _Fix:_ Constrain the select to the calling tenant in SQL (e.g. add AND eq(projects.tenantId, params.tenantId)) or otherwise include the tenantId in the WHERE clause so ownership is enforced by the DB query itself.
- **server/google-drive.ts:1724** — SELECT from tenant-scoped table 'projects' inside searchDriveFiles uses WHERE eq(projects.id, params.projectId) without tenant scoping before later comparing proj.tenantId to params.tenantId. The SQL read is not tenant-scoped at the DB level.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so reads are constrained in SQL rather than relying solely on post-query checks.
- **server/google-workspace.ts:2286** — INSERT into presenter_slide_images does not include tenant scoping (no tenant_id column/value passed). If presenter_slide_images is a tenant-scoped table, this lets session_id be abused across tenants or cause cross-tenant persistence.
  - _Fix:_ Ensure presenter_slide_images is written with an explicit tenant_id column from the trusted tenant context (e.g. include tenantId in the INSERT), or validate that session_id belongs to the expected tenant before inserting. Prefer per-tenant INSERTs or an INSERT that asserts tenant ownership (or add tenant_id NOT NULL and pass tenantId explicitly).
- **server/health-audit.ts:314** — SELECT FROM code_proposals (SELECT id,title,status,created_at FROM code_proposals ...) is executed without any tenant_id WHERE clause. code_proposals appears tenant-scoped, so this reads proposals across all tenants.
  - _Fix:_ If intended to operate system-wide, require and document explicit admin authorization. Otherwise scope the query to a tenant: WHERE tenant_id = ${tenantId} AND ... or enumerate per-tenant and run the cleanup per tenant after verifying caller privileges.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (SELECT id,name,created_at,next_run_at FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL AND ...) runs without tenant_id scoping, potentially exposing/operating on heartbeat tasks across tenants.
  - _Fix:_ Scope this query to the caller's tenant (WHERE tenant_id = ${tenantId} AND ...), or, if this is a system-level cross-tenant sweep, require explicit admin privileges and make the intent obvious (and auditable). Avoid ad-hoc cross-tenant reads by default.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW() returns schedule rows across all tenants (no tenant_id WHERE clause). research_schedules is likely tenant-scoped, so this reads and drives work for all tenants without per-tenant scoping/authorization.
  - _Fix:_ Either scope the query to a particular tenant (WHERE tenant_id = ${tenantId} ...) and run per-tenant, or make explicit that this is an admin/system sweep and gate the function with strict admin authorization/auditing. If intended to run cross-tenant, ensure subsequent updates always include tenant_id in their WHERE clauses and add logging/auditing and safeguards to prevent accidental cross-tenant effects.
- **server/heartbeat-startup.ts:76** — UPDATE against tenant-scoped table `heartbeat_tasks` without tenant_id constraint (drizzle/raw SQL: UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id}). This mutates a tenant-scoped row by id but omits an explicit tenant filter/context.
  - _Fix:_ Avoid direct cross-tenant SQL here. Use the storage API (e.g. storage.updateHeartbeatTask) which enforces tenant context, or include an explicit tenant predicate (AND tenant_id = ${t.tenantId} or equivalent assertTenantContext) in the WHERE clause. If this change is intended to be global scheduler work, move it into the dedicated scheduler/cross-tenant boundary module and clearly document/audit it.
- **server/heartbeat-startup.ts:80** — UPDATE against tenant-scoped table `heartbeat_tasks` without tenant_id constraint (drizzle/raw SQL: UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id}). No tenant filter present.
  - _Fix:_ Same fix as above — do not issue tenant-scoped updates without an explicit tenant filter. Use storage.updateHeartbeatTask or add AND tenant_id = ${t.tenantId} (or assertTenantContext) or move the logic into the scheduler cross-tenant boundary if truly intended to operate across tenants.
- **server/heartbeat.ts:2609** — UPDATE against tenant-scoped table `heartbeat_tasks` without tenant_id constraint (raw SQL: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}). This mutates approval_status on a task row by id but omits tenant filtering.
  - _Fix:_ Include an explicit tenant predicate in the UPDATE (e.g. AND tenant_id = ${tenantId}) or use the storage layer (storage.updateHeartbeatTask) which enforces tenant context. If this is intentionally cross-tenant scheduler work, move it into the dedicated scheduler boundary and document the cross-tenant semantics.
- **server/heartbeat.ts:3065** — UPDATE against tenant-scoped table `heartbeat_tasks` without tenant_id constraint (raw SQL: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}). No tenant filter present.
  - _Fix:_ Same remediation as above — never UPDATE tenant-scoped rows without scoping by tenant_id. Use storage.updateHeartbeatTask or add AND tenant_id = ${tenantId} (or assertTenantContext) or move/centralize in the approved cross-tenant scheduler boundary.
- **server/job-queue.ts:160** — claimDueJobs performs a global UPDATE on agent_jobs to set status='running' (claims) with no tenant_id constraint. This updates/claims jobs across all tenants in a single (unscoped) statement.
  - _Fix:_ Either scope claims to a specific tenant (add AND tenant_id = ${tenantId} or run per-tenant claim loops), or document and enforce that this worker runs with platform-level authority only. Prefer per-tenant claiming or require an explicit tenant argument so tenant-scoped rows are never mutated without an explicit tenant filter.
- **server/job-queue.ts:195** — reclaimExpiredLeases executes UPDATEs on agent_jobs (marking as failed_terminal or re-queued) without any tenant_id filter. These reclamation updates act across all tenant rows unscoped.
  - _Fix:_ Run reclamation per-tenant (iterate tenants) or include tenant_id in the WHERE clause. At minimum, make the operation explicit about operating across tenants and ensure only an authorized platform component can perform it. Prefer scoping by tenant to avoid cross-tenant side-effects.
- **server/job-queue.ts:335** — cancelJob updates agent_jobs SET status='cancelled' WHERE id=${id} AND status IN (...) with no tenant_id constraint, allowing cancelling jobs across tenants by id.
  - _Fix:_ Require and use tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}), or make this an admin-only operation with explicit tenant verification before performing the UPDATE. Do not mutate tenant-scoped rows by id alone.
- **server/job-worker.ts:177** — research_digest job handler reads tenantId from job.payload (const { tenantId } = job.payload) and calls generateResearchDigest(tenantId) without validating that payload.tenantId matches job.tenantId or otherwise verifying tenant ownership, allowing a payload to specify a different tenant context.
  - _Fix:_ Use the worker-provided job.tenantId (the authoritative scope) or assert payload.tenantId === job.tenantId before performing tenant-scoped reads/writes. Reject or fail the job if there is a mismatch.
- **server/job-worker.ts:154** — research_code_proposal handler reconstructs session = { sessionId, tenantId, model } from job.payload and passes it to generateCodeProposal without validating that the payload tenantId equals the job.tenantId (or otherwise verifying ownership). This lets a job payload influence which tenant is acted on.
  - _Fix:_ Do not trust tenant identifiers embedded in job.payload. Require and use job.tenantId (set when the job was enqueued) or verify payload.tenantId === job.tenantId before performing any tenant-scoped operations. If missing, fail the job rather than acting on an unverified tenant id.
- **server/lib/agent-trace.ts:125** — UPDATE against tenant-scoped table agent_trace_spans without tenant_id constraint: persistSpanClose runs UPDATE ... WHERE span_id = ${spanId} but does not constrain by tenant_id.
  - _Fix:_ Ensure all writes to tenant-scoped tables include the tenant predicate. Persist the tenantId with the open-span bookkeeping (e.g. store {promise, tenantId} in _openPromises keyed by spanId) or change persistSpanClose to accept tenantId, then use WHERE span_id = ${spanId} AND tenant_id = ${tenantId}. This prevents cross-tenant updates.
- **server/lib/inbox-ingest.ts:536** — Query reads inbox_messages (alias m) via a JOIN but only constrains inbox_classifications (alias c) by tenant_id. The JOIN/SELECT does not include an explicit tenant_id filter on inbox_messages, violating the invariant that every read against a tenant-scoped table must include an explicit tenant constraint.
  - _Fix:_ Constrain the inbox_messages side of the join to the caller tenant. For example add AND m.tenant_id = ${tenantId} to the WHERE clause, or change the JOIN to: JOIN inbox_messages m ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId}. Ensure every SELECT/JOIN/UPDATE/DELETE that touches tenant-scoped tables includes an explicit tenant_id filter.
- **server/lib/outcome-promotion.ts:158** — Query aggregates deliverable_grade_events across ALL tenants without a tenant_id filter. The SELECT (tenant_id, format, count(*)) FROM deliverable_grade_events ... GROUP BY tenant_id, format reads tenant-scoped data for every tenant in a single query, violating the invariant that tenant-scoped table reads must be constrained to the caller's tenant.
  - _Fix:_ Do not scan tenant-scoped tables across tenants in a single unconstrained query. Either: (a) restrict the query to a specific tenant by adding WHERE tenant_id = ${tenantId} (and run the job per-tenant under an explicit, authorized tenant context), or (b) require an explicit privileged/admin context and document/guard that privilege, or (c) enumerate tenants first and run the aggregation per-tenant inside an authorized loop/transaction. Ensure any write paths that follow are likewise executed under the correct tenant_id and authorization.
- **server/lib/restraint-trust.ts:86** — Dynamic SQL with sql.raw() is used to interpolate a column name derived from caller input (the `outcome` -> `column` mapping) into an UPDATE statement. Even though the code performs an allowlist/validation step, using sql.raw() with a runtime-derived identifier is risky (SQL-injection / logic-bypass / future regression) and is flagged by the isolation/injection rule.
  - _Fix:_ Avoid interpolating request-derived identifiers into SQL. Replace the dynamic UPDATE with explicit, parameterized statements selected by a server-side branch (e.g. switch on the validated outcome and run an UPDATE that references the concrete column name in the SQL template). Alternatively use a safe identifier API provided by your DB/ORM (not raw string interpolation) or prepared statements per column. Keep the tenant_id/persona_id/category WHERE clause as-is to enforce tenant isolation.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` lacks a tenant_id filter; the audit query scans running rows across all tenants.
  - _Fix:_ If the function is intended to be per-tenant, add WHERE tenant_id = ${tenantId} and accept tenantId as an argument (or run inside a withTenantTx/tenant-scoped loop). If it’s intentionally cross-tenant (global admin audit), require explicit admin authorization and document/centralize this exception.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` lacks a tenant_id filter; the audit query scans in_progress tickets across all tenants.
  - _Fix:_ Same mitigation as agent_runs: scope the query by tenant_id (accept tenantId param or run per-tenant) or require explicit admin-only execution with documented justification and auditing to avoid accidental cross-tenant data exposure.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` performed using only the primary id (WHERE id = existing.id) with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. include tenant predicate: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))) so an id cannot be used to modify another tenant's row.
- **server/memory-graph.ts:130** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — no tenant_id constraint on the UPDATE against a tenant-scoped table.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${catId} AND tenant_id = ${tenantId} (or use a parameter-bound tenantId) to ensure only the intended tenant's category row is modified.
- **server/memory-graph.ts:261** — SELECT/EXISTS check against memory_links uses only foreign ids (source_memory_id/target_memory_id) with NO tenant_id restriction — a row in memory_links could be read that references another tenant's entries.
  - _Fix:_ Ensure queries on memory_links are tenant-scoped. If memory_links is tenant-scoped, include AND tenant_id = ${tenantId}. If it's not, join to memory_entries and filter by the tenant on the joined rows (or add tenant_id to the memory_links schema and set/use it on every write).
- **server/memory-graph.ts:274** — INSERT INTO memory_links(...) does not set tenant_id. If memory_links is a tenant-scoped table, this INSERT creates a row with no tenant ownership, breaking the app-level tenant-isolation invariant.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId})), or (if memory_links intentionally has no tenant column) ensure the schema+querying approach robustly enforces tenant isolation (prefer explicit tenant_id column and app-level WHERE filters).
- **server/memory-graph.ts:709** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is executed without constraining by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}, so an id cannot be used to modify rows belonging to other tenants.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} lacks a tenant_id predicate — the UPDATE may modify triples of other tenants if ids collide or are misused.
  - _Fix:_ Constrain the UPDATE with tenant_id as well, e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}, or use a JOIN on tenant-scoped rows to ensure only the calling tenant's rows are changed.

### MEDIUM (11)

- **server/agent-desk.ts:297** — SELECT from `personas` without tenant scoping in getDesksOverview: persona names are fetched via `SELECT id, name FROM personas` with no tenant filter, leaking cross-tenant persona metadata.
  - _Fix:_ Scope persona lookup to the relevant tenant (e.g. SELECT id,name FROM personas WHERE tenant_id = ${tenantId}) or explicitly document/verify that `personas` is a global table. Prefer per-tenant queries or join persona info when selecting desks to avoid global reads.
- **server/agentic-engines.ts:374** — SELECT from `personas` without tenant scoping in gatherOperationalData: `SELECT name, role FROM personas WHERE is_active = true LIMIT 14` reads persona rows across tenants.
  - _Fix:_ If personas are per-tenant, add tenant_id scoping (e.g. WHERE tenant_id = ${tenantId} AND is_active = true). If personas are intentionally global, document this and ensure it is safe. Prefer tenant-scoped access for operational reports.
- **server/auto-asset-capture.ts:184** — Backfill query enumerates conversations across tenants without a tenant_id filter. The backfill SELECT (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 ...) reads a tenant-scoped table across all tenants, then processes those rows. This allows a background job to read/operate over other tenants' data without explicit per-tenant scoping at the query level.
  - _Fix:_ Limit the backfill to an explicit tenant context or iterate per-tenant and include tenant_id in the WHERE clause (e.g. WHERE c.tenant_id = ${tenantId} AND ...). If the intent is a global admin maintenance job, make the admin scope explicit and ensure the code path is only callable by platform-admin authority and documented as an intentional cross-tenant operation. Prefer per-tenant queries to maintain the invariant that tenant-scoped reads/writes include tenant_id constraints.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments enumerates tenant rows from tenant-scoped table without scoping to a caller tenant — an unscoped read of tenant identifiers (tenant_id > 0) across the table.
  - _Fix:_ If enumeration is necessary for the background drafter, ensure this code runs only in a trusted, server-only (non-request) context and document/verify the privilege boundary. Prefer explicitly listing tenants from a central tenants table with an admin-only path, or fetch tenant ids via a purpose-built, audited admin query. If possible, avoid broad unscoped scans and instead use explicit per-tenant scheduling or a separate index/table for cross-tenant job discovery.
- **server/glasses-gateway.ts:157** — SELECT from tenant-scoped table 'apiKeys' filters by keyHash/isRevoked but does not include an explicit tenant_id constraint. Reads from tenant-scoped tables without tenant scoping can leak data across tenants or make ownership assumptions implicit.
  - _Fix:_ Document/mark apiKeys as an auth/global table if selection across tenants is intentional. Otherwise, restrict the SELECT to the expected tenant (when available), or follow with an immediate ownership/tenant check and fail if inconsistent. Prefer queries that include eq(apiKeys.tenantId, expectedTenant) where possible.
- **server/glasses-gateway.ts:173** — UPDATE on tenant-scoped table 'apiKeys' sets lastUsedAt with WHERE eq(apiKeys.id, key.id) but lacks an explicit tenant_id constraint. This can modify records across tenants if the id is manipulated or assumptions change.
  - _Fix:_ Constrain the UPDATE with tenantId (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) so the write is enforced at SQL level to the owning tenant's row.
- **server/heartbeat.ts:256** — SELECT from tenant-scoped table `event_log` without tenant_id constraint (raw SQL: SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10). This reads pending events across tenants with no SQL tenant filter.
  - _Fix:_ If this must operate across tenants, perform the read in the approved scheduler/cross-tenant boundary module (and document it), or add an explicit tenant_id predicate to limit rows to the intended tenant. Prefer using a well-audited storage helper API that makes cross-tenant intent explicit. Avoid unscoped SELECTs on tenant-scoped tables in general-purpose modules.
- **server/heartbeat.ts:1509** — SELECT from `heartbeat_logs` lacks tenant scoping (raw SQL: SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1). This query can return rows for other tenants.
  - _Fix:_ Scope this read to the intended tenant (e.g. WHERE tenant_id = ${tenantId}) or call a storage helper that accepts an explicit tenant argument. If the intention is to check a global admin/ADMIN_TENANT_ID log, make that explicit in the WHERE clause (tenant_id = ADMIN_TENANT_ID) and document why cross-tenant access is required.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} has no tenant filter. While this is an admin helper, it reads a tenant-scoped table by id without scoping — misuse in non-admin contexts could leak tenant ownership information.
  - _Fix:_ Ensure this helper is restricted to platform-admin code paths and document its intended admin-only use. Prefer adding an explicit tenant parameter or an authorization gate so the call cannot be used from tenant-scoped code paths without verification.
- **server/job-queue.ts:406** — listJobs builds and executes a SELECT from agent_jobs that may omit tenant_id (when the caller doesn't pass tenantId). That returns rows across all tenants unless caller supplies tenantId.
  - _Fix:_ Require explicit tenantId for tenant-scoped callers, or ensure callers have admin privileges before allowing an unscoped query. If the API is intended for admins, enforce admin-only checks; otherwise add a mandatory tenant filter to the WHERE clause.
- **server/lib/system-probes.ts:163** — COUNT(*) query on `messages` table (messagesTable) runs without a tenant_id predicate; it computes total messages across tenants.
  - _Fix:_ If you need per-tenant totals, add WHERE tenant_id = ${tenantId} or call messages count per-tenant. If a global metric is required for operators, ensure the probe runs under an explicit admin-only code path and documents that it intentionally aggregates across tenants; prefer an explicit admin-context guard rather than implicit cross-tenant queries.
