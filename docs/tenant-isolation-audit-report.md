# Tenant-Isolation Audit Report

_Generated 2026-07-30T16:14:15.681Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **576/576** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **294** (CRITICAL 74 · HIGH 181 · MEDIUM 39 · LOW 0)
- Precision second-pass (`gpt-5.4`): **187** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (74)

- **server/agentic-engines.ts:243** — UPDATE ai_insights SET ... WHERE status = 'new' AND category IN (...) updates tenant-scoped rows for ALL tenants (no tenant_id constraint), potentially auto-applying insights across tenants.
  - _Fix:_ Do not perform cross-tenant UPDATEs. Either add a tenant_id = ${tenantId} predicate (and call per-tenant), or explicitly document and restrict to an operator-run single-tenant context. Ensure any bulk sweep only touches a single tenant at a time.
- **server/agentic/wake-scheduler.ts:142** — runDueWakes uses an UPDATE that selects rows via an inner SELECT without constraining by tenant_id. The subquery SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... can return rows from any tenant, so the outer UPDATE (which updates those ids) may move/claim schedules belonging to other tenants.
  - _Fix:_ Restrict the inner SELECT (and/or the outer UPDATE) to a specific tenant. Either run the claim per-tenant (SELECT ... WHERE tenant_id = <tenantId> ...) or add tenant_id checks to the outer UPDATE (e.g. WHERE id IN (...) AND tenant_id = ${tenantId}). Ensure the scheduling loop processes each tenant in isolation so rows can never be claimed/updated across tenant boundaries.
- **server/auto-memorize.ts:47** — getWatermark reads agent_knowledge rows for the auto_memorize watermark with no tenant scoping (SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ...). This returns a global value; later setWatermark writes an entry with tenant_id hardcoded to 1, meaning the watermark is stored on the admin tenant and shared globally rather than per-tenant.
  - _Fix:_ Make the watermark per-tenant. Include tenant_id in both the SELECT and INSERT/UPDATE (e.g. WHERE tenant_id = ${tenantId} when reading, and use VALUES(..., ${tenantId}, ...) when writing). If a global watermark is intended, use a genuine global config table (without tenant_id) and document it; otherwise fail-closed and scope the watermark by tenant to avoid cross-tenant watermarking and cross-tenant windowing.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` omits a tenant filter. The query `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...` does not include `tenant_id = ${tenantId}` — relying on earlier project verification is not allowed by the invariant (every DB access to tenant-scoped tables must include an explicit tenant constraint).
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. `... WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ...`) or join to a tenant-scoped parent in the same SQL and include the tenant filter. Do not rely solely on a previous verification step elsewhere — each query must enforce tenant_id in its WHERE clause.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` omits a tenant filter. The query `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...` lacks `tenant_id = ${tenantId}` and therefore can return cross-tenant rows if `project_id` is misused.
  - _Fix:_ Constrain the query with the tenant id (e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`) or join against `projects` and assert `projects.tenant_id = ${tenantId}` in the same SQL. Ensure every access to tenant-scoped tables includes an explicit tenant filter.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table `compaction_archives` omits a tenant filter. The query `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...` does not include `tenant_id = ${tenantId}` (even though `c.id` was obtained from a prior tenant-scoped query).
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}`) or perform a join to `conversations` in the same query and filter by `conversations.tenant_id = ${tenantId}`. Never rely on earlier lookups alone — each access must include the tenant filter.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` omits a tenant filter. The query `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC` lacks `tenant_id = ${tenantId}` and can expose messages across tenants if IDs are misused.
  - _Fix:_ Constrain the query with the tenant id (e.g. `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}`) or join to `conversations` and include `conversations.tenant_id = ${tenantId}` in the same SQL. Ensure every read against tenant-scoped tables includes an explicit `tenant_id` WHERE clause rather than relying on prior validation.
- **server/chat-engine.ts:4332** — Call to storage.attachCitations(...) omits any tenant context. If attachCitations writes to a tenant-scoped table (likely), this performs an INSERT/UPDATE without a tenant_id filter/column value, violating the invariant that every write to tenant-scoped tables must include tenant_id.
  - _Fix:_ Change attachCitations to accept and enforce tenant context (e.g. storage.attachCitations(messageId, citations, tenantId)), and pass the current tenant (tenantIdForScope or tenantId) from the call site. Inside storage.attachCitations, ensure SQL uses a WHERE tenant_id = ? (or Drizzle .where(eq(table.tenantId, tenantId))) and/or sets tenant_id on INSERTs. Also defensively verify the messageId belongs to the same tenant before attaching citations to prevent cross-tenant association.
- **server/commitment-drafter.ts:234** — UPDATE without tenant constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — mutates a row by id with no AND tenant_id = ${tenantId}, allowing accidental cross-tenant modification.
  - _Fix:_ Always include tenant scoping on writes to tenant-scoped tables. Change the statement to include AND tenant_id = ${tenantId} (and any draft_status guard as appropriate), e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Prefer using transactions that verify ownership before mutating.
- **server/data-protection.ts:131** — DELETE statements in permanentlyPurgeSoftDeleted() delete tenant-scoped rows without tenant_id predicates: DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}. These destructive writes lack tenant scoping and can delete rows across tenants if IDs collide or schema changes, violating tenant isolation and causing data loss.
  - _Fix:_ Include tenant scoping in all destructive statements. For example: DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}; DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Alternatively run the purge inside withTenantTx(conv.tenant_id, ...) to bind tenant context and/or use RLS enforcement so a missing WHERE cannot delete other tenants' data.
- **server/delivery-pipeline.ts:195** — updateDeliveryLog performs UPDATE on tenant-scoped table `delivery_logs` without constraining by tenant_id (where(eq(deliveryLogs.id, id))). This allows modifying another tenant's delivery row if given an id from a different tenant.
  - _Fix:_ Require a tenantId (or verify ownership) in updateDeliveryLog and include it in the WHERE (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))). Alternatively, have callers validate tenant ownership before calling and document the requirement; prefer passing tenantId explicitly to the helper to avoid accidental cross-tenant writes.
- **server/embeddings.ts:684** — UPDATE to tenant-scoped tables (memory_entries/agent_knowledge) performed without constraining by tenant_id. storeEmbeddingVec(table, id, ...) issues an UPDATE WHERE id = ${id} but does not include AND tenant_id = <tenant> — this allows cross-tenant modification.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and enforce it in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} or use Drizzle: .where(eq(table.tenantId, tenantId), eq(table.id, id))). Do not perform updates by id alone on tenant-scoped tables.
- **server/embeddings.ts:687** — SELECT from agent_knowledge in backfillMissingKnowledgeEmbeddings is not tenant-scoped: `SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ...` has no tenant_id filter. This will read rows across all tenants.
  - _Fix:_ Run backfills per-tenant (accept a tenantId parameter) or explicitly constrain the query to the intended tenant (AND tenant_id = ${tenantId}). If the intent is a system/global backfill, document and enforce admin-only execution and limit side-effects to rows belonging to a chosen tenant or perform tenant-by-tenant iteration.
- **server/embeddings.ts:716** — backfillEmbeddingVecs reads and updates memory_entries without tenant scoping — SELECT (lines ~716-720) and UPDATE (line ~726) use WHERE id = ${row.id} but no tenant_id filter. This can update embedding_vec for rows that belong to other tenants.
  - _Fix:_ Add tenant scoping to the SELECTs and UPDATEs (either accept a tenantId and filter every SELECT/UPDATE by tenant_id, or iterate per-tenant). Ensure UPDATE statements include AND tenant_id = ${tenantId} (or use Drizzle eq on the tenantId column) to prevent cross-tenant writes.
- **server/embeddings.ts:732** — backfillEmbeddingVecs reads and updates agent_knowledge without tenant scoping — SELECT (lines ~732-737) and subsequent UPDATE (line ~743) lack tenant_id filters, allowing cross-tenant reads/updates.
  - _Fix:_ Same fix as for memory_entries: make backfillEmbeddingVecs tenant-aware (accept tenantId or perform per-tenant iteration) and add WHERE tenant_id = ${tenantId} to both SELECT and UPDATE operations. Avoid updates keyed only by id for tenant-scoped tables.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` (SET status = 'archived_stale') uses id = ANY(...) but does not include tenant_id in the WHERE clause — this can modify rows for other tenants.
  - _Fix:_ Restrict the UPDATE to the tenant scope: e.g. UPDATE code_proposals SET ... WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}. Better: derive the list of ids per-tenant (or accept tenantId / run per-tenant) and never perform cross-tenant updates without explicit admin/operational safeguards.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a write against a tenant-scoped table without constraining by tenant_id — this can disable tasks for other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or resolve and validate task ownership before disabling. Prefer accepting a tenantId and only mutating rows belonging to that tenant, or iterate tenants and apply per-tenant changes.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a tenant-scoped table by id only, without verifying the session belongs to the caller's tenant or adding a tenant_id filter — a forged sessionId could permit cross-tenant modification.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or query the session first and validate session.tenant_id === tenantId before performing the update. Prefer defensive checks rather than relying on upstream service correctness.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} — no tenant_id constraint. This can modify another tenant's schedule row identified only by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or route updates through a storage helper that asserts the task's tenant before updating.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id} — missing tenant_id constraint, allowing cross-tenant writes.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or use a tenant-aware storage API that enforces ownership.
- **server/heartbeat.ts:111** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} — no tenant_id constraint (cross-tenant write risk).
  - _Fix:_ Constrain by tenant (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage-layer function that enforces tenant ownership.
- **server/heartbeat.ts:119** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id in WHERE. Recovery path can update other tenants' rows.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or use a tenant-aware helper to persist the next_run_at.
- **server/heartbeat.ts:265** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — no tenant_id constraint. This directly mutates a tenant-scoped heartbeat_tasks row by id only.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use storage.updateHeartbeatTask which should assert tenant context before mutating rows.
- **server/heartbeat.ts:269** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant_id filter, permitting cross-tenant modification.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or perform this change via the tenant-aware storage API that validates ownership.
- **server/heartbeat.ts:2950** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint. This can alter another tenant's task by id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage API (updateHeartbeatTask) that enforces tenant ownership before updating approval_status.
- **server/heartbeat.ts:3402** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — repeated occurrence with no tenant_id filter, allowing cross-tenant mutation.
  - _Fix:_ Same fix: add AND tenant_id = ${tenantId} to the WHERE clause or use a tenant-aware storage helper to perform this update after asserting the task belongs to the caller's tenant.
- **server/internal-resolver.ts:93** — UPDATE event_log is executed without constraining to the event's tenant (no WHERE tenant_id = ...). This allows finalizing/altering events across tenants when event_log is a tenant-scoped table.
  - _Fix:_ Include tenant scoping in the CAS update. Either read the event row first to obtain/verify its tenant_id and then UPDATE WHERE id = ${eventId} AND tenant_id = ${tenantId} AND status IN (...), or change finalizeEvent to accept a tenantId and add AND tenant_id = ${tenantId} to the WHERE clause to enforce per-tenant isolation.
- **server/internal-resolver.ts:107** — SELECT FROM plans by id without any tenant_id filter. Reading a tenant-scoped plans row by id only can expose or allow actions on plans belonging to other tenants.
  - _Fix:_ Require and use tenant context when resolving a plan. SELECT the plan with WHERE id = ${planId} AND tenant_id = ${tenantId} (or fetch the plan first and verify its tenant_id equals the caller/event tenant) and fail/abort if ownership does not match.
- **server/internal-resolver.ts:170** — UPDATE code_proposals transitions a proposal to an archival status by id but lacks a tenant_id constraint. This can archive or modify proposals belonging to other tenants.
  - _Fix:_ Constrain the UPDATE to the owning tenant, e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending', or verify the proposal's tenant ownership before performing the UPDATE. Ensure the resolver has the tenant context from the event and uses it in all mutating queries.
- **server/lib/action-ledger.ts:305** — UPDATE on tenant-scoped table `action_attempts` performed without any tenant_id predicate (sweepStaleAttempts updates rows across tenants).
  - _Fix:_ Restrict the UPDATE to a single tenant (ADD `AND tenant_id = ${tenantId}`) or iterate per-tenant: select stale ids grouped by tenant_id, then perform tenant-scoped transitions (or call transition()/markReconcilerDigested with each row's tenant). Avoid a cross-tenant UPDATE on a tenant-scoped table.
- **server/lib/agent-trace.ts:129** — UPDATE on tenant-scoped table `agent_trace_spans` (persistSpanClose) uses only `WHERE span_id = ${spanId}` with no tenant_id predicate — allows an update to touch another tenant's span if span_id is known or collides.
  - _Fix:_ Include tenant scoping in the UPDATE (add `AND tenant_id = ${tenantId}`) or capture/preserve the tenantId at span-open time and pass it into persistSpanClose. Avoid relying on span_id alone for cross-tenant isolation.
- **server/lib/scheduled-post-runner.ts:319** — Manually builds a Postgres text[] literal (arrLit) from caller-supplied platforms and interpolates it into SQL. This is string-interpolated SQL using request-derived values (SQL injection + tenant/isolation risk).
  - _Fix:_ Do not construct SQL fragments from untrusted input. Use parameterized queries or Drizzle/pg array binding to pass arrays safely (e.g. pass the JS array as a query parameter/JSON and cast to text[] on the server side). If you must embed literals, strictly whitelist and validate each platform value before embedding, or use sql.raw only on fully validated/allowlisted content.
- **server/memory-graph.ts:210** — INSERT INTO memory_links omits tenant_id entirely. Tenant-scoped tables must have tenant_id set on INSERT to prevent cross-tenant poisoning and orphan links.
  - _Fix:_ Include tenant_id as a NOT NULL column in the INSERT (VALUES (..., ${tenantId}, ...)), and ensure the caller provides/authorizes tenantId. If the function can be called only within a tenant context, accept tenantId explicitly and use it in the INSERT.
- **server/minerva-planner.ts:356** — The CAS UPDATE on plans (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING id, tenant_id, objective, plan_json) does not include tenant_id in the WHERE. This allows changing a plan record by id regardless of tenant, enabling cross-tenant authorization bypass.
  - _Fix:_ Require and validate the caller's tenantId and include AND tenant_id = ${tenantId} in the WHERE clause of the CAS update. If the operation must be callable from a trusted internal context, make that explicit and keep an internal-only codepath; do not expose an unscoped public API.
- **server/minerva-planner.ts:373** — Probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} runs without a tenant_id filter. Reading a tenant-scoped plans row by id without tenant scoping can leak or act on other tenants' state.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT or require tenantId as an explicit argument and validate caller ownership before probing plan state.
- **server/minerva-planner.ts:390** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 executes without tenant scoping. This can return a child plan from a different tenant and be used to make cross-tenant control-flow decisions.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}) or require/validate tenantId before performing the lookup.
- **server/minerva-planner.ts:459** — Rollback UPDATE on plans (UPDATE plans SET status='awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising') lacks tenant_id constraint. This can roll back or alter plans for other tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause and ensure tenantId is validated/required for this operation. Differentiate internal-trusted flows from external calls explicitly so untrusted callers cannot invoke unscoped updates.
- **server/oauth-subscriptions.ts:906** — SELECT queries all active oauth_subscriptions rows without scoping by tenant_id (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE). This reads subscriptions (access/refresh tokens) across tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly iterate only known tenant IDs with operator-level privileges. Avoid blanket queries over oauth_subscriptions in multi-tenant deployments unless run from a privileged/admin-only context with explicit tenant scoping.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — update omits tenant_id guard (write can affect other tenants' rows)
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or use a parameterized API that ensures the task belongs to the tenant before mutating it.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} — missing tenant_id constraint (write may modify another tenant's desk)
  - _Fix:_ Require tenant scoping on the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or verify ownership before performing the update.
- **server/process-governor.ts:977** — UPDATE governance_rules SET ... WHERE id = ${rule.id} — update lacks tenant_id constraint (mutates governance rule potentially belonging to another tenant)
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (or pass a safeRuleId + safeTenantId) so only the tenant's governance rule is modified.
- **server/proposal-verifier.ts:111** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at etc.) omits tenant_id constraint — write may affect another tenant's proposal
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE (or verify ownership up-front) to ensure modifications apply only to the tenant's proposal.
- **server/proposal-verifier.ts:396** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking 'hallucinated') lacks tenant scoping
  - _Fix:_ Restrict the UPDATE with AND tenant_id = ${tenantId} (or otherwise ensure proposal belongs to the caller) before updating validation_result.
- **server/proposal-verifier.ts:373** — UPDATE code_proposals SET status = 'rejected', verification_status = 'skipped' WHERE id = ${proposalId} — missing tenant_id (mutation without tenant scoping)
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or require/validate tenant context so only the tenant's proposal record is changed.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} — write lacks tenant_id constraint
  - _Fix:_ Include AND tenant_id = ${tenantId} on the UPDATE (and ensure attemptRebaseOnDrift is provided a tenant context) to avoid cross-tenant mutation of proposals.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge SET ... WHERE id = ${r.id} executes without restricting tenant_id — risk of updating rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. add AND tenant_id = ${r.tenant_id} to the WHERE clause (or pass and check the tenant id explicitly). Prefer using the tenant-aware helper that returns rows only for the target tenant so updates can safely use id + tenant_id together.
- **server/reference-learner.ts:290** — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} is executed without tenant_id constraint — allows modifying another tenant's memory entry.
  - _Fix:_ Add tenant scoping to the UPDATE, for example: WHERE id = ${storedId} AND tenant_id = ${input.tenantId}. Always include tenant_id on UPDATE/DELETE for tenant-scoped tables, and validate ownership before modifying rows.
- **server/research-engine.ts:163** — UPDATE research_sessions SET ... WHERE status = 'running' ... started_at < ${staleThreshold} executes with no tenant_id constraint — may mark sessions for other tenants as completed.
  - _Fix:_ Limit the cleanup to a single tenant at a time (accept tenantId param) or iterate tenants and run the cleanup per-tenant with WHERE tenant_id = ${tenantId}. Never perform cross-tenant UPDATEs on tenant-scoped tables without an explicit, auditable, and intentionally-scoped system-level gate.
- **server/routes.ts:2057** — Public presenter endpoint selects presenter_sessions by token without scoping by tenant (SELECT ... FROM presenter_sessions WHERE token = ${token}). This returns tenant-scoped row(s) without verifying the caller's tenant context.
  - _Fix:_ Bind the capability to tenant at creation and validate tenant ownership when reading: include tenant_id in the WHERE clause (WHERE token = ${token} AND tenant_id = ${expectedTenantId}) or embed/verifiably bind tenant id into the token HMAC/payload so the server can validate tenant before returning session data. At minimum, ensure the token creation and verification produce an explicit tenant that is enforced server-side before reading the row.
- **server/routes.ts:4331** — INSERT into tenant-scoped table 'project_notes' that does not set tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, risking notes being written without a tenant association or associated with the wrong tenant.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot...}, ${'system:context-guard'})`). Additionally validate that the project_id belongs to the caller's tenant before inserting (or use a tenant-scoped storage helper that enforces tenant assignment).
- **server/routes/projects.ts:185** — INSERT INTO project_notes (project_id, note, author) does not set tenant_id. Tenant-scoped table inserts must include tenant_id to prevent cross-tenant writes.
  - _Fix:_ Include tenant_id in the INSERT columns and pass the validated tenantId (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). If tenant_id should be derived, validate ownership of project_id before inserting.
- **server/routes/projects.ts:224** — INSERT INTO project_files (project_id, file_name, ...) omits tenant_id. Inserting into a tenant-scoped table without tenant_id allows cross-tenant contamination.
  - _Fix:_ Add tenant_id to the INSERT and supply the validated tenantId (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Validate project ownership before insert.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} lacks tenant_id in the WHERE clause. Updating a tenant-scoped row without tenant filtering can modify other tenants' data.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where(eq(projects.tenantId, tenantId))). Always include tenant scope on updates.
- **server/routes/projects.ts:292** — INSERT INTO project_files (project_id, file_name, ...) (multipart upload) does not include tenant_id. Tenant-scoped INSERT missing tenant identifier.
  - _Fix:_ Include tenant_id in the INSERT values (tenant_id = ${tenantId}) and ensure the tenantId is derived from the authenticated session, not from any client input.
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (after multipart upload) omits tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} so only the caller's project row is modified.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not constrain by tenant_id. Deleting from a tenant-scoped table without tenant filtering may delete another tenant's data.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the DELETE WHERE clause (or ensure project ownership is validated and include tenant_id explicitly). Always include tenant scoping on destructive operations.
- **server/sculptor.ts:93** — UPDATE on tenant-scoped table sculptor_sessions without constraining by tenant_id (WHERE id = ${session.id} only). This allows cross-tenant mutation if id is guessed or tampered with.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. AND tenant_id = ${params.tenantId} or WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Prefer using Drizzle .where(eq(table.tenantId, tenantId)) to enforce ownership.
- **server/sculptor.ts:100** — UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id} — missing tenant_id constraint AND the code writes a foreign conversation_id derived from an external run without verifying that the conversation belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Before writing a foreign id (conversation_id), verify the referenced conversation row is owned by the tenant (SELECT c.id FROM conversations c WHERE c.id = ${runResult.conversationId} AND c.tenant_id = ${params.tenantId}) and fail if not.
- **server/sculptor.ts:155** — UPDATE sculptor_sessions (monitorSession final update) uses WHERE id = ${sessionId} with no tenant_id constraint. monitorSession receives tenantId but does not assert it in the UPDATE — risk of cross-tenant modification.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) so only the owning tenant's row can be updated.
- **server/sculptor.ts:346** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} — missing tenant_id constraint. reviewSessionWork earlier authenticated tenant ownership for the SELECT, but the later UPDATE does not re-assert tenant scoping.
  - _Fix:_ Include tenant ownership in the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Alternatively perform the UPDATE via a JOIN or use a single transactional flow that enforces tenant_id to avoid TOCTOU issues.
- **server/second-opinion.ts:404** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} — no tenant_id constraint. settleFusionReservation updates a tenant-scoped ledger row by id only, allowing modification of other tenants' rows if ids are misused.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Propagate tenantId into settleFusionReservation or re-derive the tenant via a safe SELECT before updating to ensure ownership before mutating.
- **server/second-opinion.ts:423** — UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId} — missing tenant_id constraint. releaseFusionReservation can affect rows from other tenants by id alone.
  - _Fix:_ Require and enforce tenant ownership in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or look up the tenant for the reservationId before updating. Make sure reservationId cannot be used to modify another tenant's ledger row.
- **server/seed-cleanup.ts:15** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true — no tenant_id constraint. This will disable delegation tasks across all tenants rather than a single intended tenant.
  - _Fix:_ Limit the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or pass an explicit tenantId). If the intention is global, make that explicit and document why; otherwise always scope writes to tenant_id.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true — missing tenant_id constraint. This can disable session-logger tasks across tenants indiscriminately.
  - _Fix:_ Add tenant filter to the UPDATE (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or pass tenantId) to ensure only the intended tenant's tasks are modified.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true — missing tenant_id constraint. This will disable many heartbeat task rows across all tenants rather than a specific tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or accept a tenantId argument). If the intent is to modify only admin-scoped rows, explicitly include tenant_id = ${ADMIN_TENANT_ID} in both the SELECT and UPDATE to enforce isolation.
- **server/seed.ts:3097** — UPDATE on tenant-scoped table `research_programs` lacks tenant constraint: `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}` updates a row by id without verifying tenant ownership (no `tenant_id = ...`), risking cross-tenant modification.
  - _Fix:_ Include an explicit tenant constraint in the UPDATE (e.g. `WHERE id = ${p.id} AND tenant_id = ${tenantId}`) or first verify the row belongs to the caller's tenant before updating. Prefer Drizzle .where(eq(table.id, id), eq(table.tenantId, tenantId))). Ensure tenantId is validated/trusted.
- **server/seed.ts:4686** — DELETE FROM heartbeat_tasks is executed without a tenant_id constraint: `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'`. This can delete rows across all tenants.
  - _Fix:_ Add a tenant_id constraint to the DELETE (e.g. `AND tenant_id = $1`) or otherwise ensure the delete only affects rows belonging to the current tenant. Deletion across tenants is high-risk; prefer explicit scoping or a tenant-aware delete helper.
- **server/self-improvement.ts:394** — runExperiment updates the `experiments` row with .where(eq(experiments.id, experiment.id)) but does NOT constrain the UPDATE by tenant_id. This allows updating another tenant's experiment by id.
  - _Fix:_ Include tenant scoping on UPDATE: add .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) or fetch & verify the experiment belongs to the caller's tenant before updating. Prefer fail-closed if tenant mismatch.
- **server/self-improvement.ts:413** — Error-path UPDATE also updates `experiments` with .where(eq(experiments.id, experiment.id)) without a tenant_id constraint, enabling cross-tenant mutation in error handling path.
  - _Fix:_ Apply the same tenant scoping on the error-path UPDATE (AND tenant_id = caller tenant), or verify experiment.tenantId before performing this update. Never update tenant-scoped rows by id only.
- **server/storage.ts:1296** — getAllDataForExport SELECTs many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant-scoped provider keys, heartbeat_tasks, deliveryLogs, etc.) with NO tenant_id filters — returns full platform-wide data dump (all tenants).
  - _Fix:_ This is a high-risk operation and must be restricted. Require an explicit tenantId parameter and validate it, or gate the function behind an admin-only, auditable path. Prefer exporting only per-tenant data (WHERE tenant_id = ${tenantId}) unless a well-audited multi-tenant export is explicitly required and protected.
- **server/tools.ts:2981** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id, violating the requirement that every INSERT into tenant-scoped tables include the caller's tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) ) and/or derive tenant_id from the validated project owner. Ensure tenant_id column is provided and non-null on every insert to tenant-scoped tables.
- **server/tools.ts:2996** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId}) ) or otherwise ensure the row is written with the caller's tenant. Validate ownership of the project before writing and persist tenant_id explicitly.
- **server/tools.ts:2878** — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The code INSERTs (project_id, conversation_id) without recording tenant_id.
  - _Fix:_ Persist tenant_id on project_conversations inserts (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId}) ). Alternatively, join against a tenant-scoped parent and enforce tenant_id via the insert path, but always ensure tenant_id is stored for tenant-scoped rows.
- **server/tools.ts:3020** — INSERT into tenant-scoped table `project_conversations` (link endpoint) omits tenant_id. INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) has no tenant_id column set.
  - _Fix:_ Include tenant_id in the VALUES (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId}) ). Ensure every path that writes to project_conversations records the caller tenant explicitly.

### HIGH (181)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (reads experiments across all tenants).
  - _Fix:_ Scope the query to a tenant (e.g. accept a tenantId param and add WHERE tenant_id = ${tenantId}) or iterate per-tenant. Never SELECT tenant-scoped rows unfiltered across tenants.
- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` uses only the activity id (WHERE id = ...) with no tenant_id constraint — allows cross-tenant updates.
  - _Fix:_ Require the caller supply tenantId (or resolve ownership first) and include it in the WHERE clause (WHERE id = ${activityId} AND tenant_id = ${tenantId}), or verify row ownership before updating.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting is unscoped (no tenant_id filter) — reads pending high-priority insights across all tenants.
  - _Fix:_ Scope the sweep per-tenant or add a tenant_id = ${tenantId} predicate. If global sweep is intended, make the multi-tenant implications explicit and ensure callers are authorized; otherwise run the operation per tenant.
- **server/agentic-engines.ts:344** — SELECT from tenant-scoped table `heartbeat_logs` (COUNT of calls) lacks a tenant_id WHERE clause — returns data across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) or otherwise restrict the query to the intended tenant before using the data in tenant-scoped recommendations.
- **server/agentic-engines.ts:451** — SELECT recent heartbeat logs (heartbeat_logs WHERE status='success') is missing tenant_id constraint — reading logs across tenants.
  - _Fix:_ Filter the query by tenant_id (e.g. WHERE status = 'success' AND tenant_id = ${tenantId}) or run per-tenant to avoid cross-tenant data access.
- **server/agentic-engines.ts:519** — Aggregated heartbeat performance query (FROM heartbeat_logs GROUP BY task_name, status) is not tenant-scoped — aggregates logs across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the query or otherwise aggregate per-tenant. Global aggregation of tenant-scoped logs violates tenant isolation.
- **server/agentic-engines.ts:533** — SELECT from tenant-scoped table `heartbeat_tasks` uses WHERE enabled = true but omits tenant_id — reads task config across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId} AND enabled = true) or perform per-tenant queries to avoid cross-tenant data leakage.
- **server/agentic-features.ts:525** — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} has no tenant_id constraint — allows updating another tenant's lead row by id.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or verify ownership before updating.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} is missing tenant_id constraint — updates a row by id only and can affect another tenant's enrollment.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or verify that the enrollment row belongs to the caller's tenant before updating.
- **server/agentic/approvals.ts:135** — Bulk UPDATE in expireStaleApprovals: db.update(agentApprovals).set(...).where(status='pending' AND expiresAt < now()) updates approvals across all tenants (no tenant_id predicate) and then acts on returned rows.
  - _Fix:_ Scope the expiry to a specific tenant (add tenant_id = ${tenantId}) or iterate per-tenant. Bulk cross-tenant updates can incorrectly change state for tenants the caller should not touch; ensure ownership checks before mutating tenant-scoped rows.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' scans a tenant-scoped table across all tenants with no tenant_id constraint. This reads tenant-scoped data for every tenant (cross-tenant access) rather than restricting to the caller's tenant.
  - _Fix:_ Either scope the query to a specific tenant (add a WHERE tenant_id = ${tenantId} parameter and accept tenantId as a caller arg), or make this an explicitly privileged admin operation with documented, audited intent. If the sweep must iterate all tenants, perform an explicit, auditable per-tenant loop using a vetted admin/maintenance path (and avoid returning or leaking cross-tenant row contents), or move logic into a DB-side job run under a secure administrative account. In short: do not issue unscoped reads against tenant-scoped tables — add explicit tenant scoping or restrict to an admin-only, auditable flow.
- **server/agentic/resume-worker.ts:66** — Raw SELECT scans tenant-scoped tables (agent_runs JOIN agent_approvals) with NO tenant_id filter — returns rows across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId}) or drive the sweep per-tenant. If this worker must act across tenants, iterate tenants explicitly and include tenant_id in every query to enforce application-level isolation. Avoid broad cross-tenant SELECTs returning other tenants' data.
- **server/agentic/runs.ts:34** — UPDATE against tenant-scoped table agent_runs (appendStep) uses WHERE id = runId with NO tenant_id constraint — allows updating a run row from any tenant context.
  - _Fix:_ Require and pass the tenantId into appendStep, and add the tenant guard to the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Ensure callers supply/verify tenantId before invoking.
- **server/agentic/runs.ts:45** — UPDATE against tenant-scoped table agent_runs (updateRunState) uses WHERE id = runId with NO tenant_id constraint.
  - _Fix:_ Accept tenantId in updateRunState and include it in the WHERE clause (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) so state updates are scoped to the owning tenant.
- **server/agentic/runs.ts:54** — UPDATE against tenant-scoped table agent_runs (replaceRunState) uses WHERE id = runId with NO tenant_id constraint.
  - _Fix:_ Add a tenantId parameter and constrain the update with .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all call sites pass and verify the tenant.
- **server/agentic/runs.ts:58** — UPDATE against tenant-scoped table agent_runs (completeRun) uses WHERE id = runId with NO tenant_id constraint.
  - _Fix:_ Include the tenant filter in the completion update (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and require callers to provide tenantId, so marking a run completed cannot affect other tenants' rows.
- **server/agentic/runs.ts:88** — UPDATE against tenant-scoped table agent_runs (failRun) uses WHERE id = runId with NO tenant_id constraint.
  - _Fix:_ Constrain the update by tenant (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and change the API to accept/propagate tenantId to prevent cross-tenant mutation.
- **server/agentic/runs.ts:97** — UPDATE against tenant-scoped table agent_runs (pauseRun) uses WHERE id = runId with NO tenant_id constraint.
  - _Fix:_ Require tenantId and add it to the WHERE clause (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Audit all callers (including resume-worker) to pass/verify the tenant context.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id ... WHERE id = ${c.conversation_id} ... executes without scoping by tenant_id. This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL) so the update cannot modify rows belonging to other tenants.
- **server/auto-transcript.ts:179** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is performed using project_id taken from conversations rows without verifying that the referenced project is owned by the same tenant. This can create cross-tenant links.
  - _Fix:_ Before inserting, verify the project belongs to the conversation's tenant (e.g. SELECT 1 FROM projects WHERE id = ${r.project_id} AND tenant_id = ${r.tenant_id}) and only insert when that check passes. Alternatively include tenant-aware checks or store tenant_id on the join and enforce it in the INSERT WHERE/SELECT clause.
- **server/backup.ts:176** — Call to storage.getMemoryEntries(undefined, 200, 0) omits a tenant argument, which may return memories across tenants (reading tenant-scoped data without scoping by tenant).
  - _Fix:_ Pass an explicit tenantId when calling storage.getMemoryEntries, or otherwise restrict getRecentMemoryChanges to operate per-tenant or in an explicitly elevated/admin path. Document and gate any global exports so tenant data is not unintentionally aggregated across tenants.
- **server/capability-review.ts:491** — UPDATE to tenant-scoped table `capability_reviews` is executed without constraining by tenant_id. markReviewReused() runs `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` but does not include a tenant_id check, violating the tenant-isolation invariant.
  - _Fix:_ Ensure every write to tenant-scoped tables includes the tenant filter. Record the tenantId when creating PendingReuse (e.g. store tenantId in PendingReuse) and change the UPDATE to include AND tenant_id = ${pending.tenantId}. Alternatively, SELECT the row first and verify its tenant_id matches the caller's tenant before performing the UPDATE. Do not rely on in-memory maps alone for DB-level tenant scoping.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant filter: the heartbeat scanner queries rows across all tenants (`FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`) and thus reads tenant-scoped data without explicit tenant scoping.
  - _Fix:_ If this is intended admin work, ensure the code runs only in an explicit admin/trusted context and document/guard it accordingly. Otherwise restrict the SELECT to a specific tenant by adding `AND tenant_id = ${tenantId}` or require an explicit tenant parameter. If admin-level cross-tenant scanning is needed, minimize the selected columns (avoid free-text) and ensure strong admin authorization controls are enforced at the call site.
- **server/credential-vault.ts:169** — UPDATE without tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` updates a credential row by id with no tenant filter, permitting cross-tenant mutation.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. `WHERE id = ${id} AND tenant_id = ${tenantId}`) so only the owning tenant's row can be modified. Ensure the tenantId is validated and passed into this function and used in the WHERE clause.
- **server/credential-vault.ts:176** — UPDATE without tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` updates last_used_at with no tenant check after getCredentialForSite returned a row — this mutates by id without verifying tenant scope in the UPDATE.
  - _Fix:_ Change the UPDATE to include tenant scoping (e.g. `WHERE id = ${cred.id} AND tenant_id = ${tenantId}`), and ensure the function receives/uses the tenantId that was used to fetch the credential. Always include tenant_id in WHERE clauses for writes to tenant-scoped tables.
- **server/data-protection.ts:19** — memory_categories table created with tenant_id INTEGER DEFAULT 1. This provides a global default tenant and violates the invariant that tenant_id must be explicitly supplied by the application on INSERT (columns should be NOT NULL with no tenant-default).
  - _Fix:_ Remove the DEFAULT 1 on tenant_id. Require application code to pass tenant_id explicitly and make tenant_id NOT NULL (with a migration to backfill existing rows safely). If a migration is needed, backfill rows to their correct tenant values before setting NOT NULL and removing the default.
- **server/data-protection.ts:48** — ALTER TABLE agent_knowledge ADD COLUMN IF NOT EXISTS tenant_id INTEGER DEFAULT 1. Adding a tenant_id column with a default of 1 can cause new rows to be implicitly attributed to tenant 1 instead of the caller's tenant, breaking tenant isolation and the rule that INSERTs must provide tenant_id explicitly.
  - _Fix:_ Remove the DEFAULT 1. Add tenant_id as a required column with no default and update all code paths that insert into agent_knowledge to explicitly pass tenant_id. Provide a migration to correctly populate tenant_id for existing rows and then add NOT NULL (or otherwise enforce ownership) rather than relying on a default.
- **server/data-protection.ts:117** — SELECT FROM conversations in permanentlyPurgeSoftDeleted() (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) runs without constraining tenant_id. Reading tenant-scoped rows without a tenant predicate can expose data across tenants (and is inconsistent with the stated application-level isolation model).
  - _Fix:_ If this is intended to be a multi-tenant-wide admin purge, make the operation explicit and run it from a privileged admin context (e.g. withTenantTx/admin role) instead of ad-hoc reads across tenants — document and guard it. Otherwise, run per-tenant (loop over tenants) and include WHERE tenant_id = <tenant> in the SELECT. Prefer using withTenantTx to bind tenant context for the transaction so RLS/enforcement modes (if enabled) can help.
- **server/delivery-pipeline.ts:1049** — getDeliveryByStripePayment selects from tenant-scoped table `delivery_logs` filtering only on stripe_payment_id and not on tenant_id. A caller can use a payment id to retrieve another tenant's delivery row.
  - _Fix:_ Require a tenant parameter (or otherwise validate tenant ownership) and include tenant_id in the WHERE clause (e.g. WHERE stripe_payment_id = $1 AND tenant_id = $2). If global admin lookups are intended, make this explicit and restrict access to admin-only code paths.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken selects from tenant-scoped table `dfy_intake_forms` by token only, with no tenant constraint. Tokens may be unguessable but lack of tenant scoping can allow cross-tenant access to intake forms.
  - _Fix:_ Scope the lookup to the tenant when tenant context is available, or add a tenantId parameter to the function and include AND tenant_id = ${tenantId} in the WHERE. If the token must be globally resolvable, audit and document the decision and ensure tokens are unique across tenants and considered safe.
- **server/dfy-intake.ts:93** — submitDfyIntake updates `dfy_intake_forms` with .where(eq(dfyIntakeForms.id, form.id)) but does not include tenant_id in the WHERE. This allows updating a form row belonging to another tenant if an id is used from a different tenant.
  - _Fix:_ Include tenant scoping in the UPDATE: .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantIdOrProvidedTenant))). Better: fetch the form using a tenant-scoped query and/or pass tenantId into submitDfyIntake and enforce it on the UPDATE to guarantee ownership.
- **server/felix-brain.ts:19** — rehydrateTaskStateOnBoot executes a raw SELECT on agent_runs without constraining by tenant_id. This pulls rows for all tenants into process memory (tenant_id is selected but not used to scope the query), violating tenant isolation.
  - _Fix:_ Restrict the query to a single tenant or iterate per-tenant and include a WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(agentRuns.tenantId, tenantId))). Alternatively make this an admin-only, explicitly-scoped routine and keep in-memory state keyed by tenant+conversationId so rows from other tenants are never loaded into a tenant's context.
- **server/firecrawl.ts:214** — UPDATE scraped_pages uses WHERE eq(scrapedPages.id, existing[0].id) but omits tenant scoping (tenant_id). This is a write against a tenant-scoped table without a tenant_id constraint in the WHERE clause.
  - _Fix:_ Include the tenant constraint in the update WHERE clause, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Use Drizzle eq(scrapedPages.tenantId, tenantId) to ensure the update is limited to the caller's tenant.
- **server/firecrawl.ts:344** — Within the crawl loop, UPDATE scraped_pages is executed with WHERE eq(scrapedPages.id, existing[0].id) but no tenant_id condition. This writes to a tenant-scoped table without constraining the tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so updates cannot affect rows outside the caller's tenant.
- **server/google-drive.ts:547** — SELECT ... FROM projects WHERE eq(projects.id, projectId) reads a tenant-scoped table without a tenant_id filter. Although ownership is checked in application logic afterwards, the raw SELECT itself has no tenant constraint and may expose tenant-scoped data.
  - _Fix:_ Constrain the SELECT at SQL time by adding tenant_id to the WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))). Prefer failing-closed in SQL rather than relying solely on post-query application checks.
- **server/google-drive.ts:560** — UPDATE projects SET driveFolderId/driveFolderUrl ... WHERE eq(projects.id, projectId) updates a tenant-scoped table without including tenant_id in the WHERE clause, violating the requirement that each write be tenant-constrained in SQL.
  - _Fix:_ Add the tenant constraint to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the write cannot affect a project belonging to a different tenant.
- **server/google-drive.ts:826** — In uploadAndShare, code SELECTs project row with WHERE eq(projects.id, params.projectId) but no tenant_id filter. This reads a tenant-scoped table without SQL-level scoping before making decisions based on the row.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId || callerTenantId)))) or call an assertProjectInTenant helper before any DB access; prefer doing the tenant check in the SQL predicate itself.
- **server/google-drive.ts:1004** — searchDriveFiles performs SELECT { tenantId: projects.tenantId } FROM projects WHERE eq(projects.id, params.projectId) with no tenant_id constraint. This is a read against the projects table without SQL-level tenant scoping (ownership is then checked in application code).
  - _Fix:_ Constrain the SELECT with the tenant in the WHERE clause (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or use a helper that atomically verifies project ownership in SQL before returning sensitive columns.
- **server/google-drive.ts:1066** — During Drive-API fallback the code SELECTs projects.driveFolderId WHERE eq(projects.id, params.projectId) without a tenant_id filter. This accesses a tenant-scoped table without SQL scoping before using the folder id to scope subsequent Drive searches.
  - _Fix:_ Add tenant constraint to the SELECT (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the Drive folder lookup cannot leak or be influenced by another tenant's project.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` without constraining by tenant_id (global read). This can leak other tenants' proposals to the audit job.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate tenants explicitly. Alternatively require an explicit admin mode and clearly document/guard global scans. Prefer adding a tenantId parameter to archiveStaleProposals / runFullAudit and use it in the SQL filter.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` lacks a tenant_id filter — reading tenant-scoped task rows across all tenants.
  - _Fix:_ Scope the SELECT to a tenant (WHERE tenant_id = ${tenantId}) or iterate tenants explicitly. If this audit is intended to be global, require an explicit admin/maintenance mode and document the authority; otherwise make the scanner per-tenant.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images does not set tenant_id (and uses only session_id). If presenter_slide_images is tenant-scoped, this INSERT can create rows not associated with the caller's tenant; additionally the code does not validate that session_id belongs to the caller's tenant.
  - _Fix:_ Either include tenant_id explicitly in the INSERT values, or resolve/validate the referenced session row belongs to the current tenant before inserting. If presenter_slide_images is keyed by session_id, still verify session ownership (SELECT tenant_id FROM presenter_sessions WHERE id = ${sessionId}) and fail if it doesn't match the caller's tenant.
- **server/heartbeat.ts:52** — SELECT * FROM research_schedules is executed with no tenant_id filter. This reads rows across all tenants from a tenant-scoped table (research_schedules).
  - _Fix:_ If research_schedules is tenant-scoped, query per-tenant (e.g. iterate active tenant IDs and SELECT … WHERE tenant_id = $tid) or add an explicit WHERE tenant_id = ${sched.tenant_id} (or equivalent). If the table is intended to be global, document that fact and ensure schema enforces it.
- **server/heartbeat.ts:686** — SELECT * FROM event_log WHERE status = 'pending' ORDER BY ... LIMIT 10 executes without a tenant_id constraint. Reading pending events across tenants without scoping can leak or mix tenant data.
  - _Fix:_ If the daemon must process events per-tenant, execute the SELECT per-tenant (e.g. loop getActiveTenantIds and SELECT … WHERE tenant_id = $tid) or add an explicit tenant_id filter. Document/confirm if event_log is intentionally global; otherwise enforce tenant scoping.
- **server/heartbeat.ts:1856** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ... has no tenant_id filter. This reads heartbeat_logs across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE task_name = … AND status = … AND tenant_id = ${tenantId}) or otherwise ensure the query runs in the intended admin/global context only if the table is genuinely global and safe to read unscoped.
- **server/lib/action-ledger.ts:272** — SELECT in listUnknownAttemptsAllTenants queries tenant-scoped table `action_attempts` without a tenant_id filter (cross-tenant read).
  - _Fix:_ If this is intended for a reconciler, ensure the function is clearly admin-only and that callers treat results safely; ideally change the API to return rows grouped by tenant_id or accept an explicit admin-only permission check. If not intended, add `WHERE tenant_id = ${tenantId}` to scope the query to the caller's tenant.
- **server/lib/bwb-job-progress.ts:141** — Updates to tenant-scoped table `video_jobs` (setBwbPhase uses .where(scope(jobId))) may omit a tenant_id predicate when BWB_TENANT_ID is missing — scope() falls back to jobId-only predicate, enabling potential cross-tenant writes.
  - _Fix:_ Require and pass an explicit tenantId for progress writes (fail when missing) or make scope() enforce tenant presence (throw or return no-op). Always include `tenantId` in the WHERE clause for tenant-scoped tables instead of relying on an unguessable jobId.
- **server/lib/bwb-job-progress.ts:166** — Updates to tenant-scoped table `video_jobs` (updateBwbChapters uses .where(scope(jobId))) may run without a tenant_id predicate if BWB_TENANT_ID is not threaded, allowing cross-tenant modifications keyed only by jobId.
  - _Fix:_ Same as above: require an explicit tenantId or make the writer fail/abort when the environment-threaded tenant is absent. Ensure all UPDATE/SELECT/DELETE against `video_jobs` include `tenant_id = ...`.
- **server/lib/bwb-job-progress.ts:211** — completeBwbJob reads and updates `video_jobs` using scope(jobId) which can omit tenant_id (scope may lack tenant when env var missing), enabling cross-tenant reads/updates.
  - _Fix:_ Ensure scope() always includes tenant filtering or require tenantId to be passed into completeBwbJob; perform per-tenant scoped SELECTs/UPDATEs and avoid relying on jobId-only predicates for tenant isolation.
- **server/lib/decline-events.ts:97** — Constructing a Postgres array literal (`flaggedLiteral`) from request-derived categories and injecting it into the SQL expression (`${flaggedLiteral}::text[]`) risks SQL/formatting issues and is effectively string-interpolated SQL based on user input.
  - _Fix:_ Avoid building SQL literals from user input. Use proper parameterized array binding (pass the array as a parameter or use the DB/Drizzle helper to bind arrays), or store flagged categories as jsonb (parameterized) rather than interpolating a textual array literal. If a literal must be used, strictly validate/encode elements and prefer driver-supported array construction to prevent injection and parsing errors.
- **server/lib/ideabrowser-backfill.ts:104** — INSERT into `project_files` does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (${newProjectId}, ${f.fileName}, ...)`. Per invariant, tenant-scoped tables require tenant_id to be passed explicitly (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). If the table is intended to be scoped only by project_id, explicitly store tenant_id as well and/or validate ownership by joining to projects to ensure the project belongs to the expected tenant before inserting.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into `project_notes` does not set tenant_id: `INSERT INTO project_notes (project_id, note, author, created_at) VALUES (${newProjectId}, ${n.note}, ...)`. This violates the invariant that INSERTs to tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Alternatively, verify the project's tenant ownership and add tenant_id to the row to enforce tenant isolation at application level.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` is performed without constraining by tenant_id (no WHERE tenant_id = ...). This lookup in verifyApiKey queries by key_prefix only, which reads across tenants.
  - _Fix:_ Avoid unconstrained reads on tenant-scoped tables. If the intent is to resolve the tenant from the key, make the intent explicit and enforce schema guarantees: (a) allocate key_prefix as globally-unique (DB UNIQUE constraint) so a prefix lookup cannot ambiguously reveal or collide across tenants, and document the design; or (b) require the caller to present an explicit tenant identifier and add `AND tenant_id = ${tenantId}` to the WHERE clause; or (c) perform a two-step, audited lookup that limits returned fields and rate-limits/guards prefix enumeration. At minimum, add comments, tighten indexing/uniqueness, and consider rate-limiting/enumeration hardening to prevent cross-tenant discovery.
- **server/lib/scheduled-post-runner.ts:61** — SELECT from tenant-scoped table `scheduled_posts` without any tenant_id constraint (system-level poll uses no tenant filter).
  - _Fix:_ If this must remain a multi-tenant system-run, make the rationale explicit and audit it; otherwise restrict the SELECT to a single tenant (WHERE tenant_id = ${tenantId}) or iterate tenants explicitly. Prefer explicit tenant scoping on all tenant-scoped table reads to avoid accidental cross-tenant exposure or modification.
- **server/lib/scheduled-post-runner.ts:135** — UPDATE against tenant-scoped table `scheduled_posts` uses WHERE id = ${row.id} only (no tenant_id filter).
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or otherwise ensure the update is scoped to the owning tenant to prevent accidental cross-tenant writes.
- **server/lib/scheduled-post-runner.ts:217** — UPDATE against tenant-scoped table `scheduled_posts` uses WHERE id = ${row.id} only (no tenant_id filter) after per-platform publish processing.
  - _Fix:_ Constrain the UPDATE by tenant_id as well (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or perform the update via a transaction that binds the tenant explicitly to avoid cross-tenant modification.
- **server/lib/scheduled-post-runner.ts:244** — UPDATE against tenant-scoped table `scheduled_posts` in exception-handling path uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${row.tenant_id}) so error/recovery paths cannot accidentally modify another tenant's row.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without any tenant_id constraint (global audit scan).
  - _Fix:_ If this is intended to be a global/system audit, document that explicitly and consider using a dedicated admin pathway. Otherwise, restrict the query to a tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. In all cases prefer explicit tenant scoping in SQL to avoid accidental cross-tenant disclosure.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without any tenant_id constraint (global audit scan).
  - _Fix:_ As above, either scope to a specific tenant or iterate tenants explicitly. Ensure all SELECTs on tenant-scoped tables include tenant_id = <caller tenant> unless there's a deliberate, reviewed system-wide reason and appropriate safeguards are in place.
- **server/lib/system-probes.ts:163** — Unconstrained SELECT against tenant-scoped table `messagesTable`: db.select(...).from(messagesTable) executes a cross-tenant count with no tenant_id filter.
  - _Fix:_ Constrain the query to the caller's tenant (or an explicitly-authorized admin context). Either accept/derive an explicit tenantId and add a WHERE tenant_id = ${tenantId} (or Drizzle .where(eq(messagesTable.tenantId, tenantId))). If this probe truly needs a global cross-tenant metric, make that intent explicit and gate it to an admin-only context (check privileges) and document why global access is required.
- **server/memory-graph.ts:140** — UPDATE memory_categories uses a subquery that counts rows from memory_entries without restricting by tenant_id (missing tenant filter in the subquery). This can aggregate counts across tenants.
  - _Fix:_ Include tenant scoping in the subquery (e.g. AND tenant_id = ${tenantId}), or accept/pass tenantId into refreshCategoryCount and use it in the WHERE to ensure the count is restricted to the category's tenant. Alternatively JOIN to memory_categories to infer the tenant and filter accordingly.
- **server/memory-graph.ts:80** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} does not include a tenant_id constraint. This updates a tenant-scoped table row without ensuring tenant isolation.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}), or pass tenantId into the caller so the UPDATE is parameterized by the correct tenant before modifying the row.
- **server/memory-graph.ts:196** — SELECT 1 FROM memory_links ... LIMIT 1 reads from memory_links without any tenant_id constraint. If memory_links is tenant-scoped this allows checking existence/fingerprinting across tenants.
  - _Fix:_ Constrain the SELECT by tenant_id (e.g. AND tenant_id = ${tenantId}) or join via a tenant-scoped memory_entries row to ensure the read is limited to the caller's tenant.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} performs an update on a tenant-scoped table without constraining tenant_id. This can modify rows belonging to other tenants if an id is reused or guessed.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}), or use a helper that enforces tenant isolation. Ensure tenantId is passed to this function and validated.
- **server/memory-intelligence.ts:272** — db.execute(...) UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} updates a tenant-scoped table without filtering by tenant_id. The entry.id is caller-controlled (created above) but lack of tenant check risks cross-tenant modification if misused.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or use storage.updateMemoryEntry / another tenant-aware API that enforces tenant isolation. Ensure tenantId is validated and propagated to the UPDATE.
- **server/memory-intelligence.ts:315** — db.execute(...) UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in the update branch) updates memory_entries without constraining tenant_id, allowing potential cross-tenant mutation.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or call a tenant-aware helper that enforces the tenant constraint. Ensure tenantId is required and validated by the caller.
- **server/minerva-planner.ts:238** — createPlan defaults tenantId = args.tenantId ?? 1 and then INSERTs with that tenantId. Accepting tenantId from the caller (and defaulting to admin tenant 1) without validating the caller's authority can allow creating plans for arbitrary tenants (or accidentally for admin tenant).
  - _Fix:_ Do not accept an arbitrary tenantId from untrusted callers. Require the caller's authenticated tenant be passed/derived from auth context and use that for INSERT. Remove the silent default to tenant 1 for external callpaths, or validate that the actor is authorized to act on the supplied tenantId.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checks ownership with assertProjectInTenant but the INSERT omits tenant_id, violating the invariant that every insert must explicitly set tenant_id (columns are NOT NULL/no default).
  - _Fix:_ Add tenant_id to the INSERT and supply the caller's tenantId (use the verified options.tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep using parameterized Drizzle sql to avoid injection.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` does not include tenant_id. As above, the code calls assertProjectInTenant but then inserts without setting tenant_id, breaking the tenant-isolation invariant.
  - _Fix:_ Include tenant_id in the INSERT and pass the verified options.tenantId. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Use Drizzle parameterization to keep it safe.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — returns rows across tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or otherwise restrict this query to an explicit list of tenant IDs determined by trusted logic. If the intent is to discover tenants, perform an explicit, audited tenant enumeration step instead of an unscoped select.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} updates by id only, without verifying tenant_id. In a multi-tenant DB this can modify another tenant's row if ids collide or if caller is untrusted.
  - _Fix:_ Include tenant isolation in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} or AND tenant_id = ${tenantId}). Always pair primary-key updates with an explicit tenant_id check when enforcing tenant isolation at the application layer.
- **server/oauth-subscriptions.ts:966** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} (second instance) lacks tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${sub.tenant_id} or AND tenant_id = ${tenantId}) to the UPDATE to ensure the row belongs to the intended tenant before modifying it.
- **server/oauth-subscriptions.ts:981** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} (third instance) is not constrained by tenant_id and may affect rows from other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${sub.id} AND tenant_id = ${tenantId}) or otherwise ensure the operation is only performed within the intended tenant context.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(lockKey) inside a SQL template (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)). lockKey is derived from inputs (ledgerKey, tenantId) — using sql.raw with derived values risks injection and circumvents parameter binding.
  - _Fix:_ Avoid sql.raw with values derived from request input. Use parameterized interpolation instead (e.g. SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint) or pass a numeric parameter), or validate and cast the value to a numeric type before binding. Prefer the DB driver's parameter binding (no raw SQL insertion) to prevent injection and keep tenant-derived values as parameters.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} is missing a tenant_id constraint. Reading a persona by id without tenant scoping can return a persona from a different tenant.
  - _Fix:_ Add tenant filtering (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) to ensure the returned persona belongs to the calling tenant, or explicitly verify ownership before returning data.
- **server/pace-control.ts:85** — Queries against heartbeat_logs (COUNT ...) omit any tenant filter — e.g. SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}. If heartbeat_logs is tenant-scoped, this returns aggregated activity across all tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or require caller to pass a tenantId and enforce it in the query. If the intention is global metrics, make that explicit and ensure only privileged/admin code executes it.
- **server/pace-control.ts:140** — getPaceSnapshot performs SELECT COUNT ... FROM heartbeat_logs WHERE created_at > ... without tenant scoping, returning global heartbeat logs across tenants if the table is tenant-scoped.
  - _Fix:_ Require tenantId input and add WHERE tenant_id = ${tenantId} to the queries, or clearly restrict this function to admin/ops contexts and document the privileged nature. Prefer per-tenant snapshots in typical multi-tenant deployments.
- **server/pace-control.ts:147** — personaRows query SELECT COALESCE(persona_name, 'unknown') AS persona_name, COUNT(*)::int AS n FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name has no tenant_id filter and will aggregate across tenants.
  - _Fix:_ Filter by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or require an explicit tenant parameter so per-tenant rollups do not leak cross-tenant activity.
- **server/personality-files.ts:8** — CREATE TABLE defines tenant_id INTEGER NOT NULL DEFAULT 1. This default allows INSERTs that omit tenant_id and implicitly attribute rows to tenant 1, violating the invariant that tenant_id must be explicitly supplied (NOT NULL with no default).
  - _Fix:_ Remove the DEFAULT on tenant_id (make it INTEGER NOT NULL with no default) and add a migration to surface/fix any existing rows. Audit callers to ensure every INSERT explicitly passes the caller's tenantId; fail inserts that would otherwise rely on a default.
- **server/plan-executor.ts:116** — appendExecutionLog updates plans with: UPDATE plans ... WHERE id = ${planId} — no tenant_id constraint. Updating a tenant-scoped table by id only can modify another tenant's row if planId is misused or an attacker supplies it.
  - _Fix:_ Require and pass the plan's tenant_id into appendExecutionLog and include AND tenant_id = ${tenantId} in the WHERE clause (or perform the update using the claimed row returned earlier). Do this consistently for any helper that mutates tenant-scoped rows by id.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' ... WHERE id = ${planId} — missing tenant_id filter. This can change the status of a plan belonging to another tenant if planId is used cross-tenant.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}). Pass the authoritative tenant id from the claimed plan row into this update call.
- **server/plan-executor.ts:725** — Final plan state UPDATE (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint. This writes to a tenant-scoped table without scoping to the plan's tenant.
  - _Fix:_ Include tenant_id = ${row.tenant_id} in the WHERE clause (or otherwise bind the update to the claimed plan's tenant). Ensure all execution-finalizing updates are limited to the plan's tenant.
- **server/plan-executor.ts:759** — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id. Unscoped updates on plans can affect other tenants' plans.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} (or equivalent) to the WHERE clause. Make sure every UPDATE/DELETE on plans includes tenant scoping derived from the claimed plan row.
- **server/plan-executor.ts:775** — Boot-time recovery UPDATE flips executing→approved across all rows: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... — this runs unscoped across tenants.
  - _Fix:_ If this operation is intended to be global, make the admin intent explicit (e.g. run from a privileged migration/maintenance job) and document it. Otherwise, constrain the UPDATE per-tenant (e.g. run per-tenant recovery or include AND tenant_id = ${tenantId}). At minimum, require an explicit admin context and log the tenant(s) affected before performing global updates.
- **server/process-governor.ts:121** — SELECT subqueries count heartbeat_logs without constraining tenant_id (reads tenant-scoped heartbeat_logs across tenants)
  - _Fix:_ Add tenant_id = ${tenantId} to the heartbeat_logs subqueries (e.g. SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id AND tenant_id = ${tenantId} AND ...) so all log reads are scoped to the caller's tenant.
- **server/process-governor.ts:199** — SELECT AVG(duration_ms) FROM heartbeat_logs has no tenant_id filter (reads heartbeat_logs globally)
  - _Fix:_ Restrict the query to the caller's tenant: add AND tenant_id = ${tenantId} to the WHERE clause so average response time is computed only for the tenant's logs.
- **server/process-governor.ts:240** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs lacks tenant_id filter (cross-tenant read)
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause of the heartbeat_logs query to prevent aggregating other tenants' failure data.
- **server/process-governor.ts:1184** — SELECT joins governance_rules (gr) via ga.rule_id but does not constrain governance_rules by tenant_id — reads tenant-scoped governance_rules without explicit tenant filter
  - _Fix:_ When joining governance_rules, include a tenant filter on the joined table (e.g. ON gr.id = ga.rule_id AND gr.tenant_id = ${tenantId}), or select rule fields from a tenant-scoped lookup verified to belong to the same tenant.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} — no tenant_id constraint or ownership check
  - _Fix:_ Pass tenantId into updateProjectBrain (or otherwise verify caller context) and restrict the SELECT to WHERE id = ${projectId} AND tenant_id = ${tenantId} (or validate the project belongs to the caller) before reading/updating project data.
- **server/project-brain.ts:332** — Backfill SELECT across projects/conversations JOINs has no tenant_id filter — may enumerate projects from all tenants
  - _Fix:_ Scope the discovery query to a tenant (add WHERE p.tenant_id = ${tenantId} and c.tenant_id = ${tenantId}) or otherwise ensure the function runs per-tenant with tenantId param and includes tenant_id filters on all queries.
- **server/project-brain.ts:349** — SELECT conversations WHERE c.project_id = ${proj.id} lacks tenant_id constraint (reads conversations across tenants)
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (and/or ensure projects->conversations ownership is verified) so conversations are scoped to the caller's tenant.
- **server/project-brain.ts:359** — SELECT messages WHERE conversation_id = ${conv.id} has no tenant_id filter (reads messages across tenants)
  - _Fix:_ Constrain messages queries with AND tenant_id = ${tenantId} (or verify conversation ownership first) so message reads are limited to the tenant's data.
- **server/proposal-verifier.ts:33** — SELECT FROM code_proposals WHERE id = ${proposalId} does not verify tenant ownership or include tenant_id (possible cross-tenant access to proposals)
  - _Fix:_ Require a tenant context/tenantId parameter for verification, and include AND tenant_id = ${tenantId} in SELECT and all subsequent updates, or perform an explicit ownership check before reading or mutating the proposal.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` without constraining tenant_id (global scan).
  - _Fix:_ Avoid unscoped reads of tenant data. Either add a tenant_id WHERE clause or explicitly gate this operation as a trusted global system action (documented flag) and call the existing listScheduledMessages(...) API that supports the deliberate `_allowGlobalSystemUseOnly` flow. If this must scan all tenants, move to a controlled, audited system job that enforces explicit reviewer-approved policy and minimizes returned columns.
- **server/routes.ts:1117** — Middleware for /uploads selects all file_storage rows for a basename (SELECT tenant_id FROM file_storage WHERE filename = ...) with NO tenant_id filter. This enumerates other tenants' file rows and relies on post-query logic to decide access, producing a cross-tenant lookup.
  - _Fix:_ Avoid querying all tenants' rows. Instead, first check for a file_storage row scoped to the authenticated tenant (WHERE filename = ... AND tenant_id = <authedTenantId>). If none exists, deny access without performing or returning cross-tenant existence checks. If the collision-detection behavior is required, implement it under an admin-only path or namespace filenames by tenant at upload time to avoid cross-tenant filename collisions.
- **server/routes.ts:2089** — Presenter slide image fetches use presenter_slide_images SELECTs by session_id (SELECT image_data ... FROM presenter_slide_images WHERE session_id = ${sessionId} ...) with no tenant check. sessionId is derived from an earlier unscoped presenter_sessions lookup (token) — this can expose another tenant's slide images.
  - _Fix:_ Ensure any query referencing a child table enforces tenant ownership, e.g. JOIN presenter_slide_images to presenter_sessions and include presenter_sessions.tenant_id = <caller tenant> in the WHERE clause, or select the presenter_session row (scoped to tenant) and use that session id only if ownership is confirmed. Avoid relying solely on session_id without tenant verification.
- **server/routes.ts:2313** — Public order lookup endpoint queries delivery_logs by orderId / stripePaymentId with no tenant filtering (SELECT ... FROM delivery_logs WHERE orderId = ... OR stripePaymentId = ...). delivery_logs is tenant-scoped and this allows retrieving another tenant's delivery record by supplying an order/session id.
  - _Fix:_ Treat order/session ids as unguessable capability tokens only if they include explicit tenant-binding, or require additional proof (signed capability, email ownership flow) before returning any tenant-scoped row. At a minimum, when returning info from delivery_logs, validate the tenant (e.g. by storing a tenant-bound capability or signing the session) or restrict this endpoint so it cannot read arbitrary rows across tenants.
- **server/routes.ts:2421** — Asynchronous order-recovery flow queries delivery_logs by lower(customer_email) without any tenant constraint (WHERE lower(customer_email) = ${rawEmail}). This scans a tenant-scoped table across all tenants to determine matches.
  - _Fix:_ Limit the lookup scope to records that the platform is authorized to act on (for example, by requiring additional verification that ties the request to a specific tenant), or perform the search in a context that does not expose cross-tenant rows. Alternatively, maintain a separate, indexed, privacy-safe mapping for public recovery flows or ensure the background job runs with an admin/trusted context and strictly controls what is sent (and never returns full tenant data to the caller).
- **server/routes.ts:4325** — SELECT from tenant-scoped table 'conversations' without constraining to the caller's tenant. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` omits any tenant_id filter, allowing a caller to possibly read project_id for a conversation they don't own.
  - _Fix:_ Restrict the SELECT to the caller's tenant. Add a tenant_id predicate (e.g. `AND tenant_id = ${tenantId}`) or use the application's storage/Drizzle query helpers that enforce tenant scoping. Prefer verifying ownership via a tenant-scoped select from conversations before using the project_id result.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Fix:_ Include the tenant in the WHERE clause (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise validate/anchor to conv.tenantId to ensure the update cannot affect another tenant's row.
- **server/routes/conversations.ts:99** — SELECT from tenant-scoped table 'project_conversations' lacks tenant_id constraint (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}).
  - _Fix:_ Constrain the query by tenant (e.g. WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise ensure the code only inspects rows belonging to the caller's tenant.
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})) or use a storage/api function that enforces/sets tenant_id server-side after verifying ownership.
- **server/routes/conversations.ts:231** — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ...)).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})) or use an application-layer API that sets and enforces tenant ownership before inserting.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table inbox_messages is missing tenant_id predicate (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). This allows modifying a message by id without constraining to the caller's tenant.
  - _Fix:_ Add a tenant predicate to the UPDATE so the operation is scoped to the caller's tenant. Example: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}. Alternatively use Drizzle's update(...) with .where(and(eq(inbox_messages.id, msg.id), eq(inbox_messages.tenantId, tenantId))). This makes the change fail for non-owned rows and prevents cross-tenant mutation (also consider using a single statement that verifies ownership atomically to prevent TOCTOU races).
- **server/routes/projects.ts:35** — Subquery SELECT COUNT(*) FROM project_files WHERE project_id = p.id does not constrain by tenant_id. This reads a tenant-scoped table without enforcing the caller's tenant.
  - _Fix:_ Add an explicit tenant filter to the subquery (e.g. AND tenant_id = ${tenantId}) or rewrite to join projects and filter by the caller's tenant. Prefer using Drizzle eq(table.tenantId, tenantId) for consistency.
- **server/routes/projects.ts:36** — Subquery SELECT COUNT(*) FROM project_notes WHERE project_id = p.id does not constrain by tenant_id. Tenant-scoped data may be counted across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the subquery (or use a join with projects filtered by tenant) so counts are limited to the caller's tenant.
- **server/routes/projects.ts:37** — Subquery SELECT COUNT(*) FROM project_conversations WHERE project_id = p.id lacks a tenant_id constraint. This can leak cross-tenant counts.
  - _Fix:_ Constrain the subquery by tenant_id (AND tenant_id = ${tenantId}) or join to a tenant-checked parent so only the caller's tenant rows are counted.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC does not include tenant_id. This reads a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or join against projects constrained by tenant_id. Use Drizzle eq(table.tenantId, tenantId) to build the condition.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 lacks tenant_id constraint. Reading tenant-scoped project_files without tenant filtering risks cross-tenant access.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or join to projects filtered by tenant) so only files belonging to the caller's tenant are fetched.
- **server/sculptor.ts:76** — INSERT into sculptor_sessions sets parent_session_id from caller-supplied params.parentSessionId without validating ownership of the referenced parent session. This can create cross-tenant links.
  - _Fix:_ If parent_session_id is provided, validate the parent session belongs to the same tenant before inserting (e.g. SELECT id FROM sculptor_sessions WHERE id = ${params.parentSessionId} AND tenant_id = ${params.tenantId}). Reject or nullify the parent_session_id if the check fails.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' — no tenant_id constraint. This read is used to decide global updates; counting across tenants can lead to cross-tenant effects.
  - _Fix:_ Scope the SELECT by tenant_id (or ADMIN_TENANT_ID if the intended target is the admin tenant). If the intent is to act only on the admin tenant, add WHERE tenant_id = ${ADMIN_TENANT_ID} to the query.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' — missing tenant_id constraint. Used to decide a follow-up UPDATE that is currently unscoped.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} ...) or accept a tenantId parameter so the operation only examines a single tenant's rows.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ...) — no tenant_id constraint. Follow-up UPDATE is likewise unscoped and will affect all tenants.
  - _Fix:_ Restrict the SELECT to a tenant (e.g. tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the logic operates per-tenant. Avoid global scans that drive writes across tenants.
- **server/seed.ts:35** — Unscoped SELECT on tenant-scoped table `research_sessions`: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id WHERE clause, allowing cross-tenant data exposure.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. `SELECT COUNT(*) as count FROM research_sessions WHERE tenant_id = ${tenantId}`) or otherwise iterate per-known-tenant. Prefer Drizzle-style filters (where(eq(research_sessions.tenantId, tenantId))). Ensure tenantId is derived from a trusted source.
- **server/seed.ts:3092** — Unscoped SELECT on tenant-scoped table `research_programs`: `SELECT id, name, model FROM research_programs WHERE is_active = true` returns rows across all tenants (no tenant_id filter).
  - _Fix:_ Scope reads to a specific tenant (e.g. `WHERE is_active = true AND tenant_id = ${tenantId}`) or intentionally iterate explicitly over tenants after validating ownership. Use Drizzle .where(eq(table.tenantId, tenantId)) so no cross-tenant rows are returned.
- **server/seed.ts:3442** — SELECT from tenant-scoped table `conversations` lacks any tenant_id constraint. The query selects conversations across all tenants: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)`
  - _Fix:_ Constrain the SELECT to the intended tenant (e.g. add `AND c.tenant_id = 1` or use a parameterized tenantId). Also ensure the NOT EXISTS check/join is scoped (e.g. join projects/project_conversations and check matching tenant_id) so you cannot read or backfill rows from other tenants.
- **server/seed.ts:4270** — SELECT on tenant-scoped table `heartbeat_tasks` is performed with no tenant filter: `db.select().from(heartbeatTasks)`. This reads heartbeat tasks for all tenants.
  - _Fix:_ Always filter by the caller's tenant (e.g. .where(eq(heartbeatTasks.tenantId, tenantId)) or SQL `WHERE tenant_id = $1`). Use a parameterized tenantId variable rather than relying on defaults.
- **server/seed.ts:4272** — Bulk INSERT into `heartbeat_tasks` (db.insert(heartbeatTasks).values([...])) constructs new tenant-scoped rows but the inserted objects do not include tenant_id. If tenant_id is required and has no safe default, rows will be created without correct ownership (or the insert may fail).
  - _Fix:_ Include tenant_id explicitly in each INSERT value (e.g. tenant_id: 1 or tenantId variable). If using Drizzle models, set tenantId in the .values objects; do not rely on implicit defaults.
- **server/seed.ts:4339** — INSERT into `heartbeat_tasks` (Model Scout) via db.insert(...) omits tenant_id, creating tenant-ambiguous rows in a tenant-scoped table.
  - _Fix:_ Set tenant_id explicitly when inserting (or use a per-tenant insert helper). Ensure inserts are limited to the intended tenant.
- **server/seed.ts:4398** — INSERT into `heartbeat_tasks` (Daily Cloud Backup) via db.insert(...) omits tenant_id.
  - _Fix:_ Include tenant_id in the inserted object or otherwise scope the insert to the intended tenant.
- **server/seed.ts:4414** — INSERT into `heartbeat_tasks` (Memory Snapshot Backup) via db.insert(...) omits tenant_id.
  - _Fix:_ Set tenant_id explicitly for each inserted heartbeat task or use a scoped API that injects the correct tenant_id.
- **server/seed.ts:4430** — INSERT into `heartbeat_tasks` (Decision Analysis Engine) via db.insert(...) omits tenant_id.
  - _Fix:_ Include tenant_id in the inserted row(s). Always ensure create paths supply the caller's tenant_id.
- **server/seed.ts:4462** — INSERT into `heartbeat_tasks` (Process Optimization Engine) via db.insert(...) omits tenant_id.
  - _Fix:_ Provide tenant_id explicitly on INSERT and/or use tenant-scoped helpers to prevent accidental cross-tenant writes.
- **server/seed.ts:4479** — INSERT into `heartbeat_tasks` (Community Fork Scanner) via db.insert(...) omits tenant_id.
  - _Fix:_ Include tenant_id in the insert objects or otherwise scope the inserts to the correct tenant.
- **server/seed.ts:4494** — INSERT into `heartbeat_tasks` (Memory Hygiene Sweep) via db.insert(...) omits tenant_id.
  - _Fix:_ Explicitly set tenant_id when inserting. Avoid bulk inserts into tenant-scoped tables without tenant context.
- **server/seed.ts:4510** — INSERT into `heartbeat_tasks` (Embedding Backfill) via db.insert(...) omits tenant_id.
  - _Fix:_ Always include tenant_id in inserted rows for tenant-scoped tables, or use a helper that injects the caller's tenant id into every row.
- **server/seed.ts:4715** — UPDATE on tenant-scoped table `memory_entries` runs without a tenant_id constraint: `UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ...`. This modifies rows across all tenants.
  - _Fix:_ Scope the UPDATE to the target tenant (e.g. `AND tenant_id = 1` or parameterized tenantId). If the operation must run globally as an admin migration, do so in an explicit, documented admin migration path with safeguards and approval.
- **server/seed.ts:4739** — UPDATE on tenant-scoped table `conversations` runs without tenant_id constraint: `UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'`. This will update conversations for all tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. `AND tenant_id = ${tenantId}`) or iterate per-tenant. Avoid blanket updates that affect every tenant unless explicitly intended and controlled as an admin migration.
- **server/self-improvement.ts:170** — detectStagnation issues: SELECT on tenant‑scoped table `experiments` can run without a tenant filter when tenantId is not supplied (conditions may contain only category). This allows cross‑tenant reads.
  - _Fix:_ Require a tenantId (throw if missing) or always include eq(experiments.tenantId, tenantId) in the WHERE clause. Do not build a WHERE clause that omits tenant isolation; explicitly fail-closed when tenantId is not provided.
- **server/self-improvement.ts:521** — getExperimentHistory may return experiments across all tenants when tenantId is omitted — final branch returns db.select().from(experiments) without tenant_id filtering.
  - _Fix:_ Make tenantId required or fail when absent. Always include a WHERE eq(experiments.tenantId, tenantId) (or equivalent Drizzle filter) for any read from tenant-scoped `experiments`.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` lacks a tenant constraint. The query filters only by conversationId: `where(eq(messages.conversationId, conversationId))` — this allows cross-tenant reads if an attacker supplies a conversationId from another tenant.
  - _Fix:_ Add an explicit tenant filter to every messages query. For example with Drizzle: `.where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))` or use the tenant-scope helper to produce the predicate. Never rely on prior validations; enforce tenant_id in the same query.
- **server/skillify.ts:201** — SELECT from tenant-scoped table `messages` in analyzeConversation() lacks a tenant constraint. The code reads all messages for a conversation via `where(eq(messages.conversationId, conversationId))` without checking messages.tenant_id.
  - _Fix:_ Constrain this read to the caller's tenant, e.g. `.where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))`. Alternatively use `tenantScope(messages.tenantId, tenantId)`/`requireTenantScope(...)` to ensure the tenant is validated and included in the query.
- **server/skillify.ts:221** — SELECT from `personas` lacks a tenant constraint. The persona lookup `.where(eq(personas.id, conv[0].personaId))` does not verify that the persona belongs to the caller's tenant, allowing cross-tenant disclosure of persona data.
  - _Fix:_ Include tenant scoping on personas queries, for example: `.where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))`. Use the tenant-scope helpers (requireTenantScope/tenantScope) to enforce proper validation and fail-closed behavior.
- **server/skillify.ts:329** — SELECT from tenant-scoped table `messages` in skillifyConversation() lacks a tenant constraint. The recent messages are selected with `where(eq(messages.conversationId, conversationId))` and no tenant_id check.
  - _Fix:_ Add tenant scoping to the messages query: `.where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))` or apply the tenant-scope helper so the read is strictly limited to the caller's tenant.
- **server/storage.ts:239** — getConversationUnscoped performs an unscoped SELECT on tenant-scoped table `conversations` (WHERE id = ${id}) with NO tenant_id constraint — returns rows across tenants.
  - _Fix:_ Remove or heavily restrict this API. If an unscoped fetch is genuinely required, make it private/internal and ensure every caller MUST validate ownership immediately after fetch; preferably add an explicit flag/acknowledgement and log/audit or require an admin-only path. The safest fix is to require a tenantId parameter and add eq(conversations.tenantId, tenantId) to the WHERE clause.
- **server/storage.ts:294** — attachCitationMetadata issues a raw SELECT FROM `messages` using id IN (...) but does NOT filter by tenant_id. This reads a tenant-scoped table without tenant scoping.
  - _Fix:_ Require or derive a tenantId and include eq(messages.tenantId, tenantId) in the WHERE clause (or join via conversation→tenant_id). If ids are always from a previous tenant-scoped query, still validate/ensure that invariant at the callsite or accept tenantId to enforce at DB-level.
- **server/storage.ts:395** — attachCitations updates the `messages` table (tenant-scoped) with UPDATE ... WHERE id = ${messageId} but does NOT include tenant_id in the WHERE clause — unscoped write.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}), or verify the message belongs to the expected tenant before updating. Never perform destructive writes against tenant-scoped tables using only id without tenant validation.
- **server/storage.ts:768** — createKnowledge inserts into tenant-scoped table `agent_knowledge` using provided data without explicitly validating/asserting data.tenantId (no assertValidTenantId) — unvalidated INSERT into tenant-scoped table.
  - _Fix:_ Require and validate tenantId for creates (use assertValidTenantId(data.tenantId) or accept tenantId as an explicit parameter). Fail closed if tenantId is missing/invalid. This prevents accidental cross-tenant writes.
- **server/storage.ts:530** — createMemoryEntry inserts into tenant-scoped table `memory_entries` without validating that data.tenantId is present/valid — unvalidated INSERT into tenant-scoped table.
  - _Fix:_ Require and validate a positive tenantId (assertValidTenantId(data.tenantId) or accept explicit tenantId param) before inserting. Fail closed if tenantId missing/invalid.
- **server/storage.ts:803** — updateMemoryEmbedding updates `memory_entries` WHERE id = ${id} without scoping by tenant_id — unscoped write on a tenant-scoped table.
  - _Fix:_ Require a tenantId parameter or otherwise verify ownership before updating, and include eq(memory_entries.tenant_id, tenantId) in the WHERE clause to enforce tenant isolation at the DB level.
- **server/storage.ts:815** — updateKnowledgeEmbedding updates `agent_knowledge` WHERE id = ${id} with NO tenant_id filter — unscoped write on tenant-scoped table.
  - _Fix:_ Require and validate tenantId or verify the row's tenant before updating. Include the tenant filter in the WHERE clause (eq(agent_knowledge.tenant_id, tenantId)).
- **server/storage.ts:510** — getAllMemoriesForBackup does a SELECT FROM `memory_entries` with NO tenant filter — reads tenant-scoped table across all tenants.
  - _Fix:_ Require an explicit tenantId parameter (and validate it) for this API or gate it behind an admin-only code path. At minimum, document and restrict this function so it cannot be triggered from non-admin contexts.
- **server/storage.ts:934** — getHeartbeatTask selects from `heartbeat_tasks` WHERE id = ${id} WITHOUT a tenant_id constraint — returns tenant-scoped rows without tenant filtering.
  - _Fix:_ Require tenantId or otherwise verify ownership before returning the task. Add eq(heartbeat_tasks.tenantId, tenantId) to the WHERE clause or validate that the caller is privileged to view tasks across tenants.
- **server/storage.ts:1019** — claimHeartbeatTasks performs UPDATE heartbeat_tasks ... WHERE id = ${id} ... (and other predicates) but does NOT require tenant_id in the WHERE clause — unscoped write affecting tenant-scoped table.
  - _Fix:_ Ensure claim logic includes tenant scoping (e.g. AND tenant_id = ${tenantId}) or run the claiming logic per-tenant. If this is a global scheduler, make it an admin-only operation and ensure caller identity is validated before modifying tenant data.
- **server/storage.ts:1034** — markHeartbeatTaskRun updates `heartbeat_tasks` WHERE id = ${id} without a tenant_id clause — unscoped write against tenant-scoped table.
  - _Fix:_ Require tenantId or include tenant scoping in the WHERE clause to avoid updating other tenants' tasks. If invoked by scheduler, ensure scheduler is authorized and operates per-tenant.
- **server/storage.ts:1445** — supersedeAdr updates architecture_decisions rows with UPDATE ... WHERE id = ${oldId} / WHERE id = ${newId} but does NOT include tenant_id in the WHERE — writes tenant-scoped table without tenant scoping on the UPDATE statements.
  - _Fix:_ Include tenant scoping on UPDATEs (e.g. WHERE id = ${oldId} AND tenant_id = ${tenantId}) even if you previously read and validated the rows. This guards against TOCTOU and accidental cross-tenant updates if code changes.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (where clause only filters by name). This can lead to cross-tenant lookup or leakage if global uniqueness assumptions change.
  - _Fix:_ Scope the lookup to the caller's tenant. Example: include eq(customTools.tenantId, tenantId) in the WHERE clause (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))).
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` uses WHERE eq(custom_tools.id, tool.id) but does NOT include tenant_id in the predicate. Per invariant, all writes must constrain by tenant to prevent touching another tenant's rows by id.
  - _Fix:_ Add tenant scoping to the UPDATE: include eq(customTools.tenantId, tool.tenantId) (or the validated tenantId) in the WHERE clause, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Ensure `tool` includes the tenantId or pass the caller tenantId explicitly and validate it first.
- **server/tool-learning.ts:315** — DELETE on tenant-scoped table `custom_tools` uses WHERE eq(custom_tools.id, tool.id) without tenant_id constraint. This can delete another tenant's row if an id is guessed or mis-bound.
  - _Fix:_ Constrain the DELETE by tenant_id as well: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Ensure the tenantId is obtained from the inserted/validated row or passed and validated by the caller.
- **server/tool-learning.ts:365** — UPDATE that increments `usageCount` on tenant-scoped table `custom_tools` uses WHERE eq(custom_tools.id, tool.id) but lacks tenant_id constraint, allowing an update to a row belonging to another tenant by id.
  - _Fix:_ Include tenant scoping in the UPDATE predicate, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Prefer using the caller's validated tenantId rather than relying solely on id matches.
- **server/tools.ts:2976** — SELECT against tenant-scoped table `project_files` lacks an explicit tenant filter. The duplicate-check SELECT id FROM project_files WHERE project_id = ${params.id} ... does not constrain by tenant_id.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. AND tenant_id = ${tenantId}), or join to projects (with p.tenant_id = ${tenantId}) as done elsewhere. Ensure every read from tenant-scoped tables includes a tenant_id constraint tied to the caller.
- **server/tools.ts:3016** — SELECT against tenant-scoped table `project_conversations` lacks an explicit tenant filter. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} does not include tenant_id.
  - _Fix:_ Constrain reads by tenant (e.g. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) or join to the tenant-scoped projects/conversations tables to enforce tenant ownership before returning results.
- **server/tools.ts:4528** — INSERT into tenant-scoped table project_notes does not include tenant_id. The INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) is executed without explicitly setting tenant_id even though project_notes is a project/tenant-scoped table.
  - _Fix:_ Always include tenant_id in INSERTs to tenant-scoped tables. Add tenant_id to the column list and pass the validated tenant (e.g. ${params._tenantId} or a canonical tenantId retrieved after assertProjectInTenant). Example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Prefer Drizzle's typed insert helpers or explicit tenant binding to prevent future omissions.
- **server/tools.ts:4532** — INSERT into tenant-scoped table project_files does not include tenant_id. The INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) omits tenant_id.
  - _Fix:_ Include tenant_id in every INSERT into tenant-scoped tables. Add tenant_id to the column list and supply the validated tenant id (e.g. ${params._tenantId}) or derive it server-side from the project row before inserting. Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${projectId}, ...). Use parameterized Drizzle insert helpers for clarity and safety.
- **server/tools.ts:6380** — INSERT into tenant-scoped table project_files (during generate_audio) omits tenant_id. The code inserts a project_files row (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without explicitly persisting tenant_id.
  - _Fix:_ Ensure tenant_id is passed on INSERTs. After verifying ownership with assertProjectInTenant, include tenant_id in the VALUES list (e.g. ${params._tenantId}). Alternatively, query the project's tenant and use that authoritative value. Prefer Drizzle's insert API to make the tenant column explicit and avoid mistakes.
- **server/tools.ts:6914** — INSERT into tenant-scoped table project_files (during create_slideshow_video) omits tenant_id. The INSERT INTO project_files (...) VALUES (...) does not set tenant_id even though a project-scoped row is being created.
  - _Fix:_ Add tenant_id to the INSERT (tenant_id, project_id, ...) and pass the verified tenant (e.g. ${params._tenantId} or the project owner tenant). Best practice: always include tenant_id for tenant-scoped tables and use server-side assertions (assertProjectInTenant) plus explicit tenant values in the INSERT to prevent cross-tenant writes.
- **server/tools.ts:6914** — INSERT into tenant-scoped table 'project_files' does not set tenant_id (INSERT INTO project_files ... VALUES (...) lacks tenant_id). The code relies only on assertProjectInTenant(...) but the INSERT itself does not include tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)) or use Drizzle insert with tenantId set to the caller's tenant. Keep the assertProjectInTenant check, but also store tenant_id on the row to enforce application-level isolation as required.
- **server/tools/domains/files/handlers.ts:835** — INSERT into tenant-scoped table 'project_files' does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`
  - _Fix:_ Always include tenant_id in INSERTs to tenant-scoped tables. Add tenant_id to the column list and pass the caller's tenant (ctx.tenantId) as a parameter, e.g. `INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ${fileName}, ...)` (or use Drizzle API with tenantId field set).
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE executed without tenant constraint: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` does not include `tenant_id = ${tenantId}`. This allows a row id obtained (or guessed) to be modified even if it belongs to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause (defense-in-depth). For example: `WHERE id = ${row.id} AND tenant_id = ${tenantId}` (or use the row id from a prior tenant-scoped SELECT and perform the UPDATE within the same transaction while still asserting tenant_id in the WHERE). Ensure all UPDATE/DELETE statements against tenant-scoped tables always include `tenant_id = <caller tenant>`.
- **server/tools/domains/self-improvement/handlers.ts:43** — logExperimentHandler calls backing lib and forwards ctx.tenantId without validating presence/type. A write into a tenant-scoped table could occur with tenantId undefined/null, risking missing tenant_id on INSERTs or cross-tenant exposure.
  - _Fix:_ Fail-closed: validate the trusted tenant context before calling the backing lib. e.g. if (!ctx.tenantId || typeof ctx.tenantId !== 'number') return { error: 'Tenant context required' }; then pass the validated tenantId to logExperiment. Ensure backing lib can't run with undefined tenantId.
- **server/tools/domains/scratchpad/handlers.ts:44** — writeScratchpadHandler calls writeDelegationScratchpad with ctx.tenantId cast to number but does not check that ctx.tenantId is present. This may allow a write to a tenant-scoped store with an undefined/null tenant_id.
  - _Fix:_ Fail-closed: validate ctx.tenantId before doing the write (if (!ctx.tenantId || typeof ctx.tenantId !== 'number') return { success: false, error: 'Tenant context required' }). Ensure the tenantId passed to writeDelegationScratchpad is a valid number.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE on memory_entries uses WHERE id = ${id} with no tenant_id (or persona) constraint — tenant-scoped row updated without explicit tenant ownership filter.
  - _Fix:_ Include tenant scoping in the UPDATE so only the caller's tenant row may be modified, e.g. add AND tenant_id = ${tid} (and/or AND persona_id = ${personaId}) to the WHERE clause. Prefer using the Drizzle eq(table.tenantId, tenantId) helper or a storage-layer helper that enforces tenant scoping.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) lacks tenant_id constraint — bulk update by id array can affect rows from other tenants.
  - _Fix:_ Constrain the UPDATE to the tenant, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}, or perform an UPDATE using a JOIN/subselect that restricts ids to those belonging to tenant_id = ${tid}. Alternatively, use a storage-layer function that enforces tenant ownership before mutating rows.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE on memory_entries uses WHERE id = ${id} with no tenant_id (or persona) constraint — tenant-scoped row updated without explicit tenant ownership filter.
  - _Fix:_ Include tenant scoping in the UPDATE so only the caller's tenant row may be modified, e.g. add AND tenant_id = ${tid} (and/or AND persona_id = ${personaId}) to the WHERE clause. Prefer using the Drizzle eq(table.tenantId, tenantId) helper or a storage-layer helper that enforces tenant scoping.
- **server/venture-discovery/budget.ts:155** — UPDATE on tenant-scoped table agent_cost_ledger uses only id in WHERE (no tenant_id constraint). settleVentureReservation updates ledger rows by id without ensuring the row belongs to the caller's tenant.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or require/accept tenantId and verify ownership (or RETURNING and validate returned tenant_id) before applying the update. Do not update ledger rows by id alone.
- **server/venture-discovery/budget.ts:169** — UPDATE on tenant-scoped table agent_cost_ledger uses only id in WHERE (no tenant_id constraint). releaseVentureReservation clears a reservation by id without verifying tenant ownership.
  - _Fix:_ Add tenant verification on the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or change the API to accept tenantId and validate ownership (or use RETURNING to assert tenant_id) before changing the row.
- **server/video-job-runner.ts:228** — Global UPDATE on tenant-scoped table video_jobs with no tenant_id filter. recoverStaleVideoJobs marks stale jobs across all tenants (UPDATE ... WHERE status IN (...) AND updated_at < ...) without scoping to a tenant.
  - _Fix:_ Restrict the update to a specific tenant (WHERE tenant_id = <tenant>) or iterate per-tenant and apply the stale-check per-tenant. If this must be a global maintenance operation, ensure it runs in a privileged, auditable context and add explicit protections (e.g. administrator-only path) — but prefer adding tenant scoping where possible.
- **server/twilio.ts:102** — SELECT from tenant-scoped table agent_knowledge queries pairing rows across all tenants (WHERE category='messaging_pairing' AND title = ...), intentionally loading candidate rows from every tenant without tenant_id constraint.
  - _Fix:_ Avoid unrestricted cross-tenant reads. Instead, redesign pairing storage so lookups can be safely scoped by tenant (or maintain an operator-managed global pairing table with explicit access controls), or require callers to provide tenant context and filter on tenant_id. If ambiguity detection across tenants is required, perform this in a tightly controlled, auditable operator/admin flow rather than a general webhook path.
- **server/wiring-invariants.ts:57** — SELECT from research_schedules (joined to research_programs) has no tenant_id constraint. Tenant-scoped tables must be filtered by the caller's tenant.
  - _Fix:_ Constrain the query to the appropriate tenant (e.g. WHERE s.tenant_id = :tenantId) or explicitly document/require an ADMIN tenant context. Prefer Drizzle's .where(eq(table.tenantId, tenantId)) or parameterized SQL with the tenantId passed in; do not rely on global reads against tenant tables.
- **server/wiring-invariants.ts:108** — SELECT name FROM research_programs executed with no tenant_id constraint. Reads from tenant-scoped program data must be tenant-scoped.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE tenant_id = :tenantId) or run under an explicit admin/inspection context and document that. Use Drizzle's API to pass tenantId as a parameter rather than broad unscoped selects.
- **server/wiring-invariants.ts:111** — SELECT name FROM personas executed with no tenant_id constraint. Personas may be tenant-scoped and require tenant filtering.
  - _Fix:_ Only query personas for the caller's tenant (WHERE tenant_id = :tenantId) or require an explicit admin tenant context. Use parameterized queries or Drizzle .where(eq(personas.tenantId, tenantId)).
- **server/wiring-invariants.ts:142** — SELECT name FROM research_programs (in checkProposalTargets) executed with no tenant constraint. This reads tenant-scoped program rows without filtering.
  - _Fix:_ Add a WHERE tenant_id = :tenantId (or equivalent Drizzle where(expr)) and ensure tenantId is supplied to the checker, or explicitly restrict this check to an admin tenant and make that intention explicit in code.
- **server/wiring-invariants.ts:180** — Complex SELECT joining research_programs, research_experiments, code_proposals and research_sessions has no tenant_id constraints on any of the referenced tables. Cross-tenant data leakage or mis-attribution is possible.
  - _Fix:_ Add tenant filters for each tenant-scoped table (e.g. p.tenant_id = :tenantId AND e.tenant_id = :tenantId AND rs.tenant_id = :tenantId AND cp.tenant_id = :tenantId) or run the check strictly under a documented admin tenant context. Use parameterized values rather than interpolating names and prefer Drizzle query builders to express these constraints.
- **server/wiring-invariants.ts:244** — SELECT count(*) FROM tool_performance (recent window) executed with no tenant_id constraint. tool_performance appears tenant-scoped and is read without filtering.
  - _Fix:_ Filter by tenant_id (WHERE tenant_id = :tenantId) or run under an explicit admin tenant context. Ensure the tenantId is provided to the function and used in the SQL/Drizzle where clause.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tool_performance (everCount) executed with no tenant filter. This reads cumulative tenant-scoped metrics across all tenants.
  - _Fix:_ Scope the COUNT to the relevant tenant (WHERE tenant_id = :tenantId) or otherwise only run cross-tenant aggregates with an explicit, auditable admin-only path. Prefer parameterized queries.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages (recent window) executed with no tenant_id constraint. messages are almost always tenant-scoped and must be filtered by tenant.
  - _Fix:_ Add WHERE tenant_id = :tenantId to the messages count queries. If this is intentionally a global system metric, make that explicit and run under an auditable admin-only routine; otherwise scope to caller tenant.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance (recent window) executed with no tenant_id constraint. Reads tenant-scoped performance data without filtering.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = :tenantId) or require/validate an admin tenant context before performing cross-tenant queries. Use parameters rather than broad unscoped selects.
- **server/wiring-invariants.ts:361** — sql.raw used to query information_schema.tables and then to SELECT COUNT/MAX from subsystem tables (e.g. tool_performance, knowledge_nudges) without tenant filtering. These subsystem tables are tenant-scoped and are queried globally.
  - _Fix:_ Avoid unscoped raw SELECTs against tenant tables. For the COUNT/MAX query on subsystem tables, add WHERE tenant_id = :tenantId and use parameterized Drizzle queries or whitelist table names and still apply tenant filters. If information_schema is used for existence checks, do so separately and still ensure subsequent data queries are tenant-scoped.
- **server/wiring-invariants.ts:417** — SELECT aggregate from tool_performance (failures in last 7d) executed with no tenant_id constraint. Aggregation over tenant-scoped performance data is unscoped.
  - _Fix:_ Constrain aggregation to the relevant tenant (GROUP BY tool_name WHERE tenant_id = :tenantId) or run under an explicit admin aggregation path. Use Drizzle's query builder with tenantId as a parameter.
- **server/wiring-invariants.ts:535** — sql.raw SELECT COUNT and pg_total_relation_size FROM memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) executed with no tenant_id constraint. These memory/chat tables are tenant-scoped but are queried across all tenants.
  - _Fix:_ Change these queries to count rows per tenant (WHERE tenant_id = :tenantId) or iterate tenants explicitly when admin-level full-system checks are required. Avoid unscoped raw SQL; use parameterization or Drizzle and ensure tenantId is provided and enforced.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory executed with no tenant_id constraint. The subsequent message even refers to 'empty across all tenants' but the query itself is unscoped.
  - _Fix:_ If you intend to check per-tenant graph_memory, add WHERE tenant_id = :tenantId. If you intend a cross-tenant admin check, make that explicit and ensure only privileged code runs it; prefer parameterized queries and consider paginating tenants to avoid accidental cross-tenant exposure.

### MEDIUM (39)

- **server/agentic-features.ts:599** — SELECT existing enrollment (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active') omits tenant_id — may detect/operate on another tenant's enrollment.
  - _Fix:_ Constrain the lookup by tenant_id (AND tenant_id = ${params.tenantId}) or otherwise verify the sequence/enrollment ownership before deciding presence/absence.
- **server/agentic-features.ts:274** — Subquery in prevSnaps uses SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY url, created_at DESC — the subquery omits tenant_id and can return ids from other tenants.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the inner subquery (and generally ensure all subqueries on tenant-scoped tables include the tenant predicate) so comparisons remain tenant-local.
- **server/auto-memorize.ts:163** — The initial messages query pulls rows across all tenants: SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ${windowStart} AND m.created_at <= ${windowEnd} ... LIMIT 400. This read on a tenant-scoped table is not filtered by tenant_id (the code later partitions results by tenant), meaning the raw DB read is unscoped.
  - _Fix:_ If this background task must scan messages for all tenants, ensure the code that executes prompts and any subsequent writes never mixes tenants (the per-tenant partitioning must be strictly enforced). As a safer alternative, iterate tenants and run a tenant-scoped SELECT (WHERE m.tenant_id = ${tenantId}) per tenant; or annotate the query to explicitly document/justify the cross-tenant scan and guard LLM prompts to operate only on rows for one tenant at a time.
- **server/auto-asset-capture.ts:207** — backfillProjectAssets selects assistant messages for a conversation without constraining by tenant: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'. While conversations were enumerated with tenant_id, this per-message read is not explicitly guarded by tenant_id, relying instead on conversation_id to be authoritative.
  - _Fix:_ When reading messages by conversation_id in a multi-tenant environment, include an explicit tenant constraint or otherwise assert the conversation's tenant prior to the SELECT (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}). This avoids any risk of inconsistent or mislinked rows causing cross-tenant access if referential integrity is not strictly enforced.
- **server/auto-project.ts:112** — SELECT from messages omits a tenant constraint (reads a tenant-scoped table without WHERE tenant_id = <tenant>).
  - _Fix:_ Constrain the messages query to the acting tenant. e.g. add AND tenant_id = ${tenantId} (or join via conversations ensuring owner and include messages.tenant_id = ${tenantId}) so the DB read is explicitly scoped to the caller's tenant.
- **server/auto-transcript.ts:46** — SELECT from messages uses only conversation_id and lacks an explicit tenant constraint on the messages table.
  - _Fix:_ Add an explicit tenant filter to the messages query (WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise ensure the query is parameterized to only return rows for the caller's tenant. Even if conversation ownership was checked earlier, every tenant-scoped table access should include the tenant_id guard per policy.
- **server/commitment-drafter.ts:146** — SELECT from tenant-scoped table `commitments` enumerates tenant_id across all rows without constraining to the caller's tenant or an explicit admin context (`SELECT DISTINCT tenant_id ... FROM commitments WHERE tenant_id > 0 ...`).
  - _Fix:_ If this is an admin-only background job, ensure it runs under a verified admin context and document that explicitly; otherwise avoid cross-tenant SELECTs. Prefer an explicit admin-only interface or add a caller-provided tenant filter. If enumeration is required, perform it in a controlled admin service (not in request-handling code) and ensure the code path is only invocable by a trusted system job.
- **server/email.ts:458** — startInboxPolling checks for an existing inbox message via SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without tenant_id (or inbox_id) constraint. This can return a message stored for a different tenant and cause skipping or misattribution.
  - _Fix:_ Scope the existence check to the relevant inbox/tenant (e.g. include inbox_id or tenant_id in the WHERE clause). Use the inboxId (or resolved tenantId) associated with the fetched message details to ensure you only deduplicate within the intended tenant/inbox.
- **server/email.ts:504** — backfillEmptyBodies selects rows from `inbox_messages` without any tenant filter: SELECT id, message_id FROM inbox_messages WHERE (...) ORDER BY id. This iterates messages across all tenants.
  - _Fix:_ If backfill is intended to operate per-tenant, add tenant_id (or inbox_id) scoping to the query. If it is intentionally global, restrict execution to an admin-only process and document the global access; otherwise risk cross-tenant processing/leakage.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot aggregates counts from tenant-scoped tables (messages, tool_performance) without tenant_id filters (SELECT COUNT(*) FROM messages ...; SELECT COUNT(DISTINCT tool_name) FROM tool_performance ...). These queries access tenant-scoped data globally.
  - _Fix:_ If the routine must operate per-tenant, accept a tenantId and filter these queries by tenant_id. If a global view is truly intended, make the admin-only nature explicit and isolate/authorize the operation; document the risk. Prefer explicit tenant scoping where possible to avoid accidental cross-tenant data access.
- **server/dormant-deprecation.ts:203** — getEverInvokedTools queries tool_performance without a tenant filter (SELECT DISTINCT tool_name FROM tool_performance). This reads a tenant-scoped table globally.
  - _Fix:_ Either add tenant scoping (accept tenantId and filter by tenant_id) or document & enforce that this function is an admin/global-only operation. Avoid unscoped selects on tenant-scoped tables in paths reachable by non-admin code.
- **server/dormant-deprecation.ts:213** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... without tenant_id filtering, reading tenant-scoped data across tenants.
  - _Fix:_ Scope the query by tenant_id when used for tenant-specific logic, or make this an explicitly authorized global operation. Prefer adding a tenantId parameter and applying WHERE tenant_id = ${tenantId}.
- **server/embeddings.ts:244** — Use of sql.raw() with a vector literal interpolated from a request-derived embedding: `1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})` and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}. The vector is derived from generateEmbedding(input), which is external input (provider/model) — using sql.raw() here can be an injection/isolation risk if embedding shape/content is not strictly validated.
  - _Fix:_ Avoid sql.raw interpolation of request-derived values. Instead bind the embedding as a parameter (use parameterized queries / Drizzle query bindings) or store the embedding in a parameterized placeholder and cast server-side. If sql.raw is required, keep strict runtime validation (as vecLiteral does) and prefer passing tenantId and other filters as parameters to ensure both injection and isolation safety.
- **server/embeddings.ts:559** — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge: `1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})` and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}. The vec value originates from generateEmbedding(query) (external input).
  - _Fix:_ Use parameterized binding for the vector instead of sql.raw interpolation, or provide a safe DB-side function that accepts a parameterized array and casts to vector. If continuing with sql.raw, keep and centralize strong numeric/shape validation (vecLiteral) and ensure tenant scoping remains present (it is here), but prefer parameterized queries for defense-in-depth.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT from tenant-scoped table `project_files` without an explicit tenant_id constraint. The query selects by project_id only: `SELECT id FROM project_files WHERE project_id = ${newProjectId} AND file_name = ${f.fileName} LIMIT 1` — this violates the invariant that reads against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Add an explicit tenant_id filter to the WHERE clause (e.g. AND tenant_id = ${tenantId}), or JOIN via the projects table and assert the project's tenant_id matches `tenantId` before using/claiming the row. Always make the tenant constraint explicit in every SELECT against tenant-scoped tables.
- **server/lib/ideabrowser-backfill.ts:115** — SELECT from tenant-scoped table `project_notes` without an explicit tenant_id constraint. The query selects by project_id only: `SELECT id FROM project_notes WHERE project_id = ${newProjectId} AND note = ${n.note} LIMIT 1`.
  - _Fix:_ Restrict the SELECT by tenant_id (e.g. AND tenant_id = ${tenantId}) or join to projects and ensure the referenced project belongs to the tenant. Always include an explicit tenant filter for reads against tenant-scoped tables.
- **server/lib/outcome-promotion.ts:158** — Global aggregation SELECT over deliverable_grade_events lacks a tenant_id filter (reads all tenants). This function groups by tenant_id to produce cross-tenant stats but issues an unconstrained SELECT against a tenant-scoped table.
  - _Fix:_ If this is intended admin/global maintenance work, run it under an explicit admin-only context and document that the job needs cross-tenant access. Prefer safer alternatives: (a) perform the aggregation in a per-tenant loop (SELECT per tenant with WHERE tenant_id = ...), or (b) gate this function so it only runs from a trusted admin worker and ensure audit logging. Otherwise, add explicit tenant scoping to comply with the per-request tenant isolation invariant.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL lacks tenant scoping. If skills is tenant-scoped, this can return other tenants' skills rows.
  - _Fix:_ If skills is a tenant-scoped table, restrict by tenant (e.g. AND (tenant_id = ${tenantId} OR tenant_id IS NULL) ) so you still allow global (tenant-less) shared skills but avoid returning other tenants' private rows.
- **server/persona-routing-outcomes.ts:44** — Query joins personas p ON p.id = ao.persona_id but does not verify p.tenant_id matches ao.tenant_id (or the supplied tenantId). This can surface persona names from the wrong tenant if IDs collide.
  - _Fix:_ Include tenant scoping on the joined personas table (e.g. JOIN personas p ON p.id = ao.persona_id AND p.tenant_id = ao.tenant_id) or otherwise ensure the JOIN only matches personas belonging to the same tenant.
- **server/routes.ts:1531** — Public endpoint /api/public/stats performs SELECTs against tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filtering.
  - _Fix:_ If this endpoint must report global aggregates, ensure it is explicitly documented and gated (admin-only or rate-limited) and that the aggregation is safe. Otherwise, add WHERE tenant_id = <caller tenant> (or run the aggregation in a privileged background job that records pre-aggregated, non-identifying metrics) so no per-tenant data is returned without an explicit tenant scope.
- **server/routes.ts:1559** — Public endpoint /api/public/architecture issues many db.execute(...) count/SELECT queries against potentially tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) without any tenant_id constraint.
  - _Fix:_ Restrict these queries to an admin-only path or add tenant_id filters when returning tenant-specific counts. If global cross-tenant metrics are required, compute them from an isolated telemetry pipeline or ensure the caller is an authorized admin and the response is scrubbing any tenant-identifying detail.
- **server/routes.ts:765** — Backfill code enumerates projects across all tenants (SELECT p.id, p.name, p.tenant_id, t.name ... FROM projects p JOIN tenants t ON t.id = p.tenant_id WHERE p.drive_folder_id IS NULL ...) with no tenant scoping. This touches tenant-scoped rows in bulk without an explicit tenant constraint.
  - _Fix:_ Run schema backfills in a privileged, offline migration job (not in a public request handler) and explicitly document and gate that operation under a platform-admin context. If backfill logic must run at runtime, iterate per-tenant and enforce tenant scoping for each step, or run under an admin-only internal task that is auditable.
- **server/routes.ts:1820** — Demo/admin check performs SELECT id, name, role FROM personas WHERE id = 2 without tenant scoping. personas is typically tenant-scoped and this reads a tenant row with no tenant_id constraint.
  - _Fix:_ Scope queries to a tenant (WHERE id = ${id} AND tenant_id = ${tenantId}) or, if the intent is to check a platform-global persona, introduce a clear global-persona table/flag and document it. For admin-only diagnostics, ensure the caller is an admin and that reading cross-tenant rows is intended and audited.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table 'projects' lacks tenant_id constraint in WHERE clause (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure only the caller's project row is modified.
- **server/seed.ts:3450** — INSERT into `project_conversations` uses project_id and conversation_id derived from an earlier unscoped query. Because `project_conversations` has no tenant_id, this can create cross-tenant links if the source SELECT is not tenant-scoped.
  - _Fix:_ Either add a tenant_id column to `project_conversations` and set it on INSERT, or only insert when you have explicitly verified both project_id and conversation_id belong to the same tenant (e.g. confirm both rows have the expected tenant_id before inserting). Ensure the upstream SELECT is tenant-scoped.
- **server/seed.ts:4926** — UPDATE on tenant-scoped table `agent_knowledge` uses `WHERE id = ${rows[0].id}` but omits tenant_id. Although the id was selected earlier with a tenant_id filter, the UPDATE itself lacks a tenant constraint and could be abused if id is untrusted or code is refactored.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. `WHERE id = $1 AND tenant_id = $2`) to enforce ownership at the write site. Prefer defensive checks: always combine id + tenant_id in WHERE clauses when updating tenant-scoped rows.
- **server/sentiment-detector.ts:167** — logSentimentEvent inserts sentiment_events with provided conversationId but does not verify that the conversation_id belongs to the same tenant. While tenant_id is set on the event, the code does not validate ownership of the referenced conversation row, enabling association of cross-tenant conversation ids.
  - _Fix:_ Before inserting, validate that the conversationId belongs to the tenant (e.g. SELECT id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} LIMIT 1) or otherwise verify ownership. Reject/log attempts where the conversation does not belong to the provided tenant.
- **server/storage.ts:838** — archiveExpiredMemories issues a global UPDATE on `memory_entries` (tenant-scoped) with no tenant_id filter — maintenance operation affects all tenants’ data in one statement.
  - _Fix:_ Either scope archival to a specific tenant (loop per-tenant or accept tenantId) or explicitly document & gate this as an admin-only maintenance operation. Prefer per-tenant jobs or include tenant_id in WHERE so accidental cross-tenant mutation is avoided.
- **server/storage.ts:855** — archiveStaleMemories issues a global UPDATE on `memory_entries` (tenant-scoped) with no tenant_id filter — modifies rows across tenants.
  - _Fix:_ Same as archiveExpiredMemories: scope by tenant or run as an admin/maintenance job with explicit safeguards; otherwise include tenant filter to avoid cross-tenant writes.
- **server/tools.ts:8879** — UPDATE against tenant-scoped table 'proposed_skills' lacks a tenant_id constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) does not include tenant scoping.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) ) or otherwise ensure the update is limited to the caller's tenantId. Use Drizzle's eq on the tenantId column to prevent cross-tenant updates.
- **server/tools.ts:8904** — UPDATE against tenant-scoped table 'proposed_skills' lacks a tenant_id constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) does not verify tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Always include tenant_id in UPDATE WHERE clauses for tenant-scoped tables.
- **server/tools.ts:8921** — UPDATE against tenant-scoped table 'proposed_skills' lacks a tenant_id constraint. db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id)) updates by id only without tenant scoping.
  - _Fix:_ Include tenant scoping in the WHERE clause (for example: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Enforce tenant_id in all UPDATE statements on tenant-scoped tables.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against tenant-scoped table 'browser_workflows' lacks an explicit tenant_id constraint in the WHERE clause: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`
  - _Fix:_ Include the tenant scope in the UPDATE so no cross-tenant row can be modified. e.g. use: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or the Drizzle equivalent .where(eq(table.tenantId, tenantId))). Ensure tenantId is the trusted ctx value.
- **server/tools/domains/files/handlers.ts:839** — UPDATE against 'projects' omits tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`
  - _Fix:_ Constrain UPDATE statements to the caller's tenant to enforce isolation, for example: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(projects.tenantId, tenantId))). Even when projId was fetched earlier, the UPDATE must still include the tenant constraint.
- **server/tools/domains/self-improvement/handlers.ts:61** — getExperimentsHandler forwards ctx.tenantId to getExperimentHistory without verifying the tenant context. If ctx.tenantId is undefined the backend may return/global-scope results or behave unexpectedly.
  - _Fix:_ Add a fail-closed tenant check at the top of the handler (if (!ctx.tenantId) return { error: 'Tenant context required' }) or otherwise validate ctx.tenantId is a positive number before invoking getExperimentHistory so reads are constrained to the caller's tenant.
- **server/tools/domains/scratchpad/handlers.ts:62** — readScratchpadHandler calls readDelegationScratchpad with ctx.tenantId without validating that tenant context exists. If ctx.tenantId is missing the read may be unconstrained or error in the backing implementation.
  - _Fix:_ Add a guard to verify ctx.tenantId (if (!ctx.tenantId) return { error: 'Tenant context required' }) before calling readDelegationScratchpad so reads are scoped to the caller's tenant.
- **server/video-job-runner.ts:178** — Upsert (INSERT ... ON CONFLICT DO UPDATE) into tenant-scoped video_jobs does not include tenant_id equality in the onConflict update predicate. The onConflictDoUpdate.where only compares updatedAt, so the update is not constrained to the row's tenant.
  - _Fix:_ Include tenant scoping in the conflict-update predicate (e.g. onConflictDoUpdate.where: and(eq(videoJobs.tenantId, row.tenantId), lt(videoJobs.updatedAt, row.updatedAt))), or otherwise ensure the update only affects rows that match the same tenant_id as the incoming row.
- **server/video-job-runner.ts:204** — SELECT from tenant-scoped table video_jobs reads cancelRequested by jobId only (WHERE job_id = ...), without constraining by tenant_id. isCancelRequested() returns a flag based solely on jobId.
  - _Fix:_ Require tenantId when checking cancelRequested or read the job row and verify its tenant_id matches the caller before acting. Change isCancelRequested signature to accept tenantId and use WHERE job_id = ... AND tenant_id = ... (or validate after SELECT/RETURNING).
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without a tenant_id constraint. The code relies on comparing the returned tenant to the claimed tenant, but the initial query is not scoped to the caller's tenant.
  - _Fix:_ Scope the query to the claimed tenant (e.g. SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1) or fetch and then verify the returned tenant_id equals the claimed tenant, ensuring any DB read/write is constrained to expected tenant ownership.

### Suppressed — confirmed false positives (187)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights SET ... WHERE status = 'new' AND category IN (...) updates tenant-scoped rows for ALL tenants (no tenant_id constraint), potentially auto-applying insights across tenants.
  - _Why false positive:_ Platform-wide maintenance sweep updates status fields across tenants and carries tenant_id per returned row for downstream handling; no tenant-facing mixed output.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — runDueWakes uses an UPDATE that selects rows via an inner SELECT without constraining by tenant_id. The subquery SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... can return rows from any tenant, so the outer UPDATE (which updates those ids) may move/claim schedules belonging to other tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally claims due rows across all tenants and processes each row with its own tenant_id when emitting/finalizing.
- **server/chat-engine.ts:572** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` omits a tenant filter. The query `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...` does not include `tenant_id = ${tenantId}` — relying on earlier project verification is not allowed by the invariant (every DB access to tenant-scoped tables must include an explicit tenant constraint).
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; parent project was verified with tenant_id just above.
- **server/chat-engine.ts:583** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` omits a tenant filter. The query `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...` lacks `tenant_id = ${tenantId}` and therefore can return cross-tenant rows if `project_id` is misused.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through verified parent project_id.
- **server/chat-engine.ts:632** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` omits a tenant filter. The query `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...` does not include `tenant_id = ${tenantId}` (even though `c.id` was obtained from a prior tenant-scoped query).
  - _Why false positive:_ conversation ids come from prior query joining project_conversations to conversations with c.tenant_id = tenantId in same call path.
- **server/chat-engine.ts:650** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` omits a tenant filter. The query `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC` lacks `tenant_id = ${tenantId}` and can expose messages across tenants if IDs are misused.
  - _Why false positive:_ messages are fetched for conversation ids already constrained by earlier same-path query to conversations.tenant_id = tenantId.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE statements in permanentlyPurgeSoftDeleted() delete tenant-scoped rows without tenant_id predicates: DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}. These destructive writes lack tenant scoping and can delete rows across tenants if IDs collide or schema changes, violating tenant isolation and causing data loss.
  - _Why false positive:_ Expired-row housekeeping loop operates platform-wide but each conversation carries its own tenant_id and purge only deletes housekeeping-targeted rows, returning no mixed tenant data.
- **server/embeddings.ts:687** _(system-sweep)_ — SELECT from agent_knowledge in backfillMissingKnowledgeEmbeddings is not tenant-scoped: `SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ...` has no tenant_id filter. This will read rows across all tenants.
  - _Why false positive:_ Global backfill sweep updates per-row housekeeping field (embeddings) and does not return tenant data to a tenant-facing surface.
- **server/embeddings.ts:716** _(system-sweep)_ — backfillEmbeddingVecs reads and updates memory_entries without tenant scoping — SELECT (lines ~716-720) and UPDATE (line ~726) use WHERE id = ${row.id} but no tenant_id filter. This can update embedding_vec for rows that belong to other tenants.
  - _Why false positive:_ Platform-wide embedding_vec backfill on memory_entries only mutates each row's housekeeping field by its own id; no cross-tenant output or mixing shown.
- **server/embeddings.ts:732** _(system-sweep)_ — backfillEmbeddingVecs reads and updates agent_knowledge without tenant scoping — SELECT (lines ~732-737) and subsequent UPDATE (line ~743) lack tenant_id filters, allowing cross-tenant reads/updates.
  - _Why false positive:_ Platform-wide embedding_vec backfill on agent_knowledge is a maintenance sweep mutating per-row housekeeping data only, not serving mixed tenant data.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table `code_proposals` (SET status = 'archived_stale') uses id = ANY(...) but does not include tenant_id in the WHERE clause — this can modify rows for other tenants.
  - _Why false positive:_ Stale-proposal archival is a maintenance sweep across tenants that only updates status on the same rows selected; no tenant-facing data mixing.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a write against a tenant-scoped table without constraining by tenant_id — this can disable tasks for other tenants.
  - _Why false positive:_ Disabling stale heartbeat tasks is platform housekeeping across tenants, mutating only enabled/status-like state per selected row.
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} — no tenant_id constraint. This can modify another tenant's schedule row identified only by id.
  - _Why false positive:_ Heartbeat cron advances research_schedules in a platform-wide worker; it updates only scheduling metadata on the row being processed, with row-carried tenant_id already used for execution.
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id} — missing tenant_id constraint, allowing cross-tenant writes.
  - _Why false positive:_ This is the same maintenance loop updating last_run_at/next_run_at scheduling fields for the current schedule row, not exposing mixed tenant data.
- **server/heartbeat.ts:111** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} — no tenant_id constraint (cross-tenant write risk).
  - _Why false positive:_ Platform-wide schedule dispatcher mutates only next_run_at housekeeping on the selected schedule row during cron processing.
- **server/heartbeat.ts:119** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id in WHERE. Recovery path can update other tenants' rows.
  - _Why false positive:_ Recovery path in the same scheduler loop only repairs next_run_at metadata for the current row; no tenant-facing cross-mixing.
- **server/heartbeat.ts:265** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — no tenant_id constraint. This directly mutates a tenant-scoped heartbeat_tasks row by id only.
  - _Why false positive:_ Startup cleanup iterates heartbeat task rows platform-wide and normalizes only model metadata on each selected task as housekeeping.
- **server/heartbeat.ts:269** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant_id filter, permitting cross-tenant modification.
  - _Why false positive:_ Same startup maintenance loop updates only model metadata for reflection tasks, with no mixed tenant output or request-driven cross-tenant access.
- **server/heartbeat.ts:2950** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint. This can alter another tenant's task by id alone.
  - _Why false positive:_ newTask was just created in the same call path with tenantId, and update targets that freshly returned id only
- **server/heartbeat.ts:3402** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — repeated occurrence with no tenant_id filter, allowing cross-tenant mutation.
  - _Why false positive:_ newTask is created immediately above with tenantId in same flow; update only flips status on that just-created row
- **server/lib/action-ledger.ts:305** _(system-sweep)_ — UPDATE on tenant-scoped table `action_attempts` performed without any tenant_id predicate (sweepStaleAttempts updates rows across tenants).
  - _Why false positive:_ bounded reconciler sweep intentionally updates stale rows across tenants for housekeeping only and returns only count
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT INTO memory_links omits tenant_id entirely. Tenant-scoped tables must have tenant_id set on INSERT to prevent cross-tenant poisoning and orphan links.
  - _Why false positive:_ memory_links has no tenant_id and is isolated transitively through source/target memory_entries parent rows
- **server/minerva-planner.ts:459** _(upstream-verified)_ — Rollback UPDATE on plans (UPDATE plans SET status='awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising') lacks tenant_id constraint. This can roll back or alter plans for other tenants.
  - _Why false positive:_ update uses planId after earlier loaded row with tenant_id and CAS transition; rollback targets same verified row/state
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT queries all active oauth_subscriptions rows without scoping by tenant_id (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE). This reads subscriptions (access/refresh tokens) across tenants.
  - _Why false positive:_ startup validation intentionally scans all tenants' subscriptions and handles each row by its own tenant_id for housekeeping
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — update omits tenant_id guard (write can affect other tenants' rows)
  - _Why false positive:_ t.id comes from prior SELECT scoped to ht.tenant_id = tenantId, so update only affects rows already tenant-filtered
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} — missing tenant_id constraint (write may modify another tenant's desk)
  - _Why false positive:_ desk.id is sourced from immediately preceding SELECT constrained by ad.tenant_id = tenantId
- **server/proposal-verifier.ts:111** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at etc.) omits tenant_id constraint — write may affect another tenant's proposal
  - _Why false positive:_ server-side verifier worker updates proposal by internal proposalId queue/worker primary key, not a cross-tenant request boundary
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking 'hallucinated') lacks tenant scoping
  - _Why false positive:_ internal async verifier/rebase worker mutates one proposal by server-side primary key
- **server/proposal-verifier.ts:373** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', verification_status = 'skipped' WHERE id = ${proposalId} — missing tenant_id (mutation without tenant scoping)
  - _Why false positive:_ internal verifier worker marks proposal status by server-generated/global proposal id, not tenant-facing selector
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} — write lacks tenant_id constraint
  - _Why false positive:_ rebase persistence runs inside server worker on specific proposal primary key
- **server/recurring-messages.ts:300** _(upstream-verified)_ — UPDATE agent_knowledge SET ... WHERE id = ${r.id} executes without restricting tenant_id — risk of updating rows belonging to other tenants.
  - _Why false positive:_ r.id comes from rows already carrying r.tenant_id and used within same loop over previously selected jobs
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} is executed without tenant_id constraint — allows modifying another tenant's memory entry.
  - _Why false positive:_ storedId comes from prior SELECT on memory_entries scoped by input.tenantId and persona/category/dedup prefix
- **server/research-engine.ts:163** _(system-sweep)_ — UPDATE research_sessions SET ... WHERE status = 'running' ... started_at < ${staleThreshold} executes with no tenant_id constraint — may mark sessions for other tenants as completed.
  - _Why false positive:_ zombie-session cleanup is cross-tenant housekeeping updating status fields only and returns aggregate/internal ids, not tenant data mixing
- **server/routes.ts:4331** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' that does not set tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, risking notes being written without a tenant association or associated with the wrong tenant.
  - _Why false positive:_ project_notes has no tenant_id and insert is transitively scoped via project_id parent
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author) does not set tenant_id. Tenant-scoped table inserts must include tenant_id to prevent cross-tenant writes.
  - _Why false positive:_ project_notes has no tenant_id and project ownership is verified just above via projects.tenant_id
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT INTO project_files (project_id, file_name, ...) omits tenant_id. Inserting into a tenant-scoped table without tenant_id allows cross-tenant contamination.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped through validated tenant-owned project_id
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} lacks tenant_id in the WHERE clause. Updating a tenant-scoped row without tenant filtering can modify other tenants' data.
  - _Why false positive:_ same handler already verified project id belongs to tenant before updating that exact project row
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT INTO project_files (project_id, file_name, ...) (multipart upload) does not include tenant_id. Tenant-scoped INSERT missing tenant identifier.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped through validated tenant-owned project_id
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} (after multipart upload) omits tenant_id in the WHERE clause.
  - _Why false positive:_ same request path validated project ownership with tenant_id before updating same project row
- **server/routes/projects.ts:371** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not constrain by tenant_id. Deleting from a tenant-scoped table without tenant filtering may delete another tenant's data.
  - _Why false positive:_ delete targets child rows of a project after same handler verified the parent project belongs to tenant
- **server/sculptor.ts:93** _(internal-worker-pk)_ — UPDATE on tenant-scoped table sculptor_sessions without constraining by tenant_id (WHERE id = ${session.id} only). This allows cross-tenant mutation if id is guessed or tampered with.
  - _Why false positive:_ update uses server-created session.id from prior insert within same internal function, not untrusted request input
- **server/sculptor.ts:100** _(internal-worker-pk)_ — UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id} — missing tenant_id constraint AND the code writes a foreign conversation_id derived from an external run without verifying that the conversation belongs to the caller's tenant.
  - _Why false positive:_ session row is updated by server-generated primary key inside internal workflow; conversationId comes from internal launch call with same tenantId
- **server/sculptor.ts:155** _(internal-worker-pk)_ — UPDATE sculptor_sessions (monitorSession final update) uses WHERE id = ${sessionId} with no tenant_id constraint. monitorSession receives tenantId but does not assert it in the UPDATE — risk of cross-tenant modification.
  - _Why false positive:_ background monitor updates sculptor session by internal sessionId passed from creator, not directly from request boundary
- **server/sculptor.ts:346** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} — missing tenant_id constraint. reviewSessionWork earlier authenticated tenant ownership for the SELECT, but the later UPDATE does not re-assert tenant scoping.
  - _Why false positive:_ same function previously selects sculptor_session by id and tenant_id before updating that same session
- **server/seed-cleanup.ts:15** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true — no tenant_id constraint. This will disable delegation tasks across all tenants rather than a single intended tenant.
  - _Why false positive:_ boot-time cleanup intentionally disables runaway heartbeat tasks across tenants as housekeeping; no tenant data returned or mixed
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true — missing tenant_id constraint. This can disable session-logger tasks across tenants indiscriminately.
  - _Why false positive:_ seed cleanup globally disables stray session-logger heartbeat tasks as maintenance only, without exposing tenant data
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true — missing tenant_id constraint. This will disable many heartbeat task rows across all tenants rather than a specific tenant.
  - _Why false positive:_ platform-wide maintenance sweep disables orphaned repo-examination heartbeat tasks; housekeeping mutation only
- **server/seed.ts:3097** _(system-sweep)_ — UPDATE on tenant-scoped table `research_programs` lacks tenant constraint: `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}` updates a row by id without verifying tenant ownership (no `tenant_id = ...`), risking cross-tenant modification.
  - _Why false positive:_ seed reconciler iterates all active research_programs and fixes invalid model values as global housekeeping, not tenant-facing data mixing
- **server/seed.ts:4686** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without a tenant_id constraint: `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'`. This can delete rows across all tenants.
  - _Why false positive:_ boot-time purge of stale auto-disabled heartbeat tasks is platform-wide housekeeping delete with no tenant data disclosure
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT INTO project_files (...) VALUES (...) omits tenant_id, violating the requirement that every INSERT into tenant-scoped tables include the caller's tenant.
  - _Why false positive:_ project_files has no tenant_id and row is scoped transitively via validated parent project_id on tenant-scoped projects
- **server/tools.ts:2996** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and insert is correctly isolated through prior ownership check on parent project
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The code INSERTs (project_id, conversation_id) without recording tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id by schema and insert is gated by tenant-scoped project/conversation ownership checks.
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` (link endpoint) omits tenant_id. INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) has no tenant_id column set.
  - _Why false positive:_ project_conversations has no tenant_id by schema; project ownership and assertConversationInTenant enforce same-tenant parent rows before insert.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (reads experiments across all tenants).
  - _Why false positive:_ Global maintenance loop processes running experiments across tenants using each row's tenant_id and updates rows per-row without returning mixed tenant data.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_activity` uses only the activity id (WHERE id = ...) with no tenant_id constraint — allows cross-tenant updates.
  - _Why false positive:_ completeActivity is a server-side helper updating by server-generated activity primary key, not shown as taking id from an untrusted tenant request boundary.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting is unscoped (no tenant_id filter) — reads pending high-priority insights across all tenants.
  - _Why false positive:_ Durability sweep intentionally scans all tenants but carries forward each row's tenant_id and routes/updates per row without mixing tenant outputs.
- **server/agentic-engines.ts:344** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` (COUNT of calls) lacks a tenant_id WHERE clause — returns data across tenants.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column NO in schema, so global aggregate counts are intentional platform-wide data.
- **server/agentic-engines.ts:519** _(global-table)_ — Aggregated heartbeat performance query (FROM heartbeat_logs GROUP BY task_name, status) is not tenant-scoped — aggregates logs across all tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id by schema; platform-wide aggregated task metrics come from a global table.
- **server/agentic-features.ts:525** _(upstream-verified)_ — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} has no tenant_id constraint — allows updating another tenant's lead row by id.
  - _Why false positive:_ Updated rows come from a prior SELECT on lead_enrichments scoped by params.tenantId, so lead.id was already tenant-owned in the same call path.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} is missing tenant_id constraint — updates a row by id only and can affect another tenant's enrollment.
  - _Why false positive:_ row.id comes from an earlier query constrained by e.tenant_id = params.tenantId, so the update targets a tenant-verified enrollment from the same flow.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE in expireStaleApprovals: db.update(agentApprovals).set(...).where(status='pending' AND expiresAt < now()) updates approvals across all tenants (no tenant_id predicate) and then acts on returned rows.
  - _Why false positive:_ Expiry is a platform-wide housekeeping sweep over stale pending approvals; returned rows carry tenantId and follow-on run updates are applied per row.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' scans a tenant-scoped table across all tenants with no tenant_id constraint. This reads tenant-scoped data for every tenant (cross-tenant access) rather than restricting to the caller's tenant.
  - _Why false positive:_ Heartbeat maintenance sweep iterates all tenant rows and handles each with its own row-carried tenant_id, emitting per-tenant events only.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Raw SELECT scans tenant-scoped tables (agent_runs JOIN agent_approvals) with NO tenant_id filter — returns rows across all tenants.
  - _Why false positive:_ Background resume sweep scans pending runs across tenants but carries forward each row's tenant_id and performs housekeeping state transitions only.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_runs (appendStep) uses WHERE id = runId with NO tenant_id constraint — allows updating a run row from any tenant context.
  - _Why false positive:_ Helper updates agent_runs by globally unique server-generated run id in internal agent worker code, not by untrusted request-supplied tenant context.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_runs (updateRunState) uses WHERE id = runId with NO tenant_id constraint.
  - _Why false positive:_ Internal run-state mutation by server-generated agent run primary key; scoped by trusted internal workflow rather than caller-controlled cross-tenant input.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_runs (replaceRunState) uses WHERE id = runId with NO tenant_id constraint.
  - _Why false positive:_ Internal replacement of agent run state uses globally unique run id from server-side orchestration, not an untrusted tenant boundary.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_runs (completeRun) uses WHERE id = runId with NO tenant_id constraint.
  - _Why false positive:_ Run completion is an internal worker transition on server-generated run primary key; not shown as reachable from untrusted cross-tenant input.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_runs (failRun) uses WHERE id = runId with NO tenant_id constraint.
  - _Why false positive:_ Failure transition updates agent_runs by trusted server-generated run id inside internal agent orchestration.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_runs (pauseRun) uses WHERE id = runId with NO tenant_id constraint.
  - _Why false positive:_ Pause transition is an internal agent workflow update keyed by globally unique run id, not a tenant-facing request parameter.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id ... WHERE id = ${c.conversation_id} ... executes without scoping by tenant_id. This is a write to a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ Backfill sweep derives c.tenant_id from joined rows and only performs per-row housekeeping link repair before tenant-specific transcript generation.
- **server/capability-review.ts:491** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `capability_reviews` is executed without constraining by tenant_id. markReviewReused() runs `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` but does not include a tenant_id check, violating the tenant-isolation invariant.
  - _Why false positive:_ Best-effort internal attribution updates one capability_reviews row by server-held pending.reviewId from in-memory workflow state, not from untrusted request input.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` without tenant filter: the heartbeat scanner queries rows across all tenants (`FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`) and thus reads tenant-scoped data without explicit tenant scoping.
  - _Why false positive:_ Boot/scheduled platform-wide scanner intentionally fans in across tenants, processes each row with its own tenant_id, and emits only redacted admin digest content.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE without tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` updates a credential row by id with no tenant filter, permitting cross-tenant mutation.
  - _Why false positive:_ Same function first fetches the credential with getCredential(id, tenantId); subsequent update uses that verified row id only.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE without tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` updates last_used_at with no tenant check after getCredentialForSite returned a row — this mutates by id without verifying tenant scope in the UPDATE.
  - _Why false positive:_ Same function first resolves cred via getCredentialForSite(siteUrl, tenantId); update then touches only that verified row id.
- **server/data-protection.ts:117** _(system-sweep)_ — SELECT FROM conversations in permanentlyPurgeSoftDeleted() (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) runs without constraining tenant_id. Reading tenant-scoped rows without a tenant predicate can expose data across tenants (and is inconsistent with the stated application-level isolation model).
  - _Why false positive:_ This is a cross-tenant housekeeping purge loop over expired soft-deleted rows, handled per row with carried tenant_id and not returned to tenant-facing output.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake updates `dfy_intake_forms` with .where(eq(dfyIntakeForms.id, form.id)) but does not include tenant_id in the WHERE. This allows updating a form row belonging to another tenant if an id is used from a different tenant.
  - _Why false positive:_ submitDfyIntake updates the form id obtained immediately from getDfyIntakeByToken; the token lookup is the governing capability for this flow.
- **server/felix-brain.ts:19** _(system-sweep)_ — rehydrateTaskStateOnBoot executes a raw SELECT on agent_runs without constraining by tenant_id. This pulls rows for all tenants into process memory (tenant_id is selected but not used to scope the query), violating tenant isolation.
  - _Why false positive:_ Boot-time rehydration intentionally scans all pending/running rows platform-wide into internal process state; no tenant-facing mixed output is produced here.
- **server/firecrawl.ts:214** _(upstream-verified)_ — UPDATE scraped_pages uses WHERE eq(scrapedPages.id, existing[0].id) but omits tenant scoping (tenant_id). This is a write against a tenant-scoped table without a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ The row id being updated was selected just above with tenantId and url predicates, so the target row was already tenant-verified in the same call path.
- **server/firecrawl.ts:344** _(upstream-verified)_ — Within the crawl loop, UPDATE scraped_pages is executed with WHERE eq(scrapedPages.id, existing[0].id) but no tenant_id condition. This writes to a tenant-scoped table without constraining the tenant.
  - _Why false positive:_ The row id being updated was selected immediately above with tenantId and url predicates, so ownership is verified earlier in the same path.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT ... FROM projects WHERE eq(projects.id, projectId) reads a tenant-scoped table without a tenant_id filter. Although ownership is checked in application logic afterwards, the raw SELECT itself has no tenant constraint and may expose tenant-scoped data.
  - _Why false positive:_ Row is immediately ownership-checked against caller tenant before any sensitive return/use; mismatch throws fail-closed.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects SET driveFolderId/driveFolderUrl ... WHERE eq(projects.id, projectId) updates a tenant-scoped table without including tenant_id in the WHERE clause, violating the requirement that each write be tenant-constrained in SQL.
  - _Why false positive:_ Same call path verifies selected project.tenantId equals tenantId just above before this PK update executes.
- **server/google-drive.ts:826** _(upstream-verified)_ — In uploadAndShare, code SELECTs project row with WHERE eq(projects.id, params.projectId) but no tenant_id filter. This reads a tenant-scoped table without SQL-level scoping before making decisions based on the row.
  - _Why false positive:_ Function checks fetched project's tenantId against params.tenantId before proceeding; mismatch returns error before folder resolution/write path.
- **server/google-drive.ts:1004** _(upstream-verified)_ — searchDriveFiles performs SELECT { tenantId: projects.tenantId } FROM projects WHERE eq(projects.id, params.projectId) with no tenant_id constraint. This is a read against the projects table without SQL-level tenant scoping (ownership is then checked in application code).
  - _Why false positive:_ This read exists solely to verify project ownership in the same block; tenant mismatch returns immediately before any tenant-facing search occurs.
- **server/google-drive.ts:1066** _(upstream-verified)_ — During Drive-API fallback the code SELECTs projects.driveFolderId WHERE eq(projects.id, params.projectId) without a tenant_id filter. This accesses a tenant-scoped table without SQL scoping before using the folder id to scope subsequent Drive searches.
  - _Why false positive:_ Earlier in same function, project ownership was already verified against params.tenantId before fallback folder lookup.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` without constraining by tenant_id (global read). This can leak other tenants' proposals to the audit job.
  - _Why false positive:_ Global TTL audit/archival maintenance sweep; acts on rows individually and only emits admin audit findings/housekeeping updates.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from `heartbeat_tasks` lacks a tenant_id filter — reading tenant-scoped task rows across all tenants.
  - _Why false positive:_ Global maintenance scan for stuck heartbeat tasks; per-row housekeeping/admin findings only, no tenant-facing mixed content.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT INTO presenter_slide_images does not set tenant_id (and uses only session_id). If presenter_slide_images is tenant-scoped, this INSERT can create rows not associated with the caller's tenant; additionally the code does not validate that session_id belongs to the caller's tenant.
  - _Why false positive:_ presenter_slide_images is keyed by session_id and referenced schema shows presenter_sessions is tenant-scoped; child row has no indicated tenant_id and is isolated transitively via session.
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT * FROM research_schedules is executed with no tenant_id filter. This reads rows across all tenants from a tenant-scoped table (research_schedules).
  - _Why false positive:_ Heartbeat scheduler intentionally scans due schedules across tenants and processes each row with its own sched.tenant_id.
- **server/heartbeat.ts:686** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ORDER BY ... LIMIT 10 executes without a tenant_id constraint. Reading pending events across tenants without scoping can leak or mix tenant data.
  - _Why false positive:_ Background daemon processes pending events across tenants using each event's own tenant_id; no mixed tenant output returned to users.
- **server/heartbeat.ts:1856** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ... has no tenant_id filter. This reads heartbeat_logs across tenants.
  - _Why false positive:_ Referenced schema notes heartbeat_logs has no tenant_id column, indicating a platform-global log table.
- **server/lib/action-ledger.ts:272** _(system-sweep)_ — SELECT in listUnknownAttemptsAllTenants queries tenant-scoped table `action_attempts` without a tenant_id filter (cross-tenant read).
  - _Why false positive:_ Function is explicitly a cross-tenant reconciler helper; rows carry tenant_id and downstream handling uses each row's own tenant for scoped writes.
- **server/lib/ideabrowser-backfill.ts:104** _(fk-transitive)_ — INSERT into `project_files` does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (${newProjectId}, ${f.fileName}, ...)`. Per invariant, tenant-scoped tables require tenant_id to be passed explicitly (columns are NOT NULL with no default).
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped through tenant-scoped projects via project_id
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into `project_notes` does not set tenant_id: `INSERT INTO project_notes (project_id, note, author, created_at) VALUES (${newProjectId}, ${n.note}, ...)`. This violates the invariant that INSERTs to tenant-scoped tables must include tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through tenant-scoped projects via project_id
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT from tenant-scoped table `scheduled_posts` without any tenant_id constraint (system-level poll uses no tenant filter).
  - _Why false positive:_ global scheduler intentionally polls due rows across tenants and returns per-row tenant_id for isolated handling, not mixed tenant output
- **server/lib/scheduled-post-runner.ts:135** _(system-sweep)_ — UPDATE against tenant-scoped table `scheduled_posts` uses WHERE id = ${row.id} only (no tenant_id filter).
  - _Why false positive:_ worker updates housekeeping/status fields on rows it previously locked in a platform-wide scheduler loop; no tenant data returned or mixed
- **server/lib/scheduled-post-runner.ts:217** _(system-sweep)_ — UPDATE against tenant-scoped table `scheduled_posts` uses WHERE id = ${row.id} only (no tenant_id filter) after per-platform publish processing.
  - _Why false positive:_ post-publish status update is part of global maintenance runner handling each picked row independently with row-carried tenant_id
- **server/lib/scheduled-post-runner.ts:244** _(system-sweep)_ — UPDATE against tenant-scoped table `scheduled_posts` in exception-handling path uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id.
  - _Why false positive:_ exception-path release/update is within the same platform-wide scheduler loop and only mutates row status/lock fields per picked row
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` without any tenant_id constraint (global audit scan).
  - _Why false positive:_ read-only global audit sweep over stuck agent_runs reports bounded per-row findings with carried tenant_id; intentional cross-tenant housekeeping
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT from tenant-scoped table `mind_tickets` without any tenant_id constraint (global audit scan).
  - _Why false positive:_ Boot/maintenance stuck-task scan intentionally reads across tenants, carries row tenant_id per item, and only reports housekeeping status.
- **server/memory-graph.ts:80** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} does not include a tenant_id constraint. This updates a tenant-scoped table row without ensuring tenant isolation.
  - _Why false positive:_ catId comes from ensureCategory/select+insert already scoped by tenantId in same call path, so update by that verified row id is tenant-owned.
- **server/memory-graph.ts:196** _(fk-transitive)_ — SELECT 1 FROM memory_links ... LIMIT 1 reads from memory_links without any tenant_id constraint. If memory_links is tenant-scoped this allows checking existence/fingerprinting across tenants.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries already filtered to the same tenant; isolation is transitive via source/target memory ids.
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} performs an update on a tenant-scoped table without constraining tenant_id. This can modify rows belonging to other tenants if an id is reused or guessed.
  - _Why false positive:_ mem.id is selected a few lines above from memory_entries with tenant_id = tenantId in the same loop, so this update targets only tenant-verified rows.
- **server/memory-intelligence.ts:272** _(internal-worker-pk)_ — db.execute(...) UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} updates a tenant-scoped table without filtering by tenant_id. The entry.id is caller-controlled (created above) but lack of tenant check risks cross-tenant modification if misused.
  - _Why false positive:_ Async server-side worker updates newly created memory_entries row by server-generated entry.id, not an untrusted request-supplied id.
- **server/memory-intelligence.ts:315** _(internal-worker-pk)_ — db.execute(...) UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in the update branch) updates memory_entries without constraining tenant_id, allowing potential cross-tenant mutation.
  - _Why false positive:_ Same internal post-create worker pattern: update uses server-generated entry.id for the row just inserted, not caller-controlled cross-tenant input.
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checks ownership with assertProjectInTenant but the INSERT omits tenant_id, violating the invariant that every insert must explicitly set tenant_id (columns are NOT NULL/no default).
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; assertProjectInTenant verifies parent ownership before insert.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. As above, the code calls assertProjectInTenant but then inserts without setting tenant_id, breaking the tenant-isolation invariant.
  - _Why false positive:_ project_files has no tenant_id column and is transitively isolated through verified tenant-owned project parent.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — returns rows across tenants.
  - _Why false positive:_ Startup auto-reconnect sweep intentionally enumerates subscriptions across tenants and then processes each row with its own tenant_id; housekeeping only.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} updates by id only, without verifying tenant_id. In a multi-tenant DB this can modify another tenant's row if ids collide or if caller is untrusted.
  - _Why false positive:_ Startup validation loop processes subscriptions platform-wide using each row's own tenant_id; this is housekeeping only and not tenant-facing data mixing.
- **server/oauth-subscriptions.ts:966** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} (second instance) lacks tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ Same startup expiry sweep deactivates current row by server-read id during maintenance; per-row tenant_id is carried and no cross-tenant output is returned.
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} (third instance) is not constrained by tenant_id and may affect rows from other tenants.
  - _Why false positive:_ Same non-tenant-facing maintenance sweep marks stale subscriptions inactive per row; operates across tenants intentionally without mixing content.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} is missing a tenant_id constraint. Reading a persona by id without tenant scoping can return a persona from a different tenant.
  - _Why false positive:_ Schema notes personas has no tenant_id column, so base persona row is intentionally non-tenant-scoped/global; tenant-scoped adjunct data is filtered by tenant_id separately.
- **server/pace-control.ts:85** _(global-table)_ — Queries against heartbeat_logs (COUNT ...) omit any tenant filter — e.g. SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}. If heartbeat_logs is tenant-scoped, this returns aggregated activity across all tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id column and comments describe global pace caps, so cross-tenant counting is intentional platform-global behavior.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot performs SELECT COUNT ... FROM heartbeat_logs WHERE created_at > ... without tenant scoping, returning global heartbeat logs across tenants if the table is tenant-scoped.
  - _Why false positive:_ heartbeat_logs is not tenant-scoped per schema; snapshot is an operator/global metric over a global table.
- **server/pace-control.ts:147** _(global-table)_ — personaRows query SELECT COALESCE(persona_name, 'unknown') AS persona_name, COUNT(*)::int AS n FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name has no tenant_id filter and will aggregate across tenants.
  - _Why false positive:_ Per-persona aggregation is over heartbeat_logs, which the schema marks as non-tenant-scoped global telemetry.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog updates plans with: UPDATE plans ... WHERE id = ${planId} — no tenant_id constraint. Updating a tenant-scoped table by id only can modify another tenant's row if planId is misused or an attacker supplies it.
  - _Why false positive:_ appendExecutionLog is an internal executor helper operating on server-generated planId within plan-executor flow, not directly from an untrusted tenant request boundary.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' ... WHERE id = ${planId} — missing tenant_id filter. This can change the status of a plan belonging to another tenant if planId is used cross-tenant.
  - _Why false positive:_ Same call path just claimed the plan row via UPDATE ... WHERE id = planId AND status='approved' RETURNING tenant_id; subsequent failure update acts on that claimed server-owned row.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final plan state UPDATE (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) lacks a tenant_id constraint. This writes to a tenant-scoped table without scoping to the plan's tenant.
  - _Why false positive:_ Final status write occurs after the executor successfully claimed and held the specific plan row earlier in the same call path, with authoritative tenant_id carried in row.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') omits tenant_id. Unscoped updates on plans can affect other tenants' plans.
  - _Why false positive:_ Server-side executor error path updates a plan by server-held primary key planId; not shown as request-derived cross-tenant input.
- **server/plan-executor.ts:775** _(system-sweep)_ — Boot-time recovery UPDATE flips executing→approved across all rows: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... — this runs unscoped across tenants.
  - _Why false positive:_ Boot-time recovery intentionally sweeps stale/pending plans across tenants, only mutating status fields and returning ids/counts for restart logging.
- **server/process-governor.ts:121** _(fk-transitive)_ — SELECT subqueries count heartbeat_logs without constraining tenant_id (reads tenant-scoped heartbeat_logs across tenants)
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted by task_id from tenant-scoped heartbeat_tasks rows, so isolation is transitive via the parent task.
- **server/process-governor.ts:199** _(global-table)_ — SELECT AVG(duration_ms) FROM heartbeat_logs has no tenant_id filter (reads heartbeat_logs globally)
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id; this computes a platform-wide average response-time metric by design.
- **server/process-governor.ts:240** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs lacks tenant_id filter (cross-tenant read)
  - _Why false positive:_ heartbeat_logs has no tenant_id and query computes a global aggregate of failing personas, not tenant-row disclosure from a tenant-scoped table.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT FROM code_proposals WHERE id = ${proposalId} does not verify tenant ownership or include tenant_id (possible cross-tenant access to proposals)
  - _Why false positive:_ Verifier is a server-side worker operating on a proposal by primary key proposalId, with no evidence here that the id comes from an untrusted tenant request boundary.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without constraining tenant_id (global scan).
  - _Why false positive:_ Minute-runner intentionally scans recurring_message rows across tenants and then processes each row using its own carried tenant_id for downstream scoping.
- **server/routes.ts:1117** _(signed-url)_ — Middleware for /uploads selects all file_storage rows for a basename (SELECT tenant_id FROM file_storage WHERE filename = ...) with NO tenant_id filter. This enumerates other tenants' file rows and relies on post-query logic to decide access, producing a cross-tenant lookup.
  - _Why false positive:_ Cross-tenant basename lookup is used only to verify ownership and deny on mismatch; delivery assets are explicitly gated by signed URL and no foreign-tenant row data is returned.
- **server/routes.ts:2089** _(upstream-verified)_ — Presenter slide image fetches use presenter_slide_images SELECTs by session_id (SELECT image_data ... FROM presenter_slide_images WHERE session_id = ${sessionId} ...) with no tenant check. sessionId is derived from an earlier unscoped presenter_sessions lookup (token) — this can expose another tenant's slide images.
  - _Why false positive:_ Access is capability-based via prior presenter_sessions lookup by unguessable token in the same call path; child image fetch is anchored to that verified session id.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Why false positive:_ The conversation row was just created server-side with tenantId set, so updating it by its fresh server-generated id is already anchored to the caller's tenant in the same flow.
- **server/routes/conversations.ts:99** _(fk-transitive)_ — SELECT from tenant-scoped table 'project_conversations' lacks tenant_id constraint (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}).
  - _Why false positive:_ project_conversations has no tenant_id; linkage is transitively scoped by already-verified tenant-owned project and freshly created tenant-owned conversation ids.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)).
  - _Why false positive:_ project_conversations has no tenant_id and insert is the intended transitive child/link-table pattern under verified project and conversation parents.
- **server/routes/conversations.ts:231** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ...)).
  - _Why false positive:_ project_notes has no tenant_id and note insert is transitively isolated through tenant-checked project_id.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table inbox_messages is missing tenant_id predicate (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). This allows modifying a message by id without constraining to the caller's tenant.
  - _Why false positive:_ The same handler first selects inbox_messages by id and tenant_id, then updates that exact fetched row id; ownership is enforced earlier in the call path.
- **server/routes/projects.ts:35** _(fk-transitive)_ — Subquery SELECT COUNT(*) FROM project_files WHERE project_id = p.id does not constrain by tenant_id. This reads a tenant-scoped table without enforcing the caller's tenant.
  - _Why false positive:_ project_files has no tenant_id; counting child rows by project_id under outer query restricted to tenant-owned projects is transitive isolation.
- **server/routes/projects.ts:36** _(fk-transitive)_ — Subquery SELECT COUNT(*) FROM project_notes WHERE project_id = p.id does not constrain by tenant_id. Tenant-scoped data may be counted across tenants.
  - _Why false positive:_ project_notes has no tenant_id; counting notes by project_id under tenant-scoped parent projects is transitive isolation.
- **server/routes/projects.ts:37** _(fk-transitive)_ — Subquery SELECT COUNT(*) FROM project_conversations WHERE project_id = p.id lacks a tenant_id constraint. This can leak cross-tenant counts.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped by parent projects row already filtered by p.tenant_id
- **server/routes/projects.ts:319** _(upstream-verified)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC does not include tenant_id. This reads a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ same handler first verifies project ownership with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:342** _(upstream-verified)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 lacks tenant_id constraint. Reading tenant-scoped project_files without tenant filtering risks cross-tenant access.
  - _Why false positive:_ download handler checks parent project belongs to tenant before reading child project_files row by that project_id
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' — no tenant_id constraint. This read is used to decide global updates; counting across tenants can lead to cross-tenant effects.
  - _Why false positive:_ boot-time cleanup intentionally scans and disables runaway heartbeat tasks across tenants as housekeeping, not tenant-facing data access
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' — missing tenant_id constraint. Used to decide a follow-up UPDATE that is currently unscoped.
  - _Why false positive:_ maintenance cleanup globally counts and disables duplicate session-logger tasks; only housekeeping fields are changed
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ...) — no tenant_id constraint. Follow-up UPDATE is likewise unscoped and will affect all tenants.
  - _Why false positive:_ global seed reconciler disables orphaned repo-examination heartbeat tasks across tenants as housekeeping only
- **server/seed.ts:35** _(system-sweep)_ — Unscoped SELECT on tenant-scoped table `research_sessions`: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id WHERE clause, allowing cross-tenant data exposure.
  - _Why false positive:_ dev snapshot import is a boot-time global seed routine using aggregate count only to decide one-time backfill, not tenant-facing read mixing
- **server/seed.ts:3092** _(system-sweep)_ — Unscoped SELECT on tenant-scoped table `research_programs`: `SELECT id, name, model FROM research_programs WHERE is_active = true` returns rows across all tenants (no tenant_id filter).
  - _Why false positive:_ global maintenance loop scans all active research programs and updates invalid model fields per row; no tenant data returned to users
- **server/seed.ts:3442** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` lacks any tenant_id constraint. The query selects conversations across all tenants: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)`
  - _Why false positive:_ boot-time backfill scans conversations across tenants only to insert missing project_conversations links per row, without tenant-facing output
- **server/seed.ts:4270** _(system-sweep)_ — SELECT on tenant-scoped table `heartbeat_tasks` is performed with no tenant filter: `db.select().from(heartbeatTasks)`. This reads heartbeat tasks for all tenants.
  - _Why false positive:_ seed routine globally checks whether heartbeat_tasks table is empty before initializing system tasks; this is boot-time maintenance
- **server/seed.ts:4715** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` runs without a tenant_id constraint: `UPDATE memory_entries SET ... WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ...`. This modifies rows across all tenants.
  - _Why false positive:_ boot-time global maintenance normalizer updates rows in place and only logs aggregate count, with no tenant-facing data mixing.
- **server/seed.ts:4739** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` runs without tenant_id constraint: `UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'`. This will update conversations for all tenants.
  - _Why false positive:_ global idempotent backfill updates model ids across tenants and emits only aggregate counts; no tenant data returned or mixed.
- **server/skillify.ts:221** _(global-table)_ — SELECT from `personas` lacks a tenant constraint. The persona lookup `.where(eq(personas.id, conv[0].personaId))` does not verify that the persona belongs to the caller's tenant, allowing cross-tenant disclosure of persona data.
  - _Why false positive:_ schema note says personas has no tenant_id column, so this lookup is against a non-tenant-scoped/global table.
- **server/storage.ts:239** _(upstream-verified)_ — getConversationUnscoped performs an unscoped SELECT on tenant-scoped table `conversations` (WHERE id = ${id}) with NO tenant_id constraint — returns rows across tenants.
  - _Why false positive:_ explicitly dangerous internal helper documented for orchestration to derive tenantId from the row, not for HTTP use; callers must validate ownership immediately downstream.
- **server/storage.ts:294** _(upstream-verified)_ — attachCitationMetadata issues a raw SELECT FROM `messages` using id IN (...) but does NOT filter by tenant_id. This reads a tenant-scoped table without tenant scoping.
  - _Why false positive:_ this helper only re-reads message ids taken from an already tenant-scoped msgs array passed in by caller, so ownership is inherited from the prior scoped query.
- **server/storage.ts:1019** _(system-sweep)_ — claimHeartbeatTasks performs UPDATE heartbeat_tasks ... WHERE id = ${id} ... (and other predicates) but does NOT require tenant_id in the WHERE clause — unscoped write affecting tenant-scoped table.
  - _Why false positive:_ global heartbeat scheduler claims due tasks across tenants for housekeeping using row ids from prior sweep; no tenant data returned or mixed
- **server/storage.ts:1034** _(internal-worker-pk)_ — markHeartbeatTaskRun updates `heartbeat_tasks` WHERE id = ${id} without a tenant_id clause — unscoped write against tenant-scoped table.
  - _Why false positive:_ server-side worker state update by globally unique task id, not shown as coming from request input
- **server/storage.ts:1445** _(upstream-verified)_ — supersedeAdr updates architecture_decisions rows with UPDATE ... WHERE id = ${oldId} / WHERE id = ${newId} but does NOT include tenant_id in the WHERE — writes tenant-scoped table without tenant scoping on the UPDATE statements.
  - _Why false positive:_ both ADR rows are fetched immediately above with matching tenant_id and only then updated
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (where clause only filters by name). This can lead to cross-tenant lookup or leakage if global uniqueness assumptions change.
  - _Why false positive:_ name is rewritten with tenantToolName(parsed.name, tenantId) immediately above, making lookup tenant-derived despite no explicit tenant predicate
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `custom_tools` uses WHERE eq(custom_tools.id, tool.id) but does NOT include tenant_id in the predicate. Per invariant, all writes must constrain by tenant to prevent touching another tenant's rows by id.
  - _Why false positive:_ auto-fix updates the just-inserted tool row by server-generated primary key within internal validation flow
- **server/tool-learning.ts:315** _(internal-worker-pk)_ — DELETE on tenant-scoped table `custom_tools` uses WHERE eq(custom_tools.id, tool.id) without tenant_id constraint. This can delete another tenant's row if an id is guessed or mis-bound.
  - _Why false positive:_ revert deletes the just-inserted tool row by server-generated primary key within internal validation flow
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE that increments `usageCount` on tenant-scoped table `custom_tools` uses WHERE eq(custom_tools.id, tool.id) but lacks tenant_id constraint, allowing an update to a row belonging to another tenant by id.
  - _Why false positive:_ tool row is selected just above with tenant_id = tenantId before usageCount update by its id
- **server/tools.ts:2976** _(fk-transitive)_ — SELECT against tenant-scoped table `project_files` lacks an explicit tenant filter. The duplicate-check SELECT id FROM project_files WHERE project_id = ${params.id} ... does not constrain by tenant_id.
  - _Why false positive:_ project_files has no tenant_id and access is transitively scoped by prior projects tenant ownership check on project_id
- **server/tools.ts:3016** _(fk-transitive)_ — SELECT against tenant-scoped table `project_conversations` lacks an explicit tenant filter. SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} does not include tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and link is transitively scoped by prior tenant checks on project and conversation
- **server/tools.ts:4528** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not include tenant_id. The INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) is executed without explicitly setting tenant_id even though project_notes is a project/tenant-scoped table.
  - _Why false positive:_ project_notes has no tenant_id and insert is gated by assertProjectInTenant on the parent project
- **server/tools.ts:4532** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not include tenant_id. The INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by assertProjectInTenant on the parent project
- **server/tools.ts:6380** _(fk-transitive)_ — INSERT into tenant-scoped table project_files (during generate_audio) omits tenant_id. The code inserts a project_files row (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without explicitly persisting tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via project_id; ownership is checked with assertProjectInTenant before insert
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`
  - _Why false positive:_ project_files has no tenant_id and projId comes from tenant-scoped join conversations→projects with p.tenant_id = tenantId
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE executed without tenant constraint: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` does not include `tenant_id = ${tenantId}`. This allows a row id obtained (or guessed) to be modified even if it belongs to another tenant.
  - _Why false positive:_ row.id values being updated come only from the immediately preceding SELECT constrained by tenant_id = tenantId
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE on memory_entries uses WHERE id = ${id} with no tenant_id (or persona) constraint — tenant-scoped row updated without explicit tenant ownership filter.
  - _Why false positive:_ id updated is selected immediately above from memory_entries with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) lacks tenant_id constraint — bulk update by id array can affect rows from other tenants.
  - _Why false positive:_ ids in bulk update are derived from rows selected just above with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE on memory_entries uses WHERE id = ${id} with no tenant_id (or persona) constraint — tenant-scoped row updated without explicit tenant ownership filter.
  - _Why false positive:_ id updated is selected immediately above from memory_entries with tenant_id = tid and persona_id = personaId
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger uses only id in WHERE (no tenant_id constraint). settleVentureReservation updates ledger rows by id without ensuring the row belongs to the caller's tenant.
  - _Why false positive:_ best-effort worker update by server-generated reservationId primary key, not shown as sourced from request params/body/tool args
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger uses only id in WHERE (no tenant_id constraint). releaseVentureReservation clears a reservation by id without verifying tenant ownership.
  - _Why false positive:_ best-effort worker release by server-generated reservationId primary key, not shown as sourced from request params/body/tool args
- **server/video-job-runner.ts:228** _(system-sweep)_ — Global UPDATE on tenant-scoped table video_jobs with no tenant_id filter. recoverStaleVideoJobs marks stale jobs across all tenants (UPDATE ... WHERE status IN (...) AND updated_at < ...) without scoping to a tenant.
  - _Why false positive:_ Boot/interval stale-job recovery is a platform-wide housekeeping sweep that only mutates status fields per row and logs aggregate/job ids server-side; it is not a tenant-facing data mix.
- **server/wiring-invariants.ts:57** _(system-sweep)_ — SELECT from research_schedules (joined to research_programs) has no tenant_id constraint. Tenant-scoped tables must be filtered by the caller's tenant.
  - _Why false positive:_ Global integrity check over research wiring runs as platform-wide maintenance/diagnostic logic and only emits findings, not tenant-scoped content to tenant callers.
- **server/wiring-invariants.ts:108** _(system-sweep)_ — SELECT name FROM research_programs executed with no tenant_id constraint. Reads from tenant-scoped program data must be tenant-scoped.
  - _Why false positive:_ Program/persona invariant check is a platform-wide diagnostic aggregate, not a tenant-request data path; output is maintenance findings only.
- **server/wiring-invariants.ts:111** _(global-table)_ — SELECT name FROM personas executed with no tenant_id constraint. Personas may be tenant-scoped and require tenant filtering.
  - _Why false positive:_ Schema note says personas has no tenant_id, so unscoped read is intentional/global rather than a tenant-isolation issue.
- **server/wiring-invariants.ts:142** _(system-sweep)_ — SELECT name FROM research_programs (in checkProposalTargets) executed with no tenant constraint. This reads tenant-scoped program rows without filtering.
  - _Why false positive:_ Proposal-targets checker is a global boot/maintenance invariant scan producing only findings about code wiring, not tenant data disclosure.
- **server/wiring-invariants.ts:180** _(system-sweep)_ — Complex SELECT joining research_programs, research_experiments, code_proposals and research_sessions has no tenant_id constraints on any of the referenced tables. Cross-tenant data leakage or mis-attribution is possible.
  - _Why false positive:_ Outcome canary is a cross-tenant maintenance diagnostic using counts/max timestamps and program names to detect broken nightly pipelines; no tenant-facing mixed content is returned.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — SELECT count(*) FROM tool_performance (recent window) executed with no tenant_id constraint. tool_performance appears tenant-scoped and is read without filtering.
  - _Why false positive:_ Dormant-tool detector is a platform-wide housekeeping/diagnostic count query producing aggregate findings only.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT count(*) FROM tool_performance (everCount) executed with no tenant filter. This reads cumulative tenant-scoped metrics across all tenants.
  - _Why false positive:_ Historical tool_performance count is part of a global maintenance diagnostic and returns only aggregate counts in findings.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM messages (recent window) executed with no tenant_id constraint. messages are almost always tenant-scoped and must be filtered by tenant.
  - _Why false positive:_ Recent messages count is used only as a global traffic heuristic inside a maintenance checker; it does not expose tenant message content or route data to tenants.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance (recent window) executed with no tenant_id constraint. Reads tenant-scoped performance data without filtering.
  - _Why false positive:_ Distinct tool_name scan is part of platform-wide dormancy diagnostics and only compares global usage against registered tools for maintenance findings.
- **server/wiring-invariants.ts:361** _(system-sweep)_ — sql.raw used to query information_schema.tables and then to SELECT COUNT/MAX from subsystem tables (e.g. tool_performance, knowledge_nudges) without tenant filtering. These subsystem tables are tenant-scoped and are queried globally.
  - _Why false positive:_ Subsystem freshness loop is a platform-wide maintenance scan over whitelisted tables that reads only counts/max timestamps and emits diagnostic findings, not tenant content.
- **server/wiring-invariants.ts:417** _(global-table)_ — SELECT aggregate from tool_performance (failures in last 7d) executed with no tenant_id constraint. Aggregation over tenant-scoped performance data is unscoped.
  - _Why false positive:_ Wiring invariants is a platform-wide observability/self-check module; this query returns only aggregate tool failure stats and emits internal findings, not tenant data.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — sql.raw SELECT COUNT and pg_total_relation_size FROM memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) executed with no tenant_id constraint. These memory/chat tables are tenant-scoped but are queried across all tenants.
  - _Why false positive:_ Platform-wide maintenance/observability loop counting rows and relation sizes across tables; it only computes aggregate housekeeping metrics and emits threshold warnings, with no row data returned or tenant content mixed into outputs.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory executed with no tenant_id constraint. The subsequent message even refers to 'empty across all tenants' but the query itself is unscoped.
  - _Why false positive:_ Bootstrapping/health check intentionally tests whether graph_memory is globally empty and only emits a boolean/aggregate warning, not tenant-specific content.
- **server/commitment-drafter.ts:234** _(system-sweep)_ — UPDATE without tenant constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — mutates a row by id with no AND tenant_id = ${tenantId}, allowing accidental cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/commitment-drafter.ts:234 — This UPDATE is the fail-CLOSED revert branch inside the background commitment-drafter loop. row.id comes from claimedRows, which were claimed with an explicit AND c.tenant_id = ${tenantId} predicate (line 204). The rows are server-derived from a tenant-scoped claim, not attacker input, and this only reverts draft_status back to 'open' for a row this loop jus
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} — no tenant_id constraint. settleFusionReservation updates a tenant-scoped ledger row by id only, allowing modification of other tenants' rows if ids are misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — runExperiment updates the `experiments` row with .where(eq(experiments.id, experiment.id)) but does NOT constrain the UPDATE by tenant_id. This allows updating another tenant's experiment by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:339 the experiment row is created via db.insert(experiments) with a server-generated serial PK earlier in the same runExperiment() call; the UPDATE at 394 targets that same experiment.id. All callers (runSelfImprovementCycle:272) create the row internally; no request/tool-arg id.
- **server/agentic-engines.ts:533** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_tasks` uses WHERE enabled = true but omits tenant_id — reads task config across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/orchestrator-ledger.ts:450** _(upstream-verified)_ — Use of sql.raw(lockKey) inside a SQL template (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)). lockKey is derived from inputs (ledgerKey, tenantId) — using sql.raw with derived values risks injection and circumvents parameter binding.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/orchestrator-ledger.ts:445 — lockKey is not caller-supplied text: ledgerLockKey() (line 35-38) computes BigInt('0x'+sha1(ledgerKey:tenantId).slice(0,15)).toString(), i.e. a pure decimal-digit numeric string. sql.raw() embeds only [0-9] characters, so no SQL injection is possible. The advisory lock is keyed on (ledgerKey, tenantId) and the surrounding ledger SELECT/UPDATE/INSERT are all
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` lacks a tenant constraint. The query filters only by conversationId: `where(eq(messages.conversationId, conversationId))` — this allows cross-tenant reads if an attacker supplies a conversationId from another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Within analyzeConversation, the conversation ownership is asserted immediately before at skillify.ts:196-199 (WHERE eq(id) AND eq(tenantId), throws if absent); the messages SELECT at 201-203 reads the same owned conversation's messages.
