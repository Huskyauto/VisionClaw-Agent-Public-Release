# Tenant-Isolation Audit Report

_Generated 2026-08-22T15:32:32.316Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **602/602** file(s) audited · chunks 58/58 OK (0 failed)
- Findings: **267** (CRITICAL 78 · HIGH 155 · MEDIUM 34 · LOW 0)
- Precision second-pass (`gpt-5.4`): **221** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (78)

- **server/agentic/resume-worker.ts:65** — SELECT query reads agent_runs (joined with agent_approvals) without constraining to a specific tenant_id. The db.execute(sql`SELECT ... FROM agent_runs ... WHERE ...`) in resumePendingApprovalRuns returns rows across tenants because there is no WHERE r.tenant_id = ${tenantId} (the function does not accept/apply a tenant filter). This allows cross-tenant reads and potential cross-tenant actions on runs/approvals.
  - _Fix:_ Restrict the query to a caller-authorized tenant. Options: (a) make resumePendingApprovalRuns operate per-tenant (accept tenantId) and add WHERE r.tenant_id = ${tenantId}; (b) if this background worker must scan all tenants, run it with an explicit, auditable admin context and ensure the SQL uses an explicit admin-scoped path (and audit/log each tenant operation). Do NOT leave an unfiltered SELECT on tenant-scoped tables. Ensure every SELECT/UPDATE/DELETE against agent_runs/agent_approvals includes a tenant_id = <caller tenant> constraint.
- **server/agentic/wake-scheduler.ts:142** — UPDATE / claim for agent_wake_schedules does not constrain by tenant_id. The subquery SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... returns rows across all tenants, and the outer UPDATE sets status/attempts for those ids without a tenant filter — allowing cross-tenant claiming/mutation of wake rows.
  - _Fix:_ Limit the claim to a single tenant at a time or otherwise include tenant_id in the WHERE clause. E.g. iterate tenants and run the UPDATE with AND tenant_id = ${tenantId}, or add AND tenant_id = <expectedTenant> to the subquery/outer WHERE. Ensure the scheduler runs per-tenant or runs under a platform-admin guard with explicit intent and auditing.
- **server/auto-transcript.ts:165** — UPDATE conversations ... WHERE id = ${c.conversation_id} ... does not include tenant_id in the WHERE clause — modifying a tenant-scoped table by id only can affect rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL) or perform the update inside a transaction after an explicit owner check so writes cannot affect other tenants.
- **server/channel-routing.ts:16** — channel_routes table is created with no tenant_id column. Without a tenant_id column, you cannot enforce tenant isolation at the application level (and there is no RLS/fk cascade).
  - _Fix:_ Add a NOT NULL tenant_id column to channel_routes (e.g. tenant_id INTEGER NOT NULL), include tenant_id in unique constraints (e.g. UNIQUE(tenant_id, channel)), and backfill/migrate existing data. Ensure all DAL functions require and pass the tenantId when inserting/selecting/updating/deleting from this table.
- **server/channel-routing.ts:30** — listChannelRoutes performs a SELECT joining channel_routes and personas with no tenant_id filter. This can leak other tenants' persona information if those tables are tenant-scoped.
  - _Fix:_ Require the caller to pass tenantId to listChannelRoutes and add WHERE clauses to constrain both channel_routes and personas (e.g. WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId} AND cr.enabled = true). Alternatively validate the persona rows belong to the tenant before returning.
- **server/channel-routing.ts:51** — DELETE FROM channel_routes WHERE channel = ${channel} runs without restricting by tenant_id. This will delete routes for all tenants that share the same channel identifier.
  - _Fix:_ Require and pass tenantId into the function and include it in the DELETE predicate (DELETE ... WHERE channel = ${channel} AND tenant_id = ${tenantId}). Also ensure the table has tenant_id as a NOT NULL column (see migration suggestion).
- **server/channel-routing.ts:54** — INSERT INTO channel_routes (channel, persona_id) ... inserts without setting tenant_id. Tenant_id is not supplied, so multi-tenant isolation cannot be enforced and data may be assigned to an implicit/global scope.
  - _Fix:_ Change the INSERT to include tenant_id (INSERT INTO channel_routes (tenant_id, channel, persona_id) VALUES (${tenantId}, ${channel}, ${personaId})), require tenantId as a function parameter, and ensure ON CONFLICT uses tenant-aware unique keys (e.g. conflict on (tenant_id, channel)). Verify persona ownership before inserting.
- **server/channel-routing.ts:64** — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true has no tenant_id constraint. It may return a persona_id belonging to another tenant.
  - _Fix:_ Require tenantId and add AND tenant_id = ${tenantId} to the WHERE clause. Optionally also validate that the returned persona belongs to the tenant (e.g. join to personas on p.id = cr.persona_id AND p.tenant_id = ${tenantId}).
- **server/channel-routing.ts:71** — removeChannelRoute executes DELETE FROM channel_routes WHERE channel = ${channel} without a tenant filter, deleting routes across all tenants.
  - _Fix:_ Require tenantId in removeChannelRoute and include it in the WHERE clause (DELETE ... WHERE channel = ${channel} AND tenant_id = ${tenantId}). Ensure the channel_routes table includes tenant_id as described above.
- **server/chat-engine.ts:573** — SELECT from tenant-scoped table `project_files` is executed without an explicit tenant_id constraint. The query uses only project_id (which was validated earlier in code comment), but per the tenant-isolation invariant every access to a tenant-scoped table must include an explicit tenant_id filter.
  - _Fix:_ Constrain the query by tenant_id. Either add AND tenant_id = ${tenantId} to the SQL or rewrite using Drizzle: .where(eq(project_files.tenantId, tenantId)). Alternatively, join to projects and require p.tenant_id = ${tenantId} in the WHERE clause to make tenant scoping explicit and unavoidable.
- **server/chat-engine.ts:585** — SELECT from tenant-scoped table `project_notes` is executed without an explicit tenant_id constraint (WHERE project_id = ${projectId} only). Even though projectId was derived from a tenant-validated project earlier, the SQL lacks an explicit tenant_id check.
  - _Fix:_ Add an explicit tenant_id constraint to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(project_notes.tenantId, tenantId)). If relying on project_id, enforce tenant ownership via an explicit JOIN to projects with p.tenant_id = ${tenantId} so the DB-level filter is always present in the query.
- **server/chat-engine.ts:634** — SELECT from tenant-scoped table `compaction_archives` (WHERE conversation_id = ${c.id}) has no tenant_id constraint. This can allow cross-tenant reads if conversation_id is attacker-controlled or if conversation ownership assumptions are ever bypassed.
  - _Fix:_ Add an explicit tenant_id filter to the query (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}). Prefer using Drizzle's eq(compaction_archives.tenantId, tenantId) or join to conversations and require c.tenant_id = ${tenantId} so tenant scoping is enforced in the SQL itself.
- **server/chat-engine.ts:652** — SELECT from tenant-scoped table `messages` (WHERE conversation_id = ${c.id}) is executed without an explicit tenant_id constraint. The code relies on prior assumptions about conversation ownership, but the SQL itself lacks a tenant filter, violating the rule that every tenant-scoped access must include tenant_id.
  - _Fix:_ Constrain the messages query with tenant_id (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}), or use Drizzle .where(eq(messages.tenantId, tenantId)). As an alternative, join messages to conversations and require conversations.tenant_id = ${tenantId} in the same query so tenant scoping is explicit and cannot be skipped.
- **server/chat-engine.ts:4551** — storage.attachCitations(...) is invoked with only message id and collectedCitations; no tenant id/tenant scope is passed. With no RLS or FK-enforced tenant ownership, this can write/read citation rows for the wrong tenant.
  - _Fix:_ Modify storage.attachCitations to accept and enforce the caller's tenantId (or call a variant that takes tenantId). Always pass the correct tenantId/tenantIdForScope when attaching citations, and inside attachCitations ensure all queries/INSERTs include WHERE tenant_id = <tenantId> (or set tenant_id on INSERT).
- **server/commitment-drafter.ts:234** — Unscoped UPDATE against tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — missing tenant_id constraint. This can mutate another tenant's row if ids collide.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenantId}`) or otherwise verify the row's tenant before issuing the UPDATE. Fail-closed: avoid doing an unscoped UPDATE even on error paths.
- **server/data-protection.ts:168** — DELETE FROM conversations WHERE id = ${conv.id} lacks a tenant_id predicate. This writes/deletes a tenant-scoped table row without scoping by tenant_id.
  - _Fix:_ Include the conversation's tenant in the DELETE predicate (e.g. DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or run the operation in a tenant-scoped transaction (withTenantTx) so the row-level context is enforced; never delete tenant-scoped rows by id alone when the code previously read multi-tenant sets.
- **server/delivery-pipeline.ts:318** — updateDeliveryLog performs an UPDATE against delivery_logs without scoping by tenant_id (where clause is only eq(deliveryLogs.id, id)).
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog (e.g. updateDeliveryLog(id, updates, tenantId)) and include it in the WHERE clause: .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). This prevents one tenant from updating another tenant's delivery rows.
- **server/dfy-intake.ts:93** — submitDfyIntake updates dfy_intake_forms (.update(...).where(eq(dfyIntakeForms.id, form.id))) without scoping by tenant_id. This allows updating any form row by id across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId || expectedTenantId)))) or require caller-provided tenantId and validate it before updating. Never perform UPDATEs on tenant-scoped tables using only the id column.
- **server/doc-collections.ts:166** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} without also constraining tenant_id. This DELETE can remove chunks belonging to other tenants if collection_id references are inconsistent.
  - _Fix:_ Include the tenant filter on the DELETE: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Alternatively validate collection ownership first and use a transaction to ensure only that tenant's chunks are deleted.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks embedding via UPDATE ... WHERE id = ${row.id} with no tenant_id constraint. Updating by id only can modify another tenant's chunk if ids are guessed or data inconsistent.
  - _Fix:_ Use a tenant-scoped WHERE clause when updating embeddings, e.g. UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}, or select ids already scoped to the tenant and use that trusted set for updates.
- **server/email.ts:470** — SELECT from tenant-scoped table `inbox_messages` without any tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can return rows belonging to other tenants.
  - _Fix:_ Constrain the lookup to the caller's tenant (or explicitly use an admin/maintenance path with documented intent). Example: SELECT id FROM inbox_messages WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1 — or pass the resolved tenantId into this function and use Drizzle .where(eq(inboxMessages.tenantId, tenantId)).
- **server/email.ts:515** — SELECT from tenant-scoped table `inbox_messages` without tenant_id filter (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id). This reads all tenants' inbox rows.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId} AND (...)). If this is intended to be a cross-tenant maintenance job, run it under a clearly-documented admin path and include an explicit ADMIN_TENANT_ID or iterate tenants one-by-one and enforce authorization before querying each tenant's rows.
- **server/email.ts:528** — UPDATE against tenant-scoped table `inbox_messages` without tenant_id constraint (UPDATE inbox_messages SET ... WHERE id = ${row.id}). This can update rows from other tenants that share the id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or resolve/verify the tenant for row.id before performing the UPDATE. Prefer Drizzle .where(eq(inboxMessages.id, id), eq(inboxMessages.tenantId, tenantId)).
- **server/embeddings.ts:748** — UPDATE on tenant-scoped table (memory_entries or agent_knowledge via sql.raw(table)) is executed without constraining by tenant_id (UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}). Missing tenant filter allows cross-tenant modification.
  - _Fix:_ Require and pass a tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), or verify row ownership before updating. Use Drizzle's table column eq(table.tenantId, tenantId) rather than relying on id alone.
- **server/embeddings.ts:752** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint in backfillMissingKnowledgeEmbeddings (SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50). This will scan rows across all tenants.
  - _Fix:_ Scope the SELECT to a tenant (WHERE tenant_id = ${tenantId}) or implement a controlled, authorized admin/maintenance flow that iterates tenants and documents the admin intent. Never perform silent cross-tenant reads from tenant-scoped tables.
- **server/embeddings.ts:780** — SELECT from tenant-scoped table `memory_entries` without tenant_id constraint (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200). This reads rows across tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${tenantId}) or perform an explicit per-tenant backfill loop run under an authorized admin context. Ensure tenantId is supplied and used in the WHERE clause.
- **server/embeddings.ts:790** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} — missing tenant_id constraint. This can update memory entries for other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). If this backfill runs across tenants, iterate per-tenant and enforce proper authorization before updating each tenant's rows.
- **server/embeddings.ts:796** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200). This reads rows across tenants.
  - _Fix:_ Scope the SELECT by tenant (WHERE tenant_id = ${tenantId}) or implement an explicit admin-only flow that iterates tenants with authorization and documents the admin tenant used for such cross-tenant maintenance.
- **server/embeddings.ts:807** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} — missing tenant_id constraint. This allows modifying other tenants' agent_knowledge rows.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or fetch & verify the row's tenant before applying the update. Prefer Drizzle .where(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId)).
- **server/felix-brain.ts:19** — SELECT FROM tenant-scoped table 'agent_runs' is performed with no tenant_id filter (reads all tenants' in-flight agent_runs at boot). This reconstructs per-conversation task state from rows across tenants, violating tenant isolation.
  - _Fix:_ Limit rehydration to a single tenant context: either accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to the query, or iterate per-tenant and run the query scoped to each tenant. Ensure any rows pulled are validated to belong to the tenant before constructing in-memory TaskState and never materialize other tenants' state.
- **server/google-drive.ts:946** — INSERT INTO project_files does not include tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL/no default). The INSERT relies on prior checks rather than explicitly setting tenant_id, enabling missing or cross-tenant writes.
  - _Fix:_ Always include tenant_id in INSERTs to tenant-scoped tables and set it from a validated, trusted source (e.g. resolvedTenantId after assertProjectInTenant). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${resolvedTenantId}, ${params.projectId}, ${params.fileName}, ...). Ensure resolvedTenantId is validated to belong to the caller before inserting.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` without any tenant_id constraint (reads stale/other-tenant proposals). The subsequent UPDATE (lines 333-337) also updates rows by id alone with no tenant check — allows modifying other tenants' rows.
  - _Fix:_ Restrict both the SELECT and UPDATE to the intended tenant. Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or, if these actions are only allowed for the admin/system tenant, require/verify ADMIN_TENANT_ID and add WHERE tenant_id = ${ADMIN_TENANT_ID}. Always validate ownership before updating (e.g. SELECT ... WHERE id = $1 AND tenant_id = $2 to confirm before UPDATE), and include tenant_id in the UPDATE WHERE clause (UPDATE code_proposals SET ... WHERE tenant_id = ${tenantId} AND id = ANY(...)).
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` without any tenant_id constraint (identifies 'stuck' tasks across tenants). The subsequent UPDATE (lines 368-371) disables tasks by id with no tenant check — may disable other tenants' heartbeat tasks.
  - _Fix:_ Scope discovery and any UPDATE to the correct tenant. Add tenant_id = ${tenantId} (or tenant_id = ${ADMIN_TENANT_ID} if the intent is to operate only on the admin/system tenant) to the WHERE clauses of both the SELECT and UPDATE. Before disabling tasks, verify task.tenant_id matches the expected tenant and include tenant constraint in UPDATE (UPDATE heartbeat_tasks SET enabled = false WHERE tenant_id = ${tenantId} AND id = ANY(...)).
- **server/memory-graph.ts:252** — INSERT into tenant-scoped table `memory_links` does not set tenant_id (INSERT INTO memory_links (... ) VALUES (${newMemoryId}, ${target.id}, ...)). Every INSERT into tenant-scoped tables must explicitly set tenant_id; omitting it can create cross-tenant data or violate NOT NULL constraints.
  - _Fix:_ Include the tenant id in the INSERT (e.g. INSERT INTO memory_links (tenant_id, source_memory_id, target_memory_id, ...) VALUES (${tenantId}, ${newMemoryId}, ${target.id}, ...)). Ensure the calling function passes/uses the correct tenantId parameter.
- **server/memory-intelligence.ts:399** — UPDATE against tenant-scoped table 'knowledge_triples' is executed without constraining tenant (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}). This can modify rows belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or validate the row's tenant before updating. Always include the caller's tenant_id in WHERE for mutating tenant-scoped tables.
- **server/minerva-planner.ts:367** — CAS UPDATE on plans may omit tenant scoping. The WHERE clause conditionally appends AND tenant_id = ${args.tenantId} only when a tenantId is supplied; if tenantId is omitted the UPDATE (which flips plan status) runs without tenant constraint, allowing cross-tenant modification.
  - _Fix:_ Do not allow the CAS update to run without a tenant constraint. Require and validate tenantId for external callers, or fetch the row first and verify row.tenant_id === callerTenant before performing the UPDATE. Always include tenant_id in the WHERE clause for tenant-scoped writes.
- **server/minerva-planner.ts:386** — Probe SELECT from plans (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}) may execute without tenant constraint when tenantId is omitted, leaking cross-tenant data.
  - _Fix:_ Require a tenantId for external callers or always include tenant_id = <callerTenant> in the WHERE. Do explicit ownership verification before returning plan rows.
- **server/minerva-planner.ts:404** — SELECT child plan probe uses a conditional tenant filter (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}). If tenantId is omitted this read can return rows from other tenants.
  - _Fix:_ Always scope reads on tenant-scoped tables by tenant_id or require that callers provide and prove the tenant. Do not rely on optional tenant parameters for security.
- **server/minerva-planner.ts:472** — Rollback UPDATE on plans (UPDATE plans SET status = 'awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising') lacks a tenant_id constraint. This write can affect plans in other tenants.
  - _Fix:_ Include AND tenant_id = ${args.tenantId} (or row.tenant_id obtained from a trusted earlier read) in the WHERE clause, or otherwise verify ownership before performing the rollback update.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} executes with no tenant_id constraint. Personas are tenant-scoped; this allows reading a persona that belongs to another tenant (data exposure).
  - _Fix:_ Always scope persona reads by tenant (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}). Require the caller to pass tenantId and validate it before reading tenant-scoped rows.
- **server/plan-executor.ts:513** — CAS claim UPDATE on plans lacks any tenant_id constraint: `UPDATE plans SET status = 'executing' ... WHERE id = ${planId} AND status = 'approved'`. This allows claiming/modifying a plan by id across tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `AND tenant_id = ${authTenant}`) or first load & validate the plan row's tenant_id and use that value in the UPDATE. Prefer SELECT ... FOR UPDATE or a parameterized CAS that checks both id AND tenant_id so a caller cannot affect another tenant's plan.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — the UPDATE does not include tenant_id. This allows modifying tasks across tenants if an id from another tenant is present.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId}. Prefer passing tenantId through and double-check ownership before mutating rows.
- **server/process-governor.ts:642** — UPDATE agent_desks SET ... WHERE id = ${desk.id} — update lacks tenant_id constraint. Although the desks were selected earlier with tenant filter, updating by id alone can mutate another tenant's row if ids collide or data was tampered.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE (UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or perform the update using the previously-scoped row (e.g. include tenant filter in the WHERE clause on every write).
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — UPDATE has no tenant_id constraint, so it can increment trigger_count for a rule belonging to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id, e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}. Always include tenant_id on mutating queries against tenant-scoped tables.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge executed without constraining by tenant_id (UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}). This allows a row from another tenant with the same id to be modified by the system runner.
  - _Fix:_ Include the tenant guard in the WHERE clause (AND tenant_id = ${r.tenant_id}) so updates are scoped to the row's tenant. Example: WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}.
- **server/reference-learner.ts:290** — UPDATE memory_entries sets fact/last_accessed/access_count WHERE id = ${storedId} without also constraining tenant_id. This can update another tenant's memory entry if IDs collide.
  - _Fix:_ Add tenant ownership constraint to the UPDATE: WHERE id = ${storedId} AND tenant_id = ${input.tenantId}. Always parameterize and validate tenant ownership before modifying tenant-scoped rows.
- **server/research-engine.ts:165** — cleanupZombieSessions runs UPDATE research_sessions ... WHERE status = 'running' ... started_at < ${staleThreshold} without constraining by tenant_id. This will mark running sessions for all tenants as completed potentially touching other tenants' sessions.
  - _Fix:_ If intended to operate per-tenant, add AND tenant_id = ${tenantId} (and loop per tenant). If global maintenance is required, make this explicit and perform careful authorization/audit. Prefer limiting scope or passing an explicit admin/system context and log affected tenant ids.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() executed without tenant_id constraint. This deletes expired autoresearch rows across all tenants.
  - _Fix:_ Scope the DELETE to a tenant (e.g. AND tenant_id = ${tenantId}) or iterate per-tenant with an explicit system-admin path and justification. Avoid unscoped DELETEs on tenant-scoped tables; always include tenant_id.
- **server/routes.ts:2074** — Public GET /api/presenter/:token selects presenter_sessions by token without constraining by tenant_id. This leaks presenter_session → tenant mapping (tenant_id) and allows unscoped access based solely on the token value.
  - _Fix:_ Treat the token as a capability and avoid returning tenant_id or other tenant-scoped metadata, or bind the token lookup to a tenant (e.g. require the caller's session tenant or include tenant in the signed token). Alternatively, store capability-only rows that do not include tenant_id in the public lookup, or validate that the request is permitted for the tenant before returning any tenant-scoped data.
- **server/routes/conversations.ts:103** — INSERT into tenant-scoped table `project_conversations` does not set tenant_id — `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` omits tenant_id (columns are NOT NULL with no default per invariant).
  - _Fix:_ Include tenant_id explicitly in the INSERT (or use a storage API that does). Example: `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`.
- **server/routes/conversations.ts:230** — INSERT into tenant-scoped table `project_notes` omits tenant_id — `INSERT INTO project_notes (project_id, note, author) VALUES (...)` writes tenant-scoped data without tenant_id.
  - _Fix:_ Provide tenant_id in the INSERT (or use a helper that enforces it). Example: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, 'system:archive')`.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} executes without constraining by tenant_id. This allows modifying another tenant's inbox row if an attacker controls ids or bypasses prior checks.
  - _Fix:_ Include the tenant predicate in the UPDATE: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId} (or use a storage layer method that enforces scoping). Always ensure mutating statements include tenant_id checks.
- **server/routes/projects.ts:256** — INSERT into tenant-scoped table `project_notes` does not include tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (...)`. The code relies on a prior project ownership check but the INSERT itself omits tenant_id (columns are NOT NULL with no default per invariant).
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})) and ensure tenantId was validated earlier in the handler. Do not rely solely on FK/project checks — set tenant_id on every INSERT into tenant-scoped tables.
- **server/routes/projects.ts:296** — INSERT into tenant-scoped table `project_files` (base64 upload path) omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)`. Tenant id is not being written with the row.
  - _Fix:_ Pass tenantId into the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) after validating the requester's ownership of the project. Ensure all project_files rows carry tenant_id at write time.
- **server/routes/projects.ts:364** — INSERT into tenant-scoped table `project_files` (multipart upload path) omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)`. The tenant_id column is not set on new rows.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})), ensuring tenant ownership was checked before insertion. Audit other code paths that create project_files to guarantee tenant_id is always explicitly set.
- **server/seed.ts:2163** — UPDATE research_programs SET model = ... WHERE id = ${p.id} performs a write against a tenant-scoped table without constraining tenant_id. A write without tenant scoping can modify another tenant's data.
  - _Fix:_ Include tenant_id in the WHERE clause (and/or validate ownership before update). Example: UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} AND tenant_id = ${tenantId}; or use Drizzle's .where(eq(research_programs.tenant_id, tenantId)). Always enforce tenant ownership on UPDATE/DELETE.
- **server/seed.ts:3911** — UPDATE on tenant-scoped table `memory_entries` performed without constraining to a tenant (no WHERE tenant_id = ...). This updates rows across all tenants.
  - _Fix:_ Add a tenant filter to the UPDATE, e.g. WHERE tenant_id = <tenantId> AND deleted_at IS NULL AND fact LIKE ...; or perform the normalization per-tenant (loop tenants) so only that tenant's rows are modified. In Drizzle use .where(eq(memoryEntries.tenantId, tenantId)).
- **server/seed.ts:3936** — UPDATE on tenant-scoped table `conversations` performed without constraining to a tenant (UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'). This will modify conversations for all tenants.
  - _Fix:_ Include tenant filtering in the WHERE clause (e.g. WHERE tenant_id = <tenantId> AND model = 'gpt-4.1') or run the update per-tenant. In Drizzle use .where(eq(conversations.tenantId, tenantId), eq(conversations.model, 'gpt-4.1')).
- **server/self-improvement.ts:401** — runExperiment updates the experiments row using only eq(experiments.id, experiment.id) with no tenant_id constraint, permitting modification of rows belonging to other tenants if IDs are guessed or manipulated.
  - _Fix:_ Constrain updates to the caller's tenant. Add a tenant check in the WHERE clause, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Also validate that the UPDATE affected exactly one row and handle unexpected affected-row counts as an error.
- **server/self-improvement.ts:415** — In the error catch path runExperiment performs db.update(experiments).set(...).where(eq(experiments.id, experiment.id)) without restricting by tenant_id, allowing cross-tenant writes.
  - _Fix:_ Include tenant scoping in the WHERE clause on error updates as well (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Prefer validating ownership before attempting updates (fetch + verify tenant) and check affected row count after update.
- **server/storage.ts:1340** — getAllDataForExport performs many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with NO tenant_id constraints. This single API can export all tenants' data if called from an unprivileged context.
  - _Fix:_ Restrict this function to a clear admin-only path and require explicit tenant scoping (e.g. accept a TenantScope parameter and require the ALL_TENANTS sentinel for intentional global dumps). At minimum, enforce authorization checks and make the global behavior explicit in the API (use requiredTenantScope where appropriate) so ordinary callers cannot trigger cross-tenant exports.
- **server/surprise-scorer.ts:93** — UPDATE against tenant-scoped table felix_proposals missing tenant_id constraint (stampArgsEmbedding uses: WHERE id = ${proposalId} AND args_embedding IS NULL). This allows cross-tenant modification of proposal rows.
  - _Fix:_ Pass and use the tenantId to scope the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL). Alternatively require a prior tenant-validated lookup and propagate tenantId into stampArgsEmbedding so all writes include eq(felix_proposals.tenant_id, tenantId).
- **server/surprise-scorer.ts:139** — UPDATE writes actual_outcome_embedding to felix_proposals without constraining tenant (WHERE id = ${proposalId}). Missing tenant_id filter allows cross-tenant writes.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), and ensure the function has the tenantId available (propagate tenantId from caller/previous validated SELECT).
- **server/surprise-scorer.ts:148** — SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} executes without tenant_id constraint. This can return or operate on a row belonging to another tenant.
  - _Fix:_ Re-query with tenant scoping (SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure tenantId is carried into this function or obtained from a prior validated row and not defaulted to admin.)
- **server/surprise-scorer.ts:181** — UPDATE setting surprise_band = 'no_history' on felix_proposals uses WHERE id = ${proposalId} without tenant_id. This writes tenant-scoped metadata without tenant constraint.
  - _Fix:_ Add tenant constraint to the UPDATE (WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure all band/stamp updates in this flow are scoped to the same tenant parameter passed into scoreProposalSurprise or derived from the validated row.)
- **server/surprise-scorer.ts:212** — UPDATE that writes surprise_score and surprise_band to felix_proposals uses WHERE id = ${proposalId} with no tenant_id filter. Tenant-scoped write lacking tenant constraint allows cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Audit all subsequent updates/inserts in this function to ensure they include tenant_id = ${tenantId}.)
- **server/tool-learning.ts:291** — UPDATE custom_tools SET implementation = ... WHERE id = ${tool.id} is executed without a tenant_id constraint. This mutates a tenant-scoped table without verifying the caller's tenant, risking cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${tool.id} AND tenant_id = ${tenantId}). Pass the calling tenantId into tryAndRevert (or determine it from the inserted row in a transaction) and use eq(customTools.tenantId, tenantId) on all UPDATEs.)
- **server/tool-learning.ts:316** — DELETE FROM custom_tools WHERE id = ${tool.id} executes without tenant_id constraint in revertToolRow. Deleting tenant-scoped rows without tenant scoping can remove another tenant's tool.
  - _Fix:_ Always include tenant scoping on DELETE (e.g. DELETE FROM custom_tools WHERE id = ${tool.id} AND tenant_id = ${tenantId}). Ensure revertToolRow receives/derives the tenantId and use it in the WHERE clause.)
- **server/tool-learning.ts:365** — UPDATE increments usageCount on custom_tools using WHERE id = ${tool.id} with no tenant_id constraint. This is a tenant-scoped write that lacks tenant scoping and could affect other tenants' rows.
  - _Fix:_ Add tenant constraint to the UPDATE: WHERE id = ${tool.id} AND tenant_id = ${tenantId}. Ensure executeCustomTool has a validated tenantId param and use eq(customTools.tenantId, tenantId) for all writes.)
- **server/tools.ts:4552** — INSERT into project_notes omits tenant_id. The code inserts a project-scoped row (project_notes) without explicitly passing tenant_id in the VALUES list, relying solely on prior ownership checks. Per the project's invariant every INSERT into tenant-scoped tables must include the tenant_id column/value.
  - _Fix:_ Include tenant_id explicitly in the INSERT and use the already-validated tenant context (e.g. ${params._tenantId} or a validated tenantId variable). Example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Ensure tenantId is validated (numeric, positive) before use.
- **server/tools.ts:4556** — INSERT into project_files omits tenant_id. The insertion of a project_files row does not include tenant_id in the columns/VALUES, violating the rule that every INSERT into tenant-scoped tables must pass tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT and supply the verified tenant id (e.g. ${params._tenantId} or other validated tenant variable). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ...). Validate tenantId before use.
- **server/tools.ts:6405** — INSERT into project_files omits tenant_id. After assertProjectInTenant the code inserts into project_files but does not include tenant_id in the INSERT columns/VALUES, violating the requirement that tenant_id be set explicitly on tenant-scoped writes.
  - _Fix:_ Include tenant_id explicitly using the caller's validated tenant (e.g. ${params._tenantId}) in the INSERT: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Ensure params._tenantId is validated prior to use.
- **server/tools.ts:6941** — INSERT into project_files omits tenant_id. The slideshow video code inserts into project_files (video) without specifying tenant_id in the INSERT, contradicting the invariant that every tenant-scoped INSERT must include tenant_id explicitly.
  - _Fix:_ Pass tenant_id explicitly in the INSERT using the validated caller tenant (e.g. ${params._tenantId}): INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Validate tenantId before performing the insert.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` is executed without constraining to the caller's tenant (no WHERE tenant_id = ${tenantId}). This can allow cross-tenant enumeration of API keys.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or otherwise enforce an explicit admin-only authorization gate before running an unscoped query. Prefer parameterized drizzle .where(eq(provider_keys.tenantId, tenantId)) instead of an unscoped literal SQL string.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executes without a tenant_id constraint. This reads tenant-scoped provider key data across all tenants.
  - _Fix:_ Scope the aggregation to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}) or require and enforce an explicit admin authorization check before running cross-tenant queries. Use a parameterized drizzle query that includes eq(provider_keys.tenantId, tenantId).
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE memory_entries SET ... WHERE id = ${id} updates a tenant-scoped table row by id only, without also constraining by tenant_id. An attacker or bug could modify another tenant's row sharing the same id.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or perform the UPDATE via a query that always filters by tenant_id (db.execute(sql`UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid}`)). Use drizzle .where(eq(table.id, id), eq(table.tenantId, tid)).
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) updates memory_entries by id array without also constraining tenant_id. IDs could collide across tenants leading to cross-tenant updates.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE tenant_id = ${tid} AND id = ANY(${ids})) so only rows belonging to the caller's tenant are modified. Prefer using parameterized drizzle update with both tenant and id filters.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET fact = ${fact} ... WHERE id = ${id} performs an update on a tenant-scoped table without filtering by tenant_id, allowing cross-tenant modification if ids overlap.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or use drizzle's .where(eq(table.id, id), eq(table.tenantId, tid)). Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id in the WHERE clause.

### HIGH (155)

- **server/ab-optimizer.ts:110** — SELECT from experiments in runDueAbExperiments lacks a tenant_id filter (reads experiments across all tenants).
  - _Fix:_ Scope the query to a single tenant (WHERE tenant_id = <caller tenant>) or explicitly iterate tenants and run per-tenant queries. Avoid selecting tenant-scoped rows without an explicit tenant constraint.
- **server/agent-activity.ts:122** — UPDATE agentActivity in completeActivity() uses WHERE eq(agentActivity.id, activityId) with no tenant_id constraint — allows updating rows across tenants by id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND eq(agentActivity.tenantId, tenantId)) or otherwise verify ownership before performing the update.
- **server/agent-eval.ts:218** — UPDATE agent_evals sets completion fields WHERE id = ${evalId} without constraining by tenant_id — an attacker or bug could update another tenant's eval row by id.
  - _Fix:_ Add tenant_id to the WHERE clause (WHERE id = ${evalId} AND tenant_id = ${tenantId}) or validate the eval row belongs to the calling tenant before updating.
- **server/agentic-engines.ts:161** — SELECT from ai_insights in retryPendingMinervaRouting() has no tenant_id constraint and will fetch HIGH-priority applied insights across all tenants.
  - _Fix:_ Either require a tenantId parameter and filter by tenant_id, or implement an explicit, well-audited multi-tenant sweep that authorizes cross-tenant operations. Avoid unscoped reads on tenant-scoped tables.
- **server/agentic-engines.ts:247** — UPDATE ai_insights in autoApplyEligibleInsights() can run without tenant scoping when called with tenantId undefined — the WHERE clause may not include tenant_id and will affect all tenants.
  - _Fix:_ Require an explicit tenantId for this operation or ensure the WHERE includes tenant_id (and never omit it). If a global sweep is intended, make it explicit and ensure callers are authorized and the code documents the cross-tenant behavior.
- **server/agentic-features.ts:698** — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} is missing tenant_id in the WHERE clause (updates by id only).
  - _Fix:_ Constrain the UPDATE by tenant_id as well (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or validate row ownership before updating.
- **server/agentic-features.ts:773** — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' lacks tenant_id scoping — allows checking/operating on enrollments from other tenants.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause to prevent cross-tenant enumeration/operations, and/or validate sequence ownership before querying enrollments.
- **server/agentic-features.ts:932** — UPDATE outreach_enrollments SET reply_classification = ... WHERE id = ${row.id} is not tenant-scoped — it updates an enrollment by id without verifying tenant ownership.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the WHERE clause (or otherwise verify the enrollment belongs to the caller's tenant) before updating.
- **server/agentic/approvals.ts:135** — expireStaleApprovals() runs UPDATE agentApprovals ... WHERE status = 'pending' AND expiresAt < now() with no tenant filter — it will expire approvals across all tenants in a single statement.
  - _Fix:_ Scope expiry by tenant (require tenantId parameter) or run per-tenant expiry loops. At minimum, include tenant_id in the WHERE clause or clearly document and restrict this global operation and its authorization.
- **server/auto-asset-capture.ts:184** — Backfill query selects DISTINCT conversations (id, project_id, tenant_id) across the entire table with no tenant_id restriction. This reads tenant-scoped rows across all tenants in one global sweep.
  - _Fix:_ Restrict the backfill to a specific tenant (accept a tenantId parameter) or iterate per-tenant. Add WHERE c.tenant_id = ${tenantId} (or otherwise scope the operation by tenant) before reading/polling conversations.
- **server/auth.ts:1262** — Maintenance DELETE deletes expired auth_sessions across all tenants (DELETE FROM auth_sessions WHERE expires_at < ${now}) with no tenant_id constraint — a global write against a tenant-scoped table.
  - _Fix:_ Run expiration deletes per-tenant or require platform-admin context and document intent. Prefer adding WHERE tenant_id = ${tenantId} for tenant-scoped maintenance or explicitly gate this operation as a controlled platform-level job (with audit logging) if cross-tenant behavior is intended.
- **server/auth.ts:1266** — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' removes sessions across all tenants (no tenant_id filter). This is a global mutation on a tenant-scoped table.
  - _Fix:_ Either scope the deletion to a tenant (WHERE tenant_id = ${tenantId}) or perform this as an explicit platform-admin migration with clear safeguards/logging. If global cleanup is required, ensure it's executed from a trusted admin path and document the justification.
- **server/auth.ts:1244** — clearAllSessions executes DELETE FROM auth_sessions with no tenant WHERE clause, removing sessions for every tenant unconditionally.
  - _Fix:_ Require platform-admin authorization for this action and make the cross-tenant effect explicit, or change the function to accept a tenantId and delete only that tenant's sessions (DELETE FROM auth_sessions WHERE tenant_id = ${tenantId}). Add audit/logging if global clears are retained.
- **server/auto-project.ts:112** — SELECT from messages lacks tenant_id predicate (reads messages for a conversation without constraining by tenant).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise validate/derive and use the owning tenant before reading messages.
- **server/auto-transcript.ts:46** — SELECT from messages lacks tenant_id predicate (reading conversation messages without constraining by tenant).
  - _Fix:_ Constrain the query to the caller's tenant (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or validate conversation ownership and add tenant_id in the SQL WHERE clause to enforce isolation.
- **server/auto-transcript.ts:132** — Subquery SELECT COUNT(*) FROM messages used in a global scan is missing a tenant filter (counts messages for conversations without scoping to the conversation's tenant).
  - _Fix:_ Scope the subquery by tenant (for example: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0) so the existence/count is evaluated only for the conversation's tenant.
- **server/capability-review.ts:416** — String-interpolated SQL with untrusted/request-derived data: recordCapabilityReview builds an SQL array literal (arrLiteral) from review.doNotRebuild (untrusted text) and injects it into a sql`` template (`VALUES ( ... ${arrLiteral}::text[], ...)`). This is effectively string interpolation of request-derived values into SQL and risks SQL-injection / parsing issues and breaks parameterization.
  - _Fix:_ Avoid constructing SQL array literals by hand. Use a parameterized binding for the array (or store the list as json/jsonb) so the DB driver handles escaping. Example fixes: 1) pass surfaced names as a JSONB parameter and write to a jsonb column: VALUES (..., ${JSON.stringify(surfaced)}::jsonb, ...); OR 2) use your driver/Drizzle helper to bind a text[] parameter rather than embedding a literal (e.g. insert via Drizzle's insert API or use pg-format-safe array binding). Ensure all elements are sanitized and passed as bound parameters rather than concatenated into SQL text.
- **server/capability-review.ts:490** — UPDATE on tenant-scoped table capability_reviews lacks a tenant constraint: markReviewReused executes `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` without checking tenant_id. This allows a process to flip rows belonging to other tenants if review IDs collide or are leaked.
  - _Fix:_ Include tenant scoping in the WHERE clause and/or track tenantId in the pendingReuseByConversation entry. For example, persist ctx.tenantId when creating the pending entry and change the UPDATE to: WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false. This ensures only the originating tenant's row can be modified.
- **server/channel-routing.ts:48** — setChannelRoute accepts a personaId from the caller and writes it into channel_routes without validating that the persona belongs to the caller's tenant. This allows referencing another tenant's persona_id.
  - _Fix:_ Before writing, verify ownership: SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} (or otherwise verify persona.tenant_id === tenantId). Reject or error if the persona does not belong to the tenant. Require tenantId as an explicit parameter for setChannelRoute.
- **server/chat-engine.ts:3957** — recordMutation({...}) is called without any tenant identifier. This helper appears to log tool mutations but is invoked without tenant context, so mutation logs/rows may be written without a tenant_id constraint.
  - _Fix:_ Include tenantId (tenantId or tenantIdForScope) in the object passed to recordMutation, and update recordMutation to require and use tenantId in its DB queries/INSERTs (explicit WHERE tenant_id = ... or set tenant_id on INSERT). If this data must be omitted for certain sources (subagents), gate the call or use a scoped tenant value instead of null/undefined.
- **server/chat-engine.ts:4223** — recordToolExecution(conversationId, toolName, ...) is called without passing tenantId. If recordToolExecution writes to a tenant-scoped table, the write lacks an explicit tenant constraint and may cross tenant boundaries.
  - _Fix:_ Change calls to recordToolExecution to pass the caller's tenantId (or tenantIdForScope). Ensure recordToolExecution enforces tenant scoping in all DB operations (WHERE tenant_id = ... and set tenant_id on INSERT).
- **server/data-protection.ts:150** — SELECT on conversations (expired soft-deletes) does not include a tenant_id predicate — it reads rows across all tenants.
  - _Fix:_ If this function is intended to run per-tenant, add a tenant_id WHERE clause (e.g. WHERE tenant_id = ${tenantId}). If it must operate across tenants (admin purge), make that explicit and restrict the caller context (run as an admin-only job) or use a dedicated admin path; otherwise limit reads to a specific tenant or use withTenantTx to bind an explicit tenant context.
- **server/data-protection.ts:442** — Subquery (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id) in getDeletedConversations does not include tenant_id, so message counts may be computed without scoping to the caller's tenant.
  - _Fix:_ Constrain the subquery by tenant (e.g. (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id AND tenant_id = ${tenantId})) or rewrite the query to join messages with the tenant predicate to ensure all counts are tenant-scoped.
- **server/delivery-pipeline.ts:1286** — getDeliveryByStripePayment selects a delivery_logs row by stripePaymentId with no tenant_id constraint (WHERE eq(deliveryLogs.stripePaymentId, paymentId)). This can disclose another tenant's delivery record/links when called with a payment id.
  - _Fix:_ Add a tenantId parameter (or otherwise validate caller ownership) and include tenant scoping in the WHERE clause: .where(and(eq(deliveryLogs.stripePaymentId, paymentId), eq(deliveryLogs.tenantId, tenantId))). If this function must be usable without tenant context, document and enforce why that exception is safe (prefer parameterized tenant scoping).
- **server/dfy-intake.ts:50** — getDfyIntakeByToken selects dfy_intake_forms by token only (WHERE eq(dfyIntakeForms.token, token)) with no tenant_id constraint. Reading tenant-scoped table rows without a tenant filter can leak another tenant's form data.
  - _Fix:_ Require caller tenant context or otherwise validate ownership before returning the row. Either: add a tenantId parameter and include AND dfy_intake_forms.tenant_id = ${tenantId} in the WHERE, or ensure the unguessable token is tied to a tenant and enforced server-side before returning results (explicit tenant check in SQL is preferable).
- **server/doc-collections.ts:136** — listCollections uses subqueries that COUNT doc_chunks by collection_id but do not include tenant_id in the subselects (e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id)). Even though the outer query filters doc_collections by c.tenant_id, the inner queries against doc_chunks are not explicitly tenant-scoped.
  - _Fix:_ Add tenant restriction inside the subselects, e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id AND tenant_id = ${tenantId}) and similar for the DISTINCT/doc_count subquery. Explicit tenant clauses avoid relying on implicit invariants or DB constraints.
- **server/doc-collections.ts:456** — getCollectionStatus contains multiple subqueries that query doc_chunks by collection_id but do not include tenant_id (e.g. COUNT(*) FROM doc_chunks WHERE collection_id = c.id). These aggregate subqueries may surface counts for rows not belonging to the requested tenant.
  - _Fix:_ Add tenant scoping to each subquery (AND tenant_id = ${tenantId}) so all reads over tenant-scoped tables are explicitly constrained to the caller's tenant_id.
- **server/firecrawl.ts:231** — UPDATE on scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) without also scoping by tenant_id. This can modify a scraped page row belonging to another tenant if an id is reused or guessed.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise ensure the update uses both id and tenantId so writes are constrained to the caller's tenant.
- **server/firecrawl.ts:363** — In the crawl loop, existing scraped page updates use WHERE eq(scrapedPages.id, existing[0].id) without constraining by tenant_id — allowing cross-tenant updates to scrapedPages rows.
  - _Fix:_ When updating an existing scrapedPages row, include tenant scoping in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or perform the update via a parameterized statement that enforces tenant_id = ${tenantId} to guarantee tenant isolation.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' is performed without scoping by tenant_id (WHERE tenant_id = ...). The query uses only projects.id in the WHERE clause.
  - _Fix:_ Include the tenant filter in the SELECT so the database read is always scoped to the caller's tenant. E.g. add a tenant predicate: .where(eq(projects.id, projectId), eq(projects.tenantId, tenantId)) (or equivalent Drizzle .where(and(...))). Do not rely on a later in-memory check as the sole protection.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' uses WHERE projects.id = <projectId> but does NOT include tenant_id in the WHERE clause. Writes are not constrained to the caller tenant.
  - _Fix:_ Constrain updates to the tenant as well: include AND projects.tenant_id = <tenantId> in the WHERE (or .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))). Alternatively use a transactional pattern that verifies ownership and then performs the UPDATE using the tenant-scoped WHERE to avoid accidental cross-tenant writes.
- **server/google-drive.ts:829** — SELECT of a project by projectId (caller-supplied) is performed without a tenant_id WHERE clause. The code later compares proj.tenantId to a caller-provided tenantId in JS, but the DB read itself is not scoped.
  - _Fix:_ Scope the initial SELECT to the tenant to avoid reading another tenant's project: .where(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)). If params.tenantId may be absent, explicitly require/derive a validated tenantId server-side and include it in the WHERE.
- **server/heartbeat.ts:105** — SELECT * FROM research_schedules ... executed without a tenant_id filter. research_schedules appears to be tenant-scoped but is queried across tenants with no tenant constraint.
  - _Fix:_ Constrain queries to the intended tenant (e.g. WHERE tenant_id = ${tenantId}) or, if this is a deliberate cross-tenant sweep, use an explicit system API that documents and enforces safe cross-tenant access. Avoid selecting tenant-scoped rows without an explicit tenant filter to prevent accidental cross-tenant access.
- **server/heartbeat.ts:154** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — update to a tenant-scoped table is performed without constraining by tenant_id.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use the storage wrapper that enforces tenant context. This prevents modifying another tenant's schedule by id alone.
- **server/heartbeat.ts:160** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id constraint on update to a tenant-scoped table.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or call a tenant-aware storage API that enforces the tenant context before writing.
- **server/heartbeat.ts:173** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — update performed with only id filter and no tenant_id check.
  - _Fix:_ Restrict the UPDATE to the owning tenant (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage helper that validates/uses the tenant context to prevent cross-tenant modification.
- **server/heartbeat.ts:325** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — direct update to heartbeat_tasks without tenant_id constraint. heartbeat_tasks is tenant-scoped in other call sites.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use storage.updateHeartbeatTask(...) which should be tenant-aware. Avoid direct DB updates that do not validate tenant ownership.
- **server/heartbeat.ts:329** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — modifying a tenant-scoped table row without constraining tenant_id.
  - _Fix:_ Add tenant constraint to the UPDATE (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the tenant-aware storage API to ensure operations are executed in the correct tenant context.
- **server/heartbeat.ts:2817** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — direct UPDATE on heartbeat_tasks without tenant_id in WHERE clause after creating a tenant-bound task.
  - _Fix:_ Use a tenant-aware UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call a storage wrapper (updateHeartbeatTask) that enforces/validates the task's tenant before mutating approval_status.
- **server/heartbeat.ts:3269** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — updating heartbeat_tasks without constraining by tenant_id (in delegateTaskFromChat path).
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the tenant-aware storage API to set approval_status. Ensure every UPDATE/DELETE on tenant-scoped tables validates the tenant ownership.
- **server/job-queue.ts:99** — claimDueJobs performs an UPDATE/SELECT against agent_jobs with no tenant_id constraint — it can transition/return jobs across all tenants.
  - _Fix:_ Scope claims to a specific tenant (e.g. add AND tenant_id = ${tenantId} in the inner SELECT or require a tenantId parameter), or ensure this function is only executed by an operator process that is explicitly allowed to operate cross-tenant and has additional controls/auditing. Prefer explicit tenant scoping wherever possible.
- **server/job-queue.ts:134** — reclaimExpiredLeases (step 1) UPDATEs agent_jobs to failed_terminal for expired leases with no tenant_id constraint — affects rows for all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant (or iterate per-tenant) or otherwise ensure the operation runs under an explicit, auditable admin context. Prefer adding WHERE tenant_id = ${tenantId} when called from a tenant-scoped path.
- **server/job-queue.ts:148** — reclaimExpiredLeases (step 2) UPDATEs agent_jobs to requeue expired rows with no tenant_id constraint — mutates jobs across tenants.
  - _Fix:_ Same as above: add tenant scoping (WHERE tenant_id = ${tenantId}) or run per-tenant with an explicit tenant parameter; avoid blanket updates that touch multiple tenants without explicit authorization/audit.
- **server/job-queue.ts:177** — completeJob UPDATE on agent_jobs does not include tenant_id restriction — a caller can complete a job id belonging to any tenant.
  - _Fix:_ Require and validate tenant context (pass tenantId into completeJob) and include AND tenant_id = ${tenantId} in the WHERE clause, or otherwise ensure the caller holds authority to complete cross-tenant jobs.
- **server/job-queue.ts:220** — failJob SELECTs max_attempts FROM agent_jobs WHERE id = ${id} without tenant constraint — reading another tenant's job state is unscoped.
  - _Fix:_ Require a tenantId parameter for failJob (or look up tenant_id atomically together with max_attempts) and include tenant_id = ${tenantId} in the SELECT to ensure the lookup is tenant-scoped.
- **server/job-queue.ts:237** — failJob UPDATE to mark job failed_terminal does not restrict by tenant_id — may mark jobs from other tenants terminal.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (AND tenant_id = ${tenantId}), and pass/validate tenantId into failJob (or fetch/verify tenant ownership before update).
- **server/job-queue.ts:257** — failJob UPDATE that requeues a job (sets status='pending') lacks a tenant_id constraint — can requeue jobs across tenants.
  - _Fix:_ Add tenant validation and include AND tenant_id = ${tenantId} in the WHERE clause; prefer passing tenantId explicitly into this API.
- **server/job-queue.ts:274** — cancelJob UPDATE on agent_jobs WHERE id = ${id} updates rows with no tenant constraint — can cancel any tenant's job by id.
  - _Fix:_ Require tenantId (or validate job ownership) and include AND tenant_id = ${tenantId} in the WHERE to enforce tenant isolation.
- **server/job-queue.ts:283** — retryJob UPDATE resets rows across all tenants because it lacks tenant_id in the WHERE clause.
  - _Fix:_ Require/validate tenant context and include AND tenant_id = ${tenantId} in the WHERE clause to avoid cross-tenant mutation.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor queries agent_knowledge with GROUP BY tenant_id, persona_id and no tenant filter — this scans embeddings across all tenants (cross-tenant read).
  - _Fix:_ Perform per-tenant scans (iterate tenants and call the scoring per-tenant) or otherwise ensure this routine runs under an admin-only, audited context and that the cross-tenant scan is intended. Prefer limiting DB reads to a single tenant at a time by adding WHERE tenant_id = ${tenantId}.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE against tenant-scoped table `procedure_evolution_runs` is performed using only the run id (WHERE clause = eq(procedureEvolutionRuns.id, runId)) with no tenant_id constraint. This allows a runId from another tenant to be updated.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) or otherwise assert/derive the tenant and enforce it before updating. Always bind the caller's tenantId when updating tenant-scoped rows.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE against tenant-scoped table `procedure_evolution_runs` is performed using only the run id (WHERE clause = eq(procedureEvolutionRuns.id, runId)) with no tenant_id constraint. (noop_proposal path)
  - _Fix:_ Same fix as above: include eq(procedureEvolutionRuns.tenantId, tenantId) in the WHERE predicate so the update is always constrained to the caller's tenant.
- **server/lib/aevo-meta-editor.ts:499** — UPDATE against tenant-scoped table `procedure_evolution_runs` is performed using only the run id (WHERE clause = eq(procedureEvolutionRuns.id, runId)) with no tenant_id constraint. (finalization path)
  - _Fix:_ Add the tenant predicate to the WHERE (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) so only the owning tenant can change the run row.
- **server/lib/agent-trace.ts:129** — UPDATE on tenant-scoped table `agent_trace_spans` (persistSpanClose) uses WHERE span_id = ${spanId} with no tenant_id constraint. Closing a span should never update rows for another tenant.
  - _Fix:_ Record the tenantId when opening the span (or obtain it from a trusted context) and include tenant_id in the UPDATE WHERE clause (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). If needed, store tenantId with the open-span bookkeeping so the close path can assert the owner before updating.
- **server/lib/bwb-job-progress.ts:74** — scope(...) builds WHERE clauses for videoJobs that may omit tenant predicate when BWB_TENANT_ID threading is missing. Several updates/selects (setBwbPhase, updateBwbChapters, completeBwbJob, bumpBwbHeartbeat, failBwbJob) call .where(scope(...)) and can execute without eq(videoJobs.tenantId, ...) if env tenant is not set — resulting in writes/reads against a tenant-scoped table without tenant_id constraint.
  - _Fix:_ Make tenantId mandatory for these operations (pass it explicitly or throw when missing) and ensure scope() always adds eq(videoJobs.tenantId, tenantId). Do not emit only a warn: fail closed by asserting presence of a valid tenantId and include it in every WHERE clause so all reads/writes are constrained to the caller's tenant.
- **server/lib/ideabrowser-backfill.ts:49** — Query reads the tenant-scoped `projects` table without constraining by tenant_id: SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && {ideabrowser,...}::text[] GROUP BY tenant_id. This performs a cross-tenant scan and can leak or act on other tenants' data.
  - _Fix:_ Do not scan tenant-scoped tables across all tenants. Restrict the query to the explicit tenant(s) of interest (e.g. run it per tenant with WHERE tenant_id = <tenantId> or use a parameterized IN / ANY clause built from the snapshot's tenant IDs). Alternatively, iterate wantedByTenant and query each tenant individually (SELECT count(*) FROM projects WHERE tenant_id = $1 AND tags && $2::text[]), ensuring tenant_id is always passed as a bound parameter.
- **server/lib/inbox-ingest.ts:507** — UPDATE on tenant-scoped table `inbox_classifications` lacks a tenant_id constraint — WHERE clause uses only inbox_message_id. This allows a row id to be used to update a classification belonging to another tenant.
  - _Fix:_ Include tenant scoping in the UPDATE. e.g. WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}. Alternatively, join through inbox_messages to verify ownership (UPDATE inbox_classifications c SET ... FROM inbox_messages m WHERE c.inbox_message_id = ${storeResult.id} AND m.id = c.inbox_message_id AND m.tenant_id = ${tenantId}). Always enforce tenant_id in all writes to tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without constraining by tenant_id (the CTE that picks due rows does not filter by tenant). This lets the system-runner select and lock rows across all tenants in a single statement, violating the invariant that every access must be scoped by tenant via WHERE tenant_id = <caller tenant>.
  - _Fix:_ If this behavior is intentional for an admin/system-wide runner, make the scope explicit and auditable (e.g. require an explicit admin mode and document it). Otherwise, restrict the CTE to a single tenant (add WHERE tenant_id = ${tenantId} or iterate per-tenant). At minimum ensure any cross-tenant operation is only ever run under an explicit, well-audited admin context and add an explicit tenant_id guard/comment in the SQL. Prefer selecting rows per-tenant and keeping all statements parameterized with tenant_id.
- **server/lib/scheduled-post-runner.ts:319** — Construction of a Postgres array literal via string interpolation from request-derived data (`arrLit` built from params.platforms) then injected into a SQL template. Even though the platform values were filtered earlier, building a SQL fragment by hand risks injection and bypasses parameterization.
  - _Fix:_ Avoid constructing SQL literals from user input. Pass the platforms as a parameterized JS array (let Drizzle/pg bind parameters) or use a safe helper to produce an array parameter (e.g. bind platforms directly and cast to text[]). If you must build a literal, validate and canonicalize inputs strictly and use sql.raw only with a validated allowlist and clear justification.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` does not restrict by tenant_id (query scans all tenants for running rows). Every SELECT against tenant-scoped tables must include a tenant_id constraint to enforce tenant isolation at the application level.
  - _Fix:_ Add an explicit tenant_id filter to the query (e.g. WHERE tenant_id = ${tenantId} AND ...), or if this is intended to be a cross-tenant audit, require an explicit admin-only code path and document/guard it. Prefer per-tenant scans using a controlled loop when the operation must remain multi-tenant.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` performed without filtering by tenant_id (may scan rows for all tenants). This violates the requirement that every read against tenant-scoped tables include the caller's tenant filter or run only under an explicit admin context.
  - _Fix:_ Either add a WHERE tenant_id = ${tenantId} clause or require/verify an explicit admin context before running cross-tenant queries. If cross-tenant auditing is needed, make it an explicit admin operation and carefully document/guard it.
- **server/lib/system-probes.ts:163** — Drizzle SELECT count(*) FROM messages (messagesTable) is executed without a tenant_id WHERE clause, returning a global message count across tenants. This is an unscoped access to a tenant-scoped table.
  - _Fix:_ If the probe should be per-tenant, add WHERE tenant_id = ${tenantId}. If it is intentionally global for admin diagnostics, require and assert an explicit admin context before running, and document the rationale. Prefer explicit, parameterized per-tenant queries over unscoped global queries to preserve tenant isolation.
- **server/memory-graph.ts:183** — UPDATE to tenant-scoped table `memory_categories` is executed without constraining by tenant_id (and the subquery counting memory_entries also omits tenant filtering). This violates the tenant-isolation invariant — writes/reads against tenant-scoped tables must be scoped to the caller's tenant.
  - _Fix:_ Change refreshCategoryCount to accept a tenantId and include tenant_id filters. Example: pass tenantId into refreshCategoryCount( categoryId, tenantId ) and use WHERE id = ${categoryId} AND tenant_id = ${tenantId}. Also add tenant filtering inside the subquery: SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND ... .
- **server/memory-graph.ts:239** — SELECT existence check on `memory_links` omits any tenant constraint: SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) ... LIMIT 1. This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Fix:_ Include the tenant context in the existence check. If memory_links has a tenant_id column, add AND tenant_id = ${tenantId} (or join to the owning memory_entries restricted by tenant_id) so the query only examines links for the caller's tenant.
- **server/memory-graph.ts:124** — UPDATE to `memory_categories` (memory_count increment) is performed without a tenant_id WHERE clause: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}. This writes a tenant-scoped table without scoping by tenant.
  - _Fix:_ Restrict the UPDATE to the tenant, e.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Propagate tenantId into callers so it can be supplied.
- **server/memory-graph.ts:528** — UPDATE to tenant-scoped `memory_entries` omits tenant scoping: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}. Even though the row id came from a tenant-scoped SELECT, the UPDATE itself lacks an explicit tenant_id constraint.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or otherwise ensure the function receives and applies the caller's tenantId when performing the update.
- **server/minerva-planner.ts:549** — getPlan provides a code path that SELECTs a plan by id without any tenant filter when tenantId is omitted (SELECT * FROM plans WHERE id = ${planId}). This allows cross-tenant reads if callers omit tenantId.
  - _Fix:_ Require tenantId for external callers or split the API into a trusted internal path and an external path that always enforces WHERE tenant_id = ${tenantId}. Do not return tenant-scoped rows without verifying tenant ownership.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without including tenant_id, violating the invariant that every INSERT into tenant-scoped tables must pass tenantId explicitly.
  - _Fix:_ Include the tenant_id column/value in the INSERT and ensure it comes from the validated context. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Prefer using Drizzle's typed insert (.insert({...})) or a helper that enforces the tenant column so this cannot be omitted.
- **server/mpeg-engine.ts:1436** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The parallel-produce path inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without tenant_id.
  - _Fix:_ As above, add tenant_id to the column list and values (e.g. tenant_id = ${options.tenantId}) and ensure assertProjectInTenant validated ownership before the insert. Use a centralized helper or Drizzle API to perform tenant-aware inserts to prevent omissions.
- **server/oauth-subscriptions.ts:906** — Global SELECT from tenant-scoped table `oauth_subscriptions` with no tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or otherwise iterate only over an explicit, authorized tenant list. If this is intentionally a cross-tenant maintenance task, make that explicit and enforce operator-only authorization and auditing.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes without scoping by tenant_id. This exposes rows across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or otherwise make the tenant scope explicit and authorized. If you intend to enumerate tenants, require an explicit admin-mode and audit the action.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update lacks tenant_id constraint. Updating by id alone may modify a row belonging to a different tenant if ids are re-used or caller is mistaken.
  - _Fix:_ Include tenant scoping on UPDATEs (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id} OR WHERE provider = ${provider} AND tenant_id = ${tenantId}). Always combine PK predicates with tenant_id when operating on tenant-scoped tables.
- **server/oauth-subscriptions.ts:966** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update lacks tenant_id constraint (same pattern occurs multiple places in this startup validation flow).
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${tenantId}) or use provider+tenant_id predicates. Audit and ensure no cross-tenant updates can occur during startup validation.
- **server/oauth-subscriptions.ts:981** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update executed without tenant_id constraint, enabling potential cross-tenant deactivation.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or equivalent constraint) so updates always require explicit tenant ownership before changing tenant-scoped rows.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh performs a global SELECT FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter, iterating over all tenants' subscriptions.
  - _Fix:_ If the function is intended to operate per-tenant, change it to accept a tenantId and SELECT WHERE tenant_id = ${tenantId}. If it must be global (cron-style), require and document explicit operator/admin authorization and ensure the code cannot be invoked by regular tenants.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions does SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without scoping to a caller tenant — reads all tenants' rows.
  - _Fix:_ Either restrict to specific tenant(s) by passing tenantId, or ensure this routine runs only in an operator/admin context and is protected. Prefer explicit per-tenant loops that call refreshAccessToken(tenantId) with validated tenant ids.
- **server/pace-control.ts:85** — checkPace queries heartbeat_logs without any tenant_id filter (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). If heartbeat_logs is tenant-scoped, this leaks/aggregates data across tenants and can incorrectly enforce caps across tenants.
  - _Fix:_ Require an explicit tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise partition by tenant). If the intent is a global system-wide cap, make the function operator-only and document/guard it accordingly.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs over a time window with no tenant_id constraint (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...). This returns cross-tenant aggregates from a likely tenant-scoped table.
  - _Fix:_ If snapshot is intended to be per-tenant, accept tenantId and filter WHERE tenant_id = ${tenantId}. If global stats are genuinely intended, ensure the routine is only used by authorized operators and clearly documented as a global admin-only function.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ORDER BY ... — this query lacks any tenant_id filter, so tenant-specific skills from other tenants can be returned.
  - _Fix:_ Restrict tenant-scoped skills to the target tenant (e.g. add AND tenant_id = ${tenantId}) while preserving global/shared skills (persona_id IS NULL). For example: WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR persona_id IS NULL. Ensure tenantId is passed and validated.
- **server/plan-executor.ts:116** — appendExecutionLog performs `UPDATE plans ... WHERE id = ${planId}` with no tenant_id constraint. This writes execution_log on a plan row by id only, enabling cross-tenant log corruption.
  - _Fix:_ Require and pass the plan's tenant_id into appendExecutionLog and include `AND tenant_id = ${tenantId}` in the UPDATE. Alternatively, resolve and lock the plan row (SELECT tenant_id WHERE id=...) in the caller and assert tenant matches before updating.
- **server/plan-executor.ts:527** — UPDATE setting plan to 'failed' uses `WHERE id = ${planId}` (no tenant_id filter). Unscoped writes can modify other tenants' plans.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `AND tenant_id = ${row.tenant_id}`) or derive tenant_id from the claimed plan row and include it in the UPDATE to enforce per-tenant writes.
- **server/plan-executor.ts:728** — Final plan status update `UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}` is missing a tenant_id constraint, allowing status changes across tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE (e.g. `WHERE id = ${planId} AND tenant_id = ${row.tenant_id}`), or pass tenantId explicitly to the function so all writes enforce the tenant restriction.
- **server/plan-executor.ts:762** — Error-path UPDATE (`UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing'`) lacks tenant scoping. This can flip other tenants' plan states during exception handling.
  - _Fix:_ Constrain the UPDATE by tenant_id (`AND tenant_id = ${expectedTenant}`) using the plan's persisted tenant identity; fail-closed if tenant can't be resolved.
- **server/plan-executor.ts:778** — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' without any tenant constraint: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...`. This mutates rows across all tenants.
  - _Fix:_ If this is intended admin-wide behavior, make that explicit and run under an admin tenant context (e.g. `AND tenant_id = ${ADMIN_TENANT_ID}`), or iterate per-tenant and include `tenant_id = ?` in each UPDATE. Otherwise restrict to the caller's tenant to enforce isolation.
- **server/plan-executor.ts:787** — SELECT to enumerate pending plans (`SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`) has no tenant filter. It returns plan ids across tenants and later calls executePlan(id) which uses unscoped updates.
  - _Fix:_ Limit the SELECT to the caller's tenant (`WHERE status='approved' AND tenant_id = ${tenantId}`), or explicitly run this recovery loop per-tenant. Never enumerate tenant-scoped rows without scoping by tenant_id.
- **server/process-governor.ts:121** — Subquery reads heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads a tenant-scoped table without a tenant filter.
  - _Fix:_ Add tenant scoping to the subselects, e.g. AND tenant_id = ${tenantId}. Every read against heartbeat_logs must include the caller's tenant id or be performed via a function that enforces the tenant context.
- **server/process-governor.ts:155** — Subquery counts pending heartbeat_tasks by persona_id without tenant scoping ((SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) as pending_tasks). This can aggregate cross-tenant data.
  - _Fix:_ Constrain the subquery to the tenant, e.g. (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND tenant_id = ${tenantId} AND enabled = true). Ensure every reference to heartbeat_tasks includes tenant_id = callerTenant.
- **server/process-governor.ts:198** — Query over heartbeat_logs (SELECT AVG(duration_ms) FROM heartbeat_logs WHERE created_at > ...) lacks tenant_id filter and will aggregate across tenants.
  - _Fix:_ Restrict the query by tenant_id (AND tenant_id = ${tenantId}) so the average is computed only for the calling tenant. All reads of heartbeat_logs must include the tenant constraint.
- **server/process-governor.ts:240** — Query counting distinct persona_id in heartbeat_logs (cascading_failures) lacks tenant_id constraint. This reads tenant-scoped heartbeat_logs across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause. Enforce tenant scoping on all heartbeat_logs queries (including aggregated counts).
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} — projects read without tenant scoping. Project rows are likely tenant-scoped and must be filtered.
  - _Fix:_ Require the caller to pass a tenantId or otherwise obtain request tenant context, and add AND tenant_id = ${tenantId} to the WHERE clause. Validate that the project belongs to the caller's tenant before reading or operating on it.
- **server/project-brain.ts:332** — Backfill query selects DISTINCT projects JOIN conversations and uses a subselect over messages without any tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0). This iterates across all tenants.
  - _Fix:_ Scope the backfill to a specific tenant (accept tenantId param) or add tenant filters to projects/conversations/messages (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId} AND messages.tenant_id = ${tenantId}). Never iterate unbounded across tenant-scoped tables.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — conversations are queried without tenant scoping and may return rows across tenants.
  - _Fix:_ Restrict by tenant_id (AND c.tenant_id = ${tenantId}) or ensure proj was validated to belong to the tenant before querying dependent tables. Always include tenant_id on reads of conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — messages read without tenant scoping. This can expose messages across tenants if conversation ids are not verified to belong to the caller's tenant.
  - _Fix:_ Include tenant filter (AND tenant_id = ${tenantId}) or verify conversation ownership prior to selecting messages. Ensure all reads from tenant-scoped tables (messages) are constrained to the caller's tenant.
- **server/research-engine.ts:496** — INSERT INTO project_notes (project_id, note, author, created_at) is executed without an explicit tenant_id column. If project_notes is a tenant-scoped table with a non-null tenant_id column, this insert omits tenant_id and breaks the invariant (or may allow cross-tenant ambiguity).
  - _Fix:_ Include tenant_id in INSERT values (e.g. VALUES (${projectId}, ${note}, ${'Research Engine'}, NOW(), ${session.tenantId}) ) or otherwise populate the tenant_id column explicitly. If project_id implies tenant ownership, still persist tenant_id for explicit scoping and to match schema invariants.
- **server/research-proposals.ts:713** — SELECT against tenant-scoped table `research_experiments` (alias `re`) does not include an explicit tenant_id constraint in the WHERE clause — it only constrains `rs.tenant_id` via a JOIN on research_sessions. This can allow cross-tenant reads if row->session relationships are inconsistent or attacker-controlled.
  - _Fix:_ Constrain the tenant on the `research_experiments` table explicitly. For example, add `AND re.tenant_id = ${tenantId}` to the WHERE clause (or use Drizzle's .where(eq(re.tenantId, tenantId))). Do not rely solely on joined tables' tenant filters; every tenant-scoped table read must include an explicit tenant_id = <caller tenant> filter.
- **server/routes.ts:1543** — SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) are executed without any tenant_id filter in /api/public/stats.
  - _Fix:_ Constrain these aggregate queries to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or explicitly document/centralize them as platform-global admin metrics and verify the caller is a platform admin. Prefer using a storage helper that enforces/admin-scope checks rather than raw unscoped selects.
- **server/routes.ts:1573** — Multiple db.execute SELECTs in /api/public/architecture query tenant-scoped tables (conversations, messages, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, research_sessions) without any tenant_id constraint.
  - _Fix:_ Either add WHERE tenant_id = ${tenantId} to each tenant-scoped count/read, or restrict this endpoint to platform-admin callers and, if intended to be global, document and centralize the global queries. Prefer using storage functions that enforce tenant scoping / admin intent to avoid ad-hoc unscoped SQL.
- **server/routes.ts:2102** — GET /api/presenter/:token/slide/:index looks up presenter_sessions by token without tenant scoping before querying presenter_slide_images. The session lookup is unscoped/public and can reveal existence of sessions across tenants.
  - _Fix:_ Same as above: avoid raw session lookups that return tenant-scoped rows without tenant validation. Either require/derive tenant context before selecting presenter_sessions or use a capability-only mapping for public token lookups that does not expose tenant-scoped information.
- **server/routes.ts:4467** — INSERT into tenant-scoped table `project_notes` without setting tenant_id. The code executes: INSERT INTO project_notes (project_id, note, author) VALUES (...) — tenant_id is not included, violating the invariant that every INSERT into a tenant-scoped table must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT and bind it via the Drizzle/sql template (or storage layer). Example: INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projId}, ${tenantId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Also ensure the project actually belongs to the tenant (validate project.tenant_id === tenantId) before inserting, or derive tenant_id from a validated owner row rather than trusting a derived id.
- **server/routes/admin-zombie-detector.ts:38** — Query reads agent_trace_spans (a tenant-scoped activity/tracing table) with no tenant_id constraint. The SELECT in the transaction aggregates tool usage across agent_trace_spans but does not filter by the caller's tenant.
  - _Fix:_ Restrict the query to the caller's tenant by adding an explicit tenant_id filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise pass the resolved tenantId into the transaction and use it in the WHERE clause. If the intent is to perform a platform-wide aggregate, make that explicit (and only run it under a verified platform-admin guard) or move to a dedicated platform-wide table to avoid accidental cross-tenant exposure.
- **server/routes/admin-zombie-detector.ts:54** — Query builds the `used` CTE from conversations without scoping by tenant_id. The personas-last-used calculation joins against a subquery that reads conversations but lacks a tenant_id condition, allowing cross-tenant data to be read.
  - _Fix:_ Add tenant scoping to the subquery (e.g. WHERE tenant_id = ${tenantId}) or otherwise ensure the transaction is explicitly bound to a specific tenant. If a cross-tenant view is required, document and isolate it as a deliberate platform-level operation that only platform-admins may invoke.
- **server/routes/admin.ts:734** — SELECT from ai_insights executes without any tenant_id filter. If ai_insights is tenant-scoped, this admin endpoint will read insights across all tenants without constraining to the caller's tenant.
  - _Fix:_ Constrain the query with WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(table.tenantId, tenantId))). If the endpoint is intentionally platform-wide, make that explicit and ensure it runs only under an auditable platform-admin gate or uses a separate platform-level analytics table.
- **server/routes/admin.ts:744** — Query against heartbeat_tasks lacks a tenant_id constraint. Other code in the repo treats heartbeat_tasks as tenant-scoped (e.g. admin-wedges queries use tenant_id), but here the silent-failures admin endpoint selects disabled/failed heartbeat_tasks without scoping to the caller's tenant.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to the query (or equivalent Drizzle predicate) to enforce tenant isolation. If the route must examine heartbeat tasks across tenants, implement an explicit, documented cross-tenant aggregation path and ensure it only runs under a verified platform-admin guard.
- **server/routes/conversations.ts:99** — UPDATE to tenant-scoped table `conversations` lacks tenant filter — `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates by id only with no `tenant_id = <caller>` constraint.
  - _Fix:_ Constrain the UPDATE to the caller tenant (or use a storage layer helper that enforces scope). Example: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}`.
- **server/routes/conversations.ts:100** — SELECT from tenant-scoped table `project_conversations` has no tenant constraint — `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` can return cross-tenant rows.
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. `WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}`.
- **server/routes/conversations.ts:105** — UPDATE to tenant-scoped table `projects` lacks tenant filter — `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` modifies a project row by id only without verifying caller tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table `agent_jobs` is missing a tenant_id predicate (reads jobs across all tenants).
  - _Fix:_ If the route must be tenant-scoped, add a WHERE j.tenant_id = ${tenantId} (or equivalent parameter) to constrain results. If global admin visibility is intentional, make the global intent explicit (requirePlatformAdmin) and route through a storage/helper API that documents and enforces global reads (avoid implicit unscoped SELECTs).
- **server/routes/metrics.ts:63** — Query on agent_cost_ledger aggregates per-tenant costs (SELECT tenant_id, SUM(...) FROM agent_cost_ledger ...) without any tenant_id predicate — reads tenant-scoped table across all tenants.
  - _Fix:_ If cross-tenant aggregates are intended, ensure the endpoint is strictly gated (strong operator auth) and that returning tenant identifiers is acceptable; better: perform aggregation via a vetted, auditable storage helper (and/or redact tenant IDs), or add an explicit tenant_id filter to restrict results to the caller's tenant. Clearly document and harden the admin guard for any global metrics.
- **server/routes/metrics.ts:79** — Query on agent_cost_ledger for tool usage counts (GROUP BY tool_name) runs without tenant predicate and touches tenant-scoped ledger rows across all tenants.
  - _Fix:_ Require and verify strong operator auth for this endpoint, or scope the query to a given tenant (WHERE tenant_id = ...) or to explicitly approved aggregates implemented in a controlled helper. Avoid ad-hoc unscoped queries on tenant-scoped tables.
- **server/routes/metrics.ts:95** — Query on agent_cost_ledger to compute token totals over last 24h runs without a tenant predicate (reads across tenants).
  - _Fix:_ Either add tenant scoping (WHERE tenant_id = ...) for tenant-limited metrics, or ensure this global aggregation is only executed under a provably restricted operator credential and consider redacting tenant identifiers if sensitive. Prefer using a dedicated, audited helper for cross-tenant aggregates.
- **server/oauth-subscriptions.ts:325** — Cross-tenant SELECT on tenant-scoped table without tenant_id constraint: `SELECT DISTINCT tenant_id FROM oauth_subscriptions ...` (cron heal path queries oauth_subscriptions for all tenants).
  - _Fix:_ Avoid unconstrained selects against tenant-scoped tables. If this operation must scan multiple tenants, run it from an elevated platform-context (explicitly authenticated/authorised) and/or use a platform-scoped table designed for cross-tenant operations. Otherwise add an explicit tenant_id filter (or iterate per-tenant with a vetted tenant list) so every query against tenant-scoped tables is constrained by tenant_id.
- **server/routes/stripe-checkout.ts:100** — Request-derived value `priceId` is injected into a raw SQL template (db.execute(sql`... WHERE pr.id = ${priceId} ...`)). This mixes request input into interpolated SQL, creating SQL-injection risk and breaks the rule forbidding string-interpolated SQL with request-derived values.
  - _Fix:_ Stop embedding request-derived values into sql`...` templates. Use the Drizzle query builder (db.select().from(...).where(eq(prices.id, priceId), ...)) or otherwise parameterize/validate the value before use. Prefer verifying and canonicalizing priceId (e.g. allowlist or regex check) and then use Drizzle's .where(eq(..., priceId)) form so the value is passed as a bound parameter and the query is expressed through the ORM instead of string interpolation.
- **server/sculptor.ts:106** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause (UPDATE ... WHERE id = ${session.id}). This allows modifying a session row from another tenant if ids collide or are forged.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or the caller's tenant variable). Alternatively verify the session belongs to the caller's tenant before performing the UPDATE.
- **server/sculptor.ts:112** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause when setting conversation_id (UPDATE ... WHERE id = ${session.id}).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or re-use a previously-validated tenant-bounded session id; always assert tenant ownership before mutating.
- **server/sculptor.ts:167** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause when writing completion/status (UPDATE ... WHERE id = ${sessionId}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure all writes to tenant-scoped tables include tenant_id in the predicate.
- **server/sculptor.ts:358** — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause when writing review_result (UPDATE ... WHERE id = ${sessionId}).
  - _Fix:_ Include tenant guard in the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or perform a prior tenant-ownership check before updating review_result.
- **server/second-opinion.ts:404** — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant_id in WHERE clause when settling a reservation (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}). This can update ledger rows across tenants.
  - _Fix:_ Pass tenantId into settleFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or verify the row's tenant before updating). Never update tenant-scoped rows by id alone without tenant scoping.
- **server/second-opinion.ts:424** — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant_id in WHERE clause when releasing a reservation (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}).
  - _Fix:_ Accept tenantId in releaseFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or verify the reservation row's tenant) to prevent cross-tenant mutation.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM tenant-scoped table `heartbeat_tasks` lacks tenant_id filter (SELECT ... FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'). This will aggregate across tenants and subsequent actions act across tenants.
  - _Fix:_ Scope queries to a tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true ...) or explicitly document that the operation is intentionally global. Per project invariant, tenant-scoped tables must be filtered by tenant_id.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true omits tenant_id and will affect rows across all tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or iterate per-tenant) so only intended tenant rows are modified. Avoid global updates on tenant-scoped tables.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id; the subsequent disabling UPDATE likewise lacks tenant scoping.
  - _Fix:_ Include tenant_id in both SELECT and UPDATE (or otherwise narrow the operation to the intended tenant) to prevent disabling tasks in other tenants.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) omits tenant_id; the paired UPDATE that disables matching rows also lacks tenant scoping.
  - _Fix:_ Restrict these SELECT/UPDATE statements to the intended tenant(s) (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or explicitly iterate tenants. All reads/writes against heartbeat_tasks must include tenant_id.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions is executed with no tenant filter. This reads tenant-scoped rows across all tenants (no WHERE tenant_id = ...)
  - _Fix:_ Constrain the query to the caller's tenant, e.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(research_sessions.tenant_id, tenantId))). If this is intended to be owner-only seed logic, make that explicit and document the elevated scope; otherwise enforce tenant_id in the WHERE.
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id constraint. This reads tenant-scoped data for all tenants.
  - _Fix:_ Add a tenant filter to the SELECT, e.g. WHERE is_active = true AND tenant_id = ${tenantId} (or use Drizzle .where(eq(research_programs.tenant_id, tenantId))). If the code must operate across tenants intentionally, perform explicit authorization checks and document the privileged operation.
- **server/seed.ts:2515** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id) queries conversations without scoping by tenant_id. conversations is a tenant-scoped resource but the query lacks tenant constraints, risking cross-tenant data exposure/processing.
  - _Fix:_ Add tenant scoping to the outer query and to any correlated subqueries. For example: WHERE c.project_id IS NOT NULL AND c.tenant_id = ${tenantId} AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id /* and pc.tenant_id = ${tenantId} if that column exists */). If project_conversations is missing tenant_id, consider adding tenant_id to that table or join via project table to ensure tenant-isolated backfill.
- **server/seed.ts:3882** — DELETE on tenant-scoped table `heartbeat_tasks` executed without any tenant_id constraint (DELETE FROM heartbeat_tasks WHERE enabled = false AND ...). This can delete rows across all tenants.
  - _Fix:_ Restrict deletions to a specific tenant (WHERE tenant_id = <tenantId> AND ...) or otherwise ensure the maintenance operation runs per-tenant. Use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId), sql`...`).
- **server/seed.ts:3959** — Cross-tenant DELETE on tenant-scoped table `file_storage` (DELETE FROM file_storage a USING file_storage b ...). The statement operates across all tenants even though it respects tenant equality between rows — it is not limited to a caller tenant.
  - _Fix:_ Perform the dedupe per-tenant (query tenant ids and run the DELETE filtered by tenant_id = <tenantId>), or add an explicit tenant filter if this is intended to run only for a particular tenant. In Drizzle, run the duplicate-removal logic with a tenant-scoped WHERE clause for each tenant id.
- **server/seed.ts:4108** — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). If project_notes is tenant-scoped (tenant_id NOT NULL), this inserts rows with no tenant context or will fail/leave orphaned rows.
  - _Fix:_ Include tenant_id in the INSERT (resolve tenant_id from the project, e.g. SELECT tenant_id FROM projects WHERE id = ${projId} and use that value), or insert via a JOIN that supplies tenant_id. In Drizzle: .values({ projectId: projId, tenantId, note, author }).
- **server/seed.ts:4174** — UPDATE on tenant-scoped table `agent_knowledge` performed without constraining to tenant_id (UPDATE agent_knowledge SET content = ... WHERE id = ...). While the id was found via a tenant-scoped SELECT earlier, the UPDATE itself lacks a tenant filter and therefore does not enforce tenant isolation in the write.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = <tenantId>) or perform the update using the same tenant-scoped selection criteria. In Drizzle use .where(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId)).
- **server/self-improvement.ts:170** — detectStagnation builds and executes a SELECT against the tenant-scoped experiments table without requiring or enforcing a tenant_id filter when tenantId is not provided, allowing cross-tenant reads.
  - _Fix:_ Make tenantId mandatory for this function (throw if missing) or always include a tenant constraint. Example: always push eq(experiments.tenantId, tenantId) into the conditions and ensure tenantId is validated, or change the API to require tenantId and fail if absent.
- **server/self-improvement.ts:521** — getExperimentHistory returns db.select().from(experiments) when no category/tenantId filter is provided, allowing an unscoped SELECT across all tenants.
  - _Fix:_ Require the caller's tenantId or, if tenantId is optional, default to restricting to the caller's tenant. Ensure the query always includes eq(experiments.tenantId, tenantId) (or throw when tenantId is not provided) before performing the select.
- **server/skillify.ts:91** — SELECT from messages reads conversation messages without constraining by tenant_id. The query uses only conversationId and does not include tenant scoping.
  - _Fix:_ Include tenant scoping on messages reads. Add AND tenant_id = ${tenantId} (or use drizzle eq(messages.tenantId, tenantId)) to the WHERE clause, or join via the already-validated conversation row to ensure the messages query is limited to the caller's tenant.
- **server/skillify.ts:201** — SELECT from messages in analyzeConversation() fetches messages by conversationId but lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Constrain the messages query to the caller's tenant (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) so reads cannot leak other tenants' messages. Even if the conversation row was previously validated, the SQL must include the tenant filter per platform invariant.
- **server/skillify.ts:329** — SELECT recentMsgs reads messages by conversationId without a tenant_id filter in the WHERE clause.
  - _Fix:_ Require and apply tenant scoping on the messages query (add AND tenant_id = ${tenantId} or equivalent Drizzle predicate). Alternatively, fetch messages via a secure join that enforces conversation.tenant_id = tenantId.
- **server/storage.ts:303** — SELECT from messages (fetching citations) does not constrain by tenant_id. The raw query 'SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL' can return citations for message IDs without ensuring they belong to the caller's tenant.
  - _Fix:_ Accept/derive a tenantId for attachCitationMetadata and include a tenant constraint in the query (e.g. AND tenant_id = ${tenantId}), or perform the lookup via a JOIN/Drizzle predicate that enforces eq(messages.tenantId, tenantId) so the read is scoped to the caller's tenant.
- **server/storage.ts:404** — UPDATE messages SET citations = ... WHERE id = ${messageId} has no tenant_id constraint. This UPDATE on a tenant-scoped table can modify another tenant's message if messageId is attacker-controlled.
  - _Fix:_ Require and validate tenantId in the attachCitations callsite and include it in the WHERE clause (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}), or otherwise verify ownership of the message before updating.
- **server/storage.ts:841** — updateMemoryEmbedding performs UPDATE memory_entries SET embedding WHERE id = ${id} without a tenant_id filter. This mutates a tenant-scoped row by id only, with no tenant ownership guard.
  - _Fix:_ Require a tenantId parameter or otherwise verify ownership and add a tenant constraint to the WHERE clause (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))).
- **server/storage.ts:853** — updateKnowledgeEmbedding performs UPDATE agent_knowledge SET embedding WHERE id = ${id} without scoping by tenant_id. This can update another tenant's knowledge row by id.
  - _Fix:_ Require the caller to pass/validate tenantId and enforce it in the WHERE clause (e.g. .where(and(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId)))) or otherwise assert ownership before the update.
- **server/storage.ts:876** — archiveExpiredMemories executes a global UPDATE on memory_entries (SET status='archived' WHERE expires_at <= NOW()) with NO tenant_id constraint, modifying rows for all tenants.
  - _Fix:_ If archive is intended per-tenant, accept a tenantId and include eq(memory_entries.tenant_id, ${tenantId}) in the WHERE clause. If this is an intentional platform-wide operation, require an explicit admin codepath and document it (or require an ALL_TENANTS sentinel via requiredTenantScope) so it's explicit and auditable.
- **server/storage.ts:892** — archiveStaleMemories runs UPDATE memory_entries SET status='archived' based on created_at/last_accessed with NO tenant_id constraint — this mutates data for all tenants in bulk.
  - _Fix:_ Scope the UPDATE by tenant (e.g. include eq(memory_entries.tenant_id, ${tenantId})) or require an explicit admin/global sentinel and restrict the call to admin-only code. Avoid implicit unscoped bulk updates on tenant-scoped tables.
- **server/tool-learning.ts:129** — SELECT against tenant-scoped table custom_tools filters only by name (WHERE name = parsed.name) and omits tenant_id. Reads from tenant-scoped tables must be tenant-scoped to avoid cross-tenant info leaks even when names are namespaced.
  - _Fix:_ Include tenant filtering in the lookup: WHERE name = ${parsed.name} AND tenant_id = ${tenantId} (or otherwise validate the tenant context). If the design relies on global-unique names, make that explicit and document it, but still prefer defensive tenant scoping on reads.)
- **server/tools.ts:2890** — INSERT into project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without setting tenant_id on a tenant-scoped table, violating the invariant that INSERTs must include tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT. Example: INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) OR perform INSERT ... SELECT FROM projects/conversations JOINs that assert ownership and supply the tenant_id from the validated rows.
- **server/tools.ts:2993** — INSERT into project_files omits tenant_id. The INSERT into project_files does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...) RETURNING *).
  - _Fix:_ Pass tenant_id explicitly when inserting project_files (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) ) or insert via an INSERT ... SELECT that derives/validates tenant ownership from projects before writing.
- **server/tools.ts:3008** — INSERT into project_notes omits tenant_id. The code inserts project_notes (project_id, note, author) without setting tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author || 'agent'}, ${tenantId}) ) or use an INSERT ... SELECT that enforces the project belongs to the tenant before inserting.
- **server/tools.ts:3031** — INSERT into project_conversations (link_conversation path) omits tenant_id. In the link_conversation handler the code INSERTs (project_id, conversation_id) without recording tenant_id even though tenant ownership checks were performed separately.
  - _Fix:_ Set tenant_id in the inserted project_conversations row (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Alternatively perform an INSERT ... SELECT that joins projects and conversations and restricts by tenant_id to ensure the new row is tenant-scoped.
- **server/tools.ts:8911** — UPDATE on tenant-scoped table `proposed_skills` executed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This allows a caller to update a row belonging to another tenant.
  - _Fix:_ Include the tenant filter in the UPDATE WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or use a parameterized SQL update with AND tenant_id = ${tid}. Alternatively, fetch the row first and assert its tenantId === tid before updating.
- **server/tools.ts:8955** — UPDATE on tenant-scoped table `proposed_skills` executed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This promotes a proposed skill to accepted without verifying tenant ownership.
  - _Fix:_ Restrict the UPDATE to the caller's tenant, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or db.execute(sql`UPDATE proposed_skills SET ... WHERE id=${row.id} AND tenant_id=${tid}`). Validate ownership before making the change.
- **server/tools.ts:8971** — UPDATE on tenant-scoped table `proposed_skills` executed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This marks proposals rejected without enforcing tenant isolation.
  - _Fix:_ Add an explicit tenant check in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) or validate the row's tenant before updating to prevent cross-tenant modifications.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' omits tenant scoping — `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` has no WHERE tenant_id = <tenant> clause.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or use the Drizzle .where(eq(table.tenantId, tenantId))). Even when id was fetched earlier, include the tenant predicate to enforce the app-level isolation invariant.
- **server/tools/domains/files/handlers.ts:906** — UPDATE on projects table is not tenant-scoped — `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` lacks a tenant_id constraint.
  - _Fix:_ Include the tenant constraint on the UPDATE to prevent cross-tenant modification. Example: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(projects.tenantId, tenantId))). Even if projId was derived from a tenant-checked query, the UPDATE must explicitly enforce tenant_id for defense-in-depth.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE executed against tenant-scoped table (knowledge_triples) without constraining tenant_id in the WHERE clause. The code updates rows `WHERE id = ${row.id}` but does not include `AND tenant_id = ${tenantId}`, so a malicious caller who knows/guesses an id could cause cross-tenant mutation.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}. Alternatively, look up and validate the row's tenant before updating or use a storage-layer helper that enforces tenant scoping. Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` performed without constraining by tenant_id (no `WHERE tenant_id = ...`). This loads pairing rows across all tenants using a caller-derived title (`${channel}:${phone}`), violating the invariant that tenant-scoped reads must be filtered at the app level.
  - _Fix:_ Restrict the query to the target tenant (or otherwise avoid scanning all tenants). For example, persist and query `tenant_id` alongside the title and use WHERE tenant_id = ${expectedTenantId} AND title = ${`${channel}:${phone}`}. If cross-tenant lookup is intentionally required, move the table to a global/shared table with explicit design and access controls or implement an operator-only privileged path — do NOT read tenant-scoped rows across tenants in general.
- **server/video-job-runner.ts:272** — listStaleVideoJobs selects from tenant-scoped table `videoJobs` without any tenant_id filter — the query can return rows belonging to other tenants.
  - _Fix:_ Either scope this query to a specific tenant (add eq(videoJobs.tenantId, tenantId)) and run recovery per-tenant, or explicitly restrict this routine to a privileged/administrative context and document/audit that it intentionally operates across tenants. Prefer the per-tenant approach (pass a tenantId into listStaleVideoJobs/recoverStaleVideoJobs and add eq(videoJobs.tenantId, tenantId) to the WHERE clause) to enforce application-level tenant isolation.
- **server/wiring-invariants.ts:535** — Unscoped raw query against memory tables: `SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`. The MEMORY_TABLES (messages, agent_knowledge, memory_entries, etc.) are tenant-scoped but are being counted across all tenants with no tenant_id constraint.
  - _Fix:_ If global sizing is desired only for admin ops, restrict this code to an admin-only maintenance job and clearly document the intent. For per-tenant alerts, run the COUNT() per tenant (WHERE tenant_id = ${tenantId}) or aggregate with an explicit tenant_id column in the SELECT. Avoid building raw SQL with table names where possible; use safe, parameterized queries or iterate tenants explicitly.
- **server/wiring-invariants.ts:260** — Unscoped SELECT against messages table: `SELECT count(*)::int AS n FROM messages WHERE created_at > NOW() - INTERVAL '14 days'`. messages is tenant-scoped but this query counts across all tenants with no tenant_id filter — privacy-sensitive and a cross-tenant data access risk.
  - _Fix:_ Either run this query per-tenant (adding WHERE tenant_id = ${tenantId}) or, if a global metric is needed, compute and store an explicitly authorized global aggregate in a separate admin-only pipeline. Do not run raw cross-tenant counts in normal app code; require admin privileges and logging/auditing for any global scans.

### MEDIUM (34)

- **server/auto-asset-capture.ts:207** — Selecting messages by conversation_id (SELECT content FROM messages WHERE conversation_id = ${c.id} ...) has no tenant_id guard. When combined with an unscoped list of conversations, this reads messages without asserting tenant ownership in the query.
  - _Fix:_ Constrain the messages SELECT to the conversation's tenant (e.g. AND tenant_id = ${c.tenant_id}) or JOIN messages to conversations and filter by conversations. Prefer queries that assert tenant_id in SQL rather than relying solely on in-process checks.
- **server/auth.ts:1130** — cleanExpiredResetTokens issues DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} without a tenant_id filter, removing password-reset tokens across all tenants.
  - _Fix:_ Scope cleanup to a tenant (WHERE tenant_id = ${tenantId}) or run as a platform-admin job with explicit safeguards and logging. Prefer tenant-scoped maintenance or a clearly documented and controlled global maintenance path.
- **server/chat-engine.ts:3797** — logDecision(conversationId, ...) is called without tenant context. If logDecision persists data in tenant-scoped storage, it must be supplied the tenant id to avoid cross-tenant writes.
  - _Fix:_ Pass tenantId/tenantIdForScope into logDecision and make sure logDecision uses that tenant id in all DB operations. If logDecision intentionally writes global telemetry, document and isolate it; otherwise enforce per-tenant scoping.
- **server/chat-engine.ts:4591** — Inconsistent tenant parameter usage: storage.createMessage(...) used tenantIdForScope, but storage.updateConversation(conversationId, {title}, tenantId) passes tenantId (not tenantIdForScope). If tenantIdForScope can differ or be undefined for subagent sources, updateConversation may be called with an incorrect or missing tenant context.
  - _Fix:_ Normalize on a single trusted tenant identifier for persisting conversation-related rows (e.g. tenantIdForScope) and pass it to all storage.* calls. Audit callsites to ensure subagent/non-subagent paths use the correct tenant value and that storage.updateConversation enforces WHERE tenant_id = <tenant>.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant_id filter: the scanner queries overdue commitments across all tenants (`SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`). This is a cross-tenant read.
  - _Fix:_ If this broad admin-level scan is intentional, ensure it is strictly restricted to privileged/operator-only code paths and that output is sufficiently redacted (as documented). If not intended, add an explicit `tenant_id = ${tenantId}` filter or require an explicit admin capability check before performing the SELECT to avoid accidental cross-tenant data exposure.
- **server/data-protection.ts:403** — SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} omits tenant_id. This reads a tenant-scoped table (messages) without constraining to the conversation's tenant.
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or join via the conversations/projects table verifying tenant ownership before counting so the count cannot cross tenant boundaries.
- **server/data-protection.ts:360** — SELECT from project_notes WHERE project_id = ${proj.id} is executed without verifying tenant scope. If project_notes is a tenant-scoped table, this read lacks an explicit tenant_id predicate or tenant ownership check.
  - _Fix:_ Scope reads on child tables by tenant as well — either include AND tenant_id = ${tenantId} on project_notes (and project_files), or join project_notes/project_files to projects and require p.tenant_id = ${tenantId} (or use withTenantTx) so accesses cannot cross tenants.
- **server/data-protection.ts:371** — SELECT from project_files WHERE project_id = ${proj.id} is executed without a tenant_id predicate. This reads a potentially tenant-scoped table using only a foreign id derived earlier, but lacks an explicit tenant constraint.
  - _Fix:_ Add tenant scoping (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or JOIN against projects with p.tenant_id = ${tenantId} to prove the project_files rows belong to the intended tenant before reading them.
- **server/heartbeat.ts:60** — Cross-tenant SELECT from tenant-scoped table without restricting to a tenant (SELECT DISTINCT tenant_id FROM messages ...). This reads tenant-scoped rows without an application-level tenant_id constraint.
  - _Fix:_ If the code intends a cross-tenant sweep, make that explicit and safe (e.g. select only tenant_id as metadata via a dedicated global/indexed table), or otherwise restrict the query to a tenant: WHERE tenant_id = ${tenantId}. For cross-tenant worker logic prefer enumerating tenant ids from a safe metadata table or use an explicit ALL_TENANTS-aware API that documents the cross-tenant intent.
- **server/heartbeat.ts:438** — SELECT * FROM event_log WHERE status = 'pending' ... — reading event_log (likely tenant-scoped) across all tenants without a tenant_id constraint.
  - _Fix:_ If the intent is a cross-tenant event queue worker, make the cross-tenant nature explicit and document it, or alternatively select tenant ids first and then process events per-tenant. If not intentional, add WHERE tenant_id = ${tenantId} to limit results to the caller's tenant.
- **server/job-queue.ts:320** — listJobs builds a WHERE clause only if a tenantId is provided; when tenantId is omitted this SELECT queries agent_jobs across all tenants (no tenant_id constraint).
  - _Fix:_ Make tenantId required for tenant-scoped callers, or enforce an explicit admin-only path when tenantId is absent. If listing across tenants is necessary, gate the call behind an admin permission and audit/log it.
- **server/job-queue.ts:385** — getJobStats SELECTs aggregated counts from agent_jobs with no tenant_id filter — returns cross-tenant aggregates without tenant scoping.
  - _Fix:_ If these stats are intended to be per-tenant, add WHERE tenant_id = ${tenantId}. If global stats are required, ensure the caller is an explicitly authorized admin and document the cross-tenant nature and auditing.
- **server/lib/mcp-api-keys.ts:182** — Best-effort touch UPDATE on tenant-scoped table `mcp_api_keys` uses WHERE id = ${row.id} without constraining by tenant_id. Although verifyApiKey resolved the row by prefix, the subsequent write is not explicitly tenant-scoped.
  - _Fix:_ Constrain the UPDATE by tenant_id as well, e.g. UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}. This makes the write explicitly tenant-scoped and avoids reliance on implicit row identity alone.
- **server/lib/outcome-promotion.ts:158** — Scan aggregates tenant-scoped table `deliverable_grade_events` across all tenants with no tenant_id constraint (SELECT ... FROM deliverable_grade_events WHERE created_at > ...). This is a cross-tenant read of a tenant-scoped table.
  - _Fix:_ If this global scan is intentional, make the threat model explicit and ensure the job runs in an admin-only, audited context. Prefer a per-tenant iteration: fetch the list of tenant_ids to scan and run the aggregation per tenant using WHERE tenant_id = <tenant>. Alternatively, add an explicit tenant_id parameter to the query so callers must opt into a single-tenant scan. Ensure any output files or manifests are written with tenant isolation (directories/names) and that only authorized admin processes can read aggregated cross-tenant results.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() to inject `lockKey` into SQL: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). lockKey is derived from ledgerKey (caller-supplied) and tenantId — using sql.raw() bypasses parameterization and can be an injection/isolation risk if ledgerKey is not strictly validated.
  - _Fix:_ Avoid sql.raw() with any value that can originate from callers. Convert the lock value to a numeric/BigInt and pass it as a parameterized value (e.g. compute a numeric BigInt and use tx.execute(sql`SELECT pg_advisory_xact_lock(${bigintValue}::bigint)`)). Also validate/whitelist ledgerKey where possible before deriving the lock.
- **server/routes.ts:780** — Startup backfill SELECT queries iterate projects across all tenants (SELECT p.id, p.name, p.tenant_id ... FROM projects JOIN tenants ... WHERE p.drive_folder_id IS NULL) with no tenant scoping.
  - _Fix:_ When operating globally from startup/backfill code, be explicit about that fact (comment) and ensure the code is safe to run cross-tenant. If the operation is intended to be per-tenant, add tenant filtering or iterate tenants explicitly and run the backfill per-tenant to avoid accidental cross-tenant data access during backfill.
- **server/routes.ts:3459** — POST /api/messages/:id/feedback selects the messages row by message id without constraining by tenant_id before using the result. The handler then checks msgRow[0].tenantId !== tenantId after the SELECT, which still allows probing of message IDs across tenants.
  - _Fix:_ Query the messages table with an explicit tenant filter (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}) so ownership is enforced by the SQL itself. Alternatively, use a storage helper that verifies ownership prior to reading the row to avoid returning/exposing rows that don't belong to the caller.
- **server/routes/heartbeat.ts:234** — SELECT from `heartbeat_logs` uses only task_id (WHERE task_id = ${task.id}) with no tenant check. This reads log rows without ensuring they belong to the expected tenant. 
  - _Fix:_ Add an explicit tenant predicate (e.g. JOIN heartbeat_tasks ht ON hl.task_id = ht.id AND ht.tenant_id = ${ADMIN_TENANT_ID} or include AND EXISTS/AND ht.tenant_id = ${...}) or SELECT via a storage/helper that enforces tenant scoping so log rows cannot be returned for a different tenant's task.
- **server/routes/metrics.ts:113** — Query on `repair_incidents` groups by classification/action without any tenant_id predicate — may expose per-tenant incident counts or leak tenant-scoped data.
  - _Fix:_ If repair_incidents is tenant-scoped, add a tenant filter (WHERE tenant_id = ...) or route this aggregation through an admin-only helper that enforces visibility policies. If global aggregation is intended, ensure the endpoint uses strong operator auth and that exposing tenant-scoped aggregates is acceptable.
- **server/skills-marketplace.ts:209** — SELECT id FROM skills WHERE name = ${template.name} has no tenant_id constraint. If skills is tenant-scoped this can leak or collide with another tenant's skill records.
  - _Fix:_ If skills is tenant-scoped, require a tenantId parameter and add WHERE tenant_id = ${tenantId} to the query. If this table is intentionally global, add a code comment to document that and ensure the global semantics are safe and reviewed.
- **server/skills-marketplace.ts:235** — SELECT * FROM skills WHERE id = ${skillId} lacks tenant scoping. This may disclose another tenant's skill prompt/content if skills is tenant-scoped.
  - _Fix:_ Either require and pass a tenantId and constrain the SELECT with tenant_id = ${tenantId}, or explicitly document/implement that skills is a global/shared table and performs an authorized global read only.
- **server/stability-watchdog.ts:177** — SELECT FROM heartbeat_logs aggregates errors across heartbeat_logs without any tenant_id filter. This reads tenant-scoped logs across all tenants.
  - _Fix:_ If this maintenance watchdog intentionally operates across tenants, make that explicit (e.g. pass ALL_TENANTS and document). Otherwise, add WHERE tenant_id = ${tenantId} or iterate per-tenant to avoid accidental cross-tenant reads/deletes.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' ... executes without tenant_id constraint, deleting tenant-scoped rows across tenants.
  - _Fix:_ Restrict deletion to a tenant context (WHERE tenant_id = ${tenantId} AND ...), or, if cross-tenant cleanup is intended, make the cross-tenant scope explicit and audited (e.g. document and use an explicit ALL_TENANTS mechanism).
- **server/stability-watchdog.ts:261** — DELETE FROM heartbeat_logs WHERE created_at < ... is executed without a tenant_id filter and will delete heartbeat logs across all tenants.
  - _Fix:_ Limit pruning to a tenant's logs by adding WHERE tenant_id = ${tenantId} AND created_at < ...; if the system must prune across tenants, require an explicit administrative/all-tenants invocation and log that action to avoid accidental cross-tenant data modification.
- **server/storage.ts:568** — INSERT into memory_entries is performed without verifying or enforcing that data.tenantId is present/valid. The storage.createMemoryEntry code simply inserts the provided object, relying on callers to supply tenantId.
  - _Fix:_ Fail-closed by validating tenantId before insert (e.g. assertValidTenantId(data.tenantId) or require a tenantId parameter). Explicitly set tenantId in the insert and throw if missing/invalid to avoid accidental unscoped writes.
- **server/storage.ts:861** — getMemoriesWithoutEmbeddings selects memory_entries WHERE status='active' AND embedding IS NULL with NO tenant_id filter. This reads across tenants.
  - _Fix:_ If this is intended as an admin/global batch operation, make that explicit (e.g. accept a TenantScope and use requiredTenantScope or document and restrict to admin contexts). Otherwise accept/require tenantId and add tenant scoping (eq(memoryEntries.tenantId, tenantId)).
- **server/storage.ts:866** — getKnowledgeWithoutEmbeddings selects agent_knowledge rows with no tenant_id constraint (notExpired && isNull(embedding)) — reads across tenants.
  - _Fix:_ Either require an explicit tenant scope (use requiredTenantScope/ALL_TENANTS for intentional global scans) or accept/require tenantId and add eq(agentKnowledge.tenantId, tenantId) to the WHERE clause to enforce isolation.
- **server/storage.ts:547** — getAllMemoriesForBackup returns all memory_entries across tenants (no tenant_id filter). This provides unscoped access to tenant-scoped data.
  - _Fix:_ Require an explicit tenant scope or admin authorization for backups, or accept a TenantScope parameter (using requiredTenantScope/ALL_TENANTS) so the global intent is explicit and auditable. Otherwise add eq(memoryEntries.tenantId, tenantId) to limit to a tenant.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` by id is performed without a tenant_id constraint. The code reads a tenant-scoped table row (conversations) without scoping to a caller tenant before using its value; this is a deviation from the requirement that tenant-scoped reads include tenant_id filters.
  - _Fix:_ Query the conversation using both id and the claimed tenant id to verify ownership in a single, constrained read (e.g. SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1). This avoids reading arbitrary conversation rows across tenants and enforces the ownership check atomically.
- **server/wiring-invariants.ts:366** — Unscoped SELECT against subsystem table using sql.raw(): `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`. Several subsystem tables (knowledge_nudges, tool_performance, etc.) are tenant-scoped but this query aggregates across all tenants with no tenant_id filter.
  - _Fix:_ If this check is intended to run cross-tenant as an admin/observability job, make that explicit and run it under an audited admin context. Otherwise restrict the query to a specific tenant_id (WHERE tenant_id = ${tenantId}) or iterate tenants and run per-tenant checks. Prefer parameterized Drizzle queries over sql.raw() and avoid interpolating table/column names from untrusted sources.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*)::int AS cnt FROM graph_memory runs without a tenant_id constraint. graph_memory is a tenant-scoped store but this query inspects it globally.
  - _Fix:_ Either scope the check to a specific tenant (WHERE tenant_id = ${tenantId}) or run it per-tenant in an admin-only routine. Document and gate any global scans behind explicit admin intent, and prefer parameterized queries rather than raw SQL strings.
- **server/wiring-invariants.ts:244** — Unscoped SELECT on tool_performance: `SELECT count(*)::int AS n FROM tool_performance WHERE last_success_at > NOW() - INTERVAL '14 days'`. tool_performance records are tenant-scoped but this query aggregates across all tenants without tenant_id filtering.
  - _Fix:_ Scope the query by tenant_id when used for tenant-specific diagnostics, or, if intended to be a platform-wide check, run it only from an audited admin flow and make the global intent explicit. Consider aggregating per-tenant (GROUP BY tenant_id) so results remain tenant-aware.
- **server/wiring-invariants.ts:258** — Unscoped SELECT counting total rows in tool_performance: `SELECT count(*)::int AS n FROM tool_performance` — no tenant_id filter on a tenant-scoped table.
  - _Fix:_ Restrict to a tenant (WHERE tenant_id = ${tenantId}) or perform a per-tenant aggregation when running from an admin context. Avoid global, unfiltered reads of tenant-scoped tables unless explicitly required and access-controlled.
- **server/wiring-invariants.ts:417** — Unscoped aggregation on tool_performance in tool-drift detector: query aggregates SUM(success_count), SUM(fail_count) etc. without filtering by tenant_id. If tool_performance is tenant-scoped this reads across tenants.
  - _Fix:_ If tool drift detection should be tenant-local, add WHERE tenant_id = ${tenantId}. If the intent is platform-wide, make it an explicit admin-only routine and/or aggregate per-tenant (GROUP BY tenant_id) so you can reason about tenant boundaries. Avoid silent global scans of tenant-scoped tables.

### Suppressed — confirmed false positives (221)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — SELECT query reads agent_runs (joined with agent_approvals) without constraining to a specific tenant_id. The db.execute(sql`SELECT ... FROM agent_runs ... WHERE ...`) in resumePendingApprovalRuns returns rows across tenants because there is no WHERE r.tenant_id = ${tenantId} (the function does not accept/apply a tenant filter). This allows cross-tenant reads and potential cross-tenant actions on runs/approvals.
  - _Why false positive:_ Background sweep intentionally scans across tenants, carries row tenant_id, and performs housekeeping/resume work rather than returning mixed tenant data to a tenant-facing surface.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE / claim for agent_wake_schedules does not constrain by tenant_id. The subquery SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... returns rows across all tenants, and the outer UPDATE sets status/attempts for those ids without a tenant filter — allowing cross-tenant claiming/mutation of wake rows.
  - _Why false positive:_ Global scheduler claims due wake rows across tenants and then processes each row with its own tenant_id; this is platform-wide housekeeping by design.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations ... WHERE id = ${c.conversation_id} ... does not include tenant_id in the WHERE clause — modifying a tenant-scoped table by id only can affect rows belonging to other tenants.
  - _Why false positive:_ Backfill/maintenance loop selects linked rows with tenant-consistent joins and updates housekeeping linkage fields during a platform-wide reconciliation job.
- **server/channel-routing.ts:16** _(global-table)_ — channel_routes table is created with no tenant_id column. Without a tenant_id column, you cannot enforce tenant isolation at the application level (and there is no RLS/fk cascade).
  - _Why false positive:_ channel_routes is defined without tenant_id and appears intentionally global with UNIQUE(channel), indicating one platform-wide route per channel.
- **server/channel-routing.ts:30** _(global-table)_ — listChannelRoutes performs a SELECT joining channel_routes and personas with no tenant_id filter. This can leak other tenants' persona information if those tables are tenant-scoped.
  - _Why false positive:_ Reads from intentionally global channel_routes and personas schema note shows no tenant_id on personas, so no tenant filter is expected.
- **server/channel-routing.ts:51** _(global-table)_ — DELETE FROM channel_routes WHERE channel = ${channel} runs without restricting by tenant_id. This will delete routes for all tenants that share the same channel identifier.
  - _Why false positive:_ Delete operates on globally unique channel in a platform-global routing table, not a tenant-scoped table.
- **server/channel-routing.ts:54** _(global-table)_ — INSERT INTO channel_routes (channel, persona_id) ... inserts without setting tenant_id. Tenant_id is not supplied, so multi-tenant isolation cannot be enforced and data may be assigned to an implicit/global scope.
  - _Why false positive:_ Insert targets intentionally global routing table keyed by UNIQUE(channel), so absence of tenant_id matches global-table design.
- **server/channel-routing.ts:64** _(global-table)_ — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true has no tenant_id constraint. It may return a persona_id belonging to another tenant.
  - _Why false positive:_ Lookup by global channel from platform-global routing table is expected and not a tenant-isolation issue.
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute executes DELETE FROM channel_routes WHERE channel = ${channel} without a tenant filter, deleting routes across all tenants.
  - _Why false positive:_ Removal by channel acts on the same intentionally global routing table.
- **server/chat-engine.ts:573** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` is executed without an explicit tenant_id constraint. The query uses only project_id (which was validated earlier in code comment), but per the tenant-isolation invariant every access to a tenant-scoped table must include an explicit tenant_id filter.
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped through project_id; project ownership was verified just above via projects WHERE id AND tenant_id.
- **server/chat-engine.ts:585** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` is executed without an explicit tenant_id constraint (WHERE project_id = ${projectId} only). Even though projectId was derived from a tenant-validated project earlier, the SQL lacks an explicit tenant_id check.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively isolated by validated parent project_id after explicit project tenant check above.
- **server/commitment-drafter.ts:234** _(internal-worker-pk)_ — Unscoped UPDATE against tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — missing tenant_id constraint. This can mutate another tenant's row if ids collide.
  - _Why false positive:_ server worker loop updates by previously claimed server-selected row.id; id is not taken from an untrusted request boundary.
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} lacks a tenant_id predicate. This writes/deletes a tenant-scoped table row without scoping by tenant_id.
  - _Why false positive:_ This is a platform-wide purge sweep over expired soft-deleted conversations; it processes each row individually and only performs housekeeping deletes, not tenant-facing data return/mixing.
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks embedding via UPDATE ... WHERE id = ${row.id} with no tenant_id constraint. Updating by id only can modify another tenant's chunk if ids are guessed or data inconsistent.
  - _Why false positive:_ The rows being updated were selected immediately above with collection_id and tenant_id constraints, so row.id comes from an already tenant-scoped trusted set.
- **server/email.ts:470** _(system-sweep)_ — SELECT from tenant-scoped table `inbox_messages` without any tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can return rows belonging to other tenants.
  - _Why false positive:_ Shared inbox polling is a platform-wide ingest/dedup loop over external mailbox messages; the lookup is for housekeeping dedup by provider message_id, not tenant-facing data retrieval.
- **server/email.ts:515** _(system-sweep)_ — SELECT from tenant-scoped table `inbox_messages` without tenant_id filter (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id). This reads all tenants' inbox rows.
  - _Why false positive:_ backfillEmptyBodies is a maintenance backfill across all inbox rows and only fills missing body fields per row, without returning mixed tenant data to any tenant-facing surface.
- **server/email.ts:528** _(system-sweep)_ — UPDATE against tenant-scoped table `inbox_messages` without tenant_id constraint (UPDATE inbox_messages SET ... WHERE id = ${row.id}). This can update rows from other tenants that share the id.
  - _Why false positive:_ This UPDATE is part of the same maintenance backfill loop and only mutates housekeeping/content fields on rows selected by the sweep, with no tenant-facing mixing.
- **server/embeddings.ts:752** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint in backfillMissingKnowledgeEmbeddings (SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50). This will scan rows across all tenants.
  - _Why false positive:_ Boot/maintenance backfill intentionally scans across tenants, processes each row independently, and only writes embedding housekeeping fields; no tenant data is returned or mixed into one output/prompt.
- **server/embeddings.ts:780** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` without tenant_id constraint (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200). This reads rows across tenants.
  - _Why false positive:_ Platform-wide embedding_vec backfill is maintenance-only across tenants and reads per-row data solely to repair derived vector fields.
- **server/embeddings.ts:790** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} — missing tenant_id constraint. This can update memory entries for other tenants.
  - _Why false positive:_ Update is part of a cross-tenant maintenance sweep mutating only housekeeping/derived embedding_vec on rows selected by the sweep; no tenant-facing mixing.
- **server/embeddings.ts:796** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200). This reads rows across tenants.
  - _Why false positive:_ Knowledge embedding_vec backfill is a maintenance sweep across tenants and only repairs derived vector fields per row.
- **server/embeddings.ts:807** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} — missing tenant_id constraint. This allows modifying other tenants' agent_knowledge rows.
  - _Why false positive:_ Update is maintenance-only in a platform sweep, touching only derived embedding_vec for each selected row.
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT INTO project_files does not include tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL/no default). The INSERT relies on prior checks rather than explicitly setting tenant_id, enabling missing or cross-tenant writes.
  - _Why false positive:_ Schema note says project_files has no tenant_id column; isolation is transitively enforced through tenant-scoped parent project_id and assertProjectInTenant check.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` without any tenant_id constraint (reads stale/other-tenant proposals). The subsequent UPDATE (lines 333-337) also updates rows by id alone with no tenant check — allows modifying other tenants' rows.
  - _Why false positive:_ Admin health-audit TTL sweep intentionally scans and archives stale proposals platform-wide; it emits audit findings and mutates status only, with no tenant-facing data mixing.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table `heartbeat_tasks` without any tenant_id constraint (identifies 'stuck' tasks across tenants). The subsequent UPDATE (lines 368-371) disables tasks by id with no tenant check — may disable other tenants' heartbeat tasks.
  - _Why false positive:_ Platform maintenance sweep identifies and disables stuck heartbeat tasks across tenants, changing only housekeeping state and producing audit findings.
- **server/memory-graph.ts:252** _(fk-transitive)_ — INSERT into tenant-scoped table `memory_links` does not set tenant_id (INSERT INTO memory_links (... ) VALUES (${newMemoryId}, ${target.id}, ...)). Every INSERT into tenant-scoped tables must explicitly set tenant_id; omitting it can create cross-tenant data or violate NOT NULL constraints.
  - _Why false positive:_ Schema note says memory_links has no tenant_id column; link rows are transitively scoped through source/target memory_entries parent rows.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE against tenant-scoped table 'knowledge_triples' is executed without constraining tenant (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}). This can modify rows belonging to other tenants.
  - _Why false positive:_ Rows being updated come directly from a prior SELECT on knowledge_triples constrained by tenant_id = tenantId, so each c.id is already tenant-scoped in the same call path.
- **server/persona-export.ts:33** _(global-table)_ — SELECT * FROM personas WHERE id = ${personaId} executes with no tenant_id constraint. Personas are tenant-scoped; this allows reading a persona that belongs to another tenant (data exposure).
  - _Why false positive:_ schema note says personas has no tenant_id, so base persona row is intentionally non-tenant-scoped/global while tenant-scoped adjunct data is separately filtered
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim UPDATE on plans lacks any tenant_id constraint: `UPDATE plans SET status = 'executing' ... WHERE id = ${planId} AND status = 'approved'`. This allows claiming/modifying a plan by id across tenants.
  - _Why false positive:_ executePlan is an internal server worker claiming a server-generated plan by unique primary key, not directly from an untrusted tenant request boundary
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} — the UPDATE does not include tenant_id. This allows modifying tasks across tenants if an id from another tenant is present.
  - _Why false positive:_ each t.id comes from a prior SELECT on heartbeat_tasks scoped by ht.tenant_id = tenantId in the same call path
- **server/process-governor.ts:642** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} — update lacks tenant_id constraint. Although the desks were selected earlier with tenant filter, updating by id alone can mutate another tenant's row if ids collide or data was tampered.
  - _Why false positive:_ desk.id values are loaded immediately above from agent_desks with ad.tenant_id = tenantId before this update loop
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE agent_knowledge executed without constraining by tenant_id (UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}). This allows a row from another tenant with the same id to be modified by the system runner.
  - _Why false positive:_ scheduled runner sweeps due jobs across tenants but each row carries its own r.tenant_id and this update only advances housekeeping content for that same row id
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries sets fact/last_accessed/access_count WHERE id = ${storedId} without also constraining tenant_id. This can update another tenant's memory entry if IDs collide.
  - _Why false positive:_ storedId is obtained immediately above from a SELECT on memory_entries constrained by input.tenantId and persona/category/dedup prefix
- **server/research-engine.ts:165** _(system-sweep)_ — cleanupZombieSessions runs UPDATE research_sessions ... WHERE status = 'running' ... started_at < ${staleThreshold} without constraining by tenant_id. This will mark running sessions for all tenants as completed potentially touching other tenants' sessions.
  - _Why false positive:_ zombie-session cleanup is platform-wide maintenance updating only status/ended_at/summary fields and returns aggregate/internal ids, not tenant data to another tenant
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() executed without tenant_id constraint. This deletes expired autoresearch rows across all tenants.
  - _Why false positive:_ global expiry purge of expired autoresearch knowledge is housekeeping across tenants and does not mix or disclose tenant content
- **server/routes.ts:2074** _(signed-url)_ — Public GET /api/presenter/:token selects presenter_sessions by token without constraining by tenant_id. This leaks presenter_session → tenant mapping (tenant_id) and allows unscoped access based solely on the token value.
  - _Why false positive:_ Public presenter lookup is capability-token based; response does not return tenant_id and access is authorized by possession of token.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` does not set tenant_id — `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` omits tenant_id (columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project/conversation rows already checked/created under same tenant.
- **server/routes/conversations.ts:230** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` omits tenant_id — `INSERT INTO project_notes (project_id, note, author) VALUES (...)` writes tenant-scoped data without tenant_id.
  - _Why false positive:_ project_notes has no tenant_id; inserted note is scoped transitively via project_id after project ownership join check.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} executes without constraining by tenant_id. This allows modifying another tenant's inbox row if an attacker controls ids or bypasses prior checks.
  - _Why false positive:_ Same handler selected msg by id and tenant_id immediately above; update uses that verified row's pk only.
- **server/routes/projects.ts:256** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (...)`. The code relies on a prior project ownership check but the INSERT itself omits tenant_id (columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_notes has no tenant_id and note insert is correctly scoped through previously verified tenant-owned project_id.
- **server/routes/projects.ts:296** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload path) omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)`. Tenant id is not being written with the row.
  - _Why false positive:_ project_files has no tenant_id and file row is child of project; project ownership is checked with tenant_id before insert.
- **server/routes/projects.ts:364** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload path) omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...)`. The tenant_id column is not set on new rows.
  - _Why false positive:_ project_files has no tenant_id and multipart upload insert is transitively scoped by previously verified tenant-owned project.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} performs a write against a tenant-scoped table without constraining tenant_id. A write without tenant scoping can modify another tenant's data.
  - _Why false positive:_ Boot-time seed/backfill intentionally repairs research_programs across tenants and only mutates housekeeping/model field, returning no tenant data.
- **server/seed.ts:3911** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` performed without constraining to a tenant (no WHERE tenant_id = ...). This updates rows across all tenants.
  - _Why false positive:_ Global maintenance normalizer updates memory_entries in place across tenants without mixing tenant data into outputs.
- **server/seed.ts:3936** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` performed without constraining to a tenant (UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1'). This will modify conversations for all tenants.
  - _Why false positive:_ Global seed backfill intentionally updates legacy model ids across all tenants and only mutates a housekeeping field.
- **server/self-improvement.ts:401** _(internal-worker-pk)_ — runExperiment updates the experiments row using only eq(experiments.id, experiment.id) with no tenant_id constraint, permitting modification of rows belonging to other tenants if IDs are guessed or manipulated.
  - _Why false positive:_ Experiment row update is in internal worker logic using server-side experiment.id, not directly from an untrusted request boundary.
- **server/self-improvement.ts:415** _(internal-worker-pk)_ — In the error catch path runExperiment performs db.update(experiments).set(...).where(eq(experiments.id, experiment.id)) without restricting by tenant_id, allowing cross-tenant writes.
  - _Why false positive:_ Catch-path update likewise targets a server-owned experiment primary key inside internal worker flow, not caller-supplied tenant-crossing input.
- **server/surprise-scorer.ts:93** _(internal-worker-pk)_ — UPDATE against tenant-scoped table felix_proposals missing tenant_id constraint (stampArgsEmbedding uses: WHERE id = ${proposalId} AND args_embedding IS NULL). This allows cross-tenant modification of proposal rows.
  - _Why false positive:_ Server-internal worker stamps a row by server-side proposal PK after same module uses validated tenant-scoped lookup; not from request boundary.
- **server/surprise-scorer.ts:139** _(internal-worker-pk)_ — UPDATE writes actual_outcome_embedding to felix_proposals without constraining tenant (WHERE id = ${proposalId}). Missing tenant_id filter allows cross-tenant writes.
  - _Why false positive:_ Internal scorer updates proposal by server-side PK in worker flow after tenant-scoped fetch of that proposal.
- **server/surprise-scorer.ts:148** _(upstream-verified)_ — SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} executes without tenant_id constraint. This can return or operate on a row belonging to another tenant.
  - _Why false positive:_ Same call path already selected the proposal with id plus tenant_id and is re-fetching embedding for that validated row.
- **server/surprise-scorer.ts:181** _(internal-worker-pk)_ — UPDATE setting surprise_band = 'no_history' on felix_proposals uses WHERE id = ${proposalId} without tenant_id. This writes tenant-scoped metadata without tenant constraint.
  - _Why false positive:_ Internal worker writes status metadata to the same server-identified proposal row after tenant-scoped validation.
- **server/surprise-scorer.ts:212** _(internal-worker-pk)_ — UPDATE that writes surprise_score and surprise_band to felix_proposals uses WHERE id = ${proposalId} with no tenant_id filter. Tenant-scoped write lacking tenant constraint allows cross-tenant modification.
  - _Why false positive:_ Internal scorer updates score/band on previously tenant-validated proposal by server-side PK, not untrusted input.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE increments usageCount on custom_tools using WHERE id = ${tool.id} with no tenant_id constraint. This is a tenant-scoped write that lacks tenant scoping and could affect other tenants' rows.
  - _Why false positive:_ Earlier in same function the tool row is looked up with name and tenant_id, and this write uses that validated row's id.
- **server/tools.ts:4552** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The code inserts a project-scoped row (project_notes) without explicitly passing tenant_id in the VALUES list, relying solely on prior ownership checks. Per the project's invariant every INSERT into tenant-scoped tables must include the tenant_id column/value.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated transitively via validated project_id ownership.
- **server/tools.ts:4556** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The insertion of a project_files row does not include tenant_id in the columns/VALUES, violating the rule that every INSERT into tenant-scoped tables must pass tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and insert is tied to a project_id already verified in tenant.
- **server/tools.ts:6405** _(fk-transitive)_ — INSERT into project_files omits tenant_id. After assertProjectInTenant the code inserts into project_files but does not include tenant_id in the INSERT columns/VALUES, violating the requirement that tenant_id be set explicitly on tenant-scoped writes.
  - _Why false positive:_ project_files has no tenant_id column and insert uses project_id after assertProjectInTenant ownership check.
- **server/tools.ts:6941** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The slideshow video code inserts into project_files (video) without specifying tenant_id in the INSERT, contradicting the invariant that every tenant-scoped INSERT must include tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by assertProjectInTenant on tenant-scoped parent project_id
- **server/tools/domains/security/handlers.ts:163** _(upstream-verified)_ — SELECT from tenant-scoped table `provider_keys` is executed without constraining to the caller's tenant (no WHERE tenant_id = ${tenantId}). This can allow cross-tenant enumeration of API keys.
  - _Why false positive:_ query only runs under explicit tenantId === 1 admin gate; provider_keys is treated as admin-visible/global in this path
- **server/tools/domains/security/handlers.ts:260** _(upstream-verified)_ — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executes without a tenant_id constraint. This reads tenant-scoped provider key data across all tenants.
  - _Why false positive:_ aggregation is guarded by tenantId === 1 before unscoped provider_keys read
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} updates a tenant-scoped table row by id only, without also constraining by tenant_id. An attacker or bug could modify another tenant's row sharing the same id.
  - _Why false positive:_ id comes from immediately prior SELECT on memory_entries scoped by tenant_id, persona_id, category, and dedup prefix in same call path
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) updates memory_entries by id array without also constraining tenant_id. IDs could collide across tenants leading to cross-tenant updates.
  - _Why false positive:_ ids array is derived solely from rows just selected with tenant_id = tid in same function
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries SET fact = ${fact} ... WHERE id = ${id} performs an update on a tenant-scoped table without filtering by tenant_id, allowing cross-tenant modification if ids overlap.
  - _Why false positive:_ updated id is obtained from prior tenant-scoped SELECT on memory_entries in same call path
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from experiments in runDueAbExperiments lacks a tenant_id filter (reads experiments across all tenants).
  - _Why false positive:_ heartbeat maintenance sweep intentionally scans running experiments across tenants and processes each row with its own tenant_id on update
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE agent_evals sets completion fields WHERE id = ${evalId} without constraining by tenant_id — an attacker or bug could update another tenant's eval row by id.
  - _Why false positive:_ evalId is the server-generated primary key returned from the immediately preceding INSERT in same worker flow, not from an untrusted request boundary
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from ai_insights in retryPendingMinervaRouting() has no tenant_id constraint and will fetch HIGH-priority applied insights across all tenants.
  - _Why false positive:_ durability retry sweep intentionally reads across tenants and carries each row's tenant_id forward for per-row routing/update
- **server/agentic-engines.ts:247** _(system-sweep)_ — UPDATE ai_insights in autoApplyEligibleInsights() can run without tenant scoping when called with tenantId undefined — the WHERE clause may not include tenant_id and will affect all tenants.
  - _Why false positive:_ explicit backfill sweep may run globally by design; it only mutates housekeeping/status fields and returns row-carried tenant_id for per-row handling
- **server/agentic-features.ts:698** _(upstream-verified)_ — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} is missing tenant_id in the WHERE clause (updates by id only).
  - _Why false positive:_ lead.id is taken from leadRows previously selected from lead_enrichments with tenant_id = params.tenantId in same function
- **server/agentic-features.ts:773** _(upstream-verified)_ — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' lacks tenant_id scoping — allows checking/operating on enrollments from other tenants.
  - _Why false positive:_ Sequence ownership is verified immediately above with id + tenant_id; this duplicate-check is within that same trusted sequence context.
- **server/agentic-features.ts:932** _(upstream-verified)_ — UPDATE outreach_enrollments SET reply_classification = ... WHERE id = ${row.id} is not tenant-scoped — it updates an enrollment by id without verifying tenant ownership.
  - _Why false positive:_ row.id comes from a prior SELECT on outreach_enrollments scoped by params.tenantId, so the UPDATE targets a tenant-verified row from the same call path.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals() runs UPDATE agentApprovals ... WHERE status = 'pending' AND expiresAt < now() with no tenant filter — it will expire approvals across all tenants in a single statement.
  - _Why false positive:_ Global expiry sweep intentionally updates stale pending approvals across tenants and then handles each returned row with its own tenantId for linked run updates.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — Backfill query selects DISTINCT conversations (id, project_id, tenant_id) across the entire table with no tenant_id restriction. This reads tenant-scoped rows across all tenants in one global sweep.
  - _Why false positive:_ This is a platform-wide backfill loop over conversations; each row carries its own tenant_id and is processed individually rather than returned to a tenant-facing mixed output.
- **server/auth.ts:1262** _(system-sweep)_ — Maintenance DELETE deletes expired auth_sessions across all tenants (DELETE FROM auth_sessions WHERE expires_at < ${now}) with no tenant_id constraint — a global write against a tenant-scoped table.
  - _Why false positive:_ Boot-time cleanup of expired sessions is global housekeeping that only deletes stale rows and does not expose or mix tenant data.
- **server/auth.ts:1266** _(system-sweep)_ — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' removes sessions across all tenants (no tenant_id filter). This is a global mutation on a tenant-scoped table.
  - _Why false positive:_ One-time secure-boot cleanup removing legacy v1 session rows is a global maintenance mutation only, with no tenant data returned or mixed.
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT from messages lacks tenant_id predicate (reads messages for a conversation without constraining by tenant).
  - _Why false positive:_ Conversation ownership is proven just above via conversations.id + tenant_id, and messages are then read by that verified conversation_id in the same flow.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from messages lacks tenant_id predicate (reading conversation messages without constraining by tenant).
  - _Why false positive:_ assertConversationInTenant plus the subsequent tenant-scoped conversation/project checks prove ownership before reading messages for that conversation in the same call path.
- **server/auto-transcript.ts:132** _(system-sweep)_ — Subquery SELECT COUNT(*) FROM messages used in a global scan is missing a tenant filter (counts messages for conversations without scoping to the conversation's tenant).
  - _Why false positive:_ Global transcript backfill scans conversations across tenants for housekeeping, then processes each conversation separately using its row-carried tenant_id.
- **server/channel-routing.ts:48** _(global-table)_ — setChannelRoute accepts a personaId from the caller and writes it into channel_routes without validating that the persona belongs to the caller's tenant. This allows referencing another tenant's persona_id.
  - _Why false positive:_ channel_routes is defined here without tenant_id and appears intentionally global by channel
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT on conversations (expired soft-deletes) does not include a tenant_id predicate — it reads rows across all tenants.
  - _Why false positive:_ platform-wide purge job reads all expired rows and processes each using row-carried tenant_id
- **server/data-protection.ts:442** _(upstream-verified)_ — Subquery (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id) in getDeletedConversations does not include tenant_id, so message counts may be computed without scoping to the caller's tenant.
  - _Why false positive:_ outer query scopes conversations by tenant_id and message count is tied to those tenant-owned conversation ids
- **server/dfy-intake.ts:50** _(signed-url)_ — getDfyIntakeByToken selects dfy_intake_forms by token only (WHERE eq(dfyIntakeForms.token, token)) with no tenant_id constraint. Reading tenant-scoped table rows without a tenant filter can leak another tenant's form data.
  - _Why false positive:_ unguessable 48-hex token is the access-control secret for form retrieval
- **server/firecrawl.ts:231** _(upstream-verified)_ — UPDATE on scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) without also scoping by tenant_id. This can modify a scraped page row belonging to another tenant if an id is reused or guessed.
  - _Why false positive:_ row id used for update was selected immediately above with matching tenant_id
- **server/firecrawl.ts:363** _(upstream-verified)_ — In the crawl loop, existing scraped page updates use WHERE eq(scrapedPages.id, existing[0].id) without constraining by tenant_id — allowing cross-tenant updates to scrapedPages rows.
  - _Why false positive:_ row id used for update was selected immediately above with matching tenant_id
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' is performed without scoping by tenant_id (WHERE tenant_id = ...). The query uses only projects.id in the WHERE clause.
  - _Why false positive:_ same call path immediately checks selected project.tenantId against caller tenant before use
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' uses WHERE projects.id = <projectId> but does NOT include tenant_id in the WHERE clause. Writes are not constrained to the caller tenant.
  - _Why false positive:_ Same function selects project, verifies project.tenantId === tenantId, then updates by that verified projectId.
- **server/heartbeat.ts:105** _(system-sweep)_ — SELECT * FROM research_schedules ... executed without a tenant_id filter. research_schedules appears to be tenant-scoped but is queried across tenants with no tenant constraint.
  - _Why false positive:_ Heartbeat is a platform-wide scheduler sweep intentionally selecting due schedules across tenants and processing each row using sched.tenant_id.
- **server/heartbeat.ts:154** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — update to a tenant-scoped table is performed without constraining by tenant_id.
  - _Why false positive:_ Maintenance sweep updates housekeeping field next_run_at on the schedule row it is processing; no tenant data returned or mixed.
- **server/heartbeat.ts:160** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id constraint on update to a tenant-scoped table.
  - _Why false positive:_ Heartbeat scheduler advances last_run_at/next_run_at for due rows in a cross-tenant maintenance loop using each row's own tenant_id context.
- **server/heartbeat.ts:173** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — update performed with only id filter and no tenant_id check.
  - _Why false positive:_ Error-recovery update is part of the same cross-tenant housekeeping sweep and only advances scheduling metadata on the current row.
- **server/heartbeat.ts:325** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — direct update to heartbeat_tasks without tenant_id constraint. heartbeat_tasks is tenant-scoped in other call sites.
  - _Why false positive:_ Startup cleanup normalizes heartbeat task model values across all tenants as platform maintenance; only housekeeping/config fields are mutated.
- **server/heartbeat.ts:329** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — modifying a tenant-scoped table row without constraining tenant_id.
  - _Why false positive:_ Same startup maintenance loop updates model field only, with no tenant-facing data exposure or cross-tenant mixing.
- **server/heartbeat.ts:2817** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — direct UPDATE on heartbeat_tasks without tenant_id in WHERE clause after creating a tenant-bound task.
  - _Why false positive:_ Row was just created in same call path via createHeartbeatTask with tenantId, and update targets that returned newTask.id only.
- **server/heartbeat.ts:3269** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — updating heartbeat_tasks without constraining by tenant_id (in delegateTaskFromChat path).
  - _Why false positive:_ Same call path creates the heartbeat_tasks row with tenantId immediately before setting approval_status on the returned newTask.id.
- **server/job-queue.ts:99** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT against agent_jobs with no tenant_id constraint — it can transition/return jobs across all tenants.
  - _Why false positive:_ Queue worker intentionally claims due jobs across all tenants; each claimed row carries its own tenant_id for per-row processing.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases (step 1) UPDATEs agent_jobs to failed_terminal for expired leases with no tenant_id constraint — affects rows for all tenants.
  - _Why false positive:_ Lease reclamation is platform-wide queue housekeeping mutating status/error timestamps only across tenants, with no tenant data returned to tenants.
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases (step 2) UPDATEs agent_jobs to requeue expired rows with no tenant_id constraint — mutates jobs across tenants.
  - _Why false positive:_ Global lease-reclaim housekeeping intentionally scans all tenants and only mutates per-row job status fields.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob UPDATE on agent_jobs does not include tenant_id restriction — a caller can complete a job id belonging to any tenant.
  - _Why false positive:_ Server worker completion updates a server-generated job PK with fencing; no untrusted tenant boundary shown.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob SELECTs max_attempts FROM agent_jobs WHERE id = ${id} without tenant constraint — reading another tenant's job state is unscoped.
  - _Why false positive:_ Worker failure path reads job state by server-generated PK for lease-fenced processing, not a tenant request lookup.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob UPDATE to mark job failed_terminal does not restrict by tenant_id — may mark jobs from other tenants terminal.
  - _Why false positive:_ Worker failure terminal update is by server-generated job id plus attempts fencing in internal queue code.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob UPDATE that requeues a job (sets status='pending') lacks a tenant_id constraint — can requeue jobs across tenants.
  - _Why false positive:_ Worker requeue update is by server-generated job id plus attempts fencing in internal queue code.
- **server/knowledge-diversity-monitor.ts:266** _(system-sweep)_ — runKnowledgeDiversityMonitor queries agent_knowledge with GROUP BY tenant_id, persona_id and no tenant filter — this scans embeddings across all tenants (cross-tenant read).
  - _Why false positive:_ Platform-wide monitor intentionally scans all tenants, processes each tenant/persona pair separately, and writes per-tenant snapshots/counts.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` is performed using only the run id (WHERE clause = eq(procedureEvolutionRuns.id, runId)) with no tenant_id constraint. This allows a runId from another tenant to be updated.
  - _Why false positive:_ runId is created server-side by prior INSERT in same function, then updated by that unique PK only.
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` is performed using only the run id (WHERE clause = eq(procedureEvolutionRuns.id, runId)) with no tenant_id constraint. (noop_proposal path)
  - _Why false positive:_ noop path updates the same server-created procedure_evolution_runs row by internal runId from this call flow.
- **server/lib/aevo-meta-editor.ts:499** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` is performed using only the run id (WHERE clause = eq(procedureEvolutionRuns.id, runId)) with no tenant_id constraint. (finalization path)
  - _Why false positive:_ finalization updates the server-inserted run row by its generated PK within the same internal function.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Query reads the tenant-scoped `projects` table without constraining by tenant_id: SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && {ideabrowser,...}::text[] GROUP BY tenant_id. This performs a cross-tenant scan and can leak or act on other tenants' data.
  - _Why false positive:_ boot/backfill maintenance logic intentionally scans projects across tenants only to compute per-tenant counts for housekeeping and does not return mixed tenant content to tenants
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without constraining by tenant_id (the CTE that picks due rows does not filter by tenant). This lets the system-runner select and lock rows across all tenants in a single statement, violating the invariant that every access must be scoped by tenant via WHERE tenant_id = <caller tenant>.
  - _Why false positive:_ global scheduler/heartbeat runner intentionally picks due rows across tenants and then processes each returned row with its own tenant_id
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` does not restrict by tenant_id (query scans all tenants for running rows). Every SELECT against tenant-scoped tables must include a tenant_id constraint to enforce tenant isolation at the application level.
  - _Why false positive:_ read-only maintenance audit scans all tenants for stuck rows and returns bounded housekeeping findings carrying each row's own tenant_id
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT from tenant-scoped table `mind_tickets` performed without filtering by tenant_id (may scan rows for all tenants). This violates the requirement that every read against tenant-scoped tables include the caller's tenant filter or run only under an explicit admin context.
  - _Why false positive:_ read-only maintenance audit scans all tenants for stuck tickets and reports per-row tenant_id without mixing tenant content into a tenant-facing surface
- **server/memory-graph.ts:239** _(fk-transitive)_ — SELECT existence check on `memory_links` omits any tenant constraint: SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) ... LIMIT 1. This reads a tenant-scoped table without scoping to the caller's tenant.
  - _Why false positive:_ memory_links has no tenant_id column and links tenant-filtered memory_entries by foreign keys, so isolation is transitive through the parent memories
- **server/memory-graph.ts:528** _(upstream-verified)_ — UPDATE to tenant-scoped `memory_entries` omits tenant scoping: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}. Even though the row id came from a tenant-scoped SELECT, the UPDATE itself lacks an explicit tenant_id constraint.
  - _Why false positive:_ the row id being updated came from the immediately preceding SELECT on memory_entries constrained by tenant_id = tenantId in the same call path
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without including tenant_id, violating the invariant that every INSERT into tenant-scoped tables must pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via project_id; ownership is checked with assertProjectInTenant before insert
- **server/mpeg-engine.ts:1436** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The parallel-produce path inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is isolated through parent project_id; assertProjectInTenant verifies the project belongs to the tenant before insert
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — Global SELECT from tenant-scoped table `oauth_subscriptions` with no tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants.
  - _Why false positive:_ startup validation is a platform-wide maintenance sweep over active subscriptions, handled per row using each row's tenant_id and not returned to tenant-facing output
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes without scoping by tenant_id. This exposes rows across tenants.
  - _Why false positive:_ startup auto-reconnect intentionally enumerates tenants with google-workspace subscriptions and processes each tenant separately during maintenance
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update lacks tenant_id constraint. Updating by id alone may modify a row belonging to a different tenant if ids are re-used or caller is mistaken.
  - _Why false positive:_ update occurs inside startup maintenance loop over rows already selected with their tenant_id; mutation is housekeeping only (is_active flag) and not tenant-facing data exposure
- **server/oauth-subscriptions.ts:966** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update lacks tenant_id constraint (same pattern occurs multiple places in this startup validation flow).
  - _Why false positive:_ same startup maintenance loop; deactivation is housekeeping on the current row, using row-carried tenant context and no mixed tenant output
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} — update executed without tenant_id constraint, enabling potential cross-tenant deactivation.
  - _Why false positive:_ same global subscription validation sweep; status-only update within per-row processing, not a tenant-facing cross-tenant read/write surface
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh performs a global SELECT FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter, iterating over all tenants' subscriptions.
  - _Why false positive:_ proactive token refresh is a cron-style global maintenance loop across active subscriptions, processed per tenant using each row's tenant_id
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions does SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without scoping to a caller tenant — reads all tenants' rows.
  - _Why false positive:_ forceRefreshAllSubscriptions is explicitly a daily cron maintenance routine refreshing each active subscription per row by tenant_id
- **server/pace-control.ts:85** _(global-table)_ — checkPace queries heartbeat_logs without any tenant_id filter (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). If heartbeat_logs is tenant-scoped, this leaks/aggregates data across tenants and can incorrectly enforce caps across tenants.
  - _Why false positive:_ heartbeat_logs is schema-noted as having no tenant_id and is used here for intentional platform-global pace gating aggregates
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries heartbeat_logs over a time window with no tenant_id constraint (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...). This returns cross-tenant aggregates from a likely tenant-scoped table.
  - _Why false positive:_ heartbeat_logs is a global non-tenant-scoped table; snapshot returns platform-wide debugging/operational aggregates by design
- **server/persona-export.ts:39** _(global-table)_ — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ORDER BY ... — this query lacks any tenant_id filter, so tenant-specific skills from other tenants can be returned.
  - _Why false positive:_ skills table has no tenant_id column; query mixes persona-specific and global rows from a non-tenant-scoped table by design
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog performs `UPDATE plans ... WHERE id = ${planId}` with no tenant_id constraint. This writes execution_log on a plan row by id only, enabling cross-tenant log corruption.
  - _Why false positive:_ server-invoked executor updates plans by server-generated planId, not directly from a tenant request boundary
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — UPDATE setting plan to 'failed' uses `WHERE id = ${planId}` (no tenant_id filter). Unscoped writes can modify other tenants' plans.
  - _Why false positive:_ status write occurs inside executePlan after claiming the row and obtaining row.tenant_id; keyed by internal plan PK
- **server/plan-executor.ts:728** _(internal-worker-pk)_ — Final plan status update `UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}` is missing a tenant_id constraint, allowing status changes across tenants.
  - _Why false positive:_ final update is in internal executor flow for the claimed plan row, addressed by server-side unique plan id
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-path UPDATE (`UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing'`) lacks tenant scoping. This can flip other tenants' plan states during exception handling.
  - _Why false positive:_ exception-path state transition in internal executor uses the same server-side plan PK
- **server/plan-executor.ts:778** _(system-sweep)_ — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' without any tenant constraint: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...`. This mutates rows across all tenants.
  - _Why false positive:_ boot-time recovery intentionally sweeps stale plan statuses across tenants and only mutates housekeeping status fields
- **server/plan-executor.ts:787** _(system-sweep)_ — SELECT to enumerate pending plans (`SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`) has no tenant filter. It returns plan ids across tenants and later calls executePlan(id) which uses unscoped updates.
  - _Why false positive:_ boot-time recovery intentionally enumerates approved plans across tenants to re-dispatch internal execution; no tenant data is returned or mixed into output
- **server/process-governor.ts:121** _(fk-transitive)_ — Subquery reads heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads a tenant-scoped table without a tenant filter.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is counted via tenant-scoped heartbeat_tasks rows (ht.tenant_id = tenantId), so isolation is transitive through task_id
- **server/process-governor.ts:198** _(global-table)_ — Query over heartbeat_logs (SELECT AVG(duration_ms) FROM heartbeat_logs WHERE created_at > ...) lacks tenant_id filter and will aggregate across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is used here for platform-wide response-time aggregation
- **server/process-governor.ts:240** _(global-table)_ — Query counting distinct persona_id in heartbeat_logs (cascading_failures) lacks tenant_id constraint. This reads tenant-scoped heartbeat_logs across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and query computes a fleet-wide cascading-failure metric rather than tenant-scoped row access
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selects DISTINCT projects JOIN conversations and uses a subselect over messages without any tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0). This iterates across all tenants.
  - _Why false positive:_ Backfill is a platform-wide maintenance loop and processes each project's own conversations/messages to write per-project brain files, not returning or mixing multi-tenant output to a tenant-facing surface.
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} — conversations are queried without tenant scoping and may return rows across tenants.
  - _Why false positive:_ Query runs inside the same backfill maintenance sweep and scopes by the current project row being processed; output is used only for per-project housekeeping.
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} — messages read without tenant scoping. This can expose messages across tenants if conversation ids are not verified to belong to the caller's tenant.
  - _Why false positive:_ Message read is part of the same offline backfill sweep for one conversation at a time and is not exposed to tenants or combined into a shared response.
- **server/research-engine.ts:496** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author, created_at) is executed without an explicit tenant_id column. If project_notes is a tenant-scoped table with a non-null tenant_id column, this insert omits tenant_id and breaks the invariant (or may allow cross-tenant ambiguity).
  - _Why false positive:_ Schema says project_notes has no tenant_id column; insert is correctly isolated transitively via tenant-validated project_id, with assertProjectInTenant just above.
- **server/routes.ts:1543** _(global-table)_ — SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) are executed without any tenant_id filter in /api/public/stats.
  - _Why false positive:_ Endpoint is gated by requirePlatformAdmin and returns only platform-wide aggregate counts, an intentional admin/global stats surface.
- **server/routes.ts:1573** _(global-table)_ — Multiple db.execute SELECTs in /api/public/architecture query tenant-scoped tables (conversations, messages, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, research_sessions) without any tenant_id constraint.
  - _Why false positive:_ Architecture endpoint is platform-admin-only and returns platform-wide counts/listing intended as global admin telemetry rather than tenant-scoped data.
- **server/routes.ts:2102** _(signed-url)_ — GET /api/presenter/:token/slide/:index looks up presenter_sessions by token without tenant scoping before querying presenter_slide_images. The session lookup is unscoped/public and can reveal existence of sessions across tenants.
  - _Why false positive:_ Presenter token lookup is capability-style access by bearer token; the token itself authorizes access and tenant_id is not the control boundary for this public asset/session route.
- **server/routes.ts:4467** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` without setting tenant_id. The code executes: INSERT INTO project_notes (project_id, note, author) VALUES (...) — tenant_id is not included, violating the invariant that every INSERT into a tenant-scoped table must explicitly set tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id column and the project_id is derived from a conversation already constrained by tenant_id immediately above.
- **server/routes/admin-zombie-detector.ts:38** _(system-sweep)_ — Query reads agent_trace_spans (a tenant-scoped activity/tracing table) with no tenant_id constraint. The SELECT in the transaction aggregates tool usage across agent_trace_spans but does not filter by the caller's tenant.
  - _Why false positive:_ Admin-only zombie-detector intentionally computes platform-wide usage aggregates for maintenance/governance and returns only aggregate metadata, not tenant content.
- **server/routes/admin-zombie-detector.ts:54** _(system-sweep)_ — Query builds the `used` CTE from conversations without scoping by tenant_id. The personas-last-used calculation joins against a subquery that reads conversations but lacks a tenant_id condition, allowing cross-tenant data to be read.
  - _Why false positive:_ Admin-only maintenance view aggregates last-used persona stats across all conversations and exposes only counts/timestamps, not tenant conversation data.
- **server/routes/admin.ts:734** _(system-sweep)_ — SELECT from ai_insights executes without any tenant_id filter. If ai_insights is tenant-scoped, this admin endpoint will read insights across all tenants without constraining to the caller's tenant.
  - _Why false positive:_ Admin-only silent-failures endpoint intentionally scans platform-wide ai_insights for operationally stuck rows and returns maintenance metadata rather than tenant-facing mixed content.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE to tenant-scoped table `conversations` lacks tenant filter — `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates by id only with no `tenant_id = <caller>` constraint.
  - _Why false positive:_ conv was just created with this tenantId and project ownership was verified immediately above, so update by fresh server-generated conv.id is already tenant-bounded in this call path
- **server/routes/conversations.ts:100** _(fk-transitive)_ — SELECT from tenant-scoped table `project_conversations` has no tenant constraint — `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` can return cross-tenant rows.
  - _Why false positive:_ project_conversations has no tenant_id and is a join child isolated through tenant-scoped project and newly created conversation ids already verified in-path
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE to tenant-scoped table `projects` lacks tenant filter — `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` modifies a project row by id only without verifying caller tenant.
  - _Why false positive:_ project id was selected with AND tenant_id = tenantId a few lines above before this timestamp-only update
- **server/routes/goal-ledger.ts:92** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_jobs` is missing a tenant_id predicate (reads jobs across all tenants).
  - _Why false positive:_ route is protected by deps.requirePlatformAdmin, making this cross-tenant admin read explicitly gated in the same call path
- **server/routes/metrics.ts:63** _(system-sweep)_ — Query on agent_cost_ledger aggregates per-tenant costs (SELECT tenant_id, SUM(...) FROM agent_cost_ledger ...) without any tenant_id predicate — reads tenant-scoped table across all tenants.
  - _Why false positive:_ token-protected metrics endpoint emits platform-wide aggregate telemetry rather than tenant-facing row data; intended cross-tenant aggregation
- **server/routes/metrics.ts:79** _(system-sweep)_ — Query on agent_cost_ledger for tool usage counts (GROUP BY tool_name) runs without tenant predicate and touches tenant-scoped ledger rows across all tenants.
  - _Why false positive:_ platform-wide metrics aggregation over ledger rows returns only per-tool counts, not tenant data to tenants
- **server/routes/metrics.ts:95** _(system-sweep)_ — Query on agent_cost_ledger to compute token totals over last 24h runs without a tenant predicate (reads across tenants).
  - _Why false positive:_ platform-wide housekeeping/observability aggregate of total tokens only; no tenant-scoped data returned to tenant-facing surface
- **server/sculptor.ts:106** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause (UPDATE ... WHERE id = ${session.id}). This allows modifying a session row from another tenant if ids collide or are forged.
  - _Why false positive:_ session row was inserted in this function with params.tenantId and returned server-side, so update by that fresh session.id is already scoped by prior creation
- **server/sculptor.ts:112** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause when setting conversation_id (UPDATE ... WHERE id = ${session.id}).
  - _Why false positive:_ same-session update uses the just-inserted server-returned session.id from this tenant-scoped create path
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant_id in WHERE clause when settling a reservation (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}). This can update ledger rows across tenants.
  - _Why false positive:_ Internal helper settles a server-created reservation by global ledger row PK; reservationId is not shown coming from request input.
- **server/second-opinion.ts:424** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_cost_ledger` omits tenant_id in WHERE clause when releasing a reservation (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}).
  - _Why false positive:_ Internal helper releases a server-created reservation by global ledger row PK; reservationId is not shown crossing an untrusted boundary.
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM tenant-scoped table `heartbeat_tasks` lacks tenant_id filter (SELECT ... FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'). This will aggregate across tenants and subsequent actions act across tenants.
  - _Why false positive:_ Boot/seed housekeeping aggregate count across heartbeat_tasks used only to decide a global cleanup action; no tenant data returned or mixed.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true omits tenant_id and will affect rows across all tenants.
  - _Why false positive:_ Global maintenance cleanup intentionally disables runaway delegation heartbeat rows as housekeeping, not tenant-facing data access.
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id; the subsequent disabling UPDATE likewise lacks tenant scoping.
  - _Why false positive:_ Seed-time duplicate-task scan is a platform housekeeping sweep using only aggregate counts and cleanup of task metadata.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) omits tenant_id; the paired UPDATE that disables matching rows also lacks tenant scoping.
  - _Why false positive:_ Seed-time scan/disable of orphaned repo-examination heartbeat tasks is cross-tenant housekeeping, not tenant content mixing or exposure.
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed with no tenant filter. This reads tenant-scoped rows across all tenants (no WHERE tenant_id = ...)
  - _Why false positive:_ Dev snapshot import checks whether research_sessions is empty across the whole system before one-time bootstrap; aggregate count only.
- **server/seed.ts:2158** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id constraint. This reads tenant-scoped data for all tenants.
  - _Why false positive:_ Boot-time maintenance iterates all active research programs to fix invalid model names; cross-tenant housekeeping on metadata only.
- **server/seed.ts:2515** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id) queries conversations without scoping by tenant_id. conversations is a tenant-scoped resource but the query lacks tenant constraints, risking cross-tenant data exposure/processing.
  - _Why false positive:_ Boot-time backfill repairs missing project_conversations links across tenants; each row is handled independently and project_conversations is transitively scoped via tenant-scoped conversation/project.
- **server/seed.ts:3882** _(system-sweep)_ — DELETE on tenant-scoped table `heartbeat_tasks` executed without any tenant_id constraint (DELETE FROM heartbeat_tasks WHERE enabled = false AND ...). This can delete rows across all tenants.
  - _Why false positive:_ Global purge of stale auto-disabled heartbeat task rows is maintenance housekeeping and does not expose or mix tenant content.
- **server/seed.ts:3959** _(system-sweep)_ — Cross-tenant DELETE on tenant-scoped table `file_storage` (DELETE FROM file_storage a USING file_storage b ...). The statement operates across all tenants even though it respects tenant equality between rows — it is not limited to a caller tenant.
  - _Why false positive:_ Boot-time seed/index maintenance dedupe intentionally operates across tenants, only deletes duplicate durability rows and returns aggregate count, no tenant data exposure/mixing.
- **server/seed.ts:4108** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). If project_notes is tenant-scoped (tenant_id NOT NULL), this inserts rows with no tenant context or will fail/leave orphaned rows.
  - _Why false positive:_ Schema note says project_notes has no tenant_id; insert is correctly scoped transitively through tenant-scoped projects via project_id.
- **server/seed.ts:4174** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` performed without constraining to tenant_id (UPDATE agent_knowledge SET content = ... WHERE id = ...). While the id was found via a tenant-scoped SELECT earlier, the UPDATE itself lacks a tenant filter and therefore does not enforce tenant isolation in the write.
  - _Why false positive:_ The id being updated was selected immediately above with tenant_id = 1 and matching category/title in the same call path.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from messages reads conversation messages without constraining by tenant_id. The query uses only conversationId and does not include tenant scoping.
  - _Why false positive:_ distillIntent first calls analyzeConversation(conversationId, tenantId), which validates the conversation belongs to tenantId before this message fetch by that conversationId.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from messages in analyzeConversation() fetches messages by conversationId but lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ Same function verifies conversations.id + conversations.tenantId before loading messages for that validated conversationId.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT recentMsgs reads messages by conversationId without a tenant_id filter in the WHERE clause.
  - _Why false positive:_ createSkillFromConversation first calls analyzeConversation(conversationId, tenantId), which enforces conversation ownership earlier in the same path.
- **server/storage.ts:303** _(upstream-verified)_ — SELECT from messages (fetching citations) does not constrain by tenant_id. The raw query 'SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL' can return citations for message IDs without ensuring they belong to the caller's tenant.
  - _Why false positive:_ attachCitationMetadata only looks up ids from msgs already fetched by tenant-scoped getMessages/getMessagesPaginated, so the id set is pre-validated in the same path.
- **server/storage.ts:876** _(system-sweep)_ — archiveExpiredMemories executes a global UPDATE on memory_entries (SET status='archived' WHERE expires_at <= NOW()) with NO tenant_id constraint, modifying rows for all tenants.
  - _Why false positive:_ Platform-wide expiry archive only mutates housekeeping status on all tenants' rows and returns aggregate count, not tenant data.
- **server/storage.ts:892** _(system-sweep)_ — archiveStaleMemories runs UPDATE memory_entries SET status='archived' based on created_at/last_accessed with NO tenant_id constraint — this mutates data for all tenants in bulk.
  - _Why false positive:_ Platform-wide stale-memory archive is a housekeeping sweep updating status only across tenants, with no tenant-facing mixed data output.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT against tenant-scoped table custom_tools filters only by name (WHERE name = parsed.name) and omits tenant_id. Reads from tenant-scoped tables must be tenant-scoped to avoid cross-tenant info leaks even when names are namespaced.
  - _Why false positive:_ Name is rewritten with tenantToolName(parsed.name, tenantId) immediately above, so lookup is scoped by tenant-encoded unique name in the same call path.
- **server/tools.ts:2890** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without setting tenant_id on a tenant-scoped table, violating the invariant that INSERTs must include tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_conversations has no tenant_id column and is transitively scoped via validated tenant-owned project/conversation parents.
- **server/tools.ts:2993** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The INSERT into project_files does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (...) RETURNING *).
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; ownership is checked on projects before insert.
- **server/tools.ts:3008** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The code inserts project_notes (project_id, note, author) without setting tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects; ownership is checked on projects before insert.
- **server/tools.ts:3031** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) omits tenant_id. In the link_conversation handler the code INSERTs (project_id, conversation_id) without recording tenant_id even though tenant ownership checks were performed separately.
  - _Why false positive:_ project_conversations has no tenant_id column and link is only inserted after project tenant check and assertConversationInTenant validation.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' omits tenant scoping — `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` has no WHERE tenant_id = <tenant> clause.
  - _Why false positive:_ workflow is fetched just above with tenant_id = tenantId, and update uses that retrieved row id in the same call path.
- **server/tools/domains/files/handlers.ts:906** _(upstream-verified)_ — UPDATE on projects table is not tenant-scoped — `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` lacks a tenant_id constraint.
  - _Why false positive:_ projId comes from a prior join proving conversation.project_id belongs to projects.tenant_id = tenantId in the same path before the project touch update.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE executed against tenant-scoped table (knowledge_triples) without constraining tenant_id in the WHERE clause. The code updates rows `WHERE id = ${row.id}` but does not include `AND tenant_id = ${tenantId}`, so a malicious caller who knows/guesses an id could cause cross-tenant mutation.
  - _Why false positive:_ row.id values come only from the immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId, so the UPDATE targets tenant-owned rows only.
- **server/video-job-runner.ts:272** _(system-sweep)_ — listStaleVideoJobs selects from tenant-scoped table `videoJobs` without any tenant_id filter — the query can return rows belonging to other tenants.
  - _Why false positive:_ stale-job recovery is a platform-wide maintenance sweep over jobs; later claim/update uses each row's own tenantId and the routine does housekeeping/resume work rather than returning mixed tenant data.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Unscoped raw query against memory tables: `SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`. The MEMORY_TABLES (messages, agent_knowledge, memory_entries, etc.) are tenant-scoped but are being counted across all tenants with no tenant_id constraint.
  - _Why false positive:_ this is a global observability/invariant check counting rows and relation sizes for maintenance thresholds; it emits aggregate metrics only, not tenant data.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Unscoped SELECT against messages table: `SELECT count(*)::int AS n FROM messages WHERE created_at > NOW() - INTERVAL '14 days'`. messages is tenant-scoped but this query counts across all tenants with no tenant_id filter — privacy-sensitive and a cross-tenant data access risk.
  - _Why false positive:_ this is a global maintenance/observability count used to judge wiring health; it reads only an aggregate count across tenants and does not expose row data or mix content.
- **server/chat-engine.ts:634** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` (WHERE conversation_id = ${c.id}) has no tenant_id constraint. This can allow cross-tenant reads if conversation_id is attacker-controlled or if conversation ownership assumptions are ever bypassed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:632 — The compaction_archives SELECT is scoped by conversation_id = c.id, where c comes from `convs` fetched at line 593-598 with an explicit `c.tenant_id = ${tenantId}` filter (and the parent project is validated `WHERE id = projectId AND tenant_id = tenantId` at line 543). So c.id is a PK of an already-tenant-validated conversation row — no cross-tenant read possible.
- **server/chat-engine.ts:652** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` (WHERE conversation_id = ${c.id}) is executed without an explicit tenant_id constraint. The code relies on prior assumptions about conversation ownership, but the SQL itself lacks a tenant filter, violating the rule that every tenant-scoped access must include tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:650 — Same as line 632: the messages SELECT uses conversation_id = c.id where c originates from the tenant-filtered convs query (line 596: `c.tenant_id = ${tenantId}`) under a project already validated to belong to tenantId. c.id is a tenant-validated PK, so message reads cannot cross tenants.
- **server/dfy-intake.ts:93** _(signed-url)_ — submitDfyIntake updates dfy_intake_forms (.update(...).where(eq(dfyIntakeForms.id, form.id))) without scoping by tenant_id. This allows updating any form row by id across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): form row fetched by unguessable 48-hex token (the documented authorization for the public intake form); UPDATE reuses that row's own id.
- **server/doc-collections.ts:166** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} without also constraining tenant_id. This DELETE can remove chunks belonging to other tenants if collection_id references are inconsistent.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/embeddings.ts:748** _(internal-worker-pk)_ — UPDATE on tenant-scoped table (memory_entries or agent_knowledge via sql.raw(table)) is executed without constraining by tenant_id (UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}). Missing tenant filter allows cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/embeddings.ts:684 — storeEmbeddingVec is only used by embedding/backfill workers (server/embeddings.ts:693-706; server/research-engine.ts:518,1135; server/dream-consolidation.ts:363,422; server/storage.ts:806,818), with IDs obtained from database-created rows rather than request input. The UPDATE targets a globally unique serial primary key, and this worker path does not expose an attac
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT FROM tenant-scoped table 'agent_runs' is performed with no tenant_id filter (reads all tenants' in-flight agent_runs at boot). This reconstructs per-conversation task state from rows across tenants, violating tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): rehydrateTaskStateOnBoot is a boot rehydration loop over running/pending agent_runs that rebuilds in-memory TaskState keyed by each row's own conversationId (server/felix-brain.ts:13-45); state is surfaced per-conversation, not cross-tenant.
- **server/minerva-planner.ts:386** _(upstream-verified)_ — Probe SELECT from plans (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}) may execute without tenant constraint when tenantId is omitted, leaking cross-tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Child-plan lookup runs inside decidePlan's revise-idempotency branch, gated by the same route ownership check at agentic-policy.ts:113; args.planId is the verified plan id.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} — UPDATE has no tenant_id constraint, so it can increment trigger_count for a rule belonging to another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/storage.ts:1340** _(upstream-verified)_ — getAllDataForExport performs many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, etc.) with NO tenant_id constraints. This single API can export all tenants' data if called from an unprivileged context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE custom_tools SET implementation = ... WHERE id = ${tool.id} is executed without a tenant_id constraint. This mutates a tenant-scoped table without verifying the caller's tenant, risking cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE FROM custom_tools WHERE id = ${tool.id} executes without tenant_id constraint in revertToolRow. Deleting tenant-scoped rows without tenant scoping can remove another tenant's tool.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE agentActivity in completeActivity() uses WHERE eq(agentActivity.id, activityId) with no tenant_id constraint — allows updating rows across tenants by id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE on tenant-scoped table capability_reviews lacks a tenant constraint: markReviewReused executes `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` without checking tenant_id. This allows a process to flip rows belonging to other tenants if review IDs collide or are leaked.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/chat-engine.ts:4223** _(global-table)_ — recordToolExecution(conversationId, toolName, ...) is called without passing tenantId. If recordToolExecution writes to a tenant-scoped table, the write lacks an explicit tenant constraint and may cross tenant boundaries.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolExecution (felix-brain.ts:298-320) only mutates the in-process taskStateStore Map; no DB write, no tenant table.
- **server/delivery-pipeline.ts:1286** _(global-table)_ — getDeliveryByStripePayment selects a delivery_logs row by stripePaymentId with no tenant_id constraint (WHERE eq(deliveryLogs.stripePaymentId, paymentId)). This can disclose another tenant's delivery record/links when called with a payment id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/job-queue.ts:274** _(internal-worker-pk)_ — cancelJob UPDATE on agent_jobs WHERE id = ${id} updates rows with no tenant constraint — can cancel any tenant's job by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:283** _(upstream-verified)_ — retryJob UPDATE resets rows across all tenants because it lacks tenant_id in the WHERE clause.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:283 — retryJob is only reachable via POST /api/admin/agent-jobs/:id/retry (server/routes/agent-jobs.ts:62-70), gated by requirePlatformAdmin. Same admin-only operator inbox as cancelJob — cross-tenant retry by id is deliberate platform-admin capability, not attacker-influenced.
- **server/lib/agent-trace.ts:129** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_trace_spans` (persistSpanClose) uses WHERE span_id = ${spanId} with no tenant_id constraint. Closing a span should never update rows for another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/bwb-job-progress.ts:74** _(internal-worker-pk)_ — scope(...) builds WHERE clauses for videoJobs that may omit tenant predicate when BWB_TENANT_ID threading is missing. Several updates/selects (setBwbPhase, updateBwbChapters, completeBwbJob, bumpBwbHeartbeat, failBwbJob) call .where(scope(...)) and can execute without eq(videoJobs.tenantId, ...) if env tenant is not set — resulting in writes/reads against a tenant-scoped table without tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): scope() always pins the unguessable server-generated jobId (vj_<base36>_<6-byte-hex>, newBwbJobId at bwb-job-progress.ts:44-46, validated by JOB_ID_RE at :28,50); jobId comes only from BWB_JOB_ID env/explicit args threaded through the spawn chain, never a request. Tenant predicate is defense-in-depth, and a random-jobId collision is not reachable from an untrusted boundary.
- **server/lib/system-probes.ts:163** _(upstream-verified)_ — Drizzle SELECT count(*) FROM messages (messagesTable) is executed without a tenant_id WHERE clause, returning a global message count across tenants. This is an unscoped access to a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Reclassified by main-agent review: only callsite (tools.ts check_system_status) is admin-gated (ADMIN_TENANT_ID) two lines above the call. server/lib/system-probes.ts:163 db.select(count(*)).from(messagesTable) has no tenant filter; messages has tenant_id (shared/schema.ts:142-150). checkSystemStatus (119) is exposed as the check_system_status tool (server/tools.ts:3501, definitions.ts:24-31, 
- **server/memory-graph.ts:183** _(upstream-verified)_ — UPDATE to tenant-scoped table `memory_categories` is executed without constraining by tenant_id (and the subquery counting memory_entries also omits tenant filtering). This violates the tenant-isolation invariant — writes/reads against tenant-scoped tables must be scoped to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE to `memory_categories` (memory_count increment) is performed without a tenant_id WHERE clause: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}. This writes a tenant-scoped table without scoping by tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): catId is produced by ensureCategory (server/memory-graph.ts:79) which SELECTs/INSERTs scoped to tenantId (:51 'AND tenant_id = ${tenantId}', :57-58 INSERT with tenant_id); the id is server-derived within the same tenant call path, not request-supplied.
- **server/process-governor.ts:155** _(global-table)_ — Subquery counts pending heartbeat_tasks by persona_id without tenant scoping ((SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) as pending_tasks). This can aggregate cross-tenant data.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): heartbeat-task pace counting is a platform-wide throttle by design (same family as checkPace/heartbeat_logs); per-tenant fairness would need a schema migration and is tracked separately.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} — projects read without tenant scoping. Project rows are likely tenant-scoped and must be filtered.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/sculptor.ts:167** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause when writing completion/status (UPDATE ... WHERE id = ${sessionId}).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE to tenant-scoped table `sculptor_sessions` omits tenant_id in WHERE clause when writing review_result (UPDATE ... WHERE id = ${sessionId}).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/self-improvement.ts:170** _(upstream-verified)_ — detectStagnation builds and executes a SELECT against the tenant-scoped experiments table without requiring or enforcing a tenant_id filter when tenantId is not provided, allowing cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — getExperimentHistory returns db.select().from(experiments) when no category/tenantId filter is provided, allowing an unscoped SELECT across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:404** _(internal-worker-pk)_ — UPDATE messages SET citations = ... WHERE id = ${messageId} has no tenant_id constraint. This UPDATE on a tenant-scoped table can modify another tenant's message if messageId is attacker-controlled.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:841** _(internal-worker-pk)_ — updateMemoryEmbedding performs UPDATE memory_entries SET embedding WHERE id = ${id} without a tenant_id filter. This mutates a tenant-scoped row by id only, with no tenant ownership guard.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:853** _(internal-worker-pk)_ — updateKnowledgeEmbedding performs UPDATE agent_knowledge SET embedding WHERE id = ${id} without scoping by tenant_id. This can update another tenant's knowledge row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/tools.ts:8911** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `proposed_skills` executed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This allows a caller to update a row belonging to another tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8955** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `proposed_skills` executed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This promotes a proposed skill to accepted without verifying tenant ownership.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8971** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `proposed_skills` executed with WHERE eq(proposed_skills.id, row.id) only — no tenant_id constraint. This marks proposals rejected without enforcing tenant isolation.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` performed without constraining by tenant_id (no `WHERE tenant_id = ...`). This loads pairing rows across all tenants using a caller-derived title (`${channel}:${phone}`), violating the invariant that tenant-scoped reads must be filtered at the app level.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/delivery-pipeline.ts:318** _(deferred)_ — updateDeliveryLog performs an UPDATE against delivery_logs without scoping by tenant_id (where clause is only eq(deliveryLogs.id, id)).
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/minerva-planner.ts:367** _(deferred)_ — CAS UPDATE on plans may omit tenant scoping. The WHERE clause conditionally appends AND tenant_id = ${args.tenantId} only when a tenantId is supplied; if tenantId is omitted the UPDATE (which flips plan status) runs without tenant constraint, allowing cross-tenant modification.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): decidePlan CAS UPDATE only constrains tenant_id when args.tenantId != null — optional-tenant fail-open; hardening = make tenantId mandatory on the decision path.
- **server/minerva-planner.ts:404** _(deferred)_ — SELECT child plan probe uses a conditional tenant filter (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}). If tenantId is omitted this read can return rows from other tenants.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): child-plan SELECT applies the tenant filter conditionally (args.tenantId != null) — optional-tenant fail-open; hardening = require tenantId.
- **server/minerva-planner.ts:472** _(deferred)_ — Rollback UPDATE on plans (UPDATE plans SET status = 'awaiting_approval', ... WHERE id = ${args.planId} AND status = 'revising') lacks a tenant_id constraint. This write can affect plans in other tenants.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): decidePlan CAS UPDATE only constrains tenant_id when args.tenantId != null — optional-tenant fail-open; hardening = make tenantId mandatory on the decision path.
- **server/lib/scheduled-post-runner.ts:319** _(deferred)_ — Construction of a Postgres array literal via string interpolation from request-derived data (`arrLit` built from params.platforms) then injected into a SQL template. Even though the platform values were filtered earlier, building a SQL fragment by hand risks injection and bypasses parameterization.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): hand-built text[] literal from allowlist-checked platforms — injection-resistant today; refactor to parameterized bind (deferred item 5, campaign 2026-07-31).
- **server/minerva-planner.ts:549** _(deferred)_ — getPlan provides a code path that SELECTs a plan by id without any tenant filter when tenantId is omitted (SELECT * FROM plans WHERE id = ${planId}). This allows cross-tenant reads if callers omit tenantId.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
