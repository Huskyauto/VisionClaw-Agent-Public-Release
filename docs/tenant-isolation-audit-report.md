# Tenant-Isolation Audit Report

_Generated 2026-08-13T22:15:31.528Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **591/591** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **247** (CRITICAL 64 · HIGH 143 · MEDIUM 39 · LOW 1)
- Precision second-pass (`gpt-5.4`): **195** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (64)

- **server/agent-activity.ts:122** — UPDATE against tenant-scoped table `agent_activity` does not include tenant_id in the WHERE clause. This can allow updating rows across tenants by id.
  - _Fix:_ Include tenant-scoped guard in the update, e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))) or otherwise verify the activity belongs to the calling tenant before updating.
- **server/agent-eval.ts:218** — UPDATE agent_evals SET ... WHERE id = ${evalId} — the UPDATE omits tenant scoping, allowing cross-tenant modification if evalId is guessed.
  - _Fix:_ Add tenant guard to the WHERE clause, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}, or resolve/validate the tenant for evalId before updating.
- **server/agentic-engines.ts:243** — UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) — this UPDATE is not tenant-scoped and will apply across all tenants.
  - _Fix:_ Scope the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}), or run the auto-apply sweep per-tenant. Never perform cross-tenant writes without an explicit, auditable tenant constraint.
- **server/agentic-features.ts:797** — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} — the UPDATE omits tenant scoping and may modify enrollments across tenants by id.
  - _Fix:_ Include AND tenant_id = ${params.tenantId} in the WHERE clause (or validate row.tenant_id matches params.tenantId) to ensure updates only affect the caller's tenant.
- **server/agentic/approvals.ts:135** — expireStaleApprovals() performs a bulk UPDATE on agentApprovals to set status='expired' WHERE status='pending' AND expiresAt < now() without tenant scope — it will expire approvals across all tenants.
  - _Fix:_ Restrict the expiry UPDATE per-tenant (e.g. accept a tenantId parameter and add AND tenant_id = ${tenantId}), or perform the sweep per-tenant with explicit tenant scoping. Avoid globally mutating tenant-scoped rows.
- **server/agentic/runs.ts:34** — UPDATE on tenant-scoped table agent_runs (appendStep) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint. This allows updating a run row by id across tenants.
  - _Fix:_ Add tenant scoping to the update (e.g. accept tenantId in appendStep and use `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`). If callers don't have tenantId, first SELECT the run with a tenant_id check and reject/abort if it doesn't match.
- **server/agentic/runs.ts:45** — UPDATE on tenant-scoped table agent_runs (updateRunState) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Fix:_ Require/accept tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Validate callers pass the correct tenantId or load+verify the run's tenant before updating.
- **server/agentic/runs.ts:54** — UPDATE on tenant-scoped table agent_runs (replaceRunState) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Fix:_ Same fix: include tenant scoping in the WHERE clause or validate the run's tenant before replacing state. Prefer API change to accept tenantId and enforce `AND agent_runs.tenant_id = <tenantId>`.
- **server/agentic/runs.ts:58** — UPDATE on tenant-scoped table agent_runs (completeRun) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Fix:_ Include tenantId in the WHERE clause (e.g. `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`) or fetch+verify run tenant before marking complete to prevent cross-tenant modification.
- **server/agentic/runs.ts:88** — UPDATE on tenant-scoped table agent_runs (failRun) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Fix:_ Require tenant scoping on failure updates: accept tenantId and add it to the WHERE clause, or validate run ownership prior to updating to ensure only the owning tenant's rows are modified.
- **server/agentic/runs.ts:97** — UPDATE on tenant-scoped table agent_runs (pauseRun) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Fix:_ Add tenant validation or require tenantId in the API and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all callers pass tenant-scoped context.
- **server/auto-memorize.ts:62** — setWatermark reads/updates/inserts agent_knowledge rows without scoping to the calling tenant and INSERT hardcodes tenant_id = 1. This can overwrite or create watermark rows for the wrong tenant.
  - _Fix:_ Require and accept a tenantId parameter for setWatermark. Constrain the SELECT/UPDATE to WHERE tenant_id = ${tenantId} and use tenant_id = ${tenantId} on INSERT (do not hardcode 1). Also add an explicit ownership check before updating by id (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}).
- **server/channel-routing.ts:16** — channel_routes table is created with no tenant_id column. This makes it impossible to enforce per-tenant scoping at the application SQL level and allows cross-tenant linking/reads via persona_id.
  - _Fix:_ Add a tenant_id INTEGER NOT NULL column and a per-tenant uniqueness constraint (e.g. UNIQUE(tenant_id, channel)). Migrate existing rows to set tenant_id correctly, then update all application code to pass tenantId when inserting/reading. Example: ALTER TABLE channel_routes ADD COLUMN tenant_id INTEGER NOT NULL DEFAULT <safe-value>; after migration remove any default and add UNIQUE(tenant_id, channel). Ensure all queries include WHERE tenant_id = ${tenantId}.
- **server/channel-routing.ts:51** — setChannelRoute DELETE uses DELETE FROM channel_routes WHERE channel = ${channel} with no tenant constraint, deleting routes globally across tenants.
  - _Fix:_ Change setChannelRoute to accept tenantId and include it in the DELETE WHERE clause (DELETE ... WHERE channel = ${channel} AND tenant_id = ${tenantId}). Ensure any unique/index conflict handling uses the tenant scope (see table migration suggestion).
- **server/channel-routing.ts:54** — setChannelRoute INSERT/ON CONFLICT inserts into channel_routes without setting tenant_id, and the handler accepts personaId from the caller without validating that the referenced persona belongs to the caller's tenant. This can create cross-tenant links and allow a caller to point a route at another tenant's persona.
  - _Fix:_ Require tenantId as an explicit argument. Before inserting, validate ownership: SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}, and fail if not found. INSERT must set tenant_id (INSERT INTO channel_routes (tenant_id, channel, persona_id) VALUES (${tenantId}, ${channel}, ${personaId})). Use an ON CONFLICT on (tenant_id, channel) to update the correct per-tenant row.
- **server/data-protection.ts:168** — permanentlyPurgeSoftDeleted deletes from conversations with DELETE FROM conversations WHERE id = ${conv.id} without constraining by tenant_id. This violates the tenant-isolation invariant and could delete a conversation row if IDs are re-used or if id lookup is tampered with.
  - _Fix:_ Include the tenant predicate on the DELETE: DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id} (or AND tenant_id = ${tenantId} if available). Always include tenant_id on DELETE/UPDATE statements against tenant-scoped tables.
- **server/delivery-pipeline.ts:203** — updateDeliveryLog() performs an UPDATE on delivery_logs using only the delivery id (WHERE eq(deliveryLogs.id, id)) with NO tenant_id constraint. This helper is called from many places (e.g. to change status, add email flags), so it can update/overwrite another tenant's delivery row by id.
  - _Fix:_ Require/accept the caller's tenantId in updateDeliveryLog and include it in the WHERE clause (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or otherwise verify ownership (SELECT tenant_id first inside the same transaction) before applying updates.
- **server/doc-collections.ts:165** — addDocument() issues a DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete chunks belonging to other tenants' collections that share doc_path/collection_id values (or if collection_id is guessable).
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}) — ensure every destructive operation against doc_chunks includes the tenant_id filter.
- **server/embeddings.ts:748** — UPDATE against tenant-scoped table (memory_entries|agent_knowledge) performed without constraining tenant_id. storeEmbeddingVec updates embedding_vec/embedding by id but does not check tenant ownership.
  - _Fix:_ Ensure every UPDATE includes a tenant constraint. Change the API to accept an explicit tenantId (or look it up and validate ownership) and add AND tenant_id = ${tenantId} to the WHERE clause (or SELECT+verify ownership before UPDATE). Avoid relying on id-only updates across tenants.
- **server/embeddings.ts:790** — UPDATE memory_entries in backfillEmbeddingVecs uses id-only WHERE clause with no tenant_id constraint — can modify other tenants' rows.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE or verify row ownership before updating. Prefer parameterized, tenant-bound updates (or run the backfill per-tenant) to prevent cross-tenant writes.
- **server/embeddings.ts:807** — UPDATE agent_knowledge in backfillEmbeddingVecs uses id-only WHERE clause with no tenant_id constraint — can modify other tenants' rows.
  - _Fix:_ Require tenant scoping for updates. Add AND tenant_id = ${tenantId} to the WHERE clause or verify ownership via a prior SELECT; perform backfill per-tenant only.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not include tenant_id column. The INSERT INTO project_files (...) VALUES (...) omits tenant_id even though project_files is tenant-scoped and INSERTs must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT and bind it from the caller (and keep the existing assertProjectInTenant check). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Ensure options.tenantId is validated/defined before inserting.
- **server/mpeg-engine.ts:1416** — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id. The upsert for project_files omits tenant_id in the VALUES list.
  - _Fix:_ Same fix as above: include tenant_id in the INSERT (and any ON CONFLICT DO UPDATE) for project_files, e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...). Ensure the parallel path validates tenant ownership before inserting.
- **server/plan-executor.ts:121** — UPDATE plans (appendExecutionLog) omits tenant_id constraint — writes to a tenant-scoped table using only id. This can modify another tenant's plan row.
  - _Fix:_ Include the plan's tenant in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or pass and assert the owning tenant when calling appendExecutionLog. In Drizzle use .where(eq(plans.tenantId, tenantId), eq(plans.id, planId)).
- **server/plan-executor.ts:513** — CAS UPDATE on plans to claim an approved plan lacks tenant_id filtering (UPDATE plans ... WHERE id = ${planId} AND status = 'approved'). This allows cross-tenant modifications/claims.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId} AND status = 'approved') or retrieve the tenant first and re-check ownership prior to updating. Prefer parameterized tenant checks with Drizzle's eq(table.tenantId, tenantId).
- **server/plan-executor.ts:527** — UPDATE plans SET status='failed' WHERE id = ${planId} has no tenant_id filter — mutates tenant-scoped table without ensuring caller ownership.
  - _Fix:_ Add tenant_id to the WHERE clause (WHERE id=${planId} AND tenant_id=${tenantId}) or otherwise validate the plan belongs to the expected tenant before updating.
- **server/plan-executor.ts:740** — Final UPDATE on plans (setting status and appending execution_log) uses WHERE id = ${planId} with no tenant_id constraint — can update other tenants' plans.
  - _Fix:_ Include tenant filter in the UPDATE (WHERE id = ${planId} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(plans.tenantId, tenantId), eq(plans.id, planId)).
- **server/plan-executor.ts:763** — Error handler UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' omits tenant_id — can fail/mark executing plans across tenants.
  - _Fix:_ Add tenant_id to the WHERE (AND tenant_id = ${tenantId}) or otherwise scope this rollback to the plan's owner tenant before updating.
- **server/plan-executor.ts:778** — resumeStuckPlans: UPDATE plans SET status='approved' ... WHERE status='executing' AND updated_at < ... RETURNING id has no tenant_id filter — it resets 'executing' plans for all tenants globally.
  - _Fix:_ Scope the recovery update by tenant when invoked, or iterate tenants and perform the update per-tenant. Do not run an unscoped UPDATE on a tenant-scoped table; use WHERE tenant_id = ${tenantId} (or per-tenant loop).
- **server/plan-executor.ts:788** — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' LIMIT 50 does not filter by tenant_id — enumerates approved plans across all tenants for executor kicks.
  - _Fix:_ Only select approved plans for a specific tenant, or iterate tenants and select per-tenant (WHERE status='approved' AND tenant_id = ${tenantId}). Use parameterized tenant filters rather than global selects.
- **server/process-governor.ts:595** — executeAction('disable_task') updates heartbeat_tasks with UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} but omits tenant_id — a write that can disable another tenant's task row by id.
  - _Fix:_ Include tenant_id in the UPDATE: WHERE id = ${t.id} AND tenant_id = ${tenantId}. Always constrain mutating statements on tenant-scoped tables to the caller's tenant.
- **server/process-governor.ts:641** — executeAction('restart_agent') updates agent_desks: UPDATE agent_desks SET ... WHERE id = ${desk.id} without tenant_id — mutates another tenant's agent desk if ids collide.
  - _Fix:_ Restrict the UPDATE to the tenant: WHERE id = ${desk.id} AND tenant_id = ${tenantId}. Validate ownership prior to mutating rows.
- **server/process-governor.ts:977** — logAction: UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant_id filtering — will increment trigger_count across tenants.
  - _Fix:_ Include tenant_id in the WHERE (WHERE id = ${rule.id} AND tenant_id = ${tenantId}) so only rules owned by that tenant are modified.
- **server/proposal-verifier.ts:108** — Race-after-rebase UPDATE: UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} omits tenant_id — mutates a tenant-scoped proposals row without tenant scoping.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure the tenant is passed through the rebase/verify flow and used for all mutating statements.
- **server/proposal-verifier.ts:246** — persist(): UPDATE code_proposals SET verification_status = ..., verified_at = NOW() WHERE id = ${proposalId} omits tenant_id — writes verification metadata without scoping to tenant.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or require tenant argument to persist. Use parameterized Drizzle predicates to avoid cross-tenant updates.
- **server/proposal-verifier.ts:372** — attemptRebaseOnDrift: UPDATE code_proposals SET status='rejected', verification_status='skipped', ... WHERE id = ${proposalId} lacks tenant_id — mutates proposal status across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure attemptRebaseOnDrift receives and uses tenant context for all DB writes.
- **server/proposal-verifier.ts:396** — attemptRebaseOnDrift: UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (on hallucinated new OLD_CODE) omits tenant_id — writes to tenant-scoped proposals table without tenant check.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Always pass tenantId to rebase flows and guard updates with it.
- **server/proposal-verifier.ts:408** — attemptRebaseOnDrift: UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} does not include tenant_id — persists rebased diff globally across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and ensure the rebase procedure is executed with tenant context so only the owning tenant's proposal is modified.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table agent_knowledge omits tenant_id in WHERE clause (UPDATE agent_knowledge ... WHERE id = ${r.id}). This allows an update by id without re-checking that the caller/runner is operating under the correct tenant context.
  - _Fix:_ Constrain the UPDATE by tenant_id as well: e.g. UPDATE agent_knowledge SET ... WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id equality matching the owning tenant (or use the Drizzle eq(table.tenant_id, tenantId) helper).
- **server/research-engine.ts:165** — UPDATE against tenant-scoped table research_sessions performed without any tenant_id constraint (cleanupZombieSessions). This will modify rows across all tenants.
  - _Fix:_ Scope the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant and run the UPDATE per-tenant. Do NOT run an unscoped UPDATE on a tenant-scoped table. Parameterize tenant ids (no string interpolation) and fail-closed if tenant context is not available.
- **server/routes/projects.ts:184** — INSERT into project_notes does not include tenant_id (INSERT INTO project_notes (project_id, note, author) ...). Every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the inserted columns/values: INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) RETURNING *; or use a storage helper that enforces/sets tenant_id server-side.
- **server/routes/projects.ts:225** — INSERT into project_files (project_id, file_name, file_path, file_url, file_type, file_size) is missing tenant_id.
  - _Fix:_ Include tenant_id in the VALUES (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) or use a safe storage wrapper that adds tenant_id before persisting.
- **server/routes/projects.ts:293** — INSERT into project_files during multipart upload (VALUES (${projectId}, ..., ${fileUrl || downloadPath}, ...)) does not include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT columns/values (tenant_id = ${tenantId}), or use storage/upsert helpers that enforce tenant_id before inserting.
- **server/sculptor.ts:106** — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This can modify another tenant's session row if ids collide.
  - _Fix:_ Include the tenant guard in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify ownership before writing. Prefer using the local tenantId variable in scope rather than relying on a prior SELECT alone.
- **server/sculptor.ts:113** — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). This write is not scoped to the caller's tenant.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the WHERE clause (or pass the tenantId into this context) so the update only touches rows for the caller's tenant.
- **server/sculptor.ts:168** — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). monitorSession's end-of-run update can overwrite another tenant's row.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Use the tenantId parameter passed into monitorSession; do not rely on earlier reads to guarantee ownership.
- **server/sculptor.ts:363** — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). This write can update sessions across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or fetch-and-verify the row's tenant before applying the update. Every write must include tenant_id in the predicate.
- **server/second-opinion.ts:404** — UPDATE on tenant-scoped table agent_cost_ledger lacks a tenant_id constraint (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}). settleFusionReservation can modify ledger rows from other tenants if reservation ids are global.
  - _Fix:_ Scope the UPDATE by tenant. Either accept tenantId into settleFusionReservation and use WHERE id = ${reservationId} AND tenant_id = ${tenantId}, or first verify the reservation row belongs to the expected tenant before updating. Never update a tenant-scoped ledger row by id alone.
- **server/second-opinion.ts:423** — UPDATE on tenant-scoped table agent_cost_ledger lacks a tenant_id constraint (UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId}). releaseFusionReservation can touch another tenant's ledger row.
  - _Fix:_ Change releaseFusionReservation to accept tenantId (or look up and validate the row's tenant) and include AND tenant_id = ${tenantId} in the WHERE clause when updating. Enforce ownership checks before mutating ledger rows.
- **server/seed-cleanup.ts:15** — UPDATE on tenant-scoped table heartbeat_tasks lacks tenant_id constraint (UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true). This will disable tasks across all tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or another explicit tenant) or perform the change per-tenant after verifying intent. If the script is meant to run only for the admin tenant, include WHERE tenant_id = ${ADMIN_TENANT_ID}.
- **server/seed-cleanup.ts:51** — UPDATE on tenant-scoped table heartbeat_tasks lacks tenant_id constraint (UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true). This write affects all tenants matching the predicate.
  - _Fix:_ Include AND tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant variable) in the WHERE clause, or perform tenant-scoped updates only after an explicit per-tenant selection/verification step.
- **server/seed-cleanup.ts:75** — UPDATE on tenant-scoped table heartbeat_tasks lacks tenant_id constraint (UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true). This will disable matching tasks across all tenants.
  - _Fix:_ Add tenant_id filtering (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or run the operation per-tenant after explicit verification. All writes to tenant-scoped tables must include a tenant predicate.
- **server/seed.ts:2163** — UPDATE on tenant-scoped table 'research_programs' without a tenant_id constraint (UPDATE research_programs SET model = ... WHERE id = ...). Updating by id only may modify rows for other tenants if id is not scoped in the WHERE clause.
  - _Fix:_ Add tenant scoping to the UPDATE. Use both id and tenant_id in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${p.tenant_id || tenantId}). Prefer selecting tenant_id in the prior SELECT so you can enforce it on the UPDATE.
- **server/seed.ts:2447** — UPDATE on tenant-scoped table 'projects' without tenant scoping (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ...). This writes to a tenant-scoped table with no tenant_id constraint and can affect other tenants' data.
  - _Fix:_ Include tenant_id in the WHERE clause for any UPDATE to tenant-scoped tables (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Ensure the tenantId is obtained from a trusted source and validated before performing the update.
- **server/seed.ts:2516** — SELECT from (likely) tenant-scoped table 'conversations' without tenant_id filtering (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). The results are then used to INSERT links, which risks creating cross-tenant associations or leaking data across tenants.
  - _Fix:_ Only query conversations for the caller's tenant (e.g. WHERE c.tenant_id = ${tenantId} ...). Additionally, when linking project→conversation, verify ownership: ensure both project_id and conversation_id belong to the same tenant, and include tenant_id in any subsequent INSERT/UPDATE that should be tenant-scoped.
- **server/seed.ts:3853** — DELETE FROM heartbeat_tasks executed without scoping by tenant_id. heartbeat_tasks is tenant-scoped (has tenant_id index) but the DELETE has no WHERE tenant_id = ... filter, so it will delete matching rows across all tenants.
  - _Fix:_ Restrict the DELETE to the intended tenant context. Add a tenant_id predicate (e.g. WHERE tenant_id = <tenantId> AND enabled = false AND description LIKE ... ) or loop per-tenant and run the cleanup inside that tenant-scoped context. If this is intended only for system/admin tenant, explicitly include WHERE tenant_id = 1.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) in the WHERE clause (lines ~394-401 and the error-path update at ~413-415). Without also constraining by tenant_id this can modify another tenant's row by id.
  - _Fix:_ Always include tenant scoping on UPDATEs/DELETEs against tenant-scoped tables. E.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Apply same fix to the error-path update so both updates restrict by tenant_id.
- **server/self-reflection.ts:366** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = <existing id> without also constraining tenant_id. This allows updating another tenant's memory entry by id.
  - _Fix:_ Restrict updates by tenant as well as id. Change the UPDATE to include AND tenant_id = ${tenantId} in the WHERE (or use a parameterized drizzle expression like .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId))). If tenant ownership must be validated first, fetch and verify tenant_id before updating.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks is executed without a tenant_id predicate, potentially removing disabled delegation tasks for all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (WHERE tenant_id = ${tenantId} AND ...), or require an explicit admin/all-tenants sentinel and audit that path. Never perform blind multi-tenant deletes without an explicit, reviewed admin intent.
- **server/storage.ts:1304** — getAllDataForExport performs SELECTs from many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant_provider_keys, heartbeat_tasks, etc.) without any tenant_id constraints — this exposes or exports all tenants' data in one call.
  - _Fix:_ Lock this API to admin-only contexts and require an explicit tenantId or an ALL_TENANTS sentinel that is only usable by a verified admin. Prefer splitting exports to per-tenant exports where callers must pass a tenantId. Add strong authorization checks and logging for any platform-wide export operation.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table 'custom_tools' (updating implementation) constrained only by id and missing tenant_id in WHERE — allows modifying another tenant's row if ids collide or are guessed.
  - _Fix:_ Always include tenant ownership in UPDATE predicates. e.g. await db.update(customTools).set({ implementation: fixedImpl }).where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Validate tenantId is the same tenant that created/owns the tool before mutating.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table 'custom_tools' without a tenant_id constraint (WHERE eq(custom_tools.id, tool.id) only). This can delete another tenant's tool row if the id is misused.
  - _Fix:_ Delete must be scoped by tenant: await db.delete(customTools).where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the function receives/validates tenantId (or verify the row's tenant first) before deleting.
- **server/tools/domains/security/handlers.ts:163** — Unscoped SELECT against tenant-scoped table `provider_keys` — query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` has no tenant_id filter. This returns provider metadata (and counts) across all tenants and can leak sensitive provider/key presence information.
  - _Fix:_ Constrain the query to the caller's tenant. Add an explicit tenant filter (or Drizzle eq) e.g. `... FROM provider_keys WHERE tenant_id = ${tenantId} AND api_key IS NOT NULL ...` or use the Drizzle table/where helper: .where(eq(providerKeys.tenantId, tenantId)). If cross-tenant visibility is legitimately required for admin users, implement an explicit admin-only code path with audited authorization checks (do not rely on implicit admin-ness), and log/justify any cross-tenant queries.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` executed without constraining by tenant_id (no WHERE tenant_id = ...). The code loads pairing rows across all tenants by title and relies on later application checks to validate ownership — this violates the invariant that every read against a tenant-scoped table must be tenant-filtered at SQL level.
  - _Fix:_ Do not select agent_knowledge rows across tenants. Constrain the SQL to the caller/claimed tenant (e.g. add WHERE tenant_id = ${tenantId}) or perform the conversation ownership verification in SQL (JOIN agent_knowledge → conversations and include AND conversations.tenant_id = ${tenantId}). Alternatively, persist an explicit tenant_id column on agent_knowledge and use eq(agent_knowledge.tenantId, tenantId) (or Drizzle .where(eq(...), ...)) so the DB query never returns rows from other tenants. If the intent is to detect contested pairings, implement a controlled query that returns only rows whose linked conversation's tenant matches the candidate tenant and fail-closed on ambiguity, still enforcing tenant_id in the WHERE/JOIN clause.

### HIGH (143)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without constraining by tenant_id (reads experiments across all tenants).
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. If this routine must scan all tenants, run it with an explicit, audited privilege and avoid using unscoped reads on tenant tables; otherwise accept a tenantId parameter and filter by it.
- **server/agent-channels.ts:198** — JOIN reads from tenant-scoped table `channel_subscriptions` without constraining its tenant_id (the JOIN ON clause lacks cs.tenant_id = ${tenantId}).
  - _Fix:_ Constrain channel_subscriptions by tenant in the JOIN or in WHERE (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${params.tenantId}) to ensure the subscription rows are scoped to the caller's tenant.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting() reads rows WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER without tenant_id constraint (scans all tenants).
  - _Fix:_ Limit the sweep by tenant (accept tenantId param and WHERE tenant_id = ${tenantId}) or otherwise perform per-tenant operations with explicit tenant scoping to avoid touching other tenants' rows.
- **server/agentic-engines.ts:344** — SELECT COUNT from `heartbeat_logs` (heartbeat/tool calls) is executed without a tenant_id filter (FROM heartbeat_logs WHERE task_name != 'Self-Reflection'), reading cross-tenant telemetry.
  - _Fix:_ Add tenant scoping to the heartbeat_logs query (e.g. WHERE tenant_id = ${tenantId} AND task_name != ...) or otherwise ensure the function is explicitly intended and authorized to read global telemetry.
- **server/agentic-engines.ts:451** — SELECT recent heartbeat_logs (status='success') in runPredictiveEngine() is executed without tenant scoping (reads across tenants).
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status = 'success') so only logs for the caller tenant are read.
- **server/agentic-engines.ts:519** — SELECT heartbeat performance (FROM heartbeat_logs GROUP BY task_name, status) executes without tenant_id constraint and will aggregate logs across tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} (or run per-tenant) so heartbeat performance metrics are restricted to the caller's tenant.
- **server/agentic-engines.ts:534** — SELECT from `heartbeat_tasks` (taskConfig) is executed without tenant scoping (WHERE enabled = true but no tenant_id), reading task config across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or run per-tenant) to avoid reading or acting on other tenants' scheduled tasks/configuration.
- **server/agentic-features.ts:314** — Inner subquery in prevSnaps (SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY url, created_at DESC) omits tenant_id. This reads competitor_snapshots without tenant scoping inside a subquery.
  - _Fix:_ Add tenant scoping to the inner subquery (e.g. WHERE competitor_id = ${comp.id} AND tenant_id = ${params.tenantId}) so all reads of competitor_snapshots are tenant-restricted.
- **server/agentic-features.ts:638** — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' — missing tenant filter, so enrollment existence is checked across tenants.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the WHERE clause to ensure the duplicate-enrollment check is restricted to the caller's tenant.
- **server/agentic/department-budgets.ts:143** — Global SELECT on tenant-scoped table without tenant filter: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This reads across all tenants and violates the invariant that every read against a tenant-scoped table must include a tenant_id constraint.
  - _Fix:_ Restrict the query to an explicit tenant (WHERE tenant_id = <tenant>) or require an explicit admin-mode/owner-privileged path that documents and enforces cross-tenant access. If this function must sweep all tenants, make the cross-tenant intent explicit (e.g. name/authorize as an admin-only job), audit it, and ensure callers cannot invoke it with request-derived privileges. Prefer looping over an explicit list of tenant IDs or adding a parameter to only operate for a single tenant to preserve the application-level tenant-isolation invariant.
- **server/agentic/resume-worker.ts:66** — Raw SELECT across tenant-scoped tables (agent_runs / agent_approvals) with no tenant_id filter. The query selects rows from agent_runs and agent_approvals across all tenants: `SELECT ... FROM agent_runs r JOIN agent_approvals a ... WHERE r.state ? 'pendingApprovalId' ...`
  - _Fix:_ Restrict the scan to a specific tenant (WHERE r.tenant_id = ${tenantId}) or otherwise iterate per-tenant. If this worker must process multiple tenants, explicitly pass and validate a tenantId per invocation and include `AND r.tenant_id = <tenantId>` (and/or `a.tenant_id = <tenantId>`) in the WHERE clause. Ensure any results are only acted on under the same-per-tenant guard.
- **server/agentic/wake-scheduler.ts:142** — UPDATE/SELECT claims due wake rows across all tenants without constraining by tenant_id. The inner SELECT used to choose ids for the UPDATE does not include a tenant_id = <tenant> filter, allowing cross-tenant rows to be locked/processed by this job.
  - _Fix:_ Restrict selection to a single tenant per run or include tenant_id in the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND status = 'pending' ... ) or iterate tenants and run the claim per-tenant. Ensure every SELECT/UPDATE on agent_wake_schedules includes an explicit tenant_id equality filter when operating on tenant-scoped data.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... is executed without any tenant_id restriction. This enumerates conversation/project rows across tenants and then processes them, enabling cross-tenant reads and subsequent operations.
  - _Fix:_ Either run the backfill per-tenant (iterate known tenant ids and add WHERE tenant_id = ${tenantId}) or include a tenant_id constraint in the query. Any background sweep that touches tenant-scoped tables must scope each operation to a specific tenant to prevent cross-tenant access.
- **server/auto-asset-capture.ts:207** — Selecting messages by conversation_id (SELECT content FROM messages WHERE conversation_id = ${c.id} ...) without scoping by tenant_id. Messages is tenant-scoped (or implicitly so via conversation), but the query lacks an explicit tenant filter; an attacker or bug could cause cross-tenant reads/writes.
  - _Fix:_ Either join messages to conversations and include AND conversations.tenant_id = ${tenantId} in the WHERE, or add an explicit tenant_id filter (WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}). Always ensure queries against tenant-scoped tables include an explicit tenant equality filter or are otherwise robustly validated before use.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge (getWatermark) has no tenant_id constraint — reading watermark rows across tenants.
  - _Fix:_ Scope the query to a specific tenant. Pass a tenantId into getWatermark and add WHERE tenant_id = ${tenantId} (or equivalent Drizzle filter). If intent is a per-tenant watermark, do not read a global table without explicit tenant scoping; if intent is global admin data, document and centralize that behaviour clearly.
- **server/auto-memorize.ts:163** — SELECT from messages (pull recent messages window) has no tenant_id WHERE clause — it reads messages across all tenants into a single result set.
  - _Fix:_ Either query per-tenant (pass tenantId and add WHERE m.tenant_id = ${tenantId}) or explicitly perform a controlled multi-tenant iteration that enforces tenant isolation at the SQL level (e.g. fetch list of tenant ids then run per-tenant queries). Avoid reading tenant-scoped tables globally without explicit tenant scoping/authorization.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. Tenant-scoped tables must have tenant_id set on INSERT to enforce isolation; here invoice_items is inserted with only invoice_id and other columns, leaving tenant attribution ambiguous or relying on FK behavior which is not guaranteed by the invariant.
  - _Fix:_ Include tenant_id explicitly in the invoice_items INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or otherwise ensure the row is written with the caller's tenant id. If invoice_items is intentionally global, document that and ensure it's safe; otherwise add tenant_id NOT NULL and pass ${tid} when inserting.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` performed without a tenant_id constraint. The query `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` can modify another tenant's row if `pending.reviewId` collides or is manipulated.
  - _Fix:_ Require and check tenant ownership before mutating tenant-scoped rows. Pass the tenantId into markReviewReused (or store tenantId in the pendingReuse entry) and include `AND tenant_id = ${tenantId}` (or equivalent Drizzle eq(condition)) in the WHERE clause. Alternatively, SELECT the row first ensuring tenant_id matches the caller, then UPDATE by id. Ensure every write to tenant-scoped tables always constrains by tenant_id.
- **server/channel-routing.ts:31** — listChannelRoutes selects from channel_routes and joins personas (p) without any tenant filter. If personas (or channel_routes) are tenant-scoped, this leaks/returns other tenants' personas and routes.
  - _Fix:_ Require and accept a tenantId parameter in listChannelRoutes and add a WHERE clause constraining both channel_routes and the joined personas to that tenant (e.g. WHERE cr.enabled = true AND cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}). If persona ownership must be enforced, validate persona. Ensure the new tenant_id column is populated on INSERTs.
- **server/channel-routing.ts:64** — getPersonaForChannel SELECTs persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true with no tenant constraint, returning persona ids from other tenants if the table is not tenant-scoped.
  - _Fix:_ Add a tenantId parameter and constrain the query: SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId}. Also ensure inserted rows include tenant_id and that persona ownership is validated before linking.
- **server/channel-routing.ts:71** — removeChannelRoute deletes by channel only (DELETE FROM channel_routes WHERE channel = ${channel}) with no tenant constraint, risking deletion of other tenants' routes.
  - _Fix:_ Require tenantId and include it in the WHERE clause (DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}). Ensure all mutation paths are updated to include tenant scope and that the table schema includes tenant_id.
- **server/chat-engine.ts:4412** — Call to storage.attachCitations(...) is made without supplying tenant context. If attachCitations performs INSERTs/updates on a tenant-scoped table (e.g. message_citations), those writes will lack an explicit tenant_id constraint and could violate tenant isolation (INSERT without tenant_id or UPDATE/SELECT without WHERE tenant_id = ...)
  - _Fix:_ Change attachCitations invocation (and the attachCitations implementation) to include and enforce the tenant id. For example: storage.attachCitations(messageId, collectedCitations, tenantIdForScope). Inside attachCitations, ensure all INSERT/UPDATE/SELECT statements explicitly filter/insert tenant_id (e.g. .where(eq(table.tenant_id, tenantId)) and include tenant_id in INSERT payload) and validate that the referenced messageId belongs to the same tenant before mutating any rows.
- **server/commitments.ts:135** — scanAndEscalate() issues a SELECT from the tenant-scoped commitments table without any WHERE tenant_id = ... filter (returns rows across all tenants).
  - _Fix:_ Restrict the query to a specific tenant or otherwise enforce explicit admin-only authorization. Options: (A) If this should operate per-tenant, add a tenant_id parameter and change the SELECT to include WHERE tenant_id = ${tenantId}; (B) if this is an intentional admin-wide scan, gate the function behind a strong admin-only check and avoid returning tenant-identifying or free-text fields (or select only minimal redacted columns), and log that the cross-tenant access is deliberate. In general prefer iterating tenants and running tenant-scoped queries rather than unfiltered multi-tenant SELECTs to eliminate accidental cross-tenant data exposure.
- **server/crews-engine.ts:401** — addCrewTask inserts agent_id from caller input without verifying that the referenced agent row belongs to the caller's tenant. A malicious caller could link a task to an agent from another tenant (cross-tenant reference).
  - _Fix:_ Before using params.agentId, verify ownership: SELECT id, tenant_id FROM crew_agents WHERE id = <agentId> AND tenant_id = <tenantId> (or provide a getAgent helper). If the check fails, reject or set agent_id=NULL. Ensure all foreign ids coming from untrusted input are validated against tenant_id.
- **server/data-protection.ts:404** — backupTenantDataToDrive executes SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} without scoping by tenant_id. messages is tenant-scoped elsewhere — this read could surface another tenant's rows if conversation_id collisions or FK anomalies exist.
  - _Fix:_ Scope message queries to the tenant: SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId} (or ${conv.tenant_id}). Audit all message queries to ensure tenant_id is included when the table is tenant-scoped.
- **server/delivery-pipeline.ts:1071** — getDeliveryByStripePayment() SELECTs from delivery_logs by stripePaymentId with NO tenant_id filter. This can return another tenant's delivery row for a shared payment id.
  - _Fix:_ Scope the lookup by tenant (add a tenantId parameter) and include eq(deliveryLogs.tenantId, tenantId) in the WHERE clause. If this API is used by multi-tenant webhooks, the webhook handler must resolve and pass the calling tenant before calling this function.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken() SELECTs a dfy_intake_forms row by token with NO tenant_id constraint. Token-only lookup against a tenant-scoped table can return forms belonging to any tenant.
  - _Fix:_ Either add a tenantId argument and filter by tenant_id, or clearly document that the token is the sole authority and ensure the token space is globally unique and treated as an authentication bearer for that form. Prefer adding an explicit tenant filter whenever the caller has tenant context.
- **server/dfy-intake.ts:93** — submitDfyIntake() updates dfy_intake_forms (.update(...).where(eq(dfyIntakeForms.id, form.id))) without scoping the UPDATE to tenant_id. An attacker could reuse an id to update another tenant's form if ids collide or are leaked.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId)))) or otherwise verify the form's tenant before updating.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings() updates doc_chunks setting embedding using WHERE id = ${row.id} with NO tenant_id constraint. The prior SELECT filtered by collection_id and tenant_id, but the subsequent UPDATE uses only id and could modify rows of other tenants if an id is leaked/misused.
  - _Fix:_ Include tenant guard in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or update by collection_id+doc_path+chunk_index in the same transaction where possible.
- **server/email.ts:470** — startInboxPolling()/storeEmail path checks for an existing inbox_messages row with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without scoping by tenant_id. That may cause cross-tenant dedupe (skipping inserts for other tenants) or mixing data across tenants.
  - _Fix:_ Include tenant scoping in the existence check (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1). Ensure the tenant is resolved before doing the select or perform a tenant-aware dedupe.
- **server/email.ts:515** — backfillEmptyBodies() SELECTs inbox_messages rows (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id) with NO tenant_id filter, then fetches provider content and updates rows by id. This reads/updates rows across all tenants without tenant scoping.
  - _Fix:_ Limit the SELECT to a known tenant (add tenantId argument) or run the backfill per-tenant and include tenant_id in both the SELECT and subsequent UPDATEs (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Ensure background jobs that operate over tenant-scoped tables either run per-tenant or explicitly require/verify admin privileges and mark that in code.
- **server/embeddings.ts:752** — SELECT FROM tenant-scoped table agent_knowledge (backfillMissingKnowledgeEmbeddings) with no tenant_id filter — reads unscoped rows across all tenants.
  - _Fix:_ Scope the backfill to a single tenant at a time. Add a tenantId parameter to backfillMissingKnowledgeEmbeddings and include WHERE tenant_id = ${tenantId} (or iterate per-tenant), so background jobs never read other tenants' data.
- **server/embeddings.ts:780** — SELECT FROM tenant-scoped table memory_entries (backfillEmbeddingVecs) with no tenant_id filter — reads unscoped rows across all tenants.
  - _Fix:_ Limit this backfill to a specific tenant (accept tenantId) or run per-tenant: add WHERE tenant_id = ${tenantId} to the SELECT to avoid cross-tenant data exposure.
- **server/embeddings.ts:796** — SELECT FROM tenant-scoped table agent_knowledge (backfillEmbeddingVecs) with no tenant_id filter — reads unscoped rows across all tenants.
  - _Fix:_ Run the backfill for a single tenant or add a tenantId parameter and include WHERE tenant_id = ${tenantId} to confine reads to the caller's tenant.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' is executed with no tenant_id filter (reads rows across all tenants).
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId} or run per-tenant queries) or otherwise ensure explicit, auditable scoping before reading rows from tenant-scoped tables.
- **server/firecrawl.ts:214** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) with no tenant_id constraint.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so writes cannot cross tenants.
- **server/firecrawl.ts:344** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) with no tenant_id constraint inside the crawl loop.
  - _Fix:_ Add an explicit tenant_id condition to the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to ensure tenant-scoped writes are constrained to the caller's tenant.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' is performed by project id only (where(eq(projects.id, projectId))) without scoping to tenant_id before using the row.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the lookup cannot return another tenant's project.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl using only projects.id in the WHERE clause (where(eq(projects.id, projectId))) with no tenant_id constraint.
  - _Fix:_ Add the tenant filter to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) to prevent cross-tenant writes.
- **server/google-drive.ts:826** — SELECT from tenant-scoped table 'projects' during uploadAndShare is done by project id only (where(eq(projects.id, params.projectId))) without an explicit tenant_id WHERE clause prior to trusting the row.
  - _Fix:_ Include the caller's tenant in the WHERE clause (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or otherwise validate ownership via a parameterized tenant-scoped query before proceeding.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images uses session_id returned from an internal presenter API without first verifying that the session belongs to the current tenant (no tenant_id check). This lets a caller persist image blobs against an arbitrary session_id (potential cross-tenant write).
  - _Fix:_ Verify ownership of the referenced presenter session before writing. e.g. SELECT id, tenant_id FROM presenter_sessions WHERE id = ${sessionId} and tenant_id = ${tenantId} and reject if not matched. Better: include tenant_id as a NOT NULL column on presenter_slide_images and set it on INSERT (or enforce in the application logic), or perform inserts inside a transaction after explicit ownership verification. Consider adding FK/tenant scoping conventions or RLS to prevent future mistakes.
- **server/google-workspace.ts:2364** — Second INSERT into presenter_slide_images (thumbnail row) writes image data keyed by session_id without validating that the session_id is owned by the caller's tenant. Same cross-tenant write risk as above.
  - _Fix:_ As above, require verification that sessionId belongs to tenantId before persisting thumbnails. Either include tenant_id in the presenter_slide_images rows (and set it on INSERT), or query presenter_sessions with a tenant_id constraint and fail if mismatch. Use parameterized queries and transaction semantics to avoid TOCTOU where appropriate.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} performs a write to a tenant-scoped resource (presenter_sessions) without constraining by tenant_id or otherwise verifying the session belongs to the caller's tenant. This can allow updating another tenant's session.
  - _Fix:_ Constrain the UPDATE to the tenant: UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId} and check affected-row count (fail if 0). Alternatively perform a prior SELECT to validate ownership. Prefer including tenant_id in the UPDATE WHERE clause (or ensuring the session was created under this tenant) and reject/raise if ownership doesn't match.
- **server/heartbeat.ts:53** — SELECT from research_schedules has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants without an explicit tenant filter.
  - _Fix:_ If this query is intended to operate per-tenant, add an explicit tenant filter (e.g. WHERE tenant_id = ${someTenantId}) or iterate tenants and query per-tenant. If it truly must scan all tenants, document that intent and ensure downstream updates always re-assert the tenant_id when updating rows.
- **server/heartbeat.ts:102** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id constraint on the UPDATE. An attacker or bug could cause a row belonging to another tenant with the same id to be modified.
  - _Fix:_ Include tenant scoping in the UPDATE, for example: UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id} (or use a storage helper that enforces tenant_id).
- **server/heartbeat.ts:108** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${sched.tenant_id}) or use a storage-layer helper that requires and enforces tenant_id for tenant-scoped tables.
- **server/heartbeat.ts:121** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id constraint on the update after an error recovery flow.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) so recovery updates cannot affect rows belonging to other tenants.
- **server/heartbeat.ts:385** — SELECT * FROM event_log WHERE status = 'pending' ... (no tenant_id constraint). This pulls pending events for all tenants into a single tick and relies on later per-row tenant checks rather than scoping the initial query.
  - _Fix:_ Fetch pending events scoped per-tenant (e.g. iterate active tenant ids and query WHERE tenant_id = ${tid} AND status = 'pending') or include tenant_id in the WHERE to limit the result set; avoid broad multi-tenant SELECTs unless explicitly required and reviewed.
- **server/heartbeat.ts:273** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — direct UPDATE on a tenant-scoped table without tenant_id in WHERE.
  - _Fix:_ When updating heartbeat_tasks directly, include tenant scoping (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or prefer the storage.updateHeartbeatTask helper that enforces tenant context.
- **server/heartbeat.ts:277** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — modifying heartbeat_tasks by id only, no tenant constraint.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or use the storage layer to perform the update under an asserted tenant context.
- **server/heartbeat.ts:2764** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint when setting approval_status on a newly-created task.
  - _Fix:_ Include tenant scope in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update through a storage helper that enforces tenant ownership to avoid accidentally modifying another tenant's row.
- **server/heartbeat.ts:3216** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — direct DB UPDATE without tenant_id constraint in delegateTaskFromChat flow.
  - _Fix:_ Constrain the UPDATE to the task's tenant (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage API to set approval status under the correct tenant context to prevent cross-tenant modification.
- **server/internal-resolver.ts:186** — finalizeEvent is invoked without a tenant_id filter in the WHERE clause (unscoped UPDATE on tenant-scoped event_log). In resolveExperimentFailed the code path that handles an event with missing tenant_id calls finalizeEvent(eventId, ...) with no tenantId, causing an UPDATE on event_log WHERE id = ${eventId} but no tenant_id restriction.
  - _Fix:_ Do not perform unscoped mutations on tenant-scoped tables. Change the logic so finalizeEvent always receives a validated tenantId before issuing the UPDATE. If the event row has no tenant_id, fail-closed by logging and skipping finalization (do not call finalizeEvent), or make finalizeEvent require tenantId and refuse to run without it. Ensure every UPDATE/DELETE on event_log includes `AND tenant_id = <derived tenant>` so a malformed/mis-associated id cannot mutate another tenant's rows.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on procedure_evolution_runs uses only the run id in the WHERE clause (`WHERE id = runId`) and omits any tenant_id predicate. The table is tenant-scoped (rows carry tenantId), so these updates can affect another tenant's row if runId is not bound to the caller's tenant.
  - _Fix:_ Include the tenant predicate in the CAS/update WHERE. For example: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Persist the tenantId with the run and always use it for subsequent updates to avoid cross-tenant writes.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on procedure_evolution_runs uses only the run id in the WHERE clause (`WHERE id = runId`) and omits tenant scoping. This is used to mark a run done/noop and can update a run belonging to another tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))). Ensure runId is tied to the tenant when the run row is created and carried through for all later updates.
- **server/lib/aevo-meta-editor.ts:500** — UPDATE on procedure_evolution_runs uses only the run id in the WHERE clause (`WHERE id = runId`) when marking the run done, without verifying tenant_id. This allows cross-tenant state changes by run id alone.
  - _Fix:_ Restrict these updates by tenant as well: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Store and propagate tenantId alongside runId so every mutating query includes it.
- **server/lib/bwb-job-progress.ts:74** — scope(jobId, ...) builds a WHERE clause that may omit tenant scoping when BWB_TENANT_ID is missing, causing subsequent UPDATE/SELECTs that use scope(jobId) to execute without a tenant_id predicate on the tenant-scoped video_jobs table. Several callers (setBwbPhase, updateBwbChapters, bumpBwbHeartbeat, completeBwbJob, failBwbJob) rely on scope() and can therefore run unscoped when the tenant env var is not present.
  - _Fix:_ Fail closed or require tenantId rather than silently omitting the tenant predicate. Either make envTenantId throw/return error when missing, or change scope() to reject calls that lack a valid tenant and force callers to provide tenantId. At minimum, log and abort the write instead of performing it without tenant scoping; ensure every UPDATE/SELECT/DELETE on video_jobs includes eq(videoJobs.tenantId, tid).
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped table `projects` without a tenant_id constraint (grouped across all tenants). This performs a cross-tenant read (tenant_id, count(*) group) and can leak other tenants' data.
  - _Fix:_ Restrict the query to only the snapshot tenants or run per-tenant counts. For example, compute the set of snapshot tenant ids and change the query to: WHERE tenant_id = ANY(${Array.from(wantedByTenant.keys())}::int[]) AND tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id — or iterate wantedByTenant and issue a tenant-scoped COUNT per tenant. Ensure every SELECT against tenant-scoped tables includes an explicit tenant_id filter.
- **server/lib/inbox-ingest.ts:506** — UPDATE inbox_classifications executed without constraining by tenant_id (WHERE inbox_message_id = ${storeResult.id} only). This writes to a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Include the tenant_id constraint in the UPDATE (e.g. WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}) or otherwise validate ownership (join to inbox_messages and require m.tenant_id = ${tenantId}) so the row update cannot affect another tenant's data.
- **server/lib/mcp-api-keys.ts:166** — SELECT from mcp_api_keys looks up by key_prefix only (no tenant_id in WHERE). This reads a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ If possible, constrain the SELECT by tenant_id. If the design requires resolving tenant from the key, ensure the lookup is implemented with a safe, audited global index and minimal surface (e.g. a dedicated prefix→tenant mapping), and document/justify the cross-tenant lookup. Preferably store and query by (key_prefix, tenant_id) so all reads remain tenant-scoped.
- **server/lib/mention-injection.ts:121** — SELECT in fetchRadioMessages LEFT JOINs personas p ON p.id = cm.from_persona_id but does not constrain p.tenant_id. This reads the tenant-scoped personas table without a tenant filter and can surface another tenant's persona data.
  - _Fix:_ Constrain the joined personas table to the tenant (e.g. LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId}), or add an explicit WHERE/AND clause for p.tenant_id = ${params.tenantId} to ensure all reads against personas are tenant-scoped.
- **server/martech-bundle.ts:325** — UPDATE on tenantVoiceProfiles uses WHERE id = existing.id but does NOT include an explicit tenant_id constraint. Application-level tenant isolation requires writes to tenant-scoped tables to also constrain by tenant_id.
  - _Fix:_ Add an explicit tenant check to the UPDATE WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) or otherwise ensure the UPDATE is parameterized with the caller's tenantId before executing.
- **server/memory-graph.ts:124** — UPDATE memory_categories SET memory_count = ... WHERE id = ${catId} does not include a tenant_id constraint. This updates a tenant-scoped table using only the id column, lacking the required tenant_id filter.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${catId} AND tenant_id = ${tenantId} (or use a parameterized Drizzle eq on tenantId) to ensure the row belongs to the caller's tenant.
- **server/memory-graph.ts:183** — refreshCategoryCount: UPDATE memory_categories ... WHERE id = ${categoryId} lacks a tenant_id constraint. Tenant-scoped updates must filter by tenant_id.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}) or otherwise verify ownership before updating.
- **server/memory-graph.ts:239** — Existence check SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) ... LIMIT 1 lacks any tenant scoping. Checking/reading link rows without verifying tenant can allow cross-tenant information leakage or cross-tenant linking.
  - _Fix:_ Restrict existence checks to the caller's tenant by joining to memory_entries (or adding tenant_id to memory_links) and requiring the related memory entries belong to the tenant (e.g. JOIN memory_entries me ON me.id = ... AND me.tenant_id = ${tenantId}). Ensure every SELECT touching tenant-scoped data includes tenant_id filters.
- **server/memory-graph.ts:252** — INSERT INTO memory_links (...) VALUES (...) does not include tenant_id (and the INSERT lacks any tenant scoping). If memory_links is tenant-scoped or is used to relate tenant-scoped rows, inserting without tenant context can create cross-tenant links or orphaned relations.
  - _Fix:_ Either add tenant_id as a column on memory_links and set it on INSERT, or ensure insertion is performed only after verifying both memory entries belong to the caller's tenant (e.g. resolve ownership via memory_entries and insert with a tenant_id constraint). Make the tenant ownership explicit in the INSERT.
- **server/memory-graph.ts:494** — categorizeExistingMemories: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} omits a tenant_id constraint. Updating memory_entries by id only (without tenant filter) risks modifying rows from other tenants if IDs are abused or misbound.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or use an UPDATE that verifies the row's tenant ownership before changing it.
- **server/minds-engine.ts:214** — INSERT into tenant-scoped table `mind_tickets` builds the depends_on array by string-joining a request-derived value (`params.dependsOn`) into a SQL array literal (`{...}`) in the interpolated SQL. This is string-interpolated SQL using caller-provided data (injection + isolation risk).
  - _Fix:_ Do not build SQL array literals by joining user input into the query string. Instead pass the JS array as a bound parameter or use the Drizzle query builder/parameter binding for arrays so the DB driver encodes it safely. Also validate/verify every dependsOn id belongs to the same tenant before storing (e.g. query those tickets by id with tenant_id = tenantId and fail if any are missing). Example fixes: (a) use a parameterized array binding (WHERE/VALUES ... ${params.dependsOn} as an array) rather than `{${params.dependsOn.join(',')}}`, or (b) use Drizzle's insert API and supply depends_on: params.dependsOn (typed) after validating ownership.
- **server/minds-engine.ts:327** — UPDATE on tenant-scoped table `mind_events` uses an interpolated array literal of caller-supplied `eventIds`: `WHERE id = ANY({...}::int[]) AND tenant_id = ${tenantId}`. Constructing the array via string-join on request data is string-interpolated SQL and can lead to SQL-injection or other risks.
  - _Fix:_ Pass eventIds as a typed bound parameter (e.g. use the driver/Drizzle support for JS number[] → SQL int[]) instead of building '{1,2,3}' strings. Additionally validate all eventIds and/or verify they belong to the caller's tenant before performing the update (or use a parameterized WHERE id = ANY($1::int[]) with the eventIds array as the parameter).
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant filter. validateSubscriptionsOnStartup queries all active oauth_subscriptions (WHERE is_active = TRUE) across tenants.
  - _Fix:_ Scope the query to a single tenant when performing tenant-specific validation (add WHERE tenant_id = ${tenantId}), or if this startup routine intentionally needs to iterate all tenants, first query a trusted tenant list (or admin-only table) and then perform per-tenant queries that include WHERE tenant_id = ${tid} so each read is tenant-scoped.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id constraint — it reads rows across all tenants.
  - _Fix:_ If you need to enumerate tenants for a global auto-reconnect, first query a trusted tenants list (not oauth_subscriptions rows directly), then run tenant-scoped queries with WHERE tenant_id = ${tenantId}. Otherwise add an explicit tenant_id filter to this query.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh selects FROM oauth_subscriptions WHERE is_active = TRUE without restricting to a specific tenant, iterating over all tenant rows without per-tenant scoping.
  - _Fix:_ If intended to refresh tokens per-tenant, fetch a list of tenant IDs first and then for each tenant call refreshAccessToken with queries that include WHERE tenant_id = ${tenantId}. Avoid bulk SELECTs that return rows from multiple tenants without explicit tenant scoping.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions reads oauth_subscriptions WHERE is_active = TRUE without tenant constraint, iterating across all tenants in a single call.
  - _Fix:_ As with proactiveTokenRefresh, enumerate tenants explicitly (trusted tenant table or tenant ids) and run per-tenant refreshes where each SQL uses WHERE tenant_id = ${tenantId}. If a true global admin operation is required, ensure it is limited to admin-only contexts and audited.
- **server/pace-control.ts:85** — SELECT from heartbeat_logs (COUNT(*)) has no tenant filter. checkPace queries heartbeat_logs in a sliding window without WHERE tenant_id = <tenant>, so it counts runs across tenants.
  - _Fix:_ Make pace checks tenant-scoped: accept a tenantId argument (or otherwise obtain caller tenant) and add WHERE tenant_id = ${tenantId} to the heartbeat_logs queries. If the intent is a global cross-tenant metric, perform an explicit admin-only path and document it; otherwise always filter by tenant_id.
- **server/pace-control.ts:147** — getPaceSnapshot queries heartbeat_logs (per-persona aggregation) without tenant filter — it returns global data across tenants rather than per-tenant stats.
  - _Fix:_ Scope snapshot queries by tenant_id. Change queries to include WHERE tenant_id = ${tenantId} (and add tenantId as a parameter to getPaceSnapshot). If global stats are required, implement a dedicated admin-only endpoint and ensure it's intentionally privileged and audited.
- **server/process-governor.ts:155** — Subquery in desk_status computes pending_tasks with SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true — missing tenant_id filter, so task counts may include other tenants' tasks.
  - _Fix:_ Add tenant scoping in the subquery: SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true AND tenant_id = ${tenantId}. Use Drizzle .where(eq(heartbeat_tasks.tenantId, tenantId), ... ).
- **server/process-governor.ts:199** — response_time: SELECT AVG(duration_ms) FROM heartbeat_logs ... lacks tenant_id constraint — aggregates logs across tenants.
  - _Fix:_ Filter heartbeat_logs by tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at > ... ). Use parameterized tenant predicate to avoid cross-tenant leakage.
- **server/process-governor.ts:240** — cascading_failures: SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE ... has no tenant_id filter — reads other tenants' heartbeat logs.
  - _Fix:_ Scope the query by tenant_id (WHERE tenant_id = ${tenantId} AND ...). Use Drizzle eq on the table tenant column.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} omits tenant_id — can read project data belonging to other tenants.
  - _Fix:_ Require and pass tenantId to updateProjectBrain and include WHERE id = ${projectId} AND tenant_id = ${tenantId}, or validate project ownership before reading. Prefer Drizzle eq(table.tenantId, tenantId).
- **server/project-brain.ts:332** — backfillProjectBrains: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 has no tenant filtering — enumerates projects/conversations/messages across tenants.
  - _Fix:_ Scope the query by tenant_id (e.g. WHERE p.tenant_id = ${tenantId} and c.tenant_id = ${tenantId}) or iterate per-tenant. Never run cross-tenant selects on tenant-scoped tables.
- **server/project-brain.ts:349** — convRes: SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant_id — reads conversations across tenants.
  - _Fix:_ Add tenant filter (AND c.tenant_id = ${tenantId}) or change flow to require tenantId and select per-tenant. Use Drizzle eq(conversations.tenantId, tenantId).
- **server/project-brain.ts:359** — msgRes: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ORDER BY created_at ASC omits tenant_id — may retrieve messages from other tenants' conversations with the same id.
  - _Fix:_ Filter messages by tenant (AND tenant_id = ${tenantId}) or ensure conversation_id is validated to belong to the expected tenant before selecting.
- **server/proposal-verifier.ts:33** — SELECT FROM code_proposals WHERE id = ${proposalId} has no tenant_id constraint — may read proposals belonging to other tenants.
  - _Fix:_ Include tenant_id in the SELECT WHERE clause (WHERE id = ${proposalId} AND tenant_id = ${tenantId}), or require and validate caller tenant before loading proposals.
- **server/recurring-messages.ts:287** — Inserting a conversation using a persona_id taken from scheduled payload (INSERT INTO conversations (... persona_id) VALUES (... ${p.expandViaPersona} ...)) without verifying that the referenced persona belongs to the same tenant. The expandViaPersona value originates from user-supplied input and is later used to create rows tied to tenant_id = r.tenant_id, enabling cross-tenant referencing if the persona id is from another tenant.
  - _Fix:_ Validate ownership of any caller-supplied foreign id (e.g. persona_id) before using it to create or mutate tenant-scoped rows. Either (a) verify the persona row exists and its tenant_id === r.tenant_id before inserting, or (b) refuse/store only persona ids that you validated at create time. Do the check at createScheduledMessage time and/or right before creating the conversation in the runner, and fail closed if the persona does not belong to the tenant.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table memory_entries omits tenant_id in WHERE clause (UPDATE memory_entries SET ... WHERE id = ${storedId}). Although the id came from a prior tenant-scoped SELECT, the UPDATE is not constrained to tenant_id and could be abused if ids are guessed or reused.
  - _Fix:_ Include the tenant constraint in the UPDATE: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId} (and optionally persona_id = ${personaId}). Always include explicit tenant_id filters on UPDATE/DELETE statements against tenant-scoped tables.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge executed with no tenant_id constraint — deletes entries across all tenants (fire-and-forget).
  - _Fix:_ Add a tenant filter to the DELETE (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()). If the intent is a global admin sweep, require an explicit admin tenant/context and perform per-tenant deletes with explicit authorization.
- **server/research-engine.ts:496** — INSERT into project_notes omits tenant_id. If project_notes is tenant-scoped (tenant_id NOT NULL), this will create entries not attributed to the caller tenant or fail depending on schema.
  - _Fix:_ Include tenant_id in the INSERT column list and values (VALUES (${projectId}, ${tenantId}, ${note}, ...)) or otherwise derive tenant_id from a verified project ownership check (assertProjectInTenant) and pass that explicit tenant_id value into the INSERT. Never rely on implicit defaults.
- **server/research-engine.ts:504** — Second INSERT into project_notes inside findings loop similarly omits tenant_id, risking cross-tenant data writes or misattribution.
  - _Fix:_ Same fix as above: explicitly set tenant_id in the INSERT (VALUES (${projectId}, ${tenantId}, ...)) after verifying the project belongs to the session tenant. Ensure all INSERTs to tenant-scoped tables include tenant_id and are parameterized.
- **server/routes.ts:2143** — POST /api/presenter inserts into tenant-scoped table `presenter_sessions` using tenantId taken directly from request body and does not verify caller's ownership or that the tenantId is allowed. This lets a caller supply arbitrary tenant_id values when creating sessions.
  - _Fix:_ Do not accept tenantId directly from untrusted request body. Derive tenantId from authenticated context (e.g. getTenantFromRequest(req)) or validate that the caller (internal key) is authorized to act for the provided tenant. If this endpoint is intended for internal services, require a vetted internal auth and/or disallow arbitrary tenant IDs (or verify ownership/authorization before inserting). Add an explicit WHERE/ownership check prior to inserting tenant-scoped rows.
- **server/routes.ts:4338** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code executes: INSERT INTO project_notes (project_id, note, author) VALUES (...) without setting tenant_id, violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Supply tenant_id in the INSERT and bind it to the caller's tenant (and continue to verify ownership). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, use a storage helper that requires tenantId and/or ensure you call storage.createProjectNote({ tenantId, projectId: projId, note: snapshot.slice(0,5000), author: 'system:context-guard' }). Also keep the existing ownership check (ensuring projId belongs to tenant) as defense-in-depth.
- **server/routes/admin-zombie-detector.ts:38** — Query reads from agent_trace_spans (CTE 'used') without constraining by tenant_id. This can return cross-tenant trace data because there is no WHERE tenant_id = <caller tenant> filter in the CTE.
  - _Fix:_ Add an explicit tenant filter to the CTE (or otherwise scope the query) e.g. include AND tenant_id = ${tenantId} in the WHERE clause (and pass tenantId into the transaction). If the intent is a platform-wide admin view, make that explicit in code and ensure the route is strictly gated to platform-admin sessions (and document why cross-tenant reads are required).
- **server/routes/conversations.ts:96** — UPDATE on tenant-scoped table `conversations` has no tenant_id filter (WHERE id = ${conv.id} only). This write can affect rows outside the caller's tenant if IDs are guessed or mis-bound.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}. Prefer parameterized tenantId from getTenantFromRequest, and validate ownership before performing writes.
- **server/routes/conversations.ts:101** — INSERT into `project_conversations` does not set tenant ownership (no tenant_id column/value). Inserting a row that links project↔conversation without tenant scoping can create cross-tenant associations.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and/or enforce tenant check before insert). For example: INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}). Ensure the table schema includes tenant_id and that the value is the authenticated caller's tenant.
- **server/routes/conversations.ts:103** — UPDATE on `projects` (updated_at) lacks a tenant_id filter (WHERE id = ${projectId} only). This allows updating a project belonging to another tenant if projectId is controlled or misvalidated.
  - _Fix:_ Add tenant scoping to the WHERE clause: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Prefer validating project ownership earlier and always include tenant_id on all writes.
- **server/routes/conversations.ts:231** — INSERT into `project_notes` (project_id, note, author) does not include tenant_id. Creating notes linked to a project without writing tenant ownership can allow notes to be associated across tenants.
  - _Fix:_ Include tenant_id in the INSERT (and/or assert project ownership first). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'}). Ensure all tenant-scoped tables receive explicit tenant_id on INSERT and/or have WHERE tenant_id = ... on UPDATE/DELETE.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` performed without constraining by tenant_id (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). This allows modifying another tenant's row if an attacker supplies or guesses a message id.
  - _Fix:_ Include the tenant predicate in the UPDATE (and any other mutating statements). For example: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) — or use Drizzle's query builder: await db.update(inbox_messages).set({ is_read: true }).where(and(eq(inbox_messages.id, msg.id), eq(inbox_messages.tenantId, tenantId))); Ensure every INSERT/UPDATE/DELETE/SELECT against tenant-scoped tables always enforces the caller's tenant id.
- **server/routes/projects.ts:33** — SELECT against tenant-scoped tables in the projects list query uses subqueries that read project_files, project_notes, and project_conversations WITHOUT constraining those subqueries to the caller's tenant (no tenant_id filter).
  - _Fix:_ Constrain each subquery to the tenant, e.g. add AND tenant_id = ${tenantId} (or join through projects p and assert p.tenant_id = ${tenantId} inside the subquery) so counts only reflect rows for the requester's tenant.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} executes without constraining to tenant_id; this updates project rows without verifying tenant ownership in the UPDATE itself.
  - _Fix:_ Add tenant constraint to the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId} (or perform the UPDATE via a storage helper that enforces tenant scoping).
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is executed without a tenant_id constraint (second occurrence).
  - _Fix:_ Add tenant constraint to the UPDATE: WHERE id = ${projectId} AND tenant_id = ${tenantId} to ensure only the caller's tenant row is modified.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC selects from a tenant-scoped table (project_files) without a tenant_id constraint in the WHERE clause.
  - _Fix:_ Constrain the SELECT to the tenant, e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}, or JOIN to projects and assert projects.tenant_id = ${tenantId} in the same query.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${'uploads/' + filename} LIMIT 1 reads from project_files without constraining tenant_id.
  - _Fix:_ Add tenant constraint to the WHERE clause (AND tenant_id = ${tenantId}) or join to projects to enforce the tenant check in the query itself before returning file records.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} deletes from a tenant-scoped table without including tenant_id in the WHERE clause.
  - _Fix:_ Include tenant_id in the DELETE predicate (DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) or perform the deletion via a storage helper that enforces tenant ownership.
- **server/routes/watchlist.ts:38** — addWatchlistItem is called with `{ tenantId, ...req.body }` allowing an attacker-supplied tenantId in req.body to override the server-resolved tenantId and cause writes for an arbitrary tenant (tenant_id may come from untrusted input). This violates the invariant that INSERTs into tenant-scoped tables must set tenant_id from the caller/validated context.
  - _Fix:_ Do not allow req.body to override tenantId. Pass tenantId explicitly (and last) or remove tenantId from the incoming payload before calling addWatchlistItem. Example fixes: `const item = await addWatchlistItem({ ...req.body, tenantId });` or change addWatchlistItem signature to accept tenantId as a separate argument (e.g. `addWatchlistItem(tenantId, req.body)`) and ignore any tenant_id supplied in the body. Also validate/whitelist accepted fields from req.body to prevent unexpected overrides.
- **server/seed-cleanup.ts:7** — SELECT from tenant-scoped table heartbeat_tasks lacks tenant_id constraint (SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'). Reads against tenant-scoped tables must be scoped to a tenant.
  - _Fix:_ Scope this read to an explicit tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true ...) if the intention is admin/seed work, or iterate per-tenant explicitly. Avoid unscoped queries over tenant-scoped tables.
- **server/seed-cleanup.ts:44** — SELECT from tenant-scoped table heartbeat_tasks lacks tenant_id constraint (SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%'). Unscoped reads across tenants violate the tenant-isolation invariant.
  - _Fix:_ Constrain the SELECT to a tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...), or otherwise iterate tenants deliberately. Avoid global scans of tenant-scoped tables unless explicitly guarded and audited.
- **server/seed-cleanup.ts:58** — SELECT from tenant-scoped table heartbeat_tasks lacks tenant_id constraint (SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ...)). This read is unscoped across tenants.
  - _Fix:_ Scope the query by tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true ...) or iterate per-tenant explicitly. Ensure any admin/global intent is explicitly documented and guarded.
- **server/seed.ts:36** — SELECT from tenant-scoped table 'research_sessions' without a tenant_id filter (SELECT COUNT(*) FROM research_sessions). This reads potentially across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant. E.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}. Pass an explicit tenantId into the function or use the current request's tenant context when calling this seed code.
- **server/seed.ts:2158** — SELECT from tenant-scoped table 'research_programs' without a tenant_id filter (SELECT id, name, model FROM research_programs WHERE is_active = true). This enumerates programs across tenants.
  - _Fix:_ Filter by tenant_id (or include tenant_id in the SELECT and use it when updating). Example: SELECT id, tenant_id, name, model FROM research_programs WHERE is_active = true AND tenant_id = ${tenantId}.
- **server/seed.ts:3881** — UPDATE memory_entries SET fact = REPLACE(...) executed without scoping by tenant_id. memory_entries has tenant_id (there is an index) but the UPDATE lacks a tenant_id constraint and will modify rows for all tenants.
  - _Fix:_ Scope the update to a tenant (e.g. AND tenant_id = <tenantId>) or iterate tenants and apply the UPDATE per-tenant. Ensure every UPDATE against memory_entries includes WHERE tenant_id = <tenantId> (or an equivalent Drizzle .where(eq(memoryEntries.tenantId, tenantId))).
- **server/seed.ts:3907** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' is executed without a tenant_id filter. conversations is tenant-scoped (has tenant_id index) so this global UPDATE will change conversation rows across all tenants.
  - _Fix:_ Add a tenant constraint to the UPDATE (e.g. WHERE model = 'gpt-4.1' AND tenant_id = <tenantId>) or perform the migration per-tenant. Avoid running tenant-scoped schema/data migrations that update tenant tables globally without explicit tenant scoping.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` executed without a tenant_id constraint when tenantId is not provided. The query (db.select().from(experiments).where(...)) will return cross-tenant rows if detectStagnation is called with no tenantId.
  - _Fix:_ Enforce tenant scoping: require a tenantId parameter (fail closed if missing) or explicitly add eq(experiments.tenantId, tenantId) to the WHERE for all calls. If a global (multi-tenant) view is intended, make that explicit and document/secure it; otherwise throw if tenantId is not provided.
- **server/self-improvement.ts:521** — SELECT from tenant-scoped table `experiments` in getExperimentHistory falls back to an unconditional db.select().from(experiments) when no tenantId is supplied, returning data across tenants.
  - _Fix:_ Require tenantId for tenant-scoped reads or add a guard that throws if tenantId is missing. If supporting global history is intentional, move to an explicitly named function and ensure only authorized admin callers can use it.
- **server/stability-watchdog.ts:177** — SELECT from heartbeat_logs aggregates error counts without any tenant_id filter, reading across all tenants' heartbeat logs.
  - _Fix:_ Constrain the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or explicitly iterate per-tenant using the ALL_TENANTS sentinel/approved admin path. Ensure admin/global intent is explicit and reviewed before cross-tenant reads.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log runs without a tenant_id filter, which can delete event log rows across tenants.
  - _Fix:_ Limit deletion to a tenant (WHERE tenant_id = ${tenantId}) or perform per-tenant cleanup under an explicit admin/all-tenants flow. Use the tenant-scope helpers (tenantScope/requiredTenantScope) to validate intent.
- **server/stability-watchdog.ts:259** — pruneOldLogs deletes rows from heartbeat_logs older than a threshold but does not include a tenant_id constraint, allowing cross-tenant removal of logs.
  - _Fix:_ Scope the DELETE to a tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...) or run a deliberate per-tenant cleanup loop when admin-wide pruning is required. Avoid implicit global deletes on tenant-scoped tables.
- **server/stability-watchdog.ts:154** — Call to storage.createHeartbeatLog(...) when clearing a stuck task omits any tenantId. If the underlying storage function inserts into a tenant-scoped table, this may create a row in the wrong tenant or fail to set tenant_id.
  - _Fix:_ Pass the correct tenantId into storage.createHeartbeatLog (e.g. tenantId: info.tenantId or the run/task owner tenant) so the INSERT explicitly sets tenant_id. If createHeartbeatLog derives tenant from personaId, validate and document that behavior; prefer explicit tenant parameters.
- **server/storage.ts:242** — getConversationUnscoped performs a SELECT FROM conversations WHERE id = ${id} with NO tenant_id constraint (unscoped read of a tenant-scoped table).
  - _Fix:_ Avoid unscoped reads in general. If an unscoped fetch is absolutely required for orchestration, restrict usage to very narrow, audited callsites and require callers to explicitly validate/propagate the derived tenantId. Prefer returning both row and tenantId and require the caller to pass an explicit tenant context or assert an admin/ALL_TENANTS sentinel before calling.
- **server/storage.ts:298** — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL without any tenant_id constraint — reading tenant-scoped messages rows unscoped.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) and/or accept a tenantId parameter validated at function entry. If ids come from a prior tenant-scoped query, require the caller to pass the tenantId and assert it before executing this additional DB query.
- **server/storage.ts:398** — attachCitations executes UPDATE messages SET citations = ... WHERE id = ${messageId} with NO tenant_id constraint — mutating a tenant-scoped table without tenant scoping or tenant_id in the UPDATE.
  - _Fix:_ Require and validate tenantId, then include it in the WHERE clause (WHERE id = ${messageId} AND tenant_id = ${tenantId}). Do not write tenant-scoped rows by id only unless the callsite is strictly internal and audited; prefer explicit tenant scoping or a JOIN verification to ensure ownership before the UPDATE.
- **server/storage.ts:309** — createMessage looks up the referenced conversation by id using a SELECT FROM conversations WHERE id = ${data.conversationId} with NO tenant filter and derives tenant from that row. Caller-provided conversation_id is a foreign id; the referenced row is read without verifying caller ownership/context.
  - _Fix:_ Require the caller to provide/confirm tenant context or fetch the conversation with an explicit tenant constraint (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}), or else use a well-audited unscoped fetch only from trusted internal callers and document that the caller MUST validate the derived tenantId before proceeding.
- **server/storage.ts:805** — updateMemoryEmbedding executes db.update(memory_entries).set({ embedding }).where(eq(memory_entries.id, id)) without any tenant_id constraint — updating tenant-scoped rows by id only.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or accept a TenantScope/ALL_TENANTS sentinel. Ensure background/embed update jobs run with an explicit tenant context and audit trail if they must act across tenants.
- **server/storage.ts:818** — updateKnowledgeEmbedding executes db.update(agent_knowledge).set({ embedding }).where(eq(agent_knowledge.id, id)) with NO tenant_id constraint — mutating tenant-scoped agent_knowledge rows unscoped.
  - _Fix:_ Require tenantId or otherwise scope the UPDATE (e.g. AND tenant_id = ${tenantId}). For platform-wide embedding work, require an explicit ALL_TENANTS sentinel and ensure the caller is privileged and auditable.
- **server/storage.ts:841** — archiveExpiredMemories runs raw SQL UPDATE memory_entries SET status = 'archived' WHERE ... with NO tenant_id constraint — this will archive rows across all tenants.
  - _Fix:_ Either accept an explicit tenantId/TenantScope and apply requiredTenantScope(), or implement per-tenant archival loops with an explicit ALL_TENANTS sentinel and admin-only guard. Never run cross-tenant mutating statements silently without an explicit admin/ALL_TENANTS invocation and audit logging.
- **server/storage.ts:858** — archiveStaleMemories runs raw SQL UPDATE memory_entries SET status = 'archived' WHERE ... with NO tenant_id constraint — it mutates memory_entries platform-wide.
  - _Fix:_ Same as archiveExpiredMemories: require explicit tenant scoping or an auditable admin ALL_TENANTS invocation. Add tenant filters to the WHERE clause or iterate per-tenant with explicit authorization checks.
- **server/storage.ts:513** — getAllMemoriesForBackup selects memory_entries (memoryEntrySafeCols) across the entire table with no tenant filter — returns memories for all tenants.
  - _Fix:_ Require an explicit tenant parameter or an ADMIN/ALL_TENANTS sentinel and only allow this call from a privileged admin path. Add explicit authorization checks and audit logging. If this is intended to be a platform-wide backup, make the intention explicit in the API signature and require a guarded admin flag.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding performs UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL with NO tenant_id constraint — mutating a tenant-scoped table without tenant scoping.
  - _Fix:_ Require and accept tenantId (or re-use the tenant context passed to callers) and include tenant_id = ${tenantId} in the WHERE clause. Ensure helper functions that mutate tenant-scoped rows always receive/verify tenant context or are limited to privileged admin code paths.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table 'custom_tools' without constraining to the caller's tenant (no tenant_id filter). This reads global rows and can cause cross-tenant discovery/collisions.
  - _Fix:_ Scope the query to the requested tenant (e.g. add eq(customTools.tenantId, tenantId)). Example: await db.select().from(customTools).where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))).
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table 'custom_tools' by id without constraining to the caller's tenant. Reads by id must also enforce tenant ownership to prevent cross-tenant disclosure.
  - _Fix:_ Include tenant filter when selecting by id: await db.select().from(customTools).where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). If code path cannot access tenantId, ensure the caller provides/validates it before the query.
- **server/tool-learning.ts:365** — UPDATE of tenant-scoped table 'custom_tools' to increment usageCount uses only id in WHERE and lacks a tenant_id filter. This can alter usage counters across tenants.
  - _Fix:_ Constrain the update by tenant as well: await db.update(customTools).set({ usageCount: tool.usageCount + 1 }).where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). If tenantId is not available, fetch and verify the row's tenant before updating.
- **server/tools.ts:4548** — INSERT into project_notes executed without supplying tenant_id. Tenant-scoped tables must have tenant_id set on INSERT to enforce isolation; relying only on assertProjectInTenant is insufficient per the codebase invariant.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. VALUES (${params._tenantId}, ...)) or, better, use Drizzle's insert API with the tenantId column set. Ensure the inserted row's tenant_id equals the validated caller tenant (params._tenantId). Keep the assertProjectInTenant check but also write tenant_id into project_notes.
- **server/tools.ts:4551** — INSERT into project_files executed without supplying tenant_id. project_files appears to be tenant-scoped but the INSERT omits tenant_id (no NOT NULL tenant column set).
  - _Fix:_ Add tenant_id to the INSERT (or use Drizzle .insert with { tenantId: params._tenantId, ... }). Ensure the tenant_id value is the validated caller tenant and keep the assertProjectInTenant guard; do not rely solely on the guard for isolation enforcement.
- **server/tools.ts:6393** — INSERT into project_files (from generate_audio) performed without tenant_id. Although assertProjectInTenant is checked, the INSERT does not include tenant_id, violating the rule that every write to tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include tenant_id in the INSERT values (tenant_id = ${params._tenantId}) or use Drizzle's typed insert with tenantId field. This makes the row self-contained for tenant scoping and avoids cross-tenant writes if a guard is bypassed.
- **server/tools.ts:6727** — INSERT into project_files (from create_slideshow_video) performed without tenant_id. The row is written without an explicit tenant_id column despite project_files being tenant-scoped.
  - _Fix:_ Pass tenant_id explicitly in the INSERT (or use Drizzle .insert with tenantId set to params._tenantId). Also continue to validate project ownership with assertProjectInTenant, but ensure the DB write itself contains tenant_id to enforce isolation.
- **server/tools.ts:6927** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code executes a raw INSERT of (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without setting tenant_id, relying only on a prior assertProjectInTenant check.
  - _Fix:_ Include the tenant identifier explicitly in the INSERT and derive it only from a trusted source (e.g. params._tenantId after assertProjectInTenant). For example, use a parameterized INSERT/Drizzle insert that sets tenant_id: db.execute(sql`INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${outputFilename + ".mp4"}, ${outPath}, ${driveUrl || null}, ${"video"}, ${stats.size}, ${"system"})`) or use db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: params.project_id, ... }). Always ensure tenant_id is present on writes to tenant-scoped tables (and on reads/updates/deletes add WHERE tenant_id = <caller tenant>).
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against browser_workflows omits tenant scoping — "UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}" has no tenant_id constraint; invariant requires tenant-scoped operations include the caller's tenant filter.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. include AND tenant_id = ${tenantId} in the WHERE clause or use Drizzle's where(eq(table.tenantId, tenantId)) so the update is executed only for rows owned by ctx.tenantId. Example: await wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`);
- **server/tools/domains/files/handlers.ts:907** — UPDATE on projects omits tenant scoping — "UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}" lacks a tenant_id constraint. Without explicit tenant scoping this could affect a project id belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE. Include AND tenant_id = ${tenantId} in the WHERE clause or use Drizzle's where(eq(projects.tenantId, tenantId)). Example: await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${ctx.tenantId}`);
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table `knowledge_triples` executes without a tenant_id constraint (UPDATE ... WHERE id = ${row.id}). This mutates rows by id alone and violates the invariant that every write against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: include AND tenant_id = ${tenantId} in the WHERE clause (or use the storage layer API that enforces tenant scoping). Also apply the same tenant filter to any other UPDATE/DELETE statements to enforce tenant isolation.
- **server/tools/domains/security/handlers.ts:260** — Unscoped SELECT against tenant-scoped table `provider_keys` — query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` has no tenant_id filter and enumerates provider configuration across tenants.
  - _Fix:_ Restrict the aggregation to the caller's tenant: add `WHERE tenant_id = ${tenantId} AND ...` or use Drizzle .where(eq(providerKeys.tenantId, tenantId)). If this aggregated cross-tenant statistic is intended only for platform admin, move it behind an explicit admin-only guarded code path with proper authorization and auditing.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} is executed without also constraining by tenant_id. Reading a tenant-scoped `conversations` row by id alone can expose cross-tenant information and violates the requirement that reads be filtered to the caller's tenant.
  - _Fix:_ Add an explicit tenant filter to the query so the DB only returns the row when it belongs to the expected tenant, e.g. SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 (or use Drizzle: .where(and(eq(conversations.id, convId), eq(conversations.tenantId, tenantId)))). This ensures ownership is enforced by the SQL layer rather than solely by application-side comparisons.
- **server/video-job-runner.ts:235** — recoverStaleVideoJobs updates tenant-scoped videoJobs rows without any tenant_id constraint — it will mark jobs across all tenants as failed.
  - _Fix:_ Restrict the update to a specific tenant (e.g. accept a tenantId and add eq(videoJobs.tenantId, tenantId) to the WHERE clause), or run the recovery per-tenant. If this is intended global maintenance, document and gate it as an admin-only operation; otherwise add the tenant filter to avoid cross-tenant state changes.
- **server/watchlist.ts:95** — createAlert inserts watchlist_alerts with a watchlist_item_id supplied by caller (watchlistItemId) without checking that that watchlist item belongs to the tenant. This allows creating alerts that reference another tenant's item.
  - _Fix:_ Before inserting, verify the referenced watchlist_item exists and belongs to the tenant (SELECT id FROM watchlist_items WHERE id = ${watchlistItemId} AND tenant_id = ${tenantId}). Reject or fail if ownership doesn't match. Enforce the check everywhere callers supply foreign ids.
- **server/watchlist.ts:111** — getAlerts LEFT JOINs watchlist_items (wi) by id but does not constrain wi.tenant_id. A watchlist_alert (wa) filtered to the tenant can JOIN to a watchlist_items row with the same id from a different tenant, leaking another tenant's item name/category in the response.
  - _Fix:_ Constrain the JOIN to the tenant (e.g. LEFT JOIN watchlist_items wi ON wi.id = wa.watchlist_item_id AND wi.tenant_id = ${tenantId}), or add AND wi.tenant_id = ${tenantId} to the WHERE clause. Prefer explicit ownership checks on joined tables to avoid implicit cross-tenant joins.
- **server/webhookHandlers.ts:428** — handleSubscriptionActivation updates the tenants row using tenantId parsed directly from Stripe session metadata and executes `UPDATE tenants SET ... WHERE id = ${tenantId}` without verifying that the Stripe customer/subscription actually belongs to that tenant. Metadata is untrusted input — this permits an attacker to craft metadata to mutate arbitrary tenant rows.
  - _Fix:_ Validate ownership before mutating tenant rows. For example, resolve the tenant by a trusted Stripe binding (lookup tenant by stripe_customer_id or by a server-maintained mapping), verify the subscription/customer fetched from Stripe matches the tenant's stored stripe_customer_id/subscription_id, and only then perform the UPDATE. Do not accept raw tenant IDs from external metadata without cross-checks. Log and reject mismatches and fail closed so Stripe retries for operator investigation.

### MEDIUM (39)

- **server/agentic/task-forces.ts:44** — Request-derived personaIds are converted to an SQL array literal and injected into the query via a SQL fragment (sql`${literal}::int[]`). Although values are filtered to integers, this builds a SQL fragment from caller input rather than using a parameterized array, creating a potential injection/isolation risk.
  - _Fix:_ Avoid injecting pre-built SQL fragments from request data. Use parameterized array binding (pass personaIds as a parameter/JS array to the query or JSONB) or a Drizzle-supported typed parameter (e.g. pass an int[] parameter or JSONB and cast on the DB side). Validate inputs strictly and prefer parameter binding rather than embedding SQL literals.
- **server/auto-project.ts:112** — SELECT from messages WHERE conversation_id = ${conversationId} lacks tenant_id constraint. Although the conversation row was validated earlier, the messages read is not explicitly scoped to the tenant in SQL.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages query (or use a Drizzle eq on messages.tenant_id). Prefer querying messages constrained by tenant to avoid relying solely on prior checks and reduce attack surface from poisoned ids.
- **server/auto-transcript.ts:46** — SELECT from messages WHERE conversation_id = ${conversationId} has no tenant_id constraint. The code validates the conversation belongs to tenant, but the messages query itself is not scoped by tenant_id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the messages query (or use Drizzle .where(eq(messages.tenant_id, tenantId))). Even if the conversation was validated, every read against a tenant-scoped table should include the tenant filter to avoid transitive isolation bugs.
- **server/auto-transcript.ts:128** — Backfill query uses a correlated subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without tenant scoping, causing messages to be consulted across tenants when deciding which conversations to backfill.
  - _Fix:_ Constrain the subquery by tenant, e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id, or join messages and filter by tenant_id to ensure the existence/count check only considers messages for the same tenant.
- **server/auto-transcript.ts:153** — Another backfill query (SELECT DISTINCT pc.conversation_id ... WHERE ... AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0) uses a messages subquery with no tenant filter, reading message presence across tenants.
  - _Fix:_ Add tenant scoping to the subquery, e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0, or rewrite using an explicit join that enforces tenant_id equality to avoid cross-tenant reads.
- **server/business-tools.ts:127** — Unsafe string-interpolated SQL fragments are built from request-derived values (params.status and params.amount_paid) when constructing the 'updates' array (`status = '${params.status}'`, `amount_paid = ${params.amount_paid}`). Even though this particular constructed array is not used later, it is a maintenance risk and could lead to SQL injection or tenant-isolation mistakes if later executed or reused.
  - _Fix:_ Avoid constructing SQL via string interpolation with request data. Use parameterized queries (drizzle sql`` with interpolations) or the existing safe pattern used elsewhere: db.execute(sql`UPDATE invoices SET status = ${params.status}, amount_paid = COALESCE(${params.amount_paid || null}, amount_paid), updated_at = CURRENT_TIMESTAMP WHERE id = ${params.invoice_id} AND tenant_id = ${tid}`). Remove the unused raw-string fragments or replace them with safe, parameterized building of the query.
- **server/crews-engine.ts:336** — addCrewAgent inserts persona_id (params.personaId) supplied by caller without verifying that the persona belongs to the caller's tenant. If personas are tenant-scoped, this could let callers reference another tenant's persona.
  - _Fix:_ Validate persona ownership before inserting: SELECT id, tenant_id FROM personas WHERE id = <personaId> AND tenant_id = <tenantId>. If persona is global, document that constraint; otherwise reject or nullify an out-of-tenant persona_id.
- **server/crews-engine.ts:229** — createCrew inserts manager_persona_id from caller input without checking persona ownership. If persona rows are tenant-scoped, this allows linking to personas from other tenants.
  - _Fix:_ Validate managerPersonaId before using it (SELECT id, tenant_id FROM personas WHERE id = <managerPersonaId> AND tenant_id = <tenantId>), or require the API to only accept persona IDs already verified to belong to the tenant.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal ('${vec}'::vector) built from caller-supplied query embedding. This injects a string into raw SQL rather than using parameterized binding.
  - _Fix:_ Avoid sql.raw interpolation of values derived from request/user input. Use parameterized bindings or Drizzle helpers to pass the embedding as a parameter and cast safely to vector (or use a parameterized function/placeholder). If raw is unavoidable, validate and constrain the input and document why it's safe; prefer per-tenant parameterization.
- **server/embeddings.ts:252** — Same as above: ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)} interpolates a vector literal into raw SQL from a request-derived embedding.
  - _Fix:_ Use a parameterized approach instead of sql.raw interpolation. For example, pass the embedding as a parameter and cast in SQL (use parameterized $N::vector or Drizzle binding) or use validated, typed helpers to avoid string interpolation into raw SQL.
- **server/embeddings.ts:566** — Use of sql.raw() with interpolated vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) / ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). The vector originates from a user query embedding and is inserted via raw interpolation.
  - _Fix:_ Stop injecting the vector literal via sql.raw. Use parameterized queries or Drizzle parameter binding so embeddings are sent as parameters and cast safely to vector in SQL. If using raw is necessary, ensure strict validation and prefer updating the DB access layer to accept embeddings as parameters.
- **server/episode-playbooks.ts:135** — Construction of a Postgres array literal via string interpolation in the UPDATE: id = ANY(${`{${ids.join(",")}}`}::int[]). Although ids were derived from a prior DB read, this builds SQL text from joined values rather than using parameterized array binding — potential injection/vector if inputs aren't strictly numeric.
  - _Fix:_ Use Drizzle/parameterized array binding (e.g. sql.join/sql.array or pass the array as a parameter) instead of embedding a constructed '{1,2,3}' string. Example: WHERE id = ANY(${sql.array(ids, 'int')}) or id IN (${sql.join(ids.map(n => sql`${n}`), sql`, `)}).
- **server/glasses-gateway.ts:172** — UPDATE on apiKeys table sets lastUsedAt using only apiKeys.id in the WHERE clause (where(eq(apiKeys.id, key.id))) without constraining by tenant_id.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or otherwise ensure the write is scoped to the authenticated key's tenant.
- **server/lib/agent-trace.ts:124** — UPDATE agent_trace_spans sets ended_at/status/summary with WHERE span_id = ${spanId} but does not include tenant_id in the WHERE clause. agent_trace_spans is tenant-scoped and the update therefore lacks an explicit tenant constraint.
  - _Fix:_ Include tenant scoping in the UPDATE. One approach: record tenantId when inserting (persistSpanOpen) into an in-process map keyed by spanId, and use that tenantId in persistSpanClose's WHERE (AND tenant_id = ${tenantId}). Alternatively, have callers pass tenantId into persistSpanClose or run the UPDATE inside the same ALS context and read currentTenantId(). Ensure the tenant predicate is always present.
- **server/lib/mcp-api-keys.ts:181** — UPDATE mcp_api_keys SET last_used_at = NOW() issues by id only (WHERE id = ${row.id}) with no tenant_id filter — writing to tenant-scoped table without explicit tenant constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) so the touch cannot modify a row belonging to a different tenant.
- **server/lib/outcome-promotion.ts:158** — SELECT against tenant-scoped table `deliverable_grade_events` is executed without a WHERE tenant_id = ... constraint (query aggregates across all tenants). This violates the invariant that tenant-scoped reads/writes must be constrained to a caller tenant unless the cross-tenant access is an explicitly audited global job.
  - _Fix:_ If this is intended as a per-tenant operation, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) and run per-tenant. If this is meant to be a global nightly job, document and isolate the code path (restrict execution to an admin-only/privileged background process) and ensure any downstream outputs/files are produced per-tenant and never leak other tenants' data. Alternatively, make the aggregation explicit and safe by iterating tenants and applying a per-tenant WHERE clause when selecting grade events.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table 'scheduled_posts' performed without a tenant_id filter (CTE pulls due rows across all tenants).
  - _Fix:_ If this is intended to run system-wide, document and gate it behind an explicit ADMIN-only path and ensure results are never returned to unprivileged callers. Otherwise restrict the query to a tenant (e.g. add WHERE tenant_id = ${tenantId} or parameterize tenant), or perform per-tenant selection loops so every row update is constrained by tenant_id.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table 'agent_runs' is executed without filtering by tenant_id (reads running rows across all tenants).
  - _Fix:_ Either restrict the query by tenant_id (WHERE tenant_id = ${tenantId}) if the caller is tenant-scoped, or ensure this admin/system-scoped audit is executed only in an explicit admin context and that its results are not exposed to tenant-scoped callers. Add an explicit comment documenting the intended system-wide behavior and access controls.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table 'mind_tickets' is executed without filtering by tenant_id (reads in_progress rows across all tenants).
  - _Fix:_ Either restrict the query by tenant_id (WHERE tenant_id = ${tenantId}) for tenant-scoped callers, or ensure this system-wide audit runs only with explicit admin privileges and that results are handled only by trusted operator tooling. Document and enforce the admin-only execution path if system scope is intended.
- **server/lib/system-probes.ts:163** — A count query against tenant-scoped 'messages' table is executed without a tenant_id filter (db.select count(*) FROM messages).
  - _Fix:_ If the probe should be tenant-scoped, add WHERE tenant_id = ${tenantId} to the query. If it is intentionally aggregating across all tenants for internal telemetry, ensure it runs only in an admin/system context and that any surfaced values are not leaked to tenant-scoped callers; add an explicit comment/gate to make the system-wide intent and access controls clear.
- **server/minds-engine.ts:262** — SELECT uses an interpolated array literal constructed by joining `ticket.dependsOn` into `{...}`: `WHERE id = ANY({...}::int[])`. Although the ticket.dependsOn originates from the DB, it may have been written earlier from request data (see createTicket). This pattern embeds data into the SQL text instead of passing a typed array parameter (injection / correctness risk).
  - _Fix:_ Avoid constructing SQL array literals via string-join. Use a parameterized array binding or the Drizzle query builder to pass the integer array directly (e.g. WHERE id = ANY(${ticket.dependsOn}) with the driver sending a proper int[] parameter), or rewrite to use WHERE id = ANY($1::int[]) and supply the JS number[] as the bound parameter. If dependsOn originally came from user input, ensure it was validated/owned before use.
- **server/routes.ts:167** — SELECT from tenant-scoped table `file_storage` without tenant constraint during startup restore. The query db.select(...).from(fileStorage).where(and(like(fileStorage.mimeType, "image/%"), isNull(fileStorage.storageKey))) does not filter by tenant_id, so it will read rows belonging to all tenants.
  - _Fix:_ If this operation must iterate across all tenants, require explicit admin context and document it; otherwise add a tenant filter (e.g. .where(eq(fileStorage.tenantId, tenantId)) or include tenantId in the AND conditions). Ensure any background job that scans tenant data runs with explicit admin authorization and logging.
- **server/routes.ts:766** — SELECT from tenant-scoped table `projects` with no tenant constraint. The backfill query selects p.id, p.name, p.tenant_id FROM projects JOIN tenants ... WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = '' and processes projects across all tenants without an explicit tenant restriction.
  - _Fix:_ If this backfill must operate across all tenants, require and validate that the caller is a trusted admin process and clearly document that behavior. Otherwise restrict the query to a specific tenant (WHERE p.tenant_id = ${tenantId}) or process tenants one-by-one using an explicit tenant context.
- **server/routes.ts:1532** — Public stats route issues unscoped SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) without WHERE tenant_id = <tenant>. These queries return global counts across all tenants.
  - _Fix:_ Decide whether global aggregates are acceptable for this public endpoint. If not, scope the queries to the caller's tenant (use getTenantFromRequest(req) and include WHERE tenant_id = ${tenantId}). If global aggregates are intentionally exposed, gate the endpoint to admin-only and document that it scans all tenants.
- **server/routes.ts:1561** — /api/public/architecture performs multiple unscoped SQL queries (SELECT count(*) FROM conversations, messages, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, governance_rules, research_sessions) against tenant-scoped tables without filtering by tenant_id, exposing global counts.
  - _Fix:_ Either limit these queries to a tenant context (WHERE tenant_id = ${tenantId}) or enforce an admin-only guard for the endpoint and ensure the global access is an explicit design decision. Prefer explicit tenant scoping whenever possible to maintain isolation.
- **server/routes.ts:2315** — Order lookup endpoint SELECTs from tenant-scoped table `delivery_logs` without any tenant_id constraint (WHERE orderId = ${sessionId} OR stripePaymentId = ${sessionId}). This returns rows across tenants based solely on an order/session capability string.
  - _Fix:_ If the endpoint is intended as a capability URL (unguessable session id), explicitly document that reading across tenants is by design. If not, add tenant scoping (e.g. include tenant_id) or validate that the returned row belongs to the expected tenant before returning details. Consider returning minimal public-safe fields only and avoid exposing tenant identifiers.
- **server/routes/admin.ts:716** — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant_id constraint. If tool_performance is a tenant-scoped table this will surface data across tenants.
  - _Fix:_ If tool_performance is tenant-scoped, add WHERE tenant_id = ${tenantId} (or otherwise bind the tenant context) to limit results to the caller's tenant. If the query is intentionally platform-global, ensure the route is restricted to platform-admin callers and document that behavior explicitly.
- **server/routes/admin.ts:734** — Query selecting from ai_insights (WHERE status = 'new' ...) has no tenant_id constraint. If ai_insights is tenant-scoped this will return incidents across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) to the WHERE clause so only the caller's tenant incidents are returned. If cross-tenant visibility is intentional for admin use, ensure the route is strictly platform-admin gated and document the rationale.
- **server/seed.ts:4121** — UPDATE agent_knowledge SET content = ... WHERE id = ${rows[0].id} omits tenant_id even though agent_knowledge is tenant-scoped. The code previously selected an id using tenant_id = 1, but the subsequent UPDATE does not include tenant_id in the WHERE clause.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) to enforce ownership at update time. Even when the id was looked up with a tenant filter earlier, always include tenant scoping on subsequent writes to tenant-scoped tables to follow the invariant and defend against accidental cross-tenant modification.
- **server/storage.ts:826** — getMemoriesWithoutEmbeddings selects from memory_entries WHERE status='active' AND embedding IS NULL with NO tenant filter — reading tenant-scoped rows platform-wide.
  - _Fix:_ Require tenant scoping or require the caller to explicitly pass ALL_TENANTS and have admin privileges. Alternatively iterate over tenants explicitly with authorization and audit, or accept a tenantId/TenantScope parameter and apply requiredTenantScope().
- **server/storage.ts:831** — getKnowledgeWithoutEmbeddings selects agent_knowledge (knowledgeSafeCols) WHERE embedding IS NULL with NO tenant filter — global read across tenant-scoped knowledge rows.
  - _Fix:_ Require tenant scoping (tenantId or ALL_TENANTS sentinel) and appropriate admin authorization/audit. If this is a background worker intended to operate platform-wide, require an explicit, documented admin flag and logging.
- **server/storage.ts:942** — getHeartbeatTask selects from heartbeat_tasks WHERE id = ${id} with NO tenant constraint. heartbeat_tasks is tenant-scoped elsewhere; this will return a task potentially from another tenant.
  - _Fix:_ Either accept a tenantId argument and filter (WHERE id = ${id} AND tenant_id = ${tenantId}), or clearly document this function as an audited internal unscoped accessor and restrict its usage to trusted contexts only.
- **server/video-job-runner.ts:208** — isCancelRequested selects videoJobs.cancelRequested by jobId only and does not constrain by tenant_id, allowing reads of another tenant's cancel flag.
  - _Fix:_ Require/accept the tenantId alongside jobId (or lookup the job's tenant) and include eq(videoJobs.tenantId, tenantId) in the WHERE clause. Alternatively, fetch the row and validate its tenant matches the caller before returning cancelRequested.
- **server/watchlist.ts:16** — addWatchlistItem inserts created_by_persona_id and escalate_to_persona_id directly without verifying that those persona IDs belong to the same tenant. This can create cross-tenant references.
  - _Fix:_ Verify persona ownership before inserting (SELECT personas WHERE id = createdByPersonaId AND tenant_id = tenantId) and either reject or null/normalize if the persona does not belong to the tenant. Apply the same check for escalateToPersonaId.
- **server/wiring-invariants.ts:366** — checkSubsystemFreshness executes raw SQL selecting COUNT/MAX from subsystem tables (e.g. `SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`) without scoping to a tenant_id. Several of these subsystem tables (knowledge_nudges, graph_memory, tool_performance, etc.) are tenant-scoped — the query reads cross-tenant data.
  - _Fix:_ If this probe is intentionally global/admin-only, make that explicit and run it under an admin/system context (or document it). Otherwise, restrict the aggregation per-tenant (e.g. add WHERE tenant_id = ${tenantId} or iterate tenants) so you never read other tenants' rows unintentionally. Avoid constructing SQL with untrusted identifiers; if table/column names come from code, validate them against an allowlist before embedding.
- **server/wiring-invariants.ts:535** — checkMemoryGrowth uses sql.raw to query per-table totals (e.g. `SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`) with no tenant_id filter. Tables like `messages`, `graph_memory`, `memory_entries` are tenant-scoped — this reads across all tenants.
  - _Fix:_ Scope the counts to a specific tenant (WHERE tenant_id = ${tenantId}) when run in a tenant-context, or explicitly run as a system/admin diagnostic that iterates tenants and records per-tenant metrics. Prefer parameterized queries and avoid interpolating table names from unvalidated sources; if table names are internal constants, keep an allowlist and assert membership before embedding.
- **server/wiring-invariants.ts:561** — Final graph_memory probe runs `SELECT COUNT(*)::int AS cnt FROM graph_memory` with no tenant constraint. graph_memory is a tenant-scoped store; querying it across all tenants can leak counts/metadata between tenants.
  - _Fix:_ If intended as a system-wide health check, run it explicitly in an admin-only diagnostic flow and make that clear. If not, add a WHERE tenant_id = ${tenantId} or iterate tenants to collect per-tenant counts. Ensure all operational code that touches tenant-scoped tables includes an explicit tenant filter.
- **server/wiring-invariants.ts:244** — checkDormantTools queries `tool_performance` (SELECT count(*) ... FROM tool_performance WHERE last_success_at > ...) without scoping to tenant_id. tool_performance likely records per-tenant tooling metrics — this aggregates across tenants.
  - _Fix:_ Scope these diagnostics to a tenant (WHERE tenant_id = ${tenantId}) or, if the intention is to run a global admin check, make it explicit and implement an admin-only path that iterates tenants. Avoid unsigned cross-tenant aggregates in normal application code.
- **server/wiring-invariants.ts:417** — checkToolDrift runs a grouped query on tool_performance (`FROM tool_performance ... GROUP BY tool_name`) with no tenant filter, potentially exposing per-tenant tool failure information across tenants.
  - _Fix:_ Restrict the query to a tenant context (add WHERE tenant_id = ${tenantId}) or run the check per-tenant. If a global view is required for operators, perform it in an admin-only diagnostic with explicit authorization and logging, or aggregate in a separate telemetry pipeline that respects tenant boundaries.

### LOW (1)

- **server/routes.ts:2087** — Presenter slide endpoint SELECTs presenter_sessions by token without an explicit tenant_id filter. The token acts as a capability but the SQL reading a tenant-scoped table lacks a tenant WHERE clause.
  - _Fix:_ If the token is a valid capability that intentionally authorizes access regardless of tenant, ensure the capability verification code is robust and the token is single-purpose. Otherwise, add tenant scoping (or verify the session belongs to an authenticated tenant) before returning tenant-scoped data.

### Suppressed — confirmed false positives (195)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `agent_activity` does not include tenant_id in the WHERE clause. This can allow updating rows across tenants by id.
  - _Why false positive:_ Updates by server-side serial id in internal helper completeActivity; no untrusted request boundary shown.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE agent_evals SET ... WHERE id = ${evalId} — the UPDATE omits tenant scoping, allowing cross-tenant modification if evalId is guessed.
  - _Why false positive:_ evalId comes from the immediately preceding INSERT RETURNING id in same server flow, not caller input.
- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) — this UPDATE is not tenant-scoped and will apply across all tenants.
  - _Why false positive:_ Intentional platform-wide housekeeping sweep; per-row tenant_id is carried in RETURNING and used for same-tenant follow-up routing.
- **server/agentic-features.ts:797** _(upstream-verified)_ — UPDATE outreach_enrollments SET ... WHERE id = ${row.id} — the UPDATE omits tenant scoping and may modify enrollments across tenants by id.
  - _Why false positive:_ row.id is selected just above with WHERE e.tenant_id = params.tenantId, so the update targets a row already tenant-validated.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals() performs a bulk UPDATE on agentApprovals to set status='expired' WHERE status='pending' AND expiresAt < now() without tenant scope — it will expire approvals across all tenants.
  - _Why false positive:_ Global expiry housekeeping updates status only and returns per-row tenantId for isolated follow-up run handling.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_runs (appendStep) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint. This allows updating a run row by id across tenants.
  - _Why false positive:_ appendStep is an internal worker/logging helper using server-generated run.id from createRun/withRun, not request-supplied id.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_runs (updateRunState) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Why false positive:_ updateRunState is an internal server helper updating by server-generated run primary key, with no untrusted boundary shown.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_runs (replaceRunState) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Why false positive:_ replaceRunState is an internal server helper keyed by run serial id, not shown as caller-controlled.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_runs (completeRun) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Why false positive:_ completeRun is a server-invoked lifecycle transition by run PK; runId appears to be internally generated and propagated.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_runs (failRun) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Why false positive:_ failRun is an internal lifecycle helper updating a server-created run by primary key, not request-derived here.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_runs (pauseRun) uses `.where(eq(agentRuns.id, runId))` with NO tenant_id constraint.
  - _Why false positive:_ pauseRun is an internal run-state transition by server-generated run id; no untrusted cross-tenant source shown.
- **server/data-protection.ts:168** _(system-sweep)_ — permanentlyPurgeSoftDeleted deletes from conversations with DELETE FROM conversations WHERE id = ${conv.id} without constraining by tenant_id. This violates the tenant-isolation invariant and could delete a conversation row if IDs are re-used or if id lookup is tampered with.
  - _Why false positive:_ Platform-wide purge sweep selects each row with its own tenant_id and only performs housekeeping deletes, not tenant-facing data mixing.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument() issues a DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete chunks belonging to other tenants' collections that share doc_path/collection_id values (or if collection_id is guessable).
  - _Why false positive:_ Same call path first verifies doc_collections.id belongs to tenantId, and delete is constrained by that validated collection_id plus doc_path.
- **server/embeddings.ts:790** _(system-sweep)_ — UPDATE memory_entries in backfillEmbeddingVecs uses id-only WHERE clause with no tenant_id constraint — can modify other tenants' rows.
  - _Why false positive:_ Backfill is a platform maintenance loop updating only embedding_vec on rows it selected; no tenant-facing data return or mixing.
- **server/embeddings.ts:807** _(system-sweep)_ — UPDATE agent_knowledge in backfillEmbeddingVecs uses id-only WHERE clause with no tenant_id constraint — can modify other tenants' rows.
  - _Why false positive:_ Backfill is a platform maintenance loop updating only embedding_vec on rows it selected; no tenant-facing data return or mixing.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id column. The INSERT INTO project_files (...) VALUES (...) omits tenant_id even though project_files is tenant-scoped and INSERTs must set tenant_id explicitly.
  - _Why false positive:_ Referenced schema note says project_files has no tenant_id; insert is transitively isolated via tenant-verified parent project_id.
- **server/mpeg-engine.ts:1416** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id. The upsert for project_files omits tenant_id in the VALUES list.
  - _Why false positive:_ Referenced schema note says project_files has no tenant_id; insert is transitively isolated via tenant-verified parent project_id.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE on plans to claim an approved plan lacks tenant_id filtering (UPDATE plans ... WHERE id = ${planId} AND status = 'approved'). This allows cross-tenant modifications/claims.
  - _Why false positive:_ Server worker claims a plan by server-supplied global PK planId; not from request boundary, and row tenant_id is returned for downstream use.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status='failed' WHERE id = ${planId} has no tenant_id filter — mutates tenant-scoped table without ensuring caller ownership.
  - _Why false positive:_ This update occurs only after the prior CAS update returned the same plan row including tenant_id, proving ownership of the claimed row in this call path.
- **server/plan-executor.ts:740** _(upstream-verified)_ — Final UPDATE on plans (setting status and appending execution_log) uses WHERE id = ${planId} with no tenant_id constraint — can update other tenants' plans.
  - _Why false positive:_ Final status update is on the same plan row successfully claimed earlier in executePlan via the CAS update/RETURNING path.
- **server/plan-executor.ts:763** _(internal-worker-pk)_ — Error handler UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' omits tenant_id — can fail/mark executing plans across tenants.
  - _Why false positive:_ Catch-path rollback is in the same internal executor function operating on a server-invoked global PK planId, not an untrusted tenant request id.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans: UPDATE plans SET status='approved' ... WHERE status='executing' AND updated_at < ... RETURNING id has no tenant_id filter — it resets 'executing' plans for all tenants globally.
  - _Why false positive:_ Boot-time recovery intentionally scans all tenants' stale executing plans and only mutates housekeeping status fields per row; no tenant data is returned or mixed.
- **server/plan-executor.ts:788** _(system-sweep)_ — resumeStuckPlans: SELECT id FROM plans WHERE status = 'approved' LIMIT 50 does not filter by tenant_id — enumerates approved plans across all tenants for executor kicks.
  - _Why false positive:_ Boot-time maintenance globally selects approved plan ids to kick internal execution; it handles each row independently and does not expose or mix tenant content.
- **server/process-governor.ts:595** _(upstream-verified)_ — executeAction('disable_task') updates heartbeat_tasks with UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} but omits tenant_id — a write that can disable another tenant's task row by id.
  - _Why false positive:_ t.id comes from a preceding SELECT on heartbeat_tasks scoped by ht.tenant_id = tenantId in the same loop, so the row set is already tenant-owned.
- **server/process-governor.ts:641** _(upstream-verified)_ — executeAction('restart_agent') updates agent_desks: UPDATE agent_desks SET ... WHERE id = ${desk.id} without tenant_id — mutates another tenant's agent desk if ids collide.
  - _Why false positive:_ desk.id is sourced from the immediately preceding SELECT on agent_desks constrained by ad.tenant_id = tenantId, so ownership is established earlier in this call path.
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — attemptRebaseOnDrift: UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (on hallucinated new OLD_CODE) omits tenant_id — writes to tenant-scoped proposals table without tenant check.
  - _Why false positive:_ Background verifier updates a server-created proposal row by primary key only; proposalId comes from internal queue, not request input at this sink.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — attemptRebaseOnDrift: UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} does not include tenant_id — persists rebased diff globally across tenants.
  - _Why false positive:_ Server-side rebase worker persists to the same internally queued proposal id; pk is not taken from an untrusted tenant request boundary here.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table agent_knowledge omits tenant_id in WHERE clause (UPDATE agent_knowledge ... WHERE id = ${r.id}). This allows an update by id without re-checking that the caller/runner is operating under the correct tenant context.
  - _Why false positive:_ Scheduler sweep processes due rows across tenants using each row's own tenant_id earlier in the loop and only updates housekeeping content on that same row by id.
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE against tenant-scoped table research_sessions performed without any tenant_id constraint (cleanupZombieSessions). This will modify rows across all tenants.
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping sweep that only marks stale running sessions completed and returns aggregate/internal ids, not tenant-mixed content.
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id (INSERT INTO project_notes (project_id, note, author) ...). Every INSERT into a tenant-scoped table must explicitly set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through tenant-checked parent project_id after SELECT id FROM projects ... tenant_id = tenantId.
- **server/routes/projects.ts:225** _(fk-transitive)_ — INSERT into project_files (project_id, file_name, file_path, file_url, file_type, file_size) is missing tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert uses project_id whose parent project ownership was verified with tenant_id immediately above.
- **server/routes/projects.ts:293** _(fk-transitive)_ — INSERT into project_files during multipart upload (VALUES (${projectId}, ..., ${fileUrl || downloadPath}, ...)) does not include tenant_id.
  - _Why false positive:_ project_files has no tenant_id and multipart insert is scoped through previously tenant-verified parent project_id.
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This can modify another tenant's session row if ids collide.
  - _Why false positive:_ Update targets the just-inserted sculptor_sessions row returned by RETURNING * in the same server function, not an untrusted external id.
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). This write is not scoped to the caller's tenant.
  - _Why false positive:_ Conversation-id write updates the same freshly created session row by server-generated pk within one internal flow.
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). monitorSession's end-of-run update can overwrite another tenant's row.
  - _Why false positive:_ monitorSession is an internal async worker operating on a sessionId created and passed by server code, not directly from request input at this sink.
- **server/seed-cleanup.ts:15** _(system-sweep)_ — UPDATE on tenant-scoped table heartbeat_tasks lacks tenant_id constraint (UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true). This will disable tasks across all tenants.
  - _Why false positive:_ boot-time cleanup intentionally disables runaway delegation tasks across tenants as housekeeping, with no tenant data returned or mixed
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE on tenant-scoped table heartbeat_tasks lacks tenant_id constraint (UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true). This write affects all tenants matching the predicate.
  - _Why false positive:_ boot-time maintenance disables stray session-logger tasks platform-wide; housekeeping-only mutation across tenants
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE on tenant-scoped table heartbeat_tasks lacks tenant_id constraint (UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true). This will disable matching tasks across all tenants.
  - _Why false positive:_ platform-wide cleanup of orphaned repo-examination tasks is a housekeeping sweep, not tenant-facing data access
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE on tenant-scoped table 'research_programs' without a tenant_id constraint (UPDATE research_programs SET model = ... WHERE id = ...). Updating by id only may modify rows for other tenants if id is not scoped in the WHERE clause.
  - _Why false positive:_ seed repair loop scans active programs and fixes invalid model ids in place across tenants; housekeeping-only mutation with no tenant data exposure
- **server/seed.ts:2447** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' without tenant scoping (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ...). This writes to a tenant-scoped table with no tenant_id constraint and can affect other tenants' data.
  - _Why false positive:_ write is gated by existingDeploy owner-deployment check and targets a fixed owner row id for one known deployment
- **server/seed.ts:2516** _(fk-transitive)_ — SELECT from (likely) tenant-scoped table 'conversations' without tenant_id filtering (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). The results are then used to INSERT links, which risks creating cross-tenant associations or leaking data across tenants.
  - _Why false positive:_ project_conversations has no tenant_id and links conversation to its own project_id parent; backfill derives both ids from same conversation row
- **server/seed.ts:3853** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without scoping by tenant_id. heartbeat_tasks is tenant-scoped (has tenant_id index) but the DELETE has no WHERE tenant_id = ... filter, so it will delete matching rows across all tenants.
  - _Why false positive:_ seed cleanup purges auto-disabled heartbeat task rows platform-wide as housekeeping only, without returning tenant content
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = <existing id> without also constraining tenant_id. This allows updating another tenant's memory entry by id.
  - _Why false positive:_ the id being updated comes from an immediately prior SELECT on memory_entries constrained by tenant_id = tenantId
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without a tenant_id predicate, potentially removing disabled delegation tasks for all tenants.
  - _Why false positive:_ watchdog purges stale disabled delegation tasks across tenants as maintenance housekeeping and only reports aggregate count
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — Unscoped SELECT against tenant-scoped table `provider_keys` — query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` has no tenant_id filter. This returns provider metadata (and counts) across all tenants and can leak sensitive provider/key presence information.
  - _Why false positive:_ provider_keys schema note says tenant_id column NO; admin-only audit of a global table is intentional.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without constraining by tenant_id (reads experiments across all tenants).
  - _Why false positive:_ platform-wide maintenance sweep intentionally scans running experiments across tenants and then updates each row with its own row-carried tenant_id.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting() reads rows WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER without tenant_id constraint (scans all tenants).
  - _Why false positive:_ durability sweep intentionally scans pending ai_insights across tenants and processes each row using its own tenant_id without returning mixed tenant data.
- **server/agentic-engines.ts:344** _(global-table)_ — SELECT COUNT from `heartbeat_logs` (heartbeat/tool calls) is executed without a tenant_id filter (FROM heartbeat_logs WHERE task_name != 'Self-Reflection'), reading cross-tenant telemetry.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column NO; query reads global telemetry by design.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT recent heartbeat_logs (status='success') in runPredictiveEngine() is executed without tenant scoping (reads across tenants).
  - _Why false positive:_ heartbeat_logs has no tenant_id and is a global telemetry table, so recent log read is from a non-tenant-scoped table.
- **server/agentic-engines.ts:519** _(global-table)_ — SELECT heartbeat performance (FROM heartbeat_logs GROUP BY task_name, status) executes without tenant_id constraint and will aggregate logs across tenants.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column NO; aggregate performance query is on intentional platform-global telemetry.
- **server/agentic-features.ts:638** _(upstream-verified)_ — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' — missing tenant filter, so enrollment existence is checked across tenants.
  - _Why false positive:_ sequence ownership is verified immediately above via outreach_sequences id+tenant_id; duplicate check is scoped to that tenant-owned sequence
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Global SELECT on tenant-scoped table without tenant filter: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This reads across all tenants and violates the invariant that every read against a tenant-scoped table must include a tenant_id constraint.
  - _Why false positive:_ heartbeat sweep intentionally iterates all tenants, processes each row with its own tenant_id, and only emits per-tenant events/aggregate counts
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — Raw SELECT across tenant-scoped tables (agent_runs / agent_approvals) with no tenant_id filter. The query selects rows from agent_runs and agent_approvals across all tenants: `SELECT ... FROM agent_runs r JOIN agent_approvals a ... WHERE r.state ? 'pendingApprovalId' ...`
  - _Why false positive:_ worker scans pending approvals across tenants but carries row tenant_id forward and resumes each run individually; no mixed tenant output returned
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE/SELECT claims due wake rows across all tenants without constraining by tenant_id. The inner SELECT used to choose ids for the UPDATE does not include a tenant_id = <tenant> filter, allowing cross-tenant rows to be locked/processed by this job.
  - _Why false positive:_ heartbeat job claims due rows platform-wide, then emits and updates each row using its own tenant_id without combining tenant data into one response
- **server/auto-memorize.ts:163** _(system-sweep)_ — SELECT from messages (pull recent messages window) has no tenant_id WHERE clause — it reads messages across all tenants into a single result set.
  - _Why false positive:_ code explicitly performs a platform-wide sweep then partitions rows by tenant before any LLM synthesis so no prompt mixes tenants
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. Tenant-scoped tables must have tenant_id set on INSERT to enforce isolation; here invoice_items is inserted with only invoice_id and other columns, leaving tenant attribution ambiguous or relying on FK behavior which is not guaranteed by the invariant.
  - _Why false positive:_ invoice_items has no tenant_id column and is a child table isolated through tenant-scoped invoices via invoice_id
- **server/channel-routing.ts:31** _(global-table)_ — listChannelRoutes selects from channel_routes and joins personas (p) without any tenant filter. If personas (or channel_routes) are tenant-scoped, this leaks/returns other tenants' personas and routes.
  - _Why false positive:_ channel_routes is created here without tenant_id and uses UNIQUE(channel), indicating intentional platform-global routing
- **server/channel-routing.ts:64** _(global-table)_ — getPersonaForChannel SELECTs persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true with no tenant constraint, returning persona ids from other tenants if the table is not tenant-scoped.
  - _Why false positive:_ channel_routes appears intentionally global/non-tenant-scoped, so lookup by channel without tenant filter matches schema/design
- **server/commitments.ts:135** _(system-sweep)_ — scanAndEscalate() issues a SELECT from the tenant-scoped commitments table without any WHERE tenant_id = ... filter (returns rows across all tenants).
  - _Why false positive:_ Platform-wide overdue scanner processes rows with row-carried tenant_id and emits redacted admin digest only, not tenant-facing mixed content.
- **server/data-protection.ts:404** _(upstream-verified)_ — backupTenantDataToDrive executes SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} without scoping by tenant_id. messages is tenant-scoped elsewhere — this read could surface another tenant's rows if conversation_id collisions or FK anomalies exist.
  - _Why false positive:_ Conversation ids are first selected from conversations scoped by tenant_id, so conv.id is already tenant-owned in this loop.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake() updates dfy_intake_forms (.update(...).where(eq(dfyIntakeForms.id, form.id))) without scoping the UPDATE to tenant_id. An attacker could reuse an id to update another tenant's form if ids collide or are leaked.
  - _Why false positive:_ UPDATE uses form.id obtained immediately from prior token lookup of that exact row in same call path.
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings() updates doc_chunks setting embedding using WHERE id = ${row.id} with NO tenant_id constraint. The prior SELECT filtered by collection_id and tenant_id, but the subsequent UPDATE uses only id and could modify rows of other tenants if an id is leaked/misused.
  - _Why false positive:_ Updated ids come from prior SELECT constrained by collection_id and tenant_id in same function.
- **server/email.ts:515** _(system-sweep)_ — backfillEmptyBodies() SELECTs inbox_messages rows (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id) with NO tenant_id filter, then fetches provider content and updates rows by id. This reads/updates rows across all tenants without tenant scoping.
  - _Why false positive:_ Background backfill intentionally sweeps all tenants and only fills housekeeping body fields per row; no tenant-facing mixed output.
- **server/embeddings.ts:752** _(system-sweep)_ — SELECT FROM tenant-scoped table agent_knowledge (backfillMissingKnowledgeEmbeddings) with no tenant_id filter — reads unscoped rows across all tenants.
  - _Why false positive:_ Backfill is a platform-wide maintenance job updating embedding fields only; no tenant-facing data return or mixed content output.
- **server/embeddings.ts:780** _(system-sweep)_ — SELECT FROM tenant-scoped table memory_entries (backfillEmbeddingVecs) with no tenant_id filter — reads unscoped rows across all tenants.
  - _Why false positive:_ Boot/maintenance backfill scans all tenants and only updates housekeeping embedding_vec on each row by its own id; no tenant-facing mixed output.
- **server/embeddings.ts:796** _(system-sweep)_ — SELECT FROM tenant-scoped table agent_knowledge (backfillEmbeddingVecs) with no tenant_id filter — reads unscoped rows across all tenants.
  - _Why false positive:_ Platform-wide embedding backfill over expired/live knowledge rows is maintenance-only and mutates per-row vector state without returning cross-tenant content.
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' is executed with no tenant_id filter (reads rows across all tenants).
  - _Why false positive:_ Boot-time rehydration intentionally scans running/pending agent_runs across tenants and rebuilds per-conversation in-memory state; maintenance behavior, not tenant-facing aggregation.
- **server/firecrawl.ts:214** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) with no tenant_id constraint.
  - _Why false positive:_ The row id being updated comes from an immediately prior SELECT scoped by url and tenantId, so the chosen scraped_pages row was already tenant-verified in the same call path.
- **server/firecrawl.ts:344** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) with no tenant_id constraint inside the crawl loop.
  - _Why false positive:_ Inside the crawl loop, existing[0].id is obtained from a SELECT constrained by tenantId, so the subsequent UPDATE by id is operating on a row already ownership-checked upstream.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' is performed by project id only (where(eq(projects.id, projectId))) without scoping to tenant_id before using the row.
  - _Why false positive:_ Although the SELECT is by project id, the next lines explicitly compare project.tenantId to caller tenantId and throw on mismatch before trusting or using the row.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl using only projects.id in the WHERE clause (where(eq(projects.id, projectId))) with no tenant_id constraint.
  - _Why false positive:_ The UPDATE occurs only after same-function ownership validation that project.tenantId matches tenantId; projectId was verified earlier in the call path.
- **server/google-drive.ts:826** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' during uploadAndShare is done by project id only (where(eq(projects.id, params.projectId))) without an explicit tenant_id WHERE clause prior to trusting the row.
  - _Why false positive:_ The function immediately validates proj.tenantId against params.tenantId and aborts on mismatch before proceeding, then passes the tenant-checked values onward.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from research_schedules has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants without an explicit tenant filter.
  - _Why false positive:_ Heartbeat schedule poll is a platform-wide cron/maintenance sweep; each row carries its own tenant_id and downstream work is launched per-row/per-tenant rather than mixing tenant data.
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id constraint on the UPDATE. An attacker or bug could cause a row belonging to another tenant with the same id to be modified.
  - _Why false positive:_ Heartbeat is a platform-wide scheduler sweep updating only housekeeping fields on the row being processed, carrying sched.tenant_id per row and not returning mixed tenant data.
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Platform-wide heartbeat maintenance updates only last_run_at/next_run_at for the currently processed schedule row; housekeeping-only sweep with per-row tenant context.
- **server/heartbeat.ts:121** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id constraint on the update after an error recovery flow.
  - _Why false positive:_ Error-recovery path in the same heartbeat sweep only advances next_run_at on the current schedule row; housekeeping mutation only, no tenant data exposure/mixing.
- **server/heartbeat.ts:385** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... (no tenant_id constraint). This pulls pending events for all tenants into a single tick and relies on later per-row tenant checks rather than scoping the initial query.
  - _Why false positive:_ This is an intentional cross-tenant pending-event sweep; each row is processed with its own event.tenant_id and not combined into one tenant-facing output.
- **server/heartbeat.ts:273** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — direct UPDATE on a tenant-scoped table without tenant_id in WHERE.
  - _Why false positive:_ Startup heartbeat cleanup is a platform-wide maintenance pass normalizing task model values; housekeeping/system mutation only, not user-request driven or tenant data mixing.
- **server/heartbeat.ts:277** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — modifying heartbeat_tasks by id only, no tenant constraint.
  - _Why false positive:_ Same startup maintenance sweep, updating only model normalization on each task row; intentional platform-wide housekeeping without mixed tenant outputs.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE on procedure_evolution_runs uses only the run id in the WHERE clause (`WHERE id = runId`) and omits any tenant_id predicate. The table is tenant-scoped (rows carry tenantId), so these updates can affect another tenant's row if runId is not bound to the caller's tenant.
  - _Why false positive:_ runId is a server-generated primary key returned from the immediately preceding insert in the same function, then used for internal status updates; not sourced from an untrusted request boundary.
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE on procedure_evolution_runs uses only the run id in the WHERE clause (`WHERE id = runId`) and omits tenant scoping. This is used to mark a run done/noop and can update a run belonging to another tenant.
  - _Why false positive:_ This update uses the same server-generated runId created earlier in the function for internal state transition of that run row only.
- **server/lib/aevo-meta-editor.ts:500** _(internal-worker-pk)_ — UPDATE on procedure_evolution_runs uses only the run id in the WHERE clause (`WHERE id = runId`) when marking the run done, without verifying tenant_id. This allows cross-tenant state changes by run id alone.
  - _Why false positive:_ Marking the run done uses the same locally created server-side runId, matching the internal-worker-pk benign pattern rather than a cross-tenant request-driven mutation.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped table `projects` without a tenant_id constraint (grouped across all tenants). This performs a cross-tenant read (tenant_id, count(*) group) and can leak other tenants' data.
  - _Why false positive:_ Boot/backfill maintenance code intentionally scans counts across tenants and only uses aggregate per-tenant counts to decide backfill; no tenant content is returned or mixed into one tenant response.
- **server/lib/inbox-ingest.ts:506** _(upstream-verified)_ — UPDATE inbox_classifications executed without constraining by tenant_id (WHERE inbox_message_id = ${storeResult.id} only). This writes to a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ The row was just claimed/inserted for this same storeResult.id with tenant_id = tenantId and inbox_classifications is one-row-per inbox_message_id via ON CONFLICT, so the follow-on update targets that owned row.
- **server/lib/mention-injection.ts:121** _(fk-transitive)_ — SELECT in fetchRadioMessages LEFT JOINs personas p ON p.id = cm.from_persona_id but does not constrain p.tenant_id. This reads the tenant-scoped personas table without a tenant filter and can surface another tenant's persona data.
  - _Why false positive:_ Schema note says personas has no tenant_id; joined persona is referenced from tenant-scoped channel_messages and isolated transitively through that relationship.
- **server/martech-bundle.ts:325** _(upstream-verified)_ — UPDATE on tenantVoiceProfiles uses WHERE id = existing.id but does NOT include an explicit tenant_id constraint. Application-level tenant isolation requires writes to tenant-scoped tables to also constrain by tenant_id.
  - _Why false positive:_ existing is fetched immediately above via getVoiceProfile({ tenantId: opts.tenantId, profileName }), so the update-by-id is on a row already tenant-scoped in the same call path.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = ... WHERE id = ${catId} does not include a tenant_id constraint. This updates a tenant-scoped table using only the id column, lacking the required tenant_id filter.
  - _Why false positive:_ catId comes from ensureCategory(topLevel, tenantId, ...) which selects/inserts memory_categories under that tenant in the same call path before this increment.
- **server/memory-graph.ts:239** _(fk-transitive)_ — Existence check SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) ... LIMIT 1 lacks any tenant scoping. Checking/reading link rows without verifying tenant can allow cross-tenant information leakage or cross-tenant linking.
  - _Why false positive:_ memory_links has no tenant_id and both endpoints are chosen from tenant-scoped memory_entries already filtered to tenantId, so existence check is transitively scoped by referenced parent rows.
- **server/memory-graph.ts:252** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) does not include tenant_id (and the INSERT lacks any tenant scoping). If memory_links is tenant-scoped or is used to relate tenant-scoped rows, inserting without tenant context can create cross-tenant links or orphaned relations.
  - _Why false positive:_ memory_links has no tenant_id and insert uses newMemoryId plus target.id drawn from tenant-scoped memory_entries for the same tenant, so omission of tenant_id is by design.
- **server/memory-graph.ts:494** _(upstream-verified)_ — categorizeExistingMemories: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} omits a tenant_id constraint. Updating memory_entries by id only (without tenant filter) risks modifying rows from other tenants if IDs are abused or misbound.
  - _Why false positive:_ mem.id comes from the immediately preceding SELECT over memory_entries WHERE tenant_id = tenantId, so this update operates on rows already ownership-filtered in the same loop.
- **server/minds-engine.ts:327** _(upstream-verified)_ — UPDATE on tenant-scoped table `mind_events` uses an interpolated array literal of caller-supplied `eventIds`: `WHERE id = ANY({...}::int[]) AND tenant_id = ${tenantId}`. Constructing the array via string-join on request data is string-interpolated SQL and can lead to SQL-injection or other risks.
  - _Why false positive:_ Query updates tenant-scoped mind_events with explicit tenant_id = tenantId; issue described is SQL injection, not cross-tenant isolation bypass on this line.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant filter. validateSubscriptionsOnStartup queries all active oauth_subscriptions (WHERE is_active = TRUE) across tenants.
  - _Why false positive:_ Startup validation intentionally scans all active oauth_subscriptions platform-wide and then handles each row with its own tenant_id; no mixed tenant output shown.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id constraint — it reads rows across all tenants.
  - _Why false positive:_ Boot-time auto-reconnect enumerates tenant_ids across oauth_subscriptions for maintenance and processes each tenant separately via connectGoogleViaReplit(tid).
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh selects FROM oauth_subscriptions WHERE is_active = TRUE without restricting to a specific tenant, iterating over all tenant rows without per-tenant scoping.
  - _Why false positive:_ proactiveTokenRefresh is a global housekeeping loop over active subscriptions; each row carries tenant_id and refreshes are executed per-tenant.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions reads oauth_subscriptions WHERE is_active = TRUE without tenant constraint, iterating across all tenants in a single call.
  - _Why false positive:_ forceRefreshAllSubscriptions is an explicit cron-style global maintenance pass and uses each row's tenant_id independently without cross-tenant data return.
- **server/pace-control.ts:85** _(global-table)_ — SELECT from heartbeat_logs (COUNT(*)) has no tenant filter. checkPace queries heartbeat_logs in a sliding window without WHERE tenant_id = <tenant>, so it counts runs across tenants.
  - _Why false positive:_ heartbeat_logs is marked non-tenant-scoped in schema and used for platform-global pace throttling metrics by design.
- **server/pace-control.ts:147** _(global-table)_ — getPaceSnapshot queries heartbeat_logs (per-persona aggregation) without tenant filter — it returns global data across tenants rather than per-tenant stats.
  - _Why false positive:_ heartbeat_logs has no tenant_id column per schema; snapshot reads from a platform-global log table rather than a tenant-scoped table.
- **server/process-governor.ts:199** _(global-table)_ — response_time: SELECT AVG(duration_ms) FROM heartbeat_logs ... lacks tenant_id constraint — aggregates logs across tenants.
  - _Why false positive:_ heartbeat_logs is non-tenant-scoped per schema, so this aggregate is over a global operational log table.
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures: SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE ... has no tenant_id filter — reads other tenants' heartbeat logs.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is treated as a platform-global log source for failure detection.
- **server/project-brain.ts:349** _(system-sweep)_ — convRes: SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant_id — reads conversations across tenants.
  - _Why false positive:_ Boot-time backfill/maintenance loop intentionally scans all projects and processes each project/conversation separately; no tenant-facing mixed output.
- **server/project-brain.ts:359** _(system-sweep)_ — msgRes: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ORDER BY created_at ASC omits tenant_id — may retrieve messages from other tenants' conversations with the same id.
  - _Why false positive:_ Part of the same cross-tenant backfill sweep; messages are handled per conversation for offline brain generation, not returned across tenants in one response.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT FROM code_proposals WHERE id = ${proposalId} has no tenant_id constraint — may read proposals belonging to other tenants.
  - _Why false positive:_ Server-side verifier loads a proposal by globally unique proposalId argument for internal processing, not from a shown tenant request boundary.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table memory_entries omits tenant_id in WHERE clause (UPDATE memory_entries SET ... WHERE id = ${storedId}). Although the id came from a prior tenant-scoped SELECT, the UPDATE is not constrained to tenant_id and could be abused if ids are guessed or reused.
  - _Why false positive:_ storedId comes directly from the immediately preceding tenant-scoped SELECT on memory_entries for input.tenantId/personaId.
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge executed with no tenant_id constraint — deletes entries across all tenants (fire-and-forget).
  - _Why false positive:_ Global expiry housekeeping delete of expired autoresearch knowledge; maintenance-only mutation with no tenant data returned or mixed.
- **server/research-engine.ts:496** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. If project_notes is tenant-scoped (tenant_id NOT NULL), this will create entries not attributed to the caller tenant or fail depending on schema.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively through verified project_id after assertProjectInTenant.
- **server/research-engine.ts:504** _(fk-transitive)_ — Second INSERT into project_notes inside findings loop similarly omits tenant_id, risking cross-tenant data writes or misattribution.
  - _Why false positive:_ project_notes has no tenant_id column and inserts are tied to a project already verified to belong to session.tenantId.
- **server/routes.ts:4338** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code executes: INSERT INTO project_notes (project_id, note, author) VALUES (...) without setting tenant_id, violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and note is attached via project_id obtained from tenant-scoped conversation lookup.
- **server/routes/admin-zombie-detector.ts:38** _(system-sweep)_ — Query reads from agent_trace_spans (CTE 'used') without constraining by tenant_id. This can return cross-tenant trace data because there is no WHERE tenant_id = <caller tenant> filter in the CTE.
  - _Why false positive:_ Explicitly admin-only platform-wide analytics route; cross-tenant aggregate last-used counts are intentional and not tenant-scoped user data exposure.
- **server/routes/conversations.ts:96** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` has no tenant_id filter (WHERE id = ${conv.id} only). This write can affect rows outside the caller's tenant if IDs are guessed or mis-bound.
  - _Why false positive:_ conv.id was just created by storage.createConversation with tenantId, and project ownership is checked with WHERE tenant_id = tenantId before update.
- **server/routes/conversations.ts:101** _(fk-transitive)_ — INSERT into `project_conversations` does not set tenant ownership (no tenant_id column/value). Inserting a row that links project↔conversation without tenant scoping can create cross-tenant associations.
  - _Why false positive:_ project_conversations has no tenant_id and both project and conversation were tenant-verified via parent rows before insert
- **server/routes/conversations.ts:103** _(upstream-verified)_ — UPDATE on `projects` (updated_at) lacks a tenant_id filter (WHERE id = ${projectId} only). This allows updating a project belonging to another tenant if projectId is controlled or misvalidated.
  - _Why false positive:_ project ownership was checked just above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before the update
- **server/routes/conversations.ts:231** _(fk-transitive)_ — INSERT into `project_notes` (project_id, note, author) does not include tenant_id. Creating notes linked to a project without writing tenant ownership can allow notes to be associated across tenants.
  - _Why false positive:_ project_notes has no tenant_id and note is inserted only after joining conversation to a tenant-checked parent project
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_messages` performed without constraining by tenant_id (UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}). This allows modifying another tenant's row if an attacker supplies or guesses a message id.
  - _Why false positive:_ the same path first selected the message by id and tenant_id, then updates that retrieved row's id only
- **server/routes/projects.ts:33** _(fk-transitive)_ — SELECT against tenant-scoped tables in the projects list query uses subqueries that read project_files, project_notes, and project_conversations WITHOUT constraining those subqueries to the caller's tenant (no tenant_id filter).
  - _Why false positive:_ subqueries count child tables with no tenant_id, scoped transitively by outer tenant-filtered parent projects row p
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} executes without constraining to tenant_id; this updates project rows without verifying tenant ownership in the UPDATE itself.
  - _Why false positive:_ project was tenant-validated earlier in the same handler via SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} is executed without a tenant_id constraint (second occurrence).
  - _Why false positive:_ project was tenant-validated earlier in the same handler via SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:319** _(upstream-verified)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC selects from a tenant-scoped table (project_files) without a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ request first verifies the parent project belongs to tenantId; project_files is a child table without tenant_id keyed by project_id
- **server/routes/projects.ts:342** _(upstream-verified)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${'uploads/' + filename} LIMIT 1 reads from project_files without constraining tenant_id.
  - _Why false positive:_ download route verifies the parent project with tenant_id before reading child project_files row by that project_id
- **server/routes/projects.ts:371** _(upstream-verified)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} deletes from a tenant-scoped table without including tenant_id in the WHERE clause.
  - _Why false positive:_ delete route first confirms the parent project belongs to tenantId; project_files is a child table scoped by project_id
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT from tenant-scoped table heartbeat_tasks lacks tenant_id constraint (SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'). Reads against tenant-scoped tables must be scoped to a tenant.
  - _Why false positive:_ boot/seed housekeeping intentionally scans and mutates heartbeat task status globally without returning tenant data
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT from tenant-scoped table heartbeat_tasks lacks tenant_id constraint (SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%'). Unscoped reads across tenants violate the tenant-isolation invariant.
  - _Why false positive:_ Boot-time cleanup globally counts/disables runaway heartbeat tasks and only mutates housekeeping fields, returning only aggregate count/logging.
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT from tenant-scoped table heartbeat_tasks lacks tenant_id constraint (SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ...)). This read is unscoped across tenants.
  - _Why false positive:_ Global maintenance sweep over heartbeat_tasks to disable orphaned task names; no tenant data returned, only aggregate housekeeping action.
- **server/seed.ts:36** _(system-sweep)_ — SELECT from tenant-scoped table 'research_sessions' without a tenant_id filter (SELECT COUNT(*) FROM research_sessions). This reads potentially across all tenants.
  - _Why false positive:_ Dev snapshot import uses global COUNT(*) only as boot-time seed/backfill gate and does not expose or mix tenant row data.
- **server/seed.ts:2158** _(system-sweep)_ — SELECT from tenant-scoped table 'research_programs' without a tenant_id filter (SELECT id, name, model FROM research_programs WHERE is_active = true). This enumerates programs across tenants.
  - _Why false positive:_ Seed migration scans active research programs platform-wide to normalize invalid model ids; per-row update by id and logs only program names, not tenant-facing data.
- **server/seed.ts:3881** _(system-sweep)_ — UPDATE memory_entries SET fact = REPLACE(...) executed without scoping by tenant_id. memory_entries has tenant_id (there is an index) but the UPDATE lacks a tenant_id constraint and will modify rows for all tenants.
  - _Why false positive:_ Global idempotent data-normalization migration updates memory_entries text in place across tenants without returning tenant data.
- **server/seed.ts:3907** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' is executed without a tenant_id filter. conversations is tenant-scoped (has tenant_id index) so this global UPDATE will change conversation rows across all tenants.
  - _Why false positive:_ Global backfill migration updates legacy model ids in conversations as housekeeping only; no cross-tenant data returned.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from heartbeat_logs aggregates error counts without any tenant_id filter, reading across all tenants' heartbeat logs.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id; cross-tenant tenant-scoping invariant does not apply to this global log table.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log runs without a tenant_id filter, which can delete event log rows across tenants.
  - _Why false positive:_ Watchdog cleanup deletes old processed/failed event_log rows as platform housekeeping and only reports aggregate count.
- **server/stability-watchdog.ts:259** _(global-table)_ — pruneOldLogs deletes rows from heartbeat_logs older than a threshold but does not include a tenant_id constraint, allowing cross-tenant removal of logs.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id, so global pruning is expected for this log table.
- **server/stability-watchdog.ts:154** _(global-table)_ — Call to storage.createHeartbeatLog(...) when clearing a stuck task omits any tenantId. If the underlying storage function inserts into a tenant-scoped table, this may create a row in the wrong tenant or fail to set tenant_id.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id; omitting tenantId on heartbeat log creation is expected for a global table.
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL without any tenant_id constraint — reading tenant-scoped messages rows unscoped.
  - _Why false positive:_ IDs come from msgs already loaded by tenant-scoped getMessages/getMessagesPaginated queries, and this helper only rehydrates those same rows by id.
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories runs raw SQL UPDATE memory_entries SET status = 'archived' WHERE ... with NO tenant_id constraint — this will archive rows across all tenants.
  - _Why false positive:_ Platform-wide housekeeping sweep that only archives expired memory rows and returns count/ids, with no tenant data returned or mixed.
- **server/storage.ts:858** _(system-sweep)_ — archiveStaleMemories runs raw SQL UPDATE memory_entries SET status = 'archived' WHERE ... with NO tenant_id constraint — it mutates memory_entries platform-wide.
  - _Why false positive:_ Platform-wide stale-row archival updates only status housekeeping fields and returns ids/count, not tenant content.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table 'custom_tools' without constraining to the caller's tenant (no tenant_id filter). This reads global rows and can cause cross-tenant discovery/collisions.
  - _Why false positive:_ Name is transformed with tenantToolName(parsed.name, tenantId) immediately before lookup, making the existence check tenant-qualified by the namespacing scheme in this call path.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table 'custom_tools' by id without constraining to the caller's tenant. Reads by id must also enforce tenant ownership to prevent cross-tenant disclosure.
  - _Why false positive:_ Reads back the exact row just inserted in the same flow using server-generated inserted.id, so ownership is established by the prior insert for this call path.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE of tenant-scoped table 'custom_tools' to increment usageCount uses only id in WHERE and lacks a tenant_id filter. This can alter usage counters across tenants.
  - _Why false positive:_ Row was selected immediately above with tenant_id = tenantId, and update uses that verified row's primary key.
- **server/tools.ts:4548** _(fk-transitive)_ — INSERT into project_notes executed without supplying tenant_id. Tenant-scoped tables must have tenant_id set on INSERT to enforce isolation; relying only on assertProjectInTenant is insufficient per the codebase invariant.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id after assertProjectInTenant.
- **server/tools.ts:4551** _(fk-transitive)_ — INSERT into project_files executed without supplying tenant_id. project_files appears to be tenant-scoped but the INSERT omits tenant_id (no NOT NULL tenant column set).
  - _Why false positive:_ project_files has no tenant_id and is isolated transitively through project_id after assertProjectInTenant.
- **server/tools.ts:6393** _(fk-transitive)_ — INSERT into project_files (from generate_audio) performed without tenant_id. Although assertProjectInTenant is checked, the INSERT does not include tenant_id, violating the rule that every write to tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and insert is correctly scoped through tenant-owned project_id validated by assertProjectInTenant.
- **server/tools.ts:6727** _(fk-transitive)_ — INSERT into project_files (from create_slideshow_video) performed without tenant_id. The row is written without an explicit tenant_id column despite project_files being tenant-scoped.
  - _Why false positive:_ This finding refers to create_slideshow_video project_files write; project_files has no tenant_id and is transitively scoped by validated project_id.
- **server/tools.ts:6927** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code executes a raw INSERT of (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without setting tenant_id, relying only on a prior assertProjectInTenant check.
  - _Why false positive:_ project_files has no tenant_id and insert is transitively isolated via project_id after assertProjectInTenant.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against browser_workflows omits tenant scoping — "UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}" has no tenant_id constraint; invariant requires tenant-scoped operations include the caller's tenant filter.
  - _Why false positive:_ workflow was fetched in the same path with id/name plus tenant_id = tenantId before updating by its id.
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE on projects omits tenant scoping — "UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}" lacks a tenant_id constraint. Without explicit tenant scoping this could affect a project id belonging to another tenant.
  - _Why false positive:_ projId comes from a prior SELECT joining projects with p.tenant_id = tenantId in the same call path.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table `knowledge_triples` executes without a tenant_id constraint (UPDATE ... WHERE id = ${row.id}). This mutates rows by id alone and violates the invariant that every write against tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ Rows being updated were selected just above from knowledge_triples with tenant_id = tenantId, then updated by those verified ids.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — Unscoped SELECT against tenant-scoped table `provider_keys` — query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` has no tenant_id filter and enumerates provider configuration across tenants.
  - _Why false positive:_ provider_keys schema note shows no tenant_id; query is additionally gated to admin tenant only.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} is executed without also constraining by tenant_id. Reading a tenant-scoped `conversations` row by id alone can expose cross-tenant information and violates the requirement that reads be filtered to the caller's tenant.
  - _Why false positive:_ This read is a verification step comparing the conversation row's tenant_id to the claimed tenant and fails closed on mismatch.
- **server/video-job-runner.ts:235** _(system-sweep)_ — recoverStaleVideoJobs updates tenant-scoped videoJobs rows without any tenant_id constraint — it will mark jobs across all tenants as failed.
  - _Why false positive:_ Periodic stale-job recovery is a platform-wide housekeeping sweep that only mutates status fields across tenants and does not expose mixed tenant data.
- **server/embeddings.ts:748** _(system-sweep)_ — UPDATE against tenant-scoped table (memory_entries|agent_knowledge) performed without constraining tenant_id. storeEmbeddingVec updates embedding_vec/embedding by id but does not check tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/plan-executor.ts:121** _(upstream-verified)_ — UPDATE plans (appendExecutionLog) omits tenant_id constraint — writes to a tenant-scoped table using only id. This can modify another tenant's plan row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): executePlan is reached via decidePlan; the route gates requirePlatformAdmin (server/routes/agentic-policy.ts:95) and getPlan(id, tenantId) verifies ownership at server/routes/agentic-policy.ts:113 before execution.
- **server/process-governor.ts:977** _(upstream-verified)_ — logAction: UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant_id filtering — will increment trigger_count across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/proposal-verifier.ts:108** _(internal-worker-pk)_ — Race-after-rebase UPDATE: UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} omits tenant_id — mutates a tenant-scoped proposals row without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist(): UPDATE code_proposals SET verification_status = ..., verified_at = NOW() WHERE id = ${proposalId} omits tenant_id — writes verification metadata without scoping to tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — attemptRebaseOnDrift: UPDATE code_proposals SET status='rejected', verification_status='skipped', ... WHERE id = ${proposalId} lacks tenant_id — mutates proposal status across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): persist() is reached only via verifyProposalById(proposalId). All callers pass internal ids: server/agentic/autonomous-closer.ts:101-103 uses ids from a tenant-scoped fetchCandidates query (autonomous-closer.ts:86-95, WHERE tenant_id = tenantId), and scripts/reverify-proposals.ts / scripts/drain-and-verify-proposals.ts are internal batch jobs. No req/tool-arg id.
- **server/sculptor.ts:363** _(upstream-verified)_ — UPDATE on tenant-scoped table sculptor_sessions lacks a tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). This write can update sessions across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger lacks a tenant_id constraint (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}). settleFusionReservation can modify ledger rows from other tenants if reservation ids are global.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger lacks a tenant_id constraint (UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId}). releaseFusionReservation can touch another tenant's ledger row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) in the WHERE clause (lines ~394-401 and the error-path update at ~413-415). Without also constraining by tenant_id this can modify another tenant's row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:1304** _(upstream-verified)_ — getAllDataForExport performs SELECTs from many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant_provider_keys, heartbeat_tasks, etc.) without any tenant_id constraints — this exposes or exports all tenants' data in one call.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'custom_tools' (updating implementation) constrained only by id and missing tenant_id in WHERE — allows modifying another tenant's row if ids collide or are guessed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table 'custom_tools' without a tenant_id constraint (WHERE eq(custom_tools.id, tool.id) only). This can delete another tenant's tool row if the id is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` executed without constraining by tenant_id (no WHERE tenant_id = ...). The code loads pairing rows across all tenants by title and relies on later application checks to validate ownership — this violates the invariant that every read against a tenant-scoped table must be tenant-filtered at SQL level.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/agent-channels.ts:198** _(upstream-verified)_ — JOIN reads from tenant-scoped table `channel_subscriptions` without constraining its tenant_id (the JOIN ON clause lacks cs.tenant_id = ${tenantId}).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): getUnreadCount filters cm.tenant_id = ${tenantId}; the channel_subscriptions join is keyed on the caller's persona_id (personas are tenant-scoped FKs), so another tenant's subscription rows cannot match.
- **server/agentic-engines.ts:534** _(global-table)_ — SELECT from `heartbeat_tasks` (taskConfig) is executed without tenant scoping (WHERE enabled = true but no tenant_id), reading task config across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... is executed without any tenant_id restriction. This enumerates conversation/project rows across tenants and then processes them, enabling cross-tenant reads and subsequent operations.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): backfillProjectAssets only caller is a one-time startup backfill routes.ts:824; it selects c.tenant_id (auto-asset-capture.ts:185) and passes each row's tenant to captureProjectAssets(c.id,c.tenant_id,...) at auto-asset-capture.ts:217 — row-carried tenant, no tenant-facing return.
- **server/auto-asset-capture.ts:207** _(system-sweep)_ — Selecting messages by conversation_id (SELECT content FROM messages WHERE conversation_id = ${c.id} ...) without scoping by tenant_id. Messages is tenant-scoped (or implicitly so via conversation), but the query lacks an explicit tenant filter; an attacker or bug could cause cross-tenant reads/writes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The message read is inside the per-conversation backfill loop; conversation_id=c.id (auto-asset-capture.ts:209) belongs to the row whose tenant_id was selected at line 185 and passed to captureProjectAssets at line 217. Maintenance sweep, not tenant-facing.
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from agent_knowledge (getWatermark) has no tenant_id constraint — reading watermark rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` performed without a tenant_id constraint. The query `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` can modify another tenant's row if `pending.reviewId` collides or is manipulated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/chat-engine.ts:4412** _(internal-worker-pk)_ — Call to storage.attachCitations(...) is made without supplying tenant context. If attachCitations performs INSERTs/updates on a tenant-scoped table (e.g. message_citations), those writes will lack an explicit tenant_id constraint and could violate tenant isolation (INSERT without tenant_id or UPDATE/SELECT without WHERE tenant_id = ...)
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/chat-engine.ts:4332 — assistantMsg.id is server-generated two lines above (chat-engine.ts:4329, created WITH tenantIdForScope); attachCitations updates only that fresh id — never caller input.
- **server/delivery-pipeline.ts:1071** _(global-table)_ — getDeliveryByStripePayment() SELECTs from delivery_logs by stripePaymentId with NO tenant_id filter. This can return another tenant's delivery row for a shared payment id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:50** _(signed-url)_ — getDfyIntakeByToken() SELECTs a dfy_intake_forms row by token with NO tenant_id constraint. Token-only lookup against a tenant-scoped table can return forms belonging to any tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/dfy-intake.ts:52 — dfy intake token is a 48-hex crypto.randomBytes capability token (dfy-intake.ts:32) validated by strict regex; the unguessable token IS the access control for the public intake form by design.
- **server/email.ts:470** _(system-sweep)_ — startInboxPolling()/storeEmail path checks for an existing inbox_messages row with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 without scoping by tenant_id. That may cause cross-tenant dedupe (skipping inserts for other tenants) or mixing data across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): platform primary-inbox poller (single system account); message_id dedupe against its own inbox rows.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images uses session_id returned from an internal presenter API without first verifying that the session belongs to the current tenant (no tenant_id check). This lets a caller persist image blobs against an arbitrary session_id (potential cross-tenant write).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/google-workspace.ts:2364** _(fk-transitive)_ — Second INSERT into presenter_slide_images (thumbnail row) writes image data keyed by session_id without validating that the session_id is owned by the caller's tenant. Same cross-tenant write risk as above.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} performs a write to a tenant-scoped resource (presenter_sessions) without constraining by tenant_id or otherwise verifying the session belongs to the caller's tenant. This can allow updating another tenant's session.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/heartbeat.ts:2764** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint when setting approval_status on a newly-created task.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3216** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — direct DB UPDATE without tenant_id constraint in delegateTaskFromChat flow.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/lib/bwb-job-progress.ts:74** _(internal-worker-pk)_ — scope(jobId, ...) builds a WHERE clause that may omit tenant scoping when BWB_TENANT_ID is missing, causing subsequent UPDATE/SELECTs that use scope(jobId) to execute without a tenant_id predicate on the tenant-scoped video_jobs table. Several callers (setBwbPhase, updateBwbChapters, bumpBwbHeartbeat, completeBwbJob, failBwbJob) rely on scope() and can therefore run unscoped when the tenant env var is not present.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): scope() always pins the unguessable server-generated jobId (vj_<base36>_<6-byte-hex>, newBwbJobId at bwb-job-progress.ts:44-46, validated by JOB_ID_RE at :28,50); jobId comes only from BWB_JOB_ID env/explicit args threaded through the spawn chain, never a request. Tenant predicate is defense-in-depth, and a random-jobId collision is not reachable from an untrusted boundary.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — SELECT from mcp_api_keys looks up by key_prefix only (no tenant_id in WHERE). This reads a tenant-scoped table without an explicit tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/mcp-api-keys.ts:166 — verifyApiKey compares the sha256 of the FULL presented key against key_hash in the same function (mcp-api-keys.ts:163-180); the credential itself is the access control and selects the tenant.
- **server/memory-graph.ts:183** _(upstream-verified)_ — refreshCategoryCount: UPDATE memory_categories ... WHERE id = ${categoryId} lacks a tenant_id constraint. Tenant-scoped updates must filter by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/minds-engine.ts:214** _(upstream-verified)_ — INSERT into tenant-scoped table `mind_tickets` builds the depends_on array by string-joining a request-derived value (`params.dependsOn`) into a SQL array literal (`{...}`) in the interpolated SQL. This is string-interpolated SQL using caller-provided data (injection + isolation risk).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): dependsOn is typed number[] (MindTicket contract) so join() yields a numeric literal; tenant_id is a bound param in the same INSERT.
- **server/process-governor.ts:155** _(global-table)_ — Subquery in desk_status computes pending_tasks with SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true — missing tenant_id filter, so task counts may include other tenants' tasks.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): heartbeat-task pace counting is a platform-wide throttle by design (same family as checkPace/heartbeat_logs); per-tenant fairness would need a schema migration and is tracked separately.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} omits tenant_id — can read project data belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — backfillProjectBrains: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 has no tenant filtering — enumerates projects/conversations/messages across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/routes.ts:2143** _(upstream-verified)_ — POST /api/presenter inserts into tenant-scoped table `presenter_sessions` using tenantId taken directly from request body and does not verify caller's ownership or that the tenantId is allowed. This lets a caller supply arbitrary tenant_id values when creating sessions.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): routes.ts:2141 presenter_sessions INSERT is gated by a constant-time SESSION_SECRET internal-key check at routes.ts:2132-2133 (403 on mismatch); req.body is therefore a trusted server-to-server boundary, not an untrusted caller
- **server/self-improvement.ts:170** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` executed without a tenant_id constraint when tenantId is not provided. The query (db.select().from(experiments).where(...)) will return cross-tenant rows if detectStagnation is called with no tenantId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` in getExperimentHistory falls back to an unconditional db.select().from(experiments) when no tenantId is supplied, returning data across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:242** _(upstream-verified)_ — getConversationUnscoped performs a SELECT FROM conversations WHERE id = ${id} with NO tenant_id constraint (unscoped read of a tenant-scoped table).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:398** _(internal-worker-pk)_ — attachCitations executes UPDATE messages SET citations = ... WHERE id = ${messageId} with NO tenant_id constraint — mutating a tenant-scoped table without tenant scoping or tenant_id in the UPDATE.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:309** _(upstream-verified)_ — createMessage looks up the referenced conversation by id using a SELECT FROM conversations WHERE id = ${data.conversationId} with NO tenant filter and derives tenant from that row. Caller-provided conversation_id is a foreign id; the referenced row is read without verifying caller ownership/context.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): createMessage lookup only reads conversations.tenantId (int) and the message is stamped with the conversation's real tenant (storage.ts:325); a caller-supplied tenantId that mismatches throws (storage.ts:320-323). No cross-tenant data returned or wrong-tenant write.
- **server/storage.ts:805** _(internal-worker-pk)_ — updateMemoryEmbedding executes db.update(memory_entries).set({ embedding }).where(eq(memory_entries.id, id)) without any tenant_id constraint — updating tenant-scoped rows by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding executes db.update(agent_knowledge).set({ embedding }).where(eq(agent_knowledge.id, id)) with NO tenant_id constraint — mutating tenant-scoped agent_knowledge rows unscoped.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:513** _(system-sweep)_ — getAllMemoriesForBackup selects memory_entries (memoryEntrySafeCols) across the entire table with no tenant filter — returns memories for all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — stampArgsEmbedding performs UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL with NO tenant_id constraint — mutating a tenant-scoped table without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/watchlist.ts:111** _(upstream-verified)_ — getAlerts LEFT JOINs watchlist_items (wi) by id but does not constrain wi.tenant_id. A watchlist_alert (wa) filtered to the tenant can JOIN to a watchlist_items row with the same id from a different tenant, leaking another tenant's item name/category in the response.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The LEFT JOIN filters wa.tenant_id=tenantId (server/watchlist.ts:111); because createAlert only ever stores same-tenant watchlist_item_id (proven above via server/watchlist.ts:178-186 + 69-83), the joined watchlist_items rows are always same-tenant. No exploit path exists to insert a foreign item id.
- **server/delivery-pipeline.ts:203** _(deferred)_ — updateDeliveryLog() performs an UPDATE on delivery_logs using only the delivery id (WHERE eq(deliveryLogs.id, id)) with NO tenant_id constraint. This helper is called from many places (e.g. to change status, add email flags), so it can update/overwrite another tenant's delivery row by id.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
