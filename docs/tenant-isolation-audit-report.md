# Tenant-Isolation Audit Report

_Generated 2026-09-23T10:32:14.784Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **695/695** file(s) audited · chunks 64/64 OK (0 failed)
- Findings: **267** (CRITICAL 17 · HIGH 230 · MEDIUM 17 · LOW 3)
- Severe dispositions: **47 kept** · 169 precision false-positive · 31 allowlist false-positive · 0 deferred real risk

### CRITICAL (17)

- **server/agentic-features.ts:842** — INSERT into tenant-scoped outreach_sequence_steps does not set tenant_id. This violates the invariant that every insert into tenant-scoped tables must explicitly write tenantId.
  - _Fix:_ Include tenant_id = params.tenantId on each inserted outreach_sequence_steps row, and ensure the schema requires it.
- **server/embeddings.ts:855** — UPDATE against tenant-scoped table `memory_entries` in `backfillEmbeddingVecs()` uses only `WHERE id = ${row.id}` with no `tenant_id` constraint.
  - _Fix:_ Include `AND tenant_id = ${row.tenant_id}` in the UPDATE and select `tenant_id` in the preceding query so writes remain tenant-scoped.
- **server/embeddings.ts:872** — UPDATE against tenant-scoped table `agent_knowledge` in `backfillEmbeddingVecs()` uses only `WHERE id = ${row.id}` with no `tenant_id` constraint.
  - _Fix:_ Include `AND tenant_id = ${row.tenant_id}` in the UPDATE and select `tenant_id` in the preceding query.
- **server/google-workspace.ts:2287** — INSERT into tenant-scoped table `presenter_slide_images` does not set `tenant_id`. In this codebase tenant isolation relies on explicit tenant_id on every insert, so these rows can be orphaned or mis-scoped.
  - _Fix:_ Include `tenant_id` in the INSERT values using the trusted `tenantId` from function context, e.g. `INSERT INTO presenter_slide_images (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)`.
- **server/google-workspace.ts:2293** — Second INSERT into tenant-scoped table `presenter_slide_images` (thumb quality) also omits `tenant_id`. This violates the invariant that every insert into a tenant-scoped table must set tenant_id explicitly.
  - _Fix:_ Add `tenant_id` to this INSERT as well, sourced from the trusted `tenantId` argument, and ensure any unique constraints/upserts include tenant scoping if needed.
- **server/job-queue.ts:159** — claimDueJobs updates and returns rows from tenant-scoped table agent_jobs with no tenant_id constraint. This background worker can claim jobs across all tenants without an explicit tenant filter, violating the invariant that every read/write on tenant-scoped tables must be constrained by tenant_id.
  - _Fix:_ Add an explicit tenant scope parameter to claimDueJobs and include `AND tenant_id = <scope>` (or an equivalent approved all-tenants admin pathway if this table is intentionally cross-tenant operational data). If agent_jobs is truly tenant-scoped, every claim/read/update path must carry tenant_id explicitly.
- **server/job-queue.ts:195** — reclaimExpiredLeases performs UPDATEs on tenant-scoped table agent_jobs with no tenant_id constraint in either query, mutating jobs across all tenants without explicit tenant scoping.
  - _Fix:_ Require an explicit tenant scope for lease reclamation and add `AND tenant_id = <scope>` to both UPDATE statements, or document/reclassify agent_jobs as a non-tenant-scoped operational table if that is truly intended.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor reads from tenant-scoped table agent_knowledge across all tenants with no tenant_id filter. The invariant requires every read/write on tenant-scoped tables to include tenant_id = caller tenant.
  - _Fix:_ Refactor to iterate from an explicit trusted tenant list and query one tenant at a time with `WHERE tenant_id = <tenantId>`, or reclassify this workflow behind an explicit admin/all-tenants pathway if cross-tenant operational scans are intended.
- **server/memory-graph.ts:275** — INSERT into tenant-scoped table memory_links does not set tenant_id. The invariant requires every tenant-scoped insert to pass tenantId explicitly.
  - _Fix:_ Add `tenant_id` to the INSERT values for memory_links and ensure the table schema enforces NOT NULL tenant ownership.
- **server/persona-export.ts:33** — SELECT from tenant-scoped table `personas` by `id` only, with no `tenant_id` constraint. A caller who knows another tenant's personaId could export that persona's data.
  - _Fix:_ Constrain the lookup with tenant ownership, e.g. `SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}` before using the row.
- **server/recurring-messages.ts:307** — UPDATE against tenant-scoped table `agent_knowledge` uses only `WHERE id = ${r.id}` with no `tenant_id` constraint. The runner updates scheduled-message rows without enforcing tenant isolation.
  - _Fix:_ Add `AND tenant_id = ${r.tenant_id}` to the UPDATE so each write remains constrained to the owning tenant.
- **server/routes/doc-collections.ts:330** — Chunked upload finalization takes uploadId from request body and assembles the upload without verifying the upload belongs to the caller's tenant before consuming it. A tenant that learns another tenant's uploadId could finalize and read/ingest that other tenant's staged file.
  - _Fix:_ Before calling assembleChunkedFile(uploadId), load the upload record from chunkedUploads, verify upload.tenantId === tenantId, and reject on mismatch. Ideally make assembleChunkedFile accept caller tenantId and enforce ownership internally.
- **server/routes/doc-collections.ts:351** — Chunked knowledge upload finalization uses request-supplied uploadId without checking that the staged upload belongs to the caller's tenant before assembling it. This allows cross-tenant consumption of another tenant's uploaded file if the ID is known.
  - _Fix:_ Verify the upload map entry exists and its tenantId matches the authenticated tenant before assembling; move this ownership check into a shared helper used by all chunked-upload finalize routes.
- **server/routes/doc-collections.ts:393** — Chunked memory upload finalization uses request-supplied uploadId without validating tenant ownership of the staged upload before assembling it. This is a cross-tenant object reference vulnerability.
  - _Fix:_ Require an ownership check on the upload map entry (upload.tenantId === tenantId) before assembly, and enforce it inside assembleChunkedFile or a wrapper to avoid missing it on sibling routes.
- **server/skills-marketplace.ts:235** — SELECT against tenant-scoped skills table uses caller-supplied skillId with no tenant ownership verification. This allows exporting another tenant's skill by id.
  - _Fix:_ Require caller tenantId and query skills with both id = skillId AND tenant_id = <caller tenant>, or first verify ownership before export.
- **server/storage.ts:937** — UPDATE against tenant-scoped table `memory_entries` sets embeddings by id only, with no tenant_id constraint. Any caller with a foreign memory id can modify another tenant's row.
  - _Fix:_ Require tenantId for `updateMemoryEmbedding` and scope the mutation with `WHERE id = <id> AND tenant_id = <tenantId>`.
- **server/tools/domains/cmmc-prospecting/handlers.ts:157** — INSERT into tenant-scoped table project_files does not set tenant_id explicitly. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly because columns are NOT NULL with no default and isolation is application-enforced.
  - _Fix:_ Include `tenant_id` in the INSERT column list and values, sourced from trusted context: `INSERT INTO project_files (tenant_id, project_id, ...) SELECT ${ctx.tenantId}, ${ctx.projectId}, ... WHERE EXISTS (...)`.

### HIGH (230)

- **server/agent-activity.ts:61** — SELECT from tenant-scoped personas table by personaId without a tenant_id constraint. A personaId from another tenant could be read and cached.
  - _Fix:_ Constrain the query with the caller tenant, e.g. include eq(personas.tenantId, tenantId), and pass tenantId into getPersonality().
- **server/agent-activity.ts:122** — UPDATE against tenant-scoped agentActivity uses only id with no tenant_id filter. If activityId is influenced by another tenant's row, this can modify cross-tenant data.
  - _Fix:_ Require tenantId in completeActivity() and update with where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))).
- **server/agent-desk.ts:297** — SELECT from tenant-scoped personas table has no tenant_id filter when building desk overview, exposing persona names across tenants.
  - _Fix:_ Filter personas by tenant_id = tenantId, or join only tenant-owned personas when building the personaMap.
- **server/agent-eval.ts:181** — Reads persona by foreign personaId without first verifying the caller tenant owns that persona. storage.getPersona(personaId) is called without tenant scoping.
  - _Fix:_ Load the persona with a tenant-scoped query (persona id + tenantId), or add a storage method that enforces tenant ownership before use.
- **server/agent-eval.ts:221** — UPDATE to agent_evals uses only id with no tenant_id constraint. If evalId were wrong or reused, this could update another tenant's eval row.
  - _Fix:_ Add tenant scoping to the WHERE clause: WHERE id = ${evalId} AND tenant_id = ${tenantId}.
- **server/agentic-engines.ts:816** — SELECT from tenant-scoped personas table has no tenant_id filter in runDecisionEngine, so active personas from other tenants may be read into this tenant's analysis prompt.
  - _Fix:_ Constrain the personas query with tenant_id = tenantId.
- **server/agentic-features.ts:448** — Subqueries against tenant-scoped competitor_snapshots and competitor_changes count rows only by competitor_id, without tenant_id constraints. If ids are ever cross-tenant referenced, counts and timestamps can leak other-tenant data.
  - _Fix:_ Add tenant_id = params.tenantId to each subquery in listCompetitors().
- **server/agentic-features.ts:862** — Reads foreign sequenceId from caller input and queries outreach_enrollments without tenant validation on the enrollment lookup. The duplicate-enrollment check filters only by sequence_id/contact_email/status, not tenant_id.
  - _Fix:_ Add tenant_id = params.tenantId to the existing-enrollment query, and keep the prior ownership check on outreach_sequences.
- **server/agentic-features.ts:916** — Reads outreach_sequence_steps by foreign sequence_id without tenant ownership verification and without tenant_id filter. A sequence step from another tenant could be selected if sequence_id is mismatched or untrusted.
  - _Fix:_ Verify the sequence belongs to the tenant before step lookup and query steps with tenant_id = params.tenantId (or join through a tenant-scoped parent).
- **server/agentic-features.ts:1020** — UPDATE against tenant-scoped outreach_enrollments uses only id with no tenant_id constraint. Although row was initially loaded tenant-scoped, the write itself is not tenant-scoped.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the UPDATE WHERE clause.
- **server/agentic/resume-worker.ts:65** — SELECT against tenant-scoped tables agent_runs and agent_approvals has no tenant_id filter in the WHERE clause, so the sweep reads approval/run state across all tenants.
  - _Fix:_ Constrain the candidate query by tenant_id for the caller/worker scope, or redesign the worker to iterate per tenant and add `WHERE r.tenant_id = <tenant>` (and equivalent constraints for joined tables).
- **server/agentic/resume-worker.ts:170** — UPDATE agent_runs is scoped only by run_id and tenant_id values previously read from an unscoped cross-tenant SELECT. Because the initial read spans all tenants, this handler operates on foreign-tenant runs without first establishing authorized tenant ownership.
  - _Fix:_ Only process runs after selecting them from a tenant-scoped query for the current tenant context, or explicitly validate worker authorization for the row's tenant before updating/failing/resuming it.
- **server/agentic/task-forces.ts:40** — INSERT into tenant-scoped table task_forces sets project_id directly from caller input (p.projectId) without first verifying that the referenced project belongs to the same tenant.
  - _Fix:_ Before inserting, load the project by id with a tenant filter (e.g. WHERE id = p.projectId AND tenant_id = p.tenantId) and reject if not found; only then persist project_id.
- **server/agentic/wake-scheduler.ts:66** — INSERT into tenant-scoped table agent_wake_schedules sets foreign ids persona_id, conversation_id, and project_id directly from caller input without verifying the caller's tenant owns those referenced rows.
  - _Fix:_ Before inserting, validate each provided foreign id against its table with tenant_id = p.tenantId (e.g. persona, conversation, project) and reject or clear invalid references.
- **server/agentic/wake-scheduler.ts:142** — UPDATE/claim on tenant-scoped table agent_wake_schedules in runDueWakes has no tenant_id constraint, so it can read and mutate due wake rows across all tenants in one sweep.
  - _Fix:_ If this is intended as a cross-tenant system worker, document the table as globally processed; otherwise scope claims by a specific tenant_id parameter. For strict invariant compliance, add tenant_id to the WHERE clause when claiming rows.
- **server/auto-project.ts:135** — SELECT from tenant-scoped messages filters only by conversation_id and omits tenant_id. Although the conversation row was checked earlier, the query itself is not constrained by tenant_id as required by the isolation invariant.
  - _Fix:_ Add an explicit tenant filter to the messages query, e.g. `WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}`.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped messages filters only by conversation_id and omits tenant_id. The handler verified conversation ownership earlier, but the query itself still lacks the required tenant_id constraint.
  - _Fix:_ Constrain the messages read with `AND tenant_id = ${tenantId}` (or equivalent Drizzle where clause) in addition to conversation_id.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped conversations uses foreign ids from a cross-table query and constrains only by conversation id / project_id IS NULL, with no tenant_id predicate in the UPDATE itself.
  - _Fix:_ Add `AND tenant_id = ${c.tenant_id}` to the UPDATE so the write is explicitly tenant-scoped.
- **server/business-tools.ts:95** — INSERT into tenant-scoped table invoice_items does not set tenant_id. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly; relying on invoice_id linkage is not sufficient without DB enforcement.
  - _Fix:_ Include tenant_id in the invoice_items INSERT and ensure the table schema has a non-null tenant_id column. Example: INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...).
- **server/capability-review.ts:428** — INSERT into tenant-scoped table capability_reviews takes conversation_id from caller context and stores it without first verifying the referenced conversation belongs to the same tenant. This accepts a foreign id without ownership validation.
  - _Fix:_ Before inserting, query the conversations row with WHERE id = conversationId AND tenant_id = ctx.tenantId (or equivalent) and reject/null out invalid foreign ids. Apply the same ownership check for any other tenant-scoped foreign keys accepted from caller context.
- **server/ceo-orchestrator.ts:661** — SELECT against the tenant-scoped personas table is unconstrained: `db.select().from(personasTable)` loads all personas without a `tenant_id` filter, which can expose cross-tenant data and drive later routing/status updates from another tenant's rows.
  - _Fix:_ Constrain the query with the caller tenant, e.g. `.where(eq(personasTable.tenantId, plan.tenantId))`. If some personas are global, explicitly scope with `(tenant_id = plan.tenantId OR tenant_id IS NULL)` using a documented global/shared model.
- **server/channel-routing.ts:30** — SELECT from `channel_routes` has no tenant isolation. If `channel_routes` is tenant-scoped, `listChannelRoutes()` returns enabled routes across all tenants because the query only filters on `enabled = true`.
  - _Fix:_ Make `channel_routes` explicitly tenant-scoped and add `tenant_id` to the schema plus `WHERE tenant_id = <caller tenant>` on reads.
- **server/channel-routing.ts:51** — DELETE from `channel_routes` is unconstrained by tenant. If this table is tenant-scoped, a caller could remove another tenant's route because deletion is based only on `channel`.
  - _Fix:_ Add tenant scoping to the table and require `DELETE ... WHERE tenant_id = <caller tenant> AND channel = ...`.
- **server/channel-routing.ts:53** — INSERT/UPSERT into `channel_routes` does not set `tenant_id`. Under the stated invariant, every insert into a tenant-scoped table must explicitly provide tenantId.
  - _Fix:_ Add a non-null `tenant_id` column to `channel_routes` and include it in the INSERT/UPSERT values and conflict target or WHERE logic, sourcing it only from the authenticated caller tenant.
- **server/channel-routing.ts:63** — SELECT from `channel_routes` is missing a tenant filter. `getPersonaForChannel()` resolves a route by `channel` only, which can return another tenant's persona mapping if the table is tenant-scoped.
  - _Fix:_ Require tenantId as a function parameter and query with `WHERE tenant_id = <caller tenant> AND channel = ... AND enabled = true`.
- **server/channel-routing.ts:71** — DELETE from `channel_routes` in `removeChannelRoute()` lacks a tenant constraint. If tenant-scoped, this permits cross-tenant deletion by channel value alone.
  - _Fix:_ Require tenantId and delete with `WHERE tenant_id = <caller tenant> AND channel = ...`.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table project_files filters only by project_id and does not include tenant_id. Application-level isolation invariant requires every read against a tenant-scoped table to constrain tenant_id explicitly.
  - _Fix:_ Add an explicit tenant filter, e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or equivalent Drizzle `and(eq(projectFiles.projectId, projectId), eq(projectFiles.tenantId, tenantId))`).
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table project_notes filters only by project_id and does not include tenant_id. Relying on prior project ownership verification is insufficient under the stated invariant that every tenant-scoped query must explicitly constrain tenant_id.
  - _Fix:_ Add an explicit tenant filter, e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or equivalent Drizzle `and(eq(projectNotes.projectId, projectId), eq(projectNotes.tenantId, tenantId))`).
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table commitments reopens a row by id without a tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. Although reached on an invalid/missing tenantId path, the write is not tenant-scoped and could modify a row across tenants if ids are guessed or data is malformed.
  - _Fix:_ Add the tenant filter to the revert query, or fail closed without issuing any write when tenantId is invalid. For example: `WHERE id = ${row.id} AND tenant_id = ${tenantId}` after validating tenantId, otherwise skip the update entirely.
- **server/context-budget.ts:40** — `runContextBudgetAudit` calls `storage.getPersonas()` without passing or enforcing tenant ownership. If personas are tenant-scoped, this reads persona data without a tenant filter and can mix cross-tenant prompt data into the report.
  - _Fix:_ Use a tenant-scoped storage method such as `getPersonasByTenant(tenantId)` or otherwise ensure the underlying query includes `WHERE tenant_id = ${tenantId}`.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table project_notes filters only by project_id and omits tenant_id. If project_id ownership assumptions ever fail or IDs are reused/mis-associated, notes from another tenant can be included in the backup.
  - _Fix:_ Add tenant scoping to the query, e.g. `WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` or verify via a tenant-owned join before reading notes.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table project_files filters only by project_id and omits tenant_id. This can read another tenant's files if the foreign key reference is not independently enforced by tenant ownership.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause or join through a tenant-scoped parent with explicit ownership verification.
- **server/data-protection.ts:404** — SELECT COUNT(*) from tenant-scoped table messages filters only by conversation_id and omits tenant_id. This creates a cross-tenant existence/count leak for messages on the same conversation_id.
  - _Fix:_ Scope the query with `AND tenant_id = ${tenantId}` so the count is limited to the caller's tenant.
- **server/debate-engine.ts:126** — runDebate accepts a tenantId but calls `storage.getPersonas()` with no tenant scoping or ownership verification. If personas are tenant-scoped, this can load cross-tenant personas and use them in the debate.
  - _Fix:_ Use a tenant-scoped persona read such as `storage.getPersonas(tenantId)` or first verify returned personas belong to the caller's tenant before use.
- **server/deep-interview.ts:337** — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. The row is derived from a tenant-scoped project join, but the invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly.
  - _Fix:_ Include `tenant_id` in the INSERT column list and set it to `${state.tenantId}` in the SELECT/VALUES used for insertion.
- **server/doc-collections.ts:166** — DELETE from tenant-scoped table doc_chunks is constrained by doc_path and collection_id only, with no tenant_id filter. If collectionId/docPath are influenced by another tenant-owned collection/document pair, this can delete cross-tenant rows.
  - _Fix:_ Add tenant scoping to the DELETE: `DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}`.
- **server/doc-collections.ts:294** — UPDATE against tenant-scoped table doc_chunks sets embedding by row id only, with no tenant_id constraint. A mismatched or stale row id could update another tenant's chunk embedding.
  - _Fix:_ Constrain the UPDATE by tenant as well: `UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}`.
- **server/embeddings.ts:809** — UPDATE against tenant-scoped table `memory_entries` can execute without any `tenant_id` constraint when `storeEmbeddingVec()` is called for `table === "memory_entries"` without `options.tenantId`. This permits cross-tenant modification by row id.
  - _Fix:_ Require a validated `tenantId` for all `memory_entries` updates and include `AND tenant_id = ${tenantId}` in the WHERE clause. Remove the fallback branch that updates by id only.
- **server/embeddings.ts:814** — SELECT from tenant-scoped table `agent_knowledge` in `backfillMissingKnowledgeEmbeddings()` has no `tenant_id` filter. This scans rows across all tenants.
  - _Fix:_ Scope the query by tenant when running per-tenant work, or explicitly iterate tenants and query each with `WHERE tenant_id = ...`.
- **server/embeddings.ts:845** — SELECT from tenant-scoped table `memory_entries` in `backfillEmbeddingVecs()` has no `tenant_id` filter, reading rows across all tenants.
  - _Fix:_ Add `WHERE tenant_id = <tenant>` for tenant-scoped execution, or iterate tenant-by-tenant with explicit tenant filters.
- **server/embeddings.ts:861** — SELECT from tenant-scoped table `agent_knowledge` in `backfillEmbeddingVecs()` has no `tenant_id` filter, reading rows across all tenants.
  - _Fix:_ Add a tenant filter for per-tenant execution, or process rows tenant-by-tenant with explicit `WHERE tenant_id = ...`.
- **server/felix-brain.ts:23** — Boot-time rehydration selects from tenant-scoped table `agent_runs` without any tenant_id filter, loading running/pending rows across all tenants into memory.
  - _Fix:_ Scope the query by tenant_id for the intended tenant(s). If rehydrating globally is required, iterate per authorized tenant explicitly and ensure downstream state is partitioned and only exposed within that tenant.
- **server/firecrawl.ts:260** — UPDATE against tenant-scoped table scrapedPages is constrained only by id, not tenant_id. Although the id was previously selected with tenant scoping, the write itself lacks the required tenant filter invariant.
  - _Fix:_ Constrain the UPDATE with both id and tenantId, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))
- **server/firecrawl.ts:393** — UPDATE against tenant-scoped table scrapedPages is constrained only by id, not tenant_id. This violates the invariant that every write must include a tenant_id filter in the write query itself.
  - _Fix:_ Add tenant scoping directly to the UPDATE WHERE clause, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))
- **server/google-drive.ts:882** — UPDATE against tenant-scoped table tenants is constrained only by id, not tenant_id. Under the stated invariant, every write to a tenant-scoped table must include tenant_id scoping in the WHERE clause.
  - _Fix:_ Include tenant ownership in the write predicate or route through a trusted global/admin-only path explicitly documented as exempt. For tenant-scoped enforcement, use a WHERE that includes both tenant identity and the row id.
- **server/google-drive.ts:894** — SELECT from tenant-scoped table projects reads a caller-supplied foreign id (projectId) without constraining the query by tenant_id in SQL. Ownership is checked after the read, but the invariant requires tenant filtering on the query itself.
  - _Fix:_ Query projects with both project id and tenantId in the WHERE clause, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))
- **server/google-drive.ts:907** — UPDATE against tenant-scoped table projects is constrained only by project id, not tenant_id. Even though tenant ownership was checked earlier, the write query itself violates the required tenant filter invariant.
  - _Fix:_ Add tenant scoping to the UPDATE predicate, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))
- **server/google-drive.ts:1474** — SELECT from tenant-scoped table projects uses caller-supplied projectId without tenant_id constraint in SQL. Ownership is validated after the read, but the invariant requires the row read itself to be tenant-scoped.
  - _Fix:_ Fetch the project with both id and tenantId in the WHERE clause when tenantId is available, and fail closed if tenantId is absent for tenant-scoped operations.
- **server/google-drive.ts:1712** — SELECT from tenant-scoped table projects reads caller-supplied projectId without tenant_id constraint in SQL. The later tenant comparison is not sufficient under the invariant.
  - _Fix:_ Constrain the project lookup by both project id and params.tenantId in the WHERE clause.
- **server/google-workspace.ts:2018** — Tenant-scoped table `tenants` is queried with a hardcoded id (`WHERE id = 1`) instead of constraining by the caller's tenant context. This bypasses application-level tenant isolation and reads another tenant's row directly.
  - _Fix:_ Pass the caller/admin tenant explicitly and constrain the query with `WHERE tenant_id = <caller tenant>` semantics as appropriate for the schema, or use a validated ownership check before reading the tenant row. Avoid hardcoded cross-tenant reads.
- **server/google-workspace.ts:2307** — UPDATE against tenant-scoped table `presenter_sessions` is filtered only by `id = ${sessionId}` with no tenant_id constraint. If `sessionId` is guessed or reused across tenants, this can update another tenant's session.
  - _Fix:_ Constrain the UPDATE with both the trusted tenant and the session id, e.g. `WHERE id = ${sessionId} AND tenant_id = ${tenantId}`.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` has no tenant_id filter. It scans all tenants' stale proposals, violating tenant isolation.
  - _Fix:_ Require a trusted tenant context and add `WHERE tenant_id = ${tenantId}` (or equivalent Drizzle filter). If this is intentionally global admin-only maintenance, move it to a clearly global path with explicit authorization and document the exception.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` archives rows by id array only, with no tenant_id constraint. This can modify proposals belonging to other tenants.
  - _Fix:_ Add a trusted tenant filter to the UPDATE, e.g. `WHERE tenant_id = ${tenantId} AND id = ANY(...)`, or validate ownership of every id before updating.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` has no tenant_id filter. It reads tasks across all tenants.
  - _Fix:_ Add `WHERE tenant_id = ${tenantId}` (or equivalent Drizzle filter) using trusted tenant context, unless this is a deliberately centralized admin-only operation with explicit authorization.
- **server/health-audit.ts:368** — UPDATE on tenant-scoped table `heartbeat_tasks` disables tasks by id array only, with no tenant_id constraint. This can disable tasks belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. `WHERE tenant_id = ${tenantId} AND id = ANY(...)`, or validate ownership before mutation.
- **server/health-monitor.ts:187** — SELECT from tenant-scoped table `tenants` has no tenant_id constraint (`SELECT COUNT(*) ... FROM tenants WHERE is_active = true`). This reads across all tenants and breaks the app-level isolation invariant.
  - _Fix:_ If this check is meant for a tenant-scoped caller, filter by the trusted tenant id. If it is truly a global/admin health metric, keep it on an explicitly global/admin-only code path and document that `tenants` is treated as a global table here.
- **server/heartbeat-schedules.ts:60** — Cross-tenant SELECT from tenant-scoped table research_schedules has no tenant_id constraint. This reads all tenants' schedules without an explicit per-tenant ownership filter.
  - _Fix:_ If this worker is intentionally cross-tenant, first enumerate tenant IDs from a trusted source and then query research_schedules per tenant with WHERE tenant_id = <tenant>. Otherwise add a tenant_id filter to the SELECT.
- **server/heartbeat-startup.ts:81** — UPDATE against tenant-scoped table heartbeat_tasks filters only by id and omits tenant_id, so it can modify another tenant's row if ids are reused or accessed unexpectedly.
  - _Fix:_ Constrain the UPDATE by both id and tenant_id, e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}.
- **server/heartbeat-startup.ts:85** — UPDATE against tenant-scoped table heartbeat_tasks filters only by id and omits tenant_id, so it can modify another tenant's row if ids are reused or accessed unexpectedly.
  - _Fix:_ Constrain the UPDATE by both id and tenant_id, e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}.
- **server/heartbeat-task-scheduler-storage.ts:11** — Cross-tenant SELECT from tenant-scoped table heartbeat_tasks has no tenant_id constraint. This returns tasks for all tenants without an explicit per-tenant filter.
  - _Fix:_ Only query heartbeat_tasks per tenant with WHERE tenant_id = <tenant>. If a platform scheduler needs cross-tenant execution, enumerate tenants first and perform tenant-scoped queries for each tenant.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE against tenant-scoped table heartbeat_tasks filters only by id and omits tenant_id, allowing claims of tasks without an explicit tenant ownership constraint.
  - _Fix:_ Pass the task tenant alongside each claimed id and update with WHERE id = ${id} AND tenant_id = ${tenantId} AND enabled = true AND next_run_at <= NOW() AND approval_status = 'approved'.
- **server/heartbeat.ts:261** — Cross-tenant SELECT from tenant-scoped table event_log has no tenant_id constraint. It reads pending events across all tenants without an explicit tenant filter.
  - _Fix:_ Enumerate tenants first and query event_log per tenant with WHERE tenant_id = <tenant> AND status = 'pending', or otherwise add a tenant_id filter.
- **server/heartbeat.ts:1583** — SELECT from tenant-scoped table heartbeat_logs has no tenant_id constraint. It checks prior runs by task_name only, which can read another tenant's logs and affect scheduling behavior across tenants.
  - _Fix:_ Filter by the current task tenant as well, e.g. WHERE tenant_id = ${tenantId} AND task_name = ... AND status = 'success'.
- **server/heartbeat.ts:1715** — SELECT from tenant-scoped table code_health_scans has no tenant_id constraint. It reads the latest scan globally rather than restricting to the current tenant.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} when reading prior scans, or make the table explicitly global if that is the intended design.
- **server/heartbeat.ts:1724** — SELECT from tenant-scoped table code_health_findings has no tenant_id constraint. It diffs findings across scans without restricting rows to the current tenant.
  - _Fix:_ Add tenant_id filters for both the outer findings query and the NOT EXISTS subquery, e.g. WHERE tenant_id = ${tenantId} and p.tenant_id = ${tenantId}.
- **server/job-queue.ts:238** — completeJob updates tenant-scoped table agent_jobs by id/attempts only, with no tenant_id constraint. A stale or incorrect id could mark another tenant's job complete.
  - _Fix:_ Pass the persisted tenantId into completeJob and add `AND tenant_id = ${tenantId}` to the WHERE clause.
- **server/job-queue.ts:281** — failJob first reads max_attempts from tenant-scoped table agent_jobs by id only, without tenant_id verification, then later updates the row without tenant_id constraint. This allows cross-tenant reads/writes if a wrong job id is used.
  - _Fix:_ Change failJob to require tenantId and add `WHERE id = ${id} AND tenant_id = ${tenantId}` on both the SELECT and subsequent UPDATE statements.
- **server/job-queue.ts:335** — cancelJob updates tenant-scoped table agent_jobs by id only with no tenant_id filter.
  - _Fix:_ Require tenantId (or explicit admin all-tenants scope) and add `AND tenant_id = ${tenantId}` to the UPDATE WHERE clause.
- **server/knowledge-diversity-monitor.ts:189** — Uses sql.raw() inside an INTERVAL expression. Although the current value comes from a constant, raw SQL construction is explicitly disallowed because it creates injection/isolation risk if the source ever becomes request-derived or mutable.
  - _Fix:_ Avoid sql.raw(); compute the cutoff timestamp in application code (e.g. `new Date(Date.now() - hours*3600_000)`) and bind it as a parameter, or use a fully parameterized SQL expression without raw interpolation.
- **server/lib/agent-trace.ts:130** — UPDATE against tenant-scoped table agent_trace_spans constrains only span_id and omits tenant_id in the WHERE clause. Since span_id comes from application state and tenant isolation relies solely on SQL filters, this write is not tenant-scoped.
  - _Fix:_ Include tenant_id in the close-path UPDATE predicate, e.g. persist and pass tenantId into persistSpanClose and use `WHERE span_id = ${spanId} AND tenant_id = ${tenantId}`.
- **server/lib/aevo-meta-editor.ts:449** — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. Tenant isolation requires every write to tenant-scoped rows to include tenant_id in the WHERE clause.
  - _Fix:_ Add tenant scoping to the predicate, e.g. `.where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))`.
- **server/lib/aevo-meta-editor.ts:459** — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. This relies on a global row id rather than an application-level tenant filter.
  - _Fix:_ Scope the UPDATE by both run id and tenant id: `.where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))`.
- **server/lib/aevo-meta-editor.ts:472** — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. Without a tenant_id predicate, the write is not isolated by tenant at the SQL layer.
  - _Fix:_ Add `eq(procedureEvolutionRuns.tenantId, tenantId)` to the WHERE clause alongside the id filter.
- **server/lib/aevo-meta-editor.ts:502** — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. Tenant-scoped writes must always include tenant_id in the WHERE clause.
  - _Fix:_ Use a tenant-scoped WHERE clause, e.g. `.where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))`.
- **server/lib/customer-revenue-launchpad-seed.ts:103** — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly, and there is no DB default/RLS/FK enforcement to ensure tenant isolation.
  - _Fix:_ Include tenant_id = OWNER_TENANT_ID (or the verified project tenant) in the INSERT INTO project_notes columns/values, and ensure any future reads/writes to project_notes are tenant-scoped as well.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT against tenant-scoped table project_files uses only project_id with no tenant_id ownership verification. newProjectId is derived from imported snapshot data and the code does not first verify the referenced project belongs to the intended tenant before reading/writing child rows.
  - _Fix:_ Before querying or inserting into project_files, load the parent project by id with WHERE id = <projectId> AND tenant_id = <expected tenantId>, or include a join/subquery that constrains the parent project tenant. If project_files has its own tenant_id, also filter/set it explicitly.
- **server/lib/ideabrowser-backfill.ts:104** — INSERT into tenant-scoped table project_files does not set tenant_id. The write relies only on project_id, but the invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly.
  - _Fix:_ Add tenant_id to the INSERT values for project_files and ensure the parent project is verified to belong to that same tenant before inserting.
- **server/lib/ideabrowser-backfill.ts:115** — SELECT against tenant-scoped table project_notes uses only project_id with no tenant_id ownership verification. The code trusts newProjectId without first validating tenant ownership of the referenced project.
  - _Fix:_ Verify the parent project with WHERE id = <projectId> AND tenant_id = <expected tenantId> before querying/inserting notes, or join project_notes access through projects constrained by tenant_id. If project_notes has its own tenant_id, also filter on it.
- **server/lib/ideabrowser-backfill.ts:119** — INSERT into tenant-scoped table project_notes does not set tenant_id. The write relies only on project_id, violating the requirement that tenant_id be explicitly inserted on tenant-scoped tables.
  - _Fix:_ Include tenant_id explicitly in the INSERT into project_notes and ensure the referenced project_id has been ownership-checked for the same tenant before inserting.
- **server/lib/ideabrowser-dev-sync.ts:139** — INSERT into tenant-scoped table project_notes does not set tenant_id. The code writes notes using only project_id after creating a project, but the invariant requires explicit tenant_id on every INSERT into tenant-scoped tables.
  - _Fix:_ Add tenant_id to the project_notes INSERT and keep the operation scoped to the same tenant as the parent project. If the table is tenant-scoped via parent ownership only, first verify the parent project with tenant_id before inserting.
- **server/lib/ideabrowser-dev-sync.ts:143** — Subquery against tenant-scoped table project_notes checks existence by project_id only, with no tenant_id constraint. This is a tenant-isolation gap if project_ids are ever mis-associated or reused from untrusted input.
  - _Fix:_ Constrain the existence check with tenant_id as well, or join through the parent projects table with WHERE projects.tenant_id = <caller tenant>.
- **server/lib/ideabrowser-dev-sync.ts:150** — INSERT into tenant-scoped table project_files does not set tenant_id. The write is scoped only by project_id, but the invariant requires explicit tenant_id on INSERTs.
  - _Fix:_ Add tenant_id to the project_files INSERT and verify the parent project belongs to the tenant before inserting.
- **server/lib/ideabrowser-dev-sync.ts:154** — Subquery against tenant-scoped table project_files checks existence by project_id only, with no tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause for project_files existence checks, or join through projects and constrain by projects.tenant_id = <caller tenant>.
- **server/lib/inbox-ingest.ts:536** — buildInboxDigest joins tenant-scoped inbox_classifications to inbox_messages but filters only on c.tenant_id; it does not verify m.tenant_id = tenantId. If inbox_message_id ever references or resolves to a row from another tenant, digest output could read cross-tenant message metadata.
  - _Fix:_ Constrain both tables by tenant in the query, e.g. add `AND m.tenant_id = ${tenantId}` (or equivalent Drizzle `and(eq(c.tenantId, tenantId), eq(m.tenantId, tenantId))`) when joining inbox_messages.
- **server/lib/outbound-email-preflight.ts:115** — Tenant-scoped incident reporting is hardcoded to tenantId: 1 when calling reportQualityIncident for outbound quality-gate degradation/blocks. This writes to a tenant-scoped flow using the wrong tenant instead of the caller's tenant, breaking tenant isolation and misattributing incidents across tenants.
  - _Fix:_ Pass the actual caller tenantId into preflightOutboundEmail and use that tenantId in both reportQualityIncident calls instead of the hardcoded 1. If this path is truly global/admin-only, document and enforce that explicitly at the call boundary.
- **server/lib/restraint-trust.ts:79** — INSERT into tenant-scoped table trust_scores does not include an explicit tenant ownership-unique key beyond tenant_id/persona_id/category and uses ON CONFLICT DO NOTHING without a specified tenant-scoped conflict target; if the table permits conflicts on non-tenant columns, this can suppress writes unpredictably. More importantly for the invariant, the raw SQL write path relies on sql.raw(column) for dynamic SQL construction.
  - _Fix:_ Avoid sql.raw() entirely here. Use a fixed branch per allowed outcome/column or Drizzle update builders so the column name is selected in application code, not injected into SQL. Also ensure the insert/upsert targets the intended tenant-scoped unique key explicitly, e.g. ON CONFLICT (tenant_id, persona_id, category) DO NOTHING.
- **server/lib/scheduled-post-runner.ts:61** — System-scope runner updates and reads tenant-scoped table scheduled_posts across all tenants without any tenant_id constraint in the picking SELECT/UPDATE CTE. Under the stated invariant, every read/write against a tenant-scoped table must be constrained by tenant_id in SQL.
  - _Fix:_ Refactor the runner to process one tenant at a time and add tenant_id predicates to both the due-row SELECT and the UPDATE (for example, WHERE tenant_id = <target tenant> AND ...). If cross-tenant system processing is required, use an explicitly global orchestration table to enumerate tenants, then run tenant-scoped queries per tenant.
- **server/lib/skill-optimizer-promotion.ts:721** — Reads from tenant-scoped table skills by foreign id candidate.skillId without first verifying tenant ownership and without a tenant_id filter in the WHERE clause. This violates the invariant requiring tenant-scoped reads to include tenant_id, and it trusts a foreign id derived from a previously loaded candidate row.
  - _Fix:_ Add a tenant ownership check before reading the skill, and constrain the query with tenant_id as well (e.g. WHERE skills.id = candidate.skillId AND skills.tenant_id = candidate.tenantId). If skills is truly global/shared, document and enforce that consistently in schema and code; otherwise always filter by tenant_id.
- **server/lib/skill-optimizer-promotion.ts:836** — Updates tenant-scoped table skills using only id and promptContent compare-and-swap, with no tenant_id predicate. This can modify a row outside the caller tenant if ids collide with unexpected data assumptions, and violates the invariant that every write to a tenant-scoped table must be constrained by tenant_id.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = skill.id AND tenant_id = candidate.tenantId AND prompt_content = skill.promptContent). Also ensure the earlier skill lookup verified the same tenant ownership.
- **server/lib/skill-optimizer-promotion.ts:928** — Reads from tenant-scoped table skills by foreign id candidate.skillId during rollback without a tenant_id filter or prior ownership verification. This trusts a foreign reference and violates the invariant requiring tenant-scoped reads to be constrained by tenant_id.
  - _Fix:_ Constrain the rollback skill SELECT with tenant_id as well (e.g. WHERE id = candidate.skillId AND tenant_id = tenantId) and fail closed if no such tenant-owned skill exists.
- **server/lib/skill-optimizer-promotion.ts:946** — Updates tenant-scoped table skills during rollback using only id and promptContent, without a tenant_id predicate. This violates the invariant that every write to a tenant-scoped table must include tenant_id filtering.
  - _Fix:_ Add tenant_id to the rollback UPDATE WHERE clause (e.g. WHERE id = skill.id AND tenant_id = tenantId AND prompt_content = skill.promptContent) after verifying the skill row belongs to the same tenant.
- **server/lib/stuck-task-sweep.ts:56** — Raw SELECT against tenant-scoped table agent_runs has no tenant_id filter in the WHERE clause, so it scans and returns rows across all tenants.
  - _Fix:_ Constrain the query by caller tenant, e.g. add `AND tenant_id = ${tenantId}` (or equivalent Drizzle `.where(eq(agentRuns.tenantId, tenantId))`) and require tenantId as an input to this function unless this table is explicitly global.
- **server/lib/stuck-task-sweep.ts:84** — Raw SELECT against tenant-scoped table mind_tickets has no tenant_id filter in the WHERE clause, so it scans and returns rows across all tenants.
  - _Fix:_ Constrain the query by caller tenant, e.g. add `AND tenant_id = ${tenantId}` (or equivalent Drizzle `.where(eq(mindTickets.tenantId, tenantId))`) and require tenantId as an input to this function unless this table is explicitly global.
- **server/lib/system-probes.ts:163** — Count query against tenant-scoped table messages has no tenant_id filter, exposing cross-tenant totals.
  - _Fix:_ Filter by the caller tenant, e.g. `FROM messages WHERE tenant_id = ${tenantId}` or Drizzle `.where(eq(messagesTable.tenantId, tenantId))`. If this is an admin-only all-tenant health probe, keep it behind a verified admin path and document that exception explicitly.
- **server/lib/work-opportunity-radar-seed.ts:125** — INSERT into project_notes does not set tenant_id explicitly, violating the invariant that every insert into tenant-scoped tables must pass tenantId explicitly.
  - _Fix:_ Include the tenant_id column/value in the INSERT, using the validated owner tenant: `INSERT INTO project_notes (project_id, tenant_id, note, author) ... VALUES (..., ${ADMIN_TENANT_ID}, ...)`, and ensure uniqueness/NOT EXISTS checks also include tenant ownership where applicable.
- **server/lib/work-opportunity-radar-seed.ts:135** — INSERT into project_files does not set tenant_id explicitly, violating the invariant that every insert into tenant-scoped tables must pass tenantId explicitly.
  - _Fix:_ Include the tenant_id column/value in the INSERT, using the validated owner tenant: `INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_type, uploaded_by) ... VALUES (..., ${ADMIN_TENANT_ID}, ...)`, and scope any existence checks by tenant as well if the table is tenant-scoped.
- **server/memory-graph.ts:130** — UPDATE against tenant-scoped table memory_categories filters only by id and omits tenant_id. If catId is cross-tenant or corrupted, this can modify another tenant's row.
  - _Fix:_ Constrain the update with tenant ownership, e.g. `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}`.
- **server/memory-graph.ts:262** — SELECT against tenant-scoped table memory_links checks for an existing link using memory ids only and has no tenant_id constraint or ownership verification on the referenced memories. This can read cross-tenant link state if ids collide across tenants' graph data.
  - _Fix:_ Join memory_links to tenant-owned memory_entries on both endpoints or add tenant scoping to the link table/query so existence checks are limited to the caller's tenant.
- **server/memory-graph.ts:669** — SELECT count from tenant-scoped table memory_links scopes only by joining source_memory_id to a tenant-owned memory_entries row. It does not verify the target endpoint belongs to the same tenant, so cross-tenant links can be counted into this tenant's graph.
  - _Fix:_ Join both source and target memory_entries with `tenant_id = ${tenantId}` (or store tenant_id on memory_links and filter by it) before counting links.
- **server/memory-graph.ts:709** — UPDATE against tenant-scoped table memory_entries filters only by id and omits tenant_id. A stale or cross-tenant mem.id could update another tenant's memory row.
  - _Fix:_ Add tenant scoping to the write: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}`.
- **server/memory-intelligence.ts:413** — UPDATE against tenant-scoped table knowledge_triples filters only by id and omits tenant_id after selecting contradiction rows. Without tenant scoping on the write, a wrong/cross-tenant id could modify another tenant's triple.
  - _Fix:_ Constrain the update by tenant as well: `WHERE id = ${c.id} AND tenant_id = ${tenantId}`.
- **server/memory/tombstones.ts:64** — SELECT from tenant-scoped table memory_tombstones is executed directly via db.execute without using a tenant-scoped transaction/helper. While it includes a tenant_id predicate, this module bypasses the application's standard tenant-scoping path, increasing risk of cross-tenant access if caller-supplied tenantId is incorrect or unvalidated.
  - _Fix:_ Require a validated tenant context or transaction parameter (for example use withTenantTx(input.tenantId, ...) or accept a tenant-scoped tx) and keep the WHERE tenant_id = input.tenantId predicate.
- **server/model-catalog.ts:775** — SELECT from tenant-scoped table memory_entries constrains tenant_id, but subsequent INSERT into the same tenant-scoped table uses tenantId supplied via opts without any ownership validation in this function. If an internal caller passes another tenant's id, this writes cross-tenant data.
  - _Fix:_ Do not accept arbitrary tenantId here from untrusted/internal callers. Resolve tenantId from authenticated context or require the caller to pass a validated tenant id. Keep the explicit tenant_id on INSERT, but only after verifying the caller is allowed to act for that tenant.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table project_files does not set tenant_id. This code relies only on project_id ownership check, but the invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT column list and values, e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...), after validating options.tenantId is the caller's tenant.
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table project_files does not set tenant_id. Although project ownership is checked, the tenant_id column is omitted on insert, violating the explicit tenant scoping requirement for writes.
  - _Fix:_ Add tenant_id to the INSERT for project_files and supply the validated caller tenant, e.g. (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...).
- **server/oauth-subscriptions.ts:908** — SELECT from tenant-scoped table oauth_subscriptions reads all active rows with no tenant_id filter. This enumerates subscriptions across tenants.
  - _Fix:_ Constrain the query by tenant_id for the intended tenant context, or if this is truly a cross-tenant administrative job, move it behind a clearly privileged global path and document/enforce that only system/admin code can invoke it.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table oauth_subscriptions reads google-workspace rows across all tenants with no tenant_id filter.
  - _Fix:_ Add a tenant_id predicate for the intended tenant, or restrict this logic to a privileged admin-only maintenance path with explicit authorization and justification for cross-tenant access.
- **server/oauth-subscriptions.ts:1138** — seedYouTubeIfMissing defaults tenantId to 1 and inserts into tenant-scoped oauth_subscriptions for that tenant without caller ownership validation. A default tenant write is unsafe in a multi-tenant codebase.
  - _Fix:_ Remove the default tenantId=1, require an explicit validated tenantId from the caller, and ensure all invocations pass the authenticated tenant context.
- **server/persona-export.ts:39** — SELECT from tenant-scoped table `skills` uses `persona_id = ${personaId} OR persona_id IS NULL` with no tenant_id filter. This can pull skills from other tenants that reference the same persona_id or global-looking rows without validating they are truly shared.
  - _Fix:_ Add tenant scoping for tenant-owned skills, e.g. `WHERE tenant_id = ${tenantId} AND (persona_id = ${personaId} OR persona_id IS NULL)` or split truly global shared skills into a separate non-tenant table.
- **server/persona-routing-outcomes.ts:44** — Join reads from tenant-scoped table `personas` without constraining `p.tenant_id = ${tenantId}`. Although `action_outcomes` is filtered by tenant, a cross-tenant persona_id collision or bad foreign reference could leak another tenant's persona name into the cache.
  - _Fix:_ Scope both sides of the join: add `AND p.tenant_id = ${tenantId}` (or equivalent join predicate) so persona rows are verified to belong to the same tenant.
- **server/plan-executor.ts:741** — SELECT against tenant-scoped plans table uses only id with no tenant_id constraint. This reads plan metadata (source) before tenant ownership is established, violating the invariant that every access to tenant-scoped tables must be constrained by tenant_id.
  - _Fix:_ Avoid the pre-probe or combine claim logic into a single tenant-scoped statement. If this function is only invoked after ownership is known, pass tenantId in and add `AND tenant_id = ${tenantId}` to the SELECT/claim path.
- **server/plan-executor.ts:744** — UPDATE against tenant-scoped plans table claims a plan by id and status only, with no tenant_id predicate. This mutates a tenant-scoped row without an explicit tenant filter.
  - _Fix:_ Require the caller tenantId and add `AND tenant_id = ${tenantId}` to the UPDATE WHERE clause, or otherwise derive and validate tenant ownership before performing the write.
- **server/plan-executor.ts:1280** — claimStaleRecoveryChildCore updates tenant-scoped plans by id/source/status without a tenant_id constraint. This is a write on a tenant-scoped table lacking the required tenant filter.
  - _Fix:_ Add tenantId as a required parameter and constrain the UPDATE with `AND tenant_id = ${tenantId}`.
- **server/plan-executor.ts:1293** — terminalizeStaleRecoveryChildCore selects a child row from tenant-scoped plans by id/source only, without tenant_id filtering. This reads a tenant-scoped row without the required tenant constraint.
  - _Fix:_ Pass the expected tenantId into this helper and add `AND tenant_id = ${tenantId}` to the child SELECT.
- **server/plan-executor.ts:1404** — Boot recovery reads from tenant-scoped plans table across all tenants with no tenant_id constraint. The invariant requires every read/write on tenant-scoped tables to be tenant-filtered.
  - _Fix:_ If this is intended as a privileged cross-tenant maintenance path, redesign around explicit per-tenant iteration or document/use a separate global-admin access boundary plus tenant-scoped queries for each tenant.
- **server/plan-executor.ts:1540** — Transient recovery query selects from tenant-scoped plans without any tenant_id constraint, scanning failed plans across all tenants.
  - _Fix:_ Iterate per tenant and add `WHERE tenant_id = ${tenantId}` for each tenant-scoped query, or introduce an explicit audited admin boundary that still executes tenant-filtered queries.
- **server/plan-executor.ts:1599** — Query selects approved plans from tenant-scoped plans table without tenant_id constraint, causing cross-tenant reads in the recovery loop.
  - _Fix:_ Restrict the query by tenant_id, e.g. `WHERE tenant_id = ${tenantId} AND status = 'approved'`, and drive recovery per tenant.
- **server/platform-admin-conversations.ts:18** — SELECT from tenant-scoped conversations table by conversation id only, with no tenant_id filter. Even though platform-admin authorization is checked, this violates the stated invariant that isolation is enforced only by tenant_id SQL filters.
  - _Fix:_ Add an explicit tenant boundary to the query. If cross-tenant admin access is required, redesign with an audited admin-only path that still scopes queries by a chosen tenant_id rather than omitting tenant filters entirely.
- **server/process-governor.ts:1234** — UPDATE on tenant-scoped governance_rules table uses only rule id with no tenant_id constraint. A stale or mismatched rule object could increment trigger_count on another tenant's row.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the UPDATE WHERE clause.
- **server/project-brain.ts:135** — SELECT from tenant-scoped table `projects` by `id` only, with no `tenant_id` constraint. `updateProjectBrain(projectId, ...)` can read another tenant's project if called with a foreign projectId.
  - _Fix:_ Require `tenantId` as a parameter and constrain the query with `WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Also ensure callers only pass projectIds already verified to belong to the tenant.
- **server/project-brain.ts:332** — Backfill query reads tenant-scoped tables `projects`, `conversations`, and `messages` with no `tenant_id` filters, causing cross-tenant enumeration during brain backfill.
  - _Fix:_ Run backfill per tenant and add tenant filters throughout, e.g. constrain `projects`, `conversations`, and `messages` by the same tenantId in every query.
- **server/project-brain.ts:349** — SELECT from tenant-scoped table `conversations` by `project_id` only, with no `tenant_id` constraint. Even if project ownership were intended, it is not enforced at query time.
  - _Fix:_ Add `AND c.tenant_id = ${tenantId}` and ensure `proj.id` came from a tenant-scoped project query.
- **server/project-brain.ts:359** — SELECT from tenant-scoped table `messages` by `conversation_id` only, with no `tenant_id` constraint. This can read messages from another tenant's conversation if referenced indirectly.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` or first verify the conversation belongs to the tenant, then keep a tenant filter on the messages query as well.
- **server/reference-learner.ts:296** — UPDATE on tenant-scoped table memory_entries is constrained only by id = storedId, without tenant_id = input.tenantId in the WHERE clause. If storedId were ever stale/corrupted, this could update another tenant's row.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}, or use Drizzle .where(and(eq(table.id, storedId), eq(table.tenantId, input.tenantId))).
- **server/research-engine.ts:464** — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. Per invariant, tenant-scoped inserts must pass tenantId explicitly; relying only on project_id ownership is insufficient.
  - _Fix:_ Include tenant_id in the INSERT values, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...).
- **server/research-engine.ts:472** — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. This violates the requirement that every INSERT into tenant-scoped tables provide tenant_id.
  - _Fix:_ Add tenant_id to the inserted columns and values, using session.tenantId for each project note row.
- **server/routes.ts:189** — Startup restore selects from tenant-scoped file_storage without any tenant_id constraint. This reads every tenant's stored files across the whole system.
  - _Fix:_ If this restore is intended per-tenant, add a tenant_id filter in the WHERE clause. If it is truly global maintenance, iterate tenants explicitly and scope each query by fileStorage.tenantId.
- **server/routes.ts:632** — Backfill query selects from tenant-scoped projects joined to tenants without any tenant_id constraint. This reads all tenants' projects in one unscoped query.
  - _Fix:_ Scope the query by tenant_id when running in a tenant context, or move this to a clearly platform-admin/global maintenance path that intentionally processes each tenant explicitly.
- **server/routes.ts:1938** — GET /api/presenter/:token reads presenter_sessions by token only, with no tenant ownership verification. Anyone with a valid token can access the session row without an authenticated tenant check.
  - _Fix:_ If presenter sessions are intended to be public capabilities, document and isolate that model. Otherwise require auth and constrain the lookup by tenant_id, or verify that the token itself is a deliberate capability with sufficient entropy and expiry.
- **server/routes.ts:2009** — POST /api/presenter/:token/tts reads presenter_sessions by token only, with no tenant ownership verification before invoking text-to-speech.
  - _Fix:_ Require authenticated tenant ownership or explicitly treat the token as a public capability and enforce expiry/scope at the presenter session layer.
- **server/routes.ts:2043** — POST /api/presenter inserts into tenant-scoped presenter_sessions using tenantId taken directly from req.body on an internal-key-gated endpoint, without validating that the caller is authorized to create data for that tenant.
  - _Fix:_ Do not trust body-supplied tenantId. Derive tenant ownership from a trusted server-side context, or validate that the referenced presentation/session belongs to the intended tenant before inserting.
- **server/routes.ts:3262** — GET /api/marketplace/export/:id takes a foreign skill/template id from params and exports it without any tenant ownership verification.
  - _Fix:_ Before exporting, load the referenced row with a tenant_id filter or otherwise verify the caller's tenant owns the skill being exported.
- **server/routes.ts:4664** — INSERT into tenant-scoped table `project_notes` does not set `tenant_id`. The note is created from a tenant-owned conversation/project, but the INSERT only provides `(project_id, note, author)` and relies on neither RLS nor defaults.
  - _Fix:_ Include `tenant_id` explicitly in the INSERT, e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0, 5000)}, ${'system:context-guard'})`, and ensure the referenced project is tenant-owned before inserting.
- **server/routes.ts:6947** — Import handler creates personas from imported data without explicitly setting/validating `tenantId`. In this codebase every INSERT into tenant-scoped tables must pass `tenantId` explicitly; imported `rest` may omit it or carry untrusted cross-tenant values.
  - _Fix:_ On import, ignore any tenantId from the file and set the tenant explicitly from trusted context, e.g. `await storage.createPersona({ ...rest, tenantId: trustedTenantId })`, or keep this route strictly admin-only for global/shared personas if personas are truly not tenant-scoped.
- **server/routes.ts:6957** — Import handler creates memory entries from imported data without explicitly setting `tenantId` and instead spreads untrusted imported fields. This can insert rows without tenant scoping or with attacker-controlled tenant ownership.
  - _Fix:_ Derive a trusted tenant for the import operation and call `storage.createMemoryEntry({ ...rest, personaId: rest.personaId || null, tenantId: trustedTenantId })`, ignoring any tenantId present in imported data.
- **server/routes.ts:6967** — Import handler creates knowledge rows from imported data without explicitly setting `tenantId` and spreads imported fields directly. This violates the requirement that tenant-scoped INSERTs must pass tenantId explicitly from trusted context.
  - _Fix:_ Set `tenantId` explicitly from trusted context during import, e.g. `await storage.createKnowledge({ ...rest, personaId: rest.personaId || null, tenantId: trustedTenantId })`, and do not trust tenantId embedded in the import file.
- **server/routes.ts:6977** — Import handler creates conversations from imported data using untrusted imported fields and does not explicitly set/override `tenantId`. This can create cross-tenant rows or rows lacking tenant ownership.
  - _Fix:_ Set conversation tenant explicitly from trusted context during import, e.g. `await storage.createConversation({ ...rest, tenantId: trustedTenantId })`, ignoring any tenantId from the import payload.
- **server/routes.ts:6983** — Import handler creates messages for imported conversations without explicitly setting `tenantId`. Since messages are tenant-scoped and inserts must pass tenantId explicitly, this can break tenant isolation or trust imported tenant ownership.
  - _Fix:_ Pass the trusted tenant on insert, e.g. `await storage.createMessage({ ...msgRest, conversationId: newConv.id, tenantId: trustedTenantId })`, and ignore any tenantId from imported message data.
- **server/routes.ts:6995** — Import handler creates heartbeat tasks from imported data without explicitly setting `tenantId` and spreads imported fields directly. This allows untrusted tenant assignment or missing tenant scoping on a tenant-scoped INSERT.
  - _Fix:_ Explicitly set `tenantId` from trusted context when importing heartbeat tasks, e.g. `await storage.createHeartbeatTask({ ...rest, tenantId: trustedTenantId })`, and discard any tenantId from the import payload.
- **server/routes/admin-zombie-detector.ts:38** — SELECT from tenant-scoped table agent_trace_spans has no tenant_id constraint. The query aggregates tool usage across all tenants, violating tenant isolation.
  - _Fix:_ Add a tenant filter to the used CTE, e.g. `WHERE tenant_id = ${tenantId} AND tool_name IS NOT NULL ...`, or ensure the underlying function is intentionally global and moved behind a clearly global/admin-only data path.
- **server/routes/admin-zombie-detector.ts:54** — SELECT from tenant-scoped table conversations has no tenant_id constraint. Persona usage is computed across all tenants, leaking cross-tenant activity metadata.
  - _Fix:_ Constrain the conversations query with `tenant_id = ${tenantId}` in the used CTE before grouping by persona_id.
- **server/routes/admin.ts:954** — SELECT from tenant-scoped table heartbeat_tasks has no tenant_id constraint. The admin silent-failures endpoint returns disabled/failing heartbeat tasks across all tenants.
  - _Fix:_ Add `tenant_id = ${tenantId}` to the heartbeat_tasks WHERE clause, or if this is meant to be truly global, query a non-tenant-scoped table/view instead.
- **server/routes/agent-insights.ts:43** — SELECT from tenant-scoped table personas has no tenant_id constraint in scorecards. This exposes personas for all tenants to the caller.
  - _Fix:_ Filter personas by the caller tenant, e.g. `FROM personas WHERE tenant_id = ${tenantId} ORDER BY id ASC`.
- **server/routes/agent-insights.ts:208** — SELECT from tenant-scoped table personas has no tenant_id constraint in capability-map. The response includes persona data across all tenants.
  - _Fix:_ Add `tenant_id = ${tenantId}` to the personas query so only the caller's tenant personas are returned.
- **server/routes/conversations.ts:119** — UPDATE on tenant-scoped table conversations uses only WHERE id = <conv.id> and omits tenant_id scoping when attaching a project to a newly created conversation. This relies on prior application logic rather than enforcing tenant isolation in the SQL itself.
  - _Fix:_ Constrain the update with tenant ownership in SQL, e.g. UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}, or perform the update via a storage method that always scopes by tenant.
- **server/routes/conversations.ts:123** — INSERT into tenant-scoped join table project_conversations does not set tenant_id. Per the isolation invariant, every insert into a tenant-scoped table must explicitly provide tenant_id.
  - _Fix:_ Include tenant_id in the insert, e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}), and ensure downstream reads/writes also scope by tenant_id.
- **server/routes/conversations.ts:257** — INSERT into tenant-scoped table project_notes does not set tenant_id when archiving a deleted conversation into project notes. This violates the requirement that every insert explicitly provide tenant_id.
  - _Fix:_ Insert tenant_id explicitly, e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0, 8000)}, ${'system:archive'}).
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table inbox_messages lacks tenant_id constraint. The row is first selected with tenant_id, but the subsequent UPDATE only filters by id, violating the invariant that every write must be constrained in SQL by tenant_id.
  - _Fix:_ Add tenant_id to the UPDATE predicate, e.g. `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`.
- **server/routes/persona-cost.ts:40** — Tenant-scoped query on agent_activity uses `tenantId` sourced from `req.tenantId || req.user?.tenantId` instead of the trusted tenant resolver helper. This can mis-scope reads if request/user fields are stale, derived, or attacker-influenced.
  - _Fix:_ Resolve tenant exclusively via a trusted helper (e.g. getTenantFromRequest/requirePlatformAdmin-returned tenant context) and keep the SQL filter as `WHERE tenant_id = <trusted tenant>`.
- **server/routes/projects.ts:400** — INSERT into tenant-scoped table project_conversations does not set tenant_id. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly; relying on project_id/conversation_id linkage is insufficient without DB enforcement.
  - _Fix:_ Add tenant_id to the INSERT and persist the caller tenant explicitly, e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${targetProjectId}, ${conversationId}, ${tenantId}).
- **server/routes/projects.ts:568** — INSERT into tenant-scoped table project_notes does not set tenant_id. Although the parent project is ownership-checked first, the write itself omits the tenant isolation column required by the invariant.
  - _Fix:_ Include tenant_id in the INSERT, e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}).
- **server/routes/projects.ts:608** — INSERT into tenant-scoped table project_files does not set tenant_id in the base64 upload route. Parent project ownership is checked, but the tenant-scoped row is inserted without explicit tenant binding.
  - _Fix:_ Include tenant_id in the INSERT, e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}).
- **server/routes/projects.ts:616** — UPDATE against tenant-scoped table projects lacks a tenant_id predicate in the files-base64 upload route. The route verifies project ownership earlier, but the write itself is unconstrained and could touch another tenant's row if ids are reused or earlier checks drift.
  - _Fix:_ Constrain the UPDATE with tenant_id, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:677** — INSERT into tenant-scoped table project_files does not set tenant_id in the multipart upload route. The parent project is checked, but the inserted row is not explicitly bound to the caller tenant.
  - _Fix:_ Include tenant_id in the INSERT, e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId}).
- **server/routes/projects.ts:685** — UPDATE against tenant-scoped table projects lacks a tenant_id predicate in the multipart upload route. Prior ownership verification does not satisfy the invariant for the write itself.
  - _Fix:_ Add tenant_id to the WHERE clause, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:704** — SELECT from tenant-scoped table project_files is filtered only by project_id, with no tenant_id constraint in the WHERE clause. Checking the parent project earlier is not sufficient under the stated invariant.
  - _Fix:_ Filter the file query by tenant_id directly, or join through projects and constrain p.tenant_id = ${tenantId}, e.g. SELECT pf.* FROM project_files pf JOIN projects p ON p.id = pf.project_id WHERE pf.project_id = ${projectId} AND pf.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}.
- **server/routes/projects.ts:727** — SELECT from tenant-scoped table project_files in the download route lacks a tenant_id predicate; it filters by project_id and file_path only. The earlier project ownership check does not meet the invariant for this read.
  - _Fix:_ Add tenant_id to the file lookup WHERE clause, e.g. SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} AND file_path = ${'uploads/' + filename} LIMIT 1.
- **server/routes/projects.ts:756** — DELETE from tenant-scoped table project_files lacks a tenant_id predicate. The route verifies the parent project, but the delete itself is not constrained by tenant_id as required.
  - _Fix:_ Add tenant_id to the DELETE WHERE clause, e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}.
- **server/routes/research.ts:97** — INSERT into tenant-scoped table research_programs accepts personaId from request body and writes it without first verifying the caller's tenant owns that persona. This allows attaching a foreign id without ownership validation.
  - _Fix:_ Before inserting, if personaId is provided, query personas with WHERE id = ${personaId} AND tenant_id = ${tenantId} (or equivalent ownership rule) and reject if not found; then proceed with the INSERT.
- **server/routes/research.ts:114** — UPDATE to tenant-scoped table research_programs accepts personaId from request body and writes it without verifying the referenced persona belongs to the caller's tenant.
  - _Fix:_ If personaId is provided, validate ownership with a tenant-scoped lookup before the UPDATE, e.g. SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}, then only persist approved ids.
- **server/routes/research.ts:294** — INSERT into tenant-scoped table research_schedules accepts programId from request body and writes it without first verifying the caller's tenant owns that research program.
  - _Fix:_ Before inserting, if programId is provided, verify it with a tenant-scoped query such as SELECT 1 FROM research_programs WHERE id = ${programId} AND tenant_id = ${tenantId}; reject invalid or cross-tenant ids.
- **server/routes/research.ts:312** — UPDATE to tenant-scoped table research_schedules accepts programId from request body and writes it without validating that the referenced research program belongs to the caller's tenant.
  - _Fix:_ If programId is being changed, first verify ownership with WHERE id = ${programId} AND tenant_id = ${tenantId}; reject the request if the program is not tenant-owned before executing the UPDATE.
- **server/sculptor.ts:105** — UPDATE on tenant-scoped table sculptor_sessions uses only WHERE id = session.id with no tenant_id constraint. Although session.id was created earlier in this flow, the invariant requires every write against a tenant-scoped table to include tenant scoping in the WHERE clause.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}.
- **server/sculptor.ts:112** — UPDATE on tenant-scoped table sculptor_sessions sets conversation_id using only WHERE id = session.id with no tenant_id constraint.
  - _Fix:_ Constrain the write by tenant as well, e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}.
- **server/sculptor.ts:167** — UPDATE on tenant-scoped table sculptor_sessions in monitorSession uses only WHERE id = sessionId with no tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause, e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}.
- **server/sculptor.ts:358** — UPDATE on tenant-scoped table sculptor_sessions writes review_result using only WHERE id = sessionId with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}.
- **server/second-opinion.ts:501** — UPDATE on tenant-scoped table agent_cost_ledger settles a reservation row by id only, with no tenant_id constraint in the WHERE clause.
  - _Fix:_ Include tenant_id in the settlement WHERE clause, and pass the trusted caller tenant into settleFusionReservation so it updates WHERE id = ${reservationId} AND tenant_id = ${tenantId}.
- **server/seed.ts:43** — SELECT against tenant-scoped table research_sessions counts all rows with no tenant_id constraint (`SELECT COUNT(*) as count FROM research_sessions`). This can mix data across tenants and changes control flow based on other tenants' data.
  - _Fix:_ Scope the count to the intended tenant, e.g. `SELECT COUNT(*) as count FROM research_sessions WHERE tenant_id = ${tenantId}` or Drizzle `.where(eq(researchSessions.tenantId, tenantId))`.
- **server/seed.ts:2253** — INSERT into tenant-scoped join table project_conversations does not set tenant_id. The invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly.
  - _Fix:_ Include `tenant_id` in the insert values, ideally from the verified conversation/project tenant: `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${r.tenant_id}, ${r.project_id}, ${r.id}) ...` and ensure the source query selects a single verified tenant.
- **server/seed.ts:2836** — SELECT against tenant-scoped table governance_frameworks is hard-coded to `tenant_id = 1`, and subsequent inserts seed tenant-scoped data only for tenant 1. This bypasses caller/tenant scoping and would be unsafe if reused outside owner-only bootstrap logic.
  - _Fix:_ If this table is tenant-scoped, only access it through a validated tenant context and use `WHERE tenant_id = ${tenantId}` consistently. If this is intended to be owner-only/system data, move it to a genuinely global/shared table or document/enforce it as admin-only code path.
- **server/seed.ts:3397** — SELECT against tenant-scoped table conversation_templates has no tenant_id constraint, and subsequent INSERTs into conversation_templates do not set tenant_id. This breaks tenant isolation and can create rows with missing or incorrect tenant ownership.
  - _Fix:_ Scope the read by tenant_id (e.g. where(eq(conversationTemplates.tenantId, ADMIN_TENANT_ID))) and include tenantId explicitly on every inserted template row using the owning tenant's id.
- **server/seed.ts:3655** — DELETE from tenant-scoped table heartbeat_tasks removes rows based only on enabled/description/type with no tenant_id constraint, affecting all tenants.
  - _Fix:_ Add a tenant_id predicate to the DELETE (for example restrict to the intended system tenant) or iterate per tenant explicitly with WHERE tenant_id = <owned tenant>.
- **server/seed.ts:3683** — UPDATE against tenant-scoped table memory_entries modifies rows based on content only and lacks any tenant_id constraint, so it can rewrite data across all tenants.
  - _Fix:_ Add WHERE tenant_id = <intended tenant> for the normalization scope, or run the migration per tenant with explicit tenant ownership filtering.
- **server/seed.ts:3709** — UPDATE against tenant-scoped table conversations changes model for all matching rows without a tenant_id constraint, affecting every tenant's conversation data.
  - _Fix:_ Constrain the UPDATE by tenant_id for the intended tenant, or perform a controlled migration that processes each tenant explicitly.
- **server/seed.ts:3905** — INSERT into tenant-scoped table project_notes does not set tenant_id, and uses project_id from a prior query without carrying tenant ownership into the inserted row.
  - _Fix:_ Include tenantId explicitly in the INSERT (matching the verified owning tenant of the project, here tenant 1) and prefer verifying/project-loading with tenant scope before inserting child rows.
- **server/seed.ts:3950** — UPDATE against tenant-scoped table personas uses only id in the WHERE clause and has no tenant_id constraint, so it can update matching persona ids across tenants.
  - _Fix:_ Add tenant_id to the WHERE clause for the intended tenant, or execute the backfill separately per tenant with explicit tenant scoping.
- **server/self-improvement.ts:427** — UPDATE against tenant-scoped table experiments constrains only by experiments.id and omits tenant_id in the WHERE clause. If an experiment id from another tenant were ever referenced here, this would write across tenant boundaries.
  - _Fix:_ Constrain the update with both id and tenantId, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId!))).
- **server/self-improvement.ts:561** — SELECT from tenant-scoped table experiments can run with no tenant_id filter when tenantId === ALL_TENANTS, returning cross-tenant data. The invariant requires every tenant-scoped read to be constrained by tenant_id.
  - _Fix:_ Remove the ALL_TENANTS path for experiments or restrict this function to admin-only/global-safe usage outside tenant-scoped tables. Always include experiments.tenantId = caller tenant in the WHERE clause for tenant callers.
- **server/self-reflection.ts:380** — UPDATE against tenant-scoped table memory_entries updates by id only after a prior tenant-scoped SELECT. Because tenant isolation relies solely on per-query SQL filters, this write must also include tenant_id in its WHERE clause.
  - _Fix:_ Add tenant_id to the UPDATE predicate, e.g. WHERE id = ... AND tenant_id = ${tenantId}.
- **server/sessions.ts:67** — Handler uses conv.personaId derived from tenant-scoped conversation data to read a persona via storage.getPersona(conv.personaId) without first verifying the caller's tenant owns that persona. This violates the rule against dereferencing foreign ids without ownership validation.
  - _Fix:_ Verify the persona is assigned/owned by the caller's tenant before reading it, or use a tenant-scoped storage method that enforces ownership in SQL.
- **server/situation-room.ts:186** — Active task aggregation adds heartbeat tasks to persona workloads without filtering task.tenantId, unlike getActiveWorkState which does filter. This can leak cross-tenant activity into a tenant's situation snapshot.
  - _Fix:_ Skip tasks whose tenantId is present and does not match the requested tenantId before counting or exposing them in workloads.
- **server/skillify.ts:91** — SELECT from tenant-scoped messages table is filtered only by conversation_id without a tenant_id constraint. A conversationId from another tenant could cause cross-tenant message disclosure if ids are guessed or reused.
  - _Fix:_ Constrain the messages query by tenant ownership, e.g. join/filter through conversations with tenant_id = tenantId, or add a direct tenant_id predicate if messages carries tenant_id.
- **server/skillify.ts:201** — SELECT from tenant-scoped messages table in analyzeConversation is filtered only by conversation_id without a tenant_id constraint. Tenant isolation relies on application filters, so this read can leak another tenant's messages for a referenced conversation.
  - _Fix:_ Add tenant scoping to the messages read, such as joining conversations and requiring conversations.tenantId = tenantId, or filtering messages.tenantId = tenantId if available.
- **server/skills-marketplace.ts:209** — SELECT against tenant-scoped skills table has no tenant_id constraint. Checking for an existing skill by name globally can disclose or interfere with other tenants' installed skills.
  - _Fix:_ Require tenant context and filter by tenant_id in the existence check, e.g. WHERE tenant_id = <caller tenant> AND name = <template name>.
- **server/storage.ts:289** — Raw SELECT against tenant-scoped table `messages` fetches citations by message ids without any tenant_id constraint. If `attachCitationMetadata` is ever called with ids from mixed or unverified sources, it can read cross-tenant message data.
  - _Fix:_ Constrain the query with tenant ownership, e.g. include `AND tenant_id = <caller tenant>` and pass the caller tenantId into `attachCitationMetadata`, or only query through an already tenant-scoped join/path.
- **server/storage.ts:300** — `createMessage` takes foreign `conversationId` from input and reads `conversations` by id only, without first verifying ownership via caller tenant. It then derives tenantId from that row and inserts into `messages`, allowing writes against any conversation id that exists.
  - _Fix:_ Require a caller tenantId parameter and verify the parent with `WHERE id = <conversationId> AND tenant_id = <caller tenant>` before inserting. Reject if the conversation is not owned by the caller tenant.
- **server/storage.ts:680** — INSERT into tenant-scoped table `conversation_facts` does not validate or prove that the supplied foreign `conversationId` belongs to `data.tenantId` before writing. A caller can potentially write facts linked to another tenant's conversation id while setting their own tenant_id.
  - _Fix:_ Before insert, verify `SELECT 1 FROM conversations WHERE id = data.conversationId AND tenant_id = data.tenantId`, or call a shared ownership guard, and reject on mismatch.
- **server/storage.ts:716** — UPDATE against tenant-scoped table `conversation_facts` changes rows by id list only, with no tenant_id constraint in the WHERE clause. `victims` was selected with tenant scope, but the write itself is unscoped and relies on prior selection rather than enforcing tenant isolation at the mutation site.
  - _Fix:_ Add `AND tenant_id = <tenantId>` to the UPDATE WHERE clause, e.g. `where(and(inArray(conversationFacts.id, victimIds), eq(conversationFacts.tenantId, tenantId)))`.
- **server/storage.ts:742** — `upsertDailyNote` updates `daily_notes` by primary key only after calling `getDailyNote` with optional tenantId/personaId. Because tenantId is optional, this path can mutate another tenant's note when the same date/persona exists across tenants.
  - _Fix:_ Make tenantId mandatory for daily-note upserts on this tenant-scoped table, and include tenant_id in both the lookup and the UPDATE WHERE clause.
- **server/storage.ts:745** — INSERT into tenant-scoped table `daily_notes` does not enforce presence of a valid explicit tenantId. Per the invariant, every insert into a tenant-scoped table must set tenant_id explicitly and fail closed if missing/invalid.
  - _Fix:_ Validate `data.tenantId` as a positive integer before insert and reject if absent/invalid.
- **server/storage.ts:960** — SELECT from tenant-scoped table `memory_entries` in `getMemoriesWithoutEmbeddings` has no tenant_id constraint, returning rows across all tenants.
  - _Fix:_ Require a tenant scope parameter and filter with `eq(memoryEntries.tenantId, tenantId)` unless this is an explicitly platform-wide admin path using a dedicated sentinel and audited caller set.
- **server/storage.ts:971** — SELECT from tenant-scoped table `agent_knowledge` in `getKnowledgeWithoutEmbeddings` has no tenant_id constraint, returning rows across all tenants.
  - _Fix:_ Require a tenant scope parameter and filter with `eq(agentKnowledge.tenantId, tenantId)` unless this is an explicitly platform-wide admin path using a dedicated sentinel and audited caller set.
- **server/storage.ts:979** — Raw UPDATE against tenant-scoped table `memory_entries` in `archiveExpiredMemories` has no tenant_id constraint, mutating rows across all tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause for tenant-local operation, or make this an explicit platform-admin/all-tenants maintenance path with clear sentinel-based authorization and separation from tenant request flows.
- **server/storage.ts:995** — Raw UPDATE against tenant-scoped table `memory_entries` in `archiveStaleMemories` has no tenant_id constraint, mutating rows across all tenants.
  - _Fix:_ Add tenant scoping to the WHERE clause for tenant-local operation, or make this an explicit platform-admin/all-tenants maintenance path with clear sentinel-based authorization and separation from tenant request flows.
- **server/storage.ts:1008** — SELECT from tenant-scoped table `heartbeat_logs` in `pruneHeartbeatLogs` has no tenant ownership constraint, and the subsequent DELETE also acts only by ids. This performs cross-tenant reads/writes on a tenant-scoped dataset.
  - _Fix:_ Require tenant scope and constrain both the SELECT and DELETE via the parent task tenant relation (e.g. `task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = <tenantId>)`), unless this is an explicit platform-wide admin maintenance path.
- **server/storage.ts:1136** — UPDATE against tenant-scoped table `heartbeat_tasks` in `markHeartbeatTaskRun` uses only task id with no tenant_id constraint. Any caller with a foreign task id could mutate another tenant's task state.
  - _Fix:_ Require tenantId and scope the update with `WHERE id = <id> AND tenant_id = <tenantId>`.
- **server/storage.ts:1553** — `supersedeAdr` first verifies both ADR rows belong to the caller tenant, but then performs two UPDATEs by id only without tenant_id constraints. The mutation sites themselves are unscoped on a tenant-scoped table.
  - _Fix:_ Add `AND tenant_id = <tenantId>` to both UPDATE WHERE clauses so tenant isolation is enforced directly on each write.
- **server/subagents.ts:265** — Reads a persona by foreign id (agentId derived from caller params/parent conversation) via storage.getPersona(agentId) without first verifying the persona belongs to the caller's tenant. This can enable cross-tenant persona access if getPersona is not tenant-scoped internally.
  - _Fix:_ Require a tenant-scoped persona lookup, e.g. storage.getPersona(agentId, parentConv.tenantId), or first query the persona with WHERE id = agentId AND tenant_id = parentConv.tenantId and reject if not found.
- **server/subagents.ts:818** — killSubagent resolves and mutates a subagent run by id from the global in-memory activeRuns map with no tenant ownership check. A caller who can supply another tenant's run id could cancel that tenant's run.
  - _Fix:_ Thread requester tenantId into killSubagent and verify run.tenantId === requesterTenantId before mutating, or key activeRuns by tenant and id.
- **server/subagents.ts:830** — killAllSubagents can cancel runs globally or by parentConversationId without any tenant authorization check. A caller could cancel other tenants' runs by invoking it or by passing a foreign parentConversationId.
  - _Fix:_ Require requester tenantId and restrict cancellation to runs where run.tenantId matches. If filtering by parentConversationId, first verify the parent conversation belongs to the requester tenant.
- **server/subagents.ts:843** — getSubagentInfo returns subagent run details by run id from a global in-memory map with no tenant ownership validation. This can leak another tenant's task, result, conversation ids, and tool usage.
  - _Fix:_ Require requester tenantId and return info only when run.tenantId matches the caller's tenant.
- **server/tool-learning.ts:286** — UPDATE on tenant-scoped custom_tools in tryAndRevert scopes only by id and omits tenant_id. This violates the required tenant filter invariant for writes.
  - _Fix:_ Thread tenantId into tryAndRevert/revert flow and update with .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))) or equivalent.
- **server/tool-learning.ts:311** — DELETE on tenant-scoped custom_tools in revertToolRow scopes only by id and omits tenant_id. This violates the required tenant filter invariant for deletes.
  - _Fix:_ Include tenant_id in the delete predicate, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))).
- **server/tool-learning.ts:319** — INSERT into tenant-scoped experiments does not set tenant_id at all. Per the invariant, tenant-scoped inserts must pass tenantId explicitly.
  - _Fix:_ Determine the owning tenant for the experiment (e.g. the custom tool's tenant) and include tenantId in the inserted values. Thread tenantId into logToolExperiment and all callers.
- **server/tools.ts:2815** — INSERT into tenant-scoped join table project_conversations does not set tenant_id. The invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly; this insert relies only on project_id/conversation_id and does not stamp tenant ownership.
  - _Fix:_ Add tenant_id to the INSERT for project_conversations and pass the caller tenant explicitly, e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ..., tenantId).
- **server/tools.ts:2941** — INSERT into project_files does not set tenant_id. Although project ownership is checked first, the invariant requires every INSERT into tenant-scoped tables to explicitly include tenant_id.
  - _Fix:_ Include tenant_id in the project_files INSERT and set it to the caller tenantId after validating project ownership.
- **server/tools.ts:2955** — INSERT into project_notes does not set tenant_id. The handler verifies project ownership, but tenant-scoped table inserts must still explicitly stamp tenant_id.
  - _Fix:_ Add tenant_id to the project_notes INSERT and set it from the validated caller tenantId.
- **server/tools.ts:2979** — INSERT into project_conversations does not set tenant_id. Even though the project and conversation are ownership-checked, the invariant requires explicit tenant_id on every INSERT into tenant-scoped tables.
  - _Fix:_ Include tenant_id in the project_conversations INSERT and set it to tenantId.
- **server/tools.ts:4543** — SELECT against tenant-scoped conversations/project_conversations uses caller-supplied conversation id without any tenant_id constraint in the SQL. Although assertConversationInTenant(convId, params._tenantId) runs first, the query itself is not constrained by tenant_id, violating the application-level tenant filter invariant for reads.
  - _Fix:_ Add tenant-scoped WHERE clauses to both subqueries, e.g. constrain conversations by both id and tenant_id, and project_conversations via a join or prior ownership-checked project lookup scoped to the same tenant.
- **server/tools.ts:4554** — INSERT into tenant-scoped project_notes does not set tenant_id explicitly. The invariant requires every INSERT into tenant-scoped tables to provide tenant_id explicitly, and there is no DB default/RLS to enforce isolation.
  - _Fix:_ Include tenant_id in the INSERT column list and values, using the validated caller tenant (e.g. params._tenantId) after confirming the project belongs to that tenant.
- **server/tools.ts:4558** — INSERT into tenant-scoped project_files does not set tenant_id explicitly. This can create unscoped rows or break tenant isolation because inserts must stamp tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT columns and values, using the authenticated tenant after project ownership validation.
- **server/tools.ts:6474** — INSERT into tenant-scoped project_files uses only project_id after an ownership check, but does not set tenant_id explicitly. The invariant requires explicit tenant_id on every INSERT into tenant-scoped tables.
  - _Fix:_ Insert tenant_id alongside project_id and other fields, using params._tenantId after assertProjectInTenant succeeds.
- **server/tools.ts:7015** — INSERT into tenant-scoped table project_files does not set tenant_id. This code verifies project ownership with assertProjectInTenant(params.project_id, params._tenantId) but then inserts only project_id/file fields via raw SQL, violating the invariant that every INSERT into a tenant-scoped table must explicitly pass tenantId.
  - _Fix:_ Include tenant_id in the INSERT and bind it to params._tenantId (or a validated local tenantId), e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Keep the ownership check before insert.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table browser_workflows constrains only by id and omits tenant_id. The workflow row was previously selected with tenant_id, but the write itself is not tenant-scoped, violating the invariant that every write must include tenant isolation in SQL.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`.
- **server/tools/domains/files/handlers.ts:942** — INSERT into tenant-scoped table project_files does not set tenant_id explicitly. The code inserts only project_id, file metadata, and uploaded_by, relying on project_id linkage instead of the required application-level tenant_id column.
  - _Fix:_ Include tenant_id in the INSERT for project_files and set it from the trusted caller tenant/context (or the verified project's tenant), e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}).
- **server/tools/domains/files/handlers.ts:946** — UPDATE on tenant-scoped table projects constrains only by id and omits a tenant_id predicate. Although projId was previously derived via a tenant-scoped conversation/project join, this write itself is not tenant-filtered, violating the invariant that every write must include tenant_id scoping in the WHERE clause.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}.
- **server/tools/domains/memory/handlers.ts:398** — graphMemoryHandler trusts caller-supplied params.persona_id and threads it directly into SELECT/DELETE/INSERT logic on tenant-scoped graph_memory rows. This lets a caller operate on another persona's graph-memory rows within the tenant without first verifying ownership/authorization of that persona id.
  - _Fix:_ Do not accept persona_id blindly from params. Resolve persona scope from trusted context or first load/verify the referenced persona belongs to and is accessible by the caller's tenant before using it in graph_memory queries/mutations.
- **server/tools/domains/messaging/handlers.ts:48** — sendMessageHandler accepts a foreign conversationId from caller input and forwards it to deliverMessage without first verifying the caller's tenant owns that conversation. If the downstream delivery path reads or annotates the conversation, this is an IDOR/isolation risk.
  - _Fix:_ Before passing conversationId onward, load the conversation with a tenant-scoped lookup (WHERE id = ? AND tenant_id = ctx.tenantId) and reject if not found. Prefer using trusted conversation context instead of raw caller-supplied ids.
- **server/tools/domains/quality/handlers.ts:124** — critiqueResponseHandler forwards ctx.tenantId without enforcing it is present. If critiqueToolForAgent falls back to unscoped/global behavior when tenantId is undefined, tenant isolation can be bypassed.
  - _Fix:_ Fail closed before calling critiqueToolForAgent: require a valid numeric ctx.tenantId and pass only that trusted tenant id.
- **server/tools/domains/strategic-memory/handlers.ts:103** — UPDATE against tenant-scoped table memory_entries uses only WHERE id = ${id} after a prior SELECT by tenant/persona. Tenant isolation is enforced only at the SQL statement level, so this write is not constrained by tenant_id in its own WHERE clause.
  - _Fix:_ Constrain the UPDATE directly with tenant_id (and persona_id if applicable), e.g. WHERE id = ${id} AND tenant_id = ${tid} AND persona_id = ${personaId}.
- **server/tools/domains/strategic-memory/handlers.ts:164** — UPDATE against tenant-scoped table memory_entries bumps access counters using WHERE id = ANY(...) with no tenant_id constraint. Even though ids were previously selected with tenant filtering, each write must enforce tenant isolation at the SQL statement level.
  - _Fix:_ Add tenant_id = ${tid} (and persona_id = ${personaId} if appropriate) to the UPDATE WHERE clause alongside id = ANY(...).
- **server/tools/domains/strategic-memory/handlers.ts:263** — UPDATE against tenant-scoped table memory_entries uses only WHERE id = ${id} after a prior tenant-scoped SELECT. This violates the requirement that every write to a tenant-scoped table include a tenant_id filter in the statement itself.
  - _Fix:_ Change the UPDATE to include tenant_id = ${tid} and persona_id = ${personaId} in the WHERE clause.
- **server/twilio.ts:102** — SELECT against tenant-scoped table agent_knowledge reads rows by category/title only, with no tenant_id filter. This lets data from any tenant participate in the pairing lookup and is exactly the cross-tenant ambiguity/hijack surface described in the comments.
  - _Fix:_ Constrain the agent_knowledge lookup by tenant ownership rather than global category/title search. If tenant is not yet known, move pairings to a global/shared table explicitly designed for phone ownership, or derive candidate tenant(s) through a trusted ownership mapping before querying tenant-scoped rows.
- **server/twilio.ts:116** — Handler takes foreign id info.conversationId from data stored in agent_knowledge and queries conversations by id without first constraining the read to a trusted tenant in SQL (SELECT tenant_id FROM conversations WHERE id = ... LIMIT 1). Although the result is compared afterward, the invariant requires tenant isolation at query time for tenant-scoped tables.
  - _Fix:_ Query conversations with both id and tenant_id in the WHERE clause using a trusted tenant value, e.g. WHERE id = <conversationId> AND tenant_id = <claimedTenantId>. More broadly, redesign the pairing resolution so trusted tenant ownership is established before any tenant-scoped table read.
- **server/venture-discovery/budget.ts:158** — UPDATE against tenant-scoped table agent_cost_ledger constrains only by id and does not enforce tenant_id = caller tenant. If reservationId is ever misbound or reused from another tenant, this can modify another tenant's ledger row.
  - _Fix:_ Pass tenantId into settleVentureReservation() and update with WHERE id = ${reservationId} AND tenant_id = ${tenantId}.
- **server/venture-discovery/budget.ts:172** — UPDATE against tenant-scoped table agent_cost_ledger constrains only by id and does not enforce tenant_id = caller tenant. This violates the application-level tenant isolation invariant.
  - _Fix:_ Pass tenantId into releaseVentureReservation() and update with WHERE id = ${reservationId} AND tenant_id = ${tenantId}.
- **server/voice-wake.ts:38** — Voice wake configuration is stored in a single global file and handlers read/write it with no tenant ownership check or tenant scoping. In a multi-tenant app this lets one tenant overwrite and read another tenant's configuration.
  - _Fix:_ Store voice-wake settings in a tenant-scoped table or per-tenant file/key, and require tenantId on reads/writes so each handler filters by the caller's tenant.
- **server/webhook-triggers.ts:168** — Tenant-scoped query joins personas on p.id = w.persona_id without constraining personas by tenant_id. If persona IDs collide or are referenced across tenants, listTriggers can expose another tenant's persona name.
  - _Fix:_ Constrain the join with tenant ownership, e.g. LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = ${tenantId}.
- **server/webhook-triggers.ts:258** — processTriggerEvent reads a tenant-scoped persona via LEFT JOIN personas p ON p.id = w.persona_id using only a foreign key from webhook_triggers, without verifying the trigger's tenant owns that persona row.
  - _Fix:_ Scope the persona join to the trigger tenant, e.g. LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = w.tenant_id, or fetch/verify the persona separately with tenant_id filtering before use.
- **server/webhook-triggers.ts:318** — getOrCreateTriggerConversation falls back to storage.getActivePersona() with no tenant ownership check, then writes that personaId into a tenant-scoped conversation for triggerTenantId. This can attach a foreign/global persona to another tenant's conversation.
  - _Fix:_ Use a tenant-scoped active-persona lookup (or verify the returned persona has tenant_id = triggerTenantId) before assigning personaId to the new conversation.

### MEDIUM (17)

- **server/chat-engine.ts:1625** — buildMemorySection defaults tenantId to 1 (`tenantId: number = 1`). This creates a fail-open path where a caller that omits tenantId will process memory ranking/touching under tenant 1, violating the invariant that tenant scoping must always use the caller's tenant and never default.
  - _Fix:_ Remove the default tenantId of 1 and require an explicit positive tenantId parameter. Add a fail-closed runtime guard that throws if tenantId is null/undefined/non-positive, matching the stricter pattern used elsewhere in this file.
- **server/commitments.ts:135** — SELECT from tenant-scoped table commitments in `scanAndEscalate` has no tenant_id constraint. This performs a cross-tenant read of all overdue commitments.
  - _Fix:_ If cross-tenant processing is intended for a trusted platform job, document and isolate it as an explicit admin/system path; otherwise enumerate tenants and run per-tenant queries or add `WHERE tenant_id = <tenant>` to keep reads tenant-scoped.
- **server/deliverable-verifier.ts:146** — loadContracts() performs an unscoped SELECT over deliverable_contracts with no tenant_id filter. If this table is tenant-scoped, all tenants' contracts are loaded and cached globally.
  - _Fix:_ If deliverable_contracts is tenant-scoped, query with `WHERE tenant_id = <caller tenant>` and make the cache tenant-aware. If it is global by design, document/enforce that in schema and access patterns.
- **server/doc-heading-tree.ts:183** — Join query on tenant-scoped doc_heading_trees left-joins doc_collections by collection_id only, without verifying the joined collection belongs to the same tenant. This can attach cross-tenant collection metadata to a tenant-scoped tree row if ids collide or integrity is broken.
  - _Fix:_ Scope the join by tenant ownership too, e.g. `LEFT JOIN doc_collections c ON c.id = t.collection_id AND c.tenant_id = t.tenant_id`.
- **server/heartbeat.ts:246** — Handler invokes retryPendingMinervaRouting() from a cross-tenant heartbeat context without any visible per-tenant ownership verification or tenant filter in this call site.
  - _Fix:_ Ensure the called function iterates tenants explicitly and applies WHERE tenant_id = <tenant> on all tenant-scoped reads/writes, or change this caller to invoke it per tenant.
- **server/heartbeat.ts:377** — Handler invokes expireStaleApprovals() from a cross-tenant heartbeat context without any visible per-tenant ownership verification or tenant filter in this call site.
  - _Fix:_ Ensure expireStaleApprovals enforces tenant_id on every tenant-scoped query, or refactor this heartbeat path to enumerate tenants and call it per tenant.
- **server/heartbeat.ts:385** — Handler invokes resumePendingApprovalRuns() from a cross-tenant heartbeat context without any visible per-tenant ownership verification or tenant filter in this call site.
  - _Fix:_ Ensure resumePendingApprovalRuns performs tenant-scoped reads/writes with WHERE tenant_id = <tenant>, or invoke it once per enumerated tenant.
- **server/job-queue.ts:377** — getJobTenantForAdmin selects from tenant-scoped table agent_jobs by id with no tenant_id filter. Even though marked admin-only, this is still an unscoped read against a tenant-scoped table.
  - _Fix:_ Use an explicit privileged/admin exception mechanism separate from ordinary tenant-scoped helpers, or require and verify a known admin context before allowing this unscoped lookup. Prefer carrying tenant scope from the caller when possible.
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey selects from tenant-scoped mcp_api_keys by key_prefix only, with no tenant_id predicate. Although tenantId is later derived from the row, this is still an unscoped read against a tenant table and relies on global prefix uniqueness rather than the required per-query tenant filter.
  - _Fix:_ Either treat this table/path as explicitly global-auth by design, or enforce a tenant-safe lookup pattern that first resolves a trusted tenant scope and then queries with `WHERE key_prefix = ... AND tenant_id = ...`. Document the exception if global lookup is intentional.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw() inside a query, even though current input is internally derived. Raw SQL bypasses normal parameterization and is disallowed by the audit rule because it increases injection/isolation risk if the value ever becomes request-derived.
  - _Fix:_ Avoid sql.raw() and pass the advisory lock key as a bound parameter or cast a typed parameter safely in Drizzle, so the query remains fully parameterized.
- **server/quarterly-intelligence.ts:343** — Creates tenant-scoped table `model_registry_updates` without any tenant-isolation enforcement around future access, and then inserts tenant-scoped rows; this file introduces a tenant-scoped table but no evidence of consistent tenant-safe access patterns here.
  - _Fix:_ Ensure every future read/write path for `model_registry_updates` is tenant-scoped with `WHERE tenant_id = <caller tenant>` and document/implement access helpers that always require tenantId.
- **server/routes/conversations.ts:125** — UPDATE on tenant-scoped table projects uses only WHERE id = <projectId> and omits tenant_id in the SQL predicate. Even though project ownership was checked earlier, the invariant requires every write against tenant-scoped tables to be directly constrained by tenant_id.
  - _Fix:_ Add tenant_id to the WHERE clause, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/skillify.ts:223** — Lookup of persona by foreign personaId is not scoped by tenant. Although personaId comes from a tenant-scoped conversation row, the subsequent SELECT on personas lacks tenant verification and can read a persona from another tenant if ids are inconsistent or reassigned.
  - _Fix:_ Filter the persona lookup by both id and tenant_id = tenantId, or verify the persona belongs to the same tenant before reading it.
- **server/storage.ts:1161** — INSERT into `heartbeat_logs` does not set or validate tenant_id, while tenant isolation for this dataset is enforced only transitively via `task_id`. The function accepts a foreign task reference but does not verify the caller tenant owns that task before writing.
  - _Fix:_ Before insert, verify the referenced heartbeat task belongs to the acting tenant (or include explicit tenant_id if schema supports it) and reject cross-tenant task ids.
- **server/tool-learning.ts:130** — SELECT against tenant-scoped custom_tools checks only name and omits tenant_id. Although names are tenant-prefixed for non-admin tenants, tenant isolation is being enforced indirectly rather than by an explicit tenant_id filter as required by the invariant.
  - _Fix:_ Add an explicit tenant filter: .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))) to satisfy the tenant isolation invariant directly.
- **server/tool-learning.ts:156** — Reads back a custom_tools row by id only, without tenant_id constraint. Even though inserted.id is DB-derived, the invariant requires every tenant-scoped read to include tenant_id filtering.
  - _Fix:_ Constrain the read with both id and tenantId: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))).
- **server/webhooks.ts:230** — Webhook agent path calls getClientForModel(hookModel || conv.model) without passing the admin tenant. In this codebase tenant routing for provider keys/subscriptions is application-scoped; omitting tenant context risks using the wrong tenant's provider/account configuration.
  - _Fix:_ Pass ADMIN_TENANT_ID explicitly: getClientForModel(hookModel || conv.model, ADMIN_TENANT_ID).

### LOW (3)

- **server/wiring-invariants.ts:361** — Uses sql.raw() to build SQL strings directly for table-existence and freshness probes. Although current values come from in-code constants, this pattern bypasses parameterization and is explicitly disallowed because any future request-derived value here would create SQL injection and tenant-isolation risk.
  - _Fix:_ Replace sql.raw() with parameterized Drizzle queries or strictly validated identifier helpers. Keep all dynamic values from trusted allowlisted constants and avoid raw SQL construction entirely.
- **server/wiring-invariants.ts:366** — Uses sql.raw() to interpolate table and column identifiers into SQL for subsystem freshness checks. This raw-SQL pattern is prohibited and weakens safety guarantees around tenant isolation and injection prevention.
  - _Fix:_ Refactor to use Drizzle schema objects/query builder for each known table, or map allowlisted table specs to prewritten parameterized queries instead of constructing SQL with sql.raw().
- **server/wiring-invariants.ts:535** — Uses sql.raw() to construct SQL for memory-growth checks. Even though table names currently come from constants, raw SQL with interpolation is an unsafe pattern called out by the audit invariant.
  - _Fix:_ Replace with predeclared queries per allowlisted table or Drizzle builder-based count/size queries; avoid sql.raw() for dynamically assembled statements.

### Resolved — confirmed false positives (200)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/agentic-features.ts:842** _(fk-transitive)_ — INSERT into tenant-scoped outreach_sequence_steps does not set tenant_id. This violates the invariant that every insert into tenant-scoped tables must explicitly write tenantId.
  - _Why false positive:_ outreach_sequence_steps has no tenant_id and is a child of tenant-scoped outreach_sequences via sequence_id
- **server/embeddings.ts:855** _(system-sweep)_ — UPDATE against tenant-scoped table `memory_entries` in `backfillEmbeddingVecs()` uses only `WHERE id = ${row.id}` with no `tenant_id` constraint.
  - _Why false positive:_ backfillEmbeddingVecs is a cross-tenant maintenance backfill updating only housekeeping embedding_vec on rows it selected
- **server/embeddings.ts:872** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` in `backfillEmbeddingVecs()` uses only `WHERE id = ${row.id}` with no `tenant_id` constraint.
  - _Why false positive:_ backfillEmbeddingVecs is a platform-wide maintenance backfill updating only embedding_vec on selected rows, not returning mixed tenant data
- **server/job-queue.ts:159** _(system-sweep)_ — claimDueJobs updates and returns rows from tenant-scoped table agent_jobs with no tenant_id constraint. This background worker can claim jobs across all tenants without an explicit tenant filter, violating the invariant that every read/write on tenant-scoped tables must be constrained by tenant_id.
  - _Why false positive:_ claimDueJobs is a platform-wide worker claim loop; rows keep their own tenant_id and no tenant data is mixed into one tenant-facing output
- **server/job-queue.ts:195** _(system-sweep)_ — reclaimExpiredLeases performs UPDATEs on tenant-scoped table agent_jobs with no tenant_id constraint in either query, mutating jobs across all tenants without explicit tenant scoping.
  - _Why false positive:_ reclaimExpiredLeases is housekeeping across all tenants mutating only job status/lease fields, not exposing mixed tenant content
- **server/knowledge-diversity-monitor.ts:266** _(system-sweep)_ — runKnowledgeDiversityMonitor reads from tenant-scoped table agent_knowledge across all tenants with no tenant_id filter. The invariant requires every read/write on tenant-scoped tables to include tenant_id = caller tenant.
  - _Why false positive:_ monitor intentionally scans all tenant/persona pairs then processes each pair with its own tenant_id and records per-tenant snapshots
- **server/memory-graph.ts:275** _(fk-transitive)_ — INSERT into tenant-scoped table memory_links does not set tenant_id. The invariant requires every tenant-scoped insert to pass tenantId explicitly.
  - _Why false positive:_ memory_links has no tenant_id and links tenant-scoped memory_entries by foreign keys, so isolation is transitive through the parent memories
- **server/skills-marketplace.ts:235** _(global-table)_ — SELECT against tenant-scoped skills table uses caller-supplied skillId with no tenant ownership verification. This allows exporting another tenant's skill by id.
  - _Why false positive:_ Schema note says skills has no tenant_id; this read is from a platform-global table, not tenant-scoped.
- **server/tools/domains/cmmc-prospecting/handlers.ts:157** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id explicitly. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly because columns are NOT NULL with no default and isolation is application-enforced.
  - _Why false positive:_ project_files has no tenant_id and is isolated via tenant-scoped parent projects checked in JOIN/WHERE EXISTS.
- **server/agent-activity.ts:61** _(global-table)_ — SELECT from tenant-scoped personas table by personaId without a tenant_id constraint. A personaId from another tenant could be read and cached.
  - _Why false positive:_ Schema note says personas has no tenant_id, so this is not a tenant-scoped table read.
- **server/agent-desk.ts:297** _(global-table)_ — SELECT from tenant-scoped personas table has no tenant_id filter when building desk overview, exposing persona names across tenants.
  - _Why false positive:_ Schema note says personas has no tenant_id; selecting names from a global table is not cross-tenant isolation failure.
- **server/agent-eval.ts:221** _(internal-worker-pk)_ — UPDATE to agent_evals uses only id with no tenant_id constraint. If evalId were wrong or reused, this could update another tenant's eval row.
  - _Why false positive:_ evalId is a server-generated primary key returned from the immediately preceding INSERT in the same function, not from an untrusted request boundary.
- **server/agentic-engines.ts:816** _(global-table)_ — SELECT from tenant-scoped personas table has no tenant_id filter in runDecisionEngine, so active personas from other tenants may be read into this tenant's analysis prompt.
  - _Why false positive:_ Schema note says personas has no tenant_id; this is a global table read, not tenant-scoped leakage by missing tenant filter.
- **server/agentic-features.ts:862** _(upstream-verified)_ — Reads foreign sequenceId from caller input and queries outreach_enrollments without tenant validation on the enrollment lookup. The duplicate-enrollment check filters only by sequence_id/contact_email/status, not tenant_id.
  - _Why false positive:_ Sequence ownership is verified immediately above via outreach_sequences id+tenant_id; duplicate check is scoped by trusted tenant-owned sequenceId.
- **server/agentic-features.ts:916** _(fk-transitive)_ — Reads outreach_sequence_steps by foreign sequence_id without tenant ownership verification and without tenant_id filter. A sequence step from another tenant could be selected if sequence_id is mismatched or untrusted.
  - _Why false positive:_ outreach_sequence_steps has no tenant_id and is transitively isolated by tenant-scoped outreach_sequences/enrollments; enrollment rows were loaded with e.tenant_id filter.
- **server/agentic-features.ts:1020** _(upstream-verified)_ — UPDATE against tenant-scoped outreach_enrollments uses only id with no tenant_id constraint. Although row was initially loaded tenant-scoped, the write itself is not tenant-scoped.
  - _Why false positive:_ Row being updated was selected just above from outreach_enrollments with tenant_id = params.tenantId; update uses that trusted row id.
- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — SELECT against tenant-scoped tables agent_runs and agent_approvals has no tenant_id filter in the WHERE clause, so the sweep reads approval/run state across all tenants.
  - _Why false positive:_ This is a platform-wide maintenance sweep over pending/running agent_runs; it carries each row's own tenant_id and does housekeeping/resume state handling, not tenant-facing mixed output.
- **server/agentic/resume-worker.ts:170** _(system-sweep)_ — UPDATE agent_runs is scoped only by run_id and tenant_id values previously read from an unscoped cross-tenant SELECT. Because the initial read spans all tenants, this handler operates on foreign-tenant runs without first establishing authorized tenant ownership.
  - _Why false positive:_ Update is part of the same cross-tenant resume worker sweep and applies per-row using the row-carried tenant_id from the maintenance candidate set.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE/claim on tenant-scoped table agent_wake_schedules in runDueWakes has no tenant_id constraint, so it can read and mutate due wake rows across all tenants in one sweep.
  - _Why false positive:_ Heartbeat worker intentionally claims due wakes across tenants and then emits each row with its own tenant_id; this is per-row housekeeping, not mixed tenant disclosure.
- **server/auto-project.ts:135** _(upstream-verified)_ — SELECT from tenant-scoped messages filters only by conversation_id and omits tenant_id. Although the conversation row was checked earlier, the query itself is not constrained by tenant_id as required by the isolation invariant.
  - _Why false positive:_ Conversation ownership is established earlier in the same transaction with conversations id+tenant_id; messages are then read only for that trusted conversationId.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT from tenant-scoped messages filters only by conversation_id and omits tenant_id. The handler verified conversation ownership earlier, but the query itself still lacks the required tenant_id constraint.
  - _Why false positive:_ assertConversationInTenant plus the subsequent conversations and projects tenant checks in the same call path prove tenant ownership before reading messages for that conversation.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE on tenant-scoped conversations uses foreign ids from a cross-table query and constrains only by conversation id / project_id IS NULL, with no tenant_id predicate in the UPDATE itself.
  - _Why false positive:_ Backfill loop is a maintenance sweep; source rows already join projects to conversations on matching tenant ownership, and the update is housekeeping to set missing project_id.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into tenant-scoped table invoice_items does not set tenant_id. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly; relying on invoice_id linkage is not sufficient without DB enforcement.
  - _Why false positive:_ invoice_items has no tenant_id and is a child table isolated transitively through tenant-scoped invoices via invoice_id.
- **server/ceo-orchestrator.ts:661** _(global-table)_ — SELECT against the tenant-scoped personas table is unconstrained: `db.select().from(personasTable)` loads all personas without a `tenant_id` filter, which can expose cross-tenant data and drive later routing/status updates from another tenant's rows.
  - _Why false positive:_ Schema note says personas has no tenant_id; loading all personas is consistent with a platform-global table.
- **server/channel-routing.ts:30** _(global-table)_ — SELECT from `channel_routes` has no tenant isolation. If `channel_routes` is tenant-scoped, `listChannelRoutes()` returns enabled routes across all tenants because the query only filters on `enabled = true`.
  - _Why false positive:_ channel_routes is created here without tenant_id and keyed globally by UNIQUE(channel), indicating intentional platform-global routing.
- **server/channel-routing.ts:51** _(global-table)_ — DELETE from `channel_routes` is unconstrained by tenant. If this table is tenant-scoped, a caller could remove another tenant's route because deletion is based only on `channel`.
  - _Why false positive:_ DELETE targets a globally keyed channel_routes table with no tenant_id column shown; appears intentionally global.
- **server/channel-routing.ts:53** _(global-table)_ — INSERT/UPSERT into `channel_routes` does not set `tenant_id`. Under the stated invariant, every insert into a tenant-scoped table must explicitly provide tenantId.
  - _Why false positive:_ INSERT/UPSERT into channel_routes matches an intentionally global table definition with no tenant_id and UNIQUE(channel).
- **server/channel-routing.ts:63** _(global-table)_ — SELECT from `channel_routes` is missing a tenant filter. `getPersonaForChannel()` resolves a route by `channel` only, which can return another tenant's persona mapping if the table is tenant-scoped.
  - _Why false positive:_ Lookup by channel on channel_routes matches global routing table semantics; no tenant_id exists in shown schema.
- **server/channel-routing.ts:71** _(global-table)_ — DELETE from `channel_routes` in `removeChannelRoute()` lacks a tenant constraint. If tenant-scoped, this permits cross-tenant deletion by channel value alone.
  - _Why false positive:_ removeChannelRoute deletes from globally keyed channel_routes; table definition indicates platform-global configuration.
- **server/chat-engine/project-context.ts:159** _(fk-transitive)_ — SELECT from tenant-scoped table project_files filters only by project_id and does not include tenant_id. Application-level isolation invariant requires every read against a tenant-scoped table to constrain tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects, with code comment stating projectId was verified for tenant earlier.
- **server/chat-engine/project-context.ts:171** _(fk-transitive)_ — SELECT from tenant-scoped table project_notes filters only by project_id and does not include tenant_id. Relying on prior project ownership verification is insufficient under the stated invariant that every tenant-scoped query must explicitly constrain tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated transitively via project_id to a verified tenant-owned project.
- **server/context-budget.ts:40** _(global-table)_ — `runContextBudgetAudit` calls `storage.getPersonas()` without passing or enforcing tenant ownership. If personas are tenant-scoped, this reads persona data without a tenant filter and can mix cross-tenant prompt data into the report.
  - _Why false positive:_ Schema note says personas has no tenant_id; reading all personas is consistent with global/shared persona definitions.
- **server/data-protection.ts:360** _(fk-transitive)_ — SELECT from tenant-scoped table project_notes filters only by project_id and omits tenant_id. If project_id ownership assumptions ever fail or IDs are reused/mis-associated, notes from another tenant can be included in the backup.
  - _Why false positive:_ project_notes has no tenant_id column and is read per project row already selected with tenant_id = tenantId from projects.
- **server/data-protection.ts:371** _(fk-transitive)_ — SELECT from tenant-scoped table project_files filters only by project_id and omits tenant_id. This can read another tenant's files if the foreign key reference is not independently enforced by tenant ownership.
  - _Why false positive:_ project_files has no tenant_id and is read only for projects already filtered by tenant_id
- **server/data-protection.ts:404** _(upstream-verified)_ — SELECT COUNT(*) from tenant-scoped table messages filters only by conversation_id and omits tenant_id. This creates a cross-tenant existence/count leak for messages on the same conversation_id.
  - _Why false positive:_ conversation ids come from conversations already filtered by tenant_id in the same call path
- **server/debate-engine.ts:126** _(global-table)_ — runDebate accepts a tenantId but calls `storage.getPersonas()` with no tenant scoping or ownership verification. If personas are tenant-scoped, this can load cross-tenant personas and use them in the debate.
  - _Why false positive:_ personas schema shows no tenant_id, so getPersonas reads a platform-global table
- **server/deep-interview.ts:337** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. The row is derived from a tenant-scoped project join, but the invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly.
  - _Why false positive:_ project_notes has no tenant_id and insert is transitively scoped via join to tenant-filtered projects
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE against tenant-scoped table doc_chunks sets embedding by row id only, with no tenant_id constraint. A mismatched or stale row id could update another tenant's chunk embedding.
  - _Why false positive:_ row.id values are selected just above from doc_chunks with the same tenant_id and then updated in the same loop
- **server/embeddings.ts:814** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` in `backfillMissingKnowledgeEmbeddings()` has no `tenant_id` filter. This scans rows across all tenants.
  - _Why false positive:_ boot/backfill worker intentionally scans across tenants and updates each row using its own tenant_id without returning mixed tenant data
- **server/embeddings.ts:845** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` in `backfillEmbeddingVecs()` has no `tenant_id` filter, reading rows across all tenants.
  - _Why false positive:_ maintenance backfill intentionally processes rows across tenants and only mutates embedding housekeeping fields by per-row id
- **server/embeddings.ts:861** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` in `backfillEmbeddingVecs()` has no `tenant_id` filter, reading rows across all tenants.
  - _Why false positive:_ maintenance backfill intentionally processes knowledge rows across tenants and only mutates embedding housekeeping fields by per-row id
- **server/felix-brain.ts:23** _(system-sweep)_ — Boot-time rehydration selects from tenant-scoped table `agent_runs` without any tenant_id filter, loading running/pending rows across all tenants into memory.
  - _Why false positive:_ boot-time rehydration intentionally reads active rows across tenants and stores each under a tenant-carried key without mixing tenant outputs
- **server/firecrawl.ts:260** _(upstream-verified)_ — UPDATE against tenant-scoped table scrapedPages is constrained only by id, not tenant_id. Although the id was previously selected with tenant scoping, the write itself lacks the required tenant filter invariant.
  - _Why false positive:_ existing[0].id was selected immediately above from scrapedPages with matching tenantId in the same call path
- **server/firecrawl.ts:393** _(upstream-verified)_ — UPDATE against tenant-scoped table scrapedPages is constrained only by id, not tenant_id. This violates the invariant that every write must include a tenant_id filter in the write query itself.
  - _Why false positive:_ Row id comes from an immediately preceding SELECT on scrapedPages filtered by tenantId, so the updated row was already tenant-scoped in the same call path.
- **server/google-drive.ts:882** _(global-table)_ — UPDATE against tenant-scoped table tenants is constrained only by id, not tenant_id. Under the stated invariant, every write to a tenant-scoped table must include tenant_id scoping in the WHERE clause.
  - _Why false positive:_ tenants has no tenant_id and is the tenant root/global registry row; update by tenant primary key is expected, not a cross-tenant scoped child-table write.
- **server/google-drive.ts:907** _(upstream-verified)_ — UPDATE against tenant-scoped table projects is constrained only by project id, not tenant_id. Even though tenant ownership was checked earlier, the write query itself violates the required tenant filter invariant.
  - _Why false positive:_ The same function reads the project first and throws on tenant mismatch before this UPDATE, proving ownership earlier in the call path.
- **server/google-workspace.ts:2018** _(global-table)_ — Tenant-scoped table `tenants` is queried with a hardcoded id (`WHERE id = 1`) instead of constraining by the caller's tenant context. This bypasses application-level tenant isolation and reads another tenant's row directly.
  - _Why false positive:_ tenants has no tenant_id and this is a hardcoded platform-owner email lookup for fallback sharing, not a tenant-scoped table access.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` has no tenant_id filter. It scans all tenants' stale proposals, violating tenant isolation.
  - _Why false positive:_ This is a platform-wide TTL maintenance sweep over stale proposals; it only emits admin audit findings and optional housekeeping status changes, not tenant-facing mixed content.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table `code_proposals` archives rows by id array only, with no tenant_id constraint. This can modify proposals belonging to other tenants.
  - _Why false positive:_ Archiving stale proposals by collected ids is part of a global maintenance sweep and only mutates housekeeping status fields across tenants.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table `heartbeat_tasks` has no tenant_id filter. It reads tasks across all tenants.
  - _Why false positive:_ This query is a centralized housekeeping scan for stuck heartbeat tasks and does not return mixed tenant data to tenant-facing surfaces.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE on tenant-scoped table `heartbeat_tasks` disables tasks by id array only, with no tenant_id constraint. This can disable tasks belonging to other tenants.
  - _Why false positive:_ Disabling stuck heartbeat tasks by id is a platform maintenance action that only changes housekeeping state across tenants.
- **server/health-monitor.ts:187** _(global-table)_ — SELECT from tenant-scoped table `tenants` has no tenant_id constraint (`SELECT COUNT(*) ... FROM tenants WHERE is_active = true`). This reads across all tenants and breaks the app-level isolation invariant.
  - _Why false positive:_ tenants has no tenant_id and this is a global health metric reporting active tenant count
- **server/heartbeat-schedules.ts:60** _(system-sweep)_ — Cross-tenant SELECT from tenant-scoped table research_schedules has no tenant_id constraint. This reads all tenants' schedules without an explicit per-tenant ownership filter.
  - _Why false positive:_ heartbeat maintenance sweep processes due schedules across tenants using each row's tenant_id separately
- **server/heartbeat-startup.ts:81** _(system-sweep)_ — UPDATE against tenant-scoped table heartbeat_tasks filters only by id and omits tenant_id, so it can modify another tenant's row if ids are reused or accessed unexpectedly.
  - _Why false positive:_ boot-time housekeeping normalizes scheduler task models across all tenants and only mutates housekeeping fields
- **server/heartbeat-startup.ts:85** _(system-sweep)_ — UPDATE against tenant-scoped table heartbeat_tasks filters only by id and omits tenant_id, so it can modify another tenant's row if ids are reused or accessed unexpectedly.
  - _Why false positive:_ boot-time housekeeping normalizes scheduler task models across all tenants and only mutates housekeeping fields
- **server/heartbeat-task-scheduler-storage.ts:11** _(system-sweep)_ — Cross-tenant SELECT from tenant-scoped table heartbeat_tasks has no tenant_id constraint. This returns tasks for all tenants without an explicit per-tenant filter.
  - _Why false positive:_ scheduler-only cross-tenant due-task enumeration for platform heartbeat; rows carry tenant_id for per-row handling
- **server/heartbeat-task-scheduler-storage.ts:43** _(system-sweep)_ — UPDATE against tenant-scoped table heartbeat_tasks filters only by id and omits tenant_id, allowing claims of tasks without an explicit tenant ownership constraint.
  - _Why false positive:_ scheduler claim step updates only task timing fields in a platform-wide scheduler flow, not tenant-facing data
- **server/heartbeat.ts:261** _(system-sweep)_ — Cross-tenant SELECT from tenant-scoped table event_log has no tenant_id constraint. It reads pending events across all tenants without an explicit tenant filter.
  - _Why false positive:_ platform event-processing loop reads pending events across tenants and routes each using that row's own tenant_id
- **server/heartbeat.ts:1583** _(global-table)_ — SELECT from tenant-scoped table heartbeat_logs has no tenant_id constraint. It checks prior runs by task_name only, which can read another tenant's logs and affect scheduling behavior across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is used here as a global scheduler log for prior-run timing
- **server/heartbeat.ts:1715** _(global-table)_ — SELECT from tenant-scoped table code_health_scans has no tenant_id constraint. It reads the latest scan globally rather than restricting to the current tenant.
  - _Why false positive:_ code_health_scans has no tenant_id and represents platform-wide scan history
- **server/heartbeat.ts:1724** _(global-table)_ — SELECT from tenant-scoped table code_health_findings has no tenant_id constraint. It diffs findings across scans without restricting rows to the current tenant.
  - _Why false positive:_ code_health_findings has no tenant_id and stores platform-wide code health findings for global diffing/alerting
- **server/knowledge-diversity-monitor.ts:189** _(upstream-verified)_ — Uses sql.raw() inside an INTERVAL expression. Although the current value comes from a constant, raw SQL construction is explicitly disallowed because it creates injection/isolation risk if the source ever becomes request-derived or mutable.
  - _Why false positive:_ Both queries already constrain tenant_id = tenantId; finding is about raw SQL/injection, not cross-tenant isolation.
- **server/lib/aevo-meta-editor.ts:449** _(internal-worker-pk)_ — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. Tenant isolation requires every write to tenant-scoped rows to include tenant_id in the WHERE clause.
  - _Why false positive:_ runId is the server-generated primary key returned from the immediately preceding insert in the same function, not taken from an untrusted request boundary.
- **server/lib/aevo-meta-editor.ts:459** _(internal-worker-pk)_ — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. This relies on a global row id rather than an application-level tenant filter.
  - _Why false positive:_ Updates the same newly inserted run by server-generated runId within one internal call path.
- **server/lib/aevo-meta-editor.ts:472** _(internal-worker-pk)_ — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. Without a tenant_id predicate, the write is not isolated by tenant at the SQL layer.
  - _Why false positive:_ Failure update targets the current procedure_evolution_runs row via server-generated runId, not caller-controlled input.
- **server/lib/aevo-meta-editor.ts:502** _(internal-worker-pk)_ — UPDATE against tenant-scoped table procedure_evolution_runs filters only by id and omits tenant_id. Tenant-scoped writes must always include tenant_id in the WHERE clause.
  - _Why false positive:_ Final status update uses the same locally created runId from insert-returning, a server-side unique PK.
- **server/lib/customer-revenue-launchpad-seed.ts:103** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly, and there is no DB default/RLS/FK enforcement to ensure tenant isolation.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id to projects.
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT against tenant-scoped table project_files uses only project_id with no tenant_id ownership verification. newProjectId is derived from imported snapshot data and the code does not first verify the referenced project belongs to the intended tenant before reading/writing child rows.
  - _Why false positive:_ project_files has no tenant_id and newProjectId comes from idMap populated from tenant-scoped project lookup/insert, so child access is transitively scoped by parent project.
- **server/lib/ideabrowser-backfill.ts:104** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The write relies only on project_id, but the invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id; insert is correctly scoped through parent project_id referencing a tenant-scoped projects row.
- **server/lib/ideabrowser-backfill.ts:115** _(fk-transitive)_ — SELECT against tenant-scoped table project_notes uses only project_id with no tenant_id ownership verification. The code trusts newProjectId without first validating tenant ownership of the referenced project.
  - _Why false positive:_ project_notes has no tenant_id and the note row is isolated through project_id to the tenant-scoped parent project.
- **server/lib/ideabrowser-backfill.ts:119** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id. The write relies only on project_id, violating the requirement that tenant_id be explicitly inserted on tenant-scoped tables.
  - _Why false positive:_ project_notes has no tenant_id; inserting by verified/mapped parent project_id is the intended transitive isolation pattern.
- **server/lib/ideabrowser-dev-sync.ts:139** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id. The code writes notes using only project_id after creating a project, but the invariant requires explicit tenant_id on every INSERT into tenant-scoped tables.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects via project_id created just above.
- **server/lib/ideabrowser-dev-sync.ts:143** _(fk-transitive)_ — Subquery against tenant-scoped table project_notes checks existence by project_id only, with no tenant_id constraint. This is a tenant-isolation gap if project_ids are ever mis-associated or reused from untrusted input.
  - _Why false positive:_ project_notes has no tenant_id; existence check is by child FK project_id for the just-created tenant-scoped parent.
- **server/lib/ideabrowser-dev-sync.ts:150** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. The write is scoped only by project_id, but the invariant requires explicit tenant_id on INSERTs.
  - _Why false positive:_ project_files has no tenant_id and is transitively isolated through tenant-scoped projects via project_id.
- **server/lib/ideabrowser-dev-sync.ts:154** _(fk-transitive)_ — Subquery against tenant-scoped table project_files checks existence by project_id only, with no tenant_id constraint.
  - _Why false positive:_ project_files has no tenant_id; existence check is on child rows scoped by parent project_id.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — System-scope runner updates and reads tenant-scoped table scheduled_posts across all tenants without any tenant_id constraint in the picking SELECT/UPDATE CTE. Under the stated invariant, every read/write against a tenant-scoped table must be constrained by tenant_id in SQL.
  - _Why false positive:_ global maintenance runner intentionally processes due rows across tenants and carries each row's tenant_id forward; this is housekeeping orchestration, not tenant-mixed output.
- **server/lib/skill-optimizer-promotion.ts:721** _(global-table)_ — Reads from tenant-scoped table skills by foreign id candidate.skillId without first verifying tenant ownership and without a tenant_id filter in the WHERE clause. This violates the invariant requiring tenant-scoped reads to include tenant_id, and it trusts a foreign id derived from a previously loaded candidate row.
  - _Why false positive:_ schema note says skills has no tenant_id, so the flagged tenant-scoped-read premise is incorrect for this table.
- **server/lib/skill-optimizer-promotion.ts:836** _(global-table)_ — Updates tenant-scoped table skills using only id and promptContent compare-and-swap, with no tenant_id predicate. This can modify a row outside the caller tenant if ids collide with unexpected data assumptions, and violates the invariant that every write to a tenant-scoped table must be constrained by tenant_id.
  - _Why false positive:_ skills has no tenant_id per schema, so updating by id/CAS is not a tenant-scoped table isolation issue.
- **server/lib/skill-optimizer-promotion.ts:928** _(global-table)_ — Reads from tenant-scoped table skills by foreign id candidate.skillId during rollback without a tenant_id filter or prior ownership verification. This trusts a foreign reference and violates the invariant requiring tenant-scoped reads to be constrained by tenant_id.
  - _Why false positive:_ skills is not tenant-scoped in schema; read by id during rollback is not missing a tenant_id column/filter.
- **server/lib/skill-optimizer-promotion.ts:946** _(global-table)_ — Updates tenant-scoped table skills during rollback using only id and promptContent, without a tenant_id predicate. This violates the invariant that every write to a tenant-scoped table must include tenant_id filtering.
  - _Why false positive:_ skills has no tenant_id per schema, so rollback update is not a tenant-scoped write missing tenant filtering.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — Raw SELECT against tenant-scoped table agent_runs has no tenant_id filter in the WHERE clause, so it scans and returns rows across all tenants.
  - _Why false positive:_ Platform-wide stuck-task audit intentionally scans all tenants, carries row tenant_id per result, and is housekeeping/reporting not tenant-facing per-request data access.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — Raw SELECT against tenant-scoped table mind_tickets has no tenant_id filter in the WHERE clause, so it scans and returns rows across all tenants.
  - _Why false positive:_ Platform-wide maintenance audit of stuck mind_tickets across tenants; rows retain their own tenant_id and this sweep is bounded housekeeping/report output.
- **server/lib/system-probes.ts:163** _(system-sweep)_ — Count query against tenant-scoped table messages has no tenant_id filter, exposing cross-tenant totals.
  - _Why false positive:_ All surrounding probes explicitly use ALL_TENANTS for system health; message count is an aggregate health metric and does not expose tenant content.
- **server/lib/work-opportunity-radar-seed.ts:125** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id explicitly, violating the invariant that every insert into tenant-scoped tables must pass tenantId explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id to tenant-owned projects.
- **server/lib/work-opportunity-radar-seed.ts:135** _(fk-transitive)_ — INSERT into project_files does not set tenant_id explicitly, violating the invariant that every insert into tenant-scoped tables must pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped transitively via project_id to tenant-owned projects.
- **server/memory-graph.ts:130** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_categories filters only by id and omits tenant_id. If catId is cross-tenant or corrupted, this can modify another tenant's row.
  - _Why false positive:_ catId is obtained in the same call path from tenant-scoped SELECT/INSERT using tenantId, so the subsequent UPDATE by id targets a row already proven tenant-owned.
- **server/memory-graph.ts:709** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries filters only by id and omits tenant_id. A stale or cross-tenant mem.id could update another tenant's memory row.
  - _Why false positive:_ mem.id comes from the immediately preceding SELECT constrained by tenant_id, so the update is operating on rows already selected as tenant-owned in the same path.
- **server/memory-intelligence.ts:413** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples filters only by id and omits tenant_id after selecting contradiction rows. Without tenant scoping on the write, a wrong/cross-tenant id could modify another tenant's triple.
  - _Why false positive:_ Each c.id is sourced from the immediately preceding SELECT on knowledge_triples constrained by tenant_id, so the update acts on rows already verified for that tenant.
- **server/memory/tombstones.ts:64** _(upstream-verified)_ — SELECT from tenant-scoped table memory_tombstones is executed directly via db.execute without using a tenant-scoped transaction/helper. While it includes a tenant_id predicate, this module bypasses the application's standard tenant-scoping path, increasing risk of cross-tenant access if caller-supplied tenantId is incorrect or unvalidated.
  - _Why false positive:_ The query itself enforces tenant_id = input.tenantId and this finding is about architectural path choice rather than an actual missing tenant constraint on data access.
- **server/mpeg-engine.ts:1058** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. This code relies only on project_id ownership check, but the invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id column and is scoped transitively via project_id after assertProjectInTenant(projectId, tenantId).
- **server/mpeg-engine.ts:1445** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. Although project ownership is checked, the tenant_id column is omitted on insert, violating the explicit tenant scoping requirement for writes.
  - _Why false positive:_ project_files has no tenant_id column and insert is gated by assertProjectInTenant on the parent project.
- **server/oauth-subscriptions.ts:908** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions reads all active rows with no tenant_id filter. This enumerates subscriptions across tenants.
  - _Why false positive:_ Startup validation is a cross-tenant maintenance loop over oauth_subscriptions using each row's tenant_id for per-row handling, not tenant-facing data return.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions reads google-workspace rows across all tenants with no tenant_id filter.
  - _Why false positive:_ Boot-time auto-reconnect enumerates tenant ids platform-wide for maintenance and processes each tenant separately via connectGoogleViaReplit(tid).
- **server/persona-export.ts:39** _(global-table)_ — SELECT from tenant-scoped table `skills` uses `persona_id = ${personaId} OR persona_id IS NULL` with no tenant_id filter. This can pull skills from other tenants that reference the same persona_id or global-looking rows without validating they are truly shared.
  - _Why false positive:_ Schema notes show skills has no tenant_id column, so this is not a tenant-scoped table read.
- **server/persona-routing-outcomes.ts:44** _(fk-transitive)_ — Join reads from tenant-scoped table `personas` without constraining `p.tenant_id = ${tenantId}`. Although `action_outcomes` is filtered by tenant, a cross-tenant persona_id collision or bad foreign reference could leak another tenant's persona name into the cache.
  - _Why false positive:_ personas has no tenant_id column and names are joined transitively from tenant-filtered action_outcomes rows by persona_id.
- **server/plan-executor.ts:741** _(internal-worker-pk)_ — SELECT against tenant-scoped plans table uses only id with no tenant_id constraint. This reads plan metadata (source) before tenant ownership is established, violating the invariant that every access to tenant-scoped tables must be constrained by tenant_id.
  - _Why false positive:_ Internal executor probes a plan by server-side planId primary key before claim; function is worker-style and not shown taking tenant data from a request boundary.
- **server/plan-executor.ts:744** _(internal-worker-pk)_ — UPDATE against tenant-scoped plans table claims a plan by id and status only, with no tenant_id predicate. This mutates a tenant-scoped row without an explicit tenant filter.
  - _Why false positive:_ Internal worker claims a plan by globally unique plan id/status and immediately derives tenant_id from the returned row, not from caller input.
- **server/plan-executor.ts:1280** _(internal-worker-pk)_ — claimStaleRecoveryChildCore updates tenant-scoped plans by id/source/status without a tenant_id constraint. This is a write on a tenant-scoped table lacking the required tenant filter.
  - _Why false positive:_ Helper claims stale-recovery child by server-side plan primary key in internal execution flow, a worker state transition by global id.
- **server/plan-executor.ts:1293** _(internal-worker-pk)_ — terminalizeStaleRecoveryChildCore selects a child row from tenant-scoped plans by id/source only, without tenant_id filtering. This reads a tenant-scoped row without the required tenant constraint.
  - _Why false positive:_ Internal reconciliation helper locks a recovery child by globally unique child plan id within server-side workflow, not a cross-tenant request path.
- **server/plan-executor.ts:1404** _(system-sweep)_ — Boot recovery reads from tenant-scoped plans table across all tenants with no tenant_id constraint. The invariant requires every read/write on tenant-scoped tables to be tenant-filtered.
  - _Why false positive:_ resumeStuckPlans is boot-time housekeeping across tenants; it carries each row's tenant_id forward for per-row recovery and does not return mixed tenant data.
- **server/plan-executor.ts:1540** _(system-sweep)_ — Transient recovery query selects from tenant-scoped plans without any tenant_id constraint, scanning failed plans across all tenants.
  - _Why false positive:_ Boot-time recovery sweep intentionally scans stale failed plans across tenants and only resets/requeues rows using each row's own tenant_id; no tenant data is returned to a tenant-facing surface.
- **server/plan-executor.ts:1599** _(system-sweep)_ — Query selects approved plans from tenant-scoped plans table without tenant_id constraint, causing cross-tenant reads in the recovery loop.
  - _Why false positive:_ Boot recovery loop globally selects approved plans to kick execution by id; this is maintenance across tenants and only schedules per-row processing, not cross-tenant data exposure.
- **server/research-engine.ts:464** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. Per invariant, tenant-scoped inserts must pass tenantId explicitly; relying only on project_id ownership is insufficient.
  - _Why false positive:_ project_notes has no tenant_id column and is isolated transitively through project_id; code also asserts the project belongs to session.tenantId before insert.
- **server/research-engine.ts:472** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id explicitly. This violates the requirement that every INSERT into tenant-scoped tables provide tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects via project_id; prior assertProjectInTenant verifies parent ownership.
- **server/routes.ts:189** _(system-sweep)_ — Startup restore selects from tenant-scoped file_storage without any tenant_id constraint. This reads every tenant's stored files across the whole system.
  - _Why false positive:_ Startup upload restore is a platform-wide housekeeping loop over file_storage rows that only rewrites files to local disk and logs aggregate counts, without returning mixed tenant data to users.
- **server/routes.ts:632** _(system-sweep)_ — Backfill query selects from tenant-scoped projects joined to tenants without any tenant_id constraint. This reads all tenants' projects in one unscoped query.
  - _Why false positive:_ Drive backfill is a platform-wide maintenance loop over all projects; each row carries its own tenant_id and is processed individually, not returned to tenants or mixed into one tenant-facing output.
- **server/routes.ts:1938** _(signed-url)_ — GET /api/presenter/:token reads presenter_sessions by token only, with no tenant ownership verification. Anyone with a valid token can access the session row without an authenticated tenant check.
  - _Why false positive:_ Presenter token route appears to use the high-entropy token itself as the access capability; access is by possession of token rather than tenant auth.
- **server/routes.ts:2009** _(signed-url)_ — POST /api/presenter/:token/tts reads presenter_sessions by token only, with no tenant ownership verification before invoking text-to-speech.
  - _Why false positive:_ TTS route gates session access by presenter token as capability; token lookup is the intended authorization primitive rather than tenant-bound auth.
- **server/routes.ts:4664** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set `tenant_id`. The note is created from a tenant-owned conversation/project, but the INSERT only provides `(project_id, note, author)` and relies on neither RLS nor defaults.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; insert is correctly isolated transitively via project_id after tenant-checked conversation lookup.
- **server/routes.ts:6947** _(global-table)_ — Import handler creates personas from imported data without explicitly setting/validating `tenantId`. In this codebase every INSERT into tenant-scoped tables must pass `tenantId` explicitly; imported `rest` may omit it or carry untrusted cross-tenant values.
  - _Why false positive:_ Referenced schema says personas has no tenant_id, so creating personas without tenant scoping matches a global/non-tenant-scoped table pattern.
- **server/routes/admin-zombie-detector.ts:38** _(upstream-verified)_ — SELECT from tenant-scoped table agent_trace_spans has no tenant_id constraint. The query aggregates tool usage across all tenants, violating tenant isolation.
  - _Why false positive:_ Route is explicitly gated to platform admin/admin tenant before executing intentional cross-tenant aggregate admin query.
- **server/routes/admin-zombie-detector.ts:54** _(upstream-verified)_ — SELECT from tenant-scoped table conversations has no tenant_id constraint. Persona usage is computed across all tenants, leaking cross-tenant activity metadata.
  - _Why false positive:_ Same admin-only endpoint; cross-tenant persona usage aggregate is behind explicit admin tenant + admin request checks.
- **server/routes/agent-insights.ts:43** _(global-table)_ — SELECT from tenant-scoped table personas has no tenant_id constraint in scorecards. This exposes personas for all tenants to the caller.
  - _Why false positive:_ Schema note says personas has no tenant_id; query reads a global registry table, not tenant-scoped data.
- **server/routes/agent-insights.ts:208** _(global-table)_ — SELECT from tenant-scoped table personas has no tenant_id constraint in capability-map. The response includes persona data across all tenants.
  - _Why false positive:_ personas table is global by schema (no tenant_id), and code comment states persona registry is global-by-design.
- **server/routes/conversations.ts:119** _(upstream-verified)_ — UPDATE on tenant-scoped table conversations uses only WHERE id = <conv.id> and omits tenant_id scoping when attaching a project to a newly created conversation. This relies on prior application logic rather than enforcing tenant isolation in the SQL itself.
  - _Why false positive:_ Conversation row was just created server-side with tenantId, and project ownership was checked with tenant_id before this update; conv.id is not attacker-chosen.
- **server/routes/conversations.ts:123** _(fk-transitive)_ — INSERT into tenant-scoped join table project_conversations does not set tenant_id. Per the isolation invariant, every insert into a tenant-scoped table must explicitly provide tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id; join row is transitively scoped via tenant-checked project and newly created tenant-scoped conversation.
- **server/routes/conversations.ts:257** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id when archiving a deleted conversation into project notes. This violates the requirement that every insert explicitly provide tenant_id.
  - _Why false positive:_ project_notes has no tenant_id; insert is scoped through tenant-verified parent project_id.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table inbox_messages lacks tenant_id constraint. The row is first selected with tenant_id, but the subsequent UPDATE only filters by id, violating the invariant that every write must be constrained in SQL by tenant_id.
  - _Why false positive:_ Same call path first SELECTs inbox_messages by id and tenant_id, then updates that fetched row by its id.
- **server/routes/projects.ts:400** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations does not set tenant_id. The invariant requires every INSERT into a tenant-scoped table to pass tenantId explicitly; relying on project_id/conversation_id linkage is insufficient without DB enforcement.
  - _Why false positive:_ project_conversations has no tenant_id; join insert is transitively isolated by tenant-scoped parent project and conversation.
- **server/routes/projects.ts:568** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id. Although the parent project is ownership-checked first, the write itself omits the tenant isolation column required by the invariant.
  - _Why false positive:_ project_notes has no tenant_id; insert is correct by design after tenant-scoped project ownership check.
- **server/routes/projects.ts:608** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id in the base64 upload route. Parent project ownership is checked, but the tenant-scoped row is inserted without explicit tenant binding.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via prior tenant-checked parent project_id
- **server/routes/projects.ts:616** _(upstream-verified)_ — UPDATE against tenant-scoped table projects lacks a tenant_id predicate in the files-base64 upload route. The route verifies project ownership earlier, but the write itself is unconstrained and could touch another tenant's row if ids are reused or earlier checks drift.
  - _Why false positive:_ same route already verified projects.id belongs to tenant via SELECT ... WHERE id=? AND tenant_id=?
- **server/routes/projects.ts:677** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id in the multipart upload route. The parent project is checked, but the inserted row is not explicitly bound to the caller tenant.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by tenant-owned project_id
- **server/routes/projects.ts:685** _(upstream-verified)_ — UPDATE against tenant-scoped table projects lacks a tenant_id predicate in the multipart upload route. Prior ownership verification does not satisfy the invariant for the write itself.
  - _Why false positive:_ same handler already proved project ownership with tenant-scoped lookup before this update
- **server/routes/projects.ts:704** _(fk-transitive)_ — SELECT from tenant-scoped table project_files is filtered only by project_id, with no tenant_id constraint in the WHERE clause. Checking the parent project earlier is not sufficient under the stated invariant.
  - _Why false positive:_ project_files is child table without tenant_id; prior tenant-scoped project check makes project_id-scoped read tenant-safe
- **server/routes/projects.ts:727** _(fk-transitive)_ — SELECT from tenant-scoped table project_files in the download route lacks a tenant_id predicate; it filters by project_id and file_path only. The earlier project ownership check does not meet the invariant for this read.
  - _Why false positive:_ project_files lacks tenant_id and lookup is constrained to already tenant-verified parent project_id
- **server/routes/projects.ts:756** _(fk-transitive)_ — DELETE from tenant-scoped table project_files lacks a tenant_id predicate. The route verifies the parent project, but the delete itself is not constrained by tenant_id as required.
  - _Why false positive:_ delete targets child row in project_files, which is transitively isolated through verified tenant-owned project_id
- **server/sculptor.ts:105** _(upstream-verified)_ — UPDATE on tenant-scoped table sculptor_sessions uses only WHERE id = session.id with no tenant_id constraint. Although session.id was created earlier in this flow, the invariant requires every write against a tenant-scoped table to include tenant scoping in the WHERE clause.
  - _Why false positive:_ session row was created just above with params.tenantId and this immediate follow-on update uses that server-created session.id in same flow
- **server/sculptor.ts:112** _(upstream-verified)_ — UPDATE on tenant-scoped table sculptor_sessions sets conversation_id using only WHERE id = session.id with no tenant_id constraint.
  - _Why false positive:_ Updates the freshly inserted session row using server-derived session.id from the same call path; tenant was fixed at insert time.
- **server/sculptor.ts:167** _(upstream-verified)_ — UPDATE on tenant-scoped table sculptor_sessions in monitorSession uses only WHERE id = sessionId with no tenant_id constraint.
  - _Why false positive:_ monitorSession is invoked with session.id and tenantId from the creator path; this updates that same session row after tenant-scoped stats reads.
- **server/second-opinion.ts:501** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger settles a reservation row by id only, with no tenant_id constraint in the WHERE clause.
  - _Why false positive:_ Settles a server-created reservation by globally unique ledger row id; reservationId is internal bookkeeping, not shown as sourced from an untrusted tenant request boundary.
- **server/seed.ts:43** _(system-sweep)_ — SELECT against tenant-scoped table research_sessions counts all rows with no tenant_id constraint (`SELECT COUNT(*) as count FROM research_sessions`). This can mix data across tenants and changes control flow based on other tenants' data.
  - _Why false positive:_ Boot-time dev snapshot/seed control-flow check intentionally inspects global row count and does not return or mix tenant data to a tenant-facing surface.
- **server/seed.ts:2253** _(fk-transitive)_ — INSERT into tenant-scoped join table project_conversations does not set tenant_id. The invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly.
  - _Why false positive:_ project_conversations has no tenant_id column and is a child link isolated through tenant-verified project/conversation parents joined on matching tenant_id.
- **server/seed.ts:2836** _(system-sweep)_ — SELECT against tenant-scoped table governance_frameworks is hard-coded to `tenant_id = 1`, and subsequent inserts seed tenant-scoped data only for tenant 1. This bypasses caller/tenant scoping and would be unsafe if reused outside owner-only bootstrap logic.
  - _Why false positive:_ Owner/bootstrap seeding for tenant 1 only; fixed-tenant seed logic is maintenance code, not a tenant-facing access path or cross-tenant data mix.
- **server/seed.ts:3397** _(global-table)_ — SELECT against tenant-scoped table conversation_templates has no tenant_id constraint, and subsequent INSERTs into conversation_templates do not set tenant_id. This breaks tenant isolation and can create rows with missing or incorrect tenant ownership.
  - _Why false positive:_ Schema note shows conversation_templates has no tenant_id column, indicating intentionally global shared templates rather than tenant-scoped rows.
- **server/seed.ts:3655** _(system-sweep)_ — DELETE from tenant-scoped table heartbeat_tasks removes rows based only on enabled/description/type with no tenant_id constraint, affecting all tenants.
  - _Why false positive:_ Boot-time housekeeping deletes stale heartbeat task rows across tenants without exposing tenant data; maintenance-only status cleanup.
- **server/seed.ts:3683** _(system-sweep)_ — UPDATE against tenant-scoped table memory_entries modifies rows based on content only and lacks any tenant_id constraint, so it can rewrite data across all tenants.
  - _Why false positive:_ Platform-wide content normalization migration updates rows in place across tenants and only emits aggregate counts, with no tenant data returned or mixed.
- **server/seed.ts:3709** _(system-sweep)_ — UPDATE against tenant-scoped table conversations changes model for all matching rows without a tenant_id constraint, affecting every tenant's conversation data.
  - _Why false positive:_ Global migration/backfill of legacy model ids across tenants updates housekeeping/config fields only and reports counts, not tenant content.
- **server/seed.ts:3905** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes does not set tenant_id, and uses project_id from a prior query without carrying tenant ownership into the inserted row.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects; insert uses project_id loaded with tenant_id = 1.
- **server/seed.ts:3950** _(global-table)_ — UPDATE against tenant-scoped table personas uses only id in the WHERE clause and has no tenant_id constraint, so it can update matching persona ids across tenants.
  - _Why false positive:_ personas schema shows no tenant_id; this seed updates platform-global persona rows by fixed ids
- **server/sessions.ts:67** _(upstream-verified)_ — Handler uses conv.personaId derived from tenant-scoped conversation data to read a persona via storage.getPersona(conv.personaId) without first verifying the caller's tenant owns that persona. This violates the rule against dereferencing foreign ids without ownership validation.
  - _Why false positive:_ personaId is dereferenced only from conversations already fetched with required params.tenantId; personas table itself is not tenant-scoped
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped messages table is filtered only by conversation_id without a tenant_id constraint. A conversationId from another tenant could cause cross-tenant message disclosure if ids are guessed or reused.
  - _Why false positive:_ recentMsgs uses conversationId already validated by analyzeConversation(conversationId, tenantId) in same call path
- **server/skills-marketplace.ts:209** _(global-table)_ — SELECT against tenant-scoped skills table has no tenant_id constraint. Checking for an existing skill by name globally can disclose or interfere with other tenants' installed skills.
  - _Why false positive:_ skills schema shows no tenant_id, so existence check is against a non-tenant-scoped table
- **server/storage.ts:289** _(upstream-verified)_ — Raw SELECT against tenant-scoped table `messages` fetches citations by message ids without any tenant_id constraint. If `attachCitationMetadata` is ever called with ids from mixed or unverified sources, it can read cross-tenant message data.
  - _Why false positive:_ attachCitationMetadata reads only ids from msgs already loaded by tenant-scoped callers in shown paths; same-row enrichment only
- **server/storage.ts:716** _(upstream-verified)_ — UPDATE against tenant-scoped table `conversation_facts` changes rows by id list only, with no tenant_id constraint in the WHERE clause. `victims` was selected with tenant scope, but the write itself is unscoped and relies on prior selection rather than enforcing tenant isolation at the mutation site.
  - _Why false positive:_ victim ids are selected immediately above with conversation_id and tenant_id scope, then updated by that trusted id set in the same call path
- **server/storage.ts:960** _(system-sweep)_ — SELECT from tenant-scoped table `memory_entries` in `getMemoriesWithoutEmbeddings` has no tenant_id constraint, returning rows across all tenants.
  - _Why false positive:_ embedding backfill helper intentionally selects rows across tenants for maintenance and does not return data to a tenant-facing surface
- **server/storage.ts:971** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` in `getKnowledgeWithoutEmbeddings` has no tenant_id constraint, returning rows across all tenants.
  - _Why false positive:_ knowledge embedding backfill is a platform-wide maintenance read across tenants, not a tenant response path
- **server/storage.ts:979** _(system-sweep)_ — Raw UPDATE against tenant-scoped table `memory_entries` in `archiveExpiredMemories` has no tenant_id constraint, mutating rows across all tenants.
  - _Why false positive:_ archiveExpiredMemories is a cross-tenant housekeeping sweep that only flips status fields and returns aggregate/id counts
- **server/storage.ts:995** _(system-sweep)_ — Raw UPDATE against tenant-scoped table `memory_entries` in `archiveStaleMemories` has no tenant_id constraint, mutating rows across all tenants.
  - _Why false positive:_ archiveStaleMemories is a platform-wide housekeeping sweep mutating only archival status across tenants
- **server/storage.ts:1008** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` in `pruneHeartbeatLogs` has no tenant ownership constraint, and the subsequent DELETE also acts only by ids. This performs cross-tenant reads/writes on a tenant-scoped dataset.
  - _Why false positive:_ schema note shows heartbeat_logs has no tenant_id column and pruning is global log maintenance
- **server/storage.ts:1553** _(upstream-verified)_ — `supersedeAdr` first verifies both ADR rows belong to the caller tenant, but then performs two UPDATEs by id only without tenant_id constraints. The mutation sites themselves are unscoped on a tenant-scoped table.
  - _Why false positive:_ both ADR rows are verified just above with id plus tenant_id in the same call path before the id-only updates
- **server/tools.ts:2815** _(fk-transitive)_ — INSERT into tenant-scoped join table project_conversations does not set tenant_id. The invariant requires every INSERT into tenant-scoped tables to pass tenantId explicitly; this insert relies only on project_id/conversation_id and does not stamp tenant ownership.
  - _Why false positive:_ project_conversations has no tenant_id column and is transitively isolated by validated tenant-scoped project and conversation linkage.
- **server/tools.ts:2941** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. Although project ownership is checked first, the invariant requires every INSERT into tenant-scoped tables to explicitly include tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; ownership is checked on the parent before insert.
- **server/tools.ts:2955** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. The handler verifies project ownership, but tenant-scoped table inserts must still explicitly stamp tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and is a child of tenant-scoped projects; parent project tenant ownership is verified first.
- **server/tools.ts:2979** _(fk-transitive)_ — INSERT into project_conversations does not set tenant_id. Even though the project and conversation are ownership-checked, the invariant requires explicit tenant_id on every INSERT into tenant-scoped tables.
  - _Why false positive:_ project_conversations has no tenant_id column and links validated tenant-owned project and conversation, so isolation is transitive.
- **server/tools.ts:4543** _(upstream-verified)_ — SELECT against tenant-scoped conversations/project_conversations uses caller-supplied conversation id without any tenant_id constraint in the SQL. Although assertConversationInTenant(convId, params._tenantId) runs first, the query itself is not constrained by tenant_id, violating the application-level tenant filter invariant for reads.
  - _Why false positive:_ assertConversationInTenant runs immediately before the query, and resulting projectId is also checked with assertProjectInTenant before use.
- **server/tools.ts:4554** _(fk-transitive)_ — INSERT into tenant-scoped project_notes does not set tenant_id explicitly. The invariant requires every INSERT into tenant-scoped tables to provide tenant_id explicitly, and there is no DB default/RLS to enforce isolation.
  - _Why false positive:_ project_notes has no tenant_id column and insert is only reached after tenant ownership of conversation and project is verified.
- **server/tools.ts:4558** _(fk-transitive)_ — INSERT into tenant-scoped project_files does not set tenant_id explicitly. This can create unscoped rows or break tenant isolation because inserts must stamp tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and insert is tied to a project already verified to belong to the tenant.
- **server/tools.ts:6474** _(fk-transitive)_ — INSERT into tenant-scoped project_files uses only project_id after an ownership check, but does not set tenant_id explicitly. The invariant requires explicit tenant_id on every INSERT into tenant-scoped tables.
  - _Why false positive:_ project_files has no tenant_id column and insert is gated by assertProjectInTenant on parent project_id
- **server/tools.ts:7015** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id. This code verifies project ownership with assertProjectInTenant(params.project_id, params._tenantId) but then inserts only project_id/file fields via raw SQL, violating the invariant that every INSERT into a tenant-scoped table must explicitly pass tenantId.
  - _Why false positive:_ project_files has no tenant_id column and insert is gated by assertProjectInTenant on parent project_id
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table browser_workflows constrains only by id and omits tenant_id. The workflow row was previously selected with tenant_id, but the write itself is not tenant-scoped, violating the invariant that every write must include tenant isolation in SQL.
  - _Why false positive:_ workflow row was loaded immediately above with id/name plus tenant_id, and update uses that fetched row's id
- **server/tools/domains/files/handlers.ts:942** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not set tenant_id explicitly. The code inserts only project_id, file metadata, and uploaded_by, relying on project_id linkage instead of the required application-level tenant_id column.
  - _Why false positive:_ project_files has no tenant_id column and projId comes from tenant-scoped conversation->project join
- **server/tools/domains/files/handlers.ts:946** _(upstream-verified)_ — UPDATE on tenant-scoped table projects constrains only by id and omits a tenant_id predicate. Although projId was previously derived via a tenant-scoped conversation/project join, this write itself is not tenant-filtered, violating the invariant that every write must include tenant_id scoping in the WHERE clause.
  - _Why false positive:_ projId was derived just above from a join requiring projects.tenant_id = ctx.tenantId
- **server/tools/domains/strategic-memory/handlers.ts:103** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses only WHERE id = ${id} after a prior SELECT by tenant/persona. Tenant isolation is enforced only at the SQL statement level, so this write is not constrained by tenant_id in its own WHERE clause.
  - _Why false positive:_ id is selected immediately above from memory_entries with tenant_id and persona_id filters before this update
- **server/tools/domains/strategic-memory/handlers.ts:164** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries bumps access counters using WHERE id = ANY(...) with no tenant_id constraint. Even though ids were previously selected with tenant filtering, each write must enforce tenant isolation at the SQL statement level.
  - _Why false positive:_ ids being updated come directly from the tenant_id and persona_id scoped SELECT just above
- **server/tools/domains/strategic-memory/handlers.ts:263** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses only WHERE id = ${id} after a prior tenant-scoped SELECT. This violates the requirement that every write to a tenant-scoped table include a tenant_id filter in the statement itself.
  - _Why false positive:_ id is selected immediately above from memory_entries with tenant_id and persona_id filters before this update
- **server/twilio.ts:116** _(upstream-verified)_ — Handler takes foreign id info.conversationId from data stored in agent_knowledge and queries conversations by id without first constraining the read to a trusted tenant in SQL (SELECT tenant_id FROM conversations WHERE id = ... LIMIT 1). Although the result is compared afterward, the invariant requires tenant isolation at query time for tenant-scoped tables.
  - _Why false positive:_ The query is itself the ownership-verification step for an untrusted pairing; code compares conversation.tenant_id to claimed tenant and drops mismatches/ambiguity before any tenant routing.
- **server/webhook-triggers.ts:168** _(global-table)_ — Tenant-scoped query joins personas on p.id = w.persona_id without constraining personas by tenant_id. If persona IDs collide or are referenced across tenants, listTriggers can expose another tenant's persona name.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so joining persona name cannot cross tenant boundaries of a tenant-scoped personas table.
- **server/webhook-triggers.ts:258** _(global-table)_ — processTriggerEvent reads a tenant-scoped persona via LEFT JOIN personas p ON p.id = w.persona_id using only a foreign key from webhook_triggers, without verifying the trigger's tenant owns that persona row.
  - _Why false positive:_ personas is schema-noted as non-tenant-scoped; joining it from webhook_triggers does not bypass a tenant_id constraint on personas because none exists.
- **server/google-workspace.ts:2287** _(fk-transitive)_ — INSERT into tenant-scoped table `presenter_slide_images` does not set `tenant_id`. In this codebase tenant isolation relies on explicit tenant_id on every insert, so these rows can be orphaned or mis-scoped.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/google-workspace.ts:2293** _(fk-transitive)_ — Second INSERT into tenant-scoped table `presenter_slide_images` (thumb quality) also omits `tenant_id`. This violates the invariant that every insert into a tenant-scoped table must set tenant_id explicitly.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/persona-export.ts:33** _(global-table)_ — SELECT from tenant-scoped table `personas` by `id` only, with no `tenant_id` constraint. A caller who knows another tenant's personaId could export that persona's data.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): personas table has no tenant_id column (platform-global personas); tenant linkage lives in trust/skills rows fetched separately.
- **server/recurring-messages.ts:307** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` uses only `WHERE id = ${r.id}` with no `tenant_id` constraint. The runner updates scheduled-message rows without enforcing tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/storage.ts:937** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `memory_entries` sets embeddings by id only, with no tenant_id constraint. Any caller with a foreign memory id can modify another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE against tenant-scoped agentActivity uses only id with no tenant_id filter. If activityId is influenced by another tenant's row, this can modify cross-tenant data.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE against tenant-scoped table commitments reopens a row by id without a tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. Although reached on an invalid/missing tenantId path, the write is not tenant-scoped and could modify a row across tenants if ids are guessed or data is malformed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE from tenant-scoped table doc_chunks is constrained by doc_path and collection_id only, with no tenant_id filter. If collectionId/docPath are influenced by another tenant-owned collection/document pair, this can delete cross-tenant rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/embeddings.ts:809** _(system-sweep)_ — UPDATE against tenant-scoped table `memory_entries` can execute without any `tenant_id` constraint when `storeEmbeddingVec()` is called for `table === "memory_entries"` without `options.tenantId`. This permits cross-tenant modification by row id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/google-drive.ts:894** _(upstream-verified)_ — SELECT from tenant-scoped table projects reads a caller-supplied foreign id (projectId) without constraining the query by tenant_id in SQL. Ownership is checked after the read, but the invariant requires tenant filtering on the query itself.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): ensureProjectFolder selects only tenantId/driveFolderId and immediately throws on project.tenantId!==tenantId at google-drive.ts:551-553 before returning or writing anything.
- **server/google-workspace.ts:2307** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `presenter_sessions` is filtered only by `id = ${sessionId}` with no tenant_id constraint. If `sessionId` is guessed or reused across tenants, this can update another tenant's session.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/job-queue.ts:238** _(internal-worker-pk)_ — completeJob updates tenant-scoped table agent_jobs by id/attempts only, with no tenant_id constraint. A stale or incorrect id could mark another tenant's job complete.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:335** _(internal-worker-pk)_ — cancelJob updates tenant-scoped table agent_jobs by id only with no tenant_id filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/lib/agent-trace.ts:130** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_trace_spans constrains only span_id and omits tenant_id in the WHERE clause. Since span_id comes from application state and tenant isolation relies solely on SQL filters, this write is not tenant-scoped.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/memory-graph.ts:262** _(fk-transitive)_ — SELECT against tenant-scoped table memory_links checks for an existing link using memory ids only and has no tenant_id constraint or ownership verification on the referenced memories. This can read cross-tenant link state if ids collide across tenants' graph data.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): memory_links is a junction keyed by memory PKs produced by the same-tenant vector search upstream; no tenant_id column on the table.
- **server/process-governor.ts:1234** _(upstream-verified)_ — UPDATE on tenant-scoped governance_rules table uses only rule id with no tenant_id constraint. A stale or mismatched rule object could increment trigger_count on another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `projects` by `id` only, with no `tenant_id` constraint. `updateProjectBrain(projectId, ...)` can read another tenant's project if called with a foreign projectId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` by `project_id` only, with no `tenant_id` constraint. Even if project ownership were intended, it is not enforced at query time.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT from tenant-scoped table `messages` by `conversation_id` only, with no `tenant_id` constraint. This can read messages from another tenant's conversation if referenced indirectly.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/reference-learner.ts:296** _(upstream-verified)_ — UPDATE on tenant-scoped table memory_entries is constrained only by id = storedId, without tenant_id = input.tenantId in the WHERE clause. If storedId were ever stale/corrupted, this could update another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storedId used in the UPDATE at server/reference-learner.ts:290 is taken from the existing SELECT at reference-learner.ts:281-287 which filters WHERE tenant_id = ${input.tenantId} AND persona_id = ${personaId}. Ownership is proven in the same call path immediately before the write.
- **server/routes/admin.ts:954** _(upstream-verified)_ — SELECT from tenant-scoped table heartbeat_tasks has no tenant_id constraint. The admin silent-failures endpoint returns disabled/failing heartbeat tasks across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/routes/admin.ts:562 admin-only guard; heartbeat_tasks read is platform-admin diagnostics
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE on tenant-scoped table sculptor_sessions writes review_result using only WHERE id = sessionId with no tenant_id constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/self-improvement.ts:427** _(internal-worker-pk)_ — UPDATE against tenant-scoped table experiments constrains only by experiments.id and omits tenant_id in the WHERE clause. If an experiment id from another tenant were ever referenced here, this would write across tenant boundaries.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-reflection.ts:380** _(internal-worker-pk)_ — UPDATE against tenant-scoped table memory_entries updates by id only after a prior tenant-scoped SELECT. Because tenant isolation relies solely on per-query SQL filters, this write must also include tenant_id in its WHERE clause.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): UPDATE id comes from the tenant-scoped SELECT (WHERE tenant_id = ${tenantId}) directly above.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from tenant-scoped messages table in analyzeConversation is filtered only by conversation_id without a tenant_id constraint. Tenant isolation relies on application filters, so this read can leak another tenant's messages for a referenced conversation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Within analyzeConversation, the conversation ownership is asserted immediately before at skillify.ts:196-199 (WHERE eq(id) AND eq(tenantId), throws if absent); the messages SELECT at 201-203 reads the same owned conversation's messages.
- **server/storage.ts:680** _(upstream-verified)_ — INSERT into tenant-scoped table `conversation_facts` does not validate or prove that the supplied foreign `conversationId` belongs to `data.tenantId` before writing. A caller can potentially write facts linked to another tenant's conversation id while setting their own tenant_id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): conversation_facts.tenant_id NOT NULL with no default; InsertConversationFact requires tenantId at compile time.
- **server/tool-learning.ts:286** _(internal-worker-pk)_ — UPDATE on tenant-scoped custom_tools in tryAndRevert scopes only by id and omits tenant_id. This violates the required tenant filter invariant for writes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:311** _(internal-worker-pk)_ — DELETE on tenant-scoped custom_tools in revertToolRow scopes only by id and omits tenant_id. This violates the required tenant filter invariant for deletes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT against tenant-scoped table agent_knowledge reads rows by category/title only, with no tenant_id filter. This lets data from any tenant participate in the pairing lookup and is exactly the cross-tenant ambiguity/hijack surface described in the comments.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/venture-discovery/budget.ts:158** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger constrains only by id and does not enforce tenant_id = caller tenant. If reservationId is ever misbound or reused from another tenant, this can modify another tenant's ledger row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:172** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger constrains only by id and does not enforce tenant_id = caller tenant. This violates the application-level tenant isolation invariant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
