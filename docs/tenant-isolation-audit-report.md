# Tenant-Isolation Audit Report

_Generated 2026-07-25T15:10:26.050Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **569/569** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **306** (CRITICAL 99 · HIGH 163 · MEDIUM 44 · LOW 0)
- Precision second-pass (`gpt-5.4`): **185** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (99)

- **server/agentic-engines.ts:243** — Bulk UPDATE on tenant-scoped table `ai_insights` without any tenant_id constraint: `UPDATE ai_insights SET status = 'applied', action_taken = ... WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...`. This mutates rows for all tenants in one statement.
  - _Fix:_ Do NOT perform cross-tenant writes. Replace with per-tenant updates (e.g. update WHERE tenant_id = <tenant> AND status = 'new' ...) or iterate tenants and run tenant-scoped updates. Add validation to ensure no row for another tenant is modified. This is high-risk and should be fixed urgently.
- **server/agentic/runs.ts:36** — UPDATE on tenant-scoped table `agent_runs` performed without constraining by tenant_id (where(eq(agentRuns.id, runId)) with NO tenant check). This allows modifying rows across tenants.
  - _Fix:_ Include the tenant filter on all updates/deletes/reads against tenant-scoped tables. e.g. accept/derive tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Audit callers to pass tenantId where necessary.
- **server/agentic/runs.ts:51** — UPDATE on tenant-scoped table `agent_runs` (updateRunState) omits tenant_id in the WHERE clause (where(eq(agentRuns.id, runId))).
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Make the function accept tenantId or otherwise derive it and enforce it.
- **server/agentic/runs.ts:55** — UPDATE on tenant-scoped table `agent_runs` (replaceRunState) omits tenant_id in the WHERE clause.
  - _Fix:_ Require and apply tenantId in the WHERE clause (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Avoid global updates keyed only by id.
- **server/agentic/runs.ts:64** — UPDATE on tenant-scoped table `agent_runs` (completeRun) omits tenant_id in the WHERE clause, allowing cross-tenant mutation.
  - _Fix:_ Constrain the update by tenant (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Ensure completeRun is provided/derives the tenantId.
- **server/agentic/runs.ts:94** — UPDATE on tenant-scoped table `agent_runs` (failRun) omits tenant_id in the WHERE clause.
  - _Fix:_ Add tenant-scoped WHERE (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Make callers pass the tenantId to these mutators.
- **server/agentic/runs.ts:102** — UPDATE on tenant-scoped table `agent_runs` (pauseRun) omits tenant_id in the WHERE clause.
  - _Fix:_ Enforce tenant scoping in the WHERE clause (.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and update callers to supply tenantId.
- **server/agentic/wake-scheduler.ts:142** — UPDATE/SELECT on tenant-scoped table agent_wake_schedules lacks a tenant_id constraint. The UPDATE claims pending rows across all tenants (inner SELECT has no WHERE tenant_id), so one process can claim/firing schedules from multiple tenants.
  - _Fix:_ Constrain the claim to a single tenant (e.g. add AND tenant_id = ${tenantId} to the inner SELECT and pass tenantId), or iterate per-tenant/partition and run the claim per-tenant. Ensure the UPDATE/SELECT always includes tenant_id to preserve tenant isolation.
- **server/auto-memorize.ts:73** — INSERT INTO agent_knowledge in setWatermark hardcodes tenant_id = 1 when creating the watermark row. This writes tenant-scoped data into tenant #1 regardless of the actual tenant context, violating tenant isolation and corrupting tenant data.
  - _Fix:_ Do not hardcode tenant_id. Require and pass the correct tenantId for the watermark row (or store watermarks in a non-tenant-scoped store if the value is intentionally global). Enforce fail-closed tenant resolution so inserts use the caller's tenant instead of a fixed tenant id.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table `conversations` (setting project_id) lacks a tenant_id constraint in the WHERE clause. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}) or perform the update within a context that has already validated ownership and still include tenant_id in the WHERE to be fail-safe.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table `compaction_archives` without tenant constraint — query: SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...
  - _Fix:_ Constrain the query to the caller's tenant (e.g. AND tenant_id = ${tenantId}) or join against conversations/projects that include tenant_id and assert equality. Conversation content is sensitive — failing to filter by tenant_id risks cross-tenant data leakage.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` without tenant constraint — query: SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ...
  - _Fix:_ Add explicit tenant scoping (AND tenant_id = ${tenantId}) or use Drizzle .where(eq(messages.tenantId, tenantId)). Reads from messages must always include tenant filtering to prevent cross-tenant disclosure.
- **server/chat-engine.ts:766** — LATERAL subquery reads from tenant-scoped table `messages` without tenant constraint — query: SELECT MAX(created_at) AS last_msg_at FROM messages WHERE conversation_id = c.id
  - _Fix:_ Include tenant scoping in the lateral subquery (e.g. WHERE conversation_id = c.id AND tenant_id = ${tid}) or otherwise ensure the DB call is constrained via an explicit tenant_id condition. Apply the same pattern to all lateral subqueries touching tenant tables.
- **server/chat-engine.ts:767** — LATERAL subquery selects user message from tenant-scoped `messages` without tenant constraint — query: (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ORDER BY created_at DESC LIMIT 1) AS last_user_msg
  - _Fix:_ Add AND tenant_id = ${tid} (or where(eq(messages.tenantId, tid))) to the subquery. All message-level reads, including LATERAL subqueries, must include tenant scoping to enforce isolation.
- **server/chat-engine.ts:768** — LATERAL subquery selects assistant message from tenant-scoped `messages` without tenant constraint — query: (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'assistant' ORDER BY created_at DESC LIMIT 1) AS last_assistant_msg
  - _Fix:_ Constrain the subquery by tenant (AND tenant_id = ${tid}) or use Drizzle's eq(...) helper. Ensure every nested/inline SELECT against tenant tables is tenant-scoped.
- **server/chat-engine.ts:772** — LATERAL subquery reads from tenant-scoped table `compaction_archives` without tenant constraint — query: SELECT summary FROM compaction_archives WHERE conversation_id = c.id ORDER BY archived_at DESC LIMIT 1
  - _Fix:_ Add tenant scoping to the subquery (e.g. AND tenant_id = ${tid}) or join on a tenant-scoped parent and explicitly assert tenant equality. Treat compaction archives as tenant-scoped and guard all reads accordingly.
- **server/chat-engine.ts:3764** — Parsed tool arguments (parsedArgs) are taken from model/tool input and forwarded directly into executeGuardedTool(...) along with tenantId, but there is no explicit verification that any foreign IDs inside parsedArgs (e.g. project_id, conversation_id, lead_id, folder IDs) actually belong to the caller's tenant. This allows a malicious model or crafted tool args to reference other-tenant resources and trigger reads/writes via tools that assume the id is valid.
  - _Fix:_ Before calling executeGuardedTool, validate and canonicalize any foreign identifiers present in parsedArgs: either remove/ignore id fields coming from untrusted sources, or perform an ownership check (e.g. SELECT ... WHERE id = <id> AND tenant_id = currentTenant) and reject or rewrite the call if the id is not owned by the tenant. Also ensure executeGuardedTool itself enforces tenant-scoped checks for any operations it performs, and never relies solely on the presence of tenantId in the call options as a substitute for validating each referenced resource's tenant ownership.
- **server/data-protection.ts:117** — SELECT queries in permanentlyPurgeSoftDeleted read conversations across ALL tenants (no tenant_id predicate) and subsequent DELETEs remove messages/archives/conversations without scoping to a tenant.
  - _Fix:_ Restrict the initial SELECT to a specific tenant or iterate per-tenant. For each DELETE/SELECT use a tenant predicate (e.g. WHERE tenant_id = ${tenantId}) or run under an admin transaction that enforces and verifies tenant context. Never perform cross-tenant SELECT/DELETE without explicit tenant scoping and audit/authorization.
- **server/data-protection.ts:131** — DELETE FROM messages / DELETE FROM compaction_archives / DELETE FROM conversations in permanentlyPurgeSoftDeleted do not include tenant_id predicates — destructive writes may delete other tenants' data.
  - _Fix:_ Include tenant_id in the WHERE clause for each DELETE (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or perform the purge within a tenant-scoped transaction/loop that passes an explicit tenantId. Require explicit per-tenant scoping when deleting tenant-scoped tables.
- **server/embeddings.ts:684** — UPDATE statement writes embedding_vec/embedding to tenant-scoped tables (memory_entries | agent_knowledge) using only WHERE id = ${id} — no tenant_id constraint. This allows cross-tenant modification by id.
  - _Fix:_ Require and pass tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or use Drizzle’s .where(eq(table.tenantId, tenantId)). Avoid relying on id-only updates for tenant-scoped tables.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings() has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant by accepting a tenantId argument and adding AND tenant_id = ${tenantId} (or Drizzle eq guard). If this backfill must run per-tenant, iterate per-tenant with an explicit tenantId.
- **server/embeddings.ts:716** — SELECT FROM memory_entries in backfillEmbeddingVecs() lacks a tenant_id WHERE clause — reads rows across all tenants.
  - _Fix:_ Scope the query to a tenant (accept tenantId and add AND tenant_id = ${tenantId}) or otherwise ensure this code runs per-tenant with an explicit tenant filter to prevent cross-tenant data access.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id constraint. This enables cross-tenant writes by id.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and ensure tenantId is derived from a trusted source. Prefer Drizzle .where(eq(table.tenantId, tenantId)).
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge in backfillEmbeddingVecs() has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (AND tenant_id = ${tenantId}) or run the backfill per-tenant; ensure any selection/read of tenant-scoped tables always includes tenant_id.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id constraint, permitting cross-tenant modification.
  - _Fix:_ Require tenantId for the update and add AND tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where with eq on tenantId). Avoid id-only updates on tenant-scoped tables.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' performed with no tenant_id filter (reads all tenants' rows at boot)
  - _Fix:_ Do not scan agent_runs across all tenants. Require a tenant context parameter (or iterate tenants) and add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(agentRuns.tenantId, tenantId))). Ensure rehydration only reads rows for the tenant being rehydrated.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' performed with WHERE eq(projects.id, projectId) but without an explicit tenant_id constraint
  - _Fix:_ Include tenant scoping in the UPDATE's WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId} or .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Never rely solely on a prior SELECT+in-memory check — the write must be guarded by tenant_id in SQL.
- **server/google-drive.ts:946** — INSERT INTO project_files omits tenant_id — inserting a project-scoped row without explicit tenant ownership column
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${projectId}, ...)). Every INSERT into tenant-scoped tables must set tenant_id explicitly and from a validated source.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} updates a tenant-scoped table row by id only, with no tenant_id constraint (possible cross-tenant modification).
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage helper that enforces tenant scoping (assertTenantContext) before performing updates.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} lacks tenant_id constraint and can modify other tenants' rows.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage layer method that enforces tenant ownership before updating.
- **server/heartbeat.ts:111** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} does not include tenant_id in the WHERE clause.
  - _Fix:_ Constrain by tenant_id (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use assertTenantContext() and a tenant-aware update helper to ensure only the owning tenant's row is modified.
- **server/heartbeat.ts:119** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} updates schedule rows without tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${sched.tenant_id}) or defer to a storage function that verifies tenant ownership before persisting changes.
- **server/heartbeat.ts:260** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} updates a tenant-scoped heartbeat_tasks row using only id with no tenant_id restriction, risking cross-tenant mutation.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call storage.updateHeartbeatTask(...) which enforces tenant scoping. Do not use id-only updates for tenant-scoped tables.
- **server/heartbeat.ts:264** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} is executed without tenant_id in the WHERE clause (cross-tenant update risk).
  - _Fix:_ Constrain updates by tenant_id (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the tenant-aware storage API so the update cannot affect rows belonging to other tenants.
- **server/heartbeat.ts:2921** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates approval_status without constraining by tenant_id — this can change approval state for another tenant's task with the same id.
  - _Fix:_ Add tenant_id to the UPDATE predicate (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or prefer storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }) which should enforce tenant ownership before mutating the row.
- **server/heartbeat.ts:3374** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without a tenant_id condition (cross-tenant write risk).
  - _Fix:_ Restrict the UPDATE to the owning tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the tenant-aware storage API so approval_status cannot be modified for rows from other tenants.
- **server/job-queue.ts:98** — UPDATE agent_jobs ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ...) updates/claims jobs across all tenants with no tenant_id restriction — global cross-tenant claim/modification.
  - _Fix:_ Either scope this claim per-tenant (accept a tenantId and add `AND tenant_id = ${tenantId}`), or implement per-tenant queues. If global workers are required, add explicit tenant-aware fencing so rows are never modified on behalf of an unrelated tenant (e.g. include tenant_id in the inner SELECT and the outer UPDATE WHERE predicates).
- **server/job-queue.ts:134** — UPDATE agent_jobs (reclaimExpiredLeases step 1) sets status='failed_terminal' for expired leases across all tenants — no tenant_id constraint.
  - _Fix:_ Scope reclaim operations by tenant (add `AND tenant_id = ${tenantId}`) or run per-tenant reclamation. Ensure the job that performs reclamation is explicitly authorized for the tenant(s) it touches.
- **server/job-queue.ts:148** — UPDATE agent_jobs (reclaimExpiredLeases step 2) requeues expired jobs back to pending with no tenant_id constraint — touches rows across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. include `AND tenant_id = ${tenantId}`) or run the function per-tenant, ensuring only the intended tenant's jobs are modified.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts SET ... WHERE id = ${row.id} — update lacks tenant_id constraint. This writes to a tenant-scoped table without constraining by tenant, allowing cross-tenant modification if row.id is controlled or mis-associated.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or otherwise bind the update to the returned tenant id. If this is an intentional system-wide operation, make that explicit and ensure admin-only privileges + auditing are in place.
- **server/lobster.ts:605** — resumeWorkflow accepts an opaque resume token and loads paused state from disk, then continues executing steps using state.invokerTenantId without verifying the caller's tenant or authorization. The runLobster 'resume' path calls resumeWorkflow without checking that the requester belongs to the same tenant that originally created the paused state. This allows any caller who obtains/guesses a resume token to resume and execute tool/command steps under the original invoker's tenant context (tenant ID stamped into tool args), potentially causing cross-tenant actions and writes.
  - _Fix:_ Before resuming, verify the caller's authenticated tenant equals the paused state's invokerTenantId (or deny if absent). Tie resume tokens to a specific tenant when created (e.g. HMAC the token with tenant id or store a server-side token→tenant mapping) and enforce that mapping on resume. Make resume tokens single-use and time-limited, validate the requester's auth context (session/API key) matches the token owner, and explicitly refuse to execute steps under a different tenant. Log and audit resume attempts and consider encrypting/expiring state files to reduce brute-force risk.
- **server/memory-graph.ts:141** — refreshCategoryCount performs a COUNT(*) FROM memory_entries without constraining tenant_id, and then UPDATEs memory_categories WHERE id = ${categoryId} also without tenant scoping. Both the count and the update can leak/affect other tenants.
  - _Fix:_ Pass tenantId into refreshCategoryCount and add tenant filters to the subquery and the outer UPDATE (e.g. SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ...; UPDATE memory_categories ... WHERE id = ${categoryId} AND tenant_id = ${tenantId}).
- **server/memory-graph.ts:210** — INSERT into memory_links does not include tenant_id. Tenant_id is required on tenant-scoped tables; omitting it will create cross-tenant/unscoped rows.
  - _Fix:_ Include tenant_id in the INSERT columns and values (e.g. INSERT INTO memory_links (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)).
- **server/minerva-planner.ts:355** — DecidePlan's CAS UPDATE on plans does not include tenant_id in the WHERE clause (UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval'). This allows changing a plan in any tenant by id.
  - _Fix:_ Require and use tenant scoping in the CAS (e.g. add AND tenant_id = ${expectedTenantId}). If decidePlan must be tenant-checked, accept (or derive) tenantId and include tenant_id = ${tenantId} in the WHERE to enforce isolation.
- **server/minerva-planner.ts:460** — Rollback UPDATE resets decision metadata with WHERE id = ${args.planId} AND status = 'revising' but omits tenant_id; this can roll back a plan in another tenant.
  - _Fix:_ Include tenant scoping in the rollback UPDATE (e.g. WHERE id = ${args.planId} AND status = 'revising' AND tenant_id = ${row.tenant_id}). Require tenant context on decide/revise flows.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This iterates all tenants and exposes tenant-scoped rows (including secrets).
  - _Fix:_ Avoid global scans. Restrict queries to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate an explicit, validated list of tenant IDs and run per-tenant queries. If this must be an admin/global job, limit reachable columns (do NOT fetch secrets) and require an explicit, auditable admin context.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This reads tenant rows across all tenants.
  - _Fix:_ Add a tenant_id constraint or only query tenants the caller is authorized to act on. If discovering tenants is necessary, use an allowlisted/admin-only code path that does not return secrets and is properly authorized and audited.
- **server/oauth-subscriptions.ts:1258** — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (proactiveTokenRefresh: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This enumerates all tenant rows, violating per-tenant isolation.
  - _Fix:_ Run refresh logic per-tenant (accept/derive a tenantId and scope the SELECT), or otherwise restrict to an explicit admin-run list of tenants. Never select tenant-scoped secrets across ALL tenants in an unaudited/global query.
- **server/oauth-subscriptions.ts:1328** — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE). This iterates rows for every tenant without per-tenant filtering/authorization.
  - _Fix:_ Change to per-tenant iteration with explicit tenantId scoping or require an explicit privileged/admin context. Ensure calls that will act on tenant rows validate authorization and avoid bulk-selecting tenant-scoped records.
- **server/plan-executor.ts:116** — UPDATE against tenant-scoped table 'plans' (appendExecutionLog) has no tenant_id constraint (WHERE id = ${planId} only). This allows cross-tenant modification of plan rows.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or load the plan, verify plan.tenant_id matches the caller/system context, and then update. Prefer parameterized Drizzle APIs (eq(plans.tenantId, tenantId)) rather than injecting identifiers into raw SQL.
- **server/plan-executor.ts:513** — CAS UPDATE claiming a plan (UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved' ...) does not constrain by tenant_id. A process could claim/operate-on a plan from any tenant by id.
  - _Fix:_ Constrain the CAS UPDATE by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved'), or first SELECT the plan and verify its tenant_id equals the expected caller tenant before updating. Use Drizzle eq(table.tenantId, tenantId) for safety.
- **server/plan-executor.ts:527** — UPDATE plans SET status='failed' WHERE id = ${planId} (on empty/invalid plan) lacks a tenant_id guard, enabling cross-tenant writes to the plans table.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${callerTenantId}) or validate the plan's tenant before performing the update. Use parameterized Drizzle queries rather than string interpolation.
- **server/plan-executor.ts:725** — Final UPDATE to plans (setting status/composing execution_log) uses WHERE id = ${planId} without tenant_id filtering and also injects JSON.stringify(...) directly into SQL. This both allows cross-tenant writes and uses string-interpolated JSON derived from step outputs (request/agent-derived) inside SQL.
  - _Fix:_ Require tenant scoping (WHERE id = ${planId} AND tenant_id = ${callerTenantId}). Avoid injecting JSON via string interpolation; bind JSON via parameters or use Drizzle insert/update helpers that safely pass JSONB parameters. Validate the plan's tenant before composing final updates.
- **server/plan-executor.ts:759** — Error-path UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' has no tenant_id restriction, allowing the process to mark arbitrary tenant plans failed.
  - _Fix:_ Include tenant_id in the WHERE clause or check the plan's tenant before updating. Prefer explicit tenant-scoped CAS operations using parameterized queries.
- **server/plan-executor.ts:774** — resumeStuckPlans() performs a broad UPDATE across the plans table (SET status='approved' WHERE status='executing' AND updated_at < ...), with no tenant restriction. This rolls back 'executing' plans across all tenants, violating tenant isolation.
  - _Fix:_ Limit recovery operations to a specific tenant context (pass in caller tenantId) or perform the update per-tenant (e.g. WHERE tenant_id = ${tenantId} AND status='executing' ...). If this routine is intended to operate system-wide, ensure explicit design review and strong audit logs, but still avoid blind updates that mutate rows for other tenants unintentionally.
- **server/plan-executor.ts:784** — Selecting pending plans for re-kick: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 has no tenant_id constraint — the executor will enqueue plans across all tenants indiscriminately.
  - _Fix:_ Restrict pending-plan selection to a tenant scope (e.g. WHERE tenant_id = ${tenantId} AND status='approved') or iterate tenants explicitly. Ensure the executor obtains/validates the tenant context before scheduling executePlan for each id.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write to a tenant-scoped table without constraining by tenant_id. Even if id was read earlier, writes must include tenant scoping to prevent cross-tenant modification.
  - _Fix:_ Add tenant isolation to the UPDATE, e.g. UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId} (or use Drizzle .where(eq(table.tenantId, tenantId))). Validate that the fetched task belongs to the tenant before mutating.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates a tenant-scoped table without a tenant_id condition. This is a write that can affect rows from other tenants if IDs collide or are tampered with.
  - _Fix:_ Include tenant scoping on the UPDATE: WHERE id = ${desk.id} AND tenant_id = ${tenantId}. Alternatively, use a two-step verify-then-update pattern that checks desk.tenant_id === tenantId before updating.
- **server/process-governor.ts:977** — UPDATE governance_rules SET ... WHERE id = ${rule.id} executes a write against the tenant-scoped governance_rules table without constraining by tenant_id. This can increment trigger_count for rules in other tenants.
  - _Fix:_ Constrain the UPDATE to the tenant, e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}. Ensure logAction has the tenant context available when updating rules.
- **server/recurring-messages.ts:300** — UPDATE to tenant-scoped table `agent_knowledge` uses only id in WHERE clause (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}). There is no tenant_id constraint — this can modify another tenant's row if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or otherwise validate the row belongs to the tenant before updating. Never perform writes to tenant-scoped tables without enforcing tenant_id in the WHERE clause.
- **server/research-engine.ts:390** — DELETE statement deletes from tenant-scoped table `agent_knowledge` without tenant scoping (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will remove rows for all tenants, not just the intended tenant(s).
  - _Fix:_ Restrict deletes to a specific tenant (e.g. ADD AND tenant_id = ${tenantId}) or iterate per-tenant and delete only that tenant's expired rows. If this is intended as a global maintenance job, make the code explicitly require an authorized system context and/or record the tenant when deleting to avoid accidental cross-tenant data loss.
- **server/routes.ts:4324** — SELECT from conversations lacks tenant scoping — query: SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL. This reads tenant-scoped data without constraining tenant_id, allowing cross-tenant data exposure if conversationId is not validated.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or, better, fetch the conversation via the storage layer which enforces tenant ownership. Validate that the conversation belongs to the tenant before using its project_id.
- **server/routes.ts:4331** — INSERT into project_notes does not set tenant_id: INSERT INTO project_notes (project_id, note, author) VALUES (...). project_notes is tenant-scoped but tenant_id is omitted, so new rows will not be tenant-attributed and can break isolation.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'})). Also ensure projId is validated to belong to the tenant (using a tenant-scoped SELECT or storage API) before inserting. Prefer using a storage helper (e.g. storage.createProjectNote) that enforces tenant ownership.
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The statement `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` writes a row without an explicit tenant_id column.
  - _Fix:_ Supply the tenant_id explicitly in the INSERT (e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`) or use storage/drizzle helpers that set tenantId. Also validate ownership of referenced foreign ids before insert.
- **server/routes/conversations.ts:228** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` writes a tenant-scoped artifact without the tenant_id column set.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${conv.tenantId ?? tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})`) and ensure the project_id belongs to that tenant before inserting.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The statement INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (VALUES (..., ${tenantId}, ...)) or otherwise populate tenant_id from the authenticated tenant. e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ${req.body.note}, ${req.body.author || 'user'}) and ensure tenant ownership is validated before the insert.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload) does not include tenant_id. The statement INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...) omits tenant_id.
  - _Fix:_ Pass tenant_id explicitly on INSERT. e.g. INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ${f.fileName}, ${'uploads/'+uniqueName}, ${fileUrl || downloadPath}, ${f.mimeType || 'application/octet-stream'}, ${fileBuffer.length}). Validate tenant ownership of projectId before inserting.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload) does not include tenant_id. The statement INSERT INTO project_files (...) VALUES (...) omits tenant_id.
  - _Fix:_ Include tenant_id in the INSERT. e.g. INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size) VALUES (${projectId}, ${tenantId}, ${file.originalname}, ${filePath}, ${fileUrl || downloadPath}, ${file.mimetype}, ${file.size}). Ensure project ownership was validated earlier.
- **server/sculptor.ts:94** — UPDATE on tenant-scoped table `sculptor_sessions` lacks tenant_id constraint (WHERE id = ${session.id} only). This write can modify a row belonging to a different tenant if IDs collide or are misused.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise assert/validate the session belongs to the caller's tenant before updating.
- **server/sculptor.ts:101** — UPDATE on tenant-scoped table `sculptor_sessions` sets conversation_id but omits tenant_id in the WHERE clause (WHERE id = ${session.id} only).
  - _Fix:_ Constrain the update to the caller's tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or verify ownership before performing the update.
- **server/sculptor.ts:162** — UPDATE on tenant-scoped table `sculptor_sessions` performed in monitorSession lacks tenant_id constraint (WHERE id = ${sessionId} only).
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (i.e. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) to enforce tenant isolation, or fetch+validate the session's tenant before updating.
- **server/sculptor.ts:347** — UPDATE on tenant-scoped table `sculptor_sessions` (setting review_result) omits tenant_id in its WHERE clause (WHERE id = ${sessionId} only).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or otherwise verify the session returned by the earlier SELECT belongs to the caller's tenant before writing.
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger by id only (WHERE id = ${reservationId}) without constraining by tenant_id. agent_cost_ledger is tenant-scoped, so this can update another tenant's ledger row.
  - _Fix:_ Scope the UPDATE to the tenant that created the reservation. Pass tenantId into settleFusionReservation (or look it up atomically) and use WHERE id = ${reservationId} AND tenant_id = ${tenantId}; or validate the reservation belongs to the tenant before updating.
- **server/second-opinion.ts:424** — releaseFusionReservation updates agent_cost_ledger by id only (WHERE id = ${reservationId}) without tenant_id constraint, potentially touching another tenant's reservation row.
  - _Fix:_ Require the tenantId when releasing a reservation and include AND tenant_id = ${tenantId} in the WHERE clause (or verify reservation ownership prior to the UPDATE) to ensure tenant-scoped writes cannot cross tenants.
- **server/seed.ts:2860** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only id (no tenant_id constraint). This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership in the UPDATE (e.g. AND tenant_id = ${tenantId}) or join to a verification query to ensure the program belongs to the expected tenant before updating. Ensure tenantId value is trusted and validated.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id when inserting into a likely tenant-scoped table, creating records with no tenant binding or implicitly relying on defaults.
  - _Fix:_ Always set tenant_id explicitly on INSERTs into tenant-scoped tables (e.g. VALUES (${tenantId}, ${projectId}, ${conversationId})). Additionally, verify that the referenced project_id and conversation_id belong to the same tenant (ownership check) before inserting to prevent cross-tenant linking.
- **server/self-improvement.ts:401** — UPDATE on tenant-scoped table `experiments` performed without constraining by tenant_id (only filtering by experiments.id). This allows modifying rows across tenants if an id from another tenant is used.
  - _Fix:_ Include the tenant constraint in the WHERE clause for updates. For example: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Ensure config.tenantId is validated and present before performing the update.
- **server/self-improvement.ts:415** — Error-path UPDATE on tenant-scoped table `experiments` performed without tenant_id constraint. The catch block updates status/outcome for an experiment by id only, risking cross-tenant modification.
  - _Fix:_ Apply the same tenant guard in the catch update as in the success path: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Validate and require tenant context before issuing the UPDATE.
- **server/self-improvement.ts:546** — INSERT into tenant-scoped `experiments` uses tenantId: data.tenantId! which is optional in the function signature. If caller omits tenantId, this can insert without a valid tenant_id (or throw), violating the invariant that tenant_id must be provided on INSERTs.
  - _Fix:_ Validate and require tenantId before inserting (e.g. if (!data.tenantId) throw new Error('tenantId required')). Do not use non-null assertion (!) on optional tenant inputs; explicitly check and fail-closed when tenant context is missing.
- **server/storage.ts:396** — attachCitations() updates the tenant-scoped `messages` table via raw SQL (UPDATE messages SET citations = … WHERE id = ${messageId}) with no tenant_id constraint — unscoped write to a tenant table.
  - _Fix:_ Require and validate tenantId (or derive and validate ownership of the message) before performing the UPDATE. Use a parameterized Drizzle update with .where(and(eq(messages.id, messageId), eq(messages.tenantId, tenantId))). Never perform cross-tenant writes without an explicit tenant filter and explicit authorization checks.
- **server/storage.ts:803** — updateMemoryEmbedding() issues db.update(memory_entries).set(...) WHERE id = ${id} with no tenant_id constraint — unscoped UPDATE against the tenant-scoped `memory_entries` table.
  - _Fix:_ Require tenantId and include it in the WHERE clause (e.g. .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))). If the caller cannot supply tenantId, derive it from a validated parent resource and verify ownership before updating.
- **server/storage.ts:815** — updateKnowledgeEmbedding() updates `agent_knowledge` with .where(eq(agent_knowledge.id, id)) but does not constrain by tenant_id — unscoped write to a tenant-scoped table.
  - _Fix:_ Require and validate tenantId and add it to the WHERE (and(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId))). If this runs as a cross-tenant indexing job, isolate and document the job as a trusted admin task and implement strong audit logging.
- **server/storage.ts:838** — archiveExpiredMemories() executes raw SQL: UPDATE memory_entries SET status = 'archived' WHERE ... without any tenant_id constraint — unscoped bulk UPDATE across all tenants.
  - _Fix:_ Apply tenant scoping; accept a tenantId parameter (or iterate tenants) and include AND tenant_id = ${tenantId} in the WHERE, or only run this as an explicit admin/maintenance job that iterates tenants one-at-a-time and logs actions. Avoid global, tenant-agnostic updates to tenant-scoped tables.
- **server/storage.ts:855** — archiveStaleMemories() runs a raw UPDATE on `memory_entries` (WHERE status = 'active' AND created_at <= …) without tenant_id filtering — unscoped bulk write across tenants.
  - _Fix:_ Same as archiveExpiredMemories(): scope the update by tenant (require tenantId param or iterate per-tenant) and add eq(memoryEntries.tenantId, tenantId) to the WHERE. Ensure the maintenance path is auditable and authorized.
- **server/storage.ts:1296** — getAllDataForExport() performs many unscoped SELECTs from tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, etc.) and returns platform-wide data with no tenant filtering — mass cross-tenant data exposure risk.
  - _Fix:_ Restrict this API to an authorized admin role and/or require an explicit tenantId to scope exports. If an admin export of all tenants is necessary, protect the endpoint heavily (RBAC, audited operations) and document that it is an admin-only bulk export. Prefer per-tenant export endpoints that always require tenantId.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (where clause only checks name). This allows cross-tenant discovery/affects uniqueness checks.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))) or otherwise validate ownership of the row for the current tenant before relying on the result.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id lacks a tenant_id constraint (.where(eq(customTools.id, inserted.id))). Reading tenant-scoped JS/tool bodies without tenant scoping can leak sensitive code across tenants.
  - _Fix:_ Restrict selects by tenant as well as id (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) or fetch the row via the inserting transaction result rather than a second unscoped lookup.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` uses .where(eq(customTools.id, tool.id)) without also constraining tenant_id. This allows modifying another tenant's row if an id is guessed/reused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, expectedTenantId)))) and verify the caller's tenant owns the row before mutating.
- **server/tool-learning.ts:316** — DELETE on tenant-scoped table `custom_tools` uses .where(eq(customTools.id, tool.id)) with no tenant_id constraint. Deleting by id only can remove another tenant's tool if the id is known/guessed.
  - _Fix:_ Constrain deletes to the tenant (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, expectedTenantId)))) and validate ownership/authority before performing the delete.
- **server/tool-learning.ts:365** — UPDATE to increment usageCount on tenant-scoped `custom_tools` uses .where(eq(customTools.id, tool.id)) without tenant_id constraint. This allows updating other tenants' rows by id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or use an atomic statement that validates tenant ownership before updating usage metrics.
- **server/tools.ts:2979** — INSERT into project_files does not set tenant_id (tenant-scoped table writes must include tenant_id)
  - _Fix:_ Include tenant_id in the INSERT columns and VALUES (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure the tenantId is derived from the authenticated context, not user-controlled input.
- **server/tools.ts:2993** — INSERT into project_notes omits tenant_id (writes to a tenant-scoped table must include tenant_id)
  - _Fix:_ Add tenant_id to the inserted columns and pass the caller's tenantId explicitly (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Verify ownership before writing.
- **server/tools.ts:3018** — INSERT into project_conversations does not set tenant_id (tenant-scoped table writes must include tenant_id)
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Also validate ownership (project and conversation belong to tenant) before inserting.
- **server/tools.ts:4537** — SELECT from presenter_sessions uses presentation_id without constraining by tenant or verifying ownership (no WHERE tenant_id = ... and no assertConversation/assertPresentation ownership check). This can leak presenter session tokens across tenants.
  - _Fix:_ Verify the presentation/presenter_session belongs to the caller before reading (e.g. call an assertPresentationInTenant(presentationId, tenantId) helper) and/or add an explicit tenant filter to the query: SELECT token FROM presenter_sessions WHERE presentation_id = ${presentationId} AND tenant_id = ${tenantId} ORDER BY ... LIMIT 1.
- **server/tools/domains/files/handlers.ts:835** — INSERT INTO project_files omits tenant_id when inserting a tenant-scoped row (project_files). Every INSERT against tenant-scoped tables must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT column list and pass the caller's tenant (e.g. VALUES (${projId}, ${fileName}, ..., ${stat.size}, ${'agent'}, ${tenantId}) or explicitly INSERT INTO project_files (project_id, tenant_id, file_name, file_path, ...) VALUES (...). Ensure tenantId comes from the trusted ctx (verified project ownership) and not from untrusted request params.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without constraining tenant (missing tenant_id check).
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or perform the UPDATE via a storage-layer function that enforces tenant_id = ctx.tenantId. This prevents accidental or malicious cross-tenant updates if ids collide or are misused.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) without constraining tenant (missing tenant_id check).
  - _Fix:_ Restrict the UPDATE to the caller's tenant, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}, or use a parameterized query that includes tenant_id, or perform the update through a storage API that enforces tenant ownership.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without constraining tenant (missing tenant_id check).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or route updates through a storage-layer function that validates the row belongs to ctx.tenantId before modifying it.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs runs an UPDATE on videoJobs for stale statuses without any tenant_id constraint — this can mark jobs for all tenants as failed in a single global sweep.
  - _Fix:_ Do not run an unscoped update against a tenant-scoped table. Either run the sweep per-tenant (iterate known tenants or accept a tenant parameter) or include tenant_id in the WHERE clause. If a global system sweep is required, use a dedicated system-level table/flag or a safe, audited mechanism that cannot accidentally modify other tenants' active jobs.

### HIGH (163)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without constraining to a caller tenant. The heartbeat reads experiments across all tenants: `FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100`.
  - _Fix:_ Do not read tenant-scoped rows across tenants. Either iterate per-tenant (call this routine per tenant) or add a tenant filter (e.g. WHERE tenant_id = <tenantId>) so every SELECT is scoped to the intended tenant. Ensure the same applies to any follow-on updates/creates that use the rows.
- **server/agent-activity.ts:122** — UPDATE against tenant-scoped table `agent_activity` is performed using only the activity id (WHERE id = ${activityId}) with no tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Require the caller to pass tenantId and include it in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}), or load the row first and validate ownership before updating. Fail closed if the tenant can't be validated.
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with no tenant_id constraint. This can update another tenant's eval row if ids are guessed or leaked.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or otherwise validate that the eval row belongs to the caller's tenant before writing. Ensure evalId comes from a trusted, tenant-scoped context.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` without tenant filtering in retryPendingMinervaRouting: the query returns rows across tenants (`WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER}`).
  - _Fix:_ Scope the SELECT to a tenant (or iterate per-tenant) and validate tenant_id before routing. If this function intentionally operates globally, explicitly iterate tenants and perform per-tenant scoped queries to preserve tenant isolation.
- **server/agentic/approvals.ts:134** — Bulk UPDATE of `agent_approvals` to expire stale approvals is executed without a tenant constraint: it updates all tenants' pending approvals whose expiresAt < now. This performs cross-tenant writes.
  - _Fix:_ Run the expiry sweep per-tenant (or include tenant_id in the UPDATE) so you only affect the intended tenant's rows. Alternatively validate tenant ownership per-row before writing. Ensure any RETURNING/loop that follows only operates on tenant-scoped results and does not mutate other tenants' runs.
- **server/agentic/department-budgets.ts:143** — Cross-tenant SELECT: the query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads tenant-scoped rows for all tenants without constraining to a caller tenant_id.
  - _Fix:_ Constrain reads to an explicit tenant (e.g. add `WHERE tenant_id = ${tenantId}`) or ensure this function is only callable by an explicit admin context and document/guard that fact. If the intention is a system-wide sweep, make that explicit (admin-only) and perform the operation under an auditable admin path rather than an unscoped query. Avoid global scans of tenant-scoped tables in code paths that can be reached by non-admin callers.
- **server/agentic/resume-worker.ts:66** — SELECT joins tenant-scoped tables `agent_runs` and `agent_approvals` without constraining to a single tenant (no WHERE tenant_id = <caller tenant>). The query scans across tenants (even though it joins a.tenant_id = r.tenant_id) and returns rows for all tenants.
  - _Fix:_ Run per-tenant queries or explicitly filter the select by a canonical tenant parameter (e.g. WHERE r.tenant_id = ${tenantId}). If an administrative multi-tenant sweep is intended, require and document an explicit admin context; otherwise restrict to the caller's tenant.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT on conversations scans conversations across all tenants (no tenant_id WHERE clause). A global backfill read touches tenant-scoped rows without an application-level tenant filter.
  - _Fix:_ Avoid scanning tenant-scoped tables without a tenant filter. Either iterate tenants explicitly (query the tenants table and run per-tenant), or include a tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}) so each query is constrained to a single tenant.
- **server/auto-consolidation.ts:335** — SELECT DISTINCT tenant_id FROM memory_entries reads a tenant-scoped table without a tenant filter. The scheduler is enumerating tenant_ids by scanning the tenant-scoped table without an explicit tenant isolation guard in SQL.
  - _Fix:_ Replace the scan with an explicit tenant enumeration (e.g. maintain an active-tenants table or query the tenants table) or otherwise ensure the scan is done in a safe, audited manner. If scanning memory_entries is required, make the intent explicit and safe (e.g. only select tenant_id column but ensure this is an allowed admin/global operation and document the exception). Prefer per-tenant queries with WHERE tenant_id = <tenant> when performing tenant-scoped operations.
- **server/auto-memorize.ts:47** — SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' ... has no tenant_id constraint — reading/writing agent_knowledge is tenant-scoped but this query is not constrained to a caller tenant.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. AND tenant_id = ${tenantId}) so watermarks are per-tenant, or move watermark state to a truly global table if intentional (and document it).
- **server/auto-memorize.ts:62** — SELECT id FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} LIMIT 1 lacks a tenant_id predicate, so subsequent UPDATE/INSERT may act on or return rows across tenants.
  - _Fix:_ Include tenant_id in the SELECT where appropriate (e.g. WHERE tenant_id = ${tenantId}) so the lookup and subsequent UPDATE target only the intended tenant row.
- **server/auto-memorize.ts:163** — Global SELECT from messages (SELECT m.tenant_id, m.role, m.content ...) reads messages across tenants without filtering by tenant_id. Although rows are later partitioned in-app, the SQL read itself touches tenant-scoped data without an explicit tenant predicate.
  - _Fix:_ If the service must scan many tenants, implement an explicit per-tenant loop (enumerate tenants and run the messages SELECT with WHERE m.tenant_id = ${tenantId}) or otherwise document and restrict this global-scan as an audited admin operation. Prefer per-tenant SQL filters to satisfy the isolation invariant.
- **server/auto-transcript.ts:128** — SELECT from tenant-scoped table `conversations` without a tenant_id constraint (backfill selects across all tenants).
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE c.tenant_id = ${tenantId}) or iterate per-tenant and pass tenant_id into the query so the backfill cannot enumerate conversations from other tenants.
- **server/auto-transcript.ts:153** — SELECT joining project_conversations/conversations/projects (alsoLinked) runs without constraining by tenant_id — it will return rows across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE c.tenant_id = ${tenantId}) or run the query per-tenant, ensuring only rows for the intended tenant are selected.
- **server/auto-transcript.ts:171** — SELECT (missingLinks) queries `conversations` for rows lacking a tenant_id constraint, potentially returning data across tenants.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or refactor to run per-tenant with tenant_id passed into the query to prevent cross-tenant enumeration.
- **server/business-tools.ts:91** — INSERT into invoice_items does not set tenant_id. Tenant-scoped rows must include tenant_id on INSERT to enforce isolation; inserting invoice items without tenant_id can create cross-tenant data/ownership gaps.
  - _Fix:_ Add tenant_id to the invoice_items INSERT (e.g. include tenant_id column and VALUES (${tid}, ...)) or otherwise validate and provenance the invoice_id belongs to the caller's tenant before inserting. Ensure invoice_items is tenant-scoped and always populated with the caller's tenant id.
- **server/business-tools.ts:289** — logInteraction writes customer_interactions.customer_id from caller input without first verifying the customer row belongs to the caller's tenant. Even though tenant_id is set on the interaction row, the code does not confirm ownership of the referenced customer, allowing callers to reference IDs from other tenants.
  - _Fix:_ Before inserting, verify ownership with a query like SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1 and fail closed if not found. Always validate foreign IDs from caller input belong to the caller's tenant before reading/writing related records.
- **server/capability-review.ts:491** — UPDATE against tenant-scoped table `capability_reviews` omits tenant_id constraint in WHERE clause (UPDATE capability_reviews SET reused = true ... WHERE id = ${pending.reviewId} AND reused = false). This allows a cross-tenant write if an attacker can trigger/observe review ids or pending entries.
  - _Fix:_ Include tenant scoping on the UPDATE and bind the tenant id from a trusted source. Record the tenantId when inserting the pendingReuseByConversation entry (extend PendingReuse to include tenantId), and change the UPDATE to: UPDATE capability_reviews SET reused = true, reused_capability = ${...} WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false. Also validate the conversation/tenant mapping before performing the update to ensure the caller owns the tenant context.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` without tenant constraint — query: SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...
  - _Fix:_ Include an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(project_files.tenantId, tenantId)). If you rely on prior ownership verification of projectId, add the tenant condition as defense-in-depth to avoid accidental cross-tenant access.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` without tenant constraint — query: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...
  - _Fix:_ Add an explicit tenant constraint (AND tenant_id = ${tenantId}) or use Drizzle .where(eq(project_notes.tenantId, tenantId)). Even when project ownership was checked earlier, prefer requiring tenant_id in the query as defense-in-depth.
- **server/chat-engine.ts:4331** — storage.attachCitations(...) is called without any tenant context. If attachCitations inserts into a tenant-scoped table it must be given the tenant_id (the surrounding code passes tenantId to other storage calls). Currently tenant is not passed to attachCitations, risking failed INSERTs or cross-tenant writes.
  - _Fix:_ Change attachCitations call to include the tenant id/tenant scope (e.g. storage.attachCitations(messageId, citations, { tenantId: tenantIdForScope }) or storage.attachCitations(messageId, citations, tenantIdForScope) depending on API). Ensure the storage.attachCitations implementation requires and uses tenant_id for its INSERT and enforces/validates it server-side.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, toolName, ...) is invoked without any tenant identifier. If recordToolExecution writes to tenant-scoped tables, the tenant_id is not provided here, so writes could be unscoped or fail.
  - _Fix:_ Modify recordToolExecution to accept and require an explicit tenantId (or resolve tenant from conversationId in a verified way) and update this call site to pass tenantId/tenantIdForScope. Validate ownership server-side before inserting/updating any tenant-scoped rows.
- **server/commitment-drafter.ts:234** — UPDATE statement updates commitments by id only: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the owning tenant. Include AND tenant_id = ${tenantId} (or Drizzle .where(eq(commitments.tenantId, tenantId))) so the rollback/unclaim cannot affect other tenants: `WHERE id = ${row.id} AND tenant_id = ${tenantId}`.
- **server/data-protection.ts:182** — SELECT role, content FROM messages WHERE conversation_id = ${conversationId} in backupConversationToDrive lacks a tenant_id predicate. Reading messages without tenant scoping risks returning another tenant's rows.
  - _Fix:_ Constrain queries against messages with an explicit tenant predicate (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}). If conversation ownership has been validated earlier, still add tenant_id to the SELECT to meet the app-level isolation invariant.
- **server/data-protection.ts:189** — SELECT content FROM compaction_archives WHERE conversation_id = ${conversationId} in backupConversationToDrive lacks tenant_id constraint.
  - _Fix:_ Add tenant_id scoping to compaction_archives queries (e.g. AND tenant_id = ${tenantId}), or otherwise ensure the compaction_archives rows are read only when tied to a verified tenant context.
- **server/data-protection.ts:325** — In backupTenantDataToDrive, SELECT title, content FROM project_notes WHERE project_id = ${proj.id} does not include tenant_id. Project-scoped queries that omit tenant_id can leak other tenants' notes if project_id values collide or FK assumptions are broken.
  - _Fix:_ Include tenant_id predicates on project_notes (and other project-scoped tables) when reading by project_id (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}), or otherwise validate ownership before selecting.
- **server/data-protection.ts:336** — In backupTenantDataToDrive, SELECT file_name,... FROM project_files WHERE project_id = ${proj.id} has no tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or validate the project ownership and include tenant_id when selecting project_files to enforce tenant isolation.
- **server/data-protection.ts:369** — In backupTenantDataToDrive, SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} omits tenant_id. Counting messages without tenant scoping can expose cross-tenant counts or cause backups to include other tenants' messages.
  - _Fix:_ Constrain message counts by tenant (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise verify conversation ownership and add explicit tenant_id predicates to all message queries.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog performs UPDATE on tenant-scoped table delivery_logs without constraining by tenant_id (WHERE only checks id). This lets a caller who can supply or reference a delivery id update another tenant's row.
  - _Fix:_ Require the caller to pass tenantId (or look it up and validate ownership) and include it in the WHERE clause. e.g. db.update(deliveryLogs).set(updates).where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). Audit all callers to pass/validate tenantId before calling updateDeliveryLog.
- **server/delivery-pipeline.ts:1018** — getDeliveryByStripePayment selects from tenant-scoped table delivery_logs by stripePaymentId without a tenant_id constraint. This can return another tenant's delivery row given a payment id.
  - _Fix:_ Add a tenantId parameter (or require caller to provide tenant context) and include eq(deliveryLogs.tenantId, tenantId) in the WHERE. If the lookup must remain cross-tenant, explicitly document and protect it at the callsite (but prefer per-tenant scoping).
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed without a tenant_id filter. Even though collection existence is checked earlier, the DELETE statement itself is not explicitly constrained to the tenant, violating the invariant that every write/read to a tenant-scoped table include tenant scoping.
  - _Fix:_ Add tenant scoping to the DELETE: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Alternatively, pass an explicit tenantId to the helper performing the delete and use it in the WHERE clause.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates a tenant-scoped row by id only, with no tenant_id constraint. This can modify another tenant's row if an id is mis-bound.
  - _Fix:_ Include tenant filter in the UPDATE: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer passing tenantId through the embedding loop so every UPDATE is explicitly tenant-scoped.
- **server/email.ts:366** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without tenant/inbox scoping. inbox_messages is tenant-scoped (tenant_id present on INSERT); this cross-tenant lookup can leak or collide with other tenants' messages.
  - _Fix:_ Scope the existence check to the relevant tenant/inbox (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} OR message_id = ${msgId} AND inbox_id = ${inboxId}). Derive tenantId from the inbox context before the check and use it in the WHERE.
- **server/email.ts:410** — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) queries inbox_messages across all tenants with no tenant filter in backfillEmptyBodies. This scans a tenant-scoped table globally.
  - _Fix:_ Either iterate per-tenant (or per-inbox) and include tenant_id = ${tenantId} (or inbox_id) in the WHERE, or otherwise explicitly document and limit this global operation and protect it with admin-only enforcement. Prefer per-tenant scoping to satisfy the isolation invariant.
- **server/email.ts:424** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} performs an update on a tenant-scoped table without a tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE inbox_messages SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId} (obtain tenantId from the initial, scoped SELECT). Ensure the row being updated was retrieved under the same tenant context before updating.
- **server/embeddings.ts:244** — Use of sql.raw(`'${vec}'::vector`) where vec is derived from a caller-provided query embedded into SQL via string interpolation. Although vecLiteral validates numeric shape, embedding data originates from request/model output — using sql.raw with request-derived content is an injection/isolation risk per policy.
  - _Fix:_ Avoid string-interpolating embedding literals into sql.raw. Use parameterized statements or bind the vector as a parameter (e.g. pass the vector bytes as a parameter and cast on the server side, or use pgvector-safe parameterization). If raw interpolation is unavoidable, centralize and harden validation and add an explicit code comment and tests proving safety, but parameterization is recommended.
- **server/embeddings.ts:559** — Use of sql.raw(`'${vec}'::vector`) in vectorSearchKnowledge with vec produced from generateEmbedding (request/model-derived). This interpolates request-derived data into raw SQL.
  - _Fix:_ Replace the raw interpolation with a parameterized binding (e.g. compare to $1::vector) or use Drizzle mechanisms to safely pass the vector as a query parameter. Ensure any embedding value passed into sql.raw is not directly interpolated from untrusted/request-derived input.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' uses only project id in WHERE (no tenant_id constraint) before doing ownership checks in code
  - _Fix:_ Constrain the SQL to the caller's tenant in the same query (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId} or Drizzle .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Avoid fetching rows across tenants and then checking ownership in application logic — fail the SQL if tenant mismatches.
- **server/google-drive.ts:825** — SELECT projects by projectId (uploadAndShare) without scoping the query to the caller's tenant (no tenant_id in WHERE) before using project data
  - _Fix:_ Add tenant constraint to the SELECT (e.g. WHERE id = ${projectId} AND tenant_id = ${params.tenantId}). Prefer constructing the SQL so only rows belonging to the caller's tenant are returned instead of post-hoc JS checks.
- **server/google-drive.ts:1003** — SELECT projects to verify project ownership in searchDriveFiles uses only project id in WHERE (no tenant_id constraint) prior to application-level tenant checks
  - _Fix:_ Scope the lookup by tenant in SQL (WHERE id = ${projectId} AND tenant_id = ${params.tenantId}) so the DB enforces ownership rather than relying on a later JS comparison.
- **server/google-drive.ts:1066** — SELECT projects to fetch driveFolderId (searchDriveFiles pass 2) uses only project id in WHERE (no tenant_id constraint)
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE id = ${projectId} AND tenant_id = ${params.tenantId}) to ensure driveFolderId is only read for projects belonging to the caller's tenant.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` is executed with no tenant_id constraint (global scan).
  - _Fix:_ If this is intended to operate per-tenant, add a tenant_id parameter and include WHERE tenant_id = ${tenantId}. If this must run as an admin/global job, document and gate it explicitly (require an explicit admin flag) and/or iterate tenants explicitly rather than issuing an unconstrained SELECT to avoid accidental cross-tenant reads.
- **server/health-audit.ts:333** — UPDATE on `code_proposals` (SET status='archived_stale' WHERE id = ANY(...)) executes without constraining by tenant_id, allowing cross-tenant modification.
  - _Fix:_ Restrict the UPDATE to the intended tenant by adding AND tenant_id = ${tenantId} (or include tenant_id in the id-selection subquery). Alternatively, fetch and verify the tenant_id for each id before updating, or require an explicit admin context and document it.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) is executed without tenant_id filtering, allowing disabling tasks across tenants.
  - _Fix:_ Constrain the UPDATE to the target tenant with AND tenant_id = ${tenantId}, or verify each task's tenant before disabling. If this is an admin operation, require an explicit admin context and validate intent.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images writes rows keyed only by session_id (and does not include tenant_id). If presenter_slide_images is tenant-scoped (linked to presenter_sessions), this allows cross-tenant writes or tampering via a crafted session_id.
  - _Fix:_ Include tenant_id in the INSERT (and ON CONFLICT) or otherwise validate that session_id belongs to the current tenant before inserting. Prefer to SELECT/validate presenter_sessions WHERE id=${sessionId} AND tenant_id=${tenantId} and fail if mismatch, then write with explicit tenant_id.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a session record without verifying tenant ownership (no tenant_id constraint).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or first SELECT the session to verify it belongs to the current tenant. For safety, always constrain updates to tenant_id when modifying tenant-scoped rows.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table research_schedules is executed without constraining by tenant_id (cross-tenant read).
  - _Fix:_ Restrict the query to an explicit tenant or iterate per-tenant. Example: add AND tenant_id = ${someTenantId} or, if you must scan all tenants, fetch tenant IDs first and query per-tenant so every read is explicitly scoped. Avoid an unconstrained `SELECT * FROM research_schedules ...`.
- **server/heartbeat.ts:658** — SELECT * FROM event_log WHERE status = 'pending' ... fetches pending events across all tenants with no tenant_id filter. Background processing then acts on those rows — this is an unconstrained read of a tenant-scoped table.
  - _Fix:_ Process events per-tenant (e.g. iterate getActiveTenantIds() and SELECT WHERE tenant_id = ${tid} AND status='pending') or add an explicit tenant_id predicate. Ensure any background loop explicitly scopes each query to the tenant it is processing.
- **server/heartbeat.ts:1828** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... is executed without tenant scoping; heartbeat_logs is tenant-scoped so this is a cross-tenant read.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or call a tenant-aware storage helper (assertTenantContext(task.tenantId) and then query using that tenantId) so you only inspect logs belonging to the intended tenant.
- **server/internal-resolver.ts:91** — UPDATE of tenant-scoped table `event_log` (finalizeEvent) has no tenant_id constraint in WHERE clause — allows finalizing events across tenants.
  - _Fix:_ Require and pass the event's tenant_id into finalizeEvent, and include `AND tenant_id = ${tenantId}` (or equivalent Drizzle .where(eq(table.tenant_id, tenantId))) in the UPDATE WHERE clause so only the owning tenant's row can be modified.
- **server/internal-resolver.ts:106** — SELECT from tenant-scoped table `plans` uses only `id` in WHERE (no tenant_id) when resolving a plan — reads cross-tenant rows without ownership check.
  - _Fix:_ Query the plan by both id AND tenant_id (or fetch and validate the plan.tenant_id matches caller's tenant) before taking action. Pass tenant context into resolvePlanProposed or add an explicit ownership check.
- **server/internal-resolver.ts:170** — UPDATE on tenant-scoped table `code_proposals` omits tenant_id in WHERE clause — may mutate proposals belonging to other tenants.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` on the UPDATE (or verify proposal belongs to the expected tenant before updating). Make the resolver obtain/verify the proposal's tenant and use it in the WHERE clause to enforce isolation.
- **server/job-queue.ts:220** — SELECT max_attempts FROM agent_jobs WHERE id = ${id} reads a tenant-scoped row by id with NO tenant_id constraint — allows reading other tenants' jobs by id.
  - _Fix:_ Require and pass tenantId into failJob (and other job-state mutators), and SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} to enforce per-tenant ownership/fencing.
- **server/job-queue.ts:237** — UPDATE agent_jobs to mark failed_terminal in failJob lacks tenant_id in WHERE clause — can mutate jobs belonging to other tenants by id.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the UPDATE and change function signatures to accept/validate tenantId (or retrieve and assert tenant ownership before updating).
- **server/job-queue.ts:257** — UPDATE agent_jobs to requeue retryable jobs in failJob has no tenant_id constraint — cross-tenant modification risk.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `AND tenant_id = ${tenantId}`) and ensure the worker provides the tenant context when calling failJob.
- **server/job-queue.ts:177** — UPDATE agent_jobs in completeJob marks a job succeeded by id but omits tenant_id in WHERE clause — allows completing jobs across tenants if an id is known.
  - _Fix:_ Change completeJob to require tenantId (or otherwise fetch and verify tenant ownership) and include `AND tenant_id = ${tenantId}` in the WHERE clause for lease-fencing correctness.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs WHERE id = ${id} with no tenant_id constraint — an operator could cancel jobs for other tenants by id.
  - _Fix:_ Require tenantId when cancelling (or verify ownership), and include `AND tenant_id = ${tenantId}` in the WHERE clause to scope cancellation to the intended tenant.
- **server/job-queue.ts:283** — retryJob updates agent_jobs WHERE id = ${id} without tenant scoping — resets terminal state across tenants by id.
  - _Fix:_ Add tenant_id to the WHERE clause or accept tenantId in retryJob to scope manual retries to the correct tenant rows.
- **server/lib/agent-trace.ts:129** — UPDATE agent_trace_spans (span close) does not constrain by tenant_id — WHERE clause uses only span_id. This allows a caller/process with a spanId to update rows across tenants.
  - _Fix:_ Include the tenant predicate on the UPDATE (e.g. AND tenant_id = ${tenantId}) or otherwise bind the closing operation to the caller's tenant. Ensure persistSpanClose receives/validates the tenantId (from currentTenantId()) and uses it in the WHERE so closes cannot cross-tenant.
- **server/lib/bwb-job-progress.ts:74** — scope(jobId, ...) can omit a tenant_id predicate when BWB_TENANT_ID is missing, but that scope() value is used in UPDATE/SELECT/DELETE WHERE clauses. Several writes (setBwbPhase, updateBwbChapters, completeBwbJob, bumpBwbHeartbeat, failBwbJob, etc.) therefore may execute against the video_jobs table without a tenant_id filter.
  - _Fix:_ Enforce tenant scoping on every write/read: require the caller to supply a tenantId or derive it from a trusted context and fail the write if missing. At minimum, include eq(videoJobs.tenantId, tid) unconditionally in scope() (and fail when tid is absent) so no DB writes occur without an explicit tenant predicate. Avoid relying solely on unpredictable jobId secrecy for isolation.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into project_files does not include tenant_id. The platform invariant requires explicit tenant_id on every INSERT into tenant-scoped tables (no RLS / FK-enforced tenant ownership). This INSERT into project_files only sets project_id and other columns, risking misattribution or cross-tenant leakage if project_files is tenant-scoped.
  - _Fix:_ Include the tenant_id in the INSERT (e.g. add tenant_id to the column list and VALUES (${tenantId}, ...)). Also ensure prior lookups on project_files / project_id include tenant scoping (WHERE tenant_id = ${tenantId}) where appropriate to defend against orphaned or cross-tenant references.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into project_notes does not include tenant_id. The module comment explicitly expects explicit tenant_id on every INSERT; omitting tenant_id violates the tenant-isolation invariant and can misattribute notes across tenants.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, created_at, tenant_id) VALUES (..., ${tenantId})). Also audit related SELECT/UPDATE queries on project_notes to ensure they include WHERE tenant_id = ${tenantId} or otherwise verify ownership of referenced project_id before writing.
- **server/lib/scheduled-post-runner.ts:62** — SELECT from tenant-scoped table `scheduled_posts` without a tenant_id constraint in the CTE (system poll/lock selects all pending rows across tenants).
  - _Fix:_ Either scope the query to a specific tenant (WHERE tenant_id = ${tenantId}) when called on behalf of a tenant, or explicitly document+gate this as an admin/multi-tenant system job. If the intent is to operate per-tenant, change the CTE to include tenant_id and/or iterate tenants explicitly so every read is constrained by tenant_id.
- **server/lib/scheduled-post-runner.ts:191** — Error-handling UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' — missing tenant_id filter. The rollback/cleanup path updates tenant-scoped rows without constraining tenant context.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} to the WHERE clause (or include tenant scoping variable) so the release/cleanup only affects the intended tenant's row.
- **server/lib/scheduled-post-runner.ts:266** — Manual construction of a Postgres array literal `arrLit` from request-derived platforms and injecting it into the SQL text. This is string-interpolated SQL using request-derived values (injection risk).
  - _Fix:_ Use parameterized arrays instead of building a literal string (e.g. pass the JS array as a parameter or use Drizzle's array binding helpers) so the DB driver performs safe parameterization. Avoid constructing SQL array literals by concatenating user input.
- **server/memory-graph.ts:81** — UPDATE on memory_categories lacks tenant scoping (no tenant_id in WHERE). This can update another tenant's category row.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or otherwise ensure refresh is executed within the proper tenant context.
- **server/memory-graph.ts:196** — SELECT against memory_links (exists check) does not constrain tenant_id. Reading memory_links without tenant scoping may reveal cross-tenant links or allow logic to act on other tenants' links.
  - _Fix:_ Constrain the query by tenant (e.g. add AND tenant_id = ${tenantId}) or join to the owning memory_entries rows and explicitly check their tenant_id = ${tenantId} in the WHERE clause.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ... WHERE id = ${mem.id} has no tenant_id constraint. This may update another tenant's memory entry.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise verify ownership) when updating memory_entries in batch loops.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (from classifyMemoryCategory.then) lacks tenant scoping. This can modify memory rows in other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}, or validate ownership before updating.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in update flow) is executed without a tenant_id filter.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or otherwise ensure the update is restricted to the caller's tenant.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ... WHERE id = ${c.id} omits tenant scoping. The earlier SELECT included tenant_id, but the UPDATE is not constrained and could flip rows in other tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) to ensure only the calling tenant's rows are modified.
- **server/minds-engine.ts:326** — markEventsHandled uses id = ANY(${`{${eventIds.join(',')}}`}::int[]) where eventIds come from the caller. This constructs SQL via string interpolation from request-derived values and is a SQL injection risk even though tenant_id is included.
  - _Fix:_ Replace the interpolated array literal with a parameterized array binding (bind eventIds as a proper integer array parameter) or use WHERE id = ANY(${eventIdsParam}) with eventIdsParam passed as a typed array to the driver.
- **server/minerva-planner.ts:373** — Probe SELECT (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}) lacks tenant scoping. Probing plan status without tenant filter may reveal or act upon another tenant's plan.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT or fetch by id+tenant_id to ensure the probe is limited to the intended tenant.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 is not scoped by tenant_id. This can surface or match child plans across tenants.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} (or the correct tenant) to the WHERE clause so only child plans for the same tenant are considered.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` omits tenant_id. The code calls assertProjectInTenant(...) to check ownership, but the actual INSERT does not include tenant_id in the column list/VALUES.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and bind the request's tenant id). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep the ownership check (assertProjectInTenant) but still write tenant_id explicitly to enforce the app-level isolation invariant.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id. Although assertProjectInTenant(...) is called to verify ownership, the INSERT does not set tenant_id in the columns/VALUES.
  - _Fix:_ Add tenant_id to the INSERT columns and bind the tenant (options.tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Verifying project ownership is good, but every INSERT must also explicitly include tenant_id per the tenant-isolation invariant.
- **server/orchestrator-ledger.ts:445** — Use of sql.raw() with a caller-supplied/derived lockKey in a SQL statement: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). sql.raw() bypasses parameter binding and can cause injection or other unexpected behavior when the embedded value is derived from caller input (ledgerKey).
  - _Fix:_ Avoid sql.raw() for request-derived values. Bind a numeric BigInt parameter instead (e.g. compute BigInt(lockKey) and use parameterized template: tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), or use a parameterized client API (pg_advisory_xact_lock($1::bigint) with the lock value as a bound parameter). Validate/normalize ledgerKey before deriving the lock value.
- **server/outcome-tracker.ts:16** — INSERT into tenant-scoped table action_outcomes uses a caller-supplied personaId but does not verify that the persona belongs to the provided tenantId. This is a foreign-id ownership check omission: a caller could reference a persona from another tenant when creating an outcome row.
  - _Fix:_ Before inserting, validate the foreign personaId is owned by (or available to) the tenant: SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} (or otherwise check ownership). Reject or sanitize inputs that reference foreign-tenant rows. Alternatively enforce that persona selection happens server-side from a tenant-scoped list.
- **server/plan-replay.ts:56** — String-interpolated vector literal (embeddingLiteral = `[... ]`) is injected into a SQL query: ORDER BY objective_embedding <=> ${embeddingLiteral}::vector. embeddingLiteral is derived from the request objective (user input). This is effectively string-interpolated SQL using request-derived data (injection + isolation risk).
  - _Fix:_ Avoid injecting constructed SQL fragments. Bind the embedding as a query parameter or use driver-supported parameterization for vector types (pass the embedding array as a parameter), or use Drizzle parameter binding to safely supply the vector. Never splice request-derived strings directly into SQL.
- **server/plan-replay.ts:152** — INSERT into plan_replay_cache uses ${embeddingLiteral}::vector and ${JSON.stringify(...)}::jsonb interpolated into the SQL. Both values are derived from request/plan data — this is string-interpolated SQL with request-derived content and risks injection or other unsafe behavior.
  - _Fix:_ Use parameterized binding for the vector and JSONB columns (pass embedding as a typed parameter and steps as a JSON parameter), or use Drizzle insert helpers that safely supply JSONB and vector parameters rather than embedding literals into the SQL string.
- **server/plan-rollout-simulator.ts:119** — kNN query injects a vector literal via ${vec}::vector (vec built from a user/request-derived embedding). This is string-interpolated SQL with request-derived content (embedding), which is an injection/isolation risk even though tenant_id is present.
  - _Fix:_ Do not interpolate vector text into SQL. Bind the embedding with parameterized queries or use the DB driver's vector binding support. Prefer Drizzle or parameter placeholders to safely pass the vector data.
- **server/plan-rollout-simulator.ts:246** — INSERT into plan_rollout_simulations uses sql`${JSON.stringify(steps)}::jsonb` and sql`${JSON.stringify(weak)}::jsonb` — serializing request-derived steps and inserting them into SQL text with ::jsonb cast via string interpolation. This is string-interpolated SQL with request-derived values.
  - _Fix:_ Pass JSONB values as bound parameters (e.g. use parameter binding for the JSON object) or use Drizzle's insert/update helpers that accept JS objects and map them safely to jsonb, avoiding manual JSON.stringify interpolation in raw SQL templates.
- **server/policy-engine.ts:320** — recordPolicyAudit() inserts into policy_audit using ${JSON.stringify(summarizeParams(input.params))}::jsonb — summarised params are request-derived and are being interpolated into the SQL string. This is string-interpolated SQL with request-derived content against a tenant-scoped table.
  - _Fix:_ Use parameterized queries or Drizzle's insert API to pass the params summary as a bound JSONB parameter. Avoid building SQL fragments via JSON.stringify and injecting them into template SQL; this prevents injection risks and ensures correct typing.
- **server/process-governor.ts:121** — Tenant-scoped table `heartbeat_logs` is queried in subselects without constraining by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). Reads against tenant-scoped audit/log tables must be scoped to the caller tenant.
  - _Fix:_ Add tenant scoping to the subqueries, e.g. AND tenant_id = ${tenantId} (or use Drizzle .where(eq(table.tenantId, tenantId))) so the COUNT only accounts for logs belonging to the tenant's tasks.
- **server/process-governor.ts:199** — Query over `heartbeat_logs` (performance/response_time check) omits tenant_id filter: SELECT AVG(duration_ms) FROM heartbeat_logs WHERE created_at > ... AND status = 'success'. This reads global logs across tenants.
  - _Fix:_ Restrict the query to the tenant, e.g. WHERE tenant_id = ${tenantId} AND created_at > ... AND status = 'success' to enforce tenant isolation.
- **server/process-governor.ts:240** — Query in cascading_failures reads `heartbeat_logs` without a tenant filter (COUNT DISTINCT persona_id FROM heartbeat_logs WHERE status != 'success' ...). This may aggregate failures across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or equivalent Drizzle .where) so only logs for the caller's tenant are counted.
- **server/process-governor.ts:585** — In executeAction('disable_task') the SELECT uses heartbeat_logs subqueries without tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). Reading tenant-scoped logs without tenant constraint risks cross-tenant leakage.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the heartbeat_logs subqueries so the success/failure counts are scoped to the tenant's tasks.
- **server/process-governor.ts:1185** — getActionHistory does a LEFT JOIN on governance_rules (LEFT JOIN governance_rules gr ON gr.id = ga.rule_id) without verifying gr.tenant_id = ga.tenant_id. This reads a tenant-scoped table (governance_rules) without an explicit tenant constraint on the joined table.
  - _Fix:_ Add the tenant constraint to the JOIN or WHERE clause (e.g. LEFT JOIN governance_rules gr ON gr.id = ga.rule_id AND gr.tenant_id = ga.tenant_id), so joined rows are restricted to the same tenant.
- **server/project-brain.ts:135** — updateProjectBrain reads projects with SELECT name,status,description FROM projects WHERE id = ${projectId} without scoping by tenant_id. The function accepts projectId from callers and does not verify tenant ownership before reading.
  - _Fix:_ Require a tenantId parameter or resolve tenant context, and add WHERE id = ${projectId} AND tenant_id = ${tenantId}. Validate that the caller's tenant owns the project before reading or writing any project-scoped data.
- **server/project-brain.ts:332** — backfillProjectBrains queries projects and conversations without tenant scoping: SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... . This can enumerate projects across tenants.
  - _Fix:_ Scope the backfill to a tenant (accept tenantId or iterate tenants explicitly) and add tenant_id = ${tenantId} to project and conversation queries (or otherwise filter by tenant) to avoid cross-tenant data exposure.
- **server/project-brain.ts:349** — Query for conversations (SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id}) lacks tenant_id filtering. Conversations are tenant-scoped and should not be read across tenants.
  - _Fix:_ Include tenant filtering on conversations, e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}, or ensure proj was already scoped to tenant and additionally verify ownership before reading.
- **server/project-brain.ts:359** — Messages are selected without tenant context: SELECT role, content FROM messages WHERE conversation_id = ${conv.id}. `messages` is tenant-scoped and this read can leak messages across tenants.
  - _Fix:_ Add tenant scoping to the messages query, e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}, and ensure the conversation belongs to the tenant before reading messages.
- **server/reference-learner.ts:291** — UPDATE to tenant-scoped table `memory_entries` omits tenant_id in WHERE clause (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This will update the row by id without verifying it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. AND tenant_id = ${input.tenantId}) or first SELECT/verify the row's tenant_id matches input.tenantId before performing the UPDATE. All writes to tenant-scoped tables must include explicit tenant_id checks.
- **server/routes.ts:165** — SELECT from tenant-scoped table 'file_storage' without any tenant_id constraint (restoreUploadsFromDb). This reads all rows across tenants and writes files to a shared UPLOADS_DIR, allowing cross-tenant data exposure.
  - _Fix:_ Scope the SELECT to a specific tenant (e.g. add WHERE file_storage.tenant_id = <tenantId>) or run the restore per-tenant under an explicit admin/maintenance context that is auditable. Alternatively, write restored files to per-tenant directories and ensure the query only returns rows for the tenant being processed. Avoid any global scans that materialize other tenants' data into a shared filesystem path.
- **server/routes.ts:764** — Startup backfill (backfillProjectDriveFolders) SELECTs from 'projects' (tenant-scoped) with no tenant filter, iterating across all projects in the DB. This performs cross-tenant reads/operations without explicit platform-admin scoping in SQL.
  - _Fix:_ Run backfill operations explicitly as a platform-admin job and/or include an explicit admin/maintenance tenant context in the query (e.g. only run when authenticated as platform admin), or split the work to iterate tenants and filter by tenant_id for each tenant. Ensure such cross-tenant schema/maintenance jobs are auditable and gated, and consider using an explicit admin WHERE tenant_id = ADMIN_TENANT_ID when appropriate.
- **server/routes/admin.ts:569** — Query against tenant-scoped table `tool_performance` without a tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance). This will return rows across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant: add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(table.tenantId, tenantId))). If the intent is an admin-global view, make that explicit and auditable (e.g. requirePlatformAdmin + clearly-documented admin scope); otherwise restrict to tenantId.
- **server/routes/admin.ts:574** — Query against tenant-scoped table `tool_performance` (failing rate check) has no tenant_id filter (SELECT ... FROM tool_performance WHERE ...).
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId}) or otherwise ensure the query is deliberately and safely acting across tenants under an explicit platform-admin guard and audit trail.
- **server/routes/admin.ts:587** — Query against `ai_insights` selects rows (WHERE status = 'new' ...) without filtering by tenant_id. `ai_insights` appears tenant-scoped so this can leak data between tenants.
  - _Fix:_ Include tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND status = 'new' ...) or otherwise explicitly restrict this admin endpoint and document why cross-tenant access is required. Prefer adding the tenant filter to enforce the isolation invariant.
- **server/routes/admin.ts:598** — Query against tenant-scoped table `heartbeat_tasks` has no tenant_id constraint (SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE ...).
  - _Fix:_ Restrict the query to the caller's tenant (AND tenant_id = ${tenantId}) or, if an intentional cross-tenant administrative scan, make that explicit and ensure the caller is the platform admin. Prefer adding tenant_id filtering to preserve tenant isolation.
- **server/routes/admin-zombie-detector.ts:38** — The `used` CTE reads `agent_trace_spans` without any tenant_id filter (SELECT tool_name, MAX(started_at) ... FROM agent_trace_spans WHERE ...). `agent_trace_spans` is tenant-scoped so this aggregates across tenants.
  - _Fix:_ Add tenant scoping inside the CTE (e.g. WHERE tenant_id = ${tenantId} AND started_at > ...) or otherwise ensure the transaction explicitly scopes to the admin tenant. Pass tenantId into the transaction and apply it to the subquery to enforce isolation.
- **server/routes/admin-zombie-detector.ts:54** — The `used` CTE for personas aggregates `conversations` without a tenant filter (SELECT persona_id, MAX(updated_at) ... FROM conversations WHERE ...). `conversations` is tenant-scoped and this will aggregate across tenants.
  - _Fix:_ Apply tenant filtering to the subquery (e.g. WHERE tenant_id = ${tenantId} AND updated_at > ...) or otherwise narrow the query to the intended tenant. Ensure tenantId is available in the transaction and used in all tenant-scoped subqueries.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table 'conversations' lacks tenant constraint. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` does not include `tenant_id = <caller tenant>`.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. `WHERE id = ${conv.id} AND tenant_id = ${tenantId}` or `AND tenant_id = ${conv.tenantId ?? tenantId}`) so the update cannot affect rows from other tenants.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table 'projects' lacks tenant constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` does not include `tenant_id = <caller tenant>`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) to prevent accidental or malicious updates to projects owned by other tenants.
- **server/routes/conversations.ts:208** — SELECT against tenant-scoped table 'conversations' is performed without an explicit tenant filter. The query `SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} ...` lacks `c.tenant_id = <caller tenant>` in the WHERE clause.
  - _Fix:_ Add an explicit tenant predicate (e.g. `AND c.tenant_id = ${tenantId}` or `AND c.tenant_id = ${conv.tenantId ?? tenantId}`) to the WHERE so the select cannot read another tenant's conversation by id.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table 'inbox_messages' lacks tenant constraint. The code `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` updates by id only and omits `AND tenant_id = ${tenantId}`.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) so a user cannot flip read state for messages belonging to other tenants.
- **server/routes/projects.ts:319** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id filter: SELECT * FROM project_files WHERE project_id = ${projectId} ... even though project ownership was checked earlier at the application layer.
  - _Fix:_ Add an explicit tenant predicate for defense-in-depth, e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC. Do not rely solely on a prior ownership check; include tenant_id in the WHERE clause.
- **server/routes/projects.ts:342** — SELECT from tenant-scoped table `project_files` (file download) lacks tenant_id constraint: SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1.
  - _Fix:_ Constrain by tenant_id as well: SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} AND file_path = ${...} LIMIT 1. This prevents accidental cross-tenant access if project/project_id checks are bypassed.
- **server/routes/projects.ts:371** — DELETE against tenant-scoped table `project_files` lacks tenant_id predicate: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}.
  - _Fix:_ Include tenant_id in the WHERE clause (DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) or perform the delete via a join that enforces project. This defends against stale/malformed FK scenarios where project_id might reference another tenant.
- **server/seed.ts:3137** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... performs an update on a tenant-scoped table without any tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise verify the project belongs to the intended tenant before performing the write. Avoid targeted updates by id alone across tenants.
- **server/seed.ts:3205** — SELECT from tenant-scoped table `conversations` without constraining tenant (no WHERE tenant_id = ...). The query: SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. add AND c.tenant_id = ${tenantId} or iterate per tenant). Ensure any cross-table EXISTS checks also enforce tenant scoping or validate ownership via an explicit tenant-aware join.
- **server/seed.ts:3213** — INSERT into `project_conversations` does not record or verify tenant context. project_conversations has no tenant_id column and the insert uses project_id/conversation_id values derived from an unscoped SELECT, enabling potential cross-tenant linking.
  - _Fix:_ Either add a tenant_id column to project_conversations and insert it explicitly, or validate both project_id and conversation_id belong to the same tenant before inserting. Never insert foreign IDs derived from unscoped queries without verifying tenant ownership.
- **server/seed.ts:4033** — Batch INSERTs into `heartbeat_tasks` (db.insert(heartbeatTasks).values([...])) are performed without setting tenant_id on the inserted rows. heartbeat_tasks is tenant-scoped (there is an idx_heartbeat_tasks_tenant_id index), so these inserts rely on implicit defaults rather than explicit tenant ownership.
  - _Fix:_ Always include tenant_id in the inserted objects (e.g. tenantId: <tenantId>) instead of relying on schema defaults. If seeding per-tenant, iterate tenants and insert rows with the correct tenant_id; if seeding global defaults, make intent explicit and document why they are global.
- **server/seed.ts:4431** — DELETE from tenant-scoped table `heartbeat_tasks` without a tenant filter: DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE ... AND type != 'maintenance_script'. This will delete rows across all tenants.
  - _Fix:_ Constrain DELETE to a specific tenant (e.g. AND tenant_id = ${tenantId}) or iterate per-tenant. If intent is global, make that explicit and ensure operator approval; otherwise always scope destructive operations by tenant.
- **server/seed.ts:4460** — UPDATE on tenant-scoped table `memory_entries` runs without a tenant constraint. The normalizer UPDATE will modify rows across all tenants: UPDATE memory_entries SET fact = REPLACE(...) WHERE ...
  - _Fix:_ Scope the UPDATE to a tenant (e.g. AND tenant_id = ${tenantId}) or perform the fix per-tenant. Avoid running multi-tenant updates without explicit tenant scoping to prevent accidental cross-tenant data modification.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` in detectStagnation can execute without a tenant filter when tenantId is omitted, resulting in cross-tenant reads.
  - _Fix:_ Fail-closed: require a tenantId parameter (throw if missing) or explicitly demand caller-provided tenant context. If legitimate multi-tenant aggregation is needed, require explicit admin permission and a separate code path. Always add eq(experiments.tenantId, tenantId) to the WHERE clause when called in tenant context.
- **server/self-improvement.ts:521** — getExperimentHistory returns an unscoped SELECT of the `experiments` table when no tenantId is supplied (db.select().from(experiments).orderBy(...)). This allows accidental cross-tenant data exposure.
  - _Fix:_ Require tenantId for this API or fail-closed to an error when tenantId is not supplied. If optional category filtering is intended, still enforce a tenant_id constraint for non-admin callers: add WHERE tenant_id = ${tenantId} or throw when tenantId is undefined.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` without constraining tenant_id. The query filters only by conversationId but does not include messages.tenant_id = tenantId.
  - _Fix:_ Add an explicit tenant filter to the query (or use the tenant-scope helper). Example: .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). Alternatively use requireTenantScope/tenantScope to build the predicate before pushing it into the WHERE clause.
- **server/skillify.ts:201** — SELECT from tenant-scoped table `messages` without constraining tenant_id. The query uses only conversationId in the WHERE clause and lacks tenant scoping.
  - _Fix:_ Constrain the query to the caller's tenant. Example: .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). If you validated the conversation row earlier, still enforce tenant scoping on child table reads per the project's invariant or document and centralize the transitive check.
- **server/skillify.ts:221** — SELECT from `personas` by persona id with NO tenant constraint. Reading a persona referenced by a conversation without verifying the persona row belongs to the same tenant risks cross-tenant data exposure.
  - _Fix:_ Ensure the persona belongs to the tenant before reading it. Add a tenant equality check (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))) or call a dedicated assert (like assertPersonaInTenant) to fail-closed on mismatches.
- **server/skillify.ts:329** — SELECT from tenant-scoped table `messages` without constraining tenant_id. The recentMsgs query filters only by conversationId and does not include tenant scoping.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))). Use the project's tenant-scope or requireTenantScope helpers to validate/construct the predicate.
- **server/storage.ts:239** — getConversationUnscoped() performs an unscoped SELECT on the tenant-scoped `conversations` table (WHERE id = …) with no tenant_id constraint.
  - _Fix:_ Avoid unscoped reads in general. If an unscoped lookup is necessary (only for trusted orchestration), restrict usage to tightly-audited internal callsites and document/force the caller to validate/derive tenantId explicitly. Prefer adding an explicit tenantId parameter (or require an admin flag) and include a WHERE eq(conversations.tenantId, tenantId). Log and audit every caller if unscoped reads remain.
- **server/storage.ts:295** — attachCitationMetadata() issues a raw SELECT FROM messages WHERE id IN (...) without any tenant_id constraint — reading tenant-scoped `messages` rows across tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or re-use the tenant context of the messages passed into the function. If ids are derived from previously-scoped rows, still add a defensive tenant filter (pass tenantId into attachCitationMetadata and apply eq(messages.tenantId, tenantId)).
- **server/storage.ts:479** — deletePersona() transaction issues updates/deletes against tenant-scoped tables (conversations, memory_entries, daily_notes) using WHERE persona_id = … but with NO tenant_id constraint — these operations can affect rows across all tenants.
  - _Fix:_ Ensure deletes/updates that touch tenant-scoped tables include tenant scoping. Either require a tenantId parameter and add eq(table.tenantId, tenantId) to each WHERE, or resolve and verify the persona's tenant ownership and include that tenantId in each update/delete within the transaction.
- **server/storage.ts:823** — getMemoriesWithoutEmbeddings() selects from tenant-scoped `memory_entries` without any tenant_id constraint — unscoped read returning data across tenants.
  - _Fix:_ Accept a tenantId parameter and add .where(and(eq(memoryEntries.status, 'active'), isNull(memoryEntries.embedding), eq(memoryEntries.tenantId, tenantId))). If this function is intentionally a cross-tenant batch job, explicitly document and restrict the job to trusted admin code and audit the results.
- **server/storage.ts:830** — getKnowledgeWithoutEmbeddings() selects from tenant-scoped `agent_knowledge` table with no tenant_id constraint — unscoped read across tenants.
  - _Fix:_ Require tenantId and add it to the WHERE (e.g. and(notExpired, isNull(agentKnowledge.embedding), eq(agentKnowledge.tenantId, tenantId))). If run as a maintenance job across all tenants, implement an explicit per-tenant iteration with audit logging.
- **server/storage.ts:878** — getMemoryStats() when called without tenantId performs a db.select(...) FROM memory_entries with no tenant constraint and also later selects agent_knowledge without tenant scoping — unscoped reads that return cross-tenant data.
  - _Fix:_ Make tenantId required for non-admin callers or branch the function into an admin-only bulk-export path. Add eq(memoryEntries.tenantId, tenantId) and eq(agentKnowledge.tenantId, tenantId) to the queries when tenantId is supplied; otherwise require explicit admin authorization for global stats.
- **server/storage.ts:899** — getRecentDailyNotes() can query `daily_notes` by date (and persona) without tenant_id when tenantId is undefined — returns notes across tenants for the same date/persona.
  - _Fix:_ Require tenantId for this function or explicitly add tenant scoping by default. If a global scan is needed, implement it as an admin maintenance routine that iterates tenants and is clearly separated from tenant-facing APIs.
- **server/storage.ts:530** — createMemoryEntry() inserts into tenant-scoped `memory_entries` but does not validate that data.tenantId is present/valid before the INSERT (no fail-closed tenant check).
  - _Fix:_ Fail-closed: require and validate a positive integer tenantId in the data (assertValidTenantId or explicit check) before inserting. Use db.insert(...).values({ ...data, tenantId }) only after validation to avoid accidental unscoped writes.
- **server/storage.ts:768** — createKnowledge() INSERTs into tenant-scoped `agent_knowledge` using the provided data but does not assert/validate that tenantId is present and valid (no fail-closed check).
  - _Fix:_ Require and validate tenantId (e.g. assertValidTenantId(data.tenantId)) before inserting. If certain callers legitimately act as admin, require an explicit admin mode and log the action. Always include tenant_id explicitly in INSERTs to enforce isolation.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding() does UPDATE felix_proposals SET args_embedding = … WHERE id = ${proposalId} AND args_embedding IS NULL with NO tenant_id constraint — unscoped write to tenant-scoped `felix_proposals`.
  - _Fix:_ Require tenantId (or derive/validate it) and include AND tenant_id = ${tenantId} in the WHERE. All writes to tenant-scoped proposal rows should include tenant scoping to prevent accidental cross-tenant modification.
- **server/surprise-scorer.ts:139** — scoreProposalSurprise() writes actual_outcome_embedding via UPDATE felix_proposals SET actual_outcome_embedding = … WHERE id = ${proposalId} with no tenant_id constraint (unscoped write).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or accept/verify tenantId in stamp/score paths. Keep reads and subsequent writes consistent and fail-closed if tenantId is missing.
- **server/surprise-scorer.ts:212** — After scoring the code UPDATEs felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} with NO tenant_id constraint — unscoped write to tenant-scoped table.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${proposalId} AND tenant_id = ${tenantId}). Ensure every path that writes back to felix_proposals uses the same tenant check that the initial read used to avoid cross-tenant contamination.
- **server/tools.ts:2973** — SELECT from project_files used to check for duplicates lacks a tenant_id constraint (WHERE tenant_id = <caller tenant>)
  - _Fix:_ Constrain the query to the caller's tenant. Add AND tenant_id = ${tenantId} (or join projects and check p.tenant_id = ${tenantId}) so the duplicate check cannot read other tenants' rows.
- **server/tools.ts:3014** — SELECT from project_conversations to check existing links does not include a tenant_id constraint
  - _Fix:_ Restrict the SELECT to the caller's tenant (e.g. WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}), or join the projects table and enforce p.tenant_id = ${tenantId} to prevent cross-tenant reads.
- **server/tools.ts:4514** — INSERT into project_notes omitted tenant_id. Writing to a tenant-scoped table must include tenant_id explicitly (columns are NOT NULL/no default) even if ownership was previously asserted.
  - _Fix:_ Include the tenant id in the INSERT columns/values (and keep the assertProjectInTenant check): INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...).
- **server/tools.ts:4518** — INSERT into project_files omitted tenant_id. Tenant-scoped rows must be inserted with tenant_id set explicitly, not inferred from project_id alone.
  - _Fix:_ Add tenant_id to the INSERT (and keep/assert project ownership): INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ...).
- **server/tools.ts:6365** — INSERT into project_files omitted tenant_id (generate_audio path). Even though assertProjectInTenant was used, tenant_id must still be passed explicitly into the tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...).
- **server/tools.ts:6899** — INSERT into project_files omitted tenant_id (create_slideshow_video path). Tenant association must be explicitly set on INSERT rather than relying on project_id ownership alone.
  - _Fix:_ Add tenant_id to the INSERT values (and keep the assertProjectInTenant guard): INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...).
- **server/tools.ts:6899** — INSERT into tenant-scoped table 'project_files' does not include tenant_id in the VALUES list. The INSERT is parameterized but omits tenant_id even though the table appears tenant-scoped and the invariant requires tenant_id be set explicitly on every INSERT.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and prefer using Drizzle .insert/.values API). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Alternatively use db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: params.project_id, ... }) so the tenant is persisted and cannot be omitted.
- **server/tools.ts:8859** — UPDATE against tenant-scoped table 'proposed_skills' uses WHERE eq(proposedSkills.id, row.id) without constraining by tenant. This lets a caller modify rows across tenant boundaries (no tenant_id filter present in the WHERE clause).
  - _Fix:_ Add a tenant constraint to the WHERE clause. Use the tenant id that was used for the insert (tid) and the Drizzle eq helper, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)). If using raw SQL, add AND tenant_id = <tenantId>. Always enforce tenant context on UPDATE/DELETE for tenant-scoped tables.
- **server/tools.ts:8884** — UPDATE against tenant-scoped table 'proposed_skills' uses WHERE eq(proposedSkills.id, row.id) without constraining by tenant. This update path (marking a proposal accepted/promoted) does not verify tenant ownership in the SQL WHERE clause.
  - _Fix:_ Restrict the UPDATE to the caller's tenant: include eq(proposedSkills.tenantId, tid) in the WHERE condition (or tenant_id = ${tid} in SQL). Prefer using Drizzle's typed .where(eq(...), eq(...)) to avoid accidental cross-tenant writes.
- **server/tools.ts:8900** — UPDATE against tenant-scoped table 'proposed_skills' (marking rejected) filters only by id and omits tenant constraint. Without tenant_id in the WHERE clause a tenant could affect another tenant's proposed skill row.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g., .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or include AND tenant_id = ${tid} in raw SQL). Ensure every UPDATE/DELETE/SELECT on tenant-scoped tables includes the tenant_id constraint to enforce isolation.
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} lacks a tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well, e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId} (or use Drizzle .where(eq(projects.tenantId, tenantId))). Even when projId was derived from a prior tenant-scoped query, every write should include an explicit tenant filter.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table 'knowledge_triples' is performed without constraining by tenant_id (WHERE id = ${row.id} only). This violates the invariant that every write to tenant-scoped tables must include the caller's tenant constraint and risks affecting rows outside the caller's tenant if ids are misused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) so the mutation is guaranteed to affect only rows owned by the calling tenant. Prefer parameterized queries and, where multiple statements rely on earlier selects, perform the select+update in a transaction and/or include tenant_id in every write clause for defense-in-depth.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys without tenant filter — reads from a likely tenant-scoped table (provider_keys) using no WHERE tenant_id constraint (admin-only path but still returns keys across tenants).
  - _Fix:_ If provider_keys is tenant-scoped, add explicit filtering (e.g. WHERE tenant_id = ${tenantId}) or otherwise restrict scope. If the table is intentionally global, document that clearly and rename/mark it as a global table; avoid returning sensitive key material across tenants. Prefer parameterized queries that include tenantId for non-global tables.
- **server/tools/domains/security/handlers.ts:260** — COUNT(*) on provider_keys without tenant filter — admin path executes SELECT COUNT(DISTINCT provider) FROM provider_keys without constraining by tenant_id, exposing aggregated cross-tenant provider key presence.
  - _Fix:_ If provider_keys is tenant-scoped, include WHERE tenant_id = ${tenantId} (or iterate per-tenant explicitly in admin flows). If admin needs cross-tenant visibility, ensure this behavior is an explicit, reviewed admin-only capability and that provider_keys is annotated/documented as global; otherwise restrict the query to the caller's tenant.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without filtering by tenant_id (reads pairing rows across all tenants).
  - _Fix:_ Avoid querying tenant-scoped tables across tenants. Add a WHERE tenant_id = ${tenantId} filter or redesign to safely canonicalize ownership (e.g. have an explicit phone-owner column / one-canonical-row per phone) and only surface rows owned by the resolved tenant. If cross-tenant detection is required, perform it with an explicit, audited operator-only path and very careful validations rather than a blind unscoped SELECT.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped ledger row by id only, without constraining tenant_id — allows modifying another tenant's ledger row if reservationId is mis-specified.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or validate the row's tenant before updating). Never update tenant-scoped rows by id alone without verifying tenant ownership.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseVentureReservation) updates ledger rows without tenant_id constraint, permitting cross-tenant modification.
  - _Fix:_ Require tenantId in releaseVentureReservation and add AND tenant_id = ${tenantId} to the WHERE clause (or verify the reservation belongs to the tenant before releasing). Ensure all ledger writes are tenant-scoped.
- **server/video-job-runner.ts:178** — mirrorStateToDb upsert (db.insert(...).onConflictDoUpdate) updates videoJobs rows on conflict without constraining by tenant_id in the UPDATE condition — allows overwriting another tenant's job row keyed by jobId.
  - _Fix:_ Include tenant ownership in the conflict/update guard (e.g. include AND videoJobs.tenantId = ${row.tenantId} in the onConflictDoUpdate `where` clause or explicitly check tenantId before performing the update). Ensure upserts cannot modify rows belonging to a different tenant.
- **server/wiring-invariants.ts:57** — SELECT from research_schedules (joined to research_programs) is executed with no tenant_id filter — reads tenant-scoped rows across all tenants.
  - _Fix:_ Constrain the query to an explicit tenant (e.g. WHERE s.tenant_id = :tenantId or WHERE s.tenant_id = ADMIN_TENANT_ID for admin-only checks). Prefer Drizzle's .where(eq(table.tenantId, tenantId)) and pass tenantId into the function so tenant-scoped reads never span tenants unintentionally.
- **server/wiring-invariants.ts:108** — SELECT name FROM research_programs executes without a tenant_id constraint — may read tenant-scoped program rows across tenants.
  - _Fix:_ Add an explicit tenant filter (WHERE tenant_id = :tenantId) or, if research_programs is intentionally global, document that explicitly. If this is an admin-only aggregate, use ADMIN_TENANT_ID in the WHERE clause or an audited admin path.
- **server/wiring-invariants.ts:111** — SELECT name FROM personas runs with no tenant_id filter — tenant-scoped persona rows may be read across tenants.
  - _Fix:_ Add an explicit tenant filter (WHERE tenant_id = :tenantId) or otherwise ensure this is intentionally a global/shared table. Use Drizzle's .where(eq(personas.tenantId, tenantId)).
- **server/wiring-invariants.ts:142** — SELECT name FROM research_programs (in checkProposalTargets) runs without tenant_id constraint — cross-tenant reads possible.
  - _Fix:_ Constrain to a tenant via WHERE tenant_id = :tenantId or use ADMIN_TENANT_ID deliberately and explicitly for admin-only checks. Prefer Drizzle query helpers to avoid ad-hoc SQL.
- **server/wiring-invariants.ts:180** — Complex SELECT joining research_programs, research_experiments, code_proposals and research_sessions is executed without any tenant_id filtering — this aggregates/reads tenant-scoped rows across all tenants.
  - _Fix:_ Add tenant_id filters to the main FROM table and to subqueries that reference tenant-scoped tables (e.g. WHERE p.tenant_id = :tenantId AND rs.tenant_id = :tenantId). If this check must run as admin across tenants, make that explicit and protect with an admin-only path and/or aggregate-only tables.
- **server/wiring-invariants.ts:244** — SELECT count(*) FROM tool_performance WHERE last_success_at > ... is executed with no tenant_id filter — reads tool_performance across all tenants.
  - _Fix:_ Add WHERE tenant_id = :tenantId (or ADMIN_TENANT_ID) to limit to a single tenant, or implement an explicit admin-mode aggregate that is audited. Use parameterized Drizzle queries rather than unscoped SELECTs.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tool_performance (everCount) is executed without tenant_id filter — will count rows across all tenants.
  - _Fix:_ Constrain the count with WHERE tenant_id = :tenantId or run this as a documented admin/global aggregate. Prefer Drizzle's APIs and pass tenantId explicitly.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM messages WHERE created_at > ... runs with no tenant_id constraint — counts messages across all tenants.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = :tenantId). If this metric is intended to be per-tenant, require the tenantId param; if cross-tenant, make the admin intent explicit and protect it.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... is executed without tenant_id filtering — will aggregate usage across tenants.
  - _Fix:_ Include WHERE tenant_id = :tenantId (or use ADMIN_TENANT_ID with admin-only checks). Use Drizzle query builders to express the filter and avoid unscoped reads of tenant data.
- **server/wiring-invariants.ts:366** — sql.raw executes SELECT COUNT(*) and MAX(timestamp) FROM ${spec.table} with no tenant_id filter — reading subsystem tables across tenants.
  - _Fix:_ If those subsystem tables are tenant-scoped, add tenant filters (e.g. WHERE tenant_id = :tenantId). Avoid building SQL with raw table interpolations when tenant filters are required; instead use Drizzle table objects and .where(eq(table.tenantId, tenantId)). Validate that spec.table is a non-user-controlled constant before using sql.raw.
- **server/wiring-invariants.ts:417** — SELECT aggregating tool_performance (SUM success/fail, MAX last_failure_at) has no tenant_id filter — will compute drift metrics across tenants.
  - _Fix:_ Add WHERE tenant_id = :tenantId (and to any grouped aggregations) or restrict the check to an explicit admin tenant. Use parameterized queries/Drizzle to ensure tenant scoping is enforced everywhere.
- **server/wiring-invariants.ts:535** — sql.raw runs SELECT COUNT(*) and pg_total_relation_size(...) FROM memory tables (tbl.name) without tenant_id filter — counts/size measured across all tenants.
  - _Fix:_ If memory tables are tenant-scoped, add WHERE tenant_id = :tenantId to the SELECT. Avoid using string-interpolated sql.raw for queries that must include tenant filters; prefer Drizzle table access and explicit .where(eq(table.tenantId, tenantId)).
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory is executed without tenant_id filtering — reads graph_memory rows across all tenants (note: comment even mentions 'across all tenants').
  - _Fix:_ If the intent is a per-tenant check, add WHERE tenant_id = :tenantId. If the empty-check truly must be global, document and protect it via an admin-only audited endpoint; otherwise constrain by tenant and use Drizzle helpers.

### MEDIUM (44)

- **server/agentic-engines.ts:347** — SELECT from `heartbeat_logs` is executed without tenant scoping (`FROM heartbeat_logs WHERE task_name != 'Self-Reflection'`), causing cross-tenant reads of heartbeat logs.
  - _Fix:_ If heartbeat_logs are tenant-scoped, include tenant_id = ${tenantId} in the WHERE or perform per-tenant aggregation. If the intent is to be global, make that explicit and ensure it is safe and documented; otherwise restrict queries per-tenant to maintain isolation.
- **server/agentic-engines.ts:451** — SELECT from `heartbeat_logs` in runPredictiveEngine (`WHERE status = 'success' ORDER BY created_at DESC LIMIT 10`) lacks a tenant filter, returning logs from all tenants.
  - _Fix:_ Scope this query to the current tenant (WHERE tenant_id = ${tenantId} AND status = 'success' ...) or otherwise ensure the results are filtered per-tenant. Avoid global reads of tenant-scoped operational logs unless explicitly intended and controlled.
- **server/agentic/task-forces.ts:40** — String-interpolated raw SQL used to inject a request-derived personaIds array literal (${literal} used inside sql`${literal}::int[]`). Constructing/embedding SQL fragments from request input risks injection/isolation issues.
  - _Fix:_ Avoid interpolating a raw SQL fragment. Use parameterized array binding or Drizzle's insert/.values API with a properly-typed array parameter (e.g. pass personaIds as an array value) so values are passed as query parameters rather than raw SQL text. Validate/normalize input and ensure tenant scoping is always explicit.
- **server/auto-asset-capture.ts:207** — SELECT content FROM messages WHERE conversation_id = ${c.id} has no tenant_id constraint. Reading messages without verifying tenant ownership relies on the assumption that conversation_id is globally unique and correct, which violates the codebase invariant that tenant isolation be enforced by SQL filters.
  - _Fix:_ Add an explicit tenant_id predicate (AND tenant_id = ${c.tenant_id}) to the messages SELECT, or otherwise verify ownership before reading messages.
- **server/auto-project.ts:112** — SELECT role, LEFT(content, 300) FROM messages WHERE conversation_id = ${conversationId} has no tenant_id constraint. The route earlier verified the conversation belongs to tenantId, but the messages SELECT does not include tenant_id in SQL.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages SELECT so the DB enforces the same tenant constraint (defense-in-depth). Alternatively, derive messages by joining through the conversations table with its tenant_id predicate to ensure the DB enforces tenant isolation.
- **server/credential-vault.ts:169** — UPDATE on credential_vault updates last_used_at by id only: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — missing tenant_id constraint.
  - _Fix:_ Always scope mutations to the tenant. Add tenant filtering: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(table.tenantId, tenantId))).
- **server/credential-vault.ts:176** — UPDATE on credential_vault updates last_used_at by id only: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — missing tenant_id constraint.
  - _Fix:_ Scope this update to the tenant that owns the credential: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(table.tenantId, tenantId))).
- **server/data-protection.ts:406** — getDeletedConversations includes a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = conversations.id) that lacks tenant_id filtering even though the outer query is tenant-scoped. The inner SELECT can read messages across tenants if conversation ids are not globally unique per-tenant.
  - _Fix:_ Make the subquery tenant-aware (e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = conversations.id AND tenant_id = ${tenantId}) or reference the outer tenant column explicitly to ensure the messages count is scoped to the caller's tenant.
- **server/deep-interview.ts:337** — saveInterviewToKnowledge inserts into project_notes without supplying tenant_id. If project_notes is a tenant-scoped table with a NOT NULL tenant_id, this INSERT would create a row lacking explicit tenant ownership or may rely on implicit FK assumptions.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (project_id, tenant_id, content, created_at) VALUES (...) or the SELECT form) to ensure the persisted note is associated with the correct tenant. If project_notes is intentionally keyed only by project_id, document and enforce that invariant and still prefer explicit tenant scoping where possible.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` is performed without any tenant_id constraint; heartbeat_tasks elsewhere are treated as tenant-scoped.
  - _Fix:_ If scanning heartbeat tasks must be tenant-scoped, add WHERE tenant_id = ${tenantId} or iterate per-tenant. If intentional global scan is required, make the admin intent explicit (require/verify admin privileges) and document why cross-tenant access is necessary.
- **server/job-queue.ts:385** — getJobStats SELECTs counts from agent_jobs over the last 7 days with no tenant_id filter — returns cross-tenant aggregated stats.
  - _Fix:_ If this is intended for per-tenant dashboards, require a tenantId parameter and filter by `tenant_id = ${tenantId}`. If it is an operator/global metric, make the function name and authorization explicit and ensure callers are operator-only.
- **server/knowledge-diversity-monitor.ts:266** — Query aggregates `SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id` runs across all tenants (no caller-scoped tenant filter).
  - _Fix:_ If this is an operator-level/process job, ensure it's explicitly restricted to operator context. Otherwise, to respect per-tenant isolation, iterate tenants the caller is authorized for and query agent_knowledge with `WHERE tenant_id = ${tenantId}` for each tenant rather than scanning all tenants in a single unconstrained query.
- **server/lib/deliverable-exemplars.ts:81** — DELETE FROM deliverable_exemplars uses WHERE id IN (SELECT id FROM deliverable_exemplars WHERE tenant_id = ${tenantId} ...), but the outer DELETE does not include an explicit tenant_id = ${tenantId} predicate. The outer statement could affect rows if IDs are reused or in edge cases, and it violates the rule that each write must include the tenant predicate in its WHERE clause.
  - _Fix:_ Add an explicit tenant_id = ${tenantId} predicate to the outer DELETE (e.g. DELETE FROM deliverable_exemplars WHERE tenant_id = ${tenantId} AND id IN (...)) so the DELETE's WHERE clause itself enforces tenant scoping, not just the subquery.
- **server/lib/commerce-catalog.ts:91** — registerProduct inserts caller-supplied foreign IDs (missionId, postPurchaseSequenceId) into commerce_products without verifying that those referenced rows belong to the same tenant. Since there is no RLS/foreign-key cascade enforcing ownership, this can allow cross-tenant references.
  - _Fix:_ Before persisting missionId or postPurchaseSequenceId, validate ownership by querying the referenced rows with a WHERE tenant_id = ${input.tenantId} (or otherwise confirming they belong to the tenant). If the referenced row does not exist or belongs to another tenant, reject the request.
- **server/lib/outcome-promotion.ts:158** — SELECT against tenant-scoped table `deliverable_grade_events` is executed without a tenant_id WHERE clause (global scan). This reads rows across all tenants in one query, violating the requirement that tenant-scoped reads be limited to a caller's tenant.
  - _Fix:_ Avoid scanning tenant-scoped tables across all tenants in a single unrestricted query. Instead iterate per-tenant (or otherwise enumerate tenants under an explicit, authorized admin context) and run the aggregation/query with a WHERE tenant_id = <tenant> parameter for each tenant. Alternatively introduce an explicit, audited admin-only path (with strict auth checks) or a separate global summary table that is safe to read without tenant_id. Ensure the code documents and enforces the admin-only nature if a cross-tenant scan is truly necessary.
- **server/lib/mcp-api-keys.ts:166** — Lookup on tenant-scoped table `mcp_api_keys` is performed by key_prefix only (SELECT ... FROM mcp_api_keys WHERE key_prefix = ${prefix} LIMIT 1) with no tenant_id filter. This reads rows across tenants to resolve the tenant, which is a deviation from the general tenant-isolation-by-filter invariant.
  - _Fix:_ If tenant resolution from the presented key is required, make the intent explicit and harden the lookup: ensure key_prefix is globally unique (enforced by a UNIQUE index), limit returned columns to the minimum needed (tenant_id and key_hash), and keep the subsequent constant-time hash comparison. Alternatively store a separate, minimal prefix->tenant mapping table (with prefix unique) so the tenant resolution can be done safely and explicitly. If this path must remain cross-tenant, document and gate it behind a narrowly scoped/audited auth boundary and add rate-limiting/monitoring to reduce abuse risk.
- **server/lib/stuck-task-sweep.ts:59** — SELECT from tenant-scoped table `agent_runs` without a tenant_id constraint; the audit sweep reads all tenants' running agent_runs.
  - _Fix:_ If this is intended as an operator/admin multi-tenant audit, ensure the code runs under an explicit admin context and is reviewed/authorized. Otherwise, scope the SELECT by tenant_id (WHERE tenant_id = ${tenantId}) or run per-tenant to enforce tenant isolation.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without a tenant_id constraint; the sweep may expose rows across tenants (table may be present in some environments).
  - _Fix:_ Either restrict the query to a tenant (WHERE tenant_id = ${tenantId}) or ensure this audit runs only with an explicit admin privilege and audit trail. Prefer per-tenant scans to maintain the tenant-isolation invariant.
- **server/lib/system-probes.ts:163** — COUNT(*) on tenant-scoped `messages` table issued without a tenant_id filter (global totalMessages probe).
  - _Fix:_ If the probe must report per-tenant totals, add a WHERE tenant_id = ${tenantId} filter or accept an explicit tenant context. If the global count is intended for admin telemetry, ensure the probe is restricted to an admin-only path and documented as a cross-tenant operation.
- **server/minds-engine.ts:214** — INSERT into mind_tickets builds a Postgres array string via string interpolation for depends_on: `{${params.dependsOn.join(',')}}`. That is string-interpolated user-derived SQL fragment construction (injection/isolation risk).
  - _Fix:_ Use parameterized array binding instead of composing a string (e.g. pass the array as a bound parameter or use Postgres array parameters / Drizzle parameterization) to avoid injection and ensure proper typing.
- **server/minds-engine.ts:260** — SELECT ... WHERE id = ANY(${`{${ticket.dependsOn.join(',')}}`}::int[]) uses string interpolation to construct an array literal. Although ticket.dependsOn originated from DB, the original insertion used stringified input — this pattern is brittle and risks injection or mis-scoping.
  - _Fix:_ Use proper parameterized array binding (pass an actual number[] to the driver) instead of building a '{1,2}' string. For example, use id = ANY(${ticket.dependsOn}::int[]) with ticket.dependsOn bound as an array parameter, or the driver-specific array binding API.
- **server/minerva-planner.ts:506** — getPlan supports a code path that SELECTs FROM plans WHERE id = ${planId} with no tenant filter when tenantId is omitted. While comments say this is for trusted internal use, it is an unscoped read on a tenant-scoped table.
  - _Fix:_ Avoid unscoped reads in code paths exposed to any caller. Require an explicit tenantId for all external callers, or gate the unscoped path behind a clearly auditable admin-only code path and document/validate its usage.
- **server/personality-files.ts:8** — personality_files schema declares tenant_id INTEGER NOT NULL DEFAULT 1. A default tenant_id allows INSERTs that omit an explicit tenant, enabling accidental implicit assignment to tenant 1 (violates invariant that tenant_id must be explicitly passed).
  - _Fix:_ Remove the DEFAULT and require callers to always supply tenant_id explicitly (tenant_id INTEGER NOT NULL with no default). If a system-admin tenant is required for some rows, make insertion explicit in code paths and document the special-case, rather than relying on a DB DEFAULT.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id constraint (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This returns rows across all tenants (information exposure risk).
  - _Fix:_ If this is intended only for a privileged system runner, make that explicit and document/limit columns returned; otherwise add a tenant filter (e.g. WHERE tenant_id = ${tenantId}). If the runner must scan all tenants, ensure subsequent writes always re-check tenant ownership and minimize returned columns (avoid leaking sensitive content).
- **server/routes.ts:1529** — Public endpoint (/api/public/stats) issues COUNT(*) queries against tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filter, exposing cross-tenant aggregated metrics.
  - _Fix:_ Either restrict these queries to a tenant context (WHERE tenant_id = <tenantId>) or explicitly gate the endpoint to admin/operator access if platform-wide totals are intended. If global stats are required, document and centralize them as platform-shared tables (not tenant-scoped) so the invariant is preserved.
- **server/routes.ts:1557** — Public endpoint (/api/public/architecture) executes multiple COUNT/SELECT queries against tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) without tenant_id constraints, leaking global counts and potentially tenant-specific aggregates.
  - _Fix:_ Limit these queries to a tenant context when called by a tenant, or make the endpoint admin-only for platform-level reporting. If the intent is to serve platform-wide metrics, persist them to dedicated global tables at write-time rather than scanning tenant-scoped tables ad-hoc.
- **server/routes.ts:2056** — Presenter GET (/api/presenter/:token) selects presenter_sessions by token without additionally constraining by tenant_id. While the token is used as the capability, the DB read is not tenant-scoped and could return rows from any tenant if tokens are predictable or collisions occur.
  - _Fix:_ Bind the presenter session lookup to a tenant context or include tenant_id in the WHERE clause where possible. At minimum, validate that the returned session's tenant_id is consistent with any caller context or with the expected token-binding semantics; consider including tenant_id in the token payload or storing tokens with a cryptographic binding to tenantId and verifying it prior to returning session data.
- **server/routes.ts:2312** — Public order lookup (/api/store/order/:sessionId) selects from delivery_logs (tenant-scoped) using orderId/stripePaymentId without a tenant_id constraint. This returns rows across tenants based solely on an unscoped identifier.
  - _Fix:_ Either restrict the query by tenant_id when the caller is authenticated with a tenant context, or ensure the capability identifier (sessionId) is cryptographically bound to a tenant and verify ownership before returning data. If this endpoint intentionally returns global capability-protected rows, document that and ensure the sessionId space is sufficiently unguessable and that results are strictly limited to fields safe for public exposure.
- **server/routes.ts:2420** — Async order-recovery worker selects from delivery_logs by lower(customer_email) with no tenant filter (setImmediate block in /api/store/lookup-orders). This reads delivery rows across tenants for the supplied email and then emails the customer, performing cross-tenant reads.
  - _Fix:_ If the recovery flow is intended to email matches across tenants, make that explicit and ensure strict auditing and consent. Otherwise restrict the SELECT to a tenant context or require additional validation that the caller is entitled to query that tenant's delivery logs. Consider storing delivery logs in a way that avoids cross-tenant scans by email, or require a tenant parameter plus ownership verification.
- **server/routes/projects.ts:35** — Subquery reads tenant-scoped table `project_files` without tenant_id constraint: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) as file_count. The subquery does not explicitly filter by tenant_id.
  - _Fix:_ Add tenant scoping to the subquery, e.g. (SELECT COUNT(*) FROM project_files pf WHERE pf.project_id = p.id AND pf.tenant_id = ${tenantId}) as file_count, or rewrite to join project_files with projects ensuring tenant filtering is explicit.
- **server/routes/oauth-subscriptions.ts:325** — Global SELECT from tenant-scoped table `oauth_subscriptions` without tenant predicate: SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE. This reads rows across all tenants without an explicit tenant_id filter.
  - _Fix:_ If the intent is to perform a platform-wide operation, ensure the handler is strongly authenticated/authorized (it is using CRON_SECRET) and document that this is an admin/global operation. Otherwise, if the operation should be tenant-scoped, add WHERE tenant_id = ${tenantId}. For clarity and defense-in-depth, consider logging/alerting and wrapping global cross-tenant queries in a distinct admin-only code path with explicit checks.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id on a tenant-scoped table.
  - _Fix:_ Restrict the count to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise ensure this aggregation is intentionally global and performed under an admin/owner context. Avoid reading across tenants.
- **server/seed.ts:2855** — SELECT id, name, model FROM research_programs WHERE is_active = true reads a tenant-scoped table without a tenant_id filter.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or explicitly run this only in an admin/owner context. Never scan tenant data across tenants in normal seeding/maintenance logic.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... queries the conversations table without a tenant_id filter — conversations are tenant-scoped elsewhere in the code.
  - _Fix:_ Add a tenant filter (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or validate the tenant context before running this query. Do not enumerate tenant-scoped rows across all tenants.
- **server/seed.ts:4656** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} with no tenant constraint. Although the id was obtained from a prior tenant-scoped SELECT, the UPDATE itself is not tenant-constrained and could modify the same id if ids are reused across tenants or in case of race conditions.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the UPDATE using the original tenant-scoped WHERE clause to ensure the write is always limited to the intended tenant.
- **server/storage.ts:934** — getHeartbeatTask(id) selects from tenant-scoped `heartbeat_tasks` table by id only (WHERE id = …) with no tenant_id constraint.
  - _Fix:_ If this function is used in contexts where tenant isolation is required, require a tenantId parameter and add eq(heartbeatTasks.tenantId, tenantId) to the WHERE. If the function must remain unscoped for internal scheduler purposes, clearly mark it and isolate its callers to trusted system code only.
- **server/storage.ts:916** — getHeartbeatTasks() returns heartbeat_tasks unscoped when tenantId is not provided (returns all rows from heartbeat_tasks) — tenant-scoped table read without tenant filter.
  - _Fix:_ If the function is used by tenant-facing code require tenantId; if used by the scheduler intentionally across tenants, ensure the caller is a trusted system worker and add strong audit/logging and explicit naming (e.g. getAllDueHeartbeatTasksForScheduler). Prefer to make the tenant-parameter explicit.
- **server/storage.ts:991** — getDueHeartbeatTasks() runs a raw SELECT on `heartbeat_tasks` (enabled & next_run_at <= NOW()) with no tenant_id constraint — unscoped read across tenants.
  - _Fix:_ If this is a global scheduler that must read tasks for all tenants, isolate it as a trusted system job. Otherwise, add tenantId filtering or break the job into per-tenant claims to enforce tenant isolation in application logic.
- **server/storage.ts:1019** — claimHeartbeatTasks() uses raw UPDATE heartbeat_tasks SET ... WHERE id = ${id} ... without a tenant_id check — unscoped UPDATE/claim of tasks across tenant rows.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or ensure the claim path derives and verifies tenantId prior to the update. If this is a centralized scheduler, make the operation explicit and limited to trusted scheduler code only, with audit logs.
- **server/surprise-scorer.ts:181** — When there is insufficient history the code UPDATEs felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}). Be consistent: every mutation to a tenant-scoped row should include tenant_id to defend against accidental cross-tenant mutation.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' uses WHERE id = ${workflow.id} with no tenant_id constraint.
  - _Fix:_ Include the tenant scope on the UPDATE (even if the id was previously selected under tenant scope). Add AND tenant_id = ${tenantId} (or use a Drizzle .where(eq(table.tenantId, tenantId))) to the UPDATE to ensure the row belongs to the caller's tenant.
- **server/video-job-runner.ts:204** — SELECT videoJobs.cancelRequested WHERE jobId = ${jobId} (isCancelRequested) reads a tenant-scoped table by jobId only, without tenant_id constraint.
  - _Fix:_ Bind reads to the tenant when possible: accept tenantId as a parameter and add eq(videoJobs.tenantId, tenantId) to the WHERE clause (or otherwise validate that the jobId belongs to the tenant before relying on the cancel flag). This prevents leaking or acting on state for jobs owned by other tenants.
- **server/webhook-triggers.ts:60** — Query selects from webhook_triggers and LEFT JOINs personas (SELECT w.*, p.name as persona_name FROM webhook_triggers w LEFT JOIN personas p ON p.id = w.persona_id ...) but does not constrain the joined personas table by tenant_id. Personas are tenant-scoped; this can return persona rows from other tenants and leak cross-tenant data.
  - _Fix:_ Restrict the join/read against personas to the intended tenant (for example add AND p.tenant_id = <expected tenant id> to the JOIN or WHERE clause). If the trigger->persona relationship is intended to reference admin-owned personas, explicitly join/filter on the admin tenant (ADMIN_TENANT_ID) or resolve the persona via storage.getPersona(...) with an explicit tenant context instead of an unscoped SQL JOIN.
- **server/webhook-triggers.ts:122** — processTriggerEvent performs a SELECT with a LEFT JOIN to personas (SELECT w.*, p.name as persona_name FROM webhook_triggers w LEFT JOIN personas p ON p.id = w.persona_id WHERE ...) without constraining personas by tenant_id. This can surface persona data from other tenants.
  - _Fix:_ As above, ensure any read of the personas table is scoped to the expected tenant. Add a tenant filter (e.g. JOIN ... ON p.id = w.persona_id AND p.tenant_id = <expected tenant>) or fetch persona metadata through the application layer (storage.getPersona with an explicit tenant) rather than an unscoped SQL join.

### Suppressed — confirmed false positives (185)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/runs.ts:36** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_runs` performed without constraining by tenant_id (where(eq(agentRuns.id, runId)) with NO tenant check). This allows modifying rows across tenants.
  - _Why false positive:_ agent_runs row is updated by globally unique server-generated runId in internal worker helpers, not by tenant-controlled request input.
- **server/agentic/runs.ts:51** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_runs` (updateRunState) omits tenant_id in the WHERE clause (where(eq(agentRuns.id, runId))).
  - _Why false positive:_ Internal run-state mutation keyed by server-generated agent_runs primary key; no evidence of untrusted cross-tenant boundary here.
- **server/agentic/runs.ts:55** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_runs` (replaceRunState) omits tenant_id in the WHERE clause.
  - _Why false positive:_ Internal helper replaces state by globally unique runId for agent worker lifecycle, matching benign internal-worker-pk pattern.
- **server/agentic/runs.ts:64** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_runs` (completeRun) omits tenant_id in the WHERE clause, allowing cross-tenant mutation.
  - _Why false positive:_ Completion update is an internal lifecycle transition on server-created runId, not a tenant-requested arbitrary row selector.
- **server/agentic/runs.ts:94** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_runs` (failRun) omits tenant_id in the WHERE clause.
  - _Why false positive:_ Failure transition updates agent_runs by internal server-generated primary key in worker flow; fits internal-worker-pk.
- **server/agentic/runs.ts:102** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_runs` (pauseRun) omits tenant_id in the WHERE clause.
  - _Why false positive:_ Pause transition is another internal run lifecycle update by globally unique runId, with no shown untrusted source.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE/SELECT on tenant-scoped table agent_wake_schedules lacks a tenant_id constraint. The UPDATE claims pending rows across all tenants (inner SELECT has no WHERE tenant_id), so one process can claim/firing schedules from multiple tenants.
  - _Why false positive:_ Platform-wide scheduler intentionally claims due rows across tenants and processes each using its own tenant_id; no mixed tenant output surface shown.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` (setting project_id) lacks a tenant_id constraint in the WHERE clause. This can modify rows belonging to other tenants.
  - _Why false positive:_ Rows come from join enforcing p.tenant_id = c.tenant_id and carry c.tenant_id; update targets that prevalidated conversation id in same call path.
- **server/chat-engine.ts:766** _(upstream-verified)_ — LATERAL subquery reads from tenant-scoped table `messages` without tenant constraint — query: SELECT MAX(created_at) AS last_msg_at FROM messages WHERE conversation_id = c.id
  - _Why false positive:_ LATERAL subquery is correlated to conversations row c, and outer WHERE explicitly scopes c.tenant_id = tid after validTenant guard; subquery cannot range beyond already-tenant-scoped parent conversation set.
- **server/chat-engine.ts:767** _(upstream-verified)_ — LATERAL subquery selects user message from tenant-scoped `messages` without tenant constraint — query: (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ORDER BY created_at DESC LIMIT 1) AS last_user_msg
  - _Why false positive:_ Nested messages lookup is by conversation_id = c.id where outer conversations are constrained to tenant_id = tid under validTenant guard.
- **server/chat-engine.ts:768** _(upstream-verified)_ — LATERAL subquery selects assistant message from tenant-scoped `messages` without tenant constraint — query: (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'assistant' ORDER BY created_at DESC LIMIT 1) AS last_assistant_msg
  - _Why false positive:_ Assistant-message subquery is correlated to tenant-scoped conversation c; same-call-path guard enforces validTenant and outer c.tenant_id = tid.
- **server/chat-engine.ts:772** _(upstream-verified)_ — LATERAL subquery reads from tenant-scoped table `compaction_archives` without tenant constraint — query: SELECT summary FROM compaction_archives WHERE conversation_id = c.id ORDER BY archived_at DESC LIMIT 1
  - _Why false positive:_ Compaction archive lookup is correlated to conversation_id = c.id from outer conversations already filtered by c.tenant_id = tid.
- **server/data-protection.ts:117** _(system-sweep)_ — SELECT queries in permanentlyPurgeSoftDeleted read conversations across ALL tenants (no tenant_id predicate) and subsequent DELETEs remove messages/archives/conversations without scoping to a tenant.
  - _Why false positive:_ This is a platform-wide expiry purge over all tenants; rows carry tenant_id per conversation and the job performs housekeeping deletion/backup, not tenant-facing mixed output.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE FROM messages / DELETE FROM compaction_archives / DELETE FROM conversations in permanentlyPurgeSoftDeleted do not include tenant_id predicates — destructive writes may delete other tenants' data.
  - _Why false positive:_ Deletes occur inside cross-tenant soft-delete purge loop over expired conversations, a housekeeping sweep that does not return mixed tenant data.
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings() has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ Backfill scans expired/missing embeddings across tenants for maintenance and only computes/stores vectors, with no tenant-facing mixed content output.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT FROM memory_entries in backfillEmbeddingVecs() lacks a tenant_id WHERE clause — reads rows across all tenants.
  - _Why false positive:_ Memory embedding backfill is a platform-wide maintenance scan/update of housekeeping fields only, not a tenant-facing read surface.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id constraint. This enables cross-tenant writes by id.
  - _Why false positive:_ Update is part of maintenance backfill over rows selected by the sweep; it mutates only embedding_vec housekeeping data and does not mix tenant outputs.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillEmbeddingVecs() has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ Knowledge embedding backfill is a cross-tenant maintenance scan that only repairs embedding vectors and emits aggregate counts/logs.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec ... WHERE id = ${row.id} — missing tenant_id constraint, permitting cross-tenant modification.
  - _Why false positive:_ Boot/maintenance backfill scans rows platform-wide and only sets embedding_vec on each selected row; housekeeping mutation only, no tenant-facing data mixing.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' performed with WHERE eq(projects.id, projectId) but without an explicit tenant_id constraint
  - _Why false positive:_ Same function first loads the project by id and throws unless project.tenantId matches caller tenantId before performing the update.
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT INTO project_files omits tenant_id — inserting a project-scoped row without explicit tenant ownership column
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped through validated project_id ownership check via assertProjectInTenant().
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} updates a tenant-scoped table row by id only, with no tenant_id constraint (possible cross-tenant modification).
  - _Why false positive:_ Housekeeping scheduler loop updates next_run_at on rows it is already processing with row-carried sched.tenant_id; no tenant data returned or mixed.
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} lacks tenant_id constraint and can modify other tenants' rows.
  - _Why false positive:_ Platform-wide schedule runner mutates only last_run_at/next_run_at on the current schedule row during maintenance processing.
- **server/heartbeat.ts:111** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} does not include tenant_id in the WHERE clause.
  - _Why false positive:_ Same maintenance sweep advances next_run_at for the current schedule row only; housekeeping fields only, no cross-tenant content exposure.
- **server/heartbeat.ts:119** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} updates schedule rows without tenant scoping.
  - _Why false positive:_ Error-recovery branch of the same global scheduler sweep only repairs next_run_at on the current row; housekeeping-only mutation.
- **server/heartbeat.ts:260** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} updates a tenant-scoped heartbeat_tasks row using only id with no tenant_id restriction, risking cross-tenant mutation.
  - _Why false positive:_ Startup maintenance loop normalizes model field on heartbeat task rows during global housekeeping; status/config mutation only, not tenant data exposure.
- **server/heartbeat.ts:264** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} is executed without tenant_id in the WHERE clause (cross-tenant update risk).
  - _Why false positive:_ Same startup sweep performs housekeeping normalization of model on each task row without mixing tenant content into outputs.
- **server/job-queue.ts:98** _(system-sweep)_ — UPDATE agent_jobs ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ...) updates/claims jobs across all tenants with no tenant_id restriction — global cross-tenant claim/modification.
  - _Why false positive:_ Global worker queue claim intentionally operates across tenants and returns row-carried tenant_id for per-job handling.
- **server/job-queue.ts:134** _(system-sweep)_ — UPDATE agent_jobs (reclaimExpiredLeases step 1) sets status='failed_terminal' for expired leases across all tenants — no tenant_id constraint.
  - _Why false positive:_ Expired-lease reclamation is platform-wide housekeeping mutating only job status fields, not tenant-facing mixed data.
- **server/job-queue.ts:148** _(system-sweep)_ — UPDATE agent_jobs (reclaimExpiredLeases step 2) requeues expired jobs back to pending with no tenant_id constraint — touches rows across tenants.
  - _Why false positive:_ Requeue of expired jobs is a global maintenance sweep updating housekeeping fields across rows without cross-tenant output mixing.
- **server/lib/scheduled-post-runner.ts:164** _(upstream-verified)_ — UPDATE scheduled_posts SET ... WHERE id = ${row.id} — update lacks tenant_id constraint. This writes to a tenant-scoped table without constraining by tenant, allowing cross-tenant modification if row.id is controlled or mis-associated.
  - _Why false positive:_ Update uses row.id from the runner's previously selected locked row in the same call path, not untrusted caller input.
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT into memory_links does not include tenant_id. Tenant_id is required on tenant-scoped tables; omitting it will create cross-tenant/unscoped rows.
  - _Why false positive:_ memory_links has no tenant_id column and links tenant-scoped memory_entries, so isolation is transitive through referenced entries.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This iterates all tenants and exposes tenant-scoped rows (including secrets).
  - _Why false positive:_ Startup validation is a platform-wide maintenance loop that processes each subscription with its own tenant_id for per-row refresh/deactivation.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This reads tenant rows across all tenants.
  - _Why false positive:_ Boot-time auto-reconnect enumerates tenant ids for provider setup as a global maintenance action, not tenant-facing mixed output.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (proactiveTokenRefresh: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This enumerates all tenant rows, violating per-tenant isolation.
  - _Why false positive:_ Proactive token refresh is a global housekeeping sweep that handles each row using its carried tenant_id and only returns aggregate labels.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions with no tenant_id filter (forceRefreshAllSubscriptions: SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE). This iterates rows for every tenant without per-tenant filtering/authorization.
  - _Why false positive:_ Daily cron force-refresh intentionally scans all tenants and handles each row with its own tenant_id; housekeeping worker does not mix tenant data into one tenant-facing output.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'plans' (appendExecutionLog) has no tenant_id constraint (WHERE id = ${planId} only). This allows cross-tenant modification of plan rows.
  - _Why false positive:_ Internal executor helper updates a plan by server-generated planId within worker flow, not from an untrusted request boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE claiming a plan (UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved' ...) does not constrain by tenant_id. A process could claim/operate-on a plan from any tenant by id.
  - _Why false positive:_ Plan claim is an internal server worker CAS by globally unique plan primary key, returning tenant_id for per-row handling; not driven directly by cross-tenant request input.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status='failed' WHERE id = ${planId} (on empty/invalid plan) lacks a tenant_id guard, enabling cross-tenant writes to the plans table.
  - _Why false positive:_ This update occurs only after the prior claim RETURNING fetched the exact plan row including tenant_id for this planId in the same call path.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final UPDATE to plans (setting status/composing execution_log) uses WHERE id = ${planId} without tenant_id filtering and also injects JSON.stringify(...) directly into SQL. This both allows cross-tenant writes and uses string-interpolated JSON derived from step outputs (request/agent-derived) inside SQL.
  - _Why false positive:_ Final status write is in the same executePlan flow after the claimed plan row was loaded via id and tenant_id; operates on that already-verified row.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' has no tenant_id restriction, allowing the process to mark arbitrary tenant plans failed.
  - _Why false positive:_ Fatal-error cleanup is an internal worker state transition on the same server-side planId, guarded by status='executing', not an untrusted tenant request.
- **server/plan-executor.ts:774** _(system-sweep)_ — resumeStuckPlans() performs a broad UPDATE across the plans table (SET status='approved' WHERE status='executing' AND updated_at < ...), with no tenant restriction. This rolls back 'executing' plans across all tenants, violating tenant isolation.
  - _Why false positive:_ Boot-time recovery is a platform-wide maintenance sweep resetting stale statuses across tenants; it only mutates housekeeping state and returns aggregate counts/ids for requeue.
- **server/plan-executor.ts:784** _(system-sweep)_ — Selecting pending plans for re-kick: SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 has no tenant_id constraint — the executor will enqueue plans across all tenants indiscriminately.
  - _Why false positive:_ Boot-time restart intentionally selects approved plans across tenants for maintenance requeue; each plan is later processed per-row by its own id without tenant data mixing in one response.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write to a tenant-scoped table without constraining by tenant_id. Even if id was read earlier, writes must include tenant scoping to prevent cross-tenant modification.
  - _Why false positive:_ Each task id comes from an immediately prior SELECT scoped by ht.tenant_id = tenantId in the same call path, so the update targets only rows already filtered to the tenant.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates a tenant-scoped table without a tenant_id condition. This is a write that can affect rows from other tenants if IDs collide or are tampered with.
  - _Why false positive:_ Desk ids are sourced from the preceding SELECT with ad.tenant_id = tenantId in the same branch, so the subsequent update acts on tenant-filtered rows.
- **server/research-engine.ts:390** _(system-sweep)_ — DELETE statement deletes from tenant-scoped table `agent_knowledge` without tenant scoping (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will remove rows for all tenants, not just the intended tenant(s).
  - _Why false positive:_ Global expiry cleanup deletes only expired autoresearch rows; housekeeping sweep not tied to tenant-facing output.
- **server/routes.ts:4331** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id: INSERT INTO project_notes (project_id, note, author) VALUES (...). project_notes is tenant-scoped but tenant_id is omitted, so new rows will not be tenant-attributed and can break isolation.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id to tenant-scoped projects.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The statement `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` writes a row without an explicit tenant_id column.
  - _Why false positive:_ project_conversations has no tenant_id column and link row is scoped through validated project_id/conversation_id parents.
- **server/routes/conversations.ts:228** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})` writes a tenant-scoped artifact without the tenant_id column set.
  - _Why false positive:_ project_notes has no tenant_id column and note is scoped through project_id; project ownership is checked via join to projects tenant.
- **server/routes/projects.ts:184** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The statement INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and note belongs transitively to tenant-scoped project validated just above.
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (base64 upload) does not include tenant_id. The statement INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and file row is scoped through tenant-owned project_id validated earlier in handler.
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload) does not include tenant_id. The statement INSERT INTO project_files (...) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and file row is scoped through tenant-owned project_id validated earlier in handler.
- **server/sculptor.ts:94** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` lacks tenant_id constraint (WHERE id = ${session.id} only). This write can modify a row belonging to a different tenant if IDs collide or are misused.
  - _Why false positive:_ Updates freshly inserted session by server-generated primary key within same internal flow, not from untrusted request id.
- **server/sculptor.ts:101** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` sets conversation_id but omits tenant_id in the WHERE clause (WHERE id = ${session.id} only).
  - _Why false positive:_ Updates freshly inserted session by server-generated primary key within same internal flow, not from untrusted request id.
- **server/sculptor.ts:162** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` performed in monitorSession lacks tenant_id constraint (WHERE id = ${sessionId} only).
  - _Why false positive:_ Background monitor updates session by server-created sessionId passed from creator, not directly from cross-tenant request input.
- **server/seed.ts:2860** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table using only id (no tenant_id constraint). This can modify rows belonging to other tenants.
  - _Why false positive:_ boot-time seed fixer intentionally sweeps all research_programs and only normalizes a housekeeping/model field; no tenant data is returned or mixed.
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id when inserting into a likely tenant-scoped table, creating records with no tenant binding or implicitly relying on defaults.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped parent rows transitively by project/conversation foreign keys; omitting tenant_id is by design.
- **server/self-improvement.ts:546** _(upstream-verified)_ — INSERT into tenant-scoped `experiments` uses tenantId: data.tenantId! which is optional in the function signature. If caller omits tenantId, this can insert without a valid tenant_id (or throw), violating the invariant that tenant_id must be provided on INSERTs.
  - _Why false positive:_ this finding is about optionality/non-null assertion on INSERT, not an actual cross-tenant isolation bypass; the row is written with a tenantId value rather than without any tenant predicate.
- **server/storage.ts:838** _(system-sweep)_ — archiveExpiredMemories() executes raw SQL: UPDATE memory_entries SET status = 'archived' WHERE ... without any tenant_id constraint — unscoped bulk UPDATE across all tenants.
  - _Why false positive:_ archiveExpiredMemories is a platform-wide housekeeping sweep that only flips status on expired rows and returns counts/ids, not tenant content.
- **server/storage.ts:855** _(system-sweep)_ — archiveStaleMemories() runs a raw UPDATE on `memory_entries` (WHERE status = 'active' AND created_at <= …) without tenant_id filtering — unscoped bulk write across tenants.
  - _Why false positive:_ archiveStaleMemories is likewise a maintenance sweep archiving old rows by lifecycle criteria only, without exposing or mixing tenant data.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (where clause only checks name). This allows cross-tenant discovery/affects uniqueness checks.
  - _Why false positive:_ Name is deterministically tenant-prefixed via tenantToolName(parsed.name, tenantId) immediately above, so lookup is already tenant-scoped by derived unique name.
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id lacks a tenant_id constraint (.where(eq(customTools.id, inserted.id))). Reading tenant-scoped JS/tool bodies without tenant scoping can leak sensitive code across tenants.
  - _Why false positive:_ Reads back the just-inserted row by server-generated inserted.id within same internal flow, not from untrusted request input.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `custom_tools` uses .where(eq(customTools.id, tool.id)) without also constraining tenant_id. This allows modifying another tenant's row if an id is guessed/reused.
  - _Why false positive:_ Auto-fix updates the same internally created tool row by tool.id carried from prior insert/select, not attacker-controlled.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE on tenant-scoped table `custom_tools` uses .where(eq(customTools.id, tool.id)) with no tenant_id constraint. Deleting by id only can remove another tenant's tool if the id is known/guessed.
  - _Why false positive:_ Revert deletes the just-inserted internal tool row by server-held primary key, not from an external boundary.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment usageCount on tenant-scoped `custom_tools` uses .where(eq(customTools.id, tool.id)) without tenant_id constraint. This allows updating other tenants' rows by id.
  - _Why false positive:_ tool was selected a few lines above with tenant_id = tenantId, so updating by that row's id is on an already tenant-verified object in the same call path.
- **server/tools.ts:2979** _(fk-transitive)_ — INSERT into project_files does not set tenant_id (tenant-scoped table writes must include tenant_id)
  - _Why false positive:_ project_files has no tenant_id column and is scoped transitively via verified parent project_id after tenant ownership check.
- **server/tools.ts:2993** _(fk-transitive)_ — INSERT into project_notes omits tenant_id (writes to a tenant-scoped table must include tenant_id)
  - _Why false positive:_ project_notes has no tenant_id column and insert is tied to tenant-verified parent project_id.
- **server/tools.ts:3018** _(fk-transitive)_ — INSERT into project_conversations does not set tenant_id (tenant-scoped table writes must include tenant_id)
  - _Why false positive:_ project_conversations has no tenant_id column; both parent project and conversation are tenant-verified before insert.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT INTO project_files omits tenant_id when inserting a tenant-scoped row (project_files). Every INSERT against tenant-scoped tables must set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id column and row is linked through project_id obtained via join to tenant-scoped project.
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without constraining tenant (missing tenant_id check).
  - _Why false positive:_ The id being updated was selected immediately above under tenant_id = tid, persona_id, category, and prefix constraints in the same flow.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) without constraining tenant (missing tenant_id check).
  - _Why false positive:_ ids come only from rows just selected with WHERE tenant_id = tid, so update set is already tenant-scoped in same call path
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} without constraining tenant (missing tenant_id check).
  - _Why false positive:_ id is loaded immediately above from memory_entries with tenant_id = tid and persona_id filter before the update
- **server/video-job-runner.ts:228** _(system-sweep)_ — recoverStaleVideoJobs runs an UPDATE on videoJobs for stale statuses without any tenant_id constraint — this can mark jobs for all tenants as failed in a single global sweep.
  - _Why false positive:_ explicit periodic stale-job recovery sweep mutates housekeeping/status fields globally and returns only job ids for logging, not tenant data
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without constraining to a caller tenant. The heartbeat reads experiments across all tenants: `FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100`.
  - _Why false positive:_ heartbeat sweep intentionally checks running experiments across tenants and then updates each row with its own row-carried tenant_id
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with no tenant_id constraint. This can update another tenant's eval row if ids are guessed or leaked.
  - _Why false positive:_ evalId is the server-generated primary key returned from the INSERT in the same function, then updated internally, not from an untrusted request boundary
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` without tenant filtering in retryPendingMinervaRouting: the query returns rows across tenants (`WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER}`).
  - _Why false positive:_ durability sweep intentionally scans pending insights across tenants and processes each row with its own validated tenant_id
- **server/agentic/approvals.ts:134** _(system-sweep)_ — Bulk UPDATE of `agent_approvals` to expire stale approvals is executed without a tenant constraint: it updates all tenants' pending approvals whose expiresAt < now. This performs cross-tenant writes.
  - _Why false positive:_ stale-approval expiry is a platform sweep over pending approvals; it updates status fields globally and follow-on run updates use each returned row's tenantId
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Cross-tenant SELECT: the query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads tenant-scoped rows for all tenants without constraining to a caller tenant_id.
  - _Why false positive:_ heartbeat enforcement sweep intentionally scans active budgets across tenants and emits per-tenant events using each row's tenant_id
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT joins tenant-scoped tables `agent_runs` and `agent_approvals` without constraining to a single tenant (no WHERE tenant_id = <caller tenant>). The query scans across tenants (even though it joins a.tenant_id = r.tenant_id) and returns rows for all tenants.
  - _Why false positive:_ resume worker is a platform-wide sweep over pending approvals/runs; joined rows keep tenant alignment and are resumed/rejected per row using carried tenant_id
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — SELECT DISTINCT on conversations scans conversations across all tenants (no tenant_id WHERE clause). A global backfill read touches tenant-scoped rows without an application-level tenant filter.
  - _Why false positive:_ global backfill maintenance loop scans conversations across tenants and processes each conversation with its own tenant/project context rather than mixing into one tenant-facing output
- **server/auto-consolidation.ts:335** _(system-sweep)_ — SELECT DISTINCT tenant_id FROM memory_entries reads a tenant-scoped table without a tenant filter. The scheduler is enumerating tenant_ids by scanning the tenant-scoped table without an explicit tenant isolation guard in SQL.
  - _Why false positive:_ scheduler intentionally enumerates tenant ids platform-wide and then runs consolidation separately per tenant using the row-carried tenant_id
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' ... has no tenant_id constraint — reading/writing agent_knowledge is tenant-scoped but this query is not constrained to a caller tenant.
  - _Why false positive:_ Global watermark read is housekeeping for a platform-wide auto-memorize sweep, not tenant-facing data delivery.
- **server/auto-memorize.ts:62** _(system-sweep)_ — SELECT id FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} LIMIT 1 lacks a tenant_id predicate, so subsequent UPDATE/INSERT may act on or return rows across tenants.
  - _Why false positive:_ Watermark lookup/update is sweep bookkeeping for the cross-tenant job; it only mutates housekeeping state.
- **server/auto-memorize.ts:163** _(system-sweep)_ — Global SELECT from messages (SELECT m.tenant_id, m.role, m.content ...) reads messages across tenants without filtering by tenant_id. Although rows are later partitioned in-app, the SQL read itself touches tenant-scoped data without an explicit tenant predicate.
  - _Why false positive:_ This is an explicit cross-tenant maintenance scan that partitions by row-carried tenant_id before per-tenant processing and avoids mixed-tenant prompts.
- **server/auto-transcript.ts:128** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` without a tenant_id constraint (backfill selects across all tenants).
  - _Why false positive:_ BackfillProjectTranscripts is a platform-wide maintenance/backfill loop and each row is processed with its own tenant_id.
- **server/auto-transcript.ts:153** _(system-sweep)_ — SELECT joining project_conversations/conversations/projects (alsoLinked) runs without constraining by tenant_id — it will return rows across tenants.
  - _Why false positive:_ AlsoLinked is part of the same maintenance backfill; rows carry tenant_id and are handled per row without tenant-mixed output.
- **server/auto-transcript.ts:171** _(system-sweep)_ — SELECT (missingLinks) queries `conversations` for rows lacking a tenant_id constraint, potentially returning data across tenants.
  - _Why false positive:_ Missing-links query is housekeeping to repair linkage rows only; it does not expose mixed tenant data and only inserts association records.
- **server/business-tools.ts:91** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. Tenant-scoped rows must include tenant_id on INSERT to enforce isolation; inserting invoice items without tenant_id can create cross-tenant data/ownership gaps.
  - _Why false positive:_ invoice_items has no tenant_id and is transitively scoped through tenant-owned invoices; parent invoice insert is constrained to tid.
- **server/business-tools.ts:289** _(upstream-verified)_ — logInteraction writes customer_interactions.customer_id from caller input without first verifying the customer row belongs to the caller's tenant. Even though tenant_id is set on the interaction row, the code does not confirm ownership of the referenced customer, allowing callers to reference IDs from other tenants.
  - _Why false positive:_ logInteraction already verifies customer ownership with SELECT ... WHERE id = customer_id AND tenant_id = tid before insert.
- **server/chat-engine.ts:572** _(upstream-verified)_ — SELECT from tenant-scoped table `project_files` without tenant constraint — query: SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...
  - _Why false positive:_ projectId ownership is verified immediately above via SELECT * FROM projects WHERE id = projectId AND tenant_id = tenantId; project_files is then scoped by that verified parent id.
- **server/chat-engine.ts:583** _(upstream-verified)_ — SELECT from tenant-scoped table `project_notes` without tenant constraint — query: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...
  - _Why false positive:_ projectId is proven to belong to tenantId just above, and project_notes is a child table without tenant_id queried by that verified project id.
- **server/data-protection.ts:325** _(fk-transitive)_ — In backupTenantDataToDrive, SELECT title, content FROM project_notes WHERE project_id = ${proj.id} does not include tenant_id. Project-scoped queries that omit tenant_id can leak other tenants' notes if project_id values collide or FK assumptions are broken.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; projects were selected with tenant_id first.
- **server/data-protection.ts:336** _(fk-transitive)_ — In backupTenantDataToDrive, SELECT file_name,... FROM project_files WHERE project_id = ${proj.id} has no tenant_id constraint.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; parent projects are already tenant-scoped in this loop.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates a tenant-scoped row by id only, with no tenant_id constraint. This can modify another tenant's row if an id is mis-bound.
  - _Why false positive:_ Rows updated here come from a prior SELECT on doc_chunks constrained by collection_id and tenant_id in the same call path.
- **server/email.ts:410** _(system-sweep)_ — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) queries inbox_messages across all tenants with no tenant filter in backfillEmptyBodies. This scans a tenant-scoped table globally.
  - _Why false positive:_ Global backfill job scans tenant-scoped inbox_messages for housekeeping only and does not return mixed tenant data to any tenant-facing surface.
- **server/email.ts:424** _(system-sweep)_ — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} performs an update on a tenant-scoped table without a tenant_id constraint.
  - _Why false positive:_ Update is part of the same global housekeeping backfill loop, mutating only missing body fields on rows selected by the sweep.
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw(`'${vec}'::vector`) where vec is derived from a caller-provided query embedded into SQL via string interpolation. Although vecLiteral validates numeric shape, embedding data originates from request/model output — using sql.raw with request-derived content is an injection/isolation risk per policy.
  - _Why false positive:_ Query explicitly enforces tenant_id = tenantId and refuses to run without tenantId; not a cross-tenant isolation issue.
- **server/embeddings.ts:559** _(upstream-verified)_ — Use of sql.raw(`'${vec}'::vector`) in vectorSearchKnowledge with vec produced from generateEmbedding (request/model-derived). This interpolates request-derived data into raw SQL.
  - _Why false positive:_ Query explicitly constrains agent_knowledge by tenant_id = tenantId and returns [] if tenantId is absent; no cross-tenant access.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' uses only project id in WHERE (no tenant_id constraint) before doing ownership checks in code
  - _Why false positive:_ Function immediately verifies fetched project's tenantId matches caller tenantId before any folder is returned or created.
- **server/google-drive.ts:825** _(upstream-verified)_ — SELECT projects by projectId (uploadAndShare) without scoping the query to the caller's tenant (no tenant_id in WHERE) before using project data
  - _Why false positive:_ Caller-side check compares proj.tenantId to params.tenantId before using project context, and ensureProjectFolder revalidates ownership in same call path.
- **server/google-drive.ts:1003** _(upstream-verified)_ — SELECT projects to verify project ownership in searchDriveFiles uses only project id in WHERE (no tenant_id constraint) prior to application-level tenant checks
  - _Why false positive:_ Project lookup is followed immediately by tenant mismatch rejection before subsequent search logic; later DB pass is tenant-scoped via join on projects.tenant_id.
- **server/google-drive.ts:1066** _(upstream-verified)_ — SELECT projects to fetch driveFolderId (searchDriveFiles pass 2) uses only project id in WHERE (no tenant_id constraint)
  - _Why false positive:_ Same call path already verified project ownership against params.tenantId at line 1004 before reading driveFolderId.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` is executed with no tenant_id constraint (global scan).
  - _Why false positive:_ Admin health-audit performs platform-wide stale-row scan for maintenance findings; intended global housekeeping, not tenant-facing data access.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on `code_proposals` (SET status='archived_stale' WHERE id = ANY(...)) executes without constraining by tenant_id, allowing cross-tenant modification.
  - _Why false positive:_ Update archives stale proposals as maintenance based on ids selected by the global audit sweep; housekeeping-only mutation.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) is executed without tenant_id filtering, allowing disabling tasks across tenants.
  - _Why false positive:_ Update disables stale heartbeat tasks in a platform-wide maintenance sweep and only changes housekeeping status fields.
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT from tenant-scoped table research_schedules is executed without constraining by tenant_id (cross-tenant read).
  - _Why false positive:_ heartbeat is a platform-wide scheduler sweep; each schedule row carries tenant_id and downstream work is per-row scoped
- **server/heartbeat.ts:658** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... fetches pending events across all tenants with no tenant_id filter. Background processing then acts on those rows — this is an unconstrained read of a tenant-scoped table.
  - _Why false positive:_ background event sweep reads pending rows across tenants but processes each event with its own tenant_id and does not aggregate tenant data into one output
- **server/heartbeat.ts:1828** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... is executed without tenant scoping; heartbeat_logs is tenant-scoped so this is a cross-tenant read.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id, so this log table is not tenant-scoped
- **server/internal-resolver.ts:91** _(internal-worker-pk)_ — UPDATE of tenant-scoped table `event_log` (finalizeEvent) has no tenant_id constraint in WHERE clause — allows finalizing events across tenants.
  - _Why false positive:_ server-invoked resolver finalizes event_log by server-side eventId primary key, not from a request boundary
- **server/job-queue.ts:220** _(internal-worker-pk)_ — SELECT max_attempts FROM agent_jobs WHERE id = ${id} reads a tenant-scoped row by id with NO tenant_id constraint — allows reading other tenants' jobs by id.
  - _Why false positive:_ worker failJob reads agent_jobs by server-generated job id inside internal queue processing, not a tenant request parameter
- **server/job-queue.ts:237** _(internal-worker-pk)_ — UPDATE agent_jobs to mark failed_terminal in failJob lacks tenant_id in WHERE clause — can mutate jobs belonging to other tenants by id.
  - _Why false positive:_ worker failure-state update is by internal job primary key with lease-fencing attempts guard, not an untrusted cross-tenant input
- **server/job-queue.ts:257** _(internal-worker-pk)_ — UPDATE agent_jobs to requeue retryable jobs in failJob has no tenant_id constraint — cross-tenant modification risk.
  - _Why false positive:_ worker retry requeue update is by internal job primary key with attempts fencing, not caller-controlled tenant input
- **server/job-queue.ts:177** _(internal-worker-pk)_ — UPDATE agent_jobs in completeJob marks a job succeeded by id but omits tenant_id in WHERE clause — allows completing jobs across tenants if an id is known.
  - _Why false positive:_ completeJob is an internal worker state transition on a server-generated job id with attempts fencing
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The platform invariant requires explicit tenant_id on every INSERT into tenant-scoped tables (no RLS / FK-enforced tenant ownership). This INSERT into project_files only sets project_id and other columns, risking misattribution or cross-tenant leakage if project_files is tenant-scoped.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects via project_id; insert without tenant_id is by design
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The module comment explicitly expects explicit tenant_id on every INSERT; omitting tenant_id violates the tenant-isolation invariant and can misattribute notes across tenants.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects via project_id; insert without tenant_id is by design
- **server/lib/scheduled-post-runner.ts:62** _(system-sweep)_ — SELECT from tenant-scoped table `scheduled_posts` without a tenant_id constraint in the CTE (system poll/lock selects all pending rows across tenants).
  - _Why false positive:_ platform-wide scheduler intentionally polls due rows across tenants, returns each row with its own tenant_id, and processes rows independently
- **server/lib/scheduled-post-runner.ts:191** _(upstream-verified)_ — Error-handling UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing' — missing tenant_id filter. The rollback/cleanup path updates tenant-scoped rows without constraining tenant context.
  - _Why false positive:_ the row being updated was just locked and fetched by id in the same call path; cleanup targets that specific picked row already carrying tenant_id
- **server/memory-graph.ts:196** _(fk-transitive)_ — SELECT against memory_links (exists check) does not constrain tenant_id. Reading memory_links without tenant scoping may reveal cross-tenant links or allow logic to act on other tenants' links.
  - _Why false positive:_ memory_links has no tenant_id and links are created only between memory_entries already selected within the same tenant-scoped query
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ... WHERE id = ${mem.id} has no tenant_id constraint. This may update another tenant's memory entry.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries constrained by tenant_id in the same function, so the update targets tenant-owned rows from that scoped result set
- **server/minds-engine.ts:326** _(upstream-verified)_ — markEventsHandled uses id = ANY(${`{${eventIds.join(',')}}`}::int[]) where eventIds come from the caller. This constructs SQL via string interpolation from request-derived values and is a SQL injection risk even though tenant_id is included.
  - _Why false positive:_ not a tenant-isolation issue: the UPDATE includes tenant_id = tenantId, so cross-tenant access is already constrained
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The code calls assertProjectInTenant(...) to check ownership, but the actual INSERT does not include tenant_id in the column list/VALUES.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-isolated transitively via project_id; assertProjectInTenant also verifies the parent belongs to the tenant
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id. Although assertProjectInTenant(...) is called to verify ownership, the INSERT does not set tenant_id in the columns/VALUES.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped through project_id; parent ownership is checked with assertProjectInTenant
- **server/plan-replay.ts:56** _(upstream-verified)_ — String-interpolated vector literal (embeddingLiteral = `[... ]`) is injected into a SQL query: ORDER BY objective_embedding <=> ${embeddingLiteral}::vector. embeddingLiteral is derived from the request objective (user input). This is effectively string-interpolated SQL using request-derived data (injection + isolation risk).
  - _Why false positive:_ not a tenant-isolation issue: the replay lookup is constrained by tenant_id = tenantId on the tenant-scoped table
- **server/plan-replay.ts:152** _(upstream-verified)_ — INSERT into plan_replay_cache uses ${embeddingLiteral}::vector and ${JSON.stringify(...)}::jsonb interpolated into the SQL. Both values are derived from request/plan data — this is string-interpolated SQL with request-derived content and risks injection or other unsafe behavior.
  - _Why false positive:_ not a tenant-isolation issue: inserted plan_replay_cache row explicitly sets tenant_id = plan.tenantId
- **server/plan-rollout-simulator.ts:119** _(upstream-verified)_ — kNN query injects a vector literal via ${vec}::vector (vec built from a user/request-derived embedding). This is string-interpolated SQL with request-derived content (embedding), which is an injection/isolation risk even though tenant_id is present.
  - _Why false positive:_ not a tenant-isolation issue: the kNN query is explicitly scoped with tenant_id = tenantId
- **server/process-governor.ts:121** _(fk-transitive)_ — Tenant-scoped table `heartbeat_logs` is queried in subselects without constraining by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). Reads against tenant-scoped audit/log tables must be scoped to the caller tenant.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted by task_id from tenant-scoped heartbeat_tasks rows already filtered by ht.tenant_id = tenantId
- **server/process-governor.ts:199** _(global-table)_ — Query over `heartbeat_logs` (performance/response_time check) omits tenant_id filter: SELECT AVG(duration_ms) FROM heartbeat_logs WHERE created_at > ... AND status = 'success'. This reads global logs across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; this response_time metric is intentionally platform-global aggregate health, not tenant-row access
- **server/process-governor.ts:240** _(global-table)_ — Query in cascading_failures reads `heartbeat_logs` without a tenant filter (COUNT DISTINCT persona_id FROM heartbeat_logs WHERE status != 'success' ...). This may aggregate failures across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and query computes a platform-wide cascading-failure count, consistent with global governance health metric
- **server/process-governor.ts:585** _(fk-transitive)_ — In executeAction('disable_task') the SELECT uses heartbeat_logs subqueries without tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). Reading tenant-scoped logs without tenant constraint risks cross-tenant leakage.
  - _Why false positive:_ heartbeat_logs has no tenant_id and subqueries are scoped via tenant-filtered heartbeat_tasks parent rows (ht.tenant_id = tenantId)
- **server/reference-learner.ts:291** _(upstream-verified)_ — UPDATE to tenant-scoped table `memory_entries` omits tenant_id in WHERE clause (UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}). This will update the row by id without verifying it belongs to the caller's tenant.
  - _Why false positive:_ storedId comes from immediately prior SELECT on memory_entries constrained by input.tenantId and persona/category/prefix, so UPDATE by that id is already ownership-verified in same call path
- **server/routes.ts:165** _(system-sweep)_ — SELECT from tenant-scoped table 'file_storage' without any tenant_id constraint (restoreUploadsFromDb). This reads all rows across tenants and writes files to a shared UPLOADS_DIR, allowing cross-tenant data exposure.
  - _Why false positive:_ Boot-time restore scans all tenant rows for housekeeping only and writes each row independently; not exposed to tenant-facing output.
- **server/routes.ts:764** _(system-sweep)_ — Startup backfill (backfillProjectDriveFolders) SELECTs from 'projects' (tenant-scoped) with no tenant filter, iterating across all projects in the DB. This performs cross-tenant reads/operations without explicit platform-admin scoping in SQL.
  - _Why false positive:_ Startup backfill intentionally iterates all projects and handles each row with its own tenant_id for maintenance.
- **server/routes/admin.ts:569** _(upstream-verified)_ — Query against tenant-scoped table `tool_performance` without a tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance). This will return rows across all tenants.
  - _Why false positive:_ Route is explicitly gated to ADMIN_TENANT_ID plus isAdminRequest before global admin query.
- **server/routes/admin.ts:574** _(upstream-verified)_ — Query against tenant-scoped table `tool_performance` (failing rate check) has no tenant_id filter (SELECT ... FROM tool_performance WHERE ...).
  - _Why false positive:_ Same admin-only endpoint is restricted by tenantId===ADMIN_TENANT_ID and isAdminRequest before cross-tenant diagnostics query.
- **server/routes/admin.ts:587** _(upstream-verified)_ — Query against `ai_insights` selects rows (WHERE status = 'new' ...) without filtering by tenant_id. `ai_insights` appears tenant-scoped so this can leak data between tenants.
  - _Why false positive:_ Admin-only guard immediately above proves intentional platform-wide diagnostic access.
- **server/routes/admin.ts:598** _(upstream-verified)_ — Query against tenant-scoped table `heartbeat_tasks` has no tenant_id constraint (SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE ...).
  - _Why false positive:_ Admin-only route guard upstream makes this an intentional global operational view.
- **server/routes/admin-zombie-detector.ts:38** _(upstream-verified)_ — The `used` CTE reads `agent_trace_spans` without any tenant_id filter (SELECT tool_name, MAX(started_at) ... FROM agent_trace_spans WHERE ...). `agent_trace_spans` is tenant-scoped so this aggregates across tenants.
  - _Why false positive:_ Endpoint is explicitly restricted to platform admin tenant before the aggregate cross-tenant admin query.
- **server/routes/admin-zombie-detector.ts:54** _(upstream-verified)_ — The `used` CTE for personas aggregates `conversations` without a tenant filter (SELECT persona_id, MAX(updated_at) ... FROM conversations WHERE ...). `conversations` is tenant-scoped and this will aggregate across tenants.
  - _Why false positive:_ Same route has prior ADMIN_TENANT_ID and isAdminRequest guard, making global usage aggregation intentional.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' lacks tenant constraint. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` does not include `tenant_id = <caller tenant>`.
  - _Why false positive:_ conv was just created with tenantId from request, so updating by its server-generated id is already tenant-bound in this call path.
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' lacks tenant constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` does not include `tenant_id = <caller tenant>`.
  - _Why false positive:_ A few lines above, project existence is verified with WHERE id = projectId AND tenant_id = tenantId before this timestamp update.
- **server/routes/conversations.ts:208** _(upstream-verified)_ — SELECT against tenant-scoped table 'conversations' is performed without an explicit tenant filter. The query `SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} ...` lacks `c.tenant_id = <caller tenant>` in the WHERE clause.
  - _Why false positive:_ Conversation is loaded/scoped earlier via storage.getConversation(...tenantId) or admin-only unscoped path, and join also constrains project tenant.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table 'inbox_messages' lacks tenant constraint. The code `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` updates by id only and omits `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ The message row is first selected with id and tenant_id from the request, and the update uses that verified row id in the same path.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id filter: SELECT * FROM project_files WHERE project_id = ${projectId} ... even though project ownership was checked earlier at the application layer.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by prior verified tenant-owned project check on projects.id
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` (file download) lacks tenant_id constraint: SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1.
  - _Why false positive:_ project_files has no tenant_id and access is constrained by project_id after tenant-owned project verification
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE against tenant-scoped table `project_files` lacks tenant_id predicate: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}.
  - _Why false positive:_ project_files is child of tenant-scoped projects; delete is limited to verified tenant-owned project_id
- **server/seed.ts:3205** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` without constraining tenant (no WHERE tenant_id = ...). The query: SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)
  - _Why false positive:_ boot-time backfill intentionally scans across tenants and only repairs missing links per row; no tenant data is returned or mixed
- **server/seed.ts:3213** _(system-sweep)_ — INSERT into `project_conversations` does not record or verify tenant context. project_conversations has no tenant_id column and the insert uses project_id/conversation_id values derived from an unscoped SELECT, enabling potential cross-tenant linking.
  - _Why false positive:_ maintenance backfill inserts child link rows per selected conversation/project pair during platform-wide repair, with no tenant-facing output mixing
- **server/seed.ts:4431** _(system-sweep)_ — DELETE from tenant-scoped table `heartbeat_tasks` without a tenant filter: DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE ... AND type != 'maintenance_script'. This will delete rows across all tenants.
  - _Why false positive:_ platform-wide housekeeping delete only purges stale auto-disabled task rows and does not expose or mix tenant data
- **server/seed.ts:4460** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` runs without a tenant constraint. The normalizer UPDATE will modify rows across all tenants: UPDATE memory_entries SET fact = REPLACE(...) WHERE ...
  - _Why false positive:_ global maintenance normalizer updates rows in place across tenants without returning content or mixing tenants into one output
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining tenant_id. The query uses only conversationId in the WHERE clause and lacks tenant scoping.
  - _Why false positive:_ conversation row is fetched just above with id+tenantId; subsequent message read is scoped via validated parent conversation in same call path
- **server/skillify.ts:221** _(global-table)_ — SELECT from `personas` by persona id with NO tenant constraint. Reading a persona referenced by a conversation without verifying the persona row belongs to the same tenant risks cross-tenant data exposure.
  - _Why false positive:_ schema note says personas has no tenant_id; lookup is against a non-tenant-scoped table by referenced id
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining tenant_id. The recentMsgs query filters only by conversationId and does not include tenant scoping.
  - _Why false positive:_ analyzeConversation(conversationId, tenantId) already verifies the conversation belongs to tenant earlier in same flow before recentMsgs read
- **server/storage.ts:295** _(upstream-verified)_ — attachCitationMetadata() issues a raw SELECT FROM messages WHERE id IN (...) without any tenant_id constraint — reading tenant-scoped `messages` rows across tenants.
  - _Why false positive:_ ids come only from msgs already fetched with tenant-scoped getMessages/getMessagesPaginated in same call path; helper just hydrates those rows
- **server/surprise-scorer.ts:139** _(upstream-verified)_ — scoreProposalSurprise() writes actual_outcome_embedding via UPDATE felix_proposals SET actual_outcome_embedding = … WHERE id = ${proposalId} with no tenant_id constraint (unscoped write).
  - _Why false positive:_ Same call path first selects the proposal with id + tenant_id and aborts if not found; this write uses that validated proposalId immediately afterward.
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — After scoring the code UPDATEs felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} with NO tenant_id constraint — unscoped write to tenant-scoped table.
  - _Why false positive:_ scoreProposalSurprise already scoped the target row by id and tenant_id at entry and only proceeds on that verified row before writing back this result.
- **server/tools.ts:2973** _(fk-transitive)_ — SELECT from project_files used to check for duplicates lacks a tenant_id constraint (WHERE tenant_id = <caller tenant>)
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped through project_id; code already verified the parent project belongs to tenant before duplicate check.
- **server/tools.ts:3014** _(fk-transitive)_ — SELECT from project_conversations to check existing links does not include a tenant_id constraint
  - _Why false positive:_ project_conversations has no tenant_id and is scoped by tenant-owned project_id; project ownership and conversation tenant are both verified earlier in the same path.
- **server/tools.ts:4514** _(fk-transitive)_ — INSERT into project_notes omitted tenant_id. Writing to a tenant-scoped table must include tenant_id explicitly (columns are NOT NULL/no default) even if ownership was previously asserted.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; code asserts project ownership before insert.
- **server/tools.ts:4518** _(fk-transitive)_ — INSERT into project_files omitted tenant_id. Tenant-scoped rows must be inserted with tenant_id set explicitly, not inferred from project_id alone.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; code asserts project ownership before insert.
- **server/tools.ts:6365** _(fk-transitive)_ — INSERT into project_files omitted tenant_id (generate_audio path). Even though assertProjectInTenant was used, tenant_id must still be passed explicitly into the tenant-scoped table.
  - _Why false positive:_ project_files has no tenant_id and insert is correctly scoped via verified parent project_id.
- **server/tools.ts:8859** _(upstream-verified)_ — UPDATE against tenant-scoped table 'proposed_skills' uses WHERE eq(proposedSkills.id, row.id) without constraining by tenant. This lets a caller modify rows across tenant boundaries (no tenant_id filter present in the WHERE clause).
  - _Why false positive:_ This update targets row.id returned from the immediately preceding insert into proposed_skills with tenantId=tid, so the affected row's ownership is established in the same path.
- **server/tools.ts:8884** _(upstream-verified)_ — UPDATE against tenant-scoped table 'proposed_skills' uses WHERE eq(proposedSkills.id, row.id) without constraining by tenant. This update path (marking a proposal accepted/promoted) does not verify tenant ownership in the SQL WHERE clause.
  - _Why false positive:_ This update also uses row.id from the same freshly inserted proposed_skills row created under tenant tid earlier in the call path.
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} lacks a tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ projId comes from prior join that enforces p.tenant_id = ctx.tenantId in same call path
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table 'knowledge_triples' is performed without constraining by tenant_id (WHERE id = ${row.id} only). This violates the invariant that every write to tenant-scoped tables must include the caller's tenant constraint and risks affecting rows outside the caller's tenant if ids are misused.
  - _Why false positive:_ rows being updated were selected immediately above from knowledge_triples with tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys without tenant filter — reads from a likely tenant-scoped table (provider_keys) using no WHERE tenant_id constraint (admin-only path but still returns keys across tenants).
  - _Why false positive:_ schema says provider_keys has no tenant_id and code restricts this audit to admin tenant
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — COUNT(*) on provider_keys without tenant filter — admin path executes SELECT COUNT(DISTINCT provider) FROM provider_keys without constraining by tenant_id, exposing aggregated cross-tenant provider key presence.
  - _Why false positive:_ provider_keys has no tenant_id per schema and this is an admin-only aggregate audit
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped ledger row by id only, without constraining tenant_id — allows modifying another tenant's ledger row if reservationId is mis-specified.
  - _Why false positive:_ server-internal helper updates a ledger row by server-generated reservationId, not directly from request input
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseVentureReservation) updates ledger rows without tenant_id constraint, permitting cross-tenant modification.
  - _Why false positive:_ server-internal helper releases a row by server-generated reservationId, not directly from request input
- **server/video-job-runner.ts:178** _(internal-worker-pk)_ — mirrorStateToDb upsert (db.insert(...).onConflictDoUpdate) updates videoJobs rows on conflict without constraining by tenant_id in the UPDATE condition — allows overwriting another tenant's job row keyed by jobId.
  - _Why false positive:_ internal runner mirrors state by jobId from server-managed job state, not untrusted tenant input
- **server/wiring-invariants.ts:57** _(system-sweep)_ — SELECT from research_schedules (joined to research_programs) is executed with no tenant_id filter — reads tenant-scoped rows across all tenants.
  - _Why false positive:_ platform-wide invariant check reads across tenants for maintenance diagnostics only, with no tenant-facing mixed data output shown
- **server/wiring-invariants.ts:108** _(system-sweep)_ — SELECT name FROM research_programs executes without a tenant_id constraint — may read tenant-scoped program rows across tenants.
  - _Why false positive:_ platform-wide invariant check over research_programs is maintenance-only and not a tenant-facing data return path
- **server/wiring-invariants.ts:111** _(global-table)_ — SELECT name FROM personas runs with no tenant_id filter — tenant-scoped persona rows may be read across tenants.
  - _Why false positive:_ schema says personas has no tenant_id and nearby comment explicitly marks personas as global/shared
- **server/wiring-invariants.ts:142** _(global-table)_ — SELECT name FROM research_programs (in checkProposalTargets) runs without tenant_id constraint — cross-tenant reads possible.
  - _Why false positive:_ Boot-time/admin wiring invariant reads research_programs as platform seed/config data, not tenant-facing content.
- **server/wiring-invariants.ts:180** _(system-sweep)_ — Complex SELECT joining research_programs, research_experiments, code_proposals and research_sessions is executed without any tenant_id filtering — this aggregates/reads tenant-scoped rows across all tenants.
  - _Why false positive:_ Cross-tenant health canary intentionally aggregates counts/status over recent research activity and emits only invariant findings, not tenant data.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — SELECT count(*) FROM tool_performance WHERE last_success_at > ... is executed with no tenant_id filter — reads tool_performance across all tenants.
  - _Why false positive:_ Dormant-tools check is a platform-wide wiring/housekeeping aggregate over tool_performance and returns only counts/findings.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT count(*) FROM tool_performance (everCount) is executed without tenant_id filter — will count rows across all tenants.
  - _Why false positive:_ Global everCount is used only for maintenance diagnostics and does not expose tenant rows or mix content.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM messages WHERE created_at > ... runs with no tenant_id constraint — counts messages across all tenants.
  - _Why false positive:_ 14-day message count is a platform-wide traffic heuristic for housekeeping, with only aggregate count output.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... is executed without tenant_id filtering — will aggregate usage across tenants.
  - _Why false positive:_ Distinct used tool names are aggregated globally for maintenance/drift detection and no tenant content is returned.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — sql.raw executes SELECT COUNT(*) and MAX(timestamp) FROM ${spec.table} with no tenant_id filter — reading subsystem tables across tenants.
  - _Why false positive:_ Subsystem freshness probe intentionally scans tables globally and uses only row counts/max timestamps for housekeeping findings.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — SELECT aggregating tool_performance (SUM success/fail, MAX last_failure_at) has no tenant_id filter — will compute drift metrics across tenants.
  - _Why false positive:_ Tool drift detector is a platform-wide aggregate maintenance check over tool_performance producing only summarized warnings.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — sql.raw runs SELECT COUNT(*) and pg_total_relation_size(...) FROM memory tables (tbl.name) without tenant_id filter — counts/size measured across all tenants.
  - _Why false positive:_ Memory growth tripwire intentionally measures global table counts/sizes for maintenance and emits only aggregate thresholds.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory is executed without tenant_id filtering — reads graph_memory rows across all tenants (note: comment even mentions 'across all tenants').
  - _Why false positive:_ Global empty-check for graph_memory is an explicit housekeeping probe and returns only a zero/nonzero finding.
- **server/heartbeat.ts:2921** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates approval_status without constraining by tenant_id — this can change approval state for another tenant's task with the same id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3374** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without a tenant_id condition (cross-tenant write risk).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/seed.ts:3137** _(system-sweep)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... performs an update on a tenant-scoped table without any tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:3131-3138 inside seedDatabase(); guarded by isOwnerDeployment() (3133) and uses fully hardcoded literals (id=15, primary_conversation_id=272) — a one-time owner-deployment migration, not reachable from any request input.
