# Tenant-Isolation Audit Report

_Generated 2026-08-11T17:41:08.578Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **591/591** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **300** (CRITICAL 58 · HIGH 196 · MEDIUM 46 · LOW 0)
- Precision second-pass (`gpt-5.4`): **237** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (58)

- **server/agentic-engines.ts:243** — UPDATE across tenants: UPDATE ai_insights SET status='applied', action_taken=... WHERE status='new' AND category IN (...) RETURNING id, tenant_id,... — no tenant_id condition. This will modify rows for all tenants.
  - _Fix:_ Do not perform cross-tenant UPDATEs. Perform tenant-scoped updates (add WHERE tenant_id = ${tenantId}) or iterate tenants and run the update within each tenant's scope. If intentional global backfill is required, implement strict authorization and logging and ensure it cannot overwrite tenant data unintentionally.
- **server/agentic/approvals.ts:135** — Bulk UPDATE on tenant-scoped table `agent_approvals` without tenant constraint: UPDATE agentApprovals SET status='expired', ... WHERE status = 'pending' AND expiresAt < now(). This will expire pending approvals across all tenants in one unscoped statement.
  - _Fix:_ Do not perform global, cross-tenant updates. Instead, perform the expiry per-tenant (add WHERE tenant_id = ${tenantId} for a given tenant or iterate tenants) or otherwise ensure the operation is explicitly authorized and limited. Always include tenant_id in WHERE clauses for tenant-scoped writes.
- **server/agentic/resume-worker.ts:111** — SQL string interpolation embeds request-derived values (decidedBy / note) directly into the SQL fragment in the UPDATE statement, producing a SQL-injection risk and violating safe parameterization/isolation rules.
  - _Fix:_ Avoid constructing SQL literals with JS string interpolation. Use parameterized values via the sql template or Drizzle setters (e.g. use sql`error = ${errorMessage}` or db.update(...).set({ error: someValue }) ), and never inline untrusted strings into raw SQL fragments.
- **server/auto-asset-capture.ts:145** — INSERT INTO project_files is executed without providing tenant_id. project_files is project-scoped/tenant-scoped and the INSERT relies on transitive ownership via project_id rather than explicitly setting tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT into project_files (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${projectId}, ...)). If the table schema does not include tenant_id, change schema to include tenant_id or otherwise ensure an explicit tenant_id column is set on writes to enforce isolation.
- **server/auto-asset-capture.ts:164** — INSERT INTO project_notes is executed without tenant_id. The write to a tenant-scoped table (project_notes) relies on transitive project ownership instead of setting tenant_id on the row.
  - _Fix:_ Set tenant_id explicitly when inserting project_notes (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projectId}, ...)). If the table lacks tenant_id, update the schema or otherwise ensure writes cannot occur without an explicit tenant constraint.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge performed with no tenant_id constraint (reads watermark globally). This can return/operate on another tenant's watermark.
  - _Fix:_ Make getWatermark tenant-aware: accept a tenantId parameter and add a WHERE tenant_id = ${tenantId} clause (or otherwise scope the query to the calling tenant). Ensure any returned/default behavior is per-tenant.
- **server/auto-memorize.ts:62** — SELECT id FROM agent_knowledge in setWatermark() has no tenant_id constraint. The subsequent UPDATE/INSERT logic relies on that row but is not scoped to a tenant.
  - _Fix:_ Require and pass a tenantId into setWatermark(), add WHERE tenant_id = ${tenantId} to the SELECT, and only UPDATE rows that match that tenant_id (or INSERT a row for the specified tenant).
- **server/auto-memorize.ts:74** — INSERT into agent_knowledge sets tenant_id = 1 (hardcoded) instead of using the calling tenant. This will write a watermark into tenant 1 regardless of caller and can corrupt tenant isolation.
  - _Fix:_ Do not hardcode tenant_id. Accept/derive the correct tenantId in setWatermark() and INSERT with VALUES (..., ${tenantId}, ...). If the watermark is intended to be global, document that clearly; otherwise make it per-tenant.
- **server/auto-memorize.ts:163** — SELECT from messages (SELECT m.tenant_id, m.role, ...) is executed without constraining to the caller's tenant. It reads messages across tenants and relies on application partitioning after the read — this violates tenant isolation invariants.
  - _Fix:_ Avoid scanning messages across tenants in a single query. Either run per-tenant queries with WHERE m.tenant_id = ${tenantId}, or explicitly enumerate tenants and query each tenant separately. If cross-tenant reads are required for an admin task, require an explicit admin path and make that clear and gated.
- **server/auto-project.ts:112** — SELECT FROM messages by conversation_id is executed without a tenant_id filter. Although the conversation row was previously fetched with a tenant check, the messages query itself is not scoped and could read messages not constrained by tenant_id.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages SELECT (or otherwise scope the messages query to the verified conversation tenant). Prefer querying messages WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}.
- **server/auto-transcript.ts:46** — SELECT FROM messages WHERE conversation_id = ${conversationId} is executed without a tenant_id constraint. Even though the code previously asserted conversation ownership, the messages read itself is unscoped SQL against a tenant-scoped table.
  - _Fix:_ Include tenant scoping in the messages query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or fetch messages per-tenant after verifying conversation ownership. Ensure every read against tenant-scoped tables includes the tenant_id filter.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL has no tenant_id constraint. This can modify a conversation owned by another tenant if IDs are misused or poisoned.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}), or perform the update via a transaction that double-checks ownership before writing.
- **server/channel-routing.ts:17** — The channel_routes table is created with no tenant_id column. Without tenant_id the table cannot be scoped per-tenant and all rows are effectively global, enabling cross-tenant exposure and writes.
  - _Fix:_ Add a tenant_id INTEGER NOT NULL column (with a FK to tenants if present), make any uniqueness per-tenant (e.g. UNIQUE(channel, tenant_id)), and update all queries/inserts/deletes to require and set tenant_id explicitly.
- **server/chat-engine.ts:633** — SELECT from compaction_archives uses conversation_id without constraining tenant_id — tenant-scoped table read lacks tenant filter (possible cross-tenant data leak).
  - _Fix:_ Add an explicit tenant filter to the query (e.g. AND tenant_id = ${tenantId}) or join via a previously-validated parent row and include the tenant_id predicate in the WHERE/LATERAL subquery. Ensure every access to tenant-scoped tables includes eq(table.tenantId, tenantId) (or equivalent) so reads cannot return other tenants' data.
- **server/chat-engine.ts:651** — SELECT from messages WHERE conversation_id = ${c.id} has no tenant_id constraint — reading messages (tenant-scoped) without verifying tenant ownership of the row.
  - _Fix:_ Constrain the messages read to the active tenant (e.g. add AND tenant_id = ${tenantId} or use a parameterized Drizzle where(eq(messages.tenantId, tenantId))). If relying on conversation ownership, first verify the conversation belongs to the tenant and then include tenant_id in the messages query (defense-in-depth).
- **server/chat-engine.ts:761** — LATERAL query / subselects pull content from messages (e.g. (SELECT content FROM messages WHERE conversation_id = c.id ...)) without any tenant_id condition — messages is tenant-scoped but the subqueries lack tenant constraint, risking cross-tenant disclosure.
  - _Fix:_ Ensure the lateral/sub-selects include tenant scoping (e.g. WHERE conversation_id = c.id AND tenant_id = ${tid} AND role = 'user' ...). Alternatively, restructure to join messages via a conversation row that is explicitly filtered by tenant_id and include tenant checks inside the subqueries to satisfy the requirement that every tenant-scoped table access is explicitly tenant-filtered.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` executed without tenant_id constraint (WHERE id = ${row.id} only). This can modify a row belonging to a different tenant for the same id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or avoid issuing unscoped updates; fail-closed and log if tenantId is invalid. Ensure every mutation against tenant-scoped tables always includes the tenant_id guard.
- **server/data-protection.ts:168** — permanentlyPurgeSoftDeleted deletes conversations with DELETE FROM conversations WHERE id = ${conv.id} without scoping by tenant_id. This can remove a conversation across tenant boundaries.
  - _Fix:_ Include the conversation's tenant predicate in the DELETE, e.g. DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}, or perform the delete inside a transaction that sets/validates the tenant context. Always ensure every DELETE against a tenant-scoped table includes WHERE tenant_id = <owner>.
- **server/embeddings.ts:716** — UPDATE against tenant-scoped tables in storeEmbeddingVec() does not constrain by tenant_id (UPDATE ${table} ... WHERE id = ${id}) — allows cross-tenant modification.
  - _Fix:_ Require and pass a tenantId into storeEmbeddingVec(table, id, embedding, tenantId). Add a tenant filter to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(table.tenantId, tenantId)). Validate caller ownership before performing the update.
- **server/embeddings.ts:758** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} in backfillEmbeddingVecs() lacks a tenant_id constraint — allows writing to other tenants' rows.
  - _Fix:_ Include tenant scoping on UPDATEs (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update within a per-tenant backfill loop. Ensure the function knows the tenant for every row it updates.
- **server/embeddings.ts:775** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} in backfillEmbeddingVecs() lacks a tenant_id constraint — allows cross-tenant writes.
  - _Fix:_ Require tenant context for the backfill and add an AND tenant_id = ${tenantId} to the UPDATE (or use Drizzle .where(eq(table.tenantId, tenantId))). Ensure callers cannot trigger updates to rows they do not own.
- **server/firecrawl.ts:222** — UPDATE on tenant-scoped table 'scraped_pages' uses WHERE eq(scrapedPages.id, existing[0].id) but lacks an explicit tenant_id constraint. The update can modify another tenant's row if IDs overlap.
  - _Fix:_ Add tenant scoping to the UPDATE where clause. Example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include the tenant_id predicate on writes to tenant-scoped tables.
- **server/firecrawl.ts:353** — UPDATE on tenant-scoped table 'scraped_pages' inside the crawl loop uses WHERE eq(scrapedPages.id, existing[0].id) without tenant scoping. This permits cross-tenant modification.
  - _Fix:_ Require tenant scope on the UPDATE. Example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Ensure all UPDATE/DELETE statements target rows belonging to the caller's tenant.
- **server/google-drive.ts:560** — UPDATE projects SET driveFolderId = ... WHERE id = ${projectId} is executed without a tenant_id constraint. This is a cross-tenant write risk (can mutate another tenant's project).
  - _Fix:_ Add tenant scoping to the UPDATE. Example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). All writes to tenant-scoped tables must include the tenant filter.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — update by session id with no tenant_id constraint or ownership check. A caller-supplied/untrusted sessionId can cause cross-tenant modification.
  - _Fix:_ Require and enforce tenant scoping when updating sessions. Either include AND tenant_id = ${tenantId} in the WHERE clause, or first SELECT the presenter_sessions row and verify it belongs to the caller's tenant before performing the UPDATE. If this operation must run as admin, assert and document admin-only context explicitly.
- **server/heartbeat.ts:385** — SELECT * FROM event_log WHERE status = 'pending' ... has no tenant_id constraint and will return pending events across all tenants. Processing these rows without per-tenant scoping risks cross-tenant action and data exposure.
  - _Fix:_ Change the processing to operate per-tenant (e.g. query pending events for a specific tenant_id, or select distinct tenant_ids first and then fetch that tenant's events), or add an explicit WHERE tenant_id = ${expectedTenantId}. Ensure event processing functions assert and use tenant context before performing any tenant-scoped actions.
- **server/lib/scheduled-post-runner.ts:63** — SELECT (and consequent UPDATE) over scheduled_posts in the CTE is executed with no tenant_id constraint — this locks/updates rows across all tenants rather than scoping to a caller tenant.
  - _Fix:_ If this is intended to run as a system/admin process, make the admin intent explicit and add additional safeguards (audit logging, admin-only guard). Otherwise narrow the CTE to a single tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant so each read/update is explicitly constrained by tenant_id. Avoid implicit cross-tenant writes without explicit admin authorization.
- **server/memory-graph.ts:253** — INSERT into tenant-scoped table `memory_links` does not include `tenant_id` (or otherwise constrain the insert to the caller's tenant). This allows creation of cross-tenant links.
  - _Fix:_ Include tenant_id in the INSERT columns/values and bind it from the caller (e.g. INSERT INTO memory_links (tenant_id, source_memory_id, target_memory_id, ...) VALUES (${tenantId}, ${newMemoryId}, ${target.id}, ...)). Also ensure the function receives/validates the tenantId parameter and uses it for the insert.
- **server/minerva-planner.ts:548** — getPlan(planId, tenantId?) allows calling code to omit tenantId and executes: SELECT * FROM plans WHERE id = ${planId} — this reads a tenant-scoped row without any tenant_id constraint (IDOR risk).
  - _Fix:_ Make tenantId mandatory for external callers (do not rely on caller discipline). Either always include AND tenant_id = ${tenantId} in the query, or split into an internal-only helper (unexported) for trusted server-internal paths and an exported function that requires a tenantId and enforces the WHERE tenant_id filter. Log and/or throw if tenantId is omitted in untrusted contexts.
- **server/minerva-planner.ts:367** — decidePlan performs a CAS UPDATE on plans but conditionally appends tenant scoping: `${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}` — if tenantId is omitted the UPDATE runs without tenant_id guarding, allowing modification of any plan by id (cross-tenant write risk).
  - _Fix:_ Do not allow omission of tenantId for externally reachable flows. Require and validate tenantId for decidePlan, and always include AND tenant_id = ${tenantId} in the WHERE clause for the CAS UPDATE. If server-internal callers legitimately need to operate without tenant scoping, provide a separate internal API (clearly marked and not reachable from external routes) and audit its usage.
- **server/oauth-subscriptions.ts:906** — validateSubscriptionsOnStartup issues a SELECT from tenant-scoped table `oauth_subscriptions` with no tenant_id WHERE filter (reads all tenants' subscription rows). This exposes cross-tenant subscription data (including encrypted tokens) to a non-tenant-constrained query.
  - _Fix:_ Restrict reads to a specific tenant (add WHERE tenant_id = ${tenantId}) or, if a global startup sweep is required, explicitly iterate per-tenant in a trusted/admin-only loop: first obtain a vetted list of tenant IDs (from a safe source) and then run parameterized queries scoped to each tenant. Ensure only an internal, privileged process runs any cross-tenant operations and minimize returned columns (avoid returning token blobs unless strictly necessary). Consider Postgres RLS for defense in depth.
- **server/oauth-subscriptions.ts:915** — Query `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'` runs without any tenant_id restriction (returns rows across all tenants).
  - _Fix:_ Avoid unscoped queries against tenant-scoped tables. If you need to discover tenants, limit results to non-sensitive columns via a trusted admin path or build a vetted tenant-ID list elsewhere, then process each tenant with parameterized queries that include WHERE tenant_id = ${tenantId}. Make this code runnable only in an internal-admin context.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh issues `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant_id filter — iterates all tenants' subscriptions and acts on them.
  - _Fix:_ If the refresher must run across tenants, implement an explicit, auditable per-tenant loop: obtain a trusted list of tenant IDs, then for each tenant run parameterized queries scoped by tenant_id. Ensure the background job runs with appropriate internal privileges and limit fetched columns to what is strictly necessary. Consider adding RBAC checks or Postgres RLS to avoid accidental cross-tenant access.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions runs `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE` without tenant scoping, reading subscription metadata for all tenants in one query.
  - _Fix:_ Replace the global SELECT with an explicit, admin-only routine that enumerates tenant IDs from a trusted source and then performs per-tenant, parameterized queries (including WHERE tenant_id = ${tenantId}). Avoid single unscoped queries that touch tenant-scoped tables; add logging/auditing and limit returned token fields.
- **server/recurring-messages.ts:300** — UPDATE to tenant-scoped table `agent_knowledge` without tenant constraint: `UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}` does not include `AND tenant_id = ...`, allowing cross-tenant modification if ids are reused or tampered with.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}`) or use a storage-layer API that enforces ownership. Always include tenant_id in WHERE for writes to tenant-scoped tables to prevent cross-tenant writes.

- **server/reference-learner.ts:290** — UPDATE to tenant-scoped table `memory_entries` without tenant constraint: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}` omits tenant_id (and persona_id) in the WHERE clause, permitting modification of another tenant's memory entry by id.
  - _Fix:_ Include tenant ownership checks in the UPDATE (e.g. `WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}`) or perform a prior SELECT that verifies the row belongs to the tenant before updating. Prefer using a storage API that enforces tenant scoping for writes.
- **server/research-engine.ts:165** — UPDATE statement in cleanupZombieSessions updates research_sessions without any tenant_id constraint — this can mark running sessions as completed across all tenants.
  - _Fix:_ Scope the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant. If intended to be global, explicitly justify and gate it; otherwise perform the stale-check per-tenant or use a safe lease mechanism so one tenant cannot affect others.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge is executed without a tenant_id WHERE clause. agent_knowledge is tenant-scoped elsewhere, so this will delete entries across all tenants.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()), or otherwise ensure the purge runs per-tenant with the correct tenantId parameter.
- **server/routes/projects.ts:185** — INSERT into tenant-scoped table `project_notes` does not include tenant_id — INSERT INTO project_notes (project_id, note, author) ...
  - _Fix:_ Include tenant_id on insert (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ...)) or use Drizzle's insert API with tenantId in the values; ensure tenantId is the caller's tenant and validated before the INSERT.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` does not include tenant_id — INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...
  - _Fix:_ Include tenant_id on insert (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${projectId}, ${tenantId}, ...)) or use db.insert(...).values({ tenantId, ... }) so every persisted file row is tied to the caller's tenant.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload path) does not include tenant_id — INSERT INTO project_files (...) VALUES (...)
  - _Fix:_ Same fix as above: add tenant_id to the inserted columns/values (tenantId from request session) or use Drizzle insert with tenantId in values; never omit tenant_id on tenant-scoped tables.
- **server/self-improvement.ts:401** — runExperiment updates the tenant-scoped `experiments` row using only experiments.id in the WHERE clause (WHERE eq(experiments.id, experiment.id)). There is no tenant_id constraint, allowing modification of another tenant's row if IDs collide or are guessed.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (for example: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) or use a parameterized condition that verifies the experiment belongs to the current tenant before updating. Always validate ownership for writes to tenant-scoped tables.
- **server/self-improvement.ts:415** — In the error-handling path runExperiment sets status='error' using an UPDATE that filters only by experiments.id (WHERE eq(experiments.id, experiment.id)). This write lacks a tenant_id constraint and can modify another tenant's data.
  - _Fix:_ Add the tenant_id check to the WHERE clause in all UPDATEs (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))). Fail closed (throw or log) if tenantId is not available/valid.
- **server/self-reflection.ts:366** — storeLesson updates memory_entries by id only (UPDATE memory_entries SET ... WHERE id = <id>) without also constraining tenant_id. This allows updating memory entries belonging to other tenants.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) so the update can only affect rows owned by the caller's tenant. Validate the tenant context before performing the write.
- **server/storage.ts:399** — attachCitations updates tenant-scoped `messages` (UPDATE messages SET citations=...) using only id in the WHERE clause and no tenant_id constraint — this can overwrite another tenant's row.
  - _Fix:_ Require and pass tenantId into attachCitations and include it in the WHERE clause (WHERE id = ${messageId} AND tenant_id = ${tenantId}). Alternatively, re-validate message ownership prior to updating (SELECT tenant_id and compare) and fail closed on mismatch.
- **server/storage.ts:841** — archiveExpiredMemories executes a raw UPDATE on tenant-scoped `memory_entries` (UPDATE memory_entries SET status='archived' WHERE ... AND expires_at <= NOW()) with no tenant_id constraint — this will modify all tenants' rows indiscriminately.
  - _Fix:_ Make tenant scoping explicit: either accept a tenantId/TenantScope and include tenant filter (WHERE tenant_id = ${tenantId} AND ...), or require an explicit ALL_TENANTS sentinel with strict admin-only guard documented and enforced. Failing to scope background mass-updates is a severe cross-tenant risk.
- **server/storage.ts:857** — archiveStaleMemories executes a raw UPDATE on tenant-scoped `memory_entries` without tenant_id filtering (UPDATE memory_entries SET status='archived' WHERE ...), affecting all tenants.
  - _Fix:_ As with other archival jobs, require an explicit tenant scope parameter or explicit, audited ALL_TENANTS sentinel + admin-only protection. Add tenant filter to the UPDATE to avoid cross-tenant mutation.
- **server/storage.ts:1311** — getAllDataForExport performs broad SELECTs over many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant/heartbeat tables, etc.) with no tenant_id filtering — this is a massive unscoped data export risk.
  - _Fix:_ Restrict this API to authenticated admin only and require an explicit tenantId to export a single tenant, or require an explicit ALL_TENANTS sentinel that is only usable by a high-privilege admin flow. Add runtime checks and audit logging, and ensure the function cannot be invoked by non-admin callers. Prefer per-tenant exports by default.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` (updating implementation) uses only eq(customTools.id, tool.id) with NO tenant_id constraint. This can modify another tenant's row if IDs are guessed or shared.
  - _Fix:_ Always include the tenant constraint in UPDATE statements (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))). Verify the caller's tenant owns the row before mutating.
- **server/tool-learning.ts:316** — DELETE on tenant-scoped table `custom_tools` uses only eq(customTools.id, tool.id) with NO tenant_id constraint. This can delete another tenant's tool row (tenant-escape / data-loss risk).
  - _Fix:_ Include tenant scoping in the DELETE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) and ensure the operation only affects rows owned by the caller's tenant.
- **server/tool-learning.ts:365** — UPDATE on tenant-scoped table `custom_tools` (incrementing usageCount) uses only eq(customTools.id, tool.id) without a tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ Restrict UPDATEs to the tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or perform a pre-check that the tool's tenantId matches the caller's tenant before updating.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys performed without any tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). This returns provider API key data across all tenants.
  - _Fix:_ Constrain the query to the caller tenant (e.g. add WHERE tenant_id = ${tenantId}). If this is intended only for an admin audit path, make the admin intent explicit and/or limit returned columns (avoid returning key material) and log/authorize the admin action. Prefer parameterized tenant-scoped queries rather than an unrestricted aggregate over provider_keys.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM provider_keys executes without tenant_id filtering. This enumerates provider key configuration across tenants.
  - _Fix:_ Add tenant scoping, e.g. WHERE tenant_id = ${tenantId}, or explicitly document and gate this as an admin-only cross-tenant operation with appropriate auditing. Avoid unscoped queries on tenant-scoped secret/key tables.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} is executed without also constraining tenant_id. This is a write to a tenant-scoped table that could modify another tenant's ledger row if reservationId is mis-bound.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or first SELECT and verify the row belongs to the expected tenant within the same transaction before performing the UPDATE. Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id filtering.
- **server/venture-discovery/budget.ts:170** — UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'venture_discovery:released' WHERE id = ${reservationId} is executed without constraining tenant_id. This releases reservations by id only and can affect rows belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or verify ownership in the same DB transaction before updating. Always include tenant_id in write queries on tenant-scoped tables.
- **server/webhookHandlers.ts:428** — handleSubscriptionActivation updates tenants row using tenantId taken directly from Stripe session.metadata without verifying that the Stripe subscription/customer actually belongs to that tenant. An attacker could craft metadata.tenantId to mutate another tenant's billing/plan state.
  - _Fix:_ Do not trust metadata.tenantId from inbound Stripe events. Resolve the tenant via a server-side mapping (e.g. look up tenant by subscription.customer / stripe_customer_id) and only mutate the row when the Stripe customer/subscription is provably bound to that tenant. If metadata must be supported, additionally verify the tenant's existing stripe_customer_id matches the subscription.customer (or refuse to mutate if no binding exists). Always perform an ownership check before any UPDATE to tenant-scoped rows.
- **server/webhookHandlers.ts:1083** — handleSubscriptionCancelled performs UPDATE tenants SET plan='trial' WHERE id = ${tenantId} using tenantId taken from subscription.metadata without verifying ownership. Metadata is attacker-controllable, so this can be used to downgrade arbitrary tenants.
  - _Fix:_ Do not accept metadata.tenantId as authoritative. Determine tenant identity by server-side resolution (e.g. SELECT tenant WHERE stripe_customer_id = subscription.customer) and only update that tenant. If subscription.customer is missing, require additional verification or skip mutating tenant rows. Add explicit checks that the subscription/customer in Stripe is bound to the tenant before performing any tenant-scoped UPDATE.
- **server/webhookHandlers.ts:1158** — handleSubscriptionUpdated executes UPDATE tenants SET plan = ${plan} WHERE id = ${tenantId} where tenantId may originate from subscription.metadata. The handler verifies price information but does not ensure the subscription/customer actually maps to the claimed tenant, allowing metadata spoofing to change other tenants' plans.
  - _Fix:_ Resolve tenant identity using server-controlled mappings (e.g. find tenant by stripe_customer_id from the subscription) or validate that the tenant's stored stripe_customer_id matches subscription.customer before updating. Never mutate tenant-scoped rows based solely on request-supplied foreign ids in metadata without an explicit ownership/authorization check.

### HIGH (196)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` performed without a tenant_id constraint (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across tenants.
  - _Fix:_ Scope the query to a single tenant (WHERE tenant_id = <tenant>), or iterate known tenants and run a tenant-scoped query per tenant. If this heartbeat must operate globally, ensure explicit and auditable cross-tenant authorization and avoid returning tenant data to unauthorized contexts.
- **server/agent-activity.ts:60** — SELECT from `personas` by persona id is missing tenant scoping (SELECT { emoji, catchphrase } FROM personas WHERE id = personaId). This reads a persona row without validating that the persona belongs to the caller's tenant.
  - _Fix:_ Include tenant scoping when reading persona rows (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or otherwise verify the caller's tenant owns the persona before returning data.
- **server/agent-desk.ts:297** — Query selects all `personas` with no tenant filter (db.execute(sql`SELECT id, name FROM personas`)). This reads persona rows across tenants when building desks overview.
  - _Fix:_ Restrict the SELECT to the tenant (e.g. WHERE tenant_id = ${tenantId}), or otherwise validate ownership before using persona rows to build the overview.
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` omits tenant constraint: UPDATE agent_evals SET ... WHERE id = ${evalId}. A caller could update an eval row that belongs to another tenant by id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) to ensure the row being updated belongs to the caller's tenant.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` performed without tenant filter in retryPendingMinervaRouting (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ... LIMIT 50). This reads across tenants.
  - _Fix:_ Scope the sweep per-tenant (add WHERE tenant_id = ${tenantId} or iterate tenants) or otherwise perform explicit tenant-scoped queries rather than global selects/updates.
- **server/agentic-engines.ts:344** — In gatherOperationalData a query on `heartbeat_logs` lacks tenant scoping: SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'. This aggregates heartbeat logs across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or perform tenant-scoped aggregation to avoid reading or aggregating other tenants' telemetry.
- **server/agentic-engines.ts:377** — SELECT from `personas` is missing tenant scope: SELECT name, role FROM personas WHERE is_active = true LIMIT 14. This reads personas across tenants.
  - _Fix:_ Restrict persona reads to the tenant (WHERE tenant_id = ${tenantId}) or otherwise ensure only tenant-owned personas are returned.
- **server/agentic-engines.ts:451** — SELECT recent logs from `heartbeat_logs` without tenant scoping (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10). This reads logs across tenants.
  - _Fix:_ Filter heartbeat_logs by tenant_id = ${tenantId} (or perform a tenant-scoped query) so only the requested tenant's logs are read.
- **server/agentic-engines.ts:519** — SELECT on `heartbeat_logs` in runOptimizationEngine is not tenant-scoped (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This aggregates heartbeat logs across all tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the query WHERE clause or otherwise perform tenant-scoped aggregation to prevent cross-tenant data access.
- **server/agentic-engines.ts:533** — SELECT from `heartbeat_tasks` omits tenant filter (SELECT name, cron_expression, is_enabled, last_run_at, run_all FROM research_schedules WHERE tenant_id = ${tenantId}) — note: the following taskConfig SELECT (SELECT name,... FROM heartbeat_tasks WHERE enabled = true ORDER BY name) lacks tenant scoping. This reads heartbeat task configurations across tenants.
  - _Fix:_ Include tenant scoping for heartbeat_tasks (WHERE tenant_id = ${tenantId}) so task configuration reads are limited to the tenant's rows.
- **server/agentic-features.ts:563** — UPDATE lead_enrichments omits tenant scoping: UPDATE lead_enrichments SET icp_score = ..., notes = ... WHERE id = ${lead.id}. This can update another tenant's row by id alone.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) to ensure the updated row belongs to the caller's tenant.
- **server/agentic-features.ts:638** — SELECT existing enrollments omits tenant scoping: SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'. This check can be influenced by rows from other tenants.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the query to ensure the existence check is tenant-scoped before inserting a new enrollment.
- **server/agentic-features.ts:797** — UPDATE outreach_enrollments performed without tenant scoping: UPDATE outreach_enrollments SET reply_classification = ..., status = ... WHERE id = ${row.id}. The update does not ensure the enrollment belongs to the caller's tenant.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to prevent cross-tenant updates.
- **server/agentic/resume-worker.ts:66** — SELECT query scans tenant-scoped tables (agent_runs, agent_approvals) with no tenant_id constraint — the worker reads rows across all tenants.
  - _Fix:_ Constrain the query to a single tenant (e.g. WHERE r.tenant_id = ${tenantId}) or otherwise explicitly document and gate cross-tenant access. Prefer a per-tenant sweep or an admin-only path and ensure the tenant is passed as a parameter to the function rather than returning global rows.
- **server/agentic/runs.ts:34** — appendStep() updates the tenant-scoped agent_runs table but the WHERE clause only filters by run id (eq(agentRuns.id, runId)) — no tenant_id constraint, allowing cross-tenant updates if runId is known.
  - _Fix:_ Require and use the tenantId when updating agent_runs (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Add tenantId parameter to appendStep() and propagate it from callers, or fetch and assert the run's tenant before mutating.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs but restricts only by run id (eq(agentRuns.id, runId)) and does not include a tenant_id filter, permitting cross-tenant state mutation.
  - _Fix:_ Add tenant-scoping to the update (e.g. accept tenantId param and .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))).
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs using only run id in the WHERE clause (eq(agentRuns.id, runId)) with no tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (accept tenantId param) to enforce tenant isolation on updates.
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs (status/result/completedAt) but the WHERE clause filters only by run id (eq(agentRuns.id, runId)) — missing tenant_id constraint.
  - _Fix:_ Restrict the update to the caller's tenant (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Modify the function signature to accept tenantId where appropriate.
- **server/agentic/runs.ts:88** — failRun() updates agent_runs to mark a run failed but uses only run id in the WHERE clause (eq(agentRuns.id, runId)) with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the WHERE clause (add tenantId parameter and use eq(agentRuns.tenantId, tenantId)) so runs from other tenants cannot be modified.
- **server/agentic/runs.ts:97** — pauseRun() updates agent_runs (status/state) but the WHERE clause filters only by run id (eq(agentRuns.id, runId)) — missing tenant_id constraint.
  - _Fix:_ Add tenantId to the function and include eq(agentRuns.tenantId, tenantId) in the WHERE clause to enforce per-tenant updates.
- **server/agentic/wake-scheduler.ts:142** — runDueWakes performs an UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) — the inner SELECT does not filter by tenant_id. This claims/updates rows across all tenants in a tenant-scoped table.
  - _Fix:_ Either scope the claim to a single tenant (add WHERE tenant_id = <tenantId>) and run per-tenant jobs, or include an explicit tenant_id filter (or other application-level per-tenant iteration) when selecting rows to claim. Never select/claim across all tenants without an explicit, trusted tenant constraint.
- **server/auto-asset-capture.ts:184** — backfillProjectAssets selects conversations across all tenants: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ... — no tenant restriction. This reads tenant-scoped rows across tenants.
  - _Fix:_ Restrict the scan to a single tenant per run (add WHERE tenant_id = ${tenantId}) or implement a safe per-tenant scheduling loop that explicitly iterates tenants and includes tenant_id in all SELECTs. Avoid unfiltered scans across tenants against tenant-scoped tables.
- **server/auto-asset-capture.ts:207** — When backfilling, messages are queried using conversation_id only: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' — there is no tenant_id filter on messages (a tenant-scoped table).
  - _Fix:_ Include tenant_id in the messages SELECT (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id} AND role = 'assistant'), or ensure the outer flow enforces and passes a trusted tenantId and use it in the query. All reads against tenant-scoped tables must include an explicit tenant_id constraint.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id. Because there is no DB-level RLS or FK cascade enforcement, this can create line-items that are not explicitly tied to the caller's tenant and may allow cross-tenant data association or orphaned rows.
  - _Fix:_ Include tenant_id on invoice_items inserts (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or otherwise enforce tenant ownership (lookup the parent invoice's tenant_id and pass it). Ensure all tenant-scoped tables always receive tenant_id on INSERT and that subsequent reads/updates filter by tenant_id.
- **server/build-video-from-brief.ts:714** — buildVideoFromBrief accepts an input.projectId and passes it into the StartVideoJob input (and also earlier exposes it to spawned child processes) without verifying the caller owns that project. A caller could supply another tenant's project_id and cause cross-tenant association or actions.
  - _Fix:_ Validate ownership of any caller-supplied foreign id (projectId) before using it: SELECT the project by id AND tenant_id = input.tenantId (or otherwise ensure the project belongs to input.tenantId) and fail closed if not found. Also avoid propagating unvalidated foreign ids into child process env or downstream jobs; only pass the validated tenant-bound id.
- **server/capability-review.ts:491** — UPDATE against tenant-scoped table `capability_reviews` is executed without constraining by tenant_id (WHERE id = ${pending.reviewId} AND reused = false). This allows cross-tenant mutation of capability_reviews rows by id.
  - _Fix:_ Include tenant scoping in the UPDATE and verify ownership before writing. For example, persist tenantId alongside the pending entry and execute: UPDATE capability_reviews SET reused = true, reused_capability = ${...} WHERE id = ${pending.reviewId} AND tenant_id = ${expectedTenantId} AND reused = false. Alternatively, read the row first and validate its tenant_id matches the caller's tenant before updating.
- **server/channel-routing.ts:31** — listChannelRoutes SELECT joins channel_routes to personas with no tenant constraint. If personas (or channel_routes) are tenant-scoped this query can return data across tenants.
  - _Fix:_ Require a tenantId parameter and add WHERE clauses constraining both channel_routes and personas (e.g. WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}). Ensure the schema includes tenant_id and that persona ownership is verified.
- **server/channel-routing.ts:48** — setChannelRoute performs DELETE and INSERT/ON CONFLICT against channel_routes without scoping by tenant and the INSERT does not set tenant_id. If the table is meant to be tenant-scoped, this allows cross-tenant modification and orphaned global routing entries.
  - _Fix:_ Require tenantId as a parameter to setChannelRoute, include tenant_id in the INSERT/VALUES, and add tenant_id to the WHERE clause for DELETE/UPDATE (e.g. WHERE channel = ${channel} AND tenant_id = ${tenantId}). Update the table schema to include tenant_id NOT NULL.
- **server/channel-routing.ts:63** — getPersonaForChannel queries channel_routes for persona_id by channel without any tenant filter. This can leak or return a persona from a different tenant.
  - _Fix:_ Require tenantId as a parameter and add AND tenant_id = ${tenantId} to the WHERE clause. Ensure channel_routes has tenant_id and that returned persona_id is validated to belong to the tenant before use.
- **server/channels/kernel.ts:177** — dispatchInbound calls processMessage(conversationId, ...) using a conversationId returned from the adapter-provided resolveConversation callback without verifying the conversation belongs to turn.tenantId. This can allow cross-tenant access if resolveConversation or upstream adapters are compromised or buggy.
  - _Fix:_ Either make resolveConversation responsible for returning only a tenant-validated conversationId (document and enforce this contract) or have dispatchInbound explicitly verify ownership (e.g. load the conversation row and check conversation.tenant_id === turn.tenantId) before calling processMessage. Alternatively pass tenantId into processMessage and enforce tenant-scoped checks inside chat-engine.
- **server/chat-engine.ts:4408** — Call to storage.attachCitations(...) is made without passing a tenant identifier. If attachCitations performs INSERT/UPDATE against a tenant-scoped table, the write may not constrain the row to the caller's tenant and could enable cross-tenant association or tampering.
  - _Fix:_ Change attachCitations to accept an explicit tenantId (or validate message ownership) and pass tenantIdForScope here. Ensure the storage implementation includes tenant_id in INSERTs/WHERE and/or verifies the message row belongs to the caller's tenant before writing.
- **server/chat-engine.ts:4214** — getOrCreateTaskState(conversationId, ...) is invoked without a tenant context. If that helper reads/writes task state rows keyed by conversationId, it may not verify the tenant ownership, allowing cross-tenant access/modification.
  - _Fix:_ Pass the tenant id into getOrCreateTaskState (e.g. getOrCreateTaskState(conversationId, content, tenantId)) or have the function explicitly verify the conversation's tenant ownership before any SELECT/INSERT/UPDATE. All DB statements must include WHERE tenant_id = <tenant> or equivalent Drizzle guard.
- **server/chat-engine.ts:4080** — recordToolExecution(conversationId, toolName, ...) is called without a tenant identifier. If this function writes to a tenant-scoped table (tool execution logs tied to conversations), it may perform writes/updates without scoping to tenant_id.
  - _Fix:_ Modify recordToolExecution to accept and require tenantId, and pass tenantId (or tenantIdForScope) here. Ensure the implementation enforces tenant scoping on ALL DB operations (WHERE tenant_id = ... or setting tenant_id on INSERTs after validating ownership).
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` (scanAndEscalate) has no tenant_id constraint — it selects overdue commitments across all tenants. Unscoped reads of tenant-scoped data can expose tenant-specific information and risk cross-tenant data access.
  - _Fix:_ If multi-tenant enumeration is required for an admin heartbeat, restrict this code to a well-audited, admin-only context and minimize returned data (redact tenant content). Otherwise, restrict the SELECT by tenant_id = ${tenantId} or perform per-tenant passes where the tenant is explicitly supplied and validated. Ensure any subsequent updates/actions use tenant_id in WHERE clauses (they already do) and that the initial read is authorized for the executing context.
- **server/crews-engine.ts:401** — addCrewTask inserts task.agent_id and context_task_ids from caller-supplied params without verifying those referenced rows belong to the caller's tenant. A malicious caller could reference another tenant's agent or tasks.
  - _Fix:_ Before inserting, validate any foreign ids from the caller (e.g. params.agentId and each params.contextTaskIds) by SELECTing the referenced row(s) with AND tenant_id = ${tenantId}. Reject or null them if they don't exist / don't belong to the tenant. Alternatively perform the INSERT inside a withTenantTx and explicitly verify ownership of foreign rows first.
- **server/data-protection.ts:359** — backupTenantDataToDrive queries project_notes using WHERE project_id = ${proj.id} with NO tenant_id constraint. This reads another table (project_notes) using a foreign id without ensuring the row belongs to the tenant.
  - _Fix:_ Scope the query to the tenant (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or join back to the projects table and require p.tenant_id = ${tenantId} to ensure only same-tenant notes are returned.
- **server/data-protection.ts:371** — backupTenantDataToDrive queries project_files using WHERE project_id = ${proj.id} with NO tenant_id constraint. Child records are read by foreign id without verifying tenant ownership.
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or join to projects and require the project's tenant_id matches the tenant parameter to prevent cross-tenant reads.
- **server/data-protection.ts:403** — backupTenantDataToDrive runs SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} without scoping by tenant_id. This can read another tenant's messages for the same conversation id.
  - _Fix:_ Always include AND tenant_id = ${tenantId} when selecting messages by conversation_id in a tenant-scoped backup. For example: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}.
- **server/deep-interview.ts:337** — saveInterviewToKnowledge performs INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ... but does not set tenant_id on the inserted row. If project_notes is tenant-scoped (tenant_id NOT NULL/no default), this either misattributes ownership or violates the invariant that INSERTs must pass tenant_id explicitly.
  - _Fix:_ Include tenant_id in the insert (e.g. INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() ... ) or explicitly set ${state.tenantId} so persisted notes are recorded under the correct tenant and no insert omits tenant_id.
- **server/delivery-pipeline.ts:203** — updateDeliveryLog performs an UPDATE on delivery_logs without constraining by tenant_id (WHERE eq(deliveryLogs.id, id) only). This allows a caller with a deliveryId to update another tenant's delivery row.
  - _Fix:_ Add tenant scoping to the UPDATE. Either accept a tenantId parameter and use .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))) or otherwise ensure the current caller's tenant is verified and included in the WHERE clause before applying updates.
- **server/delivery-pipeline.ts:1071** — getDeliveryByStripePayment selects from delivery_logs using stripePaymentId without any tenant filter. This can return another tenant's delivery row for the same payment id.
  - _Fix:_ Require and pass a tenantId (or otherwise scope to the caller's tenant) and include WHERE eq(deliveryLogs.tenantId, tenantId). If the lookup must be global, ensure the caller is trusted and document the risk; preferably make the API accept tenantId to enforce isolation.
- **server/dfy-intake.ts:93** — submitDfyIntake performs an UPDATE on dfy_intake_forms (.where(eq(dfyIntakeForms.id, form.id))) with no tenant_id constraint. This allows updating a form row belonging to another tenant by id.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId))) or pass the expected tenantId and validate it before updating.
- **server/doc-collections.ts:165** — addDocument deletes doc_chunks with DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but omits tenant_id. This can remove chunks belonging to other tenants' collections having the same collection_id/doc_path (or cause unintended cross-tenant effects).
  - _Fix:_ Include tenant scoping in the DELETE, e.g. WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks embedding using WHERE id = ${row.id} without constraining tenant_id. Updating by id alone may modify another tenant's row if ids are not globally unique or if an attacker can influence ids.
  - _Fix:_ Add tenant constraint to the UPDATE, e.g. UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. If the code can't access tenantId at that point, thread it through from the caller.
- **server/dream-consolidation.ts:83** — getRecentSessionSummaries selects messages for a conversation with SELECT role, content FROM messages WHERE conversation_id = ${conv.id} but does not include tenant_id. messages is a tenant-scoped resource — reading by conversation_id without tenant scoping can return another tenant's messages if conversation ids are not globally protected.
  - _Fix:_ Include tenant scoping in the messages query, e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}, or join via the conversations table and assert the conversation's tenant before selecting messages.
- **server/email.ts:528** — backfillEmptyBodies updates inbox_messages with UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} but omits tenant_id in the WHERE clause. This can update another tenant's row by id.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${row.id} AND tenant_id = ${tenantId} (or include inbox_id) so updates cannot affect rows from other tenants. Thread tenantId into this code path and verify it before performing writes.
- **server/embeddings.ts:720** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings() has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Scope backfill queries per-tenant (e.g. iterate per-tenant, or accept a tenantId parameter). Add WHERE tenant_id = ${tenantId} to the SELECT or otherwise constrain the read to the caller's tenant to avoid leaking cross-tenant data.
- **server/embeddings.ts:748** — SELECT FROM memory_entries in backfillEmbeddingVecs() has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ Restrict the backfill SELECT to a specific tenant (e.g. accept tenantId, add WHERE tenant_id = ${tenantId}) or perform the backfill per-tenant. Never run cross-tenant selects on tenant-scoped tables.
- **server/embeddings.ts:764** — SELECT FROM agent_knowledge in backfillEmbeddingVecs() has no tenant_id filter — reads rows across all tenants.
  - _Fix:_ As with other backfills, constrain reads to a tenant (WHERE tenant_id = ${tenantId}) or perform per-tenant backfills so no cross-tenant data is accessed.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' without any tenant_id constraint (SELECT ... FROM agent_runs WHERE status IN ...). This reads in-flight runs for all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (or iterate tenants explicitly). e.g. WHERE tenant_id = ${tenantId} AND status IN (...) — or only select rows for a specific tenant if this boot-time function should be tenant-scoped.
- **server/google-drive.ts:547** — SELECT project row by id (FROM projects WHERE id = ${projectId}) without constraining tenant_id. This reads another tenant's project row before the code performs an ownership check in application logic.
  - _Fix:_ Perform the tenant scoping in the SQL itself (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise avoid selecting rows across tenants. Prefer including eq(projects.tenantId, tenantId) in the query to prevent cross-tenant reads.
- **server/google-drive.ts:827** — SELECT project by id in uploadAndShare (FROM projects WHERE id = ${params.projectId}) without tenant_id in the WHERE clause. The code verifies ownership after selecting, but the SELECT itself reads a potentially foreign tenant row.
  - _Fix:_ Include tenant scoping at query time (WHERE id = ${projectId} AND tenant_id = ${expectedTenantId}) so the DB only returns rows for the caller's tenant. Avoid selecting tenant-scoped rows by id alone when the id is caller-supplied.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images does not set tenant_id; session_id is coming from an external presenter service response and not validated here.
  - _Fix:_ Make presenter_slide_images tenant-scoped by including tenant_id in the INSERT (e.g. INSERT ... (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)), or validate ownership first (SELECT presenter_sessions WHERE id=${sessionId} AND tenant_id=${tenantId}) and only then insert. Do NOT rely solely on session_id from the external service without validation.
- **server/health-audit.ts:314** — SELECT from code_proposals filters only by status/created_at, but contains no tenant_id constraint. This reads tenant-scoped data globally and the subsequent UPDATE (archival) operates without tenant scoping.
  - _Fix:_ Scope the query to a tenant (e.g. WHERE tenant_id = ${tenantId} AND ...), or make the audit explicitly an admin-only operation that asserts admin privileges. For archival/UPDATE operations, include tenant_id in the WHERE clause to avoid touching other tenants' rows.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}) — no tenant_id constraint. This can update proposals from any tenant if ids are shared across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or resolve ids per-tenant and only update rows that match the expected tenant. If this is intended as a global admin sweep, require explicit admin context and checks.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL ... LIMIT 100 — no tenant_id constraint. heartbeat_tasks appear to be tenant-scoped in other code paths and this may disable tasks across tenants.
  - _Fix:_ Limit the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or otherwise explicitly require/verify admin context. Ensure any subsequent UPDATEs (disabling tasks) also include tenant_id to avoid affecting other tenants' tasks.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — no tenant_id constraint. This will disable heartbeat tasks across tenants sharing IDs.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or fetch and validate each task belongs to the tenant before disabling. If this action must be global, require an explicit admin-only guard and audit/logging.
- **server/heartbeat.ts:53** — Raw SELECT from research_schedules has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads schedule rows across all tenants without an explicit tenant filter.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant (select active tenant ids then query per tenant). If the operation must run cross-tenant, make the cross-tenant intent explicit and audit-safe (e.g. use a dedicated admin path with explicit ALL_TENANTS semantics).
- **server/heartbeat.ts:103** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} lacks a tenant_id constraint. The UPDATE relies solely on id without verifying the row belongs to the expected tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via a storage helper that asserts/uses tenant context (assertTenantContext) before writing.
- **server/heartbeat.ts:108** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} is missing a tenant_id constraint.
  - _Fix:_ Limit the UPDATE to the correct tenant by adding AND tenant_id = ${sched.tenant_id}, or use a storage API that enforces the tenant context before performing the write.
- **server/heartbeat.ts:273** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} updates a tenant-scoped table row by id only, without verifying tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use a storage helper (e.g. storage.updateHeartbeatTask) that enforces tenant context to prevent accidental cross-tenant updates.
- **server/heartbeat.ts:277** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} modifies a tenant-scoped table row without a tenant_id constraint.
  - _Fix:_ Add a tenant filter (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or delegate the update to a storage-layer function that asserts the task's tenant context (assertTenantContext) before applying changes.
- **server/heartbeat.ts:1670** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id constraint and will read heartbeat log rows across tenants.
  - _Fix:_ Restrict the query to the task's tenant (e.g. WHERE task_name = ... AND status = 'success' AND tenant_id = ${tenantId}) or otherwise ensure the selection is performed in an asserted admin context with explicit ALL_TENANTS semantics. Prefer using storage helpers that enforce tenant context.
- **server/heartbeat.ts:2764** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}), or call a storage helper that enforces tenant ownership/tenant context before updating approval_status.
- **server/job-queue.ts:93** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id constraint — the queue UPDATE/RETURNING touches jobs across all tenants.
  - _Fix:_ If the intent is per-tenant isolation, add a tenant_id parameter and include WHERE tenant_id = ${tenantId} in the inner SELECT/outer UPDATE, or partition claims per-tenant. If a global system queue is intended, document and restrict access to admin/system-only code paths and consider adding explicit ACL checks so tenant-scoped mutations cannot be made by callers lacking appropriate privileges.
- **server/job-queue.ts:131** — reclaimExpiredLeases updates agent_jobs (marking running->failed_terminal or requeueing) without any tenant_id constraint — this mutates jobs for all tenants indiscriminately.
  - _Fix:_ Scope the UPDATEs by tenant where appropriate (accept tenantId param) or ensure this reclaiming logic is explicitly authorized to run cross-tenant (documented, audited). Prefer per-tenant reclaiming or add ownership checks before terminaling/requeuing rows.
- **server/job-queue.ts:172** — completeJob updates an agent_jobs row (setting status='succeeded') by id/attempts only, with no tenant_id check — a stale or malicious caller could complete jobs belonging to other tenants.
  - _Fix:_ Require and verify tenant ownership when marking a job complete (e.g. UPDATE ... WHERE id = ${id} AND tenant_id = ${tenantId} AND attempts = ${attempts}) or perform an earlier lookup that verifies the job. If the worker legitimately runs across tenants, run these writes under a trusted system context and audit them tightly.
- **server/job-queue.ts:220** — failJob starts by SELECTing max_attempts FROM agent_jobs WHERE id = ${id} with no tenant filter, and later UPDATEs the job (failed vs requeue) without enforcing tenant ownership — allows inspection/mutation of other-tenant jobs.
  - _Fix:_ When reading or mutating agent_jobs by id, include tenant_id in the WHERE clause or pre-validate the job's tenant against the caller's tenant/context. Alternatively, require the caller to provide a tenantId and enforce it in all SELECT/UPDATE statements to prevent cross-tenant mutation.
- **server/job-queue.ts:273** — cancelJob updates agent_jobs WHERE id = ${id} ... with no tenant_id check — cancellation can affect jobs owned by other tenants.
  - _Fix:_ Add tenant isolation to cancellation (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or require an admin authorization check for cross-tenant cancellations. Ensure all operator actions that mutate tenant-scoped rows validate tenant ownership.
- **server/job-queue.ts:281** — retryJob resets state for a job id with no tenant constraint in the UPDATE — allows retrying jobs belonging to other tenants without ownership verification.
  - _Fix:_ Include tenant_id in the WHERE clause or verify ownership before performing the retry. If cross-tenant admin retries are required, gate the API behind a privileged path and log/audit the operation.
- **server/job-queue.ts:320** — listJobs queries agent_jobs without enforcing a tenant_id by default (the tenant filter is optional). When called without tenantId this will return jobs across all tenants — information disclosure risk.
  - _Fix:_ Make tenantId required for non-admin callers or automatically scope results to the caller's tenant. If an admin view needs cross-tenant visibility, require an explicit admin flag and server-side authorization checks before returning cross-tenant rows.
- **server/lib/agent-trace.ts:124** — UPDATE to tenant-scoped table agent_trace_spans omits tenant_id predicate (UPDATE ... WHERE span_id = ${spanId}). persistSpanClose can update rows across tenants if a span_id is known, violating tenant isolation.
  - _Fix:_ Include tenant_id in the UPDATE predicate. Pass the tenantId into persistSpanClose (or look it up in a trusted way) and change the WHERE to include AND tenant_id = ${tenantId} (or use a parameterized .where(eq(agent_trace_spans.tenantId, tenantId), eq(agent_trace_spans.spanId, spanId))). Failing that, first SELECT the row and assert its tenant_id matches currentTenantId() before performing the UPDATE.
- **server/lib/bwb-job-progress.ts:74** — scope() may omit the tenant predicate when BWB_TENANT_ID is not available, causing subsequent SELECT/UPDATEs (e.g. setBwbPhase, updateBwbChapters, completeBwbJob, bumpBwbHeartbeat, failBwbJob) to run without WHERE tenant_id = <tenant>. This allows writes to tenant-scoped video_jobs rows based only on jobId, breaking the required tenant scoping.
  - _Fix:_ Enforce tenant scoping on every DB operation: require a tenantId (from args or environment) and fail or no-op when missing rather than dropping the tenant predicate. Change scope() to throw or return an unusable predicate when the tenant is unknown, and update callers to handle/propagate tenantId explicitly. Alternatively always include tenant_id in the WHERE and abort the operation when tenantId cannot be determined.
- **server/lib/ideabrowser-backfill.ts:49** — Query against tenant-scoped table `projects` lacks a tenant_id constraint — it scans all tenants. The COUNT query (SELECT tenant_id, count(*) ... FROM projects WHERE tags && ... GROUP BY tenant_id) reads tenant-scoped data for every tenant in the DB, violating the invariant that every read must be constrained to the caller's tenant.
  - _Fix:_ Restrict the query to the snapshot's tenant set (or to the single caller tenant). For example, compute the list of relevant tenant IDs from the snapshot and change the query to: SELECT tenant_id, count(*)::int AS c FROM projects WHERE tenant_id = ANY(${tenantIdsLiteral}::int[]) AND tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id. If the intent is a per-tenant short-circuit, run the count per tenant or include an explicit WHERE tenant_id = ${tenantId} to avoid cross-tenant access.
- **server/lib/inbox-ingest.ts:507** — UPDATE to tenant-scoped table `inbox_classifications` performs WHERE inbox_message_id = ${storeResult.id} without constraining by tenant_id. This allows cross-tenant modification if an inbox_message_id from another tenant is used or guessed.
  - _Fix:_ Include the tenant filter in the UPDATE (e.g. add AND tenant_id = ${tenantId} to the WHERE clause) or otherwise verify the inbox_classifications row belongs to the caller's tenant before updating. Prefer a WHERE clause like `WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}` to enforce tenant isolation.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions executes a cross-tenant aggregate SELECT on deliverable_grade_events without constraining by tenant_id (SELECT tenant_id, format, count(*)... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format). This reads tenant-scoped data for all tenants in one query, violating the application's tenant-isolation invariant.
  - _Fix:_ Restrict reads to a single tenant at a time. For example, iterate over tenant ids and run the aggregation per-tenant with WHERE tenant_id = ${tenantId} (or otherwise ensure the caller's tenantId is applied). If a multi-tenant job must run globally, run it in a controlled admin-only context and ensure explicit authorization and auditing; avoid ad-hoc unfiltered queries against tenant-scoped tables.
- **server/memory-graph.ts:240** — SELECT against `memory_links` (existence check) does not constrain by tenant_id. Reading a tenant-scoped table without a tenant filter can leak or mix cross-tenant data.
  - _Fix:_ Filter the existence check by tenant (e.g. add AND tenant_id = ${tenantId}), or join through tenant-scoped `memory_entries` and assert both ends belong to the caller's tenant. Ensure requests supply a validated tenantId and use it in the WHERE clause.
- **server/memory-graph.ts:124** — UPDATE on tenant-scoped table `memory_categories` uses WHERE id = ${catId} only and lacks a tenant filter. This can inadvertently update rows belonging to another tenant if ids are mis-bound or incorrect.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}). Pass tenantId into the calling function or use a helper that enforces tenant-scoped updates.
- **server/memory-graph.ts:183** — refreshCategoryCount: UPDATE memory_categories ... WHERE id = ${categoryId} has no tenant_id constraint. Mutating a tenant-scoped table without tenant scoping violates tenant isolation.
  - _Fix:_ Accept a tenantId parameter and include it in the WHERE clause (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}). If called from places that only know categoryId, propagate and validate tenantId at those call sites.
- **server/memory-graph.ts:496** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks a tenant_id constraint. Updating by id alone can modify another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}). Ensure tenantId is passed into this function and validated before performing the update.
- **server/minds-engine.ts:214** — INSERT builds a Postgres array literal by string-concatenating params.dependsOn (`{${params.dependsOn.join(",")}}`) from caller input — this is string-interpolated SQL with request-derived data (injection + isolation risk).
  - _Fix:_ Do not construct SQL fragments by joining user arrays into SQL text. Pass arrays as parameterized values (use parameter binding or Drizzle helpers), or validate+sanitize every id as integers before binding. Example fixes: insert depends_on using a parameterized array (e.g. VALUES (..., ${params.dependsOn && params.dependsOn.length ? params.dependsOn : null}) and let the driver bind the array) or use UNNEST/unnest($1::int[]) with a bound parameter. Always avoid embedding raw joined strings from request data into sql templates.
- **server/minds-engine.ts:325** — UPDATE uses id = ANY(${`{${eventIds.join(",")}}`}::int[]) where the array is constructed by string-joining eventIds (caller-supplied) and inlined into the SQL. This is string-interpolated SQL with request-derived values (SQL injection risk) even though tenant_id is present.
  - _Fix:_ Avoid building array literals by string concatenation. Validate and coerce eventIds to integers, then pass them as a parameterized array to the query (e.g. WHERE id = ANY(${eventIds}) AND tenant_id = ${tenantId}) or use a parameterized UNNEST. Use Drizzle's query builder/parameters or proper parameter binding to ensure values are not injected into SQL text.
- **server/pace-control.ts:85** — checkPace queries heartbeat_logs for counts with no tenant_id filter (SELECT ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). This reads activity across all tenants and can leak tenant activity/usage data or allow incorrect global caps.
  - _Fix:_ Require a tenantId parameter and include WHERE tenant_id = ${tenantId} in the query. If the intent is an account-wide/global metric, make that explicit and run only in a privileged admin context; otherwise scope all queries to the caller's tenant to enforce proper isolation. Use parameterized queries and avoid embedding SQL fragments from untrusted inputs.
- **server/pace-control.ts:141** — getPaceSnapshot issues an unscoped SELECT against heartbeat_logs (COUNT over a time window) with no tenant_id restriction, returning global activity counts across tenants.
  - _Fix:_ Scope this snapshot to a tenant by adding WHERE tenant_id = ${tenantId} (and require tenantId as an argument), or move global snapshots to a clearly privileged admin-only path. Ensure any dashboard or API returning these metrics enforces caller authorization.
- **server/pace-control.ts:147** — getPaceSnapshot's personaRows query SELECT ... FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name has no tenant_id filter — it aggregates persona usage across all tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (and require tenantId param), or restrict this aggregate to an admin-only job with explicit tenant scoping. Ensure all reads/aggregates on tenant-scoped tables are parameterized per-tenant.
- **server/plan-executor.ts:117** — UPDATE plans (appendExecutionLog) modifies a tenant-scoped table without scoping by tenant_id (WHERE id = ${planId} only). This allows cross-tenant mutation/visibility if planId is misused.
  - _Fix:_ Include an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) when updating the plans row, or pass/validate the tenant id from the caller and use WHERE id = ${planId} AND tenant_id = ${tenantId} to enforce tenant isolation.
- **server/plan-executor.ts:513** — CAS UPDATE on plans to claim a plan (flip status → 'executing') uses WHERE id = ${planId} AND status = 'approved' but omits tenant_id. This reads/updates a tenant-scoped table without applying the caller's tenant constraint.
  - _Fix:_ Add tenant scoping to the CAS (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenantId} AND status = 'approved') or explicitly validate the plan's tenant before making cross-row updates.
- **server/plan-executor.ts:527** — UPDATE plans SET status='failed' ... WHERE id = ${planId} — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Constrain the update to the owning tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). If the code does not have tenantId in scope, obtain and validate it from the row before updating.
- **server/plan-executor.ts:728** — Final UPDATE on plans to set status/combine execution_log uses WHERE id = ${planId} only (no tenant_id). Tenant-scoped writes must be constrained to the plan's tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (WHERE id = ${planId} AND tenant_id = ${tenantId}) or otherwise validate/derive the tenant prior to updating.
- **server/plan-executor.ts:762** — Error-path UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' omits tenant_id filtering.
  - _Fix:_ Add tenant_id to the WHERE clause to avoid affecting rows from other tenants (WHERE id = ${planId} AND tenant_id = ${tenantId} AND status = 'executing').
- **server/plan-executor.ts:778** — resumeStuckPlans UPDATE flips status = 'executing' → 'approved' for all stalled plans matching a time window but omits any tenant filter (UPDATE plans WHERE status='executing' AND updated_at < ...). This globally modifies tenant-scoped rows.
  - _Fix:_ Scope the recovery update per-tenant (e.g. run per-tenant using WHERE tenant_id = ${tenantId}) or include a tenant_id predicate if this function is intended to run under a specific tenant context.
- **server/plan-executor.ts:787** — resumeStuckPlans selects pending plans via SELECT id FROM plans WHERE status = 'approved' ... with no tenant_id filter — iterates and requeues plans across all tenants.
  - _Fix:_ Limit the SELECT by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status = 'approved') or iterate per-tenant to preserve tenant isolation.
- **server/process-governor.ts:595** — In executeAction -> disable_task the code issues UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without adding tenant_id. The SELECT that discovered t came from tenant-scoped rows, but the UPDATE omits tenant scoping and can affect a row with the same id in another tenant context.
  - _Fix:_ Always include tenant_id in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) to enforce tenant isolation on writes.
- **server/process-governor.ts:641** — In executeAction -> restart_agent the code updates agent_desks via WHERE id = ${desk.id} without tenant_id filtering. This mutates a tenant-scoped table without scoping by tenant.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) after validating the desk belongs to the expected tenant.
- **server/process-governor.ts:977** — logAction performs UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} with no tenant_id constraint. governance_rules is tenant-scoped; updating by id only may affect another tenant's rule row.
  - _Fix:_ Include tenant scoping on the update (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) so only the intended tenant's rule is modified.
- **server/project-brain.ts:135** — updateProjectBrain reads the projects table via SELECT name,... FROM projects WHERE id = ${projectId} with no tenant_id check. This accepts a foreign projectId and reads a tenant-scoped row without verifying caller ownership.
  - _Fix:_ Require and validate tenant context (tenantId) for the caller and include WHERE id = ${projectId} AND tenant_id = ${tenantId}, or fetch the project and verify its tenant_id matches the caller before proceeding.
- **server/project-brain.ts:332** — backfillProjectBrains runs SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... with no tenant filter, enumerating projects across all tenants.
  - _Fix:_ Restrict the backfill to a single tenant per run (add WHERE p.tenant_id = ${tenantId}) or iterate tenants explicitly; never scan tenant-scoped tables globally without an explicit tenant constraint.
- **server/project-brain.ts:349** — In backfillProjectBrains: SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks tenant scoping; conversation rows are tenant-scoped and should not be queried without tenant validation.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or ensure proj.id is only used after validating its tenant.
- **server/project-brain.ts:359** — In backfillProjectBrains: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without a tenant filter. Messages are tenant-scoped and this reads data without verifying tenant ownership of conversationId.
  - _Fix:_ Add tenant scoping (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or validate the conversation's tenant before selecting messages.
- **server/recurring-messages.ts:258** — Unscoped SELECT from tenant-scoped table `agent_knowledge`: the query `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` has no tenant_id constraint and returns rows across tenants.
  - _Fix:_ Avoid unfiltered reads of tenant-scoped tables. Either (a) add a WHERE tenant_id = ${tenantId} when running per-tenant, or (b) if a global system-run is required, document/centralize that intent (e.g. call listScheduledMessages with `_allowGlobalSystemUseOnly`) and apply strict per-row tenant validation BEFORE any action on each row, minimize the window of exposure, and paginate/limit scope.

- **server/research-engine.ts:496** — INSERT INTO project_notes (summary) omits tenant_id. Project-scoped tables are tenant-scoped elsewhere and INSERTs must explicitly set tenant_id per invariant.
  - _Fix:_ Include tenant_id in the INSERT columns and pass the session's tenant (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)). Keep the assertProjectInTenant check but still write tenant_id explicitly.
- **server/research-engine.ts:504** — Per-finding INSERT INTO project_notes omits tenant_id, allowing notes to be written without explicit tenant attribution.
  - _Fix:_ Add tenant_id to the INSERT columns and bind ${session.tenantId} (INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)).
- **server/routes.ts:167** — Startup restore selects from tenant-scoped file_storage without applying a tenant_id constraint (db.select(...).from(fileStorage).where(...)). This reads rows for all tenants.
  - _Fix:_ Scope the query by tenant (e.g. add AND eq(fileStorage.tenantId, <tenant>) or run per-tenant with explicit tenantId). If this background job must process all tenants, run it with explicit administrative intent and ensure access is restricted and audited (or iterate tenants and query each tenant separately).
- **server/routes.ts:1118** — In /uploads middleware the code queries file_storage by filename without a tenant_id WHERE clause (select owners = db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This reads metadata for all tenants' files and can expose cross-tenant information.
  - _Fix:_ Avoid scanning other tenants' file rows. Instead SELECT only rows matching the caller's tenant (eq(fileStorage.tenantId, authedTenantId)). If you need to detect collisions across tenants, implement an explicit, audited administrative flow or require a platform-admin capability before reading across tenants and document the rationale.
- **server/routes.ts:4338** — INSERT into project_notes does not include tenant_id. The code inserts a project note with VALUES (${projId}, ${snapshot.slice(0, 500)}, ${'system:context-guard'}) but omits tenant_id, relying only on project_id linkage. In a multi-tenant schema without RLS or FK cascade guarantees this can write notes into the wrong tenant's namespace or create orphaned rows.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'})). Better: use a storage-layer helper (e.g. storage.createProjectNote) that requires tenantId and validates the project belongs to that tenant before persisting.
- **server/routes/admin-extra.ts:125** — Use of sql-tagged raw query with request-derived values (tenantId, limit) — db.execute(sql`... WHERE tenant_id = ${tenantId} LIMIT ${limit}`) leads to injection/isolation risk even though tenant_id is present.
  - _Fix:_ Avoid inline sql`...${...}...` with request-derived values. Use Drizzle query builder (db.select().from(...).where(eq(table.tenantId, tenantId), ...).limit(limit)) or otherwise use properly parameterized queries and validate/normalize tenantId and limit before use.
- **server/routes/admin-extra.ts:154** — Use of sql-tagged raw query with request-derived values (tenantId, status, limit) in self_initiatives listing — interpolation of request data into SQL template.
  - _Fix:_ Use Drizzle's query builder with explicit .where(eq(table.tenantId, tenantId), eq(table.status, status)) and .limit(limit) or ensure values are strongly validated and passed as parameters rather than embedded via template interpolation.
- **server/routes/admin-extra.ts:184** — UPDATE using sql template with request-derived id and tenantId (sql`UPDATE self_initiatives ... WHERE id = ${id} AND tenant_id = ${tenantId}`) — while tenant_id is present, raw template interpolation of request values risks injection/isolation bugs.
  - _Fix:_ Use Drizzle's update builder (e.g. db.update(self_initiatives).set({ ... }).where(and(eq(self_initiatives.id, id), eq(self_initiatives.tenantId, tenantId)))). This ensures parameterization and clearer tenant-scoping.
- **server/routes/admin-extra.ts:216** — Multiple db.execute(sql`... ${tenantId}`) calls in account deletion-summary use direct sql templates interpolating tenantId (and other request-derived values) into raw SQL.
  - _Fix:_ Replace raw sql templates with Drizzle query builder operations or parameterized helpers (db.select(...).from(...).where(eq(table.tenantId, tenantId))). Validate tenantId from session and avoid string interpolation of request values into SQL.
- **server/routes/admin-extra.ts:254** — schedule-deletion performs UPDATE tenants ... WHERE id = ${tenantId} using sql template with request-derived tenantId.
  - _Fix:_ Use Drizzle update builder or parameterized query methods (db.update(tenants).set(...).where(eq(tenants.id, tenantId))) and validate tenantId; avoid embedding request values into sql`...` templates.
- **server/routes/admin-extra.ts:282** — cancel-deletion performs UPDATE tenants ... WHERE id = ${tenantId} via sql template interpolating tenantId from request.
  - _Fix:_ Use Drizzle's parameterized update builder (db.update(...).set(...).where(eq(...))). Ensure tenantId is validated and do not interpolate request values into raw SQL templates.
- **server/routes/admin-extra.ts:312** — Permanent deletion executes many DELETE ... WHERE tenant_id = ${tenantId} via sql templates — request-derived tenantId interpolated into raw SQL across multiple DELETE statements.
  - _Fix:_ Use parameterized operations (db.delete(...).where(eq(table.tenantId, tenantId))) or wrap deletes in a transaction using the query builder. Ensure tenantId is strongly validated; avoid repeating raw interpolated SQL to reduce risk of injection or accidental omission of tenant constraint.
- **server/routes/admin-wedges.ts:41** — db.execute(sql`... WHERE tenant_id = ${tenantId} AND ${tag} = ANY(tags) ...`) interpolates request-derived tenantId (and tag) into a raw SQL template — injection/isolation risk.
  - _Fix:_ Use Drizzle query builder (db.select().from(projects).where(and(eq(projects.tenantId, tenantId), sql`? = ANY(tags)`, ...))) or, better, pass tag as a parameterized value via safe binding and validate tenantId. Avoid embedding request-derived values directly in sql templates.
- **server/routes/admin-wedges.ts:53** — Pending-drafts query uses sql template with tenant-derived ${tenantId} and wedge slug interpolation (sql`SELECT COUNT(*) ... WHERE tenant_id = ${tenantId} AND wedge_slug = ${w.slug} ...`).
  - _Fix:_ Use the query builder (db.select(...).from(...).where(and(eq(table.tenantId, tenantId), eq(table.wedgeSlug, w.slug)))) or otherwise parameterize values; avoid embedding request-derived/variable values directly into sql templates.
- **server/routes/admin-wedges.ts:73** — Heartbeat lookup uses sql template interpolating tenantId and a computed regex string (${slugRe}) into the raw SQL (`... WHERE tenant_id = ${tenantId} AND (LOWER(name) ~ ${slugRe} ... )`).
  - _Fix:_ Parameterize values and prefer the query builder. If you must use raw SQL for regex, ensure slugRe and tenantId are validated/escaped and passed as parameters rather than interpolated into a template literal; prefer db.select().where(eq(table.tenantId, tenantId)).and(sql`LOWER(name) ~ ?`, slugRe).
- **server/routes/admin-zombie-detector.ts:37** — Transaction block executes raw sql templates that interpolate request-derived windowDays into heavy queries (e.g. `... started_at > NOW() - (${windowDays}::int * INTERVAL '1 day')`).
  - _Fix:_ Validate and coerce windowDays to an integer, then use parameterized binding or query builder constructs to avoid embedding it directly in SQL templates. Prefer building the INTERVAL using safe parameterization rather than string/interpolation into sql``.
- **server/routes/admin.ts:514** — Admin PATCH uses raw sql template updating tenants with request-derived values (sql`UPDATE tenants SET plan = ${plan} WHERE id = ${targetId}`) — interpolation of plan and targetId from request into SQL.
  - _Fix:_ Use Drizzle update builder (db.update(tenants).set({ plan }).where(eq(tenants.id, targetId))) which parameterizes values, and validate plan/targetId server-side. Avoid inline sql template interpolation of request data.
- **server/routes/admin.ts:520** — Admin PATCH uses sql template to set trial_max_conversations with values derived from request (sql`UPDATE tenants SET trial_max_conversations = ${maxConvs} WHERE id = ${targetId}`).
  - _Fix:_ Use Drizzle's update builder or parameterized queries and validate/normalize numeric values before passing them to the DB. Avoid raw sql interpolation of untrusted inputs.
- **server/routes/admin.ts:547** — Reset usage route runs sql`UPDATE tenants SET trial_conversations_used = 0 WHERE id = ${targetId}` interpolating targetId from request into raw SQL.
  - _Fix:_ Use db.update(tenants).set({ trial_conversations_used: 0 }).where(eq(tenants.id, targetId)) or a parameterized query to ensure safe binding of request-derived values.
- **server/routes/admin.ts:799** — repair-incidents endpoint builds a dynamic WHERE clause from sql`... ${tenantId}`/filters then interpolates the combined clause into a final sql template (sql`... WHERE ${where} LIMIT ${limit}`) — multiple request-derived pieces embedded into raw SQL templates.
  - _Fix:_ Construct filters using the query builder and combine them via AND expressions (e.g. start with eq(table.tenantId, tenantId) and push additional eq(...) filters). Use db.select().from(...).where(and(...filters)).limit(limit). This keeps tenant scoping explicit and avoids concatenated/interpolated SQL fragments.
- **server/routes/agent-insights.ts:49** — Multiple db.execute(sql`...`) queries interpolate request-derived tenantId and windowDays into raw SQL (e.g. agent_activity / agent_cost_ledger queries use WHERE tenant_id = ${tenantId} AND started_at > NOW() - (${windowDays} || ' days')::interval).
  - _Fix:_ Use Drizzle's query builder with explicit eq(table.tenantId, tenantId) and parameterized interval construction (validate/normalize windowDays to an integer before binding). Avoid constructing intervals or WHERE clauses by interpolating request values into sql templates.
- **server/routes/agent-insights.ts:244** — Runs listing uses sql template with tenant-derived ${tenantId} and ${limit} (sql`FROM plans WHERE tenant_id = ${tenantId} LIMIT ${limit}`) — request values interpolated into raw SQL.
  - _Fix:_ Use db.select().from(plans).where(eq(plans.tenantId, tenantId)).orderBy(...).limit(limit) via Drizzle to ensure parameterization and explicit tenant scoping.
- **server/routes/agent-insights.ts:277** — Run detail query uses raw sql template with request-derived planId and tenantId (sql`FROM plans WHERE id = ${planId} AND tenant_id = ${tenantId}`) — interpolation risk.
  - _Fix:_ Use the query builder (db.select().from(plans).where(and(eq(plans.id, planId), eq(plans.tenantId, tenantId))).limit(1)) so parameters are bound safely and tenant ownership is explicit.
- **server/routes/agent-insights.ts:288** — Rewards query interpolates tenantId and planId into raw SQL (sql`FROM step_rewards WHERE tenant_id = ${tenantId} AND plan_id = ${planId}`).
  - _Fix:_ Use the query builder and parameterized where(eq(...)) expressions to bind tenantId and planId safely and avoid raw-template interpolation.
- **server/routes/api-v1.ts:392** — GET /api/v1/agents uses a raw sql template with request-derived tenantId in the JOIN condition (tpn.tenant_id = ${tenantId}) — tenantId interpolated into SQL template.
  - _Fix:_ Use Drizzle's select/join API to express the LEFT JOIN with a where clause using eq(tenant_persona_names.tenantId, tenantId). This keeps tenant binding explicit and avoids embedding request values into sql templates.
- **server/routes/api-v1.ts:466** — Dispatch handler resolves agent name via raw sql template interpolating request-provided agent into LOWER(name) = LOWER(${agent}) — direct interpolation of user input into SQL.
  - _Fix:_ Parameterize the agent value or use the query builder (db.select().from(personas).where(and(eq(personas.isActive, true), sql`LOWER(name) = LOWER(?)`, agent)). Prefer builder methods to ensure safe binding and avoid injection/extraction risks.
- **server/routes/api-v1.ts:655** — AG-UI/conversation persona resolution performs db.execute(sql`SELECT id FROM personas WHERE is_active = true AND LOWER(name) = LOWER(${body.agent}) LIMIT 1`) interpolating request body agent into the template.
  - _Fix:_ Use parameterized binding or Drizzle query builder for the persona lookup and validate body.agent before use. Avoid injecting request data directly into sql templates.
- **server/routes/api-v1.ts:889** — A2A handler uses raw sql template to resolve skillId (sql`... LOWER(REGEXP_REPLACE(name, '[^a-zA-Z0-9]+', '-', 'g')) = LOWER(${skillId})`) with request-derived skillId interpolated into SQL.
  - _Fix:_ Validate/normalize skillId server-side and use parameterized binding or query builder constructs for the comparison; avoid injecting untrusted params into raw sql templates.
- **server/routes/api-v1.ts:777** — Conversation poll uses raw sql template selecting messages with m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} — both values interpolated from the request into SQL.
  - _Fix:_ Prefer using Drizzle query builder (db.select(...).from(messages).join(conversations,...).where(and(eq(messages.conversationId, conversationId), eq(conversations.tenantId, tenantId))).limit(50)). This ensures parameter binding and makes tenant-scoping explicit.
- **server/routes/api-v1.ts:940** — A2A tasks/get selects assistant messages with m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} using a raw sql template — request-derived values interpolated into SQL.
  - _Fix:_ Use parameterized queries or Drizzle's builder API to bind conversationId and tenantId safely (and keep tenant checks explicit). Avoid inline interpolation of request-sourced values into sql templates.
- **server/routes/conversations.ts:99** — UPDATE on tenant-scoped table 'conversations' executed without constraining by tenant_id. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` can modify a conversation belonging to another tenant if IDs are guessed or misbound.
  - _Fix:_ Add tenant scoping to the UPDATE. Example: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use conv.tenantId if available). Even if you previously verified project ownership, every write against a tenant-scoped table must include the tenant_id WHERE clause or use a storage helper that enforces it.
- **server/routes/conversations.ts:100** — SELECT from tenant-scoped table 'project_conversations' performed without tenant constraint: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`. This can leak cross-tenant existence information or be raced into incorrect behavior.
  - _Fix:_ Include tenant scoping on the SELECT. Example: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}`. Prefer using a tenant-aware storage API if available.
- **server/routes/conversations.ts:103** — INSERT into (presumed) tenant-scoped table 'project_conversations' does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`. Tenant_id is required by the platform invariant and missing here.
  - _Fix:_ Insert must include the tenant identifier explicitly. Example: `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})` (and ensure any unique/indexes include tenant_id). Alternatively, use a storage-layer function that enforces tenant scoping.
- **server/routes/conversations.ts:105** — UPDATE on tenant-scoped table 'projects' executed without tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`. Even though project ownership was checked earlier, writes must still be constrained at the SQL layer to prevent accidental cross-tenant updates.
  - _Fix:_ Add tenant scoping to the UPDATE. Example: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Prefer using tenant-aware storage/ORM helpers that always include the tenant filter.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} executes without constraining tenant_id. This can modify another tenant's row if IDs collide or in race conditions.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (AND tenant_id = ${tenantId}) or perform the update via a storage helper that enforces tenant ownership. Example: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}.
- **server/routes/projects.ts:232** — UPDATE on tenant-scoped table `projects` lacks tenant_id constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so the write cannot affect another tenant's project even if project ownership was validated earlier.
- **server/routes/projects.ts:300** — UPDATE on tenant-scoped table `projects` lacks tenant_id constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Apply the same pattern everywhere an update touches tenant-scoped tables.
- **server/routes/projects.ts:319** — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY ...
  - _Fix:_ Add tenant_id to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or JOIN against projects and check p.tenant_id = ${tenantId} in the same SQL to ensure tenant isolation is enforced at the DB level for every read.
- **server/routes/projects.ts:342** — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or join the projects table and assert projects.tenant_id = ${tenantId} in the same SQL. Do not rely solely on a prior in-memory/project check.
- **server/routes/projects.ts:371** — DELETE on tenant-scoped table `project_files` lacks tenant_id constraint — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}
  - _Fix:_ Constrain the DELETE to the caller's tenant (e.g. WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) to prevent deleting another tenant's file rows; apply same pattern to all DELETE/UPDATE/SELECT statements on tenant-scoped tables.
- **server/sculptor.ts:105** — UPDATE on tenant-scoped table `sculptor_sessions` lacks a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This can update a row from another tenant if an id is guessed or reused.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${params.tenantId} or use the session's tenant_id) or re-query the session with tenant_id before doing the UPDATE so the write is guaranteed to be limited to the caller's tenant.
- **server/sculptor.ts:112** — UPDATE on tenant-scoped table `sculptor_sessions` lacks a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}).
  - _Fix:_ Include AND tenant_id = ${params.tenantId} (or the session's tenant id) in the WHERE clause, or perform the update by a SELECT+UPDATE that validates tenant ownership first.
- **server/sculptor.ts:167** — UPDATE on tenant-scoped table `sculptor_sessions` in monitorSession() omits tenant scoping (WHERE id = ${sessionId}). This allows cross-tenant mutation of session state.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure any caller-supplied ids are verified to belong to the caller's tenant before updates.
- **server/sculptor.ts:358** — UPDATE on tenant-scoped table `sculptor_sessions` to write review_result lacks tenant constraint (WHERE id = ${sessionId}).
  - _Fix:_ Add AND tenant_id = ${tenantId} (or verify session tenant first) to the WHERE clause so only the caller's tenant row can be updated.
- **server/second-opinion.ts:404** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (settleFusionReservation) updates a tenant-scoped ledger row by id only, with no tenant_id check. A reservation id could be reused/guessed to modify another tenant's ledger entry.
  - _Fix:_ Scope the UPDATE to the tenant (e.g. add AND tenant_id = ${tenantId}). If tenantId is not available in this function, change the API to accept tenantId or look up the reservation row and verify its tenant before performing the UPDATE.
- **server/second-opinion.ts:423** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseFusionReservation) omits tenant scoping. This can clear or modify another tenant's ledger row by id alone.
  - _Fix:_ Require and use tenant_id in the WHERE clause (or verify the reservation row's tenant before updating). Modify the function signature to accept tenantId or perform a guarded lookup+update transaction that enforces tenant ownership.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter against what appears to be a tenant-scoped table.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, include tenant_id = ${ADMIN_TENANT_ID} or the intended tenant scope in the WHERE clause. If the operation is intentionally global, make that explicit in code/comments and ensure the table is actually global (no tenant_id) to avoid accidental cross-tenant effects.
- **server/seed-cleanup.ts:15** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks without a tenant_id constraint, modifying rows across tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the operation is performed only on a global/administrative table. Prefer explicit tenant scoping to avoid accidental cross-tenant changes.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id filtering on a tenant-scoped table.
  - _Fix:_ Add tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) to the WHERE clause if the intent is to operate only on the admin tenant; otherwise make explicit and safe the global intent.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true updates tasks across all tenants because it lacks tenant scoping.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${ADMIN_TENANT_ID} or otherwise restrict to the intended tenant. If truly global, document and validate that heartbeat_tasks is a global table with no tenant_id column.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has no tenant_id condition and thus aggregates across tenants for a tenant-scoped table.
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or the correct tenant) in the WHERE clause when counting, or make the global intent explicit and safe.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true performs an unscoped update across heartbeat_tasks (no tenant_id constraint), affecting all tenants.
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) to the WHERE clause, or ensure the operation is intentionally global and safe for all tenants with an explicit global-table design and review.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions runs without filtering by tenant_id. research_sessions is tenant-scoped and this reads across all tenants.
  - _Fix:_ Restrict the query to the intended tenant. e.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId} or use Drizzle: db.select().from(researchSessions).where(eq(researchSessions.tenantId, tenantId)). Ensure the tenantId value is derived from a trusted source (not request body) when seeding/admin tasks.
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter. research_programs is tenant-scoped, so this reads all tenants' programs.
  - _Fix:_ Add a tenant constraint to the SELECT, e.g. SELECT id, name, model FROM research_programs WHERE is_active = true AND tenant_id = ${tenantId} (or use Drizzle .where(eq(researchPrograms.tenantId, tenantId))). If this migration is intended to run per-tenant, iterate tenants explicitly and restrict to each tenant's id.
- **server/seed.ts:2163** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without verifying tenant ownership. Updating by id alone can modify rows from any tenant.
  - _Fix:_ Include tenant ownership in the UPDATE, e.g. UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} AND tenant_id = ${tenantId}. Alternatively join back to confirm the program belongs to the expected tenant before updating. Ensure tenantId is validated/trusted.
- **server/seed.ts:2515** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id filter. conversations is typically tenant-scoped, so this will enumerate rows across tenants.
  - _Fix:_ Add tenant scoping to the query and its subquery, e.g. SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND c.tenant_id = ${tenantId} AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id AND pc.tenant_id = ${tenantId}). If project_conversations is not tenant-scoped, consider adding tenant_id there as well and ensure all migration backfills preserve tenant isolation.
- **server/seed.ts:3832** — DELETE FROM heartbeat_tasks executes without any tenant_id constraint. heartbeat_tasks is tenant-scoped (has tenant_id) — this DELETE will affect rows across all tenants.
  - _Fix:_ Constrain the DELETE to the intended tenant (or otherwise scope it). Example: DELETE FROM heartbeat_tasks WHERE tenant_id = 1 AND enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'. If this operation must run per-tenant, iterate tenants and run the scoped DELETE for each tenant id.
- **server/seed.ts:3860** — UPDATE memory_entries SET ... runs without a tenant_id filter. memory_entries is tenant-scoped (there is an index on tenant_id); the UPDATE will modify rows for all tenants.
  - _Fix:_ Add an explicit tenant filter to the WHERE clause (e.g. AND tenant_id = 1) or iterate only the desired tenant(s). Example: UPDATE memory_entries SET ... WHERE tenant_id = <tenantId> AND deleted_at IS NULL AND fact LIKE '%Manjaro%' ... . Avoid global updates on tenant-scoped tables.
- **server/seed.ts:3886** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped; this will update conversations across all tenants.
  - _Fix:_ Scope the UPDATE to a tenant (e.g. WHERE tenant_id = 1 AND model = 'gpt-4.1') or perform the update per-tenant. Ensure every UPDATE against tenant-scoped tables includes tenant_id = <tenant>.
- **server/self-improvement.ts:170** — detectStagnation performs a SELECT on the tenant-scoped `experiments` table without requiring or enforcing a tenant_id filter when tenantId is not provided — the query can return experiments across tenants.
  - _Fix:_ Require the caller to pass a valid tenantId (and throw if missing), or always include a tenant constraint in the WHERE clause (e.g. .where(and(eq(experiments.category, category), eq(experiments.tenantId, tenantId)))). Do not run cross-tenant queries without an explicit, validated tenant context.
- **server/self-improvement.ts:521** — getExperimentHistory, when called without a tenantId, returns db.select().from(experiments) with no tenant filter — a tenant-scoped table is read without enforcing tenant isolation.
  - _Fix:_ Require a tenantId parameter or fail if tenantId is not supplied. If the function must allow no-tenant mode for admin purposes, make that explicit and protect it behind an admin/privileged check; otherwise always include WHERE tenant_id = <caller tenant>.
- **server/stability-watchdog.ts:177** — SELECT from heartbeat_logs lacks tenant scoping — reads aggregated error logs across tenants without a tenant_id WHERE clause.
  - _Fix:_ If the watchdog must operate per-tenant, add a tenant filter (e.g. WHERE tenant_id = ${tenantId}). If the watchdog intentionally needs to query all tenants, make that explicit (use the ALL_TENANTS sentinel or an audited admin path) and document the reasoning. Prefer using the storage-layer helpers that accept an explicit tenant scope or requiredTenantScope to avoid accidental cross-tenant reads.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks executes without tenant scoping — it can remove tenant-scoped rows across all tenants.
  - _Fix:_ Constrain the DELETE to a specific tenant_id when running tenant-scoped cleanup (e.g. WHERE tenant_id = ${tenantId} AND ...). If this is intentionally global maintenance, require an explicit ALL_TENANTS sentinel or an admin-only code path and audit/confirm the behavior. Prefer using storage helper APIs that accept an explicit tenant scope to avoid accidental multi-tenant deletes.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log has no tenant_id filter — purges event_log rows across tenants rather than scoped to a tenant.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND ...) for tenant-scoped event logs, or explicitly document and gate global purges behind an admin-only/ALL_TENANTS code path. Use requiredTenantScope/tenantScope helpers or storage-layer APIs to validate intent and prevent accidental cross-tenant deletions.
- **server/storage.ts:248** — getConversationUnscoped performs a SELECT on tenant-scoped `conversations` without any tenant_id constraint.
  - _Fix:_ Remove or strictly limit use of this unscoped accessor. If truly needed, require an explicit allowlist/admin flag or an explicit caller-validated tenantId parameter and assert the caller has permission. Prefer returning the row only when the caller provides/validates tenant context (e.g. getConversation(id, tenantId)). Logging alone is not sufficient to satisfy tenant isolation.
- **server/storage.ts:298** — attachCitationMetadata executes a raw SELECT on tenant-scoped `messages` (SELECT id, citations FROM messages WHERE id IN (...)) without scoping by tenant_id.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or accept a tenantId parameter and ensure it is validated. If ids are derived from a prior tenant-scoped query, still add tenant_id to the WHERE clause to enforce isolation defensively.
- **server/storage.ts:806** — updateMemoryEmbedding updates tenant-scoped `memory_entries` by id only (WHERE id = ...) with no tenant_id constraint.
  - _Fix:_ Require a tenantId parameter or verify the memory entry's tenant before updating. Add .where(and(eq(memory_entries.id, id), eq(memory_entries.tenant_id, tenantId))) so updates cannot affect other tenants.
- **server/storage.ts:818** — updateKnowledgeEmbedding updates tenant-scoped `agent_knowledge` by id only (WHERE id = ...) with no tenant_id constraint.
  - _Fix:_ Require and use tenantId (or re-verify ownership) when updating embeddings: .where(and(eq(agent_knowledge.id, id), eq(agent_knowledge.tenant_id, tenantId))). Fail closed if tenantId is not provided/validated.
- **server/storage.ts:826** — getMemoriesWithoutEmbeddings queries `memory_entries` without any tenant_id constraint — returns rows for all tenants.
  - _Fix:_ If this is intended as a per-tenant job, accept a tenantId (or TenantScope/ALL_TENANTS sentinel) and enforce it via WHERE tenant_id = ${tenantId}. If it must be global, require an explicit ALL_TENANTS sentinel and very strong admin protection and review.
- **server/storage.ts:833** — getKnowledgeWithoutEmbeddings queries tenant-scoped `agent_knowledge` without tenant_id filtering — returns rows across tenants.
  - _Fix:_ Require a tenant scope parameter or explicit ALL_TENANTS sentinel and enforce it in the WHERE clause (or use requiredTenantScope helper). Otherwise add eq(agent_knowledge.tenant_id, tenantId) to the predicate.
- **server/storage.ts:942** — getHeartbeatTask selects from tenant-scoped `heartbeat_tasks` by id with no tenant_id constraint.
  - _Fix:_ Either require caller to provide tenantId and use .where(and(eq(heartbeat_tasks.id, id), eq(heartbeat_tasks.tenant_id, tenantId))) or make the function internal-only for admin/maintenance and document/enforce that. Prefer explicit tenant validation to avoid accidental cross-tenant reads.
- **server/storage.ts:969** — updateHeartbeatTask performs an UPDATE on tenant-scoped `heartbeat_tasks` and will update by id alone when tenantId is not provided (conditions default to id-only). This allows unscoped mutation across tenants.
  - _Fix:_ Fail closed: require tenantId (or an explicit ALL_TENANTS sentinel) for non-admin callers and include tenant filtering in the WHERE clause. Do not rely on a subselect fallback — use requiredTenantScope or explicit eq(heartbeat_tasks.tenant_id, tenantId) to enforce ownership.
- **server/storage.ts:975** — deleteHeartbeatTask deletes from tenant-scoped `heartbeat_tasks` using only id when tenantId is not supplied (conditions.default id-only), permitting unscoped deletes across tenants.
  - _Fix:_ Require and validate tenantId or an explicit admin sentinel and include tenant filtering in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Fail closed if tenantId is missing.
- **server/storage.ts:1000** — getDueHeartbeatTasks selects due `heartbeat_tasks` across all tenants (raw SELECT without tenant_id). This fetches tenant-scoped rows platform-wide with no caller-controlled tenant scope.
  - _Fix:_ If the worker should claim tasks across all tenants, require and pass an explicit ALL_TENANTS sentinel and ensure the caller is an authorized system worker. Otherwise add a tenantId parameter and scope the query to the caller's tenant. At minimum, document and gate admin-only global semantics.
- **server/storage.ts:1027** — claimHeartbeatTasks runs raw UPDATE on `heartbeat_tasks` (UPDATE ... WHERE id = ${id} ...) with no tenant constraint — claiming tasks can thus race/claim tasks belonging to other tenants.
  - _Fix:_ Ensure the claim path enforces tenant scope: either include tenant_id = ${tenantId} in the WHERE clause or require a worker-level ALL_TENANTS sentinel with strict authorization. Avoid unscoped UPDATEs on tenant-scoped task tables.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (where only eq(customTools.name, parsed.name)). This reads tenant-scoped data across tenants and can leak existence or block per-tenant operations.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) so lookups are scoped to the caller's tenant.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without a tenant_id constraint (where eq(customTools.id, inserted.id)). Reads against tenant-scoped tables must be scoped to the caller tenant.
  - _Fix:_ Add a tenant constraint when selecting by id (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) and verify the returned row belongs to the expected tenant.
- **server/tools.ts:2878** — INSERT into project_conversations without tenant_id — new link row is created using only project_id and conversation_id (no tenant constraint/column). If project_conversations is tenant-scoped, this write omits the tenant_id column required by the invariant.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})), and/or ensure the insert is executed only after verifying both referenced rows belong to the caller's tenant. Always set tenant_id explicitly on tenant-scoped table INSERTs.
- **server/tools.ts:2981** — INSERT into project_files does not set tenant_id — project_files row is inserted with project_id but no tenant_id column/value is provided. Per the invariant every INSERT into a tenant-scoped table must pass tenantId explicitly.
  - _Fix:_ Add tenant_id to the INSERT columns and bind the caller's tenantId (e.g. INSERT INTO project_files (project_id, file_name, ..., tenant_id) VALUES (..., ${tenantId})). If project_files relies on project_id for scoping, prefer still storing tenant_id for defense-in-depth and to satisfy the invariant, or enforce the tenant constraint in the INSERT via a subselect that checks project ownership.
- **server/tools.ts:2996** — INSERT into project_notes omits tenant_id — the note row is inserted (project_id, note, author) without an explicit tenant_id value. This violates the requirement that INSERTs into tenant-scoped tables include tenantId.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})) or otherwise derive and supply the caller's tenantId after verifying ownership of the referenced project. Ensure tenant_id is non-null and validated.
- **server/tools.ts:3020** — INSERT into project_conversations (link_conversation path) again omits tenant_id — the handler inserts a project↔conversation link without setting tenant_id on the row.
  - _Fix:_ When inserting into project_conversations here, include tenant_id (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})) after verifying both project and conversation belong to the caller's tenant. Make tenant_id an explicit required column for tenant-scoped tables and always bind it from the authenticated context.
- **server/tools.ts:4566** — SELECT from tenant-scoped presenter_sessions without tenant constraint or ownership check: `SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...` reads session tokens by presentation_id but does not restrict by tenant_id or assert the presentation belongs to the caller's tenant.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add `AND tenant_id = ${params._tenantId}`) or perform an explicit ownership assertion (e.g. `assertPresentationInTenant(presentationId, params._tenantId)`) before selecting presenter_sessions so rows from other tenants cannot be read.
- **server/tools.ts:6927** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT uses project_id but omits tenant_id in the VALUES list, violating the invariant that every tenant-scoped INSERT must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. VALUES (${params._tenantId}, ${params.project_id}, ...)) or otherwise set tenant_id from a trusted source after asserting project ownership. Prefer using Drizzle insert with an explicit tenantId column or ensure assertProjectInTenant returns a project object and then pass its tenantId into the insert.
- **server/tools.ts:8895** — UPDATE against tenant-scoped table 'proposed_skills' uses only WHERE id = row.id and lacks a tenant_id constraint. This allows updating rows across tenants if IDs collide or are guessed.
  - _Fix:_ Constrain UPDATE by tenant as well, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or include tenant filter in the SQL so updates only affect rows owned by the caller's tenant.
- **server/tools.ts:8922** — UPDATE against tenant-scoped table 'proposed_skills' (mark accepted / set promotedSkillId) is missing a tenant_id condition in the WHERE clause (only matches id).
  - _Fix:_ Add tenant scoping to the WHERE clause when updating proposed_skills (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Always require and check tenant ownership when modifying tenant-scoped rows.
- **server/tools.ts:8937** — UPDATE against tenant-scoped table 'proposed_skills' (mark rejected) is executed with WHERE id = row.id only and lacks tenant scoping, enabling cross-tenant updates.
  - _Fix:_ Include tenant filtering on updates to proposed_skills (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Audit all update/delete statements to ensure every tenant-scoped operation includes an explicit tenant_id constraint.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against tenant-scoped table 'browser_workflows' without constraining by tenant_id (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This can modify another tenant's rows if an id from a different tenant is used.
  - _Fix:_ Include tenant scoping in the UPDATE. e.g. UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId} (or use Drizzle's .where(eq(table.tenantId, tenantId))). Alternatively, re-verify ownership of workflow.id for ctx.tenantId before performing the UPDATE.
- **server/tools/domains/files/handlers.ts:903** — INSERT into project_files does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (${projId}, ...)). Per project_files being tenant-scoped, tenant_id must be explicitly provided on INSERT.
  - _Fix:_ Include tenant_id in the INSERT values (INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ...)) and ensure the tenantId is validated/derived from a trusted context (ctx.tenantId) or from a previously verified project ownership check.
- **server/tools/domains/files/handlers.ts:907** — UPDATE executed against tenant-scoped table 'projects' without constraining by tenant_id (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Even if projId was obtained earlier, the UPDATE itself lacks an explicit tenant filter in SQL.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}, or use a Drizzle update with .where(eq(projects.tenantId, tenantId)). Alternatively, re-check project ownership immediately prior to the UPDATE.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE against tenant-scoped table `knowledge_triples` without tenant filter. The code executes `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` (no `tenant_id` check).
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}`); Ensure every UPDATE/DELETE/SELECT that mutates or reads tenant-scoped tables includes an explicit tenant_id predicate (or an equivalent Drizzle .where(eq(..., tenantId))).
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE memory_entries SET ... WHERE id = ${id} executes without constraining the update by tenant_id. Even though the id was found earlier by a tenant-scoped SELECT, the UPDATE itself lacks tenant scoping and could modify rows across tenants in race/ID-collision scenarios.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} (or AND tenant_id = ${tid} AND persona_id = ${personaId}) so all writes are explicitly constrained to the caller's tenant.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) is executed without a tenant_id constraint. This bulk update may affect rows belonging to other tenants if ids are manipulated or computed incorrectly.
  - _Fix:_ Constrain the bulk update by tenant (and persona if appropriate), e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid} (or use WHERE tenant_id = ${tid} AND id = ANY(${ids})). Ensure the tenant is passed as a parameter to the query rather than interpolated into raw SQL.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET ... WHERE id = ${id} (recordStrategicWinHandler) lacks tenant_id scoping. Same pattern as other updates — write executed without explicit tenant constraint.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} (or include persona_id as needed). Ensure all UPDATE/DELETE statements against tenant-scoped tables include the tenant_id condition to enforce per-tenant isolation.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` executed without constraining by tenant_id (no WHERE tenant_id = ...). This loads pairing rows across all tenants.
  - _Fix:_ Restrict the query to the relevant tenant(s). If the intent is to verify a claimed pairing, query only rows for the claimed tenant (e.g. WHERE title = ${...} AND tenant_id = ${claimedTenant}) or join the conversations table and filter by the conversation's tenant_id in the same query. Avoid scanning all tenants' agent_knowledge rows in an unfiltered SELECT.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` by conversation id is performed without an application-level tenant filter. Reading a tenant-scoped table by id without constraining tenant_id violates the tenant-isolation invariant.
  - _Fix:_ Include an explicit tenant filter when reading conversations (e.g. WHERE id = ${convId} AND tenant_id = ${expectedTenant}) or perform a single JOIN that verifies the conversation belongs to the expected tenant. Do not rely on post-hoc comparisons alone — enforce tenant_id in the SQL WHERE clause.
- **server/video-job-runner.ts:235** — recoverStaleVideoJobs performs a global UPDATE on videoJobs without constraining to a tenant (no WHERE tenant_id = ...). This will mark jobs across all tenants as failed.
  - _Fix:_ Constrain the UPDATE to a specific tenant when appropriate (e.g. add eq(videoJobs.tenantId, someTenantId) to the WHERE), or iterate per-tenant. If this is intended to be a global maintenance task, document and protect it (admin-only) — but avoid accidental cross-tenant state changes.

### MEDIUM (46)

- **server/agent-channels.ts:198** — JOIN to channel_subscriptions omits tenant scoping: JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} (the query's WHERE filters only cm.tenant_id). The channel_subscriptions side is not constrained to tenant_id.
  - _Fix:_ Add an explicit tenant filter for channel_subscriptions (e.g. AND cs.tenant_id = ${tenantId}) in the join/WHERE so the subscription rows are tenant-scoped.
- **server/agentic-features.ts:313** — Subquery in detectCompetitorChanges builds a DISTINCT ON subquery against `competitor_snapshots` without tenant scoping (subquery: SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY url, created_at DESC). The inner subquery omits tenant_id, potentially mixing rows across tenants.
  - _Fix:_ Add tenant scoping to the subquery (e.g. AND tenant_id = ${params.tenantId}) to ensure the DISTINCT ON selection is limited to the tenant's snapshots.
- **server/chat-engine.ts:3814** — recordMutation({... conversationId, ...}) is invoked without an explicit tenantId. If recordMutation persists audit/mutation records into a tenant-scoped table, those writes may be missing tenant scoping.
  - _Fix:_ Include tenantId in the recordMutation call (or ensure recordMutation derives and enforces tenant ownership from conversationId). Ensure the storage layer always sets tenant_id on INSERT and includes tenant_id constraints on any updates or selects for tenant-scoped tables.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments reads across all tenant rows (no tenant_id filter). Reads from a tenant-scoped table without tenant restriction may expose tenant identifiers and enables downstream cross-tenant processing unless intentionally admin-only.
  - _Fix:_ If this enumeration is intentionally an admin/background operation, document and restrict its execution context (admin-only process, audited). Otherwise scope reads to a single tenant or use an explicit admin-only path. Consider using a dedicated admin table or service to enumerate tenants rather than scanning tenant-scoped data without explicit authorization checks.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken queries dfy_intake_forms by token with no tenant constraint. Although token is unguessable, this reads a tenant-scoped table without scoping by tenant_id.
  - _Fix:_ Either accept a tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or explicitly document and justify why this must remain global. Prefer adding tenant scoping where possible.
- **server/email.ts:471** — startInboxPolling checks for existing inbox_messages with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 but does not include tenant_id. inbox_messages is tenant-scoped; a cross-tenant message_id collision could leak presence/absence.
  - _Fix:_ Filter by tenant_id (if known in this context) or by inbox_id (which is available) to scope the lookup: WHERE message_id = ${msgId} AND inbox_id = ${inboxId} AND tenant_id = ${tenantId}. If inbox_id uniquely identifies tenant context, use it in the WHERE clause.
- **server/email.ts:515** — backfillEmptyBodies selects rows from inbox_messages WHERE body_text IS NULL ... ORDER BY id with no tenant filter. This reads across tenants.
  - _Fix:_ Restrict the query to a tenant context (e.g. add AND tenant_id = ${tenantId}) or make the operation an explicit admin-only global routine with clear justification. Prefer passing tenantId and enforcing WHERE tenant_id = ${tenantId}.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal derived from a request (the embedding vector converted to a string `'${vec}'::vector`) appears in multiple queries (vector search / backfill / store). This interpolates request-derived data into raw SQL.
  - _Fix:_ Avoid sql.raw() with interpolated request-derived values. Use parameterized binding (e.g. pass the embedding as a parameter and cast $1::vector) or Drizzle parameterization so values are bound rather than injected. If raw interpolation is absolutely necessary, validate and canonicalize inputs thoroughly and document why it's safe; additionally ensure tenant scoping is always present for the same statements.
- **server/job-queue.ts:385** — getJobStats aggregates counts FROM agent_jobs for the last 7 days with no tenant filter — it exposes cross-tenant aggregate information to any caller of this helper.
  - _Fix:_ Accept an optional tenantId parameter and scope aggregation to that tenant for normal callers. If global stats are needed, require explicit admin privileges and document the elevated access.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor queries agent_knowledge grouped by tenant_id/persona_id with no caller-tenant constraint — it reads across all tenants.
  - _Fix:_ If this routine is intended to be an admin/global diagnostic, ensure it is only callable by a privileged, audited background task. Otherwise, accept a tenantId to scope the scan or perform per-tenant iterations initiated under an explicit tenant context to avoid unscoped reads.
- **server/lib/stuck-task-sweep.ts:57** — SELECT against agent_runs (status='running') contains no tenant_id filter — scans running runs across all tenants.
  - _Fix:_ If this is an admin/global audit, require an explicit admin/cron context and document that cross-tenant scans are intentional. If not, add a WHERE tenant_id = ${tenantId} filter or accept a tenantId parameter so the query is explicitly scoped to one tenant at a time.
- **server/lib/stuck-task-sweep.ts:85** — SELECT against mind_tickets (status='in_progress') is executed without a tenant_id constraint and will surface rows across all tenants.
  - _Fix:_ Scope the query by tenant (WHERE tenant_id = ${tenantId}) or require an explicit admin/global context for cross-tenant auditing. Make the cross-tenant intent explicit in code and apply appropriate auth/audit controls.
- **server/lib/system-probes.ts:163** — db.select(count(*)) FROM messages table is executed with no tenant_id filter — this returns a global message count across tenants rather than a tenant-scoped metric.
  - _Fix:_ If the metric is meant to be global/admin-only, make that explicit and ensure only admin contexts call it. If a tenant-scoped count is required, add WHERE tenant_id = ${tenantId} (or otherwise pass/explicitly use ALL_TENANTS with clear admin guard). Avoid silently querying tenant-scoped tables without an explicit tenant constraint.
- **server/research-engine.ts:331** — SELECT name FROM personas WHERE id = ${program.persona_id} has no tenant constraint. If personas are tenant-scoped, this allows reading persona rows across tenants.
  - _Fix:_ Constrain persona lookups by tenant (e.g. SELECT name FROM personas WHERE id = ${program.persona_id} AND tenant_id = ${tenantId}) or resolve persona via a tenant-aware helper that validates ownership.
- **server/research-engine.ts:1082** — resolvePersonaId executes SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 with no tenant_id filter. This can return a persona id from the wrong tenant.
  - _Fix:_ Make resolvePersonaId tenant-aware (accept and use tenantId) and include AND tenant_id = ${tenantId} in the WHERE clause, or otherwise ensure personas are global and document that explicitly.
- **server/routes.ts:1533** — Public stats endpoint issues unscoped SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) and returns global counts. These queries read all tenants' rows without tenant_id filtering.
  - _Fix:_ If the intent is to return aggregated global metrics, ensure this is an explicit administrative/telemetry path with appropriate access control. Otherwise, scope counts to the caller's tenant with WHERE tenant_id = <tenantId> or compute global aggregates in a separate telemetry pipeline that does not read tenant rows in-process.
- **server/routes.ts:1561** — The architecture endpoint runs many unscoped SQL count queries (e.g. SELECT count(*) FROM conversations, messages, personas, projects, skills, memory_entries, etc.) against tenant-scoped tables, exposing cross-tenant aggregates without tenant_id filtering.
  - _Fix:_ Limit these queries to an appropriate admin-only route or add WHERE tenant_id = <tenantId> when returning per-tenant numbers. For global/telemetry aggregates, use a purpose-built job with explicit admin gating and auditing rather than ad-hoc unscoped selects in request handlers.
- **server/routes.ts:766** — backfillProjectDriveFolders performs SELECT p.id, p.name, p.tenant_id, t.name FROM projects JOIN tenants WHERE p.drive_folder_id IS NULL without any tenant_id restriction, iterating projects across all tenants.
  - _Fix:_ If this backfill is intended to act globally, run it as an explicitly privileged maintenance job (separate process or admin-only endpoint) and ensure it cannot be invoked by arbitrary callers. If it should be tenant-scoped, add a WHERE p.tenant_id = <tenantId> or iterate per-tenant using a trusted admin context.
- **server/routes.ts:2059** — GET /api/presenter/:token queries presenter_sessions by token without tenant_id scoping (SELECT ... FROM presenter_sessions WHERE token = ${token}). This returns tenant-associated session data based solely on a token and reads the tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Treat tokens as capability proofs if intended — document and ensure tokens are sufficiently unguessable and that returning tenant data is acceptable. Alternatively, include a tenant_id constraint when possible or validate that the token is only usable for the tenant making the request. If this endpoint is intentionally public, ensure the design is reviewed and logged as an explicit capability path.
- **server/routes.ts:2091** — GET /api/presenter/:token/slide/:index resolves session by token (SELECT id FROM presenter_sessions WHERE token = ${token}) with no tenant scoping. This reads presenter_sessions (tenant-scoped) solely by token.
  - _Fix:_ Same mitigation as above: either enforce tenant scoping or make the capability model explicit and ensure tokens are securely issued/rotated and access is audited. Consider storing a tenant-bound session key and verifying tenant ownership before returning slides.
- **server/routes.ts:2174** — POST /api/presenter/:token/repair-images uses SELECT id, presentation_id, slides FROM presenter_sessions WHERE token = ${token} without tenant filtering. The handler then writes presenter_slide_images using session_id but doesn't validate tenant ownership of the session beyond the token check.
  - _Fix:_ If token-based access is deliberate, ensure the token is an explicit capability and protect issuance/validation. Otherwise add tenant scoping (verify the caller's tenant owns the presenter_session) before performing repair writes. For internal endpoints, require a platform-admin/internal auth and document the trusted relationship.
- **server/routes/goal-ledger.ts:92** — readActiveJobs() runs a SELECT on agent_jobs without a tenant_id predicate (SELECT ... FROM agent_jobs j LEFT JOIN tenants t ... WHERE j.status IN ('pending','running')). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Add an explicit tenant filter or make the function accept a tenantId parameter. If this is only intended for platform-admin dashboards, document and gate it with requirePlatformAdmin and/or return aggregated info instead of raw per-tenant rows. Example: WHERE j.status IN (...) AND j.tenant_id = ${tenantId} (or pass ADMIN_TENANT_ID explicitly if intentionally global and ensure the admin-only guard is enforced).
- **server/routes/memory.ts:162** — Query returning linked memory entries composes tenantClause as sql`TRUE` when tenantId === ADMIN_TENANT_ID. That yields a SELECT from memory_entries without a tenant_id predicate (i.e. cross-tenant read) under the admin tenant context.
  - _Fix:_ Make the cross-tenant behavior explicit and gate it with a platform-admin check (requirePlatformAdmin) or restrict results to a supplied tenantId. If platform-admin access is intended, require and verify requirePlatformAdmin(req,res) before allowing tenantClause === TRUE; otherwise always include eq(memory_entries.tenant_id, tenantId) in the WHERE clause.
- **server/routes/metrics.ts:63** — SELECT aggregates from agent_cost_ledger (GROUP BY tenant_id) are executed without a tenant_id predicate. agent_cost_ledger is tenant-scoped and this SELECT reads across tenants.
  - _Fix:_ If per-tenant aggregation is intended for operator metrics, keep but ensure this endpoint is strictly access-controlled (it already uses METRICS_TOKEN). Otherwise, restrict queries to a single tenant by adding WHERE tenant_id = ${tenantId} or explicitly document and justify the global read. Consider performing aggregation inside an audited admin-only path.
- **server/routes/metrics.ts:79** — SELECT tool usage counts FROM agent_cost_ledger has no tenant_id predicate and therefore reads tenant-scoped rows across all tenants.
  - _Fix:_ Same as above: either add tenant scoping (WHERE tenant_id = ${tenantId}) or ensure the global aggregation is explicitly intended and strictly guarded/audited. Consider returning aggregated-only metrics without row-level details if least privilege is desired.
- **server/routes/metrics.ts:95** — SELECT token totals FROM agent_cost_ledger is executed without any tenant_id constraint (reads across tenants).
  - _Fix:_ Either scope the query to a tenant by adding WHERE tenant_id = ${tenantId}, or explicitly document and gate the global aggregation (METRICS_TOKEN protection). Consider minimizing exposure by returning only safe aggregated totals.
- **server/routes/metrics.ts:113** — SELECT from repair_incidents (GROUP BY classification, action) runs without a tenant_id predicate, reading tenant-scoped data across tenants.
  - _Fix:_ If these repair_incidents are tenant-scoped, add tenant filtering (WHERE tenant_id = ${tenantId}) or otherwise ensure this global read is intentional and strongly access-controlled and logged. Prefer aggregated-only views where possible.
- **server/routes/oauth-subscriptions.ts:325** — Cron heal step runs SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE without any tenant constraint, reading tenant-scoped subscription rows across all tenants.
  - _Fix:_ This query is inherently cross-tenant (it enumerates tenants to heal). Make this explicit and ensure the endpoint is protected by a strong secret and audited. If the intent is to limit scope, add constraints or a configuration-driven tenant whitelist. Alternatively, move tenant enumeration into a clearly documented, platform-admin-only maintenance path.
- **server/seed.ts:4100** — UPDATE agent_knowledge SET ... WHERE id = ${rows[0].id} does not include a tenant_id check. agent_knowledge is tenant-scoped (there is an index on tenant_id). Relying only on the id without asserting tenant ownership risks touching the wrong tenant's row.
  - _Fix:_ Include tenant ownership in the WHERE clause. Example: UPDATE agent_knowledge SET content = ..., priority = 10, updated_at = NOW() WHERE id = ${rows[0].id} AND tenant_id = 1. Alternatively, fetch and verify the row's tenant_id before updating.
- **server/tools.ts:4541** — INSERT into project_notes omits tenant_id: `INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...)` — writing to a project-scoped/tenant-scoped table without explicitly setting tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${projectId}, ...)`) or otherwise ensure the row is written with the caller's tenant_id. Do not rely solely on a prior assert for isolation — the row should carry tenant_id to make application-level filters unambiguous.
- **server/tools.ts:4545** — INSERT into project_files omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...)` — no tenant_id column set on write to a project-scoped table.
  - _Fix:_ Add tenant_id to the inserted columns and values (`tenant_id = ${params._tenantId}`) so the new project_files row is explicitly scoped to the caller's tenant. Alternatively, if the table is intended to be tenant-scoped, ensure all code paths include tenant_id in INSERTs.
- **server/tools.ts:6393** — INSERT into project_files omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` — writing a project-related row without explicit tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `VALUES (${params._tenantId}, ${params.project_id}, ...)`) or otherwise ensure tenant_id is persisted on project_files rows so application-level tenant filters can enforce isolation reliably.
- **server/tools.ts:6927** — INSERT into project_files omits tenant_id: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` — no tenant_id supplied on insert into project-scoped table.
  - _Fix:_ Persist the caller's tenant_id as part of the INSERT (add tenant_id to the column list and use `${params._tenantId}` in VALUES) or ensure a documented invariant that tenant_id is always stored on this table; prefer explicit tenant_id columns and WHERE filters for all tenant-scoped writes/reads.
- **server/video-job-runner.ts:208** — isCancelRequested queries videoJobs by jobId only (WHERE jobId = ?) and does not verify/require the tenant_id. Reading a tenant-scoped table without tenant scoping can leak or act on other tenants' rows.
  - _Fix:_ Require the caller/runner to pass tenantId or fetch the row and validate that videoJobs.tenantId === expectedTenantId before returning the cancel flag. Alternatively include tenant filter in the WHERE clause (eq(videoJobs.tenantId, tenantId)).
- **server/watchlist.ts:16** — addWatchlistItem uses db.execute(sql`... ${params...} ...`) string-interpolated SQL with request-derived values (tenantId, name, category, etc.). This pattern increases injection and isolation risk and bypasses Drizzle's higher-level typed query APIs.
  - _Fix:_ Use Drizzle's query builder (e.g. db.insert(watchlistItems).values(...).returning()) or ensure proper parameterization and explicit tenant handling. Validate and canonicalize request-derived inputs before including them in SQL templates.
- **server/watchlist.ts:70** — getDueWatchlistItems builds and executes a raw SQL string with ${tenantId} interpolation. Using string-interpolated SQL with request-derived values is an injection/isolation risk.
  - _Fix:_ Replace raw SQL with the Drizzle query builder (db.select().from(...).where(eq(...))). If raw SQL is necessary, ensure values are bound safely and not concatenated as strings; validate tenantId is an integer.
- **server/watchlist.ts:111** — getAlerts constructs SQL via sql template concatenation and interpolates request-derived filters (tenantId, filters.watchlistItemId, filters.acknowledged, limit). This dynamic string-interpolated SQL increases injection risk and can lead to incorrect tenant isolation when constructed incorrectly.
  - _Fix:_ Build the query using the Drizzle query builder with explicit where clauses added conditionally (e.g. .where(eq(...))). Avoid assembling SQL through nested sql`` templates for request parameters; use parameter binding or query-builder APIs.
- **server/watchlist.ts:191** — scanWatchlistItem runs a raw UPDATE with ${JSON.stringify(allResults.slice(0,20))} interpolated into the SQL template. Inserting request-derived JSON via string interpolation is brittle and risky.
  - _Fix:_ Use parameter binding or Drizzle's update API to store JSONB (e.g. db.update(watchlistItems).set({ lastResults: jsonValue }).where(...)). Validate/sanitize the JSON payload before persisting.
- **server/watchlist.ts:95** — createAlert constructs an INSERT via db.execute(sql`... ${params...} ...`) and interpolates many request-derived fields (tenantId, watchlistItemId, title, summary, matchedKeywords, etc.). This is string-interpolated SQL with request-controlled data.
  - _Fix:_ Use Drizzle's insert API (db.insert(watchlistAlerts).values(...).returning()) or ensure proper prepared-statement parameterization and input validation rather than inline SQL templates.
- **server/watchlist.ts:50** — removeWatchlistItem performs raw DELETE queries via sql templates with interpolated tenantId and itemId. Using string-interpolated SQL with request-derived values is an injection/isolation risk.
  - _Fix:_ Use the query builder (db.delete(...).where(...)) or ensure values are bound safely and validated before execution.
- **server/webhook-dedupe.ts:80** — claimWebhookEvent uses db.execute(sql`INSERT ... VALUES (${provider}, ${eventId}) ON CONFLICT ...`) with provider and eventId directly interpolated into the SQL template (both are request-derived). This is string-interpolated SQL and risks injection/isolation issues.
  - _Fix:_ Use parameterized queries via Drizzle insert helpers (db.insert(...).values(...)) or ensure proper binding of provider/eventId rather than interpolating them into a raw SQL template.
- **server/webhook-dedupe.ts:90** — claimWebhookEvent and related lookups use db.execute(sql`SELECT completed_at FROM webhook_events WHERE provider = ${provider} AND event_id = ${eventId}`) with request-derived values interpolated into raw SQL templates.
  - _Fix:_ Use the Drizzle query builder to SELECT with bound parameters (e.g. db.select({...}).from(...).where(and(eq(...), eq(...)))) to avoid accidental injection or mis-scoped queries.
- **server/webhook-dedupe.ts:108** — markWebhookEventCompleted issues an UPDATE via db.execute(sql`UPDATE webhook_events SET completed_at = now() WHERE provider = ${provider} AND event_id = ${eventId}`) interpolating request-derived values into the SQL template.
  - _Fix:_ Use Drizzle's update API with bound parameters (db.update(...).set(...).where(...)) or otherwise ensure values are safely parameterized and validated before use.
- **server/webhook-triggers.ts:89** — createTrigger inserts into webhook_triggers using db.execute(sql`INSERT ... VALUES (${config.name}, ${config.description || ''}, ${webhookKey}, ${config.personaId || null})`) with request-derived values interpolated into a raw SQL template.
  - _Fix:_ Use Drizzle's insert API (db.insert(webhookTriggers).values(...).returning()) or otherwise parameterize inputs safely, and validate/escape user-provided strings.
- **server/webhook-triggers.ts:122** — processTriggerEvent selects the trigger via raw SQL: db.execute(sql`SELECT ... WHERE w.webhook_key = ${webhookKey} AND w.enabled = true`). webhookKey is request-derived and is interpolated into the SQL template.
  - _Fix:_ Use a parameterized query (drizzle select builder) to fetch the trigger by webhook_key, and validate the key format before using it in a query.
- **server/webhook-triggers.ts:151** — processTriggerEvent inserts event rows via db.execute(sql`INSERT INTO webhook_trigger_events (trigger_id, payload, response_preview, status) VALUES (${trigger.id}, ${JSON.stringify(payload)}::jsonb, ${responsePreview}, 'success')`). The payload is request-derived and is interpolated into the SQL template.
  - _Fix:_ Use Drizzle insert with a bound JSON value (db.insert(...).values({ payload: allResultsJson })) or pass JSON as a parameter rather than interpolating JSON.stringify(...) into raw SQL. Validate payload size and content before persisting.

### Suppressed — confirmed false positives (237)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE across tenants: UPDATE ai_insights SET status='applied', action_taken=... WHERE status='new' AND category IN (...) RETURNING id, tenant_id,... — no tenant_id condition. This will modify rows for all tenants.
  - _Why false positive:_ Global maintenance backfill intentionally sweeps all tenants and then processes rows with their own tenant_id; no tenant-facing mixed output shown here.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE on tenant-scoped table `agent_approvals` without tenant constraint: UPDATE agentApprovals SET status='expired', ... WHERE status = 'pending' AND expiresAt < now(). This will expire pending approvals across all tenants in one unscoped statement.
  - _Why false positive:_ Expiry sweep intentionally updates stale pending approvals across tenants and only performs housekeeping/status transitions with per-row tenantId carried for follow-up run updates.
- **server/agentic/resume-worker.ts:111** _(upstream-verified)_ — SQL string interpolation embeds request-derived values (decidedBy / note) directly into the SQL fragment in the UPDATE statement, producing a SQL-injection risk and violating safe parameterization/isolation rules.
  - _Why false positive:_ The UPDATE is scoped by both id and tenant_id taken from the candidate row selected earlier; this finding is about interpolation, not a cross-tenant isolation gap.
- **server/auto-asset-capture.ts:145** _(fk-transitive)_ — INSERT INTO project_files is executed without providing tenant_id. project_files is project-scoped/tenant-scoped and the INSERT relies on transitive ownership via project_id rather than explicitly setting tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-isolated transitively via project_id.
- **server/auto-asset-capture.ts:164** _(fk-transitive)_ — INSERT INTO project_notes is executed without tenant_id. The write to a tenant-scoped table (project_notes) relies on transitive project ownership instead of setting tenant_id on the row.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated transitively via project_id.
- **server/auto-memorize.ts:163** _(system-sweep)_ — SELECT from messages (SELECT m.tenant_id, m.role, ...) is executed without constraining to the caller's tenant. It reads messages across tenants and relies on application partitioning after the read — this violates tenant isolation invariants.
  - _Why false positive:_ This is a platform-wide sweep that partitions rows by row-carried tenant_id before per-tenant processing, explicitly avoiding mixed-tenant prompts.
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT FROM messages by conversation_id is executed without a tenant_id filter. Although the conversation row was previously fetched with a tenant check, the messages query itself is not scoped and could read messages not constrained by tenant_id.
  - _Why false positive:_ Conversation ownership was already proven just above with WHERE id = conversationId AND tenant_id = tenantId before reading messages for that same conversation.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT FROM messages WHERE conversation_id = ${conversationId} is executed without a tenant_id constraint. Even though the code previously asserted conversation ownership, the messages read itself is unscoped SQL against a tenant-scoped table.
  - _Why false positive:_ The same call path first asserts conversation ownership and re-selects the conversation with tenant_id = tenantId before reading its messages.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL has no tenant_id constraint. This can modify a conversation owned by another tenant if IDs are misused or poisoned.
  - _Why false positive:_ Backfill loop is a maintenance sweep; candidate rows come from joins that pair conversation_id with its own tenant_id and matching project tenant, and the update only links housekeeping project_id before per-row tenant-scoped transcript save.
- **server/data-protection.ts:168** _(system-sweep)_ — permanentlyPurgeSoftDeleted deletes conversations with DELETE FROM conversations WHERE id = ${conv.id} without scoping by tenant_id. This can remove a conversation across tenant boundaries.
  - _Why false positive:_ platform purge sweep selects expired conversations with row-carried tenant_id and performs housekeeping deletion, not tenant-facing data mixing
- **server/embeddings.ts:758** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} in backfillEmbeddingVecs() lacks a tenant_id constraint — allows writing to other tenants' rows.
  - _Why false positive:_ backfill is a platform-wide maintenance job updating only embedding housekeeping fields, with no tenant-facing output or content mixing
- **server/embeddings.ts:775** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} in backfillEmbeddingVecs() lacks a tenant_id constraint — allows cross-tenant writes.
  - _Why false positive:_ backfillEmbeddingVecs is maintenance/backfill work that mutates only embedding housekeeping fields across tenants
- **server/firecrawl.ts:222** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' uses WHERE eq(scrapedPages.id, existing[0].id) but lacks an explicit tenant_id constraint. The update can modify another tenant's row if IDs overlap.
  - _Why false positive:_ the row id being updated was selected immediately above with matching url and tenant_id, proving tenant ownership in the same call path
- **server/firecrawl.ts:353** _(upstream-verified)_ — UPDATE on tenant-scoped table 'scraped_pages' inside the crawl loop uses WHERE eq(scrapedPages.id, existing[0].id) without tenant scoping. This permits cross-tenant modification.
  - _Why false positive:_ existing[0].id comes from the immediately preceding SELECT scoped by tenant_id and url, so ownership is verified upstream in-path
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects SET driveFolderId = ... WHERE id = ${projectId} is executed without a tenant_id constraint. This is a cross-tenant write risk (can mutate another tenant's project).
  - _Why false positive:_ same function first loads project by id and throws on tenant mismatch before performing the update
- **server/heartbeat.ts:385** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... has no tenant_id constraint and will return pending events across all tenants. Processing these rows without per-tenant scoping risks cross-tenant action and data exposure.
  - _Why false positive:_ background heartbeat sweep processes pending rows across tenants but uses each row's tenant_id when routing and does not return mixed tenant data
- **server/lib/scheduled-post-runner.ts:63** _(system-sweep)_ — SELECT (and consequent UPDATE) over scheduled_posts in the CTE is executed with no tenant_id constraint — this locks/updates rows across all tenants rather than scoping to a caller tenant.
  - _Why false positive:_ global scheduler intentionally locks due rows across tenants and returns each row with its own tenant_id for per-row handling
- **server/memory-graph.ts:253** _(fk-transitive)_ — INSERT into tenant-scoped table `memory_links` does not include `tenant_id` (or otherwise constrain the insert to the caller's tenant). This allows creation of cross-tenant links.
  - _Why false positive:_ memory_links has no tenant_id; links are between memory_entries, which are tenant-scoped transitively by referenced parent rows
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — validateSubscriptionsOnStartup issues a SELECT from tenant-scoped table `oauth_subscriptions` with no tenant_id WHERE filter (reads all tenants' subscription rows). This exposes cross-tenant subscription data (including encrypted tokens) to a non-tenant-constrained query.
  - _Why false positive:_ startup validation is a platform-wide maintenance loop over subscription rows, handled per row by tenant_id rather than exposed to tenants
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — Query `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'` runs without any tenant_id restriction (returns rows across all tenants).
  - _Why false positive:_ startup auto-reconnect intentionally enumerates tenant ids across subscriptions for maintenance and then processes each tenant separately
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh issues `SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant_id filter — iterates all tenants' subscriptions and acts on them.
  - _Why false positive:_ proactive token refresh is a background maintenance sweep over all active subscriptions with per-row tenant handling only
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions runs `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE` without tenant scoping, reading subscription metadata for all tenants in one query.
  - _Why false positive:_ force-refresh is an internal cron-style maintenance loop across tenants and uses per-row tenant_id for each refresh action
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE to tenant-scoped table `memory_entries` without tenant constraint: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}` omits tenant_id (and persona_id) in the WHERE clause, permitting modification of another tenant's memory entry by id.
  - _Why false positive:_ storedId comes from an immediately prior SELECT on memory_entries constrained by tenant_id and persona_id in the same call path
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge is executed without a tenant_id WHERE clause. agent_knowledge is tenant-scoped elsewhere, so this will delete entries across all tenants.
  - _Why false positive:_ global expiry purge only deletes expired knowledge rows as housekeeping and returns no tenant data
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id — INSERT INTO project_notes (project_id, note, author) ...
  - _Why false positive:_ project_notes has no tenant_id and insert is gated by prior tenant-scoped project ownership check
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id — INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size) ...
  - _Why false positive:_ project_files has no tenant_id and insert is tied to a project already verified for caller tenant
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multipart upload path) does not include tenant_id — INSERT INTO project_files (...) VALUES (...)
  - _Why false positive:_ project_files has no tenant_id and insert is tied to a project already verified for caller tenant
- **server/self-improvement.ts:401** _(internal-worker-pk)_ — runExperiment updates the tenant-scoped `experiments` row using only experiments.id in the WHERE clause (WHERE eq(experiments.id, experiment.id)). There is no tenant_id constraint, allowing modification of another tenant's row if IDs collide or are guessed.
  - _Why false positive:_ worker updates experiment by server-side primary key within internal runExperiment flow, not directly from request input
- **server/self-improvement.ts:415** _(internal-worker-pk)_ — In the error-handling path runExperiment sets status='error' using an UPDATE that filters only by experiments.id (WHERE eq(experiments.id, experiment.id)). This write lacks a tenant_id constraint and can modify another tenant's data.
  - _Why false positive:_ error-path worker update uses internal experiment primary key from same server-run flow, not untrusted tenant boundary input
- **server/self-reflection.ts:366** _(upstream-verified)_ — storeLesson updates memory_entries by id only (UPDATE memory_entries SET ... WHERE id = <id>) without also constraining tenant_id. This allows updating memory entries belonging to other tenants.
  - _Why false positive:_ row id being updated was selected immediately above with tenant_id = tenantId, so ownership is enforced in same call path
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories executes a raw UPDATE on tenant-scoped `memory_entries` (UPDATE memory_entries SET status='archived' WHERE ... AND expires_at <= NOW()) with no tenant_id constraint — this will modify all tenants' rows indiscriminately.
  - _Why false positive:_ platform-wide archival job only mutates status on expired rows and returns aggregate count/ids, with no tenant-facing data mixing
- **server/storage.ts:857** _(system-sweep)_ — archiveStaleMemories executes a raw UPDATE on tenant-scoped `memory_entries` without tenant_id filtering (UPDATE memory_entries SET status='archived' WHERE ...), affecting all tenants.
  - _Why false positive:_ platform-wide stale-memory archival only changes housekeeping status fields and does not expose mixed tenant content
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` (incrementing usageCount) uses only eq(customTools.id, tool.id) without a tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ tool row was selected immediately above with tenant_id = caller tenantId, and update uses that verified row id
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys performed without any tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). This returns provider API key data across all tenants.
  - _Why false positive:_ provider_keys schema shows no tenant_id and query is explicitly gated to admin tenantId === 1
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM provider_keys executes without tenant_id filtering. This enumerates provider key configuration across tenants.
  - _Why false positive:_ provider_keys has no tenant_id and distinct-provider count is an admin-only platform-wide audit
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` performed without a tenant_id constraint (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across tenants.
  - _Why false positive:_ heartbeat maintenance loop intentionally scans running experiments across tenants and processes each row with its own tenant_id
- **server/agent-activity.ts:60** _(global-table)_ — SELECT from `personas` by persona id is missing tenant scoping (SELECT { emoji, catchphrase } FROM personas WHERE id = personaId). This reads a persona row without validating that the persona belongs to the caller's tenant.
  - _Why false positive:_ personas schema shows no tenant_id and comments state personas are platform-shared
- **server/agent-desk.ts:297** _(global-table)_ — Query selects all `personas` with no tenant filter (db.execute(sql`SELECT id, name FROM personas`)). This reads persona rows across tenants when building desks overview.
  - _Why false positive:_ personas schema shows no tenant_id; this read joins global persona names onto tenant-scoped agent_desks.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_evals` omits tenant constraint: UPDATE agent_evals SET ... WHERE id = ${evalId}. A caller could update an eval row that belongs to another tenant by id alone.
  - _Why false positive:_ evalId is server-generated from the immediately preceding INSERT RETURNING id, then updated by primary key within same internal flow.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` performed without tenant filter in retryPendingMinervaRouting (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ... LIMIT 50). This reads across tenants.
  - _Why false positive:_ global durability sweep intentionally scans across tenants, carries each row's tenant_id, and updates per-row with matching tenant_id without tenant mixing in output.
- **server/agentic-engines.ts:344** _(global-table)_ — In gatherOperationalData a query on `heartbeat_logs` lacks tenant scoping: SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'. This aggregates heartbeat logs across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; aggregate is from a platform-global telemetry table.
- **server/agentic-engines.ts:377** _(global-table)_ — SELECT from `personas` is missing tenant scope: SELECT name, role FROM personas WHERE is_active = true LIMIT 14. This reads personas across tenants.
  - _Why false positive:_ personas schema shows no tenant_id, so active persona list is from a non-tenant-scoped global table.
- **server/agentic-engines.ts:519** _(global-table)_ — SELECT on `heartbeat_logs` in runOptimizationEngine is not tenant-scoped (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This aggregates heartbeat logs across all tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; this is aggregation over a platform-global log table.
- **server/agentic-features.ts:563** _(upstream-verified)_ — UPDATE lead_enrichments omits tenant scoping: UPDATE lead_enrichments SET icp_score = ..., notes = ... WHERE id = ${lead.id}. This can update another tenant's row by id alone.
  - _Why false positive:_ updated lead.id values come from prior tenant-scoped SELECT of lead_enrichments for params.tenantId in the same flow.
- **server/agentic-features.ts:797** _(upstream-verified)_ — UPDATE outreach_enrollments performed without tenant scoping: UPDATE outreach_enrollments SET reply_classification = ..., status = ... WHERE id = ${row.id}. The update does not ensure the enrollment belongs to the caller's tenant.
  - _Why false positive:_ row.id is obtained from the immediately preceding SELECT filtered by e.tenant_id = params.tenantId in the same call path.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query scans tenant-scoped tables (agent_runs, agent_approvals) with no tenant_id constraint — the worker reads rows across all tenants.
  - _Why false positive:_ platform-wide worker sweep intentionally scans pending approvals across tenants and processes each candidate with its own carried tenant_id; not tenant-facing output.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() updates the tenant-scoped agent_runs table but the WHERE clause only filters by run id (eq(agentRuns.id, runId)) — no tenant_id constraint, allowing cross-tenant updates if runId is known.
  - _Why false positive:_ Internal worker helper mutates agent_runs by server-generated PK runId; shown call path creates run server-side and passes run.id internally, not from request boundary.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs but restricts only by run id (eq(agentRuns.id, runId)) and does not include a tenant_id filter, permitting cross-tenant state mutation.
  - _Why false positive:_ Internal state-transition helper updates agent_runs by globally unique server-generated runId, with no evidence of untrusted caller-controlled id input in shown path.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs using only run id in the WHERE clause (eq(agentRuns.id, runId)) with no tenant_id constraint.
  - _Why false positive:_ Internal replace-state helper operates on agent_runs via server-generated primary key, not a tenant-facing id lookup in the shown code.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs (status/result/completedAt) but the WHERE clause filters only by run id (eq(agentRuns.id, runId)) — missing tenant_id constraint.
  - _Why false positive:_ Completion helper is an internal worker/state transition on agent_runs by server-generated runId; subsequent read also uses same internal PK.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs to mark a run failed but uses only run id in the WHERE clause (eq(agentRuns.id, runId)) with no tenant_id constraint.
  - _Why false positive:_ Failure-state helper updates by server-side runId only; this matches internal worker PK mutation rather than tenant-facing access.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() updates agent_runs (status/state) but the WHERE clause filters only by run id (eq(agentRuns.id, runId)) — missing tenant_id constraint.
  - _Why false positive:_ Pause helper is part of internal run lifecycle management using server-generated runId, not an untrusted cross-tenant request parameter in shown code.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — runDueWakes performs an UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) — the inner SELECT does not filter by tenant_id. This claims/updates rows across all tenants in a tenant-scoped table.
  - _Why false positive:_ Platform-wide scheduler intentionally claims due rows across tenants, then processes each row with its own tenant_id and emits per-row tenant-scoped events; no mixed tenant output.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. Because there is no DB-level RLS or FK cascade enforcement, this can create line-items that are not explicitly tied to the caller's tenant and may allow cross-tenant data association or orphaned rows.
  - _Why false positive:_ invoice_items has no tenant_id column and is a child table isolated through tenant-scoped parent invoices via invoice_id; insert without tenant_id is by design.
- **server/channel-routing.ts:31** _(global-table)_ — listChannelRoutes SELECT joins channel_routes to personas with no tenant constraint. If personas (or channel_routes) are tenant-scoped this query can return data across tenants.
  - _Why false positive:_ channel_routes is created here without tenant_id and personas also lacks tenant_id; routing appears intentionally global/platform-wide
- **server/channel-routing.ts:48** _(global-table)_ — setChannelRoute performs DELETE and INSERT/ON CONFLICT against channel_routes without scoping by tenant and the INSERT does not set tenant_id. If the table is meant to be tenant-scoped, this allows cross-tenant modification and orphaned global routing entries.
  - _Why false positive:_ setChannelRoute mutates channel_routes, a table defined here as global with UNIQUE(channel) and no tenant scoping
- **server/channel-routing.ts:63** _(global-table)_ — getPersonaForChannel queries channel_routes for persona_id by channel without any tenant filter. This can leak or return a persona from a different tenant.
  - _Why false positive:_ getPersonaForChannel reads from global channel_routes keyed only by channel; no tenant_id exists in shown schema
- **server/chat-engine.ts:4408** _(upstream-verified)_ — Call to storage.attachCitations(...) is made without passing a tenant identifier. If attachCitations performs INSERT/UPDATE against a tenant-scoped table, the write may not constrain the row to the caller's tenant and could enable cross-tenant association or tampering.
  - _Why false positive:_ assistantMsgRow is created immediately above with tenantId on messages; attachCitations is by freshly created message id in same call path
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` (scanAndEscalate) has no tenant_id constraint — it selects overdue commitments across all tenants. Unscoped reads of tenant-scoped data can expose tenant-specific information and risk cross-tenant data access.
  - _Why false positive:_ platform-wide heartbeat scanner intentionally scans all tenants, handles each row with its own tenantId, and redacts tenant content in emitted digest
- **server/data-protection.ts:359** _(fk-transitive)_ — backupTenantDataToDrive queries project_notes using WHERE project_id = ${proj.id} with NO tenant_id constraint. This reads another table (project_notes) using a foreign id without ensuring the row belongs to the tenant.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects already filtered by tenant_id before lookup by project_id
- **server/data-protection.ts:371** _(fk-transitive)_ — backupTenantDataToDrive queries project_files using WHERE project_id = ${proj.id} with NO tenant_id constraint. Child records are read by foreign id without verifying tenant ownership.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects already filtered by tenant_id before lookup by project_id
- **server/deep-interview.ts:337** _(fk-transitive)_ — saveInterviewToKnowledge performs INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ... but does not set tenant_id on the inserted row. If project_notes is tenant-scoped (tenant_id NOT NULL/no default), this either misattributes ownership or violates the invariant that INSERTs must pass tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id and is inserted via project_id selected from tenant-scoped projects with p.tenant_id = state.tenantId.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake performs an UPDATE on dfy_intake_forms (.where(eq(dfyIntakeForms.id, form.id))) with no tenant_id constraint. This allows updating a form row belonging to another tenant by id.
  - _Why false positive:_ submitDfyIntake updates the row fetched immediately above by token; the trusted DB-loaded form.id is reused, not caller-supplied arbitrary id.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument deletes doc_chunks with DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but omits tenant_id. This can remove chunks belonging to other tenants' collections having the same collection_id/doc_path (or cause unintended cross-tenant effects).
  - _Why false positive:_ same function first verifies collection ownership with SELECT id FROM doc_collections WHERE id = collectionId AND tenant_id = tenantId before deleting chunks for that collection.
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks embedding using WHERE id = ${row.id} without constraining tenant_id. Updating by id alone may modify another tenant's row if ids are not globally unique or if an attacker can influence ids.
  - _Why false positive:_ rows being updated were selected just above with WHERE collection_id = collectionId AND tenant_id = tenantId AND embedding IS NULL; update uses those verified row ids.
- **server/dream-consolidation.ts:83** _(upstream-verified)_ — getRecentSessionSummaries selects messages for a conversation with SELECT role, content FROM messages WHERE conversation_id = ${conv.id} but does not include tenant_id. messages is a tenant-scoped resource — reading by conversation_id without tenant scoping can return another tenant's messages if conversation ids are not globally protected.
  - _Why false positive:_ conversation ids are sourced from a prior query scoped by conversations.tenant_id = tenantId in the same function, then messages are read for those verified conversations.
- **server/email.ts:528** _(system-sweep)_ — backfillEmptyBodies updates inbox_messages with UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} but omits tenant_id in the WHERE clause. This can update another tenant's row by id.
  - _Why false positive:_ backfillEmptyBodies is a platform-wide maintenance backfill that only fills body_text/body_html on each inbox_messages row and does not return or mix tenant data to tenants.
- **server/embeddings.ts:720** _(upstream-verified)_ — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings() has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ the shown code explicitly refuses operation when opts.tenantId is absent and calls storage.getKnowledge with tenantId.
- **server/embeddings.ts:748** _(system-sweep)_ — SELECT FROM memory_entries in backfillEmbeddingVecs() has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ storeEmbeddingVec/backfill updates embedding housekeeping fields by server-side primary key during maintenance; no tenant-facing data is returned or mixed.
- **server/embeddings.ts:764** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillEmbeddingVecs() has no tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ backfillMissingKnowledgeEmbeddings is a cross-tenant maintenance loop generating embeddings and mutating only embedding fields, with no tenant-facing data exposure or mixed output.
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' without any tenant_id constraint (SELECT ... FROM agent_runs WHERE status IN ...). This reads in-flight runs for all tenants.
  - _Why false positive:_ boot-time rehydration intentionally scans running agent_runs across tenants and rebuilds per-row in-memory state keyed from each row's own tenant-carried state; not a tenant-facing mixed output.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images does not set tenant_id; session_id is coming from an external presenter service response and not validated here.
  - _Why false positive:_ presenter_slide_images is a child table keyed by session_id under tenant-scoped presenter_sessions; no tenant_id on child insert is consistent with transitive isolation
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from code_proposals filters only by status/created_at, but contains no tenant_id constraint. This reads tenant-scoped data globally and the subsequent UPDATE (archival) operates without tenant scoping.
  - _Why false positive:_ global TTL audit sweep intentionally scans stale code_proposals across tenants for housekeeping/admin findings, not tenant-facing mixed output
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}) — no tenant_id constraint. This can update proposals from any tenant if ids are shared across tenants.
  - _Why false positive:_ bulk archival of stale proposals is cross-tenant housekeeping updating only status fields from rows selected by the maintenance sweep
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL ... LIMIT 100 — no tenant_id constraint. heartbeat_tasks appear to be tenant-scoped in other code paths and this may disable tasks across tenants.
  - _Why false positive:_ maintenance sweep intentionally scans heartbeat tasks across tenants to find stuck rows and handles each row independently
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — no tenant_id constraint. This will disable heartbeat tasks across tenants sharing IDs.
  - _Why false positive:_ disabling stale heartbeat tasks is a platform-wide housekeeping mutation of status only, driven by the sweep-selected rows
- **server/heartbeat.ts:53** _(system-sweep)_ — Raw SELECT from research_schedules has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads schedule rows across all tenants without an explicit tenant filter.
  - _Why false positive:_ heartbeat scheduler intentionally scans due research_schedules across tenants and each row carries its own tenant_id for per-row execution
- **server/heartbeat.ts:103** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} lacks a tenant_id constraint. The UPDATE relies solely on id without verifying the row belongs to the expected tenant.
  - _Why false positive:_ maintenance scheduler updates only the current schedule row's timing fields after processing that row's own tenant-scoped work
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} is missing a tenant_id constraint.
  - _Why false positive:_ scheduler advances last_run_at/next_run_at for the processed schedule row only; this is per-row housekeeping in a cross-tenant sweep
- **server/heartbeat.ts:273** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} updates a tenant-scoped table row by id only, without verifying tenant_id.
  - _Why false positive:_ startup cleanup normalizes heartbeat task model values platform-wide and only mutates housekeeping/config fields on each processed row
- **server/heartbeat.ts:277** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} modifies a tenant-scoped table row without a tenant_id constraint.
  - _Why false positive:_ startup cleanup updates reflection task model as a platform-wide maintenance pass, changing only config fields per row
- **server/heartbeat.ts:1670** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id constraint and will read heartbeat log rows across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is an intentionally platform-global heartbeat log table.
- **server/heartbeat.ts:2764** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} updates a tenant-scoped table without constraining tenant_id.
  - _Why false positive:_ Row was just created via createHeartbeatTask with tenantId and server-generated newTask.id; the immediate update targets that trusted row.
- **server/job-queue.ts:93** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id constraint — the queue UPDATE/RETURNING touches jobs across all tenants.
  - _Why false positive:_ Global worker queue claim intentionally operates across tenants and returns rows with each row's own tenant_id for per-row processing.
- **server/job-queue.ts:131** _(system-sweep)_ — reclaimExpiredLeases updates agent_jobs (marking running->failed_terminal or requeueing) without any tenant_id constraint — this mutates jobs for all tenants indiscriminately.
  - _Why false positive:_ Expired-lease reclaim is platform-wide housekeeping mutating only job status/lease fields across tenants.
- **server/job-queue.ts:172** _(internal-worker-pk)_ — completeJob updates an agent_jobs row (setting status='succeeded') by id/attempts only, with no tenant_id check — a stale or malicious caller could complete jobs belonging to other tenants.
  - _Why false positive:_ completeJob is an internal worker state transition by server-issued job id plus attempts fencing, not a request-bound tenant operation.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob starts by SELECTing max_attempts FROM agent_jobs WHERE id = ${id} with no tenant filter, and later UPDATEs the job (failed vs requeue) without enforcing tenant ownership — allows inspection/mutation of other-tenant jobs.
  - _Why false positive:_ failJob is an internal worker path updating a server-generated job id with attempts fencing, not an untrusted cross-tenant boundary.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Query against tenant-scoped table `projects` lacks a tenant_id constraint — it scans all tenants. The COUNT query (SELECT tenant_id, count(*) ... FROM projects WHERE tags && ... GROUP BY tenant_id) reads tenant-scoped data for every tenant in the DB, violating the invariant that every read must be constrained to the caller's tenant.
  - _Why false positive:_ Boot-time backfill is a platform-wide maintenance import; this cross-tenant count is aggregate housekeeping used to decide whether to seed, not tenant-facing data return.
- **server/lib/inbox-ingest.ts:507** _(upstream-verified)_ — UPDATE to tenant-scoped table `inbox_classifications` performs WHERE inbox_message_id = ${storeResult.id} without constraining by tenant_id. This allows cross-tenant modification if an inbox_message_id from another tenant is used or guessed.
  - _Why false positive:_ Row was just claimed/inserted into inbox_classifications with the same tenantId and storeResult.id; update targets that owned row in same call path.
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — scanOutcomePromotions executes a cross-tenant aggregate SELECT on deliverable_grade_events without constraining by tenant_id (SELECT tenant_id, format, count(*)... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format). This reads tenant-scoped data for all tenants in one query, violating the application's tenant-isolation invariant.
  - _Why false positive:_ Nightly optimizer scan intentionally aggregates across tenants and then processes each grouped row with its own tenant_id; no mixed tenant content in one output.
- **server/memory-graph.ts:240** _(fk-transitive)_ — SELECT against `memory_links` (existence check) does not constrain by tenant_id. Reading a tenant-scoped table without a tenant filter can leak or mix cross-tenant data.
  - _Why false positive:_ memory_links has no tenant_id and links only IDs selected from tenant-scoped memory_entries filtered by tenantId, so isolation is transitive through the parent rows.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_categories` uses WHERE id = ${catId} only and lacks a tenant filter. This can inadvertently update rows belonging to another tenant if ids are mis-bound or incorrect.
  - _Why false positive:_ catId comes from ensureCategory/select scoped by tenant_id = tenantId in the same function path, so the updated category was tenant-verified earlier.
- **server/memory-graph.ts:496** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} lacks a tenant_id constraint. Updating by id alone can modify another tenant's row.
  - _Why false positive:_ mem.id is selected moments earlier from memory_entries WHERE tenant_id = tenantId, so the subsequent update targets rows already tenant-scoped in the same call path.
- **server/pace-control.ts:85** _(global-table)_ — checkPace queries heartbeat_logs for counts with no tenant_id filter (SELECT ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). This reads activity across all tenants and can leak tenant activity/usage data or allow incorrect global caps.
  - _Why false positive:_ heartbeat_logs is schema-noted as non-tenant-scoped and used for platform-wide pace gating, so unscoped counts are intentional.
- **server/pace-control.ts:141** _(global-table)_ — getPaceSnapshot issues an unscoped SELECT against heartbeat_logs (COUNT over a time window) with no tenant_id restriction, returning global activity counts across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and this snapshot is a platform-global operational metric by design.
- **server/pace-control.ts:147** _(global-table)_ — getPaceSnapshot's personaRows query SELECT ... FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name has no tenant_id filter — it aggregates persona usage across all tenants.
  - _Why false positive:_ heartbeat_logs is intentionally global; persona aggregation is over a non-tenant-scoped table.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE on plans to claim a plan (flip status → 'executing') uses WHERE id = ${planId} AND status = 'approved' but omits tenant_id. This reads/updates a tenant-scoped table without applying the caller's tenant constraint.
  - _Why false positive:_ Server-invoked executor claims a plan by global PK planId, not from a tenant request boundary.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status='failed' ... WHERE id = ${planId} — missing tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ This update occurs only after the prior RETURNING fetched the exact plans row including tenant_id for this planId in the same call path.
- **server/plan-executor.ts:728** _(upstream-verified)_ — Final UPDATE on plans to set status/combine execution_log uses WHERE id = ${planId} only (no tenant_id). Tenant-scoped writes must be constrained to the plan's tenant.
  - _Why false positive:_ Final status write is for the already-claimed plan row loaded earlier with tenant_id in the same execution path.
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-path UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' omits tenant_id filtering.
  - _Why false positive:_ Fatal-error cleanup updates the same executing plan by server-side PK within internal worker flow, not from untrusted input.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans UPDATE flips status = 'executing' → 'approved' for all stalled plans matching a time window but omits any tenant filter (UPDATE plans WHERE status='executing' AND updated_at < ...). This globally modifies tenant-scoped rows.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping sweep resetting stale status fields across tenants without returning tenant data.
- **server/plan-executor.ts:787** _(system-sweep)_ — resumeStuckPlans selects pending plans via SELECT id FROM plans WHERE status = 'approved' ... with no tenant_id filter — iterates and requeues plans across all tenants.
  - _Why false positive:_ Boot-time loop intentionally scans approved plans across tenants and requeues each row independently by its own PK.
- **server/process-governor.ts:595** _(upstream-verified)_ — In executeAction -> disable_task the code issues UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without adding tenant_id. The SELECT that discovered t came from tenant-scoped rows, but the UPDATE omits tenant scoping and can affect a row with the same id in another tenant context.
  - _Why false positive:_ Row id comes from a tenant-scoped SELECT on heartbeat_tasks WHERE ht.tenant_id = tenantId earlier in the same path.
- **server/process-governor.ts:641** _(upstream-verified)_ — In executeAction -> restart_agent the code updates agent_desks via WHERE id = ${desk.id} without tenant_id filtering. This mutates a tenant-scoped table without scoping by tenant.
  - _Why false positive:_ desk.id was selected from agent_desks with ad.tenant_id = tenantId immediately above in the same flow.
- **server/project-brain.ts:359** _(system-sweep)_ — In backfillProjectBrains: SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without a tenant filter. Messages are tenant-scoped and this reads data without verifying tenant ownership of conversationId.
  - _Why false positive:_ Boot/backfill loop intentionally scans all tenants and processes each conversation/project independently; no tenant-facing response or cross-tenant mixing beyond per-row maintenance output.
- **server/recurring-messages.ts:258** _(system-sweep)_ — Unscoped SELECT from tenant-scoped table `agent_knowledge`: the query `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` has no tenant_id constraint and returns rows across tenants.
  - _Why false positive:_ Heartbeat runner intentionally scans due jobs platform-wide and carries each row's tenant_id forward for per-row scoped handling.
- **server/research-engine.ts:496** _(fk-transitive)_ — INSERT INTO project_notes (summary) omits tenant_id. Project-scoped tables are tenant-scoped elsewhere and INSERTs must explicitly set tenant_id per invariant.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id; project ownership is also asserted with assertProjectInTenant.
- **server/research-engine.ts:504** _(fk-transitive)_ — Per-finding INSERT INTO project_notes omits tenant_id, allowing notes to be written without explicit tenant attribution.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id; inserts reference a project already verified in-session tenant.
- **server/routes.ts:167** _(system-sweep)_ — Startup restore selects from tenant-scoped file_storage without applying a tenant_id constraint (db.select(...).from(fileStorage).where(...)). This reads rows for all tenants.
  - _Why false positive:_ Startup restore is a platform-wide housekeeping pass restoring blobs to disk; it does not return row data to tenants and only performs maintenance per file row.
- **server/routes.ts:1118** _(signed-url)_ — In /uploads middleware the code queries file_storage by filename without a tenant_id WHERE clause (select owners = db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This reads metadata for all tenants' files and can expose cross-tenant information.
  - _Why false positive:_ Cross-tenant filename scan is used only to verify ownership collisions for asset delivery; access is gated by tenant ownership or signed URL, with delivery assets explicitly authorized by signature.
- **server/routes.ts:4338** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The code inserts a project note with VALUES (${projId}, ${snapshot.slice(0, 500)}, ${'system:context-guard'}) but omits tenant_id, relying only on project_id linkage. In a multi-tenant schema without RLS or FK cascade guarantees this can write notes into the wrong tenant's namespace or create orphaned rows.
  - _Why false positive:_ project_notes has no tenant_id column and is isolated through project_id; project_id is fetched from conversations with tenant_id = ${tenantId} immediately above.
- **server/routes/admin-extra.ts:125** _(upstream-verified)_ — Use of sql-tagged raw query with request-derived values (tenantId, limit) — db.execute(sql`... WHERE tenant_id = ${tenantId} LIMIT ${limit}`) leads to injection/isolation risk even though tenant_id is present.
  - _Why false positive:_ Query is explicitly scoped by tenant_id from authenticated session; first-pass concern is SQL interpolation, not cross-tenant isolation.
- **server/routes/admin-extra.ts:154** _(upstream-verified)_ — Use of sql-tagged raw query with request-derived values (tenantId, status, limit) in self_initiatives listing — interpolation of request data into SQL template.
  - _Why false positive:_ Both branches constrain self_initiatives by tenant_id from the auth session, so no cross-tenant read is present.
- **server/routes/admin-extra.ts:184** _(upstream-verified)_ — UPDATE using sql template with request-derived id and tenantId (sql`UPDATE self_initiatives ... WHERE id = ${id} AND tenant_id = ${tenantId}`) — while tenant_id is present, raw template interpolation of request values risks injection/isolation bugs.
  - _Why false positive:_ UPDATE includes tenant_id = session tenant and id, preventing cross-tenant modification; issue is not tenant isolation.
- **server/routes/admin-extra.ts:216** _(upstream-verified)_ — Multiple db.execute(sql`... ${tenantId}`) calls in account deletion-summary use direct sql templates interpolating tenantId (and other request-derived values) into raw SQL.
  - _Why false positive:_ All reads are constrained by tenantId from getTenantFromRequest, and the tenants table uses id not tenant_id by design.
- **server/routes/admin-extra.ts:254** _(upstream-verified)_ — schedule-deletion performs UPDATE tenants ... WHERE id = ${tenantId} using sql template with request-derived tenantId.
  - _Why false positive:_ UPDATE targets tenants.id = authenticated tenantId only; this is tenant-scoped self-service, not cross-tenant access.
- **server/routes/admin-extra.ts:282** _(upstream-verified)_ — cancel-deletion performs UPDATE tenants ... WHERE id = ${tenantId} via sql template interpolating tenantId from request.
  - _Why false positive:_ Route derives tenantId from authenticated request context and updates only tenants.id for that caller's tenant; no cross-tenant read/write path shown.
- **server/routes/admin-extra.ts:312** _(upstream-verified)_ — Permanent deletion executes many DELETE ... WHERE tenant_id = ${tenantId} via sql templates — request-derived tenantId interpolated into raw SQL across multiple DELETE statements.
  - _Why false positive:_ Account-deletion flow uses authenticated request tenantId and each DELETE is explicitly scoped to that tenant (or child rows via tenant-scoped conversations).
- **server/routes/admin-wedges.ts:41** _(upstream-verified)_ — db.execute(sql`... WHERE tenant_id = ${tenantId} AND ${tag} = ANY(tags) ...`) interpolates request-derived tenantId (and tag) into a raw SQL template — injection/isolation risk.
  - _Why false positive:_ Admin-only route verifies tenantId is the platform admin tenant before querying projects with explicit tenant_id filter.
- **server/routes/admin-wedges.ts:53** _(upstream-verified)_ — Pending-drafts query uses sql template with tenant-derived ${tenantId} and wedge slug interpolation (sql`SELECT COUNT(*) ... WHERE tenant_id = ${tenantId} AND wedge_slug = ${w.slug} ...`).
  - _Why false positive:_ Admin-only route; query includes explicit tenant_id = tenantId predicate after admin tenant check.
- **server/routes/admin-wedges.ts:73** _(upstream-verified)_ — Heartbeat lookup uses sql template interpolating tenantId and a computed regex string (${slugRe}) into the raw SQL (`... WHERE tenant_id = ${tenantId} AND (LOWER(name) ~ ${slugRe} ... )`).
  - _Why false positive:_ Admin-only route; heartbeat lookup is constrained by tenant_id = tenantId established from verified admin tenant context.
- **server/routes/admin-zombie-detector.ts:37** _(system-sweep)_ — Transaction block executes raw sql templates that interpolate request-derived windowDays into heavy queries (e.g. `... started_at > NOW() - (${windowDays}::int * INTERVAL '1 day')`).
  - _Why false positive:_ Platform-admin diagnostic intentionally analyzes usage across tenants for global tables/reporting and returns only to admin, not tenant-facing mixed output.
- **server/routes/admin.ts:514** _(upstream-verified)_ — Admin PATCH uses raw sql template updating tenants with request-derived values (sql`UPDATE tenants SET plan = ${plan} WHERE id = ${targetId}`) — interpolation of plan and targetId from request into SQL.
  - _Why false positive:_ PATCH route is guarded by admin-only check and intentionally updates a target tenant by id in the global tenants table.
- **server/routes/admin.ts:520** _(upstream-verified)_ — Admin PATCH uses sql template to set trial_max_conversations with values derived from request (sql`UPDATE tenants SET trial_max_conversations = ${maxConvs} WHERE id = ${targetId}`).
  - _Why false positive:_ Admin-only tenant-management route intentionally mutates tenants row selected by validated targetId.
- **server/routes/admin.ts:547** _(upstream-verified)_ — Reset usage route runs sql`UPDATE tenants SET trial_conversations_used = 0 WHERE id = ${targetId}` interpolating targetId from request into raw SQL.
  - _Why false positive:_ Reset-usage endpoint is platform-admin gated and updates only the specified tenant record in global tenants table.
- **server/routes/admin.ts:799** _(upstream-verified)_ — repair-incidents endpoint builds a dynamic WHERE clause from sql`... ${tenantId}`/filters then interpolates the combined clause into a final sql template (sql`... WHERE ${where} LIMIT ${limit}`) — multiple request-derived pieces embedded into raw SQL templates.
  - _Why false positive:_ Repair-incidents endpoint is admin-only and explicitly seeds WHERE with tenant_id = tenantId before adding optional filters.
- **server/routes/agent-insights.ts:49** _(upstream-verified)_ — Multiple db.execute(sql`...`) queries interpolate request-derived tenantId and windowDays into raw SQL (e.g. agent_activity / agent_cost_ledger queries use WHERE tenant_id = ${tenantId} AND started_at > NOW() - (${windowDays} || ' days')::interval).
  - _Why false positive:_ User route resolves tenantId from request context and every tenant-scoped table query includes tenant_id = tenantId.
- **server/routes/agent-insights.ts:244** _(upstream-verified)_ — Runs listing uses sql template with tenant-derived ${tenantId} and ${limit} (sql`FROM plans WHERE tenant_id = ${tenantId} LIMIT ${limit}`) — request values interpolated into raw SQL.
  - _Why false positive:_ Runs listing resolves tenantId from request context and constrains plans by tenant_id = tenantId.
- **server/routes/agent-insights.ts:277** _(upstream-verified)_ — Run detail query uses raw sql template with request-derived planId and tenantId (sql`FROM plans WHERE id = ${planId} AND tenant_id = ${tenantId}`) — interpolation risk.
  - _Why false positive:_ Query scopes plans by request tenant_id and validated planId in same statement; first-pass concern is SQL interpolation, not tenant isolation.
- **server/routes/agent-insights.ts:288** _(upstream-verified)_ — Rewards query interpolates tenantId and planId into raw SQL (sql`FROM step_rewards WHERE tenant_id = ${tenantId} AND plan_id = ${planId}`).
  - _Why false positive:_ Rewards query explicitly filters step_rewards by tenant_id and plan_id after owned plan fetch; tenant isolation is enforced in call path.
- **server/routes/api-v1.ts:392** _(upstream-verified)_ — GET /api/v1/agents uses a raw sql template with request-derived tenantId in the JOIN condition (tpn.tenant_id = ${tenantId}) — tenantId interpolated into SQL template.
  - _Why false positive:_ JOIN uses request tenantId to fetch only that tenant's override rows; personas are global and tenant_persona_names is tenant-filtered here.
- **server/routes/api-v1.ts:466** _(global-table)_ — Dispatch handler resolves agent name via raw sql template interpolating request-provided agent into LOWER(name) = LOWER(${agent}) — direct interpolation of user input into SQL.
  - _Why false positive:_ Lookup is against personas, a schema-global table with no tenant_id; no cross-tenant row isolation issue.
- **server/routes/api-v1.ts:655** _(global-table)_ — AG-UI/conversation persona resolution performs db.execute(sql`SELECT id FROM personas WHERE is_active = true AND LOWER(name) = LOWER(${body.agent}) LIMIT 1`) interpolating request body agent into the template.
  - _Why false positive:_ Persona resolution reads global personas table only; created conversation is later written with tenantId.
- **server/routes/api-v1.ts:889** _(global-table)_ — A2A handler uses raw sql template to resolve skillId (sql`... LOWER(REGEXP_REPLACE(name, '[^a-zA-Z0-9]+', '-', 'g')) = LOWER(${skillId})`) with request-derived skillId interpolated into SQL.
  - _Why false positive:_ skillId lookup targets global personas table by design; tenant isolation applies to conversation rows created afterward.
- **server/routes/api-v1.ts:777** _(upstream-verified)_ — Conversation poll uses raw sql template selecting messages with m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} — both values interpolated from the request into SQL.
  - _Why false positive:_ storage.getConversation(conversationId, tenantId) already verifies ownership and query also filters c.tenant_id = tenantId.
- **server/routes/api-v1.ts:940** _(upstream-verified)_ — A2A tasks/get selects assistant messages with m.conversation_id = ${conversationId} AND c.tenant_id = ${tenantId} using a raw sql template — request-derived values interpolated into SQL.
  - _Why false positive:_ Owned conversation is verified immediately above via storage.getConversation(..., tenantId), and SQL repeats c.tenant_id = tenantId.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' executed without constraining by tenant_id. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` can modify a conversation belonging to another tenant if IDs are guessed or misbound.
  - _Why false positive:_ conv was just server-created for this tenant and referenced by its returned id; update targets that newly created owned row, not an untrusted foreign id.
- **server/routes/conversations.ts:100** _(fk-transitive)_ — SELECT from tenant-scoped table 'project_conversations' performed without tenant constraint: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`. This can leak cross-tenant existence information or be raced into incorrect behavior.
  - _Why false positive:_ project_conversations has no tenant_id and is a link table isolated through tenant-scoped project and conversation parents already checked/created in this path.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into (presumed) tenant-scoped table 'project_conversations' does not set tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`. Tenant_id is required by the platform invariant and missing here.
  - _Why false positive:_ Insert into project_conversations without tenant_id is correct by design because table has no tenant_id and links tenant-scoped parent rows.
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' executed without tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`. Even though project ownership was checked earlier, writes must still be constrained at the SQL layer to prevent accidental cross-tenant updates.
  - _Why false positive:_ Project ownership was verified a few lines above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before this housekeeping update.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} executes without constraining tenant_id. This can modify another tenant's row if IDs collide or in race conditions.
  - _Why false positive:_ Row is selected immediately above with id and tenant_id, and update uses that verified row's id in same handler.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` lacks tenant_id constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}
  - _Why false positive:_ Same request path verifies project ownership via SELECT ... WHERE id = projectId AND tenant_id = tenantId before touching this project id.
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` lacks tenant_id constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}
  - _Why false positive:_ Project is checked earlier in same handler with tenant_id; update only touches that verified project id.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint — SELECT * FROM project_files WHERE project_id = ${projectId} ORDER BY ...
  - _Why false positive:_ project_files has no tenant_id and is scoped through tenant-checked parent project_id after prior project ownership check.
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` lacks tenant_id constraint — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1
  - _Why false positive:_ project_files has no tenant_id and access is via verified tenant-owned project_id, so isolation is transitive through projects.
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE on tenant-scoped table `project_files` lacks tenant_id constraint — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}
  - _Why false positive:_ project_files has no tenant_id; delete is constrained by project_id after verifying the parent project belongs to the tenant.
- **server/sculptor.ts:105** _(upstream-verified)_ — UPDATE on tenant-scoped table `sculptor_sessions` lacks a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This can update a row from another tenant if an id is guessed or reused.
  - _Why false positive:_ Row was just inserted with params.tenantId and returned as session; update targets that newly created verified row in same flow.
- **server/sculptor.ts:112** _(upstream-verified)_ — UPDATE on tenant-scoped table `sculptor_sessions` lacks a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}).
  - _Why false positive:_ Update applies to session row just inserted for params.tenantId in same function, using returned session.id.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter against what appears to be a tenant-scoped table.
  - _Why false positive:_ boot-time housekeeping count across tenants; only aggregate count used to decide maintenance action, no tenant data returned
- **server/seed-cleanup.ts:15** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks without a tenant_id constraint, modifying rows across tenants.
  - _Why false positive:_ boot-time global cleanup disabling runaway delegation tasks across tenants as housekeeping, not tenant-facing data access
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id filtering on a tenant-scoped table.
  - _Why false positive:_ aggregate count in maintenance sweep for duplicate session-logger tasks; no row data exposed per tenant
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true updates tasks across all tenants because it lacks tenant scoping.
  - _Why false positive:_ global housekeeping update disabling duplicate session-logger tasks across tenants
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has no tenant_id condition and thus aggregates across tenants for a tenant-scoped table.
  - _Why false positive:_ aggregate count in boot-time cleanup sweep for orphaned repo-examination tasks; no tenant content exposed
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true performs an unscoped update across heartbeat_tasks (no tenant_id constraint), affecting all tenants.
  - _Why false positive:_ global maintenance update disabling orphaned repo-examination tasks across tenants
- **server/seed.ts:2158** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter. research_programs is tenant-scoped, so this reads all tenants' programs.
  - _Why false positive:_ seed-time platform-wide repair scanning active programs to normalize invalid model ids across tenants
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table without verifying tenant ownership. Updating by id alone can modify rows from any tenant.
  - _Why false positive:_ per-row housekeeping update in platform-wide repair loop, keyed by selected row id and only fixing model field
- **server/seed.ts:2515** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id filter. conversations is typically tenant-scoped, so this will enumerate rows across tenants.
  - _Why false positive:_ boot-time backfill enumerates rows then inserts link records per row; project_conversations is transitively scoped via projects/conversations and no tenant-facing mixing
- **server/seed.ts:3832** _(system-sweep)_ — DELETE FROM heartbeat_tasks executes without any tenant_id constraint. heartbeat_tasks is tenant-scoped (has tenant_id) — this DELETE will affect rows across all tenants.
  - _Why false positive:_ boot-time purge of stale auto-disabled heartbeat rows is housekeeping across tenants, not tenant-facing data access
- **server/seed.ts:3886** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped; this will update conversations across all tenants.
  - _Why false positive:_ Boot-time seed/backfill intentionally updates legacy model ids platform-wide; housekeeping mutation only, no tenant data returned or mixed.
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from heartbeat_logs lacks tenant scoping — reads aggregated error logs across tenants without a tenant_id WHERE clause.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column; cross-tenant aggregation is by design.
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks executes without tenant scoping — it can remove tenant-scoped rows across all tenants.
  - _Why false positive:_ Watchdog cleanup is a platform-wide housekeeping purge of stale disabled tasks; only deletes old rows and emits counts.
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log has no tenant_id filter — purges event_log rows across tenants rather than scoped to a tenant.
  - _Why false positive:_ Platform-wide purge of old processed/failed event_log rows is housekeeping only and does not return tenant data.
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata executes a raw SELECT on tenant-scoped `messages` (SELECT id, citations FROM messages WHERE id IN (...)) without scoping by tenant_id.
  - _Why false positive:_ IDs come from msgs already loaded by tenant-scoped getMessages/getMessagesPaginated queries, so this metadata fetch is constrained to that vetted set.
- **server/storage.ts:826** _(system-sweep)_ — getMemoriesWithoutEmbeddings queries `memory_entries` without any tenant_id constraint — returns rows for all tenants.
  - _Why false positive:_ Global embedding backfill queue scans rows lacking embeddings across tenants for maintenance; row-by-row processing uses each row independently and does not expose mixed tenant data.
- **server/storage.ts:833** _(system-sweep)_ — getKnowledgeWithoutEmbeddings queries tenant-scoped `agent_knowledge` without tenant_id filtering — returns rows across tenants.
  - _Why false positive:_ Global embedding backfill queue for agent_knowledge is maintenance-only across tenants, processing rows independently without tenant-facing data mixing.
- **server/storage.ts:1000** _(system-sweep)_ — getDueHeartbeatTasks selects due `heartbeat_tasks` across all tenants (raw SELECT without tenant_id). This fetches tenant-scoped rows platform-wide with no caller-controlled tenant scope.
  - _Why false positive:_ due-task selection is a platform-wide worker sweep over all tenants for scheduling/housekeeping, not a tenant-facing mixed output
- **server/storage.ts:1027** _(system-sweep)_ — claimHeartbeatTasks runs raw UPDATE on `heartbeat_tasks` (UPDATE ... WHERE id = ${id} ...) with no tenant constraint — claiming tasks can thus race/claim tasks belonging to other tenants.
  - _Why false positive:_ claim step is part of the same cross-tenant scheduler sweep and only mutates housekeeping fields on each task row
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (where only eq(customTools.name, parsed.name)). This reads tenant-scoped data across tenants and can leak existence or block per-tenant operations.
  - _Why false positive:_ name is transformed with tenantToolName(parsed.name, tenantId) immediately above, making lookup tenant-derived despite no separate tenant_id predicate
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id without a tenant_id constraint (where eq(customTools.id, inserted.id)). Reads against tenant-scoped tables must be scoped to the caller tenant.
  - _Why false positive:_ row is re-read by the freshly inserted server-generated custom_tools primary key inserted.id, not an untrusted cross-tenant input
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into project_conversations without tenant_id — new link row is created using only project_id and conversation_id (no tenant constraint/column). If project_conversations is tenant-scoped, this write omits the tenant_id column required by the invariant.
  - _Why false positive:_ project_conversations has no tenant_id and link insert is guarded by tenant-scoped project creation plus conversation UPDATE ... AND tenant_id = tenantId
- **server/tools.ts:2981** _(fk-transitive)_ — INSERT into project_files does not set tenant_id — project_files row is inserted with project_id but no tenant_id column/value is provided. Per the invariant every INSERT into a tenant-scoped table must pass tenantId explicitly.
  - _Why false positive:_ project_files has no tenant_id and insert occurs only after verifying parent project belongs to tenant
- **server/tools.ts:2996** _(fk-transitive)_ — INSERT into project_notes omits tenant_id — the note row is inserted (project_id, note, author) without an explicit tenant_id value. This violates the requirement that INSERTs into tenant-scoped tables include tenantId.
  - _Why false positive:_ project_notes has no tenant_id and insert occurs only after verifying parent project belongs to tenant
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) again omits tenant_id — the handler inserts a project↔conversation link without setting tenant_id on the row.
  - _Why false positive:_ project_conversations has no tenant_id and both project ownership and conversation tenant membership are verified before insert
- **server/tools.ts:6927** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT uses project_id but omits tenant_id in the VALUES list, violating the invariant that every tenant-scoped INSERT must set tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id and is inserted only after assertProjectInTenant verifies the parent project belongs to params._tenantId
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against tenant-scoped table 'browser_workflows' without constraining by tenant_id (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This can modify another tenant's rows if an id from a different tenant is used.
  - _Why false positive:_ workflow row is loaded immediately above with tenant_id = tenantId, and the update uses that verified workflow.id
- **server/tools/domains/files/handlers.ts:903** _(fk-transitive)_ — INSERT into project_files does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES (${projId}, ...)). Per project_files being tenant-scoped, tenant_id must be explicitly provided on INSERT.
  - _Why false positive:_ project_files has no tenant_id and projId comes from a join proving the parent project belongs to ctx.tenantId
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE executed against tenant-scoped table 'projects' without constraining by tenant_id (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Even if projId was obtained earlier, the UPDATE itself lacks an explicit tenant filter in SQL.
  - _Why false positive:_ projId was obtained from a query joining projects with p.tenant_id = tenantId in the same call path
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE against tenant-scoped table `knowledge_triples` without tenant filter. The code executes `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` (no `tenant_id` check).
  - _Why false positive:_ rows being updated were selected just above from knowledge_triples with tenant_id = tenantId and valid_until IS NULL
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} executes without constraining the update by tenant_id. Even though the id was found earlier by a tenant-scoped SELECT, the UPDATE itself lacks tenant scoping and could modify rows across tenants in race/ID-collision scenarios.
  - _Why false positive:_ id comes from same-path select on memory_entries constrained by tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) is executed without a tenant_id constraint. This bulk update may affect rows belonging to other tenants if ids are manipulated or computed incorrectly.
  - _Why false positive:_ ids array is derived solely from rows selected immediately above with tenant_id = tid and persona_id = personaId
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} (recordStrategicWinHandler) lacks tenant_id scoping. Same pattern as other updates — write executed without explicit tenant constraint.
  - _Why false positive:_ id being updated was selected in the same function from memory_entries with tenant_id = tid and persona_id = personaId
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` by conversation id is performed without an application-level tenant filter. Reading a tenant-scoped table by id without constraining tenant_id violates the tenant-isolation invariant.
  - _Why false positive:_ Query is used only to verify the conversation's tenant matches claimed tenantId in the same call path, with fail-closed rejection on mismatch/ambiguity.
- **server/video-job-runner.ts:235** _(system-sweep)_ — recoverStaleVideoJobs performs a global UPDATE on videoJobs without constraining to a tenant (no WHERE tenant_id = ...). This will mark jobs across all tenants as failed.
  - _Why false positive:_ Global stale-job recovery is a platform-wide maintenance sweeper updating housekeeping/status fields only, not returning tenant data or mixing tenant content.
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from agent_knowledge performed with no tenant_id constraint (reads watermark globally). This can return/operate on another tenant's watermark.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-memorize.ts:74** _(system-sweep)_ — INSERT into agent_knowledge sets tenant_id = 1 (hardcoded) instead of using the calling tenant. This will write a watermark into tenant 1 regardless of caller and can corrupt tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:60-81 setWatermark writes a single global housekeeping watermark row (category='auto_memorize_watermark') for the platform-wide runAutoMemorize sweep; it is never returned to a tenant surface (only read by getWatermark:45-58). tenant_id=1 is the sweep's fixed housekeeping bucket, not acting-tenant data
- **server/chat-engine.ts:633** _(upstream-verified)_ — SELECT from compaction_archives uses conversation_id without constraining tenant_id — tenant-scoped table read lacks tenant filter (possible cross-tenant data leak).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:632 — The compaction_archives SELECT is scoped by conversation_id = c.id, where c comes from `convs` fetched at line 593-598 with an explicit `c.tenant_id = ${tenantId}` filter (and the parent project is validated `WHERE id = projectId AND tenant_id = tenantId` at line 543). So c.id is a PK of an already-tenant-validated conversation row — no cross-tenant read possible.
- **server/chat-engine.ts:651** _(upstream-verified)_ — SELECT from messages WHERE conversation_id = ${c.id} has no tenant_id constraint — reading messages (tenant-scoped) without verifying tenant ownership of the row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:650 — Same as line 632: the messages SELECT uses conversation_id = c.id where c originates from the tenant-filtered convs query (line 596: `c.tenant_id = ${tenantId}`) under a project already validated to belong to tenantId. c.id is a tenant-validated PK, so message reads cannot cross tenants.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on tenant-scoped table `commitments` executed without tenant_id constraint (WHERE id = ${row.id} only). This can modify a row belonging to a different tenant for the same id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — update by session id with no tenant_id constraint or ownership check. A caller-supplied/untrusted sessionId can cause cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE to tenant-scoped table `agent_knowledge` without tenant constraint: `UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}` does not include `AND tenant_id = ...`, allowing cross-tenant modification if ids are reused or tampered with.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/storage.ts:399** _(internal-worker-pk)_ — attachCitations updates tenant-scoped `messages` (UPDATE messages SET citations=...) using only id in the WHERE clause and no tenant_id constraint — this can overwrite another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:1311** _(upstream-verified)_ — getAllDataForExport performs broad SELECTs over many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant/heartbeat tables, etc.) with no tenant_id filtering — this is a massive unscoped data export risk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `custom_tools` (updating implementation) uses only eq(customTools.id, tool.id) with NO tenant_id constraint. This can modify another tenant's row if IDs are guessed or shared.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE on tenant-scoped table `custom_tools` uses only eq(customTools.id, tool.id) with NO tenant_id constraint. This can delete another tenant's tool row (tenant-escape / data-loss risk).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} is executed without also constraining tenant_id. This is a write to a tenant-scoped table that could modify another tenant's ledger row if reservationId is mis-bound.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:170** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'venture_discovery:released' WHERE id = ${reservationId} is executed without constraining tenant_id. This releases reservations by id only and can affect rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT recent logs from `heartbeat_logs` without tenant scoping (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10). This reads logs across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/agentic-engines.ts:451 — The query at server/agentic-engines.ts:451-454 reads heartbeat_logs for recent successful automated activities and feeds a platform-level predictive prompt at :456-463; it is not tenant-facing row retrieval. shared/schema.ts:557-575 shows heartbeat_logs has no tenant_id, and the surrounding engine is a platform-wide aggregate rather than a tenant response.
- **server/agentic-engines.ts:533** _(global-table)_ — SELECT from `heartbeat_tasks` omits tenant filter (SELECT name, cron_expression, is_enabled, last_run_at, run_all FROM research_schedules WHERE tenant_id = ${tenantId}) — note: the following taskConfig SELECT (SELECT name,... FROM heartbeat_tasks WHERE enabled = true ORDER BY name) lacks tenant scoping. This reads heartbeat task configurations across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/agentic-features.ts:638** _(upstream-verified)_ — SELECT existing enrollments omits tenant scoping: SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'. This check can be influenced by rows from other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/auto-asset-capture.ts:184** _(system-sweep)_ — backfillProjectAssets selects conversations across all tenants: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ... — no tenant restriction. This reads tenant-scoped rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): backfillProjectAssets only caller is a one-time startup backfill routes.ts:824; it selects c.tenant_id (auto-asset-capture.ts:185) and passes each row's tenant to captureProjectAssets(c.id,c.tenant_id,...) at auto-asset-capture.ts:217 — row-carried tenant, no tenant-facing return.
- **server/auto-asset-capture.ts:207** _(system-sweep)_ — When backfilling, messages are queried using conversation_id only: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' — there is no tenant_id filter on messages (a tenant-scoped table).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The message read is inside the per-conversation backfill loop; conversation_id=c.id (auto-asset-capture.ts:209) belongs to the row whose tenant_id was selected at line 185 and passed to captureProjectAssets at line 217. Maintenance sweep, not tenant-facing.
- **server/build-video-from-brief.ts:714** _(upstream-verified)_ — buildVideoFromBrief accepts an input.projectId and passes it into the StartVideoJob input (and also earlier exposes it to spawned child processes) without verifying the caller owns that project. A caller could supply another tenant's project_id and cause cross-tenant association or actions.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): tenantId is explicitly threaded into StartVideoJobInput on the adjacent line; downstream project_files writes are gated by assertProjectInTenant.
- **server/capability-review.ts:491** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` is executed without constraining by tenant_id (WHERE id = ${pending.reviewId} AND reused = false). This allows cross-tenant mutation of capability_reviews rows by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/channels/kernel.ts:177** _(upstream-verified)_ — dispatchInbound calls processMessage(conversationId, ...) using a conversationId returned from the adapter-provided resolveConversation callback without verifying the conversation belongs to turn.tenantId. This can allow cross-tenant access if resolveConversation or upstream adapters are compromised or buggy.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): kernel.ts:177 resolveConversation; sole caller whatsapp.ts:442 binds the callback to a tenant WA session and turn.tenantId=s.tenantId (whatsapp.ts:429-432); getOrCreateConversation only returns a conv verified for s.tenantId (whatsapp.ts:490) or freshly created with tenantId=s.tenantId (whatsapp.ts:520-525) — no attacker-supplied foreign id
- **server/chat-engine.ts:4080** _(global-table)_ — recordToolExecution(conversationId, toolName, ...) is called without a tenant identifier. If this function writes to a tenant-scoped table (tool execution logs tied to conversations), it may perform writes/updates without scoping to tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolExecution (felix-brain.ts:298-320) only mutates the in-process taskStateStore Map; no DB write, no tenant table.
- **server/data-protection.ts:403** _(upstream-verified)_ — backupTenantDataToDrive runs SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} without scoping by tenant_id. This can read another tenant's messages for the same conversation id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): COUNT(*) on messages uses conv.id from a tenant-scoped conversation set fetched at server/data-protection.ts:362 within backupTenantDataToDrive.
- **server/delivery-pipeline.ts:1071** _(global-table)_ — getDeliveryByStripePayment selects from delivery_logs using stripePaymentId without any tenant filter. This can return another tenant's delivery row for the same payment id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT project row by id (FROM projects WHERE id = ${projectId}) without constraining tenant_id. This reads another tenant's project row before the code performs an ownership check in application logic.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): ensureProjectFolder selects only tenantId/driveFolderId and immediately throws on project.tenantId!==tenantId at google-drive.ts:551-553 before returning or writing anything.
- **server/job-queue.ts:273** _(internal-worker-pk)_ — cancelJob updates agent_jobs WHERE id = ${id} ... with no tenant_id check — cancellation can affect jobs owned by other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:281** _(upstream-verified)_ — retryJob resets state for a job id with no tenant constraint in the UPDATE — allows retrying jobs belonging to other tenants without ownership verification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:283 — retryJob is only reachable via POST /api/admin/agent-jobs/:id/retry (server/routes/agent-jobs.ts:62-70), gated by requirePlatformAdmin. Same admin-only operator inbox as cancelJob — cross-tenant retry by id is deliberate platform-admin capability, not attacker-influenced.
- **server/lib/agent-trace.ts:124** _(internal-worker-pk)_ — UPDATE to tenant-scoped table agent_trace_spans omits tenant_id predicate (UPDATE ... WHERE span_id = ${spanId}). persistSpanClose can update rows across tenants if a span_id is known, violating tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/memory-graph.ts:183** _(upstream-verified)_ — refreshCategoryCount: UPDATE memory_categories ... WHERE id = ${categoryId} has no tenant_id constraint. Mutating a tenant-scoped table without tenant scoping violates tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/plan-executor.ts:117** _(upstream-verified)_ — UPDATE plans (appendExecutionLog) modifies a tenant-scoped table without scoping by tenant_id (WHERE id = ${planId} only). This allows cross-tenant mutation/visibility if planId is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): executePlan is reached via decidePlan; the route gates requirePlatformAdmin (server/routes/agentic-policy.ts:95) and getPlan(id, tenantId) verifies ownership at server/routes/agentic-policy.ts:113 before execution.
- **server/process-governor.ts:977** _(upstream-verified)_ — logAction performs UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} with no tenant_id constraint. governance_rules is tenant-scoped; updating by id only may affect another tenant's rule row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — updateProjectBrain reads the projects table via SELECT name,... FROM projects WHERE id = ${projectId} with no tenant_id check. This accepts a foreign projectId and reads a tenant-scoped row without verifying caller ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — backfillProjectBrains runs SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... with no tenant filter, enumerating projects across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:349** _(system-sweep)_ — In backfillProjectBrains: SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks tenant scoping; conversation rows are tenant-scoped and should not be queried without tenant validation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/sculptor.ts:167** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` in monitorSession() omits tenant scoping (WHERE id = ${sessionId}). This allows cross-tenant mutation of session state.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE on tenant-scoped table `sculptor_sessions` to write review_result lacks tenant constraint (WHERE id = ${sessionId}).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/second-opinion.ts:404** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (settleFusionReservation) updates a tenant-scoped ledger row by id only, with no tenant_id check. A reservation id could be reused/guessed to modify another tenant's ledger entry.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:423** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseFusionReservation) omits tenant scoping. This can clear or modify another tenant's ledger row by id alone.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions runs without filtering by tenant_id. research_sessions is tenant-scoped and this reads across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:34-36 inside importDevSnapshot() (12-); SELECT COUNT(*) is only a boot-time gate to decide whether to import a dev snapshot; returns a count, never row data to a tenant surface.
- **server/self-improvement.ts:170** _(upstream-verified)_ — detectStagnation performs a SELECT on the tenant-scoped `experiments` table without requiring or enforcing a tenant_id filter when tenantId is not provided — the query can return experiments across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — getExperimentHistory, when called without a tenantId, returns db.select().from(experiments) with no tenant filter — a tenant-scoped table is read without enforcing tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:248** _(upstream-verified)_ — getConversationUnscoped performs a SELECT on tenant-scoped `conversations` without any tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:806** _(internal-worker-pk)_ — updateMemoryEmbedding updates tenant-scoped `memory_entries` by id only (WHERE id = ...) with no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding updates tenant-scoped `agent_knowledge` by id only (WHERE id = ...) with no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:942** _(upstream-verified)_ — getHeartbeatTask selects from tenant-scoped `heartbeat_tasks` by id with no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getHeartbeatTask(id) callers routes/heartbeat.ts:103 and :121 immediately verify Number(task.tenantId)!==tenantId → 404 (heartbeat.ts:105 and :123) before any use, and are behind requireAdmin.
- **server/storage.ts:969** _(system-sweep)_ — updateHeartbeatTask performs an UPDATE on tenant-scoped `heartbeat_tasks` and will update by id alone when tenantId is not provided (conditions default to id-only). This allows unscoped mutation across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): markHeartbeatTaskRun callers are all the internal cron runner (heartbeat.ts:272,865,1097,...) using server-generated task.id; only housekeeping lastRunAt/nextRunAt are written (storage.ts:1035).
- **server/storage.ts:975** _(upstream-verified)_ — deleteHeartbeatTask deletes from tenant-scoped `heartbeat_tasks` using only id when tenantId is not supplied (conditions.default id-only), permitting unscoped deletes across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): deleteHeartbeatTask only caller is routes/heartbeat.ts:81 which always passes tenantId → tenant subselect scope applied (storage.ts:966); no unscoped caller exists.
- **server/tools.ts:4566** _(internal-worker-pk)_ — SELECT from tenant-scoped presenter_sessions without tenant constraint or ownership check: `SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ...` reads session tokens by presentation_id but does not restrict by tenant_id or assert the presentation belongs to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:4537 — The presenter_sessions SELECT keys on slideResult.presentationId, which was just created by this same create_slides tool invocation for the caller's own tenant (conversation ownership is asserted upstream at ~4500 via 'conversation not owned by tenant' and assertProjectInTenant). The presentation id is not attacker-supplied cross-tenant input; it is a self-generated art
- **server/tools.ts:8895** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'proposed_skills' uses only WHERE id = row.id and lacks a tenant_id constraint. This allows updating rows across tenants if IDs collide or are guessed.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8922** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'proposed_skills' (mark accepted / set promotedSkillId) is missing a tenant_id condition in the WHERE clause (only matches id).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8937** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'proposed_skills' (mark rejected) is executed with WHERE id = row.id only and lacks tenant scoping, enabling cross-tenant updates.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` executed without constraining by tenant_id (no WHERE tenant_id = ...). This loads pairing rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/minerva-planner.ts:548** _(deferred)_ — getPlan(planId, tenantId?) allows calling code to omit tenantId and executes: SELECT * FROM plans WHERE id = ${planId} — this reads a tenant-scoped row without any tenant_id constraint (IDOR risk).
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
- **server/delivery-pipeline.ts:203** _(deferred)_ — updateDeliveryLog performs an UPDATE on delivery_logs without constraining by tenant_id (WHERE eq(deliveryLogs.id, id) only). This allows a caller with a deliveryId to update another tenant's delivery row.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
