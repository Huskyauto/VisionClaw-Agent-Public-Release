# Tenant-Isolation Audit Report

_Generated 2026-09-03T22:36:14.600Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 149 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **512/661** file(s) audited · chunks 51/61 OK (0 failed, 10 pending)
- Findings: **209** (CRITICAL 47 · HIGH 126 · MEDIUM 36 · LOW 0)
- Severe dispositions: **173 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (47)

- **server/agentic/resume-worker.ts:65** — Raw SELECT scans agent_runs and agent_approvals across all tenants (no tenant_id WHERE filter). The db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ... WHERE ... LIMIT 50`) at line 65 returns rows for every tenant, violating the app-level tenant-isolation invariant and exposing cross-tenant data.
  - _Fix:_ Constrain the query to a single tenant. Pass an explicit tenantId into resumePendingApprovalRuns (or otherwise scope the operation) and add AND r.tenant_id = ${tenantId} (parameterized via the tagged sql) to the WHERE clause. Example: db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ON ... WHERE ( ... ) OR ( ... ) AND r.tenant_id = ${tenantId} LIMIT 50`). If the worker must legitimately scan across tenants, run it in a privileged, audited context and avoid returning tenant-scoped rows to ordinary callers; otherwise enforce per-tenant filtering everywhere.
- **server/agentic/wake-scheduler.ts:142** — Global claim/update on tenant-scoped table agent_wake_schedules without tenant_id constraint. The UPDATE that marks pending wakes as 'firing' selects ids from agent_wake_schedules using a sub-SELECT that does not restrict to a tenant. This can claim and mutate pending wakes belonging to other tenants.
  - _Fix:_ Limit the claim to a specific tenant (add AND tenant_id = ${tenantId} in the inner WHERE and the outer UPDATE) or iterate per-tenant in a platform-safe loop with explicit tenant context. If this is intended to be a multi-tenant global scheduler, ensure the call is run with a platform-privileged context and audit/authorize it; otherwise change runDueWakes to accept a tenantId and include WHERE tenant_id = ${tenantId} in both the inner SELECT and the outer UPDATE so every mutation is scoped to the owning tenant.
- **server/channel-routing.ts:53** — INSERT INTO channel_routes uses caller-supplied personaId and writes global channel_routes state without any tenant context or ownership validation. A caller can assign a persona (potentially belonging to another tenant) to a channel globally.
  - _Fix:_ Make channel_routes tenant-scoped (add tenant_id column) and require tenantId in setChannelRoute. Include tenant_id in the INSERT/ON CONFLICT and validate the supplied personaId belongs to that tenant before inserting (SELECT p.tenant_id FROM personas p WHERE p.id = ${personaId} AND p.tenant_id = ${tenantId}). Alternatively, if channel_routes are intended to be global, enforce strict server-side checks mapping persona ids to tenants before any write.
- **server/channel-routing.ts:71** — removeChannelRoute deletes rows from channel_routes by channel only, with no tenant constraint. This can delete another tenant's route if channel_routes ought to be tenant-scoped.
  - _Fix:_ Require tenantId and include it in the DELETE (DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}). If channel_routes are intentionally global, add explicit authorization checks so only authorized callers can delete global routes.
- **server/coinbase-commerce.ts:73** — Query enumerates tenant rows: SELECT coinbase_commerce_webhook_secret FROM tenants WHERE coinbase_commerce_webhook_secret IS NOT NULL ... — this reads tenant-scoped secrets across all tenants without constraining to a caller tenant, exposing sensitive tenant secrets to application logic.
  - _Fix:_ Avoid scanning tenant rows for secrets. Instead maintain a safe, indexed mapping from webhook-secret-hash (or HMAC key identifier) → tenant_id that allows a single-row lookup, or require a tenant identifier that is cryptographically bound to the payload. If you must iterate, restrict this code to an administrative offline job and do NOT expose tenant secrets in-memory; never perform unbounded selects of tenant secret columns in request-path logic.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` lacks tenant_id predicate: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This can unclaim or mutate a commitment belonging to another tenant.
  - _Fix:_ Add an explicit tenant scope to the UPDATE (e.g. `AND tenant_id = ${tenantId}`) and, where appropriate, also match the expected draft_status (e.g. draft_pending) so the revert is atomic and cannot affect other tenants. Prefer using the transaction/withTenantTx or pass the tenantId into the tx to make intent explicit.
- **server/email.ts:429** — UPDATE inbox_messages executed without tenant_id in the WHERE clause (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). This may modify rows belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise verify ownership of the row before performing the UPDATE.
- **server/embeddings.ts:852** — UPDATE memory_entries executed without tenant_id in WHERE (UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}). This can update rows belonging to other tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or resolve tenant ownership before the UPDATE. Never write to a tenant-scoped table by id alone without validating tenant_id.
- **server/embeddings.ts:869** — UPDATE agent_knowledge executed without tenant_id in WHERE (UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}). This can modify rows across tenants.
  - _Fix:_ Require and use tenantId in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise verify the row's tenant ownership before updating to prevent cross-tenant writes.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to executing a raw UPDATE on the given table without any tenant constraint (UPDATE ${table} SET ... WHERE id = ${id}) when options.tenantId is not supplied. This allows updating tenant-scoped rows without checking tenant ownership.
  - _Fix:_ Remove the fallback path or require options.tenantId for tenant-scoped tables. Always include tenant_id in the WHERE clause for memory_entries/agent_knowledge updates, or throw when tenantId is not provided so callers must pass the tenant context explicitly.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` is executed without a tenant_id WHERE filter in rehydrateTaskStateOnBoot — this reads running/pending runs across all tenants and repopulates in-memory taskStateStore with other tenants' data, violating tenant isolation.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or otherwise iterate only over an allowed list of tenant IDs. If the intent is an admin-only bulk operation, require explicit admin authorization and document it; do not silently load all tenants' rows into memory. Ensure any rehydration only materializes rows for tenants the caller is authorized to see.
- **server/google-drive.ts:1657** — INSERT into project_files omits tenant_id. project_files is tenant-scoped (or relies on project ownership); inserting a row without tenant_id creates a record that cannot be tied to the caller's tenant and risks cross-tenant data exposure or orphaned rows.
  - _Fix:_ Include the tenant_id column in the INSERT (and bind the caller's tenant id) or otherwise derive and verify the owning tenant server-side and insert it (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${resolvedTenantId}, ...)). If the table must infer tenant via project_id, ensure the INSERT is performed via a safe, validated path (e.g. CTE that joins projects on project_id and checks tenant_id) so the DB write is explicitly scoped to the tenant.
- **server/job-queue.ts:99** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without any tenant_id constraint — this can cause worker claims to move/execute jobs belonging to any tenant.
  - _Fix:_ Scope claims to a single tenant (or an explicit admin/global queue). Pass an explicit tenantId into claimDueJobs and add `AND tenant_id = ${tenantId}` to the inner SELECT (or partition by tenant in a safe way). If the worker must be multi-tenant, make that explicit and audit/limit which code paths can claim cross-tenant jobs.
- **server/job-queue.ts:135** — reclaimExpiredLeases UPDATE (marking running→failed_terminal) updates agent_jobs across all tenants — no tenant_id filter.
  - _Fix:_ Enforce tenant scoping on reclamation. Either run reclaimExpiredLeases per-tenant (accept tenantId param) or add `AND tenant_id = ${tenantId}` to the UPDATE so rows are only modified within the intended tenant scope. Alternatively, restrict this operation to an operator/admin path that is explicitly documented and audited.
- **server/job-queue.ts:149** — reclaimExpiredLeases second UPDATE (requeueing expired leases) updates agent_jobs for all tenants without tenant_id filtering.
  - _Fix:_ As above, require a tenantId and include `AND tenant_id = ${tenantId}` in the UPDATE, or explicitly restrict this background operation to an audited global admin process and document the risk. Prefer per-tenant reclamation to preserve isolation.
- **server/job-queue.ts:178** — completeJob updates agent_jobs SET status='succeeded' WHERE id = ${id} … without verifying tenant_id — a stale/late completion call could modify another tenant's job.
  - _Fix:_ Use tenant fencing: require and check tenant_id (pass tenantId into completeJob) or include an additional `AND tenant_id = ${tenantId}` in the WHERE clause. Alternatively, perform a prior read to verify the job's tenant matches the caller's tenant before updating.
- **server/job-queue.ts:221** — failJob reads max_attempts from agent_jobs WHERE id = ${id} with no tenant constraint — a caller could infer or act on another tenant's job state.
  - _Fix:_ Require tenant context for failJob: accept tenantId or otherwise resolve-and-verify the job's tenant before reading/updating. Change the SELECT to `WHERE id = ${id} AND tenant_id = ${tenantId}` (and fail closed if mismatch).
- **server/job-queue.ts:238** — failJob UPDATE to mark job failed_terminal writes agent_jobs WHERE id = ${id} … without tenant_id filtering — can dead-letter jobs for other tenants.
  - _Fix:_ Include tenant_id equality in the WHERE clause (e.g. `AND tenant_id = ${tenantId}`) or require a prior verified tenant read and only update rows that match that tenant. Ensure all failure-path updates are tenant-fenced.
- **server/job-queue.ts:258** — failJob UPDATE that re-queues a job (status='pending', set next_run_at) runs without a tenant_id filter — risk of re-scheduling jobs across tenants.
  - _Fix:_ Scope this UPDATE to the job's tenant (require tenantId and add `AND tenant_id = ${tenantId}`) or perform a verified read-then-update guarded by tenant_id. Make tenant fencing part of the lease-fencing checks.
- **server/job-queue.ts:275** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} … with no tenant constraint — an operator or caller could cancel another tenant's job by id.
  - _Fix:_ Require tenant verification on cancelJob: either pass tenantId and include `AND tenant_id = ${tenantId}` in the WHERE, or read the job's tenant first and refuse if it doesn't match the operator's scope. Enforce this consistently for operator/admin surfaces.
- **server/lib/customer-revenue-launchpad-seed.ts:103** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code inserts `project_id, note, author` without passing tenant_id, but tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Fix:_ Include the tenant_id in the inserted columns/values to enforce tenant ownership. For example: `INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projectId}, ${OWNER_TENANT_ID}, ${POINTER_NOTE}, 'seed-customer-revenue-launchpad')`. Alternatively, use an INSERT ... SELECT guarded by a `WHERE EXISTS (SELECT 1 FROM projects WHERE id = ${projectId} AND tenant_id = ${OWNER_TENANT_ID})` to ensure correct ownership while providing tenant_id explicitly.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` without any tenant_id constraint (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows for all tenants (including sensitive OAuth tokens) instead of restricting to a single tenant context.
  - _Fix:_ Avoid broad SELECTs over tenant-scoped tables. Either: (a) add an explicit WHERE tenant_id = ${tenantId} parameter so only a single tenant's rows are read; or (b) if this is an intentional cross-tenant admin operation, gate it behind an explicit, auditable admin-only path and minimize returned columns (never return encrypted secrets unless strictly necessary). Prefer iterating a vetted tenant list and querying each tenant individually with parameterized tenant_id values.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is executed with no tenant_id filter. This enumerates tenant_ids across all tenants and can be used to operate on other tenants' subscriptions without per-tenant authorization checks.
  - _Fix:_ Do not query tenant-scoped tables without an explicit tenant_id unless running under an auditable admin-only flow. Change this to either query only the intended tenant(s) (WHERE tenant_id = ${tenantId}) or require and verify an explicit admin authorization before enumerating tenants. If the intent is a startup auto-reconnect across many tenants, fetch a vetted list of tenant IDs from a trusted admin-controlled source and perform per-tenant parameterized queries.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh runs a broad SELECT over oauth_subscriptions (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) with no tenant_id filter. This reads and then proceeds to refresh tokens for all tenants, potentially exposing or acting on tokens across tenants without explicit per-tenant scoping/authorization.
  - _Fix:_ Restrict proactive refresh to a single tenant context or require an explicit, auditable admin privilege for global refresh operations. Prefer iterating a safe, explicitly-supplied list of tenant IDs and running parameterized queries using WHERE tenant_id = ${tenantId} for each tenant. Ensure any code that acts across tenants is logged/audited and minimizes exposure of sensitive fields.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions executes SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without tenant_id filtering, then calls refreshAccessToken for every tenant. This is an unscoped cross-tenant operation acting on tenant-scoped rows.
  - _Fix:_ Same mitigation as above: require an explicit admin gate/audit for global operations or change behavior to accept an explicit tenant list and run per-tenant parameterized queries (WHERE tenant_id = ${tenantId}). Avoid unscoped SELECTs over tenant-scoped tables that enable mass actions across tenants.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} executes without scoping by tenant_id. personas is a tenant-scoped resource; reading it without verifying tenant ownership can return another tenant's persona (cross-tenant data exposure).
  - _Fix:_ Include tenant_id in the WHERE clause and validate ownership before exporting. Example: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} and fail if no row is returned. Ensure all accesses to personas within this function are tenant-scoped.
- **server/plan-executor.ts:516** — UPDATE plans ... WHERE id = ${planId} AND status = 'approved' is executed without constraining by tenant_id. This allows claiming/updating a plan row across tenants based solely on plan id.
  - _Fix:_ Include the caller/validated tenant in the WHERE clause (e.g. AND tenant_id = ${callerTenantId}) when claiming the plan, or otherwise validate/derive the tenant before performing the UPDATE. Make the CAS claim require both id and tenant_id so a caller cannot affect other tenants' plans.
- **server/plan-executor.ts:793** — Boot-time recovery runs UPDATE plans SET status = 'approved' WHERE status = 'executing' ... RETURNING id without any tenant_id constraint. This updates executing plans across all tenants.
  - _Fix:_ Restrict the rollback to a specific tenant or iterate per-tenant. Add a tenant_id predicate (or otherwise enumerate tenants and run per-tenant) and ensure only intended tenant-scoped rows are modified. Log/audit this admin operation explicitly if cross-tenant behavior is required.
- **server/plan-executor.ts:802** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 executes without filtering by tenant_id. The returned plan ids are then scheduled for execution across tenants.
  - _Fix:_ Only select approved plans scoped to a specific tenant (WHERE tenant_id = ${tenantId}). If global recovery is intended, make the tenant-explicitness deliberate (e.g. loop tenants, or require platform-admin context) and record audit logs. Never implicitly operate across tenants without an explicit tenant filter.
- **server/process-governor.ts:898** — UPDATE agent_desks SET ... WHERE id = ${desk.id} executes an update on a tenant-scoped table using only the row id; it lacks a tenant_id filter and can modify rows in other tenants that share the same id.
  - _Fix:_ Include tenant scoping in the UPDATE predicate (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or otherwise validate the row's tenant ownership before performing the update.
- **server/process-governor.ts:1235** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates a tenant-scoped table by id only and does not include tenant_id in the WHERE clause — it can update another tenant's rule with the same id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or perform an ownership check before mutating governance_rules.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` lacks a tenant_id constraint: `UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id}`. This can modify a row belonging to any tenant if `id` is mis-bound or manipulated.
  - _Fix:_ Constrain the update by tenant: add `AND tenant_id = ${r.tenant_id}` to the WHERE clause (or otherwise bind the expected tenantId). Example: `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}`. Also consider using parameterized Drizzle/API helpers to enforce tenant scoping everywhere the row is updated.
- **server/research-engine.ts:169** — UPDATE statement in cleanupZombieSessions updates research_sessions across all tenants — the WHERE clause lacks any tenant_id constraint, allowing cross-tenant modification of session rows.
  - _Fix:_ Restrict the update to a specific tenant scope or perform per-tenant cleanup. For example, require a tenantId parameter and add AND tenant_id = ${tenantId} to the WHERE clause, or enumerate tenants and run the UPDATE per-tenant. Ensure any DELETE/UPDATE/SELECT against research_sessions always includes tenant_id = ${...} (or equivalent Drizzle .where(eq(...))) to enforce tenant isolation.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' runs without tenant_id filtering on a tenant-scoped table. The subsequent UPDATE disables matching rows across all tenants.
  - _Fix:_ If the intent is to act only on a specific tenant (likely ADMIN_TENANT_ID), add tenant_id = ${ADMIN_TENANT_ID} to the SELECT/UPDATE. If the change is intended globally, make that explicit in code/comments and review whether heartbeat_tasks is truly global. Prefer per-tenant iteration or a targeted WHERE tenant_id clause to avoid accidental cross-tenant changes.
- **server/seed-cleanup.ts:15** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has NO tenant filter and will disable delegation tasks for every tenant.
  - _Fix:_ Restrict the UPDATE to the intended tenant(s), e.g. WHERE type = 'delegation' AND enabled = true AND tenant_id = ${ADMIN_TENANT_ID}, or loop per-tenant with explicit checks. Never run mass updates on tenant-scoped tables without an explicit tenant_id predicate.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant_id scoping. Following UPDATE disables session-logger tasks across all tenants.
  - _Fix:_ Scope the SELECT/UPDATE to the target tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the operation is intentionally global and safe. Add tests / comments if global behavior is required.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true omits tenant_id and will affect all tenants' heartbeat_tasks.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or iterate per-tenant. Avoid idempotent-seeming maintenance that applies to every tenant unless explicitly intended and reviewed.
- **server/seed-cleanup.ts:59** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) runs without tenant_id scoping; the later UPDATE disables matching rows across tenants.
  - _Fix:_ Restrict the SELECT/UPDATE to the intended tenant (use tenant_id = ${ADMIN_TENANT_ID}) or perform per-tenant checks. If global changes are intended, add explicit justification and safeguards to prevent accidental cross-tenant modifications.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has no tenant_id predicate and will disable repo-examination tasks for every tenant.
  - _Fix:_ Constrain by tenant_id (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise explicitly handle each tenant; avoid blanket updates on tenant-scoped tables.
- **server/seed.ts:3634** — DELETE FROM heartbeat_tasks is executed without any tenant_id constraint against a tenant-scoped table (heartbeat_tasks has a tenant_id index). This will delete rows across all tenants.
  - _Fix:_ Constrain the DELETE to the intended tenant(s). e.g. include AND tenant_id = ${tenantId} (or run the purge per-tenant) or otherwise scope to a safe system tenant if that was intended. Never run blanket DELETEs on tenant-scoped tables without an explicit tenant filter.
- **server/self-improvement.ts:423** — UPDATE on tenant-scoped table `experiments` performed without constraining by tenant_id. The db.update(...).where(eq(experiments.id, experiment.id)) allows cross-tenant modification if an experiment.id is reused or guessed.
  - _Fix:_ Restrict the UPDATE to the caller's tenant. Add an explicit tenant predicate to the WHERE, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Prefer using the Drizzle query builder (db.update(...).where(...)) rather than raw SQL templates to avoid accidental missing predicates.
- **server/stability-watchdog.ts:211** — DELETE FROM 'heartbeat_tasks' (cleanupStaleData) has no tenant_id constraint — will purge disabled delegation tasks across all tenants.
  - _Fix:_ Do not run unscoped DELETE on a tenant-scoped table. Either add WHERE tenant_id = ${tenantId} and run per-tenant, or require an explicit, auditable global-admin operation before performing cross-tenant deletes. Prefer fail-closed per-tenant maintenance to avoid accidental cross-tenant data removal.
- **server/stability-watchdog.ts:225** — DELETE FROM 'event_log' (cleanupStaleData) executed with no tenant_id constraint — can remove other tenants' event logs globally.
  - _Fix:_ Constrain deletions to tenant_id = ${tenantId} or otherwise require explicit admin/global privileges and thorough audit logging before performing cross-tenant deletes. Avoid blanket deletes on tenant-scoped tables.
- **server/stability-watchdog.ts:259** — DELETE FROM 'heartbeat_logs' (pruneOldLogs) runs without any tenant_id WHERE clause — pruning will affect logs for all tenants without scoping.
  - _Fix:_ Scope pruning to a tenant (WHERE tenant_id = ${tenantId} AND created_at < ...) and/or iterate tenants explicitly. If multi-tenant/global pruning is intended, require an explicit ALL_TENANTS/administrative flag and auditing; never run unscoped destructive queries on tenant-scoped tables by default.
- **server/storage.ts:580** — SELECT * FROM tenant-scoped table `memory_entries` with no tenant filter (getAllMemoriesForBackup). This returns all tenants' memory entries to the caller.
  - _Fix:_ Restrict this API: require an explicit tenantId or an explicit ALL_TENANTS/admin sentinel, or move to an admin-only endpoint. If a global backup is intended, enforce admin authorization and log/limit usage; otherwise scope queries with requiredTenantScope(memory_entries.tenantId, tenantId).
- **server/storage.ts:1351** — getAllDataForExport selects many tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, dailyNotes, heartbeatTasks, etc.) without any tenant scoping parameter. This returns platform-wide tenant data to the caller.
  - _Fix:_ Restrict this export API to an admin-only operation that requires explicit authorization and an explicit ALL_TENANTS sentinel, or change it to require a tenantId so callers only receive a single tenant's data. Add runtime enforcement and auditing for any global export path.
- **server/tool-learning.ts:316** — revertToolRow deletes from custom_tools using db.delete(customTools).where(eq(customTools.id, tool.id)) with NO tenant_id constraint. This can delete a custom tool belonging to another tenant if an attacker controls or guesses an id.
  - _Fix:_ Require tenantId (or derive it from the tool row) and include AND tenant_id = ${tenantId} in the DELETE WHERE clause (or use .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))). Always enforce tenant scoping on destructive operations.

### HIGH (126)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without tenant_id constraint (reads all tenants' experiments).
  - _Fix:_ Scope the query to a single tenant (e.g. add AND tenant_id = ${tenantId}) or, if this heartbeat must operate across tenants, iterate tenants and run a tenant-scoped query per tenant (or require explicit admin/privileged context and audit). Avoid unconstrained selects on tenant-scoped tables.
- **server/agent-activity.ts:122** — UPDATE against tenant-scoped table `agent_activity` missing a tenant_id constraint (UPDATE agentActivity WHERE id = activityId). This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (for example: WHERE id = ${activityId} AND tenant_id = ${tenantId}) and require the caller to pass tenantId. Alternatively validate ownership before updating.
- **server/agent-eval.ts:218** — UPDATE to tenant-scoped table `agent_evals` uses WHERE id = ${evalId} without constraining tenant_id. A malicious or buggy evalId could update another tenant's row.
  - _Fix:_ Constrain updates by tenant (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}). Ensure tenantId is validated and passed through to the update.
- **server/agentic-features.ts:861** — SELECT existing enrollments from `outreach_enrollments` checks sequence_id and contact_email but does not include tenant_id, allowing cross-tenant matches.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND tenant_id = ${params.tenantId} AND status = 'active'). Validate ownership before using the result.
- **server/agentic-features.ts:1021** — UPDATE outreach_enrollments sets reply_classification/etc. WHERE id = ${row.id} without constraining tenant_id, allowing an update to a row in another tenant if id is guessed or stale.
  - _Fix:_ Add tenant scoping to the update (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to ensure the update only affects rows owned by the calling tenant.
- **server/agentic/approvals.ts:135** — Bulk UPDATE in expireStaleApprovals updates agentApprovals WHERE status='pending' AND expiresAt < now() with no tenant_id filter, affecting approvals across all tenants.
  - _Fix:_ Scope the expiry operation to a tenant (require tenantId) or, if a global sweep is intended, ensure it runs under a privileged, auditable context and consider iterating per-tenant and performing tenant-scoped updates. At minimum, include tenant_id in the WHERE clause for tenant-scoped updates.
- **server/agentic/department-budgets.ts:143** — SELECT from tenant-scoped table `department_budgets` without a tenant_id constraint (SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'). This reads tenant-scoped rows across all tenants in a non-filtered query.
  - _Fix:_ If this is intended to be an admin/global sweep, make that explicit and ensure only an authorized admin process can call it (and document/audit the operation). Otherwise restrict the query to a single tenant by adding a tenant_id filter (e.g. WHERE tenant_id = ${tenantId} AND status = 'active'). Alternatively, move cross-tenant aggregation into a dedicated, explicitly-audited admin-only routine or a global table so tenant-scoped rows are not read by non-admin contexts.
- **server/auto-asset-capture.ts:184** — Backfill SELECT scans the conversations table across all tenants without scoping to a caller tenant. The query SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... reads tenant-scoped rows for all tenants.
  - _Fix:_ Either run this backfill under an approved platform-admin context (and document/authorize that cross-tenant operation), or make the query per-tenant by adding a WHERE tenant_id = ${tenantId} filter (and accept a tenantId parameter or iterate tenant-by-tenant). Always ensure queries against tenant-scoped tables include an explicit tenant_id constraint unless the operation is explicitly authorized as a platform-level maintenance task.
- **server/auto-project.ts:135** — SELECT from messages lacks tenant constraint. The query reads messages WHERE conversation_id = ${conversationId} without also constraining m.tenant_id = ${tenantId}. This allows a conversation id to return messages that may belong to a different tenant if IDs are re-used/poisoned.
  - _Fix:_ Constrain the messages query to the caller's tenant (e.g. add AND m.tenant_id = ${tenantId}) or join via the verified conversation row (e.g. JOIN conversations c ON c.id = m.conversation_id AND c.tenant_id = ${tenantId}) to enforce ownership before reading messages.
- **server/auto-transcript.ts:46** — SELECT from messages lacks tenant constraint. The code selects messages WHERE conversation_id = ${conversationId} but does not include a tenant_id filter on messages.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or join messages to conversations with c.tenant_id = ${tenantId}) so the read is explicitly scoped to the caller's tenant.
- **server/auto-transcript.ts:132** — Subquery reads messages without tenant constraint. The backfill query uses (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 with no tenant scoping inside the subquery.
  - _Fix:_ Include tenant scoping in the subquery (e.g. SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) or otherwise ensure the subquery is explicitly constrained to the conversation's tenant.
- **server/auto-transcript.ts:160** — Subquery reads messages without tenant constraint. The alsoLinked backfill query uses (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 with no tenant scoping.
  - _Fix:_ Add tenant scoping inside the subquery (for example: SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) so the existence check is constrained to the correct tenant.
- **server/auto-transcript.ts:165** — UPDATE on conversations lacks tenant constraint. The backfill does UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL without verifying tenant_id, allowing accidental cross-tenant modification if ids are misbound.
  - _Fix:_ Include AND tenant_id = ${c.tenant_id} (or the caller's tenantId) in the WHERE clause to ensure the UPDATE only affects the tenant-owned conversation row.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table, the INSERT omits the tenant_id column (INSERT INTO invoice_items (invoice_id, ...) VALUES (${newId}, ...)).
  - _Fix:_ Ensure tenant_id is recorded on every tenant-scoped child row. Either add tenant_id to the invoice_items INSERT (e.g. VALUES (${newId}, ${tid}, ...)) or otherwise derive/verify the tenant before inserting (e.g. assert the parent invoice id belongs to the caller's tenant and include tenant_id). Make the tenant column explicit so no rows can be created without an owning tenant.
- **server/business-tools.ts:234** — addCustomer() inserts assigned_to directly from caller input into the customers row without validating that the referenced user belongs to the caller's tenant. This writes a foreign id (assigned_to) without ownership verification.
  - _Fix:_ Validate foreign IDs (like assigned_to) before storing. Query the users (or relevant) table: SELECT id FROM users WHERE id = ${params.assigned_to} AND tenant_id = ${tid} LIMIT 1 and fail if not owned by the tenant. Alternatively, restrict assigned_to to server-controlled values (e.g. current authenticated user) rather than accepting arbitrary caller-supplied IDs.
- **server/business-tools.ts:269** — updateCustomer() allows updating assigned_to (and other FK-like fields) from caller input without verifying ownership of that foreign id. The code accepts assigned_to and writes it via the updateMap/sql path but does not check that assigned_to belongs to this tenant.
  - _Fix:_ Before applying updates that set foreign IDs (assigned_to or any other FK), verify ownership: SELECT id FROM users WHERE id = ${value} AND tenant_id = ${tid} LIMIT 1 (or equivalent). Reject the update if the referenced row is not owned by the tenant. Consider an allowlist of updatable FKs and explicit validation for each.
- **server/channel-routing.ts:31** — SELECT joins the personas table without constraining by tenant. If personas are tenant-scoped, this returns persona rows across tenants (no WHERE p.tenant_id = ...)
  - _Fix:_ Make channel routing queries tenant-aware: accept a tenantId parameter and add an explicit tenant filter. For example: WHERE cr.enabled = true AND cr.tenant_id = ${tenantId} and JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId} (or p.tenant_id = cr.tenant_id). Validate callers supply tenantId and never read persona rows without that filter.
- **server/channel-routing.ts:63** — getPersonaForChannel selects persona_id from channel_routes without restricting by tenant. If channel_routes are (or should be) tenant-scoped, this will return another tenant's route.
  - _Fix:_ Require and pass tenantId to getPersonaForChannel and add WHERE tenant_id = ${tenantId} (or filter by cr.tenant_id = ${tenantId}). If channel_routes are global by design, document that explicitly and add server-side checks to avoid returning tenant-specific persona ids to callers from other tenants.
- **server/chat-engine.ts:3668** — Capability-review telemetry call (markReviewReused) invoked with conversationId but WITHOUT any tenant identifier. If markReviewReused performs reads/writes against tenant-scoped tables, it may execute those operations without scoping to the caller's tenant, breaking tenant isolation.
  - _Fix:_ Pass the caller's tenantId into markReviewReused and/or have markReviewReused require and assert tenant ownership before any DB reads/writes. Prefer an explicit parameter (e.g. markReviewReused(tenantId, conversationId, ...)) and validate that the conversation belongs to that tenant before persisting telemetry. Ensure underlying DB queries include WHERE tenant_id = <tenantId> or equivalent Drizzle .where(eq(table.tenantId, tenantId)).
- **server/chat-engine.ts:4006** — Stuck-diagnostics calls (recordToolCallForStuckDetection and postDiagnosticReport) are invoked with conversationId/tool metadata but WITHOUT passing tenantId. If these functions persist or read tenant-scoped diagnostic rows, they may omit tenant scoping and allow cross-tenant contamination or orphaned rows.
  - _Fix:_ Include tenantId in calls to stuck-diagnostics functions (e.g. recordToolCallForStuckDetection(tenantId, conversationId, ...), postDiagnosticReport(tenantId, ...)), and/or ensure those functions internally require and validate tenant ownership of any referenced conversation/tool rows. All DB operations inside these helpers must add WHERE tenant_id = <tenantId> (or use Drizzle .where(eq(table.tenantId, tenantId))). If any function must accept only a conversationId, perform an ownership check first (read the conversation with a tenant filter) before proceeding.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` uses only project_id (WHERE project_id = ${projectId}) and does NOT include tenant_id filtering. Relying on a previously-validated projectId is not an explicit SQL tenant fence.
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. AND tenant_id = ${tenantId}) or join against projects and assert p.tenant_id = ${tenantId} in the same SQL. Every read of a tenant-scoped table must include an application-level tenant_id WHERE clause.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` uses only project_id (WHERE project_id = ${projectId}) and does NOT include tenant_id filtering.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise assert tenant ownership in the same SQL). Do not rely solely on earlier validation of projectId; add an explicit tenant_id filter on every tenant-scoped query.
- **server/cmmc-assessments.ts:150** — getCmmcAccessByToken performs a SELECT joining cmmc_assessment_invitations and cmmc_assessments by tokenHash without a tenant_id constraint — this queries tenant-scoped tables across tenants using an externally-supplied token.
  - _Fix:_ Scope the lookup by tenant when a tenant context is available, or store a token→tenant mapping so lookups are a single-row, tenant-scoped query (e.g. WHERE token_hash = ... AND tenant_id = ...). If the token must be globally valid, ensure the invitation table is the authoritative, indexed mapping and design access so the query cannot be used to enumerate or access other tenants' rows. Add explicit tenant_id checks in the SQL where appropriate.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` (scanAndEscalate) omits tenant_id predicate: `SELECT id, tenant_id AS "tenantId", description, due_at AS "dueAt", ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...` — this reads rows across all tenants.
  - _Fix:_ If this is intended only for a platform-admin job, add an explicit admin-authorisation guard and document it; otherwise restrict the SELECT by tenant (e.g. `WHERE tenant_id = ${tenantId} AND ...`) or enumerate tenants first and run tenant-scoped queries to avoid cross-tenant reads.
- **server/data-protection.ts:149** — Purge sweep selects conversations across tenants without scoping: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}`. This enumerates tenant-scoped rows across all tenants (cross-tenant read) before per-row deletes.
  - _Fix:_ Either iterate tenants explicitly and run tenant-scoped SELECTs, or add an admin authorization check and ensure any returned rows are only processed by an appropriately privileged context. Prefer performing per-tenant SELECTs (e.g. include `AND tenant_id = ${tenantId}`) to satisfy tenant isolation invariants.
- **server/data-protection.ts:405** — COUNT query against tenant-scoped `messages` omits tenant predicate: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}`. This can surface cross-tenant existence/count information.
  - _Fix:_ Include the tenant filter (e.g. `AND tenant_id = ${tenantId}`) in the WHERE clause, or join to conversations ensuring conversation→tenant ownership is enforced in the same query.
- **server/data-protection.ts:360** — Query against tenant-scoped table `project_notes` omits tenant predicate: `SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}` — project_id alone is not sufficient to guarantee tenant scoping.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or join to projects with tenant_id) to ensure notes returned belong to the same tenant as the project. Never rely solely on project_id without tenant ownership verification for tenant-scoped reads.
- **server/data-protection.ts:371** — Query against tenant-scoped table `project_files` omits tenant predicate: `SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}` — this can return another tenant's files for the same project_id value if ownership isn't enforced.
  - _Fix:_ Scope the query by tenant (e.g. `WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}`) or join to projects verifying project.tenant_id = tenantId to enforce ownership before reading child rows.
- **server/dfy-intake.ts:50** — SELECT from tenant-scoped table dfy_intake_forms by token without constraining tenant_id. getDfyIntakeByToken queries dfy_intake_forms WHERE token = ... with no tenant_id filter, allowing a caller with a valid token to read a row regardless of tenant context.
  - _Fix:_ Either require and pass an explicit tenantId to getDfyIntakeByToken and add AND dfy_intake_forms.tenant_id = ${tenantId}, or resolve the token to its owning tenant server-side and validate that before returning the row. If the token is intentionally cross-tenant, document and isolate this behavior and ensure the token-to-tenant mapping is checked before returning any tenant-scoped data.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages is executed without any tenant_id constraint. getTrafficSnapshot() queries the messages table across all tenants, violating the requirement that reads of tenant-scoped tables must be tenant-scoped.
  - _Fix:_ Make getTrafficSnapshot tenant-aware (accept a tenantId parameter) and add AND tenant_id = ${tenantId} to the query, or document and enforce that this module is intentionally global and only runs with explicit global/admin privileges. Prefer per-tenant counts when decisions affect tenant-visible configuration.
- **server/dormant-deprecation.ts:179** — COUNT(DISTINCT tool_name) FROM tool_performance executed without tenant_id constraint. getTrafficSnapshot() aggregates tool performance globally instead of per-tenant.
  - _Fix:_ Filter by tenant_id (e.g. WHERE last_success_at > ... AND tenant_id = ${tenantId}) or make the function explicitly accept a tenantId if the traffic gate should be evaluated per-tenant. If the behavior is intended to be global, ensure it runs in a secured global admin context and cannot be influenced by untrusted tenants.
- **server/dormant-deprecation.ts:202** — SELECT DISTINCT tool_name FROM tool_performance executed without tenant_id constraint (getEverInvokedTools). This reads a tenant-scoped table globally.
  - _Fix:_ Add tenant scoping (accept tenantId and add WHERE tenant_id = ${tenantId}) or otherwise ensure this global read is intentionally performed in a safe admin context. Avoid scanning tenant-scoped tables without an explicit tenant filter.
- **server/dormant-deprecation.ts:213** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... executed without tenant_id constraint (getActiveTools14d). This returns cross-tenant data from a tenant-scoped table.
  - _Fix:_ Scope the query by tenant_id (accept tenantId param) and filter WHERE last_success_at > ... AND tenant_id = ${tenantId}. If the code intentionally inspects global usage, ensure it runs under an admin-only pathway and is documented as a deliberate global operation.
- **server/email.ts:371** — SELECT from inbox_messages performed without constraining by tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This reads a tenant-scoped table across all tenants.
  - _Fix:_ Include an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) or otherwise scope the lookup to the correct tenant/inbox before the SELECT so the query cannot return rows belonging to other tenants.
- **server/email.ts:416** — SELECT from inbox_messages performed without constraining by tenant_id (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id). This reads tenant-scoped inbox_messages across all tenants.
  - _Fix:_ Scope the maintenance query to a single tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. Never run wide SELECTs on tenant-scoped tables without an explicit tenant_id constraint.
- **server/embeddings.ts:811** — SELECT from agent_knowledge executed without tenant_id constraint (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50). This reads tenant-scoped rows across tenants.
  - _Fix:_ Run backfill per-tenant or add an explicit WHERE tenant_id = ${tenantId} filter. If this is intended to be a cross-tenant admin operation, do so with an explicit admin/maintenance path and clear audit/authorization, but still include tenant scoping when updating rows.
- **server/embeddings.ts:842** — SELECT from memory_entries executed without tenant_id constraint (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200). This reads tenant-scoped data across all tenants.
  - _Fix:_ Perform the backfill per-tenant (include WHERE tenant_id = ${tenantId}) or otherwise ensure the operation has explicit admin authorization and that every downstream update is tenant-scoped.
- **server/embeddings.ts:858** — SELECT from agent_knowledge executed without tenant_id constraint (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ... LIMIT 200). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Include WHERE tenant_id = ${tenantId} or make the backfill run tenant-by-tenant, and ensure subsequent updates use the same tenant context.
- **server/firecrawl.ts:239** — UPDATE on scrapedPages uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) and lacks an explicit tenant_id constraint. This allows an update to affect a row outside the caller's tenant if ids are reused or assumptions change.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so every mutation is explicitly scoped to the caller's tenant.
- **server/firecrawl.ts:372** — UPDATE on scrapedPages in the crawl loop uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) and does not include the tenant_id filter. This violates the tenant-isolation invariant.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to ensure only the calling tenant's rows can be modified.
- **server/google-drive.ts:867** — SELECT from projects fetches a project row by id with no WHERE tenant_id = <caller tenant>. Although code checks project.tenantId afterwards, the SQL itself is not tenant-scoped as required by the invariant.
  - _Fix:_ Add the tenant constraint to the SELECT (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the database read is explicitly scoped to the caller's tenant.
- **server/google-drive.ts:880** — UPDATE projects sets driveFolderId/driveFolderUrl using only projects.id in the WHERE clause (eq(projects.id, projectId)) with no tenant_id constraint. This can update another tenant's project record.
  - _Fix:_ Include the tenant filter in the UPDATE WHERE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) to enforce tenant-scoped writes.
- **server/google-drive.ts:1477** — SELECT proj in uploadAndShare reads projects by id (where(eq(projects.id, params.projectId))) without constraining the query to the caller's tenant before further logic. The subsequent ownership check is done in application logic but the SQL itself is not tenant-scoped.
  - _Fix:_ Make the SQL tenant-scoped by adding a tenant_id filter (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the DB read is limited to the caller's tenant.
- **server/glasses-gateway.ts:173** — db.update(apiKeys).set(...).where(eq(apiKeys.id, key.id)) updates apiKeys by id only and does not include a tenant_id constraint. apiKeys is tenant-associated; updating by id alone violates the invariant.
  - _Fix:_ Add the tenant constraint to the WHERE clause (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or otherwise scope the UPDATE to the apiKey's tenant to prevent cross-tenant mutations.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id. If presenter_slide_images is a tenant-scoped table this write can insert data without an explicit tenant binding.
  - _Fix:_ Include tenant_id in the INSERT values (or otherwise derive and validate the tenant) and/or verify the sessionId belongs to the expected tenant before persisting. Example: INSERT ... (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...) or perform a prior check SELECT tenant_id FROM presenter_sessions WHERE id = ${sessionId} and use that tenant_id in the insert.
- **server/google-workspace.ts:2293** — INSERT into presenter_slide_images (thumbnail path) does not include tenant_id. Same lack of tenant scoping as other presenter_slide_images writes.
  - _Fix:_ As above: add tenant_id to the INSERT and/or verify session ownership. Ensure both 'full' and 'thumb' image rows include tenant_id or are tied by a session->tenant FK validated before writing.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint. Updating presenter_sessions by id only can modify another tenant's session if session IDs are predictable or leaked.
  - _Fix:_ Constrain the UPDATE to the owning tenant, e.g. UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Alternatively, read/validate the session's tenant first and fail if it doesn't match the expected tenant context.
- **server/health-audit.ts:314** — SELECT FROM code_proposals has no tenant_id filter. code_proposals appears tenant-scoped; selecting rows across all tenants can leak or operate on other tenants' data unintentionally.
  - _Fix:_ If this scanner is intended to run per-tenant, add WHERE tenant_id = ${tenantId} (and pass tenantId). If it must run as a cross-tenant admin job, make that explicit (admin-only) and document/centralize the cross-tenant behavior. Prefer scanning per-tenant to preserve isolation.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) does not scope by tenant_id. This will update matching proposal ids across tenants without a tenant constraint.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}, or ensure the initial SELECT already limited results to a single tenant and use that tenant for the UPDATE. If the intent is cross-tenant administration, require explicit admin authorization and audit that action.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (stuck-task detection) lacks a tenant_id filter. heartbeat_tasks is likely tenant-scoped; scanning/updating tasks across tenants can disable other tenants' scheduled work.
  - _Fix:_ If this is intended as a cross-tenant housekeeping job, restrict access to an admin-only path and document it. Otherwise, scope the SELECT to a tenant (WHERE tenant_id = ${tenantId}) and run per-tenant. Avoid blind global SELECTs on tenant-scoped tables.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) updates tasks without tenant constraint. This will affect tasks across tenants with matching ids if tenant scoping isn't enforced.
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}, or ensure the earlier SELECT was tenant-scoped and validated. For cross-tenant admin operations require explicit admin authorization and add an audit log.
- **server/heartbeat-startup.ts:76** — Raw UPDATE on tenant-scoped table 'heartbeat_tasks' without constraining tenant_id. The query `UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id}` can modify a row from any tenant.
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. add `AND tenant_id = ${t.tenantId}`) or use the scoped storage helper (e.g. storage.updateHeartbeatTask) which enforces tenant scoping. Ensure every UPDATE/DELETE/SELECT on tenant-scoped tables includes an explicit tenant_id WHERE clause unless it is intentionally and safely implemented in a documented scheduler cross-tenant boundary.
- **server/heartbeat-startup.ts:80** — Raw UPDATE on tenant-scoped table 'heartbeat_tasks' without constraining tenant_id. The query `UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id}` lacks a tenant filter.
  - _Fix:_ Add an explicit tenant constraint (e.g. `AND tenant_id = ${t.tenantId}`) or perform the change via a storage-layer API that enforces tenant scoping. Avoid raw SQL updates on tenant-scoped tables without verifying tenant ownership.
- **server/heartbeat.ts:256** — SELECT from tenant-scoped table 'event_log' with no tenant_id filter: `SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10`. This reads pending events across all tenants.
  - _Fix:_ If cross-tenant processing is intentional, move this into a well-audited scheduler-only boundary and document it; otherwise add an explicit tenant_id filter (or iterate per tenant and query per-tenant). Prefer using a dedicated scheduler storage API that clearly expresses cross-tenant intent rather than ad-hoc unscoped SELECTs.
- **server/heartbeat.ts:2609** — UPDATE on tenant-scoped table 'heartbeat_tasks' without tenant constraint: `UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}`. The UPDATE omits tenant_id, allowing modification of a row in any tenant with the same id.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. `WHERE id = ${newTask.id} AND tenant_id = ${tenantId}`) or call a scoped storage method (storage.updateHeartbeatTask) that enforces tenant ownership before updating approval_status.
- **server/heartbeat.ts:3065** — UPDATE on tenant-scoped table 'heartbeat_tasks' without tenant constraint: `UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}`. This writes approval_status without verifying tenant ownership of the row.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. `AND tenant_id = ${tenantId}`) or use the application storage API that applies tenant scoping. Ensure all direct SQL updates to tenant-scoped tables include tenant_id checks to prevent cross-tenant modification.
- **server/job-queue.ts:346** — listJobs builds a SELECT from agent_jobs that may be emitted with no tenant filter (when tenantId is omitted) returning jobs across all tenants.
  - _Fix:_ Make tenant scope mandatory for tenant-facing APIs (require tenantId) and provide an explicit admin path for global listing. If global listing is required, ensure only properly authorized admin principals can call it and audit/log such calls. Alternatively return an error when tenantId is not provided unless caller is admin.
- **server/job-queue.ts:420** — getJobStats runs a SELECT FROM agent_jobs grouped by status and injects tenantFilter which may be empty — the query can aggregate across all tenants when called without tenant scope.
  - _Fix:_ Require an explicit tenant scope parameter (or ALL_TENANTS constant only allowed for admin callers). Validate the caller's permissions when `ALL_TENANTS` is used. Prefer per-tenant aggregation by adding `AND tenant_id = ${tenantId}`.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes a cross-tenant query: SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id — it scans agent_knowledge across all tenants.
  - _Fix:_ If the monitor is intended to run across tenants, confine it to an admin-only background job with strict access controls and auditing. Otherwise, change the process to iterate tenants via an explicit per-tenant loop driven by a vetted tenant list and pass tenantId into loadEmbeddings/snapshotKnowledgeDiversity so every SELECT/UPDATE is explicitly tenant-scoped.
- **server/lib/aevo-meta-editor.ts:449** — UPDATE on procedure_evolution_runs uses only the run id predicate (WHERE id = runId) and does not constrain the write to the caller's tenant (missing tenant_id filter).
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) so the UPDATE can only affect rows for the expected tenant. Ensure the tenantId value used is the caller's trusted tenant context (already available in this function).
- **server/lib/aevo-meta-editor.ts:459** — UPDATE on procedure_evolution_runs uses only the run id predicate (WHERE id = runId) and does not constrain the write to the caller's tenant (missing tenant_id filter).
  - _Fix:_ Add a tenant_id constraint to the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) to ensure the update is scoped to the correct tenant. Use the local tenantId passed into proposeProcedureEdit rather than relying solely on runId.
- **server/lib/aevo-meta-editor.ts:503** — UPDATE on procedure_evolution_runs uses only the run id predicate (WHERE id = runId) and does not constrain the write to the caller's tenant (missing tenant_id filter).
  - _Fix:_ Constrain the UPDATE by tenant as well: .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId))). Always include tenant-scoped predicates on mutations against tenant-scoped tables.
- **server/lib/agent-trace.ts:130** — UPDATE agent_trace_spans sets ended_at/status/summary with WHERE span_id = ${spanId} but does not include tenant_id, allowing potential cross-tenant updates if span_id is misused.
  - _Fix:_ Scope the UPDATE to the tenant that opened the span. For example, persist the tenantId at span open (or pass it into persistSpanClose) and change the WHERE to include tenant_id = ${tenantId} (or use .where(and(eq(agent_trace_spans.spanId, spanId), eq(agent_trace_spans.tenantId, tenantId)))) so every close/update is tenant-scoped.
- **server/lib/customer-revenue-launchpad-seed.ts:90** — SELECT from tenant-scoped table `project_notes` is missing an explicit tenant_id constraint. The query selects notes by project_id only: `SELECT id FROM project_notes WHERE project_id = ${projectId} AND note = ${POINTER_NOTE}` — relying on project_id alone is not the required application-level tenant filter.
  - _Fix:_ Constrain the read to the tenant explicitly. Add a tenant_id equality check or join through projects to verify ownership, e.g. `SELECT id FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${OWNER_TENANT_ID} AND note = ${POINTER_NOTE}` or `SELECT pn.id FROM project_notes pn JOIN projects p ON pn.project_id = p.id WHERE pn.project_id = ${projectId} AND p.tenant_id = ${OWNER_TENANT_ID} AND pn.note = ${POINTER_NOTE}` so the query cannot return rows from other tenants.
- **server/lib/inbox-ingest.ts:536** — Query joins inbox_messages m but does not constrain the joined inbox_messages rows to the tenant — SELECT c.kind... FROM inbox_classifications c JOIN inbox_messages m ON m.id = c.inbox_message_id WHERE c.tenant_id = ${tenantId} ... reads m.* without verifying m.tenant_id. Because tenant ownership is enforced only by app-level filters (no RLS), this can return rows from another tenant if ids collide.
  - _Fix:_ Constrain the joined inbox_messages to the same tenant. For example, add an explicit tenant equality check in the JOIN or WHERE: JOIN inbox_messages m ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId} (or add AND m.tenant_id = ${tenantId} to the WHERE). Ensure every read of tenant-scoped tables includes tenant_id = <caller tenant>.
- **server/lib/mention-injection.ts:121** — Query LEFT JOINs personas p ON p.id = cm.from_persona_id but does not constrain personas by tenant. The SELECT reads p.name (from_persona_name) without verifying p.tenant_id = cm.tenant_id/tenantId, risking cross-tenant data exposure if persona ids collide across tenants.
  - _Fix:_ Restrict the joined personas rows to the same tenant. Add a tenant guard in the JOIN or WHERE, e.g. LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId} (or p.tenant_id = cm.tenant_id). Ensure all joins against tenant-scoped tables include an explicit tenant_id constraint.
- **server/lib/outcome-promotion.ts:158** — SELECT from tenant-scoped table `deliverable_grade_events` is executed without a WHERE tenant_id = ... filter (global aggregation over all tenants). This violates the invariant that tenant-scoped tables must be constrained by tenant_id at the application layer.
  - _Fix:_ Narrow the query to a single tenant or iterate per-tenant under an admin-only job. Options: (a) if this routine must process one tenant at a time, add a parameter and change the query to include WHERE tenant_id = ${tenantId}; (b) if it must aggregate across tenants, ensure the aggregation is explicit and confined to a trusted admin-only background process and does not return or expose raw tenant rows to other tenants; or (c) rewrite the flow to query each tenant separately (SELECT ... FROM deliverable_grade_events WHERE tenant_id = ${tenantId} ...) and process/write per-tenant outputs to avoid cross-tenant reads.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE against tenant-scoped table `scheduled_posts` is executed without any tenant_id predicate (CTE selects ids and the UPDATE joins only on id). This reads/locks rows across all tenants in a single operation.
  - _Fix:_ Restrict the CTE/UPDATE to a single tenant (e.g. add WHERE tenant_id = ${tenantId} to the SELECT and to the UPDATE), or run the pickup loop per-tenant inside an explicit admin-approved loop. If this is intentionally system-scoped, make the admin intent explicit and add additional authorization checks and audit logging; avoid leaving raw tenant-scoped reads/locks without an explicit tenant predicate.
- **server/memory-graph.ts:124** — UPDATE on tenant-scoped table `memory_categories` omits tenant predicate — `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}` lacks `tenant_id` constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: `... WHERE id = ${catId} AND tenant_id = ${tenantId}` (or equivalent Drizzle eq on tenant_id) to enforce tenant isolation.
- **server/memory-graph.ts:543** — UPDATE on tenant-scoped table `memory_entries` omits tenant predicate — `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}` lacks `tenant_id` constraint.
  - _Fix:_ Include the tenant check in the WHERE clause. Example: `UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}` to ensure the update only affects the caller's tenant.
- **server/memory-intelligence.ts:399** — UPDATE on tenant-scoped table `knowledge_triples` omits tenant predicate — `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}` does not include `tenant_id`.
  - _Fix:_ Restrict the UPDATE by tenant as well. Example: `... WHERE id = ${c.id} AND tenant_id = ${tenantId}` so only triples belonging to the caller's tenant are modified.
- **server/martech-bundle.ts:325** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only the row id in the WHERE clause (`WHERE id = existing.id`) and does not constrain by tenant_id.
  - _Fix:_ Add an explicit tenant predicate to the UPDATE. Example using Drizzle: `.where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))` (or add `AND tenant_id = ${opts.tenantId}`) to enforce tenant ownership.
- **server/mpeg-engine.ts:1058** — INSERT INTO project_files is executed without including an explicit tenant_id column/value (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)). Per the project invariant, tenant-scoped rows must include tenant_id on INSERT. Even though assertProjectInTenant is used to verify ownership, the INSERT still omits tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). If the table is intentionally project-scoped and lacks tenant_id, ensure schema and all consumers are updated so tenant_id is stored or otherwise enforceable, and prefer explicit tenant_id columns to avoid relying solely on foreign-key checks.
- **server/mpeg-engine.ts:1445** — Duplicate occurrence: INSERT INTO project_files in parallel-produce path omits tenant_id (INSERT INTO project_files (provider?, tenant_id?) ... actually uses VALUES (${options.projectId}, ${safeTitle + ".mp4"}, ${concatResult.filePath}, ${driveUrl || null}, ${"video"}, ${concatResult.sizeBytes || 0}, ${"mpeg-engine-parallel"})). The tenant_id column/value is not inserted, violating the requirement that tenant-scoped inserts set tenant_id explicitly.
  - _Fix:_ As above, include tenant_id explicitly in the INSERT for project_files (INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure every code path that writes tenant-scoped tables includes and populates tenant_id using the caller's validated tenant context.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() to inject ledger lock key into a SQL statement (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)). The lockKey is derived from caller-provided ledgerKey and tenantId, and using sql.raw() bypasses parameterization and can enable SQL injection or break assumptions about filtering/isolation.
  - _Fix:_ Avoid sql.raw() with any caller-controlled input. Use a parameterized value (e.g. pass the bigint as a normal template parameter) or compute the numeric lock key in a safe, typed way and bind it via the query placeholder. Example: await tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`); or better, compute a numeric lockKey on the server and pass it as a bound parameter so Drizzle can sanitize it.
- **server/persona-export.ts:39** — Querying skills with: SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL — this does not filter by tenant_id. If skills are tenant-scoped (or there are tenant-specific overrides), this will return rows from other tenants and leak data.
  - _Fix:_ If skills are tenant-scoped, add tenant_id = ${tenantId} to the WHERE clause (and preserve global skills via persona_id IS NULL if intended). Example: WHERE (persona_id = ${personaId} OR persona_id IS NULL) AND (tenant_id = ${tenantId} OR tenant_id IS NULL) — or otherwise explicitly document and enforce which skill rows are global vs tenant-scoped.
- **server/pace-control.ts:85** — Queries against heartbeat_logs (COUNT, snapshots) do not include any tenant_id filter. heartbeat_logs is likely tenant-scoped; running global counts without scoping allows one tenant to affect or read another tenant's pacing metrics (cross-tenant interference/leakage).
  - _Fix:_ Make pacing functions tenant-aware: accept a tenantId parameter and filter heartbeat_logs by tenant_id = ${tenantId} (or add tenant partitioning). For aggregate snapshots that must be cross-tenant, make the intent explicit and protect admin-only access. Ensure all reads/writes on tenant-scoped tables include an explicit tenant_id predicate.
- **server/platform-admin-conversations.ts:18** — getConversationForPlatformAdmin queries the conversations table by id without any tenant_id constraint. Although the function checks isPlatformAdmin(req), the SQL itself lacks an explicit tenant filter and reads a tenant-scoped table cross-tenant.
  - _Fix:_ Either (a) add an explicit tenant_id filter when the caller's tenant is known, or (b) make the cross-tenant intent explicit in the SQL (e.g. include tenant_id in the SELECT/WHERE or an explicit marker) and ensure strong admin-only authorization, audit logging, and justification. Prefer explicitly passing a tenantId and using WHERE tenant_id = ${tenantId} when tenant-scoped access is intended.
- **server/process-governor.ts:396** — SELECT joins personas (LEFT JOIN personas p ON p.id = ad.persona_id) without constraining the joined personas table by tenant_id. If personas is tenant-scoped this can read other tenants' rows via the join.
  - _Fix:_ Restrict the join to the same tenant (for example: LEFT JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) or explicitly add AND p.tenant_id = ${tenantId} in the query so joined rows are scoped to the caller's tenant.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project row without any tenant_id constraint. If projects are tenant-scoped this leaks other tenants' project metadata.
  - _Fix:_ Pass the caller's tenantId into updateProjectBrain and add a tenant filter (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise verify ownership before reading project data.
- **server/project-brain.ts:332** — Backfill query selects DISTINCT p.id, p.name FROM projects JOIN conversations ... with no tenant_id filters on projects, conversations, or messages. This performs cross-tenant reads and will expose data across tenants.
  - _Fix:_ Scope backfill runs by tenant (add WHERE p.tenant_id = ${tenantId} and appropriate tenant filters on conversations/messages), or iterate tenants explicitly and run the backfill per-tenant so every DB read includes tenant_id = ${tenantId}.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks any tenant_id constraint; conversations are likely tenant-scoped and this can return rows from other tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) and ensure proj includes tenant context, or ensure the outer project query already restricted to a tenant and validate before using proj.id.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} executes a messages read without scoping by tenant_id (or verifying the conversation belongs to the caller), enabling cross-tenant data leakage.
  - _Fix:_ Add tenant filtering (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or validate conversation ownership before selecting messages; propagate tenantId into backfill/update flows so every message read is tenant-scoped.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id filter (global scan): `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500`.
  - _Fix:_ If this is intended to be a tenant-scoped read, add an explicit WHERE tenant_id = ${tenantId} filter. If it must scan across tenants (cron/runner), make the global intent explicit (e.g. call the existing listScheduledMessages with the documented _allowGlobalSystemUseOnly opt-in), and ensure all downstream writes use a tenant-scoped WHERE. Avoid unscoped SELECTs against tenant-scoped tables unless explicitly limited and audited.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses only id in the WHERE clause: `UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}`. The UPDATE is not constrained by tenant_id.
  - _Fix:_ Include tenant scoping in the UPDATE to prevent cross-tenant modification. For example: `WHERE id = ${storedId} AND tenant_id = ${input.tenantId}`. Prefer verifying ownership (SELECT ... WHERE id = X AND tenant_id = Y) before mutating, or always include tenant_id in the UPDATE/DELETE WHERE clause.
- **server/routes.ts:1384** — Query reads tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) without any tenant_id constraint. This SELECT aggregates data across all tenants in an endpoint that uses admin auth, but the SQL itself lacks an explicit tenant filter.
  - _Fix:_ Scope each query to an explicit tenant_id using parameterized filters (e.g. .where(eq(conversations.tenantId, tenantId)) or add WHERE tenant_id = ${ADMIN_TENANT_ID} if the intent is to lock the read to the admin/management tenant). If the endpoint truly needs cross-tenant global totals, make that explicit in code/comments and use a dedicated admin-only aggregation view/table or require and document ADMIN privileges — still prefer explicit WHERE tenant_id predicates or an explicit admin-safe aggregation to avoid accidental cross-tenant leaks.
- **server/routes.ts:1414** — Multiple raw SELECTs (count(*) queries and persona listing) against tenant-scoped tables (conversations, messages, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions, etc.) are executed without any tenant_id WHERE clause. This exposes cross-tenant aggregates/data to the SQL layer without an application-level tenant constraint in the statement.
  - _Fix:_ Restrict each count/lookup to a tenant by adding explicit tenant_id predicates (use drizzle eq(table.tenantId, tenantId) or include AND tenant_id = ${ADMIN_TENANT_ID} when querying admin-owned tenant rows). Alternatively, if global aggregates are required for platform admins, implement explicit admin-only aggregation queries or a safe materialized/global view and document that behaviour — do not rely on omission of tenant filters. Ensure all SQL statements touching tenant-scoped tables include an explicit tenant constraint or use a vetted admin aggregation path.
- **server/routes.ts:4532** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The code inserts (project_id, note, author) without passing tenant_id, relying on an earlier lookup for project_id but not persisting tenant context on the row.
  - _Fix:_ Include tenant_id explicitly in the INSERT and bind it from the authenticated tenant (or storage layer). For example: INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projId}, ${tenantId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, use a storage API that requires tenantId and enforces the tenant column server-side. Also keep the ownership check (or re-validate project belongs to tenant) as defense-in-depth.
- **server/routes/admin-zombie-detector.ts:38** — Query reads from agent_trace_spans (CTE `used`) with no tenant_id constraint. This SELECT (and subsequent LEFT JOIN) can return cross-tenant trace data because it filters only on started_at and tool_name, not tenant_id.
  - _Fix:_ Constrain the query to the caller's tenant by adding a tenant filter to the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND ...). Pass tenantId into the transaction and use the parameterized template (sql`... ${tenantId} ...`) rather than string interpolation so Drizzle binds it safely. If this endpoint is truly intended to aggregate across tenants, make that explicit and document/authorize it; otherwise restrict to the admin caller's tenantId.
- **server/routes/admin-zombie-detector.ts:54** — Query builds the `used` CTE from conversations (SELECT persona_id, MAX(updated_at) ... FROM conversations) with no tenant_id constraint. This will aggregate conversation usage across all tenants rather than limiting to the caller's tenant.
  - _Fix:_ Add a tenant filter to the CTE (e.g. WHERE tenant_id = ${tenantId} AND updated_at > ...) and ensure tenantId is supplied to the transaction and passed as a parameter to the SQL template. If cross-tenant aggregation is intended, require an explicit, well-documented admin action and ensure only properly authorized platform-operators can invoke it.
- **server/routes/conversations.ts:113** — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint. The statement `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates a conversation row without ensuring the row belongs to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. `WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or call a storage-layer helper that enforces tenant ownership). Always include AND tenant_id = <caller tenant> on writes to tenant-scoped tables.
- **server/routes/conversations.ts:114** — SELECT from tenant-scoped table 'project_conversations' lacks tenant_id constraint. The statement `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` does not restrict results to the caller's tenant.
  - _Fix:_ Constrain the SELECT by tenant, e.g. `... WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}`. All reads from tenant-scoped tables must include tenant_id checks to enforce isolation.
- **server/routes/conversations.ts:117** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id. The statement `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` writes a row without recording the tenant or validating ownership in the row itself.
  - _Fix:_ Include the tenant_id column in the INSERT values (e.g. `INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})`) or use an API that ensures the inserted row is bound to the caller's tenant. Every INSERT into tenant-scoped tables must explicitly set tenant_id.
- **server/routes/conversations.ts:119** — UPDATE on tenant-scoped table 'projects' lacks tenant_id constraint. The statement `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` updates a project row without scoping to the caller's tenant (even though project existence was previously checked).
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Never rely solely on prior existence checks — enforce tenant_id in the UPDATE/DELETE WHERE clause to maintain isolation.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} is executed without a tenant_id predicate — a tenant can flip is_read on another tenant's message if they guess an id.
  - _Fix:_ Include the tenant predicate in the UPDATE to enforce tenant isolation, e.g. await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`);. Prefer parameterized queries that always include tenant_id for mutating tenant-scoped rows.
- **server/sculptor.ts:106** — UPDATE on tenant-scoped table `sculptor_sessions` lacks tenant constraint (UPDATE ... WHERE id = ${session.id} without AND tenant_id = ...). This allows a caller that can influence `session.id` to modify rows in other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the row belongs to the caller's tenant before updating. Apply same pattern everywhere you update sculptor_sessions.
- **server/sculptor.ts:112** — UPDATE on tenant-scoped table `sculptor_sessions` sets conversation_id but omits tenant scoping (WHERE id = ${session.id} with no tenant_id check).
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}), or select-and-verify ownership first. Never perform cross-tenant updates by id alone.
- **server/sculptor.ts:168** — UPDATE on tenant-scoped table `sculptor_sessions` in monitorSession() uses WHERE id = ${sessionId} without AND tenant_id = ${tenantId}. monitorSession accepts tenantId, but the UPDATE does not constrain by it, enabling cross-tenant modification if sessionId is abused.
  - _Fix:_ Add tenant guard to the UPDATE: WHERE id = ${sessionId} AND tenant_id = ${tenantId}. Prefer verifying the session belongs to the tenant earlier in the function and fail if it does not.
- **server/sculptor.ts:359** — UPDATE on tenant-scoped table `sculptor_sessions` (setting review_result) uses WHERE id = ${sessionId} with no tenant_id constraint, allowing accidental cross-tenant writes.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or fetch+verify the session row's tenant_id before writing the review_result.
- **server/second-opinion.ts:416** — SETTLE UPDATE on tenant-scoped table `agent_cost_ledger` updates by id only (WHERE id = ${reservationId}) with no tenant_id constraint. Updating ledger rows by id without tenant scoping can allow modifying another tenant's reservation/settlement row.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or look up the reservation row and verify its tenant_id before performing the UPDATE. Prefer defensive checks so a guessed id cannot affect other tenants.
- **server/second-opinion.ts:438** — RELEASE UPDATE on tenant-scoped table `agent_cost_ledger` sets cost_usd/operation WHERE id = ${reservationId} but omits tenant_id, enabling cross-tenant mutation of ledger rows by id.
  - _Fix:_ Only update reservation rows after confirming they belong to the calling tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or perform a select-to-verify then update. Do not rely on id-only updates for tenant-scoped ledger writes.
- **server/seed.ts:2239** — SELECT (and subsequent INSERT loop) uses conversations rows without constraining by tenant_id (SELECT DISTINCT c.id, c.project_id FROM conversations c ... NOT EXISTS (SELECT 1 FROM project_conversations pc ...)). The code reads conversations across all tenants and then inserts project_conversations records based on those results, potentially operating on other tenants' data.
  - _Fix:_ Add an explicit tenant constraint to the SELECT (e.g. WHERE c.tenant_id = ${tenantId} AND ...). Before inserting or modifying join/link rows, verify ownership: confirm both project and conversation belong to the same tenant (e.g. join projects and conversations on project_id and check p.tenant_id = c.tenant_id = ${tenantId}). Alternatively, include tenant_id on the project_conversations table and set it on INSERT. Never select or mutate tenant-scoped rows without verifying tenant ownership (use Drizzle .where(eq(table.tenantId, tenantId))).
- **server/seed.ts:3662** — UPDATE memory_entries replaces text across the table without filtering by tenant_id (memory_entries is tenant-scoped). This modifies data for all tenants.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or iterate per-tenant. Ensure the operation is intentionally scoped to the platform tenant if only system rows should be changed.
- **server/seed.ts:3688** — UPDATE conversations SET model = 'gpt-5' ... runs without any tenant_id constraint. conversations is tenant-scoped, so this will update conversations for every tenant.
  - _Fix:_ Scope the update by tenant (WHERE tenant_id = ${tenantId} AND model = 'gpt-4.1') or perform the migration per-tenant. If this must touch only platform-owned rows, explicitly constrain to that tenant id.
- **server/seed.ts:3731** — DELETE FROM file_storage ... USING file_storage b ... performs cross-tenant dedupe without constraining the operation to the caller's tenant. While the query compares a.tenant_id = b.tenant_id, it still runs across all tenants and deletes rows for every tenant.
  - _Fix:_ Perform the dedupe per-tenant (e.g. supply tenant_id and include WHERE a.tenant_id = ${tenantId}) or iterate tenants. Never execute cross-tenant deletions in a single unscoped statement.
- **server/skillify.ts:91** — SELECT from tenant-scoped 'messages' table without constraining by tenant_id (recentMsgs in distillIntent).
  - _Fix:_ Include an explicit tenant filter (e.g. AND tenant_id = ${tenantId} or .where(eq(messages.tenantId, tenantId))) or otherwise validate ownership of conversation/messages before reading to avoid cross-tenant disclosure.
- **server/skillify.ts:201** — SELECT from tenant-scoped 'messages' table without tenant_id constraint (analyzeConversation).
  - _Fix:_ Constrain the query to the caller's tenant (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or verify message rows belong to the validated conversation's tenant before returning them.
- **server/skillify.ts:221** — SELECT from 'personas' by id with no tenant constraint — possible cross-tenant persona leak or unauthorized access.
  - _Fix:_ Restrict persona lookup to the conversation's tenant (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or explicitly verify that the persona belongs to the same tenant before using its name.
- **server/skillify.ts:329** — SELECT recentMsgs from 'messages' in skillifyConversation without tenant_id filter (reads last messages for LLM prompt).
  - _Fix:_ Add tenant scoping to the messages SELECT (AND tenant_id = ${tenantId}) or otherwise assert/verify that the fetched conversation belongs to the caller's tenant before reading messages.
- **server/skills-marketplace.ts:209** — SELECT id FROM 'skills' WHERE name = ... has no tenant_id constraint — may check or leak another tenant's skill installation state.
  - _Fix:_ If skills are tenant-scoped, include tenant_id in the WHERE clause (e.g. WHERE name = ${name} AND tenant_id = ${tenantId}) or require an explicit tenant context for this operation. If 'skills' is intentionally global, document that clearly and ensure the behavior is intentional and audited.
- **server/skills-marketplace.ts:235** — SELECT * FROM 'skills' WHERE id = ${skillId} executed without a tenant_id constraint — may expose tenant-scoped skill data to callers lacking tenant context.
  - _Fix:_ Either accept/require a tenantId parameter and include WHERE tenant_id = ${tenantId}, or clearly document/implement that 'skills' is a global/shared table and guard access appropriately (admin-only, audited). Avoid unscoped reads on tenant-scoped tables.
- **server/stability-watchdog.ts:177** — SELECT aggregation from 'heartbeat_logs' (recentLogs) performed with no tenant_id constraint — results aggregate errors across all tenants and are used to disable tasks potentially across tenants.
  - _Fix:_ If the watchdog must act per-tenant, scope this query by tenant_id. If global admin analysis is intended, require an explicit ALL_TENANTS flag and audit/log the cross-tenant operation; otherwise, add WHERE tenant_id = ${tenantId} or run per-tenant loops so tenant-scoped deletions/updates are constrained.
- **server/storage.ts:593** — INSERT into tenant-scoped table `memory_entries` performed without validating that the payload includes an explicit, positive tenantId. The function createMemoryEntry accepts InsertMemoryEntry and directly inserts it, relying on callers to provide tenantId.
  - _Fix:_ Enforce fail-closed semantics at this callsite: validate that data.tenantId is a positive integer (throw if missing/invalid) before inserting. Alternatively require callers to pass validated tenant scope (use assertValidTenantId/requireTenantScope) and/or document that only admin contexts may call this path.
- **server/storage.ts:891** — UPDATE on tenant-scoped table `memory_entries` (updateMemoryEmbedding) uses only id in the WHERE clause (no tenant_id constraint). This can modify rows belonging to other tenants.
  - _Fix:_ Require and validate a tenantId parameter and add a tenant equality condition (e.g. .where(and(eq(memory_entries.id, id), eq(memory_entries.tenant_id, tenantId))). If this is an admin/global operation, require an explicit ALL_TENANTS sentinel and/or an admin-only guard.
- **server/storage.ts:914** — SELECT from tenant-scoped table `memory_entries` (getMemoriesWithoutEmbeddings) performed without any tenant constraint — returns rows across all tenants.
  - _Fix:_ Add tenant scoping: accept/require a tenantId (or ALL_TENANTS sentinel) and use requiredTenantScope/tenantScope to constrain the WHERE clause. If this is an offline global worker, require explicit admin scope and guard it accordingly.
- **server/storage.ts:919** — SELECT from tenant-scoped table `agent_knowledge` (getKnowledgeWithoutEmbeddings) executed without a tenant filter — exposes knowledge rows for all tenants.
  - _Fix:_ Require a tenantId parameter (or explicit ALL_TENANTS sentinel) and use requiredTenantScope(agentKnowledge.tenantId, tenantId) in the WHERE clause. For background jobs iterate per-tenant or require admin context explicitly.
- **server/storage.ts:929** — UPDATE on tenant-scoped table `memory_entries` (archiveExpiredMemories) runs without any tenant_id constraint and will modify rows across all tenants.
  - _Fix:_ Either run this per-tenant (pass tenantId and restrict the UPDATE) or require an explicit admin/ALL_TENANTS sentinel. At minimum, restrict the operation to authorized admin contexts and document the global effect.
- **server/storage.ts:945** — UPDATE on tenant-scoped table `memory_entries` (archiveStaleMemories) performed without tenant scoping — affects all tenants' rows.
  - _Fix:_ Scope the archival to a tenant by requiring tenantId or iterate tenants in a controlled admin job. Use requiredTenantScope/tenantScope to add eq(memory_entries.tenant_id, tenantId) to the WHERE clause unless the operation is intentionally global and admin-protected.
- **server/storage.ts:583** — SELECT from tenant-scoped table `memory_entries` (getMemoryEntry) may execute without a tenant filter when tenantId is undefined. Although there is a runtime _warnUnscoped, the query can still return rows belonging to other tenants.
  - _Fix:_ Make tenantId mandatory for this read (use requireTenantScope or assertValidTenantId) or explicitly require the caller to pass ALL_TENANTS sentinel for global/admin reads. Do not allow permissive fallback that returns tenant-scoped rows without an explicit tenant constraint.
- **server/storage.ts:619** — UPDATE (deleteMemoryEntry) on tenant-scoped table `memory_entries` can run without a tenant constraint when tenantId is omitted (the code warns but proceeds). This allows cross-tenant mutation.
  - _Fix:_ Require and validate tenantId and include it in the WHERE clause (eq(memory_entries.tenantId, tenantId)). If unscoped deletes are intended only for admin, require explicit admin sentinel/authorization before allowing an unscoped operation.
- **server/storage.ts:626** — UPDATE (touchMemoryEntries) on tenant-scoped table `memory_entries` may run without tenant scoping if tenantId is omitted. The function warns but still performs an unscoped update if tenantId is undefined.
  - _Fix:_ Require tenantId (fail-closed) or use requiredTenantScope/tenantScope and include eq(memory_entries.tenantId, tenantId) in the WHERE clause. If global updates are intended, gate them behind an explicit admin sentinel or separate admin-only API.
- **server/storage.ts:718** — UPDATE on tenant-scoped table `daily_notes` (upsertDailyNote -> update path) performs db.update(dailyNotes).where(eq(dailyNotes.id, existing.id)) with no tenant_id constraint. If existing was resolved without tenant scoping, this can update another tenant's note.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(daily_notes.id, existing.id), eq(daily_notes.tenantId, data.tenantId)))). Ensure existing was resolved under a validated tenant scope and prefer requireTenantScope in callers.
- **server/storage.ts:817** — INSERT into tenant-scoped table `agent_knowledge` (createKnowledge) is performed with db.insert(agentKnowledge).values(data) but the function does not validate that data.tenantId is present/valid. This allows unscoped writes if callers omit tenantId.
  - _Fix:_ Require and validate data.tenantId (positive integer) before inserting. Use assertValidTenantId/requireTenantScope or throw on missing tenantId, or else explicitly document and restrict this API to admin contexts only.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding performs an UPDATE on felix_proposals without constraining by tenant_id (WHERE id = ${proposalId} AND args_embedding IS NULL). This can modify rows across tenants if ids collide or are guessed.
  - _Fix:_ Require and pass tenantId into stampArgsEmbedding and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use a Drizzle .where(eq(felix_proposals.tenantId, tenantId))). Validate tenant context before mutating tenant-scoped rows.
- **server/tool-learning.ts:129** — createCustomTool checks for existing tools with db.select().from(customTools).where(eq(customTools.name, parsed.name)) but does NOT filter by tenant_id. This allows cross-tenant name collisions or denial-of-service by causing false positives/negatives across tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) so uniqueness checks are performed within the caller's tenant only.
- **server/tool-learning.ts:365** — executeCustomTool updates usageCount with db.update(customTools).set(...).where(eq(customTools.id, tool.id)) without scoping by tenant_id. This mutates a tenant-scoped table row without verifying tenant ownership.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or otherwise verify the tool's tenant before updating usage counters to prevent cross-tenant mutation.

### MEDIUM (36)

- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting without tenant_id constraint (sweeps pending insights across all tenants).
  - _Fix:_ If the sweep is intended to operate per-tenant, iterate tenants and run tenant-scoped queries, or accept an explicit tenantId parameter and add WHERE tenant_id = ${tenantId}. Avoid unconstrained reads across tenant-scoped tables without explicit privileged context and auditing.
- **server/agentic-engines.ts:247** — UPDATE ai_insights in autoApplyEligibleInsights can run with no tenant filter when tenantId is undefined (the SQL builds tenantScope empty), causing multi-tenant writes.
  - _Fix:_ Require a tenantId for this operation or, when operating as a cross-tenant sweep, iterate tenants and perform tenant-scoped updates per tenant. Never perform broad updates on tenant-scoped tables without explicit privileged context and extra safeguards.
- **server/auto-asset-capture.ts:207** — SELECT from messages by conversation_id without an explicit tenant_id WHERE clause. Even though conversation rows were enumerated earlier, the messages query itself lacks tenant scoping and reads a tenant-scoped table without an explicit tenant_id condition.
  - _Fix:_ Add tenant scoping to the messages query, e.g. SELECT content FROM messages WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id} AND role = 'assistant' ORDER BY created_at ASC, or fetch messages only after asserting/deriving tenant ownership inside a tenant-scoped query to ensure no cross-tenant reads occur.
- **server/agentic/task-forces.ts:44** — String-interpolated SQL fragment used for persona_ids array: the code constructs an int[] literal string and injects it into the SQL via sql`${literal}::int[]`. This is effectively embedding a request-derived SQL fragment rather than using parameterized binding.
  - _Fix:_ Avoid injecting SQL text built from input. Use parameterized array binding or Drizzle-supported array handling instead. For example, pass personaIds as a bound parameter (e.g. use Postgres array binding or INSERT ... VALUES (${p.tenantId}, ${p.name}, ${p.mission}, ${p.personaIds ?? null}, ...) with appropriate driver support), or build the array using parameterized placeholders (sql`ARRAY[${p.personaIds.map(id => sql`${id}`)}]::int[]`) after validating each id. This removes any risk of SQL injection and keeps the query fully parameterized.
- **server/channel-routing.ts:16** — Schema created for channel_routes contains no tenant_id column and enforces UNIQUE(channel), making routing global across all tenants. For a multi-tenant system this is likely incorrect and prevents per-tenant isolation at the SQL level.
  - _Fix:_ If channel routes should be per-tenant, update the schema to include tenant_id NOT NULL and change UNIQUE(channel) to UNIQUE(tenant_id, channel). Add an index on tenant_id and ensure every INSERT/UPDATE/DELETE/SELECT against this table includes tenant_id in the WHERE or inserted values.
- **server/chat-daily-log.ts:12** — updateDailyLog calls storage.upsertDailyNote with a tenantId parameter that is optional in updateDailyLog's signature. If callers omit tenantId this may cause an upsert without a tenant context, potentially writing a note without tenant ownership set.
  - _Fix:_ Require tenantId (make it non-optional) at the updateDailyLog API level and ensure storage.upsertDailyNote always receives a validated tenantId. In storage.upsertDailyNote ensure the underlying INSERT/UPDATE includes tenant_id explicitly and that the caller is authorized for that tenant.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks performed without an explicit tenant_id constraint. addDocument runs a preliminary collection existence check scoped to tenant_id, but the subsequent DELETE uses only doc_path and collection_id and omits tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the DELETE, e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. This makes the intent explicit and avoids races or accidental cross-tenant deletes if collection_id semantics change.
- **server/heartbeat-schedules.ts:26** — SELECT DISTINCT tenant_id FROM messages WHERE tenant_id IS NOT NULL AND tenant_id > 0 enumerates tenant IDs by scanning the messages table without tenant scoping. While this enumerates tenants intentionally, it is an un-scoped SELECT on a tenant-scoped table.
  - _Fix:_ If the heartbeat intentionally performs a cross-tenant sweep, ensure this function runs only with a properly-authorized system/admin context and document the behavior. If not intentional, restrict the query to the target tenant. Prefer using a dedicated tenants table or cached tenant metadata for enumeration instead of scanning messages.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW() scans research_schedules across all tenants without a tenant_id filter. This is a cross-tenant read of a tenant-scoped table.
  - _Fix:_ If the heartbeat is meant to claim and dispatch schedules across all tenants, make it explicit this is an admin cross-tenant job and ensure appropriate authorization/auditing. If not, scope the SELECT per-tenant (WHERE tenant_id = ${tenantId}) or use a dedicated admin-only mechanism that explicitly iterates tenants and documents the cross-tenant nature.
- **server/job-queue.ts:317** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} executes without any tenant constraint — a bare SELECT on a tenant-scoped table may be used by unprivileged callers to enumerate or probe jobs.
  - _Fix:_ Restrict this function to admin-only code paths, or require the caller to be an authenticated admin. If used by non-admins, require caller-supplied tenantId and include `AND tenant_id = ${tenantId}`. Add explicit authorization checks and logging for admin lookups.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a runtime-derived column name in an UPDATE statement. Although the column value is whitelisted earlier, using sql.raw() with any runtime-derived value introduces SQL injection and maintenance risks and can lead to accidental misuse that might bypass tenant checks or other safeguards.
  - _Fix:_ Avoid embedding runtime values as raw SQL. Replace the dynamic UPDATE with a small, explicit switch/if that issues one of four fully-parameterized UPDATE statements (one per allowed column) or use the query builder schema methods to reference columns safely. Ensure the tenant_id/persona_id/category WHERE clause remains present for each branch.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is performed without filtering by tenant_id — the query scans/returns rows across all tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or make the cross-tenant nature explicit and restrict to an authorized admin path. If the intent is to run per-tenant, iterate tenants and include an explicit tenant predicate to enforce isolation.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` is executed with no tenant_id predicate, returning rows across tenants.
  - _Fix:_ Either include WHERE tenant_id = ${tenantId} when called in a tenant context, or run the sweep per-tenant with an explicit tenant filter. If cross-tenant admin access is required, gate the function behind an admin-only boundary and document auditing/consent.
- **server/lib/system-probes.ts:163** — DB count query on messagesTable (tenant-scoped) runs without any tenant_id filter, aggregating across all tenants.
  - _Fix:_ If the probe must be tenant-scoped, add WHERE tenant_id = ${tenantId} (and pass tenantId into this probe). If this is intended to be an admin/global metric, make that explicit and ensure only authorized admin code can call it; avoid silent cross-tenant reads from tenant-scoped tables.
- **server/lib/skill-optimizer-promotion.ts:687** — SELECT from `schema.skills` fetches a skill row by id without constraining by tenant_id. If `skills` is tenant-scoped, this allows reading a skill row from another tenant.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND eq(schema.skills.tenantId, candidate.tenantId) or AND eq(schema.skills.tenantId, ADMIN_TENANT_ID) as appropriate). If the skills catalog is intended to be platform-global, make that contract explicit and assert the tenant context (e.g. require ADMIN_TENANT_ID) before selecting.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table `agent_jobs` without constraining by tenant_id (no WHERE tenant_id = ...) — returns cross-tenant rows.
  - _Fix:_ Either restrict this admin query to the admin tenant explicitly (e.g. add AND j.tenant_id = ${ADMIN_TENANT_ID}) or, if the intent is to inspect only the caller's tenant, resolve the caller tenant and add WHERE j.tenant_id = ${tenantId}. Ensure all reads against tenant-scoped tables include an explicit tenant_id predicate.
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` executed with no tenant_id predicate — cross-tenant read.
  - _Fix:_ Either scope this query to the caller's tenant (WHERE tenant_id = <caller tenant>) or restrict access and clearly document justification. If the intent is operator/global aggregation, ensure the endpoint is locked to platform/operator admins and audit that this global read is acceptable.
- **server/routes/metrics.ts:79** — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) executed with no tenant_id predicate — cross-tenant read.
  - _Fix:_ Add a tenant_id WHERE clause to restrict results to the requester's tenant, or otherwise require and enforce platform/operator-only access and logging if global aggregation is intended.
- **server/routes/metrics.ts:95** — SELECT aggregating tokens FROM `agent_cost_ledger` executed without a tenant_id predicate — cross-tenant read.
  - _Fix:_ Limit aggregation to the caller's tenant via WHERE tenant_id = <caller tenant>, or ensure the endpoint is strictly operator-only and that global reads are an accepted and documented exception.
- **server/routes/metrics.ts:113** — SELECT from `repair_incidents` (tenant-scoped) executed with no tenant_id predicate — cross-tenant read.
  - _Fix:_ Constrain the query by tenant_id (WHERE tenant_id = <caller tenant>) or gate the endpoint to platform/operator admins and justify the cross-tenant aggregation in policy and logs.
- **server/routes/projects.ts:596** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP executed without a tenant_id WHERE clause. This updates a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so the write is scoped to the caller's tenant. Do not rely solely on prior existence checks for isolation.
- **server/routes/projects.ts:664** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP executed without a tenant_id WHERE clause. This modifies a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Add an explicit tenant filter to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure the mutation is limited to the caller's tenant.
- **server/routes/oauth-subscriptions.ts:325** — SELECT DISTINCT tenant_id FROM oauth_subscriptions executed with no tenant constraint — this reads tenant-scoped rows across all tenants.
  - _Fix:_ If this is an intentional platform-wide cron/heal operation, ensure it's only callable by a strongly authenticated platform admin/cron principal and document why a cross-tenant read is required. Otherwise, restrict the query to a tenant (WHERE tenant_id = <caller tenant>) or move this logic to a safe, audited admin-only path.
- **server/seed.ts:41** — SELECT executed against tenant-scoped table research_sessions with no tenant_id filter (SELECT COUNT(*) FROM research_sessions). This reads cross-tenant data.
  - _Fix:_ Restrict the query to the caller/target tenant. e.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId} or use Drizzle: db.select().from(researchSessions).where(eq(researchSessions.tenantId, tenantId)). If an aggregate across all tenants is required, perform it only from an explicitly privileged/admin context and make that explicit and safe in code.
- **server/seed.ts:3948** — UPDATE agent_knowledge SET content = ... WHERE id = ${rows[0].id} updates a row in a tenant-scoped table (agent_knowledge has tenant_id) but the UPDATE lacks an explicit tenant_id constraint. This relies on a prior SELECT that included tenant_id; missing tenant constraint on UPDATE is a TOCTOU / ownership risk.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or otherwise verify ownership atomically so the write is always constrained to the intended tenant.
- **server/self-improvement.ts:261** — Raw sql-tagged query (db.execute(sql`...`)) used to lookup a persona by config.personaId and tenantId. This uses request-derived values inside a string-interpolated SQL template.
  - _Fix:_ Avoid string-interpolated/raw SQL for request-derived values. Use the Drizzle query builder (db.select().from(personas).where(and(eq(personas.id, config.personaId), exists(agent_desks with tenant filter)))) or at minimum ensure the parameters are always passed as bound parameters and ownership is validated. Prefer explicit tenant checks (as is present) but migrate to query-builder APIs to reduce injection/isolation risks.
- **server/self-improvement.ts:406** — Raw sql-tagged UPDATE of `personas` uses interpolated persona.id and tenant id inside a SQL template (db.execute(sql`UPDATE personas ... WHERE p.id = ${persona.id} AND EXISTS (... ad.tenant_id = ${config.tenantId}) ...`)). Although an EXISTS-based tenant check is present, this is string-interpolated SQL with request-derived values.
  - _Fix:_ Use Drizzle's update builder (db.update(personas).set({ soul: newSoul }).where(and(eq(personas.id, persona.id), eq(personas.tenantId, config.tenantId)))) or perform an ownership check via a prior parameterized select and then call a parameterized update. Avoid ad-hoc string-interpolated SQL with request-derived data to reduce injection/isolation risk.
- **server/self-reflection.ts:355** — SELECT from tenant-scoped table `memory_entries` is performed via a string-interpolated SQL template that includes request-derived values (lesson.toolName / lesson.parameter concatenated into LIKE patterns). Using sql`...${"%" + lesson.toolName + "%"}` risks query-shape issues and exposes LIKE-pattern composition of untrusted input.
  - _Fix:_ Use the query builder's where/like helpers or pass the LIKE pattern as a bound parameter without manual concatenation. Sanitize or validate lesson.toolName and lesson.parameter before use. Prefer db.select().from(memory_entries).where(and(eq(memory_entries.tenantId, tenantId), eq(memory_entries.source, 'self_reflection'), eq(memory_entries.category,'actionable_lesson'), sql`fact LIKE ${likePattern}`)). If possible, avoid LIKE on untrusted input or escape metacharacters.
- **server/self-reflection.ts:392** — SELECT from tenant-scoped `memory_entries` in recallLessons uses a string-interpolated SQL template with request-derived toolName inserted into LIKE (${"%" + toolName + "%"}). This interpolates caller data into raw SQL templates.
  - _Fix:_ Pass the pattern as a bound parameter via the query builder and escape user input for LIKE, or use Drizzle's .where/like helpers. Validate & sanitize toolName before use. Prefer db.select().from(...).where(and(eq(...tenant_id...), memory_entries.fact.like(likePattern))). Avoid ad-hoc sql`...${"%"+toolName+"%"}` patterns.
- **server/sentiment-detector.ts:167** — INSERT into tenant-scoped table `sentiment_events` includes conversation_id from the caller but does not verify that the conversation_id belongs to the provided tenantId. Writing a row referencing a foreign resource without validating ownership can allow cross-tenant linking or confusion.
  - _Fix:_ Before inserting, verify the conversation belongs to the tenant (e.g. SELECT id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} LIMIT 1) or resolve/obtain conversation id from a trusted internal lookup. Alternatively, include a DB-level constraint check (tenant_id in the insert) and/or perform the insert within an authorized code path that has already validated ownership.
- **server/skill-evolution.ts:46** — Raw SQL INSERT/ON CONFLICT is used via db.execute(sql`...`) with request-derived values (tenantId, toolName, durationMs, failureReason). While tenant_id is supplied, using raw string-interpolated SQL increases risk of accidental missing predicates or injection patterns.
  - _Fix:_ Prefer Drizzle's query builder APIs (db.insert(tool_performance).values(...).onConflictDoUpdate(...)) which safely parameterize values. If raw SQL must be used, ensure all inputs are validated/sanitized and passed as bound parameters to avoid injection and accidental loss of tenant predicates.
- **server/skill-evolution.ts:104** — Raw SQL SELECT in getUnderperformingTools uses a string-interpolated sql template containing tenantId and numeric thresholds. This executes programmatic filtering via raw SQL rather than the query builder with bound parameters.
  - _Fix:_ Use the Drizzle query builder (db.select().from(tool_performance).where(and(eq(tool_performance.tenantId, tenantId), gte(...))) ) to ensure values are bound and reduce risk of accidental injection or missed tenant constraints. Validate numeric inputs before interpolation.
- **server/skill-evolution.ts:120** — Raw SQL SELECT in getExistingOptimizations uses db.execute(sql`SELECT * FROM tool_optimizations WHERE tenant_id = ${tenantId} AND tool_name = ${toolName} ...`). The query is constructed via string-interpolated sql template with request-derived toolName.
  - _Fix:_ Switch to Drizzle's query builder (db.select().from(tool_optimizations).where(and(eq(tool_optimizations.tenantId, tenantId), eq(tool_optimizations.toolName, toolName))). This ensures parameters are bound and reduces the use of raw/interpolated SQL for request-derived values.
- **server/storage.ts:1057** — UPDATE on tenant-scoped table `heartbeat_tasks` (updateHeartbeatTask) allows an unscoped update when tenantId is omitted (tenantId is optional). The code conditionally adds a tenant-scoping subselect only when tenantId !== undefined.
  - _Fix:_ Make tenantId required for tenant-scoped mutations or use requireTenantScope to force callers to explicitly pass ALL_TENANTS for platform-wide updates. Avoid optional tenant parameters that permit unscoped mutations.
- **server/storage.ts:1062** — DELETE on tenant-scoped table `heartbeat_tasks` (deleteHeartbeatTask) can execute without tenant scoping when tenantId is omitted; the tenant filter is only added conditionally.
  - _Fix:_ Require and validate tenantId (or the explicit ALL_TENANTS sentinel) for delete operations on tenant-scoped tables. If admin/global deletes are needed, require explicit admin authorization and an explicit sentinel.
- **server/tool-learning.ts:155** — After insertion the code selects the tool row with db.select().from(customTools).where(eq(customTools.id, inserted.id)) but does not include tenant_id in the WHERE. This is a tenant-scoped table read without tenant constraint.
  - _Fix:_ Constrain the lookup to the tenant (e.g. add eq(customTools.tenantId, tenantId) to the WHERE) or fetch the row using the tenant-aware transaction context to ensure reads are scoped to the caller's tenant.
