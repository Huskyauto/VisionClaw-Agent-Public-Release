# Tenant-Isolation Audit Report

_Generated 2026-07-08T22:30:50.491Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **517/517** file(s) audited · chunks 50/50 OK (0 failed)
- Findings: **276** (CRITICAL 59 · HIGH 173 · MEDIUM 43 · LOW 1)

### CRITICAL (59)

- **server/agentic-engines.ts:242** — Mass UPDATE of tenant-scoped `ai_insights` (UPDATE ai_insights SET ... WHERE status = 'new' AND category IN (...) RETURNING ...) has no tenant_id constraint. This will auto-apply insights across all tenants.
  - _Fix:_ If this operation is intended per-tenant, add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND ...). If it intentionally runs globally as an admin operation, make that explicit and restrict/authorize the call; otherwise require an explicit tenantId and include it in the WHERE clause.
- **server/auto-transcript.ts:165** — UPDATE on conversations (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL) does not include a tenant_id constraint. This can modify a conversation row belonging to another tenant if ids are forged/poisoned.
  - _Fix:_ Never update tenant-scoped tables without an explicit tenant filter. Add AND tenant_id = ${c.tenant_id} (or AND tenant_id = ${tenantId} if available) to the WHERE clause to ensure the update only affects rows owned by the acting tenant. Alternatively, perform the update via a safe join that enforces tenant ownership before the write.
- **server/business-tools.ts:84** — INSERT into invoice_items omits tenant_id. invoice_items is a tenant-scoped table but the INSERT does not set tenant_id, violating the invariant that every INSERT into tenant-scoped tables must include tenant_id.
  - _Fix:_ Include the tenant id on invoice_items inserts (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)), or derive/validate the tenant from the parent invoice before inserting. Ensure the tenant_id column is NOT NULL and always provided from the caller's authenticated tenant context.
- **server/chat-engine.ts:2095** — processMessage calls storage.getConversationUnscoped(conversationId) which performs an unscoped read of the conversations row (tenant-scoped table) using a caller-supplied conversationId before the code verifies tenant ownership. This is an IDOR/tenant-isolation gap: the conversation row is fetched without a WHERE tenant_id = <caller tenant>, so a malicious caller could look up arbitrary conversation records.
  - _Fix:_ Avoid any unscoped reads of tenant-scoped tables with request-derived IDs. Replace storage.getConversationUnscoped(...) with a tenant-scoped fetch (e.g. getConversation(conversationId, tenantId) or a query that includes WHERE tenant_id = ${tenantId}), or require the caller's tenant context first and then perform the DB read using that tenant id. If storage.getConversationUnscoped must remain, change its implementation to require/accept a tenantId and enforce WHERE tenant_id = <tenantId>, and ensure ownership is verified before any sensitive data is used.
- **server/chat-engine.ts:4586** — storage.createMemoryEntry() is called without passing tenantId. This performs an INSERT into a (likely) tenant-scoped memory table but does not set tenant_id explicitly (the code only supplies fact, category, source, status, personaId). Per the project invariant, tenant_id must be provided on every INSERT (columns are NOT NULL with no default).
  - _Fix:_ Pass the caller's tenantId explicitly into createMemoryEntry (e.g. include tenantId: tenantIdForScope or tenantId). Additionally, validate that the tenant context is available before attempting the insert and fail-safe if tenantId is missing.
- **server/compaction.ts:400** — INSERT into `compaction_archives` does not set tenant_id. Tenant-scoped tables must receive tenant_id on INSERT (columns are NOT NULL/no default).
  - _Fix:_ Ensure the archive insert includes the owning tenant_id. Either (a) accept a validated tenantId parameter to archiveMessages and INSERT tenant_id = ${tenantId}, or (b) look up the conversation → tenant mapping (SELECT tenant_id FROM conversations WHERE id = ${convId}) and use that tenant_id in the INSERT. Never write tenant-scoped rows without an explicit, validated tenant_id.
- **server/data-protection.ts:125** — permanentlyPurgeSoftDeleted performs DELETEs (DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}) without constraining by tenant_id. These DELETEs can affect rows across tenants if conversation ids are not reliably namespaced.
  - _Fix:_ Include tenant_id predicates on all DELETEs (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) and likewise for compaction_archives and conversations. Use the conv.tenant_id from the earlier query to scope each delete, or perform the deletes inside a transaction where you SET the tenant context and enforce it in SQL.
- **server/delivery-pipeline.ts:683** — SELECT from delivery_logs by stripePaymentId is executed without constraining tenant_id. This allows cross-tenant lookup/enumeration of deliveries via a payment id.
  - _Fix:_ Include tenant scoping on this lookup (e.g. add AND tenant_id = <caller tenant>) or require/accept tenantId from the caller and use it in the WHERE. Alternatively, restrict this endpoint to a trusted internal path and document that explicitly. Prefer adding tenantId parameter and using .where(and(eq(deliveryLogs.stripePaymentId, stripeId), eq(deliveryLogs.tenantId, tenantId))).
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment SELECTs delivery_logs by stripePaymentId with no tenant_id filter, exposing another tenant's delivery metadata if the payment id is provided.
  - _Fix:_ Require tenant context for this lookup or add a tenantId parameter and include eq(deliveryLogs.tenantId, tenantId) in the WHERE. If global lookups are intentional, restrict access to a trusted admin-only path and document/guard it accordingly.
- **server/embeddings.ts:684** — storeEmbeddingVec runs an UPDATE on the tenant-scoped tables memory_entries/agent_knowledge using only WHERE id = ${id} (sql.raw interpolation of table name), with no tenant_id constraint. This allows updating another tenant's embedding record by id.
  - _Fix:_ Require and pass a tenantId parameter into storeEmbeddingVec and include tenant filtering in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Validate the caller owns the row before writing. Avoid relying on id-only updates for tenant-scoped tables.
- **server/felix-brain.ts:19** — Boot-time rehydration queries agent_runs with no tenant filter and then writes per-conversation TaskState into a global in-memory map keyed only by conversationId. This can mix state across tenants (and leak cross-tenant data/state).
  - _Fix:_ Either restrict the SELECT to a specific tenant (add WHERE tenant_id = ${tenantId} or accept a tenantId parameter) or namespace the in-memory taskStateStore by tenant (e.g. map keyed by `${tenantId}:${conversationId}`) and validate ownership before populating. Ensure any read from tenant-scoped tables includes an explicit tenant_id filter.
- **server/firecrawl.ts:184** — UPDATE against tenant-scoped table 'scraped_pages' uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) and does not include the caller's tenant constraint.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so writes are always scoped to the calling tenant.
- **server/firecrawl.ts:316** — UPDATE against tenant-scoped table 'scraped_pages' (in crawl flow) uses only the row id in WHERE and omits the tenant filter (eq(scrapedPages.id, existing[0].id)).
  - _Fix:_ Add the tenant constraint to the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to ensure the update cannot affect another tenant's row.
- **server/google-drive.ts:946** — INSERT INTO project_files is executed via raw SQL without setting a tenant_id column (or otherwise constraining by tenant). If project_files is tenant-scoped, this can create cross-tenant writes.
  - _Fix:_ Either include an explicit tenant_id value in the INSERT (and ensure it is derived from a validated/owned source) or ensure the INSERT is performed via a parameterized statement that associates the row with the validated tenant. If project_files is multi-tenant, fail the insert if tenant ownership was not validated.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' ... WHERE id = ANY(${ids}::int[]) modifies rows without constraining tenant_id. This can update proposals across tenants.
  - _Fix:_ Constrain the UPDATE to the tenant (for example: UPDATE code_proposals SET status='archived_stale' WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or join/filter against a subquery that ensures ids belong to the intended tenant. Alternatively, require this action to run only in an explicit admin context and document that behaviour.
- **server/heartbeat.ts:2852** — processDelegations() calls allPersonas = await storage.getPersonas() and then matches a delegation target by name across allPersonas without verifying the matched persona belongs to the task's tenant. That allows delegating/assigning personas from other tenants into this tenant's tasks (cross-tenant assignment).
  - _Fix:_ When resolving a persona by name for a tenant-scoped operation, filter personas by tenant (e.g. storage.getPersonas(tenantId) or filter allPersonas by persona.tenantId === tenantId). Additionally, assert tenant ownership before using persona.id in any create/update (and/or use an assertTenantContext helper).
- **server/heartbeat.ts:3044** — delegateTaskFromChat() calls allPersonas = await storage.getPersonas() and then finds the target persona by name without confirming that the target persona belongs to the provided tenantId. This can let a user/agent delegate to a persona from another tenant.
  - _Fix:_ Resolve personas scoped to the tenantId parameter (e.g. storage.getPersonas(tenantId) or filter by persona.tenantId). Always verify ownership of referenced resources (personas, projects, conversations, etc.) before using their IDs in tenant-scoped inserts/updates.
- **server/memory-graph.ts:194** — INSERT INTO memory_links does not set tenant_id (and thus writes to a tenant-scoped table without specifying tenant ownership). Per schema tenant_id is required; omitting it can create cross-tenant or orphaned rows and violates tenant isolation.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO memory_links (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Also validate that both source and target memory rows belong to the same tenant before inserting.
- **server/minerva-planner.ts:355** — UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval' has no tenant_id constraint — allows changing plan rows across tenants.
  - _Fix:_ Constrain the CAS update by tenant. Either accept/derive the caller's tenantId and add AND tenant_id = ${tenantId} to the WHERE, or fetch+validate the plan's tenant_id before performing the update. Ensure the CAS uses both id AND tenant_id so an attacker cannot act on another tenant's plan.
- **server/minerva-planner.ts:459** — UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising' has no tenant_id filter — rollback can affect plans in other tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or otherwise validate caller ownership) when rolling back decision metadata. Prefer passing an explicit tenantId into decide/revise flows and use it in all UPDATE/SELECT statements against tenant-scoped tables.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(lockKey) inside a SQL template for pg_advisory_xact_lock. sql.raw() inserts raw SQL text and here the value is derived (via hashing) from a caller-supplied ledgerKey → risk of SQL injection / unsafe interpolation and breaks the rule of parameterized bindings.
  - _Fix:_ Avoid sql.raw with any request-derived input. Pass the advisory-lock key as a bound parameter instead. For example compute a numeric BigInt value and use a parameterized template: await tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`); or better, compute the numeric value server-side and pass it as a parameter so Drizzle/pg can bind it safely. If you must transform the ledgerKey, ensure it is deterministically converted to a safe numeric value prior to binding and never injected via sql.raw.
- **server/plan-executor.ts:116** — appendExecutionLog performs an UPDATE on plans: execution_log = COALESCE(execution_log, '[]'::jsonb) || ${JSON.stringify([entry])}::jsonb WHERE id = ${planId}. There is no tenant_id check in the WHERE clause (cross-tenant write risk). Additionally the SQL embeds JSON via string interpolation (${JSON.stringify([entry])}::jsonb) using potentially request-derived content (injection/isolation risk).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${expectedTenantId}). Avoid string-interpolating JSON into SQL; bind the JSON value as a parameter (or use the ORM's insert/update helpers) and cast to jsonb in a parameterized way. For example: db.update(plans).set({ execution_log: sql`COALESCE(execution_log, '[]'::jsonb) || ${jsonParam}::jsonb` }).where(and(eq(plans.id, planId), eq(plans.tenant_id, tenantId))). Ensure appendExecutionLog receives and enforces the plan's tenant id before updating.
- **server/plan-executor.ts:505** — CAS claim uses UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status = 'approved' RETURNING ... with no tenant_id constraint. Claiming a plan by id without tenant scoping can allow cross-tenant state transitions.
  - _Fix:_ Restrict the CAS to the plan's tenant (e.g. add AND tenant_id = ${expectedTenantId}) or perform a prior read to confirm the plan belongs to the expected tenant and include tenant_id in the CAS WHERE clause. Always enforce tenant_id on UPDATE/RETURNING operations on tenant-scoped tables.
- **server/plan-executor.ts:657** — Final status UPDATE on plans uses WHERE id = ${planId} (and writes execution_log via stringified JSON) but does not include tenant_id in the WHERE clause. This is a cross-tenant write risk and also uses string-interpolated JSON (injection/isolation risk).
  - _Fix:_ Add tenant scoping to the WHERE (e.g. AND tenant_id = ${row.tenant_id}) and stop embedding JSON via string interpolation. Use parameterized JSON binding or the ORM update helpers to pass JSONB safely. Ensure all UPDATE/DELETE statements on plans include tenant_id.
- **server/process-governor.ts:595** — UPDATE on tenant-scoped `heartbeat_tasks` modifies rows using `WHERE id = ${t.id}` without enforcing tenant ownership (`tenant_id` not included)
  - _Fix:_ Constrain updates to the tenant (e.g. `WHERE id = ${t.id} AND tenant_id = ${tenantId}`) or verify the task belongs to the tenant before issuing the UPDATE to prevent cross-tenant mutation.
- **server/process-governor.ts:643** — UPDATE on tenant-scoped `agent_desks` uses `WHERE id = ${desk.id}` with no tenant filter
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the WHERE clause (or verify ownership prior to update) to avoid modifying another tenant's agent_desks rows.
- **server/process-governor.ts:977** — UPDATE on `governance_rules` uses `WHERE id = ${rule.id}` without tenant constraint (risk of modifying rules for other tenants)
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the UPDATE WHERE clause, or ensure `rule` was loaded/validated under the same tenant before mutating it.
- **server/project-brain.ts:332** — Backfill query scans `projects`, `conversations`, and `messages` with no tenant filtering: `SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0`
  - _Fix:_ Scope the backfill to a single tenant by adding `WHERE p.tenant_id = ${tenantId}` (and similar tenant constraints on joined tables) or redesign the backfill to iterate per-tenant to avoid mass cross-tenant data access.
- **server/proposal-verifier.ts:107** — UPDATE on `code_proposals` clears rebased_at using `WHERE id = ${proposalId}` with no tenant constraint
  - _Fix:_ Include tenant scoping in UPDATEs (e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`) or verify the proposal belongs to the tenant before updating to avoid mutating another tenant's proposals.
- **server/proposal-verifier.ts:246** — persist() updates `code_proposals` (verification_status, verification_details, verified_at) using `WHERE id = ${proposalId}` without tenant constraint
  - _Fix:_ Require tenant context and include `AND tenant_id = ${tenantId}` in the UPDATE, or first verify proposal ownership, to avoid changing proposals across tenants.
- **server/proposal-verifier.ts:408** — UPDATE persists rebased `code_diff` / validation_result with `WHERE id = ${proposalId}` and no tenant filter
  - _Fix:_ When writing back rebased diffs or validation fields, include tenant scoping (e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`) or validate the proposal belongs to the tenant first.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge SET ... WHERE id = ${r.id} is executed with no tenant_id constraint, allowing mutation of rows belonging to other tenants.
  - _Fix:_ Include the tenant guard in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or otherwise verify ownership before writing. Always constrain UPDATE/DELETE against tenant-scoped tables to the caller's tenant.
- **server/research-engine.ts:157** — UPDATE research_sessions ... WHERE status = 'running' AND started_at < ${staleThreshold} runs without any tenant_id restriction and will mark sessions across all tenants as completed.
  - _Fix:_ Perform cleanup per-tenant or add an explicit tenant filter (WHERE tenant_id = <tenant>) or otherwise limit this maintenance operation to a safe system tenant. Never run global UPDATE/DELETE on tenant-scoped tables without a deliberate, documented global policy and additional safeguards.
- **server/research-engine.ts:389** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() executes without tenant_id constraint, which will delete expired autoresearch entries for all tenants.
  - _Fix:_ Restrict deletion to a tenant (DELETE ... WHERE tenant_id = ${tenantId} AND ...) or iterate tenants explicitly. If a global purge is required, run it under an explicit, auditable operator-only path and ensure it cannot be invoked with an unscoped request context.
- **server/routes.ts:4490** — SELECT from tenant-scoped table 'conversations' without constraining by tenant_id (reads project_id for a conversation by id but no tenant check). This allows cross-tenant data exposure if conversationId is not proven to belong to the caller.
  - _Fix:_ Constrain the SELECT to the caller's tenant. For example: SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL (or use Drizzle .where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))). If conversationId is already trusted from a previously-loaded conv object, use that conv.tenantId in the query or avoid re-querying by reusing the vetted conv data.
- **server/routes.ts:4496** — INSERT into project_notes does not set tenant_id. project_notes appears to be tenant-scoped, so inserting without tenant_id can create rows that lack tenant ownership or be used to cross-tenant write data.
  - _Fix:_ Include tenant_id in the INSERT and ensure it is the caller's tenant. For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0, 5000)}, ${'system:context-guard'}) — or use your storage layer / Drizzle API that requires/validates tenantId. Also ensure the prior lookup of projId validated that the project belongs to the caller's tenant before inserting.
- **server/safety/transactional-snapshot.ts:407** — UPDATE action_snapshots uses only WHERE id = $1 without constraining tenant_id. action_snapshots is tenant-scoped (tenant_id NOT NULL); this UPDATE can modify/claim/rollback snapshots across tenants if an attacker supplies or guesses another row id.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. AND tenant_id = $2) and pass the caller's tenant id as a parameter. Prefer using the same parameterized db/sql path as other queries (db.execute(sql`...`)) rather than raw pool.query with a single id param so the tenant scope is enforced server-side and cannot be bypassed.
- **server/sculptor.ts:94** — UPDATE sculptor_sessions SET ... WHERE id = ${session.id} is executed without constraining tenant_id. This write can modify another tenant's session row if an attacker controls session.id or the id is leaked.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or tenant_id = ${session.tenantId}). Always include tenant_id in WHERE for writes to tenant-scoped tables, even when the id was previously looked up.
- **server/sculptor.ts:102** — UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id} lacks a tenant_id constraint. This can associate conversation_ids to sessions in other tenants or otherwise mutate foreign tenant rows.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Ensure all writes to sculptor_sessions include tenant_id in the WHERE clause.
- **server/sculptor.ts:156** — UPDATE sculptor_sessions ... WHERE id = ${sessionId} in monitorSession is not tenant-scoped. A caller could cause status/result changes on another tenant's session row by supplying or reusing an id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (use the tenantId variable in scope). Do not rely on prior in-process checks — enforce tenant_id at the DB write level for all tenant-scoped tables.
- **server/sculptor.ts:347** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} omits tenant_id in the WHERE clause. This allows cross-tenant modification of review_result for session ids belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Ensure every UPDATE/DELETE on sculptor_sessions includes tenant_id = <caller tenant>.
- **server/second-opinion.ts:407** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} (settleFusionReservation) updates a tenant-scoped ledger row using only id. Without tenant_id in the WHERE clause, this can settle or alter ledger rows for other tenants when a reservation id is reused or guessed.
  - _Fix:_ Require and pass the tenantId for settlement and include AND tenant_id = ${tenantId} in the WHERE clause (or validate the reservation row belongs to the expected tenant before updating). Avoid updating ledger rows by id alone for tenant-scoped tables.
- **server/second-opinion.ts:426** — UPDATE agent_cost_ledger SET cost_usd = '0', operation = 'second_opinion:released' WHERE id = ${reservationId} (releaseFusionReservation) lacks tenant scoping. This can release reservations for other tenants if an attacker supplies a reservation id from another tenant.
  - _Fix:_ Require tenant context and constrain the UPDATE with AND tenant_id = ${tenantId}, or lookup/verify the reservation row belongs to the caller's tenant before releasing. All writes to agent_cost_ledger must include tenant_id in the WHERE clause.
- **server/seed.ts:2859** — UPDATE to tenant-scoped table research_programs lacks a tenant constraint (UPDATE research_programs SET model = ... WHERE id = ${p.id}). Without tenant_id in the WHERE clause this may modify another tenant's row with the same id.
  - _Fix:_ Include tenant ownership in the WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or validate the program row belongs to the expected tenant before updating.
- **server/seed.ts:3136** — UPDATE to tenant-scoped table projects has no tenant constraint (UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ...). This can update projects belonging to other tenants.
  - _Fix:_ Scope the UPDATE by tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise verify ownership before applying the change.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) in WHERE — missing tenant_id constraint. This can modify another tenant's experiment row by id.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) so updates are constrained to the caller's tenant.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table `experiments` uses .where(eq(experiments.id, experiment.id)) with no tenant_id check. Updates in exception handling can thus affect other tenants' rows.
  - _Fix:_ Include tenant_id in the WHERE for this UPDATE as well (and handle missing tenantId defensively). Ensure both normal and error-path updates always require tenant ownership.
- **server/self-reflection.ts:366** — UPDATE memory_entries sets fact/last_accessed/access_count WHERE id = <found id> with no tenant_id constraint. This allows updating a memory row by id belonging to another tenant.
  - _Fix:_ Constrain the UPDATE by tenant_id as well (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Never perform cross-tenant updates using id alone.
- **server/stability-watchdog.ts:211** — DELETE FROM `heartbeat_tasks` WHERE enabled = false AND type = 'delegation' AND created_at < ... executes with NO tenant_id constraint — this will delete disabled delegation tasks across all tenants (global destructive DELETE without tenant scoping).
  - _Fix:_ Fail-closed: require an explicit tenant_id in the DELETE (e.g. add AND tenant_id = ${tenantId}) or change the logic to perform per-tenant deletes only after validating the tenant. Never run blanket DELETEs on tenant-scoped tables without an explicit tenant equality in SQL.
- **server/storage.ts:1247** — getAllDataForExport performs numerous SELECTs over tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, dailyNotes, heartbeatTasks, heartbeatLogs, etc.) with no tenant_id constraints — it exports platform-wide tenant data.
  - _Fix:_ Restrict this function to an authenticated and audited admin-only context. Require explicit admin authorization and/or scope by tenantId. If export of full multi-tenant data is intentionally supported, document and gate it behind robust admin checks and logging; otherwise, return per-tenant exports only.
- **server/tool-learning.ts:283** — UPDATE on tenant-scoped table `custom_tools` uses only the primary id in WHERE and lacks a tenant_id constraint. Mutating a tenant-scoped table without verifying tenant ownership can allow cross-tenant data corruption/escapes.
  - _Fix:_ Constrain updates to the caller's tenant explicitly, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Prefer doing any validation and the update in a single transaction/atomic statement to avoid TOCTOU races.
- **server/tool-learning.ts:300** — DELETE on tenant-scoped table `custom_tools` uses only the primary id in WHERE and lacks a tenant_id constraint. Deleting rows without tenant scoping risks removing another tenant's records.
  - _Fix:_ Require tenant scoping on DELETE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure delete paths always verify ownership before removing rows.
- **server/tools.ts:9153** — UPDATE against tenant-scoped table `proposed_skills` uses only WHERE id = <row.id> without constraining tenant_id — allows cross-tenant modification by id.
  - _Fix:_ Add a tenant constraint to the WHERE clause (use the tid variable present in scope). Example using Drizzle: await db.update(proposedSkills).set({...}).where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)); Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id equality to the caller's tenant.
- **server/tools.ts:9178** — UPDATE against tenant-scoped table `proposed_skills` (promote path) omits tenant_id in WHERE clause — row identified only by id can be modified across tenants.
  - _Fix:_ Constrain the UPDATE by tenant. Example: await db.update(proposedSkills).set({ ... }).where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)); Validate the tenant context (tid) before performing the update.
- **server/tools.ts:9195** — UPDATE against tenant-scoped table `proposed_skills` (reject path) omits tenant_id in WHERE clause — allows updating another tenant's proposal by id.
  - _Fix:_ Include tenant restriction in the WHERE clause. Example: await db.update(proposedSkills).set({ ... }).where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)); Always require tenant ownership when updating tenant-scoped rows.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files does not include tenant_id. The code inserts project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without explicitly setting tenant_id, relying only on project_id ownership checks. The invariant requires every INSERT into a tenant-scoped table to set tenant_id explicitly.
  - _Fix:_ Include the tenant id explicitly in the INSERT (and bind it from the trusted ctx.tenantId). E.g. add tenant_id to the column list and VALUES (tenantId). If project_files is tenant-scoped, ensure the handler passes ctx.tenantId into the INSERT and does not rely on implicit FK behavior.
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects SET updated_at ... WHERE id = ${projId} executes without constraining by tenant_id. Even though projId was fetched earlier with a tenant-checked query, the UPDATE uses only id in the WHERE clause and can modify a project row if ids collide or logic changes.
  - _Fix:_ Constrain the UPDATE by tenant as well. Add AND tenant_id = ${tenantId} to the WHERE clause (or include an explicit tenant guard in the UPDATE parameters) so every UPDATE against a tenant-scoped table is scoped to ctx.tenantId.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} is executed without a tenant_id constraint. Although the workflow row was selected earlier using tenant-scoped queries, this UPDATE uses only id in the WHERE clause and thus violates the invariant that every write must be constrained to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId} (use the trusted ctx.tenantId). Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id in their WHERE clauses.
- **server/video-job-runner.ts:225** — Global UPDATE on tenant-scoped table videoJobs in recoverStaleVideoJobs updates rows for all tenants (UPDATE videoJobs SET status='failed' ... WHERE status IN (...) AND updatedAt < staleCutoff) with no tenant_id constraint. This can mark jobs across tenants as failed inadvertently.
  - _Fix:_ Either run this GC per-tenant (accept tenantId and scope the UPDATE) or ensure this system-wide operation is intentionally privileged and documented. Prefer adding tenant-limiting logic or an admin-only path and/or explicitly exclude tenant rows you must not touch; at minimum verify and log the tenant context when performing global writes.

### HIGH (173)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped `experiments` without constraining to a tenant (no tenant_id WHERE clause). This reads running A/B experiments across all tenants.
  - _Fix:_ Add a tenant filter to the query (e.g. WHERE tenant_id = ${callerTenantId}) or use Drizzle .where(eq(experiments.tenantId, tenantId)). If this is meant to run per-tenant, accept the tenantId and scope the SELECT accordingly.
- **server/agent-activity.ts:122** — UPDATE against tenant-scoped `agent_activity` uses WHERE id = ${activityId} only — no tenant_id constraint. This can modify rows across tenants.
  - _Fix:_ Include the tenant constraint on updates (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) or require tenantId as a parameter and use it in the WHERE clause.
- **server/agent-eval.ts:218** — UPDATE agent_evals sets completion fields with WHERE id = ${evalId} only — no tenant_id constraint. This allows updating eval rows across tenants by id.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or use Drizzle .where(and(eq(agentEvals.id, evalId), eq(agentEvals.tenantId, tenantId))). Ensure tenantId is validated and passed through the call path.
- **server/agentic-engines.ts:160** — SELECT FROM tenant-scoped `ai_insights` in retryPendingMinervaRouting lacks tenant filtering (SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ...). This reads insights across tenants without per-tenant restriction.
  - _Fix:_ Either accept a tenantId and filter by it, or if this is a deliberate multi-tenant durability sweep, ensure the code explicitly documents and enforces admin-only execution. Prefer adding WHERE tenant_id = ${tenantId} for per-tenant runs.
- **server/agentic-features.ts:599** — SELECT id FROM `outreach_enrollments` used to check existing enrollment does not include tenant_id (SELECT id FROM outreach_enrollments WHERE sequence_id = ${sequenceId} AND contact_email = ...). This can incorrectly detect duplicates across tenants or miss same-tenant duplicates.
  - _Fix:_ Add tenant_id filtering to the query (e.g. WHERE tenant_id = ${params.tenantId} AND sequence_id = ...). Always scope SELECTs on tenant-scoped tables to the caller tenant.
- **server/agentic-features.ts:671** — UPDATE outreach_enrollments SET ... WHERE id = ${enrollment.id} has no tenant_id constraint. This updates an enrollment row by id without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to ensure the caller can only modify rows within their tenant.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments SET reply_classification... WHERE id = ${row.id} lacks a tenant_id constraint. Even though row was selected earlier, the UPDATE must still include tenant scoping to avoid cross-tenant modification risks.
  - _Fix:_ Add tenant filter to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or use Drizzle .where(and(eq(outreachEnrollments.id, row.id), eq(outreachEnrollments.tenantId, params.tenantId))).
- **server/agentic-features.ts:524** — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} is executed without constraining tenant_id. This can modify lead rows across tenants.
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}). Validate tenant ownership of referenced lead rows before updating.
- **server/agentic/approvals.ts:122** — Bulk UPDATE marking approvals expired (UPDATE agent_approvals SET status = 'expired' WHERE status = 'pending' AND expiresAt < ...) has no tenant_id constraint and will expire approvals across all tenants.
  - _Fix:_ If expiry should be tenant-scoped, add tenantId to the WHERE clause. If this is an intended global sweep, restrict this operation to an admin context and document it. Prefer explicit per-tenant expiry (or iterate tenants) and always include tenant_id in WHERE for tenant-scoped tables.
- **server/agentic/department-budgets.ts:143** — Query reads from tenant-scoped table `department_budgets` without constraining by tenant_id: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`.
  - _Fix:_ Avoid unrestricted cross-tenant scans on tenant-scoped tables. Either (a) require and pass an explicit tenantId and add `WHERE tenant_id = ${tenantId}`, or (b) make this an explicit, documented admin-only operation requiring elevated privileges/audit and run it from a service account (not a per-tenant request path). If the intention is a global sweep, ensure it runs only in a trusted admin context and log/audit access; otherwise limit the query to a single tenant or move per-tenant processing into a loop that queries each tenant with an explicit tenant filter.
- **server/agentic/harness-adaptation.ts:71** — Query reads from `agent_trace_spans` with no tenant filter: mining failures uses `SELECT ... FROM agent_trace_spans ... WHERE status IN ('error','declined') ...` and does not constrain by tenant_id.
  - _Fix:_ If `agent_trace_spans` is tenant-scoped, avoid returning cross-tenant telemetry in a single query. Add an explicit tenant_id filter when running per-tenant, or ensure this function is strictly an admin-only job that runs in a trusted context (use an admin service account and audit access). Alternatively, if global mining is required for model-level adaptation, document and enforce that the operation is only callable by a privileged background process and consider moving tenant_id into the projection so downstream logic is explicit about which tenant each row belongs to.
- **server/agentic/resume-worker.ts:66** — SELECT query reads agent_runs / agent_approvals without constraining by tenant_id. This returns rows across tenants (tenant-scoped tables queried with no tenant filter).
  - _Fix:_ Add a tenant_id filter (e.g. WHERE r.tenant_id = ${tenantId}) or otherwise scope the query to an explicit admin/system context with an auditable justification. If the function must operate per-tenant, accept a tenantId parameter and include eq(agent_runs.tenant_id, tenantId) in the WHERE.
- **server/agentic/runs.ts:36** — update(agentRuns).where(eq(agentRuns.id, runId)) — appendStep updates a tenant-scoped table (agent_runs) without a tenant_id constraint.
  - _Fix:_ Require and pass tenantId into appendStep (or otherwise resolve/verify the run's tenant before updating) and include it in the WHERE (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) to enforce per-tenant scoping.
- **server/agentic/runs.ts:45** — update(agentRuns).where(eq(agentRuns.id, runId)) — updateRunState merges state into agent_runs but lacks a tenant_id constraint on the UPDATE.
  - _Fix:_ Change updateRunState to accept/validate tenantId and include eq(agentRuns.tenantId, tenantId) in the WHERE clause, or otherwise ensure the update is qualified by the owning tenant before committing.
- **server/agentic/runs.ts:54** — update(agentRuns).where(eq(agentRuns.id, runId)) — replaceRunState overwrites run state with no tenant_id filter. This can modify runs belonging to other tenants.
  - _Fix:_ Require tenantId in the call and include a tenant_id equality condition in the WHERE (e.g. and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:58** — update(agentRuns).where(eq(agentRuns.id, runId)) — completeRun marks a run completed without constraining by tenant_id.
  - _Fix:_ Include the tenant filter in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Modify callers or the function signature to supply tenantId so the update is tenant-scoped.
- **server/agentic/runs.ts:67** — update(agentRuns).where(eq(agentRuns.id, runId)) — failRun updates a tenant-scoped row without a tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and require the tenantId from callers to avoid cross-tenant modifications.
- **server/agentic/runs.ts:76** — update(agentRuns).where(eq(agentRuns.id, runId)) — pauseRun sets status='paused' on agent_runs with no tenant_id filter.
  - _Fix:_ Include eq(agentRuns.tenantId, tenantId) in the WHERE clause and change the function to accept/validate tenantId so the pause is applied only to the intended tenant's run.
- **server/agentic/wake-scheduler.ts:79** — UPDATE agent_wake_schedules ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... ) — claiming due wakes is done without a tenant_id constraint, affecting rows across all tenants.
  - _Fix:_ Scope the claim/update by tenant (e.g. run per-tenant or add tenant_id = ${tenantId} to the inner/outer WHERE) or explicitly document and gate this as a system/global operation. Prefer per-tenant scans or include tenant_id in the UPDATE's WHERE to enforce tenant isolation.
- **server/auto-memorize.ts:159** — SELECT from messages (tenant-scoped) without any tenant_id constraint — this reads user/assistant messages across all tenants in one batch.
  - _Fix:_ If this is intended as an admin/owner-only cross-tenant analytics pass, make the intent explicit and guard the caller (require admin tenant / explicit admin flag). Otherwise restrict the SELECT to a single tenant (e.g. WHERE m.tenant_id = ${tenantId} or join on conversations and filter by conversation.tenant_id = ${tenantId}). Consider iterating per-tenant (explicit tenant loop) so every read is constrained to one tenant at a time and audit who invoked the operation.
- **server/auto-memorize.ts:100** — SELECT FROM memory_entries (recent facts) with NO tenant_id filter in isDuplicate(). This dedup logic compares facts across tenants and can cause facts from one tenant to block insertion for another tenant.
  - _Fix:_ Restrict the dedupe query to the target tenant (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...). If cross-tenant dedup is intentional for platform-level reasons, require an explicit admin-only path and document it; otherwise perform dedupe per-tenant before skipping writes.
- **server/auto-transcript.ts:46** — SELECT from messages table is executed without constraining by tenant_id (WHERE clause lacks tenant filter). This reads a tenant-scoped table by conversation_id only.
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. AND tenant_id = ${tenantId}) or join messages through the already-validated conversation row so the DB read is scoped to the caller's tenant. Example: SELECT ... FROM messages WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} (or JOIN conversations c ON messages.conversation_id = c.id AND c.tenant_id = ${tenantId}).
- **server/auto-transcript.ts:128** — Backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) that queries messages without any tenant filter. That subquery reads a tenant-scoped table without scoping by tenant.
  - _Fix:_ Scope the messages subquery by tenant. For example: (SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) > 0 — or otherwise join messages with conversations/projects to ensure the count is only for the conversation's tenant.
- **server/auto-transcript.ts:153** — The 'alsoLinked' query includes a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) that queries messages with no tenant constraint. This reads a tenant-scoped table without scoping by tenant.
  - _Fix:_ Add tenant scoping to the messages subquery (e.g. SELECT COUNT(*) FROM messages m WHERE m.conversation_id = c.id AND m.tenant_id = c.tenant_id) or join messages to enforce the tenant match before using the result.
- **server/business-tools.ts:331** — SELECT in listContracts LEFT JOINs customers (cust) using cust.id = c.customer_id but does not constrain the joined customers row to the caller's tenant (no cust.tenant_id = ${tid}). This can return customer data from other tenants referenced by id.
  - _Fix:_ Constrain the joined customers row to the same tenant (e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}) or add an explicit WHERE (or additional ON condition) ensuring cust.tenant_id = c.tenant_id/${tid}. Always enforce tenant ownership on joined foreign rows when reading them.
- **server/ceo-orchestrator.ts:814** — Unfiltered SELECT from tenant-scoped table `personas` — db.select().from(personasTable) reads all personas with no tenant_id constraint. This allows cross-tenant data exposure.
  - _Fix:_ Apply a tenant filter when querying tenant-scoped tables. Pass the current tenantId into the query and restrict results (e.g. using Drizzle: .where(eq(personasTable.tenantId, plan.tenantId)) or similar). Cache only the tenant's personas, or make the call accept an explicit tenantId parameter and enforce it server-side.
- **server/channel-routing.ts:30** — Query joins global channel_routes to tenant-scoped `personas` without constraining the personas by tenant_id (SELECT cr.*, p.name as persona_name FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ...). This can leak persona names from other tenants.
  - _Fix:_ Either make channel_routes tenant-scoped (add tenant_id and filter by it) or require and pass an explicit tenantId to listChannelRoutes and add a WHERE clause to limit the join to personas with the same tenant_id (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = $tenantId). Ensure all selects against tenant-scoped tables include tenant_id filters.
- **server/channel-routing.ts:53** — INSERT/UPSERT into global channel_routes stores a persona_id provided by caller without validating that the referenced persona belongs to the caller's tenant. This writes a foreign id into a global table with no ownership check.
  - _Fix:_ Validate ownership of personaId before inserting/updating (fetch the persona row and verify persona.tenant_id === callerTenantId), or make channel_routes tenant-scoped and require tenant_id on insert. Never accept a foreign id from untrusted input without verifying the referenced row's tenant_id.
- **server/chat-engine.ts:4594** — storage.updateMemoryEmbedding(entry.id, emb) is invoked without any tenant context. This updates a tenant-scoped embedding row by id but does not constrain the update to the caller's tenant (no tenantId passed / ownership check).
  - _Fix:_ Change updateMemoryEmbedding to accept and require tenantId (or perform an ownership check) and call it with the caller's tenantId. Alternatively, fetch/verify the memory entry belongs to the tenant before updating, and use a WHERE tenant_id = <tenant> clause in the update.
- **server/chat-engine.ts:4327** — storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is called without passing tenantId. If attachCitations performs INSERTs into tenant-scoped citation tables they must receive tenant_id explicitly; currently no tenantId is supplied.
  - _Fix:_ Pass tenantId explicitly into attachCitations (e.g. storage.attachCitations(messageId, collectedCitations, tenantIdForScope)) or ensure attachCitations derives and sets tenant_id from the provided message id in a way that enforces the explicit WHERE/INSERT tenant constraint. Also validate ownership of the referenced message id before attaching citations.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` executed without any tenant_id constraint (scans overdue commitments across all tenants).
  - _Fix:_ If this is intended only for platform-admin/operator workflows, make that explicit and gate the function with an enforced admin check (do not expose it to regular tenant callers). Otherwise, restrict the SELECT to a single tenant (e.g. WHERE tenant_id = ${tenantId}) or provide an explicit admin-only API and audit log. Avoid unscoped reads from tenant-scoped tables to prevent cross-tenant data leakage.
- **server/data-protection.ts:73** — softDeleteConversation updates conversations (SET deleted_at, deleted_by) using WHERE id = ${conversationId} with NO tenant_id predicate. Even though the code previously SELECTed the conversation for the tenant, the UPDATE itself lacks an explicit tenant_id filter.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (UPDATE conversations SET ... WHERE id = ${conversationId} AND tenant_id = ${tenantId}). Failing closed at the statement-level prevents accidental cross-tenant modifications if earlier checks are bypassed or changed.
- **server/data-protection.ts:112** — permanentlyPurgeSoftDeleted SELECTs expired conversations without a tenant_id filter (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads tenant-scoped data across all tenants without an explicit tenant constraint.
  - _Fix:_ If this is intended to be a tenant-scoped operation, add a tenant_id predicate. If it is an administrative/global job, restrict access to a dedicated admin-only code path and make the admin intent explicit; still prefer to include tenant_id when issuing per-tenant deletes to avoid mistakes.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog performs UPDATE on delivery_logs with WHERE id = <id> but does not include a tenant_id constraint. This lets code update any delivery row by id across tenants.
  - _Fix:_ Require and pass the tenantId into updateDeliveryLog (e.g. updateDeliveryLog(id, updates, tenantId)) and include eq(deliveryLogs.tenantId, tenantId) in the WHERE clause. If the function is only used in flows that already validated tenant ownership, make that explicit in the signature and enforce it (do not rely on implicit trust).
- **server/delivery-pipeline.ts:720** — UPDATE on delivery_logs clears stripePaymentId / appends metadata using WHERE id = <prior.id> without also verifying tenant_id, allowing a request to modify another tenant's delivery row.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(deliveryLogs.id, prior.id), eq(deliveryLogs.tenantId, tenantId)))) or change the flow to perform this update inside a transaction where the prior row was selected with tenant scoping and that tenant value is used for the update filter. Ensure the operation requires the caller's tenant context.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} is executed without AND tenant_id = ${tenantId}. This can delete chunks across tenants if collection_id/doc_path collisions or IDs are misused.
  - _Fix:_ Include tenant scoping in the DELETE (AND tenant_id = ${tenantId}). Prefer always scoping mutations on tenant-scoped tables by tenant_id. Example: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}.
- **server/embeddings.ts:726** — backfillEmbeddingVecs does UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} without tenant_id constraint. This writes embedding_vec regardless of tenant ownership of the id.
  - _Fix:_ When updating tenant-scoped rows in batch backfills, include tenant_id in the WHERE or query rows grouped by tenant and update within that tenant context. At minimum, read the tenant_id for each row and use it in the UPDATE WHERE clause to prevent cross-tenant writes.
- **server/embeddings.ts:743** — backfillEmbeddingVecs does UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without tenant_id constraint, allowing cross-tenant writes to agent_knowledge rows.
  - _Fix:_ Same as above: include tenant scoping in the UPDATE (WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform per-tenant backfills that supply tenantId for each row. Ensure any helper that writes to tenant-scoped tables accepts and enforces tenantId.
- **server/evaluators.ts:55** — Query joins conversations (c) but only filters m.tenant_id. The SELECT reads conversation rows (persona_id) without constraining c.tenant_id, allowing cross-tenant data to be returned.
  - _Fix:_ Include the conversation tenant constraint in the query (e.g. add AND c.tenant_id = ${tenantId} or change the JOIN to ON m.conversation_id = c.id AND c.tenant_id = ${tenantId}) so every referenced tenant-scoped table is filtered by the caller's tenant.
- **server/evaluators.ts:340** — Query joins conversations (c) but only filters m.tenant_id. The SELECT reads conversation.persona_id without constraining c.tenant_id, so conversation rows from other tenants could be read.
  - _Fix:_ Add an explicit tenant filter on the conversations table (e.g. AND c.tenant_id = ${tenantId} or include it in the JOIN condition) to ensure the read is scoped to the caller's tenant.
- **server/google-drive.ts:560** — UPDATE on 'projects' sets driveFolderId/driveFolderUrl using only projects.id in the WHERE clause and does not include tenant scoping (eq(projects.id, projectId)).
  - _Fix:_ Constrain the update to the caller's tenant as well, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Even if ownership was checked earlier, always include tenant scoping on writes to tenant-scoped tables.
- **server/google-workspace.ts:2358** — INSERT/ON CONFLICT into presenter_slide_images uses session_id only (INSERT ... VALUES (${sessionId}, ...)) and does not assert the tenant or otherwise constrain the operation. If presenter_slide_images/session records are tenant-scoped this can allow writes for another tenant's session id.
  - _Fix:_ Ensure the session belongs to the calling tenant before inserting, and either include tenant_id in the presenter_slide_images table and set it in the INSERT, or otherwise perform the insert via a secured path that enforces tenant ownership (e.g. INSERT ... SELECT with a WHERE that validates session_id belongs to tenant).
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a tenant-scoped table by session id only and lacks a tenant constraint; this can modify another tenant's session data.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or otherwise verify session ownership prior to performing the UPDATE so that only the owning tenant's session is modified.
- **server/health-audit.ts:314** — SELECT from code_proposals is missing tenant scoping (no WHERE tenant_id = ...). This reads potentially across all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}), or otherwise ensure this code is explicitly running with global/admin intent and the table is intentionally global. Prefer passing a tenantId into archiveStaleProposals and adding AND tenant_id = ${tenantId} to the SELECT to enforce isolation.
- **server/heartbeat.ts:52** — SELECT on research_schedules (SELECT * FROM research_schedules WHERE ...) has no tenant_id constraint. research_schedules rows contain tenant_id elsewhere in this file (sched.tenant_id is used), so this read can return schedules across tenants.
  - _Fix:_ If the intention is to iterate per-tenant, query per tenant (e.g. fetch active tenant IDs and SELECT ... WHERE tenant_id = <tid> ...) or add an explicit tenant_id filter. Do not rely on post-selection filtering; ensure every read from tenant-scoped tables includes an application-level tenant_id constraint or is explicitly documented/implemented as a safe cross-tenant admin operation protected by proper authorization.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET ... WHERE id = ${sched.id} (multiple occurrences at ~lines 101,106,112) updates research_schedules rows by id only with no tenant_id WHERE clause. research_schedules appears to be tenant-scoped (sched.tenant_id is referenced elsewhere).
  - _Fix:_ Include tenant_id in the WHERE clause when updating/deleting tenant-scoped tables, e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}. Alternatively perform the update via a storage helper that asserts the tenant context before issuing SQL.
- **server/heartbeat.ts:585** — SELECT on event_log (SELECT * FROM event_log WHERE status = 'pending' ...) has no tenant_id constraint. event_log rows include tenant_id (used later), so this read will fetch events from all tenants.
  - _Fix:_ Either iterate per-tenant (SELECT tenant ids then SELECT ... WHERE tenant_id = <tid> AND status='pending') or include tenant_id in the WHERE clause. Ensure any processing that routes/acts on events enforces the tenant context before performing actions that should be tenant-scoped.
- **server/heartbeat.ts:257** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} (and a similar UPDATE later) performs writes to heartbeat_tasks without constraining by tenant_id. heartbeat_tasks are tenant-associated elsewhere in code.
  - _Fix:_ Add tenant_id to the WHERE clause when updating heartbeat_tasks (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenant_id}) or use the storage layer (which should assert tenant context) to perform the update.
- **server/heartbeat.ts:2946** — Direct DB UPDATE: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint. This updates approval status by id only and can affect a task belonging to another tenant if misused.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) when updating tenant-scoped rows. Prefer using a storage helper that enforces tenant ownership before any UPDATE.
- **server/heartbeat.ts:3222** — Direct DB UPDATE: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (duplicated later in delegateTaskFromChat) with no tenant_id constraint.
  - _Fix:_ Same fix: constrain UPDATE with tenant_id (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a storage API that asserts tenant context to avoid cross-tenant writes.
- **server/internal-resolver.ts:91** — UPDATE event_log executed without scoping to tenant_id — finalizeEvent updates a tenant-scoped table by id only (no WHERE tenant_id = ...), allowing cross-tenant mutation/finalization of events.
  - _Fix:_ Require and pass a tenantId (or look it up atomically) into finalizeEvent and include AND tenant_id = ${tenantId} in the WHERE clause; alternatively JOIN against event_log with the tenant context when finalizing so you never update another tenant's rows.
- **server/internal-resolver.ts:107** — SELECT FROM plans by id with no tenant constraint (SELECT id, objective, status FROM plans WHERE id = ${planId}) — resolving/auto-rejecting plans without verifying the plan belongs to the intended tenant allows cross-tenant decisions.
  - _Fix:_ Fetch the plan with a tenant predicate (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or derive tenant_id from the correlated event_log row and verify ownership before taking action (and fail/abort if ownership mismatch). Always validate tenant ownership before calling decidePlan.
- **server/internal-resolver.ts:170** — UPDATE code_proposals sets status='archived_verification_failed' WHERE id = ${proposalId} AND status = 'pending' — no tenant_id constraint, so proposals for other tenants may be modified.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or otherwise verify the proposal's tenant before mutating. Ensure resolver functions accept/resolve with tenant context (from event row or event payload) and enforce tenant equality on every read/write to tenant-scoped tables.
- **server/lib/agent-trace.ts:129** — UPDATE against tenant-scoped table agent_trace_spans omits tenant filter — the UPDATE uses WHERE span_id = ${spanId} with no tenant_id predicate, so a span_id from another tenant could be updated.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Pass tenantId into persistSpanClose (or read currentTenantId() inside it) and add AND tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Ensure every UPDATE/DELETE/SELECT on tenant-scoped tables includes the tenant predicate.
- **server/lib/inbox-ingest.ts:431** — SELECT from tenant-scoped table `inbox_messages` lacks a tenant_id constraint. The orphan-recovery query (`SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ON c.inbox_message_id = m.id WHERE m.message_id = ${id} AND c.id IS NULL LIMIT 1`) can return rows across tenants.
  - _Fix:_ Restrict the query to the caller's tenant. Add `AND m.tenant_id = ${tenantId}` (or equivalent Drizzle `.where(eq(m.tenantId, tenantId))`) to the WHERE clause so only that tenant's rows are considered. Example: `WHERE m.message_id = ${id} AND m.tenant_id = ${tenantId} AND c.id IS NULL`.
- **server/lib/inbox-ingest.ts:491** — UPDATE on tenant-scoped table `inbox_classifications` omits tenant_id in the WHERE clause. `UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}` may update another tenant's row that happens to reference that inbox_message_id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant by adding `AND tenant_id = ${tenantId}` (or equivalent Drizzle `.where(eq(table.tenantId, tenantId))`). Example: `WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}`.
- **server/lib/restraint-trust.ts:88** — Dynamic use of sql.raw() with a request-derived value for the column name in the UPDATE statement (sql`SET ${sql.raw(column)} = ${sql.raw(column)} + ${delta} ...`). The column is chosen based on the caller-supplied `outcome` and, although whitelisted earlier, this pattern risks SQL-injection-like misuse or future regressions where an unvalidated identifier could be used.
  - _Fix:_ Avoid embedding identifiers via sql.raw(). Instead, implement the update as one of a small, explicit set of parameterized statements (e.g. switch on outcome and call db.update(...).set({ action_alpha: sql`action_alpha + ${delta}` }) or similar), or use Drizzle's safe identifier APIs if available. Keep the mapping server-side and eliminate runtime raw interpolation of request-derived values to remove injection/isolation risk.
- **server/martech-bundle.ts:314** — UPDATE to tenant-scoped table (tenant_voice_profiles) uses only id in WHERE clause and does not include tenant_id, so a row belonging to another tenant could be updated if the id is known or abused.
  - _Fix:_ Include an explicit tenant check in the UPDATE WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) or otherwise ensure the UPDATE is parameterized with the caller's tenant_id.
- **server/memory-graph.ts:125** — UPDATE memory_categories sets memory_count WHERE id = ${categoryId} without constraining by tenant_id. This can update another tenant's category row if the id is known.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}) and ensure tenantId is passed into refreshCategoryCount so the write is constrained to the caller's tenant.
- **server/memory-graph.ts:181** — SELECT FROM memory_links checks for an existing link by source/target ids but does not restrict the query by tenant_id. This read can leak presence of cross-tenant links.
  - _Fix:_ Constrain the existence check by tenant (for example join memory_links to memory_entries or add AND tenant_id = ${tenantId}) so the query only considers links belonging to the caller's tenant.
- **server/memory-graph.ts:392** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} does not include tenant_id in the WHERE clause, allowing updates to rows from other tenants if ids are known.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) so the write cannot affect rows outside the caller's tenant.
- **server/minerva-planner.ts:373** — SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} has no tenant_id filter — reads tenant-scoped data without scoping to caller's tenant.
  - _Fix:_ Include tenant scoping in the probe SELECT (e.g. WHERE id = ${args.planId} AND tenant_id = ${tenantId}) or first fetch the plan with tenant verification. If this probe is used in retry/idempotency logic, require the caller to provide tenantId or derive it from a trusted server context and validate ownership before returning data.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ... lacks tenant_id constraint — may expose/return plan IDs from other tenants.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) to the child-plan lookup, or first resolve/verify the parent plan's tenant and pass that tenantId into this query. Avoid relying on implicit trust of planId alone.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT relies on project_id ownership check but does not write tenant_id into the row, violating the invariant that tenant_id must be set explicitly on tenant-scoped tables.
  - _Fix:_ Include tenant_id explicitly in the INSERT column list and values (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Keep the existing assertProjectInTenant check (ownership validation) but still persist tenant_id to avoid cross-tenant ambiguity and enforce isolation at the application level.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` does not include tenant_id. As above, the upsert/write omits tenant_id even though this table is tenant-scoped.
  - _Fix:_ Add tenant_id to the INSERT statement (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Continue to verify project ownership (assertProjectInTenant) but ensure the tenant_id column is explicitly set on every insert.
- **server/personality-files.ts:5** — CREATE TABLE personality_files defines tenant_id with DEFAULT 1 (tenant_id INTEGER NOT NULL DEFAULT 1). This allows inserts to rely on a default tenant and violates the invariant that every INSERT must pass tenantId explicitly (tenant-scoped columns must not have a default).
  - _Fix:_ Remove the DEFAULT on tenant_id and ensure the column is NOT NULL with no default. Enforce at the schema level that every INSERT/UPSERT must explicitly provide tenant_id; run a code audit to ensure all INSERTs pass tenantId explicitly.
- **server/plan-executor.ts:706** — resumeStuckPlans executes an UPDATE to flip any stale executing plans back to 'approved' without restricting by tenant_id (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id). This will operate across all tenants and can reset other tenants' plans unexpectedly.
  - _Fix:_ If intended to be system-global, make this explicit and document the behavior. Otherwise, restrict recovery to specific tenants or iterate per-tenant (ADD tenant filtering) so tenant isolation is preserved. At minimum, include tenant_id in the WHERE or run the operation per-tenant with an explicit admin context.
- **server/plan-executor.ts:716** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is executed with no tenant_id constraint; later executePlan is invoked for each returned id. This can cause processing of plans across tenants without tenant-scoped gating.
  - _Fix:_ Only select pending/approved plans for the tenant(s) this executor is authorized to run. Either add tenant_id = ${tenantId} to the query or iterate per-tenant in a controlled system process that explicitly acknowledges cross-tenant scope (with appropriate admin privileges and audit).
- **server/plan-replay.ts:59** — SQL embeds an embedding literal into the query text with a cast: 1 - (objective_embedding <=> ${embeddingLiteral}::vector) ... ORDER BY objective_embedding <=> ${embeddingLiteral}::vector. The embeddingLiteral is derived from request data and is being string-interpolated into SQL (vector literal + cast), creating parameterization and injection risk as well as bypassing safe binding. 
  - _Fix:_ Avoid injecting array text into the SQL string. Parameterize the embedding and cast safely (use prepared parameters and cast on the parameter, or use proper driver support for vector parameters). If the DB driver/ORM doesn't support direct binding to vector types, use a safe helper that sends the embedding as a parameter (not by concatenating into SQL) or persist embeddings in a param-friendly form. Ensure the tenant_id filter remains present (it already is) and do not interpolate request-derived values into SQL text.
- **server/plan-replay.ts:148** — INSERT INTO plan_replay_cache uses ${embeddingLiteral}::vector and ${JSON.stringify(...)}::jsonb interpolation directly in SQL. Both the embedding and the JSON are request-derived and are being injected into the SQL text rather than passed as bound parameters (injection/isolation risk).
  - _Fix:_ Use parameterized binding for the embedding and the JSON payload: pass the embedding as a parameter (or use the driver's vector helper) and pass plan_json as a JSONB parameter rather than string-interpolating and appending ::vector/::jsonb. Alternatively use the ORM insert helpers that accept JS objects and let the driver handle parameterization.
- **server/plan-rollout-simulator.ts:119** — kNN query over felix_proposals interpolates a vector literal built from generateEmbedding(blob): '... 1 - (args_embedding <=> ${vec}::vector) AS args_sim ... ORDER BY args_embedding <=> ${vec}::vector'. The vector text (${vec}) is constructed from request-derived data and string-injected into SQL (casting to ::vector). This is a parameterization/injection risk and violates the 'no string-interpolated SQL' rule.
  - _Fix:_ Do not inline embedding text into SQL. Parameterize the embedding value and perform any required cast via a parameterized expression or use DB/ORM support for vector parameters. If the vector-extension requires a literal, provide a safe server-side wrapper that binds the array as a parameter or stores the vector first and then references it by id. Ensure tenant_id is still included (it is) and avoid direct string interpolation of request-derived blob data.
- **server/plan-rollout-simulator.ts:240** — When persist=true the code INSERTs steps_json and weak_links_json using sql`${JSON.stringify(steps)}::jsonb` and sql`${JSON.stringify(weak)}::jsonb`, i.e. string-interpolating request-derived JSON and appending ::jsonb in the SQL. This risks SQL injection / broken parameterization and should not be done by concatenating JSON text into SQL.
  - _Fix:_ Bind JSON/objects as parameters and let the driver cast to jsonb (or use the ORM's insert helpers that accept JS objects). For example: db.insert(plan_rollout_simulations).values({ tenant_id: tenantId, plan_summary: options.planSummary ?? '', steps_json: steps, predicted_success: chained, estimated_cost_cents: totalCost, weak_links_json: weak }) — or otherwise pass JSON as bound parameters rather than embedding stringified JSON into the SQL text.
- **server/proactive-engine.ts:139** — SELECT from tenant-scoped table `proactive_actions` without tenant filter: `SELECT persona_id FROM proactive_actions WHERE id = ${actionId}`
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${actionId} AND tenant_id = ${tenantId}`) or first verify the action belongs to the caller's tenant before using the returned persona_id.
- **server/process-governor.ts:121** — Subquery reads from tenant-scoped `heartbeat_logs` without scoping by tenant: `(SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id AND status = 'success' ...)` (and a sibling failures COUNT)
  - _Fix:_ Constrain the subselects by tenant (e.g. `... WHERE task_id = ht.id AND tenant_id = ht.tenant_id AND ...`) or include `AND tenant_id = ${tenantId}` to ensure logs belong to the caller's tenant.
- **server/process-governor.ts:155** — Subquery reads from tenant-scoped `heartbeat_tasks` without scoping by tenant: `(SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true)`
  - _Fix:_ Include tenant filtering in the subquery (e.g. `... WHERE persona_id = ad.persona_id AND tenant_id = ad.tenant_id AND enabled = true`) so counts are limited to the tenant context.
- **server/process-governor.ts:199** — SELECT on tenant-scoped `heartbeat_logs` missing tenant constraint: `SELECT AVG(duration_ms) as avg_ms FROM heartbeat_logs WHERE created_at > ...`
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or equivalent) to the WHERE clause to scope this aggregation to the caller's tenant.
- **server/process-governor.ts:241** — SELECT on tenant-scoped `heartbeat_logs` missing tenant constraint: `SELECT COUNT(DISTINCT persona_id) as failing_agents FROM heartbeat_logs WHERE status != 'success' ...`
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause so the distinct persona count is limited to the tenant's logs.
- **server/project-brain.ts:135** — SELECT from tenant-scoped `projects` by id without any tenant filter: `SELECT name, status, description FROM projects WHERE id = ${projectId}`
  - _Fix:_ Require and pass the tenantId (or otherwise verify project ownership) and constrain the query (`WHERE id = ${projectId} AND tenant_id = ${tenantId}`) to prevent cross-tenant reads.
- **server/project-brain.ts:349** — SELECT conversations by project id without tenant constraint: `SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}`
  - _Fix:_ Include tenant scoping (e.g. `AND c.tenant_id = ${tenantId}`) or otherwise verify the project → conversation relationship is constrained to the tenant before reading conversations.
- **server/project-brain.ts:359** — SELECT messages by conversation id without tenant constraint: `SELECT role, content FROM messages WHERE conversation_id = ${conv.id}`
  - _Fix:_ Constrain message reads to the tenant (e.g. `WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}`) or validate conversation ownership prior to querying messages.
- **server/proposal-verifier.ts:33** — SELECT from `code_proposals` by id with no tenant constraint: `SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId}`
  - _Fix:_ Pass tenant context into the verifier and scope the SELECT (e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`) or validate ownership before reading to prevent cross-tenant disclosure.
- **server/reference-learner.ts:291** — UPDATE memory_entries SET fact = ... WHERE id = ${storedId} is executed without constraining tenant_id, allowing cross-tenant modification of memory entries.
  - _Fix:_ Add tenant ownership checks to the WHERE clause (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) or assert ownership before updating. All writes to tenant-scoped tables must include tenant_id.
- **server/research-engine.ts:310** — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} updates a tenant-scoped row without checking tenant_id, potentially modifying another tenant's program.
  - _Fix:_ Constrain the update by tenant (e.g. WHERE id = ${programId} AND tenant_id = ${tenantId}) after verifying the program belongs to the expected tenant. Always include tenant_id in UPDATEs on tenant-scoped tables.
- **server/research-engine.ts:450** — SELECT from research_experiments WHERE session_id = ${sessionId} AND status = 'keep' lacks a tenant_id filter and may return other tenants' experiments.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} (or equivalent assert) to the WHERE clause or otherwise verify the experiments belong to the session's tenant before reading them.
- **server/research-engine.ts:459** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} is executed without tenant_id verification, allowing reading a program that may belong to a different tenant.
  - _Fix:_ Query the program with an explicit tenant filter (WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}) or assert program ownership before using its data.
- **server/research-engine.ts:464** — SELECT summary FROM research_sessions WHERE id = ${sessionId} is performed with no tenant_id constraint; this can disclose a different tenant's session summary.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}) or otherwise validate that the session belongs to the expected tenant before reading it.
- **server/research-engine.ts:814** — UPDATE research_experiments SET ... WHERE id = ${experimentId} is executed without a tenant_id constraint, allowing modification of experiments in other tenants.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}). Always include tenant_id in UPDATE/DELETE statements on tenant-scoped tables.
- **server/research-engine.ts:853** — UPDATE research_sessions SET total_experiments = ... WHERE id = ${session.sessionId} runs without tenant_id in WHERE, enabling cross-tenant writes.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} to the WHERE clause (or otherwise verify session ownership) to ensure only the intended tenant's session row is updated.
- **server/research-engine.ts:981** — SELECT name FROM research_programs WHERE id = ${session.programId} (in injectKeepedFinding) executes without tenant scoping, reading a possibly foreign tenant program.
  - _Fix:_ Include tenant_id in the SELECT (WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}) or assert program ownership prior to use.
- **server/research-engine.ts:1476** — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND replayed_at IS NULL is executed without tenant_id constraint during replayHighValueFindings — can mark rows across tenants as replayed.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or validate the tenant before updating) so only the intended tenant's experiments are claimed/updated.
- **server/research-engine.ts:1519** — UPDATE research_experiments SET replayed_at = NULL WHERE id = ${row.id} AND replayed_proposal_id IS NULL runs without tenant scoping and may clear replay claims for experiments in other tenants.
  - _Fix:_ Add a tenant constraint (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId} AND replayed_proposal_id IS NULL) so the reset only affects the correct tenant's rows.
- **server/routes.ts:2214** — INSERT INTO presenter_sessions uses tenantId taken directly from request body and inserts it into a tenant-scoped table without validating the caller's right to act for that tenant (tenantId is untrusted input).
  - _Fix:_ Do not accept arbitrary tenantId from client input. Derive tenantId from the authenticated session, or if this route is intended to be called by an internal service (protected by SESSION_SECRET), additionally validate that the provided tenantId is allowed (e.g. check that the session/internal key maps to that tenant or require an explicit platform-admin role). Alternatively, remove tenantId from the request and enforce tenant association server-side.
- **server/routes/admin-zombie-detector.ts:55** — Query aggregates from `conversations` (CTE `used`) without constraining by tenant_id — this reads tenant-scoped data across all tenants.
  - _Fix:_ Add an explicit tenant filter to the CTE (e.g. WHERE persona_id IS NOT NULL AND tenant_id = ${tenantId} AND updated_at > ...) or otherwise ensure the query is explicitly limited to the caller's tenant. If cross-tenant aggregation is intended, document/justify it and ensure only a verified platform-admin code path (and auditing) is used.
- **server/routes/conversations.ts:68** — SELECT against personas omits tenant scoping: `SELECT reasoning_config FROM personas WHERE id = ${activePersona.id}` has no tenant_id constraint.
  - _Fix:_ Constrain the SELECT to the request tenant (or validate ownership) — e.g. fetch reasoning_config via a tenant-scoped storage helper or: `SELECT reasoning_config FROM personas WHERE id = ${activePersona.id} AND tenant_id = ${tenantId}` (or use conv.tenantId/validated persona owner). Ensure the persona id is validated to belong to the caller's tenant before returning sensitive config.
- **server/routes/conversations.ts:98** — UPDATE on conversations lacks tenant scoping: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` updates by id only.
  - _Fix:_ Add tenant constraint to the UPDATE to prevent cross-tenant modification, e.g. `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use conv.tenantId if available). Prefer using tenant-scoped storage helpers that enforce this automatically.
- **server/routes/conversations.ts:102** — INSERT into project_conversations omits tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id.
  - _Fix:_ If project_conversations is tenant-scoped, include tenant_id in the inserted row: `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conv.id}, ${tenantId})`. Alternatively, use a storage API that enforces the tenant value before inserting.
- **server/routes/conversations.ts:104** — UPDATE on projects lacks tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` performs an update by id only.
  - _Fix:_ Add tenant constraint to the UPDATE to enforce caller's tenant: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}`. Prefer verifying ownership earlier and always applying tenant scoping in SQL.
- **server/routes/inbox-notifications.ts:101** — UPDATE on inbox_messages omits tenant scoping: when marking a message read the code runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `tenant_id` in WHERE.
  - _Fix:_ Include the tenant constraint on the UPDATE to prevent marking another tenant's message: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`. Use tenant-scoped storage helpers or pass tenantId when updating.
- **server/routes/heartbeat.ts:102** — Approval UPDATE on heartbeat_tasks lacks tenant scoping: `UPDATE heartbeat_tasks SET approval_status = 'approved', enabled = true, next_run_at = ${nextRun} WHERE id = ${taskId}` does not restrict by tenant.
  - _Fix:_ Constrain the UPDATE by tenant to avoid approving tasks across tenants. Use the fetched task's tenant_id: `UPDATE heartbeat_tasks SET ... WHERE id = ${taskId} AND tenant_id = ${task.tenantId}` (or include the current request tenant). Prefer tenant-scoped storage/update helper that enforces this check.
- **server/routes/heartbeat.ts:116** — Rejection UPDATE on heartbeat_tasks lacks tenant scoping: `UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId}` does not restrict by tenant.
  - _Fix:_ Add tenant constraint to the UPDATE so only the owning tenant (or authorized admin for that tenant) can change approval: `UPDATE heartbeat_tasks SET ... WHERE id = ${taskId} AND tenant_id = ${task.tenantId}`. Use tenant-scoped storage helpers for updates.
- **server/routes/projects.ts:184** — INSERT into tenant-scoped table `project_notes` does not set tenant_id in the INSERT statement.
  - _Fix:_ Include the tenant_id column explicitly in the INSERT (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${id}, ${tenantId}, ...)) and/or use a parameterized Drizzle insert that sets tenantId. Do not rely on prior app-layer project checks — the SQL must be constrained with the tenant id.
- **server/routes/projects.ts:224** — INSERT into tenant-scoped table `project_files` (base64 upload path) does not set tenant_id in the INSERT statement.
  - _Fix:_ Include tenant_id explicitly when inserting project_files (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${projectId}, ${tenantId}, ...)). Ensure every INSERT to tenant-scoped tables passes the caller's tenantId as a parameter.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multipart upload path) does not set tenant_id in the INSERT statement.
  - _Fix:_ Include tenant_id explicitly when inserting project_files (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${projectId}, ${tenantId}, ...)). Always pass tenantId in INSERTs rather than relying solely on prior ownership checks.
- **server/routes/projects.ts:319** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id predicate (SELECT * FROM project_files WHERE project_id = ${projectId} ...).
  - _Fix:_ Constrain reads with the tenant id in SQL (e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ...). Do not rely only on a prior app-level project existence check — the SQL must include the tenant predicate.
- **server/routes/projects.ts:342** — SELECT from tenant-scoped table `project_files` (file download) lacks an explicit tenant_id predicate (WHERE project_id = ${projectId} AND file_path = ...).
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE project_id = ${projectId} AND file_path = ${path} AND tenant_id = ${tenantId}) so the DB query is explicitly tenant-scoped.
- **server/routes/projects.ts:371** — DELETE from tenant-scoped table `project_files` does not include tenant_id in the WHERE clause (DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId}).
  - _Fix:_ Add tenant_id to the DELETE WHERE clause (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) to ensure DB-level tenant scoping for destructive operations.
- **server/routes/projects.ts:33** — Correlated subqueries counting project-related rows (project_files, project_notes, project_conversations) in the projects listing do not include tenant_id predicates (they rely on project_id correlation instead of an explicit tenant_id check).
  - _Fix:_ Make the subqueries explicitly tenant-scoped (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or rewrite them to join the parent project and include a tenant_id predicate so every read against tenant-scoped tables contains an explicit tenant_id constraint.
- **server/seed.ts:34** — SELECT from tenant-scoped table research_sessions with no tenant_id constraint (SELECT COUNT(*) FROM research_sessions). This can expose cross-tenant data during snapshot import.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE tenant_id = ${tenantId}) or scope the count per-tenant before acting. Use parameterized Drizzle .where(eq(table.tenantId, tenantId)).
- **server/seed.ts:2854** — SELECT from tenant-scoped table research_programs without a tenant filter (SELECT id, name, model FROM research_programs WHERE is_active = true). This reads records across all tenants.
  - _Fix:_ Add a tenant constraint (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or pass the intended tenant id into the query via a parameterized where clause.
- **server/seed.ts:3204** — SELECT from tenant-scoped table conversations (and related project_conversations existence check) is executed without any tenant_id constraint. This can enumerate conversations across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE c.tenant_id = ${tenantId} AND ...) and ensure the NOT EXISTS check also constrains project_conversations by tenant. Use parameterized Drizzle where clauses.
- **server/seed.ts:3204** — SELECT from tenant-scoped table `conversations` without a tenant_id filter. The query SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no WHERE c.tenant_id = <tenant> constraint, so it may read rows across all tenants.
  - _Fix:_ Constrain the SELECT to the intended tenant (e.g. AND c.tenant_id = <tenantId>), or iterate tenants explicitly. Validate project/conversation ownership before acting to avoid cross-tenant effects.
- **server/seed.ts:3212** — INSERT into `project_conversations` uses project_id and conversation_id derived from an unfiltered conversations query. The code inserts (project_id, conversation_id) without validating that the referenced project/conversation belong to the same tenant.
  - _Fix:_ Verify ownership of project_id and conversation_id (ensure both rows have the caller's tenant_id) before inserting. Prefer adding tenant_id to project_conversations (and populate it) or include tenant_id in INSERT to make tenant binding explicit.
- **server/seed.ts:4402** — UPDATE against tenant-scoped table `memory_entries` lacks a tenant_id constraint. The memory-normalizer UPDATE runs globally (UPDATE memory_entries SET ... WHERE ...), touching rows for all tenants.
  - _Fix:_ Add AND tenant_id = <tenantId> to the WHERE clause (or run per-tenant). Avoid broad updates that affect every tenant; validate the tenant context and restrict updates accordingly.
- **server/seed.ts:4680** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = <id> without constraining tenant_id. Although the row was selected earlier with tenant_id = 1, the UPDATE itself does not include tenant_id and could affect a different tenant if ids collide or assumptions change.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), or re-query/validate ownership before updating to ensure tenant isolation.
- **server/seed.ts:4492** — SELECT against tenant-scoped table `heartbeat_tasks` lacks tenant filter (SELECT count(*) ... FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'). Subsequent UPDATE (disabling delegation tasks) also lacks tenant constraint, so it will affect all tenants.
  - _Fix:_ Add tenant_id = <tenantId> to SELECT and UPDATE statements, or loop per-tenant. Never perform global selects/updates on tenant-scoped tables without explicit tenant scoping.
- **server/seed.ts:4512** — UPDATE heartbeat_tasks SET cron_expression = ... WHERE name = 'Self-Reflection' ... is executed without a tenant_id constraint, affecting all tenants' tasks.
  - _Fix:_ Restrict the UPDATE with AND tenant_id = <tenantId> (or run per-tenant) so only the intended tenant's heartbeat_tasks are modified.
- **server/seed.ts:4516** — UPDATE heartbeat_tasks SET cron_expression = ... WHERE name = 'Memory Consolidation' ... has no tenant filter and will change tasks across all tenants.
  - _Fix:_ Include tenant_id in WHERE clause (e.g. AND tenant_id = ${tenantId}) or process tenants individually to preserve isolation.
- **server/seed.ts:4521** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant constraint. The subsequent UPDATE disabling these tasks (lines 4528-4531) also omits tenant scoping, disabling tasks for all tenants.
  - _Fix:_ Constrain both SELECT and UPDATE by tenant_id, or iterate tenants and operate only on their rows after verifying tenant context.
- **server/seed.ts:4535** — SELECT ... FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ...) is executed without tenant scoping. The later UPDATE that disables matching tasks (lines 4551-4563) likewise lacks tenant_id, impacting all tenants' heartbeat_tasks.
  - _Fix:_ Add tenant_id = <tenantId> to the WHERE clauses for both SELECT and UPDATE, or perform the logic per-tenant after ownership verification.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` can be executed without constraining by tenant_id (conditions may contain only category). This allows cross-tenant reads when tenantId is omitted.
  - _Fix:_ Always include tenant constraint in the WHERE clause (e.g. AND tenant_id = <caller tenant>) or refuse to run the query when tenantId is not supplied. Avoid building a WHERE that can be a single-category predicate without tenant scoping.
- **server/self-improvement.ts:516** — getExperimentHistory falls back to returning db.select().from(experiments)... when no tenantId is provided — an unscoped SELECT on tenant-scoped table `experiments` which can leak other tenants' data.
  - _Fix:_ Require tenantId (fail-closed) or always add a tenant_id = ${tenantId} filter. Do not perform global selects on tenant-scoped tables.
- **server/skill-seeker.ts:754** — listGaps() branch executes SELECT * FROM capability_gaps WHERE status = ${status} ... without tenant_id filter when caller provided a status but no tenantId. This is an unscoped read of a tenant-scoped table.
  - _Fix:_ Require tenant context or add tenant_id = ${tenantId} to the WHERE. If the intent is an admin/global view, make that explicit and gated by admin privileges; otherwise fail-closed.
- **server/skill-seeker.ts:764** — listGaps() final else branch executes SELECT * FROM capability_gaps ORDER BY ... LIMIT ... with no tenant_id filter (unscoped read of tenant-scoped table).
  - _Fix:_ Do not allow an unscoped select; require tenantId or explicitly gate and document global queries with strict admin checks. Add WHERE tenant_id = ${tenantId} for normal callers.
- **server/skillify.ts:195** — Query selects from `conversations` by id without scoping the SQL to the caller's tenant (no WHERE tenant_id = <tenantId>). The code relies on a post-query JS check (conv[0].tenantId !== tenantId) instead of enforcing tenant isolation in SQL.
  - _Fix:_ Add an explicit tenant WHERE clause to the DB query (e.g. .where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))) or use the tenant-scope helpers / assertConversationInTenant before querying) so the DB never returns rows from other tenants.
- **server/skillify.ts:203** — Query selects messages by conversation_id without including an explicit tenant_id constraint in the SQL (WHERE tenant_id = <tenantId>). Reads against tenant-scoped tables must be constrained in SQL.
  - _Fix:_ Include tenant scoping in the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise ensure the DB WHERE includes the tenant equality using tenantScope/requireTenantScope.
- **server/skillify.ts:223** — Selecting a persona by id (personas.id = conv[0].personaId) without any tenant_id constraint. This can return a persona from a different tenant because the SQL is not tenant-scoped.
  - _Fix:_ Scope the persona SELECT to the tenant (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))) or call a helper that validates persona ownership before reading it.
- **server/skillify.ts:91** — distillIntent reads recent messages (SELECT FROM messages WHERE conversation_id = ...) without an explicit tenant_id WHERE constraint. Even though the conversation is validated elsewhere, every read against tenant-scoped tables must include the tenant filter in SQL.
  - _Fix:_ Add the tenant condition to the query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or use tenantScope helpers to build the predicate.
- **server/skillify.ts:331** — skillifyConversation reads recent messages (SELECT FROM messages WHERE conversation_id = ...) without including tenant_id in the SQL WHERE clause.
  - _Fix:_ Add an explicit tenant equality to the messages query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or use the project's tenant-assert helpers before reading messages.
- **server/stability-watchdog.ts:177** — Query aggregates error counts from `heartbeat_logs` (SELECT ... FROM heartbeat_logs WHERE status = 'error' AND created_at > ...) with NO tenant_id constraint — this will aggregate logs across all tenants and may cause cross-tenant actions (e.g. disabling tasks) based on other tenants' logs.
  - _Fix:_ Scope the SELECT to a tenant (e.g. add AND tenant_id = ${tenantId}) or explicitly iterate per-tenant, passing the tenant into the query. If the watchdog must run cross-tenant, do so via a controlled admin path that intentionally and safely handles multi-tenant data and documents that behavior.
- **server/stability-watchdog.ts:225** — DELETE FROM `event_log` WHERE ... executes without a tenant_id constraint, removing old event_log rows across all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. WHERE tenant_id = ${tenantId} AND ...) or perform per-tenant pruning. If event_log is truly global, document that explicitly; otherwise include tenant_id in the WHERE to enforce isolation.
- **server/stability-watchdog.ts:259** — pruneOldLogs runs DELETE FROM `heartbeat_logs` WHERE created_at < ... with NO tenant_id constraint (and uses sql.raw for the interval). This will prune heartbeat logs across all tenants rather than per-tenant.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. WHERE tenant_id = ${tenantId} AND created_at < ...) or iterate tenants and prune logs per-tenant. Avoid unscoped deletes on tenant-scoped tables; validate intent and use the tenantScope/assert helpers.
- **server/storage.ts:236** — getConversationUnscoped performs SELECT FROM conversations without any tenant_id constraint (unscoped read).
  - _Fix:_ Remove or tightly restrict use of unscoped reads. If an unscoped fetch is absolutely required, ensure the caller is an explicit, well-audited admin context and add structured auditing + a required caller-provided tenant check before returning any data. Prefer adding a tenantId parameter and enforcing WHERE tenant_id = tenantId.
- **server/storage.ts:245** — createConversation inserts into conversations with an optional tenantId and does not validate/require tenantId before INSERT. INSERTs into tenant-scoped tables must include and validate tenant_id.
  - _Fix:_ Require tenantId (assertValidTenantId) on the API surface and validate it here before inserting; always pass tenantId explicitly in the .values() call and error otherwise.
- **server/storage.ts:286** — attachCitationMetadata executes raw SQL SELECT FROM messages WHERE id IN (...) without scoping by tenant_id. messages is tenant-scoped — unscoped reads can leak cross-tenant data.
  - _Fix:_ Include tenant filtering (e.g. AND tenant_id = ${tenantId}) or only query for ids that belong to a previously-validated tenant. Prefer passing tenantId into attachCitationMetadata and add WHERE messages.tenant_id = tenantId.
- **server/storage.ts:386** — attachCitations executes raw UPDATE messages SET citations = ... WHERE id = ${messageId} without any tenant_id constraint — this can modify messages across tenants.
  - _Fix:_ Require and validate the message's tenant ownership before updating (fetch message.tenant_id and compare to caller tenantId), or accept a validated tenantId parameter and add WHERE tenant_id = tenantId to the UPDATE.
- **server/storage.ts:513** — createMemoryEntry inserts into memory_entries without validating that tenantId is present/valid. INSERTs into tenant-scoped tables must set tenant_id explicitly and be validated.
  - _Fix:_ Require tenantId on the InsertMemoryEntry (assertValidTenantId) and reject/normalize requests without a valid tenantId; ensure the tenantId is included in .values() for the INSERT.
- **server/storage.ts:576** — createConversationFact inserts into conversation_facts without validating or requiring tenantId. conversation_facts is tenant-scoped and INSERT must set tenant_id explicitly.
  - _Fix:_ Require/validate tenantId (assertValidTenantId) before INSERT and include it explicitly in the values(). If conversationId is passed, validate ownership by reading the conversation's tenant_id first and comparing it to caller tenantId.
- **server/storage.ts:635** — upsertDailyNote may INSERT into daily_notes using data without validating tenantId. daily_notes is tenant-scoped and INSERT must include tenant_id.
  - _Fix:_ Require/validate tenantId (assertValidTenantId) before inserting. Consider making tenantId required on the API and include WHERE tenant_id = tenantId for update paths as defense-in-depth.
- **server/storage.ts:771** — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} with no tenant_id constraint — writes to tenant-scoped rows are unscoped.
  - _Fix:_ Require a tenantId parameter or otherwise validate the row's tenant ownership before updating. Use .where(and(eq(memory_entries.id, id), eq(memory_entries.tenant_id, tenantId))).
- **server/storage.ts:783** — updateKnowledgeEmbedding updates agent_knowledge SET embedding WHERE id = ${id} without tenant scoping — tenant-scoped table modified by id alone.
  - _Fix:_ Require and validate tenantId (or fetch and check the row's tenant_id first) and include tenant filter in the UPDATE (eq(agent_knowledge.tenant_id, tenantId)).
- **server/storage.ts:807** — archiveExpiredMemories executes a raw UPDATE on memory_entries across all rows (WHERE status = 'active' AND expires_at <= NOW()) with no tenant filter, potentially affecting all tenants.
  - _Fix:_ Restrict archival to a specific tenant (pass tenantId and add AND tenant_id = ${tenantId}), or run per-tenant. If this is intended to be global, document and gate it to an admin-only path and clearly assert that behavior.
- **server/storage.ts:823** — archiveStaleMemories executes a raw UPDATE on memory_entries using created_at/last_accessed criteria with no tenant filter — modifies rows across tenants.
  - _Fix:_ Scope this operation by tenant (e.g. accept tenantId and add AND tenant_id = ${tenantId}) or perform per-tenant runs. If global behavior is deliberate, only allow it under an audited admin context.
- **server/storage.ts:903** — getHeartbeatTask selects heartbeat_tasks WHERE id = ${id} with no tenant_id constraint. heartbeat_tasks is tenant-scoped and this read is unscoped.
  - _Fix:_ Require tenantId in the function signature and add WHERE tenant_id = tenantId, or fetch the row and validate ownership before returning.
- **server/storage.ts:923** — updateHeartbeatTask will UPDATE heartbeat_tasks without a tenant filter when tenantId is not provided — the fallback path updates tasks by id only, allowing cross-tenant mutation.
  - _Fix:_ Make tenantId required (assertValidTenantId) or always include tenant scoping in the WHERE clause. Avoid the current subselect pattern and use eq(heartbeat_tasks.tenantId, tenantId) for clarity and correctness.
- **server/storage.ts:933** — deleteHeartbeatTask deletes from heartbeat_tasks WHERE id = ${id} with no tenant filter when tenantId is omitted — can delete another tenant's task.
  - _Fix:_ Require tenantId and include tenant condition in the WHERE (eq(heartbeat_tasks.tenantId, tenantId)), or otherwise enforce admin-only semantics explicitly.
- **server/storage.ts:939** — fixStaleBackupSchedules executes a raw UPDATE on heartbeat_tasks (matching types and next_run_at) with no tenant_id filter — affects tasks for all tenants.
  - _Fix:_ If intended to operate per-tenant, iterate per-tenant or accept a tenantId parameter; otherwise restrict to an admin-only job and make that explicit and audited. Prefer adding tenant scoping to the WHERE clause.
- **server/storage.ts:955** — getDueHeartbeatTasks selects due heartbeat_tasks (enabled/next_run_at/approved) across all tenants with no tenant filter.
  - _Fix:_ If the worker should claim tasks across tenants, ensure the caller is an authorized system worker; otherwise require tenant scoping in queries or partition work per-tenant. Add tenant constraints or an explicit admin/system guard.
- **server/storage.ts:970** — claimHeartbeatTasks updates heartbeat_tasks SET last_run_at/next_run_at WHERE id = ${id} ... without tenant_id in the WHERE clause, allowing claims/updates across tenants.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or require the caller to pass and validate tenantId. Ensure claim operations are authenticated as system-level if intended to be cross-tenant.
- **server/storage.ts:985** — markHeartbeatTaskRun updates heartbeat_tasks WHERE id = ${id} without tenant scoping — a write to a tenant-scoped table is unscoped.
  - _Fix:_ Require tenantId and include eq(heartbeat_tasks.tenantId, tenantId) in the WHERE clause or validate ownership before updating.
- **server/subagents.ts:248** — spawnSubagent calls storage.getConversation(parentConversationId, params.tenantId ?? ADMIN_TENANT_ID) — if caller omits tenantId the code falls back to ADMIN_TENANT_ID and reads the conversation without verifying the caller owns it. This allows unverified reads of conversations.
  - _Fix:_ Require the caller's tenantId and validate it. Do not default to ADMIN_TENANT_ID for ownership checks. Fetch the conversation and assert its tenant_id equals the caller's tenantId (or explicitly assert admin rights) before proceeding.
- **server/subagents.ts:753** — announceResult creates a message in the parent conversation using tenantId: run.tenantId ?? 1 (falls back to admin tenant). If run.tenantId is missing/undefined this may write under admin tenant or the wrong tenant.
  - _Fix:_ Ensure run.tenantId is always present and validated. Do not default to tenant 1; require the run to carry a validated tenantId and assert it before calling createMessage. Add checks to createMessage or upstream to reject undefined tenant contexts.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding issues UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL with no tenant_id constraint, modifying rows across tenants.
  - _Fix:_ Accept/require tenantId and include AND tenant_id = ${tenantId} in the UPDATE, or re-fetch the row using tenant scoping first and validate ownership prior to updating.
- **server/surprise-scorer.ts:141** — scoreProposalSurprise updates felix_proposals SET actual_outcome_embedding WHERE id = ${proposalId} with no tenant_id filter — write to tenant-scoped table is unscoped.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or validate ownership before performing the UPDATE.
- **server/surprise-scorer.ts:212** — scoreProposalSurprise updates felix_proposals SET surprise_score, surprise_band WHERE id = ${proposalId} without tenant scoping — writes are unscoped and can affect other tenants' rows.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause or validate the row's tenant ownership prior to writing.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (checks existence by name but omits tenant filter). This can leak/affect other tenants or produce incorrect behavior.
  - _Fix:_ Include the caller's tenant in the WHERE clause, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Ensure all SELECTs against tenant-scoped tables filter by tenantId.
- **server/tool-learning.ts:350** — UPDATE to increment usageCount on tenant-scoped table `custom_tools` uses only id in WHERE and lacks tenant_id constraint. Writes must be constrained to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Consider combining selection+update or using tenant-aware returned ids to prevent accidental cross-tenant updates.
- **server/tools.ts:3004** — SELECT COUNT(*) from tenant-scoped messages table executed without any tenant_id constraint (db.select(...).from(messagesTable) has no WHERE tenant_id = ...). This returns global message counts across all tenants and violates the tenant-isolation invariant.
  - _Fix:_ Restrict the query to the caller's tenant. Pass the caller tenantId into the query and add a WHERE filter (or use the storage helper that enforces tenant scope). For example, use db.select(...).from(messagesTable).where(eq(messagesTable.tenant_id, tenantId)) or an equivalent parameterized sql`SELECT count(*) FROM messages WHERE tenant_id = ${tenantId}` so only the calling tenant's rows are counted.
- **server/tools.ts:4395** — SELECT from tenant-scoped table memory_entries without constraining tenant_id. The query `SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ...` lacks any tenant_id filter and can leak other tenants' memory entries.
  - _Fix:_ Scope the read to the caller's tenant. Add an explicit tenant_id constraint (e.g. WHERE tenant_id = ${params._tenantId} AND ...) or use Drizzle's .where(eq(memoryEntries.tenantId, tenantId)) so only the requesting tenant's rows are returned.
- **server/tools.ts:4719** — INSERT into project_notes omits tenant_id. The INSERT `INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...)` does not set tenant_id, but project_notes is tenant-scoped (other code stores tenant_id on related tables).
  - _Fix:_ Always include tenant_id in INSERTs to tenant-scoped tables. e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ... )`. Keep the existing assertProjectInTenant check, but also persist tenant_id explicitly.
- **server/tools.ts:4723** — INSERT into project_files omits tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...)` does not set tenant_id for a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ...)`). Verify ownership (assertProjectInTenant) and persist tenant_id atomically to avoid cross-tenant contamination.
- **server/tools.ts:6659** — INSERT into project_files omits tenant_id (in generate_audio). The code inserts uploaded audio metadata `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` without tenant_id even though the project_id came from caller input.
  - _Fix:_ Before inserting, ensure project ownership (assertProjectInTenant is used here) and include tenant_id in the INSERT, e.g. `INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...)`. Use parameterized Drizzle APIs or .where(eq(...)) and avoid relying on ownership checks alone — always persist tenant_id explicitly.
- **server/tools.ts:7193** — INSERT into tenant-scoped table `project_files` does not include tenant_id column — tenant isolation relies on application-level SQL filters and INSERTs must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT (and prefer Drizzle's insert API). Example: await db.execute(sql`INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${outputFilename + ".mp4"}, ${outPath}, ${driveUrl || null}, ${"video"}, ${stats.size}, ${"system"})`); Also keep the assertProjectInTenant check (ownership) but still explicitly persist tenant_id to ensure the row is scoped to the tenant.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE on tenant-scoped table 'knowledge_triples' omits tenant_id constraint in WHERE clause (updates by id only). This allows cross-tenant row mutation if an id from another tenant is referenced or if concurrency/race changes ownership between the SELECT and UPDATE.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: await db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId} AND valid_until IS NULL`); — or use Drizzle's .where(eq(table.tenantId, tenantId)) to ensure tenant scoping and include any existing predicate (e.g. valid_until IS NULL) to avoid accidental cross-tenant or stale updates.
- **server/tools/domains/security/handlers.ts:163** — Query on provider_keys executed without tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...) — reads a likely tenant-scoped table across all tenants.
  - _Fix:_ Constrain queries on tenant-scoped tables to the caller's tenant. Add WHERE tenant_id = ${tenantId} (or equivalent parameterized filter) or otherwise ensure the query is intentionally global and properly authorized/isolated. If this is an admin-only inspection, document and gate it explicitly and/or restrict returned columns to avoid exposure of other tenants' secrets.
- **server/tools/domains/security/handlers.ts:260** — Query on provider_keys executed without tenant_id constraint (SELECT COUNT(DISTINCT provider) ... FROM provider_keys ...) — aggregates across provider_keys with no tenant filter.
  - _Fix:_ Apply an explicit tenant filter (WHERE tenant_id = ${tenantId}) when reading tenant-scoped tables, or ensure the operation is intentionally global and performed only after an explicit admin authorization check. Avoid returning or aggregating sensitive per-tenant key material across tenants without strong, auditable admin controls.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table `memory_entries` omits tenant_id constraint (WHERE id = ${id} only). This allows updating a row by id without ensuring it belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller tenant as well. e.g. WHERE id = ${id} AND tenant_id = ${tid} (or use Drizzle .where(eq(memoryEntries.tenantId, tid))).
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE on `memory_entries` (UPDATE ... WHERE id = ANY(${ids})) lacks a tenant_id constraint. ids are derived from a prior SELECT, but the UPDATE must still enforce tenant scoping to prevent cross-tenant modifications.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids as any}) AND tenant_id = ${tid}, or use Drizzle .where(and(eq(memoryEntries.tenantId, tid), in(memoryEntries.id, ids))).
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against `memory_entries` omits tenant_id constraint (WHERE id = ${id} only). This can allow updating arbitrary rows by id across tenants.
  - _Fix:_ Require tenant scoping on the UPDATE: WHERE id = ${id} AND tenant_id = ${tid} (or equivalent Drizzle .where(eq(table.tenantId, tid))).
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger omits tenant_id constraint (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}). This allows updating a ledger row by id without verifying the row belongs to the caller's tenant.
  - _Fix:_ Pass the tenantId into settleVentureReservation (or look up the reservation row first) and include a tenant_id check in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or verify ownership before performing the UPDATE.
- **server/venture-discovery/budget.ts:171** — UPDATE against tenant-scoped table agent_cost_ledger in releaseVentureReservation omits tenant_id constraint (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}). This updates rows by id without verifying tenant ownership.
  - _Fix:_ Require tenantId (or resolve it from the reservation row) and constrain the UPDATE to that tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or validate the reservation belongs to the tenant before releasing it.
- **server/video-job-runner.ts:204** — SELECT reading cancelRequested from videoJobs uses only jobId (WHERE jobId = ${jobId}) with no tenant_id constraint. Reads against a tenant-scoped table must be scoped by tenant to avoid cross-tenant access.
  - _Fix:_ Require/accept tenantId and include it in the WHERE clause (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}) or fetch the row by jobId and verify the tenant_id matches the expected tenant before returning the cancel flag.
- **server/webhook-triggers.ts:60** — SELECT in listTriggers LEFT JOINs personas (LEFT JOIN personas p ON p.id = w.persona_id) without constraining the personas read by tenant. That join can return persona rows from other tenants because no persona.tenant_id constraint is applied.
  - _Fix:_ When joining tenant-scoped tables like personas, add a tenant filter (e.g. AND p.tenant_id = w.tenant_id or otherwise resolve and verify the persona belongs to the expected tenant) or fetch persona information via a tenant-aware storage API that validates ownership.
- **server/webhook-triggers.ts:122** — processTriggerEvent SELECT joins webhook_triggers to personas without any tenant constraint on personas (WHERE w.webhook_key = ${webhookKey}). An attacker-provided webhookKey could cause the code to read persona rows across tenants (or use persona_id from the trigger without verifying ownership).
  - _Fix:_ Do not read tenant-scoped persona rows without validating tenant ownership. Either include an explicit tenant constraint in the JOIN (e.g. JOIN personas p ON p.id = w.persona_id AND p.tenant_id = <expectedTenant>) or resolve persona via a tenant-aware API and validate that the persona belongs to the intended tenant/context before using it.

### MEDIUM (43)

- **server/agent-activity.ts:60** — SELECT from `personas` by persona id lacks any tenant constraint. The function getPersonality(personaId) reads persona rows without verifying tenant ownership.
  - _Fix:_ Change the API to accept tenantId (or otherwise obtain tenant context) and include a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or use Drizzle eq(personas.tenantId, tenantId). Ensure cached entries are keyed by tenant+persona to avoid cross-tenant leakage.
- **server/agent-channels.ts:198** — Query joins channel_subscriptions (alias cs) but does not constrain cs.tenant_id to the caller tenant. The unread-count SELECT can therefore match subscription rows from other tenants.
  - _Fix:_ Add tenant scoping to the join (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}), or otherwise ensure all joined tables are filtered by the caller's tenant_id.
- **server/agent-desk.ts:297** — SELECT id,name FROM `personas` is executed with no tenant filter when building the desks overview. This enumerates personas globally rather than tenant-scoped ones.
  - _Fix:_ Restrict the personas query to the caller tenant (e.g. WHERE tenant_id = ${tenantId}) or pass tenantId into the query / use Drizzle eq(personas.tenantId, tenantId).
- **server/agentic-engines.ts:342** — Query in gatherOperationalData selects from `heartbeat_logs` without a tenant_id constraint (FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads heartbeat logs across tenants.
  - _Fix:_ Include tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND task_name != ...) or restrict these metrics to the caller's tenant. If a global metric is intended, require explicit admin privileges and document it.
- **server/agentic-engines.ts:374** — SELECT name, role FROM `personas` WHERE is_active = true LIMIT 14 runs with no tenant constraint. This can expose personas from other tenants.
  - _Fix:_ Filter personas by tenant (WHERE tenant_id = ${tenantId}) or fetch only tenant-scoped personas; add tenantId param if needed.
- **server/auto-memorize.ts:47** — SELECT FROM agent_knowledge to read the auto_memorize watermark does not constrain tenant_id — it may return a watermark row belonging to another tenant.
  - _Fix:_ When watermark is meant to be global/admin, explicitly add WHERE tenant_id = ${ADMIN_TENANT_ID} to reads. If it is per-tenant, add WHERE tenant_id = ${tenantId}. Avoid relying on implicit latest-row semantics across tenants.
- **server/auto-memorize.ts:62** — UPDATE/INSERT path for the watermark does an initial SELECT (to detect existing) without filtering by tenant_id, but later INSERT hardcodes tenant_id = 1. The initial read may match a row from a different tenant.
  - _Fix:_ Perform the SELECT/UPDATE with an explicit tenant_id filter (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND category = ... AND title = ...). Make the tenant scope consistent between read and write.
- **server/auto-asset-capture.ts:184** — Backfill query SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... has NO tenant WHERE filter and scans conversations across all tenants.
  - _Fix:_ If backfill is intended to operate across all tenants, require an explicit admin-only invocation. Otherwise restrict the scan to a single tenant (WHERE c.tenant_id = ${tenantId}) or iterate tenants explicitly so each read is constrained to the tenant being processed. Ensure any downstream writes are done only after explicit ownership checks (assertConversationInTenant is used later — prefer verifying before the initial scan or include tenant filter in the query).
- **server/auto-transcript.ts:179** — INSERT INTO project_conversations is performed using project_id and conversation_id from prior queries without explicitly enforcing tenant ownership at the point of insert. If project_conversations is not tenant-scoped, this can create cross-tenant links; if it is tenant-scoped, the insert lacks tenant_id.
  - _Fix:_ Ensure the insert is only performed for matching tenant rows. Options: (a) add tenant_id column to project_conversations and set it on insert, (b) insert via INSERT ... SELECT ... FROM projects p JOIN conversations c WHERE p.id = ${r.project_id} AND c.id = ${r.id} AND p.tenant_id = c.tenant_id AND p.tenant_id = ${r.tenant_id}, or (c) add an explicit prior verification that the project and conversation belong to the same tenant before inserting.
- **server/capability-review.ts:490** — UPDATE capability_reviews sets reused = true WHERE id = ${pending.reviewId} AND reused = false but does not include a tenant_id constraint. capability_reviews is tenant-scoped, so this UPDATE can modify another tenant's row by id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${pending.reviewId} AND tenant_id = ${expectedTenantId} AND reused = false). If the tenant id is not available in this function, persist it into the pending entry when recording the review (pending.reviewTenantId) or re-query and verify the row belongs to the caller's tenant before updating.
- **server/compaction.ts:463** — INSERT into `memory_entries` writes tenant_id using the function parameter `tenantId` which is optional and not validated. This can result in NULL/incorrect tenant_id or implicit cross-tenant writes.
  - _Fix:_ Require and validate tenantId (must be integer > 0) before inserting. If the tenant must be derived, resolve it from a trusted source (conversation → tenant mapping) and assert ownership before inserting. Fail closed if tenant is missing or invalid.
- **server/credential-vault.ts:169** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} — missing tenant_id constraint on an UPDATE to a tenant-scoped table.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or otherwise ensure the row identity and ownership are validated in the same statement. Avoid updating tenant-scoped rows by id alone without constraining tenant_id.
- **server/credential-vault.ts:176** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} — missing tenant_id constraint on an UPDATE after looking up credentials.
  - _Fix:_ Use a tenant-scoped UPDATE (WHERE id = ${cred.id} AND tenant_id = ${tenantId}) or pass the tenantId into the UPDATE. Even if you previously fetched the row, include tenant_id in the UPDATE to enforce application-level multi-tenant isolation and prevent accidental cross-tenant writes.
- **server/crews-engine.ts:883** — addFlowStep inserts params.crewId into flow_steps without validating that the referenced crew belongs to params.tenantId. A foreign-id (crew_id) from caller input is written without verifying ownership.
  - _Fix:_ Before inserting, verify the crew belongs to the caller's tenant (e.g. call getCrew(params.crewId, params.tenantId) and null/throw if not found). Alternatively, NULL-out or reject cross-tenant crew_id values. Ensure every stored foreign id is either validated or constrained by AND tenant_id = <caller tenant> on subsequent lookups.
- **server/data-protection.ts:145** — verifyMessageSaved queries messages by conversation_id and role (SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} ...) but the function has no tenantId parameter and the SQL has no tenant_id constraint. This reads a tenant-scoped table without explicitly constraining by tenant.
  - _Fix:_ Add a tenantId parameter to verifyMessageSaved and include AND tenant_id = ${tenantId} in the WHERE clause. If conversation_id is globally unique and you keep the current API, explicitly document that assumption and still prefer to enforce tenant_id at the SQL level for defense-in-depth.
- **server/event-bus.ts:161** — Query JOINs personas p ON p.id = es.persona_id but does not constrain personas by tenant. Reading persona_name from the personas table without scoping p.tenant_id can leak cross-tenant persona data.
  - _Fix:_ Scope the join to the tenant, e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = ${tenantId}, or add AND p.tenant_id = ${tenantId} to the WHERE clause, so every tenant-scoped table read includes the tenant filter.
- **server/event-bus.ts:175** — Wildcard subscriber lookup joins personas p but does not constrain p.tenant_id. The fallback query may return persona rows from other tenants.
  - _Fix:_ Add a tenant constraint for the personas table (e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = ${tenantId}) or add p.tenant_id = ${tenantId} to the WHERE clause to enforce tenant scoping.
- **server/event-bus.ts:283** — getEventSubscriptions SELECT joins personas p without filtering p.tenant_id. Returning persona_name from personas is a read of a tenant-scoped table with no tenant constraint.
  - _Fix:_ Constrain the personas table to the same tenant (e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = ${tenantId}) or add p.tenant_id = ${tenantId} to the WHERE clause so the query is fully tenant-scoped.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks is executed without any tenant constraint. If heartbeat_tasks is a tenant-scoped table this will enumerate tasks across tenants.
  - _Fix:_ If heartbeat_tasks is tenant-scoped, add tenant filtering (e.g. WHERE tenant_id = ${tenantId}) or accept/derive a tenantId for this scanner. If the table is intentionally global, document that fact and keep it excluded from tenant-scoping checks.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) performs a write without tenant scoping. This may disable tasks across tenants if the table is tenant-scoped.
  - _Fix:_ Restrict the UPDATE to the appropriate tenant (e.g. add AND tenant_id = ${tenantId}), or verify ids are filtered to only include rows belonging to the intended tenant before performing the UPDATE. If heartbeat_tasks is global by design, document and justify that global privilege.
- **server/heartbeat.ts:2901** — processDelegations() and other code call existingTasks = await storage.getHeartbeatTasks() without passing tenantId, then use that to enforce caps / duplicate detection globally. This can cause delegation cap/duplicate checks to be applied cross-tenant and may allow a task in tenant A to collide with a task in tenant B.
  - _Fix:_ Use tenant-scoped fetches (e.g. storage.getHeartbeatTasks(undefined, tenantId)) or otherwise filter results by tenantId before enforcing per-tenant limits or duplicate detection. All quota and uniqueness checks that are per-tenant must operate on tenant-scoped data.
- **server/lib/bwb-job-progress.ts:144** — UPDATE on video_jobs uses .where(scope(jobId, ...)) where scope may omit tenant_id when BWB_TENANT_ID is not set — writes can thus cross tenants (scope falls back to jobId-only and only warns).
  - _Fix:_ Always enforce tenant scoping at runtime: require an explicit tenantId parameter (and fail/throw if missing) or make scope() require/derive a validated tenantId and fail-open only after explicit operator decision. Change .where(scope(...)) to include an explicit eq(videoJobs.tenantId, tenantId) predicate derived from a trusted caller context.
- **server/lib/bwb-job-progress.ts:169** — UPDATE on video_jobs (.where(scope(jobId,...))) may run without a tenant_id predicate when env BWB_TENANT_ID is missing — risk of cross-tenant modification.
  - _Fix:_ Require and pass a verified tenantId into updateBwbChapters (or make scope throw if no tenantId available). Ensure the final .where includes eq(video_jobs.tenant_id, tenantId) so updates are always tenant-scoped.
- **server/lib/bwb-job-progress.ts:203** — SELECT from video_jobs uses .where(scope(jobId)) where scope can omit tenant_id (relies on BWB_TENANT_ID env). This read can return rows from other tenants if the tenant env/threading is missing.
  - _Fix:_ Use an explicit tenant predicate in the SELECT (e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId)))) and require tenantId be supplied by callers or validated from a trusted env before performing the query. Fail fast if tenant context is unavailable.
- **server/lib/bwb-job-progress.ts:211** — UPDATE of video_jobs (completeBwbJob) uses .where(scope(jobId)) without guaranteeing a tenant_id predicate — scope may drop tenant filter if BWB_TENANT_ID isn't set, allowing cross-tenant updates.
  - _Fix:_ Ensure completeBwbJob has a validated tenantId and include eq(video_jobs.tenant_id, tenantId) in the WHERE. If relying on env-threading is intended, make the absence of BWB_TENANT_ID a hard error rather than a one-time warning so writes cannot cross tenants silently.
- **server/lib/bwb-job-progress.ts:230** — bumpBwbHeartbeat performs an UPDATE .where(scope(jobId, ...)) that may lack a tenant_id clause if environment threading (BWB_TENANT_ID) is missing — could bump another tenant's job row.
  - _Fix:_ Require an explicit tenantId parameter or assert current tenant context before performing the heartbeat. Always include tenant predicate in the WHERE (eq(video_jobs.tenant_id, tenantId)).
- **server/lib/bwb-job-progress.ts:254** — failBwbJob updates video_jobs with .where(scope(jobId, { notDone: true })) which can omit tenant_id when BWB_TENANT_ID is not set — a failed build could mark another tenant's job failed.
  - _Fix:_ Enforce tenant scoping by requiring and using a validated tenantId in the WHERE clause (add eq(video_jobs.tenant_id, tenantId)) or fail if tenant context is not available. Avoid relying solely on an untrusted or optional env threading variable for tenant isolation.
- **server/minerva-planner.ts:506** — SELECT * FROM plans WHERE id = ${planId} (when tenantId is omitted) executes a tenant-scoped read without tenant filtering — relies on caller being 'trusted' but the function is public and can be misused.
  - _Fix:_ Remove the implicit trusted-path overload or make it explicit and internal-only. Prefer requiring an explicit tenantId for all callers and always include tenant_id = ${tenantId} in the WHERE. If server-internal callers truly need unscoped access, use a dedicated internal API and clearly document/limit its usage.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table 'agent_knowledge' performed without any tenant_id constraint (global scan).
  - _Fix:_ If this global scan is intentional for the cron runner, make that explicit (e.g. require/verify an internal `_allowGlobalSystemUseOnly` guard) and document it. Otherwise restrict the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant so queries never read unfiltered tenant rows.
- **server/research-engine.ts:945** — SELECT * FROM research_sessions WHERE id = ${sessionId} (in getResearchSessionStatus) has no tenant_id filter; a caller could obtain another tenant's session row if they know an id.
  - _Fix:_ Require and enforce tenant context (e.g. SELECT ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or verify ownership before returning session details to callers.
- **server/research-engine.ts:1054** — INSERT INTO model_registry_updates ... VALUES (...) is missing an explicit tenant_id column/value; the table has a tenant_id column with a DEFAULT, so rows may be created under the wrong tenant (or default tenant).
  - _Fix:_ Always include tenant_id explicitly in INSERTs for tenant-scoped tables (VALUES (${session.tenantId}, ...)). Do not rely on defaults; ensure the inserted row is associated with the correct tenant.
- **server/routes.ts:161** — SELECT from tenant-scoped table `file_storage` without tenant constraint during startup restore (db.select(...).where(like(fileStorage.mimeType, "image/%"), isNull(fileStorage.storageKey))). This reads rows for all tenants unscoped.
  - _Fix:_ Scope the query to a specific tenant context or run the restore per-tenant under an explicit admin-only flow. Add AND eq(fileStorage.tenantId, <tenantId>) (or equivalent) or otherwise ensure the operation runs with an explicit, auditable platform-admin intent and does not write another tenant's data to a shared filesystem path.
- **server/routes.ts:769** — SELECT projects JOIN tenants (SELECT p.id, p.name, p.tenant_id, t.name as tenant_name FROM projects p JOIN tenants t ON t.id = p.tenant_id WHERE p.drive_folder_id IS NULL ...) reads all projects across tenants with no tenant_id constraint.
  - _Fix:_ If this backfill is intended to run platform-wide only by an admin process, enforce an explicit admin-only gate and document it. Otherwise restrict the query to operate for a single tenant at a time (WHERE p.tenant_id = ${tenantId} or equivalent) or iterate tenants in an admin-controlled loop so tenant isolation is explicit in code.
- **server/routes.ts:1602** — Public stats endpoint runs SELECT count(*) FROM tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filtering, returning aggregated counts across all tenants without explicit platform-admin authorization.
  - _Fix:_ If platform-wide aggregate metrics are intended, ensure the endpoint is admin-gated or the aggregation is performed in a dedicated global metrics pipeline (separate table or view). Otherwise, add tenant scoping (WHERE tenant_id = ${tenantId}) so only per-tenant stats are returned to authenticated callers.
- **server/routes.ts:1630** — Multiple db.execute calls in the public architecture endpoint query tenant-scoped tables (e.g. conversations, messages, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) without tenant filters, exposing cross-tenant aggregate counts.
  - _Fix:_ Restrict these queries to an admin-only endpoint or convert them to use platform-level metrics tables that are explicitly intended to be global. If per-tenant data is required, add WHERE tenant_id = ${tenantId} (or equivalent) and require authentication/authorization for tenants querying their own metrics.
- **server/seed.ts:3212** — INSERT into project_conversations omits tenant identification (INSERT INTO project_conversations (project_id, conversation_id) VALUES ...). If project_conversations is tenant-scoped, tenant_id is not being set on insert.
  - _Fix:_ If project_conversations is tenant-scoped, include tenant_id in the INSERT (VALUES (${tenantId}, ${projectId}, ${conversationId})) and/or derive/validate tenant ownership of the referenced project and conversation before inserting. Ensure every tenant-scoped INSERT sets tenant_id explicitly.
- **server/storage.ts:843** — getMemoryStats will SELECT all memory_entries (and agent_knowledge) when tenantId is undefined, returning cross-tenant aggregated data. Reads against tenant-scoped tables must be scoped to caller tenant.
  - _Fix:_ Require tenantId or assert caller admin privileges. If tenantId can be optional, ensure callers that omit it are admin-only and that this is clearly enforced by an explicit admin check (not implicit optional parameter). Prefer assertValidTenantId or fail-closed behavior.
- **server/surprise-scorer.ts:149** — Re-fetch of args_embedding uses SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} without tenant scoping; although the function earlier selected the row with tenant_id it should consistently include tenant_id on subsequent accesses to avoid TOCTOU issues.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT to prevent races and ensure the re-fetch is scoped to the same tenant context.
- **server/tool-learning.ts:155** — SELECT by id from tenant-scoped table `custom_tools` without constraining tenant_id. Reads should be scoped to the caller's tenant.
  - _Fix:_ Add tenant scoping to the selection, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Avoid relying solely on id for tenant authorization checks.
- **server/twilio.ts:87** — SELECT from `agent_knowledge` (messaging_pairing) does not include a tenant_id constraint. Reading tenant-scoped pairing records without scoping can expose pairings across tenants or leak tenant-specific data.
  - _Fix:_ Store/read pairings in a tenant-scoped manner and include tenant_id in the WHERE clause when querying agent_knowledge (e.g. WHERE tenant_id = ${expectedTenantId} AND category = 'messaging_pairing' AND title = ${key}). If the webhook has no caller tenant, restrict pairing lookup to records that are provably scoped (or use a dedicated tenant-aware pairing table) and always verify ownership of returned data before use.
- **server/webhook-triggers.ts:81** — createTrigger inserts the user-supplied personaId into webhook_triggers without validating that the referenced persona belongs to the correct tenant/admin context. This stores a foreign id that may reference a persona from another tenant.
  - _Fix:_ Validate the provided personaId before inserting (e.g. look up the persona and confirm its tenant_id matches the expected tenant or ADMIN_TENANT_ID), or disallow arbitrary personaId in the insert and require a tenant-validated selection.
- **server/wiring-invariants.ts:366** — SELECT COUNT/MAX query uses string-interpolated table/column names and scans tenant-scoped tables without a tenant_id filter (no WHERE tenant_id = ...). This performs unscoped reads against per-tenant data (rows + last_write) and uses sql.raw() with an interpolated identifier.
  - _Fix:_ If this probe is intended to run as a global/admin diagnostic, make that explicit and harden the call: avoid injecting identifiers via raw string interpolation (validate spec.table/column against an allowlist) and require an explicit admin context before running cross-tenant queries. If the intent is tenant-scoped, add WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(table.tenantId, tenantId))). Prefer parameterized APIs or validated identifier mapping instead of sql.raw interpolation.
- **server/wiring-invariants.ts:535** — Memory-growth probe performs SELECT COUNT/pg_total_relation_size FROM ${tbl.name} via sql.raw without scoping by tenant_id. Tables listed (e.g. messages, memory_entries, graph_memory) are likely tenant-scoped, so this is an unfiltered cross-tenant read.
  - _Fix:_ If the aggregation is intended to be global/admin-only, require and assert an explicit admin execution context and document that this is a deliberate cross-tenant check. If the probe should be per-tenant, add a tenant filter (WHERE tenant_id = ${tenantId}) or run the same probe per-tenant. Also avoid building the FROM clause with raw string interpolation; use a validated table-name allowlist or proper identifier-quoting to prevent accidental SQL injection or misuse.

### LOW (1)

- **server/heartbeat.ts:68** — SELECT id FROM research_programs WHERE tenant_id = ${sched.tenant_id} — noted for context: many adjacent queries act cross-tenant. (This one is OK because it DOES filter by tenant_id.)
  - _Fix:_ No change needed for this specific query; keep using explicit tenant_id filters. Use the same pattern for other direct SQL calls against tenant-scoped tables.
