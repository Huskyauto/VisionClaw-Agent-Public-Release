# Tenant-Isolation Audit Report

_Generated 2026-07-24T15:39:20.790Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **565/565** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **279** (CRITICAL 75 · HIGH 165 · MEDIUM 39 · LOW 0)
- Precision second-pass (`gpt-5.4`): **168** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (75)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without any tenant_id constraint (reads experiments/metadata across all tenants).
  - _Fix:_ Scope the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or make this function per-tenant (accept a tenantId and run the SELECT/processing for that tenant). Do NOT perform a global SELECT of tenant-scoped rows; if an operator-intended cross-tenant sweep is required, implement an explicit admin-only path that validates privileges and handles tenants one-by-one.
- **server/agentic/runs.ts:34** — appendStep() performs an UPDATE on the tenant-scoped agent_runs table using only the run id (WHERE id = runId) with no tenant_id constraint, allowing cross-tenant modifications if a runId from another tenant is supplied.
  - _Fix:_ Require or derive tenantId in appendStep and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or otherwise verify the caller owns the run before updating. Do not update tenant-scoped rows by id alone.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) but does not constrain by tenant_id. This allows an attacker or buggy code with a runId from another tenant to alter that tenant's state.
  - _Fix:_ Require tenantId (or load & verify the run's tenant) and add a tenant_id equality check to the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))).
- **server/agentic/runs.ts:54** — replaceRunState() replaces state on agent_runs using only the run id in the WHERE clause (no tenant_id filter), enabling cross-tenant state overwrite by runId.
  - _Fix:_ Add tenant scoping to the update: require tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or otherwise verify ownership before mutating.
- **server/agentic/runs.ts:58** — completeRun() updates an agent_runs row to mark completion using only the run id in the WHERE clause (no tenant_id constraint), allowing modification of runs across tenants by id.
  - _Fix:_ Add a tenant_id condition to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or otherwise ensure the caller's tenant owns the run prior to completing it. Propagate tenantId where needed.
- **server/agentic/runs.ts:88** — failRun() updates agent_runs to a failed state using only the run id in the WHERE clause (no tenant_id constraint), permitting cross-tenant updates if runId from another tenant is provided.
  - _Fix:_ Require tenantId or verify ownership and include tenant scoping in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))).
- **server/agentic/runs.ts:97** — pauseRun() sets status/state on agent_runs using only the run id in the WHERE clause (no tenant_id constraint), enabling cross-tenant pauses via runId alone.
  - _Fix:_ Add tenant scoping: require tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all updates to tenant-scoped tables verify tenant ownership.
- **server/agentic/resume-worker.ts:66** — The candidate SELECT (lines ~66–85) reads agent_runs JOIN agent_approvals without a tenant_id WHERE filter — it scans rows across tenants. Per the project's rule that tenant isolation is enforced by app-level SQL filters, unscoped SELECTs over tenant-scoped tables risk leaking cross-tenant data.
  - _Fix:_ Run per-tenant sweeps or otherwise include an explicit tenant_id filter in the query (e.g. WHERE r.tenant_id = ${tenantId}), or restrict this background job to an authorized admin/maintenance context and document that authority. If kept global, ensure callers are privileged and that any subsequent mutations re-verify tenant ownership before update. Additionally, prefer selecting only rows you will act on under an explicit tenant context rather than a global scan.
- **server/agentic/wake-scheduler.ts:142** — runDueWakes issues a cross-tenant UPDATE/SELECT on agent_wake_schedules without constraining by tenant_id. The inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) and the outer UPDATE do not restrict rows to a specific tenant, allowing claims/updates across tenants.
  - _Fix:_ Restrict the selection and updates to a single tenant. Either run claim/processing per-tenant, or include tenant_id = <tenantId> in both the inner SELECT and the outer UPDATE (and in the RETURNING). For example, SELECT id FROM agent_wake_schedules WHERE tenant_id = $tid AND status = 'pending' ... and UPDATE ... WHERE id IN (...) AND tenant_id = $tid. Avoid global unscoped UPDATEs that can move/claim other tenants' rows.
- **server/auto-memorize.ts:62** — setWatermark writes/updates agent_knowledge but uses no tenant scoping and inserts tenant_id = 1 (hard-coded). This creates/updates a global watermark row tied to the admin tenant, causing watermark writes to be attributed to the wrong tenant and allowing cross-tenant contamination.
  - _Fix:_ Do not hardcode tenant_id. Accept an explicit tenantId and INSERT/UPDATE agent_knowledge rows with tenant_id = ${tenantId}. Also SELECT/UPDATE by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND category = ... AND title = ...). Ensure watermark reads/updates are per-tenant.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table 'commitments' without tenant_id constraint — `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` can modify a row belonging to another tenant.
  - _Fix:_ Constrain the update to the owning tenant. Use WHERE id = ${row.id} AND tenant_id = ${row.tenantId} (and preserve draft_status guard if appropriate) so the revert only affects the intended tenant's row.
- **server/data-protection.ts:131** — permanentlyPurgeSoftDeleted performs destructive deletes without tenant scoping: DELETE FROM messages WHERE conversation_id = ${conv.id}; DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}; DELETE FROM conversations WHERE id = ${conv.id}. None of these DELETEs include a tenant_id predicate, allowing cross-tenant deletion if IDs collide or were enumerated.
  - _Fix:_ Never run DELETEs on tenant-scoped tables without constraining tenant_id. Change the DELETE statements to include AND tenant_id = ${conv.tenant_id} (or use the tenantId you fetched) and/or run the purge inside a tenant-aware transaction (withTenantTx) to ensure isolation. Consider fail-safe checks that confirm conv.tenant_id before any destructive operations.
- **server/embeddings.ts:684** — storeEmbeddingVec performs an UPDATE on a tenant-scoped table (memory_entries or agent_knowledge) using only id (and a validated table name) but does NOT constrain the UPDATE to the caller's tenant (no WHERE tenant_id = ...). This allows cross-tenant writes by id.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Additionally, validate that the id belongs to the tenant before updating (or perform the update via a parameterized ORM call that includes tenant scoping). Avoid single-key id-only updates on tenant-scoped tables.
- **server/embeddings.ts:726** — backfillEmbeddingVecs performs UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} without constraining the UPDATE by tenant_id — cross-tenant write by id is possible.
  - _Fix:_ Change the UPDATE to include tenant ownership (WHERE id = ${row.id} AND tenant_id = ${tenantId}) and/or perform the update only after verifying the row's tenant_id matches the intended tenant. Prefer parameterized/Drizzle updates that include tenant scoping instead of id-only updates.
- **server/embeddings.ts:743** — backfillEmbeddingVecs executes UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without a tenant_id constraint — this updates rows across tenants by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or verify ownership before updating. Ensure all writes to tenant-scoped tables include the tenant_id filter.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} runs without any tenant_id constraint or ownership verification of the referenced session. This allows writing to another tenant's session row if sessionId is attacker-controlled.
  - _Fix:_ Verify the session belongs to the expected tenant before updating (e.g. SELECT tenant_id FROM presenter_sessions WHERE id = ${sessionId} and confirm it matches the caller tenant), or include AND tenant_id = ${tenantId} in the UPDATE WHERE clause. If this operation must be admin-global, require explicit admin authorization and document it; otherwise always scope writes by tenant_id.
- **server/job-queue.ts:98** — claimDueJobs() atomically claims jobs via an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... LIMIT ${limit} FOR UPDATE SKIP LOCKED) with NO tenant_id filtering. A worker can therefore claim and run jobs for any tenant.
  - _Fix:_ Restrict claims to the intended tenant scope (e.g. include AND tenant_id = ${tenantId} in the inner SELECT) or implement an explicit per-tenant claim strategy. If the worker is a privileged system process that must operate across tenants, ensure additional checks are applied after claim to enforce tenant context before executing job handlers (and document admin-only privileges). Prefer explicit tenant-scoped queries to avoid accidental cross-tenant execution.
- **server/job-queue.ts:134** — reclaimExpiredLeases() issues UPDATEs against agent_jobs (marking terminal failed or requeued) with NO tenant_id constraint, affecting running/pending jobs across all tenants.
  - _Fix:_ Scope these UPDATE statements by tenant_id or otherwise ensure only intended tenant rows are transitioned. If this routine must operate across tenants as an operator action, require a clear operator/admin boundary and audit, and ensure it cannot be invoked in a per-tenant context without tenant restriction.
- **server/job-queue.ts:148** — Second step in reclaimExpiredLeases() re-queues expired 'running' rows back to pending via UPDATE agent_jobs ... WHERE status = 'running' AND lease_until < NOW() WITHOUT tenant filtering, possibly re-queuing other tenants' jobs incorrectly.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or accept an explicit tenantId parameter for per-tenant reclaim operations. If global reclaim is required, make invocation restricted to an operator context and add extra safeguards/auditing.
- **server/lib/scheduled-post-runner.ts:60** — CTE selects/updates tenant-scoped table `scheduled_posts` without any tenant_id constraint (SELECT id FROM scheduled_posts ... FOR UPDATE / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). This updates rows across tenants without binding the operation to an explicit tenant context.
  - _Fix:_ If this is intended as a system-wide admin worker, make the multi-tenant scope explicit and ensure every subsequent write/UPDATE includes the row's tenant (e.g. add AND tenant_id = ${row.tenant_id} to WHERE clauses). If the operation should only affect a single tenant, add WHERE tenant_id = ${tenantId} to the CTE and UPDATE. Always include tenant_id in UPDATE/DELETE predicates when modifying tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE on tenant-scoped table `scheduled_posts` omits tenant_id in the WHERE clause (UPDATE scheduled_posts ... WHERE id = ${row.id}). Writing tenant rows by id-only can accidentally modify another tenant's row.
  - _Fix:_ Include the tenant guard in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) or otherwise bind the update to the tenant returned when the row was selected. Always constrain writes to tenant_id to enforce application-level isolation.
- **server/lib/scheduled-post-runner.ts:191** — Error-path UPDATE on tenant-scoped table `scheduled_posts` omits tenant_id in the WHERE clause (UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing').
  - _Fix:_ Add tenant-scoped predicate to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} AND status = 'publishing') so the rollback/cleanup cannot affect rows belonging to other tenants.
- **server/memory-graph.ts:140** — UPDATE memory_categories sets memory_count from a subquery on memory_entries but the subquery and the outer UPDATE lack tenant_id filters. This can aggregate counts across tenants and modify a category row without confirming tenant ownership.
  - _Fix:_ Require tenant context for refreshCategoryCount (add a tenantId parameter) and include tenant_id = ${tenantId} both in the subquery (FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ...) and in the UPDATE WHERE clause (WHERE id = ${categoryId} AND tenant_id = ${tenantId}).
- **server/memory-graph.ts:209** — INSERT INTO memory_links does not include tenant_id (INSERT INTO memory_links (source_memory_id, target_memory_id, ... ) VALUES (...)). If memory_links is a tenant-scoped table, tenant_id must be set explicitly on every INSERT; otherwise links can cross tenants or be attributed incorrectly.
  - _Fix:_ Add tenant_id to the memory_links schema and include tenant_id: ${tenantId} in the INSERT values. Ensure every code path that writes memory_links passes the caller's tenantId and that the table has a NOT NULL tenant_id column (and index) to enforce per-tenant isolation at the application level.
- **server/memory-graph.ts:408** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is performed without constraining tenant_id. This can update a memory entry belonging to a different tenant if ids are misused or attacker supplies ids.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or use storage helpers that accept tenantId and enforce ownership. Audit all UPDATE/DELETE statements on memory_entries to ensure tenant_id is included.
- **server/memory-intelligence.ts:271** — db.execute(sql`UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}`) is executed without constraining tenant_id. Even though entry.id was just created, the UPDATE must still include tenant scoping to prevent cross-tenant modification if misused.
  - _Fix:_ Use the storage API that accepts tenantId to perform the update (e.g. storage.updateMemoryEntry(entry.id, { category_id: categoryId }, tenantId)) or include AND tenant_id = ${tenantId} in the WHERE clause. Ensure all direct db updates to memory_entries include tenant_id.
- **server/minerva-planner.ts:508** — SELECT from tenant-scoped table `plans` without tenant_id constraint when tenantId is omitted (SELECT * FROM plans WHERE id = ${planId}). This allows cross-tenant reads if this code is called in an untrusted context.
  - _Fix:_ Always constrain reads to the caller's tenant. Require caller to pass tenantId or assert trusted internal context before running a tenant-agnostic query. Replace the branch with a single parameterized query that includes AND tenant_id = ${tenantId}, or explicitly gate the omitted-tenant branch behind a well-audited internal-only API and assert caller trust before executing.
- **server/minerva-planner.ts:355** — UPDATE on tenant-scoped table `plans` performed without constraining by tenant_id (UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING ...). This can allow modifying another tenant's plan (cross-tenant write).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId} AND status = 'awaiting_approval'), or require and verify a caller-provided tenantId before executing. If this operation is intentionally global/internal, restrict access to a trusted internal-only call path and enforce that in code (assert trusted principal / use withTenantTx).
- **server/minerva-planner.ts:459** — Rollback UPDATE on tenant-scoped table `plans` executed without tenant_id constraint (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'). This can roll back or mutate another tenant's plan.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId} AND status = 'revising'), or perform this rollback within a verified internal/trusted transaction where the tenant is asserted. Prefer explicit tenant checks over implicit trust.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` with WHERE is_active = TRUE but no tenant_id constraint. This query even selects refresh_token (sensitive fields) across all tenants at startup (validateSubscriptionsOnStartup).
  - _Fix:_ Do NOT query tenant-scoped subscription rows across tenants. If the operation must enumerate subscriptions for administrative maintenance, restrict it to explicit admin-only code paths with careful authorization and logging, or scope the query to a single tenant_id (WHERE tenant_id = ${tenantId}). Avoid bulk reads of sensitive tokens across tenants; prefer per-tenant operations or use Postgres RLS (or equivalent) if admin-scoped global operations are required and audited.
- **server/plan-executor.ts:116** — UPDATE plans in appendExecutionLog does not constrain by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows modifying a plan row without verifying the caller/processor's tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}), or change appendExecutionLog signature to accept and use the plan's tenant_id (passed from the claimed plan row) so every update enforces tenant isolation.
- **server/plan-executor.ts:513** — Claiming the plan uses UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' with NO tenant_id constraint. This mutates a tenant-scoped table by id only, allowing cross-tenant claims if planId is attacker-controllable.
  - _Fix:_ Add tenant scoping to the CAS claim (e.g. AND tenant_id = ${callerTenantId}) or fetch the plan row with tenant verification before updating; ensure the executor runs updates always constrained to the plan's tenant_id.
- **server/plan-executor.ts:527** — UPDATE plans SET status = 'failed' ... WHERE id = ${planId} (no tenant_id filter). Writes to tenant-scoped table are not constrained to the plan's tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or use the plan row's tenant_id) in the WHERE clause for all plan updates to prevent cross-tenant modifications.
- **server/plan-executor.ts:725** — Final UPDATE of plans (setting status = completed/failed and appending execution_log) uses WHERE id = ${planId} with no tenant_id constraint, allowing cross-tenant state changes.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or compute/propagate the plan's tenant and use it in the WHERE clause when persisting final status.
- **server/plan-executor.ts:759** — Error-path UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' lacks a tenant_id constraint; an exception handler is mutating tenant-scoped rows by id only.
  - _Fix:_ Add tenant scoping to this rescue UPDATE (AND tenant_id = ${tenantId} or otherwise ensure the plan's tenant is asserted) to enforce tenant isolation on failure-path writes.
- **server/plan-executor.ts:775** — resumeStuckPlans executes UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id with NO tenant_id filter. This scans/mutates all tenants' plans in one statement.
  - _Fix:_ Either run recovery per-tenant (pass tenantId into resumeStuckPlans) or add tenant filtering to the UPDATE so only the intended tenant's rows are affected (e.g. WHERE tenant_id = ${tenantId} AND ...). Ensure boot/recovery respects tenant boundaries.
- **server/plan-executor.ts:785** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is executed with no tenant_id filter — the executor will enumerate approved plans across all tenants.
  - _Fix:_ Limit this query by tenant_id (or iterate tenants explicitly) so pending plan selection only returns rows belonging to the intended tenant context; avoid global SELECTs on tenant-scoped tables.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} executes with no tenant_id constraint. A caller supplying a projectId can read project metadata across tenants.
  - _Fix:_ Require and validate the caller's tenantId and include it in the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}), or verify ownership before reading project data to enforce tenant isolation.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... has NO tenant filtering and will enumerate projects across tenants.
  - _Fix:_ Scope backfill queries by tenant_id (e.g. WHERE p.tenant_id = ${tenantId}) or iterate tenants explicitly; never run cross-tenant SELECTs on tenant-scoped tables without explicit tenant isolation.
- **server/project-brain.ts:352** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed with no tenant filter when called from backfill logic, allowing reading conversations belonging to other tenants.
  - _Fix:_ Include tenant scoping (e.g. AND c.tenant_id = ${tenantId}) or fetch conversations by both project_id and tenant_id to ensure the conversation belongs to the expected tenant.
- **server/project-brain.ts:360** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant constraint (called from backfillProjectBrains / updateProjectBrain). This can expose messages across tenants.
  - _Fix:_ Add tenant filtering to the query (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or validate conversation ownership before reading messages.
- **server/proposal-verifier.ts:33** — SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId} has NO tenant_id constraint. A caller supplying proposalId can read proposals across tenants.
  - _Fix:_ Require and validate tenant context and include tenant_id in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or otherwise verify ownership before selecting proposal rows.
- **server/proposal-verifier.ts:241** — persist() updates code_proposals (UPDATE code_proposals SET verification_status=..., verified_at=NOW() WHERE id = ${proposalId}) without constraining by tenant_id, allowing cross-tenant mutation of proposals.
  - _Fix:_ Constrain all UPDATE/INSERT/DELETE on code_proposals with tenant_id (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), and propagate tenantId into persist() so the write is validated against the owning tenant.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift the updater that sets code_proposals status to 'rejected' and writes validation_result uses WHERE id = ${proposalId} with no tenant filter; likewise the later UPDATE that writes the rebased code_diff uses WHERE id = ${proposalId} without tenant_scoping.
  - _Fix:_ Include tenant_id = ${tenantId} in these UPDATE statements (or verify and pass the proposal's tenant before mutating) so rebase outcomes and proposal state changes only affect the owning tenant's rows.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` uses only WHERE id = ${r.id} and lacks a tenant_id constraint. This can allow cross-tenant modification if ids are misused or logic changes.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause. For example: UPDATE agent_knowledge SET ... WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}; or prefer using a tenant + id pair (AND persona_id if applicable) to enforce multi-tenant isolation.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses only WHERE id = ${storedId} and omits tenant_id (and persona_id) in the WHERE clause, allowing potential cross-tenant writes.
  - _Fix:_ Constrain the UPDATE by tenant (and persona) in the WHERE clause. For example: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}; This ensures a row is only modified within its tenant context.
- **server/research-engine.ts:158** — cleanupZombieSessions() runs an UPDATE on research_sessions without scoping by tenant_id — this will mark sessions from all tenants as completed (mass cross-tenant modification).
  - _Fix:_ Restrict the cleanup to a single tenant at a time (add a tenantId parameter or iterate per-tenant) and include WHERE tenant_id = ${tenantId} in the UPDATE. If cleaning multi-tenant-wide data is intentional, make it explicit and ensure only trusted operator code can invoke it.
- **server/routes.ts:4330** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, allowing creation of a note not tied to the caller's tenant (data corruption/cross-tenant contamination).
  - _Fix:_ Include tenant_id explicitly in the INSERT and use the caller's tenantId (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'})`). Even better: validate that the projectId belongs to the tenant before inserting, or call a storage helper (e.g. storage.createProjectNote) that enforces tenant scoping.
- **server/sculptor.ts:94** — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET status = 'failed' WHERE id = ${session.id}). This allows cross-tenant mutation if ids collide or are guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise bind and verify tenantId before performing the UPDATE. Always ensure UPDATE/DELETE statements against tenant-scoped tables constrain by tenant_id.
- **server/sculptor.ts:101** — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}). Missing tenant filter can permit cross-tenant writes.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${tenantId}). Use the function's tenantId parameter to guard mutations on tenant-scoped rows.
- **server/sculptor.ts:162** — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). This unscoped update can modify another tenant's session row.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). Every write against tenant-scoped tables must include tenant_id in the WHERE to enforce isolation.
- **server/sculptor.ts:346** — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). Missing tenant filter allows cross-tenant modification of review_result.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or re-validate tenant ownership before updating. Ensure all UPDATE/DELETE statements on tenant-scoped tables are tenant-scoped.
- **server/second-opinion.ts:406** — UPDATE to agent_cost_ledger omits tenant_id in WHERE clause (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}). agent_cost_ledger is tenant-scoped elsewhere; this UPDATE can modify another tenant's ledger row.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or pass and verify the tenantId associated with the reservationId before settling. Always include tenant_id in write WHERE clauses for tenant-scoped tables.
- **server/second-opinion.ts:425** — UPDATE to agent_cost_ledger omits tenant_id in WHERE clause (UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId}). This release path can alter another tenant's reservation row if not scoped.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or otherwise verify reservation ownership before updating. Ensure reservation settle/release operations always enforce tenant isolation.
- **server/seed.ts:2860** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table by id only, without verifying tenant_id — this can modify records belonging to other tenants.
  - _Fix:_ Add a tenant constraint to the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${expectedTenantId}). Ensure the SELECT that produced p included tenant_id so you can validate ownership before updating.
- **server/seed.ts:3137** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... updates the projects table without a tenant_id constraint — a cross-tenant write risk.
  - _Fix:_ Scope updates to the tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Avoid relying on id-only predicates for tenant-scoped tables; always include tenant ownership validation.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is executed without setting tenant_id — inserting into a tenant-scoped table without tenant_id will create rows that are not bound to the caller's tenant (and may violate NOT NULL constraints).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conversationId})), or, if project_conversations intentionally derives tenant from project, first validate ownership by selecting the project row with tenant_id = ${tenantId} before inserting.
- **server/seed.ts:4431** — DELETE FROM heartbeat_tasks is executed without a tenant_id filter (DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'). heartbeat_tasks is tenant-scoped (indexes reference tenant_id), so this DELETE may remove rows for all tenants, causing cross-tenant data loss/configuration changes.
  - _Fix:_ Add an explicit tenant constraint (e.g. AND tenant_id = ${tenantId}) or run the cleanup per-tenant. If the intent is to affect only system/owner tenant(s), explicitly pass that tenant id. Use parameterized values / Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)).
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` in detectStagnation can execute without a tenant_id predicate. The code builds `conditions` with category always present but only adds tenant filter when tenantId argument is provided; when tenantId is omitted this becomes an unscoped cross-tenant read.
  - _Fix:_ Fail-closed or require tenantId. Always include a tenant filter on tenant-scoped reads (e.g. .where(and(eq(experiments.category, category), eq(experiments.tenantId, tenantId)))) or throw if tenantId is not supplied/validated. Do not allow a path that executes SELECT without tenant_id.
- **server/self-improvement.ts:521** — getExperimentHistory may return an unscoped SELECT of the `experiments` table when caller does not supply tenantId (the function falls back to db.select().from(experiments)... without tenant filtering). This results in cross-tenant disclosure.
  - _Fix:_ Require and validate tenantId for this API (fail-closed if missing) or always apply the caller's tenant filter. Never perform unscoped reads on tenant-scoped tables; if tenantId is optional, explicitly document and enforce the global/shared semantics or disallow absence.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` without constraining by tenant_id. The query `db.select().from(messages).where(eq(messages.conversationId, conversationId))` lacks any `tenant_id` filter, allowing cross-tenant reads if an attacker supplies or guesses a conversationId.
  - _Fix:_ Restrict message reads to the caller's tenant. Add an explicit tenant predicate, e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). Alternatively use the tenantScope / requireTenantScope helper to produce the equality predicate and fail-closed on invalid tenantId.
- **server/skillify.ts:201** — SELECT from tenant-scoped table `messages` without constraining by tenant_id. The query `db.select().from(messages).where(eq(messages.conversationId, conversationId))` in analyzeConversation omits tenant scoping and can return another tenant's messages.
  - _Fix:_ Scope messages to the conversation's tenant before reading. Use the provided tenantId parameter in the WHERE clause, e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or build the predicate with tenantScope(requireTenantScope) to ensure fail-closed behavior.
- **server/skillify.ts:221** — SELECT from tenant-scoped table `personas` without validating tenant ownership. The query `.where(eq(personas.id, conv[0].personaId))` does not verify that the persona belongs to the same tenant, permitting cross-tenant disclosure or misuse of persona records.
  - _Fix:_ Verify persona ownership by adding a tenant filter when querying personas, e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId))). If personaId comes from an untrusted source, call a canonical assert/guard that fails-closed if ownership does not match.
- **server/skillify.ts:329** — SELECT from tenant-scoped table `messages` without constraining by tenant_id. The recentMsgs query `db.select().from(messages).where(eq(messages.conversationId, conversationId))` in skillifyConversation omits tenant scoping and can leak messages across tenants.
  - _Fix:_ Include the tenant constraint in the query. For example: .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). Use tenantScope/requireTenantScope helpers to validate tenantId and prevent fail-open cases.
- **server/storage.ts:396** — attachCitations issues: UPDATE messages SET citations = ... WHERE id = ${messageId} — no tenant_id constraint. This is an unscoped write to a tenant-scoped table.
  - _Fix:_ Require the caller to pass tenantId (or resolve it from the conversation) and include AND tenant_id = ${tenantId} in the WHERE clause, or perform the update via a JOIN that validates ownership. Do NOT update by id alone.
- **server/storage.ts:838** — archiveExpiredMemories issues: UPDATE memory_entries SET status = 'archived' WHERE ... (expires_at <= NOW()) — no tenant_id constraint. This will archive rows across all tenants.
  - _Fix:_ Run per-tenant archive (require tenantId) or include tenant_id = ${tenantId} in the WHERE. If this is intended to be global maintenance, restrict to an authorized admin job and document/authorize it explicitly.
- **server/storage.ts:855** — archiveStaleMemories executes: UPDATE memory_entries SET status = 'archived' WHERE status = 'active' AND created_at <= ${cutoff} ... — no tenant scoping. Unscoped mass mutation on a tenant-scoped table.
  - _Fix:_ Scope the UPDATE by tenant_id (e.g. require tenantId param and add eq(memory_entries.tenantId, tenantId)), or perform per-tenant operations under a safely-authorized admin job.
- **server/storage.ts:1296** — getAllDataForExport issues a large set of db.select() calls over tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, fileStorage, etc.) with no tenant filtering — full-platform export without tenant restriction.
  - _Fix:_ Restrict this endpoint to properly authenticated/authorized admin roles only. Prefer implementing per-tenant export functions that require a tenantId filter, or ensure the caller's admin identity is validated before returning cross-tenant data. Log and audit export invocations.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` uses only the row id in the WHERE clause (eq(customTools.id, tool.id)) and omits tenant_id. Without tenant scoping this can modify another tenant's row if ids are guessed or otherwise abused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, expectedTenantId)))`) and/or pass and check the tenantId explicitly to ensure the caller is authorized to modify that tenant's row.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` uses only the row id in the WHERE clause (eq(customTools.id, tool.id)) and omits tenant_id. This can delete another tenant's row if id is abused.
  - _Fix:_ Constrain DELETEs to the tenant, e.g. `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, expectedTenantId)))`. Ensure revert/delete operations always validate tenant ownership before removing rows.
- **server/tool-learning.ts:365** — UPDATE incrementing `usageCount` on tenant-scoped table `custom_tools` uses only the row id in the WHERE clause and omits tenant_id. This can corrupt usage counters for other tenants' tools.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, expectedTenantId)))`. Prefer to include tenantId when selecting the tool and carry that tenantId through subsequent updates/deletes to enforce ownership.
- **server/tools.ts:2876** — INSERT INTO project_conversations (project_id, conversation_id) in the project CREATE flow does not set tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId})). Ensure the value comes from the validated caller tenant and not from untrusted input.
- **server/tools.ts:2979** — INSERT INTO project_files does not include tenant_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (...)). Tenant-scoped tables must receive tenant_id on INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and pass the caller's tenantId (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Also consider validating that the project_id belongs to the tenant before inserting.
- **server/tools.ts:2993** — INSERT INTO project_notes (project_id, note, author) does not include tenant_id. Tenant-scoped tables require tenant_id on writes.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author||'agent'}, ${tenantId})). Ensure the project ownership check is performed (it already is) and persist tenant_id accordingly.
- **server/tools.ts:3018** — INSERT INTO project_conversations (project_id, conversation_id) in link_conversation handler omits tenant_id on INSERT.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})), and ensure prior ownership checks (e.g. assertConversationInTenant) are used to validate the referenced conversation belongs to the tenant.

### HIGH (165)

- **server/agent-channels.ts:198** — JOIN against tenant-scoped table `channel_subscriptions` omits tenant_id filter in the JOIN condition, allowing subscription rows from other tenants to contribute to the result.
  - _Fix:_ Constrain the join to the caller's tenant, e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}, or include cs.tenant_id = ${tenantId} in the WHERE clause. Ensure every reference to channel_subscriptions is tenant-scoped.
- **server/agentic-engines.ts:162** — SELECT from tenant-scoped table `ai_insights` (retryPendingMinervaRouting) without tenant scoping — reads applied/high insights across tenants.
  - _Fix:_ Run this durability sweep per-tenant (accept a tenantId and WHERE tenant_id = ${tenantId}) or otherwise add explicit tenant scoping and validation. Do not perform unscoped SELECT/UPDATE on tenant-scoped tables.
- **server/agentic-engines.ts:243** — UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) RETURNING ... — no tenant_id constraint. This bulk update may modify rows across all tenants.
  - _Fix:_ Restrict the UPDATE to a tenant (WHERE tenant_id = ${tenantId} AND ...), or iterate tenants and apply the update per-tenant. Ensure action_taken/status changes are always tenant-scoped.
- **server/agentic-engines.ts:344** — SELECT aggregated counts from `heartbeat_logs` without tenant constraint in gatherOperationalData (reads logs across tenants).
  - _Fix:_ Add tenant filtering (WHERE tenant_id = ${tenantId}) to heartbeat_logs queries or perform per-tenant aggregation. Ensure operational metrics are limited to the requested tenant.
- **server/agentic-engines.ts:451** — SELECT from `heartbeat_logs` (recentLogs) without tenant scoping — may return logs from other tenants.
  - _Fix:_ Include WHERE tenant_id = ${tenantId} (or otherwise scope by tenant) when querying heartbeat_logs for tenant-specific data.
- **server/agentic-engines.ts:519** — SELECT grouping heartbeat_logs (heartbeatPerformance) has no tenant_id filter — returns data across tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or run the aggregation per tenant. Avoid unscoped aggregation on tenant-scoped logs.
- **server/agentic-engines.ts:533** — SELECT from `heartbeat_tasks` (taskConfig) omits tenant scoping — may enumerate tasks across tenants.
  - _Fix:_ Add a tenant filter (WHERE tenant_id = ${tenantId}) if heartbeat_tasks is tenant-scoped, or otherwise explicitly document and protect this global access as admin-only.
- **server/agentic-features.ts:524** — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} lacks tenant_id constraint — an update against a tenant-scoped table without tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) to ensure the update only affects the intended tenant's row.
- **server/agentic-features.ts:599** — SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} ... does not include tenant_id — reading a tenant-scoped table without tenant constraint.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the WHERE clause (or otherwise ensure sequence_id is validated to belong to the tenant) so existing-enrollment checks are tenant-scoped.
- **server/agentic-features.ts:671** — UPDATE outreach_enrollments ... WHERE id = ${enrollment.id} omits tenant_id — performing a write against a tenant-scoped table without tenant constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to avoid cross-tenant updates. Use the tenant from the original select to double-check ownership before updates.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments ... WHERE id = ${row.id} (classifyReply) has no tenant_id constraint — writing to a tenant-scoped table without tenant scoping.
  - _Fix:_ Constrain the UPDATE with tenant_id (WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or use a parameterized drizzle update with eq(outreach_enrollments.tenantId, params.tenantId) to ensure the enrollment belongs to the caller's tenant.
- **server/agentic/approvals.ts:135** — Bulk UPDATE agentApprovals to status='expired' WHERE status='pending' AND expiresAt < NOW() executes without tenant_id scoping — modifies approvals across all tenants.
  - _Fix:_ Either run expiry per-tenant (accept a tenantId) or add tenant scoping to the update (e.g. WHERE tenant_id = ${tenantId} AND ...). If the intent is a global sweep, require an explicit admin-only path and guard it with strict privileges and logging.
- **server/auto-memorize.ts:47** — getWatermark reads agent_knowledge for the auto_memorize watermark without any tenant_id constraint, returning (and later using) a global watermark. This mixes tenant data if multiple tenants are present.
  - _Fix:_ Make the watermark tenant-scoped: accept a tenantId parameter and SELECT content FROM agent_knowledge WHERE tenant_id = ${tenantId} AND category = ... AND title = ... LIMIT 1. Do not use a global shared watermark row across tenants.
- **server/auto-memorize.ts:163** — runAutoMemorize pulls messages across all tenants in a single unscoped query (SELECT m.tenant_id, m.role, m.content ... FROM messages ... WHERE created_at > ...). Although the code partitions rowsByTenant afterwards, the initial read is an unscoped read of a tenant-scoped table without a tenant_id WHERE clause, violating the 'must filter by tenant' invariant and increasing blast radius.
  - _Fix:_ Avoid a single global messages read. Either iterate tenants and query messages WHERE tenant_id = ${tenantId} for each tenant, or otherwise ensure the initial query is properly limited to a tenant context. If a global scan is required, run it from a privileged maintenance job with explicit justification and careful safeguards (and still prefer explicitly including tenant_id in the WHERE or use a dedicated admin-only path documented & reviewed).
- **server/auto-transcript.ts:171** — Backfill inserts project_conversations rows without proving the project belongs to the same tenant. The missingLinks query (lines ~171-175) selects conversations with project_id but does not validate that the referenced project is owned by the conversation's tenant, and the subsequent INSERT (line ~179) writes project_conversations using those project_id values with no tenant check. This can create cross-tenant links if conversation.project_id has been poisoned or inconsistent.
  - _Fix:_ Before inserting project_conversations, ensure the project is owned by the same tenant (or include tenant_id in project_conversations and set it). For example, perform an INSERT ... SELECT that only inserts when a matching project row exists with p.id = r.project_id AND p.tenant_id = r.tenant_id, or explicitly check the project row first: INSERT INTO project_conversations (project_id, conversation_id) SELECT ${r.project_id}, ${r.id} WHERE EXISTS (SELECT 1 FROM projects p WHERE p.id = ${r.project_id} AND p.tenant_id = ${r.tenant_id}); alternatively add tenant_id to project_conversations and set it on insert (and filter queries by tenant_id). This guarantees backfill cannot link projects across tenants.
- **server/business-tools.ts:84** — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table, this writes rows without the caller's tenant context and can link or leak data across tenants.
  - _Fix:_ Include tenant_id in the invoice_items insert (and pass the validated tenant id), e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...). If invoice_items is intentionally not tenant-scoped, document the invariant and ensure downstream queries always join/guard by the parent invoice's tenant_id.
- **server/business-tools.ts:74** — createInvoice inserts invoices.customer_id from caller input without first verifying that the referenced customer belongs to the caller's tenant. This can create cross-tenant references (invoice row linked to a customer owned by another tenant).
  - _Fix:_ Validate ownership of params.customer_id before using it (e.g. SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1) and reject if not found. Alternatively, omit unverified customer_id or enforce server-side mapping to the tenant's customer records.
- **server/business-tools.ts:276** — logInteraction inserts a customer_interactions row referencing params.customer_id without verifying the customer belongs to the caller's tenant. Although tenant_id is set on the interaction, this can create a cross-tenant link to a customer id owned by another tenant.
  - _Fix:_ Verify ownership of params.customer_id before inserting (e.g. SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1) and return an error if the customer does not belong to the tenant. Ensure all handlers that accept foreign IDs validate ownership before writing references.
- **server/ceo-orchestrator.ts:841** — SELECT from personas table without tenant_id constraint (cachedPersonas = await db.select().from(personasTable)). This reads the tenant-scoped personas table globally and can expose/choose persona rows from other tenants.
  - _Fix:_ Scope the query to the plan's tenant. E.g. filter the select by tenant_id = plan.tenantId or use a tenant-scoped helper/transaction (withTenantTx) so cachedPersonas contains only rows for plan.tenantId. Alternatively, query for only the needed persona names/ids with an explicit WHERE tenant_id = ${plan.tenantId}.
- **server/channels/kernel.ts:180** — dispatchInbound accepts a conversationId from the adapter-supplied resolveConversation and passes it directly to processMessage without verifying that the conversation belongs to turn.tenantId. This lets an untrusted/compromised adapter (or a bug) supply a foreign conversation_id that could allow cross-tenant access if downstream DB operations do not enforce tenant_id filters.
  - _Fix:_ Do not trust an adapter-provided conversationId by itself. Either: (a) require resolveConversation to return an ownership-checked object (e.g. {conversationId, tenantId}) and have dispatchInbound assert returned.tenantId === turn.tenantId before calling processMessage; (b) validate the conversation row's tenant_id in the kernel (SELECT ... WHERE id = ? AND tenant_id = ?) before proceeding; or (c) change processMessage API to require tenantId and enforce tenant-scoped SQL filters inside processMessage. Ensure every DB read/write on tenant-scoped tables includes WHERE tenant_id = <caller tenant> (or equivalent Drizzle .where(eq(..., tenantId))).
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` omits a tenant_id constraint (WHERE tenant_id = ${tenantId}). The query is: SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...
  - _Fix:_ Constrain the read to the caller's tenant. Add an explicit tenant filter (e.g. AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(projectFiles.tenantId, tenantId)). Even if you validated projectId earlier, include tenant_id for defense-in-depth and to satisfy the invariant.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` omits a tenant_id constraint. The query is: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...
  - _Fix:_ Add an explicit tenant filter (AND tenant_id = ${tenantId}) or use Drizzle .where(eq(projectNotes.tenantId, tenantId)) so every read is explicitly scoped to the caller's tenant. Do not rely solely on project_id provenance.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table `compaction_archives` omits a tenant_id constraint. The query is: SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) in this query or otherwise verify ownership of the conversation/ archive row before returning content. Prefer parameterized Drizzle filters (eq(table.tenantId, tenantId)).
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` omits a tenant_id constraint. The query is: SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC
  - _Fix:_ Add an explicit tenant constraint (AND tenant_id = ${tenantId}) or use Drizzle .where(eq(messages.tenantId, tenantId)). Even when conversation_id came from a prior tenant-scoped query, each read against tenant-scoped tables must include tenant_id to enforce isolation.
- **server/chat-engine.ts:766** — LATERAL subqueries select from tenant-scoped table `messages` without a tenant_id constraint. Example: (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ORDER BY created_at DESC LIMIT 1) — no tenant_id filter.
  - _Fix:_ Ensure lateral/subqueries also include tenant filters (e.g. AND tenant_id = ${tenantId}) or otherwise correlate via a tenant_id predicate. Use Drizzle parameterized SQL or eq(messages.tenantId, tenantId) so every access to messages is explicit about the tenant.
- **server/chat-engine.ts:772** — LATERAL subquery reads `compaction_archives` without a tenant_id constraint. Example: (SELECT summary FROM compaction_archives WHERE conversation_id = c.id ORDER BY archived_at DESC LIMIT 1) — missing tenant filter.
  - _Fix:_ Add tenant scoping to the lateral query (e.g. AND tenant_id = ${tenantId}) or join via a tenant-checked relation. Apply the same rule to all LATERAL/inline subqueries so every read against tenant-scoped tables includes tenant_id.
- **server/chat-engine.ts:3738** — recordMutation(...) is called without any tenant identifier. This writes mutation/log data that appears tenant-scoped but no tenantId is supplied, risking cross-tenant attribution or writes.
  - _Fix:_ Include the caller's tenantId in the recordMutation payload (e.g. recordMutation({... , tenantId})) or ensure recordMutation validates ownership by resolving the conversation/resource and enforcing WHERE tenant_id = tenantId before any write.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, toolName, ...) is called without tenantId. This writes tool-execution telemetry tied to a foreign conversation id but does not verify or pass tenant context, risking cross-tenant writes/reads.
  - _Fix:_ Add tenantId to the call (recordToolExecution(conversationId, toolName, success, tenantId)) or pre-validate that the conversationId belongs to the caller's tenant (e.g. lookup with WHERE tenant_id = tenantId) before writing records.
- **server/chat-engine.ts:4332** — storage.attachCitations(messageId, collectedCitations) is called without tenantId. Attaching citations likely inserts rows referencing a message; relying only on messageId without enforcing the message's tenant can allow cross-tenant citation writes/reads.
  - _Fix:_ Either pass tenantId explicitly to attachCitations (storage.attachCitations(messageId, collectedCitations, tenantId)) or have attachCitations validate the message belongs to the caller's tenant (select message WHERE id = messageId AND tenant_id = tenantId) before inserting citations. Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' without tenant_id constraint in scanAndEscalate — SELECT id, tenant_id, ... FROM commitments WHERE status='active' AND due_at < NOW() will return rows across all tenants.
  - _Fix:_ If this is expected to be a global admin scanner, ensure the function is run in an admin-only context and that any surfaced data is appropriately redacted/audited. If it should be tenant-scoped, add AND tenant_id = ${tenantId} (and accept tenantId as parameter) or otherwise enforce tenant scoping before selecting rows.
- **server/crews-engine.ts:883** — addFlowStep inserts a crew_id (params.crewId) into flow_steps without verifying that the referenced crew belongs to the caller's tenant. This writes a foreign id provided by the caller with no ownership check.
  - _Fix:_ Before inserting, validate the provided crewId belongs to params.tenantId (e.g. call getCrew(params.crewId, params.tenantId) or SELECT id FROM crews WHERE id = $1 AND tenant_id = $2). Reject or null the crewId if it does not belong to the tenant. Alternatively run the insert inside withTenantTx and explicitly verify the referenced row's tenant match first.
- **server/data-protection.ts:117** — permanentlyPurgeSoftDeleted selects expired conversations across all tenants (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) with NO tenant_id constraint, reading tenant-scoped data globally.
  - _Fix:_ Limit this admin purge to an explicit tenantId parameter or iterate per-tenant (e.g. SELECT tenants.id then run purge per tenant), or require an explicit admin context. Any SELECT on tenant-scoped tables should include WHERE tenant_id = <expected tenant> unless the call is an audited cross-tenant maintenance job with strict access control.
- **server/data-protection.ts:182** — backupConversationToDrive reads messages with SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} — messages is tenant-scoped elsewhere, but this SELECT omits tenant_id and therefore can read messages across tenants if conversation_id is ambiguous or misused.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) to enforce application-level tenant isolation for message reads.
- **server/data-protection.ts:189** — backupConversationToDrive reads compaction_archives with SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} without scoping by tenant_id.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the query (or otherwise verify the archive rows belong to the same tenant) before reading tenant-scoped archive rows. Ensure every SELECT against tenant-scoped tables includes tenant_id filtering.
- **server/data-protection.ts:324** — backupTenantDataToDrive queries project_notes by project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) but does not include tenant_id. project_notes is effectively tenant-scoped by project; missing tenant predicate allows reading another tenant's notes if project_id were reused or mis-associated.
  - _Fix:_ Constrain project_notes queries with the tenant id (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or JOIN via projects and assert p.tenant_id = ${tenantId}. Validate foreign IDs before reading tenant-scoped tables.
- **server/data-protection.ts:335** — backupTenantDataToDrive queries project_files by project_id (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}) without a tenant_id predicate, allowing cross-tenant reads.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or otherwise verify that project_files rows belong to the same tenant before reading them.
- **server/data-protection.ts:368** — backupTenantDataToDrive runs SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} without scoping by tenant_id. messages is tenant-scoped elsewhere; this allows cross-tenant inquiry or incorrect counts.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the COUNT query (or use a JOIN to ensure the conversation belongs to the tenant). Enforce tenant predicates on every read from tenant-scoped tables to prevent cross-tenant data leakage.
- **server/delivery-pipeline.ts:189** — UPDATE on tenant-scoped table `delivery_logs` without tenant_id constraint (updateDeliveryLog uses WHERE id = <id> only).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or accept/verify the tenantId in updateDeliveryLog so every mutation enforces ownership.
- **server/delivery-pipeline.ts:683** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId with NO tenant_id filter (idempotency lookup uses WHERE stripe_payment_id = ...).
  - _Fix:_ Scope this lookup to the caller's tenant (e.g. add AND tenant_id = ${req.tenantId}) or require the caller to provide an explicit tenant context. Never query tenant-scoped rows globally by a business key without validating tenant ownership.
- **server/delivery-pipeline.ts:720** — UPDATE on tenant-scoped table `delivery_logs` clearing stripePaymentId using only id in WHERE (no tenant_id check).
  - _Fix:_ Add tenant enforcement to the UPDATE (e.g. WHERE id = ${prior.id} AND tenant_id = ${prior.tenantId || callerTenantId}) so a row from another tenant cannot be modified.
- **server/delivery-pipeline.ts:1014** — SELECT from tenant-scoped table `delivery_logs` by stripePaymentId with NO tenant filter (getDeliveryByStripePayment returns a row regardless of tenant).
  - _Fix:_ Require and use tenantId to scope this lookup, or document and enforce that this function is admin-only and cannot be called from untrusted contexts. Prefer: WHERE stripe_payment_id = ${paymentId} AND tenant_id = ${tenantId}.
- **server/doc-collections.ts:165** — DELETE from tenant-scoped table `doc_chunks` without tenant_id constraint (DELETE WHERE doc_path = ... AND collection_id = ... but missing tenant_id).
  - _Fix:_ Include tenant scoping in the DELETE (e.g. add AND tenant_id = ${tenantId}) or verify collection ownership first and pass tenantId into this query so only the intended tenant's chunks are removed.
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table `doc_chunks` setting embedding WHERE id = <id> without tenant_id constraint.
  - _Fix:_ Include tenant constraint in the UPDATE (e.g. UPDATE ... WHERE id = ${row.id} AND tenant_id = ${collectionTenantId}) or perform the update within a transaction where the tenant is validated to own the row before writing.
- **server/email.ts:365** — SELECT from tenant-scoped table `inbox_messages` using only message_id (no tenant or inbox scoping) to detect existence; this may match another tenant's record.
  - _Fix:_ Scope the existence check by tenant_id or inbox_id (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} OR AND tenant_id = ${tenantId}) so a message in one tenant doesn't prevent storing/processing for another.
- **server/email.ts:410** — SELECT from tenant-scoped table `inbox_messages` (backfillEmptyBodies) with no tenant_id or inbox filter — selects rows across tenants.
  - _Fix:_ Run backfill per-tenant or include an explicit tenant_id / inbox_id filter in the query to limit the operation to a single tenant's messages; avoid global scans on tenant-scoped tables in multi-tenant environments.
- **server/email.ts:424** — UPDATE on tenant-scoped table `inbox_messages` updating body_text/body_html WHERE id = <id> without tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update using the tenant-aware row previously retrieved to ensure the row belongs to the tenant being processed.
- **server/embeddings.ts:687** — backfillMissingKnowledgeEmbeddings runs a SELECT on agent_knowledge (SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ...) with no tenant_id filter — a read across all tenants' rows.
  - _Fix:_ Scope backfill queries to a tenant (pass tenantId into the backfill routine) or otherwise iterate per-tenant. Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise ensure the operation only touches rows owned by a single tenant). Do not run cross-tenant reads on tenant-scoped tables.
- **server/embeddings.ts:716** — backfillEmbeddingVecs selects rows from memory_entries (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200) without any tenant_id constraint — a read across all tenants.
  - _Fix:_ Run backfilling per-tenant or add tenant scoping (AND tenant_id = ${tenantId}). Ensure any maintenance job that touches tenant-scoped tables is explicitly bound to a tenant and/or runs with an audited system-account pattern that is documented and safe.
- **server/embeddings.ts:732** — backfillEmbeddingVecs selects rows from agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE ...) with no tenant_id filter — a read across all tenants.
  - _Fix:_ As with memory_entries, scope the select to a tenant (add AND tenant_id = ${tenantId}) or run the backfill per-tenant. Avoid unscoped reads from tenant-scoped tables.
- **server/felix-brain.ts:19** — Unscoped SELECT from tenant-scoped table 'agent_runs' — query does not constrain tenant_id. This boot-time rehydration reads rows for all tenants.
  - _Fix:_ Restrict the query to a single tenant context. Either run the rehydration per-tenant (adding WHERE tenant_id = <tenantId>) or add an explicit tenant_id filter derived from a trusted tenant context. Avoid scanning cross-tenant rows into in-memory structures.
- **server/google-drive.ts:527** — SELECT from tenant-scoped table 'projects' by id (WHERE eq(projects.id, projectId)) has no tenant_id filter — reading a tenant-scoped row without SQL-level tenant constraint.
  - _Fix:_ Scope the SELECT by tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise query via a trusted assertProjectInTenant helper that uses a parameterized tenant filter to avoid cross-tenant reads.
- **server/google-drive.ts:826** — SELECT from tenant-scoped table 'projects' by id in uploadAndShare (WHERE eq(projects.id, params.projectId)) lacks tenant_id in the WHERE clause — reading a referenced foreign id without SQL tenant scoping.
  - _Fix:_ Query projects with an explicit tenant filter or call an assertProjectInTenant helper before any SELECT/UPDATE that touches tenant-scoped data. For example: WHERE id = ${projectId} AND tenant_id = ${callerTenantId}.
- **server/google-drive.ts:1003** — SELECT from tenant-scoped table 'projects' to validate project ownership (WHERE eq(projects.id, params.projectId)) does not include tenant_id in the WHERE clause — a tenant-scoped SELECT without tenant constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or fetch via a helper that enforces tenant scoping before returning project data. This prevents accidental cross-tenant reads.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images does not set tenant_id (INSERT INTO presenter_slide_images (session_id, slide_index, image_data, image_size, quality) VALUES (...) ) — writing to a likely tenant-scoped table without recording tenant context.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO presenter_slide_images (..., tenant_id) VALUES (..., ${tenantId})) or otherwise derive/validate tenant ownership server-side before inserting. Prefer using Drizzle's insert helpers with the tenant column set so every row is explicitly tenant-scoped.
- **server/health-audit.ts:314** — SELECT FROM code_proposals (SELECT id, title, status, created_at FROM code_proposals WHERE status IN (...) AND created_at < ...) contains no tenant_id filter. If code_proposals is tenant-scoped, this reads across tenants and can leak or operate on other tenants' proposals.
  - _Fix:_ If this audit is intended to operate per-tenant, add WHERE tenant_id = ${tenantId} (or accept a tenantId/admin flag and enforce explicit admin authorization). If it is intended as an admin/global operation, require explicit admin checks and make that intent explicit in code and docs. Avoid silent cross-tenant reads.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) executes without scoping by tenant_id. This can modify proposals across tenants.
  - _Fix:_ Restrict the UPDATE to the tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or perform an ownership check per id before updating. If running as a global admin task, require explicit admin authorization and document the elevated privilege.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL ...) has no tenant_id filter. If heartbeat_tasks is tenant-scoped, this reads tasks for all tenants.
  - _Fix:_ Scope the SELECT by tenant_id when intended to inspect a single tenant's tasks, or require explicit admin privileges for global scans. Add WHERE tenant_id = ${tenantId} when run per-tenant, or add an explicit admin guard if global.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables tasks without scoping by tenant_id. This can disable tasks belonging to other tenants if heartbeat_tasks is tenant-scoped.
  - _Fix:_ Restrict the UPDATE with tenant ownership (e.g. AND tenant_id = ${tenantId}) or verify each task's tenant before disabling. If this is intended to be a global/admin action, require explicit admin authorization and make it explicit in code.
- **server/heartbeat.ts:52** — Raw SELECT from tenant-scoped table 'research_schedules' has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Fix:_ Constrain the query to a tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant using a safe list of tenant IDs. Alternatively use a storage helper that requires/validates the tenant context (assertTenantContext) before selecting.
- **server/heartbeat.ts:101** — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause (UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id}).
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update via a storage wrapper that enforces/asserts the task's tenant.
- **server/heartbeat.ts:106** — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause (UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id}).
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or use a tenant-aware storage API that asserts the tenant before updating.
- **server/heartbeat.ts:112** — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause (UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id}).
  - _Fix:_ Restrict the UPDATE by tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call into storage helpers that require tenant context.
- **server/heartbeat.ts:119** — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause in the error-recovery path (UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id}).
  - _Fix:_ Include tenant_id in the WHERE (AND tenant_id = ${sched.tenant_id}) or use a tenant-asserting update helper to avoid cross-tenant writes.
- **server/heartbeat.ts:658** — SELECT from tenant-scoped table 'event_log' queries pending events across all tenants (SELECT * FROM event_log WHERE status = 'pending' ... ) with no tenant_id restriction.
  - _Fix:_ Process events per-tenant or add tenant_id filtering to the query (e.g. WHERE tenant_id = ${someTenant}). If the engine must poll across tenants, explicitly and safely enumerate active tenants and query each tenant's events in a tenant-asserted context.
- **server/heartbeat.ts:1814** — SELECT from tenant-scoped table 'heartbeat_logs' (SELECT created_at FROM heartbeat_logs WHERE task_name = ... ) lacks tenant_id constraint and reads logs across tenants.
  - _Fix:_ Constrain the query by tenant_id (e.g. WHERE task_name = ... AND tenant_id = ${tenantId}) or use a storage API that requires/validates the tenant context, especially for tasks intended to run per-tenant.
- **server/heartbeat.ts:260** — UPDATE on tenant-scoped 'heartbeat_tasks' (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}) omits tenant_id in the WHERE clause — write is not constrained to the task's tenant.
  - _Fix:_ Either use storage.updateHeartbeatTask(...) (tenant-aware) or include AND tenant_id = ${t.tenantId} in the WHERE clause to ensure the update affects only the owned row.
- **server/heartbeat.ts:264** — UPDATE on tenant-scoped 'heartbeat_tasks' (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}) omits tenant_id in the WHERE clause.
  - _Fix:_ Add tenant scoping to the UPDATE (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or route through a tenant-asserting storage helper so cross-tenant mutation cannot occur.
- **server/heartbeat.ts:3208** — UPDATE on tenant-scoped 'heartbeat_tasks' sets approval_status without scoping by tenant (UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}).
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${newTask.tenantId} or ${tenantId}) or use the storage API to update approval_status in a tenant-validated manner to prevent cross-tenant modification.
- **server/heartbeat.ts:3360** — UPDATE on tenant-scoped 'heartbeat_tasks' sets approval_status without tenant scoping (UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}).
  - _Fix:_ Restrict the UPDATE by tenant_id (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call a tenant-aware storage helper to perform the update.
- **server/heartbeat.ts:2863** — Call to storage.getHeartbeatTasks() with no tenant argument returns global task list and is used for delegation cap / duplicate checks — logic operates across all tenants instead of the task's tenant.
  - _Fix:_ Pass the current tenantId to storage.getHeartbeatTasks(tenantId) or otherwise scope the in-memory checks to the task's tenant. Avoid reading global task lists when making tenant-scoped decisions (delegation caps, duplicate detection).
- **server/internal-resolver.ts:91** — finalizeEvent() updates event_log WITHOUT constraining by tenant_id (UPDATE event_log ... WHERE id = ${eventId} AND status IN (...)). This can finalize/alter events belonging to other tenants.
  - _Fix:_ Include tenant scoping in the CAS WHERE clause (e.g. add AND tenant_id = ${tenantId}), or require a prior read that verifies event_log.tenant_id matches the caller/actor and pass tenantId into finalizeEvent. Do not perform terminal updates without verifying ownership or operator/admin authorization.
- **server/internal-resolver.ts:106** — SELECT from plans reads a plan by id with NO tenant_id constraint (SELECT ... FROM plans WHERE id = ${planId} LIMIT 1). This may expose or act on another tenant's plan.
  - _Fix:_ Filter by tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or fetch the plan and explicitly verify its tenant before taking any action. Propagate caller tenant into this resolver path so ownership can be asserted.
- **server/internal-resolver.ts:170** — UPDATE on code_proposals archives a proposal by id WITHOUT restricting to the owning tenant (UPDATE code_proposals ... WHERE id = ${proposalId} AND status = 'pending').
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or first SELECT+verify ownership) so only proposals belonging to the caller's tenant can be modified. Ensure the resolver receives/validates tenant context for these operations.
- **server/lib/bwb-job-progress.ts:74** — scope() may omit the tenant predicate: when envTenantId() returns null the generated WHERE clause for videoJobs operations contains only jobId. Multiple UPDATE/SELECT statements call .where(scope(...)) and therefore can operate without tenant_id restriction if BWB_TENANT_ID is missing.
  - _Fix:_ Do not allow writes/reads against tenant-scoped video_jobs without an explicit tenant_id. Require and assert a tenantId (fail-closed) instead of warning; always include eq(videoJobs.tenantId, tid) in the WHERE. If env threading can be missing in subprocesses, change the spawn/propagation to always pass tenantId or make the functions refuse to run when tenantId is absent.
- **server/lib/ideabrowser-backfill.ts:49** — Query aggregates projects across ALL tenants without constraining by tenant_id: `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id`. This reads tenant-scoped table `projects` without an application-level tenant filter and therefore violates the tenant-isolation invariant.
  - _Fix:_ Restrict the aggregation to only the snapshot/target tenants (do not scan all tenants). For example, compute the list of tenant ids from the snapshot and use a parameterized WHERE tenant_id = ANY(${Array.from(wantedByTenant.keys())}::int[]) or iterate per-tenant and run the count scoped to that tenant. Avoid string-concatenating identifiers; pass tenant-id arrays as parameters to the SQL to ensure proper isolation and prevent accidental cross-tenant access.
- **server/lib/mcp-api-keys.ts:166** — verifyApiKey() queries the tenant-scoped table `mcp_api_keys` using only `WHERE key_prefix = ${prefix}` (no `tenant_id` constraint). The invariant requires every read against a tenant-scoped table to be constrained by tenant_id; a tenant-less lookup here violates that rule and enables cross-tenant lookup/enumeration risks.
  - _Fix:_ Enforce tenant scoping at the SQL level. Options include: (a) make `key_prefix` globally unique (unique index) so prefix-only lookups cannot return rows from other tenants and clearly document the safety; (b) require or derive a tenant_id prior to the SELECT and add `AND tenant_id = ${tenantId}` to the WHERE clause; or (c) introduce a small global mapping table (key_prefix → tenant_id) with a UNIQUE index and use that mapping in a single, minimal select. Additionally, restrict the SELECT to only the minimal columns needed (key_hash + tenant_id), add rate-limiting to mitigate prefix enumeration, and audit the flow so any cross-tenant reads are eliminated or explicitly justified and hardened.
- **server/lib/stuck-task-sweep.ts:56** — SELECT against tenant-scoped table `agent_runs` without a tenant_id constraint (SELECT ... FROM agent_runs WHERE status = 'running' ...). This scans rows across tenants and returns tenant_id in results without being filtered to a caller tenant.
  - _Fix:_ If this is an intentional multi-tenant audit, make that explicit and document it. Otherwise restrict the query to a tenant (WHERE tenant_id = ${tenantId}). For any reporting that returns tenant-scoped rows to callers, ensure the caller is authorized for those tenant_ids and that queries always include tenant_id predicates.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against tenant-scoped table `mind_tickets` without a tenant_id constraint (SELECT ... FROM mind_tickets WHERE status = 'in_progress' ...). This reads across tenants.
  - _Fix:_ Either scope the query to a tenant (WHERE tenant_id = ${tenantId}) or, if this is intentionally a global administrative audit, ensure the operation runs in an explicit admin context and that downstream consumers are allowed to see cross-tenant data. Prefer including tenant_id in the WHERE clause for tenant-scoped reads.
- **server/lib/system-probes.ts:163** — SELECT count(*) FROM tenant-scoped table `messages` executed without a tenant_id filter (db.select(...).from(messagesTable)). This returns a global message count across tenants rather than a caller-scoped value.
  - _Fix:_ If per-tenant counts are required here, add WHERE tenant_id = ${tenantId}. If the probe must be global for operational reasons, make the admin/global intent explicit, and ensure the routine only runs in an authorized admin context and documents the cross-tenant read.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table tenant_voice_profiles uses only the row id in WHERE (eq(tenantVoiceProfiles.id, existing.id)) and does not constrain by tenant_id. This allows updating another tenant's row if an id is supplied/mis-bound.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) or otherwise verify ownership before updating. Prefer using parameterized helpers that always enforce tenantId.
- **server/memory-graph.ts:80** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} is executed without constraining by tenant_id. This can increment counts on categories belonging to other tenants if ids are misused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}), and/or make ensureCategory return/require tenant context so updates always include tenant_id.
- **server/memory-graph.ts:196** — Existence check SELECT 1 FROM memory_links WHERE (source_memory_id = ... OR target_memory_id = ...) LIMIT 1 lacks any tenant scoping. If memory_links are tenant-scoped, this will check/return links across tenants; if memory_links are not tenant-scoped, the design allows cross-tenant linking.
  - _Fix:_ Either add tenant_id to the memory_links table and include AND tenant_id = ${tenantId} in this SELECT, or join through memory_entries to assert the candidate row belongs to the caller's tenant (e.g. JOIN memory_entries me ON me.id = CASE ... AND me.tenant_id = ${tenantId}). Ensure existence checks are tenant-scoped.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} is executed without tenant scoping. Although the id came from a tenant-scoped SELECT earlier, the UPDATE should still constrain by tenant_id to avoid accidental cross-tenant writes.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}), or pass tenantId into the helper so updates always assert ownership. Audit all UPDATEs on knowledge_triples to ensure tenant filtering.
- **server/minerva-planner.ts:373** — SELECT probe (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}) performed without tenant_id constraint while used to decide idempotency. This read can leak or mis-evaluate another tenant's plan.
  - _Fix:_ Constrain the probe SELECT by tenant_id: SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId}. Require the calling layer to pass/verify expectedTenantId or only allow this probe on trusted internal contexts with an explicit guard.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list. The application only calls assertProjectInTenant() before this INSERT, but the SQL write itself lacks an explicit tenant_id column per the project's invariant.
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Do not rely solely on an application-level ownership check — always set tenant_id explicitly in tenant-scoped INSERTs (or use a shared helper that enforces the tenant column).
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list. Although assertProjectInTenant(...) is called, the SQL statement itself writes no tenant_id column.
  - _Fix:_ Pass tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure all code paths that write tenant-scoped tables include tenant_id to prevent cross-tenant writes if the application guard is bypassed or incorrect.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM `oauth_subscriptions` filtered only by provider (no tenant_id). This reads subscription metadata across tenants.
  - _Fix:_ If this is intended to operate across tenants, make the admin intent explicit and ensure proper authorization/auditing. Otherwise restrict the query with WHERE tenant_id = ${tenantId} (or pass an explicit list of tenant IDs you are authorized to act on). Avoid unconstrained queries against tenant-scoped tables.
- **server/oauth-subscriptions.ts:1258** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM `oauth_subscriptions` WHERE is_active = TRUE — no tenant_id constraint. This enumerates active subscriptions across tenants in proactiveTokenRefresh.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) or require an explicit admin authorization boundary before enumerating rows across tenants. Prefer per-tenant refresh scheduling or an admin-only mechanism with strict access controls and auditing instead of unconstrained multi-tenant SELECTs.
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM `oauth_subscriptions` WHERE is_active = TRUE in forceRefreshAllSubscriptions is unconstrained by tenant_id, enumerating active subscriptions across tenants.
  - _Fix:_ Limit to a single tenant or require explicit admin privileges/audit before running cross-tenant operations. If the intention is to refresh all tenants, implement an approved, auditable admin workflow (or build a safe per-tenant loop driven by a trusted tenant list) rather than an unconstrained SELECT on a tenant-scoped table.
- **server/pace-control.ts:85** — COUNT query against heartbeat_logs lacks tenant scoping — no WHERE tenant_id = ${tenantId}. This returns cross-tenant aggregate usage and can allow tenants to observe/affect others' pacing.
  - _Fix:_ Accept/derive a tenantId (or otherwise partition by tenant) and add a WHERE tenant_id = ${tenantId} clause (parameterized) to the query. Ensure all heartbeat_logs reads (global and per-persona counts) are filtered by that tenantId, and propagate tenant scoping into callers of checkPace.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs without any tenant filter (both the total/filters query and the per-persona GROUP BY). This produces cross-tenant usage snapshots.
  - _Fix:_ Add a tenantId parameter to getPaceSnapshot (or otherwise scope the snapshot to a tenant) and include WHERE tenant_id = ${tenantId} in both queries. If an operator-level global snapshot is required, restrict that API to admin-only contexts and make the scope explicit in the function name/documentation.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} is executed without tenant_id in the WHERE clause (the SELECT that populated t had tenant filtering, but the subsequent UPDATE uses id only). This can mutate another tenant's task if ids collide or are re-used.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) to ensure the mutation is applied only within the tenant's scope.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} is executed without tenant_id scoping after selecting desks filtered by tenant_id. The write therefore is not explicitly constrained to the tenant.
  - _Fix:_ Add tenant_id = ${tenantId} to the UPDATE WHERE clause (WHERE id = ${desk.id} AND tenant_id = ${tenantId}) to prevent cross-tenant modifications.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant_id filtering. This updates governance_rules by id only and could affect another tenant's rule record.
  - _Fix:_ Constrain the update by tenant_id (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) when logging rule triggers to ensure the rule updated belongs to the same tenant that owns the rule.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` executed without any tenant_id constraint (global read).
  - _Fix:_ If this is meant to be a per-tenant query, add an explicit WHERE tenant_id = ${tenantId}. If the cron runner legitimately needs a global scan, make that explicit (e.g. call a documented internal API / pass an `_allowGlobalSystemUseOnly` flag) and ensure every subsequent write/UPDATE uses the per-row tenant_id to constrain the write. Avoid unscoped reads unless the call site is a clearly-audited system runner.
- **server/research-engine.ts:390** — startResearchSession() issues DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() with no tenant_id constraint — this will remove expired autoresearch knowledge for all tenants.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${session.tenantId} AND source = 'autoresearch' AND expires_at < NOW()), or run the cleanup per-tenant after validating ownership.
- **server/routes.ts:2141** — POST /api/presenter INSERTS into tenant-scoped table `presenter_sessions` using tenantId taken directly from the request body (untrusted input) without verifying the caller is authorized for that tenant.
  - _Fix:_ Do not accept tenantId from arbitrary callers. Derive tenantId from the caller's authenticated session (getTenantFromRequest) or require and verify platform-admin rights (e.g. requirePlatformAdmin) before allowing an explicit tenantId. If an internal service must create sessions for arbitrary tenants, ensure the caller is strongly authenticated and authorized and add explicit checks that the caller can act for the target tenant (and audit/log the action).
- **server/routes.ts:2312** — GET /api/store/order/:sessionId selects from tenant-scoped table `delivery_logs` by order/stripe id without a tenant_id constraint (no WHERE tenant_id = ...). This reads tenant data without constraining to the caller's tenant.
  - _Fix:_ Either require the tenant context and add WHERE delivery_logs.tenant_id = <caller tenant> OR validate that the sessionId is a true unguessable capability and treat it as the sole auth (but ensure this design is explicit and safe). Prefer explicitly verifying the tenant ownership of the delivery row before returning any tenant-scoped fields (or return only non-sensitive public fields).
- **server/routes.ts:2420** — Async order-lookup (setImmediate) queries tenant-scoped `delivery_logs` by lower(customer_email) without restricting by tenant_id, performing cross-tenant reads.
  - _Fix:_ Avoid performing cross-tenant reads based solely on email. If this behavior is intentional (lookup across platform), restrict this flow to platform-admin callers and document/audit it. Otherwise add tenant scoping (WHERE tenant_id = <caller tenant>) or partition the lookup per-tenant. At minimum ensure the code cannot expose another tenant's delivery rows to an attacker and that any emails sent only contain links for rows verified to belong to the platform account intended.
- **server/routes.ts:2564** — POST /api/store/verify-orders queries tenant-scoped `delivery_logs` with lower(customerEmail) = ${rawEmail} and returns order lists without applying tenant_id constraints.
  - _Fix:_ Either add tenant scoping (return only orders for the caller's tenant) or explicitly require and verify platform-admin privileges for cross-tenant lookup. If the endpoint is meant to operate as a platform-level recovery (email-based capability), ensure the lookup is implemented in a way that cannot be abused to enumerate or expose other tenants' data and that returned rows are validated to belong to the intended tenant before disclosure.
- **server/routes.ts:4324** — SELECT from tenant-scoped table 'conversations' without constraining by tenant_id. The query `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` can return data for a conversation owned by a different tenant.
  - _Fix:_ Restrict the query to the caller's tenant. For example: `SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL`. Prefer using the conversation/ storage helper that enforces tenant scoping (or use conv.tenantId) and/or explicitly verify the conversation belongs to the tenant before reading related ids.
- **server/routes/admin.ts:585** — Query reads from tenant-scoped table `ai_insights` without constraining by tenant_id. The SELECT on ai_insights (status = 'new' ...) lacks a WHERE tenant_id = <caller tenant>, so it can return rows across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (or to the intended admin-owned tenant) by adding an explicit tenant filter, e.g. WHERE tenant_id = ${tenantId} AND status = 'new' ...; ensure tenantId is derived from the authenticated session and that the handler's admin gating matches the intended scope (platform-owner vs single-tenant).
- **server/routes/admin.ts:606** — Query reads from tenant-scoped table `ai_insights` without constraining by tenant_id. The 'stuckPending' SELECT on ai_insights (status = 'applied' ...) has no tenant filter and may leak data across tenants.
  - _Fix:_ Add an explicit tenant constraint to the WHERE clause, e.g. WHERE tenant_id = ${tenantId} AND status = 'applied' ...; ensure tenantId is validated from the request and that only the platform-owner/admin is allowed to access cross-tenant data if that is intended.
- **server/routes/admin.ts:598** — Query reads from tenant-scoped table `heartbeat_tasks` without constraining by tenant_id. The disabled heartbeat_tasks query (WHERE enabled = false OR consecutive_failures >= 3) lacks a tenant_id filter and can return rows for all tenants.
  - _Fix:_ Include tenant scoping in the query, e.g. WHERE tenant_id = ${tenantId} AND (enabled = false OR consecutive_failures >= 3) ..., and ensure tenantId is taken from the authenticated session (or explicitly use the ADMIN_TENANT_ID if the intent is to operate only on the platform-owner tenant).
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table `conversations` lacks tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}) — this can modify a conversation belonging to another tenant if ids are guessed or mixed up.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}. Prefer using the conversation's known tenant (conv.tenantId ?? tenantId) to be defensive.
- **server/routes/conversations.ts:99** — SELECT from `project_conversations` omits tenant scoping (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}). If project_conversations is tenant-scoped this can read another tenant's linkage.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}), or otherwise ensure the query is executed with the resolved tenant (conv.tenantId ?? tenantId) to enforce isolation.
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table `project_conversations` does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). Without an explicit tenant_id INSERT, rows may be orphaned or assigned incorrectly.
  - _Fix:_ Include tenant_id in the INSERT columns and values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})). Use the verified tenant (conv.tenantId ?? tenantId) if available.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table `projects` lacks tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This can update another tenant's project if an id is abused.
  - _Fix:_ Add tenant scoping: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Always include tenant_id on UPDATE/DELETE for tenant-scoped tables.
- **server/routes/conversations.ts:227** — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). If project_notes is tenant-scoped, missing tenant_id allows notes to be created without tenant attribution or associated with the wrong tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0,8000)}, ${'system:archive'})). Use the verified tenant for the values.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} — missing tenant_id constraint. This path marks a message read without constraining to the caller's tenant, allowing cross-tenant mutation if a message id is guessed.
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}. Prefer using parameterized tenantId from the authenticated session.
- **server/routes/memory.ts:157** — Conditional tenantClause may evaluate to TRUE for the platform admin (ADMIN_TENANT_ID), causing an unscoped SELECT from the tenant-scoped memory_entries table (i.e. a cross-tenant read when tenantId === ADMIN_TENANT_ID).
  - _Fix:_ Avoid substituting a bare TRUE into the WHERE for tenant-scoped tables. Either (a) always include eq(memory_entries.tenant_id, tenantId) and gate platform-global behavior behind an explicit requirePlatformAdmin() check that documents the intent, or (b) if an admin must retrieve cross-tenant data, call a clearly-named platform-admin-only function that performs the global query and is auditable. Do not rely on an ad-hoc TRUE clause which silently removes tenant isolation.
- **server/routes/metrics.ts:63** — SELECT from agent_cost_ledger aggregates cost per tenant over the last 24h without any tenant_id WHERE predicate — a tenant-scoped table is being scanned across all tenants.
  - _Fix:_ If this multi-tenant aggregation is intentional for an operator-only metrics endpoint, require a strong platform/operator auth check (not just a token existence) and document it clearly. Alternatively, restrict queries to a single tenant (WHERE tenant_id = ${tenantId}) or move cross-tenant analytics to a separate audited analytics job/process. Ensure access is logged and limited to authorized operators only.
- **server/routes/public-chat.ts:278** — convTenantId is set to conv.tenantId ?? ADMIN_TENANT_ID and then used for reads/writes (storage.createMessage, storage.getMessages, storage.getMemoryEntries, storage.getKnowledge). If conv.tenantId is null, the code falls back to ADMIN_TENANT_ID and may cause tenant-scoped storage calls to run in the platform-admin context, effectively performing cross-tenant reads/writes.
  - _Fix:_ Do not fall back to ADMIN_TENANT_ID silently. Require that public conversations always have a valid tenant_id and reject or remap requests for legacy/nullable records. If platform-admin behavior is required for some conversations, require an explicit platform-admin gate and a clearly-audited code path. Ensure storage APIs called here are always passed the exact tenant id associated with the conversation (never a global/admin sentinel) unless the action is intentionally and explicitly allowed for platform admins.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id filter — reading a tenant-scoped table without constraining by tenant allows cross-tenant data exposure.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise pass the tenant context into the query. Use Drizzle's .where(eq(research_sessions.tenant_id, tenantId)) or include tenant_id in the SQL parameters.
- **server/seed.ts:2855** — SELECT id, name, model FROM research_programs WHERE is_active = true lacks any tenant_id constraint — reading all tenants' research_programs.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or load tenant_id in the SELECT and filter by the expected tenant before operating on rows.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... runs against conversations with no tenant filter (and the subquery against project_conversations also lacks tenant scope) — reading tenant-scoped conversation rows across tenants.
  - _Fix:_ Add tenant scoping to the query and the subquery (e.g. WHERE c.tenant_id = ${tenantId} AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.tenant_id = ${tenantId} AND pc.project_id = c.project_id AND pc.conversation_id = c.id)). Fetch tenant_id as part of the row if needed and validate it before using results.
- **server/seed.ts:3205** — SELECT from tenant-scoped table `conversations` is executed without any tenant_id constraint. The query (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)) can return rows across all tenants, causing cross-tenant linking when later INSERTs run.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. add AND c.tenant_id = ${tenantId} or use a per-tenant loop). Alternatively ensure project_conversations includes tenant_id and insert with that tenant_id. Use parameterized tenantId (no string interpolation) or Drizzle .where(eq(conversations.tenantId, tenantId)).
- **server/seed.ts:4459** — UPDATE on tenant-scoped table `memory_entries` runs without a tenant_id constraint (UPDATE memory_entries SET fact = REPLACE(...) WHERE deleted_at IS NULL AND fact LIKE ...). This will modify memory entries across all tenants rather than scoping to a tenant context.
  - _Fix:_ Scope the UPDATE to a tenant (e.g. add AND tenant_id = ${tenantId} or iterate per-tenant). Use parameterized tenantId (no string interpolation) or Drizzle .where(eq(memoryEntries.tenantId, tenantId)) to ensure changes only affect the intended tenant.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` updates by experiment id only (.where(eq(experiments.id, experiment.id))) with no tenant_id constraint. This allows modifying another tenant's row if an id is guessed or mixed up.
  - _Fix:_ Constrain updates to the caller's tenant. Add tenant guard to the WHERE clause, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))) (or use the authoritative tenant variable available in scope). Always include tenant_id in UPDATE/DELETE predicates for tenant-scoped tables.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table `experiments` (in catch) uses only experiment id in WHERE and lacks tenant_id constraint, enabling cross-tenant modification on failure paths.
  - _Fix:_ Same fix as above — include the tenant constraint in the WHERE clause for error updates (AND eq(experiments.tenantId, config.tenantId)) and ensure the tenant used is the validated, authoritative tenant for the caller.
- **server/storage.ts:295** — attachCitationMetadata performs: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL — no tenant_id constraint. This reads a tenant-scoped table without scoping by tenant.
  - _Fix:_ Require/accept the tenantId (or derive it) and include AND tenant_id = ${tenantId} in the WHERE, or join via conversations to ensure message ids belong to the caller's tenant. Avoid selecting messages by id alone.
- **server/storage.ts:480** — deletePersona transaction updates conversations with .where(eq(conversations.personaId, id)) without any tenant scoping — touching a tenant-scoped table across all tenants.
  - _Fix:_ Require a tenantId parameter for persona deletion (or otherwise restrict to a tenancy-aware admin path) and include eq(conversations.tenantId, tenantId) in the WHERE for each per-tenant update/delete. If the intent is a global persona delete, make the admin intent explicit and audit/authorize the caller.
- **server/storage.ts:481** — deletePersona transaction updates memory_entries with .where(eq(memory_entries.personaId, id)) and no tenant filter — unscoped cross-tenant write.
  - _Fix:_ Constrain the update by tenant (eq(memory_entries.tenantId, tenantId)) or iterate per-tenant after explicit admin authorization. Require tenant context for tenant-scoped mutations.
- **server/storage.ts:482** — deletePersona transaction deletes daily_notes with .where(eq(daily_notes.personaId, id)) without tenant scoping — unscoped delete against a tenant-scoped table.
  - _Fix:_ Include tenant filtering (eq(daily_notes.tenantId, tenantId)) or require explicit admin authorization and make tenant-scoped behavior deliberate before deleting across tenants.
- **server/storage.ts:803** — updateMemoryEmbedding executes: UPDATE memory_entries SET embedding = ... WHERE id = ${id} — no tenant_id constraint. This updates embeddings on a tenant-scoped table by id only.
  - _Fix:_ Require a tenantId argument (or resolve it safely) and include AND tenant_id = ${tenantId} in the WHERE clause. Alternatively, look up/verify the row's tenant before updating.
- **server/storage.ts:815** — updateKnowledgeEmbedding executes: UPDATE agent_knowledge SET embedding = ... WHERE id = ${id} — no tenant_id constraint. Unscoped write to tenant-scoped agent_knowledge.
  - _Fix:_ Require caller tenantId or verify ownership prior to updating and include eq(agent_knowledge.tenantId, tenantId) in the WHERE to prevent cross-tenant writes.
- **server/storage.ts:823** — getMemoriesWithoutEmbeddings selects memory_entries WHERE status='active' AND embedding IS NULL without tenant scoping — returns rows across tenants.
  - _Fix:_ Add tenant scoping (pass tenantId or use tenantScope) so background embedding work operates only on rows belonging to the intended tenant, or make this an explicitly authorized cross-tenant admin operation.
- **server/storage.ts:828** — getKnowledgeWithoutEmbeddings selects agent_knowledge WHERE ... WITHOUT tenant filter — reads tenant-scoped knowledge across tenants.
  - _Fix:_ Require/accept tenantId and include eq(agent_knowledge.tenantId, tenantId) (or use tenantScope) to restrict results to the caller's tenant, or document and gate this as an admin-only job.
- **server/storage.ts:934** — getHeartbeatTask executes: SELECT * FROM heartbeat_tasks WHERE id = ${id} with no tenant filter. heartbeat_tasks has tenant_id — this reads a tenant-scoped table unscoped.
  - _Fix:_ Require a tenantId parameter and include eq(heartbeat_tasks.tenantId, tenantId) in the WHERE, or otherwise validate caller authorization before returning task details.
- **server/storage.ts:991** — getDueHeartbeatTasks runs a raw SELECT FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ... with no tenant constraint. This returns due tasks for all tenants.
  - _Fix:_ If the scheduler must iterate cross-tenant tasks, ensure this function runs in a properly authorized/system context. Otherwise, add tenant scoping or provide a separate per-tenant fetch that the scheduler can call safely with authorized tenant context.
- **server/storage.ts:1019** — claimHeartbeatTasks updates heartbeat_tasks (UPDATE ... WHERE id = ${id} AND enabled = true AND next_run_at <= NOW()) with no tenant constraint — unscoped mutation of tenant-scoped rows.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or ensure claimHeartbeatTasks is only invoked in a trusted system context that is authorized to touch tasks across tenants. Prefer explicit per-tenant claim semantics.
- **server/storage.ts:1035** — markHeartbeatTaskRun updates heartbeat_tasks .where(eq(heartbeat_tasks.id, id)) without tenant filtering. Unscoped write to a tenant-scoped table.
  - _Fix:_ Require caller to pass tenantId or restrict operation by adding AND tenant_id = ${tenantId} to the WHERE (or validate ownership before updating).
- **server/surprise-scorer.ts:95** — stampArgsEmbedding executes: UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL — missing tenant_id constraint. This writes to a tenant-scoped table by id alone.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or require the caller to pass tenantId and verify ownership before updating. Do not update by id-only.
- **server/surprise-scorer.ts:141** — scoreProposalSurprise runs: UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId} — no tenant filter. Unscoped write into tenant-scoped table.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE or require/verify tenant context before writing the embedding.
- **server/surprise-scorer.ts:214** — scoreProposalSurprise updates: UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} — missing tenant_id constraint. Unscoped write to tenant-scoped table.
  - _Fix:_ Constrain the UPDATE by tenant (AND tenant_id = ${tenantId}) or validate row ownership before updating. Ensure all writes to felix_proposals include tenant checks.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without scoping by tenant_id (where clause uses only name). This leaks cross-tenant existence information and can cause incorrect cross-tenant conflict handling.
  - _Fix:_ Include tenant filtering in the query, e.g. add `and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))`. Always constrain SELECTs on tenant-scoped tables to the caller's tenantId.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without also verifying tenant ownership (missing tenant_id constraint).
  - _Fix:_ Add tenant guard to the query, e.g. `.where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))` or otherwise validate that the returned row belongs to the expected tenant before using it.
- **server/tools.ts:2911** — SELECTs in the project LIST query reference tenant-scoped tables (project_files, project_notes, project_conversations) via subqueries without an explicit tenant_id filter.
  - _Fix:_ Add an explicit tenant filter to each subquery (e.g. AND tenant_id = ${tenantId}) or JOIN through a table already scoped to the caller's tenant. Every read against tenant-scoped tables must include WHERE tenant_id = <caller tenant>.
- **server/tools.ts:2973** — Duplication check SELECT against project_files uses project_id and file_name but does not include an explicit tenant_id filter.
  - _Fix:_ Constrain the SELECT with tenant_id = ${tenantId} (or JOIN via a tenant-scoped parent like projects with an explicit tenant_id check) so the lookup cannot return rows belonging to other tenants.
- **server/tools.ts:3014** — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} lacks an explicit tenant_id filter on a tenant-scoped table.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or join via a tenant-scoped parent) so the read is explicitly limited to the caller's tenant.
- **server/tools.ts:6899** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The code calls assertProjectInTenant(...) to verify ownership but the INSERT statement omits tenant_id (VALUES clause: project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by). Under the project's invariant every INSERT into a tenant-scoped table must explicitly supply tenant_id (columns are NOT NULL with no default) rather than relying solely on prior checks.
  - _Fix:_ Include tenant_id explicitly in the INSERT and bind it from the validated tenant context (e.g. VALUES (${params._tenantId}, ${params.project_id}, ... ) or use Drizzle's insert with tenantId field). Alternatively, fetch/derive the project's tenant_id server-side and use that explicit value in the INSERT. Keep the existing assertProjectInTenant() ownership check as defense-in-depth, but do not omit tenant_id in the write to a tenant-scoped table.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE on browser_workflows (last_replayed) executes without constraining by tenant_id — raw SQL: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${workflow.id} AND tenant_id = ${tenantId}) or otherwise ensure the statement writes only the caller's tenant rows. Prefer binding the caller tenant and enforcing the tenant check in the WHERE clause for all UPDATE/DELETE statements on tenant-scoped tables.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files omits tenant scoping / tenant_id column: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${projId}, ...). If project_files is tenant-scoped this writes without an explicit tenant_id.
  - _Fix:_ Include tenant_id in the INSERT payload (tenant_id: ctx.tenantId) or otherwise derive and validate ownership and set the tenant_id column explicitly. If project_files' tenant is implied via project_id, still make tenant explicit or validate project ownership immediately prior to INSERT and include tenant_id to make the write auditable and enforced at the SQL level.
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} executes without a tenant filter. This updates a potentially tenant-scoped row by id only, lacking an AND tenant_id = <caller> guard.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projId} AND tenant_id = ${ctx.tenantId}) so the write cannot affect projects belonging to other tenants. Validate project ownership earlier and always include the tenant condition in SQL writes/updates on tenant-scoped tables.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE to tenant-scoped table 'knowledge_triples' is executed without a tenant_id constraint (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}). This allows mutating rows by id across tenants.
  - _Fix:_ Constrain every UPDATE to the caller's tenant. For example, include AND tenant_id = ${tenantId} in the WHERE clause (and optionally AND valid_until IS NULL): db.execute(sql`UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}`); or first verify the row belongs to tenantId before performing the update.
- **server/tools/domains/security/handlers.ts:163** — Query against provider_keys executes without scoping by tenant_id. The SQL reads provider_keys across all tenants (no WHERE tenant_id = ${tenantId}). Even though this branch runs only for tenantId===1, it still queries a tenant-scoped table without an explicit tenant filter, violating the invariant that tenant-scoped reads must be constrained to the caller's tenant.
  - _Fix:_ Restrict the query to the caller tenant by adding an explicit tenant filter, e.g. WHERE tenant_id = ${tenantId} (or equivalent scanSql`... tenant_id = ${tenantId} ...`). If the intent is to aggregate across tenants for an admin operation, do this intentionally and explicitly (and document/authorize it), or perform a controlled, explicit admin-only aggregation that cannot be invoked from normal tenants.
- **server/tools/domains/security/handlers.ts:260** — Query counting provider_keys (SELECT COUNT(DISTINCT provider) ...) is executed without a tenant_id constraint. This reads a table that is typically tenant-scoped across all tenants with no WHERE tenant_id = ${tenantId}, violating the requirement that every read against tenant-scoped tables be constrained to the caller's tenant.
  - _Fix:_ Add an explicit tenant filter to the COUNT query (e.g. WHERE api_key IS NOT NULL AND api_key != '' AND tenant_id = ${tenantId}) or, if the intention is to compute a global cross-tenant metric for the owner tenant, perform a clearly-documented and authorized admin aggregation routine instead of an unconstrained query from a regular tool path.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE memory_entries SET ... WHERE id = ${id} is missing a tenant_id constraint. This write to a tenant-scoped table is constrained only by a prior SELECT; the UPDATE by id alone could modify rows from other tenants if ids are reused or attacker manipulates id values.
  - _Fix:_ Include the tenant constraint on the UPDATE. Example: UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} (or WHERE tenant_id = ${tid} AND id = ${id}). Alternatively use Drizzle's .where(eq(table.tenantId, tid)) to ensure tenant scoping is always applied.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) lacks a tenant_id filter. This performs a write against a tenant-scoped table without explicitly constraining to the caller's tenant.
  - _Fix:_ Add the tenant constraint to the UPDATE, e.g. UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE tenant_id = ${tid} AND id = ANY(${ids}). Or use Drizzle .where(eq(table.tenantId, tid)). Ensure the tenant is enforced even when updating by id list.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id} is missing a tenant_id constraint. The update uses only the row id and does not include tenant scoping, allowing potential cross-tenant modification if ids collide or are misused.
  - _Fix:_ Constrain the update to the tenant, e.g. UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id} AND tenant_id = ${tid} (or WHERE tenant_id = ${tid} AND id = ${id}). Prefer using Drizzle's .where(eq(table.tenantId, tid)) so tenant scoping cannot be accidentally omitted.
- **server/twilio.ts:102** — SELECT from agent_knowledge performed without any tenant_id filter — this loads content::jsonb rows across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId}), or otherwise perform the ownership verification in the DB (join agent_knowledge → conversations on conversation id and require matching tenant_id) so you never SELECT raw content rows from other tenants. If the cross-tenant scan is intentional for admin tooling, ensure it runs under a clearly separated admin path and never exposes other-tenants' content to unprivileged contexts.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} executed in settleVentureReservation has no tenant_id constraint — this can update ledger rows across tenants if a reservationId refers to another tenant.
  - _Fix:_ Require and pass tenantId into settleVentureReservation and include AND tenant_id = ${tenantId} in the WHERE clause, or first SELECT and validate the reservation row's tenant_id matches the expected tenant before updating. Never update ledger rows by id alone without verifying tenant ownership.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} in releaseVentureReservation lacks a tenant_id constraint — releasing a reservation could affect another tenant's ledger row.
  - _Fix:_ Like settleVentureReservation, require tenant ownership: accept tenantId (or check the reservation row) and add AND tenant_id = ${tenantId} to the UPDATE WHERE clause, or perform a prior ownership check and abort if it doesn't match.
- **server/video-job-runner.ts:178** — DB upsert on video_jobs (db.insert(...).onConflictDoUpdate(...)) does not include an explicit tenant_id guard in the onConflict update clause. The upsert/update path can modify an existing job row regardless of its tenant (conflict is based only on jobId).
  - _Fix:_ Ensure the on-conflict update is constrained to the same tenant (e.g. add a WHERE clause that checks existing tenant_id = ${row.tenantId} or include tenant_id in the conflict target/unique key), or avoid unconditional upsert and instead do an INSERT then an UPDATE that includes tenant check. At minimum, verify the existing row's tenant matches state.tenant_id before applying the update.
- **server/video-job-runner.ts:204** — SELECT cancelRequested FROM video_jobs WHERE jobId = ${jobId} is done without scoping by tenant_id. isCancelRequested reads a tenant-scoped table by jobId alone and can return flags for jobs owned by other tenants.
  - _Fix:_ Require tenantId as an argument (or otherwise resolve ownership) and include eq(videoJobs.tenantId, tenantId) in the WHERE clause. Alternatively, validate job ownership first (read the job row with tenant check) and then read the cancel flag to avoid cross-tenant reads.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs performs a db.update(videoJobs).set(...).where(...) without any tenant_id filter — it will mark stale jobs across all tenants as failed.
  - _Fix:_ Limit the sweep to an explicit tenant (if intended to run per-tenant) or run this maintenance path under a dedicated admin/maintenance context with clear authorization. If the function is intended to operate globally, document and gate it as an admin-only maintenance operation; otherwise add tenant scoping to the WHERE clause to enforce tenant isolation.
- **server/wiring-invariants.ts:244** — Query reads from tenant-scoped table "tool_performance" (SELECT count(*) FROM tool_performance ...) with no tenant_id constraint — this returns/aggregates rows across all tenants.
  - _Fix:_ Constrain the query to a tenant (e.g. WHERE tenant_id = :tenantId) or explicitly run under a vetted admin/maintenance path that passes an ADMIN_TENANT_ID and documents the global intent. Alternatively iterate per-tenant and aggregate per-tenant results. Never issue unconstrained reads against tenant-scoped tables from request-derived contexts.
- **server/wiring-invariants.ts:258** — Query SELECT count(*)::int AS n FROM tool_performance executes with no tenant_id filter — counts rows across tenants.
  - _Fix:_ Add a tenant_id filter (WHERE tenant_id = :tenantId) or require/validate an explicit admin tenant context (ADMIN_TENANT_ID) before performing global scans. If the check truly needs global counts, make the admin privilege explicit and document it; otherwise run per-tenant.
- **server/wiring-invariants.ts:260** — Query SELECT count(*)::int AS n FROM messages WHERE created_at > ... lacks any tenant_id constraint — reads message data across tenants.
  - _Fix:_ Restrict to the calling tenant (WHERE tenant_id = :tenantId) or perform per-tenant analysis. If this is intended as a global/system metric, require an explicit admin tenant parameter (e.g. ADMIN_TENANT_ID) and ensure only privileged code paths can call it.
- **server/wiring-invariants.ts:293** — Query SELECT DISTINCT tool_name FROM tool_performance ... (recent window) is executed without a tenant_id constraint — returns tool usage across tenants.
  - _Fix:_ Add WHERE tenant_id = :tenantId (or explicit ADMIN_TENANT_ID for an intentional global audit) so results are scoped to the tenant. If you need system-wide telemetry, ensure the call is limited to authorized admin contexts and is explicit about cross-tenant scope.
- **server/wiring-invariants.ts:366** — sql.raw(`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`) probes subsystem tables (e.g. tool_performance, knowledge_nudges) without any tenant_id filtering — reading tenant-scoped tables across tenants.
  - _Fix:_ Avoid unconstrained sql.raw against tenant-scoped tables. Either add an explicit tenant_id WHERE clause (e.g. `... FROM ${spec.table} WHERE tenant_id = ${tenantId}`) or iterate tenants and run the probe per-tenant. If this is an admin/global probe, require passing an ADMIN_TENANT_ID and restrict calling contexts accordingly.
- **server/wiring-invariants.ts:417** — Tool-drift query reads from tenant-scoped table "tool_performance" (SELECT ... FROM tool_performance WHERE last_failure_at > ...) with no tenant_id constraint — aggregates failure/success counts across tenants.
  - _Fix:_ Scope the query with WHERE tenant_id = :tenantId or otherwise run per-tenant. If this check is intentionally global for operations, require an explicit admin tenant parameter and ensure only privileged code calls it. Do not leave tenant-scoped tables unread without tenant filtering.
- **server/wiring-invariants.ts:535** — Memory-growth probe runs sql.raw(`SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`) for tables that are tenant-scoped (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) with no tenant_id constraint — counts rows across tenants.
  - _Fix:_ For tenant-scoped stores, either include WHERE tenant_id = :tenantId in the query or iterate tenants and compute per-tenant counts. If a true global metric is required, make that explicit by passing and checking an ADMIN_TENANT_ID and limiting access to admin-only code paths.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*)::int AS cnt FROM graph_memory executes with no tenant_id constraint — reads 'graph_memory' across all tenants (and even the code comment references "across all tenants").
  - _Fix:_ If the intent is to check per-tenant graph_memory, add WHERE tenant_id = :tenantId or loop over tenants. If the intent is a global admin check, require and validate an explicit admin tenant context (ADMIN_TENANT_ID) and document the privileged nature of the operation. Do not leave tenant-scoped table queries unconstrained.
- **server/workflow-templates.ts:66** — Hard-coded foreign id project_id = 14 is passed into a tool that may create or update tenant-scoped resources. Using a constant project_id risks cross-tenant references without verifying ownership.
  - _Fix:_ Do not hard-code a foreign resource id. Either create the project under the caller's tenant, accept a project_id from the caller only after verifying that the referenced project belongs to context.tenantId, or validate ownership inside the tool before performing any writes. Ensure any DB writes include tenant_id explicitly.

### MEDIUM (39)

- **server/agentic/department-budgets.ts:143** — Query selects tenant-scoped rows across ALL tenants without a tenant_id WHERE filter: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This reads tenant-scoped data for all tenants in one sweep without applying per-caller tenant isolation.
  - _Fix:_ If this is intended admin/global work, ensure the code runs only in a properly-authorized admin context and document that explicitly. Otherwise, restrict the query by passing an explicit tenantId and adding `WHERE tenant_id = ${tenantId}`. If a cross-tenant sweep is required, move the logic into a clearly-audited admin job (with strong auth) or process per-tenant using a controlled list of tenant IDs to avoid accidental cross-tenant data exposure.
- **server/auto-asset-capture.ts:184** — backfillProjectAssets selects conversations across tenants with no tenant_id filter (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...). This reads a tenant-scoped table without restricting to a caller tenant.
  - _Fix:_ If this backfill is intended to run per-tenant, iterate tenants and query conversations WHERE tenant_id = ${tenantId} OR include tenant filtering in the SELECT. If it must scan globally, document and gate it as an explicit system maintenance job and ensure later operations enforce tenant ownership before writes.
- **server/auto-asset-capture.ts:207** — backfillProjectAssets reads messages for a conversation without tenant scoping (SELECT content FROM messages WHERE conversation_id = ${c.id} ...). messages is tenant-scoped and the query lacks AND tenant_id = ${c.tenant_id}, so it may read/write across tenants implicitly.
  - _Fix:_ Add tenant scoping to the messages query (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${c.tenant_id}) or otherwise fetch messages within an explicit tenant context after validating ownership (assertConversationInTenant) before reading messages.
- **server/auto-consolidation.ts:335** — checkAllTenants runs SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL without a tenant_id filter to enumerate tenants. This is an unscoped read of a tenant-scoped table.
  - _Fix:_ If enumerating tenants is required for the scheduler, keep this as a privileged maintenance action but make it explicit (documented and gated). Prefer maintaining a separate tenant registry table to enumerate active tenants, or ensure this global scan is performed only by a system maintenance job with explicit safeguards. Otherwise, avoid unscoped SELECTs and run per-tenant queries using tenant_id WHERE clauses.
- **server/auto-project.ts:112** — checkAndAutoCreateProject queries messages for a conversation without constraining by tenant (SELECT role, LEFT(content, 300) FROM messages WHERE conversation_id = ${conversationId} ...). Although the conversation row was read with tenant_id earlier, the messages read itself lacks AND tenant_id = ${tenantId}.
  - _Fix:_ Include tenant scoping on message reads (WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or otherwise fetch messages in a tenant-scoped query after validating the conversation belongs to the tenant. Ensure every read from messages includes tenant_id or is executed inside an explicitly documented privileged job.
- **server/channel-routing.ts:31** — Query joins channel_routes -> personas without constraining personas by tenant (JOIN personas p ON p.id = cr.persona_id). If personas is tenant-scoped this reads tenant data with no tenant_id filter.
  - _Fix:_ Either store persona display/name in channel_routes to avoid the join, or include a tenant context and join with an explicit tenant filter (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}). If channel routing is intentionally global, ensure the code runs only in an appropriate admin/global context and documents the intent.
- **server/capability-review.ts:416** — Constructing a Postgres array literal (arrLiteral) from untrusted review.doNotRebuild names and embedding it into the INSERT statement (used as ${arrLiteral}::text[]) — this builds SQL-affecting text from request-derived values and risks injection/isolation mistakes.
  - _Fix:_ Avoid building SQL array literals by string concatenation. Use a parameterized array binding or store the list as jsonb (e.g. pass JSON.stringify(surfaced) and cast to jsonb) or use a driver/ORM API that supports passing JS arrays to Postgres as text[] parameters. If a literal is unavoidable, ensure robust escaping and prefer passing a typed parameter (driver-level array) rather than hand-crafted SQL fragments.
- **server/chat-engine.ts:3697** — markReviewReused(conversationId, toolName) is invoked (fire-and-forget) with only conversationId and toolName and no tenant context. If markReviewReused persists telemetry tied to a conversation, it must be constrained by tenant.
  - _Fix:_ Pass tenantId explicitly to markReviewReused or have the function validate the conversation belongs to the tenant (fetch conversation with tenant_id filter) before persisting any rows.
- **server/chat-engine.ts:4015** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is called without tenant context. If this routine persists detection/diagnostic records, they may be written without tenant scoping.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection or ensure it performs an ownership check on conversationId (enforce WHERE tenant_id = tenantId) before any DB writes.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM tenant-scoped table 'commitments' without scoping to a caller tenant — this enumerates tenant rows across all tenants (SELECT reads tenant-scoped data without a tenant equality filter).
  - _Fix:_ If this is intended admin/system work, ensure the route/process is strictly admin-only and audited. Otherwise, avoid broad cross-tenant SELECTs; require an explicit admin privilege check or move this logic into an admin-only path. If the intent is to act per-tenant from application context, query only the caller's tenant_id.
- **server/credential-vault.ts:169** — UPDATE on credential_vault missing tenant filter — `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` can update a credential row for a different tenant.
  - _Fix:_ Include tenant scoping in the UPDATE: WHERE id = ${id} AND tenant_id = ${tenantId}. Ensure updates to tenant-scoped rows always include the caller's tenant_id.
- **server/credential-vault.ts:176** — UPDATE on credential_vault missing tenant filter — `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` can update another tenant's row when called without tenant scoping.
  - _Fix:_ Use WHERE id = ${cred.id} AND tenant_id = ${tenantId} (pass tenantId into this function if not already available). All mutations on tenant-scoped tables must include tenant_id constraints.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint. Even though runId was created earlier for a tenant, the update is not explicitly tenant-scoped.
  - _Fix:_ Include tenant constraint in the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) so the write is provably scoped to the caller's tenant.
- **server/felix-loop.ts:689** — Error-path UPDATE on 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint when recording run errors.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) before updating run state/errors.
- **server/firecrawl.ts:213** — UPDATE of scrapedPages uses .where(eq(scrapedPages.id, existing[0].id)) without also scoping by tenant_id. The existing row was looked up with tenant filter, but the UPDATE itself lacks the tenant constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the write cannot affect another tenant's row even if an id collision or logic flaw occurs.
- **server/firecrawl.ts:336** — UPDATE of scrapedPages inside crawl loop uses .where(eq(scrapedPages.id, existing[0].id)) without tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE with the tenant id (e.g. AND eq(scrapedPages.tenantId, tenantId)) to enforce tenant-local writes.
- **server/google-drive.ts:560** — UPDATE projects SET driveFolderId/driveFolderUrl uses WHERE eq(projects.id, projectId) with no tenant_id constraint after earlier ownership checks.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so the write is explicitly constrained to the tenant-owning project row.
- **server/google-drive.ts:1066** — SELECT to fetch projects.driveFolderId (WHERE eq(projects.id, params.projectId)) during Drive-search fallback has no tenant_id constraint — reading a tenant-scoped table without a SQL tenant filter (this happens after an earlier ownership check but the query itself is unscoped).
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure the Drive-folder lookup cannot return another tenant's folder info.
- **server/job-queue.ts:385** — getJobStats() aggregates counts from agent_jobs (SELECT status, COUNT(*) ... WHERE created_at > ...) with NO tenant_id filter, returning cross-tenant statistics where a caller might expect tenant-scoped data.
  - _Fix:_ Make tenant scoping optional but explicit: accept a tenantId parameter and, when present, add WHERE tenant_id = ${tenantId}. Ensure endpoints exposing these stats enforce proper auth/authorization for global vs tenant-scoped views.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor() runs a query grouping agent_knowledge by tenant_id, persona_id to find pairs to process (SELECT tenant_id, persona_id, COUNT(*) ... HAVING COUNT(*) >= ${MIN_ENTRIES}) — this read touches a tenant-scoped table without constraining to a caller tenant.
  - _Fix:_ If this background job intentionally scans across tenants, ensure it runs only in an authorized operator context and is audited. Otherwise, change the design to accept/iterate over a single tenantId or otherwise enforce tenant-scoped queries (e.g. iterate tenant list in an admin-only routine and process each tenant with explicit tenant_id filters).
- **server/lib/agent-trace.ts:124** — UPDATE against tenant-scoped table agent_trace_spans omits tenant_id in WHERE clause (UPDATE ... WHERE span_id = ${spanId}). This allows a close operation to touch any row with the same span_id across tenants.
  - _Fix:_ Include the tenant predicate on the UPDATE. Persist the tenantId alongside the spanId when opening the span (e.g. store tenantId in the _openPromises/context map) and change the UPDATE to WHERE span_id = ${spanId} AND tenant_id = ${tenantId} so closes are tenant-scoped.
- **server/lib/action-ledger.ts:139** — prepareAttempt INSERT into action_attempts writes foreign ids (plan_id, run_id, conversation_id) taken from caller input without validating that those referenced rows belong to the caller's tenant.
  - _Fix:_ Before inserting, validate any provided foreign id (planId, runId, conversationId, etc.) by fetching the referenced row(s) with a tenant_id = ${i.tenantId} predicate (or otherwise asserting ownership). Reject or null-out foreign ids that do not belong to the tenant.
- **server/lib/commerce-catalog.ts:91** — registerProduct INSERT into commerce_products stores mission_id and post_purchase_sequence_id from caller input without verifying those referenced resources belong to the provided tenantId.
  - _Fix:_ Before persisting missionId/postPurchaseSequenceId, verify ownership by querying the referenced tables with WHERE tenant_id = ${input.tenantId} AND id = ${missionId|postPurchaseSequenceId}. Fail/normalize when the referenced row is missing or belongs to another tenant.
- **server/lib/decline-events.ts:85** — recordDecline INSERT into decline_events includes persona_id and conversation_id from caller input without verifying those foreign ids belong to the provided tenantId.
  - _Fix:_ Validate personaId and conversationId ownership before inserting (e.g. SELECT the rows with WHERE tenant_id = ${opts.tenantId} AND id = ${personaId|conversationId}) or otherwise reject inputs that reference rows outside the tenant.
- **server/orchestrator-ledger.ts:445** — Use of sql.raw(${lockKey}) in SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint) — lockKey is derived from ledgerKey (an external/caller-provided option). Using sql.raw with request-derived data risks SQL-injection or accidental SQL context embedding.
  - _Fix:_ Avoid sql.raw with caller-controlled values. Pass the numeric lock key as a parameterized binding (e.g. compute as a BigInt and use pg_advisory_xact_lock(${BigInt(lockKey)}::bigint) or build the lock value server-side and bind it with the normal SQL parameter interpolation) so Drizzle parameterization is used rather than sql.raw.
- **server/research-engine.ts:322** — Query SELECT name FROM personas WHERE id = ${program.persona_id} reads a persona row without any tenant scoping/ownership check. If personas are tenant-scoped, this allows cross-tenant reads.
  - _Fix:_ If personas are tenant-scoped, add tenant filtering (e.g. WHERE id = ${program.persona_id} AND tenant_id = ${tenantId}) or otherwise document and enforce that personas are intentionally global. Prefer using a helper that validates tenant ownership before returning persona data.
- **server/research-engine.ts:980** — resolvePersonaId(personaSlug, _tenantId) issues SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 but ignores the provided tenantId parameter — persona lookup is not tenant-scoped.
  - _Fix:_ Use the provided tenantId in the query (e.g. WHERE name = ${personaSlug} AND tenant_id = ${tenantId} LIMIT 1) or explicitly document personas as global. Failing to scope by tenant risks cross-tenant leakage when personas are per-tenant.
- **server/routes.ts:764** — SELECT from tenant-scoped table `projects` without tenant constraint in backfillProjectDriveFolders(); the query selects projects across all tenants (no WHERE tenant_id = ...)
  - _Fix:_ Scope the query to an explicit tenant or run with a verified platform-admin context. If cross-tenant work is intended, perform it under an audited admin-only flow and/or iterate tenants explicitly (SELECT tenants first, then operate per-tenant with WHERE projects.tenant_id = <tid>) so every tenant-scoped access is explicitly authorized and auditable.
- **server/routes.ts:1529** — Public stats endpoint queries tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) without any tenant_id constraint, exposing aggregated per-tenant data across the whole platform.
  - _Fix:_ If the intent is to expose system-wide aggregate metrics, mark this explicitly and ensure admin-only access. Otherwise, restrict counts to the caller's tenant (WHERE tenant_id = <caller tenant>) or compute tenant-aggregates via an admin-only path that enforces platform-admin checks before running cross-tenant queries.
- **server/routes.ts:1558** — /api/public/architecture runs multiple SELECT count(*) FROM <tenant-scoped table> queries (conversations, messages, personas, projects, skills, memory_entries, etc.) without tenant_id filters, reading data across all tenants.
  - _Fix:_ Restrict these queries to the caller's tenant or gate the endpoint to platform admins only. If system-wide aggregates are required, run them in a clearly-audited admin-only context and avoid unauthenticated cross-tenant reads; otherwise add WHERE table.tenant_id = <caller tenant> (or equivalent Drizzle eq filter).
- **server/routes/goal-ledger.ts:92** — SELECT on `agent_jobs` omits tenant scoping (SELECT ... FROM agent_jobs j LEFT JOIN tenants t ON t.id = j.tenant_id WHERE j.status IN (...) LIMIT 20). This returns active jobs across all tenants without an explicit tenant filter.
  - _Fix:_ If the intent is to show only admin-owned/system jobs, add an explicit tenant filter (e.g. WHERE j.tenant_id = ${ADMIN_TENANT_ID}) or otherwise document/guard that this admin-only route intentionally returns cross-tenant rows. If the route should be tenant-scoped, add AND j.tenant_id = ${tenantId} (or equivalent) to the WHERE clause.
- **server/routes/metrics.ts:79** — SELECT from agent_cost_ledger grouping by tool_name (tool usage counts) runs across all tenant rows with no tenant_id predicate — reads tenant-scoped data globally.
  - _Fix:_ Restrict to a single tenant when used from tenant-scoped UI (add WHERE tenant_id = ${tenantId}) or gate cross-tenant aggregation behind an explicit platform-admin/operator check and move heavy cross-tenant scans to an audited analytics pipeline to reduce blast radius.
- **server/routes/metrics.ts:95** — SELECT SUM(tokens_in/tokens_out) FROM agent_cost_ledger uses no tenant_id predicate and therefore aggregates across all tenant rows.
  - _Fix:_ As above: either scope to the caller's tenant (WHERE tenant_id = ${tenantId}) or ensure the endpoint is strictly operator-only and runs with explicit platform-admin authorization and auditing. Consider pre-aggregating token totals into a dedicated ops-only table to avoid ad-hoc cross-tenant queries.
- **server/routes/metrics.ts:113** — SELECT from repair_incidents over the last 7 days has no tenant_id predicate; if repair_incidents is tenant-scoped this is an unscoped cross-tenant read.
  - _Fix:_ If repair_incidents contains tenant-scoped data, add WHERE tenant_id = ${tenantId} for tenant-scoped callers, or require explicit platform/operator auth for cross-tenant aggregation and restrict/ log this action. Prefer moving cross-tenant analytics to an audited operator-only path.
- **server/routes/oauth-subscriptions.ts:325** — Cron 'heal' path executes: SELECT DISTINCT tenant_id FROM oauth_subscriptions WHERE provider IN (...) AND is_active = FALSE — this scans oauth_subscriptions across all tenants with no tenant filter.
  - _Fix:_ If the cron is intentionally global, ensure the endpoint can only be triggered by a strongly protected operator secret (already present) and add explicit platform-admin audit logging. Alternatively scope the query to specific tenant(s) passed by a safe, validated parameter or run the global scan from a maintenance/analytics worker process rather than a public HTTP handler.
- **server/self-improvement.ts:546** — INSERT into `experiments` uses tenantId: data.tenantId! with a non-null assertion but no runtime validation. If caller passes undefined or an untrusted/incorrect tenantId, the insert may fail or record incorrect tenant ownership.
  - _Fix:_ Validate that data.tenantId is a positive integer and that it equals the caller's authenticated tenant (or otherwise derive tenant from authoritative context). Do not rely on a TypeScript non-null assertion for runtime correctness; fail if tenantId is missing or untrusted.
- **server/storage.ts:245** — getConversationUnscoped performs: SELECT ... FROM conversations WHERE id = ${id} with no tenant constraint. This function is explicitly unscoped by design but is a direct read of a tenant-scoped table without tenant validation.
  - _Fix:_ If this unscoped API is unavoidable, keep it strongly internal-only, log and audit all usages (already done), and ensure callers ALWAYS validate the returned row's tenantId before proceeding. Prefer returning tenantId alongside row and requiring callers to pass tenantId into subsequent storage calls.
- **server/storage.ts:875** — getMemoryStats accepts optional tenantId but, when tenantId is undefined, selects memory_entries and agent_knowledge without tenant scoping. Optional omission leads to unscoped cross-tenant reads.
  - _Fix:_ Fail-closed: require tenantId for tenant-scoped stats or explicitly document and gate global stats for admin-only use. Prefer using tenantScope helper to enforce scoping.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} executes without scoping by tenant. Reading a tenant-scoped table without an explicit tenant constraint violates the tenant-isolation invariant.
  - _Fix:_ Avoid querying conversations by id alone. Instead, verify ownership atomically (e.g. SELECT tenant_id FROM conversations WHERE id = ${convId} AND tenant_id = ${claimedTenant} LIMIT 1) or perform a join from agent_knowledge->conversations that enforces equality on tenant_id so you never perform unconstrained reads across tenants.

### Suppressed — confirmed false positives (168)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without any tenant_id constraint (reads experiments/metadata across all tenants).
  - _Why false positive:_ Heartbeat maintenance sweep intentionally scans running experiments across tenants and processes each row with its own tenant_id for updates; no tenant-facing mixed output shown.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() performs an UPDATE on the tenant-scoped agent_runs table using only the run id (WHERE id = runId) with no tenant_id constraint, allowing cross-tenant modifications if a runId from another tenant is supplied.
  - _Why false positive:_ Server-side helper updates agent_runs by globally unique run id created server-side; no untrusted request boundary shown for runId in this call path.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs with WHERE eq(agentRuns.id, runId) but does not constrain by tenant_id. This allows an attacker or buggy code with a runId from another tenant to alter that tenant's state.
  - _Why false positive:_ Internal worker/state helper mutates agent_runs by server-generated primary key runId, not shown as coming from req/body/params/tool args.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() replaces state on agent_runs using only the run id in the WHERE clause (no tenant_id filter), enabling cross-tenant state overwrite by runId.
  - _Why false positive:_ Replace-state helper operates on server-created run primary key and no cross-tenant request-originated runId is evidenced here.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates an agent_runs row to mark completion using only the run id in the WHERE clause (no tenant_id constraint), allowing modification of runs across tenants by id.
  - _Why false positive:_ Completion helper updates a run by internal server-generated PK; subsequent read also uses same internal runId and no untrusted boundary is shown.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs to a failed state using only the run id in the WHERE clause (no tenant_id constraint), permitting cross-tenant updates if runId from another tenant is provided.
  - _Why false positive:_ Failure-state helper appears internal and keyed only by server-generated agent_run id, with no evidence runId is attacker-controlled.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() sets status/state on agent_runs using only the run id in the WHERE clause (no tenant_id constraint), enabling cross-tenant pauses via runId alone.
  - _Why false positive:_ Pause helper mutates by internal run primary key; snippet shows surrounding helpers are internal run-lifecycle functions, not direct tenant request handlers.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — The candidate SELECT (lines ~66–85) reads agent_runs JOIN agent_approvals without a tenant_id WHERE filter — it scans rows across tenants. Per the project's rule that tenant isolation is enforced by app-level SQL filters, unscoped SELECTs over tenant-scoped tables risk leaking cross-tenant data.
  - _Why false positive:_ Background sweep intentionally scans pending approvals across tenants, joins on matching tenant_id, and handles each candidate under its row-carried tenant_id.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — runDueWakes issues a cross-tenant UPDATE/SELECT on agent_wake_schedules without constraining by tenant_id. The inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) and the outer UPDATE do not restrict rows to a specific tenant, allowing claims/updates across tenants.
  - _Why false positive:_ Heartbeat scheduler is a platform-wide maintenance loop claiming due wake rows across tenants and emitting per-row events with each row's own tenant_id.
- **server/embeddings.ts:684** _(system-sweep)_ — storeEmbeddingVec performs an UPDATE on a tenant-scoped table (memory_entries or agent_knowledge) using only id (and a validated table name) but does NOT constrain the UPDATE to the caller's tenant (no WHERE tenant_id = ...). This allows cross-tenant writes by id.
  - _Why false positive:_ Helper is used here by embedding backfill housekeeping to update only embedding fields on rows selected by the sweep; no tenant-facing data mixing shown.
- **server/embeddings.ts:726** _(system-sweep)_ — backfillEmbeddingVecs performs UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} without constraining the UPDATE by tenant_id — cross-tenant write by id is possible.
  - _Why false positive:_ Backfill loop is platform-wide housekeeping updating only embedding_vec on selected rows; operates per row and does not expose mixed tenant data.
- **server/embeddings.ts:743** _(system-sweep)_ — backfillEmbeddingVecs executes UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} without a tenant_id constraint — this updates rows across tenants by id.
  - _Why false positive:_ Backfill of knowledge embeddings is a maintenance sweep mutating only embedding housekeeping fields across rows.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs() atomically claims jobs via an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... LIMIT ${limit} FOR UPDATE SKIP LOCKED) with NO tenant_id filtering. A worker can therefore claim and run jobs for any tenant.
  - _Why false positive:_ Global worker claim loop intentionally operates across tenants and returns each claimed row with its own tenant_id for per-row handling.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases() issues UPDATEs against agent_jobs (marking terminal failed or requeued) with NO tenant_id constraint, affecting running/pending jobs across all tenants.
  - _Why false positive:_ Lease reclamation is platform-wide housekeeping changing only job status/lease fields, with no tenant data returned to tenant-facing surfaces.
- **server/job-queue.ts:148** _(system-sweep)_ — Second step in reclaimExpiredLeases() re-queues expired 'running' rows back to pending via UPDATE agent_jobs ... WHERE status = 'running' AND lease_until < NOW() WITHOUT tenant filtering, possibly re-queuing other tenants' jobs incorrectly.
  - _Why false positive:_ Requeue of expired leases is a global maintenance transition on job status fields only.
- **server/lib/scheduled-post-runner.ts:60** _(system-sweep)_ — CTE selects/updates tenant-scoped table `scheduled_posts` without any tenant_id constraint (SELECT id FROM scheduled_posts ... FOR UPDATE / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). This updates rows across tenants without binding the operation to an explicit tenant context.
  - _Why false positive:_ Heartbeat runner is a platform-wide dispatcher that claims due rows and carries tenant_id per picked row for subsequent per-row processing.
- **server/lib/scheduled-post-runner.ts:164** _(system-sweep)_ — UPDATE on tenant-scoped table `scheduled_posts` omits tenant_id in the WHERE clause (UPDATE scheduled_posts ... WHERE id = ${row.id}). Writing tenant rows by id-only can accidentally modify another tenant's row.
  - _Why false positive:_ Worker updates the same claimed scheduled_post row by its id as part of global dispatch housekeeping/output status; row was selected earlier and includes tenant_id per row.
- **server/lib/scheduled-post-runner.ts:191** _(system-sweep)_ — Error-path UPDATE on tenant-scoped table `scheduled_posts` omits tenant_id in the WHERE clause (UPDATE scheduled_posts ... WHERE id = ${row.id} AND status = 'publishing').
  - _Why false positive:_ Error-path release/update is part of the same global scheduled-post worker, mutating only the previously claimed row's status/lock fields.
- **server/memory-graph.ts:209** _(fk-transitive)_ — INSERT INTO memory_links does not include tenant_id (INSERT INTO memory_links (source_memory_id, target_memory_id, ... ) VALUES (...)). If memory_links is a tenant-scoped table, tenant_id must be set explicitly on every INSERT; otherwise links can cross tenants or be attributed incorrectly.
  - _Why false positive:_ memory_links has no tenant_id by schema and links tenant-scoped memory_entries via foreign keys, so omission of tenant_id on insert matches transitive isolation design.
- **server/memory-graph.ts:408** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} is performed without constraining tenant_id. This can update a memory entry belonging to a different tenant if ids are misused or attacker supplies ids.
  - _Why false positive:_ Rows updated here come only from the immediately prior SELECT on memory_entries scoped by tenant_id = tenantId; mem.id is sourced from that scoped result set.
- **server/memory-intelligence.ts:271** _(upstream-verified)_ — db.execute(sql`UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}`) is executed without constraining tenant_id. Even though entry.id was just created, the UPDATE must still include tenant scoping to prevent cross-tenant modification if misused.
  - _Why false positive:_ entry.id was just created via storage.createMemoryEntry with the same tenantId in this call path, so the async category update targets that newly created tenant-owned row.
- **server/minerva-planner.ts:459** _(upstream-verified)_ — Rollback UPDATE on tenant-scoped table `plans` executed without tenant_id constraint (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'). This can roll back or mutate another tenant's plan.
  - _Why false positive:_ This rollback occurs only after the prior CAS UPDATE RETURNING at line 355 claimed the specific row and returned row.tenant_id for the same planId in the same internal flow.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` with WHERE is_active = TRUE but no tenant_id constraint. This query even selects refresh_token (sensitive fields) across all tenants at startup (validateSubscriptionsOnStartup).
  - _Why false positive:_ Startup validation is a platform-wide maintenance sweep over active subscriptions; it processes each row with its own tenant_id and does not return mixed tenant data to a tenant-facing surface.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — UPDATE plans in appendExecutionLog does not constrain by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows modifying a plan row without verifying the caller/processor's tenant.
  - _Why false positive:_ appendExecutionLog is an internal executor helper operating on a server-scheduled planId primary key, not a request-derived tenant boundary input.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Claiming the plan uses UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' with NO tenant_id constraint. This mutates a tenant-scoped table by id only, allowing cross-tenant claims if planId is attacker-controllable.
  - _Why false positive:_ executePlan is an internal worker claiming a plan by server-driven primary key for background execution, matching the internal-worker-pk pattern.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status = 'failed' ... WHERE id = ${planId} (no tenant_id filter). Writes to tenant-scoped table are not constrained to the plan's tenant.
  - _Why false positive:_ This failure update is in the same executePlan call after the claim RETURNING fetched the exact plan row, including tenant_id; it is mutating that already-claimed row.
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final UPDATE of plans (setting status = completed/failed and appending execution_log) uses WHERE id = ${planId} with no tenant_id constraint, allowing cross-tenant state changes.
  - _Why false positive:_ Final status write occurs in the same worker flow after the plan was claimed and loaded by the earlier CAS UPDATE RETURNING for this planId.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' lacks a tenant_id constraint; an exception handler is mutating tenant-scoped rows by id only.
  - _Why false positive:_ The rescue UPDATE is in an internal background worker handling a server-invoked plan execution keyed by planId, not a direct untrusted request parameter.
- **server/plan-executor.ts:775** _(system-sweep)_ — resumeStuckPlans executes UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id with NO tenant_id filter. This scans/mutates all tenants' plans in one statement.
  - _Why false positive:_ resumeStuckPlans is boot-time housekeeping across all tenants, only resetting status/updated_at and re-queueing by per-row id without exposing mixed tenant content.
- **server/plan-executor.ts:785** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is executed with no tenant_id filter — the executor will enumerate approved plans across all tenants.
  - _Why false positive:_ Boot-time recovery intentionally scans plans across tenants to restart stuck/approved work; it only queues per-plan executePlan(id) and returns aggregate counts, not tenant data.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId} has NO tenant_id constraint. A caller supplying proposalId can read proposals across tenants.
  - _Why false positive:_ verifyProposalById is a server worker function operating on a globally unique proposal PK argument, not a request boundary shown here.
- **server/proposal-verifier.ts:241** _(internal-worker-pk)_ — persist() updates code_proposals (UPDATE code_proposals SET verification_status=..., verified_at=NOW() WHERE id = ${proposalId}) without constraining by tenant_id, allowing cross-tenant mutation of proposals.
  - _Why false positive:_ persist is an internal helper updating the same code_proposals row by server-carried proposalId from verifier workflow, not directly from untrusted caller input.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — In attemptRebaseOnDrift the updater that sets code_proposals status to 'rejected' and writes validation_result uses WHERE id = ${proposalId} with no tenant filter; likewise the later UPDATE that writes the rebased code_diff uses WHERE id = ${proposalId} without tenant_scoping.
  - _Why false positive:_ attemptRebaseOnDrift mutates the proposal identified by the worker's server-side proposalId PK during internal verification/rebase processing.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE on tenant-scoped table `agent_knowledge` uses only WHERE id = ${r.id} and lacks a tenant_id constraint. This can allow cross-tenant modification if ids are misused or logic changes.
  - _Why false positive:_ Scheduler loop processes due rows across tenants and updates only the current row's housekeeping payload by its own id; no tenant data is returned or mixed.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses only WHERE id = ${storedId} and omits tenant_id (and persona_id) in the WHERE clause, allowing potential cross-tenant writes.
  - _Why false positive:_ storedId comes immediately from a prior SELECT on memory_entries constrained by tenant_id and persona_id in the same call path, so the update targets that verified row.
- **server/research-engine.ts:158** _(system-sweep)_ — cleanupZombieSessions() runs an UPDATE on research_sessions without scoping by tenant_id — this will mark sessions from all tenants as completed (mass cross-tenant modification).
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping update of status/ended_at/summary on stale sessions; it returns only ids/counts and does not expose mixed tenant content.
- **server/routes.ts:4330** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The statement `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, allowing creation of a note not tied to the caller's tenant (data corruption/cross-tenant contamination).
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively through project_id to tenant-scoped projects; omitting tenant_id on insert is by design.
- **server/sculptor.ts:94** _(upstream-verified)_ — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET status = 'failed' WHERE id = ${session.id}). This allows cross-tenant mutation if ids collide or are guessed.
  - _Why false positive:_ Updates the row just inserted with tenant_id=params.tenantId; session.id comes from RETURNING on that insert, not cross-tenant input.
- **server/sculptor.ts:101** _(upstream-verified)_ — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}). Missing tenant filter can permit cross-tenant writes.
  - _Why false positive:_ Same call path: session row was created immediately above with tenant_id=params.tenantId and session.id is derived from that insert result.
- **server/sculptor.ts:162** _(upstream-verified)_ — UPDATE to sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). This unscoped update can modify another tenant's session row.
  - _Why false positive:_ monitorSession is invoked with session.id and tenantId from createAgentSession; sessionId is bound to the newly created tenant-scoped row in the same flow.
- **server/seed.ts:2860** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table by id only, without verifying tenant_id — this can modify records belonging to other tenants.
  - _Why false positive:_ Boot-time repair sweep updates housekeeping/config field model across rows and does not return tenant data to tenant-facing surfaces.
- **server/seed.ts:3137** _(upstream-verified)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... updates the projects table without a tenant_id constraint — a cross-tenant write risk.
  - _Why false positive:_ Guarded by existingDeploy check that specifically verifies owner deployment/tenant 1; targeted one-off owner-data seed mutation.
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is executed without setting tenant_id — inserting into a tenant-scoped table without tenant_id will create rows that are not bound to the caller's tenant (and may violate NOT NULL constraints).
  - _Why false positive:_ project_conversations schema note says tenant_id column: NO; join table is tenant-isolated transitively via tenant-scoped project/conversation parents.
- **server/seed.ts:4431** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without a tenant_id filter (DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'). heartbeat_tasks is tenant-scoped (indexes reference tenant_id), so this DELETE may remove rows for all tenants, causing cross-tenant data loss/configuration changes.
  - _Why false positive:_ Platform-wide seed cleanup deletes only stale auto-disabled housekeeping rows and does not mix or expose tenant data.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant_id. The query `db.select().from(messages).where(eq(messages.conversationId, conversationId))` lacks any `tenant_id` filter, allowing cross-tenant reads if an attacker supplies or guesses a conversationId.
  - _Why false positive:_ Same call path first calls analyzeConversation(conversationId, tenantId), which verifies the conversation belongs to tenantId before reading messages by that conversationId.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant_id. The query `db.select().from(messages).where(eq(messages.conversationId, conversationId))` in analyzeConversation omits tenant scoping and can return another tenant's messages.
  - _Why false positive:_ Conversation ownership is checked immediately above with conversations.id + conversations.tenantId before selecting messages for that conversation.
- **server/skillify.ts:221** _(global-table)_ — SELECT from tenant-scoped table `personas` without validating tenant ownership. The query `.where(eq(personas.id, conv[0].personaId))` does not verify that the persona belongs to the same tenant, permitting cross-tenant disclosure or misuse of persona records.
  - _Why false positive:_ Referenced schema says personas has no tenant_id, so this lookup is against a non-tenant-scoped/global table.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without constraining by tenant_id. The recentMsgs query `db.select().from(messages).where(eq(messages.conversationId, conversationId))` in skillifyConversation omits tenant scoping and can leak messages across tenants.
  - _Why false positive:_ skillifyConversation first calls analyzeConversation(conversationId, tenantId), which fail-closed verifies tenant ownership of the conversation.
- **server/storage.ts:838** _(system-sweep)_ — archiveExpiredMemories issues: UPDATE memory_entries SET status = 'archived' WHERE ... (expires_at <= NOW()) — no tenant_id constraint. This will archive rows across all tenants.
  - _Why false positive:_ This is a platform-wide housekeeping archive sweep that only flips status on expired rows and returns counts/ids, not tenant data to a tenant-facing surface.
- **server/storage.ts:855** _(system-sweep)_ — archiveStaleMemories executes: UPDATE memory_entries SET status = 'archived' WHERE status = 'active' AND created_at <= ${cutoff} ... — no tenant scoping. Unscoped mass mutation on a tenant-scoped table.
  - _Why false positive:_ This is a platform-wide stale-memory housekeeping sweep that only mutates archival status and returns counts/ids, with no cross-tenant data mixing.
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` uses only the row id in the WHERE clause (eq(customTools.id, tool.id)) and omits tenant_id. Without tenant scoping this can modify another tenant's row if ids are guessed or otherwise abused.
  - _Why false positive:_ The row being updated is the same tenant-scoped tool row previously selected in executeCustomTool with customTools.tenantId = tenantId, so the id is already tenant-validated in this flow.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE incrementing `usageCount` on tenant-scoped table `custom_tools` uses only the row id in the WHERE clause and omits tenant_id. This can corrupt usage counters for other tenants' tools.
  - _Why false positive:_ tool was selected just above with both name and customTools.tenantId = tenantId, so incrementing by that validated row id is in the same verified call path.
- **server/tools.ts:2876** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) in the project CREATE flow does not set tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ Referenced schema says project_conversations has no tenant_id; link row is transitively isolated via tenant-scoped parent project/conversation, and the conversation link is prevalidated by tenant-scoped UPDATE above.
- **server/tools.ts:2979** _(fk-transitive)_ — INSERT INTO project_files does not include tenant_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (...)). Tenant-scoped tables must receive tenant_id on INSERT.
  - _Why false positive:_ project_files has no tenant_id and insert is gated by owned projects row scoped by tenant_id
- **server/tools.ts:2993** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author) does not include tenant_id. Tenant-scoped tables require tenant_id on writes.
  - _Why false positive:_ project_notes has no tenant_id and insert is preceded by tenant-scoped project ownership check
- **server/tools.ts:3018** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) in link_conversation handler omits tenant_id on INSERT.
  - _Why false positive:_ project_conversations has no tenant_id and both parent project and conversation are verified in tenant before insert
- **server/agentic-engines.ts:162** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` (retryPendingMinervaRouting) without tenant scoping — reads applied/high insights across tenants.
  - _Why false positive:_ platform-wide durability sweep intentionally scans all tenants and processes each row with its own row-carried tenant_id
- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) RETURNING ... — no tenant_id constraint. This bulk update may modify rows across all tenants.
  - _Why false positive:_ bulk housekeeping sweep intentionally updates status fields across tenants and returns rows only for internal follow-up
- **server/agentic-engines.ts:344** _(global-table)_ — SELECT aggregated counts from `heartbeat_logs` without tenant constraint in gatherOperationalData (reads logs across tenants).
  - _Why false positive:_ heartbeat_logs schema note shows no tenant_id, indicating platform-global operational logs
- **server/agentic-features.ts:524** _(upstream-verified)_ — UPDATE lead_enrichments SET ... WHERE id = ${lead.id} lacks tenant_id constraint — an update against a tenant-scoped table without tenant scoping.
  - _Why false positive:_ updated row ids come from prior SELECT of lead_enrichments scoped to params.tenantId in same function loop
- **server/agentic-features.ts:671** _(upstream-verified)_ — UPDATE outreach_enrollments ... WHERE id = ${enrollment.id} omits tenant_id — performing a write against a tenant-scoped table without tenant constraint.
  - _Why false positive:_ Update uses enrollment.id selected earlier in same flow from tenant-scoped processing with params.tenantId; ownership is established before this write.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE outreach_enrollments ... WHERE id = ${row.id} (classifyReply) has no tenant_id constraint — writing to a tenant-scoped table without tenant scoping.
  - _Why false positive:_ row.id comes from prior SELECT constrained by e.tenant_id = params.tenantId, so the updated enrollment was already ownership-checked in the same call path.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE agentApprovals to status='expired' WHERE status='pending' AND expiresAt < NOW() executes without tenant_id scoping — modifies approvals across all tenants.
  - _Why false positive:_ This is a global expiry sweep over stale pending approvals; it returns per-row tenantId for follow-up handling and performs housekeeping/status updates only.
- **server/auto-memorize.ts:163** _(system-sweep)_ — runAutoMemorize pulls messages across all tenants in a single unscoped query (SELECT m.tenant_id, m.role, m.content ... FROM messages ... WHERE created_at > ...). Although the code partitions rowsByTenant afterwards, the initial read is an unscoped read of a tenant-scoped table without a tenant_id WHERE clause, violating the 'must filter by tenant' invariant and increasing blast radius.
  - _Why false positive:_ This is a maintenance sweep that scans messages across tenants but explicitly partitions by row-carried tenant_id before any per-tenant synthesis, avoiding mixed-tenant output.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into invoice_items does not set tenant_id. If invoice_items is a tenant-scoped table, this writes rows without the caller's tenant context and can link or leak data across tenants.
  - _Why false positive:_ invoice_items has no tenant_id and is a line-item child of tenant-scoped invoices; inserting by invoice_id only is correct by design.
- **server/ceo-orchestrator.ts:841** _(global-table)_ — SELECT from personas table without tenant_id constraint (cachedPersonas = await db.select().from(personasTable)). This reads the tenant-scoped personas table globally and can expose/choose persona rows from other tenants.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so this is not a tenant-scoped table read.
- **server/chat-engine.ts:572** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` omits a tenant_id constraint (WHERE tenant_id = ${tenantId}). The query is: SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ...
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; project ownership was verified just above via projects.id + tenant_id before reading by project_id.
- **server/chat-engine.ts:583** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` omits a tenant_id constraint. The query is: SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ...
  - _Why false positive:_ project_notes has no tenant_id and is scoped transitively by verified project_id ownership noted above
- **server/chat-engine.ts:632** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` omits a tenant_id constraint. The query is: SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ...
  - _Why false positive:_ c.id comes from prior conversations query constrained by c.tenant_id = tenantId in same call path
- **server/chat-engine.ts:650** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` omits a tenant_id constraint. The query is: SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC
  - _Why false positive:_ messages are read for c.id values already selected from conversations with c.tenant_id = tenantId above
- **server/chat-engine.ts:766** _(upstream-verified)_ — LATERAL subqueries select from tenant-scoped table `messages` without a tenant_id constraint. Example: (SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ORDER BY created_at DESC LIMIT 1) — no tenant_id filter.
  - _Why false positive:_ subqueries are correlated to c.id from outer conversations rows filtered by c.tenant_id = tid in same query
- **server/chat-engine.ts:772** _(upstream-verified)_ — LATERAL subquery reads `compaction_archives` without a tenant_id constraint. Example: (SELECT summary FROM compaction_archives WHERE conversation_id = c.id ORDER BY archived_at DESC LIMIT 1) — missing tenant filter.
  - _Why false positive:_ archive subquery is correlated to tenant-scoped outer conversations rows filtered by c.tenant_id = tid
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table 'commitments' without tenant_id constraint in scanAndEscalate — SELECT id, tenant_id, ... FROM commitments WHERE status='active' AND due_at < NOW() will return rows across all tenants.
  - _Why false positive:_ boot/scheduled scanner intentionally sweeps all tenants and uses per-row tenantId while emitting redacted admin digest only
- **server/data-protection.ts:117** _(system-sweep)_ — permanentlyPurgeSoftDeleted selects expired conversations across all tenants (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) with NO tenant_id constraint, reading tenant-scoped data globally.
  - _Why false positive:_ global purge job intentionally scans expired rows across tenants and processes each row with its own tenant_id
- **server/data-protection.ts:182** _(upstream-verified)_ — backupConversationToDrive reads messages with SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} — messages is tenant-scoped elsewhere, but this SELECT omits tenant_id and therefore can read messages across tenants if conversation_id is ambiguous or misused.
  - _Why false positive:_ backupConversationToDrive first verifies conversation id belongs to tenantId before reading child messages for that conversation
- **server/data-protection.ts:324** _(fk-transitive)_ — backupTenantDataToDrive queries project_notes by project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) but does not include tenant_id. project_notes is effectively tenant-scoped by project; missing tenant predicate allows reading another tenant's notes if project_id were reused or mis-associated.
  - _Why false positive:_ project_notes has no tenant_id and is child-scoped through projects already filtered by tenant_id
- **server/data-protection.ts:335** _(fk-transitive)_ — backupTenantDataToDrive queries project_files by project_id (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}) without a tenant_id predicate, allowing cross-tenant reads.
  - _Why false positive:_ project_files has no tenant_id and is child-scoped through tenant-filtered projects
- **server/data-protection.ts:368** _(upstream-verified)_ — backupTenantDataToDrive runs SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} without scoping by tenant_id. messages is tenant-scoped elsewhere; this allows cross-tenant inquiry or incorrect counts.
  - _Why false positive:_ conv.id comes from conversations already filtered by tenant_id in the same call path
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE from tenant-scoped table `doc_chunks` without tenant_id constraint (DELETE WHERE doc_path = ... AND collection_id = ... but missing tenant_id).
  - _Why false positive:_ same function first verifies doc_collections ownership with id and tenant_id before deleting child chunks for that collection
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE on tenant-scoped table `doc_chunks` setting embedding WHERE id = <id> without tenant_id constraint.
  - _Why false positive:_ rows being updated were selected immediately above with collection_id and tenant_id constraints
- **server/email.ts:410** _(system-sweep)_ — SELECT from tenant-scoped table `inbox_messages` (backfillEmptyBodies) with no tenant_id or inbox filter — selects rows across tenants.
  - _Why false positive:_ global backfill housekeeping loop updates empty-body fields per row and does not return or mix tenant content to tenants
- **server/email.ts:424** _(system-sweep)_ — UPDATE on tenant-scoped table `inbox_messages` updating body_text/body_html WHERE id = <id> without tenant_id constraint.
  - _Why false positive:_ Boot/backfill maintenance loop updates only housekeeping content fields on rows it selected; no tenant-facing mixed output.
- **server/embeddings.ts:716** _(system-sweep)_ — backfillEmbeddingVecs selects rows from memory_entries (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200) without any tenant_id constraint — a read across all tenants.
  - _Why false positive:_ Global vector backfill reads each row's own embedding and updates only embedding_vec by PK; housekeeping-only maintenance with no tenant-facing data return.
- **server/embeddings.ts:732** _(system-sweep)_ — backfillEmbeddingVecs selects rows from agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE ...) with no tenant_id filter — a read across all tenants.
  - _Why false positive:_ Global vector backfill for agent_knowledge updates only embedding_vec per selected row; no cross-tenant mixing into one response/prompt.
- **server/google-drive.ts:527** _(global-table)_ — SELECT from tenant-scoped table 'projects' by id (WHERE eq(projects.id, projectId)) has no tenant_id filter — reading a tenant-scoped row without SQL-level tenant constraint.
  - _Why false positive:_ The flagged line queries tenants, which is schema-global and not tenant-scoped despite first-pass text mentioning projects.
- **server/google-drive.ts:826** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' by id in uploadAndShare (WHERE eq(projects.id, params.projectId)) lacks tenant_id in the WHERE clause — reading a referenced foreign id without SQL tenant scoping.
  - _Why false positive:_ Same call path immediately verifies proj.tenantId against params.tenantId before use and fails closed on mismatch.
- **server/google-drive.ts:1003** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' to validate project ownership (WHERE eq(projects.id, params.projectId)) does not include tenant_id in the WHERE clause — a tenant-scoped SELECT without tenant constraint.
  - _Why false positive:_ This exact lookup is the ownership verification step; code immediately compares proj.tenantId to params.tenantId and aborts on mismatch before tenant-facing search.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images does not set tenant_id (INSERT INTO presenter_slide_images (session_id, slide_index, image_data, image_size, quality) VALUES (...) ) — writing to a likely tenant-scoped table without recording tenant context.
  - _Why false positive:_ presenter_slide_images is keyed by session_id and referenced schema shows parent presenter_sessions is tenant-scoped; child insert without tenant_id is consistent with transitive isolation.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals (SELECT id, title, status, created_at FROM code_proposals WHERE status IN (...) AND created_at < ...) contains no tenant_id filter. If code_proposals is tenant-scoped, this reads across tenants and can leak or operate on other tenants' proposals.
  - _Why false positive:_ Admin/health-audit maintenance scan intentionally operates platform-wide and only emits audit findings/counts; no tenant-facing content mixing.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) executes without scoping by tenant_id. This can modify proposals across tenants.
  - _Why false positive:_ Platform-wide stale-row archiver mutates only status field on selected rows; housekeeping-only global maintenance.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL ...) has no tenant_id filter. If heartbeat_tasks is tenant-scoped, this reads tasks for all tenants.
  - _Why false positive:_ Platform-wide heartbeat audit scans for stale tasks and only reports/admin-disables housekeeping state; no tenant content exposure.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables tasks without scoping by tenant_id. This can disable tasks belonging to other tenants if heartbeat_tasks is tenant-scoped.
  - _Why false positive:_ Boot/maintenance audit disables stale heartbeat rows platform-wide; housekeeping mutation only, no tenant-facing data mixing.
- **server/heartbeat.ts:52** _(system-sweep)_ — Raw SELECT from tenant-scoped table 'research_schedules' has no tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due research schedules across tenants and processes each row with its own sched.tenant_id.
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause (UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id}).
  - _Why false positive:_ Platform-wide heartbeat loop updates only scheduling fields for the row it just processed; per-row tenant_id is carried on sched.
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause (UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ... WHERE id = ${sched.id}).
  - _Why false positive:_ Platform-wide scheduler advances last_run_at/next_run_at for the fetched schedule row only; housekeeping write, no cross-tenant data exposure.
- **server/heartbeat.ts:112** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause (UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id}).
  - _Why false positive:_ Same maintenance loop advancing next_run_at on a schedule row already selected and handled with its own tenant context.
- **server/heartbeat.ts:119** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_schedules' omits tenant_id in WHERE clause in the error-recovery path (UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id}).
  - _Why false positive:_ Error-recovery advance of next_run_at is part of the same cross-tenant housekeeping sweep and only mutates scheduling metadata.
- **server/heartbeat.ts:658** _(system-sweep)_ — SELECT from tenant-scoped table 'event_log' queries pending events across all tenants (SELECT * FROM event_log WHERE status = 'pending' ... ) with no tenant_id restriction.
  - _Why false positive:_ Pending events are intentionally polled platform-wide, then each event is routed using its own event.tenant_id; no mixed tenant output here.
- **server/heartbeat.ts:1814** _(global-table)_ — SELECT from tenant-scoped table 'heartbeat_logs' (SELECT created_at FROM heartbeat_logs WHERE task_name = ... ) lacks tenant_id constraint and reads logs across tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id, so this log table is not tenant-scoped.
- **server/heartbeat.ts:260** _(system-sweep)_ — UPDATE on tenant-scoped 'heartbeat_tasks' (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}) omits tenant_id in the WHERE clause — write is not constrained to the task's tenant.
  - _Why false positive:_ Startup cleanup intentionally normalizes heartbeat task config across tenants; housekeeping-only mutation in platform maintenance pass.
- **server/heartbeat.ts:264** _(system-sweep)_ — UPDATE on tenant-scoped 'heartbeat_tasks' (UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}) omits tenant_id in the WHERE clause.
  - _Why false positive:_ Startup cleanup normalizes reflection task model in a platform-wide maintenance loop; no tenant data returned or mixed.
- **server/heartbeat.ts:3360** _(upstream-verified)_ — UPDATE on tenant-scoped 'heartbeat_tasks' sets approval_status without tenant scoping (UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}).
  - _Why false positive:_ Row is created immediately above with tenantId, and the update targets that freshly created newTask.id in the same call path.
- **server/heartbeat.ts:2863** _(global-table)_ — Call to storage.getHeartbeatTasks() with no tenant argument returns global task list and is used for delegation cap / duplicate checks — logic operates across all tenants instead of the task's tenant.
  - _Why false positive:_ heartbeat tasks appear to be platform-global scheduler state; referenced schema does not show a tenant-scoped task table and this is a global cap/duplicate check
- **server/internal-resolver.ts:91** _(internal-worker-pk)_ — finalizeEvent() updates event_log WITHOUT constraining by tenant_id (UPDATE event_log ... WHERE id = ${eventId} AND status IN (...)). This can finalize/alter events belonging to other tenants.
  - _Why false positive:_ internal resolver finalizes event_log by server-side eventId in a worker flow, not from a request boundary
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Query aggregates projects across ALL tenants without constraining by tenant_id: `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id`. This reads tenant-scoped table `projects` without an application-level tenant filter and therefore violates the tenant-isolation invariant.
  - _Why false positive:_ boot/backfill maintenance aggregates per-tenant counts across all tenants for housekeeping and does not return mixed tenant content to tenants
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT against tenant-scoped table `agent_runs` without a tenant_id constraint (SELECT ... FROM agent_runs WHERE status = 'running' ...). This scans rows across tenants and returns tenant_id in results without being filtered to a caller tenant.
  - _Why false positive:_ read-only global stuck-row audit intentionally scans tenants and carries tenant_id per row for maintenance reporting
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT against tenant-scoped table `mind_tickets` without a tenant_id constraint (SELECT ... FROM mind_tickets WHERE status = 'in_progress' ...). This reads across tenants.
  - _Why false positive:_ read-only global stuck-row audit intentionally scans tenants and carries tenant_id per row for maintenance reporting
- **server/lib/system-probes.ts:163** _(system-sweep)_ — SELECT count(*) FROM tenant-scoped table `messages` executed without a tenant_id filter (db.select(...).from(messagesTable)). This returns a global message count across tenants rather than a caller-scoped value.
  - _Why false positive:_ operational system probe uses a global count for health/diagnostics, not tenant-facing data access
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE against tenant-scoped table tenant_voice_profiles uses only the row id in WHERE (eq(tenantVoiceProfiles.id, existing.id)) and does not constrain by tenant_id. This allows updating another tenant's row if an id is supplied/mis-bound.
  - _Why false positive:_ update uses existing.id obtained immediately from getVoiceProfile({ tenantId, profileName }), so ownership is established earlier in the same call path
- **server/memory-graph.ts:80** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} is executed without constraining by tenant_id. This can increment counts on categories belonging to other tenants if ids are misused.
  - _Why false positive:_ catId comes from ensureCategory/selects scoped by tenantId immediately above in the same flow, so the updated category was tenant-verified upstream
- **server/memory-graph.ts:196** _(fk-transitive)_ — Existence check SELECT 1 FROM memory_links WHERE (source_memory_id = ... OR target_memory_id = ...) LIMIT 1 lacks any tenant scoping. If memory_links are tenant-scoped, this will check/return links across tenants; if memory_links are not tenant-scoped, the design allows cross-tenant linking.
  - _Why false positive:_ memory_links has no tenant_id and links are created only among memory_entries already filtered by tenant_id in the same flow
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} is executed without tenant scoping. Although the id came from a tenant-scoped SELECT earlier, the UPDATE should still constrain by tenant_id to avoid accidental cross-tenant writes.
  - _Why false positive:_ ids updated come directly from a prior SELECT on knowledge_triples constrained by tenant_id = tenantId in the same call path
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list. The application only calls assertProjectInTenant() before this INSERT, but the SQL write itself lacks an explicit tenant_id column per the project's invariant.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via project_id; assertProjectInTenant verifies parent ownership before insert
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id in the VALUES list. Although assertProjectInTenant(...) is called, the SQL statement itself writes no tenant_id column.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via project_id; assertProjectInTenant verifies parent ownership before insert
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM `oauth_subscriptions` filtered only by provider (no tenant_id). This reads subscription metadata across tenants.
  - _Why false positive:_ startup validation intentionally enumerates subscriptions across tenants for maintenance/reconnect and handles each row by its own tenant_id
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM `oauth_subscriptions` WHERE is_active = TRUE — no tenant_id constraint. This enumerates active subscriptions across tenants in proactiveTokenRefresh.
  - _Why false positive:_ proactive token refresh is a platform-wide maintenance loop over active subscriptions using each row's tenant_id separately
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT provider, tenant_id FROM `oauth_subscriptions` WHERE is_active = TRUE in forceRefreshAllSubscriptions is unconstrained by tenant_id, enumerating active subscriptions across tenants.
  - _Why false positive:_ forceRefreshAllSubscriptions is an explicit cron-style maintenance sweep processing each subscription per-row by tenant_id
- **server/pace-control.ts:85** _(global-table)_ — COUNT query against heartbeat_logs lacks tenant scoping — no WHERE tenant_id = ${tenantId}. This returns cross-tenant aggregate usage and can allow tenants to observe/affect others' pacing.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is described as a platform-global pacing log used for global caps
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries heartbeat_logs without any tenant filter (both the total/filters query and the per-persona GROUP BY). This produces cross-tenant usage snapshots.
  - _Why false positive:_ heartbeat_logs has no tenant_id and this snapshot reads the platform-global pacing log for operator/debug usage
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} is executed without tenant_id in the WHERE clause (the SELECT that populated t had tenant filtering, but the subsequent UPDATE uses id only). This can mutate another tenant's task if ids collide or are re-used.
  - _Why false positive:_ task ids come from a prior SELECT on heartbeat_tasks constrained by ht.tenant_id = tenantId in the same branch
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} is executed without tenant_id scoping after selecting desks filtered by tenant_id. The write therefore is not explicitly constrained to the tenant.
  - _Why false positive:_ desk ids come from a prior SELECT on agent_desks constrained by ad.tenant_id = tenantId in the same branch
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` executed without any tenant_id constraint (global read).
  - _Why false positive:_ heartbeat runner intentionally scans due recurring_message rows across tenants and subsequent operations use each row's tenant_id
- **server/research-engine.ts:390** _(system-sweep)_ — startResearchSession() issues DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() with no tenant_id constraint — this will remove expired autoresearch knowledge for all tenants.
  - _Why false positive:_ global expiry cleanup deletes only expired autoresearch housekeeping rows and does not return or mix tenant data
- **server/routes.ts:2141** _(upstream-verified)_ — POST /api/presenter INSERTS into tenant-scoped table `presenter_sessions` using tenantId taken directly from the request body (untrusted input) without verifying the caller is authorized for that tenant.
  - _Why false positive:_ route is gated by x-internal-key/SESSION_SECRET before accepting body tenantId, so tenant choice is restricted to authenticated internal caller
- **server/routes.ts:2312** _(signed-url)_ — GET /api/store/order/:sessionId selects from tenant-scoped table `delivery_logs` by order/stripe id without a tenant_id constraint (no WHERE tenant_id = ...). This reads tenant data without constraining to the caller's tenant.
  - _Why false positive:_ order lookup is explicitly capability-style by unguessable Stripe session id and returns only public-safe fields
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` lacks tenant_id constraint (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}) — this can modify a conversation belonging to another tenant if ids are guessed or mixed up.
  - _Why false positive:_ conv was just created in this same call path with tenantId, so updating by its server-generated conv.id is already ownership-safe here
- **server/routes/conversations.ts:99** _(fk-transitive)_ — SELECT from `project_conversations` omits tenant scoping (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}). If project_conversations is tenant-scoped this can read another tenant's linkage.
  - _Why false positive:_ project_conversations has no tenant_id and is a link/child table between tenant-scoped parent rows already verified by project tenant check and newly created conversation tenant
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})). Without an explicit tenant_id INSERT, rows may be orphaned or assigned incorrectly.
  - _Why false positive:_ project_conversations has no tenant_id by schema; insert is correct via parent project/conversation tenant scoping
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` lacks tenant constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}). This can update another tenant's project if an id is abused.
  - _Why false positive:_ same call path first proves project ownership with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before updating that project id
- **server/routes/conversations.ts:227** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). If project_notes is tenant-scoped, missing tenant_id allows notes to be created without tenant attribution or associated with the wrong tenant.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; projId is resolved through join requiring p.tenant_id = conv.tenantId/tenantId
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} — missing tenant_id constraint. This path marks a message read without constraining to the caller's tenant, allowing cross-tenant mutation if a message id is guessed.
  - _Why false positive:_ same handler first SELECTs inbox_messages by id and tenant_id, then updates the fetched msg.id only after ownership is established
- **server/routes/metrics.ts:63** _(system-sweep)_ — SELECT from agent_cost_ledger aggregates cost per tenant over the last 24h without any tenant_id WHERE predicate — a tenant-scoped table is being scanned across all tenants.
  - _Why false positive:_ operator metrics endpoint protected by METRICS_TOKEN intentionally aggregates across tenants and emits only metrics, not tenant-facing mixed content
- **server/seed.ts:35** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id filter — reading a tenant-scoped table without constraining by tenant allows cross-tenant data exposure.
  - _Why false positive:_ boot/dev snapshot seed checks global row count for housekeeping initialization, not tenant-facing data return or mixed output
- **server/seed.ts:2855** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true lacks any tenant_id constraint — reading all tenants' research_programs.
  - _Why false positive:_ boot-time repair scans active research_programs globally to normalize model values and only mutates housekeeping field model
- **server/seed.ts:4459** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` runs without a tenant_id constraint (UPDATE memory_entries SET fact = REPLACE(...) WHERE deleted_at IS NULL AND fact LIKE ...). This will modify memory entries across all tenants rather than scoping to a tenant context.
  - _Why false positive:_ Boot/seed maintenance normalizer intentionally updates all tenants' memory rows in place and only logs aggregate counts; no tenant data is returned or mixed into one output.
- **server/storage.ts:295** _(upstream-verified)_ — attachCitationMetadata performs: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL — no tenant_id constraint. This reads a tenant-scoped table without scoping by tenant.
  - _Why false positive:_ attachCitationMetadata only re-reads ids from msgs already fetched by getMessages/getMessagesPaginated with tenant-scoped predicates, so ownership is established earlier in the same call path.
- **server/storage.ts:480** _(system-sweep)_ — deletePersona transaction updates conversations with .where(eq(conversations.personaId, id)) without any tenant scoping — touching a tenant-scoped table across all tenants.
  - _Why false positive:_ personas is global (no tenant_id); deleting a global persona intentionally clears references across tenant-scoped child tables as housekeeping, without returning tenant data.
- **server/storage.ts:481** _(system-sweep)_ — deletePersona transaction updates memory_entries with .where(eq(memory_entries.personaId, id)) and no tenant filter — unscoped cross-tenant write.
  - _Why false positive:_ Deleting a global persona intentionally supersedes referencing memory rows across tenants as housekeeping tied to the global parent persona, with no tenant data exposure.
- **server/storage.ts:482** _(system-sweep)_ — deletePersona transaction deletes daily_notes with .where(eq(daily_notes.personaId, id)) without tenant scoping — unscoped delete against a tenant-scoped table.
  - _Why false positive:_ Deleting a global persona intentionally removes referencing daily_notes across tenants as maintenance for a platform-global parent object, not a tenant-facing data mix.
- **server/storage.ts:823** _(system-sweep)_ — getMemoriesWithoutEmbeddings selects memory_entries WHERE status='active' AND embedding IS NULL without tenant scoping — returns rows across tenants.
  - _Why false positive:_ This is a background backlog fetch for embedding maintenance across all tenants; rows are processed individually for housekeeping rather than returned to a tenant-facing surface.
- **server/storage.ts:828** _(system-sweep)_ — getKnowledgeWithoutEmbeddings selects agent_knowledge WHERE ... WITHOUT tenant filter — reads tenant-scoped knowledge across tenants.
  - _Why false positive:_ Cross-tenant selection of knowledge rows lacking embeddings is for platform-wide embedding maintenance, with per-row processing and no tenant-facing mixed output shown.
- **server/storage.ts:991** _(system-sweep)_ — getDueHeartbeatTasks runs a raw SELECT FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ... with no tenant constraint. This returns due tasks for all tenants.
  - _Why false positive:_ cross-tenant scheduler fetch of due heartbeat tasks is platform-wide maintenance and returns row-carried tenantId for per-row handling
- **server/storage.ts:1019** _(system-sweep)_ — claimHeartbeatTasks updates heartbeat_tasks (UPDATE ... WHERE id = ${id} AND enabled = true AND next_run_at <= NOW()) with no tenant constraint — unscoped mutation of tenant-scoped rows.
  - _Why false positive:_ claim step is part of platform-wide heartbeat scheduler mutating housekeeping timing fields only across tenants
- **server/surprise-scorer.ts:141** _(upstream-verified)_ — scoreProposalSurprise runs: UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId} — no tenant filter. Unscoped write into tenant-scoped table.
  - _Why false positive:_ same function first loads proposal by id and tenant_id, then writes derived embedding for that verified row
- **server/surprise-scorer.ts:214** _(upstream-verified)_ — scoreProposalSurprise updates: UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} — missing tenant_id constraint. Unscoped write to tenant-scoped table.
  - _Why false positive:_ same call path already verified proposal ownership with WHERE id and tenant_id before computing and updating score
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without scoping by tenant_id (where clause uses only name). This leaks cross-tenant existence information and can cause incorrect cross-tenant conflict handling.
  - _Why false positive:_ tool name is rewritten with tenantToolName(parsed.name, tenantId), making lookup tenant-derived rather than cross-tenant
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id without also verifying tenant ownership (missing tenant_id constraint).
  - _Why false positive:_ re-read by freshly inserted server-generated custom_tools primary key within same function, not from an untrusted request boundary
- **server/tools.ts:2911** _(fk-transitive)_ — SELECTs in the project LIST query reference tenant-scoped tables (project_files, project_notes, project_conversations) via subqueries without an explicit tenant_id filter.
  - _Why false positive:_ project_files/project_notes/project_conversations have no tenant_id and are counted via project_id from already tenant-scoped parent projects row
- **server/tools.ts:2973** _(upstream-verified)_ — Duplication check SELECT against project_files uses project_id and file_name but does not include an explicit tenant_id filter.
  - _Why false positive:_ ownership of parent project is checked immediately above with projects.id and tenant_id; project_files is tenant-scoped transitively via project_id
- **server/tools.ts:3014** _(upstream-verified)_ — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} lacks an explicit tenant_id filter on a tenant-scoped table.
  - _Why false positive:_ same path verifies project ownership and conversation tenant before duplicate check; project_conversations is child table scoped through validated parent project
- **server/tools.ts:6899** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The code calls assertProjectInTenant(...) to verify ownership but the INSERT statement omits tenant_id (VALUES clause: project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by). Under the project's invariant every INSERT into a tenant-scoped table must explicitly supply tenant_id (columns are NOT NULL with no default) rather than relying solely on prior checks.
  - _Why false positive:_ schema note says project_files has no tenant_id; insert is correctly scoped transitively through assertProjectInTenant on parent project_id
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE on browser_workflows (last_replayed) executes without constraining by tenant_id — raw SQL: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`.
  - _Why false positive:_ workflow row was selected immediately above with tenant_id = tenantId, and update uses that verified row's id
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into project_files omits tenant scoping / tenant_id column: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${projId}, ...). If project_files is tenant-scoped this writes without an explicit tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is linked through tenant-scoped projects/project_id validated by tenant-scoped join
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} executes without a tenant filter. This updates a potentially tenant-scoped row by id only, lacking an AND tenant_id = <caller> guard.
  - _Why false positive:_ projId comes from prior query joining conversations to projects with p.tenant_id = tenantId in same path
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE to tenant-scoped table 'knowledge_triples' is executed without a tenant_id constraint (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}). This allows mutating rows by id across tenants.
  - _Why false positive:_ row.id values come from immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — Query against provider_keys executes without scoping by tenant_id. The SQL reads provider_keys across all tenants (no WHERE tenant_id = ${tenantId}). Even though this branch runs only for tenantId===1, it still queries a tenant-scoped table without an explicit tenant filter, violating the invariant that tenant-scoped reads must be constrained to the caller's tenant.
  - _Why false positive:_ schema note says provider_keys has no tenant_id, so this is not a tenant-scoped table
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — Query counting provider_keys (SELECT COUNT(DISTINCT provider) ...) is executed without a tenant_id constraint. This reads a table that is typically tenant-scoped across all tenants with no WHERE tenant_id = ${tenantId}, violating the requirement that every read against tenant-scoped tables be constrained to the caller's tenant.
  - _Why false positive:_ provider_keys is schema-global with no tenant_id; admin-only aggregate is not cross-tenant isolation issue
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE memory_entries SET ... WHERE id = ${id} is missing a tenant_id constraint. This write to a tenant-scoped table is constrained only by a prior SELECT; the UPDATE by id alone could modify rows from other tenants if ids are reused or attacker manipulates id values.
  - _Why false positive:_ id is obtained from prior SELECT on memory_entries constrained by tenant_id = tid and persona/category in same flow
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids}) lacks a tenant_id filter. This performs a write against a tenant-scoped table without explicitly constraining to the caller's tenant.
  - _Why false positive:_ ids array is derived solely from rows just selected with tenant_id = tid in same function
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id} is missing a tenant_id constraint. The update uses only the row id and does not include tenant scoping, allowing potential cross-tenant modification if ids collide or are misused.
  - _Why false positive:_ id comes from immediately preceding tenant-scoped SELECT on memory_entries with tenant_id = tid
- **server/video-job-runner.ts:204** _(internal-worker-pk)_ — SELECT cancelRequested FROM video_jobs WHERE jobId = ${jobId} is done without scoping by tenant_id. isCancelRequested reads a tenant-scoped table by jobId alone and can return flags for jobs owned by other tenants.
  - _Why false positive:_ Server-internal runner reads cancel flag by jobId for its own globally unique job row; not shown as coming from req/body/tool args.
- **server/video-job-runner.ts:228** _(system-sweep)_ — recoverStaleVideoJobs performs a db.update(videoJobs).set(...).where(...) without any tenant_id filter — it will mark stale jobs across all tenants as failed.
  - _Why false positive:_ Global stale-job recovery is a maintenance sweep updating housekeeping/status fields across tenants and only returns jobIds/count for logging.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Query reads from tenant-scoped table "tool_performance" (SELECT count(*) FROM tool_performance ...) with no tenant_id constraint — this returns/aggregates rows across all tenants.
  - _Why false positive:_ Boot/maintenance invariant check intentionally computes global telemetry counts and emits findings, not tenant-facing mixed row data.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — Query SELECT count(*)::int AS n FROM tool_performance executes with no tenant_id filter — counts rows across tenants.
  - _Why false positive:_ Global wiring-health count over tool_performance is maintenance telemetry only, not tenant-scoped data returned to a tenant surface.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Query SELECT count(*)::int AS n FROM messages WHERE created_at > ... lacks any tenant_id constraint — reads message data across tenants.
  - _Why false positive:_ Global chat-volume count is part of a maintenance/invariant probe and returns only aggregate counts, with no tenant content mixing.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — sql.raw(`SELECT COUNT(*)::int AS rows, MAX(${spec.timestampColumn}) AS last_write FROM ${spec.table}`) probes subsystem tables (e.g. tool_performance, knowledge_nudges) without any tenant_id filtering — reading tenant-scoped tables across tenants.
  - _Why false positive:_ Subsystem freshness probe is a global maintenance check using row counts/max timestamps only, with no tenant-facing content exposure.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Memory-growth probe runs sql.raw(`SELECT COUNT(*)::int AS cnt, pg_total_relation_size('${tbl.name}') AS bytes FROM ${tbl.name}`) for tables that are tenant-scoped (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, etc.) with no tenant_id constraint — counts rows across tenants.
  - _Why false positive:_ Memory-growth tripwire is a platform-wide observability check returning aggregate row counts/table sizes only, not tenant data.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*)::int AS cnt FROM graph_memory executes with no tenant_id constraint — reads 'graph_memory' across all tenants (and even the code comment references "across all tenants").
  - _Why false positive:_ Global emptiness check on graph_memory is maintenance telemetry only and returns a single aggregate count-derived finding.
