# Tenant-Isolation Audit Report

_Generated 2026-08-14T13:44:03.603Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **594/594** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **292** (CRITICAL 58 · HIGH 187 · MEDIUM 45 · LOW 2)
- Precision second-pass (`gpt-5.4`): **229** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (58)

- **server/agentic-engines.ts:243** — Bulk UPDATE on `ai_insights` without tenant scoping: `UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...` will modify rows across all tenants.
  - _Fix:_ This is a high-impact multi-tenant write — change to a per-tenant update (add `AND tenant_id = ${tenantId}`) or iterate tenants and apply the policy per-tenant. Never perform cross-tenant state-changing updates without explicit tenant scoping and operator intent.
- **server/auto-memorize.ts:74** — setWatermark() INSERT hard-codes tenant_id = 1 when creating the watermark (INSERT ... tenant_id, ... VALUES (..., 1, ...)). This writes a tenant-scoped row to the wrong tenant.
  - _Fix:_ Do NOT hard-code tenant IDs. Make setWatermark accept the caller's tenantId and INSERT tenant_id = ${tenantId}. Validate the tenant before writing.
- **server/commitment-drafter.ts:234** — UPDATE on commitments without tenant filter — `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` can modify a row in any tenant because it lacks `AND tenant_id = ${tenantId}`.
  - _Fix:_ Constrain the UPDATE to the owning tenant. Example: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Also ensure the tenantId used is validated and derived from a trusted source before executing the statement.
- **server/data-protection.ts:168** — DELETE FROM conversations is executed without scoping to tenant_id (DELETE FROM conversations WHERE id = ${conv.id}). This deletes a tenant-scoped row without an explicit tenant predicate.
  - _Fix:_ Add an explicit tenant predicate to the DELETE, e.g. DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id} (or AND tenant_id = ${tenantId}) so the delete cannot affect another tenant's rows.
- **server/embeddings.ts:748** — UPDATE performed on tenant-scoped table (memory_entries or agent_knowledge) without constraining tenant_id. storeEmbeddingVec updates embedding_vec/embedding by id only, allowing cross-tenant modification.
  - _Fix:_ Require the caller to pass tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Validate ownership before updating. Also avoid embedding string interpolation by using a parameterized binding for the vector value rather than sql.raw interpolation.
- **server/embeddings.ts:790** — UPDATE memory_entries in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant_id — allows modifying other tenants' rows.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise verify ownership before updating. Prefer parameterized bindings for vector values instead of inlined sql.raw.
- **server/embeddings.ts:807** — UPDATE agent_knowledge in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant_id — allows modifying other tenants' rows.
  - _Fix:_ Add tenant_id to the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or verify the row belongs to the expected tenant before updating. Use parameterized bindings for vector literals instead of sql.raw interpolation.
- **server/health-audit.ts:314** — SELECT from code_proposals omits any tenant scoping — query pulls proposals across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or otherwise run the scanner per-tenant under an explicit admin/operational context. If the subsequent UPDATE is performed, ensure you only update rows belonging to the intended tenant (include tenant_id in the WHERE or use a CTE that filters by tenant_id before UPDATE).
- **server/health-audit.ts:333** — UPDATE code_proposals uses WHERE id = ANY(${ids}::int[]) with no tenant_id filter — may modify proposals for other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or join/update via a CTE that ensures only rows for the intended tenant are changed. If this routine is intentionally global/admin, explicitly require and validate an admin context and still include tenant_id in predicates when updating tenant-scoped rows.
- **server/job-queue.ts:98** — claimDueJobs performs an UPDATE/SELECT on agent_jobs without scoping by tenant_id (no WHERE tenant_id = <...>). This atomically claims jobs across all tenants, violating the requirement that every access to tenant-scoped tables include an explicit tenant constraint.
  - _Fix:_ If the worker must only process a single tenant at a time, accept a tenantId parameter and add `AND tenant_id = ${tenantId}` to the WHERE clauses (both in the inner SELECT and the outer UPDATE). If a global queue is required, create a purpose-built global_jobs table (not tenant-scoped) or otherwise document+protect the global semantics; do NOT leave tenant-scoped agent_jobs being updated without an explicit tenant filter. Ensure the code path is audited and access-limited if global operations are unavoidable.
- **server/job-queue.ts:134** — reclaimExpiredLeases updates agent_jobs (marking failed_terminal) without any tenant_id constraint — it mutates rows for all tenants indiscriminately.
  - _Fix:_ Scope the UPDATE to a specific tenant (e.g. add `AND tenant_id = ${tenantId}`) or run reclamation per-tenant. Alternatively, move any global reclaim logic to a non-tenant-scoped table and keep tenant-scoped state strictly filtered. Avoid blind updates across tenant-scoped tables.
- **server/job-queue.ts:148** — reclaimExpiredLeases' second UPDATE (requeueing expired rows back to pending) operates on agent_jobs without a tenant_id WHERE clause, re-queuing jobs across tenants.
  - _Fix:_ Same fix: perform reclamation per-tenant (pass tenantId) and include `AND tenant_id = ${tenantId}` in the WHERE. If global reclamation is required, use a global queue table instead of reusing a tenant-scoped table.
- **server/job-queue.ts:177** — completeJob updates agent_jobs (set status='succeeded') using only id/attempts/status fencing, with no tenant_id constraint — a caller could complete a job belonging to another tenant if they know the id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `AND tenant_id = ${tenantId}`) and require callers to pass the job's tenantId (or validate the job belongs to the expected tenant before updating). This preserves tenant isolation and prevents cross-tenant state mutation.
- **server/job-queue.ts:220** — failJob SELECTs max_attempts FROM agent_jobs WHERE id = ${id} without scoping by tenant_id — reads tenant-scoped state across tenants.
  - _Fix:_ Read max_attempts with an explicit tenant filter (`WHERE id = ${id} AND tenant_id = ${tenantId}`) and require the caller supply/validate tenantId. Prevent reading another tenant's job metadata.
- **server/job-queue.ts:237** — failJob's UPDATE to mark failed_terminal writes to agent_jobs without any tenant_id constraint, mutating jobs across tenants.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE and require the caller to supply the tenantId (or verify job ownership) before performing terminal failure transitions. Alternatively perform per-tenant failure handling only.
- **server/job-queue.ts:257** — failJob's retry-path UPDATE (re-scheduling with next_run_at) operates on agent_jobs without scoping by tenant_id, affecting jobs belonging to any tenant.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause and require/validate tenantId for the operation. If the retry logic must remain global, move to a non-tenant-scoped table or implement an explicit and auditable global policy.
- **server/job-queue.ts:283** — retryJob updates agent_jobs to reset status/attempts without any tenant_id constraint — an operator could reset jobs across tenants.
  - _Fix:_ Require tenantId as a parameter and add `AND tenant_id = ${tenantId}` to the WHERE, or restrict this endpoint to an admin flow that explicitly documents cross-tenant scope and enforces strict operator authentication. Prefer per-tenant scoping to maintain isolation.
- **server/lib/action-ledger.ts:304** — Cross-tenant UPDATE: sweepStaleAttempts executes UPDATE action_attempts ... WHERE id IN (SELECT id FROM action_attempts WHERE state IN (...) ...) with no tenant_id constraint in the inner SELECT, allowing rows from any tenant to be transitioned.
  - _Fix:_ Constrain the inner SELECT (and the UPDATE) by tenant_id or otherwise restrict this operation to a maintenance context that is explicitly authorized. Prefer per-tenant scoped updates (e.g. include AND tenant_id = ${tenantId}) or iterate tenant-by-tenant to avoid cross-tenant writes.
- **server/memory-graph.ts:252** — INSERT INTO memory_links (...) VALUES (...) — INSERT does not set tenant_id. If memory_links is a tenant-scoped table this allows creating rows with no tenant attribution (or implicit admin/system defaults), violating the invariant that INSERTs must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT values (and any other tenant-scoped columns). For example: INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count, tenant_id) VALUES (..., ${tenantId}). If memory_links is intentionally global, document and enforce that; otherwise add tenantId and validate caller ownership.
- **server/minerva-planner.ts:367** — decidePlan performs a CAS UPDATE on the tenant-scoped plans table but conditionally omits tenant scoping when args.tenantId is not provided (`... WHERE id = ${args.planId} AND status = 'awaiting_approval' ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}`). If called without tenantId from an untrusted caller this allows cross-tenant modification (IDOR).
  - _Fix:_ Require and validate tenantId for all external callers (do not allow it to be optional), or always include tenant_id in the WHERE clause and fail if tenantId is not supplied. Treat omission as an error (fail-closed) rather than falling back to an unscoped UPDATE.
- **server/minerva-planner.ts:472** — Rollback UPDATE after createPlan failure runs `UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'` with NO tenant_id constraint. This can modify a plan belonging to a different tenant if called in an unscoped context.
  - _Fix:_ Include tenant_id = ${args.tenantId} in the WHERE clause (and require args.tenantId to be present), or fetch and verify the plan's tenant_id first and only rollback when it matches the caller's tenant. Treat tenantId omission as an error (fail-closed).
- **server/plan-executor.ts:117** — UPDATE plans executed without scoping by tenant_id (appendExecutionLog updates plans WHERE id = ${planId} with no tenant filter).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId}) or resolve the plan first and verify its tenant before updating. Ensure appendExecutionLog receives/validates the plan's tenant and uses it in the UPDATE.
- **server/plan-executor.ts:513** — CAS UPDATE on plans to claim execution (UPDATE plans ... WHERE id = ${planId} AND status = 'approved') has no tenant_id constraint — plan mutation by id only is used without tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${expectedTenantId}) or fetching step must validate ownership and then include tenant_id in the UPDATE to prevent cross-tenant mutation. Make the claim operation require the plan's tenant id.
- **server/plan-executor.ts:527** — UPDATE plans SET status='failed' WHERE id = ${planId} (no tenant_id filter) — unscoped write to tenant-scoped table.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or validate the plan's tenant before performing the write.
- **server/plan-executor.ts:728** — Final UPDATE of plans (set status/combine execution_log) WHERE id = ${planId} lacks tenant_id constraint — writes to plans table are not tenant-scoped.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${planId} AND tenant_id = ${row.tenant_id}) or perform updates via parameterized tenant-aware queries after verifying plan ownership.
- **server/plan-executor.ts:762** — Error-path UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' has no tenant_id filter — error recovery mutates plans without scoping by tenant.
  - _Fix:_ Add tenant constraint to the UPDATE (AND tenant_id = ${tenantId}) or verify the plan's tenant before performing recovery updates.
- **server/plan-executor.ts:778** — resumeStuckPlans UPDATE resets all executing plans to 'approved' WHERE status = 'executing' AND updated_at < ... — no tenant_id scoping; mass cross-tenant mutation possible.
  - _Fix:_ Scope the recovery UPDATE by tenant or run per-tenant (e.g. WHERE status='executing' AND updated_at < ... AND tenant_id = ${tenantId}) and/or require admin caller context. Avoid global uninhibited updates to tenant-scoped tables.
- **server/plan-executor.ts:787** — SELECT id FROM plans WHERE status = 'approved' ... LIMIT 50 is not constrained by tenant_id — reading tenant-scoped table without tenant filter.
  - _Fix:_ Make this query tenant-aware (e.g. WHERE status = 'approved' AND tenant_id = ${tenantId}). If the runner is intended to act across tenants, iterate tenants explicitly and include tenant_id parameterized in each query.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped table by id only with no tenant_id filter or ownership verification.
  - _Fix:_ Require tenant context or validate that the fetched proposal belongs to the caller's tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), or fetch the row and explicitly assert proposal.tenant_id === callerTenant before proceeding.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} used to clear rebased_at/mark race is executed without tenant scoping — unscoped write to tenant-scoped table.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or ensure the proposal's tenant was previously validated and pass that into the query to prevent cross-tenant mutations.
- **server/proposal-verifier.ts:245** — persist() updates code_proposals: UPDATE code_proposals SET verification_status/verification_details ... WHERE id = ${proposalId} with no tenant_id filter — verification writes are not tenant-scoped.
  - _Fix:_ Constrain the UPDATE with tenant_id (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or require the caller to pass/verify the proposal's tenant before persisting verification metadata.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift branch, UPDATE code_proposals SET status='rejected' ... WHERE id = ${proposalId} is executed with no tenant constraint — LLM-driven rebase actions mutate proposal rows without tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or verify ownership at start and use that tenant for all subsequent updates. Never mutate tenant-scoped rows by id alone.
- **server/proposal-verifier.ts:408** — Persistence of rebased diff: UPDATE code_proposals SET code_diff = ${newCodeDiff} ... WHERE id = ${proposalId} lacks tenant scoping — rewrites proposal code_diff across tenants are possible.
  - _Fix:_ Scope the UPDATE by tenant_id (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or require and validate the proposal's tenant before performing the update.
- **server/research-engine.ts:165** — UPDATE research_sessions in cleanupZombieSessions has no tenant scope — it will mark running sessions completed across all tenants.
  - _Fix:_ Do NOT run an unscoped UPDATE on a tenant-scoped table. Restrict the UPDATE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant and run the query using a parameterized tenant_id. If this is intended as an admin/global operation, require an explicit admin context and audit/guard the call; otherwise fail-closed by requiring a tenant parameter.
- **server/research-engine.ts:416** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() is unscoped — it deletes agent_knowledge rows for all tenants.
  - _Fix:_ Always include tenant scoping on destructive operations against tenant-scoped tables. Change the DELETE to include AND tenant_id = ${tenantId} (or run per-tenant with a safe, parameterized tenant_id). If this is a multi-tenant sweep that must run across tenants, require an explicit admin-only routine that iterates tenants and applies the deletion per-tenant with parameterized queries.
- **server/routes.ts:167** — Unscoped SELECT from tenant-scoped `file_storage` during startup restore — no tenant_id constraint.
  - _Fix:_ Scope the query to a specific tenant (e.g. add WHERE file_storage.tenant_id = <tenant>) or iterate per-tenant. Do not read all tenants' file blobs in one unscoped query. If this is intended to run as an admin migration, run it in an explicit offline/admin migration context and ensure outputs are written to tenant-scoped locations to avoid cross-tenant exposure.
- **server/routes/admin-zombie-detector.ts:54** — CTE `used` selects from conversations with no tenant_id constraint. conversations is a tenant-scoped table; this will surface persona usage across all tenants.
  - _Fix:_ Add tenant scoping to the conversations CTE (e.g. WHERE tenant_id = ${tenantId} AND persona_id IS NOT NULL ...) so only the caller's tenant data is aggregated. If a cross-tenant report is required, implement an explicit, deliberate admin path that iterates/aggregates per-tenant under a verified platform-admin gate rather than issuing unscoped queries against tenant-scoped tables.
- **server/routes/inbox-notifications.ts:101** — UPDATE on `inbox_messages` without tenant_id constraint. After selecting the message with a tenant-scoped WHERE, the code executes: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} — the UPDATE lacks AND tenant_id = ${tenantId}. This allows modifying a row by id without re-checking tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or reuse the previously-verified message row and perform any mutation via a storage helper that enforces tenant scope. Always bind tenant_id on UPDATE/DELETE statements for tenant-scoped tables.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions sets status/result by id without constraining by tenant_id. This writes to a tenant-scoped table but lacks WHERE tenant_id = <tenant> — a caller could affect rows in other tenants if ids collide or are forged.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise validate the session belongs to the current tenant before updating. Prefer parameterized tenantId from the trusted ctx.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions sets conversation_id by id without constraining by tenant_id. This write into a tenant-scoped table is not scoped to the caller's tenant.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} (or the session's tenant) to the WHERE clause, or perform an ownership check prior to updating the row.
- **server/sculptor.ts:168** — UPDATE sculptor_sessions in monitorSession updates a session row (status/result/etc.) by id only, with no tenant_id constraint. Monitor/async jobs may therefore cross-tenant modify sessions if ids are misused.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) so only the owning tenant's session row can be modified. Use the tenantId passed into monitorSession.
- **server/sculptor.ts:358** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} — missing tenant_id constraint. This writes tenant-scoped data without verifying ownership.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or otherwise ensure the session belongs to the tenant making the request) in the WHERE clause before updating review_result.
- **server/seed.ts:2163** — UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} lacks a tenant_id constraint. This can modify a program belonging to another tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or perform the update via Drizzle with .where(eq(research_programs.id, p.id), eq(research_programs.tenantId, tenantId)). Ensure p.id was resolved within the caller's tenant prior to updating.
- **server/seed.ts:2455** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... runs without a tenant_id constraint. projects is tenant-scoped so this may modify another tenant's project row.
  - _Fix:_ Constrain the UPDATE to the intended tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Prefer using Drizzle .where(eq(projects.id, 15), eq(projects.tenantId, tenantId)) or pass tenant context into the seed action so only the targeted tenant row is updated.
- **server/self-improvement.ts:171** — SELECT from tenant-scoped table `experiments` can execute without a tenant_id constraint. detectStagnation builds `conditions` optionally including tenantId; when tenantId is omitted the query uses only category and will return experiments across all tenants.
  - _Fix:_ Fail closed or require a tenantId. Always include a tenant filter in the WHERE clause (e.g. add eq(experiments.tenantId, tenantId) or throw if tenantId is absent). If a global scan is needed, make it an explicit, admin-only function with documented justification.
- **server/self-improvement.ts:521** — Unscoped read: db.select().from(experiments) returns experiment rows across all tenants when getExperimentHistory is called without a tenantId. This allows cross-tenant data exposure.
  - _Fix:_ Require the caller to pass a tenantId and add a WHERE eq(experiments.tenantId, tenantId). If the API must support global reads, implement an explicit admin-only path and audit it; otherwise fail the request when tenantId is missing.
- **server/self-improvement.ts:401** — UPDATE on tenant-scoped table `experiments` uses only the experiment id in the WHERE clause (.where(eq(experiments.id, experiment.id))). There is no tenant_id constraint, so this update could modify another tenant's row if an id is controlled or leaked.
  - _Fix:_ Constrain updates by tenant as well as id. E.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Prefer passing the tenantId you inserted with the experiment (or read it back) and verify ownership before updating.
- **server/self-improvement.ts:415** — Error-path UPDATE on `experiments` also uses only experiment id in the WHERE clause (.where(eq(experiments.id, experiment.id))). No tenant_id check is present on the write path.
  - _Fix:_ Same fix as above: include tenant scoping in the WHERE clause for the error-path update (AND eq(experiments.tenantId, config.tenantId) or equivalent). Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant ownership checks.
- **server/self-reflection.ts:366** — UPDATE on tenant-scoped table `memory_entries` uses only the returned row id in WHERE (WHERE id = ... ) with no tenant_id constraint. Although the id was selected earlier with tenant_id filtering, the subsequent UPDATE is unscoped and could affect rows from other tenants if ids are confused or a race occurs.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Prefer doing SELECT ... FOR UPDATE or using an atomic UPDATE that includes tenant_id in the predicate and RETURNING to avoid TOCTOU issues.
- **server/storage.ts:513** — getAllMemoriesForBackup does a global SELECT from tenant-scoped table 'memory_entries' with no tenant filter (full export across tenants).
  - _Fix:_ Require an explicit tenant parameter (or an explicit ALL_TENANTS sentinel) and protect this call behind an admin-only authorization check. If this must be a platform-wide backup, make the admin intent explicit and gate it with robust authentication and logging; otherwise scope to a single tenant.
- **server/storage.ts:1304** — getAllDataForExport issues many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant_provider_keys, heartbeat_tasks, delivery_logs, etc.) without any tenant filtering — full platform export risk.
  - _Fix:_ Make this function explicitly admin-only and require an explicit tenant parameter or an explicit ALL_TENANTS sentinel. Implement authorization checks and logging; if the intent is per-tenant export, add tenantId and scope every query with eq(table.tenantId, tenantId). Avoid do-everything platform-wide exports without strict access control.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (db.select().from(customTools).where(eq(customTools.name, parsed.name))). This allows cross-tenant discovery/interaction if names overlap or are guessed.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or otherwise ensure the lookup is scoped to the caller's tenantId passed into the function.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id without tenant_id constraint (db.select().from(customTools).where(eq(customTools.id, inserted.id))). Reading a row by id must still be tenant-scoped to avoid cross-tenant access to rows with the same id value.
  - _Fix:_ Add a tenant constraint to the query (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId || inserted.tenantId)))) or otherwise fetch by both id and tenantId.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` targeting a row by id without tenant_id constraint (.where(eq(customTools.id, tool.id))). This can update another tenant's row if id collisions occur or if an id is attacker-supplied.
  - _Fix:_ Constrain updates to the tenant (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId || tool.tenantId)))) and/or require the calling context to supply tenantId; never rely on id alone for tenant-scoped mutations.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` using only id in WHERE (db.delete(customTools).where(eq(customTools.id, tool.id))). Missing tenant_id constraint allows accidental or malicious deletion of another tenant's tool row.
  - _Fix:_ Require and enforce tenant scoping on deletes (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId || tool.tenantId)))) so deletions are limited to the caller's tenant.
- **server/tool-learning.ts:365** — UPDATE on tenant-scoped table `custom_tools` to increment usageCount uses only id in WHERE (.where(eq(customTools.id, tool.id))). No tenant_id constraint present, allowing cross-tenant modification of usage counters or other side-effects.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId || tool.tenantId)))) or otherwise ensure the update is executed with the correct tenantId context.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` executed with no tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys WHERE ...). This reads tenant-scoped data across tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId}) or otherwise enforce an explicit, auditable admin-only pathway. If the intent is to allow admin-wide aggregation, perform an explicit admin-check and return only safe, non-sensitive aggregated metrics (avoid exposing raw rows or per-tenant key material).
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(...) from tenant-scoped table `provider_keys` executed with no tenant_id constraint (SELECT COUNT(DISTINCT provider) ... FROM provider_keys WHERE ...). This aggregates across all tenants without tenant scoping.
  - _Fix:_ If this must be an admin-only aggregate, gate it behind a strict admin check and logging, and avoid returning tenant-specific sensitive details. Otherwise add tenant scoping (WHERE tenant_id = ${tenantId}) to limit results to the caller's tenant.

### HIGH (187)

- **server/ab-optimizer.ts:110** — SELECT across `experiments` omits tenant filter: `SELECT id, tenant_id, hypothesis, metric, persona_id, metadata, created_at FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100` reads rows for all tenants.
  - _Fix:_ Scope this query to a single tenant (e.g. add `WHERE tenant_id = ${tenantId}`) or explicitly iterate per-tenant. If this heartbeat is intended to run cross-tenant, make that explicit and ensure downstream actions always use the row's tenant_id and enforce ownership checks before any writes.
- **server/agent-activity.ts:122** — UPDATE on `agent_activity` missing tenant constraint: `await db.update(agentActivity).set(updates).where(eq(agentActivity.id, activityId))` can modify a row belonging to another tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `.where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))`) or require a tenantId parameter and validate ownership before updating.
- **server/agent-channels.ts:198** — JOIN against `channel_subscriptions` lacks tenant scoping: `JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}` (the query only constrains `cm.tenant_id`). This can match subscription rows from other tenants.
  - _Fix:_ Add tenant constraint for the subscription table in the JOIN (e.g. `AND cs.tenant_id = ${tenantId}`) or include `cs.tenant_id = cm.tenant_id` in the join/where so all joined rows are tenant-scoped.
- **server/agent-eval.ts:218** — UPDATE on `agent_evals` omits tenant scoping: `UPDATE agent_evals SET ... WHERE id = ${evalId}` updates eval rows without ensuring they belong to the caller's tenant.
  - _Fix:_ Add tenant constraint to the WHERE clause (e.g. `WHERE id = ${evalId} AND tenant_id = ${tenantId}`) or otherwise verify the eval row's tenant before performing updates.
- **server/agentic-engines.ts:161** — SELECT in `retryPendingMinervaRouting` reads `ai_insights` without tenant scoping: `SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50`.
  - _Fix:_ Either restrict the sweep to a specified tenant (add `tenant_id = ${tenantId}`) or ensure the function explicitly validates/handles per-tenant processing. Avoid bulk cross-tenant selects/updates without explicit intent and checks.
- **server/agentic-engines.ts:344** — SELECT against `heartbeat_logs` in `gatherOperationalData` omits tenant filter: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` reads logs across tenants.
  - _Fix:_ Restrict the query to the tenant (e.g. add `WHERE tenant_id = ${tenantId} AND task_name != 'Self-Reflection'`) or otherwise ensure cross-tenant reads are deliberate and safe.
- **server/agentic-engines.ts:451** — SELECT of recent heartbeat logs in `runPredictiveEngine` omits tenant scope: `SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10`.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` (or equivalent Drizzle predicate) so only this tenant's logs are used for analysis.
- **server/agentic-engines.ts:519** — `runOptimizationEngine` queries `heartbeat_logs` without tenant restriction: `SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY task_name, status` — reads aggregated data across tenants.
  - _Fix:_ Filter by tenant (e.g. `WHERE tenant_id = ${tenantId}`) or otherwise scope the aggregate to the target tenant before computing recommendations.
- **server/agentic-engines.ts:533** — Query of `heartbeat_tasks` in `runOptimizationEngine` lacks tenant scoping: `SELECT name, cron_expression, enabled, last_run_at, run_all FROM heartbeat_tasks WHERE enabled = true ORDER BY name`.
  - _Fix:_ Add `WHERE tenant_id = ${tenantId}` (or include tenant filter in the query) to avoid using other tenants' task configuration when generating tenant-specific optimizations.
- **server/agentic-features.ts:313** — Subquery in `detectCompetitorChanges` omits tenant filter: inner `SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY url, created_at DESC` does not constrain `tenant_id`, allowing cross-tenant rows to affect the result set.
  - _Fix:_ Include `AND tenant_id = ${params.tenantId}` in the inner subquery (and any other subqueries) so previous/latest snapshots are selected only for the target tenant.
- **server/agentic-features.ts:638** — Existence check before enrollment omits tenant scoping: `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` can match enrollments from other tenants.
  - _Fix:_ Add `AND tenant_id = ${params.tenantId}` to the query to ensure the existence check is tenant-scoped before inserting a new enrollment.
- **server/agentic-features.ts:797** — UPDATE on `outreach_enrollments` after classification omits tenant constraint: `UPDATE outreach_enrollments SET ... WHERE id = ${row.id}` may update the wrong tenant's row.
  - _Fix:_ Include `AND tenant_id = ${params.tenantId}` in the WHERE clause (or use a Drizzle eq on tenantId) to ensure only the tenant's enrollment row is modified.
- **server/agentic/approvals.ts:134** — Bulk expiry update in `expireStaleApprovals` omits tenant scoping: `UPDATE agent_approvals SET status = 'expired', decidedAt = ... WHERE status = 'pending' AND expiresAt < now()` will mark pending approvals expired across all tenants.
  - _Fix:_ Scope the expiry to a specific tenant (add `AND tenant_id = ${tenantId}`) or iterate per-tenant. If a global sweep is intended, make it explicit and ensure downstream per-run updates validate tenant ownership before touching runs.
- **server/agentic/department-budgets.ts:143** — Query SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes a cross-tenant scan without constraining tenant_id. This reads tenant-scoped data for all tenants in one query and thus bypasses the application-level per-tenant filter invariant.
  - _Fix:_ If this function is intended to operate per-tenant, add an explicit tenant_id filter (WHERE tenant_id = ${tenantId}) and accept a tenantId parameter. If a global sweep across all tenants is intended, require and enforce an explicit admin-only execution context (document + assert admin privileges before running) or move the logic into an admin-only service/job and audit it; alternatively implement DB-side protections (e.g. RLS) or an explicit code-level guard to make the cross-tenant intent explicit and prevent accidental invocation by non-admin code.
- **server/agentic/resume-worker.ts:66** — SELECT query pulls agent_runs JOIN agent_approvals without constraining to a tenant (no WHERE tenant_id = <tenant>), returning cross-tenant rows.
  - _Fix:_ Restrict the initial candidate query to a specific tenant (pass tenantId into the worker / iterate tenants) or otherwise include an explicit WHERE a.tenant_id = ${tenantId} / r.tenant_id = ${tenantId}. If this job must be multi-tenant/admin, ensure it runs with an explicit, auditable admin privilege and that downstream logic treats rows as admin-only (and not execute per-tenant continuations without verifying the tenant context).
- **server/agentic/runs.ts:34** — appendStep() updates agent_runs without scoping by tenant (WHERE eq(agentRuns.id, runId) only). This allows cross-tenant updates if runId is mis-bound to another tenant.
  - _Fix:_ Require and pass tenantId into appendStep and include it in the WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Update all callers to supply/verify the tenant before calling appendStep (or resolve the run and assert its tenant matches the caller).
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs without a tenant constraint (.where(eq(agentRuns.id, runId))).
  - _Fix:_ Add tenant scoping to the WHERE clause (accept tenantId and use and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers provide and/or verify tenantId before invoking this function.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs without a tenant constraint (.where(eq(agentRuns.id, runId))).
  - _Fix:_ Require tenantId and include eq(agentRuns.tenantId, tenantId) in the WHERE predicate. Update callers accordingly so all updates are tenant-scoped.
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs (status/result/completedAt) without scoping the UPDATE to the tenant (WHERE eq(agentRuns.id, runId) only).
  - _Fix:_ Add tenant scoping: include eq(agentRuns.tenantId, tenantId) in the WHERE clause (and accept tenantId as an argument or resolve + verify the run's tenant before updating).
- **server/agentic/runs.ts:88** — failRun() updates agent_runs status/error/completedAt without including tenant scoping in the WHERE clause.
  - _Fix:_ Require tenantId and add it to the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers provide/verify tenantId prior to calling failRun to prevent cross-tenant writes.
- **server/agentic/runs.ts:97** — pauseRun() updates agent_runs status and state without constraining by tenant (WHERE eq(agentRuns.id, runId) only).
  - _Fix:_ Change pauseRun to accept tenantId and include eq(agentRuns.tenantId, tenantId) in the WHERE clause, or otherwise resolve and assert the run's tenant before performing the update so all writes are tenant-scoped.
- **server/agentic/wake-scheduler.ts:142** — Global UPDATE/claim on tenant-scoped table without tenant filter: the single-statement claim sets status = 'firing' for rows selected by an inner SELECT that does NOT restrict by tenant_id. This can mutate/claim wakes across tenants.
  - _Fix:_ Scope the claim to a specific tenant (or run per-tenant). Add AND tenant_id = <tenantId> (or equivalent) to the inner SELECT/outer UPDATE, or iterate tenants and run the claim per-tenant so no cross-tenant rows are selected/updated. Ensure every UPDATE on agent_wake_schedules includes tenant_id = callerTenant.
- **server/auto-asset-capture.ts:145** — INSERT into project_files omits tenant_id. The code comments rely on transitive isolation via project ownership, but per invariant every INSERT on tenant-scoped data must include an explicit tenant_id column/value.
  - _Fix:_ Make the tenant explicit in the INSERT. Either add tenant_id to project_files (if not present) and include ${tenantId} in the VALUES, or validate/derive the project->tenant mapping and use an INSERT that writes tenant_id explicitly. Alternatively, perform this write via a query that enforces the project belongs to the caller's tenant in the WHERE clause (e.g. insert-select from projects where id=${projectId} AND tenant_id=${tenantId}).
- **server/auto-asset-capture.ts:164** — INSERT into project_notes omits tenant_id. This writes a project-scoped note without an explicit tenant_id column/value, relying on the project_id alone for transitive isolation.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. add tenant_id column and VALUES ${tenantId}), or perform an INSERT selecting/validating the project row belongs to the caller's tenant (INSERT ... SELECT ... FROM projects WHERE id=${projectId} AND tenant_id=${tenantId}) so the tenant is enforced server-side for every write.
- **server/auto-memorize.ts:47** — getWatermark() reads agent_knowledge without constraining by tenant_id (SELECT ... FROM agent_knowledge ...). agent_knowledge rows are tenant-scoped elsewhere, so this can return another tenant's watermark.
  - _Fix:_ Change getWatermark to accept a tenantId and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise scope the SELECT to the intended tenant). Ensure callers pass the correct tenantId.
- **server/auto-memorize.ts:62** — setWatermark() first SELECT (SELECT id FROM agent_knowledge ...) does not filter by tenant_id, so it may acquire an id belonging to another tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the SELECT used to find an existing watermark. Make setWatermark accept the tenantId and use it consistently for all reads/updates in this operation.
- **server/auto-memorize.ts:163** — runAutoMemorize pulls messages across all tenants in a single query (SELECT ... FROM messages m ... WHERE m.created_at > ... AND m.role IN ...). This SELECT lacks a tenant_id = <tenant> constraint and will mix data from multiple tenants.
  - _Fix:_ Do per-tenant queries instead of one global query: enumerate tenants (or query tenant-specific watermark per tenant) and run SELECT ... FROM messages WHERE tenant_id = ${tenantId} AND created_at ... for each tenant. Alternatively, require and pass a tenantId into runAutoMemorize and scope the query to that tenant.
- **server/auto-transcript.ts:165** — Backfill code executes an UPDATE on conversations without constraining by tenant_id (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL). That can modify another tenant's conversation if IDs are guessed/poisoned.
  - _Fix:_ Include AND tenant_id = ${c.tenant_id} in the UPDATE WHERE clause (or otherwise verify ownership) so the update only affects rows belonging to the intended tenant.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. In a multi-tenant DB where tenant isolation is enforced only by application-level filters, omitting tenant_id on tenant-scoped child rows lets an attacker/bug create rows not tied to the caller's tenant (or orphan rows), breaking the invariant that every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Fix:_ Ensure every INSERT into tenant-scoped tables includes the tenant_id column explicitly (or validate ownership before inserting). For this case either: (1) add tenant_id to the invoice_items INSERT (INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)), or (2) explicitly verify the invoice_id belongs to the caller's tenant before inserting and set tenant_id accordingly. Also consider adding a NOT NULL tenant_id column to invoice_items (if missing), a DB constraint/foreign key linking invoice_items.tenant_id = invoices.tenant_id, and (ideally) Postgres RLS or FK checks to enforce tenant isolation at the DB level.
- **server/capability-review.ts:490** — UPDATE to tenant-scoped table `capability_reviews` performed without constraining by tenant_id. The query `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` does not include a tenant check, so a reviewer in one tenant could affect rows belonging to another tenant if an id is reused or attacker can influence the reviewId mapping.
  - _Fix:_ Include the tenant_id in the WHERE clause and/or require the caller to supply/verify tenant context. Change the query to: `WHERE id = ${pending.reviewId} AND tenant_id = ${tenantId} AND reused = false`. Pass tenantId into markReviewReused (or validate that the in-memory pending entry is bound to the caller's tenant) before executing the UPDATE. This enforces the invariant that every write against a tenant-scoped table is tenant-constrained.
- **server/channel-routing.ts:31** — Query JOINs into personas (JOIN personas p ON p.id = cr.persona_id) with no tenant_id constraint. If personas is a tenant-scoped table this SELECT can expose persona rows from other tenants.
  - _Fix:_ Enforce tenant isolation: either make channel_routes tenant-scoped (add tenant_id column, set it on INSERT, and filter WHERE cr.tenant_id = tenantId) or, at minimum, constrain the JOIN/WHERE to the caller's tenant (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = :tenantId or add WHERE p.tenant_id = :tenantId). Ensure tenantId is derived from an authenticated/trusted source, not caller-supplied raw input.
- **server/channel-routing.ts:54** — INSERT into channel_routes sets persona_id from the caller-supplied personaId without validating that the referenced persona belongs to the caller's tenant and without recording any tenant_id on the channel_routes row.
  - _Fix:_ Before inserting/updating, verify ownership: SELECT 1 FROM personas WHERE id = :personaId AND tenant_id = :tenantId and fail if not found. Better: add tenant_id to channel_routes (NOT NULL), populate it from the caller's tenant, and ensure all reads/updates/deletes on channel_routes filter by tenant_id so a tenant cannot write references to another tenant's persona.
- **server/chat-engine.ts:633** — SELECT from tenant-scoped table `compaction_archives` uses only conversation_id in the WHERE clause and lacks tenant_id = ${tenantId}. This can allow cross-tenant access if conversation ids are reused or an attacker obtains a conversation id.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${tenantId}) or perform the lookup via a join that enforces the conversation's tenant (JOIN conversations c ON c.id = compaction_archives.conversation_id AND c.tenant_id = ${tenantId}). Always include tenant_id in direct reads of tenant-scoped tables.
- **server/chat-engine.ts:651** — SELECT from tenant-scoped table `messages` is executed with WHERE conversation_id = ${c.id} but no tenant_id constraint. Even though the conversation list was tenant-filtered earlier, the query itself does not enforce tenant_id, increasing risk of cross-tenant reads if assumptions change.
  - _Fix:_ Include tenant scoping on the messages query (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or join messages to conversations and filter by conversations.tenant_id = ${tenantId}. Ensure every read against messages explicitly constrains to the caller's tenant.
- **server/chat-engine.ts:4421** — Call to storage.attachCitations(...) does not include tenant context. This writes/associates citation rows after creating a tenant-scoped message but does not pass tenant_id (or otherwise verify ownership), risking cross-tenant writes/reads.
  - _Fix:_ Pass the tenant id when attaching citations (e.g. storage.attachCitations(messageId, collectedCitations, tenantIdForScope)) or have attachCitations internally validate that the target message belongs to the caller's tenant (e.g. SELECT/WHERE tenant_id = ? before insert). Ensure every DB write inside attachCitations uses WHERE tenant_id = <caller tenant> (or equivalent Drizzle .where(eq(...))) so tenant isolation cannot be bypassed.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant constraint — the scanner does `SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' ...` and returns rows across all tenants.
  - _Fix:_ If cross-tenant enumeration is not intended, add `AND tenant_id = ${tenantId}` to scope the query to the caller. If this is an admin/system job that intentionally scans all tenants, make the admin intent explicit (e.g. admin-only code path), document it, and minimize returned columns. Consider moving to a dedicated admin-only query or table to avoid accidental exposure via non-admin callers.
- **server/delivery-pipeline.ts:203** — UPDATE on delivery_logs performed without constraining by tenant_id (db.update(deliveryLogs).set(...).where(eq(deliveryLogs.id, id))). This allows cross-tenant modification of delivery rows by id.
  - _Fix:_ Require the caller's tenant id or otherwise include tenant scoping in the WHERE clause, e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))). Consider changing updateDeliveryLog signature to accept tenantId and enforce it on every update.
- **server/delivery-pipeline.ts:1071** — SELECT from delivery_logs by stripePaymentId without tenant_id constraint (db.select().from(deliveryLogs).where(eq(deliveryLogs.stripePaymentId, paymentId))). This can return another tenant's delivery row.
  - _Fix:_ Scope by tenant. Add tenantId parameter and use WHERE stripe_payment_id = ... AND tenant_id = <tenantId>, or require the caller to provide and validate tenant ownership before returning the row.
- **server/dfy-intake.ts:50** — SELECT from dfy_intake_forms by token only (no tenant_id constraint). Token lookup returns tenant-scoped form data (PII) without tenant scoping.
  - _Fix:_ Include tenant scoping on the lookup or document and enforce that token-based access is intentionally multi-tenant. Prefer: .where(and(eq(dfyIntakeForms.token, token), eq(dfyIntakeForms.tenantId, tenantId))) or otherwise validate tenant ownership before returning the form.
- **server/dfy-intake.ts:93** — UPDATE dfy_intake_forms uses WHERE id = form.id only (.where(eq(dfyIntakeForms.id, form.id))). Missing tenant_id constraint allows updating another tenant's form row.
  - _Fix:_ Add tenant scoping to the UPDATE: include AND eq(dfyIntakeForms.tenantId, form.tenantId) (or accept tenantId param) so only the owning tenant's form can be modified.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks executed without tenant_id in WHERE clause (DELETE FROM doc_chunks WHERE doc_path = ... AND collection_id = ...). This can delete chunks across tenants if collection_id/doc_path are not globally protected.
  - _Fix:_ Add tenant constraint to the DELETE, e.g. WHERE doc_path = ... AND collection_id = ... AND tenant_id = ${tenantId}. Ensure collection existence check and subsequent deletes are consistently tenant-scoped.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} executed without tenant_id constraint. This updates a chunk row by id across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Propagate tenantId into the function or validate ownership before updating rows by id.
- **server/dream-consolidation.ts:83** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} ... executed without tenant_id constraint. messages is tenant-scoped; reading by conversation id alone may cross tenants if conversation ids collide or are not globally unique.
  - _Fix:_ Include tenant scoping on the messages query (AND tenant_id = ${tenantId}) or otherwise validate that the conversation belongs to the tenant before selecting messages. Prefer defensive WHERE clauses that include tenant_id.
- **server/email.ts:471** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 executed without tenant_id constraint while polling shared inbox. inbox_messages is tenant-scoped; this can return another tenant's stored message and cause cross-tenant behavior.
  - _Fix:_ Include tenant scoping in the existence check, e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1, or normalize message ownership before the check. Ensure polling stores/queries are always tenant-aware.
- **server/email.ts:515** — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') AND (body_html IS NULL OR body_html = '') ORDER BY id executed without tenant_id constraint in backfillEmptyBodies(). This scans inbox_messages across tenants.
  - _Fix:_ Restrict the query to a tenant (WHERE tenant_id = ${tenantId}) or iterate tenant-by-tenant under a trusted admin path. Avoid global scans over tenant-scoped inbox_messages unless explicitly intended and access-controlled.
- **server/embeddings.ts:752** — SELECT FROM agent_knowledge (backfillMissingKnowledgeEmbeddings) is not scoped by tenant_id — reads rows across all tenants.
  - _Fix:_ Scope the backfill to a specific tenant (accept tenantId or iterate per-tenant) and add AND tenant_id = ${tenantId} to the WHERE clause. Avoid cross-tenant queries on tenant-scoped tables.
- **server/embeddings.ts:780** — SELECT FROM memory_entries (backfillEmbeddingVecs) is not scoped by tenant_id — reads rows across all tenants.
  - _Fix:_ Require a tenantId parameter (or iterate per-tenant) and add AND tenant_id = ${tenantId} to the WHERE clause so only a single tenant's rows are processed.
- **server/embeddings.ts:796** — SELECT FROM agent_knowledge (backfillEmbeddingVecs) is not scoped by tenant_id — reads rows across all tenants.
  - _Fix:_ Limit the query to a single tenant by accepting tenantId (or looping per tenant) and adding AND tenant_id = ${tenantId} to the WHERE clause. Avoid global reads on tenant-scoped tables.
- **server/embeddings.ts:244** — Use of sql.raw() with an interpolated vector literal (vec) derived from request input/LLM output inside the query (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). This constructs SQL via string interpolation of a request-derived value.
  - _Fix:_ Avoid sql.raw interpolation of request-derived data. Bind the vector as a parameter (prepared/parameterized query) or use a safe driver API for pgvector so the value is passed as a bound parameter rather than injected text. Validate and canonicalize data, and ensure tenant scoping remains present.
- **server/embeddings.ts:252** — ORDER BY uses sql.raw() with an interpolated vector literal (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) where vec is derived from the caller's query/LLM output.
  - _Fix:_ Replace sql.raw interpolation with a parameterized binding for the vector. Use Drizzle/pg parameter binding or a safe helper that constructs a vector parameter rather than injecting SQL text. Keep the tenant WHERE clause (already present) and avoid raw interpolation.
- **server/embeddings.ts:566** — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). The vec value is derived from the request/LLM embedding and is being injected into raw SQL.
  - _Fix:_ Do not inject the vector text via sql.raw. Instead bind the vector value as a parameter (use parameterized queries or driver-level binding for pgvector). Validate the embedding shape beforehand and keep tenant scoping; avoid constructing SQL fragments from request-derived strings.
- **server/embeddings.ts:572** — ORDER BY uses sql.raw() with interpolated vector literal (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) where the vector originates from request/LLM output.
  - _Fix:_ Pass the vector as a bound parameter to the query (avoid sql.raw interpolation). Use safe driver/ORM support for pgvector comparisons so the vector value is not inserted as raw SQL text. Maintain tenant_id filtering and validation.
- **server/episode-playbooks.ts:68** — String-interpolated vector literal (${embeddingLiteral}::vector) is injected into SQL for nearest-neighbor ordering/score. The embedding is derived at runtime (LLM/provider) and is interpolated into the query string context.
  - _Fix:_ Avoid inserting embedding text as a raw snippet; instead bind the embedding as a parameter so the DB receives it via a prepared/parameterized value (e.g. $1::vector). Alternatively, provide a Drizzle-safe binding helper for pgvector comparisons to prevent SQL construction from request-derived strings. Always keep tenant_id filter (already present).
- **server/episode-playbooks.ts:120** — String-interpolated embedding literal (embeddingLiteral) used in SELECT/ORDER BY as ${embeddingLiteral}::vector when retrieving playbooks — embedding is runtime-derived and is being injected into SQL.
  - _Fix:_ Use parameterized query bindings for the embedding (pass as a bound parameter cast to vector) or use a safe helper/ORM facility to compare vectors, avoiding insertion of a runtime-produced string into raw SQL. Continue to enforce tenant_id in WHERE clauses.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' is executed without any tenant_id filter (reads running/pending runs across all tenants).
  - _Fix:_ Restrict the query to the calling tenant (e.g. add WHERE tenant_id = ${tenantId} or .where(eq(agent_runs.tenantId, tenantId)), or perform the rehydration per-tenant context so you only read that tenant's rows. Failing closed (skip/ignore rows not matching the expected tenant) is required for tenant isolation.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with NO tenant_id constraint. This updates a row by id without explicitly scoping to the tenant.
  - _Fix:_ Include the tenant constraint in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId} or .where(and(eq(felix_loop_runs.id, runId), eq(felix_loop_runs.tenantId, tenantId)))) to ensure the update only affects rows belonging to the caller's tenant.
- **server/firecrawl.ts:214** — UPDATE scraped_pages .where(eq(scrapedPages.id, existing[0].id)) updates a tenant-scoped table row by id but lacks an explicit tenant_id condition.
  - _Fix:_ Add tenant scoping to the update (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the update cannot touch another tenant's row even if the id is supplied or mismatched.
- **server/firecrawl.ts:344** — UPDATE scraped_pages .where(eq(scrapedPages.id, existing[0].id)) inside crawl loop updates a scraped_pages row by id without an explicit tenant_id filter.
  - _Fix:_ Include tenant filtering on updates (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise assert ownership before performing the update to enforce tenant isolation.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' by project id uses WHERE eq(projects.id, projectId) but does NOT include tenant_id in the WHERE clause (querying a tenant-scoped table without tenant scoping).
  - _Fix:_ Scope the SELECT to the caller's tenant (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or immediately validate tenant ownership via a tenant-scoped WHERE before returning any row. Always prefer SQL-level scoping (WHERE tenant_id = ...) to enforce isolation.
- **server/google-drive.ts:560** — UPDATE projects SET driveFolderId/... uses WHERE eq(projects.id, projectId) without a tenant_id constraint, allowing modification of a project row by id without explicit tenant scoping.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so only the owning tenant's project row can be modified.
- **server/google-drive.ts:826** — SELECT from tenant-scoped table 'projects' in uploadAndShare is performed with WHERE eq(projects.id, params.projectId) and no tenant_id filter. Although code later compares proj.tenantId to params.tenantId, the initial SQL is not scoped by tenant.
  - _Fix:_ Add the tenant filter in the SQL (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) so the DB enforces isolation. Avoid selecting tenant-scoped rows by id alone; always include tenant_id in WHERE or use a tenant-scoped helper/assertion before performing DB reads/writes.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks omits tenant scoping — enumerates tasks across tenants without filtering tenant_id.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, add WHERE tenant_id = ${tenantId} (and run the scanner per-tenant) or otherwise ensure the query only targets the intended tenant. Avoid global selects that touch tenant-scoped resources without explicit admin/tenant context checks.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) has no tenant_id filter — can disable tasks for other tenants.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or derive ids from a tenant-scoped SELECT/CTE and use that for the UPDATE so only the intended tenant's rows are modified.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images does not include tenant_id — writing to a tenant-scoped table without setting tenant_id explicitly or validating ownership of the referenced session_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. VALUES (${sessionId}, ${idx}, ..., ${tenantId}, ...)). Additionally, before writing, verify the session_id belongs to the expected tenant (or query presenter_sessions with WHERE id = ${sessionId} AND tenant_id = ${tenantId}) to prevent cross-tenant writes when sessionId could be untrusted.
- **server/google-workspace.ts:2364** — Second INSERT/UPsert into presenter_slide_images (thumb) similarly omits tenant_id — writes tenant-scoped data without explicit tenant column.
  - _Fix:_ Include tenant_id in this INSERT/ON CONFLICT upsert and/or verify session ownership before persisting thumbnails. Ensure all presenter_slide_images rows record tenant_id and are written only when tenant is known/trusted.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks tenant scoping — updates a presenter session by id with no tenant_id predicate or ownership check.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first SELECT/validate presenter_sessions row to confirm it belongs to the caller's tenant before performing the update. Never update tenant-scoped rows by foreign id alone without verifying ownership.
- **server/heartbeat.ts:53** — SELECT from tenant-scoped table `research_schedules` without a tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Fix:_ If this is intentional for a system-wide sweep, be explicit and treat it as an admin-wide operation. Otherwise, add a WHERE tenant_id = ${<tenantId>} filter. At minimum, ensure any subsequent UPDATE/DELETE that acts on rows found here also includes tenant_id = ${sched.tenant_id} to avoid cross-tenant modifications.
- **server/heartbeat.ts:102** — UPDATE on tenant-scoped table `research_schedules` uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Fix:_ Scope the UPDATE to the row's tenant: add AND tenant_id = ${sched.tenant_id} (or include tenant_id in the WHERE clause from the selecting context) so writes cannot affect another tenant's row by id alone.
- **server/heartbeat.ts:108** — UPDATE on tenant-scoped table `research_schedules` uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Fix:_ Include tenant_id = ${sched.tenant_id} in the WHERE clause to ensure the update is scoped to the same tenant that owns the schedule row.
- **server/heartbeat.ts:113** — UPDATE on tenant-scoped table `research_schedules` uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause so next_run_at/last_run_at changes are applied only to the owning tenant's row.
- **server/heartbeat.ts:121** — UPDATE on tenant-scoped table `research_schedules` (recovery path) uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Fix:_ Include tenant_id = ${sched.tenant_id} in the WHERE clause to avoid accidentally modifying rows belonging to other tenants.
- **server/heartbeat.ts:385** — SELECT from tenant-scoped table `event_log` without tenant_id constraint (SELECT * FROM event_log WHERE status='pending' ...). This returns pending events across all tenants.
  - _Fix:_ If the intention is to process only a specific tenant, add WHERE tenant_id = ${tenantId}. If the system must poll all tenants, be explicit about admin scope and ensure all downstream writes/updates for processed rows include tenant_id in their WHERE clauses (e.g., UPDATE ... WHERE id = ${id} AND tenant_id = ${tenant_id}).
- **server/heartbeat.ts:273** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — raw UPDATE on tenant-scoped `heartbeat_tasks` without tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g., WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) so changes to task rows cannot affect another tenant's task by id alone.
- **server/heartbeat.ts:277** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — raw UPDATE on tenant-scoped `heartbeat_tasks` without tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} (or equivalent) to the WHERE clause to ensure the update is scoped to the owning tenant.
- **server/heartbeat.ts:2764** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — raw UPDATE to tenant-scoped `heartbeat_tasks` without tenant_id constraint (in processDelegations).
  - _Fix:_ Include tenant_id = ${newTask.tenantId} (or tenantId variable used when creating the task) in the WHERE clause when updating approval_status to ensure approval state cannot be modified across tenants.
- **server/heartbeat.ts:3216** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — raw UPDATE to tenant-scoped `heartbeat_tasks` without tenant_id constraint (in delegateTaskFromChat).
  - _Fix:_ Add AND tenant_id = ${newTask.tenantId} to the WHERE clause (or use a storage helper that enforces tenant scoping) so the approval_status update is bound to the task's tenant and cannot affect tasks in other tenants.
- **server/job-queue.ts:320** — listJobs builds and executes a SELECT FROM agent_jobs without a tenant_id constraint when tenantId is not provided. That returns rows across tenants by default (data exposure risk).
  - _Fix:_ Require callers to pass a tenantId for tenant-scoped listing APIs, or explicitly gate and audit cross-tenant listing to admin-only paths. Prefer default behavior that applies `WHERE tenant_id = ${tenantId}` unless an explicit, strongly-authenticated admin intent is present.
- **server/job-queue.ts:385** — getJobStats runs an aggregate SELECT FROM agent_jobs without any tenant_id filter, returning counts across tenants (potential information disclosure).
  - _Fix:_ Add an option to scope stats by tenant and default to per-tenant aggregation: e.g. `WHERE tenant_id = ${tenantId} AND created_at > ...`. If cluster-wide stats are required, move them to a dedicated global metrics pipeline and ensure access is limited to admins.
- **server/lib/action-ledger.ts:268** — Cross-tenant SELECT: listUnknownAttemptsAllTenants queries action_attempts WHERE state = 'unknown' with no tenant_id predicate.
  - _Fix:_ If this is not intentionally a cross-tenant reconciler path, add a tenant filter (WHERE tenant_id = ${tenantId}) or otherwise ensure only a privileged maintenance process may run this. If it is intentionally cross-tenant, document and justify the exception and ensure downstream writes always constrain by the row's tenant_id.
- **server/lib/agent-trace.ts:124** — UPDATE without tenant filter: persistSpanClose runs UPDATE agent_trace_spans ... WHERE span_id = ${spanId} with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) or otherwise validate the span belongs to the current tenant before updating to prevent cross-tenant modification.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE on procedure_evolution_runs lacks tenant predicate: the code updates procedureEvolutionRuns by id only (.where(eq(procedureEvolutionRuns.id, runId)) ) without checking tenant_id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND eq(procedureEvolutionRuns.tenantId, tenantId)) or pass/verify the run's tenantId when performing the update to prevent cross-tenant mutations.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE on procedure_evolution_runs lacks tenant predicate: the noop-completion update uses .where(eq(procedureEvolutionRuns.id, runId)) without tenant filtering.
  - _Fix:_ Add tenant_id to the WHERE clause (eq(procedureEvolutionRuns.tenantId, tenantId)) or otherwise bind updates to the creating tenant to avoid touching another tenant's run row.
- **server/lib/aevo-meta-editor.ts:500** — UPDATE on procedure_evolution_runs lacks tenant predicate: final `done` update uses .where(eq(procedureEvolutionRuns.id, runId)) without tenant scoping.
  - _Fix:_ Constrain updates by tenant (include eq(procedureEvolutionRuns.tenantId, tenantId)) or pass the tenantId into the update call to enforce per-tenant isolation.
- **server/lib/bwb-job-progress.ts:141** — Potential cross-tenant UPDATE: setBwbPhase updates video_jobs using .where(scope(jobId, ...)) and scope may omit tenant predicate when BWB_TENANT_ID is not provided, allowing updates across tenants keyed only by jobId.
  - _Fix:_ Require and pass an explicit tenantId for progress writers, and ensure scope always includes eq(videoJobs.tenantId, tenantId). If env-threading is relied on, fail or no-op when the tenant id is missing instead of falling back to jobId-only writes.
- **server/lib/bwb-job-progress.ts:166** — Potential cross-tenant UPDATE: updateBwbChapters updates video_jobs using .where(scope(jobId, ...)) where scope can omit tenant_id if env tenant is missing, enabling cross-tenant writes keyed only by jobId.
  - _Fix:_ Make tenantId mandatory for these updates or ensure scope always includes a tenant predicate. If the tenant cannot be determined, abort the write to avoid touching other tenants' rows.
- **server/lib/bwb-job-progress.ts:200** — Potential cross-tenant SELECT: completeBwbJob reads video_jobs via .where(scope(jobId)) and scope may lack tenant predicate if BWB_TENANT_ID is not set, exposing/reading rows across tenants.
  - _Fix:_ Ensure the SELECT includes eq(videoJobs.tenantId, tenantId) by requiring the tenant id to be provided (avoid relying on environment-only threading), or otherwise restrict this code path to a trusted process with explicit tenant context.
- **server/lib/bwb-job-progress.ts:211** — Potential cross-tenant UPDATE: completeBwbJob later updates video_jobs using .where(scope(jobId)) - if scope omits tenant_id the update could affect other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (eq(videoJobs.tenantId, tenantId)) or require explicit tenantId propagation and fail fast when it's not available.
- **server/lib/bwb-job-progress.ts:227** — Potential cross-tenant UPDATE: bumpBwbHeartbeat updates video_jobs using .where(scope(jobId, ...)) and scope may omit tenantId, allowing heartbeat writes to affect rows across tenants.
  - _Fix:_ Require tenantId or otherwise ensure scope always includes eq(videoJobs.tenantId, tenantId). If the tenant is unknown, do not perform the update.
- **server/lib/bwb-job-progress.ts:245** — Potential cross-tenant UPDATE: failBwbJob updates video_jobs using .where(scope(jobId, { notDone: true })) and scope can omit tenant predicate when the env tenant is missing, permitting cross-tenant status changes.
  - _Fix:_ Add a required tenant_id predicate to these updates or make the function require a tenantId parameter; avoid falling back to jobId-only scoping to preserve tenant isolation.
- **server/lib/ideabrowser-backfill.ts:49** — Query reads the tenant-scoped projects table without constraining to a specific tenant_id (SELECT tenant_id, count(*) ... FROM projects WHERE tags && ... GROUP BY tenant_id). This performs an unscoped read across all tenants, violating the invariant that tenant-scoped tables must be filtered by tenant_id at the application level.
  - _Fix:_ Avoid cross-tenant reads. Run per-tenant queries or explicit per-snapshot-tenant checks instead of aggregating across all tenants. For example, iterate the snapshot's tenant IDs and for each do: SELECT count(*) FROM projects WHERE tenant_id = ${tenantId} AND tags && ${IDEA_TAG_LITERAL}::text[]. If this aggregate must run as an admin script, ensure it runs in a tightly controlled admin-only context and is audited/logged, but prefer per-tenant WHERE clauses to enforce isolation.
- **server/lib/ideabrowser-score.ts:181** — Unsafe string-interpolated SQL array literal built from caller-derived projectIds (`const literal = `{${ids.join(",")}}``) is injected into a db.execute(sql`... ANY(${literal}::int[])`) call. This constructs SQL text from user-controlled values instead of using a parameterized array, creating an injection risk and undermining DB-level safety assumptions.
  - _Fix:_ Do not build a Postgres array literal by joining ids into SQL text. Use a parameterized array (e.g. pass the numeric ids as a JS array bound to the query), or use Drizzle/pg helpers to bind an int[] parameter (e.g. ANY(${sql.array(ids, 'int')}) or WHERE rm.project_id = ANY(${ids}::int[]) via a parameter). Alternatively use Drizzle's query builder .where(in(table.projectId, ids)). Always let the driver parameterize the IDs rather than interpolate them into SQL text.
- **server/lib/inbox-ingest.ts:506** — UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id} — the UPDATE lacks an explicit tenant_id predicate. Per the project's tenant-isolation invariant, every UPDATE against tenant-scoped tables must also constrain by tenant_id to avoid cross-tenant modification races when IDs collide.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}). Prefer dual-key CAS predicates (tenant_id + PK) on all UPDATE/DELETE statements touching tenant-scoped tables to ensure a caller cannot affect another tenant's rows.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions performs a SELECT on tenant-scoped table deliverable_grade_events without constraining the query to a specific tenant (no WHERE tenant_id = ...). This reads across all tenants.
  - _Fix:_ Restrict the query to a single tenant or otherwise make cross-tenant access explicit and auditable. Options: (A) change scanOutcomePromotions to accept a tenantId and add WHERE tenant_id = ${tenantId} to the query so it only processes one tenant at a time; (B) if the job must iterate all tenants, make that explicit (e.g. enumerate tenants with a separate admin-only query and then run the per-tenant aggregation for each), and ensure the job runs in a privileged, auditable context. Avoid scanning tenant-scoped tables without explicit tenant scoping.
- **server/lib/scheduled-post-runner.ts:61** — Query selects/upates scheduled_posts without any tenant_id filter (SELECT id FROM scheduled_posts ... FOR UPDATE SKIP LOCKED / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). scheduled_posts is tenant-scoped elsewhere; performing unlocked cross-tenant SELECT/UPDATE risks violating tenant isolation.
  - _Fix:_ Either scope the CTE to a single tenant (add WHERE tenant_id = ${tenantId}) and run the runner per-tenant, or explicitly iterate tenants and run a tenant-scoped SELECT ... FOR UPDATE per tenant. If the intent is a system-wide admin job, make that explicit and restrict execution to an admin-only context and retain tenant_id in the UPDATE (e.g. JOIN on tenant_id or include tenant_id in the CTE) so every UPDATE also includes WHERE tenant_id = <row tenant> to maintain explicit application-level isolation.
- **server/martech-bundle.ts:325** — UPDATE on tenantVoiceProfiles uses only id in WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) or otherwise ensure the UPDATE is parameterized by the caller's tenant to prevent cross-tenant modification.
- **server/memory-graph.ts:124** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — update lacks tenant_id constraint; writes to a tenant-scoped table are not filtered by tenant.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. UPDATE ... WHERE id = ${catId} AND tenant_id = ${tenantId}) or pass tenantId into the helper so the query can include eq(memoryCategories.tenantId, tenantId).
- **server/memory-graph.ts:183** — refreshCategoryCount uses a subquery that SELECTs count(*) FROM memory_entries without filtering by tenant_id, and then UPDATEs memory_categories WHERE id = ${categoryId} without tenant constraint. Both the SELECT and UPDATE are missing tenant scoping.
  - _Fix:_ Require and pass tenantId into refreshCategoryCount and add AND tenant_id = ${tenantId} to the inner SELECT and AND tenant_id = ${tenantId} to the UPDATE target (or otherwise constrain both queries via the tenant parameter). Example: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status = 'active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId}. 
- **server/memory-graph.ts:239** — SELECT 1 FROM memory_links WHERE ... LIMIT 1 checks links by ids without constraining by tenant_id — reads a tenant-scoped resource (memory_links / related rows) with no tenant filter.
  - _Fix:_ Constrain the existence check to the caller's tenant (e.g. join to memory_entries or include memory_links.tenant_id = ${tenantId} if the table has that column). Ensure all SELECTs on memory_links include tenant scoping to avoid cross-tenant discovery.
- **server/memory-graph.ts:494** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — UPDATE targets a tenant-scoped table but uses only id in WHERE clause (no tenant_id constraint).
  - _Fix:_ Include tenant filtering on the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or use a transaction context bound to the tenant and ensure the query is parameterized accordingly so it cannot modify another tenant's rows.
- **server/memory-intelligence.ts:273** — db.execute(UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}) — UPDATE uses only id in WHERE clause without tenant_id constraint; writes to a tenant-scoped table are not scoped to caller tenant.
  - _Fix:_ Include tenant filtering on the UPDATE (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or call a storage helper that enforces tenant ownership. Prefer passing tenantId into the helper and adding eq(memory_entries.tenant_id, tenantId).
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — UPDATE uses only id in WHERE clause, missing tenant_id constraint even though earlier SELECT restricted by tenant_id.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) or otherwise ensure the update is executed within a tenant-bound transaction and the query includes the tenant filter to prevent accidental cross-tenant updates.
- **server/minds-engine.ts:214** — INSERT into mind_tickets uses string-interpolated dependsOn array from caller input (`${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}`), which is user-derived and injected directly into the SQL string (SQL injection + isolation risk).
  - _Fix:_ Avoid building array literals via string interpolation. Pass the PK array as a parameterized value or use proper Drizzle array binding (e.g. WHERE id = ANY(${sql.array(eventIds, 'int')}), or use parameterized INSERT with a proper array parameter). Validate/sanitize the input list as integers before using it.
- **server/minds-engine.ts:327** — markEventsHandled updates mind_events using a string-interpolated array literal from caller-supplied eventIds (`WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[])`). This directly injects request-derived values into SQL.
  - _Fix:_ Use parameterized array parameters instead of interpolating into SQL. For example, pass eventIds as a bound parameter or use a helper to produce a safe array literal (sql.array or Drizzle's array binding). Validate that eventIds are numeric and belong to the caller's tenant before performing the update.
- **server/minerva-planner.ts:386** — Probe SELECT after a CAS miss executes `SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}` — if args.tenantId is omitted this reads a tenant-scoped row without a tenant_id constraint, risking cross-tenant data disclosure.
  - _Fix:_ Same mitigation as for the CAS UPDATE: require tenantId for external calls or always include tenant_id and error if it is missing. Never perform tenant-scoped reads without an explicit tenant constraint on untrusted inputs.
- **server/minerva-planner.ts:549** — getPlan falls back to an unscoped SELECT when tenantId is omitted (`SELECT * FROM plans WHERE id = ${planId}`), which can leak tenant-scoped data if called from an untrusted context.
  - _Fix:_ Make tenantId mandatory for external callers or always constrain queries to tenant_id and return null/error when tenantId is not supplied. Document and enforce an internal-only API surface for any unscoped access, and ensure callers cannot omit tenantId.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not include tenant_id (INSERT INTO project_files ... VALUES (...)) — row will not be explicitly tagged with tenant ownership.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Even if you validated ownership via assertProjectInTenant, the row must still carry tenant_id per the tenancy invariant.
- **server/mpeg-engine.ts:1416** — INSERT into tenant-scoped table `project_files` does not include tenant_id (INSERT INTO project_files ... VALUES (...)) in the parallel path.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure all write paths consistently set tenant_id explicitly.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` without a tenant_id filter (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE) — reads all tenants' subscriptions.
  - _Fix:_ Restrict queries to the intended tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = TRUE) or, if an admin/global scan is intended, document and isolate this behavior and ensure it runs under an explicit admin-only codepath.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes without tenant constraint — reads data across all tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) when operating on tenant-scoped rows. If this query intentionally needs to enumerate tenants, ensure it runs in an audited admin context and not as a per-tenant operation.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} lacks a tenant_id constraint — modifying tenant-scoped rows without explicit tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${tenantId}) to prevent accidental cross-tenant modification. Prefer always pairing id filters with tenant_id for safety.
- **server/oauth-subscriptions.ts:1258** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id (SELECT provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE) — iterates across tenants.
  - _Fix:_ Scope this query to a specific tenant (WHERE tenant_id = ${tenantId}) or move to an explicit admin-only routine and document/guard it. Avoid cross-tenant SELECTs in per-tenant code paths.
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE executes without tenant filter — returns rows for all tenants.
  - _Fix:_ Limit to a tenant when appropriate (WHERE tenant_id = ${tenantId}) or ensure the caller is explicitly an admin/global runner; document and protect cross-tenant scans.
- **server/pace-control.ts:85** — SELECT from tenant-scoped table `heartbeat_logs` without tenant scoping (COUNT... FROM heartbeat_logs WHERE created_at > ... AND <laneFilter>) — counts across tenants.
  - _Fix:_ Add tenant_id filter to the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or otherwise partition queries by tenant. Ensure checkPace is executed in a tenant-scoped context or as an explicit admin/global routine.
- **server/pace-control.ts:140** — SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... is executed without tenant_id constraint in getPaceSnapshot — aggregates across tenants.
  - _Fix:_ Restrict the snapshot queries to a tenant (WHERE tenant_id = ${tenantId}) or provide an explicit admin-only path for global snapshots. Avoid running unscoped queries on tenant-scoped tables in per-tenant contexts.
- **server/pace-control.ts:147** — SELECT grouping persona counts FROM heartbeat_logs WHERE created_at > ... is executed without tenant scoping — returns per-persona counts across all tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or run this as a documented admin/global operation. All reads from tenant-scoped tables should be tenant-scoped unless explicitly and safely intended otherwise.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without verifying tenant ownership or adding tenant_id filter — resource access by project id only.
  - _Fix:_ Require caller tenant context or explicitly include tenant_id in the WHERE clause (WHERE id = ${projectId} AND tenant_id = ${tenantId}). If projectId comes from an untrusted caller, validate that the project's tenant matches the caller before reading or writing project-scoped data.
- **server/project-brain.ts:332** — Backfill query selects projects JOIN conversations without any tenant_id constraints (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...).
  - _Fix:_ Scope the backfill to a specific tenant (add WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}), or iterate tenants and run per-tenant so no cross-tenant data is read or processed.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is executed without tenant scoping — reading conversations by project id only can cross tenants.
  - _Fix:_ Add tenant constraint (AND c.tenant_id = ${tenantId}) or ensure proj was resolved with tenant and use that tenant in the query to prevent cross-tenant reads.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant constraint — messages (tenant-scoped) read without validating ownership of conversation/conversationId.
  - _Fix:_ Include tenant scoping (AND tenant_id = ${tenantId}) or validate conversation -> project -> tenant ownership before selecting messages. Always propagate and use tenantId in queries that touch tenant-scoped tables.
- **server/process-governor.ts:595** — In disable_task action, UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} is executed without tenant_id constraint — this may disable tasks across tenants if id collisions or leaked ids occur.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${t.id} AND tenant_id = ${tenantId}) or perform the update using both id and tenant to ensure only the intended tenant's tasks are modified.
- **server/process-governor.ts:640** — In restart_agent action, UPDATE agent_desks SET ... WHERE id = ${desk.id} is executed without tenant_id filter — modifies agent_desks by id only without verifying tenant ownership.
  - _Fix:_ Add tenant scope to the UPDATE (WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or verify desk.tenant_id equals the caller's tenant before mutating. Always include tenant_id in mutating statements.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` without a tenant_id WHERE clause (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This is a cross-tenant read with no tenant-level SQL filter.
  - _Fix:_ If this global scan is intentional (cron runner), document and justify it and ensure every subsequent write/operation enforces per-row tenant checks before acting. Otherwise, restrict the query by tenant_id (e.g. add AND tenant_id = ${tenantId}) or provide an explicit `_allowGlobalSystemUseOnly`/admin path that is gated and auditable. Preferably iterate tenants explicitly and query per-tenant to avoid accidental data exposure.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` lacks a tenant_id constraint (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}).
  - _Fix:_ Include tenant_id in the WHERE clause to prevent cross-tenant modification (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}). Even if the row was read earlier, the UPDATE must still constrain by tenant_id to maintain the app-level isolation invariant.
- **server/reference-learner.ts:290** — UPDATE against tenant-scoped table `memory_entries` lacks a tenant_id constraint (UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}).
  - _Fix:_ Constrain the UPDATE by tenant_id (and persona_id if relevant) to enforce per-tenant safety, e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}. Do not rely solely on a prior SELECT to assert ownership.
- **server/research-engine.ts:495** — INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) does not set tenant_id. (Same pattern repeated at lines ~503-506.)
  - _Fix:_ project_notes appears tenant-scoped — include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)). Keep the existing assertProjectInTenant check (good) but also persist the tenant_id field explicitly to prevent cross-tenant attribution.
- **server/routes.ts:766** — Unscoped SELECT from tenant-scoped `projects` (backfillProjectDriveFolders) — no tenant_id constraint on the query.
  - _Fix:_ Either restrict the operation to an explicit admin/migration context that is not reachable from tenant requests, or add a tenant_id filter and process projects per-tenant. If the code must operate across tenants, ensure it's executed with explicit platform-admin intent (separate migration job) and avoid performing tenant-entity mutations from generic startup code without explicit admin gating.
- **server/routes.ts:1118** — SELECT from tenant-scoped `file_storage` by filename without tenant_id constraint in uploads middleware. Reading all owners for a basename may expose or mix tenant rows.
  - _Fix:_ Avoid scanning all tenants' rows for a filename. Prefer a tenant-scoped lookup first (WHERE filename = X AND tenant_id = authTenant) and then, if necessary, a separated and minimal existence check for collisions in other tenants (e.g. EXISTS(...) returning a boolean). Ensure the logic cannot reveal or accidentally restore another tenant's file data to a different tenant's context.
- **server/routes.ts:4338** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The raw SQL INSERT (`INSERT INTO project_notes (project_id, note, author) VALUES (...)`) writes a row referencing a project but omits tenant_id, relying only on earlier SELECT that fetched projId. Without explicitly writing tenant_id on INSERT (and with no RLS/fk cascade), this can create notes with no tenant or attach them to the wrong tenant.
  - _Fix:_ Include tenant_id in the INSERT and ensure ownership: validate the project belongs to the caller's tenant and then INSERT with tenant_id = ${tenantId} (or call storage.createProjectNote which enforces tenant). Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}). Prefer using the storage layer or a parameterized Drizzle insert that always sets tenant_id from the authenticated session rather than relying on upstream queries.
- **server/routes/admin-zombie-detector.ts:38** — SELECT reads from agent_trace_spans (CTE `used`) with no tenant_id constraint. agent_trace_spans appears tenant-scoped, so this query can aggregate/return cross-tenant usage.
  - _Fix:_ Restrict the query to the caller's tenant by adding a tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}) to the agent_trace_spans selection, or if the intent is a platform-wide aggregate, make that explicit (accept an explicit admin-scoped parameter and/or iterate per-tenant) and document that the table is global. Never read a tenant-scoped table without an explicit tenant filter.
- **server/routes/archive-rescue.ts:276** — INSERT builds a Postgres text[] by string-interpolating savedPaths into the SQL literal (${`{...}`}), where savedPaths is derived from uploaded filenames/paths. This constructs SQL text rather than using parameterized array binding and risks SQL injection and incorrect escaping (also bypasses safe parameterization patterns).
  - _Fix:_ Avoid string-interpolating arrays into SQL text. Use parameterized bindings or Drizzle insert APIs that accept array values (e.g. pass the array as a query parameter or use db.insert with the demo_image_paths column set to the savedPaths array). Ensure all user- or request-derived values are passed as parameters, not injected into SQL syntax.
- **server/routes/conversations.ts:98** — UPDATE on conversations omits a tenant_id constraint: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}. This modifies a tenant-scoped table without constraining the update to the caller's tenant.
  - _Fix:_ Add the tenant constraint to the WHERE clause (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or use the storage layer API that enforces tenant scope. Always include tenant_id in UPDATE/DELETE statements for tenant-scoped tables.
- **server/routes/conversations.ts:104** — UPDATE on projects omits a tenant_id constraint: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}. This updates a tenant-scoped table without restricting to the caller's tenant.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or call a helper/storage method that enforces tenant ownership. Never perform writes to tenant-scoped tables without verifying tenant ownership in the SQL WHERE clause.
- **server/routes/goal-ledger.ts:92** — Unscoped SELECT against tenant-scoped table `agent_jobs` (no tenant_id predicate). The helper `readActiveJobs()` issues a query returning jobs across all tenants.
  - _Fix:_ If this is intended to be cross-tenant only for platform ops, ensure the route is strongly gated (requirePlatformAdmin) and add an explicit code comment justifying the global read; otherwise restrict the query to the caller's tenant (add WHERE j.tenant_id = ${tenantId} or pass tenantId into readActiveJobs and bind it into the SQL).
- **server/routes/tenants.ts:268** — Using personaId from a global template / activePersona (template.personaId || activePersona?.id) when creating a tenant-scoped conversation without verifying that the referenced persona belongs to the caller's tenant or is explicitly allowed global persona. This can create a conversation that references a persona from another tenant (cross-tenant reference) or cause downstream reads of the persona row without a tenant ownership check.
  - _Fix:_ Before passing personaId into storage.createConversation, validate ownership of the persona id: e.g. if (personaId) { const p = await storage.getPersona(personaId); if (!p || p.tenantId !== tenantId && !isPlatformPersona(p)) return res.status(400/403); } Alternatively, copy the persona data into the conversation (instead of storing a foreign id) or ensure storage.createConversation itself loads the persona row with a tenant filter and rejects non-owned personas. Document and enforce the allowed global/platform personas explicitly.
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} with no tenant_id constraint. This can modify another tenant's ledger row if reservationId is mis-bound.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or validate that the reservation row belongs to the expected tenant before settling. Propagate the tenantId into settleFusionReservation and releaseFusionReservation calls.
- **server/second-opinion.ts:424** — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without a tenant_id constraint, allowing unintended cross-tenant ledger modifications.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or fetch+verify the row's tenant before performing the UPDATE. Make reservationId operations defensive against cross-tenant use.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true — no tenant_id constraint. heartbeat_tasks appears tenant-scoped elsewhere; this will disable delegation tasks across all tenants unintentionally.
  - _Fix:_ Scope the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or iterate tenants explicitly). If the intent is system-global, document it clearly and ensure this code runs with explicit admin authority; otherwise add tenant filters or per-tenant handling.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true — missing tenant_id constraint. This may disable tenant-specific tasks globally.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or parameterize the target tenant) or perform per-tenant disables intentionally and explicitly.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true — performed without tenant_id constraint despite heartbeat_tasks being updated with tenant_id in other places.
  - _Fix:_ Restrict the UPDATE to the intended tenant(s) (include AND tenant_id = ${ADMIN_TENANT_ID} or loop per tenant). If a global change is intended, make that explicit and gated by admin-only execution; otherwise add tenant checks to prevent cross-tenant impacts.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions executed with no tenant_id constraint. research_sessions is tenant-scoped but this query aggregates across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or pass an explicit tenant context. Use Drizzle .where(eq(research_sessions.tenantId, tenantId)) or include tenant_id in the SQL template to avoid cross-tenant reads.
- **server/seed.ts:2158** — SELECT id, name, model FROM research_programs WHERE is_active = true executed with no tenant_id constraint. research_programs is tenant-scoped — this reads programs for all tenants.
  - _Fix:_ Add a tenant filter to the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) before iterating. Use Drizzle .where(eq(research_programs.tenantId, tenantId), eq(research_programs.isActive, true)).
- **server/seed.ts:2516** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... executes against conversations without filtering by tenant_id. conversations is tenant-scoped, so this reads across tenants.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...) or use Drizzle .where(eq(conversations.tenantId, tenantId)). If the intent is to backfill only for a specific tenant, ensure tenant_id is included and that any subsequent INSERTs respect tenant boundaries.
- **server/seed.ts:3853** — DELETE from tenant-scoped table `heartbeat_tasks` has no tenant_id constraint. This DELETE will affect rows across all tenants.
  - _Fix:_ Constrain the DELETE to a specific tenant (e.g. WHERE tenant_id = <tenant>) or perform the cleanup per-tenant in a loop. If the intention is to only operate on the system/admin tenant, explicitly add AND tenant_id = 1 (or use the runtime tenant id). Prefer using Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) or a parameterized SQL binding rather than an unconstrained DELETE.
- **server/seed.ts:3881** — UPDATE on tenant-scoped table `memory_entries` lacks any tenant_id constraint. This backfill will modify memory entries for all tenants.
  - _Fix:_ Restrict the UPDATE to a tenant (e.g. add AND tenant_id = <tenant>), or iterate over tenants and run a per-tenant UPDATE. Use parameterized bindings or Drizzle's eq(memoryEntries.tenantId, tenantId) in the WHERE clause to ensure tenant isolation.
- **server/seed.ts:3907** — UPDATE on tenant-scoped table `conversations` modifies all rows (no tenant_id in WHERE).
  - _Fix:_ Add a tenant_id predicate to the WHERE clause (e.g. WHERE model = 'gpt-4.1' AND tenant_id = <tenant>) or perform the update per-tenant. Use Drizzle's .where(eq(conversations.tenantId, tenantId)) or parameterized SQL to avoid cross-tenant changes.
- **server/seed.ts:4121** — UPDATE against tenant-scoped table `agent_knowledge` uses only id in WHERE and omits tenant_id. Updating by id without verifying tenant allows changing rows in other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or verify the tenant ownership before issuing the UPDATE. Prefer Drizzle .where(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId)) or bind the tenant id obtained from the prior SELECT to the UPDATE to enforce isolation.
- **server/stability-watchdog.ts:177** — SELECT FROM heartbeat_logs is executed without any tenant_id constraint. This aggregates error counts across all tenants and is used to disable heartbeat tasks; performing this SELECT unscoped against a tenant-scoped table can read cross-tenant data.
  - _Fix:_ Either add an explicit tenant_id filter to scope the query to a single tenant, or if this is intentionally an administrative multi-tenant sweep, make the intent explicit (use the project's ALL_TENANTS sentinel or a dedicated admin-only API that documents and authorizes cross-tenant operations). Avoid implicit unscoped queries against tenant-scoped tables.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks is executed without any tenant_id constraint. This will delete disabled delegation tasks across all tenants, which violates tenant isolation if heartbeat_tasks is tenant-scoped.
  - _Fix:_ Restrict the DELETE to a specific tenant (add WHERE tenant_id = ${tenantId}) or, if the operation must run globally, first enumerate tenants and perform per-tenant deletes or use an explicit admin-scoped API/flag that makes the cross-tenant intent auditable. Prefer using storage helper functions that accept the explicit ALL_TENANTS sentinel when admin-wide changes are required.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log is executed without any tenant_id constraint. If event_log is tenant-scoped, this will remove events across all tenants without scoping.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) or, for an intended global purge, make the admin intent explicit (iterate tenants, or use an admin-only codepath that documents/authorizes cross-tenant deletions). Use tenant-scope helpers or storage APIs which accept explicit ALL_TENANTS when needed.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs in pruneOldLogs is executed without a tenant_id constraint. This will prune logs for all tenants indiscriminately if heartbeat_logs is tenant-scoped.
  - _Fix:_ Scope the delete by tenant_id, or if a global prune is required, implement it via an explicit admin path that either iterates tenants and runs per-tenant deletes or uses a clearly labeled ALL_TENANTS-safe helper. Avoid raw unscoped deletes on tenant-scoped tables.
- **server/storage.ts:298** — Unscoped SELECT from tenant-scoped table 'messages' in attachCitationMetadata — query omits tenant_id constraint.
  - _Fix:_ Restrict the SELECT to the owning tenant. Pass the conversation/tenantId into attachCitationMetadata and add a WHERE tenant_id = ${tenantId} (or join via conversations and filter by conversations.tenant_id). Avoid returning tenant data without explicit tenant-scoping or an ADMIN/ALL_TENANTS sentinel guarded by authorization.
- **server/storage.ts:399** — Unscoped UPDATE on tenant-scoped table 'messages' in attachCitations — no tenant_id constraint in WHERE clause.
  - _Fix:_ Include tenant filtering on the UPDATE (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}), or otherwise validate the message's tenant before updating. Do not rely solely on primary-key uniqueness to enforce tenant isolation.
- **server/storage.ts:248** — getConversationUnscoped performs a SELECT on tenant-scoped table 'conversations' without any tenant_id constraint (intentionally unscoped helper).
  - _Fix:_ Mark this API-level escape hatch as admin-only and require explicit authorization, or remove it. If an unscoped read is truly needed, require an explicit ALL_TENANTS sentinel and audit/guard the call site; otherwise call getConversation(id, tenantId) which enforces tenant ownership.
- **server/storage.ts:624** — UPDATE conversation_facts SET status = ... is executed with WHERE id IN (victims) but no tenant_id constraint — write uses primary key only, allowing potential cross-tenant mutation if IDs collide or callers misuse the API.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}), or perform the UPDATE using the same tenant-scoped filter used to select victims (preferred). Always enforce tenant equality on mutation statements, even when operating by id.
- **server/storage.ts:806** — updateMemoryEmbedding updates tenant-scoped table 'memory_entries' using WHERE id = ${id} only (no tenant_id constraint).
  - _Fix:_ Require and validate the tenant context (tenantId) and add a WHERE clause that includes eq(memory_entries.tenant_id, tenantId) (or use requiredTenantScope). This prevents accidental cross-tenant writes to embeddings.
- **server/storage.ts:818** — updateKnowledgeEmbedding updates tenant-scoped table 'agent_knowledge' with WHERE id = ${id} but no tenant_id constraint.
  - _Fix:_ Require the caller to pass/verify tenantId and add eq(agent_knowledge.tenant_id, tenantId) to the WHERE clause (or otherwise verify ownership) before updating embedding vectors.
- **server/storage.ts:841** — archiveExpiredMemories executes a global UPDATE on tenant-scoped table 'memory_entries' (no tenant_id filter).
  - _Fix:_ If this maintenance job should run per-tenant, iterate tenants or accept a tenantId/ALL_TENANTS sentinel and add tenant filtering. Otherwise, document and protect the admin privilege required to run this platform-wide mutation; do not silently perform unscoped updates.
- **server/storage.ts:858** — archiveStaleMemories executes a global UPDATE on tenant-scoped table 'memory_entries' without tenant_id constraint.
  - _Fix:_ Same as above: require explicit tenant scoping (per-tenant run or ALL_TENANTS with admin guard) and add tenant_id filtering to the UPDATE to preserve tenant isolation.
- **server/storage.ts:482** — deletePersona performs updates/deletes against tenant-scoped tables (conversations, memory_entries, daily_notes) without constraining by tenant_id — actions will affect all tenants' rows referencing the persona.
  - _Fix:_ Enforce tenant scoping on these mutations. Either accept a tenantId parameter and include eq(...tenant_id, tenantId) in each WHERE clause, or implement persona deletion in a tenant-aware manner so one tenant's persona removal does not impact others.
- **server/storage.ts:969** — updateHeartbeatTask can perform updates against heartbeat_tasks without tenant_id constraint when tenantId is omitted (WHERE falls back to id only).
  - _Fix:_ Make tenantId required for updates or always include a tenant check in the WHERE clause (e.g. AND tenant_id = ${tenantId}). If unscoped updates are needed for internal maintenance, require an explicit admin sentinel + authorization.
- **server/storage.ts:975** — deleteHeartbeatTask can delete from heartbeat_tasks using only id when tenantId is omitted (no tenant_id constraint), permitting cross-tenant deletes.
  - _Fix:_ Require tenant context for deletion operations or include tenant scoping in the WHERE clause. Protect any admin/unscoped delete with explicit admin checks and an ALL_TENANTS sentinel.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding executes UPDATE felix_proposals ... WHERE id = ${proposalId} without tenant_id constraint — writing a tenant-scoped table without tenant filtering.
  - _Fix:_ Require and validate tenantId when stamping args_embedding and add tenant filtering to the UPDATE (AND tenant_id = ${tenantId}). If called during proposal creation where tenant is known, pass that tenantId through to the function.
- **server/surprise-scorer.ts:141** — scoreProposalSurprise updates actual_outcome_embedding on felix_proposals with WHERE id = ${proposalId} but no tenant_id constraint — write lacks tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} on the UPDATE. Ensure the caller provides and you verify tenant ownership before mutating a proposal row.
- **server/surprise-scorer.ts:149** — Re-fetch of args_embedding uses SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} without tenant filter.
  - _Fix:_ Add tenant scoping to this SELECT (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) to avoid exposing or relying on rows from other tenants. Require the tenantId be passed in and validated.
- **server/surprise-scorer.ts:213** — UPDATE that writes surprise_score and surprise_band uses WHERE id = ${proposalId} with no tenant_id filter — cross-tenant mutation risk.
  - _Fix:_ Include tenant filter (AND tenant_id = ${tenantId}) on the UPDATE, and require caller-provided tenantId be validated before writing. Prefer using parameterized Drizzle eq(...) calls instead of unscoped id-only updates.
- **server/tools.ts:2994** — INSERT into project_files does not include tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) omits tenant_id, violating the invariant that tenant-scoped inserts must explicitly set tenant_id.
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) or otherwise ensure the table is not tenant-scoped. If the table is intended to be tenant-scoped, set tenant_id from the caller's tenant after validating ownership of the referenced project.
- **server/tools.ts:3008** — INSERT into project_notes does not include tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id, violating the requirement that every insert to a tenant-scoped table pass tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})) after verifying the caller owns the project. If project_notes is intentionally unscoped, document that and ensure other code does not assume a tenant_id column exists.
- **server/tools.ts:2890** — INSERT into project_conversations during project create does not include tenant_id. The code inserts (project_id, conversation_id) without tenant_id even though this links entities across tenants.
  - _Fix:_ Set tenant_id explicitly on INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId})) or ensure the join table is intentionally unscoped and that ownership checks are performed and enforced before insertion. Prefer explicit tenant_id to avoid cross-tenant contamination.
- **server/tools.ts:3031** — INSERT into project_conversations in link_conversation handler omits tenant_id. The insert INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not set tenant_id even though the handler links tenant-scoped resources.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})) after verifying ownership of the project and conversation. If the table intentionally lacks tenant_id, document and justify this design and ensure all code enforces ownership checks before writes.
- **server/tools.ts:4552** — INSERT into tenant-scoped table `project_notes` omits tenant_id in the column list/VALUES. The code verifies project ownership via assertProjectInTenant but does not include tenant_id in the INSERT, violating the invariant that tenant_id must be passed explicitly for tenant-scoped writes.
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Keep or add the assertProjectInTenant check before inserting to ensure ownership, but still supply tenant_id explicitly.
- **server/tools.ts:4556** — INSERT into tenant-scoped table `project_files` omits tenant_id in the column list/VALUES. Although assertProjectInTenant was used to confirm ownership, the INSERT does not set tenant_id explicitly as required by the tenancy invariant.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Ensure assertProjectInTenant ran beforehand to validate ownership, then write tenant_id from the verified caller context.
- **server/tools.ts:6405** — INSERT into tenant-scoped table `project_files` (from generate_audio) omits tenant_id in the column list/VALUES. The code calls assertProjectInTenant, but the INSERT itself does not include tenant_id.
  - _Fix:_ Modify the INSERT to include tenant_id (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...)). Keep the assertProjectInTenant check to verify ownership before inserting.
- **server/tools.ts:6939** — INSERT into tenant-scoped table `project_files` (from create_slideshow_video) omits tenant_id in the column list/VALUES. The code verifies project ownership but fails to pass tenant_id explicitly in the INSERT.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...)). Ensure assertProjectInTenant validated ownership prior to performing the write.
- **server/tools.ts:6939** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) call omits tenant_id even though project_files is tenant-scoped (no RLS/fk cascade).
  - _Fix:_ Include the tenant id column/value in the INSERT. For example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Even when you validated project ownership (assertProjectInTenant), you must still persist tenant_id explicitly to enforce application-level isolation.
- **server/tools.ts:8909** — UPDATE on tenant-scoped table 'proposed_skills' uses only the id in the WHERE clause (where(eq(proposedSkills.id, row.id))) — no tenant_id constraint. This allows a tenant to update another tenant's proposed_skills row by id.
  - _Fix:_ Add a tenant_id filter to the WHERE clause. Use something like .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) (or the equivalent SQL WHERE id = $1 AND tenant_id = $2) so updates are scoped to the caller's tenant.
- **server/tools.ts:8934** — UPDATE on tenant-scoped table 'proposed_skills' (marking accepted/promoted) filters only by id and not tenant_id (where(eq(proposedSkills.id, row.id))). This permits cross-tenant modification of proposals.
  - _Fix:_ Constrain the update by tenant as well. e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or include tenant_id in the raw SQL WHERE clause to ensure only the tenant's row is updated.
- **server/tools.ts:8950** — UPDATE on tenant-scoped table 'proposed_skills' when jury rejects (where(eq(proposedSkills.id, row.id))) lacks a tenant_id constraint. The update can affect rows across tenants if ids collide or are guessed.
  - _Fix:_ Require tenant scoping on updates. Add tenant filtering: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or use a helper that enforces tenant constraints for proposed_skills updates to prevent cross-tenant modifications.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against tenant-scoped table `browser_workflows` without constraining by tenant_id: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`.
  - _Fix:_ Include the caller's tenant in the WHERE clause for the UPDATE (do not rely on a prior SELECT). E.g. use wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`) or the Drizzle .where(eq(browserWorkflows.tenantId, tenantId)) form to enforce per-tenant scoping on every write.
- **server/tools/domains/files/handlers.ts:900** — UPDATE on tenant-scoped `projects` table omits tenant filter: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`.
  - _Fix:_ Constrain the UPDATE by tenant as well as id to enforce tenant isolation. For example: await db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`) or use Drizzle's .where(eq(projects.tenantId, tenantId)). Every UPDATE/DELETE on tenant-scoped tables must include the tenant_id guard.
- **server/tools/domains/knowledge/handlers.ts:87** — UPDATE of tenant-scoped table `knowledge_triples` is missing a tenant_id constraint (WHERE id = ${row.id} without AND tenant_id = ${tenantId}). This allows an attacker who knows/guesses a row id to affect rows belonging to other tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: UPDATE ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Alternatively, perform the update through a storage/DAO helper that enforces tenant scoping, or re-query the row with tenant_id and bail if it doesn't belong to the tenant (defense-in-depth). Ensure all writes/updates/deletes against tenant-scoped tables include tenant_id in the WHERE clause.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without constraining `tenant_id` (reads all tenants' pairing rows).
  - _Fix:_ Restrict the agent_knowledge query to the relevant tenant(s). Pass the caller/handler tenantId into findOrCreateConversationForPhone and add WHERE tenant_id = ${tenantId} (or otherwise ensure only operator-provisioned/global pairing rows are read). If cross-tenant reads are required, move such rows to a true global table with explicit access controls and avoid scanning tenant-scoped tables unfiltered.
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` by id with no tenant filter. Reads the conversation row across all tenants.
  - _Fix:_ When reading conversations to verify ownership, include the caller/expected tenant in the WHERE clause (e.g. WHERE id = ${conversationId} AND tenant_id = ${expectedTenantId}) instead of selecting by id alone. Alternatively, pass the caller tenantId to the function and use it to constrain the query so tenant isolation is preserved.
- **server/venture-discovery/budget.ts:155** — UPDATE on tenant-scoped table `agent_cost_ledger` uses WHERE id = ${reservationId} with no tenant_id check — an UPDATE by id alone can modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership in the UPDATE predicate (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Add tenantId as a parameter to settleVentureReservation so the update is always constrained to the reservation's tenant (defense-in-depth even if reservationId was created earlier in the same process).
- **server/venture-discovery/budget.ts:169** — UPDATE on tenant-scoped table `agent_cost_ledger` (release path) uses WHERE id = ${reservationId} without tenant_id constraint.
  - _Fix:_ As with settle, require and use tenantId in releaseVentureReservation and include WHERE id = ${reservationId} AND tenant_id = ${tenantId} to ensure a caller cannot affect another tenant's ledger row by id.
- **server/video-job-runner.ts:208** — SELECT from tenant-scoped table `video_jobs` without constraining by tenant_id — isCancelRequested(jobId) queries cancelRequested by jobId only, allowing cross-tenant reading of cancel flags.
  - _Fix:_ Pass the caller/tenantId into isCancelRequested and include a tenant filter in the WHERE clause (e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId)))) so the cancel flag is only visible to the owning tenant. If the call site cannot supply tenantId, ensure the function verifies ownership by first resolving and validating the job's tenant before returning cancel state.
- **server/video-job-runner.ts:236** — UPDATE on tenant-scoped table `video_jobs` performed with no tenant_id constraint — recoverStaleVideoJobs() marks stale jobs FAILED across all tenants.
  - _Fix:_ Either restrict the update to a specific tenant (include eq(videoJobs.tenantId, tenantId)) or ensure this operation is executed with explicit admin intent/privilege and audited. If the intention is per-tenant recovery, iterate tenants and run a tenant-scoped UPDATE for each; otherwise document and protect this global operation behind an admin-only path to avoid accidentally affecting other tenants' jobs.
- **server/whatsapp-approval.ts:376** — When tenantApprovalPhones has no per-tenant approval phone configured, the code iterates all tenant approval entries and calls getPendingConfirmations() then resolveToolConfirmation(found.id, approved) without verifying the found confirmation belongs to the tenant whose phone matched the sender. This can let a WhatsApp approval for tenant A resolve a pending confirmation that belongs to tenant B.
  - _Fix:_ Before resolving a pending confirmation, verify its tenant ownership. Filter pending confirmations by the matched tenant id (tid) or ensure the returned confirmation record includes tenantId and assert equality with tid. Alternatively have resolveToolConfirmation validate that the caller/approver is authorized for the confirmation's tenant and refuse cross-tenant approvals.

### MEDIUM (45)

- **server/auto-project.ts:112** — Query of messages for a conversation is not scoped by tenant (SELECT ... FROM messages WHERE conversation_id = ${conversationId}). messages is tenant-scoped elsewhere; the query lacks AND tenant_id = ${tenantId}.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages SELECT (or otherwise validate/derive tenant ownership) so messages read are constrained to the caller's tenant. If conversation ownership has already been validated, still include tenant_id guard to follow the codebase invariant.
- **server/auto-transcript.ts:46** — Selecting messages for a conversation omits tenant scoping (SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}). messages is tenant-scoped; the WHERE clause lacks tenant_id = ${tenantId}.
  - _Fix:_ Include AND tenant_id = ${tenantId} on the messages SELECT, or otherwise ensure messages are fetched only for the verified tenant. Prefer explicit SQL filtering to enforce isolation.
- **server/auto-transcript.ts:128** — Backfill query selects conversations with a subquery that counts messages using an unscoped messages table (AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0). The subquery does not enforce tenant_id and may count messages from other tenants for that conversation id.
  - _Fix:_ Scope the subquery to the conversation's tenant (e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0) or restructure to an explicit join that includes tenant filtering.
- **server/auto-transcript.ts:153** — alsoLinked query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without tenant scoping, potentially counting messages across tenants.
  - _Fix:_ Add tenant scoping to the subquery (AND tenant_id = c.tenant_id) or use an explicit join that validates message rows belong to the same tenant as the conversation.
- **server/capability-review.ts:416** — SQL array literal (`arrLiteral`) is constructed by stringifying/sanitizing untrusted asset names and then interpolated into a SQL template as `${arrLiteral}::text[]`. Building SQL array literals by concatenation of request-derived values is error-prone and risks injection or parsing issues.
  - _Fix:_ Avoid constructing SQL literals by hand. Instead parameterize the array properly (pass a JS array and let the DB driver/drizzle bind it as a text[]), or store surfaced names as JSONB using a parameterized JSON value, or insert rows into a separate normalized table. If you must emit a Postgres array literal, ensure robust escaping and preferably use driver-level array binding or Drizzle helpers so untrusted input is never directly embedded in SQL text.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` is executed without a tenant_id constraint (WHERE tenant_id = ...). The code relies on prior verification of project ownership but the SQL here has no explicit tenant filter.
  - _Fix:_ Explicitly constrain the query to the caller's tenant. e.g. add AND tenant_id = ${tenantId} to the WHERE clause or use a parameterized Drizzle `.where(eq(project_files.tenantId, tenantId))`. Do not rely only on previously-checked projectId — every tenant-scoped read must include the tenant_id filter.
- **server/chat-engine.ts:584** — SELECT from tenant-scoped table `project_notes` is executed without a tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id.
  - _Fix:_ Add an explicit tenant check to the query (AND tenant_id = ${tenantId}) or join with projects and filter by p.tenant_id = ${tenantId}. Prefer using Drizzle's `.where(eq(project_notes.tenantId, tenantId))` to ensure tenant-scoped reads.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM commitments enumerates tenant IDs from a tenant-scoped table without applying a caller-specific tenant filter — this reads tenant identifiers across all tenants.
  - _Fix:_ If enumerating tenants is necessary for a system-wide job, ensure this code runs only in a trusted admin context and is properly access-controlled/audited. If not, restrict enumeration to the specific tenant (add `WHERE tenant_id = ${tenantId}`) or use a separate admin-only mechanism to discover tenants.
- **server/data-protection.ts:360** — Selecting project notes without scoping to tenant_id: SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} ORDER BY created_at ASC. This reads a tenant-scoped table without a tenant filter.
  - _Fix:_ Constrain the SELECT by tenant, e.g. SELECT ... FROM project_notes WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} ORDER BY ... (or join through projects filtered by tenant_id) to guarantee tenant isolation.
- **server/data-protection.ts:371** — Selecting project files without scoping to tenant_id: SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} ORDER BY created_at ASC. Reads a tenant-scoped table with no tenant predicate.
  - _Fix:_ Add tenant scoping to the query, e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}, or otherwise join against projects filtered by p.tenant_id = ${tenantId} before returning project_files rows.
- **server/data-protection.ts:404** — Counting messages without scoping to tenant_id: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}. This may include messages from other tenants if tenant is not enforced on the read.
  - _Fix:_ Include tenant scoping in the count (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id} or tenant_id = ${tenantId}) so the message count is restricted to the intended tenant.
- **server/data-protection.ts:443** — Subquery used in getDeletedConversations reads messages without tenant scoping: (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id) as message_count. The nested SELECT lacks an explicit tenant_id predicate.
  - _Fix:_ Constrain the subquery by tenant, e.g. (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id AND tenant_id = ${tenantId}) as message_count, so per-tenant results cannot leak or be mixed.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages (no tenant filter) — reads messages across all tenants to compute traffic gate metrics. Tenant-scoped table accessed without tenant_id constraint.
  - _Fix:_ If this metric is intended to be per-tenant, add tenant_id filtering. If global aggregate is intentional, make the global intent explicit and restrict access to a trusted admin context; otherwise enforce tenant scoping (WHERE tenant_id = ...).
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance (no tenant filter). Reads tenant-scoped tool_performance across all tenants.
  - _Fix:_ Add tenant scoping if the gate should operate per-tenant (WHERE tenant_id = ...). If a global metric is required, ensure this code runs under a trusted global admin context and document the exception.
- **server/dormant-deprecation.ts:203** — SELECT DISTINCT tool_name FROM tool_performance executed without tenant_id constraint in getEverInvokedTools(). This returns tool names across all tenants.
  - _Fix:_ Scope queries to a tenant where appropriate (WHERE tenant_id = ...). If the intention is to work globally, require an explicit admin/trusted context and document/centralize the global-read exception.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (no tenant filter) in getActiveTools14d(). This reads tenant-scoped table across tenants.
  - _Fix:_ Add tenant filtering when the operation should be per-tenant (AND tenant_id = ...). If this is intentionally global, gate it to an admin-only path and document the global-read rationale.
- **server/episode-playbooks.ts:135** — Building an SQL array literal via string interpolation for id ANY({...}::int[]) using ids.join(',') — this inlines values into the SQL text and can be an injection/isolation risk if values are not strictly validated.
  - _Fix:_ Use parameterized bindings for arrays (e.g. pass an array parameter or use sql.join/sql.array helpers) rather than constructing an array literal via string interpolation. Ensure ids are validated as integers before binding. Keep the tenant_id constraint in the WHERE clause.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from agent_runs is executed without a tenant_id filter (SELECT id, tenant_id, status,... FROM agent_runs WHERE status = 'running' ...). agent_runs contains tenant_id — a global SELECT over a tenant-scoped table violates the requirement that tenant-scoped reads be explicitly constrained.
  - _Fix:_ If this is an audit intended to run across tenants, make that explicit in the code and caller privileges; otherwise, accept a tenantId parameter and add WHERE tenant_id = ${tenantId}. Alternatively, iterate tenants and run the same query per-tenant so every read is explicitly scoped by tenant_id.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from mind_tickets is executed without a tenant_id filter (SELECT id, tenant_id,... FROM mind_tickets WHERE status = 'in_progress' ...). mind_tickets appears tenant-scoped but is queried globally here.
  - _Fix:_ Same fix as for agent_runs: either add an explicit tenant_id WHERE clause (pass tenantId into the function) or iterate tenants and run the query per-tenant. If the absence of tenant filtering is intentional for an admin audit, document and gate the function to admin-only usage and log that it operates system-wide.
- **server/lib/system-probes.ts:163** — Database probe counts messages via db.select(count(*)).from(messagesTable) with no tenant_id WHERE clause, returning a system-wide message count from a tenant-scoped table.
  - _Fix:_ If the probe must be cross-tenant for admin telemetry, make that explicit and restrict the tool to admin contexts; otherwise, accept a tenantId and add WHERE tenant_id = ${tenantId} to the query. Prefer an explicit ALL_TENANTS sentinel or an audited admin-only path rather than leaving a silent cross-tenant read.
- **server/minds-engine.ts:262** — checkDependenciesMet builds an SQL ANY(...) array via string interpolation (`ANY(${`{${ticket.dependsOn.join(",")}}`}::int[])`). Although ticket.dependsOn often comes from the DB, it can originate from user-supplied data and is interpolated into SQL, risking injection.
  - _Fix:_ Avoid string-concatenating array literals. Use parameterized array binding or Drizzle helpers to supply arrays safely (e.g. client/driver array parameter or sql.array). Ensure dependsOn is validated/normalized to an array of integers before use.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw(${lockKey}) inside a query (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)) where the raw value is derived from caller-provided ledgerKey — use of sql.raw with request-derived data risks SQL-injection/semantics issues.
  - _Fix:_ Avoid sql.raw with any request-derived value. If a numeric lock key is required, validate/convert it to a safe parameter and pass it as a bound parameter (e.g. compute BigInt on server side and pass it via ${} so Drizzle parameterizes it). If sql.raw is unavoidable, strictly validate/sanitize the value to a numeric literal before embedding.
- **server/process-governor.ts:119** — Subqueries counting heartbeat_logs in task_failure_rate use task_id = ht.id but do not include an explicit tenant_id filter on heartbeat_logs — reading a tenant-scoped logs table without tenant constraint in subqueries.
  - _Fix:_ Add tenant scoping inside the subqueries (e.g. AND tenant_id = ${tenantId}), or rewrite the query to join on heartbeat_tasks with an explicit tenant_id to guarantee the logs counted belong to the same tenant.
- **server/process-governor.ts:153** — Subquery in desk_status SELECT uses (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true) without tenant filtering — counts tasks across tenants could be included.
  - _Fix:_ Constrain the subquery by tenant (AND tenant_id = ${tenantId}) or join heartbeat_tasks with ad on tenant_id to ensure the pending_tasks count is tenant-scoped.
- **server/process-governor.ts:239** — Cascading_failures query SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... has no tenant_id filter — reports global failure counts across tenants.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) to the query to restrict results to the intended tenant.
- **server/routes.ts:2315** — Unscoped SELECT from tenant-scoped `delivery_logs` by order/session id (public order lookup) — no tenant_id constraint. The route returns tenant-related fields and uses tenantId later for signing appPlayLink.
  - _Fix:_ If this is intended as a capability-based public endpoint (unguessable session id), explicitly document and harden it (e.g. limit returned tenant-identifying data). Consider verifying that the retrieved log is tied to a capability (unguessable token) and avoid exposing tenant identifiers unless required. If tenant-scoped access is expected, add WHERE tenant_id = <caller tenant> or require authentication.
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` without scoping the read to the caller's tenant (query aggregates per-tenant across all tenants). The SQL returns tenant-level costs across all tenants.
  - _Fix:_ If the endpoint must return cross-tenant aggregates, ensure it is strictly operator-only (already gated by METRICS_TOKEN) and document/justify the cross-tenant access. If the query should be tenant-scoped, add a WHERE tenant_id = ${tenantId} or provide a parameter allowing an explicit tenant selection that is validated and authorized.
- **server/routes/metrics.ts:79** — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) without tenant scoping — this reads rows across all tenants.
  - _Fix:_ Same mitigation as above: either require/verify operator-level access and document the global read, or add a tenant_id WHERE clause to scope results to a single tenant when appropriate.
- **server/routes/metrics.ts:95** — SELECT SUM(tokens_in/tokens_out) FROM `agent_cost_ledger` without tenant scoping — global aggregation over a tenant-scoped table is performed with no tenant predicate.
  - _Fix:_ Either restrict to a tenant (WHERE tenant_id = ${tenantId}) when the caller should see only their data, or ensure the endpoint is properly limited to an operator role and add an explicit comment/authorization check explaining why a global read is required.
- **server/skillify.ts:221** — Query reads personas by id without constraining to the caller's tenant. The code uses conv[0].personaId (derived from a conversation) and queries the personas table with WHERE personas.id = conv[0].personaId but does NOT verify the persona row belongs to the same tenant. This can leak cross-tenant persona metadata.
  - _Fix:_ Constrain the personas lookup to the conversation's tenant. e.g. include AND eq(personas.tenantId, conv[0].tenantId) in the .where(...) or use the tenant-scope helpers (requireTenantScope/tenantScope) or a storage helper that accepts an explicit tenant/all-tenants sentinel. Fail-closed if tenantId is missing.
- **server/storage.ts:827** — getMemoriesWithoutEmbeddings reads from tenant-scoped table 'memory_entries' with no tenant filter (returns rows across tenants).
  - _Fix:_ Require a tenant parameter (or explicit ALL_TENANTS sentinel for administrative jobs) and apply requiredTenantScope(memory_entries.tenant_id, tenantId) or equivalent to the WHERE clause. If this function is used by a background worker that legitimately needs cross-tenant access, make that explicit and guard it with admin-only credentials.
- **server/storage.ts:833** — getKnowledgeWithoutEmbeddings reads from tenant-scoped table 'agent_knowledge' with no tenant filter (potential cross-tenant exposure).
  - _Fix:_ Add tenant scoping (require tenantId or an explicit ALL_TENANTS sentinel) and use requiredTenantScope/tenantScope to constrain the query to the intended tenant(s). Guard any platform-wide read behind an admin-only path.
- **server/storage.ts:650** — upsertDailyNote updates daily_notes using WHERE id = existing.id without enforcing tenant_id — allows modifying another tenant's note if ids are misused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(daily_notes.id, existing.id), eq(daily_notes.tenantId, data.tenantId)))) or look up & assert ownership before updating. Require tenantId be present when mutating tenant-scoped rows.
- **server/storage.ts:942** — getHeartbeatTask selects heartbeat_tasks by id without tenant scoping — heartbeat_tasks are tenant-scoped elsewhere and this function can return another tenant's task by id.
  - _Fix:_ Require tenantId or otherwise verify ownership before returning a task row. Add an overloaded API or explicit admin-only call for unscoped access; otherwise include eq(heartbeat_tasks.tenantId, tenantId) in the WHERE clause.
- **server/surprise-scorer.ts:107** — scoreProposalSurprise default parameter tenantId = 1 (admin) — callers that omit tenantId will operate under admin tenant by default, enabling accidental cross-tenant access.
  - _Fix:_ Do not default tenantId to an admin value. Require tenantId explicitly (no default) or accept an explicit ALL_TENANTS sentinel and gate admin access. Validate caller ownership before using the tenantId to scope queries.
- **server/surprise-scorer.ts:315** — getSurpriseStats defaults tenantId = 1 (admin) so callers that omit tenantId will see admin/other-tenant data.
  - _Fix:_ Require tenantId be passed explicitly (no default to 1). If an admin endpoint is needed, create a separate admin-only function that requires explicit authorization and an explicit ALL_TENANTS sentinel.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE memory_entries SET ... WHERE id = ${id} — UPDATE against tenant-scoped table `memory_entries` uses only id in WHERE clause and lacks an explicit tenant_id = ${tid} constraint.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tid}) to enforce application-level tenant isolation even when id values came from a prior tenant-scoped SELECT.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) — UPDATE against tenant-scoped table `memory_entries` uses ids only and lacks an explicit tenant_id = ${tid} constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ANY(${ids}) AND tenant_id = ${tid}), or include tenant_id in the id selection and use a parameterized combined predicate to ensure the update cannot affect rows outside the caller's tenant.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE memory_entries SET ... WHERE id = ${id} — UPDATE against tenant-scoped table `memory_entries` uses only id in WHERE clause and lacks an explicit tenant_id = ${tid} constraint.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${id} AND tenant_id = ${tid}) to enforce the required application-level tenant isolation, even when id originated from a prior tenant-scoped query.
- **server/wiring-invariants.ts:366** — checkSubsystemFreshness executes SELECT COUNT / MAX(timestamp) FROM <table> for subsystem tables (e.g. tool_performance, knowledge_nudges, graph_memory) with no tenant_id constraint — these are tenant-scoped tables and the query reads across all tenants.
  - _Fix:_ If the intent is an admin-wide health check, make that explicit and run under an admin/owner context, otherwise include tenant_id filters or aggregate per-tenant (e.g. GROUP BY tenant_id) to avoid unscoped reads that violate tenant isolation. Ensure any code that reads tenant-scoped data documents why cross-tenant visibility is safe and limited to admin-only flows.
- **server/wiring-invariants.ts:535** — checkMemoryGrowth performs SELECT COUNT(*) and pg_total_relation_size(...) FROM memory tables (including tenant-scoped tables like messages, graph_memory, memory_entries) without filtering by tenant_id, reading tenant data across all tenants.
  - _Fix:_ Either restrict the measurement to a specific tenant (pass tenantId) or compute per-tenant aggregates (GROUP BY tenant_id) for tenant-scoped tables. If a global admin metric is intended, ensure this path is explicitly guarded as an admin-only diagnostic and documented as such.
- **server/wiring-invariants.ts:244** — checkDormantTools queries tool_performance (SELECT count(*) FROM tool_performance ...) without any tenant_id constraint — tool_performance appears to be tenant-scoped, so this reads across tenants without scoping.
  - _Fix:_ Scope the query by tenant_id or aggregate per-tenant to preserve tenant isolation, or require admin-only execution and document the reason for cross-tenant inspection. Prefer explicit tenant filter parameters to avoid accidental cross-tenant data exposure.
- **server/wiring-invariants.ts:293** — Used-tool lookup (SELECT DISTINCT tool_name FROM tool_performance ...) runs without tenant_id restriction, inspecting recent tool activity across all tenants.
  - _Fix:_ Add tenant scoping or compute per-tenant sets. If cross-tenant aggregation is desired only for admin diagnostics, gate the call behind an explicit admin flag and log that it intentionally inspects all tenants.
- **server/wiring-invariants.ts:417** — checkToolDrift queries tool_performance (SELECT tool_name, SUM(success_count), SUM(fail_count) ... WHERE last_failure_at > ...) and groups/filters without tenant_id — this aggregates tenant-scoped metrics across all tenants.
  - _Fix:_ Include tenant_id in WHERE/GROUP BY or run the drift computation per-tenant. If global aggregation is intentional for platform-wide observability, make the admin-only nature explicit and ensure the code path cannot be triggered by non-admin contexts.
- **server/wiring-invariants.ts:561** — A probe SELECT COUNT(*) FROM graph_memory runs without tenant_id and will read counts across tenants; graph_memory is a tenant-scoped store (the check treats empty graph_memory as a cross-tenant problem).
  - _Fix:_ Restrict this probe to a tenant when intending to check a tenant-scoped store, or iterate tenants and report per-tenant counts. If a system-wide check is intended, ensure only an admin context can run it and document the reason for cross-tenant access.

### LOW (2)

- **server/routes.ts:1532** — Multiple unscoped SELECT COUNT(*) queries against tenant-scoped tables (conversations/messages/heartbeat_tasks/memory_entries/heartbeat_logs) in public stats endpoint — no tenant_id filter.
  - _Fix:_ If these totals are intended to be platform-wide this should be explicit and access-gated (e.g. platform-admin). Otherwise, scope counts to the caller's tenant (WHERE tenant_id = <caller tenant>) or aggregate per-tenant and return only the permitted view.
- **server/routes.ts:1561** — Multiple unscoped SELECT queries in /api/public/architecture against tenant-scoped tables (e.g. personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs) without tenant_id constraints.
  - _Fix:_ Either restrict this endpoint to platform admin callers or scope the queries by tenant_id when invoked in a tenant context. Avoid returning cross-tenant aggregated data unless intentionally global and access-controlled.

### Suppressed — confirmed false positives (229)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — Bulk UPDATE on `ai_insights` without tenant scoping: `UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...` will modify rows across all tenants.
  - _Why false positive:_ Global maintenance backfill intentionally sweeps all tenants; per-row tenant_id is carried for subsequent handling and this only mutates housekeeping/status fields.
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations is executed without scoping to tenant_id (DELETE FROM conversations WHERE id = ${conv.id}). This deletes a tenant-scoped row without an explicit tenant predicate.
  - _Why false positive:_ Purge loop is a platform-wide housekeeping sweep over expired soft-deleted conversations; each row carries its own tenant_id and the routine does not return mixed tenant data.
- **server/embeddings.ts:790** _(system-sweep)_ — UPDATE memory_entries in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant_id — allows modifying other tenants' rows.
  - _Why false positive:_ Embedding backfill is a global maintenance sweep that only stamps missing embedding_vec on selected rows and does not expose or mix tenant data.
- **server/embeddings.ts:807** _(system-sweep)_ — UPDATE agent_knowledge in backfillEmbeddingVecs uses WHERE id = ${row.id} without tenant_id — allows modifying other tenants' rows.
  - _Why false positive:_ Embedding backfill is a global maintenance sweep that only stamps missing embedding_vec on selected rows and does not expose or mix tenant data.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals uses WHERE id = ANY(${ids}::int[]) with no tenant_id filter — may modify proposals for other tenants.
  - _Why false positive:_ Stale-proposal archival is a platform-wide TTL housekeeping update that changes only status fields and does not return tenant content to tenants.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs an UPDATE/SELECT on agent_jobs without scoping by tenant_id (no WHERE tenant_id = <...>). This atomically claims jobs across all tenants, violating the requirement that every access to tenant-scoped tables include an explicit tenant constraint.
  - _Why false positive:_ Global worker queue claim intentionally operates across tenants; each claimed row retains its own tenant_id for per-job processing rather than mixing tenant data.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases updates agent_jobs (marking failed_terminal) without any tenant_id constraint — it mutates rows for all tenants indiscriminately.
  - _Why false positive:_ Lease reclamation is platform-wide queue housekeeping that only mutates job state fields across rows and does not expose mixed tenant content.
- **server/job-queue.ts:148** _(system-sweep)_ — reclaimExpiredLeases' second UPDATE (requeueing expired rows back to pending) operates on agent_jobs without a tenant_id WHERE clause, re-queuing jobs across tenants.
  - _Why false positive:_ Requeueing expired leases is platform-wide queue housekeeping that only mutates status/timing fields and does not expose mixed tenant data.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs (set status='succeeded') using only id/attempts/status fencing, with no tenant_id constraint — a caller could complete a job belonging to another tenant if they know the id.
  - _Why false positive:_ completeJob is a server worker state transition by globally unique job id plus attempt fencing, not a request-scoped tenant operation.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob SELECTs max_attempts FROM agent_jobs WHERE id = ${id} without scoping by tenant_id — reads tenant-scoped state across tenants.
  - _Why false positive:_ failJob is an internal worker path reading its own claimed job by server-generated primary key/id and attempts, not untrusted caller input.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob's UPDATE to mark failed_terminal writes to agent_jobs without any tenant_id constraint, mutating jobs across tenants.
  - _Why false positive:_ terminal failure update is part of internal worker lease-fenced state transitions on a server-generated job id.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — failJob's retry-path UPDATE (re-scheduling with next_run_at) operates on agent_jobs without scoping by tenant_id, affecting jobs belonging to any tenant.
  - _Why false positive:_ retry reschedule update is likewise an internal worker transition keyed by server-generated job id with attempt fencing.
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — Cross-tenant UPDATE: sweepStaleAttempts executes UPDATE action_attempts ... WHERE id IN (SELECT id FROM action_attempts WHERE state IN (...) ...) with no tenant_id constraint in the inner SELECT, allowing rows from any tenant to be transitioned.
  - _Why false positive:_ bounded reconciler sweep intentionally operates platform-wide and only moves stale rows to housekeeping state 'unknown' without returning tenant data or mixing content.
- **server/memory-graph.ts:252** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) — INSERT does not set tenant_id. If memory_links is a tenant-scoped table this allows creating rows with no tenant attribution (or implicit admin/system defaults), violating the invariant that INSERTs must pass tenantId explicitly.
  - _Why false positive:_ memory_links has no tenant_id column; isolation is transitive through tenant-scoped memory_entries referenced by source/target memory ids.
- **server/minerva-planner.ts:472** _(upstream-verified)_ — Rollback UPDATE after createPlan failure runs `UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'` with NO tenant_id constraint. This can modify a plan belonging to a different tenant if called in an unscoped context.
  - _Why false positive:_ rollback uses args.planId only after the prior CAS RETURNING row established the exact plan row and its tenant_id in the same call path.
- **server/plan-executor.ts:117** _(upstream-verified)_ — UPDATE plans executed without scoping by tenant_id (appendExecutionLog updates plans WHERE id = ${planId} with no tenant filter).
  - _Why false positive:_ appendExecutionLog is called from executePlan after claim RETURNING fetched the owned plan row; this helper is in that internal call path, not a direct tenant boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS UPDATE on plans to claim execution (UPDATE plans ... WHERE id = ${planId} AND status = 'approved') has no tenant_id constraint — plan mutation by id only is used without tenant scoping.
  - _Why false positive:_ executePlan is an internal executor claiming a plan by server-generated primary key, then carrying row.tenant_id forward; not shown as request-derived input.
- **server/plan-executor.ts:527** _(upstream-verified)_ — UPDATE plans SET status='failed' WHERE id = ${planId} (no tenant_id filter) — unscoped write to tenant-scoped table.
  - _Why false positive:_ the failed-status update occurs immediately after the claim RETURNING row for this exact plan in the same internal execution path.
- **server/plan-executor.ts:728** _(internal-worker-pk)_ — Final UPDATE of plans (set status/combine execution_log) WHERE id = ${planId} lacks tenant_id constraint — writes to plans table are not tenant-scoped.
  - _Why false positive:_ Server-side executor updates one plan by server-provided unique planId and uses loaded row.tenant_id for events; not from a request boundary.
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-path UPDATE plans SET status='failed' WHERE id = ${planId} AND status = 'executing' has no tenant_id filter — error recovery mutates plans without scoping by tenant.
  - _Why false positive:_ Fatal-error recovery updates the same server-invoked plan row by unique planId inside executor flow, not from untrusted tenant input.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans UPDATE resets all executing plans to 'approved' WHERE status = 'executing' AND updated_at < ... — no tenant_id scoping; mass cross-tenant mutation possible.
  - _Why false positive:_ Boot-time recovery intentionally sweeps stale executing plans across tenants and only resets housekeeping status fields, returning ids for internal restart.
- **server/plan-executor.ts:787** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' ... LIMIT 50 is not constrained by tenant_id — reading tenant-scoped table without tenant filter.
  - _Why false positive:_ Boot-time recovery intentionally selects approved plans across tenants only to enqueue internal execution by each row id; no tenant data returned to tenant-facing surface.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped table by id only with no tenant_id filter or ownership verification.
  - _Why false positive:_ Verifier is a server-side worker function reading one proposal by unique proposalId, not shown as sourced from req/body/tool args.
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} used to clear rebased_at/mark race is executed without tenant scoping — unscoped write to tenant-scoped table.
  - _Why false positive:_ Internal verifier updates its current proposal row by server-invoked unique proposalId during drift handling.
- **server/proposal-verifier.ts:245** _(internal-worker-pk)_ — persist() updates code_proposals: UPDATE code_proposals SET verification_status/verification_details ... WHERE id = ${proposalId} with no tenant_id filter — verification writes are not tenant-scoped.
  - _Why false positive:_ persist() is helper for internal verifier workflow, updating one proposal by server-side unique proposalId rather than tenant-controlled request input.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — In attemptRebaseOnDrift branch, UPDATE code_proposals SET status='rejected' ... WHERE id = ${proposalId} is executed with no tenant constraint — LLM-driven rebase actions mutate proposal rows without tenant scoping.
  - _Why false positive:_ Rebase branch mutates the same proposal row inside internal verification worker using unique proposalId, not a cross-tenant request boundary.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — Persistence of rebased diff: UPDATE code_proposals SET code_diff = ${newCodeDiff} ... WHERE id = ${proposalId} lacks tenant scoping — rewrites proposal code_diff across tenants are possible.
  - _Why false positive:_ Rebased diff persistence writes back to the single internally processed proposal by unique proposalId.
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE research_sessions in cleanupZombieSessions has no tenant scope — it will mark running sessions completed across all tenants.
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping sweep marking stale running sessions completed; it mutates status/end fields only and returns ids for internal logging.
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() is unscoped — it deletes agent_knowledge rows for all tenants.
  - _Why false positive:_ Expiry cleanup deletes expired autoresearch knowledge across tenants as maintenance; no tenant content is mixed into outputs.
- **server/routes/admin-zombie-detector.ts:54** _(system-sweep)_ — CTE `used` selects from conversations with no tenant_id constraint. conversations is a tenant-scoped table; this will surface persona usage across all tenants.
  - _Why false positive:_ Admin/reporting query intentionally aggregates platform-wide usage across tenants and returns only aggregate counts/last-used metadata, not tenant row data or mixed tenant content.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on `inbox_messages` without tenant_id constraint. After selecting the message with a tenant-scoped WHERE, the code executes: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} — the UPDATE lacks AND tenant_id = ${tenantId}. This allows modifying a row by id without re-checking tenant ownership.
  - _Why false positive:_ The message row is selected immediately above with id and tenant_id, and the UPDATE uses that verified row's server-read primary key, not untrusted input directly.
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets status/result by id without constraining by tenant_id. This writes to a tenant-scoped table but lacks WHERE tenant_id = <tenant> — a caller could affect rows in other tenants if ids collide or are forged.
  - _Why false positive:_ Updates the same sculptor_sessions row just inserted server-side in this function, by server-generated/returned primary key, not from an untrusted request boundary.
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE sculptor_sessions sets conversation_id by id without constraining by tenant_id. This write into a tenant-scoped table is not scoped to the caller's tenant.
  - _Why false positive:_ Updates the newly created session by its server-returned primary key within the same internal flow; id is not attacker-controlled across tenants.
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE sculptor_sessions in monitorSession updates a session row (status/result/etc.) by id only, with no tenant_id constraint. Monitor/async jobs may therefore cross-tenant modify sessions if ids are misused.
  - _Why false positive:_ Async monitor worker updates a specific session by server-originated sessionId/runId pair passed from creation flow, not by untrusted caller input.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} lacks a tenant_id constraint. This can modify a program belonging to another tenant.
  - _Why false positive:_ Boot/seed maintenance loop intentionally scans all active programs and normalizes only the model field; housekeeping mutation with no tenant-facing mixed output.
- **server/seed.ts:2455** _(system-sweep)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... runs without a tenant_id constraint. projects is tenant-scoped so this may modify another tenant's project row.
  - _Why false positive:_ Owner-deployment seed/migration logic is an intentional maintenance path scoped to fixed tenant 1 rows, not a tenant-request path.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (db.select().from(customTools).where(eq(customTools.name, parsed.name))). This allows cross-tenant discovery/interaction if names overlap or are guessed.
  - _Why false positive:_ name is first transformed with tenantToolName(parsed.name, tenantId), making lookup tenant-specific by construction before the SELECT
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT from tenant-scoped table `custom_tools` by id without tenant_id constraint (db.select().from(customTools).where(eq(customTools.id, inserted.id))). Reading a row by id must still be tenant-scoped to avoid cross-tenant access to rows with the same id value.
  - _Why false positive:_ row is re-read by the just-inserted server-generated primary key inserted.id within the same internal flow, not from an untrusted request boundary
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `custom_tools` targeting a row by id without tenant_id constraint (.where(eq(customTools.id, tool.id))). This can update another tenant's row if id collisions occur or if an id is attacker-supplied.
  - _Why false positive:_ UPDATE uses tool.id from the just-created/internal validation workflow, not attacker-supplied cross-tenant input
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table `custom_tools` using only id in WHERE (db.delete(customTools).where(eq(customTools.id, tool.id))). Missing tenant_id constraint allows accidental or malicious deletion of another tenant's tool row.
  - _Why false positive:_ DELETE targets tool.id carried through the internal try-and-revert path for the newly created row, not untrusted caller input
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` to increment usageCount uses only id in WHERE (.where(eq(customTools.id, tool.id))). No tenant_id constraint present, allowing cross-tenant modification of usage counters or other side-effects.
  - _Why false positive:_ tool row was selected immediately above with WHERE name AND tenant_id = tenantId; subsequent usageCount update is on that verified row id
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` executed with no tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys WHERE ...). This reads tenant-scoped data across tenants.
  - _Why false positive:_ schema note says provider_keys has no tenant_id, so this is not a tenant-scoped table access
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(...) from tenant-scoped table `provider_keys` executed with no tenant_id constraint (SELECT COUNT(DISTINCT provider) ... FROM provider_keys WHERE ...). This aggregates across all tenants without tenant scoping.
  - _Why false positive:_ provider_keys is schema-global with no tenant_id column; aggregate is over a global table
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT across `experiments` omits tenant filter: `SELECT id, tenant_id, hypothesis, metric, persona_id, metadata, created_at FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100` reads rows for all tenants.
  - _Why false positive:_ heartbeat maintenance loop intentionally scans running experiments across tenants and later updates each row using its own row-carried tenant_id
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on `agent_evals` omits tenant scoping: `UPDATE agent_evals SET ... WHERE id = ${evalId}` updates eval rows without ensuring they belong to the caller's tenant.
  - _Why false positive:_ evalId is the server-generated PK returned from the immediately preceding INSERT in the same internal flow, not taken from an untrusted request boundary.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT in `retryPendingMinervaRouting` reads `ai_insights` without tenant scoping: `SELECT id, tenant_id, category, title, summary, details FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50`.
  - _Why false positive:_ explicit durability sweep intentionally scans all tenants, carries each row's tenant_id forward, validates it, and processes rows per-tenant without mixing output.
- **server/agentic-engines.ts:344** _(global-table)_ — SELECT against `heartbeat_logs` in `gatherOperationalData` omits tenant filter: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'` reads logs across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id column, indicating a platform-global log table; query only returns aggregate counts.
- **server/agentic-engines.ts:519** _(global-table)_ — `runOptimizationEngine` queries `heartbeat_logs` without tenant restriction: `SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY task_name, status` — reads aggregated data across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is treated as a platform-global operational log; this query uses only grouped aggregates.
- **server/agentic-features.ts:797** _(upstream-verified)_ — UPDATE on `outreach_enrollments` after classification omits tenant constraint: `UPDATE outreach_enrollments SET ... WHERE id = ${row.id}` may update the wrong tenant's row.
  - _Why false positive:_ row.id comes from an earlier SELECT on outreach_enrollments scoped by params.tenantId and contact_email in the same call path.
- **server/agentic/approvals.ts:134** _(system-sweep)_ — Bulk expiry update in `expireStaleApprovals` omits tenant scoping: `UPDATE agent_approvals SET status = 'expired', decidedAt = ... WHERE status = 'pending' AND expiresAt < now()` will mark pending approvals expired across all tenants.
  - _Why false positive:_ stale-approval expiry is a platform-wide housekeeping sweep; it returns per-row tenant_id and subsequent run updates are scoped per row, with no tenant-facing mixed data output.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Query SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes a cross-tenant scan without constraining tenant_id. This reads tenant-scoped data for all tenants in one query and thus bypasses the application-level per-tenant filter invariant.
  - _Why false positive:_ heartbeat-callable enforcement intentionally scans all tenants, then handles each row with its own tenant_id and emits per-tenant events without mixing tenant content.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query pulls agent_runs JOIN agent_approvals without constraining to a tenant (no WHERE tenant_id = <tenant>), returning cross-tenant rows.
  - _Why false positive:_ Global maintenance sweep processes mixed-tenant rows but carries each row's tenant_id for per-row continuation handling; not a tenant-facing combined output.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() updates agent_runs without scoping by tenant (WHERE eq(agentRuns.id, runId) only). This allows cross-tenant updates if runId is mis-bound to another tenant.
  - _Why false positive:_ Helper updates agent_runs by server-generated PK runId in internal agent worker code, not shown as coming from request/tool input.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs without a tenant constraint (.where(eq(agentRuns.id, runId))).
  - _Why false positive:_ Internal state-transition helper operates on globally unique runId in server worker context, not an untrusted tenant boundary.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs without a tenant constraint (.where(eq(agentRuns.id, runId))).
  - _Why false positive:_ Internal replace-state helper uses server-controlled runId primary key inside agent runtime code.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs (status/result/completedAt) without scoping the UPDATE to the tenant (WHERE eq(agentRuns.id, runId) only).
  - _Why false positive:_ Completion helper is internal server-side lifecycle code updating by globally unique runId, with no untrusted caller id shown.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs status/error/completedAt without including tenant scoping in the WHERE clause.
  - _Why false positive:_ Failure-path lifecycle helper updates by server-generated runId in internal agent code, not request-derived.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() updates agent_runs status and state without constraining by tenant (WHERE eq(agentRuns.id, runId) only).
  - _Why false positive:_ Pause helper is internal server-side run management by global PK rather than tenant-exposed selector.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — Global UPDATE/claim on tenant-scoped table without tenant filter: the single-statement claim sets status = 'firing' for rows selected by an inner SELECT that does NOT restrict by tenant_id. This can mutate/claim wakes across tenants.
  - _Why false positive:_ Scheduler intentionally claims due wakes across tenants and then handles each row with its own tenant_id; housekeeping/dispatch loop, not tenant-mixed response data.
- **server/auto-asset-capture.ts:145** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The code comments rely on transitive isolation via project ownership, but per invariant every INSERT on tenant-scoped data must include an explicit tenant_id column/value.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped transitively through project_id parent.
- **server/auto-asset-capture.ts:164** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. This writes a project-scoped note without an explicit tenant_id column/value, relying on the project_id alone for transitive isolation.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively through project_id parent.
- **server/auto-memorize.ts:163** _(system-sweep)_ — runAutoMemorize pulls messages across all tenants in a single query (SELECT ... FROM messages m ... WHERE m.created_at > ... AND m.role IN ...). This SELECT lacks a tenant_id = <tenant> constraint and will mix data from multiple tenants.
  - _Why false positive:_ Boot/cron-style global sweep partitions rows by row-carried tenant_id before any per-tenant synthesis; comments explicitly forbid mixed-tenant prompts.
- **server/auto-transcript.ts:165** _(system-sweep)_ — Backfill code executes an UPDATE on conversations without constraining by tenant_id (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL). That can modify another tenant's conversation if IDs are guessed/poisoned.
  - _Why false positive:_ Maintenance backfill loops over globally selected tenant-qualified rows and performs housekeeping link/project_id reconciliation plus transcript generation per row using c.tenant_id; not a tenant-facing mixed output.
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. In a multi-tenant DB where tenant isolation is enforced only by application-level filters, omitting tenant_id on tenant-scoped child rows lets an attacker/bug create rows not tied to the caller's tenant (or orphan rows), breaking the invariant that every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Why false positive:_ invoice_items has no tenant_id column and is a child of tenant-scoped invoices via invoice_id created in the same transaction.
- **server/channel-routing.ts:31** _(global-table)_ — Query JOINs into personas (JOIN personas p ON p.id = cr.persona_id) with no tenant_id constraint. If personas is a tenant-scoped table this SELECT can expose persona rows from other tenants.
  - _Why false positive:_ Schema note says personas has no tenant_id, and channel_routes is a singleton channel->persona registry with UNIQUE(channel), indicating platform-global routing rather than tenant-scoped data.
- **server/channel-routing.ts:54** _(global-table)_ — INSERT into channel_routes sets persona_id from the caller-supplied personaId without validating that the referenced persona belongs to the caller's tenant and without recording any tenant_id on the channel_routes row.
  - _Why false positive:_ Writes target platform-global channel_routes and reference personas, which schema marks as non-tenant-scoped; no tenant isolation invariant applies here.
- **server/chat-engine.ts:651** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` is executed with WHERE conversation_id = ${c.id} but no tenant_id constraint. Even though the conversation list was tenant-filtered earlier, the query itself does not enforce tenant_id, increasing risk of cross-tenant reads if assumptions change.
  - _Why false positive:_ This message read is inside a loop over prior tenant-filtered conversation rows c; scoping is inherited from the verified conversation set in the same call path.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` without tenant constraint — the scanner does `SELECT id, tenant_id AS "tenantId", ... FROM commitments WHERE status = 'active' ...` and returns rows across all tenants.
  - _Why false positive:_ Explicit platform-wide heartbeat scanner scans all tenants, carries each row's tenantId separately, and emits only redacted admin digest data without mixing tenant content.
- **server/delivery-pipeline.ts:1071** _(upstream-verified)_ — SELECT from delivery_logs by stripePaymentId without tenant_id constraint (db.select().from(deliveryLogs).where(eq(deliveryLogs.stripePaymentId, paymentId))). This can return another tenant's delivery row.
  - _Why false positive:_ retryDelivery first loads delivery_logs with tenant-scoped where when tenantId is provided; the subsequent update is in the same verified call path.
- **server/dfy-intake.ts:50** _(signed-url)_ — SELECT from dfy_intake_forms by token only (no tenant_id constraint). Token lookup returns tenant-scoped form data (PII) without tenant scoping.
  - _Why false positive:_ Access is intentionally by high-entropy token; token is the authorization mechanism for public intake form retrieval.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — UPDATE dfy_intake_forms uses WHERE id = form.id only (.where(eq(dfyIntakeForms.id, form.id))). Missing tenant_id constraint allows updating another tenant's form row.
  - _Why false positive:_ Update uses form.id obtained immediately from prior token-validated lookup of the same row; no separate untrusted tenant boundary here.
- **server/doc-collections.ts:165** _(upstream-verified)_ — DELETE FROM doc_chunks executed without tenant_id in WHERE clause (DELETE FROM doc_chunks WHERE doc_path = ... AND collection_id = ...). This can delete chunks across tenants if collection_id/doc_path are not globally protected.
  - _Why false positive:_ A few lines above, collection ownership is verified with id + tenant_id; delete is limited to that verified collection_id/doc_path pair.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} executed without tenant_id constraint. This updates a chunk row by id across tenants.
  - _Why false positive:_ Rows being updated were selected just above with collection_id + tenant_id and row.id comes from that trusted result set.
- **server/email.ts:471** _(system-sweep)_ — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 executed without tenant_id constraint while polling shared inbox. inbox_messages is tenant-scoped; this can return another tenant's stored message and cause cross-tenant behavior.
  - _Why false positive:_ Shared-inbox poller is a platform-wide maintenance ingest loop deduping by provider message_id before storage, not returning mixed tenant data to a tenant-facing surface.
- **server/email.ts:515** _(system-sweep)_ — SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') AND (body_html IS NULL OR body_html = '') ORDER BY id executed without tenant_id constraint in backfillEmptyBodies(). This scans inbox_messages across tenants.
  - _Why false positive:_ Backfill scans all tenants only to fill missing body fields per row in a housekeeping job; it does not expose mixed tenant data in one response.
- **server/embeddings.ts:752** _(system-sweep)_ — SELECT FROM agent_knowledge (backfillMissingKnowledgeEmbeddings) is not scoped by tenant_id — reads rows across all tenants.
  - _Why false positive:_ Embedding backfill is a platform-wide maintenance job updating per-row embedding fields only; no tenant-facing mixed output.
- **server/embeddings.ts:780** _(system-sweep)_ — SELECT FROM memory_entries (backfillEmbeddingVecs) is not scoped by tenant_id — reads rows across all tenants.
  - _Why false positive:_ Vector backfill over memory_entries is housekeeping that mutates only embedding_vec per selected row and returns aggregate counts.
- **server/embeddings.ts:796** _(system-sweep)_ — SELECT FROM agent_knowledge (backfillEmbeddingVecs) is not scoped by tenant_id — reads rows across all tenants.
  - _Why false positive:_ Vector backfill over agent_knowledge is a maintenance sweep updating rows individually without exposing cross-tenant content.
- **server/embeddings.ts:244** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal (vec) derived from request input/LLM output inside the query (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). This constructs SQL via string interpolation of a request-derived value.
  - _Why false positive:_ This query already constrains memory_entries by tenant_id in the WHERE clause; finding is not a tenant-isolation issue.
- **server/embeddings.ts:252** _(upstream-verified)_ — ORDER BY uses sql.raw() with an interpolated vector literal (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) where vec is derived from the caller's query/LLM output.
  - _Why false positive:_ ORDER BY occurs within a query already scoped by tenant_id; no cross-tenant isolation gap at this line.
- **server/embeddings.ts:566** _(upstream-verified)_ — Use of sql.raw() with an interpolated vector literal in vectorSearchKnowledge (1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)})). The vec value is derived from the request/LLM embedding and is being injected into raw SQL.
  - _Why false positive:_ Query is explicitly tenant-scoped with WHERE tenant_id = ${tenantId}; finding is SQL injection, not cross-tenant isolation.
- **server/embeddings.ts:572** _(upstream-verified)_ — ORDER BY uses sql.raw() with interpolated vector literal (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) where the vector originates from request/LLM output.
  - _Why false positive:_ ORDER BY uses raw vector literal but same query is constrained by tenant_id = ${tenantId}; no tenant-isolation gap shown.
- **server/episode-playbooks.ts:68** _(upstream-verified)_ — String-interpolated vector literal (${embeddingLiteral}::vector) is injected into SQL for nearest-neighbor ordering/score. The embedding is derived at runtime (LLM/provider) and is interpolated into the query string context.
  - _Why false positive:_ Nearest-neighbor SELECT includes WHERE tenant_id = ${tenantId} AND run_type = ${runType}; issue is raw vector syntax, not cross-tenant access.
- **server/episode-playbooks.ts:120** _(upstream-verified)_ — String-interpolated embedding literal (embeddingLiteral) used in SELECT/ORDER BY as ${embeddingLiteral}::vector when retrieving playbooks — embedding is runtime-derived and is being injected into SQL.
  - _Why false positive:_ Retrieval query is directly scoped by WHERE tenant_id = ${tenantId}; no missing tenant filter on the flagged access.
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' is executed without any tenant_id filter (reads running/pending runs across all tenants).
  - _Why false positive:_ Boot-time rehydration intentionally scans all running/pending agent_runs and only rebuilds in-memory task state; cross-tenant read is for housekeeping, not tenant-facing mixed output.
- **server/firecrawl.ts:214** _(upstream-verified)_ — UPDATE scraped_pages .where(eq(scrapedPages.id, existing[0].id)) updates a tenant-scoped table row by id but lacks an explicit tenant_id condition.
  - _Why false positive:_ existing[0].id comes from an immediately preceding SELECT scoped by url and tenantId, so the updated row was already tenant-verified in the same path.
- **server/firecrawl.ts:344** _(upstream-verified)_ — UPDATE scraped_pages .where(eq(scrapedPages.id, existing[0].id)) inside crawl loop updates a scraped_pages row by id without an explicit tenant_id filter.
  - _Why false positive:_ Update targets existing[0].id fetched just above with WHERE url = pageUrl AND tenantId = tenantId; ownership is enforced earlier in same call path.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' by project id uses WHERE eq(projects.id, projectId) but does NOT include tenant_id in the WHERE clause (querying a tenant-scoped table without tenant scoping).
  - _Why false positive:_ Code immediately validates project.tenantId === tenantId and throws on mismatch before any folder return/write; ownership enforcement is in the same path.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects SET driveFolderId/... uses WHERE eq(projects.id, projectId) without a tenant_id constraint, allowing modification of a project row by id without explicit tenant scoping.
  - _Why false positive:_ UPDATE occurs only after the same function fetched the project and checked project.tenantId matches caller tenantId; prior ownership check fail-closes.
- **server/google-drive.ts:826** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' in uploadAndShare is performed with WHERE eq(projects.id, params.projectId) and no tenant_id filter. Although code later compares proj.tenantId to params.tenantId, the initial SQL is not scoped by tenant.
  - _Why false positive:_ Caller-side check immediately compares proj.tenantId to params.tenantId and returns error on mismatch before proceeding; same-path ownership verification exists.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from heartbeat_tasks omits tenant scoping — enumerates tasks across tenants without filtering tenant_id.
  - _Why false positive:_ Health audit is a platform-wide maintenance scan over heartbeat_tasks and optional disablement of stale rows; housekeeping across tenants, not tenant-facing data mixing.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) has no tenant_id filter — can disable tasks for other tenants.
  - _Why false positive:_ Global health-audit housekeeping disables stale heartbeat tasks found by a platform-wide sweep; mutates status only and does not expose mixed tenant data.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT INTO presenter_slide_images does not include tenant_id — writing to a tenant-scoped table without setting tenant_id explicitly or validating ownership of the referenced session_id.
  - _Why false positive:_ presenter_slide_images is scoped transitively via tenant-scoped presenter_sessions through session_id; child insert without tenant_id is by design.
- **server/google-workspace.ts:2364** _(fk-transitive)_ — Second INSERT/UPsert into presenter_slide_images (thumb) similarly omits tenant_id — writes tenant-scoped data without explicit tenant column.
  - _Why false positive:_ Thumbnail row is another presenter_slide_images child insert scoped through tenant-owned presenter_sessions.session_id.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from tenant-scoped table `research_schedules` without a tenant_id constraint (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Why false positive:_ Heartbeat intentionally scans due research_schedules across tenants and then processes each row using its own sched.tenant_id.
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Why false positive:_ Maintenance loop updates housekeeping fields on the same schedule rows selected by the cross-tenant sweep; per-row tenant is carried and no tenant data is returned/mixed.
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Why false positive:_ Same platform-wide scheduler sweep updating last_run_at/next_run_at housekeeping only for each selected row.
- **server/heartbeat.ts:113** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Why false positive:_ Same cross-tenant maintenance scheduler loop updating only next_run_at for each selected schedule row.
- **server/heartbeat.ts:121** _(system-sweep)_ — UPDATE on tenant-scoped table `research_schedules` (recovery path) uses only WHERE id = ${sched.id} with no tenant_id check.
  - _Why false positive:_ Recovery-path housekeeping update within the same global schedule sweep; changes only next_run_at on the selected row.
- **server/heartbeat.ts:385** _(system-sweep)_ — SELECT from tenant-scoped table `event_log` without tenant_id constraint (SELECT * FROM event_log WHERE status='pending' ...). This returns pending events across all tenants.
  - _Why false positive:_ Global event-processing loop intentionally polls pending events across tenants and routes each using row-carried tenant_id; failures are persisted with id+tenant_id.
- **server/heartbeat.ts:273** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — raw UPDATE on tenant-scoped `heartbeat_tasks` without tenant_id constraint.
  - _Why false positive:_ Startup normalization of heartbeat task models is platform-wide maintenance on scheduled-task rows, changing housekeeping/config fields only.
- **server/heartbeat.ts:277** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — raw UPDATE on tenant-scoped `heartbeat_tasks` without tenant_id constraint.
  - _Why false positive:_ Same startup maintenance sweep normalizing reflection task model across task rows; status/config mutation only, no tenant data exposure.
- **server/heartbeat.ts:2764** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — raw UPDATE to tenant-scoped `heartbeat_tasks` without tenant_id constraint (in processDelegations).
  - _Why false positive:_ Updates freshly created task by server-generated newTask.id within same internal flow; id not from untrusted request boundary.
- **server/heartbeat.ts:3216** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — raw UPDATE to tenant-scoped `heartbeat_tasks` without tenant_id constraint (in delegateTaskFromChat).
  - _Why false positive:_ Updates just-inserted heartbeat task using returned server-generated PK newTask.id in same function, not caller-controlled.
- **server/job-queue.ts:385** _(system-sweep)_ — getJobStats runs an aggregate SELECT FROM agent_jobs without any tenant_id filter, returning counts across tenants (potential information disclosure).
  - _Why false positive:_ Cluster-wide aggregate counts only; returns no tenant row data or mixed content, matching operator/housekeeping stats pattern.
- **server/lib/action-ledger.ts:268** _(system-sweep)_ — Cross-tenant SELECT: listUnknownAttemptsAllTenants queries action_attempts WHERE state = 'unknown' with no tenant_id predicate.
  - _Why false positive:_ Code explicitly documents cross-tenant reconciler sweep; each row carries own tenant_id for downstream scoped writes.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE on procedure_evolution_runs lacks tenant predicate: the code updates procedureEvolutionRuns by id only (.where(eq(procedureEvolutionRuns.id, runId)) ) without checking tenant_id.
  - _Why false positive:_ Updates run row just inserted in same function via returned runId server-generated PK, not untrusted input.
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE on procedure_evolution_runs lacks tenant predicate: the noop-completion update uses .where(eq(procedureEvolutionRuns.id, runId)) without tenant filtering.
  - _Why false positive:_ Noop completion targets same freshly created procedure_evolution_runs row by internal runId PK.
- **server/lib/aevo-meta-editor.ts:500** _(internal-worker-pk)_ — UPDATE on procedure_evolution_runs lacks tenant predicate: final `done` update uses .where(eq(procedureEvolutionRuns.id, runId)) without tenant scoping.
  - _Why false positive:_ Final done update uses runId from prior insert in same execution path, a server-generated primary key.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Query reads the tenant-scoped projects table without constraining to a specific tenant_id (SELECT tenant_id, count(*) ... FROM projects WHERE tags && ... GROUP BY tenant_id). This performs an unscoped read across all tenants, violating the invariant that tenant-scoped tables must be filtered by tenant_id at the application level.
  - _Why false positive:_ boot/backfill maintenance code intentionally aggregates counts across tenants and only uses per-tenant counts to decide whether snapshot tenants need backfill; no tenant data returned to tenant-facing surface
- **server/lib/ideabrowser-score.ts:181** _(upstream-verified)_ — Unsafe string-interpolated SQL array literal built from caller-derived projectIds (`const literal = `{${ids.join(",")}}``) is injected into a db.execute(sql`... ANY(${literal}::int[])`) call. This constructs SQL text from user-controlled values instead of using a parameterized array, creating an injection risk and undermining DB-level safety assumptions.
  - _Why false positive:_ not a tenant-isolation finding; query explicitly constrains both revenue_missions and mission_evidence by tenantId
- **server/lib/inbox-ingest.ts:506** _(upstream-verified)_ — UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id} — the UPDATE lacks an explicit tenant_id predicate. Per the project's tenant-isolation invariant, every UPDATE against tenant-scoped tables must also constrain by tenant_id to avoid cross-tenant modification races when IDs collide.
  - _Why false positive:_ the row was just claimed/inserted for this tenant with inbox_message_id = storeResult.id immediately above, so the follow-up update targets that same claimed row
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — scanOutcomePromotions performs a SELECT on tenant-scoped table deliverable_grade_events without constraining the query to a specific tenant (no WHERE tenant_id = ...). This reads across all tenants.
  - _Why false positive:_ nightly optimizer scan intentionally aggregates deliverable_grade_events across tenants, then processes each tenant_id separately and writes generated files per tenant without mixing tenants into one prompt/output
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — Query selects/upates scheduled_posts without any tenant_id filter (SELECT id FROM scheduled_posts ... FOR UPDATE SKIP LOCKED / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). scheduled_posts is tenant-scoped elsewhere; performing unlocked cross-tenant SELECT/UPDATE risks violating tenant isolation.
  - _Why false positive:_ platform-wide scheduler intentionally locks and updates due rows across tenants, returning each row with its own tenant_id for per-row handling rather than mixing tenant content into one tenant-facing response
- **server/martech-bundle.ts:325** _(upstream-verified)_ — UPDATE on tenantVoiceProfiles uses only id in WHERE clause (where(eq(tenantVoiceProfiles.id, existing.id))) — missing tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ existing row is fetched just above via getVoiceProfile({ tenantId: opts.tenantId, profileName }), so the update by existing.id is on a row already proven to belong to that tenant
- **server/memory-graph.ts:239** _(fk-transitive)_ — SELECT 1 FROM memory_links WHERE ... LIMIT 1 checks links by ids without constraining by tenant_id — reads a tenant-scoped resource (memory_links / related rows) with no tenant filter.
  - _Why false positive:_ memory_links has no tenant_id and links are derived from memory_entries already filtered to the caller tenant, so existence check is transitively tenant-scoped through tenant-owned endpoint rows
- **server/memory-graph.ts:494** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — UPDATE targets a tenant-scoped table but uses only id in WHERE clause (no tenant_id constraint).
  - _Why false positive:_ Row ids come from an immediately preceding SELECT on memory_entries scoped by tenant_id = tenantId; update is applied only to those selected rows in same call path.
- **server/memory-intelligence.ts:273** _(internal-worker-pk)_ — db.execute(UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id}) — UPDATE uses only id in WHERE clause without tenant_id constraint; writes to a tenant-scoped table are not scoped to caller tenant.
  - _Why false positive:_ Async server-internal post-create update targets the freshly created entry by server-generated primary key entry.id, not an untrusted request id.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — UPDATE uses only id in WHERE clause, missing tenant_id constraint even though earlier SELECT restricted by tenant_id.
  - _Why false positive:_ Each c.id comes from the immediately preceding contradictions SELECT constrained by tenant_id = tenantId in the same function.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id (INSERT INTO project_files ... VALUES (...)) — row will not be explicitly tagged with tenant ownership.
  - _Why false positive:_ project_files has no tenant_id column and is isolated transitively through tenant-scoped parent project_id, with assertProjectInTenant verifying ownership first.
- **server/mpeg-engine.ts:1416** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id (INSERT INTO project_files ... VALUES (...)) in the parallel path.
  - _Why false positive:_ project_files has no tenant_id column and is a child of tenant-scoped projects; assertProjectInTenant confirms parent belongs to the tenant before insert.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without a tenant_id filter (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE) — reads all tenants' subscriptions.
  - _Why false positive:_ Startup validation intentionally scans active subscriptions across tenants for maintenance/refresh and processes each row using its own row-carried tenant_id.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes without tenant constraint — reads data across all tenants.
  - _Why false positive:_ Boot-time auto-reconnect intentionally enumerates google-workspace subscription tenants globally for housekeeping, then handles each tenant separately by tenant_id.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} lacks a tenant_id constraint — modifying tenant-scoped rows without explicit tenant scoping.
  - _Why false positive:_ Global startup maintenance loop updates housekeeping field is_active per row selected from a cross-tenant sweep; no tenant data is returned or mixed.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant_id (SELECT provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE) — iterates across tenants.
  - _Why false positive:_ Global maintenance refresh loop scans all active subscriptions and processes each row with its own tenant_id; no tenant data is returned to tenant-facing output.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE executes without tenant filter — returns rows for all tenants.
  - _Why false positive:_ Daily force-refresh cron intentionally operates across tenants and handles each subscription using row-carried tenant_id only.
- **server/pace-control.ts:85** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` without tenant scoping (COUNT... FROM heartbeat_logs WHERE created_at > ... AND <laneFilter>) — counts across tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this pace counter is on a platform-global table by design.
- **server/pace-control.ts:140** _(global-table)_ — SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... is executed without tenant_id constraint in getPaceSnapshot — aggregates across tenants.
  - _Why false positive:_ heartbeat_logs is explicitly non-tenant-scoped per schema, so global snapshot aggregation is expected on a platform-global table.
- **server/pace-control.ts:147** _(global-table)_ — SELECT grouping persona counts FROM heartbeat_logs WHERE created_at > ... is executed without tenant scoping — returns per-persona counts across all tenants.
  - _Why false positive:_ Grouping from heartbeat_logs is against a table with no tenant_id column, matching intentional global telemetry.
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selects projects JOIN conversations without any tenant_id constraints (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...).
  - _Why false positive:_ Backfill is a platform-wide maintenance routine creating per-project brain files; it sweeps across tenants intentionally rather than serving tenant-facing reads.
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is executed without tenant scoping — reading conversations by project id only can cross tenants.
  - _Why false positive:_ This conversation scan is inside the global backfill sweep and processes each project/conversation individually for housekeeping output only.
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} has no tenant constraint — messages (tenant-scoped) read without validating ownership of conversation/conversationId.
  - _Why false positive:_ Message reads occur within the same maintenance backfill loop and are used to build per-project artifacts, not mixed into a tenant response surface.
- **server/process-governor.ts:595** _(upstream-verified)_ — In disable_task action, UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} is executed without tenant_id constraint — this may disable tasks across tenants if id collisions or leaked ids occur.
  - _Why false positive:_ Rows being updated were selected moments earlier from heartbeat_tasks with ht.tenant_id = tenantId, so t.id values are pre-scoped in the same call path.
- **server/process-governor.ts:640** _(upstream-verified)_ — In restart_agent action, UPDATE agent_desks SET ... WHERE id = ${desk.id} is executed without tenant_id filter — modifies agent_desks by id only without verifying tenant ownership.
  - _Why false positive:_ desk.id comes from a prior SELECT on agent_desks constrained by ad.tenant_id = tenantId in the same action, so ownership is established upstream.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` without a tenant_id WHERE clause (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This is a cross-tenant read with no tenant-level SQL filter.
  - _Why false positive:_ Heartbeat runner intentionally scans recurring_message jobs across tenants and then uses each row's tenant_id for subsequent per-row conversation access/inserts.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` lacks a tenant_id constraint (UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}).
  - _Why false positive:_ storedId comes directly from immediately preceding tenant-scoped SELECT on memory_entries with tenant_id and persona_id, so the UPDATE targets that verified row
- **server/research-engine.ts:495** _(fk-transitive)_ — INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) does not set tenant_id. (Same pattern repeated at lines ~503-506.)
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-isolated through tenant-scoped parent project_id after assertProjectInTenant check
- **server/routes.ts:766** _(system-sweep)_ — Unscoped SELECT from tenant-scoped `projects` (backfillProjectDriveFolders) — no tenant_id constraint on the query.
  - _Why false positive:_ startup drive-folder backfill intentionally scans all projects, carries each row's own tenant_id, and performs housekeeping creation per project without returning mixed tenant data
- **server/routes.ts:4338** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The raw SQL INSERT (`INSERT INTO project_notes (project_id, note, author) VALUES (...)`) writes a row referencing a project but omits tenant_id, relying only on earlier SELECT that fetched projId. Without explicitly writing tenant_id on INSERT (and with no RLS/fk cascade), this can create notes with no tenant or attach them to the wrong tenant.
  - _Why false positive:_ project_notes has no tenant_id column and insert is tied to project_id obtained from tenant-scoped conversation lookup
- **server/routes/admin-zombie-detector.ts:38** _(system-sweep)_ — SELECT reads from agent_trace_spans (CTE `used`) with no tenant_id constraint. agent_trace_spans appears tenant-scoped, so this query can aggregate/return cross-tenant usage.
  - _Why false positive:_ admin-only route performs platform-wide aggregate usage reporting, not tenant-facing data access, and returns aggregated counts/last-used metadata
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on conversations omits a tenant_id constraint: UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}. This modifies a tenant-scoped table without constraining the update to the caller's tenant.
  - _Why false positive:_ conversation row was just created for tenantId in same path and project ownership was verified by tenant-scoped SELECT before attaching
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on projects omits a tenant_id constraint: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}. This updates a tenant-scoped table without restricting to the caller's tenant.
  - _Why false positive:_ projectId was verified moments earlier via SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId in the same call path
- **server/routes/goal-ledger.ts:92** _(system-sweep)_ — Unscoped SELECT against tenant-scoped table `agent_jobs` (no tenant_id predicate). The helper `readActiveJobs()` issues a query returning jobs across all tenants.
  - _Why false positive:_ platform-admin-only dashboard intentionally reads active jobs across tenants for ops visibility and returns admin aggregate/listing, not tenant-facing mixed output
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true — no tenant_id constraint. heartbeat_tasks appears tenant-scoped elsewhere; this will disable delegation tasks across all tenants unintentionally.
  - _Why false positive:_ boot-time cleanup intentionally disables runaway heartbeat tasks across tenants and only mutates housekeeping status fields
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true — missing tenant_id constraint. This may disable tenant-specific tasks globally.
  - _Why false positive:_ boot-time cleanup globally disables stray session-logger heartbeat tasks; status-only maintenance sweep, no tenant data returned
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true — performed without tenant_id constraint despite heartbeat_tasks being updated with tenant_id in other places.
  - _Why false positive:_ boot-time cleanup globally disables orphaned repo-examination heartbeat tasks; housekeeping-only mutation across tenants
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions executed with no tenant_id constraint. research_sessions is tenant-scoped but this query aggregates across all tenants.
  - _Why false positive:_ seed/import routine checks whether any research_sessions exist globally before one-time snapshot import; aggregate count only, no tenant data exposure
- **server/seed.ts:2158** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true executed with no tenant_id constraint. research_programs is tenant-scoped — this reads programs for all tenants.
  - _Why false positive:_ seed maintenance scans active research_programs platform-wide to repair invalid model ids; admin housekeeping only, no tenant-facing mixed output
- **server/seed.ts:2516** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... executes against conversations without filtering by tenant_id. conversations is tenant-scoped, so this reads across tenants.
  - _Why false positive:_ boot-time backfill links conversations to projects across tenants, processing each row independently and inserting only relation rows
- **server/seed.ts:3853** _(system-sweep)_ — DELETE from tenant-scoped table `heartbeat_tasks` has no tenant_id constraint. This DELETE will affect rows across all tenants.
  - _Why false positive:_ startup purge removes stale auto-disabled heartbeat task rows platform-wide; housekeeping delete only, no tenant content exposure
- **server/seed.ts:3881** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` lacks any tenant_id constraint. This backfill will modify memory entries for all tenants.
  - _Why false positive:_ global backfill normalizes misspelling in memory_entries across tenants; maintenance-only mutation, not tenant data disclosure or mixing
- **server/seed.ts:3907** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` modifies all rows (no tenant_id in WHERE).
  - _Why false positive:_ global backfill updates legacy conversation model ids across tenants; housekeeping-only mutation with no tenant-facing readout
- **server/seed.ts:4121** _(global-table)_ — UPDATE against tenant-scoped table `agent_knowledge` uses only id in WHERE and omits tenant_id. Updating by id without verifying tenant allows changing rows in other tenants.
  - _Why false positive:_ the shown update is on personas by global id, and personas are schema-global without tenant_id; first-pass issue text mismatches code/table
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT FROM heartbeat_logs is executed without any tenant_id constraint. This aggregates error counts across all tenants and is used to disable heartbeat tasks; performing this SELECT unscoped against a tenant-scoped table can read cross-tenant data.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is treated as platform-global watchdog telemetry
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without any tenant_id constraint. This will delete disabled delegation tasks across all tenants, which violates tenant isolation if heartbeat_tasks is tenant-scoped.
  - _Why false positive:_ global housekeeping purge of old disabled tasks; only deletes stale rows and returns aggregate count
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log is executed without any tenant_id constraint. If event_log is tenant-scoped, this will remove events across all tenants without scoping.
  - _Why false positive:_ platform-wide cleanup of processed/failed event rows; housekeeping-only mutation with aggregate count output
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs in pruneOldLogs is executed without a tenant_id constraint. This will prune logs for all tenants indiscriminately if heartbeat_logs is tenant-scoped.
  - _Why false positive:_ heartbeat_logs has no tenant_id and old-log pruning is global log maintenance
- **server/storage.ts:298** _(upstream-verified)_ — Unscoped SELECT from tenant-scoped table 'messages' in attachCitationMetadata — query omits tenant_id constraint.
  - _Why false positive:_ ids come from msgs already fetched with tenant-scoped queries, so citation lookup is limited to validated same-tenant message ids
- **server/storage.ts:624** _(upstream-verified)_ — UPDATE conversation_facts SET status = ... is executed with WHERE id IN (victims) but no tenant_id constraint — write uses primary key only, allowing potential cross-tenant mutation if IDs collide or callers misuse the API.
  - _Why false positive:_ victim ids are selected immediately above with conversationId and tenantId filters, then updated from that scoped set
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories executes a global UPDATE on tenant-scoped table 'memory_entries' (no tenant_id filter).
  - _Why false positive:_ platform-wide lifecycle sweep archives expired memories; housekeeping status change only with no tenant data returned
- **server/storage.ts:858** _(system-sweep)_ — archiveStaleMemories executes a global UPDATE on tenant-scoped table 'memory_entries' without tenant_id constraint.
  - _Why false positive:_ platform-wide lifecycle sweep archives stale memories; housekeeping-only status mutation with no cross-tenant output
- **server/tools.ts:2994** _(fk-transitive)_ — INSERT into project_files does not include tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) omits tenant_id, violating the invariant that tenant-scoped inserts must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; code verifies project ownership before insert.
- **server/tools.ts:3008** _(fk-transitive)_ — INSERT into project_notes does not include tenant_id. The INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id, violating the requirement that every insert to a tenant-scoped table pass tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped via project_id to a tenant-scoped project, with owner check immediately above.
- **server/tools.ts:2890** _(fk-transitive)_ — INSERT into project_conversations during project create does not include tenant_id. The code inserts (project_id, conversation_id) without tenant_id even though this links entities across tenants.
  - _Why false positive:_ project_conversations has no tenant_id and join row is transitively scoped by tenant-owned project/conversation; preceding update only links if conversation belongs to tenant.
- **server/tools.ts:3031** _(fk-transitive)_ — INSERT into project_conversations in link_conversation handler omits tenant_id. The insert INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) does not set tenant_id even though the handler links tenant-scoped resources.
  - _Why false positive:_ project_conversations has no tenant_id and both project ownership and same-tenant conversation ownership are verified before insert.
- **server/tools.ts:4552** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` omits tenant_id in the column list/VALUES. The code verifies project ownership via assertProjectInTenant but does not include tenant_id in the INSERT, violating the invariant that tenant_id must be passed explicitly for tenant-scoped writes.
  - _Why false positive:_ project_notes has no tenant_id and insert is transitively scoped through project_id after assertProjectInTenant confirms ownership.
- **server/tools.ts:4556** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id in the column list/VALUES. Although assertProjectInTenant was used to confirm ownership, the INSERT does not set tenant_id explicitly as required by the tenancy invariant.
  - _Why false positive:_ project_files has no tenant_id column and is child-scoped via verified tenant-owned project_id
- **server/tools.ts:6405** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (from generate_audio) omits tenant_id in the column list/VALUES. The code calls assertProjectInTenant, but the INSERT itself does not include tenant_id.
  - _Why false positive:_ project_files has no tenant_id column; assertProjectInTenant verifies parent project ownership before insert
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against tenant-scoped table `browser_workflows` without constraining by tenant_id: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`.
  - _Why false positive:_ workflow row was selected immediately above with tenant_id filter; update uses that verified row id
- **server/tools/domains/files/handlers.ts:900** _(upstream-verified)_ — UPDATE on tenant-scoped `projects` table omits tenant filter: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`.
  - _Why false positive:_ projId comes from prior join to projects with p.tenant_id = tenantId, so update targets verified tenant-owned project
- **server/tools/domains/knowledge/handlers.ts:87** _(upstream-verified)_ — UPDATE of tenant-scoped table `knowledge_triples` is missing a tenant_id constraint (WHERE id = ${row.id} without AND tenant_id = ${tenantId}). This allows an attacker who knows/guesses a row id to affect rows belonging to other tenants.
  - _Why false positive:_ each row.id being updated was selected immediately above from knowledge_triples constrained by tenant_id = tenantId
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT from tenant-scoped table `conversations` by id with no tenant filter. Reads the conversation row across all tenants.
  - _Why false positive:_ cross-tenant read is used only to verify claimed ownership against info.tenantId and rows are fail-closed on mismatch/ambiguity
- **server/video-job-runner.ts:208** _(internal-worker-pk)_ — SELECT from tenant-scoped table `video_jobs` without constraining by tenant_id — isCancelRequested(jobId) queries cancelRequested by jobId only, allowing cross-tenant reading of cancel flags.
  - _Why false positive:_ server-side runner helper reads by globally unique jobId for internal dispatch/cancel polling, not from a request boundary in this call path.
- **server/video-job-runner.ts:236** _(system-sweep)_ — UPDATE on tenant-scoped table `video_jobs` performed with no tenant_id constraint — recoverStaleVideoJobs() marks stale jobs FAILED across all tenants.
  - _Why false positive:_ periodic stale-job recovery is a platform-wide housekeeping sweep updating status fields only across tenants, with no tenant data mixed into a tenant-facing output.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on commitments without tenant filter — `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` can modify a row in any tenant because it lacks `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/embeddings.ts:748** _(system-sweep)_ — UPDATE performed on tenant-scoped table (memory_entries or agent_knowledge) without constraining tenant_id. storeEmbeddingVec updates embedding_vec/embedding by id only, allowing cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from code_proposals omits any tenant scoping — query pulls proposals across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): archiveStaleProposals is a platform TTL maintenance sweep; it selects code_proposals older than PROPOSAL_TTL_DAYS and (apply) updates status='archived_stale' by id set (server/health-audit.ts:314-338) — housekeeping-only status change, not a tenant-facing surface.
- **server/job-queue.ts:283** _(internal-worker-pk)_ — retryJob updates agent_jobs to reset status/attempts without any tenant_id constraint — an operator could reset jobs across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/routes.ts:167** _(system-sweep)_ — Unscoped SELECT from tenant-scoped `file_storage` during startup restore — no tenant_id constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): startup restoreUploadsFromDb rehydrates the servable uploads dir from file_storage; boot-time system context, not a request path.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} — missing tenant_id constraint. This writes tenant-scoped data without verifying ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/self-improvement.ts:171** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` can execute without a tenant_id constraint. detectStagnation builds `conditions` optionally including tenantId; when tenantId is omitted the query uses only category and will return experiments across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:401** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` uses only the experiment id in the WHERE clause (.where(eq(experiments.id, experiment.id))). There is no tenant_id constraint, so this update could modify another tenant's row if an id is controlled or leaked.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-improvement.ts:415** _(internal-worker-pk)_ — Error-path UPDATE on `experiments` also uses only experiment id in the WHERE clause (.where(eq(experiments.id, experiment.id))). No tenant_id check is present on the write path.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-reflection.ts:366** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `memory_entries` uses only the returned row id in WHERE (WHERE id = ... ) with no tenant_id constraint. Although the id was selected earlier with tenant_id filtering, the subsequent UPDATE is unscoped and could affect rows from other tenants if ids are confused or a race occurs.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): UPDATE id comes from the tenant-scoped SELECT (WHERE tenant_id = ${tenantId}) directly above.
- **server/storage.ts:513** _(system-sweep)_ — getAllMemoriesForBackup does a global SELECT from tenant-scoped table 'memory_entries' with no tenant filter (full export across tenants).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/storage.ts:1304** _(upstream-verified)_ — getAllDataForExport issues many SELECTs across tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant_provider_keys, heartbeat_tasks, delivery_logs, etc.) without any tenant filtering — full platform export risk.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE on `agent_activity` missing tenant constraint: `await db.update(agentActivity).set(updates).where(eq(agentActivity.id, activityId))` can modify a row belonging to another tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/agent-channels.ts:198** _(upstream-verified)_ — JOIN against `channel_subscriptions` lacks tenant scoping: `JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}` (the query only constrains `cm.tenant_id`). This can match subscription rows from other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): getUnreadCount filters cm.tenant_id = ${tenantId}; the channel_subscriptions join is keyed on the caller's persona_id (personas are tenant-scoped FKs), so another tenant's subscription rows cannot match.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT of recent heartbeat logs in `runPredictiveEngine` omits tenant scope: `SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10`.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/agentic-engines.ts:451 — The query at server/agentic-engines.ts:451-454 reads heartbeat_logs for recent successful automated activities and feeds a platform-level predictive prompt at :456-463; it is not tenant-facing row retrieval. shared/schema.ts:557-575 shows heartbeat_logs has no tenant_id, and the surrounding engine is a platform-wide aggregate rather than a tenant response.
- **server/agentic-engines.ts:533** _(global-table)_ — Query of `heartbeat_tasks` in `runOptimizationEngine` lacks tenant scoping: `SELECT name, cron_expression, enabled, last_run_at, run_all FROM heartbeat_tasks WHERE enabled = true ORDER BY name`.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/agentic-features.ts:638** _(upstream-verified)_ — Existence check before enrollment omits tenant scoping: `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'` can match enrollments from other tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/auto-memorize.ts:47** _(system-sweep)_ — getWatermark() reads agent_knowledge without constraining by tenant_id (SELECT ... FROM agent_knowledge ...). agent_knowledge rows are tenant-scoped elsewhere, so this can return another tenant's watermark.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `capability_reviews` performed without constraining by tenant_id. The query `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` does not include a tenant check, so a reviewer in one tenant could affect rows belonging to another tenant if an id is reused or attacker can influence the reviewId mapping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/chat-engine.ts:633** _(upstream-verified)_ — SELECT from tenant-scoped table `compaction_archives` uses only conversation_id in the WHERE clause and lacks tenant_id = ${tenantId}. This can allow cross-tenant access if conversation ids are reused or an attacker obtains a conversation id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:632 — The compaction_archives SELECT is scoped by conversation_id = c.id, where c comes from `convs` fetched at line 593-598 with an explicit `c.tenant_id = ${tenantId}` filter (and the parent project is validated `WHERE id = projectId AND tenant_id = tenantId` at line 543). So c.id is a PK of an already-tenant-validated conversation row — no cross-tenant read possible.
- **server/chat-engine.ts:4421** _(internal-worker-pk)_ — Call to storage.attachCitations(...) does not include tenant context. This writes/associates citation rows after creating a tenant-scoped message but does not pass tenant_id (or otherwise verify ownership), risking cross-tenant writes/reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/chat-engine.ts:4332 — assistantMsg.id is server-generated two lines above (chat-engine.ts:4329, created WITH tenantIdForScope); attachCitations updates only that fresh id — never caller input.
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks tenant scoping — updates a presenter session by id with no tenant_id predicate or ownership check.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/job-queue.ts:320** _(system-sweep)_ — listJobs builds and executes a SELECT FROM agent_jobs without a tenant_id constraint when tenantId is not provided. That returns rows across tenants by default (data exposure risk).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/lib/agent-trace.ts:124** _(internal-worker-pk)_ — UPDATE without tenant filter: persistSpanClose runs UPDATE agent_trace_spans ... WHERE span_id = ${spanId} with no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/bwb-job-progress.ts:141** _(internal-worker-pk)_ — Potential cross-tenant UPDATE: setBwbPhase updates video_jobs using .where(scope(jobId, ...)) and scope may omit tenant predicate when BWB_TENANT_ID is not provided, allowing updates across tenants keyed only by jobId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:166** _(internal-worker-pk)_ — Potential cross-tenant UPDATE: updateBwbChapters updates video_jobs using .where(scope(jobId, ...)) where scope can omit tenant_id if env tenant is missing, enabling cross-tenant writes keyed only by jobId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:200** _(internal-worker-pk)_ — Potential cross-tenant SELECT: completeBwbJob reads video_jobs via .where(scope(jobId)) and scope may lack tenant predicate if BWB_TENANT_ID is not set, exposing/reading rows across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): jobId is an unguessable random id (vj_<base36>_<6-byte-hex>); scope() warns once when BWB_TENANT_ID unthreaded — documented defense-in-depth.
- **server/lib/bwb-job-progress.ts:227** _(internal-worker-pk)_ — Potential cross-tenant UPDATE: bumpBwbHeartbeat updates video_jobs using .where(scope(jobId, ...)) and scope may omit tenantId, allowing heartbeat writes to affect rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:245** _(internal-worker-pk)_ — Potential cross-tenant UPDATE: failBwbJob updates video_jobs using .where(scope(jobId, { notDone: true })) and scope can omit tenant predicate when the env tenant is missing, permitting cross-tenant status changes.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): failBwbJob pinned to the same unguessable random jobId; documented warn-once fallback.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — update lacks tenant_id constraint; writes to a tenant-scoped table are not filtered by tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): catId is produced by ensureCategory (server/memory-graph.ts:79) which SELECTs/INSERTs scoped to tenantId (:51 'AND tenant_id = ${tenantId}', :57-58 INSERT with tenant_id); the id is server-derived within the same tenant call path, not request-supplied.
- **server/minds-engine.ts:214** _(upstream-verified)_ — INSERT into mind_tickets uses string-interpolated dependsOn array from caller input (`${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}`), which is user-derived and injected directly into the SQL string (SQL injection + isolation risk).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): dependsOn is typed number[] (MindTicket contract) so join() yields a numeric literal; tenant_id is a bound param in the same INSERT.
- **server/minerva-planner.ts:386** _(upstream-verified)_ — Probe SELECT after a CAS miss executes `SELECT status, ceo_decision FROM plans WHERE id = ${args.planId} ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}` — if args.tenantId is omitted this reads a tenant-scoped row without a tenant_id constraint, risking cross-tenant data disclosure.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Child-plan lookup runs inside decidePlan's revise-idempotency branch, gated by the same route ownership check at agentic-policy.ts:113; args.planId is the verified plan id.
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without verifying tenant ownership or adding tenant_id filter — resource access by project id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` lacks a tenant_id constraint (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/routes.ts:1118** _(upstream-verified)_ — SELECT from tenant-scoped `file_storage` by filename without tenant_id constraint in uploads middleware. Reading all owners for a basename may expose or mix tenant rows.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): this SELECT of owner tenantIds IS the isolation check — non-owners get 404 on the next line.
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} with no tenant_id constraint. This can modify another tenant's ledger row if reservationId is mis-bound.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:424** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without a tenant_id constraint, allowing unintended cross-tenant ledger modifications.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/storage.ts:399** _(internal-worker-pk)_ — Unscoped UPDATE on tenant-scoped table 'messages' in attachCitations — no tenant_id constraint in WHERE clause.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:248** _(upstream-verified)_ — getConversationUnscoped performs a SELECT on tenant-scoped table 'conversations' without any tenant_id constraint (intentionally unscoped helper).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:806** _(internal-worker-pk)_ — updateMemoryEmbedding updates tenant-scoped table 'memory_entries' using WHERE id = ${id} only (no tenant_id constraint).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding updates tenant-scoped table 'agent_knowledge' with WHERE id = ${id} but no tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:482** _(global-table)_ — deletePersona performs updates/deletes against tenant-scoped tables (conversations, memory_entries, daily_notes) without constraining by tenant_id — actions will affect all tenants' rows referencing the persona.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:479 — The personas table has no tenant_id column — personas are global/platform-wide by design (mapped per-tenant only via tenant_persona_names). deletePersona is invoked solely from DELETE /api/personas/:id, which is gated by requirePlatformAdmin (routes/personas.ts:134-136). The cascade cleanup of conversations/memory_entries/daily_notes by persona_id is an intended platfo
- **server/storage.ts:969** _(system-sweep)_ — updateHeartbeatTask can perform updates against heartbeat_tasks without tenant_id constraint when tenantId is omitted (WHERE falls back to id only).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): markHeartbeatTaskRun callers are all the internal cron runner (heartbeat.ts:272,865,1097,...) using server-generated task.id; only housekeeping lastRunAt/nextRunAt are written (storage.ts:1035).
- **server/storage.ts:975** _(upstream-verified)_ — deleteHeartbeatTask can delete from heartbeat_tasks using only id when tenantId is omitted (no tenant_id constraint), permitting cross-tenant deletes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): deleteHeartbeatTask only caller is routes/heartbeat.ts:81 which always passes tenantId → tenant subselect scope applied (storage.ts:966); no unscoped caller exists.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — stampArgsEmbedding executes UPDATE felix_proposals ... WHERE id = ${proposalId} without tenant_id constraint — writing a tenant-scoped table without tenant filtering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/surprise-scorer.ts:141** _(upstream-verified)_ — scoreProposalSurprise updates actual_outcome_embedding on felix_proposals with WHERE id = ${proposalId} but no tenant_id constraint — write lacks tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/tools.ts:6939** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (from create_slideshow_video) omits tenant_id in the column list/VALUES. The code verifies project ownership but fails to pass tenant_id explicitly in the INSERT.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/tools.ts:6914 — The same insert is preceded by the explicit project ownership guard at server/tools.ts:6912-6914. project_files is not tenant_id-bearing in the actual schema (shared/schema.ts:1199-1209), so omission of tenant_id is not a cross-tenant write by itself; ownership is inherited from project_id.
- **server/tools.ts:6939** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) call omits tenant_id even though project_files is tenant-scoped (no RLS/fk cascade).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column; server/tools.ts:6898 assertProjectInTenant(params.project_id, params._tenantId) guard
- **server/tools.ts:8909** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' uses only the id in the WHERE clause (where(eq(proposedSkills.id, row.id))) — no tenant_id constraint. This allows a tenant to update another tenant's proposed_skills row by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8934** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' (marking accepted/promoted) filters only by id and not tenant_id (where(eq(proposedSkills.id, row.id))). This permits cross-tenant modification of proposals.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:8950** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'proposed_skills' when jury rejects (where(eq(proposedSkills.id, row.id))) lacks a tenant_id constraint. The update can affect rows across tenants if ids collide or are guessed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` without constraining `tenant_id` (reads all tenants' pairing rows).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` uses WHERE id = ${reservationId} with no tenant_id check — an UPDATE by id alone can modify rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` (release path) uses WHERE id = ${reservationId} without tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/minerva-planner.ts:549** _(deferred)_ — getPlan falls back to an unscoped SELECT when tenantId is omitted (`SELECT * FROM plans WHERE id = ${planId}`), which can leak tenant-scoped data if called from an untrusted context.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
