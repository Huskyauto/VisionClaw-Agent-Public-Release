# Tenant-Isolation Audit Report

_Generated 2026-08-01T14:18:12.164Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **581/581** file(s) audited · chunks 53/53 OK (0 failed)
- Findings: **277** (CRITICAL 69 · HIGH 168 · MEDIUM 39 · LOW 1)
- Precision second-pass (`gpt-5.4`): **229** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (69)

- **server/agentic-engines.ts:243** — Bulk UPDATE on tenant-scoped table `ai_insights` without tenant constraint (UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...)). This can change status/action_taken for rows in all tenants in one operation.
  - _Fix:_ Do not perform unscoped bulk writes. Run this update per-tenant (accept or discover tenantId) and include tenant_id = ${tenantId} in the WHERE clause, or otherwise limit the update to an explicit, validated tenant list before writing.
- **server/agentic/runs.ts:34** — appendStep() updates agent_runs without a tenant_id constraint (WHERE only filters by id). This permits cross-tenant modification if called with an arbitrary runId.
  - _Fix:_ Include tenant scoping on all updates to tenant-scoped tables. Change the WHERE to include tenant_id (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and pass the caller's tenantId into appendStep. Validate ownership before mutating.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs using only the run id in the WHERE clause; no tenant_id constraint is applied.
  - _Fix:_ Require and use the caller's tenantId in the WHERE clause (AND agent_runs.tenant_id = <tenantId>) to ensure updates cannot affect other tenants' rows. Add tenantId parameter to the function if needed.
- **server/agentic/runs.ts:54** — replaceRunState() updates agent_runs using only the run id in the WHERE clause; no tenant_id constraint is applied.
  - _Fix:_ Add tenantId filtering to the WHERE clause and require the caller to provide tenantId. Use and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)).
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs (status/result/completed_at) using only the run id in the WHERE clause; tenant scoping is missing.
  - _Fix:_ Constrain the UPDATE by tenant (AND agent_runs.tenant_id = <tenantId>) — e.g. change the function to accept tenantId and apply and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)) to prevent cross-tenant writes.
- **server/agentic/runs.ts:88** — failRun() updates agent_runs (status/error/completed_at) using only the run id in the WHERE clause; no tenant_id constraint is present.
  - _Fix:_ Require and apply tenantId in the WHERE clause to ensure only rows belonging to the caller's tenant are modified (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/runs.ts:97** — pauseRun() updates agent_runs (status/state) using only the run id in the WHERE clause; missing tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE (AND agent_runs.tenant_id = <tenantId>) and require caller tenantId parameter so a caller cannot pause/modify runs from another tenant.
- **server/auth.ts:1155** — clearAllSessions executes DELETE FROM auth_sessions with no WHERE clause, deleting sessions for all tenants (global destructive action).
  - _Fix:_ Require an explicit admin-only guard and/or require a tenant_id parameter for tenant-scoped clears. Prefer DELETE FROM auth_sessions WHERE tenant_id = ${tenantId} when invoked to clear sessions for one tenant. If a global clear is necessary, gate it behind strict admin authorization and logging, and make it an explicit admin operation (not a general-purpose function callable in tenant context).
- **server/auto-memorize.ts:73** — INSERT into agent_knowledge hardcodes tenant_id = 1. Writing tenant-scoped table with a fixed tenant id can misattribute watermarks and cross-contaminate tenant data.
  - _Fix:_ Accept and pass the correct tenantId (caller context) into setWatermark and use that tenantId in the INSERT (and UPDATE WHERE clauses). Never hardcode tenant ids for tenant-scoped writes.
- **server/auto-memorize.ts:163** — SELECT from messages reads recent messages across all tenants (no WHERE tenant_id). The code then partitions rows by tenant in-app — this is a cross-tenant read of a tenant-scoped table.
  - _Fix:_ Scope the query to a specific tenant per execution (e.g. run per-tenant watermark or iterate tenants and run tenant-scoped queries), or add WHERE m.tenant_id = ${tenantId} if intended to operate for a single tenant. Avoid global SELECTs that return other tenants' messages.
- **server/commitment-drafter.ts:236** — UPDATE executed without tenant constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — this unclaims a commitment by id with no `tenant_id` guard, allowing cross-tenant modification if `row.id` is attacker-controlled or malformed.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenantId}`) or avoid performing the update when the row's tenantId is invalid. Failing closed (skip the revert) is safer than an unscoped UPDATE. Ensure all UPDATE/DELETE statements include `AND tenant_id = $tenant`.
- **server/credential-vault.ts:169** — UPDATE on credential_vault missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — called from getDecryptedPassword(id, tenantId). This mutates a row by id without verifying tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${id} AND tenant_id = ${tenantId}` (use the tenantId passed to the function). Alternatively, perform the update inside the same validated read/transaction that confirmed the credential belonged to the tenant.
- **server/credential-vault.ts:176** — UPDATE on credential_vault missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — called from getLoginCredentials(siteUrl, tenantId). This updates last_used_at without ensuring the row belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${cred.id} AND tenant_id = ${tenantId}`. Ensure any post-read mutations include the same tenant-scoped WHERE clause or are performed in a transaction using the validated tenant context.
- **server/data-protection.ts:131** — DELETE FROM messages WHERE conversation_id = ${conv.id} executes without constraining by tenant_id. Same for DELETE FROM compaction_archives and DELETE FROM conversations (lines 131-133). This allows deleting rows belonging to other tenants if conversation ids collide or earlier SELECT returned rows from other tenants.
  - _Fix:_ Include tenant scoping in each destructive statement, e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Do the same for compaction_archives and conversations; or run the purge logic inside a transaction that sets the tenant context (withTenantTx) before issuing deletes.
- **server/delivery-pipeline.ts:195** — updateDeliveryLog performs an UPDATE on tenant-scoped table delivery_logs without constraining by tenant_id (WHERE eq(delivery_logs.id, id) only). This allows updating another tenant's delivery row if called with a delivery id from a different tenant.
  - _Fix:_ Require and pass the tenantId into updateDeliveryLog and include it in the WHERE clause (e.g. .where(and(eq(delivery_logs.id, id), eq(delivery_logs.tenantId, tenantId)))) or otherwise assert caller authorization before performing the UPDATE.
- **server/embeddings.ts:716** — Background backfill routines (backfillEmbeddingVecs, backfillMissingKnowledgeEmbeddings) read and UPDATE memory_entries and agent_knowledge without any tenant_id scoping. SELECTs and subsequent UPDATEs operate across all rows which can modify data for other tenants.
  - _Fix:_ Run backfills per-tenant or add tenant_id filters to the SELECT and UPDATE statements. Ensure any background job accepts an explicit tenant context or iterates tenants and enforces WHERE tenant_id = ${tenantId} on all reads and writes to tenant-scoped tables.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images is missing tenant_id (INSERT statement does not set tenant_id). If presenter_slide_images is tenant-scoped this allows persisting/overwriting images for sessions belonging to other tenants.
  - _Fix:_ Verify the presenter session belongs to the current tenant before writing. Include tenant_id in the INSERT (and ON CONFLICT handling), or join via session→presenter_sessions filtered by tenant_id. e.g. validate session with SELECT id, tenant_id FROM presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId} and then INSERT ... tenant_id = ${tenantId}.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint. Updating by session id alone can modify another tenant's session if ids are guessed or misrouted.
  - _Fix:_ Enforce tenant-scoped update by adding AND tenant_id = ${tenantId} to the WHERE clause (or verify session ownership first). If this code is running with admin privileges, require an explicit admin flag and auditing; otherwise always constrain by tenant_id.
- **server/health-audit.ts:314** — SELECT FROM code_proposals has no tenant filter (SELECT id, title, status, created_at FROM code_proposals ...). code_proposals is likely tenant-scoped — this reads stale proposals across tenants.
  - _Fix:_ Scope the query to a specific tenant (WHERE tenant_id = ${tenantId}) or require an explicit admin/maintenance context. If the intent is a cross-tenant admin scan, require an explicit admin flag and make that explicit in code and audit logs.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) has no tenant constraint — this will modify proposals across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or restrict ids to proposals owned by the intended tenant. If this is intended to run as a global admin operation, require explicit admin authorization and narrow the UPDATE to rows previously selected with tenant context.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into tenant-scoped table `project_files` does not set tenant_id. Platform invariant says every INSERT must explicitly provide tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT VALUES (e.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)), or validate ownership by resolving the project record and setting tenant_id from it before inserting. If project_files is intentionally not tenant-scoped, document that explicitly; otherwise fix the schema usage.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. Every INSERT into tenant-scoped tables must explicitly supply tenant_id to avoid cross-tenant writes.
  - _Fix:_ Include tenant_id in the INSERT VALUES (e.g. INSERT INTO project_notes (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)), or derive and validate tenant_id from the project row before inserting. Audit other backfill writes to ensure tenant_id is always set.
- **server/lib/scheduled-post-runner.ts:319** — SQL array literal `arrLit` is constructed by string-concatenating user-supplied platform names and then interpolated into the SQL query (VALUES (${params.tenantId}, ${arrLit}::text[], ...)). This is a SQL-injection risk because user data is embedded into a query string literal.
  - _Fix:_ Avoid building SQL literals from user input. Use parameterized queries or driver/ORM support for arrays (pass the JS array as a parameter or use Drizzle's typed array binding) so platform names are passed as bind parameters rather than interpolated into SQL. Validate/normalize platform names separately but do not embed them into SQL text directly.
- **server/memory-graph.ts:210** — INSERT into memory_links omits tenant_id entirely (INSERT INTO memory_links (... ) VALUES (...)). Tenant_id must be provided on insert for tenant-scoped tables.
  - _Fix:_ Include tenant_id in the inserted columns and pass the caller's tenant id (e.g. INSERT INTO memory_links (..., tenant_id) VALUES (..., ${tenantId})), and validate the source/target memory rows belong to that tenant before inserting.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The statement inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without an explicit tenant_id column, relying on assertProjectInTenant for ownership but not recording tenant_id in the row. With no RLS or FK cascade, this can create cross-tenant data/write ambiguity.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ..., uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...)). If relying on assertProjectInTenant, still bind the verified tenantId into the INSERT rather than omitting it. Ensure tenantId is validated and non-null before writing.
- **server/mpeg-engine.ts:1417** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The parallel-produce code inserts project_files rows without an explicit tenant_id column (even though it calls assertProjectInTenant), leaving rows missing the tenant discriminator.
  - _Fix:_ Add tenant_id to the column list and values (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ${safeTitle + '.mp4'}, ...)). Always write the tenant_id you validated; do not rely on foreign-key inference or implicit ownership.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table `oauth_subscriptions` without scoping by tenant_id (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows across all tenants.
  - _Fix:_ Restrict this query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = TRUE) or iterate per-tenant from a trusted tenant list. If this is an intentional cross-tenant maintenance task, make it explicit and run under a dedicated admin tooling path with additional safeguards and auditing.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is missing tenant_id filtering and returns rows across all tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or otherwise restrict to an explicit, validated list of tenant IDs before performing per-tenant actions. Avoid blind, global selects on tenant-scoped tables.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh executes a global SELECT FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter, enumerating subscriptions across tenants.
  - _Fix:_ Require a tenantId parameter or iterate explicitly over an approved list of tenant IDs. Update the query to include WHERE tenant_id = ${tenantId} to enforce per-tenant isolation at the application level.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without constraining by tenant_id, exposing cross-tenant data and enabling cross-tenant operations.
  - _Fix:_ Avoid unbounded reads across all tenants. If a global maintenance job is required, run it under a dedicated admin context with explicit authorization; otherwise, change the query to scope by tenant_id or accept a tenantId to operate on one tenant at a time.
- **server/pace-control.ts:85** — checkPace queries heartbeat_logs without any tenant_id constraint (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). heartbeat_logs appears tenant-scoped so this counts across tenants.
  - _Fix:_ Accept and require a tenantId parameter (or otherwise determine current tenant) and add WHERE tenant_id = ${tenantId} to the query. Enforce per-tenant scoping for both global and per-persona caps to prevent cross-tenant leakage or denial-by-noise.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs without tenant scoping (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...). This returns global data across tenants for what appears to be tenant-scoped activity.
  - _Fix:_ Limit the snapshot to a tenant by adding a tenant_id filter (WHERE tenant_id = ${tenantId}) or make it explicit that this is an admin/global endpoint and gate it accordingly. Avoid unscoped queries on tenant-scoped tables.
- **server/persona-export.ts:33** — exportPersona reads personas via SELECT * FROM personas WHERE id = ${personaId} without verifying tenant ownership (no tenant_id filter). A caller-supplied personaId can be used to read another tenant's persona.
  - _Fix:_ Verify ownership by including tenant_id in the query (e.g. SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}) or first fetch and validate that the persona belongs to the requesting tenant before returning or using persona data.
- **server/plan-executor.ts:116** — appendExecutionLog() updates the tenant-scoped plans table using only plan id (UPDATE plans ... WHERE id = ${planId}) with no tenant_id constraint. This allows cross-tenant writes if an attacker or buggy caller supplies another planId.
  - _Fix:_ Constrain the UPDATE by tenant_id (and/or verify ownership first). Pass the caller's tenantId into appendExecutionLog or read the plan row and confirm plan. Example: UPDATE plans SET ... WHERE id = ${planId} AND tenant_id = ${callerTenantId}. Fail closed if tenant mismatch.
- **server/plan-executor.ts:513** — CAS claim uses UPDATE plans ... WHERE id = ${planId} AND status = 'approved' without scoping by tenant_id. Claiming a plan by id alone can let code running for one tenant move another tenant's plan into 'executing' (and then execute steps under the wrong tenant context).
  - _Fix:_ Include tenant_id in the CAS WHERE clause and/or require caller-provided tenantId and verify ownership prior to the UPDATE. e.g. WHERE id = ${planId} AND status = 'approved' AND tenant_id = ${callerTenantId}. If this function is invoked from untrusted input, validate the tenant before proceeding.
- **server/plan-executor.ts:527** — Code sets a plan to 'failed' using UPDATE plans SET status = 'failed' WHERE id = ${planId} with NO tenant_id constraint. Unscoped writes to tenant-scoped plans permit cross-tenant state changes.
  - _Fix:_ Always include AND tenant_id = ${callerTenantId} in WHERE clauses for modifying plans, or fetch-and-verify the plan's tenant_id before updating. Ensure callers supply/are validated against tenant context.
- **server/plan-executor.ts:725** — Final status update on plan uses UPDATE plans ... WHERE id = ${planId} without tenant scoping. This finalizes plan state across tenants if planId points to a different tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${callerTenantId}) or otherwise validate that the plan belongs to the invoking tenant before applying the UPDATE. Fail closed on mismatch.
- **server/plan-executor.ts:759** — Error-path recovery updates plans (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') with no tenant_id filter. This can mark other tenants' plans failed.
  - _Fix:_ Include tenant filter in the WHERE clause (AND tenant_id = ${callerTenantId}) or look up and validate the plan's tenant before running the recovery UPDATE. Ensure recovery code cannot affect plans outside intended tenant scope.
- **server/plan-executor.ts:775** — resumeStuckPlans() runs a cross-tenant UPDATE that flips ALL stale executing plans to 'approved' (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...), with no tenant_id constraint. This globally modifies tenant-scoped rows and can unintentionally surface or restart work across tenants.
  - _Fix:_ Restrict boot-time recovery to a specific tenant context or iterate per-tenant (e.g., load tenant list and run the UPDATE per tenant_id). If a global admin operation is intended, require an explicit admin flag and audit; otherwise include tenant scoping in the WHERE clause.
- **server/personality-files.ts:8** — Table definition for personality_files declares tenant_id INTEGER NOT NULL DEFAULT 1. The DEFAULT allows INSERTs to omit tenant_id and implicitly attribute rows to tenant id 1 (admin), violating the rule that every INSERT must explicitly pass tenant_id.
  - _Fix:_ Remove the DEFAULT and require explicit tenant_id on INSERT (e.g., tenant_id INTEGER NOT NULL with no DEFAULT). Migrate existing rows as needed and audit callers to ensure every insert/upsert supplies tenantId. If a system/global row is required, provide an explicit scope column or separate global table rather than using a default tenant id.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not constrain the write to the tenant (no tenant_id in WHERE). This can modify another tenant's row if id collisions or guesses occur.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId} (or pass safeTenantId) to ensure the update only affects the caller's tenant rows.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} is missing a tenant_id constraint. The code selected desks by tenant earlier but the UPDATE does not re-assert tenant ownership before mutating rows.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) to prevent cross-tenant modifications.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant scoping. This updates governance rules by id without verifying tenant ownership.
  - _Fix:_ Include tenant filter in the UPDATE, e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}, or otherwise verify the rule belongs to the tenant before incrementing trigger_count.
- **server/proposal-verifier.ts:245** — persist() updates code_proposals SET verification_status/verification_details WHERE id = ${proposalId} without scoping by tenant_id. This mutates a tenant-scoped table by id only.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or ensure persist receives and uses the verified tenantId to prevent updating other tenants' proposals.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift the UPDATE code (e.g. setting status='rejected' / validation_result) uses WHERE id = ${proposalId} with no tenant constraint — writes back to code_proposals by id only.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or otherwise ensure the proposal belongs to the tenant before persisting rebase outcomes.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes to a tenant-scoped table without a tenant_id WHERE clause.
  - _Fix:_ Include tenant filtering (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or require/validate tenant ownership before persisting the rebased diff.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id} updates a tenant-scoped table by id only with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) so the write cannot affect another tenant's row.
- **server/research-engine.ts:400** — DELETE from agent_knowledge without tenant scoping (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will remove entries for all tenants, not just the invoking tenant.
  - _Fix:_ Always include tenant_id in DELETE predicates (e.g. DELETE ... WHERE tenant_id = ${session.tenantId} AND source = 'autoresearch' AND expires_at < NOW()). If this is intended as a global maintenance operation, make it an explicit admin job and consider iterating per-tenant to avoid accidental cross-tenant data removal.
- **server/routes/projects.ts:185** — INSERT into project_notes does not set tenant_id. tenant-scoped tables require tenant_id to be passed explicitly on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT: INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId}) and/or validate that the provided tenantId matches the project's tenant before inserting.
- **server/routes/projects.ts:224** — INSERT into project_files (base64 upload) does not include tenant_id. Tenant-scoped records must include tenant_id on insert.
  - _Fix:_ Add tenant_id to the INSERT: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (${projectId}, ..., ${tenantId}) or otherwise ensure tenant_id is explicitly persisted from the authenticated context.
- **server/routes/projects.ts:292** — INSERT into project_files (multipart upload) does not include tenant_id. Tenant-scoped records must include tenant_id on insert.
  - _Fix:_ Add tenant_id to the INSERT: INSERT INTO project_files (..., file_size, tenant_id) VALUES (..., ${file.size}, ${tenantId}) to ensure the row is scoped to the caller's tenant.
- **server/seed.ts:2163** — Unscoped UPDATE on tenant-scoped table research_programs: UPDATE research_programs SET model = ... WHERE id = ${p.id} lacks tenant_id constraint — this can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or perform the update via Drizzle with both eq(research_programs.id, p.id) and eq(research_programs.tenantId, tenantId). Never update by id alone when table is tenant-scoped.
- **server/seed.ts:2516** — Unscoped read from tenant-scoped table conversations: SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id filter and is followed by inserts that may create cross-tenant links (project_conversations).
  - _Fix:_ Scope the SELECT to the caller tenant (e.g. WHERE c.tenant_id = ${tenantId}). Ensure any subsequent writes that associate conversations with projects include tenant_id or only act on rows owned by the caller. Consider making project_conversations tenant-scoped (add tenant_id) and include it on INSERT to prevent cross-tenant linking.
- **server/seed.ts:3771** — DELETE FROM tenant-scoped table `heartbeat_tasks` has no tenant_id restriction — will delete matching rows across all tenants.
  - _Fix:_ Add an explicit tenant filter to the DELETE (e.g. WHERE tenant_id = <tenant> AND enabled = false AND ...) or otherwise restrict the operation to the system tenant (tenant_id = 1) if that is the intent. Prefer Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)).
- **server/seed.ts:3799** — UPDATE on tenant-scoped table `memory_entries` does not include tenant_id in WHERE, causing changes to memory entries across all tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = <tenant>) or run the update scoped to the system tenant explicitly. Use Drizzle's .where(eq(memoryEntries.tenantId, tenantId)) to enforce the invariant.
- **server/seed.ts:3825** — UPDATE on tenant-scoped table `conversations` (gpt-4.1→gpt-5 backfill) lacks a tenant_id constraint and will update conversations across all tenants.
  - _Fix:_ Scope the UPDATE to a tenant (e.g. WHERE model = 'gpt-4.1' AND tenant_id = <tenant>) or otherwise iterate per-tenant. Use Drizzle .where(eq(conversations.tenantId, tenantId)).
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` performed without enforcing tenant_id when tenantId is omitted. detectStagnation builds conditions with category only if no tenantId is provided, leading to cross-tenant reads.
  - _Fix:_ Fail closed or require tenantId. Always include a tenant constraint in the WHERE (e.g. .where(and(eq(experiments.category, category), eq(experiments.tenantId, tenantId)))) or throw if tenantId is undefined. Do not execute a global SELECT on experiments without explicit admin intent and authorization.
- **server/self-improvement.ts:511** — getExperimentHistory can return experiments across all tenants when tenantId is not provided — the code falls back to db.select().from(experiments) with no tenant filter, causing cross-tenant data disclosure.
  - _Fix:_ Require and validate a tenantId parameter for this API, or enforce tenant scoping in the query. If unauthenticated/admin access is intended, gate it with an explicit admin check. Otherwise add WHERE tenant_id = <tenantId> (or Drizzle eq(experiments.tenantId, tenantId)).
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table `experiments` uses only the experiment id in WHERE (where(eq(experiments.id, experiment.id))). This allows updating rows from other tenants by id (no tenant_id constraint).
  - _Fix:_ Include tenant scoping in UPDATEs. e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) or fetch the experiment row with tenant check first and then update. Always constrain UPDATE/DELETE by tenant_id to match the caller's tenant.
- **server/self-reflection.ts:366** — UPDATE memory_entries is executed using only id in WHERE (WHERE id = <id>) after finding an existing row. The UPDATE lacks tenant_id restriction, allowing modification of another tenant's memory entry by id.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = <id> AND tenant_id = ${tenantId} (or the Drizzle equivalent). Alternatively, perform the UPDATE using the previously selected row but include the tenant_id check to fail closed if tenantId doesn't match.
- **server/storage.ts:396** — UPDATE on tenant-scoped table `messages` in attachCitations uses WHERE id = ${messageId} with no tenant_id constraint (raw SQL). This allows cross-tenant writes to messages.
  - _Fix:_ Require and validate tenantId for attachCitations and include it in the WHERE clause (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}). Never perform updates to tenant-scoped rows by id only.
- **server/storage.ts:1296** — getAllDataForExport runs multiple SELECTs over tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, etc.) with no tenant filtering — exporting data for all tenants. This is a mass cross-tenant data leak if callable by non-admin contexts.
  - _Fix:_ Restrict this function to a strongly-authenticated admin-only path and require an explicit admin context; or make it take a tenantId and only export that tenant's data. Add explicit checks that prevent accidental use in regular tenant contexts.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without tenant_id constraint (reads custom_tools by name using db.select().from(customTools).where(eq(customTools.name, parsed.name))). This can leak cross-tenant rows or be abused to detect/operate on other tenants' tools.
  - _Fix:_ Scope the lookup to the caller's tenant. Include the tenant filter (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or otherwise require and validate tenant ownership before reading tenant-scoped rows.
- **server/tool-learning.ts:291** — UPDATE against tenant-scoped table `custom_tools` without tenant_id constraint (.where(eq(customTools.id, tool.id))). Updating implementation by id only can modify another tenant's row if ids are misused or attacker-controlled.
  - _Fix:_ Include tenant scoping on updates (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) and/or require tenantId in the call. Validate that the tool row being updated belongs to the expected tenant before performing the update.
- **server/tool-learning.ts:316** — DELETE against tenant-scoped table `custom_tools` without tenant_id constraint (await db.delete(customTools).where(eq(customTools.id, tool.id))). This revert path can delete another tenant's tool row if the id is mis-bound.
  - _Fix:_ Require and use the tenant id in the WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or otherwise ensure ownership before deleting. Change revertToolRow signature to accept tenantId or derive it from the tool row and enforce it in the delete.
- **server/tool-learning.ts:365** — UPDATE against tenant-scoped table `custom_tools` to increment usageCount uses only id in WHERE (.where(eq(customTools.id, tool.id))) with NO tenant_id constraint. This can increment usageCount on another tenant's row if ids are misused.
  - _Fix:_ Scope the update by tenant (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or include and validate tenantId for the updater to ensure the update can only affect rows owned by the tenant.
- **server/tools.ts:6926** — INSERT into tenant-scoped table project_files does not include tenant_id. The raw SQL INSERT INTO project_files (...) VALUES (...) omits tenant_id even though tenant isolation is enforced at the application layer and INSERTs must supply tenant_id.
  - _Fix:_ Include tenant_id in the INSERT. Pass the caller's tenant id explicitly (e.g. add tenant_id column and VALUES (${params._tenantId}) or use a Drizzle insert: await db.insert(projectFiles).values({ tenantId: params._tenantId, projectId: ..., ... }). Ensure assertProjectInTenant still runs before inserting.
- **server/tools/domains/files/handlers.ts:829** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` omits an explicit tenant_id column on a table that is tenant-scoped.
  - _Fix:_ Always supply tenant_id explicitly on INSERTs into tenant-scoped tables. Add tenant_id to the column list and pass the caller's tenant (e.g. `..., tenant_id, ... VALUES (${projId}, ${fileName}, ..., ${stat.size}, ${'agent'}, ${tenantId})`). Alternatively, verify ownership and then set tenant_id = ctx.tenantId when inserting.
- **server/video-job-runner.ts:236** — recoverStaleVideoJobs executes a wide UPDATE on videoJobs to mark stale jobs failed without any tenant_id filter. This can flip jobs across all tenants to 'failed', causing data loss/service disruption for other tenants.
  - _Fix:_ Do not run unscoped updates against tenant-scoped tables. Either run the recovery per-tenant (iterate tenants and include tenant_id in the WHERE), or restrict this operation to an operator/admin context and explicitly include tenant_id in the query. At minimum add AND video_jobs.tenant_id = ${tenantId} (or equivalent) to the WHERE clause, or otherwise ensure the routine is an admin-only job with careful audit and safeguards.

### HIGH (168)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without tenant_id filter (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads metadata across all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant explicitly. If this function is intended to process all tenants, implement a controlled per-tenant loop that queries one tenant at a time rather than doing an unscoped global SELECT.
- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` without tenant constraint (UPDATE agent_activity ... WHERE id = ${activityId}). This can modify another tenant's row by id.
  - _Fix:_ Require the caller to pass tenantId and include it in the WHERE clause (e.g. WHERE id = ${activityId} AND tenant_id = ${tenantId}), or load the row first and validate tenant ownership before updating.
- **server/agent-channels.ts:198** — JOIN to channel_subscriptions omits tenant scoping on the subscriptions table (JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}). cs.tenant_id is not constrained.
  - _Fix:_ Include tenant scoping in the join or WHERE clause (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${params.tenantId}) to ensure subscriptions and messages belong to the same tenant.
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` without tenant constraint (UPDATE agent_evals SET ... WHERE id = ${evalId}). This can update eval rows across tenants by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or fetch-and-validate the eval row's tenant before performing the update.
- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` without tenant filter in retryPendingMinervaRouting (SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ${PENDING_PLAN_MARKER}). This reads rows from all tenants.
  - _Fix:_ Either accept a tenantId and scope the SELECT to that tenant, or iterate tenants explicitly and run the query per-tenant. Avoid wide unscoped SELECTs over tenant-scoped data.
- **server/agentic-engines.ts:451** — SELECT from `heartbeat_logs` without tenant scoping (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10) in runPredictiveEngine — reads logs across tenants.
  - _Fix:_ Scope the query by tenant (WHERE tenant_id = ${tenantId} AND status = 'success') or run per-tenant to avoid cross-tenant data visibility.
- **server/agentic-engines.ts:519** — SELECT from `heartbeat_logs` without tenant scoping (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...) in runOptimizationEngine — reads/aggregates heartbeat logs across tenants.
  - _Fix:_ Restrict the query to the tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise run the aggregation per-tenant; avoid global aggregates over tenant-scoped tables.
- **server/agentic-engines.ts:533** — SELECT from `heartbeat_tasks` without tenant scoping (SELECT name, cron_expression, enabled, last_run_at, run_all FROM research_schedules WHERE tenant_id = ${tenantId} then SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ORDER BY name) — the heartbeat_tasks query lacks tenant_id and will return tasks across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the heartbeat_tasks query or run the selection per-tenant to ensure only the tenant's configured tasks are read.
- **server/agentic-features.ts:638** — UPDATE on tenant-scoped table `outreach_enrollments` without tenant constraint (UPDATE outreach_enrollments SET status = 'completed' ... WHERE id = ${enrollment.id}). The update was performed using a row previously selected with a tenant filter, but the UPDATE itself lacks an explicit tenant_id clause.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the UPDATE WHERE clause (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) to avoid accidental cross-tenant updates if ids are guessed or rows drift.
- **server/agentic-features.ts:671** — UPDATE on tenant-scoped table `outreach_enrollments` without tenant constraint (UPDATE outreach_enrollments SET current_step = ${nextStep}, ... WHERE id = ${enrollment.id}).
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${params.tenantId}) or perform an owner-validated conditional update to ensure the row belongs to the caller's tenant.
- **server/agentic-features.ts:721** — UPDATE on tenant-scoped table `outreach_enrollments` without tenant constraint (UPDATE outreach_enrollments SET reply_classification = ..., WHERE id = ${row.id}).
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or validate ownership before updating to prevent cross-tenant modification.
- **server/agentic/approvals.ts:134** — Bulk UPDATE on tenant-scoped table `agent_approvals` without tenant filter (UPDATE agent_approvals SET status = 'expired' WHERE status = 'pending' AND expiresAt < now). This will expire pending approvals across all tenants in one operation.
  - _Fix:_ Perform expiry per-tenant (accept/derive a tenantId) or include tenant_id = ${tenantId} in the WHERE clause. If the intent is to sweep all tenants, do so by enumerating tenants and running a tenant-scoped update for each to avoid accidental cross-tenant writes.
- **server/agentic/resume-worker.ts:66** — SELECT query reads agent_runs / agent_approvals without constraining tenant_id — the candidate query can return rows for any tenant.
  - _Fix:_ Do not scan tenant-scoped tables across all tenants. Add a tenant filter (WHERE agent_runs.tenant_id = <tenantId> or equivalent) or iterate per-authorized tenant. If this is a global worker that must scan all tenants, ensure the code explicitly and safely scopes processing per-tenant and enforces ownership checks before any state mutations.
- **server/agentic/wake-scheduler.ts:146** — Sub-select that claims due wakes does not constrain by tenant_id: the inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) can select rows for any tenant.
  - _Fix:_ Limit the claim to a specific tenant or iterate tenants and run per-tenant claims, or add tenant_id = <tenant> to the WHERE clause. If this is intended to be multi-tenant system work, ensure the operation runs with explicit system/admin authorization and audit logging; otherwise, restrict the subquery with tenant_id to avoid cross-tenant claiming.
- **server/agentic/task-forces.ts:40** — createTaskForce accepts a projectId from the caller and inserts it into task_forces without first verifying that the referenced project belongs to the caller's tenant.
  - _Fix:_ Before inserting, validate ownership: SELECT the project by id AND tenant_id = p.tenantId (or otherwise assert the project belongs to the tenant). Reject the request if the project isn't found/owned. Alternatively, only accept projectId values resolved server-side after ownership checks.
- **server/auth.ts:1163** — loadSessionsFromDb selects all auth_sessions (SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions) without filtering by tenant. This reads session data across tenants into process memory.
  - _Fix:_ If intended (startup bootstrap), ensure this runs only in a trusted admin context and that the code handling loaded sessions cannot be abused. Otherwise restrict the query to the tenant(s) this process is responsible for, or move to per-tenant loading logic.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge (getWatermark) lacks tenant_id constraint — reads a tenant-scoped table without scoping to a tenant.
  - _Fix:_ Include tenant_id in the WHERE clause (or pass tenantId and use Drizzle .where(eq(agent_knowledge.tenant_id, tenantId))). Ensure watermark is scoped per tenant or explicitly document intended global behavior.
- **server/auto-memorize.ts:62** — SELECT from agent_knowledge (setWatermark) lacks tenant_id constraint — may return another tenant's row for update/insert logic.
  - _Fix:_ Restrict the SELECT to the caller's tenant (WHERE tenant_id = ${tenantId}) before deciding UPDATE vs INSERT. Use tenant-aware selection to avoid cross-tenant mutation.
- **server/chat-engine.ts:4333** — Call to storage.attachCitations(...) is made without any tenant context/tenant_id. This may write citation rows (or perform tenant-scoped lookups) without constraining the operation to the caller's tenant.
  - _Fix:_ Ensure tenant scoping is provided. Either pass tenantId (e.g. tenantIdForScope) into storage.attachCitations, or have the function validate ownership by checking the message row's tenant_id before inserting/updating citations. Prefer an explicit tenant_id parameter and server-side verification rather than relying on derived/implicit lookup.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, ...) is invoked using a foreign id (conversationId) but no tenant context/tenant_id is supplied. This writes/records tool-execution data tied to a conversation without proving the caller's tenant owns that conversation.
  - _Fix:_ Pass the tenant id (tenantId or tenantIdForScope) into recordToolExecution, or first fetch/verify that the conversationId belongs to the caller's tenant before recording. Update recordToolExecution API to accept tenantId and enforce ownership checks on the write path to prevent cross-tenant writes/reads.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` with NO tenant_id filter in scanAndEscalate: the query reads all tenants' commitments (SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...).
  - _Fix:_ If this is intended only for platform-admins, require and verify an explicit admin authorization boundary before running cross-tenant queries and document that behavior. Otherwise, scope the query to a tenant (e.g. add `WHERE c.tenant_id = ${tenantId}`) or provide a controlled admin API that requires an explicit tenant parameter and audit logging. Never expose fan-in reads to unprivileged callers.
- **server/data-protection.ts:118** — SELECTing expired soft-deleted conversations without tenant_id constraint (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows for all tenants.
  - _Fix:_ Add a tenant filter (WHERE tenant_id = ${tenantId}) when the operation is intended to run per-tenant, or iterate tenants explicitly and scope each sub-operation. If this is an admin job that must operate across tenants, ensure subsequent destructive statements always include the row's tenant_id and run under an appropriate admin context with strict audit logging.
- **server/data-protection.ts:183** — In backupConversationToDrive the messages SELECT omits tenant_id: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ORDER BY created_at ASC. Although the conversation row was verified to belong to tenantId, the messages query is not constrained, allowing reading another tenant's messages if conversation_id mapping is inconsistent.
  - _Fix:_ Add tenant scoping to message reads, e.g. SELECT ... FROM messages WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} ORDER BY ...; similarly constrain compaction_archives queries to tenant_id.
- **server/data-protection.ts:190** — In backupConversationToDrive the compaction_archives SELECT omits tenant_id: SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} ORDER BY archived_at ASC.
  - _Fix:_ Constrain the query by tenant_id (AND tenant_id = ${tenantId}) to ensure archives belong to the caller's tenant before reading or including them in backups.
- **server/delivery-pipeline.ts:1034** — getDeliveryStats issues an unscoped SELECT from delivery_logs when tenantId is omitted (await db.select().from(deliveryLogs) ), returning rows across all tenants.
  - _Fix:_ Always require an explicit tenant context or enforce an authorization check for global admin use. Prefer scoping the query with WHERE tenant_id = ${tenantId} (or disallow calling without tenantId).
- **server/delivery-pipeline.ts:1049** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId without any tenant_id constraint (.where(eq(deliveryLogs.stripePaymentId, paymentId))). This can leak another tenant's delivery row for the same payment id.
  - _Fix:_ Add a tenant parameter (or resolve tenant from a trusted source) and include eq(delivery_logs.tenant_id, tenantId) in the WHERE clause before returning the row.
- **server/dfy-intake.ts:93** — submitDfyIntake UPDATE on dfy_intake_forms sets responses/status using WHERE id = ${form.id} only — no tenant_id constraint. This can allow updating a form across tenants if ids collide or are used incorrectly.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(dfy_intake_forms.id, form.id), eq(dfy_intake_forms.tenant_id, form.tenantId)))) or otherwise assert the form belongs to the expected tenant before persisting responses.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with no tenant_id constraint. This can delete chunks belonging to other tenants that share collection_id/doc_path values.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${tenantId}) or acquire and validate the collection's tenant before deleting chunks.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings issues UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} without any tenant_id constraint. Updating by id only risks cross-tenant modification if ids are mis-used.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update via a previously-scoped transaction/handle that asserts the row belonged to the tenant selected earlier.
- **server/email.ts:518** — backfillEmptyBodies updates inbox_messages SET body_text/body_html WHERE id = ${row.id} without a tenant_id constraint. Updating by id only can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update via an earlier per-tenant-scoped query and validate ownership before the write.
- **server/embeddings.ts:684** — UPDATE to tenant-scoped tables (memory_entries / agent_knowledge) performed without constraining by tenant_id. storeEmbeddingVec() updates rows by id only (no tenant_id check).
  - _Fix:_ Require and pass an explicit tenantId to storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Validate the caller is authorized for that tenant before performing writes.
- **server/felix-brain.ts:19** — Query selects rows from tenant-scoped table 'agent_runs' without scoping by tenant_id (no WHERE tenant_id = ...)
  - _Fix:_ Restrict the query to a specific tenant or iterate per-tenant. Add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or run the rehydration in per-tenant context so you never read other tenants' rows.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} but does not include tenant_id = ${tenantId}, allowing cross-tenant updates if runId belongs to another tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) or perform the update inside a tenant-bound transaction/context to ensure only the owning tenant's row is modified.
- **server/felix-loop.ts:690** — Error-path UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} without tenant_id = ${tenantId}, risking cross-tenant writes when recording failures.
  - _Fix:_ Constrain the UPDATE with the tenant id (WHERE id = ${runId} AND tenant_id = ${tenantId}) or otherwise ensure the update runs in a tenant-bound context so only the originating tenant's row is updated.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) without constraining by tenant_id. Reading a foreign project id without tenant-scoped WHERE risks exposing another tenant's project.
  - _Fix:_ Constrain the SELECT by tenant (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise resolve the project within an authenticated tenant context before using it.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) but omits tenant_id. This can update a project belonging to a different tenant.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or perform the update only after verifying ownership and in a tenant-bound context.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) lacks tenant scoping — disabling tasks by id only can affect tasks for other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. AND tenant_id = ${tenantId}) or perform the disable against the rows selected earlier joined with tenant_id to ensure only the intended tenant's tasks are modified. If this is an admin operation, require explicit, auditable admin privileges.
- **server/heartbeat-context.ts:182** — Call to storage.getPersonas() without a tenant argument — may return personas across tenants and leak cross-tenant persona metadata into the task prompt/context.
  - _Fix:_ Use a tenant-scoped API (e.g. storage.getPersonas(tenantId)) or filter returned personas by tenantId. Ensure storage.getPersonas supports scoping and that this function only includes personas belonging to the current tenant in prompts and delegations.
- **server/heartbeat-context.ts:136** — Call to storage.getProviderKeys() without tenant scoping. Provider keys may be tenant-specific: this call could expose other tenants' provider key metadata to the task context.
  - _Fix:_ Pass tenantId to storage.getProviderKeys (e.g. storage.getProviderKeys(tenantId)) or otherwise ensure only keys for the current tenant are returned. If provider keys are global by design, document that clearly and ensure no sensitive key material is leaked into prompts.
- **server/heartbeat.ts:53** — SELECT from research_schedules has no tenant_id filter (SELECT * FROM research_schedules ...). This reads rows across all tenants without constraining to a caller/owner tenant.
  - _Fix:_ If research_schedules is tenant-scoped, include tenant filtering (e.g. WHERE tenant_id = ${schedTenant} or query per-tenant). Alternatively enumerate tenants explicitly and query per-tenant. Always assert/derive the tenant context before selecting tenant-scoped rows.
- **server/heartbeat.ts:102** — UPDATE statements on research_schedules set next_run_at/last_run_at use WHERE id = ${sched.id} without verifying tenant (no tenant_id in WHERE). This can update another tenant's row if IDs are abused or mis-matched.
  - _Fix:_ Add tenant guard to the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}), or use an application helper that enforces tenant ownership when updating tenant-scoped tables.
- **server/heartbeat.ts:378** — SELECT from event_log (SELECT * FROM event_log WHERE status = 'pending' ...) has no tenant_id constraint. This reads pending events across tenants and processes them without prior per-tenant scoping/validation.
  - _Fix:_ Either query pending events per-tenant or add tenant_id to the WHERE clause. Ensure any UPDATE/processing of event_log rows also includes tenant_id in the WHERE to avoid cross-tenant modifications.
- **server/heartbeat.ts:265** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executes without a tenant_id constraint. heartbeat_tasks is tenant-scoped but the UPDATE could affect rows belonging to other tenants if the id is misused.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use storage/update helpers that assert tenant context before issuing SQL.
- **server/heartbeat.ts:270** — Second normalization UPDATE on heartbeat_tasks (SET model = 'gemini-2.5-flash' WHERE id = ${t.id}) lacks tenant scoping; this writes to a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Same fix: add AND tenant_id = ${t.tenantId} or use the storage layer that enforces tenant ownership when updating tenant-scoped rows.
- **server/heartbeat.ts:1534** — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id predicate. heartbeat_logs are tenant-scoped but the query returns global results.
  - _Fix:_ Restrict the query to the task's tenant (e.g. WHERE tenant_id = ${tenantId} AND task_name = ...) or otherwise scope the lookup to the asserted tenant context before reading tenant-scoped logs.
- **server/heartbeat.ts:2628** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant constraint after creating a new task. If ids collide or are mis-bound, this can touch another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update through a storage helper that enforces tenant ownership/validation.
- **server/heartbeat.ts:3079** — Another occurrence: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) lacks tenant_id in the WHERE clause, permitting cross-tenant updates.
  - _Fix:_ Same as above — add tenant_id to the WHERE clause or use a tenant-aware storage API to update approval_status.
- **server/internal-resolver.ts:184** — finalizeEvent called without passing tenantId (UPDATE event_log executes with no tenant constraint). This will update event_log rows across tenants when the function falls back to the branch that lacks tenant scoping.
  - _Fix:_ Always scope the finalizeEvent UPDATE to the event's tenant. Either (A) derive the tenant inside finalizeEvent (SELECT tenant_id FOR id) and include AND tenant_id = <tenantId> in the WHERE clause, or (B) require callers to pass tenantId (and ensure callers fetch the event's tenant before calling). Never run the UPDATE without a tenant_id filter.
- **server/internal-resolver.ts:229** — finalizeEvent called without passing tenantId (UPDATE event_log executes with no tenant constraint). This archives an orphan delivery event but does not constrain the update to the event's tenant.
  - _Fix:_ Same fix as above: ensure finalizeEvent is always executed with an explicit tenant_id WHERE clause (derive tenant_id if necessary) so an orphan event resolution cannot modify another tenant's rows.
- **server/internal-resolver.ts:259** — finalizeEvent called without tenantId when finalizing unknown/unsupported events. The UPDATE may affect rows across tenants because no tenant filter is supplied.
  - _Fix:_ Ensure every call to finalizeEvent passes the event's tenantId, or make finalizeEvent read the event's tenant_id itself and enforce AND tenant_id = ${tenantId} in the UPDATE. Refuse to write when tenant is unknown.
- **server/job-queue.ts:98** — claimDueJobs performs UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status='pending' ... LIMIT ${limit} FOR UPDATE SKIP LOCKED) without any tenant_id constraint. This reads/updates agent_jobs across tenants in one statement.
  - _Fix:_ If the worker must be tenant-scoped, add tenant_id = ${tenantId} to the inner SELECT/outer UPDATE. If cross-tenant processing is intended, make the operation explicit and ensure the caller is privileged (admin worker) and authorization is enforced. Prefer per-tenant claiming or include tenant parameter to avoid accidental cross-tenant claims.
- **server/job-queue.ts:134** — reclaimExpiredLeases updates agent_jobs rows (both terminal and requeue passes) without any tenant_id filter, touching running/pending jobs across tenants.
  - _Fix:_ Scope these UPDATEs by tenant_id when reclaiming per-tenant leases, or require an explicit admin-only operation with proper authorization. Prefer separate per-tenant reclaim APIs or pass tenantId into the function and add WHERE tenant_id = ${tenantId}.
- **server/job-queue.ts:177** — completeJob updates agent_jobs SET status='succeeded' WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint — a late/slow worker could mark another tenant's job as succeeded.
  - _Fix:_ Add tenantId to the function signature (or otherwise derive the tenant) and include AND tenant_id = ${tenantId} in the WHERE clause. Use the tenant-scoped WHERE to enforce lease-fencing safely.
- **server/job-queue.ts:220** — failJob does a SELECT max_attempts FROM agent_jobs WHERE id = ${id} with no tenant constraint, allowing observing another tenant's job parameters.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT (and change API to accept tenantId where appropriate), or otherwise ensure the call is authorized for cross-tenant access. Subsequent UPDATEs in failJob must also be tenant-scoped.
- **server/job-queue.ts:237** — failJob UPDATEs agent_jobs to failed_terminal or pending without any tenant_id WHERE clause — this can mutate jobs belonging to other tenants.
  - _Fix:_ Require and use tenant_id in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}). If cross-tenant updates are intentional, make them explicit and enforce admin-only authorization.
- **server/job-queue.ts:273** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint, allowing cancellation of jobs belonging to other tenants.
  - _Fix:_ Add tenant scope to the operation (e.g. require tenantId and add AND tenant_id = ${tenantId}) or restrict this API to privileged/admin callers with explicit checks.
- **server/job-queue.ts:283** — retryJob resets attempts/status on agent_jobs WHERE id = ${id} AND status IN (...) with no tenant_id constraint, enabling cross-tenant mutation.
  - _Fix:_ Require tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or implement authorization to ensure only permitted actors can retry cross-tenant jobs.
- **server/job-queue.ts:320** — listJobs constructs and emits a SELECT FROM agent_jobs without a tenant_id constraint if the caller does not pass tenantId. That returns/reads job rows for all tenants by default.
  - _Fix:_ Make tenantId mandatory for non-admin callers, or ensure the function enforces caller authorization when tenantId is omitted. Prefer explicit scoping: include WHERE tenant_id = ${tenantId} unless an admin privilege check passes.
- **server/job-queue.ts:385** — getJobStats runs an aggregate SELECT on agent_jobs WHERE created_at > ... with no tenant_id filter, returning cross-tenant counts.
  - _Fix:_ Add an optional tenantId parameter and scope the aggregation to that tenant (WHERE tenant_id = ${tenantId}) or force admin-only invocation for global stats. Never run cross-tenant aggregates in unprivileged contexts.
- **server/lib/agent-trace.ts:129** — UPDATE to tenant-scoped table agent_trace_spans uses WHERE span_id = ${spanId} with no tenant_id predicate. This allows an UPDATE to affect another tenant's row if span_id collisions or misuse occur.
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Modify persistSpanClose to accept or resolve the current tenant (pass currentTenantId() from callers or include tenantId in the span bookkeeping) so every UPDATE is explicitly scoped to the owning tenant.
- **server/lib/bwb-job-progress.ts:144** — UPDATE on tenant-scoped table video_jobs uses .where(scope(jobId,...)) where scope can omit tenant_id when BWB_TENANT_ID threading is missing. That results in writes that are only pinned by jobId (no tenant predicate), enabling cross-tenant modification.
  - _Fix:_ Make tenant scoping mandatory — require and validate a tenantId parameter (or fail when env BWB_TENANT_ID is absent) before performing any UPDATE. Ensure scope() always includes eq(videoJobs.tenantId, tid) (and abort if tid is null) so every UPDATE includes tenant_id in its WHERE clause.
- **server/lib/bwb-job-progress.ts:169** — UPDATE on tenant-scoped table video_jobs (updateBwbChapters) calls .where(scope(...)) but scope may omit tenant_id if BWB_TENANT_ID is not set, allowing cross-tenant writes keyed only by jobId.
  - _Fix:_ Require the tenantId be present and add an explicit tenant_id predicate to the WHERE clause. If env threading can be missing, fail closed (throw/return) rather than updating without tenant scoping.
- **server/lib/bwb-job-progress.ts:203** — SELECT ... FROM video_jobs uses .where(scope(jobId)) but scope may omit tenant_id when BWB_TENANT_ID threading is missing, causing cross-tenant reads (and subsequent writes based on that read) to occur.
  - _Fix:_ Ensure scope() always includes tenant predicate (eq(videoJobs.tenantId, tid)) and fail/abort when tenant id is not available. Alternatively change callers to pass an explicit tenantId and use it in the WHERE to guarantee tenant-scoped reads.
- **server/lib/bwb-job-progress.ts:211** — UPDATE ... WHERE scope(jobId) used to mark job done; scope may omit tenant predicate resulting in an unscoped UPDATE against video_jobs (cross-tenant risk).
  - _Fix:_ Require tenantId and include eq(videoJobs.tenantId, tenantId) in the WHERE clause. If tenantId is not known, do not perform the UPDATE — fail closed or log and skip, rather than updating without tenant scoping.
- **server/lib/bwb-job-progress.ts:230** — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) used by bumpBwbHeartbeat can run without a tenant_id predicate if BWB_TENANT_ID is missing, allowing cross-tenant row updates.
  - _Fix:_ Make envTenantId required for heartbeat writes or accept tenantId as an explicit function parameter. Always include tenant_id in the WHERE and abort if tenant cannot be determined to prevent cross-tenant writes.
- **server/lib/bwb-job-progress.ts:254** — UPDATE video_jobs .where(scope(jobId, { notDone: true })) in failBwbJob may omit tenant_id when BWB_TENANT_ID is not set, permitting cross-tenant modifications keyed only by jobId.
  - _Fix:_ Require tenant context and include eq(videoJobs.tenantId, tenantId) in the WHERE. If the tenant id is unknown, do not perform the UPDATE; instead return/raise so the operation cannot affect other tenants.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT from tenant-scoped table `project_files` without constraining by tenant_id. The query checks only project_id and file_name, but per invariant every read against a tenant-scoped table must include WHERE tenant_id = <caller tenant> (or equivalent).
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE project_id = ${newProjectId} AND file_name = ${f.fileName} AND tenant_id = ${tenantId}) or JOIN to projects and assert the project's tenant_id matches before reading. Ensure all selects against tenant-scoped tables include the caller's tenant id.
- **server/lib/ideabrowser-backfill.ts:116** — SELECT from tenant-scoped table `project_notes` without constraining by tenant_id. The query tests project_id and note but omits a tenant_id filter, violating the tenant isolation rule for reads.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE project_id = ${newProjectId} AND note = ${n.note} AND tenant_id = ${tenantId}) or verify ownership via a join to projects and assert the project's tenant_id matches the caller's tenant. Ensure all SELECTs against tenant-scoped tables include tenant_id.
- **server/lib/mcp-api-keys.ts:69** — String-built SQL array literal from caller-derived 'scopes' (scopesLit) is interpolated into a query (VALUES (... ${scopesLit}::text[], ...)). Constructing a SQL literal from user input risks injection and bypassing DB-level parameterization/isolation.
  - _Fix:_ Do not serialize a text[] literal manually. Use parameterized array binding / Drizzle's array handling or pass scopes as a proper parameter (e.g. use a parameterized ARRAY or jsonb column and cast on the server side). Ensure user-provided elements are passed as bound parameters rather than concatenated into SQL text.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without constraining by tenant_id (CTE + subsequent UPDATE sets rows to 'publishing' across all tenants).
  - _Fix:_ Constrain the selection/update to a specific tenant (e.g. add AND tenant_id = ${someTenantId}) or iterate per-tenant under an audited admin context. If the runner must legitimately process across tenants, make that explicit (require an admin service-account authorization) and add an explicit tenant_id check/audit in the query to avoid accidental cross-tenant action.
- **server/lib/scheduled-post-runner.ts:134** — UPDATE on tenant-scoped table `scheduled_posts` (marking blocked by quality gate) uses WHERE id = ${row.id} with NO tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) to ensure the update only affects the intended tenant's row. Alternatively, perform the update inside a transaction that first locks/verifies tenant ownership.
- **server/lib/scheduled-post-runner.ts:217** — UPDATE on tenant-scoped table `scheduled_posts` (writing publish results) uses WHERE id = ${row.id} with NO tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${row.tenant_id} to the WHERE so only the intended tenant's row is mutated. Prefer parameterized tenant filters for all writes to tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:244** — Error-path UPDATE on tenant-scoped table `scheduled_posts` uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id — allows cross-tenant mutation on exception paths.
  - _Fix:_ Include tenant_id in the WHERE (AND tenant_id = ${row.tenant_id}) when releasing/marking rows in error paths. Ensure all error/recovery code paths maintain the same tenant-scoped WHERE protections as the normal path.
- **server/lib/system-probes.ts:163** — COUNT(*) query against tenant-scoped messages table without a tenant_id constraint — aggregates messages across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add WHERE tenant_id = ${tenantId} or use Drizzle .where(eq(messagesTable.tenantId, tenantId))). Pass tenantId into checkSystemStatus or read it from the tenant context (currentTenantId()) and fail/handle null explicitly.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table tenant_voice_profiles uses only id in the WHERE clause (where id = existing.id) and lacks an explicit tenant_id constraint.
  - _Fix:_ Include the tenant_id constraint in the WHERE clause to ensure the row belongs to the caller's tenant (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Always constrain tenant-scoped updates/deletes/reads by tenant_id, even when using a previously-fetched id.
- **server/memory-graph.ts:81** — UPDATE to tenant-scoped table memory_categories omits tenant_id in WHERE clause (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}).
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) to enforce tenant isolation and avoid touching another tenant's row.
- **server/memory-graph.ts:140** — refreshCategoryCount performs tenant-unguarded operations: the inner SELECT counting memory_entries lacks tenant_id and the outer UPDATE to memory_categories lacks tenant_id (both in the same SQL block).
  - _Fix:_ Include tenant scoping on both sides: in the inner COUNT add AND tenant_id = ${tenantId} (or pass tenantId into refreshCategoryCount) and on the outer UPDATE add AND tenant_id = ${tenantId} so only the intended tenant's category row is affected.
- **server/memory-graph.ts:196** — SELECT against memory_links does not include tenant scoping (SELECT 1 FROM memory_links WHERE ...). memory_links appears to be tenant-scoped, so this read can return rows from other tenants.
  - _Fix:_ Add tenant filtering or join to a tenant-scoped memory_entries row (e.g. JOIN memory_entries ... AND me.tenant_id = ${tenantId}) or include tenant_id = ${tenantId} in the WHERE clause to limit the check to the caller's tenant.
- **server/memory-graph.ts:409** — UPDATE memory_entries SET category_id = ... WHERE id = ${mem.id} omits tenant_id in WHERE clause. Even though mem was selected earlier with a tenant filter, the UPDATE itself is not tenant-scoped.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE's WHERE clause (or include the tenant check in the transaction) to ensure the update cannot affect another tenant's row.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (from classifyMemoryCategory callback) omits tenant_id in WHERE clause.
  - _Fix:_ Constrain the UPDATE with AND tenant_id = ${tenantId} (pass tenantId into this callback or use a tenant-scoped transaction) so category updates cannot cross tenant boundaries.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in update flow) omits tenant_id in WHERE clause.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE so it only updates rows belonging to the caller's tenant; ensure tenantId is available in scope.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} omits tenant_id. knowledge_triples is tenant-scoped in other queries, but this UPDATE does not constrain by tenant.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or join to a tenant-scoped row) to ensure only the intended tenant's triple rows are modified.
- **server/minds-engine.ts:327** — SQL uses a string-interpolated array constructed from caller-supplied eventIds: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]). Building SQL array literals via string interpolation with request-derived values can lead to injection/isolation risks.
  - _Fix:_ Use a parameterized array binding instead of interpolating into an SQL literal. For example, pass eventIds as a bound parameter (WHERE id = ANY(${sql.array(eventIds, 'int')})) or otherwise bind the array safely; always validate eventIds and keep tenant_id = ${tenantId} in the clause.
- **server/minerva-planner.ts:214** — INSERT into mind_tickets builds an array literal for depends_on via string interpolation from params.dependsOn: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. This composes SQL fragments from caller-controlled input.
  - _Fix:_ Avoid constructing SQL array literals by string concatenation. Instead bind the array as a parameter (e.g. use a proper parameterized int[] binding or Drizzle's array support) and validate that dependsOn contains only numeric ids. Always include tenant scoping on subsequent reads/updates that reference these ids.
- **server/orchestrator-ledger.ts:450** — Using sql.raw(${lockKey}) to embed a value derived from opts.ledgerKey (caller-provided) into SQL: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). This uses sql.raw() with request-derived data and bypasses parameter binding (injection/isolation risk).
  - _Fix:_ Do not use sql.raw() with caller-controlled input. Derive a numeric lock value server-side (e.g. compute and convert to BigInt) and pass it as a bound parameter: const lockBigInt = BigInt(ledgerLockKey(...)); await tx.execute(sql`SELECT pg_advisory_xact_lock(${lockBigInt}::bigint)`); or otherwise ensure the value is fully validated and passed as a parameter rather than raw SQL.
- **server/process-governor.ts:121** — Subquery counts heartbeat_logs without scoping by tenant. The subselects COUNT(*) FROM heartbeat_logs (e.g. lines selecting successes/failures) do not include a tenant_id filter and can return cross-tenant data.
  - _Fix:_ Add tenant scoping to the subqueries, e.g. AND tenant_id = ${tenantId} (or reference the outer tenant param) so heartbeat_logs counts are constrained to the caller's tenant.
- **server/process-governor.ts:155** — Subquery counting heartbeat_tasks for a persona (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id ...) lacks tenant scoping. This may count tasks from other tenants.
  - _Fix:_ Include tenant scoping in the subquery (e.g. AND tenant_id = ${tenantId} or AND tenant_id = ad.tenant_id) so the pending_tasks count is tenant-isolated.
- **server/process-governor.ts:198** — Query computing average response time reads heartbeat_logs but lacks a tenant_id constraint (SELECT AVG(duration_ms) FROM heartbeat_logs ...). This aggregates across tenants.
  - _Fix:_ Restrict heartbeat_logs by tenant: add AND tenant_id = ${tenantId} (or equivalent) to the WHERE clause so avg duration is computed only for the caller's tenant.
- **server/process-governor.ts:240** — cascading_failures query SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... has no tenant_id filter, allowing cross-tenant aggregation.
  - _Fix:_ Add tenant filtering (e.g. AND tenant_id = ${tenantId}) to the heartbeat_logs WHERE clause so distinct persona counts are limited to the tenant.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant constraint; projectId is a foreign id provided to the function and ownership is not verified.
  - _Fix:_ Require and use a tenantId parameter (or otherwise verify the project's tenant_id) and add WHERE id = ${projectId} AND tenant_id = ${tenantId} to ensure the project belongs to the caller's tenant.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... does not filter by tenant_id — it will enumerate projects across all tenants.
  - _Fix:_ Scope discovery by tenant (e.g. add WHERE p.tenant_id = ${tenantId}) or iterate per-tenant explicitly; never perform cross-tenant selects without an explicit system-level justification and guard.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} queries conversations by project_id without any tenant constraint, allowing cross-tenant traversal.
  - _Fix:_ Include tenant scoping (e.g. AND c.tenant_id = ${tenantId} or join via project tenant) and ensure project -> conversation ownership is validated before reading conversations.
- **server/project-brain.ts:360** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks tenant scoping/ownership verification. Messages are being read by conversation id only, which can span tenants if not validated.
  - _Fix:_ Ensure the conversation is confirmed to belong to the expected tenant (e.g. join conversations on tenant_id or pass tenantId) and include a tenant_id constraint or validated conversation ownership before selecting messages.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} does not verify tenant ownership or include tenant_id. The verifier reads proposal rows by id only.
  - _Fix:_ Require tenant context or validate proposal. Add AND tenant_id = ${tenantId} (or fetch the proposal then verify its tenant_id equals the caller's tenant) before proceeding.
- **server/recurring-messages.ts:258** — Runner SELECT: SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500 is executed without a tenant filter and returns rows across tenants.
  - _Fix:_ Either scope the initial query by tenant (preferred) or make this explicit system-only code path obvious and tightly guard it (e.g. require a system flag) — but in any case ensure reads of tenant-scoped tables are deliberate and safe. If global scans are required, document and isolate the code path and enforce per-row ownership checks before any mutation.
- **server/reference-learner.ts:290** — UPDATE against tenant-scoped table memory_entries without tenant_id constraint (UPDATE memory_entries SET ... WHERE id = ${storedId}). This allows modifying a row by id regardless of tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) or otherwise verify ownership before performing the UPDATE. Prefer parameterized Drizzle `.where(eq(table.id, id), eq(table.tenantId, tenantId))` style checks.
- **server/research-engine.ts:165** — UPDATE on research_sessions performed without tenant_id scoping (cleanupZombieSessions uses UPDATE research_sessions ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}). This will mark sessions across all tenants as completed.
  - _Fix:_ Restrict the update to the intended tenant(s). Either run this cleanup per-tenant (AND tenant_id = ${tenantId}) or, if it must run globally, ensure it is executed from an admin-only maintenance path with explicit audit/consent. Prefer parameterized WHERE tenant_id filters when invoked by code that has tenant context.
- **server/routes.ts:2120** — INSERT into tenant-scoped table presenter_sessions uses tenantId taken directly from request body without verifying caller ownership.
  - _Fix:_ Do not accept tenantId from an untrusted request body. Derive tenantId from the authenticated session or internal/trusted server context, or explicitly validate that the caller (or the internal key) is authorized to create data for the supplied tenant. If this route must accept an explicit tenantId (internal tooling), restrict it to a strongly authenticated admin/internal channel and validate the tenant exists and the caller is permitted to act for that tenant.
- **server/routes.ts:1530** — Public endpoint /api/public/stats executes SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memoryEntries, heartbeatLogs) with no tenant_id filter, returning global counts to unauthenticated callers.
  - _Fix:_ Either restrict this endpoint to admin requests or aggregate counts in a safe, pre-computed manner that does not expose per-tenant data. If per-tenant stats are required, add WHERE clauses using the caller's tenant_id or require an admin auth check before returning cross-tenant data.
- **server/routes.ts:1558** — Public endpoint /api/public/architecture issues multiple SELECTs (personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) with no tenant_id scoping, exposing cross-tenant counts and lists to unauthenticated callers.
  - _Fix:_ Avoid querying tenant-scoped tables without scoping to the caller's tenant. Either require admin authentication for this endpoint, return only global-safe information, or restrict queries to a specific tenant_id derived from the caller's authenticated context. Consider precomputing and sanitizing public aggregates to avoid ad-hoc cross-tenant reads.
- **server/routes.ts:4331** — INSERT into tenant-scoped table 'project_notes' is missing tenant_id. The code inserts (project_id, note, author) without setting tenant_id, violating the invariant that every INSERT into tenant-scoped tables must explicitly supply tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and bind the caller's tenant (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})). Alternatively use a storage helper that enforces tenant scoping and/or validate ownership of the project before inserting.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant: e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId} (or use conv.tenantId). Prefer using the conversation's tenant from storage to avoid TOCTOU races.
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Fix:_ Explicitly set tenant_id on INSERT: INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}). Ensure tenantId is validated and not derived from untrusted input.
- **server/routes/conversations.ts:229** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ..., 'system:archive')).
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})). Even if project ownership was checked earlier, the tenant_id must be persisted explicitly to enforce isolation at the application SQL layer.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` missing tenant_id predicate. Code runs: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` — this will flip the is_read flag for a message by id without constraining to the caller's tenant, enabling cross-tenant mutation.
  - _Fix:_ Include the tenant predicate in the UPDATE (or use the Drizzle update API with a tenant equality clause). Example fix: use `WHERE id = ${msg.id} AND tenant_id = ${tenantId}` (or `await db.update(inboxMessages).set({ isRead: true }).where(and(eq(inboxMessages.id, msg.id), eq(inboxMessages.tenantId, tenantId)))`). Verify all DB mutating statements against tenant-scoped tables always include the tenant_id filter.
- **server/routes/projects.ts:33** — Project list query includes dependent subqueries (project_files, project_notes, project_conversations) that do NOT constrain those tenant-scoped tables to the caller's tenant (no tenant_id in the subquery WHERE clauses).
  - _Fix:_ Constrain dependent subqueries to the caller tenant. Example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}). Alternatively join those tables and include AND tenant_id = ${tenantId} to each dependent read to enforce tenant isolation.
- **server/routes/projects.ts:232** — UPDATE projects SET updated_at = ... WHERE id = ${projectId} executes without constraining projects by tenant_id. An UPDATE against a tenant-scoped table must include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId} (or use a parameterized Drizzle eq check) to prevent cross-tenant updates.
- **server/routes/projects.ts:300** — UPDATE projects SET updated_at = ... WHERE id = ${projectId} executes without constraining projects by tenant_id (second upload handler).
  - _Fix:_ Include tenant_id in the WHERE clause: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId} to enforce tenant isolation.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} ... lacks tenant_id constraint. Reads against tenant-scoped project_files must include tenant_id in the WHERE clause even if the parent project was checked earlier.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or join to projects and require p.tenant_id = ${tenantId}) to ensure the returned files belong to the caller's tenant.
- **server/routes/projects.ts:342** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 has no tenant_id constraint. This read can leak rows if project_files contains rows referencing other tenants.
  - _Fix:_ Constrain the query to tenant_id: SELECT ... FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} AND tenant_id = ${tenantId} LIMIT 1.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not include tenant_id. Deletes against tenant-scoped tables must include tenant_id to prevent cross-tenant deletions.
  - _Fix:_ Include tenant_id in the WHERE clause: DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId} (or verify tenant ownership and still include tenant_id defensively).
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without constraining tenant_id (UPDATE ... WHERE id = ${session.id}). This allows cross-tenant modification of sessions.
  - _Fix:_ Include the tenant filter on updates (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or WHERE id = ${session.id} AND tenant_id = ${tenantId}). Alternatively pass tenantId into the update helper and use it in the WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))).
- **server/sculptor.ts:113** — UPDATE sculptor_sessions SET conversation_id ... WHERE id = ${session.id} lacks a tenant_id constraint, permitting cross-tenant writes to session rows.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Ensure any helper that updates sessions always receives and checks tenantId before writing.
- **server/sculptor.ts:168** — UPDATE sculptor_sessions (monitorSession) uses WHERE id = ${sessionId} without tenant scoping. monitorSession is passed tenantId but the UPDATE does not enforce it, enabling cross-tenant state changes.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or load+verify session ownership earlier). Prefer: UPDATE sculptor_sessions ... WHERE id = ${sessionId} AND tenant_id = ${tenantId} (or Drizzle .where(eq(table.tenantId, tenantId))).
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} is executed without constraining tenant_id. Even though session was previously read, the UPDATE should still include tenant filtering to prevent cross-tenant writes.
  - _Fix:_ Use the tenant-scoped condition in the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or verify session.tenant_id === tenantId before updating and include the tenant filter in the SQL.
- **server/second-opinion.ts:405** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} lacks tenant scoping. settleFusionReservation can therefore update ledger rows belonging to other tenants if a reservationId is misused.
  - _Fix:_ Require and pass tenantId into settleFusionReservation (or look up the row first) and include AND tenant_id = ${tenantId} in the WHERE clause. Example: UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} AND tenant_id = ${tenantId}.
- **server/second-opinion.ts:424** — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseFusionReservation) does not include tenant_id, allowing cross-tenant ledger modifications.
  - _Fix:_ Ensure reservation release is tenant-scoped: either accept tenantId in releaseFusionReservation or perform a tenant-checked lookup before updating, and include AND tenant_id = ${tenantId} in the WHERE clause to prevent touching other tenants' rows.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter. heartbeat_tasks appears tenant-scoped elsewhere, so this reads tenant-specific data across all tenants.
  - _Fix:_ If this operation is intended to be admin-only and target the ADMIN_TENANT_ID, include tenant_id = ${ADMIN_TENANT_ID}. If it must be global, document that heartbeat_tasks is intentionally global; otherwise scope the query to a tenant or iterate tenants explicitly while verifying admin privileges.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks a tenant_id constraint and will disable delegation tasks across all tenants.
  - _Fix:_ Scope the UPDATE to the intended tenant(s), e.g. WHERE type = 'delegation' AND enabled = true AND tenant_id = ${ADMIN_TENANT_ID}, or otherwise iterate per-tenant with explicit admin authorization. Never run broad tenant-scoped updates without an explicit tenant filter.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant scoping and reads across tenants.
  - _Fix:_ Add tenant_id = ${ADMIN_TENANT_ID} (or other intended tenant) if the action is admin-scoped, or otherwise ensure the query is run per-tenant with proper authorization.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant_id filter and will disable matching tasks for all tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${ADMIN_TENANT_ID} or operate per-tenant after verifying admin privileges. Make the intent explicit in code comments and require an explicit admin context for cross-tenant changes.
- **server/seed-cleanup.ts:59** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (... name ILIKE ... ) executes without tenant_id scoping, reading tenant-scoped task rows across all tenants.
  - _Fix:_ If the script should only affect the admin tenant, add tenant_id = ${ADMIN_TENANT_ID}. Otherwise, perform per-tenant checks with explicit authorization and avoid unscoped selects on tenant-scoped tables.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true lacks tenant scoping and will disable matching tasks for all tenants.
  - _Fix:_ Require an explicit tenant filter (e.g. tenant_id = ${ADMIN_TENANT_ID}) or iterate tenant-by-tenant with proper admin checks. Avoid global updates on tenant-scoped tables without an explicit, auditable admin intent.
- **server/seed.ts:36** — Unscoped read from tenant-scoped table research_sessions: SELECT COUNT(*) FROM research_sessions has no tenant_id filter.
  - _Fix:_ Restrict reads to the caller's tenant. Pass a tenantId and add WHERE tenant_id = ${tenantId} (or use Drizzle: .where(eq(research_sessions.tenantId, tenantId))). Avoid global COUNT(*) on tenant-scoped tables unless intentionally cross-tenant and audited.
- **server/seed.ts:2158** — Unscoped read from tenant-scoped table research_programs: SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE tenant_id = ${tenantId} OR use Drizzle .where(eq(research_programs.tenantId, tenantId))). If you need to operate across tenants, do so explicitly and with audit/review, iterating per-tenant and using an explicit trusted context.
- **server/seed.ts:4011** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} but does not include tenant_id in the WHERE clause; the update is unscoped at the statement level.
  - _Fix:_ Add tenant_id to the WHERE (e.g. WHERE id = ${id} AND tenant_id = <tenant>) to ensure the row belongs to the expected tenant. Prefer Drizzle .where(eq(agentKnowledge.tenantId, tenantId)) in addition to matching id.
- **server/storage.ts:295** — SELECT from tenant-scoped table `messages` without any tenant_id constraint in attachCitationMetadata (raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL). This can leak citations across tenants.
  - _Fix:_ Include tenant scoping when selecting citations (e.g. accept a tenantId parameter and add AND tenant_id = ${tenantId} or join against a tenant-scoped conversation set). Avoid unscoped raw selects on tenant-scoped tables.
- **server/storage.ts:803** — UPDATE memory_entries sets embedding WHERE id = ${id} with no tenant filter (updateMemoryEmbedding). memory_entries is tenant-scoped — this is an unscoped write.
  - _Fix:_ Require tenantId (or otherwise validate ownership) before updating embedding and add tenant filter to the WHERE clause (e.g. AND tenant_id = ${tenantId}).
- **server/storage.ts:815** — UPDATE agent_knowledge sets embedding WHERE id = ${id} with no tenant filter (updateKnowledgeEmbedding). agent_knowledge is tenant-scoped — this is an unscoped write.
  - _Fix:_ Require tenantId (or validate ownership) and include tenant_id in the WHERE clause (e.g. .where(and(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId)))).
- **server/storage.ts:823** — SELECT memory_entries without tenant scoping in getMemoriesWithoutEmbeddings (no tenantId parameter/WHERE tenant_id = ...). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Add tenant scoping (accept a tenantId param and use tenantScope or eq(memoryEntries.tenantId, tenantId)) so the query only returns rows for the caller's tenant.
- **server/storage.ts:828** — SELECT agent_knowledge without tenant scoping in getKnowledgeWithoutEmbeddings (no tenantId parameter/WHERE tenant_id = ...). This reads tenant-scoped rows across tenants.
  - _Fix:_ Require/accept tenantId and add tenant filter (e.g. .where(and(notExpired, eq(agentKnowledge.tenantId, tenantId)))) before returning results.
- **server/storage.ts:838** — UPDATE memory_entries SET status='archived' WHERE ... (archiveExpiredMemories) executes without any tenant filter — a cross-tenant mutation.
  - _Fix:_ Scope archival updates by tenant. Either run per-tenant (accept tenantId) or iterate tenants; include tenant_id = ${tenantId} in the WHERE clause to avoid archiving other tenants' rows.
- **server/storage.ts:854** — UPDATE memory_entries SET status='archived' WHERE ... (archiveStaleMemories) executes without tenant filtering — unscoped write across tenants.
  - _Fix:_ As above, require tenant scoping (tenantId param) and include tenant_id = ${tenantId} in the WHERE to limit impact to a single tenant at a time.
- **server/storage.ts:916** — getHeartbeatTasks returns all heartbeat_tasks when tenantId is undefined (no tenant filter). heartbeat_tasks is tenant-scoped; this reads across tenants.
  - _Fix:_ Require tenantId or, at minimum, fail-closed for HTTP handlers. Add eq(heartbeatTasks.tenantId, tenantId) to the WHERE when tenantId is provided, and avoid returning unscoped rows when no tenantId passed.
- **server/storage.ts:934** — getHeartbeatTask selects heartbeat_tasks WHERE id = ${id} with no tenant constraint. This reads a tenant-scoped row by id only (unscoped read).
  - _Fix:_ Add tenant scoping (accept tenantId or otherwise validate ownership) and include tenant_id in the WHERE clause to ensure the caller can only read their tenant's task.
- **server/storage.ts:959** — updateHeartbeatTask uses WHERE id = ${id} (and conditionally a subselect when tenantId provided). If tenantId is omitted this UPDATE runs unscoped and can modify another tenant's heartbeat_task.
  - _Fix:_ Require tenantId and use a direct equality check (eq(heartbeatTasks.tenantId, tenantId)) in the WHERE. Avoid subselect shims — use explicit tenant equality to fail closed when tenantId is missing.
- **server/storage.ts:965** — deleteHeartbeatTask deletes from heartbeat_tasks with WHERE id = ${id} when tenantId is omitted (no tenant filter). This allows cross-tenant deletion of tasks.
  - _Fix:_ Require tenantId and include tenant filter in the DELETE (e.g. .where(and(eq(heartbeatTasks.id, id), eq(heartbeatTasks.tenantId, tenantId))). Do not execute destructive operations without tenant scoping.
- **server/storage.ts:1019** — claimHeartbeatTasks updates heartbeat_tasks WHERE id = ${id} AND enabled = true ... with no tenant filter. This is an unscoped write on a tenant-scoped table.
  - _Fix:_ Include tenant scoping (e.g. AND tenant_id = ${tenantId}) or ensure the caller supplies and validates tenantId before attempting to claim tasks. Avoid updating tenant-scoped rows by id only.
- **server/storage.ts:768** — createKnowledge inserts into tenant-scoped table agent_knowledge using caller-supplied `data` but does not assert/validate tenantId is present or owned. An INSERT without explicit tenant validation risks missing tenant_id or being set from an untrusted source.
  - _Fix:_ Validate tenantId explicitly (e.g. assertValidTenantId(data.tenantId)) or require the caller to pass tenantId separately after server-side ownership checks. Always ensure tenant_id is set and trusted before inserting.
- **server/storage.ts:530** — createMemoryEntry inserts into memory_entries without validating that data.tenantId is present/authorized. This allows accidental unscoped writes if callers omit tenantId or pass an incorrect tenant.
  - _Fix:_ Require and validate tenantId (assertValidTenantId) or make tenantId a mandatory argument provided by server-side code after ownership validation; include tenant_id explicitly in the INSERT values.
- **server/storage.ts:1445** — supersedeAdr updates architecture_decisions with WHERE id = ${oldId} and WHERE id = ${newId} but does not include tenant_id in the UPDATE WHERE clauses. These are unscoped updates on a tenant-scoped table.
  - _Fix:_ Use explicit tenant scoping in the UPDATE statements (e.g. .where(and(eq(architectureDecisions.id, oldId), eq(architectureDecisions.tenantId, tenantId))). Even if rows were previously validated, always include tenant_id in destructive/mutating WHERE clauses to avoid TOCTOU risks.
- **server/storage.ts:585** — createConversationFact inserts into conversation_facts using caller-supplied `data` but does not explicitly validate tenantId. INSERT may be missing tenant_id or be set from an untrusted source.
  - _Fix:_ Require/validate tenantId (assertValidTenantId or server-side ownership check) before inserting. Ensure tenant_id column is present in the INSERT and trusted.
- **server/storage.ts:239** — getConversationUnscoped performs a SELECT FROM conversations WHERE id = ${id} with NO tenant filter (explicitly unscoped helper). Although logged, this function returns tenant-scoped rows without tenant constraint and can be used incorrectly in handlers.
  - _Fix:_ Keep this helper strictly internal and audited; require callers to explicitly validate the returned row's tenantId before further use. Prefer renaming to make misuse unlikely and, where possible, avoid exposing unscoped fetchers to HTTP handlers.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` using id only (db.select().from(customTools).where(eq(customTools.id, inserted.id))) with NO tenant_id constraint. Reading a tenant-scoped row by id without tenant scoping can expose or confuse rows across tenants.
  - _Fix:_ Add an explicit tenant filter when fetching by id (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) or derive/require the tenantId and validate ownership before returning the row.
- **server/tenant-fork.ts:232** — UPDATE on a tenant-scoped config table during two-pass self-reparenting uses only the PK in WHERE (.where(eq(entry.table.id, p.newId))) without constraining tenant_id to the new tenant. This update could in theory affect rows outside the intended tenant context if IDs collide or are manipulated.
  - _Fix:_ Include tenant scoping on the reparent update (e.g. .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId)))) so every write is explicitly limited to the target tenant's rows.
- **server/tools.ts:4566** — Querying tenant-scoped table `presenter_sessions` without enforcing tenant isolation. The code runs: SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ... — there is no WHERE tenant_id = <tenant> constraint and no prior ownership assertion for that presentation_id.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. WHERE presentation_id = ${presentationId} AND tenant_id = ${params._tenantId}) or call a server-side ownership-assertion helper (e.g. assertPresenterSessionInTenant(presentationId, tenantId)) before selecting. Ensure every read/write to tenant-scoped tables includes an explicit tenant_id filter or an authoritative ownership check.
- **server/tools.ts:8891** — UPDATE against proposed_skills uses WHERE id = row.id but does not constrain by tenant_id. Updating a tenant-scoped table without a tenant_id WHERE clause can modify another tenant's row.
  - _Fix:_ Add a tenant filter to the UPDATE. Constrain by tenant id in the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or sql`... WHERE id = ${row.id} AND tenant_id = ${tid}`). Use the Drizzle eq helper for safe parameterization.
- **server/tools.ts:8916** — UPDATE on proposed_skills after promotion uses WHERE(id) only and lacks tenant_id constraint. This write can affect rows outside the caller's tenant.
  - _Fix:_ Include tenant scoping in the update WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Prefer using Drizzle's typed update helpers to ensure tenant_id is enforced.
- **server/tools.ts:8933** — UPDATE on proposed_skills to mark 'rejected' uses WHERE id = row.id without tenant constraint. Tenant isolation requirement is not enforced on this write.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or include AND tenant_id = ${tid} in the SQL). Ensure every update/delete on tenant-scoped tables includes an explicit tenant_id filter.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table 'browser_workflows' omits tenant filter — the UPDATE statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` has no WHERE tenant_id = <tenant> constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Include the tenant id in the WHERE clause (e.g. `WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`) or derive the tenant from ctx and use a parameterized Drizzle eq(file.tenantId, tenantId) predicate to ensure tenant isolation.
- **server/tools/domains/files/handlers.ts:833** — UPDATE against tenant-scoped table 'projects' omits tenant filter — `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Include the tenant constraint on the UPDATE (e.g. `WHERE id = ${projId} AND tenant_id = ${tenantId}`) or use an equivalent Drizzle where(eq(projects.tenantId, tenantId)) to ensure the update cannot affect rows belonging to other tenants. If ownership was verified earlier, prefer still adding the tenant predicate to the UPDATE to follow the codebase invariant.
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE against tenant-scoped table knowledge_triples does not constrain by tenant_id (WHERE clause uses only id = ${row.id}). This allows a tenant-scoped row id from one tenant to be updated even if it belongs to another tenant.
  - _Fix:_ Include the tenant filter on the UPDATE to enforce application-level isolation. For example: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}; or use Drizzle/parameterized APIs to always add eq(table.tenantId, tenantId) to WHERE clauses. Enforce a code review/test to require tenant_id in all UPDATE/DELETE statements against tenant-scoped tables.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys executed without constraining to tenant_id. The query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` does not filter by tenant_id and thus may expose provider key rows across tenants (even though executed only for admin tenant in code).
  - _Fix:_ If provider_keys is tenant-scoped, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) and bind the caller's tenantId. If provider_keys is intentionally global, make that explicit in code/comments and ensure only a hardened admin path can call this. Avoid running unscoped selects against potentially tenant-scoped tables.
- **server/tools/domains/security/handlers.ts:260** — COUNT(DISTINCT provider) query against provider_keys lacks a tenant constraint. The admin-only query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` does not include tenant_id scoping and can reveal cross-tenant provider configuration if provider_keys is tenant-scoped.
  - _Fix:_ If provider_keys holds tenant-scoped rows, add `WHERE tenant_id = ${tenantId}` (or equivalent drizzle .where) to scope the count to the caller's tenant. If it's intentionally a global table, explicitly document that and gate this code behind a strictly-controlled admin-only interface so it cannot leak tenant-specific data.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE against tenant-scoped table memory_entries does not constrain by tenant_id (UPDATE memory_entries SET ... WHERE id = ${id}). This permits updating a row by id without also verifying it belongs to the caller's tenant.
  - _Fix:_ Restrict the UPDATE by tenant_id as well (e.g. WHERE id = ${id} AND tenant_id = ${tid}) or perform the UPDATE via a storage layer call that enforces tenant scoping. Always include the caller's tenantId in the WHERE clause for writes to tenant-scoped tables.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table memory_entries uses only id membership (WHERE id = ANY(${ids})) with no tenant_id constraint. IDs were selected earlier under a tenant filter, but the subsequent UPDATE lacks an explicit tenant_id guard and could update rows outside the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}, or perform the update through a storage API that enforces tenant isolation. Never rely solely on prior selection; every write must include an explicit tenant_id constraint.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table memory_entries does not include a tenant_id constraint (UPDATE memory_entries SET ... WHERE id = ${id}). This updates by id alone and lacks explicit verification of tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${id} AND tenant_id = ${tid}, or route the update through a storage helper that enforces tenant ownership. Ensure every read/write on tenant-scoped tables includes tenant_id filtering.
- **server/twilio.ts:102** — SELECT from agent_knowledge (messaging_pairing) executed without constraining by tenant_id. This queries a tenant-scoped table (agent_knowledge) across tenants and relies on subsequent application logic to validate ownership, violating the invariant that every read must include tenant filtering.
  - _Fix:_ Avoid unscoped reads. Restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND title = ${...}) or redesign pairing storage so lookups are tenant-scoped (or move any global/administrative cross-tenant lookup into an explicit operator-only path with strong audit/authorization). If cross-tenant enumeration is absolutely required, make it an auditable operator action, not a normal webhook path.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped table by id only with no tenant_id constraint. This can modify rows belonging to a different tenant if an id is mis-bound.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId}). Adjust the API to accept/derive tenantId (pass it through from the caller) so the UPDATE becomes UPDATE ... WHERE id = ${reservationId} AND tenant_id = ${tenantId} to enforce per-tenant ownership.
- **server/venture-discovery/budget.ts:169** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table by id only with no tenant_id constraint.
  - _Fix:_ Same remediation as settleVentureReservation: require/propagate tenantId into releaseVentureReservation and include AND tenant_id = ${tenantId} in the WHERE clause so the release can only affect that tenant's ledger rows.
- **server/wiring-invariants.ts:244** — SELECT from tenant-scoped table `tool_performance` without any tenant_id constraint (db.execute(sql`... FROM tool_performance ...`)). This reads tenant-scoped data across all tenants.
  - _Fix:_ Constrain the query to an explicit tenant via WHERE tenant_id = <tenant> (or pass ADMIN_TENANT_ID if this must be an admin-scoped check), or iterate tenants and run per-tenant checks. Use parameterized Drizzle filters (e.g. .where(eq(table.tenantId, tenantId))) rather than relying on a cross-tenant SELECT.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM `tool_performance` without tenant_id constraint (db.execute(sql`SELECT count(*)::int AS n FROM tool_performance`)). Reads aggregate across tenants.
  - _Fix:_ If this is intended to be per-tenant, add WHERE tenant_id = <tenant>. If intended as an admin/global metric, explicitly query for the admin tenant (WHERE tenant_id = ADMIN_TENANT_ID) or document and centralize the global-read path. Avoid unscoped reads of tenant tables.
- **server/wiring-invariants.ts:260** — SELECT count(*) FROM `messages` without tenant_id constraint (db.execute(sql`SELECT count(*)::int AS n FROM messages WHERE created_at > ...`)). This counts messages across all tenants.
  - _Fix:_ Limit to a tenant context (WHERE tenant_id = <tenant>) or explicitly run per-tenant aggregates. If you truly need a cross-tenant admin summary, use an explicit admin tenant id or a vetted cross-tenant mechanism and document that exception.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM `tool_performance` without tenant_id constraint (db.execute(sql`... FROM tool_performance ...`)). This reads tenant-scoped rows across tenants.
  - _Fix:_ Scope the query by tenant_id or iterate tenants to produce per-tenant results. If this caller is an admin-only check, explicitly pass ADMIN_TENANT_ID in the WHERE clause to avoid accidental cross-tenant data exposure.
- **server/wiring-invariants.ts:366** — sql.raw SELECT COUNT/MAX FROM `${spec.table}` (e.g. knowledge_nudges, tool_performance) is executed without a tenant_id WHERE clause. Even though table name and column are constants, the SELECT reads tenant-scoped tables across tenants.
  - _Fix:_ Run the freshness probe per-tenant (WHERE tenant_id = <tenant>) or explicitly bound to ADMIN_TENANT_ID for admin intent. Prefer parameterized table access via Drizzle and avoid raw unscoped SELECTs against tenant tables; if raw is required, include a tenant_id predicate.
- **server/wiring-invariants.ts:417** — SELECT from `tool_performance` (tool-drift detector) lacks any tenant_id constraint (db.execute(sql`SELECT ... FROM tool_performance WHERE last_failure_at > ...`)). This aggregates tenant-scoped metrics across all tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = <tenant>) or run the drift check per tenant. If a global view is required for operators, make the admin scope explicit by filtering on ADMIN_TENANT_ID or using a dedicated cross-tenant audit path.
- **server/wiring-invariants.ts:535** — sql.raw SELECT COUNT/pg_total_relation_size FROM ${tbl.name} (memory-growth probe) queries tenant-scoped tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without any tenant_id filter.
  - _Fix:_ Either run these counts per-tenant (adding WHERE tenant_id = <tenant>) or, if intended as an admin/global metric, explicitly bind to ADMIN_TENANT_ID. Replace string-interpolated raw SQL with parameterized Drizzle queries where possible and ensure tenant_id is enforced.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM `graph_memory` executed without tenant_id constraint (db.execute(sql`SELECT COUNT(*)::int AS cnt FROM graph_memory`)). This reads a tenant-scoped table across all tenants.
  - _Fix:_ Scope the query by tenant_id or iterate tenants. If the intent is an admin-wide check, explicitly query the admin tenant (WHERE tenant_id = ADMIN_TENANT_ID) and document the exception to tenant isolation rules.

### MEDIUM (39)

- **server/agentic-engines.ts:345** — SELECT from `heartbeat_logs` without tenant scoping (SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection') inside gatherOperationalData — reads cross-tenant logs.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or make the call explicitly per-tenant to avoid leaking/aggregating other tenants' heartbeat logs.
- **server/auto-asset-capture.ts:184** — Backfill query enumerates conversations across all tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... ) with no tenant filter — a background job will read rows for every tenant without per-tenant scoping or authorization checks.
  - _Fix:_ Run backfill on a per-tenant basis (query tenants then process each tenant separately) or otherwise ensure the job is explicitly authorized as a global maintenance task. If processing all tenants is intended, add clear comments and audit/csrf protections and ensure no tenant context variables are implicitly applied to downstream writes without explicit ownership checks.
- **server/auth.ts:1150** — clearExpiredSessions executes DELETE FROM auth_sessions WHERE expires_at < ${now} without tenant scoping; this is a global cleanup that mutates rows across tenants.
  - _Fix:_ If this is a scheduled global maintenance task, document and run it under an explicitly authorized system context. Otherwise, consider performing per-tenant cleanup (DELETE FROM auth_sessions WHERE tenant_id = ${tenantId} AND expires_at < ...) or ensure the caller is an admin/system process that is permitted to act cross-tenant.
- **server/auth.ts:1049** — cleanExpiredResetTokens runs DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} with no tenant constraint, deleting reset-token rows across all tenants.
  - _Fix:_ Either perform per-tenant cleanup, or ensure this routine is explicitly executed only by an authorized system process and document that it touches global tenant-scoped tables. If tenant-scoped behavior is desired, add tenant_id filtering to the DELETE.
- **server/auto-transcript.ts:128** — Backfill query selects DISTINCT conversations (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...) with no tenant_id WHERE — enumerates conversations across tenants for backfill without scoping.
  - _Fix:_ Require an explicit tenant parameter for backfill operations or add WHERE c.tenant_id = ${tenantId} to the query. If backfill is an admin/global task, ensure it runs with elevated authority and is documented and audited.
- **server/auto-transcript.ts:153** — SELECT DISTINCT joining project_conversations, conversations and projects runs without scoping by tenant and may traverse multiple tenants' data.
  - _Fix:_ Add tenant scoping (e.g. WHERE c.tenant_id = ${tenantId}) or require an explicit tenant parameter for the backfill operation. Ensure joins that assume project/conv tenant alignment still include an outer tenant filter to avoid cross-tenant exposure.
- **server/auto-transcript.ts:171** — Missing-links query (SELECT c.id, c.project_id, c.tenant_id FROM conversations ...) lacks a tenant WHERE clause and iterates across tenants when filling missing links/backfilling.
  - _Fix:_ Scope this query by tenant (WHERE c.tenant_id = ${tenantId}) or restrict execution to an authorized admin context; ensure any writes derived from these rows are performed with correct tenant checks.
- **server/data-protection.ts:369** — In backupTenantDataToDrive the per-conversation message-count check SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} lacks tenant scoping — it can count messages across tenants for the same conversation id.
  - _Fix:_ Add tenant filter to the count query (WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise ensure conv.id was sourced only from tenant-scoped queries and enforce tenant_id in all message reads/writes.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken selects a dfy_intake_forms row by token without constraining by tenant_id. The table is tenant-scoped but lookup is token-only.
  - _Fix:_ If token-only access is intentionally public, document the exception and ensure tokens are cryptographically unguessable and audited. Otherwise require tenant context or include tenant_id in the WHERE clause (or return tenantId along with the form and enforce it for subsequent writes).
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot runs an unscoped SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' with no tenant_id filter — it aggregates across tenants.
  - _Fix:_ If this is intended to be tenant-specific, add tenant_id filtering. If it is intended as a global/admin metric, ensure the code runs only in a trusted admin context and document this exception; otherwise scope queries per-tenant.
- **server/dormant-deprecation.ts:179** — getTrafficSnapshot runs an unscoped SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... with no tenant_id filter — aggregating across all tenants.
  - _Fix:_ Scope the distinct-tools query by tenant (WHERE tenant_id = ${tenantId}) for per-tenant decisions, or ensure the routine is intentionally global and restricted to admin-only execution.
- **server/dormant-deprecation.ts:204** — getEverInvokedTools runs SELECT DISTINCT tool_name FROM tool_performance without tenant scoping, returning tool names across all tenants.
  - _Fix:_ If the computation should be per-tenant, add tenant_id filtering; if intended global/admin, restrict execution to admin context and document the exception.
- **server/dormant-deprecation.ts:214** — getActiveTools14d runs SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... with no tenant_id constraint, returning active tools across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause for tenant-scoped behavior, or ensure this is only run in an admin/global context with proper authorization and documentation.
- **server/email.ts:459** — startInboxPolling checks for existing inbox message by executing SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 with no tenant/inbox scoping. This can match message_id across tenants and cause incorrect 'already stored' decisions.
  - _Fix:_ Include inbox_id or tenant_id in the WHERE clause (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} ) to ensure the lookup is tenant/inbox-scoped.
- **server/email.ts:503** — backfillEmptyBodies SELECTs id/message_id FROM inbox_messages WHERE body_text IS NULL ... with no tenant filter, returning rows across tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant or inbox (WHERE tenant_id = ${tenantId} or inbox_id = ${inboxId}) or iterate per-tenant to avoid cross-tenant processing.
- **server/embeddings.ts:241** — Use of sql.raw() / string-interpolated SQL with a vector literal derived from request/caller input (the embedding produced from the query). The code interpolates '${vec}' into sql.raw() for ORDER BY and SELECT expressions (embedding_vec <=> '...') raising injection/isolation risk per project invariant.
  - _Fix:_ Avoid injecting derived values into raw SQL. Bind the vector via a parameterized API (use proper Drizzle binding or a safe parameter placeholder) or use a safe driver-supported method to pass the vector. If raw is unavoidable, canonicalize and strongly validate (as vecLiteral already does) and include tenant scoping; prefer parameterized queries to eliminate interpolation.
- **server/episode-playbooks.ts:67** — Embedding literal (constructed from generateEmbedding(goal)) is interpolated directly into SQL (SELECT / ORDER BY / INSERT) as a string like ${embeddingLiteral}::vector. The embedding value is derived from caller input (goal) and is embedded into SQL without a parameterized binding, creating injection / isolation risk.
  - _Fix:_ Do not interpolate embedding strings directly into SQL. Use parameterized bindings or a driver-supported vector parameter; or store the embedding as a JSONB / bytea parameter and compute/compare vector operations server-side via safe parameter substitution. Always keep tenant_id in WHERE clauses (these queries do include tenant_id, but the interpolation should still be removed).
- **server/firecrawl.ts:214** — UPDATE against tenant-scoped table 'scraped_pages' uses WHERE eq(scrapedPages.id, existing[0].id) but omits a tenant_id constraint. Even though the prior SELECT included tenant_id, the UPDATE lacks an explicit tenant filter.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) to guard against accidental cross-tenant updates.
- **server/firecrawl.ts:344** — UPDATE against tenant-scoped table 'scraped_pages' (in crawl loop) does .where(eq(scrapedPages.id, existing[0].id)) without also constraining tenant_id, allowing a potential cross-tenant update.
  - _Fix:_ Add tenant scoping to the WHERE clause (and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or otherwise ensure the update executes under the correct tenant context.
- **server/glasses-gateway.ts:172** — db.update(apiKeys)...where(eq(apiKeys.id, key.id)) updates the apiKeys row without constraining by tenant_id. If apiKeys is tenant-scoped, this lacks explicit tenant scoping on the write.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or ensure writes happen inside a tenant-bound context so only the owning tenant's apiKeys row is modified.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (SELECT id,name,created_at,next_run_at FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL ...) has no tenant filter. This may surface tenant-scoped heartbeat tasks from other tenants.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, include WHERE tenant_id = ${tenantId} or otherwise restrict the scan to the intended tenant(s). If this is an intentional global admin sweep, require an explicit admin flag and document it.
- **server/heartbeat.ts:2583** — Calling storage.getHeartbeatTasks() with no tenant filter and then using the returned list for dedupe/cap checks (existingTasks.filter(...) / find(...)) can compare tasks across tenants. The handler uses those results to decide whether to block or allow creating tasks for the current tenant.
  - _Fix:_ Fetch only the current tenant's heartbeat tasks (e.g. storage.getHeartbeatTasks(undefined, tenantId) or add explicit WHERE tenant_id = tenantId) before dedupe/cap checks. Ensure all in-memory checks operate on tenant-scoped data to avoid cross-tenant interference.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor issues SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} which reads agent_knowledge across all tenants (no tenant filter).
  - _Fix:_ If this is intended as an admin/global diagnostic, restrict invocation to an admin-only process and document/authorize it. Otherwise, provide a tenant-scoped API (accept tenantId) and add WHERE tenant_id = ${tenantId} to avoid cross-tenant reads.
- **server/lib/outcome-promotion.ts:158** — Query reads from tenant-scoped table 'deliverable_grade_events' without a tenant_id WHERE clause (aggregates across all tenants). This is a SELECT against a tenant-scoped table with no tenant filter in application-level SQL.
  - _Fix:_ If this is an intentional global/admin scan, document and gate it behind an explicit admin-only execution path and audit logging. If it should be per-tenant, restrict the query to a single tenant (WHERE tenant_id = <tenant>) or iterate tenants and run per-tenant queries so every access to tenant-scoped data is explicit and authorized.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` is performed without a tenant_id WHERE filter (scans all tenants).
  - _Fix:_ If the audit is intended to be multi-tenant admin-scoped, require and enforce a privileged admin/service context and add explicit auditing. Otherwise add tenant_id = ${tenantId} to the WHERE clause or iterate per-tenant to avoid cross-tenant data exposure.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` is performed without a tenant_id WHERE filter (scans all tenants).
  - _Fix:_ Same remediation as for agent_runs: either restrict the query by tenant_id or run under an explicit, audited admin context. Prefer adding AND tenant_id = ${tenantId} when used by non-admin callers.
- **server/persona-export.ts:39** — Query for skills: SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL has no tenant scoping. If skills are tenant-scoped, this will return rows from other tenants.
  - _Fix:_ If skills are tenant-scoped, include tenant filtering (e.g. AND (tenant_id = ${tenantId} OR tenant_id IS NULL) or similar). If skills are intentionally global/shared, add a code comment asserting that and documenting the global nature to avoid confusion.
- **server/research-engine.ts:480** — INSERT into project_notes omits tenant_id in VALUES (INSERT INTO project_notes (project_id, note, author, created_at) ...). For tenant-scoped tables, tenant_id must be set explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)). If project_notes is intentionally keyed by project_id only, document and enforce ownership checks; otherwise ensure tenant_id is persisted.
- **server/research-engine.ts:488** — INSERT into project_notes inside loop omits tenant_id in VALUES (INSERT INTO project_notes (project_id, note, author, created_at) ...). This writes notes without an explicit tenant_id column set.
  - _Fix:_ As above, include tenant_id in the INSERT (INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ${noteContent}, ...)). Ensure all inserts into tenant-scoped tables set tenant_id explicitly to prevent cross-tenant writes.
- **server/routes.ts:765** — Startup backfill selects from projects (tenant-scoped) without a tenant_id WHERE clause: SELECT p.id, p.name, p.tenant_id ... FROM projects p JOIN tenants t ... — this reads all tenants' rows.
  - _Fix:_ If this backfill is intended to act across all tenants by design, run it in a controlled migration/admin context (not request-handling) and ensure it runs with sufficient privileges and audit logging. Otherwise, scope the query per-tenant or add an explicit admin-only guard and document the cross-tenant intent. Prefer running as a DB migration or offline job rather than ad-hoc runtime code when modifying tenant-scoped data.
- **server/routes.ts:2313** — Public capability endpoint /api/store/order/:sessionId queries deliveryLogs (tenant-scoped) with no tenant_id constraint: WHERE orderId = ${sessionId} OR stripePaymentId = ${sessionId}. This reads delivery rows across tenants based solely on session id.
  - _Fix:_ Treat capability tokens as such but be explicit in threat model. If relying on unguessable session IDs is the design, ensure tokens are cryptographically strong and rotate them as needed; otherwise, include tenant ownership checks (e.g., require tenant context or additional proof) before returning rows from tenant-scoped tables. Alternatively make the endpoint admin-only or scope results by tenant_id when available.
- **server/routes/conversations.ts:99** — SELECT from tenant-scoped table 'project_conversations' lacks tenant_id constraint (SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}).
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. AND tenant_id = ${tenantId} (or filter by conv.tenantId). This prevents cross-tenant information leaks.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table 'projects' lacks tenant_id constraint in WHERE clause (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}).
  - _Fix:_ Add tenant scoping to the UPDATE to avoid touching other tenants' rows: WHERE id = ${projectId} AND tenant_id = ${tenantId}.
- **server/seed.ts:3343** — Select from tenant-scoped table `heartbeat_tasks` without restricting by tenant_id (reads all tenants' heartbeat tasks).
  - _Fix:_ Constrain the SELECT to the intended tenant (e.g. .where(eq(heartbeatTasks.tenantId, tenantId)) or add WHERE tenant_id = <tenant>) so you don't read rows belonging to other tenants.
- **server/storage.ts:874** — getMemoryStats may SELECT memory_entries and agent_knowledge without tenant filtering when tenantId is omitted — returning cross-tenant data if caller fails to pass tenantId.
  - _Fix:_ Fail-closed: require tenantId (or explicitly assert if an admin-only function) or otherwise call _warnUnscoped and ensure only admin contexts can call unscoped. Prefer making tenantId a required argument and add eq(...tenantId) filters.
- **server/storage.ts:991** — getDueHeartbeatTasks selects heartbeat_tasks across all tenants (no tenant filter). This reads tenant-scoped tasks without tenant scoping.
  - _Fix:_ If the maintenance worker needs to process tasks across tenants, ensure the worker runs with an explicit admin context and that subsequent per-task operations validate and use the task's tenantId. Otherwise, scope the query per-tenant.
- **server/subagents.ts:248** — spawnSubagentImpl calls storage.getConversation(params.parentConversationId, params.tenantId ?? ADMIN_TENANT_ID) — if params.tenantId is not supplied the code falls back to ADMIN_TENANT_ID. A caller-provided parentConversationId can be read under the admin tenant fallback, risking improper cross-tenant reads/writes if tenantId is omitted.
  - _Fix:_ Require the caller to provide tenantId (fail closed) or derive tenantId server-side from the authenticated request. Avoid defaulting to an admin tenant constant for ownership checks; validate that the caller is authorized to reference the parentConversationId's tenant.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is executed without also scoping by tenant_id. This is a read against a tenant-scoped table with no tenant WHERE-clause.
  - _Fix:_ Verify ownership atomically by checking both id and the claimed tenant in the same query (e.g. SELECT 1 FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1) or otherwise include tenant_id in the WHERE clause so the DB-level read is tenant-scoped before trusting the returned row.
- **server/video-job-runner.ts:208** — SELECT cancelRequested FROM videoJobs WHERE jobId = ${jobId} is performed without constraining by tenant_id. This reads a tenant-scoped row without an explicit tenant filter.
  - _Fix:_ Make the read tenant-scoped: accept/derive tenantId (the runner already has state.tenant_id) and query WHERE jobId = ${jobId} AND tenant_id = ${tenantId} LIMIT 1. Adjust callers (runChaptersInBackground) to pass the owning tenantId so reads are always constrained to the job owner.

### LOW (1)

- **server/auto-consolidation.ts:335** — Global scan query reads DISTINCT tenant_id from memory_entries (SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL) without per-request tenant scoping. While intended for a scheduler, it accesses tenant-scoped rows without an explicit tenant constraint.
  - _Fix:_ If this is meant to discover tenants for a global maintenance task, document that intent and run under an explicit admin/system context. Otherwise, restrict to a single tenant or enumerate tenants from a vetted tenant registry table rather than scanning tenant-scoped data tables directly.

### Suppressed — confirmed false positives (229)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — Bulk UPDATE on tenant-scoped table `ai_insights` without tenant constraint (UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...)). This can change status/action_taken for rows in all tenants in one operation.
  - _Why false positive:_ Platform-wide backfill sweep; processes rows with row-carried tenant_id and only mutates housekeeping status/action fields before per-row tenant routing.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() updates agent_runs without a tenant_id constraint (WHERE only filters by id). This permits cross-tenant modification if called with an arbitrary runId.
  - _Why false positive:_ Server-side worker helper updates agent_runs by globally unique run id created by createRun; not shown crossing an untrusted request boundary.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs using only the run id in the WHERE clause; no tenant_id constraint is applied.
  - _Why false positive:_ Internal run-state worker mutation by server-generated primary key runId, with no evidence of untrusted caller-controlled id.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() updates agent_runs using only the run id in the WHERE clause; no tenant_id constraint is applied.
  - _Why false positive:_ Internal helper replaces run state by server-generated run primary key rather than tenant request input.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs (status/result/completed_at) using only the run id in the WHERE clause; tenant scoping is missing.
  - _Why false positive:_ Completion path updates run by internal server-generated primary key; follow-up read uses same run id and row tenantId.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs (status/error/completed_at) using only the run id in the WHERE clause; no tenant_id constraint is present.
  - _Why false positive:_ Failure-state update is an internal worker operation on a server-created run id, not proven to be attacker supplied.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() updates agent_runs (status/state) using only the run id in the WHERE clause; missing tenant scoping.
  - _Why false positive:_ Pause helper mutates by internal run primary key within agent orchestration flow; no untrusted boundary shown.
- **server/auth.ts:1155** _(system-sweep)_ — clearAllSessions executes DELETE FROM auth_sessions with no WHERE clause, deleting sessions for all tenants (global destructive action).
  - _Why false positive:_ Global session purge/maintenance function intentionally clears all sessions platform-wide; housekeeping only, no tenant data returned or mixed.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on credential_vault missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` — called from getDecryptedPassword(id, tenantId). This mutates a row by id without verifying tenant ownership.
  - _Why false positive:_ getDecryptedPassword first calls getCredential(id, tenantId), which verifies ownership before this last_used_at stamp on the same id in the same call path.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on credential_vault missing tenant constraint: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` — called from getLoginCredentials(siteUrl, tenantId). This updates last_used_at without ensuring the row belongs to the caller's tenant.
  - _Why false positive:_ Row is fetched immediately above via getCredentialForSite(siteUrl, tenantId); update uses that validated row id only for last_used_at housekeeping.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} executes without constraining by tenant_id. Same for DELETE FROM compaction_archives and DELETE FROM conversations (lines 131-133). This allows deleting rows belonging to other tenants if conversation ids collide or earlier SELECT returned rows from other tenants.
  - _Why false positive:_ Platform-wide purge of expired soft-deleted conversations; rows are selected with their own tenant_id and then only housekeeping deletes are performed, with no tenant-facing mixed output.
- **server/embeddings.ts:716** _(system-sweep)_ — Background backfill routines (backfillEmbeddingVecs, backfillMissingKnowledgeEmbeddings) read and UPDATE memory_entries and agent_knowledge without any tenant_id scoping. SELECTs and subsequent UPDATEs operate across all rows which can modify data for other tenants.
  - _Why false positive:_ Background backfill iterates across tenants only to repair embedding/vector fields in place; no tenant data is returned or mixed into one tenant-facing surface.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images is missing tenant_id (INSERT statement does not set tenant_id). If presenter_slide_images is tenant-scoped this allows persisting/overwriting images for sessions belonging to other tenants.
  - _Why false positive:_ presenter_slide_images is referenced via tenant-scoped presenter_sessions and schema note does not show tenant_id on child table, so insert without tenant_id matches transitive isolation design.
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint. Updating by session id alone can modify another tenant's session if ids are guessed or misrouted.
  - _Why false positive:_ Update is on newly created presenter_sessions row identified by server-generated sessionId from same call path, not an untrusted cross-tenant request id.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals has no tenant filter (SELECT id, title, status, created_at FROM code_proposals ...). code_proposals is likely tenant-scoped — this reads stale proposals across tenants.
  - _Why false positive:_ Global health-audit scan intentionally inspects stale proposals across tenants for maintenance findings; this is an admin/housekeeping sweep, not tenant-facing data access.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) has no tenant constraint — this will modify proposals across tenants.
  - _Why false positive:_ Global maintenance job archives stale proposals selected by the sweep; it mutates only status housekeeping fields and does not expose mixed tenant content.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. Platform invariant says every INSERT must explicitly provide tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ Schema says project_files has no tenant_id; child rows are scoped transitively through tenant-scoped projects via project_id.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. Every INSERT into tenant-scoped tables must explicitly supply tenant_id to avoid cross-tenant writes.
  - _Why false positive:_ Schema says project_notes has no tenant_id; child rows are scoped transitively through tenant-scoped projects via project_id.
- **server/memory-graph.ts:210** _(fk-transitive)_ — INSERT into memory_links omits tenant_id entirely (INSERT INTO memory_links (... ) VALUES (...)). Tenant_id must be provided on insert for tenant-scoped tables.
  - _Why false positive:_ Schema says memory_links has no tenant_id; links are between tenant-scoped memory_entries and isolation is transitively enforced through source/target memory ids.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The statement inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without an explicit tenant_id column, relying on assertProjectInTenant for ownership but not recording tenant_id in the row. With no RLS or FK cascade, this can create cross-tenant data/write ambiguity.
  - _Why false positive:_ project_files has no tenant_id and is tenant-scoped transitively via verified parent project_id after assertProjectInTenant
- **server/mpeg-engine.ts:1417** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The parallel-produce code inserts project_files rows without an explicit tenant_id column (even though it calls assertProjectInTenant), leaving rows missing the tenant discriminator.
  - _Why false positive:_ project_files has no tenant_id and insert is correctly scoped through project_id after assertProjectInTenant
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT FROM tenant-scoped table `oauth_subscriptions` without scoping by tenant_id (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE). This reads rows across all tenants.
  - _Why false positive:_ startup validation intentionally iterates active subscriptions across tenants using each row's own tenant_id for maintenance
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is missing tenant_id filtering and returns rows across all tenants.
  - _Why false positive:_ boot-time reconnect sweep intentionally enumerates provider rows across tenants and handles each tenant separately by tenant_id
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh executes a global SELECT FROM oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter, enumerating subscriptions across tenants.
  - _Why false positive:_ proactive refresh is a platform-wide maintenance loop over active subscriptions with per-row tenant_id-scoped handling
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions performs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without constraining by tenant_id, exposing cross-tenant data and enabling cross-tenant operations.
  - _Why false positive:_ daily force-refresh is an intentional cross-tenant housekeeping sweep and refreshes each subscription using its own tenant_id
- **server/pace-control.ts:85** _(global-table)_ — checkPace queries heartbeat_logs without any tenant_id constraint (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). heartbeat_logs appears tenant-scoped so this counts across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; pace control is using a platform-global log table intentionally not tenant-scoped
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries heartbeat_logs without tenant scoping (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...). This returns global data across tenants for what appears to be tenant-scoped activity.
  - _Why false positive:_ heartbeat_logs has no tenant_id and snapshot reads aggregate platform-global pacing data from that global table
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog() updates the tenant-scoped plans table using only plan id (UPDATE plans ... WHERE id = ${planId}) with no tenant_id constraint. This allows cross-tenant writes if an attacker or buggy caller supplies another planId.
  - _Why false positive:_ appendExecutionLog is an internal worker helper updating plans by server-side planId within executor flow, not directly from an untrusted request boundary
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — CAS claim uses UPDATE plans ... WHERE id = ${planId} AND status = 'approved' without scoping by tenant_id. Claiming a plan by id alone can let code running for one tenant move another tenant's plan into 'executing' (and then execute steps under the wrong tenant context).
  - _Why false positive:_ executePlan is an internal executor claiming a plan by server-side primary key and then uses returned tenant_id for subsequent scoped actions
- **server/plan-executor.ts:527** _(upstream-verified)_ — Code sets a plan to 'failed' using UPDATE plans SET status = 'failed' WHERE id = ${planId} with NO tenant_id constraint. Unscoped writes to tenant-scoped plans permit cross-tenant state changes.
  - _Why false positive:_ same call path already claimed the exact plan row at line 513 and obtained its tenant_id; this failure update is on that previously verified row id
- **server/plan-executor.ts:725** _(upstream-verified)_ — Final status update on plan uses UPDATE plans ... WHERE id = ${planId} without tenant scoping. This finalizes plan state across tenants if planId points to a different tenant.
  - _Why false positive:_ final update uses planId for already-loaded row; same call path carries row.tenant_id and operates on that fetched plan
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path recovery updates plans (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') with no tenant_id filter. This can mark other tenants' plans failed.
  - _Why false positive:_ executor recovery is server-invoked worker logic updating by server-side plan serial id, not untrusted request input
- **server/plan-executor.ts:775** _(system-sweep)_ — resumeStuckPlans() runs a cross-tenant UPDATE that flips ALL stale executing plans to 'approved' (UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ...), with no tenant_id constraint. This globally modifies tenant-scoped rows and can unintentionally surface or restart work across tenants.
  - _Why false positive:_ boot-time stale-plan recovery intentionally sweeps all tenants and only flips status/queues per-row ids, with no tenant data returned or mixed
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} does not constrain the write to the tenant (no tenant_id in WHERE). This can modify another tenant's row if id collisions or guesses occur.
  - _Why false positive:_ t.id comes from prior SELECT scoped by ht.tenant_id = tenantId in same call path
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} is missing a tenant_id constraint. The code selected desks by tenant earlier but the UPDATE does not re-assert tenant ownership before mutating rows.
  - _Why false positive:_ desk.id comes from prior SELECT scoped by ad.tenant_id = tenantId in same function
- **server/proposal-verifier.ts:245** _(internal-worker-pk)_ — persist() updates code_proposals SET verification_status/verification_details WHERE id = ${proposalId} without scoping by tenant_id. This mutates a tenant-scoped table by id only.
  - _Why false positive:_ verifier persist is internal async worker updating proposal by server-invoked primary key, not directly from request boundary
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — In attemptRebaseOnDrift the UPDATE code (e.g. setting status='rejected' / validation_result) uses WHERE id = ${proposalId} with no tenant constraint — writes back to code_proposals by id only.
  - _Why false positive:_ rebase path is internal verifier workflow mutating proposal by server-side proposal id
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes to a tenant-scoped table without a tenant_id WHERE clause.
  - _Why false positive:_ rebased diff persistence is internal worker update by globally unique proposal primary key
- **server/recurring-messages.ts:300** _(upstream-verified)_ — UPDATE agent_knowledge SET content = ${JSON.stringify(updated)}, updated_at = NOW() WHERE id = ${r.id} updates a tenant-scoped table by id only with no tenant_id constraint.
  - _Why false positive:_ r.id is from rows already carrying r.tenant_id and prior operations in same loop use that tenant context
- **server/research-engine.ts:400** _(system-sweep)_ — DELETE from agent_knowledge without tenant scoping (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will remove entries for all tenants, not just the invoking tenant.
  - _Why false positive:_ expired autoresearch knowledge purge is cross-tenant housekeeping delete of expired rows only, with no tenant data exposure or mixing
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. tenant-scoped tables require tenant_id to be passed explicitly on INSERTs.
  - _Why false positive:_ project_notes has no tenant_id and insert is preceded by tenant-scoped projects ownership check on parent project
- **server/routes/projects.ts:224** _(fk-transitive)_ — INSERT into project_files (base64 upload) does not include tenant_id. Tenant-scoped records must include tenant_id on insert.
  - _Why false positive:_ project_files has no tenant_id and insert is preceded by tenant-scoped projects ownership check on parent project
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into project_files (multipart upload) does not include tenant_id. Tenant-scoped records must include tenant_id on insert.
  - _Why false positive:_ project_files has no tenant_id and insert is preceded by tenant-scoped projects ownership check on parent project
- **server/seed.ts:2163** _(system-sweep)_ — Unscoped UPDATE on tenant-scoped table research_programs: UPDATE research_programs SET model = ... WHERE id = ${p.id} lacks tenant_id constraint — this can modify rows belonging to other tenants.
  - _Why false positive:_ boot-time seed repair updates housekeeping/model field across tenants and does not expose or mix tenant data
- **server/seed.ts:2516** _(system-sweep)_ — Unscoped read from tenant-scoped table conversations: SELECT DISTINCT c.id, c.project_id FROM conversations c ... has no tenant_id filter and is followed by inserts that may create cross-tenant links (project_conversations).
  - _Why false positive:_ boot-time backfill processes each conversation row with its own project_id to create missing join rows; project_conversations is transitively scoped and no tenant data is returned
- **server/seed.ts:3771** _(system-sweep)_ — DELETE FROM tenant-scoped table `heartbeat_tasks` has no tenant_id restriction — will delete matching rows across all tenants.
  - _Why false positive:_ platform-wide housekeeping purge removes stale auto-disabled heartbeat task rows only and does not return or mix tenant content
- **server/seed.ts:3799** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` does not include tenant_id in WHERE, causing changes to memory entries across all tenants.
  - _Why false positive:_ global normalizer sweep updates text in place across rows as maintenance only and does not expose or combine tenant data
- **server/seed.ts:3825** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` (gpt-4.1→gpt-5 backfill) lacks a tenant_id constraint and will update conversations across all tenants.
  - _Why false positive:_ boot-time legacy model-id backfill is a platform-wide maintenance update of status/config data only, with no tenant-facing disclosure or mixing
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE memory_entries is executed using only id in WHERE (WHERE id = <id>) after finding an existing row. The UPDATE lacks tenant_id restriction, allowing modification of another tenant's memory entry by id.
  - _Why false positive:_ same call path first selects existing memory_entries row with tenant_id = tenantId, then updates that selected id
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` without tenant_id constraint (reads custom_tools by name using db.select().from(customTools).where(eq(customTools.name, parsed.name))). This can leak cross-tenant rows or be abused to detect/operate on other tenants' tools.
  - _Why false positive:_ Name is transformed with tenantToolName(parsed.name, tenantId) immediately before lookup, making the read tenant-derived in the same call path.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `custom_tools` without tenant_id constraint (.where(eq(customTools.id, tool.id))). Updating implementation by id only can modify another tenant's row if ids are misused or attacker-controlled.
  - _Why false positive:_ Auto-fix updates the just-created/loaded tool row by server-held primary key within internal validation flow, not from an untrusted request boundary.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE against tenant-scoped table `custom_tools` without tenant_id constraint (await db.delete(customTools).where(eq(customTools.id, tool.id))). This revert path can delete another tenant's tool row if the id is mis-bound.
  - _Why false positive:_ Revert deletes the internally managed tool row by server-held primary key in the same validation workflow, not from untrusted input.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE against tenant-scoped table `custom_tools` to increment usageCount uses only id in WHERE (.where(eq(customTools.id, tool.id))) with NO tenant_id constraint. This can increment usageCount on another tenant's row if ids are misused.
  - _Why false positive:_ tool is first loaded with WHERE name AND tenant_id = tenantId, then the usage update targets that fetched row in the same function.
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into tenant-scoped table project_files does not include tenant_id. The raw SQL INSERT INTO project_files (...) VALUES (...) omits tenant_id even though tenant isolation is enforced at the application layer and INSERTs must supply tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insertion is guarded by assertProjectInTenant against tenant-scoped parent project.
- **server/tools/domains/files/handlers.ts:829** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The INSERT `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)` omits an explicit tenant_id column on a table that is tenant-scoped.
  - _Why false positive:_ project_files has no tenant_id column and projId is derived from conversation joined to projects with p.tenant_id = tenantId in the same path.
- **server/video-job-runner.ts:236** _(system-sweep)_ — recoverStaleVideoJobs executes a wide UPDATE on videoJobs to mark stale jobs failed without any tenant_id filter. This can flip jobs across all tenants to 'failed', causing data loss/service disruption for other tenants.
  - _Why false positive:_ This is a platform-wide stale-job recovery sweeper that only flips housekeeping/status fields and returns aggregate/job ids, not tenant data to users.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without tenant_id filter (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads metadata across all tenants.
  - _Why false positive:_ Heartbeat maintenance loop intentionally scans running experiments across tenants and processes each row with its own tenant_id for scoped updates.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_evals` without tenant constraint (UPDATE agent_evals SET ... WHERE id = ${evalId}). This can update eval rows across tenants by id.
  - _Why false positive:_ evalId is server-generated from the immediately preceding INSERT in the same function, then updated by global PK; not sourced from an untrusted tenant boundary.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from tenant-scoped table `ai_insights` without tenant filter in retryPendingMinervaRouting (SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ${PENDING_PLAN_MARKER}). This reads rows from all tenants.
  - _Why false positive:_ This is an explicit durability sweep across tenants; each row carries its own tenant_id and is processed per-row for routing, not returned/mixed to a tenant-facing surface.
- **server/agentic-engines.ts:451** _(global-table)_ — SELECT from `heartbeat_logs` without tenant scoping (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10) in runPredictiveEngine — reads logs across tenants.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column, so these logs are platform-global by design.
- **server/agentic-engines.ts:519** _(global-table)_ — SELECT from `heartbeat_logs` without tenant scoping (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...) in runOptimizationEngine — reads/aggregates heartbeat logs across tenants.
  - _Why false positive:_ Referenced schema says heartbeat_logs has no tenant_id column, so global aggregation over heartbeat_logs is expected.
- **server/agentic-features.ts:638** _(upstream-verified)_ — UPDATE on tenant-scoped table `outreach_enrollments` without tenant constraint (UPDATE outreach_enrollments SET status = 'completed' ... WHERE id = ${enrollment.id}). The update was performed using a row previously selected with a tenant filter, but the UPDATE itself lacks an explicit tenant_id clause.
  - _Why false positive:_ The enrollment row being updated was selected just above with e.tenant_id = params.tenantId and locked FOR UPDATE, so ownership was enforced earlier in the same call path.
- **server/agentic-features.ts:671** _(upstream-verified)_ — UPDATE on tenant-scoped table `outreach_enrollments` without tenant constraint (UPDATE outreach_enrollments SET current_step = ${nextStep}, ... WHERE id = ${enrollment.id}).
  - _Why false positive:_ The UPDATE operates on enrollment rows previously selected in the same function with e.tenant_id = params.tenantId and FOR UPDATE OF e SKIP LOCKED.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE on tenant-scoped table `outreach_enrollments` without tenant constraint (UPDATE outreach_enrollments SET reply_classification = ..., WHERE id = ${row.id}).
  - _Why false positive:_ row was fetched immediately above with e.tenant_id = params.tenantId and only that row.id is updated, so tenant ownership was already established in the same path.
- **server/agentic/approvals.ts:134** _(system-sweep)_ — Bulk UPDATE on tenant-scoped table `agent_approvals` without tenant filter (UPDATE agent_approvals SET status = 'expired' WHERE status = 'pending' AND expiresAt < now). This will expire pending approvals across all tenants in one operation.
  - _Why false positive:_ This is a platform-wide expiry sweep; it only mutates housekeeping/status fields and then handles each returned row with its own tenantId, without exposing mixed tenant data.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query reads agent_runs / agent_approvals without constraining tenant_id — the candidate query can return rows for any tenant.
  - _Why false positive:_ Global worker sweep intentionally scans pending approval runs across tenants, joins approvals on matching tenant_id, and processes each candidate with its row-carried tenant_id.
- **server/agentic/wake-scheduler.ts:146** _(system-sweep)_ — Sub-select that claims due wakes does not constrain by tenant_id: the inner SELECT (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) can select rows for any tenant.
  - _Why false positive:_ This is a global scheduler claim loop for due wakes; claimed rows are processed individually using each row's tenant_id and not mixed into any tenant-facing response.
- **server/auth.ts:1163** _(system-sweep)_ — loadSessionsFromDb selects all auth_sessions (SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions) without filtering by tenant. This reads session data across tenants into process memory.
  - _Why false positive:_ Startup/session-cache bootstrap reads all auth_sessions for process-wide housekeeping; not tenant-facing output and rows retain tenant_id in cache.
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table `commitments` with NO tenant_id filter in scanAndEscalate: the query reads all tenants' commitments (SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...).
  - _Why false positive:_ Intentional cross-tenant overdue-commitment scanner; each row carries tenant_id and emitted digest is redacted, not mixing tenant content into tenant-facing output.
- **server/data-protection.ts:118** _(system-sweep)_ — SELECTing expired soft-deleted conversations without tenant_id constraint (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows for all tenants.
  - _Why false positive:_ Platform-wide purge sweep over expired soft-deleted conversations; per-row tenant_id is carried into backup and operation is housekeeping/destructive, not tenant-facing fan-in output.
- **server/delivery-pipeline.ts:1034** _(system-sweep)_ — getDeliveryStats issues an unscoped SELECT from delivery_logs when tenantId is omitted (await db.select().from(deliveryLogs) ), returning rows across all tenants.
  - _Why false positive:_ When tenantId is omitted this returns only aggregate counts over delivery_logs, not row data or mixed tenant content.
- **server/dfy-intake.ts:93** _(upstream-verified)_ — submitDfyIntake UPDATE on dfy_intake_forms sets responses/status using WHERE id = ${form.id} only — no tenant_id constraint. This can allow updating a form across tenants if ids collide or are used incorrectly.
  - _Why false positive:_ Update uses form.id from getDfyIntakeByToken in the same call path; ownership of the target row is already fixed by that earlier lookup, so caller cannot choose a cross-tenant id here.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with no tenant_id constraint. This can delete chunks belonging to other tenants that share collection_id/doc_path values.
  - _Why false positive:_ collection ownership is verified immediately above via doc_collections WHERE id and tenant_id before deleting chunks for that collectionId
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings issues UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} without any tenant_id constraint. Updating by id only risks cross-tenant modification if ids are mis-used.
  - _Why false positive:_ row.id values come from a prior SELECT on doc_chunks scoped by collection_id and tenant_id in the same function
- **server/email.ts:518** _(system-sweep)_ — backfillEmptyBodies updates inbox_messages SET body_text/body_html WHERE id = ${row.id} without a tenant_id constraint. Updating by id only can modify rows belonging to other tenants.
  - _Why false positive:_ boot/maintenance backfill scans all inbox_messages and only fills missing body fields by each row's own primary key; no tenant-facing mixed output
- **server/felix-brain.ts:19** _(system-sweep)_ — Query selects rows from tenant-scoped table 'agent_runs' without scoping by tenant_id (no WHERE tenant_id = ...)
  - _Why false positive:_ boot-time rehydration intentionally scans running/pending agent_runs across tenants and stores per-row tenant-carried state in memory, not tenant-facing mixed output
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} but does not include tenant_id = ${tenantId}, allowing cross-tenant updates if runId belongs to another tenant.
  - _Why false positive:_ internal loop updates its own felix_loop_runs row by server-generated runId, not from an untrusted request boundary
- **server/felix-loop.ts:690** _(internal-worker-pk)_ — Error-path UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} without tenant_id = ${tenantId}, risking cross-tenant writes when recording failures.
  - _Why false positive:_ error-path recovery update targets the same internal server-generated runId for the current worker run
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) without constraining by tenant_id. Reading a foreign project id without tenant-scoped WHERE risks exposing another tenant's project.
  - _Why false positive:_ same function immediately checks project.tenantId against caller tenantId and throws on mismatch before any return/use
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) but omits tenant_id. This can update a project belonging to a different tenant.
  - _Why false positive:_ update occurs only after same-function ownership check confirmed project.tenantId matches tenantId
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) lacks tenant scoping — disabling tasks by id only can affect tasks for other tenants.
  - _Why false positive:_ health audit is a platform-wide housekeeping sweep disabling stale heartbeat_tasks by selected ids; mutates housekeeping state only, no tenant data returned or mixed
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from research_schedules has no tenant_id filter (SELECT * FROM research_schedules ...). This reads rows across all tenants without constraining to a caller/owner tenant.
  - _Why false positive:_ Heartbeat cron sweep intentionally scans due research_schedules across tenants and processes each row with its own sched.tenant_id; no mixed tenant output.
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE statements on research_schedules set next_run_at/last_run_at use WHERE id = ${sched.id} without verifying tenant (no tenant_id in WHERE). This can update another tenant's row if IDs are abused or mis-matched.
  - _Why false positive:_ Maintenance sweep updates housekeeping schedule fields for rows it just selected; per-row tenant_id is carried and no tenant data is returned or mixed.
- **server/heartbeat.ts:378** _(system-sweep)_ — SELECT from event_log (SELECT * FROM event_log WHERE status = 'pending' ...) has no tenant_id constraint. This reads pending events across tenants and processes them without prior per-tenant scoping/validation.
  - _Why false positive:_ Global worker claims pending event_log rows across tenants but routes each using event.tenant_id and failure updates are tenant-scoped; this is per-row background processing.
- **server/heartbeat.ts:265** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executes without a tenant_id constraint. heartbeat_tasks is tenant-scoped but the UPDATE could affect rows belonging to other tenants if the id is misused.
  - _Why false positive:_ Startup normalization is a platform-wide housekeeping pass over heartbeat_tasks, mutating only model housekeeping fields on worker-selected rows.
- **server/heartbeat.ts:270** _(system-sweep)_ — Second normalization UPDATE on heartbeat_tasks (SET model = 'gemini-2.5-flash' WHERE id = ${t.id}) lacks tenant scoping; this writes to a tenant-scoped table without constraining tenant_id.
  - _Why false positive:_ Same startup housekeeping sweep; only normalizes task model for rows being processed, with no tenant-facing data mixing.
- **server/heartbeat.ts:1534** _(global-table)_ — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id predicate. heartbeat_logs are tenant-scoped but the query returns global results.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this log lookup is against a non-tenant-scoped table.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status='pending' ... LIMIT ${limit} FOR UPDATE SKIP LOCKED) without any tenant_id constraint. This reads/updates agent_jobs across tenants in one statement.
  - _Why false positive:_ Job queue worker intentionally claims due agent_jobs across tenants and returns row-carried tenant_id for per-job processing; this is platform-wide background dispatch, not tenant mixing.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases updates agent_jobs rows (both terminal and requeue passes) without any tenant_id filter, touching running/pending jobs across tenants.
  - _Why false positive:_ Global lease-reclaim housekeeping sweep across tenants; only mutates job status fields and returns counts, not tenant-facing mixed data.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates agent_jobs SET status='succeeded' WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint — a late/slow worker could mark another tenant's job as succeeded.
  - _Why false positive:_ Server worker completion updates a job by server-issued primary key with attempt fencing; not shown as coming from untrusted request input.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob does a SELECT max_attempts FROM agent_jobs WHERE id = ${id} with no tenant constraint, allowing observing another tenant's job parameters.
  - _Why false positive:_ Internal worker failure path reads job metadata by server-issued job id, not from a tenant request boundary in the shown call path.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — failJob UPDATEs agent_jobs to failed_terminal or pending without any tenant_id WHERE clause — this can mutate jobs belonging to other tenants.
  - _Why false positive:_ Internal worker failure-state update by globally unique job id plus attempts fencing; no evidence id is user-controlled across tenants.
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT from tenant-scoped table `project_files` without constraining by tenant_id. The query checks only project_id and file_name, but per invariant every read against a tenant-scoped table must include WHERE tenant_id = <caller tenant> (or equivalent).
  - _Why false positive:_ project_files has no tenant_id and is tenant-isolated through tenant-scoped parent projects via project_id
- **server/lib/ideabrowser-backfill.ts:116** _(fk-transitive)_ — SELECT from tenant-scoped table `project_notes` without constraining by tenant_id. The query tests project_id and note but omits a tenant_id filter, violating the tenant isolation rule for reads.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-isolated through tenant-scoped parent projects via project_id
- **server/lib/mcp-api-keys.ts:69** _(upstream-verified)_ — String-built SQL array literal from caller-derived 'scopes' (scopesLit) is interpolated into a query (VALUES (... ${scopesLit}::text[], ...)). Constructing a SQL literal from user input risks injection and bypassing DB-level parameterization/isolation.
  - _Why false positive:_ not a tenant-isolation finding; query inserts explicit caller-supplied tenantId and issue is SQL-literal construction, outside cross-tenant scope
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without constraining by tenant_id (CTE + subsequent UPDATE sets rows to 'publishing' across all tenants).
  - _Why false positive:_ platform-wide runner intentionally processes due rows across tenants and carries each row's own tenant_id forward per row
- **server/lib/scheduled-post-runner.ts:134** _(system-sweep)_ — UPDATE on tenant-scoped table `scheduled_posts` (marking blocked by quality gate) uses WHERE id = ${row.id} with NO tenant_id constraint.
  - _Why false positive:_ maintenance runner updates the locked row selected by the cross-tenant sweep and uses row-carried tenant_id context; no tenant-facing mixed output
- **server/lib/scheduled-post-runner.ts:217** _(system-sweep)_ — UPDATE on tenant-scoped table `scheduled_posts` (writing publish results) uses WHERE id = ${row.id} with NO tenant_id constraint.
  - _Why false positive:_ status/result write is part of per-row processing in a platform-wide scheduler using each picked row's tenant_id context
- **server/lib/scheduled-post-runner.ts:244** _(system-sweep)_ — Error-path UPDATE on tenant-scoped table `scheduled_posts` uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id — allows cross-tenant mutation on exception paths.
  - _Why false positive:_ error-path release/update is still within the same per-row cross-tenant scheduler sweep and only mutates housekeeping/status fields
- **server/martech-bundle.ts:314** _(upstream-verified)_ — UPDATE on tenant-scoped table tenant_voice_profiles uses only id in the WHERE clause (where id = existing.id) and lacks an explicit tenant_id constraint.
  - _Why false positive:_ update uses id from prior getVoiceProfile({ tenantId, profileName }) lookup in same call path, so tenant ownership is established earlier
- **server/memory-graph.ts:81** _(upstream-verified)_ — UPDATE to tenant-scoped table memory_categories omits tenant_id in WHERE clause (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}).
  - _Why false positive:_ catId comes from ensureCategory(categoryName, tenantId, personaId), which SELECTs/INSERTs memory_categories scoped by tenantId immediately above.
- **server/memory-graph.ts:196** _(fk-transitive)_ — SELECT against memory_links does not include tenant scoping (SELECT 1 FROM memory_links WHERE ...). memory_links appears to be tenant-scoped, so this read can return rows from other tenants.
  - _Why false positive:_ memory_links has no tenant_id column and links tenant-scoped memory_entries selected within the same tenant; isolation is transitive through source/target memory IDs.
- **server/memory-graph.ts:409** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ... WHERE id = ${mem.id} omits tenant_id in WHERE clause. Even though mem was selected earlier with a tenant filter, the UPDATE itself is not tenant-scoped.
  - _Why false positive:_ mem.id is from a preceding SELECT on memory_entries constrained by tenant_id = tenantId in the same function/loop.
- **server/memory-intelligence.ts:273** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (from classifyMemoryCategory callback) omits tenant_id in WHERE clause.
  - _Why false positive:_ entry.id is the server-created row returned by storage.createMemoryEntry with tenantId supplied just above; update targets that freshly created tenant-owned row.
- **server/memory-intelligence.ts:316** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} (in update flow) omits tenant_id in WHERE clause.
  - _Why false positive:_ entry.id is the newly created replacement row from storage.createMemoryEntry called with tenantId immediately above, so ownership is established earlier in the same flow.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} omits tenant_id. knowledge_triples is tenant-scoped in other queries, but this UPDATE does not constrain by tenant.
  - _Why false positive:_ c.id values come from the immediately preceding SELECT on knowledge_triples constrained by tenant_id = tenantId and valid_until IS NULL.
- **server/minds-engine.ts:327** _(upstream-verified)_ — SQL uses a string-interpolated array constructed from caller-supplied eventIds: WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]). Building SQL array literals via string interpolation with request-derived values can lead to injection/isolation risks.
  - _Why false positive:_ Although the array literal construction is unsafe style, the UPDATE itself is tenant-scoped with tenant_id = tenantId; this is not a cross-tenant isolation miss.
- **server/minerva-planner.ts:214** _(upstream-verified)_ — INSERT into mind_tickets builds an array literal for depends_on via string interpolation from params.dependsOn: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. This composes SQL fragments from caller-controlled input.
  - _Why false positive:_ Reported issue is SQL array construction, not tenant isolation; surrounding code explicitly requires a valid tenantId and no cross-tenant query is shown here.
- **server/process-governor.ts:121** _(global-table)_ — Subquery counts heartbeat_logs without scoping by tenant. The subselects COUNT(*) FROM heartbeat_logs (e.g. lines selecting successes/failures) do not include a tenant_id filter and can return cross-tenant data.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is treated as a platform-global log keyed by task_id; counts are for tenant-scoped heartbeat_tasks rows selected by ht.tenant_id = tenantId.
- **server/process-governor.ts:198** _(global-table)_ — Query computing average response time reads heartbeat_logs but lacks a tenant_id constraint (SELECT AVG(duration_ms) FROM heartbeat_logs ...). This aggregates across tenants.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; this is a platform-global telemetry aggregate by design
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures query SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... has no tenant_id filter, allowing cross-tenant aggregation.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is queried as global health telemetry
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... does not filter by tenant_id — it will enumerate projects across all tenants.
  - _Why false positive:_ backfillProjectBrains is a platform-wide maintenance backfill creating per-project local files, not tenant-facing mixed output
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} queries conversations by project_id without any tenant constraint, allowing cross-tenant traversal.
  - _Why false positive:_ conversation scan occurs inside the same global backfill maintenance loop processing one project at a time
- **server/project-brain.ts:360** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} lacks tenant scoping/ownership verification. Messages are being read by conversation id only, which can span tenants if not validated.
  - _Why false positive:_ message reads are part of the maintenance backfill and handled per conversation without tenant-facing mixed response
- **server/recurring-messages.ts:258** _(system-sweep)_ — Runner SELECT: SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500 is executed without a tenant filter and returns rows across tenants.
  - _Why false positive:_ scheduled-message runner intentionally scans across tenants but carries row tenant_id forward for each job's per-row scoped handling
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries without tenant_id constraint (UPDATE memory_entries SET ... WHERE id = ${storedId}). This allows modifying a row by id regardless of tenant ownership.
  - _Why false positive:_ storedId comes from an immediately prior SELECT constrained by tenant_id and persona/category dedupe, so update targets that verified row
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE on research_sessions performed without tenant_id scoping (cleanupZombieSessions uses UPDATE research_sessions ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}). This will mark sessions across all tenants as completed.
  - _Why false positive:_ cleanupZombieSessions is global housekeeping that only flips stale session status fields and returns aggregate/internal ids, not tenant content
- **server/routes.ts:2120** _(upstream-verified)_ — INSERT into tenant-scoped table presenter_sessions uses tenantId taken directly from request body without verifying caller ownership.
  - _Why false positive:_ route is gated by x-internal-key checked against SESSION_SECRET before accepting request-body tenantId
- **server/routes.ts:4331** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' is missing tenant_id. The code inserts (project_id, note, author) without setting tenant_id, violating the invariant that every INSERT into tenant-scoped tables must explicitly supply tenant_id.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively via project_id; project_id was fetched from conversations with tenant_id = tenantId just above.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' lacks tenant_id constraint in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Why false positive:_ Conversation row was just created by storage.createConversation with tenantId, so updating by that fresh server-generated conv.id is in the same trusted call path.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped parent rows already verified in-path: project checked with tenant_id and conversation was freshly created for tenantId.
- **server/routes/conversations.ts:229** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not set tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ..., 'system:archive')).
  - _Why false positive:_ project_notes has no tenant_id and is scoped through project_id; query above joins projects and constrains p.tenant_id to conv.tenantId/tenantId before insert.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` missing tenant_id predicate. Code runs: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` — this will flip the is_read flag for a message by id without constraining to the caller's tenant, enabling cross-tenant mutation.
  - _Why false positive:_ The same handler selected the message by id AND tenant_id = tenantId immediately above; the update uses that verified row's msg.id.
- **server/routes/projects.ts:33** _(fk-transitive)_ — Project list query includes dependent subqueries (project_files, project_notes, project_conversations) that do NOT constrain those tenant-scoped tables to the caller's tenant (no tenant_id in the subquery WHERE clauses).
  - _Why false positive:_ Subqueries count child tables with no tenant_id, scoped transitively by project_id against parent projects already filtered by p.tenant_id = tenantId.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET updated_at = ... WHERE id = ${projectId} executes without constraining projects by tenant_id. An UPDATE against a tenant-scoped table must include tenant_id in the WHERE clause.
  - _Why false positive:_ The same handler previously verified the target project with SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before this timestamp update.
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET updated_at = ... WHERE id = ${projectId} executes without constraining projects by tenant_id (second upload handler).
  - _Why false positive:_ The same upload handler verified project ownership with id and tenant_id earlier in the call path before updating updated_at.
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} ... lacks tenant_id constraint. Reads against tenant-scoped project_files must include tenant_id in the WHERE clause even if the parent project was checked earlier.
  - _Why false positive:_ project_files has no tenant_id and is scoped via project_id; the parent project was verified with tenant_id = tenantId immediately above.
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${...} LIMIT 1 has no tenant_id constraint. This read can leak rows if project_files contains rows referencing other tenants.
  - _Why false positive:_ project_files has no tenant_id and is transitively isolated by project_id; parent project ownership was checked with tenant_id earlier in this handler.
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} does not include tenant_id. Deletes against tenant-scoped tables must include tenant_id to prevent cross-tenant deletions.
  - _Why false positive:_ project_files has no tenant_id and delete is constrained by project_id after verifying the parent project belongs to tenantId in the same handler.
- **server/sculptor.ts:106** _(upstream-verified)_ — UPDATE sculptor_sessions executed without constraining tenant_id (UPDATE ... WHERE id = ${session.id}). This allows cross-tenant modification of sessions.
  - _Why false positive:_ Row was just inserted with params.tenantId and update uses returned server-created session.id in same call path.
- **server/sculptor.ts:113** _(upstream-verified)_ — UPDATE sculptor_sessions SET conversation_id ... WHERE id = ${session.id} lacks a tenant_id constraint, permitting cross-tenant writes to session rows.
  - _Why false positive:_ conversation_id update targets the freshly inserted session row returned from INSERT for this tenant in same function.
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} is executed without constraining tenant_id. Even though session was previously read, the UPDATE should still include tenant filtering to prevent cross-tenant writes.
  - _Why false positive:_ Same function previously selects sculptor_sessions by id and tenant_id and only then updates that sessionId.
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter. heartbeat_tasks appears tenant-scoped elsewhere, so this reads tenant-specific data across all tenants.
  - _Why false positive:_ Boot-time cleanup/reconciler intentionally scans across tenants for housekeeping counts only; no tenant data returned.
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks a tenant_id constraint and will disable delegation tasks across all tenants.
  - _Why false positive:_ Global housekeeping sweep intentionally disables runaway tasks across tenants; mutates status only and mixes no tenant content.
- **server/seed-cleanup.ts:45** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant scoping and reads across tenants.
  - _Why false positive:_ Maintenance cleanup count across all tenants for duplicate task names is housekeeping-only aggregate.
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true has no tenant_id filter and will disable matching tasks for all tenants.
  - _Why false positive:_ Platform-wide cleanup disabling duplicate session-logger tasks is housekeeping-only cross-tenant maintenance.
- **server/seed-cleanup.ts:59** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (... name ILIKE ... ) executes without tenant_id scoping, reading tenant-scoped task rows across all tenants.
  - _Why false positive:_ Cross-tenant count for orphaned repo-examination tasks is a maintenance aggregate, not tenant-facing data access.
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true lacks tenant scoping and will disable matching tasks for all tenants.
  - _Why false positive:_ Global cleanup disabling orphaned repo-examination tasks is status-only housekeeping across tenants.
- **server/seed.ts:36** _(system-sweep)_ — Unscoped read from tenant-scoped table research_sessions: SELECT COUNT(*) FROM research_sessions has no tenant_id filter.
  - _Why false positive:_ Boot-time dev snapshot import/seed logic intentionally checks global emptiness and does not return tenant data to callers.
- **server/seed.ts:2158** _(system-sweep)_ — Unscoped read from tenant-scoped table research_programs: SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter.
  - _Why false positive:_ Seed maintenance pass scans all active programs to normalize model names; cross-tenant housekeeping update only.
- **server/seed.ts:4011** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} but does not include tenant_id in the WHERE clause; the update is unscoped at the statement level.
  - _Why false positive:_ The row id being updated was selected immediately above with tenant_id = 1, so this update is bound to that verified tenant-scoped row.
- **server/storage.ts:295** _(upstream-verified)_ — SELECT from tenant-scoped table `messages` without any tenant_id constraint in attachCitationMetadata (raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL). This can leak citations across tenants.
  - _Why false positive:_ attachCitationMetadata only re-reads ids from msgs already fetched with conversationId+tenantId filters in the same call path.
- **server/storage.ts:838** _(system-sweep)_ — UPDATE memory_entries SET status='archived' WHERE ... (archiveExpiredMemories) executes without any tenant filter — a cross-tenant mutation.
  - _Why false positive:_ Platform-wide housekeeping archive updates only status on expired memory rows and returns aggregate/ids, not tenant content.
- **server/storage.ts:854** _(system-sweep)_ — UPDATE memory_entries SET status='archived' WHERE ... (archiveStaleMemories) executes without tenant filtering — unscoped write across tenants.
  - _Why false positive:_ Platform-wide stale-memory archival is housekeeping-only status mutation across tenants, with no cross-tenant content mixing.
- **server/storage.ts:1019** _(system-sweep)_ — claimHeartbeatTasks updates heartbeat_tasks WHERE id = ${id} AND enabled = true ... with no tenant filter. This is an unscoped write on a tenant-scoped table.
  - _Why false positive:_ claimHeartbeatTasks is part of platform-wide due-task scheduler operating across tenants and only mutates housekeeping run timestamps per row
- **server/storage.ts:1445** _(upstream-verified)_ — supersedeAdr updates architecture_decisions with WHERE id = ${oldId} and WHERE id = ${newId} but does not include tenant_id in the UPDATE WHERE clauses. These are unscoped updates on a tenant-scoped table.
  - _Why false positive:_ same function first loads both ADR rows with id+tenantId and returns undefined unless both belong to tenant before updating by those validated ids
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` using id only (db.select().from(customTools).where(eq(customTools.id, inserted.id))) with NO tenant_id constraint. Reading a tenant-scoped row by id without tenant scoping can expose or confuse rows across tenants.
  - _Why false positive:_ row is re-read immediately after server-side insert using inserted.id just created in same flow with tenantId already set on insert
- **server/tenant-fork.ts:232** _(upstream-verified)_ — UPDATE on a tenant-scoped config table during two-pass self-reparenting uses only the PK in WHERE (.where(eq(entry.table.id, p.newId))) without constraining tenant_id to the new tenant. This update could in theory affect rows outside the intended tenant context if IDs collide or are manipulated.
  - _Why false positive:_ reparent update uses newId values returned from inserts in the same transaction after row.tenantId was set to newTenantId
- **server/tools.ts:8891** _(upstream-verified)_ — UPDATE against proposed_skills uses WHERE id = row.id but does not constrain by tenant_id. Updating a tenant-scoped table without a tenant_id WHERE clause can modify another tenant's row.
  - _Why false positive:_ update targets proposedSkills row.id just inserted earlier in same flow for tenant tid; same call path establishes the row being updated
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table 'browser_workflows' omits tenant filter — the UPDATE statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` has no WHERE tenant_id = <tenant> constraint.
  - _Why false positive:_ workflow was loaded immediately above with id/name plus tenant_id, and update uses that verified row's id
- **server/tools/domains/files/handlers.ts:833** _(upstream-verified)_ — UPDATE against tenant-scoped table 'projects' omits tenant filter — `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ projId comes from conversation->project join that already enforces p.tenant_id = tenantId before the project timestamp update
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples does not constrain by tenant_id (WHERE clause uses only id = ${row.id}). This allows a tenant-scoped row id from one tenant to be updated even if it belongs to another tenant.
  - _Why false positive:_ row.id values being updated come from the contradictions query immediately above filtered by tenant_id = tenantId
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys executed without constraining to tenant_id. The query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` does not filter by tenant_id and thus may expose provider key rows across tenants (even though executed only for admin tenant in code).
  - _Why false positive:_ provider_keys schema shows no tenant_id column, so this is an intentionally non-tenant-scoped table
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — COUNT(DISTINCT provider) query against provider_keys lacks a tenant constraint. The admin-only query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` does not include tenant_id scoping and can reveal cross-tenant provider configuration if provider_keys is tenant-scoped.
  - _Why false positive:_ provider_keys has no tenant_id column per schema; global admin audit count is expected
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries does not constrain by tenant_id (UPDATE memory_entries SET ... WHERE id = ${id}). This permits updating a row by id without also verifying it belongs to the caller's tenant.
  - _Why false positive:_ id is selected immediately above from memory_entries under tenant_id/persona/category constraints, then updated by that verified id
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses only id membership (WHERE id = ANY(${ids})) with no tenant_id constraint. IDs were selected earlier under a tenant filter, but the subsequent UPDATE lacks an explicit tenant_id guard and could update rows outside the caller's tenant.
  - _Why false positive:_ ids array is derived from rows selected just above with tenant_id/persona/category filters before the access-count update
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries does not include a tenant_id constraint (UPDATE memory_entries SET ... WHERE id = ${id}). This updates by id alone and lacks explicit verification of tenant ownership.
  - _Why false positive:_ id comes from prior select on memory_entries constrained by tenant_id, persona_id, and category in the same flow
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped table by id only with no tenant_id constraint. This can modify rows belonging to a different tenant if an id is mis-bound.
  - _Why false positive:_ reservationId is a server-generated ledger primary key returned by internal reservation logic, and this helper is an internal best-effort state transition by pk
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseVentureReservation) updates a tenant-scoped table by id only with no tenant_id constraint.
  - _Why false positive:_ Server-side helper updates a ledger row by serial id parameter, not shown crossing an untrusted request boundary; best-effort internal state transition by PK.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — SELECT from tenant-scoped table `tool_performance` without any tenant_id constraint (db.execute(sql`... FROM tool_performance ...`)). This reads tenant-scoped data across all tenants.
  - _Why false positive:_ Boot/maintenance invariant check intentionally aggregates cross-tenant counts for wiring health and only emits findings/counts, not tenant data.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT count(*) FROM `tool_performance` without tenant_id constraint (db.execute(sql`SELECT count(*)::int AS n FROM tool_performance`)). Reads aggregate across tenants.
  - _Why false positive:_ Cross-tenant total-row count used only for platform wiring diagnostics; no tenant content returned or mixed into tenant-facing output.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*) FROM `messages` without tenant_id constraint (db.execute(sql`SELECT count(*)::int AS n FROM messages WHERE created_at > ...`)). This counts messages across all tenants.
  - _Why false positive:_ Cross-tenant message count is a housekeeping diagnostic signal inside a platform-wide invariant check and exposes only an aggregate count.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM `tool_performance` without tenant_id constraint (db.execute(sql`... FROM tool_performance ...`)). This reads tenant-scoped rows across tenants.
  - _Why false positive:_ Maintenance check reads distinct tool names across tenants to detect dormant wiring; emits only platform-level findings, not tenant row data.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — sql.raw SELECT COUNT/MAX FROM `${spec.table}` (e.g. knowledge_nudges, tool_performance) is executed without a tenant_id WHERE clause. Even though table name and column are constants, the SELECT reads tenant-scoped tables across tenants.
  - _Why false positive:_ Subsystem freshness probe intentionally scans tables platform-wide for counts/last-write housekeeping metrics and does not return tenant data.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — sql.raw SELECT COUNT/pg_total_relation_size FROM ${tbl.name} (memory-growth probe) queries tenant-scoped tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without any tenant_id filter.
  - _Why false positive:_ Memory-growth tripwire is a platform-wide observability/housekeeping count+size probe only; no tenant row content is returned or mixed.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM `graph_memory` executed without tenant_id constraint (db.execute(sql`SELECT COUNT(*)::int AS cnt FROM graph_memory`)). This reads a tenant-scoped table across all tenants.
  - _Why false positive:_ Global bootstrap check counts graph_memory rows across tenants only to detect emptiness; aggregate housekeeping signal with no tenant data exposure.
- **server/auto-memorize.ts:73** _(system-sweep)_ — INSERT into agent_knowledge hardcodes tenant_id = 1. Writing tenant-scoped table with a fixed tenant id can misattribute watermarks and cross-contaminate tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:60-81 setWatermark writes a single global housekeeping watermark row (category='auto_memorize_watermark') for the platform-wide runAutoMemorize sweep; it is never returned to a tenant surface (only read by getWatermark:45-58). tenant_id=1 is the sweep's fixed housekeeping bucket, not acting-tenant data
- **server/auto-memorize.ts:163** _(system-sweep)_ — SELECT from messages reads recent messages across all tenants (no WHERE tenant_id). The code then partitions rows by tenant in-app — this is a cross-tenant read of a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:163-193 platform-wide sweep selects m.tenant_id per row then partitions rowsByTenant (187-193), failing closed on unscoped rows (190); each LLM prompt uses one tenant's rows only (198-206)
- **server/commitment-drafter.ts:236** _(upstream-verified)_ — UPDATE executed without tenant constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` — this unclaims a commitment by id with no `tenant_id` guard, allowing cross-tenant modification if `row.id` is attacker-controlled or malformed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/delivery-pipeline.ts:195** _(internal-worker-pk)_ — updateDeliveryLog performs an UPDATE on tenant-scoped table delivery_logs without constraining by tenant_id (WHERE eq(delivery_logs.id, id) only). This allows updating another tenant's delivery row if called with a delivery id from a different tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateDeliveryLog is only ever called with deliveryId returned by createDeliveryLog within deliverDigitalProduct itself (server/delivery-pipeline.ts:653,755,813,836,847,896,960,729); no caller passes a request/tool-arg-derived id.
- **server/persona-export.ts:33** _(global-table)_ — exportPersona reads personas via SELECT * FROM personas WHERE id = ${personaId} without verifying tenant ownership (no tenant_id filter). A caller-supplied personaId can be used to read another tenant's persona.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): personas table has no tenant_id column (platform-global personas); tenant linkage lives in trust/skills rows fetched separately.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks tenant scoping. This updates governance rules by id without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/self-improvement.ts:170** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` performed without enforcing tenant_id when tenantId is omitted. detectStagnation builds conditions with category only if no tenantId is provided, leading to cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:511** _(upstream-verified)_ — getExperimentHistory can return experiments across all tenants when tenantId is not provided — the code falls back to db.select().from(experiments) with no tenant filter, causing cross-tenant data disclosure.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/self-improvement.ts:394** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` uses only the experiment id in WHERE (where(eq(experiments.id, experiment.id))). This allows updating rows from other tenants by id (no tenant_id constraint).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:396** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `messages` in attachCitations uses WHERE id = ${messageId} with no tenant_id constraint (raw SQL). This allows cross-tenant writes to messages.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:1296** _(upstream-verified)_ — getAllDataForExport runs multiple SELECTs over tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, etc.) with no tenant filtering — exporting data for all tenants. This is a mass cross-tenant data leak if callable by non-admin contexts.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_activity` without tenant constraint (UPDATE agent_activity ... WHERE id = ${activityId}). This can modify another tenant's row by id.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): completeActivity's activityId is the PK returned by startActivity's own INSERT earlier in the same server flow; it is never caller-supplied, and the row only carries status/completedAt/summary.
- **server/agentic-engines.ts:533** _(global-table)_ — SELECT from `heartbeat_tasks` without tenant scoping (SELECT name, cron_expression, enabled, last_run_at, run_all FROM research_schedules WHERE tenant_id = ${tenantId} then SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ORDER BY name) — the heartbeat_tasks query lacks tenant_id and will return tasks across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from agent_knowledge (getWatermark) lacks tenant_id constraint — reads a tenant-scoped table without scoping to a tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-memorize.ts:62** _(system-sweep)_ — SELECT from agent_knowledge (setWatermark) lacks tenant_id constraint — may return another tenant's row for update/insert logic.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:73 — setWatermark/getWatermark maintain a single GLOBAL process watermark for the auto-memorize background loop (a heartbeat/admin-context job, tools.ts force-run is owner-gated). The row stores only an ISO timestamp under category 'auto_memorize_watermark' — no tenant data. The hardcoded tenant_id=1 is the intentional admin/global home for this singleton sentinel; it 
- **server/chat-engine.ts:4333** _(internal-worker-pk)_ — Call to storage.attachCitations(...) is made without any tenant context/tenant_id. This may write citation rows (or perform tenant-scoped lookups) without constraining the operation to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storage.attachCitations (storage.ts:392-401) does UPDATE messages SET citations WHERE id = messageId; messages has tenant_id (schema.ts:147). Both callers pass a server-generated PK of a message the code just inserted: chat-engine.ts:4332 uses (assistantMsgRow).id and routes.ts:5450 uses (__asstMsg).id. No request/tool-arg-derived id.
- **server/chat-engine.ts:4004** _(global-table)_ — recordToolExecution(conversationId, ...) is invoked using a foreign id (conversationId) but no tenant context/tenant_id is supplied. This writes/records tool-execution data tied to a conversation without proving the caller's tenant owns that conversation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): recordToolExecution (felix-brain.ts:298-320) only mutates the in-process taskStateStore Map; no DB write, no tenant table.
- **server/data-protection.ts:183** _(upstream-verified)_ — In backupConversationToDrive the messages SELECT omits tenant_id: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ORDER BY created_at ASC. Although the conversation row was verified to belong to tenantId, the messages query is not constrained, allowing reading another tenant's messages if conversation_id mapping is inconsistent.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): backupConversationToDrive validates the conversation belongs to the tenant before the messages SELECT (server/data-protection.ts:177); the conversationId is proven tenant-owned upstream in the same call path.
- **server/data-protection.ts:190** _(upstream-verified)_ — In backupConversationToDrive the compaction_archives SELECT omits tenant_id: SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} ORDER BY archived_at ASC.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): backupConversationToDrive validates the conversation belongs to the tenant before the messages SELECT (server/data-protection.ts:177); the conversationId is proven tenant-owned upstream in the same call path.
- **server/delivery-pipeline.ts:1049** _(global-table)_ — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId without any tenant_id constraint (.where(eq(deliveryLogs.stripePaymentId, paymentId))). This can leak another tenant's delivery row for the same payment id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/embeddings.ts:684** _(system-sweep)_ — UPDATE to tenant-scoped tables (memory_entries / agent_knowledge) performed without constraining by tenant_id. storeEmbeddingVec() updates rows by id only (no tenant_id check).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/embeddings.ts:687-710 backfillMissingKnowledgeEmbeddings scans autoresearch rows missing embeddings and writes only the embedding column via storeEmbeddingVec; housekeeping maintenance, no tenant-facing return.
- **server/heartbeat.ts:2628** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant constraint after creating a new task. If ids collide or are mis-bound, this can touch another tenant's row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3079** _(internal-worker-pk)_ — Another occurrence: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat path) lacks tenant_id in the WHERE clause, permitting cross-tenant updates.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/internal-resolver.ts:184** _(internal-worker-pk)_ — finalizeEvent called without passing tenantId (UPDATE event_log executes with no tenant constraint). This will update event_log rows across tenants when the function falls back to the branch that lacks tenant scoping.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): fail-closed branch: event id came from the resolver's own dropped-event sweep; archives the event without touching tenant data.
- **server/internal-resolver.ts:229** _(internal-worker-pk)_ — finalizeEvent called without passing tenantId (UPDATE event_log executes with no tenant constraint). This archives an orphan delivery event but does not constrain the update to the event's tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): orphan-delivery archival; event id from the resolver's own sweep, terminal-status transition only.
- **server/internal-resolver.ts:259** _(internal-worker-pk)_ — finalizeEvent called without tenantId when finalizing unknown/unsupported events. The UPDATE may affect rows across tenants because no tenant filter is supplied.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): no-handler archival; event id from the resolver's own sweep.
- **server/job-queue.ts:273** _(internal-worker-pk)_ — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint, allowing cancellation of jobs belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:283** _(internal-worker-pk)_ — retryJob resets attempts/status on agent_jobs WHERE id = ${id} AND status IN (...) with no tenant_id constraint, enabling cross-tenant mutation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:320** _(system-sweep)_ — listJobs constructs and emits a SELECT FROM agent_jobs without a tenant_id constraint if the caller does not pass tenantId. That returns/reads job rows for all tenants by default.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/job-queue.ts:385** _(system-sweep)_ — getJobStats runs an aggregate SELECT on agent_jobs WHERE created_at > ... with no tenant_id filter, returning cross-tenant counts.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/lib/agent-trace.ts:129** _(internal-worker-pk)_ — UPDATE to tenant-scoped table agent_trace_spans uses WHERE span_id = ${spanId} with no tenant_id predicate. This allows an UPDATE to affect another tenant's row if span_id collisions or misuse occur.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/lib/agent-trace.ts:129 — persistSpanClose updates agent_trace_spans WHERE span_id = ${spanId}. spanId is an internally generated, in-process trace context id (ctx.spanId), never accepted from a request or attacker. Each close is bound to a span this same process opened; no attacker-influenced input reaches the WHERE clause, so no cross-tenant write is reachable.
- **server/lib/bwb-job-progress.ts:144** _(internal-worker-pk)_ — UPDATE on tenant-scoped table video_jobs uses .where(scope(jobId,...)) where scope can omit tenant_id when BWB_TENANT_ID threading is missing. That results in writes that are only pinned by jobId (no tenant predicate), enabling cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:169** _(internal-worker-pk)_ — UPDATE on tenant-scoped table video_jobs (updateBwbChapters) calls .where(scope(...)) but scope may omit tenant_id if BWB_TENANT_ID is not set, allowing cross-tenant writes keyed only by jobId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:203** _(internal-worker-pk)_ — SELECT ... FROM video_jobs uses .where(scope(jobId)) but scope may omit tenant_id when BWB_TENANT_ID threading is missing, causing cross-tenant reads (and subsequent writes based on that read) to occur.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): jobId is an unguessable random id (vj_<base36>_<6-byte-hex>); scope() warns once when BWB_TENANT_ID unthreaded — documented defense-in-depth.
- **server/lib/bwb-job-progress.ts:211** _(internal-worker-pk)_ — UPDATE ... WHERE scope(jobId) used to mark job done; scope may omit tenant predicate resulting in an unscoped UPDATE against video_jobs (cross-tenant risk).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): jobId is an unguessable random id (vj_<base36>_<6-byte-hex>); scope() warns once when BWB_TENANT_ID unthreaded — documented defense-in-depth.
- **server/lib/bwb-job-progress.ts:230** _(internal-worker-pk)_ — UPDATE video_jobs .where(scope(jobId, { notDone: true, notFailed: true })) used by bumpBwbHeartbeat can run without a tenant_id predicate if BWB_TENANT_ID is missing, allowing cross-tenant row updates.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:254** _(internal-worker-pk)_ — UPDATE video_jobs .where(scope(jobId, { notDone: true })) in failBwbJob may omit tenant_id when BWB_TENANT_ID is not set, permitting cross-tenant modifications keyed only by jobId.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): failBwbJob pinned to the same unguessable random jobId; documented warn-once fallback.
- **server/lib/system-probes.ts:163** _(upstream-verified)_ — COUNT(*) query against tenant-scoped messages table without a tenant_id constraint — aggregates messages across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Reclassified by main-agent review: only callsite (tools.ts check_system_status) is admin-gated (ADMIN_TENANT_ID) two lines above the call. server/lib/system-probes.ts:163 db.select(count(*)).from(messagesTable) has no tenant filter; messages has tenant_id (shared/schema.ts:142-150). checkSystemStatus (119) is exposed as the check_system_status tool (server/tools.ts:3501, definitions.ts:24-31, 
- **server/memory-graph.ts:140** _(upstream-verified)_ — refreshCategoryCount performs tenant-unguarded operations: the inner SELECT counting memory_entries lacks tenant_id and the outer UPDATE to memory_categories lacks tenant_id (both in the same SQL block).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/orchestrator-ledger.ts:450** _(upstream-verified)_ — Using sql.raw(${lockKey}) to embed a value derived from opts.ledgerKey (caller-provided) into SQL: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). This uses sql.raw() with request-derived data and bypasses parameter binding (injection/isolation risk).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): pg_advisory_xact_lock is a lock, not a tenant table. lockKey is a purely numeric BigInt(sha1).toString() (server/orchestrator-ledger.ts:35-38) — no injectable text. The ledger read/write in the same tx are tenant-scoped ('AND tenant_id = ${tenantId}' at :448).
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} has no tenant constraint; projectId is a foreign id provided to the function and ownership is not verified.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT ... FROM code_proposals WHERE id = ${proposalId} does not verify tenant ownership or include tenant_id. The verifier reads proposal rows by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/proposal-verifier.ts:31-35 SELECT by proposalId; every caller is internal (server/agentic/autonomous-closer.ts:86-103 with tenant-scoped candidate ids, plus scripts/reverify-proposals.ts and scripts/drain-and-verify-proposals.ts). No request/tool-arg id path.
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE sculptor_sessions (monitorSession) uses WHERE id = ${sessionId} without tenant scoping. monitorSession is passed tenantId but the UPDATE does not enforce it, enabling cross-tenant state changes.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} lacks tenant scoping. settleFusionReservation can therefore update ledger rows belonging to other tenants if a reservationId is misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:424** _(internal-worker-pk)_ — UPDATE agent_cost_ledger SET cost_usd = '0' ... WHERE id = ${reservationId} (releaseFusionReservation) does not include tenant_id, allowing cross-tenant ledger modifications.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/storage.ts:803** _(internal-worker-pk)_ — UPDATE memory_entries sets embedding WHERE id = ${id} with no tenant filter (updateMemoryEmbedding). memory_entries is tenant-scoped — this is an unscoped write.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:815** _(internal-worker-pk)_ — UPDATE agent_knowledge sets embedding WHERE id = ${id} with no tenant filter (updateKnowledgeEmbedding). agent_knowledge is tenant-scoped — this is an unscoped write.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:823** _(system-sweep)_ — SELECT memory_entries without tenant scoping in getMemoriesWithoutEmbeddings (no tenantId parameter/WHERE tenant_id = ...). This reads tenant-scoped rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getMemoriesWithoutEmbeddings only caller is the background embedding backfill heartbeat.ts:1912; rows feed embedding generation (updateMemoryEmbedding) and are never returned to a tenant-facing surface.
- **server/storage.ts:828** _(system-sweep)_ — SELECT agent_knowledge without tenant scoping in getKnowledgeWithoutEmbeddings (no tenantId parameter/WHERE tenant_id = ...). This reads tenant-scoped rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getKnowledgeWithoutEmbeddings only caller is heartbeat.ts:1913 background embedding worker; rows only drive updateKnowledgeEmbedding, not returned to any tenant surface.
- **server/storage.ts:916** _(system-sweep)_ — getHeartbeatTasks returns all heartbeat_tasks when tenantId is undefined (no tenant filter). heartbeat_tasks is tenant-scoped; this reads across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getHeartbeatTasks() no-arg callers are internal cron/maintenance (heartbeat.ts:214,280,1038,2877; stability-watchdog.ts:193) and the platform-admin status route (routes/heartbeat.ts:142-144 requirePlatformAdmin). Each returned task carries its own tenantId; no req-param path.
- **server/storage.ts:934** _(upstream-verified)_ — getHeartbeatTask selects heartbeat_tasks WHERE id = ${id} with no tenant constraint. This reads a tenant-scoped row by id only (unscoped read).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getHeartbeatTask(id) callers routes/heartbeat.ts:103 and :121 immediately verify Number(task.tenantId)!==tenantId → 404 (heartbeat.ts:105 and :123) before any use, and are behind requireAdmin.
- **server/storage.ts:965** _(upstream-verified)_ — deleteHeartbeatTask deletes from heartbeat_tasks with WHERE id = ${id} when tenantId is omitted (no tenant filter). This allows cross-tenant deletion of tasks.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): deleteHeartbeatTask only caller is routes/heartbeat.ts:81 which always passes tenantId → tenant subselect scope applied (storage.ts:966); no unscoped caller exists.
- **server/storage.ts:768** _(upstream-verified)_ — createKnowledge inserts into tenant-scoped table agent_knowledge using caller-supplied `data` but does not assert/validate tenantId is present or owned. An INSERT without explicit tenant validation risks missing tenant_id or being set from an untrusted source.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:768 — createKnowledge() inserts a typed InsertKnowledge with tenantId supplied by callers; agent_knowledge.tenantId is NOT NULL so an unscoped insert fails closed at the DB. Callers pass authenticated tenantId (routes.ts:6240 `{...parsed.data, tenantId}`, doc-collections, knowledge/handlers.ts). Additionally the path already enforces a reserved-category guard against the act
- **server/storage.ts:530** _(upstream-verified)_ — createMemoryEntry inserts into memory_entries without validating that data.tenantId is present/authorized. This allows accidental unscoped writes if callers omit tenantId or pass an incorrect tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/storage.ts:585** _(upstream-verified)_ — createConversationFact inserts into conversation_facts using caller-supplied `data` but does not explicitly validate tenantId. INSERT may be missing tenant_id or be set from an untrusted source.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): conversation_facts.tenant_id NOT NULL with no default; InsertConversationFact requires tenantId at compile time.
- **server/storage.ts:239** _(upstream-verified)_ — getConversationUnscoped performs a SELECT FROM conversations WHERE id = ${id} with NO tenant filter (explicitly unscoped helper). Although logged, this function returns tenant-scoped rows without tenant constraint and can be used incorrectly in handlers.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/tools.ts:4566** _(internal-worker-pk)_ — Querying tenant-scoped table `presenter_sessions` without enforcing tenant isolation. The code runs: SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ... — there is no WHERE tenant_id = <tenant> constraint and no prior ownership assertion for that presentation_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:4537 — The presenter_sessions SELECT keys on slideResult.presentationId, which was just created by this same create_slides tool invocation for the caller's own tenant (conversation ownership is asserted upstream at ~4500 via 'conversation not owned by tenant' and assertProjectInTenant). The presentation id is not attacker-supplied cross-tenant input; it is a self-generated art
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from agent_knowledge (messaging_pairing) executed without constraining by tenant_id. This queries a tenant-scoped table (agent_knowledge) across tenants and relies on subsequent application logic to validate ownership, violating the invariant that every read must include tenant filtering.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/wiring-invariants.ts:417** _(system-sweep)_ — SELECT from `tool_performance` (tool-drift detector) lacks any tenant_id constraint (db.execute(sql`SELECT ... FROM tool_performance WHERE last_failure_at > ...`)). This aggregates tenant-scoped metrics across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): wiring-invariants.ts:417 tool_drift SUM aggregate; platform health call path, no tenant-facing row output
- **server/lib/scheduled-post-runner.ts:319** _(deferred)_ — SQL array literal `arrLit` is constructed by string-concatenating user-supplied platform names and then interpolated into the SQL query (VALUES (${params.tenantId}, ${arrLit}::text[], ...)). This is a SQL-injection risk because user data is embedded into a query string literal.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): hand-built text[] literal from allowlist-checked platforms — injection-resistant today; refactor to parameterized bind (deferred item 5, campaign 2026-07-31).
- **server/personality-files.ts:8** _(deferred)_ — Table definition for personality_files declares tenant_id INTEGER NOT NULL DEFAULT 1. The DEFAULT allows INSERTs to omit tenant_id and implicitly attribute rows to tenant id 1 (admin), violating the rule that every INSERT must explicitly pass tenant_id.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): runtime CREATE TABLE personality_files tenant_id NOT NULL DEFAULT 1 — drop-default migration needed (deferred item 3, campaign 2026-07-31).
- **server/agentic/task-forces.ts:40** _(deferred)_ — createTaskForce accepts a projectId from the caller and inserts it into task_forces without first verifying that the referenced project belongs to the caller's tenant.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): createTaskForce inserts caller-supplied projectId without verifying project→tenant ownership; hardening = assertProjectInTenant before insert (row itself is tenant-stamped; reads are tenant-filtered, so exposure is a dangling FK association).
- **server/storage.ts:959** _(deferred)_ — updateHeartbeatTask uses WHERE id = ${id} (and conditionally a subselect when tenantId provided). If tenantId is omitted this UPDATE runs unscoped and can modify another tenant's heartbeat_task.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): updateHeartbeatTask falls back to an unscoped UPDATE when tenantId omitted — same optional-tenant fail-open family as getMemoryStats/getHeartbeatTasks (new deferral, campaign 2026-08-01 pass 2).
