# Tenant-Isolation Audit Report

_Generated 2026-09-11T23:04:06.489Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 480 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **190/670** file(s) audited · chunks 20/62 OK (0 failed, 42 pending)
- Findings: **74** (CRITICAL 16 · HIGH 47 · MEDIUM 9 · LOW 2)
- Severe dispositions: **63 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (16)

- **server/agentic/resume-worker.ts:65** — SELECT against tenant-scoped tables (agent_runs JOIN agent_approvals) with NO tenant_id constraint — the query scans rows across all tenants and can leak cross-tenant data.
  - _Fix:_ Restrict the query to a specific tenant or iterate per-tenant. Add an explicit tenant_id filter (e.g. WHERE r.tenant_id = ${tenantId}) or rewrite using Drizzle with .where(eq(agentRuns.tenantId, tenantId)) and ensure the joined approvals are also constrained to the same tenant. If the worker must operate across tenants, enumerate tenants and run a tenant-scoped query per tenant rather than a global, unconstrained SELECT.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table `commitments` omits tenant_id in WHERE clause. The statement UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} can modify a row belonging to another tenant if id is attacker-controlled or incorrect.
  - _Fix:_ Always include tenant scoping in mutation WHERE clauses. Change the statement to include AND tenant_id = ${row.tenantId} (or equivalent): UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}. This enforces tenant isolation and prevents cross-tenant updates.
- **server/deep-interview.ts:337** — INSERT into project_notes omits tenant_id. The INSERT uses a SELECT to derive project_id but does not supply tenant_id on the inserted row, violating the invariant that every tenant-scoped INSERT must set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT. For example: INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, ${state.tenantId}, ${briefContent}, NOW() FROM ... WHERE ... AND p.tenant_id = ${state.tenantId} LIMIT 1. Alternatively perform the insert within a tenant-scoped transaction (withTenantTx) and still explicitly set tenant_id on the row to avoid relying on implicit ownership.
- **server/embeddings.ts:806** — storeEmbeddingVec falls back to a raw UPDATE without a tenant filter when options.tenantId is undefined (UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}). This allows unscoped writes to tenant-scoped tables (memory_entries / agent_knowledge).
  - _Fix:_ Remove the unscoped fallback. Require a positive tenantId for updates to tenant-scoped tables and always include WHERE tenant_id = ${tenantId}. If a global/admin migration must run unscoped, provide a distinct, clearly-audited admin path and do not expose it to normal callers.
- **server/felix-brain.ts:23** — Query against tenant-scoped table `agent_runs` executed without constraining by tenant_id. The boot-time rehydration SELECT pulls rows for all tenants (no WHERE tenant_id = ...), allowing cross-tenant data exposure into in-memory task state.
  - _Fix:_ Restrict the query to the intended tenant(s). Add a tenant_id filter to the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND ...), or iterate over an explicit, validated tenant list and run the select per-tenant. Never load rows for all tenants into a per-tenant in-memory store.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' uses WHERE eq(projects.id, projectId) with no tenant_id constraint. This updates project rows without constraining to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or include the tenant check in the SQL so the update can only affect rows owned by the validated tenant.
- **server/google-drive.ts:1664** — INSERT into tenant-scoped table 'project_files' does not set tenant_id. The raw INSERT inserts project_id and other columns but omits tenant_id, violating the requirement that every INSERT must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT and bind it from a validated/trusted tenant variable (not user input). Example: INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${params.projectId}, ${validatedTenantId}, ${params.fileName}, ...). Ensure validatedTenantId was confirmed via ownership checks.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id — inserting into a likely tenant-scoped table using only session_id/foreign key without validating ownership or stamping tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT ... (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)). Before persisting, validate that sessionId belongs to the expected tenant (SELECT tenant_id FROM presenter_sessions WHERE id = ${sessionId}) and fail if it does not. Do not rely on the session_id FK alone for isolation.
- **server/job-queue.ts:160** — UPDATE on tenant-scoped table 'agent_jobs' without any tenant_id constraint (claimDueJobs). This atomically claims jobs across all tenants (UPDATE agent_jobs ... WHERE id IN (SELECT id FROM agent_jobs ... LIMIT ${limit} FOR UPDATE SKIP LOCKED)), allowing cross-tenant mutation/read.
  - _Fix:_ Restrict claims to an explicit tenant scope. Either (a) run claim loops per-tenant (SELECT tenant ids then claim jobs WHERE tenant_id = <tenant> FOR UPDATE SKIP LOCKED), or (b) require workers to supply an allowed tenant list and add AND tenant_id = ${tenantId} to the inner SELECT/outer UPDATE. Ensure the claim logic never updates rows belonging to other tenants.
- **server/job-queue.ts:195** — UPDATE on tenant-scoped table 'agent_jobs' without tenant_id constraint (reclaimExpiredLeases step 1). This may mark jobs from any tenant as failed_terminal based only on global lease expiry criteria.
  - _Fix:_ Include tenant_id scoping in the UPDATE or perform reclamation per-tenant. If reclamation must be global (operator/admin action), require an explicit admin scope or a controlled background task with documented admin privileges and audit logging. Prefer per-tenant reclamation to preserve isolation.
- **server/job-queue.ts:209** — UPDATE on tenant-scoped table 'agent_jobs' without tenant_id constraint (reclaimExpiredLeases step 2). Re-queues expired jobs across all tenants back to pending without tenant isolation.
  - _Fix:_ Scope the UPDATE by tenant_id (or perform per-tenant requeue passes). Alternatively, iterate tenants and run a tenant-scoped UPDATE for each to avoid cross-tenant effects.
- **server/job-queue.ts:238** — UPDATE on tenant-scoped table 'agent_jobs' without tenant_id constraint (completeJob). A worker can complete any job id if status/attempts match, without verifying the tenant ownership of the caller/context.
  - _Fix:_ Require caller to pass tenantId (or validate current execution tenant) and add AND tenant_id = ${tenantId} to the WHERE clause for completeJob. Enforce lease-fencing + tenant fencing together so stale/forked writers cannot modify other tenants' rows.
- **server/job-queue.ts:281** — SELECT on tenant-scoped table 'agent_jobs' without tenant_id constraint (failJob looks up max_attempts by id). This lets a caller infer/operate on another tenant's job record and drives subsequent unscoped UPDATEs.
  - _Fix:_ Include tenant_id in the SELECT (require tenantId param) or fetch max_attempts as part of a tenant-scoped SELECT WHERE id = ${id} AND tenant_id = ${tenantId}. Similarly, all subsequent UPDATEs in failJob must include AND tenant_id = ${tenantId}.
- **server/job-queue.ts:298** — UPDATE on tenant-scoped table 'agent_jobs' without tenant_id constraint (failJob terminal branch). The function mutates job state (failed_terminal) without tenant scoping in WHERE, enabling cross-tenant state change.
  - _Fix:_ Add tenant_id checks to the UPDATE (AND tenant_id = ${tenantId}) and require the caller to supply/verify tenantId. If the caller is a worker, run the handler inside a tenant context or read the job's tenant_id first and validate against the worker's allowed scope before mutating.
- **server/job-queue.ts:318** — UPDATE on tenant-scoped table 'agent_jobs' without tenant_id constraint (failJob retry branch). The retry scheduling backoff update is unscoped and can affect other tenants' jobs.
  - _Fix:_ Require tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) and derive/validate tenantId from a trusted source before changing next_run_at/status.
- **server/job-queue.ts:336** — UPDATE on tenant-scoped table 'agent_jobs' without tenant_id constraint (cancelJob). This cancels a job by id with no tenant filter, allowing cancellation of jobs from other tenants.
  - _Fix:_ Require tenantId for cancelJob or add AND tenant_id = ${tenantId} to the WHERE clause. If this API is intended for operators, ensure it's guarded by admin authorization and audit logging; tenant-facing routes must only cancel jobs in their own tenant.

### HIGH (47)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects from experiments without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across all tenants.
  - _Fix:_ Scope the query to a single tenant (e.g. accept a tenantId parameter and add WHERE tenant_id = ${tenantId}), or iterate tenants and run per-tenant queries. Never select tenant-scoped rows across tenants in a single query.
- **server/agent-activity.ts:122** — completeActivity updates agent_activity by id only (WHERE id = activityId) with no tenant_id constraint, allowing an update to affect a row belonging to another tenant if the id is guessed/ reused.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) or require the caller to pass tenantId and validate ownership before updating.
- **server/agent-eval.ts:218** — runEval updates agent_evals using WHERE id = ${evalId} with no tenant_id constraint (UPDATE agent_evals ... WHERE id = ${evalId}). This can update another tenant's eval row if id is known.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. add AND tenant_id = ${tenantId} in the WHERE clause) or use a parameterized Drizzle update with eq(agent_evals.tenantId, tenantId) to ensure tenant ownership.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting selects ai_insights rows WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER WITHOUT a tenant_id filter — reading rows across tenants.
  - _Fix:_ Restrict the sweep to a tenant (accept tenantId or iterate known tenants) and add WHERE tenant_id = ${tenantId} or otherwise ensure each query is explicitly tenant-scoped to prevent cross-tenant reads.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights builds and runs an UPDATE against ai_insights that may execute without a tenant constraint when tenantId parameter is omitted (tenantScope becomes empty), causing writes across tenants: UPDATE ai_insights ... WHERE status='new' AND category IN (...) ${tenantScope}.
  - _Fix:_ Avoid running unscoped APPLY/UPDATE across tenants. Require a tenantId (or explicitly document/require an allowlist) and always include WHERE tenant_id = ${tenantId}. If a global sweep is intended, perform per-tenant transactions that explicitly include tenant_id in the WHERE clause.
- **server/agentic-features.ts:861** — enrollInSequence checks for existing outreach_enrollments using SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' WITHOUT scoping by tenant_id — allowing cross-tenant enrollment checks to leak or incorrectly block enrollments.
  - _Fix:_ Include tenant scoping in the duplicate check (AND tenant_id = ${params.tenantId}). Always include tenant_id in SELECTs on tenant-scoped tables when the caller's tenant is known.
- **server/agentic-features.ts:1021** — classifyReply updates outreach_enrollments by id only (UPDATE outreach_enrollments SET ... WHERE id = ${row.id}) without a tenant_id constraint, permitting modification of an enrollment row belonging to another tenant if the id is known or raced.
  - _Fix:_ Constrain UPDATEs with tenant_id (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}). Even when the row was just selected, use tenant-scoped WHERE to prevent TOCTOU or id-guessing cross-tenant updates.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to set status='expired' WHERE status='pending' AND expiresAt < now() with no tenant_id constraint — this updates pending approvals across all tenants.
  - _Fix:_ Scope expiry updates per-tenant (add tenant filter) or iterate tenants and run tenant-scoped updates. If a global sweep is required, perform it as a controlled per-tenant operation that includes WHERE tenant_id = ${tenantId} to preserve tenant isolation.
- **server/agentic/department-budgets.ts:142** — A SELECT reads the tenant-scoped table `department_budgets` without constraining by tenant_id: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This performs a cross-tenant read of a tenant-scoped table.
  - _Fix:_ If this is not meant to scan all tenants, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}). If the sweep across tenants is intentional, make that explicit (admin/system-only operation) and ensure the code runs under a properly authorized admin principal and is auditable. Alternatively, iterate per-tenant and query each tenant_id with an explicit WHERE tenant_id = ${tid} to preserve tenant-isolation in application-level filters.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table agent_wake_schedules performed without any tenant_id constraint (subquery selects pending rows across all tenants). This allows the heartbeat to claim/modify schedules belonging to any tenant.
  - _Fix:_ Constrain the UPDATE/subquery by tenant_id (e.g. process wakes per-tenant or add AND tenant_id = ${tenantId} to the subquery and outer UPDATE). If the job must be global, add an explicit, well-audited platform-admin guard and clearly document why cross-tenant mutations are required, but prefer per-tenant processing to preserve isolation.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT from conversations queries project-scoped/tenant-scoped rows without any tenant_id filter, enumerating conversations across all tenants for backfill.
  - _Fix:_ Restrict the backfill query to a specific tenant (add WHERE c.tenant_id = ${tenantId}) or iterate tenants explicitly under a platform-admin, audited, and throttled maintenance path. Avoid broad cross-tenant selects that expose or drive writes into other tenants' projects.
- **server/auto-asset-capture.ts:207** — Selecting messages for a conversation (SELECT content FROM messages WHERE conversation_id = ${c.id} ...) omits tenant_id in the WHERE clause, so messages may be read across tenants if conversation ids collide or are misused.
  - _Fix:_ Include tenant scoping in the query (AND tenant_id = ${c.tenant_id}) or otherwise ensure the query is executed within a tenant-context that guarantees the conversation belongs to that tenant before reading messages.
- **server/channel-routing.ts:30** — Query reads from the tenant-scoped personas table via a JOIN (JOIN personas p ON p.id = cr.persona_id) without any tenant_id constraint — this can return persona records from other tenants.
  - _Fix:_ Require a tenant context (pass tenantId into listChannelRoutes) and add a WHERE clause filtering personas by p.tenant_id = <tenantId>, or make channel_routes tenant-scoped (add tenant_id column) and join/filter on that tenant_id so only same-tenant personas are returned.
- **server/channel-routing.ts:48** — setChannelRoute accepts a personaId from caller input and INSERTs/UPDATES it into the global channel_routes table without validating that the personaId belongs to the caller's tenant — allows cross-tenant reference/injection of foreign persona IDs.
  - _Fix:_ Validate that the provided personaId is owned by the caller's tenant before inserting/updating (e.g. SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = <callerTenant>). Alternatively, make channel_routes tenant-scoped (add tenant_id column and include it in INSERT/ON CONFLICT) so mappings are per-tenant and require tenantId in the API.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` has no tenant_id constraint — query only filters by project_id. Relying on prior projectId validation in-app is not an allowed substitute for an explicit SQL tenant filter.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join against projects and assert p.tenant_id = ${tenantId} in the same statement. Every read against a tenant-scoped table must include the caller's tenant scope in SQL.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` has no tenant_id constraint — query only filters by project_id.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or otherwise include an equivalent Drizzle where(eq(table.tenantId, tenantId)). Do not rely on prior in-memory validation alone — the SQL must enforce tenant scope.
- **server/cmmc-prospect-artifacts.ts:93** — Use of inline SQL expression with request-derived value in tx.execute(sql`SELECT pg_advisory_xact_lock(hashtext(${`cmmc-prospect-artifact:${params.tenantId}:${filename}`}))`) — constructing a DB-side lock key from untrusted input risk (injection / cross-tenant lock semantics) and is treated as string-interpolated SQL in the audit invariant.
  - _Fix:_ Avoid composing DB-side SQL identifiers/expressions from raw request-derived values. Compute a safe, deterministic lock key server-side (e.g. hash with crypto on the server and pass it as a parameter), or pass tenantId and a server-sanitized artifact id as separate parameters to a parameterized query. Ensure any value embedded into sql`` is parameterized and sanitized, or use a numeric advisory key derived in server code rather than string-concatenating untrusted input into SQL.
- **server/commitments.ts:135** — SELECT against tenant-scoped table `commitments` without tenant_id constraint. The heartbeat scanner runs a SELECT that reads commitments across all tenants (SELECT id, tenant_id, description, ... FROM commitments WHERE status = 'active' ... ) which returns tenant-scoped rows without restricting to a caller tenant.
  - _Fix:_ If this is intended admin/operational behavior, ensure it is executed under an explicit, logged admin context and avoid selecting or exposing tenant-scoped free-text columns (e.g. description). Otherwise, add tenant scoping (WHERE tenant_id = ${tenantId}) or use an admin-only audit path that is explicitly reviewed, or maintain a separate admin index/table that permits safe cross-tenant scanning.
- **server/data-protection.ts:150** — SELECT over conversations (expired soft-deletes) with no tenant_id predicate: SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}. This reads tenant-scoped rows across all tenants without an explicit tenant constraint.
  - _Fix:_ If this is intended to be per-tenant, add a WHERE tenant_id = ${tenantId} clause or run the operation within an explicit admin flow that enumerates tenant IDs safely. If this is an admin-wide purge, ensure it's executed under a controlled admin context (e.g. withTenantTx + explicit authorization) and audit that cross-tenant reads are acceptable. Prefer per-tenant iteration using app-level tenant predicates.
- **server/data-protection.ts:360** — SELECT from project_notes lacks a tenant_id filter: SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} ... — no tenant predicate is applied to this tenant-scoped table read.
  - _Fix:_ Constrain the query to the caller tenant (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or include tenant_id in the SELECT source and use it in the WHERE. Always add an explicit tenant predicate even when project_id was looked up previously.
- **server/data-protection.ts:371** — SELECT from project_files lacks a tenant_id filter: SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} ... — tenant predicate missing on a tenant-scoped table read.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId}) or join via a project row that is constrained to tenant and select project_files with WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} to enforce isolation.
- **server/data-protection.ts:404** — COUNT(*) query against messages omits tenant predicate: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}. This reads a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or run the check inside a tenant-scoped transaction/operation to guarantee the messages counted belong to the caller's tenant.
- **server/doc-collections.ts:166** — DELETE from tenant-scoped table `doc_chunks` is missing a tenant_id constraint (WHERE tenant_id = ...). The DELETE uses doc_path + collection_id only, but the invariant requires explicit tenant scoping on every write against tenant-scoped tables.
  - _Fix:_ Include the tenant filter in the DELETE to enforce isolation, e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. (Also prefer using the previously-validated tenantId variable rather than relying on collection_id alone.)
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table `doc_chunks` uses only WHERE id = ${row.id} and omits tenant_id. Even though the selected rows originated from a tenant-scoped query, the UPDATE should still include an explicit tenant constraint to prevent cross-tenant writes if IDs are ever misused or a bug/untrusted id is provided.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE doc_chunks SET embedding = ${JSON.stringify(embedding)}::jsonb WHERE id = ${row.id} AND tenant_id = ${tenantId}. Use the function's tenantId parameter to bind the tenant explicitly.
- **server/email.ts:370** — SELECT from tenant-scoped table `inbox_messages` without constraining tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can read/misattribute rows belonging to other tenants.
  - _Fix:_ Include tenant context in the query (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or otherwise scope by inbox_id/tenant_id. Prefer a tenant-aware lookup helper so callers must supply the tenant before querying inbox_messages.
- **server/email.ts:415** — SELECT from tenant-scoped table `inbox_messages` without any tenant_id or inbox_id constraint (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id). This reads rows across tenants.
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or to the specific inbox (WHERE inbox_id = ${inboxId} AND tenant_id = ${tenantId}). Avoid scanning the whole table without explicit tenant scoping.
- **server/email.ts:429** — UPDATE against tenant-scoped table `inbox_messages` lacks tenant constraint (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). This can update another tenant's row if id collisions occur.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or include inbox_id in the WHERE clause. Ensure the tenantId used comes from a trusted source (e.g. a prior tenant-scoped SELECT or function parameter).
- **server/embeddings.ts:842** — SELECT from tenant-scoped table `memory_entries` without tenant_id constraint (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200). This reads rows across all tenants.
  - _Fix:_ Add tenant scoping to the SELECT (WHERE tenant_id = ${tenantId}) or iterate per-tenant in an admin-only migration helper. Ensure any subsequent UPDATE includes the same tenant constraint.
- **server/embeddings.ts:852** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} executed without tenant_id constraint. This can overwrite embedding_vec for rows belonging to other tenants.
  - _Fix:_ Use a tenant-scoped UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). If the backfill is intended to be global, make it an explicit admin-only routine that requires admin credentials and documents the cross-tenant operation.
- **server/embeddings.ts:858** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint (SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200). This reads rows across tenants.
  - _Fix:_ Restrict query by tenant (WHERE tenant_id = ${tenantId}) or perform per-tenant backfill via an admin-controlled loop. Avoid blanket selects across tenant-scoped tables in normal code paths.
- **server/embeddings.ts:869** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed without tenant_id constraint. This can modify rows belonging to other tenants.
  - _Fix:_ Ensure the UPDATE includes AND tenant_id = ${tenantId}, or call storeEmbeddingVec with a validated tenantId to perform a tenant-scoped update. If running a global backfill, make the admin intent explicit and gated.
- **server/embeddings.ts:811** — backfillMissingKnowledgeEmbeddings SELECTs rows from `agent_knowledge` (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE ...) with no tenant constraint. Even though tenant_id is returned and later used, the initial cross-tenant SELECT violates the tenant-scoping invariant.
  - _Fix:_ Avoid scanning agent_knowledge across all tenants in normal code. Either accept a tenantId parameter and restrict the SELECT to that tenant, or implement an explicit admin migration tool that documents and requires elevated privileges.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant_id constraint. Although 'existing' came from a prior tenant-scoped SELECT, the UPDATE itself is not scoped and could modify another tenant's row if ids are mis-bound.
  - _Fix:_ Include the tenant constraint on the UPDATE. e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or otherwise assert ownership immediately before the UPDATE and add tenantId to the WHERE clause.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) with no tenant_id constraint (inside crawl loop).
  - _Fix:_ Scope the UPDATE to the caller tenant by adding eq(scrapedPages.tenantId, tenantId) to the WHERE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Verify ownership before update and fail-closed if it doesn't match.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint — updating a tenant-scoped row by id only can modify another tenant's session.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or validate ownership first (SELECT tenant_id FROM presenter_sessions WHERE id = ${sessionId} and confirm it matches tenantId) before performing the update.
- **server/health-audit.ts:314** — SELECT FROM code_proposals with no tenant_id filter — reading tenant-scoped proposals across all tenants (and later acting on them) without scoping to a tenant.
  - _Fix:_ Either scope this query to a tenant (WHERE tenant_id = ${tenantId}) when run in tenant-context, or require an explicit, authenticated admin context for cross-tenant operations. If this is intended to run per-tenant, iterate tenant IDs and run the query per-tenant; never read tenant-scoped tables globally from unscoped handlers.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) — performing an UPDATE on a tenant-scoped table without constraining tenant_id, which may modify rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or build the ids as pairs of (id, tenant_id) and update only matching tenant rows. If this is an admin operation, require and validate an explicit admin context before running cross-tenant updates.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE ... (next_run_at <= NOW()) lacks a tenant_id constraint — this pulls scheduled jobs across all tenants without tenant scoping before processing.
  - _Fix:_ Scope the SELECT by tenant_id when running per-tenant work (e.g. WHERE tenant_id = ${tenantId} AND ...), or run a separate admin-permitted scheduler that explicitly documents cross-tenant behavior. Before performing tenant-scoped updates, always include AND tenant_id = ${sched.tenant_id} (or validate ownership) to avoid cross-tenant effects.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks is executed without any tenant_id filter (raw SQL selection across a tenant-scoped table). This reads tenant-scoped rows with no application-level tenant constraint.
  - _Fix:_ Restrict the query to an explicit tenant context (e.g. add WHERE tenant_id = ${tenantId}) or otherwise document/implement an auditable cross-tenant admin path. Prefer using storage helpers that assert tenant context or pass an explicit ALL_TENANTS sentinel and validate it's intentionally administrative.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE heartbeat_tasks ... WHERE id = ${id} is executed with no tenant_id constraint. An UPDATE to a tenant-scoped table by id only can modify another tenant's row if the id is guessable or mis-associated.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or resolve and validate the tenant before updating. Alternatively use a storage-level updateHeartbeatTask API that enforces/asserts tenant context.
- **server/heartbeat-startup.ts:76** — Raw SQL UPDATE on heartbeat_tasks (SET model = ...) uses WHERE id = ${t.id} with no tenant_id constraint. This writes a tenant-scoped table without enforcing the task's tenant ownership in SQL.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${t.tenantId}) or call the storage/update wrapper that includes tenant scoping/assertion. Ensure cross-tenant admin intentions are explicit and protected.
- **server/heartbeat-startup.ts:80** — Raw SQL UPDATE on heartbeat_tasks (SET model = 'gemini-2.5-flash') uses WHERE id = ${t.id} without tenant_id restriction — a tenant-scoped write lacking tenant constraint.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage API that enforces/validates tenant context before updating.
- **server/heartbeat.ts:256** — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id filter. event_log rows include tenant_id, so this SELECT reads pending events across all tenants with no application-enforced tenant constraint.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant using an explicit allowed admin path. If this is intended cross-tenant behavior, make it explicit and restrict to an admin-only scheduler and auditably validate tenant_id for subsequent writes/updates.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without a tenant_id condition. This can change approval_status for a task id regardless of tenant ownership.
  - _Fix:_ Scope the UPDATE by tenant (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use storage.updateHeartbeatTask/newTask wrapper that enforces the task belongs to the current tenant before changing approval_status.
- **server/heartbeat.ts:3065** — Another raw UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} appears with no tenant_id constraint. This repeats the same tenant-scoping gap when setting approval state.
  - _Fix:_ Require tenant scoping in the UPDATE (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or replace the raw SQL with a storage API that asserts tenant context/ownership before modifying approval_status.
- **server/job-queue.ts:377** — SELECT tenant_id FROM agent_jobs WHERE id = ${id} (getJobTenantForAdmin) — reads a tenant-scoped table by id with no tenant constraint. While the function is documented as an admin lookup, the raw SELECT can be abused if called from tenant-scoped code paths.
  - _Fix:_ Make admin-only intent explicit: restrict this helper to admin-only callers, or require an explicit caller-provided admin token. Prefer returning tenant only after verifying the caller has platform-admin privileges. If used from tenant/tenant-scoped handlers, instead require tenantId param and validate ownership.
- **server/knowledge-diversity-monitor.ts:266** — SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} — scans a tenant-scoped table across all tenants without per-tenant isolation. This background job reads all tenants' agent_knowledge rows unscoped.
  - _Fix:_ If the monitor must run cross-tenant, require this background task to run with explicit admin privilege and strong audit controls. Prefer enumerating tenant ids via a separate admin-only index/table and then invoking per-tenant snapshot routines that use WHERE tenant_id = ${tenantId}. At minimum, document and protect this cross-tenant operation as an admin-only maintenance job.

### MEDIUM (9)

- **server/auth.ts:1327** — DELETE FROM auth_sessions is executed with no WHERE clause (deletes all sessions across all tenants) inside clearAllSessionsAsPlatformAdmin().
  - _Fix:_ Keep this as an explicit platform-admin-only operation only if intended; otherwise restrict deletion to a tenant (WHERE tenant_id = ${tenantId}). If global deletion is intended, ensure the function is strictly guarded, logged/audited, and requires an explicit platform-admin authority token at the API surface.
- **server/channel-routing.ts:61** — getPersonaForChannel returns a persona_id from the global channel_routes table without tenant scoping — the returned ID may reference a persona in another tenant and be used downstream without ownership checks.
  - _Fix:_ Require/accept tenantId and include it in the SELECT (or store tenant_id on channel_routes and filter by it) so the function returns only persona mappings for the caller's tenant; additionally, ensure downstream consumers validate persona ownership before reading tenant-scoped persona rows.
- **server/commitment-drafter.ts:146** — SELECT across tenant-scoped table `commitments` without a tenant_id filter. The query enumerates tenant_id values by scanning the entire commitments table (SELECT DISTINCT tenant_id ...) which reads tenant-scoped data for all tenants without an explicit tenant constraint.
  - _Fix:_ If this background job must discover tenants, ensure the code runs with audited admin privileges and only selects the tenant_id column (avoid selecting tenant-scoped sensitive columns). Prefer an explicit audit-approved admin API or a separate lightweight index/table for tenant enumeration. If this is not an admin-only operation, restrict the SELECT to a single tenant (WHERE tenant_id = ${tenantId}) or otherwise limit fields returned to non-sensitive columns only.
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table `dfy_intake_forms` by token (getDfyIntakeByToken) does not include a tenant_id constraint. This returns a form row purely by token without limiting to a tenant context.
  - _Fix:_ If multi-tenant access-by-token is not intentional, require and use tenantId when looking up forms: SELECT ... FROM dfy_intake_forms WHERE token = ${token} AND tenant_id = ${tenantId}. If global token lookup is intentional (unguessable public token), document the reason clearly and ensure callers validate the security model; otherwise, make the lookup tenant-scoped or provide a tenant-scoped wrapper.
- **server/falsified-routes.ts:174** — SQL uses a string-built array literal in an interpolated sql template: id = ANY({${ids.join(",")}}::int[]) — the code constructs the SQL fragment via string interpolation rather than passing the id array as a parameter. This is a SQL-injection / correctness risk when untrusted or derived values are embedded into SQL text.
  - _Fix:_ Pass the id list as a parameterized array instead of building an array literal string. For example, use ANY(${ids}::int[]) with ids as an actual JS array (or use Drizzle helpers) so the driver parameterizes the values. Avoid manual string-joining to produce SQL syntax from runtime values.
- **server/glasses-gateway.ts:157** — SELECT from apiKeys uses WHERE keyHash=... AND isRevoked=false with no tenant constraint. If apiKeys is a tenant-scoped table this performs a cross-tenant lookup without scoping to a caller tenant.
  - _Fix:_ If apiKeys rows are tenant-scoped, consider including tenant scoping where appropriate or otherwise document and justify the global lookup (auth path). If you keep a global lookup, ensure downstream operations always enforce tenant isolation (e.g. never perform writes based solely on an id returned here without re-checking tenant ownership).
- **server/glasses-gateway.ts:173** — UPDATE on apiKeys sets lastUsedAt with WHERE eq(apiKeys.id, key.id) but does not include a tenant_id constraint. This write to a tenant-scoped table is not tenant-scoped.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))) or include tenantId in the WHERE to ensure the update cannot affect another tenant's row.
- **server/heartbeat-schedules.ts:26** — SELECT DISTINCT tenant_id FROM messages is a query against the messages table (tenant-scoped) with no tenant filter — it reads tenant-scoped data to enumerate tenants.
  - _Fix:_ If the intent is to enumerate tenants, derive the list from a canonical tenants table (SELECT id FROM tenants WHERE ...) rather than scanning messages across all tenants. If scanning messages is required, run with an explicit admin-only code path and document/authorize it; otherwise scope reads to a tenant or perform per-tenant queries.
- **server/job-queue.ts:406** — SELECT FROM agent_jobs with a dynamic whereClause that may omit tenant_id (listJobs). When called without tenantId this function will list jobs across all tenants, performing a read on a tenant-scoped table without tenant restriction.
  - _Fix:_ Require callers to explicitly pass a tenantId (or an ALL_TENANTS constant) and enforce caller authorization for ALL_TENANTS. For tenant-facing endpoints, reject calls that omit tenantId. Log/guard admin usage paths that request global listings.

### LOW (2)

- **server/auth.ts:1335** — Housekeeping DELETE FROM auth_sessions WHERE expires_at < ${now} runs across all tenants without tenant scoping.
  - _Fix:_ Either run per-tenant cleanup (adding tenant_id = ${tenantId}) or, if global cleanup is acceptable, ensure it is executed only from a controlled maintenance context and is auditable. Consider documenting the maintenance intent to avoid accidental cross-tenant effects.
- **server/auth.ts:1339** — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' deletes rows across tenants (global cleanup of legacy data) with no tenant constraint.
  - _Fix:_ If this migration/cleanup must be global, run it as a one-time, documented, and audited migration job. Otherwise, scope deletes to tenants or add explicit platform-admin gating and logging so cross-tenant mutations are intentional and tracked.
