# Tenant-Isolation Audit Report

_Generated 2026-09-12T06:47:02.187Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 251 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **419/670** file(s) audited · chunks 40/62 OK (0 failed, 22 pending)
- Findings: **149** (CRITICAL 23 · HIGH 104 · MEDIUM 21 · LOW 1)
- Severe dispositions: **127 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (23)

- **server/ab-optimizer.ts:110** — SELECT on tenant-scoped table `experiments` performed without any tenant_id filter (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads experiments across all tenants.
  - _Fix:_ Scope this query to a single tenant. Either accept a tenantId and add WHERE tenant_id = ${tenantId}, or iterate tenants explicitly and run a tenant-scoped query per tenant. Do NOT read metadata for other tenants in a single global query.
- **server/agentic/wake-scheduler.ts:146** — Unscoped SELECT against tenant-scoped table agent_wake_schedules inside a multi-row UPDATE. The inner query `SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ...` has no tenant_id filter, so it can claim/modify rows across tenants.
  - _Fix:_ Restrict the claim to a single tenant per run or include an explicit tenant_id predicate. For example, iterate tenants or pass a tenantId and add `AND tenant_id = ${tenantId}` to the inner SELECT (and to the surrounding UPDATE), or perform the claim in a per-tenant lease loop. Ensure every UPDATE/SELECT touching agent_wake_schedules includes WHERE tenant_id = <caller tenant>.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' executed without tenant_id constraint (unscoped write). The statement: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` can modify a row in any tenant if id is provided, violating tenant isolation.
  - _Fix:_ Do not perform unscoped updates. Either skip the unclaim when tenantId is invalid, or include the tenant filter in the WHERE clause. For example: `UPDATE commitments SET draft_status='open', drafted_at=NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId}`. If row.tenantId is missing/invalid, fail-closed (log and skip) rather than issuing an unscoped update.
- **server/deep-interview.ts:337** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT uses INSERT INTO project_notes (project_id, content, created_at) SELECT ... FROM ... — tenant_id is omitted, violating the invariant that every INSERT into a tenant-scoped table must pass tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT values. For example: INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, ${state.tenantId}, ${briefContent}, NOW() FROM ... WHERE ...; (or use p.tenant_id if guaranteed to match state.tenantId). Ensure the tenant_id value is an explicit, trusted integer derived from the caller context.
- **server/embeddings.ts:806** — UPDATE against tenant-scoped table performed without tenant_id constraint and using sql.raw(table): await db.execute(sql`UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}`). When no tenantId is supplied this updates rows across all tenants (or the wrong tenant).
  - _Fix:_ Require a positive tenantId for storeEmbeddingVec and always include AND tenant_id = ${tenantId} in the WHERE clause. Avoid emitting the table name via sql.raw at runtime — instead dispatch to table-specific parameterized queries (e.g. distinct branches for memory_entries and agent_knowledge) and parameterize all values.
- **server/embeddings.ts:842** — backfillEmbeddingVecs selects and updates memory_entries/agent_knowledge without any tenant_id constraint (SELECT id, embedding FROM memory_entries ... LIMIT 200; UPDATE memory_entries SET ... WHERE id = ${row.id}). These reads/writes can operate across tenants and overwrite embedding_vec for rows belonging to other tenants.
  - _Fix:_ Make backfillEmbeddingVecs tenant-aware: either iterate per-tenant and include tenant_id = ${tenantId} in both SELECT and UPDATE, or ensure each UPDATE includes AND tenant_id = <owner tenant> (derived from the selected row) and validate ownership before writing. Avoid global SELECT/UPDATE across tenant-scoped tables.
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot executes a SELECT from agent_runs without filtering by tenant_id. This reads tenant-scoped rows across all tenants and rebuilds in-memory TaskState for every returned row, violating tenant isolation.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId} or accept a tenantId parameter) or iterate per-authorized tenant explicitly. Ensure any rows used to populate in-memory TaskState are filtered to the caller's tenant, and validate row.tenant_id before creating state. If this operation must be global (admin-only), make the admin-only access explicit and audited; otherwise scope the query to the caller's tenant.
- **server/health-audit.ts:314** — SELECT FROM code_proposals reads rows without a tenant_id filter (pulls stale proposals across all tenants).
  - _Fix:_ Scope the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND status IN (...)) or explicitly document and gate as an admin cross-tenant operation. If this is an admin-only sweep, require/validate an admin context before running; otherwise add tenant_id filtering.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates proposals by id only and lacks tenant_id constraint, enabling cross-tenant data modification.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or restrict ids to those owned by the target tenant. If this action must be cross-tenant (admin use), require explicit admin authorization and make that explicit in the code path.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE/SELECT that can claim agent_jobs rows across all tenants: the inner SELECT (SELECT id FROM agent_jobs WHERE status = 'pending' AND next_run_at <= NOW() ...) lacks any tenant_id filter.
  - _Fix:_ Scope claims to a specific tenant (e.g. add AND tenant_id = ${tenantId} or run per-tenant claim loops), or otherwise enforce a safe multi-tenant claiming design (explicit tenant parameter, partitioning, or a dedicated global-queue design that is intentionally audited). Never perform cross-tenant UPDATE/SELECT without an explicit tenant boundary.
- **server/job-queue.ts:195** — reclaimExpiredLeases step 1 UPDATE touches running/expired agent_jobs rows across tenants (no tenant_id in WHERE). This can mark other tenants' jobs failed_terminal.
  - _Fix:_ Include tenant_id scoping in the UPDATE (or iterate per-tenant). Ensure reclaimed/terminal transitions are applied only within the intended tenant scope or by a clearly authenticated admin pathway with audit controls.
- **server/job-queue.ts:209** — reclaimExpiredLeases step 2 UPDATE re-queues expired running rows back to pending without any tenant_id constraint (affects all tenants).
  - _Fix:_ Apply tenant scoping to the UPDATE (or perform per-tenant processing). If a global reclaim is required, restrict it to a trusted admin context and log/audit each tenant mutation explicitly.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor issues a SELECT that aggregates agent_knowledge across all tenants (SELECT tenant_id, persona_id, COUNT(*) ... FROM agent_knowledge ...) with no tenant-scoped WHERE — a cross-tenant scan of tenant-scoped data.
  - _Fix:_ If this is an admin-level diagnostic, make the admin intent explicit and gate the call behind an audited admin path. Prefer per-tenant invocation (call snapshot per tenant) or otherwise ensure the operation runs under an audited, privileged context and follows least privilege. Avoid unscoped cross-tenant reads in normal background jobs.
- **server/lib/action-ledger.ts:300** — UPDATE on tenant-scoped table `action_attempts` (sweepStaleAttempts) performs a cross-tenant mutation with NO tenant_id constraint — it can transition rows across all tenants to 'unknown'.
  - _Fix:_ Avoid cross-tenant UPDATEs on tenant-scoped tables. Add a tenantId parameter and include tenant_id = ${tenantId} in the WHERE clause (or otherwise scope the inner SELECT by tenant_id). If a platform-wide reconciler must run, implement explicit privileged service logic (separate maintenance role/table) and thorough auditing; do NOT rely on an un-scoped UPDATE against tenant-scoped data.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT statement: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) lacks tenant_id; tenant isolation invariant requires tenant_id to be explicitly set on INSERT.
  - _Fix:_ Include the tenant_id column/value in the INSERT and ensure it's derived from a validated tenant context. For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Also validate options.tenantId is a positive integer (and keep the existing assertProjectInTenant check) before performing the INSERT.
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT in the parallel path: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) omits tenant_id, violating the requirement that INSERTs set tenant_id explicitly.
  - _Fix:_ Add tenant_id to the INSERT columns/values and ensure the tenant context is validated. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...). Also ensure assertProjectInTenant(options.projectId, options.tenantId) is invoked and succeeds before inserting.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() with a value derived from caller-supplied ledgerKey/tenantId (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)). This constructs SQL with a dynamically-derived string via sql.raw(), risking SQL injection if ledgerKey is attacker-controlled.
  - _Fix:_ Avoid sql.raw() for request-derived values. Compute a numeric BigInt lock value server-side and pass it as a parameterized value (e.g. SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)) or use a parameter binding mechanism supported by Drizzle. Validate/strictly-encode ledgerKey before use and limit ledgerKey formats to a safe whitelist.
- **server/plan-executor.ts:554** — CAS claim update on plans omits tenant_id in WHERE: `UPDATE plans SET status = 'executing' WHERE id = ${planId} AND status = 'approved' RETURNING ...`. This allows claiming a plan row by id regardless of tenant ownership.
  - _Fix:_ Include the plan's tenant constraint in the CAS (e.g. AND tenant_id = ${callerTenantId}) or otherwise validate the caller's/worker's tenant context before the UPDATE. If this operation must run cluster-wide, require an explicit admin context and audit/log it. Prefer SELECT ... FOR UPDATE / CAS that includes tenant_id so the DB write is always scoped to the validated tenant.
- **server/process-governor.ts:898** — UPDATE on tenant-scoped table `agent_desks` lacks a tenant constraint (UPDATE agent_desks ... WHERE id = ${desk.id}) — this can modify another tenant's row.
  - _Fix:_ Include the tenant filter on the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) to ensure only the caller's tenant rows are modified.
- **server/process-governor.ts:1235** — UPDATE governance_rules increments trigger_count with no tenant constraint (UPDATE governance_rules ... WHERE id = ${rule.id}) — can update rules belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) so only the caller's rule row is modified.
- **server/recurring-messages.ts:300** — UPDATE on tenant-scoped table `agent_knowledge` performed without constraining by tenant_id. The cron runner selects rows globally (allowed) but this UPDATE uses only WHERE id = ${r.id}, enabling a race or accidental cross-tenant modification.
  - _Fix:_ Constrain the UPDATE to the owning tenant. For example: UPDATE agent_knowledge SET ... WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. Alternatively, resolve and pass the tenantId separately and use WHERE id = ${r.id} AND tenant_id = ${tenantId} to ensure per-row tenant isolation.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` performed without constraining by tenant_id (or persona_id). The code read an existing row with a tenant-scoped SELECT, but the subsequent UPDATE uses only WHERE id = ${storedId}, allowing updates to a row id that may not belong to the caller's tenant in certain race conditions.
  - _Fix:_ Include tenant-scoped constraints in the UPDATE. For example: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}. This ensures the write is only applied to the expected tenant's row.
- **server/routes.ts:4545** — INSERT into tenant-scoped table 'project_notes' is missing tenant_id in the VALUES list. The code inserts project_id, note, author but does not set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT so rows are explicitly tenant-scoped. Example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}) or use a storage/Drizzle helper that always supplies tenantId. Also keep the ownership check (ensure projId belongs to the caller's tenant) and prefer a single storage-layer API to centralize tenant scoping.

### HIGH (104)

- **server/agent-activity.ts:60** — getPersonality() SELECTs from `personas` by persona id without constraining by tenant. A caller supplies personaId (potentially untrusted) but tenant ownership is not verified before reading.
  - _Fix:_ Require/accept tenantId in getPersonality and add a tenant constraint (e.g. .where(and(eq(personas.id, personaId), eq(personas.tenantId, tenantId))). If the function is called in contexts without tenant information, callers must validate ownership first.
- **server/agent-desk.ts:297** — Query `SELECT id, name FROM personas` with no tenant filter. This reads persona rows across all tenants and is used to map persona ids to names (possible cross-tenant data leakage).
  - _Fix:_ Restrict the query to the caller's tenant: SELECT id, name FROM personas WHERE tenant_id = ${tenantId}. Ensure getDesksOverview accepts a tenantId and uses it in the personas query.
- **server/agent-manager.ts:374** — Query `SELECT name, role FROM personas WHERE is_active = true LIMIT 14` has no tenant_id constraint; it reads personas across tenants and then uses them in tenant-scoped reports/prompts.
  - _Fix:_ Filter personas by tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true). If the intention is to use global personas, document that they are intentionally global; otherwise require tenant scoping and verify ownership.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting() runs a SELECT on tenant-scoped table `ai_insights` with no tenant_id constraint (SELECT ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ...). This reads insights across all tenants.
  - _Fix:_ Either accept a tenantId and add WHERE tenant_id = ${tenantId}, or if the sweep must run globally, ensure explicit per-row tenant validation before taking action. Avoid performing cross-tenant updates using unscoped selects.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights() executes an UPDATE on `ai_insights` without a tenant filter when tenantId is not provided (WHERE status='new' AND category IN (...)). This can update rows across all tenants.
  - _Fix:_ Require a tenantId or make the global behavior explicit and safe. Prefer performing tenant-scoped updates (add WHERE tenant_id = ${tenantId}) or iterate tenants to apply changes per-tenant. Document and review any intended cross-tenant bulk operations.
- **server/agentic-engines.ts:374** — Query `SELECT name, role FROM personas WHERE is_active = true LIMIT 14` (in gatherOperationalData) lacks tenant scoping and can read personas from other tenants.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise ensure personas is intentionally global and safe to read across tenants.
- **server/agentic-features.ts:861** — enrollInSequence() checks for an existing enrollment via SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant_id. This can cause cross-tenant detection/logic errors or allow enrollment collisions across tenants.
  - _Fix:_ Include tenant scoping in the existence check, e.g. WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND tenant_id = ${params.tenantId} AND status = 'active'. Always constrain lookup queries on tenant-scoped tables by tenant_id.
- **server/agentic-features.ts:1021** — classifyReply() does an UPDATE on outreach_enrollments (UPDATE ... WHERE id = ${row.id}) without also constraining tenant_id. While the row was previously SELECTed with a tenant filter, the UPDATE can still affect a same-id row from another tenant in a race.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}). As a general rule, every UPDATE/DELETE/SELECT against tenant-scoped tables must include tenant_id to prevent cross-tenant writes.
- **server/agentic/approvals.ts:135** — expireStaleApprovals() performs a bulk UPDATE on agentApprovals to set status='expired' using only status and expiry conditions (no tenant_id constraint). This will expire approvals across all tenants in one operation.
  - _Fix:_ If expiry must be tenant-scoped, require tenantId and add WHERE tenant_id = ${tenantId} AND ...; if a global sweep is intended, ensure explicit review and per-row tenant validation before taking actions that affect linked tenant-scoped resources. Consider processing tenants individually or adding tenant scoping to the initial UPDATE and/or the return/loop logic.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets is executed without constraining by tenant_id. This reads across all tenants from a tenant-scoped table (department_budgets) and therefore violates the invariant that tenant-scoped reads must be constrained to the caller's tenant.
  - _Fix:_ If this is meant to be a per-tenant operation, add a tenantId parameter and include WHERE tenant_id = ${tenantId} to the query. If the cross-tenant sweep is intentional (admin/system job), make the admin intent explicit: require an explicit admin/system authorization guard and document it, or run the query under an explicitly privileged system context. Prefer adding an explicit parameter like tenantId or clearly protecting this code path so non-admin callers cannot invoke it.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT against tenant-scoped tables: the query at line 65 selects from agent_runs (and joins agent_approvals) without constraining by tenant_id, allowing the worker to read rows belonging to any tenant.
  - _Fix:_ Restrict the query to a single tenant or require an explicit admin authorization. For example, accept a tenantId parameter (or enumerate tenants safely) and add AND r.tenant_id = ${tenantId} to the WHERE clause. If this sweep must run across all tenants, ensure it runs in a privileged/admin context and minimize returned data, or refactor to iterate per-tenant with explicit tenant_id filtering so every read is protected by an app-level tenant_id WHERE clause.
- **server/auto-asset-capture.ts:185** — Unscoped SELECT over tenant-scoped conversations: `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (...)` reads conversations across all tenants (no tenant_id WHERE clause).
  - _Fix:_ If this backfill is intended to be tenant-scoped, add a `WHERE c.tenant_id = ${tenantId}` predicate or run the backfill per-tenant with proper authorization. If it must run cross-tenant as an admin job, ensure the code path is explicitly admin-only and audited; otherwise constrain by tenant.
- **server/auto-asset-capture.ts:188** — Unscoped subquery against messages: the subselect `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant')` does not include tenant_id, allowing cross-tenant message inspection.
  - _Fix:_ Constrain the subquery by tenant (e.g. `... FROM messages WHERE conversation_id = c.id AND role = 'assistant' AND tenant_id = c.tenant_id`) or rewrite to JOIN messages with conversations including an explicit tenant filter. Avoid referencing tenant-scoped tables without an explicit tenant_id predicate.
- **server/auto-asset-capture.ts:208** — Unscoped SELECT from messages: `SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'` lacks a tenant_id filter and may read messages from other tenants if conversation IDs are not globally enforced.
  - _Fix:_ Include tenant scoping on the messages query (e.g. `WHERE conversation_id = ${c.id} AND role = 'assistant' AND tenant_id = ${c.tenant_id}`), or join through conversations to ensure the row belongs to the expected tenant before reading message content.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL updates a tenant-scoped table without constraining by tenant_id. This can modify another tenant's conversation row if IDs collide or data is poisoned.
  - _Fix:_ Include an explicit tenant filter in the UPDATE (e.g. WHERE id = ${c.conversation_id} AND tenant_id = ${c.tenant_id} AND project_id IS NULL) so writes are always constrained to the intended tenant before modifying rows.
- **server/business-tools.ts:95** — INSERT into invoice_items does not set tenant_id (possible tenant-scoped table written without tenant_id).
  - _Fix:_ Ensure invoice_items is written with explicit tenant ownership. Either: (1) add tenant_id to invoice_items (NOT NULL) and include VALUES (${tid}, ...) on INSERT so every row is tagged with the caller tenant, or (2) if invoice_items intentionally has no tenant_id, verify and enforce ownership of the parent invoice before inserting (SELECT invoices WHERE id = ${newId} AND tenant_id = ${tid}) and ensure every subsequent read/write that touches invoice_items is constrained by the parent invoice's tenant. Do not rely on foreign-key cascade or implicit assumptions — enforce the tenant_id at the application SQL level.
- **server/capability-review.ts:428** — recordCapabilityReview inserts conversation_id (and persona_id) from the caller context directly into capability_reviews without verifying that the referenced conversation/row actually belongs to the caller's tenant.
  - _Fix:_ Before inserting a foreign id (conversation_id, persona_id), validate ownership. e.g. SELECT 1 FROM conversations WHERE id = ${ctx.conversationId} AND tenant_id = ${ctx.tenantId} LIMIT 1 and abort if not found. Only write the foreign id after verifying it belongs to ctx.tenantId (or derive conversation id server-side from an authenticated context). This prevents cross-tenant referencing when there is no FK/DB-level tenant enforcement.
- **server/chat-engine.ts:3731** — capability-review.markReviewReused(conversationId, toolName) is invoked without any tenant context. This fire-and-forget call takes a conversation id (foreign id) but does not pass tenantId or otherwise validate that the conversation belongs to the caller's tenant, so any DB writes/reads inside markReviewReused may run unscoped and violate tenant isolation.
  - _Fix:_ Change the call to pass the caller's tenantId (e.g. m.markReviewReused(tenantId, conversationId, toolName)) and update capability-review.markReviewReused to require and use tenantId in all DB queries/INSERTs (WHERE tenant_id = $tenantId or Drizzle .where(eq(table.tenantId, tenantId))). If the module must accept a conversationId only, have it first load the conversation row constrained by tenant_id and fail if no match (explicit ownership check) before performing any writes.
- **server/chat-engine.ts:4070** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is called without tenant context. The function receives a conversation id (foreign id) but is not given tenantId nor is an ownership check performed here, so any persistence inside recordToolCallForStuckDetection may be executed without tenant scoping.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection (e.g. recordToolCallForStuckDetection(tenantId, conversationId, toolName, parsedArgs, round)) and/or ensure the implementation immediately validates ownership by querying the conversation with WHERE tenant_id = tenantId before any reads/writes. All DB operations must include the tenant_id filter.
- **server/chat-engine/project-context.ts:23** — Query reads project_conversations (pc.project_id) without constraining project_conversations by tenant_id. The WHERE clause filters projects by p.tenant_id but does not assert pc.tenant_id, so rows in project_conversations belonging to other tenants could be considered via the join.
  - _Fix:_ Add an explicit tenant filter on the project_conversations table (e.g. AND pc.tenant_id = ${tenantId}) or otherwise ensure every access to project_conversations includes eq(projectConversations.tenantId, tenantId). Do not rely solely on a joined projects.tenant_id predicate to enforce tenant isolation.
- **server/chat-engine/project-context.ts:45** — EXISTS subquery reads project_conversations without a tenant_id constraint (pc.conversation_id = ${conversationId} AND pc.project_id = p.id). This accesses a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Constrain the EXISTS subquery to the caller tenant (e.g. WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId}) or otherwise include eq(project_conversations.tenantId, tenantId) to enforce tenant isolation.
- **server/chat-engine/project-context.ts:159** — SELECT from project_files uses only project_id = ${projectId} and does not include tenant_id = ${tenantId}. Although projectId was previously validated, this query touches a tenant-scoped table without the required tenant_id filter at the SQL level.
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or use Drizzle's .where(eq(projectFiles.tenantId, tenantId)) so every read against project_files is directly scoped to the caller tenant.
- **server/chat-engine/project-context.ts:171** — SELECT from project_notes filters by project_id but omits tenant_id = ${tenantId}. This reads a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(projectNotes.tenantId, tenantId)) to ensure proper tenant-scoped access.
- **server/chat-engine/project-context.ts:181** — Query joins project_conversations (pc) to conversations (c) and filters c.tenant_id = ${tenantId} but does not filter project_conversations by tenant_id. Accessing project_conversations without pc.tenant_id = ${tenantId} violates the required per-table tenant-scoping invariant.
  - _Fix:_ Add an explicit tenant filter on project_conversations (e.g. AND pc.tenant_id = ${tenantId}) or otherwise ensure every read/write of project_conversations uses eq(projectConversations.tenantId, tenantId). Avoid relying solely on the joined conversation/project row to imply tenant ownership.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' without a tenant_id constraint. The scanner queries all overdue commitments across tenants: `SELECT id, tenant_id AS "tenantId", description, due_at ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...` — this reads tenant-scoped data cross-tenant and risks isolation violation/data exposure.
  - _Fix:_ Either scope this query to a specific tenant (add `WHERE tenant_id = ${tenantId}`) or ensure this operation is explicitly an admin-only routine with hardened access controls and minimal returned columns. If cross-tenant admin visibility is required, restrict returned fields to non-sensitive identifiers only, document the admin-only requirement, and ensure the caller must be authorized as a platform admin before running this query.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` without any tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants and can leak/operate on other tenants' data.
  - _Fix:_ Scope the query to an explicit tenant or iterate per-tenant. e.g. SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff} AND tenant_id = ${tenantId} — or enumerate tenants and run tenant-scoped queries or run inside a transaction with withTenantTx for each tenant.
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table dfy_intake_forms without any tenant_id constraint (getDfyIntakeByToken uses only token). This returns a row across tenants given a valid token and relies on token secrecy instead of explicit tenant scoping.
  - _Fix:_ Include explicit tenant scoping in the query or validate tenant ownership after lookup. Options: (a) change signature to accept tenantId and query with .where(and(eq(dfyIntakeForms.token, token), eq(dfyIntakeForms.tenantId, tenantId))); or (b) keep token-only lookup but immediately verify the returned row. Avoid returning rows for a tenant other than the caller's tenant.
- **server/doc-collections.ts:166** — DELETE against tenant-scoped table doc_chunks omits tenant_id in WHERE clause: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}. This writes to a tenant-scoped table without an explicit tenant equality check.
  - _Fix:_ Add an explicit tenant equality check to the WHERE clause, e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Ensure collectionId is validated (belongs to tenant) if tenantId is not available locally.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages (user messages over 14d) is executed without any tenant_id filter. messages appears to be tenant-scoped, so this global count can mix tenant data and drive cross-tenant decisions (traffic gate) incorrectly.
  - _Fix:_ Restrict the query to the intended tenant context by adding tenant_id = ${tenantId} or otherwise computing per-tenant snapshots. If the intent is to operate platform-wide, make that explicit and never use tenant-scoped tables without an explicit global policy; otherwise accept a tenantId parameter and add WHERE tenant_id = ${tenantId}.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance (14d) is executed with no tenant filter. tool_performance is tenant-scoped elsewhere; running this global query can mix tenants and lead to incorrect auto-deprecation behavior.
  - _Fix:_ Add explicit tenant scoping (e.g. WHERE last_success_at > ... AND tenant_id = ${tenantId}) or change the function to accept/require tenantId. If this module intentionally computes global platform metrics, make that explicit and avoid querying tenant-scoped tables without a deliberate and documented global aggregation path.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance in getEverInvokedTools has no tenant constraint. This reads a tenant-scoped table globally and will return names across all tenants.
  - _Fix:_ If the intent is per-tenant behavior, add tenant_id = ${tenantId} to the WHERE clause and accept tenantId as input. If a global roll-up is required, implement a controlled aggregation that is clearly separated from per-tenant operations and documented as platform-scoped.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (getActiveTools14d) lacks tenant scoping. This reads tenant-scoped telemetry across all tenants and can incorrectly influence candidate computations.
  - _Fix:_ Add tenant scoping (e.g. WHERE last_success_at > ... AND tenant_id = ${tenantId}) and propagate tenantId through the call path. If the function must remain platform-global, explicitly document and separate that logic and avoid mixing with per-tenant decisions.
- **server/email.ts:370** — SELECT from tenant-scoped table inbox_messages without tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This read may touch rows across tenants and can cause cross-tenant dedup/visibility leaks.
  - _Fix:_ Constrain the lookup to the relevant tenant/inbox (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} OR tenant_id = ${tenantId}). If you intentionally deduplicate globally, document that behaviour and ensure it's safe; otherwise perform per-tenant checks or include inbox/tenant filters.
- **server/email.ts:415** — SELECT from tenant-scoped table inbox_messages without tenant_id constraint (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL ...) ORDER BY id). This scans/updates messages across all tenants.
  - _Fix:_ Run the backfill per-tenant (pass tenantId or inboxId into the query) or add a tenant_id (or inbox_id) WHERE clause. Avoid global scans over tenant tables; iterate tenants explicitly if an admin/global operation is intended and ensure proper authorization.
- **server/firecrawl.ts:231** — UPDATE against tenant-scoped table 'scrapedPages' lacks a tenant_id constraint in WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) or use a parameterized Drizzle predicate to include scrapedPages.tenantId = tenantId to prevent cross-tenant writes.
- **server/firecrawl.ts:363** — UPDATE against tenant-scoped table 'scrapedPages' lacks a tenant_id constraint in WHERE clause (.where(eq(scrapedPages.id, existing[0].id))).
  - _Fix:_ Add an explicit tenant predicate to the WHERE clause, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include tenant scoping on updates to tenant-scoped tables.
- **server/glasses-gateway.ts:173** — UPDATE against tenant-scoped table 'apiKeys' (setting lastUsedAt) uses .where(eq(apiKeys.id, key.id)) with NO tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE to avoid modifying another tenant's key in edge/TOCTOU cases. Use .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))) or .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, (req as any).tenantId))).
- **server/google-drive.ts:866** — SELECT from tenant-scoped table 'projects' fetches a project by id using .where(eq(projects.id, projectId)) without constraining by tenant_id.
  - _Fix:_ Scope the lookup to the caller's tenant in the WHERE clause (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the query cannot return another tenant's project. Alternatively, pass tenantId in and include it in the parameterized query.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl with .where(eq(projects.id, projectId)) but lacks a tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE to prevent cross-tenant modification. Use .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or otherwise ensure the UPDATE is only applied when the project belongs to the calling tenant.
- **server/google-drive.ts:1473** — SELECT from tenant-scoped table 'projects' (resolving project for uploadAndShare) uses .where(eq(projects.id, params.projectId)) without a tenant_id filter before relying on the row.
  - _Fix:_ Constrain the SELECT to the caller's tenant: .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))). If the caller cannot provide tenantId, validate and fail-closed if ownership cannot be asserted prior to subsequent operations.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id (writes to a tenant-scoped artifact table without scoping to caller tenant).
  - _Fix:_ Persist tenant_id with the row (e.g. INSERT INTO presenter_slide_images (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)) or otherwise validate/ensure the session_id belongs to the caller's tenant before inserting. Prefer using the Drizzle schema so tenantId is enforced in application code.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint. This updates a tenant-scoped row by ID only, allowing cross-tenant modification if sessionId is misused.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) and/or verify the session belongs to the caller's tenant before performing the update. Ensure all UPDATE/DELETE statements on tenant-scoped tables include tenant_id in the WHERE clause.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks fetches tasks without a tenant_id filter (selects possibly cross-tenant heartbeat tasks).
  - _Fix:_ If the intent is tenant-scoped scanning, add WHERE tenant_id = ${tenantId}. If this is an intended global/admin sweep, require and validate an explicit admin/system context before enumerating cross-tenant tasks.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) updates tasks by id only and lacks tenant_id constraint, allowing disabling tasks across tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the operation only affects tasks belonging to the intended tenant. If this is an administrative cross-tenant operation, require explicit admin authorization and document it.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT from tenant-scoped table heartbeat_tasks without any tenant_id constraint (raw SQL: SELECT * FROM heartbeat_tasks WHERE ...). This reads rows across tenants without an explicit tenant filter.
  - _Fix:_ If this is intended to operate cross-tenant, make that explicit in the API signature and call-sites (e.g., name the function to indicate cross-tenant behavior) and audit its usage. Otherwise restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId}) or pass an explicit ALL_TENANTS flag and document the reasoning. Prefer using a storage helper that enforces the expected tenant scope rather than raw SQL.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks ... WHERE id = ${id} executed without a tenant_id constraint. This updates a tenant-scoped row by id only, allowing modification of rows across tenants if called without additional checks.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or require/accept the owning tenant id as a parameter and use it in the UPDATE. Alternatively use the existing storage abstraction that enforces tenant scoping to perform the update atomically (id + tenant_id).
- **server/heartbeat-startup.ts:75** — Raw UPDATE on heartbeat_tasks (tenant-scoped) without tenant_id in WHERE clause (db.execute(sql`UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id}`)). This modifies tenant-scoped rows by id only.
  - _Fix:_ Either call a storage wrapper that enforces tenant scoping (e.g. storage.updateHeartbeatTask) or add AND tenant_id = ${t.tenantId} to the WHERE so the update is limited to the expected tenant. Audit other raw db.execute uses against heartbeat_tasks and replace with tenant-aware operations.
- **server/heartbeat.ts:255** — SELECT from event_log without a tenant_id filter (sql`SELECT * FROM event_log WHERE status = 'pending' ...`). event_log is used with tenant_id later, so selecting across tenants here reads tenant-scoped rows without an explicit scoping filter.
  - _Fix:_ Either restrict the SELECT by tenant_id (when processing events for a single tenant) or make the cross-tenant intent explicit and centralize this logic in a scheduler boundary that is reviewed/approved. If cross-tenant processing is required, ensure every subsequent write/update uses the event's tenant_id in its WHERE clauses and that this cross-tenant read is acceptable for your threat model.
- **server/heartbeat.ts:2609** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executed without tenant_id constraint. Although the task was created with a tenantId, the UPDATE modifies by id only and can affect rows across tenants if misused.
  - _Fix:_ Change this to use a tenant-scoped update (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use storage.updateHeartbeatTask which enforces tenant scoping. Always include tenant_id in UPDATE/DELETE statements on tenant-scoped tables.
- **server/heartbeat.ts:3065** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) with no tenant_id constraint. This writes to a tenant-scoped table by id only.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or use the storage abstraction (storage.updateHeartbeatTask) which should enforce tenant scoping. Ensure all paths that change heartbeat_tasks include the owning tenant in their filters to prevent cross-tenant modifications.
- **server/job-queue.ts:238** — completeJob updates agent_jobs (SET status='succeeded'...) by id without restricting tenant_id in WHERE. A stale or forged id could let a caller mutate another tenant's job state.
  - _Fix:_ Require tenant scoping on completion (e.g. accept/verify tenantId and include AND tenant_id = ${tenantId} in the WHERE), or fetch-and-validate the job's tenant before applying the update to ensure the caller is operating within the job's tenant.
- **server/job-queue.ts:281** — failJob first does SELECT max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint — reading tenant-scoped data by id without scope enforcement.
  - _Fix:_ Require the caller to provide/verify tenantId or perform a tenant-scoped lookup (WHERE id = ${id} AND tenant_id = ${tenantId}) to avoid leaking or acting on another tenant's job metadata.
- **server/job-queue.ts:298** — failJob's terminal-path UPDATE (mark failed_terminal) updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but omits tenant_id — this can mutate other tenants' rows by id.
  - _Fix:_ Include tenant_id in the WHERE clause and/or require the caller to assert/validate the tenant before performing the mutation to maintain tenant isolation.
- **server/job-queue.ts:318** — failJob's retry-path UPDATE that re-queues (sets status='pending', next_run_at=...) updates by id/attempts without tenant_id scoping.
  - _Fix:_ Add tenant_id to the WHERE clause (and/or pass tenantId into failJob) so only the owning tenant's job can be re-queued.
- **server/job-queue.ts:335** — cancelJob updates agent_jobs WHERE id = ${id} AND status IN (...) without any tenant_id constraint — allowing cancellation of jobs by id across tenants.
  - _Fix:_ Require tenant scoping for cancellation (e.g. AND tenant_id = ${tenantId}) or restrict this helper to admin-only usage with explicit access control and auditing.
- **server/lib/action-ledger.ts:266** — SELECT from tenant-scoped table `action_attempts` (listUnknownAttemptsAllTenants) has NO tenant_id constraint — it returns rows across all tenants.
  - _Fix:_ If this is unintended, constrain the query by tenant_id (e.g. add a tenantId parameter and WHERE tenant_id = ${tenantId}). If cross-tenant scanning is required for a reconciler, document and audit that exception and implement explicit guarding/authorization (or use a dedicated global table/purpose-built maintenance role) — do NOT expose or return cross-tenant rows to tenant-scoped callers.
- **server/lib/ideabrowser-backfill.ts:49** — Query aggregates counts from the tenant-scoped `projects` table without constraining by tenant_id (SELECT tenant_id, count(*) FROM projects WHERE tags && ... GROUP BY tenant_id). This reads across all tenants and violates the invariant that tenant-scoped reads must be limited to the caller's tenant.
  - _Fix:_ Avoid a global scan over the tenant-scoped `projects` table. Instead, iterate the snapshot's tenant IDs (wantedByTenant) and perform a per-tenant COUNT query with WHERE tenant_id = <that tenant>. Example: for each t in wantedByTenant: SELECT COUNT(*) FROM projects WHERE tenant_id = ${t} AND tags && ${IDEA_TAG_LITERAL}::text[]. This preserves the intended short-circuit logic while ensuring each query is explicitly scoped to a single tenant.
- **server/lib/inbox-ingest.ts:536** — Query joins inbox_messages (alias m) but only constrains inbox_classifications (alias c) by tenant_id. The SELECT reads columns from inbox_messages without an explicit tenant_id filter on that table, risking cross-tenant data leakage if ids collide across tenants.
  - _Fix:_ Constrain every tenant-scoped table in the query. Add a tenant filter for inbox_messages (e.g. include AND m.tenant_id = ${tenantId} in the WHERE clause or change the JOIN to ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId}). Ensure every referenced tenant-scoped table is explicitly scoped to the caller's tenant.
- **server/lib/mention-injection.ts:120** — Query left-joins personas (alias p) but does not constrain personas by tenant_id. The query selects p.name while only filtering channel_messages (cm) by tenant_id, allowing potential leakage of persona rows from other tenants if persona IDs collide.
  - _Fix:_ Scope all tenant-scoped tables in the query. Constrain personas to the same tenant (for example change the JOIN to LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId}, or add AND p.tenant_id = ${params.tenantId} in the WHERE). Ensure every table in joins is explicitly filtered by tenant_id.
- **server/lib/outcome-promotion.ts:158** — SELECT from tenant-scoped table 'deliverable_grade_events' is executed without a tenant_id constraint (scans across all tenants).
  - _Fix:_ Constrain the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId}) or otherwise enforce that this routine is only run under a trusted/admin context. If the scan must aggregate across tenants for a nightly job, run it under a controlled admin job with explicit authorization and auditing, or iterate per-tenant (select tenant ids then run per-tenant queries) so application-level tenant isolation is preserved.
- **server/lib/restraint-trust.ts:90** — Use of sql.raw() with a request-derived value (column) in a tenant-scoped UPDATE. The column name is selected from a parameter-derived 'outcome' and inserted via sql.raw(${column}), which risks SQL injection and breaks the stated rule to avoid sql.raw/string-interpolated SQL with request-derived values.
  - _Fix:_ Avoid inserting any runtime/request-derived token into raw SQL. Instead, implement a safe, explicit branch per allowed column (e.g. switch on outcome and run one of four parameterized UPDATE statements that set the concrete column name), or map to prebuilt, trusted SQL fragments and never accept arbitrary input. If a dynamic column is truly required, strictly validate/normalize the outcome (already done) and use the database/ORM identifier API (not raw interpolated SQL) or an explicit allowlist-to-statement mapping so no unescaped value can reach SQL text.
- **server/lib/scheduled-post-runner.ts:61** — SELECT in CTE reads from tenant-scoped table `scheduled_posts` without any tenant_id filter (SELECT id FROM scheduled_posts ...). This allows cross-tenant scanning/locking of rows under the system runner context.
  - _Fix:_ Either restrict the SELECT to an explicit tenant_id or make the cross-tenant behavior explicit and enforce it via a narrow, auditable admin-only API. If the runner must act per-tenant, iterate tenants and include WHERE tenant_id = <tenant> (or use a withTenantTx wrapper) so every read/lock is scoped to a known tenant. If truly administrative, require an explicit adminTenantId parameter and document/guard the operation with admin-only checks and audit logging.
- **server/lib/stuck-task-sweep.ts:56** — SELECT against tenant-scoped table `agent_runs` without tenant_id constraint (SELECT id, tenant_id, ... FROM agent_runs WHERE status = 'running' ...). This scans across tenants.
  - _Fix:_ If this is intended as an admin/system-wide audit, document and restrict access to an admin-only path. Otherwise, scope queries to a tenant by adding WHERE tenant_id = ${tenantId} (or run inside withTenantTx) so reads only return rows belonging to the caller's tenant.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against tenant-scoped table `mind_tickets` without tenant_id constraint (SELECT id, tenant_id, ... FROM mind_tickets WHERE status = 'in_progress' ...). This scans across tenants.
  - _Fix:_ Same as above: either restrict this query by tenant_id, or ensure the operation is explicitly admin-only and audited. Prefer per-tenant queries or run with an explicit admin context to avoid accidental cross-tenant data exposure.
- **server/martech-bundle.ts:314** — UPDATE on tenant-scoped table tenant_voice_profiles uses only the row id in WHERE clause and lacks an explicit tenant_id predicate. This can allow a row id from another tenant to be updated if an attacker/bug obtains it.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add a tenant check to the WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))) or in raw SQL add AND tenant_id = ${opts.tenantId}). Always include tenant_id on tenant-scoped writes.
- **server/memory-graph.ts:129** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} does not include a tenant_id predicate. memory_categories is tenant-scoped, so this update could affect a category owned by a different tenant if catId is misbound.
  - _Fix:_ Include tenant scoping on the UPDATE. Use WHERE id = ${catId} AND tenant_id = ${tenantId} (or equivalent Drizzle .where(..., eq(memoryCategories.tenantId, tenantId))). Verify tenantId is the caller's tenant when performing the update.
- **server/memory-graph.ts:708** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped table using only the memory id with no tenant_id constraint. A memory id from another tenant could be modified.
  - _Fix:_ Add an explicit tenant predicate to the UPDATE to enforce ownership, e.g. UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Alternatively, use a parameterized Drizzle .where(eq(memoryEntries.id, mem.id), eq(memoryEntries.tenantId, tenantId)). Ensure every write includes tenant scoping.
- **server/memory-intelligence.ts:399** — UPDATE against tenant-scoped table knowledge_triples without tenant_id constraint (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}). This allows modifying rows by id across tenants.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or use a storage/Drizzle helper that enforces tenant scope): UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} AND tenant_id = ${tenantId}.
- **server/oauth-subscriptions.ts:906** — SELECT FROM tenant-scoped table 'oauth_subscriptions' without tenant_id filter (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This returns rows across all tenants.
  - _Fix:_ Constrain queries to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or explicitly document/implement an admin-only path that requires a vetted admin context. If the operation must iterate tenants, perform an explicit, auditable admin flow that signals cross-tenant work and enforces additional safeguards.
- **server/oauth-subscriptions.ts:915** — SELECT FROM tenant-scoped table 'oauth_subscriptions' without tenant filter (SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'). This reads data for all tenants.
  - _Fix:_ If you intend to enumerate tenants, require explicit admin authorization and a clear admin codepath. Otherwise add WHERE tenant_id = ${tenantId} to limit the read to the caller's tenant.
- **server/oauth-subscriptions.ts:956** — UPDATE against tenant-scoped table 'oauth_subscriptions' using only id in WHERE clause (UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id}) — missing tenant_id constraint.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id || tenantId}). Do not rely solely on id; enforce tenant_id to prevent accidental cross-tenant modification.
- **server/oauth-subscriptions.ts:1258** — SELECT FROM tenant-scoped table 'oauth_subscriptions' without tenant filter (SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). Reads across all tenants.
  - _Fix:_ Add a tenant_id constraint (WHERE tenant_id = ${tenantId}) or implement an explicit, auditable admin-only flow for cross-tenant processing with additional safeguards.
- **server/oauth-subscriptions.ts:1328** — SELECT FROM tenant-scoped table 'oauth_subscriptions' without tenant filter (SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE). This enumerates subscriptions across tenants.
  - _Fix:_ Restrict the query to a tenant with WHERE tenant_id = ${tenantId}, or move this to an explicit admin flow that requires proper authorization and logging before touching multi-tenant data.
- **server/orchestrator-ledger.ts:461** — UPDATE against tenant-scoped table 'memory_entries' uses WHERE id = ${row.id} without also constraining tenant_id (UPDATE memory_entries SET ... WHERE id = ${row.id}). The earlier SELECT did filter by tenant, but the UPDATE lacks an explicit tenant_id guard.
  - _Fix:_ Add tenant_id to the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) to enforce tenant isolation and avoid accidental cross-tenant updates.
- **server/pace-control.ts:85** — COUNT query against tenant-scoped table 'heartbeat_logs' omits tenant_id (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}). This aggregates activity across all tenants.
  - _Fix:_ Require and use tenantId in the checkPace API and add WHERE tenant_id = ${tenantId} to the query. If the intent is global monitoring, move to a clearly privileged admin path and document it.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs without tenant constraint (SELECT COUNT ... FROM heartbeat_logs WHERE created_at > ...). This returns cross-tenant aggregates/rows.
  - _Fix:_ Scope snapshot queries to a tenant by adding WHERE tenant_id = ${tenantId} (and accept a tenantId parameter). If a global snapshot is truly required, make it an explicit admin-only operation with strict authorization and logging.
- **server/persona-export.ts:33** — SELECT FROM tenant-scoped table 'personas' without tenant constraint (SELECT * FROM personas WHERE id = ${personaId}). This reads persona rows regardless of tenant ownership.
  - _Fix:_ Include tenant_id in the SELECT (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}), or perform an explicit ownership check before exporting. Ensure callers supply tenantId and validate it before returning persona data.
- **server/persona-export.ts:39** — SELECT from 'skills' uses WHERE persona_id = ${personaId} OR persona_id IS NULL without any tenant constraint. If 'skills' is tenant-scoped, this will read other tenants' rows or mix tenant-global rows without explicit handling.
  - _Fix:_ If 'skills' is tenant-scoped, add AND tenant_id = ${tenantId} (or design an explicit scheme for global defaults vs tenant entries). Otherwise assert and document that skills is intentionally global; prefer an explicit join/marker for global rows instead of omitting tenant scoping.
- **server/plan-executor.ts:877** — resumeStuckPlans rolls back stuck 'executing' rows without scoping by tenant: `UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id`. This updates rows for all tenants.
  - _Fix:_ If this recovery is intended to be tenant-scoped, change the UPDATE to include a tenant_id predicate (or iterate per-tenant). If it must be global, require an explicit admin-only operation and add audit logging/authorization to avoid accidental cross-tenant state changes.
- **server/plan-executor.ts:886** — resumeStuckPlans selects approved plans for re-dispatch without tenant scoping: `SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50`. This will kick plans from any tenant.
  - _Fix:_ Limit the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or make this boot recovery explicitly run-per-tenant under an admin-authorized path. Document and gate global behavior with admin checks and audit records.
- **server/process-governor.ts:396** — SELECT from `personas` is joined without constraining the personas table to the caller tenant (JOIN personas p ON p.id = ad.persona_id) — read from a tenant-scoped table occurs with no tenant_id filter.
  - _Fix:_ Constrain the join to the same tenant (e.g. JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) or otherwise add WHERE p.tenant_id = ${tenantId} so the personas row read is scoped to the caller tenant.
- **server/process-governor.ts:890** — SELECT in restart_agent joins `personas` (LEFT JOIN personas p ON p.id = ad.persona_id) without constraining `personas` by tenant — reading a tenant-scoped table with no tenant filter.
  - _Fix:_ Add tenant constraint to the join (e.g. LEFT JOIN personas p ON p.id = ad.persona_id AND p.tenant_id = ${tenantId}) to ensure persona data is scoped to the caller tenant.
- **server/project-brain.ts:135** — SELECT from `projects` is executed using projectId only (SELECT name, status, description FROM projects WHERE id = ${projectId}) with no tenant constraint — reading a tenant-scoped table without verifying ownership.
  - _Fix:_ Require and pass an explicit tenantId into updateProjectBrain and add a tenant filter (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). If the caller cannot be trusted, validate project ownership before reading.
- **server/project-brain.ts:332** — Backfill query selects projects and conversations with no tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... ) — reads tenant-scoped tables without tenant filters.
  - _Fix:_ Scope the query by tenant (e.g. JOIN ... WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}) or iterate per-tenant to ensure only that tenant's projects/conversations are processed. Ensure tenantId is provided to backfillProjectBrains or the function only operates on global tables.
- **server/project-brain.ts:350** — SELECT from `conversations` (WHERE c.project_id = ${proj.id}) is executed with no tenant constraint — reading tenant-scoped conversation rows without verifying tenant ownership.
  - _Fix:_ Add tenant scoping (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) and ensure proj came from the same tenant; pass tenantId into the function if needed.
- **server/project-brain.ts:359** — SELECT from `messages` (WHERE conversation_id = ${conv.id}) is executed with no tenant constraint — reading tenant-scoped message rows without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) and ensure conversation ownership is validated before reading.
- **server/research-engine.ts:169** — UPDATE on research_sessions in cleanupZombieSessions lacks any tenant_id constraint — the WHERE clause only filters status, active session ids, and started_at, so this can complete (or mutate) running sessions across all tenants.
  - _Fix:_ Scope the UPDATE to tenant(s). Either iterate per-tenant and include WHERE tenant_id = ${tenantId}, or include a tenant_id IN (...) / specific tenant_id filter in the WHERE clause (parameterized via sql`... ${tenantId} ...`) so no cross-tenant rows are modified. Ensure any list-based interpolation is parameterized (sql.join) not string-concatenated.
- **server/research-engine.ts:456** — INSERT INTO project_notes omits tenant_id — inserting project-scoped notes without providing tenant_id violates the tenant-isolation invariant (tenant_id must be passed explicitly) and risks DB constraint failures or cross-tenant misattribution.
  - _Fix:_ Include tenant_id in the INSERT column list and values (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ${note}, ${'Research Engine'}, NOW())). Also ensure ownership was previously validated (assertProjectInTenant is used earlier — keep that guard) before inserting.
- **server/research-engine.ts:465** — Second INSERT INTO project_notes (inside loop) also omits tenant_id — same tenant-isolation violation as above for per-finding notes.
  - _Fix:_ Same fix as above: include tenant_id in the INSERT columns and values (e.g. VALUES (${projectId}, ${session.tenantId}, ${noteContent}, ${'Research Engine'}, NOW())). Ensure project ownership was validated before any write and use parameterized queries (drizzle/sql tagged parameters) to avoid injection.
- **server/routes.ts:181** — SELECT from tenant-scoped table `file_storage` without tenant constraint (restoreUploadsFromDb). This reads all tenants' file rows (e.g. SELECT ... FROM file_storage WHERE mimeType LIKE 'image/%' AND storage_key IS NULL) allowing cross-tenant access during startup restore.
  - _Fix:_ Scope the query to a specific tenant (e.g. add AND file_storage.tenant_id = ${tenantId}) or iterate per-tenant. If this is intended as a global migration, run it as an explicit, audited migration script with an explicit trusted tenant context rather than an unscoped startup query.
- **server/routes.ts:470** — UPDATE on tenant-scoped table `order_lookup_codes` without tenant constraint (backfill): UPDATE order_lookup_codes SET tenant_id = <owner> WHERE tenant_id IS NULL updates rows across all tenants.
  - _Fix:_ Perform tenant-aware backfill: explicitly target only rows that belong to the legacy trusted tenant or run per-tenant updates. Alternatively run the migration as a one-off with explicit admin approval and isolate its scope to avoid accidental cross-tenant writes.
- **server/routes.ts:477** — DELETE from tenant-scoped table `order_lookup_codes` without tenant constraint (pruneOrderLookupCodes): DELETE FROM order_lookup_codes WHERE expires_at < NOW() affects rows for all tenants.
  - _Fix:_ Restrict deletions to a tenant context (e.g. DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < NOW()) or iterate tenants and prune per-tenant. If global sweep is required, perform it in a controlled migration/maintenance script executed under an audited admin context.
- **server/routes.ts:499** — SELECT referencing tenant-scoped tables in subqueries without tenant constraints (cleanupTestTenants). Subqueries use SELECT DISTINCT tenant_id FROM conversations and FROM projects with no tenant filter, enabling cross-tenant enumeration influencing which tenant rows are considered safe-to-delete.
  - _Fix:_ Replace the unscoped subqueries with tenant-scoped existence checks tied to the candidate tenant id (e.g. WHERE EXISTS (SELECT 1 FROM conversations WHERE tenant_id = ${candidateId}) ) or otherwise perform this cleanup with explicit per-tenant checks under an audited admin migration script.
- **server/routes.ts:620** — SELECT from tenant-scoped table `projects` with no tenant constraint (backfillProjectDriveFolders): SELECT p.id, p.name, p.tenant_id ... FROM projects p JOIN tenants t ... WHERE p.drive_folder_id IS NULL. This reads all projects across tenants.
  - _Fix:_ Scope the backfill to a specific tenant or iterate tenants and run a per-tenant query (add WHERE p.tenant_id = ${tenantId}). If a global scan is necessary, run it as an explicit, audited maintenance job that is aware of cross-tenant implications.
- **server/routes.ts:1387** — Admin endpoint queries tenant-scoped tables without tenant constraint (/api/public/stats): SELECT count(*) FROM conversations/messages/heartbeat_tasks/memory_entries/heartbeat_logs are unscoped and read across all tenants.
  - _Fix:_ If cross-tenant aggregation is intended only for platform admins, make the intention explicit (e.g. require platform admin and use a clearly-audited admin aggregation function) or restrict to a tenant context when used in tenant-scoped views (add WHERE tenant_id = ${tenantId}). Consider using audited read-only admin views or separate reporting tables for global metrics.
- **server/routes.ts:1416** — Admin endpoint performs multiple unscoped SELECTs against tenant-scoped tables (/api/public/architecture): queries like SELECT count(*) FROM conversations/messages/personas/projects/skills/memory_entries/heartbeat_tasks/heartbeat_logs/inbox_messages/governance_rules/research_sessions lack tenant_id filters.
  - _Fix:_ Either scope these counts to a tenant (WHERE tenant_id = ${tenantId}) when used in tenant contexts or, if global counts are required for platform admins, execute them through an explicit, auditable admin reporting pathway (e.g. dedicated reporting queries run under an admin-only migration/reporting job) and document the cross-tenant access.
- **server/routes.ts:972** — SELECT from tenant-scoped table `file_storage` without tenant constraint in uploads middleware (owners = SELECT tenant_id FROM file_storage WHERE filename = ...). This returns owners across all tenants and then attempts to infer authorization, potentially leaking cross-tenant existence.
  - _Fix:_ Include tenant scoping in the query where appropriate (e.g. WHERE filename = ${baseName} AND tenant_id = ${authedTenantId}) or implement explicit logic that first checks for rows belonging to the authenticated tenant and only then handles alternate signed-URL flows. If enumerating all owners is required, perform it under a trusted admin context and avoid exposing existence differences to tenants.
- **server/routes.ts:1918** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint (GET /api/presenter/:token). Query: SELECT ... FROM presenter_sessions WHERE token = ${token}. No tenant_id filter — allows token-based lookup across tenants without enforcing tenant ownership at the SQL layer.
  - _Fix:_ Either include tenant_id in the WHERE clause (where tenant is known) or, after selecting by token, explicitly verify the returned row's tenant_id against an authenticated or signed tenant context before returning tenant-scoped data. Prefer adding eq(presenter_sessions.tenant_id, <expectedTenantId>) to the query for defence-in-depth.
- **server/routes.ts:1946** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint (GET /api/presenter/:token/slide/:index). The session lookup SELECT id, tenant_id FROM presenter_sessions WHERE token = ${token} is unscoped by tenant.
  - _Fix:_ Constrain the session lookup by tenant when available or, if token is the sole authorisation, ensure the token is cryptographically bound to the tenant and enforce that binding at query time (e.g. add tenant_id=... in WHERE) so SQL-level filtering enforces isolation.
- **server/routes.ts:2053** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint (POST /api/presenter/:token/repair-images): SELECT id, tenant_id, presentation_id, slides FROM presenter_sessions WHERE token = ${token} — no tenant_id filter.
  - _Fix:_ Either include the tenant constraint in the WHERE clause or, after retrieval, verify the session's tenant_id against an authenticated or signed tenant context. Prefer SQL-level scoping (WHERE token = ${token} AND tenant_id = ${expectedTenantId}) to avoid cross-tenant leaks or writes.
- **server/routes/conversations.ts:118** — UPDATE on tenant-scoped table 'conversations' performed without constraining tenant_id in WHERE clause (UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or pass conv.tenantId and verify ownership before updating. Always enforce tenant_id in UPDATE WHERE clauses.
- **server/routes/conversations.ts:122** — INSERT into tenant-scoped table 'project_conversations' omits tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})).
  - _Fix:_ Add tenant_id to the inserted columns and values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})). Ensure the tenant is validated to own the referenced project/conversation before inserting.
- **server/routes/conversations.ts:124** — UPDATE on tenant-scoped table 'projects' executed without tenant_id constraint (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Alternatively, include an ownership check prior to updating and then include tenant_id in the WHERE clause to prevent cross-tenant modification.
- **server/routes/conversations.ts:256** — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note.slice(0, 8000)}, ${'system:archive'})). Verify the project belongs to the tenant before inserting.

### MEDIUM (21)

- **server/auto-consolidation.ts:335** — SELECT DISTINCT tenant_id FROM memory_entries is executed without constraining the query to a caller tenant (no WHERE tenant_id = ...). This reads across all tenants.
  - _Fix:_ If this background job intentionally scans all tenants, document it and ensure the code runs with explicit admin privileges and auditing. Otherwise restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${tenantId} or otherwise scope by tenant) to enforce tenant isolation.
- **server/auto-project.ts:135** — Query selecting messages: SELECT role, LEFT(content, 300) ... FROM messages WHERE conversation_id = ${conversationId} — the messages table read is not constrained by tenant_id.
  - _Fix:_ Add an explicit tenant filter to the messages query (e.g. AND tenant_id = ${tenantId}) or otherwise validate/derive the tenant in SQL (JOIN on conversations with tenant filter) before reading messages to ensure tenant-scoped reads cannot leak cross-tenant data.
- **server/auto-transcript.ts:46** — Query selecting messages for a conversation: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} — messages read lacks tenant_id filter.
  - _Fix:_ Constrain the messages read with tenant ownership (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) even if you previously verified the conversation, to avoid relying on implicit referential invariants and prevent cross-tenant reads.
- **server/auto-transcript.ts:128** — Backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without scoping messages by tenant_id. This reads tenant-scoped messages without an explicit tenant constraint.
  - _Fix:_ Add tenant scoping to the subquery (e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) or otherwise ensure the subquery is executed with an appropriate tenant constraint to prevent cross-tenant data access.
- **server/auto-transcript.ts:153** — alsoLinked scan includes a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0 but does not restrict the messages table by tenant_id, causing tenant-scoped reads without explicit tenant scoping.
  - _Fix:_ Restrict the subquery by tenant (e.g. AND tenant_id = c.tenant_id) or refactor the query to join messages via a tenant-aware predicate so counts only reflect messages owned by the conversation's tenant.
- **server/capability-review.ts:456** — recordCapabilityReview stores ctx.conversationId in an in-memory pendingReuseByConversation map (and later trusts it to mark reuse) without verifying the conversation belongs to the provided tenantId.
  - _Fix:_ Validate the conversation ownership before creating the pending entry (same check as above). Alternatively, store only server-validated keys (e.g. the inserted review id) or perform a tenant-scoped lookup when marking reuse to ensure the conversation truly belongs to the tenant before attributing reuse.
- **server/chat-engine.ts:4076** — postDiagnosticReport([stuckPattern]) is invoked without tenant context. If postDiagnosticReport persists diagnostic rows tied to a tenant or conversation, calling it without tenantId may cause tenant-ambiguous writes or missing ownership checks.
  - _Fix:_ Ensure postDiagnosticReport accepts and is called with tenantId (or that stuckPattern includes tenant-scoped metadata and the callee enforces tenant filtering). Prefer explicit tenant parameters in telemetry/diagnostic write paths and add ownership validation where applicable.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table `project_notes` uses only project_id (WHERE project_id = ${proj.id}) and lacks an explicit tenant_id predicate. Reads against tenant-scoped tables must include tenant scoping at the app layer.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} (or join on p.tenant_id and compare to ${tenantId}), so the read is constrained to the caller's tenant.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` uses only project_id (WHERE project_id = ${proj.id}) and omits an explicit tenant_id predicate.
  - _Fix:_ Constrain the query to the tenant, e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}, or otherwise ensure project_files rows are filtered to the caller's tenant before reading.
- **server/data-protection.ts:403** — COUNT(*) query against tenant-scoped table `messages` has no tenant predicate (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}). This is an existence/oracle/read across tenant data.
  - _Fix:_ Include tenant scoping in the count: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId} — or derive/verify tenant ownership before querying.
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table doc_chunks uses WHERE id = ${row.id} without constraining tenant_id. Although id is a primary key, the write omits explicit tenant scoping and could affect another tenant if assumptions change.
  - _Fix:_ Constrain updates by tenant as well: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Use the tenantId parameter passed into generateCollectionEmbeddings to enforce ownership on the UPDATE.
- **server/glasses-gateway.ts:157** — SELECT from tenant-scoped table 'apiKeys' is performed without a tenant_id constraint (looking up by keyHash).
  - _Fix:_ Authentication lookups by keyHash are often necessary, but ensure ownership is validated immediately and that subsequent mutations include tenant scoping. If possible, bind the api key to a tenant earlier (or limit the returned fields) and treat this SELECT as guarded. Consider additionally restricting queries (or the update below) by apiKeys.tenantId where context allows, or at minimum verifying and asserting the returned row's tenantId before any side effects.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} without tenant-scoping. Although documented for admin use, this reads tenant-scoped data by bare id with no access check in this helper.
  - _Fix:_ Either (a) rename and clearly restrict this helper to authenticated platform-admin paths only and enforce that at call sites, or (b) require callers to provide an expected tenantId and validate it (WHERE id = ${id} AND tenant_id = ${tenantId}). Add explicit docstring and runtime guard to prevent misuse by tenant-scoped code paths.
- **server/job-worker.ts:147** — research_code_proposal job handler rebuilds a session object using tenantId from job.payload (session = { sessionId, tenantId, model }) but does not verify that payload.tenantId matches the claimed job.tenantId before using it. A malicious/poisoned payload could make the handler operate on another tenant's resources.
  - _Fix:_ Validate that payload tenantId (and any foreign ids) either match job.tenantId or otherwise verify ownership before performing any tenant-scoped reads/writes. Prefer using job.tenantId as the source of truth when available.
- **server/job-worker.ts:168** — research_digest handler reads tenantId from job.payload and calls generateResearchDigest(tenantId) without verifying that payload.tenantId equals job.tenantId (or otherwise checking ownership). This can cause reads/writes against a tenant-scoped dataset referenced only by caller-supplied payload.
  - _Fix:_ Ensure handlers validate ownership: require payload.tenantId === job.tenantId (or reject/massage the payload), or use job.tenantId directly for downstream tenant-scoped operations. Add explicit checks and throw on mismatch.
- **server/lib/scheduled-post-runner.ts:320** — Postgres array literal `arrLit` is constructed by string-concatenating user-supplied platform names and then injected into an SQL statement (arrLit built from params.platforms and used in the INSERT). This is a string-interpolated SQL pattern using request-derived data and risks SQL injection or incorrect SQL semantics.
  - _Fix:_ Avoid constructing SQL/text-array literals by string concatenation. Instead pass the platforms array as a parameter and let the driver/ORM bind it (e.g. use Drizzle's array binding or parameterized values), or use a JSONB column and pass the array as a parameter. Ensure all request-derived values are passed as bound parameters rather than spliced into raw SQL fragments.
- **server/lib/system-probes.ts:163** — A DB probe runs a global count on `messages` (db.select(...).from(messagesTable)) with no tenant filter. Counting tenant-scoped table rows without tenant context leaks cross-tenant information to callers of the probe.
  - _Fix:_ If a global message count is needed only for admin diagnostics, restrict the tool to admin-only callers and document it. Otherwise, provide a tenant-scoped count by adding WHERE tenant_id = ${tenantId} or invoke an explicit admin-only API. Ensure callers cannot pass an arbitrary tenant context leading to cross-tenant exposure.
- **server/platform-admin-conversations.ts:18** — Cross-tenant SELECT on conversations with no tenant_id WHERE clause. While there is an isPlatformAdmin(req) guard, the DB query itself does not constrain tenant_id: `.where(and(eq(conversations.id, conversationId), isNull(conversations.deletedAt)))`. This reads tenant-scoped data without a tenant predicate.
  - _Fix:_ Either add AND conversation.tenant_id = ${someTenantId} when called in tenant-scoped flows, or (if truly a platform-admin cross-tenant endpoint) keep the admin guard but explicitly document and audit every cross-tenant access. Prefer returning the conversation's tenant_id as part of the result and record the admin actor in an access audit log.
- **server/plan-replay.ts:59** — String-built vector literal from user/objective is interpolated into SQL: `... objective_embedding <=> ${embeddingLiteral}::vector`. embeddingLiteral is built (`'['+embedding.join(',')+']'`) from a request-derived objective and then injected into the query context.
  - _Fix:_ Avoid constructing SQL literals from request-derived data. Pass embeddings as bound parameters or use the DB/driver-supported parameterized vector binding instead of assembling a string and interpolating it into the query. This reduces injection risk and makes intent explicit (e.g. use parameterized array binding or a safe helper for pgvector values).
- **server/plan-rollout-simulator.ts:119** — predictStep builds a vector string `vec = toPgVector(emb)` from the embedding and interpolates `${vec}::vector` into SQL in the kNN query. The embedding originates from request-driven inputs (step args).
  - _Fix:_ Do not inject stringified vectors into SQL. Use parameterized binding for the vector/array or a safe helper that binds the embedding as a parameter (or use a prepared statement / pgvector driver API). Ensure any request-derived blobs are passed as parameters rather than concatenated into SQL text.
- **server/plan-rollout-simulator.ts:246** — simulatePlanRollout persists steps by interpolating JSON into SQL using a nested sql template: `${sql`${JSON.stringify(steps)}::jsonb`}`. Steps are caller-supplied and being inlined into the query expression.
  - _Fix:_ Avoid inlining JSON via nested sql templates for request-derived payloads. Instead pass the JSON as a single bound parameter (e.g. VALUES (${tenantId}, ${options.planSummary ?? ""}, ${JSON.stringify(steps)}::jsonb, ...)) or use driver-bound parameters so the DB sees the value as data, not SQL text. Validate/normalize the steps before persisting and add explicit tenant scoping (already present) and error handling.

### LOW (1)

- **server/lib/skill-evolution-lessons.ts:304** — SQL uses a LIKE pattern built via string concatenation (title LIKE ${prefix + "%"}) where `prefix` is derived from input. While this is used as a parameter, concatenating user-derived content into SQL pattern strings can lead to unintended matching or injection-like pitfalls if not sanitized.
  - _Fix:_ Pass the LIKE pattern as a bound parameter without manual concatenation (e.g. const pattern = prefix + '%'; then use WHERE title LIKE ${pattern}) and ensure the prefix is validated/sanitized. Prefer parameter binding APIs provided by the ORM rather than building SQL fragments by concatenation.
