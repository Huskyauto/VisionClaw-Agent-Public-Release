# Tenant-Isolation Audit Report

_Generated 2026-06-25T23:35:08.325Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **405/405** file(s) audited · chunks 51/51 OK (0 failed)
- Findings: **296** (CRITICAL 71 · HIGH 172 · MEDIUM 52 · LOW 1)

### CRITICAL (71)

- **server/agentic-engines.ts:243** — autoApplyEligibleInsights() performs UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) RETURNING id, tenant_id,... with no tenant_id constraint — this bulk UPDATE can modify rows for all tenants.
  - _Fix:_ Never perform cross-tenant UPDATEs. Limit the update to a tenant (WHERE tenant_id = ${tenantId}) or loop tenants explicitly and apply the change per-tenant. If the intention is a global sweep, implement per-tenant batching to enforce ownership checks before modifying rows.
- **server/agentic/resume-worker.ts:66** — Background candidate SELECT scans agent_runs/agent_approvals across all tenants (no tenant_id filter). This reads tenant-scoped rows for every tenant rather than constraining to a single tenant context.
  - _Fix:_ Limit the initial candidate query by tenant (AND r.tenant_id = <tenantId>) or iterate per-tenant. If a global scan is required, perform per-tenant scoped queries or otherwise ensure an explicit admin-audit policy and strong justification; prefer adding a tenant_id predicate to the WHERE clause so every read is tenant-scoped.
- **server/agentic/wake-scheduler.ts:79** — runDueWakes performs an UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules ... LIMIT ${limit} ...) returning rows across tenants — there is no tenant_id constraint on the claimed rows. This atomically updates (and returns) tenant-scoped rows for all tenants in one operation.
  - _Fix:_ Restrict the claim/update to a specific tenant (WHERE tenant_id = ${tenantId}) or run the wake processing per-tenant. If a multi-tenant sweep is intended, query and claim rows grouped by tenant and apply an explicit tenant_id predicate in each UPDATE to preserve tenant isolation.
- **server/ceo-orchestrator.ts:815** — Selecting all rows from the personas table without constraining to the current tenant (cachedPersonas = await db.select().from(personasTable)). This reads a tenant-scoped table across tenants, allowing cross-tenant data exposure and misuse of persona IDs.
  - _Fix:_ Restrict the query to the plan owner’s tenant. Pass the tenantId into the query and add an explicit WHERE clause (or Drizzle .where(eq(personasTable.tenant_id, plan.tenantId))). Alternatively, fetch only the personas the current tenant is authorized to see or move persona caching into a per-tenant cache. Ensure every read from personas includes tenant_id = <caller tenant>.
- **server/channel-routing.ts:30** — Query joins channel_routes to personas (SELECT ... FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ...) but does not constrain the personas rows to a tenant. If personas is tenant-scoped this leaks persona data across tenants.
  - _Fix:_ Either make channel_routes tenant-scoped (add tenant_id) and filter by the caller's tenant, or add an explicit filtering condition to the JOIN (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = <callerTenantId>). Expose/accept a tenantId parameter to listChannelRoutes so it can enforce WHERE p.tenant_id = <callerTenantId> (or where cr.tenant_id = <callerTenantId>) for proper isolation.
- **server/data-protection.ts:112** — permanentlyPurgeSoftDeleted() selects expired conversations across all tenants (no tenant_id filter) and then deletes messages, compaction_archives, and conversations by conversation_id with no tenant scoping. This routine can remove data for any tenant without per-tenant constraints, risking accidental or malicious cross-tenant deletion.
  - _Fix:_ Scope purge operations per-tenant or require explicit admin confirmation. At minimum, include tenant_id in the initial query or accept an explicit tenantId parameter. When deleting related rows, include AND tenant_id = <conv.tenant_id> (or perform deletes via joins that verify tenant_id), or run this logic under an explicit, audited admin/maintenance context (and document/limit its use). Consider using withTenantTx or adding explicit checks that each conv.tenant_id is validated before deleting other tenant-scoped tables.
- **server/delivery-pipeline.ts:683** — SELECT from tenant-scoped delivery_logs by stripePaymentId with NO tenant_id constraint (eq(deliveryLogs.stripePaymentId, req.stripePaymentId)). This can return another tenant's delivery row and cause cross-tenant idempotent behavior/exposure.
  - _Fix:_ Include tenant scoping in the query (e.g. and(eq(deliveryLogs.stripePaymentId, req.stripePaymentId), eq(deliveryLogs.tenantId, req.tenantId))) or require a tenantId parameter and filter on it. If this lookup must be global, implement an explicit, documented, and audited admin-only path rather than an unscoped application query.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment performs SELECT FROM delivery_logs WHERE stripePaymentId = paymentId with NO tenant_id constraint, exposing another tenant's delivery row (and PII) to callers.
  - _Fix:_ Require and use tenantId to scope the lookup (WHERE stripe_payment_id = $1 AND tenant_id = $2). If a cross-tenant global lookup is intentionally needed, gate it behind an audited/admin-only path.
- **server/heartbeat.ts:52** — SELECT from research_schedules executed without scoping by tenant_id (SELECT * FROM research_schedules ...). This reads rows across all tenants.
  - _Fix:_ Scope the query to a tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant (getActiveTenantIds() then query each tenant). Alternatively use a storage helper that enforces tenant context.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET next_run_at ... WHERE id = ${sched.id} (no tenant_id constraint). This can update a schedule belonging to a different tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or call a storage-layer updater that asserts tenant ownership.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Add tenant check in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or use a storage API that enforces assertTenantContext before updating.
- **server/heartbeat.ts:112** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — no tenant_id constraint on update.
  - _Fix:_ Constrain updates by tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use storage.markResearchScheduleRun that accepts/validates tenant context.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} executed without tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause or use a storage helper that requires/validates tenant ownership before modifying rows.
- **server/heartbeat.ts:251** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. This directly mutates a tenant-scoped table without asserting tenant ownership.
  - _Fix:_ Use storage.updateHeartbeatTask(t.id, {...}) which enforces tenant context or add AND tenant_id = ${t.tenantId} to the WHERE clause (validate/resolve tenant via assertTenantContext first).
- **server/heartbeat.ts:255** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} (second occurrence) lacks a tenant_id WHERE filter.
  - _Fix:_ Add tenant scoping (AND tenant_id = ${t.tenantId}) or use the storage-layer updater that validates tenant ownership before writing.
- **server/heartbeat.ts:553** — SELECT * FROM event_log WHERE status = 'pending' ... is executed with no tenant filter — reads pending events across all tenants.
  - _Fix:_ Query events per-tenant or include tenant_id = ${tenantId} in the WHERE clause, or use an event-store API that returns only the tenant's events. If the heartbeat must process events across tenants, enumerate active tenant IDs and query per tenant.
- **server/heartbeat.ts:1588** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... has no tenant_id filter — reads logs across tenants.
  - _Fix:_ Restrict heartbeat_logs queries to the tenant (e.g. WHERE task_name = ... AND tenant_id = ${tenantId}) or use storage.getHeartbeatLogs with tenantId to ensure correct scoping.
- **server/heartbeat.ts:2766** — processDelegations calls storage.getPersonas() with NO tenant parameter. This can return personas across tenants and allow cross-tenant delegation/inspection.
  - _Fix:_ Call a tenant-scoped API (e.g. storage.getPersonas(tenantId) or storage.getPersonas(undefined, tenantId)) or otherwise assert and pass the task's tenantId so only that tenant's personas are returned.
- **server/heartbeat.ts:2815** — processDelegations calls storage.getHeartbeatTasks() with NO tenant parameter and then uses the result to enforce delegation caps / duplicate detection. This can operate on tasks from all tenants.
  - _Fix:_ Pass the tenantId into getHeartbeatTasks (e.g. storage.getHeartbeatTasks(undefined, tenantId)) or otherwise filter the returned tasks to the current tenant before enforcing caps/duplicates.
- **server/heartbeat.ts:2514** — buildTaskContext calls storage.getPersonas() with no tenant scoping (const allPersonas = await storage.getPersonas()), potentially leaking personas across tenants into the task context.
  - _Fix:_ Call a tenant-scoped personas getter or pass tenantId (e.g. storage.getPersonas(?, tenantId)) and/or validate via assertTenantContext so only personas owned by the tenant are returned.
- **server/heartbeat.ts:2860** — Direct DB UPDATE: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — no tenant_id constraint. This write can affect a row belonging to another tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause, or prefer storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }, tenantId) that asserts tenant ownership.
- **server/heartbeat.ts:2958** — delegateTaskFromChat calls storage.getPersonas() without tenant scoping (const allPersonas = await storage.getPersonas()), then selects a target persona by name. This may match personas from other tenants.
  - _Fix:_ Query personas scoped to the provided tenantId (storage.getPersonas(undefined, tenantId) or similar) and validate the chosen persona belongs to tenantId before proceeding.
- **server/heartbeat.ts:3236** — Direct DB UPDATE: UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} in delegateTaskFromChat — missing tenant_id constraint.
  - _Fix:_ Constrain the update by tenant_id (AND tenant_id = ${tenantId}) or use the storage API that will assert tenant ownership before changing approval_status.
- **server/internal-resolver.ts:91** — finalizeEvent() updates event_log without constraining by tenant_id (WHERE id = ${eventId} AND status IN (...)). This can mutate/finalize events across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${tenantId}) or first SELECT the event row and validate its tenant before updating. If the resolver runs without a tenant context, ensure it intentionally acts only on system-level events and document/guard that with RBAC/authorization checks.
- **server/lib/scheduled-post-runner.ts:62** — SELECT FROM tenant-scoped table `scheduled_posts` without constraining by tenant_id (CTE 'due' selects id FROM scheduled_posts with no tenant filter). This allows the system runner to read rows across all tenants without an explicit tenant constraint.
  - _Fix:_ Restrict the SELECT to a specific tenant by adding `AND tenant_id = ${someTenantId}` or otherwise explicitly document and enforce a safe system-scope pattern (e.g. iterate per-tenant or include tenant_id in the CTE). Ensure the caller's tenant context is always applied when reading tenant-scoped tables, or make the cross-tenant intent explicit and safe with additional access checks.
- **server/lib/scheduled-post-runner.ts:70** — UPDATE on tenant-scoped table `scheduled_posts` (UPDATE ... FROM due WHERE sp.id = due.id) executes without a tenant_id constraint. The CTE 'due' was populated without tenant bounding, and the UPDATE does not include tenant_id in its WHERE clause.
  - _Fix:_ Include tenant_id in the UPDATE's WHERE clause (e.g. `WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id` or otherwise ensure the CTE contains tenant_id and the UPDATE matches on tenant_id). Prefer making tenant explicit so writes cannot affect other tenants' rows.
- **server/memory-graph.ts:195** — INSERT into memory_links omits tenant_id column entirely — tenant_id is required for tenant-scoped tables and INSERTs must explicitly pass tenantId.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO memory_links (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Ensure link creation always records the tenant and that the earlier existence check also filters by tenant_id.
- **server/minerva-planner.ts:355** — CAS UPDATE on plans (decidePlan) does not restrict by tenant. The statement updates plans WHERE id = ${args.planId} AND status = 'awaiting_approval' RETURNING tenant_id,... — no tenant_id in WHERE, allowing cross-tenant state changes.
  - _Fix:_ Require and pass tenantId into decidePlan (or otherwise assert ownership) and include AND tenant_id = ${tenantId} in the WHERE clause. All CAS/UPDATE paths that change plan state must be tenant-scoped.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(lockKey) inside a query: `tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`)` where lockKey is derived from caller-supplied ledgerKey/tenantId. This mixes raw SQL with request-derived data and creates SQL-injection/isolation-risk (and bypasses parameter binding).
  - _Fix:_ Avoid sql.raw with any request-derived value. Pass a validated numeric value as a bound parameter instead, e.g. compute a BigInt and use parameter binding: `await tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`);` — after validating ledgerKey input. Alternatively validate and cast the lock value server-side and never interpolate raw strings. Validate ledgerKey length/charset before use.
- **server/plan-executor.ts:101** — UPDATE plans in appendExecutionLog() updates a tenant-scoped table by id only (WHERE id = ${planId}) with NO tenant_id constraint. This can modify another tenant's plan if planId is mis-used.
  - _Fix:_ Include the plan's tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or accept/require tenantId as a parameter and use it in the query to ensure tenant-scoped updates.
- **server/plan-executor.ts:324** — Claim CAS query (UPDATE plans ... WHERE id = ${planId} AND status = 'approved' RETURNING ...) performs a write/read on plans without scoping to tenant_id. executePlan(planId) has no tenant context so a caller could claim another tenant's plan by id.
  - _Fix:_ Restrict the CAS by tenant too: require the caller to pass tenantId (or use the tenant_id returned from a prior verified lookup) and add AND tenant_id = ${tenantId} to the WHERE clause; never rely on id-alone for tenant-scoped mutations.
- **server/plan-executor.ts:338** — UPDATE plans SET status = 'failed' WHERE id = ${planId} executes against tenant-scoped table without tenant_id filter when plan has no steps.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Pass tenantId into this code path or derive it from a previously-validated row and use it for all subsequent queries.
- **server/plan-executor.ts:423** — Final UPDATE on plans (setting status/execution_log) uses WHERE id = ${planId} with NO tenant_id constraint, possibly updating other tenants' rows.
  - _Fix:_ Use WHERE id = ${planId} AND tenant_id = ${tenantId} (require tenantId param or reuse the PlanRow.tenant_id read earlier) so all plan mutations are scoped to the owning tenant.
- **server/plan-executor.ts:457** — Error-path UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' lacks tenant_id constraint and may flip another tenant's plan into failed state.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or require tenant context) to the WHERE clause to ensure the update only affects the intended tenant's plan.
- **server/plan-executor.ts:473** — resumeStuckPlans(): UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id runs across all tenants (no tenant_id filter). This globally resets plans for all tenants.
  - _Fix:_ This recovery must be tenant-scoped. Either run per-tenant (pass tenantId) or add a tenant_id = ${tenantId} filter. If boot recovery must act across tenants, iterate tenants and perform a tenant-scoped update per tenant rather than a global UPDATE.
- **server/plan-executor.ts:482** — resumeStuckPlans(): SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 selects approved plans globally (no tenant_id constraint) and will kick plans from other tenants into execution.
  - _Fix:_ Scope this SELECT by tenant_id (e.g. WHERE status = 'approved' AND tenant_id = ${tenantId}). If the intention is to process across tenants, iterate a tenant list and perform tenant-scoped selects rather than a single global query.
- **server/proposal-verifier.ts:33** — verifyProposalById(): initial SELECT FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped proposals table by id only with NO tenant_id constraint. A caller could access another tenant's proposal by id.
  - _Fix:_ Require tenantId (or verify ownership) when invoking verification and add AND tenant_id = ${tenantId} to the SELECT. All subsequent logic should use the verified tenantId.
- **server/proposal-verifier.ts:245** — persist(): UPDATE code_proposals SET verification_status = ... WHERE id = ${proposalId} updates a tenant-scoped table without tenant_id in WHERE, potentially modifying another tenant's proposal.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and pass/derive tenantId from the verified proposal row.
- **server/proposal-verifier.ts:372** — attemptRebaseOnDrift(): UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} updates code_proposals without tenant constraint when LLM decides "NO_LONGER_APPLICABLE".
  - _Fix:_ Add tenant scoping (AND tenant_id = ${tenantId}) to these UPDATEs and require tenantId be passed to attemptRebaseOnDrift to prevent cross-tenant modifications.
- **server/proposal-verifier.ts:396** — attemptRebaseOnDrift(): UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (marking 'hallucinated') lacks tenant_id in WHERE.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause and ensure the function has tenant context before performing updates.
- **server/proposal-verifier.ts:408** — attemptRebaseOnDrift(): UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diff to proposal without tenant filter.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE and only persist rebased diffs after verifying the proposal belongs to the tenant invoking the operation.
- **server/recurring-messages.ts:294** — UPDATE on tenant-scoped table `agent_knowledge` lacks a tenant_id constraint (UPDATE agent_knowledge SET content = ..., updated_at = NOW() WHERE id = ${r.id}). Updating by id alone can modify rows belonging to other tenants if ids collide or are mis-attributed.
  - _Fix:_ Include tenant isolation in the UPDATE WHERE clause, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. Alternatively, perform the UPDATE using a previously-scoped-select (with tenant_id) and pass the tenant id explicitly to the UPDATE to ensure the row belongs to the expected tenant.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses only id in WHERE (UPDATE memory_entries SET fact = ..., last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${storedId}) without constraining tenant_id. This can update another tenant's memory if ids are misused.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId} (or persona_id if appropriate). Always include the calling tenant's tenant_id in UPDATE/DELETE WHERE clauses to enforce isolation.
- **server/research-engine.ts:156** — UPDATE on tenant-scoped table research_sessions (cleanupZombieSessions) has no tenant_id constraint — this will mark sessions across all tenants as completed.
  - _Fix:_ Scope the update by tenant (or iterate per-tenant). e.g. add a tenantId parameter and include `AND tenant_id = ${tenantId}` (parameterized) in the WHERE clause, or query affected session ids per-tenant and update only those. Never run an unscoped UPDATE on a tenant-scoped table.
- **server/research-engine.ts:388** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' ... has no tenant_id constraint — will delete autoresearch knowledge for all tenants.
  - _Fix:_ Restrict deletes to the caller's tenant, e.g. `DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()` or perform per-tenant cleanup. Always include tenant_id in DELETEs on tenant-scoped tables.
- **server/routes.ts:160** — Startup restoreUploadsFromDb reads all rows from file_storage without any tenant_id constraint (selecting filename/data/storageKey). This can restore files for all tenants into the shared uploads directory, leading to cross-tenant data exposure.
  - _Fix:_ Constrain the query by tenant (e.g. .where(eq(fileStorage.tenantId, tenantId)) or iterate per-tenant explicitly), or run the restore only for a specific trusted tenant/admin context. Ensure any disk-write of tenant files is done only for the intended tenant and never mixes files across tenants.
- **server/routes.ts:3961** — Batch DB restore: selecting file_storage rows by filename (inArray(fileStorage.filename, [...missingFiles])) with NO tenant_id filter, then writing those files to the uploads dir. This can pull another tenant's file by filename and make it available to the current tenant/conversation.
  - _Fix:_ Add tenant scoping to the query (e.g. .where(and(inArray(fileStorage.filename, [...missingFiles]), eq(fileStorage.tenantId, convTenantId)))) so only files owned by the conversation's tenant are restored. Validate ownership before writing files to disk.
- **server/routes.ts:4471** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT `INSERT INTO project_notes (project_id, note, author) VALUES (...)` omits tenant_id, so the row is not explicitly tied to a tenant (violates invariant that tenant_id must be passed on INSERT).
  - _Fix:_ Include tenant_id in the INSERT and ensure the project belongs to the caller's tenant. Example: `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})`. Also validate that `projId` refers to a project owned by the same tenant before inserting.
- **server/sculptor.ts:94** — UPDATE on tenant-scoped table sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This relies solely on id and does not enforce caller's tenant scope.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise validate ownership before mutating. Prefer always constraining by tenant_id for writes to tenant-scoped tables.
- **server/sculptor.ts:101** — UPDATE on tenant-scoped table sculptor_sessions omits tenant_id in WHERE clause (UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id}). No tenant filter is present.
  - _Fix:_ Add tenant constraint to the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or verify the session belongs to the caller's tenant before updating.
- **server/sculptor.ts:156** — UPDATE on tenant-scoped table sculptor_sessions omits tenant_id in WHERE clause inside monitorSession (UPDATE sculptor_sessions ... WHERE id = ${sessionId}). The update can affect a row outside the caller's tenant if IDs are misused.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or re-query/verify ownership of the session before performing the update. Ensure all writes to tenant-scoped tables include tenant_id predicates.
- **server/sculptor.ts:346** — UPDATE on tenant-scoped table sculptor_sessions omits tenant_id in WHERE clause when saving review_result (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). Missing tenant scoping allows cross-tenant mutation.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or validate that the session belongs to the provided tenant before updating. Enforce tenant scoping on all writes.
- **server/safety/transactional-snapshot.ts:407** — pool.query rollback clears undone_at on action_snapshots using only id (UPDATE action_snapshots SET undone_at = NULL WHERE id = $1). The UPDATE lacks tenant_id scoping so it could affect snapshots belonging to other tenants.
  - _Fix:_ Include tenant scoping in the rollback UPDATE (e.g. UPDATE action_snapshots SET undone_at = NULL WHERE id = $1 AND tenant_id = $2 with [row.id, ctx.tenantId]) or use a transaction-validated claim that includes tenant_id. Always include tenant_id on writes to tenant-scoped tables.
- **server/second-opinion.ts:405** — settleFusionReservation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}) without scoping by tenant_id. This mutates a tenant-scoped ledger row without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${expectedTenantId}) or re-validate the reservation row belongs to the caller's tenant before settling. Ensure ledger updates include tenant_id predicates.
- **server/second-opinion.ts:424** — releaseFusionReservation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger SET cost_usd = '0', ... WHERE id = ${reservationId}) and omits tenant_id. This can release/modify reservations across tenants if id is misused.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. WHERE id = ${reservationId} AND tenant_id = ${expectedTenantId}) or pre-fetch/verify the reservation's tenant before modifying. Enforce tenant_id in all writes to tenant-scoped tables.
- **server/seed.ts:2859** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a program row by id without validating tenant ownership (no tenant_id constraint). This can modify another tenant's row if ids overlap or are global.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId} or use Drizzle .where(and(eq(researchPrograms.id, p.id), eq(researchPrograms.tenantId, tenantId)))). Always verify tenant ownership before updating tenant-scoped rows.
- **server/seed.ts:3212** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is executed without setting tenant_id. If project_conversations is a tenant-scoped table, the INSERT omits the required tenant_id column and will create cross-tenant links or violate the invariant that tenant_id must be provided on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id})). Before inserting, validate that both project and conversation belong to the same tenant and that the caller is authorized for that tenant.
- **server/seed.ts:4374** — DELETE FROM heartbeat_tasks WHERE enabled = false executes against a tenant-scoped table without any tenant_id constraint, deleting rows across all tenants.
  - _Fix:_ Restrict the delete to a specific tenant (e.g. WHERE enabled = false AND tenant_id = <tenantId>) or iterate per-tenant. Use Drizzle's where(eq(heartbeatTasks.tenantId, tenantId)) or parameterize tenant_id in the SQL.
- **server/seed.ts:4402** — UPDATE memory_entries ... WHERE deleted_at IS NULL ... performs mass updates on the tenant-scoped table 'memory_entries' without a tenant_id constraint, which will modify memory for all tenants.
  - _Fix:_ Add AND tenant_id = <tenantId> to the WHERE clause (or use Drizzle .where(eq(memoryEntries.tenantId, tenantId))). If this is intended as a global migration for tenant 1 only, set tenant_id = 1 explicitly in the WHERE; otherwise run per-tenant.
- **server/seed.ts:4501** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks for all tenants (no tenant_id filter), disabling tasks across tenants.
  - _Fix:_ Add AND tenant_id = <tenantId> to the WHERE clause or run the update per-tenant. Use Drizzle's .where(eq(heartbeatTasks.tenantId, tenantId)) to ensure changes are limited to the intended tenant.
- **server/seed.ts:4512** — UPDATE heartbeat_tasks SET cron_expression = '0 * * * *' WHERE name = 'Self-Reflection' AND cron_expression != '0 * * * *' updates tenant-scoped heartbeat_tasks for all tenants because there is no tenant_id constraint.
  - _Fix:_ Restrict the update to the intended tenant (e.g. WHERE name = 'Self-Reflection' AND cron_expression != '0 * * * *' AND tenant_id = <tenantId>) or perform per-tenant reconciliations. Prefer Drizzle .where(eq(...)) filters.
- **server/seed.ts:4517** — UPDATE heartbeat_tasks SET cron_expression = '0 */2 * * *' WHERE name = 'Memory Consolidation' AND cron_expression NOT IN ('0 */2 * * *') updates the tenant-scoped table without restricting by tenant_id, affecting all tenants.
  - _Fix:_ Include AND tenant_id = <tenantId> in the WHERE clause or execute the update per-tenant. Use parameterized queries / Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)).
- **server/seed.ts:4528** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true disables tasks across all tenants because there is no tenant_id restriction.
  - _Fix:_ Add AND tenant_id = <tenantId> or apply the update per-tenant. Enforce tenant checks using Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) or include tenant_id in the raw SQL parameters.
- **server/seed.ts:4551** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true updates heartbeat_tasks for all tenants (no tenant_id constraint), potentially disabling many tenants' tasks.
  - _Fix:_ Include AND tenant_id = <tenantId> in the WHERE clause or process this logic per-tenant. Use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) to guarantee per-tenant updates.
- **server/seed.ts:4680** — UPDATE agent_knowledge SET content = ${platformBriefingContent}, priority = 10, updated_at = NOW() WHERE id = ${rows[0].id} updates the tenant-scoped table 'agent_knowledge' without a tenant_id WHERE constraint. Although the id came from a prior SELECT that included tenant_id = 1, the UPDATE itself lacks an explicit tenant check.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. AND tenant_id = <tenantId>) or include tenant_id = 1 in the WHERE. Prefer parameterized Drizzle updates with .where(eq(agentKnowledge.tenantId, tenantId)).
- **server/self-improvement.ts:394** — runExperiment updates the experiments row using .where(eq(experiments.id, experiment.id)) with NO tenant constraint — an UPDATE to a tenant-scoped table without tenant_id in the WHERE can modify another tenant's row if an id is known.
  - _Fix:_ Constrain the UPDATE by tenant as well: include tenant id in the WHERE (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Alternatively, fetch and validate the experiment belongs to config.tenantId before updating. Apply same fix to the error-path UPDATE at lines 413-415.
- **server/storage.ts:1206** — getAllDataForExport reads many tenant-scoped tables without any tenant filter (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) and returns a full platform-wide export.
  - _Fix:_ Restrict this function to an explicit admin context and require an admin-authenticated tenantId parameter OR split the export to accept a tenantId and only export that tenant's data. At minimum, require an explicit confirmation flag and strong authorization; prefer scoping each SELECT by tenant_id.
- **server/tool-learning.ts:283** — UPDATE on tenant-scoped table `custom_tools` updates implementation where id = tool.id but does NOT include tenant_id in the WHERE clause. This permits modifying another tenant's tool if the id is known.
  - _Fix:_ Enforce tenant isolation on writes. Add tenant_id to the WHERE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or otherwise require and verify the row's tenant before performing the update.
- **server/tool-learning.ts:300** — DELETE on tenant-scoped table `custom_tools` deletes by id only and does NOT constrain by tenant_id. This can delete another tenant's tool if the id is known.
  - _Fix:_ Require and enforce tenant scoping on deletes. Add tenant_id to the WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or validate ownership before deleting.
- **server/venture-discovery/budget.ts:155** — settleVentureReservation performs an UPDATE on agent_cost_ledger using only the reservation id (WHERE id = ${reservationId}) with no tenant_id constraint. That lets a caller (or bug) that supplies or obtains a reservation id settle or modify a ledger row belonging to another tenant.
  - _Fix:_ Include tenant scoping when settling a reservation. Either pass tenantId into settleVentureReservation and add AND tenant_id = ${tenantId} to the WHERE clause, or first SELECT the row by id and verify its tenant_id matches the expected tenant before updating (and fail if it doesn't).
- **server/venture-discovery/budget.ts:169** — releaseVentureReservation issues an UPDATE on agent_cost_ledger (SET cost_usd='0' WHERE id = ${reservationId}) without checking tenant_id. This can zero-out another tenant's reservation row if the id is not validated.
  - _Fix:_ Same fix as for settle: require/accept tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or verify the reservation row belongs to the tenant before releasing it.

### HIGH (172)

- **server/ab-optimizer.ts:110** — SELECT from experiments in runDueAbExperiments() does not constrain by tenant_id (reads tenant-scoped table across tenants).
  - _Fix:_ Restrict this query to a specific tenant (WHERE tenant_id = $1) and run the heartbeat per-tenant, or otherwise enumerate tenant_ids and query per-tenant. Do not issue a cross-tenant SELECT against experiments.
- **server/agent-activity.ts:122** — UPDATE agentActivity in completeActivity() uses WHERE eq(agentActivity.id, activityId) with no tenant_id constraint — updates a tenant-scoped table by id only.
  - _Fix:_ Require and accept tenantId in completeActivity() (or look up tenant_id earlier) and include eq(agentActivity.tenantId, tenantId) in the WHERE clause to prevent modifying rows belonging to other tenants.
- **server/agentic-engines.ts:115** — routeInsightToMinerva() SELECT on plans uses WHERE source = ... AND source_ref = ... but does not constrain tenant_id — may return a plan belonging to another tenant.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${insight.tenantId}) or otherwise ensure the plan lookup only returns plans for the same tenant as the insight.
- **server/agentic-engines.ts:150** — routeInsightToMinerva() UPDATE ai_insights ... WHERE id = ${insight.insightId} lacks a tenant_id constraint — updating a tenant-scoped table by id only.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId}) or require tenantId and validate ownership before updating.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting() SELECT FROM ai_insights queries status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER with no tenant constraint — a sweep touching rows across all tenants.
  - _Fix:_ Either run the sweep per-tenant (pass tenantId and add WHERE tenant_id = ...) or otherwise partition the operation so updates/reads are always constrained to the tenant in scope.
- **server/agentic-features.ts:526** — UPDATE lead_enrichments in scoreLeads() uses WHERE id = ${lead.id} with no tenant_id constraint — updating a tenant-scoped table by id only.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or pass/validate tenant ownership before performing the UPDATE.
- **server/agentic-features.ts:600** — enrollInSequence() runs a SELECT on outreach_enrollments (SELECT id FROM outreach_enrollments WHERE sequence_id = ... AND contact_email = ... AND status = 'active') without tenant_id — reading another tenant's enrollments and allowing cross-tenant checks to succeed/fail incorrectly.
  - _Fix:_ Include tenant scoping in the SELECT (AND tenant_id = ${params.tenantId}) so the existence check only considers enrollments for the caller's tenant.
- **server/agentic-features.ts:638** — advanceSequence() updates outreach_enrollments SET status = 'completed' WHERE id = ${enrollment.id} with no tenant_id constraint — modifying tenant-scoped rows by id only.
  - _Fix:_ Include tenant scoping in the UPDATE (WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) or tie the UPDATE to the earlier FOR UPDATE-selected rows that were already tenant-scoped to ensure ownership before writing.
- **server/agentic-features.ts:674** — advanceSequence() updates outreach_enrollments SET current_step = ... WHERE id = ${enrollment.id} with no tenant_id constraint — write against a tenant-scoped table without tenant filter.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${params.tenantId}) to ensure the enrollment being updated belongs to the caller's tenant.
- **server/agentic-features.ts:724** — classifyReply() updates outreach_enrollments SET reply_classification, reply_content, status WHERE id = ${row.id} with no tenant_id clause — updating a tenant-scoped table by id only.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or require and verify tenant ownership before executing the UPDATE.
- **server/agentic/approvals.ts:97** — expireStaleApprovals() does a bulk UPDATE on agentApprovals to mark status='expired' for stale approvals but the UPDATE has no tenant_id constraint — affecting approvals across tenants.
  - _Fix:_ Perform expiry per-tenant (add tenant_id = ${tenantId}) or enumerate tenants and run the expiry per-tenant so no bulk cross-tenant UPDATE is executed. Always include tenant scoping in UPDATE/DELETE operations on tenant-scoped tables.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets executes without a tenant_id WHERE clause, reading a tenant-scoped table across all tenants.
  - _Fix:_ If this is intended as a per-tenant operation, add an explicit WHERE tenant_id = ${tenantId} filter. If it is intentionally a multi-tenant admin sweep, make the admin intent explicit (e.g. require an ADMIN_TENANT_ID constant or an explicit admin flag), log/audit the action, and ensure only privileged code can call it. Avoid blind cross-tenant reads from tenant-scoped tables unless you're performing a controlled admin job with explicit checks.
- **server/agentic/harness-adaptation.ts:72** — Query against agent_trace_spans omits any tenant_id restriction (SELECT ... FROM agent_trace_spans ... LIMIT 4000). This reads a tenant-scoped table without constraining to a caller tenant.
  - _Fix:_ Either add an explicit WHERE tenant_id = ${tenantId} parameter (and accept tenantId into mineFailures) or, if this is an intended global/admin harvest, require and use the ADMIN_TENANT_ID constant or an explicit admin-only path and document that this is a privileged, cross-tenant operation. Ensure such cross-tenant reads are audited and confined to code paths that have verified admin privileges.
- **server/agentic/runs.ts:39** — appendStep updates agent_runs with .where(eq(agentRuns.id, runId)) but does not include a tenant_id constraint. This allows updating a run row without verifying the caller's tenant owns it.
  - _Fix:_ Require the tenantId when appending a step (e.g. where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or fetch and verify ownership before updating. Make appendStep tenant-scoped.
- **server/agentic/runs.ts:51** — updateRunState updates agent_runs .where(eq(agentRuns.id, runId)) with no tenant_id constraint, permitting cross-tenant updates to run state.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Pass tenantId into callers or validate ownership prior to updating.
- **server/agentic/runs.ts:55** — replaceRunState updates agent_runs .where(eq(agentRuns.id, runId)) without constraining by tenant_id, allowing unauthorized cross-tenant state replacement.
  - _Fix:_ Add tenant_id verification in the update WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) and require callers to provide/validate tenantId.
- **server/agentic/runs.ts:64** — completeRun updates agent_runs .where(eq(agentRuns.id, runId)) with no tenant_id filter, so callers can mark runs completed for other tenants.
  - _Fix:_ Constrain the update by tenant (AND agent_runs.tenant_id = <tenantId>) or otherwise verify ownership before modifying run rows.
- **server/agentic/runs.ts:73** — failRun updates agent_runs .where(eq(agentRuns.id, runId)) without tenant scoping, enabling cross-tenant state tampering.
  - _Fix:_ Require and use tenantId in the WHERE clause (e.g. and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers pass tenantId.
- **server/agentic/runs.ts:81** — pauseRun updates agent_runs .where(eq(agentRuns.id, runId)) with no tenant_id predicate, allowing a tenant to pause/modify runs belonging to other tenants.
  - _Fix:_ Add tenant scoping to the update (e.g. where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and surface tenantId to callers so they can assert ownership.
- **server/auto-asset-capture.ts:145** — INSERT into project_files occurs without proving the referenced project row is owned by the caller's tenant. project_files has no tenant_id column; code only asserted the conversation belongs to the tenant but did not verify the project (conversation.project_id) belongs to the tenant. A poisoned/foreign conversation.project_id could let an attacker write files into another tenant's project.
  - _Fix:_ Before inserting into project_files, verify the project belongs to the acting tenant (e.g. SELECT id FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}) or include tenant-scoped metadata on the insert. Alternatively, require and pass a project row that was fetched with a tenant_id constraint and use that trusted row's id for the insert.
- **server/auto-asset-capture.ts:164** — INSERT into project_notes writes a note for project_id derived from the conversation without verifying the project is owned by the acting tenant. project_notes has no tenant_id column and the project reference was not guarded against cross-tenant poisoning.
  - _Fix:_ Ensure the project row referenced by projectId is owned by tenantId before inserting notes (e.g. SELECT id FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}) or include tenant information on the note write so transitive isolation cannot be bypassed.
- **server/auto-transcript.ts:179** — INSERT INTO project_conversations (project_id, conversation_id) uses project_id taken from a conversations row returned by a query that did not verify the project row's tenant. This can create a project_conversations link across tenants if conversation.project_id points to a project owned by another tenant.
  - _Fix:_ Before inserting project_conversations based on conversation.project_id, verify the project belongs to the same tenant (SELECT id FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}). Only insert when that tenant-ownership check passes, or persist tenant_id alongside project_conversations and constrain writes accordingly.
- **server/auto-memorize.ts:100** — isDuplicate() queries recent memory_entries without any tenant_id filter (SELECT fact FROM memory_entries ... LIMIT 200). This reads tenant-scoped memory_entries across all tenants for deduplication, potentially causing cross-tenant leakage and incorrect deduplication.
  - _Fix:_ Restrict dedupe scans to the tenant context being processed (WHERE tenant_id = ${tenantId}) or otherwise isolate per-tenant datasets. If global deduplication is intentional, make that explicit and ensure proper governance/consent; prefer per-tenant operation to maintain isolation.
- **server/auto-memorize.ts:159** — runAutoMemorize pulls messages across ALL tenants (SELECT FROM messages LEFT JOIN conversations ...) without a tenant_id constraint. This background job reads tenant-scoped messages across tenants in one global query, which violates per-tenant isolation expectations and risks exposing tenant data to admin-level processing without explicit scoping.
  - _Fix:_ If the job must process per-tenant data, iterate tenants and run tenant-scoped queries (include WHERE c.tenant_id = ${tenantId} or m.tenant_id if present). If a global aggregation is intended, document it clearly and ensure appropriate access controls/auditing; otherwise add tenant filtering to the messages SELECT.
- **server/autonomy.ts:185** — createRule inserts autonomy_rules.persona_id from caller-supplied data without verifying that the referenced persona belongs to the same tenant.
  - _Fix:_ Before inserting, validate the personaId (if provided) belongs to the tenant: SELECT id FROM personas WHERE id = ${data.personaId} AND tenant_id = ${tenantId}; reject the request if not found. Or disallow arbitrary persona_id in the API and require only persona IDs scoped to the tenant. Consider enforcing this at DB level (multi-column FK or application-enforced invariant) if appropriate.
- **server/autonomy.ts:211** — updateRule allows updating persona_id (via COALESCE(${data.personaId...})) on autonomy_rules using an arbitrary caller-supplied personaId without validating that persona belongs to the tenant.
  - _Fix:_ When changing persona_id ensure the new persona belongs to the same tenant (e.g. SELECT id FROM personas WHERE id = ${data.personaId} AND tenant_id = ${tenantId} and fail the update if not). Alternatively restrict updates so persona_id cannot be set to an id outside the tenant, or add DB-level constraints to prevent cross-tenant references.
- **server/business-tools.ts:84** — INSERT into invoice_items omits tenant_id. If invoice_items is a tenant-scoped table, this insert creates rows that are not constrained to the caller's tenant (orphaned rows / cross-tenant contamination). There is no verification that invoice_items belongs to the same tenant as the parent invoice.
  - _Fix:_ Include tenant_id in the invoice_items INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)). Ensure invoice_items is designed as tenant-scoped and that the application always sets tenant_id from the authenticated caller (not from untrusted input). Consider adding a DB constraint (tenant_id NOT NULL) and/or enforcing tenant ownership server-side when creating line items.
- **server/business-tools.ts:334** — LEFT JOIN customers cust ON cust.id = c.customer_id has no tenant constraint on the joined customers row. Because there is no DB-level FK/tenant enforcement, this join can return a customer from a different tenant if c.customer_id points to an id from another tenant — resulting in cross-tenant data exposure.
  - _Fix:_ Restrict the joined customer to the same tenant in the JOIN condition (or WHERE) e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = ${tid}. Always ensure every join that references another tenant-scoped table also constrains that table by tenant_id to enforce application-level isolation.
- **server/channel-routing.ts:54** — setChannelRoute(channel, personaId) inserts a caller-supplied persona_id into channel_routes without verifying that the persona belongs to the caller's tenant. This lets a caller associate another tenant's persona ID (foreign id) into routing state.
  - _Fix:_ Require and use a tenantId parameter in setChannelRoute and validate ownership of the persona before inserting (e.g. SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). Alternatively, store tenant_id in channel_routes and enforce the tenant_id when inserting/updating so callers cannot link or overwrite cross-tenant data.
- **server/chat-engine.ts:4152** — Call to storage.attachCitations(...) performs a write related to a message but does not pass any tenant identifier. If the underlying citations table is tenant-scoped, this insert may omit tenant_id (or rely on implicit lookup) and violate tenant isolation.
  - _Fix:_ Pass the tenant context explicitly to the storage API (e.g. storage.attachCitations(messageId, citations, tenantIdForScope)) or have attachCitations validate/derive and assert the message's tenant ownership before performing any writes. Ensure the storage implementation always includes a WHERE tenant_id = <tenant> (or sets tenant_id on INSERT) so writes cannot cross tenants.
- **server/chat-engine.ts:4410** — Call to storage.createMemoryEntry(...) inserts memory entries but does not include a tenantId. In a multi-tenant schema where memory entries are tenant-scoped (tenant_id NOT NULL), this either fails or creates entries not associated with the caller's tenant, breaking isolation.
  - _Fix:_ Include the tenant id in the create call (e.g. storage.createMemoryEntry({ ..., tenantId: tenantIdForScope })) or dispatch through a storage API that enforces and requires tenant context. Do not rely on later post-facto association — tenant_id must be set explicitly on INSERT and validated.
- **server/chat-engine.ts:4417** — Call to storage.updateMemoryEmbedding(entry.id, emb) performs an UPDATE by id with no tenant constraint passed. Per the codebase invariant, every UPDATE against tenant-scoped tables must include tenant scoping to prevent cross-tenant modification.
  - _Fix:_ Change the API call to include tenant context (for example storage.updateMemoryEmbedding(entry.id, emb, tenantIdForScope)) or have the storage layer internally require and check tenant ownership (e.g. UPDATE ... WHERE id=$id AND tenant_id=$tenantId). Always include the tenant_id constraint on UPDATEs to enforce isolation.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without any tenant_id constraint (background scanner selects all tenants).
  - _Fix:_ If this is intended to run as a multi-tenant admin job, make the cross-tenant intention explicit and protect it (e.g. run under a dedicated admin principal, document/limit scope). Otherwise, add an explicit tenant_id filter (WHERE tenant_id = ${tenantId}) or otherwise restrict the SELECT to the intended tenant. Consider using explicit admin APIs or a dedicated audit/ops route rather than ad-hoc cross-tenant queries.
- **server/compaction.ts:463** — INSERT into tenant-scoped table `memory_entries` using a possibly-undefined/unchecked tenantId (VALUES ... , ${tenantId}). The function accepts tenantId?: number and does not validate it before inserting.
  - _Fix:_ Require and validate a proper tenantId before inserting (e.g. ensure typeof tenantId === 'number' && tenantId > 0). If the conversation→tenant mapping must be derived, resolve and validate tenant ownership server-side before any INSERT. Do not insert NULL or unvalidated tenant identifiers into tenant-scoped tables.
- **server/credential-vault.ts:169** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} — UPDATE against tenant-scoped table lacks tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Even when ownership was previously checked via a separate query, always include tenant_id in UPDATE/DELETE statements to avoid race/TOCTOU or accidental cross-tenant modifications.
- **server/credential-vault.ts:176** — UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} — UPDATE against tenant-scoped table lacks tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${cred.id} AND tenant_id = ${tenantId}) or pass the resolved tenantId explicitly to this operation. Always enforce tenant_id at every write to tenant-scoped tables.
- **server/crews-engine.ts:854** — addFlowStep inserts a flow_steps.row with crew_id taken from caller params without verifying that the provided crewId belongs to the caller's tenant. This writes a cross-reference to another tenant's crew (or arbitrary id) because crew ownership is not validated before INSERT.
  - _Fix:_ Before inserting the flow_step, verify ownership of params.crewId (e.g. const crew = await getCrew(params.crewId, params.tenantId); if (!crew) reject). Alternatively, constrain the INSERT to only accept crew rows belonging to the tenant (e.g. INSERT ... SELECT ... FROM crews WHERE id = $crewId AND tenant_id = $tenantId) or perform the operation inside a tenant-scoped transaction (withTenantTx) / server-side check so a foreign crew_id cannot be linked from another tenant.
- **server/delivery-pipeline.ts:720** — UPDATE on tenant-scoped delivery_logs (clearing stripePaymentId / updating metadata) uses only WHERE id = prior.id with NO tenant_id constraint. This allows modifying another tenant's row if an id from another tenant is supplied.
  - _Fix:_ Restrict the UPDATE by tenantId: add AND tenant_id = <caller tenant> to the WHERE clause (or take tenantId as a parameter and use eq(deliveryLogs.tenantId, tenantId)). Alternatively perform the change via a transaction that first re-selects the row scoped to tenant and then updates by that scoped row id.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog() issues an UPDATE against tenant-scoped delivery_logs with .where(eq(deliveryLogs.id, id)) but WITHOUT any tenant_id constraint. This helper is widely used and will perform cross-tenant updates if misused.
  - _Fix:_ Add tenant scoping to this helper API: accept tenantId (or an object with tenant context) and include eq(deliveryLogs.tenantId, tenantId) in the WHERE clause. Update all call sites to pass the appropriate tenantId or use an internal admin-only variant with explicit checks/audit.
- **server/email.ts:349** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 executed during inbox polling has NO tenant_id constraint. A message_id lookup without tenant scoping can cause cross-tenant duplication checks / skipping inserts across tenants.
  - _Fix:_ Scope the existence check to the tenant (WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or otherwise derive the intended tenant before querying. If message_id is globally unique and you intend global lookup, make that explicit and document/audit it.
- **server/embeddings.ts:684** — storeEmbeddingVec performs UPDATE ${sql.raw(table)} SET embedding_vec = ... WHERE id = ${id} without any tenant_id constraint. This writes vector data into tenant-scoped tables (memory_entries/agent_knowledge) unscoped and can overwrite another tenant's row.
  - _Fix:_ Require and accept tenantId in storeEmbeddingVec (or resolve it via a secure context) and include AND tenant_id = ${tenantId} in the WHERE clause. Alternatively, load/verify the row belongs to the tenant before updating.
- **server/event-bus.ts:166** — UPDATE against tenant-scoped table event_log uses WHERE id = ${eventId} with no tenant_id constraint — allows updating other tenants' rows by id.
  - _Fix:_ Include tenant scoping in the WHERE clause, e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId}, or validate the row belongs to the caller's tenant before updating.
- **server/event-bus.ts:201** — UPDATE against tenant-scoped table event_log uses WHERE id = ${eventId} with no tenant_id constraint (processing_result update) — missing tenant scoping.
  - _Fix:_ Add tenant filter to the UPDATE: WHERE id = ${eventId} AND tenant_id = ${tenantId}. Always pair id-based updates with tenant_id to enforce isolation.
- **server/felix-brain.ts:19** — SELECT from agent_runs fetches rows across all tenants (no tenant_id filter) during rehydration — unsafely mixes/reads other tenants' in-flight runs into in-memory store.
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or otherwise iterate per-tenant with explicit, validated tenant context — never read tenant-scoped tables across tenants in a single unscoped query.
- **server/felix-loop.ts:676** — UPDATE felix_loop_runs SET ... WHERE id = ${runId} is executed without tenant_id constraint — write to a tenant-scoped table by id only, missing tenant scoping.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) or otherwise ensure the runId belongs to the tenant before updating.
- **server/felix-loop.ts:690** — Error-path UPDATE felix_loop_runs SET ended_at = NOW(), error = ... WHERE id = ${runId} lacks tenant_id in WHERE clause — missing tenant isolation on an UPDATE in the error handler.
  - _Fix:_ Constrain the UPDATE by tenant: WHERE id = ${runId} AND tenant_id = ${tenantId}. Ensure all UPDATE/DELETE statements against tenant-scoped tables always include tenant_id filtering.
- **server/firecrawl.ts:192** — UPDATE on tenant-scoped table 'scraped_pages' is performed using only the row id (where(eq(scrapedPages.id, existing[0].id))) with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))). Always include tenant filter on UPDATE to prevent cross-tenant modification even if the id was previously selected.
- **server/firecrawl.ts:316** — UPDATE on tenant-scoped table 'scraped_pages' is performed using only the row id (where(eq(scrapedPages.id, existing[0].id))) with no tenant_id constraint (in crawl loop).
  - _Fix:_ Add an explicit tenant constraint to the UPDATE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))). Enforce tenant_id on all UPDATE/DELETE statements against tenant-scoped tables.
- **server/google-drive.ts:560** — UPDATE on 'projects' table (db.update(projects).set(...).where(eq(projects.id, projectId))) is executed without an explicit tenant filter, even though ownership was checked earlier.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the write is always constrained to the caller's tenant and cannot be abused in races or via unexpected input.
- **server/health-audit.ts:314** — SELECT from code_proposals executes without any tenant_id filter. If code_proposals is tenant-scoped this will read rows across all tenants (cross-tenant access).
  - _Fix:_ If code_proposals is tenant-scoped, scope this query to a tenant (e.g. add WHERE tenant_id = ${tenantId}) and run the scanner per-tenant or require an explicit admin/system context. If global admin behaviour is intended, make that explicit and protect this path so only trusted system callers can run it.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' uses WHERE id = ANY(${ids}::int[]) with no tenant_id constraint. This will modify proposals for all tenants matching those ids (cross-tenant modification).
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or iterate per-tenant. Ensure only an explicit system/admin job can perform a global update and document/authorize that intent.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks (stuck tasks) executes with no tenant_id filter. If heartbeat_tasks is tenant-scoped this will surface tasks for all tenants (cross-tenant access).
  - _Fix:_ If heartbeat_tasks are per-tenant, add WHERE tenant_id = ${tenantId} and run per-tenant; otherwise require explicit admin/system context and guard this code to avoid accidental cross-tenant reads.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) has no tenant_id constraint and will disable tasks across tenants if heartbeat_tasks are tenant-scoped.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or perform per-tenant updates. If a global disable is intended, require an explicit admin/system-only path and add strong safeguards/logging.
- **server/internal-resolver.ts:106** — resolvePlanProposed() reads plans with SELECT ... FROM plans WHERE id = ${planId} LIMIT 1 without verifying tenant ownership.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or look up the event's tenant and validate the plan belongs to that tenant before acting. If the caller is system-global, require explicit admin authorization/intent before mutating tenant-scoped rows.
- **server/internal-resolver.ts:170** — resolveExperimentFailed() updates code_proposals (UPDATE ... WHERE id = ${proposalId} AND status = 'pending') without scoping by tenant_id, allowing cross-tenant mutation.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}). If event data does not include tenant, resolve the event-to-tenant mapping first and enforce the tenant check before updating tenant-scoped rows.
- **server/job-queue.ts:95** — claimDueJobs() UPDATE agent_jobs ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) performs claims across the agent_jobs table without any tenant_id filter, touching tenant-scoped rows for all tenants.
  - _Fix:_ If workers must be tenant-isolated, add a tenant_id filter or accept an explicit tenant parameter. If the worker is intentionally multi-tenant, ensure this is an approved design and protect mutator endpoints with appropriate operator-level authorization and auditing. At minimum, document the global nature and consider adding tenant fencing where appropriate.
- **server/job-queue.ts:131** — reclaimExpiredLeases() runs UPDATEs against agent_jobs to mark reclaimed/terminal status without tenant_id constraints (updates all tenants' running jobs whose leases expired).
  - _Fix:_ Scope reclamation to a tenant when invoked on behalf of a tenant, or require explicit system-admin privilege for global reclamation. Add tenant_id = ${tenantId} to the WHERE clause when reclaiming per-tenant, or document and gate the global behavior behind admin-only operations.
- **server/job-queue.ts:173** — completeJob() UPDATE agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} does not validate tenant_id, allowing a caller who knows an id to complete jobs belonging to other tenants.
  - _Fix:_ Add tenant_id to the WHERE predicate (e.g. AND tenant_id = ${tenantId}) or require the caller to present the tenant context and verify ownership before marking a job as succeeded.
- **server/job-queue.ts:201** — failJob() SELECT max_attempts FROM agent_jobs WHERE id = ${id} and subsequent UPDATEs modify agent_jobs without constraining by tenant_id, enabling cross-tenant reads/writes by job id alone.
  - _Fix:_ Include tenant_id checks on SELECT/UPDATE statements (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}), or fetch the job and verify its tenant before performing failure handling. Ensure background/system workers perform these operations under an explicit tenant context or with admin-level safeguards.
- **server/job-queue.ts:238** — cancelJob() UPDATE agent_jobs WHERE id = ${id} AND status IN (...) does not check tenant_id, allowing cancelling jobs belonging to other tenants by id.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or require an explicit tenant context for cancellation calls. If operator/admin flows must cancel across tenants, gate them behind admin authorization and auditing.
- **server/job-queue.ts:246** — retryJob() UPDATE agent_jobs WHERE id = ${id} AND status IN (...) performs retries without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the UPDATE predicate (WHERE id = ${id} AND tenant_id = ${tenantId} ...) or require that callers supply and validate tenant context before performing retry operations.
- **server/lib/agent-trace.ts:124** — persistSpanClose() UPDATE agent_trace_spans SET ... WHERE span_id = ${spanId} updates span rows without constraining tenant_id, allowing a close to affect spans in any tenant.
  - _Fix:_ Persist the tenant_id at open time and include tenant_id in the UPDATE predicate (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}), or fetch/verify the span's tenant before performing the close. Ensure the closing path has access to the same tenant context used to open the span.
- **server/lib/inbox-ingest.ts:431** — SELECT to find an existing orphan inbox row omits tenant scoping: "SELECT m.id FROM inbox_messages m LEFT JOIN inbox_classifications c ... WHERE m.message_id = ${id} AND c.id IS NULL" lacks m.tenant_id = ${tenantId}. This can return rows from other tenants.
  - _Fix:_ Add an explicit tenant predicate to the WHERE clause (e.g. AND m.tenant_id = ${tenantId}) so the lookup is scoped to the caller's tenant. Failing closed is preferred if tenantId cannot be asserted.
- **server/lib/inbox-ingest.ts:491** — UPDATE on tenant-scoped table `inbox_classifications` updates rows by inbox_message_id only: "UPDATE inbox_classifications SET routed_to = ... WHERE inbox_message_id = ${storeResult.id}" — no tenant_id check. This can modify another tenant's classification row if inbox_message_id is not globally unique across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. "WHERE inbox_message_id = ${storeResult.id} AND tenant_id = ${tenantId}". Make tenant_id part of the WHERE to ensure the update cannot affect other tenants' rows.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE of tenant-scoped table `scheduled_posts` uses `WHERE id = ${row.id}` but omits `AND tenant_id = ${row.tenant_id}`. Relying on id alone skips the application-level tenant constraint required by this codebase invariant.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}`) to ensure the update cannot affect rows from other tenants. Always bind tenant filters on updates to tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:192** — UPDATE of tenant-scoped table `scheduled_posts` in the error-handling path updates rows using `WHERE id = ${row.id} AND status = 'publishing'` but omits `tenant_id`, allowing cross-tenant updates in exceptional cases.
  - _Fix:_ Add `AND tenant_id = ${row.tenant_id}` to the WHERE clause (or otherwise assert tenant context) before performing the update. Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id checks, including in error-paths.
- **server/lib/scheduled-post-runner.ts:266** — Building a Postgres array literal via string interpolation from caller-supplied `params.platforms` (arrLit = `{...}`) and injecting it into SQL (`${arrLit}::text[]`). This is manual string interpolation of request-derived data into SQL (injection / isolation risk).
  - _Fix:_ Use parameterized queries or Drizzle's array support rather than constructing a literal string. For example, pass the JS array as a parameter mapped to a Postgres array (properly parameterized) or use helper functions to safely construct an array parameter. Validate/whitelist elements before using them.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` by `key_prefix` without constraining by tenant_id. The query locates a key row without an explicit tenant filter (tenant is inferred from the row), which violates the rule that reads against tenant-scoped tables must apply the caller's tenant constraint.
  - _Fix:_ If this lookup is intended to resolve tenant by key, document it clearly and consider additional validation (e.g. ensure callers cannot supply conflicting tenant context). Otherwise, if the caller should supply tenantId, add `AND tenant_id = ${expectedTenantId}`. Prefer explicit tenant scoping on reads where appropriate.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table `tenant_voice_profiles` uses only eq(tenant_voice_profiles.id, existing.id) in the WHERE clause — no explicit tenant_id check. This can allow cross-tenant updates if id is mis-bound.
  - _Fix:_ Include the tenant predicate in the UPDATE WHERE clause (e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))) so every write to a tenant-scoped table is explicitly constrained to the caller's tenant. Alternatively, use the same tenant-scoped lookup as the guard before updating.
- **server/memory-graph.ts:66** — UPDATE against memory_categories omits tenant scoping — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. AND tenant_id = ${tenantId}). Prefer passing tenantId into this code path so the UPDATE is parameterized and cannot affect other tenants.
- **server/memory-graph.ts:126** — refreshCategoryCount updates memory_categories and computes count from memory_entries without tenant scoping. Both the subquery and the UPDATE lack tenant filters.
  - _Fix:_ Add tenant scoping to the subquery and the UPDATE (e.g. SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ...) and WHERE id = ${categoryId} AND tenant_id = ${tenantId}. Change refreshCategoryCount signature to accept tenantId and use it in both places.
- **server/memory-graph.ts:181** — SELECT from memory_links to check for existing link does not constrain by tenant (WHERE ...). memory_links appears tenant-scoped but the query omits tenant_id.
  - _Fix:_ Include tenant scoping in the EXISTS/SELECT check (e.g. AND tenant_id = ${tenantId}) or otherwise ensure links are filtered by the caller's tenant before deciding existence.
- **server/memory-graph.ts:393** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant filter. This can update a row in another tenant if ids collide.
  - _Fix:_ Constrain updates by tenant (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}). Alternatively use a storage helper that enforces tenant ownership before updating.
- **server/memory-intelligence.ts:273** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — no tenant filter applied after classifyMemoryCategory resolves an id.
  - _Fix:_ Include tenant scoping on the UPDATE (AND tenant_id = ${tenantId}) or route this through a storage/update wrapper that enforces the tenant. Ensure the tenantId used here is the same tenant that created the entry.
- **server/memory-intelligence.ts:316** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${entry.id} — second occurrence with no tenant filter (in update flow).
  - _Fix:_ As above: add AND tenant_id = ${tenantId} or use a tenant-aware storage API to prevent cross-tenant writes.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — missing tenant scoping on the UPDATE. The SELECT that produced c included tenant_id, but the UPDATE relies on id only.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) to ensure the write cannot affect another tenant's row. Prefer passing tenantId through and using it in all modifying statements.
- **server/minerva-planner.ts:373** — Probe SELECT (fallback) reads plans WHERE id = ${args.planId} with no tenant filter.
  - _Fix:_ Add tenant scoping to the probe SELECT (e.g. WHERE id = ${args.planId} AND tenant_id = ${tenantId}) or only run this probe in a trusted internal context that guarantees tenant isolation. Prefer making tenant explicit to avoid accidental cross-tenant reads.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1 — no tenant constraint when checking for child revision plan.
  - _Fix:_ Include AND tenant_id = ${row.tenant_id} (or the caller's tenant) to ensure only child plans in the same tenant are considered. Pass tenantId through the flow and use it in this query.
- **server/minerva-planner.ts:467** — Rollback UPDATE resets a plan WHERE id = ${args.planId} AND status = 'revising' but omits tenant scoping.
  - _Fix:_ Constrain the rollback UPDATE with AND tenant_id = ${tenantId} to ensure only the intended tenant's row is modified. All code paths that update plans must include tenant_id in WHERE.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code calls assertProjectInTenant() but the INSERT omits tenant_id, violating the invariant that every INSERT into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (and bind the caller's tenantId). e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...). Ensure tenantId is validated (assertProjectInTenant) before the write.
- **server/mpeg-engine.ts:1487** — INSERT into tenant-scoped table `project_files` (parallel path) does not include tenant_id. Even though assertProjectInTenant is called, the INSERT omits tenant_id and thus can produce tenant-ambiguous/invalid rows or violate schema invariants.
  - _Fix:_ Add tenant_id to the INSERT columns and values (use the validated options.tenantId). Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...). Keep the pre-insert ownership check (assertProjectInTenant).
- **server/pace-control.ts:85** — Query against tenant-scoped table heartbeat_logs lacks any tenant filter: `FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}` — checkPace does not accept/require a tenantId and will compute global counts across tenants, breaking tenant isolation and allowing one tenant to affect another's pace limits.
  - _Fix:_ Require a tenantId (or otherwise resolve tenant context) in checkPace and add `AND tenant_id = ${tenantId}` to the WHERE clause. If multi-tenant-global counts are intentionally needed, clearly document and isolate the feature; otherwise enforce per-tenant scoping for all reads on heartbeat_logs.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs without tenant scoping: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > NOW() - (...)` — returns cross-tenant aggregates and exposes tenant-blind usage data.
  - _Fix:_ Accept/derive a tenantId and add `WHERE tenant_id = ${tenantId}` (or otherwise enforce tenant scoping). If an operator-level global snapshot is needed, make that explicit and gated by admin privileges; do not use the same function for per-tenant views.
- **server/process-governor.ts:121** — Subqueries in the heartbeat task failure check SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ... do not include tenant_id. heartbeat_logs is a tenant-scoped table, so these counts can cross tenant boundaries.
  - _Fix:_ Include tenant scoping in these subqueries (e.g. WHERE task_id = ht.id AND tenant_id = ${tenantId} AND ...). Ensure every SELECT against heartbeat_logs is filtered by tenant_id.
- **server/process-governor.ts:199** — response_time check SELECT AVG(duration_ms) FROM heartbeat_logs ... lacks tenant_id filter and will compute averages across all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so aggregated metrics are per-tenant only.
- **server/process-governor.ts:240** — cascading_failures query SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE status != 'success' ... has NO tenant_id constraint and can count failures from other tenants.
  - _Fix:_ Filter heartbeat_logs by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND status != 'success' ...) to avoid cross-tenant contamination.
- **server/process-governor.ts:595** — In executeAction('disable_task') the code updates heartbeat_tasks via UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} without scoping by tenant_id. Although t was selected from tenant-scoped heartbeat_tasks, the UPDATE should still include tenant filtering.
  - _Fix:_ Include tenant_id in the UPDATE (WHERE id = ${t.id} AND tenant_id = ${tenantId}) to enforce isolation and prevent accidental cross-tenant updates if ids are misused.
- **server/process-governor.ts:641** — In executeAction('restart_agent') the code updates agent_desks WHERE id = ${desk.id} without tenant_id. Desk rows were selected using tenant filter, but the UPDATE lacks tenant scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause (UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId}) to ensure only the tenant's rows are changed.
- **server/process-governor.ts:976** — logAction() updates governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} without tenant_id; that UPDATE can mutate another tenant's rule.
  - _Fix:_ Restrict the UPDATE to the tenant (WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or pass tenantId into logAction and use it in the WHERE clause to guarantee tenant-scoped mutation.
- **server/project-brain.ts:135** — updateProjectBrain(): SELECT name, status, description FROM projects WHERE id = ${projectId} reads a tenant-scoped table without tenant_id filtering. projectId comes from caller input and ownership is not verified.
  - _Fix:_ Require and enforce tenantId in this API and add AND tenant_id = ${tenantId} to the SELECT. Alternatively, first verify the caller's tenant owns the project (SELECT tenant_id) and use that tenantId in subsequent queries.
- **server/project-brain.ts:332** — backfillProjectBrains(): SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... WHERE (SELECT COUNT(*) FROM messages ...) > 0 queries projects/conversations/messages with NO tenant scoping, touching multiple tenants' data.
  - _Fix:_ Run backfill per-tenant or add tenant_id filters to each involved table (projects, conversations, messages). Ensure the operation iterates tenants and scopes all reads/writes by tenant_id.
- **server/project-brain.ts:350** — backfillProjectBrains(): SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} has NO tenant filter and will iterate conversations across tenants.
  - _Fix:_ Filter conversations by tenant (e.g. AND tenant_id = ${tenantId}) or perform the backfill per-tenant and pass tenantId into this SELECT.
- **server/project-brain.ts:359** — backfillProjectBrains(): SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping; conversation_id is derived from a previous query that lacked tenant constraint, enabling cross-tenant reads.
  - _Fix:_ Include tenant_id in the messages query (AND tenant_id = ${tenantId}) or ensure conversation ownership is validated before reading messages.
- **server/proactive-engine.ts:138** — rateProactiveAction(): SELECT persona_id FROM proactive_actions WHERE id = ${actionId} reads a tenant-scoped table by id only (no tenant_id). Although the prior UPDATE included tenant_id, this separate SELECT can leak or return rows from other tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the SELECT (or include tenantId when reading) so persona_id lookup is scoped to the tenant. Always verify ownership before using foreign ids obtained from request-derived identifiers.
- **server/recurring-messages.ts:252** — SELECT from tenant-scoped table `agent_knowledge` runs without any tenant_id constraint (SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). This reads rows from all tenants in one unscoped query.
  - _Fix:_ Restrict the query to a single tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant explicitly. If a global scan is required for a system runner, make that explicit and ensure the code applies per-row tenant checks before performing any tenant-scoped actions; prefer selecting by tenant_id or using a dedicated system-safe API that enumerates tenant ids first.
- **server/research-engine.ts:309** — UPDATE research_programs SET model = ... WHERE id = ${programId} has no tenant_id constraint — updates a tenant-scoped program without verifying tenant ownership.
  - _Fix:_ Include tenant constraint in the UPDATE: `UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} AND tenant_id = ${tenantId}` (parameterized). Alternatively verify ownership before updating and include tenant_id in the WHERE clause for defense-in-depth.
- **server/research-engine.ts:449** — SELECT FROM research_experiments uses session_id only (`WHERE session_id = ${sessionId}`) with no tenant_id constraint. This reads tenant-scoped rows without scoping to the session's tenant.
  - _Fix:_ Add tenant scoping to the query, e.g. `WHERE session_id = ${sessionId} AND tenant_id = ${session.tenantId}` or join to research_sessions and filter by that session's tenant_id. Always constrain reads on tenant-scoped tables to the caller's tenant.
- **server/research-engine.ts:458** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} lacks a tenant_id constraint — reading a tenant-scoped program without verifying ownership.
  - _Fix:_ Include tenant check in the SELECT: `WHERE id = ${session.programId} AND tenant_id = ${session.tenantId}` or otherwise verify the program belongs to the session. Do not read tenant-scoped rows by id alone.
- **server/research-engine.ts:463** — SELECT summary FROM research_sessions WHERE id = ${sessionId} has no tenant_id constraint — reading session info without tenant scoping.
  - _Fix:_ Constrain by tenant: `SELECT summary FROM research_sessions WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}` (parameterized). Always include tenant_id when reading tenant-scoped tables.
- **server/research-engine.ts:468** — INSERT INTO project_notes does not set tenant_id. tenant-scoped tables must receive tenant_id explicitly on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT values (and columns), e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ...)`. If project_notes is scoped via project_id, still persist tenant_id explicitly (fail-closed if unknown).
- **server/research-engine.ts:545** — UPDATE research_sessions SET ... WHERE id = ${sessionId} has no tenant_id constraint — this updates a tenant-scoped session without verifying tenant ownership (endSession path).
  - _Fix:_ Add tenant scoping to the UPDATE: `WHERE id = ${sessionId} AND tenant_id = ${session.tenantId}`. For extra safety, verify the session belongs to the tenant before writing.
- **server/research-engine.ts:810** — UPDATE research_experiments SET ... WHERE id = ${experimentId} (and other experiment updates) lack tenant_id constraints — writing to a tenant-scoped table by id only risks cross-tenant modification.
  - _Fix:_ Add tenant scoping on UPDATEs, e.g. `WHERE id = ${experimentId} AND tenant_id = ${session.tenantId}` or join via session_id to ensure tenant ownership. Ensure all UPDATE/DELETE statements on research_experiments include tenant_id.
- **server/research-engine.ts:977** — SELECT name FROM research_programs WHERE id = ${session.programId} in injectKeepedFinding has no tenant check — reading a referenced program without verifying it belongs to the session's tenant.
  - _Fix:_ Include `AND tenant_id = ${session.tenantId}` or otherwise verify program ownership before using its data. Any read of a tenant-scoped table using a foreign id must validate tenant ownership first.
- **server/research-engine.ts:1583** — UPDATE code_proposals SET ... WHERE id = ${proposalId} (safeApplyProposal path) updates a tenant-scoped table without including tenant_id in the WHERE clause — even though proposal was previously fetched, writes should still be constrained to the caller's tenant.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause: `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`. Use defense-in-depth: always include tenant_id on writes to tenant-scoped tables.
- **server/research-engine.ts:1468** — UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} (replayHighValueFindings) omits tenant_id — claiming work without scoping can modify another tenant's rows.
  - _Fix:_ Restrict the conditional claim to the tenant: `UPDATE research_experiments SET replayed_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId} AND replayed_at IS NULL RETURNING id`. Always include tenant_id when updating tenant-scoped tables.
- **server/routes.ts:2212** — POST /api/presenter inserts a presenter_sessions row using tenantId supplied in the request body (VALUES (${tenantId}, ...)) without validating that the caller is authorized to act for that tenant. Allowing an unvalidated tenantId from the caller can create sessions attributed to arbitrary tenants.
  - _Fix:_ Do not trust a caller-supplied tenantId. Derive tenantId from an authenticated session or from a validated internal mapping. If this endpoint must accept a tenantId (internal clients), validate the caller's authority for that tenant (e.g. internal key -> allowed tenant list) before inserting, or restrict the API to run only with a privileged internal service account and document/validate that mapping.
- **server/routes.ts:4465** — SELECT from tenant-scoped table 'conversations' lacks a tenant_id constraint. The code queries `SELECT project_id FROM conversations WHERE id = ${conversationId} ...` without verifying the conversation belongs to the caller's tenant.
  - _Fix:_ Restrict the SELECT to the caller's tenant (or validate ownership first). For example: `SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} AND project_id IS NOT NULL`. Alternatively, load the conversation via a tenant-scoped storage call or use conv.tenantId and deny access if it doesn't match the caller's tenant.
- **server/routes/admin.ts:587** — Query on ai_insights (SELECT ... FROM ai_insights WHERE status = 'new' ...) has no tenant_id constraint — will surface insights from all tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise constrain by tenant) so the query only returns rows owned by the caller's tenant. If platform-wide visibility is intentionally required, ensure this is explicit and restricted to a properly verified platform-admin path.
- **server/routes/admin.ts:598** — Query on heartbeat_tasks (SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3) has no tenant_id constraint — returns tasks across all tenants.
  - _Fix:_ Constrain the query with WHERE tenant_id = ${tenantId} (or equivalent) if heartbeat_tasks is tenant-scoped. If platform-global visibility is intended, make sure the endpoint is strictly limited to a verified platform admin and document the rationale; otherwise limit results to a single tenant.
- **server/routes/admin.ts:609** — Query on ai_insights (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' ...) lacks tenant_id filtering — can expose other tenants' insights.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or otherwise constrain by tenant) so only rows owned by the caller's tenant are returned. If cross-tenant visibility is deliberate for platform admins, make that explicit and ensure only verified platform-admins may call this route.
- **server/routes/conversations.ts:98** — UPDATE on conversations omits tenant scoping — SQL: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`. This updates a tenant-scoped table by id only, allowing modifying rows belonging to other tenants if an attacker knows an id.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `WHERE id = ${conv.id} AND tenant_id = ${tenantId}`) or bind/use conv.tenantId to ensure the update only affects rows owned by the caller's tenant. Prefer using storage-layer helpers that enforce tenant ownership.
- **server/routes/conversations.ts:102** — INSERT into project_conversations does not set tenant_id — SQL: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})`. Tenant-scoped tables must be written with tenant_id provided (columns are NOT NULL, no default).
  - _Fix:_ Include tenant_id in the INSERT values (e.g. `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conv.id}, ${tenantId})`) or call a storage-layer function that supplies and verifies tenant_id.
- **server/routes/conversations.ts:227** — INSERT into project_notes omits tenant_id — SQL: `INSERT INTO project_notes (project_id, note, author) VALUES (${projId}, ${note.slice(0, 8000)}, ${'system:archive'})`. Inserting into a tenant-scoped table without tenant_id can create rows that aren't owned by the correct tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${projId}, ..., ${'system:archive'}, ${tenantId})`) or use a storage-layer API which enforces and sets tenant_id.
- **server/routes/heartbeat.ts:102** — UPDATE on heartbeat_tasks omits tenant scoping in approve flow — SQL: `UPDATE heartbeat_tasks SET approval_status = 'approved', enabled = true, next_run_at = ${nextRun} WHERE id = ${taskId}`. An admin/authenticated caller could approve tasks from other tenants by id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. `WHERE id = ${taskId} AND tenant_id = ${task.tenantId ?? tenantId}`) or use a storage function that enforces tenant ownership before mutating rows.
- **server/routes/heartbeat.ts:116** — UPDATE on heartbeat_tasks omits tenant scoping in reject flow — SQL: `UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId}`. Missing tenant filter allows rejecting/affecting tasks across tenants.
  - _Fix:_ Add `AND tenant_id = ${task.tenantId ?? tenantId}` to the WHERE clause or delegate to a storage-layer method that enforces tenant ownership prior to updates.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages sets is_read by id only — SQL: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`. The SELECT that preceded it was tenant-scoped, but the UPDATE itself lacks `tenant_id`, allowing modification of other tenants' messages by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) or use storage-layer APIs that enforce tenant ownership for the mutation.
- **server/seed.ts:34** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id filter. research_sessions is tenant-scoped (tenant_id exists on INSERT), so this read returns/considers data across all tenants.
  - _Fix:_ Add an application-level tenant filter to the query (e.g. WHERE tenant_id = ${tenantId} or use Drizzle .where(eq(research_sessions.tenantId, tenantId))). If the intent is a global count for admin scripts, make that explicit and run it from an admin-only codepath, not from tenant-scoped logic.
- **server/seed.ts:2854** — SELECT id, name, model FROM research_programs WHERE is_active = true runs without a tenant_id constraint. research_programs is tenant-scoped (seed/insert statements include tenant_id), so this will iterate all tenants' programs.
  - _Fix:_ Constrain the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise ensure it's executed only in an explicit admin context. Prefer using Drizzle .where(eq(researchPrograms.tenantId, tenantId)).
- **server/seed.ts:3204** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... runs with no tenant filter and then the script inserts matching rows into project_conversations. conversations appears tenant-scoped (other code inserts tenant_id into conversation-related tables), so this reads across all tenants.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...). Ensure subsequent inserts respect the same tenant and include tenant_id on project_conversations inserts if that table is tenant-scoped.
- **server/seed.ts:3204** — SELECT from tenant-scoped table 'conversations' without any tenant_id constraint. The query selects conversations with a project_id and checks project_conversations but does not restrict to a tenant, allowing cross-tenant reads.
  - _Fix:_ Add a tenant filter to the WHERE clause (e.g. AND c.tenant_id = <tenantId>) or join to projects/project_conversations with an explicit tenant constraint. Prefer using parameterized Drizzle filters (.where(eq(conversations.tenantId, tenantId))) rather than raw interpolation.
- **server/seed.ts:4492** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' reads a tenant-scoped table without a tenant_id constraint, returning a global count rather than a tenant-scoped one.
  - _Fix:_ Constrain reads to the current tenant (e.g. WHERE enabled = true AND type = 'delegation' AND tenant_id = <tenantId>) or perform the check per-tenant. Use parameterized values or Drizzle .where filters.
- **server/seed.ts:4521** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' counts heartbeat_tasks without a tenant_id constraint and will return cross-tenant results.
  - _Fix:_ Scope the SELECT to the running tenant (e.g. add AND tenant_id = <tenantId>) or iterate per-tenant. Use Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) to avoid accidental cross-tenant counting.
- **server/seed.ts:4535** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) reads heartbeat_tasks without tenant restriction and can return global counts across tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = <tenantId>) or run the query per-tenant. Prefer Drizzle parameterized filters rather than raw SQL without tenant context.
- **server/self-improvement.ts:170** — detectStagnation builds a WHERE clause from conditions but allows calling without tenantId — the query at lines 170-173 can run against the experiments table with NO tenant filter when tenantId is omitted.
  - _Fix:_ Require caller tenantId (fail closed) or always include tenant constraint. Example: if tenantId is undefined throw an error, or ensure .where(and(eq(experiments.category, category), eq(experiments.tenantId, tenantId))). Do not issue unscoped selects against the tenant-scoped experiments table.
- **server/self-improvement.ts:511** — getExperimentHistory will return db.select().from(experiments) with no tenant constraint when tenantId is not provided — this returns cross-tenant experiment rows.
  - _Fix:_ Require tenantId for this API (fail closed) or ensure every code path adds eq(experiments.tenantId, tenantId) to the WHERE. If supporting an admin/global view, require explicit admin authorization and a deliberate parameter to opt into unscoped reads.
- **server/skill-seeker.ts:743** — listGaps has code paths that SELECT from capability_gaps without a tenant_id filter (e.g. when only status is provided, or when neither status nor tenantId are provided) — this allows cross-tenant reads of a tenant-scoped table.
  - _Fix:_ Disallow unscoped reads: require tenantId and include AND tenant_id = ${tenantId} in the WHERE for non-admin callers. If global listing is needed, restrict it to explicitly-authorized admin code paths and explicitly document/authorize that behavior; otherwise fail closed and return an error or an empty list.
- **server/skillify.ts:196** — SELECT from `conversations` has no tenant_id constraint in the WHERE clause (reads a tenant-scoped table without scoping).
  - _Fix:_ Include an explicit tenant filter in the query (e.g. .where(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId)) or use tenantScope/assertTenantContext) so the row is selected only if it belongs to the caller's tenant.
- **server/skillify.ts:203** — SELECT from `messages` has no tenant_id constraint (reads tenant-scoped table using only conversationId).
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)) or validate conversation ownership before each message read and include tenant filtering to enforce isolation.)
- **server/skillify.ts:91** — SELECT from `messages` (recentMsgs) has no tenant_id constraint — reading messages for a conversation without scoping to tenant.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))). If you already validated conversation ownership earlier, still include the tenant_id equality in this SELECT to satisfy the single-point filtering invariant.
- **server/skillify.ts:331** — SELECT from `messages` (recentMsgs in skillifyConversation) lacks a tenant_id constraint.
  - _Fix:_ Scope this messages SELECT to the tenant (e.g. .where(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or otherwise require/derive tenantId and use tenantScope/requireTenantScope to produce an eq predicate.
- **server/skillify.ts:223** — SELECT from `personas` uses personaId from the conversation but does not include a tenant_id constraint or otherwise verify the persona belongs to the caller's tenant (foreign-id read without ownership validation).
  - _Fix:_ Verify the persona belongs to the same tenant before reading it (e.g. .where(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)) or call a helper like assertConversationInTenant/assertProjectInTenant equivalent for personas) so foreign IDs from conversation rows cannot be used to read other tenants' rows.
- **server/storage.ts:284** — Raw SELECT from tenant-scoped messages table without tenant filter (attachCitationMetadata). The query `SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL` has no tenant_id constraint, so it can return citations across tenants.
  - _Fix:_ Pass the caller's tenantId into attachCitationMetadata (or derive it safely) and add an explicit tenant filter, e.g. `... WHERE id IN (...) AND tenant_id = ${tenantId} AND citations IS NOT NULL`. Use parameterized Drizzle expressions rather than concatenated raw SQL where possible.
- **server/storage.ts:385** — UPDATE to tenant-scoped messages table without tenant filter (attachCitations). `UPDATE messages SET citations = ... WHERE id = ${messageId}` lacks tenant scoping and can modify other tenants' rows.
  - _Fix:_ Require or pass the message's tenantId to attachCitations and include it in the WHERE clause (or validate ownership before update), e.g. `WHERE id = ${messageId} AND tenant_id = ${tenantId}`. Avoid blind updates by id alone for tenant-scoped tables.
- **server/storage.ts:531** — UPDATE memory_entries without tenant constraint (touchMemoryEntries). The update `WHERE id IN (ids)` touches memory entries across tenants because no tenant_id is enforced.
  - _Fix:_ Require a tenantId parameter (fail-closed) or otherwise validate ownership for the ids, and include `eq(memoryEntries.tenantId, tenantId)` in the WHERE clause, or group ids by tenant and update per-tenant to avoid cross-tenant writes.
- **server/storage.ts:730** — UPDATE memory_entries.embedding without tenant constraint (updateMemoryEmbedding). `UPDATE memory_entries SET embedding ... WHERE id = ${id}` lacks tenant scoping and can modify rows from other tenants.
  - _Fix:_ Require/accept tenantId and include a tenant_id check in the WHERE clause (or otherwise validate row ownership before updating). Prefer to update using both id and tenant_id.
- **server/storage.ts:742** — UPDATE agent_knowledge.embedding without tenant constraint (updateKnowledgeEmbedding). `UPDATE agent_knowledge SET embedding ... WHERE id = ${id}` has no tenant filter.
  - _Fix:_ Require tenantId (or validate ownership) and include `AND tenant_id = ${tenantId}` in the WHERE clause when updating tenant-scoped agent_knowledge rows.
- **server/storage.ts:766** — Mass UPDATE on memory_entries (archiveExpiredMemories) with no tenant filter. The statement `UPDATE memory_entries SET status = 'archived' WHERE ...` affects all tenants' rows.
  - _Fix:_ If archiving should be per-tenant, add a tenantId filter (or run per-tenant job). If intentionally global, make this explicit and ensure only an admin context can call it. Prefer an argument to scope the operation and fail-closed if omitted.
- **server/storage.ts:782** — Mass UPDATE on memory_entries (archiveStaleMemories) with no tenant filter. The statement archives rows across all tenants without scoping.
  - _Fix:_ Require a tenantId or run per-tenant; include `AND tenant_id = ${tenantId}` in the WHERE or otherwise ensure the caller is authorized to perform a global archival.
- **server/storage.ts:470** — deletePersona performs updates/deletes against tenant-scoped tables without tenant scoping. For example `tx.update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id))` and `tx.update(memory_entries).set({ status: 'superseded' }).where(eq(memory_entries.personaId, id))` have no tenant_id constraint and will affect rows across all tenants.
  - _Fix:_ If persona deletions should be tenant-scoped, require a tenantId and include `eq(...tenantId, tenantId)` in those WHERE clauses. If personas are global and intended to be removed platform-wide, document and gate this as an admin-only operation; otherwise update/delete per-tenant.
- **server/surprise-scorer.ts:93** — UPDATE felix_proposals sets args_embedding using `WHERE id = ${proposalId} AND args_embedding IS NULL` but omits tenant scoping (stampArgsEmbedding). This can modify proposals for other tenants with the same id if ids are not globally unique per-tenant.
  - _Fix:_ Require and use tenantId in stampArgsEmbedding (or re-query the row with tenant filter) and include `AND tenant_id = ${tenantId}` in the WHERE clause. Fail-closed when tenantId is not supplied.
- **server/surprise-scorer.ts:139** — UPDATE felix_proposals SET actual_outcome_embedding ... WHERE id = ${proposalId} lacks tenant constraint (scoreProposalSurprise). This writes to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId}`) or require tenantId validation before performing the update.
- **server/surprise-scorer.ts:181** — UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} has no tenant filter; the function previously queried by tenant but later updates omit tenant scoping.
  - _Fix:_ Include tenant_id in the WHERE clause of all subsequent updates (e.g. `AND tenant_id = ${tenantId}`) to maintain defense-in-depth even after an initial tenant-scoped SELECT.
- **server/surprise-scorer.ts:212** — UPDATE felix_proposals SET surprise_score = ..., surprise_band = ... WHERE id = ${proposalId} is performed without tenant scoping. Writing scoring/band fields must be constrained to the proposal's tenant.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause for the UPDATE and/or require tenantId be asserted at the top of the function. Ensure all writes to felix_proposals include tenant checks.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` uses only name equality and does NOT constrain by tenant_id. Application invariant requires all reads of tenant-scoped tables include a tenant_id filter.
  - _Fix:_ Restrict the lookup to the caller's tenant. Add tenantId to the WHERE (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))) or otherwise validate the row belongs to the provided tenant before proceeding.
- **server/tool-learning.ts:155** — SELECT by id from tenant-scoped table `custom_tools` uses only id and does NOT constrain by tenant_id. This allows reading a tool row from any tenant by id.
  - _Fix:_ Include tenant-scoped filtering on reads. Use .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))) or pass/validate the tenantId to ensure the returned row belongs to the caller's tenant.
- **server/tool-learning.ts:350** — UPDATE on tenant-scoped table `custom_tools` increments usageCount by id only and does NOT include tenant_id in the WHERE clause, allowing cross-tenant modifications by id.
  - _Fix:_ Include tenant_id in the update WHERE clause (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or otherwise assert the tool belongs to the caller's tenant before changing usageCount.
- **server/tools.ts:9225** — Handler uses a caller-supplied foreign id (params._conversationId) to read the conversations table without first verifying the conversation belongs to the caller's tenant. The query reads FROM conversations c (SELECT c.project_id ...) with no c.tenant_id = ${tenantId} guard or assertConversationInTenant call, enabling an IDOR / cross-tenant reference risk.
  - _Fix:_ Validate ownership of the referenced conversation before using it. Either (a) call the existing assertConversationInTenant(params.conversationId, tenantId) helper (as used elsewhere) and abort if it returns false, or (b) include an explicit tenant filter on the conversation row (e.g. add AND c.tenant_id = ${tenantId} to the WHERE clause). Ensure this check occurs atomically prior to any downstream writes (project_files insert) to avoid race/window-of-opportunity issues.
- **server/tools.ts:10783** — SELECT from tenant-scoped table memory_entries without tenant filter. The query SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' omits any tenant_id constraint, allowing cross-tenant data exposure.
  - _Fix:_ Constrain reads to the caller's tenant (e.g. add AND tenant_id = ${params._tenantId} or equivalent Drizzle .where(eq(memoryEntries.tenantId, tenantId))). If this data is intended per-tenant, require router-injected _tenantId and include it in the WHERE clause (fail-closed if missing).
- **server/tools.ts:11106** — INSERT into project_notes omits tenant context. INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) does not include tenant_id; tenant-scoped tables must receive tenant_id on write.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${tenantId}, ...)), and ensure tenantId is the authenticated caller's tenant. Alternatively, use a helper that stamps tenant_id server-side after asserting ownership of the referenced project.
- **server/tools.ts:11111** — INSERT into project_files omits tenant context. INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) does not set tenant_id, risking cross-tenant writes or errors (tenant_id columns are NOT NULL).
  - _Fix:_ Include tenant_id when inserting (INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${tenantId}, ...)). Verify the project belongs to the tenant before inserting, or use a centralized API that enforces tenant stamping and ownership checks.
- **server/tools.ts:11331** — SELECT from presenter_sessions without tenant constraint. SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} ... reads session tokens by presentation_id but does not verify tenant ownership or include tenant_id filter — possible token exposure across tenants.
  - _Fix:_ Verify the presentation/presenter session belongs to the caller's tenant before reading (e.g. assertPresentationInTenant(presentationId, tenantId)), and/or add AND tenant_id = ${params._tenantId} to the WHERE clause. Do not rely solely on ambient assumptions about presentationId uniqueness across tenants.
- **server/tools.ts:17013** — UPDATE against tenant-scoped table 'browser_workflows' lacks a tenant filter. The query `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` does not constrain by tenant_id.
  - _Fix:_ Include the tenant constraint in the WHERE clause. e.g. wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`) or use Drizzle .where(eq(browserWorkflows.id, workflow.id), eq(browserWorkflows.tenantId, tenantId)).
- **server/tools.ts:18992** — UPDATE against tenant-scoped table 'memory_entries' uses only id in WHERE. The statement `UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id}` does not verify tenant_id (or persona_id).
  - _Fix:_ Restrict updates to the caller's tenant (and persona if appropriate). Add tenant_id and persona_id to the WHERE clause (or use Drizzle .where with eq(memoryEntries.tenantId, tid) and eq(memoryEntries.personaId, personaId)) to ensure tenant isolation.
- **server/tools.ts:19165** — UPDATE against tenant-scoped table 'memory_entries' uses id = ANY(${ids}) without tenant constraint: `UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE id = ANY(${ids})`. This can update rows across tenants if ids are abused.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. `WHERE id = ANY(${ids}) AND tenant_id = ${tid}` (or use a join/Drizzle .where(eq(memoryEntries.tenantId, tid)) plus id filter) so only this tenant's rows are modified.
- **server/tools.ts:19773** — UPDATE against tenant-scoped table 'memory_entries' lacks tenant/persona guard. The query `UPDATE memory_entries SET fact = ${fact}, ... WHERE id = ${id}` updates by id only.
  - _Fix:_ Add tenant_id and persona_id checks to the WHERE clause (or use Drizzle .where with eq(memoryEntries.tenantId, tid) and eq(memoryEntries.personaId, personaId)) to enforce tenant isolation when updating memory_entries.
- **server/tools.ts:19628** — Drizzle update on tenant-scoped table 'proposed_skills' omits tenant constraint. The call `db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id))` identifies row by id only and does not ensure the row belongs to the caller's tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause: add eq(proposedSkills.tenantId, tid) (or AND tenant_id = ${tid} in raw SQL) — e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)).
- **server/tools.ts:19654** — Drizzle update on tenant-scoped table 'proposed_skills' (marking accepted/promoted) omits tenant constraint. `db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id))` does not restrict by tenant_id.
  - _Fix:_ Constrain the update to the tenant: include eq(proposedSkills.tenantId, tid) in the WHERE clause (or use tenant_id = ${tid}) to prevent cross-tenant modification.
- **server/tools.ts:19670** — Drizzle update on tenant-scoped table 'proposed_skills' (marking rejected) omits tenant constraint: `db.update(proposedSkills).set(...).where(eq(proposedSkills.id, row.id))`.
  - _Fix:_ Add tenant scoping to the WHERE: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or equivalent SQL with AND tenant_id = ${tid}, ensuring only the caller's tenant row is modified.
- **server/video-job-runner.ts:178** — mirrorStateToDb performs an upsert (db.insert(videoJobs).onConflictDoUpdate(...)) without constraining the update to the tenant. The onConflict update's `where` only checks updatedAt, not tenant_id, so a conflicting jobId row could be updated irrespective of tenant ownership in some race scenarios.
  - _Fix:_ Ensure the upsert/update is tenant-scoped. For example, include a condition that the existing row's tenant_id matches the incoming row. Use a WHERE clause such as AND eq(videoJobs.tenantId, ${row.tenantId}) (or equivalent combined predicate) in onConflictDoUpdate to avoid cross-tenant updates, or pre-validate job ownership before applying the update.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs issues a global UPDATE on videoJobs to mark stale jobs failed, with no tenant_id filter. This mutates rows across all tenants in one operation and violates the requirement that tenant-scoped table writes be constrained to a tenant.
  - _Fix:_ Run recovery per-tenant or otherwise include tenant scoping. If the operation intentionally spans tenants (maintenance), ensure it is executed in an admin/maintenance context with explicit awareness and logging; otherwise add tenant constraints to the UPDATE or iterate tenants and apply per-tenant updates so tenant_id is included in the WHERE clause.
- **server/watchlist.ts:111** — Query joins watchlist_items (tenant-scoped) but does not constrain the joined `wi` rows to the caller's tenant. The SQL builds: SELECT wa.*, wi.name as watchlist_name, wi.category FROM watchlist_alerts wa LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id WHERE wa.tenant_id = ${tenantId}. The LEFT JOIN does not include wi.tenant_id = ${tenantId}, so persona/watchlist item fields from other tenants could be returned if alert rows reference ids from other tenants.
  - _Fix:_ Ensure every access to tenant-scoped tables includes an explicit tenant_id constraint. Either add the tenant predicate to the JOIN (e.g. LEFT JOIN watchlist_items wi ON wi.id = wa.watchlist_item_id AND wi.tenant_id = ${tenantId}), or fetch the watchlist item name via a separate tenant-scoped lookup (storage API) or persist the needed display fields on the alert row at write time so reads do not need to join an unscoped tenant table.
- **server/webhook-triggers.ts:60** — listTriggers SELECT performs LEFT JOIN personas p ON p.id = w.persona_id but does not constrain personas by tenant_id. personas is a tenant-scoped table; reading it without a tenant filter can leak another tenant's persona data.
  - _Fix:_ Do not read tenant-scoped personas without a tenant constraint. Options: (1) include persona tenant check in the JOIN (e.g. LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = <expectedTenant>), (2) avoid joining directly and instead resolve persona name via tenant-scoped storage.getPersona(personaId, tenantId) when rendering admin views, or (3) store persona_name on webhook_triggers at creation time so list queries don't need to join the personas table.
- **server/webhook-triggers.ts:122** — processTriggerEvent SELECT queries webhook_triggers LEFT JOIN personas p ON p.id = w.persona_id WHERE w.webhook_key = ${webhookKey} AND w.enabled = true. The JOIN reads the tenant-scoped personas table without constraining p.tenant_id, which can expose persona details across tenants.
  - _Fix:_ When resolving persona information for global webhook triggers, do not directly JOIN the tenant-scoped personas table without tenant scoping. Either include an explicit tenant_id condition in the JOIN (if you can determine the correct tenant), look up the persona via the tenant-aware storage API after selecting the trigger, or store the persona name on the trigger row at insert time so reads don't require an unscoped join.
- **server/wiring-invariants.ts:244** — SELECT against tenant-scoped table `tool_performance` executed without any tenant_id constraint.
  - _Fix:_ Don't run unfiltered queries against tenant-scoped tables. Either add an explicit tenant filter (e.g. WHERE tenant_id = :tenantId) or, for admin/aggregate checks, iterate all tenants and run the query per-tenant (or explicitly use a known system tenant id like ADMIN_TENANT_ID only when intended). Avoid global SELECTs that cross tenants.
- **server/wiring-invariants.ts:258** — SELECT count(*) FROM tenant-scoped table `tool_performance` executed without tenant_id constraint (ever-count).
  - _Fix:_ Restrict the query to a tenant (WHERE tenant_id = :tenantId) or run it per-tenant. If you intend a global (cross-tenant) metric, make that explicit and ensure it's safe and approved; otherwise do not read tenant data across tenants.
- **server/wiring-invariants.ts:260** — SELECT against tenant-scoped table `messages` executed without any tenant_id constraint.
  - _Fix:_ Add a WHERE tenant_id = :tenantId (or iterate tenants) when querying messages. Cross-tenant message counts leak/aggregate tenant data and violate per-tenant isolation enforced by application-level filters.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tenant-scoped table `tool_performance` executed without tenant_id constraint.
  - _Fix:_ Constrain tool_performance queries by tenant_id or run them per-tenant. If you need cross-tenant aggregates, make that explicit and ensure appropriate authorization and sanitization before reading tenant-scoped tables.
- **server/wiring-invariants.ts:366** — Dynamic SELECT COUNT/MAX FROM subsystem table (${spec.table}) is executed without any tenant_id filter — reads tenant-scoped tables (e.g. knowledge_nudges, tool_performance) across all tenants.
  - _Fix:_ When probing subsystem tables, include tenant_id in the WHERE clause or run the probe per-tenant. Avoid building table queries that implicitly aggregate across tenants; if the intent is to check schema existence only, separate that from tenant-scoped data checks.
- **server/wiring-invariants.ts:417** — SELECT aggregation from tenant-scoped table `tool_performance` (tool-drift check) executed without tenant_id constraint.
  - _Fix:_ Scope tool_drift analysis to a tenant by adding WHERE tenant_id = :tenantId or run drift checks per-tenant. Do not perform aggregations over tenant data without explicit tenant scoping and authorization.
- **server/wiring-invariants.ts:535** — SELECT COUNT / pg_total_relation_size FROM various memory tables (e.g. agent_knowledge, memory_entries, messages, graph_memory) executed without tenant_id constraint.
  - _Fix:_ Memory-growth checks must either be limited to a tenant (WHERE tenant_id = :tenantId) or explicitly iterate tenants. If certain memory tables are intentionally global, document and centralize that decision; otherwise protect per-tenant data by applying tenant filters.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM tenant-scoped table `graph_memory` executed without tenant_id constraint (the code even notes it reports 'across all tenants').
  - _Fix:_ Do not treat tenant-scoped data as global. Either run the check per-tenant or add an explicit tenant filter. If the intent is a global admin-level check, make that explicit and ensure only privileged code paths can perform it and that it's safe to aggregate.
- **server/workflow-templates.ts:66** — Hard-coded foreign id `project_id: 14` is passed to a tool that may mutate tenant-scoped rows without first verifying the project belongs to the caller's tenant.
  - _Fix:_ Resolve the project by id and verify its tenant_id matches context.tenantId before passing it to mutating tools. Prefer passing only tenant-scoped identifiers validated against context (or use a tenant-local project slug), and ensure every downstream INSERT/UPDATE includes the caller's tenant_id.

### MEDIUM (52)

- **server/agent-channels.ts:198** — Query in getUnreadCount() JOINs channel_subscriptions (cs) but the JOIN condition does not constrain cs.tenant_id — reads channel_subscriptions without bounding tenant.
  - _Fix:_ Constrain the join to the caller's tenant (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${tenantId}) or add WHERE cs.tenant_id = ${tenantId} so the channel_subscriptions read is tenant-scoped.
- **server/agentic-engines.ts:341** — gatherOperationalData() queries heartbeat_logs with no tenant filter (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection') — reading a tenant-scoped log table globally.
  - _Fix:_ Scope heartbeat_logs queries to the requested tenant (WHERE tenant_id = ${tenantId}) or run the sweep per-tenant. Avoid global reads of tenant-scoped heartbeat_logs.
- **server/agentic-engines.ts:449** — runPredictiveEngine() selects recentLogs from heartbeat_logs (WHERE status='success') with no tenant filter — reading tenant-scoped logs globally.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = ${tenantId} AND status='success') or otherwise fetch only logs belonging to the tenant being processed.
- **server/agentic-engines.ts:516** — runOptimizationEngine() queries heartbeat_logs (SELECT ... FROM heartbeat_logs GROUP BY ...) without any tenant_id filter — reading aggregated task data across tenants.
  - _Fix:_ Restrict heartbeat_logs aggregation to the tenant (include WHERE tenant_id = ${tenantId}) or perform per-tenant aggregation to preserve tenant isolation.
- **server/agentic-engines.ts:531** — runOptimizationEngine() selects heartbeat_tasks WHERE enabled = true with no tenant filter — reading tenant-scoped task config globally.
  - _Fix:_ Include tenant scoping (WHERE tenant_id = ${tenantId} AND enabled = true) so only that tenant's heartbeat_tasks are read/used.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge to read the auto_memorize watermark does not include any tenant_id filter. agent_knowledge is written elsewhere with tenant_id = 1 (admin), but the SELECT may return rows for other tenants or surprise on multi-tenant deployments.
  - _Fix:_ Scope the watermark read to the intended tenant (e.g. tenant_id = ADMIN_TENANT_ID) or make the watermark table/query explicitly global. If the watermark is meant to be per-tenant, add AND tenant_id = ${tenantId} to the WHERE clause and pass the tenant context.
- **server/auto-memorize.ts:62** — setWatermark's lookup (SELECT id FROM agent_knowledge WHERE category = ... AND title = ...) lacks a tenant filter; subsequent UPDATE/INSERT operate without ensuring they target the intended tenant row.
  - _Fix:_ Include tenant_id in the SELECT/UPDATE path (e.g. WHERE category = ... AND title = ... AND tenant_id = ${tenantId}) or explicitly use the admin tenant_id if this watermark is intentionally global (document and assert that behavior). Ensure any UPDATE/DELETE targets rows constrained by tenant_id.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} — no tenant_id constraint. Although the code earlier verifies the collection belongs to the tenant, the DELETE itself is unscoped at SQL level and violates the invariant.
  - _Fix:_ Include tenant_id in the DELETE WHERE clause (AND tenant_id = ${tenantId}) or perform the DELETE via a query using the already-verified collection row while ensuring tenant scoping is enforced at SQL level.
- **server/email.ts:394** — backfillEmptyBodies() runs SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id with NO tenant_id filtering — reading across all tenant rows.
  - _Fix:_ If the job is intended to operate per-tenant, iterate tenants and run the SELECT scoped to each tenant_id. If it is intentionally global/admin, make that explicit and ensure appropriate access controls/auditing. Prefer adding WHERE tenant_id = ${tenantId} when running per-tenant.
- **server/email.ts:407** — UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} — update lacks tenant_id constraint. Updating by id only can accidentally modify rows belonging to other tenants if ids are correlated or a bug introduces a foreign id.
  - _Fix:_ Constrain the UPDATE to tenant_id as well (WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update via a tenant-scoped SELECT/UPDATE to ensure the row belongs to the expected tenant before mutating it.
- **server/embeddings.ts:716** — backfillEmbeddingVecs() selects rows from memory_entries without any tenant_id constraint (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200). This iterates and updates rows across tenants in an unscoped manner.
  - _Fix:_ Run backfill per-tenant (iterate tenants and include WHERE tenant_id = ${tenantId}) or if global operation is desired, document and gate it as an admin task. Ensure subsequent UPDATEs also include tenant scoping.
- **server/embeddings.ts:743** — backfillEmbeddingVecs() updates agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} with NO tenant_id constraint. This writes tenant-scoped rows without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or perform the backfill as a per-tenant operation (select+update within the same tenant scope). Ensure storeEmbeddingVec (the centralized updater) also requires tenantId to prevent unscoped writes.
- **server/glasses-gateway.ts:156** — SELECT from 'api_keys' is done without scoping by tenant (where(eq(apiKeys.keyHash, keyHash), eq(apiKeys.isRevoked, false))). If 'api_keys' is tenant-scoped this allows a lookup across tenants.
  - _Fix:_ If api_keys is tenant-scoped, include a tenant filter when possible (eq(apiKeys.tenantId, expectedTenantId)). If the table is intentionally global (lookups by keyHash must cross-tenant), document that explicitly and add code-level comments and access controls to make the exception explicit; otherwise add tenant scoping to the SELECT.
- **server/glasses-gateway.ts:172** — UPDATE on 'api_keys' (db.update(apiKeys).set(...).where(eq(apiKeys.id, key.id))) omits a tenant_id constraint.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) so updates cannot accidentally modify a row from another tenant.
- **server/job-queue.ts:282** — listJobs() builds a SELECT FROM agent_jobs and will return rows across all tenants if tenantId is not supplied (whereClause may be empty). This exposes other tenants' job data to callers that don't scope by tenant.
  - _Fix:_ Require a tenantId argument for non-admin callers or enforce authorization checks; default to scoping queries by the caller's tenant. If administrative views are needed, require explicit admin privileges and audit access.
- **server/job-queue.ts:347** — getJobStats() SELECTs aggregated counts FROM agent_jobs over the last 7 days without tenant_id filtering, returning cross-tenant metrics to any caller that can reach this function.
  - _Fix:_ Scope stats to the caller's tenant (WHERE tenant_id = ${tenantId}), or provide a separate admin-only endpoint for global stats protected by RBAC and auditing.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor() SELECTs tenant_id, persona_id FROM agent_knowledge GROUP BY tenant_id, persona_id and then processes every tenant/persona pair. This reads tenant-scoped rows across all tenants without per-caller tenant validation.
  - _Fix:_ If this is an intentional system-wide diagnostic, ensure the function runs under a controlled system/admin context and is protected accordingly. Otherwise, limit queries to the caller's tenant (add WHERE tenant_id = ${tenantId}) or require admin authorization for global scans.
- **server/lib/bwb-job-progress.ts:141** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId,...)) where scope() may omit the tenant_id predicate if BWB_TENANT_ID is missing. This allows updates that are not constrained by tenant_id.
  - _Fix:_ Ensure the tenant predicate is ALWAYS present. Require/validate a tenantId parameter (or throw) and include eq(videoJobs.tenantId, tenantId) in the WHERE clause unconditionally, or fail closed if BWB_TENANT_ID is not set. Do not rely on an optional env-threaded tenant as the only guard.
- **server/lib/bwb-job-progress.ts:166** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId,...)) where scope() may omit the tenant_id predicate if BWB_TENANT_ID is missing. This can perform cross-tenant updates.
  - _Fix:_ Require and pass an explicit tenantId to update functions, and have scope() include the tenant predicate always (or make the function fail if tenant is unknown). Add explicit tests that ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id.
- **server/lib/bwb-job-progress.ts:200** — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope() may omit tenant_id when BWB_TENANT_ID is unknown, allowing reads from other tenants.
  - _Fix:_ Always include AND tenant_id = <caller tenant> in the WHERE clause. If tenant is unknown, fail closed (return no rows) or require the caller to provide tenantId rather than dropping the predicate.
- **server/lib/bwb-job-progress.ts:211** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId)) which can omit tenant_id when the env-threaded tenant is missing — enabling cross-tenant writes.
  - _Fix:_ Change scope() to require tenantId (or throw) and ensure the UPDATE's WHERE includes eq(videoJobs.tenantId, tenantId). Audit callers to pass tenantId explicitly rather than relying on optional environment threading.
- **server/lib/bwb-job-progress.ts:227** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId,...)) where scope() may not include tenant_id if BWB_TENANT_ID is not provided, permitting unscoped updates.
  - _Fix:_ Fail closed when tenant_id is not available. Ensure scope() always returns a clause that includes tenant predicate or make callers pass tenantId and validate it before running the update.
- **server/lib/bwb-job-progress.ts:245** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId,...)) and may drop tenant scoping when the env tenant is missing, enabling unintended cross-tenant modification of rows (e.g. marking other tenants' jobs failed).
  - _Fix:_ Require tenantId for fail paths as well. Include tenant predicate in the WHERE unconditionally or abort the operation if tenant context cannot be determined. Add logging/tests to detect missing tenant context.
- **server/lib/mcp-api-keys.ts:69** — Building a Postgres text[] literal via string interpolation from caller-supplied `scopes` (scopesLit = `{...}`) and passing it into SQL (`${scopesLit}::text[]`). This manually composes SQL from request-derived data.
  - _Fix:_ Use parameterized array binding instead of hand-crafting a text[] literal. Let Drizzle bind the JS array as a parameter, or use database client helpers to pass arrays safely, and/or whitelist/validate scope values before persisting.
- **server/lib/mcp-api-keys.ts:182** — UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id} executes without an explicit tenant_id constraint. Updating by id only can bypass application-level tenant scoping guarantees.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}`) or otherwise bind the update to the resolved tenant to enforce the application-level tenancy invariant.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without constraining by tenant_id — raw SQL query returns rows across all tenants.
  - _Fix:_ If this must be a multi-tenant audit, restrict access to an admin-only path and document it. Otherwise add an application-level tenant filter (e.g. WHERE tenant_id = <callerTenantId>) or accept an explicit tenantId param and apply it to the query to enforce tenant isolation.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` without constraining by tenant_id — raw SQL query returns rows across all tenants.
  - _Fix:_ If this is intended to run cross-tenant only as an admin job, ensure the callsite is admin-only and audited. Otherwise add a tenant filter (e.g. WHERE tenant_id = <callerTenantId>) or accept an explicit tenantId param and apply it to the query to enforce the project's tenant-isolation invariant.
- **server/minds-engine.ts:214** — INSERT INTO mind_tickets uses a constructed array literal for depends_on: ${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}. This builds an SQL array literal from caller-provided values via string interpolation.
  - _Fix:_ Avoid constructing array literals via string-joining. Use properly parameterized array binding (pass an actual array value if the driver supports it) or use parameterized helpers so user-derived values are never interpolated into SQL syntax. Validate/sanitize ticket.dependsOn elements as integers before use.
- **server/oauth-subscriptions.ts:884** — SELECT query on tenant-scoped table `oauth_subscriptions` queries all active subscriptions across tenants (WHERE is_active = TRUE) without constraining by tenant_id. This reads sensitive per-tenant data (tokens/refresh tokens) without an application-level tenant filter.
  - _Fix:_ If the intent is to operate per-tenant, iterate tenants and run tenant-scoped queries (add WHERE tenant_id = ${tenantId}). If this is an intentional global admin operation, make the admin authority explicit and still avoid returning raw token fields; otherwise add an explicit tenant filter to the SELECT to enforce tenant isolation.
- **server/oauth-subscriptions.ts:893** — SELECT tenant_id, is_active FROM `oauth_subscriptions` WHERE provider = 'google-workspace' runs without a tenant_id filter, returning rows across all tenants.
  - _Fix:_ Restrict this query to a specific tenant when performing tenant-scoped work, or explicitly enumerate/validate tenant ids before operating. For global startup auto-reconnect logic, fetch tenant ids from an explicit tenants table or configuration and run per-tenant queries rather than an unconstrained SELECT.
- **server/oauth-subscriptions.ts:1236** — proactiveTokenRefresh() executes a SELECT on `oauth_subscriptions` with WHERE is_active = TRUE but no tenant_id constraint, iterating subscriptions across all tenants. This reads tenant-scoped secrets without per-tenant scoping in the query.
  - _Fix:_ If the function must refresh tokens across tenants, make that intention explicit and perform per-tenant, per-subscription operations by enumerating tenant ids (from a tenants table or safe whitelist) and running tenant-scoped SELECT/UPDATE queries. Otherwise add WHERE tenant_id = ${tenantId} to the query to enforce isolation.
- **server/oauth-subscriptions.ts:1306** — forceRefreshAllSubscriptions() selects provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without a tenant_id filter, returning rows across tenants for a mass refresh operation.
  - _Fix:_ Avoid unconstrained selects over tenant-scoped tables. Either require an explicit admin/operational context and read tenant ids from a trusted tenants list, then perform per-tenant refreshes (tenant-scoped queries), or add an explicit tenant filter. Ensure any global operation is audited and intentionally authorized.
- **server/orchestrator-ledger.ts:452** — UPDATE against tenant-scoped table memory_entries omits an explicit tenant filter: `UPDATE memory_entries SET ... WHERE id = ${row.id}`. Per the project's invariant every write against a tenant-scoped table must include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping on the UPDATE to defend against accidental cross-tenant updates (even if id was selected earlier). For example: `UPDATE memory_entries SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Apply the same constraint to any UPDATE/DELETE on tenant-scoped tables.
- **server/routes.ts:1599** — Public stats endpoint queries tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with global COUNT(*) queries and no tenant_id filters, exposing aggregate counts across all tenants.
  - _Fix:_ If global aggregates are acceptable, restrict this endpoint to admin-only or sanitize that it's intentionally global. Otherwise scope counts per-tenant (WHERE tenant_id = ...) or provide only non-sensitive public metrics that don't reveal multi-tenant data.
- **server/routes.ts:1627** — Public architecture endpoint executes multiple SELECT count(*) FROM <tenant-scoped table> queries (personas, projects, skills, memory_entries, etc.) with NO tenant_id constraints, returning cross-tenant aggregate data.
  - _Fix:_ Restrict this endpoint to platform admins or add tenant scoping. If multi-tenant counts are required, compute them in an admin-only context or explicitly document and harden that these are non-sensitive global metrics.
- **server/routes.ts:767** — backfillProjectDriveFolders performs SELECT over projects JOIN tenants to find projects needing drive folders without scoping to any tenant (no caller tenant filter). This enumerates projects across all tenants.
  - _Fix:_ Run backfill operations under an explicit admin context and make that intent obvious, or accept/require an explicit tenant parameter and filter by tenant_id. Audit background jobs that touch tenant-scoped tables to ensure they run with explicit, intentional privileges.
- **server/routes/admin.ts:568** — SELECT against tool_performance (SELECT DISTINCT tool_name FROM tool_performance) has no tenant_id constraint — may return cross-tenant data.
  - _Fix:_ If tool_performance is tenant-scoped, add a WHERE tenant_id = ${tenantId} predicate (or join to a tenant-scoped table) so results are limited to the caller's tenant. If the table is intentionally global, document that explicitly and keep the admin-only guard; otherwise require an explicit tenant filter or scope the query to ADMIN_TENANT_ID only when appropriate.
- **server/routes/admin.ts:574** — Query on tool_performance (SELECT ... FROM tool_performance WHERE last_failure_at > ...) lacks tenant_id filtering — returns platform-wide rows.
  - _Fix:_ If tool_performance contains tenant-specific metrics, add WHERE tenant_id = ${tenantId}. If the query is intended to be global, make that explicit (comment) and keep the platform-admin guard; otherwise scope to a tenant or provide an explicit tenant parameter.
- **server/routes/conversations.ts:99** — SELECT from project_conversations omits tenant scoping — SQL: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`. Reading a tenant-scoped join table without tenant filter can leak cross-tenant associations.
  - _Fix:_ Constrain the SELECT by tenant (e.g. `... WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}`) or use a storage API that enforces tenant ownership.
- **server/routes/conversations.ts:104** — UPDATE on projects omits tenant scoping — SQL: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`. Even though projectId was previously checked, the UPDATE itself lacks tenant_id in the WHERE clause, violating the invariant that SQL must enforce tenant scope.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. `WHERE id = ${projectId} AND tenant_id = ${tenantId}`) so the DB update cannot affect other tenants even if earlier checks are bypassed.
- **server/routes/conversations.ts:68** — SELECT from personas omits tenant scoping — SQL: `SELECT reasoning_config FROM personas WHERE id = ${activePersona.id}`. If personas is tenant-scoped this read is missing a tenant_id constraint and could leak another tenant's data.
  - _Fix:_ Include tenant scoping in the query (e.g. add `AND tenant_id = ${tenantId}`) or use a storage helper that returns persona data scoped to the caller's tenant.
- **server/routes/slack.ts:124** — resolveFirstWordPersona constructs and executes a sql`` template that interpolates `token` (derived from Slack request text) directly into the query: db.execute(sql`... LOWER(p.name) = LOWER(${token}) ...`). This is string-interpolated SQL with a request-derived value (injection + isolation risk according to the project invariant).
  - _Fix:_ Avoid embedding request-derived values into raw SQL templates. Use the Drizzle query-builder APIs (db.select().from().leftJoin().where(eq(...), or(...))) so values are passed as bound parameters (e.g. .where(eq(personas.name, token)) or equivalent). Keep the existing token validation, but replace sql`` interpolation with explicit .where(eq(...)) / .or() expressions or properly-parameterized functions to eliminate string-interpolated SQL.
- **server/routes/stripe-checkout.ts:102** — Anonymous/unauthenticated checkout allowlist check uses db.execute(sql`... WHERE pr.id = ${priceId} ...`) where `priceId` comes from req.body. This is string-interpolated SQL with request-derived input.
  - _Fix:_ Replace the ad-hoc sql`` execution with Drizzle's query builder (db.select().from(prices).join(products).where(eq(prices.id, priceId), eq(prices.active, true), eq(products.active, true), ...)). This keeps priceId as a bound parameter and avoids string-interpolated SQL. Also ensure priceId is validated/sanitized (already partially done) and keep the additional anonymous/product-kind checks intact via query-builder predicates.
- **server/routes/stats.ts:54** — Tenant-scoped counts use string-interpolated sql templates for tenant filtering: .where(s`${conversations.tenantId} = ${tenantId}`) where tenantId is resolved from the request. The code uses raw sql templating instead of Drizzle eq() helpers.
  - _Fix:_ Use Drizzle's typed predicates to express tenant constraints, e.g. .where(eq(conversations.tenantId, tenantId)). Avoid building WHERE clauses via sql`` templates with request-derived values; prefer the query-builder .where(eq(...)) form so the tenant-id constraint is explicit and parameterized.
- **server/routes/stats.ts:55** — Messages count query embeds a request-derived tenantId inside a raw sql subquery: .where(s`${messages.conversationId} IN (SELECT id FROM conversations WHERE tenant_id = ${tenantId})`). This is string-interpolated SQL with a request-derived value and bypasses Drizzle predicate helpers.
  - _Fix:_ Rewrite the subquery using Drizzle query-builder constructs (for example, use a .where(in(messages.conversationId, db.select({ id: conversations.id }).from(conversations).where(eq(conversations.tenantId, tenantId)))) or join to conversations and then .where(eq(conversations.tenantId, tenantId))). This ensures tenant scoping is applied via eq()/in() predicates and avoids interpolated sql`` with request data.
- **server/storage.ts:503** — SELECT from tenant-scoped memory_entries without tenant constraint when tenantId omitted (getMemoryEntry). If tenantId is undefined the function warns but still performs `WHERE id = ...` with no tenant filter, enabling a cross-tenant read.
  - _Fix:_ Make tenantId required (fail-closed) or enforce assertValidTenantId and include the tenant filter in the WHERE. If an unscoped getter is required, isolate and document it clearly and restrict callers; prefer an explicit `getMemoryEntryUnscoped` with strong logging/auditing.
- **server/storage.ts:862** — SELECT heartbeat_tasks WHERE id = ${id} with no tenant filter (getHeartbeatTask). heartbeat_tasks is tenant-scoped but this getter does not restrict by tenant, enabling cross-tenant read by id.
  - _Fix:_ Add a tenantId parameter or otherwise scope the lookup (e.g. `.where(and(eq(heartbeat_tasks.id, id), eq(heartbeat_tasks.tenantId, tenantId)))`). If an unscoped admin getter is required, make it explicit and audit its callers.
- **server/storage.ts:610** — UPDATE daily_notes performed by id without an explicit tenant_id check in upsertDailyNote (uses `where(eq(dailyNotes.id, existing.id))`). If existing was discovered without tenant scoping this can update another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `.where(and(eq(dailyNotes.id, existing.id), eq(dailyNotes.tenantId, existing.tenantId)))`) or require tenantId in the API and assert ownership before mutating.
- **server/surprise-scorer.ts:200** — SELECT AVG(...) FROM felix_proposals WHERE id = ANY(${neighborIds}::int[]) executes without a tenant filter. Although neighborIds originated from a tenant-scoped query, this SELECT does not explicitly constrain tenant_id which widens the blast radius if neighborIds are ever influenced.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE (and `AND id = ANY(...)`) or re-query neighbors joined on tenant_id to guarantee the AVG is computed only over the intended tenant's rows.
- **server/storage.ts:806** — getMemoryStats may perform broad SELECTs without tenant scoping when tenantId is undefined (`await db.select(...).from(memory_entries)`), returning memory entries across tenants.
  - _Fix:_ Require tenantId (fail-closed) or ensure that unconstrained calls are only made from admin context. Prefer explicit tenant scoping in all selects against tenant-scoped tables to prevent accidental multi-tenant data exposure.
- **server/tools.ts:12118** — Drizzle SELECT from fileStorage without tenant constraint. db.select(...).from(fileStorage) enumerates persisted file records but does not filter by tenant. If fileStorage is tenant-scoped, this can expose other tenants' uploaded file metadata.
  - _Fix:_ If fileStorage is per-tenant, add an explicit WHERE tenant_id = ${params._tenantId} (or Drizzle .where(...) filter) or use a tenant-scoped query helper. If fileStorage is intentionally global, document that explicitly; otherwise fail-closed and restrict results to the caller's tenant.
- **server/video-job-runner.ts:204** — isCancelRequested reads videoJobs.cancelRequested using WHERE jobId = ${jobId} without also filtering by tenant_id. This is a SELECT against a tenant-scoped table with no tenant constraint (can leak or act on rows belonging to other tenants).
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}) or otherwise validate that the jobId being queried belongs to the expected tenant before returning the cancel flag. If tenantId is not available in this context, read the job state from the authoritative source first and verify ownership.

### LOW (1)

- **server/storage.ts:240** — getConversationUnscoped performs an explicit unscoped SELECT on conversations (`WHERE id = ${id}`) and is intentionally unsafe (logs a warning). This is a deliberate unscoped read of a tenant-scoped table.
  - _Fix:_ If unscoped reads are necessary, keep them isolated and audit-callers; require explicit naming (e.g. getConversationUnscoped) and ensure every caller validates tenant ownership immediately after use. Consider stricter logging or limiting use to trusted internal paths.
