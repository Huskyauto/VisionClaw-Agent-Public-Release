# Tenant-Isolation Audit Report

_Generated 2026-08-17T23:41:33.170Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **598/598** file(s) audited · chunks 56/56 OK (0 failed)
- Findings: **283** (CRITICAL 74 · HIGH 163 · MEDIUM 46 · LOW 0)
- Precision second-pass (`gpt-5.4`): **224** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (74)

- **server/agentic/runs.ts:34** — appendStep updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with NO tenant_id constraint — allows cross-tenant modification if runId is known.
  - _Fix:_ Constrain updates to the caller's tenant. Add a tenantId parameter to appendStep and include it in the WHERE clause, e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Validate the caller-supplied tenant before mutating.
- **server/agentic/runs.ts:45** — updateRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with NO tenant_id constraint — tenant-scoped table write missing tenant check.
  - _Fix:_ Require the caller pass tenantId and add it to the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Alternatively resolve the run's tenant and verify it before updating.
- **server/agentic/runs.ts:54** — replaceRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) without tenant_id filtering — violates tenant isolation invariant.
  - _Fix:_ Include tenant scoping in the update. Accept tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers supply/verify tenantId.
- **server/agentic/runs.ts:58** — completeRun updates agent_runs (sets status/result/completedAt) using only run id in WHERE, with NO tenant_id constraint — allows cross-tenant writes to run records.
  - _Fix:_ Add tenantId to completeRun signature and include eq(agentRuns.tenantId, tenantId) in the WHERE clause (or otherwise verify ownership) so only the calling tenant can mark its runs complete.
- **server/agentic/runs.ts:88** — failRun updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with NO tenant_id constraint — tenant isolation missing on failure path.
  - _Fix:_ Require and use tenantId when calling failRun; include it in the WHERE clause (AND eq(agentRuns.tenantId, tenantId)). Audit all call sites to pass the tenant and enforce ownership.
- **server/agentic/runs.ts:97** — pauseRun updates agent_runs (status/state) using only run id in WHERE, lacking tenant_id constraint — unscoped writes to a tenant-scoped table.
  - _Fix:_ Add tenantId to pauseRun arguments and include it in the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers pass the correct tenant and that all run-state mutators are tenant-scoped.
- **server/auto-memorize.ts:74** — INSERT INTO agent_knowledge hardcodes tenant_id = 1 when creating a watermark — writes will be attributed to tenant 1 for all runs.
  - _Fix:_ Do NOT hardcode tenant_id. Accept/derive the calling tenant and INSERT with tenant_id = ${tenantId} (pass tenantId into setWatermark) so new rows are stored under the correct tenant.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL lacks tenant_id filtering — this can modify another tenant's conversation row.
  - _Fix:_ Include AND tenant_id = ${c.tenant_id} (or AND tenant_id = ${tenantId}) in the UPDATE WHERE clause to ensure the update only affects rows owned by the calling tenant.
- **server/channel-routing.ts:16** — channel_routes table is created without a tenant_id column, making routing entries globally shared across tenants and preventing application-level tenant-scoping.
  - _Fix:_ Add a NOT NULL tenant_id column to channel_routes (and a unique constraint that includes tenant_id, e.g. UNIQUE(tenant_id, channel)). Update ensureChannelRoutingTable to create tenant_id and any FK to tenants (but do not rely on FK for isolation); then update all queries to filter by tenant_id.
- **server/channel-routing.ts:48** — setChannelRoute inserts/updates and deletes channel_routes entries without any tenant context and accepts personaId from caller input without verifying ownership. This allows a caller to create or overwrite mappings that reference personas from other tenants (or globally modify routes).
  - _Fix:_ Require a tenantId parameter for setChannelRoute. Before inserting/updating, validate that the provided personaId belongs to the caller's tenant (SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). Persist tenant_id into channel_routes on INSERT and use ON CONFLICT (tenant_id, channel) DO UPDATE. For deletes, include tenant_id in the WHERE clause so the operation is scoped to the caller's tenant.
- **server/channel-routing.ts:71** — removeChannelRoute deletes channel_routes entries by channel only, with no tenant scoping. This allows deletion of other tenants' channel routes.
  - _Fix:_ Require tenantId for removeChannelRoute and run DELETE FROM channel_routes WHERE tenant_id = ${tenantId} AND channel = ${channel}. Ensure any UNIQUE constraints include tenant_id so operations conflict only within a tenant.
- **server/commitment-drafter.ts:232** — Unscoped UPDATE on tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` is executed without constraining WHERE tenant_id = ${tenantId}. This can modify rows belonging to other tenants.
  - _Fix:_ Add explicit tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenantId}) or avoid running the unclaim at all when the tenant is invalid. Ensure every UPDATE/DELETE on commitments includes AND tenant_id = ${tenantId}. Consider performing this inside the same transaction/lock that validated tenant ownership.
- **server/data-protection.ts:168** — DELETE FROM conversations executed without constraining by tenant_id. The code previously read expired rows including tenant_id but the final DELETE uses only the conversation id, allowing a race or FK anomaly to delete another tenant's conversation.
  - _Fix:_ Add a tenant predicate to the DELETE, e.g. `DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}` (or pass the explicit tenantId). Prefer parameterized values (no raw SQL) so the delete is atomic and scoped to the tenant.
- **server/embeddings.ts:742** — UPDATE against tenant-scoped table (memory_entries | agent_knowledge) in storeEmbeddingVec does not constrain by tenant_id. The function updates embedding_vec / embedding by id only, allowing cross-tenant writes.
  - _Fix:_ Require & accept the caller's tenantId in storeEmbeddingVec, and include AND tenant_id = ${tenantId} in the WHERE clause. Example: UPDATE <table> SET ... WHERE id = ${id} AND tenant_id = ${tenantId}. Validate the table name via the allowlist (already present) and validate embedding with vecLiteral before constructing any raw fragment.
- **server/embeddings.ts:751** — SELECT from agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter. This reads tenant-scoped rows across ALL tenants.
  - _Fix:_ Scope the backfill to a single tenant (accept tenantId param) or iterate per-tenant. Add WHERE tenant_id = ${tenantId} to the SELECT so only rows owned by the caller/explicit tenant are processed.
- **server/embeddings.ts:780** — SELECT from memory_entries in backfillEmbeddingVecs lacks tenant_id constraint and therefore reads memory entries across tenants.
  - _Fix:_ Run backfill per-tenant (accept tenantId) or otherwise restrict the SELECT with AND tenant_id = ${tenantId}. Never scan tenant-scoped tables globally without an explicit admin/operational justification and checks.
- **server/embeddings.ts:790** — UPDATE memory_entries in backfillEmbeddingVecs uses WHERE id = ${row.id} with no tenant_id constraint—possible cross-tenant writes.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use a per-tenant backfill loop). Validate row ownership before updating or join on tenant-scoped predicate to ensure only the intended tenant's row is modified.
- **server/embeddings.ts:796** — SELECT from agent_knowledge in backfillEmbeddingVecs has no tenant_id filter — reads knowledge rows for all tenants.
  - _Fix:_ Scope this query by tenant_id = ${tenantId} or run backfill per-tenant. Avoid global reads of tenant-scoped tables; require an explicit admin flow if global maintenance is required, with safeguards and auditing.
- **server/embeddings.ts:807** — UPDATE agent_knowledge in backfillEmbeddingVecs uses WHERE id = ${row.id} with no tenant_id constraint — allows cross-tenant writes.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE WHERE clause or only operate on rows fetched for the same tenant. Prefer performing selects and updates within the same tenant-scoped transaction or pass tenantId through to storeEmbeddingVec and use it in the WHERE.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` (archiving) without constraining by tenant_id — mass write may modify proposals belonging to other tenants.
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or load/verify each proposal's tenant before updating. Alternatively, perform this operation only under a trusted admin pathway and log/require explicit admin consent. Avoid blind UPDATEs by id alone on tenant-scoped tables.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — writes to tenant-scoped table without tenant_id constraint, potentially disabling tasks for other tenants.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. AND tenant_id = ${tenantId}) or verify the tenant ownership of each id before changing state. Never update tenant-scoped resources solely by id without verifying tenant ownership.
- **server/google-workspace.ts:2358** — INSERT into `presenter_slide_images` performed without supplying/validating tenant_id. If `presenter_slide_images` is tenant-scoped this allows inserting data not tied/validated to a tenant.
  - _Fix:_ Include tenant_id as an explicit column/value in the INSERT (e.g. VALUES (${tenantId}, ...)) or resolve the session → tenant mapping and validate ownership before inserting. Do not rely solely on session_id without verifying that the session belongs to the caller's tenant.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} without constraining by tenant_id. This updates a tenant-scoped session row by id only, allowing cross-tenant modification if sessionId is untrusted or an attacker can supply it.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first SELECT the session by id and assert its tenant_id equals the caller's tenant before updating. Always enforce ownership when updating rows referenced by foreign ids.
- **server/minds-engine.ts:327** — SQL array literal is constructed via string interpolation from caller-supplied eventIds: `WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}`. This mixes request-derived values into SQL text/array literal and can lead to SQL-injection and/or tenant-isolation bypass.
  - _Fix:_ Stop building Postgres array literals via string.join. Use parameterized array bindings (e.g. pass eventIds as an actual array parameter / use Drizzle's array helper) and validate every id. Example: WHERE id = ANY(${eventIds}::int[]) with a proper parameterized array binding, or use WHERE id = ANY($1::int[]) with eventIds passed as an array. Additionally, validate/normalize eventIds (ensure integers) and verify the rows belong to tenantId before updating.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` is executed without constraining by tenant_id (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This exposes rows across all tenants (sensitive tokens/metadata).
  - _Fix:_ Avoid global SELECTs on tenant-scoped tables. Either iterate per-tenant (explicit tenant_id) from a trusted admin-only code path, or require an explicit admin context and include a WHERE tenant_id = ${tenantId} filter. Do not return or process encrypted tokens for tenants other than the caller unless running in an audited, privileged admin routine; if an admin routine is required, isolate it, log/authorize it, and avoid loading raw token columns unless necessary.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is run with no tenant_id constraint. This reads subscription rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (WHERE tenant_id = ${tenantId}) when reading tenant-scoped tables, or make sure the call runs only in an authorized global-admin context and minimize exposed columns. Prefer per-tenant operations rather than unbounded queries.
- **server/oauth-subscriptions.ts:956** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} updates a tenant-scoped table using only id in the WHERE clause (no tenant_id check). Without an application-level tenant constraint this could modify another tenant's row if IDs are guessed or misused.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) for all UPDATE/DELETE statements on tenant-scoped tables. Alternatively, use (provider, tenant_id) or otherwise assert ownership before mutating rows.
- **server/oauth-subscriptions.ts:966** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} (no tenant_id constraint) — same unsafe pattern present in another branch of startup validation.
  - _Fix:_ Same fix: require AND tenant_id = ${sub.tenant_id} (or use provider+tenant_id) in the WHERE clause and validate ownership before modifying tenant-scoped rows.
- **server/oauth-subscriptions.ts:981** — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} executed without tenant_id constraint (again in a different branch). This mutates tenant-scoped data without enforcing tenant ownership in the WHERE clause.
  - _Fix:_ Ensure every UPDATE/DELETE against oauth_subscriptions includes tenant_id (e.g. WHERE id = ${sub.id} AND tenant_id = ${sub.tenant_id}) or otherwise verifies ownership before writing. Centralize safe update helpers to avoid repeats.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh() executes an unbounded SELECT on oauth_subscriptions: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE. This enumerates subscriptions across tenants (sensitive) without per-tenant scoping.
  - _Fix:_ Do not perform unscoped reads of tenant-scoped tables. If the intended behavior is a global admin refresh, ensure the execution context is strictly authorized and minimize data retrieved (avoid tokens/refresh tokens), or instead run refresh per-tenant by iterating a trusted tenant list and using WHERE tenant_id = ${tenantId} for each query.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions() issues SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant constraint, reading rows for all tenants and then performing refresh operations across tenants.
  - _Fix:_ Either require an explicit privileged admin execution context and harden logging/authorization, or rework to operate strictly per-tenant by selecting only for a given tenant_id. Always include tenant_id in WHERE clauses for tenant-scoped tables and avoid bulk cross-tenant operations that handle sensitive credentials.
- **server/plan-executor.ts:117** — appendExecutionLog() updates the tenant-scoped plans table without constraining by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows cross-tenant modification of a plan row by id.
  - _Fix:_ Include the plan's tenant in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenant}). Pass the plan's tenantId into appendExecutionLog or look it up in a safe, validated context and always enforce WHERE tenant_id = <caller tenant> on the UPDATE.
- **server/plan-executor.ts:513** — executePlan() claims a plan via UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' without scoping the operation to a tenant (no tenant_id filter). A caller can claim/operate on any plan id across tenants.
  - _Fix:_ Require and use the caller's tenant context when claiming plans: include AND tenant_id = ${callerTenant} in the UPDATE. Alternatively validate the plan's tenant before proceeding and fail-closed if it doesn't match the caller's tenant.
- **server/plan-executor.ts:728** — After execution, code updates the plan status via UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} with no tenant_id constraint. This can modify plans across tenants by id.
  - _Fix:_ Restrict the UPDATE to the plan's tenant (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id} or the caller tenant). Ensure you pass and validate tenant context when updating plans.
- **server/plan-executor.ts:762** — Error-handling rollback uses UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' without a tenant_id constraint, allowing cross-tenant state changes.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${callerTenant} or tenant_id = ${row.tenant_id}). Ensure all error-path updates honor the same tenant boundary used when the plan was claimed.
- **server/plan-executor.ts:778** — resumeStuckPlans() resets all stale executing plans back to 'approved' via UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id with no tenant restriction. This mass-update touches rows across all tenants.
  - _Fix:_ Avoid cross-tenant mass updates. Restrict the query to a specific tenant context (WHERE tenant_id = ${callerTenant} AND ...), or iterate tenants explicitly in an admin-only routine that is clearly authorized. If the function must operate globally, require an explicit, elevated admin tenancy and document/guard it, but otherwise enforce per-tenant WHERE tenant_id = <caller tenant>.
- **server/plan-executor.ts:787** — resumeStuckPlans() selects pending approved plans via SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 with no tenant_id filter, then re-queues execution for those plan ids without any tenant scoping.
  - _Fix:_ When selecting pending plans to kick off, filter by tenant (e.g. WHERE tenant_id = ${callerTenant} AND status = 'approved') or otherwise ensure the function runs per-tenant. Never iterate or act on tenant-scoped rows without applying the caller's tenant_id in the WHERE clause.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` omits tenant_id in the WHERE clause (UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}). This allows an update to affect a row belonging to any tenant.
  - _Fix:_ Include the tenant constraint in the WHERE clause. E.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}` (or pass the tenantId explicitly into the query). If the runner must operate globally, still always constrain any write with the row's tenant_id before performing UPDATE/DELETE.
- **server/reference-learner.ts:291** — UPDATE against tenant-scoped table `memory_entries` omits tenant_id in the WHERE clause (UPDATE memory_entries SET fact = ..., last_accessed = NOW() WHERE id = ${storedId}). This can update rows across tenants if ids collide or are mis-used.
  - _Fix:_ Constrain the UPDATE by tenant: `WHERE id = ${storedId} AND tenant_id = ${input.tenantId}` (or otherwise verify ownership before performing the UPDATE). Always include tenant_id in WHERE for updates on tenant-scoped tables.
- **server/research-engine.ts:165** — UPDATE in cleanupZombieSessions modifies `research_sessions` without scoping by tenant (UPDATE research_sessions SET status = 'completed' ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}). This can change session state for sessions belonging to any tenant.
  - _Fix:_ Restrict the cleanup to the intended tenant scope. If the routine must be global, iterate per-tenant or include an explicit tenant_id predicate. E.g. add `AND tenant_id = ${tenantId}` or perform the update within a per-tenant loop. Never perform unscoped UPDATE/DELETE on tenant-scoped tables.
- **server/research-engine.ts:416** — DELETE from tenant-scoped table `agent_knowledge` is executed without tenant scoping (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will delete autoresearch entries across all tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. `AND tenant_id = ${tenantId}`) or run the delete per-tenant. If global cleanup is intended, require an explicit admin-controlled code path and log/monitor it; otherwise, fail-closed and scope the delete.
- **server/research-engine.ts:496** — INSERT into `project_notes` omits tenant identification (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...)). Per codebase invariant tenant-scoped tables require tenant_id to be provided on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Supply tenant_id explicitly in the INSERT (and determine it from the project ownership after assertProjectInTenant). Example: `INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ${note}, ${'Research Engine'}, NOW())`.
- **server/research-engine.ts:503** — Another INSERT into `project_notes` (for each finding) omits tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (${projectId}, ${noteContent}, ${'Research Engine'}, NOW())). Missing tenant_id risks mis-attribution or DB errors.
  - _Fix:_ Include tenant_id in these INSERTs as well (e.g. `VALUES (${projectId}, ${session.tenantId}, ${noteContent}, ${'Research Engine'}, NOW())`) or derive/validate the correct tenant before inserting. Ensure all writes to tenant-scoped tables set tenant_id explicitly.
- **server/routes/conversations.ts:100** — UPDATE on tenant-scoped table `conversations` without tenant_id constraint in WHERE clause (`UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`).
  - _Fix:_ Include tenant scoping in the UPDATE so only the caller's tenant row can be modified, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or perform the update via a storage helper that enforces tenant ownership).
- **server/routes/conversations.ts:101** — SELECT from tenant-scoped table `project_conversations` without tenant_id constraint (`SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`).
  - _Fix:_ Constrain the SELECT to the caller's tenant by adding AND tenant_id = ${tenantId}, e.g. WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId} (or use a repository/storage method that enforces tenant scoping).
- **server/routes/conversations.ts:104** — INSERT into tenant-scoped table `project_conversations` that does not set tenant_id (`INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)`).
  - _Fix:_ Include tenant_id in the INSERT columns and pass the caller's tenant id (VALUES (${projectId}, ${conv.id}, ${tenantId})) or use an insertion helper that sets tenant_id and enforces ownership. Ensure the column is NOT NULL and validated server-side before insert.
- **server/routes/conversations.ts:106** — UPDATE on tenant-scoped table `projects` without tenant_id constraint in WHERE clause (`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so you cannot update another tenant's project. Prefer using the earlier validated tenant check and include the tenant_id in the SQL WHERE to satisfy defense-in-depth.
- **server/routes/conversations.ts:229** — INSERT into tenant-scoped table `project_notes` that does not set tenant_id (`INSERT INTO project_notes (project_id, note, author) VALUES (...)`).
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) and/or verify project ownership before inserting. Use parameterized values and storage-layer helpers that enforce tenant isolation.
- **server/seed.ts:2163** — UPDATE on tenant-scoped table research_programs without tenant_id constraint (UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}). This can modify another tenant's row if ids are global or otherwise violate tenant isolation.
  - _Fix:_ Include tenant scoping in the UPDATE. Use WHERE id = ${p.id} AND tenant_id = ${tenantId} (or Drizzle .where(eq(research_programs.tenantId, tenantId), eq(research_programs.id, p.id))). If p.id is derived from an untrusted or cross-tenant source, validate ownership before updating.
- **server/seed.ts:2515** — SELECT DISTINCT ... FROM conversations without tenant restriction (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...project_conversations...)). conversations appears tenant-scoped but the query does not filter by tenant_id (and project_conversations lacks tenant_id), risking cross-tenant data access and incorrect cross-tenant link creation.
  - _Fix:_ Filter by tenant (e.g. WHERE c.tenant_id = ${tenantId}) and ensure project_conversations is joined/filtered within the same tenant context. Better: include tenant_id on project_conversations (if schema permits) and use a tenant-scoped join: WHERE c.tenant_id = ${tenantId} AND EXISTS/NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id AND pc.tenant_id = ${tenantId}). Validate ownership of project_id before backfilling links.
- **server/self-improvement.ts:401** — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) in WHERE — no tenant_id constraint. This can modify rows belonging to other tenants if IDs collide or are guessed.
  - _Fix:_ Include the tenant predicate in the WHERE (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, <callerTenantId>)))) or validate ownership before updating. Prefer requiring and using the caller's tenantId when updating.
- **server/self-improvement.ts:415** — Error-path UPDATE on tenant-scoped `experiments` (catch block) updates by id only with no tenant_id constraint, allowing cross-tenant modification.
  - _Fix:_ As above, include tenant filtering in the WHERE clause (AND tenant_id = <callerTenantId>) or verify the experiment belongs to the caller's tenant before updating. Never update tenant-scoped rows by id alone.
- **server/self-reflection.ts:366** — UPDATE memory_entries in storeLesson updates rows WHERE id = <found id> with no tenant_id constraint. Updating by id alone can modify another tenant's memory entry.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = <id> AND tenant_id = ${tenantId}) or perform the UPDATE using the tenant-scoped predicate. Alternatively, fetch and validate the row's tenant_id prior to updating to ensure ownership.
- **server/stability-watchdog.ts:177** — SELECT aggregation from heartbeat_logs has no tenant_id filter: the query aggregates error counts across all heartbeat_logs without scoping to a tenant, causing cross-tenant reads and selection of other tenants' failure data.
  - _Fix:_ Scope the aggregation per-tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate tenants and run tenant-scoped queries. Avoid unscoped reads on tenant-scoped tables; require explicit tenant_id in the WHERE clause.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks executed without tenant scoping: cleanupStaleData deletes heartbeat_tasks WHERE enabled = false ... created_at < ... with no tenant_id constraint, which can remove other tenants' tasks.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. AND tenant_id = ${tenantId}) or perform cleanup in a per-tenant loop. Never run DELETE/UPDATE on tenant-scoped tables without an explicit tenant_id filter.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log executed without tenant scoping: pruneOldLogs removes old event_log rows based solely on created_at and status, lacking a tenant_id WHERE clause and risking cross-tenant data deletion.
  - _Fix:_ Add an explicit tenant_id filter (WHERE tenant_id = ${tenantId} AND ...) or run prune per tenant. Ensure deletions/updates on tenant-scoped tables always include tenant_id filtering.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs executed without tenant scoping: pruneOldLogs removes old heartbeat_logs across all tenants (created_at cutoff) with no tenant_id in the WHERE clause, enabling cross-tenant deletion.
  - _Fix:_ Restrict the DELETE to a specific tenant (WHERE tenant_id = ${tenantId} ...) or perform pruning per-tenant. All writes/deletes against tenant-scoped tables must include tenant_id in the SQL filter.
- **server/storage.ts:398** — attachCitations UPDATE on tenant-scoped table `messages` writes without any tenant_id constraint (UPDATE messages SET ... WHERE id = ${messageId}).
  - _Fix:_ Fail-closed: require the caller to pass/validate the message's tenantId and include AND tenant_id = ${tenantId} in the WHERE clause (or perform a verified join to ensure ownership). Never execute UPDATE/INSERT/DELETE against tenant-scoped tables by id alone.
- **server/storage.ts:841** — archiveExpiredMemories executes a global UPDATE on tenant-scoped table `memory_entries` with NO tenant filter (UPDATE memory_entries SET status='archived' WHERE ...). This will modify rows across all tenants.
  - _Fix:_ Either run this per-tenant (accept tenantId and validate), or make sure the caller is an explicit admin and require an explicit ALL_TENANTS sentinel and authorization. Prefer per-tenant execution or include tenant predicate to fail-closed for scoped jobs.
- **server/storage.ts:857** — archiveStaleMemories executes a global UPDATE on tenant-scoped table `memory_entries` with NO tenant filter (UPDATE memory_entries SET status='archived' WHERE ...). This modifies data for all tenants.
  - _Fix:_ Same as archiveExpiredMemories: require explicit tenant scope or explicit, well-audited admin context. Prefer iterating per-tenant or adding tenant predicates to the UPDATE to avoid cross-tenant mutation.
- **server/storage.ts:1304** — getAllDataForExport issues multiple SELECTs that read tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with NO tenant filters — platform-wide export of tenant data.
  - _Fix:_ This operation must be restricted and explicit: require an authenticated admin context, and require an explicit ALL_TENANTS sentinel. Prefer splitting into a per-tenant export API that requires a validated tenantId, or add an explicit admin-only guard and strong audit/logging before returning cross-tenant data.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table `felix_proposals` (setting args_embedding) lacks a tenant_id constraint in the WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND args_embedding IS NULL) or change stampArgsEmbedding to accept and use the tenantId so every UPDATE is scoped to the caller's tenant.
- **server/surprise-scorer.ts:139** — UPDATE on tenant-scoped table `felix_proposals` (setting actual_outcome_embedding) omits tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) so the write cannot affect rows belonging to other tenants.
- **server/surprise-scorer.ts:148** — SELECT args_embedding FROM `felix_proposals` re-fetch is missing a tenant_id filter (reads tenant-scoped data without tenant constraint).
  - _Fix:_ Add tenant scoping to the SELECT (e.g. SELECT args_embedding FROM felix_proposals WHERE id = ${proposalId} AND tenant_id = ${tenantId}) to ensure the row belongs to the expected tenant.
- **server/surprise-scorer.ts:181** — UPDATE setting surprise_band = 'no_history' on `felix_proposals` does not include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${proposalId} AND tenant_id = ${tenantId}) so band updates are applied only to the intended tenant's row.
- **server/surprise-scorer.ts:212** — UPDATE setting surprise_score / surprise_band on `felix_proposals` is missing tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) to prevent cross-tenant modification of proposal scoring fields.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` (setting implementation during auto-fix) uses WHERE eq(custom_tools.id, tool.id) without also scoping by tenant_id.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${tool.id} AND tenant_id = ${tenantId}) to avoid modifying another tenant's row; ensure tenantId is available in this context and passed down to the update call.
- **server/tool-learning.ts:316** — DELETE FROM `custom_tools` in revertToolRow uses only the id in the WHERE clause and omits tenant_id, risking deletion of other tenants' data if IDs collide or are misused.
  - _Fix:_ Require and apply tenantId in the DELETE WHERE clause (e.g. WHERE id = ${tool.id} AND tenant_id = ${tenantId}) and ensure revertToolRow receives/validates the tenant context before deleting.
- **server/tool-learning.ts:365** — UPDATE incrementing usageCount on `custom_tools` (where eq(custom_tools.id, tool.id)) lacks tenant_id in the WHERE clause.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${tool.id} AND tenant_id = ${tenantId}) so usage counters cannot be incremented for rows that belong to other tenants; ensure the tenantId is passed into this update.
- **server/tools/domains/security/handlers.ts:163** — SELECT from tenant-scoped table `provider_keys` performed with no tenant_id constraint (SELECT provider, LENGTH(api_key) as key_len FROM provider_keys ...). This returns rows across all tenants.
  - _Fix:_ Restrict the query to the caller's tenant (or otherwise enforce an explicit admin-only audited path). E.g. include WHERE tenant_id = ${tenantId} (or equivalent drizzle .where(eq(table.tenantId, tenantId))). If this is intended to be an admin-only global audit, make the admin-check explicit and avoid returning raw key material (or redact/aggregate) to reduce blast radius.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed without tenant_id constraint. This enumerates provider key configuration platform-wide.
  - _Fix:_ Apply tenant scoping: add WHERE tenant_id = ${tenantId} (or pass tenantId into the query via parameterized drizzle API). If the call is intended only for a privileged admin, perform an explicit admin authorization check and avoid unconstrained reads of tenant-scoped secrets; consider aggregating/redacting results to non-sensitive metadata.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id constraint. This reads pairing rows across all tenants (title = <channel:phone>) and may disclose or allow processing of cross-tenant pairing data.
  - _Fix:_ Do NOT select agent_knowledge rows across all tenants. Constrain the query to the relevant tenant(s) (e.g. add AND tenant_id = ${tenantId}) or redesign the pairing lookup so it does not require scanning other tenants' rows. If the intent is to detect contested pairings across tenants, perform that check via a safe admin-only path or with explicit authorization, or use a canonical single-tenant mapping table. Consider enforcing Postgres RLS or a join against conversations with tenant filtering to avoid cross-tenant reads.
- **server/twilio.ts:116** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is performed without constraining by tenant_id. This reads a tenant-scoped conversations row by id across tenants rather than verifying ownership within the expected tenant context.
  - _Fix:_ Avoid reading tenant-scoped `conversations` rows without a tenant filter. Replace the check with a tenant-scoped lookup, e.g. SELECT 1 FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1 and treat absence as a mismatch. This verifies ownership without returning cross-tenant data. Alternatively, perform an explicit join that enforces tenant_id or use RLS to ensure the query cannot access other tenants' rows.
- **server/video-job-runner.ts:236** — recoverStaleVideoJobs performs a global UPDATE on the tenant-scoped videoJobs table without constraining by tenant_id. This will mark jobs from all tenants as failed when the heartbeat cutoff is hit, violating tenant isolation and potentially impacting other customers.
  - _Fix:_ Restrict the update to the intended tenant(s). Either run the stale-job recovery per-tenant (accept a tenantId param and include eq(videoJobs.tenantId, tenantId) in the WHERE), or enumerate tenants and apply the update per-tenant. Do NOT issue a single cross-tenant UPDATE on a tenant-scoped table.

### HIGH (163)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across tenants.
  - _Fix:_ Scope the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant. Ensure any processing of rows enforces tenant isolation before reading/updating other tenant-scoped tables.
- **server/agent-channels.ts:194** — Query joins `channel_subscriptions` without scoping that joined table to the caller's tenant (JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} ... WHERE cm.tenant_id = ${tenantId}). The join can match subscription rows from other tenants.
  - _Fix:_ Include the tenant constraint on channel_subscriptions in the JOIN (e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${params.tenantId}) or add an explicit WHERE clause filtering cs.tenant_id = tenantId to ensure tenant-scoped access.
- **server/agentic-engines.ts:347** — Query against `heartbeat_logs` missing tenant scoping (FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads logs across tenants.
  - _Fix:_ Restrict the query to the tenant (e.g. WHERE tenant_id = ${tenantId} AND task_name != 'Self-Reflection') or otherwise iterate per-tenant so results are tenant-scoped.
- **server/agentic-engines.ts:452** — SELECT FROM `heartbeat_logs` (recentLogs) without a tenant_id constraint (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...). This will return rows across tenants.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND status = 'success') to ensure isolation.
- **server/agentic-engines.ts:521** — Query aggregating `heartbeat_logs` (heartbeatPerformance) lacks tenant scoping (FROM heartbeat_logs GROUP BY task_name, status ...). This aggregates data across all tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause so aggregation is per-tenant, or perform per-tenant aggregation loops.
- **server/agentic-engines.ts:534** — SELECT from `heartbeat_tasks` (taskConfig) uses WHERE enabled = true but omits tenant_id; heartbeat_tasks is tenant-scoped elsewhere. This reads tasks across tenants.
  - _Fix:_ Restrict this query to the tenant (e.g. WHERE tenant_id = ${tenantId} AND enabled = true) to avoid cross-tenant exposure.
- **server/agentic-engines.ts:243** — UPDATE on tenant-scoped table `ai_insights` performed without tenant_id constraint (UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...). This updates rows across all tenants.
  - _Fix:_ Execute tenant-scoped updates (e.g. include WHERE tenant_id = ${tenantId}) or perform the sweep per-tenant so each UPDATE is constrained to a single tenant. Avoid mass cross-tenant updates.
- **server/agentic-engines.ts:161** — SELECT from `ai_insights` in retryPendingMinervaRouting lacks a tenant filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ... LIMIT 50). This can return rows for multiple tenants and lead to cross-tenant routing actions.
  - _Fix:_ Either run this routine per-tenant (adding WHERE tenant_id = ${tenantId}) or otherwise ensure the query is tenant-scoped before acting on returned rows.
- **server/agentic-features.ts:313** — Subquery over `competitor_snapshots` used in a WHERE ... NOT IN (...) lacks tenant scoping (inner SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ...). The inner subquery can return ids from other tenants and affect tenant-scoped outer results.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the inner subquery as well (and in all parts of composite queries) so every reference to tenant-scoped tables is filtered to the current tenant.
- **server/agentic-features.ts:638** — Lookup for existing enrollment misses tenant scoping (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'). This can match an active enrollment belonging to another tenant.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the WHERE clause when checking for existing enrollments to ensure the check is tenant-scoped before inserting.
- **server/agentic-features.ts:797** — UPDATE on `outreach_enrollments` omits tenant scoping (UPDATE outreach_enrollments SET ... WHERE id = ${row.id}). Although the row was previously fetched with a tenant filter, the UPDATE itself lacks tenant_id in the WHERE clause and could update a row with the same id in another tenant.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) to make the write operation tenant-scoped and prevent accidental cross-tenant modifications.
- **server/agentic/approvals.ts:135** — Bulk UPDATE on `agent_approvals` to expire pending approvals lacks tenant scoping (UPDATE agentApprovals SET status = 'expired' ... WHERE status = 'pending' AND expiresAt < now()). This updates approvals across all tenants in one operation.
  - _Fix:_ Perform expiry on a per-tenant basis (add tenant_id = ${tenantId} or accept a tenantId parameter and iterate tenants), or otherwise ensure the operation is explicitly intended and safe to run across all tenants. Always include tenant_id in UPDATE/DELETE against tenant-scoped tables unless the multi-tenant sweep is intentional and reviewed.
- **server/agentic/department-budgets.ts:143** — The query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads a tenant-scoped table without any tenant_id constraint. This performs a cross-tenant scan/enumeration of department budgets and then calls per-tenant logic for each row, violating the invariant that every read/write on tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Either restrict the query to a single tenant (add `WHERE tenant_id = ${tenantId}` and accept a tenantId parameter), or if a global sweep across tenants is intentional, make that explicit and privileged: require an admin-only execution context, document/authorize the operation, and ensure the code path is audited. Prefer per-tenant invocations (safer) or a dedicated, well-audited admin job rather than an unconstrained SELECT.
- **server/agentic/wake-scheduler.ts:142** — UPDATE/SELECT claims pending agent_wake_schedules rows without constraining by tenant_id (subquery selects ids from agent_wake_schedules WHERE status = 'pending' ... FOR UPDATE SKIP LOCKED with no tenant filter). This can claim/fire wakes for rows belonging to other tenants.
  - _Fix:_ Restrict the claim to a specific tenant (e.g. include WHERE tenant_id = ${tenantId} in the subquery), or iterate per-tenant to claim only that tenant's rows. Ensure every UPDATE/SELECT that mutates or locks agent_wake_schedules includes tenant_id = <expected tenant>.
- **server/auth.ts:1211** — DELETE FROM auth_sessions (clearAllSessions) issues an unscoped delete that wipes sessions for all tenants (no tenant_id constraint). This is a cross-tenant destructive operation.
  - _Fix:_ Require explicit platform-admin authorization and an auditable path for global session clears; provide a tenant-scoped variant that deletes sessions WHERE tenant_id = ${tenantId}. If the global action is intended, gate it behind requirePlatformAdmin and a clear confirmation path.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge in getWatermark() has no tenant_id constraint — reads watermark globally across tenants.
  - _Fix:_ Scope the query to the correct tenant (e.g. add AND tenant_id = ${tenantId} or pass a tenantId into getWatermark and filter by it) so each tenant's watermark is isolated.
- **server/auto-memorize.ts:62** — SELECT id FROM agent_knowledge in setWatermark() lacks a tenant_id filter — may return another tenant's row.
  - _Fix:_ Include tenant_id = ${tenantId} in the SELECT that finds an existing watermark (or otherwise derive the row by tenant) before updating.
- **server/auto-memorize.ts:68** — UPDATE agent_knowledge SET ... WHERE id = ${existing.rows[0].id} does not constrain by tenant_id — update could affect another tenant's row.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause (or perform the UPDATE by id only after selecting that id with tenant scoping) to ensure tenant isolation.
- **server/auto-project.ts:112** — SELECT FROM messages WHERE conversation_id = ${conversationId} has no tenant_id constraint — reading messages without explicit tenant scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages query (or otherwise enforce/verify conversation ownership immediately before) so message reads are restricted to the caller's tenant.
- **server/auto-transcript.ts:46** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} lacks tenant_id filtering — message rows are not explicitly scoped to the tenant.
  - _Fix:_ Constrain the query with AND tenant_id = ${tenantId} (or include tenant-scoped verification earlier and still add the tenant filter) to prevent cross-tenant reads.
- **server/auto-transcript.ts:128** — Backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) with no tenant filter — message existence/count is checked across all tenants.
  - _Fix:_ Make the correlated subquery tenant-aware, e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) or otherwise restrict by tenant to avoid cross-tenant data leakage.
- **server/auto-transcript.ts:153** — alsoLinked backfill query contains a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without tenant constraint — can detect messages across tenants.
  - _Fix:_ Restrict the subquery to the conversation's tenant, e.g. add AND tenant_id = c.tenant_id, or otherwise ensure the message count is evaluated per-tenant.
- **server/capability-review.ts:490** — markReviewReused updates capability_reviews without any tenant_id constraint and the function has no tenant context. The pendingReuseByConversation entry does not include tenantId, so this UPDATE (WHERE id = ${pending.reviewId} AND reused = false) can modify a row belonging to another tenant if ids collide or if an attacker manipulates conversation IDs.
  - _Fix:_ Ensure tenant isolation by validating ownership before mutating tenant-scoped rows. Options: (1) Persist tenantId in pendingReuseByConversation when creating the pending entry and include AND tenant_id = ${pendingTenantId} in the UPDATE WHERE clause; or (2) change markReviewReused to accept tenantId (or look up the review row first) and verify the review's tenant_id matches the caller before performing the UPDATE. Always include tenant_id constraints on UPDATE/DELETE for tenant-scoped tables.
- **server/channel-routing.ts:30** — listChannelRoutes SELECT joins personas (likely a tenant-scoped table) but does not constrain either channel_routes or personas by tenant_id. This can leak or mix personas from other tenants.
  - _Fix:_ Require a tenantId parameter for listChannelRoutes and add WHERE cr.tenant_id = ${tenantId} (and/or p.tenant_id = ${tenantId}). Ensure persona_name is read only after verifying p.tenant_id = tenantId. If channel_routes is changed to include tenant_id, include it in the JOIN/WHERE and add an index/unique constraint on (tenant_id, channel).
- **server/channel-routing.ts:63** — getPersonaForChannel selects persona_id from channel_routes without any tenant filter, returning persona ids that may belong to other tenants.
  - _Fix:_ Require a tenantId parameter and add WHERE tenant_id = ${tenantId} AND channel = ${channel} AND enabled = true. If persona ownership must be enforced, additionally verify the persona belongs to that tenant before returning it.
- **server/chat-engine.ts:633** — Query reads from `compaction_archives` by conversation_id without constraining tenant_id. This can expose another tenant's archive data.
  - _Fix:_ Constrain the query to the caller's tenant, e.g. `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}`, or use Drizzle `.where(eq(compactionArchives.tenantId, tenantId))`. Ensure every read against tenant-scoped tables includes the tenant filter.
- **server/chat-engine.ts:651** — Selecting `role, content, created_at` from `messages` by conversation_id omits a tenant_id filter, risking cross-tenant message disclosure.
  - _Fix:_ Add tenant scoping to the query: `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}`, or use Drizzle `.where(eq(messages.tenantId, tenantId))`. Audit all `messages` reads to ensure the tenant is always enforced in SQL.
- **server/chat-engine.ts:768** — LATERAL subqueries selecting from `messages` (e.g. `(SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ...)`) omit tenant_id constraints. Even though the outer `conversations` rows are tenant-scoped, the inner `messages` selects are unscoped at SQL level and can leak data if assumptions change.
  - _Fix:_ Include tenant scoping in the subqueries, e.g. `(SELECT content FROM messages WHERE conversation_id = c.id AND tenant_id = ${tenantId} AND role = 'user' ...)` (and similarly for assistant). Prefer explicit `AND tenant_id = ${tenantId}` or Drizzle `eq(messages.tenantId, tenantId)` in every subselect to guarantee isolation.
- **server/chat-engine.ts:4105** — Call to recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) uses conversationId but does not pass tenantId or perform an explicit ownership check before invoking. If this function reads/writes tenant-scoped tables it can violate tenant isolation.
  - _Fix:_ Include tenantId when calling recordToolCallForStuckDetection, or ensure the function immediately verifies that the conversation belongs to the tenant before any DB queries/updates. Any SQL touching tenant-scoped tables must constrain by tenant_id.
- **server/chat-engine.ts:4092** — Calls to recordToolExecution(conversationId, toolName, ...) are made without passing tenantId or performing an ownership check on conversationId. If recordToolExecution writes to tenant-scoped tables (tool execution logs / identifiers) those writes may lack a tenant_id constraint.
  - _Fix:_ Pass tenantId into recordToolExecution (or validate ownership first). Ensure the implementation includes WHERE tenant_id = tenantId (or Drizzle .where(eq(..., tenantId))) on all reads/updates/inserts, and that any INSERT explicitly sets tenant_id (NOT NULL columns).
- **server/chat-engine.ts:4227** — Call to getOrCreateTaskState(conversationId, ...) uses conversationId but does not pass tenantId or validate that the conversation belongs to the caller's tenant before creating/reading task state. This can allow cross-tenant access or writes without tenant_id filtering.
  - _Fix:_ Pass tenantId into getOrCreateTaskState or perform an explicit ownership check of conversationId before calling. Ensure getOrCreateTaskState enforces tenant scoping on any DB SELECT/INSERT/UPDATE (WHERE tenant_id = tenantId and INSERTs set tenant_id explicitly).
- **server/chat-engine.ts:4421** — storage.attachCitations(messageId, collectedCitations) is invoked without providing tenantId. If attachCitations writes citation rows into tenant-scoped tables it may do so without a tenant_id constraint or without validating the message ownership.
  - _Fix:_ Call storage.attachCitations with the tenantId (e.g. storage.attachCitations(messageId, collectedCitations, tenantId)) or ensure attachCitations resolves/validates the message belongs to the tenant and includes tenant_id in any INSERT/UPDATE/SELECT. All DB operations on tenant-scoped tables must include tenant_id filters and explicit tenant_id values for INSERTs.
- **server/commitments.ts:135** — Unscoped SELECT from tenant-scoped table `commitments`: the heartbeat scanner runs `SELECT id, tenant_id AS "tenantId", description, due_at ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...` with no tenant_id filter. This reads tenant-scoped rows across all tenants (including free-text `description`).
  - _Fix:_ If this is intended only for platform-admin fans-in, make that explicit (restrict the API to an admin role) and remove sensitive free-text fields from the SELECT (do not fetch `description`), or persist a safe, redacted audit view for admin use. If this should be per-tenant, add `AND tenant_id = $tenantId` and run per-tenant. Always avoid selecting tenant free-text without strong admin justification and explicit access controls.
- **server/deep-interview.ts:337** — INSERT INTO project_notes does not provide a tenant_id when writing a tenant-scoped artifact. The INSERT builds content and selects p.id from projects but does not set tenant_id explicitly, relying on the project relationship rather than populating the tenant column.
  - _Fix:_ Include tenant_id in the INSERT so the row is explicitly scoped, e.g. `INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() FROM ... WHERE p.tenant_id = ${state.tenantId}`. Ensure tenant_id is passed as a parameter and not inferred implicitly.
- **server/data-protection.ts:403** — SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} is executed without scoping by tenant_id. This allows a cross-tenant existence/count oracle (and may leak or act on another tenant's rows) because the query lacks a tenant predicate.
  - _Fix:_ Scope message queries to the conversation's tenant, e.g. `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}` (or pass the explicit tenantId). Always include tenant_id on SELECTs against tenant-scoped tables.
- **server/delivery-pipeline.ts:255** — updateDeliveryLog performs an UPDATE on delivery_logs without scoping by tenant_id (WHERE only matches id). This allows a caller that can trigger updateDeliveryLog to modify another tenant's delivery row.
  - _Fix:_ Add a tenant filter to the UPDATE (e.g. accept tenantId in the function and use .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId))) ) or otherwise ensure the caller-provided id is verified to belong to the caller's tenant before updating.
- **server/delivery-pipeline.ts:1142** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with NO tenant_id constraint. Returning a delivery by payment id without tenant scoping can expose another tenant's delivery row.
  - _Fix:_ Require and use a tenantId parameter (or otherwise resolve/validate the tenant) and add a WHERE tenant_id = ${tenantId} clause to the query, or document and enforce a safe global policy if lookup must remain global (prefer not to).
- **server/dfy-intake.ts:93** — submitDfyIntake updates dfy_intake_forms (responses/status/submittedAt) using WHERE eq(dfyIntakeForms.id, form.id) with NO tenant_id constraint. This allows updating a form row without verifying it belongs to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId))) or otherwise confirm the form belongs to the expected tenant before persisting responses.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete chunks across tenants that share the same collection_id/doc_path (or if IDs are reused/mis-specified).
  - _Fix:_ Add tenant scoping to the DELETE: include AND tenant_id = ${tenantId} (and ensure tenantId is passed into addDocument and validated against the collection ownership before destructive operations).
- **server/doc-collections.ts:294** — generateCollectionEmbeddings performs UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} without constraining tenant_id. Updating by id alone can modify rows belonging to another tenant if an id is mis-attributed or leaked.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or perform the update via a parameterized query that enforces the tenant context retrieved earlier.
- **server/email.ts:471** — startInboxPolling / pollSharedInbox checks for existing inbox_messages with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 (no tenant_id). This lookup can collide across tenants and cause processing/visibility mixups.
  - _Fix:_ When checking for an existing message, scope by tenant (or by inboxId mapped to tenant) – e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1. Resolve tenantId from the inbox or from message metadata before querying.
- **server/email.ts:515** — backfillEmptyBodies performs SELECT id, message_id FROM inbox_messages WHERE ... ORDER BY id without tenant scoping. A background job selecting across inbox_messages may read rows for all tenants without tenant restriction.
  - _Fix:_ Either iterate per-tenant (query by tenant_id) or otherwise restrict the query to an intended operational tenant/context. Ensure background maintenance queries include tenant_id or are explicitly documented as platform-level and safe.
- **server/email.ts:528** — backfillEmptyBodies updates inbox_messages with UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} but does not include tenant_id in the WHERE. That allows writing to another tenant's message row if row.id was not tenant-validated.
  - _Fix:_ Include tenant scoping on the UPDATE: WHERE id = ${row.id} AND tenant_id = ${tenantId}. Ensure row.tenant_id is known/validated before performing updates (e.g. obtain tenantId in the same query that selected rows).
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' without any tenant_id filter (reads all tenants during boot rehydration).
  - _Fix:_ Restrict the query to the intended tenant(s). Either run a per-tenant rehydration (WHERE tenant_id = <tenant>) or otherwise apply the current AsyncLocal/caller tenant context to the WHERE clause so rows from other tenants are not read.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint (write not scoped to tenant).
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or use an explicit eq(table.tenantId, tenantId) predicate). Always constrain writes to the caller's tenant.
- **server/felix-loop.ts:689** — Error-path UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint (write not scoped to tenant).
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) so error logging cannot update another tenant's run rows. Ensure all UPDATE/DELETE statements against tenant-scoped tables include tenant_id checks.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' using WHERE id = ${projectId} with no tenant_id constraint (reads project across tenants).
  - _Fix:_ Constrain the SELECT by tenant, e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId} (or validate caller tenant via assertProjectInTenant before running the query). Every read of tenant-scoped tables must include tenant_id filtering.
- **server/google-drive.ts:560** — UPDATE on tenant-scoped table 'projects' uses WHERE id = ${projectId} with no tenant_id constraint (write not scoped to tenant).
  - _Fix:_ Include tenant scoping in the UPDATE: add AND tenant_id = ${tenantId} to the WHERE clause (or use an equivalent Drizzle eq on the projects.tenantId column). Never update tenant-scoped rows without confirming ownership in the WHERE clause.
- **server/google-drive.ts:826** — SELECT from tenant-scoped table 'projects' (resolving project by id) without a tenant_id filter in uploadAndShare — the code later checks tenant ownership but the initial read is unscoped.
  - _Fix:_ Add tenant scoping to the SELECT (WHERE id = ${projectId} AND tenant_id = ${callerTenantId}) or otherwise fetch via an assert/helper that enforces tenant ownership before using project data. Avoid any unscoped reads of tenant-scoped tables.
- **server/google-drive.ts:1066** — SELECT from tenant-scoped table 'projects' to fetch driveFolderId uses WHERE id = ${projectId} with no tenant_id constraint (read not scoped to tenant) in the Drive search fallback path.
  - _Fix:_ Include tenant scoping on this SELECT (AND p.tenant_id = ${params.tenantId}) or perform the lookup via a helper that asserts project ownership earlier. All reads against tenant-scoped tables must include tenant_id in the WHERE clause.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` without scoping by tenant_id.
  - _Fix:_ If `code_proposals` is tenant-scoped, restrict reads to the caller/admin context by adding a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or explicitly document/require admin privileges. Do not perform tenant-wide scans from a non-admin context. Consider adding an explicit tenantId parameter to runFullAudit and pass it through to archiveStaleProposals, or otherwise ensure this code only runs under a vetted admin identity before querying across tenants.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` without scoping by tenant_id. This reads tasks across all tenants.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, add AND tenant_id = ${tenantId} (or otherwise pass a tenantId) to the WHERE clause to limit results to the intended tenant. If the intent is a global/system audit, require explicit admin context and document it; otherwise scope to the caller's tenant.
- **server/heartbeat.ts:53** — SELECT from research_schedules omits any tenant_id constraint (SELECT * FROM research_schedules ...). This reads tenant-scoped rows across all tenants without an application-enforced tenant filter.
  - _Fix:_ Either restrict the query to a specific tenant (WHERE tenant_id = ${...}) or iterate per-tenant explicitly (fetch tenant ids then query per-tenant). Alternatively use a storage helper that accepts ALL_TENANTS intentionally and documents that behavior; do NOT issue an unscoped SELECT against a tenant-scoped table from a multi-tenant process without an explicit tenant filter or explicit system-intent justification.
- **server/heartbeat.ts:103** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} has no tenant_id constraint. This updates a tenant-scoped row (sched has tenant_id) without constraining to the tenant.
  - _Fix:_ Include tenant qualification in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or perform the update through a storage helper that enforces/validates the tenant context.
- **server/heartbeat.ts:108** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} is missing a tenant filter. Writing to a tenant-scoped table must assert the tenant.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or use a tenant-aware storage API (or assertTenantContext) before updating to prevent cross-tenant updates.
- **server/heartbeat.ts:113** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} lacks tenant qualification. This can advance/modify another tenant's schedule if ids are misaligned or leak occurs.
  - _Fix:_ Constrain updates with tenant_id (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or prefer a storage-layer update that enforces tenant ownership checks.
- **server/heartbeat.ts:385** — SELECT * FROM event_log WHERE status = 'pending' ORDER BY ... LIMIT 10 executes without tenant_id filtering. This reads pending events across all tenants into a per-event processing loop that assumes tenant_id later — it violates explicit tenant-scoping at read time.
  - _Fix:_ Filter the SELECT by tenant_id or process events per-tenant (e.g., fetch active tenant ids and query event_log WHERE tenant_id = ${tid} AND status = 'pending'), or use a storage API that returns pending events per-tenant. Never perform a global unscoped SELECT on a tenant-scoped table.
- **server/heartbeat.ts:1670** — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 has no tenant_id constraint. This causes cross-tenant behavior: a successful run in tenant A could prevent runs for tenant B.
  - _Fix:_ Restrict the query to the task's tenant (e.g., add WHERE tenant_id = ${tenantId}) or use a tenant-aware helper (assertTenantContext or storage.getHeartbeatLogs(tenantId,...)). Ensure per-tenant heartbeat_log lookups do not mix tenants.
- **server/heartbeat.ts:273** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} updates a tenant-scoped table using only id with no tenant_id constraint.
  - _Fix:_ Add tenant qualification to the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call a tenant-aware storage/update helper that enforces the caller's tenant context.
- **server/heartbeat.ts:277** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} similarly lacks a tenant filter and may modify another tenant's row.
  - _Fix:_ Include tenant_id in the WHERE or use storage.updateHeartbeatTask (or assertTenantContext) to ensure the update only affects the intended tenant's row.
- **server/heartbeat.ts:2764** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without constraining tenant_id. This writes approval_status on a heartbeat_tasks row without verifying its tenant ownership.
  - _Fix:_ Constrain the update (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the change via a tenant-aware storage API that validates the tenant on update. Avoid direct unscoped UPDATEs to tenant-scoped tables.
- **server/heartbeat.ts:3216** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} again omits tenant_id. Updating heartbeat_tasks without tenant scoping can affect other tenants.
  - _Fix:_ Add tenant constraint (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage layer which enforces tenant ownership; ensure all direct SQL UPDATE/DELETE on tenant-scoped tables include tenant_id checks.
- **server/job-queue.ts:98** — claimDueJobs performs UPDATE/SELECT on agent_jobs without constraining by tenant_id. This atomically claims jobs across all tenants, violating per-tenant scoping invariant.
  - _Fix:_ Either scope claims to a single tenant (pass & use tenantId in the WHERE clause) or explicitly document and harden the global queue design (ensure only a trusted system worker runs this and add authorization checks). Prefer adding a tenant parameter and adding AND tenant_id = ${tenantId} to the inner SELECT/outer UPDATE to enforce tenant isolation.
- **server/job-queue.ts:134** — reclaimExpiredLeases updates agent_jobs (marking terminal or requeued) without any tenant_id constraint, affecting jobs for all tenants in bulk.
  - _Fix:_ Restrict reclaim operations by tenant (iterate per-tenant or accept tenantId) or partition work per-tenant. At minimum include tenant_id = ${tenantId} in the WHERE clauses or perform tenant-scoped queries to avoid touching other tenants' jobs.
- **server/job-queue.ts:220** — failJob queries and updates agent_jobs (SELECT max_attempts WHERE id=..., UPDATE ... WHERE id=...) without scoping by tenant_id. This allows cross-tenant mutation/poisoning of job rows.
  - _Fix:_ Pass/derive tenantId and include AND tenant_id = ${tenantId} on the SELECT and all subsequent UPDATEs, or otherwise validate the job's tenant before mutating it. Avoid relying solely on id/attempts for fencing without tenant checks.
- **server/job-queue.ts:371** — cancelPendingJobsForTenant builds a text[] array literal via string interpolation (excludeLiteral = `{...}`) and injects it into SQL. Although sanitized, this is string-interpolated SQL using user-controlled input (opts.excludeKinds) and risks SQL injection / isolation mistakes.
  - _Fix:_ Use parameterized arrays rather than constructing a Postgres array literal string. Pass the excludeKinds array as a parameter and use SQL like AND ( ${exclude.length ? sql`NOT (kind = ANY(${exclude}))` : sql`TRUE`} ) or equivalent Drizzle parameterized array support. Avoid manual string interpolation of SQL with user-derived values.
- **server/knowledge-graph.ts:198** — loadDurableTriples issues an UPDATE using id = ANY(${`{${ids.join(",")}}`}::int[]) where the array literal is built by string-interpolating ids into the SQL. This is string-interpolated SQL with runtime-derived values (ids).
  - _Fix:_ Use a parameterized array instead of composing a '{...}' literal. Pass ids as a parameter (e.g. AND id = ANY(${ids}) or use the driver's proper array binding) so the DB driver can safely parameterize values and preserve tenant scoping (tenant_id = ${tenantId} already present).
- **server/lib/bwb-job-progress.ts:74** — scope() allows omitting tenant predicate: it constructs WHERE clauses that may only include jobId when env BWB_TENANT_ID is missing, enabling subsequent reads/writes to video_jobs to run without tenant_id filtering.
  - _Fix:_ Require an explicit tenantId for scope() (do not fall back to jobId-only writes). Fail closed when tenantId is not supplied (throw or return no-op) and update all callers to pass tenantId (or derive currentTenantId()) so every SELECT/UPDATE/DELETE against video_jobs includes a tenant_id predicate.
- **server/lib/bwb-job-progress.ts:141** — UPDATE on tenant-scoped table video_jobs performed via .where(scope(jobId,...)) — scope() may omit tenant_id when BWB_TENANT_ID is not set, allowing cross-tenant updates pinned only to jobId.
  - _Fix:_ Always include the tenant predicate in the UPDATE (e.g. .where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId), ...))). Validate/require tenantId at the API boundary and fail the write when tenant is unknown.
- **server/lib/bwb-job-progress.ts:169** — UPDATE on tenant-scoped table video_jobs (updateBwbChapters) uses .where(scope(jobId,...)) which can omit tenant_id, permitting cross-tenant modification based solely on jobId when env BWB_TENANT_ID is missing.
  - _Fix:_ Change updateBwbChapters to accept/require tenantId and include eq(videoJobs.tenantId, tenantId) in the WHERE clause. If tenantId is not available, fail closed instead of writing without tenant scoping.
- **server/lib/bwb-job-progress.ts:200** — SELECT from tenant-scoped table video_jobs uses .where(scope(jobId)) — scope() can omit tenant_id, so this read can return another tenant's row when BWB_TENANT_ID is not present.
  - _Fix:_ Require tenantId for this lookup and add an explicit tenant_id predicate (WHERE jobId = $ AND tenant_id = $). Do not perform tenant-scoped reads based only on jobId; fail when tenant is unknown.
- **server/lib/bwb-job-progress.ts:211** — UPDATE on tenant-scoped table video_jobs uses .where(scope(jobId)) — scope() may omit tenant_id, allowing an update that affects rows across tenants when env BWB_TENANT_ID is not set.
  - _Fix:_ Include an explicit tenant predicate in the UPDATE (.where(and(eq(videoJobs.jobId, jobId), eq(videoJobs.tenantId, tenantId)))) and require tenantId be provided by the caller or derived securely; fail closed if not available.
- **server/lib/bwb-job-progress.ts:230** — UPDATE (heartbeat) against tenant-scoped table video_jobs uses .where(scope(jobId,...)) — scope() can drop tenant_id, permitting cross-tenant heartbeat updates when BWB_TENANT_ID is missing.
  - _Fix:_ Require tenantId for bumpBwbHeartbeat and include eq(videoJobs.tenantId, tenantId) in the WHERE clause. If tenant context is not available, refuse to perform the write to avoid touching other tenants' rows.
- **server/lib/bwb-job-progress.ts:245** — UPDATE marking job failed writes to tenant-scoped table video_jobs via .where(scope(jobId,...)); scope() may omit tenant_id, enabling a cross-tenant state change (fail/phase) when BWB_TENANT_ID is not present.
  - _Fix:_ Make failBwbJob require tenantId and ensure the UPDATE includes eq(videoJobs.tenantId, tenantId) in the WHERE. Remove the fallback behavior that writes with jobId-only scoping; fail closed if tenant is unknown.
- **server/lib/ideabrowser-backfill.ts:49** — Query reads the tenant-scoped `projects` table across ALL tenants (SELECT tenant_id, count(*) ... FROM projects WHERE tags && ...) without constraining to the caller's tenant(s). This exposes/aggregates data for tenants other than the intended one and violates the invariant that every read against a tenant-scoped table must be constrained by tenant_id.
  - _Fix:_ Restrict the aggregation to only the relevant tenant(s) from the snapshot instead of querying all tenants. For example, compute the list of snapshot tenant ids and add a WHERE tenant_id = ANY(${tenantIdArray}) clause (binding the array as a parameter), or run a per-tenant count for each snapshot tenant. Ensure every SELECT/UPDATE/DELETE against tenant-scoped tables includes an explicit tenant_id filter.
- **server/lib/inbox-ingest.ts:507** — UPDATE to tenant-scoped table inbox_classifications lacks a tenant_id constraint (updates by inbox_message_id only). This can modify/overwrite another tenant's row with the same inbox_message_id.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. add "AND tenant_id = ${tenantId}" to the WHERE clause (or use the tenant from the previously-returned row). Ensure every write to tenant-scoped tables includes the tenant_id filter.
- **server/lib/mention-injection.ts:121** — SELECT from channel_messages LEFT JOIN personas joins on persona id only (ON p.id = cm.from_persona_id) but does not constrain the joined personas row to the same tenant. This can leak/read persona.name from another tenant.
  - _Fix:_ Constrain the join so persona rows are tenant-scoped, for example: LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = cm.tenant_id (or include "AND p.tenant_id = ${params.tenantId}"). Ensure all JOINs to tenant-scoped tables enforce tenant equality.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id filter (CTE selects due rows across all tenants and UPDATE matches only on id). This reads/updates tenant-scoped rows across tenants without per-tenant constraint.
  - _Fix:_ Avoid implicit cross-tenant scans/updates. Either run the runner per-tenant (pass a tenantId and add WHERE tenant_id = ${tenantId}), or include tenant_id in the CTE and in the UPDATE predicate (e.g. SELECT id, tenant_id FROM scheduled_posts ... WHERE tenant_id = ${tenantId} ...; UPDATE ... WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id). If the system must operate across tenants, make the cross-tenant intent explicit, restrict to admin-only code paths, and ensure every write still includes the owning tenant_id in the WHERE clause to prevent accidental cross-tenant side effects.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` does not filter by tenant_id. The query reads rows across all tenants (WHERE status = 'running' ... ) without tenant scoping.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId} AND status = 'running' ...) or, if the intent is an admin cross-tenant audit, make that explicit and ensure this code path is only callable by authorized admin contexts. Prefer per-tenant queries or return tenant_id with each row and ensure downstream logic treats tenant_id explicitly when taking any action.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` does not filter by tenant_id. The query reads rows across all tenants (WHERE status = 'in_progress' ... ) without tenant scoping.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId} AND status = 'in_progress' ...) or, if this is an intentional cross-tenant audit, restrict invocation to an admin-only path and ensure any follow-up actions include explicit tenant_id checks. Avoid unscoped reads of tenant-scoped tables in non-admin code paths.
- **server/lib/system-probes.ts:163** — db.select(...).from(messagesTable) is executed without a tenant_id WHERE clause (a raw count(*) on messages is run across all tenants). This reads tenant-scoped message rows globally without tenant scoping.
  - _Fix:_ If this probe must be per-tenant, add a tenant_id filter (WHERE tenant_id = ${tenantId}) or accept a tenantId argument and scope queries accordingly. If it is legitimately a global admin probe, make that explicit, restrict access to admin contexts only, and ensure any usage that exposes results to non-admins is removed. Prefer explicit, audited cross-tenant operations rather than implicit unscoped queries.
- **server/memory-graph.ts:183** — refreshCategoryCount updates memory_categories without constraining by tenant_id. The UPDATE uses WHERE id = ${categoryId} only, allowing a caller to modify another tenant's category count by id.
  - _Fix:_ Include tenant context in the update. Either accept/derive tenantId and add AND tenant_id = ${tenantId} to the WHERE clause, or perform the count/update via a subquery/join that enforces tenant_id (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Prefer using the same tenant parameter used to select categories earlier.
- **server/memory-graph.ts:123** — classifyMemoryCategory (empty-categories branch) increments memory_categories.memory_count with an UPDATE that lacks a tenant_id constraint (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This can mutate another tenant's row by id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) and/or pass tenantId through this code path so the write is explicitly scoped. Alternatively, use a transaction that inserts/updates within the same tenant-scoped select to avoid cross-tenant effects.
- **server/memory-graph.ts:494** — categorizeExistingMemories updates memory_entries with WHERE id = ${mem.id} but omits tenant_id in the WHERE clause (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). Even though the rows were selected for a given tenant earlier, the UPDATE is unscoped and could affect a row id belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}), or call a storage helper that enforces tenant ownership (storage.updateMemoryEntry) passing tenantId to ensure the write cannot cross tenants.
- **server/memory-intelligence.ts:399** — extractTriples updates knowledge_triples rows by id only (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}) after selecting contradictions filtered by tenant_id. The subsequent UPDATE lacks a tenant_id check and could update another tenant's row if ids are reused or ownership differs.
  - _Fix:_ Scope the UPDATE by tenant as well (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}), or perform the update via a statement that includes tenant_id in the WHERE clause. Ensure every UPDATE/DELETE on tenant-scoped tables includes the tenant_id guard or uses storage helpers that enforce ownership.
- **server/minds-engine.ts:262** — checkDependenciesMet builds an ANY(...) array literal by joining ticket.dependsOn: `WHERE id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) AND tenant_id = ${tenantId}`. The dependsOn value originates from stored/earlier input and is used to compose SQL text rather than a parameter, risking injection or malformed queries and weakening isolation guarantees.
  - _Fix:_ Pass the dependsOn list as a parameterized array instead of interpolating into SQL text (e.g. WHERE id = ANY(${ticket.dependsOn}::int[])). Ensure dependsOn is normalized to an array of integers and/or validate each referenced ticket belongs to the same tenant before trusting it. Prefer using Drizzle/pg array binding helpers or JSONB storage to avoid manual literal construction.
- **server/minds-engine.ts:214** — createTicket constructs a Postgres array literal for depends_on by interpolating params.dependsOn: `${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}`. This injects caller-supplied data into SQL text rather than using a parameter, enabling SQL-injection / malformed-array risks and undermining tenant-scoped integrity if misused.
  - _Fix:_ Use parameterized array insertion (e.g. pass dependsOn as a native array parameter or JSONB) instead of composing a textual array literal. Example: INSERT ... depends_on = ${params.dependsOn ?? null} with the DB driver/ORM binding an int[] or use JSONB and validate that every referenced ticket id belongs to the provided tenant before inserting.
- **server/minerva-planner.ts:549** — getPlan allows calling code to omit tenantId and performs `SELECT * FROM plans WHERE id = ${planId}` with NO tenant_id constraint. This SELECT against a tenant-scoped table can return cross-tenant data if invoked without a tenant filter (IDOR risk).
  - _Fix:_ Require tenantId for external callers or always include tenant scoping in the query. If internal callers legitimately omit tenantId, enforce a clear internal-only boundary (private/internal API) and audit callers. Prefer a single explicit API surface that always passes and applies tenant_id in the WHERE clause for tenant-scoped tables.
- **server/orchestrator-ledger.ts:458** — UPDATE against tenant-scoped table `memory_entries` does not include a tenant_id constraint: `UPDATE memory_entries SET ... WHERE id = ${row.id}`. Per the tenant-isolation invariant, every write to a tenant-scoped table must constrain by tenant_id.
  - _Fix:_ Include the tenant filter in the UPDATE WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) so the update cannot affect rows belonging to another tenant.
- **server/pace-control.ts:85** — Query against tenant-scoped table `heartbeat_logs` is not constrained by tenant_id: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}`. The function does not accept a tenantId and aggregates across all tenants.
  - _Fix:_ Scope these queries to a specific tenant. Add a tenantId parameter to checkPace (and callers) and include `AND tenant_id = ${tenantId}` (or equivalent Drizzle .where(eq(...))) in the WHERE clause to enforce tenant isolation.
- **server/pace-control.ts:140** — getPaceSnapshot queries `heartbeat_logs` without tenant scoping: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...`. This returns global/mixed-tenant data rather than per-tenant and violates the tenant-isolation requirement.
  - _Fix:_ If the snapshot is intended to be per-tenant, accept a tenantId and add `WHERE tenant_id = ${tenantId}` to these queries. If global aggregation is deliberate, document/validate that `heartbeat_logs` is a genuinely global table; otherwise constrain by tenant.
- **server/persona-export.ts:33** — Read of persona by id without tenant constraint: `SELECT * FROM personas WHERE id = ${personaId}`. This reads a tenant-scoped row (persona) using a foreign id with no ownership/tenant verification (rule #3).
  - _Fix:_ Verify ownership by adding tenant scoping to the query (e.g. `SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}`) and handle the case where no row is returned (unauthorized/missing). Ensure callers provide tenantId and/or perform an explicit ownership check before exporting.
- **server/persona-export.ts:39** — Query against `skills` lacks tenant scoping: `SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ...`. Without a tenant_id constraint this may return skills from other tenants.
  - _Fix:_ Restrict tenant-specific skills with tenant_id = ${tenantId} while still allowing global skills. For example: `WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR (persona_id IS NULL)` or use Drizzle .where(and(...)) to make intent explicit and enforce tenant isolation.
- **server/process-governor.ts:121** — Subquery reads heartbeat_logs without constraining tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads a tenant-scoped table without tenant isolation.
  - _Fix:_ Include tenant scoping in the subquery, e.g. AND tenant_id = ${tenantId} (or AND tenant_id = ht.tenant_id) so logs are filtered to the caller's tenant, or join on task id with an explicit tenant filter.
- **server/process-governor.ts:155** — Subquery counting heartbeat_tasks for persona pending_tasks omits tenant restriction: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true). This reads a tenant-scoped table without tenant isolation.
  - _Fix:_ Add tenant constraint to the subquery (e.g. AND tenant_id = ${tenantId} or AND tenant_id = ad.tenant_id) so pending task counts are restricted to the caller's tenant.
- **server/process-governor.ts:199** — Query over heartbeat_logs to compute AVG(duration_ms) lacks tenant filtering (WHERE created_at > ... AND duration_ms IS NOT NULL AND status = 'success'). This reads a tenant-scoped table without tenant isolation.
  - _Fix:_ Restrict the query by tenant_id = ${tenantId} (or add an appropriate join) to ensure metrics are computed only for the requesting tenant.
- **server/process-governor.ts:240** — cascading_failures query reads heartbeat_logs without a tenant filter (COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE ...). This can count other tenants' agents.
  - _Fix:_ Add a tenant_id = ${tenantId} predicate to the WHERE clause (or otherwise scope by tenant) to enforce tenant isolation.
- **server/process-governor.ts:585** — In disable_task the subselects counting heartbeat_logs (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id AND ...) do not include tenant_id, so they read tenant-scoped heartbeat_logs without tenant constraint.
  - _Fix:_ Constrain those subselects by tenant_id (e.g. AND tenant_id = ${tenantId} or AND tenant_id = ht.tenant_id) to ensure logs belong to the tenant of the task being evaluated.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates by id only without scoping to tenant_id. Because ids may collide across tenants, this can modify another tenant's task.
  - _Fix:_ Require tenant scoping in mutating statements: include AND tenant_id = ${tenantId} in the WHERE clause, or verify the task's tenant prior to updating.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} performs a mutation by id alone with no tenant constraint. This can affect agent_desks belonging to other tenants with the same id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or otherwise verify desk belongs to the caller's tenant before applying updates.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates a rule by id without verifying tenant_id. This allows cross-tenant mutation if ids collide.
  - _Fix:_ Scope the UPDATE to the caller's tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or verify ownership before updating trigger counts.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads the projects table by id with no tenant constraint. Projects are tenant-scoped so this can expose another tenant's project data.
  - _Fix:_ Require a tenantId parameter and include WHERE tenant_id = ${tenantId} AND id = ${projectId}, or verify the project's tenant ownership before reading or using the project data.
- **server/project-brain.ts:332** — Backfill query selects DISTINCT p.id, p.name FROM projects JOIN conversations ... and checks messages without any tenant scoping. Projects/conversations/messages are tenant-scoped but no tenant_id filters are used.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId} and filter messages by tenant_id) or run the backfill per-tenant and require tenant context before reading these tables.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping. Conversations are tenant-scoped and must be filtered by tenant_id.
  - _Fix:_ Include tenant filtering (e.g. AND c.tenant_id = ${tenantId}) or derive tenantId from the project and verify conversation ownership before reading.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant restriction. Messages are tenant-scoped (conversation-scoped) and must be restricted to the caller's tenant.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or validate the conversation belongs to the tenant) before selecting messages.
- **server/proposal-verifier.ts:33** — SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id with no tenant constraint. code_proposals is tenant-scoped and this may expose or operate on another tenant's proposals.
  - _Fix:_ Require tenant context and include AND tenant_id = ${tenantId} (or fetch by id after verifying ownership) to ensure only proposals belonging to the caller's tenant are read.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at / marking race outcome) updates a tenant-scoped table by id without tenant filtering.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or verify the proposal's tenant before mutating it.
- **server/proposal-verifier.ts:246** — persist() issues UPDATE code_proposals SET verification_status = ${status} ... WHERE id = ${proposalId} without a tenant_id constraint. This mutates a tenant-scoped row by id only.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (persist should accept/derive tenantId) or validate proposal ownership prior to updating verification columns.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift the UPDATE code_proposals SET status='rejected', ... WHERE id = ${proposalId} is executed with no tenant constraint; this mutates tenant-scoped rows without verifying ownership.
  - _Fix:_ Scope the UPDATE to tenant_id = ${tenantId} (pass tenantId into attemptRebaseOnDrift or look up and validate proposal tenant) before writing verification/status fields.
- **server/proposal-verifier.ts:396** — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (mark 'hallucinated' outcome) updates code_proposals without tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or validate proposal ownership prior to persisting validation changes.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diff back to the proposal but lacks tenant filtering on the UPDATE.
  - _Fix:_ Ensure attemptRebaseOnDrift and callers have tenant context and perform the UPDATE with WHERE id = ${proposalId} AND tenant_id = ${tenantId} (or validate ownership) to prevent cross-tenant writes.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` is performed without a tenant_id filter (SELECT id,title,content,tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). Unscoped reads of tenant data increase risk of accidental cross-tenant access.
  - _Fix:_ If the code genuinely needs a global scan (cron/system use), make that explicit and auditable (e.g. centralize the global-scanning code and document/guard it). Otherwise restrict the SELECT by tenant_id (or call the existing listScheduledMessages API with the explicit system/global flag and then enforce per-row tenant-scoped writes). Avoid unscoped reads unless strictly necessary and ensure any subsequent writes include tenant constraints.
- **server/routes.ts:167** — Startup restoreUploadsFromDb reads file_storage rows without scoping by tenant (db.select(...).from(fileStorage).where(like(mimeType,'image/%'), isNull(storageKey))). This pulls raw file data for all tenants into the process and can lead to cross-tenant data exposure.
  - _Fix:_ Scope the query by tenant (e.g. add eq(fileStorage.tenantId, tenantId) or iterate per-tenant) or run the restore as an explicit admin-only per-tenant job. If the operation must process all tenants, ensure it runs under an explicit platform-admin context with audit/logging and avoids writing tenant data to a shared, public disk path.
- **server/routes.ts:4342** — Raw INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT: INSERT INTO project_notes (project_id, note, author) VALUES (...) — tenant_id is not set, and there is no DB row-level security or FK cascade to enforce tenancy.
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. VALUES (${projId}, ${snapshot}, ${'system:context-guard'}, ${tenantId}) or INSERT INTO project_notes (tenant_id, project_id, note, author) ...). Alternatively, use a storage layer function that requires/validates tenantId and fails if ownership is not proven. Ensure tenant_id is NOT NULL on the row and validate the project belongs to the tenant before inserting.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` omits tenant_id in the WHERE clause (`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`). This relies on a prior SELECT for ownership and is vulnerable to TOCTOU / accidental cross-tenant mutation.
  - _Fix:_ Include the tenant predicate in the UPDATE to enforce ownership at the DB layer, e.g. `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`. Prefer single-statement guarded updates rather than relying solely on a prior SELECT for authorization.
- **server/routes/public-chat.ts:278** — Handler defaults conv.tenantId to ADMIN_TENANT_ID (convTenantId = conv.tenantId ?? ADMIN_TENANT_ID) before persisting messages. If conv.tenantId is absent or malformed this will write messages under the admin tenant, violating tenant isolation and potentially causing cross-tenant data leakage.
  - _Fix:_ Do NOT default to ADMIN_TENANT_ID. Require an explicit tenant id derived from the authenticated/validated public-chat tenant (the tenant resolved from the token/slug) and validate that the conversation's tenant matches it. If conv.tenantId is missing or mismatched, reject the request (4xx) rather than falling back to a different tenant. Example fixes: use const convTenantId = conv.tenantId; if (!convTenantId || convTenantId !== tenant.id) return res.status(404).json({ error: 'Conversation not found' }); then call storage.createMessage with that convTenantId.
- **server/sculptor.ts:106** — UPDATE against tenant-scoped table `sculptor_sessions` uses WHERE id = ${session.id} with NO tenant_id constraint.
  - _Fix:_ Include the caller's tenant in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) so writes cannot affect rows belonging to other tenants.
- **server/sculptor.ts:112** — UPDATE against tenant-scoped table `sculptor_sessions` sets conversation_id but omits tenant_id in the WHERE clause (WHERE id = ${session.id} only).
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or session.tenantId) to enforce tenant isolation.
- **server/sculptor.ts:167** — UPDATE against tenant-scoped table `sculptor_sessions` (monitorSession) uses WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Fix:_ Add the tenant filter for the running monitor (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) so the periodic updater cannot modify other tenants' rows.
- **server/sculptor.ts:358** — UPDATE against tenant-scoped table `sculptor_sessions` (writing review_result) uses WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise bind the write to the calling tenant) to ensure the review write is tenant-scoped.
- **server/second-opinion.ts:405** — UPDATE against tenant-scoped table `agent_cost_ledger` sets settled cost using WHERE id = ${reservationId} with NO tenant_id constraint.
  - _Fix:_ Also constrain the UPDATE by tenant_id (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). If settleFusionReservation lacks tenant context, change its signature to accept tenantId (or look up and verify the reservation's tenant) before performing the UPDATE.
- **server/second-opinion.ts:424** — UPDATE against tenant-scoped table `agent_cost_ledger` (releaseFusionReservation) uses WHERE id = ${reservationId} with NO tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or require/verify tenant context before releasing a reservation to avoid cross-tenant ledger mutations.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM tenant-scoped table `heartbeat_tasks` omits tenant_id — reads global counts across tenants.
  - _Fix:_ Scope queries to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) or otherwise document/justify cross-tenant intent. Prefer per-tenant operations to preserve tenant isolation.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has NO tenant_id constraint — will disable tasks across all tenants.
  - _Fix:_ Add a tenant_id filter (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or iterate per-tenant, so only the targeted tenant's heartbeat_tasks are modified.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id — reads across tenants.
  - _Fix:_ Apply tenant scoping (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) or otherwise restrict to intended tenant before making update decisions.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant_id, allowing cross-tenant modifications.
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or appropriate tenant param) in the WHERE clause, or perform tenant-scoped updates only.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) omits tenant_id — counts tasks across tenants.
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND ...) so subsequent actions operate only within that tenant's data.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false ... AND enabled = true has NO tenant_id constraint — will disable matching tasks platform-wide.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or run the reconciler per-tenant to avoid mutating other tenants' rows.
- **server/seed.ts:2158** — SELECT from tenant-scoped table research_programs without restricting by tenant_id (SELECT id, name, model FROM research_programs WHERE is_active = true). This reads across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant. E.g. add WHERE tenant_id = ${tenantId} or use Drizzle: db.select().from(research_programs).where(eq(research_programs.tenantId, tenantId)). If the intent is to iterate per-tenant, enumerate tenants explicitly and run per-tenant queries.
- **server/seed.ts:36** — SELECT COUNT(*) FROM research_sessions without a tenant filter (SELECT COUNT(*) as count FROM research_sessions). This counts sessions across all tenants rather than the caller's tenant.
  - _Fix:_ Restrict the count to the tenant in question, e.g. SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId} or use Drizzle with eq(research_sessions.tenantId, tenantId). If global counts are intentional, document explicitly and ensure it's safe.
- **server/seed.ts:3906** — UPDATE on tenant-scoped table `memory_entries` is executed without constraining to a tenant (no WHERE tenant_id = ...). This will modify rows across all tenants.
  - _Fix:_ Add a tenant_id constraint to the WHERE clause (e.g. AND tenant_id = <systemTenantId> or loop per-tenant) or otherwise validate that this global sweep is intentional and safe. In Drizzle use .where(eq(memoryEntries.tenantId, tenantId)) or include tenant_id in the SQL template.
- **server/seed.ts:3932** — UPDATE on tenant-scoped table `conversations` (SET model = 'gpt-5') is performed without a tenant_id constraint, affecting all tenant rows.
  - _Fix:_ Restrict the UPDATE to the intended tenant(s) by adding WHERE tenant_id = <tenantId> (or use a tenant-scoped migration that iterates tenants). When using Drizzle prefer .where(eq(conversations.tenantId, tenantId)). If a global migration is required, make the operation explicit and gated (admin-only) with review.
- **server/seed.ts:3878** — DELETE FROM `heartbeat_tasks` performed without any tenant_id constraint. This will delete matching disabled tasks across all tenants.
  - _Fix:_ Constrain the DELETE to the target tenant(s) (e.g. WHERE tenant_id = <tenantId> AND ...). If the intention is to only affect system/admin tenant rows, include tenant_id = 1. Prefer Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) to ensure tenant isolation.
- **server/self-improvement.ts:170** — SELECT from tenant-scoped table `experiments` may run without a tenant filter. detectStagnation builds conditions with only category when tenantId is not provided, then executes db.select().from(experiments) with that single condition — allowing cross-tenant reads.
  - _Fix:_ Always constrain this query to a tenant. Require tenantId (throw if missing) or always add eq(experiments.tenantId, tenantId) to the WHERE. Avoid allowing a path that runs a global SELECT against a tenant-scoped table.
- **server/self-improvement.ts:521** — getExperimentHistory will run db.select().from(experiments) without any tenant filter when tenantId is omitted, returning cross-tenant data.
  - _Fix:_ Require tenantId for this API or default to scoping queries to the caller's tenant. If tenantId may be optional for legitimate reasons, ensure only explicitly global tables are queried without tenant constraints; experiments is tenant-scoped so always filter by tenant_id.
- **server/skillify.ts:91** — SELECT from messages omits tenant scoping: recentMsgs query uses only conversationId (eq(messages.conversationId, conversationId)) with no tenant_id constraint, risking cross-tenant data disclosure.
  - _Fix:_ Constrain the messages read to the caller's tenant, e.g. add AND tenant_id = ${tenantId} (or use eq(messages.tenantId, tenantId)) so the query cannot return another tenant's messages.
- **server/skillify.ts:201** — SELECT from messages in analyzeConversation omits tenant scoping: messages are queried by conversationId only (eq(messages.conversationId, conversationId)) with no tenant_id constraint.
  - _Fix:_ Add an explicit tenant filter to the messages query (AND tenant_id = ${tenantId} or eq(messages.tenantId, tenantId)), or otherwise ensure the messages table access is restricted to the conversation's tenant at the SQL level.
- **server/skillify.ts:221** — SELECT from personas lacks tenant constraint: personaRows lookup uses eq(personas.id, conv[0].personaId) but does not restrict by tenant_id, so persona data from other tenants could be disclosed.
  - _Fix:_ Constrain the personas query by tenant (e.g. AND personas.tenant_id = ${tenantId} or eq(personas.tenantId, tenantId)) or resolve persona ownership before reading to ensure tenant isolation.
- **server/storage.ts:242** — getConversationUnscoped performs a SELECT from tenant-scoped table `conversations` with NO tenant_id constraint.
  - _Fix:_ Avoid unscoped reads. Require the caller to pass an explicit tenantId (or an explicit ALL_TENANTS sentinel for admin callers) or return the tenantId alongside the row and force the caller to validate ownership. If this function must exist for internal orchestration, make the risk explicit in the signature and audit-callsites; otherwise replace with getConversation(id, tenantId).
- **server/storage.ts:298** — attachCitationMetadata SELECTs `messages` by id without scoping to tenant_id (reads tenant-scoped table without tenant constraint).
  - _Fix:_ Include tenant scoping in the query. Add a tenantId parameter (or derive it from the originating msgs and validate) and add WHERE tenant_id = ${tenantId}, or join via conversation -> tenant to enforce ownership before reading. Avoid any raw SELECT by id against tenant-scoped tables without an explicit tenant filter.
- **server/storage.ts:524** — createMemoryEntry inserts into tenant-scoped table `memory_entries` but does not validate that data.tenantId is present and a positive integer before the INSERT.
  - _Fix:_ Enforce/validate tenantId at the storage boundary (e.g., assertValidTenantId or explicit check) and throw if missing/invalid. Require callers to pass a positive integer tenantId; do not rely on DB not-null behavior as the only guard.
- **server/storage.ts:624** — evictOldestConversationFacts selects victim ids scoped by tenant but then updates conversation_facts using an IN(idList) without an explicit tenant_id constraint on the UPDATE. This writes a tenant-scoped table without a tenant condition.
  - _Fix:_ Include the tenant filter on the UPDATE (e.g., AND tenant_id = ${tenantId} or use WHERE id IN (...) AND tenant_id = ${tenantId}), or perform the eviction/update inside the same transaction/statement that derived the ids while preserving the tenant filter to ensure modifications cannot cross tenants.
- **server/storage.ts:805** — updateMemoryEmbedding updates `memory_entries` embedding using only eq(memory_entries.id, id) with NO tenant_id constraint.
  - _Fix:_ Require and validate tenantId for this operation and include a tenant-id predicate (e.g., .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId)))) or otherwise verify ownership before updating.
- **server/storage.ts:818** — updateKnowledgeEmbedding updates `agent_knowledge` embedding using only eq(agent_knowledge.id, id) with NO tenant_id constraint.
  - _Fix:_ Require/accept tenantId and include it in the WHERE clause, or verify the row belongs to the caller's tenant before updating (e.g., check tenant_id then perform update). Do not update tenant-scoped rows by id alone.
- **server/storage.ts:482** — deletePersona transaction updates tenant-scoped tables (`conversations`, `memory_entries`, `daily_notes`) by personaId without scoping those updates to a tenant_id. These updates can affect rows across tenants.
  - _Fix:_ When modifying tenant-scoped tables while cleaning up a persona, include tenant predicates or otherwise verify ownership before updating/deleting (e.g., condition on conversations.tenant_id, memory_entries.tenant_id, daily_notes.tenant_id). If persona is global but affects tenant data, the cleanup must iterate per-tenant or require explicit admin authorization and validation.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` filters only by name (eq(customTools.name, parsed.name)) and does not include tenant_id, allowing cross-tenant reads/matches.
  - _Fix:_ Include tenant scoping in the SELECT (and other lookups) by adding eq(customTools.tenantId, tenantId) or otherwise ensure lookups are always filtered to the caller's tenant.
- **server/tool-learning.ts:155** — SELECT custom_tools WHERE id = inserted.id is performed without constraining by tenant_id.
  - _Fix:_ When selecting rows from tenant-scoped tables by id, also include tenant_id in the WHERE clause (e.g. WHERE id = ${inserted.id} AND tenant_id = ${tenantId}) to ensure the row belongs to the expected tenant.
- **server/tools.ts:2890** — INSERT into project_conversations omits tenant_id. Writing to a tenant-scoped table without setting tenant_id allows cross-tenant contamination or violates the rule that tenant_id must be explicitly provided on insert.
  - _Fix:_ Include tenant_id in the INSERT column list and value list (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). Ensure the value is the caller's tenantId, not derived from untrusted input.
- **server/tools.ts:2994** — INSERT into project_files omits tenant_id. Tenant-scoped tables require tenant_id to be explicitly set on INSERT; omitting it breaks the invariant and can result in cross-tenant writes or DB errors if tenant_id is NOT NULL.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure uploaded_by and other fields are validated, and that tenantId used is the caller's trusted tenant context.
- **server/tools.ts:3008** — INSERT into project_notes omits tenant_id. Writing to a tenant-scoped table without tenant_id violates the requirement that every INSERT include the caller tenant and may allow cross-tenant contamination.
  - _Fix:_ Add tenant_id to the INSERT columns and values (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). Ensure the tenantId is the authenticated caller's tenant.
- **server/tools.ts:3032** — INSERT into project_conversations (link path) omits tenant_id. This is a write into a tenant-scoped table without explicitly setting tenant_id, violating the requirement to always set tenant_id on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). Prefer validating both project and conversation ownership and set tenant_id from the trusted caller context.
- **server/tools.ts:4552** — INSERT into tenant-related table `project_notes` does not include tenant_id. The INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) omits tenant_id, violating the rule that every INSERT into a tenant-scoped table must set tenant_id explicitly.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...)). Keep the assertProjectInTenant() check (defense-in-depth) but still write the tenant_id column so the row is unambiguously scoped at the DB level.
- **server/tools.ts:4556** — INSERT into tenant-related table `project_files` does not include tenant_id. The INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) omits tenant_id.
  - _Fix:_ Add tenant_id to the column list and pass the caller's tenant (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${projectId}, ...)). Retain the assertProjectInTenant() guard but ensure tenant_id is persisted on every row.
- **server/tools.ts:6405** — INSERT into tenant-related table `project_files` (audio upload path) omits tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) is missing tenant_id.
  - _Fix:_ Explicitly include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)) after confirming ownership with assertProjectInTenant(). This ensures DB rows are explicitly tenant-scoped.
- **server/tools.ts:6939** — INSERT into tenant-related table `project_files` (slideshow video) omits tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) does not set tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Keep the assertProjectInTenant() check but also write tenant_id to enforce tenant-scoped rows at insert time.
- **server/tools.ts:6939** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The statement db.execute(sql`INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`) omits tenant scoping so the row could be inserted without tenant ownership recorded, breaking tenant isolation.
  - _Fix:_ Include tenant_id explicitly in the INSERT and pass the caller's tenant (e.g. params._tenantId) as a parameter. Example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Keep the existing assertProjectInTenant check (or equivalent ownership verification) before the INSERT to ensure the tenant owns the referenced project.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table `browser_workflows` lacks a tenant_id constraint. The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` can modify a workflow belonging to another tenant if an id is reused or attacker-controlled.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. include the tenant in the WHERE clause: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); or use Drizzle's eq(table.tenantId, tenantId) so every write is explicitly tenant-scoped.
- **server/tools/domains/files/handlers.ts:907** — UPDATE on `projects` omits tenant scoping. The code calls `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` without constraining by tenant_id, allowing updates to projects outside the caller's tenant.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE. For example: db.execute(sql`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${ctx.tenantId}`); or use Drizzle's where(eq(projects.tenantId, ctx.tenantId)) so the write is guaranteed scoped to the caller's tenant.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE statement on tenant-scoped table `knowledge_triples` is missing an explicit tenant_id constraint. The code executes `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` without `AND tenant_id = ${tenantId}`.
  - _Fix:_ Constrain every UPDATE/DELETE to the caller's tenant. Change the query to include the tenant check, e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}` (or the equivalent Drizzle .where(eq(table.tenantId, tenantId))). Even if `row` was selected earlier with a tenant filter, add the tenant guard on the UPDATE for defense-in-depth and to satisfy the tenant-isolation invariant.
- **server/tools/domains/strategic-memory/handlers.ts:93** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint. Although id was discovered via a previous tenant-scoped SELECT, the UPDATE has no tenant guard and could affect rows across tenants (TOCTOU / misuse risk).
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) to ensure the update is constrained to the caller's tenant. Prefer single-statement upsert/dedup patterns or wrap select+update in a transaction and re-check tenant_id for safety.
- **server/tools/domains/strategic-memory/handlers.ts:148** — Bulk UPDATE on `memory_entries` uses WHERE id = ANY(${ids}) without tenant_id constraint. ids are derived from a prior tenant-scoped SELECT, but the UPDATE itself lacks tenant scoping and may update rows across tenants if ids are tampered with or in a race.
  - _Fix:_ Constrain the UPDATE by tenant: e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Alternatively, include tenant_id in the ids-to-update check or perform the update using a statement that joins against a tenant-scoped subquery so the DB enforces the tenant restriction in the same statement.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE on `memory_entries` performed with WHERE id = ${id} and missing tenant_id filter (record_strategic_win path). The prior SELECT ensured the id belonged to the tenant, but the UPDATE itself is not tenant-scoped, creating a risk.
  - _Fix:_ Add tenant scoping to the UPDATE statement (WHERE id = ${id} AND tenant_id = ${tid}). Prefer combining the dedup-detect-and-update into a single tenant-scoped DB statement or transaction to avoid TOCTOU and cross-tenant update risk.
- **server/video-job-runner.ts:208** — isCancelRequested queries videoJobs by jobId only (SELECT cancel_requested WHERE jobId = ...), with no tenant_id filter. Reading tenant-scoped state without verifying the caller/owner tenant can leak or allow cross-tenant influence.
  - _Fix:_ Require and enforce tenant context when reading the cancel flag. Change isCancelRequested to accept (jobId, tenantId) and query WHERE jobId = ... AND tenantId = ..., or lookup the job and verify its tenant matches the caller before returning cancel state. Avoid any reads from tenant-scoped tables that are not scoped by the owning tenant.

### MEDIUM (46)

- **server/agentic-features.ts:679** — Non-parameterized/string-interpolated SQL used to build a Postgres ANY array: sequence_id = ANY(${`{${seqIds.join(",")}}`}::int[]). Constructing array literal via string interpolation risks SQL/semantic issues and bypasses normal parameterization patterns.
  - _Fix:_ Use a parameterized array binding (e.g. sequence_id = ANY(${sql.array(seqIds, 'int')})) or pass the array as a parameter instead of building an SQL literal via string concatenation to avoid injection and maintain correct query parameterization.
- **server/auth.ts:1097** — DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} deletes expired reset tokens across all tenants (no tenant_id constraint).
  - _Fix:_ Run expiry cleanup per-tenant, or add an explicit tenant_id filter when intended. If global cleanup is required, ensure it runs in a platform-admin context with auditing/comment explaining the cross-tenant scope and why it's safe.
- **server/auth.ts:1206** — DELETE FROM auth_sessions WHERE expires_at < ${now} removes expired auth_sessions across all tenants (no tenant_id constraint).
  - _Fix:_ Limit session cleanup to a tenant when running in tenant context, or perform global cleanup only under a vetted platform-admin maintenance job. Prefer adding WHERE tenant_id = ${tenantId} when invoked per-tenant.
- **server/auth.ts:1216** — Unscoped DELETE FROM auth_sessions WHERE expires_at < ${now} in loadSessionsFromDb — removes expired sessions across tenants without tenant_id constraint.
  - _Fix:_ Avoid global deletions in general request code; either restrict the DELETE to a tenant_id or move global maintenance to an explicit admin/maintenance job with proper scoping and audit controls.
- **server/auth.ts:1219** — SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions reads all sessions across tenants (no tenant_id filter).
  - _Fix:_ If the intent is to load only sessions relevant to this process (e.g. for a particular tenant), add WHERE tenant_id = ${tenantId}. If a global load is required, ensure it runs under an explicit platform-admin maintenance path with review/approval and audit logging.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... scans conversations across all tenants without a tenant_id filter. This is a tenant-scoped table read performed globally.
  - _Fix:_ Limit the backfill to a single tenant at a time (add WHERE tenant_id = ${tenantId}) or iterate tenants and run the backfill per-tenant. Ensure any derived writes use asserted tenant ownership (and document/require platform-admin privileges for any deliberate cross-tenant maintenance).
- **server/chat-engine.ts:572** — Query reads from tenant-scoped table `project_files` without an explicit tenant_id constraint. The code relies on prior verification of projectId but the SQL itself lacks a tenant filter.
  - _Fix:_ Add an explicit tenant constraint to the query, e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`, or use Drizzle `.where(eq(projectFiles.tenantId, tenantId))` to ensure every access enforces tenant isolation at the SQL level.
- **server/chat-engine.ts:3836** — Call to markReviewReused(conversationId, toolName) passes only conversationId (foreign id) with no tenant context. If markReviewReused performs reads/writes against tenant-scoped tables it may operate without a tenant filter/ownership check.
  - _Fix:_ Pass the tenantId (or tenant-scoped context) into markReviewReused, or ensure markReviewReused internally validates the conversation belongs to the caller's tenant before any DB access. All DB writes/reads must include WHERE tenant_id = <tenant> or equivalent Drizzle .where(eq(..., tenantId)).
- **server/commitment-drafter.ts:146** — Unscoped SELECT enumerating tenant ids: `SELECT DISTINCT tenant_id AS "tenantId" FROM commitments ...` queries the tenant-scoped `commitments` table without a tenant filter, exposing which tenants have candidate rows.
  - _Fix:_ If the enumeration is part of an admin background job, ensure the routine runs under a controlled admin context and document/limit the exposure. If not intended, restrict scope (e.g. run per-tenant or persist an admin-only summary table). Avoid ad-hoc cross-tenant SELECTs on tenant-scoped tables without explicit admin authorization and auditing.
- **server/data-protection.ts:360** — Selecting project_notes with `WHERE project_id = ${proj.id}` omits an explicit tenant constraint. If project_notes is a tenant-scoped table, reads should include tenant_id to enforce isolation at the application layer.
  - _Fix:_ Constrain reads to the tenant as well, e.g. `SELECT ... FROM project_notes WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` or join via projects and check p.tenant_id = ${tenantId}. Prefer explicit tenant predicates on tenant-scoped tables.
- **server/data-protection.ts:371** — Selecting project_files with `WHERE project_id = ${proj.id}` omits an explicit tenant constraint. This read may touch tenant-scoped data without an application-level tenant predicate.
  - _Fix:_ Add tenant scoping to the query, e.g. `SELECT ... FROM project_files WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` or ensure the query joins to projects and verifies p.tenant_id = ${tenantId}. Always include tenant_id predicates on tenant-scoped tables.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken selects dfy_intake_forms by token with NO tenant_id constraint. Although the token is unguessable, the query reads a tenant-scoped table without explicit tenant scoping.
  - _Fix:_ Either include tenant_id in the WHERE clause (if caller supplies/knows tenant) or, if token is the intended sole auth, ensure token values are unique and validated to map to a tenant and make that explicit in code/comments. Prefer adding a tenant check or returning tenantId alongside the form so subsequent writes can be tenant-scoped.
- **server/episode-playbooks.ts:133** — String-interpolated SQL used to build an ANY(int[]) clause: id = ANY(${`{${ids.join(",")}}`}::int[]) — building SQL by concatenating IDs as a string risks injection-like mistakes and bypasses proper parameterization.
  - _Fix:_ Use parameterized constructs (e.g. sql.join with sql`${id}` entries, Drizzle's inArray helper, or pass an array parameter) instead of building a literal string. Example: WHERE id = ANY(${sql.raw(`ARRAY[${sql.join(ids.map(n => sql`${n}`), sql`,`)}]::int[]`)}) or better yet use inArray(deliveries.id, ids) / sql.join to keep parameters safe.
- **server/episode-playbooks.ts:68** — String interpolation of embeddingLiteral into a vector comparison/ORDER BY (goal_embedding <=> ${embeddingLiteral}::vector) without runtime validation of the embedding. This mixes runtime data into SQL without the defensive checks used elsewhere (vecLiteral), risking malformed SQL or injection if embedding data is poisoned.
  - _Fix:_ Validate the embedding with the same vecLiteral runtime checks used elsewhere before embedding it into the query. Prefer constructing a validated sql.raw fragment (or a parameterized form accepted by the DB driver) after validation, e.g. const vec = vecLiteral(embedding); ... ORDER BY goal_embedding <=> ${sql.raw(`'${vec}'::vector`)}. Also ensure tenant_id filtering is present (it already is here).
- **server/job-queue.ts:177** — completeJob updates agent_jobs by id/attempts but does not enforce tenant_id in the WHERE clause, allowing a caller who knows an id to mutate a job row belonging to another tenant.
  - _Fix:_ Require and check tenantId when completing jobs (e.g. add AND tenant_id = ${tenantId} to the WHERE) or derive/validate the tenant from the claimed job before writing. Use the job's tenantId from the claimed result as the authoritative tenant scope for the update.
- **server/job-queue.ts:273** — cancelJob updates agent_jobs WHERE id = ${id} without checking tenant_id, so an operator call could cancel jobs from other tenants if given an id.
  - _Fix:_ Require tenant context for cancel operations (add tenantId param) and add AND tenant_id = ${tenantId} to the WHERE clause. Validate the caller is authorized for that tenant before mutating rows.
- **server/job-queue.ts:282** — retryJob updates agent_jobs by id without enforcing tenant_id, permitting cross-tenant state changes if an id is supplied.
  - _Fix:_ Require tenant scoping (pass tenantId) and include AND tenant_id = ${tenantId} in the WHERE clause. Validate tenant ownership before changing job state.
- **server/lib/mcp-api-keys.ts:182** — Best-effort touch UPDATE on mcp_api_keys uses WHERE id = ${row.id} without constraining by tenant_id. Although id is typically unique, missing tenant constraint violates the invariant and risks touching the wrong tenant row in some scenarios.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. "WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}" (or supply the resolved tenantId) before updating last_used_at.
- **server/lib/ideabrowser-score.ts:186** — SQL interpolates a hand-built array literal (${literal} like "{1,2,3}") into the query for rm.project_id = ANY(...). This is string-interpolated SQL using request-derived values (projectIds) and risks SQL-injection / isolation mistakes.
  - _Fix:_ Avoid injecting a literal string. Use a proper parameterized array binding (pass the ids as a parameterized array) or Drizzle helpers (e.g. ANY(${ids}::int[]) with ids as a parameter) so values are bound safely, or iterate/construct a parameterized IN/ANY clause. Keep tenant filters (they exist) and ensure inputs are validated as integers before binding.
- **server/lib/outcome-promotion.ts:158** — scanOutcomePromotions() issues a SELECT across deliverable_grade_events without restricting to a single tenant (no WHERE tenant_id = <tenant>), reading tenant-scoped rows for all tenants.
  - _Fix:_ If this is intended as a multi-tenant nightly scan, mark the function as an administrative/aggregator job (document/guard it) and ensure it runs in a controlled context. Otherwise, constrain the query to a caller-provided tenant_id (WHERE tenant_id = ${tenantId}) or require explicit operator/admin authorization. Avoid ambient cross-tenant reads from request-handlers that act on a single tenant.
- **server/lib/restraint-trust.ts:88** — recordReputationOutcome() uses sql.raw(column) to inject a dynamic column name into an UPDATE. Using sql.raw with runtime values risks SQL-injection or accidental misuse if the allowlist/validation changes.
  - _Fix:_ Avoid sql.raw with runtime data where possible. Instead, implement the column update via an explicit switch/case that emits one of four fixed parameterized UPDATE statements (one per allowed column), or use strongly-typed query builders that accept a column enum. If sql.raw must be used, keep the allowlist check and add unit tests + strict asserts that column is one of the approved identifiers before using sql.raw.
- **server/lib/scheduled-post-runner.ts:319** — Construction of a Postgres array literal (arrLit) from request-derived platforms and embedding it into the SQL template: const arrLit = `{...}` then used as ${arrLit}::text[]. This builds SQL/array text from caller input rather than using a parameterized array, risking injection or type/casting issues.
  - _Fix:_ Do not interpolate raw array literals built from request data. Use parameterized/typed bindings or Drizzle helpers to pass a JS array as a text[] parameter (or use sql`ARRAY[...]` with parameterized elements). E.g. pass the platforms as a parameterized array or construct the array with parameter placeholders rather than string-concatenating, or validate/whitelist each platform and use explicit parameterization to eliminate injection risk.
- **server/orchestrator-ledger.ts:450** — Use of sql.raw() with a value derived from function inputs (lockKey) in: `tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`)`. This injects a request-derived value into raw SQL (SQL injection + tenant-isolation risk per rule #4).
  - _Fix:_ Avoid sql.raw() for request-derived values. Bind the value as a parameter instead (use parameterized template interpolation). Example: `await tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`);` or otherwise pass a properly-typed parameter rather than sql.raw(). Validate/normalize ledgerKey beforehand.
- **server/process-governor.ts:281** — (Related) The memory_write_rate and other queries correctly include tenant_id; review the sibling subqueries that omit tenant filters to ensure consistency.
  - _Fix:_ Audit nearby subqueries for missing tenant predicates; ensure every read against tenant-scoped tables includes tenant_id = ${tenantId}.
- **server/routes.ts:649** — cleanupTestTenants executes SELECT ... NOT IN (SELECT DISTINCT tenant_id FROM conversations ...) and NOT IN (SELECT DISTINCT tenant_id FROM projects ...) — it queries tenant-scoped tables (conversations, projects) without any tenant_id = <caller tenant> constraint.
  - _Fix:_ Restrict the existence checks to the specific candidate tenant (e.g. use WHERE tenant_id = ${tid} and LIMIT 1 or COUNT) instead of scanning DISTINCT tenant_id across the whole table. If the code intentionally inspects all tenants for cleanup, gate the routine to platform-admin only and document that it requires platform-level access.
- **server/routes.ts:766** — backfillProjectDriveFolders selects FROM projects JOIN tenants WHERE p.drive_folder_id IS NULL ... with no tenant_id scoping — reads project rows across all tenants.
  - _Fix:_ Perform the backfill per-tenant (iterate tenant ids and scope the SELECT by p.tenant_id = ${tid}) or require and assert platform-admin privileges and ensure the operation is run in a controlled migration context. Avoid unscoped reads of tenant-scoped tables in regular runtime paths.
- **server/routes.ts:1118** — Uploads middleware selects tenantId from file_storage WHERE filename = ${baseName} without scoping by tenant. The code then applies app logic to determine ownership, but the raw SELECT reads rows for all tenants for that filename.
  - _Fix:_ If the intent is only to verify that the authenticated tenant owns a row, query with eq(fileStorage.filename, baseName) AND eq(fileStorage.tenantId, authedTenantId) (LIMIT 1). If you need to detect basename collisions across tenants, make that explicit and run under platform-admin authority, or ensure only tenantId column is selected and no other sensitive fields are returned; document and audit the behavior.
- **server/routes.ts:2059** — GET /api/presenter/:token queries presenter_sessions by token (SELECT id, tenant_id, ... WHERE token = ${token}) without also scoping by tenant. presenter_sessions has tenant_id — unscoped read can reveal session existence across tenants if token handling is not sufficiently secret.
  - _Fix:_ Bind presenter session lookups to an explicit tenant when possible, or ensure the token is a strong capability and the route is treated as a capability endpoint (document threat model). Alternatively, include tenant scoping when a tenant context is available, or encrypt/sign the token payload so the SQL lookup can include tenant verification.
- **server/routes.ts:2319** — Public order lookup SELECT from delivery_logs uses WHERE orderId = ... OR stripePaymentId = ... with no tenant_id constraint. delivery_logs appears tenant-scoped; this unscoped read can access rows across tenants.
  - _Fix:_ Add tenant scoping where appropriate (e.g. eq(deliveryLogs.tenantId, tenantId)) or, if this endpoint is intentionally capability-based (unguessable session id), validate and document that the returned fields are safe and avoid any unscoped SELECTs that return tenant identifiers. Consider verifying the delivery row's tenant before returning any data or re-signing capability links per-tenant.
- **server/routes.ts:2427** — Async order-recovery worker selects orderId FROM delivery_logs WHERE lower(customer_email) = ${rawEmail} without tenant scoping. This background lookup examines tenant-scoped delivery rows across the entire platform.
  - _Fix:_ Limit the lookup to the intended tenant scope where possible, or perform this operation under an explicit platform-admin migration/job context with restricted execution and logging. If the goal is to find orders by email across tenants, ensure this behavior is intentional and documented; otherwise replace with a per-tenant lookup (eq(deliveryLogs.tenantId, tenantId)).
- **server/routes.ts:2571** — Verify-orders handler queries delivery_logs with WHERE lower(customer_email) = ${rawEmail} (and later filters) without tenant_id scoping, returning up to 50 rows. This exposes order rows across tenants to the verification flow.
  - _Fix:_ Scope the SELECT by tenant where appropriate, or otherwise ensure the single-use code consumption and subsequent response logic do not leak cross-tenant data. Prefer adding eq(deliveryLogs.tenantId, tenantId) when a tenant context exists, or explicitly run the verify flow as an admin-backed, audited routine if cross-tenant search is required.
- **server/routes.ts:6540** — storage.createMessage(...) called during import without passing tenantId. Call: await storage.createMessage({ ...msgRest, conversationId: newConv.id }); — per project invariant, INSERTs into tenant-scoped tables must explicitly set tenant_id; omitting tenantId risks incorrect tenant assignment or cross-tenant data leakage.
  - _Fix:_ Pass tenantId explicitly when creating messages during import (e.g. storage.createMessage({ ...msgRest, conversationId: newConv.id, tenantId: rest.tenantId || <appropriateTenant> })). If storage.createMessage is expected to derive tenant from the conversation, make that behavior explicit and audited; better practice is to require and assert tenantId at the callsite or validate conversation->tenant ownership before inserting.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table `agent_jobs` without any tenant_id predicate (query returns rows across all tenants).
  - _Fix:_ If this is intended only for platform operators, ensure the caller is strictly gated (requirePlatformAdmin) and document the global intent. Otherwise scope the query to the caller: add a WHERE j.tenant_id = ${tenantId} (or accept a tenantId parameter and bind it via the sql tag) to enforce per-tenant isolation.
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` (aggregating cost per tenant) runs without a tenant_id predicate — reads across all tenants.
  - _Fix:_ If these are intended operator-level aggregate metrics, ensure the endpoint is strictly operator-only (current METRICS_TOKEN check helps) and clearly document the global read. If the handler should be per-tenant, add a tenant_id filter (`WHERE tenant_id = ${tenantId} AND ...`) or restrict aggregation scope. Consider adding additional access controls (platform-admin check) or redact sensitive columns as needed.
- **server/routes/metrics.ts:79** — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) is executed without any tenant_id constraint (global read across tenants).
  - _Fix:_ Either scope the query to a tenant (`WHERE tenant_id = ${tenantId}`) if per-tenant metrics are intended, or ensure the endpoint remains operator-only with strong auth and document the global-read intent. Consider explicit platform-admin gating in addition to the token.
- **server/routes/metrics.ts:95** — SELECT aggregating tokens from tenant-scoped table `agent_cost_ledger` runs without a tenant_id filter (global aggregation).
  - _Fix:_ If per-tenant isolation is required, add `WHERE tenant_id = ${tenantId}`. If global aggregation for operators is intended, ensure the endpoint is strongly protected and document that it reads across all tenants (or move this aggregation to an internal-only tool).
- **server/routes/metrics.ts:113** — SELECT from `repair_incidents` executes without tenant scoping (GROUP BY classification/action across all tenants). If `repair_incidents` is tenant-scoped this is a cross-tenant read.
  - _Fix:_ If this table is tenant-scoped, add a tenant_id predicate to limit results to the caller's tenant. If the aggregation is intended for operators, ensure the endpoint is strictly operator-only (strong token/session gating) and add a comment describing the intentional global read to avoid accidental exposure.
- **server/routes/stripe-checkout.ts:104** — Request-derived value (priceId from req.body) is interpolated into a raw SQL tagged template: db.execute(sql`... WHERE pr.id = ${priceId} ...`). This is string-interpolated SQL with request data (injection/isolation risk) and bypasses the Drizzle query-builder APIs used elsewhere.
  - _Fix:_ Avoid embedding request-derived values inside raw SQL templates. Use the Drizzle query builder APIs (e.g. db.select().from(prices).where(eq(prices.id, priceId), eq(prices.active, true), ...)) so values are bound safely, and validate priceId format server-side (or cast to expected type) before use. If raw SQL is required, pass parameters through proper parameter binding (not via uncontrolled string interpolation) and keep the server-side allowlist/validation in place.
- **server/storage.ts:826** — getMemoriesWithoutEmbeddings selects from tenant-scoped table `memory_entries` with NO tenant filter (returns rows across tenants).
  - _Fix:_ Make tenant scoping explicit: accept a tenantId or the ALL_TENANTS sentinel. For non-admin callers require a tenantId and use requiredTenantScope/tenantScope helpers; for admin-only paths require explicit admin authorization and clearly documented intent.
- **server/storage.ts:831** — getKnowledgeWithoutEmbeddings selects from tenant-scoped table `agent_knowledge` with NO tenant filter (returns rows across tenants).
  - _Fix:_ Require an explicit tenant scope parameter (tenantId or ALL_TENANTS) validated via requiredTenantScope/tenantScope. If this is intended to be an admin-only job, require explicit admin context and audit it.
- **server/storage.ts:512** — getAllMemoriesForBackup SELECTs `memory_entries` without any tenant filter (returns rows across tenants).
  - _Fix:_ Make the admin intent explicit (require explicit admin authorization and an ALL_TENANTS sentinel) or accept a tenantId parameter and return only that tenant's rows. Ensure backup/export paths are auditable and gated.
- **server/storage.ts:942** — getHeartbeatTask selects from tenant-scoped table `heartbeat_tasks` by id with NO tenant_id constraint.
  - _Fix:_ Require the caller to pass tenantId and scope the query (.where(and(eq(heartbeatTasks.id, id), eq(heartbeatTasks.tenantId, tenantId)))) or validate ownership via a join/subselect before returning the row.
- **server/tools.ts:2926** — Correlated subqueries in the projects LIST query (SELECT COUNT(*) FROM project_files / project_notes / project_conversations WHERE project_id = p.id) do not include an explicit tenant_id filter. Per the invariant, reads against tenant-scoped tables must include tenant_id = <caller tenant>.
  - _Fix:_ Add tenant_id = ${tenantId} to each subquery (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}). Even when correlated by project_id, explicitly constrain by tenant_id to enforce app-level isolation.
- **server/tools.ts:2988** — Duplicate-check SELECT against project_files (SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ... AND file_url = ...) omits tenant_id. Reading a tenant-scoped table without constraining tenant_id risks cross-tenant data access.
  - _Fix:_ Constrain the query with tenant_id = ${tenantId} (e.g. WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND ...). Always validate ownership via tenant_id for any lookups based on foreign ids.
- **server/tools.ts:3028** — SELECT from project_conversations (SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId}) omits tenant_id. This read of a tenant-scoped table is not constrained to the caller's tenant.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}), or otherwise assert ownership via a helper before reading/inserting.
- **server/tools.ts:3042** — SELECT in project SEARCH query uses correlated subqueries (COUNT(*) FROM project_files / project_notes WHERE project_id = p.id) which do not include tenant_id = ${tenantId}. Reads against tenant-scoped tables must include explicit tenant filters.
  - _Fix:_ Add tenant_id = ${tenantId} to each correlated subquery (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}). Ensure all aggregated counts or EXISTS checks include the tenant constraint.

### Suppressed — confirmed false positives (224)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with NO tenant_id constraint — allows cross-tenant modification if runId is known.
  - _Why false positive:_ Server-side worker mutator on globally unique agent_runs.id; no evidence runId comes from untrusted tenant request boundary.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with NO tenant_id constraint — tenant-scoped table write missing tenant check.
  - _Why false positive:_ Internal agent run state transition by server-generated primary key; call pattern shown is worker-style, not tenant-facing input.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState updates agent_runs by id only (.where(eq(agentRuns.id, runId))) without tenant_id filtering — violates tenant isolation invariant.
  - _Why false positive:_ replaceRunState mutates agent_runs by server-side run id in internal run lifecycle module; fits worker-by-PK pattern.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun updates agent_runs (sets status/result/completedAt) using only run id in WHERE, with NO tenant_id constraint — allows cross-tenant writes to run records.
  - _Why false positive:_ completeRun is internal lifecycle completion on globally unique run id, then reads row and carries its own tenantId downstream.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun updates agent_runs by id only (.where(eq(agentRuns.id, runId))) with NO tenant_id constraint — tenant isolation missing on failure path.
  - _Why false positive:_ failRun is internal worker failure-path update by server-generated run primary key, not shown crossing an untrusted request boundary.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun updates agent_runs (status/state) using only run id in WHERE, lacking tenant_id constraint — unscoped writes to a tenant-scoped table.
  - _Why false positive:_ pauseRun is another internal run-state mutator by server-generated PK within agentic worker flow.
- **server/auto-memorize.ts:74** _(system-sweep)_ — INSERT INTO agent_knowledge hardcodes tenant_id = 1 when creating a watermark — writes will be attributed to tenant 1 for all runs.
  - _Why false positive:_ Watermark row is housekeeping for a platform-wide auto-memorize sweep; hardcoded tenant id is used for maintenance state, not tenant-facing content isolation.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL lacks tenant_id filtering — this can modify another tenant's conversation row.
  - _Why false positive:_ Backfill loop is maintenance across tenants; source rows already join conversations/projects with matching tenant_id and each row carries its own tenant_id.
- **server/channel-routing.ts:16** _(global-table)_ — channel_routes table is created without a tenant_id column, making routing entries globally shared across tenants and preventing application-level tenant-scoping.
  - _Why false positive:_ channel_routes is defined as a single global routing registry with unique(channel) and joins to non-tenant-scoped personas.
- **server/channel-routing.ts:48** _(global-table)_ — setChannelRoute inserts/updates and deletes channel_routes entries without any tenant context and accepts personaId from caller input without verifying ownership. This allows a caller to create or overwrite mappings that reference personas from other tenants (or globally modify routes).
  - _Why false positive:_ Writes target intentionally global channel_routes table; personas schema note also indicates non-tenant-scoped registry.
- **server/channel-routing.ts:71** _(global-table)_ — removeChannelRoute deletes channel_routes entries by channel only, with no tenant scoping. This allows deletion of other tenants' channel routes.
  - _Why false positive:_ Delete operates on intentionally global channel_routes entries keyed only by channel.
- **server/data-protection.ts:168** _(system-sweep)_ — DELETE FROM conversations executed without constraining by tenant_id. The code previously read expired rows including tenant_id but the final DELETE uses only the conversation id, allowing a race or FK anomaly to delete another tenant's conversation.
  - _Why false positive:_ Global purge sweep processes previously selected rows and only performs housekeeping deletion; no tenant data is returned or mixed into one tenant-facing output.
- **server/embeddings.ts:751** _(system-sweep)_ — SELECT from agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter. This reads tenant-scoped rows across ALL tenants.
  - _Why false positive:_ Backfill is a platform-wide maintenance loop over expired/missing embeddings; per-row processing only computes/stores embeddings and does not expose mixed tenant data.
- **server/embeddings.ts:780** _(system-sweep)_ — SELECT from memory_entries in backfillEmbeddingVecs lacks tenant_id constraint and therefore reads memory entries across tenants.
  - _Why false positive:_ Embedding vector backfill is maintenance across tenants, mutating housekeeping-derived vector fields only and not returning tenant data.
- **server/embeddings.ts:790** _(system-sweep)_ — UPDATE memory_entries in backfillEmbeddingVecs uses WHERE id = ${row.id} with no tenant_id constraint—possible cross-tenant writes.
  - _Why false positive:_ Update occurs inside global embedding backfill sweep and only sets derived embedding_vec on the selected row.
- **server/embeddings.ts:796** _(system-sweep)_ — SELECT from agent_knowledge in backfillEmbeddingVecs has no tenant_id filter — reads knowledge rows for all tenants.
  - _Why false positive:_ Knowledge embedding vector backfill is a maintenance sweep across tenants, not a tenant-facing read path.
- **server/embeddings.ts:807** _(system-sweep)_ — UPDATE agent_knowledge in backfillEmbeddingVecs uses WHERE id = ${row.id} with no tenant_id constraint — allows cross-tenant writes.
  - _Why false positive:_ Update is part of global maintenance backfill and only sets housekeeping/derived embedding_vec on selected rows.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE on tenant-scoped table `code_proposals` (archiving) without constraining by tenant_id — mass write may modify proposals belonging to other tenants.
  - _Why false positive:_ Archiving stale proposals is a platform-wide housekeeping sweep changing only status fields; no tenant data is exposed to another tenant-facing surface.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) — writes to tenant-scoped table without tenant_id constraint, potentially disabling tasks for other tenants.
  - _Why false positive:_ Disabling stuck heartbeat tasks is cross-tenant maintenance mutating only housekeeping state, not mixing tenant content into outputs.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into `presenter_slide_images` performed without supplying/validating tenant_id. If `presenter_slide_images` is tenant-scoped this allows inserting data not tied/validated to a tenant.
  - _Why false positive:_ Referenced parent presenter_sessions is tenant-scoped; child presenter_slide_images is inserted via session_id and is isolated transitively if child table has no tenant_id.
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} without constraining by tenant_id. This updates a tenant-scoped session row by id only, allowing cross-tenant modification if sessionId is untrusted or an attacker can supply it.
  - _Why false positive:_ sessionId is server-generated from the immediately preceding presenter-session creation response, then updated by primary key in internal server flow.
- **server/minds-engine.ts:327** _(upstream-verified)_ — SQL array literal is constructed via string interpolation from caller-supplied eventIds: `WHERE id = ANY(${`{${eventIds.join(",")}}`}::int[]) AND tenant_id = ${tenantId}`. This mixes request-derived values into SQL text/array literal and can lead to SQL-injection and/or tenant-isolation bypass.
  - _Why false positive:_ Query includes tenant_id = tenantId in the WHERE clause; this is not a missing tenant-isolation predicate finding.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` is executed without constraining by tenant_id (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This exposes rows across all tenants (sensitive tokens/metadata).
  - _Why false positive:_ Startup validation is a platform-wide housekeeping loop over active subscriptions; it processes each row with its own tenant_id and does not return mixed tenant data to a tenant-facing surface.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' is run with no tenant_id constraint. This reads subscription rows across all tenants.
  - _Why false positive:_ Boot-time auto-reconnect enumerates google-workspace subscriptions globally only to derive per-row tenantIds for maintenance, with no tenant-facing data output.
- **server/oauth-subscriptions.ts:956** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} updates a tenant-scoped table using only id in the WHERE clause (no tenant_id check). Without an application-level tenant constraint this could modify another tenant's row if IDs are guessed or misused.
  - _Why false positive:_ This update is inside the startup housekeeping sweep and mutates only status on the exact rows just selected globally; no cross-tenant data is exposed or mixed.
- **server/oauth-subscriptions.ts:966** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} (no tenant_id constraint) — same unsafe pattern present in another branch of startup validation.
  - _Why false positive:_ Same startup maintenance sweep; per-row status-only deactivation of expired subscriptions, not a tenant-facing cross-tenant access path.
- **server/oauth-subscriptions.ts:981** _(system-sweep)_ — UPDATE oauth_subscriptions SET is_active = FALSE WHERE id = ${sub.id} executed without tenant_id constraint (again in a different branch). This mutates tenant-scoped data without enforcing tenant ownership in the WHERE clause.
  - _Why false positive:_ Same housekeeping routine; status-only mutation during global validation of stale subscriptions with per-row tenant handling.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh() executes an unbounded SELECT on oauth_subscriptions: SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE. This enumerates subscriptions across tenants (sensitive) without per-tenant scoping.
  - _Why false positive:_ Proactive refresh is a global maintenance loop over active subscriptions, using each row's tenant_id independently and not aggregating tenant content into one response.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions() issues SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant constraint, reading rows for all tenants and then performing refresh operations across tenants.
  - _Why false positive:_ Force-refresh-all is explicitly a cron-style maintenance routine exercising refresh tokens per subscription/tenant, not a tenant-facing bulk read.
- **server/plan-executor.ts:117** _(internal-worker-pk)_ — appendExecutionLog() updates the tenant-scoped plans table without constraining by tenant_id (UPDATE plans ... WHERE id = ${planId}). This allows cross-tenant modification of a plan row by id.
  - _Why false positive:_ appendExecutionLog is an internal executor helper operating on a server-generated planId within the worker call path, not directly from an untrusted tenant request boundary.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — executePlan() claims a plan via UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' without scoping the operation to a tenant (no tenant_id filter). A caller can claim/operate on any plan id across tenants.
  - _Why false positive:_ executePlan is an internal worker claiming a plan by globally unique primary key and immediately carrying forward the row's tenant_id; planId is used as server-side job identity.
- **server/plan-executor.ts:728** _(internal-worker-pk)_ — After execution, code updates the plan status via UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} with no tenant_id constraint. This can modify plans across tenants by id.
  - _Why false positive:_ Final status update occurs within the same internal executor flow for the already-claimed plan primary key, not from a cross-tenant caller-supplied selector.
- **server/plan-executor.ts:762** _(internal-worker-pk)_ — Error-handling rollback uses UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' without a tenant_id constraint, allowing cross-tenant state changes.
  - _Why false positive:_ Error-path rollback is part of the internal executor state machine for a server-managed planId, updating only the worker's current row.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans() resets all stale executing plans back to 'approved' via UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id with no tenant restriction. This mass-update touches rows across all tenants.
  - _Why false positive:_ Boot-time recovery is a platform-wide housekeeping sweep that only resets status fields and requeues work; it does not expose or mix tenant data in any tenant-facing output.
- **server/plan-executor.ts:787** _(system-sweep)_ — resumeStuckPlans() selects pending approved plans via SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 with no tenant_id filter, then re-queues execution for those plan ids without any tenant scoping.
  - _Why false positive:_ Boot-time recovery sweep intentionally operates across all tenants and only requeues plan ids/status, with no tenant data returned or mixed into one tenant-facing output.
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE in cleanupZombieSessions modifies `research_sessions` without scoping by tenant (UPDATE research_sessions SET status = 'completed' ... WHERE status = 'running' ${notInClause} AND started_at < ${staleThreshold}). This can change session state for sessions belonging to any tenant.
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping update of status fields only; it returns ids for logging and does not expose or mix tenant content.
- **server/research-engine.ts:416** _(system-sweep)_ — DELETE from tenant-scoped table `agent_knowledge` is executed without tenant scoping (DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW()). This will delete autoresearch entries across all tenants.
  - _Why false positive:_ Expired autoresearch deletion is global maintenance cleanup of stale rows, not tenant-facing data access or cross-tenant content mixing.
- **server/research-engine.ts:496** _(fk-transitive)_ — INSERT into `project_notes` omits tenant identification (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...)). Per codebase invariant tenant-scoped tables require tenant_id to be provided on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ project_notes has no tenant_id and is tenant-scoped transitively through validated project_id ownership via assertProjectInTenant(projectId, session.tenantId).
- **server/research-engine.ts:503** _(fk-transitive)_ — Another INSERT into `project_notes` (for each finding) omits tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) VALUES (${projectId}, ${noteContent}, ${'Research Engine'}, NOW())). Missing tenant_id risks mis-attribution or DB errors.
  - _Why false positive:_ project_notes has no tenant_id and insert is isolated through the tenant-validated parent project_id after assertProjectInTenant.
- **server/routes/conversations.ts:100** _(upstream-verified)_ — UPDATE on tenant-scoped table `conversations` without tenant_id constraint in WHERE clause (`UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}`).
  - _Why false positive:_ conv was just created with tenantId by server code, and project ownership was checked with WHERE id = projectId AND tenant_id = tenantId earlier in the same path.
- **server/routes/conversations.ts:101** _(fk-transitive)_ — SELECT from tenant-scoped table `project_conversations` without tenant_id constraint (`SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}`).
  - _Why false positive:_ project_conversations has no tenant_id and is an association isolated transitively by tenant-scoped parent project and conversation ids already validated/created in-tenant.
- **server/routes/conversations.ts:104** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` that does not set tenant_id (`INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)`).
  - _Why false positive:_ project_conversations has no tenant_id and insert is correct by design through tenant-scoped parent project/conversation linkage.
- **server/routes/conversations.ts:106** _(upstream-verified)_ — UPDATE on tenant-scoped table `projects` without tenant_id constraint in WHERE clause (`UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}`).
  - _Why false positive:_ The same call path already verified project ownership with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before this timestamp update.
- **server/routes/conversations.ts:229** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` that does not set tenant_id (`INSERT INTO project_notes (project_id, note, author) VALUES (...)`).
  - _Why false positive:_ project_notes has no tenant_id and the parent project_id is derived from a join constrained by p.tenant_id = conv.tenantId/tenantId in the same path.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE on tenant-scoped table research_programs without tenant_id constraint (UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}). This can modify another tenant's row if ids are global or otherwise violate tenant isolation.
  - _Why false positive:_ boot-time seed repair updates each selected row by its own id; housekeeping-only model fix, not tenant-facing data mixing
- **server/seed.ts:2515** _(system-sweep)_ — SELECT DISTINCT ... FROM conversations without tenant restriction (SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...project_conversations...)). conversations appears tenant-scoped but the query does not filter by tenant_id (and project_conversations lacks tenant_id), risking cross-tenant data access and incorrect cross-tenant link creation.
  - _Why false positive:_ boot-time backfill across tenants creates missing child links per conversation/project row; project_conversations is transitively scoped and no tenant data is returned to users
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE memory_entries in storeLesson updates rows WHERE id = <found id> with no tenant_id constraint. Updating by id alone can modify another tenant's memory entry.
  - _Why false positive:_ row id being updated was selected immediately above with WHERE tenant_id = tenantId, so ownership is established in same call path
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT aggregation from heartbeat_logs has no tenant_id filter: the query aggregates error counts across all heartbeat_logs without scoping to a tenant, causing cross-tenant reads and selection of other tenants' failure data.
  - _Why false positive:_ heartbeat_logs schema note says tenant_id column NO; cross-tenant aggregation on global operational logs is intended
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks executed without tenant scoping: cleanupStaleData deletes heartbeat_tasks WHERE enabled = false ... created_at < ... with no tenant_id constraint, which can remove other tenants' tasks.
  - _Why false positive:_ maintenance purge of old disabled delegation tasks is platform-wide housekeeping and not tenant-facing data mixing
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log executed without tenant scoping: pruneOldLogs removes old event_log rows based solely on created_at and status, lacking a tenant_id WHERE clause and risking cross-tenant data deletion.
  - _Why false positive:_ platform-wide deletion of old processed/failed event rows is housekeeping only; no tenant content returned or mixed
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs executed without tenant scoping: pruneOldLogs removes old heartbeat_logs across all tenants (created_at cutoff) with no tenant_id in the WHERE clause, enabling cross-tenant deletion.
  - _Why false positive:_ heartbeat_logs has no tenant_id per schema and pruning old global operational logs is intended
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories executes a global UPDATE on tenant-scoped table `memory_entries` with NO tenant filter (UPDATE memory_entries SET status='archived' WHERE ...). This will modify rows across all tenants.
  - _Why false positive:_ global archive of expired memories is maintenance-only status transition across tenants, with no cross-tenant read exposure
- **server/storage.ts:857** _(system-sweep)_ — archiveStaleMemories executes a global UPDATE on tenant-scoped table `memory_entries` with NO tenant filter (UPDATE memory_entries SET status='archived' WHERE ...). This modifies data for all tenants.
  - _Why false positive:_ global archive of stale memories is housekeeping status mutation only and does not expose or mix tenant data
- **server/surprise-scorer.ts:93** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `felix_proposals` (setting args_embedding) lacks a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ Server worker helper updates by server-generated proposal primary key; call site shown is internal after tenant-scoped fetch, not direct untrusted input.
- **server/surprise-scorer.ts:139** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `felix_proposals` (setting actual_outcome_embedding) omits tenant_id in the WHERE clause.
  - _Why false positive:_ Internal scoring worker operates on previously tenant-verified proposal id, not request-boundary input.
- **server/surprise-scorer.ts:148** _(upstream-verified)_ — SELECT args_embedding FROM `felix_proposals` re-fetch is missing a tenant_id filter (reads tenant-scoped data without tenant constraint).
  - _Why false positive:_ Same function already fetched this proposal with WHERE id = proposalId AND tenant_id = tenantId a few lines above; re-fetch uses same internal id.
- **server/surprise-scorer.ts:181** _(internal-worker-pk)_ — UPDATE setting surprise_band = 'no_history' on `felix_proposals` does not include tenant_id in the WHERE clause.
  - _Why false positive:_ Internal worker status update by previously tenant-verified proposal primary key, not untrusted caller input.
- **server/surprise-scorer.ts:212** _(internal-worker-pk)_ — UPDATE setting surprise_score / surprise_band on `felix_proposals` is missing tenant_id in the WHERE clause.
  - _Why false positive:_ Internal worker writes score/band on the same server-selected proposal row by PK after tenant-scoped read.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE incrementing usageCount on `custom_tools` (where eq(custom_tools.id, tool.id)) lacks tenant_id in the WHERE clause.
  - _Why false positive:_ Tool row was selected immediately above with WHERE name = n AND tenant_id = tenantId; update applies to that fetched row in same call path.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from tenant-scoped table `provider_keys` performed with no tenant_id constraint (SELECT provider, LENGTH(api_key) as key_len FROM provider_keys ...). This returns rows across all tenants.
  - _Why false positive:_ Schema note says provider_keys has no tenant_id; query is additionally gated by tenantId === 1 admin path.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM `provider_keys` executed without tenant_id constraint. This enumerates provider key configuration platform-wide.
  - _Why false positive:_ provider_keys is schema-global with no tenant_id and this aggregate enumeration is explicitly restricted to tenantId === 1.
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is performed without constraining by tenant_id. This reads a tenant-scoped conversations row by id across tenants rather than verifying ownership within the expected tenant context.
  - _Why false positive:_ Read is used only to verify conversation ownership against claimed tenantId inside a fail-closed pairing validation loop; mismatches are skipped and not routed.
- **server/video-job-runner.ts:236** _(system-sweep)_ — recoverStaleVideoJobs performs a global UPDATE on the tenant-scoped videoJobs table without constraining by tenant_id. This will mark jobs from all tenants as failed when the heartbeat cutoff is hit, violating tenant isolation and potentially impacting other customers.
  - _Why false positive:_ Global stale-job recovery is a maintenance sweep that only mutates housekeeping/status fields across tenants and returns job ids for logs, not tenant-facing mixed data.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT from tenant-scoped table `experiments` without a tenant_id constraint (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across tenants.
  - _Why false positive:_ Heartbeat-callable global sweep intentionally scans running experiments across tenants and then updates each row with its own row-carried tenant_id; no mixed tenant output is returned.
- **server/agentic-engines.ts:347** _(global-table)_ — Query against `heartbeat_logs` missing tenant scoping (FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads logs across tenants.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id column, indicating a platform-global log table intentionally queried without tenant scoping.
- **server/agentic-engines.ts:452** _(global-table)_ — SELECT FROM `heartbeat_logs` (recentLogs) without a tenant_id constraint (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...). This will return rows across tenants.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column; query is against a platform-wide log table.
- **server/agentic-engines.ts:521** _(global-table)_ — Query aggregating `heartbeat_logs` (heartbeatPerformance) lacks tenant scoping (FROM heartbeat_logs GROUP BY task_name, status ...). This aggregates data across all tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column, so cross-tenant scoping does not apply to this platform-global aggregation.
- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE on tenant-scoped table `ai_insights` performed without tenant_id constraint (UPDATE ai_insights SET status = 'applied' ... WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ...). This updates rows across all tenants.
  - _Why false positive:_ Auto-apply is a global backfill/maintenance sweep over insights; it mutates status/action fields and then routes each returned row using its own tenant_id with validation.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT from `ai_insights` in retryPendingMinervaRouting lacks a tenant filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ... LIMIT 50). This can return rows for multiple tenants and lead to cross-tenant routing actions.
  - _Why false positive:_ Durability retry is a global maintenance sweep; each selected insight carries its own tenant_id and routing/update operations are performed per-row with tenant validation.
- **server/agentic-features.ts:797** _(upstream-verified)_ — UPDATE on `outreach_enrollments` omits tenant scoping (UPDATE outreach_enrollments SET ... WHERE id = ${row.id}). Although the row was previously fetched with a tenant filter, the UPDATE itself lacks tenant_id in the WHERE clause and could update a row with the same id in another tenant.
  - _Why false positive:_ UPDATE uses row.id obtained immediately above from outreach_enrollments SELECT scoped by params.tenantId; same call path proves tenant ownership before write.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE on `agent_approvals` to expire pending approvals lacks tenant scoping (UPDATE agentApprovals SET status = 'expired' ... WHERE status = 'pending' AND expiresAt < now()). This updates approvals across all tenants in one operation.
  - _Why false positive:_ Intentional global expiry sweep; updates housekeeping status on all expired approvals and then handles each returned row with its own tenantId.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — The query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads a tenant-scoped table without any tenant_id constraint. This performs a cross-tenant scan/enumeration of department budgets and then calls per-tenant logic for each row, violating the invariant that every read/write on tenant-scoped tables must be constrained to the caller's tenant.
  - _Why false positive:_ Heartbeat-wide enforcement intentionally scans all active budgets and emits per-row tenant-scoped events using row.tenant_id without mixing tenant content.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE/SELECT claims pending agent_wake_schedules rows without constraining by tenant_id (subquery selects ids from agent_wake_schedules WHERE status = 'pending' ... FOR UPDATE SKIP LOCKED with no tenant filter). This can claim/fire wakes for rows belonging to other tenants.
  - _Why false positive:_ Global scheduler intentionally claims due wake rows across tenants, then emits and finalizes each row using its carried tenant_id.
- **server/auto-project.ts:112** _(upstream-verified)_ — SELECT FROM messages WHERE conversation_id = ${conversationId} has no tenant_id constraint — reading messages without explicit tenant scoping.
  - _Why false positive:_ Conversation ownership is verified just above via conversations WHERE id = conversationId AND tenant_id = tenantId before reading its messages.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} lacks tenant_id filtering — message rows are not explicitly scoped to the tenant.
  - _Why false positive:_ assertConversationInTenant plus scoped conversation/project checks earlier in same function prove tenant ownership before message read.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Backfill query uses a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) with no tenant filter — message existence/count is checked across all tenants.
  - _Why false positive:_ Backfill is a maintenance sweep selecting conversation ids and tenant ids only, then processing each row with autoSaveProjectTranscript(c.id, c.tenant_id); no mixed tenant output.
- **server/auto-transcript.ts:153** _(system-sweep)_ — alsoLinked backfill query contains a subquery (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) without tenant constraint — can detect messages across tenants.
  - _Why false positive:_ alsoLinked is part of the same backfill maintenance sweep and carries c.tenant_id per row into downstream processing without mixing tenant data.
- **server/channel-routing.ts:30** _(global-table)_ — listChannelRoutes SELECT joins personas (likely a tenant-scoped table) but does not constrain either channel_routes or personas by tenant_id. This can leak or mix personas from other tenants.
  - _Why false positive:_ channel_routes is created here as a platform-global routing table with UNIQUE(channel), and referenced personas schema shows no tenant_id
- **server/channel-routing.ts:63** _(global-table)_ — getPersonaForChannel selects persona_id from channel_routes without any tenant filter, returning persona ids that may belong to other tenants.
  - _Why false positive:_ getPersonaForChannel reads from the same global channel_routes mapping; personas also have no tenant_id per schema
- **server/chat-engine.ts:633** _(upstream-verified)_ — Query reads from `compaction_archives` by conversation_id without constraining tenant_id. This can expose another tenant's archive data.
  - _Why false positive:_ query is keyed by c.id from an already tenant-scoped conversations set; compaction_archives row is transitively tied to that scoped conversation
- **server/chat-engine.ts:651** _(upstream-verified)_ — Selecting `role, content, created_at` from `messages` by conversation_id omits a tenant_id filter, risking cross-tenant message disclosure.
  - _Why false positive:_ messages are read by conversation_id derived from already tenant-scoped conversation rows in this loop
- **server/chat-engine.ts:768** _(upstream-verified)_ — LATERAL subqueries selecting from `messages` (e.g. `(SELECT content FROM messages WHERE conversation_id = c.id AND role = 'user' ...)`) omit tenant_id constraints. Even though the outer `conversations` rows are tenant-scoped, the inner `messages` selects are unscoped at SQL level and can leak data if assumptions change.
  - _Why false positive:_ subqueries are correlated to c.id from conversations already filtered by c.tenant_id = tid in the same SQL block
- **server/commitments.ts:135** _(system-sweep)_ — Unscoped SELECT from tenant-scoped table `commitments`: the heartbeat scanner runs `SELECT id, tenant_id AS "tenantId", description, due_at ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...` with no tenant_id filter. This reads tenant-scoped rows across all tenants (including free-text `description`).
  - _Why false positive:_ this is a platform-wide heartbeat sweep; it carries each row's tenantId separately and comments show emitted digest content is redacted, not mixed into a tenant-facing surface
- **server/deep-interview.ts:337** _(fk-transitive)_ — INSERT INTO project_notes does not provide a tenant_id when writing a tenant-scoped artifact. The INSERT builds content and selects p.id from projects but does not set tenant_id explicitly, relying on the project relationship rather than populating the tenant column.
  - _Why false positive:_ project_notes has no tenant_id per schema and is inserted via tenant-scoped parent projects filtered by p.tenant_id = state.tenantId
- **server/data-protection.ts:403** _(upstream-verified)_ — SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} is executed without scoping by tenant_id. This allows a cross-tenant existence/count oracle (and may leak or act on another tenant's rows) because the query lacks a tenant predicate.
  - _Why false positive:_ conv.id comes from conversations already filtered by tenant_id = tenantId in the same call path; count is only for those tenant-scoped conversation ids
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. This can delete chunks across tenants that share the same collection_id/doc_path (or if IDs are reused/mis-specified).
  - _Why false positive:_ same function first verifies doc_collections.id = collectionId AND tenant_id = tenantId; subsequent delete is scoped to that owned collection id
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings performs UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} without constraining tenant_id. Updating by id alone can modify rows belonging to another tenant if an id is mis-attributed or leaked.
  - _Why false positive:_ row.id values come from a prior SELECT on doc_chunks constrained by collection_id and tenant_id in the same function
- **server/email.ts:515** _(system-sweep)_ — backfillEmptyBodies performs SELECT id, message_id FROM inbox_messages WHERE ... ORDER BY id without tenant scoping. A background job selecting across inbox_messages may read rows for all tenants without tenant restriction.
  - _Why false positive:_ background maintenance sweep intentionally scans all tenants only to backfill empty body fields per row; no tenant-facing mixed output
- **server/email.ts:528** _(system-sweep)_ — backfillEmptyBodies updates inbox_messages with UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} but does not include tenant_id in the WHERE. That allows writing to another tenant's message row if row.id was not tenant-validated.
  - _Why false positive:_ housekeeping backfill updates only body fields on rows selected by the platform-wide sweep; per-row maintenance mutation, not tenant data mixing
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' without any tenant_id filter (reads all tenants during boot rehydration).
  - _Why false positive:_ boot-time rehydration intentionally reads running/pending agent_runs across tenants to rebuild in-memory state keyed per row; internal maintenance, not tenant-facing mixed output
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint (write not scoped to tenant).
  - _Why false positive:_ felix_loop_runs update is by server-generated runId inside internal loop worker, not from an untrusted request boundary
- **server/felix-loop.ts:689** _(internal-worker-pk)_ — Error-path UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} with no tenant_id constraint (write not scoped to tenant).
  - _Why false positive:_ error-path update also targets server-generated internal runId within the same worker flow, not caller-controlled
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' using WHERE id = ${projectId} with no tenant_id constraint (reads project across tenants).
  - _Why false positive:_ Read is immediately followed by explicit tenant ownership check and abort before any return/use to caller.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' uses WHERE id = ${projectId} with no tenant_id constraint (write not scoped to tenant).
  - _Why false positive:_ Same call path verified project.tenantId matches tenantId just above before update executes.
- **server/google-drive.ts:826** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' (resolving project by id) without a tenant_id filter in uploadAndShare — the code later checks tenant ownership but the initial read is unscoped.
  - _Why false positive:_ Function checks proj.tenantId against params.tenantId immediately after read and fails closed before proceeding.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from tenant-scoped table `code_proposals` without scoping by tenant_id.
  - _Why false positive:_ Stale-proposal TTL audit is a platform-wide maintenance sweep that archives old rows and emits admin findings, not tenant-facing mixed output.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from tenant-scoped table `heartbeat_tasks` without scoping by tenant_id. This reads tasks across all tenants.
  - _Why false positive:_ Heartbeat-task stuck check is a global housekeeping sweep that disables stale tasks/status only and emits audit findings.
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from research_schedules omits any tenant_id constraint (SELECT * FROM research_schedules ...). This reads tenant-scoped rows across all tenants without an application-enforced tenant filter.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due schedules across tenants and each row is processed with its own sched.tenant_id.
- **server/heartbeat.ts:103** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} has no tenant_id constraint. This updates a tenant-scoped row (sched has tenant_id) without constraining to the tenant.
  - _Why false positive:_ Global scheduler updates only the currently iterated due schedule row as housekeeping, using row-carried tenant context without tenant mixing.
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} is missing a tenant filter. Writing to a tenant-scoped table must assert the tenant.
  - _Why false positive:_ Global scheduler marks the iterated schedule run timestamps as housekeeping only; no tenant data returned or mixed.
- **server/heartbeat.ts:113** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} lacks tenant qualification. This can advance/modify another tenant's schedule if ids are misaligned or leak occurs.
  - _Why false positive:_ Global scheduler advances next_run_at for the iterated schedule row only as maintenance behavior.
- **server/heartbeat.ts:385** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ORDER BY ... LIMIT 10 executes without tenant_id filtering. This reads pending events across all tenants into a per-event processing loop that assumes tenant_id later — it violates explicit tenant-scoping at read time.
  - _Why false positive:_ Pending-event worker intentionally sweeps across tenants and processes each event with its own tenant_id in isolation.
- **server/heartbeat.ts:1670** _(global-table)_ — SELECT created_at FROM heartbeat_logs ... WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 has no tenant_id constraint. This causes cross-tenant behavior: a successful run in tenant A could prevent runs for tenant B.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this is a platform-global log table rather than tenant-scoped data.
- **server/heartbeat.ts:273** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} updates a tenant-scoped table using only id with no tenant_id constraint.
  - _Why false positive:_ Startup heartbeat cleanup is a platform-wide maintenance pass normalizing housekeeping fields on task rows, not tenant-facing data access.
- **server/heartbeat.ts:277** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} similarly lacks a tenant filter and may modify another tenant's row.
  - _Why false positive:_ Same startup maintenance sweep updating only model metadata across due heartbeat rows, with no tenant data returned or mixed.
- **server/heartbeat.ts:2764** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without constraining tenant_id. This writes approval_status on a heartbeat_tasks row without verifying its tenant ownership.
  - _Why false positive:_ newTask is created immediately above with the same tenantId via storage.createHeartbeatTask; follow-up update targets that freshly created row by returned id.
- **server/heartbeat.ts:3216** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} again omits tenant_id. Updating heartbeat_tasks without tenant scoping can affect other tenants.
  - _Why false positive:_ newTask is created in the same call path with tenantId set, then updated by its returned id; ownership is established by the preceding create.
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs performs UPDATE/SELECT on agent_jobs without constraining by tenant_id. This atomically claims jobs across all tenants, violating per-tenant scoping invariant.
  - _Why false positive:_ Global worker queue claim intentionally operates across all tenants and returns row-carried tenant_id for per-job processing, matching platform-wide worker sweep behavior.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases updates agent_jobs (marking terminal or requeued) without any tenant_id constraint, affecting jobs for all tenants in bulk.
  - _Why false positive:_ Expired-lease reclaimer is cross-tenant housekeeping that only mutates job status/lease fields and does not expose or mix tenant content.
- **server/job-queue.ts:371** _(upstream-verified)_ — cancelPendingJobsForTenant builds a text[] array literal via string interpolation (excludeLiteral = `{...}`) and injects it into SQL. Although sanitized, this is string-interpolated SQL using user-controlled input (opts.excludeKinds) and risks SQL injection / isolation mistakes.
  - _Why false positive:_ The UPDATE is explicitly tenant-scoped with WHERE tenant_id = tenantId; finding is about string-built array syntax, not a cross-tenant isolation failure.
- **server/knowledge-graph.ts:198** _(upstream-verified)_ — loadDurableTriples issues an UPDATE using id = ANY(${`{${ids.join(",")}}`}::int[]) where the array literal is built by string-interpolating ids into the SQL. This is string-interpolated SQL with runtime-derived values (ids).
  - _Why false positive:_ The UPDATE includes WHERE tenant_id = tenantId, so tenant ownership is already enforced despite the array-literal construction issue.
- **server/lib/bwb-job-progress.ts:200** _(internal-worker-pk)_ — SELECT from tenant-scoped table video_jobs uses .where(scope(jobId)) — scope() can omit tenant_id, so this read can return another tenant's row when BWB_TENANT_ID is not present.
  - _Why false positive:_ Server-invoked job progress helper uses env/server-supplied jobId, not request input; worker updates one job row by server-generated identifier.
- **server/lib/bwb-job-progress.ts:211** _(internal-worker-pk)_ — UPDATE on tenant-scoped table video_jobs uses .where(scope(jobId)) — scope() may omit tenant_id, allowing an update that affects rows across tenants when env BWB_TENANT_ID is not set.
  - _Why false positive:_ Internal background completion path updates a single video_jobs row by server/job-process jobId rather than untrusted tenant request data.
- **server/lib/bwb-job-progress.ts:230** _(internal-worker-pk)_ — UPDATE (heartbeat) against tenant-scoped table video_jobs uses .where(scope(jobId,...)) — scope() can drop tenant_id, permitting cross-tenant heartbeat updates when BWB_TENANT_ID is missing.
  - _Why false positive:_ Heartbeat bump is an internal subprocess timer touching one server-owned job by jobId, not a cross-tenant request boundary.
- **server/lib/bwb-job-progress.ts:245** _(internal-worker-pk)_ — UPDATE marking job failed writes to tenant-scoped table video_jobs via .where(scope(jobId,...)); scope() may omit tenant_id, enabling a cross-tenant state change (fail/phase) when BWB_TENANT_ID is not present.
  - _Why false positive:_ Failure marking is an internal worker state transition keyed by server-side jobId, matching the benign worker-by-PK/state-transition pattern.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — Query reads the tenant-scoped `projects` table across ALL tenants (SELECT tenant_id, count(*) ... FROM projects WHERE tags && ...) without constraining to the caller's tenant(s). This exposes/aggregates data for tenants other than the intended one and violates the invariant that every read against a tenant-scoped table must be constrained by tenant_id.
  - _Why false positive:_ Boot/backfill routine intentionally scans counts across snapshot tenants only to decide whether to seed; it returns no tenant data and performs housekeeping/backfill work.
- **server/lib/mention-injection.ts:121** _(fk-transitive)_ — SELECT from channel_messages LEFT JOIN personas joins on persona id only (ON p.id = cm.from_persona_id) but does not constrain the joined personas row to the same tenant. This can leak/read persona.name from another tenant.
  - _Why false positive:_ Schema note says personas is not tenant-scoped; join cannot require personas.tenant_id, so this is not a tenant-isolation leak on a tenant-scoped joined table.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id filter (CTE selects due rows across all tenants and UPDATE matches only on id). This reads/updates tenant-scoped rows across tenants without per-tenant constraint.
  - _Why false positive:_ Global scheduler intentionally claims due rows across tenants, returns each row with its own tenant_id, and performs per-row publishing/housekeeping rather than mixing tenant data into one tenant-facing output.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` does not filter by tenant_id. The query reads rows across all tenants (WHERE status = 'running' ... ) without tenant scoping.
  - _Why false positive:_ Read-only cross-tenant maintenance audit over stuck rows returns bounded diagnostics with row-carried tenant_id; it is not serving tenant-facing mixed content.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT from tenant-scoped table `mind_tickets` does not filter by tenant_id. The query reads rows across all tenants (WHERE status = 'in_progress' ... ) without tenant scoping.
  - _Why false positive:_ Read-only stuck-task audit intentionally scans globally for maintenance/reporting and carries tenant_id per row without tenant content mixing.
- **server/memory-graph.ts:123** _(upstream-verified)_ — classifyMemoryCategory (empty-categories branch) increments memory_categories.memory_count with an UPDATE that lacks a tenant_id constraint (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}). This can mutate another tenant's row by id.
  - _Why false positive:_ catId comes from ensureCategory(category, tenantId, personaId), which selects/inserts memory_categories scoped by the same tenantId immediately above.
- **server/memory-graph.ts:494** _(upstream-verified)_ — categorizeExistingMemories updates memory_entries with WHERE id = ${mem.id} but omits tenant_id in the WHERE clause (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}). Even though the rows were selected for a given tenant earlier, the UPDATE is unscoped and could affect a row id belonging to another tenant.
  - _Why false positive:_ mem.id is taken from a prior SELECT on memory_entries filtered by tenant_id = tenantId in the same function loop.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — extractTriples updates knowledge_triples rows by id only (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}) after selecting contradictions filtered by tenant_id. The subsequent UPDATE lacks a tenant_id check and could update another tenant's row if ids are reused or ownership differs.
  - _Why false positive:_ c.id comes from contradictions selected just above from knowledge_triples with tenant_id = tenantId in the same call path.
- **server/minds-engine.ts:262** _(upstream-verified)_ — checkDependenciesMet builds an ANY(...) array literal by joining ticket.dependsOn: `WHERE id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]) AND tenant_id = ${tenantId}`. The dependsOn value originates from stored/earlier input and is used to compose SQL text rather than a parameter, risking injection or malformed queries and weakening isolation guarantees.
  - _Why false positive:_ Even with manual array literal construction, the queried rows are still constrained by AND tenant_id = tenantId; this is not a cross-tenant isolation gap.
- **server/minds-engine.ts:214** _(upstream-verified)_ — createTicket constructs a Postgres array literal for depends_on by interpolating params.dependsOn: `${params.dependsOn && params.dependsOn.length > 0 ? `{${params.dependsOn.join(",")}}` : null}`. This injects caller-supplied data into SQL text rather than using a parameter, enabling SQL-injection / malformed-array risks and undermining tenant-scoped integrity if misused.
  - _Why false positive:_ INSERT writes tenant_id = params.tenantId on the new row; this finding is about array-literal construction, not missing tenant isolation.
- **server/orchestrator-ledger.ts:458** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` does not include a tenant_id constraint: `UPDATE memory_entries SET ... WHERE id = ${row.id}`. Per the tenant-isolation invariant, every write to a tenant-scoped table must constrain by tenant_id.
  - _Why false positive:_ row.id is loaded immediately before from memory_entries filtered by source, category, and tenant_id = tenantId inside the same transaction.
- **server/pace-control.ts:85** _(global-table)_ — Query against tenant-scoped table `heartbeat_logs` is not constrained by tenant_id: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}`. The function does not accept a tenantId and aggregates across all tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, matching an intentionally platform-global throttle/log table.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries `heartbeat_logs` without tenant scoping: `SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...`. This returns global/mixed-tenant data rather than per-tenant and violates the tenant-isolation requirement.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column and this function returns aggregate pace/debug snapshot data by design.
- **server/persona-export.ts:33** _(global-table)_ — Read of persona by id without tenant constraint: `SELECT * FROM personas WHERE id = ${personaId}`. This reads a tenant-scoped row (persona) using a foreign id with no ownership/tenant verification (rule #3).
  - _Why false positive:_ Schema note says personas has no tenant_id column, so this read is from a non-tenant-scoped/global table.
- **server/persona-export.ts:39** _(global-table)_ — Query against `skills` lacks tenant scoping: `SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL ...`. Without a tenant_id constraint this may return skills from other tenants.
  - _Why false positive:_ Schema note says skills has no tenant_id column; query intentionally includes persona-specific and global (persona_id IS NULL) skills from a non-tenant-scoped table.
- **server/process-governor.ts:121** _(global-table)_ — Subquery reads heartbeat_logs without constraining tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads a tenant-scoped table without tenant isolation.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so counting logs by task_id is against a global log table rather than a tenant-scoped one.
- **server/process-governor.ts:199** _(global-table)_ — Query over heartbeat_logs to compute AVG(duration_ms) lacks tenant filtering (WHERE created_at > ... AND duration_ms IS NOT NULL AND status = 'success'). This reads a tenant-scoped table without tenant isolation.
  - _Why false positive:_ heartbeat_logs schema note shows no tenant_id column, so tenant filter is not applicable
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures query reads heartbeat_logs without a tenant filter (COUNT(DISTINCT persona_id) FROM heartbeat_logs WHERE ...). This can count other tenants' agents.
  - _Why false positive:_ heartbeat_logs has no tenant_id per schema note; counting failures is over a non-tenant-scoped table
- **server/process-governor.ts:585** _(global-table)_ — In disable_task the subselects counting heartbeat_logs (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id AND ...) do not include tenant_id, so they read tenant-scoped heartbeat_logs without tenant constraint.
  - _Why false positive:_ heartbeat_logs has no tenant_id column, so these subselects cannot add tenant scoping at this table
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} updates by id only without scoping to tenant_id. Because ids may collide across tenants, this can modify another tenant's task.
  - _Why false positive:_ t.id comes only from prior SELECT of heartbeat_tasks scoped by ht.tenant_id = tenantId in same call path
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} performs a mutation by id alone with no tenant constraint. This can affect agent_desks belonging to other tenants with the same id.
  - _Why false positive:_ desk.id is sourced from earlier SELECT on agent_desks with ad.tenant_id = tenantId in same function
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selects DISTINCT p.id, p.name FROM projects JOIN conversations ... and checks messages without any tenant scoping. Projects/conversations/messages are tenant-scoped but no tenant_id filters are used.
  - _Why false positive:_ boot/backfill routine intentionally scans across tenants to create per-project local brain files; not tenant-facing per-row request handling
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping. Conversations are tenant-scoped and must be filtered by tenant_id.
  - _Why false positive:_ backfill loop is a platform-wide housekeeping pass over each project/conversation, not a tenant-request query
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant restriction. Messages are tenant-scoped (conversation-scoped) and must be restricted to the caller's tenant.
  - _Why false positive:_ message reads occur inside the same backfill housekeeping sweep for each conversation, not a tenant-facing mixed response
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at / marking race outcome) updates a tenant-scoped table by id without tenant filtering.
  - _Why false positive:_ Background verifier worker updates a server-supplied proposal serial id, not request-derived input.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist() issues UPDATE code_proposals SET verification_status = ${status} ... WHERE id = ${proposalId} without a tenant_id constraint. This mutates a tenant-scoped row by id only.
  - _Why false positive:_ persist() is used by the verifier worker on a server-controlled proposal id, not a tenant request boundary.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — In attemptRebaseOnDrift the UPDATE code_proposals SET status='rejected', ... WHERE id = ${proposalId} is executed with no tenant constraint; this mutates tenant-scoped rows without verifying ownership.
  - _Why false positive:_ Rebase helper is part of internal verifier flow and mutates by server-controlled proposal primary key.
- **server/proposal-verifier.ts:396** _(internal-worker-pk)_ — UPDATE code_proposals SET validation_result = ... WHERE id = ${proposalId} (mark 'hallucinated' outcome) updates code_proposals without tenant scoping.
  - _Why false positive:_ Internal verifier marks rebase outcome on a proposal row addressed by trusted server-side id.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diff back to the proposal but lacks tenant filtering on the UPDATE.
  - _Why false positive:_ Internal worker persists rebased diff to proposal by server-generated/loaded primary key, not untrusted caller input.
- **server/recurring-messages.ts:258** _(system-sweep)_ — SELECT from tenant-scoped table `agent_knowledge` is performed without a tenant_id filter (SELECT id,title,content,tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500). Unscoped reads of tenant data increase risk of accidental cross-tenant access.
  - _Why false positive:_ Heartbeat runner intentionally scans due jobs across tenants and then handles each row with its own tenant_id.
- **server/routes.ts:167** _(system-sweep)_ — Startup restoreUploadsFromDb reads file_storage rows without scoping by tenant (db.select(...).from(fileStorage).where(like(mimeType,'image/%'), isNull(storageKey))). This pulls raw file data for all tenants into the process and can lead to cross-tenant data exposure.
  - _Why false positive:_ Boot-time restore processes all image rows platform-wide and only writes files to disk, not tenant-facing mixed output.
- **server/routes.ts:4342** _(fk-transitive)_ — Raw INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The INSERT: INSERT INTO project_notes (project_id, note, author) VALUES (...) — tenant_id is not set, and there is no DB row-level security or FK cascade to enforce tenancy.
  - _Why false positive:_ project_notes has no tenant_id and is inserted via tenant-scoped parent project resolved from conversation in same tenant.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on tenant-scoped table `inbox_messages` omits tenant_id in the WHERE clause (`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`). This relies on a prior SELECT for ownership and is vulnerable to TOCTOU / accidental cross-tenant mutation.
  - _Why false positive:_ Same handler first SELECTs the exact inbox_messages row by id and tenant_id before marking that fetched row read.
- **server/sculptor.ts:106** _(upstream-verified)_ — UPDATE against tenant-scoped table `sculptor_sessions` uses WHERE id = ${session.id} with NO tenant_id constraint.
  - _Why false positive:_ This updates the session row just inserted/returned in the same function with tenant_id set from params.tenantId.
- **server/sculptor.ts:112** _(upstream-verified)_ — UPDATE against tenant-scoped table `sculptor_sessions` sets conversation_id but omits tenant_id in the WHERE clause (WHERE id = ${session.id} only).
  - _Why false positive:_ This updates the freshly inserted session row created in the same call with the caller's tenant_id already fixed.
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `agent_cost_ledger` sets settled cost using WHERE id = ${reservationId} with NO tenant_id constraint.
  - _Why false positive:_ internal helper settles a server-created reservation row by primary key, not from request boundary
- **server/second-opinion.ts:424** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `agent_cost_ledger` (releaseFusionReservation) uses WHERE id = ${reservationId} with NO tenant_id constraint.
  - _Why false positive:_ internal helper releases a server-created reservation row by primary key, not caller-controlled
- **server/seed-cleanup.ts:8** _(system-sweep)_ — SELECT count(*) FROM tenant-scoped table `heartbeat_tasks` omits tenant_id — reads global counts across tenants.
  - _Why false positive:_ boot/seed housekeeping count across tenants used only to drive maintenance, not tenant-facing data output
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true has NO tenant_id constraint — will disable tasks across all tenants.
  - _Why false positive:_ platform-wide cleanup intentionally disables runaway tasks as housekeeping across tenants
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' omits tenant_id — reads across tenants.
  - _Why false positive:_ cross-tenant count in maintenance reconciler only determines housekeeping action, no tenant data exposed
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant_id, allowing cross-tenant modifications.
  - _Why false positive:_ platform-wide cleanup disabling duplicate session-logger tasks is housekeeping across tenants
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) omits tenant_id — counts tasks across tenants.
  - _Why false positive:_ maintenance scan counts orphaned repo-examination tasks across tenants for cleanup only
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false ... AND enabled = true has NO tenant_id constraint — will disable matching tasks platform-wide.
  - _Why false positive:_ platform-wide housekeeping update disables orphaned repo-examination tasks without returning mixed tenant data
- **server/seed.ts:2158** _(system-sweep)_ — SELECT from tenant-scoped table research_programs without restricting by tenant_id (SELECT id, name, model FROM research_programs WHERE is_active = true). This reads across all tenants.
  - _Why false positive:_ seed-time model backfill intentionally iterates active programs across tenants and only fixes model field per row
- **server/seed.ts:36** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions without a tenant filter (SELECT COUNT(*) as count FROM research_sessions). This counts sessions across all tenants rather than the caller's tenant.
  - _Why false positive:_ dev snapshot import uses global count as one-time bootstrap guard, not tenant-facing access or mixed output
- **server/seed.ts:3906** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` is executed without constraining to a tenant (no WHERE tenant_id = ...). This will modify rows across all tenants.
  - _Why false positive:_ Boot-time seed normalizer intentionally updates rows across tenants; housekeeping mutation only and no tenant-facing mixed output.
- **server/seed.ts:3932** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` (SET model = 'gpt-5') is performed without a tenant_id constraint, affecting all tenant rows.
  - _Why false positive:_ Global seed backfill intentionally rewrites legacy model ids across all conversation rows; housekeeping migration only.
- **server/seed.ts:3878** _(system-sweep)_ — DELETE FROM `heartbeat_tasks` performed without any tenant_id constraint. This will delete matching disabled tasks across all tenants.
  - _Why false positive:_ Boot-time cleanup intentionally purges stale auto-disabled heartbeat rows across tenants; housekeeping delete with aggregate logging only.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from messages omits tenant scoping: recentMsgs query uses only conversationId (eq(messages.conversationId, conversationId)) with no tenant_id constraint, risking cross-tenant data disclosure.
  - _Why false positive:_ Same call path first calls analyzeConversation(conversationId, tenantId), which verifies the conversation belongs to tenantId before recentMsgs are read for that conversation.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from messages in analyzeConversation omits tenant scoping: messages are queried by conversationId only (eq(messages.conversationId, conversationId)) with no tenant_id constraint.
  - _Why false positive:_ Immediately above, conversation row is fetched with id+tenantId and not-found aborts; messages are then read only for that verified conversationId in same function.
- **server/skillify.ts:221** _(global-table)_ — SELECT from personas lacks tenant constraint: personaRows lookup uses eq(personas.id, conv[0].personaId) but does not restrict by tenant_id, so persona data from other tenants could be disclosed.
  - _Why false positive:_ Schema note shows personas has no tenant_id column, so tenant scoping is not applicable to this lookup.
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata SELECTs `messages` by id without scoping to tenant_id (reads tenant-scoped table without tenant constraint).
  - _Why false positive:_ attachCitationMetadata only re-reads ids from already tenant-scoped msgs supplied by getMessages/getMessagesPaginated, so ownership is established earlier in the same call path.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` filters only by name (eq(customTools.name, parsed.name)) and does not include tenant_id, allowing cross-tenant reads/matches.
  - _Why false positive:_ name is rewritten with tenantToolName(parsed.name, tenantId), so lookup is already tenant-qualified via tenant-specific name
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT custom_tools WHERE id = inserted.id is performed without constraining by tenant_id.
  - _Why false positive:_ row was just inserted with trusted tenantId and returned inserted.id is server-derived unique pk
- **server/tools.ts:2890** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. Writing to a tenant-scoped table without setting tenant_id allows cross-tenant contamination or violates the rule that tenant_id must be explicitly provided on insert.
  - _Why false positive:_ project_conversations has no tenant_id and is linked only after project and conversation are tenant-validated
- **server/tools.ts:2994** _(fk-transitive)_ — INSERT into project_files omits tenant_id. Tenant-scoped tables require tenant_id to be explicitly set on INSERT; omitting it breaks the invariant and can result in cross-tenant writes or DB errors if tenant_id is NOT NULL.
  - _Why false positive:_ project_files has no tenant_id and parent project ownership is checked before insert
- **server/tools.ts:3008** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. Writing to a tenant-scoped table without tenant_id violates the requirement that every INSERT include the caller tenant and may allow cross-tenant contamination.
  - _Why false positive:_ project_notes has no tenant_id and parent project ownership is checked before insert
- **server/tools.ts:3032** _(fk-transitive)_ — INSERT into project_conversations (link path) omits tenant_id. This is a write into a tenant-scoped table without explicitly setting tenant_id, violating the requirement to always set tenant_id on INSERTs.
  - _Why false positive:_ project_conversations has no tenant_id; project ownership and conversation tenant ownership are validated before insert
- **server/tools.ts:4552** _(fk-transitive)_ — INSERT into tenant-related table `project_notes` does not include tenant_id. The INSERT INTO project_notes (project_id, note, author, created_at) VALUES (...) omits tenant_id, violating the rule that every INSERT into a tenant-scoped table must set tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id and project ownership is asserted before insert
- **server/tools.ts:4556** _(fk-transitive)_ — INSERT into tenant-related table `project_files` does not include tenant_id. The INSERT INTO project_files (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (...) omits tenant_id.
  - _Why false positive:_ project_files has no tenant_id and project ownership is asserted before insert
- **server/tools.ts:6405** _(fk-transitive)_ — INSERT into tenant-related table `project_files` (audio upload path) omits tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) is missing tenant_id.
  - _Why false positive:_ project_files has no tenant_id and assertProjectInTenant enforces parent project ownership before insert
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table `browser_workflows` lacks a tenant_id constraint. The statement `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` can modify a workflow belonging to another tenant if an id is reused or attacker-controlled.
  - _Why false positive:_ workflow row is selected immediately above with tenant_id filter, then updated by that verified row id
- **server/tools/domains/files/handlers.ts:907** _(upstream-verified)_ — UPDATE on `projects` omits tenant scoping. The code calls `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` without constraining by tenant_id, allowing updates to projects outside the caller's tenant.
  - _Why false positive:_ projId comes from conversation->project join constrained by p.tenant_id = ctx.tenantId just above
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE statement on tenant-scoped table `knowledge_triples` is missing an explicit tenant_id constraint. The code executes `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` without `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ each row.id being updated was selected immediately above from knowledge_triples with tenant_id = tenantId
- **server/tools/domains/strategic-memory/handlers.ts:93** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${id} with no tenant_id constraint. Although id was discovered via a previous tenant-scoped SELECT, the UPDATE has no tenant guard and could affect rows across tenants (TOCTOU / misuse risk).
  - _Why false positive:_ id is obtained from prior SELECT on memory_entries scoped by tenant_id and persona/category in same flow
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — Bulk UPDATE on `memory_entries` uses WHERE id = ANY(${ids}) without tenant_id constraint. ids are derived from a prior tenant-scoped SELECT, but the UPDATE itself lacks tenant scoping and may update rows across tenants if ids are tampered with or in a race.
  - _Why false positive:_ ids array is derived from rows selected just above with tenant_id/persona/category filters in same call path
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE on `memory_entries` performed with WHERE id = ${id} and missing tenant_id filter (record_strategic_win path). The prior SELECT ensured the id belonged to the tenant, but the UPDATE itself is not tenant-scoped, creating a risk.
  - _Why false positive:_ id was selected immediately above from memory_entries with tenant_id and persona/category constraints
- **server/commitment-drafter.ts:232** _(upstream-verified)_ — Unscoped UPDATE on tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` is executed without constraining WHERE tenant_id = ${tenantId}. This can modify rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/embeddings.ts:742** _(system-sweep)_ — UPDATE against tenant-scoped table (memory_entries | agent_knowledge) in storeEmbeddingVec does not constrain by tenant_id. The function updates embedding_vec / embedding by id only, allowing cross-tenant writes.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` omits tenant_id in the WHERE clause (UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}). This allows an update to affect a row belonging to any tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The runner is a cron loop reading recurring_message rows (recurring-messages.ts:258-263 selecting id + tenant_id); r.id is a server DB PK from that same query and the UPDATE at :300 only writes back the recomputed payload/updated_at for that same row. No untrusted id, no tenant-facing read.
- **server/reference-learner.ts:291** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits tenant_id in the WHERE clause (UPDATE memory_entries SET fact = ..., last_accessed = NOW() WHERE id = ${storedId}). This can update rows across tenants if ids collide or are mis-used.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storedId used in the UPDATE at server/reference-learner.ts:290 is taken from the existing SELECT at reference-learner.ts:281-287 which filters WHERE tenant_id = ${input.tenantId} AND persona_id = ${personaId}. Ownership is proven in the same call path immediately before the write.
- **server/self-improvement.ts:401** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) in WHERE — no tenant_id constraint. This can modify rows belonging to other tenants if IDs collide or are guessed.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/self-improvement.ts:415** _(internal-worker-pk)_ — Error-path UPDATE on tenant-scoped `experiments` (catch block) updates by id only with no tenant_id constraint, allowing cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:398** _(internal-worker-pk)_ — attachCitations UPDATE on tenant-scoped table `messages` writes without any tenant_id constraint (UPDATE messages SET ... WHERE id = ${messageId}).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:1304** _(upstream-verified)_ — getAllDataForExport issues multiple SELECTs that read tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) with NO tenant filters — platform-wide export of tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `custom_tools` (setting implementation during auto-fix) uses WHERE eq(custom_tools.id, tool.id) without also scoping by tenant_id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE FROM `custom_tools` in revertToolRow uses only the id in the WHERE clause and omits tenant_id, risking deletion of other tenants' data if IDs collide or are misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id constraint. This reads pairing rows across all tenants (title = <channel:phone>) and may disclose or allow processing of cross-tenant pairing data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
- **server/agentic-engines.ts:534** _(global-table)_ — SELECT from `heartbeat_tasks` (taskConfig) uses WHERE enabled = true but omits tenant_id; heartbeat_tasks is tenant-scoped elsewhere. This reads tasks across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/agentic-features.ts:638** _(upstream-verified)_ — Lookup for existing enrollment misses tenant scoping (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'). This can match an active enrollment belonging to another tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from agent_knowledge in getWatermark() has no tenant_id constraint — reads watermark globally across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-memorize.ts:68** _(internal-worker-pk)_ — UPDATE agent_knowledge SET ... WHERE id = ${existing.rows[0].id} does not constrain by tenant_id — update could affect another tenant's row.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): singleton watermark row; id from its own category+title SELECT one statement above (system row, tenant 1 by design).
- **server/capability-review.ts:490** _(internal-worker-pk)_ — markReviewReused updates capability_reviews without any tenant_id constraint and the function has no tenant context. The pendingReuseByConversation entry does not include tenantId, so this UPDATE (WHERE id = ${pending.reviewId} AND reused = false) can modify a row belonging to another tenant if ids collide or if an attacker manipulates conversation IDs.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/capability-review.ts:491 — markReviewReused UPDATEs capability_reviews.reused (a telemetry flag) WHERE id = pending.reviewId. reviewId is a server-generated PK stored in the in-process pendingReuseByConversation map keyed by the conversationId of the review that created it — it is not attacker-influenced and maps 1:1 to the review just recorded for that conversation's tenant. Only a bo
- **server/chat-engine.ts:4105** _(internal-worker-pk)_ — Call to recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) uses conversationId but does not pass tenantId or perform an explicit ownership check before invoking. If this function reads/writes tenant-scoped tables it can violate tenant isolation.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): recordToolCallForStuckDetection is in-memory loop diagnostics keyed by conversationId; no DB write occurs.
- **server/chat-engine.ts:4092** _(global-table)_ — Calls to recordToolExecution(conversationId, toolName, ...) are made without passing tenantId or performing an ownership check on conversationId. If recordToolExecution writes to tenant-scoped tables (tool execution logs / identifiers) those writes may lack a tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Same helper recordToolExecution (felix-brain.ts:298-320) — in-memory Map only, no DB/tenant table.
- **server/delivery-pipeline.ts:1142** _(global-table)_ — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with NO tenant_id constraint. Returning a delivery by payment id without tenant scoping can expose another tenant's delivery row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:93** _(signed-url)_ — submitDfyIntake updates dfy_intake_forms (responses/status/submittedAt) using WHERE eq(dfyIntakeForms.id, form.id) with NO tenant_id constraint. This allows updating a form row without verifying it belongs to the caller's tenant.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): form row fetched by unguessable 48-hex token (the documented authorization for the public intake form); UPDATE reuses that row's own id.
- **server/email.ts:471** _(system-sweep)_ — startInboxPolling / pollSharedInbox checks for existing inbox_messages with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 (no tenant_id). This lookup can collide across tenants and cause processing/visibility mixups.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): platform primary-inbox poller (single system account); message_id dedupe against its own inbox rows.
- **server/lib/bwb-job-progress.ts:141** _(internal-worker-pk)_ — UPDATE on tenant-scoped table video_jobs performed via .where(scope(jobId,...)) — scope() may omit tenant_id when BWB_TENANT_ID is not set, allowing cross-tenant updates pinned only to jobId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:169** _(internal-worker-pk)_ — UPDATE on tenant-scoped table video_jobs (updateBwbChapters) uses .where(scope(jobId,...)) which can omit tenant_id, permitting cross-tenant modification based solely on jobId when env BWB_TENANT_ID is missing.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/system-probes.ts:163** _(upstream-verified)_ — db.select(...).from(messagesTable) is executed without a tenant_id WHERE clause (a raw count(*) on messages is run across all tenants). This reads tenant-scoped message rows globally without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Reclassified by main-agent review: only callsite (tools.ts check_system_status) is admin-gated (ADMIN_TENANT_ID) two lines above the call. server/lib/system-probes.ts:163 db.select(count(*)).from(messagesTable) has no tenant filter; messages has tenant_id (shared/schema.ts:142-150). checkSystemStatus (119) is exposed as the check_system_status tool (server/tools.ts:3501, definitions.ts:24-31, 
- **server/memory-graph.ts:183** _(upstream-verified)_ — refreshCategoryCount updates memory_categories without constraining by tenant_id. The UPDATE uses WHERE id = ${categoryId} only, allowing a caller to modify another tenant's category count by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): refreshCategoryCount receives categoryId from ensureCategory (server/memory-graph.ts:120,132) which is tenant-scoped (:51,:57); category_id belongs to the same tenant, id never request-derived.
- **server/process-governor.ts:155** _(global-table)_ — Subquery counting heartbeat_tasks for persona pending_tasks omits tenant restriction: (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id AND enabled = true). This reads a tenant-scoped table without tenant isolation.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): heartbeat-task pace counting is a platform-wide throttle by design (same family as checkPace/heartbeat_logs); per-tenant fairness would need a schema migration and is tracked separately.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates a rule by id without verifying tenant_id. This allows cross-tenant mutation if ids collide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} reads the projects table by id with no tenant constraint. Projects are tenant-scoped so this can expose another tenant's project data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId} reads a proposal by id with no tenant constraint. code_proposals is tenant-scoped and this may expose or operate on another tenant's proposals.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/proposal-verifier.ts:31-35 SELECT by proposalId; every caller is internal (server/agentic/autonomous-closer.ts:86-103 with tenant-scoped candidate ids, plus scripts/reverify-proposals.ts and scripts/drain-and-verify-proposals.ts). No request/tool-arg id path.
- **server/sculptor.ts:167** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `sculptor_sessions` (monitorSession) uses WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:358** _(upstream-verified)_ — UPDATE against tenant-scoped table `sculptor_sessions` (writing review_result) uses WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): reviewSessionWork fetches the session WHERE id AND tenant_id at the top and returns early if absent; the later UPDATE by id targets the already-tenant-verified row.
- **server/self-improvement.ts:170** _(upstream-verified)_ — SELECT from tenant-scoped table `experiments` may run without a tenant filter. detectStagnation builds conditions with only category when tenantId is not provided, then executes db.select().from(experiments) with that single condition — allowing cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/self-improvement.ts:521** _(upstream-verified)_ — getExperimentHistory will run db.select().from(experiments) without any tenant filter when tenantId is omitted, returning cross-tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): The only unscoped caller is server/routes.ts:6290-6298 which is gated by isAdminRequest (6293) — intentional admin cross-tenant telemetry. The tool caller server/tools/domains/self-improvement/handlers.ts:66 passes ctx.tenantId, scoping the query (514-519).
- **server/storage.ts:242** _(upstream-verified)_ — getConversationUnscoped performs a SELECT from tenant-scoped table `conversations` with NO tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:524** _(upstream-verified)_ — createMemoryEntry inserts into tenant-scoped table `memory_entries` but does not validate that data.tenantId is present and a positive integer before the INSERT.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): InsertMemoryEntry.tenantId is schema-required (memory_entries.tenant_id NOT NULL, no default) — the type system forces every caller to supply it.
- **server/storage.ts:805** _(internal-worker-pk)_ — updateMemoryEmbedding updates `memory_entries` embedding using only eq(memory_entries.id, id) with NO tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding updates `agent_knowledge` embedding using only eq(agent_knowledge.id, id) with NO tenant_id constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:482** _(global-table)_ — deletePersona transaction updates tenant-scoped tables (`conversations`, `memory_entries`, `daily_notes`) by personaId without scoping those updates to a tenant_id. These updates can affect rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:479 — The personas table has no tenant_id column — personas are global/platform-wide by design (mapped per-tenant only via tenant_persona_names). deletePersona is invoked solely from DELETE /api/personas/:id, which is gated by requirePlatformAdmin (routes/personas.ts:134-136). The cascade cleanup of conversations/memory_entries/daily_notes by persona_id is an intended platfo
- **server/tools.ts:6939** _(fk-transitive)_ — INSERT into tenant-related table `project_files` (slideshow video) omits tenant_id. The INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...) does not set tenant_id explicitly.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column (isolated via project_id FK); server/tools.ts:2969 owner check SELECT id FROM projects WHERE id=params.id AND tenant_id=tenantId gates the insert
- **server/tools.ts:6939** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The statement db.execute(sql`INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`) omits tenant scoping so the row could be inserted without tenant ownership recorded, breaking tenant isolation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): shared/schema.ts:1357-1367 project_files has NO tenant_id column (isolated via project_id FK); server/tools.ts:2969 owner check SELECT id FROM projects WHERE id=params.id AND tenant_id=tenantId gates the insert
- **server/delivery-pipeline.ts:255** _(deferred)_ — updateDeliveryLog performs an UPDATE on delivery_logs without scoping by tenant_id (WHERE only matches id). This allows a caller that can trigger updateDeliveryLog to modify another tenant's delivery row.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/minerva-planner.ts:549** _(deferred)_ — getPlan allows calling code to omit tenantId and performs `SELECT * FROM plans WHERE id = ${planId}` with NO tenant_id constraint. This SELECT against a tenant-scoped table can return cross-tenant data if invoked without a tenant filter (IDOR risk).
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
