# Tenant-Isolation Audit Report

_Generated 2026-08-06T23:08:18.086Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **587/587** file(s) audited · chunks 55/55 OK (0 failed)
- Findings: **299** (CRITICAL 74 · HIGH 189 · MEDIUM 35 · LOW 1)
- Precision second-pass (`gpt-5.4`): **249** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (74)

- **server/agentic-engines.ts:243** — autoApplyEligibleInsights executes a non-tenant-scoped UPDATE: "UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ..." — this bulk-updates rows for all tenants.
  - _Fix:_ Do not run a cross-tenant UPDATE. Restrict the UPDATE to a tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or run the policy per-tenant. Ensure any RETURNING rows are limited to the intended tenant before post-processing and routing.
- **server/agentic/wake-scheduler.ts:142** — runDueWakes updates agent_wake_schedules via an id IN (SELECT id FROM agent_wake_schedules ... FOR UPDATE SKIP LOCKED) subquery that contains no tenant_id filter. The outer UPDATE also has no tenant_id constraint. This allows the scheduler to claim/update pending wakes across all tenants (cross-tenant mutation/visibility).
  - _Fix:_ Constrain the claim/update to a specific tenant or perform per-tenant claims. Add an explicit tenant_id = ${tenantId} predicate to both the inner SELECT and the outer UPDATE (or otherwise supply and bind a tenantId when calling runDueWakes). Alternatively, iterate tenants and run the FOR UPDATE SKIP LOCKED selection per-tenant, or include tenant_id in the SELECT and UPDATE (e.g. WHERE tenant_id = ${tenantId} AND status = 'pending' AND wake_at <= now()). Ensure every read/update on agent_wake_schedules includes tenant_id to restore proper tenant isolation.
- **server/auto-memorize.ts:62** — setWatermark performs SELECT/UPDATE/INSERT on agent_knowledge without scoping to the caller tenant and the INSERT hardcodes tenant_id = 1. This can overwrite or create watermarks for the wrong tenant.
  - _Fix:_ Accept a tenantId parameter and: (1) SELECT/UPDATE only rows WHERE tenant_id = ${tenantId}; (2) when inserting, set tenant_id = ${tenantId} (do not hardcode). Ensure all writes/updates include AND tenant_id = ${tenantId}.
- **server/auto-memorize.ts:163** — SELECT from messages (pull recent messages window) lacks any tenant_id filter. The code then sends per-tenant transcripts to LLMs; an unscoped SELECT can return messages across tenants and leak data to other tenants' prompts.
  - _Fix:_ Scope the messages SELECT by tenant (e.g. AND m.tenant_id = ${tenantId}) or iterate per-tenant by querying a list of tenants and running tenant-scoped queries for each. Never perform cross-tenant message reads that are not explicitly and safely partitioned by tenant_id.
- **server/chat-engine.ts:3623** — Call to logDecision(conversationId, ...) uses a foreign conversation_id but does not pass/verify tenant context. This may result in writes/reads against a tenant-scoped table without constraining by tenant_id (violates tenant-isolation invariant).
  - _Fix:_ Pass the caller's tenantId into logDecision or have logDecision require and enforce tenant scoping. Alternatively, query/verify the conversation row first (SELECT ... WHERE id = conversationId AND tenant_id = <tenant>) and fail if not owned, before calling logDecision.
- **server/chat-engine.ts:4038** — recordToolExecution(conversationId, toolName, ...) is invoked with a foreign conversation_id but no tenant identifier or prior ownership check. If recordToolExecution writes to tenant-scoped tables, this can allow cross-tenant writes/reads.
  - _Fix:_ Include tenantId in the call (recordToolExecution(tenantId, conversationId, ...)) or ensure recordToolExecution internally enforces WHERE tenant_id = <tenant>. At minimum, validate that conversationId belongs to the caller's tenant before writing.
- **server/chat-engine.ts:4042** — Second call to recordToolExecution(conversationId, toolName, true, ids) uses conversationId without tenant context or ownership validation. This risks writing tool-execution telemetry against another tenant's conversation.
  - _Fix:_ As above: pass tenantId into recordToolExecution or perform an explicit ownership check (SELECT ... WHERE id = conversationId AND tenant_id = <tenant>) before any write.
- **server/chat-engine.ts:4050** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is called without tenantId. If that function writes/reads stuck-diagnostics tied to a conversation, it must be tenant-scoped; currently no tenant constraint is visible here.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection or ensure the callee requires tenant scoping. Alternatively, verify conversation ownership prior to invoking diagnostic functions that persist data.
- **server/chat-engine.ts:4172** — getOrCreateTaskState(conversationId, ...) is invoked without tenantId. If it reads/creates persistent task state keyed by conversationId, there is no ownership check shown and it may access/modify another tenant's rows.
  - _Fix:_ Pass tenantId into getOrCreateTaskState or perform an explicit ownership verification for conversationId before delegating. The callee should enforce tenant_id in any SELECT/INSERT/UPDATE.
- **server/chat-engine.ts:4365** — storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is called without a tenantId. Attaching citations likely writes to a tenant-scoped table related to messages; no tenant constraint is passed here.
  - _Fix:_ Change attachCitations to accept a tenantId parameter and ensure it uses WHERE tenant_id = <tenant> when resolving the message row, or verify the assistantMsgRow belongs to the caller's tenant before attaching citations. Never rely solely on message id without enforcing tenant scoping.
- **server/data-protection.ts:149** — Global SELECT of expired conversations pulls rows across all tenants: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` has NO tenant_id constraint. This lets the maintenance job enumerate other tenants' conversations and then later perform destructive operations without per-tenant scoping.
  - _Fix:_ Either restrict the query to a specific tenant (pass tenantId into this function and add `AND tenant_id = ${tenantId}`), or iterate tenants explicitly and perform per-tenant purges. Ensure every subsequent DELETE/UPDATE in the purge loop also includes the owning tenant_id predicate (e.g. `WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}`).
- **server/data-protection.ts:163** — DELETE from messages uses `DELETE FROM messages WHERE conversation_id = ${conv.id}` with NO tenant_id constraint. messages is tenant-scoped elsewhere; deleting by conversation_id alone risks removing another tenant's messages if ids collide or FK assumptions change.
  - _Fix:_ Include the tenant predicate on destructive statements: `DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}` (or equivalent). Always constrain deletes/updates to the owning tenant when removing tenant-scoped rows.
- **server/data-protection.ts:164** — DELETE from compaction_archives uses `DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}` with NO tenant_id constraint, allowing cross-tenant deletions.
  - _Fix:_ Add tenant scoping to the DELETE, e.g. `DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}` (or otherwise verify and enforce the archive row's tenant before deleting).
- **server/data-protection.ts:165** — DELETE from conversations uses `DELETE FROM conversations WHERE id = ${conv.id}` with NO tenant_id constraint. This can delete conversations across tenants (race/ID reuse scenarios).
  - _Fix:_ Always include tenant scoping on destructive operations: `DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}`. Prefer defensive patterns that require both id + tenant_id for deletes/updates.
- **server/embeddings.ts:684** — UPDATE on tenant-scoped table (memory_entries | agent_knowledge) performed without constraining by tenant_id. storeEmbeddingVec builds a table name and runs: UPDATE <table> SET ... WHERE id = ${id} — no tenant filter. This allows cross-tenant writes to tenant-scoped rows.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and include it in the WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Avoid relying on process-global context. Prefer parameterized operations instead of composing raw SQL for the table/values where possible, and validate the caller is authorized for that tenant before updating.
- **server/embeddings.ts:726** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without constraining by tenant_id. This is a write to a tenant-scoped table without tenant filtering, enabling modification of other tenants' rows.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or otherwise ensure the update runs per-tenant with an explicit tenantId parameter. Avoid blind updates keyed only by id when tables are multi-tenant.
- **server/embeddings.ts:743** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint. This writes to a tenant-scoped table without ensuring ownership, allowing cross-tenant modification.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or redesign backfill to process per-tenant with an explicit tenantId. Also consider making storeEmbeddingVec accept tenantId and use that consistently rather than updating by id only.
- **server/google-drive.ts:946** — INSERT INTO project_files is performed without supplying tenant_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (...)). If project_files is tenant-scoped and tenant_id is NOT NULL with no default, this is a direct cross-tenant write risk (and may create rows with no tenant).
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. VALUES (${params.projectId}, ${tenantId}, ${params.fileName}, ...)) or otherwise derive and bind the owning tenant into the INSERT after verifying ownership with a tenant-scoped lookup. Ensure all tenant-scoped INSERTs set tenant_id from a trusted source.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images does not include tenant_id (INSERT INTO presenter_slide_images (session_id, slide_index, image_data, image_size, quality) ...). This writes to a likely tenant-scoped table using a session_id-derived value without scoping by tenant.
  - _Fix:_ Include tenant_id in the INSERT (and mark it NOT NULL in the model) or otherwise validate that sessionId belongs to the expected tenant before writing. Prefer using the Drizzle model/values with tenantId set (e.g. .values({ tenantId, ... })) or add AND tenant_id = ${tenantId} checks on writes derived from session IDs.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. Updating presenter_sessions by id only can modify another tenant's session if sessionId is untrusted.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first SELECT and verify the session row's tenant_id matches the trusted tenantId before applying the update. Avoid trusting session IDs returned from external/internal HTTP calls without ownership verification.
- **server/heartbeat.ts:381** — SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10 runs without any tenant_id constraint. This reads pending events for all tenants and then processes/routs them based on event.tenant_id — a missing tenant filter here permits cross-tenant enumeration and processing risks.
  - _Fix:_ Either query pending events per-tenant (iterate active tenant IDs and select WHERE tenant_id = ${tid} AND status = 'pending') or add an explicit tenant_id parameter to the query. If cross-tenant polling is intended, make it explicit via a tenant-aware storage API that documents and enforces safe handling.
- **server/heartbeat.ts:2680** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without any tenant_id constraint after creating a delegated task. This can change approval state of a task record regardless of tenant ownership.
  - _Fix:_ Use storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }) or include AND tenant_id = ${tenantId} in the UPDATE WHERE clause to ensure approval_status is only changed for the tenant that owns the task. Prefer tenant-aware storage APIs rather than raw SQL.
- **server/heartbeat.ts:3131** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) lacks tenant scoping. This updates heartbeat_tasks approval state without verifying the task belongs to the caller's tenant.
  - _Fix:_ Scope the UPDATE by tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call the storage-layer update function that enforces tenant ownership. Avoid raw SQL updates on tenant-scoped tables without a tenant filter.
- **server/internal-resolver.ts:91** — finalizeEvent performs an UPDATE on tenant-scoped table `event_log` without requiring/enforcing tenant_id when tenantId is omitted — the SQL fragment omits any tenant filter if tenantId is null, allowing unscoped terminal-state mutations.
  - _Fix:_ Require the event's tenant be resolved BEFORE calling finalizeEvent and make tenantId a mandatory parameter (or refuse to run if tenantId is absent). Always include AND tenant_id = ${tenantId} in the UPDATE so finalization cannot affect other tenants' rows.
- **server/job-queue.ts:98** — claimDueJobs runs an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) to claim jobs but does not constrain by tenant_id — it atomically claims jobs across all tenants with no tenant filter.
  - _Fix:_ If the intention is to only claim jobs for a single tenant context, add WHERE tenant_id = ${tenantId} (or accept an explicit tenantId parameter). If the worker must operate globally, ensure the design/privileges are deliberate and document why cross-tenant claims are permitted; otherwise enforce tenant-level WHERE clauses on claims.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE selects due rows and UPDATE applies across all tenants). This reads+updates tenant rows across tenants with no WHERE tenant_id = ... filter.
  - _Fix:_ Constrain the initial select/update to a specific tenant (e.g. add WHERE tenant_id = ${tenantId} in the CTE and UPDATE) or explicitly document/guard that this runner is running in an authorized admin/system context and implement an explicit tenant iteration so each SQL statement includes a tenant_id filter. Prefer per-tenant queries (or pass tenantId into the query) so application-level filters enforce isolation.
- **server/memory-graph.ts:252** — INSERT INTO memory_links does not set tenant_id (memory_links is tenant-scoped). New links will be created without an explicit tenant_id, violating the invariant and enabling cross-tenant linkage or orphaned rows.
  - _Fix:_ Include tenant_id in the INSERT column list and pass the caller's tenantId value (e.g. INSERT INTO memory_links (..., tenant_id, ...) VALUES (..., ${tenantId}, ...)). Ensure the tenant is validated/propagated into this function and cannot be derived from untrusted input.
- **server/minerva-planner.ts:367** — UPDATE against tenant-scoped table `plans` is conditionally scoped. The UPDATE CAS uses ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}, so when args.tenantId is omitted the WHERE clause lacks tenant_id and can modify rows across tenants.
  - _Fix:_ Enforce tenant scoping unconditionally. Require a validated tenantId for this operation and include AND tenant_id = ${tenantId} in the WHERE clause (or use Drizzle .where(eq(plans.tenant_id, tenantId))). If truly needed for trusted internal callers, use a clearly separate internal-only API and callsites that pass a server-validated tenantId; do NOT leave an optional inline SQL fragment that can run unscoped.
- **server/minerva-planner.ts:472** — Rollback UPDATE (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising') has no tenant_id constraint at all, so it can modify any tenant's plan row by id.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${args.planId} AND status = 'revising' AND tenant_id = ${tenantId}) and require tenantId be supplied/validated by the caller. Treat this as a critical fix: unscoped UPDATE to a tenant-scoped table must not be allowed.
- **server/mpeg-engine.ts:1051** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) call omits tenant_id even though this is a per-project/tenant resource and table rows are expected to be tenant-scoped.
  - _Fix:_ Include tenant_id in the INSERT columns and VALUES (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Alternatively, if project_files is intentionally keyed by project_id only, ensure schema and invariants are documented; otherwise always store tenant_id explicitly and/or derive it from a verified project ownership guard.
- **server/mpeg-engine.ts:1417** — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id. The upsert for project_files in produceVideoParallel does not pass tenant_id in the INSERT values.
  - _Fix:_ Same fix as above: add tenant_id to the INSERT columns and VALUES (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)) and ensure the tenant is validated (assertProjectInTenant) prior to the write.
- **server/plan-executor.ts:118** — appendExecutionLog() performs UPDATE plans ... WHERE id = ${planId} without constraining by tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add AND tenant_id = ${expectedTenant}) or fetch the row first and verify its tenant_id matches the caller/system context before updating. If this is intended as a cross-tenant maintenance action, make it explicit, audited, and run under an admin/system tenant identity rather than omitting tenant checks.
- **server/plan-executor.ts:513** — executePlan() claims a plan via UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved' — this CAS update modifies a tenant-scoped row without any tenant_id constraint, allowing cross-tenant claiming by planId.
  - _Fix:_ Restrict the CAS UPDATE by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${callerTenant} AND status = 'approved'), or require the caller to pass/derive the tenantId and verify it before the CAS. Alternatively SELECT the row first, validate tenant_id, then perform an update scoped to that tenant to ensure tenant isolation.
- **server/plan-executor.ts:728** — Final status update (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) does not include a tenant_id filter on a tenant-scoped table.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${planTenant}) or validate the plan's tenant before performing the UPDATE. Ensure every UPDATE that mutates plan state verifies the plan belongs to the intended tenant.
- **server/plan-executor.ts:762** — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') updates a tenant-scoped row without filtering by tenant_id.
  - _Fix:_ Add tenant filtering (AND tenant_id = ${planTenant}) or perform a prior read to verify the plan's tenant identity before changing status. All error recovery writes must be tenant-scoped to avoid affecting other tenants.
- **server/plan-executor.ts:778** — resumeStuckPlans() runs UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — this mass update touches all tenants' plans because no tenant_id constraint is applied.
  - _Fix:_ If intended as a global maintenance job, make it explicit and run under an audited admin/system context; otherwise scope the update per-tenant (e.g. iterate tenants and update WHERE tenant_id = X AND ...). At minimum add tenant filtering or an explicit admin guard and logging/auditing to avoid cross-tenant state changes.
- **server/plan-executor.ts:787** — resumeStuckPlans() then selects pending approved plans via SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 without tenant scoping, reading tenant-scoped table rows across tenants.
  - _Fix:_ Scope this read to a tenant (WHERE tenant_id = ${tenant}) or, if the process must scan many tenants, iterate per-tenant and enforce explicit admin/system authorization and audit. Never perform unscoped reads of tenant-scoped tables from request-driven or background code without an explicit and safe multi-tenant maintenance design.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} has no tenant_id constraint — a write to a tenant-scoped table without tenant scoping can affect other tenants' rows.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or otherwise validate the task belongs to the tenant before mutating it.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} lacks tenant_id constraint — this mutates a tenant-scoped table without assuring ownership.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or fetch+validate ownership before performing the update.
- **server/proposal-verifier.ts:33** — SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped proposals table without scoping by tenant_id — a caller-supplied proposalId can access proposals across tenants.
  - _Fix:_ Require tenant context on verifier calls and add WHERE tenant_id = ${tenantId} to the SELECT (or validate that the proposal belongs to the caller's tenant) before reading or operating on the row.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) updates a tenant-scoped table without tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause or otherwise validate ownership before updating the proposal row.
- **server/proposal-verifier.ts:246** — persist() performs UPDATE code_proposals ... WHERE id = ${proposalId} without a tenant filter — verification metadata is written to a proposal without confirming tenant ownership.
  - _Fix:_ Require tenant context and add tenant_id = ${tenantId} to the WHERE clause when updating verification_status/details, or validate the proposal belongs to the tenant before persisting.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} is executed without tenant scoping after LLM indicates 'NO_LONGER_APPLICABLE' — this mutates proposal state across tenants.
  - _Fix:_ Apply tenant scoping (AND tenant_id = ${tenantId}) or validate proposal ownership prior to changing status/verification fields.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without tenant filtering.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause so only the intended tenant's proposal is modified, or require/validate tenant ownership before updating.
- **server/routes/conversations.ts:99** — UPDATE on tenant-scoped table without tenant constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` has no `AND tenant_id = ${tenantId}` filter.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or use a storage/Drizzle call that enforces tenant scope. Never issue writes to tenant-scoped tables without an explicit tenant filter.
- **server/routes/conversations.ts:100** — SELECT against (likely) tenant-scoped join table without tenant constraint: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` does not filter by tenant_id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or fetch via a tenant-aware storage function. If project_conversations is multi-tenant, every read must include tenant_id = caller tenant.
- **server/routes/conversations.ts:103** — INSERT into a tenant-scoped table without tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` does not set tenant_id explicitly.
  - _Fix:_ If project_conversations is tenant-scoped, include the tenant_id column in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ...)). Alternatively, use a tenant-aware API that guarantees tenant is set.
- **server/routes/conversations.ts:105** — UPDATE on tenant-scoped table without tenant constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` lacks `AND tenant_id = ${tenantId}`.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or use a tenant-aware helper to perform the update. Verifying ownership earlier is not sufficient — the SQL statement itself must include tenant filtering.
- **server/routes/conversations.ts:229** — INSERT into project-scoped table without tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id on insert.
  - _Fix:_ If project_notes is tenant-scoped, add tenant_id to the INSERT (INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ...)). Alternatively use a storage layer which enforces tenant assignment on created rows.
- **server/routes/projects.ts:185** — INSERT into tenant-scoped table `project_notes` does not include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})), or use an abstraction that sets the tenant before persisting. Do not rely on prior checks alone.
- **server/routes/projects.ts:225** — INSERT into tenant-scoped table `project_files` (files-base64 handler) does not include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})), or use a DB helper that binds tenant context explicitly.
- **server/routes/projects.ts:292** — INSERT into tenant-scoped table `project_files` (multer upload handler) does not include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})), or derive tenant from the already-validated project and set it explicitly.
- **server/stability-watchdog.ts:177** — SELECT from tenant-scoped table `heartbeat_logs` without a tenant_id constraint (reads error counts across all tenants).
  - _Fix:_ Constrain the query to a tenant (WHERE tenant_id = ...) or explicitly document/authorize a platform-wide sweep by using the explicit ALL_TENANTS sentinel / admin-safe path. If the intent is to run across all tenants, use a guarded admin API and the tenant-scope helpers (or iterate tenants and run per-tenant queries) so isolation checks are explicit.
- **server/stability-watchdog.ts:211** — DELETE FROM tenant-scoped table `heartbeat_tasks` with no tenant_id filter — performs cross-tenant deletions.
  - _Fix:_ Do NOT run an unscoped DELETE against a tenant-scoped table. Either (a) add WHERE tenant_id = <admin-chosen tenant> (for per-tenant cleanups), or (b) if truly intended to purge across all tenants, require an explicit admin sentinel/guard and call into a vetted admin routine that documents the cross-tenant effect. Prefer iterating tenants and running scoped deletes so each operation is auditable and constrained.
- **server/stability-watchdog.ts:225** — DELETE FROM tenant-scoped table `event_log` with no tenant_id filter — performs cross-tenant deletions.
  - _Fix:_ Same fix as above: scope deletions to a tenant_id, or require an explicit and well-audited admin path (or use the ALL_TENANTS sentinel via the tenant-scope helpers after a deliberate check). Avoid silent global deletes on tenant-scoped tables.
- **server/stability-watchdog.ts:259** — DELETE FROM tenant-scoped table `heartbeat_logs` with no tenant_id constraint (pruning old logs across all tenants).
  - _Fix:_ Scope pruning to a tenant (WHERE tenant_id = ...) or implement a safe, explicit admin-only sweep that either iterates tenants and runs per-tenant deletions or uses an approved global-administration path. Make tenant intent explicit (e.g., pass ALL_TENANTS sentinel and require admin privileges) to avoid accidental cross-tenant data operations.
- **server/storage.ts:398** — attachCitations updates messages via raw SQL: UPDATE messages SET citations = ... WHERE id = ${messageId} with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Require and validate tenantId for this operation and include AND tenant_id = ${tenantId} in the WHERE clause (or use a Drizzle .where(eq(messages.tenantId, tenantId))). Do not rely on id-only filtering unless the id is proven to belong to the caller's tenant immediately prior to the UPDATE (and enforce that check atomically if possible).
- **server/storage.ts:482** — deletePersona performs tx.update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id)) without tenant scoping — this will clear persona references across all tenants.
  - _Fix:_ Scope the update by tenant (require tenantId parameter or iterate per-tenant) or perform the operation within a context that proves admin intent. If the persona is global but conversation links are per-tenant, update only rows where conversation.tenant_id matches the intended tenant(s). Add explicit checks and/or require caller to pass an admin flag.
- **server/storage.ts:841** — archiveExpiredMemories runs raw SQL UPDATE memory_entries SET status='archived' WHERE ... with NO tenant_id constraint (unscoped write across tenant-scoped memory_entries).
  - _Fix:_ Either require a tenantId/ALL_TENANTS sentinel and use requiredTenantScope to limit which tenant(s) are affected, or run per-tenant updates. For global maintenance tasks, make the platform intent explicit and gate by an admin-only path that documents and audits the global operation.
- **server/storage.ts:858** — archiveStaleMemories runs raw SQL UPDATE memory_entries SET status='archived' WHERE ... with NO tenant_id constraint (unscoped write across tenant-scoped memory_entries).
  - _Fix:_ Same as above: require explicit tenant scoping (tenantId or ALL_TENANTS sentinel) or perform per-tenant maintenance. Make global mutation intentions explicit and auditable.
- **server/storage.ts:806** — updateMemoryEmbedding executes db.update(memory_entries).set({ embedding }).where(eq(memory_entries.id, id)) with NO tenant_id constraint — unscoped write to a tenant-scoped table by id only.
  - _Fix:_ Require and validate the tenantId for this operation, or include tenant scoping in the WHERE clause (e.g. and eq(memory_entries.tenantId, tenantId)). If the caller only has an id, re-check ownership before updating to ensure the id belongs to the caller's tenant.
- **server/storage.ts:818** — updateKnowledgeEmbedding executes db.update(agent_knowledge).set({ embedding }).where(eq(agent_knowledge.id, id)) with NO tenant_id constraint — unscoped write to a tenant-scoped table by id only.
  - _Fix:_ Require caller-provided tenantId (and validate it) or include an explicit tenant filter in the WHERE clause (eq(agent_knowledge.tenantId, tenantId)). Verify ownership before updating by id.
- **server/storage.ts:1311** — getAllDataForExport performs db.select() FROM many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, etc.) with NO tenant_id constraints — exports data across all tenants.
  - _Fix:_ Restrict exports to a specific tenant by requiring tenantId param and adding tenant filters on each table, or add an explicit admin-only path that documents and authorizes global exports. Treat a global export as a high-risk operation and gate it behind strong authorization and audit logging.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding executes raw SQL UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Fix:_ Require and pass tenantId into stampArgsEmbedding, and include AND tenant_id = ${tenantId} in the WHERE clause (or verify ownership atomically before UPDATE). Do not perform id-only updates on tenant-scoped tables without tenant validation.
- **server/surprise-scorer.ts:141** — scoreProposalSurprise updates actual_outcome_embedding via raw SQL: UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId} with NO tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or require tenantId on the function and validate ownership before writing.
- **server/surprise-scorer.ts:181** — When not enough neighbors, code runs UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} with NO tenant_id constraint (unscoped write).
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) so band/state changes only affect the intended tenant's row.
- **server/surprise-scorer.ts:212** — After scoring, code updates surprise_score and surprise_band with UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} — NO tenant_id constraint (unscoped write).
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or otherwise verify ownership before mutating the row so one tenant cannot influence another's proposal rows.
- **server/tools.ts:2982** — INSERT into project_files does not set tenant_id. The INSERT: "INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)" lacks an explicit tenant_id column/value, violating the invariant that tenant-scoped tables must be written with the caller's tenant.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Also ensure project ownership is validated (project belongs to tenant) before write and set tenant_id from the validated tenantId rather than deriving it from untrusted inputs.
- **server/tools.ts:2996** — INSERT into project_notes does not set tenant_id. The INSERT: "INSERT INTO project_notes (project_id, note, author) VALUES (...)" omits tenant_id for a tenant-scoped resource.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})) and ensure the project ownership check is performed prior to the insert. Never rely solely on project_id without stamping tenant_id explicitly.
- **server/tools.ts:2878** — INSERT into project_conversations (during project create/link flow) omits tenant_id. The statement "INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING" writes a tenant-scoped table without an explicit tenant_id column/value.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ...). Ensure any earlier checks that validated conversation ownership are used to set the tenant_id (do not accept or derive tenant from untrusted inputs).
- **server/tools.ts:3020** — INSERT into project_conversations (link_conversation path) omits tenant_id. The code executes "INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)" after validating the conversation, but it does not store tenant_id on the new row.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Ensure assertConversationInTenant result is used to derive tenantId and that the tenantId is persisted on the inserted row.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE against tenant-scoped table knowledge_triples uses only 'WHERE id = ${row.id}' with no tenant_id constraint. Every write to tenant-scoped tables must include the caller's tenant filter to enforce isolation.
  - _Fix:_ Include the tenant filter in the UPDATE (and other mutating queries). For example: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}. Or at minimum verify the row's tenant matches tenantId before updating. Ensure all mutating SQL statements against tenant-scoped tables always constrain by tenant_id.
- **server/venture-discovery/budget.ts:155** — UPDATE on tenant-scoped table agent_cost_ledger (settleVentureReservation) lacks tenant_id constraint — UPDATE ... WHERE id = ${reservationId} can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or first verify the reservation row belongs to the tenant before updating. Prefer an atomic UPDATE that includes tenant_id to prevent cross-tenant modification.
- **server/venture-discovery/budget.ts:171** — UPDATE on tenant-scoped table agent_cost_ledger (releaseVentureReservation) lacks tenant_id constraint — UPDATE ... WHERE id = ${reservationId} can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) or verify ownership of the reservation id before releasing. This prevents an attacker from releasing/altering another tenant's reservation.

### HIGH (189)

- **server/ab-optimizer.ts:110** — SELECT on tenant-scoped table `experiments` executed without constraining to a tenant (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads rows across all tenants.
  - _Fix:_ Add a tenant filter to the query (e.g. WHERE tenant_id = ${tenantId} ... or pass a tenantId and use eq(experiments.tenantId, tenantId)). If the heartbeat must scan per-tenant, iterate tenants and perform a tenant-scoped query per tenant rather than scanning all rows.
- **server/agent-eval.ts:218** — UPDATE to tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with no tenant constraint. This can update a row belonging to a different tenant if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or otherwise validate the eval row belongs to the caller's tenant before updating.
- **server/agentic-engines.ts:161** — SELECT in retryPendingMinervaRouting reads `ai_insights` rows without a tenant filter (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER}). This returns rows across tenants.
  - _Fix:_ Scope the sweep per tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate tenants explicitly. Never perform bulk updates/reads across all tenants without explicit tenant scoping.
- **server/agentic-engines.ts:344** — In gatherOperationalData a heartbeat_logs count query is executed without tenant scoping (SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads platform-wide heartbeat logs rather than tenant-specific ones.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND ...) or otherwise compute heartbeat stats per-tenant; avoid cross-tenant reads of tenant-scoped operational logs.
- **server/agentic-engines.ts:451** — recentLogs query in runPredictiveEngine reads `heartbeat_logs` without a tenant filter (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...). This returns other tenants' logs.
  - _Fix:_ Restrict this query to the tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'success' ...) or run per-tenant queries only.
- **server/agentic-engines.ts:519** — heartbeatPerformance query in runOptimizationEngine aggregates from `heartbeat_logs` with no tenant filtering (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This aggregates across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause, or perform tenant-scoped aggregation. Avoid using platform-wide heartbeat logs for a tenant-specific optimization engine.
- **server/agentic-engines.ts:533** — taskConfig query selects from `heartbeat_tasks` without tenant scoping (SELECT ... FROM heartbeat_tasks WHERE enabled = true ORDER BY name). If heartbeat_tasks is tenant-scoped, this reads other tenants' tasks.
  - _Fix:_ Scope the select to the tenant (e.g. WHERE tenant_id = ${tenantId} AND enabled = true) or otherwise clarify that heartbeat_tasks are global. If they are per-tenant, add the tenant filter.
- **server/agentic-features.ts:658** — UPDATE outreach_enrollments sets status = 'completed' WHERE id = ${enrollment.id} with no tenant filter. This is a write to a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) or validate enrollment.tenant_id before updating.
- **server/agentic-features.ts:700** — UPDATE outreach_enrollments to advance the enrollment uses WHERE id = ${enrollment.id} with no tenant constraint. This can update another tenant's enrollment if ids collide.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}) or otherwise assert ownership before writing.
- **server/agentic-features.ts:599** — SELECT existing enrollment uses WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant filtering. This reads enrollments across tenants and can cause logic errors (duplicates check bypass) or cross-tenant info leaks.
  - _Fix:_ Add tenant scoping to the lookup: WHERE tenant_id = ${params.tenantId} AND sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} ... so the existence check is tenant-scoped.
- **server/agentic-features.ts:749** — UPDATE outreach_enrollments sets reply_classification, status, etc. using WHERE id = ${row.id} with no tenant constraint. This writes to a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or verify the enrollment belongs to the tenant before updating.
- **server/agentic-features.ts:525** — UPDATE lead_enrichments sets icp_score etc. using WHERE id = ${lead.id} without a tenant_id constraint. This updates a tenant-scoped row without scoping to the caller's tenant.
  - _Fix:_ Add tenant-scoping to the UPDATE (e.g. WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}) or validate the lead row belongs to the tenant prior to updating.
- **server/agentic/department-budgets.ts:143** — Query selects DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' with no tenant_id WHERE clause — reads rows for all tenants rather than a single tenant context.
  - _Fix:_ If this is intended to run per-tenant, add an explicit tenant_id filter (WHERE tenant_id = ${tenantId}) or accept a tenantId parameter and constrain the query. If it must scan all tenants, ensure the caller is an authorized admin/system job and document that fact; otherwise restrict the query to a single tenant or iterate tenants via a safe admin-only code path.
- **server/agentic/resume-worker.ts:66** — SELECT across agent_runs/agent_approvals scans rows for all tenants without constraining by tenant_id (no WHERE tenant_id = <caller tenant>) — reads tenant-scoped tables cross-tenant.
  - _Fix:_ Restrict the query to a specific tenant context or iterate per-tenant. Add an explicit tenant filter (e.g. WHERE r.tenant_id = ${tenantId}) or otherwise ensure this worker only processes rows for an authorized tenant context.
- **server/agentic/runs.ts:34** — appendStep() updates agent_runs using WHERE id = runId but does not include tenant_id in the WHERE clause — update against a tenant-scoped table without tenant constraint.
  - _Fix:_ Require and use the caller's tenantId when updating run rows, e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers pass tenantId or look up-and-verify run ownership before updating.
- **server/agentic/runs.ts:45** — updateRunState() updates agent_runs WHERE id = runId with no tenant_id constraint — tenant-scoped write missing tenant filter.
  - _Fix:_ Include tenant scoping in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) or otherwise validate run ownership before performing the update. Add tenantId parameter to the function if needed.
- **server/agentic/runs.ts:54** — replaceRunState() performs an UPDATE on agent_runs WHERE id = runId but omits tenant_id — missing tenant scoping on a tenant-scoped table.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and require the caller to supply/verify tenantId.
- **server/agentic/runs.ts:58** — completeRun() updates agent_runs status/result WHERE id = runId with no tenant_id constraint — tenant-scoped write lacks tenant filter.
  - _Fix:_ Constrain the update by tenant (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or validate run ownership prior to updating. Ensure subsequent read (select by id) also enforces tenant scoping.
- **server/agentic/runs.ts:70** — completeRun() later does db.select().from(agentRuns).where(eq(agentRuns.id, runId)) without tenant filtering — tenant-scoped read missing tenant constraint.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or fetch the run via a tenant-scoped accessor to avoid cross-tenant data exposure.
- **server/agentic/runs.ts:88** — failRun() updates agent_runs WHERE id = runId but omits tenant_id — tenant-scoped update missing tenant filter.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) and require the caller to supply/verify tenantId before marking runs failed.
- **server/agentic/runs.ts:97** — pauseRun() updates agent_runs WHERE id = runId without including tenant_id — missing tenant constraint on a tenant-scoped table.
  - _Fix:_ Require tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))) to ensure updates cannot affect runs from other tenants.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge (getWatermark) has no tenant_id constraint — background watermark read is not scoped to a tenant.
  - _Fix:_ Pass the tenantId into getWatermark and include WHERE tenant_id = ${tenantId} in the query (or otherwise scope reads to the intended tenant). Avoid cross-tenant reads.
- **server/auto-project.ts:112** — Query that fetches recent messages for a conversation (SELECT role, LEFT(content, 300) FROM messages WHERE conversation_id = ${conversationId}) does not include tenant_id. Even though conversation row was validated earlier, the messages query itself is unscoped.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the messages SELECT so the read is explicitly scoped to the caller tenant, or otherwise derive tenant from the validated conversation row and use it in the WHERE clause.
- **server/auto-transcript.ts:46** — Selecting messages for a verified conversation (SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}) is missing tenant_id in WHERE clause. This allows a read against a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the messages SELECT (or use a validated tenant variable derived from assertConversationInTenant) so all message reads are explicitly scoped to the owning tenant.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL is executed without scoping by tenant. This can modify a conversation row in another tenant if IDs are manipulated.
  - _Fix:_ Add AND tenant_id = ${c.tenant_id} (or AND tenant_id = ${tenantId}) to the UPDATE to ensure the update is applied only for the owning tenant.
- **server/channels/kernel.ts:180** — dispatchInbound calls processMessage(conversationId, ...) using a conversationId obtained from the adapter callback (opts.resolveConversation) without verifying that the conversation belongs to turn.tenantId. This lets a malicious or buggy adapter supply a foreign conversation_id and have processMessage perform reads/writes on a conversation owned by another tenant.
  - _Fix:_ Do not trust an adapter-supplied conversation id alone. Either (A) require resolveConversation to validate and assert ownership (e.g., only return a conversationId after confirming the row tenant_id === turn.tenantId), or (B) have dispatchInbound explicitly verify ownership before calling processMessage by fetching the conversation with a tenant-scoped query (SELECT ... FROM conversations WHERE id = ? AND tenant_id = ?) and rejecting if not found. Also ensure processMessage and all downstream DB operations always include WHERE tenant_id = <tenant> (or accept an explicit tenantId proven/validated by the caller) and avoid using sql.raw()/string interpolation with request-derived ids.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table 'project_files' has no tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC`.
  - _Fix:_ Enforce tenant scoping on this query. Add an explicit tenant_id filter (e.g. `... WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`), or JOIN through projects and require p.tenant_id = ${tenantId}. Prefer using Drizzle's eq(fileStorage.tenantId, tenantId) when constructing queries to avoid accidental omission. Do not rely solely on prior project ownership checks — each DB read must include the tenant filter.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table 'project_notes' has no tenant_id constraint. The query uses only project_id: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ORDER BY created_at DESC LIMIT 20`.
  - _Fix:_ Add explicit tenant scoping to the query (for example: `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}`), or join to projects and require p.tenant_id = ${tenantId}. Use Drizzle's .where(eq(table.tenantId, tenantId)) to ensure consistent tenant filters on tenant-scoped tables.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table 'compaction_archives' lacks tenant scoping: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ORDER BY archived_at DESC LIMIT 1`. No tenant_id filter present.
  - _Fix:_ Constrain by tenant_id as well: `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}` (or the equivalent Drizzle .where(eq(...))). If conversation ownership was previously validated, still include tenant_id in this query to enforce isolation at every DB access.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table 'messages' is performed without a tenant_id constraint: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC`. This can allow cross-tenant data exposure if conversation_id is incorrect or attacker-controlled.
  - _Fix:_ Add an explicit tenant filter to the messages query: `WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}` (or Drizzle .where(eq(messages.tenantId, tenantId))). Alternatively, ensure the conversation_id was verified and include tenant_id regardless — every read against tenant-scoped tables must include tenant scoping in the SQL itself.
- **server/chat-engine.ts:4056** — postDiagnosticReport([stuckPattern]) is invoked without any tenant context. If postDiagnosticReport persists diagnostic reports into tenant-scoped tables, this call could escape tenant boundaries.
  - _Fix:_ Include tenantId in the call (postDiagnosticReport(report, { tenantId })) or modify postDiagnosticReport to require tenant-scoped metadata and enforce WHERE tenant_id checks on DB writes.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` executed without constraining by tenant_id (unclaim path uses WHERE id = ${row.id} only). This can modify rows across tenants if IDs collide or row ownership differs.
  - _Fix:_ Restrict the UPDATE to the owning tenant. Include AND tenant_id = ${row.tenantId} (or the proper tenant variable) in the WHERE clause, or fetch/validate the row's tenant before updating. E.g. UPDATE commitments SET draft_status='open', drafted_at=NULL WHERE id=${row.id} AND tenant_id=${row.tenantId}.
- **server/compaction.ts:401** — INSERT into `compaction_archives` does not include tenant_id — inserting a tenant-scoped archive without tenant ownership. If compaction_archives is a tenant-scoped table, this creates orphaned/unspecified-tenant rows.
  - _Fix:_ Include the tenant_id when inserting archive records (e.g. VALUES (..., ${convTenantId}, ...)) or derive and validate the tenant from the conversation (JOIN or pre-query). Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly.
- **server/credential-vault.ts:169** — UPDATE on `credential_vault` sets last_used_at WHERE id = ${id} with no tenant_id constraint (getDecryptedPassword). This can update another tenant's row if IDs collide.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}. Validate the credential belongs to the tenant before performing the update.
- **server/credential-vault.ts:176** — UPDATE on `credential_vault` sets last_used_at WHERE id = ${cred.id} with no tenant_id constraint (getLoginCredentials). This may update rows across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or use the tenant from the fetched credential) before updating last_used_at. Always scope UPDATEs to the caller's tenant.
- **server/data-protection.ts:214** — SELECT of messages inside backupConversationToDrive uses `SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}` with NO tenant_id constraint. Although the function earlier verified the conversation row belongs to the tenant, the invariant requires explicit tenant scoping on reads against tenant-scoped tables.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the messages SELECT (and every other read of tenant-scoped tables) rather than relying on a prior conversation read. This avoids relying on implicit FK ownership and prevents accidental cross-tenant reads if schema/assumptions change.
- **server/data-protection.ts:221** — SELECT of compaction_archives inside backupConversationToDrive uses `SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId}` with NO tenant_id constraint, allowing cross-tenant reads.
  - _Fix:_ Constrain the select with `AND tenant_id = ${tenantId}` (or verify ownership via an explicit checked field) so archives are only read for the caller's tenant. Apply the same tenant predicate to all reads of tenant-scoped tables.
- **server/delivery-pipeline.ts:203** — updateDeliveryLog performs an UPDATE on delivery_logs without constraining by tenant_id (where clause is eq(deliveryLogs.id, id) only). This allows cross-tenant modifications if called with an id from a different tenant.
  - _Fix:_ Add tenant scoping to the update (e.g. accept a tenantId parameter and use .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or validate the tenant ownership before updating. Make the function signature explicit about tenant context so every callsite must provide the correct tenant filter.
- **server/delivery-pipeline.ts:1071** — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with NO tenant filter. This returns tenant-scoped rows across tenants.
  - _Fix:_ Require a tenantId parameter (or otherwise infer/verify tenant context) and add a WHERE tenant_id = ${tenantId} to the query. If the lookup must remain global, document and restrict this API to trusted/admin-only callers, but prefer explicit tenant scoping for safety.
- **server/dfy-intake.ts:93** — submitDfyIntake performs an UPDATE on dfy_intake_forms with .where(eq(dfyIntakeForms.id, form.id)) but without tenant_id in the WHERE clause. This can modify another tenant's form if ids collide or are supplied maliciously.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. .where(and(eq(dfyIntakeForms.id, form.id), eq(dfyIntakeForms.tenantId, form.tenantId || providedTenantId)))). Ensure the function ties the form id to its tenant before updating.
- **server/doc-collections.ts:165** — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. doc_chunks is tenant-scoped and must be narrowed to the caller's tenant.
  - _Fix:_ Include tenant scoping on the DELETE, e.g. DELETE ... WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Also consider using the earlier verified collection->tenant mapping to assert the tenant and pass it into the DELETE explicitly.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings is executed without any tenant_id constraint: SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50. This reads rows across all tenants.
  - _Fix:_ If the backfill is intended to be tenant-scoped, iterate per tenant and include WHERE tenant_id = ${tenantId}. If it must run as an admin/global operation, explicitly bind and document ADMIN/maintenance tenant context (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID}) and ensure this admin path is tightly controlled and audited.
- **server/embeddings.ts:716** — SELECT FROM memory_entries in backfillEmbeddingVecs is executed without tenant filtering: SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200. This reads embeddings across all tenants.
  - _Fix:_ Run backfills per-tenant or constrain the query with WHERE tenant_id = ${tenantId} (or an explicit ADMIN_TENANT_ID if intended), and ensure the maintenance job iterates safely over tenants rather than leaking cross-tenant data.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge in backfillEmbeddingVecs is executed without tenant filtering: SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200. This reads data across tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant. If run as an administrative global task, make the admin tenant explicit and secure the execution path.
- **server/felix-brain.ts:19** — SELECT from tenant-scoped table 'agent_runs' without any tenant_id constraint (rehydration reads all tenants' rows).
  - _Fix:_ Constrain the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or perform a per-tenant rehydration loop that binds tenant_id from a trusted source. Never scan tenant-scoped tables across tenants without explicit tenant scoping.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} but omits tenant_id check when writing run metadata (ended_at, context_summary, etc.).
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) so the write is constrained to the caller's tenant.
- **server/felix-loop.ts:690** — Error-path UPDATE on 'felix_loop_runs' (recording failure) uses WHERE id = ${runId} without tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) when updating run rows in error handlers to prevent cross-tenant modifications.
- **server/firecrawl.ts:214** — UPDATE scrapedPages uses .where(eq(scrapedPages.id, existing[0].id)) but omits tenant scoping (tenantId). This updates a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Add tenant scoping to the WHERE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise validate ownership before performing the UPDATE.
- **server/firecrawl.ts:344** — UPDATE scrapedPages in crawl loop uses .where(eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint, allowing cross-tenant writes.
  - _Fix:_ Include tenant filter on the UPDATE (e.g. AND eq(scrapedPages.tenantId, tenantId)) or ensure the prior lookup enforces tenant ownership and re-query with tenant scoping before updating.
- **server/google-drive.ts:547** — SELECT from tenant-scoped table 'projects' by project id (.where(eq(projects.id, projectId))) without constraining the query by tenant_id. The code checks project.tenantId after the read, but the SELECT itself reads another tenant's row without an application-level WHERE tenant_id filter.
  - _Fix:_ Scope the SELECT to the caller's tenant (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so no cross-tenant row is read even transiently. Prefer binding tenant in the SQL rather than post-hoc checks.
- **server/google-drive.ts:560** — UPDATE projects sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE eq(projects.id, projectId) AND eq(projects.tenantId, tenantId)) to ensure only the owning tenant's project row is modified.
- **server/google-drive.ts:826** — SELECT projects by id in uploadAndShare (.where(eq(projects.id, params.projectId))) without including tenant_id in the query. The code validates ownership after the SELECT rather than constraining the read itself.
  - _Fix:_ Include tenant_id in the SELECT's WHERE clause (e.g. WHERE eq(projects.id, params.projectId) AND eq(projects.tenantId, params.tenantId)) or use an assertion helper that runs a tenant-scoped query before proceeding.
- **server/health-audit.ts:314** — SELECT FROM code_proposals queries tenant-scoped table code_proposals with no tenant_id filter (SELECT id, title, status, created_at FROM code_proposals WHERE ...). This reads across tenants.
  - _Fix:_ Scope the query to a tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or explicitly run this as an authorized admin operation that iterates tenants. If intended as per-tenant work, pass in tenantId and add WHERE tenant_id = ${tenantId} to the SELECT.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows in code_proposals without constraining by tenant_id, potentially affecting other tenants' proposals.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or derive the ids per-tenant and update only those matching the expected tenant. Alternatively, perform the update with an explicit admin-owned audit path and authorization check.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks queries heartbeat_tasks without a tenant_id filter (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE ...). heartbeat_tasks appear to be tenant-scoped elsewhere and this will scan across tenants.
  - _Fix:_ If this is intended to run per-tenant, accept/derive a tenantId and add WHERE tenant_id = ${tenantId}. If this is a global admin audit, require explicit admin authorization and/or iterate tenants while scoping each SELECT to that tenant to avoid accidental cross-tenant reads.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) updates heartbeat_tasks without scoping by tenant_id, possibly disabling tasks for other tenants.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or otherwise ensure the ids list was derived only from rows belonging to the expected tenant. Prefer per-tenant updates rather than unscoped global UPDATEs.
- **server/heartbeat.ts:53** — SELECT from research_schedules is executed without any tenant_id filter (SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW()). This reads tenant-scoped rows across all tenants without an explicit per-tenant constraint.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add AND tenant_id = ${schedTenantId}) or iterate per-tenant (fetch active tenant IDs and query research_schedules for each tenant). Alternatively, if the intent is a controlled system-wide scan, make that explicit by using a storage helper that accepts ALL_TENANTS and documents the cross-tenant behavior.
- **server/heartbeat.ts:102** — UPDATE research_schedules sets next_run_at WHERE id = ${sched.id} without constraining by tenant_id. Updating a tenant-scoped table by id only can modify rows belonging to other tenants if an id is misused/guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage-level helper that enforces tenant scoping for the update.
- **server/heartbeat.ts:108** — UPDATE research_schedules SET last_run_at/next_run_at WHERE id = ${sched.id} is executed without tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or use a tenant-aware storage API to perform the update to ensure the row belongs to the expected tenant.
- **server/heartbeat.ts:113** — UPDATE research_schedules SET next_run_at WHERE id = ${sched.id} executed without tenant_id constraint (in the 'else' branch).
  - _Fix:_ Constrain the update with AND tenant_id = ${sched.tenant_id} or call a storage function that enforces tenant ownership before updating.
- **server/heartbeat.ts:121** — In the recovery path, UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} lacks a tenant_id filter.
  - _Fix:_ Include tenant scoping (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage-level updater that validates tenant ownership to avoid cross-tenant updates.
- **server/heartbeat.ts:269** — Raw UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} executed without constraining by tenant_id. This modifies heartbeat_tasks rows without verifying tenant ownership.
  - _Fix:_ Use a tenant-scoped WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call storage.updateHeartbeatTask / another tenant-aware helper that enforces the tenant constraint before updating.
- **server/heartbeat.ts:273** — Raw UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} executed without tenant_id constraint (normalizing reflection-model).
  - _Fix:_ Restrict the UPDATE to the correct tenant (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or rely on the storage layer's tenant-aware update function to avoid touching rows from other tenants.
- **server/heartbeat.ts:1586** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 has no tenant_id filter. heartbeat_logs is tenant-related here (later tenantId is used), so this read can return another tenant's log.
  - _Fix:_ Constrain the query to the task's tenant (e.g. add AND tenant_id = ${tenantId}) or use a tenant-aware storage helper (e.g. storage.getHeartbeatLogsForTenant) so the lookup only inspects logs belonging to the intended tenant.
- **server/internal-resolver.ts:184** — In resolveExperimentFailed the code path that detects a missing tenant_id calls finalizeEvent(eventId, ...) without a tenantId, which will run the unscoped UPDATE from finalizeEvent and may finalize rows across tenants.
  - _Fix:_ Do NOT call finalizeEvent without an explicit tenantId. On missing tenant_id, fail closed (log and do nothing) or otherwise bail without performing an unscoped UPDATE. If a finalization is required, fetch and pass the resolved tenant_id into finalizeEvent and use the tenant-scoped form.
- **server/job-queue.ts:134** — reclaimExpiredLeases performs UPDATEs on tenant-scoped table `agent_jobs` (marking terminal and re-queue rows) without any tenant_id filters — these bulk operations will affect rows across tenants.
  - _Fix:_ Scope reclamation updates by tenant where appropriate (e.g. accept tenantId param and add WHERE tenant_id = ${tenantId}) or otherwise explicitly document and gate global reclamation to an admin-only code path. Avoid blind cross-tenant UPDATEs.
- **server/job-queue.ts:148** — Second UPDATE in reclaimExpiredLeases (requeueing expired leases back to pending) lacks tenant_id restriction and will requeue jobs for all tenants indiscriminately.
  - _Fix:_ Same as above: add tenant-scoping (WHERE tenant_id = ${tenantId}) or otherwise restrict this operation to an explicit, audited global admin path.
- **server/job-queue.ts:177** — completeJob issues UPDATE agent_jobs SET status='succeeded' ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint — a caller could complete a job belonging to a different tenant by id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or require callers to pass/validate the job's tenant before completing. Enforce lease-fencing with both id+attempts AND tenant_id.
- **server/job-queue.ts:220** — failJob first SELECTs max_attempts FROM agent_jobs WHERE id = ${id} with no tenant constraint, then later UPDATEs agent_jobs to failed / pending without tenant filters — allowing a caller to examine/mutate other tenants' jobs by id.
  - _Fix:_ Always include tenant_id = ${tenantId} when reading or mutating agent_jobs; require the caller/worker to provide/verify tenant ownership before operating on a job id. Make the tenant part of the lease-fencing checks.
- **server/job-queue.ts:237** — Terminal-path UPDATE in failJob (marking failed_terminal) updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but has no tenant_id check — can dead-letter jobs across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause and ensure callers supply the tenant context for the failed job before performing the terminal update.
- **server/job-queue.ts:257** — Retry-path UPDATE in failJob (scheduling next_run_at) updates agent_jobs without any tenant filter, potentially mutating jobs from other tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise validate ownership) before changing retry state.
- **server/job-queue.ts:274** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint — a cancel by id may affect a job from another tenant.
  - _Fix:_ Require tenant-bound cancellation: include tenant_id = ${tenantId} in the WHERE or require the operator to pass/verify the job's tenant before cancelling.
- **server/job-queue.ts:283** — retryJob resets a job to pending via UPDATE agent_jobs ... WHERE id = ${id} AND status IN (...) without tenant scoping, permitting cross-tenant retries by id.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause or require tenant verification before allowing manual retry of a job.
- **server/job-queue.ts:320** — listJobs builds a SELECT from agent_jobs and will execute it with NO tenant_id WHERE clause when the caller omits a tenantId filter — returning or operating across tenants.
  - _Fix:_ Make tenantId required for tenant-scoped listings in non-admin contexts, or ensure this function is only exposed to admin callers. At minimum, document and protect the unscoped path; prefer adding WHERE tenant_id = ${tenantId} when a tenantId is supplied and restrict the no-tenant case.
- **server/job-queue.ts:385** — getJobStats runs SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > ... with no tenant_id constraint — aggregates across all tenants and exposes cross-tenant counts.
  - _Fix:_ If per-tenant stats are required, add a tenant_id filter parameter and include WHERE tenant_id = ${tenantId}. If global stats are admin-only, restrict access to admin-only code paths and document the intent.
- **server/lib/bwb-job-progress.ts:74** — scope() builds the WHERE clause for videoJobs updates/selects but omits a tenant predicate when envTenantId() returns null (BWB_TENANT_ID missing). That allows UPDATE/SELECTs against the tenant-scoped video_jobs table to execute without a tenant_id constraint (writes pinned only by jobId), violating tenant isolation invariants.
  - _Fix:_ Require an explicit tenantId (do not silently fall back to jobId-only). Fail closed when the tenant is unknown (throw / return early) or derive tenant from request context and always include eq(videoJobs.tenantId, tenantId) in the WHERE clause. Remove the "allow unscoped when missing" path and the single-warning behavior.
- **server/lib/bwb-job-progress.ts:141** — setBwbPhase performs db.update(videoJobs).where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit tenant_id, causing an UPDATE against the tenant-scoped video_jobs table without a tenant constraint.
  - _Fix:_ Ensure the call provides a validated tenantId (or have scope() assert tenant presence). Always include AND tenant_id = <tenant> in the WHERE clause; fail the call if tenant is not known rather than relying solely on jobId.
- **server/lib/bwb-job-progress.ts:166** — updateBwbChapters performs db.update(videoJobs).where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit tenant_id, allowing an UPDATE on the tenant-scoped video_jobs table without tenant scoping.
  - _Fix:_ Require and validate a tenantId for this operation and include eq(videoJobs.tenantId, tenantId) in the WHERE. If tenantId is unavailable, abort (fail closed) instead of performing an unscoped update.
- **server/lib/bwb-job-progress.ts:200** — completeBwbJob reads the live row via db.select(...).where(scope(jobId)) and later updates video_jobs with db.update(...).where(scope(jobId)). If scope() omits tenant_id, both the SELECT and UPDATE run without tenant scoping, violating tenant isolation.
  - _Fix:_ Pass and assert a tenantId to these calls and ensure scope() always includes eq(videoJobs.tenantId, tenantId). If tenant cannot be determined, refuse to perform the select/update (fail closed) to avoid cross-tenant modifications.
- **server/lib/bwb-job-progress.ts:227** — bumpBwbHeartbeat executes db.update(videoJobs).where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit tenant_id, allowing an UPDATE against video_jobs without tenant scoping.
  - _Fix:_ Require a validated tenantId for heartbeat updates and include tenant predicate in the WHERE. If the tenant is not available, do not perform the DB update.
- **server/lib/bwb-job-progress.ts:245** — failBwbJob executes db.update(videoJobs).where(scope(jobId, { notDone: true })) — scope() may omit tenant_id, permitting an UPDATE that can mark another tenant's job as failed because tenant scoping is not enforced.
  - _Fix:_ Ensure tenantId is provided and validated before performing the update, and always include eq(videoJobs.tenantId, tenantId) in the WHERE clause. Fail the operation if tenant context is missing rather than relying solely on jobId.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT from tenant-scoped `projects` table without constraining to a tenant_id. The query `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id` aggregates across all tenants, allowing one tenant's rows to affect short-circuit logic for other tenants (misattribution / skipped backfill).
  - _Fix:_ Restrict reads to the intended tenant(s). Options: (a) run the count query per snapshot tenant and include `WHERE tenant_id = ${tenantId}` for each, or (b) pass an explicit list of tenant ids and use a parameterized WHERE (e.g. `WHERE tenant_id = ANY(${tenantIdArray}::int[])`) so the DB only returns counts for those tenants. Avoid global, cross-tenant aggregation when enforcing per-tenant invariants.
- **server/lib/jury-experience.ts:174** — String-interpolated SQL used to inject an embedding vector literal into the query (sql`${`[${embedding.join(",")}]`}::vector`). The embedding is derived from request data and is embedded into SQL text that may bypass proper parameterization leading to injection or type-casting issues.
  - _Fix:_ Do not inline-build SQL literals from request-derived values. Bind the embedding as a proper parameter (e.g. pass the array as a parameter and use parameterized array/vector handling or use the DB/driver's vector parameter support). Example: use a parameterized insert for the vector column (e.g. VALUES (${tenantId}, ${parsed.requestClass}, ..., ${embedding}::vector) where the driver/pg binding sends the array as a parameter) or construct the query so the DB cast is applied to a parameter ($1::vector) rather than interpolating a string. Validate and normalize the embedding before binding.
- **server/lib/outcome-promotion.ts:158** — A cross-tenant aggregate SELECT runs without a tenant_id WHERE clause: "SELECT tenant_id, format, count(*)... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format" — this reads a tenant-scoped table across all tenants.
  - _Fix:_ Restrict scans to a single tenant at a time or run the job per-tenant. Add a tenant_id filter (WHERE tenant_id = ${tenantId}) or, if this background job must examine every tenant, ensure it runs in an audited admin context with explicit authorization and hard-controls. Prefer refactoring to iterate tenant-by-tenant to preserve the invariant that tenant-scoped reads are constrained by tenant_id.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` without a tenant_id filter. The query returns tenant_id for each row but does not restrict by tenant, exposing rows across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId}) or, if an all-tenant audit is required, move this functionality behind an explicit admin-only API and ensure callers are authorized and the exposure is intentional and documented. When returning results to non-admin callers, filter by the caller's tenant_id.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` performed with no tenant_id constraint, returning rows across tenants (or emitting tenant_id values) without per-tenant filtering.
  - _Fix:_ Either add WHERE tenant_id = ${tenantId} to scope results to a tenant, or restrict this multi-tenant scan to an admin-only codepath with explicit authorization. Avoid returning cross-tenant data to unprivileged callers.
- **server/martech-bundle.ts:325** — UPDATE on tenant-scoped table `tenant_voice_profiles` filters only by id (WHERE id = existing.id) and does NOT constrain by tenant_id. Application-level tenant isolation invariant requires every write to include the caller's tenant filter.
  - _Fix:_ Add an explicit tenant check to the UPDATE WHERE clause (e.g. WHERE id = ${existing.id} AND tenant_id = ${opts.tenantId}) or use a Drizzle .where(and(eq(table.id, existing.id), eq(table.tenantId, opts.tenantId))). Also verify `existing` belongs to the caller before updating as defense-in-depth.
- **server/memory-graph.ts:124** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} updates a tenant-scoped table by id only and omits a tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}). Prefer using parameterized bindings rather than trusting id-only updates so a row from another tenant cannot be mutated.
- **server/memory-graph.ts:183** — refreshCategoryCount runs an UPDATE on memory_categories by id and the embedded subquery SELECTs FROM memory_entries without restricting on tenant_id. Both the outer UPDATE and the inner SELECT lack tenant constraints, allowing cross-tenant counting and updates.
  - _Fix:_ Add tenant_id filters to the subquery and to the outer UPDATE (e.g. SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} ... ; UPDATE memory_categories SET ... WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Ensure the function accepts/propagates the tenantId and uses it in both places.
- **server/memory-graph.ts:239** — Existence check SELECT 1 FROM memory_links ... LIMIT 1 does not filter on tenant_id. This reads a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or join to memory_entries (with tenant filter) as appropriate. Always ensure SELECTs against tenant-scoped tables include tenant_id.
- **server/memory-graph.ts:494** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped table by id only and omits tenant_id in the WHERE clause.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) so the update cannot affect rows belonging to other tenants.
- **server/memory-intelligence.ts:399** — UPDATE knowledge_triples SET valid_until = ... WHERE id = ${c.id} updates a tenant-scoped table by id only and omits tenant_id. Although the id originated from a tenant-scoped query, the invariant requires explicit tenant constraints on writes.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}) to ensure the update cannot be applied to another tenant's row.
- **server/minerva-planner.ts:386** — SELECT probe (SELECT status, ceo_decision FROM plans ...) conditionally omits tenant filtering when args.tenantId is undefined, allowing cross-tenant reads (IDOR).
  - _Fix:_ Do not perform tenant-scoped SELECTs without an explicit tenant filter. Require tenantId as a parameter for external/tool-facing callers and always include AND tenant_id = ${tenantId} (or Drizzle eq(table.tenantId, tenantId)). If internal callers legitimately omit tenantId, isolate that callpath (internal API) so external callers cannot trigger an unscoped read.
- **server/minerva-planner.ts:403** — Child-plan SELECT (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...) conditionally appends tenant filter. When args.tenantId is omitted this query can return rows from other tenants.
  - _Fix:_ Always constrain child-plan lookups by tenant. Require and pass tenantId, and add AND tenant_id = ${tenantId} (or use Drizzle .where(eq(...))) to the query. If the code path is intended for internal callers only, split the function and ensure callers in tenant-facing routes cannot omit tenantId.
- **server/minerva-planner.ts:549** — getPlan falls back to an unscoped SELECT when tenantId is omitted: SELECT * FROM plans WHERE id = ${planId}. This allows cross-tenant reads if callers omit tenantId.
  - _Fix:_ Require tenantId for any external/tool-facing callers and always include AND tenant_id = ${tenantId} in the SELECT. If this function must support internal trusted callers without tenant scoping, separate the internal-only variant (with explicit trust boundary) from the tenant-facing API and ensure the public callsite always supplies tenantId.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. validateSubscriptionsOnStartup runs SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE and processes rows for all tenants with no tenant_id filter.
  - _Fix:_ If this routine must operate across all tenants, ensure it runs under a privileged/admin context and that handling of secrets is tightly controlled (avoid logging secrets, minimize in-memory lifetime). If it is intended to be per-tenant, restrict the query with WHERE tenant_id = ${tenantId} or iterate tenants in a controlled, audited way. Prefer explicit tenant scoping for reads where possible.
- **server/oauth-subscriptions.ts:915** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. A later query SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' fetches across all tenants.
  - _Fix:_ Restrict this query by tenant_id where appropriate. If the intention is to discover tenant_ids for global startup auto-reconnect, make that explicit and run only in a secure, admin-only startup path; otherwise narrow the query to the intended tenant set.
- **server/oauth-subscriptions.ts:1258** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. proactiveTokenRefresh issues SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE and then refreshes tokens across all tenants.
  - _Fix:_ If proactiveTokenRefresh must iterate all tenants, ensure it is executed in a hardened admin background job with least-privilege handling of secrets. If the function should be tenant-scoped, change it to accept a tenantId and add WHERE tenant_id = ${tenantId}. Consider minimizing returned columns and avoiding decryption/logging of secrets during the loop.
- **server/oauth-subscriptions.ts:1328** — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. forceRefreshAllSubscriptions SELECTs provider and tenant_id FROM oauth_subscriptions WHERE is_active = TRUE (global sweep).
  - _Fix:_ Either make this function explicitly admin-only and harden handling/auditing, or require an explicit tenantId parameter and add WHERE tenant_id = ${tenantId} for tenant-scoped refreshes. Consider adding RBAC checks and limiting which callers can trigger a global sweep.
- **server/pace-control.ts:85** — SELECT from tenant-scoped table `heartbeat_logs` without tenant constraint. checkPace queries heartbeat_logs to count runs in the sliding window but does not filter by tenant_id, so counts are global across tenants.
  - _Fix:_ Make pace checks tenant-aware: accept a tenantId parameter and include WHERE tenant_id = ${tenantId} in the query. If the semantics are intentionally global, document and restrict the call site to an admin context. Also ensure persona-level filters remain correct when scoping by tenant.
- **server/pace-control.ts:140** — SELECT from tenant-scoped table `heartbeat_logs` without tenant constraint. getPaceSnapshot queries recent heartbeat_logs to produce dashboard snapshots but lacks tenant_id filtering (returns global counts).
  - _Fix:_ Scope the snapshot to a tenant by adding tenant_id = ${tenantId} to the WHERE clause and by accepting tenantId as an argument. If a global cross-tenant admin view is required, restrict access to admin users and audit usage; otherwise prefer per-tenant aggregation.
- **server/process-governor.ts:198** — SELECT AVG(duration_ms) from heartbeat_logs is missing tenant scoping — query reads cross-tenant rows without WHERE tenant_id = ${tenantId}.
  - _Fix:_ Add tenant scoping to the query (e.g. AND tenant_id = ${tenantId}) or otherwise validate/derive tenantId so the AVG only uses the caller's tenant data.
- **server/process-governor.ts:240** — SELECT COUNT(DISTINCT persona_id) from heartbeat_logs is missing tenant scoping — reads failing agent counts across all tenants.
  - _Fix:_ Constrain the query by tenant (e.g. add AND tenant_id = ${tenantId}) so counts are per-tenant only.
- **server/process-governor.ts:121** — Subselects counting heartbeat_logs inside task_failure_rate omit tenant constraint — these correlated subqueries can read logs from other tenants.
  - _Fix:_ Include tenant filter in the subselects (e.g. AND tenant_id = ${tenantId}) or otherwise ensure the referenced heartbeat_logs rows are scoped to the same tenant as the heartbeat_tasks row.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} has no tenant filter — increments rule counters without scoping to tenant.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so only the current tenant's rule row is updated.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project row without any tenant check — a caller-supplied projectId can access projects across tenants.
  - _Fix:_ Require and pass a tenantId to updateProjectBrain and include WHERE tenant_id = ${tenantId} in the SELECT, or validate that the project belongs to the caller's tenant before reading/updating project-scoped data.
- **server/project-brain.ts:332** — Backfill query selecting projects joined to conversations (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...) lacks tenant filtering — it enumerates projects across all tenants.
  - _Fix:_ Scope the backfill to a specific tenant (add WHERE p.tenant_id = ${tenantId} or require tenantId param) to avoid cross-tenant data access.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} executes without tenant scoping — conversations may be tenant-scoped and are being queried by project id only.
  - _Fix:_ Include tenant filtering (e.g. AND tenant_id = ${tenantId}) or ensure proj.id is validated as belonging to the tenant before querying conversations.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping — can expose messages from other tenants' conversations if IDs collide or are not validated.
  - _Fix:_ Add tenant constraint (e.g. AND tenant_id = ${tenantId}) or require/validate tenant ownership of the conversation prior to selecting messages.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` uses only `WHERE id = ${r.id}` and does not include `tenant_id` — this can modify rows belonging to another tenant if an id from a different tenant is supplied or iterated.
  - _Fix:_ Include the tenant in the WHERE clause (e.g. `WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}`) so every UPDATE is constrained to the row's tenant. Do not rely on the fact that the id was read earlier without re-checking tenant ownership in the UPDATE statement.
- **server/reference-learner.ts:290** — UPDATE on tenant-scoped table `memory_entries` uses `WHERE id = ${storedId}` only and omits tenant_id/persona_id checks. This can update another tenant's memory entry if ids collide or are misused.
  - _Fix:_ Constrain the UPDATE by tenant and persona (for example: `WHERE id = ${storedId} AND tenant_id = ${input.tenantId} AND persona_id = ${personaId}`) so writes cannot affect rows outside the caller's tenant. Always include tenant_id in UPDATE/DELETE WHERE clauses rather than relying solely on previously-read id values.
- **server/research-engine.ts:165** — UPDATE on tenant-scoped table research_sessions is executed without any tenant_id constraint — this will mark running sessions as completed across all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant. Add a tenant filter to the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND status = 'running' ...) or, if cleaning across many tenants is intentional, iterate per-tenant with explicit tenant context. With Drizzle, prefer .where(eq(research_sessions.tenant_id, tenantId)). Ensure the tenantId used is validated as a positive integer before use.
- **server/research-engine.ts:400** — DELETE FROM agent_knowledge is executed without any tenant_id constraint — this will delete autoresearch knowledge entries for all tenants.
  - _Fix:_ Scope the DELETE to the session's tenant. Add AND tenant_id = ${session.tenantId} to the WHERE clause (or use Drizzle's .where(eq(agent_knowledge.tenant_id, session.tenantId))). Validate session.tenantId before use to fail-closed if absent.
- **server/routes.ts:2059** — GET /api/presenter/:token selects presenter_sessions by token with no tenant scoping and returns session data. tenant_id is taken from DB row but not enforced against caller; token is used as a capability but the DB read is unscoped.
  - _Fix:_ Treat the token strictly as a capability (documented), and ensure the query excludes tenant-scoped columns or explicitly validates the token maps to a tenant authorized for this request. Prefer storing and validating a signed capability (HMAC) that binds both token and tenant, or include tenant_id in the query and verify the requester is authorized for that tenant before returning data.
- **server/routes.ts:2144** — INSERT INTO presenter_sessions uses tenantId taken directly from the request body (caller-supplied) without validating the caller is authorized to create sessions for that tenant.
  - _Fix:_ Do not accept tenantId directly from untrusted callers. Validate that the caller (session, internal key, or admin) is permitted to create a session for the specified tenant, or derive tenantId from the authenticated session or an internally-validated capability. If the endpoint is truly internal, document and strongly protect it (e.g. mutual TLS, internal-only network).
- **server/routes.ts:4333** — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts (project_id, note, author) without setting tenant_id, violating the invariant that tenant-scoped INSERTs must pass tenantId explicitly.
  - _Fix:_ Include tenant_id in the INSERT (and value param) so the row is created under the caller's tenant. E.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, use a storage helper that enforces tenant scoping and ownership checks before inserting.
- **server/routes/admin.ts:744** — SELECT FROM tenant-scoped table `heartbeat_tasks` executed without constraining by tenant_id. The query (`SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3 ...`) can return rows for other tenants because there is no `WHERE tenant_id = <caller tenant>` or equivalent filter.
  - _Fix:_ If the intent is to only surface heartbeat tasks for the calling tenant, add an explicit tenant predicate, e.g. `WHERE tenant_id = ${tenantId} AND (enabled = false OR consecutive_failures >= 3) ...`. If cross-tenant visibility is intended, make the allowed scope explicit (require platform admin via requirePlatformAdmin) and audit that this cross-tenant access is acceptable. Prefer using parameterized queries (drizzle sql templates or db.select(...).where(eq(...))) and avoid returning tenant data without a tenant filter.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table `agent_jobs` is executed without any tenant_id predicate (global query).
  - _Fix:_ Add an explicit tenant constraint to the query (e.g. WHERE j.tenant_id = ${tenantId} or WHERE j.tenant_id = ${ADMIN_TENANT_ID} depending on intent). If the query must remain global, make that explicit (audit/log) and guard the route with a platform-admin check and clear justification.
- **server/routes/projects.ts:232** — UPDATE projects SET ... WHERE id = ${projectId} is missing an explicit tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure the update cannot affect another tenant's row.
- **server/routes/projects.ts:300** — UPDATE projects SET ... WHERE id = ${projectId} is missing an explicit tenant_id constraint (multer upload handler).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Even after validating the project earlier, every write should include the tenant_id WHERE clause.
- **server/routes/projects.ts:319** — SELECT * FROM project_files WHERE project_id = ${projectId} lacks an explicit tenant_id constraint.
  - _Fix:_ Constrain the read to the caller's tenant (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join back to projects with a tenant check. Every read of tenant-scoped tables must include tenant_id.
- **server/routes/projects.ts:342** — SELECT file record from `project_files` WHERE project_id = ${projectId} AND file_path = ... has no tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or otherwise ensure the query is constrained to the caller's tenant. Do not rely solely on prior project checks.
- **server/routes/projects.ts:371** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks an explicit tenant_id constraint.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}) so the deletion is enforced by tenant at the DB level.
- **server/routes/projects.ts:34** — Subqueries counting project_files/project_notes/project_conversations in the projects listing do not include tenant_id filters (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id).
  - _Fix:_ Constrain subqueries to the tenant (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or rewrite using JOINs that enforce tenant_id. Every read against tenant-scoped tables must include the tenant constraint.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without constraining to tenant_id (missing AND tenant_id = ${...}). This allows modifying a session row by id across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. add AND tenant_id = ${params.tenantId} (or ${tenantId} in scope). Prefer passing the tenantId into the function/context and always include AND tenant_id = ${tenantId} on writes to tenant-scoped tables.
- **server/sculptor.ts:112** — UPDATE sculptor_sessions setting conversation_id WHERE id = ${session.id} has no tenant_id constraint. This can update another tenant's row if ids collide or are guessable.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${params.tenantId} or the session's tenant) so writes are limited to the caller's tenant. Validate ownership before writing.
- **server/sculptor.ts:169** — monitorSession updates sculptor_sessions (status/result/tool_calls_count/tokens_used/completed_at) using WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant: include AND tenant_id = ${tenantId} in the WHERE clause (pass tenantId into monitorSession if needed). Always scope writes to tenant-scoped tables by tenant_id.
- **server/sculptor.ts:360** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} lacks tenant scoping — write performed without verifying the session belongs to the caller's tenant.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause and/or re-fetch and verify the session belongs to the tenant before writing. Ensure all updates to tenant-scoped tables include tenant_id checks.
- **server/second-opinion.ts:405** — settleFusionReservation issues UPDATE agent_cost_ledger ... WHERE id = ${reservationId} but does not constrain by tenant_id. agent_cost_ledger is tenant-scoped, so this can modify another tenant's ledger row if ids are misused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Propagate tenantId into settleFusionReservation (or look up and verify the reservation row's tenant before updating) and enforce ownership on ledger writes.
- **server/second-opinion.ts:425** — releaseFusionReservation issues UPDATE agent_cost_ledger ... WHERE id = ${reservationId} with NO tenant_id constraint, allowing cross-tenant modification of ledger rows.
  - _Fix:_ Constrain the UPDATE by tenant_id (AND tenant_id = ${tenantId}) and ensure releaseFusionReservation receives/derives the tenantId (or verifies the reservation belongs to the caller) before performing the update.
- **server/seed-cleanup.ts:15** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true runs without tenant_id constraint, disabling delegation tasks across all tenants.
  - _Fix:_ If the change should be limited to a specific tenant, add AND tenant_id = ${tenantId} (e.g. ADMIN_TENANT_ID) to the WHERE clause. If global action is intended, ensure the caller is an admin and that this global behaviour is acceptable; otherwise restrict updates to the intended tenant(s).
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant_id restriction and will disable matching tasks across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or use ADMIN_TENANT_ID if this should only affect admin tenant). Ensure any writes to tenant-scoped tables include tenant_id in the WHERE clause to enforce isolation.
- **server/seed-cleanup.ts:75** — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has no tenant_id constraint and will disable matching tasks for all tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant by adding AND tenant_id = ${tenantId} (e.g. ADMIN_TENANT_ID) or otherwise ensure the script runs with explicit global authority. Always include tenant_id when modifying tenant-scoped tables.
- **server/seed.ts:36** — SELECT against tenant-scoped table research_sessions without any tenant_id constraint (SELECT COUNT(*) FROM research_sessions). This reads across all tenants.
  - _Fix:_ Constrain the query to the caller's tenant. Pass a tenantId and use a WHERE tenant_id = ${tenantId} (or Drizzle .where(eq(research_sessions.tenantId, tenantId))). Example: SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}.
- **server/seed.ts:2158** — SELECT from research_programs without tenant constraint (SELECT id, name, model FROM research_programs WHERE is_active = true). This returns programs from all tenants.
  - _Fix:_ Limit to the current tenant. Add WHERE tenant_id = ${tenantId} (or use Drizzle .where(eq(research_programs.tenantId, tenantId))). If the intent is cross-tenant maintenance, make that explicit and run as an admin-only job with explicit tenant scoping in each update step.
- **server/seed.ts:2163** — UPDATE on research_programs (UPDATE research_programs SET model = ... WHERE id = ${p.id}) lacks a tenant_id constraint; this may modify rows belonging to other tenants if ids collide.
  - _Fix:_ Include tenant ownership in the WHERE clause when updating tenant-scoped rows: e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId} (or Drizzle .where(and(eq(research_programs.id, p.id), eq(research_programs.tenantId, tenantId)))). Do not update by id alone unless tenant is verified.
- **server/seed.ts:2450** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... — update against projects has no tenant filter. This can change another tenant's project row.
  - _Fix:_ Add tenant scoping to the UPDATE: include tenant_id = ${tenantId} (or use Drizzle .where(eq(projects.tenantId, tenantId))). Ensure tenant ownership is validated before any write to tenant-scoped tables.
- **server/seed.ts:2515** — SELECT from conversations without tenant constraint (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). Conversations are tenant-scoped; this reads across tenants.
  - _Fix:_ Constrain the SELECT to the tenant (WHERE c.tenant_id = ${tenantId}) or otherwise verify caller ownership of returned conversation rows before using them.
- **server/seed.ts:3812** — UPDATE on tenant-scoped table `memory_entries` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This will modify rows across all tenants.
  - _Fix:_ Add an explicit tenant filter to the WHERE clause (e.g. AND tenant_id = 1 or parametrize tenantId) or perform the update per-tenant. With Drizzle prefer .where(eq(memoryEntries.tenantId, tenantId)). Ensure tenantId is a trusted value.
- **server/seed.ts:3838** — UPDATE on tenant-scoped table `conversations` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This will update conversations for all tenants.
  - _Fix:_ Include tenant constraint in the WHERE clause (e.g. WHERE model = 'gpt-4.1' AND tenant_id = <tenant>) or iterate per-tenant. Using Drizzle: .where(eq(conversations.tenantId, tenantId)). Ensure tenantId is validated/trusted.
- **server/seed.ts:3784** — DELETE FROM `heartbeat_tasks` is executed without a tenant_id constraint. `heartbeat_tasks` is tenant-scoped, so this DELETE can remove rows across all tenants.
  - _Fix:_ Restrict the DELETE to the intended tenant (e.g. add AND tenant_id = 1 or parametrize tenantId) or limit the scope explicitly. Prefer Drizzle .where(eq(heartbeatTasks.tenantId, tenantId)) to avoid cross-tenant deletion.
- **server/seed.ts:4051** — UPDATE on tenant-scoped table `agent_knowledge` uses only id in the WHERE clause (WHERE id = $id) and does not include tenant_id. Even if the id was obtained earlier with a tenant filter, the UPDATE itself lacks an explicit tenant constraint.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = $id AND tenant_id = <tenant>) or use Drizzle .where(and(eq(agentKnowledge.id, id), eq(agentKnowledge.tenantId, tenantId))). Always enforce tenant_id at the SQL statement doing the mutation as an invariant.
- **server/self-improvement.ts:170** — detectStagnation performs a SELECT on the tenant-scoped experiments table without guaranteeing a tenant_id filter when tenantId is omitted. The .where(...) uses only category if tenantId isn't provided, allowing cross-tenant reads.
  - _Fix:_ Make tenant scoping mandatory (throw if tenantId is missing) or always include an explicit tenant filter in the WHERE (e.g. .where(and(eq(experiments.category, category), eq(experiments.tenantId, tenantId)))). Avoid building queries that can execute without tenant constraints.
- **server/self-improvement.ts:401** — UPDATE on tenant-scoped table experiments uses WHERE eq(experiments.id, experiment.id) with no tenant_id constraint. Updating by id alone can modify rows belonging to other tenants.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) or pass and use the caller's tenantId to guard every UPDATE/DELETE against tenant-scoped tables.
- **server/self-improvement.ts:415** — In the error path the code updates experiments (SET status='error', outcome=...) using WHERE eq(experiments.id, experiment.id) without a tenant_id condition, risking cross-tenant modifications.
  - _Fix:_ Same fix as above: add an explicit tenant filter to the UPDATE (AND experiments.tenant_id = <caller tenant>) or ensure the update is executed in a context that enforces the tenant id (and validate it).
- **server/self-reflection.ts:366** — storeLesson updates memory_entries using WHERE id = <existing id> without also constraining tenant_id. Although the row was located earlier with a tenant filter, the UPDATE lacks tenant-scoping and could affect another tenant's record with the same id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or use an UPDATE that includes both id and tenant_id to ensure the operation is constrained to the caller's tenant.
- **server/storage.ts:242** — getConversationUnscoped performs SELECT FROM conversations WHERE id = ${id} with NO tenant_id constraint (unscoped read of a tenant-scoped table).
  - _Fix:_ Avoid unscoped reads. Require caller to pass tenantId or explicitly require admin privilege. If an unscoped fetch is unavoidable, restrict use to internal orchestration with explicit audit/logging and require downstream callers to validate tenant ownership; preferably add an explicit flag/param and enforce it at callsites.
- **server/storage.ts:298** — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL with NO tenant_id constraint on messages (tenant-scoped table read).
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or, if ids come from a tenant-scoped prior read, pass the validated tenantId into attachCitationMetadata and assert it before executing the raw query.
- **server/storage.ts:827** — getMemoriesWithoutEmbeddings selects from memory_entries WHERE status = 'active' AND embedding IS NULL with NO tenant_id constraint — unscoped read across tenants.
  - _Fix:_ Require a tenantId or an explicit ALL_TENANTS sentinel (and use requiredTenantScope when appropriate). If this is a background worker intended to process all tenants, make that explicit and ensure worker runs under an audited admin context.
- **server/storage.ts:833** — getKnowledgeWithoutEmbeddings selects from agent_knowledge with no tenant filter (notExpired && isNull(embedding)) — unscoped read across tenants.
  - _Fix:_ Require tenant scoping (tenantId param or ALL_TENANTS sentinel) and use requiredTenantScope/tenantScope to ensure reads are restricted to the intended tenant(s). If run as a global background job, make the admin intent explicit and auditable.
- **server/storage.ts:942** — getHeartbeatTask selects from heartbeat_tasks WHERE id = ${id} with NO tenant_id constraint — unscoped read of a tenant-scoped table.
  - _Fix:_ Require tenantId or otherwise scope lookup (e.g. .where(and(eq(heartbeatTasks.id, id), eq(heartbeatTasks.tenantId, tenantId)))) or require an explicit admin sentinel when a cross-tenant task lookup is intended.
- **server/tool-learning.ts:291** — UPDATE of tenant-scoped table `custom_tools` uses only id in WHERE (db.update(...).where(eq(customTools.id, tool.id))) with no tenant_id constraint — allows modifying a row if id is known across tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or otherwise verify ownership before performing the update.
- **server/tool-learning.ts:316** — DELETE from tenant-scoped table `custom_tools` uses only id in WHERE (db.delete(...).where(eq(customTools.id, tool.id))) with no tenant_id constraint — allows deleting a row by id across tenants.
  - _Fix:_ Require and include the tenant ownership filter in the DELETE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or explicitly verify the row belongs to the caller's tenant prior to deletion.
- **server/tool-learning.ts:365** — UPDATE of tenant-scoped table `custom_tools` to increment usageCount uses only id in WHERE (db.update(...).where(eq(customTools.id, tool.id))) with no tenant_id constraint.
  - _Fix:_ Add a tenant check to the UPDATE (e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId)))) or ensure the update is performed only after an ownership-validated fetch to prevent cross-tenant modification.
- **server/tools.ts:2915** — Subquery counts in the project list omit tenant scoping. Example: "(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)" (and similar COUNTs for project_notes and project_conversations) do not filter by tenant_id.
  - _Fix:_ Constrain subqueries by tenant_id to the caller's tenant. Example: (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = p.tenant_id) or explicitly AND tenant_id = ${tenantId}. Prefer binding to the verified tenant value rather than relying on implicit joins.
- **server/tools.ts:3031** — Subquery counts in the project search omit tenant scoping. The SELECT uses subqueries like "(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)" and "(SELECT COUNT(*) FROM project_notes WHERE project_id = p.id)" without tenant filters.
  - _Fix:_ Add tenant constraints to these subqueries (e.g. AND tenant_id = p.tenant_id or AND tenant_id = ${tenantId}) so counts cannot cross tenants. Always include tenant_id in reads against tenant-scoped tables.
- **server/tools.ts:2976** — Duplicate-check SELECT against project_files in add_file lacks tenant constraint. The dupCheck queries: "SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ..." do not filter by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND file_name = ${fname} ...). Even if project ownership was checked earlier, every read against a tenant-scoped table must explicitly constrain tenant_id.
- **server/tools.ts:3016** — Existence-check SELECT against project_conversations in link_conversation omits tenant scoping. The statement "SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId}" does not include tenant_id.
  - _Fix:_ Restrict the check with tenant_id (e.g. WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) so the existence/duplication logic cannot be influenced across tenants.
- **server/tools.ts:4529** — SELECT on tenant-scoped tables (conversations / project_conversations) without tenant_id constraint. The COALESCE subquery queries conversations and project_conversations by conversation_id/project_id but does not include WHERE tenant_id = <caller tenant>.
  - _Fix:_ Constrain these reads to the caller's tenant. Example: include tenant_id in the WHERE clauses or join through a verified tenant-scoped query. E.g. WHERE id = ${convId} AND tenant_id = ${params._tenantId} for conversations, and WHERE conversation_id = ${convId} AND tenant_id = ${params._tenantId} for project_conversations (or use the app's Drizzle eq(table.tenantId, tenantId) helper). Do not rely solely on prior in-memory asserts — enforce tenant scope in the SQL itself.
- **server/tools.ts:4565** — SELECT from presenter_sessions by presentation_id with no tenant_id filter. This reads a likely tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Add an explicit tenant filter to the SELECT, e.g. WHERE presentation_id = ${slideResult.presentationId} AND tenant_id = ${params._tenantId}, or otherwise use a Drizzle .where(eq(table.tenantId, tenantId)). If presentation ownership must be asserted first, still include tenant_id in the SQL to meet the project invariant.
- **server/tools.ts:4540** — INSERT INTO project_notes does not set tenant_id. Project-scoped persistent writes omit tenant_id in VALUES (tenant_id column not provided).
  - _Fix:_ Include tenant_id explicitly in the INSERT values (and bind it from the authenticated caller). Example: INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ...). If you rely on prior ownership checks, still write tenant_id into the row to enforce tenant scoping at the data layer.
- **server/tools.ts:4544** — INSERT INTO project_files (presentation path) omits tenant_id in VALUES. Tenant identifier is not persisted on the project_files row.
  - _Fix:_ Add tenant_id to the INSERT column list and VALUES, e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${projectId}, ...). Always persist tenant_id on tenant-scoped tables.
- **server/tools.ts:6390** — INSERT INTO project_files in generate_audio path omits tenant_id. The code relies on assertProjectInTenant but does not include tenant_id in the inserted row.
  - _Fix:_ Include tenant_id in the INSERT (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...) so every persisted project_files row is explicitly tenant-scoped by the caller's tenant.
- **server/tools.ts:6922** — INSERT INTO project_files in create_slideshow_video omits tenant_id. The write to project_files does not set tenant_id even though project is asserted.
  - _Fix:_ Persist tenant_id explicitly: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...). Do not rely only on prior assertProjectInTenant; include tenant_id in the SQL to enforce isolation at the data level.
- **server/tools.ts:6926** — INSERT into tenant-scoped table 'project_files' omits tenant_id. The INSERT uses project_id (already asserted owned) but does not set tenant_id column.
  - _Fix:_ Include tenant_id in the INSERT and set it to the caller's tenant (e.g. ${params._tenantId}). Prefer using Drizzle's typed insert (db.insert(projectFiles).values({...})) and ensure tenant_id is explicitly provided. Example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...).
- **server/tools.ts:8891** — UPDATE against tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE clause and lacks a tenant_id constraint. This can allow cross-tenant updates by id alone.
  - _Fix:_ Add a tenant filter to the WHERE clause so updates are scoped to the tenant. For example: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) (ensure 'tid' is the calling tenant id). Use Drizzle's eq helper for safe parameterization.
- **server/tools.ts:8916** — UPDATE against tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE clause and lacks a tenant_id constraint. This can allow cross-tenant updates by id alone.
  - _Fix:_ Add a tenant filter to the WHERE clause so updates are scoped to the tenant. For example: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) (ensure 'tid' is the calling tenant id). Use Drizzle's eq helper for safe parameterization.
- **server/tools.ts:8933** — UPDATE against tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE clause and lacks a tenant_id constraint. This can allow cross-tenant updates by id alone.
  - _Fix:_ Add a tenant filter to the WHERE clause so updates are scoped to the tenant. For example: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) (ensure 'tid' is the calling tenant id). Use Drizzle's eq helper for safe parameterization.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed without tenant scoping: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` lacks a tenant_id constraint, allowing cross-tenant modification if an attacker knows another workflow.id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. include tenant_id in the WHERE clause: `WHERE id = ${workflow.id} AND tenant_id = ${tenantId}` (or use Drizzle: .where(and(eq(browserWorkflows.id, workflow.id), eq(browserWorkflows.tenantId, tenantId)))) and/or verify the row belongs to ctx.tenantId before updating.
- **server/tools/domains/files/handlers.ts:835** — INSERT into project_files omits tenant scoping column/value: `INSERT INTO project_files (project_id, file_name, ...) VALUES (${projId}, ${fileName}, ...)` does not set tenant_id explicitly. Tenant-scoped INSERTs must include the tenant identifier.
  - _Fix:_ Include tenant_id in the INSERT values (and bind it from the trusted ctx): e.g. `INSERT INTO project_files (project_id, tenant_id, file_name, file_path, ...) VALUES (${projId}, ${tenantId}, ${fileName}, ${absPath}, ...)`. Ensure the tenantId is taken from the trusted ctx and validated (not from untrusted params).
- **server/tools/domains/files/handlers.ts:839** — UPDATE projects executed without tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` lacks tenant_id in the WHERE clause, allowing modification of a project row by id alone without verifying tenant ownership at the SQL level.
  - _Fix:_ Add tenant scoping to the UPDATE: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}` (or use Drizzle .where(and(eq(projects.id, projId), eq(projects.tenantId, tenantId)))). Also consider checking ownership earlier and failing closed if the project is not owned by ctx.tenantId.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys is executed without a tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). This reads a tenant-scoped table across tenants (even though the branch is admin-only) and can expose provider key material or counts across tenant boundaries.
  - _Fix:_ Restrict these queries to an explicit tenant scope (e.g. WHERE tenant_id = ${tenantId}) or otherwise document and enforce a deliberate admin-only cross-tenant access policy. Avoid selecting raw API key material; if only a count/telemetry is needed, perform an aggregated/count query scoped per-tenant or require an auditable owner-only API and ensure the query is parameterized with the tenantId.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM provider_keys is executed without a tenant_id constraint. This reads a tenant-scoped table across all tenants (admin branch) and may leak cross-tenant provider configuration.
  - _Fix:_ Scope the query by tenant (e.g. WHERE tenant_id = ${tenantId}) or implement an explicit, auditable admin-only path that intentionally aggregates across tenants. Prefer per-tenant aggregates or require an owner-only API with clear authorization checks before allowing cross-tenant reads.
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE on tenant-scoped table `memory_entries` updates row by id only (WHERE id = ${id}) with no tenant_id constraint. This allows an update to a memory entry owned by another tenant if an attacker can guess an id.
  - _Fix:_ Constrain UPDATEs to the caller's tenant. Add tenant_id = ${tid} (and persona_id when relevant) to the WHERE clause, e.g. WHERE id = ${id} AND tenant_id = ${tid}; or first SELECT the row and verify its tenant_id equals ctx.tenantId before updating.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with no tenant_id constraint. This will bump last_accessed / access_count for rows across tenants if ids contain cross-tenant ids.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Alternatively, only update ids that were fetched under the tenant-scoped SELECT (filter ids to those rows whose tenant_id matches ctx.tenantId) before executing the UPDATE.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE on tenant-scoped table `memory_entries` updates row by id only (WHERE id = ${id}) with no tenant_id constraint (mirror of earlier dedupe/update). This can update another tenant's memory entry.
  - _Fix:_ Add tenant scoping to the UPDATE statement, e.g. WHERE id = ${id} AND tenant_id = ${tid}; or verify the row's tenant_id belongs to ctx.tenantId before performing the UPDATE.
- **server/video-job-runner.ts:178** — Upsert/ON CONFLICT update against tenant-scoped table video_jobs does not constrain by tenant_id — onConflictDoUpdate updates by jobId and a timestamp check only, with no tenant_id condition. This can allow cross-tenant writes if jobId collisions or other issues occur.
  - _Fix:_ Include tenant scoping in the onConflict/update predicate (e.g. include eq(videoJobs.tenantId, row.tenantId) in the WHERE clause) or otherwise ensure the upsert only affects rows with the matching tenant_id. Alternatively, check existing row's tenant_id first and abort if it mismatches.
- **server/video-job-runner.ts:236** — Global UPDATE in recoverStaleVideoJobs modifies video_jobs rows across all tenants (WHERE status IN (...) AND updatedAt < staleCutoff) without tenant_id constraint. This will mark stale jobs for every tenant in a single call and violates per-tenant isolation invariants.
  - _Fix:_ Restrict the stale-job sweep to an explicit administrative/maintenance context that is authorized to act across tenants, or iterate per-tenant and include WHERE tenant_id = ${tenantId} for each tenant. At minimum, add tenant scoping or require an explicit admin flag and audit the operation so it cannot accidentally affect other tenants.
- **server/webhook-triggers.ts:60** — SELECT joins the tenant-scoped `personas` table without constraining by tenant. The query `SELECT w.*, p.name as persona_name FROM webhook_triggers w LEFT JOIN personas p ON p.id = w.persona_id ...` can read persona rows across tenants because there is no WHERE p.tenant_id = <expected tenant>.
  - _Fix:_ Restrict the join to the intended tenant context (for example: JOIN personas p ON p.id = w.persona_id AND p.tenant_id = <admin or resolved tenant id>), or fetch persona via the application storage layer with an explicit tenantId check before returning persona_name. Ensure any persona data returned is validated to belong to the expected tenant (ADMIN_TENANT_ID if triggers are admin-scoped).
- **server/webhook-triggers.ts:89** — INSERT into `webhook_triggers` stores a caller-supplied personaId directly (`VALUES (..., ${config.personaId || null})`) with no verification that the referenced persona belongs to the expected tenant. This lets an attacker or misconfigured caller reference a persona from another tenant.
  - _Fix:_ Validate persona ownership before persisting persona_id. For example, if caller is admin, ensure the personaId belongs to the admin tenant; otherwise, look up the persona via the storage layer using the caller's tenantId and reject or clear personaId if it does not belong to that tenant. Never persist foreign tenant ids without explicit ownership checks.
- **server/webhook-triggers.ts:122** — Query that fetches a trigger (`SELECT w.*, p.name as persona_name ... WHERE w.webhook_key = ${webhookKey} ...`) LEFT JOINs `personas` without constraining persona rows to a tenant. This performs an unconstrained read against the tenant-scoped `personas` table when resolving persona_name for a trigger.
  - _Fix:_ When resolving persona information for a trigger, either (a) restrict the join by persona.tenant_id = <admin or resolved tenant id>, or (b) remove the join and resolve persona via an explicit, tenant-scoped lookup (storage.getPersona with tenantId) and validate ownership before returning persona details. Ensure persona reads are always performed with an explicit tenant_id filter.
- **server/wiring-invariants.ts:244** — Querying tenant-scoped table `tool_performance` without tenant_id constraint (SELECT count(*) FROM tool_performance ...). This reads across all tenants.
  - _Fix:_ Scope the query by tenant (e.g. add WHERE tenant_id = <tenantId> or iterate tenants and run the check per-tenant). If the intent is a global metric, make that explicit and ensure only authorized admin code runs it; otherwise enforce tenant_id filtering in the SQL.
- **server/wiring-invariants.ts:258** — Querying tenant-scoped table `tool_performance` without tenant_id constraint (SELECT count(*) FROM tool_performance). This reads across all tenants.
  - _Fix:_ Add a tenant_id WHERE clause or run this aggregation per-tenant. If a cross-tenant total is intentionally required, document and gate it to admin-only code paths; otherwise enforce tenant scoping.
- **server/wiring-invariants.ts:260** — Querying tenant-scoped `messages` table without tenant_id constraint (SELECT count(*) FROM messages ...). This aggregates chat volume across tenants.
  - _Fix:_ Restrict the query to a specific tenant (WHERE tenant_id = <tenantId>) or iterate tenants to compute per-tenant counts. If this global check is intended, make the admin intent explicit and ensure it's safe and authorized.
- **server/wiring-invariants.ts:293** — Querying tenant-scoped table `tool_performance` without tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance ...). This can leak/aggregate data across tenants.
  - _Fix:_ Add WHERE tenant_id = <tenantId> or otherwise perform the uniqueness calculation per-tenant. If a global view is required, restrict to admin usage and explicitly document that the table is being read across tenants.
- **server/wiring-invariants.ts:366** — Using sql.raw to SELECT COUNT/MAX from subsystem tables (spec.table) such as `knowledge_nudges` and `tool_performance` without any tenant_id constraint. These subsystem tables are tenant-scoped but are queried across all tenants here.
  - _Fix:_ Scope these probes by tenant (WHERE tenant_id = <tenantId>) or run the freshness check per-tenant. Avoid unscoped COUNT/MAX on tenant-scoped tables unless you intentionally require a cross-tenant admin report and have explicit authorization/audit around it.
- **server/wiring-invariants.ts:417** — Querying tenant-scoped table `tool_performance` without tenant_id constraint in tool-drift detector (SELECT ... FROM tool_performance WHERE last_failure_at > ...). This groups/aggregates across tenants.
  - _Fix:_ Add tenant_id = <tenantId> to the WHERE/GROUP BY logic or run the drift detection per tenant. If the check must be global, require explicit admin scoping and document the cross-tenant semantics.
- **server/wiring-invariants.ts:535** — Using sql.raw to SELECT COUNT/pg_total_relation_size FROM MEMORY_TABLES (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without tenant_id filtering. These are likely tenant-scoped stores and are being aggregated across tenants.
  - _Fix:_ Perform counts and size checks per-tenant (add WHERE tenant_id = <tenantId> or iterate tenants). If a global health check is desired, make it explicit and restrict to admin-only operations with documented intent.
- **server/wiring-invariants.ts:561** — Querying tenant-scoped `graph_memory` without tenant_id constraint (SELECT COUNT(*) FROM graph_memory). This reads across all tenants.
  - _Fix:_ Scope the query by tenant_id (WHERE tenant_id = <tenantId>) or run it per-tenant. If a cross-tenant aggregate is required, ensure it's an explicit admin-only check and that this behavior is documented and approved.

### MEDIUM (35)

- **server/agent-channels.ts:194** — Query joins channel_subscriptions (cs) without constraining cs.tenant_id. The join uses cs.persona_id = ${personaId} but omits cs.tenant_id, so channel_subscriptions rows from other tenants could be matched.
  - _Fix:_ Include cs.tenant_id = ${tenantId} in the JOIN/WHERE (or otherwise scope the join) so the channel_subscriptions side is constrained to the caller's tenant.
- **server/agentic-features.ts:274** — Subquery inside prevSnaps (SELECT DISTINCT ON (url) id FROM competitor_snapshots WHERE competitor_id = ${comp.id} ORDER BY ...) lacks tenant scoping even though outer queries reference tenant_id. A subquery reads competitor_snapshots without tenant constraint.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the inner subquery as well (or reference only ids already known to belong to the tenant) so all reads against competitor_snapshots are tenant-scoped.
- **server/agentic-features.ts:653** — SELECT against outreach_sequence_steps uses sequence_id from an enrollment but does not include tenant scoping (SELECT * FROM outreach_sequence_steps WHERE sequence_id = ${enrollment.sequence_id} AND step_number = ${enrollment.current_step}). If outreach_sequence_steps is tenant-scoped, the row read is not explicitly constrained to tenant.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${params.tenantId}) or otherwise ensure sequence_id uniqueness across tenants. Prefer explicit tenant WHERE clauses for safety.
- **server/auto-transcript.ts:128** — Backfill query scans conversations across all tenants: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... — there is no tenant_id constraint. This enumerates conversations system-wide without an explicit tenant filter.
  - _Fix:_ If the intent is to backfill for all tenants, make that explicit and ensure downstream processing enforces per-tenant scoping safely (prefer iterating tenants and executing tenant-scoped queries). Otherwise add WHERE c.tenant_id = ${tenantId} or otherwise limit the query by tenant.
- **server/auto-transcript.ts:171** — Query to find missing links (SELECT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND EXISTS (SELECT 1 FROM projects p WHERE p.id = c.project_id AND p.tenant_id = c.tenant_id) AND NOT EXISTS (...)) scans across tenants without an external tenant constraint — this is a global scan of tenant-scoped tables.
  - _Fix:_ Either restrict the query to a single tenant (WHERE c.tenant_id = ${tenantId}), or explicitly iterate tenants and run tenant-scoped queries. Ensure any cross-tenant scans are intentional and safe, and document/guard them appropriately.
- **server/capability-review.ts:415** — Manual construction of a Postgres array literal from untrusted data (surfaced names) and embedding it into the SQL VALUES clause. The code builds arrLiteral as a JS string from review.doNotRebuild (untrusted/retrieved content) and then injects that string into the db.execute(sql`... VALUES (..., ${arrLiteral}::text[], ...)`). This is effectively string-interpolated SQL with request-derived content and risks injection or subtle parsing errors (and is flagged by the project's isolation/injection policy).
  - _Fix:_ Stop assembling a DB array literal by hand. Pass the names as a native parameterized array (or as JSONB) instead of building a literal string. Example fixes: 1) keep surfaced as a JS string[] and use the DB driver's array binding (e.g. VALUES (..., ${surfacedArray}::text[], ...)) so the array is sent as a parameter rather than hand-crafted SQL; or 2) store surfaced_names as jsonb and INSERT the JS array via a parameterized JSON value; or 3) use the driver's prepared/parameterized APIs to bind a text[] parameter. Always avoid concatenating or hand-formatting SQL literals from untrusted input, and ensure tenant_id continues to be passed explicitly (it already is).
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` performed with no tenant_id filter (scanAndEscalate reads all commitments across tenants). This is a cross-tenant read.
  - _Fix:_ If this cross-tenant scan is intentional for platform-admin workflows, document/approve it and ensure the consumer path is strictly admin-only and redacts tenant-scoped content. Otherwise, scope the SELECT by tenant_id = ${tenantId} or split admin-only logic into a separate, explicitly-audited routine that enforces least privilege and logging.
- **server/data-protection.ts:356** — SELECT project_notes by project_id (`SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}`) does not include tenant scoping. The code earlier ensures `proj` came from a tenant-scoped query, but the invariant requires explicit tenant constraints on reads of tenant-scoped tables.
  - _Fix:_ Add tenant scoping to these queries (e.g. `AND tenant_id = ${tenantId}`) or join back to projects (which already has tenant_id) to enforce the tenant predicate at query time. Apply the same for project_files and similar per-project reads.
- **server/data-protection.ts:368** — SELECT project_files by project_id (`SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}`) lacks an explicit tenant_id constraint, relying on prior project selection instead of enforcing tenant scoping on the table being read.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` (or equivalent join to a tenant-scoped projects row) when querying project_files. Ensure every read of a tenant-scoped table includes an explicit tenant predicate rather than depending on prior reads/ownership assumptions.
- **server/dfy-intake.ts:50** — getDfyIntakeByToken selects dfy_intake_forms by token without any tenant_id constraint. The dfy_intake_forms table is tenant-scoped, so this read is not constrained to a caller's tenant.
  - _Fix:_ If the token is intended to be globally-unguessable and to authorize access across tenants, document that explicitly and ensure this is only used in safe contexts; otherwise add tenant scoping (require/derive tenantId and include eq(dfyIntakeForms.tenantId, tenantId)) or store/verify the tenant_id alongside the token and filter on it.
- **server/email.ts:471** — Inbox polling dedupe query SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 has no tenant or inbox scoping. This checks for an existing message globally and may cause cross-tenant behaviour when multiple tenants share message ids or inboxes.
  - _Fix:_ Scope the dedupe query by inbox_id or tenant_id (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} LIMIT 1) so the check is limited to the relevant tenant/inbox context.
- **server/email.ts:516** — backfillEmptyBodies selects inbox_messages rows with empty bodies via SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') AND (body_html IS NULL OR body_html = '') ORDER BY id with no tenant or inbox constraint. This scans the global table and operates across tenants.
  - _Fix:_ Restrict backfill scans to a specific inbox or tenant (add WHERE tenant_id = ${tenantId} or WHERE inbox_id = ${inboxId}) or otherwise run as an admin-only job with documented intentional global scope. Prefer explicit scoping in SQL to avoid accidental cross-tenant processing.
- **server/lib/restraint-trust.ts:88** — Dynamic SQL identifier via sql.raw(column) using a column derived from caller-supplied outcome (even though validated/whitelisted) — this embeds a runtime-derived identifier into the query text instead of using a fully-parameterized, compile-time-safe path.
  - _Fix:_ Avoid embedding runtime-derived identifiers into SQL text. Replace the dynamic-column UPDATE with an explicit, outcome-driven code path (e.g. switch on outcome and run one of four fixed parameterized UPDATE statements), or use a database API that supports passing identifiers safely (e.g. a proper identifier binding helper). Keep the allowlist validation, but remove sql.raw(column) usage so no request-derived value ever becomes raw SQL text.
- **server/lib/system-probes.ts:163** — COUNT(*) query against tenant-scoped table `messages` (messagesTable) runs with no tenant_id filter, aggregating message counts across all tenants.
  - _Fix:_ If the probe is intended to report per-tenant metrics, include WHERE tenant_id = ${tenantId}; if it must aggregate across tenants, ensure the probe runs only in an authorized admin/system context and consider exposing per-tenant breakdowns or guarding this query behind explicit admin-only access so tenant data isn't unintentionally exposed.
- **server/minds-engine.ts:214** — Create mind_ticket INSERT builds a Postgres array string via `{${params.dependsOn.join(",")}}` from request-provided params and interpolates it into the SQL template. This is string-interpolated SQL content derived from caller input (injection risk / isolation risk).
  - _Fix:_ Avoid constructing SQL array text by string-joining user input. Pass arrays as proper bound parameters or use Drizzle's array binding (or parameterized queries) so the DB driver handles escaping/binding, e.g. use WHERE id = ANY(${params.dependsOn}) or equivalent parameterized INSERT for depends_on. Validate/normalize dependsOn as an array of integers before passing.
- **server/minds-engine.ts:262** — checkDependenciesMet builds an ANY(...) array via string interpolation: id = ANY(${`{${ticket.dependsOn.join(",")}}`}::int[]). This interpolates values into SQL text (derived from ticket.dependsOn) rather than using parameterized array binding.
  - _Fix:_ Use parameterized array binding instead of constructing a literal array string. For example, pass dependsOn as an array parameter and use id = ANY(${ticket.dependsOn}) with the driver/ORM handling binding, or explicit parameterization via Drizzle/pg array support. Also validate that dependsOn elements are integers.
- **server/minds-engine.ts:327** — markEventsHandled uses id = ANY(${`{${eventIds.join(",")}}`}::int[]) — it constructs a PostgreSQL array literal by joining caller-supplied eventIds and embeds it into SQL, which is a string-interpolated SQL value derived from request input.
  - _Fix:_ Replace the ad-hoc array literal construction with a parameterized array binding (pass eventIds as an array parameter to the query or use the ORM's array helper). Validate eventIds elements are integers before binding to avoid injection and ensure tenant isolation is preserved.
- **server/recurring-messages.ts:258** — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id constraint (global scan). This returns rows for all tenants in a single query.
  - _Fix:_ If this global scan is intentional for a cron/runner, restrict downstream updates/operations by including tenant checks on every subsequent write (prefer including tenant_id in the SELECT’s WHERE when possible). Otherwise, add `AND tenant_id = ${tenantId}` to the query to scope to a single tenant. For global runner use, ensure the function explicitly documents and enforces per-row tenant ownership on every update — prefer selecting with tenant_id and iterate per-tenant.
- **server/routes.ts:167** — SELECT from file_storage in restoreUploadsFromDb (startup) has no tenant_id filter — reads all tenants' file rows.
  - _Fix:_ If this operation must be tenant-scoped, add a WHERE file_storage.tenant_id = <owner> or iterate per-tenant; otherwise ensure it runs only with explicit admin intent and that restored files cannot be served to other tenants. Avoid blanket selects across tenant-scoped tables.
- **server/routes.ts:766** — SELECT FROM projects JOIN tenants queries all projects across tenants (no tenant_id constraint). This reads tenant-scoped rows without limiting to a caller/admin context.
  - _Fix:_ Restrict the query to an explicit tenant context (WHERE p.tenant_id = <tenant>) or require/verify platform-admin context before operating across tenants. If cross-tenant admin backfill is intentional, gate it behind an explicit admin check and document the behaviour.
- **server/routes.ts:1118** — SELECT file_storage rows by filename without tenant filter in /uploads middleware — fetches all owners for a basename (no tenant_id in WHERE).
  - _Fix:_ If the intent is to check existence across tenants defend explicitly: (a) limit returned columns and avoid returning any PII, and (b) clearly document and gate this behaviour. Where possible, prefer parameterized queries scoped to tenant or use an existence-check that does not disclose rows belonging to other tenants.
- **server/routes.ts:1561** — Public architecture endpoint issues many SELECTs against tenant-scoped tables (personas, projects, skills, memory_entries, heartbeat_tasks, etc.) without tenant_id constraints — exposing cross-tenant data/metadata.
  - _Fix:_ Either scope these queries to a tenant (WHERE tenant_id = <tenant>) or restrict the endpoint to platform-admins. Avoid selecting tenant-scoped rows in public endpoints unless the result is intentionally aggregated and safe to publish.
- **server/routes.ts:2091** — SELECT from presenter_slide_images by session_id (derived from an earlier unscoped presenter_sessions lookup) has no tenant_id constraint — a caller-supplied token/param is used to fetch images with no tenant verification.
  - _Fix:_ Either include tenant scoping on presenter_slide_images (e.g. session->tenant join or tenant_id column) or verify the session belongs to an authorized tenant before fetching images. Ensure session lookup enforces ownership or that the capability token is validated in a way that prevents cross-tenant access.
- **server/routes.ts:2174** — SELECT presenter_sessions WHERE token = ${token} in /api/presenter/:token/repair-images reads presenter_sessions unscoped (no tenant check) after only an internal-key check — tenant ownership of the session is not enforced.
  - _Fix:_ Either scope the query by tenant_id or verify that the internal authentication mechanism explicitly authorizes operations against the tenant referenced by the session. Prefer deriving tenant context from a validated capability tied to the token or include tenant in the guard and check it against the session's tenant_id.
- **server/routes/admin.ts:731** — SELECT FROM `ai_insights` performed without a tenant constraint. The route queries high/priority self-heal insights (`SELECT id, engine_type, category, title, priority, created_at FROM ai_insights WHERE status = 'new' ...`) but does not include `tenant_id = <caller tenant>` (or equivalent), so it may surface insights from other tenants.
  - _Fix:_ Restrict reads to the caller's tenant by adding `AND tenant_id = ${tenantId}` (or build the query via Drizzle with eq(ai_insights.tenantId, tenantId)). If platform-wide admin intent exists, require and document a platform-admin gate (requirePlatformAdmin) and ensure the cross-tenant read is approved and logged.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table `inbox_messages` omits tenant_id in the WHERE clause: `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}`.
  - _Fix:_ Include the tenant predicate in the update to prevent accidental cross-tenant mutation, e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}` (or use the originally-validated msg.tenant_id as a bound parameter).
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` is performed without a tenant_id WHERE clause (aggregates over all tenants).
  - _Fix:_ Either restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or make the global aggregation explicit and ensure the metrics token is strictly operator-only and audited. Prefer adding an explicit application-level tenant predicate when per-tenant data is intended, or clearly gate and document true cross-tenant aggregations.
- **server/routes/oauth-subscriptions.ts:325** — SELECT DISTINCT tenant_id FROM tenant-scoped table `oauth_subscriptions` without tenant predicate (scans all tenants) in cron/heal path.
  - _Fix:_ If the heal/cron action must be global, ensure the endpoint is strictly authenticated/authorized (it is), and explicitly document this global intent. Otherwise constrain the query via a tenant predicate (e.g. tenant_id = ${ADMIN_TENANT_ID} or accept a validated list of tenant IDs) so tenant-scoped tables are not touched without an explicit application-level filter.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' executes without scoping to tenant_id. heartbeat_tasks appears tenant-scoped elsewhere, so this read may cross tenants.
  - _Fix:_ If this operation is intended to be per-tenant, add WHERE tenant_id = ${tenantId} (or run using ADMIN_TENANT_ID explicitly). If the script must act globally, make the intent explicit and ensure it's run under an admin/global context; otherwise scope reads to the appropriate tenant.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant filter; it reads possibly tenant-scoped rows across tenants.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) if the intent is tenant-local. If this is an admin/global maintenance action, guarantee it's executed in an explicit admin context and document it.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE '%examine repository%' OR ... ) executes without tenant scoping.
  - _Fix:_ If the check should be per-tenant, add tenant_id = ${tenantId} to the WHERE clause. Otherwise, ensure the global intent is deliberate and executed under proper admin privileges.
- **server/seed.ts:2523** — INSERT into project_conversations omits tenant_id (INSERT INTO project_conversations (project_id, conversation_id) VALUES ...). If project_conversations is meant to be tenant-scoped, this write creates relations without tenant context and can cause cross-tenant linking.
  - _Fix:_ Ensure project_conversations includes tenant_id and set it explicitly on INSERT: INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conversationId}) ON CONFLICT DO NOTHING. If the table is intentionally global, document and guard cross-tenant linking explicitly.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining tenant_id (reads by name lack an explicit tenant filter).
  - _Fix:_ Include the tenant ownership filter in the WHERE clause (e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))) or otherwise validate the row belongs to the provided tenant before using it).
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` uses only id (where(eq(customTools.id, inserted.id))) with no tenant_id constraint.
  - _Fix:_ Add an explicit tenant constraint to the SELECT (e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))) so rows are always validated to belong to the caller's tenant.
- **server/video-job-runner.ts:208** — SELECT on tenant-scoped table video_jobs in isCancelRequested reads by jobId only (WHERE jobId = ...) with no tenant_id constraint. This reveals cancel state for a job without verifying that the caller is the owning tenant.
  - _Fix:_ Require and pass tenantId into isCancelRequested and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise verify the job belongs to the expected tenant before returning cancel status). Treat DB-read failures conservatively but still enforce tenant scoping on reads.

### LOW (1)

- **server/routes.ts:1532** — Public stats endpoint runs SELECTs against tenant-scoped tables (conversations, messages, heartbeatTasks, memory_entries, heartbeat_logs) without tenant_id filters, aggregating data across tenants.
  - _Fix:_ If these global aggregates are intentional, keep them admin-gated or ensure aggregated numbers cannot be abused; otherwise add tenant_id filters or provide admin-only endpoints for cross-tenant aggregates.

### Suppressed — confirmed false positives (249)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:243** _(system-sweep)_ — autoApplyEligibleInsights executes a non-tenant-scoped UPDATE: "UPDATE ai_insights SET status = 'applied', action_taken = ${AUTO_APPLY_REASON} WHERE status = 'new' AND category IN (...) RETURNING id, tenant_id, ..." — this bulk-updates rows for all tenants.
  - _Why false positive:_ Platform-wide backfill sweep intentionally updates eligible ai_insights across tenants and routes each returned row using its own tenant_id; housekeeping/status mutation, not tenant-mixed output.
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — runDueWakes updates agent_wake_schedules via an id IN (SELECT id FROM agent_wake_schedules ... FOR UPDATE SKIP LOCKED) subquery that contains no tenant_id filter. The outer UPDATE also has no tenant_id constraint. This allows the scheduler to claim/update pending wakes across all tenants (cross-tenant mutation/visibility).
  - _Why false positive:_ Heartbeat scheduler intentionally claims due wakes across all tenants and processes each row with its own tenant_id when emitting/finalizing; cross-tenant maintenance loop, no tenant-mixed response.
- **server/auto-memorize.ts:163** _(system-sweep)_ — SELECT from messages (pull recent messages window) lacks any tenant_id filter. The code then sends per-tenant transcripts to LLMs; an unscoped SELECT can return messages across tenants and leak data to other tenants' prompts.
  - _Why false positive:_ Global message scan is explicitly partitioned by m.tenant_id before any prompt construction; comments and code show per-tenant synthesis with fail-closed skip on invalid tenant ids.
- **server/data-protection.ts:149** _(system-sweep)_ — Global SELECT of expired conversations pulls rows across all tenants: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` has NO tenant_id constraint. This lets the maintenance job enumerate other tenants' conversations and then later perform destructive operations without per-tenant scoping.
  - _Why false positive:_ Permanent purge is a platform-wide housekeeping sweep over expired soft-deleted conversations; each row carries tenant_id and downstream child deletes are per-row scoped, with no tenant-facing mixed output.
- **server/data-protection.ts:163** _(upstream-verified)_ — DELETE from messages uses `DELETE FROM messages WHERE conversation_id = ${conv.id}` with NO tenant_id constraint. messages is tenant-scoped elsewhere; deleting by conversation_id alone risks removing another tenant's messages if ids collide or FK assumptions change.
  - _Why false positive:_ The actual DELETE shown already includes AND tenant_id = conv.tenant_id, so the first-pass claim of missing tenant scoping does not match the code.
- **server/data-protection.ts:164** _(upstream-verified)_ — DELETE from compaction_archives uses `DELETE FROM compaction_archives WHERE conversation_id = ${conv.id}` with NO tenant_id constraint, allowing cross-tenant deletions.
  - _Why false positive:_ flagged DELETE already includes AND tenant_id = conv.tenant_id; first-pass claim does not match code
- **server/data-protection.ts:165** _(system-sweep)_ — DELETE from conversations uses `DELETE FROM conversations WHERE id = ${conv.id}` with NO tenant_id constraint. This can delete conversations across tenants (race/ID reuse scenarios).
  - _Why false positive:_ boot/cron purge intentionally sweeps expired conversations across tenants and only performs housekeeping deletion per row
- **server/embeddings.ts:684** _(system-sweep)_ — UPDATE on tenant-scoped table (memory_entries | agent_knowledge) performed without constraining by tenant_id. storeEmbeddingVec builds a table name and runs: UPDATE <table> SET ... WHERE id = ${id} — no tenant filter. This allows cross-tenant writes to tenant-scoped rows.
  - _Why false positive:_ embedding backfill/update is platform-wide maintenance updating housekeeping fields only, not tenant-facing data mixing
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without constraining by tenant_id. This is a write to a tenant-scoped table without tenant filtering, enabling modification of other tenants' rows.
  - _Why false positive:_ backfillEmbeddingVecs is a maintenance sweep that updates only embedding_vec on selected rows across tenants
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint. This writes to a tenant-scoped table without ensuring ownership, allowing cross-tenant modification.
  - _Why false positive:_ backfillEmbeddingVecs is a maintenance sweep that updates only embedding_vec on selected rows across tenants
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT INTO project_files is performed without supplying tenant_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (...)). If project_files is tenant-scoped and tenant_id is NOT NULL with no default, this is a direct cross-tenant write risk (and may create rows with no tenant).
  - _Why false positive:_ project_files has no tenant_id column and ownership is transitively enforced via tenant-scoped project after assertProjectInTenant
- **server/heartbeat.ts:381** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10 runs without any tenant_id constraint. This reads pending events for all tenants and then processes/routs them based on event.tenant_id — a missing tenant filter here permits cross-tenant enumeration and processing risks.
  - _Why false positive:_ global heartbeat worker intentionally polls pending events across tenants and processes each row using its own tenant_id
- **server/heartbeat.ts:2680** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without any tenant_id constraint after creating a delegated task. This can change approval state of a task record regardless of tenant ownership.
  - _Why false positive:_ updates freshly created server-side task by its generated primary key; id is not from untrusted request boundary
- **server/heartbeat.ts:3131** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) lacks tenant scoping. This updates heartbeat_tasks approval state without verifying the task belongs to the caller's tenant.
  - _Why false positive:_ updates freshly created server-side task by its generated primary key; id is not from untrusted request boundary
- **server/job-queue.ts:98** _(system-sweep)_ — claimDueJobs runs an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs WHERE status = 'pending' ... ) to claim jobs but does not constrain by tenant_id — it atomically claims jobs across all tenants with no tenant filter.
  - _Why false positive:_ Global worker queue claim is intentional cross-tenant housekeeping; it only mutates job lease/status and returns rows to internal workers, not tenant-facing mixed output.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE selects due rows and UPDATE applies across all tenants). This reads+updates tenant rows across tenants with no WHERE tenant_id = ... filter.
  - _Why false positive:_ Heartbeat runner intentionally picks due scheduled posts across tenants for background publishing; rows are handled per-row with returned tenant_id, not exposed as mixed tenant output.
- **server/memory-graph.ts:252** _(fk-transitive)_ — INSERT INTO memory_links does not set tenant_id (memory_links is tenant-scoped). New links will be created without an explicit tenant_id, violating the invariant and enabling cross-tenant linkage or orphaned rows.
  - _Why false positive:_ memory_links has no tenant_id column per schema note; link rows are transitively scoped by source/target memory_entries foreign keys.
- **server/minerva-planner.ts:367** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `plans` is conditionally scoped. The UPDATE CAS uses ${args.tenantId != null ? sql`AND tenant_id = ${args.tenantId}` : sql``}, so when args.tenantId is omitted the WHERE clause lacks tenant_id and can modify rows across tenants.
  - _Why false positive:_ Optional unscoped path is explicitly for internal system callers operating on server-generated plan ids, not request-derived ids; tenant-facing routes pass tenantId.
- **server/minerva-planner.ts:472** _(upstream-verified)_ — Rollback UPDATE (UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising') has no tenant_id constraint at all, so it can modify any tenant's plan row by id.
  - _Why false positive:_ This rollback runs only after the earlier CAS returned row.tenant_id for the same planId, so ownership of the exact row was already established in the same call path.
- **server/mpeg-engine.ts:1051** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The INSERT INTO project_files (...) VALUES (...) call omits tenant_id even though this is a per-project/tenant resource and table rows are expected to be tenant-scoped.
  - _Why false positive:_ project_files has no tenant_id column per schema note and is inserted only after assertProjectInTenant validates the tenant-scoped parent project.
- **server/mpeg-engine.ts:1417** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id. The upsert for project_files in produceVideoParallel does not pass tenant_id in the INSERT values.
  - _Why false positive:_ project_files has no tenant_id column per schema note and is inserted only after assertProjectInTenant validates the tenant-scoped parent project.
- **server/plan-executor.ts:118** _(upstream-verified)_ — appendExecutionLog() performs UPDATE plans ... WHERE id = ${planId} without constraining by tenant_id on a tenant-scoped table.
  - _Why false positive:_ appendExecutionLog is an internal helper called from executePlan after the plan row is claimed/read and its tenant_id is loaded; same call path already established the exact row context.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — executePlan() claims a plan via UPDATE plans SET status='executing' WHERE id = ${planId} AND status='approved' — this CAS update modifies a tenant-scoped row without any tenant_id constraint, allowing cross-tenant claiming by planId.
  - _Why false positive:_ executePlan is an internal worker entrypoint claiming by server-generated planId, not directly from an untrusted tenant request boundary.
- **server/plan-executor.ts:728** _(upstream-verified)_ — Final status update (UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId}) does not include a tenant_id filter on a tenant-scoped table.
  - _Why false positive:_ Final status update occurs after the same execution path claimed and loaded the specific plan row including tenant_id; this is mutation of the already-owned row.
- **server/plan-executor.ts:762** _(upstream-verified)_ — Error-path UPDATE (UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing') updates a tenant-scoped row without filtering by tenant_id.
  - _Why false positive:_ Error-path update is within executePlan for the same internally claimed planId; the row context was already established earlier in the call path.
- **server/plan-executor.ts:778** _(system-sweep)_ — resumeStuckPlans() runs UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id — this mass update touches all tenants' plans because no tenant_id constraint is applied.
  - _Why false positive:_ Boot-time recovery is intentional platform-wide housekeeping resetting stale statuses and restarting by id; it mutates only workflow status fields and does not expose mixed tenant data.
- **server/plan-executor.ts:787** _(system-sweep)_ — resumeStuckPlans() then selects pending approved plans via SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 without tenant scoping, reading tenant-scoped table rows across tenants.
  - _Why false positive:_ Boot-time recovery sweep intentionally scans plans across tenants and only schedules per-row execution by id; no tenant data is returned or mixed in one output.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} has no tenant_id constraint — a write to a tenant-scoped table without tenant scoping can affect other tenants' rows.
  - _Why false positive:_ t.id comes from a prior SELECT limited to heartbeat_tasks with tenant_id = tenantId in the same call path.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} lacks tenant_id constraint — this mutates a tenant-scoped table without assuring ownership.
  - _Why false positive:_ desk.id is sourced from the immediately preceding SELECT on agent_desks constrained by ad.tenant_id = tenantId.
- **server/proposal-verifier.ts:33** _(internal-worker-pk)_ — SELECT id, target_file, code_diff, ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped proposals table without scoping by tenant_id — a caller-supplied proposalId can access proposals across tenants.
  - _Why false positive:_ Verifier is a server-side worker operating by globally unique proposal id, invoked asynchronously rather than directly from a tenant request boundary.
- **server/proposal-verifier.ts:107** _(internal-worker-pk)_ — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) updates a tenant-scoped table without tenant scoping.
  - _Why false positive:_ Update is part of the same server-internal verifier flow acting on a server-supplied proposal primary key.
- **server/proposal-verifier.ts:246** _(internal-worker-pk)_ — persist() performs UPDATE code_proposals ... WHERE id = ${proposalId} without a tenant filter — verification metadata is written to a proposal without confirming tenant ownership.
  - _Why false positive:_ persist() is only used within the verifier worker on a globally unique proposal id, not a cross-tenant request parameter in this snippet.
- **server/proposal-verifier.ts:372** _(internal-worker-pk)_ — UPDATE code_proposals SET status = 'rejected', ... WHERE id = ${proposalId} is executed without tenant scoping after LLM indicates 'NO_LONGER_APPLICABLE' — this mutates proposal state across tenants.
  - _Why false positive:_ State change occurs inside verifier/rebase worker logic keyed by server-invoked proposal id.
- **server/proposal-verifier.ts:408** _(internal-worker-pk)_ — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without tenant filtering.
  - _Why false positive:_ Rebased diff persistence is in the same internal verifier worker operating on a globally unique proposal primary key.
- **server/routes/conversations.ts:99** _(upstream-verified)_ — UPDATE on tenant-scoped table without tenant constraint: `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` has no `AND tenant_id = ${tenantId}` filter.
  - _Why false positive:_ conv was just created with tenantId and project ownership was checked with projects.id + tenant_id = tenantId before this update.
- **server/routes/conversations.ts:100** _(fk-transitive)_ — SELECT against (likely) tenant-scoped join table without tenant constraint: `SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}` does not filter by tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and is a join child isolated through tenant-scoped project and conversation parents already validated in this flow.
- **server/routes/conversations.ts:103** _(fk-transitive)_ — INSERT into a tenant-scoped table without tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)` does not set tenant_id explicitly.
  - _Why false positive:_ project_conversations has no tenant_id; inserting the project/conversation link is correct via tenant-scoped parent rows.
- **server/routes/conversations.ts:105** _(upstream-verified)_ — UPDATE on tenant-scoped table without tenant constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` lacks `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ The same block first verifies the project row with WHERE id = projectId AND tenant_id = tenantId, then updates only that verified id.
- **server/routes/conversations.ts:229** _(fk-transitive)_ — INSERT into project-scoped table without tenant_id: `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id on insert.
  - _Why false positive:_ project_notes has no tenant_id and is scoped through validated tenant-owned projects/project_id
- **server/routes/projects.ts:185** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id.
  - _Why false positive:_ project_notes has no tenant_id; insert is tied to a project already checked with tenant_id
- **server/routes/projects.ts:225** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (files-base64 handler) does not include tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is attached to a project prevalidated for the caller tenant
- **server/routes/projects.ts:292** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (multer upload handler) does not include tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is attached to a project prevalidated for the caller tenant
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` without a tenant_id constraint (reads error counts across all tenants).
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; watchdog reads platform-global log data by design
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM tenant-scoped table `heartbeat_tasks` with no tenant_id filter — performs cross-tenant deletions.
  - _Why false positive:_ maintenance purge of old disabled tasks across tenants only deletes stale housekeeping rows and returns counts/ids internally
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM tenant-scoped table `event_log` with no tenant_id filter — performs cross-tenant deletions.
  - _Why false positive:_ maintenance cleanup deletes old processed/failed event rows across tenants without returning tenant data
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM tenant-scoped table `heartbeat_logs` with no tenant_id constraint (pruning old logs across all tenants).
  - _Why false positive:_ heartbeat_logs has no tenant_id and pruning platform-global logs is expected
- **server/storage.ts:841** _(system-sweep)_ — archiveExpiredMemories runs raw SQL UPDATE memory_entries SET status='archived' WHERE ... with NO tenant_id constraint (unscoped write across tenant-scoped memory_entries).
  - _Why false positive:_ global lifecycle maintenance archives expired memory rows by status only and returns counts, not tenant data
- **server/storage.ts:858** _(system-sweep)_ — archiveStaleMemories runs raw SQL UPDATE memory_entries SET status='archived' WHERE ... with NO tenant_id constraint (unscoped write across tenant-scoped memory_entries).
  - _Why false positive:_ global lifecycle maintenance archives stale memory rows by status/age only and returns counts, not tenant data
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — stampArgsEmbedding executes raw SQL UPDATE felix_proposals SET args_embedding = ${vec}::vector WHERE id = ${proposalId} AND args_embedding IS NULL with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ stampArgsEmbedding is invoked after same-call-path SELECT by proposalId and tenantId confirms owned row; write is best-effort backfill for that verified row
- **server/surprise-scorer.ts:141** _(upstream-verified)_ — scoreProposalSurprise updates actual_outcome_embedding via raw SQL: UPDATE felix_proposals SET actual_outcome_embedding = ${outcomeVec}::vector WHERE id = ${proposalId} with NO tenant_id constraint.
  - _Why false positive:_ scoreProposalSurprise first loads the proposal with WHERE id and tenant_id, and only proceeds if found; subsequent update targets that verified row in same call path
- **server/surprise-scorer.ts:181** _(upstream-verified)_ — When not enough neighbors, code runs UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} with NO tenant_id constraint (unscoped write).
  - _Why false positive:_ same function already verified proposal ownership with tenant-scoped SELECT before stamping no_history on that row
- **server/surprise-scorer.ts:212** _(upstream-verified)_ — After scoring, code updates surprise_score and surprise_band with UPDATE felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} — NO tenant_id constraint (unscoped write).
  - _Why false positive:_ same call path fetched the row with id and tenant_id before computing score, so final update applies to previously verified proposal
- **server/tools.ts:2982** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. The INSERT: "INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)" lacks an explicit tenant_id column/value, violating the invariant that tenant-scoped tables must be written with the caller's tenant.
  - _Why false positive:_ project_files has no tenant_id and is isolated through tenant-scoped parent projects; ownership is checked on projects before insert
- **server/tools.ts:2996** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. The INSERT: "INSERT INTO project_notes (project_id, note, author) VALUES (...)" omits tenant_id for a tenant-scoped resource.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; code verifies project belongs to tenant before insert
- **server/tools.ts:2878** _(fk-transitive)_ — INSERT into project_conversations (during project create/link flow) omits tenant_id. The statement "INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING" writes a tenant-scoped table without an explicit tenant_id column/value.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped parent project/conversation; code only inserts after tenant-scoped conversation update succeeds
- **server/tools.ts:3020** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) omits tenant_id. The code executes "INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)" after validating the conversation, but it does not store tenant_id on the new row.
  - _Why false positive:_ project_conversations has no tenant_id and transitive isolation is enforced by prior project ownership check plus assertConversationInTenant
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE against tenant-scoped table knowledge_triples uses only 'WHERE id = ${row.id}' with no tenant_id constraint. Every write to tenant-scoped tables must include the caller's tenant filter to enforce isolation.
  - _Why false positive:_ rows being updated come directly from preceding SELECT on knowledge_triples constrained by tenant_id = tenantId in the same function
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT on tenant-scoped table `experiments` executed without constraining to a tenant (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This reads rows across all tenants.
  - _Why false positive:_ heartbeat/maintenance loop intentionally scans running experiments across tenants and then processes each row with its own row-carried tenant_id.
- **server/agent-eval.ts:218** _(internal-worker-pk)_ — UPDATE to tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with no tenant constraint. This can update a row belonging to a different tenant if ids collide.
  - _Why false positive:_ row is inserted server-side just above and updated by returned serial id within same internal worker flow, not from an untrusted request boundary.
- **server/agentic-engines.ts:161** _(system-sweep)_ — SELECT in retryPendingMinervaRouting reads `ai_insights` rows without a tenant filter (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER}). This returns rows across tenants.
  - _Why false positive:_ durability sweep intentionally scans pending rows across tenants and routes each using that row's validated tenant_id; no mixed tenant output returned.
- **server/agentic-engines.ts:344** _(global-table)_ — In gatherOperationalData a heartbeat_logs count query is executed without tenant scoping (SELECT COUNT(*) ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads platform-wide heartbeat logs rather than tenant-specific ones.
  - _Why false positive:_ schema note says heartbeat_logs has no tenant_id, indicating a platform-global log table by design.
- **server/agentic-features.ts:658** _(upstream-verified)_ — UPDATE outreach_enrollments sets status = 'completed' WHERE id = ${enrollment.id} with no tenant filter. This is a write to a tenant-scoped table without constraining tenant_id.
  - _Why false positive:_ the actual UPDATE shown includes AND tenant_id = params.tenantId and status guard, so the first-pass claim does not match the code.
- **server/agentic-features.ts:700** _(upstream-verified)_ — UPDATE outreach_enrollments to advance the enrollment uses WHERE id = ${enrollment.id} with no tenant constraint. This can update another tenant's enrollment if ids collide.
  - _Why false positive:_ the UPDATE in this block explicitly constrains by tenant_id = params.tenantId; no missing tenant scope.
- **server/agentic-features.ts:749** _(upstream-verified)_ — UPDATE outreach_enrollments sets reply_classification, status, etc. using WHERE id = ${row.id} with no tenant constraint. This writes to a tenant-scoped row without verifying tenant ownership.
  - _Why false positive:_ Row id used in UPDATE comes from a prior SELECT on outreach_enrollments scoped by params.tenantId, so ownership is established in the same call path.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Query selects DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' with no tenant_id WHERE clause — reads rows for all tenants rather than a single tenant context.
  - _Why false positive:_ This is an explicit platform-wide heartbeat sweep iterating rows with their own tenant_id and emitting per-tenant budget events, not returning mixed tenant data.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT across agent_runs/agent_approvals scans rows for all tenants without constraining by tenant_id (no WHERE tenant_id = <caller tenant>) — reads tenant-scoped tables cross-tenant.
  - _Why false positive:_ Background worker sweep intentionally scans pending approvals across tenants and carries each row's tenant_id forward for per-row handling.
- **server/agentic/runs.ts:34** _(internal-worker-pk)_ — appendStep() updates agent_runs using WHERE id = runId but does not include tenant_id in the WHERE clause — update against a tenant-scoped table without tenant constraint.
  - _Why false positive:_ appendStep is an internal helper operating on server-generated agent_runs primary keys, not directly using untrusted tenant-crossing request input.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState() updates agent_runs WHERE id = runId with no tenant_id constraint — tenant-scoped write missing tenant filter.
  - _Why false positive:_ updateRunState is an internal server-side run-state transition by globally unique run id, a known worker/helper pattern.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState() performs an UPDATE on agent_runs WHERE id = runId but omits tenant_id — missing tenant scoping on a tenant-scoped table.
  - _Why false positive:_ replaceRunState updates internal agent_runs state by server-managed primary key rather than a caller-controlled cross-tenant boundary.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun() updates agent_runs status/result WHERE id = runId with no tenant_id constraint — tenant-scoped write lacks tenant filter.
  - _Why false positive:_ completeRun is an internal lifecycle update on agent_runs by server-generated run id.
- **server/agentic/runs.ts:70** _(internal-worker-pk)_ — completeRun() later does db.select().from(agentRuns).where(eq(agentRuns.id, runId)) without tenant filtering — tenant-scoped read missing tenant constraint.
  - _Why false positive:_ Follow-on read in completeRun is by the same internal server-managed run id to distill the just-completed run, not a tenant-facing lookup.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun() updates agent_runs WHERE id = runId but omits tenant_id — tenant-scoped update missing tenant filter.
  - _Why false positive:_ failRun is an internal status transition by globally unique run primary key.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun() updates agent_runs WHERE id = runId without including tenant_id — missing tenant constraint on a tenant-scoped table.
  - _Why false positive:_ pauseRun is an internal helper updating a server-created run by primary key, not a request-scoped tenant lookup.
- **server/auto-transcript.ts:46** _(upstream-verified)_ — Selecting messages for a verified conversation (SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}) is missing tenant_id in WHERE clause. This allows a read against a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ same call path first asserts conversation in tenant and re-reads conversation with tenant_id before selecting its messages.
- **server/auto-transcript.ts:165** _(system-sweep)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL is executed without scoping by tenant. This can modify a conversation row in another tenant if IDs are manipulated.
  - _Why false positive:_ boot/backfill maintenance loop selects only tenant-matched conversation/project links and updates housekeeping linkage per row; no tenant-facing mixed output.
- **server/chat-engine.ts:572** _(fk-transitive)_ — SELECT from tenant-scoped table 'project_files' has no tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id: `SELECT file_name, file_type, file_path, file_url, uploaded_by FROM project_files WHERE project_id = ${projectId} ORDER BY created_at DESC`.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped via parent projects row already verified with tenant_id.
- **server/chat-engine.ts:583** _(fk-transitive)_ — SELECT from tenant-scoped table 'project_notes' has no tenant_id constraint. The query uses only project_id: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} ORDER BY created_at DESC LIMIT 20`.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped via parent projects row already verified with tenant_id.
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE on tenant-scoped table `commitments` executed without constraining by tenant_id (unclaim path uses WHERE id = ${row.id} only). This can modify rows across tenants if IDs collide or row ownership differs.
  - _Why false positive:_ row.id comes from immediately prior tenant-scoped claim RETURNING c.id,c.tenant_id; unclaim targets that claimed row only.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on `credential_vault` sets last_used_at WHERE id = ${id} with no tenant_id constraint (getDecryptedPassword). This can update another tenant's row if IDs collide.
  - _Why false positive:_ same function first loads credential with getCredential(id, tenantId); subsequent last_used_at update targets that verified row by its unique id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on `credential_vault` sets last_used_at WHERE id = ${cred.id} with no tenant_id constraint (getLoginCredentials). This may update rows across tenants.
  - _Why false positive:_ Row is fetched immediately before via getCredentialForSite(siteUrl, tenantId); update uses that verified row id only.
- **server/data-protection.ts:214** _(upstream-verified)_ — SELECT of messages inside backupConversationToDrive uses `SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId}` with NO tenant_id constraint. Although the function earlier verified the conversation row belongs to the tenant, the invariant requires explicit tenant scoping on reads against tenant-scoped tables.
  - _Why false positive:_ Flagged read is already tenant-scoped in shown query chain; conversation, messages, and archives all include tenant_id = tenantId.
- **server/data-protection.ts:221** _(upstream-verified)_ — SELECT of compaction_archives inside backupConversationToDrive uses `SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId}` with NO tenant_id constraint, allowing cross-tenant reads.
  - _Why false positive:_ Archive read in same function explicitly filters compaction_archives by tenant_id = tenantId.
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings is executed without any tenant_id constraint: SELECT id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50. This reads rows across all tenants.
  - _Why false positive:_ Global maintenance backfill scans rows platform-wide, processes each row independently, and only writes embedding housekeeping fields by row id.
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT FROM memory_entries in backfillEmbeddingVecs is executed without tenant filtering: SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200. This reads embeddings across all tenants.
  - _Why false positive:_ BackfillEmbeddingVecs is a platform-wide maintenance job updating embedding_vec per row without returning tenant data or mixing content into one tenant-facing output.
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillEmbeddingVecs is executed without tenant filtering: SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200. This reads data across tenants.
  - _Why false positive:_ Platform-wide embedding_vec backfill over agent_knowledge is housekeeping only, with per-row updates and no tenant-facing data exposure.
- **server/felix-brain.ts:19** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' without any tenant_id constraint (rehydration reads all tenants' rows).
  - _Why false positive:_ Boot-time rehydration intentionally scans running/pending rows across tenants to rebuild in-memory state; it is maintenance logic using row-carried tenant data, not tenant-facing output.
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'felix_loop_runs' uses WHERE id = ${runId} but omits tenant_id check when writing run metadata (ended_at, context_summary, etc.).
  - _Why false positive:_ felix-loop updates a server-created run row by runId inside internal worker flow; id is not shown crossing an untrusted tenant request boundary.
- **server/firecrawl.ts:214** _(upstream-verified)_ — UPDATE scrapedPages uses .where(eq(scrapedPages.id, existing[0].id)) but omits tenant scoping (tenantId). This updates a tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ updated row id comes from immediately prior SELECT scoped by url and tenantId
- **server/firecrawl.ts:344** _(upstream-verified)_ — UPDATE scrapedPages in crawl loop uses .where(eq(scrapedPages.id, existing[0].id)) with no tenant_id constraint, allowing cross-tenant writes.
  - _Why false positive:_ updated row id comes from immediately prior SELECT scoped by pageUrl and tenantId
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE projects sets driveFolderId/driveFolderUrl using WHERE eq(projects.id, projectId) with no tenant_id constraint.
  - _Why false positive:_ same call path already loaded project and rejected tenant mismatch before UPDATE
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals queries tenant-scoped table code_proposals with no tenant_id filter (SELECT id, title, status, created_at FROM code_proposals WHERE ...). This reads across tenants.
  - _Why false positive:_ global maintenance audit intentionally scans stale proposals across tenants and only emits admin findings/housekeeping
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates rows in code_proposals without constraining by tenant_id, potentially affecting other tenants' proposals.
  - _Why false positive:_ maintenance sweep archives only stale status field for ids selected by the same global audit path
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks queries heartbeat_tasks without a tenant_id filter (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE ...). heartbeat_tasks appear to be tenant-scoped elsewhere and this will scan across tenants.
  - _Why false positive:_ global maintenance audit scans heartbeat housekeeping rows across tenants for stale scheduling
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) updates heartbeat_tasks without scoping by tenant_id, possibly disabling tasks for other tenants.
  - _Why false positive:_ maintenance sweep only disables stale heartbeat tasks as housekeeping, not tenant data mixing
- **server/heartbeat.ts:53** _(system-sweep)_ — SELECT from research_schedules is executed without any tenant_id filter (SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW()). This reads tenant-scoped rows across all tenants without an explicit per-tenant constraint.
  - _Why false positive:_ platform heartbeat intentionally scans due schedules across tenants and then processes each row with its own tenant_id
- **server/heartbeat.ts:102** _(system-sweep)_ — UPDATE research_schedules sets next_run_at WHERE id = ${sched.id} without constraining by tenant_id. Updating a tenant-scoped table by id only can modify rows belonging to other tenants if an id is misused/guessed.
  - _Why false positive:_ heartbeat loop updates housekeeping fields on per-row schedules selected by the global sweep using row-carried tenant context
- **server/heartbeat.ts:108** _(system-sweep)_ — UPDATE research_schedules SET last_run_at/next_run_at WHERE id = ${sched.id} is executed without tenant_id constraint.
  - _Why false positive:_ Heartbeat is a platform-wide scheduler sweep updating only housekeeping fields on due schedule rows; no tenant data is returned or mixed.
- **server/heartbeat.ts:113** _(system-sweep)_ — UPDATE research_schedules SET next_run_at WHERE id = ${sched.id} executed without tenant_id constraint (in the 'else' branch).
  - _Why false positive:_ Global heartbeat maintenance loop advances next_run_at only; per-row housekeeping across tenants is intentional.
- **server/heartbeat.ts:121** _(system-sweep)_ — In the recovery path, UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} lacks a tenant_id filter.
  - _Why false positive:_ Recovery path is still the same platform-wide scheduler housekeeping sweep, mutating only next_run_at.
- **server/heartbeat.ts:269** _(system-sweep)_ — Raw UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} executed without constraining by tenant_id. This modifies heartbeat_tasks rows without verifying tenant ownership.
  - _Why false positive:_ Startup cleanup loop intentionally normalizes heartbeat task config across all tenants/system rows; only housekeeping/config field mutated, no cross-tenant data exposure.
- **server/heartbeat.ts:273** _(system-sweep)_ — Raw UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} executed without tenant_id constraint (normalizing reflection-model).
  - _Why false positive:_ Startup maintenance normalization of heartbeat task model across rows is an intentional cross-tenant housekeeping/config update.
- **server/heartbeat.ts:1586** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 has no tenant_id filter. heartbeat_logs is tenant-related here (later tenantId is used), so this read can return another tenant's log.
  - _Why false positive:_ heartbeat_logs has no tenant_id column per schema, so this lookup is against a non-tenant-scoped log table.
- **server/job-queue.ts:134** _(system-sweep)_ — reclaimExpiredLeases performs UPDATEs on tenant-scoped table `agent_jobs` (marking terminal and re-queue rows) without any tenant_id filters — these bulk operations will affect rows across tenants.
  - _Why false positive:_ Lease reclamation is a platform-wide housekeeping sweep over queued jobs, only mutating status/lease timing fields and returning counts/ids, not tenant-facing mixed content.
- **server/job-queue.ts:148** _(system-sweep)_ — Second UPDATE in reclaimExpiredLeases (requeueing expired leases back to pending) lacks tenant_id restriction and will requeue jobs for all tenants indiscriminately.
  - _Why false positive:_ Requeueing expired leases is intentional global queue maintenance affecting housekeeping state only.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob issues UPDATE agent_jobs SET status='succeeded' ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint — a caller could complete a job belonging to a different tenant by id.
  - _Why false positive:_ completeJob is an internal worker state-transition by server-generated job primary key with lease-fencing attempts guard, not a request-bound tenant input path.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob first SELECTs max_attempts FROM agent_jobs WHERE id = ${id} with no tenant constraint, then later UPDATEs agent_jobs to failed / pending without tenant filters — allowing a caller to examine/mutate other tenants' jobs by id.
  - _Why false positive:_ failJob is an internal worker-only job transition/read by server-generated job primary key; id is not shown as coming from an untrusted tenant request boundary.
- **server/job-queue.ts:237** _(internal-worker-pk)_ — Terminal-path UPDATE in failJob (marking failed_terminal) updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but has no tenant_id check — can dead-letter jobs across tenants.
  - _Why false positive:_ Terminal fail transition is an internal worker mutation by server-generated job id plus attempts fencing, matching internal-worker-pk.
- **server/job-queue.ts:257** _(internal-worker-pk)_ — Retry-path UPDATE in failJob (scheduling next_run_at) updates agent_jobs without any tenant filter, potentially mutating jobs from other tenants.
  - _Why false positive:_ Retry-path state transition updates by server-side job primary key/id with compare-and-set fields; this is internal worker logic, not shown crossing an untrusted request boundary.
- **server/job-queue.ts:385** _(global-table)_ — getJobStats runs SELECT status, COUNT(*) FROM agent_jobs WHERE created_at > ... with no tenant_id constraint — aggregates across all tenants and exposes cross-tenant counts.
  - _Why false positive:_ Comment marks this as operator dashboard header aggregate stats; query returns only global counts, not tenant row data, consistent with intentional platform-wide admin stats.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT from tenant-scoped `projects` table without constraining to a tenant_id. The query `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id` aggregates across all tenants, allowing one tenant's rows to affect short-circuit logic for other tenants (misattribution / skipped backfill).
  - _Why false positive:_ This is a platform-wide backfill/maintenance routine aggregating per-row tenant_id counts for housekeeping short-circuit logic, not returning tenant data to a tenant-facing surface.
- **server/lib/outcome-promotion.ts:158** _(system-sweep)_ — A cross-tenant aggregate SELECT runs without a tenant_id WHERE clause: "SELECT tenant_id, format, count(*)... FROM deliverable_grade_events WHERE created_at > ... GROUP BY tenant_id, format" — this reads a tenant-scoped table across all tenants.
  - _Why false positive:_ Nightly optimizer scans all tenants for promotion stats, then processes each tenant separately by row-carried tenant_id and writes per-tenant outputs.
- **server/lib/stuck-task-sweep.ts:56** _(system-sweep)_ — SELECT from tenant-scoped table `agent_runs` without a tenant_id filter. The query returns tenant_id for each row but does not restrict by tenant, exposing rows across tenants.
  - _Why false positive:_ Read-only housekeeping sweep intentionally audits stuck rows across tenants and carries tenant_id per row; no tenant-mixed prompt/output shown.
- **server/lib/stuck-task-sweep.ts:84** _(system-sweep)_ — SELECT from tenant-scoped table `mind_tickets` performed with no tenant_id constraint, returning rows across tenants (or emitting tenant_id values) without per-tenant filtering.
  - _Why false positive:_ Read-only housekeeping sweep intentionally audits stuck rows across tenants and carries tenant_id per row; no tenant-mixed prompt/output shown.
- **server/martech-bundle.ts:325** _(upstream-verified)_ — UPDATE on tenant-scoped table `tenant_voice_profiles` filters only by id (WHERE id = existing.id) and does NOT constrain by tenant_id. Application-level tenant isolation invariant requires every write to include the caller's tenant filter.
  - _Why false positive:_ existing row is fetched via getVoiceProfile({tenantId, profileName}); update uses that verified row's id, so tenant ownership is enforced earlier in the same call path.
- **server/memory-graph.ts:124** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} updates a tenant-scoped table by id only and omits a tenant_id constraint.
  - _Why false positive:_ catId comes from ensureCategory, which selects/inserts memory_categories scoped by tenant_id=${tenantId}; subsequent id-only count increment targets that verified row.
- **server/memory-graph.ts:239** _(fk-transitive)_ — Existence check SELECT 1 FROM memory_links ... LIMIT 1 does not filter on tenant_id. This reads a tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ memory_links has no tenant_id column and links memory_entries already selected within one tenant; isolation is transitive via tenant-scoped endpoint rows.
- **server/memory-graph.ts:494** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} updates a tenant-scoped table by id only and omits tenant_id in the WHERE clause.
  - _Why false positive:_ mem.id comes from a prior SELECT on memory_entries constrained by tenant_id=${tenantId}; update is on rows already verified in the same call path.
- **server/memory-intelligence.ts:399** _(upstream-verified)_ — UPDATE knowledge_triples SET valid_until = ... WHERE id = ${c.id} updates a tenant-scoped table by id only and omits tenant_id. Although the id originated from a tenant-scoped query, the invariant requires explicit tenant constraints on writes.
  - _Why false positive:_ c.id comes from contradictions query on knowledge_triples with tenant_id=${tenantId}; update applies only to rows verified in the immediately preceding scoped read.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. validateSubscriptionsOnStartup runs SELECT id, provider, tenant_id, ... FROM oauth_subscriptions WHERE is_active = TRUE and processes rows for all tenants with no tenant_id filter.
  - _Why false positive:_ Startup validation intentionally sweeps active subscriptions across tenants and processes each row with its own tenant_id for housekeeping/token validation.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. A later query SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' fetches across all tenants.
  - _Why false positive:_ Boot-time auto-reconnect enumerates tenant_ids platform-wide and then handles each tenant separately; this is a maintenance sweep, not tenant-facing data exposure.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. proactiveTokenRefresh issues SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE and then refreshes tokens across all tenants.
  - _Why false positive:_ proactiveTokenRefresh is a background refresh loop over all active subscriptions, using each row's tenant_id independently for per-tenant token refresh housekeeping.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — SELECT from tenant-scoped table `oauth_subscriptions` without tenant constraint. forceRefreshAllSubscriptions SELECTs provider and tenant_id FROM oauth_subscriptions WHERE is_active = TRUE (global sweep).
  - _Why false positive:_ forceRefreshAllSubscriptions is an explicit cron-style global maintenance sweep that iterates active subscriptions and refreshes each tenant separately.
- **server/pace-control.ts:85** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` without tenant constraint. checkPace queries heartbeat_logs to count runs in the sliding window but does not filter by tenant_id, so counts are global across tenants.
  - _Why false positive:_ Schema says heartbeat_logs has no tenant_id; this pace gate is intentionally global platform throttling, matching a known global heartbeat/throttle table pattern.
- **server/pace-control.ts:140** _(global-table)_ — SELECT from tenant-scoped table `heartbeat_logs` without tenant constraint. getPaceSnapshot queries recent heartbeat_logs to produce dashboard snapshots but lacks tenant_id filtering (returns global counts).
  - _Why false positive:_ heartbeat_logs is a non-tenant-scoped global heartbeat/throttle log table, so this snapshot is over intentional platform-global data.
- **server/process-governor.ts:198** _(global-table)_ — SELECT AVG(duration_ms) from heartbeat_logs is missing tenant scoping — query reads cross-tenant rows without WHERE tenant_id = ${tenantId}.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is used here as a platform-global performance signal; query returns aggregate AVG only.
- **server/process-governor.ts:240** _(global-table)_ — SELECT COUNT(DISTINCT persona_id) from heartbeat_logs is missing tenant scoping — reads failing agent counts across all tenants.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id, and this reads only an aggregate distinct failing-agent count from that global telemetry table.
- **server/process-governor.ts:121** _(global-table)_ — Subselects counting heartbeat_logs inside task_failure_rate omit tenant constraint — these correlated subqueries can read logs from other tenants.
  - _Why false positive:_ The subselects read heartbeat_logs, which has no tenant_id by schema and is an intentional global telemetry/heartbeat table rather than tenant-scoped content.
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` uses only `WHERE id = ${r.id}` and does not include `tenant_id` — this can modify rows belonging to another tenant if an id from a different tenant is supplied or iterated.
  - _Why false positive:_ Maintenance loop iterates rows already carrying tenant_id and updates only housekeeping payload/timestamps for each row; no tenant-facing data mixing.
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses `WHERE id = ${storedId}` only and omits tenant_id/persona_id checks. This can update another tenant's memory entry if ids collide or are misused.
  - _Why false positive:_ storedId is selected immediately above from memory_entries constrained by tenant_id and persona_id in the same call path before the update.
- **server/research-engine.ts:165** _(system-sweep)_ — UPDATE on tenant-scoped table research_sessions is executed without any tenant_id constraint — this will mark running sessions as completed across all tenants.
  - _Why false positive:_ Global stale-session cleanup updates only status/ended_at/summary housekeeping fields and returns ids/counts, not tenant data.
- **server/research-engine.ts:400** _(system-sweep)_ — DELETE FROM agent_knowledge is executed without any tenant_id constraint — this will delete autoresearch knowledge entries for all tenants.
  - _Why false positive:_ Global expiry purge deletes expired autoresearch knowledge rows as housekeeping; no tenant-facing output or cross-tenant content mixing.
- **server/routes.ts:2059** _(signed-url)_ — GET /api/presenter/:token selects presenter_sessions by token with no tenant scoping and returns session data. tenant_id is taken from DB row but not enforced against caller; token is used as a capability but the DB read is unscoped.
  - _Why false positive:_ Presenter token is a bearer capability; access is intentionally authorized by possession of the token rather than caller tenant context.
- **server/routes.ts:2144** _(upstream-verified)_ — INSERT INTO presenter_sessions uses tenantId taken directly from the request body (caller-supplied) without validating the caller is authorized to create sessions for that tenant.
  - _Why false positive:_ Route is gated by x-internal-key checked against SESSION_SECRET before using caller-supplied tenantId; not an untrusted tenant boundary.
- **server/routes.ts:4333** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' does not include tenant_id. The code inserts (project_id, note, author) without setting tenant_id, violating the invariant that tenant-scoped INSERTs must pass tenantId explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is child data scoped transitively through tenant-scoped projects via project_id.
- **server/routes/admin.ts:744** _(system-sweep)_ — SELECT FROM tenant-scoped table `heartbeat_tasks` executed without constraining by tenant_id. The query (`SELECT id, name, enabled, consecutive_failures, last_run_at FROM heartbeat_tasks WHERE enabled = false OR consecutive_failures >= 3 ...`) can return rows for other tenants because there is no `WHERE tenant_id = <caller tenant>` or equivalent filter.
  - _Why false positive:_ Admin diagnostic view surfaces platform-wide task health and is not tenant-facing workload data; cross-tenant operational listing is intentional.
- **server/routes/goal-ledger.ts:92** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_jobs` is executed without any tenant_id predicate (global query).
  - _Why false positive:_ Query feeds /api/admin/goal-ledger route guarded by requirePlatformAdmin in the same call path, making global visibility explicit and authorized.
- **server/routes/projects.ts:232** _(upstream-verified)_ — UPDATE projects SET ... WHERE id = ${projectId} is missing an explicit tenant_id constraint.
  - _Why false positive:_ project ownership is checked earlier in the same handler via SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId
- **server/routes/projects.ts:300** _(upstream-verified)_ — UPDATE projects SET ... WHERE id = ${projectId} is missing an explicit tenant_id constraint (multer upload handler).
  - _Why false positive:_ same multer handler verifies the parent project belongs to tenant before this timestamp update
- **server/routes/projects.ts:319** _(fk-transitive)_ — SELECT * FROM project_files WHERE project_id = ${projectId} lacks an explicit tenant_id constraint.
  - _Why false positive:_ project_files has no tenant_id and is scoped through tenant-checked parent project_id
- **server/routes/projects.ts:342** _(fk-transitive)_ — SELECT file record from `project_files` WHERE project_id = ${projectId} AND file_path = ... has no tenant_id constraint.
  - _Why false positive:_ project_files has no tenant_id and lookup is by tenant-verified project_id, so isolation is transitive through projects
- **server/routes/projects.ts:371** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} lacks an explicit tenant_id constraint.
  - _Why false positive:_ project_files has no tenant_id and delete is constrained by tenant-owned project_id
- **server/routes/projects.ts:34** _(fk-transitive)_ — Subqueries counting project_files/project_notes/project_conversations in the projects listing do not include tenant_id filters (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id).
  - _Why false positive:_ project_files/project_notes/project_conversations are child tables without tenant_id counted via tenant-scoped parent projects row p
- **server/seed-cleanup.ts:15** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true runs without tenant_id constraint, disabling delegation tasks across all tenants.
  - _Why false positive:_ Boot-time cleanup intentionally disables runaway heartbeat tasks across all tenants; housekeeping/status-only mutation with no tenant data returned.
- **server/seed-cleanup.ts:52** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant_id restriction and will disable matching tasks across tenants.
  - _Why false positive:_ Global seed cleanup disables duplicate session-logger heartbeat tasks platform-wide; status-only maintenance across tenants.
- **server/seed-cleanup.ts:75** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (...) AND enabled = true has no tenant_id constraint and will disable matching tasks for all tenants.
  - _Why false positive:_ Platform-wide housekeeping removes orphaned repo-examination heartbeat tasks by disabling them; no tenant content mixed or exposed.
- **server/seed.ts:36** _(system-sweep)_ — SELECT against tenant-scoped table research_sessions without any tenant_id constraint (SELECT COUNT(*) FROM research_sessions). This reads across all tenants.
  - _Why false positive:_ Boot-time import checks whether research_sessions is globally empty before one-time snapshot restore; aggregate count only, no tenant data exposure.
- **server/seed.ts:2158** _(system-sweep)_ — SELECT from research_programs without tenant constraint (SELECT id, name, model FROM research_programs WHERE is_active = true). This returns programs from all tenants.
  - _Why false positive:_ Seed maintenance scans all active research programs to repair invalid model ids platform-wide; read is internal maintenance and not tenant-facing.
- **server/seed.ts:2163** _(system-sweep)_ — UPDATE on research_programs (UPDATE research_programs SET model = ... WHERE id = ${p.id}) lacks a tenant_id constraint; this may modify rows belonging to other tenants if ids collide.
  - _Why false positive:_ Follow-on update repairs each selected research_program row individually during platform-wide maintenance; no cross-tenant mixing or tenant-facing output.
- **server/seed.ts:2515** _(system-sweep)_ — SELECT from conversations without tenant constraint (SELECT DISTINCT c.id, c.project_id FROM conversations c ...). Conversations are tenant-scoped; this reads across tenants.
  - _Why false positive:_ Boot-time backfill finds missing project_conversations links across tenants and inserts per-row links only; maintenance does not expose tenant data.
- **server/seed.ts:3812** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This will modify rows across all tenants.
  - _Why false positive:_ Global text-normalization maintenance updates memory_entries across tenants in place; no row data returned or mixed into any tenant-facing output.
- **server/seed.ts:3838** _(system-sweep)_ — UPDATE on tenant-scoped table `conversations` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This will update conversations for all tenants.
  - _Why false positive:_ Platform-wide legacy model-id backfill updates conversations in place across tenants; housekeeping-only mutation, not tenant-facing.
- **server/seed.ts:3784** _(system-sweep)_ — DELETE FROM `heartbeat_tasks` is executed without a tenant_id constraint. `heartbeat_tasks` is tenant-scoped, so this DELETE can remove rows across all tenants.
  - _Why false positive:_ Seed housekeeping purges stale auto-disabled heartbeat task rows across tenants; maintenance-only delete with no tenant data exposure.
- **server/seed.ts:4051** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` uses only id in the WHERE clause (WHERE id = $id) and does not include tenant_id. Even if the id was obtained earlier with a tenant filter, the UPDATE itself lacks an explicit tenant constraint.
  - _Why false positive:_ The id being updated was selected immediately above with tenant_id = 1 and specific category/title filter in the same call path.
- **server/self-improvement.ts:401** _(internal-worker-pk)_ — UPDATE on tenant-scoped table experiments uses WHERE eq(experiments.id, experiment.id) with no tenant_id constraint. Updating by id alone can modify rows belonging to other tenants.
  - _Why false positive:_ update is by server-generated experiment.id within internal self-improvement flow, not from an untrusted request boundary
- **server/self-improvement.ts:415** _(internal-worker-pk)_ — In the error path the code updates experiments (SET status='error', outcome=...) using WHERE eq(experiments.id, experiment.id) without a tenant_id condition, risking cross-tenant modifications.
  - _Why false positive:_ error-path update targets the same internal experiment row by server-held primary key, not caller-controlled input
- **server/self-reflection.ts:366** _(upstream-verified)_ — storeLesson updates memory_entries using WHERE id = <existing id> without also constraining tenant_id. Although the row was located earlier with a tenant filter, the UPDATE lacks tenant-scoping and could affect another tenant's record with the same id.
  - _Why false positive:_ the id being updated was selected immediately above with WHERE tenant_id = tenantId, so ownership is established in the same call path
- **server/storage.ts:298** _(upstream-verified)_ — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) AND citations IS NOT NULL with NO tenant_id constraint on messages (tenant-scoped table read).
  - _Why false positive:_ ids come from msgs already loaded by tenant-scoped getMessages/getMessagesPaginated queries in the same flow, so raw citation fetch is limited to those validated rows
- **server/storage.ts:827** _(system-sweep)_ — getMemoriesWithoutEmbeddings selects from memory_entries WHERE status = 'active' AND embedding IS NULL with NO tenant_id constraint — unscoped read across tenants.
  - _Why false positive:_ background embedding backfill intentionally scans rows across tenants for housekeeping and returns internal worker input, not tenant-facing mixed output
- **server/storage.ts:833** _(system-sweep)_ — getKnowledgeWithoutEmbeddings selects from agent_knowledge with no tenant filter (notExpired && isNull(embedding)) — unscoped read across tenants.
  - _Why false positive:_ background embedding backfill intentionally scans tenant rows platform-wide for housekeeping only, not tenant-facing data return
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE of tenant-scoped table `custom_tools` uses only id in WHERE (db.update(...).where(eq(customTools.id, tool.id))) with no tenant_id constraint — allows modifying a row if id is known across tenants.
  - _Why false positive:_ tool row is obtained earlier via tenant-scoped lookup and this update uses that validated row in the same execution path
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE of tenant-scoped table `custom_tools` to increment usageCount uses only id in WHERE (db.update(...).where(eq(customTools.id, tool.id))) with no tenant_id constraint.
  - _Why false positive:_ tool is selected immediately above with name and tenantId filter, then usageCount update applies to that verified row in the same call path
- **server/tools.ts:2915** _(fk-transitive)_ — Subquery counts in the project list omit tenant scoping. Example: "(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)" (and similar COUNTs for project_notes and project_conversations) do not filter by tenant_id.
  - _Why false positive:_ Counts are on child tables with no tenant_id and parent projects row is already filtered by p.tenant_id.
- **server/tools.ts:3031** _(fk-transitive)_ — Subquery counts in the project search omit tenant scoping. The SELECT uses subqueries like "(SELECT COUNT(*) FROM project_files WHERE project_id = p.id)" and "(SELECT COUNT(*) FROM project_notes WHERE project_id = p.id)" without tenant filters.
  - _Why false positive:_ Subquery counts target project_files/project_notes, which are tenant-scoped transitively via tenant-filtered parent project p.
- **server/tools.ts:2976** _(fk-transitive)_ — Duplicate-check SELECT against project_files in add_file lacks tenant constraint. The dupCheck queries: "SELECT id FROM project_files WHERE project_id = ${params.id} AND file_name = ${fname} ..." do not filter by tenant_id.
  - _Why false positive:_ project_files has no tenant_id; duplicate check is scoped by owned project_id after project tenant ownership check.
- **server/tools.ts:3016** _(fk-transitive)_ — Existence-check SELECT against project_conversations in link_conversation omits tenant scoping. The statement "SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId}" does not include tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id; both project and conversation ownership are verified in-tenant before existence check/insert.
- **server/tools.ts:4529** _(upstream-verified)_ — SELECT on tenant-scoped tables (conversations / project_conversations) without tenant_id constraint. The COALESCE subquery queries conversations and project_conversations by conversation_id/project_id but does not include WHERE tenant_id = <caller tenant>.
  - _Why false positive:_ Conversation tenant ownership is asserted before query, and resulting projectId is re-validated with assertProjectInTenant before use.
- **server/tools.ts:4540** _(fk-transitive)_ — INSERT INTO project_notes does not set tenant_id. Project-scoped persistent writes omit tenant_id in VALUES (tenant_id column not provided).
  - _Why false positive:_ project_notes has no tenant_id and is inserted only after assertProjectInTenant confirms owned parent project.
- **server/tools.ts:4544** _(fk-transitive)_ — INSERT INTO project_files (presentation path) omits tenant_id in VALUES. Tenant identifier is not persisted on the project_files row.
  - _Why false positive:_ project_files has no tenant_id and insert is correctly scoped through validated parent project.
- **server/tools.ts:6390** _(fk-transitive)_ — INSERT INTO project_files in generate_audio path omits tenant_id. The code relies on assertProjectInTenant but does not include tenant_id in the inserted row.
  - _Why false positive:_ project_files has no tenant_id; insert occurs only after assertProjectInTenant on supplied project_id.
- **server/tools.ts:6922** _(fk-transitive)_ — INSERT INTO project_files in create_slideshow_video omits tenant_id. The write to project_files does not set tenant_id even though project is asserted.
  - _Why false positive:_ project_files is a child table without tenant_id and write is guarded by assertProjectInTenant.
- **server/tools.ts:6926** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' omits tenant_id. The INSERT uses project_id (already asserted owned) but does not set tenant_id column.
  - _Why false positive:_ project_files has no tenant_id by schema, so omitting tenant_id on insert is by-design under owned project.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed without tenant scoping: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}` lacks a tenant_id constraint, allowing cross-tenant modification if an attacker knows another workflow.id.
  - _Why false positive:_ workflow is selected immediately above with WHERE id/name and tenant_id = tenantId, then updated by that verified row's id.
- **server/tools/domains/files/handlers.ts:835** _(fk-transitive)_ — INSERT into project_files omits tenant scoping column/value: `INSERT INTO project_files (project_id, file_name, ...) VALUES (${projId}, ${fileName}, ...)` does not set tenant_id explicitly. Tenant-scoped INSERTs must include the tenant identifier.
  - _Why false positive:_ project_files has no tenant_id and is a child of tenant-scoped projects; projId is derived from a join enforcing projects.tenant_id = tenantId.
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE projects executed without tenant scoping: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}` lacks tenant_id in the WHERE clause, allowing modification of a project row by id alone without verifying tenant ownership at the SQL level.
  - _Why false positive:_ projId comes from the prior SELECT joining projects with p.tenant_id = tenantId, so the project ownership was verified earlier in the same path.
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys is executed without a tenant_id constraint (SELECT provider, LENGTH(api_key) ... FROM provider_keys ...). This reads a tenant-scoped table across tenants (even though the branch is admin-only) and can expose provider key material or counts across tenant boundaries.
  - _Why false positive:_ schema note shows provider_keys has no tenant_id, so this is not a tenant-scoped table and the admin-only aggregate read is platform-global.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM provider_keys is executed without a tenant_id constraint. This reads a tenant-scoped table across all tenants (admin branch) and may leak cross-tenant provider configuration.
  - _Why false positive:_ provider_keys has no tenant_id per schema, so counting configured providers is a read from a global table, not a cross-tenant isolation flaw.
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` updates row by id only (WHERE id = ${id}) with no tenant_id constraint. This allows an update to a memory entry owned by another tenant if an attacker can guess an id.
  - _Why false positive:_ id is obtained from an immediately preceding SELECT on memory_entries constrained by tenant_id = tid and persona_id, then updated by that verified id.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ANY(${ids}) with no tenant_id constraint. This will bump last_accessed / access_count for rows across tenants if ids contain cross-tenant ids.
  - _Why false positive:_ ids array is built solely from rows fetched just above with tenant_id = tid and persona_id constraints, so the UPDATE only targets previously tenant-scoped rows.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE on tenant-scoped table `memory_entries` updates row by id only (WHERE id = ${id}) with no tenant_id constraint (mirror of earlier dedupe/update). This can update another tenant's memory entry.
  - _Why false positive:_ id comes from the prior tenant-scoped SELECT on memory_entries with tenant_id = tid and persona_id, so ownership is enforced earlier in the same path.
- **server/video-job-runner.ts:236** _(system-sweep)_ — Global UPDATE in recoverStaleVideoJobs modifies video_jobs rows across all tenants (WHERE status IN (...) AND updatedAt < staleCutoff) without tenant_id constraint. This will mark stale jobs for every tenant in a single call and violates per-tenant isolation invariants.
  - _Why false positive:_ this is a platform-wide stale-job recovery sweeper that only mutates housekeeping/status fields across rows and does not return mixed tenant data.
- **server/webhook-triggers.ts:60** _(global-table)_ — SELECT joins the tenant-scoped `personas` table without constraining by tenant. The query `SELECT w.*, p.name as persona_name FROM webhook_triggers w LEFT JOIN personas p ON p.id = w.persona_id ...` can read persona rows across tenants because there is no WHERE p.tenant_id = <expected tenant>.
  - _Why false positive:_ webhook_triggers is created here without tenant_id and personas schema note says tenant_id NO; this is not a tenant-scoped join
- **server/webhook-triggers.ts:89** _(global-table)_ — INSERT into `webhook_triggers` stores a caller-supplied personaId directly (`VALUES (..., ${config.personaId || null})`) with no verification that the referenced persona belongs to the expected tenant. This lets an attacker or misconfigured caller reference a persona from another tenant.
  - _Why false positive:_ webhook_triggers is a non-tenant-scoped table here and personas has no tenant_id per schema note, so persisting persona_id is not a tenant_id bypass
- **server/webhook-triggers.ts:122** _(global-table)_ — Query that fetches a trigger (`SELECT w.*, p.name as persona_name ... WHERE w.webhook_key = ${webhookKey} ...`) LEFT JOINs `personas` without constraining persona rows to a tenant. This performs an unconstrained read against the tenant-scoped `personas` table when resolving persona_name for a trigger.
  - _Why false positive:_ trigger lookup is against global webhook_triggers by webhook_key and joined personas also has no tenant_id per schema note
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Querying tenant-scoped table `tool_performance` without tenant_id constraint (SELECT count(*) FROM tool_performance ...). This reads across all tenants.
  - _Why false positive:_ global maintenance/invariant check intentionally aggregates tool_performance for health metrics only, not tenant-facing row data
- **server/wiring-invariants.ts:258** _(system-sweep)_ — Querying tenant-scoped table `tool_performance` without tenant_id constraint (SELECT count(*) FROM tool_performance). This reads across all tenants.
  - _Why false positive:_ boot/maintenance invariant probe counting all tool_performance rows is cross-tenant housekeeping/observability only
- **server/wiring-invariants.ts:260** _(system-sweep)_ — Querying tenant-scoped `messages` table without tenant_id constraint (SELECT count(*) FROM messages ...). This aggregates chat volume across tenants.
  - _Why false positive:_ global dormant-tools check uses aggregate message count for health diagnostics only and does not expose tenant content
- **server/wiring-invariants.ts:293** _(system-sweep)_ — Querying tenant-scoped table `tool_performance` without tenant_id constraint (SELECT DISTINCT tool_name FROM tool_performance ...). This can leak/aggregate data across tenants.
  - _Why false positive:_ distinct tool names across tool_performance are used for platform-wide dormant-tool detection, not tenant-facing mixed data output
- **server/wiring-invariants.ts:366** _(system-sweep)_ — Using sql.raw to SELECT COUNT/MAX from subsystem tables (spec.table) such as `knowledge_nudges` and `tool_performance` without any tenant_id constraint. These subsystem tables are tenant-scoped but are queried across all tenants here.
  - _Why false positive:_ subsystem freshness loop is a platform-wide housekeeping probe using COUNT/MAX only, with no tenant content returned
- **server/wiring-invariants.ts:417** _(system-sweep)_ — Querying tenant-scoped table `tool_performance` without tenant_id constraint in tool-drift detector (SELECT ... FROM tool_performance WHERE last_failure_at > ...). This groups/aggregates across tenants.
  - _Why false positive:_ tool drift detector is a global observability/invariant check over aggregated counters, not a tenant data exposure path
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Using sql.raw to SELECT COUNT/pg_total_relation_size FROM MEMORY_TABLES (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without tenant_id filtering. These are likely tenant-scoped stores and are being aggregated across tenants.
  - _Why false positive:_ memory growth tripwire intentionally does platform-wide COUNT/size housekeeping and emits only aggregate thresholds
- **server/wiring-invariants.ts:561** _(system-sweep)_ — Querying tenant-scoped `graph_memory` without tenant_id constraint (SELECT COUNT(*) FROM graph_memory). This reads across all tenants.
  - _Why false positive:_ graph_memory emptiness check is a global bootstrap/housekeeping probe reporting only aggregate count
- **server/chat-engine.ts:4038** _(internal-worker-pk)_ — recordToolExecution(conversationId, toolName, ...) is invoked with a foreign conversation_id but no tenant identifier or prior ownership check. If recordToolExecution writes to tenant-scoped tables, this can allow cross-tenant writes/reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:4004 — recordToolExecution (server/felix-brain.ts:298) is a purely in-memory operation on the taskStateStore Map keyed by conversationId; it performs NO database write to any tenant-scoped table. There is no cross-tenant persistence risk.
- **server/chat-engine.ts:4042** _(global-table)_ — Second call to recordToolExecution(conversationId, toolName, true, ids) uses conversationId without tenant context or ownership validation. This risks writing tool-execution telemetry against another tenant's conversation.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Same helper recordToolExecution (felix-brain.ts:298-320) — in-memory Map only, no DB/tenant table.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images does not include tenant_id (INSERT INTO presenter_slide_images (session_id, slide_index, image_data, image_size, quality) ...). This writes to a likely tenant-scoped table using a session_id-derived value without scoping by tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): presenter_slide_images has no tenant_id column (shared/schema-orphans.ts:344-352); isolated via session_id FK to presenter_sessions (tenant-scoped, shared/schema.ts:2082-2084). sessionId=presData.id is returned from the /api/presenter POST that just created the session with tenantId (server/google-workspace.ts:2325-2341).
- **server/google-workspace.ts:2378** _(internal-worker-pk)_ — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. Updating presenter_sessions by id only can modify another tenant's session if sessionId is untrusted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/google-workspace.ts:2378 UPDATE presenter_sessions by sessionId=presData.id, the PK returned from the session just created in this same call path via the /api/presenter POST carrying tenantId (server/google-workspace.ts:2325-2341). Not an untrusted foreign id.
- **server/internal-resolver.ts:91** _(system-sweep)_ — finalizeEvent performs an UPDATE on tenant-scoped table `event_log` without requiring/enforcing tenant_id when tenantId is omitted — the SQL fragment omits any tenant filter if tenantId is null, allowing unscoped terminal-state mutations.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): finalizeEvent only mutates event_log housekeeping status/processing_result by the event's own id (server/internal-resolver.ts:91-100); eventId originates from internally-emitted bus events dispatched via resolveDroppedEvent (server/attention-handlers/owner-notify.ts:106), not an untrusted boundary.
- **server/storage.ts:398** _(internal-worker-pk)_ — attachCitations updates messages via raw SQL: UPDATE messages SET citations = ... WHERE id = ${messageId} with NO tenant_id constraint — unscoped write to a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): attachCitations only caller is routes.ts:5450 using __asstMsg.id created at routes.ts:5447 via storage.createMessage({conversationId, role, content, tenantId}) — a server-generated PK for the caller's own tenant; messageId never comes from an untrusted boundary.
- **server/storage.ts:482** _(global-table)_ — deletePersona performs tx.update(conversations).set({ personaId: null }).where(eq(conversations.personaId, id)) without tenant scoping — this will clear persona references across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/storage.ts:479 — The personas table has no tenant_id column — personas are global/platform-wide by design (mapped per-tenant only via tenant_persona_names). deletePersona is invoked solely from DELETE /api/personas/:id, which is gated by requirePlatformAdmin (routes/personas.ts:134-136). The cascade cleanup of conversations/memory_entries/daily_notes by persona_id is an intended platfo
- **server/storage.ts:806** _(internal-worker-pk)_ — updateMemoryEmbedding executes db.update(memory_entries).set({ embedding }).where(eq(memory_entries.id, id)) with NO tenant_id constraint — unscoped write to a tenant-scoped table by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:818** _(internal-worker-pk)_ — updateKnowledgeEmbedding executes db.update(agent_knowledge).set({ embedding }).where(eq(agent_knowledge.id, id)) with NO tenant_id constraint — unscoped write to a tenant-scoped table by id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateKnowledgeEmbedding callers are heartbeat.ts:1926, 2652, 2682 using k.id from getKnowledgeWithoutEmbeddings (server rows); only the embedding column is mutated.
- **server/storage.ts:1311** _(upstream-verified)_ — getAllDataForExport performs db.select() FROM many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, delivery_logs, etc.) with NO tenant_id constraints — exports data across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger (settleVentureReservation) lacks tenant_id constraint — UPDATE ... WHERE id = ${reservationId} can modify rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:171** _(internal-worker-pk)_ — UPDATE on tenant-scoped table agent_cost_ledger (releaseVentureReservation) lacks tenant_id constraint — UPDATE ... WHERE id = ${reservationId} can modify rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agentic-engines.ts:451** _(global-table)_ — recentLogs query in runPredictiveEngine reads `heartbeat_logs` without a tenant filter (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ...). This returns other tenants' logs.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/agentic-engines.ts:451 — The query at server/agentic-engines.ts:451-454 reads heartbeat_logs for recent successful automated activities and feeds a platform-level predictive prompt at :456-463; it is not tenant-facing row retrieval. shared/schema.ts:557-575 shows heartbeat_logs has no tenant_id, and the surrounding engine is a platform-wide aggregate rather than a tenant response.
- **server/agentic-engines.ts:519** _(global-table)_ — heartbeatPerformance query in runOptimizationEngine aggregates from `heartbeat_logs` with no tenant filtering (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This aggregates across tenants.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): heartbeat_logs global telemetry aggregate (task_name/status rollup only).
- **server/agentic-engines.ts:533** _(global-table)_ — taskConfig query selects from `heartbeat_tasks` without tenant scoping (SELECT ... FROM heartbeat_tasks WHERE enabled = true ORDER BY name). If heartbeat_tasks is tenant-scoped, this reads other tenants' tasks.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): UNCERTAIN resolved by main-agent review: heartbeat_tasks has NO tenant_id column — platform-global cron config table. server/agentic-engines.ts:533-537 selects heartbeat_tasks (tenant-scoped table, shared/schema.ts:668-686) WHERE enabled=true with NO tenant filter, while sibling queries in the same function are tenant-scoped (research_schedules WHERE tenant_id, agentic-engines.ts:527-529; mess
- **server/auto-memorize.ts:47** _(system-sweep)_ — SELECT from agent_knowledge (getWatermark) has no tenant_id constraint — background watermark read is not scoped to a tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-memorize.ts:45-58 getWatermark reads the single global sweep watermark (housekeeping marker, one row); not tenant-facing data
- **server/auto-project.ts:112** _(upstream-verified)_ — Query that fetches recent messages for a conversation (SELECT role, LEFT(content, 300) FROM messages WHERE conversation_id = ${conversationId}) does not include tenant_id. Even though conversation row was validated earlier, the messages query itself is unscoped.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/auto-project.ts:95-100 verifies the conversation belongs to the tenant (SELECT ... FROM conversations WHERE id=conversationId AND tenant_id=tenantId; returns null if not found) before the messages read at 112-116 scoped by that same conversation_id.
- **server/channels/kernel.ts:180** _(upstream-verified)_ — dispatchInbound calls processMessage(conversationId, ...) using a conversationId obtained from the adapter callback (opts.resolveConversation) without verifying that the conversation belongs to turn.tenantId. This lets a malicious or buggy adapter supply a foreign conversation_id and have processMessage perform reads/writes on a conversation owned by another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): kernel.ts:177 resolveConversation; sole caller whatsapp.ts:442 binds the callback to a tenant WA session and turn.tenantId=s.tenantId (whatsapp.ts:429-432); getOrCreateConversation only returns a conv verified for s.tenantId (whatsapp.ts:490) or freshly created with tenantId=s.tenantId (whatsapp.ts:520-525) — no attacker-supplied foreign id
- **server/chat-engine.ts:632** _(upstream-verified)_ — SELECT from tenant-scoped table 'compaction_archives' lacks tenant scoping: `SELECT summary, content FROM compaction_archives WHERE conversation_id = ${c.id} ORDER BY archived_at DESC LIMIT 1`. No tenant_id filter present.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:632 — The compaction_archives SELECT is scoped by conversation_id = c.id, where c comes from `convs` fetched at line 593-598 with an explicit `c.tenant_id = ${tenantId}` filter (and the parent project is validated `WHERE id = projectId AND tenant_id = tenantId` at line 543). So c.id is a PK of an already-tenant-validated conversation row — no cross-tenant read possible.
- **server/chat-engine.ts:650** _(upstream-verified)_ — SELECT from tenant-scoped table 'messages' is performed without a tenant_id constraint: `SELECT role, content, created_at FROM messages WHERE conversation_id = ${c.id} ORDER BY created_at ASC`. This can allow cross-tenant data exposure if conversation_id is incorrect or attacker-controlled.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/chat-engine.ts:650 — Same as line 632: the messages SELECT uses conversation_id = c.id where c originates from the tenant-filtered convs query (line 596: `c.tenant_id = ${tenantId}`) under a project already validated to belong to tenantId. c.id is a tenant-validated PK, so message reads cannot cross tenants.
- **server/delivery-pipeline.ts:1071** _(global-table)_ — getDeliveryByStripePayment selects from delivery_logs by stripePaymentId with NO tenant filter. This returns tenant-scoped rows across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/delivery-pipeline.ts:1018 — getDeliveryByStripePayment is only invoked from Stripe-signature-verified webhook handlers (server/webhookHandlers.ts:543,1218) with the authentic Stripe payment id. No LLM/request-influenced path reaches it, and stripe_payment_id resolves to exactly one row (platform-wide unique index). It is used as an idempotency/dup-guard for genuine Stripe events, not a
- **server/dfy-intake.ts:93** _(signed-url)_ — submitDfyIntake performs an UPDATE on dfy_intake_forms with .where(eq(dfyIntakeForms.id, form.id)) but without tenant_id in the WHERE clause. This can modify another tenant's form if ids collide or are supplied maliciously.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): form row fetched by unguessable 48-hex token (the documented authorization for the public intake form); UPDATE reuses that row's own id.
- **server/doc-collections.ts:165** _(upstream-verified)_ — addDocument issues DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with NO tenant_id constraint. doc_chunks is tenant-scoped and must be narrowed to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/google-drive.ts:547** _(upstream-verified)_ — SELECT from tenant-scoped table 'projects' by project id (.where(eq(projects.id, projectId))) without constraining the query by tenant_id. The code checks project.tenantId after the read, but the SELECT itself reads another tenant's row without an application-level WHERE tenant_id filter.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): ensureProjectFolder selects only tenantId/driveFolderId and immediately throws on project.tenantId!==tenantId at google-drive.ts:551-553 before returning or writing anything.
- **server/job-queue.ts:274** _(internal-worker-pk)_ — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint — a cancel by id may affect a job from another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:283** _(internal-worker-pk)_ — retryJob resets a job to pending via UPDATE agent_jobs ... WHERE id = ${id} AND status IN (...) without tenant scoping, permitting cross-tenant retries by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/job-queue.ts:320** _(system-sweep)_ — listJobs builds a SELECT from agent_jobs and will execute it with NO tenant_id WHERE clause when the caller omits a tenantId filter — returning or operating across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/lib/bwb-job-progress.ts:74** _(internal-worker-pk)_ — scope() builds the WHERE clause for videoJobs updates/selects but omits a tenant predicate when envTenantId() returns null (BWB_TENANT_ID missing). That allows UPDATE/SELECTs against the tenant-scoped video_jobs table to execute without a tenant_id constraint (writes pinned only by jobId), violating tenant isolation invariants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): scope() always pins the unguessable server-generated jobId (vj_<base36>_<6-byte-hex>, newBwbJobId at bwb-job-progress.ts:44-46, validated by JOB_ID_RE at :28,50); jobId comes only from BWB_JOB_ID env/explicit args threaded through the spawn chain, never a request. Tenant predicate is defense-in-depth, and a random-jobId collision is not reachable from an untrusted boundary.
- **server/lib/bwb-job-progress.ts:141** _(internal-worker-pk)_ — setBwbPhase performs db.update(videoJobs).where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit tenant_id, causing an UPDATE against the tenant-scoped video_jobs table without a tenant constraint.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:166** _(internal-worker-pk)_ — updateBwbChapters performs db.update(videoJobs).where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit tenant_id, allowing an UPDATE on the tenant-scoped video_jobs table without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:227** _(internal-worker-pk)_ — bumpBwbHeartbeat executes db.update(videoJobs).where(scope(jobId, { notDone: true, notFailed: true })) — scope() may omit tenant_id, allowing an UPDATE against video_jobs without tenant scoping.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/lib/bwb-job-progress.ts:141 — setBwbPhase updates video_jobs (tenant_id is defined at shared/schema.ts:2692-2695), but jobId is obtained from BWB_JOB_ID and validated against the generated vj_ format at server/lib/bwb-job-progress.ts:31-45. This is an internal progress worker using a server-generated globally unique job PK, not a request-supplied identifier; missing BWB_TENANT_ID merely
- **server/lib/bwb-job-progress.ts:245** _(internal-worker-pk)_ — failBwbJob executes db.update(videoJobs).where(scope(jobId, { notDone: true })) — scope() may omit tenant_id, permitting an UPDATE that can mark another tenant's job as failed because tenant scoping is not enforced.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): failBwbJob pinned to the same unguessable random jobId; documented warn-once fallback.
- **server/minerva-planner.ts:386** _(upstream-verified)_ — SELECT probe (SELECT status, ceo_decision FROM plans ...) conditionally omits tenant filtering when args.tenantId is undefined, allowing cross-tenant reads (IDOR).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Child-plan lookup runs inside decidePlan's revise-idempotency branch, gated by the same route ownership check at agentic-policy.ts:113; args.planId is the verified plan id.
- **server/process-governor.ts:977** _(upstream-verified)_ — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} has no tenant filter — increments rule counters without scoping to tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project row without any tenant check — a caller-supplied projectId can access projects across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selecting projects joined to conversations (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...) lacks tenant filtering — it enumerates projects across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} executes without tenant scoping — conversations may be tenant-scoped and are being queried by project id only.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping — can expose messages from other tenants' conversations if IDs collide or are not validated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/sculptor.ts:106** _(internal-worker-pk)_ — UPDATE sculptor_sessions executed without constraining to tenant_id (missing AND tenant_id = ${...}). This allows modifying a session row by id across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:74-82 session.id is server-generated PK from the tenant-scoped insert in this same call
- **server/sculptor.ts:112** _(internal-worker-pk)_ — UPDATE sculptor_sessions setting conversation_id WHERE id = ${session.id} has no tenant_id constraint. This can update another tenant's row if ids collide or are guessable.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:74-82 session.id is server-generated PK from the tenant-scoped insert in this same call
- **server/sculptor.ts:169** _(internal-worker-pk)_ — monitorSession updates sculptor_sessions (status/result/tool_calls_count/tokens_used/completed_at) using WHERE id = ${sessionId} with NO tenant_id constraint.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): sessionId was created by this module earlier in the same monitor loop; not caller-supplied.
- **server/sculptor.ts:360** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} lacks tenant scoping — write performed without verifying the session belongs to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/second-opinion.ts:405** _(internal-worker-pk)_ — settleFusionReservation issues UPDATE agent_cost_ledger ... WHERE id = ${reservationId} but does not constrain by tenant_id. agent_cost_ledger is tenant-scoped, so this can modify another tenant's ledger row if ids are misused.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/second-opinion.ts:425** _(internal-worker-pk)_ — releaseFusionReservation issues UPDATE agent_cost_ledger ... WHERE id = ${reservationId} with NO tenant_id constraint, allowing cross-tenant modification of ledger rows.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): second-opinion.ts:405 UPDATE agent_cost_ledger WHERE id=reservationId; settleFusionReservation only caller is second-opinion.ts:573 passing budget.reservationId (server-generated serial PK from the reservation row created earlier in the same flow); never request-derived
- **server/seed.ts:2450** _(system-sweep)_ — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... — update against projects has no tenant filter. This can change another tenant's project row.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/seed.ts:3131-3138 inside seedDatabase(); guarded by isOwnerDeployment() (3133) and uses fully hardcoded literals (id=15, primary_conversation_id=272) — a one-time owner-deployment migration, not reachable from any request input.
- **server/self-improvement.ts:170** _(upstream-verified)_ — detectStagnation performs a SELECT on the tenant-scoped experiments table without guaranteeing a tenant_id filter when tenantId is omitted. The .where(...) uses only category if tenantId isn't provided, allowing cross-tenant reads.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): All callers supply tenantId: server/tools/domains/self-improvement/handlers.ts:78-81 checks ctx.tenantId non-null then calls detectStagnation(category, tenantId); server/self-improvement.ts:228 requires tenantId then 232 passes it. When present the query filters eq(experiments.tenantId, tenantId) (168-171). Returns aggregate stagnation stats, not row data.
- **server/storage.ts:242** _(upstream-verified)_ — getConversationUnscoped performs SELECT FROM conversations WHERE id = ${id} with NO tenant_id constraint (unscoped read of a tenant-scoped table).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getConversationUnscoped is only called on the isAdminRequest branch and every caller validates conv.tenantId!==tenantId immediately after: conversations.ts:143,174,204 and routes.ts:3392.
- **server/storage.ts:942** _(upstream-verified)_ — getHeartbeatTask selects from heartbeat_tasks WHERE id = ${id} with NO tenant_id constraint — unscoped read of a tenant-scoped table.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getHeartbeatTask(id) callers routes/heartbeat.ts:103 and :121 immediately verify Number(task.tenantId)!==tenantId → 404 (heartbeat.ts:105 and :123) before any use, and are behind requireAdmin.
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE from tenant-scoped table `custom_tools` uses only id in WHERE (db.delete(...).where(eq(customTools.id, tool.id))) with no tenant_id constraint — allows deleting a row by id across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/tools.ts:4565** _(internal-worker-pk)_ — SELECT from presenter_sessions by presentation_id with no tenant_id filter. This reads a likely tenant-scoped table without constraining to the caller's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:4537 — The presenter_sessions SELECT keys on slideResult.presentationId, which was just created by this same create_slides tool invocation for the caller's own tenant (conversation ownership is asserted upstream at ~4500 via 'conversation not owned by tenant' and assertProjectInTenant). The presentation id is not attacker-supplied cross-tenant input; it is a self-generated art
- **server/tools.ts:8891** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE clause and lacks a tenant_id constraint. This can allow cross-tenant updates by id alone.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8916** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE clause and lacks a tenant_id constraint. This can allow cross-tenant updates by id alone.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/tools.ts:8933** _(internal-worker-pk)_ — UPDATE against tenant-scoped table 'proposed_skills' uses only proposed_skills.id in WHERE clause and lacks a tenant_id constraint. This can allow cross-tenant updates by id alone.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 5) 2026-08-01): row.id is the PK returned by this same tool call's own INSERT into proposed_skills a few lines above; it is never caller-supplied, so the WHERE(id) UPDATE targets the row just created.
- **server/video-job-runner.ts:178** _(internal-worker-pk)_ — Upsert/ON CONFLICT update against tenant-scoped table video_jobs does not constrain by tenant_id — onConflictDoUpdate updates by jobId and a timestamp check only, with no tenant_id condition. This can allow cross-tenant writes if jobId collisions or other issues occur.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): upsert conflict target is the unguessable runner-generated random jobId (vj_<base36>_<hex>); rows are created and mirrored only by the runner itself.
- **server/compaction.ts:401** _(deferred)_ — INSERT into `compaction_archives` does not include tenant_id — inserting a tenant-scoped archive without tenant ownership. If compaction_archives is a tenant-scoped table, this creates orphaned/unspecified-tenant rows.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 3) 2026-08-01): compaction_archives INSERT omits tenant_id (schema declares NOT NULL, DB column has no default) — thread the conversation's tenantId into the archive INSERT; same legacy-tenant-column family as the DEFAULT-1 tables follow-up.
- **server/delivery-pipeline.ts:203** _(deferred)_ — updateDeliveryLog performs an UPDATE on delivery_logs without constraining by tenant_id (where clause is eq(deliveryLogs.id, id) only). This allows cross-tenant modifications if called with an id from a different tenant.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 2026-08-01): updateDeliveryLog by id only; all callers internal but signature accepts any id — thread tenantId through ~5 call sites (deferred item 7, campaign 2026-07-31).
- **server/minerva-planner.ts:403** _(deferred)_ — Child-plan SELECT (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...) conditionally appends tenant filter. When args.tenantId is omitted this query can return rows from other tenants.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): child-plan SELECT applies the tenant filter conditionally (args.tenantId != null) — optional-tenant fail-open; hardening = require tenantId.
- **server/minerva-planner.ts:549** _(deferred)_ — getPlan falls back to an unscoped SELECT when tenantId is omitted: SELECT * FROM plans WHERE id = ${planId}. This allows cross-tenant reads if callers omit tenantId.
  - _Why false positive:_ DEFERRED until 2026-09-30 (task-118 audit-recovery campaign 2026-08-01 (pass 4) 2026-08-01): getPlan falls back to SELECT by id alone when tenantId omitted — optional-tenant fail-open; hardening = require tenantId on all lookups.
