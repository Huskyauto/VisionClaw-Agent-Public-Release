# Tenant-Isolation Audit Report

_Generated 2026-09-04T22:19:07.679Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 472 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **190/662** file(s) audited · chunks 20/61 OK (0 failed, 41 pending)
- Findings: **70** (CRITICAL 23 · HIGH 35 · MEDIUM 12 · LOW 0)
- Severe dispositions: **58 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (23)

- **server/agentic/wake-scheduler.ts:142** — UPDATE/SELECT claims pending wakes without tenant scoping: the claim uses `WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED)` with no tenant_id filter. This can claim/update rows for other tenants (cross-tenant mutation/locking).
  - _Fix:_ Perform tenant-scoped claims. Either iterate tenants and run the claim per-tenant (include `WHERE tenant_id = ${tenantId}` inside the inner SELECT and outer UPDATE), or accept a tenantId argument and include `AND tenant_id = ${tenantId}`. Ensure any returned/updated rows are always filtered by tenant before changing status or emitting tenant-scoped events.
- **server/auto-transcript.ts:165** — UPDATE to tenant-scoped table 'conversations' lacks tenant constraint: `UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL` — this is a write to a tenant-scoped table without WHERE tenant_id = ...
  - _Fix:_ Include an explicit tenant filter in the UPDATE (for example: `WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}`) or otherwise ensure the write is executed in a tenant-scoped transaction/gate that proves ownership before modifying tenant-scoped rows.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' without tenant_id predicate: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This unscoped UPDATE can modify rows belonging to other tenants.
  - _Fix:_ Scope the UPDATE to the tenant (and ideally the expected draft_status) — e.g. `WHERE id = ${row.id} AND tenant_id = ${row.tenantId} AND draft_status = 'draft_pending'`. Prefer verifying ownership (validate tenantId) before running any un-scoped mutation, or run inside a withTenantTx/transaction that enforces tenant context.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed without a tenant_id constraint (delete in addDocument). This is a destructive write against a tenant-scoped table missing tenant scoping.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}). Ensure the tenantId used is the caller's validated tenant.
- **server/email.ts:415** — SELECT over `inbox_messages` in backfillEmptyBodies runs without any tenant constraint: `SELECT id, message_id FROM inbox_messages WHERE ...`. This scans messages for all tenants and can leak cross-tenant data.
  - _Fix:_ Limit the backfill to a single tenant/inbox. Pass or derive a tenantId (or inboxId) and include `AND tenant_id = ${tenantId}` (or `AND inbox_id = ${inboxId}`) in the WHERE clause. Alternatively iterate per-tenant to process only rows owned by that tenant.
- **server/email.ts:428** — UPDATE against tenant-scoped table `inbox_messages` performed without tenant constraint: `UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id}`. This may update another tenant's row if ids collide or are mis-resolved.
  - _Fix:_ Include the tenant constraint (or inbox_id) on the UPDATE: `WHERE id = ${row.id} AND tenant_id = ${tenantId}` (or `AND inbox_id = ${inboxId}`). Resolve tenantId/inboxId before the update and use it as a WHERE clause to enforce ownership.
- **server/embeddings.ts:842** — SELECT on tenant-scoped table `memory_entries` in backfillEmbeddingVecs has no tenant constraint: `SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200`. This reads memory entries across all tenants.
  - _Fix:_ Run backfill per-tenant or include `AND tenant_id = ${tenantId}`. Avoid scanning tenant-scoped tables globally; derive tenantId for each batch and restrict the query to that tenant.
- **server/embeddings.ts:806** — storeEmbeddingVec fallback executes a raw table update using sql.raw(table) and no tenant constraint: `UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}`. This both uses sql.raw() with a caller-supplied table name and performs an unconstrained write to a tenant-scoped table when tenantId is omitted.
  - _Fix:_ Disallow the unscoped fallback — require a positive tenantId for any write to tenant-scoped tables. Replace the raw update with parameterized, tenant-scoped queries (e.g. `UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tenantId}`). Remove the code path that updates a table without tenant ownership, and avoid sql.raw() for table names unless fully validated and paired with tenant_id in the WHERE clause.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scraped_pages' omits tenant filter in WHERE: db.update(scrapedPages).set(...).where(eq(scrapedPages.id, existing[0].id))
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Alternatively include tenantId in the WHERE when computing existing rows and reuse that in the UPDATE to make the tenant constraint explicit.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scraped_pages' omits tenant filter in WHERE: db.update(scrapedPages).set(...).where(eq(scrapedPages.id, existing[0].id)) (inside crawl loop)
  - _Fix:_ Add an explicit tenant constraint to the UPDATE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Do not rely solely on a prior SELECT for isolation — every write must include the tenant filter.
- **server/google-drive.ts:867** — SELECT from tenant-scoped table 'projects' by project id without scoping to the caller's tenant: .from(projects).where(eq(projects.id, projectId))
  - _Fix:_ Scope the SELECT to the caller's tenant (or include a tenant check in the WHERE). For example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). This prevents cross-tenant reads and enforces ownership at query time.
- **server/google-drive.ts:880** — UPDATE on tenant-scoped table 'projects' omits tenant filter in WHERE: await db.update(projects).set({...}).where(eq(projects.id, projectId))
  - _Fix:_ Include the tenant constraint in the UPDATE to prevent cross-tenant writes. Example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). If tenant was validated earlier, still include the tenant equality in the WHERE to make the write safe and race-condition resistant.
- **server/google-drive.ts:1477** — SELECT from tenant-scoped table 'projects' during uploadAndShare resolves a project by id without including a tenant_id constraint: .from(projects).where(eq(projects.id, params.projectId))
  - _Fix:_ Add explicit tenant scoping to the SELECT (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId)))) or perform the SELECT via a helper that enforces tenant ownership before proceeding. Every read of tenant-scoped data must be constrained to the caller's tenant in the SQL WHERE clause.
- **server/heartbeat.ts:2609** — Raw UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — approval_status is being set on a heartbeat_tasks row without constraining tenant_id. This can change approval state for a task id without verifying ownership or tenant, allowing cross-tenant approval-state mutation.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call storage.updateHeartbeatTask (or another storage-layer helper) that enforces tenant ownership. Validate the task's tenant before updating approval_status.
- **server/heartbeat.ts:3064** — Raw UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (in delegateTaskFromChat) updates a tenant-scoped table without a tenant_id constraint. This is a direct write that can affect tasks outside the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage API which validates tenant context. Always verify ownership/tenant before mutating approval_status on heartbeat_tasks.
- **server/job-queue.ts:99** — claimDueJobs performs an UPDATE/SELECT against agent_jobs without any tenant_id constraint — this mutates/claims jobs across all tenants.
  - _Fix:_ Scope claims to a specific tenant or require an explicit system/admin context. Add tenant_id filtering (or a secure multi-tenant claiming strategy) or assert the caller is an authorized global worker and document/audit that capability. Ensure lease/claim writes include tenant_id in their WHERE/CAS checks to prevent cross-tenant mutation.
- **server/job-queue.ts:135** — reclaimExpiredLeases STEP 1 UPDATE sets failed_terminal on agent_jobs rows without a tenant_id filter — can terminate jobs for any tenant.
  - _Fix:_ Include tenant_id in the UPDATE or require an explicit tenant parameter (or run this as an admin-only operation with strict controls). Ensure reclamation/terminal transitions are tenant-scoped or validated against an authorized global operation.
- **server/job-queue.ts:149** — reclaimExpiredLeases STEP 2 requeues expired agent_jobs back to pending without a tenant_id restriction — affects jobs across tenants.
  - _Fix:_ Add tenant_id filtering or require the caller to supply/verify tenant scope. If this is intended to be global, enforce admin-only execution and add audit controls; otherwise, scope the UPDATE to the tenant to avoid cross-tenant side effects.
- **server/job-queue.ts:178** — completeJob updates agent_jobs to 'succeeded' by id/attempts but does not include tenant_id in the WHERE clause — a stale or rogue caller could complete another tenant's job.
  - _Fix:_ Include tenant_id in the WHERE clause or require the caller to pass the job's tenant and verify it matches the executing context. Use tenant-aware fencing in the UPDATE to prevent cross-tenant writes.
- **server/job-queue.ts:221** — failJob selects max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id check — this reads another tenant's job metadata and lets callers make decisions based on it.
  - _Fix:_ Require tenant_id as part of the lookup or restrict this helper to admin contexts with strict authorization. Prefer SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} or fetch via a prior scoped claim that includes tenant_id.
- **server/job-queue.ts:238** — failJob's terminal UPDATE transitions a job to failed_terminal without any tenant_id constraint in the WHERE clause — can dead-letter jobs from any tenant.
  - _Fix:_ Require tenant_id scoping in the UPDATE (WHERE id = ${id} AND tenant_id = ${tenantId} ...) or enforce that the worker holds a tenant-scoped lease before mutating status. Add tenant fencing to all terminal-state mutations.
- **server/job-queue.ts:258** — failJob's retry-path UPDATE re-schedules jobs (sets status=pending/next_run_at) without a tenant_id filter in the WHERE clause — can requeue jobs across tenants.
  - _Fix:_ Add tenant_id to the WHERE clause or validate the caller's tenant context prior to performing the update. All requeue logic must be tenant-scoped or explicitly authorized global actions.
- **server/job-queue.ts:275** — cancelJob performs UPDATE agent_jobs SET status='cancelled' WHERE id = ${id} ... without tenant_id — an operator/API could cancel another tenant's job by id.
  - _Fix:_ Require tenant_id in the WHERE clause and/or require operator authorization. Change the statement to include AND tenant_id = ${tenantId} or validate caller privileges for cross-tenant cancel operations.

### HIGH (35)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` is executed without constraining by tenant_id (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across all tenants.
  - _Fix:_ Limit the selection to a specific tenant (e.g. accept a tenantId param or iterate per-tenant) and add WHERE tenant_id = ${tenantId}. If the heartbeat must scan all tenants, make that explicit and ensure the caller is privileged; otherwise scope the query to the caller's tenant to preserve isolation.
- **server/agent-activity.ts:122** — UPDATE against tenant-scoped table `agent_activity` omits tenant scoping: `await db.update(agentActivity).set(updates).where(eq(agentActivity.id, activityId));`. This can modify rows belonging to other tenants.
  - _Fix:_ Require the caller to pass tenantId and include it in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))). If tenantId cannot be supplied here, change the API to accept it so updates are tenant-scoped.
- **server/agentic-features.ts:861** — SELECT checking for an existing enrollment is not tenant-scoped: `SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'`. This can observe or block enrollments across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${params.tenantId}) to ensure the uniqueness/existence check is constrained to the caller's tenant before inserting the enrollment.
- **server/agentic/resume-worker.ts:65** — Unscoped SELECT on tenant-scoped tables: the initial candidate query scans agent_runs (and joined agent_approvals) without constraining by tenant_id, returning rows across all tenants.
  - _Fix:_ Scope the query to a single tenant before reading tenant-scoped rows. Either add an explicit WHERE r.tenant_id = ${tenantId} (or equivalent drizzle .where(eq(agentRuns.tenantId, tenantId))) or iterate known tenant ids and run this query per-tenant. Do not perform an unfiltered cross-tenant SELECT; every read against a tenant-scoped table must include the caller's tenant filter (or be otherwise provably equivalent and safe).
- **server/auto-asset-capture.ts:184** — Unscoped SELECT over tenant-scoped conversations table: `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0` has no `WHERE tenant_id = ...`. This reads conversations across all tenants without scoping.
  - _Fix:_ Avoid unscoped multi-tenant reads. Either query per-tenant (fetch tenant ids and loop) or add an explicit `WHERE c.tenant_id = ${tenantId}` clause. If a system-wide job must process all tenants, enumerate tenants and run a tenant-scoped query for each to keep ownership checks explicit and auditable.
- **server/auto-asset-capture.ts:207** — Selecting messages without tenant constraint: `SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'` does not include `tenant_id`. This reads message rows without enforcing tenant ownership in the query.
  - _Fix:_ Scope message reads to the owning tenant. Add `AND tenant_id = ${c.tenant_id}` or join to conversations and require `m.tenant_id = c.tenant_id`. Prefer explicit tenant filters rather than relying on id-space uniqueness or later assertion checks.
- **server/auto-transcript.ts:132** — SELECT subquery reads tenant-scoped table 'messages' without constraining by tenant_id: `AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`
  - _Fix:_ Restrict the subquery by tenant (e.g. `AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0`) or rewrite to join messages with an explicit tenant_id filter so every read from messages is scoped to the conversation's tenant.
- **server/auto-transcript.ts:160** — SELECT subquery in the project_conversations query reads tenant-scoped table 'messages' without tenant scoping: `AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`
  - _Fix:_ Add tenant scoping to the subquery (e.g. `AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0`) or join messages with an explicit tenant_id filter so the messages read is always constrained to the correct tenant.
- **server/business-tools.ts:95** — INSERT into invoice_items omits tenant_id. If invoice_items is a tenant-scoped table, inserting line items without explicitly setting tenant_id can create rows not tied to the caller's tenant (cross-tenant leakage or orphaned data). The loop also does not validate that the new invoice_id/newId belongs to the tenant before inserting line items.
  - _Fix:_ Make the invoice_items INSERT include tenant_id (and use the trusted tid value). Example: INSERT INTO invoice_items (tenant_id, invoice_id, description, quantity, unit_price, amount, sort_order) VALUES (${tid}, ${newId}, ...). Alternatively, enforce/validate the invoice belongs to the caller's tenant before inserting items and add a DB-level tenant_id column + constraint so every line item must carry tenant_id.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' with no tenant_id filter: `SELECT id, tenant_id AS "tenantId", description, due_at AS "dueAt", ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ...`. This reads across all tenants.
  - _Fix:_ If this is intended only for platform-admin/operational aggregation, make that explicit and restrict the code path to an authorized admin context. Otherwise add `AND tenant_id = ${tenantId}` to the WHERE clause (or enumerate tenants first and run per-tenant queries), or use a tenant-scoped transaction (withTenantTx) so RLS/SET_LOCAL protects rows.
- **server/data-protection.ts:149** — SELECT from tenant-scoped table 'conversations' without tenant_id constraint: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}`. This enumerates expired soft-deleted conversations across all tenants.
  - _Fix:_ Run purge per-tenant or ensure the caller is a privileged admin and explicitly authorizes cross-tenant operations. Prefer enumerating tenant IDs and performing tenant-scoped SELECT/DELETE (e.g. include `AND tenant_id = ${tenantId}`) or run the purge under an admin restriction that is auditable.
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table dfy_intake_forms without constraining tenant_id (getDfyIntakeByToken). This read can return another tenant's intake form by token.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or require/validate the tenant context for this lookup. If the token must remain globally-lookupable, explicitly document and harden that pattern (e.g. canonicalize token ownership) and minimize returned tenant-identifying data.
- **server/doc-collections.ts:138** — Correlated subqueries in listCollections count rows in doc_chunks using WHERE collection_id = c.id but do not include tenant scoping. These reads may aggregate data across tenants.
  - _Fix:_ Constrain the subqueries by tenant (e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id AND tenant_id = c.tenant_id) ...) or otherwise include AND tenant_id = ${tenantId} to enforce tenant isolation.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding ... WHERE id = ${row.id} is executed without also constraining tenant_id. Although id was sourced from a prior tenant-scoped SELECT, the UPDATE lacks an explicit tenant filter and can mistakenly modify other tenants' rows if IDs are misaligned.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}), or perform the update using a row set that already enforces tenant id in the WHERE clause.
- **server/doc-collections.ts:459** — Subqueries in getCollectionStatus reference doc_chunks with WHERE collection_id = c.id (and similar) but do not include tenant scoping. These aggregated reads may pull cross-tenant data.
  - _Fix:_ Restrict subqueries to the tenant (e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id AND tenant_id = c.tenant_id) ...) or include tenant_id = ${tenantId} so results are limited to the caller's tenant.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages (getTrafficSnapshot) is executed without a tenant_id filter. messages is typically tenant-scoped; this global read mixes activity across tenants and can break the traffic gate logic or leak metrics.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) or, if this routine intentionally operates globally, document and isolate its use and ensure the tables are truly global (not tenant-scoped). Prefer explicit tenant parameters for per-tenant decisions.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance (getTrafficSnapshot) runs without tenant_id filter. tool_performance is likely tenant-scoped; counting globally can mis-evaluate traffic gates and candidate lists.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND last_success_at > ...) or otherwise perform tenant-aware aggregation. If the intent is a global algorithm, ensure the table is not tenant-scoped.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance (getEverInvokedTools) is executed without tenant scoping. This read can produce a global set rather than a tenant-specific one, violating tenant isolation invariants.
  - _Fix:_ Include tenant filtering (e.g. WHERE tenant_id = ${tenantId}) for tenant-scoped tables, or redesign the logic to accept a tenant parameter and compute per-tenant sets.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (getActiveTools14d) lacks tenant_id scoping. This returns active tools across all tenants rather than per-tenant results used later in candidate computation.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND last_success_at > ...) or clearly separate global vs per-tenant logic and ensure tables used are appropriate for the intended scope.
- **server/email.ts:371** — SELECT against tenant-scoped table `inbox_messages` performed without constraining by tenant_id (or inbox_id). The pollSharedInbox existence check queries `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1` with no tenant/inbox filter.
  - _Fix:_ Restrict the lookup to the relevant tenant or inbox. For example, resolve the tenant (or use the current inboxId) and run: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} AND inbox_id = ${inboxId} /* or AND tenant_id = ${tenantId} */ LIMIT 1`. Ensure every read uses an explicit tenant_id (or a tenant-bound inbox_id) to enforce isolation.
- **server/embeddings.ts:811** — Maintenance SELECT on `agent_knowledge` (backfillMissingKnowledgeEmbeddings) pulls rows across all tenants: `SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50` — no tenant-scoped WHERE filter.
  - _Fix:_ Process backfills per-tenant or explicitly constrain to a tenant for each batch. Either add `AND tenant_id = ${tenantId}` or iterate tenants and run the SELECT scoped to each tenant so embedding work never reads another tenant's rows.
- **server/embeddings.ts:852** — UPDATE to `memory_entries` sets embedding_vec using `WHERE id = ${row.id}` with no tenant constraint, allowing cross-tenant modification for that id.
  - _Fix:_ Include tenant ownership in the UPDATE: `UPDATE memory_entries SET ... WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}` (or scoped via the tenantId used when selecting rows). Always pair writes with a tenant_id WHERE clause.
- **server/embeddings.ts:858** — SELECT on tenant-scoped table `agent_knowledge` in backfillEmbeddingVecs runs without tenant filtering: `SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200`. This reads rows for all tenants.
  - _Fix:_ Constrain the SELECT to a specific tenant (e.g. `AND tenant_id = ${tenantId}`) or iterate per-tenant. Backfill jobs must not scan tenant-scoped tables globally without explicit tenant scoping.
- **server/embeddings.ts:869** — UPDATE on `agent_knowledge` writes embedding_vec using `WHERE id = ${row.id}` without constraining by tenant_id, so rows for other tenants could be modified.
  - _Fix:_ Include tenant_id in the WHERE clause (for example `WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}`) or run the update in a tenant-scoped context. Ensure all writes to tenant-scoped tables include tenant ownership checks.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` with no tenant_id filter — rehydrateTaskStateOnBoot queries agent_runs across all tenants (SELECT ... FROM agent_runs WHERE status IN ('running','pending') ...) and then processes tenant_id from rows in-memory.
  - _Fix:_ Restrict the query to an explicit tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant in a controlled, authenticated admin flow. If this must run across tenants at boot, ensure it runs under a privileged maintenance path (not on regular request handling) and enforces strict access controls and audit logging. Prefer explicit per-tenant queries to avoid accidental cross-tenant data exposure.
- **server/google-workspace.ts:2286** — INSERT into presenter_slide_images does not set tenant_id. The INSERT SQL (INSERT INTO presenter_slide_images (... ) VALUES (...) ...) omits tenant_id, so if presenter_slide_images is tenant-scoped this will create rows not tied/guarded to the caller tenant.
  - _Fix:_ Include the canonical tenantId in the inserted row (e.g. tenant_id = ${tenantId}) or derive and validate the session->tenant mapping before inserting. Prefer using Drizzle/typed INSERT with values({ tenantId, ... }) or add an explicit tenant_id column to the VALUES clause. Ensure ownership is validated (session belongs to tenant) before persisting.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. This updates a tenant-scoped session row by id only, allowing accidental modification of another tenant's session if ids collide or if there is no additional ownership check.
  - _Fix:_ Constrain the UPDATE to the session's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first validate that the session belongs to the current tenant before performing the update. Prefer selecting the session with tenant_id first and failing if ownership doesn't match.
- **server/health-audit.ts:314** — SELECT FROM code_proposals lacks any tenant_id filter. The query selects pending/unverified proposals across all tenants, which can leak tenant-scoped data to this caller or cause cross-tenant operations.
  - _Fix:_ If the scan should be per-tenant, add a WHERE tenant_id = ${tenantId} or iterate tenants explicitly and scope queries. If the intent is a global admin sweep, require and enforce explicit admin authorization and make that explicit in code and logging; otherwise restrict the SELECT to the relevant tenant context.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) has no tenant_id constraint. This will update rows across tenants if ids span multiple tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or supply per-tenant id lists and update only within the intended tenant. If this is an admin cross-tenant action, require explicit admin checks before executing.
- **server/heartbeat-startup.ts:75** — Raw UPDATE against tenant-scoped table `heartbeat_tasks` omits a tenant_id constraint (UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id}). This updates rows by id without also constraining tenant_id, allowing cross-tenant mutation if ids are misused.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage API (e.g. storage.updateHeartbeatTask) or otherwise assert the tenant context before performing the UPDATE so writes are scoped to the correct tenant.
- **server/heartbeat-startup.ts:80** — Raw UPDATE against tenant-scoped table `heartbeat_tasks` omits a tenant_id constraint (UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id}). This mutates a task row without an explicit tenant binding.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call into the storage layer which enforces tenant context. Always assert/propagate tenantId for cross-tenant-safe operations.
- **server/heartbeat.ts:255** — SELECT * FROM event_log WHERE status = 'pending' ... executes a cross-tenant read from `event_log` with no tenant_id constraint. `event_log` rows include tenant_id — reading pending events across all tenants without explicit, authorized cross-tenant handling violates tenant isolation.
  - _Fix:_ Either iterate per-authorized tenant and include WHERE tenant_id = <tenant> for each query, or move this cross-tenant behavior into a dedicated, audited scheduler boundary that explicitly documents/authorizes cross-tenant reads. Do not SELECT tenant-scoped rows without an explicit tenant filter.
- **server/job-queue.ts:346** — listJobs builds and executes a SELECT from agent_jobs without a tenant_id constraint when tenantId is omitted — by default this can return jobs across all tenants.
  - _Fix:_ Require callers to explicitly pass a tenantId (or an ALL_TENANTS constant) and enforce admin authorization when ALL_TENANTS is requested. If the function must support cross-tenant listing, document and gate it behind admin privileges and audit logs; otherwise always include tenant_id in the WHERE.
- **server/job-queue.ts:420** — getJobStats composes a SELECT COUNT(*) FROM agent_jobs and omits tenant filtering when scope==ALL_TENANTS — aggregate counts can leak tenant activity without explicit admin authorization.
  - _Fix:_ Require explicit admin authorization for ALL_TENANTS scope and document it. If tenant-level stats are the normal path, make tenant_id mandatory; otherwise add an authorization check and audit trail for cross-tenant aggregations.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes a SELECT grouping over agent_knowledge across all tenants (no tenant_id filter) to enumerate (tenant_id, persona_id) pairs — this scans tenant data globally.
  - _Fix:_ Ensure this operation is performed only by an admin/maintenance task with strict access controls and audit logging. If possible, iterate tenants via an explicit admin-only list or schedule per-tenant runs that explicitly pass tenant_id, or otherwise require and validate admin privileges before running cross-tenant scans.

### MEDIUM (12)

- **server/agentic-engines.ts:161** — SELECT from tenant-scoped table `ai_insights` in retryPendingMinervaRouting is executed without a tenant_id constraint: `SELECT id, tenant_id, category, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER} LIMIT 50`. This reads rows across all tenants.
  - _Fix:_ Either make this function accept a tenantId and add `AND tenant_id = ${tenantId}`, or otherwise restrict access to an admin-only path and document the multi-tenant sweep risk. Prefer per-tenant sweeps or explicit admin authorization when operating across tenants.
- **server/agentic-engines.ts:247** — UPDATE on tenant-scoped table `ai_insights` (autoApplyEligibleInsights) can run without a tenant filter when tenantId is omitted (tenantScope = sql``), allowing status/action_taken changes across all tenants.
  - _Fix:_ Require a tenantId (remove optional scoping) or ensure the caller explicitly supplies tenantId. If a global sweep is intended, make it an explicit, privileged operation and audit/log it; otherwise add `AND tenant_id = ${tenantId}` to the WHERE clause so updates are tenant-scoped.
- **server/agentic/approvals.ts:135** — Bulk UPDATE expiry sweep (expireStaleApprovals) updates `agent_approvals` rows across tenants without a tenant_id constraint: `UPDATE agentApprovals SET status='expired' WHERE status='pending' AND expiresAt < ...`. This performs tenant-crossing updates in one statement.
  - _Fix:_ Either scope the bulk expiry to a specific tenant (require tenantId) or, if a global sweep is intended, ensure this path is privileged and that per-row tenant checks / transaction boundaries are appropriate. Prefer updating per-tenant (add `AND tenant_id = ${tenantId}`) or selecting rows first and applying per-row updates while verifying tenant ownership.
- **server/cmmc-assessments.ts:145** — getCmmcAccessByToken performs a SELECT joining cmmcAssessmentInvitations and cmmcAssessments without including an explicit tenant_id constraint in the WHERE clause. This reads tenant-scoped rows without enforcing the caller's tenant boundary (relies solely on tokenHash filter).
  - _Fix:_ Constrain the query to the expected tenant (or require a tenantId parameter) so every access to tenant-scoped tables includes eq(<table>.tenantId, tenantId). If token-based access is required, bind the token to a tenant at creation and validate that association in the WHERE clause (e.g. WHERE token_hash = ... AND cmmc_assessments.tenant_id = <tenantId>), or return only the minimal data necessary and ensure downstream updates always re-check tenant ownership before changing tenant-scoped rows.
- **server/data-protection.ts:360** — SELECT from tenant-scoped table 'project_notes' without tenant_id constraint: `SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} ...`. The query relies only on project_id and omits tenant predicate.
  - _Fix:_ Add an explicit tenant predicate (e.g. `AND tenant_id = ${tenantId}`) to the SELECT, or verify ownership of project_id before querying related tables so reads cannot cross tenant boundaries if IDs collide across tenants.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table 'project_files' without tenant_id constraint: `SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} ...`. Missing tenant scoping.
  - _Fix:_ Include `AND tenant_id = ${tenantId}` or otherwise validate the project ownership before enumerating related project_files to ensure tenant isolation.
- **server/data-protection.ts:404** — COUNT query against tenant-scoped table 'messages' without tenant_id predicate: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}`. This is an existence/oracle read that lacks tenant scoping.
  - _Fix:_ Scope the COUNT by tenant (e.g. `WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}`) or verify the conversation ownership prior to querying messages to avoid cross-tenant information leaks.
- **server/falsified-routes.ts:111** — String-composed embedding literal (embeddingLiteral = `[${embedding.join(",")}]`) is interpolated into SQL expressions (objective_embedding <=> ${embeddingLiteral}::vector) using template interpolation. The embedding is derived from caller-supplied objective and is being injected into SQL text/expressions rather than passed as a properly typed parameter/array binding.
  - _Fix:_ Avoid constructing SQL fragments by concatenating/serializing request-derived values. Instead pass the embedding as a bound parameter/array (let the driver parameterize it) or use Drizzle/PG parameter binding for vector/array types (e.g. use parameter placeholder and cast safely: objective_embedding <=> $1::vector). This eliminates injection risk and keeps tenant isolation checks intact.
- **server/falsified-routes.ts:175** — Array literal is built via string interpolation for an IN/ANY clause: id = ANY(${`{${ids.join(",")}}`}::int[]). Although `ids` came from prior DB results here, constructing SQL array literals by string concatenation risks injection if the values were untrusted and is fragile for parameterization/typing.
  - _Fix:_ Use parameterized array binding rather than interpolating a textual array literal. For example, pass `ids` as a parameter and use WHERE id = ANY($1::int[]) (or use Drizzle helpers to bind arrays) to avoid string interpolation and ensure safe, typed parameters.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (stuck heartbeat detection) does not constrain by tenant_id. If heartbeat_tasks are tenant-scoped, this will surface tasks across tenants without scoping/authorization.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, scope the SELECT by tenant_id or enumerate tenants intentionally in a controlled admin flow. If cross-tenant enumeration is intended, ensure this code runs under explicit admin privileges and document/guard that behavior.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) has no tenant_id constraint. This will disable tasks across tenants if ids include tasks from other tenants.
  - _Fix:_ Restrict the UPDATE to the task's tenant (e.g. AND tenant_id = ${tenantId}) or perform per-tenant updates after verifying ownership. If the operation must be global, require explicit admin authorization and add comments/auditing to make the cross-tenant intent explicit.
- **server/job-queue.ts:317** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} has no tenant constraint — exposes tenant mapping by job id to callers of this helper if misused outside admin flows.
  - _Fix:_ Restrict this helper to admin-only code paths and add explicit authorization checks. Prefer an admin-only SQL path or accept a caller-provided tenantId and verify it, or at minimum audit/callsite-guard use to ensure it's not reachable from tenant-scoped routes.
