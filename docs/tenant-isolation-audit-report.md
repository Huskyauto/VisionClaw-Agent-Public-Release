# Tenant-Isolation Audit Report

_Generated 2026-07-23T10:35:13.774Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **562/562** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **292** (CRITICAL 96 · HIGH 170 · MEDIUM 25 · LOW 1)
- Precision second-pass (`gpt-5.4`): **168** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (96)

- **server/agentic-engines.ts:244** — autoApplyEligibleInsights executes an UPDATE on `ai_insights` without a tenant_id constraint (UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...)). This is a cross-tenant write that can modify rows belonging to other tenants.
  - _Fix:_ Avoid bulk updates that span tenants. Either restrict the UPDATE to a single tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant and perform tenant-scoped updates. Ensure any RETURNING/side-effects are tenant-scoped as well.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals without constraining by tenant_id (UPDATE agentApprovals SET status='expired' ... WHERE status='pending' AND lt(expiresAt, now())). This will expire pending approvals across all tenants and then proceed to operate on runs by tenant, creating a dangerous multi-tenant write.
  - _Fix:_ Expiry should be performed in a tenant-scoped manner. Either accept/derive a tenantId and add AND tenant_id = ${tenantId} to the WHERE, or iterate per-tenant and run the expiry per-tenant. Bulk cross-tenant updates that change state must be avoided.
- **server/agentic/runs.ts:36** — appendStep updates agent_runs using WHERE id = runId only (no tenant_id constraint). This allows cross-tenant modification of run rows when called with an arbitrary runId.
  - _Fix:_ Require and pass the caller's tenantId into appendStep and include it in the UPDATE WHERE clause (e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))). Enforce tenant scoping on every update against agent_runs.
- **server/agentic/runs.ts:45** — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) but does not restrict by tenant_id. This permits cross-tenant writes to run state.
  - _Fix:_ Add tenant scoping: accept tenantId (or otherwise obtain caller tenant) and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all state-updating paths include the tenant check.
- **server/agentic/runs.ts:54** — replaceRunState updates agent_runs using only run id in WHERE clause (no tenant constraint), enabling cross-tenant state replacement.
  - _Fix:_ Require tenantId and include it in the WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Audit other run-modifying helpers for the same pattern.
- **server/agentic/runs.ts:58** — completeRun updates agent_runs (status/result/completedAt) using WHERE id = runId only — missing tenant_id constraint which allows cross-tenant writes.
  - _Fix:_ Add tenant validation (pass tenantId into completeRun) and include eq(agentRuns.tenantId, tenantId) in the UPDATE WHERE clause. Also propagate tenant scoping to any helper that may call completeRun.
- **server/agentic/runs.ts:88** — failRun updates agent_runs setting status/error/completedAt using WHERE id = runId only — no tenant_id filter, permitting cross-tenant updates.
  - _Fix:_ Require tenantId and add it to the UPDATE WHERE clause (and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all public/worker paths include the tenant check before mutating agent_runs.
- **server/agentic/runs.ts:97** — pauseRun updates agent_runs to status='paused' using only run id in the WHERE clause (no tenant constraint), enabling cross-tenant state changes.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or change the API to accept tenantId and use .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))).
- **server/agentic/wake-scheduler.ts:146** — Claiming due wake rows uses a subquery that selects FROM agent_wake_schedules without constraining tenant_id — the statement will transition pending wakes across ALL tenants to 'firing' (cross-tenant row modification).
  - _Fix:_ Restrict the claim to a single tenant at a time (e.g. run per-tenant claims with WHERE tenant_id = <tenantId>), or include a tenant_id predicate in the inner SELECT so you never update/lock rows belonging to other tenants. Avoid a global UPDATE that moves rows across tenant boundaries.
- **server/auto-memorize.ts:47** — getWatermark() reads agent_knowledge rows with no tenant_id filter (SELECT content FROM agent_knowledge WHERE category=... AND title=...), so watermark lookup is global and can return another tenant's value.
  - _Fix:_ Make the watermark per-tenant: require/accept a tenantId parameter and include AND tenant_id = ${tenantId} in the SELECT. Ensure every read against agent_knowledge that is tenant-scoped includes the caller's tenant_id filter.
- **server/auto-memorize.ts:74** — setWatermark() INSERT hard-codes tenant_id = 1 when creating the watermark row, and its SELECT/UPDATE logic omits tenant scoping — this writes/overwrites a watermark on the admin tenant (or an arbitrary tenant) rather than the acting tenant.
  - _Fix:_ Change setWatermark to accept a tenantId and write that tenant's watermark (VALUES (..., ${tenantId}, ...)). Also ensure the initial SELECT/UPDATE that finds existing watermark rows filters by tenant_id so updates only affect the intended tenant.
- **server/auto-transcript.ts:165** — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — performing a write against a tenant-scoped table without constraining by tenant_id. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${c.tenant_id}) or otherwise verify ownership of the conversation and project before performing the UPDATE (use assertConversationInTenant/assertProjectInTenant). Always include tenant_id in WHERE for updates to tenant-scoped tables.
- **server/auto-transcript.ts:179** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING executed using rows derived from an unscoped SELECT. This may create cross-tenant links (write) because the INSERT does not validate that the project and conversation belong to the same tenant.
  - _Fix:_ Before inserting, verify tenant ownership of both project_id and conversation_id (e.g. ensure projects.tenant_id = conversations.tenant_id = targetTenantId) or add tenant scoping to the initial SELECT that supplies these values. If project_conversations has a tenant_id column, populate it; otherwise ensure the operation is performed per-tenant with explicit checks.
- **server/data-protection.ts:72** — UPDATE on conversations omits tenant scoping. The UPDATE that marks a conversation deleted uses WHERE id = ${conversationId} but does not constrain by tenant_id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or verify ownership before updating so the change cannot affect another tenant's rows.
- **server/data-protection.ts:125** — Deletes in permanentlyPurgeSoftDeleted lack tenant scoping. The function deletes rows from messages, compaction_archives, and conversations using only conversation_id / id — no tenant_id predicate, which can remove data across tenants.
  - _Fix:_ Add tenant predicates to each DELETE (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or perform deletes inside a transaction after verifying the conv belongs to the intended tenant. Prefer explicit tenant checks on every destructive statement.
- **server/embeddings.ts:684** — UPDATE against tenant-scoped table (memory_entries or agent_knowledge via storeEmbeddingVec) does not constrain by tenant_id. The function updates embedding_vec / embedding WHERE id = ${id} with no tenant check, allowing cross-tenant modification.
  - _Fix:_ Require and pass an explicit tenantId into storeEmbeddingVec and include AND tenant_id = ${tenantId} in the UPDATE WHERE clause (or use Drizzle's eq(table.tenantId, tenantId)). Validate caller ownership before performing the update.
- **server/embeddings.ts:726** — UPDATE memory_entries in backfillEmbeddingVecs sets embedding_vec WHERE id = ${row.id} with no tenant_id constraint — allows updating arbitrary tenants' rows.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and ensure the backfill runs with a verified tenant context, or iterate per-tenant and parameterize tenantId.
- **server/embeddings.ts:743** — UPDATE agent_knowledge in backfillEmbeddingVecs sets embedding_vec WHERE id = ${row.id} with no tenant_id constraint — allows updating arbitrary tenants' rows.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and ensure the backfill runs per-tenant with a validated tenantId. Additionally, make storeEmbeddingVec accept a tenantId and enforce it there as well.
- **server/felix-brain.ts:19** — Boot-time rehydration queries agent_runs without any tenant filter — SELECT reads tenant-scoped rows across all tenants.
  - _Fix:_ Scope the query by tenant (e.g. add WHERE tenant_id = ${tenantId} or iterate per-tenant) or run one query per tenant within the proper tenant context. Never perform cross-tenant SELECTs on tenant-scoped tables at boot.
- **server/google-drive.ts:946** — INSERT INTO project_files omits tenant_id entirely — inserting into a tenant-scoped table without explicitly setting tenant_id (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT values (and ensure the caller's tenant is validated/owned before inserting). Always set tenant_id explicitly on inserts into tenant-scoped tables.
- **server/google-workspace.ts:2359** — INSERT into presenter_slide_images performed without any tenant scoping (no tenant_id column passed). This writes image blobs tied to a session but does not verify/record the tenant owning that session.
  - _Fix:_ Include tenant_id in the INSERT (and schema) or otherwise validate session ownership before writing. E.g. fetch/verify that presenter_sessions.id = sessionId is owned by the expected tenant, then INSERT with tenant_id = <tenantId> (or add tenant_id to the unique key) so writes are constrained to the correct tenant. Use parameterized Drizzle inserts rather than unsuffixed SQL where possible.
- **server/google-workspace.ts:2363** — Thumbnail INSERT into presenter_slide_images (thumb row) also performed with no tenant scoping (no tenant_id passed).
  - _Fix:_ Same as above: verify the session belongs to the caller's tenant before persisting thumbnails, and include tenant_id in the INSERT/ON CONFLICT key or otherwise ensure multi-tenant isolation (e.g. WHERE tenant_id = ${tenantId} on update paths). Prefer using the tenantId from the trusted execution context rather than only session_id.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a tenant-scoped resource without constraining by tenant_id or validating session ownership.
  - _Fix:_ Verify that the presenter_session with id = sessionId belongs to the caller's tenant before updating (or include AND tenant_id = ${tenantId} in the WHERE clause). Never update tenant-scoped rows using only a foreign id supplied/returned by another service without an explicit tenant check.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) executes a cross-tenant write (no tenant filter). Selected IDs may belong to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the update using a join to the earlier tenant-scoped selection to ensure only rows for the intended tenant are modified. Avoid updating by id alone unless you have already validated ownership per id.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id without checking tenant ownership (no tenant filter), potentially disabling other tenants' tasks.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or join to a tenant-scoped subquery of the previously-selected rows to ensure only tasks belonging to the intended tenant are modified. Require explicit tenant validation before any cross-row mutation.
- **server/internal-resolver.ts:93** — UPDATE event_log finalization omits tenant scoping — `finalizeEvent` updates event_log WHERE id = ${eventId} but does not constrain by tenant_id. This lets the resolver mutate events across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. add `AND tenant_id = ${tenantId}`) and ensure finalizeEvent accepts/validates a tenantId (or resolves it from the event record before writing). Always verify ownership/audit context before mutating tenant-scoped rows.
- **server/internal-resolver.ts:107** — SELECT FROM plans reads a plan by id with no tenant constraint (`SELECT id, objective, status FROM plans WHERE id = ${planId} LIMIT 1`). A caller-supplied planId (from event data) could reference another tenant's row.
  - _Fix:_ Fetch the plan using a tenant-scoped WHERE clause (e.g. `WHERE id = ${planId} AND tenant_id = ${tenantId}`) or first resolve/validate the event's tenant and explicitly verify the plan belongs to that tenant before any decision/update. Pass tenantId into this function rather than relying on unvalidated foreign ids.
- **server/internal-resolver.ts:171** — UPDATE code_proposals mutates a tenant-scoped table by id only (`UPDATE code_proposals SET ... WHERE id = ${proposalId} AND status = 'pending'`) without constraining by tenant_id — an event with a proposalId could affect another tenant's proposal.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. `WHERE id = ${proposalId} AND tenant_id = ${tenantId} AND status = 'pending'`) or otherwise validate ownership of the proposal before changing its status. Ensure the resolver is passed/derives the tenantId for the event and uses it in all DML against tenant-scoped tables.
- **server/lib/action-ledger.ts:304** — UPDATE on tenant-scoped table `action_attempts` performed without any tenant_id predicate (sweepStaleAttempts updates rows across all tenants).
  - _Fix:_ Do not perform cross-tenant UPDATEs. Scope the UPDATE to a tenant (e.g. include AND tenant_id = ${tenantId}), or iterate per-tenant and call the tenant-scoped transition()/markReconcilerDigested for each row. If the reconciler must be multi-tenant, it must still perform per-row writes using that row's tenant_id in the WHERE clause so no single UPDATE touches other tenants' rows.
- **server/memory-graph.ts:194** — INSERT into tenant-scoped table `memory_links` does not set tenant_id (INSERT INTO memory_links (... ) VALUES (...)). This creates cross-tenant links and allows insertion of rows not bound to the caller's tenant.
  - _Fix:_ Include tenant_id in the INSERT and set it from the verified tenantId parameter (e.g. INSERT ... (source_memory_id, target_memory_id, link_type, strength, confidence, source_count, tenant_id) VALUES (..., ${tenantId})), and ensure the caller's tenant actually owns the source/target memory ids before inserting. If memory_links is intended to be tenant-scoped, make tenant_id NOT NULL and enforce it at application-level (and consider DB constraints to prevent accidental omissions).
- **server/minerva-planner.ts:355** — UPDATE plans CAS (compare-and-swap) is executed without scoping to tenant_id. The query `UPDATE plans ... WHERE id = ${args.planId} AND status = 'awaiting_approval'` can modify a plan belonging to any tenant.
  - _Fix:_ Require the caller's tenant context (pass tenantId into decidePlan or look it up from a trusted session) and include `AND tenant_id = ${tenantId}` in the WHERE clause. Alternatively fetch the row with tenant scoping first and use that tenant when performing the CAS update so the UPDATE can never affect other tenants' rows.
- **server/minerva-planner.ts:459** — Rollback UPDATE on plans (`UPDATE plans SET ... WHERE id = ${args.planId} AND status = 'revising'`) is executed without a tenant_id condition, allowing modification of another tenant's plan during error handling.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. `AND tenant_id = ${tenantId}`) and ensure tenantId is derived from a validated/trusted source (or passed into decidePlan). Treat rollback/cleanup queries the same as normal workflow queries — never update tenant-scoped tables without tenant verification.
- **server/oauth-subscriptions.ts:906** — SELECT from tenant-scoped table oauth_subscriptions without tenant_id filter (reads id, provider, tenant_id, expires_at, refresh_token, consecutive_failures) — global query exposes sensitive per-tenant tokens/data.
  - _Fix:_ Restrict this query to a specific tenant_id or explicitly iterate tenants with an admin-only control path. If the intent is a global admin sweep, document and gate it (require an admin-only code path) or move global operations to a dedicated admin-only table. Never return refresh_token/access_token rows to unscoped callers.
- **server/plan-executor.ts:116** — appendExecutionLog updates the plans table with WHERE id = ${planId} but does not constrain by tenant_id. This allows updating a plan row belonging to any tenant by id, violating tenant isolation.
  - _Fix:_ Require a tenant check on the UPDATE. Either accept/require the plan's tenant_id as an additional argument and use WHERE id = ${planId} AND tenant_id = ${tenantId}, or look up/verify the plan's tenant before updating and include tenant_id in the WHERE clause to ensure the caller can only modify rows for that tenant.
- **server/plan-executor.ts:513** — Claiming a plan uses UPDATE plans ... WHERE id = ${planId} AND status = 'approved' RETURNING tenant_id, ... but does not include a tenant_id constraint. Without explicit tenant scoping an attacker could cause the executor to claim/operate on another tenant's plan ID.
  - _Fix:_ Include tenant scoping in the CAS/claim query (e.g. AND tenant_id = ${callerTenantId}) or otherwise validate the caller/context is authorized for the plan's tenant before flipping status. If this is intended to be a system-wide worker, ensure the worker runs with explicit tenant context per-plan (e.g. acquire plan by id+tenant) and never mutate a plan without confirming tenant_id.
- **server/plan-executor.ts:527** — When a claimed plan has no steps the code runs UPDATE plans SET status = 'failed' WHERE id = ${planId} without constraining by tenant_id. This can change someone else's tenant-scoped plan state by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or use the claimed plan's tenant_id to guard the update. Prefer parameterized updates that include tenant_id to enforce isolation.
- **server/plan-executor.ts:725** — Final plan status update uses UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} without tenant_id in the WHERE clause, so plan status and execution_log can be written for any tenant's plan by id.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${planTenantId}). Ensure the plan's tenant is derived from the claimed row and passed to append/commit operations so no cross-tenant writes are possible.
- **server/plan-executor.ts:759** — Error-handling cleanup issues UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' without tenant_id filtering. This may mark a different tenant's plan as failed if IDs collide or inputs are tampered with.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${planTenantId} AND status = 'executing') and/or verify the plan's tenant before performing cleanup updates. Prefer passing the plan's tenant through all update helpers.
- **server/plan-executor.ts:775** — resumeStuckPlans updates plans globally: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This performs cross-tenant writes without tenant scoping and can reset executing plans for all tenants indiscriminately.
  - _Fix:_ If this operation must be global, restrict it to an admin-only code path with explicit justification and auditing. Otherwise iterate per-tenant or include a tenant_id parameter to limit the scope (e.g. UPDATE plans SET ... WHERE tenant_id = ${tenantId} AND ...). At minimum, add logging/auditing and admin authorization checks and/or perform per-tenant recovery to preserve tenant isolation.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write against a tenant-scoped table without constraining tenant_id. This can modify other tenants' rows if ids collide or were tampered.
  - _Fix:_ Include tenant protection in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) or use the tenant-scoped row identifier returned earlier to ensure only the intended tenant's row is updated.
- **server/process-governor.ts:641** — UPDATE agent_desks ... WHERE id = ${desk.id} updates a tenant-scoped table without tenant_id constraint (resetting desks by id only).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause (or otherwise validate the desk belongs to the tenant before updating) to prevent cross-tenant modifications.
- **server/process-governor.ts:976** — UPDATE governance_rules SET last_triggered_at = ..., trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance_rules without scoping by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${rule.id} AND tenant_id = ${tenantId}) to ensure rule counters are only modified for the correct tenant.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at / setting rebase_outcome) updates a tenant-scoped table without scoping by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or verify that the proposal belongs to the tenant before performing updates.
- **server/proposal-verifier.ts:246** — persist() issues UPDATE code_proposals SET verification_status/... WHERE id = ${proposalId} without tenant scoping, allowing verification state writes to affect other tenants' proposals.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and change caller/site code to supply/validate tenantId for verification operations.
- **server/proposal-verifier.ts:372** — In attemptRebaseOnDrift the code updates code_proposals to mark a proposal 'rejected' / set validation_result WHERE id = ${proposalId} with no tenant_id check.
  - _Fix:_ Constrain these UPDATEs with tenant_id = ${tenantId} (and ensure tenantId is provided to attemptRebaseOnDrift), validating ownership before mutating proposal rows.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} persists rebased diffs without tenant scoping.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (and require tenantId parameter) so rebased diffs are only stored for the owning tenant's proposal.
- **server/research-engine.ts:158** — UPDATE on research_sessions in cleanupZombieSessions has no tenant_id constraint — this will mark stale 'running' sessions completed across all tenants.
  - _Fix:_ Constrain the UPDATE by tenant. Either run the query per-tenant (e.g. iterate tenant ids present in activeSessions and include WHERE tenant_id = ${tenantId}) or add a tenant_id parameter to the SQL (WHERE tenant_id = ${tenantId} AND status = 'running' ...). Never perform cross-tenant updates from a single unscoped statement.
- **server/research-engine.ts:390** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() lacks tenant_id — this will delete autoresearch knowledge for all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()), or run per-tenant. Ensure this cleanup runs only for the intended tenant(s).
- **server/routes.ts:4324** — SELECT from tenant-scoped table 'conversations' is performed without constraining by tenant_id (db.execute(sql`SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL`)). This can return data from a conversation belonging to another tenant.
  - _Fix:_ Constrain the SELECT by the caller's tenant. e.g. add AND tenant_id = ${tenantId} or, better, use a storage-layer/Drizzle select that includes .where(eq(conversations.tenant_id, tenantId)) (or storage.getConversation(conversationId, tenantId)). Always verify ownership before using returned foreign IDs.
- **server/routes.ts:4330** — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). tenant_id must be stored on insert to enforce isolation.
  - _Fix:_ Include tenant_id explicitly in the INSERT and set it to the caller's tenant (VALUES (${tenantId}, ${projId}, ...)) or use a storage-layer helper that enforces tenant scoping. Also ensure the referenced project_id is owned by the tenant (verify ownership) before inserting.
- **server/sculptor.ts:94** — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This can modify a session belonging to another tenant.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or the caller's tenantId). Always constrain mutations on tenant-scoped tables by tenant_id.
- **server/sculptor.ts:101** — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}). This can modify a session belonging to another tenant.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}). Ensure every write against sculptor_sessions includes tenant_id.
- **server/sculptor.ts:162** — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). This terminal update (status/result/completed_at) can affect another tenant's session.
  - _Fix:_ Restrict the UPDATE to the caller's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}). All post-run writes must include tenant_id to enforce isolation.
- **server/sculptor.ts:347** — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId}). This allows updating another tenant's session review_result.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) before updating review_result. Validate tenant ownership prior to mutating tenant-scoped rows.
- **server/second-opinion.ts:407** — SETTLE operation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}) with no tenant_id constraint. agent_cost_ledger is tenant-scoped, so this may modify another tenant's ledger row.
  - _Fix:_ Require and pass the tenantId into settleFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise validate the reservation row belongs to the tenant) before updating. Always scope ledger updates to tenant_id.
- **server/second-opinion.ts:424** — RELEASE operation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger SET cost_usd='0' ... WHERE id = ${reservationId}) with no tenant_id constraint. This can affect another tenant's reservation.
  - _Fix:_ Add tenant scoping to releaseFusionReservation (accept tenantId or validate ownership) and include AND tenant_id = ${tenantId} in the WHERE clause. Never update tenant-scoped ledger rows by id alone without confirming tenant_id.
- **server/seed.ts:2860** — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table by id only, with no tenant_id constraint. This can modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership in the UPDATE predicate (e.g. WHERE id = ${p.id} AND tenant_id = ${tenantId}) or JOIN against the tenant-scoped table to ensure the row belongs to the caller's tenant. Use Drizzle .where(eq(research_programs.tenant_id, tenantId)).
- **server/seed.ts:3137** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 ... executes an UPDATE on projects (a tenant-scoped table) without constraining tenant_id. This may affect projects in other tenants.
  - _Fix:_ Restrict the UPDATE to the intended tenant (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}) or include tenant_id in the WHERE clause. Verify ownership before updating. Use Drizzle eq(table.tenantId, tenantId) in the where clause.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is executed without supplying tenant_id. project_conversations is tenant-scoped; the insert will create rows with no tenant association (or default) and may link resources across tenants.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conversationId})). Additionally, validate that project_id and conversation_id belong to the same tenant before inserting.
- **server/seed.ts:3137** — UPDATE on tenant-scoped table `projects` without tenant_id constraint. The statement updates project id=15 globally (no WHERE tenant_id = <tenant>) allowing cross-tenant modification.
  - _Fix:_ Constrain the UPDATE to the intended tenant, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or use a Drizzle .where(eq(projects.tenantId, tenantId))). Ensure tenantId is the system/seed tenant when running seeding logic.
- **server/seed.ts:3205** — SELECT from tenant-scoped table `conversations` has no tenant_id filter. The query selects conversations across all tenants and later inserts links for returned rows without verifying ownership.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE c.tenant_id = ${tenantId}) or otherwise verify ownership before acting. Always validate that any project_id/conversation_id belongs to the caller tenant before further processing.
- **server/seed.ts:3213** — INSERT into `project_conversations` uses project_id and conversation_id derived from prior SELECTs without verifying tenant ownership or including tenant_id. project_conversations has no tenant_id column in this schema, so the insert can link resources across tenants.
  - _Fix:_ Before inserting, verify both project and conversation rows belong to the same tenant (SELECT projects.tenant_id, conversations.tenant_id and assert equality). Prefer adding tenant_id to project_conversations (and populate it) or otherwise include ownership checks to prevent cross-tenant linking.
- **server/seed.ts:4459** — UPDATE on tenant-scoped table `memory_entries` (REPLACE operation) runs without any tenant_id constraint. This will modify memory rows across all tenants.
  - _Fix:_ Include an explicit tenant filter in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or iterate per-tenant. Ensure the seeded/maintenance migrations declare and use the correct tenant context before performing mass updates.
- **server/seed.ts:4656** — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} but does not include tenant_id = <tenant>. Per the project's invariant, tenant-scoped writes must be constrained by tenant_id, not only by id.
  - _Fix:_ Add tenant_id constraint to the UPDATE (e.g. WHERE id = ${rows[0].id} AND tenant_id = ${tenantId}) or use a Drizzle .where(eq(agentKnowledge.tenantId, tenantId)) to ensure the operation is scoped to the intended tenant.
- **server/self-reflection.ts:366** — UPDATE on tenant-scoped table memory_entries is performed using only WHERE id = <id> with no tenant_id constraint. This allows an attacker or race to update a row belonging to another tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add AND tenant_id = ${tenantId} to the WHERE clause (or equivalent Drizzle eq(memoryEntries.tenantId, tenantId)) so writes are always tenant-scoped.
- **server/self-improvement.ts:394** — UPDATE on tenant-scoped table experiments uses only eq(experiments.id, experiment.id) in the WHERE clause and does not constrain by tenant_id. This can update another tenant's experiment row by id.
  - _Fix:_ Include the tenant filter in the update, e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) or otherwise ensure the update is constrained to the experiment's tenant.
- **server/self-improvement.ts:413** — Error-path UPDATE on tenant-scoped table experiments uses only eq(experiments.id, experiment.id) in WHERE and omits tenant_id constraint, allowing cross-tenant writes on error handling.
  - _Fix:_ As above, add an explicit tenant constraint to the WHERE clause in error-path updates (AND tenant_id = config.tenantId or equivalent Drizzle eq) to ensure tenant-scoped writes.
- **server/skillify.ts:91** — SELECT from tenant-scoped table `messages` without a tenant filter. recentMsgs = db.select().from(messages).where(eq(messages.conversationId, conversationId)) can return messages from other tenants.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or use tenantScope on messages. Always include tenant_id in WHERE for tenant-scoped reads.
- **server/skillify.ts:201** — SELECT from tenant-scoped table `messages` in analyzeConversation lacks tenant filtering: .where(eq(messages.conversationId, conversationId)). This can leak cross-tenant messages.
  - _Fix:_ Add tenant constraint to the messages query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise validate the conversation's tenant and scope the messages query accordingly.
- **server/skillify.ts:221** — SELECT from `personas` reads persona by id with .where(eq(personas.id, conv[0].personaId)) but does not verify persona.tenant_id == tenantId. This allows reading a persona belonging to another tenant.
  - _Fix:_ Scope the personas lookup to tenantId (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))) or call a guard that asserts persona ownership before reading.
- **server/skillify.ts:329** — SELECT from tenant-scoped table `messages` in skillifyConversation: .where(eq(messages.conversationId, conversationId)) with no tenant constraint — same cross-tenant-read risk as other message queries.
  - _Fix:_ Include tenant scoping on the messages query (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or otherwise validate ownership before reading messages.
- **server/skills-marketplace.ts:213** — INSERT INTO skills (...) VALUES (...) — the INSERT omits tenant_id. Per the invariant all INSERTs into tenant-scoped tables must set tenant_id explicitly. This will create rows with no tenant linkage or rely on DB defaults that do not exist.
  - _Fix:_ Accept and pass a tenantId to installSkillFromTemplate (or otherwise determine the target tenant) and include tenant_id in the INSERT columns and VALUES (e.g. INSERT INTO skills (tenant_id, name, ...) VALUES (${tenantId}, ...)). If this table is intentionally global, make that explicit and ensure schema + access controls reflect a global table.
- **server/skills-marketplace.ts:224** — SELECT * FROM skills WHERE id = ${skillId} (exportSkill) executed without tenant filter. Reading tenant-scoped skills by id without scoping can leak other tenants' skill data.
  - _Fix:_ Require and use a tenantId parameter and include AND tenant_id = ${tenantId} in the WHERE clause, or otherwise validate caller permission before returning skill data.
- **server/skills-marketplace.ts:252** — INSERT INTO skills (...) VALUES (...) in importSkill omits tenant_id column. This writes tenant-scoped table without setting tenant_id which violates the invariant and can create cross-tenant pollution.
  - _Fix:_ Require a tenantId for importSkill and include tenant_id in the INSERT. If importSkill is meant to be admin/global, make that explicit and protect the endpoint accordingly; otherwise fail-closed.
- **server/stability-watchdog.ts:177** — SELECT FROM heartbeat_logs to detect flaky tasks (recentLogs) has no tenant filter. The query aggregates errors across all tenants, and subsequent actions (disabling tasks via storage.updateHeartbeatTask) may disable tasks across tenants or make wrong cross-tenant decisions.
  - _Fix:_ Scope the query per-tenant (e.g. GROUP BY tenant_id, task_id and process per-tenant), or add AND tenant_id = ${tenantId} to the WHERE clause. If the watchdog is intentionally global/admin, make that explicit and ensure actions that modify tenant-scoped rows are executed per-tenant or guarded by admin privileges.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' ... is executed with no tenant constraint. This can delete disabled delegation tasks across all tenants.
  - _Fix:_ Add tenant scoping to the DELETE (e.g. WHERE tenant_id = ${tenantId} AND ...) or iterate per-tenant and confirm this is an admin-only global purge before running unscoped deletions.
- **server/stability-watchdog.ts:225** — DELETE FROM event_log WHERE ... is executed without tenant scoping. This will remove event_log rows across all tenants rather than per-tenant.
  - _Fix:_ Include tenant_id in the WHERE clause or run the cleanup per-tenant. If this is intended to be a global admin maintenance action, ensure it's only callable in a privileged context and document it explicitly.
- **server/stability-watchdog.ts:259** — DELETE FROM heartbeat_logs WHERE created_at < ... is executed without tenant scoping (pruneOldLogs). This will prune heartbeat logs for all tenants indiscriminately.
  - _Fix:_ Restrict this prune to a tenant (WHERE tenant_id = ${tenantId} AND ...) or run per-tenant in a controlled admin loop. If a global purge is desired, require explicit admin intent and protect the operation.
- **server/storage.ts:509** — getAllMemoriesForBackup selects all memory_entries with no tenant filter. memory_entries is tenant-scoped — this returns every tenant's memories (large cross-tenant data leak).
  - _Fix:_ Restrict backups to a single tenant at a time (require tenantId) or gate this API to authorized admin processes only. If an export across tenants is necessary, ensure it is executed only by an authorized admin path and authorization is enforced.
- **server/storage.ts:1296** — getAllDataForExport SELECTs many tenant-scoped tables (conversations, messages, memory_entries, agent_knowledge, daily_notes, tenant-specific tables, etc.) with no tenant filtering — this exports all tenants' data in one call.
  - _Fix:_ This is a high-risk cross-tenant data leak. Require a tenantId for export APIs or restrict this function to an admin-only path with explicit authorization, audit logging, and safeguards. Prefer per-tenant export endpoints rather than a global dump.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` updates implementation using only the id in the WHERE clause (.where(eq(customTools.id, tool.id))) with no tenant_id constraint. This allows cross-tenant mutation if an attacker can target an id.
  - _Fix:_ Require and apply tenant scoping on updates. Use .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantIdOrToolTenant))) and ensure the function has a verified tenantId parameter (or tool.tenantId) before performing the update.
- **server/tool-learning.ts:316** — DELETE on tenant-scoped table `custom_tools` deletes by id with no tenant_id constraint (db.delete(customTools).where(eq(customTools.id, tool.id))). This can delete another tenant's row if ids are known/guessed.
  - _Fix:_ Constrain deletes to the tenant: include tenant_id in the WHERE clause, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Pass and verify the tenantId into revertToolRow to enforce ownership before deleting.
- **server/tool-learning.ts:365** — UPDATE on tenant-scoped table `custom_tools` increments usageCount using only id in the WHERE clause (.where(eq(customTools.id, tool.id))) with no tenant_id constraint, allowing cross-tenant mutation.
  - _Fix:_ Scope usage updates by tenant as well: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Ensure the code has and uses a verified tenantId (or tool.tenantId) for all UPDATEs to tenant-scoped tables.
- **server/tools.ts:2876** — INSERT into project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without stamping tenant context, creating risk of cross-tenant linkage or failing DB constraints if tenant_id is required.
  - _Fix:_ Include tenant_id in the INSERT and set it to the caller's tenant (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${newProject.id}, ${params._conversationId}, ${tenantId}) ), or use INSERT ... SELECT ... FROM projects p WHERE p.id = ${newProject.id} AND p.tenant_id = ${tenantId} to ensure the row is only created for the verified tenant. Wrap in the same transaction as the ownership check.
- **server/tools.ts:2980** — INSERT into project_files omits tenant_id. The handler inserts a project_files row (project_id, file_name, file_path, ...) without explicitly passing tenant_id, violating the invariant that tenant-scoped INSERTs must include the caller's tenant.
  - _Fix:_ Add tenant_id to the INSERT (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) or perform INSERT ... SELECT ... FROM projects p WHERE p.id = ${params.id} AND p.tenant_id = ${tenantId} so the new row is guaranteed tied to the caller's tenant. Ensure this is executed after the project ownership check and ideally inside a transaction.
- **server/tools.ts:2994** — INSERT into project_notes omits tenant_id. The code inserts (project_id, note, author) without tenant context, risking cross-tenant contamination if project_notes is tenant-scoped.
  - _Fix:_ Stamp tenant_id on the insert (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${params.id}, ${params.note}, ${params.author || 'agent'}, ${tenantId})) or use an INSERT ... SELECT guarded by projects WHERE p.id=${params.id} AND p.tenant_id=${tenantId}. Run within the same transaction as the ownership verification.
- **server/tools.ts:3018** — INSERT into project_conversations (link_conversation path) omits tenant_id. The handler inserts a project_conversations row for a caller-supplied conversationId but the INSERT does not set tenant_id (even though the code earlier asserts the conversation belongs to the tenant).
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})) or use INSERT ... SELECT ... FROM conversations c JOIN projects p ... WHERE c.id=${params.conversationId} AND c.tenant_id=${tenantId} AND p.id=${params.id} AND p.tenant_id=${tenantId} to ensure the row is tied to the verified tenant. Prefer doing the insert in the same transaction as the ownership assertion.
- **server/tools.ts:6899** — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without setting tenant_id even though project_files is tenant-scoped and INSERTs must include tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Prefer using the Drizzle insert API with values({ tenantId: params._tenantId, ... }) or ensure tenant_id is set to the caller's tenant after verifying ownership (assertProjectInTenant) before persisting.
- **server/tools/domains/files/handlers.ts:837** — INSERT into project_files omits tenant_id — INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)
  - _Fix:_ Include the tenant_id column and pass the caller's tenant (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projId}, ${fileName}, ...)) so every row is explicitly scoped to the tenant. Ensure the value comes from the trusted ctx.tenantId and not user params.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys executed without a tenant_id filter; this returns provider API key rows across all tenants (potential exposure of sensitive secrets)
  - _Fix:_ Constrain the query to the caller tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise restrict this code path to an audited, explicit owner-only admin API that clearly documents and authorizes cross-tenant access. Do NOT query provider_keys across tenants from a normal tool flow; if the owner truly needs global visibility, implement a safe admin-only API with explicit audit/logging and minimal result exposure (e.g. counts or redacted rows).
- **server/tools/domains/strategic-memory/handlers.ts:92** — UPDATE against tenant-scoped table memory_entries uses WHERE id = ${id} with NO tenant_id constraint, allowing cross-tenant row updates if an id is abused or races occur.
  - _Fix:_ Include the tenant filter in the UPDATE (and optionally persona) to enforce application-level isolation. e.g. UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} (and/or AND persona_id = ${personaId}). Or perform the UPDATE via a storage-layer helper / Drizzle .where(eq(table.tenantId, tid)) to guarantee tenant scoping.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE against tenant-scoped table memory_entries uses WHERE id = ANY(${ids}) with NO tenant_id constraint, allowing last_accessed/access_count bump for rows in other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE memory_entries SET ... WHERE id = ANY(${ids}) AND tenant_id = ${tid}. Prefer using Drizzle .where(eq(table.tenantId, tid)) or a storage helper that enforces tenant ownership to avoid accidental cross-tenant writes.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE against tenant-scoped table memory_entries uses WHERE id = ${id} with NO tenant_id constraint (recordStrategicWin path), permitting cross-tenant modification of memory rows.
  - _Fix:_ Constrain the UPDATE by tenant (and persona if appropriate): UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tid} (and/or AND persona_id = ${personaId}). Alternatively, perform the update through a vetted storage API or Drizzle .where(eq(table.tenantId, tid)) to ensure tenant isolation.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is executed without any tenant_id constraint (reads id + content::jsonb for all matching title rows across tenants). This leaks cross-tenant pairing rows and their JSON content and violates the tenant-isolation-by-SQL invariant.
  - _Fix:_ Restrict the query to the caller's tenant (e.g. add AND tenant_id = ${expectedTenantId}) or change the data model to include an ownership/validated flag so you can safely lookup only rows validated for the caller. If the intention is to detect cross-tenant conflicts, perform a minimized, audited check that does not return full content (or run this check under an operator/admin-only path) and ensure any reads are explicitly authorized and logged.
- **server/wiring-invariants.ts:260** — SELECT count(*)::int AS n FROM messages WHERE created_at > ... runs without a tenant_id constraint — messages are typically tenant-scoped and contain sensitive user data, so this reads across all tenants.
  - _Fix:_ Restrict this query to a single tenant by adding WHERE tenant_id = :tenantId, or execute the check per-tenant. If an aggregate across all tenants is truly required for admin telemetry, ensure the routine is restricted to super-admin context and clearly audited; prefer per-tenant aggregation to maintain isolation guarantees.
- **server/workflow-templates.ts:63** — Calls executeTool('produce_video', videoParams) with project_id: 14 (hard-coded project foreign id) and passes tenant context as _tenantId (underscore key). There is no verification that project_id=14 belongs to context.tenantId and using a hard-coded/unchecked project id can cause writes to or association with another tenant's project.
  - _Fix:_ Do not hard-code foreign ids. Resolve the project within the caller's tenant (e.g. lookup project by slug/name constrained by tenant_id and ensure ownership) or accept project_id from the caller and validate ownership before passing it to the tool. Pass tenant context under the expected key (e.g. tenantId) and ensure downstream code uses it to include WHERE tenant_id = :tenantId on any INSERT/UPDATE/SELECT. If the tool needs to act across tenants, require an explicit, audited admin flow.

### HIGH (170)

- **server/ab-optimizer.ts:110** — runDueAbExperiments performs a SELECT on the tenant-scoped `experiments` table without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This can read rows from other tenants.
  - _Fix:_ Scope the query to a tenant (e.g. pass a tenantId into runDueAbExperiments and add WHERE tenant_id = ${tenantId}) or iterate per-tenant. Never issue cross-tenant SELECTs on tenant-scoped tables.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting issues a SELECT on the tenant-scoped `ai_insights` table without a tenant_id filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ...). This reads insights across all tenants.
  - _Fix:_ Either run the sweep per-tenant (accept a tenantId parameter) or add AND tenant_id = ${tenantId} to the WHERE clause. If you must sweep all tenants, use an explicit operator-only path that is reviewed for multi-tenant implications and ensure callers are privileged.
- **server/agentic-engines.ts:347** — gatherOperationalData queries heartbeat_logs without scoping by tenant (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads logs across tenants and can leak tenant data into the operational analysis.
  - _Fix:_ Add tenant scoping to heartbeat_logs queries (e.g. WHERE tenant_id = ${tenantId} AND ...). If the intent is a global system metric, make that explicit and document why it's safe; otherwise restrict to the caller's tenant context.
- **server/agentic-engines.ts:451** — runPredictiveEngine selects recent heartbeat_logs (SELECT ... FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10) with no tenant_id filter, reading logs from all tenants.
  - _Fix:_ Restrict heartbeat_logs queries to the tenant in question (WHERE tenant_id = ${tenantId}) or otherwise ensure the call is run in a privileged, audited operator-only context that intentionally aggregates across tenants.
- **server/agentic-engines.ts:519** — runOptimizationEngine queries heartbeat_logs in a non-tenant-scoped way (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This reads aggregated heartbeat data across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} in the WHERE clause (or run the aggregation per tenant). If an operator-level global aggregation is required, ensure it runs in a privileged job with explicit approval and isolation guarantees.
- **server/agentic-engines.ts:533** — runOptimizationEngine queries heartbeat_tasks with no tenant filter (SELECT name, cron_expression, enabled, last_run_at, run_all FROM heartbeat_tasks WHERE enabled = true ORDER BY name). heartbeat_tasks is tenant-scoped elsewhere, so this likely reads tasks for all tenants.
  - _Fix:_ Restrict the query with WHERE tenant_id = ${tenantId} (or otherwise scope to the intended tenant). Avoid issuing tenant-scoped reads that lack tenant_id constraints.
- **server/agentic/department-budgets.ts:143** — runBudgetEnforcement() executes `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` against a tenant-scoped table with no tenant_id constraint. This performs an unfiltered cross-tenant read on a tenant-scoped table, violating the codebase invariant that every read/write must be constrained to the caller's tenant.
  - _Fix:_ Avoid unfiltered SELECTs on tenant-scoped tables. If this must operate per-tenant, change the function to accept an explicit tenantId and add `WHERE tenant_id = ${tenantId}` (or otherwise bind tenant_id as a parameter). If a global sweep is genuinely required, move it into a clearly-audited admin-only path that cannot be invoked from tenant contexts and document/guard it accordingly; do NOT perform ad-hoc unfiltered reads from code reachable by non-admin callers.
- **server/agentic/resume-worker.ts:66** — SELECT query loads agent_runs / agent_approvals rows without constraining by tenant_id — cross-tenant read across all tenants.
  - _Fix:_ Filter the query to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId}) or iterate per-tenant with an explicit tenantId parameter. Avoid global, un-scoped reads of tenant-scoped tables unless running under a documented, audited admin flow with explicit safeguards.
- **server/agentic/runs.ts:70** — completeRun later selects the run by id (db.select().from(agentRuns).where(eq(agentRuns.id, runId))). That SELECT lacks tenant_id filtering and can leak another tenant's run data to the caller.
  - _Fix:_ Restrict the SELECT by tenant (where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))) or fetch the run in the same call that has tenant context. Avoid selecting tenant-scoped rows without verifying tenant ownership.
- **server/auto-memorize.ts:163** — runAutoMemorize pulls messages across all tenants in one query (SELECT ... FROM messages LEFT JOIN conversations ... WHERE created_at > ... AND role IN (...)) with no tenant_id restriction, then partitions results in-memory. Reading many tenants' rows in a single, unconstrained SQL read risks leaking or mixing tenant data at the DB layer and bypasses the required per-tenant SQL guard.
  - _Fix:_ Avoid a single unconstrained SELECT over tenant-scoped messages. Instead iterate per-tenant (SELECT messages WHERE tenant_id = ${tenantId} ... / use per-tenant watermark) or otherwise restrict the SQL with an explicit tenant_id predicate. Ensure the DB read for each LLM prompt is limited to one tenant at the SQL level.
- **server/auto-transcript.ts:128** — Backfill SELECT scans conversations without scoping by tenant (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c ...). This reads a tenant-scoped table across all tenants.
  - _Fix:_ Run the backfill per-tenant or add an explicit WHERE c.tenant_id = ${tenantId} (and iterate tenants under an authorized admin path). Ensure only an authorized system account may enumerate across tenants and log/authorize the operation.
- **server/auto-transcript.ts:153** — Query selecting linked project_conversations/conversations (SELECT DISTINCT pc.conversation_id, pc.project_id, c.tenant_id ...) has no tenant restriction. This reads tenant-scoped rows across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. WHERE c.tenant_id = ${tenantId}) or iterate per-tenant. If this must run as a global admin job, gate it behind an audited admin path and do not expose results to request contexts without authorization.
- **server/auto-transcript.ts:171** — SELECT to find missing links (SELECT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...) has no tenant filter and reads tenant-scoped rows across tenants.
  - _Fix:_ Limit this SELECT per-tenant (WHERE c.tenant_id = ${tenantId}) or run the operation under an authorized global admin flow. Avoid global scans in code running with request-level privileges.
- **server/business-tools.ts:74** — createInvoice inserts an invoice with a caller-supplied customer_id without first verifying that the referenced customer belongs to the caller's tenant.
  - _Fix:_ Perform an ownership check before storing the foreign id. Example: SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1 and reject if not found (same pattern as createContract). Alternatively, validate/resolve the customer server-side and only then include customer_id in the INSERT.
- **server/business-tools.ts:84** — INSERT into invoice_items omits tenant_id — writing to a tenant-scoped table without setting tenant_id breaks the app-level isolation invariant (or may allow cross-tenant linkage if invoice_items is tenant-scoped).
  - _Fix:_ Include tenant_id in the invoice_items INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)), or otherwise ensure invoice_items is not tenant-scoped. Also consider validating that the parent invoice belongs to the tenant before inserting, or derive tenant_id from the invoice in a single statement to guarantee correct ownership.
- **server/capability-review.ts:490** — UPDATE against tenant-scoped table `capability_reviews` performs a write with no tenant constraint (WHERE id = ${pending.reviewId} AND reused = false). This allows a caller/process that can trigger markReviewReused to flip rows belonging to other tenants (tenant isolation not enforced).
  - _Fix:_ Include the tenant context in the UPDATE WHERE clause (e.g. AND tenant_id = ${tenantId}). Persist tenantId when creating the pendingReuse entry (pendingReuseByConversation) or change markReviewReused to accept/derive the tenantId, then perform a parameterized UPDATE that includes both id and tenant_id (or conversation_id) to ensure the row belongs to the caller's tenant. Example fixes: (1) store tenantId in pendingReuseByConversation in recordCapabilityReview and then use it here: WHERE id = ${pending.reviewId} AND tenant_id = ${pending.tenantId} AND reused = false; or (2) change markReviewReused signature to accept tenantId and use WHERE id = ${pending.reviewId} AND tenant_id = ${tenantId} AND reused = false. Always use parameterized sql (drizzle sql`` interpolation) rather than string-building.
- **server/chat-engine.ts:572** — SELECT from tenant-scoped table `project_files` without an explicit tenant filter (WHERE tenant_id = ...). The query uses only project_id, relying on prior verification of the project in JS instead of enforcing tenant scoping in the SQL.
  - _Fix:_ Include tenant scoping in the query (e.g. ... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join via projects and assert p.tenant_id = ${tenantId} in the SQL. Always pass and use the tenantId at the DB level to prevent accidental IDORs.
- **server/chat-engine.ts:583** — SELECT from tenant-scoped table `project_notes` without an explicit tenant filter (WHERE tenant_id = ...). The query filters only by project_id.
  - _Fix:_ Add tenant scoping to the query (e.g. SELECT ... FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join to projects with a tenant_id check. Enforce tenant_id in every DB read on tenant-scoped tables.
- **server/chat-engine.ts:632** — SELECT from tenant-scoped table `compaction_archives` by conversation_id only (WHERE conversation_id = ${c.id}) with no tenant_id constraint. This can return rows for conversations belonging to other tenants if conversation ids are guessable or passed around.
  - _Fix:_ Constrain the query by tenant (e.g. ... WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or join to conversations/projects that are tenant-scoped to enforce ownership in SQL. Never rely solely on upstream JS checks for tenant isolation.
- **server/chat-engine.ts:650** — SELECT from tenant-scoped table `messages` using only conversation_id (WHERE conversation_id = ${c.id}) with no tenant_id constraint. The messages table is tenant-scoped and must be filtered by tenant at the SQL level.
  - _Fix:_ Add tenant scoping to the messages query (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or perform a JOIN to conversations with a tenant filter. Ensure all message reads include tenant_id in the SQL WHERE clause.
- **server/chat-engine.ts:767** — Correlated subquery selects from `messages` (SELECT content FROM messages WHERE conversation_id = c.id ...) inside a larger query but the subquery lacks an explicit tenant_id condition. Even though the outer query filters conversations by tenant, the messages subquery itself has no tenant_id WHERE clause.
  - _Fix:_ Add tenant scoping inside correlated subqueries (e.g. SELECT content FROM messages WHERE conversation_id = c.id AND tenant_id = ${tenantId}) or rewrite the query to JOIN messages with an explicit tenant_id predicate. Enforce tenant_id in every SELECT including correlated subqueries.
- **server/chat-engine.ts:772** — Correlated subquery selects from `compaction_archives` (SELECT summary FROM compaction_archives WHERE conversation_id = c.id ...) without an explicit tenant_id constraint in the subquery.
  - _Fix:_ Add tenant scoping in the subquery (e.g. WHERE conversation_id = c.id AND tenant_id = ${tenantId}) or JOIN compaction_archives with conversations/projects ensuring tenant_id matches. Always include tenant_id in SQL reads from tenant-scoped tables, including in lateral/subqueries.
- **server/chat-engine.ts:831** — Scalar subquery counts `project_files` with (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) but lacks tenant scoping. project_files appears tenant-scoped and should not be read without tenant_id in SQL.
  - _Fix:_ Include tenant_id in the subquery (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or rewrite as a JOIN that enforces p.tenant_id = ${tenantId}. Ensure all subqueries enforce tenant_id at the DB level.
- **server/chat-engine.ts:4331** — storage.attachCitations(...) is invoked without an explicit tenant id. If attachCitations performs inserts into a tenant-scoped table it will not receive tenant_id here (the call uses only message id + citations).
  - _Fix:_ Pass the tenant id explicitly (e.g. tenantIdForScope) to storage.attachCitations or change its API to require tenantId. Alternatively, have attachCitations validate the message belongs to the caller's tenant (SELECT message WHERE id=... AND tenant_id=...) before any INSERTs and ensure every INSERT sets tenant_id.
- **server/chat-engine.ts:3697** — Background call markReviewReused(conversationId, toolName) is invoked without any tenant context. If markReviewReused reads/writes tenant-scoped tables using conversationId, ownership/isolation is not enforced here.
  - _Fix:_ Pass tenantId to markReviewReused (or otherwise propagate tenant context) and/or verify conversation ownership before calling. Ensure the implementation enforces tenant_id filtering on any SELECT/UPDATE/DELETE and sets tenant_id on INSERTs.
- **server/chat-engine.ts:4016** — recordToolCallForStuckDetection(conversationId, ...) is called without tenant information. If it persists diagnostics tied to a conversation, it may perform tenant-scoped writes/reads without tenant_id constraints.
  - _Fix:_ Include tenantId in the call or validate conversation → tenant ownership inside recordToolCallForStuckDetection (e.g. lookup conversation with WHERE tenant_id = <caller> before any mutation). Ensure all INSERTs set tenant_id explicitly.
- **server/chat-engine.ts:4022** — postDiagnosticReport([stuckPattern]) is invoked with no tenant context. If postDiagnosticReport writes tenant-scoped diagnostic rows it has no tenant_id to enforce isolation.
  - _Fix:_ Change postDiagnosticReport to accept tenantId (or ensure stuckPattern includes tenant) and enforce tenant_id on any DB writes/queries. Always constrain queries/updates with tenant_id or validate ownership first.
- **server/chat-engine.ts:4004** — recordToolExecution(conversationId, toolName, ...) is called without passing tenantId. If recordToolExecution writes/read conversation-scoped or tenant-scoped tables, it may operate without tenant isolation checks.
  - _Fix:_ Pass tenantId when calling recordToolExecution or ensure recordToolExecution validates the conversation belongs to the caller's tenant before any DB access. All INSERTs must include tenant_id and all SELECT/UPDATE/DELETE must filter by tenant_id.
- **server/commitment-drafter.ts:234** — Unscoped UPDATE against tenant-scoped table `commitments`: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` lacks tenant_id constraint and can modify rows across tenants.
  - _Fix:_ Include the tenant filter on the UPDATE to enforce tenant isolation. For example: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}` (or otherwise validate/derive the tenant before executing the statement). Ensure every UPDATE/DELETE/SELECT against `commitments` is scoped to the tenant.
- **server/data-protection.ts:111** — SELECT of expired soft-deleted conversations runs without tenant scoping. permanentlyPurgeSoftDeleted selects conversations with deleted_at < cutoff across all tenants.
  - _Fix:_ Scope the purge query to a tenant (pass a tenantId) or perform this operation under an explicit privileged/admin context after additional checks. Avoid running cross-tenant SELECTs unless explicitly gated and audited.
- **server/data-protection.ts:176** — SELECT of messages in backupConversationToDrive lacks tenant scoping. After verifying the conversation belongs to the tenant, the code queries messages WHERE conversation_id = ${conversationId} without also constraining tenant_id.
  - _Fix:_ Include AND tenant_id = ${tenantId} on message and archive SELECTs, or otherwise ensure every read against tenant-scoped tables has an explicit tenant_id predicate.
- **server/data-protection.ts:183** — SELECT of compaction_archives in backupConversationToDrive omits tenant scoping. The archive rows are read by conversation_id only, with no tenant_id predicate.
  - _Fix:_ Add tenant_id = ${tenantId} to the SELECT (and any subsequent operations), or otherwise validate ownership before reading archives to prevent cross-tenant exposure.
- **server/data-protection.ts:318** — SELECT from project_notes is done by project_id only and does not include an explicit tenant_id filter. The code iterates tenant-owned projects but issues queries against project_notes without tenant predicate.
  - _Fix:_ Constrain project_notes (and project_files) queries to the tenant as well (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or join through the projects table and assert p.tenant_id = ${tenantId} to ensure ownership.
- **server/data-protection.ts:329** — SELECT from project_files by project_id omits tenant scoping. The code reads project_files WHERE project_id = ${proj.id} with no tenant predicate.
  - _Fix:_ Require tenant scoping on project_files queries (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or validate via a join to ensure the files belong to a project owned by the tenant.
- **server/data-protection.ts:362** — Message count query in backupTenantDataToDrive uses only conversation_id. It runs SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} without tenant scoping, even though conv rows were tenant-filtered earlier.
  - _Fix:_ Add tenant_id = ${tenantId} to the COUNT query (or otherwise assert message ownership) so counts cannot leak or reflect other tenants' messages.
- **server/data-protection.ts:401** — Subquery in getDeletedConversations counts messages without tenant scoping. The SELECT includes (SELECT COUNT(*) FROM messages WHERE conversation_id = conversations.id) which will count messages irrespective of tenant_id.
  - _Fix:_ Constrain the subquery to the tenant (e.g. (SELECT COUNT(*) FROM messages WHERE conversation_id = conversations.id AND tenant_id = ${tenantId})). Ensure every nested query against tenant-scoped tables includes the tenant predicate.
- **server/delivery-pipeline.ts:188** — updateDeliveryLog() performs UPDATE on tenant-scoped delivery_logs using only delivery id (WHERE eq(delivery_logs.id, id)) with NO tenant_id constraint.
  - _Fix:_ Require and pass the caller's tenantId into updateDeliveryLog (e.g. updateDeliveryLog(id, tenantId, updates)) and include it in the WHERE clause: WHERE id = <id> AND tenant_id = <tenantId>. Failing that, only call this helper from code that has already validated ownership and make the function assert that tenant to avoid accidental cross-tenant writes.
- **server/delivery-pipeline.ts:683** — SELECT of delivery_logs by stripePaymentId is executed with NO tenant_id constraint (db.select().from(deliveryLogs).where(eq(deliveryLogs.stripePaymentId, req.stripePaymentId))). This can match rows from other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE stripe_payment_id = <id> AND tenant_id = <tenantId>) or require the caller to supply tenantId. If this query is intended to be global, document that it is an explicit admin-level cross-tenant operation and restrict caller privileges accordingly.
- **server/delivery-pipeline.ts:720** — Transactional UPDATE that clears stripePaymentId uses WHERE eq(delivery_logs.id, prior.id) with NO tenant_id constraint, allowing cross-tenant modification of delivery_logs.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = <tenantId>) or pass tenantId into this code path and validate that the prior row belongs to the expected tenant before performing the update.
- **server/delivery-pipeline.ts:1013** — getDeliveryByStripePayment(paymentId) SELECTs delivery_logs WHERE stripe_payment_id = ${paymentId} with NO tenant filter, returning rows across tenants.
  - _Fix:_ Add an explicit tenantId parameter and filter by tenant (WHERE stripe_payment_id = <id> AND tenant_id = <tenantId>) or make callers perform an ownership/tenant check before calling this helper.
- **server/doc-collections.ts:165** — ADD DOCUMENT: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} is executed with NO tenant_id constraint. collection_id alone doesn't enforce tenant isolation (no FK/RLS guarantee).
  - _Fix:_ Include tenant scoping in the DELETE (AND tenant_id = ${tenantId}) or first validate that the collection_id belongs to the provided tenant and then include tenant_id in the DELETE to prevent deleting other tenants' chunks.
- **server/doc-collections.ts:294** — generateCollectionEmbeddings updates doc_chunks with: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} but does NOT restrict by tenant_id. This permits updating a chunk row without verifying tenant ownership.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or fetch the row by id+tenant before updating to ensure the chunk belongs to the expected tenant.
- **server/email.ts:409** — backfillEmptyBodies(): SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id runs without tenant_id filtering and iterates rows across tenants. Subsequent UPDATE uses WHERE id = ${row.id} (no tenant_id), so writes may touch tenant-scoped data without tenant constraint.
  - _Fix:_ Run the backfill per-tenant (or include tenant_id in the SELECT and subsequent UPDATE). Ensure each UPDATE includes AND tenant_id = ${tenantId} (or inbox_id) so writes are always constrained to the correct tenant.
- **server/embeddings.ts:688** — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter. This reads tenant-scoped rows across tenants.
  - _Fix:_ Run backfills per-tenant (accept a tenantId argument) or otherwise include AND tenant_id = ${tenantId} in the WHERE clause. Verify the backfill is intentionally global and authorized before leaving it un-scoped.
- **server/embeddings.ts:716** — SELECT FROM memory_entries in backfillEmbeddingVecs has no tenant_id filter. This reads tenant-scoped rows across tenants.
  - _Fix:_ Run this maintenance per-tenant (accept a tenantId) or include AND tenant_id = ${tenantId} in the WHERE clause to ensure tenant isolation when reading rows.
- **server/embeddings.ts:732** — SELECT FROM agent_knowledge in backfillEmbeddingVecs has no tenant_id filter. This reads tenant-scoped rows across tenants.
  - _Fix:_ Scope the SELECT to a tenant (AND tenant_id = ${tenantId}) or run the backfill per-tenant with a verified tenant context.
- **server/felix-loop.ts:676** — UPDATE felix_loop_runs sets ended_at/context_summary/etc WHERE id = ${runId} but does not include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) to ensure only the caller's tenant row is modified, or fetch-and-verify the row's tenant before updating.
- **server/felix-loop.ts:690** — Error-path UPDATE on felix_loop_runs (set ended_at,error WHERE id=${runId}) lacks tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId}) when updating run rows on error to avoid cross-tenant modifications.
- **server/firecrawl.ts:184** — db.update(scrapedPages).where(eq(scrapedPages.id, existing[0].id)) updates scraped_pages by id only — no tenant_id constraint on the UPDATE, despite the table being tenant-scoped.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise verify ownership before writing.
- **server/firecrawl.ts:307** — Within crawl loop, updating scraped_pages uses .where(eq(scrapedPages.id, existing[0].id)) without tenant scoping, allowing potential cross-tenant writes if an id is reused or manipulated.
  - _Fix:_ Add tenant filtering to the UPDATE (AND eq(scrapedPages.tenantId, tenantId)) or re-query/validate ownership before performing the update.
- **server/google-drive.ts:560** — ensureProjectFolder updates projects (.where(eq(projects.id, projectId))) without constraining the UPDATE to the tenant — write occurs on a tenant-scoped table without tenant_id in WHERE.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise assert ownership prior to performing the update.
- **server/health-audit.ts:314** — SELECT FROM code_proposals queries proposals across all tenants (no WHERE tenant_id = ...). This can read tenant-scoped proposal rows globally.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) or explicitly run this scan in a privileged/admin context only and make that explicit. If the intent is per-tenant TTL, iterate tenants and run tenant-scoped queries to avoid cross-tenant reads.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks queries enabled/stuck tasks with no tenant constraint (no WHERE tenant_id = ...). Heartbeat tasks are typically tenant-scoped and this read can expose tasks across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or otherwise scope to an explicit admin context that is intentionally global). If the audit should operate per-tenant, iterate tenants and run tenant-scoped queries; if it must be global, make the admin-only nature explicit and restrict who can invoke it.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table 'research_schedules' without constraining by tenant_id (no WHERE tenant_id = ...) — returns rows across all tenants.
  - _Fix:_ Filter the query by tenant (or explicitly by ADMIN_TENANT_ID when operating as system/admin). For example, add WHERE tenant_id = ${tenantId} (or pass tenantId via assertTenantContext) or iterate per-tenant and run tenant-scoped queries. Avoid broad SELECT * FROM research_schedules without tenant scope.
- **server/heartbeat.ts:101** — UPDATE against tenant-scoped table 'research_schedules' uses WHERE id = ${sched.id} but lacks an explicit tenant_id constraint. Without RLS this can modify another tenant's row if id is known.
  - _Fix:_ Include tenant scoping in UPDATEs, e.g. UPDATE research_schedules SET ... WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id} (or assert/derive the expected tenantId and use it in the WHERE clause). Ensure every write to tenant-scoped tables also checks tenant_id.
- **server/heartbeat.ts:106** — UPDATE against tenant-scoped table 'research_schedules' (last_run_at/next_run_at) without tenant_id in WHERE clause.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage helper that enforces tenant scoping. Do not rely solely on id for isolation.
- **server/heartbeat.ts:111** — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Constrain by tenant_id in the UPDATE: WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id} (or assert tenant context before updating). Ensure all UPDATE/DELETE statements on tenant-scoped tables include tenant filtering.
- **server/heartbeat.ts:119** — UPDATE research_schedules in error-recovery path uses WHERE id = ${sched.id} with no tenant_id constraint.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${sched.tenant_id}) or call a storage API that enforces tenant checks. Avoid updating rows solely by id when tenant isolation is required.
- **server/heartbeat.ts:658** — SELECT * FROM event_log WHERE status = 'pending' — no tenant_id constraint; reads pending events across all tenants.
  - _Fix:_ Scope the select by tenant (e.g. WHERE status = 'pending' AND tenant_id = ${tenantId}) or iterate over tenants and query per-tenant. If system needs to process events across tenants, do so explicitly and safely (e.g. assert admin context and include tenant_id in subsequent updates).
- **server/heartbeat.ts:260** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. This updates heartbeat_tasks without verifying tenant ownership of the row.
  - _Fix:_ Include tenant scoping on UPDATEs: WHERE id = ${t.id} AND tenant_id = ${t.tenant_id} (or use the asserted ADMIN_TENANT_ID). Prefer storage helper functions that require/validate tenant context before mutating tenant-scoped rows.
- **server/heartbeat.ts:3208** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint on a tenant-scoped table (occurs twice in file).
  - _Fix:_ Constrain the update by tenant_id (e.g. WHERE id = ${newTask.id} AND tenant_id = ${newTask.tenantId || tenantId}). Alternatively, use the storage layer API to update approval_status while enforcing tenant checks.
- **server/heartbeat.ts:2833** — Call to storage.getHeartbeatTasks() without passing tenantId (in processDelegations). This reads heartbeat tasks without an explicit tenant constraint, which may return tasks from other tenants.
  - _Fix:_ Use a tenant-scoped variant of the storage call (pass tenantId) or assert tenant context before calling. Any handler that inspects or counts tenant-scoped rows must request only that tenant's rows or otherwise validate row ownership before acting.
- **server/lib/action-ledger.ts:268** — SELECT from tenant-scoped table `action_attempts` with no tenant_id filter (listUnknownAttemptsAllTenants returns rows across all tenants).
  - _Fix:_ If this cross-tenant read is intentional for an out-of-band reconciler, ensure it runs in a tightly-controlled admin context and that any subsequent writes are performed with per-row tenant scoping. Otherwise add WHERE tenant_id = ${tenantId} (or provide a dedicated admin-only path and document the privilege). Prefer explicit per-tenant queries or an audited admin-only job.
- **server/lib/agent-trace.ts:124** — UPDATE on tenant-scoped table `agent_trace_spans` performed without tenant_id in the WHERE clause (persistSpanClose uses WHERE span_id = ${spanId} only).
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Persist the tenantId when opening the span (store it in SpanContext) so the close can reliably constrain the UPDATE to the owning tenant.
- **server/lib/bwb-job-progress.ts:144** — UPDATE on tenant-scoped table `video_jobs` uses scope(jobId) which may omit tenant_id when env/threaded tenant info is missing — writes may affect rows across tenants.
  - _Fix:_ Require an explicit tenantId (fail closed if missing) or always include tenant_id in the WHERE clause. Avoid relying solely on unguessable jobId for isolation; pass tenantId through callsites and use eq(videoJobs.tenantId, tenantId) in the WHERE so a missing environment variable can't cause cross-tenant writes.
- **server/lib/bwb-job-progress.ts:169** — UPDATE on tenant-scoped table `video_jobs` uses scope(jobId) which can omit tenant_id (updateBwbChapters). This allows cross-tenant writes when BWB_TENANT_ID is not set.
  - _Fix:_ Require and use an explicit tenantId when building the WHERE clause (scope(jobId, { tenantId })) and fail the operation when tenantId is unavailable, or perform a per-tenant check before the write to ensure tenant-scoped WHERE is present.
- **server/lib/bwb-job-progress.ts:200** — SELECT from tenant-scoped table `video_jobs` uses scope(jobId) which may omit tenant_id (completeBwbJob reads chapters without guaranteed tenant constraint).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the SELECT predicate or require an explicit tenantId argument for the function so reads never cross tenants. If this is intended for admin cross-tenant tooling, gate it behind an admin-only path and document/authorize it.
- **server/lib/bwb-job-progress.ts:211** — UPDATE on tenant-scoped table `video_jobs` uses scope(jobId) (completeBwbJob) and may run without tenant_id, allowing cross-tenant writes.
  - _Fix:_ Ensure the UPDATE includes eq(videoJobs.tenantId, tenantId). If tenantId may be unknown, change the API to require tenantId, or fetch the row and perform a per-row, tenant-scoped update using the discovered tenant_id.
- **server/lib/bwb-job-progress.ts:230** — UPDATE on tenant-scoped table `video_jobs` uses scope(jobId) and may omit tenant_id (bumpBwbHeartbeat).
  - _Fix:_ Require tenant context and include tenant_id in the WHERE. Do not rely solely on jobId for isolation; pass and assert tenantId so the WHERE is always eq(videoJobs.tenantId, tenantId) AND eq(videoJobs.jobId, jobId).
- **server/lib/bwb-job-progress.ts:254** — UPDATE on tenant-scoped table `video_jobs` uses scope(jobId) and may omit tenant_id (failBwbJob). This can mark rows belonging to other tenants as failed when BWB_TENANT_ID is missing.
  - _Fix:_ Require and pass tenantId into failBwbJob and include eq(videoJobs.tenantId, tenantId) in the WHERE. If the env-threading is relied upon, fail fast and log when tenant info is absent rather than performing unscoped writes.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into project_files does not set tenant_id. Platform invariant requires tenant_id to be provided on tenant-scoped INSERTs (no default); omitting it can misattribute rows or violate DB constraints.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${newProjectId}, ...)). Also defensively validate that newProjectId belongs to the expected tenant (join to projects or assert ownership) before inserting.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into project_notes does not set tenant_id. Every INSERT into tenant-scoped tables must explicitly supply tenant_id — columns are NOT NULL with no default.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, ...) VALUES (${tenantId}, ${newProjectId}, ${n.note}, ...)). Also verify the project_id's ownership matches tenantId before writing.
- **server/lib/scheduled-post-runner.ts:60** — SELECT/UPDATE against tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE selects due rows across all tenants). This updates rows across tenants from a single system-scoped runner.
  - _Fix:_ If this runner must operate cross-tenant, make that explicit (doc & audit) and ensure the operation is only callable by a hardened admin context. Otherwise restrict the initial SELECT/CTE to a specific tenant_id or iterate per-tenant with an explicit tenant filter. At minimum, add server-side authorization checks and logging/audit for cross-tenant updates to reduce blast radius.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} executed without scoping by tenant_id. This writes tenant-scoped rows without verifying the tenant, risking cross-tenant state changes if row ids are misread or attacker-controlled.
  - _Fix:_ Include AND tenant_id = ${row.tenant_id} (or the caller's tenant) in the WHERE clause for UPDATEs that modify tenant-scoped rows, or otherwise ensure the code only operates in an explicit admin context with audited privileges. Prefer two-column WHERE (id + tenant_id) for all UPDATE/DELETE against tenant-scoped tables.
- **server/lib/scheduled-post-runner.ts:192** — Error-path UPDATE scheduled_posts uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id, allowing potential cross-tenant updates in exception handling paths.
  - _Fix:_ Add tenant_id = ${row.tenant_id} to the WHERE clause (or restrict by caller tenant) when updating tenant-scoped tables in error/retry paths. Always use id + tenant_id in WHERE for mutating statements on tenant-scoped tables.
- **server/memory-graph.ts:125** — UPDATE on tenant-scoped table `memory_categories` (refreshCategoryCount) omits any tenant_id constraint in the WHERE clause, allowing the category_count to be modified across tenants.
  - _Fix:_ Require and pass the tenantId to refreshCategoryCount and include an explicit tenant filter in the UPDATE (e.g. WHERE id = ${categoryId} AND tenant_id = ${tenantId}). Alternatively, perform the UPDATE inside a tenant-scoped transaction or verify the category row belongs to the caller's tenant before updating.
- **server/memory-graph.ts:66** — UPDATE in classifyMemoryCategory increments memory_categories.memory_count (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}) without constraining by tenant_id — allows modifying another tenant's category counts.
  - _Fix:_ Add the tenant filter to the UPDATE (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}), or otherwise prove/verify the category belongs to the expected tenant before changing counts. Prefer passing tenantId into this helper rather than relying on implicit trust.
- **server/memory-graph.ts:181** — SELECT existence check against `memory_links` (SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ...) has no tenant constraint — can detect or act upon links across tenants.
  - _Fix:_ Constrain the existence check to the tenant (e.g. add AND tenant_id = ${tenantId}) or join to memory_entries to assert both endpoints belong to the expected tenant. Ensure every SELECT/UPDATE/DELETE on tenant-scoped tables includes tenant_id in the WHERE clause.
- **server/minerva-planner.ts:373** — Unscoped SELECT on plans: `SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}` reads plan data without tenant_id filtering, enabling cross-tenant information disclosure (IDOR).
  - _Fix:_ Include `AND tenant_id = ${tenantId}` in the SELECT or require/accept a tenantId parameter and validate caller ownership before reading. If this code path is intended to be internal, make that explicit and non-exported; otherwise enforce tenant scoping.
- **server/minerva-planner.ts:390** — Unscoped SELECT for child plan discovery: `SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ...` lacks a tenant filter and can return rows from other tenants.
  - _Fix:_ Restrict the query by tenant (e.g. `WHERE parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}`) or otherwise ensure the call is provided a tenantId and ownership has been validated prior to running this query.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' without tenant_id filter — reads rows across all tenants.
  - _Fix:_ Add a tenant_id filter when reading tenant-scoped oauth_subscriptions, or if the intent is to enumerate tenants for a startup reconciliation, explicitly fetch tenant IDs from a tenants table and operate per-tenant with proper admin authorization. Avoid broad unscoped selects on tenant-scoped tables.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh issues a SELECT across oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter (iterates all tenants). This reads tenant-scoped rows globally and drives per-tenant refresh operations.
  - _Fix:_ If this background task must touch all tenants, make the scope explicit and run under a privileged/admin context, or fetch a vetted list of tenant IDs and then perform per-tenant scoped queries/updates (each with WHERE tenant_id = ...). Avoid unscoped reads that can leak or modify tenant data.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — no tenant_id constraint; iterates all tenant rows without per-tenant scoping.
  - _Fix:_ As above, either restrict to a single tenant or explicitly enumerate tenants via a safe tenants list and perform per-tenant scoped operations. Ensure any mass-refresh logic is gated to an admin-only path and minimizes exposure of tenant-scoped secrets.
- **server/pace-control.ts:85** — checkPace queries heartbeat_logs with no tenant_id filter (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND <laneFilter>) — heartbeat_logs is tenant-scoped but this read is unscoped, allowing cross-tenant aggregation.
  - _Fix:_ Require a tenant_id parameter and include WHERE tenant_id = ${tenantId} in the query. If this is intended to be a global metric, move it to a global admin-only path or aggregate into a dedicated global table. Never run unscoped queries against tenant-scoped tables from regular code paths.
- **server/pace-control.ts:140** — getPaceSnapshot performs unscoped SELECTs against heartbeat_logs (COUNT and persona aggregation) without filtering by tenant_id, exposing usage data across tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to these queries (and accept tenantId as an argument). If an operator-level cross-tenant snapshot is required, implement an explicitly-administered routine with documented permissions or a separate global telemetry table.
- **server/pace-control.ts:147** — getPaceSnapshot persona aggregation query SELECT ... FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name has no tenant_id filter — returns persona counts across all tenants.
  - _Fix:_ Include tenant_id filtering in the GROUP BY query (WHERE tenant_id = ${tenantId} AND created_at > ...). Require the caller to provide/authorize the tenant context or run this only from an admin-only diagnostics path.
- **server/process-governor.ts:121** — Subqueries counting heartbeat_logs (successes/failures) in task_failure_rate lack tenant_id constraint (reads from tenant-scoped heartbeat_logs without scoping by tenant).
  - _Fix:_ Add tenant scoping to the subqueries (e.g. AND tenant_id = ${tenantId}) or otherwise ensure heartbeat_logs reads are filtered by the same tenant to enforce isolation.
- **server/process-governor.ts:198** — Response-time query aggregates heartbeat_logs but does not filter by tenant_id (SELECT AVG(duration_ms) FROM heartbeat_logs ...).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause so metrics are calculated only for the requesting tenant.
- **server/process-governor.ts:240** — cascading_failures query reads heartbeat_logs without a tenant_id filter (COUNT DISTINCT persona_id FROM heartbeat_logs ...).
  - _Fix:_ Constrain the query by tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) to avoid cross-tenant data exposure.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a project without constraining tenant_id (project is tenant-scoped).
  - _Fix:_ Require a tenantId parameter (or derive validated tenant context) and add WHERE tenant_id = ${tenantId} to the query, or otherwise validate that the caller owns the project before reading.
- **server/project-brain.ts:332** — Backfill query scans projects and conversations across the DB without tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... ), exposing cross-tenant data.
  - _Fix:_ Restrict the backfill to a tenant (add tenant_id = ${tenantId} filters or accept a tenantId argument) so only that tenant's projects/conversations/messages are processed.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is executed without tenant_id filtering (conversations are tenant-scoped), allowing cross-tenant reads during backfill.
  - _Fix:_ Add tenant filtering (e.g. AND c.tenant_id = ${tenantId}) or require tenant context when fetching conversations by project_id.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping, enabling cross-tenant leakage when conversation ids are processed without verifying tenant ownership.
  - _Fix:_ Filter messages by tenant (AND tenant_id = ${tenantId}) or ensure conversation_id is validated as belonging to the tenant before reading messages.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped code_proposals row without any tenant_id constraint.
  - _Fix:_ Require and use tenant context (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or verify ownership before reading proposals.
- **server/recurring-messages.ts:300** — UPDATE against tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} with NO tenant_id constraint. When the runner executes a global scan it later updates rows by id only, violating the invariant that every write must be constrained to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or otherwise validate the row belongs to the tenant before updating. Prefer using both id + tenant_id to make the intent explicit and avoid cross-tenant writes.
- **server/recurring-messages.ts:287** — Inserting a conversation with persona_id set from stored payload (`p.expandViaPersona`) without verifying that the referenced persona belongs to the same tenant. A foreign id from untrusted/stored input is used directly in an INSERT into the tenant-scoped `conversations` table.
  - _Fix:_ Before using persona_id from the payload, verify ownership (e.g. SELECT id FROM personas WHERE id = ${p.expandViaPersona} AND tenant_id = ${r.tenant_id} LIMIT 1) and only use it if it exists and belongs to the tenant. Reject or map invalid persona ids to a tenant-local default. Always validate foreign ids against tenant ownership prior to writes.
- **server/reference-learner.ts:290** — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${storedId} with NO tenant_id constraint. The code earlier found an existing row by tenant_id, but the subsequent UPDATE targets the row by id only.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${storedId} AND tenant_id = ${input.tenantId}) or re-query/validate that the storedId belongs to the expected tenant before updating. Every UPDATE must include the tenant_id filter to enforce isolation.
- **server/research-engine.ts:470** — INSERT INTO project_notes omits tenant_id. Tenant-scoped table INSERTs must set tenant_id explicitly; otherwise rows may land without tenant scoping or violate NOT NULL constraints.
  - _Fix:_ Include tenant_id explicitly in the INSERT columns/values (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${session.tenantId}, ${projectId}, ${note}, ${author}, NOW())). Ensure project_notes is typed/validated as tenant-scoped and pass the session/claim tenant id rather than deriving it from project_id alone.
- **server/research-engine.ts:478** — Second INSERT INTO project_notes (for each finding) also omits tenant_id, risking cross-tenant writes or schema constraint failures.
  - _Fix:_ Same fix as above: include tenant_id in the INSERT columns and pass the owning tenant (e.g. ${session.tenantId}). Prefer fail-closed: verify assertProjectInTenant(projectId, tenantId) and then set tenant_id explicitly.
- **server/routes.ts:2312** — SELECT from tenant-scoped delivery_logs without tenant filter in /api/store/order/:sessionId — returns delivery log rows for an arbitrary sessionId supplied by the caller.
  - _Fix:_ Ensure the sessionId truly acts as an unguessable capability (documented & hardened) or add additional checks (e.g. verify the caller's tenant owns the delivery_log row or restrict to authenticated users). Consider limiting returned fields to the minimum necessary and avoid exposing tenant identifiers directly.
- **server/routes.ts:2420** — SELECT from tenant-scoped delivery_logs by customer email (lower(customer_email) = ${rawEmail}) in async path of /api/store/lookup-orders without tenant restriction.
  - _Fix:_ This performs an unscoped search across all tenants by email. To reduce risk, restrict lookup to data the user can legitimately access, rate-limit/monitor the operation (already done), or require stronger verification. Consider storing per-tenant hashed indexes or only performing per-tenant lookups (or otherwise ensure no cross-tenant PII is exposed by this query path).
- **server/routes.ts:2564** — SELECT from tenant-scoped delivery_logs by email in /api/store/verify-orders (returns the customer's orders across tenants) without tenant_id constraint.
  - _Fix:_ After verifying the one-time code, ensure the returned orders are strictly scoped to the intended tenant or that the operation is otherwise safe (only returns capability-safe session IDs). Prefer returning only unguessable capability links and avoid returning internal tenant-scoped fields. Consider adding an explicit tenant scoping model for this flow.
- **server/routes/admin-zombie-detector.ts:36** — SQL query reads from agent_trace_spans without constraining to the caller's tenant. The CTE (`used`) selects from agent_trace_spans with no `tenant_id = ...` filter, so results can span all tenants.
  - _Fix:_ If the intent is to show only the caller's tenant data, add a tenant predicate to the WHERE clause (e.g. `WHERE tenant_id = ${tenantId} AND ...`). If the intent is a platform-wide admin view, make that explicit and ensure only an appropriately-authorized, audited platform-admin identity can run the query (and document that it intentionally reads all tenants). Prefer explicit parameterized tenant scoping rather than relying on implicit admin assumptions.
- **server/routes/admin-zombie-detector.ts:54** — SQL query aggregates conversations (used CTE) without a tenant filter. The query computing persona usage selects from `conversations` but lacks `tenant_id = ...`, so it will aggregate across all tenants.
  - _Fix:_ Add explicit tenant scoping (e.g. `WHERE tenant_id = ${tenantId} AND ...`) before grouping/aggregating. If a cross-tenant aggregation is required, require and validate a platform-admin context explicitly and document the behavior; do not assume implicit admin consent is sufficient.
- **server/routes/admin.ts:568** — SELECT DISTINCT tool_name FROM tool_performance is executed with NO tenant constraint. If `tool_performance` is a tenant-scoped table, this returns platform-wide data without an explicit tenant filter.
  - _Fix:_ If you mean to show only the caller's tenant data, add `WHERE tenant_id = ${tenantId}`. If platform-wide metrics are intended, make the platform-scope explicit and ensure the route is strictly gated (and logged/audited). Avoid leaving tenant-scoped tables unfiltered even in admin code unless intentionally designed and authorized.
- **server/routes/admin.ts:574** — Query against `tool_performance` to list failing tools has no tenant filter. The admin endpoint runs `SELECT ... FROM tool_performance WHERE ...` without `tenant_id = ...`, potentially exposing cross-tenant performance data.
  - _Fix:_ Add `AND tenant_id = ${tenantId}` to the WHERE clause to scope to the caller's tenant. If a platform-wide view is required for owner operators, require a verified platform-admin context and make the cross-tenant intent explicit in code and logging.
- **server/routes/admin.ts:587** — Query selects rows from `ai_insights` (status = 'new') without any tenant constraint. `ai_insights` appears to be tenant-scoped but is read here with no `tenant_id = ...` filter.
  - _Fix:_ Constrain the query to the caller's tenant with `WHERE tenant_id = ${tenantId} AND ...`. If platform-wide inspection is intended, require and verify an explicit platform-admin privilege and document that the query intentionally spans tenants.
- **server/routes/admin.ts:598** — Query against `heartbeat_tasks` (disabled/failing tasks) omits a tenant filter. `heartbeat_tasks` is likely tenant-scoped but is read without `tenant_id = ...`.
  - _Fix:_ Add a tenant predicate (`AND tenant_id = ${tenantId}`) to limit results to the caller's tenant. If this is meant to be a platform-wide admin audit, make that explicit and ensure proper authorization and auditing.
- **server/routes/admin.ts:609** — Query selects from `ai_insights` for pending Minerva routing without constraining by tenant. This reads tenant-scoped insight rows across all tenants.
  - _Fix:_ Restrict the query with `WHERE tenant_id = ${tenantId} AND ...` to enforce tenant isolation. If a global admin view is required, validate platform-admin privileges explicitly and document the cross-tenant access rationale.
- **server/routes/conversations.ts:98** — UPDATE on 'conversations' omits tenant filter. The statement 'UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}' updates a tenant-scoped table using only the conversation id, without constraining by tenant_id.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or use storage layer helper that enforces tenant scoping. Prefer defense-in-depth: enforce tenant_id at every SQL write even if application logic already checked ownership.
- **server/routes/conversations.ts:102** — INSERT into 'project_conversations' does not set tenant_id. The statement 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})' writes into a tenant-scoped mapping table but omits tenant_id in the inserted columns.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})) and ensure tenantId comes from the authenticated session. Validate ownership prior to insert.
- **server/routes/conversations.ts:104** — UPDATE on 'projects' omits tenant filter. The statement 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' updates a tenant-scoped table by id only, missing tenant_id qualification.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so writes cannot affect other tenants' rows even if an id is guessed or reused.
- **server/routes/conversations.ts:227** — INSERT into 'project_notes' lacks tenant context. The statement 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' creates a row in a tenant-scoped table but does not include tenant_id (or otherwise validate that the inserted row will be associated with the caller's tenant).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) and ensure the project belongs to the tenant before inserting. Always persist tenant_id for tenant-scoped tables.
- **server/routes/inbox-notifications.ts:101** — UPDATE on 'inbox_messages' performs 'UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}' without tenant_id constraint. This is an in-handler write that omits tenant scoping even though the row was earlier selected with tenant_id filtering.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or perform the update via a storage helper that enforces tenant ownership. Do not rely solely on a previously fetched row for isolation — enforce it at the SQL layer.
- **server/routes/research.ts:294** — INSERT into research_schedules sets program_id from caller input without verifying the referenced research_programs row belongs to the caller's tenant. A tenant can create a schedule that references another tenant's program_id.
  - _Fix:_ Before inserting, verify the programId (if provided) belongs to the requesting tenant. e.g. SELECT id FROM research_programs WHERE id = ${programId} AND tenant_id = ${tenantId} and reject the request if not found (or set program_id to NULL). Enforce this check server-side and do not trust client-supplied foreign IDs.
- **server/routes/research.ts:312** — UPDATE of research_schedules assigns program_id from caller input without validating ownership of the referenced research_programs row. A tenant could set their schedule to reference a program belonging to another tenant.
  - _Fix:_ When updating program_id, first verify the program belongs to the caller's tenant (SELECT id FROM research_programs WHERE id = ${programId} AND tenant_id = ${tenantId}). If the check fails, reject the update. Alternatively, only allow program_id to be changed to NULL or to program ids confirmed to be owned by the tenant.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id constraint (tenant-scoped table). This reads global session data across tenants.
  - _Fix:_ Constrain the query to the intended tenant (e.g. WHERE tenant_id = ${tenantId}). If this is intended to be scoped to tenant 1 for seeding, include tenant_id = 1 explicitly. Prefer Drizzle-style filters (e.g. .where(eq(research_sessions.tenant_id, tenantId))).
- **server/seed.ts:2855** — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter; research_programs is tenant-scoped. This reads programs for all tenants.
  - _Fix:_ Add a tenant constraint to the SELECT (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise limit to the intended tenant. Use Drizzle .where(eq(research_programs.tenant_id, tenantId)).
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... checks for missing project_conversations rows but lacks any tenant_id constraint on conversations/project_conversations. These tables are tenant-scoped and this query can read cross-tenant data.
  - _Fix:_ Add tenant scoping to the SELECT and to the NOT EXISTS subquery (e.g. WHERE c.tenant_id = ${tenantId} AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.tenant_id = ${tenantId} AND pc.project_id = c.project_id AND pc.conversation_id = c.id)). Verify tenant ownership before acting on results.
- **server/skill-seeker.ts:749** — seekAndLearn defaults tenantId to 1 (admin) when caller omits tenantId. This can cause writes/creations to be attributed to the admin tenant instead of the caller's tenant, leading to cross-tenant data contamination.
  - _Fix:_ Do not default to the admin tenant. Require the caller to supply a valid tenantId and validate it (fail closed if missing). If admin-level actions are intended, require explicit elevated authorization/consent.
- **server/skill-seeker.ts:779** — runSkillSeekerSweep defaults tenantId to 1 (admin). A sweep invoked without an explicit tenantId will operate on the admin tenant, causing cross-tenant changes.
  - _Fix:_ Remove the admin default; require an explicit tenantId and validate it. If a platform-level sweep is required, expose it via a privileged/admin-only path that is auditable.
- **server/skill-seeker.ts:653** — Auto-creation of tools calls executeTool("create_tool", { ..., _tenantId: ADMIN_TENANT_ID }) — tooling is created under the admin tenant rather than the gap's tenant. This can misassign ownership and blur tenant isolation/authorization boundaries.
  - _Fix:_ Create tools in the gap's tenant context (tenantId) or route tool creation through an explicit, auditable admin approval flow. Avoid silently creating tenant-scoped resources under ADMIN_TENANT_ID for other tenants.
- **server/skill-seeker.ts:708** — Auto-creation of skills calls executeTool("manage_skills", { ..., _tenantId: ADMIN_TENANT_ID }) — new skills are created under the admin tenant rather than the requesting tenant, risking cross-tenant ownership and privilege issues.
  - _Fix:_ Create skills under the requesting tenant (tenantId) or require an explicit admin-reviewed promotion flow. Do not default auto-created tenant-scoped artifacts into the admin tenant.
- **server/skills-marketplace.ts:207** — SELECT id FROM skills WHERE name = ${template.name} is executed with no tenant filter. If `skills` is tenant-scoped this can collide across tenants or leak presence of a skill across tenants.
  - _Fix:_ Make marketplace functions tenant-aware (accept a tenantId) and scope SELECTs to tenant_id = ${tenantId}, or explicitly use a global/shared skills table if intended and document that design decision.
- **server/storage.ts:239** — getConversationUnscoped performs an unscoped SELECT from conversations (no tenant_id filter). This returns conversation rows across all tenants.
  - _Fix:_ Avoid unscoped reads in application code. Require an explicit tenantId parameter and add eq(conversations.tenantId, tenantId) to the WHERE clause, or restrict this function to admin-only with explicit authorization and logging.
- **server/storage.ts:295** — attachCitationMetadata issues raw SELECT FROM messages WHERE id IN (...) without any tenant_id constraint. messages is tenant-scoped so this can return citations from other tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or accept a tenantId parameter and join/filter by messages.tenant_id to ensure citations are only read for the caller's tenant.
- **server/storage.ts:396** — attachCitations does an UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id condition. This can update message rows across tenants.
  - _Fix:_ Require and validate tenantId for this operation and include it in the WHERE clause (AND tenant_id = ${tenantId}). Parameterize tenant from a trusted source (not request body) or look up+validate ownership before updating.
- **server/storage.ts:516** — getMemoryEntry constructs a WHERE with only eq(memory_entries.id, id) when tenantId is undefined (warns but proceeds). This performs an unscoped SELECT on a tenant-scoped table.
  - _Fix:_ Fail-closed: require tenantId (or assert the caller has admin rights) and always include eq(memory_entries.tenantId, tenantId) in the WHERE. Remove patterns that merely warn and then proceed unscoped.
- **server/storage.ts:530** — createMemoryEntry inserts into memory_entries without validating that tenantId is present/trusted. Insert may either fail or, if tenantId provided untrusted, create a row under the wrong tenant.
  - _Fix:_ Validate that data.tenantId is an explicit, positive tenant id and that the caller is authorized for that tenant before inserting. Do not accept tenantId directly from untrusted input without ownership checks.
- **server/storage.ts:545** — updateMemoryEntry performs UPDATE memory_entries ... WHERE id = ... (tenant filter omitted if tenantId undefined — only a warning is logged). This allows unscoped writes to a tenant-scoped table.
  - _Fix:_ Require tenantId (fail-closed) and include eq(memory_entries.tenantId, tenantId) in the WHERE clause, or otherwise verify ownership before updating.
- **server/storage.ts:553** — deleteMemoryEntry updates memory_entries (status='superseded') using only id when tenantId is undefined (the code warns but proceeds). This is an unscoped write against a tenant-scoped table.
  - _Fix:_ Require tenantId and include it in the WHERE (AND tenant_id = ${tenantId}). Do not rely on warning-only behaviour for tenant scoping.
- **server/storage.ts:561** — touchMemoryEntries does UPDATE memory_entries ... WHERE id IN (...) without a tenant_id constraint when tenantId is undefined (it only warns). This can modify rows belonging to other tenants.
  - _Fix:_ Require tenantId and always include eq(memory_entries.tenantId, tenantId) in the WHERE, or restrict the function to callers that can prove ownership of the provided ids.
- **server/storage.ts:621** — evictOldestConversationFacts runs an UPDATE conversation_facts SET status='expired' WHERE id IN (victims...) but the UPDATE does not constrain tenant_id. Although victim ids were selected earlier with a tenant filter, the subsequent UPDATE uses only ids and can affect rows outside the tenant if ids are tampered with.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId} or WHERE id = ANY(...) AND tenant_id = ${tenantId}). Prefer updating by a tenant-scoped predicate rather than relying on a previously fetched id list.
- **server/storage.ts:641** — getDailyNote constructs a WHERE without tenant filter if tenantId is not supplied (only persona/date conditions). daily_notes has tenant_id — this returns other tenants' notes.
  - _Fix:_ Require tenantId and add eq(daily_notes.tenantId, tenantId) to the WHERE. Do not proceed with an unscoped SELECT even with a warning.
- **server/storage.ts:647** — upsertDailyNote updates daily_notes WHERE id = existing.id without constraining tenant_id. This is a write to a tenant-scoped table by id only.
  - _Fix:_ Include tenant scoping on the UPDATE (AND tenant_id = ${data.tenantId} or similar) and validate that the existing row belongs to the caller's tenant before mutating.
- **server/storage.ts:803** — updateMemoryEmbedding updates memory_entries SET embedding WHERE id = ${id} with no tenant constraint. This writes to a tenant-scoped table without checking tenant ownership.
  - _Fix:_ Require the caller to pass tenantId or otherwise validate ownership of the memory entry and include eq(memory_entries.tenantId, tenantId) in the WHERE clause.
- **server/storage.ts:815** — updateKnowledgeEmbedding updates agent_knowledge SET embedding WHERE id = ${id} with no tenant filter. agent_knowledge is tenant-scoped; this is an unscoped write.
  - _Fix:_ Require tenantId or validate ownership before updating and include eq(agent_knowledge.tenantId, tenantId) in the WHERE clause.
- **server/storage.ts:838** — archiveExpiredMemories issues a global UPDATE memory_entries SET status='archived' WHERE ... without tenant scoping. This will archive rows across all tenants.
  - _Fix:_ If this is intended to run per-tenant, require a tenantId parameter and include it in the WHERE. If it must run globally, ensure the caller is a tightly-controlled admin process and that authorization is explicit.
- **server/storage.ts:855** — archiveStaleMemories updates memory_entries SET status='archived' WHERE ... with no tenant filter (global write across tenant-scoped table).
  - _Fix:_ Same as above — scope the archival to a tenant parameter or restrict to authorized admin background jobs that are explicitly authorized to operate across tenants.
- **server/subagents.ts:248** — spawnSubagent calls storage.getConversation(parentConversationId, params.tenantId ?? ADMIN_TENANT_ID). If params.tenantId is omitted the code passes ADMIN_TENANT_ID as the tenant argument without verifying the caller has admin privileges, allowing potential unintended admin-scoped reads.
  - _Fix:_ Do not default to ADMIN_TENANT_ID. Require the caller to provide tenantId or derive it from an authenticated request context. If admin-level lookups are required, enforce explicit admin authorization before using ADMIN_TENANT_ID.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding performs UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL with no tenant_id constraint. felix_proposals is tenant-scoped.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or accept tenantId as a parameter and validate it prior to updating.
- **server/surprise-scorer.ts:141** — scoreProposalSurprise updates actual_outcome_embedding with WHERE id = ${proposalId} but omits tenant_id. This writes to a tenant-scoped table without ensuring tenant ownership.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the UPDATE or otherwise verify the row belongs to the expected tenant before mutating.
- **server/surprise-scorer.ts:181** — When not enough neighbors exist the code UPDATEs felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} without constraining tenant_id.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) to avoid changing rows in other tenants; require tenantId in the function signature or validate ownership first.
- **server/surprise-scorer.ts:212** — After computing the score the code UPDATEs felix_proposals SET surprise_score = ${meanDist}, surprise_band = ${band} WHERE id = ${proposalId} with no tenant_id predicate. This writes tenant-scoped fields without tenant filtering.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE or require/validate tenantId prior to writing. All UPDATEs/Inserts on tenant-scoped tables must include the tenant filter.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` is performed without constraining by tenant_id (where(eq(customTools.name, parsed.name)) only). This can read rows across tenants.
  - _Fix:_ Scope the lookup to the caller's tenant. Add a tenant filter, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))) or otherwise ensure the query is always parameterized by the explicit tenantId passed to the API.
- **server/tool-learning.ts:155** — SELECT from tenant-scoped table `custom_tools` by id is performed without a tenant_id constraint (.where(eq(customTools.id, inserted.id))). This can expose/return rows belonging to other tenants if mis-used.
  - _Fix:_ Include tenant scoping when selecting by id: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))) or fetch the row within the same transaction/context that guarantees tenant ownership.
- **server/tools.ts:4514** — INSERT into project_notes uses project_id but does not include tenant_id. If project_notes is a tenant-scoped table (tenant_id NOT NULL), this INSERT will create rows with no explicit tenant binding, violating the invariant that every INSERT must pass tenantId explicitly.
  - _Fix:_ Include the tenant_id value in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, ... ) VALUES (${params._tenantId}, ${projectId}, ...)). Ensure the tenantId is validated (assertProjectInTenant was used earlier — keep that) and originate the tenant value from the trusted/asserted source, not from untrusted input.
- **server/tools.ts:4518** — INSERT into project_files omits tenant_id and inserts only project_id/file metadata. If project_files is tenant-scoped, the row will be created without an explicit tenant_id (columns are expected NOT NULL per invariant).
  - _Fix:_ Explicitly include tenant_id in the INSERT (INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${projectId}, ...)). Continue to verify ownership with assertProjectInTenant before the write and use the verified tenantId when inserting.
- **server/tools.ts:6365** — INSERT into project_files (in generate_audio) writes project file metadata using params.project_id but does not include tenant_id in the INSERT. This can violate the requirement that INSERTs into tenant-scoped tables must explicitly set tenant_id.
  - _Fix:_ Add tenant_id to the INSERT (INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Also ensure assertProjectInTenant is used (it is in this handler) and use the verified tenantId rather than trusting raw input.
- **server/tools.ts:6899** — INSERT into project_files (in create_slideshow_video) omits tenant_id and writes only project_id and file metadata. If project_files requires tenant_id, this violates the invariant requiring explicit tenant stamping on INSERTs.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${projectId}, ...)). Ensure assertProjectInTenant is invoked before the write and use the validated tenant id value for the inserted row.
- **server/tools.ts:8864** — UPDATE against tenant-scoped table 'proposed_skills' uses only the row id in WHERE clause (where(eq(proposedSkills.id, row.id))) and lacks a tenant_id constraint. Without tenant scoping this UPDATE could affect rows belonging to other tenants if ids collide or are guessed.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well, e.g. .where(eq(proposedSkills.id, row.id)).where(eq(proposedSkills.tenantId, tid)) or use a combined SQL WHERE (id = ... AND tenant_id = ...). Prefer the Drizzle eq(table.tenantId, tenantId) pattern to enforce tenant isolation.
- **server/tools.ts:8889** — UPDATE against tenant-scoped table 'proposed_skills' (marking accepted/promoted) uses only the row id in WHERE clause and lacks a tenant_id constraint. This can lead to cross-tenant modification.
  - _Fix:_ Add an explicit tenant filter to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id)).where(eq(proposedSkills.tenantId, tid)) or equivalent SQL), ensuring updates only affect rows belonging to the caller's tenant.
- **server/tools.ts:8905** — UPDATE against tenant-scoped table 'proposed_skills' (marking rejected) uses only the row id in WHERE clause and omits tenant_id. Tenant ownership is not enforced in the UPDATE statement.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. .where(eq(proposedSkills.id, row.id)).where(eq(proposedSkills.tenantId, tid)) or add AND tenant_id = ${tid} to the SQL) so the operation cannot affect other tenants' rows.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE executed against browser_workflows without constraining tenant (no tenant_id in WHERE) — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${workflow.id} AND tenant_id = ${tenantId}) so the update only affects rows owned by the caller's tenant (or use Drizzle .where(eq(table.tenantId, tenantId))).
- **server/tools/domains/files/handlers.ts:840** — UPDATE executed against projects without tenant scoping — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}
  - _Fix:_ Constrain the UPDATE by tenant (e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}) to avoid modifying other tenants' rows. Prefer using Drizzle helper .where(eq(projects.tenantId, tenantId)).
- **server/tools/domains/knowledge/handlers.ts:85** — UPDATE against tenant-scoped table `knowledge_triples` omits tenant filter — WHERE id = ${row.id} has no tenant_id constraint, violating the invariant that every write must be constrained to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. add AND tenant_id = ${tenantId} to the WHERE clause) or perform the update via a storage-layer function that enforces tenant scoping. Example fix: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId}.
- **server/tools/domains/security/handlers.ts:260** — COUNT(DISTINCT provider) FROM provider_keys executed with no tenant_id constraint; even though run only for tenantId===1, the query reads provider_keys across tenants
  - _Fix:_ Either add a tenant_id filter (WHERE tenant_id = ${tenantId}) to restrict results to the caller's tenant, or, if global owner visibility is intentional, move this into a clearly-audited, owner-only admin path and ensure the code documents and justifies the global query. Consider returning tenant-scoped aggregates or redacted information rather than raw cross-tenant counts/rows.
- **server/twilio.ts:116** — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} is performed without scoping by tenant_id. Reading the conversations table by id with no tenant constraint can expose existence/ownership across tenants.
  - _Fix:_ Include the tenant filter when querying conversations (e.g. WHERE id = ${conversationId} AND tenant_id = ${claimedTenantId}) or pass/verify tenant context into the query so the DB read is limited to the expected tenant. Avoid unscoped reads against tenant-scoped tables unless running under an operator/admin pathway with explicit authorization.
- **server/venture-discovery/budget.ts:155** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (settleVentureReservation) updates a tenant-scoped table using only the row id and no tenant_id constraint. This could allow a caller to settle (or overwrite) another tenant's ledger rows by id.
  - _Fix:_ Require and use tenantId when settling reservations and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Alternatively, perform the settle inside the same transaction that created the reservation (where tenant context is proven) or verify the reservation row's tenant before updating.
- **server/venture-discovery/budget.ts:170** — UPDATE agent_cost_ledger ... WHERE id = ${reservationId} (releaseVentureReservation) modifies a tenant-scoped table without constraining by tenant_id, allowing cross-tenant modification of ledger rows by id.
  - _Fix:_ Take tenantId as a parameter (or look it up within a validated transaction) and include tenant_id in the WHERE clause when releasing reservations (WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Ensure release operations only affect rows owned by the caller's tenant.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs performs a DB UPDATE on videoJobs across all tenants without scoping by tenant_id (it marks matching stale rows as failed globally). This mutates tenant-scoped data for every tenant and can incorrectly change other tenants' job states.
  - _Fix:_ Limit the sweep to the tenants this process is authorized to manage, or perform the stale-job recovery per-tenant (pass tenantId into the function) and include tenant_id in the UPDATE WHERE clause. If the intent is global housekeeping, ensure this operation is run by an operator/admin process with explicit authorization and auditing, and document that cross-tenant writes are intentional.
- **server/watchlist.ts:111** — Cross-tenant data exposure: SELECT joins watchlist_items (wi) without constraining the joined rows to the caller's tenant. The base query filters watchlist_alerts by wa.tenant_id = ${tenantId} but the LEFT JOIN ON wa.watchlist_item_id = wi.id does not ensure wi.tenant_id matches. Because tenant scoping is enforced only by application filters (no RLS), this can return another tenant's watchlist item name/category when IDs collide.
  - _Fix:_ Constrain the joined table to the tenant as well. For example change the JOIN to: LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id AND wi.tenant_id = wa.tenant_id (or add AND wi.tenant_id = ${tenantId}). Ensure every JOIN or subsequent SELECT on tenant-scoped tables includes an explicit tenant_id filter.
- **server/wiring-invariants.ts:244** — Querying tenant-scoped table "tool_performance" without any tenant_id constraint (SELECT count(*) FROM tool_performance ...). This reads across all tenants.
  - _Fix:_ Make the check explicit about whether it should run across all tenants. If it must be per-tenant, add a tenantId parameter and include WHERE tenant_id = :tenantId (or use Drizzle .where(eq(table.tenantId, tenantId))). To intentionally aggregate across tenants, ensure this code is only callable by an admin process and document the intent; prefer iterating tenants and running per-tenant queries to preserve isolation semantics.
- **server/wiring-invariants.ts:258** — SELECT count(*)::int AS n FROM tool_performance is executed with no tenant_id filter (reads total rows across all tenants).
  - _Fix:_ If the intent is per-tenant checks, pass tenantId and add WHERE tenant_id = :tenantId. If the intent is a global admin aggregate, make that explicit and restrict who can run this code; otherwise iterate tenants and run tenant-scoped queries to avoid cross-tenant data access.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... executes without a tenant_id filter (reads tool usage across all tenants).
  - _Fix:_ Add WHERE tenant_id = :tenantId or run the check for each tenant separately. Avoid querying tenant-scoped tables globally without a documented admin-only path and explicit authorization.
- **server/wiring-invariants.ts:366** — Using sql.raw to SELECT COUNT/MAX FROM ${spec.table} (e.g. tool_performance, knowledge_nudges) without constraining by tenant_id. Several SUBSYSTEM_TABLES are tenant-scoped and are being aggregated across tenants.
  - _Fix:_ For subsystem tables that are tenant-scoped, parameterize the tenant and add WHERE tenant_id = :tenantId (use Drizzle .where(eq(...))). If you must check existence of table-specific rows across tenants, do so via an explicit admin flow that iterates tenants and runs tenant-scoped queries rather than a single global count.
- **server/wiring-invariants.ts:417** — checkToolDrift queries tool_performance (SELECT ... FROM tool_performance WHERE last_failure_at > ...) without a tenant_id constraint — this reads and aggregates tenant data across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = :tenantId) and run drift checks per-tenant, or restrict this operation to an admin-only aggregated telemetry pipeline that is explicitly authorized and audited. Prefer per-tenant checks to maintain isolation.
- **server/wiring-invariants.ts:535** — checkMemoryGrowth uses sql.raw to SELECT COUNT and pg_total_relation_size FROM memory tables (e.g. messages, agent_knowledge, graph_memory) without tenant filtering; many of these tables are tenant-scoped.
  - _Fix:_ Run memory-growth checks per-tenant by passing tenantId and adding WHERE tenant_id = :tenantId to the COUNT query. If you need global metrics, implement an explicit, authorized admin aggregation that iterates tenants and collects per-tenant metrics instead of querying tenant tables globally.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*)::int AS cnt FROM graph_memory runs without filtering by tenant_id. graph_memory is likely tenant-scoped, so this is aggregating across tenants.
  - _Fix:_ Limit to a tenant via WHERE tenant_id = :tenantId or iterate tenants and run the query per-tenant. Ensure any global aggregation is performed only by an authorized admin path and is intentionally documented.
- **server/wiring-invariants.ts:183** — Subselects in checkOutcomeCanary query tenant-scoped tables (research_experiments, code_proposals and research_sessions) without tenant_id constraints (e.g. WHERE e.program_id = p.id ...). These reads aggregate per-program activity across tenants.
  - _Fix:_ If experiments/proposals are tenant-scoped, include tenant filtering (e.g. AND e.tenant_id = :tenantId) and run the canary per-tenant. Alternatively, if this must be global admin telemetry, make that explicit and restrict access; preferred approach is per-tenant checks to preserve isolation.

### MEDIUM (25)

- **server/auto-asset-capture.ts:184** — backfillProjectAssets scans conversations across all tenants with SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... without constraining tenant_id. This global scan reads tenant-scoped tables without a tenant WHERE clause.
  - _Fix:_ If this is intended as a global maintenance job, explicitly document and isolate it (run in admin/maintenance context). Prefer iterating tenants and running tenant-scoped queries with WHERE tenant_id = ${tenantId}, or otherwise add an explicit tenant_id predicate when a single-tenant operation is intended. Ensure any subsequent writes are constrained to the correct tenant.
- **server/credential-vault.ts:169** — Unscoped UPDATE against tenant-scoped table `credential_vault`: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}` lacks tenant_id constraint (called from getDecryptedPassword).
  - _Fix:_ Scope the UPDATE to the tenant to avoid cross-tenant mutations. Use `WHERE id = ${id} AND tenant_id = ${tenantId}` (the function already receives tenantId) so only the owning tenant's row is updated.
- **server/credential-vault.ts:176** — Unscoped UPDATE against tenant-scoped table `credential_vault`: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}` lacks tenant_id constraint (called from getLoginCredentials).
  - _Fix:_ Include the tenant filter when updating last_used_at: `UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}`. Always constrain updates by tenant_id to enforce tenant isolation.
- **server/data-protection.ts:24** — DDL creates memory_categories.tenant_id with DEFAULT 1. Adding a tenant_id default undermines the invariant that INSERTs must pass tenant_id explicitly and can result in implicit attribution to tenant 1.
  - _Fix:_ Remove the DEFAULT 1, require INSERTs to supply tenant_id explicitly, and if necessary run a careful backfill/migration that sets tenant_id intentionally for existing rows.
- **server/data-protection.ts:48** — DDL adds agent_knowledge.tenant_id with DEFAULT 1. The default value lets inserts omit tenant_id and implicitly associate rows with tenant 1, violating the requirement that INSERTs must explicitly set tenant_id.
  - _Fix:_ Drop the DEFAULT, require callers to supply tenant_id on INSERT, and backfill existing data intentionally. Audit code paths that insert into agent_knowledge to ensure tenant_id is always provided.
- **server/delegation-events.ts:62** — getRecentEvents returns events that lack tenantId to any caller. The filter (e => !e.tenantId || e.tenantId === tenantId) will expose events missing tenant metadata to any tenant requesting them.
  - _Fix:_ Only return events that explicitly match the caller's tenantId. Treat events without a tenantId as sensitive and either drop them or require a follow-up validation step before exposing them. Ensure events are recorded with tenantId where appropriate.
- **server/email.ts:365** — Polling: SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed without a tenant_id constraint, used to detect duplicates across tenants.
  - _Fix:_ Include tenant scoping in the existence check (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId} OR inbox_id = ${inboxId}) or otherwise derive tenant from the inbox/context before the SELECT so messages are deduplicated per-tenant only.
- **server/heartbeat.ts:1814** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... — no tenant_id constraint. This reads heartbeat_logs across tenants.
  - _Fix:_ If heartbeat_logs are tenant-scoped, add tenant_id = ${tenantId} to the WHERE clause (or otherwise assert admin intent). If the query is intended to be per-tenant, pass the tenant id into the query; if system-global logs are intended, document that and ensure the schema reflects that.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() to interpolate a column name derived from caller input (outcome → column). Although the column value is allowlisted above, sql.raw() with a request-derived identifier is an injection/isolation risk and bypasses normal query-building safeguards.
  - _Fix:_ Avoid sql.raw() with request-derived values. Implement the update as an explicit branch (switch on outcome -> call UPDATE setting the specific column), or map the outcome to a prebuilt Drizzle update expression so the column name is never injected into raw SQL at runtime. Keep the tenant_id WHERE clause (already present) and continue validating delta as a numeric parameter.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` filters by status/updated_at but does not include tenant_id. This read returns rows across all tenants (the function is an audit), which may expose tenant identifiers and state across tenants.
  - _Fix:_ If intended as a cross-tenant admin audit, document and gate this function to admin-only contexts and audit its outputs. Otherwise, accept a tenantId parameter and include WHERE tenant_id = ${tenantId}, or run per-tenant iterations so queries always include the tenant_id filter.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` performed without tenant_id filter (may run across tenants).
  - _Fix:_ Either restrict the sweep to a tenant (add tenant_id = ${tenantId}) or enforce admin-only execution with explicit auditing and rate-limiting. Prefer per-tenant queries with tenant_id in WHERE to maintain the project's tenant-isolation invariants.
- **server/lib/system-probes.ts:163** — A global messages count is computed via db.select().from(messagesTable) without scoping by tenant_id. This reads a tenant-scoped table across all tenants for a system probe.
  - _Fix:_ If an aggregated, cross-tenant metric is required, ensure this probe is explicitly admin-only and that results are handled securely and not exposed to tenant-scoped contexts. If the probe should be per-tenant, accept a tenantId parameter and include WHERE tenant_id = ${tenantId} in the query to enforce tenant isolation.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw(lockKey) in SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint) — sql.raw is being fed a value derived from ledgerKey (a caller-supplied identifier), which risks SQL injection or accidental injection of untrusted content into SQL.
  - _Fix:_ Avoid sql.raw with request-derived values. Compute a numeric BigInt lock value server-side and pass it as a parameterized binding (e.g. sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`), or use a safe helper that ensures only numeric values are injected. If ledgerKey is user-controlled, validate/sanitize before use.
- **server/reference-learner.ts:292** — Handler accepts a personaId from caller input and uses it as persona_id when inserting a tenant-scoped `memory_entries` row, without verifying that the persona belongs to the tenant. This uses a caller-supplied foreign id without ownership validation.
  - _Fix:_ Validate the input personaId belongs to the tenant before persisting (e.g. SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${input.tenantId}). If the persona is global/intentional, document and enforce that invariant; otherwise reject or remap personaId to a tenant-local persona. Always verify foreign ids from callers before reading/writing tenant-scoped rows.
- **server/research-engine.ts:322** — SELECT name FROM personas WHERE id = ${program.persona_id} is executed without a tenant filter. If personas is tenant-scoped, this allows reading a persona belonging to a different tenant.
  - _Fix:_ If personas are tenant-scoped, include tenant_id in the WHERE clause (WHERE id = ${program.persona_id} AND tenant_id = ${tenantId}) or otherwise document that personas are global and safe to read cross-tenant. Prefer explicit scoping to avoid accidental cross-tenant reads.
- **server/research-engine.ts:980** — resolvePersonaId(param personaSlug, _tenantId) ignores the tenantId parameter and does SELECT id FROM personas WHERE name = ${personaSlug} LIMIT 1 without tenant scoping. This can return a persona owned by another tenant.
  - _Fix:_ Use the tenantId parameter in the query: SELECT id FROM personas WHERE name = ${personaSlug} AND tenant_id = ${tenantId} LIMIT 1. If personas are intentionally global, add a clear comment and audit that global access is acceptable; otherwise enforce tenant isolation.
- **server/routes.ts:165** — SELECT from tenant-scoped file_storage without tenant_id constraint (restoreUploadsFromDb reads all fileStorage rows).
  - _Fix:_ Scope the query to a specific tenant (WHERE tenant_id = <tid>) or iterate per-tenant under a platform-admin-only path. Avoid scanning/operating on other tenants' rows in a single unscoped query.
- **server/routes.ts:1116** — SELECT from tenant-scoped file_storage without tenant_id constraint (uploads middleware queries all rows for a basename).
  - _Fix:_ Avoid broad cross-tenant SELECTs. If the intention is to verify ownership, prefer querying WHERE filename = <name> AND tenant_id = <authedTenantId>, or explicitly document and gate this cross-tenant behavior to a platform-admin-only routine. If keeping the ‘check all owners’ approach, ensure the code cannot leak existence information and that the operation is restricted to trusted callers only.
- **server/routes.ts:764** — SELECT of projects (tenant-scoped) without tenant_id constraint in backfillProjectDriveFolders — reads all projects across tenants.
  - _Fix:_ Run backfill operations per-tenant (loop tenants and run the SELECT/processing scoped to that tenant) or ensure this startup backfill runs under a clearly-documented platform-admin context that intentionally requires global access. Prefer explicit tenant_id predicates when possible.
- **server/routes.ts:1529** — Multiple SELECT COUNT(*) queries over tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) in a public endpoint (/api/public/stats) without tenant_id filters.
  - _Fix:_ If these are intended to be global aggregated metrics, move aggregation to a controlled admin-only path or to materialized/view-based jobs that avoid ad-hoc scans. If per-tenant data is intended, add WHERE tenant_id = <caller tenant> and require auth.
- **server/routes.ts:1558** — Numerous unscoped SELECTs against tenant-scoped tables in /api/public/architecture (counts and lists of personas, projects, skills, messages, etc.).
  - _Fix:_ Restrict this endpoint to admin users or aggregate via a background job that produces sanitized public metrics. For any on-demand queries, add tenant_id filters or otherwise ensure no cross-tenant data leakage can occur.
- **server/routes/goal-ledger.ts:92** — SELECT from 'agent_jobs' returns rows across tenants without filtering by tenant_id. The query 'SELECT ... FROM agent_jobs j LEFT JOIN tenants t ON t.id = j.tenant_id WHERE j.status IN (..)' reads tenant-scoped rows with no tenant predicate, exposing cross-tenant data to the caller.
  - _Fix:_ If the intent is to show only the caller's tenant jobs, add WHERE j.tenant_id = ${tenantId}. If this must remain a global admin view, ensure the endpoint is strictly platform-admin gated and document the deliberate cross-tenant access; otherwise restrict results or add an explicit tenant filter parameter to avoid accidental data leakage.
- **server/skill-seeker.ts:827** — After resolving gaps the code calls executeTool("sync_personas", { _tenantId: ADMIN_TENANT_ID }) to sync personas using the admin tenant context. This performs a platform-level operation under admin credentials which may have side-effects affecting tenant-scoped data.
  - _Fix:_ Ensure persona sync under ADMIN_TENANT_ID is intentional and gated (audit/log/authorize). If the sync should act per-tenant, call it with the specific tenantId or expose an admin-only operator endpoint with proper checks and justification.
- **server/tools.ts:4540** — SELECT token FROM presenter_sessions WHERE presentation_id = ${slideResult.presentationId} executes without a tenant_id constraint. If presenter_sessions is tenant-scoped, this read lacks an explicit tenant filter or an ownership assertion for the presentation_id being read.
  - _Fix:_ Either add tenant scoping to the query (e.g. AND tenant_id = ${params._tenantId}) or assert ownership of the referenced presentation_id before performing the SELECT. Prefer using an ownership-assert helper that validates the presentation belongs to the caller's tenant and then use the trusted tenantId in the query.
- **server/video-job-runner.ts:204** — SELECT videoJobs.cancelRequested WHERE jobId = ${jobId} (isCancelRequested) reads a tenant-scoped table by jobId only and does not include tenant_id. This allows reading cancel flags for jobs owned by other tenants.
  - _Fix:_ Pass/derive the tenantId and include it in the WHERE clause (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}). If jobId is globally-unique but you must enforce ownership, verify the job's tenant before returning cancel state.

### LOW (1)

- **server/routes.ts:1819** — SELECT from personas WHERE id = 2 without tenant_id constraint in demo/admin check (felix-check).
  - _Fix:_ If this check is intended to be global/admin-only, ensure the endpoint is admin-gated (it is) and comment the reason for the unscoped access. Otherwise, scope queries to tenant_id where appropriate.

### Suppressed — confirmed false positives (168)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agentic-engines.ts:244** _(system-sweep)_ — autoApplyEligibleInsights executes an UPDATE on `ai_insights` without a tenant_id constraint (UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...)). This is a cross-tenant write that can modify rows belonging to other tenants.
  - _Why false positive:_ Platform-wide backfill sweep intentionally updates eligible insights across tenants and carries tenant_id per returned row for same-tenant follow-up routing; not tenant-facing mixed output.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals without constraining by tenant_id (UPDATE agentApprovals SET status='expired' ... WHERE status='pending' AND lt(expiresAt, now())). This will expire pending approvals across all tenants and then proceed to operate on runs by tenant, creating a dangerous multi-tenant write.
  - _Why false positive:_ Expiry sweep intentionally expires stale approvals across all tenants, then processes each returned row with its own tenantId; housekeeping/state transition only.
- **server/agentic/runs.ts:36** _(internal-worker-pk)_ — appendStep updates agent_runs using WHERE id = runId only (no tenant_id constraint). This allows cross-tenant modification of run rows when called with an arbitrary runId.
  - _Why false positive:_ Helper mutates agent_runs by server-generated serial PK and shown call path uses run.id created server-side in withRun, not request-derived tenant-crossing input.
- **server/agentic/runs.ts:45** _(internal-worker-pk)_ — updateRunState updates agent_runs with .where(eq(agentRuns.id, runId)) but does not restrict by tenant_id. This permits cross-tenant writes to run state.
  - _Why false positive:_ Run-state helper operates on globally unique server-generated runId for internal worker/orchestration state transitions rather than untrusted request-boundary input.
- **server/agentic/runs.ts:54** _(internal-worker-pk)_ — replaceRunState updates agent_runs using only run id in WHERE clause (no tenant constraint), enabling cross-tenant state replacement.
  - _Why false positive:_ Internal helper replaces state on agent_runs by server-generated primary key; no evidence here of untrusted cross-tenant id source.
- **server/agentic/runs.ts:58** _(internal-worker-pk)_ — completeRun updates agent_runs (status/result/completedAt) using WHERE id = runId only — missing tenant_id constraint which allows cross-tenant writes.
  - _Why false positive:_ Completion helper updates a run by globally unique server-created runId and immediately re-reads same PK internally; matches worker PK pattern.
- **server/agentic/runs.ts:88** _(internal-worker-pk)_ — failRun updates agent_runs setting status/error/completedAt using WHERE id = runId only — no tenant_id filter, permitting cross-tenant updates.
  - _Why false positive:_ Failure-state helper is an internal run-lifecycle mutation by server-generated run PK, not a tenant-facing arbitrary id update in the shown code.
- **server/agentic/runs.ts:97** _(internal-worker-pk)_ — pauseRun updates agent_runs to status='paused' using only run id in the WHERE clause (no tenant constraint), enabling cross-tenant state changes.
  - _Why false positive:_ Pause helper updates internal run row by globally unique runId used within orchestration helpers, fitting internal-worker-pk.
- **server/agentic/wake-scheduler.ts:146** _(system-sweep)_ — Claiming due wake rows uses a subquery that selects FROM agent_wake_schedules without constraining tenant_id — the statement will transition pending wakes across ALL tenants to 'firing' (cross-tenant row modification).
  - _Why false positive:_ Heartbeat maintenance loop intentionally claims due wake rows across tenants, then emits/updates each row with its own carried tenant_id; housekeeping scheduler behavior.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL — performing a write against a tenant-scoped table without constraining by tenant_id. This can modify rows belonging to other tenants.
  - _Why false positive:_ Rows come from prior join enforcing conversations.id = pc.conversation_id and projects.id = pc.project_id with p.tenant_id = c.tenant_id, so conversation/project tenant match is established earlier in same call path before the UPDATE.
- **server/auto-transcript.ts:179** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING executed using rows derived from an unscoped SELECT. This may create cross-tenant links (write) because the INSERT does not validate that the project and conversation belong to the same tenant.
  - _Why false positive:_ project_conversations has no tenant_id and rows come from conversations.project_id on a tenant-scoped parent; link table is tenant-isolated transitively.
- **server/data-protection.ts:72** _(upstream-verified)_ — UPDATE on conversations omits tenant scoping. The UPDATE that marks a conversation deleted uses WHERE id = ${conversationId} but does not constrain by tenant_id.
  - _Why false positive:_ flag is stale; the actual UPDATE includes AND tenant_id = ${tenantId}.
- **server/data-protection.ts:125** _(system-sweep)_ — Deletes in permanentlyPurgeSoftDeleted lack tenant scoping. The function deletes rows from messages, compaction_archives, and conversations using only conversation_id / id — no tenant_id predicate, which can remove data across tenants.
  - _Why false positive:_ platform-wide expiry purge selects expired conversations across tenants and processes each row with its own tenant_id; deletes are housekeeping, not tenant-facing mixed output.
- **server/embeddings.ts:726** _(system-sweep)_ — UPDATE memory_entries in backfillEmbeddingVecs sets embedding_vec WHERE id = ${row.id} with no tenant_id constraint — allows updating arbitrary tenants' rows.
  - _Why false positive:_ boot/maintenance backfill scans rows across tenants and only fills missing embedding_vec on each selected row; housekeeping mutation with no tenant data returned or mixed.
- **server/embeddings.ts:743** _(system-sweep)_ — UPDATE agent_knowledge in backfillEmbeddingVecs sets embedding_vec WHERE id = ${row.id} with no tenant_id constraint — allows updating arbitrary tenants' rows.
  - _Why false positive:_ same maintenance backfill for agent_knowledge; per-row housekeeping update only, no tenant-facing data mixing.
- **server/felix-brain.ts:19** _(system-sweep)_ — Boot-time rehydration queries agent_runs without any tenant filter — SELECT reads tenant-scoped rows across all tenants.
  - _Why false positive:_ boot-time rehydration intentionally reads running agent_runs across tenants to rebuild in-memory state per row; internal maintenance, not tenant-facing mixed output.
- **server/google-drive.ts:946** _(fk-transitive)_ — INSERT INTO project_files omits tenant_id entirely — inserting into a tenant-scoped table without explicitly setting tenant_id (columns are NOT NULL with no default).
  - _Why false positive:_ project_files has no tenant_id and insert is gated by assertProjectInTenant on tenant-scoped projects; child row is isolated through project_id.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) executes a cross-tenant write (no tenant filter). Selected IDs may belong to other tenants.
  - _Why false positive:_ stale-proposal archival is a platform-wide TTL housekeeping sweep across tenants, mutating only status fields and emitting aggregate/internal findings.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id without checking tenant ownership (no tenant filter), potentially disabling other tenants' tasks.
  - _Why false positive:_ Boot/maintenance audit sweeps stale heartbeat_tasks across tenants and only flips enabled=false on selected rows; housekeeping only, no tenant-facing mixed output.
- **server/internal-resolver.ts:93** _(internal-worker-pk)_ — UPDATE event_log finalization omits tenant scoping — `finalizeEvent` updates event_log WHERE id = ${eventId} but does not constrain by tenant_id. This lets the resolver mutate events across tenants.
  - _Why false positive:_ finalizeEvent is an internal resolver helper updating event_log by server-side eventId primary key, not from a request boundary.
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — UPDATE on tenant-scoped table `action_attempts` performed without any tenant_id predicate (sweepStaleAttempts updates rows across all tenants).
  - _Why false positive:_ Reconciler sweep intentionally operates platform-wide, marking stale action_attempts to unknown as housekeeping only; later per-row handling uses carried tenant_id.
- **server/memory-graph.ts:194** _(fk-transitive)_ — INSERT into tenant-scoped table `memory_links` does not set tenant_id (INSERT INTO memory_links (... ) VALUES (...)). This creates cross-tenant links and allows insertion of rows not bound to the caller's tenant.
  - _Why false positive:_ memory_links has no tenant_id column; links are isolated transitively through source/target memory_entries, which are tenant-scoped and filtered elsewhere in the flow.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT from tenant-scoped table oauth_subscriptions without tenant_id filter (reads id, provider, tenant_id, expires_at, refresh_token, consecutive_failures) — global query exposes sensitive per-tenant tokens/data.
  - _Why false positive:_ validateSubscriptionsOnStartup is a startup maintenance loop over all active subscriptions; it uses each row's tenant_id for per-row reconnect/validation and is not a tenant-facing data return path.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — appendExecutionLog updates the plans table with WHERE id = ${planId} but does not constrain by tenant_id. This allows updating a plan row belonging to any tenant by id, violating tenant isolation.
  - _Why false positive:_ appendExecutionLog is an internal worker helper called during plan execution with server-side planId, not directly from untrusted request input.
- **server/plan-executor.ts:513** _(internal-worker-pk)_ — Claiming a plan uses UPDATE plans ... WHERE id = ${planId} AND status = 'approved' RETURNING tenant_id, ... but does not include a tenant_id constraint. Without explicit tenant scoping an attacker could cause the executor to claim/operate on another tenant's plan ID.
  - _Why false positive:_ executePlan is a server-side worker claiming a plan by globally unique primary key; planId is used as internal job identity, not shown as direct cross-tenant request input here.
- **server/plan-executor.ts:527** _(upstream-verified)_ — When a claimed plan has no steps the code runs UPDATE plans SET status = 'failed' WHERE id = ${planId} without constraining by tenant_id. This can change someone else's tenant-scoped plan state by id.
  - _Why false positive:_ This failure update occurs only after the immediately preceding claim RETURNING row for the same planId inside the same worker flow, so ownership/context was established earlier in the call path.
- **server/plan-executor.ts:725** _(internal-worker-pk)_ — Final plan status update uses UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} without tenant_id in the WHERE clause, so plan status and execution_log can be written for any tenant's plan by id.
  - _Why false positive:_ Server-invoked executor updates a single plan by server-side planId; tenant context is already loaded as row.tenant_id and not taken from a request boundary here.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-handling cleanup issues UPDATE plans SET status = 'failed' WHERE id = ${planId} AND status = 'executing' without tenant_id filtering. This may mark a different tenant's plan as failed if IDs collide or inputs are tampered with.
  - _Why false positive:_ Fatal-error cleanup in the same internal executor path updates one server-selected plan by primary key, not from untrusted tenant input.
- **server/plan-executor.ts:775** _(system-sweep)_ — resumeStuckPlans updates plans globally: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This performs cross-tenant writes without tenant scoping and can reset executing plans for all tenants indiscriminately.
  - _Why false positive:_ Boot-time recovery intentionally sweeps stale/pending plans across tenants, only mutating status fields and returning ids for internal restart scheduling.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write against a tenant-scoped table without constraining tenant_id. This can modify other tenants' rows if ids collide or were tampered.
  - _Why false positive:_ Task ids come from a prior SELECT on heartbeat_tasks scoped by ht.tenant_id = tenantId in the same call path; update acts on those claimed rows.
- **server/process-governor.ts:641** _(upstream-verified)_ — UPDATE agent_desks ... WHERE id = ${desk.id} updates a tenant-scoped table without tenant_id constraint (resetting desks by id only).
  - _Why false positive:_ Desk ids are sourced from an earlier SELECT on agent_desks with ad.tenant_id = tenantId in the same branch, so ownership is established upstream.
- **server/research-engine.ts:158** _(system-sweep)_ — UPDATE on research_sessions in cleanupZombieSessions has no tenant_id constraint — this will mark stale 'running' sessions completed across all tenants.
  - _Why false positive:_ Zombie-session cleanup is a cross-tenant housekeeping sweep that only marks stale running sessions completed and logs ids internally.
- **server/research-engine.ts:390** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() lacks tenant_id — this will delete autoresearch knowledge for all tenants.
  - _Why false positive:_ Expired autoresearch knowledge deletion is a platform-wide housekeeping purge across tenants and does not return or mix tenant data.
- **server/routes.ts:4330** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_notes' omits tenant_id (INSERT INTO project_notes (project_id, note, author) VALUES (...)). tenant_id must be stored on insert to enforce isolation.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via project_id to projects
- **server/sculptor.ts:94** _(internal-worker-pk)_ — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This can modify a session belonging to another tenant.
  - _Why false positive:_ updates freshly inserted sculptor_sessions row by server-generated session.id within same internal flow, not from untrusted request id
- **server/sculptor.ts:101** _(internal-worker-pk)_ — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}). This can modify a session belonging to another tenant.
  - _Why false positive:_ updates newly created session by server-generated primary key in same internal execution path
- **server/sculptor.ts:162** _(internal-worker-pk)_ — UPDATE sculptor_sessions performed without a tenant_id constraint (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). This terminal update (status/result/completed_at) can affect another tenant's session.
  - _Why false positive:_ background monitor updates the specific session created/launched internally using carried sessionId, not caller-supplied cross-tenant input
- **server/second-opinion.ts:407** _(internal-worker-pk)_ — SETTLE operation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}) with no tenant_id constraint. agent_cost_ledger is tenant-scoped, so this may modify another tenant's ledger row.
  - _Why false positive:_ best-effort settle updates a reservation row by internal reservationId from server-created ledger reservation, not request-supplied id
- **server/second-opinion.ts:424** _(internal-worker-pk)_ — RELEASE operation updates agent_cost_ledger by id only (UPDATE agent_cost_ledger SET cost_usd='0' ... WHERE id = ${reservationId}) with no tenant_id constraint. This can affect another tenant's reservation.
  - _Why false positive:_ release targets server-created reservation by internal primary key, not an untrusted cross-tenant boundary value
- **server/seed.ts:2860** _(system-sweep)_ — UPDATE research_programs SET model = ... WHERE id = ${p.id} updates a tenant-scoped table by id only, with no tenant_id constraint. This can modify rows belonging to other tenants.
  - _Why false positive:_ boot-time seed reconciliation sweeps all active research_programs and only normalizes model field per row without exposing mixed tenant data
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) is executed without supplying tenant_id. project_conversations is tenant-scoped; the insert will create rows with no tenant association (or default) and may link resources across tenants.
  - _Why false positive:_ project_conversations has no tenant_id column and serves as link table isolated transitively through tenant-scoped project/conversation parents
- **server/seed.ts:3205** _(system-sweep)_ — SELECT from tenant-scoped table `conversations` has no tenant_id filter. The query selects conversations across all tenants and later inserts links for returned rows without verifying ownership.
  - _Why false positive:_ Boot-time backfill/housekeeping loop intentionally scans all tenants and only repairs missing link rows, not tenant-facing data exposure.
- **server/seed.ts:3213** _(system-sweep)_ — INSERT into `project_conversations` uses project_id and conversation_id derived from prior SELECTs without verifying tenant ownership or including tenant_id. project_conversations has no tenant_id column in this schema, so the insert can link resources across tenants.
  - _Why false positive:_ Insert is part of the same boot-time global backfill that repairs join-table links row-by-row and does not return mixed tenant data.
- **server/seed.ts:4459** _(system-sweep)_ — UPDATE on tenant-scoped table `memory_entries` (REPLACE operation) runs without any tenant_id constraint. This will modify memory rows across all tenants.
  - _Why false positive:_ Platform-wide maintenance normalizer intentionally updates housekeeping/content-correction across tenants without exposing mixed data.
- **server/seed.ts:4656** _(upstream-verified)_ — UPDATE on tenant-scoped table `agent_knowledge` uses WHERE id = ${rows[0].id} but does not include tenant_id = <tenant>. Per the project's invariant, tenant-scoped writes must be constrained by tenant_id, not only by id.
  - _Why false positive:_ The id being updated was selected immediately above with tenant_id = 1, so ownership is enforced earlier in the same call path.
- **server/self-reflection.ts:366** _(upstream-verified)_ — UPDATE on tenant-scoped table memory_entries is performed using only WHERE id = <id> with no tenant_id constraint. This allows an attacker or race to update a row belonging to another tenant.
  - _Why false positive:_ The row id used in the update comes from a prior SELECT on memory_entries constrained by tenant_id = tenantId.
- **server/skillify.ts:221** _(global-table)_ — SELECT from `personas` reads persona by id with .where(eq(personas.id, conv[0].personaId)) but does not verify persona.tenant_id == tenantId. This allows reading a persona belonging to another tenant.
  - _Why false positive:_ Referenced schema says personas has no tenant_id column, so this lookup is not a tenant-scoped read.
- **server/skills-marketplace.ts:213** _(global-table)_ — INSERT INTO skills (...) VALUES (...) — the INSERT omits tenant_id. Per the invariant all INSERTs into tenant-scoped tables must set tenant_id explicitly. This will create rows with no tenant linkage or rely on DB defaults that do not exist.
  - _Why false positive:_ Referenced schema says skills has no tenant_id column, so omitting tenant_id on insert matches a global table design.
- **server/skills-marketplace.ts:224** _(global-table)_ — SELECT * FROM skills WHERE id = ${skillId} (exportSkill) executed without tenant filter. Reading tenant-scoped skills by id without scoping can leak other tenants' skill data.
  - _Why false positive:_ skills table is schema-noted as not tenant-scoped; unscoped read is consistent with global skills
- **server/skills-marketplace.ts:252** _(global-table)_ — INSERT INTO skills (...) VALUES (...) in importSkill omits tenant_id column. This writes tenant-scoped table without setting tenant_id which violates the invariant and can create cross-tenant pollution.
  - _Why false positive:_ skills has no tenant_id column, so insert without tenant_id matches global table design
- **server/stability-watchdog.ts:177** _(global-table)_ — SELECT FROM heartbeat_logs to detect flaky tasks (recentLogs) has no tenant filter. The query aggregates errors across all tenants, and subsequent actions (disabling tasks via storage.updateHeartbeatTask) may disable tasks across tenants or make wrong cross-tenant decisions.
  - _Why false positive:_ heartbeat_logs is schema-noted as not tenant-scoped; watchdog aggregation is over a global table
- **server/stability-watchdog.ts:211** _(system-sweep)_ — DELETE FROM heartbeat_tasks WHERE enabled = false AND type = 'delegation' ... is executed with no tenant constraint. This can delete disabled delegation tasks across all tenants.
  - _Why false positive:_ maintenance purge deletes stale disabled tasks across tenants and only emits aggregate counts, not tenant data
- **server/stability-watchdog.ts:225** _(system-sweep)_ — DELETE FROM event_log WHERE ... is executed without tenant scoping. This will remove event_log rows across all tenants rather than per-tenant.
  - _Why false positive:_ global housekeeping deletes old processed/failed event rows and only returns aggregate count, with no tenant-facing data mixing
- **server/stability-watchdog.ts:259** _(global-table)_ — DELETE FROM heartbeat_logs WHERE created_at < ... is executed without tenant scoping (pruneOldLogs). This will prune heartbeat logs for all tenants indiscriminately.
  - _Why false positive:_ heartbeat_logs has no tenant_id column; pruning old global logs is expected
- **server/tool-learning.ts:291** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` updates implementation using only the id in the WHERE clause (.where(eq(customTools.id, tool.id))) with no tenant_id constraint. This allows cross-tenant mutation if an attacker can target an id.
  - _Why false positive:_ tool row was looked up earlier in same call path with tenant_id constraint, so id-based update applies to that verified tenant-owned row
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE on tenant-scoped table `custom_tools` increments usageCount using only id in the WHERE clause (.where(eq(customTools.id, tool.id))) with no tenant_id constraint, allowing cross-tenant mutation.
  - _Why false positive:_ executeCustomTool first selects tool by name and tenant_id, then updates that verified row by its id
- **server/tools.ts:2876** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without stamping tenant context, creating risk of cross-tenant linkage or failing DB constraints if tenant_id is required.
  - _Why false positive:_ project_conversations has no tenant_id and insert occurs only after scoped conversation/project ownership check, so isolation is transitive via parent rows
- **server/tools.ts:2980** _(fk-transitive)_ — INSERT into project_files omits tenant_id. The handler inserts a project_files row (project_id, file_name, file_path, ...) without explicitly passing tenant_id, violating the invariant that tenant-scoped INSERTs must include the caller's tenant.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by verified tenant-owned projects row via project_id
- **server/tools.ts:2994** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The code inserts (project_id, note, author) without tenant context, risking cross-tenant contamination if project_notes is tenant-scoped.
  - _Why false positive:_ project_notes has no tenant_id and insert is tied to a project already checked with tenant_id
- **server/tools.ts:3018** _(fk-transitive)_ — INSERT into project_conversations (link_conversation path) omits tenant_id. The handler inserts a project_conversations row for a caller-supplied conversationId but the INSERT does not set tenant_id (even though the code earlier asserts the conversation belongs to the tenant).
  - _Why false positive:_ project_conversations has no tenant_id and links verified tenant-owned project to verified same-tenant conversation
- **server/tools.ts:6899** _(fk-transitive)_ — INSERT into tenant-scoped table 'project_files' does not include tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without setting tenant_id even though project_files is tenant-scoped and INSERTs must include tenant_id.
  - _Why false positive:_ project_files has no tenant_id; assertProjectInTenant verifies parent project ownership before insert
- **server/tools/domains/files/handlers.ts:837** _(fk-transitive)_ — INSERT into project_files omits tenant_id — INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)
  - _Why false positive:_ project_files has no tenant_id and projId is derived from conversation joined to tenant-owned project
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys executed without a tenant_id filter; this returns provider API key rows across all tenants (potential exposure of sensitive secrets)
  - _Why false positive:_ provider_keys schema shows no tenant_id and this admin-only audit returns only provider and key length
- **server/tools/domains/strategic-memory/handlers.ts:92** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses WHERE id = ${id} with NO tenant_id constraint, allowing cross-tenant row updates if an id is abused or races occur.
  - _Why false positive:_ id being updated comes directly from prior SELECT on memory_entries constrained by tenant_id and persona_id in same path
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses WHERE id = ANY(${ids}) with NO tenant_id constraint, allowing last_accessed/access_count bump for rows in other tenants.
  - _Why false positive:_ ids array is built only from rows previously selected with tenant_id and persona_id filters in same function
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE against tenant-scoped table memory_entries uses WHERE id = ${id} with NO tenant_id constraint (recordStrategicWin path), permitting cross-tenant modification of memory rows.
  - _Why false positive:_ updated id originates from immediately prior tenant-scoped SELECT on memory_entries for same tenant/persona
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT count(*)::int AS n FROM messages WHERE created_at > ... runs without a tenant_id constraint — messages are typically tenant-scoped and contain sensitive user data, so this reads across all tenants.
  - _Why false positive:_ platform-wide wiring telemetry uses only aggregate cross-tenant message counts and returns no tenant data
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments performs a SELECT on the tenant-scoped `experiments` table without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This can read rows from other tenants.
  - _Why false positive:_ Heartbeat maintenance sweep intentionally scans running experiments across tenants and then handles each row with its own tenant_id for scoped updates.
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting issues a SELECT on the tenant-scoped `ai_insights` table without a tenant_id filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ...). This reads insights across all tenants.
  - _Why false positive:_ Durability retry sweep intentionally processes pending insights across tenants and carries each row's tenant_id forward per-row without mixing output to tenants.
- **server/agentic-engines.ts:347** _(global-table)_ — gatherOperationalData queries heartbeat_logs without scoping by tenant (SELECT ... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). This reads logs across tenants and can leak tenant data into the operational analysis.
  - _Why false positive:_ heartbeat_logs is marked tenant_id column NO, so this is a platform-global log table rather than tenant-scoped data.
- **server/agentic-engines.ts:519** _(global-table)_ — runOptimizationEngine queries heartbeat_logs in a non-tenant-scoped way (SELECT task_name, status, COUNT(*) ... FROM heartbeat_logs GROUP BY ...). This reads aggregated heartbeat data across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and is a platform-global table; this query only reads aggregated operational metrics.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — runBudgetEnforcement() executes `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` against a tenant-scoped table with no tenant_id constraint. This performs an unfiltered cross-tenant read on a tenant-scoped table, violating the codebase invariant that every read/write must be constrained to the caller's tenant.
  - _Why false positive:_ Heartbeat budget enforcement is a platform-wide sweep over active budgets, with each row processed using its own tenant_id and emitting per-tenant events only.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query loads agent_runs / agent_approvals rows without constraining by tenant_id — cross-tenant read across all tenants.
  - _Why false positive:_ Maintenance sweep resumes pending approvals across tenants but processes each candidate row with its carried tenant_id and does not return mixed tenant data.
- **server/agentic/runs.ts:70** _(internal-worker-pk)_ — completeRun later selects the run by id (db.select().from(agentRuns).where(eq(agentRuns.id, runId))). That SELECT lacks tenant_id filtering and can leak another tenant's run data to the caller.
  - _Why false positive:_ Read by globally unique run id occurs inside a server-side completion worker path, not from an untrusted request boundary, to drive internal distillation.
- **server/auto-transcript.ts:128** _(system-sweep)_ — Backfill SELECT scans conversations without scoping by tenant (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c ...). This reads a tenant-scoped table across all tenants.
  - _Why false positive:_ Backfill job is a platform-wide housekeeping sweep that carries each conversation row's tenant_id into per-row processing and transcript generation.
- **server/auto-transcript.ts:153** _(system-sweep)_ — Query selecting linked project_conversations/conversations (SELECT DISTINCT pc.conversation_id, pc.project_id, c.tenant_id ...) has no tenant restriction. This reads tenant-scoped rows across tenants.
  - _Why false positive:_ Backfill/reconciliation sweep intentionally scans cross-tenant links, then updates and processes each row with its own tenant_id as housekeeping.
- **server/auto-transcript.ts:171** _(system-sweep)_ — SELECT to find missing links (SELECT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...) has no tenant filter and reads tenant-scoped rows across tenants.
  - _Why false positive:_ Global backfill/housekeeping loop repairs missing project_conversations links per row and does not return tenant data to a tenant-facing surface.
- **server/business-tools.ts:84** _(fk-transitive)_ — INSERT into invoice_items omits tenant_id — writing to a tenant-scoped table without setting tenant_id breaks the app-level isolation invariant (or may allow cross-tenant linkage if invoice_items is tenant-scoped).
  - _Why false positive:_ invoice_items has no tenant_id and is a child table isolated through tenant-scoped parent invoices via invoice_id.
- **server/capability-review.ts:490** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `capability_reviews` performs a write with no tenant constraint (WHERE id = ${pending.reviewId} AND reused = false). This allows a caller/process that can trigger markReviewReused to flip rows belonging to other tenants (tenant isolation not enforced).
  - _Why false positive:_ Internal async worker marks a row by server-stored reviewId from in-memory pendingReuse state, not directly from an untrusted request boundary.
- **server/chat-engine.ts:572** _(upstream-verified)_ — SELECT from tenant-scoped table `project_files` without an explicit tenant filter (WHERE tenant_id = ...). The query uses only project_id, relying on prior verification of the project in JS instead of enforcing tenant scoping in the SQL.
  - _Why false positive:_ Project ownership is verified immediately above with SELECT from projects by id and tenant_id before using project_id to read child project_files.
- **server/chat-engine.ts:583** _(upstream-verified)_ — SELECT from tenant-scoped table `project_notes` without an explicit tenant filter (WHERE tenant_id = ...). The query filters only by project_id.
  - _Why false positive:_ Project ownership is verified immediately above with tenant-scoped projects lookup before reading child project_notes by project_id.
- **server/chat-engine.ts:767** _(upstream-verified)_ — Correlated subquery selects from `messages` (SELECT content FROM messages WHERE conversation_id = c.id ...) inside a larger query but the subquery lacks an explicit tenant_id condition. Even though the outer query filters conversations by tenant, the messages subquery itself has no tenant_id WHERE clause.
  - _Why false positive:_ Subquery is correlated to outer conversations rows already filtered by c.tenant_id = tid, so message reads are scoped to tenant-owned conversations in the same SQL.
- **server/chat-engine.ts:772** _(upstream-verified)_ — Correlated subquery selects from `compaction_archives` (SELECT summary FROM compaction_archives WHERE conversation_id = c.id ...) without an explicit tenant_id constraint in the subquery.
  - _Why false positive:_ Subquery is correlated to outer conversations rows already filtered by c.tenant_id = tid, so archive reads are limited to tenant-owned conversations in the same SQL.
- **server/chat-engine.ts:831** _(fk-transitive)_ — Scalar subquery counts `project_files` with (SELECT COUNT(*) FROM project_files WHERE project_id = p.id) but lacks tenant scoping. project_files appears tenant-scoped and should not be read without tenant_id in SQL.
  - _Why false positive:_ project_files has no tenant_id and is a child table of tenant-scoped projects; counting by project_id under tenant-scoped projects is transitively isolated.
- **server/data-protection.ts:111** _(system-sweep)_ — SELECT of expired soft-deleted conversations runs without tenant scoping. permanentlyPurgeSoftDeleted selects conversations with deleted_at < cutoff across all tenants.
  - _Why false positive:_ permanentlyPurgeSoftDeleted is a cross-tenant housekeeping sweep; each row carries its own tenant_id and only purge/backup maintenance occurs.
- **server/data-protection.ts:318** _(fk-transitive)_ — SELECT from project_notes is done by project_id only and does not include an explicit tenant_id filter. The code iterates tenant-owned projects but issues queries against project_notes without tenant predicate.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects already filtered by tenant_id.
- **server/data-protection.ts:329** _(fk-transitive)_ — SELECT from project_files by project_id omits tenant scoping. The code reads project_files WHERE project_id = ${proj.id} with no tenant predicate.
  - _Why false positive:_ project_files has no tenant_id and is read via project_id from projects already scoped to the tenant.
- **server/doc-collections.ts:165** _(upstream-verified)_ — ADD DOCUMENT: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} is executed with NO tenant_id constraint. collection_id alone doesn't enforce tenant isolation (no FK/RLS guarantee).
  - _Why false positive:_ same call path first verifies doc_collections id belongs to provided tenant before deleting chunks for that collectionId/docPath
- **server/doc-collections.ts:294** _(upstream-verified)_ — generateCollectionEmbeddings updates doc_chunks with: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} but does NOT restrict by tenant_id. This permits updating a chunk row without verifying tenant ownership.
  - _Why false positive:_ rows were selected immediately above from doc_chunks with collection_id and tenant_id filter, then updated by those row ids
- **server/email.ts:409** _(system-sweep)_ — backfillEmptyBodies(): SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id runs without tenant_id filtering and iterates rows across tenants. Subsequent UPDATE uses WHERE id = ${row.id} (no tenant_id), so writes may touch tenant-scoped data without tenant constraint.
  - _Why false positive:_ global maintenance backfill scans all tenants and only fills missing body fields per row; no tenant-facing mixed output
- **server/embeddings.ts:688** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillMissingKnowledgeEmbeddings has no tenant_id filter. This reads tenant-scoped rows across tenants.
  - _Why false positive:_ global backfill processes rows individually to add embeddings and does not return or mix tenant content into one tenant-facing output
- **server/embeddings.ts:716** _(system-sweep)_ — SELECT FROM memory_entries in backfillEmbeddingVecs has no tenant_id filter. This reads tenant-scoped rows across tenants.
  - _Why false positive:_ platform-wide vector backfill reads rows across tenants only to populate housekeeping embedding_vec per row
- **server/embeddings.ts:732** _(system-sweep)_ — SELECT FROM agent_knowledge in backfillEmbeddingVecs has no tenant_id filter. This reads tenant-scoped rows across tenants.
  - _Why false positive:_ platform-wide vector backfill for agent_knowledge mutates only embedding_vec per row and emits aggregate counts
- **server/felix-loop.ts:676** _(internal-worker-pk)_ — UPDATE felix_loop_runs sets ended_at/context_summary/etc WHERE id = ${runId} but does not include tenant_id in the WHERE clause.
  - _Why false positive:_ runId is used by internal server-side felix loop worker; update is by server-generated primary key, not untrusted request input
- **server/felix-loop.ts:690** _(internal-worker-pk)_ — Error-path UPDATE on felix_loop_runs (set ended_at,error WHERE id=${runId}) lacks tenant_id constraint.
  - _Why false positive:_ error-path update uses same internal server-generated felix loop run primary key, not a cross-tenant request boundary
- **server/firecrawl.ts:184** _(upstream-verified)_ — db.update(scrapedPages).where(eq(scrapedPages.id, existing[0].id)) updates scraped_pages by id only — no tenant_id constraint on the UPDATE, despite the table being tenant-scoped.
  - _Why false positive:_ Row id comes from a prior SELECT on scraped_pages filtered by tenantId in the same call path.
- **server/firecrawl.ts:307** _(upstream-verified)_ — Within crawl loop, updating scraped_pages uses .where(eq(scrapedPages.id, existing[0].id)) without tenant scoping, allowing potential cross-tenant writes if an id is reused or manipulated.
  - _Why false positive:_ existing[0].id is obtained immediately above from a tenant-scoped SELECT on scraped_pages.
- **server/google-drive.ts:560** _(upstream-verified)_ — ensureProjectFolder updates projects (.where(eq(projects.id, projectId))) without constraining the UPDATE to the tenant — write occurs on a tenant-scoped table without tenant_id in WHERE.
  - _Why false positive:_ Function explicitly loads project, checks project.tenantId === tenantId, then updates that verified project id.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals queries proposals across all tenants (no WHERE tenant_id = ...). This can read tenant-scoped proposal rows globally.
  - _Why false positive:_ Global TTL audit/archival sweep intentionally scans stale proposals across tenants and only archives/statuses them plus emits audit findings.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks queries enabled/stuck tasks with no tenant constraint (no WHERE tenant_id = ...). Heartbeat tasks are typically tenant-scoped and this read can expose tasks across tenants.
  - _Why false positive:_ Global housekeeping sweep for stuck heartbeat tasks; it disables stale tasks and reports counts/findings, not tenant-facing mixed data.
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT from tenant-scoped table 'research_schedules' without constraining by tenant_id (no WHERE tenant_id = ...) — returns rows across all tenants.
  - _Why false positive:_ Heartbeat scheduler intentionally scans due research_schedules across tenants and processes each row using its own tenant_id.
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_schedules' uses WHERE id = ${sched.id} but lacks an explicit tenant_id constraint. Without RLS this can modify another tenant's row if id is known.
  - _Why false positive:_ Maintenance loop updates the same schedule row selected by the global sweep; per-row handling uses row-carried tenant context.
- **server/heartbeat.ts:106** _(system-sweep)_ — UPDATE against tenant-scoped table 'research_schedules' (last_run_at/next_run_at) without tenant_id in WHERE clause.
  - _Why false positive:_ Global heartbeat maintenance updates scheduling fields on the row it is processing; housekeeping only.
- **server/heartbeat.ts:111** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ... WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Same system-wide scheduler loop advancing next_run_at on the current row; housekeeping mutation only.
- **server/heartbeat.ts:119** _(system-sweep)_ — UPDATE research_schedules in error-recovery path uses WHERE id = ${sched.id} with no tenant_id constraint.
  - _Why false positive:_ Error-recovery path in the same maintenance sweep only advances scheduling metadata for the current row.
- **server/heartbeat.ts:658** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' — no tenant_id constraint; reads pending events across all tenants.
  - _Why false positive:_ Heartbeat processes pending events platform-wide, then routes each using that event's own tenant_id; no mixed tenant output here.
- **server/heartbeat.ts:260** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. This updates heartbeat_tasks without verifying tenant ownership of the row.
  - _Why false positive:_ Startup cleanup is a platform-wide housekeeping pass normalizing heartbeat_tasks metadata across rows, not tenant-facing data access.
- **server/lib/action-ledger.ts:268** _(system-sweep)_ — SELECT from tenant-scoped table `action_attempts` with no tenant_id filter (listUnknownAttemptsAllTenants returns rows across all tenants).
  - _Why false positive:_ Code explicitly documents a bounded cross-tenant reconciler sweep where each row carries tenant_id and downstream writes use each row's own tenant.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. Platform invariant requires tenant_id to be provided on tenant-scoped INSERTs (no default); omitting it can misattribute rows or violate DB constraints.
  - _Why false positive:_ project_files has no tenant_id per schema and is a child of tenant-scoped projects via project_id, so tenant isolation is transitive.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. Every INSERT into tenant-scoped tables must explicitly supply tenant_id — columns are NOT NULL with no default.
  - _Why false positive:_ project_notes has no tenant_id per schema and is a child of tenant-scoped projects via project_id, so omitting tenant_id is correct by design.
- **server/lib/scheduled-post-runner.ts:60** _(system-sweep)_ — SELECT/UPDATE against tenant-scoped table `scheduled_posts` performed without any tenant_id constraint (CTE selects due rows across all tenants). This updates rows across tenants from a single system-scoped runner.
  - _Why false positive:_ Global scheduled-post runner intentionally sweeps due rows across tenants; each row carries its own tenant_id and is processed individually, not returned to a tenant-facing mixed surface.
- **server/lib/scheduled-post-runner.ts:164** _(upstream-verified)_ — UPDATE scheduled_posts ... WHERE id = ${row.id} executed without scoping by tenant_id. This writes tenant-scoped rows without verifying the tenant, risking cross-tenant state changes if row ids are misread or attacker-controlled.
  - _Why false positive:_ row.id comes from the immediately preceding CTE that selected and returned the exact scheduled_posts row (including tenant_id); this update is on that locked runner-owned row, not request-derived input.
- **server/lib/scheduled-post-runner.ts:192** _(upstream-verified)_ — Error-path UPDATE scheduled_posts uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id, allowing potential cross-tenant updates in exception handling paths.
  - _Why false positive:_ Error-path update targets the same previously selected runner-owned row by id while still in publishing state; ownership/scoping was established earlier in the same call path.
- **server/memory-graph.ts:66** _(upstream-verified)_ — UPDATE in classifyMemoryCategory increments memory_categories.memory_count (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}) without constraining by tenant_id — allows modifying another tenant's category counts.
  - _Why false positive:_ catId is produced by ensureCategory, which first selects/inserts memory_categories with tenant_id = tenantId in the same call path; the update applies to that verified category row.
- **server/memory-graph.ts:181** _(fk-transitive)_ — SELECT existence check against `memory_links` (SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ...) has no tenant constraint — can detect or act upon links across tenants.
  - _Why false positive:_ memory_links has no tenant_id column and links two memory_entries already selected/scoped within the same tenant, so isolation is transitive through tenant-scoped endpoints.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' without tenant_id filter — reads rows across all tenants.
  - _Why false positive:_ Startup reconciliation intentionally enumerates subscriptions across tenants to reconnect each tenant separately by its own tenant_id; this is maintenance logic, not tenant-facing mixed output.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh issues a SELECT across oauth_subscriptions WHERE is_active = TRUE with no tenant_id filter (iterates all tenants). This reads tenant-scoped rows globally and drives per-tenant refresh operations.
  - _Why false positive:_ Background token refresh intentionally scans active subscriptions platform-wide and then refreshes each tenant independently using row-carried tenant_id; no mixed tenant data is returned.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE — no tenant_id constraint; iterates all tenant rows without per-tenant scoping.
  - _Why false positive:_ Daily force-refresh is a platform-wide housekeeping loop over active subscriptions, operating per row with its own tenant_id and only mutating refresh state/log labels.
- **server/pace-control.ts:85** _(global-table)_ — checkPace queries heartbeat_logs with no tenant_id filter (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND <laneFilter>) — heartbeat_logs is tenant-scoped but this read is unscoped, allowing cross-tenant aggregation.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column; this is a platform-global throttle/pace table by design, so unscoped aggregate reads are expected.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot performs unscoped SELECTs against heartbeat_logs (COUNT and persona aggregation) without filtering by tenant_id, exposing usage data across tenants.
  - _Why false positive:_ heartbeat_logs is documented with no tenant_id and used here for operator/debug pace telemetry across the platform
- **server/pace-control.ts:147** _(global-table)_ — getPaceSnapshot persona aggregation query SELECT ... FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name has no tenant_id filter — returns persona counts across all tenants.
  - _Why false positive:_ persona aggregation reads heartbeat_logs, a non-tenant-scoped global telemetry table
- **server/process-governor.ts:121** _(fk-transitive)_ — Subqueries counting heartbeat_logs (successes/failures) in task_failure_rate lack tenant_id constraint (reads from tenant-scoped heartbeat_logs without scoping by tenant).
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted only for tenant-scoped heartbeat_tasks selected by ht.tenant_id = tenantId via task_id = ht.id
- **server/process-governor.ts:198** _(global-table)_ — Response-time query aggregates heartbeat_logs but does not filter by tenant_id (SELECT AVG(duration_ms) FROM heartbeat_logs ...).
  - _Why false positive:_ heartbeat_logs has no tenant_id and this computes platform-level response-time telemetry from a global table
- **server/process-governor.ts:240** _(global-table)_ — cascading_failures query reads heartbeat_logs without a tenant_id filter (COUNT DISTINCT persona_id FROM heartbeat_logs ...).
  - _Why false positive:_ query reads from heartbeat_logs, which schema marks as non-tenant-scoped global telemetry
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query scans projects and conversations across the DB without tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ... ), exposing cross-tenant data.
  - _Why false positive:_ backfillProjectBrains is a platform-wide backfill/housekeeping routine processing rows across tenants rather than serving tenant-facing output
- **server/project-brain.ts:349** _(system-sweep)_ — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} is executed without tenant_id filtering (conversations are tenant-scoped), allowing cross-tenant reads during backfill.
  - _Why false positive:_ conversation scan occurs inside a global backfill loop for per-project brain generation, not a tenant-facing mixed response
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without tenant scoping, enabling cross-tenant leakage when conversation ids are processed without verifying tenant ownership.
  - _Why false positive:_ message reads are part of the same maintenance backfill and do not return mixed tenant data to a tenant-facing surface
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` uses WHERE id = ${r.id} with NO tenant_id constraint. When the runner executes a global scan it later updates rows by id only, violating the invariant that every write must be constrained to the caller's tenant.
  - _Why false positive:_ global scheduler sweep updates each agent_knowledge row by its own server-selected primary key as housekeeping using row-carried tenant context, not tenant-mixed output
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` uses WHERE id = ${storedId} with NO tenant_id constraint. The code earlier found an existing row by tenant_id, but the subsequent UPDATE targets the row by id only.
  - _Why false positive:_ storedId comes from immediately prior SELECT scoped by tenant_id/persona/category, then updated in same flow
- **server/research-engine.ts:470** _(fk-transitive)_ — INSERT INTO project_notes omits tenant_id. Tenant-scoped table INSERTs must set tenant_id explicitly; otherwise rows may land without tenant scoping or violate NOT NULL constraints.
  - _Why false positive:_ project_notes has no tenant_id and is tenant-isolated through tenant-verified parent project_id
- **server/research-engine.ts:478** _(fk-transitive)_ — Second INSERT INTO project_notes (for each finding) also omits tenant_id, risking cross-tenant writes or schema constraint failures.
  - _Why false positive:_ project_notes has no tenant_id and inserts are tied to assertProjectInTenant-verified project_id
- **server/routes.ts:2312** _(signed-url)_ — SELECT from tenant-scoped delivery_logs without tenant filter in /api/store/order/:sessionId — returns delivery log rows for an arbitrary sessionId supplied by the caller.
  - _Why false positive:_ public order lookup treats Stripe checkout session id as unguessable capability and returns only public-safe fields
- **server/routes/admin-zombie-detector.ts:36** _(system-sweep)_ — SQL query reads from agent_trace_spans without constraining to the caller's tenant. The CTE (`used`) selects from agent_trace_spans with no `tenant_id = ...` filter, so results can span all tenants.
  - _Why false positive:_ explicit platform-admin-only aggregate audit across tenants; outputs usage aggregates, not tenant content
- **server/routes/admin-zombie-detector.ts:54** _(system-sweep)_ — SQL query aggregates conversations (used CTE) without a tenant filter. The query computing persona usage selects from `conversations` but lacks `tenant_id = ...`, so it will aggregate across all tenants.
  - _Why false positive:_ explicit platform-admin-only aggregate persona-usage query across tenants; returns counts/last-used only
- **server/routes/admin.ts:568** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance is executed with NO tenant constraint. If `tool_performance` is a tenant-scoped table, this returns platform-wide data without an explicit tenant filter.
  - _Why false positive:_ admin-only platform-wide health audit computing distinct used tool names, not tenant-facing mixed content
- **server/routes/admin.ts:574** _(system-sweep)_ — Query against `tool_performance` to list failing tools has no tenant filter. The admin endpoint runs `SELECT ... FROM tool_performance WHERE ...` without `tenant_id = ...`, potentially exposing cross-tenant performance data.
  - _Why false positive:_ admin-only platform-wide failing-tool health query for housekeeping/ops visibility
- **server/routes/admin.ts:587** _(system-sweep)_ — Query selects rows from `ai_insights` (status = 'new') without any tenant constraint. `ai_insights` appears to be tenant-scoped but is read here with no `tenant_id = ...` filter.
  - _Why false positive:_ admin-only platform-wide inspection of stuck ai_insights for operational triage
- **server/routes/admin.ts:598** _(system-sweep)_ — Query against `heartbeat_tasks` (disabled/failing tasks) omits a tenant filter. `heartbeat_tasks` is likely tenant-scoped but is read without `tenant_id = ...`.
  - _Why false positive:_ admin-only platform-wide heartbeat task health query for maintenance/ops
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on 'conversations' omits tenant filter. The statement 'UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}' updates a tenant-scoped table using only the conversation id, without constraining by tenant_id.
  - _Why false positive:_ conv was just created with tenantId in this handler, and project ownership was verified by SELECT ... FROM projects WHERE id = projectId AND tenant_id = tenantId before the update.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into 'project_conversations' does not set tenant_id. The statement 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})' writes into a tenant-scoped mapping table but omits tenant_id in the inserted columns.
  - _Why false positive:_ project_conversations has no tenant_id and is a child mapping isolated transitively through tenant-scoped projects/conversations already verified in this flow.
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on 'projects' omits tenant filter. The statement 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' updates a tenant-scoped table by id only, missing tenant_id qualification.
  - _Why false positive:_ The same call path verified project ownership with WHERE id = projectId AND tenant_id = tenantId immediately above, so this timestamp-only update targets a validated tenant-owned row.
- **server/routes/conversations.ts:227** _(fk-transitive)_ — INSERT into 'project_notes' lacks tenant context. The statement 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' creates a row in a tenant-scoped table but does not include tenant_id (or otherwise validate that the inserted row will be associated with the caller's tenant).
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; projId is derived from a join requiring p.tenant_id = conv.tenantId/tenantId.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE on 'inbox_messages' performs 'UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}' without tenant_id constraint. This is an in-handler write that omits tenant scoping even though the row was earlier selected with tenant_id filtering.
  - _Why false positive:_ The message row was selected in the same handler with WHERE id = msgId AND tenant_id = tenantId, and the update uses that fetched row's id only to mark it read.
- **server/seed.ts:35** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions is executed with no tenant_id constraint (tenant-scoped table). This reads global session data across tenants.
  - _Why false positive:_ Boot-time snapshot import intentionally checks global research_sessions state for maintenance/seeding and does not return tenant data to any tenant-facing surface.
- **server/seed.ts:2855** _(system-sweep)_ — SELECT id, name, model FROM research_programs WHERE is_active = true has no tenant_id filter; research_programs is tenant-scoped. This reads programs for all tenants.
  - _Why false positive:_ Boot-time fixer scans all active research_programs to repair invalid model fields platform-wide; this is housekeeping across tenants, not tenant-facing data exposure.
- **server/seed.ts:3205** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... checks for missing project_conversations rows but lacks any tenant_id constraint on conversations/project_conversations. These tables are tenant-scoped and this query can read cross-tenant data.
  - _Why false positive:_ Boot-time backfill intentionally scans conversations across tenants to restore missing project_conversations links, mutating only linkage housekeeping rows and not returning mixed tenant content.
- **server/skill-seeker.ts:708** _(global-table)_ — Auto-creation of skills calls executeTool("manage_skills", { ..., _tenantId: ADMIN_TENANT_ID }) — new skills are created under the admin tenant rather than the requesting tenant, risking cross-tenant ownership and privilege issues.
  - _Why false positive:_ schema notes skills has no tenant_id column, so creating/querying skills without tenant scoping matches an intentionally global table design.
- **server/skills-marketplace.ts:207** _(global-table)_ — SELECT id FROM skills WHERE name = ${template.name} is executed with no tenant filter. If `skills` is tenant-scoped this can collide across tenants or leak presence of a skill across tenants.
  - _Why false positive:_ skills has no tenant_id column per schema, so existence check by name is against a global/shared table, not a cross-tenant leak.
- **server/storage.ts:295** _(upstream-verified)_ — attachCitationMetadata issues raw SELECT FROM messages WHERE id IN (...) without any tenant_id constraint. messages is tenant-scoped so this can return citations from other tenants.
  - _Why false positive:_ ids come only from msgs already fetched with tenant-scoped getMessages/getMessagesPaginated queries, so the follow-up citation lookup is constrained to those same tenant-owned message ids.
- **server/storage.ts:621** _(upstream-verified)_ — evictOldestConversationFacts runs an UPDATE conversation_facts SET status='expired' WHERE id IN (victims...) but the UPDATE does not constrain tenant_id. Although victim ids were selected earlier with a tenant filter, the subsequent UPDATE uses only ids and can affect rows outside the tenant if ids are tampered with.
  - _Why false positive:_ Victim ids are selected immediately above with tenant_id = tenantId and then updated from that DB-derived id list in the same call path.
- **server/storage.ts:838** _(system-sweep)_ — archiveExpiredMemories issues a global UPDATE memory_entries SET status='archived' WHERE ... without tenant scoping. This will archive rows across all tenants.
  - _Why false positive:_ Global archival job mutates only housekeeping status on expired memory rows across tenants and returns aggregate count/ids, not tenant-facing mixed data.
- **server/storage.ts:855** _(system-sweep)_ — archiveStaleMemories updates memory_entries SET status='archived' WHERE ... with no tenant filter (global write across tenant-scoped table).
  - _Why false positive:_ Global stale-memory archival is a maintenance sweep that only updates status fields across tenants without exposing mixed tenant data.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` is performed without constraining by tenant_id (where(eq(customTools.name, parsed.name)) only). This can read rows across tenants.
  - _Why false positive:_ Tool name is deterministically rewritten with tenantToolName(..., tenantId) before lookup, so the queried name is already tenant-scoped.
- **server/tool-learning.ts:155** _(upstream-verified)_ — SELECT from tenant-scoped table `custom_tools` by id is performed without a tenant_id constraint (.where(eq(customTools.id, inserted.id))). This can expose/return rows belonging to other tenants if mis-used.
  - _Why false positive:_ The id comes from the row just inserted with tenantId in the same flow; selecting by inserted.id only re-reads that same tenant-owned row.
- **server/tools.ts:4514** _(fk-transitive)_ — INSERT into project_notes uses project_id but does not include tenant_id. If project_notes is a tenant-scoped table (tenant_id NOT NULL), this INSERT will create rows with no explicit tenant binding, violating the invariant that every INSERT must pass tenantId explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively tenant-scoped through verified project_id after assertProjectInTenant.
- **server/tools.ts:4518** _(fk-transitive)_ — INSERT into project_files omits tenant_id and inserts only project_id/file metadata. If project_files is tenant-scoped, the row will be created without an explicit tenant_id (columns are expected NOT NULL per invariant).
  - _Why false positive:_ project_files has no tenant_id column and is transitively tenant-scoped through verified project_id after assertProjectInTenant.
- **server/tools.ts:6365** _(fk-transitive)_ — INSERT into project_files (in generate_audio) writes project file metadata using params.project_id but does not include tenant_id in the INSERT. This can violate the requirement that INSERTs into tenant-scoped tables must explicitly set tenant_id.
  - _Why false positive:_ project_files has no tenant_id column and insert is tied to a project_id verified by assertProjectInTenant.
- **server/tools.ts:6899** _(fk-transitive)_ — INSERT into project_files (in create_slideshow_video) omits tenant_id and writes only project_id and file metadata. If project_files requires tenant_id, this violates the invariant requiring explicit tenant stamping on INSERTs.
  - _Why false positive:_ project_files has no tenant_id column and insert is tied to a project_id verified by assertProjectInTenant.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE executed against browser_workflows without constraining tenant (no tenant_id in WHERE) — UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}
  - _Why false positive:_ workflow is first loaded with id/name plus tenant_id = tenantId, then updated by that trusted row's id in the same call path.
- **server/tools/domains/files/handlers.ts:840** _(upstream-verified)_ — UPDATE executed against projects without tenant scoping — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}
  - _Why false positive:_ projId is derived from a prior join requiring projects.tenant_id = tenantId, so the subsequent project timestamp update targets a tenant-verified project.
- **server/tools/domains/knowledge/handlers.ts:85** _(upstream-verified)_ — UPDATE against tenant-scoped table `knowledge_triples` omits tenant filter — WHERE id = ${row.id} has no tenant_id constraint, violating the invariant that every write must be constrained to the caller's tenant.
  - _Why false positive:_ Rows being updated were selected immediately above from knowledge_triples with tenant_id = tenantId and valid_until IS NULL; update iterates those verified rows.
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — COUNT(DISTINCT provider) FROM provider_keys executed with no tenant_id constraint; even though run only for tenantId===1, the query reads provider_keys across tenants
  - _Why false positive:_ provider_keys is schema-noted as non-tenant-scoped and query is gated to admin tenant for platform-wide aggregate
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT tenant_id FROM `conversations` WHERE id = ${info.conversationId} is performed without scoping by tenant_id. Reading the conversations table by id with no tenant constraint can expose existence/ownership across tenants.
  - _Why false positive:_ same call path verifies conversationId belongs to claimed tenantId and uses unscoped read only for ownership check, failing closed on mismatch/ambiguity
- **server/video-job-runner.ts:228** _(system-sweep)_ — recoverStaleVideoJobs performs a DB UPDATE on videoJobs across all tenants without scoping by tenant_id (it marks matching stale rows as failed globally). This mutates tenant-scoped data for every tenant and can incorrectly change other tenants' job states.
  - _Why false positive:_ global stale-job recovery is a housekeeping sweep updating status fields only, with no tenant data returned to tenant-facing surface
- **server/wiring-invariants.ts:366** _(system-sweep)_ — Using sql.raw to SELECT COUNT/MAX FROM ${spec.table} (e.g. tool_performance, knowledge_nudges) without constraining by tenant_id. Several SUBSYSTEM_TABLES are tenant-scoped and are being aggregated across tenants.
  - _Why false positive:_ platform-wide subsystem freshness probe is observability/housekeeping aggregate over tables and emits only aggregate findings, not tenant content
- **server/wiring-invariants.ts:535** _(system-sweep)_ — checkMemoryGrowth uses sql.raw to SELECT COUNT and pg_total_relation_size FROM memory tables (e.g. messages, agent_knowledge, graph_memory) without tenant filtering; many of these tables are tenant-scoped.
  - _Why false positive:_ memory-growth check is platform-wide observability using only row counts and relation sizes, with no tenant content returned or mixed into tenant output
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*)::int AS cnt FROM graph_memory runs without filtering by tenant_id. graph_memory is likely tenant-scoped, so this is aggregating across tenants.
  - _Why false positive:_ Global housekeeping invariant check counting rows across all tenants; only emits aggregate warning text and returns no tenant data.
- **server/wiring-invariants.ts:183** _(system-sweep)_ — Subselects in checkOutcomeCanary query tenant-scoped tables (research_experiments, code_proposals and research_sessions) without tenant_id constraints (e.g. WHERE e.program_id = p.id ...). These reads aggregate per-program activity across tenants.
  - _Why false positive:_ Platform-wide canary over nightly programs for maintenance telemetry; aggregates counts/max timestamps only and emits invariant findings, not tenant-facing mixed row data.
