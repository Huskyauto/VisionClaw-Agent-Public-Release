# Tenant-Isolation Audit Report

_Generated 2026-07-23T13:30:10.546Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **562/562** file(s) audited · chunks 54/54 OK (0 failed)
- Findings: **285** (CRITICAL 97 · HIGH 155 · MEDIUM 31 · LOW 2)
- Precision second-pass (`gpt-5.4`): **174** severe finding(s) suppressed as confirmed false positives — they do NOT count toward pass/fail (see below).

### CRITICAL (97)

- **server/agent-activity.ts:122** — UPDATE agent_activity is executed without constraining tenant_id — completeActivity(activityId, ...) can update rows across tenants.
  - _Fix:_ Require/accept tenantId in completeActivity and add a tenant filter to the update (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))). Failing that, at minimum validate ownership before updating.
- **server/agentic-engines.ts:243** — UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) is executed without tenant_id filtering — this will modify rows for all tenants.
  - _Fix:_ Make this an explicit per-tenant update (add AND tenant_id = ${tenantId}) or iterate tenants and run the UPDATE per-tenant. Never run multi-tenant updates that affect tenant-scoped tables without tenant scoping.
- **server/agentic-features.ts:721** — UPDATE outreach_enrollments sets reply_classification WHERE id = ${row.id} but does not include tenant_id in the WHERE clause — allows modifying another tenant's enrollment row by id.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or verify ownership before updating to prevent cross-tenant mutation.
- **server/agentic/approvals.ts:135** — Bulk UPDATE agentApprovals to set status='expired' WHERE status='pending' AND expiresAt < now() is executed without tenant scoping — expires approvals across all tenants in one operation.
  - _Fix:_ Scope expiry updates to a specific tenant (accept tenantId and add eq(agentApprovals.tenantId, tenantId)) or iterate tenants and run the expiry per-tenant. Avoid global bulk updates on tenant-scoped tables.
- **server/agentic/resume-worker.ts:66** — SELECT query scans tenant-scoped tables (agent_runs, agent_approvals) without constraining to a caller tenant_id. This returns rows across tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add AND r.tenant_id = ${tenantId} or accept a tenantId param) or otherwise run the worker under an explicit, auditable admin context. Every read against tenant-scoped tables must include an application-level WHERE tenant_id = <tenant> filter.
- **server/agentic/runs.ts:39** — UPDATE agent_runs in appendStep() uses WHERE id = runId only and lacks a tenant_id constraint. This allows mutation of another tenant's run if runId is known.
  - _Fix:_ Add tenant scoping to the WHERE clause, e.g. .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). If tenantId is not available to the caller, require it as a parameter and enforce it.
- **server/agentic/runs.ts:51** — UPDATE agent_runs in updateRunState() uses WHERE id = runId only and omits tenant scoping, allowing cross-tenant updates.
  - _Fix:_ Require and apply tenantId when updating run state: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure callers supply the correct tenantId.
- **server/agentic/runs.ts:55** — UPDATE agent_runs in replaceRunState() filters only on id (eq(agentRuns.id, runId)) and does not include tenant_id, enabling cross-tenant state replacement.
  - _Fix:_ Include tenant filter: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Make tenantId an explicit parameter if needed.
- **server/agentic/runs.ts:64** — UPDATE agent_runs in completeRun() uses WHERE id = runId only and lacks tenant_id constraint — a caller could mark another tenant's run completed.
  - _Fix:_ Constrain the update to the tenant: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Propagate tenantId to callers where required.
- **server/agentic/runs.ts:94** — UPDATE agent_runs in failRun() updates by run id only (eq(agentRuns.id, runId)) without tenant scoping, permitting cross-tenant mutation of run rows.
  - _Fix:_ Add tenant condition to the WHERE clause: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Make tenantId explicit in the API surface.
- **server/agentic/runs.ts:102** — UPDATE agent_runs in pauseRun() filters only on id (and not tenant_id), allowing a pause to apply to a run belonging to a different tenant.
  - _Fix:_ Require tenantId and apply it in the WHERE: .where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId))). Ensure all code paths call pauseRun with the correct tenant.
- **server/chat-engine.ts:4332** — storage.attachCitations((assistantMsgRow as any).id, collectedCitations) is invoked without passing any tenant identifier. If attachCitations performs INSERTs into a tenant-scoped table, those writes may be created with no tenant_id (or with an unvalidated association), violating tenant isolation and potentially causing cross-tenant leakage or NOT NULL failures.
  - _Fix:_ Ensure tenant context is supplied and enforced for this write. Either (a) change the call to pass the tenant (e.g. storage.attachCitations(messageId, citations, tenantIdForScope)), or (b) have storage.attachCitations validate ownership of the message id (SELECT ... WHERE id = ? AND tenant_id = ?) before any INSERT/UPDATE and include tenant_id on all inserts. Audit attachCitations implementation to guarantee every INSERT/UPDATE uses WHERE tenant_id = <caller tenant> or explicitly sets tenant_id from a validated source.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` executed without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This allows reverting/affecting rows across tenants if id is provided from a different tenant context.
  - _Fix:_ Include an explicit tenant filter in the WHERE clause and validate ownership before updating. Example: UPDATE commitments SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Alternatively, derive tenantId from the row and assert it matches the expected tenant before performing the UPDATE.
- **server/credential-vault.ts:169** — UPDATE on tenant-scoped table `credential_vault` executed without tenant constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). This can update another tenant's row by id.
  - _Fix:_ Constrain the UPDATE by tenant_id, using the provided tenantId parameter: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}. Also check the affected-row count to ensure the operation touched exactly one row and bail if not.
- **server/credential-vault.ts:176** — UPDATE on tenant-scoped table `credential_vault` executed without tenant constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}). This can update another tenant's row when cred.id is not validated to belong to the caller's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}. Prefer validating that the retrieved credential belongs to the tenant before performing the UPDATE and fail closed if not.
- **server/data-protection.ts:117** — Global purge query selects conversations without scoping by tenant_id (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < $1). This and the subsequent per-conversation deletes operate without an explicit tenant predicate, allowing cross-tenant deletes/reads.
  - _Fix:_ Constrain the maintenance SELECT to the tenant you're operating on or iterate tenants explicitly. Include tenant_id predicates on all downstream DELETE/SELECT statements (or run the per-tenant work inside withTenantTx). Example: SELECT ... FROM conversations WHERE tenant_id = ${tenantId} AND deleted_at < ${cutoff}. When deleting messages/archives/conversations, add AND tenant_id = ${conv.tenant_id} (or use withTenantTx to set app.current_tenant and enforce RLS).
- **server/data-protection.ts:131** — DELETE FROM messages WHERE conversation_id = ${conv.id} executes without a tenant_id predicate. Deleting by conversation_id only, without tenant scoping, can remove another tenant's data if assumptions about global uniqueness or ownership change.
  - _Fix:_ Add tenant scoping to destructive writes: DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Prefer running per-tenant purge inside a transaction with explicit tenant context (withTenantTx) to ensure every statement is constrained.
- **server/data-protection.ts:132** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks tenant_id constraint. This deletes tenant-scoped rows without scoping to the owning tenant.
  - _Fix:_ Include tenant scoping on deletes: DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Use withTenantTx or otherwise ensure tenant context is enforced for every write.
- **server/data-protection.ts:133** — DELETE FROM conversations WHERE id = ${conv.id} is executed without an explicit tenant_id WHERE clause. This can delete a conversation row that may be claimed by another tenant if assumptions change or IDs collide in future migrations.
  - _Fix:_ Always include tenant_id on deletes: DELETE FROM conversations WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Prefer per-tenant transactions (withTenantTx) to enforce tenant isolation at the DB layer during maintenance operations.
- **server/felix-brain.ts:19** — Boot-time query reads from tenant-scoped table 'agent_runs' without any tenant constraint (SELECT ... FROM agent_runs WHERE status IN ...). This loads rows for all tenants into in-memory state and breaks tenant isolation.
  - _Fix:_ Limit the query to a single tenant or iterate per-tenant. Add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq(agentRuns.tenantId, tenantId)) and obtain tenantId from a trusted source, or rework rehydration to explicitly run once per known tenant. Do not perform cross-tenant reads on boot.
- **server/google-workspace.ts:2358** — INSERT into presenter_slide_images is executed without supplying tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include the canonical tenantId in the INSERT (e.g. add tenant_id => ${tenantId} to the values) and/or validate that session_id belongs to the tenant before persisting. Prefer parameterized Drizzle inserts (db.insert(...).values({ tenantId, ... })) so tenant is always set from the trusted context, not from any caller-supplied data.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}) updates proposals without scoping by tenant_id, potentially archiving proposals across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the UPDATE via a per-tenant query (UPDATE ... WHERE id = ANY(${ids}) AND tenant_id = ${tenantId}). Ensure the audit only modifies rows for the intended tenant or runs under an explicit, audited admin path.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}) disables tasks without constraining by tenant_id, possibly affecting other tenants' scheduled tasks.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform per-tenant updates. Require an explicit tenant context for any operation that mutates tenant-scoped tables in bulk.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table research_schedules without a tenant_id constraint (reads schedules across all tenants).
  - _Fix:_ Restrict the SELECT to an explicit tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant via an authoritative tenant list and query each tenant separately. Ensure every read from tenant-scoped tables includes the caller/asserted tenant_id (or use a storage API that enforces it).
- **server/heartbeat.ts:658** — SELECT * FROM event_log WHERE status = 'pending' ... — no tenant_id constraint. This reads pending events across all tenants in one query.
  - _Fix:_ Query pending events per-tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'pending') or iterate active tenants and fetch each tenant's pending events separately. Never perform unscoped reads on tenant-scoped event tables.
- **server/internal-resolver.ts:91** — UPDATE on tenant-scoped table `event_log` (finalizeEvent) has no tenant_id constraint in the WHERE clause. This can finalize or mutate events belonging to other tenants.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. add AND tenant_id = ${tenantId}) or otherwise derive/validate the event's tenant before mutating. If resolver must operate cross-tenant, require explicit admin system context and document it; prefer an explicit tenant parameter and CAS that includes tenant_id.
- **server/internal-resolver.ts:107** — SELECT from tenant-scoped table `plans` by id (resolvePlanProposed) with NO tenant_id check. The code reads a plan referenced by an external event without verifying the caller/owner tenant.
  - _Fix:_ Load the plan by both id AND tenant_id (e.g. WHERE id=${planId} AND tenant_id=${tenantId}) or resolve the owning tenant for the event first and validate ownership before reading/updating. Never trust an id alone from an external event — ensure tenant scoping or use an admin-only flow.
- **server/internal-resolver.ts:170** — UPDATE on tenant-scoped table `code_proposals` (resolveExperimentFailed) uses WHERE id = ${proposalId} AND status = 'pending' with NO tenant_id constraint. That allows archiving proposals across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or otherwise fetch and validate the proposal's tenant prior to updating. Ensure events carry or allow resolution of the owning tenant before mutating tenant data.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into `project_files` omits tenant_id. Platform convention requires tenant_id be set on tenant-scoped tables (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${tenantId}, ${newProjectId}, ...)). Ensure the inserted project_id belongs to the same tenant (verify ownership) before inserting.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into `project_notes` omits tenant_id. Tenant-scoped inserts must explicitly set tenant_id per platform invariant.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, ...) VALUES (${tenantId}, ${newProjectId}, ${n.note}, ...)) and ensure the project_id belongs to that tenant prior to insert.
- **server/lib/scheduled-post-runner.ts:60** — Atomic CTE selects/updates scheduled_posts across all tenants with no tenant_id constraint (SELECT ... FROM scheduled_posts ... FOR UPDATE / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). This reads and writes a tenant-scoped table without constraining to a caller tenant.
  - _Fix:_ Either restrict the CTE to a specific tenant_id (preferred) or explicitly iterate per-tenant and include tenant_id in both the CTE's WHERE and the UPDATE WHERE clause. If this behavior is intentionally system-wide, make the admin intent explicit and add defensive checks (e.g. explicit admin-mode flag) and still include tenant_id in row-level UPDATEs to avoid accidental cross-tenant writes.
- **server/memory-graph.ts:195** — INSERT INTO memory_links (...) VALUES (...) — the INSERT does not set tenant_id. Tenant-scoped tables must receive tenant_id on INSERT; omitting it allows cross-tenant link creation or will fail if tenant_id is NOT NULL without default.
  - _Fix:_ Include tenant_id explicitly in the INSERT values (and pass tenantId into linkRelatedMemories). Example: INSERT INTO memory_links (source_memory_id, target_memory_id, link_type, strength, confidence, source_count, tenant_id) VALUES (..., ${tenantId}). Also ensure any subsequent reads/writes on memory_links filter by tenant_id.
- **server/minerva-planner.ts:355** — UPDATE on tenant-scoped table `plans` performed without constraining by tenant_id (CAS update uses WHERE id = ${args.planId} AND status = 'awaiting_approval' but no tenant filter). This allows a caller to flip the status of a plan belonging to any tenant by id.
  - _Fix:_ Require the caller's tenant context (or explicit tenantId) and include AND tenant_id = ${tenantId} in the WHERE clause. If this function must be multi-tenant-aware, accept a tenantId parameter and use it in the CAS WHERE so the update is only applied to the caller's tenant.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id column — row is created without an explicit tenant constraint.
  - _Fix:_ Include tenant_id explicitly in the INSERT VALUES (e.g. VALUES (${options.tenantId}, ...)) or otherwise ensure the tenant_id column is set from a trusted value. Parameterize the value; do not rely solely on prior ownership checks.
- **server/mpeg-engine.ts:1498** — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id — new row may be written without tenant attribution.
  - _Fix:_ Pass tenant_id explicitly in the INSERT (e.g. VALUES (${options.tenantId}, ...)) and ensure it is taken from a trusted source (and/or re-verify ownership) before inserting.
- **server/orchestrator-ledger.ts:445** — Use of sql.raw() to interpolate a computed advisory-lock key (lockKey) into SQL: `SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`. lockKey is derived from caller-supplied ledgerKey/tenantId and is being injected into the SQL text unparameterized.
  - _Fix:_ Avoid sql.raw() with caller-derived values. Compute a numeric value server-side (BigInt) and pass it as a parameter so the driver parameterizes it, e.g. `SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)` or use a parameter placeholder. Validate/normalize ledgerKey before use.
- **server/oauth-subscriptions.ts:906** — validateSubscriptionsOnStartup runs `SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant_id filter — it enumerates subscriptions (sensitive tokens/metadata) across all tenants.
  - _Fix:_ If this routine must operate per-tenant, iterate tenants and query per-tenant with `WHERE tenant_id = ${tenantId}`. If it truly needs to be global, ensure it only runs in a privileged admin context and log/audit that behavior; avoid returning raw secret-bearing rows into unscoped callers.
- **server/oauth-subscriptions.ts:915** — Unscoped SELECT `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'` (no tenant_id) — reads rows across tenants.
  - _Fix:_ Add tenant scoping (e.g. `WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}`) when intended to operate per-tenant, or restrict this call to admin-only startup logic that explicitly needs global enumeration and handle secrets carefully.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh queries `oauth_subscriptions` with `WHERE is_active = TRUE` and no tenant_id filter — iterates secrets/tokens across all tenants.
  - _Fix:_ Perform refresh per-tenant (accept tenantId or enumerate tenant ids in a safe, audited admin context). Replace the global unscoped query with per-tenant queries (`WHERE tenant_id = ${tenantId}`) or require explicit admin authorization for global operations.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions executes `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant constraint — reading tenant-scoped subscription rows globally.
  - _Fix:_ If forced-global behavior is necessary, restrict execution to a privileged admin job and carefully validate/limit returned data. Prefer iterating over a vetted list of tenant IDs and performing parameterized per-tenant refresh queries (`WHERE tenant_id = ${tenantId}`).
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} executes without constraining tenant_id. This mutates a tenant-scoped table by id only — a malicious or buggy rule could affect rows belonging to other tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the UPDATE (e.g. UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} AND tenant_id = ${tenantId}) or verify that the task id belongs to the tenant before updating.
- **server/process-governor.ts:642** — UPDATE agent_desks SET ... WHERE id = ${desk.id} is executed without a tenant_id constraint. This updates agent_desks (tenant-scoped) by id only and can modify other tenants' rows.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or first verify the desk's tenant ownership before persisting changes.
- **server/process-governor.ts:977** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} lacks a tenant_id condition. This mutates governance rules globally by id and can affect other tenants' rules.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) to ensure only the tenant's rule row is updated.
- **server/proposal-verifier.ts:246** — UPDATE code_proposals SET verification_status = ..., verified_at = NOW() WHERE id = ${proposalId} updates a tenant-scoped table without a tenant_id condition. This allows mutating other tenants' proposals by id alone.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or validate the proposal belongs to the tenant before updating.
- **server/proposal-verifier.ts:373** — UPDATE code_proposals SET status = 'rejected', verification_status = 'skipped', ... WHERE id = ${proposalId} is executed without scoping by tenant_id. This mutates proposal rows across tenants.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or otherwise verify ownership before changing status.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes to a tenant-scoped table without specifying tenant_id. This can alter proposals belonging to other tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or ensure the proposal's tenant is checked and passed into the function.
- **server/research-engine.ts:158** — UPDATE against research_sessions performed without any tenant_id constraint (cleanupZombieSessions). This UPDATE will mark running sessions across all tenants as completed and can cause cross-tenant state corruption.
  - _Fix:_ Restrict the UPDATE to the intended tenant(s). Either run cleanup per-tenant (pass tenantId into cleanupZombieSessions or iterate tenants) or add a WHERE tenant_id = ${<tenantId>} clause to the SQL. Ensure any dynamic tenant list is validated and parameterized (use sql.join/sql``) rather than string interpolation.
- **server/research-engine.ts:390** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() is executed without a tenant_id filter. agent_knowledge is tenant-scoped, so this will delete entries for all tenants.
  - _Fix:_ Limit deletion to the current tenant. Change the statement to include tenant_id = ${tenantId} (e.g. DELETE FROM agent_knowledge WHERE tenant_id = ${tenantId} AND source = 'autoresearch' AND expires_at < NOW()). Ensure the tenantId used is the caller/session tenant and is validated as an integer > 0.
- **server/research-engine.ts:470** — INSERT into project_notes omits tenant_id. project_notes appears tenant-scoped and INSERTs must explicitly set tenant_id; omitting it risks cross-tenant contamination or DB constraint failures.
  - _Fix:_ Include tenant_id in the INSERT columns and pass the caller's tenant (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ${note}, ${'Research Engine'}, NOW())). Also keep the existing assertProjectInTenant check to verify project ownership.
- **server/research-engine.ts:478** — Second INSERT into project_notes (creating a finding note) also omits tenant_id, writing a tenant-scoped table without explicit tenant ownership metadata.
  - _Fix:_ As above, add tenant_id to the columns and values (INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ${noteContent}, ${'Research Engine'}, NOW())). Ensure project ownership is validated before insertion.
- **server/routes.ts:165** — restoreUploadsFromDb reads fileStorage.filename and fileStorage.data for all rows without any tenant_id constraint — this can expose raw file blobs across tenants.
  - _Fix:_ Scope the lookup to a single tenant (e.g. add WHERE file_storage.tenant_id = <tid>) or iterate per-tenant. Avoid selecting raw data for all tenants in one unscoped query; only restore files for the tenant context you control (or run as a privileged, audited migration tool with explicit tenant scoping).
- **server/routes.ts:4324** — SELECT from tenant-scoped table `conversations` executed without constraining by tenant_id. The query: `SELECT project_id FROM conversations WHERE id = ${conversationId} AND project_id IS NOT NULL` lacks `tenant_id = ${tenantId}`. This can allow cross-tenant data exposure if conversationId is attacker-controlled or not validated.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add `AND tenant_id = ${tenantId}`) or use a storage/ORM call that enforces tenant scoping (e.g. storage.getConversation(conversationId, tenantId)). Validate ownership of the referenced conversation before using its fields.
- **server/seed.ts:2855** — SELECT from tenant-scoped table 'research_programs' without constraining tenant_id (SELECT id, name, model FROM research_programs WHERE is_active = true). This reads programs across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or iterate per-tenant. Never perform cross-tenant SELECTs on tenant-scoped tables without an explicit tenant filter.
- **server/seed.ts:2860** — UPDATE on tenant-scoped table 'research_programs' uses WHERE id = ${p.id} but omits tenant_id. Updating by id alone can modify rows belonging to other tenants.
  - _Fix:_ Include tenant ownership in the predicate (e.g. UPDATE research_programs SET model = ... WHERE id = ${p.id} AND tenant_id = ${tenantId}), or first verify the row's tenant matches the intended tenant before performing the update.
- **server/seed.ts:35** — SELECT COUNT(*) FROM research_sessions executed without any tenant filter. research_sessions is tenant-scoped, so this returns cross-tenant counts.
  - _Fix:_ Add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) when querying tenant-scoped tables. If the intention is to check emptiness per-tenant, scope the count to that tenant explicitly.
- **server/seed.ts:3137** — UPDATE on 'projects' sets primary_conversation_id WHERE id = 15 without constraining tenant_id. projects is likely tenant-scoped so this may modify another tenant's project.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId} AND primary_conversation_id IS NULL) or validate the project's tenant before updating.
- **server/seed.ts:3205** — SELECT DISTINCT c.id, c.project_id FROM conversations c ... does not constrain tenant_id on conversations (or on the project_conversations subquery). conversations and project_conversations are tenant-scoped, so this can return cross-tenant rows.
  - _Fix:_ Restrict both the outer query and the NOT EXISTS subquery to the intended tenant (e.g. WHERE c.tenant_id = ${tenantId} AND NOT EXISTS(SELECT 1 FROM project_conversations pc WHERE pc.tenant_id = ${tenantId} AND pc.project_id = c.project_id ...)). Ensure all joins/subqueries enforce tenant ownership.
- **server/seed.ts:3213** — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id for a tenant-scoped table. The table likely requires tenant_id and inserting without it (or with a wrong default) can create records not bound to the correct tenant.
  - _Fix:_ Include tenant_id in the INSERT values (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${r.project_id}, ${r.id}) ) and ensure the tenant matches both project and conversation ownership. Validate ownership before inserting.
- **server/self-improvement.ts:401** — UPDATE on tenant-scoped table `experiments` uses only eq(experiments.id, experiment.id) in the WHERE clause, missing a tenant_id constraint. Without RLS, this allows modifying any experiment row by id across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId)))) or fetch+verify ownership before the UPDATE. Never update tenant-scoped rows by id alone.
- **server/self-improvement.ts:415** — Error-path UPDATE on tenant-scoped `experiments` likewise updates by id only (.where(eq(experiments.id, experiment.id))) with no tenant_id constraint — allows cross-tenant modification on error handling path.
  - _Fix:_ As above, add tenant_id to the WHERE clause for the error-status UPDATE, or validate the experiment belongs to the caller's tenant before performing any updates. Ensure all UPDATE/DELETE statements on tenant-scoped tables include tenant filters.
- **server/skills-marketplace.ts:213** — INSERT INTO `skills` does not set tenant_id. If the `skills` table is tenant-scoped with tenant_id NOT NULL (no default), this allows creating rows without tenant ownership (cross-tenant write).
  - _Fix:_ Require a tenantId parameter and include tenant_id in the INSERT values (or otherwise ensure the row is created under the correct tenant). Use requireTenantScope/assertValidTenantId to validate tenant input before insertion.
- **server/skills-marketplace.ts:252** — INSERT INTO `skills` in importSkill does not include tenant_id. This may create skill rows without tenant ownership if `skills` is tenant-scoped, enabling cross-tenant data mixing.
  - _Fix:_ Require caller-supplied tenantId (or determine tenant from context) and insert tenant_id into the row. Validate tenantId with assertValidTenantId or requireTenantScope before performing the INSERT.
- **server/storage.ts:396** — attachCitations updates messages (UPDATE messages SET citations = ...) by message id only — no tenant_id constraint on the tenant-scoped messages table (unscoped write).
  - _Fix:_ Require and validate the caller's tenantId and include it in the WHERE clause (e.g. WHERE id = ${messageId} AND tenant_id = ${tenantId}). Failing an explicit positive tenantId must throw (fail-closed).
- **server/storage.ts:803** — updateMemoryEmbedding updates memory_entries by id only (WHERE id = ...), with no tenant_id filter on the tenant-scoped memory_entries table (unscoped write).
  - _Fix:_ Require tenant ownership before mutating the row. Either accept a tenantId parameter and use WHERE id = ${id} AND tenant_id = ${tenantId}, or lookup/validate the row's tenant and enforce it before performing the update.
- **server/storage.ts:815** — updateKnowledgeEmbedding updates agent_knowledge by id only (WHERE id = ...), missing a tenant_id constraint on the tenant-scoped agent_knowledge table (unscoped write).
  - _Fix:_ Require/accept tenantId and add WHERE id = ${id} AND tenant_id = ${tenantId}, or validate the record's tenant before updating. Fail closed if tenantId is missing or invalid.
- **server/storage.ts:839** — archiveExpiredMemories runs a raw UPDATE on memory_entries (SET status='archived' WHERE ... expires_at <= NOW()) without any tenant_id scoping — a bulk, cross-tenant mutation on a tenant-scoped table.
  - _Fix:_ If this job must act per-tenant, iterate tenants and run the UPDATE per-tenant with WHERE tenant_id = ${tenantId} AND ...; otherwise restrict this operation to an admin-only path with clearly auditable intentions. Do not perform a global UPDATE without an explicit admin/tenant scoping guard.
- **server/storage.ts:855** — archiveStaleMemories runs a raw UPDATE on memory_entries (SET status='archived' WHERE created_at <= ... AND last_accessed <= ...) with no tenant_id constraint — bulk cross-tenant mutation on a tenant-scoped table.
  - _Fix:_ Run this per-tenant (loop tenants and include WHERE tenant_id = ${tenantId}) or require an explicit admin-scoped codepath. Ensure explicit tenant filtering is present in the SQL to avoid cross-tenant data changes.
- **server/storage.ts:1296** — getAllDataForExport performs many unscoped selects (conversations, messages, memory_entries, agent_knowledge, daily_notes, heartbeat_tasks, etc.) and returns platform-wide data across tenants without tenant_id filters.
  - _Fix:_ This export is a high-risk global dump. Either (a) require an explicit, authenticated admin context and enforce it at the API boundary, or (b) make the function accept a tenantId and add WHERE tenant_id = ${tenantId} for each tenant-scoped table. Log and audit any access, and ensure an explicit approval gate for global exports.
- **server/storage.ts:545** — updateMemoryEntry can execute an UPDATE against memory_entries using only id in the WHERE when tenantId is omitted (the code warns but still allows an unscoped update).
  - _Fix:_ Require/validate tenantId and include it in the WHERE (AND tenant_id = ${tenantId}). If tenantId is omitted, throw instead of performing an unscoped write.
- **server/storage.ts:553** — deleteMemoryEntry updates memory_entries (set status='superseded') using only id in the WHERE when tenantId is omitted — unscoped write across tenants is possible.
  - _Fix:_ Require tenantId and scope the update: WHERE id = ${id} AND tenant_id = ${tenantId}. Fail closed if tenantId is not provided.
- **server/storage.ts:561** — touchMemoryEntries updates memory_entries (lastAccessed/accessCount) using inArray(id list) but will perform the update unscoped if tenantId is omitted (tenantScope not added).
  - _Fix:_ Require tenantId and include eq(memoryEntries.tenantId, tenantId) in the WHERE; do not allow the function to proceed with only a warning.
- **server/storage.ts:1019** — claimHeartbeatTasks runs UPDATE heartbeat_tasks ... WHERE id = ${id} ... without a tenant_id constraint (unscoped write / claiming tasks across tenants).
  - _Fix:_ Include tenant scoping when claiming tasks (require/accept tenantId and add WHERE id = ${id} AND tenant_id = ${tenantId}), or ensure the scheduler is a platform-level trusted component and document that explicitly. Prefer per-tenant claiming to avoid accidental cross-tenant mutations.
- **server/storage.ts:961** — updateHeartbeatTask will perform UPDATE heartbeat_tasks WHERE id = ${id} if tenantId is omitted (the fallback adds a subselect only when tenantId provided). That allows unscoped updates to tenant-scoped heartbeat_tasks.
  - _Fix:_ Require tenantId for update/delete operations on tenant-scoped tables or always enforce tenant equality in the WHERE clause. Replace the id-IN-subselect pattern with direct tenant equality checks (WHERE id = ${id} AND tenant_id = ${tenantId}).
- **server/storage.ts:967** — deleteHeartbeatTask deletes heartbeat_tasks by id only when tenantId is omitted (unscoped delete on a tenant-scoped table).
  - _Fix:_ Require tenantId and use WHERE id = ${id} AND tenant_id = ${tenantId}. For admin/global deletion, require an explicit admin path and authorization check.
- **server/surprise-scorer.ts:95** — stampArgsEmbedding runs UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL — no tenant_id constraint on the tenant-scoped felix_proposals table (unscoped write).
  - _Fix:_ Require and pass tenantId into stampArgsEmbedding and include AND tenant_id = ${tenantId} in the WHERE. Alternatively, validate the row's tenant before updating and fail-closed if tenant information is missing.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table `custom_tools` uses .where(eq(customTools.id, tool.id)) without restricting tenant_id. This allows an update (implementation patch) to affect a row in any tenant if the id is known/guessed.
  - _Fix:_ Always include the tenant filter on updates: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, <callerTenantId>))). Ensure tryAndRevert has access to the caller tenantId (or tool.tenantId) and use it in the WHERE clause to enforce isolation.
- **server/tool-learning.ts:316** — DELETE in revertToolRow calls db.delete(customTools).where(eq(customTools.id, tool.id)) with no tenant_id constraint. This can delete another tenant's custom tool by id (no RLS present).
  - _Fix:_ Require and use tenantId for deletions. Change revertToolRow to accept/derive tenantId and delete with .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Failing to include tenantId must be treated as a security defect and fixed immediately.
- **server/tools.ts:4513** — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code inserts (project_id, note, author, created_at) without passing the caller's tenant_id explicitly.
  - _Fix:_ Always include tenant_id as an explicit column/value on INSERTs into tenant-scoped tables. e.g. INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${params._tenantId}, ${projectId}, ${note}, 'system', NOW()). Do this after verifying ownership (assertProjectInTenant) to prevent cross-tenant injection/elevation.
- **server/tools.ts:4518** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code inserts (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) without tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT. e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${params._tenantId}, ${projectId}, ${fileName}, 'presentation', ${editUrl}, ${editUrl}, 'Felix', NOW()). Keep the prior assertProjectInTenant check, and pass the validated tenant id into the SQL parameters.
- **server/tools.ts:6365** — INSERT into tenant-scoped table `project_files` (audio upload path) does not include tenant_id. The INSERT VALUES omit tenant_id even though the caller's project_id was validated just above.
  - _Fix:_ Add tenant_id to the INSERT column list and VALUES using the validated params._tenantId. e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${filename + '.' + ext}, ${outPath}, ${driveUrl || null}, 'audio', ${audioBuffer.length}, 'system'). This ensures DB-level rows are explicitly tagged with the caller tenant.
- **server/tools.ts:6899** — INSERT into tenant-scoped table `project_files` (video output) omits tenant_id. The code writes project_files VALUES without providing tenant_id, even after asserting project ownership.
  - _Fix:_ Include tenant_id explicitly in the INSERT using the validated params._tenantId. e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ${outputFilename + '.mp4'}, ${outPath}, ${driveUrl || null}, 'video', ${stats.size}, 'system'). Ensure tenant is validated before the INSERT and the tenant_id value is parameterized (no string interpolation).
- **server/tools.ts:6899** — INSERT into tenant-scoped table project_files omits tenant_id. The query at line 6899 inserts project file metadata but does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) ...). Reliance on a prior assertProjectInTenant check does not satisfy the invariant that every INSERT must explicitly include tenant_id.
  - _Fix:_ Include the tenant_id column/value in the INSERT and ensure it's the verified tenant context. For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Keep the assertProjectInTenant guard (or similar ownership check) but also persist tenant_id in the row to enforce application-level isolation for future reads/operations.
- **server/tools/domains/security/handlers.ts:163** — SELECT from provider_keys without tenant_id constraint. The query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` is executed (even if gated to admin tenant) against what appears to be a tenant-scoped table, exposing cross-tenant sensitive information (API keys).
  - _Fix:_ Constrain all queries on tenant-scoped tables to the caller's tenant (e.g. add `WHERE tenant_id = ${tenantId}` or equivalent drizzle `.where(eq(table.tenant_id, tenantId))`). If this table is intended to be global, document that explicitly and avoid returning any key material or key-lengths; prefer admin-only audited flows that aggregate counts without exposing sensitive columns.
- **server/tools/domains/security/handlers.ts:260** — SELECT COUNT(DISTINCT provider) FROM provider_keys executed without tenant_id filter. The query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` runs for owner/admin and lacks tenant scoping, allowing cross-tenant enumeration of provider keys.
  - _Fix:_ Restrict the aggregation to the caller's tenant by adding `WHERE tenant_id = ${tenantId}` (or equivalent). If a true global admin aggregate is required, implement an audited admin-only API that explicitly documents and justifies cross-tenant visibility and avoids returning per-tenant-sensitive details.
- **server/tools/domains/strategic-memory/handlers.ts:95** — UPDATE statement updates memory_entries WHERE id = ${id} with no tenant_id (or persona_id) constraint — tenant-scoped table writes/updates must include tenant scoping to prevent cross-tenant modifications.
  - _Fix:_ Include tenant scoping in the WHERE clause (and persona_id if appropriate). e.g. WHERE id = ${id} AND tenant_id = ${tid} (and AND persona_id = ${personaId} if you want to double-check persona scope). Use parameterized values as done elsewhere rather than string interpolation.
- **server/tools/domains/strategic-memory/handlers.ts:148** — UPDATE statement updates memory_entries WHERE id = ANY(${ids}) with no tenant_id (or persona_id) constraint — this can bump access_count/last_accessed for rows from other tenants if ids are known or mis-bound.
  - _Fix:_ Constrain the UPDATE by tenant (and persona if desired). For example: UPDATE memory_entries SET last_accessed = NOW(), access_count = access_count + 1 WHERE tenant_id = ${tid} AND id = ANY(${ids as any}) (or include persona_id = ${personaId} as well). Ensure ids are derived from previously tenant-scoped selects or validate ownership before update.
- **server/tools/domains/strategic-memory/handlers.ts:242** — UPDATE statement updates memory_entries WHERE id = ${id} with no tenant_id (or persona_id) constraint — same cross-tenant update risk as other UPDATEs in this module.
  - _Fix:_ Add tenant scoping to the WHERE clause (and persona_id if appropriate). For example: UPDATE memory_entries SET fact = ${fact}, last_accessed = NOW(), access_count = access_count + 1 WHERE id = ${id} AND tenant_id = ${tid} (and AND persona_id = ${personaId} to be stricter).
- **server/wiring-invariants.ts:180** — Outcome canary query reads research_experiments, code_proposals, research_sessions and research_schedules without any tenant constraint. Those tables are tenant-scoped, so counts and last_run values aggregate across tenants and may leak tenant data or produce incorrect alerts.
  - _Fix:_ Add tenant scoping to every referenced table/subquery (e.g. WHERE e.tenant_id = :tenantId, rs.tenant_id = :tenantId, s.tenant_id = :tenantId) or run the canary per-tenant using an explicit tenant parameter (for admin-only checks use ADMIN_TENANT_ID explicitly but ensure intent is documented). Always include the application-level tenant filter when accessing tenant-scoped tables.
- **server/wiring-invariants.ts:244** — Query on tool_performance (COUNT WHERE last_success_at > ...) has no tenant_id filter. tool_performance is tenant-scoped, so this aggregates usage across tenants and may expose or conflate tenant data.
  - _Fix:_ Restrict tool_performance queries to the intended tenant (e.g. WHERE tenant_id = :tenantId AND last_success_at > ...) or iterate tenants explicitly. Ensure any aggregate intended to be global is explicitly documented and uses an admin-only tenancy model.
- **server/wiring-invariants.ts:258** — SELECT COUNT(*) FROM tool_performance executed without tenant scoping. This reads tenant-scoped data across all tenants.
  - _Fix:_ Add WHERE tenant_id = :tenantId (or equivalent) or otherwise explicitly document and enforce that this check should run in an admin-only context using a known admin tenant id.
- **server/wiring-invariants.ts:260** — SELECT COUNT(*) FROM messages (recent 14d) runs without a tenant filter. messages is tenant-scoped; this aggregates message counts across tenants and may leak information or skew checks that rely on per-tenant volume.
  - _Fix:_ Scope the messages COUNT to the caller tenant (WHERE tenant_id = :tenantId) or run the check per-tenant. Do not query tenant-scoped message tables globally without an explicit admin tenant context and documentation.
- **server/wiring-invariants.ts:293** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant constraint. It will return tool usage across all tenants rather than per-tenant usage.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = :tenantId ...) or run this logic per tenant. If a multi-tenant aggregation is desired, make that explicit and protect access to aggregated data accordingly.
- **server/wiring-invariants.ts:366** — sql.raw is used to SELECT COUNT/MAX FROM ${spec.table} (e.g. knowledge_nudges, tool_performance) without any tenant filter. These subsystem tables are tenant-scoped, so the freshness check reads across tenants.
  - _Fix:_ Avoid global/raw queries against tenant-scoped subsystem tables. Build parameterized queries that include WHERE tenant_id = :tenantId (use query builder helpers rather than raw string interpolation) or run the freshness probe per-tenant. If a table is intentionally global, mark it as such in comments/config.
- **server/wiring-invariants.ts:417** — Tool-drift query reads tool_performance without tenant scoping (WHERE last_failure_at > ... but no tenant_id). This allows cross-tenant aggregation and potential data exposure.
  - _Fix:_ Add tenant scoping (e.g. WHERE tenant_id = :tenantId AND last_failure_at > ...) or run the detector per-tenant. Use parameterized Drizzle conditions instead of relying on raw SQL when possible.
- **server/wiring-invariants.ts:535** — Memory-growth probe uses sql.raw to SELECT COUNT and pg_total_relation_size FROM ${tbl.name} (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, ... ) with no tenant filter. For tenant-scoped memory tables this reads data across tenants.
  - _Fix:_ For tenant-scoped memory tables perform counts scoped to the tenant (e.g. SELECT COUNT(*) FROM table WHERE tenant_id = :tenantId). Avoid interpolating table names for tenant-scoped data; instead use parameterized/ORM queries per tenant or an explicit admin tenancy model with documentation.
- **server/wiring-invariants.ts:561** — SELECT COUNT(*) FROM graph_memory executes without tenant scoping; the check even notes 'empty across all tenants' which implies graph_memory is tenant-scoped. Querying without WHERE tenant_id permits cross-tenant observation and incorrect remediation advice.
  - _Fix:_ Scope this check to a specific tenant (WHERE tenant_id = :tenantId) or iterate tenants. If the intent is to check global seeding, execute the check under a documented admin tenant and/or add safeguards to avoid accidental cross-tenant data reads.
- **server/workflow-templates.ts:67** — The tenant context passed to executeTool is named _tenantId rather than tenantId or tenant_id. If produce_video / executeTool expects tenantId/tenant_id, it may ignore _tenantId and perform DB writes without tenant scoping, causing cross-tenant writes or orphaned rows.
  - _Fix:_ Pass the tenant using the canonical key used across tooling (e.g. tenantId or tenant_id) and ensure executeTool/produce_video validates the tenant and includes tenant_id on all DB writes. Audit produce_video/executeTool to confirm which key they accept and to make tenant propagation explicit and type-checked.

### HIGH (155)

- **server/ab-optimizer.ts:110** — SELECT FROM experiments is executed without constraining tenant_id (reads running A/B tests across all tenants).
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE category='ab_test' AND status='running' AND tenant_id = ${tenantId}) or iterate per-tenant so you never read other tenants' experiments.
- **server/agentic-engines.ts:161** — Durability sweep selects ai_insights rows (retryPendingMinervaRouting) without a tenant_id filter — reads insights across tenants.
  - _Fix:_ Scope the sweep to a particular tenant (accept tenantId param) or include tenant_id = ${tenantId} in the WHERE clause; when doing multi-tenant sweeps, iterate tenants explicitly and run tenant-scoped queries per-tenant.
- **server/agentic-engines.ts:344** — Query against heartbeat_logs (count of task calls) in gatherOperationalData lacks a tenant_id filter — leaking global heartbeat log counts into tenant-scoped analysis.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to the heartbeat_logs query (or otherwise scope to the tenant) so only tenant-specific logs are read.
- **server/agentic-engines.ts:451** — SELECT recent heartbeat_logs (runPredictiveEngine) executed without tenant_id filter — returns logs from all tenants.
  - _Fix:_ Restrict the query with WHERE tenant_id = ${tenantId} (or accept tenantId and run per-tenant) to avoid cross-tenant data exposure.
- **server/agentic-engines.ts:519** — SELECT aggregating heartbeat_logs (runOptimizationEngine) executed without tenant_id filter — aggregates global task performance across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause for heartbeat_logs aggregations, or perform the aggregation per-tenant explicitly to avoid mixing tenants' telemetry.
- **server/agentic-engines.ts:533** — SELECT FROM heartbeat_tasks (taskConfig) executed without tenant_id filter — reads tasks across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) to the heartbeat_tasks query so only tenant-owned tasks are considered.
- **server/agentic-features.ts:599** — SELECT id FROM outreach_enrollments to check existing enrollment is executed without tenant_id filter — can detect or block enrollments across tenants.
  - _Fix:_ Add tenant_id = ${params.tenantId} to the WHERE clause when checking for existing enrollments (or otherwise verify enrollment ownership) before deciding to reject/allow.
- **server/agentic/department-budgets.ts:143** — Query selects from tenant-scoped table `department_budgets` without constraining by tenant_id: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This reads tenant-scoped rows across all tenants without an application-level tenant filter.
  - _Fix:_ Either restrict the query to a single tenant (add `WHERE tenant_id = ${tenantId}`) or move this logic into an explicitly privileged admin-only process with strong access controls and audit. If the intent is to process per-tenant, iterate tenant_ids from a trusted source and call tenant-scoped queries (e.g. call checkDepartmentBudget for each tenant) rather than issuing an unconstrained cross-tenant SELECT.
- **server/agentic/task-forces.ts:44** — INSERT builds a persona_ids SQL array literal via string construction and injects it into the SQL (sql`${literal}::int[]`). The literal originates from caller-supplied personaIds and is inserted as a raw SQL fragment (string-interpolated SQL), creating an injection / isolation risk.
  - _Fix:_ Avoid constructing SQL array literals as raw fragments. Use parameterized binding or the query builder/Drizzle insert API to pass a typed integer array (e.g. provide persona_ids as a JS number[] parameter or use a safe array-binding helper). Sanitize/validate inputs and keep all values as bound parameters rather than raw SQL text.
- **server/agentic/wake-scheduler.ts:146** — Row selection used to claim pending wakes does not constrain by tenant_id. The inner SELECT that picks pending ids (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) lacks a tenant_id = ... filter, allowing cross-tenant schedules to be claimed/updated.
  - _Fix:_ Limit the inner SELECT to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant: enumerate tenant_ids and run the claim/update per-tenant so all reads/updates against agent_wake_schedules include an explicit tenant_id equality filter.
- **server/auto-memorize.ts:163** — The core messages SELECT pulls messages across all tenants (SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ${windowStart} ... ORDER BY m.tenant_id ASC LIMIT 400) and then partitions tenant-side. This performs a tenant-scoped table read without an explicit tenant_id equality filter.
  - _Fix:_ Do not mix tenants in a single query. Either run a per-tenant query with WHERE m.tenant_id = ${tenantId} (loop tenants) or query a list of tenant_ids and then fetch messages per-tenant. Ensure every read against messages includes an explicit tenant_id = ... predicate to enforce isolation in SQL.
- **server/auto-memorize.ts:47** — getWatermark() queries agent_knowledge (SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ...) without any tenant_id equality. agent_knowledge is tenant-scoped; this read can return another tenant's watermark.
  - _Fix:_ Make the watermark tenant-scoped: add a tenant_id parameter to getWatermark and include WHERE tenant_id = ${tenantId} in the SELECT (or otherwise scope the query to the tenant's id).
- **server/auto-memorize.ts:74** — setWatermark() INSERT into agent_knowledge hardcodes tenant_id = 1 (INSERT ... tenant_id, 1 ...). This writes the watermark into tenant #1 regardless of which tenant is being processed, violating tenant isolation and likely corrupting per-tenant state.
  - _Fix:_ Store the watermark with the correct tenant_id. Accept a tenantId parameter to setWatermark and INSERT tenant_id = ${tenantId} (and ensure corresponding SELECT/UPDATE queries filter by that tenant_id). If the watermark is intended to be per-tenant, do not hardcode tenant 1.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table 'conversations' without constraining tenant_id in WHERE clause (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL). This performs a write to a tenant-scoped resource without an explicit tenant filter.
  - _Fix:_ Include an explicit tenant constraint in the UPDATE (e.g. AND tenant_id = ${c.tenant_id} or AND tenant_id = ${tenantId}) or perform the update via a join that validates project/conversation ownership within the same transaction. Ensure every UPDATE/DELETE on tenant-scoped tables includes tenant_id in the WHERE clause.
- **server/business-tools.ts:74** — createInvoice inserts an invoice with a caller-supplied customer_id without first verifying that the referenced customer belongs to the caller's tenant. This allows creating an invoice row (tenant_id = caller) that references a customer_id owned by another tenant.
  - _Fix:_ Before inserting the invoice (or storing customer_id on the invoice row), verify ownership: SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1 and reject if not found. Alternatively, enforce the referential check and only accept customer_id values that belong to the caller's tenant.
- **server/business-tools.ts:276** — logInteraction inserts a customer_interactions row referencing params.customer_id without first verifying that the customer belongs to the caller's tenant. This creates the possibility of cross-tenant references even though the interaction row is stamped with the caller tenant_id.
  - _Fix:_ Validate the customer_id ownership prior to inserting (SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} LIMIT 1) and reject the request if the customer does not belong to the tenant. Ensure all handler paths that accept foreign IDs perform this check consistently.
- **server/ceo-orchestrator.ts:841** — Reads the personas table without scoping to the plan's tenant: `const cachedPersonas = await db.select().from(personasTable);` — this returns personas across tenants and can lead to cross-tenant data leakage or selection of a persona id that does not belong to the plan's tenant.
  - _Fix:_ Only load personas for the relevant tenant (or otherwise verify ownership) before using them. For example, query with a tenant filter (e.g. WHERE tenant_id = ${plan.tenantId}) or call a helper that validates a persona id belongs to plan.tenantId. Do not rely on an unscoped global fetch of personas when assigning personaId to tenant-scoped resources.
- **server/channel-routing.ts:31** — Query joins channel_routes to personas without any tenant constraint: `FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ...` — if personas is a tenant-scoped table this can surface or join persona rows from other tenants (tenant isolation violation).
  - _Fix:_ Either make channel_routes tenant-scoped (include tenant_id and filter by it) or, when joining to personas, add an explicit tenant filter (e.g. WHERE p.tenant_id = <expectedTenantId>). Validate the persona_id is owned by the correct tenant before returning persona data. If channel routes are intended to be global/admin-only, restrict this API to an admin tenant and document/enforce that constraint.
- **server/channels/kernel.ts:177** — dispatchInbound accepts a conversationId from an adapter-supplied resolveConversation callback and immediately passes it to processMessage without verifying the conversation belongs to the caller's tenant (turn.tenantId). This allows a malicious or buggy adapter to supply a foreign conversation_id and cause cross-tenant reads/writes downstream.
  - _Fix:_ Enforce tenant ownership before using the conversation id. Either (A) require resolveConversation to return a structure that includes the resolved tenant and validate it equals turn.tenantId, or (B) have the kernel perform an explicit DB lookup: SELECT id FROM conversations WHERE id = <conversationId> AND tenant_id = <turn.tenantId> (using Drizzle .where(eq(table.tenantId, tenantId))) and fail if not found. Also consider passing tenantId into processMessage and make sure all downstream DB queries include tenant_id filters on tenant-scoped tables.
- **server/data-protection.ts:182** — SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} reads messages for a conversation without including tenant_id = ${tenantId}. Even though the conversation was validated earlier, every read against a tenant-scoped table must include the tenant predicate per project invariant.
  - _Fix:_ Add tenant scoping to message reads: SELECT ... FROM messages WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} ORDER BY created_at ASC. If you have already validated the conversation, still include the tenant predicate on each query, or perform the sequence inside withTenantTx so RLS/enforcement applies.
- **server/data-protection.ts:189** — SELECT content, archived_at FROM compaction_archives WHERE conversation_id = ${conversationId} queries compaction_archives without restricting by tenant_id, allowing access to another tenant's archive rows.
  - _Fix:_ Add tenant predicate to the archive query: SELECT ... FROM compaction_archives WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId} ORDER BY archived_at ASC. Alternatively run the backup inside withTenantTx so all reads are tenant-scoped.
- **server/data-protection.ts:368** — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} is used to check message counts without tenant_id scoping. This read may surface or operate on other tenants' rows if tenant constraints are not applied consistently.
  - _Fix:_ Add tenant scoping to the count: SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}. Prefer per-tenant scoped transactions (withTenantTx) for all conversation-related reads.
- **server/data-protection.ts:406** — getDeletedConversations runs a subquery (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id) without scoping messages by tenant_id. Even though the outer query filters conversations by tenant_id, inner reads against tenant-scoped tables must still include tenant predicates per the project's invariant.
  - _Fix:_ Constrain the subquery to the tenant: (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id AND tenant_id = ${tenantId}) AS message_count, or perform the count with an explicit JOIN that includes tenant predicates, or run the whole query inside withTenantTx to ensure DB-enforced tenant isolation.
- **server/deep-interview.ts:337** — INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ... WHERE pc.conversation_id = ${state.conversationId} AND p.tenant_id = ${state.tenantId} inserts a row into project_notes but does NOT set project_notes.tenant_id explicitly. The invariant requires every INSERT into a tenant-scoped table to include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT columns/values. For example: INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() FROM ... WHERE ... LIMIT 1. Ensure tenant_id is explicitly provided (parameterized) for all tenant-scoped INSERTs.
- **server/delivery-pipeline.ts:189** — updateDeliveryLog() issues an UPDATE against tenant-scoped table `delivery_logs` using only the delivery id (WHERE id = ...) with no tenant_id constraint. This allows updating another tenant's delivery row if an id is known.
  - _Fix:_ Include tenant scoping in the UPDATE. Change the function signature to accept tenantId (or require callers pass it) and use a WHERE clause that includes both id and tenant_id (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))). Ensure all call sites pass the correct tenantId or perform authorization checks before calling.
- **server/delivery-pipeline.ts:686** — SELECT on `delivery_logs` by stripePaymentId is executed without filtering by tenant_id. A tenant-scoped table is being read across tenants (stripePaymentId is not sufficient to scope to the caller's tenant).
  - _Fix:_ Add tenant scoping to the query (e.g. include AND delivery_logs.tenant_id = ${req.tenantId}) or otherwise restrict the lookup to the intended tenant. If this lookup is intended to be global, document and justify it; otherwise require the caller's tenant and enforce it in the WHERE clause.
- **server/delivery-pipeline.ts:725** — An UPDATE to `delivery_logs` clears stripePaymentId (WHERE id = prior.id) without verifying tenant ownership (no tenant_id in WHERE). This can modify another tenant's row.
  - _Fix:_ Include tenant scoping in the UPDATE by adding AND tenant_id = ${prior.tenantId || callerTenantId}. Prefer using a transaction that verifies tenant ownership before mutating, and ensure the caller's tenant is passed through and enforced.
- **server/delivery-pipeline.ts:1014** — getDeliveryByStripePayment() SELECTs from `delivery_logs` by stripePaymentId without constraining by tenant_id, returning a tenant-scoped row across tenants.
  - _Fix:_ Accept a tenantId parameter (or require one) and add WHERE tenant_id = ${tenantId} to the query. If a global search is required, document the risk and ensure the caller is trusted.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed as DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} with no tenant_id constraint. This can delete chunks belonging to other tenants if collection_id is not globally unique or misassociated.
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}). Ensure the collection existence check and subsequent deletes are both guarded by the same tenant_id to prevent cross-tenant deletion.
- **server/email.ts:366** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed with no tenant_id constraint. inbox_messages is tenant-scoped; this read may return rows from other tenants.
  - _Fix:_ Scope this existence check by tenant or inbox (e.g. WHERE message_id = ${msgId} AND inbox_id = ${inboxId} OR tenant_id = ${tenantId}). Use the inbox/inboxId or tenant context to constrain the lookup so it cannot observe other tenants' messages.
- **server/email.ts:411** — backfillEmptyBodies() runs SELECT id, message_id FROM inbox_messages WHERE ... with no tenant_id filter, scanning a tenant-scoped table across all tenants.
  - _Fix:_ Restrict the SELECT to a tenant (or to the targeted inbox) by adding WHERE tenant_id = ${tenantId} or WHERE inbox_id = ${inboxId} as appropriate. If the routine is intended to operate per-tenant, accept a tenantId and iterate per-tenant to avoid cross-tenant reads.
- **server/email.ts:424** — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} is performed without tenant_id in the WHERE clause. This is a write to a tenant-scoped table that lacks tenant scoping and could modify another tenant's row by id.
  - _Fix:_ Include tenant scoping in the UPDATE: add AND tenant_id = ${tenantId} (or AND inbox_id = ${inboxId}) in the WHERE clause. Ensure the code derives and passes the tenant context for each row before performing updates.
- **server/embeddings.ts:684** — UPDATE to tenant-scoped table (memory_entries / agent_knowledge) performed without constraining by tenant_id. storeEmbeddingVec(table, id, ...) updates row by id only, allowing cross-tenant writes if called with an id from another tenant.
  - _Fix:_ Require the caller to pass tenantId into storeEmbeddingVec and include AND tenant_id = ${tenantId} in the WHERE clause (parameterized). Example: UPDATE <table> SET ... WHERE id = ${id} AND tenant_id = ${tenantId}. Avoid relying on id-only updates for tenant-scoped tables.
- **server/embeddings.ts:687** — backfillMissingKnowledgeEmbeddings selects from agent_knowledge without any tenant_id filter. This reads across all tenants' knowledge rows and then calls storeEmbeddingVec which performs tenant-unsafe updates.
  - _Fix:_ Run backfills on a per-tenant basis or otherwise include a tenant_id filter in the SELECT. E.g. accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to the SELECT, and ensure subsequent updates are also constrained by tenant_id.
- **server/embeddings.ts:716** — backfillEmbeddingVecs selects memory_entries rows without tenant_id filtering and later UPDATEs memory_entries SET embedding_vec WHERE id = ${row.id} without tenant constraint. Both the SELECT and UPDATE operate across all tenants.
  - _Fix:_ Perform backfill per-tenant (accept tenantId) or add tenant_id = ${tenantId} to both the SELECTs and the UPDATEs. Ensure every write includes the tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}).
- **server/embeddings.ts:732** — backfillEmbeddingVecs selects agent_knowledge rows without tenant_id filtering and later UPDATEs agent_knowledge WHERE id = ${row.id} without tenant constraint. This allows cross-tenant reads and writes.
  - _Fix:_ As above, scope the backfill to a specific tenant (pass tenantId) or add tenant_id = ${tenantId} to both the SELECT and the UPDATE statements. All writes to tenant-scoped tables must include tenant_id in the WHERE clause.
- **server/google-drive.ts:547** — Query selects project row by id from tenant-scoped table 'projects' without scoping by tenant_id (WHERE eq(projects.id, projectId)). Reading a tenant-scoped row by id without tenant constraint can leak another tenant's data.
  - _Fix:_ Include tenant scoping in the SELECT (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or use Drizzle eq(projects.tenantId, tenantId). If you must read then verify, do so using a parameterized query that enforces p.tenant_id = callerTenant before returning any columns.
- **server/google-drive.ts:560** — UPDATE against the tenant-scoped 'projects' table uses only the project primary key in the WHERE clause (WHERE eq(projects.id, projectId)) without also constraining tenant_id. Even if ownership was checked earlier, the UPDATE itself is not scoped and could modify a foreign-tenant row if prior checks were bypassed.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well, e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId} (Drizzle: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))). This makes the write fail-safe and enforces isolation at the SQL level.
- **server/google-drive.ts:826** — Handler reads a project row by id (SELECT ... FROM projects WHERE id = ${params.projectId}) using a caller-supplied projectId but does not include tenant scoping in the SQL. The SELECT occurs before ownership is effectively validated, so it constitutes an unscoped read of a tenant-scoped table.
  - _Fix:_ Perform tenant-scoped SELECTs (e.g. WHERE id = ${projectId} AND tenant_id = ${callerTenantId}) or otherwise enforce ownership in the same parameterized query. Avoid reading tenant-scoped rows by id without including the caller's tenant in the WHERE clause to prevent accidental cross-tenant disclosure.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a session row without constraining by tenant_id. If presenter_sessions is tenant-scoped this allows an update to another tenant's session via a session id.
  - _Fix:_ Constrain updates by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or validate session ownership before the UPDATE. Use the trusted tenantId from the execution context rather than any caller-provided identifier.
- **server/health-audit.ts:314** — SELECT FROM code_proposals (stale proposals) runs without a tenant_id filter. code_proposals appears tenant-scoped, so this reads across tenants.
  - _Fix:_ Run per-tenant or add a tenant_id = ${tenantId} predicate. If this audit is an admin/global scan, make explicit that it is running in an admin context and document/guard the operation; otherwise require a tenant parameter and scope the SELECT to it.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks queries enabled/never-run tasks with no tenant_id filter. heartbeat_tasks is likely tenant-scoped, so this returns tasks across tenants.
  - _Fix:_ Scope the SELECT by tenant (WHERE tenant_id = ${tenantId}) or otherwise limit the audit to a specific tenant. If heartbeat_tasks are global, document that and verify it is intended.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET ... WHERE id = ${sched.id} — no tenant_id constraint. This can modify a schedule belonging to another tenant if ids are global.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id} (or better, use a storage helper that asserts tenant context).
- **server/heartbeat.ts:107** — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${sched.tenant_id}) or perform the update through a tenant-aware storage function that validates ownership before writing.
- **server/heartbeat.ts:112** — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant constraint.
  - _Fix:_ Restrict the UPDATE to the schedule's tenant (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use tenant-aware storage APIs.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Include AND tenant_id = ${sched.tenant_id} in the WHERE clause or call a storage helper that enforces the task's tenant context before updating.
- **server/heartbeat.ts:260** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. heartbeat_tasks are tenant-scoped and this update can affect other tenants' rows.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use storage.updateHeartbeatTask which enforces tenant context.
- **server/heartbeat.ts:264** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant filter on a tenant-scoped table.
  - _Fix:_ Constrain the update by tenant (AND tenant_id = ${t.tenantId}) or perform the mutation via a tenant-aware storage wrapper that asserts the tenant context.
- **server/heartbeat.ts:1814** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... — no tenant_id constraint. heartbeat_logs is likely tenant-scoped; this reads logs across tenants.
  - _Fix:_ Add tenant filtering (e.g. WHERE task_name = ... AND tenant_id = ${tenantId}) or use a storage API that enforces tenant context when reading heartbeat logs.
- **server/heartbeat.ts:2908** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint after creating a task. This may change approval_status for a task ID from another tenant.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use a tenant-aware update method. Prefer calling storage.updateHeartbeatTask which should assert tenant ownership before mutating.
- **server/heartbeat.ts:3360** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint (appears in delegateTaskFromChat).
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}) or use a storage wrapper that enforces the caller's tenant ownership before updating approval_status.
- **server/job-queue.ts:177** — completeJob updates `agent_jobs` by id/status/attempts but does not constrain by tenant_id. Completing a job by id can affect jobs belonging to other tenants.
  - _Fix:_ Add tenant_id to the function signature and to the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}), or otherwise verify the job's tenant before completing.
- **server/job-queue.ts:220** — failJob reads max_attempts and performs terminal/requeue UPDATEs on `agent_jobs` by id with NO tenant_id constraints. Both the SELECT and UPDATEs can be applied to another tenant's job id.
  - _Fix:_ Require and use tenant_id when selecting/updating agent_jobs (SELECT ... WHERE id=${id} AND tenant_id=${tenantId}), or validate ownership upfront. Update the function signatures to accept tenantId (or enforce admin-only usage) and include tenant_id in all WHERE clauses.
- **server/job-queue.ts:273** — cancelJob updates `agent_jobs` WHERE id = ${id} AND status IN (...) with NO tenant_id constraint. Tenants (or callers) may cancel jobs that belong to other tenants by id.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or require caller-side verification of the job's tenant before allowing cancellation. Prefer explicit tenant parameter and parameterized WHERE clauses.
- **server/job-queue.ts:282** — retryJob updates `agent_jobs` WHERE id = ${id} AND status IN (...) with NO tenant_id constraint. This allows retries/resets of jobs across tenants by id.
  - _Fix:_ Include tenant_id in the WHERE clause or accept tenantId as parameter and validate ownership prior to touching the job. Ensure retry operations cannot be invoked by unprivileged callers using only a job id.
- **server/lib/decline-events.ts:82** — Manual construction and interpolation of a Postgres array literal from request-derived flaggedCategories (flaggedLiteral) is used inside the SQL template. This builds SQL syntax from user input and risks injection or malformed-literal issues.
  - _Fix:_ Avoid building SQL array literals by hand. Use proper parameterized array binding supported by your DB/ORM or persist the values as a JSONB parameter instead. If your driver/ORM lacks array-binding helpers, pass each element as a bound parameter or use a JSONB column (JSON.stringify(opts.flaggedCategories) :: jsonb) so no SQL syntax is constructed from user input. Validate/escape inputs at the element level and remove the manual '{...}' string construction.
- **server/lib/agent-trace.ts:124** — UPDATE to agent_trace_spans omits tenant_id in the WHERE clause (UPDATE ... WHERE span_id = ${spanId}). agent_trace_spans is tenant-scoped, so this update can modify rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE predicate. Retrieve the tenant id from the span context (currentTenantId() or persisted open-row value) and add AND tenant_id = ${tenantId} to the WHERE clause, or change the function signature to accept tenantId and assert it. Never perform updates on tenant-scoped tables keyed only by a UUID without also filtering by tenant_id.
- **server/lib/action-ledger.ts:304** — sweepStaleAttempts performs an UPDATE on action_attempts without a tenant_id predicate (it updates rows whose id IN (SELECT id FROM action_attempts ...)). This will transition rows for all tenants, breaking the expectation that writes are constrained to an owner's tenant.
  - _Fix:_ Run the sweep per-tenant (accept a tenantId and include WHERE tenant_id = ${tenantId} in both the inner SELECT and/or outer UPDATE), or otherwise explicitly document and gate this cross-tenant maintenance as an operator-only job. Prefer per-tenant reconciliation to preserve tenant isolation and to make writes auditable per tenant.
- **server/lib/bwb-job-progress.ts:74** — The scope() helper can return a predicate that omits tenant_id when env BWB_TENANT_ID (or explicit tenant) is missing; multiple UPDATE/SELECT calls use scope(...) which may therefore modify rows across tenants (writes pinned only by jobId).
  - _Fix:_ Fail closed when tenant context is not available: require a valid tenantId for any writer path rather than silently omitting the tenant predicate. Change scope() to require tenantId (or have callers pass tenantId explicitly) and abort/throw when missing, or document and gate the no-tenant behavior strictly to operator-only processes. This ensures UPDATE/SELECT statements always include AND video_jobs.tenant_id = ${tenantId}.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT on tenant-scoped table `projects` has no tenant_id WHERE clause — the query aggregates project counts across all tenants.
  - _Fix:_ Restrict the aggregation to the tenants you intend to operate on (e.g. WHERE tenant_id = ${tenantId} or WHERE tenant_id = ANY(${array_of_snapshot_tenant_ids}::int[])). If the cross-tenant aggregate is intentional, make it an explicit admin-only operation with clear comments and bounds; otherwise add the tenant_id filter so per-tenant isolation is preserved.
- **server/lib/ideabrowser-backfill.ts:100** — SELECT FROM tenant-scoped table `project_files` uses project_id but does not constrain by tenant_id — reading rows without explicit tenant scoping.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. AND tenant_id = ${tenantId}) or otherwise verify ownership of the referenced project_id before querying project_files so a row from another tenant can't be read.
- **server/lib/ideabrowser-backfill.ts:115** — SELECT FROM tenant-scoped table `project_notes` uses project_id but lacks tenant_id filtering — reading without explicit tenant scoping.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE, or otherwise assert/verify that the referenced project_id belongs to the tenant before querying project_notes.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` is done without a tenant_id constraint (lookup by key_prefix only). This reads tenant-scoped rows without constraining them to a caller's tenant.
  - _Fix:_ If the intent is to resolve tenant from the presented key, make the design explicit and minimise cross-tenant exposure: (a) prefer a two-step safe flow—SELECT only the hashed secret (not other tenant-sensitive fields) by prefix, perform the constant-time hash check, then load tenant-scoped data only for the verified key; or (b) require/accept a tenantId from the caller and include `AND tenant_id = ${tenantId}` in the WHERE clause; or (c) move prefix→tenant mapping into a dedicated, tightly-scoped/authenticated mapping table so accidental cross-tenant reads are impossible. Also ensure tight rate-limiting on prefix lookups and a unique index on key_prefix to limit enumeration risk.
- **server/lib/scheduled-post-runner.ts:164** — Per-row UPDATE to scheduled_posts uses WHERE id = ${row.id} with no tenant_id guard. This is a write to a tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) to ensure the update cannot affect rows in other tenants.
- **server/lib/scheduled-post-runner.ts:191** — Error/release-path UPDATE to scheduled_posts uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id. This can update/delete rows across tenants on error handling paths.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} AND status = 'publishing') so error handling cannot affect other tenants' rows.
- **server/lib/scheduled-post-runner.ts:266** — SQL array literal for platforms (arrLit) is constructed via string interpolation from request-derived input and then injected into the SQL template (VALUES (${params.tenantId}, ${arrLit}::text[], ...)). This is string-interpolated SQL using request-derived data (injection + isolation risk).
  - _Fix:_ Avoid building SQL fragments from user input. Pass the platforms as a parameterized array (e.g. use pg parameter array binding or Drizzle helpers such as sql.array/typed parameters) instead of embedding an array literal string. Ensure proper parameterization/escaping rather than concatenation.
- **server/martech-bundle.ts:325** — UPDATE on tenantVoiceProfiles performed using only the profile PK (WHERE id = existing.id) with no tenant_id constraint. This updates a tenant-scoped table without explicitly constraining the tenant.
  - _Fix:_ Include the tenant_id in the WHERE clause (or otherwise verify ownership) when updating tenantVoiceProfiles. Example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Failing that, re-query/validate the row belongs to the caller's tenant before updating.
- **server/memory-graph.ts:66** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — no tenant_id constraint. This mutates a tenant-scoped table row without constraining by tenant.
  - _Fix:_ Constrain the UPDATE to the tenant, e.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Alternatively, pass tenantId into this code path and validate category ownership before updating.
- **server/memory-graph.ts:126** — refreshCategoryCount updates memory_categories using a subquery that counts memory_entries but neither the subquery nor the outer UPDATE include a tenant filter. This can aggregate/count across tenants and update the wrong tenant's row.
  - _Fix:_ Change refreshCategoryCount to accept tenantId and use it in both the subquery and the outer WHERE. Example: UPDATE memory_categories SET memory_count = (SELECT count(*) FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status='active' AND deleted_at IS NULL) WHERE id = ${categoryId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:181** — SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ... LIMIT 1 — no tenant constraint. If memory_links is tenant-scoped, this query checks/reads links without constraining by tenant.
  - _Fix:_ Include tenant filtering in the existence check (or otherwise join to a tenant-scoped table and ensure the linked row belongs to the same tenant). For example, add AND tenant_id = ${tenantId} or JOIN memory_entries to ensure the candidate link is within the same tenant context.
- **server/minerva-planner.ts:373** — SELECT from tenant-scoped table `plans` executed without tenant_id constraint (probe SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}). This can expose or read rows across tenants (IDOR).
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE id = ${args.planId} AND tenant_id = ${tenantId}). Ensure the function receives/derives the caller's tenant and use it for every read from plans.
- **server/minerva-planner.ts:391** — SELECT id FROM plans WHERE parent_plan_id = ${args.planId} executed without tenant_id constraint. This may return child plans belonging to other tenants.
  - _Fix:_ Filter by tenant_id as well (e.g. WHERE parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}). Ensure the tenantId used is the caller's tenant and propagate it into this query.
- **server/minerva-planner.ts:459** — UPDATE rollback on tenant-scoped table `plans` performed without tenant scoping (WHERE id = ${args.planId} AND status = 'revising' with no tenant_id). This can roll back a plan in another tenant inadvertently.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause of the rollback UPDATE and ensure the tenantId is validated from the caller/session before performing the rollback.
- **server/minds-engine.ts:214** — INSERT into tenant-scoped table `mind_tickets` builds a Postgres array string from request-derived params.dependsOn via string interpolation (`{${params.dependsOn.join(',')}}`). This is string-interpolated SQL using caller data and risks SQL injection or isolation bypass.
  - _Fix:_ Avoid building SQL array literals by concatenation. Validate and sanitize dependsOn (ensure each element is an integer), then pass the array as a parameterized bind (e.g. use a parameterized array or Drizzle's array binding) instead of embedding a formatted string. Example: VALUES (..., ${params.dependsOn && params.dependsOn.length > 0 ? params.dependsOn : null}) with a proper array parameter, or use explicit INSERT helpers in Drizzle to bind the array safely.
- **server/minds-engine.ts:327** — UPDATE on tenant-scoped table `mind_events` uses string-interpolated array literal for eventIds (`id = ANY(${`{${eventIds.join(',')}}`}::int[])`) where eventIds is request-derived. This is string-interpolated SQL with caller data and risks SQL injection and isolation issues.
  - _Fix:_ Use parameterized array binding instead of concatenating into an array literal. Validate/normalize eventIds (ensure numeric ids), then use a parameterized query such as WHERE id = ANY(${eventIds}) AND tenant_id = ${tenantId} (binding eventIds as an array), or use Drizzle/pg parameter arrays to safely pass the list.
- **server/pace-control.ts:85** — SELECT COUNT(*) FROM tenant-scoped table `heartbeat_logs` is executed without any tenant_id filter — counting activity across all tenants.
  - _Fix:_ Require and accept a tenantId (or an admin-only mode) and add `AND tenant_id = ${tenantId}` to the WHERE clause. If this query is meant to be per-tenant, enforce the tenant filter; if it must be global, restrict execution to an explicit admin context and document it.
- **server/pace-control.ts:140** — getPaceSnapshot queries `heartbeat_logs` (SELECT ... WHERE created_at > ...) without tenant_id — returns cross-tenant activity data.
  - _Fix:_ Make this function tenant-scoped by accepting a tenantId parameter and adding `WHERE tenant_id = ${tenantId}` (or require an explicit admin flag). Ensure any dashboard/ops use of a global query is limited to authorized admin code paths.
- **server/plan-executor.ts:116** — UPDATE on tenant-scoped table `plans` executed without constraining by tenant_id (appendExecutionLog updates WHERE id = ${planId} only). This allows cross-tenant writes to a plan row identified only by id.
  - _Fix:_ Change appendExecutionLog to accept/derive the plan's tenant_id and include AND tenant_id = ${tenantId} in the WHERE clause (or use a parameterized claim that includes tenant_id). Validate the tenant at claim time and carry it into every subsequent UPDATE to ensure writes are scoped to the plan's tenant.
- **server/plan-executor.ts:527** — UPDATE on tenant-scoped table `plans` performed without tenant_id constraint (sets status='failed' WHERE id = ${planId}).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}) or derive tenantId from the previously returned/claimed plan row and use it in the UPDATE to prevent modifying another tenant's plan.
- **server/plan-executor.ts:725** — Final UPDATE to `plans` updates status/execution_log WHERE id = ${planId} with no tenant_id filter. Writes to tenant-scoped table are not being constrained to the owning tenant.
  - _Fix:_ Use the plan's tenant_id (from the claimed row) in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Alternatively, pass tenantId into this update helper so every write enforces tenant scoping.
- **server/plan-executor.ts:759** — Error-path UPDATE on `plans` (marking failed) uses WHERE id = ${planId} AND status = 'executing' but omits tenant_id. This can change plan state for other tenants' rows.
  - _Fix:_ Add tenant scoping to the WHERE clause (AND tenant_id = ${tenantId}) using the plan's tenant_id established when the plan was claimed; make sure all error/recovery paths include the tenant guard.
- **server/plan-executor.ts:775** — resumeStuckPlans performs a global UPDATE resetting any stale 'executing' plans to 'approved' without tenant restriction (WHERE status='executing' AND updated_at < ...). This modifies rows across all tenants.
  - _Fix:_ Restrict the reset to a specific tenant context or iterate per-tenant (or require and verify an explicit admin/maintenance tenantId) and include tenant_id = ${tenantId} in the WHERE clause. Ensure this maintenance operation is explicitly authorized and audited if it must act cross-tenant.
- **server/plan-executor.ts:784** — resumeStuckPlans SELECTs pending plans via SELECT id FROM plans WHERE status = 'approved' ... with no tenant filter and then calls executePlan() on them. This enumerates and triggers execution for plans from any tenant without scoping the operation.
  - _Fix:_ Limit the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or only run this boot-time kick for tenants the process is authorized to act on. If global behavior is required, ensure the caller is a trusted admin process and add explicit authorization and logging; otherwise, iterate per-tenant and pass tenant_id into executePlan so all downstream writes remain scoped.
- **server/process-governor.ts:121** — Subquery reads heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads a tenant-scoped table (heartbeat_logs) without a tenant constraint.
  - _Fix:_ Add tenant_id = ${tenantId} to the subquery (e.g. SELECT COUNT(*) FROM heartbeat_logs WHERE tenant_id = ${tenantId} AND task_id = ht.id ...) or otherwise validate ownership of the referenced task before counting logs.
- **server/process-governor.ts:155** — Subquery counts heartbeat_tasks without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_tasks WHERE persona_id = ad.persona_id ...). This reads a tenant-scoped table (heartbeat_tasks) without a tenant constraint.
  - _Fix:_ Include tenant_id = ${tenantId} in the subquery (e.g. SELECT COUNT(*) FROM heartbeat_tasks WHERE tenant_id = ${tenantId} AND persona_id = ad.persona_id ...) or validate persona ownership before querying.
- **server/process-governor.ts:199** — Query aggregates heartbeat_logs (AVG(duration_ms) FROM heartbeat_logs ...) with no tenant_id filter. Reading heartbeat_logs without tenant scoping can leak other tenants' telemetry.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...) so only the caller's tenant data is aggregated.
- **server/process-governor.ts:240** — Query counts distinct persona_id from heartbeat_logs (COUNT(DISTINCT persona_id) FROM heartbeat_logs ...) but omits tenant_id filter. This reads a tenant-scoped table without tenant constraint.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE tenant_id = ${tenantId} AND status != 'success' ...) or otherwise ensure persona ownership is validated.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed without filtering by tenant_id. projects is a tenant-scoped table; querying by id alone can expose data from other tenants.
  - _Fix:_ Require and pass tenantId into updateProjectBrain and add WHERE tenant_id = ${tenantId} (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}), or validate project ownership before reading.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 lacks tenant scoping on projects, conversations, and messages. This can enumerate projects across tenants.
  - _Fix:_ Add tenant_id filters (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId} AND (SELECT COUNT(*) FROM messages m WHERE m.tenant_id = ${tenantId} AND m.conversation_id = c.id AND role = 'assistant') > 0) or otherwise restrict the backfill to a specific tenant supplied by the caller.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed without tenant_id scoping. Conversations are tenant-scoped and this can return other tenants' conversations if project ids overlap across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or ensure proj was fetched within the tenant context and validated before use.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant_id constraint. messages is tenant-scoped and reading by conversation id alone may access another tenant's messages.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or verify conversation ownership first.
- **server/proposal-verifier.ts:33** — SELECT id, target_file, code_diff, validation_result, description, rationale FROM code_proposals WHERE id = ${proposalId} lacks a tenant_id filter. code_proposals is tenant-scoped; selecting by id only can return other tenants' proposals.
  - _Fix:_ Require tenantId for verification calls and constrain the SELECT (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or validate proposal ownership before reading.
- **server/reference-learner.ts:290** — UPDATE memory_entries executed without constraining by tenant_id (WHERE id = ${storedId} only). This allows a caller who can influence storedId to update a row belonging to another tenant.
  - _Fix:_ Include the tenant check in the UPDATE predicate. For example: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId}. Also verify rowCount/RETURNING to ensure the row belonged to that tenant and fail if not.
- **server/recurring-messages.ts:300** — UPDATE agent_knowledge sets content by id only (`WHERE id = ${r.id}`) with no tenant_id constraint. Even though the row was read from a global scan, the write can modify a row across tenants if an id is known.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) to ensure the write is limited to the owning tenant. Also check the affected rowCount and fail/log if zero to detect mismatches.
- **server/routes.ts:1529** — /api/public/stats executes SELECT count(*) FROM conversations/messages/heartbeat_tasks/memory_entries/heartbeat_logs with no tenant_id filter — querying tenant-scoped tables unscoped leaks per-tenant counts/metadata across tenants.
  - _Fix:_ If cross-tenant aggregation is intentional, compute/store these metrics in a dedicated, access-controlled metrics table or only allow admin access. Otherwise add tenant_id filtering (WHERE tenant_id = <caller tenant>) or aggregate in a background job that intentionally and safely exposes only non-sensitive, approved metrics.
- **server/routes.ts:1557** — /api/public/architecture performs many raw SELECTs (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) without tenant_id constraints — tenant-scoped tables are being read globally from an unauthenticated/public endpoint.
  - _Fix:_ Restrict these queries to an admin-only endpoint or build an explicit aggregated view/table for public consumption. If per-tenant data is required, add WHERE tenant_id = <caller tenant> (or only return intentionally anonymized/global counts computed offline).
- **server/routes.ts:2420** — Background job in /api/store/lookup-orders does db.select(...).from(deliveryLogs).where(lower(delivery_logs.customer_email) = ${rawEmail}) with NO tenant_id constraint and then proceeds to send a recovery email. This exposes the ability to discover orders across tenants by email and can return tenant-scoped rows to the recovery flow.
  - _Fix:_ Restrict deliveryLogs lookups by tenant where possible, or otherwise ensure the recovery flow cannot leak or operate on cross-tenant data. Consider storing per-tenant indices or perform the lookup within the tenant context; if a platform-level lookup is required, perform it with elevated (audited) privileges and minimize returned data, and ensure email-sending cannot be abused to enumerate tenant data.
- **server/routes.ts:2564** — POST /api/store/verify-orders performs db.select() FROM delivery_logs WHERE lower(customer_email) = ${rawEmail} and returns a list of orders, without any tenant_id filtering. This returns tenant-scoped order rows for the provided email address across all tenants (subject to code verification), violating per-tenant read scoping.
  - _Fix:_ Tie codes to a tenant context (store tenant_id with the order-lookup code) and, on verification, only return orders for that specific tenant_id. Alternatively, include tenant_id in the verification flow or scope the query to a single tenant; avoid unscoped searches on tenant-scoped tables that return lists of rows across tenants.
- **server/routes.ts:4330** — INSERT into `project_notes` does not set tenant_id. The code performs `INSERT INTO project_notes (project_id, note, author) VALUES (...)` without supplying tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL with no default) and omission can cause privacy/misattribution or DB errors.
  - _Fix:_ Include tenant_id in the INSERT (e.g. `INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, 'system:context-guard')`) or call a storage helper that enforces tenant-scoped persistence and validates that the project belongs to the tenant before inserting.
- **server/routes/admin.ts:569** — SELECT from tool_performance has no tenant filter — queries tenant-scoped telemetry without constraining to caller's tenant.
  - _Fix:_ Add an application-level tenant filter (e.g. WHERE tenant_id = ${tenantId}) or otherwise scope the query to the caller's tenant. If platform-wide aggregation is intended, make that explicit (requirePlatformAdmin) and centralize the cross-tenant semantics so the isolation invariant is not violated accidentally.
- **server/routes/admin.ts:574** — SELECT from tool_performance (recent failures) omits tenant_id — returns per-tool performance across all tenants.
  - _Fix:_ Constrain this query by tenant_id (e.g. AND tenant_id = ${tenantId}) before aggregation, or explicitly document+gate cross-tenant aggregation behind a platform-admin-only path that intentionally queries all tenants.
- **server/routes/admin.ts:587** — SELECT from ai_insights (status='new') has no tenant filter — reads incident rows across all tenants.
  - _Fix:_ Add a tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}) to limit results to the caller's tenant, or explicitly require and validate a platform-admin contract for cross-tenant reads and centralize that behavior.
- **server/routes/admin.ts:598** — SELECT from heartbeat_tasks omits tenant constraint — can leak tenant-scoped heartbeat task state across tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} (or equivalent) in the WHERE clause to scope to the caller's tenant, or make cross-tenant intent explicit and guarded by a platform-admin-only policy.
- **server/routes/admin.ts:609** — SELECT from ai_insights (status='applied' and specific action_taken) lacks tenant_id filter — reads tenant-scoped records without scoping.
  - _Fix:_ Constrain by tenant_id (WHERE tenant_id = ${tenantId}) or move this cross-tenant inspection behind an auditable platform-admin path that intentionally and safely aggregates across tenants.
- **server/routes/admin-zombie-detector.ts:38** — Query over agent_trace_spans (used→used AS ...) does not include tenant_id — may return trace data from other tenants.
  - _Fix:_ Add tenant scoping to the agent_trace_spans subquery (e.g. WHERE tenant_id = ${tenantId} ...) or otherwise ensure the endpoint only returns data scoped to the caller's tenant. If platform-level aggregation is required, make it explicit and centrally guarded by platform-admin checks.
- **server/routes/admin-zombie-detector.ts:54** — Query that builds 'used' for personas (SELECT persona usage from conversations) omits tenant_id — aggregating conversation usage across tenants.
  - _Fix:_ Constrain the conversations query by tenant_id (e.g. WHERE tenant_id = ${tenantId}) before grouping by persona_id, or require an explicit platform-admin gate for any cross-tenant aggregation and document the intended behavior.
- **server/routes/conversations.ts:98** — UPDATE on conversations omits tenant constraint — 'UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}' can modify a conversation in another tenant if an attacker can control ids.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId}) or use a storage API that enforces tenant ownership before updating.
- **server/routes/conversations.ts:99** — SELECT from project_conversations omits tenant constraint — 'SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}' reads a tenant-scoped table without filtering by tenant_id.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or use a tenant-aware storage helper.
- **server/routes/conversations.ts:102** — INSERT into project_conversations omits tenant_id — 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})) or use a storage API that populates tenant_id.
- **server/routes/conversations.ts:104** — UPDATE on projects omits tenant constraint — 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' can update another tenant's project if ids are guessed.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure only the caller's tenant row is modified.
- **server/routes/conversations.ts:227** — INSERT into project_notes omits tenant_id — 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' does not set tenant_id on a tenant-scoped table, allowing notes to be associated without tenant ownership enforced at DB layer.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) or use a tenant-aware helper to persist project notes.
- **server/routes/inbox-notifications.ts:101** — UPDATE inbox_messages omits tenant constraint — 'UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}' updates a message by id only, enabling marking messages from other tenants if an id is known.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${msg.id} AND tenant_id = ${tenantId}) or use a storage function that enforces tenant ownership before mutating the row.
- **server/routes/metrics.ts:63** — SELECT from tenant-scoped table `agent_cost_ledger` with no tenant_id constraint (aggregates cost across all tenants).
  - _Fix:_ If this should be tenant-scoped, add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(..., tenantId))). If the aggregate across tenants is intentional, ensure the endpoint is strictly operator-only (strong auth, audited) and consider returning only non-sensitive, fully-aggregated metrics or masking tenant identifiers to reduce blast radius.
- **server/routes/metrics.ts:79** — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) with no tenant_id constraint (reads across all tenants).
  - _Fix:_ Either restrict the query to the caller's tenant (WHERE tenant_id = ${tenantId}) or, if a global aggregate is required, ensure the endpoint is tightly protected (operator-only) and limit the exposed dimensions to avoid per-tenant leakage.
- **server/routes/metrics.ts:95** — SELECT from tenant-scoped table `agent_cost_ledger` (token totals) with no tenant_id constraint (global read of tenant data).
  - _Fix:_ Constrain to tenant_id = ${tenantId} when serving tenant requests. If global totals are required for operators, move to a dedicated, strongly-authenticated admin-only path and minimize the sensitivity of returned data.
- **server/routes/oauth-subscriptions.ts:325** — SELECT DISTINCT tenant_id FROM tenant-scoped table `oauth_subscriptions` with no tenant_id constraint — enumerates tenant IDs with inactive Google subscriptions.
  - _Fix:_ Avoid global scans that enumerate tenant IDs in a handler exposed beyond a highly-trusted operator context. Either (1) restrict the operation to platform-admin only and document/verify the auth/secret gating, or (2) constrain the query to a single tenant (WHERE tenant_id = ${tenantId}) for tenant-scoped endpoints. If a global repair/cron must run, run it from an out-of-band, audited admin process with no external HTTP exposure.
- **server/sculptor.ts:94** — UPDATE on tenant-scoped table `sculptor_sessions` lacks tenant_id constraint (WHERE id = ${session.id} only). This can modify another tenant's row if ids collide or are guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise verify the session belongs to the caller's tenant before updating. Ensure every UPDATE/DELETE on sculptor_sessions enforces AND tenant_id = <caller tenant>.
- **server/sculptor.ts:101** — UPDATE on tenant-scoped table `sculptor_sessions` (setting conversation_id) lacks tenant_id constraint (WHERE id = ${session.id} only).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or check ownership first. All mutation queries against sculptor_sessions must include tenant_id = <caller tenant>.
- **server/sculptor.ts:162** — UPDATE on tenant-scoped table `sculptor_sessions` in monitorSession() lacks tenant_id constraint (WHERE id = ${sessionId} only).
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) so the periodic monitor cannot update sessions belonging to other tenants. Maintain tenant_id in all WHERE clauses for sculptor_sessions.
- **server/sculptor.ts:347** — UPDATE on tenant-scoped table `sculptor_sessions` (writing review_result) lacks tenant_id constraint (WHERE id = ${sessionId} only).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or assert session ownership before writing the review_result. Every write to sculptor_sessions must be guarded by tenant_id = <caller tenant>.
- **server/second-opinion.ts:407** — UPDATE on tenant-scoped table `agent_cost_ledger` (settleFusionReservation) uses WHERE id = ${reservationId} without constraining tenant_id. Updating by id alone can affect another tenant's ledger row.
  - _Fix:_ Require/resupply the tenant_id when settling a reservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or first select and verify the reservation row's tenant before updating. Ensure settleFusionReservation cannot update rows outside the caller's tenant.
- **server/second-opinion.ts:426** — UPDATE on tenant-scoped table `agent_cost_ledger` (releaseFusionReservation) uses WHERE id = ${reservationId} without constraining tenant_id. This can zero-out or alter another tenant's reservation row by id alone.
  - _Fix:_ Require the tenant_id (or verify reservation ownership) and include AND tenant_id = ${tenantId} in the WHERE clause, or fetch+validate the reservation row's tenant before performing the UPDATE. Enforce tenant scoping on all ledger UPDATEs.
- **server/seed.ts:3205** — SELECT from conversations (unlinked detection) has no tenant constraint. The query: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id)` can return rows across all tenants, and subsequent INSERTs (project_conversations) will link conversations/projects potentially across tenant boundaries.
  - _Fix:_ Constrain the SELECT to a specific tenant (e.g. add `AND c.tenant_id = ${tenantId}` or equivalent Drizzle .where(eq(conversations.tenantId, tenantId))). If this is intended to run only for tenant 1 in seed code, make tenant explicit in the WHERE and in any derived INSERTs. Always validate ownership of referenced project_id/conversation_id before inserting links.
- **server/seed.ts:4431** — DELETE from heartbeat_tasks is executed without a tenant filter: `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'`. heartbeat_tasks is tenant-scoped (indexes reference tenant_id) so this will delete rows across all tenants.
  - _Fix:_ Add an explicit tenant predicate (e.g. `AND tenant_id = ${tenantId}`) or run the cleanup per-tenant so only tasks for the intended tenant are affected. Never perform DELETE/UPDATE on tenant-scoped tables without scoping to the caller's tenant.
- **server/seed.ts:4459** — UPDATE memory_entries (normalizer) runs without tenant scoping: `UPDATE memory_entries SET fact = REPLACE(fact, 'Manjaro', 'wellness-program') WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ...`. memory_entries is tenant-scoped (index exists on tenant_id) so this will modify memory rows across all tenants.
  - _Fix:_ Restrict the update to the intended tenant (e.g. add `AND tenant_id = ${tenantId}`) or iterate per-tenant and apply the UPDATE with the tenant bound. Ensure every UPDATE against tenant-scoped tables includes an explicit tenant_id filter.
- **server/self-improvement.ts:171** — SELECT from tenant-scoped table `experiments` can execute without a tenant_id filter when tenantId is omitted. The .where(...) builds conditions optionally and will run with only category predicate if tenantId is not provided, causing an unscoped read across tenants.
  - _Fix:_ Enforce tenant scoping: require tenantId (fail-closed) or always include eq(experiments.tenantId, tenantId) in the WHERE. If tenantId is optional by design, explicitly restrict the API (or return empty) when tenantId is not provided to avoid cross-tenant data exposure.
- **server/self-improvement.ts:521** — Unscoped SELECT: getExperimentHistory falls back to db.select().from(experiments) with no tenant_id condition when no category/tenantId conditions are supplied, allowing cross-tenant data leaks.
  - _Fix:_ Require a valid tenantId or fail-closed; always add WHERE tenant_id = <caller tenant> to queries against experiments. If callers legitimately need global access, move to a truly global table or implement an explicit admin-only codepath with strict authorization checks.
- **server/skillify.ts:91** — SELECT from `messages` by conversationId is executed without scoping to tenant (no WHERE tenant_id = …). This reads tenant-scoped data without an explicit tenant filter.
  - _Fix:_ Constrain the query to the caller's tenant, e.g. add .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId))) or use tenantScope(requireTenantScope(messages.tenantId, tenantId)) after validating the conversation belongs to the tenant.
- **server/skillify.ts:201** — SELECT from `messages` (in analyzeConversation) uses only conversationId and lacks an explicit tenant_id condition. Reading a tenant-scoped table without a tenant filter risks cross-tenant data exposure.
  - _Fix:_ Add an explicit tenant constraint to the WHERE clause (eq(messages.tenantId, tenantId)) or use the tenantScope helper. Alternatively, validate and then include the tenantId in the query to enforce scoping at the SQL level.
- **server/skillify.ts:221** — SELECT from `personas` by persona id is executed without scoping to tenant (no WHERE tenant_id = …). If `personas` is tenant-scoped, this can return persona rows from other tenants.
  - _Fix:_ Include a tenant constraint on the persona lookup (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, tenantId)))) or otherwise verify the persona belongs to the tenant before using its data.
- **server/skillify.ts:329** — SELECT from `messages` (recent messages for LLM prompt) is executed without scoping to tenant (no WHERE tenant_id = …). This fetches tenant-scoped messages without an explicit tenant filter.
  - _Fix:_ Constrain this read by tenant (e.g. .where(and(eq(messages.conversationId, conversationId), eq(messages.tenantId, tenantId)))) or derive and validate tenant ownership and include tenantId in the SQL predicate.
- **server/skills-marketplace.ts:207** — SELECT id FROM `skills` WHERE name = ${template.name} runs with no tenant constraint. If `skills` is tenant-scoped this can read across tenants.
  - _Fix:_ If skills are tenant-scoped, require and pass a tenantId and add WHERE tenant_id = ${tenantId}. If skills are intentionally global, document that and ensure the table has no tenant_id column; otherwise enforce tenant-scoping at the SQL level.
- **server/skills-marketplace.ts:224** — SELECT * FROM `skills` WHERE id = ${skillId} has no tenant constraint. Reading a tenant-scoped skills row by id without tenant scoping risks exposing another tenant's skill.
  - _Fix:_ If `skills` are tenant-scoped, include WHERE tenant_id = ${tenantId} (and require tenantId from the caller). If the table is intentionally global, make that explicit in schema and code comments.
- **server/storage.ts:239** — getConversationUnscoped performs an unscoped SELECT on the tenant-scoped conversations table (WHERE tenant_id is not applied).
  - _Fix:_ Either remove/limit use of this unscoped accessor or require/propagate an explicit tenantId parameter and add a WHERE tenant_id = <tenantId> filter. If the function must remain unscoped, restrict its use to trusted admin-only codepaths and add strong audit/logging + code comments asserting the caller must check ownership.
- **server/storage.ts:295** — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) without any tenant_id constraint on the tenant-scoped messages table.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or join through conversations (with tenant filter) to ensure returned rows belong to the expected tenant. Propagate a validated tenantId into this function and use it in the WHERE clause.
- **server/storage.ts:824** — getMemoriesWithoutEmbeddings selects from memory_entries (WHERE status='active' AND embedding IS NULL) without tenant_id scoping — returns rows across tenants.
  - _Fix:_ Accept/require a tenantId and add eq(memoryEntries.tenantId, tenantId) to the WHERE clause, or make this function explicitly admin-only and document/guard it accordingly.
- **server/storage.ts:830** — getKnowledgeWithoutEmbeddings selects from agent_knowledge with no tenant_id constraint (reads across tenants).
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE agent_knowledge.tenant_id = ${tenantId}) or require an admin-only contract for this operation. Prefer requiring tenantId and fail-closed if omitted.
- **server/storage.ts:519** — getMemoryEntry can perform an unscoped SELECT when tenantId is not provided (conds may lack tenant filter; function merely warns).
  - _Fix:_ Make tenantId required for tenant-scoped reads or fail-closed instead of only warning. If unscoped reads are genuinely needed, move them to a dedicated admin API and clearly restrict/authorize that path.
- **server/storage.ts:641** — getDailyNote builds conditions and may execute a SELECT from daily_notes without tenant scoping when tenantId is omitted (relies on optional tenant argument + warn elsewhere).
  - _Fix:_ Require tenantId or make the absence of tenantId an explicit, auditable admin action. Prefer failing when tenantId is missing for tenant-scoped reads.
- **server/storage.ts:935** — getHeartbeatTask selects heartbeat_tasks by id only (WHERE id = ...) with no tenant_id constraint. heartbeat_tasks contains tenant_id; this read is unscoped.
  - _Fix:_ Either accept a tenantId and require WHERE id = ${id} AND tenant_id = ${tenantId}, or otherwise restrict usage to a trusted scheduler/worker context and document/authorize it.
- **server/storage.ts:991** — getDueHeartbeatTasks queries heartbeat_tasks for due tasks across all tenants (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() AND approval_status = 'approved') with no tenant scoping. This reads tenant-scoped rows globally.
  - _Fix:_ If the scheduler must run across tenants, ensure this codepath is explicitly admin-only and that the worker runtime is authorized to act at platform scope. Otherwise, iterate per-tenant and scope by tenant_id to avoid accidental cross-tenant exposure or actions.
- **server/surprise-scorer.ts:149** — Later in scoreProposalSurprise the code re-SELECTs args_embedding FROM felix_proposals WHERE id = ${proposalId} without tenant scoping (unscoped read).
  - _Fix:_ Include tenant scoping in the SELECT (AND tenant_id = ${tenantId}) or re-use the already-validated tenant context from the earlier secure SELECT. Ensure helper functions that lazily stamp or re-query a proposal also require/propagate tenantId.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` filters only on name (eq(customTools.name, parsed.name)) and omits tenant_id. This can match rows belonging to other tenants because the query lacks a tenant constraint.
  - _Fix:_ Constrain reads to the caller's tenant. Add tenant filtering to the WHERE, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). If tenantId isn't available in that scope, pass it in explicitly before querying.
- **server/tool-learning.ts:155** — SELECT by id on `custom_tools` uses .where(eq(customTools.id, inserted.id)) with no tenant_id guard. Without RLS an attacker who learns/guesses an id could read another tenant's tool.
  - _Fix:_ Include tenant constraint when selecting by id: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). If you only have the inserted row, prefer selecting by both id and tenantId or carry tenantId from the insertion result.
- **server/tool-learning.ts:365** — UPDATE to increment usageCount on `custom_tools` uses .where(eq(customTools.id, tool.id)) without a tenant_id filter. This allows an authenticated caller to modify another tenant's row if they can reference the id.
  - _Fix:_ Include tenant scoping on the update: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Prefer passing the tenantId down from the caller or deriving it from the tool row and validating it before executing the update.
- **server/tools.ts:2876** — INSERT into tenant-scoped table project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without explicitly setting tenant_id, relying on prior checks/joins. If project_conversations requires tenant_id (or must be explicitly scoped), this can create unsafely-scoped rows or fail DB constraints.
  - _Fix:_ Include the caller's tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})). If the table is intentionally single-tenant via project_id, still record tenant_id to make tenant ownership explicit and robust against future schema/usage changes. Alternatively derive tenant_id from the validated project row and pass it explicitly in the VALUES clause.
- **server/tools.ts:2980** — INSERT into tenant-scoped table project_files omits tenant_id. After verifying the project belongs to the tenant, the INSERT INTO project_files (project_id, file_name, ...) does not include tenant_id in the VALUES list.
  - _Fix:_ Pass tenant_id explicitly when inserting project_files (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). If relying on project_id alone for linkage, still include tenant_id to enforce and audit tenant ownership at the row level in application SQL.
- **server/tools.ts:2994** — INSERT into tenant-scoped table project_notes omits tenant_id. The handler verifies project ownership but then inserts INTO project_notes (project_id, note, author) without an explicit tenant_id column/value.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})). Always pass tenant_id explicitly for tenant-scoped tables rather than relying only on FK relationships.
- **server/tools.ts:3018** — INSERT into tenant-scoped table project_conversations (linking path) omits tenant_id. In the link_conversation path the code inserts (project_id, conversation_id) without explicitly passing tenant_id even though tenant ownership checks were performed separately.
  - _Fix:_ Set tenant_id explicitly on the INSERT (INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Prefer explicit tenant column population for all tenant-scoped writes so the application-level isolation invariant is consistently enforced.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table browser_workflows omits a tenant_id filter (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This can update a row belonging to another tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. e.g. use a parameterized WHERE clause that includes tenant_id: UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId} (or the Drizzle equivalent: .where(and(eq(browserWorkflows.id, workflow.id), eq(browserWorkflows.tenantId, tenantId)))) so the update cannot affect other tenants' rows.
- **server/tools/domains/files/handlers.ts:839** — UPDATE against tenant-scoped table projects omits a tenant_id filter (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Although projId was derived earlier, the UPDATE itself lacks an explicit tenant constraint and could modify another tenant's project if projId ownership is not enforced here.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE. For example: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId} (or use Drizzle: .where(and(eq(projects.id, projId), eq(projects.tenantId, tenantId)))) to ensure the write is scoped to the caller's tenant. Alternatively, re-check ownership immediately prior to the UPDATE and include tenant_id in the statement to enforce it atomically.
- **server/tools/domains/knowledge/handlers.ts:84** — UPDATE statement updates knowledge_triples by id without constraining tenant_id. The code executes: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` — this can modify a row belonging to another tenant if an id from a different tenant is used.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`) or verify ownership before the update. Prefer using the Drizzle query-builder (.where(eq(table.tenantId, tenantId))) or parameterized SQL with tenant_id to ensure every UPDATE/DELETE is explicitly constrained to ctx.tenantId. Also consider wrapping selection+update in a transaction and/or using RETURNING to confirm the updated row belonged to the caller's tenant.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id constraint (SELECT id, content::jsonb FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ...). This reads pairing rows across all tenants based on caller-supplied phone/title.
  - _Fix:_ Avoid scanning tenant-scoped tables across tenants. Add an explicit tenant filter (e.g. WHERE tenant_id = ${callerTenantId}) or move messaging_pairing into a properly-scoped global/operator-managed table. If cross-tenant checks are absolutely required, perform them only in an operator/admin context and minimize the returned fields (do not return full content), or change the schema so pairing ownership is stored in a proper tenant_id column and queries can be safely constrained. Ensure any data read from agent_knowledge is treated as untrusted and never leaks other tenants' content.
- **server/wiring-invariants.ts:57** — SELECT from research_schedules (joined to research_programs) is executed without constraining to a tenant (no WHERE tenant_id = ...). If research_schedules is tenant-scoped this exposes/aggregates schedule data across tenants.
  - _Fix:_ Constrain the query to an explicit tenant (e.g. WHERE s.tenant_id = :tenantId) or run the check per-tenant using the appropriate caller/admin tenant id. If the table is truly global, document that explicitly; otherwise add the tenant filter and propagate a tenantId parameter into the checker.
- **server/workflow-templates.ts:66** — produce_video is called with a hard-coded project_id = 14. project_id is a foreign key likely belonging to a tenant; using a constant risks writing/associating data to the wrong tenant's project (cross-tenant modification).
  - _Fix:_ Do not hard-code project ids. Accept project id as tenant-scoped input validated for ownership, or resolve a project belonging to context.tenantId before passing project_id. Validate that the referenced project belongs to the caller's tenant before any writes.

### MEDIUM (31)

- **server/agentic-features.ts:187** — Subqueries in listCompetitors use competitor_snapshots / competitor_changes without tenant_id filters inside the subqueries (e.g. SELECT COUNT(*) FROM competitor_snapshots WHERE competitor_id = c.id) — may surface other tenants' snapshots/changes if IDs collide or are global.
  - _Fix:_ Add tenant_id = c.tenant_id (or tenant_id = ${params.tenantId}) inside those subqueries so counts and last_snapshot are scoped to the tenant, e.g. SELECT COUNT(*) FROM competitor_snapshots WHERE competitor_id = c.id AND tenant_id = c.tenant_id.
- **server/auto-asset-capture.ts:184** — Backfill query selects conversations across all tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ... ) without restricting to a caller tenant. This reads tenant-scoped conversation rows without an equality filter.
  - _Fix:_ Scan tenants explicitly or add a tenant_id = ${tenantId} restriction. If the backfill must process all tenants, perform a controlled per-tenant loop (query distinct tenant_ids then run per-tenant queries with WHERE tenant_id = ...) so every tenant-scoped read includes an explicit tenant_id equality predicate.
- **server/auto-consolidation.ts:335** — Tenant discovery query selects DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL without constraining to a single tenant. This is a read of a tenant-scoped table without an equality filter.
  - _Fix:_ If you need to enumerate active tenant_ids, run a narrow, audited listing (acceptable for system-level jobs) or better: maintain a separate table of active tenants or iterate known tenant ids and run per-tenant checks with WHERE tenant_id = ${tenantId}, so individual memory_entries reads/updates are always filtered by tenant_id.
- **server/auto-transcript.ts:128** — SELECT from tenant-scoped table 'conversations' (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c ...) has no tenant_id WHERE filter. This reads across tenants without scoping to a particular tenant.
  - _Fix:_ If this is intended to operate across all tenants, add an explicit comment and restrict execution to an admin/background context. Otherwise add a WHERE c.tenant_id = ${tenantId} filter (or accept a tenantId parameter) so reads are limited to the caller's tenant.
- **server/auto-transcript.ts:153** — SELECT involving project_conversations / conversations / projects joins (SELECT DISTINCT pc.conversation_id, pc.project_id, c.tenant_id FROM project_conversations pc JOIN conversations c ON c.id = pc.conversation_id JOIN projects p ON p.id = pc.project_id AND p.tenant_id = c.tenant_id ...) does not constrain by tenant_id. This reads tenant-scoped tables without an explicit tenant WHERE clause.
  - _Fix:_ Scope the query to a tenant when running in non-global/backfill contexts by adding WHERE c.tenant_id = ${tenantId} (or similar). If the operation is intended to scan all tenants, ensure it runs only in a controlled admin/background job and document that explicitly.
- **server/auto-transcript.ts:179** — INSERT into project_conversations (INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) ON CONFLICT DO NOTHING) does not set or verify tenant_id. If project_conversations is tenant-scoped (or relies on transitive ownership), this inserts rows without enforcing tenant isolation at the SQL level.
  - _Fix:_ Either add a tenant_id column and populate it on INSERT, or ensure the insert is performed only after proving ownership (and include tenant scoping where possible). Prefer explicit tenant_id columns/values or include the tenant filter in the statement/transaction to avoid accidental cross-tenant linkage.
- **server/business-tools.ts:84** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table (tenant_id is NOT NULL), this INSERT omits the tenant identifier and will break isolation or allow cross-tenant leakage.
  - _Fix:_ Either include tenant_id in the invoice_items INSERT (VALUES (${tid}, ...)) or (if invoice_items is intentionally global) document that it is not tenant-scoped. Prefer adding tenant_id to invoice_items and populate it from the caller's tenant to enforce isolation.
- **server/business-tools.ts:150** — logExpense inserts an expense with a caller-supplied project_id but does not validate that the project_id belongs to the caller's tenant before storing it. This can create records that reference foreign rows from other tenants.
  - _Fix:_ If project_id is a cross-reference to a tenant-scoped projects table, verify ownership before inserting: SELECT id FROM projects WHERE id = ${params.project_id} AND tenant_id = ${tid} LIMIT 1 and reject if not found. Alternatively, deny setting project_id from unvalidated input.
- **server/capability-review.ts:491** — UPDATE on capability_reviews omits tenant scoping: `UPDATE capability_reviews SET reused = true, reused_capability = ... WHERE id = ${pending.reviewId} AND reused = false` — the UPDATE identifies the row only by id (no tenant_id) and may modify a row belonging to another tenant if ids collide or if pending tracking is not tenant-namespaced.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${pending.reviewId} AND tenant_id = ${expectedTenantId} AND reused = false), or store tenantId alongside pending.reviewId in pendingReuseByConversation and validate it prior to performing the UPDATE. Always verify ownership before mutating tenant-scoped rows.
- **server/data-protection.ts:324** — SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} is executed without scoping project_notes by tenant_id. project_notes is tenant-scoped in this app and the caller did not include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant constraint on project_notes queries, e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} (or join to projects and assert p.tenant_id = ${tenantId}), or insert tenant_id into the SELECT/INSERTs so rows are always associated with the tenant.
- **server/data-protection.ts:335** — SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} lacks an explicit tenant_id predicate. project_files is tenant-scoped and should be queried with tenant scoping.
  - _Fix:_ Add tenant scoping: SELECT ... FROM project_files WHERE project_id = ${proj.id} AND tenant_id = ${tenantId} ORDER BY created_at ASC. Alternatively, fetch within a withTenantTx or join to projects and verify p.tenant_id = ${tenantId}.
- **server/embeddings.ts:241** — Use of sql.raw() with an interpolated vector literal derived from a request/query embedding: sql.raw(`'${vec}'::vector`) (used in vectorSearchMemory). Although embedding arrays are validated, interpolating request-derived payloads into raw SQL increases injection risk and bypasses parameterization.
  - _Fix:_ Avoid building SQL fragments via string interpolation. Use parameterized binding for the vector and cast it in SQL (e.g. WHERE embedding_vec IS NOT NULL AND embedding_vec <=> ${sql.raw('($1)::vector', [vec])} — or better, pass the literal as a parameter and cast the parameter: WHERE embedding_vec <=> ${vec}::vector if supported by the driver, or store embeddings in a temp parameterized form). If driver limitations require string form, ensure the literal is produced and passed as a parameter (not injected into sql.raw) or perform the per-tenant operation in-memory and use safe Drizzle bindings.
- **server/embeddings.ts:556** — Use of sql.raw() with an interpolated vector literal derived from a request/query embedding in vectorSearchKnowledge (embedding vector inserted as sql.raw(`'${vec}'::vector`)). This interpolates request-derived data into raw SQL.
  - _Fix:_ Replace sql.raw interpolation with a fully parameterized binding for the vector value, or otherwise avoid inserting user/request-derived strings into sql.raw. Ensure the embedding is bound as a parameter and cast in SQL rather than concatenated into the SQL string.
- **server/job-queue.ts:98** — Atomic claim/update (claimDueJobs) updates rows in tenant-scoped table `agent_jobs` without any tenant_id filter. The worker claims jobs across tenants with no tenant-scoping in the SQL.
  - _Fix:_ If this worker is intentionally global, ensure the function is only callable by trusted/system context and document it. If the intent is per-tenant claiming, add tenant_id filtering (e.g. WHERE tenant_id = ${tenantId}) or accept a tenant-scoped claim path. Prefer explicit tenant context (or an admin-only name) rather than implicit cross-tenant UPDATEs.
- **server/job-queue.ts:134** — UPDATE reclaimExpiredLeases mutates `agent_jobs` rows (marking terminal/requeued) without tenant_id scoping. This is a cross-tenant modification of a tenant-scoped table.
  - _Fix:_ Either restrict the sweep to a specific tenant (pass tenantId) or ensure it runs only in a system/admin context. If it must be global, add code-level controls and auditing; otherwise add AND tenant_id = ${tenantId} where appropriate.
- **server/job-queue.ts:385** — getJobStats runs SELECT status, COUNT(*) FROM `agent_jobs` without any tenant_id filter — returning global per-status counts from a tenant-scoped table.
  - _Fix:_ If global stats are intended only for operators, ensure this function is admin-only and audited. Otherwise add an optional tenantId parameter and filter WHERE tenant_id = ${tenantId} so tenants only see their own job stats.
- **server/knowledge-diversity-monitor.ts:266** — Query in runKnowledgeDiversityMonitor selects tenant_id, persona_id FROM `agent_knowledge` grouped across all tenants (no tenant_id WHERE). This reads a tenant-scoped table globally.
  - _Fix:_ If the monitor must scan all tenants, ensure this code runs under a trusted, audited system context. Otherwise iterate per-tenant (provide tenantId) and query agent_knowledge WHERE tenant_id = ${tenantId} to keep tenant isolation enforced at SQL level.
- **server/lib/action-ledger.ts:268** — listUnknownAttemptsAllTenants SELECT queries action_attempts without filtering by tenant_id (cross-tenant read). While the reconciler helper is documented as cross-tenant by design, this reads tenant-scoped data across all tenants.
  - _Fix:_ If cross-tenant operation is intentional, ensure it is limited to trusted operator/background contexts only and cannot be invoked from tenant-scoped request paths. Otherwise restrict the query to a specific tenant by adding WHERE tenant_id = ${tenantId} or run the reconciler in a per-tenant loop so every read is explicitly scoped to a tenant.
- **server/lib/mcp-api-keys.ts:70** — The code builds a Postgres text[] literal (`scopesLit = `{...}``) by string concatenation from caller-supplied scopes and interpolates it into the SQL (`... ${scopesLit}::text[] ...`). This is string-interpolated SQL using request-derived data and risks SQL/formatting errors or injection if escaping is insufficient.
  - _Fix:_ Avoid constructing SQL array literals by hand. Instead pass the scopes as a parameterized array (let the DB driver/ORM bind the JS array) or store scopes as JSONB/text[] via a parameterized value. For example, bind the scopes array directly (parameterized) and cast to text[] server-side, or use a JSONB column. Use Drizzle/pg parameter binding APIs rather than manual string building to eliminate injection risk.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from agent_runs scans rows with status='running' but has no tenant_id filter. This reads a tenant-scoped table across tenants without WHERE tenant_id = ...
  - _Fix:_ If this is intended to be an admin-wide sweep, make that explicit; otherwise restrict the query by tenant_id. Prefer per-tenant scanning with tenant_id in the WHERE clause to preserve tenant isolation.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from mind_tickets queries status='in_progress' with no tenant_id filter. This reads a tenant-scoped table across tenants without constraining to a tenant.
  - _Fix:_ Add tenant_id to the WHERE clause or convert the routine to an explicit admin-mode per-tenant iteration that passes tenant_id into the query to avoid cross-tenant reads.
- **server/lib/system-probes.ts:163** — Probe uses db.select({ count: sql<number>`count(*)::int` }).from(messagesTable) without any tenant_id filter. This reads a tenant-scoped messages table globally.
  - _Fix:_ Make the probe tenant-scoped (accept a tenantId and filter WHERE tenant_id = ...) or, if the aggregate is intentionally global, ensure the function is restricted to admins and clearly documented. Prefer per-tenant metrics to preserve tenant isolation.
- **server/recurring-messages.ts:258** — Unscoped SELECT from tenant-scoped table agent_knowledge: `SELECT id, title, content, tenant_id FROM agent_knowledge WHERE category = 'recurring_message' LIMIT 500` (no tenant_id filter). This reads rows across all tenants.
  - _Fix:_ If a global scan is intended only for the system cron runner, make the intent explicit and restrict the caller to a system-only context (e.g. an internal flag/_allowGlobalSystemUseOnly or check process-privilege) and document it. Prefer adding a tenant filter where possible, or at minimum ensure strict per-row tenant enforcement and auditing before any per-tenant side-effect. Consider paginating per-tenant instead of a single unscoped scan.
- **server/routes.ts:2056** — GET /api/presenter/:token runs SELECT ... FROM presenter_sessions WHERE token = ${token} without scoping to a tenant. Reading a tenant-scoped presenter_sessions table directly by token may return rows across tenants without explicit ownership verification.
  - _Fix:_ Ensure tokens are cryptographically bound to a tenant and verify the token => tenant mapping before returning tenant-scoped data, or include an explicit tenant_id check (eq(presenter_sessions.tenant_id, validatedTenantId)). If the token is a capability, validate it cryptographically and avoid returning extra tenant-sensitive fields unless verified.
- **server/routes.ts:2088** — Presenter slide image handler queries presenter_slide_images WHERE session_id = ${sessionId} (and other fields) without first verifying the session's tenant ownership for the caller — session_id is a foreign id derived from a previous query but ownership is not checked.
  - _Fix:_ Verify the presenter_sessions row owner (tenant_id) and ensure the caller is authorized for that tenant before querying presenter_slide_images. Alternatively join presenter_slide_images with presenter_sessions and add WHERE presenter_sessions.tenant_id = <caller tenant> to the query.
- **server/routes.ts:2312** — /api/store/order/:sessionId selects from delivery_logs by orderId/stripePaymentId with no tenant_id constraint. delivery_logs appears tenant-scoped; this read is performed without enforcing tenant ownership (relies on the session id as capability).
  - _Fix:_ Treat the sessionId as a capability only if intentionally designed; otherwise enforce tenant scoping (WHERE tenant_id = <caller tenant>) or validate that the found delivery_log belongs to the expected tenant before returning sensitive fields. If capability semantics are required, document and harden them (unguessable IDs, minimal returned fields).
- **server/routes/metrics.ts:113** — SELECT from `repair_incidents` with no tenant_id constraint (reads counts across all tenants).
  - _Fix:_ Add a tenant predicate (WHERE tenant_id = ${tenantId}) for tenant-scoped use. If the intent is to produce a global operational metric, ensure the endpoint is strictly operator-only and consider limiting exposure of tenant-level breakdowns.
- **server/video-job-runner.ts:225** — UPDATE on tenant-scoped table `video_jobs` performed without tenant_id constraint in recoverStaleVideoJobs; the query marks stale jobs failed across all tenants.
  - _Fix:_ Constrain maintenance updates to the intended tenant(s) or run them under an explicit operator/admin context. If the sweeper must operate globally, ensure it's executed only by an admin-job/process (not in a tenant-facing path), log/authorize the operation, and/or add a safe tenant filter when invoked from tenant-scoped code. Consider adding an explicit tenant_id filter or an admin-only flag to prevent accidental cross-tenant changes.
- **server/watchlist.ts:111** — SELECT joins tenant-scoped table `watchlist_items` (aliased as wi) without constraining it to the caller's tenant. The base query constrains watchlist_alerts via wa.tenant_id = ${tenantId}, but the LEFT JOIN ON wa.watchlist_item_id = wi.id does not ensure wi.tenant_id = ${tenantId}, so persona/site data from other tenants could be leaked via the joined columns (wi.name, wi.category).
  - _Fix:_ Constrain the joined tenant-scoped table to the caller's tenant. For example, include wi.tenant_id = ${tenantId} in the join condition (LEFT JOIN watchlist_items wi ON wa.watchlist_item_id = wi.id AND wi.tenant_id = ${tenantId}) or otherwise filter wi by tenant_id in the WHERE clause to ensure no cross-tenant rows are returned.
- **server/webhook-triggers.ts:60** — Query selects webhook triggers and LEFT JOINs the `personas` table (p) without scoping that joined table by tenant. `personas` is tenant-scoped; returning p.name via the join can leak persona data from other tenants.
  - _Fix:_ When joining tenant-scoped tables, include an explicit tenant constraint. For example: LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = ${expectedTenantId} (or otherwise ensure the persona lookup is performed within the expected tenant context such as ADMIN_TENANT_ID when appropriate). Ensure the chosen tenantId is the correct, trusted tenant for this API path.
- **server/webhook-triggers.ts:122** — SELECT in processTriggerEvent joins `personas` (p) to webhook_triggers (w) without enforcing a tenant constraint on personas. This can expose persona names from other tenants when a trigger references a persona_id.
  - _Fix:_ Add tenant scoping to the persona lookup (e.g. include AND p.tenant_id = ${expectedTenantId} in the JOIN) or fetch the persona via a separate tenant-scoped query that verifies ownership before returning persona fields. If triggers are global but persona references must be admin-scoped, explicitly use ADMIN_TENANT_ID when resolving persona data.

### LOW (2)

- **server/auto-asset-capture.ts:207** — Messages are read by conversation_id only (SELECT content FROM messages WHERE conversation_id = ${c.id} ...) without an explicit tenant_id equality even though messages are tenant-scoped. While the code later passes tenant_id and asserts ownership, this individual SELECT lacks the tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${c.tenant_id} to the messages SELECT, or otherwise ensure the query is executed in a per-tenant context so every read against messages carries an explicit tenant_id equality clause.
- **server/video-job-runner.ts:203** — SELECT from tenant-scoped table `video_jobs` without tenant constraint in isCancelRequested (reads cancelRequested WHERE jobId = ...).
  - _Fix:_ When reading tenant-scoped rows, include tenant_id in the WHERE clause (e.g. WHERE job_id = ${jobId} AND tenant_id = ${tenantId}) or otherwise validate ownership before acting on the result. Alternatively, ensure jobId is globally unique and unguessable and document why tenant filtering is unnecessary, or scope this helper to internal/privileged code only.

### Suppressed — confirmed false positives (174)

_Cleared by the precision second-pass; shown for the audit trail, not counted toward the pass/fail exit code._

- **server/agent-activity.ts:122** _(internal-worker-pk)_ — UPDATE agent_activity is executed without constraining tenant_id — completeActivity(activityId, ...) can update rows across tenants.
  - _Why false positive:_ Updates by server-generated activity PK in internal helper; no request-boundary tenant input shown.
- **server/agentic-engines.ts:243** _(system-sweep)_ — UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) is executed without tenant_id filtering — this will modify rows for all tenants.
  - _Why false positive:_ Intentional platform-wide backfill sweep; per-row tenant_id is carried forward and operation only mutates housekeeping status/action fields.
- **server/agentic-features.ts:721** _(upstream-verified)_ — UPDATE outreach_enrollments sets reply_classification WHERE id = ${row.id} but does not include tenant_id in the WHERE clause — allows modifying another tenant's enrollment row by id.
  - _Why false positive:_ Updated row id comes from immediately preceding SELECT constrained by e.tenant_id = params.tenantId.
- **server/agentic/approvals.ts:135** _(system-sweep)_ — Bulk UPDATE agentApprovals to set status='expired' WHERE status='pending' AND expiresAt < now() is executed without tenant scoping — expires approvals across all tenants in one operation.
  - _Why false positive:_ Global expiry housekeeping sweep; returned rows are processed with their own tenantId and only status fields are bulk-mutated.
- **server/agentic/resume-worker.ts:66** _(system-sweep)_ — SELECT query scans tenant-scoped tables (agent_runs, agent_approvals) without constraining to a caller tenant_id. This returns rows across tenants.
  - _Why false positive:_ Background resume sweep intentionally scans across tenants and uses row-carried tenant_id; not a tenant-facing mixed-content read.
- **server/agentic/runs.ts:39** _(internal-worker-pk)_ — UPDATE agent_runs in appendStep() uses WHERE id = runId only and lacks a tenant_id constraint. This allows mutation of another tenant's run if runId is known.
  - _Why false positive:_ Run state update helper operates on server-generated run PK in internal worker/orchestration code, not shown as sourced from untrusted request input.
- **server/agentic/runs.ts:51** _(internal-worker-pk)_ — UPDATE agent_runs in updateRunState() uses WHERE id = runId only and omits tenant scoping, allowing cross-tenant updates.
  - _Why false positive:_ Internal run-state helper updates by server-generated run PK; no untrusted cross-tenant request boundary shown.
- **server/agentic/runs.ts:55** _(internal-worker-pk)_ — UPDATE agent_runs in replaceRunState() filters only on id (eq(agentRuns.id, runId)) and does not include tenant_id, enabling cross-tenant state replacement.
  - _Why false positive:_ Internal orchestration helper replacing state by server-generated run PK, not caller-supplied tenant selector.
- **server/agentic/runs.ts:64** _(internal-worker-pk)_ — UPDATE agent_runs in completeRun() uses WHERE id = runId only and lacks tenant_id constraint — a caller could mark another tenant's run completed.
  - _Why false positive:_ Completion helper updates internal agent run by globally unique server-side PK; no request-boundary id source shown.
- **server/agentic/runs.ts:94** _(internal-worker-pk)_ — UPDATE agent_runs in failRun() updates by run id only (eq(agentRuns.id, runId)) without tenant scoping, permitting cross-tenant mutation of run rows.
  - _Why false positive:_ Failure helper mutates internal run row by server-generated PK in server code path, not demonstrated as user-controlled.
- **server/agentic/runs.ts:102** _(internal-worker-pk)_ — UPDATE agent_runs in pauseRun() filters only on id (and not tenant_id), allowing a pause to apply to a run belonging to a different tenant.
  - _Why false positive:_ Pause helper is internal orchestration on server-generated run id; no evidence of untrusted caller-controlled id.
- **server/commitment-drafter.ts:234** _(system-sweep)_ — UPDATE on tenant-scoped table `commitments` executed without constraining by tenant_id (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This allows reverting/affecting rows across tenants if id is provided from a different tenant context.
  - _Why false positive:_ Platform-wide worker reverts only housekeeping status on rows previously claimed with tenant-scoped selection; no tenant data returned or mixed.
- **server/credential-vault.ts:169** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` executed without tenant constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id}). This can update another tenant's row by id.
  - _Why false positive:_ Same call path first loads the credential via getCredential(id, tenantId); update is on that verified row id.
- **server/credential-vault.ts:176** _(upstream-verified)_ — UPDATE on tenant-scoped table `credential_vault` executed without tenant constraint (UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id}). This can update another tenant's row when cred.id is not validated to belong to the caller's tenant.
  - _Why false positive:_ Credential is first fetched by getCredentialForSite(siteUrl, tenantId); subsequent last_used_at update uses the verified credential id.
- **server/data-protection.ts:117** _(system-sweep)_ — Global purge query selects conversations without scoping by tenant_id (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < $1). This and the subsequent per-conversation deletes operate without an explicit tenant predicate, allowing cross-tenant deletes/reads.
  - _Why false positive:_ This is a global expiry purge over soft-deleted rows, carrying each row's tenant_id for per-row handling and not exposing mixed tenant data.
- **server/data-protection.ts:131** _(system-sweep)_ — DELETE FROM messages WHERE conversation_id = ${conv.id} executes without a tenant_id predicate. Deleting by conversation_id only, without tenant scoping, can remove another tenant's data if assumptions about global uniqueness or ownership change.
  - _Why false positive:_ Per-row maintenance delete within a global purge; acts on child rows of each expired conversation and does not mix tenant-facing output.
- **server/data-protection.ts:132** _(system-sweep)_ — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks tenant_id constraint. This deletes tenant-scoped rows without scoping to the owning tenant.
  - _Why false positive:_ Per-row maintenance delete within a global purge; housekeeping operation over expired conversations only, with no tenant-facing data mixing.
- **server/data-protection.ts:133** _(system-sweep)_ — DELETE FROM conversations WHERE id = ${conv.id} is executed without an explicit tenant_id WHERE clause. This can delete a conversation row that may be claimed by another tenant if assumptions change or IDs collide in future migrations.
  - _Why false positive:_ Final delete in a platform-wide soft-delete expiry sweep; maintenance-only mutation, no cross-tenant response or prompt composition.
- **server/felix-brain.ts:19** _(system-sweep)_ — Boot-time query reads from tenant-scoped table 'agent_runs' without any tenant constraint (SELECT ... FROM agent_runs WHERE status IN ...). This loads rows for all tenants into in-memory state and breaks tenant isolation.
  - _Why false positive:_ Boot-time rehydration intentionally scans all in-flight agent_runs to rebuild in-memory state; rows are handled individually, not exposed or combined into one tenant response.
- **server/google-workspace.ts:2358** _(fk-transitive)_ — INSERT into presenter_slide_images is executed without supplying tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Why false positive:_ presenter_slide_images is scoped transitively by session_id to tenant-scoped presenter_sessions; insert without tenant_id matches child-table pattern.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}) updates proposals without scoping by tenant_id, potentially archiving proposals across tenants.
  - _Why false positive:_ Health audit archives stale proposals platform-wide as maintenance; only status field is mutated and no tenant data is returned to tenant-facing surfaces.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}) disables tasks without constraining by tenant_id, possibly affecting other tenants' scheduled tasks.
  - _Why false positive:_ Platform-wide maintenance disables stale heartbeat tasks by status only; housekeeping mutation with no tenant-content mixing or disclosure.
- **server/heartbeat.ts:52** _(system-sweep)_ — SELECT from tenant-scoped table research_schedules without a tenant_id constraint (reads schedules across all tenants).
  - _Why false positive:_ Heartbeat scheduler intentionally scans due schedules across tenants and then processes each row with its own tenant_id.
- **server/heartbeat.ts:658** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... — no tenant_id constraint. This reads pending events across all tenants in one query.
  - _Why false positive:_ Platform-wide heartbeat sweep processes pending rows across tenants, but each row carries its own tenant_id and is routed per-row; no mixed tenant output.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into `project_files` omits tenant_id. Platform convention requires tenant_id be set on tenant-scoped tables (columns are NOT NULL with no default).
  - _Why false positive:_ project_files has no tenant_id column and is transitively scoped through tenant-owned projects via project_id.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into `project_notes` omits tenant_id. Tenant-scoped inserts must explicitly set tenant_id per platform invariant.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively scoped through tenant-owned projects via project_id.
- **server/lib/scheduled-post-runner.ts:60** _(system-sweep)_ — Atomic CTE selects/updates scheduled_posts across all tenants with no tenant_id constraint (SELECT ... FROM scheduled_posts ... FOR UPDATE / UPDATE scheduled_posts ... FROM due WHERE sp.id = due.id). This reads and writes a tenant-scoped table without constraining to a caller tenant.
  - _Why false positive:_ Scheduled-post runner is a cross-tenant housekeeping dispatcher that locks and updates rows per-row, returning each row's tenant_id for isolated downstream handling rather than mixing tenant data.
- **server/memory-graph.ts:195** _(fk-transitive)_ — INSERT INTO memory_links (...) VALUES (...) — the INSERT does not set tenant_id. Tenant-scoped tables must receive tenant_id on INSERT; omitting it allows cross-tenant link creation or will fail if tenant_id is NOT NULL without default.
  - _Why false positive:_ memory_links has no tenant_id column; links are between tenant-scoped memory_entries and isolation is enforced transitively through the linked memory rows.
- **server/mpeg-engine.ts:1132** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id column — row is created without an explicit tenant constraint.
  - _Why false positive:_ project_files has no tenant_id column and insert is preceded by assertProjectInTenant(projectId, tenantId), confirming tenant-owned parent project.
- **server/mpeg-engine.ts:1498** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (parallel path) omits tenant_id — new row may be written without tenant attribution.
  - _Why false positive:_ project_files has no tenant_id column and insert is preceded by assertProjectInTenant(projectId, tenantId), confirming tenant-owned parent project.
- **server/orchestrator-ledger.ts:445** _(upstream-verified)_ — Use of sql.raw() to interpolate a computed advisory-lock key (lockKey) into SQL: `SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`. lockKey is derived from caller-supplied ledgerKey/tenantId and is being injected into the SQL text unparameterized.
  - _Why false positive:_ Not a tenant-isolation finding: tenant-scoped memory_entries access below is constrained by tenant_id; flagged sql.raw lock key concerns injection, not cross-tenant data access.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — validateSubscriptionsOnStartup runs `SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant_id filter — it enumerates subscriptions (sensitive tokens/metadata) across all tenants.
  - _Why false positive:_ Startup validation is a platform-wide maintenance loop over all active subscriptions, using each row's tenant_id for per-row handling.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — Unscoped SELECT `SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace'` (no tenant_id) — reads rows across tenants.
  - _Why false positive:_ Startup auto-reconnect intentionally enumerates google-workspace subscription tenants globally and then processes each tenant separately by row-carried tenant_id.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh queries `oauth_subscriptions` with `WHERE is_active = TRUE` and no tenant_id filter — iterates secrets/tokens across all tenants.
  - _Why false positive:_ Proactive token refresh is a cron/maintenance loop across tenants that iterates active rows and refreshes each subscription using its own tenant_id.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions executes `SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE` with no tenant constraint — reading tenant-scoped subscription rows globally.
  - _Why false positive:_ Force-refresh is explicitly a daily cron maintenance job operating across all active subscriptions, with per-row tenant_id scoping during refresh.
- **server/process-governor.ts:595** _(upstream-verified)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} executes without constraining tenant_id. This mutates a tenant-scoped table by id only — a malicious or buggy rule could affect rows belonging to other tenants.
  - _Why false positive:_ Rows updated by id come only from the immediately preceding SELECT scoped to ht.tenant_id = tenantId, so t.id is already tenant-owned in this call path.
- **server/process-governor.ts:642** _(upstream-verified)_ — UPDATE agent_desks SET ... WHERE id = ${desk.id} is executed without a tenant_id constraint. This updates agent_desks (tenant-scoped) by id only and can modify other tenants' rows.
  - _Why false positive:_ desk.id values are sourced from the prior SELECT constrained by ad.tenant_id = tenantId, so the update target was ownership-verified upstream.
- **server/research-engine.ts:158** _(system-sweep)_ — UPDATE against research_sessions performed without any tenant_id constraint (cleanupZombieSessions). This UPDATE will mark running sessions across all tenants as completed and can cause cross-tenant state corruption.
  - _Why false positive:_ Zombie-session cleanup is a platform-wide housekeeping update of stale status fields only; it returns ids for logs and does not mix tenant data into a tenant-facing output.
- **server/research-engine.ts:390** _(system-sweep)_ — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() is executed without a tenant_id filter. agent_knowledge is tenant-scoped, so this will delete entries for all tenants.
  - _Why false positive:_ Expired autoresearch knowledge purge is cross-tenant housekeeping deletion of expired rows, not tenant-facing data access or mixing.
- **server/research-engine.ts:470** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. project_notes appears tenant-scoped and INSERTs must explicitly set tenant_id; omitting it risks cross-tenant contamination or DB constraint failures.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by project_id; project ownership is verified by assertProjectInTenant just above
- **server/research-engine.ts:478** _(fk-transitive)_ — Second INSERT into project_notes (creating a finding note) also omits tenant_id, writing a tenant-scoped table without explicit tenant ownership metadata.
  - _Why false positive:_ project_notes has no tenant_id and inserts are correctly scoped through tenant-validated parent project_id
- **server/routes.ts:165** _(system-sweep)_ — restoreUploadsFromDb reads fileStorage.filename and fileStorage.data for all rows without any tenant_id constraint — this can expose raw file blobs across tenants.
  - _Why false positive:_ boot-time restore sweeps all file rows for housekeeping, writes blobs back to disk only, and does not return or mix tenant data to any tenant-facing surface
- **server/seed.ts:2855** _(system-sweep)_ — SELECT from tenant-scoped table 'research_programs' without constraining tenant_id (SELECT id, name, model FROM research_programs WHERE is_active = true). This reads programs across all tenants.
  - _Why false positive:_ boot-time fixer intentionally scans all tenants' research_programs and only updates housekeeping field model per row without exposing data
- **server/seed.ts:2860** _(system-sweep)_ — UPDATE on tenant-scoped table 'research_programs' uses WHERE id = ${p.id} but omits tenant_id. Updating by id alone can modify rows belonging to other tenants.
  - _Why false positive:_ seed maintenance updates invalid model values across tenants as a platform-wide housekeeping fix, with no tenant-facing data return or mixing
- **server/seed.ts:35** _(system-sweep)_ — SELECT COUNT(*) FROM research_sessions executed without any tenant filter. research_sessions is tenant-scoped, so this returns cross-tenant counts.
  - _Why false positive:_ dev snapshot import is a boot-time maintenance path using a global count gate, not a tenant-facing read or mixed-content output
- **server/seed.ts:3205** _(system-sweep)_ — SELECT DISTINCT c.id, c.project_id FROM conversations c ... does not constrain tenant_id on conversations (or on the project_conversations subquery). conversations and project_conversations are tenant-scoped, so this can return cross-tenant rows.
  - _Why false positive:_ boot-time backfill intentionally scans rows across tenants and links each conversation to its own project_id without exposing mixed tenant data
- **server/seed.ts:3213** _(fk-transitive)_ — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...) omits tenant_id for a tenant-scoped table. The table likely requires tenant_id and inserting without it (or with a wrong default) can create records not bound to the correct tenant.
  - _Why false positive:_ project_conversations has no tenant_id and is a link table correctly scoped by parent project/conversation ids during backfill
- **server/skills-marketplace.ts:213** _(global-table)_ — INSERT INTO `skills` does not set tenant_id. If the `skills` table is tenant-scoped with tenant_id NOT NULL (no default), this allows creating rows without tenant ownership (cross-tenant write).
  - _Why false positive:_ skills schema shows no tenant_id; insert into platform-global skills table is not a tenant-isolation flaw.
- **server/skills-marketplace.ts:252** _(global-table)_ — INSERT INTO `skills` in importSkill does not include tenant_id. This may create skill rows without tenant ownership if `skills` is tenant-scoped, enabling cross-tenant data mixing.
  - _Why false positive:_ skills has no tenant_id column, so importing a skill without tenant scoping matches a global table pattern.
- **server/storage.ts:839** _(system-sweep)_ — archiveExpiredMemories runs a raw UPDATE on memory_entries (SET status='archived' WHERE ... expires_at <= NOW()) without any tenant_id scoping — a bulk, cross-tenant mutation on a tenant-scoped table.
  - _Why false positive:_ global archival of expired memory rows is housekeeping-only status mutation across tenants, not tenant data exposure or mixing.
- **server/storage.ts:855** _(system-sweep)_ — archiveStaleMemories runs a raw UPDATE on memory_entries (SET status='archived' WHERE created_at <= ... AND last_accessed <= ...) with no tenant_id constraint — bulk cross-tenant mutation on a tenant-scoped table.
  - _Why false positive:_ bulk stale-memory archival is a maintenance sweep changing status only across tenants, with no tenant-facing mixed output.
- **server/storage.ts:1019** _(system-sweep)_ — claimHeartbeatTasks runs UPDATE heartbeat_tasks ... WHERE id = ${id} ... without a tenant_id constraint (unscoped write / claiming tasks across tenants).
  - _Why false positive:_ heartbeat task claiming is part of platform-wide scheduler housekeeping; rows carry their own tenant_id and this path only updates run timestamps, not tenant content exposure.
- **server/surprise-scorer.ts:95** _(internal-worker-pk)_ — stampArgsEmbedding runs UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL — no tenant_id constraint on the tenant-scoped felix_proposals table (unscoped write).
  - _Why false positive:_ server-side helper updates felix_proposals by server-generated proposalId in internal scoring flow, not from an untrusted request boundary
- **server/tools.ts:4513** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id. The code inserts (project_id, note, author, created_at) without passing the caller's tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through tenant-validated project_id
- **server/tools.ts:4518** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code inserts (project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) without tenant_id.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped through tenant-validated project_id
- **server/tools.ts:6365** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (audio upload path) does not include tenant_id. The INSERT VALUES omit tenant_id even though the caller's project_id was validated just above.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped by asserted project ownership via project_id
- **server/tools/domains/security/handlers.ts:163** _(global-table)_ — SELECT from provider_keys without tenant_id constraint. The query `SELECT provider, LENGTH(api_key) as key_len FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` is executed (even if gated to admin tenant) against what appears to be a tenant-scoped table, exposing cross-tenant sensitive information (API keys).
  - _Why false positive:_ provider_keys schema shows no tenant_id and query is admin-gated for platform-wide audit
- **server/tools/domains/security/handlers.ts:260** _(global-table)_ — SELECT COUNT(DISTINCT provider) FROM provider_keys executed without tenant_id filter. The query `SELECT COUNT(DISTINCT provider) as cnt FROM provider_keys WHERE api_key IS NOT NULL AND api_key != ''` runs for owner/admin and lacks tenant scoping, allowing cross-tenant enumeration of provider keys.
  - _Why false positive:_ provider_keys schema shows no tenant_id and aggregate is admin-gated for platform-wide audit
- **server/tools/domains/strategic-memory/handlers.ts:95** _(upstream-verified)_ — UPDATE statement updates memory_entries WHERE id = ${id} with no tenant_id (or persona_id) constraint — tenant-scoped table writes/updates must include tenant scoping to prevent cross-tenant modifications.
  - _Why false positive:_ id comes directly from the immediately preceding tenant+persona-scoped SELECT on memory_entries, so the UPDATE is scoped by prior ownership check in the same call path.
- **server/tools/domains/strategic-memory/handlers.ts:148** _(upstream-verified)_ — UPDATE statement updates memory_entries WHERE id = ANY(${ids}) with no tenant_id (or persona_id) constraint — this can bump access_count/last_accessed for rows from other tenants if ids are known or mis-bound.
  - _Why false positive:_ ids are derived only from rows fetched just above with tenant_id and persona_id filters, so the bulk UPDATE only touches previously tenant-scoped rows.
- **server/tools/domains/strategic-memory/handlers.ts:242** _(upstream-verified)_ — UPDATE statement updates memory_entries WHERE id = ${id} with no tenant_id (or persona_id) constraint — same cross-tenant update risk as other UPDATEs in this module.
  - _Why false positive:_ id is selected in the same function from memory_entries with tenant_id and persona_id constraints before the UPDATE.
- **server/wiring-invariants.ts:180** _(system-sweep)_ — Outcome canary query reads research_experiments, code_proposals, research_sessions and research_schedules without any tenant constraint. Those tables are tenant-scoped, so counts and last_run values aggregate across tenants and may leak tenant data or produce incorrect alerts.
  - _Why false positive:_ boot/admin wiring-invariant canary intentionally aggregates platform-wide schedule/experiment/proposal health and emits only maintenance findings, not tenant data.
- **server/wiring-invariants.ts:244** _(system-sweep)_ — Query on tool_performance (COUNT WHERE last_success_at > ...) has no tenant_id filter. tool_performance is tenant-scoped, so this aggregates usage across tenants and may expose or conflate tenant data.
  - _Why false positive:_ global dormant-tool health check intentionally counts tool_performance across all tenants for maintenance and returns only aggregate diagnostics.
- **server/wiring-invariants.ts:258** _(system-sweep)_ — SELECT COUNT(*) FROM tool_performance executed without tenant scoping. This reads tenant-scoped data across all tenants.
  - _Why false positive:_ platform-wide maintenance check intentionally reads total historical tool_performance rows and emits only aggregate wiring findings.
- **server/wiring-invariants.ts:260** _(system-sweep)_ — SELECT COUNT(*) FROM messages (recent 14d) runs without a tenant filter. messages is tenant-scoped; this aggregates message counts across tenants and may leak information or skew checks that rely on per-tenant volume.
  - _Why false positive:_ recent messages are counted only as a global traffic proxy inside a maintenance detector; it returns aggregate counts, not tenant message content.
- **server/wiring-invariants.ts:293** _(system-sweep)_ — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant constraint. It will return tool usage across all tenants rather than per-tenant usage.
  - _Why false positive:_ distinct used tool names are aggregated globally for a maintenance-only dormant-tool detector, with no tenant-facing row data exposure.
- **server/wiring-invariants.ts:366** _(system-sweep)_ — sql.raw is used to SELECT COUNT/MAX FROM ${spec.table} (e.g. knowledge_nudges, tool_performance) without any tenant filter. These subsystem tables are tenant-scoped, so the freshness check reads across tenants.
  - _Why false positive:_ subsystem freshness probe is a platform-wide housekeeping check over tables and timestamps, producing only global row-count/last-write diagnostics.
- **server/wiring-invariants.ts:417** _(system-sweep)_ — Tool-drift query reads tool_performance without tenant scoping (WHERE last_failure_at > ... but no tenant_id). This allows cross-tenant aggregation and potential data exposure.
  - _Why false positive:_ tool drift detector intentionally aggregates failures across tenants for system health and emits only tool-level warning summaries.
- **server/wiring-invariants.ts:535** _(system-sweep)_ — Memory-growth probe uses sql.raw to SELECT COUNT and pg_total_relation_size FROM ${tbl.name} (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, ... ) with no tenant filter. For tenant-scoped memory tables this reads data across tenants.
  - _Why false positive:_ memory-growth tripwire is a platform observability check that reads only table-wide counts/size and returns aggregate maintenance warnings, not tenant data.
- **server/wiring-invariants.ts:561** _(system-sweep)_ — SELECT COUNT(*) FROM graph_memory executes without tenant scoping; the check even notes 'empty across all tenants' which implies graph_memory is tenant-scoped. Querying without WHERE tenant_id permits cross-tenant observation and incorrect remediation advice.
  - _Why false positive:_ graph_memory empty-table probe is an intentional global bootstrap/housekeeping check and only reports whether the table is empty across the platform.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — SELECT FROM experiments is executed without constraining tenant_id (reads running A/B tests across all tenants).
  - _Why false positive:_ Platform-wide maintenance sweep reads rows across tenants but processes each row with its own tenant_id and updates only that row/emits per-tenant follow-up.
- **server/agentic-engines.ts:161** _(system-sweep)_ — Durability sweep selects ai_insights rows (retryPendingMinervaRouting) without a tenant_id filter — reads insights across tenants.
  - _Why false positive:_ Durability sweep intentionally scans pending rows across tenants and routes each using the row-carried tenant_id after validation; no mixed tenant output.
- **server/agentic-engines.ts:344** _(global-table)_ — Query against heartbeat_logs (count of task calls) in gatherOperationalData lacks a tenant_id filter — leaking global heartbeat log counts into tenant-scoped analysis.
  - _Why false positive:_ heartbeat_logs schema shows no tenant_id; aggregate counts from a platform-global telemetry table are intentionally unscoped.
- **server/agentic-engines.ts:519** _(global-table)_ — SELECT aggregating heartbeat_logs (runOptimizationEngine) executed without tenant_id filter — aggregates global task performance across tenants.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is a platform-global telemetry table; aggregate performance query matches global-table pattern.
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Query selects from tenant-scoped table `department_budgets` without constraining by tenant_id: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This reads tenant-scoped rows across all tenants without an application-level tenant filter.
  - _Why false positive:_ Heartbeat enforcement sweep intentionally iterates active budgets across tenants and handles each row with its own tenant_id, emitting per-tenant events only.
- **server/agentic/wake-scheduler.ts:146** _(system-sweep)_ — Row selection used to claim pending wakes does not constrain by tenant_id. The inner SELECT that picks pending ids (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) lacks a tenant_id = ... filter, allowing cross-tenant schedules to be claimed/updated.
  - _Why false positive:_ Global due-wake claimer is a housekeeping sweep over pending schedules; each claimed row is subsequently emitted/updated using its own tenant_id with no tenant data mixing.
- **server/auto-memorize.ts:163** _(system-sweep)_ — The core messages SELECT pulls messages across all tenants (SELECT m.tenant_id, m.role, m.content, ... FROM messages m ... WHERE m.created_at > ${windowStart} ... ORDER BY m.tenant_id ASC LIMIT 400) and then partitions tenant-side. This performs a tenant-scoped table read without an explicit tenant_id equality filter.
  - _Why false positive:_ Maintenance sweep reads recent messages across tenants but explicitly partitions by row tenant_id before per-tenant processing, with comments and code preventing mixed-tenant prompts.
- **server/auto-transcript.ts:165** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' without constraining tenant_id in WHERE clause (UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL). This performs a write to a tenant-scoped resource without an explicit tenant filter.
  - _Why false positive:_ The row comes from an earlier join enforcing p.tenant_id = c.tenant_id for the specific conversation/project pair, so the UPDATE by conversation PK is operating on a tenant-validated row in the same call path.
- **server/ceo-orchestrator.ts:841** _(global-table)_ — Reads the personas table without scoping to the plan's tenant: `const cachedPersonas = await db.select().from(personasTable);` — this returns personas across tenants and can lead to cross-tenant data leakage or selection of a persona id that does not belong to the plan's tenant.
  - _Why false positive:_ Referenced schema says personas has no tenant_id column, so this is an intentionally non-tenant-scoped/global table read.
- **server/channel-routing.ts:31** _(global-table)_ — Query joins channel_routes to personas without any tenant constraint: `FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id ...` — if personas is a tenant-scoped table this can surface or join persona rows from other tenants (tenant isolation violation).
  - _Why false positive:_ personas has no tenant_id column and channel_routes is a global routing table keyed by channel, so the join is over global tables rather than tenant-scoped data.
- **server/data-protection.ts:368** _(upstream-verified)_ — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} is used to check message counts without tenant_id scoping. This read may surface or operate on other tenants' rows if tenant constraints are not applied consistently.
  - _Why false positive:_ conv.id values come from a prior SELECT of conversations filtered by tenant_id = tenantId, and this count is only an internal existence check before calling a tenant-scoped backup function.
- **server/data-protection.ts:406** _(upstream-verified)_ — getDeletedConversations runs a subquery (SELECT COUNT(*)::int FROM messages WHERE conversation_id = conversations.id) without scoping messages by tenant_id. Even though the outer query filters conversations by tenant_id, inner reads against tenant-scoped tables must still include tenant predicates per the project's invariant.
  - _Why false positive:_ The subquery counts messages for each outer conversations row already constrained by conversations.tenant_id = tenantId, so the count is tied to a tenant-validated parent row in the same query.
- **server/deep-interview.ts:337** _(fk-transitive)_ — INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ... WHERE pc.conversation_id = ${state.conversationId} AND p.tenant_id = ${state.tenantId} inserts a row into project_notes but does NOT set project_notes.tenant_id explicitly. The invariant requires every INSERT into a tenant-scoped table to include tenant_id.
  - _Why false positive:_ project_notes has no tenant_id column and is inserted via project_id selected from tenant-scoped projects filtered by p.tenant_id = state.tenantId.
- **server/email.ts:366** _(global-table)_ — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 is executed with no tenant_id constraint. inbox_messages is tenant-scoped; this read may return rows from other tenants.
  - _Why false positive:_ code operates on a single shared inbox poller (pollSharedInbox/getPrimaryInboxId); message_id dedupe is against a platform-global mailbox feed, not tenant-facing lookup.
- **server/email.ts:411** _(system-sweep)_ — backfillEmptyBodies() runs SELECT id, message_id FROM inbox_messages WHERE ... with no tenant_id filter, scanning a tenant-scoped table across all tenants.
  - _Why false positive:_ backfillEmptyBodies is a maintenance backfill over all inbox_messages that only fetches missing body content and updates each row by its own id, with no tenant-facing mixed output.
- **server/email.ts:424** _(system-sweep)_ — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} is performed without tenant_id in the WHERE clause. This is a write to a tenant-scoped table that lacks tenant scoping and could modify another tenant's row by id.
  - _Why false positive:_ row update occurs inside a housekeeping backfill loop over previously selected rows and only fills body_text/body_html for that row, not exposing mixed tenant data.
- **server/embeddings.ts:687** _(system-sweep)_ — backfillMissingKnowledgeEmbeddings selects from agent_knowledge without any tenant_id filter. This reads across all tenants' knowledge rows and then calls storeEmbeddingVec which performs tenant-unsafe updates.
  - _Why false positive:_ this is a platform-wide embedding backfill selecting expired-safe rows and mutating only housekeeping/vector fields per row, with no tenant-facing data mixing.
- **server/embeddings.ts:716** _(system-sweep)_ — backfillEmbeddingVecs selects memory_entries rows without tenant_id filtering and later UPDATEs memory_entries SET embedding_vec WHERE id = ${row.id} without tenant constraint. Both the SELECT and UPDATE operate across all tenants.
  - _Why false positive:_ backfillEmbeddingVecs is a maintenance sweep across tenants that only populates embedding_vec per row and returns aggregate counts, not cross-tenant content to users.
- **server/embeddings.ts:732** _(system-sweep)_ — backfillEmbeddingVecs selects agent_knowledge rows without tenant_id filtering and later UPDATEs agent_knowledge WHERE id = ${row.id} without tenant constraint. This allows cross-tenant reads and writes.
  - _Why false positive:_ Global backfill job updates housekeeping field embedding_vec across rows and returns only aggregate counts; no tenant-facing data mixing.
- **server/google-drive.ts:547** _(upstream-verified)_ — Query selects project row by id from tenant-scoped table 'projects' without scoping by tenant_id (WHERE eq(projects.id, projectId)). Reading a tenant-scoped row by id without tenant constraint can leak another tenant's data.
  - _Why false positive:_ Same call path immediately verifies fetched project's tenantId matches caller tenant before any return/use.
- **server/google-drive.ts:560** _(upstream-verified)_ — UPDATE against the tenant-scoped 'projects' table uses only the project primary key in the WHERE clause (WHERE eq(projects.id, projectId)) without also constraining tenant_id. Even if ownership was checked earlier, the UPDATE itself is not scoped and could modify a foreign-tenant row if prior checks were bypassed.
  - _Why false positive:_ Update is preceded in the same function by explicit ownership check rejecting tenant mismatch for the projectId.
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT FROM code_proposals (stale proposals) runs without a tenant_id filter. code_proposals appears tenant-scoped, so this reads across tenants.
  - _Why false positive:_ Admin health-audit stale-proposal TTL sweep intentionally scans across tenants and only emits audit findings/archives status, not tenant-facing mixed content.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT FROM heartbeat_tasks queries enabled/never-run tasks with no tenant_id filter. heartbeat_tasks is likely tenant-scoped, so this returns tasks across tenants.
  - _Why false positive:_ Admin health-audit heartbeat housekeeping sweep intentionally scans across tenants and only flags/disables stale tasks, with no tenant content returned to tenants.
- **server/heartbeat.ts:101** _(system-sweep)_ — UPDATE research_schedules SET ... WHERE id = ${sched.id} — no tenant_id constraint. This can modify a schedule belonging to another tenant if ids are global.
  - _Why false positive:_ Platform scheduler loop updates per-row next_run_at using row-carried sched.id/sched.tenant_id for housekeeping only, not tenant-facing data.
- **server/heartbeat.ts:107** _(system-sweep)_ — UPDATE research_schedules SET last_run_at = NOW(), next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Why false positive:_ Platform scheduler loop advances last_run_at/next_run_at on the current schedule row as housekeeping in a cross-tenant maintenance pass.
- **server/heartbeat.ts:112** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${nextDate} WHERE id = ${sched.id} — missing tenant constraint.
  - _Why false positive:_ Platform scheduler loop updates next_run_at on the current schedule row during maintenance; per-row housekeeping only.
- **server/heartbeat.ts:120** _(system-sweep)_ — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — missing tenant_id constraint on a tenant-scoped table.
  - _Why false positive:_ Error-recovery branch of the scheduler sweep advances next_run_at for the current row only; housekeeping mutation, no cross-tenant exposure.
- **server/heartbeat.ts:260** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — no tenant_id constraint. heartbeat_tasks are tenant-scoped and this update can affect other tenants' rows.
  - _Why false positive:_ Startup cleanup normalizes heartbeat task model across rows as platform maintenance; only housekeeping field changed, no tenant-facing data mixing.
- **server/heartbeat.ts:264** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — missing tenant filter on a tenant-scoped table.
  - _Why false positive:_ Boot-time startup cleanup/seed logic intentionally sweeps heartbeat tasks across tenants and only normalizes housekeeping config on each row by its own id; no tenant data is returned or mixed.
- **server/heartbeat.ts:1814** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... — no tenant_id constraint. heartbeat_logs is likely tenant-scoped; this reads logs across tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column, so this log lookup is against a platform-global table, not a tenant-scoped one.
- **server/heartbeat.ts:2908** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint after creating a task. This may change approval_status for a task ID from another tenant.
  - _Why false positive:_ The row is created immediately above with tenantId set from the current flow, and the update targets that freshly returned newTask.id rather than untrusted input.
- **server/heartbeat.ts:3360** _(upstream-verified)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint (appears in delegateTaskFromChat).
  - _Why false positive:_ The task is inserted just above with the current tenantId and the follow-up update only touches that newly created row by returned id.
- **server/job-queue.ts:177** _(internal-worker-pk)_ — completeJob updates `agent_jobs` by id/status/attempts but does not constrain by tenant_id. Completing a job by id can affect jobs belonging to other tenants.
  - _Why false positive:_ completeJob is an internal worker state-transition keyed by server-issued job id plus attempts fencing, not by an untrusted request boundary.
- **server/job-queue.ts:220** _(internal-worker-pk)_ — failJob reads max_attempts and performs terminal/requeue UPDATEs on `agent_jobs` by id with NO tenant_id constraints. Both the SELECT and UPDATEs can be applied to another tenant's job id.
  - _Why false positive:_ failJob is an internal worker retry/terminal transition by server-issued job id with attempts fencing; id is not shown coming from req/tool input.
- **server/lib/action-ledger.ts:304** _(system-sweep)_ — sweepStaleAttempts performs an UPDATE on action_attempts without a tenant_id predicate (it updates rows whose id IN (SELECT id FROM action_attempts ...)). This will transition rows for all tenants, breaking the expectation that writes are constrained to an owner's tenant.
  - _Why false positive:_ This is an explicit cross-tenant reconciler sweep that only transitions stale housekeeping state to unknown in bounded batches and returns a count, not tenant data.
- **server/lib/ideabrowser-backfill.ts:49** _(system-sweep)_ — SELECT on tenant-scoped table `projects` has no tenant_id WHERE clause — the query aggregates project counts across all tenants.
  - _Why false positive:_ Boot-time backfill/maintenance code intentionally aggregates counts across tenants only to decide per-tenant backfill completeness; no tenant-facing data is returned or mixed into one output.
- **server/lib/ideabrowser-backfill.ts:100** _(fk-transitive)_ — SELECT FROM tenant-scoped table `project_files` uses project_id but does not constrain by tenant_id — reading rows without explicit tenant scoping.
  - _Why false positive:_ project_files has no tenant_id and is transitively scoped by tenant-owned projects via project_id; newProjectId comes from tenant-scoped project lookup/insert in this same routine.
- **server/lib/ideabrowser-backfill.ts:115** _(fk-transitive)_ — SELECT FROM tenant-scoped table `project_notes` uses project_id but lacks tenant_id filtering — reading without explicit tenant scoping.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped by tenant-owned projects via project_id; newProjectId comes from tenant-scoped project lookup/insert in this same routine.
- **server/lib/scheduled-post-runner.ts:164** _(system-sweep)_ — Per-row UPDATE to scheduled_posts uses WHERE id = ${row.id} with no tenant_id guard. This is a write to a tenant-scoped table without constraining by tenant_id.
  - _Why false positive:_ Runner is a platform-wide background dispatcher updating each already-selected row by its own PK as housekeeping/status mutation, not serving cross-tenant data.
- **server/lib/scheduled-post-runner.ts:191** _(system-sweep)_ — Error/release-path UPDATE to scheduled_posts uses WHERE id = ${row.id} AND status = 'publishing' but omits tenant_id. This can update/delete rows across tenants on error handling paths.
  - _Why false positive:_ Error-path release update is part of the same maintenance runner and only mutates status/lock/error fields on the currently processed row.
- **server/martech-bundle.ts:325** _(upstream-verified)_ — UPDATE on tenantVoiceProfiles performed using only the profile PK (WHERE id = existing.id) with no tenant_id constraint. This updates a tenant-scoped table without explicitly constraining the tenant.
  - _Why false positive:_ existing row is obtained immediately above via getVoiceProfile({ tenantId: opts.tenantId, profileName }), so the PK used for update was already tenant-scoped in the same call path.
- **server/memory-graph.ts:181** _(fk-transitive)_ — SELECT 1 FROM memory_links WHERE (source_memory_id = ${newMemoryId} AND target_memory_id = ${target.id}) OR ... LIMIT 1 — no tenant constraint. If memory_links is tenant-scoped, this query checks/reads links without constraining by tenant.
  - _Why false positive:_ memory_links has no tenant_id and links are between memory_entries already selected for one tenant; isolation is transitive through source/target memory entry IDs.
- **server/minerva-planner.ts:459** _(upstream-verified)_ — UPDATE rollback on tenant-scoped table `plans` performed without tenant scoping (WHERE id = ${args.planId} AND status = 'revising' with no tenant_id). This can roll back a plan in another tenant inadvertently.
  - _Why false positive:_ Earlier CAS UPDATE RETURNING at line 363 claims the specific plan row by id/status and yields row.tenant_id; this rollback is in the same call path for that already-claimed plan.
- **server/minds-engine.ts:214** _(upstream-verified)_ — INSERT into tenant-scoped table `mind_tickets` builds a Postgres array string from request-derived params.dependsOn via string interpolation (`{${params.dependsOn.join(',')}}`). This is string-interpolated SQL using caller data and risks SQL injection or isolation bypass.
  - _Why false positive:_ Tenant ownership is enforced earlier by getMind(params.mindId, params.tenantId); the flagged issue is SQL construction, not a cross-tenant isolation failure.
- **server/minds-engine.ts:327** _(upstream-verified)_ — UPDATE on tenant-scoped table `mind_events` uses string-interpolated array literal for eventIds (`id = ANY(${`{${eventIds.join(',')}}`}::int[])`) where eventIds is request-derived. This is string-interpolated SQL with caller data and risks SQL injection and isolation issues.
  - _Why false positive:_ The UPDATE includes tenant_id = tenantId, so cross-tenant rows are still scoped even though the array literal construction is unsafe for SQL injection.
- **server/pace-control.ts:85** _(global-table)_ — SELECT COUNT(*) FROM tenant-scoped table `heartbeat_logs` is executed without any tenant_id filter — counting activity across all tenants.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id column; this pace throttle log is an intentionally global control table.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot queries `heartbeat_logs` (SELECT ... WHERE created_at > ...) without tenant_id — returns cross-tenant activity data.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id, and this function returns platform-wide pace/debug snapshot data from that global table.
- **server/plan-executor.ts:116** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `plans` executed without constraining by tenant_id (appendExecutionLog updates WHERE id = ${planId} only). This allows cross-tenant writes to a plan row identified only by id.
  - _Why false positive:_ appendExecutionLog is an internal server worker helper invoked during executePlan on a server-generated planId, not directly from an untrusted request boundary.
- **server/plan-executor.ts:527** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `plans` performed without tenant_id constraint (sets status='failed' WHERE id = ${planId}).
  - _Why false positive:_ This failure update occurs inside executePlan after the worker has claimed the plan row and obtained row.tenant_id; planId is the internal worker key.
- **server/plan-executor.ts:725** _(internal-worker-pk)_ — Final UPDATE to `plans` updates status/execution_log WHERE id = ${planId} with no tenant_id filter. Writes to tenant-scoped table are not being constrained to the owning tenant.
  - _Why false positive:_ Final status write is part of the same internal executePlan worker operating on the previously claimed plan by server-side primary key.
- **server/plan-executor.ts:759** _(internal-worker-pk)_ — Error-path UPDATE on `plans` (marking failed) uses WHERE id = ${planId} AND status = 'executing' but omits tenant_id. This can change plan state for other tenants' rows.
  - _Why false positive:_ Error-path status update is inside the internal executePlan worker and uses the same server-invoked plan primary key, not caller-supplied tenant context.
- **server/plan-executor.ts:775** _(system-sweep)_ — resumeStuckPlans performs a global UPDATE resetting any stale 'executing' plans to 'approved' without tenant restriction (WHERE status='executing' AND updated_at < ...). This modifies rows across all tenants.
  - _Why false positive:_ resumeStuckPlans is a boot-time maintenance sweep that intentionally acts across all tenants, only mutating housekeeping status fields and returning counts/ids for requeue.
- **server/plan-executor.ts:784** _(system-sweep)_ — resumeStuckPlans SELECTs pending plans via SELECT id FROM plans WHERE status = 'approved' ... with no tenant filter and then calls executePlan() on them. This enumerates and triggers execution for plans from any tenant without scoping the operation.
  - _Why false positive:_ This boot-time recovery SELECT enumerates pending plan ids for a platform-wide maintenance loop and requeues each row individually without mixing tenant data into one tenant-facing output.
- **server/process-governor.ts:121** _(fk-transitive)_ — Subquery reads heartbeat_logs without scoping by tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). This reads a tenant-scoped table (heartbeat_logs) without a tenant constraint.
  - _Why false positive:_ heartbeat_logs has no tenant_id and is counted via tenant-scoped parent heartbeat_tasks ht filtered by ht.tenant_id = tenantId
- **server/process-governor.ts:199** _(global-table)_ — Query aggregates heartbeat_logs (AVG(duration_ms) FROM heartbeat_logs ...) with no tenant_id filter. Reading heartbeat_logs without tenant scoping can leak other tenants' telemetry.
  - _Why false positive:_ heartbeat_logs is schema-noted as not tenant-scoped and this route computes platform-wide telemetry aggregate
- **server/process-governor.ts:240** _(global-table)_ — Query counts distinct persona_id from heartbeat_logs (COUNT(DISTINCT persona_id) FROM heartbeat_logs ...) but omits tenant_id filter. This reads a tenant-scoped table without tenant constraint.
  - _Why false positive:_ heartbeat_logs is schema-noted as not tenant-scoped and query only returns a global aggregate count of failing persona ids
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id WHERE (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 lacks tenant scoping on projects, conversations, and messages. This can enumerate projects across tenants.
  - _Why false positive:_ backfillProjectBrains is a platform-wide backfill job that intentionally sweeps all tenants to create per-project local brain files
- **server/project-brain.ts:350** _(system-sweep)_ — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} is executed without tenant_id scoping. Conversations are tenant-scoped and this can return other tenants' conversations if project ids overlap across tenants.
  - _Why false positive:_ within the same backfill sweep, conversations are processed per project row rather than exposed to a tenant-facing surface
- **server/project-brain.ts:359** _(system-sweep)_ — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant_id constraint. messages is tenant-scoped and reading by conversation id alone may access another tenant's messages.
  - _Why false positive:_ within the same backfill sweep, messages are processed per conversation to build per-project files, not returned across tenants in one response
- **server/reference-learner.ts:290** _(upstream-verified)_ — UPDATE memory_entries executed without constraining by tenant_id (WHERE id = ${storedId} only). This allows a caller who can influence storedId to update a row belonging to another tenant.
  - _Why false positive:_ storedId comes directly from a prior SELECT on memory_entries scoped by tenant_id = input.tenantId and persona/category/fact constraints in the same call path
- **server/recurring-messages.ts:300** _(system-sweep)_ — UPDATE agent_knowledge sets content by id only (`WHERE id = ${r.id}`) with no tenant_id constraint. Even though the row was read from a global scan, the write can modify a row across tenants if an id is known.
  - _Why false positive:_ scheduler loop intentionally scans and updates jobs across tenants using each row's own tenant_id and only mutates housekeeping content/updated_at
- **server/routes.ts:1529** _(global-table)_ — /api/public/stats executes SELECT count(*) FROM conversations/messages/heartbeat_tasks/memory_entries/heartbeat_logs with no tenant_id filter — querying tenant-scoped tables unscoped leaks per-tenant counts/metadata across tenants.
  - _Why false positive:_ public endpoint intentionally exposes platform-global aggregate counts rather than per-tenant data
- **server/routes.ts:4330** _(fk-transitive)_ — INSERT into `project_notes` does not set tenant_id. The code performs `INSERT INTO project_notes (project_id, note, author) VALUES (...)` without supplying tenant_id. Tenant-scoped tables require tenant_id on INSERT (columns are NOT NULL with no default) and omission can cause privacy/misattribution or DB errors.
  - _Why false positive:_ project_notes has no tenant_id column and is transitively tenant-scoped via project_id to tenant-scoped projects.
- **server/routes/admin.ts:569** _(upstream-verified)_ — SELECT from tool_performance has no tenant filter — queries tenant-scoped telemetry without constraining to caller's tenant.
  - _Why false positive:_ Route is explicitly restricted to ADMIN_TENANT_ID plus isAdminRequest before executing cross-tenant admin telemetry query.
- **server/routes/admin.ts:574** _(upstream-verified)_ — SELECT from tool_performance (recent failures) omits tenant_id — returns per-tool performance across all tenants.
  - _Why false positive:_ Platform-admin-only gate immediately above proves intentional cross-tenant admin access for this diagnostic query.
- **server/routes/admin.ts:587** _(upstream-verified)_ — SELECT from ai_insights (status='new') has no tenant filter — reads incident rows across all tenants.
  - _Why false positive:_ Same call path enforces ADMIN_TENANT_ID and isAdminRequest before this cross-tenant admin inspection query.
- **server/routes/admin.ts:598** _(upstream-verified)_ — SELECT from heartbeat_tasks omits tenant constraint — can leak tenant-scoped heartbeat task state across tenants.
  - _Why false positive:_ Heartbeat task query is inside an admin-only endpoint guarded by ADMIN_TENANT_ID and isAdminRequest.
- **server/routes/admin.ts:609** _(upstream-verified)_ — SELECT from ai_insights (status='applied' and specific action_taken) lacks tenant_id filter — reads tenant-scoped records without scoping.
  - _Why false positive:_ Cross-tenant ai_insights inspection is intentionally behind the same explicit platform-admin gate.
- **server/routes/admin-zombie-detector.ts:38** _(upstream-verified)_ — Query over agent_trace_spans (used→used AS ...) does not include tenant_id — may return trace data from other tenants.
  - _Why false positive:_ Endpoint checks ADMIN_TENANT_ID and isAdminRequest before running this cross-tenant zombie-detector aggregation.
- **server/routes/admin-zombie-detector.ts:54** _(upstream-verified)_ — Query that builds 'used' for personas (SELECT persona usage from conversations) omits tenant_id — aggregating conversation usage across tenants.
  - _Why false positive:_ Conversation-usage aggregation is in a platform-admin-only route with explicit tenant/admin guard above.
- **server/routes/conversations.ts:98** _(upstream-verified)_ — UPDATE on conversations omits tenant constraint — 'UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}' can modify a conversation in another tenant if an attacker can control ids.
  - _Why false positive:_ conv.id comes from storage.createConversation just above using current tenantId, so update targets the newly created caller-owned row, not untrusted input.
- **server/routes/conversations.ts:99** _(fk-transitive)_ — SELECT from project_conversations omits tenant constraint — 'SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}' reads a tenant-scoped table without filtering by tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id and links tenant-scoped project/conversation already verified/created in-tenant.
- **server/routes/conversations.ts:102** _(fk-transitive)_ — INSERT into project_conversations omits tenant_id — 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)' does not set tenant_id on a tenant-scoped table.
  - _Why false positive:_ project_conversations is a tenant-less join table; insert is correct after tenant-scoped project check and tenant-scoped conversation creation.
- **server/routes/conversations.ts:104** _(upstream-verified)_ — UPDATE on projects omits tenant constraint — 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' can update another tenant's project if ids are guessed.
  - _Why false positive:_ same path first verifies projects.id belongs to tenant via WHERE id = projectId AND tenant_id = tenantId before this update.
- **server/routes/conversations.ts:227** _(fk-transitive)_ — INSERT into project_notes omits tenant_id — 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' does not set tenant_id on a tenant-scoped table, allowing notes to be associated without tenant ownership enforced at DB layer.
  - _Why false positive:_ project_notes has no tenant_id and is a child of tenant-scoped projects; projId is derived from join enforcing project tenant ownership.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE inbox_messages omits tenant constraint — 'UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}' updates a message by id only, enabling marking messages from other tenants if an id is known.
  - _Why false positive:_ msg was selected immediately above with WHERE id = msgId AND tenant_id = tenantId; update uses that verified row id.
- **server/routes/metrics.ts:79** _(system-sweep)_ — SELECT from tenant-scoped table `agent_cost_ledger` (tool usage counts) with no tenant_id constraint (reads across all tenants).
  - _Why false positive:_ operator-token metrics endpoint returns only global aggregated tool counts, not tenant-specific rows or mixed tenant content.
- **server/routes/metrics.ts:95** _(system-sweep)_ — SELECT from tenant-scoped table `agent_cost_ledger` (token totals) with no tenant_id constraint (global read of tenant data).
  - _Why false positive:_ operator-token metrics endpoint returns only global aggregate token totals with no tenant identifiers or row data.
- **server/routes/oauth-subscriptions.ts:325** _(system-sweep)_ — SELECT DISTINCT tenant_id FROM tenant-scoped table `oauth_subscriptions` with no tenant_id constraint — enumerates tenant IDs with inactive Google subscriptions.
  - _Why false positive:_ cron-secret-gated maintenance sweep intentionally scans all tenants to heal inactive subscriptions and only processes each tenant id separately.
- **server/sculptor.ts:94** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` lacks tenant_id constraint (WHERE id = ${session.id} only). This can modify another tenant's row if ids collide or are guessed.
  - _Why false positive:_ server-internal function updates freshly inserted sculptor_sessions row by server-generated primary key, not untrusted request-supplied id.
- **server/sculptor.ts:101** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` (setting conversation_id) lacks tenant_id constraint (WHERE id = ${session.id} only).
  - _Why false positive:_ server-internal post-launch update targets the just-created session by its server-generated id, not caller-controlled row selection.
- **server/sculptor.ts:162** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `sculptor_sessions` in monitorSession() lacks tenant_id constraint (WHERE id = ${sessionId} only).
  - _Why false positive:_ background monitor updates a session by server-originated sessionId/runId established at creation time, not from an untrusted tenant request boundary.
- **server/second-opinion.ts:407** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` (settleFusionReservation) uses WHERE id = ${reservationId} without constraining tenant_id. Updating by id alone can affect another tenant's ledger row.
  - _Why false positive:_ Best-effort internal helper updates a server-created reservation row by serial primary key; reservationId is not shown coming from an untrusted request boundary.
- **server/second-opinion.ts:426** _(internal-worker-pk)_ — UPDATE on tenant-scoped table `agent_cost_ledger` (releaseFusionReservation) uses WHERE id = ${reservationId} without constraining tenant_id. This can zero-out or alter another tenant's reservation row by id alone.
  - _Why false positive:_ Internal release helper mutates a server-created ledger reservation by primary key only; id is not shown as caller-controlled.
- **server/seed.ts:3205** _(system-sweep)_ — SELECT from conversations (unlinked detection) has no tenant constraint. The query: `SELECT DISTINCT c.id, c.project_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM project_conversations pc WHERE pc.project_id = c.project_id AND pc.conversation_id = c.id)` can return rows across all tenants, and subsequent INSERTs (project_conversations) will link conversations/projects potentially across tenant boundaries.
  - _Why false positive:_ Boot-time backfill intentionally scans all tenants and inserts missing child links per row; project_conversations is transitively scoped via tenant-scoped conversation/project ids and does not return mixed tenant data.
- **server/seed.ts:4431** _(system-sweep)_ — DELETE from heartbeat_tasks is executed without a tenant filter: `DELETE FROM heartbeat_tasks WHERE enabled = false AND description LIKE '%[auto-disabled:%' AND type != 'maintenance_script'`. heartbeat_tasks is tenant-scoped (indexes reference tenant_id) so this will delete rows across all tenants.
  - _Why false positive:_ Boot-time housekeeping delete intentionally purges stale disabled heartbeat tasks across tenants; this is maintenance-only and does not expose or mix tenant data.
- **server/seed.ts:4459** _(system-sweep)_ — UPDATE memory_entries (normalizer) runs without tenant scoping: `UPDATE memory_entries SET fact = REPLACE(fact, 'Manjaro', 'wellness-program') WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ...`. memory_entries is tenant-scoped (index exists on tenant_id) so this will modify memory rows across all tenants.
  - _Why false positive:_ Global maintenance normalizer updates memory rows in place across tenants without returning data; this is a housekeeping/content-fix sweep, not a tenant-facing mixed read.
- **server/skillify.ts:91** _(upstream-verified)_ — SELECT from `messages` by conversationId is executed without scoping to tenant (no WHERE tenant_id = …). This reads tenant-scoped data without an explicit tenant filter.
  - _Why false positive:_ This call path first runs analyzeConversation(conversationId, tenantId), which verifies the conversation belongs to tenantId; subsequent message read is tied to that validated conversation.
- **server/skillify.ts:201** _(upstream-verified)_ — SELECT from `messages` (in analyzeConversation) uses only conversationId and lacks an explicit tenant_id condition. Reading a tenant-scoped table without a tenant filter risks cross-tenant data exposure.
  - _Why false positive:_ Immediately above, analyzeConversation verifies conversations.id + tenant_id before loading messages for that conversation, so ownership is enforced earlier in the same call path.
- **server/skillify.ts:221** _(global-table)_ — SELECT from `personas` by persona id is executed without scoping to tenant (no WHERE tenant_id = …). If `personas` is tenant-scoped, this can return persona rows from other tenants.
  - _Why false positive:_ Schema note says personas has no tenant_id column, so persona lookup is from a non-tenant-scoped/global table.
- **server/skillify.ts:329** _(upstream-verified)_ — SELECT from `messages` (recent messages for LLM prompt) is executed without scoping to tenant (no WHERE tenant_id = …). This fetches tenant-scoped messages without an explicit tenant filter.
  - _Why false positive:_ createSkillFromConversation first calls analyzeConversation(conversationId, tenantId), which verifies tenant ownership of the conversation before fetching recent messages for that same conversation.
- **server/skills-marketplace.ts:207** _(global-table)_ — SELECT id FROM `skills` WHERE name = ${template.name} runs with no tenant constraint. If `skills` is tenant-scoped this can read across tenants.
  - _Why false positive:_ skills has no tenant_id column and is platform-global by schema
- **server/skills-marketplace.ts:224** _(global-table)_ — SELECT * FROM `skills` WHERE id = ${skillId} has no tenant constraint. Reading a tenant-scoped skills row by id without tenant scoping risks exposing another tenant's skill.
  - _Why false positive:_ skills table is not tenant-scoped; schema shows no tenant_id
- **server/storage.ts:295** _(upstream-verified)_ — attachCitationMetadata executes raw SQL: SELECT id, citations FROM messages WHERE id IN (...) without any tenant_id constraint on the tenant-scoped messages table.
  - _Why false positive:_ ids come only from msgs already fetched with tenant-scoped queries in same call path
- **server/storage.ts:991** _(system-sweep)_ — getDueHeartbeatTasks queries heartbeat_tasks for due tasks across all tenants (SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() AND approval_status = 'approved') with no tenant scoping. This reads tenant-scoped rows globally.
  - _Why false positive:_ platform-wide scheduler intentionally reads due tasks across tenants and carries each row tenantId separately
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT by id on `custom_tools` uses .where(eq(customTools.id, inserted.id)) with no tenant_id guard. Without RLS an attacker who learns/guesses an id could read another tenant's tool.
  - _Why false positive:_ Read-back uses server-generated inserted.id from the immediately preceding insert, not untrusted caller input.
- **server/tool-learning.ts:365** _(upstream-verified)_ — UPDATE to increment usageCount on `custom_tools` uses .where(eq(customTools.id, tool.id)) without a tenant_id filter. This allows an authenticated caller to modify another tenant's row if they can reference the id.
  - _Why false positive:_ tool was selected just above with tenant_id = tenantId; update uses that verified row's id.
- **server/tools.ts:2876** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations omits tenant_id. The code inserts (project_id, conversation_id) without explicitly setting tenant_id, relying on prior checks/joins. If project_conversations requires tenant_id (or must be explicitly scoped), this can create unsafely-scoped rows or fail DB constraints.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped through tenant-checked project/conversation parents.
- **server/tools.ts:2980** _(fk-transitive)_ — INSERT into tenant-scoped table project_files omits tenant_id. After verifying the project belongs to the tenant, the INSERT INTO project_files (project_id, file_name, ...) does not include tenant_id in the VALUES list.
  - _Why false positive:_ project_files has no tenant_id and insert occurs only after project ownership check; child is scoped via project_id.
- **server/tools.ts:2994** _(fk-transitive)_ — INSERT into tenant-scoped table project_notes omits tenant_id. The handler verifies project ownership but then inserts INTO project_notes (project_id, note, author) without an explicit tenant_id column/value.
  - _Why false positive:_ project_notes has no tenant_id and insert occurs only after project ownership check; child is scoped via project_id.
- **server/tools.ts:3018** _(fk-transitive)_ — INSERT into tenant-scoped table project_conversations (linking path) omits tenant_id. In the link_conversation path the code inserts (project_id, conversation_id) without explicitly passing tenant_id even though tenant ownership checks were performed separately.
  - _Why false positive:_ project_conversations has no tenant_id and both project ownership and conversation tenant membership are validated before insert.
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table browser_workflows omits a tenant_id filter (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This can update a row belonging to another tenant.
  - _Why false positive:_ workflow row is fetched immediately above with tenant_id = tenantId; update uses that verified workflow.id.
- **server/tools/domains/files/handlers.ts:839** _(upstream-verified)_ — UPDATE against tenant-scoped table projects omits a tenant_id filter (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}). Although projId was derived earlier, the UPDATE itself lacks an explicit tenant constraint and could modify another tenant's project if projId ownership is not enforced here.
  - _Why false positive:_ projId comes from conversation->project join constrained by p.tenant_id = tenantId just above.
- **server/tools/domains/knowledge/handlers.ts:84** _(upstream-verified)_ — UPDATE statement updates knowledge_triples by id without constraining tenant_id. The code executes: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}` — this can modify a row belonging to another tenant if an id from a different tenant is used.
  - _Why false positive:_ row.id values come from the immediately preceding SELECT on knowledge_triples filtered by tenant_id = tenantId.
