# Tenant-Isolation Audit Report

_Generated 2026-09-16T09:55:43.335Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 489 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **191/680** file(s) audited · chunks 20/63 OK (0 failed, 43 pending)
- Findings: **78** (CRITICAL 17 · HIGH 43 · MEDIUM 16 · LOW 2)
- Severe dispositions: **60 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (17)

- **server/agentic-engines.ts:247** — autoApplyEligibleInsights performs an UPDATE on `ai_insights` that can run unscoped when tenantId is undefined — the UPDATE WHERE clause can affect all tenants (no required tenant filter).
  - _Fix:_ Do not perform tenant-global writes without explicit operator intent. Require a tenantId param (do not default to undefined) or, when running a global admin sweep, ensure a separate, well-audited admin path. Add WHERE tenant_id = ${tenantId} to tenant-scoped updates or iterate tenant-by-tenant with explicit authorization checks.
- **server/agentic/approvals.ts:134** — expireStaleApprovals runs a bulk UPDATE on `agent_approvals` (marking pending approvals expired) without scoping by tenant — this performs tenant-scoped writes across all tenants.
  - _Fix:_ Make expiry tenant-scoped. Either require a tenantId parameter and include WHERE tenant_id = ${tenantId}, or perform an explicitly authorized global admin sweep that is well-audited. Never perform unscoped updates on tenant-scoped tables in normal application code.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on agent_wake_schedules claims/marks pending wakes using a subselect that does NOT restrict by tenant_id. The statement `WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED)` can select and mutate rows belonging to any tenant.
  - _Fix:_ Run the claim operation per-tenant or accept an explicit tenantId parameter and add `AND tenant_id = ${tenantId}` to both the inner SELECT and the outer UPDATE so only that tenant's schedules are claimed. Alternatively iterate tenants and apply FOR UPDATE SKIP LOCKED within each tenant scope. Ensure all returned/updated rows remain constrained to the tenant context.
- **server/auto-transcript.ts:165** — UPDATE on tenant-scoped table "conversations" lacks a tenant_id constraint (UPDATE conversations SET project_id = ... WHERE id = ${c.conversation_id} AND project_id IS NULL). This can modify rows belonging to other tenants.
  - _Fix:_ Add an explicit tenant filter to the UPDATE, e.g. WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id} (or the calling tenantId). Never perform UPDATE/DELETE on tenant-scoped tables without constraining tenant_id.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table this lets an item row be created without associating it to the caller's tenant (or will fail if tenant_id is NOT NULL).
  - _Fix:_ Include tenant_id in the INSERT into invoice_items (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or otherwise derive/validate tenant ownership of the invoice before inserting. Ensure every tenant-scoped table row is written with tenant_id set from the authenticated caller.
- **server/channel-routing.ts:48** — setChannelRoute inserts/updates channel_routes using caller-supplied personaId with no verification that the persona belongs to the caller's tenant. This writes a global mapping that can reference another tenant's persona_id, enabling cross-tenant reference/injection.
  - _Fix:_ Require a tenantId argument and validate ownership before writing: SELECT 1 FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId} and fail if not found. Alternatively make channel_routes a tenant-scoped table (add tenant_id NOT NULL) and include tenant_id in the INSERT/ON CONFLICT and all WHERE clauses (e.g. UNIQUE(channel, tenant_id)). Do not accept or persist foreign tenant-scoped IDs from callers without explicit ownership checks.
- **server/chat-engine.ts:1621** — buildMemorySection declares a default tenantId = 1. If a caller omits the tenantId argument this routine (and the downstream calls it makes) will run under tenant 1, enabling silent fallback to an admin/default tenant and risking cross-tenant reads/writes (leakage or mis-attribution). This defeats the project's fail-closed tenant scoping guarantees.
  - _Fix:_ Remove the hardcoded default. Make tenantId required (or default to null) and enforce a runtime fail-closed guard (throw if tenantId is missing or not a positive integer), mirroring buildSystemPrompt's pattern. Audit all callers to ensure they always pass the resolved caller tenantId. Also add tests to prevent future accidental calls without an explicit tenantId.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table `commitments` is executed without constraining by tenant_id (unscoped UPDATE: WHERE id = ${row.id}). This can mutate rows across tenants.
  - _Fix:_ Do NOT issue an UPDATE on a tenant-scoped table without a tenant filter. Either skip the unclaim when tenantId is invalid, or include a tenant filter using the claim row's tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenantId}). Prefer fail-closed: if tenant context is unavailable, log and skip rather than performing an unscoped UPDATE.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table 'agent_runs' is executed without any tenant_id constraint (reads all tenants' rows during boot rehydration). This allows cross-tenant data exposure into the in-memory taskStateStore.
  - _Fix:_ Restrict the query to a specific tenant (or iterate per-tenant) by adding a tenant predicate, or accept an explicit tenantId parameter. Example fix: add "AND tenant_id = ${tenantId}" to the WHERE clause (and validate tenantId before use), or fetch runs grouped/filtered by tenant and only rebuild state for the caller's tenant. Also ensure rows from other tenants are never loaded into the shared taskStateStore.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) — no tenant_id constraint.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or perform the update with a combined id+tenant predicate so a row belonging to another tenant cannot be modified. Also consider checking the number of affected rows and failing if zero to detect races.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id))) inside the crawl loop — no tenant_id constraint.
  - _Fix:_ Same fix as above: add the tenant constraint to the WHERE (e.g. and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))) and/or verify affected row count to avoid cross-tenant modifications under races.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' uses only project id in the WHERE clause (db.update(projects).set(...).where(eq(projects.id, projectId))). There is no tenant_id filter on the write.
  - _Fix:_ Add the tenant constraint to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so only the owning tenant's project can be modified. Prefer using id+tenant predicates for all writes and verify affected row count.
- **server/google-drive.ts:1664** — INSERT into project_files omits tenant_id entirely (INSERT INTO project_files(... ) VALUES (...)). project_files is tenant-scoped and tenant_id must be provided (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant_id value in the INSERT (use the validated tenant id, e.g. VALUES (..., ${resolvedTenantId}, ...)) and ensure ownership was validated prior to inserting (assertProjectInTenant or equivalent). Use parameterized Drizzle insert with tenantId set to avoid accidental cross-tenant registration.
- **server/health-audit.ts:314** — SELECT from code_proposals has no tenant scoping (no WHERE tenant_id = ...). This read then leads to an unconditional UPDATE that archives proposals by id (see lines ~333-337), allowing stale proposals from all tenants to be affected.
  - _Fix:_ Limit the SELECT to a single tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant in an explicit admin-only sweep. When performing the UPDATE, include the same tenant_id constraint (e.g. UPDATE code_proposals SET ... WHERE tenant_id = ${tenantId} AND id = ANY(...)). Alternatively, require an explicit admin-only guard and document that this is a cross-tenant admin operation.
- **server/health-audit.ts:333** — UPDATE on code_proposals (UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(...)) has no tenant scoping. This can modify rows across tenants by id.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE tenant_id = ${tenantId} AND id = ANY(...)) or perform the archive per-tenant after selecting only that tenant's rows. If this is intended as a global/admin sweep, gate the operation with an explicit admin-only check and audit log, but still include tenant_id in the WHERE when targeting tenant-scoped rows.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL AND (...)) lacks a tenant_id filter. The code later performs UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) which will disable tasks across tenants.
  - _Fix:_ Scope both the SELECT and the subsequent UPDATE to a tenant (WHERE tenant_id = ${tenantId}) or iterate per-tenant and operate only on that tenant's tasks. If a cross-tenant administrative sweep is intended, require an explicit admin guard and still include tenant_id in the UPDATE predicate to avoid accidental cross-tenant modifications.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) lacks tenant scoping and will disable heartbeat tasks across tenants based solely on id.
  - _Fix:_ Include tenant_id in the UPDATE predicate (e.g. WHERE tenant_id = ${tenantId} AND id = ANY(...)) or perform updates in a per-tenant loop after selecting only that tenant's rows. Add admin gating if this must be a cross-tenant operation.

### HIGH (43)

- **server/ab-optimizer.ts:110** — runDueAbExperiments SELECTs from `experiments` without scoping to a tenant (no WHERE tenant_id = <tenant>), reading rows across all tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE category='ab_test' AND status='running' AND tenant_id = ${tenantId}) or iterate per-tenant. Ensure every read against tenant-scoped `experiments` includes the caller's tenant_id (or use Drizzle's eq(experiments.tenantId, tenantId)).
- **server/agent-activity.ts:60** — getPersonality reads from `personas` using only persona id (WHERE id = personaId) with no tenant constraint — it can leak or access personas belonging to other tenants.
  - _Fix:_ Include tenant ownership validation when fetching a persona (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}), or require callers to pass tenantId and use eq(personas.tenantId, tenantId). Verify ownership before caching/returning persona data.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs from `ai_insights` without scoping by tenant (WHERE status='applied' AND priority='high' AND action_taken = ...), returning rows across all tenants.
  - _Fix:_ Restrict the sweep to a tenant (pass tenantId or iterate over tenants) and include WHERE tenant_id = ${tenantId}. If a global sweep is required, make the cross-tenant semantics explicit and ensure proper authorization; otherwise always tenant-scope reads of tenant-scoped tables.
- **server/agentic-features.ts:1021** — classifyReply updates outreach_enrollments with UPDATE ... WHERE id = ${row.id} but omits tenant scoping (no WHERE tenant_id = ${params.tenantId}), allowing a write that isn't constrained to the caller's tenant.
  - _Fix:_ Add tenant verification to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) so writes cannot affect rows outside the caller's tenant. Prefer using both id and tenant_id for destructive updates.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table with NO tenant_id constraint. This reads rows across all tenants (cross-tenant access) without an explicit admin/owner guard.
  - _Fix:_ If this query must be per-tenant, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) so every read is constrained to the caller's tenant. If the intent is an admin-wide sweep, make that explicit: require an ADMIN context (use ADMIN_TENANT_ID or an authenticated admin-only code path), document the cross-tenant intent, and ensure the function cannot be invoked by unprivileged callers. In either case keep the tenant id as a bound parameter (not string-interpolated) and log/audit admin-level cross-tenant access.
- **server/agentic/resume-worker.ts:65** — Initial candidate SELECT queries agent_runs (joined with agent_approvals) without any tenant_id constraint — it reads rows across all tenants (cross-tenant read).
  - _Fix:_ Constrain the SELECT to a specific tenant or otherwise enforce tenant scoping at the SQL layer. For example, add a WHERE clause filtering r.tenant_id = ${tenantId} (or accept a tenantId parameter and iterate per-tenant), or narrow the query to only system/admin contexts that are explicitly audited. Ensure every read against tenant-scoped tables (agent_runs, agent_approvals) includes AND eq(tenant_id, <caller tenant>) or equivalent so tenant isolation is enforced at the application SQL layer.
- **server/auto-asset-capture.ts:184** — SELECT DISTINCT from conversations (`SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL ...`) does not include a tenant_id constraint and therefore reads conversations across all tenants.
  - _Fix:_ Scope this maintenance/backfill query to a single tenant (e.g. add `WHERE c.tenant_id = ${tenantId}`) or explicitly iterate tenants and run the query per-tenant. If the job is intended to be cross-tenant, make that explicit and ensure it runs under a platform-admin path with audited safeguards; otherwise fail-closed and filter by tenant to preserve isolation.
- **server/auto-project.ts:135** — SELECT from tenant-scoped table "messages" is missing a tenant_id constraint (WHERE tenant_id = ${tenantId}).
  - _Fix:_ Constrain the query to the caller's tenant, e.g. add AND tenant_id = ${tenantId} to the WHERE clause (or join via conversations and assert c.tenant_id = ${tenantId} and include m.tenant_id = ${tenantId}). Always parameterize the tenant filter.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table "messages" (reading messages for a conversation) has no tenant_id constraint in the WHERE clause.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}, or otherwise ensure the SQL includes tenant_id = ${tenantId} as part of the WHERE to enforce isolation.
- **server/auto-transcript.ts:128** — Subquery COUNT(*) FROM messages in the conversations backfill query is executed without constraining messages by tenant_id (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id).
  - _Fix:_ Constrain the subquery by tenant, e.g. SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id (or AND tenant_id = ${expectedTenant}) so the messages table is always scoped to the owning tenant.
- **server/auto-transcript.ts:153** — Subquery COUNT(*) FROM messages in the 'alsoLinked' backfill query uses messages without a tenant_id constraint (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id).
  - _Fix:_ Include tenant scoping in the subquery, for example: SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id (or add AND tenant_id = ${tenantId}). Always ensure message reads include tenant_id.
- **server/channel-routing.ts:30** — SELECT joins tenant-scoped table `personas` without constraining by tenant_id. The query returns persona_name via JOIN personas p ON p.id = cr.persona_id but does not restrict p.tenant_id (or cr by tenant), allowing cross-tenant persona leakage.
  - _Fix:_ Make channel routing tenant-aware or validate persona ownership. Either (A) add a tenantId parameter to listChannelRoutes and include WHERE p.tenant_id = ${tenantId} (or cr.tenant_id = ${tenantId} if you change channel_routes to be tenant-scoped), or (B) keep channel_routes global but validate each referenced persona row belongs to the caller's tenant before returning it. Never join or read tenant-scoped tables without a tenant_id filter.
- **server/channel-routing.ts:61** — getPersonaForChannel selects persona_id from a global channel_routes table and returns it with no tenant scoping or ownership check. Consumers may use that persona_id to read or act on a persona belonging to another tenant.
  - _Fix:_ Make getPersonaForChannel tenant-aware: accept tenantId and SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId} (if channel_routes becomes tenant-scoped). If channel_routes remains global, validate the returned persona_id belongs to the caller's tenant by checking personas.tenant_id before returning it. Always verify ownership of foreign IDs returned to callers.
- **server/chat-engine.ts:3763** — Telemetry call markReviewReused(conversationId, toolName) is invoked with a foreign identifier (conversationId) but no tenant context is passed or ownership verification performed. If the callee reads/writes DB rows keyed only by conversation_id, this can allow cross-tenant observations or writes.
  - _Fix:_ Pass the caller's tenant id into the telemetry function (e.g. markReviewReused(tenantId, conversationId, toolName)) or perform an explicit ownership check before invoking it (verify the conversation belongs to tenantId). Ensure the callee uses WHERE tenant_id = ? on all DB operations or accepts tenantId and enforces it server-side.
- **server/chat-engine.ts:3881** — Call to recordErrorForRetryStormDetection(conversationId, ...) passes only conversationId (a foreign id) with no tenant context or prior ownership verification. If recordErrorForRetryStormDetection performs DB reads/writes by conversation_id alone, tenant isolation may be violated.
  - _Fix:_ Include the tenant id when invoking the stuck-diagnostics helper (e.g. recordErrorForRetryStormDetection(tenantId, conversationId, ...)) or validate that the conversation belongs to tenantId before calling. Modify the callee to accept and enforce tenant_id in its queries (WHERE tenant_id = ?).
- **server/chat-engine.ts:4102** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is invoked with conversationId but without tenant context or an ownership check. This can lead to cross-tenant reads/writes if the callee uses conversation_id alone.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection (or perform a prior ownership verification) and ensure the callee includes tenant_id in all SQL filters. Prefer changing the API to accept tenantId and have the callee enforce WHERE tenant_id = ? for any DB operations.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` is missing an explicit tenant_id constraint. The query uses only project_id (project_files WHERE project_id = ${projectId}) which relies on prior validation rather than enforcing tenant isolation at the row level.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. SELECT ... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId}. Alternatively join back to projects and assert p.tenant_id = ${tenantId} in the same query so every read on a tenant-scoped table includes tenant_id = <caller tenant>.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` is missing an explicit tenant_id constraint. The query reads project_notes WHERE project_id = ${projectId} without restricting tenant_id, risking cross-tenant data exposure if project_id validation is bypassed or incorrect.
  - _Fix:_ Constrain the query by tenant, e.g. SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY created_at DESC LIMIT 20. If project_notes lacks tenant_id, add the column and ensure INSERTs/SELECTs include tenant_id.
- **server/cmmc-assessments.ts:150** — getCmmcAccessByToken performs a SELECT/JOIN on tenant-scoped tables (cmmc_assessment_invitations / cmmc_assessments) without an explicit tenant_id = <caller tenant> WHERE clause. The lookup is driven solely by the token hash, so the query does not include an explicit tenant constraint.
  - _Fix:_ Either (a) require/accept an explicit tenantId from the caller and add eq(...tenantId, tenantId) to the WHERE clause, or (b) ensure the function clearly documents that token lookups are authority-scoped and that every subsequent action that uses the returned rows enforces the returned row's tenant_id in its WHERE clauses. Prefer adding an explicit tenant filter when possible (or validate and return tenantId but ensure all downstream writes/reads use that tenantId in their WHERE conditions).
- **server/data-protection.ts:149** — SELECT from tenant-scoped table `conversations` without a tenant_id predicate: `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}`
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. WHERE tenant_id = <tenant>) or run the purge inside an admin/batched context that explicitly scopes work per-tenant. If this is an intentional global maintenance job, make the tenant-scan explicit and ensure only an authorized admin path can call it; otherwise add `AND tenant_id = ${tenantId}` (or iterate tenants explicitly via a controlled list).
- **server/data-protection.ts:359** — SELECT from tenant-scoped table `project_notes` using only project_id (caller-supplied foreign id) with no tenant_id check: `SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}`
  - _Fix:_ Add an application-level tenant ownership check on project_notes (e.g. include `AND tenant_id = ${proj.tenant_id}` or `AND tenant_id = ${tenantId}`), or JOIN project_notes to projects and assert project.tenant_id = caller tenant before reading. Never trust a foreign id alone without verifying the referenced row belongs to the caller's tenant.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` using only project_id with no tenant_id predicate: `SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}`
  - _Fix:_ Scope the query to the caller's tenant (e.g. add `AND tenant_id = ${proj.tenant_id}` or `AND tenant_id = ${tenantId}`), or resolve project_files via a JOIN to projects to ensure the project belongs to the tenant. Do not rely on project_id alone to enforce isolation.
- **server/data-protection.ts:403** — COUNT(*) on tenant-scoped `messages` without tenant constraint: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}` (conv rows were filtered by tenant earlier, but the messages query lacks tenant_id)
  - _Fix:_ Include tenant scoping in the messages COUNT (e.g. `WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}` or `AND tenant_id = ${tenantId}`). Always add tenant_id predicates when touching tenant-scoped child tables, even when the parent id was previously filtered, to avoid cross-tenant races or FK anomalies.
- **server/dfy-intake.ts:52** — SELECT from tenant-scoped table `dfy_intake_forms` is performed using only the secret token (WHERE token = ...) and does NOT constrain the row to a tenant_id. This violates the invariant that tenant-scoped reads must include tenant_id and allows a token (if known) to leak another tenant's form.
  - _Fix:_ Namespace or validate the token by tenant: include tenant_id in the WHERE clause (e.g. WHERE token = ${token} AND tenant_id = ${tenantId}), or derive the tenant from the caller/server context and enforce that the retrieved row belongs to that tenant. If the public-token pattern is intentional, document the risk and ensure tokens are single-tenant (e.g. embed tenant in the token or store token→tenant mapping and always verify ownership before returning data).
- **server/email.ts:370** — SELECT on tenant-scoped table `inbox_messages` without tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This read can cross tenant boundaries and cause stored-message lookup/logic to leak or be influenced by other tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise resolve the tenant first and scope the lookup by tenant_id. Ensure polling logic uses tenant-aware queries so a message for one tenant can't suppress storing for another.
- **server/email.ts:415** — Backfill SELECT/UPDATE against tenant-scoped table `inbox_messages` with no tenant_id filter. The code SELECTs rows with empty bodies across all tenants and later UPDATEs rows by id without constraining tenant_id, which can read and write other tenants' rows.
  - _Fix:_ Query and update inbox_messages scoped to a specific tenant (WHERE tenant_id = ${tenantId}). If this is an admin multi-tenant maintenance job, make the intent explicit and run under a clearly documented admin tenant constant or iterate per-tenant and include tenant_id in every DML statement.
- **server/embeddings.ts:799** — storeEmbeddingVec branch falls back to updating tenant-scoped tables without a tenant_id constraint: UPDATE ${sql.raw(table)} ... WHERE id = ${id}. This performs a write to a tenant-scoped table (memory_entries or agent_knowledge) without scoping to a tenant.
  - _Fix:_ Require and pass tenantId for all writes to tenant-scoped tables. Do not execute unconditional UPDATE/INSERT on tenant-scoped tables without tenant_id. Remove the fallback path or require an explicit admin-context with clear auditing (and include tenant_id in WHERE or use an explicit ADMIN_TENANT_ID constant where appropriate).
- **server/embeddings.ts:845** — Backfill selecting/updating memory_entries and agent_knowledge without tenant scoping. Queries like SELECT id, embedding FROM memory_entries ... LIMIT 200 and subsequent UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} lack tenant_id filters, causing cross-tenant reads/writes.
  - _Fix:_ Perform backfills per-tenant (iterate tenants and include tenant_id in WHERE), or explicitly run under a documented admin context that records intent and uses tenant-scoped WHERE clauses on DML (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Avoid unconstrained SELECT/UPDATE on tenant-scoped tables.
- **server/google-drive.ts:866** — SELECT reads project row by project id without scoping to the caller's tenant (where(eq(projects.id, projectId))). Reading a tenant-scoped table row without applying tenant_id in the WHERE can expose another tenant's data.
  - _Fix:_ Constrain reads to the caller's tenant in the query (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) so the DB never returns rows from other tenants. Avoid reading full row data before verifying ownership.
- **server/google-drive.ts:1473** — SELECT that resolves a project for uploadAndShare reads projects by id without applying a tenant filter (.where(eq(projects.id, params.projectId))). This reads a tenant-scoped row before verifying caller ownership.
  - _Fix:_ Scope the SELECT to the caller's tenant (e.g. add AND eq(projects.tenantId, params.tenantId)) so the query only returns projects owned by the caller. Do not rely on post-read comparisons; prefer pushing tenant checks into the WHERE clause.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at IS NOT NULL AND next_run_at <= NOW() lacks a tenant_id constraint. This reads schedule rows across all tenants and subsequently performs tenant-scoped actions using sched.tenant_id.
  - _Fix:_ Either (a) scope the SELECT to a specific tenant when running per-tenant (WHERE tenant_id = ${tenantId}), or (b) make the cross-tenant intent explicit (admin-only worker) and ensure safer processing by iterating tenant-by-tenant and applying tenant-scoped WHERE filters for any subsequent UPDATEs. At minimum, restrict UPDATEs/claims to include tenant_id = sched.tenant_id to avoid accidental cross-tenant effects.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT from tenant-scoped table heartbeat_tasks with no tenant_id constraint (SELECT * FROM heartbeat_tasks ... ). This reads task rows across all tenants without an application-enforced tenant filter.
  - _Fix:_ If this is intended to operate cross-tenant, make that explicit and restrict to an admin/cron context. Otherwise add an application-level tenant filter (e.g. WHERE tenant_id = ${tenantId}) or accept an ALL_TENANTS sentinel only in a tightly-audited scheduler boundary. Prefer using a parameterized storage API that requires the tenant context rather than an unscoped SELECT.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks ... WHERE id = ${id} ... without tenant_id constraint. This will update heartbeat_tasks rows by id across tenants and lacks an application-level tenant check.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or route the update through a storage API that enforces the tenant context. If this function is intentionally cross-tenant (scheduler claim), document and gate it to an explicit scheduler/admin caller and use a safe, audited path.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — raw UPDATE on tenant-scoped table without tenant_id in WHERE. This mutates tenant-scoped rows without enforcing tenant ownership.
  - _Fix:_ Constrain the update by tenant (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call a storage-layer helper that requires the tenant context. If the change is intentionally global, make the admin intent explicit and document the authority to perform cross-tenant writes.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — raw UPDATE on tenant-scoped table without tenant_id in WHERE. This mutates tenant-scoped rows without enforcing tenant ownership.
  - _Fix:_ Constrain the update by tenant (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call a storage-layer helper that requires the tenant context. If the change is intentionally global, make the admin intent explicit and document the authority to perform cross-tenant writes.
- **server/heartbeat.ts:255** — SELECT * FROM event_log WHERE status = 'pending' ... — querying event_log (which includes tenant_id) with no tenant_id filter. This reads pending events across tenants without an application-level tenant constraint.
  - _Fix:_ Either iterate per-tenant (select WHERE tenant_id = ?) or explicitly document and gate this as a scheduler/admin cross-tenant operation. Prefer adding an explicit tenant_id predicate or using a scheduler-only API that makes cross-tenant intent and auditing explicit.
- **server/heartbeat.ts:2677** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — updating a tenant-scoped table row without a tenant_id predicate. The UPDATE may affect rows without verifying tenant ownership at the SQL level.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId} or ${newTask.tenantId}), or use a storage-layer helper that enforces tenant context. If this is intended to be cross-tenant, restrict and audit the path explicitly.
- **server/job-queue.ts:160** — UPDATE on tenant-scoped table `agent_jobs` (claimDueJobs) has no tenant_id constraint in its WHERE clause. This UPDATE will transition jobs across all tenants to 'running' without per-tenant scoping.
  - _Fix:_ Restrict claiming to an explicit tenant scope (or clearly document & gate global admin usage). Add a tenantId parameter and include `AND tenant_id = ${tenantId}` in the inner SELECT/outer WHERE, or run claim/reclaim per-tenant inside a controlled admin-only loop. Ensure any caller that runs this is an authorized system worker.
- **server/job-queue.ts:196** — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 1) has no tenant_id constraint in its WHERE clause. This will mark jobs from all tenants as failed_terminal when leases expire.
  - _Fix:_ Perform lease-reclaim per-tenant or add tenant scoping: either loop tenants and run the reclaim for each (with `AND tenant_id = ${tenantId}`) or otherwise ensure this operation is strictly admin-only and audited. Prefer explicit tenant_id filters to avoid cross-tenant mutation.
- **server/job-queue.ts:210** — UPDATE on tenant-scoped table `agent_jobs` (reclaimExpiredLeases step 2) has no tenant_id constraint in its WHERE clause. This re-queues pending jobs globally without tenant scoping.
  - _Fix:_ Same as above — scope the requeue to a tenant (e.g. pass tenantId) or run the reclaim per-tenant. Avoid unscoped updates on tenant-scoped tables to prevent accidental cross-tenant state changes.
- **server/job-queue.ts:299** — UPDATE on tenant-scoped table `agent_jobs` (failJob terminal branch) modifies job rows without a tenant_id constraint in the WHERE clause. This can mark jobs for other tenants as failed_terminal.
  - _Fix:_ Include tenant validation in failJob (pass tenantId or validate ownership) and add `AND tenant_id = ${tenantId}` to the WHERE. If the worker must act globally, ensure the caller is a trusted admin worker and add explicit audit/logging and/or RBAC checks.
- **server/job-queue.ts:319** — UPDATE on tenant-scoped table `agent_jobs` (failJob retry branch) updates retry scheduling without tenant scoping (no tenant_id in WHERE). This re-schedules jobs across tenants indiscriminately.
  - _Fix:_ Same remediation: include tenant_id in WHERE (require tenantId param) or run per-tenant. Ensure the backoff/reschedule code cannot affect other tenants' jobs unintentionally.
- **server/knowledge-diversity-monitor.ts:266** — SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id — this scans `agent_knowledge` across all tenants without tenant_id filtering. It performs cross-tenant enumeration and drives per-tenant processing without explicit tenant scoping for each mutation.
  - _Fix:_ If this is intended as an admin/system job, ensure it is run only by an authorized background worker and add clear audit controls. Prefer iterating over a vetted tenant list and running per-tenant queries (each with `WHERE tenant_id = ${tenantId}`) to keep tenant operations explicit and avoid accidental cross-tenant mutations.

### MEDIUM (16)

- **server/agent-desk.ts:297** — getDesksOverview executes `SELECT id, name FROM personas` with NO tenant filter, returning persona rows across tenants.
  - _Fix:_ Scope the personas query to the requested tenant (e.g. SELECT id, name FROM personas WHERE tenant_id = ${tenantId}) or explicitly validate persona ownership before using persona names to build the overview.
- **server/agentic-engines.ts:374** — runDecisionEngine queries `personas` with `SELECT name, role FROM personas WHERE is_active = true LIMIT 14` with no tenant constraint — can expose persona info from other tenants.
  - _Fix:_ Filter personas by tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise validate persona rows belong to the tenant before using them in prompts or outputs.
- **server/agentic-features.ts:861** — enrollInSequence checks for existing outreach_enrollments with SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but does NOT include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the check (AND tenant_id = ${params.tenantId}) to ensure the existence check is constrained to the caller's tenant and cannot touch or be influenced by other tenants' rows.
- **server/auto-asset-capture.ts:207** — Querying messages for a conversation (`SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant'`) does not include a tenant_id constraint. If messages is a tenant-scoped table this allows cross-tenant reads (the enclosing backfill loop selected conv rows without tenant scoping).
  - _Fix:_ Include the tenant check in the messages query (e.g. `AND tenant_id = ${c.tenant_id}`) or join messages to conversations and require `conversations.tenant_id = ${tenantId}` so every row read is constrained to the owning tenant. Ensure any maintenance loops preserve tenant scoping when operating over multiple tenants.
- **server/business-tools.ts:235** — addCustomer allows caller-supplied assigned_to to be stored on the new customer row without verifying that the referenced user (assigned_to) belongs to the same tenant.
  - _Fix:_ Validate that params.assigned_to (if provided) refers to a user that belongs to the caller's tenant before inserting (e.g. SELECT id FROM users WHERE id = ${params.assigned_to} AND tenant_id = ${tid} LIMIT 1). Or reject/clear assigned_to unless it refers to a tenant-local user.
- **server/business-tools.ts:250** — updateCustomer accepts params.assigned_to and will write it into the customers row (via updateMap/sql.raw) without verifying the referenced user belongs to the caller's tenant.
  - _Fix:_ When updating assigned_to (or any foreign user id), first verify ownership: query the users (or relevant) table to ensure the id exists and tenant_id = caller tenant. Reject or clear the value if it does not belong to the tenant.
- **server/commitment-drafter.ts:146** — SELECT DISTINCT tenant_id FROM `commitments` is executed without a tenant filter (cross-tenant scan). Any SELECT against a tenant-scoped table lacks tenant_id constraint here.
  - _Fix:_ If this enumeration is intentional (a global background job), ensure it runs only in a privileged/admin context and document/guard that fact. If not necessary, restrict the query to a specific tenant. Prefer having an explicit admin-only code path or separate admin table for cross-tenant bookkeeping instead of unbounded SELECTs against tenant-scoped tables.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` (scanAndEscalate) is performed with no tenant_id constraint (SELECT id, tenant_id, description, ... FROM commitments WHERE ...).
  - _Fix:_ If this heartbeat is intended to fan-in across all tenants, isolate/mark it as an admin-only operation and avoid selecting tenant-sensitive free-text fields (e.g. description) unless strictly necessary. Otherwise add WHERE tenant_id = ${tenantId} (or accept a tenantId param) so reads are scoped to a single tenant. If admin visibility is required, ensure the operation can only run in a tightly-controlled admin context and minimize selected columns to avoid exposing tenant content.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks is executed without an explicit tenant_id constraint: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}. Although collection existence is checked earlier, the DELETE SQL itself lacks tenant scoping and could affect rows for other tenants if collection_id were ambiguous or validation changed.
  - _Fix:_ Add tenant scoping to the DELETE statement (e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}), or perform the DELETE via a parameterized query that includes the tenant check / use the same validated collection row to drive a tenant-scoped deletion to ensure the DB write is explicitly tenant-constrained.
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table doc_chunks lacks tenant scoping: UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}. Even though the id was obtained from an earlier tenant-scoped SELECT, the UPDATE statement itself does not include tenant_id and could accidentally modify another tenant's row if assumptions change.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. UPDATE doc_chunks SET embedding = ${...}::jsonb WHERE id = ${row.id} AND tenant_id = ${tenantId}. Alternatively, update via the previously-validated selection (e.g. use collection_id+chunk_index or include tenant_id) so every write includes an explicit tenant check.
- **server/embeddings.ts:256** — Use of sql.raw() with a vector literal derived from request input (queryEmbedding → vec) in vectorSearchMemory (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). Although vec is syntactically validated, this is a request-derived interpolation into raw SQL.
  - _Fix:_ Avoid sql.raw interpolation of request-derived values. Use parameterized queries or pgvector-safe binding APIs. If raw injection is unavoidable, ensure strict validation/conversion and prefer passing the vector as a parameterized value (or a prepared statement) rather than embedding it into SQL text.
- **server/episode-playbooks.ts:67** — String-interpolated embedding literal (${embeddingLiteral}::vector) derived from generateEmbedding(goal) is injected into SQL (SELECT ... goal_embedding <=> ${embeddingLiteral}::vector ...). This is request-derived SQL interpolation (embedding is derived from user-supplied goal).
  - _Fix:_ Avoid embedding vector literals directly into SQL text. Use safe parameterization or a prepared statement / driver binding for vector values. Validate and convert embeddings to a safe parameter form instead of string interpolation; or use sql.raw only with strictly validated, non-user-controlled inputs and document the validation step.
- **server/job-queue.ts:239** — UPDATE on tenant-scoped table `agent_jobs` (completeJob) updates a job's status to 'succeeded' without verifying tenant_id. The op relies solely on id/attempts for fencing.
  - _Fix:_ Add tenant fencing to the completeJob API (accept/derive tenantId) and include `AND tenant_id = ${tenantId}` in the WHERE clause, or otherwise validate the job's tenant before writing to prevent cross-tenant completes if IDs are misused.
- **server/job-queue.ts:281** — SELECT max_attempts FROM `agent_jobs` uses `WHERE id = ${id}` with no tenant_id check. Reading job metadata without tenant scoping can be misused in multi-tenant contexts.
  - _Fix:_ When this lookup is performed in tenant-sensitive flows, require or accept a tenantId and add `AND tenant_id = ${tenantId}` to the SELECT, or perform the lookup within an admin-only guarded path and document it.
- **server/job-queue.ts:336** — UPDATE on tenant-scoped table `agent_jobs` (cancelJob) updates by id only (no tenant_id constraint). An operator or API that calls cancelJob could cancel another tenant's job by id alone.
  - _Fix:_ Require tenantId to be supplied by the caller and add `AND tenant_id = ${tenantId}` to the WHERE, or restrict this API to admin-only endpoints with explicit authorization and audit logging. Prefer tenant-checked cancellation in tenant-facing routes.
- **server/job-queue.ts:406** — SELECT ... FROM agent_jobs is executed with a potentially-empty WHERE clause (listJobs). If caller does not pass tenantId, the query returns rows across all tenants.
  - _Fix:_ Require callers to provide an explicit tenantId unless the caller is an authorized admin. Alternatively default to the current request's tenant context and refuse to list jobs globally from tenant-facing routes. If global listing is needed, gate it behind admin RBAC and audit logs.

### LOW (2)

- **server/episode-playbooks.ts:133** — UPDATE episode_playbooks uses id = ANY({<ids>}::int[]) where the array literal is constructed via string interpolation (`{${ids.join(",")}}`). Although ids originate from DB rows, constructing SQL array literals via string interpolation can be brittle and risks injection if ids are not strictly validated.
  - _Fix:_ Use parameterized array binding rather than constructing an array literal string (e.g. use sql.join on parameterized values or pass an array parameter supported by the DB driver). Ensure any interpolated identifiers are strictly validated as integers before inclusion.
- **server/job-queue.ts:378** — SELECT tenant_id FROM agent_jobs WHERE id = ${id} (getJobTenantForAdmin) has no tenant constraint — the function is an admin helper but performs an unscoped read on a tenant-scoped table.
  - _Fix:_ Limit usage to admin-only code paths and add an authorization check around callers. If exposing to non-admin callers, require an additional guard and parameterize the tenantId to avoid accidental cross-tenant access.
