# Tenant-Isolation Audit Report

_Generated 2026-09-18T19:08:41.942Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 495 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **191/686** file(s) audited · chunks 20/63 OK (0 failed, 43 pending)
- Findings: **72** (CRITICAL 8 · HIGH 42 · MEDIUM 20 · LOW 2)
- Severe dispositions: **50 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (8)

- **server/business-tools.ts:95** — INSERT into invoice_items omits tenant_id. If invoice_items is a tenant-scoped table this will create rows lacking the required tenant_id (or allow cross-tenant association), violating the invariant that every INSERT into tenant-scoped tables must pass tenant_id explicitly.
  - _Fix:_ Include the tenant id in the INSERT. For example: INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). Alternatively, if invoice_items is intentionally non-tenant-scoped, document that explicitly and ensure schema/enforcement reflects that decision. Also add a unit test that validates invoice_items rows are always created with the correct tenant_id.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table `commitments` without tenant_id constraint (UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}). This allows the code to modify a commitment row by id across tenants.
  - _Fix:_ Always include tenant scoping on mutating statements. Change the statement to include AND tenant_id = ${tenantId} (or the validated tenant id derived from the row) — e.g. UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}. Also validate/derive tenantId before issuing the UPDATE and fail-closed if tenantId is missing.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks uses WHERE doc_path = ${docPath} AND collection_id = ${collectionId} but omits tenant_id. This is a write (DELETE) against a tenant-scoped table without a tenant constraint.
  - _Fix:_ Include tenant scoping in the DELETE (e.g. AND tenant_id = ${tenantId}) or ensure the collection check earlier is used to derive/verify tenantId and apply it in the DELETE. Never perform destructive ops on tenant-scoped tables without an explicit tenant_id WHERE clause.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} omits tenant_id. Updating a tenant-scoped row by id alone risks affecting another tenant's row if ids collide or are mis-derived.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Prefer binding the tenantId from the earlier SELECT that enumerated rows to ensure the update cannot escape the tenant context.
- **server/embeddings.ts:809** — storeEmbeddingVec falls back to executing an UPDATE on a tenant-scoped table without any tenant_id constraint (UPDATE ${sql.raw(table)} ... WHERE id = ${id}). This allows a write that can modify rows across tenants.
  - _Fix:_ Do not allow an unscoped path. Require and validate a positive tenantId for all writes to tenant-scoped tables. Change the fallback to throw if tenantId is not provided, or include AND tenant_id = ${tenantId} in the WHERE clause. Use parameterized Drizzle queries rather than sql.raw when possible.
- **server/embeddings.ts:855** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without a tenant_id constraint. This is a tenant-unsafe write that can modify records belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and require a validated tenantId input to the backfill routine. Prefer Drizzle parameterized updates to avoid string-interpolated/unscoped SQL.
- **server/embeddings.ts:872** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} is executed with no tenant_id constraint. This performs an unscoped write against a tenant-scoped table and can overwrite other tenants' rows.
  - _Fix:_ Require and validate tenantId and include AND tenant_id = ${tenantId} in the WHERE clause. Prefer parameterized Drizzle updates and remove any code paths that perform unscoped UPDATEs on tenant-scoped tables.
- **server/google-drive.ts:1665** — INSERT INTO project_files does not set tenant_id (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) ...). For tenant-scoped tables, every INSERT must include tenant_id explicitly; omitting it breaks tenant isolation.
  - _Fix:_ Include tenant_id in the INSERT columns and bind it to the validated tenant (e.g. VALUES (..., ${resolvedTenantId}, ...)). Also ensure ownership is validated (assertProjectInTenant or equivalent) before performing the INSERT. Prefer using parameterized Drizzle insert helpers that include tenantId explicitly.

### HIGH (42)

- **server/ab-optimizer.ts:110** — runDueAbExperiments reads from experiments without constraining to a tenant (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This allows cross-tenant reads of tenant-scoped experiment rows/metadata.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${someTenantId} AND ...) or explicitly restrict this background sweep to run per-tenant (iterate tenants and run tenant-scoped SELECT). If this is intended to be a global admin operation, require and enforce an admin-only guard and audit the operation.
- **server/agent-activity.ts:122** — completeActivity performs UPDATE agentActivity ... WHERE id = ${activityId} with NO tenant_id constraint. This can update rows belonging to other tenants if an id is guessed or misused.
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause, e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))). Ensure callers pass the tenantId and it is used in the WHERE to prevent cross-tenant modification.
- **server/agent-eval.ts:221** — After running an eval, the code updates agent_evals with WHERE id = ${evalId} but does NOT include tenant_id in the WHERE clause. This can modify rows from other tenants if evalId is guessed or misused.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}. Validate evalId ownership before updating or use the inserted eval row's tenant to guard the update.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting selects from ai_insights with no tenant filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ${PENDING_PLAN_MARKER}). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Scope the sweep to a specific tenant (require tenantId param and add WHERE tenant_id = ${tenantId}) or implement per-tenant iteration under an explicit admin-only path. If global scans are required, ensure only privileged admin contexts can run them and add auditing.
- **server/agentic-engines.ts:247** — autoApplyEligibleInsights performs an UPDATE on ai_insights that can run without a tenant filter when tenantId is undefined (the WHERE uses ${tenantScope} which is empty when tenantId is not passed). This allows cross-tenant writes to ai_insights.
  - _Fix:_ Do not perform multi-tenant UPDATEs by default. Require an explicit tenantId parameter, or when a global sweep is intended, ensure it's only callable by an admin and runs per-tenant (looping calls with a tenant-specific WHERE tenant_id = ${tenantId}). Always include tenant_id in the UPDATE WHERE clause to prevent accidental cross-tenant writes.
- **server/agentic-features.ts:1021** — classifyReply updates outreach_enrollments with WHERE id = ${row.id} but does NOT include tenant_id in the WHERE clause. Although the enrollment row was selected earlier under tenant scope, the UPDATE should still enforce tenant ownership to prevent cross-tenant modification if ids are guessed or tampered with.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}. Prefer validating ownership up-front and always include tenant_id in write WHERE clauses for tenant-scoped tables.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to mark pending approvals as expired without any tenant constraint (UPDATE agentApprovals SET status='expired' WHERE status='pending' AND lt(expiresAt, now())). This writes across tenants.
  - _Fix:_ Scope the bulk expiry to a tenant (require tenantId parameter) or perform the expiry per-tenant in an explicit admin-only job. If a global expiry is intended, run it under a privileged admin process and ensure each resulting per-run update enforces tenant_id in its WHERE clause. Always include tenant_id when updating tenant-scoped tables.
- **server/agentic/department-budgets.ts:143** — Unscoped SELECT on tenant-scoped table `department_budgets` — the query SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' reads across all tenants with no tenant_id filter.
  - _Fix:_ Do not perform unscoped reads against tenant-scoped tables. Either (a) scope the query to a specific tenant (add `WHERE tenant_id = ${tenantId} AND ...`), or (b) make the admin intent explicit and require an explicit admin/owner context (e.g. use ADMIN_TENANT_ID or an explicit admin-only API) plus documented authorization. If the function must sweep all tenants, ensure it runs under a tightly-controlled admin path and is clearly separated from tenant-scoped code paths.
- **server/agentic/resume-worker.ts:65** — SELECT against tenant-scoped tables (agent_runs / agent_approvals) is executed without a tenant_id constraint — the query at db.execute(sql`SELECT ... FROM agent_runs ... LEFT JOIN agent_approvals ... WHERE ... LIMIT 50`) returns rows across all tenants.
  - _Fix:_ Constrain the initial candidate query to a specific tenant (add WHERE r.tenant_id = ${tenantId}) or otherwise iterate per-tenant with an explicit, authorized tenantId parameter. If this worker must process multiple tenants, enumerate and claim each tenant explicitly and run the query per-tenant; do not run a global cross-tenant SELECT. Ensure any raw sql uses include eq(agent_runs.tenant_id, tenantId) (or ${tenantId} in the sql template) so every read is scoped to the caller's tenant.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table agent_wake_schedules performed without constraining by tenant_id (the subselect in the WHERE clause selects pending rows across all tenants). This allows cross-tenant rows to be claimed/updated by the scheduler without an application-level tenant filter.
  - _Fix:_ Restrict the claim/update to a single tenant context or include tenant_id in the WHERE clause. Options: (a) run per-tenant sweeps under an explicit tenantId and include WHERE tenant_id = ${tenantId} in the subselect, or (b) add tenant_id to the outer WHERE/subselect (e.g. WHERE id IN (SELECT id FROM agent_wake_schedules WHERE tenant_id = ${tenantId} AND status='pending' ...)). Ensure the scheduler is executed with appropriate privileges if it must operate cross-tenant and document that explicitly (and gate with platform-admin checks).
- **server/channels/kernel.ts:180** — dispatchInbound accepts a conversationId from the adapter (resolveConversation) and immediately passes it to processMessage without verifying that the conversation belongs to turn.tenantId. This allows an attacker or buggy adapter to supply a foreign conversation_id and lets downstream DB access operate on a conversation that may belong to another tenant.
  - _Fix:_ Before calling processMessage, validate ownership of the conversationId against the caller's tenantId (e.g. SELECT id FROM conversations WHERE id = $1 AND tenant_id = $2 and fail if no row). Alternatively require resolveConversation to return an ownership-validated object (or to perform the DB check itself) and/or make processMessage enforce tenant checks on every conversation-scoped read/write. Do not rely solely on the adapter-provided tenantId.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` omits an explicit tenant_id constraint (WHERE tenant_id = ...). The query relies only on project_id being previously validated, but the codebase invariant requires each access to tenant-scoped tables include an explicit tenant_id filter.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. `... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(projectFiles.tenantId, tenantId))). Do not rely solely on previously-validated foreign ids.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` omits an explicit tenant_id constraint (WHERE tenant_id = ...). The query only filters by project_id.
  - _Fix:_ Include the tenant constraint: `SELECT ... FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or Drizzle .where(eq(projectNotes.tenantId, tenantId))). Every tenant-scoped table access must include tenant_id.
- **server/chat-engine/project-context.ts:181** — Query joining project_conversations (pc) and conversations (c) filters c.tenant_id but does not include a tenant_id constraint on project_conversations (pc). This is an access against a tenant-scoped link table without an explicit tenant filter.
  - _Fix:_ Add an explicit tenant check for the link table as well, e.g. `... WHERE pc.project_id = ${projectId} AND pc.tenant_id = ${tenantId} AND c.id != ${conversationId} AND c.tenant_id = ${tenantId}` (or join to projects and filter project.tenant_id = ${tenantId}). Do not rely on indirect constraints.
- **server/data-protection.ts:149** — SELECT across conversations (background purge) reads rows from the tenant-scoped `conversations` table without scoping to a caller tenant (no WHERE tenant_id = ...). This enumerates/operates across tenants with no application-level tenant filter.
  - _Fix:_ Either require/validate an explicit admin context before running this cross-tenant purge, or (preferred for safety) restructure to iterate per-tenant and include an explicit WHERE tenant_id = ${tenantId} predicate on the SELECT so every read/write against tenant-scoped tables carries an application-level tenant filter (or run inside withTenantTx for enforced context).
- **server/dfy-intake.ts:50** — SELECT from dfy_intake_forms by token omits tenant filter (no WHERE tenant_id = ...). Reading a tenant-scoped row without constraining by tenant can return another tenant's form.
  - _Fix:_ Either require and pass the caller's tenantId and include AND tenant_id = ${tenantId} in the WHERE, or explicitly validate that the returned row belongs to the expected tenant before returning it. If the token is intended to be a global public auth mechanism, make that policy explicit and document why a tenant filter is unnecessary.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' has no tenant filter. messages appears to be a tenant-scoped table but is being scanned globally here.
  - _Fix:_ If this code is intended to operate per-tenant, accept a tenantId and add WHERE tenant_id = ${tenantId}. If it truly needs global platform metrics, make the global intent explicit and document/authorize the global query; otherwise restrict the query to tenant scope to preserve isolation.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... runs without tenant filter. tool_performance is tenant-scoped elsewhere; this global read breaks the tenant isolation invariant.
  - _Fix:_ Either add a tenant_id filter (AND tenant_id = ${tenantId}) or require an explicit admin/global scope and document it. Avoid scanning tenant-scoped tables across all tenants unless the operation is explicitly a platform-wide admin task with proper authorization.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance (getEverInvokedTools) is executed without any tenant_id constraint. This reads tenant-scoped data globally.
  - _Fix:_ Parameterize the function to accept a tenantId and include WHERE tenant_id = ${tenantId}, or else move to a dedicated, well-documented global-admin path that explicitly acknowledges and authorizes cross-tenant reads of tool_performance.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... (getActiveTools14d) is executed without tenant scoping. This is a global read over a tenant-scoped table.
  - _Fix:_ Restrict this query to a tenant via WHERE tenant_id = ${tenantId} or require an explicit global/admin mode (with review/authorization). Ensure any operation over tenant-scoped tables includes an application-level tenant filter to preserve isolation.
- **server/email.ts:370** — SELECT from tenant-scoped table inbox_messages executed without a tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can cause cross-tenant deduping/reads (a message for tenant A could block storing for tenant B).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise resolve the message->tenant mapping before the SELECT. Prefer parameterized queries or Drizzle .where(eq(inbox_messages.tenantId, tenantId), eq(inbox_messages.messageId, msgId)).
- **server/email.ts:415** — SELECT from tenant-scoped table inbox_messages performed without tenant_id filter (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR body_text = '') AND ...). This enumerates rows across all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. add AND tenant_id = ${tenantId}) or run this backfill per-tenant. If this is intended to be a cross-tenant maintenance job, make that explicit and run under an admin/maintenance path that uses ADMIN_TENANT_ID and appropriate auditing/authorization.
- **server/email.ts:429** — UPDATE on tenant-scoped table inbox_messages uses WHERE id = ${row.id} with no tenant_id constraint. Updating by id only can accidentally modify records belonging to other tenants if ids are global or if the earlier tenant resolution is incorrect.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or derive tenantId from the row and validate it before updating. Prefer Drizzle .where(eq(inbox_messages.id, id), eq(inbox_messages.tenantId, tenantId)).
- **server/embeddings.ts:814** — SELECT from tenant-scoped table agent_knowledge performed without tenant filter (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ... LIMIT 50). This reads rows across all tenants.
  - _Fix:_ Scope backfill reads to a tenant (e.g. add AND tenant_id = ${tenantId}) or iterate backfill work per-tenant with an explicit maintenance operation running under a trusted admin context. If this is an intentional global backfill, make it explicit and run under admin tenant constant with auditing.
- **server/embeddings.ts:845** — SELECT from tenant-scoped table memory_entries without tenant filter (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200). This enumerates tenant data across all tenants.
  - _Fix:_ Restrict the SELECT to a tenant (AND tenant_id = ${tenantId}) or perform the backfill per-tenant from an authorized maintenance job. Avoid global scans that read tenant-scoped tables without an explicit admin tenancy context.
- **server/embeddings.ts:861** — SELECT from tenant-scoped table agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ...) is executed without tenant_id filtering — reading rows across tenants.
  - _Fix:_ Scope this SELECT to a tenant (AND tenant_id = ${tenantId}) or run the backfill per-tenant under an explicit admin/maintenance context. Ensure the code cannot accidentally process or leak other tenants' knowledge rows.
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot executes a SELECT from agent_runs without filtering by tenant_id (SELECT id, tenant_id, ... FROM agent_runs WHERE status IN ('running','pending') ...). This reads running/pending agent_runs for all tenants into memory, violating tenant isolation.
  - _Fix:_ Restrict the query to a single tenant or an explicit allowed set. Pass the caller/admin tenantId into rehydrateTaskStateOnBoot (or iterate only over known/allowed tenant IDs) and add WHERE tenant_id = ${tenantId} to the SQL. Ensure any rows loaded are validated for ownership before using their data (e.g., only rehydrate states for tenant IDs the process is authorized to handle).
- **server/firecrawl.ts:239** — UPDATE on scrapedPages uses WHERE eq(scrapedPages.id, existing[0].id) but does not include a tenant_id constraint. This violates the invariant requiring all tenant-scoped writes be constrained to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. add AND eq(scrapedPages.tenantId, tenantId) to the WHERE clause (or include both conditions with and(...)). Prefer using the same tenantId variable you used on the initial SELECT to make the write explicit and safe.
- **server/firecrawl.ts:372** — UPDATE on scrapedPages (in crawl loop) uses WHERE eq(scrapedPages.id, existing[0].id) without constraining by tenant_id. Every write to tenant-scoped tables must include the caller's tenant filter.
  - _Fix:_ Include the tenant constraint in the WHERE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Ensure the tenantId is the caller's tenant and not derived from user input.
- **server/glasses-gateway.ts:173** — db.update(apiKeys).where(eq(apiKeys.id, key.id)) updates an apiKeys row by id without also asserting the tenant (tenant_id) in the WHERE. If apiKeys are tenant-scoped, this allows a write without an explicit tenant constraint.
  - _Fix:_ Add the tenant constraint to the WHERE clause when updating apiKeys, e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))). Prefer binding tenant from the authenticated key rather than trusting other sources.
- **server/google-drive.ts:866** — SELECT from projects fetches a project by id (.where(eq(projects.id, projectId))) without scoping the SELECT to the caller's tenant (no WHERE tenant_id = ...). Reads against tenant-scoped tables must be tenant-constrained to avoid cross-tenant exposure.
  - _Fix:_ Add an explicit tenant filter to the query, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Alternatively use a helper that asserts project ownership in the WHERE to avoid returning rows for other tenants.
- **server/google-drive.ts:882** — db.update(projects).set(...).where(eq(projects.id, projectId)) updates the projects row by id without constraining the write to the caller's tenant. This allows cross-tenant writes if projectId is attacker-controlled.
  - _Fix:_ Include the tenant constraint in the WHERE, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Always require the tenant ownership to be part of the update predicate.
- **server/google-drive.ts:1476** — SELECT of a project in uploadAndShare (.where(eq(projects.id, params.projectId))) reads a project by id without including a tenant_id WHERE filter. The code does check proj.tenantId later, but the SELECT itself is not tenant-scoped as required by the invariant.
  - _Fix:_ Scope the SELECT to the caller's tenant in the WHERE, e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))) or otherwise ensure the query only returns rows belonging to the caller. Doing the ownership check in the WHERE prevents accidental cross-tenant leakage.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` has no tenant_id constraint. This reads proposals across all tenants.
  - _Fix:_ Scope this query to a tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant under an explicit admin flow. Ensure the scanner accepts a tenantId parameter or otherwise enforces explicit tenant scoping before reading tenant-scoped tables.
- **server/health-audit.ts:333** — UPDATE on `code_proposals` lacks tenant scoping (WHERE id = ANY(...) only). This can update rows for other tenants.
  - _Fix:_ Add a tenant filter to the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or perform the update per-tenant after verifying ownership. Never update tenant-scoped rows by id alone in a multi-tenant environment.
- **server/health-audit.ts:346** — SELECT from `heartbeat_tasks` is not constrained by tenant_id. This can enumerate/flag heartbeat tasks across all tenants.
  - _Fix:_ Restrict the SELECT to a tenant (e.g. WHERE tenant_id = ${tenantId} AND enabled = true ...), or explicitly accept/verify an admin tenant context before scanning. Avoid global reads of tenant-scoped tables without explicit intent and protection.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) lacks tenant constraint and may disable tasks for other tenants.
  - _Fix:_ Include tenant filtering in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or perform the disabling operation per-tenant after verifying ownership of the task rows.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... is performed without tenant_id in the WHERE clause. This updates tenant-scoped rows across tenants without enforcing tenant ownership.
  - _Fix:_ Add a tenant check to the UPDATE (e.g. AND tenant_id = ${tenantId}) or fetch and use the task's tenant before performing the update. If this is intentionally cross-tenant claiming logic, centralize and audit the claim path to ensure only authorized scheduler code can perform it.
- **server/job-queue.ts:154** — claimDueJobs performs a global UPDATE on agent_jobs (setting status='running', attempts++, lease_until) without constraining by tenant_id. The atomic claim can move/modify jobs for any tenant because there is no WHERE tenant_id = ... filter.
  - _Fix:_ Either scope claims to a specific tenant (add a tenantId parameter and include AND tenant_id = ${tenantId} in the WHERE/inner SELECT) or clearly document and restrict this API to a privileged system context. Prefer per-tenant claiming or an explicit admin-only execution path with authorization checks to avoid accidental cross-tenant job processing.
- **server/job-queue.ts:192** — reclaimExpiredLeases runs two UPDATEs that modify agent_jobs rows (terminal/failure and requeue paths) without any tenant_id constraint. These statements can mutate jobs for all tenants in bulk.
  - _Fix:_ Make reclaimExpiredLeases operate per-tenant (accept tenantId or iterate tenants) or add tenant-scoping to the UPDATE WHERE clauses (AND tenant_id = ${tenantId}). If designed to be global, gate the function behind a well-audited admin/maintenance path and document the intent; otherwise enforce tenant-scoping to preserve isolation.
- **server/job-queue.ts:233** — completeJob updates agent_jobs to mark a job 'succeeded' using WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but omits tenant_id. A stale or malicious caller could complete a job from a different tenant.
  - _Fix:_ Require and validate the job's tenant scope before writing. Add a tenantId parameter to completeJob and include AND tenant_id = ${tenantId} in the WHERE clause, or read & assert the job's tenant_id first and fail if it doesn't match the expected caller scope.
- **server/job-queue.ts:272** — failJob performs a SELECT and subsequent UPDATE on agent_jobs without constraining by tenant_id. The SELECT max_attempts uses WHERE id = ${id} (no tenant filter), and the UPDATE that marks failed or requeues also omits tenant_id.
  - _Fix:_ Require caller-provided tenant context or look up the job and validate its tenant against the caller before mutating. Modify both the SELECT and UPDATE to include AND tenant_id = ${tenantId} (or pass tenantId into the function) to prevent cross-tenant writes, or restrict the API to a trusted admin context with explicit checks.

### MEDIUM (20)

- **server/auto-asset-capture.ts:184** — SELECT DISTINCT FROM conversations scans the conversations table without a tenant filter (no WHERE tenant_id = ...). This reads tenant-scoped rows across all tenants in a background backfill job.
  - _Fix:_ If the backfill is intended to operate per-tenant, iterate tenants and add WHERE tenant_id = ${tenantId}. If it must run globally, ensure the operation is gated by an explicit platform-admin check and audit trail. Prefer explicit tenant-scoped processing to preserve the principle that every tenant-scoped read is guarded by an application-level filter.
- **server/auto-asset-capture.ts:207** — SELECT from messages uses only conversation_id and role as predicates and omits tenant_id. messages is tenant-scoped, so this read could return rows from other tenants if conversation_id collisions or lookups are possible without the tenant constraint.
  - _Fix:_ Constrain message reads to the owning tenant, e.g. add AND tenant_id = ${c.tenant_id} (or join messages to conversations and require messages.tenant_id = conversations.tenant_id) so every message read is explicitly scoped to the correct tenant before further processing.
- **server/business-tools.ts:251** — updateCustomer accepts and writes an assigned_to foreign id (assigned_to = params.assigned_to) without validating that the referenced user belongs to the caller's tenant. This can allow recording references to identities from other tenants.
  - _Fix:_ Validate foreign IDs before storing them. For assigned_to, query the users table (or appropriate principal table) to ensure the given user id exists and has tenant_id = tid (or otherwise restrict to tenant-scoped users). If cross-tenant assignment is not allowed, reject the request when the check fails. Add tests to exercise this ownership check.
- **server/cmmc-assessments.ts:150** — SELECT from tenant-scoped tables `cmmc_assessment_invitations` / `cmmc_assessments` (getCmmcAccessByToken) performs lookup by tokenHash but the WHERE clause does not include an explicit caller tenant_id constraint. This is a tenant-scoped table read without an explicit tenant filter.
  - _Fix:_ Where possible accept and apply the caller's tenantId and add it to the WHERE clause (e.g. `eq(cmmcAssessmentInvitations.tenantId, <callerTenant>)`) so the lookup is always scoped to the caller's tenant. If this is an intentionally token-based cross-tenant access path, make that explicit in the code and harden it (ensure tokens are unguessable, rate-limited/audited) — prefer explicit tenant scoping when caller tenant is known.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` is executed without a tenant_id filter (SELECT ... FROM commitments WHERE status = 'active' ...). This reads rows across all tenants and can lead to cross-tenant information exposure or accidental cross-tenant operations downstream.
  - _Fix:_ Either scope this query to a specific tenant (WHERE tenant_id = ${tenantId}) or make the cross-tenant intent explicit and enforce additional safeguards: require an explicit admin-only path, log/authorize the operation, and ensure any exported/emailed content is strictly redacted. If platform-admin visibility is required, consider enumerating tenants and querying per-tenant with explicit tenant_id in each iteration rather than a single unconstrained SELECT.
- **server/data-protection.ts:359** — SELECT from `project_notes` uses only project_id but omits an explicit tenant_id predicate when querying a tenant-scoped table: `SELECT ... FROM project_notes WHERE project_id = ${proj.id}`.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId} or .where(eq(project_notes.tenant_id, tenantId))) to every query against tenant-scoped tables. If project_id was obtained from a tenant-scoped projects row, still include tenant_id to protect against FK anomalies and future schema changes (or query via a JOIN that includes p.tenant_id = ${tenantId}).
- **server/data-protection.ts:370** — SELECT from `project_files` uses only project_id but omits an explicit tenant_id predicate when querying a tenant-scoped table: `SELECT ... FROM project_files WHERE project_id = ${proj.id}`.
  - _Fix:_ Scope this query by tenant: add AND tenant_id = ${tenantId} (or equivalent Drizzle where) so reads against tenant-scoped tables always include the tenant predicate, even when using a foreign key such as project_id.
- **server/data-protection.ts:403** — COUNT query against `messages` omits tenant scoping: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}`. Without an explicit tenant_id predicate this can become a cross-tenant read if conversation_id alone is insufficient to guarantee ownership.
  - _Fix:_ Add tenant scoping to the messages count (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id} or AND tenant_id = ${tenantId}). Prefer using the tenant value known for the conversation to make the predicate explicit and race-resistant.
- **server/deep-interview.ts:337** — INSERT ... SELECT that reads `project_conversations pc` uses pc.conversation_id = ${state.conversationId} but does not include an explicit tenant_id predicate on project_conversations (a tenant-scoped table). The query relies on joining to `projects p` with p.tenant_id = ${state.tenantId}, but the codebase policy requires an explicit tenant predicate on each tenant-scoped table.
  - _Fix:_ Add explicit tenant predicates for all tenant-scoped tables involved (e.g. ensure `project_conversations` is filtered by tenant_id = ${state.tenantId} as well), or restructure the statement to JOIN/WHERE that asserts tenant ownership for every table referenced. Alternatively execute within withTenantTx(tenantId, ...) to get enforced transactional tenant context plus application-level WHERE clauses.
- **server/google-workspace.ts:2287** — INSERT INTO presenter_slide_images does not include tenant_id. If presenter_slide_images is a tenant-scoped table, this write will create rows without explicit tenant ownership.
  - _Fix:_ If presenter_slide_images is tenant-scoped, include tenant_id in the INSERT values (and validate session ownership). Otherwise document why this table is intentionally global. Prefer passing tenantId explicitly (and/or join via session→tenant validation) when persisting images.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} does not verify tenant ownership. Updating by session id alone can modify another tenant's session.
  - _Fix:_ Ensure the update is tenant-scoped: e.g. UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId} (or validate session belongs to tenantId before updating). Do not rely on id-only updates for tenant-scoped resources.
- **server/heartbeat-schedules.ts:60** — SELECT from tenant-scoped table research_schedules is executed without any tenant_id constraint (SELECT * FROM research_schedules ...).
  - _Fix:_ If this is intended to be a cross-tenant sweep, document and centralize the behavior in a clearly-named scheduler boundary; otherwise add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}) or restrict the selected columns to non-sensitive metadata. Ensure any subsequent per-tenant work asserts tenant context before touching tenant-scoped rows.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} executes without constraining tenant_id. This mutates a tenant-scoped table by id only and does not include tenant ownership in the WHERE clause.
  - _Fix:_ Include tenant isolation in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the update via a storage API that asserts the task's tenant before mutating.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} executes without tenant_id in the WHERE clause.
  - _Fix:_ Add tenant-aware WHERE (AND tenant_id = ${t.tenantId}) or use an application-layer storage helper that validates/limits the update to the task's tenant.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks WHERE ... is executed with no tenant_id constraint. This reads tenant-scoped tasks across tenants without an explicit documented cross-tenant boundary.
  - _Fix:_ If this function is intentionally cross-tenant (scheduler boundary), document and isolate it clearly; otherwise add a tenant_id filter. Consider returning only the columns required and ensure callers assert tenant context before performing tenant-scoped actions.
- **server/heartbeat.ts:258** — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id constraint, reading pending event rows across tenants.
  - _Fix:_ If the processor is intended to handle events across tenants, explicitly document and centralize that cross-tenant responsibility; otherwise add a tenant_id filter (or iterate per-tenant) and ensure the processing path asserts tenant ownership before any tenant-scoped side-effects.
- **server/heartbeat.ts:2679** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without constraining tenant_id after creating a new task. The write to a tenant-scoped table lacks an explicit tenant_id WHERE clause.
  - _Fix:_ Include tenant_id in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenant}) or perform the approval_status change via the storage API that validates tenant ownership of the task.
- **server/heartbeat.ts:3152** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed (delegateTaskFromChat) with no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. AND tenant_id = ${tenantId}) or use a storage method that enforces the tenant context before mutating approval_status.
- **server/job-queue.ts:334** — cancelJob updates agent_jobs WHERE id = ${id} AND status IN (...) without a tenant_id constraint, allowing cancellation of jobs across tenants if called without further authorization.
  - _Fix:_ Add an explicit tenantId parameter and include AND tenant_id = ${tenantId} in the WHERE, or ensure this function is only callable by an authorized admin route that validated ownership. Prefer explicit scoping for tenant-facing callers.
- **server/job-queue.ts:391** — listJobs builds a SELECT from agent_jobs that may execute without any tenant_id WHERE clause when the caller omits tenantId. This can return jobs across tenants (sensitive multi-tenant data exposure) if used by non-admin callers.
  - _Fix:_ Require an explicit tenantId for tenant-facing callers or require an explicit ALL_TENANTS admin scope and enforce authorization. If tenantId is optional, ensure callers are privileged and audited; for tenant APIs, always include AND tenant_id = ${tenantId}.

### LOW (2)

- **server/agentic/task-forces.ts:44** — User-supplied personaIds are converted to an array literal string and interpolated into SQL via sql`${literal}::int[]`. Although integers are filtered, this uses a string-interpolated SQL fragment derived from caller input, which is a risky pattern (sql.raw/string-interpolation with request-derived value).
  - _Fix:_ Avoid injecting sanitized strings into SQL fragments. Instead pass persona IDs as a parameterized array or use Drizzle/parameter binding to supply an int[] safely (e.g. supply the array as a parameter or use JSONB and cast to int[] server-side). This preserves parameterization and eliminates the need for raw SQL fragments derived from caller input.
- **server/job-queue.ts:375** — getJobTenantForAdmin executes SELECT tenant_id FROM agent_jobs WHERE id = ${id} with no tenant filter. While this is intended to find the job's tenant for admin workflows, it's an unscoped SELECT against a tenant-scoped table.
  - _Fix:_ Document and limit this API to admin-only usage. As a defense-in-depth measure, consider returning tenant_id only to authenticated/authorized admin callers, or accept an admin-authorized token/context to ensure this unscoped lookup cannot be abused by tenant-level callers.
