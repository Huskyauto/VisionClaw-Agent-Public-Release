# Tenant-Isolation Audit Report

_Generated 2026-09-19T23:20:33.069Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 303 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **386/689** file(s) audited · chunks 35/64 OK (0 failed, 29 pending)
- Findings: **122** (CRITICAL 31 · HIGH 71 · MEDIUM 20 · LOW 0)
- Severe dispositions: **102 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (31)

- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` lacks a tenant constraint (WHERE only uses id). This can update rows belonging to other tenants.
  - _Fix:_ Require caller to supply tenantId (add parameter) and include it in the WHERE clause, e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))). Always include tenant scope on UPDATE/DELETE.
- **server/agent-eval.ts:221** — UPDATE on tenant-scoped table `agent_evals` uses WHERE id = ${evalId} with no tenant_id constraint. An attacker or bug could update another tenant's eval row.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}. Prefer using Drizzle's where(eq(agentEvals.tenantId, tenantId)) patterns and validate tenantId is provided.
- **server/agentic-engines.ts:686** — autoApplyEligibleInsights executes an UPDATE on tenant-scoped table `ai_insights` that may run without a tenant filter when tenantId is undefined, allowing cross-tenant modification.
  - _Fix:_ Do not run the bulk UPDATE without a tenant scope. Require tenantId or iterate per validated tenant ids and include WHERE tenant_id = ${tenantId} in the UPDATE to prevent accidental cross-tenant writes.
- **server/agentic-features.ts:1021** — UPDATE on tenant-scoped table `outreach_enrollments` uses WHERE id = ${row.id} with no tenant_id constraint. This can modify another tenant's enrollment row.
  - _Fix:_ Include tenant ownership in the WHERE clause (AND tenant_id = ${params.tenantId}) to ensure the update only affects rows belonging to the caller's tenant.
- **server/agentic/approvals.ts:135** — expireStaleApprovals runs a bulk UPDATE on tenant-scoped table `agent_approvals` to mark pending approvals expired but lacks a tenant_id constraint, causing cross-tenant modifications.
  - _Fix:_ Either require a tenantId parameter, run per-tenant with explicit validated tenant ids, or include tenant_id = ${tenantId} in the WHERE clause. Bulk expiry across all tenants must only run in an auditable admin context with explicit safeguards.
- **server/agentic/wake-scheduler.ts:142** — Global UPDATE/SELECT against tenant-scoped table `agent_wake_schedules` lacks any tenant_id constraint. The statement updates rows WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) — the inner SELECT can pick rows from any tenant and the outer UPDATE applies to those ids without restricting tenant, violating tenant isolation invariants.
  - _Fix:_ Make the claim/scan tenant-aware. Either (A) run the wake-claim loop per-tenant (iterate tenants and pass a tenantId) and add AND tenant_id = ${tenantId} to the inner SELECT/outer UPDATE, or (B) include tenant_id in the inner WHERE (e.g. WHERE tenant_id = ${tenantId} AND status = 'pending' ...), so every read/write on agent_wake_schedules is constrained by tenant_id. Ensure any subsequent updates also include tenant_id checks (or use the returned row.tenant_id for per-row verification) and avoid selecting/locking rows across tenants in a single global statement.
- **server/business-tools.ts:95** — INSERT into invoice_items omits tenant_id. The transactional insert of line-items (invoice_items) does not pass the caller's tenant id, violating the invariant that every INSERT into a tenant-scoped table must include tenant_id.
  - _Fix:_ Include tenant_id on the invoice_items INSERT (and any tenant-scoped child tables). For example: INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). If invoice_items is intentionally global, document that explicitly; otherwise make tenant_id NOT NULL and ensure all inserts set it to the caller's tenant.
- **server/ceo-orchestrator.ts:661** — SELECT from the personas table without scoping to the caller tenant. cachedPersonas = await db.select().from(personasTable) reads all personas across tenants (no WHERE tenant_id = plan.tenantId).
  - _Fix:_ Filter persona queries by the plan owner tenant. e.g. use a tenant-scoped query (Drizzle): await db.select().from(personasTable).where(eq(personasTable.tenantId, plan.tenantId)) or otherwise ensure personasTable is constrained to plan.tenantId before caching. Avoid loading cross-tenant persona rows into memory.
- **server/deep-interview.ts:337** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT uses: INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() FROM ... WHERE p.tenant_id = ${state.tenantId} — but project_notes.tenant_id is not populated on insert.
  - _Fix:_ Explicitly persist tenant_id in the INSERT: INSERT INTO project_notes (project_id, tenant_id, content, created_at) SELECT p.id, p.tenant_id, ${briefContent}, NOW() ... OR INSERT ... VALUES (..., ${state.tenantId}, ...). Do not rely on implicit defaults; always set tenant_id from a validated/owned source before inserting.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images is executed without supplying tenant_id. The statement inserts session_id/slide_index/image_data/etc. but does not include tenant scoping; presenter_slide_images is a tenant-scoped artifact table and the INSERT (and its ON CONFLICT upsert) can associate data with a session belonging to any tenant.
  - _Fix:_ Include tenant_id explicitly in the inserted columns and values (e.g. VALUES (${sessionId}, ${idx}, ${imgBuf}, ${imgBuf.length}, 'full', ${tenantId})) and/or validate that sessionId belongs to the current tenant before inserting. If session ownership is required, first query presenter_sessions WHERE id = ${sessionId} AND tenant_id = ${tenantId} (or join) and fail if no row, then perform the INSERT with tenant_id set. Avoid relying on implicit session-only constraints.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} runs without constraining by tenant_id. This allows modifying any presenter's slides by ID across tenants.
  - _Fix:_ Restrict the UPDATE to the current tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or perform an ownership check first (SELECT id, tenant_id FROM presenter_sessions WHERE id = ${sessionId}) and ensure tenant matches the caller before updating. Prefer parameterized placeholders (as already used) and include tenant_id in the WHERE clause to enforce isolation.
- **server/job-queue.ts:159** — claimDueJobs performs an UPDATE (setting status='running', attempts, lease_until) on agent_jobs without constraining by tenant_id. This writes to a tenant-scoped table across all tenants in a single statement.
  - _Fix:_ Restrict claims to an explicit tenant scope (e.g. pass a tenantId or otherwise partition work) or ensure each UPDATE/SELECT uses AND tenant_id = ${tenantId}. If the worker must operate cross-tenant, make tenant-aware claims in a safe per-tenant loop (resolve tenant_id first then UPDATE/claim rows WHERE tenant_id = ${tenantId} AND ...).
- **server/job-queue.ts:195** — reclaimExpiredLeases Step 1 updates agent_jobs to failed_terminal for expired leases without any tenant_id constraint. This mutates tenant-scoped rows globally.
  - _Fix:_ Include tenant_id in the WHERE clause (or iterate per-tenant) when changing job status. Alternatively require caller to provide the tenantId for the reclaim operation so the UPDATE can be scoped (AND tenant_id = ${tenantId}).
- **server/job-queue.ts:209** — reclaimExpiredLeases Step 2 re-queues expired running jobs (UPDATE status='pending', next_run_at=...) without a tenant_id filter, affecting rows across all tenants.
  - _Fix:_ Scope the UPDATE by tenant_id (AND tenant_id = ${tenantId}) or perform requeueing per-tenant. Ensure background reclaim operations never run unscoped updates on tenant-scoped tables.
- **server/job-queue.ts:238** — completeJob updates agent_jobs to 'succeeded' using only id/attempts as fencing guards, with no tenant_id constraint. A stale or crafted call could update another tenant's job row.
  - _Fix:_ Require and check tenant_id (or validate job.tenant_id) before performing the UPDATE, e.g. UPDATE ... WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}. Alternatively resolve the job's tenant first and include it in the WHERE clause for lease-fencing.
- **server/job-queue.ts:298** — failJob's terminal UPDATE (setting failed_terminal) writes to agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} with no tenant_id constraint — possible cross-tenant mutation.
  - _Fix:_ Add a tenant_id guard to the WHERE clause (AND tenant_id = ${tenantId}) or require tenantId in the function and verify it matches the persisted tenant for the job before performing the UPDATE.
- **server/job-queue.ts:318** — failJob's retry-path UPDATE (setting status='pending' and next_run_at) updates agent_jobs without tenant_id scoping.
  - _Fix:_ Scope the UPDATE with tenant_id (AND tenant_id = ${tenantId}) or perform per-tenant requeueing to avoid modifying other tenants' job rows.
- **server/job-queue.ts:335** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) with no tenant_id constraint, allowing cancellation of arbitrary job rows across tenants by id.
  - _Fix:_ Require tenant context or include AND tenant_id = ${tenantId} in the WHERE clause. For admin ops that need to act across tenants, ensure the API path is admin-only and audited; for tenant-facing operations always enforce tenant_id in the UPDATE.
- **server/memory-graph.ts:125** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} — missing tenant_id predicate. This UPDATE can modify a category belonging to a different tenant because the tenant constraint is not included.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. For example: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Alternatively, perform the update inside a tenant-scoped transaction or first verify the category's tenant ownership before updating.
- **server/memory-graph.ts:708** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} — missing tenant_id predicate. This write can affect a memory entry belonging to another tenant because the tenant filter is omitted.
  - _Fix:_ Include tenant scoping on the UPDATE. For example: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Also consider checking the number of affected rows to detect tenant-mismatch and fail closed if zero rows updated.
- **server/memory-intelligence.ts:413** — UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} — missing tenant_id predicate. The UPDATE modifies triples by id only and can update another tenant's row.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g.: UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id} AND tenant_id = ${tenantId}. As defense-in-depth, verify the SELECT that found the contradiction included tenant_id and check affected rows after UPDATE to ensure ownership.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw(lockKey) inside a SQL template for pg_advisory_xact_lock with lockKey derived from caller-provided ledgerKey/tenantId. sql.raw with potentially request-derived data can lead to SQL injection or bypass of parameterization.
  - _Fix:_ Avoid sql.raw for values derived from user/caller input. Use parameterized binding for the lock value (pass a bigint/number as a parameter) or validate/normalize the lock value server-side before use. Example: compute a validated BigInt and use sql`SELECT pg_advisory_xact_lock(${validatedBigInt}::bigint)` (or call the DB API with a parameter placeholder) instead of sql.raw(lockKey). Also validate ledgerKey/tenantId provenance before use.
- **server/plan-executor.ts:741** — SELECT source FROM plans WHERE id=${planId} is executed without constraining tenant_id. This reads a tenant-scoped table row by plan id alone and can leak or allow acting on another tenant's plan before ownership is validated.
  - _Fix:_ Constrain the SELECT by tenant (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId}) or, if you must discover the tenant first, perform a SELECT FOR UPDATE that returns tenant_id then use the same tenant value in subsequent UPDATE/claim in the same transaction. Never infer tenant ownership from an untrusted plan id alone.
- **server/plan-executor.ts:744** — UPDATE plans SET status='executing' WHERE id = ${planId} AND status = 'approved' RETURNING ... performs a state-changing claim without any tenant_id constraint. This can flip the status of a plan belonging to any tenant, enabling cross-tenant modification.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${expectedTenantId}) or perform a transactional flow: SELECT tenant_id FOR UPDATE (validating caller/system rights) then UPDATE ... WHERE id = ${planId} AND tenant_id = ${thatTenantId} AND status = 'approved'. If the executor is acting as an admin/system actor, explicitly use ADMIN_TENANT_ID in the WHERE clause and document intent.
- **server/plan-executor.ts:1279** — claimStaleRecoveryChildCore runs UPDATE plans SET status='executing' WHERE id=${planId} AND source='stale-plan-recovery' AND status='approved' RETURNING ... with no tenant_id filter. This claims a recovery child plan across tenants without tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${expectedTenantId}) or require the caller to pass/derive the tenant and perform the claim inside a transaction using FOR UPDATE semantics so the claim cannot affect another tenant's row.
- **server/process-governor.ts:898** — UPDATE against tenant-scoped table `agent_desks` lacks tenant_id constraint (UPDATE agent_desks SET ... WHERE id = ${desk.id}). This can modify another tenant's row when ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) and ensure tenantId is the same tenant used to select the row. Validate/propagate tenantId into the function if necessary.
- **server/process-governor.ts:1235** — UPDATE against tenant-scoped table `governance_rules` lacks tenant_id constraint (UPDATE governance_rules SET ... WHERE id = ${rule.id}). This may update rules for a different tenant.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}). Use the tenantId available to the caller (pass it into logAction) so the update only affects the intended tenant's row.
- **server/project-brain.ts:135** — SELECT from tenant-scoped table `projects` is missing tenant scoping (SELECT name, status, description FROM projects WHERE id = ${projectId}). updateProjectBrain takes only projectId and may read another tenant's project.
  - _Fix:_ Require and pass an explicit tenantId into updateProjectBrain, and add WHERE tenant_id = ${tenantId} (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Validate tenantId before using it.
- **server/project-brain.ts:332** — Backfill query reads tenant-scoped tables without tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ...). This can enumerate projects/conversations across tenants.
  - _Fix:_ Scope the backfill to a specific tenant (add p.tenant_id = ${tenantId} and c.tenant_id = ${tenantId}) or iterate tenants explicitly and run per-tenant queries. Never run cross-tenant selects without explicit platform-admin authorization and audit.
- **server/project-brain.ts:352** — SELECT from tenant-scoped table `conversations` lacks tenant filter (SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}). This may return conversations from other tenants.
  - _Fix:_ Include tenant scoping (e.g. AND c.tenant_id = ${tenantId}) or join/filter by the project's tenant_id. Ensure conv queries are executed per-tenant only.
- **server/project-brain.ts:360** — SELECT from tenant-scoped table `messages` lacks tenant constraint (SELECT role, content FROM messages WHERE conversation_id = ${conv.id}). This can leak messages from other tenants' conversations.
  - _Fix:_ Add tenant filtering (e.g. AND tenant_id = ${tenantId}) or resolve the conversation's tenant and use it to scope the query. Ensure all reads from messages are constrained by tenant_id.

### HIGH (71)

- **server/ab-optimizer.ts:110** — SELECT from tenant-scoped table `experiments` is executed without constraining to a tenant (no tenant_id in WHERE). This reads experiments for all tenants.
  - _Fix:_ Scope the query to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant explicitly. Avoid wide cross-tenant SELECTs unless run under a tightly-audited admin path.
- **server/agentic-engines.ts:499** — SELECT from tenant-scoped table `ai_insights` (filtering for PENDING_PLAN_MARKER) is executed without a tenant_id constraint, returning rows across tenants.
  - _Fix:_ Scope this durability-sweep SELECT to a specific tenant (add tenant filter) or explicitly iterate validated tenant ids. Avoid ad-hoc cross-tenant reads; if multi-tenant sweep is required, implement explicit, auditable tenant iteration.
- **server/agentic-engines.ts:192** — suppressPendingRejectedPlanDuplicates performs a SELECT on tenant-scoped tables (`plans` / `ai_insights`) with an optional tenantScope. When called without a tenantId it runs cross-tenant, potentially exposing/affecting other tenants.
  - _Fix:_ Make tenantId required for non-admin callers or perform explicit, validated per-tenant iteration. Ensure the SQL always includes tenant_id = ${tenantId} for normal (non-admin) execution paths.
- **server/agentic-features.ts:861** — SELECT checking for an existing outreach enrollment omits tenant_id in WHERE: SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'. This can detect/act on other tenants' enrollments.
  - _Fix:_ Add tenant scoping to the SELECT, e.g. AND tenant_id = ${params.tenantId}, before making decisions based on the result.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' — query against a tenant-scoped table lacks a tenant_id constraint.
  - _Fix:_ If this is intended to run per-tenant, add an explicit tenant_id filter (e.g. WHERE tenant_id = ${tenantId} or use eq(department_budgets.tenantId, tenantId) via Drizzle). If the operation truly must sweep across all tenants, make that explicit and restrict execution to an admin/owner-only context (use ADMIN_TENANT_ID or an authenticated admin process), document the privilege, and ensure only admin codepaths can call it. Avoid unguarded cross-tenant reads from tenant-scoped tables to prevent isolation/ leakage risks.
- **server/agentic/resume-worker.ts:65** — SELECT query reads agent_runs (a tenant-scoped table) without constraining by tenant_id — it returns rows across all tenants.
  - _Fix:_ Restrict the candidate query to a specific tenant (e.g. add AND r.tenant_id = ${tenantId} or use Drizzle eq(agentRuns.tenantId, tenantId)) or otherwise iterate per-tenant explicitly. Ensure every SELECT against tenant-scoped tables includes an application-level tenant_id filter (or perform an explicit, audited admin flow) so tenant isolation is enforced at the app layer.
- **server/auth.ts:198** — validateApiKey() reads the tenant-scoped `api_keys` table using only the keyHash and isRevoked filters — there is no tenant_id constraint in the WHERE clause.
  - _Fix:_ Restrict the lookup or post-check to bind the result to an expected tenant context. If possible, include tenant_id in the WHERE clause (or otherwise validate ownership) before trusting/setting req.tenantId. If a global lookup is required to map credential→tenant, ensure the code documents why and applies additional protections (rate limiting, constant-time checks, minimal returned columns), or consider encoding tenant into the presented credential so a tenant-scoped lookup is possible.
- **server/channel-routing.ts:31** — Query joins channel_routes to personas without scoping personas to a tenant. The JOIN (JOIN personas p ON p.id = cr.persona_id) can surface persona rows belonging to other tenants because there's no tenant filter.
  - _Fix:_ Either make channel_routes tenant-scoped (add tenant_id column and include it in all reads/writes) or, if channel_routes is global, restrict the joined personas to the caller's tenant (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = <callerTenant>). Ensure every read of personas in this context is filtered by tenant_id.
- **server/channel-routing.ts:48** — setChannelRoute(channel, personaId) inserts/updates channel_routes using a caller-supplied personaId without validating that the persona belongs to the caller's tenant. This writes a foreign id derived from caller input into a global mapping with no ownership check.
  - _Fix:_ Validate that the provided personaId belongs to the caller's tenant before inserting/updating. Prefer making channel_routes tenant-scoped (tenant_id column) and enforce WHERE tenant_id = <callerTenant> on writes. If channel_routes must remain global, perform an ownership/authorization check: SELECT tenant_id FROM personas WHERE id = ${personaId} and verify it matches the caller's tenant prior to INSERT/UPDATE.
- **server/ceo-orchestrator.ts:1095** — storage.updateConversation(childConv.id, ..., childConv.tenantId ?? ADMIN_TENANT_ID) falls back to ADMIN_TENANT_ID when childConv.tenantId is missing. This can perform a write under the admin tenant (cross-tenant write) instead of using the plan owner's tenant.
  - _Fix:_ Use the known plan owner tenant (plan.tenantId) for updates, or ensure childConv.tenantId is validated and never missing. Do NOT default to ADMIN_TENANT_ID; if tenant is unknown, fail safely or derive tenant from the plan/request context and enforce it in the update call.
- **server/ceo-orchestrator.ts:1225** — storage.updateConversation(retryConv.id, ..., retryConv.tenantId ?? ADMIN_TENANT_ID) uses ADMIN_TENANT_ID fallback when retryConv.tenantId is missing, risking cross-tenant writes to admin data.
  - _Fix:_ Same as above: avoid defaulting to ADMIN_TENANT_ID. Pass the correct tenant (plan.tenantId) or validate/require retryConv.tenantId. Ensure updateConversation is always called with the plan/request owner's tenant to maintain tenant isolation.
- **server/chat-engine.ts:586** — Raw/template SQL interpolation with request-derived values used in a tenant-scoped query. The code builds scopeFilter and then composes a db.execute(sql`... ${tid} ... ${personaId} ...`) for recent conversations. This mixes caller-provided values directly into raw SQL templates (injection + isolation risk) instead of using a typed Drizzle query builder or explicit eq(...) WHERE clauses.
  - _Fix:_ Avoid composing raw/tagged-template SQL with request-derived values. Use Drizzle query builder APIs (db.select(...).from(...).where(eq(table.tenantId, tenantId), eq(table.personaId, personaId), ...)) or otherwise ensure parameterized WHERE clauses are expressed via eq(table.tenantId, tenantId). If you must keep the hand-written SQL, remove direct interpolation of request-derived values and bind them via parameters, and ensure every tenant-scoped table in the query has an explicit tenant_id = callerTenant constraint.
- **server/chat-engine.ts:660** — Raw/template SQL interpolation with request-derived tenantId used in projects query: db.execute(sql`SELECT ... FROM projects p WHERE ... AND p.tenant_id = ${tid}`). This is a string-interpolated SQL usage containing a caller-derived value.
  - _Fix:_ Replace the raw SQL template with Drizzle query builder calls: e.g. db.select({ id: projects.id, name: projects.name, ... }).from(projects).where(eq(projects.tenantId, tenantId)).orderBy(...).limit(...). This keeps tenant scoping explicit and avoids dangerous string-interpolation patterns.
- **server/chat-engine.ts:3805** — call to markReviewReused(conversationId, toolName) is made without passing tenant context or verifying ownership of the conversationId. The callee may read/write tenant-scoped data using only conversationId and thus perform queries/updates without a WHERE tenant_id = <caller tenant> constraint.
  - _Fix:_ Pass the caller's tenantId into markReviewReused (e.g. markReviewReused(tenantId, conversationId, ...)) or explicitly verify ownership before calling (SELECT conversation WHERE id = ? AND tenant_id = ?). Enforce that all DB accesses in the callee include a tenant_id filter (Drizzle .where(eq(table.tenantId, tenantId))) and that any INSERTs set tenant_id explicitly.
- **server/chat-engine.ts:3926** — call to recordErrorForRetryStormDetection(conversationId, ...) is invoked without tenant context. This helper likely queries conversation-scoped rows or writes diagnostics tied to a conversation; without tenant scoping those operations can cross tenant boundaries.
  - _Fix:_ Include tenantId when calling recordErrorForRetryStormDetection or perform an explicit ownership check on conversationId prior to calling. Update the callee to require tenantId and ensure all SQL reads/writes are constrained by WHERE tenant_id = <tenantId> and that INSERTs explicitly set tenant_id.
- **server/chat-engine.ts:4160** — call to recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) is performed without passing tenantId or verifying that the conversationId belongs to the caller. The callee may access tenant-scoped state without applying tenant filters.
  - _Fix:_ Pass tenantId into recordToolCallForStuckDetection (or verify conversation ownership beforehand). Modify the callee to accept tenantId and ensure it applies tenant_id filters on all reads/updates and includes tenant_id on INSERTs (Drizzle .where(eq(table.tenantId, tenantId))).
- **server/chat-engine.ts:4167** — postDiagnosticReport([stuckPattern]) is called without tenant context after recordToolCallForStuckDetection produced a stuckPattern. If postDiagnosticReport persists or reads diagnostic data tied to conversations, lacking tenant scoping risks cross-tenant visibility or writes.
  - _Fix:_ Ensure diagnostic reporting APIs accept and require tenantId (e.g. postDiagnosticReport(tenantId, patterns)). Before calling, associate the pattern with the verified tenant. In the callee, enforce WHERE tenant_id = <tenantId> on read/update queries and include tenant_id on any INSERTs.
- **server/cmmc-assessments.ts:150** — getCmmcAccessByToken performs a SELECT joining cmmc_assessment_invitations and cmmc_assessments filtered only by the invite token hash/expiry but does not constrain the query to a caller tenant (no tenant_id condition). This reads tenant-scoped tables without an application-level tenant filter, violating the tenant-isolation invariant.
  - _Fix:_ Add an explicit tenant constraint to the query or require the caller to provide a tenantId and include eq(...tenantId, callerTenant) in the WHERE clause. If the API is intended to be token-only, store/verify tokens so they are namespaced to a tenant (e.g. include tenantId in the token binding) and enforce eq(cmmc_assessment_invitations.tenant_id, <expectedTenant>) in the query. Avoid returning rows from tenant-scoped tables based solely on an unhashed or global lookup without verifying tenant ownership.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' issued without constraining tenant_id (unbounded WHERE id = ${row.id}). This unscoped UPDATE can modify a row belonging to any tenant and breaks tenant isolation.
  - _Fix:_ Do not execute updates against tenant-scoped tables without the tenant filter. Either remove the unscoped unclaim or constrain it to the owning tenant. For example, validate the tenant and run: await db.execute(sql`UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}`); or (preferably) skip/unclaim only when a valid tenantId > 0 is present. Also include additional guards such as AND draft_status = 'draft_pending' to avoid accidental state changes.
- **server/data-protection.ts:149** — SELECT from tenant-scoped table `conversations` without tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows for all tenants.
  - _Fix:_ Restrict the initial scan by tenant (e.g. iterate tenants and include WHERE tenant_id = ${tenantId}) or accept a tenantId parameter and add AND tenant_id = ${tenantId}. Alternatively perform per-tenant transactions (withTenantTx) so subsequent deletes are scoped to the same tenant context.
- **server/data-protection.ts:359** — SELECT from tenant-scoped table `project_notes` uses only project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id}) with no tenant_id predicate. project_notes is tenant-scoped but not constrained here.
  - _Fix:_ Add an explicit tenant predicate: add AND tenant_id = ${tenantId} (or AND tenant_id = ${proj.tenant_id} if available). Never rely on referencing project_id alone — include tenant_id on reads/writes for tenant-scoped tables.
- **server/data-protection.ts:370** — SELECT from tenant-scoped table `project_files` uses only project_id (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id}) with no tenant_id predicate.
  - _Fix:_ Add an explicit tenant predicate: include AND tenant_id = ${tenantId} (or AND tenant_id = ${proj.tenant_id}) to constrain reads to the caller's tenant.
- **server/doc-collections.ts:166** — DELETE from tenant-scoped table `doc_chunks` is missing an explicit tenant_id constraint in the WHERE clause (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}).
  - _Fix:_ Constrain the DELETE to the caller's tenant in the SQL WHERE clause. For example: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Do not rely on prior application checks; include tenant_id in the statement itself.
- **server/doc-collections.ts:294** — UPDATE on tenant-scoped table `doc_chunks` lacks a tenant_id constraint (UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id}).
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause to prevent cross-tenant writes. For example: UPDATE doc_chunks SET embedding = ${...}::jsonb WHERE id = ${row.id} AND tenant_id = ${tenantId}. Ensure the function has access to the tenantId and uses it in the SQL template.
- **server/email.ts:371** — SELECT from tenant-scoped table `inbox_messages` without tenant_id constraint (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This can read rows across tenants and allow cross-tenant detection/leakage.
  - _Fix:_ Include the tenant filter (or another tenant-scoped discriminator) in the WHERE clause. e.g. SELECT id FROM inbox_messages WHERE message_id = ${msgId} AND tenant_id = ${tenantId} LIMIT 1 — or scope by inbox_id if appropriate after resolving the tenant for the message before dedupe checks.
- **server/email.ts:416** — SELECT from tenant-scoped table `inbox_messages` without tenant_id constraint in backfillEmptyBodies (SELECT id, message_id FROM inbox_messages ...). This can enumerate inbox messages across all tenants.
  - _Fix:_ Require and pass an explicit tenantId into backfillEmptyBodies or otherwise restrict the query: SELECT id, message_id FROM inbox_messages WHERE tenant_id = ${tenantId} AND (body_text IS NULL OR ...) ...; process per-tenant to avoid cross-tenant reads.
- **server/email.ts:429** — UPDATE to tenant-scoped table `inbox_messages` without tenant_id constraint (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). This writes across tenants if id collisions or DB row ids are not checked with tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE inbox_messages SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Prefer passing tenantId into backfill flow and validate ownership before updating.
- **server/embeddings.ts:809** — UPDATE via dynamic table path without tenant constraint: await db.execute(sql`UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}`) in storeEmbeddingVec default branch. For tenant-scoped tables (e.g. memory_entries) this performs an unconstrained write.
  - _Fix:_ Do not perform updates without an explicit tenantId. Require options.tenantId for tenant-scoped tables and include AND tenant_id = ${tenantId} in the WHERE. Remove the fallback branch that issues an unconstrained UPDATE, or validate the caller's authority and pass tenantId explicitly.
- **server/embeddings.ts:814** — SELECT from tenant-scoped table `agent_knowledge` without tenant filter in backfillMissingKnowledgeEmbeddings (SELECT id, tenant_id, title, content FROM agent_knowledge ...). This reads across tenants.
  - _Fix:_ Run backfill per-tenant (accept a tenantId parameter) or otherwise include tenant_id = ${tenantId} in the WHERE. If operator-wide maintenance is intended, make it explicit and gated, but still audit/record that it reads all tenants.
- **server/embeddings.ts:845** — SELECT from tenant-scoped table `memory_entries` without tenant constraint in backfillEmbeddingVecs (SELECT id, embedding FROM memory_entries ...). This can read rows across tenants.
  - _Fix:_ Perform backfill on a per-tenant basis (accept tenantId) or include tenant_id = ${tenantId} in the WHERE. Never run tenant-scoped reads without an explicit, validated tenant context.
- **server/embeddings.ts:855** — UPDATE to tenant-scoped table `memory_entries` without tenant constraint in backfillEmbeddingVecs (UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}). This writes embeddings without verifying tenant ownership of the row.
  - _Fix:_ Include tenant scoping in the UPDATE: UPDATE memory_entries SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Change the backfill to operate per-tenant or validate row. Avoid unconstrained writes to tenant tables.
- **server/embeddings.ts:861** — SELECT from tenant-scoped table `agent_knowledge` without tenant constraint in backfillEmbeddingVecs (SELECT id, embedding FROM agent_knowledge ...). This reads across tenants.
  - _Fix:_ Same as above: perform per-tenant backfill (require tenantId) or add AND tenant_id = ${tenantId} to the query so retrieval is tenant-scoped.
- **server/embeddings.ts:872** — UPDATE to tenant-scoped table `agent_knowledge` without tenant constraint in backfillEmbeddingVecs (UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}). This writes across tenants without ownership validation.
  - _Fix:_ Include tenant scoping on the UPDATE: UPDATE agent_knowledge SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Change maintenance jobs to iterate per-tenant or otherwise require explicit tenant context before mutating tenant-scoped rows.
- **server/felix-brain.ts:23** — Unscoped SELECT from tenant-scoped table `agent_runs` — rehydrateTaskStateOnBoot queries agent_runs without filtering by tenant_id, which reads in-flight runs for all tenants.
  - _Fix:_ Restrict the query to the caller/hosted tenant(s). Either add a WHERE tenant_id = ${tenantId} clause (and run once per intended tenant) or otherwise enumerate/validate the set of tenants this process is allowed to touch before selecting rows. Do NOT scan agent_runs across all tenants at boot if the process should only operate on a single tenant or a limited set — this leaks cross-tenant data. Ensure any subsequent processing also enforces tenant checks.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) and lacks a tenant_id constraint — this allows a write across tenants if an id from another tenant is supplied/found.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE clause. e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Ensure any UPDATE/DELETE against tenant-scoped tables always includes the tenantId filter.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) inside the crawl loop and omits tenant scoping — this can mutate another tenant's row.
  - _Fix:_ Restrict the UPDATE to the caller's tenant. Change the WHERE to include tenant filter: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Apply same pattern to any in-loop updates/inserts on tenant-scoped tables.
- **server/google-drive.ts:1664** — INSERT INTO project_files is executed without passing/setting tenant_id. If project_files is tenant-scoped (tenant_id NOT NULL), this INSERT omits tenant ownership and can create records not tied to the caller's tenant.
  - _Fix:_ Include the tenant_id column in the INSERT and set it to the validated tenant (e.g. resolvedTenantId), and/or derive tenant_id from the project after verifying ownership. Prefer fail-closed: verify project belongs to caller's tenant before inserting and then INSERT with tenant_id = ${resolvedTenantId}.
- **server/health-audit.ts:314** — SELECT FROM code_proposals (SELECT id, title, status, created_at FROM code_proposals ...) is executed with no tenant_id filter. code_proposals appears to be tenant-scoped, so this reads proposals across all tenants.
  - _Fix:_ Scope the query to a tenant (add WHERE tenant_id = ${tenantId}) or require this audit function to run under an explicit admin context and document/guard that fact. If the intent is per-tenant auditing, accept a tenantId parameter and use it in the WHERE clause. If the intent is global admin maintenance, require an explicit admin-only check before running and annotate the code to make that decision explicit.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates proposals without constraining by tenant_id — this can flip status for rows across tenants that share ids.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or resolve ids by selecting only proposals belonging to the intended tenant and updating those. If running as a system-wide admin operation is intended, gate the code behind an explicit admin authorization check and make that explicit in the code/comments.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE enabled = true AND last_run_at IS NULL ... ) is executed without tenant_id filtering. heartbeat_tasks are likely tenant-scoped, so this returns tasks across tenants.
  - _Fix:_ Scope the query to tenant (add AND tenant_id = ${tenantId} or accept tenantId param) or require/verify an explicit system-admin context before running a global sweep. If archiving/updating multiple tenants is desired, iterate tenants explicitly under an authorized admin flow rather than issuing unscoped queries.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id without limiting to a tenant. This can modify tasks across tenants that share ids.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or select and verify tenant ownership of the ids before performing the update. If the operation is intended to be global, require explicit admin authorization and document/guard the behavior.
- **server/heartbeat.ts:2682** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executes without a tenant_id constraint. Even though newTask was just created, updating by id without tenant scoping can allow unintended cross-tenant modification or make it easier to misuse/attack.
  - _Fix:_ Scope the UPDATE to the task's tenant or use the storage API that enforces tenant ownership. For example: await db.execute(sql`UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} AND tenant_id = ${tenantId}`) or call storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }, tenantId). Also consider assertTenantContext(tenantId) before the update.
- **server/heartbeat.ts:3155** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without filtering by tenant_id. This updates approval_status on a tenant-scoped table without validating tenant ownership.
  - _Fix:_ Add an explicit tenant filter or use the tenant-aware storage helper. e.g. await db.execute(sql`UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} AND tenant_id = ${tenantId}`) or use storage.updateHeartbeatTask(newTask.id, { approvalStatus: 'pending' }, tenantId) after asserting tenant context.
- **server/job-queue.ts:281** — failJob reads max_attempts by id (SELECT max_attempts FROM agent_jobs WHERE id = ${id}) without scoping by tenant_id before deciding retry/terminal behavior.
  - _Fix:_ Include tenant_id in the SELECT or require the caller to pass tenantId; validate the resolved tenant matches the worker context before proceeding. This prevents cross-tenant information disclosure or logic applied to another tenant's job.
- **server/lib/action-ledger.ts:268** — listUnknownAttemptsAllTenants performs a SELECT on tenant-scoped table `action_attempts` without any tenant_id constraint (SELECT ... FROM action_attempts WHERE state = 'unknown'). This reads rows across tenants without an application-level tenant filter.
  - _Fix:_ If cross-tenant reads are NOT intended, add a tenant_id predicate (e.g. WHERE tenant_id = ${tenantId} AND ...). If this helper is intentionally cross-tenant (reconciler), make that explicit and restrict use to an admin/maintenance context, or return only tenant-scoped rows but include tenant_id in the projection and ensure all later writes use that tenant_id in their WHERE clauses. Prefer the safer pattern: SELECT id, tenant_id, ... FROM action_attempts WHERE state='unknown' LIMIT ... then perform per-row guarded updates using WHERE id = ${id} AND tenant_id = ${tenantId}.
- **server/lib/action-ledger.ts:304** — sweepStaleAttempts issues an UPDATE on tenant-scoped table `action_attempts` without constraining by tenant_id (UPDATE action_attempts SET state='unknown' WHERE id IN (SELECT id FROM action_attempts WHERE state IN (...) ...)). This will update rows across all tenants in the matched id set.
  - _Fix:_ Restrict updates by tenant. For example, run the sweep per-tenant or rewrite the statement to include tenant_id in the outer WHERE (e.g. UPDATE action_attempts SET ... WHERE id IN (SELECT id FROM action_attempts WHERE tenant_id = ${tenantId} AND state IN (...) LIMIT ${capped})). Alternatively, SELECT ids with their tenant_id and perform guarded UPDATEs using WHERE id = ${id} AND tenant_id = ${tenantId} to ensure writes remain tenant-scoped.
- **server/lib/agent-trace.ts:125** — persistSpanClose issues an UPDATE on tenant-scoped table `agent_trace_spans` using only span_id in the WHERE clause (WHERE span_id = ${spanId}) with no tenant_id constraint. Close operations are therefore not scoped to the tenant that opened the span and can modify rows across tenants.
  - _Fix:_ Include tenant scoping on the UPDATE. Options: (a) persist the tenantId when opening the span (e.g. map spanId→tenantId) and pass it into persistSpanClose, then use WHERE span_id = ${spanId} AND tenant_id = ${tenantId}; or (b) store tenant_id alongside an in-memory record for the open span and use it when closing. Never perform the close UPDATE without an explicit tenant_id predicate on tenant-scoped tables.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT on tenant-scoped table `projects` without constraining to a caller tenant (reads across tenants). The query `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id` does not include a WHERE tenant_id = <caller tenant> filter.
  - _Fix:_ Restrict the aggregation to only the tenant(s) you intend to inspect. Options: (a) run this check per-tenant and add WHERE tenant_id = ${tenantId}; (b) if you need counts for an explicit set of snapshot tenants, pass that tenant-id list and use WHERE tenant_id = ANY(${tenantIdArray}) (parameterized); or (c) perform per-tenant queries inside the loop. Never run wide SELECTs across tenant-scoped tables in production.
- **server/lib/inbox-ingest.ts:536** — Query joins inbox_messages (alias m) but does not constrain the joined inbox_messages rows by tenant_id. The SELECT reads fields from inbox_messages (m.from_address, m.subject, m.message_id) while only inbox_classifications (c) is explicitly tenant-scoped in the WHERE clause (c.tenant_id = ${tenantId}). This can allow cross-tenant leakage if ids collide or foreign keys are not enforced.
  - _Fix:_ Add an explicit tenant filter for the joined table, e.g. include AND m.tenant_id = ${tenantId} (or add the tenant check into the JOIN: JOIN inbox_messages m ON m.id = c.inbox_message_id AND m.tenant_id = ${tenantId}). Ensure every referenced tenant-scoped table in the query is constrained by the caller's tenantId.
- **server/lib/mention-injection.ts:120** — Query LEFT JOINs personas (alias p) but does not constrain persona rows by tenant_id. The SELECT reads p.name AS from_persona_name while only channel_messages (cm) is filtered by cm.tenant_id = ${params.tenantId}. Without an explicit tenant filter on personas, a persona row from another tenant could be returned for the same id.
  - _Fix:_ Constrain the joined personas table to the tenant as well. For example, change the JOIN to: LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId} (or add an explicit WHERE p.tenant_id = ${params.tenantId}). Ensure every tenant-scoped table referenced in queries is filtered by the caller's tenantId.
- **server/lib/outcome-promotion.ts:158** — Global aggregation reads deliverable_grade_events without tenant_id filter (SELECT tenant_id, format, count(*) FROM deliverable_grade_events ...). This is a cross-tenant read on a tenant-scoped table and violates the invariant that all tenant-scoped reads must be constrained by tenant_id.
  - _Fix:_ Avoid scanning tenant-scoped tables across tenants in a single unbounded query. Either (a) restrict the query to a specific tenant by adding WHERE tenant_id = ${tenantId} and run the scan per-tenant, or (b) if a global admin aggregation is required, make the admin intent explicit and enforce strict admin-only authorization and careful minimization/aggregation of returned data. Prefer per-tenant scans (iterate tenants or accept tenantId parameter) so every SELECT includes WHERE tenant_id = ${tenantId}.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table 'scheduled_posts' performed without an explicit tenant_id filter (CTE 'due' selects rows across all tenants and the UPDATE applies without tenant_id predicate).
  - _Fix:_ Constrain the selection and update to the explicit tenant_id(s). For example, include tenant_id in the CTE (SELECT id, tenant_id FROM scheduled_posts WHERE tenant_id = ${someTenant}) and then UPDATE ... WHERE sp.id = due.id AND sp.tenant_id = due.tenant_id. If the runner must operate across tenants, iterate per-tenant or return tenant_id in the CTE and use it in the UPDATE so every write includes an explicit tenant_id predicate.
- **server/lib/system-probes.ts:163** — SELECT from tenant-scoped table (messagesTable) without a tenant_id constraint — db.select(...).from(messagesTable) lacks WHERE tenant_id = ...
  - _Fix:_ Constrain this count to an explicit tenant (e.g. add .where(eq(messagesTable.tenantId, tenantId)) or call a storage helper that accepts an explicit tenant). If this cross-tenant aggregate is intended only for an admin/system probe, ensure it only runs in an authorized admin context and is explicitly audited/approved rather than relying on an unconstrained raw query.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table tenantVoiceProfiles lacks an explicit tenant_id constraint — .where(eq(tenantVoiceProfiles.id, existing.id)) does not assert the row belongs to the caller's tenant
  - _Fix:_ Add an ownership constraint to the WHERE clause so the write is constrained to the caller's tenant, e.g. .where(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)). Alternatively, perform the update via a statement that explicitly includes tenant_id in the predicate to prevent cross-tenant updates.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not set tenant_id. Although ownership is checked via assertProjectInTenant, the INSERT omits the tenant_id column/value, violating the invariant that every INSERT into tenant-scoped tables must pass tenantId explicitly.
  - _Fix:_ Include the tenant_id column and pass the verified tenant value in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ..., uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...)). Keep the assertProjectInTenant check (or equivalent) to validate ownership, and use parameterized Drizzle values (${options.tenantId}) rather than string interpolation.
- **server/mpeg-engine.ts:1444** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The parallel-produce path also checks ownership via assertProjectInTenant but the INSERT omits tenant_id, violating the requirement that INSERTs include tenant context.
  - _Fix:_ Add tenant_id to the INSERT columns and supply the caller's validated tenant id (e.g. INSERT INTO project_files (tenant_id, project_id, ..., uploaded_by) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure assertProjectInTenant validated ownership before inserting and use Drizzle parameter interpolation for tenantId.
- **server/oauth-subscriptions.ts:906** — Global read from tenant-scoped table `oauth_subscriptions` without tenant filter (SELECT ... WHERE is_active = TRUE). This reads subscriptions for all tenants.
  - _Fix:_ Restrict the query to a single tenant or iterate per-tenant. Add a WHERE tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(oauth_subscriptions.tenant_id, tenantId))). If the intent is a controlled cross-tenant operation, require an explicit admin/ops flow and document/authorize it.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' executes without a tenant_id constraint — exposes data across tenants.
  - _Fix:_ Add tenant scoping: include AND tenant_id = ${tenantId} or otherwise ensure only permitted tenant rows are returned. If the query intends to find tenants for an admin auto-reconnect operation, require an explicit admin-only path and audit logging.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh reads oauth_subscriptions with WHERE is_active = TRUE and no tenant_id constraint — enumerates active subscriptions across all tenants.
  - _Fix:_ Either accept a tenantId parameter and filter by tenant_id, or query a safe tenant list and process per-tenant. Never run broad tenant-scoped SELECTs without explicit tenant scoping or an audited admin workflow.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions queries oauth_subscriptions WHERE is_active = TRUE with no tenant filter — performs global reads across tenants.
  - _Fix:_ Avoid unscoped reads. Require an explicit admin flow or iterate per-tenant using an authorized tenant list. Add WHERE tenant_id = ${tenantId} for tenant-scoped processing.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} lacks a tenant_id filter — reads a persona record without verifying it belongs to the caller's tenant.
  - _Fix:_ Constrain persona reads to the caller's tenant: add AND tenant_id = ${tenantId} (or use Drizzle .where(eq(personas.tenant_id, tenantId))). Validate ownership before exporting persona data.
- **server/persona-export.ts:39** — Query on `skills` uses WHERE persona_id = ${personaId} OR persona_id IS NULL with NO tenant filter. This can return tenant-specific skills belonging to other tenants.
  - _Fix:_ If skills are tenant-scoped, include tenant filtering. For example: WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR (persona_id IS NULL). Explicitly pass and check tenant_id to avoid cross-tenant leakage. If skills is intentionally global, document that and ensure persona-specific rows still include tenant scoping.
- **server/pace-control.ts:85** — COUNT(*) FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter} executes without any tenant_id constraint — reads/aggregates heartbeat logs across all tenants.
  - _Fix:_ Require a tenantId argument for pace checks and add WHERE tenant_id = ${tenantId} to the query (or otherwise partition by tenant). If the aggregation must be global, restrict it to an admin-only path with explicit authorization and logging.
- **server/pace-control.ts:140** — Summary query against heartbeat_logs (COUNT / lane breakdown) lacks tenant scoping and aggregates across all tenants.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = ${tenantId}) or require this endpoint be admin-only and explicitly documented/audited. Per-tenant dashboards must include tenant_id in the WHERE clause.
- **server/pace-control.ts:147** — Persona-level aggregation SELECT ... FROM heartbeat_logs WHERE created_at > ... GROUP BY persona_name runs without tenant filter — exposes per-persona activity across tenants.
  - _Fix:_ Include WHERE tenant_id = ${tenantId} to confine results to a single tenant, or require an explicit admin-level request that documents intent and authorization.
- **server/plan-executor.ts:1404** — resumeStuckPlans selects staleCandidates from plans (status='executing' ... updated_at < ...) without any tenant_id constraint: SELECT id, tenant_id, status, plan_json, execution_log, version FROM plans WHERE ... LIMIT 50. This reads tenant-scoped rows across all tenants and exposes plan_json/execution_log data.
  - _Fix:_ If the boot-recovery worker must scan across tenants, make the intent explicit and run the scan as an admin-only operation with documented audit controls. Otherwise restrict the query to a specific tenant_id or paginate per-tenant. At minimum, avoid returning full plan_json/execution_log to unprivileged contexts and perform any state-changing operations only with a subsequent tenant-validated transactional claim (SELECT ... FOR UPDATE with tenant_id) before updates.
- **server/plan-executor.ts:1540** — Transient-recovery candidate query (SELECT id, tenant_id, status, ceo_decision, ceo_decided_at, plan_json, execution_log FROM plans WHERE status='failed' AND ceo_decision='approved' ...) omits tenant_id. It reads failed plans and their plan_json/execution_log across tenants.
  - _Fix:_ Either scope the query to a tenant_id or run it only in an explicitly authorized admin context. When scanning multiple tenants, ensure subsequent updates are guarded by tenant-scoped WHERE clauses and that sensitive fields (plan_json/execution_log) are not exposed to unprivileged code paths.
- **server/plan-executor.ts:1599** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 is used to fetch pending plans without restricting tenant_id. This schedules executePlan(r.id) for plans across all tenants without an explicit tenant guard at selection time.
  - _Fix:_ If the executor is intended to process plans across tenants, make that explicit and ensure executePlan's claim step is tenant-scoped (see above). Otherwise restrict this query by tenant_id or iterate per-tenant to avoid accidental cross-tenant dispatch. Ensure every UPDATE/DELETE on plans includes AND tenant_id = ${tenantId}.
- **server/plan-executor.ts:1710** — reconcileStaleRecoveryChildren selects candidates from plans (WHERE source = 'stale-plan-recovery' AND status = 'executing' AND updated_at < ...) without tenant_id. This enumerates in-flight recovery children across tenants.
  - _Fix:_ Restrict the initial scan to an authorized admin context or to a specific tenant_id. For each candidate, all mutating actions must be performed inside a transaction and include tenant_id in the WHERE/FOR UPDATE clauses (which the code later does). Prefer selecting WITH tenant_id in the outer query or document an explicit admin-only sweep and add audit checks.
- **server/recurring-messages.ts:307** — UPDATE on tenant-scoped table 'agent_knowledge' lacks a tenant_id constraint. The runner queries rows across all tenants and then issues UPDATE agent_knowledge SET content = ... WHERE id = ${r.id} without AND tenant_id = ${r.tenant_id}, allowing the update to affect rows outside the intended tenant context.
  - _Fix:_ Add the tenant filter to the UPDATE so it is always tenant-scoped, e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}. Prefer passing an explicit tenantId variable rather than relying only on the previously-read row id. This ensures the write cannot cross-tenant even if ids are manipulated or code paths change.
- **server/reference-learner.ts:296** — UPDATE on tenant-scoped table 'memory_entries' omits tenant scoping. The code updates memory_entries SET ... WHERE id = ${storedId} without also constraining tenant_id = ${input.tenantId}, enabling cross-tenant modification if ids are misused.
  - _Fix:_ Constrain the UPDATE by tenant as well: UPDATE memory_entries SET ... WHERE id = ${storedId} AND tenant_id = ${input.tenantId}. Always include tenant_id in WHERE clauses for updates/deletes on tenant-scoped tables and/or verify ownership before mutating.

### MEDIUM (20)

- **server/agentic-engines.ts:482** — SELECT DISTINCT tenant_id FROM `heartbeat_tasks` scans tenant-scoped table without scoping to a caller tenant. It performs a cross-tenant enumeration.
  - _Fix:_ If this is an administrative multi-tenant sweep, ensure it runs in an audited admin context. Otherwise restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) or pass an explicit safe tenant list into the function.
- **server/auth.ts:338** — deleteSession() executes DELETE FROM auth_sessions WHERE token = ... without constraining by tenant_id. This mutates a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ If possible require or derive the tenantId and include AND tenant_id = ${tenantId} in the DELETE. If the token-alone key is authoritative, ensure the flow that calls deleteSession has already validated the session/tenant ownership; otherwise change the API to accept tenantId or look up-and-verify tenant before delete.
- **server/auth.ts:1242** — clearExpiredSessions() runs a global DELETE FROM auth_sessions WHERE expires_at < ${now} with no tenant_id constraint, modifying rows for all tenants.
  - _Fix:_ Run expiry cleanup per-tenant (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ...) or gate this global maintenance to a platform-admin/maintenance context with explicit justification. If global deletion is intentional, document it clearly and ensure only a trusted/system context runs it.
- **server/auth.ts:1136** — cleanExpiredResetTokens() issues DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} with no tenant_id constraint, affecting reset tokens across all tenants.
  - _Fix:_ Either constrain cleanup to a tenant context (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ...) or ensure this job runs only as a trusted maintenance operation. Prefer per-tenant cleanup or otherwise explicitly document/authorize the global operation.
- **server/auth.ts:1339** — loadSessionsFromDb() executes DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' without any tenant_id constraint, removing session rows across tenants.
  - _Fix:_ If this is a one-time migration/maintenance step, ensure it runs in a trusted platform-admin context and is documented. Otherwise, perform tenant-scoped cleanup or add an explicit platform-admin guard and audit the action.
- **server/auto-project.ts:135** — Query reads messages via WHERE conversation_id = ${conversationId} but does not include a tenant_id constraint on the messages table. This is a SELECT against a tenant-scoped table with no tenant filter.
  - _Fix:_ Add an explicit tenant filter to the messages query (e.g. AND tenant_id = ${tenantId}) or otherwise prove/record that conversation ownership was validated immediately prior to this query. Prefer the explicit WHERE tenant_id = ${tenantId} to avoid relying on transitive trust.
- **server/auto-transcript.ts:46** — autoSaveProjectTranscript() selects messages with WHERE conversation_id = ${conversationId} but does not constrain the messages query by tenant_id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the messages SELECT, or otherwise verify the conversation->messages relationship under the same tenant within the same transaction before reading. Explicit tenant-scoped WHERE clauses are preferred for isolation.
- **server/auto-asset-capture.ts:207** — backfillProjectAssets() reads messages via WHERE conversation_id = ${c.id} AND role = 'assistant' without including tenant_id in the WHERE clause on the messages table.
  - _Fix:_ Constrain the SELECT to the tenant (e.g. AND tenant_id = ${c.tenant_id}) or ensure the conversation id was validated to belong to that tenant within the same trust boundary. Prefer explicit tenant WHERE filters on tenant-scoped tables.
- **server/business-tools.ts:234** — Handlers addCustomer / updateCustomer accept an assigned_to foreign id from caller input and persist it without verifying that the referenced user (or assignee) belongs to the caller's tenant. Writing foreign IDs without ownership checks can break tenant isolation when there is no DB-level tenant enforcement.
  - _Fix:_ Validate foreign IDs before storing them. For assigned_to, SELECT the referenced user row and confirm tenant_id = caller tenant (e.g. SELECT id FROM users WHERE id = ${params.assigned_to} AND tenant_id = ${tid} LIMIT 1) and reject if not found. Alternatively restrict assigned_to to an allowlist or require tenant-scoped user ids only settable by privileged roles.
- **server/channel-routing.ts:63** — getPersonaForChannel returns persona_id from a global channel_routes table without any tenant context. Consumers may use that persona_id to act on behalf of a tenant without verifying ownership.
  - _Fix:_ Either return persona ids scoped to the caller's tenant, or add tenant_id to channel_routes and include the caller tenant when selecting. At minimum, validate that the returned persona_id belongs to the caller's tenant before it is used.
- **server/channels/kernel.ts:176** — dispatchInbound delegates conversation resolution to adapter.resolveConversation and then calls processMessage(conversationId, ...) using turn.tenantId, but does not verify that the resolved conversationId actually belongs to turn.tenantId. A malicious/buggy adapter could return a conversation id owned by a different tenant and cause cross-tenant access.
  - _Fix:_ After resolveConversation returns a conversationId, verify ownership: query the conversation row and assert its tenant_id === turn.tenantId (or otherwise validate via storage API) before calling processMessage. Alternatively, require resolveConversation to return both conversationId and tenantId and assert they match.
- **server/chat-engine.ts:1624** — buildMemorySection signature uses a default tenantId = 1. A default admin/tenant id risks silent fail-open behavior if callers accidentally omit tenantId, potentially causing cross-tenant data exposure or writes under the wrong tenant.
  - _Fix:_ Make tenantId a required parameter (no admin default). Enforce a runtime fail-closed guard: throw if tenantId is missing or not a positive integer. Ensure all callers supply the resolved caller tenant explicitly. If a default is required for internal tests, make it guarded by a clearly named test-only path, not the production API signature.
- **server/data-protection.ts:403** — COUNT(*) query against `messages` uses only conversation_id (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) without tenant_id constraint. Even if conv was selected earlier with a tenant filter, this query lacks the required explicit tenant predicate.
  - _Fix:_ Scope the count to the tenant as well: SELECT COUNT(*)::int FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id /* or tenantId */}. Always include tenant_id on queries against tenant-scoped tables.
- **server/google-drive.ts:1473** — Code selects the project row by projectId without first validating the caller's tenant ownership (projects row returned even if it belongs to a different tenant). While the code later uses the project's tenant to route uploads, it does not enforce that the caller belongs to that tenant, potentially allowing cross-tenant uploads when a caller supplies only projectId.
  - _Fix:_ Fail-closed: require and verify the caller's tenant context before resolving project folders. If a caller-supplied tenantId is available, compare it to proj.tenantId and reject mismatches; otherwise require the caller be running in the tenant context (or explicitly disallow routing into arbitrary project folders). Always validate ownership before performing upload/drive routing.
- **server/heartbeat-startup.ts:81** — UPDATE on tenant-scoped table heartbeat_tasks uses WHERE id = ${t.id} with NO tenant_id constraint. This mutates a tenant-scoped row without constraining by the row's tenant.
  - _Fix:_ Include the tenant filter or use the storage API that enforces tenant context. e.g. use storage.updateHeartbeatTask(t.id, { model: 'gpt-5-mini' }, t.tenantId) or add AND tenant_id = ${t.tenantId} to the WHERE clause (and/or assertTenantContext(t.tenantId) before the update).
- **server/heartbeat-startup.ts:85** — UPDATE on tenant-scoped table heartbeat_tasks uses WHERE id = ${t.id} with NO tenant_id constraint. This changes another tenant's task row if misused.
  - _Fix:_ Constrain the UPDATE to the task's tenant or use the storage wrapper. e.g. storage.updateHeartbeatTask(t.id, { model: 'gemini-2.5-flash' }, t.tenantId) or add AND tenant_id = ${t.tenantId} to the WHERE clause after asserting the tenant context.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} without scoping. While this helper is intended for admin use, it reads a tenant-scoped table unscoped and could be misused from non-admin paths.
  - _Fix:_ Keep this helper admin-only and strongly document/enforce access control, or require an admin-scoped caller and/or parameterize by expected tenant. If used from tenant-facing code, change to SELECT tenant_id WHERE id=${id} AND tenant_id=${expectedTenant} to avoid cross-tenant lookup.
- **server/lib/restraint-trust.ts:90** — Use of sql.raw() with a runtime-derived column identifier (the 'column' variable derived from caller-provided 'outcome'). Even though the code validates the outcome against a whitelist, using sql.raw() to inject an identifier into SQL carries injection and isolation risks and bypasses Drizzle's safe identifier handling.
  - _Fix:_ Avoid injecting identifiers with sql.raw(). Instead, branch on the validated outcome and run a parameterized UPDATE that directly references the concrete column name (e.g. if outcome === 'action_ok' then UPDATE ... SET action_alpha = action_alpha + ${delta} ...). Alternatively use Drizzle's programmatic update helpers or a safe identifier API rather than sql.raw to ensure the identifier is not introduced into the SQL text at runtime.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table 'agent_runs' performed with no tenant_id constraint (scans running rows across all tenants).
  - _Fix:_ If this tool is intended to be tenant-scoped, add AND tenant_id = ${tenantId} to the WHERE clause (or run per-tenant). If it is intentionally cross-tenant, make that explicit and ensure callers are privileged and audited; otherwise avoid global scans that can leak cross-tenant state.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table 'mind_tickets' performed with no tenant_id constraint (scans in_progress rows across all tenants).
  - _Fix:_ As above, either scope this audit by tenant (add tenant_id = ${tenantId}) or, if a cross-tenant audit is required, ensure the operation runs under an explicit admin context with appropriate access controls and auditing. Avoid unconstrained queries on tenant-scoped tables.
