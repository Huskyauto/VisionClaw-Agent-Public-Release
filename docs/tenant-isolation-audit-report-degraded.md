# Tenant-Isolation Audit Report

_Generated 2026-09-19T18:29:11.201Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 497 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **192/689** file(s) audited · chunks 20/64 OK (0 failed, 44 pending)
- Findings: **110** (CRITICAL 27 · HIGH 70 · MEDIUM 12 · LOW 1)
- Severe dispositions: **97 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (27)

- **server/agent-activity.ts:118** — completeActivity performs db.update(agentActivity).set(...) without restricting the UPDATE to the owning tenant (where only eq(agentActivity.id, activityId) is used). This allows an attacker or bug to update another tenant's activity row by id.
  - _Fix:_ Require and accept tenantId in completeActivity, and include it in the WHERE clause (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))). If the caller cannot provide tenantId, first fetch the row and verify its tenant_id equals the caller's tenant before performing the update.
- **server/agentic-features.ts:1020** — classifyReply updates outreach_enrollments with UPDATE ... WHERE id = ${row.id} but does not include tenant_id in the WHERE clause. This lets the update succeed even if the row id belonged to a different tenant, enabling cross-tenant modification by id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or re-query and validate the enrollment's tenant_id equals the caller's tenant before applying the update. Require tenantId in the call site if necessary.
- **server/agentic/approvals.ts:134** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to mark expired approvals without scoping to a tenant (UPDATE agentApprovals SET status = 'expired' WHERE status = 'pending' AND expires_at < now()). This will expire pending approvals across all tenants in one operation.
  - _Fix:_ Make this operation either accept a tenantId and include tenant_id = ${tenantId} in the WHERE, or perform it per-tenant by iterating tenant ids. If a global expiry is truly required, ensure the caller is an authorized system cron and carefully audit the consequences; preferred fix is explicit per-tenant scoping to avoid accidental cross-tenant state changes.
- **server/agentic/resume-worker.ts:65** — Unchecked cross-tenant SELECT: the raw query at resumePendingApprovalRuns selects from agent_runs (joined with agent_approvals) without constraining rows to a single tenant (no WHERE r.tenant_id = <tenantId>). This reads tenant-scoped data across all tenants.
  - _Fix:_ Restrict the SELECT to a specific tenant (e.g. add WHERE r.tenant_id = ${tenantId} or accept a tenantId param and query per-tenant). If the intent is a global system scan, make that explicit and run under a hardened admin path with strict access controls / auditing; otherwise iterate tenants and run tenant-scoped queries so every read against tenant-scoped tables includes an explicit tenant_id filter.
- **server/agentic/wake-scheduler.ts:142** — UPDATE statement claims due wakes using a sub-select that lacks any tenant_id constraint: it updates agent_wake_schedules WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED). This can move rows across tenants and violates tenant isolation rules.
  - _Fix:_ This is a critical tenant-isolation bug. Ensure the UPDATE/inner SELECT is tenant-scoped. Either run the claim per-tenant (include WHERE tenant_id = <tenant> in the inner SELECT) or add an explicit tenant_id = ? predicate to both the sub-select and update. Avoid global selection across tenants from a tenant-scoped table.
- **server/business-tools.ts:96** — INSERT into invoice_items does not set tenant_id. Tenant-scoped child table rows must include tenant_id in INSERT to preserve application-level isolation.
  - _Fix:_ Include the tenant_id when inserting invoice line items (and any other tenant-scoped child tables). Example: INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). Also audit invoice_items reads/updates to ensure WHERE tenant_id = ${tid} is always present, or validate the parent invoice belongs to the tenant before inserting.
- **server/chat-engine.ts:1624** — buildMemorySection declares tenantId defaulting to 1 (tenantId: number = 1). This silently falls back to the admin tenant when callers omit tenantId, enabling cross-tenant reads/writes via downstream calls that use this tenantId (e.g. proactiveContextLoad, recordHydeAttempt, etc.). This defeats the project's fail-closed tenant isolation guarantees and can leak data between tenants.
  - _Fix:_ Remove the implicit admin default and enforce a fail-closed contract. Make tenantId required (or default to null/undefined) and validate at runtime, throwing if missing or invalid — same pattern used by buildSystemPrompt. Example fixes: 1) change signature to buildMemorySection(memories: MemoryEntry[], userMessage?: string, tenantId: number | null, personaId?: number | null) and at the top throw if tenantId is null/invalid; or 2) keep an optional tenantId but default to null and return/throw when not a positive integer. Audit all callers to ensure a positive tenantId is always passed.
- **server/chat-engine/project-context.ts:46** — Query uses project_conversations (pc) inside an EXISTS without constraining pc to the caller's tenant: `SELECT 1 FROM project_conversations pc WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id` — no tenant_id check on pc.
  - _Fix:_ Add an explicit tenant constraint on project_conversations (e.g. AND pc.tenant_id = ${tenantId} or the Drizzle equivalent). Do not rely on joined rows alone; every read against a tenant-scoped table must include the tenant_id filter.
- **server/chat-engine/project-context.ts:159** — Selecting project_files by project_id only: `SELECT ... FROM project_files WHERE project_id = ${projectId}` — no tenant_id constraint on project_files.
  - _Fix:_ Include tenant scoping on the query (e.g. AND tenant_id = ${tenantId} or where(eq(project_files.tenantId, tenantId))). Even if projectId was validated earlier, every tenant-scoped table read must include an explicit tenant filter.
- **server/chat-engine/project-context.ts:171** — Selecting project_notes by project_id only: `SELECT note, author, created_at FROM project_notes WHERE project_id = ${projectId}` — missing tenant_id constraint on project_notes.
  - _Fix:_ Add an explicit tenant filter (e.g. AND tenant_id = ${tenantId} / where(eq(project_notes.tenantId, tenantId))). Do not assume prior validation of projectId is sufficient for row-level isolation.
- **server/chat-engine/project-context.ts:184** — Query joins project_conversations (pc) and conversations (c) but only applies tenant_id to conversations: `WHERE pc.project_id = ${projectId} ... AND c.tenant_id = ${tenantId}` — pc is read without an explicit tenant_id constraint.
  - _Fix:_ Constrain pc as well (e.g. AND pc.tenant_id = ${tenantId} or use where(eq(project_conversations.tenantId, tenantId))). Every access to a tenant-scoped table must include the tenant filter.
- **server/cmmc-assessments.ts:150** — getCmmcAccessByToken looks up invitations/assessments by token without scoping to a tenant: the SELECT / JOIN against cmmcAssessmentInvitations / cmmcAssessments has no tenant_id WHERE clause, allowing cross-tenant reads by token alone.
  - _Fix:_ Either require the caller to provide tenant context and add eq(...tenantId...) to the WHERE clause, or otherwise redesign token lookup so it is tenant-scoped (e.g. store/require tenantId with the token or include tenant_id in the WHERE). Ensure any SELECT against tenant-scoped tables always includes tenant_id.
- **server/cmmc-prospect-artifacts.ts:93** — Uses string-interpolated SQL inside sql`...` with request-derived values to build an advisory lock key: `tx.execute(sql`SELECT pg_advisory_xact_lock(hashtext(${`cmmc-prospect-artifact:${params.tenantId}:${filename}`}))`)` — this embeds caller-controlled data into SQL construction and risks injection/isolation issues.
  - _Fix:_ Avoid embedding request-derived strings into raw SQL. Compute a stable lock key in the application (e.g. a numeric hash/BigInt) and pass it as a parameterized value, or use a parameter-bound expression. Example: compute a deterministic bigint from the tenant+filename and call `pg_advisory_xact_lock($1)` with that numeric parameter (or use Drizzle's proper parameter binding). Ensure the lock invocation is parameterized and does not interpolate raw request strings into SQL text.
- **server/commitment-drafter.ts:234** — Unscoped UPDATE against tenant-scoped table `commitments` — `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}` has no tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the owning tenant (and existing draft_status guard) so the statement cannot affect other tenants. e.g. UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}; (or AND tenant_id = ${tenantId} AND draft_status = 'draft_pending' as appropriate). Always include tenant_id for writes to tenant-scoped tables.
- **server/embeddings.ts:256** — Use of sql.raw() with an interpolated vector literal derived from the caller's query embedding in vectorSearchMemory (embedding is derived from user-supplied `query`). This constructs a raw `'[x,y,...]'::vector` fragment via string interpolation and risks SQL-injection / isolation issues.
  - _Fix:_ Avoid constructing SQL vector literals via string interpolation. Pass the vector data as a bound parameter (use Drizzle/pg parameter binding) or a safe helper that casts parameters to the vector type, or provide a safe API in the DB layer for pgvector operations. Validate and canonicalize the embedding and prefer using a parameterized expression rather than sql.raw(`'${vec}'::vector`).
- **server/embeddings.ts:583** — Use of sql.raw() with an interpolated vector literal derived from the caller's query embedding in the vectorSearchKnowledge pgvector query. The vector (`vec`) is built from the user `query` embedding and injected into raw SQL.
  - _Fix:_ Same fix as above: do not interpolate vector text into raw SQL. Use parameterized bindings for the vector, or a Drizzle-safe API to compare vectors, and validate the embedding shape before any interpolation. This prevents injection or malformed-literal risks.
- **server/embeddings.ts:809** — storeEmbeddingVec falls back to executing an UPDATE on the chosen table without any tenant constraint when options.tenantId is undefined. That updates tenant-scoped tables (memory_entries / agent_knowledge) without restricting to a tenant, breaking the tenant-isolation invariant.
  - _Fix:_ Require a positive tenantId for any operation that updates tenant-scoped tables. Remove the fallback branch that updates without tenant filtering, or add an explicit WHERE tenant_id = ${tenantId} and require tenantId in the function signature / caller. If a global admin operation is intended, make it explicit (separate admin-only API) and audit it carefully.
- **server/episode-playbooks.ts:68** — String-interpolated embedding literal (${embeddingLiteral}::vector) is injected directly into SQL in distillEpisode's similarity SELECT. embeddingLiteral is produced from an embedding of the user-supplied goal — this is effectively string-interpolated SQL with request-derived content and can enable injection or malformed-literal attacks.
  - _Fix:_ Do not inject embedding text directly. Use a validated vector representation and parameter binding, or a helper that safely binds vector parameters to the query. At minimum, validate the embedding shape via vecLiteral and use a parameterized expression rather than raw string interpolation.
- **server/episode-playbooks.ts:92** — INSERT INTO episode_playbooks uses ${embeddingLiteral}::vector in VALUES via string interpolation — embeddingLiteral is derived from a model embedding of a caller-supplied goal. This is string-interpolated SQL with request-derived content.
  - _Fix:_ Replace the embedded literal with a parameterized bind for the vector column (or use a safe helper to cast a bound parameter to pgvector). Validate vector contents (numeric array shape) and avoid direct string interpolation into SQL.
- **server/episode-playbooks.ts:118** — retrieveEpisodePlaybooks interpolates embeddingLiteral into the SELECT/ORDER BY (goal_embedding <=> ${embeddingLiteral}::vector). embeddingLiteral is derived from the caller's goal (request-derived), so this is string-interpolated SQL with untrusted content.
  - _Fix:_ Use parameterized vector bindings for similarity comparisons or a Drizzle-safe API for pgvector comparisons. Validate embeddings and avoid crafting raw vector SQL strings from request-influenced data.
- **server/email.ts:370** — In startInboxPolling the code queries inbox_messages by message_id with SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 but does not restrict by tenant_id. This reads a tenant-scoped table without tenant scoping and can cause cross-tenant leaks.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}). If message ownership is not yet known, first resolve the tenant (via the inbox mapping) and then query using both id/message_id and tenant_id. Do not perform blind global lookups on tenant-scoped tables.
- **server/email.ts:415** — backfillEmptyBodies SELECTs rows from inbox_messages without any tenant_id WHERE clause (SELECT id, message_id FROM inbox_messages WHERE ...). This enumerates rows across all tenants and breaks tenant isolation.
  - _Fix:_ Run the backfill per-tenant or add a tenantId parameter and a WHERE tenant_id = ${tenantId}. If the intent is to scan all tenants, make that explicit and require admin authorization and auditing.
- **server/email.ts:429** — backfillEmptyBodies performs UPDATE inbox_messages SET body_text = ${bodyText}, body_html = ${bodyHtml} WHERE id = ${row.id} without tenant_id constraint. This updates tenant-scoped rows without constraining by tenant, enabling accidental cross-tenant modification.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or resolve the tenant before updating. Avoid single-column PK-only updates on tenant-scoped tables unless you have explicit admin intent and authorization.
- **server/google-workspace.ts:2286** — INSERT into presenter_slide_images does not include tenant_id; inserting binary slide images into a (likely) tenant-scoped table using only session_id can allow cross-tenant writes if session_id is abused or misbound.
  - _Fix:_ Include the canonical tenant id in the INSERT (e.g. add tenant_id column and VALUES (${tenantId}, ...)), or resolve/verify the session->tenant mapping first and only insert when the session belongs to the current tenant. Prefer a parametrized insert that sets tenant_id explicitly (or use a single INSERT ... SELECT that joins presenter_sessions to validate ownership).
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks any tenant constraint. This updates a presenter session by id without verifying that the session belongs to the caller's tenant, enabling cross-tenant modification of session data.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) and verify the rowCount/returning result to ensure the session belonged to the expected tenant. Alternatively, fetch the session and validate its tenant_id before performing the update, and fail closed if it doesn't match.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) performs a write across selected proposal ids without constraining by tenant_id. This can modify proposals belonging to other tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or resolve each id's tenant and only update rows whose tenant_id matches the authorized tenant (or require an explicit, authenticated admin context for global updates). Verify row counts to ensure only intended tenant rows were modified.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) disables heartbeat tasks by id without verifying tenant ownership. This can disable tasks across tenants.
  - _Fix:_ Constrain the UPDATE to the tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or resolve each task's tenant and only disable tasks belonging to the authorized tenant. If the intent is a global admin action, require and check an explicit admin context before making cross-tenant writes.

### HIGH (70)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects from tenant-scoped `experiments` without constraining by tenant_id (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across all tenants in a context that does not enforce per-caller tenant ownership.
  - _Fix:_ Either require a tenantId parameter and filter the SELECT by tenant_id = <tenantId>, or if this is intended as a multi-tenant sweep ensure the code path is strictly admin-only and that any eventual per-row actions re-validate ownership before disclosing data. Prefer running per-tenant iterations (select tenants then query each tenant's experiments) so every read is explicitly tenant-scoped.
- **server/agentic-engines.ts:500** — retryPendingMinervaRouting executes a SELECT on tenant-scoped `ai_insights` without a tenant_id constraint (SELECT id, tenant_id, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = <marker> LIMIT 50). This reads pending rows across all tenants in a non-scoped query.
  - _Fix:_ Scope this query per-tenant (add WHERE tenant_id = ${tenantId}) or iterate tenant IDs and run the query per-tenant. If the intent is a global sweep, ensure the caller is admin-only and that any use of these rows never leaks them to non-authorized tenants; preferred fix is explicit per-tenant iteration.
- **server/agentic-features.ts:861** — enrollInSequence checks for an existing active enrollment using SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' — this SELECT omits tenant_id, so it may match another tenant's enrollment and improperly block or influence the caller.
  - _Fix:_ Include tenant scoping in the duplicate-check SELECT (e.g. WHERE tenant_id = ${params.tenantId} AND sequence_id = ...). Always include tenant_id in reads that gate writes to tenant-scoped tables.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' executes against a tenant-scoped table without constraining by tenant_id. This reads cross-tenant data in a context that appears to be a background sweep.
  - _Fix:_ Either scope the query to a specific tenant (add WHERE tenant_id = ${tenantId}) or make the admin-intent explicit and guarded (e.g. require ADMIN_TENANT_ID and an explicit admin-only path). If the function must iterate all tenants, ensure it is only callable by an admin service account and document/guard that fact; otherwise convert the sweep into a per-tenant call that supplies tenantId to every read/write to enforce tenant isolation.
- **server/agentic/self-initiative.ts:76** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ...`)) used with request-derived tenantId in SELECT against tenant-scoped table `repair_incidents`.
  - _Fix:_ Avoid inline sql`` interpolation of request-derived values. Use Drizzle query builder (e.g. db.select().from(table).where(eq(table.tenantId, tenantId))) or otherwise use parameterized APIs so tenant_id filtering and binding are explicit and not subject to injection/templating mistakes.
- **server/agentic/self-initiative.ts:81** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ... ${since30d} ...`)) used with request-derived tenantId in aggregation query against `repair_incidents`.
  - _Fix:_ Replace raw sql`` with Drizzle query builder or explicit parameterized calls; ensure tenant_id is provided via a safe parameter and use .where(eq(table.tenantId, tenantId)) rather than string interpolation.
- **server/agentic/self-initiative.ts:87** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ...`)) used with request-derived tenantId in SELECT from `decline_events`.
  - _Fix:_ Use the Drizzle query builder with explicit .where(eq(table.tenantId, tenantId)) and avoid sql`` interpolation of request-derived values.
- **server/agentic/self-initiative.ts:91** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ...`)) used with request-derived tenantId in SELECT from `audit_leads` (total count).
  - _Fix:_ Use typed query builder calls that add the tenant filter via eq(auditLeads.tenantId, tenantId) instead of interpolating tenantId into a raw SQL template.
- **server/agentic/self-initiative.ts:95** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ...`)) used with request-derived tenantId in SELECT from `audit_leads` (recent).
  - _Fix:_ Use Drizzle's query builder and parameter binding, ensuring tenant_id is enforced through .where(eq(..., tenantId)). Avoid sql`` interpolation for request-sourced values.
- **server/agentic/self-initiative.ts:100** — Raw SQL template interpolation used with request-derived tenantId inside a sub-select: (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}) used in query against `heartbeat_logs`.
  - _Fix:_ Avoid building subqueries via sql`` with interpolated tenant values. Use the query builder to construct the subquery and enforce tenant filter explicitly (or join on the properly constrained heartbeat_tasks table) to prevent injection/isolation issues.
- **server/agentic/self-initiative.ts:108** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ...`)) used with request-derived tenantId in SELECT from `eval_runs`.
  - _Fix:_ Use Drizzle query builder and parameter binding, and ensure tenant_id is enforced via .where(eq(evalRuns.tenantId, tenantId)) rather than raw SQL interpolation.
- **server/agentic/self-initiative.ts:114** — Raw SQL template interpolation (db.execute(sql`... ${tenantId} ...`)) used with request-derived tenantId in SELECT from `self_initiatives`.
  - _Fix:_ Use Drizzle API to build the SELECT with an explicit tenant_id predicate (eq(selfInitiatives.tenantId, tenantId)) instead of interpolating tenantId into sql``.
- **server/agentic/self-initiative.ts:266** — Raw SQL template interpolation (db.execute(sql`SELECT signature FROM self_initiatives WHERE tenant_id = ${tenantId} ...`)) used with request-derived tenantId.
  - _Fix:_ Switch to the Drizzle query builder or parameterized query; ensure tenant scoping is applied via builder-style .where(eq(selfInitiatives.tenantId, tenantId)). Avoid inline sql`` with request-sourced values.
- **server/agentic/self-initiative.ts:301** — INSERT into tenant-scoped table `self_initiatives` is performed via raw sql template with interpolated tenantId and other request-derived values (title, rationale, telemetry JSON).
  - _Fix:_ Prefer using Drizzle's insert API (db.insert(selfInitiatives).values({...})) which provides typed binding and avoids manual interpolation. If raw SQL must be used, ensure proper parameter binding and validation of all request-derived fields before interpolation.
- **server/agentic/self-initiative.ts:341** — Raw SQL template interpolation used for INSERT into `notifications` with request-derived tenantId and content from LLM output inserted directly via sql``.
  - _Fix:_ Validate and sanitize LLM-derived content and use Drizzle parameterized insert methods (or parameter binding) to avoid SQL injection and ensure tenant-scoped writes are explicit and safe.
- **server/agentic/step-reward.ts:140** — Raw SQL template interpolation (db.execute(sql`INSERT INTO step_rewards ... VALUES (${i.tenantId}, ...)`)) used with request-derived tenantId and other input fields.
  - _Fix:_ Use Drizzle's insert API with explicit values and typed binding (db.insert(stepRewards).values(...)) to avoid inline sql interpolation of request-derived values and to guarantee tenant_id is set correctly.
- **server/agentic/step-reward.ts:171** — Raw SQL template interpolation (db.execute(sql`SELECT * FROM step_rewards WHERE tenant_id = ${tenantId} AND plan_id = ${planId} ...`)) used with request-derived tenantId and planId.
  - _Fix:_ Use Drizzle query builder with .where(eq(stepRewards.tenantId, tenantId), eq(stepRewards.planId, planId)) or other parameterized APIs to avoid interpolation and ensure tenant isolation is explicit.
- **server/agentic/task-forces.ts:40** — Raw SQL template interpolation used for INSERT into tenant-scoped `task_forces` with interpolated p.tenantId and other request-derived fields.
  - _Fix:_ Use Drizzle's insert API (db.insert(taskForces).values(...)) so tenant_id and other fields are bound safely, or at minimum parameterize values rather than injecting them into a sql`` string.
- **server/agentic/task-forces.ts:58** — Raw SQL template interpolation used for SELECT FROM `task_forces` with request-derived tenantId (and optional status) interpolated into sql``.
  - _Fix:_ Use the query builder and .where(eq(taskForces.tenantId, tenantId), status ? eq(taskForces.status, status) : undefined) to make tenant scoping explicit and avoid string interpolation of request values.
- **server/agentic/task-forces.ts:65** — Raw SQL template interpolation used for SELECT * FROM task_forces WHERE id = ${id} AND tenant_id = ${tenantId} — tenant check present but values are interpolated into raw SQL.
  - _Fix:_ Prefer db.select().from(taskForces).where(and(eq(taskForces.id, id), eq(taskForces.tenantId, tenantId))). This uses typed bindings and avoids building SQL with interpolated request values.
- **server/agentic/task-forces.ts:87** — Raw SQL template interpolation used in UPDATE statement on tenant-scoped `task_forces` with interpolated id and tenantId in WHERE clause.
  - _Fix:_ Use a parameterized update via query builder (db.update(taskForces).set(...).where(and(eq(taskForces.id, id), eq(taskForces.tenantId, tenantId)))) to ensure safe binding and explicit tenant scoping.
- **server/agentic/task-forces.ts:112** — Raw SQL template interpolation used for UPDATE task_forces SET status = 'paused' WHERE id = ${id} AND tenant_id = ${tenantId} — request-derived values interpolated directly.
  - _Fix:_ Replace with the Drizzle update builder with explicit tenant predicate to avoid raw interpolation and to make the tenant-scope constraint explicit and less error-prone.
- **server/agentic/task-forces.ts:120** — Raw SQL template interpolation used for UPDATE task_forces SET status = 'active' WHERE id = ${id} AND tenant_id = ${tenantId} — interpolating request-derived values into sql``.
  - _Fix:_ Use db.update(...).where(and(eq(taskForces.id, id), eq(taskForces.tenantId, tenantId))) via Drizzle to bind parameters safely and ensure tenant isolation.
- **server/agentic/task-forces.ts:128** — Raw SQL template interpolation used for UPDATE task_forces that sets result JSON and matches tenant via WHERE id = ${id} AND tenant_id = ${tenantId}.
  - _Fix:_ Use the Drizzle update API with explicit parameter binding; ensure tenant predicate is applied via the query builder rather than interpolating values into sql``.
- **server/agentic/wake-scheduler.ts:66** — Raw SQL template interpolation used for INSERT into tenant-scoped `agent_wake_schedules` with interpolated p.tenantId and other request-derived fields.
  - _Fix:_ Use Drizzle's insert API to bind tenantId and other values safely (db.insert(agentWakeSchedules).values(...)). Sanitise/validate any request-derived JSON before storing and avoid constructing SQL with interpolated request values.
- **server/agentic/wake-scheduler.ts:100** — Raw SQL template interpolation used for UPDATE agent_wake_schedules ... WHERE tenant_id = ${tenantId} AND (trigger_event = ${eventType} OR trigger_event = ${wildcard}) AND (trigger_filter IS NULL OR ${dataJson}::jsonb @> trigger_filter) — request-derived eventType/dataJson interpolated into sql``.
  - _Fix:_ Construct this query with the query builder and bind parameters safely. For JSON containment predicates, pass JSON as a bound parameter (not via string interpolation) and validate eventType/triggerFilter inputs before use. Avoid embedding raw JSON strings into sql``.
- **server/agentic/wake-scheduler.ts:119** — Raw SQL template interpolation used for UPDATE agent_wake_schedules SET status = 'cancelled' WHERE id = ${id} AND tenant_id = ${tenantId} — request-derived values interpolated.
  - _Fix:_ Use Drizzle's update builder with explicit parameter binding and tenant predicate (eq(agentWakeSchedules.id, id), eq(agentWakeSchedules.tenantId, tenantId)) to avoid inline sql interpolation.
- **server/agentic/wake-scheduler.ts:127** — Raw SQL template interpolation used for SELECT * FROM agent_wake_schedules WHERE tenant_id = ${tenantId} ... — tenantId is interpolated into sql``.
  - _Fix:_ Use Drizzle select builder with .where(eq(agentWakeSchedules.tenantId, tenantId)) and parameterized status filter rather than interpolating tenantId into raw SQL.
- **server/attention-handlers/feature-announce.ts:99** — Raw SQL template interpolation used for INSERT into tenant-scoped `notifications` with interpolated tenantId and LLM output (title/body/metadata) directly placed into sql``.
  - _Fix:_ Validate and sanitize LLM-derived content and use a parameterized insert via the query builder (db.insert(notifications).values(...)) to ensure tenant_id and payload are bound safely rather than interpolated into raw SQL.
- **server/attention-handlers/owner-notify.ts:34** — Raw SQL template interpolation used for SELECT 1 FROM delivery_logs WHERE id = ${data.deliveryId} AND tenant_id = ${tenantId} — tenant check present but values are interpolated into sql``.
  - _Fix:_ Use the query builder (db.select().from(deliveryLogs).where(and(eq(deliveryLogs.id, data.deliveryId), eq(deliveryLogs.tenantId, tenantId)))) to bind parameters and avoid inline sql interpolation.
- **server/attention-handlers/owner-notify.ts:38** — Raw SQL template interpolation used for SELECT 1 FROM code_proposals WHERE id = ${data.proposalId} AND tenant_id = ${tenantId} — values interpolated into sql``.
  - _Fix:_ Use Drizzle query builder with explicit tenant predicate and parameter binding to ensure safe checking of referenced rows.
- **server/attention-handlers/owner-notify.ts:42** — Raw SQL template interpolation used for SELECT 1 FROM plans WHERE id = ${data.planId} AND tenant_id = ${tenantId} — interpolated values in sql``.
  - _Fix:_ Replace with a parameterized select built via the query builder (db.select().from(plans).where(and(eq(plans.id, data.planId), eq(plans.tenantId, tenantId)))). Avoid using raw sql interpolation for request-derived ids.
- **server/attention-handlers/owner-notify.ts:82** — Raw SQL template interpolation used for INSERT INTO notifications (tenant_id, ...) VALUES (${tenantId}, 'digest', ${title}, ${message}, 'owner_digest', ${metadata}::jsonb) — tenantId and payload are interpolated into sql``.
  - _Fix:_ Use Drizzle insert builder with parameter binding (db.insert(notifications).values({...})) and sanitize JSON payloads; avoid inserting untrusted/LLM-derived data via raw SQL interpolation.
- **server/attention-scorer.ts:84** — Raw SQL template interpolation used for SELECT 1 FROM event_log WHERE tenant_id = ${tenantId} AND event_type = ${eventType} ... — tenantId and eventType interpolated into sql``.
  - _Fix:_ Use the query builder (db.select().from(eventLog).where(and(eq(eventLog.tenantId, tenantId), eq(eventLog.eventType, eventType), ...))) to avoid raw interpolation and ensure correct tenant scoping and parameter binding.
- **server/audit-fulfillment.ts:79** — Raw SQL template interpolation used for SELECT icp_hint, notes, tier_interest FROM audit_leads WHERE tenant_id = ${tenantId} AND email = ${customerEmail} — tenantId and customerEmail interpolated into sql``.
  - _Fix:_ Use the Drizzle select builder with .where(and(eq(auditLeads.tenantId, tenantId), eq(auditLeads.email, customerEmail))) to bind parameters safely and avoid inline SQL interpolation of request values.
- **server/auth.ts:198** — Query selects from tenant-scoped table `apiKeys` using only the keyHash (no tenant_id constraint). This lets the code read rows across tenants during API-key validation, relying on the returned row to set the request tenant rather than bounding the query to a caller-provided/known tenant.
  - _Fix:_ Include tenant scoping in the lookup or otherwise prove the key is namespaced. Options: (1) if you already know the tenant context, add AND tenant_id = ${tenantId}; (2) if the lookup must resolve tenant from the key, ensure key material is globally unguessable/unique and treat this code-path as a privileged auth step, or (preferred) namespace keyHash by tenant when storing so lookups always include tenant_id. At minimum, after selecting the row, validate that the row's tenantId matches any other asserted tenant context before using it.
- **server/business-tools.ts:269** — updateCustomer accepts/sets assigned_to (a foreign id) from caller input without verifying that the referenced user belongs to the caller's tenant. This lets a caller attach a customer row to a foreign user id belonging to another tenant.
  - _Fix:_ Before persisting any foreign-id fields (assigned_to, owner_id, project_id, etc.), verify ownership: SELECT id FROM users WHERE id = ${params.assigned_to} AND tenant_id = ${tid} LIMIT 1 and reject if not found. Alternatively, disallow cross-tenant FK assignment and/or co-write tenant_id into the referenced column only after ownership is validated.
- **server/commitments.ts:135** — Cross-tenant SELECT from tenant-scoped table `commitments` with no tenant filter — the scanner executes: SELECT ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL AND due_at < NOW() ... (no tenant_id predicate).
  - _Fix:_ If this multi-tenant fan-in is intentional for operator/admin visibility, make the behavior explicit and strongly gated (admin-only) and ensure any returned content is redacted and cannot be used to infer or access tenant-scoped data. Otherwise, restrict the SELECT to a specific tenant (WHERE tenant_id = ${tenantId}) or enumerate tenants first and run per-tenant SELECTs so every read against commitments is tenant-scoped.
- **server/data-protection.ts:150** — SELECT against tenant-scoped table `conversations` without tenant_id predicate (reads soft-deleted conversations across all tenants).
  - _Fix:_ Limit the query to a specific tenant or iterate per-tenant. Example: add `AND tenant_id = ${someTenantId}` or, if this cron must act per-tenant, load tenants and run the SELECT/DELETE inside a tenant-scoped loop or a withTenantTx to ensure operations are explicitly scoped.
- **server/dfy-intake.ts:50** — Lookup by token queries tenant-scoped table dfy_intake_forms without constraining to the caller's tenant (SELECT ... FROM dfy_intake_forms WHERE token = ...). This returns a tenant's form by token alone and can produce cross-tenant access if tokens are misused.
  - _Fix:_ Require and use a tenant identifier when resolving tokens, or otherwise validate ownership before returning the row. Example: SELECT ... FROM dfy_intake_forms WHERE token = ${token} AND tenant_id = ${tenantId} (or pass tenantId into the function and use Drizzle eq(dfyIntakeForms.tenantId, tenantId)). If token-only public access is intentional, document the risk and ensure tokens are single-tenant-scoped during generation.
- **server/doc-collections.ts:166** — DELETE statement deletes from tenant-scoped table doc_chunks without a tenant_id filter (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}). Every write against tenant-scoped tables must include the tenant constraint.
  - _Fix:_ Add the tenant constraint to the DELETE (AND tenant_id = ${tenantId}). E.g. use: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId} — or use Drizzle .where(eq(docChunks.collectionId, collectionId), eq(docChunks.tenantId, tenantId), eq(docChunks.docPath, docPath)). Ensure every SQL touching tenant-scoped tables includes the tenant_id check.
- **server/embeddings.ts:845** — backfillEmbeddingVecs reads memory_entries (SELECT id, embedding FROM memory_entries ...) without any tenant_id filter. This enumerates rows across all tenants and breaches the rule that reads against tenant-scoped tables must be tenant-constrained.
  - _Fix:_ Run backfill per-tenant (iterate tenants) or add an explicit tenantId parameter and WHERE tenant_id = ${tenantId}. If a global admin background job MUST run across tenants, make it an explicit admin routine and restrict/authorize its execution.
- **server/embeddings.ts:855** — Within backfillEmbeddingVecs the UPDATE of memory_entries to set embedding_vec uses WHERE id = ${row.id} but has no tenant_id guard — updating tenant-scoped rows without tenant constraint risks cross-tenant writes.
  - _Fix:_ Include AND tenant_id = ${row.tenant_id} (or require tenantId at callsite) in the UPDATE, or perform backfills per-tenant so all writes include the tenant's id as a WHERE clause.
- **server/embeddings.ts:861** — backfillEmbeddingVecs SELECT from agent_knowledge (SELECT id, embedding FROM agent_knowledge ...) runs without a tenant_id filter, reading records across tenants.
  - _Fix:_ Same as for memory_entries backfill: perform per-tenant backfill (iterate tenants) or require/accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to the SELECT.
- **server/embeddings.ts:872** — backfillEmbeddingVecs updates agent_knowledge rows (UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}) without constraining by tenant_id, allowing cross-tenant writes.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${row.tenant_id}) or perform the operation within a per-tenant loop that ensures tenant_id is part of the WHERE clause.
- **server/embeddings.ts:814** — backfillMissingKnowledgeEmbeddings SELECTs agent_knowledge rows across all tenants (no tenant_id WHERE clause). This reads tenant-scoped data without tenant scoping.
  - _Fix:_ Either accept/require a tenantId and add WHERE tenant_id = ${tenantId}, or explicitly make this an admin-only migration with proper authorization and logging and avoid implicit cross-tenant reads.
- **server/event-bus.ts:173** — Queries selecting event_subscriptions JOIN personas do not constrain the joined personas by tenant. Example: SELECT ... FROM event_subscriptions es JOIN personas p ON p.id = es.persona_id WHERE es.tenant_id = ${tenantId} — the JOIN lacks p.tenant_id = es.tenant_id, so persona rows from other tenants could be returned if persona ids collide across tenants.
  - _Fix:_ Add tenant ownership checks for joined tenant-scoped tables. Change the JOIN to include tenant scoping (e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = es.tenant_id) or add WHERE p.tenant_id = ${tenantId} to ensure persona rows belong to the same tenant.
- **server/event-bus.ts:187** — Wildcard subscriber lookup repeats the JOIN of event_subscriptions to personas without enforcing personas' tenant ownership (same pattern as the exact query). This may surface personas from other tenants if ids collide.
  - _Fix:_ As above, ensure joined persona rows are constrained by tenant (JOIN ... ON p.id = es.persona_id AND p.tenant_id = es.tenant_id or add p.tenant_id = ${tenantId} in WHERE). Always verify ownership for referenced foreign rows.
- **server/event-bus.ts:305** — getEventSubscriptions SELECT joins event_subscriptions to personas but does not ensure the persona row is tenant-scoped to the same tenant (JOIN personas p ON p.id = es.persona_id with WHERE es.tenant_id = ${tenantId}). The persona ownership is not verified in SQL.
  - _Fix:_ Include persona tenant scoping in the JOIN or WHERE (e.g. JOIN personas p ON p.id = es.persona_id AND p.tenant_id = es.tenant_id) so that returned persona_name always comes from a persona that belongs to the same tenant.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` is executed without a tenant_id filter. The boot-time rehydration query retrieves rows across all tenants and then loads per-row data into an in-memory taskStateStore — this allows cross-tenant reads and can surface other tenants' state into process memory.
  - _Fix:_ Constrain the SELECT to a specific tenant (e.g. WHERE tenant_id = ${tenantId}) or iterate explicitly per-tenant with an authorized admin flow. If the intention is to rehydrate only for a single tenant, accept/require a tenantId parameter and add WHERE tenant_id = ${tenantId} to the query. If rehydration must cover all tenants, make this operation an explicit, audited, admin-only maintenance task (with proper authorization) and avoid populating per-tenant runtime maps indiscriminately; instead rehydrate into tenant-scoped stores or run per-tenant jobs so no tenant's data is mixed into other tenants' contexts.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' executes without constraining by tenant_id in the WHERE clause (where(eq(scrapedPages.id, existing[0].id)) ). This allows an update to a row that may belong to another tenant.
  - _Fix:_ Include the tenant constraint in the WHERE clause. For example: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Prefer combining id+tenant checks for all UPDATE/DELETE operations on tenant-scoped tables.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' executes without constraining by tenant_id in the WHERE clause inside the crawl loop (where(eq(scrapedPages.id, existing[0].id)) ).
  - _Fix:_ Add tenant scoping to the update WHERE clause, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Ensure every UPDATE/DELETE on tenant-scoped tables includes eq(table.tenantId, tenantId).
- **server/glasses-gateway.ts:173** — UPDATE on apiKeys table updates lastUsedAt using only eq(apiKeys.id, key.id) with no tenant_id constraint. If apiKeys is a tenant-scoped table this lacks explicit tenant scoping in the SQL WHERE clause.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))). Always include the tenant_id equality in WHERE for tenant-scoped tables even when you previously selected the row.
- **server/google-drive.ts:866** — SELECT from projects reads a project row via where(eq(projects.id, projectId)) without scoping the query to the caller's tenant (no projects.tenant_id = tenantId). This reads a foreign id without an explicit tenant filter.
  - _Fix:_ Scope the SELECT to the caller's tenant. Use where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or otherwise include the tenant id in the WHERE so the row read is provably owned by the caller's tenant before proceeding.
- **server/google-drive.ts:879** — UPDATE of projects (setting driveFolderId/driveFolderUrl) uses where(eq(projects.id, projectId)) without an explicit tenant_id constraint. This can update a project belonging to another tenant.
  - _Fix:_ Add tenant scoping to the UPDATE WHERE clause, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Always require tenant equality in UPDATE/DELETE for tenant-scoped tables.
- **server/google-drive.ts:1473** — SELECT of projects in uploadAndShare does where(eq(projects.id, params.projectId)) with no tenant constraint. This reads a foreign project id without constraining to the caller's tenant before further logic.
  - _Fix:_ Include tenant scoping when selecting the project (e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId || callerTenantId)) )). Validate ownership via a tenant-scoped query and fail-closed if the project does not belong to the caller's tenant.
- **server/health-audit.ts:314** — SELECT FROM code_proposals is executed without any tenant_id filter (SELECT id, title, status, created_at FROM code_proposals ...). This reads proposals across tenants without scoping to a tenant or requiring explicit admin intent.
  - _Fix:_ Either add tenant scoping (WHERE tenant_id = ${tenantId}) and run per-tenant, or require an explicit admin-only parameter/authorization for a global audit and make that explicit in the code. At minimum, accept a tenantId argument and filter the SELECT, or document and gate the global operation behind an authenticated admin path.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks is executed without tenant_id filtering (SELECT id, name, created_at, next_run_at FROM heartbeat_tasks WHERE enabled = true ...). This reads scheduled tasks across all tenants rather than being tenant-scoped.
  - _Fix:_ Scope heartbeat task queries to a tenant (WHERE tenant_id = ${tenantId}) when the intent is to audit a single tenant, or require explicit admin authorization for a global scan. Prefer an explicit per-tenant loop if the intention is to act tenant-by-tenant.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE heartbeat_tasks ... WHERE id = ${id} ... is executed without constraining by tenant_id (claimDueHeartbeatTasksAcrossTenants). This updates tenant-scoped rows without verifying the tenant, allowing potential cross-tenant mutation if an id is guessed or misrouted.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or verify the row's tenant ownership before updating. Prefer passing a (id, tenantId) pair to this function and use both in the UPDATE predicate to ensure tenant-scoped writes cannot cross tenants.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} — directly updates a tenant-scoped table by id without constraining tenant_id. This can mutate another tenant's row if ids are mis-used.
  - _Fix:_ Include tenant scoping in the UPDATE predicate (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the change via the storage layer that enforces tenant ownership (e.g. storage.updateHeartbeatTask) which accepts tenantId for validation.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} — updates a tenant-scoped table without constraining tenant_id.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause, or route the change through the tenant-aware storage API so the write is validated against the task's tenant before mutating the row.
- **server/heartbeat.ts:2681** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — performs an UPDATE on tenant-scoped heartbeat_tasks without including tenant_id. Even though newTask was just created, the UPDATE lacks explicit tenant scoping and could affect another tenant's row if ids are misused.
  - _Fix:_ Use a tenant-aware update: include AND tenant_id = ${newTask.tenantId} in the WHERE clause, or call the tenant-scoped storage API (e.g. storage.updateHeartbeatTask) with the tenantId to set approval_status in a validated manner.
- **server/heartbeat.ts:3154** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — second occurrence of an UPDATE to heartbeat_tasks without tenant_id scoping in delegateTaskFromChat. This mutates a tenant-scoped row by id only.
  - _Fix:_ Ensure the UPDATE is tenant-scoped (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage layer that enforces tenant ownership when changing approval_status.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE on agent_jobs (setting status='running', lease_until, attempts, etc.) without any tenant_id constraint. This mutates rows for all tenants in a shared tenant-scoped table.
  - _Fix:_ Scope claims to an explicit tenant (e.g. accept a tenantId or claim per-tenant) or otherwise ensure the DB UPDATE includes AND tenant_id = ${tenantId}. If the worker must be multi-tenant, implement a safe per-tenant claiming loop (or an explicit platform-admin path) and audit/authorize the caller that can act across tenants.
- **server/job-queue.ts:195** — reclaimExpiredLeases first UPDATE (marking running→failed_terminal) updates agent_jobs across all tenants with no tenant_id filter.
  - _Fix:_ When reclaiming leases, either iterate per-tenant (scoping each UPDATE by tenant_id) or add an explicit tenant filter / admin-auth guard. Never perform bulk terminal/retire updates on tenant-scoped rows without explicit tenant scoping or approved platform-admin context.
- **server/job-queue.ts:209** — reclaimExpiredLeases second UPDATE (requeueing expired running jobs back to pending) updates agent_jobs without tenant_id constraint, affecting all tenants.
  - _Fix:_ Scope the requeue UPDATE to a tenant (or run per-tenant). Ensure the reclaim path cannot inadvertently requeue/modify jobs from tenants outside the intended scope unless running under a privileged, audited platform-admin routine.
- **server/job-queue.ts:238** — completeJob updates agent_jobs to 'succeeded' using only id/attempts as fencing guards; there is no tenant_id constraint on the UPDATE. A caller could complete a job id belonging to another tenant if invoked with that id/attempts.
  - _Fix:_ Require the caller to provide and validate tenantId (or re-read and assert the row's tenant_id equals the worker's execution context) and include AND tenant_id = ${tenantId} in the UPDATE. Alternatively, perform a prior SELECT of tenant_id and verify it against the worker's tenant context before completing.
- **server/job-queue.ts:298** — failJob UPDATE to mark a job failed_terminal updates agent_jobs by id/status/attempts but does NOT include tenant_id. This allows marking jobs belonging to other tenants as failed_terminal.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or require the caller to pass/verify tenantId before performing the UPDATE. Use the same lease-fencing + tenant check to prevent stale or cross-tenant writes.
- **server/job-queue.ts:318** — failJob UPDATE to requeue a job (status='pending', set next_run_at) uses id/status/attempts only and lacks tenant_id scoping, permitting cross-tenant mutation of job rows.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) or require a prior verification step that reads and confirms the job's tenant_id matches the worker's tenant context before performing the update.
- **server/job-queue.ts:336** — cancelJob updates agent_jobs (setting status='cancelled') by id and status only; no tenant_id is enforced. This lets an actor cancel jobs owned by other tenants if they can supply an id.
  - _Fix:_ Require tenantId as a parameter for tenant-scoped cancel operations and include AND tenant_id = ${tenantId} in the WHERE clause, or enforce platform-admin authorization and audit logs for cross-tenant cancellations.

### MEDIUM (12)

- **server/agentic-engines.ts:686** — autoApplyEligibleInsights builds an UPDATE on `ai_insights` where tenant filtering is conditional (tenantScope may be empty). When called with no tenantId, the UPDATE will touch rows across all tenants (UPDATE ai_insights SET status='applied' ... WHERE status='new' AND category IN (...) <no tenant constraint>).
  - _Fix:_ Make tenantId required for this function or explicitly enforce that code paths calling this intentionally pass a tenantId. If a global sweep is required, call the function per-tenant rather than issuing an unscoped UPDATE. Always include tenant_id = ${tenantId} in the WHERE clause when modifying tenant-scoped tables.
- **server/auth.ts:206** — UPDATE on `apiKeys` uses WHERE apiKeys.id = <id> without constraining tenant_id. An attacker who can influence or discover an id could update rows that belong to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) or otherwise include tenant_id in the WHERE clause so the update cannot affect keys from other tenants.
- **server/auto-asset-capture.ts:207** — Backfill SELECT from `messages` uses conversation_id only (WHERE conversation_id = ${c.id} ...) but omits tenant_id. While c was derived from an earlier query that included tenant_id, this SELECT touches a tenant-scoped table without an explicit tenant filter, increasing risk if upstream assumptions change or rows are malformed.
  - _Fix:_ Add an explicit tenant constraint to the messages query (e.g. AND tenant_id = ${c.tenant_id}) or join through the conversations table to ensure the message rows are scoped to the expected tenant before reading/writing downstream.
- **server/data-protection.ts:359** — SELECT from `project_notes` uses `WHERE project_id = ${proj.id}` but does not include an explicit tenant_id predicate. Every read against tenant-scoped tables must include an application-level tenant predicate.
  - _Fix:_ Constrain by tenant_id as well, e.g. `WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` or join through `projects` and verify `projects.tenant_id = ${tenantId}` in the same query to ensure the notes belong to the caller's tenant.
- **server/data-protection.ts:371** — SELECT from `project_files` uses `WHERE project_id = ${proj.id}` with no tenant_id predicate. This can read another tenant's project_files if numeric IDs collide or under race conditions.
  - _Fix:_ Include tenant scoping: `WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}` or join projects and assert `projects.tenant_id = ${tenantId}` in the query.
- **server/data-protection.ts:404** — SELECT COUNT(*) FROM `messages` WHERE conversation_id = ${conv.id} lacks a tenant_id predicate (called when iterating conversations). Even though `conv` was selected earlier with tenant context, this separate query must still include tenant scoping to avoid cross-tenant reads/race conditions.
  - _Fix:_ Add tenant scoping to the count: `SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}` (or `AND tenant_id = ${tenantId}`) so every read against tenant-scoped tables includes an explicit tenant predicate.
- **server/heartbeat-schedules.ts:26** — SELECT from tenant-scoped table `messages` without a tenant_id constraint (SELECT DISTINCT tenant_id FROM messages ...). This is a cross-tenant read without an explicit tenant filter.
  - _Fix:_ If you truly need a cross-tenant tenant-id enumeration, read from a dedicated platform/global tenants/metadata table instead of scanning a tenant-scoped content table. Otherwise restrict the query with WHERE tenant_id = ${tenantId}. If this is intended privileged cross-tenant behavior, centralize and document it (and ensure only an audited admin/scheduler path can call it).
- **server/heartbeat-schedules.ts:60** — SELECT from tenant-scoped table `research_schedules` without a tenant_id constraint (SELECT * FROM research_schedules WHERE ...). This returns rows for all tenants.
  - _Fix:_ If this is intended to be a cross-tenant scheduler, move this logic to an explicit scheduler boundary and document/audit it. Otherwise add WHERE tenant_id = ${tenantId} (or pass an explicit tenant list) so reads are scoped to a single tenant. Prefer reading a global scheduling index/metadata table if only discovery is required.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks is executed without a tenant_id constraint (getDueHeartbeatTasksAcrossTenants). This reads tenant-scoped heartbeat_tasks across all tenants.
  - _Fix:_ If scheduler-wide discovery is required, ensure this cross-tenant operation is only reachable from the single trusted scheduler module. If not, add WHERE tenant_id = ${tenantId} or accept an explicit tenantId parameter. Consider factoring cross-tenant scheduler queries into a clearly-audited admin boundary and/or using a global index of due tasks.
- **server/heartbeat.ts:260** — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id constraint. This reads pending events across tenants.
  - _Fix:_ If event processing must be cross-tenant, ensure this code is a controlled, audited cross-tenant consumer. Otherwise add WHERE tenant_id = ${tenantId} or iterate over an explicit list of tenant ids to process per-tenant, or maintain a global event queue that records tenant ids in a dedicated metadata index for safe enumeration.
- **server/job-queue.ts:281** — failJob first does SELECT max_attempts FROM agent_jobs WHERE id = ${id} without scoping by tenant_id. Reading tenant-scoped rows by id with no tenant constraint can be used to infer/act on other tenants' jobs.
  - _Fix:_ When reading job metadata by id, either require an explicit tenantId parameter and include WHERE tenant_id = ${tenantId} or ensure the call runs in a platform-admin context with proper auditing. Prefer a single read+write CAS that includes tenant_id for fencing.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor executes SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge GROUP BY tenant_id, persona_id — this enumerates/reads all tenants' agent_knowledge rows without scoping to a caller-supplied tenant_id.
  - _Fix:_ Either run this scan exclusively from a trusted, audited platform-admin process (and clearly document/limit its privileges), or change the design to iterate tenants and run per-tenant queries that include WHERE tenant_id = ${tenantId}. Avoid unscoped reads of tenant-scoped tables from tenant-facing code paths.

### LOW (1)

- **server/job-queue.ts:377** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} with no tenant constraint. While intended as an admin helper, it reads a tenant-scoped table by id without scoping to the caller's tenant or explicitly gating to an admin context.
  - _Fix:_ Clearly restrict this helper to platform-admin-only code paths and document/ enforce that callers are privileged. Alternatively require an explicit caller-provided tenant scope or an admin token so ordinary tenant-facing code cannot use it to enumerate or infer other tenants' job ids.
