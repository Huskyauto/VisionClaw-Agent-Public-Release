# Tenant-Isolation Audit Report

_Generated 2026-09-17T23:30:16.109Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 0 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **682/682** file(s) audited · chunks 63/63 OK (0 failed)
- Findings: **215** (CRITICAL 51 · HIGH 127 · MEDIUM 36 · LOW 1)
- Severe dispositions: **11 kept** · 129 precision false-positive · 38 allowlist false-positive · 0 deferred real risk

### CRITICAL (51)

- **server/agentic/resume-worker.ts:65** — Unscoped read from tenant-scoped tables: the main candidate query (SELECT ... FROM agent_runs LEFT JOIN agent_approvals ...) does not filter by tenant_id, so it enumerates rows across all tenants.
  - _Fix:_ Restrict the query to a single tenant (or otherwise enforce a tenant scope) instead of scanning all rows. Add a WHERE clause filtering agent_runs.tenant_id = ${tenantId} (pass tenantId into the function or worker context), or run a per-tenant worker that supplies the tenant id. Ensure every SELECT/UPDATE/DELETE on tenant-scoped tables includes a parameterized tenant_id check (e.g. WHERE eq(agent_runs.tenant_id, tenantId)).
- **server/chat-engine/project-context.ts:159** — SELECT from project_files uses only project_id and does not include tenant_id (or an equivalent tenant constraint). Although projectId was previously validated to belong to the tenant, the invariant requires each tenant-scoped table access to include an explicit tenant constraint in SQL.
  - _Fix:_ Add an explicit tenant constraint to this query, e.g. `SELECT ... FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or equivalent Drizzle where(eq(project_files.tenantId, tenantId))). Do not rely solely on earlier project validation; include tenant_id in the WHERE clause for every tenant-scoped table access.
- **server/chat-engine/project-context.ts:171** — SELECT from project_notes filters only by project_id and lacks tenant_id. This reads a tenant-scoped table (project_notes) without an explicit tenant constraint.
  - _Fix:_ Add tenant scoping to the query, e.g. `WHERE project_id = ${projectId} AND tenant_id = ${tenantId}` (or Drizzle where(eq(project_notes.tenantId, tenantId))). Ensure every read against project-scoped tables includes tenant_id.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table 'commitments' is executed without constraining by tenant_id (WHERE id = ${row.id} only). This allows modifying a row across tenants if the row.id comes from another tenant.
  - _Fix:_ Include the tenant filter on the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or avoid issuing the UPDATE when tenantId is unknown/invalid. In this error/unclaim path, fail-closed: do not perform an unscoped mutation. Ensure every UPDATE/DELETE against tenant-scoped tables includes AND tenant_id = <caller tenant>.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} — missing tenant_id filter. This DELETE can remove another tenant's chunks if IDs/paths collide.
  - _Fix:_ Include tenant scoping on the DELETE (e.g. add AND tenant_id = ${tenantId}), or delete by (collection_id, doc_path, tenant_id) to ensure only the intended tenant's rows are affected.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} — update uses only id and omits tenant_id. This can modify another tenant's chunk if row.id is ambiguous across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or update by collection_id + chunk id with tenant_id to ensure the update affects only the intended tenant's row.
- **server/embeddings.ts:809** — storeEmbeddingVec fallback branch updates a tenant-scoped table without tenant constraint: `UPDATE ${sql.raw(table)} ... WHERE id = ${id}` (no tenant_id). This allows modifying rows belonging to any tenant when options.tenantId is omitted.
  - _Fix:_ Do not perform UPDATEs on tenant-scoped tables without an explicit, validated tenantId. Require a positive tenantId parameter for tenant-scoped tables and include `AND tenant_id = ${tenantId}` in the WHERE clause. If a tenantId is not provided, throw rather than executing an unscoped UPDATE.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE on agent_jobs (claiming due jobs) without constraining by tenant_id. The inner SELECT used to pick rows also has no tenant filter. This updates/returns rows across all tenants.
  - _Fix:_ If this is intended to operate per-tenant, scope the claim to a tenant (e.g. add AND tenant_id = ${tenantId} into the inner SELECT / outer UPDATE). If it must be global, ensure this is an explicit, auditable admin/system-only path and enforce authorization and tenant-scoping at the application boundary (or derive and validate tenant before mutating). Prefer resolving the job's tenant first and then doing a tenant-scoped UPDATE to avoid accidental cross-tenant claims.
- **server/job-queue.ts:196** — reclaimExpiredLeases STEP 1: UPDATE agent_jobs to failed_terminal for expired leases executes without any tenant_id constraint, mutating rows from all tenants.
  - _Fix:_ Scope the UPDATE to a specific tenant when invoked in a tenant-scoped context (add AND tenant_id = ${tenantId}), or ensure the function is explicitly an admin/system operation protected by authorization. If running globally, perform per-tenant iterations with explicit tenant context to avoid accidental cross-tenant modifications.
- **server/job-queue.ts:209** — reclaimExpiredLeases STEP 2: UPDATE agent_jobs to requeue expired leases runs without tenant_id constraint and will affect rows across tenants.
  - _Fix:_ Same fix as above — either make this per-tenant (AND tenant_id = ${tenantId}) or ensure it's only runnable by a trusted admin/system process and implemented as an explicit per-tenant loop so tenant isolation isn't violated implicitly.
- **server/job-queue.ts:238** — completeJob UPDATE on agent_jobs updates status/completion by id/attempts but does not include tenant_id in the WHERE clause. A stale/borrowed writer could complete a job for the wrong tenant.
  - _Fix:_ Require the caller to provide the persisted tenantId (or resolve it before the UPDATE) and include AND tenant_id = ${tenantId} in the WHERE clause to preserve tenant isolation. Alternatively, read the job's tenant first and fence the write to that tenant id.
- **server/job-queue.ts:298** — failJob UPDATE to mark a job failed_terminal updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but lacks tenant_id constraint — allowing cross-tenant mutation.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (resolving tenantId from the persisted row or having the caller pass and validate it). Ensure any caller-supplied tenantId is verified against the stored job to prevent payload-based scope escalation.
- **server/job-queue.ts:318** — failJob's non-terminal retry UPDATE (setting pending/next_run_at) updates agent_jobs without a tenant_id constraint in the WHERE clause.
  - _Fix:_ As with other mutators, require tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}) or resolve the job's tenant prior to updating. Ensure lease-fencing plus tenant scoping to prevent stale workers from touching other tenants' rows.
- **server/job-queue.ts:335** — cancelJob performs an UPDATE on agent_jobs by id/status without any tenant_id constraint, allowing cancelling jobs across tenants by id.
  - _Fix:_ Require the tenant (or admin assertion) and include AND tenant_id = ${tenantId} in the WHERE clause. For admin operator APIs that act cross-tenant, enforce explicit authorization checks and audit logging, and prefer a dedicated admin path that first reads the job's tenant then performs a tenant-scoped update.
- **server/lib/ideabrowser-backfill.ts:101** — SELECT from project_files lacks an explicit tenant_id constraint (WHERE tenant_id = <tenant>) — query filters only by project_id and file_name.
  - _Fix:_ Constrain reads to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or include tenant_id in the JOIN/predicate. Prefer an explicit tenant check instead of relying on project_id alone (there is no RLS/foreign-key cascade guarantee).
- **server/lib/ideabrowser-backfill.ts:116** — SELECT from project_notes lacks an explicit tenant_id constraint — it filters only by project_id and note.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent) to the WHERE clause when selecting project_notes, or otherwise ensure the read is scoped to the caller's tenant before returning results.
- **server/lib/scheduled-post-runner.ts:61** — SELECT ... FROM scheduled_posts executed without any tenant_id filter (CTE selects due rows across all tenants and then updates them). This allows cross-tenant locking/processing of scheduled_posts in a single operation.
  - _Fix:_ If this runner must operate per-tenant, add an explicit tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}) or iterate per-tenant and scope the SELECT/UPDATE to that tenant. If cross-tenant admin behavior is intentional, make that explicit and restrict/authorize the caller to an admin context and add best-effort auditing. Avoid acquiring FOR UPDATE locks across all tenants in a single uncontrolled query.
- **server/oauth-subscriptions.ts:906** — SELECT reads all rows from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This returns sensitive per-tenant tokens/metadata across all tenants.
  - _Fix:_ If this is supposed to operate per-tenant, add a WHERE tenant_id = ${tenantId} filter. If the code must run across tenants (admin/system task), make the intent explicit, reduce the selected columns (never select access_token/refresh_token in bulk), and gate the operation behind a strict admin-only check and audit logging. Consider iterating a vetted tenant list rather than selecting entire table.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' queries a tenant-scoped table without filtering by tenant_id, enumerating tenants that have subscriptions.
  - _Fix:_ Avoid scanning tenant-scoped tables without explicit admin authorization. If you need tenant ids, build them from a safe, explicit source or add strict admin checks. Prefer queries that include WHERE tenant_id = ${tenantId} when running in a tenant context, or otherwise limit returned columns and document/secure the cross-tenant operation.
- **server/oauth-subscriptions.ts:1258** — proactiveTokenRefresh executes SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE without a tenant_id constraint, reading subscription rows for all tenants (sensitive data exposure and cross-tenant action).
  - _Fix:_ If this background job is intended to refresh tokens for all tenants, explicitly require a privileged admin context and minimize selected sensitive fields (do not bulk-select tokens). If it should be tenant-scoped, add WHERE tenant_id = ${tenantId} or iterate an explicitly-allowed tenant list. Log and audit any cross-tenant refresh activity.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions runs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant filter, then attempts refreshes across tenants. This performs cross-tenant reads/writes on a tenant-scoped table without explicit admin/authorization checks.
  - _Fix:_ Require an explicit admin-only pathway and audit when performing global operations. If intended to be per-tenant, add WHERE tenant_id = ${tenantId}. Avoid scanning the entire table without safeguards; restrict columns and ensure actions that update tenant-scoped rows include tenant_id and are performed under an approved admin context.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} does not verify tenant ownership of the persona. The handler reads a tenant-scoped row (persona) by id provided by the caller without constraining by tenant_id, risking cross-tenant data exposure.
  - _Fix:_ Constrain persona fetch to the caller's tenant: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. Validate that the returned persona belongs to the requesting tenant and fail if not. Alternatively, require and propagate an authenticated tenantId and use it for all persona lookups.
- **server/plan-executor.ts:610** — UPDATE on tenant-scoped table 'plans' performed without constraining by tenant_id (CAS claim: `WHERE id = ${planId} AND status = 'approved'`). This lets the executor change a plan row by id across tenants without an explicit tenant constraint.
  - _Fix:_ Constrain the claim update to the plan's tenant. Either (A) first SELECT the plan row (id) to get its tenant_id (SELECT ... FOR UPDATE) and then use that tenant_id in the UPDATE WHERE clause, or (B) require an explicit caller tenantId and include `AND tenant_id = ${tenantId}` in the WHERE. Keep this check inside the same transaction to avoid TOCTOU races.
- **server/process-governor.ts:899** — UPDATE against tenant-scoped table `agent_desks` does not constrain by tenant_id (WHERE id = ${desk.id} only). This allows modifying rows from other tenants if id values collide.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId}. Alternatively, verify the row belongs to the tenant before updating (SELECT ... WHERE id = ${desk.id} AND tenant_id = ${tenantId}).
- **server/process-governor.ts:1235** — UPDATE on `governance_rules` (setting last_triggered_at/trigger_count) lacks a tenant_id constraint (WHERE id = ${rule.id}). This can increment trigger counts for rules belonging to other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE governance_rules SET ... WHERE id = ${rule.id} AND tenant_id = ${tenantId}. Ensure rule.tenantId is used or passed into logAction to prevent cross-tenant writes.
- **server/project-brain.ts:135** — SELECT from `projects` by id (SELECT name, status, description FROM projects WHERE id = ${projectId}) has no tenant_id constraint. The function accepts projectId and may read another tenant's project.
  - _Fix:_ Require and validate tenant context for updateProjectBrain (add tenantId parameter) and scope the query: SELECT ... FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId} OR verify project ownership before proceeding.
- **server/project-brain.ts:332** — Backfill query selects projects and joins conversations without any tenant filtering (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...). This can read all tenants' data.
  - _Fix:_ Scope the backfill by tenant_id (e.g. WHERE p.tenant_id = ${tenantId}) or run backfill on a per-tenant basis after verifying tenant ownership; add tenant_id filters to joins and subqueries (conversations, messages).
- **server/project-brain.ts:349** — Querying conversations by project_id (SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}) lacks tenant scoping. project_id alone is insufficient to ensure the conversation belongs to the expected tenant.
  - _Fix:_ Include tenant filtering (e.g. WHERE c.project_id = ${proj.id} AND c.tenant_id = ${tenantId}) or verify the parent project's tenant before enumerating conversations.
- **server/project-brain.ts:359** — Selecting messages by conversation_id (SELECT role, content FROM messages WHERE conversation_id = ${conv.id}) has no tenant constraint. This can expose messages from other tenants when conversation ids are global or not validated.
  - _Fix:_ Include tenant filtering on messages (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or otherwise verify that the conversation belongs to the tenant before reading its messages.
- **server/recurring-messages.ts:307** — UPDATE against tenant-scoped table `agent_knowledge` omits tenant_id in WHERE clause: `UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}`. This can modify another tenant's row if IDs collide or if the caller has access to rows across tenants.
  - _Fix:_ Constrain the UPDATE to the tenant explicitly (e.g. add `AND tenant_id = ${r.tenant_id}`) or use Drizzle's .where(eq(agentKnowledge.tenantId, r.tenant_id)) so every write is scoped to the row's tenant. If this path is intentionally global, ensure the code verifies ownership per-row before any write and include tenant_id in the WHERE to make the mutation atomic and tenant-scoped.
- **server/reference-learner.ts:296** — UPDATE against tenant-scoped table `memory_entries` omits tenant_id in WHERE clause: `UPDATE memory_entries SET fact = ..., WHERE id = ${storedId}`. After a tenant-scoped SELECT, the code updates by id only without verifying tenant ownership in the update, risking cross-tenant modification.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. `WHERE id = ${storedId} AND tenant_id = ${input.tenantId}`) or use Drizzle's .where(eq(memoryEntries.tenantId, input.tenantId), eq(memoryEntries.id, storedId)). Always verify ownership or include tenant_id in the write's WHERE to enforce tenant isolation.
- **server/research-engine.ts:170** — UPDATE against tenant-scoped table research_sessions lacks a tenant_id constraint (cleanupZombieSessions). This UPDATE can modify rows across all tenants.
  - _Fix:_ Restrict the UPDATE to a single tenant (or iterate per-tenant) by adding a WHERE tenant_id = <tenantId> clause or otherwise scoping the operation. If the intent is global maintenance, ensure the operation is explicitly authorized and audited; prefer per-tenant cleanup jobs or a safe admin-only path to avoid cross-tenant mutations.
- **server/routes/conversations.ts:123** — INSERT into a likely tenant-scoped join table 'project_conversations' omits tenant_id. The query `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT and column list (and pass the authenticated tenantId), e.g. `INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${projectId}, ${conv.id}, ${tenantId})`. Ensure the storage schema expects tenant_id and populate it on all inserts.
- **server/routes/conversations.ts:256** — INSERT into a tenant-scoped table 'project_notes' omits tenant_id. The query `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id.
  - _Fix:_ Include tenant_id explicitly in the INSERT and column list, e.g. `INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${projId}, ${note.slice(0,8000)}, ${'system:archive'}, ${tenantId})`. Ensure all INSERTs into tenant-scoped tables set tenant_id from the authenticated context.
- **server/sculptor.ts:105** — UPDATE to sculptor_sessions omits tenant constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This allows cross-tenant writes if session.id is forged or collides.
  - _Fix:_ Include the tenant filter in the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or otherwise assert/propagate the tenant and use it in the update to enforce tenant isolation.
- **server/sculptor.ts:113** — UPDATE to sculptor_sessions omits tenant constraint (UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}). This can modify a session belonging to another tenant.
  - _Fix:_ Add AND tenant_id = ${params.tenantId} to the WHERE clause (or pass and use a scoped tenant variable) so writes are constrained to the caller's tenant.
- **server/sculptor.ts:168** — UPDATE to sculptor_sessions performed in monitorSession lacks a tenant_id filter (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). monitorSession accepts tenantId but does not use it in this UPDATE, enabling cross-tenant modification.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) so only the owning tenant's rows are updated; fail or log if the update affects 0 rows to detect mismatches.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} omits tenant constraint. This write could overwrite another tenant's session review_result.
  - _Fix:_ Include tenant_id = ${tenantId} (or otherwise derived/validated tenant) in the WHERE clause to enforce tenant-scoped writes (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}).
- **server/second-opinion.ts:417** — settleFusionReservation updates agent_cost_ledger without scoping to tenant (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}). The ledger is tenant-scoped; updating by id alone can alter another tenant's ledger row.
  - _Fix:_ Require and pass the tenantId into settleFusionReservation and include AND tenant_id = ${tenantId} in the WHERE clause (or otherwise validate the reservation row belongs to the expected tenant) before performing the UPDATE.
- **server/second-opinion.ts:438** — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without tenant constraint. This can release/modify reservations for other tenants.
  - _Fix:_ Accept/derive tenantId in releaseFusionReservation and update WHERE id = ${reservationId} AND tenant_id = ${tenantId}; validate the row belongs to the tenant before mutating it.
- **server/self-improvement.ts:427** — UPDATE on tenant-scoped table "experiments" is executed without constraining by tenant_id (.where(eq(experiments.id, experiment.id)) only). This allows an experiment row from another tenant to be modified.
  - _Fix:_ Include the tenant filter in the WHERE clause. e.g. .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId))). Prefer Drizzle query builders rather than raw SQL templates and validate the returning row belonged to the caller's tenant.
- **server/storage.ts:976** — archiveExpiredMemories() performs a global UPDATE on memory_entries (status -> 'archived') with no tenant_id constraint, mutating rows across all tenants.
  - _Fix:_ Restrict archival to a specific tenant by requiring a validated tenantId and adding eq(memoryEntries.tenantId, tenantId) to the WHERE clause, or run the sweep per-tenant with an explicit admin-only code path. Do NOT run blanket, unscoped UPDATEs against tenant-scoped tables.
- **server/storage.ts:991** — archiveStaleMemories() issues a global UPDATE on memory_entries to 'archived' without any tenant filter, potentially modifying all tenants' data.
  - _Fix:_ Require an explicit tenant scope and add tenant predicate (eq(memoryEntries.tenantId, tenantId) or requiredTenantScope) or perform per-tenant updates in a controlled admin-only procedure. Fail-closed rather than issuing unscoped updates.
- **server/storage.ts:1402** — getAllDataForExport() performs many SELECTs across tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, etc.) with no tenant filtering, returning platform-wide tenant data.
  - _Fix:_ This function must be restricted to an explicit admin-only context and require an explicit ALL_TENANTS sentinel or per-tenant parameter. Otherwise, refactor to accept a tenantId and apply requiredTenantScope on all tenant-scoped queries. Add runtime guards so non-admin callers cannot call unscoped exports.
- **server/tools.ts:2931** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without an explicit tenant_id column/value, relying on project_id alone — violating the invariant that every INSERT into a tenant-scoped table must pass tenant_id explicitly.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId}) ) or otherwise populate the tenant_id column from the caller's trusted tenant context. Do not rely solely on project_id FK joins for ownership enforcement; per the contract every tenant-scoped table row must carry the tenant_id provided by the caller.
- **server/tools.ts:2946** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) ... lacks tenant_id, allowing creation of rows without explicit tenant scoping.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})) or otherwise ensure tenant_id is explicitly written from the authenticated caller's tenant. Validate ownership of the referenced project before inserting.
- **server/tools.ts:2806** — INSERT into tenant-scoped table `project_conversations` (in project create flow) does not set tenant_id. The code INSERTs (project_id, conversation_id) ON CONFLICT DO NOTHING without including tenant_id, which can result in rows that lack explicit tenant scoping.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ${tenantId})) and/or ensure the tenant_id column is populated from the caller's trusted tenant context. Prefer explicit tenant field on every tenant-scoped INSERT rather than inferring from related rows.
- **server/tools.ts:2969** — INSERT into tenant-scoped table `project_conversations` (link_conversation handler) does not set tenant_id. The code inserts (project_id, conversation_id) without an explicit tenant_id even though a tenant check was performed earlier — the row itself is still created without tenant_id.
  - _Fix:_ Write tenant_id explicitly in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Even when ownership is asserted separately, every tenant-scoped table insert must include the caller's tenant_id to satisfy application-level isolation guarantees.
- **server/venture-discovery/budget.ts:155** — UPDATE against tenant-scoped table agent_cost_ledger uses only WHERE id = ${reservationId} with NO tenant_id constraint — this allows modifying reservation rows belonging to other tenants.
  - _Fix:_ Make settleVentureReservation require and accept the caller's tenantId (or fetch+verify the row's tenant before update) and include tenant_id in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or use a Drizzle .where(eq(agent_cost_ledger.tenantId, tenantId)) to enforce ownership atomically.
- **server/venture-discovery/budget.ts:169** — UPDATE against tenant-scoped table agent_cost_ledger uses only WHERE id = ${reservationId} with NO tenant_id constraint — this allows releasing (setting cost → 0) reservation rows for other tenants.
  - _Fix:_ Make releaseVentureReservation require and accept the caller's tenantId (or fetch+verify the row's tenant before update) and include tenant_id in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}), or use a Drizzle .where(eq(agent_cost_ledger.tenantId, tenantId)) to enforce ownership atomically.
- **server/webhookHandlers.ts:539** — Writes tenants row using tenant id taken directly from Stripe session.metadata (untrusted) without verifying that the Stripe subscription/customer actually belongs to that tenant. An attacker controlling Stripe-side metadata could cause plan/stripe_customer_id/stripe_subscription_id to be applied to an arbitrary tenant.
  - _Fix:_ Do not trust metadata. Verify ownership before mutating tenant rows: resolve the subscription/customer via the Stripe API and map it to the intended tenant (e.g. lookup tenant by subscription.customer or by a server-side binding), or require that the subscription's customer matches the tenant's stored stripe_customer_id. Only then run UPDATE tenants ... WHERE id = <verified tenant id> (or prefer an UPDATE by tenant selected via a verified stripe_customer_id lookup).

### HIGH (127)

- **server/ab-optimizer.ts:110** — runDueAbExperiments selects from experiments without constraining to a tenant (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This exposes metadata from all tenants to the heartbeat caller.
  - _Fix:_ Scope the query to a single tenant before reading tenant-scoped rows (e.g. accept/derive a tenantId and add WHERE tenant_id = ${tenantId}), or iterate per-tenant and run the selection per-tenant. Never SELECT tenant-scoped rows across all tenants in application code unless explicitly justified and guarded.
- **server/agentic-engines.ts:161** — retryPendingMinervaRouting SELECTs ai_insights without a tenant filter (SELECT id, tenant_id, category, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ...). This reads rows for all tenants.
  - _Fix:_ Scope the read by tenant (e.g. accept a tenantId and include WHERE tenant_id = ${tenantId}), or implement an explicit admin-only path that is audited and safe. At minimum, avoid blind cross-tenant SELECTs; validate tenant_id before using or returning rows.
- **server/agentic-engines.ts:248** — autoApplyEligibleInsights can perform a global UPDATE on ai_insights when tenantId is omitted (UPDATE ... WHERE status = 'new' AND category IN (...) ${tenantScope}). When tenantScope is empty this updates rows across all tenants.
  - _Fix:_ Require an explicit tenantId for this operation or ensure tenant_scope is always populated. If a global sweep is intended, make it an explicit, audited admin operation and add safeguards (per-tenant iteration, logging, operator confirmation). Avoid implicit global UPDATEs on tenant-scoped tables.
- **server/agentic-features.ts:861** — enrollInSequence checks for existing enrollments using sequence_id and contact_email without scoping by tenant (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'). This can leak/affect enrollments across tenants and allow cross-tenant conflicts.
  - _Fix:_ Include tenant ownership in the check (AND tenant_id = ${params.tenantId}) so the duplicate-check is strictly tenant-scoped before inserting. Always verify foreign/resource ids belong to the caller's tenant when using them in reads or writes.
- **server/agentic/approvals.ts:134** — expireStaleApprovals performs a bulk UPDATE on agentApprovals with no tenant constraint (UPDATE agentApprovals SET status = 'expired' WHERE status = 'pending' AND expiresAt < ...). This will change approvals across all tenants.
  - _Fix:_ Scope expiry to a tenant (accept a tenantId to expire per-tenant) or iterate per-tenant explicitly. At minimum include tenant constraints when updating tenant-scoped tables in bulk, and ensure downstream run updates are applied only for the matching tenant.
- **server/agentic/department-budgets.ts:143** — Query selects from tenant-scoped table `department_budgets` without constraining by tenant_id: `SELECT DISTINCT department FROM department_budgets WHERE status = 'active'`. Later code uses `row.tenant_id` but the SELECT did not include tenant_id; this can lead to incorrect/undefined tenant values and cross-tenant information access/leakage.
  - _Fix:_ Restrict the query to a tenant or explicitly return tenant_id. If this sweep is intended per-tenant, add `WHERE tenant_id = ${tenantId} AND status = 'active'`. If it must scan all tenants, SELECT tenant_id as well: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` and then pass the explicit tenant_id into subsequent calls. Ensure every read/write against tenant-scoped tables includes an explicit `tenant_id = <tenant>` filter (or otherwise validates the tenant) before using results.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on agent_wake_schedules claims pending wakes using an inner SELECT that does not constrain by tenant_id (no WHERE tenant_id = ...). This allows the scheduler to atomically claim and mutate rows across all tenants.
  - _Fix:_ Either scope the claim to a specific tenant (add AND tenant_id = ${tenantId} in the inner SELECT/outer UPDATE), or iterate tenants and run a per-tenant claim; ensure any row-locking SELECT/UPDATE always includes tenant_id = <current tenant> so the app-level filter enforces tenant isolation.
- **server/business-tools.ts:95** — INSERT into invoice_items does not include tenant_id. This writes tenant-scoped data (invoice line items) without explicitly setting tenant_id, violating the invariant that every INSERT into tenant-scoped tables must include the caller's tenant and risking cross-tenant data leakage.
  - _Fix:_ Either add tenant_id to the invoice_items table INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)) or explicitly validate the parent invoice belongs to the caller's tenant before inserting and then set tenant_id = ${tid}. Also audit any subsequent updates/deletes/reads on invoice_items to ensure they are constrained by tenant_id in WHERE clauses.
- **server/channel-routing.ts:31** — listChannelRoutes joins the personas table (tenant-scoped) but does not constrain the join by tenant_id. This reads tenant-scoped persona rows without any tenant filter, allowing cross-tenant data exposure.
  - _Fix:_ Make channel routing tenant-aware: add a tenant_id column to channel_routes (or accept a tenantId parameter) and include WHERE clauses or JOIN predicates that constrain personas by tenant_id (e.g. JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}). Alternatively, validate the caller's tenant before returning persona_name.
- **server/channel-routing.ts:54** — setChannelRoute inserts persona_id provided by the caller into the global channel_routes table without validating that the persona belongs to the caller's tenant. This allows one tenant to point a route at another tenant's persona.
  - _Fix:_ Require and pass the caller's tenantId into setChannelRoute, and before inserting validate ownership (SELECT id FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}). Better: make channel_routes tenant-scoped (add tenant_id column) and INSERT tenant_id explicitly, and enforce a constraint/validation that persona_id belongs to the same tenant.
- **server/chat-engine.ts:3804** — Telemetry helper markReviewReused(conversationId, toolName) is invoked with only conversationId (no tenant context). If that helper performs any DB writes or reads against tenant-scoped tables it has no tenant_id to constrain queries — risking cross-tenant writes/reads or inserts that omit tenant_id.
  - _Fix:_ Pass the caller tenantId into markReviewReused (e.g. markReviewReused(conversationId, toolName, tenantId)) and update the helper to use tenantId in all SQL/Drizzle queries (WHERE tenant_id = ...) or validate ownership of conversationId before any access. If the helper must accept just conversationId, have it perform an explicit ownership lookup (with tenant filter) before any read/write, and always include tenant_id on INSERTs.
- **server/chat-engine.ts:4146** — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) (and the subsequent postDiagnosticReport call) are invoked without any tenant identifier. If recordToolCallForStuckDetection/postDiagnosticReport persist diagnostics to tenant-scoped tables, those operations will lack a tenant_id constraint (or may insert rows missing tenant_id).
  - _Fix:_ Modify recordToolCallForStuckDetection and postDiagnosticReport calls to receive and use the tenantId (e.g. recordToolCallForStuckDetection(tenantId, conversationId, toolName, ...)), and change their implementations to include WHERE tenant_id = ? on reads/updates and to set tenant_id on INSERTs. Alternatively, perform an explicit ownership check for conversationId using the tenant before calling these functions.
- **server/chat-engine/project-context.ts:23** — SELECT from project_conversations (`pc`) is executed without constraining pc.tenant_id to the caller's tenant. The query joins projects and filters p.tenant_id, but it still reads rows from project_conversations without an explicit tenant filter.
  - _Fix:_ Include an explicit tenant filter on project_conversations, e.g. add `AND pc.tenant_id = ${tenantId}` (or equivalent Drizzle where(eq(project_conversations.tenantId, tenantId))). Do this everywhere project_conversations is read so every tenant-scoped table row read is explicitly tenant-scoped.
- **server/chat-engine/project-context.ts:44** — EXISTS(...) subquery reads project_conversations (`pc`) without constraining pc.tenant_id. This lets the EXISTS test touch a tenant-scoped table without an explicit tenant filter.
  - _Fix:_ Constrain the subquery to the caller's tenant by adding `AND pc.tenant_id = ${tenantId}` (or using Drizzle's eq) inside the EXISTS condition, ensuring the project_conversations rows are tenant-scoped.
- **server/chat-engine/project-context.ts:181** — Query joining project_conversations (pc) and conversations (c) filters c.tenant_id but does not include a tenant constraint on pc. This reads project_conversations without explicitly scoping it to the tenant.
  - _Fix:_ Constrain project_conversations in the WHERE clause as well, e.g. `AND pc.tenant_id = ${tenantId}` (or where(eq(project_conversations.tenantId, tenantId))). Apply the same tenant-scoping pattern to all joins that touch tenant-scoped tables.
- **server/cmmc-assessments.ts:150** — getCmmcAccessByToken performs a SELECT joining cmmc_assessment_invitations and cmmc_assessments and filters by token/expires/revoked but does not include an explicit tenant_id constraint on the tenant-scoped tables. This reads tenant-scoped tables without an explicit tenant filter.
  - _Fix:_ Require and/or validate tenant ownership when resolving token-based access, or include an explicit tenant constraint in the query. For example, change the lookup to accept an expected tenantId (if available) and add `AND cmmc_assessment_invitations.tenant_id = ${tenantId}` (and/or similar eq checks) to the WHERE clause, or otherwise validate the resolved assessment.invitation pair maps to the intended tenant before returning it.
- **server/commitments.ts:135** — SELECT from tenant-scoped table 'commitments' is performed without any tenant_id constraint (SELECT ... FROM commitments WHERE status = 'active' ...). This reads rows across all tenants into application memory.
  - _Fix:_ If this is intended only for a privileged admin digest, enforce explicit admin-only access control at the call site and document/justify the global read. Otherwise, scope the query by tenant_id (WHERE tenant_id = ${tenantId}) or enumerate tenants first and run per-tenant queries. Avoid blanket cross-tenant reads from tenant-scoped tables unless strictly necessary and access-controlled.
- **server/data-protection.ts:150** — SELECT FROM tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Fix:_ Restrict the query to a single tenant (e.g. WHERE tenant_id = ${someTenantId} AND deleted_at IS NOT NULL ...), or run this purge in a privileged admin context that explicitly documents and authorizes cross-tenant operations. Prefer per-tenant iteration or use withTenantTx to bind tenant context before selecting rows.
- **server/data-protection.ts:403** — SELECT COUNT(*) FROM `messages` with only conversation_id (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) and no tenant_id predicate. This can act as a cross-tenant existence/oracle and leak information about other tenants' conversations.
  - _Fix:_ Scope the COUNT to the tenant (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenant.tenant_id || tenantId}), or use the tenant_id obtained from the earlier conversation row when counting messages to ensure the read is constrained to that tenant.
- **server/dfy-intake.ts:52** — Select from tenant-scoped table dfy_intake_forms by token without constraining by tenant_id (no WHERE tenant_id = ...). This lets the token lookup return rows across tenants.
  - _Fix:_ Constrain the lookup to the owning tenant (e.g. accept/derive a tenantId and add AND dfy_intake_forms.tenant_id = ${tenantId} to the WHERE clause) or otherwise ensure the token lookup is scoped per-tenant (composite unique token+tenant and include tenant in the query).
- **server/doc-collections.ts:136** — SELECT in listCollections uses subselects like (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id) without tenant_id filtering. Those subqueries read a tenant-scoped table (doc_chunks) without an explicit tenant constraint.
  - _Fix:_ Add tenant_id filtering inside the subselects (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) or rewrite the query to join doc_chunks with an explicit dc.tenant_id = ${tenantId} predicate so all reads are tenant-scoped.
- **server/doc-collections.ts:458** — getCollectionStatus SELECT includes subselects (COUNT/SUM) over doc_chunks using WHERE collection_id = c.id but no tenant_id filter, so doc_chunks reads are not explicitly tenant-scoped.
  - _Fix:_ Add tenant scoping to those subselects (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) or JOIN doc_chunks with an explicit tenant predicate so counts/sums reflect only the requested tenant's data.
- **server/email.ts:370** — SELECT on inbox_messages missing tenant filter: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1` executes without constraining by tenant_id, allowing cross-tenant discovery/matches.
  - _Fix:_ Constrain the lookup by the owning tenant. Determine the tenant for this message (e.g. via inbox mapping or by resolving inbox->tenant) and add `AND tenant_id = ${tenantId}` (or use a tenant-bound lookup function). Avoid global SELECTs on tenant-scoped tables.
- **server/email.ts:415** — Backfill query and update operate on inbox_messages without tenant scoping: `SELECT id, message_id FROM inbox_messages ...` and later `UPDATE inbox_messages SET ... WHERE id = ${row.id}` lack tenant_id constraints, so reads and writes may touch rows across tenants.
  - _Fix:_ Run backfill per-tenant (or include a WHERE tenant_id = ${tenantId}) and include tenant_id in the UPDATE WHERE clause. Ensure every read/update on inbox_messages includes the tenant_id of the owner before modifying rows.
- **server/embeddings.ts:845** — Backfill reads/updates memory_entries without tenant_id filtering: `SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200` and subsequent `UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}` are performed across all tenants.
  - _Fix:_ Do backfill on a per-tenant basis (iterate tenants or accept tenantId argument) and include `AND tenant_id = ${tenantId}` in SELECTs and `AND tenant_id = ${tenantId}` in UPDATEs. Never read/update memory_entries across tenants without explicit tenant scoping.
- **server/embeddings.ts:814** — BackfillMissingKnowledgeEmbeddings selects agent_knowledge rows across all tenants: `SELECT id, tenant_id, title, content FROM agent_knowledge WHERE ... LIMIT 50` reads tenant-scoped data without constraining to a tenant context.
  - _Fix:_ Perform knowledge backfills per-tenant (or otherwise ensure an explicit, validated tenantId filter). Replace global SELECT with tenant-scoped SELECT (e.g. `AND tenant_id = ${tenantId}`) or iterate tenants and call the backfill for each tenant.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table 'agent_runs' without constraining tenant_id in the WHERE clause (reads rows across all tenants).
  - _Fix:_ Restrict the query to an explicit tenant context or make the admin/global intent explicit and gated. For example, accept a tenantId parameter and add WHERE tenant_id = ${tenantId}, or if the process truly must scan all tenants, require an explicit admin-only path (with environment guard and audit logging) and document that this is an intentional global operation. Avoid issuing unfiltered selects against tenant-scoped tables.
- **server/google-drive.ts:866** — SELECT from tenant-scoped 'projects' table filters only by project id (WHERE projects.id = $projectId) and does not include the caller's tenant_id constraint.
  - _Fix:_ Constrain the SELECT to the caller's tenant (e.g. add AND eq(projects.tenantId, tenantId) or include the tenantId in the WHERE clause). Alternatively, use a central assertProjectInTenant(projectId, tenantId) helper before running the query and still include the tenant predicate in the SQL to make the statement self-contained and avoid TOCTOU/IDOR risks.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped 'projects' table (setting driveFolderId/driveFolderUrl) uses WHERE projects.id = $projectId only and lacks a tenant_id constraint.
  - _Fix:_ Include the tenant constraint in the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))) or otherwise ensure the WHERE clause verifies ownership) so the update cannot affect another tenant's project row.
- **server/google-drive.ts:1473** — SELECT used to resolve a project in uploadAndShare queries projects by id only (WHERE projects.id = $projectId) without scoping the statement to the caller's tenant.
  - _Fix:_ Add tenant scoping to the SELECT (e.g. WHERE projects.id = ${projectId} AND projects.tenant_id = ${params.tenantId}) or call a tenant-assert helper first and still keep the tenant predicate in the SQL to avoid cross-tenant reads.
- **server/health-audit.ts:314** — SELECT from code_proposals is not tenant-scoped — query lacks a WHERE tenant_id = ... constraint. This reads pending/proposed/unverified proposals across all tenants.
  - _Fix:_ Restrict the SELECT to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or require and enforce an explicit admin-only context. If the intent is to operate per-tenant, accept a tenantId parameter and filter all queries by it. If the intent is an admin/global sweep, require an explicit admin authorization check and record that intent clearly.
- **server/health-audit.ts:333** — UPDATE of code_proposals sets status = 'archived_stale' using WHERE id = ANY(...) without constraining tenant_id. This can modify rows from any tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or otherwise ensure the ids list is pre-filtered to only include rows belonging to the target tenant or that the operation is explicitly run under an admin-only context with explicit checks.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks does not filter by tenant_id. heartbeat_tasks appears to be schedule/task data which is likely tenant-scoped; this query will surface tasks for all tenants.
  - _Fix:_ Scope the query to a tenant (add WHERE tenant_id = ${tenantId}) or add an explicit admin guard if the operation must examine global tasks. Prefer passing tenantId into the audit routine and using WHERE tenant_id = ${tenantId} for safety.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a write with no tenant constraint — it may disable tasks across tenants.
  - _Fix:_ Constrain the UPDATE to only affect rows for the intended tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or verify the ids are limited to the tenant before updating. If this is intended as an admin/global action, require an explicit admin authorization flow and audit log the change.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules is executed with no tenant_id constraint. This reads tenant-scoped schedule rows across tenants without an explicit tenant filter.
  - _Fix:_ Either restrict the query to a single tenant (WHERE tenant_id = ${tenantId}) or, if this is intentionally a cross-tenant scheduler, ensure it's clearly executed under a documented admin context and/or use a dedicated scheduler API that asserts admin privileges and documents safe cross-tenant behavior. Prefer using the storage layer that centralizes and documents cross-tenant access rules.
- **server/heartbeat-task-scheduler-storage.ts:11** — getDueHeartbeatTasksAcrossTenants performs SELECT * FROM heartbeat_tasks ... with no tenant_id constraint. This reads tenant-scoped heartbeat_tasks without scoping to a tenant.
  - _Fix:_ If the routine must enumerate tasks across tenants, isolate and document this as an admin/scheduler-only API. Otherwise add WHERE tenant_id = ${tenantId} or switch to a storage method that enforces tenant scoping. Ensure any subsequent updates include tenant_id in the WHERE as well.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... is executed without a tenant_id constraint. Updating tenant-scoped rows without tenant_id allows cross-tenant modification if ids are manipulated or assumptions change.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or require the caller to provide and assert the task's tenant before performing the UPDATE. Alternatively provide a scheduler-specific storage API that atomically claims tasks and enforces correct tenant binding.
- **server/heartbeat.ts:257** — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id constraint. This reads pending events across tenants and then processes them; initial read is not tenant-scoped.
  - _Fix:_ Either constrain the SELECT to a tenant (WHERE tenant_id = ${tenantId}) or, if this is intentionally cross-tenant processing, make the admin intent explicit and ensure the code performing processing always verifies tenant_id on subsequent mutations (and documents this cross-tenant privilege). Prefer using an admin-only storage helper that centralizes and auditable cross-tenant reads.
- **server/heartbeat.ts:1579** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... is executed without a tenant_id constraint. This can return another tenant's heartbeat_logs row and affect per-tenant scheduling behavior.
  - _Fix:_ Add tenant filtering (e.g. WHERE tenant_id = ${tenantId} AND task_name = ...) so the check is limited to the intended tenant, or use a storage-layer helper that enforces tenant-scoped log reads. Avoid global queries against tenant-scoped logs unless explicitly run in an admin context with clear safeguards.
- **server/heartbeat.ts:2679** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause after creating a tenant-scoped task. The update therefore isn't constrained to the task's tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${newTask.tenantId}) or use the storage API that updates the task and enforces the tenant context. Always pass and check tenantId when mutating tenant-scoped rows created from external input.
- **server/heartbeat.ts:3151** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause (second occurrence). This mutates a tenant-scoped row without constraining by tenant.
  - _Fix:_ Add AND tenant_id = ${newTask.tenantId} to the WHERE or call a storage-layer update that requires the caller's tenant and enforces the tenant constraint before updating approval_status.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} is executed without a tenant_id constraint. This updates a tenant-scoped heartbeat_tasks row by id only, which can unintentionally affect the wrong tenant if assumptions change.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${t.tenantId}) or use the storage API (e.g. storage.updateHeartbeatTask) which requires an explicit tenant argument. Ensure all UPDATE/DELETE/SELECT against tenant-scoped heartbeat_tasks include tenant scoping.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} is executed without a tenant_id constraint. This mutates a tenant-scoped row without confirming tenant ownership.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call the storage layer that enforces tenant context. Audit similar startup-time patches to ensure tenant scoping is always applied.
- **server/job-queue.ts:281** — failJob first SELECT (SELECT max_attempts FROM agent_jobs WHERE id = ${id}) reads agent_jobs by id without constraining tenant_id. This leaks the ability to fetch another tenant's job metadata by id.
  - _Fix:_ Lookup the job row with tenant scoping (e.g. SELECT max_attempts FROM agent_jobs WHERE id = ${id} AND tenant_id = ${tenantId}) or resolve the tenant for this id via a guarded admin path before reading. Never rely on payload-supplied tenant values; derive tenant scope from the persisted row or an authenticated admin assertion.
- **server/job-queue.ts:377** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} reads a job row by id without any tenant_id constraint. While the function's purpose is to return the tenant, calling code could misuse this unscoped SELECT in tenant-facing contexts.
  - _Fix:_ Limit this helper to admin-only code paths and document/guard it accordingly. Alternatively, require an admin-scoped authorization and avoid exposing an unscoped read in tenant-facing code. Prefer APIs that accept a tenantId and enforce it in the query.
- **server/job-queue.ts:406** — listJobs constructs and executes a SELECT FROM agent_jobs with a dynamically-built whereClause that may omit tenant_id (if caller doesn't pass tenantId). This allows unscoped listing of agent_jobs if callers omit tenantId.
  - _Fix:_ Make tenantId a required parameter for tenant-facing listing or require explicit ALL_TENANTS sentinel combined with admin authorization for cross-tenant listing. Enforce adding AND tenant_id = ${tenantId} into the whereClause for tenant-scoped calls; for admin/global calls require explicit authorization and auditing.
- **server/lib/aevo-meta-editor.ts:446** — UPDATE against tenant-scoped table `procedure_evolution_runs` performed without constraining by tenant (WHERE does not include tenantId). This allows a row id to be updated across tenants if an attacker can supply/guess runId.
  - _Fix:_ Include the tenant predicate in the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) so the UPDATE is strictly tenant-scoped. Prefer composing the condition with the calling tenantId rather than relying on previously-created ids alone.
- **server/lib/aevo-meta-editor.ts:456** — UPDATE against tenant-scoped table `procedure_evolution_runs` performed without constraining by tenant (WHERE only filters by id). Missing tenant check may allow cross-tenant modification when runId is re-used or guessed.
  - _Fix:_ Add an explicit tenant predicate to the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))) to enforce tenant isolation on the update.
- **server/lib/aevo-meta-editor.ts:500** — UPDATE against tenant-scoped table `procedure_evolution_runs` performed without constraining by tenant (WHERE only filters by id). This update path can modify another tenant's run row if runId is not tenant-scoped in the DB-level predicate.
  - _Fix:_ Constrain the UPDATE to the caller's tenant by adding eq(procedureEvolutionRuns.tenantId, tenantId) into the WHERE clause (e.g. .where(and(eq(procedureEvolutionRuns.id, runId), eq(procedureEvolutionRuns.tenantId, tenantId)))).
- **server/lib/chronicle-precision.ts:99** — Query reads from tenant-scoped table `skills` without constraining it to the caller's tenant. The LEFT JOIN uses `LEFT JOIN skills s ON s.id = ps.promoted_skill_id` but does not include `s.tenant_id = <tenant>`; this can leak or mix rows across tenants if skill ids are not globally namespaced.
  - _Fix:_ Add an explicit tenant filter on the joined table. For example: `LEFT JOIN skills s ON s.id = ps.promoted_skill_id AND s.tenant_id = ${tenantId}` (or the Drizzle equivalent, e.g. include `eq(skills.tenantId, tenantId)` in the join condition). Ensure every FROM/JOIN referencing tenant-scoped tables includes a tenant_id constraint.
- **server/lib/ideabrowser-backfill.ts:105** — INSERT into project_files does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Fix:_ Include tenant_id in the INSERT column list and VALUES (e.g. INSERT INTO project_files (project_id, tenant_id, file_name, ...) VALUES (${newProjectId}, ${tenantId}, ${f.fileName}, ...)). Ensure the tenant value is the caller's tenant and not derived from untrusted input.
- **server/lib/ideabrowser-backfill.ts:120** — INSERT into project_notes omits tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and pass the caller's tenantId in VALUES (e.g. INSERT INTO project_notes (project_id, tenant_id, note, author, ...) VALUES (..., ${tenantId}, ...)). Do not rely on implicit ownership from project_id.
- **server/lib/ideabrowser-dev-sync.ts:140** — INSERT into project_notes (via SELECT ... WHERE NOT EXISTS) does not include tenant_id; the NOT EXISTS subquery also omits tenant_id — reads/writes against project_notes are not tenant-constrained.
  - _Fix:_ Include tenant_id in both the INSERT target columns and in the NOT EXISTS predicate (e.g. WHERE NOT EXISTS (SELECT 1 FROM project_notes WHERE project_id = ${projectId} AND tenant_id = ${tenantId} AND author = ...)). Always pass tenant_id explicitly on INSERT.
- **server/lib/ideabrowser-dev-sync.ts:151** — INSERT into project_files (via SELECT ... WHERE NOT EXISTS) omits tenant_id; the NOT EXISTS check also lacks tenant_id — write and existence-check are not tenant-scoped.
  - _Fix:_ Add tenant_id to the INSERT columns and ensure the NOT EXISTS subquery also filters by tenant_id (AND tenant_id = ${tenantId}). Enforce the caller's tenant on both the read (existence check) and the write.
- **server/lib/mcp-api-keys.ts:166** — SELECT against tenant-scoped table mcp_api_keys is performed without an explicit tenant_id WHERE clause. The query resolves a row by key_prefix only: `SELECT id, tenant_id, key_hash, scopes, revoked_at FROM mcp_api_keys WHERE key_prefix = ${prefix} LIMIT 1`. This reads a tenant-scoped table without constraining to the caller's tenant context.
  - _Fix:_ Ensure tenant scoping is enforced at the application level. If the intent is to resolve tenant from the key prefix, make that explicit and harden it (e.g. guarantee global uniqueness of key_prefix with a DB constraint and document that this query is the tenant-resolution step). Alternatively, perform tenant-aware lookup (if a caller-supplied tenantId is available) with `WHERE key_prefix = ${prefix} AND tenant_id = ${tenantId}`. If you must resolve tenant via the prefix, isolate that resolution to a single safe primitive, validate uniqueness via a global UNIQUE index, and ensure all subsequent accesses to tenant-scoped tables use the resolved tenant_id in their WHERE clauses.
- **server/lib/restraint-trust.ts:90** — Use of sql.raw() with a column name derived from caller input (the `outcome`→`column` mapping) when updating trust_scores. Although the column name is allowlisted earlier, embedding a runtime-derived identifier via sql.raw() creates a SQL-injection / misuse risk and is flagged by the project's isolation/injection rule.
  - _Fix:_ Replace the dynamic sql.raw(column) update with static, parameterized statements. For example, switch on the validated outcome and issue one of several fixed UPDATE statements that reference concrete column names (action_alpha, action_beta, restraint_alpha, restraint_beta) rather than composing the column identifier at runtime. Alternatively use Drizzle's update().set({...}) with explicit keys. Maintain the tenant_id WHERE clause (already present). Example: switch(outcome){ case 'action_ok': await db.execute(sql`UPDATE trust_scores SET action_alpha = action_alpha + ${delta}, updated_at = NOW() WHERE tenant_id = ${tenantId} AND persona_id = ${personaId} AND category = ${category}`); break; ... }
- **server/lib/scheduled-post-runner.ts:319** — Building a Postgres array literal (arrLit) from caller-supplied platforms and embedding it into SQL. Platforms are request-derived and are turned into a string array literal; this is a string-interpolated SQL construction based on user input and risks injection or parsing errors.
  - _Fix:_ Use parameterized array binding instead of constructing a text[] literal string. For example, pass the JS array as a parameter and cast it server-side (or use Drizzle/sql helpers like sql.array(lowered, 'text')) so the DB receives a safe parameter rather than a hand-built literal. Validate and canonicalize platform values before binding.
- **server/memory-graph.ts:130** — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} executes without any tenant_id predicate — this can modify another tenant's category if a crafted categoryId is supplied or an attacker can cause a mismatched id to be used.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}. Or use Drizzle’s .where(eq(memory_categories.tenantId, tenantId)) API so every write is explicitly tenant-scoped.
- **server/memory-graph.ts:709** — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} has no tenant_id predicate — this can update a memory row belonging to another tenant.
  - _Fix:_ Add an explicit tenant predicate to the UPDATE. Example: UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} AND tenant_id = ${tenantId}. Alternatively, perform the update inside a tenant-scoped transaction or use a storage helper that enforces tenant ownership before writing.
- **server/mpeg-engine.ts:1058** — INSERT into project_files does not include tenant_id column (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) ...). Per the project invariant, every INSERT into tenant-scoped tables must include tenant_id explicitly. Relying on project_id/foreign-key alone is not sufficient.
  - _Fix:_ Include tenant_id in the INSERT columns and values (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Continue to keep the assertProjectInTenant ownership check, but also persist tenant_id on write to enforce the application-level tenancy invariant and avoid relying on implicit FK behavior.
- **server/mpeg-engine.ts:1447** — INSERT into project_files (in produceVideoParallel) omits tenant_id in the column list and VALUES. Even though ownership is checked via assertProjectInTenant, the invariant requires tenant_id to be set explicitly on every tenant-scoped INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and pass ${options.tenantId} as the corresponding value. Keep the assertProjectInTenant guard, but also persist tenant_id to ensure correct tenant scoping and to avoid cross-tenant contamination or DB constraint surprises.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw() with a lock key derived from caller-provided ledgerKey: the interpolated raw value (${sql.raw(lockKey)}) is request-derived and injected into SQL, creating an injection risk and bypassing parameter binding.
  - _Fix:_ Avoid sql.raw() for request-derived data. Compute the advisory-lock key server-side and pass it as a parameter (use a parameterized query / Drizzle binding) instead of injecting raw SQL. Example: use a parameterized call such as tx.execute(sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)`) or a parameterized raw query with positional parameters to ensure the value is bound, not concatenated.
- **server/pace-control.ts:85** — COUNT(*) query on heartbeat_logs lacks any tenant scoping. checkPace() reads global heartbeat_logs without filtering by tenant_id, allowing cross-tenant telemetry leakage and incorrect rate-limiting across tenants.
  - _Fix:_ Require and accept a tenantId for pace checks (or explicitly document this is an admin/global view). Add a WHERE tenant_id = ${tenantId} filter to the query and propagate tenantId through callers. If the intention is to partition by lane+tenant, include both filters in the WHERE clause.
- **server/pace-control.ts:140** — getPaceSnapshot() queries heartbeat_logs for totals and per-persona counts without any tenant_id filter. This exposes aggregated runtime data across all tenants and breaks tenant isolation.
  - _Fix:_ Add tenant scoping: make getPaceSnapshot accept a tenantId (or provide a clear admin-only interface) and include WHERE tenant_id = ${tenantId} in both the total and per-persona queries so snapshots are per-tenant only.
- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL lacks tenant scoping. If the skills table is tenant-scoped, this will return persona-specific skill rows across tenants (and global rows), leaking data.
  - _Fix:_ Include tenant scoping in the skills query. For example: SELECT ... FROM skills WHERE (persona_id = ${personaId} AND tenant_id = ${tenantId}) OR (persona_id IS NULL). Ensure persona-specific rows are filtered by tenant_id and that global (persona_id IS NULL) rows are explicitly allowed.
- **server/plan-executor.ts:985** — SELECT from tenant-scoped table 'plans' (staleCandidates query) without a tenant_id filter. resumeStuckPlans() enumerates 'executing' plans across all tenants in a single query.
  - _Fix:_ Avoid cross-tenant SELECTs without an explicit admin/intent guard. If the job must operate per-tenant, iterate tenants and include `WHERE tenant_id = ${tenantId}` for each; otherwise require/verify an explicit platform-admin context and document/telemetry the cross-tenant scope. Prefer selecting per-tenant rows (or adding an ADMIN_TENANT_ID guard) rather than a wide multi-tenant SELECT.
- **server/plan-executor.ts:1051** — SELECT from tenant-scoped table 'plans' (transientFailures query) without a tenant_id filter — this reads failed plans across all tenants to decide transient recovery.
  - _Fix:_ As above: restrict to a specific tenant context (loop tenants individually) or require explicit admin authorization before running cross-tenant recovery. Add `AND tenant_id = ${tenantId}` or otherwise scope the query, and ensure any subsequent UPDATEs use the same tenant_id to avoid cross-tenant effects.
- **server/plan-executor.ts:1110** — SELECT id FROM plans WHERE status = 'approved' is executed without a tenant_id filter; the code then schedules executePlan for each returned id, potentially acting across tenants.
  - _Fix:_ Do not enumerate pending approved plans across all tenants without explicit scope. Either (A) run pending work per-tenant (SELECT ... WHERE tenant_id = ${tenantId} ...), or (B) require this boot recovery to run under an explicit admin tenant context and make that explicit in code/audit logs. Ensure downstream executePlan continues to validate/require tenant ownership on writes.
- **server/research-engine.ts:457** — INSERT into project_notes does not set tenant_id. The INSERTs at the auto-deposit code paths (summary and per-finding) omit tenant_id, risking cross-tenant writes or schema errors if tenant_id is required.
  - _Fix:_ Include the tenant_id explicitly in the INSERT INTO project_notes VALUES list (e.g. VALUES (${projectId}, ${note}, ${author}, NOW(), ${session.tenantId}) or use an explicit column list that includes tenant_id). Alternatively, resolve and assert project ownership and use the project's tenant_id when persisting notes.
- **server/routes.ts:977** — Query against tenant-scoped table `file_storage` without tenant_id constraint: owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName));
  - _Fix:_ Avoid scanning all tenants' rows by filename. Restrict the lookup to the authenticated tenant (eq(fileStorage.tenantId, authedTenantId)) or perform two explicit checks: (A) is there an owner row for the authenticated tenant, and (B) are there any owner rows at all (if you must treat collisions differently). Do not return or leak tenant-scoped rows; if you must examine other tenants' state for collision-detection, limit the returned columns and scope the logic to a safe admin-only path or use an HMACed capability that binds filename→tenant.
- **server/routes.ts:4651** — INSERT into project_notes omits tenant_id. The INSERT statement at lines 4651-4654 inserts (project_id, note, author) without specifying tenant_id; per project/notes tenant-scoping policy every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Fix:_ Include the tenant_id column/value in the INSERT (and continue to validate ownership). For example: INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Alternatively, call a storage-layer helper (e.g. storage.createProjectNote) that enforces tenant scoping. Ensure project_id ownership was validated (it is) and persist tenant_id to prevent cross-tenant data insertion.
- **server/routes/conversations.ts:119** — UPDATE on tenant-scoped table 'conversations' without tenant constraint. The query `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` does not include AND tenant_id = ${tenantId}.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId}` (or use conv.tenantId when validated). Always include tenant_id in WHERE for tenant-scoped updates.
- **server/routes/conversations.ts:125** — UPDATE on tenant-scoped table 'projects' without tenant constraint. The query `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` lacks AND tenant_id = ${tenantId}.
  - _Fix:_ Add tenant filtering to the UPDATE, e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}` to ensure only the caller's tenant row is modified.
- **server/routes/conversations.ts:236** — SELECT against tenant-scoped table 'conversations' without tenant constraint. The query `SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}` does not constrain c.tenant_id.
  - _Fix:_ Include the conversation tenant filter in the WHERE clause, e.g. `WHERE c.id = ${id} AND c.tenant_id = ${conv.tenantId ?? tenantId} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}`. Always include tenant_id for tenant-scoped tables even when other joins are tenant-filtered.
- **server/routes/inbox-notifications.ts:101** — UPDATE against tenant-scoped table `inbox_messages` is missing a tenant_id predicate. The code runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `AND tenant_id = ${tenantId}`.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. `WHERE id = ${msg.id} AND tenant_id = ${tenantId}`) or use the storage layer method that enforces tenant scoping. Always include an explicit tenant_id predicate on writes to tenant-scoped tables.
- **server/routes/projects.ts:401** — INSERT into project_conversations does not set tenant_id. Tenant-scoped tables must receive an explicit tenant_id on INSERT to prevent cross-tenant leakage.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${targetProjectId}, ${conversationId}, ${tenantId})). If the table is intentionally global, document and verify that it is not tenant-scoped; otherwise store tenant_id and/or validate ownership before inserting.
- **server/routes/projects.ts:569** — INSERT INTO project_notes omits tenant_id in the values. Tenant-scoped tables require tenant_id to be set on INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (${id}, ${req.body.note}, ${req.body.author || 'user'}, ${tenantId})). Alternatively, validate and derive tenant ownership and explicitly set tenant_id before inserting.
- **server/routes/projects.ts:608** — INSERT INTO project_files (files-base64 handler) does not include tenant_id. Tenant-scoped tables must have tenant_id set on INSERT.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, tenant_id) VALUES (..., ${tenantId})). Ensure tenantId is derived from the authenticated session and not from client input.
- **server/routes/projects.ts:676** — INSERT INTO project_files (multipart upload handler) omits tenant_id. Tenant-scoped tables must be explicitly assigned tenant_id on INSERT.
  - _Fix:_ Add tenant_id to the INSERT columns and values (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Validate the project ownership before inserting and include tenantId in the row.
- **server/routes/projects.ts:755** — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} — DELETE lacks tenant_id constraint on project_files.
  - _Fix:_ Restrict DELETE by tenant (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}). Even if you previously checked the parent project, include tenant_id in the DELETE to avoid accidental cross-tenant deletion if foreign keys are stale or absent.
- **server/routes/tenants.ts:274** — Inserting a message without an explicit tenant_id: storage.createMessage({ conversationId: conv.id, role: ..., content: ... }) does not include tenantId. The invariant requires every INSERT into tenant-scoped tables to pass tenant_id explicitly (columns are NOT NULL with no default). Relying on conversationId/foreign keys or storage internals is not acceptable for tenant isolation.
  - _Fix:_ Include tenantId on the inserted row (e.g. await storage.createMessage({ conversationId: conv.id, tenantId, role: 'system', content: ... })). Additionally, ensure storage.createMessage validates that the conversationId belongs to the provided tenant (defensive check) and/or requires tenantId as a mandatory argument so callers cannot omit it.
- **server/routes/tenants.ts:279** — Inserting messages in a loop without an explicit tenant_id: storage.createMessage({ conversationId: conv.id, role: 'user', content: msg }) omits tenantId. Every INSERT into tenant-scoped tables must set tenant_id explicitly rather than deriving it from related rows.
  - _Fix:_ Pass tenantId for each created message (e.g. await storage.createMessage({ conversationId: conv.id, tenantId, role: 'user', content: msg })). Also add a server-side assertion in storage.createMessage to verify the conversation belongs to the tenant to prevent accidental cross-tenant writes.
- **server/seed-cleanup.ts:7** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter. heartbeat_tasks appears tenant-scoped so this reads across tenants.
  - _Fix:_ Scope reads to a tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true ...) or explicitly document and use a global/admin table if cross-tenant queries are intended. Prefer explicit tenant_id in predicates.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks a tenant constraint — this will disable delegation tasks across all tenants.
  - _Fix:_ Constrain the UPDATE to the intended tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or AND tenant_id = ${someTenant}) or otherwise restrict to specific tenant ids to avoid cross-tenant side effects.
- **server/seed-cleanup.ts:44** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant restriction and will count across tenants.
  - _Fix:_ Add tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) to the WHERE clause, or make the intent explicit if a cross-tenant scan is desired and safe.
- **server/seed-cleanup.ts:51** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is not scoped to tenant_id and may disable tasks for all tenants.
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or other intended tenant) to the WHERE clause so only the target tenant's rows are modified.
- **server/seed-cleanup.ts:58** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has no tenant filter; reads tenant-scoped table across tenants.
  - _Fix:_ Include tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) in the WHERE clause, or change to an explicit admin/global table if cross-tenant introspection was intended and audited.
- **server/seed-cleanup.ts:74** — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ...) AND enabled = true lacks tenant scoping and can modify other tenants' rows.
  - _Fix:_ Restrict the UPDATE by tenant (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) or otherwise ensure the intended tenant context is enforced before performing the write.
- **server/seed.ts:42** — Unscoped SELECT on tenant-scoped table research_sessions: `SELECT COUNT(*) as count FROM research_sessions` executes a global read across all tenants with no tenant_id filter.
  - _Fix:_ Restrict the query to the caller's tenant. Pass the tenant id explicitly and add a WHERE tenant_id = ${tenantId} (or use Drizzle: .where(eq(research_sessions.tenantId, tenantId))). If the intent is to check only a specific tenant's sessions, scope the COUNT to that tenant; if inspecting global state is required, ensure this code runs with explicit admin intent and is not reachable by tenant-scoped callers.
- **server/seed.ts:2240** — Unscoped SELECT joining tenant-scoped tables conversations and projects without constraining to a specific tenant: the query `SELECT DISTINCT c.id, c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id AND p.tenant_id = c.tenant_id WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)` performs a read across all tenants (no WHERE c.tenant_id = <tenant>).
  - _Fix:_ Add an explicit tenant constraint (e.g. `WHERE c.tenant_id = ${tenantId}` and `AND p.tenant_id = ${tenantId}`) or use Drizzle .where(eq(conversations.tenantId, tenantId)). Every read of tenant-scoped tables must be limited to the caller's tenant to preserve isolation. If this is intended as an admin/global maintenance operation, gate it behind an explicit admin path and clearly document that it performs global operations.
- **server/seed.ts:3639** — DELETE FROM heartbeat_tasks is executed without any tenant_id constraint. heartbeat_tasks is tenant-scoped (there is an index on tenant_id), so this DELETE can remove rows across all tenants.
  - _Fix:_ Constrain the DELETE to an explicit tenant (e.g. AND tenant_id = <tenant>) or iterate per-tenant. If the intent is to only affect platform/system tasks, explicitly filter by the platform/admin tenant id (ADMIN_TENANT_ID). Avoid global deletes on tenant-scoped tables.
- **server/seed.ts:3667** — UPDATE memory_entries ... WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ... runs without any tenant_id constraint. memory_entries is tenant-scoped (there is an index on tenant_id), so this UPDATE will modify rows for every tenant.
  - _Fix:_ Add an explicit tenant_id filter (e.g. AND tenant_id = <tenant>) or perform the normalization per-tenant after validating authorization. If a global migration is intended, perform it under an explicit, audited maintenance path that verifies operator intent and documents tenant impact.
- **server/seed.ts:3693** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped (index on tenant_id), so this will update conversations across all tenants.
  - _Fix:_ Include a tenant_id filter (e.g. AND tenant_id = <tenant>) or run the update per-tenant after verifying scope/authorization. If this is intended as a global migration, perform it in a controlled migration script that either records the tenant context or uses an admin-only path with explicit consent.
- **server/self-reflection.ts:380** — UPDATE on tenant-scoped table "memory_entries" uses WHERE id = <row id> but does not include tenant_id = <caller tenant>. A row id from another tenant could be updated if supplied/obtained.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or perform the update via Drizzle's update with .where(and(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tenantId))). Validate the update returned a row and fail if not.
- **server/sentiment-detector.ts:167** — INSERT into tenant-scoped table "sentiment_events" records conversation_id supplied by caller without verifying the conversation actually belongs to the caller's tenant. This can associate arbitrary conversation IDs with the tenant or misattribute events.
  - _Fix:_ Verify ownership of the conversation_id (e.g. SELECT conversation WHERE id = conversationId AND tenant_id = tenantId) before inserting, or derive conversation_id from a trusted storage lookup rather than accepting raw caller input.
- **server/skills-marketplace.ts:209** — SELECT against tenant-scoped table 'skills' without tenant_id constraint (SELECT id FROM skills WHERE name = ${template.name}). This can leak or conflict with another tenant's rows.
  - _Fix:_ Scope the lookup to a tenant. Require a tenantId parameter (or explicitly use the admin/owner tenant only if intended) and add AND tenant_id = ${tenantId} to the WHERE clause. If the intent is to check global uniqueness, make that explicit and document why; otherwise enforce per-tenant uniqueness/filtering before deciding install.
- **server/skills-marketplace.ts:235** — SELECT from tenant-scoped table 'skills' without tenant_id constraint (SELECT * FROM skills WHERE id = ${skillId}). This can return another tenant's skill for export (cross-tenant disclosure).
  - _Fix:_ Require and pass a tenantId to exportSkill and constrain the query: WHERE id = ${skillId} AND tenant_id = ${tenantId}. If global admin export is intended, explicitly document and gate the operation and/or use an admin tenant constant when querying.
- **server/storage.ts:584** — getAllMemoriesForBackup() selects all memory_entries without any tenant_id constraint, returning tenant-scoped rows platform-wide.
  - _Fix:_ Require an explicit tenant scope parameter (or the explicit ALL_TENANTS sentinel) and apply requiredTenantScope(memoryEntries.tenantId, tenantId) or otherwise restrict this call to an admin-only path. Do not perform unscoped selects on tenant-scoped tables.
- **server/storage.ts:959** — getMemoriesWithoutEmbeddings() selects from memory_entries without any tenant filter; this reads tenant-scoped rows across all tenants.
  - _Fix:_ Add a tenantId parameter (or require ALL_TENANTS) and use requiredTenantScope(memoryEntries.tenantId, tenantId) / tenantScope(...) to enforce tenant isolation. If this is an admin-only batch job, require an explicit admin sentinel and guard the call appropriately.
- **server/storage.ts:969** — getKnowledgeWithoutEmbeddings() selects agent_knowledge rows with no tenant constraint, returning tenant-scoped data platform-wide.
  - _Fix:_ Require and validate a tenantId (or explicit ALL_TENANTS sentinel) and use requiredTenantScope(agentKnowledge.tenantId, tenantId) to scope the query, or restrict this function to a guarded admin-only path.
- **server/storage.ts:936** — updateMemoryEmbedding(id, embedding) updates memory_entries.embedding WHERE id = <id> with no tenant predicate — allows modifying a memory entry by id across tenants.
  - _Fix:_ Accept and validate a tenantId parameter (assertValidTenantId) and include eq(memoryEntries.tenantId, tenantId) in the WHERE clause (or use requiredTenantScope) to ensure the caller is only able to update embeddings for their tenant's rows.
- **server/storage.ts:587** — getMemoryEntry(id, tenantId?) allows caller to omit tenantId (silently warning) and will perform a SELECT on memory_entries by id without a tenant predicate when tenantId is undefined, leaking tenant-scoped data.
  - _Fix:_ Make tenantId required for this API (use requireTenantScope/requiredTenantScope) or fail-closed when tenantId is not provided. Do not rely on a warning path that permits unscoped reads; validate and enforce tenant scoping at storage boundary.
- **server/surprise-scorer.ts:93** — UPDATE on tenant-scoped table 'felix_proposals' omits tenant_id constraint (stampArgsEmbedding). The query updates args_embedding WHERE id = ${proposalId} but does not ensure the row belongs to the expected tenant.
  - _Fix:_ Include tenant scoping in the UPDATE. Either accept/derive a tenantId in stampArgsEmbedding and add `AND tenant_id = ${tenantId}` to the WHERE clause, or first SELECT the row with tenant_id and bail if it doesn't match. Never update tenant-scoped rows by id alone.
- **server/tool-learning.ts:129** — SELECT on tenant-scoped table 'custom_tools' omits tenant_id filter when checking for existing name collisions. The query checks only `WHERE name = parsed.name` and can leak cross-tenant state or incorrectly block creation.
  - _Fix:_ Filter by tenantId when checking for existing tools. Pass the caller's tenantId into the function and use `.where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId)))` (or equivalent) so reads are tenant-scoped.
- **server/tool-learning.ts:155** — SELECT of final tool row uses only `WHERE id = ${inserted.id}` with no tenant_id constraint. Reads against tenant-scoped custom_tools must be tenant-scoped.
  - _Fix:_ Add tenant scoping to this final lookup, e.g. `.where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId)))` (pass tenantId through as needed). Do not rely on id-only filtering for tenant-scoped tables.
- **server/tool-learning.ts:291** — UPDATE on tenant-scoped table 'custom_tools' performed with `WHERE id = ${tool.id}` only (auto-fix path), missing tenant_id constraint. This updates an implementation without verifying tenant ownership.
  - _Fix:_ Include tenant filtering on updates, e.g. `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId)))`. Ensure the auto-fix flow has the tenantId and fails closed if tenant cannot be determined.
- **server/tool-learning.ts:316** — DELETE on tenant-scoped table 'custom_tools' performed with `WHERE id = ${tool.id}` only (revertToolRow). The deletion is not constrained by tenant_id and could affect another tenant's row with the same id in some scenarios.
  - _Fix:_ Constrain deletes to the tenant as well: `.where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId || tenantId)))`. Ensure revertToolRow receives/derives tenantId and refuses to run without it.
- **server/tools.ts:4546** — INSERT into tenant-scoped table `project_notes` does not include tenant_id in the VALUES list. The code inserts project-scoped data without explicitly setting tenant_id even though row-level security/foreign-key enforcement is not relied on.
  - _Fix:_ Include the caller's tenant id explicitly in the INSERT (and ensure it was validated). e.g. ensure project ownership via assertProjectInTenant(projectId, tenantId) and then insert with tenant_id: db.execute(sql`INSERT INTO project_notes (tenant_id, project_id, note, author, created_at) VALUES (${tenantId}, ${projectId}, ${note}, ${author}, NOW())`). Do not rely on project_id alone to scope the row.
- **server/tools.ts:4550** — INSERT into tenant-scoped table `project_files` omits tenant_id. The handler writes a project-scoped file row (project_id provided) but does not include tenant_id in VALUES.
  - _Fix:_ Pass the tenant id explicitly in the INSERT and ensure project ownership was verified beforehand. For example: await db.execute(sql`INSERT INTO project_files (tenant_id, project_id, file_name, file_type, file_path, file_url, uploaded_by, created_at) VALUES (${tenantId}, ${projectId}, ${fileName}, ${fileType}, ${filePath}, ${fileUrl}, ${uploadedBy}, NOW())`). If ownership is validated earlier, still include tenant_id to satisfy the tenant-isolation invariant.
- **server/tools.ts:6464** — INSERT into tenant-scoped table `project_files` (from generate_audio) does not set tenant_id. The code inserts a file record for params.project_id without explicitly saving the caller's tenant_id, relying only on project_id and a prior assertProjectInTenant check.
  - _Fix:_ Even when assertProjectInTenant was used, include tenant_id in the INSERT and derive it from the validated params._tenantId. Example: await db.execute(sql`INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${tenantId}, ${params.project_id}, ${fileName}, ${filePath}, ${fileUrl}, ${fileType}, ${fileSize}, ${uploadedBy})`). This prevents accidental cross-tenant writes if other code paths omit the guard.
- **server/tools.ts:7006** — INSERT into tenant-scoped table `project_files` omits tenant_id. The INSERT uses project_id after asserting project ownership, but the project_files row itself is not being written with tenant_id (columns are NOT NULL with no default per invariant).
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)) or use a storage helper that writes tenant_id atomically. Do not rely on implicit ownership via project_id — every tenant-scoped table write must explicitly set tenant_id.
- **server/tools.ts:8977** — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) and lacks a tenant_id constraint. This allows a caller with a row id to affect rows belonging to other tenants.
  - _Fix:_ Constrain updates to the caller's tenant as well, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or use a helper that enforces tenant filtering for all UPDATE/DELETE operations on tenant-scoped tables.
- **server/tools.ts:9007** — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) and lacks a tenant_id constraint (security path after managed-skill-safety rejection).
  - _Fix:_ Add tenant scoping to the WHERE clause (include eq(proposedSkills.tenantId, tid) or equivalent) so only rows belonging to the caller's tenant can be modified.
- **server/tools.ts:9021** — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) when marking a proposal 'accepted' and adding promotedSkillId; tenant filter is missing.
  - _Fix:_ Add tenantId constraint to the UPDATE (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))) to enforce tenant isolation on writes.
- **server/tools.ts:9038** — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) when marking a proposal 'rejected'; tenant constraint is missing.
  - _Fix:_ Constrain the UPDATE by tenant (include eq(proposedSkills.tenantId, tid) in the WHERE clause) or use a centralized helper to enforce tenant scoping on all updates/deletes for tenant-scoped tables.
- **server/tools/domains/browser/handlers.ts:165** — UPDATE against tenant-scoped table `browser_workflows` lacks a tenant constraint in the WHERE clause: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. Under the project's invariant, every UPDATE must be constrained to the caller's tenant to prevent cross-tenant writes.
  - _Fix:_ Include the tenant scoping in the WHERE clause (or otherwise enforce tenant ownership at the SQL layer). For example: wfDb.execute(wfSql`UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id} AND tenant_id = ${tenantId}`); Alternatively, perform the UPDATE inside a tenant-scoped SELECT/transaction guard that asserts ownership before the write.
- **server/tools/domains/knowledge/handlers.ts:91** — UPDATE against tenant-scoped table `knowledge_triples` without tenant constraint: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}`
  - _Fix:_ Add an explicit tenant restriction to the UPDATE (e.g. `WHERE id = ${row.id} AND tenant_id = ${tenantId}`), or perform the update via a parameterized query that includes tenantId. Even if `row.id` was selected from a tenant-scoped query earlier, every write must still enforce the tenant filter in the WHERE clause to prevent cross-tenant modification.
- **server/tools/domains/files/handlers.ts:934** — INSERT into `project_files` does not set tenant identity: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`
  - _Fix:_ Make the tenant explicit on INSERT. If `project_files` is tenant-scoped, include the tenant_id column and pass the caller's tenant (e.g. add `tenant_id` to the column list and `${tenantId}` to VALUES). If tenant is derived via `project_id`, ensure you validate project ownership immediately before INSERT and still persist tenant_id (or otherwise enforce tenant in the DB) since there is no RLS/DB default.
- **server/tools/domains/files/handlers.ts:937** — UPDATE on tenant-scoped `projects` table without tenant constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}`. Do not rely solely on earlier reads for ownership — every write must include an explicit tenant filter in the WHERE clause.
- **server/tools/domains/strategic-memory/handlers.ts:98** — UPDATE memory_entries uses WHERE id = ${id} with no tenant_id constraint. This allows updating rows across tenants if ids collide or are guessed, violating tenant isolation which relies on application-level filters.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. add AND tenant_id = ${tid} to the WHERE clause (WHERE id = ${id} AND tenant_id = ${tid}). Prefer applying tenant filter consistently on all UPDATE/DELETE statements and consider doing the SELECT+UPDATE in a transaction or using SELECT ... FOR UPDATE to avoid TOCTOU races.
- **server/tools/domains/strategic-memory/handlers.ts:159** — UPDATE memory_entries sets last_accessed/access_count WHERE id = ANY(${ids}) with no tenant_id constraint. This can update rows belonging to other tenants if ids span tenants.
  - _Fix:_ Constrain the UPDATE by tenant, e.g. WHERE id = ANY(${ids}) AND tenant_id = ${tid}, or use WHERE tenant_id = ${tid} AND id = ANY(${ids}). Ensure all multi-row UPDATEs include the tenant filter and consider transactional updates to prevent cross-tenant modification races.
- **server/tools/domains/strategic-memory/handlers.ts:258** — UPDATE memory_entries uses WHERE id = ${id} with no tenant_id constraint (in recordStrategicWin). Missing tenant scoping can allow cross-tenant updates.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) and ensure the earlier SELECT/INSERT/UPDATE operations always include tenant_id. Consider transactionally binding the read and write to avoid TOCTOU issues.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` is performed without any tenant_id constraint (loads all candidate pairing rows across tenants). This violates the tenant-isolation invariant and can lead to cross-tenant pairing/lookup/metadata leakage or hijack.
  - _Fix:_ Do not scan the entire agent_knowledge table across tenants. Constrain the query so only rows whose claimed conversation actually belong to the same tenant are returned (or join to conversations to validate ownership in-SQL). For example, change the query to join agent_knowledge -> conversations using the content->>'conversationId' and require the claimed tenant to match the conversation's tenant: SELECT ak.id, ak.content::jsonb AS info FROM agent_knowledge ak JOIN conversations c ON ((ak.content::jsonb->>'conversationId')::int = c.id AND (ak.content::jsonb->>'tenantId')::int = c.tenant_id) WHERE ak.category = 'messaging_pairing' AND ak.title = ${`${channel}:${phone}`} ORDER BY ak.id DESC; This ensures only internally-consistent rows that resolve to an existing conversation/tenant are returned and prevents cross-tenant enumeration/leakage.
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` by id is executed without a tenant_id constraint. Reading conversations without constraining tenant_id violates the tenant-isolation invariant and may expose or allow routing to conversations owned by other tenants.
  - _Fix:_ Include tenant filtering in the conversations query when verifying a pairing. For example, verify in-SQL that the claimed conversation belongs to the claimed tenant: SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${(info as any).tenantId} LIMIT 1; — or, better, perform the verification as part of a single joined query (see previous suggestion) so the database only returns conversation rows that match both id and tenant. Never rely on unchecked JSON fields from agent_knowledge as proof of ownership without validating against conversations. If you must inspect multiple rows, restrict the DB result set via an explicit join/tenant predicate rather than selecting all rows across tenants.
- **server/webhookHandlers.ts:564** — Calls storage.updateTenant(tenantId, ...) where tenantId is parsed from session.metadata.visionclaw_tenant_id (untrusted Stripe metadata) without verifying the caller is authorized to modify that tenant.
  - _Fix:_ Validate that the tenantId in metadata is genuine for this Stripe event before mutating tenant state. For example, require a trusted mapping (customer id → tenant id) or fetch the Stripe customer/subscription and confirm it is bound to the tenant. If no authoritative mapping exists, refuse to mutate tenant-scoped state and surface the event for manual review.
- **server/webhookHandlers.ts:1250** — UPDATE tenants SET plan = 'trial' WHERE id = ${tenantId} — handler uses tenantId derived from subscription.metadata without verifying ownership. Metadata-provided tenantId is untrusted and may allow changing another tenant's plan.
  - _Fix:_ When metadata.tenantId is present, verify the association between the Stripe object and the tenant (e.g. ensure the Stripe customer or subscription is mapped to that tenant in your DB) before updating tenant rows. If the event contains only a Stripe customer id, prefer to resolve tenant via a lookup by stripe_customer_id and update only the resolved tenant.
- **server/webhookHandlers.ts:1325** — UPDATE tenants SET plan = ${plan} WHERE id = ${tenantId} — tenantId may be taken from subscription.metadata and used to change tenant plan without confirming the subscription/customer actually belongs to that tenant.
  - _Fix:_ Do not accept tenant-scoped IDs from Stripe metadata as authoritative. Re-fetch the subscription/customer via the Stripe API and confirm the tenant ownership mapping (e.g. match subscription.customer to tenants.stripe_customer_id) before applying updates. If ownership cannot be proven, throw and surface for manual review so Stripe will retry.

### MEDIUM (36)

- **server/auto-asset-capture.ts:184** — Backfill query selects conversations (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...) without a tenant_id filter, scanning conversations for all tenants.
  - _Fix:_ Limit the backfill to a specific tenant (add WHERE c.tenant_id = ${tenantId}) or iterate tenants and run the query per-tenant. Never run maintenance queries that read tenant-scoped tables across all tenants without an explicit application-level tenant filter.
- **server/auto-asset-capture.ts:207** — Querying messages for a conversation (SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant') is executed without constraining by tenant_id. Although conversation_id is used, the messages table access itself is unscoped in this query.
  - _Fix:_ Either join messages to conversations and filter by conversations.tenant_id = ${tenantId}, or include an explicit tenant constraint when selecting messages. Prefer per-tenant iteration so every read against tenant-scoped tables includes WHERE tenant_id = <tenant>.
- **server/channel-routing.ts:51** — setChannelRoute DELETE branch removes a route by channel without any tenant constraint. If channel_routes should be per-tenant, this will delete other tenants' routes.
  - _Fix:_ If routes are per-tenant, make channel_routes include tenant_id and require tenantId in setChannelRoute; use DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}. If the table is intentionally global, document the global scope and ensure callers cannot pass cross-tenant persona ids.
- **server/channel-routing.ts:64** — getPersonaForChannel reads persona_id from channel_routes by channel globally, with no tenant context. A global lookup can return a persona id belonging to a different tenant (and downstream code may then access tenant-scoped personas without verifying ownership).
  - _Fix:_ Make channel_routes tenant-scoped (add tenant_id) or accept a tenantId parameter and filter: SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId}. If channel_routes must remain global, verify the returned persona belongs to the caller's tenant before using it.
- **server/channel-routing.ts:71** — removeChannelRoute deletes routes by channel without any tenant constraint, potentially removing routes for other tenants if channel_routes is intended to be tenant-scoped.
  - _Fix:_ Require tenant context and scope the delete to the tenant: DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}. Or make channel_routes explicitly global and ensure only authorized global operations can modify it.
- **server/data-protection.ts:360** — SELECT FROM tenant-scoped table `project_notes` using only project_id (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} ...) with no tenant_id constraint. The code assumes project_id implies tenant ownership but there is no DB-enforced isolation.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}), or join through a tenant-checked projects row (ensure the SELECT only returns notes for projects belonging to the calling tenant). Validate ownership before reading associated rows.
- **server/data-protection.ts:371** — SELECT FROM tenant-scoped table `project_files` using only project_id (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} ...) with no tenant_id constraint — potential cross-tenant data exposure.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId}) or join to projects filtered by tenant_id to ensure project_files returned belong to the caller's tenant.
- **server/embeddings.ts:259** — Use of sql.raw() with an interpolated vector literal built from an external embedding: `... embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}`. The vector string originates from a provider result and is injected into raw SQL via string interpolation.
  - _Fix:_ Avoid injecting raw string SQL constructed from external data. If the DB/driver cannot bind vector parameters directly, ensure `vecLiteral` rigidly validates and sanitizes shape (it already performs numeric checks) and centralize/limit raw interpolation. Where possible, use parameter binding APIs or driver-supported typed parameterization instead of sql.raw with string interpolation.
- **server/google-drive.ts:1664** — INSERT INTO project_files is executed without an explicit tenant_id column value. If project_files is a tenant-scoped table (tenant_id NOT NULL), this creates a cross-tenant write risk or will fail the NOT NULL constraint.
  - _Fix:_ If project_files has a tenant_id column, include tenantId in the INSERT values (and derive/validate it from a trusted source such as assertProjectInTenant). If the table intentionally relies on project_id -> projects.tenant_id, document and enforce that invariant and prefer inserting a tenant_id column to make row ownership explicit.
- **server/heartbeat-schedules.ts:26** — SELECT from messages enumerates tenant rows without an explicit tenant_id constraint (SELECT DISTINCT tenant_id FROM messages ...). This is a tenant-scoped table access with no per-caller tenant filter.
  - _Fix:_ If this is intended as an admin cross-tenant enumeration, make that explicit and isolate it to a platform/admin-only path (e.g. use an admin-only metadata/tenants table or an explicit admin tenant id). Otherwise add an explicit tenant_id filter (WHERE tenant_id = ${tenantId}) or move this logic into a storage API that enforces who may enumerate tenants.
- **server/lib/mcp-api-keys.ts:70** — Manual construction of a Postgres array literal (scopesLit) from caller-supplied data and interpolation into an SQL template. Building scopesLit = `{...}` by hand and inserting it into the SQL risks SQL-injection / malformed-literal issues when scopes are derived from untrusted input.
  - _Fix:_ Do not assemble SQL literals by string-concatenation. Pass scopes as a proper parameterized JS array so the driver/Drizzle can bind it as a Postgres text[] (or use sql.array(scopes, 'text')). Example: db.execute(sql`INSERT ... (scopes) VALUES (${params.tenantId}, ${name}, ${prefix}, ${hash}, ${scopes}::text[], ...)`) or use Drizzle's typed array binding instead of building a scopesLit string.
- **server/lib/jury-experience.ts:175** — Interpolation of an embedding-derived string into SQL: `const embeddingFragment = embedding ? sql`${`[${embedding.join(",")}]`}::vector` : sql`NULL`;` builds SQL content from external data (embedding) and injects it into the query fragment. This is a string-interpolated SQL fragment derived from non-constant data.
  - _Fix:_ Avoid building SQL fragments from joined strings. Parameterize the embedding properly or use the DB/driver support for vector/array parameters (e.g. pass the numeric array as a parameter or use a safe helper such as sql.array or a prepared statement that binds the numeric array, or use pgvector driver bindings). This prevents any possibility of injection/malformed-literal issues and ensures correct typed binding for the vector column.
- **server/lib/outcome-promotion.ts:158** — Query aggregates rows from tenant-scoped table deliverable_grade_events without any tenant_id WHERE constraint (SELECT tenant_id, format, count(*) ... FROM deliverable_grade_events ...). This reads across all tenants' data in a context that appears to be a per-tenant safety/quality pipeline and violates the invariant that tenant-scoped tables must be constrained by tenant at the application layer.
  - _Fix:_ Either restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) and run the scan per-tenant, or explicitly gate this cross-tenant aggregation behind an admin-only job with documented authorization checks. If the intent is to aggregate per-tenant, loop over tenants or parameterize the tenant and avoid unbounded cross-tenant SELECTs. Ensure any file outputs or downstream actions are segregated by tenant and that only privileged/admin processes may run global scans.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from agent_runs queries rows with status='running' and age criteria but does not filter by tenant_id. This reads tenant-scoped rows across all tenants.
  - _Fix:_ If the audit should be tenant-scoped, add a tenant_id predicate (e.g. WHERE tenant_id = ${tenantId}). If the audit intentionally scans across tenants, ensure it runs only in a properly authorized admin context and that the behavior is documented/audited. Prefer explicit per-tenant iteration to avoid accidental cross-tenant exposure.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from mind_tickets queries rows with status='in_progress' and age criteria but does not filter by tenant_id. This reads tenant-scoped rows across all tenants.
  - _Fix:_ Add an explicit tenant_id filter if the function should be tenant-scoped, or otherwise restrict execution to an authorized admin context and document/audit the cross-tenant access. Consider iterating per-tenant to avoid wide cross-tenant reads.
- **server/lib/system-probes.ts:163** — A count(*) query against the messages table is executed without a tenant_id filter (db.select count(*) from messagesTable), returning cross-tenant metrics from a tenant-scoped table.
  - _Fix:_ Either restrict the count to a specific tenant (WHERE tenant_id = ${tenantId}) or, if an aggregate across all tenants is required for system probes, ensure the probe only runs in an authorized admin context and explicitly document/audit the cross-tenant access. Prefer explicit per-tenant aggregation when exposing tenant-level diagnostics.
- **server/platform-admin-conversations.ts:21** — SELECT from tenant-scoped table 'conversations' without tenant_id constraint (cross-tenant lookup). While the function is guarded by isPlatformAdmin(req), it performs an unscoped read of a tenant-scoped table.
  - _Fix:_ If the cross-tenant access is intentional, make the cross-tenant boundary explicit: require a separate admin-capability token and audit/log the tenant(s) accessed. Alternatively, accept an explicit tenantId param and include `AND conversations.tenant_id = ${tenantId}` in the WHERE clause. At minimum, add an explicit comment noting this is an audited, intentional cross-tenant read and ensure robust access logging.
- **server/routes.ts:1925** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint in public handler: SELECT ... FROM presenter_sessions WHERE token = ${token}
  - _Fix:_ Treat a presenter token as an explicit capability (and document it) or, if the endpoint must be tenant-bound, include AND tenant_id = ${expectedTenantId} (or validate the caller's session tenant before returning the row). If the token is the auth mechanism, ensure the token is unguessable, rotation/revocation is possible, and avoid returning any tenant-sensitive fields unless the token proof binds to that tenant.
- **server/routes.ts:1953** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint: sessionResult = await db.execute(sql`SELECT id, tenant_id FROM presenter_sessions WHERE token = ${token}`)
  - _Fix:_ Either require the requester to present the tenant context and include AND tenant_id = ${tenant} in the WHERE, or treat token strictly as a capability and validate its binding (HMAC/signature) to a tenant before using it. Avoid selecting global owner rows unless necessary; prefer checking existence for the caller's tenant first.
- **server/routes.ts:1996** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint in /api/presenter/:token/tts: SELECT id FROM presenter_sessions WHERE token = ${token}
  - _Fix:_ As above, either constrain by tenant_id or validate the token as an explicit capability bound to a tenant. If capability-based access is intended, ensure tokens are unguessable, have limited lifetime, and that any subsequent operations enforce tenant-level checks when required.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table 'agent_jobs' without tenant constraint. The query `SELECT ... FROM agent_jobs j LEFT JOIN tenants t ON t.id = j.tenant_id WHERE j.status IN ('pending', 'running') ... LIMIT 20` returns rows across tenants with no tenant_id filter.
  - _Fix:_ If the intent is per-tenant data, add `WHERE j.tenant_id = ${tenantId}` (pass tenantId from request). If cross-tenant admin visibility is intended, make that explicit (e.g. requirePlatformAdmin and/or use an explicit ALL_TENANTS constant) and document it; otherwise restrict the query to the caller's tenant.
- **server/routes/projects.ts:616** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — missing tenant filter. UPDATE against a tenant-scoped table must constrain by tenant_id.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}). If you already validated project ownership earlier, still include the tenant_id WHERE clause as defense-in-depth.
- **server/routes/projects.ts:684** — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} — missing tenant filter (second occurrence).
  - _Fix:_ Add tenant_id constraint to the UPDATE (WHERE id = ${projectId} AND tenant_id = ${tenantId}) to ensure the mutation is limited to the caller's tenant.
- **server/routes/projects.ts:703** — SELECT * FROM project_files WHERE project_id = ${projectId} — no tenant_id constraint on the project_files table.
  - _Fix:_ Add an explicit tenant constraint (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or join to a tenant-filtered parent row (and ensure that join includes a p.tenant_id = ${tenantId}) so every read against tenant-scoped tables is constrained to the caller's tenant.
- **server/routes/projects.ts:726** — SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ${'uploads/' + filename} — missing tenant_id constraint.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or otherwise assert tenant ownership (and still prefer an explicit tenant_id filter) before returning file metadata.
- **server/self-improvement.ts:262** — Raw SQL (db.execute(sql`...`)) is used with request-derived values (config.personaId and tenantId) to SELECT a persona. Using string-interpolated/raw SQL with request-derived values increases injection/isolation risk and bypasses Drizzle's structured query builders.
  - _Fix:_ Use Drizzle's query builder (db.select().from(personas).where(...)) or parameterized Drizzle calls and/or split into an ownership-check using the agent_desks table with eq(agent_desks.tenant_id, tenantId). Avoid inline raw SQL templates for request-derived values.
- **server/self-improvement.ts:410** — Raw SQL UPDATE on personas (db.execute(sql`UPDATE personas ... WHERE p.id = ${persona.id} AND EXISTS (SELECT ... ad.tenant_id = ${config.tenantId})`)) is executed via string-interpolated SQL with request-derived tenantId/persona.id. Even though the query includes an ownership check, using raw SQL with interpolated inputs increases risk and bypasses Drizzle's safer builders.
  - _Fix:_ Use Drizzle's update API (db.update(personas).set({ soul: newSoul }).where(and(eq(personas.id, persona.id), /* ownership check */ ... ))). Prefer an explicit ownership check with a separate safe query, then perform the update with an additional tenant_id predicate so the update cannot affect other tenants.
- **server/self-reflection.ts:364** — Raw SQL SELECT (db.execute(sql`SELECT id FROM memory_entries WHERE tenant_id = ${tenantId} ...`)) uses interpolated tenantId and other request-driven values in a sql template. This pattern is repeated across the file and increases injection/isolation risk compared to Drizzle query builders.
  - _Fix:_ Replace raw sql templates with Drizzle query builder calls (db.select().from(memory_entries).where(...)). Keep the tenant predicate explicit (eq(memory_entries.tenantId, tenantId)). Avoid free-form sql`` interpolation of request-derived inputs.
- **server/self-reflection.ts:392** — INSERT INTO memory_entries is performed via db.execute(sql`INSERT INTO memory_entries (...) VALUES (${tenantId}, ${personaId || 2}, ${fact}, ...)`) — raw SQL with interpolated request-derived values rather than using Drizzle insert helpers.
  - _Fix:_ Use Drizzle insert: await db.insert(memory_entries).values({ tenantId, personaId: personaId || 2, fact, category: 'actionable_lesson', source: 'self_reflection', createdAt: sql`NOW()` }) to ensure parameters are handled consistently and keep tenant_id explicit.
- **server/situation-room.ts:156** — Raw SQL SELECT used to aggregate persona workloads (db.execute(sql`SELECT ... FROM conversations c LEFT JOIN messages m ... WHERE c.tenant_id = ${tenantId} ...`)). Using string-interpolated raw SQL with request-derived tenantId rather than Drizzle builders increases risk and makes reviews harder.
  - _Fix:_ Replace with Drizzle query builder (db.select().from(conversations).leftJoin(messages,...).where(eq(conversations.tenantId, tenantId)) ...) so tenant scoping is explicit and parameterization is consistent.
- **server/situation-room.ts:215** — Raw SQL SELECT against tenant-scoped table "sentiment_events" using db.execute(sql`... WHERE tenant_id = ${tenantId} ...`). This uses string-interpolated SQL with tenantId instead of Drizzle's structured API.
  - _Fix:_ Use Drizzle's db.select().from(sentiment_events).where(eq(sentiment_events.tenantId, tenantId)) and aggregate via the query builder. Keep tenant predicate explicit to enforce isolation.
- **server/skill-evolution.ts:46** — INSERT/ON CONFLICT into tenant-scoped table "tool_performance" is executed via db.execute(sql`...`) with interpolated request-derived tenantId/toolName/duration. Using inline sql templates with request-derived values increases injection/isolation risk and bypasses Drizzle helpers.
  - _Fix:_ Use Drizzle's insert().values(...).onConflictDoUpdate(...) API (or parameterized queries) so values are properly bound and tenant_id is explicit. Ensure WHERE/ON CONFLICT targets include tenant_id to prevent cross-tenant effects.
- **server/skill-evolution.ts:103** — Raw SQL SELECT (db.execute(sql`SELECT ... FROM tool_performance WHERE tenant_id = ${tenantId} ...`)) uses interpolated tenantId. This pattern bypasses Drizzle builders and uses sql template interpolation with request-derived inputs.
  - _Fix:_ Switch to Drizzle query builder (db.select().from(tool_performance).where(eq(tool_performance.tenantId, tenantId)).and(...)) to ensure proper parameterization and explicit tenant scoping.
- **server/skill-evolution.ts:376** — INSERT into tenant-scoped table "wellbeing_interventions" is performed via db.execute(sql`INSERT INTO wellbeing_interventions (...) VALUES (${tenantId}, ${userId}, ${interventionId}, ...)`) using raw SQL interpolation with request-supplied values.
  - _Fix:_ Use Drizzle insert (db.insert(wellbeing_interventions).values({...})) to bind parameters safely and make tenant scoping explicit. If userId/interventionId reference other tenant-scoped rows, validate ownership before inserting.
- **server/storage.ts:722** — getDailyNotes(personaId?, tenantId?) can be called without tenantId and will return daily_notes rows without applying a tenant filter (tenantScope optional), potentially exposing other tenants' notes.
  - _Fix:_ Require tenantId for tenant-scoped reads or make the tenant parameter mandatory in the public API. Use requiredTenantScope(dailyNotes.tenantId, tenantId) to fail-closed when tenantId is missing, or document and gate this function as admin-only if global access is intended.
- **server/webhookHandlers.ts:1216** — Reads tenant via storage.getTenant(tenantId) where tenantId is taken from invoice.metadata (untrusted). Using metadata-derived tenant id to select tenant data without cross-checking can leak or mis-associate information.
  - _Fix:_ When a tenant id is supplied in webhook metadata, validate it against a trusted anchor (for example: map Stripe customer id → tenant, or confirm via a Stripe API lookup) before using it to read or act on tenant-scoped data. Prefer resolving tenant by server-side stored stripe_customer_id when possible.

### LOW (1)

- **server/routes.ts:2060** — SELECT from tenant-scoped table `presenter_sessions` without tenant constraint in internal (SESSION_SECRET) /api/presenter/:token/repair-images handler: SELECT id, tenant_id, presentation_id, slides FROM presenter_sessions WHERE token = ${token}
  - _Fix:_ Even for internal-key-gated endpoints, prefer scoping by tenant when possible, or document and harden the capability token usage. Alternatively, require both token and expected tenant_id (or include tenant binding in the token) and include AND tenant_id = ${expectedTenantId} so the DB read is explicitly tenant-scoped.

### Resolved — confirmed false positives (167)

_Cleared by precision triage or the human-verified allowlist; shown for the audit trail, not counted toward pass/fail._

- **server/agentic/resume-worker.ts:65** _(system-sweep)_ — Unscoped read from tenant-scoped tables: the main candidate query (SELECT ... FROM agent_runs LEFT JOIN agent_approvals ...) does not filter by tenant_id, so it enumerates rows across all tenants.
  - _Why false positive:_ Global maintenance sweep over all tenants; uses row-carried tenant_id and returns only aggregate counts, not tenant-facing mixed data.
- **server/chat-engine/project-context.ts:159** _(fk-transitive)_ — SELECT from project_files uses only project_id and does not include tenant_id (or an equivalent tenant constraint). Although projectId was previously validated to belong to the tenant, the invariant requires each tenant-scoped table access to include an explicit tenant constraint in SQL.
  - _Why false positive:_ project_files has no tenant_id and is project-scoped child data; access is via project_id after project was verified for tenant.
- **server/chat-engine/project-context.ts:171** _(fk-transitive)_ — SELECT from project_notes filters only by project_id and lacks tenant_id. This reads a tenant-scoped table (project_notes) without an explicit tenant constraint.
  - _Why false positive:_ project_notes has no tenant_id and is project-scoped child data; access is via project_id after project was verified for tenant.
- **server/doc-collections.ts:294** _(upstream-verified)_ — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} — update uses only id and omits tenant_id. This can modify another tenant's chunk if row.id is ambiguous across tenants.
  - _Why false positive:_ Rows being updated were selected immediately above from doc_chunks with collection_id and tenant_id, so row ids are already tenant-scoped in the same call path.
- **server/job-queue.ts:160** _(system-sweep)_ — claimDueJobs performs an UPDATE on agent_jobs (claiming due jobs) without constraining by tenant_id. The inner SELECT used to pick rows also has no tenant filter. This updates/returns rows across all tenants.
  - _Why false positive:_ Global worker claim loop intentionally operates across tenants and returns each job with its own tenant_id for per-row handling.
- **server/job-queue.ts:196** _(system-sweep)_ — reclaimExpiredLeases STEP 1: UPDATE agent_jobs to failed_terminal for expired leases executes without any tenant_id constraint, mutating rows from all tenants.
  - _Why false positive:_ Platform-wide lease-reclaim housekeeping mutates only job status fields across tenants and does not expose mixed tenant data.
- **server/job-queue.ts:209** _(system-sweep)_ — reclaimExpiredLeases STEP 2: UPDATE agent_jobs to requeue expired leases runs without tenant_id constraint and will affect rows across tenants.
  - _Why false positive:_ Platform-wide requeue housekeeping updates only status/scheduling fields across tenants and does not expose mixed tenant data.
- **server/job-queue.ts:238** _(internal-worker-pk)_ — completeJob UPDATE on agent_jobs updates status/completion by id/attempts but does not include tenant_id in the WHERE clause. A stale/borrowed writer could complete a job for the wrong tenant.
  - _Why false positive:_ Internal worker completion updates a globally unique server-generated job id with attempt fencing, not a cross-tenant request-boundary id.
- **server/job-queue.ts:298** _(internal-worker-pk)_ — failJob UPDATE to mark a job failed_terminal updates agent_jobs WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but lacks tenant_id constraint — allowing cross-tenant mutation.
  - _Why false positive:_ Internal worker failure path updates by server-generated global job id with attempt fencing, not untrusted tenant-supplied input.
- **server/job-queue.ts:318** _(internal-worker-pk)_ — failJob's non-terminal retry UPDATE (setting pending/next_run_at) updates agent_jobs without a tenant_id constraint in the WHERE clause.
  - _Why false positive:_ Worker retry-state update uses server-side job PK with CAS fields (status/attempts); not shown crossing an untrusted request boundary.
- **server/lib/ideabrowser-backfill.ts:101** _(fk-transitive)_ — SELECT from project_files lacks an explicit tenant_id constraint (WHERE tenant_id = <tenant>) — query filters only by project_id and file_name.
  - _Why false positive:_ project_files has no tenant_id and is scoped through tenant-owned projects via project_id.
- **server/lib/ideabrowser-backfill.ts:116** _(fk-transitive)_ — SELECT from project_notes lacks an explicit tenant_id constraint — it filters only by project_id and note.
  - _Why false positive:_ project_notes has no tenant_id and is scoped through tenant-owned projects via project_id.
- **server/lib/scheduled-post-runner.ts:61** _(system-sweep)_ — SELECT ... FROM scheduled_posts executed without any tenant_id filter (CTE selects due rows across all tenants and then updates them). This allows cross-tenant locking/processing of scheduled_posts in a single operation.
  - _Why false positive:_ Heartbeat-style batch runner intentionally processes due rows across tenants, carrying each row's tenant_id forward rather than serving mixed tenant data.
- **server/oauth-subscriptions.ts:906** _(system-sweep)_ — SELECT reads all rows from tenant-scoped table `oauth_subscriptions` without constraining by tenant_id (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE). This returns sensitive per-tenant tokens/metadata across all tenants.
  - _Why false positive:_ Startup validation is a platform-wide maintenance pass over active subscriptions; it handles each row with its own tenant_id.
- **server/oauth-subscriptions.ts:915** _(system-sweep)_ — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' queries a tenant-scoped table without filtering by tenant_id, enumerating tenants that have subscriptions.
  - _Why false positive:_ Startup auto-reconnect enumerates subscription tenant_ids for a cross-tenant maintenance task, not a tenant-facing data return.
- **server/oauth-subscriptions.ts:1258** _(system-sweep)_ — proactiveTokenRefresh executes SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE without a tenant_id constraint, reading subscription rows for all tenants (sensitive data exposure and cross-tenant action).
  - _Why false positive:_ Proactive token refresh is a periodic maintenance sweep over active subscriptions, processing each row by its carried tenant_id.
- **server/oauth-subscriptions.ts:1328** _(system-sweep)_ — forceRefreshAllSubscriptions runs SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE with no tenant filter, then attempts refreshes across tenants. This performs cross-tenant reads/writes on a tenant-scoped table without explicit admin/authorization checks.
  - _Why false positive:_ Force-refresh-all is an explicit global cron-style maintenance operation over active subscriptions with per-row tenant handling.
- **server/persona-export.ts:33** _(fk-transitive)_ — SELECT * FROM personas WHERE id = ${personaId} does not verify tenant ownership of the persona. The handler reads a tenant-scoped row (persona) by id provided by the caller without constraining by tenant_id, risking cross-tenant data exposure.
  - _Why false positive:_ personas has no tenant_id per schema; tenant-scoped companion data is filtered by tenant_id, so first-pass tenant_id-missing claim on personas is inapplicable.
- **server/plan-executor.ts:610** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'plans' performed without constraining by tenant_id (CAS claim: `WHERE id = ${planId} AND status = 'approved'`). This lets the executor change a plan row by id across tenants without an explicit tenant constraint.
  - _Why false positive:_ Executor claims a plan by server-side PK and immediately derives tenant_id from RETURNING for subsequent scoped updates; not shown as caller-controlled cross-tenant access.
- **server/process-governor.ts:899** _(upstream-verified)_ — UPDATE against tenant-scoped table `agent_desks` does not constrain by tenant_id (WHERE id = ${desk.id} only). This allows modifying rows from other tenants if id values collide.
  - _Why false positive:_ desk.id values come from the immediately preceding SELECT constrained by ad.tenant_id = tenantId in the same flow.
- **server/recurring-messages.ts:307** _(system-sweep)_ — UPDATE against tenant-scoped table `agent_knowledge` omits tenant_id in WHERE clause: `UPDATE agent_knowledge SET content = ... WHERE id = ${r.id}`. This can modify another tenant's row if IDs collide or if the caller has access to rows across tenants.
  - _Why false positive:_ scheduler is a platform-wide maintenance loop processing each row with its own r.tenant_id; update is by server-selected PK and only advances housekeeping fields in the row payload
- **server/reference-learner.ts:296** _(upstream-verified)_ — UPDATE against tenant-scoped table `memory_entries` omits tenant_id in WHERE clause: `UPDATE memory_entries SET fact = ..., WHERE id = ${storedId}`. After a tenant-scoped SELECT, the code updates by id only without verifying tenant ownership in the update, risking cross-tenant modification.
  - _Why false positive:_ storedId comes from an immediately prior SELECT on memory_entries constrained by input.tenantId and persona/category/fact prefix in the same path
- **server/research-engine.ts:170** _(system-sweep)_ — UPDATE against tenant-scoped table research_sessions lacks a tenant_id constraint (cleanupZombieSessions). This UPDATE can modify rows across all tenants.
  - _Why false positive:_ global zombie-session cleanup intentionally sweeps stale running sessions across tenants and only mutates status/ended_at/summary housekeeping fields, returning ids for logs only
- **server/routes/conversations.ts:123** _(fk-transitive)_ — INSERT into a likely tenant-scoped join table 'project_conversations' omits tenant_id. The query `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${projectId}, ${conv.id})` does not set tenant_id.
  - _Why false positive:_ project_conversations schema note shows no tenant_id; insert is into child join table after project ownership is checked via projects.tenant_id and conversation was just created for tenantId
- **server/routes/conversations.ts:256** _(fk-transitive)_ — INSERT into a tenant-scoped table 'project_notes' omits tenant_id. The query `INSERT INTO project_notes (project_id, note, author) VALUES (...)` does not set tenant_id.
  - _Why false positive:_ project_notes schema note shows no tenant_id; note is a child of tenant-scoped projects and projId was verified through join with projects.tenant_id earlier in same path
- **server/sculptor.ts:105** _(internal-worker-pk)_ — UPDATE to sculptor_sessions omits tenant constraint (UPDATE sculptor_sessions SET status = 'failed' ... WHERE id = ${session.id}). This allows cross-tenant writes if session.id is forged or collides.
  - _Why false positive:_ update uses session.id from a server-side INSERT RETURNING * in the same function, not from an untrusted request boundary
- **server/sculptor.ts:113** _(internal-worker-pk)_ — UPDATE to sculptor_sessions omits tenant constraint (UPDATE sculptor_sessions SET conversation_id = ${runResult.conversationId} WHERE id = ${session.id}). This can modify a session belonging to another tenant.
  - _Why false positive:_ update targets the freshly created sculptor_sessions row by server-generated session.id from same function, not caller-controlled
- **server/second-opinion.ts:417** _(internal-worker-pk)_ — settleFusionReservation updates agent_cost_ledger without scoping to tenant (UPDATE agent_cost_ledger ... WHERE id = ${reservationId}). The ledger is tenant-scoped; updating by id alone can alter another tenant's ledger row.
  - _Why false positive:_ Reservation settlement is an internal helper updating a server-created ledger row by serial PK, not from a request boundary in shown call path.
- **server/second-opinion.ts:438** _(internal-worker-pk)_ — releaseFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} without tenant constraint. This can release/modify reservations for other tenants.
  - _Why false positive:_ Release helper is internal/server-invoked and mutates a server-created reservation row by PK only.
- **server/storage.ts:976** _(system-sweep)_ — archiveExpiredMemories() performs a global UPDATE on memory_entries (status -> 'archived') with no tenant_id constraint, mutating rows across all tenants.
  - _Why false positive:_ Global archival sweep only flips housekeeping status on expired memory rows and returns count/ids, not tenant-mixed content.
- **server/storage.ts:991** _(system-sweep)_ — archiveStaleMemories() issues a global UPDATE on memory_entries to 'archived' without any tenant filter, potentially modifying all tenants' data.
  - _Why false positive:_ Platform-wide stale-memory archival is housekeeping-only status mutation, not tenant-facing data mixing.
- **server/tools.ts:2931** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` does not set tenant_id. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) without an explicit tenant_id column/value, relying on project_id alone — violating the invariant that every INSERT into a tenant-scoped table must pass tenant_id explicitly.
  - _Why false positive:_ project_files has no tenant_id per schema and is scoped transitively through tenant-checked parent project_id.
- **server/tools.ts:2946** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (project_id, note, author) ... lacks tenant_id, allowing creation of rows without explicit tenant scoping.
  - _Why false positive:_ project_notes has no tenant_id per schema and insert is preceded by tenant ownership check on parent project.
- **server/tools.ts:2806** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` (in project create flow) does not set tenant_id. The code INSERTs (project_id, conversation_id) ON CONFLICT DO NOTHING without including tenant_id, which can result in rows that lack explicit tenant scoping.
  - _Why false positive:_ project_conversations has no tenant_id per schema; insert occurs only after tenant-scoped conversation/project linkage succeeds.
- **server/tools.ts:2969** _(fk-transitive)_ — INSERT into tenant-scoped table `project_conversations` (link_conversation handler) does not set tenant_id. The code inserts (project_id, conversation_id) without an explicit tenant_id even though a tenant check was performed earlier — the row itself is still created without tenant_id.
  - _Why false positive:_ project_conversations has no tenant_id per schema and both project ownership and conversation tenant membership are validated before insert.
- **server/ab-optimizer.ts:110** _(system-sweep)_ — runDueAbExperiments selects from experiments without constraining to a tenant (SELECT id, tenant_id, hypothesis, ... FROM experiments WHERE category = 'ab_test' AND status = 'running'). This exposes metadata from all tenants to the heartbeat caller.
  - _Why false positive:_ heartbeat maintenance sweep intentionally scans running experiments across tenants and processes each row with its own tenant_id before scoped updates/events
- **server/agentic-engines.ts:161** _(system-sweep)_ — retryPendingMinervaRouting SELECTs ai_insights without a tenant filter (SELECT id, tenant_id, category, ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = ...). This reads rows for all tenants.
  - _Why false positive:_ durability sweep intentionally reads pending insights across tenants and handles each row using its row-carried tenant_id; no tenant-facing mixed output shown
- **server/agentic-engines.ts:248** _(system-sweep)_ — autoApplyEligibleInsights can perform a global UPDATE on ai_insights when tenantId is omitted (UPDATE ... WHERE status = 'new' AND category IN (...) ${tenantScope}). When tenantScope is empty this updates rows across all tenants.
  - _Why false positive:_ explicit backfill sweep intentionally bulk-updates eligible insights across tenants and then processes returned rows per-row with validated tenant_id; housekeeping/status change not tenant-facing data exposure
- **server/agentic/approvals.ts:134** _(system-sweep)_ — expireStaleApprovals performs a bulk UPDATE on agentApprovals with no tenant constraint (UPDATE agentApprovals SET status = 'expired' WHERE status = 'pending' AND expiresAt < ...). This will change approvals across all tenants.
  - _Why false positive:_ stale-approval expiry is a platform-wide housekeeping sweep; returned rows carry tenant_id and downstream run updates are per-row tenant-scoped
- **server/agentic/department-budgets.ts:143** _(system-sweep)_ — Query selects from tenant-scoped table `department_budgets` without constraining by tenant_id: `SELECT DISTINCT department FROM department_budgets WHERE status = 'active'`. Later code uses `row.tenant_id` but the SELECT did not include tenant_id; this can lead to incorrect/undefined tenant values and cross-tenant information access/leakage.
  - _Why false positive:_ heartbeat sweep intentionally scans active budgets across tenants, selects tenant_id explicitly, and emits per-row tenant-scoped events without mixed tenant output
- **server/agentic/wake-scheduler.ts:142** _(system-sweep)_ — UPDATE on agent_wake_schedules claims pending wakes using an inner SELECT that does not constrain by tenant_id (no WHERE tenant_id = ...). This allows the scheduler to atomically claim and mutate rows across all tenants.
  - _Why false positive:_ heartbeat scheduler intentionally claims due wakes across tenants and emits/finalizes each claimed row using its own tenant_id
- **server/business-tools.ts:95** _(fk-transitive)_ — INSERT into invoice_items does not include tenant_id. This writes tenant-scoped data (invoice line items) without explicitly setting tenant_id, violating the invariant that every INSERT into tenant-scoped tables must include the caller's tenant and risking cross-tenant data leakage.
  - _Why false positive:_ invoice_items has no tenant_id and is a child table of tenant-scoped invoices; parent invoice is created in same transaction with tenant_id set
- **server/channel-routing.ts:31** _(global-table)_ — listChannelRoutes joins the personas table (tenant-scoped) but does not constrain the join by tenant_id. This reads tenant-scoped persona rows without any tenant filter, allowing cross-tenant data exposure.
  - _Why false positive:_ channel_routes is a platform-global routing table with no tenant_id by design, so joining referenced personas is part of global routing rather than per-tenant isolation logic
- **server/channel-routing.ts:54** _(global-table)_ — setChannelRoute inserts persona_id provided by the caller into the global channel_routes table without validating that the persona belongs to the caller's tenant. This allows one tenant to point a route at another tenant's persona.
  - _Why false positive:_ channel_routes is described as global and referenced personas table has no tenant_id; no evidence of tenant-scoped isolation requirement here
- **server/chat-engine/project-context.ts:23** _(fk-transitive)_ — SELECT from project_conversations (`pc`) is executed without constraining pc.tenant_id to the caller's tenant. The query joins projects and filters p.tenant_id, but it still reads rows from project_conversations without an explicit tenant filter.
  - _Why false positive:_ project_conversations has no tenant_id and is transitively scoped by joined projects filtered on p.tenant_id
- **server/chat-engine/project-context.ts:44** _(fk-transitive)_ — EXISTS(...) subquery reads project_conversations (`pc`) without constraining pc.tenant_id. This lets the EXISTS test touch a tenant-scoped table without an explicit tenant filter.
  - _Why false positive:_ project_conversations has no tenant_id; EXISTS only tests linkage to tenant-scoped projects already filtered by p.tenant_id
- **server/chat-engine/project-context.ts:181** _(fk-transitive)_ — Query joining project_conversations (pc) and conversations (c) filters c.tenant_id but does not include a tenant constraint on pc. This reads project_conversations without explicitly scoping it to the tenant.
  - _Why false positive:_ project_conversations has no tenant_id and conversations join is constrained by c.tenant_id, providing transitive isolation
- **server/cmmc-assessments.ts:150** _(upstream-verified)_ — getCmmcAccessByToken performs a SELECT joining cmmc_assessment_invitations and cmmc_assessments and filters by token/expires/revoked but does not include an explicit tenant_id constraint on the tenant-scoped tables. This reads tenant-scoped tables without an explicit tenant filter.
  - _Why false positive:_ token itself is the capability being resolved; join enforces same-tenant pairing between invitation and assessment, so no caller tenant filter is required at this lookup point
- **server/commitments.ts:135** _(system-sweep)_ — SELECT from tenant-scoped table 'commitments' is performed without any tenant_id constraint (SELECT ... FROM commitments WHERE status = 'active' ...). This reads rows across all tenants into application memory.
  - _Why false positive:_ platform-wide scanner intentionally sweeps all tenants and processes each row with its own tenantId while emitting only redacted admin digest data
- **server/data-protection.ts:150** _(system-sweep)_ — SELECT FROM tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This reads rows across all tenants.
  - _Why false positive:_ global expiry purge intentionally scans across tenants and then handles each row using its own tenant_id for scoped deletes/backups
- **server/email.ts:370** _(system-sweep)_ — SELECT on inbox_messages missing tenant filter: `SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1` executes without constraining by tenant_id, allowing cross-tenant discovery/matches.
  - _Why false positive:_ shared-inbox polling is a platform-wide ingestion loop deduping by external message_id; it does not return tenant data and only skips already-stored messages.
- **server/email.ts:415** _(system-sweep)_ — Backfill query and update operate on inbox_messages without tenant scoping: `SELECT id, message_id FROM inbox_messages ...` and later `UPDATE inbox_messages SET ... WHERE id = ${row.id}` lack tenant_id constraints, so reads and writes may touch rows across tenants.
  - _Why false positive:_ backfillEmptyBodies is a housekeeping sweep across inbox_messages that fills body fields per row by primary key and does not mix tenant data into one tenant-facing output.
- **server/embeddings.ts:845** _(system-sweep)_ — Backfill reads/updates memory_entries without tenant_id filtering: `SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200` and subsequent `UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}` are performed across all tenants.
  - _Why false positive:_ embedding_vec backfill is a maintenance loop over all tenants; each row is updated individually by its own id and only housekeeping/vector fields are mutated.
- **server/embeddings.ts:814** _(system-sweep)_ — BackfillMissingKnowledgeEmbeddings selects agent_knowledge rows across all tenants: `SELECT id, tenant_id, title, content FROM agent_knowledge WHERE ... LIMIT 50` reads tenant-scoped data without constraining to a tenant context.
  - _Why false positive:_ missing-embedding backfill intentionally scans tenant rows globally but processes each row with its carried tenant_id via storeEmbeddingVec, with no tenant-facing mixed output.
- **server/felix-brain.ts:23** _(system-sweep)_ — SELECT from tenant-scoped table 'agent_runs' without constraining tenant_id in the WHERE clause (reads rows across all tenants).
  - _Why false positive:_ boot-time rehydration intentionally scans running/pending agent_runs across tenants and rehydrates in-memory state keyed by each row's own tenant_id without mixing into one tenant response.
- **server/google-drive.ts:866** _(upstream-verified)_ — SELECT from tenant-scoped 'projects' table filters only by project id (WHERE projects.id = $projectId) and does not include the caller's tenant_id constraint.
  - _Why false positive:_ same function immediately verifies project.tenantId === tenantId and fails closed before using the row, so ownership is enforced in this call path.
- **server/google-drive.ts:879** _(upstream-verified)_ — UPDATE on tenant-scoped 'projects' table (setting driveFolderId/driveFolderUrl) uses WHERE projects.id = $projectId only and lacks a tenant_id constraint.
  - _Why false positive:_ update occurs only after the same-function ownership check at lines 866-872 confirmed the project belongs to tenantId.
- **server/google-drive.ts:1473** _(upstream-verified)_ — SELECT used to resolve a project in uploadAndShare queries projects by id only (WHERE projects.id = $projectId) without scoping the statement to the caller's tenant.
  - _Why false positive:_ same call path checks params.tenantId against proj.tenantId before proceeding, and then ensureProjectFolder re-validates ownership.
- **server/health-audit.ts:333** _(system-sweep)_ — UPDATE of code_proposals sets status = 'archived_stale' using WHERE id = ANY(...) without constraining tenant_id. This can modify rows from any tenant.
  - _Why false positive:_ stale-proposal archival is a maintenance mutation of status only over selected ids; it does not expose mixed tenant content beyond housekeeping.
- **server/health-audit.ts:368** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) performs a write with no tenant constraint — it may disable tasks across tenants.
  - _Why false positive:_ Cross-tenant housekeeping sweep disables stale heartbeat tasks in bulk; no tenant-facing data return or mixing.
- **server/heartbeat-schedules.ts:60** _(system-sweep)_ — SELECT * FROM research_schedules is executed with no tenant_id constraint. This reads tenant-scoped schedule rows across tenants without an explicit tenant filter.
  - _Why false positive:_ Scheduler intentionally scans due research schedules across tenants and processes each row with its own tenant_id.
- **server/heartbeat-task-scheduler-storage.ts:11** _(system-sweep)_ — getDueHeartbeatTasksAcrossTenants performs SELECT * FROM heartbeat_tasks ... with no tenant_id constraint. This reads tenant-scoped heartbeat_tasks without scoping to a tenant.
  - _Why false positive:_ Dedicated scheduler-only API intentionally enumerates due heartbeat tasks across tenants for per-row processing.
- **server/heartbeat-task-scheduler-storage.ts:43** _(system-sweep)_ — UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... is executed without a tenant_id constraint. Updating tenant-scoped rows without tenant_id allows cross-tenant modification if ids are manipulated or assumptions change.
  - _Why false positive:_ Scheduler claim step mutates housekeeping fields on previously enumerated due task ids; cross-tenant by design, no tenant data mixing.
- **server/heartbeat.ts:257** _(system-sweep)_ — SELECT * FROM event_log WHERE status = 'pending' ... is executed without a tenant_id constraint. This reads pending events across tenants and then processes them; initial read is not tenant-scoped.
  - _Why false positive:_ Heartbeat worker intentionally processes pending events across tenants and carries each event's tenant_id into downstream routing/update.
- **server/heartbeat.ts:1579** _(global-table)_ — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... is executed without a tenant_id constraint. This can return another tenant's heartbeat_logs row and affect per-tenant scheduling behavior.
  - _Why false positive:_ Schema note says heartbeat_logs has no tenant_id; this log lookup is against a non-tenant-scoped table.
- **server/heartbeat-startup.ts:81** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id} is executed without a tenant_id constraint. This updates a tenant-scoped heartbeat_tasks row by id only, which can unintentionally affect the wrong tenant if assumptions change.
  - _Why false positive:_ Startup maintenance loop normalizes model field across due heartbeat tasks platform-wide; housekeeping-only mutation in sweep.
- **server/heartbeat-startup.ts:85** _(system-sweep)_ — UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id} is executed without a tenant_id constraint. This mutates a tenant-scoped row without confirming tenant ownership.
  - _Why false positive:_ Startup maintenance loop applies reflection model normalization platform-wide; housekeeping-only mutation in sweep.
- **server/job-queue.ts:377** _(upstream-verified)_ — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} reads a job row by id without any tenant_id constraint. While the function's purpose is to return the tenant, calling code could misuse this unscoped SELECT in tenant-facing contexts.
  - _Why false positive:_ Helper is explicitly documented as admin-only tenant resolver used to obtain immutable tenant scope before admin retry.
- **server/lib/aevo-meta-editor.ts:446** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` performed without constraining by tenant (WHERE does not include tenantId). This allows a row id to be updated across tenants if an attacker can supply/guess runId.
  - _Why false positive:_ runId is the server-generated primary key from the immediately preceding INSERT in the same function, not from an untrusted request boundary.
- **server/lib/aevo-meta-editor.ts:456** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` performed without constraining by tenant (WHERE only filters by id). Missing tenant check may allow cross-tenant modification when runId is re-used or guessed.
  - _Why false positive:_ updates the same server-created procedure_evolution_runs row by its just-inserted PK runId within one internal flow.
- **server/lib/aevo-meta-editor.ts:500** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `procedure_evolution_runs` performed without constraining by tenant (WHERE only filters by id). This update path can modify another tenant's run row if runId is not tenant-scoped in the DB-level predicate.
  - _Why false positive:_ final status update targets the run row created earlier in this function via server-generated runId, not caller-controlled input.
- **server/lib/chronicle-precision.ts:99** _(fk-transitive)_ — Query reads from tenant-scoped table `skills` without constraining it to the caller's tenant. The LEFT JOIN uses `LEFT JOIN skills s ON s.id = ps.promoted_skill_id` but does not include `s.tenant_id = <tenant>`; this can leak or mix rows across tenants if skill ids are not globally namespaced.
  - _Why false positive:_ schema note shows skills has no tenant_id; join is by promoted_skill_id from tenant-scoped proposed_skills row, so isolation is transitive through the parent-scoped reference.
- **server/lib/ideabrowser-backfill.ts:105** _(fk-transitive)_ — INSERT into project_files does not set tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Why false positive:_ project_files has no tenant_id per schema and is a child of tenant-scoped projects via project_id, so omitting tenant_id on insert is by design.
- **server/lib/ideabrowser-backfill.ts:120** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT.
  - _Why false positive:_ project_notes has no tenant_id per schema and is a child table isolated through tenant-scoped projects by project_id.
- **server/lib/ideabrowser-dev-sync.ts:140** _(fk-transitive)_ — INSERT into project_notes (via SELECT ... WHERE NOT EXISTS) does not include tenant_id; the NOT EXISTS subquery also omits tenant_id — reads/writes against project_notes are not tenant-constrained.
  - _Why false positive:_ project_notes has no tenant_id and insert/existence check are keyed by project_id of the tenant-scoped project row just created in this flow.
- **server/lib/ideabrowser-dev-sync.ts:151** _(fk-transitive)_ — INSERT into project_files (via SELECT ... WHERE NOT EXISTS) omits tenant_id; the NOT EXISTS check also lacks tenant_id — write and existence-check are not tenant-scoped.
  - _Why false positive:_ project_files has no tenant_id and is correctly scoped transitively by the tenant-owned project_id just inserted above.
- **server/lib/mcp-api-keys.ts:166** _(upstream-verified)_ — SELECT against tenant-scoped table mcp_api_keys is performed without an explicit tenant_id WHERE clause. The query resolves a row by key_prefix only: `SELECT id, tenant_id, key_hash, scopes, revoked_at FROM mcp_api_keys WHERE key_prefix = ${prefix} LIMIT 1`. This reads a tenant-scoped table without constraining to the caller's tenant context.
  - _Why false positive:_ this query is the authentication primitive that resolves tenant_id from the presented API key itself before later tenant-scoped use; no caller tenant context exists yet.
- **server/lib/restraint-trust.ts:90** _(upstream-verified)_ — Use of sql.raw() with a column name derived from caller input (the `outcome`→`column` mapping) when updating trust_scores. Although the column name is allowlisted earlier, embedding a runtime-derived identifier via sql.raw() creates a SQL-injection / misuse risk and is flagged by the project's isolation/injection rule.
  - _Why false positive:_ trust_scores update already constrains by tenant_id in the same statement; this is not a cross-tenant isolation flaw.
- **server/lib/scheduled-post-runner.ts:319** _(upstream-verified)_ — Building a Postgres array literal (arrLit) from caller-supplied platforms and embedding it into SQL. Platforms are request-derived and are turned into a string array literal; this is a string-interpolated SQL construction based on user input and risks injection or parsing errors.
  - _Why false positive:_ finding is about SQL construction of a text[] literal, not tenant isolation; tenant_id is explicitly written on insert.
- **server/memory-graph.ts:130** _(upstream-verified)_ — UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} executes without any tenant_id predicate — this can modify another tenant's category if a crafted categoryId is supplied or an attacker can cause a mismatched id to be used.
  - _Why false positive:_ catId comes from ensureCategory/topLevel resolved within the same tenant-scoped flow; categories were selected/created with tenant_id = tenantId just above.
- **server/memory-graph.ts:709** _(upstream-verified)_ — UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id} has no tenant_id predicate — this can update a memory row belonging to another tenant.
  - _Why false positive:_ mem.id is taken from a prior SELECT on memory_entries constrained by tenant_id = tenantId in the same function; update targets only those fetched rows.
- **server/mpeg-engine.ts:1058** _(fk-transitive)_ — INSERT into project_files does not include tenant_id column (INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) ...). Per the project invariant, every INSERT into tenant-scoped tables must include tenant_id explicitly. Relying on project_id/foreign-key alone is not sufficient.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped transitively via project_id; assertProjectInTenant verifies parent project ownership before insert.
- **server/mpeg-engine.ts:1447** _(fk-transitive)_ — INSERT into project_files (in produceVideoParallel) omits tenant_id in the column list and VALUES. Even though ownership is checked via assertProjectInTenant, the invariant requires tenant_id to be set explicitly on every tenant-scoped INSERT.
  - _Why false positive:_ project_files has no tenant_id column and is tenant-scoped transitively via project_id; guarded by assertProjectInTenant before insert.
- **server/pace-control.ts:85** _(global-table)_ — COUNT(*) query on heartbeat_logs lacks any tenant scoping. checkPace() reads global heartbeat_logs without filtering by tenant_id, allowing cross-tenant telemetry leakage and incorrect rate-limiting across tenants.
  - _Why false positive:_ heartbeat_logs is schema-global with no tenant_id column and pace control is described as a global/per-persona cap over platform heartbeat telemetry.
- **server/pace-control.ts:140** _(global-table)_ — getPaceSnapshot() queries heartbeat_logs for totals and per-persona counts without any tenant_id filter. This exposes aggregated runtime data across all tenants and breaks tenant isolation.
  - _Why false positive:_ heartbeat_logs has no tenant_id column and this snapshot reads platform-global pacing telemetry for dashboard/debugging.
- **server/persona-export.ts:39** _(global-table)_ — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL lacks tenant scoping. If the skills table is tenant-scoped, this will return persona-specific skill rows across tenants (and global rows), leaking data.
  - _Why false positive:_ skills has no tenant_id column in schema; query combines persona-specific/global skill rows from a non-tenant-scoped table.
- **server/plan-executor.ts:985** _(system-sweep)_ — SELECT from tenant-scoped table 'plans' (staleCandidates query) without a tenant_id filter. resumeStuckPlans() enumerates 'executing' plans across all tenants in a single query.
  - _Why false positive:_ resumeStuckPlans is an explicit boot-time recovery loop across tenants; rows carry tenant_id and subsequent mutations are scoped per row, with no tenant-facing mixed output.
- **server/plan-executor.ts:1051** _(system-sweep)_ — SELECT from tenant-scoped table 'plans' (transientFailures query) without a tenant_id filter — this reads failed plans across all tenants to decide transient recovery.
  - _Why false positive:_ transient recovery is part of the same cross-tenant housekeeping sweep; each update is constrained by candidate.tenant_id and only status/log fields are mutated.
- **server/plan-executor.ts:1110** _(system-sweep)_ — SELECT id FROM plans WHERE status = 'approved' is executed without a tenant_id filter; the code then schedules executePlan for each returned id, potentially acting across tenants.
  - _Why false positive:_ pending approved-plan enumeration is a boot-time dispatcher across tenants; it schedules background execution and downstream plan writes validate tenant ownership per row.
- **server/research-engine.ts:457** _(fk-transitive)_ — INSERT into project_notes does not set tenant_id. The INSERTs at the auto-deposit code paths (summary and per-finding) omit tenant_id, risking cross-tenant writes or schema errors if tenant_id is required.
  - _Why false positive:_ project_notes has no tenant_id column and is isolated transitively through project_id; assertProjectInTenant confirms the target project belongs to session.tenantId before insert.
- **server/routes.ts:977** _(signed-url)_ — Query against tenant-scoped table `file_storage` without tenant_id constraint: owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName));
  - _Why false positive:_ Signed URL path is the access control for delivery assets, and non-delivery files are only allowed if one of the returned owner tenantIds matches authedTenantId; tenant id in lookup is used for ownership collision handling, not row disclosure.
- **server/routes.ts:4651** _(fk-transitive)_ — INSERT into project_notes omits tenant_id. The INSERT statement at lines 4651-4654 inserts (project_id, note, author) without specifying tenant_id; per project/notes tenant-scoping policy every INSERT into tenant-scoped tables must include tenant_id explicitly.
  - _Why false positive:_ project_notes has no tenant_id column and is tenant-scoped transitively via validated project_id from conversations WHERE tenant_id = tenantId.
- **server/routes/conversations.ts:119** _(upstream-verified)_ — UPDATE on tenant-scoped table 'conversations' without tenant constraint. The query `UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}` does not include AND tenant_id = ${tenantId}.
  - _Why false positive:_ conv.id is a freshly created server-side conversation for tenantId via storage.createConversation, so updating by that server-generated PK is already scoped in the same call path.
- **server/routes/conversations.ts:125** _(upstream-verified)_ — UPDATE on tenant-scoped table 'projects' without tenant constraint. The query `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}` lacks AND tenant_id = ${tenantId}.
  - _Why false positive:_ project ownership was verified immediately above with SELECT id FROM projects WHERE id = projectId AND tenant_id = tenantId before this timestamp-only update.
- **server/routes/conversations.ts:236** _(upstream-verified)_ — SELECT against tenant-scoped table 'conversations' without tenant constraint. The query `SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId}` does not constrain c.tenant_id.
  - _Why false positive:_ The same call path already loaded conv via storage.getConversation(id, tenantId) or admin helper and rejected tenant mismatch before this join; query only derives linked project_id for that verified conversation.
- **server/routes/inbox-notifications.ts:101** _(upstream-verified)_ — UPDATE against tenant-scoped table `inbox_messages` is missing a tenant_id predicate. The code runs `UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}` without `AND tenant_id = ${tenantId}`.
  - _Why false positive:_ msg was selected just above with WHERE id = msgId AND tenant_id = tenantId, so updating by msg.id in the same request is scoped to the verified tenant-owned row.
- **server/routes/projects.ts:401** _(fk-transitive)_ — INSERT into project_conversations does not set tenant_id. Tenant-scoped tables must receive an explicit tenant_id on INSERT to prevent cross-tenant leakage.
  - _Why false positive:_ project_conversations has no tenant_id column and links tenant-scoped parent rows already validated/created for the same tenant.
- **server/routes/projects.ts:569** _(fk-transitive)_ — INSERT INTO project_notes omits tenant_id in the values. Tenant-scoped tables require tenant_id to be set on INSERT.
  - _Why false positive:_ project_notes has no tenant_id column and insert is scoped transitively through project_id after project ownership check with tenant_id.
- **server/routes/projects.ts:608** _(fk-transitive)_ — INSERT INTO project_files (files-base64 handler) does not include tenant_id. Tenant-scoped tables must have tenant_id set on INSERT.
  - _Why false positive:_ project_files has no tenant_id column and insert is scoped transitively through validated tenant-owned project_id.
- **server/routes/projects.ts:676** _(fk-transitive)_ — INSERT INTO project_files (multipart upload handler) omits tenant_id. Tenant-scoped tables must be explicitly assigned tenant_id on INSERT.
  - _Why false positive:_ project_files has no tenant_id column and insert is scoped transitively through validated tenant-owned project_id.
- **server/routes/projects.ts:755** _(fk-transitive)_ — DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} — DELETE lacks tenant_id constraint on project_files.
  - _Why false positive:_ project_files has no tenant_id column; delete is constrained by validated tenant-owned parent project_id, so isolation is transitive via the parent.
- **server/seed-cleanup.ts:7** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' has no tenant_id filter. heartbeat_tasks appears tenant-scoped so this reads across tenants.
  - _Why false positive:_ boot-time cleanup counts tasks globally for maintenance only and does not return tenant data
- **server/seed-cleanup.ts:16** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true lacks a tenant constraint — this will disable delegation tasks across all tenants.
  - _Why false positive:_ boot-time housekeeping intentionally disables runaway tasks across tenants without exposing mixed tenant data
- **server/seed-cleanup.ts:44** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' has no tenant restriction and will count across tenants.
  - _Why false positive:_ global maintenance count for duplicate session-logger tasks; aggregate only, no tenant-facing data
- **server/seed-cleanup.ts:51** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true is not scoped to tenant_id and may disable tasks for all tenants.
  - _Why false positive:_ maintenance cleanup intentionally disables matching bad tasks platform-wide as housekeeping
- **server/seed-cleanup.ts:58** _(system-sweep)_ — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) has no tenant filter; reads tenant-scoped table across tenants.
  - _Why false positive:_ global maintenance scan for orphaned repo-examination tasks; aggregate only
- **server/seed-cleanup.ts:74** _(system-sweep)_ — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ...) AND enabled = true lacks tenant scoping and can modify other tenants' rows.
  - _Why false positive:_ housekeeping update disables malformed tasks across tenants and does not mix tenant content into outputs
- **server/seed.ts:42** _(system-sweep)_ — Unscoped SELECT on tenant-scoped table research_sessions: `SELECT COUNT(*) as count FROM research_sessions` executes a global read across all tenants with no tenant_id filter.
  - _Why false positive:_ boot-time snapshot import checks global emptiness of research_sessions for one-time seed behavior, not tenant-facing read
- **server/seed.ts:2240** _(system-sweep)_ — Unscoped SELECT joining tenant-scoped tables conversations and projects without constraining to a specific tenant: the query `SELECT DISTINCT c.id, c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id AND p.tenant_id = c.tenant_id WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)` performs a read across all tenants (no WHERE c.tenant_id = <tenant>).
  - _Why false positive:_ boot-time backfill repairs missing project_conversations links across tenants using each row's own ids; not tenant-facing
- **server/seed.ts:3639** _(system-sweep)_ — DELETE FROM heartbeat_tasks is executed without any tenant_id constraint. heartbeat_tasks is tenant-scoped (there is an index on tenant_id), so this DELETE can remove rows across all tenants.
  - _Why false positive:_ global purge of stale auto-disabled heartbeat tasks is housekeeping-only and non-tenant-facing
- **server/seed.ts:3667** _(system-sweep)_ — UPDATE memory_entries ... WHERE deleted_at IS NULL AND fact LIKE '%Manjaro%' ... runs without any tenant_id constraint. memory_entries is tenant-scoped (there is an index on tenant_id), so this UPDATE will modify rows for every tenant.
  - _Why false positive:_ idempotent global text-normalization migration on memory_entries is maintenance-only and does not expose data across tenants
- **server/seed.ts:3693** _(system-sweep)_ — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id constraint. conversations is tenant-scoped (index on tenant_id), so this will update conversations across all tenants.
  - _Why false positive:_ global model-id backfill on conversations is a maintenance migration, not a tenant-facing cross-tenant access path
- **server/self-reflection.ts:380** _(upstream-verified)_ — UPDATE on tenant-scoped table "memory_entries" uses WHERE id = <row id> but does not include tenant_id = <caller tenant>. A row id from another tenant could be updated if supplied/obtained.
  - _Why false positive:_ Updated id is sourced from immediately preceding SELECT scoped by tenant_id = tenantId, so the row id is already tenant-owned in this call path.
- **server/skills-marketplace.ts:209** _(global-table)_ — SELECT against tenant-scoped table 'skills' without tenant_id constraint (SELECT id FROM skills WHERE name = ${template.name}). This can leak or conflict with another tenant's rows.
  - _Why false positive:_ Schema note says skills has no tenant_id column, so this is not a tenant-scoped table lookup.
- **server/skills-marketplace.ts:235** _(global-table)_ — SELECT from tenant-scoped table 'skills' without tenant_id constraint (SELECT * FROM skills WHERE id = ${skillId}). This can return another tenant's skill for export (cross-tenant disclosure).
  - _Why false positive:_ Schema note says skills has no tenant_id column; export reads from a global table, not a tenant-scoped one.
- **server/tool-learning.ts:129** _(upstream-verified)_ — SELECT on tenant-scoped table 'custom_tools' omits tenant_id filter when checking for existing name collisions. The query checks only `WHERE name = parsed.name` and can leak cross-tenant state or incorrectly block creation.
  - _Why false positive:_ Name is transformed with tenantToolName(parsed.name, tenantId) just above, making the lookup tenant-derived despite no separate tenant_id predicate.
- **server/tool-learning.ts:155** _(internal-worker-pk)_ — SELECT of final tool row uses only `WHERE id = ${inserted.id}` with no tenant_id constraint. Reads against tenant-scoped custom_tools must be tenant-scoped.
  - _Why false positive:_ Read-back uses server-generated inserted.id from the immediately preceding insert in the same flow, not an untrusted cross-tenant request id.
- **server/tools.ts:4546** _(fk-transitive)_ — INSERT into tenant-scoped table `project_notes` does not include tenant_id in the VALUES list. The code inserts project-scoped data without explicitly setting tenant_id even though row-level security/foreign-key enforcement is not relied on.
  - _Why false positive:_ project_notes has no tenant_id and is transitively scoped through tenant-checked project_id after assertProjectInTenant
- **server/tools.ts:4550** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The handler writes a project-scoped file row (project_id provided) but does not include tenant_id in VALUES.
  - _Why false positive:_ project_files has no tenant_id and insert is scoped via validated project_id after assertProjectInTenant
- **server/tools.ts:6464** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` (from generate_audio) does not set tenant_id. The code inserts a file record for params.project_id without explicitly saving the caller's tenant_id, relying only on project_id and a prior assertProjectInTenant check.
  - _Why false positive:_ project_files has no tenant_id; insert uses project_id only after assertProjectInTenant confirms tenant ownership
- **server/tools.ts:7006** _(fk-transitive)_ — INSERT into tenant-scoped table `project_files` omits tenant_id. The INSERT uses project_id after asserting project ownership, but the project_files row itself is not being written with tenant_id (columns are NOT NULL with no default per invariant).
  - _Why false positive:_ project_files has no tenant_id and row is isolated through tenant-owned project_id verified by assertProjectInTenant
- **server/tools/domains/browser/handlers.ts:165** _(upstream-verified)_ — UPDATE against tenant-scoped table `browser_workflows` lacks a tenant constraint in the WHERE clause: `UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}`. Under the project's invariant, every UPDATE must be constrained to the caller's tenant to prevent cross-tenant writes.
  - _Why false positive:_ workflow row was selected immediately above with tenant_id = tenantId, and update uses that fetched row's id in same call path
- **server/tools/domains/knowledge/handlers.ts:91** _(upstream-verified)_ — UPDATE against tenant-scoped table `knowledge_triples` without tenant constraint: `UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id}`
  - _Why false positive:_ rows being updated were selected just above from knowledge_triples with tenant_id = tenantId in the same loop
- **server/tools/domains/files/handlers.ts:934** _(fk-transitive)_ — INSERT into `project_files` does not set tenant identity: `INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (...)`
  - _Why false positive:_ project_files has no tenant_id column and is linked via project_id to a tenant-scoped projects row already validated by tenant-filtered join
- **server/tools/domains/files/handlers.ts:937** _(upstream-verified)_ — UPDATE on tenant-scoped `projects` table without tenant constraint: `UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId}`
  - _Why false positive:_ projId comes from prior SELECT joining conversations to projects with p.tenant_id = ctx.tenantId in the same call path
- **server/tools/domains/strategic-memory/handlers.ts:98** _(upstream-verified)_ — UPDATE memory_entries uses WHERE id = ${id} with no tenant_id constraint. This allows updating rows across tenants if ids collide or are guessed, violating tenant isolation which relies on application-level filters.
  - _Why false positive:_ id being updated was selected immediately above from memory_entries with tenant_id = tid and matching persona/category filters
- **server/tools/domains/strategic-memory/handlers.ts:159** _(upstream-verified)_ — UPDATE memory_entries sets last_accessed/access_count WHERE id = ANY(${ids}) with no tenant_id constraint. This can update rows belonging to other tenants if ids span tenants.
  - _Why false positive:_ ids array is derived solely from rows just selected with tenant_id = tid before the UPDATE in the same function
- **server/tools/domains/strategic-memory/handlers.ts:258** _(upstream-verified)_ — UPDATE memory_entries uses WHERE id = ${id} with no tenant_id constraint (in recordStrategicWin). Missing tenant scoping can allow cross-tenant updates.
  - _Why false positive:_ id comes from an immediately preceding SELECT on memory_entries constrained by tenant_id = tid
- **server/twilio.ts:116** _(upstream-verified)_ — SELECT from tenant-scoped table `conversations` by id is executed without a tenant_id constraint. Reading conversations without constraining tenant_id violates the tenant-isolation invariant and may expose or allow routing to conversations owned by other tenants.
  - _Why false positive:_ this read is only a verification step on a specific conversation id from each candidate pairing and the function fail-closes on tenant mismatch/ambiguity before returning any routing decision
- **server/commitment-drafter.ts:234** _(upstream-verified)_ — UPDATE against tenant-scoped table 'commitments' is executed without constraining by tenant_id (WHERE id = ${row.id} only). This allows modifying a row across tenants if the row.id comes from another tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): row.id comes from claimedRows produced by the claim UPDATE that is tenant-scoped (commitment-drafter.ts:204: WHERE c.tenant_id = ${tenantId}). The line-234 UPDATE is a fail-closed unclaim (reverts only housekeeping fields draft_status/drafted_at) for a row already proven to belong to the iterated tenant.
- **server/doc-collections.ts:166** _(upstream-verified)_ — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} — missing tenant_id filter. This DELETE can remove another tenant's chunks if IDs/paths collide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/doc-collections.ts:160-167 addDocument() verifies collection ownership (SELECT id FROM doc_collections WHERE id=collectionId AND tenant_id=tenantId, returns error if not found) immediately before the DELETE scoped by collection_id.
- **server/embeddings.ts:809** _(system-sweep)_ — storeEmbeddingVec fallback branch updates a tenant-scoped table without tenant constraint: `UPDATE ${sql.raw(table)} ... WHERE id = ${id}` (no tenant_id). This allows modifying rows belonging to any tenant when options.tenantId is omitted.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): storeEmbeddingVec (server/embeddings.ts:678-685) only updates the embedding_vec/embedding housekeeping columns; callers pass server-generated ids (research-engine.ts:508,1046 insertedId/knowledgeId; agent-knowledge-refresh.ts:562 maintenance scan row.id; backfill loops). Never writes tenant-facing content.
- **server/job-queue.ts:335** _(internal-worker-pk)_ — cancelJob performs an UPDATE on agent_jobs by id/status without any tenant_id constraint, allowing cancelling jobs across tenants by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): completeJob (server/job-queue.ts:172-187) is called only from job-worker.ts:78 with job.id/job.attempts obtained from claimDueJobs (server-generated PK); attempts fence guards it. No caller passes a request/tool-arg id.
- **server/process-governor.ts:1235** _(upstream-verified)_ — UPDATE on `governance_rules` (setting last_triggered_at/trigger_count) lacks a tenant_id constraint (WHERE id = ${rule.id}). This can increment trigger counts for rules belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): process-governor.ts:977 UPDATE governance_rules WHERE id; rule id loaded via loadRules tenant-scoped SELECT (process-governor.ts:57 WHERE tenant_id); runs per-tenant
- **server/project-brain.ts:135** _(system-sweep)_ — SELECT from `projects` by id (SELECT name, status, description FROM projects WHERE id = ${projectId}) has no tenant_id constraint. The function accepts projectId and may read another tenant's project.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:135 SELECT projects WHERE id inside updateProjectBrain; callers are routes.ts:5513 (conv ownership verified at routes.ts:3563-3566) and backfillProjectBrains platform sweep (routes.ts:825); output is a per-project on-disk brain file keyed by projectId, never a mixed tenant-facing response
- **server/project-brain.ts:332** _(system-sweep)_ — Backfill query selects projects and joins conversations without any tenant filtering (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ...). This can read all tenants' data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:328-337 backfillProjectBrains is a platform-wide sweep across all projects (only caller routes.ts:825); writes per-project files, no tenant-facing mixed output
- **server/project-brain.ts:349** _(system-sweep)_ — Querying conversations by project_id (SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}) lacks tenant scoping. project_id alone is insufficient to ensure the conversation belongs to the expected tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:349 SELECT conversations WHERE project_id inside backfillProjectBrains sweep loop (project-brain.ts:345-354); per-project file generation only
- **server/project-brain.ts:359** _(system-sweep)_ — Selecting messages by conversation_id (SELECT role, content FROM messages WHERE conversation_id = ${conv.id}) has no tenant constraint. This can expose messages from other tenants when conversation ids are global or not validated.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): project-brain.ts:359 SELECT messages WHERE conversation_id inside backfillProjectBrains sweep (project-brain.ts:358-364); iterates rows produced by the sweep, writes per-project brain file
- **server/sculptor.ts:168** _(internal-worker-pk)_ — UPDATE to sculptor_sessions performed in monitorSession lacks a tenant_id filter (UPDATE sculptor_sessions SET ... WHERE id = ${sessionId}). monitorSession accepts tenantId but does not use it in this UPDATE, enabling cross-tenant modification.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:106 monitorSession is called only with session.id freshly created at 74-82 (server-generated PK); no caller passes a request-derived id
- **server/sculptor.ts:359** _(upstream-verified)_ — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} omits tenant constraint. This write could overwrite another tenant's session review_result.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/sculptor.ts:283-287 reviewSessionWork SELECTs the session WHERE id=sessionId AND tenant_id=tenantId and returns 'Session not found' otherwise, proving ownership before the UPDATE at 347
- **server/self-improvement.ts:427** _(internal-worker-pk)_ — UPDATE on tenant-scoped table "experiments" is executed without constraining by tenant_id (.where(eq(experiments.id, experiment.id)) only). This allows an experiment row from another tenant to be modified.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/self-improvement.ts:401 — experiment.id is a server-generated PK from db.insert(experiments)...returning() at self-improvement.ts:339 within the same runExperiment call, using config.tenantId. The UPDATE at 401 writes back the row this run just created; id is never attacker-supplied. Internal server-generated PK.
- **server/storage.ts:1402** _(upstream-verified)_ — getAllDataForExport() performs many SELECTs across tenant-scoped tables (conversations, messages, memoryEntries, agentKnowledge, etc.) with no tenant filtering, returning platform-wide tenant data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getAllDataForExport callers routes.ts:6423 and routes.ts:6450 are gated by requireAdmin, which is requirePlatformAdmin (routes.ts:5610) requiring tenantId===ADMIN_TENANT_ID + admin session (auth.ts:460-466). Intentional admin-only cross-tenant export.
- **server/venture-discovery/budget.ts:155** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger uses only WHERE id = ${reservationId} with NO tenant_id constraint — this allows modifying reservation rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/venture-discovery/budget.ts:169** _(internal-worker-pk)_ — UPDATE against tenant-scoped table agent_cost_ledger uses only WHERE id = ${reservationId} with NO tenant_id constraint — this allows releasing (setting cost → 0) reservation rows for other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): settleVentureReservation is only called by the internal venture-discovery loop with reservation.reservationId returned by the server-side reserve() (server/venture-discovery/loop.ts:212,341; reservationId minted at server/venture-discovery/budget.ts:129); no request/tool-arg id.
- **server/agentic-features.ts:861** _(upstream-verified)_ — enrollInSequence checks for existing enrollments using sequence_id and contact_email without scoping by tenant (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'). This can leak/affect enrollments across tenants and allow cross-tenant conflicts.
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 (pass 2) 2026-08-01): the dedupe SELECT is scoped to sequence_id, and the sequence was tenant-verified (WHERE id AND tenant_id) one statement above; enrollment rows only exist under that sequence.
- **server/chat-engine.ts:4146** _(internal-worker-pk)_ — recordToolCallForStuckDetection(conversationId, toolName, parsedArgs, round) (and the subsequent postDiagnosticReport call) are invoked without any tenant identifier. If recordToolCallForStuckDetection/postDiagnosticReport persist diagnostics to tenant-scoped tables, those operations will lack a tenant_id constraint (or may insert rows missing tenant_id).
  - _Why false positive:_ allowlist (task-118 audit-recovery campaign 2026-08-01 2026-08-01): recordToolCallForStuckDetection is in-memory loop diagnostics keyed by conversationId; no DB write occurs.
- **server/data-protection.ts:403** _(upstream-verified)_ — SELECT COUNT(*) FROM `messages` with only conversation_id (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) and no tenant_id predicate. This can act as a cross-tenant existence/oracle and leak information about other tenants' conversations.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): COUNT(*) on messages uses conv.id from a tenant-scoped conversation set fetched at server/data-protection.ts:362 within backupTenantDataToDrive.
- **server/dfy-intake.ts:52** _(signed-url)_ — Select from tenant-scoped table dfy_intake_forms by token without constraining by tenant_id (no WHERE tenant_id = ...). This lets the token lookup return rows across tenants.
  - _Why false positive:_ allowlist (independent completed-audit path review 2026-09-03 2026-09-03): The public GET/POST routes at server/routes/audit.ts:1051-1085 deliberately use the token as the sole bearer credential. createDfyIntakeForm generates a 24-byte crypto.randomBytes token (48 hex characters, 192 bits) and shared/schema.ts:3424 makes it globally unique. submitDfyIntake's follow-on UPDATE additionally requires both the server-returned form.id and form.tenantId (dfy-intake.ts:96-99), so the capability cannot r
- **server/health-audit.ts:314** _(system-sweep)_ — SELECT from code_proposals is not tenant-scoped — query lacks a WHERE tenant_id = ... constraint. This reads pending/proposed/unverified proposals across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): archiveStaleProposals is a platform TTL maintenance sweep; it selects code_proposals older than PROPOSAL_TTL_DAYS and (apply) updates status='archived_stale' by id set (server/health-audit.ts:314-338) — housekeeping-only status change, not a tenant-facing surface.
- **server/health-audit.ts:346** _(system-sweep)_ — SELECT from heartbeat_tasks does not filter by tenant_id. heartbeat_tasks appears to be schedule/task data which is likely tenant-scoped; this query will surface tasks for all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Same TTL sweep flags stuck heartbeat_tasks and disables them (enabled=false) by id set (server/health-audit.ts:346-369); housekeeping mutation of a maintenance loop.
- **server/heartbeat.ts:2679** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause after creating a tenant-scoped task. The update therefore isn't constrained to the task's tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/heartbeat.ts:3151** _(internal-worker-pk)_ — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} is executed without tenant_id in the WHERE clause (second occurrence). This mutates a tenant-scoped row without constraining by tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): Actual UPDATE at server/heartbeat.ts:2922 sets approval_status by newTask.id where newTask was just created via storage.createHeartbeatTask({...tenantId}) at server/heartbeat.ts:2909-2919 — server-generated PK stamped with the correct tenant.
- **server/job-queue.ts:281** _(internal-worker-pk)_ — failJob first SELECT (SELECT max_attempts FROM agent_jobs WHERE id = ${id}) reads agent_jobs by id without constraining tenant_id. This leaks the ability to fetch another tenant's job metadata by id.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): failJob (server/job-queue.ts:211-268) reads max_attempts by the worker-owned job.id; sole callers are job-worker.ts:62 and :82 which pass job.id from claimDueJobs (server-generated PK).
- **server/job-queue.ts:406** _(system-sweep)_ — listJobs constructs and executes a SELECT FROM agent_jobs with a dynamically-built whereClause that may omit tenant_id (if caller doesn't pass tenantId). This allows unscoped listing of agent_jobs if callers omit tenantId.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/job-queue.ts:93-118 claimDueJobs is the worker's platform-wide claim loop (called only from job-worker.ts:106); it flips housekeeping status/lease fields on due rows and each claimed row carries its own tenant_id (returned + dispatched by kind internally). No untrusted id in the WHERE.
- **server/storage.ts:584** _(system-sweep)_ — getAllMemoriesForBackup() selects all memory_entries without any tenant_id constraint, returning tenant-scoped rows platform-wide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/storage.ts:959** _(system-sweep)_ — getMemoriesWithoutEmbeddings() selects from memory_entries without any tenant filter; this reads tenant-scoped rows across all tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getMemoriesWithoutEmbeddings only caller is the background embedding backfill heartbeat.ts:1912; rows feed embedding generation (updateMemoryEmbedding) and are never returned to a tenant-facing surface.
- **server/storage.ts:969** _(system-sweep)_ — getKnowledgeWithoutEmbeddings() selects agent_knowledge rows with no tenant constraint, returning tenant-scoped data platform-wide.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): getKnowledgeWithoutEmbeddings only caller is heartbeat.ts:1913 background embedding worker; rows only drive updateKnowledgeEmbedding, not returned to any tenant surface.
- **server/storage.ts:936** _(internal-worker-pk)_ — updateMemoryEmbedding(id, embedding) updates memory_entries.embedding WHERE id = <id> with no tenant predicate — allows modifying a memory entry by id across tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): updateMemoryEmbedding callers are the background embedding worker heartbeat.ts:1919 and heartbeat.ts:2751 using m.id from getMemoriesWithoutEmbeddings (server rows); only the embedding column is written. No request-derived id.
- **server/storage.ts:587** _(system-sweep)_ — getMemoryEntry(id, tenantId?) allows caller to omit tenantId (silently warning) and will perform a SELECT on memory_entries by id without a tenant predicate when tenantId is undefined, leaking tenant-scoped data.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-31 (8 parallel read-only subagents + main-agent review) 2026-07-31): server/storage.ts:510 — memory_entries.tenant_id is NOT NULL (shared/schema.ts:153-160), but the unfiltered method is used by server/backup.ts:114-122 for the platform-wide automated memory snapshot, not a tenant-facing response; its output is uploaded as an automated backup at server/backup.ts:124 onward.
- **server/surprise-scorer.ts:93** _(upstream-verified)_ — UPDATE on tenant-scoped table 'felix_proposals' omits tenant_id constraint (stampArgsEmbedding). The query updates args_embedding WHERE id = ${proposalId} but does not ensure the row belongs to the expected tenant.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25b (8 parallel read-only subagents + main-agent review) 2026-07-25): server/surprise-scorer.ts:93 — stampArgsEmbedding() UPDATEs felix_proposals by primary-key id. Both callers use an already-tenant-validated PK: scoreProposalSurprise() calls it only after SELECT ... WHERE id=proposalId AND tenant_id=tenantId (line 112), and felix-loop.ts:646 passes newProposalId — a proposal the loop just created in its own tenant context. proposalId is not attacker-influence
- **server/tool-learning.ts:291** _(internal-worker-pk)_ — UPDATE on tenant-scoped table 'custom_tools' performed with `WHERE id = ${tool.id}` only (auto-fix path), missing tenant_id constraint. This updates an implementation without verifying tenant ownership.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tool-learning.ts:291-293 UPDATE by tool.id inside tryAndRevert, called only with the freshly inserted row from createCustomTool (server/tool-learning.ts:134-146).
- **server/tool-learning.ts:316** _(internal-worker-pk)_ — DELETE on tenant-scoped table 'custom_tools' performed with `WHERE id = ${tool.id}` only (revertToolRow). The deletion is not constrained by tenant_id and could affect another tenant's row with the same id in some scenarios.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): revertToolRow (server/tool-learning.ts:315-319) is only called from tryAndRevert, which is only called by createCustomTool with the just-inserted row (server/tool-learning.ts:134-146). tool.id is the server-generated PK of a row inserted with tenantId at server/tool-learning.ts:141.
- **server/tools.ts:8977** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) and lacks a tenant_id constraint. This allows a caller with a row id to affect rows belonging to other tenants.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9007** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) and lacks a tenant_id constraint (security path after managed-skill-safety rejection).
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9021** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) when marking a proposal 'accepted' and adding promotedSkillId; tenant filter is missing.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/tools.ts:9038** _(internal-worker-pk)_ — UPDATE against tenant-scoped table `proposed_skills` uses only the row id in WHERE (where(eq(proposedSkills.id, row.id))) when marking a proposal 'rejected'; tenant constraint is missing.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): server/tools.ts:8822-8831 row.id is the server-generated PK returned by db.insert(proposedSkills).values({tenantId: tid,...}).returning() in this same call path; row.id is never a caller-supplied id
- **server/twilio.ts:102** _(upstream-verified)_ — SELECT from tenant-scoped table `agent_knowledge` is performed without any tenant_id constraint (loads all candidate pairing rows across tenants). This violates the tenant-isolation invariant and can lead to cross-tenant pairing/lookup/metadata leakage or hijack.
  - _Why false positive:_ allowlist (triage-campaign-2026-07-25 (8 parallel read-only subagents + main-agent review) 2026-07-25): twilio.ts:102 phone->tenant resolution scan of agent_knowledge; each candidate ownership is verified in the same call path (twilio.ts:116-120 asserts conversation.tenant_id === pairing.tenantId) and fails closed on ambiguous multi-tenant pairings (twilio.ts:129-133); returns null when unverified
